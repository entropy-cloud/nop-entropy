<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>@groupId@</groupId>
    <artifactId>@artifactId@</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <nop-stream.version>@nop-stream.version@</nop-stream.version>
    </properties>

    <dependencies>
        <!-- XDSL 声明式编排入口（传递 nop-stream-core / nop-stream-cep / nop-xdefs） -->
        <dependency>
            <groupId>io.github.entropy-cloud</groupId>
            <artifactId>nop-stream-flow</artifactId>
            <version>${nop-stream.version}</version>
        </dependency>
        <!-- 窗口算子执行（IWindowOperatorFactory）与 checkpoint 执行器 -->
        <dependency>
            <groupId>io.github.entropy-cloud</groupId>
            <artifactId>nop-stream-runtime</artifactId>
            <version>${nop-stream.version}</version>
        </dependency>
        <!-- .stream.xml 解析（DslModelParser）与 VFS 初始化（传递 nop-core） -->
        <dependency>
            <groupId>io.github.entropy-cloud</groupId>
            <artifactId>nop-xlang</artifactId>
            <version>${nop-stream.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>6.0.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.2</version>
                <configuration>
                    <!-- 拓扑失控（如 supervision 循环重启）时显式超时失败，而非无限挂起 -->
                    <forkedProcessTimeoutInSeconds>300</forkedProcessTimeoutInSeconds>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
