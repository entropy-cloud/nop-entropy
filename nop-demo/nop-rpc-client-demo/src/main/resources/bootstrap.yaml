nop.cluster.discovery.nacos.enabled: true
nop.cluster.discovery.nacos.server-addr: localhost:8848

nop:
  application:
    name: rpc-demo-consumer
  datasource:
    disabled: true

quarkus:
  application:
    name: ${nop.application.name}