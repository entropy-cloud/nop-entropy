nop:
  debug: true
  datasource:
    driver-class-name: org.h2.Driver
    jdbc-url: jdbc:h2:mem:test
    username: sa
    password:

  orm:
    init-database-schema: true

  credential:
    # 测试用主密钥（web 测试仅需 bean 初始化通过，不实际加密）
    master-keys:
      - "keyA:test-passphrase-for-keyA-autotest"
    active-key-id: keyA
