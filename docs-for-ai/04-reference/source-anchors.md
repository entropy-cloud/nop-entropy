# 实现锚点

本页的作用不是让开发 AI 去大范围读源码，而是给 `docs-for-ai/` 中的关键规则提供最小实现锚点。

正常开发时，走到本页通常已经是最后一步：

1. 先读完相关 guide / runbook。
2. 如果还需要确认定义，优先对这里列出的类、接口、方法做 LSP / definition lookup。
3. 不要因为这里给了路径，就默认转成大范围源码阅读。

## 核心锚点表

| 规则 ID | 锚点 | 说明 |
|---------|------|------|
| `GEN-001` | `/nop/templates/orm` | ORM 模型是项目骨架和多模块生成的起点 |
| `GEN-002` | `*-codegen/postcompile/gen-orm.xgen` | `*-codegen` 负责从源模型驱动项目级生成 |
| `GEN-003` | `*-meta/precompile/gen-meta.xgen` | `*-meta` 负责生成 XMeta |
| `GEN-004` | `*-meta/postcompile/gen-i18n.xgen` | `*-meta` 负责生成 i18n |
| `GEN-005` | `*-web/precompile/gen-page.xgen` | `*-web` 基于 XMeta 生成页面文件 |
| `GEN-006` | `nop-kernel/nop-codegen/src/main/resources/_vfs/nop/templates/meta/src/main/resources/_vfs/{moduleId}/model/_module-meta.json.xgen` + `.../module-meta.json.xgen` | `*-meta` 负责生成模块级 `module-meta.json`，为 web 层暴露稳定的模块元数据边界 |
| `GEN-007` | `nop-kernel/nop-codegen/src/main/resources/_vfs/nop/templates/orm-web/@init.xrun` + `nop-kernel/nop-codegen/src/main/resources/_vfs/nop/templates/orm-web/src/main/resources/_vfs/{moduleId}/auth/_{moduleName}.action-auth.xml.xgen` + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/functions/GlobalFunctions.java` (`loadDeltaJson`) | ORM web 模板通过 `loadDeltaJson("/{moduleId}/model/module-meta.json")` 读取模块级 meta，TOPM icon 取 `moduleMeta.icon` |
| `GEN-010` | `nop-kernel/nop-xlang-java/src/main/java/io/nop/xlang/java/gen/task/XlangJavaGenTask.java` + `nop-kernel/nop-xlang-java/src/main/java/io/nop/xlang/java/gen/GeneratedManifestFiles.java` + `nop-kernel/nop-xlang-java-e2e/pom.xml` | java 生成类后端构建任务与双清单装载/漏跑判别锚点；`nop-xlang-java-e2e` 为模块接入 exec-maven-plugin 增量 execution 的全真链路参考实现（见 `docs-for-ai/02-core-guides/xlang-and-xpl-basics.md` 「构建集成与新模块接入」） |
| `GEN-008` | 维护脚本 | 仓库内固定检查 source `model/*.orm.xml` 的 root/entity icon，以及 source `*.action-auth.xml` 中显式 `TOPM` / `SUBM` 资源的 icon；该脚本属于维护层，不作为普通开发 AI 的源码阅读入口 |
| `GEN-009` | `pom.xml` (`exec-maven-plugin` 的 `precompile` / `precompile2` / `postcompile` executions) | 根 POM 定义 codegen 任务绑定的 Maven phase、classpath 可见性与 `CodeGenTask` 入口；排查“为什么没执行/为什么读不到资源”时先校准这里 |
| `EXT-001` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/` + `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/xdsl.xdef` | 平台所有 DSL 的元模型定义入口；理解“先有 XDef 再有 XDSL”时从这里开始 |
| `EXT-002` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdsl/XDslExtender.java` | `x:extends`、`x:gen-extends`、`x:post-extends`、`x:override` 的核心展开与合并执行链 |
| `EXT-003` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/store/DeltaResourceStore.java` + `DeltaResourceStoreBuilder.java` | Delta/VFS 分层资源解析的核心实现；`super:`、tenant 层、delta 层都从这里落地 |
| `EXT-004` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdsl/DslModelParser.java` + `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/util/INeedInit.java` | XDSL 模型到运行时对象的统一解析与初始化链 |
| `EXT-005` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/functions/GlobalFunctions.java` (`loadDeltaJson`) | JSON/YAML 类模型同样可复用 Delta 机制，不局限于 XML DSL |
| `EXT-012` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdsl/XDslExtender.java` (`:115-125` `xtend` 末尾 `setAttr(OVERRIDE, REPLACE)` + `:408-436` `extendsSub` 递归) | 子节点 `x:extends` 的隐式 `replace` 语义（XDSL 链路）：子节点一旦自己 resolve 过 `x:extends`，立即被标记为 `x:override="replace"`，父继承方向在该子树上不再合并。详见 `docs-for-ai/02-core-guides/xdef-and-xdsl.md` 「子节点 `x:extends` 的隐式 `replace` 语义」 |
| `EXT-013` | `nop-kernel/nop-core/src/main/java/io/nop/core/lang/json/delta/DeltaJsonLoader.java` + `nop-kernel/nop-core/src/main/java/io/nop/core/lang/json/delta/JsonExtender.java` (`:60-93` `xtendMap`，`:88-92` 自动 replace) + `JsonMerger.java` + `DeltaJsonSaver.java` | 无 xdef 的并行 JSON 合并链路：在原生 `Map<String,Object>` 上直接执行 `x:extends` / `x:override`，不经过 `XNode`，也不需要 schema。与 `XDslExtender` 对"子节点 `x:extends` → 自动 replace"语义的实现一致。适用于 i18n.yaml、amis 页面 JSON 等"无 schema 差量"。详见 `docs-for-ai/02-core-guides/xdef-and-xdsl.md` 「无 xdef 的并行 JSON 合并链路」 |
| `EXT-006` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xpl/xlib/XplLibTagCompiler.java` (`isAllowedUnknownAttr`) | 编译器对带名字空间的属性默认放宽校验；除显式 `checkNs` 外，这些属性可作为扩展属性参与编译期元编程 |
| `EXT-007` | `nop-kernel/nop-xlang/src/main/resources/_vfs/nop/core/xlib/meta-gen.xlib` + `meta-prop.xlib` + `nop-frontend-support/nop-web/src/main/resources/_vfs/nop/web/xlib/control.xlib` | 扩展属性驱动的编译期元编程真实案例：读取节点上的名字空间属性并展开 meta / control 等标准结构 |
| `EXT-008` | `nop-frontend-support/nop-web/src/main/resources/_vfs/nop/web/xlib/flux-control.xlib` | Flux 控件映射库：78 个 `{mode}-{type}` 标签（含 prototype 别名），替代 control.xlib 输出 Flux JSON 控件。详见 `docs-for-ai/02-core-guides/flux-rendering.md` |
| `EXT-009` | `nop-frontend-support/nop-web/src/main/resources/_vfs/nop/web/xlib/flux-web.xlib` + `flux-web/` 目录 + `nop-frontend-support/nop-web/src/main/java/io/nop/web/page/WebPageHelper.java` (`applyViewOverride`/`internalLoadPage`/`toFluxPagePath`) | Flux 页面生成库：28 个标签（含共享容器分派 `GenContainerModel`），替代 web.xlib 输出 Flux 页面 JSON；完整支持 `layout-syntax-reference.md` 中所有 layout DSL 语法（`*` 必填、`@` 只读、`!` 隐藏标签、跨列、分组、Tab、查询运算符）；`NormalizeAction` 实现 onClick 优先透传 + AMIS actionType 到 Flux ActionSchema 转换；页面级容器 `tabs`/`wizard`/`group`/`complex` 输出 Flux `TabsSchema`/`WizardSchema`/`GridSchema`/`PageSchema`（`items`/`steps` 字段、`body` 数组直接书写、tab/step 内容优先级 `page` > `body` > `name`；`complex` 的 `header`/`footer`/`aside`/`body` 四槽位直接映射 `PageSchema` 四区域）；`embed` 页面类型（`page_embed.xpl`）按 `path` 加载外部 view.xml 的 page/grid 或 page.yaml，经 `applyViewOverride`（JsonMerger delta 合并）应用 `<override>`；cell 级 `<view>` 的 `<override>` 由两端 `GenDispView`（flux-web.xlib + web.xlib）同样经 `applyViewOverride` 消费；Flux 已确认不支持的 form/page 属性见 `docs-for-ai/02-core-guides/flux-rendering.md`。 |
| `EXT-010` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdsl/json/DslModelToXNodeTransformer.java` (`transformUnion` / `getSubSchema`) + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xmeta/validate/SchemaBasedValidator.java` | union schema 的 subtype 路由与运行时校验锚点：先按 `typeValue` 精确匹配，再允许 `typeValue="*"` fallback；缺失或未知 subtype 需显式报错 |
| `EXT-011` | `nop-frontend-support/nop-web/src/main/resources/_vfs/nop/core/registry/page.register-model.xml` + `nop-frontend-support/nop-web/src/main/java/io/nop/web/page/PageModelLoaderFactory.java` (`PageModelLoader.loadObjectFromPath`) + `nop-frontend-support/nop-web/src/main/resources/_vfs/nop/web/xlib/flux-web.xlib` (`LoadPage` 标签) + `nop-frontend-support/nop-web/src/main/java/io/nop/web/WebConfigs.java` (`CFG_WEB_RENDER_MODE`) | flux.yaml 页面覆盖机制：`flux.yaml` 注册为 `xpage` 模型文件类型；Flux 模式（`nop.web.render-mode=flux`）下，加载 `*.page.yaml` 时顶层入口（`PageModelLoader`）与子页面入口（`flux-web.xlib:LoadPage`）均优先检测同目录 `*.flux.yaml`，命中则改加载它；复合扩展名由 `StringHelper.fileType` 识别（`main.flux.yaml` → `flux.yaml`）。详见 `docs-for-ai/02-core-guides/flux-rendering.md` |
| `XDEF-001` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdef/parse/XDefinitionParser.java` (`:333-389` 读取 + `:452-471` 默认值推断) + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdsl/json/DslBeanModelParser.java` (`:130-152` 按 bean-\* 映射填充) + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdef/impl/XDefToObjMeta.java` (`:230-243,694-737`) + `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/ApiConstants.java` (`TREE_BEAN_PROP_TYPE="$type"` / `TREE_BEAN_PROP_BODY="$body"`) | `xdef:bean-*` 属性族（bean-body-prop/bean-value-prop/bean-tag-prop/bean-child-name 等）控制 XDSL 节点→Java Bean 的属性映射；`$type`/`$body` 是 TreeBean 通用树结构的特殊属性约定，JSON↔XML 转换器据此区分标签名/body 与普通字段 |
| `XPT-001` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xpath/operator/XPathOperatorRegistry.java` + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xpath/selector/XPathSelector.java` + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xpath/parse/XPathSelectorParser.java` | xpath selector/operator 注册与解析：8 个 `$xxx` operator 全部注册于 registry，parser 手写递归下降（联合选择 `\|`、谓词 `[expr]`、`..` 边界抛错）。详见 `docs-for-ai/02-core-guides/xpath-and-xtransform.md` |
| `XPT-002` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xt/core/XtTransformCompiler.java` + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xt/core/XtTransformContext.java` + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xt/core/XtTransform.java` | xt 编译/上下文/执行入口：`compileAll` 管线（import 合并 → inherits 合并 → 编译期 ID 检查），context 注入 6 个内置变量并共享循环引用 visited 状态 |
| `XPT-003` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xt/rules/ApplyTemplateRule.java` + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xt/rules/ApplyMappingRule.java` + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xt/rules/ChooseRule.java` | xt 核心规则 dispatch：apply-template/apply-mapping 的前缀 id 解析、循环引用检测与 mapping match/default 分发 |
| `BIZ-001` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/biz/ICrudBiz.java` | 标准 CRUD 业务接口契约 |
| `BIZ-002` | `nop-service-framework/nop-biz/src/main/java/io/nop/biz/crud/CrudBizModel.java` | 实体型服务默认基类 |
| `BIZ-003` | `CrudBizModel#requireEntity` | 普通 BizModel 获取实体的安全路径 |
| `BIZ-004` | `CrudBizModel#doFindList` / `doFindPage` | 普通查询应优先走的安全 API |
| `BIZ-007` | `CrudBizModel#prepareFindPageQuery` / `appendOrderByPk` | 查询预处理会追加数据权限、默认 filter / orderBy，并在需要时补主键排序 |
| `BIZ-005` | `nop-job/nop-job-dao/src/main/java/io/nop/job/biz/INopJobScheduleBiz.java` + `nop-job/nop-job-service/src/main/java/io/nop/job/service/entity/NopJobScheduleBizModel.java` | 跨 BizModel 协作通常通过 `I*Biz` 接口 |
| `BIZ-006` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/annotations/biz/BizLoader.java` + `nop-demo/nop-delta-demo/src/main/java/io/nop/demo/biz/LoginApiBizModelDelta.java` | 扩展 GraphQL 返回字段优先 `@BizLoader`；新增字段时可配合 `autoCreateField=true` 和 `@LazyLoad` |
| `DDD-001` | `nop-service-framework/nop-biz-auth-api/src/main/java/io/nop/auth/api/messages/LoginRequest.java` + `LoginResult.java` | 仓库内真实存在 `@DataBean + ExtensibleBean + @PropMeta + Jackson 注解` 的 message bean 模式 |
| `DDD-002` | `nop-kernel/nop-core/src/main/java/io/nop/core/lang/json/JsonTool.java` | JSON / YAML 默认统一入口应校准到 `JsonTool` |
| `DDD-003` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/convert/ConvertHelper.java` | 类型转换默认统一入口应校准到 `ConvertHelper` |
| `DDD-004` | `nop-kernel/nop-commons/src/main/java/io/nop/commons/util/StringHelper.java` | 字符串 helper 默认应优先校准到 `StringHelper` |
| `DDD-005` | `nop-kernel/nop-commons/src/main/java/io/nop/commons/util/DateHelper.java` | 日期 helper 默认应优先校准到 `DateHelper` |
| `DDD-006` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/time/CoreMetrics.java` | 获取当前时间一律使用 `CoreMetrics`，禁止 `System.currentTimeMillis()`/`System.nanoTime()`/`LocalDateTime.now()`/`LocalDate.now()`/`new Date()`/`new Timestamp(...)`。按返回类型选 `currentTimeMillis`/`currentTimestamp`/`currentDateTime`/`currentDate`/`nanoTime` |
| `MODEL-INIT-001` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/util/INeedInit.java` + `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdsl/DslModelParser.java` | `INeedInit` 是模型加载后的延迟初始化约定；标准 XDSL 解析链会在反序列化后自动调用 `init()`，除非显式 `disableInit(true)` |
| `MODEL-INIT-002` | `nop-network/nop-rpc/nop-rpc-model/src/main/java/io/nop/rpc/model/ApiModel.java` + `ApiServiceModel.java` + `ApiMethodModel.java` | 典型上下文回填模式：根模型级联 `init()` 子模型，为子节点补父引用和派生信息 |
| `MODEL-INIT-003` | `nop-utils/nop-fsm/src/main/java/io/nop/fsm/model/StateMachineModel.java` + `nop-rule/nop-rule-core/src/main/java/io/nop/rule/core/model/RuleModel.java` | 典型运行期索引/映射构建模式：在 `init()` 中把 DSL 列表整理成高频访问所需的 map / 派生结构 |
| `MODEL-INIT-004` | `nop-persistence/nop-orm-model/src/main/java/io/nop/orm/model/loader/OrmModelLoader.java` | 手工 merge / 组装模型后，通常需要显式 `model.init()`，然后再进入 `freeze(true)` 等后续阶段 |
| `MAP-001` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/record/record-mapping.xdef` + `record-mappings.xdef` | Record Mapping DSL 权威结构定义：`field` / `patternField`、`when`、`mandatory`、`schema`、`mapping`、`itemMapping`、`varName` 等都以这里为准 |
| `MAP-002` | `nop-kernel/nop-record-mapping/src/main/java/io/nop/record_mapping/impl/RecordMappingTool.java` | Record Mapping 执行顺序锚点：`when` 判断、取值、`valueExpr`、类型转换、`schema` 校验、`mandatory` 校验、`ignoreWhenEmpty`、上下文变量写入等核心语义都落在这里 |
| `MAP-003` | `nop-kernel/nop-record-mapping/src/main/java/io/nop/record_mapping/impl/ModelBasedRecordMapping.java` + `nop-kernel/nop-record-mapping/src/main/java/io/nop/record_mapping/model/RecordFieldMappingConfig.java` | 对象 / 集合 / Map 递归映射锚点：`mapping`、`itemMapping`、`flattenFrom/To`、目标对象创建策略等从这里确认 |
| `MAP-004` | `nop-kernel/nop-record-mapping/src/main/java/io/nop/record_mapping/RecordMappingContext.java` + `nop-service-framework/nop-gateway/src/main/java/io/nop/gateway/core/executor/MappingProcessor.java` | 执行期上下文与 gateway 接线锚点：`varName`/scope/cache 在 `RecordMappingContext`，`requestMapping.bodyMapping` / `responseMapping.bodyMapping` 在 `MappingProcessor` 接入 |
| `TXN-001` | `nop-service-framework/nop-biz/src/main/java/io/nop/biz/service/BizActionInvoker.java` | 非 query 的 Biz 操作默认会进入事务 |
| `IOC-001` | `nop-kernel/nop-core/src/main/java/io/nop/core/reflect/impl/ClassModelBuilder.java` | private 字段不会成为可靠注入点 |
| `RESOLVE-001` | `nop-kernel/nop-core/src/main/java/io/nop/core/lang/json/bind/ValueResolverCompilerRegistry.java` + `nop-kernel/nop-core/src/main/java/io/nop/core/lang/json/bind/IValueResolverCompiler.java`（`BIND_EXPR_SYMBOL = '@'`）+ `nop-kernel/nop-core/src/main/java/io/nop/core/lang/json/bind/resolver/ConfigValueResolver.java` | XDSL delta 加载阶段的 `@`-prefix value resolver 注册表与编译入口；内置 `@cfg:` / `@i18n:` / `@var:` / `@uuid:` / `@load:` / `@empty:`，加载期一次性求值（非 reactive） |
| `RESOLVE-002` | `nop-kernel/nop-core/src/main/java/io/nop/core/lang/json/JsonTool.java`（`loadDeltaBeanFromResource`，L191-199）+ `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/functions/GlobalFunctions.java`（`loadDeltaJson`） | YAML/JSON delta 加载默认启用 `ValueResolverCompilerRegistry.DEFAULT` + `featurePredicateEvaluator` + `deltaExtendsGenerator`，scheduler.yaml 等文件中的 `@cfg:` 在这里求值 |
| `RESOLVE-003` | `nop-core-framework/nop-ioc/src/main/java/io/nop/ioc/loader/ConfigExpressionProcessor.java`（`parsePrefixExpr`）+ `nop-core-framework/nop-ioc/src/main/java/io/nop/ioc/impl/resolvers/ConfigValueResolver.java` + `nop-core-framework/nop-ioc/src/main/java/io/nop/ioc/IocConstants.java`（`PREFIX_CFG = "@cfg:"`、`PREFIX_R_CFG = "@r-cfg:"`） | IoC 容器中 `@cfg:` / `@r-cfg:`（reactive）/ `@bean:` / `@inject-ref:` 等的另一套实现，作用于 `*.beans.xml` 与 `@InjectValue`，与 XDSL delta 加载那套写法相同但实现独立 |
| `CFG-001` | `nop-core-framework/nop-config/src/main/java/io/nop/config/starter/ConfigStarter.java` | 配置系统会加载 `bootstrap.yaml`、`application.yaml` 与 profile 配置 |
| `IOC-002` | `nop-core-framework/nop-ioc/src/main/java/io/nop/ioc/loader/AppBeanContainerLoader.java` + `nop-kernel/nop-core/src/main/java/io/nop/core/module/ModuleManager.java` | 模块通过 `/_module` 发现，app bean 默认按 `app.beans.xml` / `app-*.beans.xml` 文件规则加载 |
| `INFRA-001` | `nop-job/nop-job-dao/src/main/java/io/nop/job/dao/store/JobScheduleStoreImpl.java` | 原始 DAO、`saveEntityDirectly`、`REQUIRES_NEW` 属于 infra/store 边界模式 |
| `JOB-001` | `nop-job/nop-job-coordinator/src/main/java/io/nop/job/coordinator/engine/JobTimeoutCheckerImpl.java` (`scanTaskTimeouts` / `resolveAliveWorkerIds` / `tryMarkSuspiciousIfWorkerGone` / `markSuspiciousAsTimeout` / `tryMarkTimeout`) + `nop-job/nop-job-dao/src/main/java/io/nop/job/dao/helper/JobFireStateMachine.java` (`resolveFinalStatus`) + `nop-job/nop-job-worker/src/main/java/io/nop/job/worker/engine/JobWorkerScannerImpl.java` (`executeTask` CLAIMED→RUNNING CAS / `handleExecutionResult`) | `nop-job` worker-liveness / timeout / completion 锚点：超时检查器通过 `INamingService.getInstances(AppConfig.appName())` 解析健康实例集合，用 `workerInstanceId` 判定 worker 是否存活；`CLAIMED/RUNNING` 且 owner 不活时首轮转 `SUSPICIOUS`，下一轮转 `TIMEOUT`。`SUSPICIOUS` 既阻止旧 worker 迟到结果覆盖（worker 侧把它视为 concurrently-finalized），也在 fire 聚合中作为“无 pending task 时等价 TIMEOUT”的中间态。 |
| `TEST-001` | `nop-autotest/nop-autotest-junit/src/main/java/io/nop/autotest/junit/JunitAutoTestCase.java` | 快照测试基类与 `@NopTestConfig` 约束 |
| `TEST-002` | `nop-autotest/nop-autotest-junit/src/main/java/io/nop/autotest/junit/JunitBaseTestCase.java` | 普通容器内测试基类 |
| `TEST-003` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/annotations/autotest/NopTestConfig.java` | 测试配置注解的真实属性 |
| `TEST-004` | `nop-ai/nop-ai-toolkit/src/test/resources/_vfs/nop/ai/beans/test-mock.beans.xml` + `nop-ai/nop-ai-toolkit/src/test/java/io/nop/ai/toolkit/tools/HttpRequestExecutorTest.java` | 测试专用 beans + `testBeansFile` 是仓库里的真实 mock bean 模式 |
| `TEST-005` | `nop-autotest/nop-autotest-core/src/main/java/io/nop/autotest/core/AutoTestCase.java` + `nop-autotest/nop-autotest-junit/src/main/java/io/nop/autotest/junit/JunitAutoTestCase.java` | AutoTest 常用 `input/request/output/outputText` helper，且录制完成会抛专用结束异常 |
| `DBG-001` | `nop-service-framework/nop-biz/src/main/resources/_vfs/nop/biz/beans/biz-defaults.beans.xml` | `nop.debug=true` 时会注册 DevDoc / DevTool 相关调试能力 |
| `DBG-002` | `nop-service-framework/nop-biz/src/main/java/io/nop/biz/dev/DevDocBizModel.java` | DevDoc 提供 beans、configVars、graphql 等调试查询 |
| `DBG-003` | `nop-service-framework/nop-biz/src/main/java/io/nop/biz/dev/DevToolBizModel.java` | DevTool 提供刷新 VFS 与清理缓存能力 |
| `DBG-004` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/store/DumpNamespaceHandler.java` | `_dump/{appName}/...` 是最终合并结果的重要调试出口 |
| `DBG-005` | `nop-service-framework/nop-biz/src/main/java/io/nop/biz/impl/BizObjectManager.java`（`dumpGraphQLSchema`） | `nop.debug=true` 时启动自动 dump GraphQL schema 到 `_dump/{appName}/nop/main/graphql/schema.graphql` |
| `GQL-001` | `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConstants.java` + `nop-service-framework/nop-graphql/nop-graphql-orm/src/main/java/io/nop/graphql/orm/OrmFetcherBuilder.java` + `nop-service-framework/nop-graphql/nop-graphql-orm/src/main/java/io/nop/graphql/orm/fetcher/OrmEntityPropConnectionFetcher.java` | relation 字段可通过 `graphql:filter` / `graphql:orderBy` 补默认查询元数据。OrmFetcherBuilder 还处理 `graphql:queryMethod`/`graphql:connectionProp`/`graphql:maxFetchSize`/`graphql:disableLogicalDelete`/`graphql:authObjName` |
| `GQL-002` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/schema/obj-schema.xdef` (prop 定义行 61-130) + `schema.xdef` | `graphql:*` 属性的 schema 定义完整集合：filter/orderBy/selection/inputType/transFilter 子元素 + 属性 |
| `GQL-003` | `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/utils/GraphQLObjMetaHelper.java` | 读取 `graphql:queryMethod`/`graphql:type`/`graphql:authObjName` 做 GraphQL 类型映射 |
| `GQL-004` | `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/schema/meta/ObjMetaToGraphQLDefinition.java` | 处理 `graphql:inputType` 自定义输入类型 |
| `GQL-005` | `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/fetcher/DictLabelFetcherProvider.java` | 处理 `graphql:dictName`+`graphql:dictValueProp` → `DictLabelFetcher` 实时解析 dict code→label |
| `GQL-006` | `nop-frontend-support/nop-ui/src/main/java/io/nop/xui/utils/XuiViewAnalyzer.java` | 处理 `graphql:labelProp`/`graphql:jsonComponentProp`/`graphql:selection` → 补全 GraphQL selection |
| `GQL-007` | `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/xdsl/ExtPropsGetter.java` | 通用工具：`getTreeBean()` 读取 `graphql:filter`、`getOrderBy()` 读取 `graphql:orderBy` |
| `GQL-008` | `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConstants.java` (`ATTR_GRAPHQL_*` and `TAG_GRAPHQL_*` 常量) | `graphql:*` 属性名常量全集定义 |
| `GQL-009` | `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/web/GraphQLWebService.java` (`runGraphQL` / `runRest` / `doPageQuery` / `runProxy`) + `nop-spring/nop-spring-web-starter/src/main/java/io/nop/spring/web/service/SpringGraphQLWebService.java` + `nop-quarkus/nop-quarkus-web/src/main/java/io/nop/quarkus/web/service/QuarkusGraphQLWebService.java` + `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/utils/GraphQLNameHelper.java` (`getOperationName`) | 所有 HTTP 入口（`/graphql`、`/r/{opName}`、`/p/{opName}`、`/px/{svc}/{method}`、`/jsonrpc`）统一由 `GraphQLWebService` 适配到 `IGraphQLEngine`，按 operationName `{bizObj}__{method}` 路由到同一个 BizModel 方法。`/r/` 与 `/p/` 共用 `runRest()` 执行路径，`/p/` 仅在响应阶段通过 `buildWebContent()` 增加二进制/文件/流支持。operationName 分隔符 `__` 定义在 `GraphQLConstants.OBJ_ACTION_SEPARATOR` |
| `IOC-003` | `nop-persistence/nop-dao/src/main/resources/_vfs/nop/dao/beans/dao-defaults.beans.xml` + `nop-persistence/nop-orm/src/main/resources/_vfs/nop/orm/beans/orm-defaults.beans.xml` + `nop-service-framework/nop-biz/src/main/resources/_vfs/nop/biz/beans/biz-defaults.beans.xml` | 平台内置 bean 广泛使用 `nop*` 命名，这是仓库强约定，但不是 IoC 层面的保留前缀规则 |
| `CODE-001` | `nop-persistence/nop-dao/src/main/java/io/nop/dao/coderule/ICodeRule.java` + `ICodeRuleGenerator.java` + `ICodeRuleVariable.java` + `CodeRuleParams.java` | 业务编码规则契约：`ICodeRuleGenerator.generate(ruleName, bean)` 是调用入口；`ICodeRule` 是模式引擎；`ICodeRuleVariable` 是可扩展变量（`{@type:options}`） |
| `CODE-002` | `nop-persistence/nop-dao/src/main/java/io/nop/dao/seq/ISequenceGenerator.java` + `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java` | 序列号原子引擎：`generateLong(seqName, useDefault)`；实现用 DB 行锁（`session.lock`）+ `cacheSize` 批量取号 + snowflake/uuid 兜底 + `REQUIRES_NEW` 独立事务（不被外层回滚） |
| `CODE-003` | `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/coderule/SysCodeRuleGenerator.java` + `DefaultCodeRule.java` | CodeRule 实现：按 `ruleName` 查 `NopSysCodeRule` → 取 `codePattern`+`seqName` → `codeRule.generate(pattern, now, seqSupplier, bean)`；`DefaultCodeRule` 内置 9 个变量（year/month/dayOfMonth/hour/minute/second/seq/randNumber/prop），用 `StringHelper.renderTemplate2` 解析 `{@type:options}` |
| `CODE-004` | `nop-kernel/nop-codegen/src/main/resources/_vfs/nop/templates/meta/.../_{entityModel.shortName}.xmeta.xgen`（`biz:codeRule` 行） + `nop-sys/nop-sys-service/_cases/io/nop/sys/service/TestCodeRuleAutoExpr/` | autoExpr 自动集成链：ORM 列 `tagSet="code"` → codegen 生成 `biz:codeRule="${shortName}@${col.name}"` → `CrudBizModel.doSave` 的 autoExpr 自动调 `ICodeRuleGenerator` 取号。TestCodeRuleAutoExpr 是端到端集成测试证据 |
| `SYS-001` | `nop-sys/model/nop-sys.orm.xml` + `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java` + `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/BroadcastEventProcessor.java` + `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/NonBroadcastEventProcessor.java` + `nop-batch/nop-batch-sys/src/main/resources/_vfs/nop/batch-task/sys-event/non-broadcast-consumer.batch.xml` + `nop-batch/nop-batch-sys/src/main/resources/_vfs/nop/job/conf/scheduler.yaml` + `nop-sys/nop-sys-dao/src/test/java/io/nop/sys/dao/message/TestSysDaoMessageService.java` | sys-event 当前可靠性基线：普通事件保留 `NopSysEvent` 作为 shared queue，`NonBroadcastEventProcessor` 作为唯一核心实现（simple mode 与 batch mode 共享同一 claim/process/handleResult 语义），simple mode 使用 `findNext` keyset pagination（禁止 OFFSET）；`nop-batch-sys` 通过 batch DSL（`.batch.xml`）+ scheduler 直接调用 `nopBatchTaskRunner.executeAsync` 加载任务，DSL loader/processor/consumer 通过 `<source>` XPL 委托 `SysDaoMessageService` 方法；广播事件使用 `NopSysBroadcastEvent` append-only 事件流 + `BroadcastEventProcessor` 通过 `findNext` keyset pagination + 内存游标 + 时间窗口消费 |
| `AUDIT-001` | `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java` + `nop-sys/model/nop-sys.orm.xml`（`NopSysChangeLog` 实体）+ `nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml`（`nopOrmEntityChangeLogInterceptor`，`feature:on="nop.orm.audit.enabled == '' or ..."` 默认启用） | 字段级变更日志内置实现：`postUpdate` 用 `orm_forEachDirtyProp` diff dirty 字段写 `NopSysChangeLog`；开关 `tagSet="audit"`/`audit-save`（实体级）、`tagSet="no-audit"`（列级），常量在 `OrmModelConstants.TAG_AUDIT/TAG_AUDIT_SAVE/TAG_NO_AUDIT` |
| `AUDIT-002` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/IOrmInterceptor.java` + `nop-persistence/nop-orm/src/main/java/io/nop/orm/support/MultiOrmInterceptor.java` + `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/orm/orm-interceptor.xdef` | 通用 ORM 拦截器（应用层 trigger）：`IOrmInterceptor` 8 hook（pre/post save/update/delete/load/reset/flush）+ `MultiOrmInterceptor` 按 `IOrdered` 聚合；`orm-interceptor.xml` 配置式回调（per-entity + XPL source），schema `/nop/schema/orm/orm-interceptor.xdef` |
| `REPORT-001` | `nop-report/nop-report-core/src/main/java/io/nop/report/core/engine/IReportEngine.java` + `ReportEngine.java` + `nop-report/nop-report-core/src/main/java/io/nop/report/core/XptConstants.java` | 报表引擎入口：`getRenderer(path, renderType)` / `getHtmlRenderer` / `generateFromXptModel` / `buildXptModelFromImpModel`；文件类型 `FILE_TYPE_XPT_XLSX`/`FILE_TYPE_XPT_XML`，渲染类型 `RENDER_TYPE_HTML`/`RENDER_TYPE_XLSX`/`RENDER_TYPE_PDF`；`getXptModel` 经 `XptModelLoader`→`ExcelToXptModelTransformer`（批注→模型）+ `XptModelInitializer`（文本→表达式） |
| `REPORT-002` | `nop-format/nop-excel/src/main/java/io/nop/excel/model/_gen/_XptCellModel.java`（指令权威，`xml name:` 注释即批注 key）+ `nop-report/nop-report-core/src/main/java/io/nop/report/core/build/ExcelToXptModelTransformer.java`（批注解析）+ `nop-report/nop-report-core/src/main/java/io/nop/report/core/engine/expand/AbstractCellExpander.java` | XPT 单元格展开模型：`expandType`(r/c)、`expandExpr`、`field`、`ds`、`rowParent`/`colParent`(父子层级)、`valueExpr`、`formatExpr`；单元格文本 `*=^ds!field`(向下展开)/`*=>ds!field`(向右)/`${...}` |
| `REPORT-003` | `nop-format/nop-excel/src/main/java/io/nop/excel/model/_gen/_ExcelImage.java`（`print` 标志，注释"控制图片是否被打印。套打对应的背景图片不需要被打印"）+ 渲染器 `nop-report-core/.../renderer/XlsxReportRendererFactory.java` + `nop-format/nop-excel/.../renderer/HtmlReportRendererFactory.java` + `nop-report-pdf/.../renderer/PdfReportRendererFactory.java`（PDFBox 直接渲染，非 xlsx 转换）+ 范例 `nop-report-demo/.../_vfs/nop/report/demo/base/09-套打.xpt.xlsx` | 套打机制：`ExcelImage.print=false`（对应 OOXML `fPrintsWithSheet="0"`）背景图屏幕可见、打印隐藏；三渲染器共享展开逻辑、各自输出 xlsx/html/pdf |
| `DOC-001` | `docs-for-ai/02-core-guides/frontend-rendering-pipeline.md` | 框架无关的页面生成管线文档 |
| `DOC-002` | `docs-for-ai/02-core-guides/amis-rendering.md` | AMIS 渲染管线文档 `(AMIS)` |
| `DOC-003` | `docs-for-ai/02-core-guides/flux-rendering.md` | Flux 渲染管线文档 `(Flux)` |
| `UI-001` | `nop-auth/nop-auth-web/src/main/resources/_vfs/nop/auth/pages/NopAuthResource/NopAuthResource.view.xml` | 树形 CRUD、`@TreeChildren`、`add-child`、`gen-control`、`loadDataOnce` 的综合参考 |
| `UI-002` | `nop-job/nop-job-web/src/main/resources/_vfs/nop/job/pages/NopJobSchedule/NopJobSchedule.view.xml` | row action 直连业务 API、运行态 drawer + vertical tabs、关联子页组合的综合参考 |
| `UI-003` | `nop-rule/nop-rule-web/src/main/resources/_vfs/nop/rule/pages/NopRuleNode/NopRuleNode.view.xml` + `nop-rule/nop-rule-web/src/main/resources/_vfs/nop/rule/pages/NopRuleNode/ref-ruleDefinition.page.yaml` | `gen-control` 高阶控件、`add-child` 上下文传递、`fixedProps` 关联子表页参考 |
| `UI-004` | `nop-wf/nop-wf-web/src/main/resources/_vfs/nop/wf/designer/designer.page.yaml` | `x:gen-extends` 与大块手写 page schema 混合的专用设计器页面参考 |
| `BATCH-001` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/task/batch.xdef` | batch DSL xdef schema（副本见 `04-reference/xdefs/batch.xdef`） |
| `BATCH-002` | `nop-batch/nop-batch-dsl/src/main/resources/_vfs/nop/batch/xlib/batch.xlib` | batch 运行时标签库 |
| `WF-001` | `nop-wf/nop-wf-core/src/main/resources/_vfs/nop/wf/base/oa.xwf` | `oa.xwf` 基模板：workflow 领域把审批共性沉淀为 base XDSL 的代表案例 |
| `WF-002` | `nop-wf/nop-wf-core/src/main/resources/_vfs/nop/wf/xlib/oa.xlib` + `nop-wf/nop-wf-core/src/main/resources/_vfs/nop/wf/xlib/wf-actor.xlib` | `oa:*` 与 `wf-actor:*` 说明领域规则可以外置为标签库，而不是写死在引擎里 |
| `WF-003` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/wf/wf.xdef` | workflow 只是平台 XDSL 体系中的一个 schema；适合拿来观察通用机制在具体领域的落地 |
| `WF-004` | `nop-wf/nop-wf-core/src/main/java/io/nop/wf/core/engine/WorkflowEngineImpl.java` (`doInvokeAction` / `doReject`) + `nop-wf/nop-wf-core/src/main/resources/_vfs/nop/wf/xlib/wf-vote.xlib` | vote-group 的完成/拒绝判定槽位接线与 `wf-vote:*` 标签库实现锚点 |
| `WF-005` | `nop-wf/nop-wf-core/src/main/java/io/nop/wf/core/service/impl/WorkflowServiceImpl.java` + `nop-wf/nop-wf-core/src/main/java/io/nop/wf/core/store/IWorkflowStore.java` | `signalWf` / `transferActors` 公共 API 与跨工作流 store 扩展锚点 |
| `WF-006` | `nop-wf/nop-wf-scheduler/src/main/java/io/nop/wf/scheduler/WfTaskScanner.java` + `nop-wf/nop-wf-scheduler/src/main/resources/_vfs/nop/job/conf/scheduler.yaml` | 调度器模块、定时扫描与 nop-job YAML 注册锚点 |
| `XLANG-001` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/xpl.xdef` | XPL 模板文件的基础 schema |
| `XLANG-002` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/xlib.xdef` | XLib 文件的基础 schema |
| `XLANG-003` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/xdsl.xdef` | 通用 XDSL 扩展语法 schema |
| `XLANG-004` | `nop-wf/nop-wf-web/src/main/resources/_vfs/nop/wf/xlib/dingflow-gen/impl_GenComponents.xpl` | `c:unit`、`c:for`、`c:if`、`${...}` 的真实模板例子 |
| `XLANG-005` | `nop-task/nop-task-core/src/main/resources/_vfs/nop/task/xlib/task.xlib` | `xpl:is` 与 XLib 入口的真实例子 |
| `XLANG-006` | `nop-runner/nop-cli-core/tasks/gen-web.xrun` | `xpl:lib` 调用 XLib 的 runner 任务例子 |
| `XLANG-007` | `nop-kernel/nop-core/precompile/src/main/java/io/nop/core/type/PredefinedGenericTypes.java.xgen` | 文本输出型 `.xgen` 模板例子 |
| `XLANG-008` | `nop-kernel/nop-xlang/precompile/gen-xlang-xdsl.xgen` + `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/schema/obj-schema.xdef` + `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/schema/schema.xdef` | XDef schema 变更会驱动 `nop-xlang` 的 XDSL 生成链；共享 prop 定义常同时分布在 `obj-schema.xdef` / `schema.xdef` |
| `AUTH-001` | `nop-auth/nop-auth-service/src/main/resources/_vfs/nop/auth/beans/auth-service.beans.xml` (bean `nopAuthFilterConfig`) | HTTP 路径认证配置：`defaultPublic`/`publicPaths`/`authPaths`/`servicePaths` |
| `AUTH-002` | `nop-service-framework/nop-biz-auth-core/src/main/java/io/nop/auth/core/filter/AuthFilterConfig.java` | 认证配置类，`isPublicPath()` 判定逻辑 |
| `AUTH-003` | `nop-service-framework/nop-biz-auth-core/src/main/java/io/nop/auth/core/filter/AuthHttpServerFilter.java` | 认证过滤器实现，token 解析、OAuth、cookie 处理 |
| `AUTH-MFA-001` | `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/login/LoginServiceImpl.java` (`loginAsync:302` / `createSessionForUserAsync:404` / `mfaVerifyAsync:548` / `checkMfaRequired:1012`，`ERR_AUTH_MFA_REQUIRED` 抛于 `:386`/`:423`) + `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/biz/LoginApiBizModel.java` (`mfaVerifyAsync:197` / `sendSmsCode:173`) + `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/entity/NopAuthUserBizModel.java` (`bindMfa:237`/`confirmMfa:605`/`unbindMfa:677`/`resetUserMfa:941`) | MFA 两阶段登录锚点：第一因子校验 → `checkMfaRequired` 判定（W15 增 headers 参数——可信设备豁免输入）→ `MfaChallengeStore.create` → 抛 `ERR_AUTH_MFA_REQUIRED`（errorParams 携带 challengeToken/mfaType/loginType）→ `mfaVerify` 第二因子；SSO/信道登录同样拦截（loginType 参数化）；用户自助/管理员 API 见 NopAuthUserBizModel |
| `AUTH-MFA-002` | `nop-service-framework/nop-biz-auth-core/src/main/java/io/nop/auth/core/totp/TOTPAuthenticator.java` (`generateSecret:69` / `verify:109` / `verifyRaw:134`) + `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/mfa/store/DbMfaChallengeStore.java` + `DbSmsCodeStore.java` + `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/NopAuthErrors.java` (`ERR_AUTH_MFA_REQUIRED`/`ERR_AUTH_MFA_FAIL` 等，`:79`+) | TOTP（RFC 6238：HMAC-SHA1/6 位/30s/±1 窗口，防重放 lastVerifiedWindow）+ MFA/SMS 存储后端（db 默认/local/redis）+ MFA/SMS 错误码定义 |
| `AUTH-MFA-003` | `nop-service-framework/nop-biz-auth-api/src/main/java/io/nop/auth/api/mfa/MfaRequired.java` + `IOperationMfaChecker.java` + `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/reflection/ReflectionBizModelBuilder.java`（读取 + 构建期两约束）+ `GraphQLFieldDefinition.java`（mfaRequiredMeta + deepClone）+ `GraphQLExecutor.java`（两检查点 `checkOperationMfa`）+ `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/mfa/OperationMfaCheckerImpl.java`（§3.3 判定链 + `X-Nop-Op-Mfa-Token` 票头）+ `MfaFactorVerifier.java`（三处因子校验收敛 + TOTP 窗口统一推进）+ `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/biz/LoginApiBizModel.java`（`mfaVerifyOperation`）| 操作级 MFA（W12）：`@MfaRequired` 声明敏感操作（构建期拒绝 subscription/publicAccess 组合）→ executor 检查点委托 `IOperationMfaChecker`（GraphQLEngine 可选注入，未部署零介入）→ scene=operation challenge → `mfaVerifyOperation`（登录态同会话验证，成功不签发凭证）→ 一次性短 TTL 票（绑定 operation+sessionId）携头重试；`nop.auth.operation-mfa.enabled` 缺省 false |
| `AUTH-MFA-004` | `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/mfa/RoleMfaPolicyEvaluator.java`（factorLevel 强度表 sms/email=1、totp=2、webauthn=3、未知 0 fail-closed；max/AND 合并；角色快照 = buildUserContext 口径含隐式 user 角色链）+ `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/login/LoginServiceImpl.java`（`checkMfaRequired` 第三态 MFA_RESTRICTED + `completeLogin` 受限变体）+ `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/auth/IUserContext.java`（`isMfaRestricted()` default 方法）+ `nop-service-framework/nop-biz-auth-core/src/main/java/io/nop/auth/core/login/UserContextImpl.java` + `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/login/DaoUserContextCache.java`（序列化白名单两触点）+ `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/engine/GraphQLExecutor.java`（受限分支：query/publicAccess mutation 放行，其余路由 checker）+ `OperationMfaCheckerImpl.java`（受限分支前置 enabled 门 + 白名单短路 + `ERR_AUTH_MFA_RESTRICTED_SESSION`）+ `LoginApiBizModel.java`（`verifyChannelProof:384` 登记通道票——W15 增可选 channel 参数（phone|email））+ `NopAuthUserBizModel.java`（`bindMfa` proof 前置 + `confirmMfa` 策略校验）+ `nop-service-framework/nop-biz-auth-core/src/main/java/io/nop/auth/core/mfa/IMfaLoginPolicyService.java`（OAuth 接入 SPI）| 角色级强制策略与受限会话（W13）：三层判定矩阵（全局开关 → 角色策略 maxLevel → 用户级 setting）不达标 → 受限签发（标志先设后存）→ executor/checker 双触点拦截（白名单 = MFA 绑定引导类 + verifyChannelProof + logout/refresh；不受 operation-mfa.enabled 门控）→ 登记通道 proof（scene=channel-proof 已验证票 token 往返，防 enrollment attack；W15 email 解锁：phone 优先缺失回退 email + 双通道 channel 选择）→ confirmMfa 防降级 → 重新登录完整两阶段；无策略行 = 一期行为 |
| `AUTH-MFA-006` | `nop-service-framework/nop-biz-auth-core/src/main/java/io/nop/auth/core/mfa/store/EmailCodeStore.java` + `LocalEmailCodeStore.java` + `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/mfa/store/DbEmailCodeStore.java` + `RedisEmailCodeStore.java` + `MfaStoreProvider.java`（第三组 map `emailCodeStores` + 工厂方法 `getEmailCodeStore`）+ `nop-auth/nop-auth-service/src/main/resources/_vfs/nop/auth/beans/auth-service.beans.xml`（collect-beans `nopEmailCodeStore_` 前缀 + `nopActiveEmailCodeStore` 工厂 bean）+ `NopAuthUserBizModel.java`（`bindMfa` email 分派 + `MfaFactorVerifier` email 分支消费）+ `LoginServiceImpl.java`（`sendMfaEmailCode`/`checkEmailRateLimit`）| 邮件验证码因子（W15）：`SmsCodeStore` 同形平行接口（key 通道隔离 `mfa-email:`/`proof-email:`）+ 三实现 W8 装配复刻；发送链路复用 `IEmailSender`（`nop-integration-api` 零变更，`@Nullable` fail-closed）+ `{code}` 模板配置化 + 收件目标服务端解析；`nop.auth.email-code.*` 六项配置组（enabled 缺省 false——三发码入口统一前置显式拒绝）+ email+IP 双维度限流 |
| `AUTH-MFA-007` | `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/mfa/MfaTrustedDeviceManager.java`（`fingerprint` 三输入 SHA-256 + `isExempted` 固定窗口 + `register` upsert/满员/并发归一 + 撤销矩阵 + 审计五事件）+ `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/login/LoginServiceImpl.java`（`checkMfaRequired` headers 参数 + 豁免分支（`policy.allowTrustedDevice` AND 合并首个消费点）+ `verifySecondFactorAndComplete` headers 穿线与登记（`ATTR_TRUSTED_DEVICE_REGISTERED` attr 回填）+ `isPasswordLoginType`）+ `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/entity/NopAuthUserBizModel.java`（`listTrustedDevices:872`/`removeTrustedDevice:897` + unbindMfa/confirmMfa/resetUserMfa 撤销钩子）+ `nop-service-framework/nop-biz-auth-api/src/main/java/io/nop/auth/api/messages/MfaVerifyRequest.java`（`rememberDevice` 可选字段）+ `LoginResult.java`（`trustedDeviceRegistered` 可选字段）| 可信设备（W15，记住此设备）：`X-Nop-Mfa-Device-Id` + UA + Accept-Language → SHA-256 指纹（device-id 缺失降级正常 MFA）；豁免仅密码类 loginType（1/2/3/5，信道路径 headers=null 结构性跳过；OAuth 副本不加豁免分支——专项回归断言）；固定窗口 30d 命中只更新 lastUsedAt 不续期；撤销矩阵（到期惰性/自助移除物理删除/因子变更与 reset 全量删除/策略禁跳过不删行）；登记失败不阻断登录（显式 true/false 回填） |
| `CRED-001` | `nop-credential/nop-credential-api/src/main/java/io/nop/credential/api/ICredentialProvider.java` + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/CredentialProviderImpl.java` (`:46`，唯一明文出口) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/crypto/CredentialCipher.java` (`:28`，`cv1:{keyId}:{v1密文}`) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/entity/NopCredentialBizModel.java` (`saveCredential:124`/`maskList:237`/`reencryptAll:289`) | 凭证库明文边界：SPI 接口在 api 层、唯一明文出口实现 `CredentialProviderImpl` 在 service 层；`getCredential`/`getCredentialData` 不暴露为 BizModel/GraphQL 方法；xmeta `data` 列 `published=false`；`get`/`findPage` 返回前恒置空 `data`；所有 getter fail-closed |
| `CRED-002` | `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/oauth/OAuthFlowService.java` + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/oauth/CredentialOAuthApiBizModel.java` (`beginOAuthFlow:47` 登录态 / `oauthCallback:59` publicAccess) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/oauth/NopCredentialOauthStateStore.java`（state 一次性消费） + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/oauth/OAuthTokenClient.java`（令牌端点协议） + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/CredentialProviderImpl.java` (`engineGetDecryptedFields:232`/`engineUpdateTokenFields:245`/`engineUpdateInLock:283`，引擎通道) | OAuth 出站客户端（W9）：授权码闭环（发起 mutation → 单一公开回调 `GET /r/CredentialOAuthApi__oauthCallback` → token 加密回写引擎保留字段）；state 原子一次性消费（条件 UPDATE + affected-row 判定）；取用时惰性刷新（DB 行级锁 `lockEntity` 跨副本互斥）；保留字段名（accessToken/refreshToken/expiresAt/tokenType/scope）registry 层独占；`WebContentBean` HTML 跳转页承载回调响应（biz 层无 30x 原语） |
| `CRED-003` | `nop-credential/nop-credential-kms-vault/src/main/java/io/nop/credential/kms/vault/VaultCredentialKeyProvider.java` (`init:144` 启动期材料物化 / `fetchMaterial:248` KV v2 读取) + `nop-credential/nop-credential-kms-vault/src/main/resources/_vfs/nop/credential/beans/app-kms-vault.beans.xml`（同名 bean `nopCredentialKeyProvider` + `if-property key-provider=vault` 门控） + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/crypto/DefaultCredentialKeyProvider.java` (`init:77`，default-bean 守卫——非 local 抛 `key-provider-module-missing`、master-keys 残留抛 `master-keys-residual`) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/entity/NopCredentialBizModel.java` (`reencryptAll:389` keyset 游标翻页 / `buildReencryptQuery:437` 确定性排序) | 外部 KMS 集成（W10，材料交付模式）：Vault KV v2 经 `IHttpClient` 零 SDK；启动期 fail-closed（不可达/401/403/404/材料非法/来源混合/残余含 active 全部拒绝启动、无本地降级）；运行期零托管端调用；NopIoC default bean 无条件兜底的前提由守卫修正（模块缺失 → 启动失败）；迁移残余列表只解不加密 + WARN 审计；reencryptAll 分页完备性（orderBy credentialId + 游标翻页全覆盖） |
| `CRED-004` | `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/CredentialOwnership.java` (`:25`，scope 语义/admin 判定 `nop.credential.admin-roles`/可见性/写分级 denial reason) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/CredentialProviderImpl.java` (`assertOwnershipForPlaintext:492`/`assertOwnershipForMaskOrTest`，解密前 per-method 矩阵) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/entity/NopCredentialBizModel.java` (`saveCredential:161` 归属输入+分级+不可变 / `defaultPrepareQuery:385` 结构性读过滤 / `assertWriteAllowed:405` / `batchGet:772` 过滤语义；六旁路动作禁用) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/entity/NopCredentialUsageBizModel.java` (`:33` 查询面限管理员) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/oauth/OAuthFlowService.java` (`assertBeginOwnership:128` W9 回补) | 凭证归属统一（W11 Part A，scope=system\|user + ownerId）：provider 明文出口 user 级 owner 唯一（无上下文/非 owner/admin 一律拒）、管理面 owner+admin，先序 delFlag；BizModel 两层防御（读过滤 `scope=system ∨ NULL ∨ (user ∧ ownerId=本人)`、写分级 system=管理员/user=owner+admin、单条越权归一"不存在"）；六旁路动作收口（update/batchDelete/updateByQuery/deleteByQuery/copyForNew 禁用、batchGet 过滤）；存量 NULL 视同 system（DDL 无默认值裁定） |
| `CRED-005` | `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/CredentialProviderImpl.java` (`assertRoleAuthForPlaintext`/`findGrantedRoleIds`，明文出口 RBAC 收紧、归属校验之后解密之前) + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/entity/NopCredentialAuthBizModel.java`（grant/revoke 幂等 + admin-only 查询面 + 七旁路禁用） + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/entity/NopCredentialBizModel.java`（`prepareDeleteWithUsageCheck` 授权行父侧级联清理） + `nop-credential/model/nop-credential.orm.xml`（`NopCredentialAuth` 实体 + `auths` to-many） | RBAC 细粒度授权（W11 Part B，角色↔凭证实例二值 use 授权）：默认开放可选收紧（无记录=一期行为/有记录+无上下文放行（服务级信任）/有记录+有上下文角色求交，admin 不豁免）；仅 system 级、user 级不叠加；引擎内部通道豁免裁定（明文不外泄+入口已门控）；grant/revoke 幂等契约（唯一约束兜底）；roleId 字符串软引用不做跨模块存在性校验 |
| `CRED-006` | `nop-integration/nop-integration-api/src/main/java/io/nop/integration/api/credential/CredentialResolutionSupport.java`（共享解析支持：优先级链/fail-closed/typeName 家族校验/值转换单点） + `nop-integration/nop-integration-sms-tencent/src/main/java/io/nop/integration/sms/TencentSmsSender.java` + `nop-integration/nop-integration-sms-yunpian/src/main/java/io/nop/integration/sms/yunpian/YunpianSmsSender.java`（credentialId 属性 + 发送期惰性解析双构造点 + `@PostConstruct` 幂等 registerUsage catch-all WARN） + `nop-credential/nop-credential-api/src/main/java/io/nop/credential/api/ICredentialMigrationSupport.java` + `CredentialLookup.java`（迁移支持 SPI；`CredentialData.typeName` 增量承载错型校验） + `nop-credential/nop-credential-service/src/main/java/io/nop/credential/service/CredentialMigrationSupportImpl.java`（bean `nopCredentialMigrationSupport`，createCredential 复用 saveCredential 语义） + `nop-metadata/nop-metadata-service/src/main/java/io/nop/metadata/service/connection/MetaDataSourceConnectionProcessor.java`（`mergeCredentialConfig` 单点解析 + testConnect 白名单结构化映射，14 消费点零改动） + `nop-metadata/nop-metadata-service/src/main/java/io/nop/metadata/service/entity/NopMetaDataSourceBizModel.java`（bindCredential/unbindCredential/migrateDataSourcesCredential + delete/deleteByQuery 双覆写 unregister，`nop.metadata.credential-admin-roles` admin 判定） | 凭证深度迁移消费链（W16 首批）：`credentialId` 空白=静态值现状路径零回归；非空=凭证字段集整组取凭证库（同名静态值忽略）+ 强 fail-closed 不静默回退（部署不一致/缺失/软删/错型/必填空/转换失败全显式抛错）；consumerRef `integration:<channelType>`（bean 初始化登记）/ `metadata:NopMetaDataSource:<dataSourceId>`（bind/迁移登记、unbind/换绑/行删除解除）；批量迁移 per-row REQUIRES_NEW 幂等反查（主源 consumerRef 唯一约束 + 名称辅助截断哈希；软删命中计失败清单不重建）；无 ORM/DDL（JSON 键方案） |
| `CRED-007` | `nop-integration/nop-integration-email-tencent/src/main/java/io/nop/integration/email/tencent/TencentEmailSender.java`（credentialId + 发送期解析 + `createClient(ResolvedCredential)` seam + `@PostConstruct` 登记） + `nop-integration/nop-integration-email-java/src/main/java/io/nop/integration/email/java/JavaEmailSender.java` + `MailConfig.java`（credentialId 持于 MailConfig；解析在 `withTransport` 吞异常 try 之前 fail-closed 穿透；`connectTransport(ResolvedCredential)` seam；smtp-email 跨字段"至少一字段非空"解析侧兜底） + `nop-integration/nop-integration-feishu/src/main/java/io/nop/integration/feishu/client/FeishuCredentials.java`（第五键 `@InjectValue` 并列） + `FeishuClient.java`（start 期解析副本 + start 期登记（结论 8 家族偏差）+ token 刷新/重连用已捕获值） + `nop-integration/nop-integration-oss/src/main/java/io/nop/integration/oss/OssConfig.java`（credentialId `@ConfigField(name="credentialId")` camelCase 钉住） + `OssFileServiceClientFactory.java`（构造期一次解析 + `createAwsCredentials` seam + enabled 门控零介入） + `nop-integration/nop-integration-sftp/src/main/java/io/nop/integration/sftp/SftpConfig.java`（credentialId 持有） + `SftpClient.java`（解析先于 `ERR_SFTP_CONNECT_FAIL` 包装点 + `newJsch`/`openConnection` seam） + `SftpClientFactory.java`（provider 持有传递 + 登记） + `nop-credential/nop-credential-service/src/main/resources/_vfs/nop/credential/types/`（扩展五类型实例：tencent-email/smtp-email/feishu-app/oss-s3/sftp-ssh） | 凭证深度迁移扩展批次消费链（W16-impl-ext）：五家族分层时序（Email×2/SFTP 逐次消费期、Feishu start 期解析副本、OSS 构造期）；五枚 consumerRef token（`integration:tencent-email`/`smtp-email`/`feishu-app`/`oss-s3`/`sftp-ssh`）；feishu/oss credentialId 配置键（`nop.integration.feishu.credentialId` @InjectValue / `nop.integration.oss.credentialId` config-prefix + @ConfigField）；ChannelConfig options 面无 credentialId（负向钉死）；Email MFA 发码链 E2E（nop-auth-service `TestJavaEmailSenderMfaE2E`） |
| `SEC-CFG-001` | `nop-core-framework/nop-config/src/main/java/io/nop/config/enhancer/DefaultConfigValueEnhancer.java` (`:67`，`@sec:` 前缀检测) + `nop-core-framework/nop-config/src/main/java/io/nop/config/starter/ConfigStarter.java` (`:449`/`:473`，装配 enhancer + `AESTextCipher`) + `nop-kernel/nop-commons/src/main/java/io/nop/commons/crypto/impl/AESTextCipher.java` + `nop-kernel/nop-commons/src/main/java/io/nop/commons/CommonConstants.java` (`:33`，`SEC_VALUE_PREFIX="@sec:"`) | `@sec:` 配置值加密：启动期 `DefaultConfigValueEnhancer` 检测 `@sec:` 前缀 → `AESTextCipher.decrypt` 解 `v1:` 密文。服务配置文件静态密钥（与凭证库 `CRED-001` 服务 DB 行级数据互补） |
| `TNT-001` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/context/IContext.java` | 上下文持有租户 ID（`getTenantId()` / `setTenantId()`） |
| `TNT-002` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/context/ContextProvider.java` | `currentTenantId()` / `runWithTenant()` / `runWithoutTenantId()` |
| `TNT-003` | `nop-persistence/nop-orm-eql/src/main/java/io/nop/orm/eql/compile/EqlTransformVisitor.java` | EQL 编译自动注入 `tenantId = ?` |
| `TNT-004` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/persister/EntityPersisterImpl.java` | 实体加载时自动填充/校验租户 ID |
| `TNT-005` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/id/OrmEntityIdGenerator.java` | 实体保存时自动填充租户 ID |
| `TNT-006` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/session/TenantOrmSessionEntityCache.java` | Session 缓存按租户分区隔离 |
| `TNT-007` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/sql/GenSqlHelper.java` | SQL 生成自动追加租户过滤条件 |
| `TNT-008` | `nop-persistence/nop-orm-model/src/main/java/io/nop/orm/model/init/OrmEntityModelInitializer.java` | `useTenant=true` 时自动创建 `nopTenantId` 列 |
| `VFS-001` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/store/DeltaResourceStore.java` | Delta 层式解析：tenant → deltaLayers → base；`getSuperResource()` 实现 `super:` 语义 |
| `VFS-002` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/store/DeltaResourceStoreBuilder.java` | VFS 构建入口：classpath 扫描、VFS index、libPaths、当前项目资源、duplicate 检测 |
| `VFS-003` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/store/DefaultVirtualFileSystem.java` | VFS 全局单例；namespace handler 注册（`super:` / `raw:` / `v:` / `module:` 等） |
| `VFS-004` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/scan/ClassPathScanner.java` | Classpath 扫描 `_vfs/` 的 JAR/ZIP/directory 实现 |
| `VFS-005` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/store/InMemoryResourceStore.java` | VFS 内存资源树，`addResourceIfAbsent() first-wins` |
| `VFS-006` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/VirtualFileSystem.java` | VFS 全局单例持有者（`instance()` / `registerInstance()`） |
| `INT-001` | `nop-integration/nop-integration-api/src/main/java/io/nop/integration/api/sms/ISmsSender.java` + `SmsMessage.java` | 外发短信默认接口与消息对象 |
| `INT-002` | `nop-integration/nop-integration-api/src/main/java/io/nop/integration/api/email/IEmailSender.java` | 外发邮件默认接口 |
| `RPT-001` | `nop-report/model/nop-report.orm.xml` + `nop-report/nop-report-web/src/main/resources/_vfs/nop/report/auth/_nop-report.action-auth.xml` | `nop-report` 的数据集、报表定义、结果文件和后台页面入口 |
| `DB-001` | `nop-persistence/nop-dao/src/main/java/io/nop/dao/DaoConfigs.java` (`CFG_AUTO_CONVERT_EMPTY_STRING_TO_NULL`) | 空字符串转 NULL 配置项，默认 `true`，Oracle 兼容 |
| `DB-002` | `nop-persistence/nop-dao/src/main/java/io/nop/dao/dialect/impl/DialectImpl.java` (`getStringBinder` / `jdbcSet`) | Dialect 层空字符串转 NULL、VARCHAR 超长自动提升 CLOB 的实现 |
| `DB-003` | `nop-kernel/nop-dataset/src/main/java/io/nop/dataset/binder/DataParameterBinders.java` (`STRING_EX`) | 空字符串转 NULL 的参数绑定器实现 |
| `DB-004` | `nop-persistence/nop-dao/src/main/java/io/nop/dao/dialect/model/_gen/_DialectFeatures.java` | Dialect 特性标志（useAsInFrom、supportNullsFirst 等） |
| `DQL-001` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/beans/query/QueryBean.java` | DQL 核心模型：sourceName/fields/owner/aggFunc/dimFields/filter/orderBy |
| `DQL-002` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/IOrmTemplate.java:190-202` | DQL 执行入口：`findListByQuery(QueryBean)`、`findFirstByQuery()`、`existsByQuery()` |
| `DQL-003` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/mdx/MdxQueryExecutor.java` | 主子表维度对齐执行引擎：split → 分别 execute → Hash Join merge |
| `DQL-004` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/mdx/MdxQuerySplitter.java` | QueryBean 按 owner 属性拆分为主查询 + 子查询列表 |
| `DQL-005` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/sql_lib/QuerySqlItemModel.java` | `<query>` 标签：XML source → QueryBean 反序列化模型 |
| `DQL-006` | `nop-persistence/nop-orm/src/main/java/io/nop/orm/dao/DaoQueryHelper.java` | QueryBean → EQL SQL 转换工具 |
| `RPC-001` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/rpc/IRpcService.java` | NopRPC 核心接口：`callAsync(serviceMethod, request, cancelToken)`，所有 RPC 实现统一于此 |
| `RPC-002` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/rpc/IRpcServiceInvoker.java` | 服务发现与路由抽象：`invokeAsync(serviceName, serviceMethod, request, cancelToken)` |
| `RPC-003` | `nop-cluster/nop-rpc-cluster/src/main/java/io/nop/rpc/cluster/ClusterRpcServiceInvoker.java` | `IRpcServiceInvoker` 默认实现：服务名白名单校验 + `ClusterRpcClient` |
| `RPC-004` | `nop-cluster/nop-rpc-cluster/src/main/java/io/nop/rpc/cluster/ClusterRpcClient.java` | 集群 RPC 客户端：通过 `IServerChooser` 选择实例，支持重试 |
| `RPC-005` | `nop-network/nop-rpc/nop-rpc-http/src/main/java/io/nop/rpc/http/HttpRpcService.java` | HTTP RPC 实现：将 `ApiRequest` 序列化为 HTTP 请求并通过 `IHttpClient` 发送 |
| `RPC-006` | `nop-network/nop-rpc/nop-rpc-api/src/main/java/io/nop/rpc/api/ContextBinder.java` | RPC 上下文绑定：从 `ApiRequest.headers` 提取 locale/timezone/tenant 等传播到 `IContext` |
| `RPC-007` | `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/web/GraphQLWebService.java` (`runProxy`) | `/px/` 分布式代理入口：从 `ApiRequest.headers` 原样转发 HTTP 头到远程服务 |
| `RPC-008` | `nop-service-framework/nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/rpc/RpcServiceOnGraphQL.java` | 将 GraphQL BizModel 包装为 `IRpcService`，支持字段选择 |
| `RPC-009` | `nop-kernel/nop-api-core/src/main/java/io/nop/api/core/util/ApiHeaders.java` (`getSvcTargetHost`/`setSvcTargetHost`) + `nop-cluster/nop-cluster-core/src/main/java/io/nop/cluster/chooser/filter/SpecificServiceInstanceFilter.java` | 指定实例路由：`nop-svc-target-host` header → 实例过滤器只保留 host 匹配实例；装配于 `nop-cluster/nop-rpc-cluster/src/main/resources/_vfs/nop/rpc/beans/rpc-cluster-defaults.beans.xml`（`nopServiceInstanceFilter_specific`） |
| `NET-001` | `nop-network/nop-http/nop-http-api/src/main/java/io/nop/http/api/utils/FileDownloadSink.java` + `FileTransferHelper.java` | 文件传输协议层：`.part` 续传状态机、206 追加/200 重写、SHA-256/SHA-1 流式摘要、校验来源解析（显式 > `x-content-sha256` 头 > sidecar `{url}.sha256`）、原子 rename；三客户端共用保证行为等价 |
| `NET-002` | `nop-network/nop-http/nop-http-client-apache/src/main/java/io/nop/http/apache/ProgressFileEntityProducer.java` | BINARY 上传流式 producer（x-file-* 头协议）；JDK 侧为 `ProgressBodyPublisher`，OkHttp 侧为 `ProgressRequestBody` |
| `NET-003` | `nop-network/nop-vertx/nop-vertx-mqtt-server/src/main/java/io/nop/vertx/mqtt/server/impl/MqttSessionManager.java` + `nop-network/nop-vertx/nop-vertx-mqtt-server/src/main/java/io/nop/vertx/mqtt/server/router/MqttTopicMatcher.java` + `nop-network/nop-vertx/nop-vertx-mqtt-server/src/main/java/io/nop/vertx/mqtt/server/bus/MqttServerMessageService.java` | MQTT 订阅路由（`+`/`#` 通配）、会话接管/清理、`IMessageService` 双向桥（下行按订阅匹配 publish、上行分发到 `IMessageConsumer`） |
| `MOD-001` | `nop-kernel/nop-core/src/main/java/io/nop/core/module/ModuleManager.java` | 模块管理器全局单例；`discover()` 扫描 `*/*/_module`，`getEnabledModules()` 供所有消费者遍历 |
| `MOD-002` | `nop-kernel/nop-core/src/main/java/io/nop/core/module/ModuleModel.java` | 模块数据模型；对应 `app.module.yaml` 的 Java bean |
| `MOD-003` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/store/ModuleNamespaceHandler.java` | 实现 `module:` VFS 名字空间；`getResource()` 按模块隔离查找 |
| `MOD-004` | `nop-kernel/nop-core/src/main/java/io/nop/core/resource/ResourceHelper.java` | `getModuleId()`、`getModuleName()`、`getModuleIdFromModuleName()` 等转换方法 |
| `MOD-005` | `nop-kernel/nop-core/src/main/java/io/nop/core/CoreConfigs.java` | `CFG_MODULE_ENABLED_MODULE_NAMES`、`CFG_MODULE_DISABLED_MODULE_NAMES` 配置定义 |
| `AISEC-001` | `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/security/DefaultPathAccessChecker.java` (`checkAccess` + `resolveSymlinkRealPath`) | 路径访问检查默认实现：词法规范化 + 敏感前缀/文件名检查 + symlink 真实路径解析（`toRealPath`，解析失败 fail-closed 拒绝）。设计 §4.3「符号链接绕过」防御锚点 |
| `AISEC-002` | `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/security/DockerSandboxBackend.java` (`execute` + `validateHostPath` + `allowedBaseDirs`) | Docker sandbox 工作目录挂载前必须通过 `validateHostPath` 白名单校验（禁 `..`、必须真实路径、必须在 `allowedBaseDirs` 内），失败抛 `SandboxException(HOST_PATH_NOT_ALLOWED)`，绝不静默挂载 |
| `AISEC-003` | `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/security/Slf4jAuditLogger.java` (`buildMessage` + `sanitize`) | 审计日志默认实现：写入前清理所有字段的 `\n`/`\r`（防日志注入），日志行包含 `timestamp` 与 `actorId`。设计 §4.5 审计字段锚点 |
| `AIREL-001` | `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/DefaultAgentEngine.java` (`agentExecutor` / `callAgentTimeoutMs` / `llmTimeoutMs` / `toolTimeoutMs` / `lockLeaseMs` / `lockRenewIntervalMs` + `getAgentExecutor` + `getLockRenewExecutor` + `startLockRenewal` / `handleLeaseLost`) | 引擎可靠性/超时配置锚点：三个入口点（`doExecute`/`resumeSession`/`restoreSession`）使用专用 executor（`nop-ai-agent-exec` 守护线程池，替代 `ForkJoinPool.commonPool()`）；`callChatWithTimeout` 给 LLM 调用加 wall-clock 超时；工具 fanout per-tool `.orTimeout`；call-agent 超时时 `engine.cancelSession(forced=true)` 取消子 agent；takeover lock 心跳续期（plan 273）——执行期间按 `lockRenewIntervalMs` 周期 `tryRenew` 推后 `LOCK_EXPIRES_AT`（防长时执行租约过期 double-execution），`tryRenew` 返回 false 时 `handleLeaseLost` 中止本侧执行并置 session `failed`，续期任务在所有 release 路径取消。默认值：`callAgentTimeoutMs=60000`、`llmTimeoutMs=120000`、`toolTimeoutMs=300000`（`<=0` 禁用 LLM/工具超时）、`lockLeaseMs=1800000`、`lockRenewIntervalMs=600000`（`<=0` 禁用续期） |
| `META-001` | `nop-metadata/nop-metadata-service/src/main/java/io/nop/metadata/service/query/MetaAggregationExecutor.java` (`executeAggregation` + 7 路径分派) | 指标/维度聚合执行器：按 tableType + joinType 分派到 7 条执行路径（entity 单表 / external 单表 / sql 视图 / entity↔entity join / external↔external join / mixed same-db join / cross-db in-memory）；含 GROUP BY 维度分桶 + aggFunc 指标聚合 + having/orderBy 后处理。计划 `2026-07-19-1250-4` 拆分为 7 个 Processor 已完成（`service/query/` 下 7 个 AggregationProcessor，`MetaAggregationExecutor` 为纯分派器——职责即 7 路径分派，不随行数增长漂移） |
| `META-002` | `nop-metadata/nop-metadata-service/src/main/java/io/nop/metadata/service/tableref/MetaTableReferenceResolver.java` (`resolve`) | 逻辑表 → TableReference 解析：按 tableType（entity/external/sql）分派端点解析；entity → 平台 IOrmSessionFactory 注册的实体；external → 限定表名原生 SQL；sql → sourceSql 子查询。是 queryTableData / profileTable / executeQualityRule 等入口共享的依赖路径 |
| `META-003` | `nop-metadata/nop-metadata-service/src/main/java/io/nop/metadata/service/quality/MetaQualityRuleExecutor.java` (`judge` + `evalExpectPassWhen`) | 单条质量规则执行：支持 ruleType=not_null / unique / regex / volume / custom_sql / field-level 等；evalExpectPassWhen 解析 expectPassWhen 表达式（eq/gt/lt/ge/le + 数字），非法表达式显式抛 `ERR_QUALITY_EXPECT_PASS_WHEN_INVALID`（plan 2026-07-19-1250-3 Phase 5 AR-11） |
| `META-004` | `nop-metadata/nop-metadata-service/src/main/java/io/nop/metadata/service/lineage/SqlColumnLineageExtractor.java` (`extract` + `resolveTransformType`) | SQL AST 列级血缘抽取：解析 SELECT 列表达式 → 追溯到源表源列；产出列级 `NopMetaLineageEdge`（含 transformType: direct/derived/aggregated——direct 直接列引用 / derived 派生表达式 / aggregated 聚合函数，与 `_NopMetadataCoreConstants.LINEAGE_TRANSFORM_*` 及 `nop-metadata/nop-metadata-meta/src/main/resources/_vfs/dict/meta/lineage-transform.dict.yaml` 一致）；同包 `SqlSourceTableExtractor` 抽取表级血缘 |
| `META-005` | `nop-metadata/nop-metadata-service/src/main/java/io/nop/metadata/service/quality/MetaQualityCheckpointScheduler.java` (`init` + `registerCheckpoint` + `executeScheduledCheckpoint`) | cron 调度器：启动 scanner 全量注册 ACTIVE 检查点的 cron job（D4）；运行时增量（save/delete override 调用 register/unregister）；BeanMethodJobInvoker 反射调用 `executeScheduledCheckpoint` 委托 raw impl `NopMetaQualityCheckpointBizModel.executeCheckpoint`（D3 path b，绕过 BizProxy 事务隔离问题） |
| `PERSIST-001` | `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java` (`tryResetLease` / `releaseLock`) + `nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/mfa/store/DbMfaChallengeStore.java` + `nop-persistence/nop-orm/src/main/java/io/nop/orm/loader/JdbcQueryExecutor.java`（`eql.isAllowUnderscoreName()`） | **直接 SQL 使用边界的规范锚点**（plan 2255）：EQL 条件更新/删除规范样例（实体名 + `session.executeUpdate` 返回 affected rows，带 version/holderId 条件）；raw SQL 豁免样例（单语句原子计数/一次性消费/绕过会话缓存读）；`orm()` 执行的文本一律按 EQL 编译（物理表名需 `allowUnderscoreName`，否则 `ERR_EQL_UNKNOWN_ENTITY_NAME`）。判定规则见 `../02-core-guides/model-first-development.md`「直接 SQL 的使用边界」 |

## AI 网关 failover 锚点

| 规则 ID | 锚点 | 说明 |
|---------|------|------|
| `AIGW-001` | `nop-ai/nop-ai-gateway/src/main/java/io/nop/ai/gateway/failover/ChatServiceFailoverAdapter.java` | 本地形态透明账号切换适配器（`IChatService` 包装 `nopChatService`）：非流式切换链（分类 → 熔断记账 → 重选 → 预算 → fail-loud）+ 并发 acquire/复查 + CACHE_STATE_LOST 原地重发；bean `nopChatServiceFailoverAdapter`（非默认，部署方显式切换注入目标） |
| `AIGW-002` | `nop-ai/nop-ai-gateway/src/main/java/io/nop/ai/gateway/failover/FailoverStreamFlow.java` | 本地形态流式缓冲/重订阅：首段缓冲窗口（N/T 可配置）→ 窗口内失败重订阅（消耗预算）→ 窗口外断流；并发 +1/-1 配对；per-attempt token + 取消顺序契约 |
| `AIGW-003` | `nop-ai/nop-ai-gateway/src/main/java/io/nop/ai/gateway/failover/AiGatewayFailoverInterceptor.java` | 网关形态拦截器（`IGatewayInterceptor`）：onRequest 接管 + converter 转换 + properties 下沉；invoke 非流式切换/重试；onError 分类（rethrow/降级终止）；onStreamElement per-attempt 反向转换；onStreamComplete 流式成功熔断恢复 |
| `AIGW-004` | `nop-ai/nop-ai-gateway/src/main/java/io/nop/ai/gateway/failover/GatewayStreamingRetryCallback.java` + `GatewayStreamingLifecycleListener.java` | 网关流式重执行回调（窗口内失败 → 新 `HttpRequest`，null = 断流）+ 生命周期监听器（fetch 发起 +1 / 流终止 -1，重订阅配对）；经 `IGatewayContext` attribute 注入（挂载契约见 `docs-for-ai/03-modules/nop-ai-gateway.md`） |
| `AIGW-005` | `nop-service-framework/nop-gateway/src/main/java/io/nop/gateway/core/executor/BufferedStreamingPublisher.java` | nop-gateway 缓冲/重订阅层（零 nop-ai 依赖）：`bufferEnabled`/`bufferSize`/`bufferTimeMs`（gateway.xdef `streaming`）；`IStreamingRetryCallback`/`IStreamingLifecycleListener` 通用扩展点 |
| `AIGW-006` | `nop-ai/nop-ai-core/src/main/java/io/nop/ai/core/routing/ModelClassRouter.java` + `ISelectionStrategy.java` + `RuleBasedSelectionStrategy.java` + `ConcurrencyRegistry.java` | 模型类路由组游走原语 + 选择策略接口/默认/规则实现 + 并发注册表（nop-ai-core，编排层消费；全池饱和 `ERR_AI_MODEL_CLASS_SATURATED` fail-loud） |
| `AIGW-007` | `nop-ai/nop-ai-core/src/main/java/io/nop/ai/core/reliability/ThresholdBreaker.java` + `LlmErrorClassifier.java` | 熔断三态（CLOSED/OPEN/HALF_OPEN + 冷却 + 探活，粒度 `provider:model`）+ 传输异常级错误分类（复用机制，不新建判定） |
| `AIGW-008` | `nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/ai/model-class.xdef` + `llm.xdef` + `gateway.xdef` | 配置面：模型类路由组（`_default.model-class.xml` opt-in）、`<accounts>` + `concurrencyLimit`（provider 级缺省 + 账号级覆盖）、gateway `streaming` 缓冲参数 |
| `AIGW-009` | `nop-ai/nop-ai-gateway/src/main/java/io/nop/ai/gateway/failover/IFailoverMetrics.java` + `FailoverMetricsImpl.java` | 指标契约接口 + micrometer 默认实现（bean `nopAiFailoverMetrics`，`ioc:default="true"`）；指标族 `nop.ai.gateway.failover.*` 完整契约（维度/单位/语义/触发事件/近似语义）见需求规格文档 §3.6 |

## nop-plugin 锚点

| 规则 ID | 锚点 | 说明 |
|---------|------|------|
| `PLG-001` | `nop-core-framework/nop-plugin/nop-plugin-api/src/main/java/io/nop/plugin/api/IPlugin.java` | 单层六态状态机契约：`load/unload/activate/deactivate/getState/getService(s)/updateConfig/invokeCommand/isStateMachineAware` default 方法（默认实现显式失败）+ 兼容路径语义 |
| `PLG-002` | `nop-core-framework/nop-plugin/nop-plugin-api/src/main/java/io/nop/plugin/api/PluginState.java` | 单层六态状态机契约（2026-08-22 R1 定位反转：getService/INACTIVE 快速失败语义并入 `IPlugin`，一个定义至多一个激活；生命周期实现见 `VfsPluginDefinition`） |
| `PLG-003` | `nop-core-framework/nop-plugin/nop-plugin-api/src/main/java/io/nop/plugin/api/IPluginScope.java` | effect 自管理契约：`effect(Disposable)`/`effects()`（LIFO 回退 quiescence）；激活期 `getService` 返回真实 bean |
| `PLG-004` | `nop-core-framework/nop-plugin/nop-plugin-api/src/main/java/io/nop/plugin/api/IPluginActivator.java` | 激活器双参数契约：`activate(scope, config)`（返回值非 null 自动注册 effect） |
| `PLG-005` | `nop-core-framework/nop-plugin/nop-plugin-manager/src/main/java/io/nop/plugin/manager/IPluginManager.java` | manager 契约：双轨路由（坐标→uber jar / 路径→VFS `*.plugin.xml`）、`loadPlugin/unloadPlugin/activatePlugin/deactivatePlugin/getPlugin/getLoadedPlugins/reloadPlugin`（HMR，jar 轨显式失败）/`reconcilePlugins`（插件级 coeffect 批量编排） |
| `PLG-006` | `nop-core-framework/nop-plugin/nop-plugin-manager/src/main/java/io/nop/plugin/manager/impl/PluginManagerImpl.java` (`checkChangedAndReload:605`) | HMR 显式检查入口（宿主定时调用；框架不起轮询线程）；允许 ACTIVATED/中间态定义进入变更检测（仅跳过 UNLOADED）；`ResourceComponentManager.checkChanged` 严格 lastModified 比对 |
| `PLG-007` | `nop-core-framework/nop-plugin/nop-plugin-manager/src/main/java/io/nop/plugin/manager/resolver/HttpPluginResourceResolver.java` | artifact 下载 + SHA256 校验：hash 来源优先级 header → `{url}.sha256` → `expectedHashes` map；fail-fast（`ERR_PLUGIN_SHA256_MISMATCH`/`ERR_PLUGIN_CHECKSUM_NOT_AVAILABLE`）；缓存重验/遗留重下载/`nop.plugin.skip-cache-verify` |
| `PLG-008` | `nop-core-framework/nop-plugin/nop-plugin-api/src/main/resources/_vfs/nop/schema/plugin/plugin.xdef` | 插件定义 schema：`requires`（csv-set）/`if-property`（`propName\|expectedValue`）/`activator`（bean-name）/`<beans>`（复用 beans.xdef）；属性集冻结（守护测试 `TestPluginXdef#testAttributeSetFrozen`） |
| `PLG-009` | `nop-core-framework/nop-plugin/nop-plugin-support/src/main/java/io/nop/plugin/support/AbstractPlugin.java` | jar 轨基类：aware 路径 `start/stop` = `load+activate` / `deactivate+unload`（门控恒空集无条件激活、激活回调跳过）；非 aware 插件走旧 start/stop 语义 |

## 当前最重要的校准点

1. 不要把 `gen-service.xgen` / `gen-web.xgen` 写成当前仓库的通用生成链路。
2. 不要在普通 BizModel 示例里把直接 `dao()` 访问写成默认做法。
3. 不要把 `@BizMutation @Transactional` 写成普通服务层模板。
4. 不要在 IoC 示例里出现 `@Inject private Foo foo;`。
5. 不要把 infra/store 层的 DAO 直接操作误写成通用业务层模式。
6. 不要把固定调试 URL 当作平台不变量；优先按 DevDoc / DevTool 的 GraphQL 能力理解。
7. 不要把 ORM 模块级 TOPM icon 的来源误写成 `/{moduleId}/orm/app.orm.xml`；当前默认链路是 `model/*.orm.xml` 根 `<orm ext:icon>` -> `*-meta/module-meta.json` -> `*-web`。

## nop-stream 锚点

| 规则 ID | 锚点 | 说明 |
|---------|------|------|
| `STRM-001` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/model/StreamModel.java` | 管道规范模型容器，持有 `StreamComponents` 和 transformations map |
| `STRM-002` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/model/StreamComponents.java` | 管道元数据注册表：transforms/streams/windowingStrategies/requirements/checkpointParticipants |
| `STRM-003` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/model/StreamModelFingerprint.java` | 编译期 fingerprint，`isCompatibleWith()` 拒绝不兼容 requirements 组合 |
| `STRM-004` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/graph/StreamGraphGenerator.java` | Transformation DAG → StreamGraph （Graph Path 第一阶段），含 `populateStreamModel()` |
| `STRM-005` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/graph/StreamGraph.java` | 逻辑流拓扑；现携带 `StreamModel` |
| `STRM-006` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/jobgraph/JobGraphGenerator.java` | StreamGraph → JobGraph（Graph Path 第二阶段，算子链化优化）；现传播 `StreamModel` |
| `STRM-007` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/jobgraph/JobGraph.java` | 可执行 DAG；现携带 `StreamModel` |
| `STRM-008` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/graph/PartitionedPlanGenerator.java` | JobGraph → PartitionedPlan；负责 fingerprint 验证 |
| `STRM-009` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/common/typeutils/IStreamSerializer.java` | `TypeSerializer` 子接口，增加 `serialize()`/`deserialize()` 方法（G40 桥接） |
| `STRM-010` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/common/typeutils/JsonToolSerializer.java` | `IStreamSerializer` 默认实现，包装 `JsonTool` |
| `STRM-011` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/common/typeutils/TypeSerializer.java` | Flink 风格序列化器接口（copy/clone 方法，无 serialize/deserialize） |
| `STRM-012` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/common/state/StateDescriptor.java` | 状态描述符；现携带 `TypeSerializer` 引用（G41，默认 `JsonToolSerializer`） |
| `STRM-013` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/common/state/backend/memory/MemoryStateSerDe.java` | 序列化桥接：使用 `IStreamSerializer` 从 descriptor（G40 落地） |
| `STRM-014` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/common/state/backend/memory/MemoryOperatorStateBackend.java` | Operator state 后端（Plan 12a） |
| `STRM-015` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/common/state/backend/memory/MemoryKeyedStateBackend.java` | Keyed state 后端 |
| `STRM-016` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/checkpoint/CheckpointCoordinator.java` | Checkpoint 协调器 |
| `STRM-017` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/checkpoint/PendingCheckpoint.java` | 待完成 checkpoint 状态 |
| `STRM-018` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/operators/windowing/WindowOperator.java` | 窗口算子 |
| `STRM-019` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/operators/IWindowOperatorFactory.java` | 窗口算子工厂接口 |
| `STRM-020` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/operators/HeapInternalTimerService.java` | 堆内 timer 服务 |
| `STRM-021` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/execution/InputGate.java` | 多 `InputChannel` 合并读取入口（core/execution）；承担生产 barrier 对齐（`handleBarrierNonRecursive()`）、watermark 合并、channel 阻塞/恢复（`blockConsumption`/`resumeConsumptionAll`） |
| `STRM-022` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/execution/CheckpointBarrierTracker.java` | task 内 barrier ACK 聚合器（core/execution，`@Internal`）；追踪 `operatorsToAck` 计数，收齐后回调 `Consumer<TaskStateSnapshot>` 通知 task snapshot 完成 |
| `STRM-023` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/execution/task/StreamTaskInvokable.java` | 图模型执行 invokable（core/execution/task）；按 JobGraph 位置扮演 SOURCE/MIDDLE/SINK/SELF_CONTAINED 四种角色，持有 `MailboxExecutor` 控制面，主循环 `processInputGate()` |
| `STRM-024` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/execution/GraphModelCheckpointExecutor.java` | checkpoint 启用的执行入口（runtime/execution）；构建 `GraphExecutionPlan` → `CheckpointCoordinator` → `CheckpointBarrierTracker` per subtask，注册 abort handler，处理 DRAIN/CANCEL/SUSPEND 终止 |
| `STRM-025` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/execution/task/TaskExecutor.java` | task 线程池调度器（core/execution/task，`@Internal`）；按 `JobVertex` parallelism 创建 N 个 `Task`/`SubtaskTask` 并提交，`awaitCompletion`/`shutdown` 生命周期 |
| `STRM-026` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/execution/task/SubtaskTask.java` | 分布式执行单元（core/execution/task），`Runnable`，绑定 `Subtask` 实例；状态机 CREATED→RUNNING→CANCELING→COMPLETED/FAILED/CANCELED，`cancel()` 经 `Thread.interrupt()` 中断阻塞读 |
| `STRM-027` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/execution/ResultPartition.java` | 进程内数据交换生产端（core/execution）；`LinkedBlockingQueue<StreamElement>`（容量 1024）+ `END_OF_STREAM` sentinel；`RemoteResultPartition`（runtime/transport）扩展为 `IMessageService` 跨 JVM |
| `STRM-028` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/execution/RecordWriter.java` | 算子输出 facade（core/execution）；按 `IPartitioner` 选择 `ResultPartition`，watermark/barrier 广播到全部分区 |
| `STRM-029` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/operators/AbstractStreamOperator.java` | 算子基类（core/operators）；持有 `IStateBackend`/`IKeyedStateBackend`/`IOperatorStateBackend`、`TimerServiceManager`、`lastSnapshotResult`、`pendingRestoreState`（restore-before-open 桥接） |
| `STRM-030` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/jobgraph/OperatorChain.java` | 算子链融合容器（core/jobgraph）；`open()`/`close()` 生命周期，链内通过 `ChainingOutput` 直接调用，跨链走 `RecordWriter`/`InputGate` |
| `STRM-031` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/operators/TimestampsAndWatermarksOperator.java` | 自动插入的 watermark 注入算子（core/operators）；按 `watermarkInterval`（默认 200ms）周期发射，`WatermarkStrategy` + `TimestampAssigner` + idle 标记 |
| `STRM-032` | `nop-stream/nop-stream-cep/src/main/java/io/nop/stream/cep/operator/CepOperator.java` | CEP 算子（cep/operator）；`open()` 从 `stateBackend` 创建 `IKeyedStateBackend`（无配置时 fallback `MemoryKeyedStateBackend` + WARN），NFA state / element queue / SharedBuffer 全部落到 keyed state store |
| `STRM-033` | `nop-stream/nop-stream-connector-jdbc/src/main/java/io/nop/stream/connector/jdbc/JdbcTwoPhaseCommitSink.java` | 事务型 JDBC sink（Stage 52）；extends `TwoPhaseCommitSinkFunction<IN>`，内存缓冲模型 + epoch ledger 幂等 commit（复合键 `(epoch_id, subtask_id)`），对 JDBC 目标实现 exactly-once 输出（**P=N：LOCAL 已证明（CI 可复现）；真实多 JVM 形态 gated 手动验证（CONN-01 successor，F-07 诚实化）**：per-subtask 拷贝 + 台账复合键；跨并行度**恢复**被 typed 拒绝 `ERR_STREAM_2PC_SINK_PARALLELISM_CHANGE_UNSUPPORTED`） |
| `STRM-034` | `nop-message/nop-message-debezium/src/main/java/io/nop/message/debezium/engine/NopStreamOffsetBackingStore.java` | CDC offset 存储（Stage 53）；implements Kafka Connect `OffsetBackingStore`，in-memory `ConcurrentHashMap<ByteBuffer,ByteBuffer>` + 静态 connector-name registry 桥接 source function 实例与 engine 反射实例；`toSerializable/fromSerializable` base64 序列化供 checkpoint |
| `STRM-035` | `nop-stream/nop-stream-connector-debezium/src/main/java/io/nop/stream/connector/debezium/DebeziumCdcSourceFunction.java` | CDC source（Stage 53）；implements `CheckpointedSourceFunction<ChangeEvent>` + `DrainableSource`，`snapshotState`/`initializeState` 经 `"cdc-offsets"` key round-trip offset map，`config` 非 transient（`DebeziumConfig implements Serializable`） |
| `STRM-036` | `nop-stream/nop-stream-connector/src/main/java/io/nop/stream/connector/file/FileTwoPhaseCommitSink.java` | exactly-once 文件 sink（Stage 53）；extends `TwoPhaseCommitSinkFunction<IN>`，temp file + `Files.move(ATOMIC_MOVE)` + manifest 原子更新，幂等 commit 守卫，final-exists/manifest-missing 修复（**P=N 已证明（LOCAL，CONN-01 successor）**：per-subtask 拷贝 + `.sK` 后缀输出隔离；跨并行度**恢复**被 typed 拒绝 `ERR_STREAM_2PC_SINK_PARALLELISM_CHANGE_UNSUPPORTED`） |
| `STRM-037` | `nop-stream/nop-stream-cep/src/main/java/io/nop/stream/cep/nfa/sharedbuffer/SharedBuffer.java` + `nop-stream/nop-stream-cep/src/main/java/io/nop/stream/cep/operator/CepOperator.java`（`registerCacheStatisticsTimer` / `onCacheStatisticsTimer` / `releaseCacheStatisticsTimer`） | CEP SharedBuffer 缓存原语（Stage 54，G65）；`eventsBufferCache`/`entryCache` 为 Guava `Cache`（`maximumSize` + `recordStats` + `RemovalListener` 仅 SIZE 驱逐 debug 日志），替代原双结构竞态 `LruCache`；`SharedBuffer.logCacheStatistics()` 输出 hit/miss/eviction/size；`CepOperator` 经 `getProcessingTimeService().registerTimer(ts, this::onCacheStatisticsTimer)` 注册**周期性**统计 timer（独立于 `onProcessingTime` CEP 事件处理回调），按 `CEP_CACHE_STATISTICS_INTERVAL` 间隔 re-arm anchored 到 fire time，`close()` cancel future；**2026-08-13（plan `2026-08-13-0132-1`）接线状态更新**：`ProcessingTimeService` 已生产接线——`StreamTaskInvokable` 构造函数无条件注入 `TaskProcessingTimeService`/`TimerServiceManager` 到算子链（先于任何 open），`ProcessingTimeServiceDriver`（daemon）在 `invoke()` 启动处 start、finally shutdown，经 mailbox 投递 fire mail 在 task 线程触发回调；无服务时 `registerCacheStatisticsTimer` null 守卫 + WARN（不 NPE、不静默跳过），`inProcessingTime()` 模式 `processElement` 服务缺失时显式抛异常 |
| `STRM-038` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/metrics/StreamMetricsRegistries.java` | nop-stream 进程级组合 MeterRegistry（item 16 P-REQ-1）：base `SimpleMeterRegistry` 成员保累计值，后端成员（如 Prometheus）可随时挂载/摘除；指标名表唯一权威位置 `docs-for-ai/03-modules/nop-stream.md` |
| `STRM-039` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/ops/StreamOpsHttpServer.java` | 运维 HTTP 端点（item 16 P-REQ-3/5/6 载体）：JDK 内建 HttpServer 宿主 coordinator 进程，`GET /metrics`（TextFormat 0.0.4 默认 + OpenMetrics Accept 协商）+ `/jobs` 生命周期端点族 + `/jobs/{jobId}/checkpoints`（overview/history 含 failureCause）+ `/jobs/{jobId}/threaddump`；未启用配置 start() 显式抛 `IllegalStateException`（显式关闭语义） |
| `STRM-040` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/ops/OpsJobManager.java` | 多作业运维管理器（item 16 P-REQ-5/11/12）：submit 与 launch 同路径接线（工厂制品 → durable 恢复 → remote-deploy → 分布式 commit/abort → start → assignTasks → deployTask）+ 治理扫描（checkpoint 观测历史条数/时长双约束裁剪 + 终态作业记录保留）+ 告警服务注册 |
| `STRM-041` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/health/JobHealthStateMachine.java` | 逻辑健康状态机（item 16 P-REQ-7）：七态迁移合法性表 fail-fast（非法迁移/自迁移抛 `IllegalStateException`）、`JobHealthListener` 派发、`job health transition:` 日志锚点；由 JobCoordinator 真实生命周期事件驱动（start/globalRecovery/failJob/terminate/durable-checkpoint 回愈） |
| `STRM-042` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/alert/AlertService.java` | 告警服务（item 16 P-REQ-12）：路由表 JOB_FAILED→ERROR、RECOVERY_STARTED/JOB_DEGRADED→WARN；渠道异常 containment；`fromProperties` 配置键族（logging 默认开、webhook 启用时 url 必填 fail-fast）；渠道实现 `LoggingAlertChannel`（`nop-stream alert:` 前缀）/ `WebhookAlertChannel`（异步有界队列投递） |
| `STRM-043` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/maintain/StreamStateResetTool.java` + `StreamMaintenanceMain.java` | 状态重置工具（item 16 P-REQ-10）：清理 `<base>/<jobId>/`（durable checkpoint + manifest + source cursor），拒绝语义（非重放源/活跃 coordinator/目录不存在）显式报错；与离线 reshard 收敛为同一维护入口族（`reset-state`/`reshard` 子命令） |
| `STRM-044` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/event/StreamJobEventBus.java` | 作业事件总线（item 16 P-REQ-2）：JobCoordinator/CheckpointCoordinator 真实路径同步派发，监听器异常捕获记录；`LoggingJobEventListener` 内建（`nop-stream job event:` 前缀 = 多 JVM 日志断言锚点） |
| `STRM-045` | `nop-stream/nop-stream-rocksdb/src/main/java/io/nop/stream/rocksdb/metrics/RocksDBMetricsRecorder.java` | RocksDB 状态后端指标 recorder（item 16 P-REQ-8）：`nop.stream.state.rocksdb.*` gauge 族（block cache/memtable/compaction/键量级），挂点 `RocksDBKeyedStateBackend` 打开路径 |
| `STRM-046` | `nop-stream/quickstart/`（`generate.sh` + `verify.sh` + `template/`） | 快速起步脚手架（item 17 P-REQ-25）：复制/替换生成入门 Maven 工程（3 拓扑：最小链路/窗口聚合+keyed state/CEP）；`verify.sh` 为端到端脚本化验证载体（生成 → `mvn test` → 3 拓扑全绿，失败非零退出） |
| `STRM-047` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/connector/registry/StreamConnectorRegistry.java` | 连接器 SPI 注册中心（item 19 P-REQ-28）：of/fromContainer 按类型聚合工厂 bean、方向作用域类型名解析（别名次级）、构建期校验（重复/描述符一致性）、未知类型 fail-fast（含已注册清单，`ERR_STREAM_CONNECTOR_TYPE_NOT_FOUND`） |
| `STRM-048` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/connector/registry/StreamConnectorCatalog.java` | 注册中心维护/探测工具入口（item 19 预绑定运行时消费者）：`discoverConnectorBeansResources()` VFS 发现 `connector-*.beans.xml` + `renderListing()` 清单 + `probe()` 探测（解析→构造→单一事实源运行期校验→关闭）；字段级 conf 校验显式不在此（item 20 边界） |
| `STRM-049` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/connector/registry/`（`IStreamConnectorFactory` + `IStreamSourceFunctionFactory`/`IStreamSplitSourceFactory`/`IStreamSinkFactory` + `ConnectorCapabilityDescriptor`/`ConnectorParamDescriptor` + `StreamConnectorConfig`） | SPI 工厂契约族与能力描述符（item 19）：三工厂形态（push SourceFunction / FLIP-27 Source / SinkFunction——不引入新执行契约）；描述符字段与能力矩阵列对齐（交付语义枚举/并行度枚举（2PC ⟹ `PARALLEL` 不变式，CONN-01 successor）/恢复语义枚举摘要/参数规格） |
| `STRM-050` | `nop-stream/nop-stream-connector*/src/main/resources/_vfs/nop/stream/beans/connector-{file,message,jdbc,debezium,batch}.beans.xml` + 同模块 `*ConnectorFactory.java`（8 个） | 8 端点组件注册（item 19）：无状态工厂 bean 经 NopIoC 显式装配承载（文件名不匹配 `app-*.beans.xml` 模式故不进全局容器）；注册资格集 = 仅端点组件（Reader/Builder 显式不注册——非独立实例化端点/非端点辅助物） |
| `STRM-051` | `nop-stream/nop-stream-runtime/src/test/java/io/nop/stream/runtime/connector/TestStreamConnectorRegistryDiscovery.java` + `TestStreamConnectorRegistryE2E.java` | 注册发现测试（P-REQ-28 验收锚点）：5 beans 文件发现→容器装配→8 组件枚举/能力矩阵逐项断言（`docs-for-ai/03-modules/nop-stream-connectors.md` 的代码级单一事实源）/全部 probe 构造；E2E = registry 构造端点 → `env.execute()` → batch-consumer 收集与 2PC file sink 经真实 checkpoint 提交（Anti-Hollow 接线证明） |
| `STRM-052` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/connector/`（`ConnectivityCheckable` + `StreamConnectivityProber` + `ConnectivityProbeOutcome`） | 提交前探测契约与驱动（item 20 P-REQ-13）：additive 能力接口（jdbc-2pc/file-2pc/batch-loader/debezium-cdc/batch-consumer 实现；message 族不实现=显式 skip）；分派序 = 能力接口 → FLIP-27 `Source`（H-1 no-op 枚举探测）→ 2PC 基契约（H-5 begin+rollback）→ 显式 SKIP；失败 = `ERR_STREAM_CONNECTIVITY_CHECK_FAILED` 显式码，绝不静默通过 |
| `STRM-053` | `nop-stream/nop-stream-flow/src/main/java/io/nop/stream/flow/validate/`（`StreamConfValidator` + `StreamConfValidationReport` + `ValidationIssue`） | conf-validate/dry-run 校验核心（item 20 P-REQ-14，D7）：层 1 xdef 字段级 + 层 2 完整图构建不 execute（XDSL 模式）/ 描述符参数字段级 + catalog 构造（连接器模式）+ 层 3 逐端点探测（`--connect`）；逐条结构化错误（层号+端点+错误码+**选项名必含**）；exit 0（含显式 SKIP）/1/2 契约 |
| `STRM-054` | `nop-stream/nop-stream-runtime/src/main/java/io/nop/stream/runtime/maintain/StreamConfValidateCommand.java`（+ `StreamMaintenanceMain` 子命令） | 提交前校验入口族成员（item 20 P-REQ-13/14，D1）：`conf-validate file=<path> [--connect]` / `dry-run file=<path>`；bean 来源三形态（程序化 resolver 重载 / 显式容器包装 `BeanContainerFunctionResolver`（flow）/ 缺省全局容器）；用法错误 exit 2（VFS 优先、本地文件回落的路径解析） |
| `STRM-055` | `nop-stream/nop-stream-core/src/main/java/io/nop/stream/core/credentials/StreamCredentialSupport.java` + `nop-stream-connector-debezium/.../DebeziumCdcSourceFunction.java`（provider 注入 + `effectiveEngineConfig`） | 凭据加密接入（item 20 P-REQ-14，D4）：`credential:{id}#{field}` 引用语法 + 经 `ICredentialProvider` 唯一解密点解析 + 三类 fail-closed typed 错误码；CDC 必达点 = 引用串驻留 Serializable 配置、解密只在引擎侧瞬态副本（序列化路径永无明文，transient provider 每 JVM 重注入）；解密成功=凭据可达（不依赖 `testCredential()` W2 桩）；core 唯一新增依赖 `nop-credential-api`，`nop-message-debezium` 零变更 |
| TS-001 | `nop-treesitter`: `TSParser.parse` / `TSTree`（不可变树 + S-expression 渲染） | 解析入口与树的公共契约；错误输入返回 ERROR/MISSING 恢复树而非异常 |
| TS-002 | `nop-treesitter`: `Language.fromClasspath("/grammars/<name>/tree-sitter-<name>-blob.bin")` | 五内置语法 blob（json/java/javascript/typescript/tsx）；blob 格式 v4 见模块内 `blob-format.md` |
| TS-003 | `nop-treesitter`: `ITreeSitterLanguageProvider`（`META-INF/services` 扩展，custom 覆盖内置同名语法） | 语法注册 SPI；默认实现为 IoC bean `treeSitterLanguageProvider` |
| TS-004 | `nop-treesitter`: `TreeSitterBizModel`（GraphQL `TreeSitter__parseTreeSitter`，错误码 `nop.err.treesitter.unknown-language`） | GraphQL 门面；未知语法抛带码 NopException |

## 当 `docs-for-ai` 仍有歧义时

1. 先回到这里找锚点。
2. 优先对锚点对应的类、接口、方法做 LSP / definition lookup。
3. 只有在文档维护或阻塞性例外场景下，才直接读取少量源码。
4. 一旦发现 `docs-for-ai` 有误，先修正文档，不要把“去读源码”变成默认工作流。

## 相关文档

- `../INDEX.md`
- `../00-start-here/ai-defaults.md`
- `../02-core-guides/domain-logic-and-ddd.md`
- `../02-core-guides/dto-json-and-message-beans.md`
- `../02-core-guides/model-first-development.md`
- `../02-core-guides/service-layer.md`
