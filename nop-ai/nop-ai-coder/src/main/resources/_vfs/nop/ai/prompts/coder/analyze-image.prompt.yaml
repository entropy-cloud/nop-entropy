template: |
  请分析所附截图，按照指定markdown格式返回。如果某部分无内容，则该部分（包括其标题）在输出中可以省略。
  
  【返回格式】
  ### 提取表单
  <<无表单时整段省略>>
  
  ### 提取表格
   <<无表格时整段省略>>
  
  ### 提取mermaid图
   <<无图形时整段省略>>
  
  ### 主要业务需求或业务规则
   <<无内容时输出无>>

outputs:
  - name: RESULT
    type: String
    format: markdown