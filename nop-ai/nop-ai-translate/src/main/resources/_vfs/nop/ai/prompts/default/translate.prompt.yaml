template: >
  将以下${fromLang}技术文档的片段翻译为清晰、准确且专业的${toLang}。保留markdown格式（如标题、列表、代码块、链接等）。
  注意，输出结果只包含译文,不要加额外的说明，不需要放到markdown代码块中，不要拆分原文中的code block，不要修改原文中markdown的层次,不要增加原文中不存在的markdown标题。
  
  待翻译的内容如下：  
  
  ${content}
    
