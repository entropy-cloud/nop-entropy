<plan x:schema="/nop/schema/ai/agent-plan.xdef" xmlns:x="/nop/schema/xdsl.xdef"
      title="Dangling Deps Plan" status="pending">

    <purpose>Plan with a dangling dependsOn reference for validation test</purpose>

    <goal>Should be rejected at load time by AgentPlanValidator</goal>

    <phases>
        <phase name="P1" status="pending">
            <tasks>
                <task taskNo="A" title="Task A" dependsOn="NONEXISTENT" status="pending">
                    <instructions>A depends on a task that does not exist</instructions>
                </task>
            </tasks>
        </phase>
    </phases>

</plan>
