template: >
  {{include: generate-mindmap.prompt.md}}

inputs:
  - name: repositoryUrl
  - name: branchName
  - name: maxDepth
    defaultExpr: 5
  - name: maxNodes
    defaultExpr: 50

outputs:
  - name: RESULT
    type: json