quarkus:
  log:
    level: INFO

    category:
      "io.nop":
        level: INFO

nop:
  application:
    locale: en

  orm:
    init-database-schema: true

  datasource:
    driver-class-name: org.h2.Driver
    jdbc-url: jdbc:h2:mem:test
    username: sa
    password: