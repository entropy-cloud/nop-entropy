# roadmap item 35 (plan 2026-09-24-1400-1 enum #29, R1 F9 直接落地): a stray
# `;` is an empty statement — almost always a leftover (the classic
# `if (x); doY();` accident included, since the empty_statement is the if's
# consequence).
id: quality/no-empty-statement
language: Java
severity: warning
message: "孤立的分号是空语句 (Stray semicolon is an empty statement; remove it)"
rule:
  kind: empty_statement
metadata:
  category: quality
  autoFixable: false
  version: "1.0"
  source: "ai-dev/plans/nop-lint/2026-09-24-1400-1-rule-library-48.md enum #29"
