class Hot {
    int score(int a, int b) {
        int total = 0;
        if (a > 0) { total += 1; }
        if (a > 1) { total += 2; }
        if (a > 2) { total += 3; }
        if (a > 3) { total += 4; }
        if (a > 4) { total += 5; }
        if (a > 5) { total += 6; }
        if (a > 6) { total += 7; }
        if (a > 7) { total += 8; }
        if (a > 8) { total += 9; }
        if (a > 9) { total += 10; }
        if (a > 10) { total += 11; }
        return total;
    }
}
