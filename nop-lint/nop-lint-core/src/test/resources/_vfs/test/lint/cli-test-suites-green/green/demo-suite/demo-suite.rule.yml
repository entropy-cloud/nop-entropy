# The nop-lint test subcommand's green fixture (roadmap item 39): loads the
# canonical fixture rule through the XDSL pipeline like every RuleTester
# suite. Layout: <suites-root>/<category>/<rule-name>/.
x:extends: /test/lint/cli-rules/demo/no-print.rule.yml
