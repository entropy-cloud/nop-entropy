# nop-lint check CLI test rule (plan 2026-09-22-1045-1): a warning-severity
# pattern rule over the typescript binding (language TypeScript resolves
# case-insensitively to the ServiceLoader-discovered binding).
id: demo/ts-no-console
language: TypeScript
severity: warning
message: "Do not use console.log for logging"
rule:
  pattern: "console.log($$$ARGS)"
metadata:
  category: style
  autoFixable: false
  version: "1.0"
  source: "test-fixture"
