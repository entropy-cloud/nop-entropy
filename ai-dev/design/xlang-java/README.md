# xlang-java 设计（占位）

> 本目录为 mission `xlang-execution-optimization` W1-design 工作项产出物占位。
> 计划产出：`README.md`（更新本文件）+ `01-architecture-baseline.md`（Executable→Java 转译器、节点映射策略、SourceLocation 保真、生成类加载与 ResourceComponentManager 集成、_gen 构建任务、EvalMethod 调用约定）。
> 目标模块：`nop-kernel` 下新建 `nop-xlang-java`（W 阶段后由 I1 创建，当前不存在）。
> 在 W1-design 完成前本文件不承载任何设计结论。
