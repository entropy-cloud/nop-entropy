# 2026-06-10

## Goal Driver Dryrun 修复

**Scope**: `ai-dev/tools/opencode-goal-driver/`

### 改动内容

1. **`runner.js`**: 修复 mock 响应使用正确的 `<AI_STEP_RESULT>` tag（原来用 `<ROADMAP_RESULT>` 等不匹配的 tag 导致 dryrun 立即失败）
   - 所有 mock 响应统一使用 `<AI_STEP_RESULT>` 标签
   - `PLAN_DRAFT` mock 增加 `<FLOW_VARS><PLAN_FILE>` 输出
   - `ROADMAP_CHECK` mock 增加 `<NEXT_ITEM>` 输出
   - 新增 `BUILD_VERIFY` mock 响应（返回 `pass`）
   - `STEP_KEY_MAP` 增加 `BUILD_VERIFY` 映射

2. **`engine.js`**: `_templateVar` 不再在变量缺失时 throw，改为 WARNING 日志 + 保留原始 `{{var}}` 占位符
   - 防止 PLAN_FILE 缺失时整条流程崩溃

3. **`build-verify.md`**: 修复第 9 行缩进错误（` 3.` → `3.`）

### 验证
- `node --test test/engine.test.js` — 54 tests, 0 fail
- `node src/main.js nop-ai-agent --dry-run` — Status: completed, 20 steps, 0 failures

---

## Plan 130: StepResult Unification — Phase 1 完成

**Scope**: `ai-dev/tools/opencode-goal-driver/src/engine.js`, `ai-dev/tools/opencode-goal-driver/test/engine.test.js`
**Plan**: `ai-dev/plans/130-goal-driver-engine-stepresult-unification.md`

### 改动内容

1. **`engine.js`**: 全面重构所有步骤执行器为统一返回 `StepResult { marker, vars, ok, text }`
   - `_executeToolStep()`: delegate 返回值转换为 `{ marker: ok?"pass":"fail", ok: true, vars: {}, text }`
   - `_executeScriptStep()`: 返回值规范化为 StepResult（含 vars 支持）
   - `_executeScriptStepWithOverride()`: delegate.runScript 返回值包装为 StepResult
   - `_executeAgentStep()`: 完整 marker 提取链（XML tag + parse agent fallback + correction）内部化，vars 不再旁路写入 flowVars
   - 新增 `_runCorrectionAgent()`: 独立辅助方法，递归保护，直接调用 `delegates.runAgent`
   - `_executeGroupStep()`: 移除 `subDef.type !== "tool"` 硬编码，使用 `result.marker`，聚合子步骤 vars
   - `_runChildSubflow()`: 返回 `{ childResult, childFlowVars }` 而非旁路写入
   - `_executeSubflowStep()`: 通过 StepResult.vars 传播变量（单个和 forEach last-write-wins 聚合）
   - `run()` 主循环: 删除 `_resolveMarker()` 调用，添加 `result.vars` 统一合并，移除 `stepDef.type !== "tool"` 硬编码
   - 删除 `_resolveMarker()` 方法和 `markerCorrectionCounts`

2. **测试**：新增 5 个测试
   - group step 聚合子步骤 vars 到 StepResult.vars
   - subflow step 通过 StepResult.vars 传播变量（单个和 forEach）
   - agent marker correction 在 executor 内部完成
   - group 内 tool sub-step ok:false 不触发 error 路径

### 验证
`node --test ai-dev/tools/opencode-goal-driver/test/engine.test.js` — 54 tests (49 original + 5 new), 0 fail

### 独立 Closure Audit
由独立子 agent 完成，所有 12 条 Exit Criteria PASS，8 条 Closure Gates 7 PASS + 1 (logs) 待完成，Anti-Hollow 检查 CLEAN。

---

## opencode-goal-driver: subflow 步骤类型实现

**Scope**: `ai-dev/tools/opencode-goal-driver/`

### 改动内容

1. **`engine.js`**: 新增 `_executeSubflowStep()` + `_runChildSubflow()` + `_allVars()`
   - 单次执行模式：child `completed` → marker `complete`，其他 → `failed`
   - forEach 模式：从 context var 解析列表，逐项运行 child，聚合为 `all_complete`/`some_failed`/`all_failed`
   - `flowArgs` 模板变量解析（`{{var}}`），child flowVars 传播回 parent
   - 注册 `"subflow"` 类型到 `run()` 主循环和 `_resolveMarker()`

2. **`flow-loader.js`**: `loadSubFlow(name)` 从文件系统动态加载
   - 默认搜索 `flows/{name}.json`，通过 `delegates.config.subflowDir` 可配置
   - 支持 `this` 上下文（`delegates.loadSubFlow = loadSubFlow` 调用时自动绑定）

3. **`main.js`**: `loadSubFlow` 注入 delegates

4. **测试**：8 个新测试覆盖完整 subflow 行为：
   - 单次 complete/failed
   - forEach all_complete/some_failed/all_failed/empty
   - flowArgs 模板解析
   - flowVars 从 child→parent 传播
   - 全部从临时目录动态加载 subflow JSON 文件

### 验证
`node --test test/engine.test.js` — 49 tests, 0 fail

### Doc-sync
- `flow-engine-design.md` §2.7 (Subflow Execution) 新增

## opencode-goal-driver: template syntax `{{var}}` + flowVars 机制

**Scope**: `ai-dev/tools/opencode-goal-driver/`

### 改动内容

1. **Template 语法切换 `{var}` → `{{var}}`**
   - `engine.js`: `_templateVar` regex from `/\{(\w+)\}/g` → `/\{\{(\w+)\}\}/g`，unresolved 直接 throw
   - 9 个 prompt 文件：`{module}` → `{{module}}`，`{PLAN_FILE}` → `{{PLAN_FILE}}`
   - 避免与 AI 默认的 `{...}` 花括号语法冲突，允许 `{YYYY}` 等字面量通过

2. **`flowVars` 动态上下文传递机制**
   - `engine.js`: `flowVars` Map 维护运行时变量池
   - Agent 步骤从输出 `<FLOW_VARS><VAR>val</VAR></FLOW_VARS>` 自动提取变量
   - Script 步骤通过 `{ marker, vars }` 返回值传递变量
   - `_buildPrompt` 合并 `delegates.vars` + `flowVars` 构建模板上下文
   - 优先级：`flowVars` > `delegates.vars` > 字面量

3. **测试适配**
   - 更新 `engine.test.js` 模板变量测试用 `{{var}}` 语法
   - 更新 integration test mock 中 `PLAN_DRAFT` 输出 `<FLOW_VARS>` 和 `EXECUTE` mock
   - 全部 41 测试通过

### 偏差
- 无 plan 文件，ad-hoc 推进（与用户连续对话）

### 验证
`node --test test/engine.test.js` — 41 tests, 0 fail

### Doc-sync
- `flow-engine-design.md` §2.2（template vars）, §2.4（flowVars）已更新
- `context-passing-and-foreach-design.md` 已删除（冗余）

## Plan 129: nop-ai-agent 枚举统一 (L0-2) — completed

## Plan 129: nop-ai-agent 枚举统一 (L0-2) — completed

**Module**: nop-ai-agent
**Work Item**: L0-2

### 完成内容

1. **Phase 1 — Fix record-mappings dict references**
   - `agent-plan.record-mappings.xml`：`taskStatus` → `status`（匹配 `_AgentPlanTaskModel.setStatus()`）
   - dict 引用从 `AgentTaskStatus`/`AgentPlanStatus` 统一到 `AgentExecStatus`
   - 消除运行时 dict lookup 失败和静默数据丢失到 extProps 的问题

2. **Phase 2 — Add test for Markdown status parsing**
   - 新增 `TestAgentPlanRecordMapping`：8 个测试方法
   - 覆盖全部 4 个 AgentExecStatus 枚举值（pending/running/completed/failed）
   - 覆盖 planStatus 和 task status 字段
   - 关键断言：Task 字段名为 `status`（非 `taskStatus`），验证无静默数据丢失
   - 添加 `nop-record-mapping` 测试依赖

3. **Phase 3 — Update roadmap and verify build**
   - Roadmap L0-2 标记为 ✅
   - §5 技术债表和 §7 审计检查清单更新
   - `./mvnw test -pl nop-ai/nop-ai-agent -am` BUILD SUCCESS

### 关键决策

- 不创建独立的 `AgentTaskStatus`/`AgentPlanStatus`，统一到已有的 `AgentExecStatus`（xdef/generated code 已统一使用）
- record-mapping field name `taskStatus` → `status` 修复静默数据丢失 bug（`AbstractComponentModel` 的 `IPropSetMissingHook` 导致值被存入 extProps 而非 status 属性）

### Commits

- `67907bcae` fix(ai): 统一 record-mappings 枚举引用到 AgentExecStatus [L0-2]
- `f59d46de2` test(ai): 添加 AgentPlan record-mapping 状态解析测试 [L0-2]
- `4a0d6fec2` docs(ai): 更新 roadmap L0-2 为完成, 关闭 plan 129 [L0-2]
