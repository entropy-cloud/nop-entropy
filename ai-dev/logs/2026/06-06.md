# 2026-06-06 开发日志

### 2026-06-06 Plan 122 完成：nop-code r10~r12 对抗性审查修复收口

- Plan 122 全部 7 个 Phase 执行完成，29 条审计发现（AR-145~AR-175）全部修复
- Phase 1 (AR-170+AR-155): Auth 权限对齐 — 10 个只读 @BizQuery 从 @Auth(roles="admin") 改为 @Auth(permissions="NopCodeIndex:query")
- Phase 2 (AR-146~150): 多语言适配器修复 — Java RecordDeclaration、Python __init__ 全限定名、Python 相对 import、控制流块遍历、注解值提取
- Phase 3 (AR-158): 缓存不变性修复 — persistSingleFileInSession 创建新 SymbolTable 避免修改缓存实例
- Phase 4 (AR-145/159/160/161): Flow 检测修复 — 多监听器映射、externalCallCount 计数、guessExtension 类名后缀、ExtDataHelper 精确注解匹配
- Phase 5 (AR-171/166/168/167): 索引/搜索修复 — triggerFullIndex null pattern、deleteFileRecords 搜索引擎同步、buildFilePathCache 移除限制、TEXT 搜索模式
- Phase 6 (AR-151/152/153/172/173/174): 图分析修复 — escapeJson 控制字符、接口重载前缀匹配、ORM 唯一约束、图大小检查、双向边统计
- Phase 7 (AR-162/163/164/175/156/157): ChangeAnalyzer + P3 治理 — workingDirectory 参数、超时截断标记、包名提取、类名匹配、死代码标记
- Closure Audit: 独立 subagent (houyi) 验证全部 29 条修复，Anti-Hollow Check 通过，0 空壳实现
- 验证: `./mvnw test -pl nop-code -am` 通过 (436 tests, 0 failures)
- Commits: 7 commits across all phases
- Doc-sync: No owner-doc update required

### 2026-06-06 Agent Memory / Compaction / Session 深度对比分析

- 新增 `ai-dev/analysis/agent-survey/2026-06-06-agent-memory-compaction-session-deep-dive.md`
- 源码级深度调研 10 个框架的 memory/compaction/session 机制（10 个并行子 agent 调研）
- 关键发现：
  - Compaction 策略 4 级分类：多级渐进 (PilotDeck/Reasonix) > 中间件管道 (DeepAgents) > 专用 Agent (OpenCode) > 基础摘要 (pi-agent 等)
  - 修正前期分析错误：Reasonix tool result 阈值 3000→8000 tokens（源码确认）、Hermes 有 ContextCompressor（前期分析遗漏）
  - Token 计数：仅 5 框架用精确 BPE 计数，其余依赖 char/4 估算
  - Session 拓扑：pi-agent 树形 > OpenCode V2 Context Epoch > 其余线性
  - 独特机制：oh-my-pi TTSR 跨 compaction 存活、oh-my-claudecode PreCompact 3-hook 链、Hermes 方言式 Honcho 用户建模
- 对 Nop Agent 设计建议：5 层渐进压缩 + 精确 token 计数 + DB 持久化 + 3 层记忆
- 独立审查完成（houyi subagent）：1 CRITICAL + 3 MAJOR 修正，已全部应用并添加脚注
- Doc-sync: 新增分析文档，前期分析文档待修正 Reasonix 阈值和 Hermes 压缩描述

### 2026-06-06 Nop AI Agent 高层设计：Actor Runtime Vision

- 新增 `ai-dev/design/nop-ai-agent/nop-ai-agent-actor-runtime-vision.md` — 基于 12+ 框架调研成果的 Actor 模式多 Agent 运行时高层构想
  - 核心理念：Virtual Thread Actor + IMessageService 内存队列 + DB 持久化 + 多租户隔离
  - Platform Layer 六大组件：ActorRuntime, ActorRegistry, MessageRouter, TeamManager, RecoveryManager, ResourceGuard
  - Actor 生命周期状态机（7 状态）、团队通信模型、多租户资源配额、自动恢复策略
  - 与 oh-my-opencode Team Mode 的 6 维差异化对比
  - 5 阶段实施路线（Phase 1+4 可并行）
- 更新 `ai-dev/design/nop-ai-agent/nop-ai-agent-multi-agent.md` — 新增 §8 演进方向，引用 actor-runtime-vision 作为 Phase 2+ 具体方案
- 更新 `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` — Phase 4 交付物新增 Actor Runtime 平台层引用
- 更新 `ai-dev/design/nop-ai-agent/README.md` — 新增高层构想层分类，更新阅读顺序和文档统计（17→18 篇，5→6 层）
- 独立审查完成（houyi subagent）：8 个发现（2 MAJOR, 4 MINOR, 2 INFO），全部已修正
  - MAJOR: "19 框架"→"12+ 框架"；权限继承归因 oh-my-opencode→OpenCode
  - MINOR: Actor 状态枚举统一(3→7)、IMessageService 模块归属修正、no-orchestrator 标记"Nop 已遵循"、oh-my-opencode "多进程"→"独立 session"
  - INFO: Session 存储 Phase 演进注释、nop-stream CEP 标记 Phase 3+
- Doc-sync: 设计文档索引更新完毕

### 2026-06-06 Plan 121 完成：nop-code 深度审计剩余发现收口

- Plan 121 全部 5 个 Phase 执行完成 + 独立 closure audit 通过
- Phase 1（安全修复）: CodeSearchService ReDoS防护(globToRegex)、NopCodeIndexBizModel 1MB长度限制、CodeIndexService路径校验加固(WARN日志+绝对路径拒绝)
- Phase 2（ORM/字典）: 创建code/semantic_relation_type字典(8值)、NopCodeDependency引入dependencyKeyHash解决MySQL唯一键限制、NopCodeSemanticEdge添加displayName/icon
- Phase 3（测试质量）: TestFlowDetector添加criticality断言、TestBizModelErrorPaths消除恒真断言、TestNopCodeFlowBizModel内容验证、TestNopSearchIntegration替换assumeTrue
- Phase 4（代码质量）: 删除entityToCodeSymbol/NopCodeException/NopCodeConstants/NopCodeConfigs、修复import格式、清理重复注释、GraphSnapshot EdgeKey NPE修复、Community可变列表、21个源文件通配符import替换
- Phase 5（文档与P3）: 创建docs-for-ai/03-modules/nop-code.md、审计字段统一、JavaFileAnalyzer LOG.debug、indexLocks内存泄漏修复、errorMessage设置、provenance字典
- 验证: `./mvnw test -pl nop-code -am` 全部通过
- 独立子 agent closure audit: 28项全部PASS
- Commits: 30a0ffaca, 29586f1ca, 262af072b, dc1dd546a, f60f1a089, 5987e37aa, 14f8cd3ae
- Doc-sync: docs-for-ai/INDEX.md 更新路由、docs-for-ai/03-modules/nop-code.md 创建

### 2026-06-06 Plan 120 完成：nop-code P2/P3 审计发现收口

- Plan 120 Phase 7（P3 治理项）执行完成，全部 7 个 Phase 已关闭
- AR-92: `CodeCacheManager` 并发策略统一，`synchronized` 改为 `ReentrantLock`，保持 `LinkedHashMap` accessOrder LRU 语义
- AR-106: `CommunityDetector.calculateCohesion` 去除双向边重复计数，只统计出边（callees）
- AR-122: `CodeSearchService` 评分函数改为连续评分（位置惩罚 + 长度权重），替代固定层级
- AR-142: `usageCount` 死字段在 ORM 中标记 `notGenCode="true"` 停止代码生成
- 深度审计 04: NopCodeSemanticEdge 的 `CREATE_TIME` 统一为 `CREATED_TIME`
- 深度审计 17-01: `size() == 0` 改为 `isEmpty()`
- 验证: `./mvnw test -pl nop-code -am` 全部通过
- 独立子 agent closure audit 完成，全部 Exit Criteria 和 Closure Gates PASS
- Commit: `8f220626e`
- Doc-sync: No owner-doc update required

### 2026-06-06 Plan 119 执行：nop-code r9 对抗性审查 P0/P1 修复

- Plan 119 全部 4 个 Phase 执行完成
- Phase 1 (AR-124): 增量索引路径比较失效——添加 `MappedPathResource` 内部类包装 `IResource`，统一 `pathMapper` 应用于 `currentResources`，使 `IncrementalDetector.detectResourceChanges` 比较的 key 均为相对路径
- Phase 2 (AR-127+AR-128): 并发锁机制统一——提取 `withIndexLock(indexId, Supplier<T>)` 和 `withIndexLock(indexId, Runnable)` 方法，`indexDirectory`/`triggerIncrementalIndex`/`indexFile` 共享同一套 `indexLocks`；移除 `deleteIndex` 中的 `indexLocks.remove(indexId)` 消除锁竞态
- Phase 3 (AR-125): `NopCodeSymbol.view.xml` 的 GraphQL selection 和表单字段 ID 从 `isStatic`/`isAbstract` 改为 `staticFlag`/`abstractFlag`
- Phase 4 (AR-126+AR-129): `indexFile` 添加 `updateIndexStats(indexId)` 调用；`findReferencedBy` 改用 `FilterBeans.in("symbolId", ...)` 查询所有同名符号
- 验证: `./mvnw test -pl nop-code -am` 118 tests pass, 0 failures, 14 skipped
- Commit: `433e55bc7`
- Doc-sync: No owner-doc update required

### 2026-06-06 Plan 117 执行：nop-code 审计发现修复

- Plan 117 全部 4 个 Phase 执行完成
- Phase 1: 在 `nop-code.orm.xml` 的 `<dicts>` 段新增 `code/call_type` 字典（含 CONSTRUCTOR 值），为 NopCodeIndex 实体补充 createTime/updateTime 审计列
- Phase 2: CodeCacheManager 从插入序 LinkedHashMap 改为 access-order LRU + CacheEntry TTL 包装（CACHE_TTL_MS=3600000），新增 TestCodeCacheManager 覆盖 LRU/TTL/容量
- Phase 3: 从 nop-code-service/api/dto 迁移 31 个纯 DTO 到 nop-code-api（io.nop.code.api.dto），保留 SymbolDTO/FileAnalysisDTO/AnnotationUsageDTO（依赖 nop-code-core）。nop-code-service 新增对 nop-code-api 依赖
- Phase 4: 新增 TestGraphAnalysisE2E（getCriticalNodes + getKnowledgeGaps）和 TestFlowAnalysisE2E（detectFlows + analyzeChanges + detectDeadCode）
- 验证: `./mvnw clean install -pl nop-code -am` 通过, `./mvnw test -pl nop-code -am` 通过
- Commits: `1a1dd7bf6`, `da6d5efb7`, `8224c8dd3`, `042b2fc90`
- Doc-sync: No owner-doc update required（ORM 补全和内部模块结构调整不改公共 API 契约）
