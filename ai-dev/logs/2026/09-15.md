# 2026-09-15

### P2 round-4 工具系统语义修复执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0818-3-p2-round4-toolkit.md`，work-item P2-ROUND4-TOOLKIT，4 项 deep-audit round-4 缺陷，nop-ai-toolkit + nop-ai-agent 接口面）

- **Phase 1 update-todos 会话作用域**（裁定 A）：`IToolExecuteContext` 新增 default `getSessionId()`（返回 null，二进制兼容）；`AgentToolExecuteContext.getSessionId()` 加 `@Override`、toolkit `ToolExecuteContext` 增 sessionId 字段 + builder（`builder().sessionId(...)`）；`UpdateTodosExecutor` todo 表改按 `context.getSessionId()` 键控（删除恒返回 `"default"` 的 `getListKey` 空操作路径），**无会话标识 fail-closed**（read/write 均返回显式错误，不落全局表）。回归测试重写 `UpdateTodosExecutorTest`（+跨会话隔离 / +write 空列表只清自己会话 / +无会话 fail-closed / 既有用例按会话键隔离）。
- **Phase 2 executeParallel maxConcurrency 生效**：`ToolManagerImpl.executeParallel` 用 `Semaphore(maxConcurrency, fair)` 限制在飞数——permit 从提交持有到调用完成，超限调用**排队不丢弃**；null/非正数 = 无上限（全部立即提交）；`IToolManager.callTools` javadoc 同步精确语义。回归测试新增 `ToolManagerImplParallelTest` 5 例（maxConcurrency=2 时 5 调用在飞数 ≤2 且达界 / null 无上限 / =1 串行 / 上限大于批长不节流 / 结果保序）。
- **Phase 3 skill load 真实语义**（裁定 B）：`SkillExecutor.handleLoad` 从 VFS `/nop/skills/<name>/` 读真实目录——返回文件清单 + 内联首个描述文件内容（README.txt/README.md/SKILL.md/skill.md/description.txt，超 4000 字符截断标注）；目录不存在/不可读返回显式错误；删除"目录存在即 loaded successfully"空操作。回归测试：`SkillExecutorVfsTest.testLoadVfsSkillReturnsRealContent` 断言具体文件清单 + 描述内容（非成功字符串）+ 空操作消息缺席。
- **Phase 4 SSRF 解析时权威防护接线**（裁定 B，消除 javadoc 虚假声称）：`HttpRequestExecutor`/`GraphqlQueryExecutor` 新增默认 `IDnsResolver = SsrfGuardDnsResolver`（`@Inject @Nullable setDnsResolver` 可覆盖，SearchEngineExecutor 先例），发请求前解析目标主机名——解析到内网/元数据地址（含 DNS rebinding 多答案集）fail-closed 不进传输；`SsrfGuardDnsResolver`/`SsrfAddressGuard` javadoc 改写为实际装配（toolkit 执行器级解析校验对全部 client 生效；redirect-hop client 内拦截仅 Apache 支持，生产部署可装配 `HttpClientConfig.dnsResolver`）。回归测试：两个执行器测试注入 `SsrfGuardDnsResolver(mockDelegate)`——hostname 解析到内网被拒不进传输 / 公共 IP 放行 / resolver 被运行时消费（resolveCount 断言）/ 不可解析 fail-closed。
- 偏差：无（Non-Goals 全遵守——未改 `IToolManager` 公共接口签名、未重接 bash 装配、未跑全量构建）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-toolkit` 全绿（242 tests 0 失败 0 错误，含新增 14 例）；下游 `nop-ai-agent` 3395 tests 零回归（接口 default 方法 + @Override 改动）；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors）。
- Doc-sync: `ai-dev/design/nop-ai-agent/04-tool-invocation.md`（§四 maxConcurrency 生效语义 + §4.1 update-todos 会话作用域 + §4.2 skill load 语义 + §4.3 SSRF 两阶段实际装配）、`docs-for-ai/03-modules/nop-ai.md`（新增"工具执行语义（nop-ai-toolkit，P2 round-4 收口）"小节）、`ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md`（4 项勾选）已更新；`nop-ai-tool-filesystem-design.md` 无相关契约描述，显式 `No owner-doc update required`。

### P2 round-4 可靠性面修复与死面裁定执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0818-2-p2-round4-reliability-surface.md`，work-item P2-ROUND4-RELIABILITY-SURFACE，2 项缺陷 + 2 项死面裁定，nop-ai-core + nop-ai-api）

- **Phase 1 机密收敛 + 表收缩**：`ConcurrencyRegistry.release` 下溢异常消息 accountKey 掩码化（`accountKey=***` / 主账号 `accountKey=null`，与 `ModelClassCandidate.toString()` 掩码姿态一致，错误码 `ERR_AI_AGENT_INVALID_ARG` + ARG_MSG 契约不变）——编排缺陷检测路径不再泄漏备用账号 apiKey；`ThresholdBreaker` 三个状态机方法（allowCall/recordSuccess/recordFailure）全部移入 per-key `entries.compute` 临界区（弃 per-entry synchronized，与 ConcurrencyRegistry 同款 per-key 串行化），**pristine 条目（CLOSED + 0 失败 + 无 probe 在飞）在归位同一次临界区内移除**——带失败记忆/OPEN/probe 在飞条目永不移除，熔断判定数学不变，gateway 任意 model 路由不再无界增长表。回归测试 +5：`TestConcurrencyRegistry.underflowMessageDoesNotLeakAccountKey`（掩码断言 + 明文缺席 + 主账号 null 字样）+ `TestThresholdBreaker` 4 例（100 key 收缩至 0 / 失败记忆保留 / OPEN·HALF_OPEN 不删·恢复后删 / 移除后 fresh 条目按阈值正常熔断，反射断言内部 map 大小）。
- **Phase 2 nop-ai-core `api/` 死面裁定（22 类全部 reserved 保留 + 登记）**：全仓 import 扫描确认 22 个公共类零消费者（embedding/vectorstore/classifier/document/support/tokenizer/chat/commons 族）——`IEmbeddingModel`/`IVectorStore`/`ITokenCountEstimator` 既有 P1-MA5-003 SPI 声明（不动）、`IAiChatProgressListener`/`AiSystemMessage`/`AiToolMessage` 既有 `@Deprecated`（不动）、其余 16 类新增 javadoc `RESERVED`（与 ChatStreamAccumulator 先例同形，记录拒绝删除理由 = SPI 契约族公共面 + nop-ai-rag 落点声明）；清单登记 `ai-dev/design/nop-ai/04-rag-module-position.md` §六（逐类消费状态/javadoc/裁定表）+ `docs-for-ai/03-modules/nop-ai.md` 子模块表。
- **Phase 3 nop-ai-api `crud/` 66 个生成面裁定（保留为生成面 + 显式登记）**：22 `NopAiXxxApi`（`extends ICrudApi<I,O>`）+ 44 Input/Output Bean 全部为 `//__XGEN_FORCE_OVERRIDE__` codegen 产物（平台 `crud-api` 模板，`gen-crud-api.xgen` 触发点）；wire 名与 nop-ai-service 22 个 `NopAiXxxBizModel`（`CrudBizModel<T>`）`@BizModel` 逐一对齐（0 缺口）；全仓 main/test 零直接 import 属平台生成面常态（nop-auth 27/nop-sys 19 同类同零消费）；登记 `docs-for-ai/03-modules/nop-ai.md` 子模块表（nop-ai-api 行）+ `docs-for-ai/02-core-guides/api-model-and-codegen.md` §CRUD API 代码生成"生成面零消费者语义"小节。
- 偏差：Phase 3 owner doc 落点在 `docs-for-ai/`（api-model-and-codegen.md = codegen 契约权威文档），非 `ai-dev/design/nop-ai-agent/`（该目录无 api 契约文档，以实际落点为准）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-api -am` 全绿（nop-ai-core 399 = 394 + 5 新增，nop-ai-api 全绿）；下游 nop-ai-agent 3395 + nop-ai-gateway 193 零回归；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors，9 warnings 全为其他历史计划存量）。
- Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-reliability.md` §3.3/§5.1（ThresholdBreaker 收缩 + per-key compute 线程安全改写）、`ai-dev/design/nop-ai-gateway/02-account-failover-requirement.md` §3.3（下溢消息掩码补记）、`ai-dev/design/nop-ai/04-rag-module-position.md` §六（死面清单）、`docs-for-ai/03-modules/nop-ai.md`（nop-ai-rag 行 + nop-ai-api 行）、`docs-for-ai/02-core-guides/api-model-and-codegen.md`（生成面零消费者语义）、`ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md`（3 项勾选）已更新。

### P2 round-4 LLM 调用路径契约修复执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0818-1-p2-round4-call-path-contract.md`，work-item P2-ROUND4-CALL-PATH，5 项 deep-audit round-4 缺陷，nop-ai-core + nop-ai-api）

- **Phase 1 OpenAI 非流式 tool_calls 解析补齐**（`OpenAiDialect.parseResponse`）：非流式路径解析 `choices.0.message.tool_calls[]`（id/function.name/function.arguments，arguments 兼容 JSON 字符串与结构化 Map）并回填为独立 `ChatToolCallMessage`（单一拆分模型，与其余 4 dialect 解析行为一致）；删除"保持现状"注释；畸形 arguments JSON 不静默吞——WARN 日志（含 callId/name）+ arguments 置 null（与合法 `{}` 空 Map 可区分）；`ILlmDialect.parseResponse` javadoc 声明"全部 5 方言一致解析工具调用，无例外"。回归测试 +3（tool_calls 断言具体字段 + 无 tool_calls 空列表负例 + 畸形 args null 标记）。
- **Phase 2 ChatUsage.copy null 安全 + totalTokens 保真**（裁定 A）：构造器 `promptTokens + completionTokens` 拆箱改 null-safe（仅双非 null 才求和）；`copy()` 改为逐字段复制并显式复制 `totalTokens`（不再经构造器重算，保留 provider 上报的含缓存 token 统计）；`ChatResponse.copy()` 对含 null token 的合法 usage 不再 NPE（经 ChatUsage.copy 修复验证）。回归测试 +5（TestChatUsageCopy 4 例 + TestChatResponse.copy 1 例，含缓存 token 场景 totalTokens 不被重算覆盖）。
- **Phase 3 callAsync null options 守卫 + ToolCallAccumulator 显式处置**：`callAsync` 对 `request.getOptions() == null` null-safe（按无 options 语义，stream 走默认值 true，与 options.stream==null 既有行为等价）；`ToolCallAccumulator.toToolCall` 畸形 arguments JSON 不再静默落空 Map——WARN 日志（`nop.ai.tool-call-args-parse-fail`，含 callId/name）+ arguments 置 null（与合法 `{}` 空 Map 可区分，repair 层 `ArgumentStructureRepairStage` 已能归一 null）。回归测试 +3（TestChatServiceImplCallPathContract null options 经公开入口默认流式路径不 NPE；TestStreamAggregator 畸形 args null + WARN 日志捕获 + `{}` 空 Map 对照）。
- **Phase 4 ChatOptions.merge 列表字段覆盖语义**（裁定 A）：stop/tools 从 `addAll` 追加改为整体覆盖（与 javadoc"非null值会覆盖"一致，router 逐跳 `copy().merge(tierOptions)` 不再重复累加）；全部字段 merge 行为清单复核 = 标量覆盖 + 列表覆盖 + null 不覆盖，实现与 javadoc 无漂移。回归测试 +4（stop 覆盖/tools 覆盖/null 不覆盖/标量语义不变）。
- 偏差：无（Non-Goals 全遵守——未改其余 4 dialect 既有解析行为、未改 ChatUsage/ChatOptions 公开 API 签名、未改 `ILlmDialect` 接口签名、未跑全量构建）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-api -am` 全绿（nop-ai-core 394 + nop-ai-api 57，0 失败 0 错误）；跨模块下游 nop-ai-agent 路由（TestSmartModelRouter 17/9/2 例）+ nop-ai-gateway failover 全绿；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors）。
- Doc-sync: `ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md` 5 项勾选（OpenAiDialect 非流式丢 tool_calls / ChatUsage.copy null NPE / callAsync null options NPE / ChatOptions.merge append / ToolCallAccumulator JSON 吞错）；`No owner-doc update required`——`ai-dev/design/nop-ai-agent/` 各 owner doc（reliability/02-execution-model/llm-layer）无相关契约描述需同步（已逐篇检索确认）。

### P2 round-4 session 生命周期与租约契约修复执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0534-3-p2-round4-session-lifecycle.md`，work-item P2-ROUND4-SESSION，3 项 deep-audit round-4 缺陷）

- **Phase 1 锁门控（resume/wake 锁前零就地变更）**：`AgentSessionLifecycle.resumeSession` 的 tenant 作用域 reset 块（denialLedger.reset / postDenialGuard.reset / setStatus(running) / SESSION_RESUMED 事件）整体后移至 `tryAcquire`+`putIfAbsent` 成功之后（异常传播仍经 slotRegistered 门控释放租约）；`wakeSession` 的 deliverWake / setStatus(running) / SESSION_WOKE 事件同样后移——失败路径（锁被他人持有）零内存变更：cached 会话保持 paused/waiting、暂停证据不被清除、wait 条件不被标记 satisfied、不发误导性事件；锁释放后重试可正常恢复。
- **Phase 2 终态 save 先于租约释放（四条路径）**：doExecute（DefaultAgentEngine.dispatchExecution）/ resumeSession / wakeSession / restoreSession（dispatchRestoreExecution）统一重构为「executor 内层 finally 清理（handle/status/registry/actor）→ 成功分支 `save` → 终态 checkpoint remove → 外层 finally `releaseLockQuietly` + 取消续租」；executor 异常分支保持原语义（不落盘、租约仍释放）；save 失败显式传播且租约仍释放（无永久持锁、无静默成功）。
- **Phase 3 wakeSession 租户上下文重建**：裁定 (A) 与 resumeSession 对齐——捕获 `session.getTenantId()`，同步阶段（deliverWake/event 发布）与 worker lambda 均恢复 tenant（`ThreadLocalTenantResolver.set/clear` 对称，restore 调用方上下文）；此前 wake 全路径以 null tenant 运行（跨租户可见性缺陷）且无注释。
- 偏差：无（Non-Goals 全遵守——未改 `DbSessionTakeoverLock` CAS/lease 语义、未改 restoreSession 既有 set(null) tenant 语义、未引入新存储/锁实现、未跑全量构建）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -o` 全绿（3395 tests 0 失败 0 错误）；新增回归测试 2 类 11 例——`TestSessionLifecycleLockGate`（7 例：resume/wake 锁门控 × InMemory/FileBacked、事件缺席/在位、wake 租户上下文同步+worker 断言、调用方租户恢复）+ `TestSessionLifecycleSaveBeforeRelease`（6 例：四路径 save→release 顺序断言 + save 失败错误传播/租约仍释放 ×2）；既有生命周期测试（TestResumeSession/TestEngineEntryCleanupSymmetry/TestSessionTakeoverLockEngineWiring/TestStickyPauseRecovery 等）零回归。
- Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-reliability.md` §13.1（裁定 C round-4 补记 + 实现状态收口条目）、`ai-dev/design/nop-ai-agent/nop-ai-agent-session-and-storage.md` §11.3（写回顺序规则 4：save → checkpoint remove → release）已更新；`ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md` 3 项勾选。

### P2 round-3 测试反模式与弱断言硬化执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0534-2-p2-round3-test-hardening.md`，work-item P2-ROUND3-TESTS，8 项；4 模块纯测试变更，零生产代码改动）

- **Phase 1 TestAiFileTool（nop-ai-mcp-server，15→20 例）**：+2 同前缀兄弟目录负例（baseDir=`target/test-ai-file-tool`，兄弟 `target/test-ai-file-tool-evil`，读/写均经 MCP 入口断言 `ERR_MCP_PATH_ESCAPE`——防 `isInsideBaseDir` 退化为 `startsWith(basePath)`）+2 符号链接负例（沙箱内 symlink → 沙箱外目录，canonical 后越界被拒，`Files.createSymbolicLink`）；merge 正例改写——预置 `option value="a"` + 传入 `value="b"`，断言 merge 后两 option 并存（可区分 merge 分支与普通覆写分支，替代恒真 `exists && length>0`）。
- **Phase 2 ShellCommandExecutorTest（nop-ai-shell，25 例）**：`testInputRedirectFromFile` 注册测试专用 cat 命令（读 stdin 回显），`cat < test_input.txt` 断言 stdout 含 "hello from file"（echo 不读 stdin，原 `echo < f` 仅断言退出码）；`testGroupExprEnvironmentRestore` 改真实 group 表达式 + 执行前后 `getExportedEnv` 断言 + group 外对照 export 泄漏证明（防还原断言空转）。
- **Phase 3（nop-ai-tools / nop-ai-agent）**：TestThoughtStorage 两处恒真断言替换——相对路径 `./_tmp/...` 经 `FileHelper.resolveFile` 落盘断言真实解析目录（含空父目录清理）、null/空路径写 `~/.mcp_sequential_thinking` 后断言 + 仅清理本测试产物（不触碰用户既有数据）；TestAuditEvent **裁定 (A) 整类删除**（8 方法全为不可变值对象镜像断言，equals/hashCode/toString 无独立规格，真实行为面由 TestAuditLoggerDefault/TestSlf4jAuditLogger 覆盖），删除文件。
- **Phase 4（nop-ai-agent）**：7 类残余 P-1 镜像断言逐处处置（处置表见 plan Verification）——TestChannelKind 删 `valuesMatchDesignSpec`、TestSkillModel 删字段往返 + 枚举 valueOf 行留计数、TestPathAccessDecision 删 `enumHasAllowAndDenyValues`、TestTeamSpec 删 3 个纯 getter 镜像测试、TestContributionAndPayload 删计数测试、TestUsageRecord 删 `nullableFieldsDefaultToNull`、TestPermission 删 equals/hashCode/toString 镜像；TestPipelineCompactor 11→15 例：+策略异常吞掉继续升级（invoked 计数断言）、+null 结果跳过、+不缓解结果最终无缩减（compactedMessages=null/tokens 不变/retained=原数）、+`isRelieved` `<=` 边界（`(100,10,100,10)`→true、`(101,10,…)`/`(100,11,…)`→false）。
- 偏差：group 表达式按模块 parser 语法改为 `{ export GROUP_VAR=1 echo in_group; }`（BashSyntaxParser 把 env 赋值挂到后续命令，原计划示例形态不解析）；TestTeamSpec `teamStatusAndMemberRoleEnumsAreStable` 未列入 round-3 清单，按 Non-Goals 保持不动。
- 验证：`./mvnw test -pl nop-ai/nop-ai-mcp-server -am`、`-pl nop-ai/nop-ai-shell -am`、`-pl nop-ai/nop-ai-tools -am`、`-pl nop-ai/nop-ai-agent -am` 全绿（exit 0）；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors，11 个既有 warning，本计划文件零告警）。
- Doc-sync: `ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md`（8 项勾选 + 收口注记）已更新；No owner-doc update required（测试强化不改变契约面）。

### P2 round-3 owner-doc 契约漂移与锚点修复执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0534-1-p2-round3-owner-doc-drift.md`，work-item P2-ROUND3-DOCS，8 项）

- **Phase 1 react-engine.md 契约面漂移**：§9 Actor 状态表删除 `cancelling` 行（`AgentActorStatus` 7 值权威），两级取消改写为经 `AgentExecutionContext` 取消标志 + `ICancelToken.interrupt` 的过渡行为（非独立状态）；§9 末行 "cancelSession default UOE 预留" 改写为 live 语义（`IAgentEngine.java:49-51` default 抛 `ERR_AGENT_CANCEL_SESSION_NOT_SUPPORTED`，`DefaultAgentEngine.java:565` 已实现）；§3.2 接口片段重写为 live 形态——`execute` 抽象（`:15`）+ 其余 default 抛 `NopAiAgentException(ERR_AGENT_*_NOT_SUPPORTED)` + 补 `resumeSession`/`restoreSession`/`wakeSession`/`restorePendingSessions`/`close` 五 API（锚点 `IAgentEngine.java:79/:118/:145/:217/:240`，实现 `AgentSessionLifecycle.java:222/:500/:395` + `DefaultAgentEngine.java:690/:839`）。
- **Phase 2 Hook 清单补全 + 跨文档一致**：02-execution-model.md §5.1 Layer 2 扩展 5→7 点（新增 `BEFORE_TOOL_RESULT_PROCESSED`/`AFTER_TOOL_RESULT_PROCESSED`，锚点 `AgentToolDispatcher.java:390/:462` + re-entry 计数 `:393-398/:465-470`），分层口径 5+7=12 注明；设计结论 "10 个生命周期点" → 12；react-engine.md §8 "核心 7 点" 改写为 "Layer 1 核心 5 + 2 重入点" 子集口径，两份文档枚举一致（`AgentLifecyclePoint.java:4-15` 12 值全覆）。
- **Phase 3 失效锚点重钉 + 计数修正**：security-and-permissions.md:616 沙箱锚点重钉至 `DefaultAgentEngineConfig.java:142-143`/`DefaultAgentEngine.java:456/:257`/`ReActAgentExecutorBuilder.java:523`/`ReActAgentExecutor.java:166/:219/:244-246/:341-342`（删 `:1520`/`:3114`/`:299` 超长锚点）；channel-connector.md:169 payload 锚点重钉 `ReActAgentExecutor.java:1333-1340`/`:971-974/:973`；reliability.md §13 七处锚点重钉（resumeSession/wakeSession 门禁 `AgentSessionLifecycle.java:227-230/:400-403`、denial reset `:251/:260`、`Checkpoint.computeIdempotencyKey:210-213`、future 完成 `IAgentEngine.java:15`+`DefaultAgentEngine.java:839`、paused break `ReActAgentExecutor.java:745-748`、后循环 guard `:1265-1279/:1301-1308`）；session-and-storage.md §16.1 "6 张表"→7（新增 `nop_ai_channel_session` 行，锚点 `nop-ai.orm.xml:1418-1420` + `ChannelSessionStoreImpl.java:33`）；04-tool-invocation.md:72 AskOracleExecutor 99→91 行、01-architecture-baseline.md:75 `buildBaseExecutionContext` → `AgentSessionLifecycle.java:138` + `:146` 补 plan 224 async mailbox 落地、context-model.md:205/207 `IToolExecuteContext` 22→24 处（live grep 计数）。
- 偏差：无（纯文档修订，零代码变更；Non-Goals 全遵守——未动 nop-ai 源码、未处置测试反模式/round-4 项）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0；9 份 owner doc 无告警；全部重钉锚点 grep/sed 抽查可解析；计数（7 表 / 24 实现 / 91 行）与 live 一致。
- Doc-sync: `ai-dev/design/nop-ai-agent/` 下 9 份 owner doc（react-engine/02-execution-model/security-and-permissions/channel-connector/reliability/session-and-storage/04-tool-invocation/01-architecture-baseline/context-model）+ `ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md`（8 项勾选）已更新。

### P2 模块卫生 round-3 closure re-verify（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-3-p2-module-hygiene-round3.md`，round-2 closure visit）

- CLOSURE_SCRIPT_CHECK FAIL（`ledger completion formula unmet: missing-pass:test`）根因：`## Verification` pass 行带前导空格（` - pass test ...`）被 ledger parser 按 prose 忽略——与 `ai-dev/logs/2026/09-14.md`、本文件 0116-2 已记录同类根因一致。修复：pass 行改为列 0 `- pass test ...` 并追加本 visit pass 行。
- 本 visit 独立验证：权威引擎 plan-check（attractor-guided-engineering-template）`derivedCompleted=true`、49/49 全勾选 exit=0；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors，11 warnings 全为其他历史/兄弟计划存量，本计划文件零告警）。
- 语义复核 Phase 1-4 全过（live repo）：AiFileTool `@Auth` `AiTool:read/write`（AiFileTool.java:59/71/121）+ service-layer.md:203 BizObjName `AiTool` + McpConstants.BIZ_OBJ_AI_TOOL="AiTool"；`FileTool__grepFiles.tool.json` + 4 个转正 `.tool.json`（源码树零 task.json 残留，仅测试 javadoc 历史注释）；ChatStreamAccumulator/parts/ContentPart javadoc reserved 登记 + design doc 状态对齐；4 pom 未用依赖删除 + agent h2 直接声明；module-groups.md:17/:86 "工具执行层 19 个 *Executor"（live 实测 19）+ 4 个 beans.xml 补 x:schema/XML 声明/`xmlns:ioc="ioc"`（`urn: nop-ioc` 仅 git-ignored `_dump` 生成物残留，非源码）。
- 入账：plan `## Verification` 追加 round-2 pass test 行 + `## Closure` round-2 dispatch/accepted receipt（#audit-…-2-5dd9ffec）；roadmap 10 项已勾选、daily log 已同步。

### M8-P1 round-4 re-verify（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-2-m8-p1-round4-remediation.md`，round-5 closure visit）

- CLOSURE_SCRIPT_CHECK FAIL（`ledger completion formula unmet: missing-pass:test`）根因：`## Verification` pass 行带前导空格（` - pass test ...`）被 ledger parser 按 prose 忽略——与 `ai-dev/logs/2026/09-14.md` 已记录同类根因一致。修复：pass 行改为列 0 `- pass test ...`。
- 本 visit 独立验证：`./mvnw test -pl nop-ai/nop-ai-toolkit -am` 全绿（227 tests 0 失败 0 错误；BashExecutorTest 17/17、TestBashToolDefaultWiring 2/2、BashSandboxTest 12 中 2 docker 条件按 CI 回退语义跳过）；`check-doc-links.mjs --strict` 退出码 0（0 errors，11 warnings 全为其他历史计划/兄弟计划存量，本计划文件零告警）；`plan-check.mjs --strict` 26/26 全勾选 exit=0。
- 语义复核 Phase 1-2 全过：`classifyFailure` 非基础设施非零退出码返 null（DockerBashSandbox.java:196-202）、`BashExecutor.toResult` 保真 exitCode 与输出、beans.xml 注释 opt-in 装配示例 + 默认 fail-closed、`docs-for-ai/03-modules/nop-ai.md:137-146` opt-in 指引在位、roadmap M8 已勾选；无 in-scope defect 降级。
- 入账：plan `## Verification` 追加 round-5 pass test 行 + `## Closure` round-5 dispatch/accepted receipt（#audit-…-5-ac9736f0）。

### P2 模块卫生 round-3 第一批执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-3-p2-module-hygiene-round3.md`，work-item P2-HYGIENE，10 项）

- **Phase 1 命名契约收口（AiFileTool + FileTool 工具描述文件）**：裁定方案 A——`AiFileTool.java` 3 处 `@Auth` 权限字符串 `AiFileTool:read/write` → `AiTool:read/write`（与 `@BizModel("AiTool")` wire 操作名面一致，`<BizObjName>:<action>` 约定成立；方案 B 改 BizModel 名会破坏全部 wire 操作名，弃）；docs `service-layer.md:203` 权限命名表 BizObjName 列 `AiFileTool` → `AiTool`。FileTool 工具描述文件：`FileTool_grepFiles.tool.json`（单下划线）git mv → `FileTool__grepFiles.tool.json`；4 个死 `.task.json`（loadDslSchema/loadDslSchemaForFileType/loadDslFile/saveDslFile，全仓无 loader）转 `.tool.json` 接线（内容与真实注册操作入参一致，同时消除死资源与这 4 个操作的 schema 派生静默回退）。测试：`TestAiFileTool` +1 例（反射断言全部 `@Auth` 权限以 `McpConstants.BIZ_OBJ_AI_TOOL + ":"` 开头）；新增 `TestGraphQLToolProviderSpecLoading` 4 例（`ToolSpecificationLoader` 对 grepFiles 命中 JSON 英文描述、4 个原 task.json 操作可加载、`GraphQLToolProvider.getTool` 端到端非静默回退、未知操作反例 null）。
- **Phase 2 公共 API 死面裁定（均 reserved）**：`ChatStreamAccumulator`（nop-ai-api）javadoc 标注"当前无生产消费者、为统一累积预留"，与各 dialect 自实现并存（dialect 流式事件语义各异，统一属 optimization candidate）；`ChatUserMessage.parts`/`ContentPart` javadoc 改写为"未接线 reserved，image/audio 无 wire 编码，接线前按预留 API 面使用"（删除"330 ResponsesDialect 负责"隐含承诺）；`ai-dev/design/nop-ai-responses-migration-design.md` 使用契约多模态条目补状态对齐裁定。grep 证明 main 代码零消费（`ResponsesDialect.buildContentParts` 为无关 wire-map 本地 helper）。
- **Phase 3 未用依赖清理（4 pom）**：mcp-server 删 `nop-graphql-core`；coder 删 `nop-ai-api` + `nop-ui`；shell 删 `jline-reader`(optional)；agent 删 `nop-autotest-junit`(test)。删除前逐模块 grep 零引用证明。**偏差**：删除后 agent 的 DB 存储测试（TestMultiTenantDbIsolation 等）因 H2 驱动原经 nop-autotest-junit 传递提供而 ClassNotFound——按 declare-what-you-use 补 `com.h2database:h2` test 直接依赖（版本 BOM 管理），语义等价。4 模块联测 3768 tests 0 失败 0 错误。
- **Phase 4 文档 + XDSL 卫生**：`module-groups.md:17/:86` toolkit 描述"工具抽象层" → "工具执行层：19 个具体 `*Executor` + `LocalToolFileSystem` + `ToolManagerImpl`/`DefaultToolExecutorProvider`"（live 实测 19 个执行器，plan 基线所称 30 个不准确）；toolkit `ai-toolkit-defaults.beans.xml` + `test-mock.beans.xml` 补 `x:schema` 且 `xmlns:ioc="urn: nop-ioc:1.0"`（空格 URI）→ `ioc`；mcp-server `ai-mcp-server-defaults.beans.xml` + tools `ai-tools-defaults.beans.xml` 补 XML 声明（plan 声称"tools 侧已修"不准确——此前仅修了 namespace）。全仓扫描零 `urn: ` 遗留、零 NODECL、零 NOXSCHEMA。
- 偏差汇总：Phase 3 的 agent h2 直接声明（等价替代原传递依赖）；Phase 4 额外补 tools-side XML 声明（Proof 同型遗留扫描要求）；toolkit 执行器计数以 live 19 个为准。
- 验证：`./mvnw test -pl nop-ai/nop-ai-mcp-server,nop-ai/nop-ai-tools -am`（Phase 1）、`-pl nop-ai/nop-ai-api,nop-ai/nop-ai-core -am`（Phase 2）、`-pl nop-ai/nop-ai-mcp-server,nop-ai/nop-ai-coder,nop-ai/nop-ai-shell,nop-ai/nop-ai-agent -am`（Phase 3，3768/0/0）、`-pl nop-ai/nop-ai-toolkit,nop-ai/nop-ai-mcp-server -am`（Phase 4）全绿；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: `docs-for-ai/02-core-guides/service-layer.md`（权限表）、`docs-for-ai/01-repo-map/module-groups.md`（toolkit 分层描述）、`ai-dev/design/nop-ai-responses-migration-design.md`（多模态状态对齐）、`ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md`（10 项勾选 + 收口注记）已更新。

### M8-P1 round-4 closure audit（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-2-m8-p1-round4-remediation.md`）

- 独立 closure audit（closer-session-2026-09-15-0442）通过：Phase 1-2 全部落地并经 live repo 复核——`classifyFailure` 非基础设施非零退出码返 null（DockerBashSandbox.java:196-202）、`execute()` 唯一消费点走 result 路径、`toResult` 保真退出码与输出；`BashSandboxTest` 固化断言修正 + 137/124/BACKEND_UNAVAILABLE/CONTAINER_START_FAILED 分支保持；beans.xml 注释 opt-in 装配示例 + `TestBashToolDefaultWiring` 生产文件守卫 + setter 接线运行时消费断言 + 未接线 fail-closed 逐字断言 + `docs-for-ai/03-modules/nop-ai.md` opt-in 指引。
- 本 visit 独立验证：`./mvnw test -pl nop-ai/nop-ai-toolkit -am` 全绿（227 tests 0 失败 0 错误；BashExecutorTest 17/17、TestBashToolDefaultWiring 2/2、BashSandboxTest 10 过 + 2 docker 条件按 CI 回退语义跳过）；`check-doc-links.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --severity high` 0 发现；`plan-check.mjs --strict` 26/26 全勾选。
- 入账：plan `## Verification` pass test 行 + `## Closure` dispatch/accepted receipt（#audit-…-4-fb649bfc）。

### M8-P1 round-4 remediation 执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-2-m8-p1-round4-remediation.md`）

- **Phase 1 classifyFailure 分类修正 + BashExecutor 保真**：裁定 Option (A)——`classifyFailure` 对不匹配基础设施/容器启动/137/124 特征的非零退出码返回 null → `execute()` 走正常 `BashSandboxResult` result 路径 → `toResult` 复用既有 exitCode≠0 failure 契约（备选 B 新增 COMMAND_FAILED 枚举被拒：枚举保持"沙箱/基础设施失败"语义，命令失败经 result 路径自然表达，避免枚举扩散与异常 catch 特判）。落地：`DockerBashSandbox.classifyFailure` 末支 `CONTAINER_START_FAILED` → `null`（:174-198），类 javadoc "observe a clean exit" 表述同步；`BashSandboxFailureReason` javadoc 闭集语义改写为"仅沙箱/基础设施失败"；`BashExecutor` 复核零改码（命令失败已走 `toResult` 保真，`BashSandboxException` catch 仅剩真正沙箱失败，空输出兜底文本成立）。测试：`BashSandboxTest.dockerBackendClassifiesFailures`（原固化断言 :203-204 改为 null，保留 BACKEND_UNAVAILABLE/CONTAINER_START_FAILED/137/124 分支）+ 新增 docker 条件 e2e `dockerBackendCommandFailurePreservesExitCodeAndOutput`（`sh -c 'echo failing-output; exit 2'` 经 `execute()` 全链断言 exitCode=2 + 输出）；`BashExecutorTest` +2（stub sandbox `toResult` 保真、真实 HostBashSandbox 全链 `echo cmd-failing-output; exit 2`）；`bash.tool.xml` 错误契约对齐（error 为合并输出 + 真实退出码 + sandbox refusal 语义 + fail-closed 指引）。
- **Phase 2 ai-tools:bash opt-in 接线与文档化**：裁定 Option (A)——`ai-toolkit-defaults.beans.xml` 提供注释装配示例（HostBashSandbox/DockerBashSandbox 两选项 + `<property name="sandbox" ref="..."/>` 接线）+ 文档指引（备选 B 条件注册/独立 beans 文件被拒：bean-id 覆盖规则脆弱、Docker 构造参数（镜像/目录白名单）不映射简单配置属性、无安全收益）。落地：beans.xml 注释示例（bean id `nopBashHostSandbox`/`nopBashDockerSandbox`，Docker 示例 `alpine:3.19`）；`BashExecutorTest` +2（setSandbox 接线运行时被消费、未接线 fail-closed 错误消息逐字断言）；新增 `TestBashToolDefaultWiring`（VFS 读生产 beans 文件：默认 `ai-tools:bash` 零 sandbox 属性保持 fail-closed + 示例类名/bean id/属性名守卫，不启动 IoC 容器——toolkit test classpath 无 IHttpClient 实现）；`docs-for-ai/03-modules/nop-ai.md` 工具配置段新增 "bash 工具 sandbox 接线（nop-ai-toolkit）" 子节（默认 fail-closed + 两后端权衡 + 装配形态 + 硬约束声明）。
- 偏差：无（Non-Goals 全部遵守：未动 docker 命令构建/沙箱隔离语义、未新增沙箱实现、未跑全量构建）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-toolkit -am` 全绿（226 tests 0 失败 0 错误，2 个 docker 条件测试按 CI 回退语义跳过，判定依据 = stub `toResult` 保真 + `classifyFailure` 修正分支单测 + 真实 Host 全链测试）；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（9 warnings 全为无关历史计划，本任务涉及文件零告警）。
- Doc-sync: `docs-for-ai/03-modules/nop-ai.md`、`bash.tool.xml`、`DockerBashSandbox.java`/`BashSandboxFailureReason.java` javadoc 已同步。

### M7-P1 round-3 remediation 执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-1-m7-p1-round3-remediation.md`）

- **Phase 1 tools/toolkit VFS 消重**：`git mv` nop-ai-toolkit beans 文件 `ai-tools-defaults.beans.xml` → `ai-toolkit-defaults.beans.xml`；`nop-ai-toolkit.beans` autoconfig 同步指向新路径；3 处文档/注释引用 + agent 模块 3 处注释同步；同 mission 兄弟计划（M8-P1/P2）Target 路径同步。裁定记录：选 (A) 改名 toolkit 侧（备选 B 改名 tools 侧被拒：live 引用面在 toolkit 侧；备选 C 合并被拒：两 jar 会互相依赖 bean 类）。验证：`./mvnw test -pl nop-ai/nop-ai-tools,nop-ai/nop-ai-toolkit -am` 全绿；src+target 双重 Proof 无同名路径。
- **Phase 2 gateway 自动装配**：新增 `/nop/autoconfig/nop-ai-gateway.beans`；删除 3 层 `_module`（不匹配 `*/*/_module`，且 `/nop/ai/_module` 已被 4 jar 占用）；`ai-gateway-defaults.beans.xml` 补 feishu-defaults import（nop-integration-feishu 是 compile 依赖但此前零装配入口，FeishuConnector @Inject 无法解析）。新增 `TestGatewayAutoAssemblyIoC`（full app 容器断言 12 bean + same-instance 接线）。W7 harness 改 `initializeTo(REGISTER_COMPONENT)`（app 容器现含 gateway bean，避免遮蔽 per-test holder）。gateway 193 tests 全绿；`docs-for-ai/03-modules/nop-ai-gateway.md` 装配表述同步。
- **Phase 3 team 工具 .tool.xml**：5 个 `team-*.tool.xml` 新增（字段按 executor 入参定义）；新增 `TestTeamToolDiscovery`（loadTool/listTools/buildToolDefinitions 三路径）。**发现并修复潜伏缺陷**：`read-spill.tool.xml`/`set-active-tags.tool.xml` 的 `meta="true"` 属性形式违反 tool.xdef（须 `<meta>true</meta>` 子元素）——此前无测试触发全量 tool.xml 加载。agent 3398 tests 全绿；`nop-ai-agent-actor-runtime-vision.md` §8.2 补发现路径注记。
- **Phase 4 文档契约收敛**：react-engine.md §5.1/§5.2 steering 语义改写为 round 边界 drain（对齐 `ReActAgentExecutor.java:1234-1242`），mid-round 标 successor；tool-dsl.md/runtime-semantics.md `paralllel`→`parallel`，裁定不引入 deprecated alias（零消费）；prefixLength/prefixHash 三处改写为 head-anchor 保留机制（Layer 2/3），字段方案标未落地。
- 偏差：Phase 3 额外修复 read-spill/set-active-tags 的 meta 属性形式（计划未列，属发现路径落地的必要前置）；Phase 2 增加 feishu-defaults import（计划未列，属自动装配可启动的必要前置）。
- 验证：涉及模块测试全绿 + `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: `docs-for-ai/02-core-guides/ioc-and-config.md`、`docs-for-ai/03-modules/nop-ai-gateway.md`、`ai-dev/design/nop-datav/ai-design.md`、`ai-dev/design/nop-ai-agent/nop-ai-agent-react-engine.md`、`nop-ai-tool-dsl.md`、`nop-ai-agent-runtime-semantics.md`、`nop-ai-agent-reliability.md`、`01-architecture-baseline.md`、`nop-ai-agent-llm-layer.md`、`nop-ai-agent-actor-runtime-vision.md` 已更新。