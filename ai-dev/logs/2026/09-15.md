# 2026-09-15

### M8-P1 round-4 closure audit（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-2-m8-p1-round4-remediation.md`）

- 独立 closure audit（closer-session-2026-09-15-0442）通过：Phase 1-2 全部落地并经 live repo 复核——`classifyFailure` 非基础设施非零退出码返 null（DockerBashSandbox.java:196-202）、`execute()` 唯一消费点走 result 路径、`toResult` 保真退出码与输出；`BashSandboxTest` 固化断言修正 + 137/124/BACKEND_UNAVAILABLE/CONTAINER_START_FAILED 分支保持；beans.xml 注释 opt-in 装配示例 + `TestBashToolDefaultWiring` 生产文件守卫 + setter 接线运行时消费断言 + 未接线 fail-closed 逐字断言 + `docs-for-ai/03-modules/nop-ai.md` opt-in 指引。
- 本 visit 独立验证：`./mvnw test -pl nop-ai/nop-ai-toolkit -am` 全绿（227 tests 0 失败 0 错误；BashExecutorTest 17/17、TestBashToolDefaultWiring 2/2、BashSandboxTest 10 过 + 2 docker 条件按 CI 回退语义跳过）；`check-doc-links.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --severity high` 0 发现；`plan-check.mjs --strict` 26/26 全勾选。
- 入账：plan `## Verification` pass test 行 + `## Closure` dispatch/accepted receipt（#audit-…-4-fb649bfc）。

### M8-P1 round-4 remediation 执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-2-m8-p1-round4-remediation.md`）

- **Phase 1 classifyFailure 分类修正 + BashExecutor 保真**：裁定 Option (A)——`classifyFailure` 对不匹配基础设施/容器启动/137/124 特征的非零退出码返回 null → `execute()` 走正常 `BashSandboxResult` result 路径 → `toResult` 复用既有 exitCode≠0 failure 契约（备选 B 新增 COMMAND_FAILED 枚举被拒：枚举保持"沙箱/基础设施失败"语义，命令失败经 result 路径自然表达，避免枚举扩散与异常 catch 特判）。落地：`DockerBashSandbox.classifyFailure` 末支 `CONTAINER_START_FAILED` → `null`（:174-198），类 javadoc "observe a clean exit" 表述同步；`BashSandboxFailureReason` javadoc 闭集语义改写为"仅沙箱/基础设施失败"；`BashExecutor` 复核零改码（命令失败已走 `toResult` 保真，`BashSandboxException` catch 仅剩真正沙箱失败，空输出兜底文本成立）。测试：`BashSandboxTest.dockerBackendClassifiesFailures`（原固化断言 :203-204 改为 null，保留 BACKEND_UNAVAILABLE/CONTAINER_START_FAILED/137/124 分支）+ 新增 docker 条件 e2e `dockerBackendCommandFailurePreservesExitCodeAndOutput`（`sh -c 'echo failing-output; exit 2'` 经 `execute()` 全链断言 exitCode=2 + 输出）；`BashExecutorTest` +2（stub sandbox `toResult` 保真、真实 HostBashSandbox 全链 `echo cmd-failing-output; exit 2`）；`bash.tool.xml` 错误契约对齐（error 为合并输出 + 真实退出码 + sandbox refusal 语义 + fail-closed 指引）。
- **Phase 2 ai-tools:bash opt-in 接线与文档化**：裁定 Option (A)——`ai-toolkit-defaults.beans.xml` 提供注释装配示例（HostBashSandbox/DockerBashSandbox 两选项 + `<property name="sandbox" ref="..."/>` 接线）+ 文档指引（备选 B 条件注册/独立 beans 文件被拒：bean-id 覆盖规则脆弱、Docker 构造参数（镜像/目录白名单）不映射简单配置属性、无安全收益）。落地：beans.xml 注释示例（bean id `nopBashHostSandbox`/`nopBashDockerSandbox`，Docker 示例 `alpine:3.19`）；`BashExecutorTest` +2（setSandbox 接线运行时被消费、未接线 fail-closed 错误消息逐字断言）；新增 `TestBashToolDefaultWiring`（VFS 读生产 beans 文件：默认 `ai-tools:bash` 零 sandbox 属性保持 fail-closed + 示例类名/bean id/属性名守卫，不启动 IoC 容器——toolkit test classpath 无 IHttpClient 实现）；`docs-for-ai/03-modules/nop-ai.md` 工具配置段新增 "bash 工具 sandbox 接线（nop-ai-toolkit）" 子节（默认 fail-closed + 两后端权衡 + 装配形态 + 硬约束声明）。
- 偏差：无（Non-Goals 全部遵守：未动 docker 命令构建/沙箱隔离语义、未新增沙箱实现、未跑全量构建）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-toolkit -am` 全绿（226 tests 0 失败 0 错误，2 个 docker 条件测试按 CI 回退语义跳过，判定依据 = stub `toResult` 保真 + `classifyFailure` 修正分支单测 + 真实 Host 全链测试）；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（9 warnings 全为无关历史计划，本任务涉及文件零告警）。
- Doc-sync: `docs-for-ai/03-modules/nop-ai.md`、`bash.tool.xml`、`DockerBashSandbox.java`/`BashSandboxFailureReason.java` javadoc 已同步。

### M7-P1 round-3 remediation 执行（plan `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-15-0116-1-m7-p1-round3-remediation.md`）

- **Phase 1 tools/toolkit VFS 消重**：`git mv` nop-ai-toolkit beans 文件 `ai-tools-defaults.beans.xml` → `ai-toolkit-defaults.beans.xml`；`nop-ai-toolkit.beans` autoconfig 同步指向新路径；3 处文档/注释引用 + agent 模块 3 处注释同步；同 mission 兄弟计划（M8-P1/P2）Target 路径同步。裁定记录：选 (A) 改名 toolkit 侧（备选 B 改名 tools 侧被拒：live 引用面在 toolkit 侧；备选 C 合并被拒：两 jar 会互相依赖 bean 类）。验证：`./mvnw test -pl nop-ai/nop-ai-tools,nop-ai/nop-ai-toolkit -am` 全绿；src+target 双重 Proof 无同名路径。
- **Phase 2 gateway 自动装配**：新增 `/nop/autoconfig/nop-ai-gateway.beans`；删除 3 层 `_module`（不匹配 `*/*/_module`，且 `/nop/ai/_module` 已被 4 jar 占用）；`ai-gateway-defaults.beans.xml` 补 feishu-defaults import（nop-integration-feishu 是 compile 依赖但此前零装配入口，FeishuConnector @Inject 无法解析）。新增 `TestGatewayAutoAssemblyIoC`（full app 容器断言 12 bean + same-instance 接线）。W7 harness 改 `initializeTo(REGISTER_COMPONENT)`（app 容器现含 gateway bean，避免遮蔽 per-test holder）。gateway 193 tests 全绿；`docs-for-ai/03-modules/nop-ai-gateway.md` 装配表述同步。
- **Phase 3 team 工具 .tool.xml**：5 个 `team-*.tool.xml` 新增（字段按 executor 入参定义）；新增 `TestTeamToolDiscovery`（loadTool/listTools/buildToolDefinitions 三路径）。**发现并修复潜伏缺陷**：`read-spill.tool.xml`/`set-active-tags.tool.xml` 的 `meta="true"` 属性形式违反 tool.xdef（须 `<meta>true</meta>` 子元素）——此前无测试触发全量 tool.xml 加载。agent 3398 tests 全绿；`nop-ai-agent-actor-runtime-vision.md` §8.2 补发现路径注记。
- **Phase 4 文档契约收敛**：react-engine.md §5.1/§5.2 steering 语义改写为 round 边界 drain（对齐 `ReActAgentExecutor.java:1234-1242`），mid-round 标 successor；tool-dsl.md/runtime-semantics.md `paralllel`→`parallel`，裁定不引入 deprecated alias（零消费）；prefixLength/prefixHash 三处改写为 head-anchor 保留机制（Layer 2/3），字段方案标未落地。
- 偏差：Phase 3 额外修复 read-spill/set-active-tags 的 meta 属性形式（计划未列，属发现路径落地的必要前置）；Phase 2 增加 feishu-defaults import（计划未列，属自动装配可启动的必要前置）。
- 验证：涉及模块测试全绿 + `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: `docs-for-ai/02-core-guides/ioc-and-config.md`、`docs-for-ai/03-modules/nop-ai-gateway.md`、`ai-dev/design/nop-datav/ai-design.md`、`ai-dev/design/nop-ai-agent/nop-ai-agent-react-engine.md`、`nop-ai-tool-dsl.md`、`nop-ai-agent-runtime-semantics.md`、`nop-ai-agent-reliability.md`、`01-architecture-baseline.md`、`nop-ai-agent-llm-layer.md`、`nop-ai-agent-actor-runtime-vision.md` 已更新。