### 2026-07-26 (修正 commit 72249cacf 遗漏的 8 处 `${'$'}{var}` 过度转义 + Navigation.ts 缺省 URL)

- **Fix remaining `${'$'}{var}→${var}`**: NopAuthResource.view.xml (4 处 body 上下文)、NopAuthUser.view.xml (2 处 action API data) 修正为 `${id}`/`${siteId}`；2 处 `<gen-control>` 内的 `${'$'}{icon}`/`${'$'}{siteId}` 确认正确保留（Xpl-xjson 上下文需转义）。`rg` 扫描 auth pages 确认无其他残余。
- **Fix Navigation.ts 缺省 URL**: `resolveBaseUrl` 硬编码 fallback `http://localhost:4173` → `http://localhost:8080`（与 playwright config 默认 backend 端口一致），此前不设 `BASE_URL` 时 22 个浏览器测全失败。
- **e2e 验证**: `pnpm test:auth` → 38 passed。2 个 flaky failure（create new role/user，重跑 6/6 通过）。
- **Commit**: `dec2012c2` — 3 files, 7 insertions, 7 deletions.
- **事后纠正**: 误以为 SPA 空白因缺 `.js` 文件，解压了 `nop-web-site` 中所有 `.js.gz`。确认 Quarkus/Undertow 自动 serving `.js.gz` 带 `Content-Encoding: gzip`，解压多余已回滚。`nop-entropy-e2e/README.md` Troubleshooting 新增「SPA blank」条目防止重犯。

### 2026-07-26 (nop-stream-production Plan 29 — SerializerFingerprint schema compatibility system)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-1000-1-serializer-fingerprint-schema-compat.md` execution:
  - **Phase 1 — fingerprint core + schema resolver**:
    - New `SerializerFingerprint` (`@DataBean`, `Serializable`) in `io.nop.stream.core.checkpoint` with fields `stateName` / `schemaVersion` (default `1`) / `schemaChecksum` and full `equals()`/`hashCode()` over all three fields. Distinct from `StreamModelFingerprint` (DAG-level) — this is per-state.
    - New `StateSchemaResolver` in `io.nop.stream.core.common.state` computing a SHA-256 hex digest of a canonical type-signature string (`stateType=...;valueType=...[;mapKeyType=...][;accumulatorType=...][;aggregateFunctionType=...]`). Two input modes:
      - `fromDescriptor(stateType, descriptor)` — used at `getState()` time; explicit `stateType` parameter required because `ListStateDescriptor` is shared by `MemoryListState`/`MemoryInternalListState` and `ReducingStateDescriptor` is shared by `MemoryReducingState`/`MemoryInternalAppendingState`.
      - `fromTypeMetadata(stateName, stateType, valueTypeFqn, mapKeyTypeFqn, accumulatorTypeFqn, aggregateFunctionTypeFqn)` — used during snapshot serialization where only the recorded type-metadata strings are available.
    - Both modes produce identical checksums for the same type signature (verified by `TestStateSchemaResolver` parity tests for all 6 keyed state types).
    - **Tests**: `TestSerializerFingerprint` (equals/hashCode/version-default/toString), `TestStateSchemaResolver` (determinism, value-type/stateType/mapKeyType/accumulator/aggregateFunction differences, list-vs-internal-list distinction, descriptor↔string parity for all keyed state types, SHA-256 length=64).
    - **Verification** `./mvnw test -pl nop-stream/nop-stream-core -am -T 1C -Dtest='TestSerializerFingerprint,TestStateSchemaResolver'` → 0 failures.
    - **Doc-sync**: No owner-doc update required for Phase 1 (pure internal computation, no behavior change yet).
  - **Phase 2 — snapshot embedding + getState()-time compatibility check**:
    - `MemoryStateSerDe.snapshot{Value,Map,Appending,List,InternalList,Reducing,Aggregating,InternalAggregating}State` now embeds `schemaChecksum` (SHA-256 hex) and `schemaVersion=1` into each per-state info `Map<String,Object>` via a new private `embedSchemaFingerprint(info, stateType, descriptor)` helper. The new fields ride inside the existing per-state JSON structure (no changes to `TaskStateSnapshot`/`StateSnapshot`/`CheckpointSerDe` for fingerprint propagation).
    - `MemoryKeyedStateBackend.{getState,getMapState,getListState,getReducingState,getAggregatingState,getInternalAppendingState(Reducing),getInternalAppendingState(Aggregating),getInternalListState}` now invoke a new private `verifySchemaCompatibility(stateName, stateType, currentDescriptor, restoredDescriptor)` when a state already exists in the `states` map (i.e., restored from checkpoint). Mismatch throws `NopStreamException(ERR_STREAM_STATE_SCHEMA_MISMATCH)` with `stateName`/`expectedChecksum`/`actualChecksum` params. Comparison is between two independently-sourced descriptors (live code vs checkpoint), so NOT tautological.
    - `ERR_STREAM_STATE_SCHEMA_MISMATCH` + `ARG_EXPECTED_CHECKSUM`/`ARG_ACTUAL_CHECKSUM` added to `NopStreamErrors`.
    - **Tests**: `TestStateSchemaCompatibility` (matching→returns restored state; value-type mismatch→throws; mapKey mismatch→throws; fresh start→skips check; same-type/different-name→no conflict; snapshot info contains checksum+version); `TestStateSchemaFingerprintEndToEnd` (E2E: snapshot→CheckpointSerDe serialize→LocalFileCheckpointStorage persist→reload JSON→assert schemaChecksum present→deserialize→restoreState→getState succeeds with matching type; wiring-verified by asserting mismatched getState throws); extended `TestCheckpointSerDeConsistency` with `schemaChecksum` JSON-field check (covered by E2E; explicit `TestCheckpointSerDeConsistency#testSerializedCheckpointContainsFormatVersion` and `#testDeserializeLegacyCheckpointWithoutFormatVersionSucceeds` are Phase 3 deliverables).
    - **Verification** `./mvnw test -pl nop-stream -T 1C -Dtest='!TestCheckpointSerDeConsistency'` → 569 tests, 0 failures (formatVersion assertion in TestCheckpointSerDeConsistency pending Phase 3). Pre-existing unrelated failure in `nop-xlang` `TestFeatureConditionEvaluator#testVirtualNode` confirmed by running on clean `master` (not caused by this plan).
    - **Doc-sync**: No owner-doc update required for Phase 2 code changes (internal implementation, no user-visible API change). Design doc update is in Phase 3.
  - **Phase 3 — CheckpointSerDe format versioning + design doc updates**:
    - `CheckpointSerDe.serializeCheckpoint` / `serializeEpochManifest` now emit a top-level `formatVersion: 2` field (G59). `deserializeCheckpoint` / `deserializeEpochManifest` use a new `detectFormatVersion(map)` helper to recognize the envelope and log a debug message for legacy v1 JSON (absent field). Backward-compatible — legacy JSON still deserializes.
    - New public constants `CURRENT_FORMAT_VERSION=2`, `LEGACY_FORMAT_VERSION=1` on `CheckpointSerDe`.
    - **Tests**: extended `TestCheckpointSerDeConsistency` with `testSerializedCheckpointContainsFormatVersion` (asserts both checkpoint + epoch manifest JSON contain `formatVersion=2`) and `testDeserializeLegacyCheckpointWithoutFormatVersionSucceeds` (round-trips a hand-written JSON without the field). E2E `TestStateSchemaFingerprintEndToEnd` now also asserts the persisted JSON's `formatVersion=2`.
    - **Doc updates**: `state-management-design.md §6.3` got a "Stage 29 落地的最终实现" section documenting the finalized fingerprint computation approach (type-signature SHA-256, per-state info map embedding, `getState()`-time comparison, schemaVersion=1, backward compat). `checkpoint-design.md §8.4.1` got a "Stage 29 实现分歧" note documenting the divergence from the original pseudo-code (no `OperatorSnapshot` wrapper; comparison at `getState()` time in `MemoryKeyedStateBackend` not in storage layer; checksum comparison of restored descriptor vs current descriptor, not stored checksum vs computed checksum). `08-gap-analysis.md` G12/G40/G41/G59 marked `✅ Closed (Stage 29)` with plan path.
    - **Verification** `./mvnw test -pl nop-stream -am -T 1C` → 573 tests, 0 failures, 0 errors across all nop-stream modules.
    - **Doc-sync**: `state-management-design.md §6`, `checkpoint-design.md §8.4.1`, `08-gap-analysis.md` updated; design docs now reflect finalized implementation.

### 2026-07-26 (nop-stream-production Plan {3} — Phase 2 + Phase 3 execution: connector split, interrupt contract, doc reconciliation)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0804-3-api-type-doc-contract.md` Phase 2 + Phase 3 execution:
  - **P1-8** `InputGate.readSingleChannel` interrupt alignment (was already implemented in prior run):
    - Fixed 2 broken P1-8 tests that were added in a prior run but never verified:
      - `TestInputGateTermination.testSingleChannelInterruptReturnsEmpty`: `Thread.interrupted()` was checking the MAIN thread, not the reader thread. Fixed to capture `Thread.currentThread().isInterrupted()` inside the reader thread before exit.
      - `TestTaskLifecycle.testSubtaskTaskCancelViaInterruptReachesCanceled`: test used `createVertex(lambda)` but `SubtaskTask` uses `StreamTaskInvokable` which doesn't call the JobVertex invokable lambda. Rewrote to use a real blocking `InputGate` (single channel, empty `ResultPartition`) — the exact code path P1-8 fixed. Verifies: cancel() → CANCELING + interrupt → InputGate returns empty → processInputGate breaks → invoke() returns → CANCELED (not FAILED, not COMPLETED).
  - **AR-2** Connector module split (completed the split that was partially started in a prior run):
    - Fixed broken `StreamConnectors.java` in `nop-stream-connector-batch` (had doubled package refs `io.nop.stream.connector.batch.io.nop.stream.connector.batch.BatchLoaderSourceFunction`)
    - Deleted from base `nop-stream-connector`: `StreamConnectors`, `BatchConsumerSinkFunction`, `BatchLoaderSourceFunction`, `DebeziumCdcSourceFunction` (+ their tests)
    - Base `nop-stream-connector` pom: removed `optional=true` deps on `nop-batch-core` and `nop-message-debezium` (root cause of `NoClassDefFoundError`)
    - Migrated tests to new modules: 5 batch tests → `nop-stream-connector-batch`, 2 debezium tests → `nop-stream-connector-debezium`, 1 split test (`TestConnectorResourceManagement` → split MessageSource part stays in base, Debezium part moved to debezium)
    - Updated `package-info.java` documenting the AR-2 split rationale
    - Base module now loadable without optional deps (only depends on `nop-stream-core` + test-only `nop-message-core`)
  - **P1-15** `BatchConsumerSinkFunction` boundary tests + null check:
    - Added null check in `consume(R value)`: rejects null with `ERR_STREAM_NULL_ARG` (was silently accepting null into buffer)
    - Added `testBatchSizeZeroRejected`, `testBatchSizeNegativeRejected`, `testConsumeNullRejected` tests
    - Concurrency: verified premise that nop-stream operators are single-threaded per subtask (`StreamSinkOperator.processElement` → `consume` is sequential from one task thread). Added Javadoc thread-safety contract note instead of a concurrency test.
  - **Phase 3 doc reconciliation** (P1-16/P1-17/P1-18/P1-19):
    - `README.md §1.2`: `state`→`common/state`, `time`→`common/eventtime`, `functions`→`common/functions`
    - `README.md §1.2`: `CheckpointCoordinator` moved from core `checkpoint` to runtime `checkpoint`; `GraphModelCheckpointExecutor` moved from runtime `checkpoint` to runtime `execution`
    - `README.md §1.2`: `TimestampsAndWatermarksOperator` moved from runtime `watermark` (deleted row) to core `operators`
    - `README.md §1.1` + §1.4: connector rows split for AR-2; checkpoint layer label fixed
    - `time-model-design.md §6`: "runtime 模块中" → "core 模块（`nop-stream-core/.../operators/`）中"
    - `docs-for-ai/INDEX.md`: removed phantom `nop-stream-checkpoint`/`nop-stream-flink`, added `nop-stream-connector-batch`/`nop-stream-connector-debezium`
    - Fixed 2 pre-existing broken links in `ai-dev/backlog/nop-stream-flink-comparison-roadmap.md` to satisfy `check-doc-links.mjs --strict` exit code 0
  - **Tests** `./mvnw test -pl nop-stream -am -T 1C` → 570 tests, 0 failures. All connector module tests pass individually.

### 2026-07-26 (Plan 321 — Xpl过度转义修正 Phase 1-3 execution + 额外80处 retention文件修复)

- Plan `ai-dev/plans/321-xpl-escaping-over-escape-fix.md` 执行（Phase 1-3 + Phase 4 in progress）:
  - **Phase 1** `.xgen` 模板 7 处 `$id`/`$ids` → `${'$'}{id}`/`${'$'}{ids}` 已修复。`mvn install -DskipTests` 重建后 `_gen/` 文件正确输出 `${id}`。
  - **Phase 2** 2 个 `.page.yaml` 文件 8 处 `${'$'}{xxx}` → `${xxx}` 已修复（assign-auth 6 处 + change-self-pass 2 处）。
  - **额外发现** `ai-dev/tools/check-xpl-escaping.mjs` 的 `isGenerated` 正则 `\/_[a-z].*\.view\.xml$` 错误匹配 `_vfs/` 目录前缀，导致 14 个 retention-layer `.view.xml` 文件被误标为 `[GEN]`。修复为 `\/_[a-z][a-zA-Z0-9]*\.view\.xml$`。
  - **额外的 Phase 3** 通过自动脚本修正 14 个 retention `.view.xml` 文件中 80 处过度转义——覆盖 nop-auth, nop-code, nop-dyn, nop-job, nop-sys, nop-wf 模块。
  - **验证** 工具重跑：0 warning / 57 info（全部 `ESCAPE_OK`，正确转义）。
  - **Doc** `xpl-escaping-reference.md` 新增 `_gen/` 修复流程表；Plan 321 更新（基线/范围/Phase 3 实际完成项/额外发现/Closure Evidence 预填）。
  - **Deferred** e2e 测试因环境依赖待 infra 就绪；doc link check 2 errors 42 warnings 均为 `ai-dev/` 预存问题。
  - **Full `mvn install -DskipTests`** 因 `nop-stream-connector-batch` 预存编译错误阻塞（独立问题）。

### 2026-07-26 (nop-stream-production Plan {3} — API/type contract & doc reconciliation Phase 1 execution)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0804-3-api-type-doc-contract.md` Phase 1 execution:
  - **P1-1 Fix** `StreamComponents` 7 registry → 3 active + 4 dead disposed:
    - `transforms` → `Map<String, Transformation<?>>` (was `Map<String, Object>`)
    - `streams` → `Map<String, StreamEdge>` (was `Map<String, Object>`)
    - `windowingStrategies` → `Map<String, WindowingStrategy>` (was `Map<String, Object>`)
    - `coders`/`schemas`/`environments`/`sideInputs` deleted (dead registries: no register methods, never written, never read)
    - Constructor reduced from 10 args to 6 args (all callers use no-arg constructor)
    - `StreamGraphGenerator.detectWindowingStrategies` body removed (was storing `StreamOperatorFactory<?>` into `windowingStrategies` — type mismatch, dead write, no reader)
    - `core-design.md §1.2` updated: aspirational Beam type names (`PTransform`/`PCollection`/`Coder`/`Schema`/`SideInput`) mapped to real types; dead registry disposition documented
  - **P1-2 Fix** `StreamComponents.getBean(String id, Class<T> clazz)`:
    - Now searches across all 3 active registries (transforms → streams → windowingStrategies)
    - `clazz.isInstance(bean)` check; type mismatch throws `ERR_STREAM_TYPE_MISMATCH` with `ARG_EXPECTED_TYPE`/`ARG_ACTUAL_TYPE` (no more delayed ClassCastException at use site)
    - Null id/clazz rejected
    - 4 new tests: `testGetBeanReturnsMatchingType`, `testGetBeanRejectsTypeMismatch`, `testGetBeanThrowsOnMissingId`, `testGetBeanRejectsNullArgs`
  - **P1-3 Fix** `StreamSinkOperator` dead branch deletion:
    - Deleted 3 `else if (userFunction instanceof TwoPhaseCommitSinkFunction)` dead branches in `processBarrier`/`notifyCheckpointComplete`/`notifyCheckpointAborted` (TPCSF implements CheckpointParticipant, so the `if (instanceof CheckpointParticipant)` branch always wins)
    - Preserved standalone `if (userFunction instanceof TwoPhaseCommitSinkFunction)` in `restoreState` (live participant-state recovery)
    - grep `else if.*instanceof TwoPhaseCommitSinkFunction` → 0 matches in file
  - **P1-6 Fix** `StateDescriptor<T>`:
    - field: `TypeSerializer<?>` → `TypeSerializer<T>`
    - getter: `<S> TypeSerializer<S> getSerializer()` → `TypeSerializer<T> getSerializer()` (removed unconstrained `<S>` and `@SuppressWarnings("unchecked")`)
    - setter: `setSerializer(TypeSerializer<?>)` → `setSerializer(TypeSerializer<T>)`
    - `state-management-design.md §2.2` updated: added `serializer` row with correct `TypeSerializer<T>` type
  - **P1-7 Fix** `IInternalStateBackend.getInternalAppendingState`:
    - Reducing overload: `<N, IN, ACC> InternalAppendingState<K,N,IN,ACC,ACC>` → `<N, IN> InternalAppendingState<K,N,IN,IN,IN>` (removed unconstrained `<ACC>`; reduce semantics is (IN,IN)→IN)
    - Aggregating overload: reviewed — IN/ACC/OUT already correctly constrained by `AggregatingStateDescriptor<IN,ACC,OUT>` argument; Javadoc clarified
    - `MemoryKeyedStateBackend` reducing overload implementation updated to match
    - `state-management-design.md §2.1` table updated: reducing overload return type corrected
- 验证: `./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS; `./mvnw test -pl nop-stream -am -T 1C` → BUILD SUCCESS (all 7 nop-stream submodules green).
- Doc-sync: `core-design.md §1.2` + `state-management-design.md §2.1/§2.2` updated. `ai-dev/logs/2026/07-26.md` updated (this entry).



### 2026-07-26 (nop-stream-production Plan {2} — parallel execution & CEP correctness Phase 1+2+3 execution + closure)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0804-2-parallel-execution-cep-correctness.md` execution (all 3 Phases + closure):
  - **Phase 1 (AR-1 / AR-4 / P0-1)** — operator instance isolation + CEP non-keyed entry:
    - **AR-1 Fix** Added `StreamOperator.copyForSubtask()` default that throws `UnsupportedOperationException` (No-Silent-No-Op contract); `AbstractStreamOperator` overrides with serialization-based deep copy (transient state left null, re-init in `open()`). Concrete overrides in 10 production operators — 6 prior-handled operators (StreamSourceOperator, StreamMap, StreamFilter, StreamFlatMap, StreamSinkOperator, StreamReduceOperator) override to share user function across subtasks; 4 prior-unhandled operators (ProcessOperator, CepOperator, TimestampsAndWatermarksOperator, WindowOperator) override to share immutable config + leave transient state empty. WindowOperator gained a package-private copy constructor (14 final fields). `OperatorChain.shallowCopyOperator` instanceof chain replaced with single `op.copyForSubtask()` call. New `@Shareable` marker annotation lets stateless operators opt out (returns `this`).
    - **AR-1 续 (test stub migration)** 8 stateless test stub operators across `TestParallelGraphExecution` / `TestGraphExecutionPlan` / `TestBufferPoolWiring` / `TestTaskExecutor` / `TestEndToEndPipeline` (TestMapOperator, TestFilterOperator) / `TestJobGraphGenerator` / `TestStreamGraphGenerator` / `TestOneInputTransformation` migrated to `@Shareable`. `TestSavepointEndToEnd.EpochCapturingOperator` marked `@Shareable` (test probe requires shared counter across subtasks).
    - **AR-4 Fix** `SimpleStreamOperatorFactory.createStreamOperator` now `LOG.warn` + throws `StreamException` on `NotSerializableException` instead of silently returning the shared template (defense-in-depth; production path bypasses this SPI via `getRawOperator()`, so this is for future codegen/test-harness consumers). `@Shareable` opt-out supported.
    - **P0-1 Fix** `SingleOutputStreamOperatorImpl.forceNonParallel()` no longer throws — it sets a new mutable `parallelismLocked` flag on `Transformation`. Propagation chain: `Transformation.parallelismLocked` → `StreamGraphGenerator.resolveParallelism()` + `applyParallelismLock()` → `StreamNode.parallelismLocked` → `JobGraphGenerator.createJobVertex()` → `JobVertex.parallelismLocked` → `GraphExecutionPlan.resolveParallelism()` forces parallelism=1 even with DeploymentPlan override. `TestForceNonParallel` updated to verify lock-flag behavior. `CEP.pattern(nonKeyedStream, pattern)` end-to-end verified by new `TestCepNonKeyedEntryE2E`.
  - **Phase 2 (AR-3)** — partition policy inference de-stringification:
    - `PartitionedPlanGenerator.inferPartitionPolicy` class-name `contains("Hash"/"Rebalance"/"Broadcast")` fallback removed; non-PartitionPolicyAware partitioners fail-fast with `StreamException(ERR_STREAM_INVALID_STATE)` explaining the typed-declaration requirement.
    - `GraphExecutionPlan.resolvePartitionPolicy` updated symmetrically: `instanceof PartitionPolicyAware` check added, silent "non-null→HASH" default replaced with the same fail-fast. Two inference paths now consistent.
    - `ForwardPartitioner` now implements `PartitionPolicyAware` returning `PartitionPolicy.FORWARD` (explicit declaration; behavior unchanged since prior fallback was also FORWARD).
    - 3 existing tests rewritten: `TestPartitionPolicyInference` (5 tests), `TestPartitionedPlanGeneratorGetName` (3 tests), `TestParallelGraphExecution.testParallelism2_hash` + `TestGraphExecutionPlan.testPartitionerWiredThroughJobEdge` updated to use PartitionPolicyAware partitioners.
  - **Phase 3 (P0-4 / P1-13 / P1-14 / P1-12)** — CEP/watermark test behavioralization:
    - **P0-4** `TestCepOperatorDanglingCleanup.testDanglingCleanupReleasesSharedBuffer` now asserts `assertTrue(partialMatchesEmpty, ...)` + `assertTrue(getPartialMatches().isEmpty())` before close() — anti-hollow. Test also given a windowed pattern (`within(10s)`) so the dangling-cleanup logic actually fires (without `within()`, `wt <= 0` short-circuits cleanup — the prior test was hollow because it never asserted).
    - **P1-13** `TestCepOperatorStateBackendWiring` refactored from 3 internal-accessor-coupled tests (`getKeyedStateBackend()` / `getNFAStateForTesting()`) to 3 behavior tests: `testConfiguredStateBackendProcessesMatchesAndSnapshotsState`, `testNoBackendConfiguredStillMatchesViaFallback`, `testSnapshotPreservesWatermarkForRestore` (reads snapshot's operator-state key, not internal accessor).
    - **P1-14** `TestAfterMatchSkipStrategies` rewritten: 13 metadata-only tests consolidated into 1 `allFactoryMethodsProduceExpectedMetadata`; 5 new NFA-behavior tests run the same `a1 a2 end` sequence under noSkip / skipPastLastEvent / skipToNext and assert distinct match counts (anti-hollow cross-check).
    - **P1-12** New `TestWatermarkMultiInputCombineWire` (3 tests) exercises `AbstractStreamOperator.processWatermark1/2` + `processWatermarkStatus2` runtime valve wiring, lifting the prior "Anti-Hollow exemption" in `TestIndexedCombinedWatermarkStatus`.
  - **Plan Status: active → completed**; all 3 Phases ticked to `[x]` + Status → `completed`. Closure Gates all ticked. Closure section populated with evidence + follow-up note recommending independent audit.
- 偏差: The original `TestCepOperatorDanglingCleanup.testDanglingCleanupReleasesSharedBuffer` had no `within()` clause so its computed-but-discarded `partialMatchesEmpty` was always false. The P0-4 fix added a `within(10s)` clause to make the cleanup logic actually fire — otherwise the anti-hollow assertion would have been impossible to satisfy. The test name is preserved; the pattern is windowed.
- Baseline fix: `TestOperatorLifecycle.java` had `import io.nop.api.core.exception.NopException` (wrong package — singular) — pre-existing compile break fixed by switching to `io.nop.stream.core.exceptions.StreamException` (per AGENTS.md two-tier error-handling convention for module-internal test code).
- 验证:
  - `./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS (all 41 modules).
  - `./mvnw test -pl nop-stream -T 1C` → BUILD SUCCESS (all 7 nop-stream submodules green).
  - Targeted regression: `TestOperatorSubtaskIsolation` (10) + `TestParallelismLockedPropagation` (2) + `TestForceNonParallel` (2) + `TestPartitionPolicyInference` (5) + `TestPartitionedPlanGeneratorGetName` (3) + `TestParallelGraphExecution` (8) + `TestGraphExecutionPlan` (10) + `TestBufferPoolWiring` (7) + `TestCepOperatorDanglingCleanup` (3) + `TestCepOperatorStateBackendWiring` (3) + `TestAfterMatchSkipStrategies` (5) + `TestCepSkipStrategyE2E` (8) + `TestCepPublicApiE2E` (4) + `TestCepNonKeyedEntryE2E` (2) + `TestWatermarkMultiInputCombineWire` (3) + `TestIndexedCombinedWatermarkStatus` (6) — all green.
- Doc-sync: No owner-doc updates required per Exit Criteria (copyForSubtask / @Shareable / parallelismLocked are internal contracts; the PartitionPolicyAware interface already exists; the test refactor is pure-test). `ai-dev/logs/2026/07-26.md` updated (this entry).



### 2026-07-26 (nop-stream-production Plan {1} — checkpoint recovery & exactly-once integrity Phase 2 + Phase 3 execution + closure)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0804-1-checkpoint-recovery-exactly-once-integrity.md` execution (Phase 2 + Phase 3 + closure):
  - **Phase 2 (P1-4 / P1-5 / P1-9 / P1-10)** — already-landed source-side fixes from prior runs verified in working tree, plus this run filled the remaining gaps:
    - **P1-4** `AbstractStreamOperator.restoreState` now propagates to `initializeState(TaskStateSnapshot)` so `ICheckpointedFunction.initializeState` fires on the production restore path (verified by new `TestOperatorLifecycle.testProductionRestorePathInvokesUdfInitializeState` + `testProductionRestorePathWithNullSnapshotStillFiresInitializeState`).
    - **P1-5** `OperatorChain.finish()` exists and is wired into `StreamTaskInvokable.invokeSource/invokeMiddle/invokeSink/invokeSelfContained` (4 sites) before MAX_WATERMARK. New `TestOperatorChainLifecycle.finishFlushesBufferedSinkBeforeClose` + `finishInvokesFinishOnEveryOperatorInReverseOrder` assert the tail-batch flush contract.
    - **P1-9** `MessageSourceFunction` uses `volatile Throwable pendingError` + rethrow in `run()`. New `TestMessageSourceFunctionThreadSafety.testCollectFailureSurfacesFromRun` + `testTypeMismatchSurfacesFromRun` assert the pipeline fails (not silent EOS).
    - **P1-10** `ResultPartition.close()` uses blocking `queue.put(END_OF_STREAM)` instead of `queue.clear()`. `RecordWriter.close()` updated to handle the new checked-exception (collect/suppress per partition). `TestResultPartitionDeadlock` rewritten: `testFullQueueDrainsAfterCloseNoDataLoss` + `testCloseOnFullQueueBackpressuresUntilConsumerDrains` + `testInterruptDuringCloseRethrowsAndDoesNotDropData` replace the prior data-loss-condoning assertions; `TestDataExchange` test methods updated to declare `throws Exception`.
  - **Phase 3 (P0-5 / P0-6 / P0-7 / P0-8)**:
    - **P0-5 Proof** New `TestStreamModelFingerprintRecoveryCompat` exercises the production `GraphModelCheckpointExecutor.validateFingerprintCompatibility` (same fingerprint → success, different → throws StreamException). Per-state `SerializerFingerprint` schema (zero-implementation) routed to Stage 29 successor in Deferred.
    - **P0-6 Fix+Proof** `TaskManager.receiveAssignment:262-266` + `triggerCheckpoint:340-343` hardened from `LOG.warn + return` to `throw new StreamException(ERR_STREAM_FENCING_TOKEN_MISMATCH)`. New error code registered in `NopStreamErrors`. New `TestFencingTokenRejection` asserts both entry points throw + carry the new error code; existing `TestTaskManager.testReceiveAssignmentRejectsStaleFencingToken` + `testTriggerCheckpointWithStaleTokenIsIgnored` updated from `assertDoesNotThrow` to `assertThrows`.
    - **P0-7 Fix+Proof** `GraphModelCheckpointExecutor.restoreTaskStatesFromSource` now invokes new `validateReverseVertexDifferential` upfront — rejects checkpoint-only vertices (deleted stateful) with `ERR_STREAM_SAVEPOINT_VERTEX_DIFFERENTIAL`, and adds an explicit forward-differential pre-check (`ERR_STREAM_CHECKPOINT_EXECUTOR_RESTORE_FAILED`) that fires independent of invokable installation state. Both epoch and savepoint restore entry points pass the checkpoint's `Set<TaskLocation>` to the shared path. New `TestSavepointVertexSetDifferential` provides 3 forward guards + 2 reverse rejections + 1 same-set baseline.
    - **P0-8 Proof** New `TestStateShardRescale` covers 2→4, 4→2 cross-shardCount restore, plus determinism after rescale.
  - **Plan Status: active → completed**; Phase 2 / Phase 3 all checkboxes ticked to `[x]` + Status → `completed`.
- 验证:
  - `./mvnw install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS (all 6 stream submodules).
  - `./mvnw test -pl nop-stream/nop-stream-core -Dtest='TestStateShardRescale,TestStateShardRouting,TestOperatorChainLifecycle,TestOperatorLifecycle,TestResultPartitionDeadlock'` → 33 tests, 0 failures.
  - `./mvnw test -pl nop-stream/nop-stream-runtime -Dtest='TestFencingTokenRejection,TestSavepointVertexSetDifferential,TestStreamModelFingerprintRecoveryCompat,TestTaskManager'` → 31 tests, 0 failures.
  - `./mvnw test -pl nop-stream/nop-stream-connector -Dtest='TestMessageSourceFunctionThreadSafety,TestBatchConsumerSinkFunction'` → 8 tests, 0 failures.
  - Whole-stream regression: nop-stream-core 1110 tests / nop-stream-runtime 570 tests / nop-stream-connector 55 tests / nop-stream-cep 282 tests / nop-stream-fraud-example 25 tests — **0 failures / 0 errors**.
- 偏差：none (execution matched Exit Criteria; forward-direction hardening was implemented as an explicit pre-check inside `validateReverseVertexDifferential` rather than relying solely on the production `stateLookup.lookup` lambda throw, so the regression guards fire even when invokables are not yet installed — strictly stronger contract).
- Doc-sync: `ai-dev/design/nop-stream/checkpoint-design.md` §8.2 fencing + §8.6 reverse reject are aligned (no drift); `docs-for-ai/02-core-guides/error-handling.md` already documents the platform error-code conventions — new `ERR_STREAM_FENCING_TOKEN_MISMATCH` / `ERR_STREAM_SAVEPOINT_VERTEX_DIFFERENTIAL` follow the same pattern (no new convention introduced).



### 2026-07-26 (doc-contract-drift-remediation Phase 2 — 分析交付物对齐修正后基线 + closure)

- Plan `ai-dev/plans/nop-deep-analysis/2026-07-26-0816-1-doc-contract-drift-remediation.md` 执行（Phase 2 + Plan Status → completed + roadmap/source-audit 闭环）:
  - **Fix [P1-A4-01] A4 findCount 类型**：`2026-07-24-nop-graphql-service-frontend.md:113` 将 `findCount`→Int` 改为 `findCount`→Long`（与 `GraphQLObjMetaHelper.java:27-28` 的 `GraphQLScalarType.Long` 一致）；其余 4 映射（findFirst/findList/findPage/findConnection）未动。
  - **Fix [P1-A5-02] A5 虚构子模块**：`2026-07-24-nop-module-matrix.md:207-208` 删除 `nop-stream-flink`/`nop-stream-checkpoint` 作为真实子模块的引用，改为「Flink API 兼容代码（`DataStreamSource`/`Transformation`/`StreamGraph`/`JobGraph`）位于 `nop-stream-core` 内部，非独立子模块」+ 6 个真实子模块逐一点名 + 显式声明「仓库内不存在…」；保留「API 对齐 Flink、实现自研」定性结论与 `nop-stream-flink-comparison-deep-dive.md` 文档引用（该文档为真实文件，非虚构）。
  - **Fix [P1-A2-01] A2/A5/A7 锚点重指（10 处）**：把用于 `GraphQLWebService`/「HTTP 入口统一分发」语义的 `GQL-008` 全部改为 Phase 1 重标后的 `GQL-009`——A2 `:33/:43/:59/:199/:203`（5 处）、A5 `:48/:97`（2 处）、A7 `:111/:118/:142`（3 处）。保留所有 `GQL-008 = GraphQLConstants` 的正确用法（A2 `:226`、A5 `:255/:280`、A4 `:94/:96/:277`）。
  - **核查 capstone A7**：`rg "nop-stream-flink|nop-stream-checkpoint" 2026-07-26-nop-platform-deep-introduction.md` → 无命中，A7 **未**继承虚构子模块漂移（无需改）；A7 的 GQL-008 误用（3 处）已随锚点重指一并处理。
  - **GQL ID 一致性 grep 验证**：`rg "GQL-008" ai-dev/analysis/2026-07/*.md` 共 6 处命中（A5:255/280、A4:94/96/277、A2:226），全部指向 `GraphQLConstants` 语义；`rg "GQL-009" …` 共 10 处命中（A5:48/97、A2:33/43/59/199/203、A7:111/118/142），全部指向 `GraphQLWebService`/HTTP 入口语义；mission 内 `GQL-008`/`GQL-009` 语义不再冲突。
  - **Plan Status: active → completed**；Closure Gates 7/7 勾选（含 Anti-Hollow Check：grep 抽查 `source-anchors.md` 的 `GQL-009` 与交付物中 `GQL-009` 引用一一对应、无残留 `GQL-008→GraphQLWebService`；`module-groups.md` 子模块清单与 `nop-stream/pom.xml` 逐项一致；A4 findCount 类型与 `GraphQLObjMetaHelper.java` 一致）。
  - **roadmap 闭环**：`ai-dev/design/nop-deep-analysis/nop-deep-analysis-roadmap.md` 对应工作项（doc-contract-drift-remediation）由 ❌ → ✅。
  - **source audits 闭环**：multi-audit 中 P1-A4-01 / P1-A5-02(DRIFT-1) / P1-A2-01(DRIFT-2) 三项 audit status → closed（若对应 audit 文件有 Audit Status 字段）。
- 验证:
  - `node ai-dev/tools/check-doc-links.mjs --strict` → 40 broken links（JSON 计数，全部位于其他 mission 文件）；`ai-dev/analysis/2026-07/` + `docs-for-ai/01-repo-map/module-groups.md` + `docs-for-ai/04-reference/source-anchors.md` + nop-deep-analysis plans 均 0 broken link，未引入新漂移。
  - `echo 'nop-deep-analysis: no typecheck step (analysis mission)' && echo '…no build step…' && echo '…no lint step…'` → 通过（纯文档计划，零代码变更）。
- 偏差：无（Phase 2 严格按 Exit Criteria 执行；A5 `:280` 锚点 ID 枚举行按 plan 指示保留——它是 source-anchors.md 引用清单，列举存在的锚点 ID，不构成 `GQL-008→GraphQLWebService` 语义误用）。
- Doc-sync: 本 Phase 改的是分析交付物（A2/A4/A5/A7）+ plan 自身 + roadmap + 日志；owner doc（`module-groups.md` / `source-anchors.md`）已在 Phase 1 先行对齐，本 Phase 不引入新 owner-doc 漂移。



### 2026-07-26 (doc-contract-drift-remediation Phase 1 — docs-for-ai 权威根因漂移修正)

- Plan `ai-dev/plans/nop-deep-analysis/2026-07-26-0816-1-doc-contract-drift-remediation.md` 执行（Phase 1）:
  - **Fix [P1-A5-02/DRIFT-1] `docs-for-ai/01-repo-map/module-groups.md:23`**：nop-stream 子模块清单从「8 个含虚构 `nop-stream-checkpoint` / `nop-stream-flink`」修正为「与 `nop-stream/pom.xml:16-23` 一一对应的真实 6 个」(`nop-stream-core` / `nop-stream-cep` / `nop-stream-connector` / `nop-stream-runtime` / `nop-stream-flow` / `nop-stream-fraud-example`)；Flink API 兼容层说明并入 `nop-stream-core` 职责（点名列出 `DataStreamSource`/`Transformation`/`StreamGraph`/`JobGraph`，非独立子模块）；检查点存储抽象并入 `nop-stream-runtime` 职责（与已有「检查点协调器」并列）；显式声明「仓库内不存在 `nop-stream-flink` / `nop-stream-checkpoint` 子模块」。
  - **Fix [P1-A2-01/DRIFT-2] `docs-for-ai/04-reference/source-anchors.md:81`**：`GraphQLWebService.java` 行的重复 ID `GQL-002`（与第 74 行 `obj-schema.xdef` 的 `GQL-002` 冲突）重标为唯一新 ID `GQL-009`（grep 确认该 ID 在全文未被占用）。`file:line` 描述与正文不变。
- 验证:
  - `rg "nop-stream-flink|nop-stream-checkpoint" docs-for-ai/01-repo-map/module-groups.md` → 仅命中「仓库内不存在…」显式声明句（非子模块清单引用），drift 已清。
  - `rg -c "GQL-002" docs-for-ai/04-reference/source-anchors.md` → 1（仅 obj-schema.xdef）；`rg -c "GQL-009" …` → 1（仅 GraphQLWebService.java）；冲突消除。
  - `node ai-dev/tools/check-doc-links.mjs --strict` → 40 broken links（JSON 计数），全部位于其他 mission 文件（nop-stream-flink-comparison / nop-metadata / nop-ai-agent / nop-batch / flink-source-audit / nopstream-live-audit / distributed-comparison / operator-subtask / testing-module-docs / post-completion-housekeeping / serializer-fingerprint）；`module-groups.md`、`source-anchors.md`、`ai-dev/analysis/2026-07/` 均 0 broken link，未引入新漂移。
  - `echo 'nop-deep-analysis: no typecheck step (analysis mission)' && echo '…no build step…' && echo '…no lint step…'` → 通过（纯文档计划，零代码变更）。
- 偏差：无（Phase 1 严格按 Exit Criteria 执行）。
- Doc-sync: 本 Phase 编辑的就是 owner doc 本身（`module-groups.md` / `source-anchors.md`），无需额外 owner-doc 更新；下游交付物对齐在 Phase 2 处理。



### 2026-07-26 (A7 capstone 闭环 — closure audit PASS + Plan Status completed)

- Plan `ai-dev/plans/nop-deep-analysis/2026-07-26-0703-1-a7-capstone-deep-introduction.md` closure:
  - **独立子 agent closure audit**（ses_064647aecffeIj95LVNg6MO1sK，subagent_type=explore，read-only 对抗性审查）：**PASS on substance**，5 项 Minor findings（计数 22 vs 23 / 节点 21 vs 23 / successor path 警告 / 排除项标签 / plan-checklist 绝对路径 bug），无 Blocker、无 Major。
  - **Minor findings 修复**：capstone §2 节点计数修正为 23 行覆盖 25 节点；§8.5 计数数学修正为 23 entry（22 原始纳入 + 1 合并）；plan L279 successor path 重排避免 doc-link 误判。
  - **Plan Status: active → completed**；12/12 Closure Gates 勾选；`## Closure` 段填入 audit evidence（含 Phase 1/2/3/4 Exit Criteria 28/28 PASS、Anti-Hollow 4 子项 PASS、Deferred 项分类检查 PASS、tool 检查结果）。
  - **roadmap A7 标记 done**：`ai-dev/design/nop-deep-analysis/nop-deep-analysis-roadmap.md` Work Item Status A7 行 `planned → done`（closure audit 通过后）；`Last Updated: 2026-07-24 → 2026-07-26`。
- 验证:
  - `node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/nop-deep-analysis/2026-07-26-0703-1-a7-capstone-deep-introduction.md --strict` → **exit 0**（1/1 plan passed，62 items 全部勾选，Closure Evidence 已写入）。
  - `node ai-dev/tools/check-doc-links.mjs --strict` → exit 1（2 errors 均在 `ai-dev/backlog/nop-stream-flink-comparison-roadmap.md` L112/L133，pre-existing，**非** nop-deep-analysis mission 文件引入；A7 占位符 error 清零；nop-deep-analysis mission 文件 0 errors）。
  - `echo 'nop-deep-analysis: no typecheck step (analysis mission)' && echo 'nop-deep-analysis: no build step (analysis mission)' && echo 'nop-deep-analysis: no lint step (analysis mission)'` → 通过（分析型 mission，无代码变更）。
- 偏差：plan-checklist 工具用绝对路径调用会因 `path.join(cwd, absPath)` bug 报 "Plan file not found"，改用相对路径成功；属工具缺陷非 plan 缺陷。
- Doc-sync: 仅修改本 plan 自身（Status / Closure Gates / Closure 段 / Deferred But Adjudicated / Non-Blocking Follow-ups）+ roadmap 自身（Work Item Status / Last Updated）+ capstone 自身（§2/§8.5 计数修正）；**未修改任何 `docs-for-ai/`**（是否更新 `docs-for-ai/` 是结论分支的建议对象，最终决策由人确认）。
- **nop-deep-analysis mission 全部 7 个工作项（A1–A7）done**，capstone 深度介绍材料已落地。



### 2026-07-26 (A7 capstone Phase 4 — deferred 裁定、roadmap 占位符修正与准确性终检)

- Plan `ai-dev/plans/nop-deep-analysis/2026-07-26-0703-1-a7-capstone-deep-introduction.md` 执行（Phase 4）:
  - **A1–A6 deferred / open-question 项逐项裁定**：22 项纳入项 + 5 项明确排除项全部裁定，记录于 capstone §8（`ai-dev/analysis/2026-07/2026-07-26-nop-platform-deep-introduction.md:308-379`）:
    - **迁移决策 1 项**（item 1 + 7b 合并）：裁定为「迁回 docs-for-ai（选择性沉淀，非整体迁移）」，依据 capstone §7-a。
    - **独立文档治理任务 14 项**（successor 路径明确）：A1 §6(b)/(c)/(d)、A2 §8(a)/(b)、A4 Open Q(a)/(b)/(c)/(d)、A5 §6(a)/(b)/(d)、A6 §7(a)/(c)，每项写明具体目标文件/章节。
    - **residual-watch-only 8 项**（理由明确）：A1 §6(a)/(e)、A2 §8(c)/(d)、A3 §7(a)/(b)、A5 §6(c)、A6 §7(d)。
    - **明确排除 5 项**：A4 Open Q(e)、A6 §7 mission-driver 引擎、A2 §8 @BizAction-AOP、A3 §7(b) aop-codegen、A3 §7(d) 增量构建。
  - **验证 Phase 3 结论分支⑦已给出迁移建议**：capstone §7-a 已给出「不整体迁移、选择性沉淀」的带依据建议 + 5 类具体沉淀路径 + 风险/代价 + 「最终决策由人确认」。本 phase 只验证存在，不重复撰写。
  - **修正 roadmap A7 deliverable 占位符**：`ai-dev/design/nop-deep-analysis/nop-deep-analysis-roadmap.md:172` 从 `2026-07-XX-nop-platform-deep-introduction.md` 修正为 `2026-07-26-nop-platform-deep-introduction.md`。
  - **顺带修正 plan 自身占位符文本引用**：plan L51 + L181 的 `2026-07-XX-...` 反引号引用改为双引号字符串（避免被 doc-link 工具误判为路径），并标注「Phase 4 已执行」。
  - **doc-links 终检**：`node ai-dev/tools/check-doc-links.mjs --strict` → 2 errors（均为 pre-existing，位于 `ai-dev/backlog/nop-stream-flink-comparison-roadmap.md` L112/L133，**非** nop-deep-analysis mission 文件引入）；A7 占位符 error 清零（roadmap L172 修正后由 1 → 0）；A7 plan 自身 warning 清零（L51/L181 反引号改字符串后由 2 → 0）；nop-deep-analysis mission 文件（roadmap + 7 plan + 7 analysis）0 issues。
  - **准确性终检**：capstone 每条涉及代码/工具的事实性论断都有对应 source-anchor / A× 章节 / 文件路径可验证——架构图 21 节点逐条对应 A× 章节（§2 证据表）；优势/差距矩阵 S1–S7 + G1–G11 每条标注 A× 章节；趋势对标每条标注 URL + 访问日期 + 复用 A× §Y；演进建议 E1–E8 每条标注依据差距编号；结论分支标注依据 + 风险 + 「最终决策由人确认」；§8 裁定逐项标注 successor 路径或 residual 理由。
- 偏差：无（plan 完全按 Phase 4 Exit Criteria 执行）。
- 验证：纯文档计划，零代码改动。
- Doc-sync: No owner-doc update required（本 plan 仅产出分析文档 + 修正 roadmap 自身占位符；`docs-for-ai/` 是否更新是结论分支的建议对象，非本 plan 执行动作）。



### 2026-07-26 (A7 capstone Phase 3 — capstone 深度介绍材料主体撰写)

- Plan `ai-dev/plans/nop-deep-analysis/2026-07-26-0703-1-a7-capstone-deep-introduction.md` 执行（Phase 3）:
  - **产出 capstone 文档**：`ai-dev/analysis/2026-07/2026-07-26-nop-platform-deep-introduction.md`（~430 行），含完整元数据（Status: resolved / Date / Scope / Conclusion / Superseded By）。
  - **八章节齐备**（每章对 A1–A6 的引用用 `file:line` 或章节锚点格式）:
    1. **一句话设计哲学定位**：候选 C（合并版）作为主候选，附 A1 公理 + A2–A6 工程证据双向支撑。
    2. **架构总览图（mermaid）**：A1–A6 六层叠加图（理论→引擎→模型驱动→服务/前端→模块生态→工程化/AI 协同），含 21 个节点 / 边的可追溯证据表（每节点对应 A× 章节 + source-anchor）。
    3. **核心差异化能力矩阵**：12 个能力维度（综合 A1–A6），每项标注证据 + 同类竞品典型做法。
    4. **优势/差距矩阵**：S1–S7 优势 + G1–G11 差距（按重要度排序，标注性质：差距 vs 取舍 vs residual）。
    5. **下一代框架趋势对标**：四主线（云原生/AI 原生/低代码/代码生成），每主线 ≥3 条平台级宏观趋势调研，附 URL + 访问日期 2026-07-26；明确标注「复用 A× §Y」vs「本 phase 新增」。
    6. **演进建议**：8 条（E1–E8），按 E1（启动性能）> E6（source-anchors）> E2（BizModel codegen）> E4/E5（测试覆盖）> E7（模块文档）> E3/E8（feasibility）排序，每条标注依据 + 风险 + 是否推荐为路线图输入。
    7. **结论分支**：(a) 不整体迁移、选择性沉淀（§7-a 具体沉淀建议分项）；(b) 作为路线图输入（按 §6 优先级）。每条标注依据 + 风险 + 「最终决策由人确认」。
    8. **References**：平台内部（file 锚点）+ 外部联网调研（四主线分类）。
  - **额外章节 §8 Adjudication of Deferred Items**：22 项纳入项 + 5 项明确排除项逐项裁定（迁移决策 1 项合并 + 独立文档治理任务 14 项 successor 明确 + residual-watch-only 8 项理由明确 + 排除项 5 项），无未裁定项。该章节是 Phase 4 产出，但在 Phase 3 一并写入文档（避免 Phase 4 重复打开文件）。
- 偏差：无（plan 完全按 Phase 3 Exit Criteria 执行；§8 提前写入是因 Phase 4 的裁定结果需要文档落地，与 Phase 3 的文档撰写职责一致）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` → 3 issues（A7 plan 自身 2 处占位符文本引用 warning + roadmap L172 占位符 error，Phase 4 修正 roadmap 后清零；plan 自身 2 warning 是描述占位符的指令文本，属预期）。
- Doc-sync: No owner-doc update required（本 plan 仅产出分析文档，不修改 `docs-for-ai/`；是否更新 `docs-for-ai/` 是结论分支的建议对象，不是本 plan 的执行动作）。



### 2026-07-26 (A7 capstone Phase 2 — 下一代框架趋势联网对标汇总)

- Plan `ai-dev/plans/nop-deep-analysis/2026-07-26-0703-1-a7-capstone-deep-introduction.md` 执行（Phase 2）:
  - **联网调研 4 主线**，每主线 ≥1 条平台级宏观趋势（附 URL + 访问日期 2026-07-26），结果记录于 `_tmp/a7-phase2-working-draft.md`:
    - **云原生主线**：(1) GraalVM native image 已成 JVM 云原生标配（Quarkus native 70.5 MB RSS vs Spring Boot 149.4 MB，heap 3.2 MB）；(2) Java 25 + 框架 AOT 协同（即使无 native 也显著降低启动）；(3) serverless / 容器-first 成为新框架分水岭。
    - **AI 原生主线**：(1) Spec-Driven Development（SDD）产业化（DeepLearning.AI 课程 + AIware 2025 keynote「spec 即可执行引力中心」）；(2) **AGENTS.md 跨工具标准化**——2025-08-28 GitHub Copilot coding agent 官方支持 AGENTS.md，社区共识其已成为新跨工具标准（区别于旧的 `.github/copilot-instructions.md`），nop 在标准化之前就采用；(3) SDD 课程化与会议化。
    - **低代码/metadata-driven 主线**：(1) BaaS + metadata-driven 成主流（Supabase PostgREST auto-REST + Hasura auto-GraphQL，覆盖 50–80% 后端手工开发）；(2) DB-first 反向生成的产业强势；(3) MDE + AI 融合学术产业化（Rädler 2025 systematic review Springer 53 引用 + ACM AI-Assisted Modeling DSL）。
    - **代码生成/model-driven 主线**：(1) 2025 ~41% 代码 AI 生成但 LLM 生成代码质量/可控性仍是问题；(2) build-time codegen 与 LLM 角色分工（确定性 vs 启发性）；(3) DSL 工程化在 LLM 时代被重估（可控性 + 可审计）。
  - **每主线 3 栏对照表**（趋势核心诉求 vs nop 对应能力 vs 差异点/差距）成型；与 A1–A6 既有调研去重，明确标注「复用 A× §Y」vs「本 phase 新增」。
  - **去重确认**：A2 §7（Spring/Quarkus/Micronaut/Helidon 四维）、A6 §6（Devin/Cursor/Claude Code）、A4 §5.5（Formily/Lowcode Engine）、A5 §5.4（DataHub/OpenMetadata）、A1 §5 + A3 §6（MDSD/MPS/DOP/JHipster 等）均已覆盖，本 phase 仅补平台级宏观。
- 偏差：2 次 web search 超时（低代码主线首次、codegen vs LLM 首次），改用更聚焦查询重试成功。
- 验证：纯调研型 phase，无代码改动；`node ai-dev/tools/check-doc-links.mjs --strict` → 3 errors（与本 phase 无关，A7 capstone 文档未创建的悬空链接待 Phase 3 清零）。
- Doc-sync: No owner-doc update required（Phase 2 为联网调研，仅写 `_tmp/` 工作草稿与 daily log）。



### 2026-07-26 (A7 capstone Phase 1 — 源材料汇总与核心叙事提炼)

- Plan `ai-dev/plans/nop-deep-analysis/2026-07-26-0703-1-a7-capstone-deep-introduction.md` 执行（Phase 1）:
  - **逐章精读 A1–A6**（六份分析共 ~2000 行），每章提取 (a) 一句话核心论断 / (b) 关键差异化 / (c) source-anchor 清单 / (d) 联网对标方向 / (e) Conclusion，结果记录于 `_tmp/a7-phase1-working-draft.md` §1。
  - **核心叙事脉络**：理论(A1 GRC 9 公理)→引擎(A2 加载期烘焙)→模型驱动(A3 持续再生+Delta)→服务/前端(A4 xmeta 单源)→模块生态(A5 一致骨架)→工程化/AI 协同(A6 平台结构友好)——一条贯穿的"复杂性前移到加载期 + 定制回流到 Delta 层"主线。
  - **deferred/open-question 清单全量登记**（22 项纳入 + 5 项明确排除）：plan Non-Blocking Follow-ups + 六份分析 Open Questions 两处来源均覆盖，含迁移决策(1) + A1 §6 五项 + A2 §8 四项 + A3 §7 两项 + A4 四项 + A5 四项 + A6 §7 四项；明确排除项（nop-chaos 前端仓库 / mission-driver 引擎外部 / @BizAction-AOP 关系归 A4 residual / aop execution 归 A2 residual / 增量构建归 A6 residual）单列。
  - **一句话设计哲学定位候选 3 版本**：候选 A（差量组合代数）/ B（模型+AI 协同）/ C（合并版主候选），每版标注 A1 公理 + A2–A6 证据双向支撑。
  - **四主线（云原生/AI 原生/低代码/代码生成）缺口表**成型：每主线列出 A1–A6 已覆盖的领域微观对标 + A7 待补的平台级宏观趋势缺口，为 Phase 2 联网调研做准备。
- 偏差：无（plan 完全按 Phase 1 Exit Criteria 执行）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` → 3 errors（pre-existing，含本 plan 9 处因 capstone 文档尚未创建的悬空链接，Phase 3 创建后清零；非本 phase 引入的 broken link）。
- Doc-sync: No owner-doc update required（Phase 1 为源材料汇总，仅写 `_tmp/` 工作草稿与 daily log）。



### 2026-07-26 (Stage 27 Targeted failover 可行性裁定 — NO-GO)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0433-2-targeted-failover.md` 执行（Phase 1 — 可行性裁定，no-go 收口）:
  - **NO-GO 裁定（Decision）**: targeted failover 在 nop-stream 当前架构下不可行。三条决定性依据（均经 live code 核对 + 独立子 agent closure audit 复验）:
    1. **全 pipelined → 单 region**: `JobGraphGenerator.determinePartitionType()`（`:546-554`）从不返回 BLOCKING——null partitioner → PIPELINED，非 null → PIPELINED_BOUNDED，两者均 `pipelined=true`（`ResultPartitionType.java:49,59`）。每个 JobGraph = 单 pipelined connected component = 单 region。vertex 级 targeted = global，零收益。
    2. **drain/reconnect 不可设计（关键路径）**: `ResultPartition` 持有 `LinkedBlockingQueue`（`:49`），`InputChannel` 直接持有 `ResultPartition` 引用（`:25`），by-reference 直连。scoped 重启三个结构死锁：上游 `queue.put()` 永久阻塞 / 下游 channel 不 close 永挂 / 无 mid-execution 重启入口（`checkTaskFailures` 在 `awaitCompletion` 后运行）。
    3. **架构前置超出 scope**: 解除 no-go 需五项前置（blocking edge + region 概念 + supervision loop + drain/reconnect + per-region 计数器），全部超出 in-process scope，属 Stage 44 / vision 决策。
  - **交付物**: `ai-dev/design/nop-stream/failover-design.md`（新增，裁定文档：§一 裁定结论 / §二 架构事实 / §三 可行性分析 / §四 裁定汇总 / §五 架构前置 / §六 G 归属 / §七 与 global recovery 关系 / §八 拒绝的替代方案）。
  - **owner-doc 同步**: `checkpoint-design.md` §8.1.2 新增 "Region failover 可行性裁定（Stage 27 — NO-GO）" 小节；`01-architecture-baseline.md` §五 line 333 更新（Stage 27 NO-GO 引用）；`nop-stream-production-roadmap.md` Stage 27 标记 `done`（裁定交付），G55 region scheduling 归属改为 Stage 44 only。
  - **G57/G28(续)/per-region-counter deferred → Stage 44**: 三项均裁定为架构前置未满足的 out-of-scope improvement，附 Why Not Blocking Closure + Stage 44 successor path，记录于 plan `Deferred But Adjudicated` + `failover-design.md` §六。
  - **Phase 2/3 cancelled**: go gate 未通过（no-go），不执行；items 全部 [x]（N/A 标注）。
  - **closure audit（独立子 agent, ses_064a0de2dffeAPa5SdYWWkjsaU）**: 7/7 checks PASS。所有架构事实独立复验（`determinePartitionType` 行号、`ResultPartitionType` pipelined 字段、`globalRecovery` 唯一入口、`region` 生产代码零匹配）。
- **验证**: `./mvnw test -pl nop-stream -am -T 1C` → exit 0（baseline 绿，零代码变更）；`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` → exit 0（41/41 items checked）；`typecheck`/`lint` not configured。
- 纯文档计划，零 .java 变更（git status 仅 4 .md 修改 + 1 .md 新增）。
- Doc-sync: `failover-design.md`（新增）、`checkpoint-design.md` §8.1.2、`01-architecture-baseline.md` §五、`nop-stream-production-roadmap.md` Stage 27。



### 2026-07-26 (Stage 28 RPC 暴露 + Dispatcher 裁定 + backpressure 契约 — Phase 2, G26 Decision + G27 backpressure 契约 + gap-analysis 更新)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0433-1-rpc-dispatcher-backpressure.md` 执行（Phase 2）:
  - **G26 裁定（Decision）**: dispatcher 最小化为有意设计。`IStreamExecutionDispatcher` javadoc 新增「G26 / Stage 28 decision — intentional minimization」段（部署入口，非生命周期管理器；coordinator 是 execute() 局部变量；异步 submit+poll 属 Stage 39）。`01-architecture-baseline.md` §五 新增「Dispatcher 最小化 — 有意设计（G26, Stage 28 Decision）」段。
  - **G27 backpressure 契约定位（Decision）**: `01-architecture-baseline.md` §五 新增「进程内 backpressure 契约 — IBufferPool 两级（G27, Stage 28 Decision）」段（in-process = IBufferPool 两级；跨 JVM = IMessageService Stage 40；CREDIT_BASED/ACK_WINDOW 永久排除）。
  - **CREDIT_BASED/ACK_WINDOW 永久排除处理（Fix）**:
    - `FlowControlPolicy` 枚举移除两个值，仅保留 `BLOCKING_QUEUE`（+ class javadoc 记录裁定）。
    - `RecordWriter.validateFlowControlPolicy` javadoc 更新 + 异常消息更新（保留 fail-fast 防御，#24）。
    - `JobEdge.java:79` javadoc 更新。
    - `TestEdgeConfigIntegration` 移除 3 个测试（testCreditBasedPolicyThrows/testAckWindowPolicyThrows/testMultiPartitionCreditBasedThrows）+ class javadoc 更新 + 新增 `testFlowControlPolicyEnumContainsOnlyBlockingQueue`（pin 枚举单值，防未来回退）。
    - `01-architecture-baseline.md` §六「分布式背压」→「进程内背压」重写（仅 BLOCKING_QUEUE + 跨 JVM 由 IMessageService）。
    - `ai-dev/design/nop-stream/README.md` flow control 行更新。
    - **`rg "CREDIT_BASED|ACK_WINDOW" nop-stream --type java` = 零匹配**（exit 1）。
  - **更新 `08-gap-analysis.md`**: G23 标记 closed（coordinator 6 方法，task 4 方法，local 契约完整）；G26 标记 Decision（非 Hollow）；G27 标记 closed（永久排除 + IBufferPool 契约定位）。Item 12a mapping + Conclusion 同步。
- **验证**: `./mvnw test -pl nop-stream -am -T 1C` → 全绿；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS；`typecheck`/`lint` not configured。
- Doc-sync: `01-architecture-baseline.md` §五/§六、`ai-dev/design/nop-stream/README.md`、`08-gap-analysis.md` 已更新。



- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0433-1-rpc-dispatcher-backpressure.md` 执行（Phase 1）:
  - **G23 控制面 RPC 暴露（3 方法）**: `IStreamCoordinatorRpcService` 新增 `terminate(JobTerminationMode)` / `abortCheckpoint(long epochId)` / `getJobStatus()`。`JobCoordinator` 实现委托已有内部方法（非空壳）:
    - `terminate` 已有 4 模式实现（`:716`），新增 `@Override` 暴露到 RPC 接口。
    - `abortCheckpoint(epochId)` 委托 `CheckpointCoordinator.getPendingCheckpoint(epochId)` → `abortPendingCheckpoint(pending, reason)`，触发现有 LOCAL abort handler 路径。unmatched epochId 显式 warn + return（#24 无静默跳过）。
    - `getJobStatus()` 返回新增 `JobStatusResponse` DTO（`@DataBean Serializable`，含 `JobStatus` + `failureCause`）。
  - **live defect 修复 — `terminateCancel()` 设 CANCELED**: `JobCoordinator.terminateCancel()` 在 `stop()` 前显式设 `jobStatus = JobStatus.CANCELED`（闭合 `JobStatus.java` 记录的已知 gap）。`JobStatus.java` javadoc 同步更新。
  - **live defect 修复 — DRAIN + SUSPEND CheckpointType 一致化**: 对齐 `checkpoint-design.md §7.3`（DRAIN/SUSPEND → TERMINAL_SAVEPOINT）:
    - `JobCoordinator.terminateDrain()` COMPLETED_POINT_TYPE → TERMINAL_SAVEPOINT。
    - `GraphModelCheckpointExecutor.handleJobTermination` SUSPEND 分支 SAVEPOINT → TERMINAL_SAVEPOINT。
  - **接线验证（#23）+ 无静默跳过（#24）+ 新功能测试（#25）**: 新增 `TestCoordinatorRpcControlPlane`（8 test）: terminateCancel 设 CANCELED、DRAIN/SUSPEND 用 TERMINAL_SAVEPOINT、abortCheckpoint 触发 abort handler（#23 wiring）、abortCheckpoint unmatched epochId 显式 no-op（#24）、abortCheckpoint not-running 显式 no-op、getJobStatus 返回 RUNNING/FAILED/CANCELED + failure cause。
  - **mock 更新**: `TestTaskManager.MockCoordinatorRpcService` + `TestTaskManagerLivenessAndReporting.CapturingCoordinatorRpc` 补 3 个新接口方法。`TestJobCoordinatorRestartStrategy` 的 12 处 `getJobStatus()` 调用适配新返回类型。
- **验证**: `./mvnw test -pl nop-stream -am -T 1C` → 全绿；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS；`typecheck`/`lint` not configured。
- Doc-sync: `ai-dev/design/nop-stream/01-architecture-baseline.md` §五 控制面角色表 + JobStatus 表更新（新增 RPC 方法、CANCELED 显式转换）。



- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0207-3-buffer-pool.md` 执行（Phase 2）:
  - **回归**：现有 `BLOCKING_QUEUE` flow control 路径在新 pool 模型下行为不变（`TestBufferPoolPhase2Regression.testBlockingQueuePathUnchangedWithPoolAttached`：pool 感知 partition + BLOCKING_QUEUE EdgeConfig，emit/read 正常且 pool 计量随流移动）。
  - **`CREDIT_BASED`/`ACK_WINDOW` fail-fast 未破坏**：`RecordWriter.validateFlowControlPolicy` 仍显式抛 `UnsupportedOperationException`（既有 `TestEdgeConfigIntegration.testCreditBasedPolicyThrows`/`testAckWindowPolicyThrows` 全绿）。
  - **Remote 排除显式化（Anti-Hollow）**：`RemoteResultPartition`/`RemoteInputChannel` 代码注释 + design doc §六 标注为**有意排除**；新增 `TestBufferPoolRemoteExclusion`（runtime）**显式断言两路径行为差异**——Remote 写入不移动任何 pool 计量（`getBufferPool()==null`，3 次 remote write 后 pool.getGlobalUsage()==0），而 local pool-bound partition 写入移动计量（==1），证明差异是有意设计而非遗漏。
  - **`IWriteStatus` 语义明确（裁定）**：`isBackpressured`/`getAvailableCapacity`/`getTotalCapacity` 保持 **per-partition 私有队列口径**（`TestBufferPoolPhase2Regression.testIWriteStatusRemainsPerPartitionScoped`/`testGlobalMeterSeparateFromPerPartitionMeter`：p0 满→per-partition backpressured 但全局 pool 未 backpressured；无全局 accessor 在 IWriteStatus 上，全局视图只在 pool 上）。
  - **owner-doc**: §六 已在 Phase 1 更新（含 Remote 排除 + IWriteStatus 语义条目），Phase 2 无额外 owner-doc 改动。
- **验证**: `./mvnw test -pl nop-stream -am -T 1C` → 549/549 pass（+2 Remote 排除 +3 回归）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS；`typecheck`/`lint` not configured。
- Doc-sync: No additional owner-doc update required（§六 已含 Phase 2 覆盖项）。

### 2026-07-26 (Stage 26 Buffer pool 抽象 — G53 Phase 1, IBufferPool SPI + 注入路径)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0207-3-buffer-pool.md` 执行（Phase 1）:
  - **新增 `IBufferPool` SPI + `BufferPool` Memory 实现**（`execution/buffer/`）:
    - 元素计数制（与 `ResultPartition` 队列 / `EdgeConfig.queueCapacity` 单位一致），per-job 单实例，全局聚合上限可配。
    - Memory 实现用**公平** Semaphore（FIFO，防 fan-out 下单一 partition 独占配额）。
    - **耗尽契约（裁定）**：`acquire()` 满时**阻塞**（与 `queue.put()` 语义一致），**不抛 RuntimeException**；阻塞可被 release / interrupt / close 唤醒。
  - **`ResultPartition` 改造**（保留向后兼容）:
    - 新增 pool 感知构造 `ResultPartition(int capacity, IBufferPool bufferPool)`；旧 2 构造 pool=null（测试/Remote 子类不改，行为不变）。
    - pool!=null 时：`write()` 先 `pool.acquire()`（全局 backpressure）再 `queue.put()`（per-partition backpressure）；`read()` 归还配额；`close()` 对被强制丢弃的元素归还配额（防 leak）；被 interrupt 的 write 归还已取配额。
    - 新增 `getBufferPool()` 供接线验证。
  - **`GraphExecutionPlan` 改造**:
    - 新增 pool 字段 + `getBufferPool()` / `closeBufferPool()`；新 build() 重载显式接收 pool（null 显式 fail-fast，#24）；旧 4 重载内部自建 per-build pool（`DEFAULT_GLOBAL_BUFFER_CAPACITY=65536`，向后兼容、不强制迁移调用点）。
    - 接线 `EdgeConfig.queueCapacity` → per-partition 容量（`resolvePartitionCapacity`，不再恒用 1024）。
    - `create()` 路径保持 pool=null（Remote 运行时构建器排除）。
  - **生命周期接线**: `StreamExecutionEnvironment.execute()` finally 关闭 pool；`GraphModelCheckpointExecutor` 5 个 execute 入口的 finally 关闭 pool（恢复重建 plan 时新 build()=新 pool，不复用失败 attempt pool，避免 leaked permits 饿死恢复）。
  - **Remote 排除显式化**: `RemoteResultPartition` / `RemoteInputChannel` 注释 + design doc 标注为**有意排除**（跨 JVM bound 属 Stage 40 `IMessageService` 后端），非遗漏。
  - **`RecordWriter`**: 新增 package-private `getPartitions()` 供接线验证测试访问。
  - **新增测试**（25 个，全绿）:
    - `TestBufferPool`（11，#24 显式失败 + 公平性）：容量/计量/acquire-release 循环、耗尽阻塞、release/interrupt/close 唤醒、tryAcquire 超时、close 幂等、close 后 acquire 显式抛异常、fairness FIFO。
    - `TestBufferPoolResultPartitionIntegration`（7，#23 接线 + #22 E2E + #24）：write 取/read 还（pool 计量可观测）、多 partition 共享全局 usage、全局耗尽阻塞生产者、per-partition 容量仍生效、close 丢弃元素归还配额（防 leak）、interrupted write 归还配额、pool=null 向后兼容。
    - `TestBufferPoolWiring`（7，#22 + #23 + #24）：旧重载自建 pool、显式 pool 跨所有 partition 共享、EdgeConfig.queueCapacity=10→容量=10、DeploymentPlan EdgeConfig 接线、create() 路径 pool=null、显式 null pool fail-fast、E2E build→pool→多 partition→全局耗尽 backpressure。
  - **owner-doc**: `01-architecture-baseline.md` §六 新增「进程内缓冲池（IBufferPool，G53）」段（单位/基数/注入路径/耗尽契约/公平性/EdgeConfig 接线/Remote 排除/IWriteStatus 语义/生命周期 决策表）。
- **偏差**: 无（实现与 plan Phase 1 决策一致）。
- **验证**: `./mvnw test -pl nop-stream/nop-stream-core -am -Dtest=TestBufferPool,TestBufferPoolResultPartitionIntegration,TestBufferPoolWiring` → 25/25 pass；`./mvnw test -pl nop-stream -am -T 1C` → 547/547 pass（首次 1 flaky `TestCheckpointCoordinatorRaceCondition` 直接测 `CheckpointCoordinator.acknowledgeTask` 并发、不经 ResultPartition/buffer pool，重跑全绿=pre-existing flakiness）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS。
- Doc-sync: `ai-dev/design/nop-stream/01-architecture-baseline.md` §六 已更新。

### 2026-07-26 (Stage 25 Per-task failure detection — G56/G58 Phase 3, restart strategy + cancel normalization)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0207-2-per-task-failure-detection.md` 执行（Phase 3）:
  - **JobStatus + failJob（G56）**:
    - 新增 `JobStatus` 枚举（`coordinator/`，`@Internal`）：`CREATED/RUNNING/FAILED/CANCELED`，`isTerminal()` 工具方法。
    - `JobCoordinator` 新增 `volatile JobStatus jobStatus`（构造时 CREATED；`start()` 转 RUNNING）、`volatile Throwable jobFailureCause`。
    - 新增 `failJob(Throwable cause)` 方法：幂等（FAILED 时二次调用 no-op）、设置 cause、转 FAILED、停止 failureDetector。
    - `assignTasks()` 新增 guard：`jobStatus == FAILED` 时拒绝并 warn（#24 不静默跳过）。
  - **global restart strategy（G56）**:
    - 新增 `AtomicInteger restartCount`（仅 `globalRecovery()` 递增）+ `volatile int maxRestarts`（默认 3，可配）。
    - `globalRecovery()` 入口：`newCount = restartCount.incrementAndGet()`；若 `newCount > maxRestarts` → `failJob(StreamException("cap exceeded"))` 并 return（不再继续恢复，#22 端到端 + #23 接线）。
    - 显式记录 Deferred：Stage 27 scoped/targeted 重启不走 `globalRecovery()`，需自带 per-region 计数器（plan Deferred But Adjudicated 段已记录）。
    - 公开 API：`getMaxRestarts/setMaxRestarts`、`getRestartCount`、`getJobStatus`、`getJobFailureCause`。
  - **cancel 规范化（G58）**:
    - `Task.cancel()` / `SubtaskTask.cancel()` 已在 Phase 1 统一经 `CANCELING` 中间态；对 RUNNING 态有效（Phase 1 实现）。
    - 分布式 `RunningTask.cancel()` 新增 mailbox signalCancel 调用：在 `future.cancel(true)` 之前 `inv.getMailboxExecutor().signalCancel()`（与 Stage 17 mailbox cooperative cancel 对齐，镜像 LOCAL 路径 `GraphModelCheckpointExecutor.java:683`）。
    - null-check 防御：`RunningTask.invokable` 为 volatile，由 `setInvokable()` 延迟设置（30s `waitForInvokable` 窗口）；cancel 到达而 invokable 未安装时，跳过 mailbox 调用，仅做状态转换 + `future.cancel(true)` + `invokableLatch.countDown()`，不抛 NPE。
  - **新增测试**:
    - `TestJobCoordinatorRestartStrategy`（7 测试，#22 端到端 + #23 接线 + #24 显式处理）：JobStatus 初始 CREATED→RUNNING、counter 每次 globalRecovery 递增、达 maxRestarts 触发 failJob（counter=3 cap=2 时 FAILED）、FAILED 拒绝 assignTasks、failJob 幂等、maxRestarts=0 首次 recovery 即 failJob、多 FAILED 报告 → cap → FAILED（端到端）。
    - `TestTaskLifecycle.testTaskCancelFromRunningTransitionsToCanceling`（G58）：blocking invokable + Thread.sleep 验证 RUNNING→CANCELING 转换可观测。
    - `TestTaskManagerLivenessAndReporting.cancelAfterInvokableInvokesMailboxSignalCancel`（G58 #23 mock verify）：直接构造 `TaskManager.RunningTask`，安装 invokable，调用 `cancel()`，断言 `inv.getMailboxExecutor().isCancelled() == true`。
    - `TestTaskManagerLivenessAndReporting.cancelWithoutInvokableDoesNotThrow`（G58 null-check）：未安装 invokable 时 cancel 不抛 NPE。
  - **owner-doc**: `01-architecture-baseline.md` §五 新增「JobStatus + Restart Strategy（G56）」段（状态表 + 计数器规则 + cancel 规范化）。
- **验证**: `./mvnw test -pl nop-stream -am -T 1C` → BUILD SUCCESS（core + runtime + cep + connector + flow + fraud-example，0 failures）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS。

### 2026-07-26 (Stage 25 Per-task failure detection — G52 Phase 2, terminal-state reporting + liveness)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0207-2-per-task-failure-detection.md` 执行（Phase 2）:
  - **新增 DTO + RPC**:
    - `TaskStatusReport`（`@DataBean Serializable`，`coordinator/`）：per-task 终态报告 DTO，含 jobId/vertexId/subtaskIndex/attemptNumber/terminalState[COMPLETED|FAILED]/errorCause/lastProgressTime/fencingToken/reportedAt。
    - `TaskProgress`（`@DataBean Serializable`，`coordinator/`）：per-task liveness 进度 DTO，含 vertexId/subtaskIndex/attemptNumber/lastProgressTime。
    - `IStreamCoordinatorRpcService` 从 1 方法扩到 3 方法：`receiveCheckpointAck`（旧）+ `reportTaskStatus(TaskStatusReport)` + `reportNodeTaskLiveness(nodeId, List<TaskProgress>)`。
  - **liveness writer（4 角色全覆盖）**:
    - `StreamTaskInvokable` 新增 `volatile long lastProgressTime`（构造时初始化为 now）+ `markProgress()` + `getLastProgressTime()`。
    - MIDDLE/SINK：`processInputGate()` 每轮 read 成功后 `markProgress()`。
    - SOURCE/SELF_CONTAINED：`invokeSource()`/`invokeSelfContained()` 在 `sourceOp.run()` 前后 `markProgress()`；`StreamSourceOperator.SourceContext.collect/collectWithTimestamp` 每条记录 `markProgress()`（通过 `setProgressMarker(this::markProgress)` 回调，由 `wireMailboxToHeadSource()` 同步设置）。
  - **liveness carrier**:
    - `TaskManager.heartbeat()`（每 5s）在 `renewLease` 后读取本节点各 RunningTask 的 `invokable.getLastProgressTime()`（null-check：invokable 未安装时跳过该 task，不抛 NPE），批量调用 `coordinatorRpcService.reportNodeTaskLiveness(nodeId, List<TaskProgress>)`；RPC 失败 warn 但不中断 heartbeat 循环（#24 显式处理）。
  - **terminal-state 上报**:
    - `TaskManager.RunningTask` 新增 `attemptNumber` 字段（从 TaskAssignment 传入）；`run()` finally 块新增 `reportTerminalStatus(success)` 方法： canceled 跳过（cancel 由 coordinator 发起，无需回声），COMPLETED/FAILED 调用 `coordinatorRpcService.reportTaskStatus(...)`；RPC 失败 warn 不吞（#24）。
  - **coordinator 处理**:
    - `JobCoordinator` 实现 `reportTaskStatus`：fencing token 校验（拒绝 stale token #23）+ 更新 per-subtask liveness map + FAILED 时（`autoRecoverOnFailedReport=true`）触发 `globalRecovery()`；COMPLETED 仅记录不触发恢复。
    - `JobCoordinator` 实现 `reportNodeTaskLiveness`：单调更新 per-subtask liveness map（仅在 reported > current 时更新）。
    - `JobCoordinator.detectFailures()` 扩展：除节点 lease 检测外，新增 per-task stall 检测（`lastProgressTime < now - taskTimeoutMs`），两类失败并存触发 `globalRecovery()`；新分配但未上报 liveness 的 task 给宽限期（无 liveness 记录时不视为 stall，由 lease 兜底）。
    - 新增可配项：`taskTimeoutMs`（默认 60s）+ `autoRecoverOnFailedReport`（默认 true；EmbeddedDistributedExecutor 置 false 保留同步失败传播路径，避免 recovery 后无 invokable 重装死锁）。
  - **新增测试**:
    - `TestJobCoordinatorPerTaskFailure`（6 测试，#22 端到端 + #23 接线）：FAILED 触发 recovery（token 变化 + history 累积）、stale token 拒绝、COMPLETED 不触发 recovery、fresh liveness 不触发、stale liveness 触发（detectFailures 路径）、null/empty progress 批次安全。
    - `TestTaskManagerLivenessAndReporting`（5 测试，#23 接线 + null-check）：无 task 时无 liveness 上报、invokable 未安装时跳过该 task（不抛 NPE）、invokable 安装后 liveness 上报、RunningTask 完成后 COMPLETED 上报、cancel 路径不产生 spurious FAILED 上报。
  - **回归修复**: `EmbeddedDistributedExecutor` 设 `coordinator.setAutoRecoverOnFailedReport(false)`，避免 Phase 2 触发的 globalRecovery 在 embedded 路径下重新分配 task 但不重装 invokable 导致的 30s 死锁（`TestDistributedE2EIntegration.testDistributed_taskFailurePropagates`）。
  - **Mock 更新**: `TestTaskManager.MockCoordinatorRpcService` 实现 3-方法新接口（捕获 statusReports/livenessReports）。
  - **owner-doc**: `01-architecture-baseline.md` §五 控制面角色表更新 RuntimeNode 行（per-task liveness 上报）；`07-distributed-comparison.md` 故障检测维度更新（per-task 失败检测 Gap → Closed）。
- **验证**: `./mvnw test -pl nop-stream -am -T 1C` → BUILD SUCCESS（core + runtime + cep + connector + flow + fraud-example，0 failures）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS。

### 2026-07-26 (Stage 25 Per-task failure detection — G54/G56 Phase 1, state machine + attempt tracking)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0207-2-per-task-failure-detection.md` 执行（Phase 1）:
  - **状态机扩展**（G54/G58）:
    - 新增 `TaskStateTransition`（`nop-stream-core/.../execution/`，`@Internal`）：统一 Task/SubtaskTask 转换模型。9 态 `CREATED/SCHEDULED/DEPLOYING/RUNNING/RECOVERING/CANCELING/COMPLETED/FAILED/CANCELED`；显式合法转换表（CREATED→{SCHEDULED,CANCELING,FAILED}、SCHEDULED→{DEPLOYING,RECOVERING,CANCELING,FAILED}、DEPLOYING→{RUNNING,RECOVERING,CANCELING,FAILED}、RUNNING→{COMPLETED,RECOVERING,CANCELING,FAILED}、RECOVERING→{SCHEDULED,CANCELING,FAILED}、CANCELING→{CANCELED}）；终态吸收（COMPLETED/FAILED/CANCELED 无出转换）；自环/跨阶段跳跃非法→抛 `ERR_STREAM_INVALID_STATE`（#24 无静默跳过）。提供 `validateTransition(Task.State, Task.State)` 与 `validateTransition(SubtaskTask.State, SubtaskTask.State)` 桥接重载。
    - `Task.State` 从 5 态扩到 9 态（新增 `SCHEDULED/DEPLOYING/RECOVERING/CANCELING`，G58）；`Task.run()` 改为完整链 `CREATED→SCHEDULED→DEPLOYING→RUNNING→COMPLETED`（错误转 FAILED，cancel 闭环转 CANCELED）；`Task.cancel()` 统一经 `CANCELING`：非 RUNNING 态由 cancel 闘环直接推进 `CANCELING→CANCELED`，RUNNING 态留给 run 闘环 finalize；新增 `markScheduled/markRecovering/markFailed` 公开 API（供外部编排器显式驱动中间态）。
    - `SubtaskTask.State` 同步从 6 态扩到 9 态（新增 `SCHEDULED/DEPLOYING/RECOVERING`，CANCELING 已存在）；`SubtaskTask.run()` 改为完整链；`SubtaskTask.cancel()` 统一经 CANCELING（保留 RUNNING 路径的 `Thread.interrupt()`）。
  - **attempt 追踪**（G56-half）:
    - `TaskAssignment` 新增 `int attemptNumber` 字段（默认 1，新增 8 参数构造器；旧 7 参数构造器委托 attemptNumber=1，向后兼容）。`@DataBean` 自动生成 getter/setter。
    - `ClusterRegistry.assignTask` 新签名 `(jobId, vertexId, subtaskIndex, nodeId, attemptId, fencingToken, attemptNumber)`（7 参数）；旧 6 参数签名保留为 `default` 方法委托 attemptNumber=1（外部调用者向后兼容）。新增 `getAttemptHistory(jobId, vertexId, subtaskIndex)` 返回单调递增 `List<TaskAssignment>`。
    - `InMemoryClusterRegistry`：`taskAssignments: Map<String, TaskAssignment>` 改为 `taskAssignmentHistory: Map<String, List<TaskAssignment>>`；assignTask 用 synchronized 块保护 read-modify-write（append 语义）；getTaskAssignment 返回 list 末尾（最新 attempt）；getAttemptHistory 返回 unmodifiable copy。monotonic 倒退时 warn 但仍 append（#24 显式处理）。
    - `JdbcClusterRegistry`：DDL PRIMARY KEY 改为 `(job_id, vertex_id, subtask_index, attempt_number)`；新增 `attempt_number INT NOT NULL DEFAULT 1` 列；assignTask 不再 DELETE-then-INSERT，改为纯 INSERT（约束冲突显式抛 = #24）；getTaskAssignment 改为 `ORDER BY attempt_number DESC LIMIT 1`；getAttemptHistory 改为 `ORDER BY attempt_number ASC`；removeTaskAssignment 删除该 task 全部 attempt 行（仍用于显式清理）。
    - `JobCoordinator` 新增 `attemptCounters: Map<String, Integer>`（per-subtask 计数器，key=`{vertexId}/{subtaskIndex}`）；`assignTasks()` 每次（re）assignment 递增计数器并传给 `clusterRegistry.assignTask` 与 `TaskAssignment`；`globalRecovery()` **不再调用 `removeTaskAssignment`**（保留 ClusterRegistry 历史；仅清空 in-memory 工作集 `taskAssignmentMap`），符合 G56 历史保留要求。
  - **新增测试**:
    - `TestTaskStateTransition`（14 测试）：每条合法转换、终态吸收、自环非法、跨阶段跳跃非法、validateTransition 抛异常、Task/SubtaskTask 桥接、Task 与 SubtaskTask 共享同一转换模型（G58 统一性断言）。
    - `TestInMemoryClusterRegistryAttemptHistory`（6 测试）：首次 attempt 可读、多 attempt append 不覆盖、不同 subtask 独立历史、未知 task 空列表、remove 清空、history unmodifiable。
    - `TestJdbcClusterRegistry` 更新：`testAssignTaskOverwrite` 改为 `testAssignTaskPreservesAttemptHistory`（多 attempt 累积 + 最新读 + 历史读）；新增 `testAttemptHistoryEmptyForUnknownTask`；`testFullClusterLifecycle` 改用显式 attemptNumber（5→reassign 用 attemptNumber=2，断言 history size=2）。
    - `TestJobCoordinatorAttemptTracking`（4 测试，#23 接线验证）：首次 assignTasks 全部 attemptNumber=1、globalRecovery 递增 attemptNumber（history 累积 + 最新读 = 2）、多次 globalRecovery 产生单调 history、attemptId（UUID）与 attemptNumber 同时填充。
    - `TestTaskLifecycle` 新增 `testTaskCancelIsIdempotentAfterCanceled`（G58 cancel 幂等性）。
  - **Mock 更新**: `TestJobCoordinator.MockClusterRegistry` 与 `TestTaskManager.MockClusterRegistry` 实现 `assignTask(7-arg)` 新签名 + `getAttemptHistory`（返回空 list）。
  - **owner-doc**: `01-architecture-baseline.md` §五 控制面角色表更新：JobCoordinator 行补「per-subtask attempt 编号（G56）、global restart 上限、JobStatus 终态、per-task 终态上报处理（G52）」；TaskAttempt 行补「attemptNumber（单调递增，per-subtask）+ ClusterRegistry.getAttemptHistory（G56）」；ClusterRegistry 行补「attempt 历史 append-only（G56，非覆盖式）」。
- **验证**: `./mvnw test -pl nop-stream -am -T 1C` → BUILD SUCCESS（core + runtime + cep + connector + flow + fraud-example，0 failures）。

### 2026-07-26 (Stage 24 DeploymentPlan subtask 分配 + 平台 discovery — G50/G51, Phase 1 + Phase 2)

- Plan `ai-dev/plans/nop-stream-production/2026-07-26-0207-1-deployment-plan-discovery.md` 执行:
  - **Phase 1**（DeploymentPlan 承载 subtask→node 分配，G50）:
    - 新增 `DeploymentAssignment`（`nop-stream-core/.../execution/plan/`，`@DataBean Serializable`）：承载 `(vertexId, subtaskIndex) → nodeId` 物理映射，不可变，`getNodeForSubtask(vertexId, subtaskIndex)` 查询。
    - `DeploymentPlan` 新增 `DeploymentAssignment assignment` 字段（第 9 构造参数；旧 8 参数构造委托，向后兼容）。序列化 round-trip 覆盖（有/无 assignment）。
    - `IDeploymentPlanProvider` 新增 `generateDistributed(PartitionedPlan, List<String> activeNodeIds)` 方法；`DefaultDeploymentPlanProvider` 实现为抛 `UnsupportedOperationException`（core 无 cluster 访问）。
    - `IStreamExecutionDispatcher` 新增 `getExpectedNodeIds(PartitionedPlan)` 方法，供 environment 在 plan 生成期获取预期节点集。
    - `StreamExecutionEnvironment.generateDeploymentPlan()` 增加 mode 分支：DISTRIBUTED 模式调用 `executionDispatcher.getExpectedNodeIds()` + `provider.generateDistributed()`；LOCAL 仍走 `generateLocal()`。
    - `DeploymentPlanGenerator.generateDistributed()` 实现 round-robin 分配（复用 JobCoordinator 现有 round-robin 语义，无容量感知），空节点集显式抛异常。
    - `JobCoordinator.assignTasks()` 改为优先消费 `DeploymentPlan.getAssignment()` 已物化的映射（`useMaterialized` 分支），无映射时 fallback 到 runtime round-robin（LOCAL plan）。不完整映射显式抛异常。
    - `EmbeddedDistributedExecutor` 改为调用 `coordinator.assignTasks()`（消费 DeploymentPlan 映射），替代旧的直接 assignment 循环；invokable 安装改为基于 `coordinator.getTaskAssignments()` 回读。
  - **Phase 1 测试**（新增）:
    - `TestDeploymentAssignment`（5 测试）：查询、空值、isEmpty、序列化 round-trip、不可变性。
    - `TestDeploymentPlan`（4 测试）：默认 LOCAL、带 assignment、序列化 round-trip（有/无 assignment）。
    - `TestDeploymentPlanGenerator`（8 测试）：LOCAL 无 assignment、distributed round-robin（3 节点 4 subtask）、多 vertex、单节点、空节点集抛异常、null plan 抛异常、remote transport、全 subtask 有映射。
    - `TestJobCoordinatorAssignmentFromPlan`（4 测试）：消费物化映射、fallback runtime round-robin、wiring（断言 plan 映射被消费）、不完整映射抛异常。
    - `TestDeploymentAssignmentE2E`（4 测试）：端到端 execute→provider→assignment→ClusterRegistry、wiring（DISTRIBUTED 模式产生 assignment）、getExpectedNodeIds 一致性、round-robin 跨全节点。
  - **Phase 2**（平台 discovery 接入，G51）:
    - `nop-stream-runtime/pom.xml` 新增 `nop-cluster-core` Maven 依赖。
    - 新增 `StreamNodeAutoRegistration`（`nop-stream-runtime/.../cluster/`，`@Internal`）：消费 `INamingService`，`@PostConstruct` 调用 `registerInstance` / `@PreDestroy` 调用 `unregisterInstance`。注册用 `INamingService`（write），不用 `IDiscoveryClient`（read-only）。ServiceInstance 映射：`instanceId`=nodeId、`addr`+`port`=endpoint 解析、`weight`=capacity、`metadata["capacity"]`=capacity。注册失败传播异常（不 `catch{}`）。
    - `EmbeddedDistributedExecutor` 新增接受 `INamingService` 的构造器（第 4 参数，可选 null=向后兼容）；每个节点在 TaskManager.start() 后通过 `StreamNodeAutoRegistration.start()` 注册到平台 discovery；finally 块内 stop。
  - **Phase 2 测试**（新增 `TestDiscoveryRegistration`，7 测试）:
    - `testStreamNodeAutoRegistrationRegistersWithNamingService` — registerInstance 被调用、字段映射正确、stop 后 unregisterInstance 被调用。
    - `testWiringUsesRegisterInstanceNotDiscoveryRead` — wiring（#23）：registerInstance > 0、getInstances == 0。
    - `testRegistrationFailurePropagatesNotSwallowed` — no-silent-skip（#24）：注册失败抛异常。
    - `testEndpointParsing` — host:port 解析。
    - `testE2EDiscoveryRegistrationWithDistributedExecution` — 端到端：execute() → 节点注册到 INamingService → 执行完成 → 注销。
    - `testTwoViewConsistencyDiscoveryVsClusterRegistry` — 两视图一致性（#23）：注册的节点 = ClusterRegistry 活跃节点。
    - `testCoexistenceClusterRegistryAssignmentStillWorks` — 共存不破坏：无 naming service 时回归路径通过。
  - **文档同步**: `01-architecture-baseline.md` §四 DeploymentPlan 行补 `DeploymentAssignment`；§五 控制面角色更新 JobCoordinator（消费 DeploymentPlan 分配）；新增「平台 discovery 单向注册」决策段（G51, D7 deferred，含 ServiceInstance 字段映射表）。
- **验证**: `./mvnw test -pl nop-stream -am -T 1C` → BUILD SUCCESS（core 1055 tests + runtime 515 tests, 0 failures）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS。

### 2026-07-26 (Stage 19 checkpoint 并发与共享状态 — G31/G33, Phase 1 + Phase 2)

- Plan `ai-dev/plans/nop-stream-production/2026-07-25-2300-1-checkpoint-concurrency.md` 执行（continuation）:
  - **Phase 1**（minPause 接线 + stale 警告/文档修正）:
    - `CheckpointCoordinator` 新增 `lastCompletedTimestamp` 字段（volatile，写于 `onCompletePersistSuccess` monitor 内），在 `tryTriggerPendingCheckpoint` 内做 last-completed minPause 门控：`minPause > 0 && lastCompletedAt > 0 && checkpointType == CHECKPOINT && now - lastCompletedAt < minPause` 时返回 `TriggerOutcome(THROTTLED_MIN_PAUSE, null)`。minPause=0 或首次触发（无前序完成）不节流；savepoint/terminal 类型（`SAVEPOINT`/`COMPLETED_POINT_TYPE`/`TERMINAL_SAVEPOINT`/`EXPORTED_SAVEPOINT`）显式绕过（对齐 Flink：minPause 仅作用于周期 checkpoint）。maxConcurrent gating 仍作用于所有类型。
    - 新增 `TriggerRejectionReason` 枚举（`TRIGGERED`/`THROTTLED_MIN_PAUSE`/`REJECTED_MAX_CONCURRENT`/`NO_TASKS_TO_ACK`）与 `TriggerOutcome` 值对象。新内部入口 `tryTriggerCheckpointWithReason(checkpointType)` 暴露拒绝原因；公开 `tryTriggerPendingCheckpoint` 保持原签名并委托。
    - **失败计数器污染修复**：`startCheckpointScheduler` 的 fixed-rate loop 改用 `tryTriggerCheckpointWithReason`，对 `THROTTLED_MIN_PAUSE`/`REJECTED_MAX_CONCURRENT`（正常背压）**不** inflate `consecutiveTriggerFailures`；仅 `NO_TASKS_TO_ACK`（真失败）或异常才计数。
    - **stale 警告移除**：构造器对 `maxConcurrentCheckpoints > 1` 不再 `LOG.warn`「降级到 1 / 不会生效」，改为 `LOG.info` 说明 Coordinator 层尊重配置值，task 级多 epoch 仍属 Stage 45（对齐 live 行为，对齐 `checkpoint-design.md` §2.8 / §13.2）。
    - **无静默跳过**：minPause 节流路径返回 null + DEBUG 日志（带 `elapsed`/`minPause`/`lastCompletedAt`），与 `REJECTED_MAX_CONCURRENT` 在 reason / 日志层面可区分；非 `continue` / 非空返回。
  - **现有测试 minPause=0 适配**：minPause 现被强制执行（默认 500ms），原依赖「无节流」语义的多测试在「完成 cp1 后立即触发 cp2」时会失败。逐个加 `config.setMinPause(0L)` / `.minPause(0L)`（这些测试本意非 throttle 语义）：`TestCheckpointCoordinator`(setUp)、`TestCheckpointIntegration`(setUp)、`TestCheckpointLifecycleIsolation`(setUp)、`TestCheckpointSuccessMapCleanup`(setUp)、`TestE2EMultipleCheckpoints`(replaceAll 5 处)、`TestDistributedExactlyOnce`(replaceAll 6 处)、`TestExactlyOnceCorrectnessFixes`(testCommitFailureRetrySucceeds / testFailedCommitsAreRetriedOnNextCheckpoint 2 处)。
  - **Phase 1 测试**：新增 `TestCheckpointMinPauseAndFailureCounter`（9 测试，全部经 coordinator 路径）：
    - `firstTriggerNeverThrottledByMinPause` / `minPauseThrottlesAfterCompletionAndRecoversAfterWait` / `minPauseZeroDisablesThrottle` — last-completed 节流语义
    - `savepointAndTerminalTypesBypassMinPause` — savepoint/terminal 绕过 minPause（仍受 maxConcurrent）
    - `throttleReasonAndMaxConcurrentReasonAreDistinguishable` — reason 可区分（scheduler 据此避免污染计数）
    - `minPauseThrottleDoesNotInflateFailureCounter` / `maxConcurrentRejectionDoesNotInflateFailureCounter` — 反复背压期间 `getConsecutiveTriggerFailures() == 0`
    - `noTasksToAckInflatesFailureCounter` / `failureCounterResetsOnSuccessfulCompletion` — 真失败仍计数、成功仍重置
  - **Phase 2**（Coordinator 共存 pending 独立 ACK/complete/abort/timeout）：
    - **No code change needed** — Current Baseline 已记录共存设计为 live（每 pending 独立 ack 快照 / 独立 taskStates map / 独立 scheduleTimeout / 按 checkpointId 索引 + 条件 remove）。逐路径核对 `tryTriggerPendingCheckpoint`/`acknowledgeTask`/`completePendingCheckpoint`/`abortPendingCheckpoint`/`scheduleTimeout`/`cleanupOldCheckpoints` 确认共存不变量，未发现 race / 误删 / 计数漂移。
    - **新增 `TestCheckpointCoexistenceViaCoordinator`**（5 测试，**全部经 coordinator 路径，禁用 hollow `new PendingCheckpoint + forceComplete()` 模式**）：
      - `twoPendingCoexistAndCompleteIndependentlyViaCoordinator` — cp1 不 ACK → cp2 触发 → 两者真实共存 → 各自 ACK 至 complete → `getLatestCheckpoint` 各自正确 → taskStates 内容不串扰（cp1 的 tag 不出现在 cp2 storage，反之亦然）→ numPending 归零
      - `endToEndEachPendingIndependentlyDurable` — 从 `tryTriggerPendingCheckpoint` 到 storage 写入 + `getLatestCheckpoint` 的多 pending 端到端路径各自独立跑通
      - `abortOfOnePendingDoesNotAffectTheOther` — cp1 在途 abort → cp2 仍 RUNNING，独立 ACK/complete
      - `timeoutOfOnePendingDoesNotAbortTheOther` — 错峰触发（cp1 t=0/300ms timeout，cp2 t=150/300ms timeout），t=350 验证 cp1 已 timeout-abort 而 cp2 仍 RUNNING 并可在自身 timeout（t=450）前 ACK 至 complete → 证明 `scheduleTimeout` 按 checkpointId 独立作用
      - `storageFailureOnOnePendingPropagatesAndLeavesOtherUnaffected` — 选择性 storage 失败（仅第 1 次 storeCheckPoint 抛 StreamException）：cp1 进入 FAILED 状态（**不**被静默吞掉）、cp2 仍 RUNNING 并独立 complete → 证明多 pending 下 complete/abort 失败传播有覆盖（Plan 规则 #24 Anti-No-Silent-No-Op）
  - **文档同步**: `checkpoint-design.md` §2.8 重写为「Coordinator 层并发模型 + 共存独立流转正确性来源（live 设计逐项枚举）+ minPause(last-completed) + task 层 forward-looking 缺口（Stage 45）」；§9.4 配置表 `minPause`/`maxConcurrentCheckpoints` 行对齐 live 行为；§13.2「失败可观测」补「节流/拒绝不计数」语义，「并发能力一致」补注 Coordinator 层 ✅ / task 层 ❌ Stage 45；`08-gap-analysis.md:99` G31 行标 ✅ Closed（item 19）+ Plan 路径 + 详细落地描述。
  - **G33 裁定（Deferred But Adjudicated）**: 内存全量后端下无跨 checkpoint 共享状态可消费（`CompletedCheckpoint.taskStates` 为独立 `byte[]`/`HashMap` 拷贝），SharedStateRegistry 唯一 load-bearing 消费者为 Stage 31 RocksDB 增量 SST 共享。本 plan 不引入无消费者空壳抽象，显式延后至 Stage 31。roadmap scope-change 同步：Stage 19 deliverable 移除 G33 → Stage 31（已含）。
- **验证**: `./mvnw test -pl nop-stream/nop-stream-runtime -am -T 1C` → BUILD SUCCESS，0 failures（含新增 9 Phase 1 测试 + 5 Phase 2 测试 + 全部既有测试）。

### 2026-07-26 (code cleanup & P3 minor improvements — Stage 23, G68/G64/G62)

- Plan `2026-07-25-2200-3-code-cleanup-p3.md` executed:
  - **Scope**: 纯清理与重构，无功能性变更。收口 G68（OperatorChain javadoc）、G64（反射工厂空 catch 消除），清理跨 plan deferred 代码卫生项，G62 经核对降级为 Decision。
  - Phase 1 (doc & dead code cleanup):
    - G68: `OperatorChain.java:93-94` javadoc "opened in forward order" → "opened in reverse order (tail to head)"（与 `:102` 逆序实现对齐，仅改注释）。
    - `CheckpointMetricsSnapshot.toString()` 补 `failureCause` 字段输出（字段 `:26` + getter `:75` 已存在，toString 漏输出）。新增 `TestCheckpointMetrics#testSnapshotToString_includesFailureCause` 断言 toString 含 `failureCause=` 子串与值。
    - `PartitionPolicy` 删除零引用枚举值 `UNION`、`SINGLETON`（grep 全模块 main+test=0）。
    - BarrierAligner 删除：`BarrierAligner.java`（`@Deprecated @Internal`，零生产调用者）+ `AlignedBarrier.java`（仅 BarrierAligner 使用）+ 3 个 test（`TestBarrierAligner`/`TestBarrierAlignerStarvation`/`TestBarrierAlignerLoopComplete`）。空 barrier 目录已清理。
    - 8 个 @Deprecated 死类删除（删除前逐条 grep 确认零外部生产引用，依赖图仅相互引用）：`WindowOperatorTimerService`、`ProcessWindowAggregationFunction`、`WindowAggregationFunction`（被其余 4 个待删类 + test 引用，同删）、`ReduceAggregationFunction`、`AggregateAggregationFunction`、`ApplyAggregationFunction`、`CoMapFunction`、`CoFlatMapFunction` + `TestWindowAggregationFunction.java`。`HeapInternalTimerService.java:29` 残留 javadoc `{@code WindowOperatorTimerService}` 文本引用已清理。空 `co/` 目录已清理。
    - owner-doc: `docs-for-ai/04-reference/source-anchors.md` STRM-033 条目移除（BarrierAligner 删除后无指向目标）。`ai-dev/design/nop-stream/checkpoint-design.md` §2.2 ALIGNING 行 + `component-roadmap.md` 多输入对齐统一行/演进路径/缺口表 同步标注 BarrierAligner 已删除（避免指向已删文件）。
  - Phase 2 (G64 empty catch + G62 Decision):
    - G64: `WindowedStreamImpl.java:161-163` 空 catch 块消除。catch 块现记 WARN 日志（`nop-stream-runtime not on classpath; IWindowOperatorFactory unavailable (window operations will fail fast at call-site)`），`defaultFactory` 保持 null，既有 call-site fail-fast（apply/aggregate/reduce/process 四处 `factory==null` 抛 `StreamException`）行为不变。新增 slf4j Logger 字段。
    - G64 测试：新增 `TestWindowedStreamFactoryFailFast`（4 测试：apply/reduce/aggregate/process）。利用 `StreamComponents` 空实例（`getWindowOperatorFactory()` 返回 null）使 `getFactory()` 经 `components != null` 分支返回 null（无需 surefire classpath 排除），断言四处均抛含 `nop-stream-runtime on classpath` 的 StreamException——验证 call-site fail-fast 在空 catch 消除后仍生效（Anti-Hollow）。
    - G62 Decision：`state-management-design.md` 新增 §2.4「MergingState 抽象：设计决策（G62）」——记录**不**引入无消费方空壳接口的决策、唯一 merge 调用点（`WindowOperator.mergeWindowContents()` `:1294`/`:1333`/`:1417`）、延后理由（引入须同时迁移调用点属优化非清理）、successor 路径。
    - G62 roadmap 同步：`nop-stream-production-roadmap.md` Stage 23 Deliverables 的 G62 条目标注 Decision-only（对齐 `08-gap-analysis.md:135` 可选项标注，避免 roadmap→plan 治理 drift）。
  - **验证**: `./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS（main+test 编译通过）。`./mvnw test -pl nop-stream -am -T 1C` → **478 pass, 0 failures, 0 errors**（含新增 4 G64 测试 + 1 toString 测试）。注：`TestCheckpointIntegration.testFullCheckpointLifecycle` 为 pre-existing flaky test（clean HEAD 经 git stash 验证同样偶发失败 `expected:<0> but was:<-1>`，时序敏感；本 plan 仅纯清理/改 javadoc/改 toString/空 catch→日志，不触及 checkpoint 通知路径）。
  - **Anti-Hollow 豁免**: G62 本 plan 仅 Decision 无代码（不引入空壳接口）；G64 仅消除空 catch（行为不变，既有 call-site fail-fast 由 4 个新断言验证运行时仍生效）。
  - **验证**: `./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS（main+test 编译通过，含 test 类删除）。`./mvnw test -pl nop-stream -am -T 1C` → 478 pass, 0 failures, 0 errors（注：`TestCheckpointIntegration.testFullCheckpointLifecycle` 为 pre-existing flaky test，clean HEAD 同样偶发失败 `expected:<0> but was:<-1>`，与本 plan 无关——本 plan 仅删死代码/改 javadoc/改 toString 格式）。`node ai-dev/tools/check-doc-links.mjs --strict` HEAD parity：HEAD=40 warnings+3 errors，本 plan 后=40+3，零新增 broken link（3 errors 均为 pre-existing 缺失 analysis 文件引用，与本 plan 无关）。
