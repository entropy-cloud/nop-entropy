# 2026-05-03 dev log

## nop-code vs graphify 对比分析 + 语义边设计

### 做了什么

1. **graphify vs nop-code 全面对比**：深度阅读 graphify 6 个核心模块（extract.py 1700行、cluster.py、analyze.py、build.py、detect.py、cache.py）和 nop-code-core 全部 34 个 Java 文件
2. **确认 nop-code 已包含 graphify 有价值的全部核心功能**：
   - Leiden 社区检测（且增加了大图优化、超时控制、模块度评估）
   - 入口点/God Node 评分（双向分析 vs graphify 单向度数排序）
   - 影响范围分析（graphify 完全没有此功能）
   - 通用多语言模型（CodeSymbolKind 17种 vs graphify 的 4 字段 dict）
3. **新增设计文档**：`ai-dev/design/nop-code/semantic-edge-design.md`
   - 记录 graphify 的语义边（INFERRED 边）机制
   - 设计 nop-code 的 `CodeSemanticEdge` 模型 + `ISemanticEdgeExtractor` 接口
   - Phase 1: 确定性语义边（名称相似度、文档关键词、注解模式，不依赖 LLM）
   - Phase 2: LLM 增强（通过 nop-ai 集成）

### 关键发现

- graphify 的独特价值在多模态处理（PDF/图片/视频）和 LLM 语义边，不在代码索引本身
- nop-code 的架构（ORM + GraphQL + IoC）是 graphify（纯文件系统 + dict）的降维打击
- 语义边是唯一值得从 graphify 借鉴的核心功能，已完整记录设计

### 写入文件

- `ai-dev/design/nop-code/semantic-edge-design.md` (新增)
