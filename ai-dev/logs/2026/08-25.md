# 2026-08-25

### plan346 Phase 1 收口：nop-task / nop-report / nop-rule 三单元处置（子代理执行，主会话验收提交）

- **Plan**: `ai-dev/plans/346-check2-audit-p0-p3-remediation.md` Phase 1。三个子代理并行处置（模块互不相交），主会话独立复跑测试验收。
- **nop-task（19 条：14 修+5 暂缓）**：P0×2（suspend 出口 `metrics.endStep(meter)` 判空；TaskImpl 挂起分支持久化 SUSPENDED(20)、不 runCleanup/endTask/COMPLETED）。P1 修 3 暂缓 3（fork stepPath 竞态需 ORM 唯一索引〔plan-first 保护区〕；continuation-skip outputs/stateBean 持久化需格式兼容设计）。P2 修 5 暂缓 1（persistVars 涉 nop-xdefs）。P3 修 4 暂缓 1（DaoTaskStateStore 装配，`_dao.beans.xml` 生成文件）。Graph 错误边修复伴随事实纠正：生产方 nextOnError 在 TaskStepExecution 层即被拦截。新增 9 测试类 17 用例 + 8 task.xml 资源；stash 红 14 处；`-pl nop-task/nop-task-core` 116/0/0/0。
- **nop-report（26 条：21 修+2 暂缓+3 不修复）**：P0 COUNTIF/SUMIF 多字符操作符显式匹配 + 顺带修 SUMIF 迭代器别名错位（写测试时发现的相邻缺陷）。P1×5 全修（avg 分母/childCell/row 方向递归/col 方向 descendants/FontManager 接线）。暂缓 2（minReuse 语义需 owner 裁定；getRowIndex O(n²) 需索引缓存设计）；不修复 3（契约统一爆炸半径/既定设计权衡/兼容性）。新增 11 类 24 例 + 既有 +16 例；红验证含 TextWrapHelper 死循环至 JVM OOM 的强红；core 64+pdf 12+docx 2+demo 24 全绿。
- **nop-rule（15 条：12 修+1 复查非问题+2 暂缓）**：P0 DecoratedExecutableRule 正常路径补 afterExecute。复查非问题 1：空输入规则编译抛裸 IAE 前提有误（`ApiStringHelper.isEmptyObject` 不判空集合，stash 实测可正常编译，保留行为固化测试）。暂缓 2（空 `<or>` 恒真需 nop-core/nop-xlang/nop-rule 平台统一；规则日志落库需 saver 设计）。新增 4 类 16 例；红 8 处；8 子模块 43/0/0/0。注意：service 侧测试须与 core/dao 同 reactor 跑（单 `-pl service` 会解析 m2 旧 dao 构件误红）。
- **Phase 1 终局**：10 单元 226 条全部标注；13 条 P0 = 12 已修复 + 1 复查非问题；plan346 Phase 1 转 completed。跨模块遗留（记录于各标注）：空 `<or>` 平台统一、XptErrors col-parent 错误码字符串与 row 版重复待决策。
- **验证**：主会话独立复跑 `-pl nop-task/nop-task-core,nop-report/nop-report-core,...,nop-rule/nop-rule-web` BUILD SUCCESS；`node ai-dev/tools/check-doc-links.mjs --strict` 0 errors（顺带修复 HEAD 既有的 2 处 docs-for-ai 外部仓反引号路径引用〔flux-rendering.md:455 / orm-model-design.md:208〕+ plan346 相对路径 warning）。
- Doc-sync: No owner-doc update required（模块内部缺陷处置，无平台约定变化）。

### plan346 check2 处置战役续：nop-job 单元收口（18/18 标注，代码主体为 08-24 遗留 commit 的补标注 + 4 条残留修复）

- **Plan**: `ai-dev/plans/346-check2-audit-p0-p3-remediation.md` Phase 1。
- **背景**：nop-job 的修复代码已于 08-24 落地（commit `35623e49cf`，14/18 条），但当次会话中断未做报告标注与 plan 勾选。本次逐条对照 live code 复核该 commit 对 18 条发现的覆盖，补齐标注，并修复 4 条残留。
- **08-25 新修复 4 条**（均红验证）：
  - P3-8 cursor null 防御：`AbstractBatchScanner.drainBatch` 推进前校验排序键非 null，null 时 warn + markDrained（红：满批末条 null 键触发第二次 fetch，fetchCalls 1≠2）。
  - P3-10 `BeanContainerInvokerResolver`：错型 bean 抛 `ERR_JOB_INVOKER_NOT_FOUND`（带 beanName/actualType），对齐 DefaultJobInvokerResolver（红：HEAD 抛裸 ClassCastException）。
  - P3-11 dispatched metrics 虚增：`IJobFireStore.insertTasksAndMarkFireDispatching` 改返回 boolean（静默跳过=false），dispatcher 条件计数 + debug 留痕（红：仅回退 dispatcher 时 onFiresDispatched 0≠1）；接口签名变更同步 4 个测试 stub。
  - 格式修正：`HttpRpcPollTaskClient` 双 import 一行（08-24 commit 引入）拆开。
- **裁定暂缓 1 条**：P3-7 trigger 热路径重建缓存——报告自述无功能危害；`LimitCountTrigger`/`HandleMisfireTrigger` 等链上包装含实例状态，缓存键与状态性边界属设计决策且需 benchmark 支撑。
- **报告事实纠正 1 处**：P3-6 ThreadLocal「跨线程 beginScan 互相清空」前提不成立（ThreadLocal 按线程隔离），真实缺陷仅空 HashMap 内存驻留，修复（endScan 改 remove）与纠正同落标注。
- **验证**：`./mvnw test -pl nop-job/nop-job-core,nop-job/nop-job-local,nop-job/nop-job-dao,nop-job/nop-job-coordinator` BUILD SUCCESS（coordinator 197 run 0 fail，含新 TestJobDispatcherDispatchedMetrics 2 用例、TestAbstractBatchScanner +2、TestBeanContainerInvokerResolver 3、TestJobStoreImpl 返回值断言）；`-pl nop-job/nop-job-service` 47 run 0 fail。
- **环境**：会话初执行全量 `./mvnw clean install -DskipTests -T 1C` 成功，本地仓库已播种 io.nop 构件，后续单元测试可 `-pl` 免 `-am`。
- Commit: `35623e49cf`（08-24 主体）+ 本次提交（见 git log）
- Doc-sync: No owner-doc update required（修复均为模块内部缺陷处置，无平台约定变化；新错误码 ERR_JOB_CALENDAR_INVALID_YEAR_DAYS 于 08-24 落地，本模块无 i18n bundle 惯例已核对）。
