### 2026-07-08（删除 NopSysEventPartition 残留实体 + nop-batch-sys 测试绿测）

- 删除 `NopSysEventPartition`：该实体（表 `nop_sys_event_partition`）是早期独立分区租约设计的残留，已被 `NopSysEvent` 行上的 `leaseOwner`/`leaseExpireTime` 字段取代，无任何代码引用。
  - 删除 `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/entity/NopSysEventPartition.java`
  - 从 `nop-sys/model/nop-sys.orm.xml` 移除对应 `<entity>` 定义（原 line 633-657）
  - 此前编译阻塞的根因正是：retention 类 `extends _NopSysEventPartition`，但 `_gen/_NopSysEventPartition.java` 从未生成。删除实体后阻塞消失。
- 修复 `TestSysEventBatchTrigger`：原测试在字段初始化时调用不存在的 `getBean()`，且缺少 `@NopTestConfig` + `@Inject` 模式。按 `TestSysDaoMessageService` 的既有模式重写。
- 验证：
  - `./mvnw -pl nop-sys/nop-sys-dao compile -DskipTests` → BUILD SUCCESS
  - `./mvnw -pl nop-sys/nop-sys-dao install -DskipTests` → BUILD SUCCESS
  - `./mvnw -pl nop-batch/nop-batch-sys test` → BUILD SUCCESS（2 tests, 0 failures, 0 errors）
- Doc-sync: 无 owner-doc 需更新（`NopSysEventPartition` 从未出现在任何文档中）。

### 2026-07-08（xpl-fn 编译期箭头函数检测）

- 在 `XLangCompileTool.compileEvalFunction()` 中增加编译期检查：如果 body 解析结果为 `ArrowFunctionExpression`，抛出 `ERR_XPL_FN_BODY_IS_FUNCTION`（`nop.err.xlang.xpl.fn-body-is-function`）。
- 新增错误码于 `XLangErrors.java:899`，提示用户 body 中直接写表达式即可，不需要重复函数参数声明。
- 改动点：`XLangCompileTool.java:324-329`（check + throw）、`XLangErrors.java:899-901`（error code）。
- 验证：`./mvnw test -pl nop-kernel/nop-xlang -am` → 453 tests, 0 failures, 0 errors。
- Doc-sync: 无变更（batch-dsl.md 已使用正确写法，无待办标记）。

### 2026-07-08（BeanMethodJobInvoker test fix）

- "testMissingMethodName" 测试修复：由于 `methodName` 现在默认为 `"execute"`，该测试不再抛出 `NopException`，改为验证默认方法被成功调用（`isNoArgCalled()` 为 true）。
- 验证：`./mvnw test -pl nop-job/nop-job-local -am -Dtest=TestBeanMethodJobInvoker -Dsurefire.failIfNoSpecifiedTests=false` → BUILD SUCCESS（17 tests, 0 failures）。
- Doc-sync: No owner-doc update required（纯测试变更，无行为契约变化）。

### 2026-07-08（batch/sys-event 文档边界澄清）

- 新增 `nop-batch/nop-batch-sys` 模块：反向依赖 `nop-sys-dao`，提供 `SysEventBatchTrigger`，把 batch 仅作为普通事件的周期扫描/触发执行机制，而不把 `nop-batch` 直接下沉到 `nop-sys-dao`。
- 更新 `nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java`：保留 `nop-sys-dao` 作为事件存储与触发原语提供方，新增对 batch trigger 可见的轻量入口（fetch/claim/process getter facade），不改变现有直接触发路径。
- 更新 `docs-for-ai/03-modules/nop-sys.md`、`docs-for-ai/03-modules/nop-batch.md`、`docs-for-ai/04-reference/source-anchors.md`、`ai-dev/design/nop-sys/sys-event-architecture.md`：明确新的模块边界为“sys 提供原语，batch-sys 提供触发器”。
- 更新 `docs-for-ai/02-core-guides/batch-dsl.md`：补充 `retryPolicy`、`retryOneByOne`、`skipPolicy`、`transactionScope`、`dispatcher` 的成功/失败语义，并补充通过 `nop-job` 或 `IScheduledExecutor` 调度 batch 的选择建议；同时把语义边界修正为“支持 chunk flush，但保留逐条记录结果”。
- 更新 `ai-dev/design/nop-sys/sys-event-architecture.md`：修正普通事件失败策略，删除基础设施层默认 partition head blocking，改为失败后更新 `scheduleTime + retryTimes` 重新投递；同时把 batch 语义表述收敛为“可 chunk flush，但必须保留逐条 event outcome”。
- 修订已关闭计划 `ai-dev/plans/288-nop-sys-sys-event-reliability-refactor.md` 的事实性表述：撤回“普通事件默认 head blocking”与“单 event / 连续成功前缀 ack”这两处与 live code 不一致的 closure 文字，改为以 `ConsumeLater` 重排队 + 逐条状态更新为准。
- 偏差：中途根据 `nop-batch` 实现复核结果，撤回了“普通事件默认 head blocking”的设计表述；实现阶段进一步改为新增 `nop-batch-sys` 反向依赖模块，而不是让 `nop-sys-dao` 直接依赖 `nop-batch`。
- 验证：`./mvnw -pl nop-sys/nop-sys-dao -Dtest=TestSysDaoMessageService test` 受现有 `NopSysEventPartition` 缺失 `_gen` 类型阻塞；`./mvnw -pl nop-batch/nop-batch-sys -am -Dtest=io.nop.batch.sys.TestSysEventBatchTrigger -Dsurefire.failIfNoSpecifiedTests=false test` 因 reactor 过大在 120s 超时，未完成最终绿测。`node ai-dev/tools/check-doc-links.mjs --strict` 的上一次执行结果仍为通过（0 errors, 53 warnings；warning 为既有计划文档断链，非本次引入）。
- Doc-sync: `docs-for-ai/02-core-guides/batch-dsl.md` 已更新。
- 改进 `nop-job-local` 的 `BeanMethodJobInvoker`：用 `IClassModel` + `IFunctionModel` 替代原始 `java.lang.reflect.Method` 反射调用。现在支持三种模式：
  - 0-arg 方法 → `call0`
  - 单 `Map<String, Object>` 参数方法 → `call1`（兼容现有用法）
  - 多 `@Name` 注解参数方法 → `buildArgValues(methodParams)` 按名匹配，自动转型
  - 同时修复了两个缺陷：`invokeMethod` 现在返回 `Object` 而不是 `void`（保留了返回值）；如果返回值是 `CompletionStage` 则异步 `.thenApply()` 链式等待完成再返回 `JobFireResult`，避免异步方法结果丢失。
  - 验证：`./mvnw -pl nop-job/nop-job-local -am test` 通过（17 tests, 0 failures）。
