# 2026-08-16

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-16-2137-1` AR-1 双 generate executor 数据集可见性接线，全绿）

- **Fix AR-1（dashboard）**：`DatavGenerateDashboardExecutor.executeAsync` 解析可见性身份（`ChatBiDatasetVisibility.resolveOperator/resolveAdmin`，fail-closed，禁用本类 SYSTEM_OPERATOR 回退）传入 `validateAndPlanPanel`——数据集已加载且 status=1 但 `!isVisible` → 显式拒绝 `ERR_DATAV_CHATBI_DATASET_NO_ACCESS`（错误体经 `ValidationError.datasetNoAccess` 携带 errorCode + datasetSid + userName 三要素，`formatValidationError` 增加 userName 槽）；不存在/非活跃仍走 `ERR_DATAV_CHATBI_GENERATE_DATASET_NOT_FOUND` 分工；`:275` 不实注释（「按 RLS 由 DAO 处理」）替换为真实机制描述（镜像 `DatavListDatasetsExecutor` 口径）。
- **Fix AR-1（screen）**：`DatavGenerateScreenExecutor` 同款接线，预加载 cache 命中与「补查一次」两来源统一收口在 `validateAndPlanWidget` 判定点（预加载侧不静默过滤，无 NOT_FOUND 误报旁路）。
- **Schema/prompt 文本**：两个 generate `.tool.xml`（description Validation 段 + schemaJson datasetSid 描述）与 `ChatBiSystemPrompt` 两条 datasetSid 约束同步为「active AND visible to the current user」，标注 NO_ACCESS/NOT_FOUND 分工。
- **测试迁移（预期内，非回归）**：6 个 generate 路径测试类 fixture createdBy 对齐调用身份——`TestDatavGenerateDashboardExecutor`/`TestDatavGenerateScreenExecutor`（→OPERATOR）、`TestNopDatavChatBiDashboardE2E`/`TestNopDatavChatBiScreenE2E`（→CALLER）、`TestNopDatavChatBiScreenAction`（→OPERATOR）、`TestNopDatavChatBiTransactionBoundary.seedDataset`（→CALLER）。
- **新增回归 ×8**：{alice 拒绝(含错误体三要素 + 无落库残留断言), owner 放行, admin 跨 createdBy 放行, 无身份 fail-closed 拒绝} × {dashboard, screen}，镜像 `testQueryInvisibleDatasetReturnsExplicitError`/`testQueryVisibleForAdminAcrossCreators` 模式。
- **Doc-sync（Phase 2）**：`ai-design.md` §6 消费方清单补全为 6 工具 + 新增「数据集 sid 全部消费面固定清单」（ChatBI 6 工具 / `PanelDataBinder` 经 Dashboard RLS / 快照序列化暴露面→backlog #72），§8.5/§9.7 安全边界同步；`permission-sharing-design.md` §D4 承重前提修正（「D2 收敛 + generate 接线后仅 admin 可达」+ AR-1 教训：排除论证前提须随新消费工具复核 + 交叉引用 §6 清单）。
- 验证：`./mvnw test -pl nop-datav -am -T 1C` BUILD SUCCESS（datav-service **629/0/0** = 621 + 8 新增；GenerateDashboard 16、GenerateScreen 21、DashboardE2E 5、ScreenE2E 4、ScreenAction 5、TransactionBoundary 5 全绿）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS；`scan-hollow-implementations.mjs --module nop-datav --severity high` 0 findings；`check-doc-links.mjs --strict` 退出码 0（6 warning 均为无关 plan 338 既有项）。注：首轮全量跑 `nop-stream-rocksdb` 计时基准用例在 `-T 1C` 负载下抖动失败（ratio 2.78），单跑通过（ratio 0.058）——上游环境噪音，与本 plan 无关；重跑 reactor BUILD SUCCESS。
- Doc-sync: `ai-design.md` §6/§8.5/§9.7、`permission-sharing-design.md` §D4、本条目；plan Phase 1+2 勾选。

### 2026-08-16（MISSION nop-datav DRAFT — audit `2026-08-16-0719` 处置 + plan `2026-08-16-2137-1` 起草转 active）

- **Audit 处置**：open audit `ai-dev/audits/nop-datav/2026-08-16-0719-open-audit-nop-datav.md`（1 P0 + 2 P2）——AR-1 `[P0]`（`datav-generate-dashboard`/`datav-generate-screen` 绕过 P1-03 数据集可见性边界，非 admin 可构造引用任意数据集的看板并经 `PanelDataBinder` 外泄数据）入 plan；AR-2/AR-3（P2）与 AR-1 建议 4（独立裁定项）登记 `nop-datav-audit-followups.md` **#70/#71/#72**（Sources 头同步）；audit status open → **planned**。
- **Plan 起草**：`ai-dev/plans/nop-datav/2026-08-16-2137-1-chatbi-generate-dataset-visibility-closure.md`（Execution Order 1 of 1）——两 executor 接 `ChatBiDatasetVisibility`（身份禁用 SYSTEM_OPERATOR 回退）、screen 双检查点收口、不实注释 + tool schema 文本修正、fixture 迁移预警（createdBy="test" vs operator 不对齐，直调 + E2E 类均涉及）、{拒绝/owner/admin/无身份}×{dashboard,screen} 测试矩阵、owner doc §D4 承重前提修正 + §6 消费面固定清单。
- **Draft Review（两轮独立子 agent 对抗性审查，含想象性分析）**：R1（`ses_ff530314bffe9tKs7MdFKPoWaq`）1 Major（backlog #70-72 未登记却用完成时态引用）+ 4 Minor，全部修复（backlog 实际登记 + UkHeuristic 移出对齐清单 + 错误体措辞对齐先例 + Phase 1 doc 归属声明 + gates 补 scan-hollow）；R2（`ses_ff526777dffenaZ7zjxIyAZOcA`）5 项修复逐项验证到位、无 Blocker/新增 Major、baseline 与 live repo 零漂移、结论「可以转 active」；残余 2 文案级 Minor（载体措辞删 how、测试项补 Proof 标注）已修正。Plan Status → **active**。
- 工具：`check-doc-links.mjs --strict` 退出码 0（6 warning 均为无关 plan 338 既有项）。

### 2026-08-16（MISSION nop-datav CLOSURE — plan `2026-08-15-2146-3` 收口，Plan Status → completed）

- 独立子 agent closure audit（fresh session `ses_ff65c1b45ffeanpNMCICDA9tR1`，research-only）：19/19 全 PASS（Phase 3 五项 file:line 证据 + Phase 4 三项含 git diff wire 兼容程序化核验 + Phase 5 两项 + Phase 1-2 抽查六项 + Anti-Hollow 三项：CrudBizModel.delete(id)→doDelete→虚分派 doDeleteEntity 源码链 + 四覆写零吞异常 + 生成物 diff 机械再生无手改签名），期间独立复跑 4 个新测试类 20/20 green，verdict **CLOSABLE**。Minor×4（plan 文本「96 个 define」立项旧数、+1 新增键为 D1(b) 裁定 additive、索引 21=D3 裁定结果、mutate 自检不可事后复现但零残留）均非阻塞，已记入 plan Non-Blocking Follow-ups。
- Closure Gates 9 项全勾选：`./mvnw test -pl nop-datav -am -T 1C` BUILD SUCCESS（datav-service 621/0/0）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS（生成物再生零手改）；`check-plan-checklist.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --module nop-datav --severity high` 0 findings；`check-doc-links.mjs --strict` 退出码 0（6 条 warning 均为无关 plan 338 既有项）。
- Plan 收口：Closure 段写入 audit 证据（逐 Phase file:line 清单 + 工具退出码 + Anti-Hollow 结论）；Plan Status → completed（Completed: 2026-08-16）。
- Roadmap/backlog/audit 同步：`nop-datav-roadmap.md` Last updated 前置本轮 CLOSURE 记录；`nop-datav-audit-followups.md` plan 3 标注已 completed 2026-08-16（三 plan 批次全部完成）；源审计 `ai-dev/audits/nop-datav/2026-08-15-1913-multi-audit-nop-datav.md` Audit Status → closed（P0/P1 全部 remediated，P2 已 triage 至 backlog）。

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-15-2146-3` Phase 5 测试缺口补齐（P1-11 + P1-12），全绿）

- **Fix P1-11**：`TestNopDatavReportE2E.testImChannelPartialNoBindingAnySentDelivered`——`setNoBindingUsers(Set.of("u1"))` + recipients `["u1","u2"]` → 经 executor 端到端交付 SUCCEEDED + `deliveredChannels="im"`（anySent 渠道聚合语义锚定）；同时断言 u1 也被逐 userId 尝试（NO_BINDING 不中断循环）。与既有全 SENT / 全 NO_BINDING 用例构成聚合语义三分支完整覆盖。
- **Fix P1-12**：新增 `TestPanelSqlBuilder`（纯 JUnit 无容器，7 用例）——空 SQL 分支 A（null/""→NopException + ERR_DATAV_QUERY_FAILED + panelId param）、未声明占位符分支 B、已声明 null 值绑定 ?（JDBC NULL 语义）、多占位符按出现顺序绑定（重复占位符逐次绑定）、**注入回归锚点**（`north' OR '1'='1` 进 `getParams()` 绑定通道、SQL 文本仅含 `?`）、静态 SQL 无占位符透传。
- **Mutate 自检（两处均先红后恢复）**：①`sendToUsersAggregated` 临时改为「任一 non-SENT 即渠道失败」→ partial-success 用例红（expected SUCCEEDED got FAILED）后恢复；②`PanelSqlBuilder` 临时内联拼接参数值 → 2 用例红后恢复。`git diff` 确认两实现零残留改动。
- 验证：`./mvnw test -pl nop-datav -am -T 1C` BUILD SUCCESS（datav-service **621/0/0** = 611 + 2 i18n + 7 SqlBuilder + 1 IM partial）。
- Doc-sync: 本条目；plan Phase 5 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-15-2146-3` Phase 4 错误码语言契约收敛（P1-07），全绿）

- **Fix P1-07**：`NopDatavErrors.java` 全部 100 个 `ErrorCode.define` 描述改中文（`ERR_DATAV_CHATBI_GENERATE_DUPLICATE_DASHBOARD_NAME` Phase 2 已中文，本轮其余 99 个）；**转换类分界逐条裁定**：nop-datav 全部错误码均为业务错误码（not-found/owner/配置/上限/契约拒绝），无一属「裸异常转换、逐字保留历史消息」类——零英文例外，`error-handling.md` 白名单无需变更。
- **en i18n 键（load-bearing）**：新增 `nop-datav-meta/src/main/resources/_vfs/i18n/en/nop-datav-errors.i18n.yaml`——100 键（键=错误码全串，先例 `nop-cli-core/nop-cli-errors.i18n.yaml`；`I18nMessagesLoader` 扫描 `/i18n/{locale}` 下非 `_` 前缀 `*.i18n.yaml`，nop-datav-meta 的 retention 文件机制核实后选择独立文件而非并入 `nop-datav.i18n.yaml`——错误码键与实体 displayName 键关注点分离）；en 翻译沿用原英文描述；占位符集合与 define() 逐键一致（脚本核验 100/100 无漂移）。
- **wire 兼容锚定**：错误码字符串键与 ARG 常量集合零变更（git diff 仅描述行）；新增 `TestNopDatavErrorsI18n`（2 用例：全部 define 描述含非 ASCII（中文契约锚定）+ en locale 全 100 键可解析且占位符集合与 define() 一致 + 错误码字符串唯一性）。
- **既有断言零破坏核实**：`NopException.getMessage()` 恒内嵌 `errorCode=nop.err.datav.*` 子串，既有 message-text 断言（`TestNopDatavAlertE2E` field/column/template、`TestNopDatavReportE2E` channel/notify 等）经 errorCode 子串继续命中——611 全量用例改中文后全绿（无断言需改写；「断言同步更新」以实测全绿方式兑现）。
- **实现期坑**：`-pl nop-datav/nop-datav-service` 单模块跑测试取 ~/.m2 旧 meta jar（无新 i18n 文件）→ en 键解析失败；`-am` 联编 meta 后通过。i18n 文件跨模块时必须带 `-am` 验证。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am -T 1C` BUILD SUCCESS（613/0/0 = 611 + 2 新增）。
- Doc-sync: `NopDatavErrors.java` 头部语言契约 javadoc；本条目；plan Phase 4 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-15-2146-3` Phase 3 级联删除/解绑补齐（P1-09），全绿）

- **Fix P1-09 四处缺口**（主代码 + 回归测试本轮收口）：
  - `NopDatavReportTaskBizModel.doDeleteEntity`：既有 `unregisterTask` 之后级联物理删除 ReportDelivery 子行（`deleteByQuery`，镜像 Dashboard/Screen 先例）；
  - `NopDatavAlertRuleBizModel.doDeleteEntity`：级联物理删除 AlertState 子行（`unregisterRule` 保持）；
  - `NopDatavChatSessionBizModel`：覆写 `doDeleteEntity` 级联删除 ChatMessage——标准 `__delete` 与自定义 `deleteChatSession` 语义收敛（只复用 ChatBiSessionManager 的消息级联片段，不整体调用其 `requireSession` owner 校验 + session 自删，避免双重删除与 owner-scope 校验注入 admin 标准路径）；
  - `NopDatavDashboardTabBizModel`：覆写 `doDeleteEntity` 删除前**解绑**（置 null）引用该 tab 的 `Panel.tabId`（裁定：解绑而非拒绝删除——Panel.tabId 可空，解绑后面板回落看板默认层；拒绝式会阻塞页签管理。禁止悬挂引用）。
- **回归测试 ×4**（新增 `TestNopDatavSubEntityDeleteCascade`，5 用例）：全部经 biz 层标准 delete action 触发（Anti-Hollow 接线验证），物理删除终态经 jdbcTemplate 原生 count 断言（不受 ORM session 缓存影响）——①删 ReportTask → delivery 行数 0（他人 task 的 delivery 不受扰动）+ job 即时注销；②删 AlertRule → state 行数 0 + job 注销；③a 标准 `__delete` 删 ChatSession → 消息行数 0；③b 自定义路径 `deleteSession` 同终态（语义收敛锚定）；④删 Tab → 面板保留 + tabId=null + 无悬挂引用（`count where TAB_ID=tab` = 0）。
- 验证：`./mvnw test -pl nop-datav -am -T 1C` BUILD SUCCESS（datav-service **611/0/0** = 606 + 5 新增；既有 Dashboard/Screen/Panel 级联测试不回归）。
- Doc-sync: `model-design.md`「级联删除/解绑契约」章节（4 聚合根级联矩阵 + Tab 解绑裁定 + 失败异常传播约定）；本条目；plan Phase 3 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-15-2146-3` Phase 2 UK/索引物化与迁移脚本（P0-03 + P1-08），全绿）

- **Fix P0-03**：`nop-datav.orm.xml` 8 个 `<unique-key>` 全部补 `constraint="UK_..."` 属性；经 `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` 重建，三方言 `_create_nop-datav.sql` 各含 8 条 UK 约束、三方言 `_add_tenant_nop-datav.sql` 再生为 `drop + add constraint ... unique (NOP_TENANT_ID, ...)`（平台 `AddTenantIdToUniqueKey` 机制自动接线，无手改生成物）。**D3 收敛（P2-11）**：`UQ_NOP_DATAV_ALERT_STATE_RULE` 更名 `UK_NOP_DATAV_ALERT_STATE_RULE`（前缀统一 UK_），删除与其物理重复的 `IX_NOP_DATAV_ALERT_STATE_RULE` 普通索引。
- **Fix P1-08**：新增存量迁移脚本 ×3 方言：`_add_unique_nop-datav.sql`（8 条 ALTER TABLE ADD CONSTRAINT + 头部审计 SQL 注释，fail-fast 策略——不自动去重，重复行使 ADD CONSTRAINT 失败由运维裁定）+ `_add_index_nop-datav.sql`（21 条 CREATE INDEX；方言列名大小写对齐生成物惯例：MySQL/Oracle 大写、PG 小写；语法对照 nop-auth `_add_ext_login_unique.sql` / nop-metadata `upgrade-*.sql` 先例）。
- **D1(b) dashboard 侧重名兜底（入 scope 实施）**：`DatavGenerateDashboardExecutor` 镜像 Screen executor 裁定 Q——`existsDashboardByName` 预检查 + 新错误码 `ERR_DATAV_CHATBI_GENERATE_DUPLICATE_DASHBOARD_NAME`（中文描述）+ 底层 UK 冲突映射（复用 `DatavGenerateScreenExecutor.isUniqueConstraintViolation`）。
- **测试（7 新增全绿）**：`TestNopDatavUniqueKeyEnforcement`（6 用例：三方言 DDL 发射 8 UK 断言——镜像 nop-metadata `TestNopMetaDdlUniqueKeyEmission` 先例；H2 测试库 5 类重复插入被数据库拒绝——shareToken/(sessionId,seq)/(dashboardId,snapshotVersion)/dashboardName/alertRuleId，锚定「数据库层强制非应用层检查」）+ `TestDatavGenerateDashboardExecutor.testDuplicateDashboardNameRejectedWithStructuredError`（重名 → 结构化错误码，LLM rename-retry 契约）。
- **测试 fixture 排查**：全量重跑零破坏（各套件 dashboard/screen 名天然互异；UK 物化对既有 599 用例零影响）。
- 验证：`./mvnw test -pl nop-datav -am -T 1C` BUILD SUCCESS（datav-service 606/0/0）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS。
- Doc-sync: `model-design.md`（Phase 1 已落档裁定与实现零漂移复核）；`nop-datav-audit-followups.md` P2-11/P2-14 标注已消化；本条目；plan Phase 2 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-15-2146-3` Phase 1 唯一作用域与物化策略裁定）

- **D1 裁定（P2-14）**：dashboardName/screenName 全表唯一（tenant 部署经再生的 `_add_tenant_nop-datav.sql` 重建为 NOP_TENANT_ID 前缀，平台 `ddl.xlib` `AddTenantIdToUniqueKey` 机制）。组合键 `(tenantId,createdBy,name)` 核实不可表达（tenantId 非基础模型列，`<unique-key columns>` 只能引用已声明列）→ 降为不可选。dashboard 侧 ChatBI 重名兜底（镜像 Screen executor 裁定 Q）显式裁定**入本 plan scope**（Phase 2 实施），消除物化引入裸 DB 异常的回归面。
- **D2 裁定**：三条建表路径（deploy SQL 三方言 / auto-DDL / 测试基类 createAllTables）共用 `DdlSqlCreator`→`ddl.xlib`（`uniqueKey.constraint` 属性驱动，`nop-datav-codegen/postcompile/gen-orm.xgen` 于 mvn install 再生）；迁移脚本形态 = 手写 `_add_unique_nop-datav.sql` + `_add_index_nop-datav.sql` 两文件 × 三方言（对齐 nop-auth `_add_ext_login_unique.sql` / nop-metadata `upgrade-*.sql` 先例）。
- **D3 裁定**：22 索引中 `IX_NOP_DATAV_ALERT_STATE_RULE` 与 UK 物化后物理重复 → 从模型删除（P2-11 收敛）；UQ_ 前缀统一为 UK_；其余 21 索引全量物化；`IX_NOP_DATAV_CHAT_MSG_SESSION` 最左前缀理论冗余 watch-only 保留。
- **存量重复数据策略**：预检 + fail-fast（脚本头注释审计 SQL，重复行使 ADD CONSTRAINT 按库默认行为失败，运维裁定去重后重试；拒绝自动去重——无规范赢家、掩盖根因）。
- 全部裁定落档 `ai-dev/design/nop-datav/model-design.md`（唯一键矩阵 8 键 / 索引矩阵 / 物化路径三方言）。
- Doc-sync: `model-design.md` 新增「唯一键与索引物化契约」章节；本条目；plan Phase 1 勾选。

### 2026-08-16（MISSION nop-datav CLOSURE — plan `2026-08-15-2146-2` 收口，Plan Status → completed）

- 独立子 agent closure audit（fresh session `ses_ff754b301ffewfjGhKGvEm4oMH`，research-only）：四 Phase 全部 Exit Criteria + Closure Gates 逐项 PASS（file:line 证据），Anti-Hollow 结论 **GENUINE**（四条调用链 live 追踪：createExportTask→afterCommit、triggerReportNow→execute→afterCommit、evaluateAlertNow→evaluate→afterCommit→REQUIRES_NEW 回写、chatToDashboard/chatToScreen→挂起→补偿删除，seam/裸 JDBC 断言证明运行时真实触发），生成物零触碰（git status 核查），verdict **CLOSABLE**。Minor×2：测试文件目录-包不一致（当场修复迁移至 entity/）；handler 解析容忍为既有语义观察（无行动）。
- Closure Gates 9 项全勾选：`./mvnw test -pl nop-datav -am` reactor BUILD SUCCESS（datav-service 599/0/0，复跑二次确认稳定）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS；`check-plan-checklist.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --module nop-datav --severity high` 0 findings；`check-doc-links.mjs --strict` 退出码 0（6 条 warning 均为无关 plan 338 既有项）。
- Plan 收口：Closure 段写入 audit 证据（逐 Phase file:line 清单 + 平台机制事实两项）；Plan Status → completed（Completed: 2026-08-16）；Non-Blocking Follow-ups 增补 audit Minor 观察项。
- Roadmap/backlog 同步：`nop-datav-roadmap.md` Last updated 前置本轮 CLOSURE 记录；`nop-datav-audit-followups.md` 两处 plan 状态标注已 completed 2026-08-16。

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-15-2146-2` Phase 4 资源上界补齐（P1-04 + AR-3），全绿）

- **Fix P1-04**：`DatavQueryDatasetExecutor` maxRows 服务端硬钳制到 `[1, CFG_DATAV_CHATBI_MAX_ROWS]`（null/<=0 落配置缺省，>0 取 min）——返回值恒 >0，`doQuery` 的 LongRangeBean 恒非 null（消除「传 0/负数 → range=null 全表物化」绕过面）；`datav-query-dataset.tool.xml` schemaJson 补 `minimum/maximum` + 漂移裁定声明（schema 值为缺省快照，运行时以配置钳制为准——tool 描述 + `NopDatavConfigs` Description 同步「缺省值→硬上限」语义）。**实现期坑**：schemaJson 为 XML 文本节点，描述含裸 `<=` 破坏 XML 解析（tool manager 加载失败 → 全 ChatBI 套件崩）——改用 "non-positive" 措辞，`xml.etree` 校验通过。
- **Fix AR-3**：`PanelDataExporter.exportDashboard` 入口面板数前置上限——**复用 `CFG_DATAV_DASHBOARD_QUERY_MAX_PANELS`**（裁定：同为性能界防 SQL 放大，共用一闸；对照 layout 上界因存储容量界独立配置）；超限抛 `ERR_DATAV_DASHBOARD_PANEL_LIMIT_EXCEEDED`（panelCount/maxPanels，显式失败非静默截断），校验先于任何面板取数；报告交付路径（复用 exportDashboard）自动获得同保护。
- **测试**：新增 `TestDatavQueryDatasetMaxRowsClamp`（`@NopTestProperty` 压配置到 3：0/负数/巨值/缺失四态→3、边界 =3 放行、低于上限按入参——mutate-fail 锚定）+ `TestPanelDataExporterPanelCap`（51 面板→结构化错误含 params、50 面板边界放行产出 50 sheet）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service` **599/0/0** 全绿（595 + 4 新增，复跑二次确认稳定；期间一次 audit 假阳性出现在 tool.xml 损坏期 run，修复后连续两次全绿）。
- Doc-sync: `ai-design.md` §7 增补 §7.1（钳制语义 + schema 漂移裁定 + 锚定）；`runtime-design.md` 新增 §十一（AR-3 裁定 + 四路径防护对称性表 + 拒绝方案）；本条目；plan Phase 4 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-15-2146-2` Phase 3 长事务收敛（P1-05 + P1-06），全绿）

- **Decision P1-06（主案 (a)）**：事务内写 interim 状态（不含 lastNotifiedTime）→ afterCommit 发送 → 成功后 REQUIRES_NEW 独立短事务重载状态行回写；失败 ERROR 日志不回写（rearm 契约保持）。拒绝 (b) 异步线程（引用未提交数据风险）、(c) 维持现状（已确认缺陷）。**实现期实证**：afterCommit listener 执行期间外层事务仍在线程注册表（cleanup 在 commit() 返回后），回写用 REQUIRED 会「加入已提交事务」→ SQL 悬空丢失 → 必须 REQUIRES_NEW。
- **Fix P1-06**：`AlertEvaluator` 注入 ITransactionTemplate/IOrmTemplate（setter，null 安全降级同步路径）；三个通知点（TRIGGER/RECOVER/REARM）统一经 `notifyWithRearmContract`——无事务（cron/直调）同步发送+立即回写（逐条等价，`TestNopDatavAlertE2E` 28 用例全绿锚定）；事务内延迟分支。语义微调落档 §12c（evaluateAlertNow 返回中间态等 4 项）。
- **Decision P1-05**：(a) 工具集无回读已生成实体的工具 → 即时提交不破坏多轮语义；(b) 失败语义选「每工具独立短事务 + 失败补偿删除」（拒绝缓冲 spec——工具协议须返真实 id；拒绝接受部分落库）；(c) 生成 executor 创建包 REQUIRED 短事务（无 ambient 新开=原子+即时提交；有 ambient 加入=修复前行为）。**实现期实证 2**：异常路径下 ambient 事务已被 runWithoutTransaction 恢复注册且注定回滚 → 补偿删除用 REQUIRED 会随之回滚（生成物复活）→ 必须 REQUIRES_NEW。
- **Fix P1-05**：`NopDatavChatBiBizModel.chatToDashboard/chatToScreen` 经 `runWithoutTransaction` 挂起 ambient 事务执行 LLM 循环（机制锚定：每次 LLM 调用时 isTransactionOpened=false）；handler 改工厂方法（成功生成 id 记入补偿清单）；循环异常 → 补偿删除（Dashboard+Panel+DatasetRef / Screen+ScreenWidget，REQUIRES_NEW+新 session，逐 id 容错 ERROR）；`DatavGenerateDashboard/ScreenExecutor` 创建阶段包 REQUIRED 短事务。
- **测试**：新增 `TestNopDatavChatBiTransactionBoundary` 5 用例（事务挂起机制锚定+即时提交可见、失败补偿×3 含 ambient 事务内 REQUIRES_NEW 存活锚定、成功路径保留）+ `TestNopDatavAlertNotifyTransactionBoundary` 4 用例（延迟发送+回写 TRIGGER/RECOVER、发送失败 rearm 重试闭环、无事务同步守护）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service` **595/0/0** 全绿（586 + 9 新增）；既有 ChatBI/alert/生成 executor 全套件不回归。
- Doc-sync: `ai-design.md` 新增 §11（三决策点裁定+拒绝方案+实现形态+语义锚定）；`schedule-report-design.md` 新增 §12c（主案 (a) 契约+语义微调 4 项+拒绝方案）；本条目；plan Phase 3 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan `2026-08-15-2146-2` Phase 2 异步提交时序与可观测性（AR-2），全绿）

- **修复**：`NopDatavExportTaskBizModel.createExportTask` 与 `ReportDeliveryExecutor.execute` 的 worker 提交改为事务提交后执行——事务上下文内经 `ITransactionTemplate.afterCommit` 注册（export 侧复用 `CrudBizModel.txn()`，**不重复声明字段**：上一 run 的影子 protected `transactionTemplate` 字段被 NopIoC 经基类 setter 注入绕过、恒为 null，已删除改走 `txn()`）；无事务上下文（cron/恢复/裸调用）保持立即提交（`isTransactionOpened` 守护，不抛 `ERR_TXN_NOT_IN_TRANSACTION`）；回滚时 onAfterCommit 不触发（无孤儿异步任务）。null 分支两侧从静默 `return null` 改为短退避重查（3×200ms）→ ERROR 日志 + `markFailedSafe`（No Silent No-Op）。
- **配套出参契约**：`createExportTask` 返回 detached 副本（镜像 Phase 1 share 模式）——afterCommit 语义下 worker 在 commit 后立即推高 version，attached 出参触发 GraphQL 响应装载 `entity-version-changed`（E2E 实证复现后修复）；响应字段不变。
- **测试设计落档（Proof 裁定 (a)+(c) 组合）**：新增 `TestNopDatavAsyncSubmitTransactionPath` 6 用例——(a) `runInTransaction` 内直调：事务内 seam 计数 0（仅注册）/commit 后 +1/回滚不触发且任务行回滚不存在（export+report 双侧）；(c) 无事务直调：立即 +1 + 任务推进 SUCCEEDED（守护分支锚定）；外加 graphQLEngine mutation 真实事务路径 E2E（mutation 返回 → 裸 JDBC 轮询 SUCCEEDED，plan Closure Gates 的 AR-2 常驻资产）。seam：双侧 `submitExecutionInvokedCount`（package-private/public getter）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service` **586/0/0** 全绿（580 + 6 新增）；stuck 扫描器/报告 E2E/alert E2E 全部不回归。
- Doc-sync: `runtime-design.md` 新增 §十（导出事务边界 + detached 出参契约 + 拒绝方案）；`schedule-report-design.md` 新增 §12b（报告侧对称裁定）；本条目；plan Phase 2 勾选。

### 2026-08-16（MISSION nop-datav CLOSURE — plan `2026-08-15-2146-1` 收口，Plan Status → completed）

- 独立子 agent closure audit（fresh session `ses_ff7e5d880ffeElwhq5KgP50wUN`，research-only）：9/9 全 PASS（P0-01/P0-02/P1-01/P1-02/P1-03/P1-10 逐项 live 代码证据 + 测试 Anti-Hollow + docs + deferred 诚实性），verdict **CLOSABLE**；审计期间独立复跑 5 个权限测试类 41/41 green。非阻塞观察：`requirePanelDashboardAccess` checker==null 时放行——逐字节镜像仓库先例 `requireSourceAccess`（非新降级，checker-present 路径有测试锚定）。
- Closure Gates 9 项全勾选：`./mvnw test -pl nop-datav -am`（reactor 59 模块 BUILD SUCCESS，datav-service 578/578）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS（未改 ORM/xmeta/I*Biz，`_gen` 零触碰无漂移）；`check-plan-checklist.mjs --strict` / `scan-hollow-implementations.mjs --module nop-datav --severity high`（0 findings）/ `check-doc-links.mjs --strict`（0 errors）均退出码 0。
- Plan 收口：Closure 段写入 audit 证据（逐缺陷 file:line 证据清单）；Plan Status → completed（Completed: 2026-08-16）；AR-7 缓存键耦合从 Non-Blocking Follow-ups 改记「已显式裁定落档非 debt」。
- 累计变更：production+test action-auth/data-auth 五处 XML、NopDatavPanelBizModel（归属校验）、ChatBI 身份接线（BizModel/Loop/Context/Visibility/3 executor）、NopDatavSingleWriterCrudBizModel + 3 BizModel、NopDatavErrors +4 错误码、21 新测试（6 RBAC + 5 data-auth + 4 ChatBI + 6 单写点）；docs：permission-sharing-design.md（D1-D5 + 17 实体矩阵 + drift 纠正）、ai-design.md §6。
- Roadmap/backlog 同步：`nop-datav-roadmap.md` Last updated 前置本轮 CLOSURE 记录；`nop-datav-audit-followups.md` 标注 plan 1 已 completed（plan 2/3 仍 planned，共享审计文档状态不动）。

### 2026-08-16（MISSION nop-datav EXECUTE — plan 2026-08-15-2146-1 Phase 5 快照/告警状态 mutation 面裁剪，全绿）

- **P1-10（D5 方案 c）**：新增 `NopDatavSingleWriterCrudBizModel<T>`（覆写全部 13 个标准 mutation 入口：save/saveOrUpdate/copyForNew/update/batchUpdate/updateByQuery/batchModify/delete/batchDelete/deleteByQuery + m2m 三件套，一律抛子类 `stdMutationNotAllowedError()` 提供的结构化错误码 + ARG_ACTION/ARG_BIZ_OBJ_NAME）；`NopDatavDashboardSnapshotBizModel`/`NopDatavScreenSnapshotBizModel`/`NopDatavAlertStateBizModel` 改继承该基类（分别返回 `ERR_DATAV_SNAPSHOT_STD_MUTATION_NOT_ALLOWED` / `ERR_DATAV_ALERT_STATE_STD_MUTATION_NOT_ALLOWED`）。领域写入点（publish/rollback/AlertEvaluator）全走 DAO 直写不受影响（实现前逐一核实）。
- 测试：新增 `TestNopDatavSingleWriterMutationPruning`（6 用例，经 `IGraphQLEngine` 全链：admin 过 action-auth 后被 BizModel 守卫结构化拒绝——Snapshot save/update、ScreenSnapshot delete、AlertState update/batchDelete 均断言专属错误码；Snapshot findPage 查询面回归放行）；test action-auth 补 `NopDatavAlertState-main` FNPT 块（镜像 production）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service` **578/0/0** 全绿（572 + 6 新增；含 publish/rollback/alert/share E2E 领域写入点回归）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS（未改 ORM/xmeta/I*Biz，生成物无漂移）。
- Doc-sync: 本条目；plan Phase 5 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan 2026-08-15-2146-1 Phase 4 ChatBI 数据集可见性接线，全绿）

- **P1-03（D4 选项 B）**：
  - `ChatBiToolExecuteContext` 新增 `admin` 标志（3 参构造 + `isAdmin()`，2 参构造回归兼容委托 false）；`ChatBiToolCallingLoop` 新增 admin 传递重载（7 参 + 6 参+admin，既有 5/6 参重载委托 admin=false 回归兼容）；`NopDatavChatBiBizModel` 的 chatToQuery（单轮+会话）/chatToDashboard/chatToScreen 四处解析身份（operator 经 `NopDatavOperatorResolver`，admin 经 `IServiceContext.getUserContext()` 回退线程级 `IUserContext.get()`）并随循环传递。
  - 新增 `ChatBiDatasetVisibility`（单点判定：admin 全量 / 非 admin `createdBy == operator` / 无身份 fail-closed；强转耦合契约读取 context）。
  - `DatavListDatasetsExecutor`：list SQL 下推 createdBy 过滤（admin 不加）+ 无身份空列表；:97-99 不实注释改写为真实机制（nop-report 数据集无 DAO 层 RLS，可见性由本 executor 显式实施）。
  - `DatavQueryDatasetExecutor`/`DatavDescribeDatasetExecutor`：not-found 之后执行前可达性校验 → 显式拒绝携带新错误码 `ERR_DATAV_CHATBI_DATASET_NO_ACCESS`（describe 为一致扩展：同属 schema 泄露面，已在 D4 落档）。
  - `PanelDataBinder` 检查点：D4 definitive 排除（授权经 Dashboard RLS 传递 + 发布即领域共享语义 + D2 后 DatasetRef 绑定面 admin-only）。
- **AR-7 缓存键耦合落档**：`ai-design.md` §6 重写（可见性契约 + 硬约束提醒——可见性仅 ChatBI executor 路径生效不触 `DashboardPanelQueryCache`，缓存键无需按用户失效；未来引入面板路径 per-user 可见性时必须同时改缓存键）；§6 旧文「复用既有 owner RLS」不实陈述一并纠正。
- 测试：`TestDatavListAndDescribeExecutors` +2（list 三态：alice 仅己/admin 全量/无身份 fail-closed 空；describe 拒绝带结构化错误码 + owner/admin 放行）；`TestDatavQueryDatasetExecutor` +2（query 拒绝带码；admin 跨 createdBy 放行真实行集）；既有直调用例全部改传显式身份 context；`TestNopDatavChatBiE2E` 主线/maxiter 设置 admin IUserContext（finally 清理）；`TestNopDatavChatBiSessionE2E` 数据集 owner 改为 alice（与 operator 一致）。
- 验证：ChatBI 相关 80/80；全模块 **572/0/0** 全绿（568 + 4 新增）。
- Doc-sync: `ai-design.md` §6 + 头部 Status；本条目；plan Phase 4 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan 2026-08-15-2146-1 Phase 3 子实体与 FilterState 收口，全绿）

- **P1-01**：`nop-datav.action-auth.xml` 5 个只读子实体（DashboardSnapshot/DashboardTab/DatasetRef/ScreenWidget/ScreenSnapshot）query 收敛 `admin`（D2）；test action-auth 镜像（DashboardSnapshot 原有条目改绑 + 新增 4 个子实体块）。
- **P1-02**：FilterState mutation 收敛 `admin`（production + test action-auth）；`nop-datav.data-auth.xml`（production + test）新增 `NopDatavFilterState` obj（admin 无 filter；user `eq userName ${$context.userName}`）——设计 §69 语义在继承 CRUD 层落地。
- 测试：`TestNopDatavRbacAuth` +3（5 子实体 findPage user 拒 + admin 放行；FilterState `__save` user 拒）；`TestNopDatavDataAuth` +2（FilterState findPage userName 隔离 + admin 全见；`__get` 他人行 ERR_AUTH_NO_DATA_AUTH + owner 放行）。
- 平台层序发现（记录）：D2 收敛后 `testFailClosed_nonAdminNoMatchingRole` 的原断言（GraphQL 层 ERR_AUTH_NO_DATA_AUTH）不可达——RBAC 层先拒（ERR_AUTH_NO_PERMISSION，defense in depth）；该测试改为锚定 RBAC 先拒语义，纯 data-auth fail-closed 由 `testFailClosed_directCheckerThrows`（直调 checker，viewer 角色）继续锚定。本 plan 收口后全部 user 可达 query FNPT 均有匹配的 user data-auth 规则（无合法用户落入 fail-closed 陷阱）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service` **568/0/0** 全绿（563 + 5 新增）。
- Doc-sync: 本条目；plan Phase 3 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan 2026-08-15-2146-1 Phase 2 Panel 锚点修复，全绿）

- **P0-01**：`nop-datav.action-auth.xml` Panel `FNPT:query/mutation` 均收敛 `admin`（D1 裁定同步执行）；test `_vfs/test/datav/auth/app.action-auth.xml` 镜像同步。
- **P0-02**：`NopDatavPanelBizModel` 新增 `requirePanelDashboardAccess(panel, action, context)`（镜像 `requireSourceAccess` 先例：加载所属 Dashboard → `checker.isPermitted("NopDatavDashboard", ...)` → 不通过抛 `ERR_AUTH_NO_DATA_AUTH` + `ARG_BIZ_OBJ_NAME`），接入 getPanelData/refreshPanel/resolveLinkage/resolveJump 四 action（`requireEntity(Panel)` 之后）。Screen 侧核实：全部自定义 action 锚定 `requireEntity(Screen)`（有 RLS），无对称入口。
- 测试：`TestNopDatavRbacAuth` +3（user `__update` 拒 ERR_AUTH_NO_PERMISSION / user `__findPage` 拒（D1 query 收敛）/ admin `__update` 放行）；`TestNopDatavDataAuth` +3（非 owner 未发布四 action 全拒 ERR_AUTH_NO_DATA_AUTH / 已发布四 action 全放行 / owner 未发布放行）。全部经 `IGraphQLEngine` 全链。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service` **563/0/0** 全绿（557 既有 + 6 新增）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS（生成管线无漂移）。
- Doc-sync: 本条目；plan Phase 2 勾选。

### 2026-08-16（MISSION nop-datav EXECUTE — plan 2026-08-15-2146-1 Phase 1 收口策略裁定）

- Phase 1（Decision | Fix）完成：D1-D5 全部落档 `ai-dev/design/nop-datav/permission-sharing-design.md` 新增章节「权限收口裁定与最终权限矩阵（plan 2026-08-15-2146-1）」。
  - **D1**：Panel query+mutation 均收敛 `admin`。枚举向量根因（`Panel__findPage` 全量可读 panelConfig）与运行时读路径核实（getDashboardData 经 DAO 内部加载、getPublishedDashboard 消费快照、四面板 action 为独立 FNPT 权限点）均写入裁定理由；「保留 + 间接 RLS」分支因 data-auth DSL 无跨实体嵌套过滤先例 + Panel 无 publishStatus 列被拒。
  - **D2**：DashboardSnapshot/DashboardTab/DatasetRef/ScreenWidget/ScreenSnapshot query 收敛 `admin`（镜像 Share/ExportTask D1 先例）；逐实体核实无 user 侧 GraphQL 消费（运行时路径全走快照/DAO 内部加载）。
  - **D3**：FilterState = query 保留 `admin,user` + data-auth `eq userName`（user 规则）+ mutation 收敛 `admin`。
  - **D4**：ChatBI 可见性 = 选项 B（`NopReportDataset.createdBy`/admin），list 侧 SQL 过滤 + describe/query 侧显式拒绝（含 describe 执行器一致扩展）；选项 A（NopReportDatasetAuth ACL）确认为 nop-report 过时残骸被拒；**PanelDataBinder 检查点 definitive 排除**（授权经 Dashboard RLS 传递成立：Phase 2 归属校验 + getDashboardData requireEntity + 发布即领域显式共享语义 + D2 后 DatasetRef 绑定面 admin-only）；AR-7 缓存键耦合显式裁定（可见性仅 ChatBI executor 路径生效、不触 DashboardPanelQueryCache，缓存键无需按用户失效，硬约束提醒已落档）。
  - **D5**：方案 (c) BizModel 覆写 13 个标准 mutation 入口抛结构化异常（显式拒绝），xbiz remove/xmeta 禁写因全仓无先例被拒；领域写入点全走 DAO 直写已核实不受影响。
- owner-doc drift 修复（guide 规则 15 归 Fix）：§「自定义 action 行级校验清单」Panel 四行「Panel 实体 checkDataAuth 已覆盖」不实陈述纠正（实际机制 = panel→Dashboard 归属校验）；§「子资源行级裁定」表「经 Dashboard 归属隐式覆盖」不实陈述纠正；§49 plan {1} 偏离裁定增补 2146-1 收口引用（其余 12 实体维持平台默认）；17 实体最终权限矩阵落档（本节为单一事实来源）。
- 平台机制核实（裁定证据）：`SiteCacheData.isPermitted → isUserInAnyRole → isUserInRole("user") 对任意已认证用户恒真`（action-auth 隐式 user 角色）；`ObjDataAuthModel.isUserInRole` 同样「总是假定具有 user 角色」（data-auth user 规则对任意已认证用户可命中）——两者共同保证「query=admin,user 收敛为 admin 后 user 先被 action-auth 拒绝（ERR_AUTH_NO_PERMISSION）」与「FilterState user 规则对全部已认证用户生效」。
- Doc-sync: `check-doc-links.mjs --strict` 退出码 0（0 errors）；`permission-sharing-design.md` Last Reviewed → 2026-08-16；本条目。
