# 2026-08-16

### W11-impl(A) 执行 Phase 3：文档同步 + 收口验证

- **Plan**：`ai-dev/plans/2026-08-14-2342-3-credential-scope-ownership.md`（Phase 3 completed，plan 收口）
- **owner doc**（`docs-for-ai/03-modules/nop-credential.md`）：新增"凭证归属（W11，scope=system|user + ownerId）"章节（字段语义与不可变规则 / 消费侧 per-method 矩阵表 / BizModel 两层防御 / 六旁路动作收口表 / 管理面权限 admin-roles+action-auth 变更 / usageScope 废弃说明 / DDL 迁移）+ 功能概览/核心实体/管理 API 表/关键配置/源码锚点（+`CredentialOwnership`/`NopCredentialUsageBizModel`）/SPI 表/发起语义/`@sec:` 对照同步。
- **source-anchors**：新增 `CRED-004`（归属统一锚点：CredentialOwnership `:25` + provider 归属校验 `:492` + BizModel `saveCredential:161`/`defaultPrepareQuery:385`/`batchGet:772` + usage BizModel `:33` + `assertBeginOwnership:128`）。
- **设计标注**（`02-phase2-design.md` §5.3 两处 impl 裁定标注）：(1) scope DDL **不加默认值**（NULL 视同 system 的语义等价裁定 + NULL 分支专项测试）；(2) 继承动作面收口扩展为六旁路（batchGet=过滤语义裁定；无登录态内部调用 system 放行/user 拒绝；beginOAuthFlow 按 §5.1 结论 5 写类矩阵 owner+管理员）。
- **roadmap**：W11-impl `todo` → `doing`——Part A 收口记录（plan 路径+交付摘要）+ Part B（RBAC successor）待办登记 + "done 以双 plan closure audit 为准，勿因 Part A 标 done"警示。
- **验证**：`./mvnw test -pl nop-credential -am -T 1C` BUILD SUCCESS（service 144 + kms-vault 32 + web 1）；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（ai-service 25/ai-core 217/ai-gateway 84 全绿）；`check-doc-links.mjs --strict` exit 0（0 errors；6 warnings 均为本 plan 文件反引号 VFS 相对路径提示，W9/W10 先例同口径）；`scan-hollow-implementations.mjs --module nop-credential --severity high` exit 0，全量 6 条 high = 1 pre-existing（标准 save 禁用，行号漂移 105→127）+ 5 条本 plan 新增同模式有意禁用（update/batchDelete/updateByQuery/deleteByQuery/copyForNew，plan 预裁定的同模式：显式消息+javadoc+收口测试），**0 条非预裁定 NEW 发现**。
- Doc-sync：`docs-for-ai/03-modules/nop-credential.md` + `docs-for-ai/04-reference/source-anchors.md` + 设计文档两处标注 + roadmap。

### W11-impl(A) 执行 Phase 2：BizModel 归属过滤 + 写分级 + 继承动作面收口 + action-auth + W9 回补

- **Plan**：`ai-dev/plans/2026-08-14-2342-3-credential-scope-ownership.md`（Phase 2 completed）
- **`NopCredentialBizModel`**：(1) `saveCredential` +可选 `scope`/`ownerId` 输入（空串归一为未传）——创建：user 级普通用户强制 ownerId=本人/管理员代建须显式指定（审计 createdBy≠ownerId，ORM 自动戳）、system 级（含缺省）限登录管理员、无登录态内部调用维持一期可建 system；更新：写分级前置（system=管理员、user=owner+管理员）+ 归属不可变（显式传变更值拒 `ownership-immutable`，等值传入放行并 NULL→显式规范化）。(2) 读类结构性过滤：覆盖 `defaultPrepareQuery`（非管理员 `scope=system ∨ scope IS NULL ∨ (user ∧ ownerId=本人)`，经 `invokeDefaultPrepareQuery` 统一作用于 findPage/findList/findFirst/findCount——GraphQL 级测试实证覆盖机制生效）；`get` 越权抛 `UnknownEntityException`（与软删除同口径）；`maskList`/`test` BizModel 层可见性预检 → 不可见归一 `ERR_CREDENTIAL_NOT_FOUND`（防 provider 归属错误码泄露存在性）。(3) 六旁路收口：`update`/`batchDelete`/`updateByQuery`/`deleteByQuery`/`copyForNew` 禁用抛 `UnsupportedOperationException`；`batchGet` 改走可见性过滤语义（裁定记录于各方法 javadoc）；`reencryptAll` 限登录管理员。
- **`NopCredentialUsageBizModel`**：查询面（findPage/findList/findFirst/findCount/get/batchGet）限管理员（`defaultPrepareQuery` + get/batchGet 前置 requireCredentialAdmin；无登录态同样拒绝——usage 面唯一合法消费方为管理员管理面）。
- **W9 回补**：`OAuthFlowService.beginOAuthFlow` 归属校验（`requireOauth2Credential` 重构返回实体）——system=管理员、user=owner+管理员。**裁定**：plan 措辞 "user=owner" 与其自引的 §5.3 写类矩阵（owner+管理员）冲突，按设计文档落地 owner+管理员（admin 本就可经 saveCredential 更新 user 级，不扩大权限面）；回调路径不加校验（state bearer capability 语义不变）。W9 既有测试 TestUserContext 提升为 admin 角色（system 级发起契约）。
- **action-auth delta**：`NopCredential:mutation`（含 test）/`saveCredential` 对登录用户开放（去掉 roles——分级由 BizModel 运行时判定，避免 user 级功能 action 层 403 空洞）；usage query/mutation 收紧 `roles=admin`（与生成基件同构嵌套确保按 resource id 精确合并）；delete/reencryptAll 维持 admin。
- **Web 最小面**：`NopCredential.view.xml` add 表单 +scope 下拉（新字典 `dict/credential/scope.dict.yaml`，xmeta `<schema dict>` 引用）+ownerId 条件输入（`visibleOn scope=='user'`）+ add 提交 data 模板带 scope/ownerId；edit 表单不含归属输入（不可变，缺省保持）；列表/详情展示列继承 `_gen`（codegen 已含）。`NopCredentialUsageBizModel` 页面无改动。
- **测试**：新增 `TestNopCredentialOwnershipBizModel`（20 用例，GraphQL 引擎驱动）：普通用户建 user 级（ownerId 强制本人/不可指定他人）/非管理员建 system 拒（显式+缺省）/管理员建 system+代建（ownerId=他人、createdBy≠ownerId）/user 无 owner 拒/system 带 ownerId 拒/非法 scope 拒/可见性边界（NULL+system+own 可见、他人 user 级不可见、admin 全见）/findList 同过滤/get 越权归一 unknown-entity/maskList·test 越权归一 not-found/六动作收口/batchGet 过滤语义/归属不可变（等值放行+变更拒+缺省保持）/写分级（非管理员改删 system 拒、改删他人 user 级归一或拒、owner 改删自己放行）/两层防御（绕 BizModel 直调 provider 仍 owner-only 拒）/reencryptAll·usage 面非管理员拒、admin 放行/无登录态一期回归。`TestOAuthFlowService` +6 W9 回补用例（system 非管理员拒/管理员放行/user owner 放行/他人拒/admin 放行/NULL 存量行视同 system 拒）。
- **验证**：`./mvnw clean install -pl nop-credential -am -T 1C` BUILD SUCCESS（service 144/144 = 既有 106 + 新增 38；kms-vault 32/32；web 1/1 页面加载含 view/action-auth/dict 变更）；`./mvnw checkstyle:check -pl nop-credential -Pqa` exit 0；`./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` BUILD SUCCESS（nop-auth-service 偶发 tenant-cache order flake 重跑绿，clean tree 已证明 pre-existing）；`-pl nop-ai -am -fn` nop-ai 全模块 SUCCESS（ai-service 25/25，仅 nop-auth-service flake）。
- 偏差：W9 begin user 级语义裁定为 owner+管理员（见上，plan 文本已标注）。
- Doc-sync：owner doc 归属章节按 plan 归属 Phase 3 统一补。

### W11-impl(A) 执行 Phase 1：ORM 归属字段 + provider 归属校验

- **Plan**：`ai-dev/plans/2026-08-14-2342-3-credential-scope-ownership.md`（Phase 1 completed）
- **ORM（model-first）**：`nop-credential.orm.xml` `NopCredential` +2 列：`scope`（VARCHAR 20，**无 DDL 默认值**——存量 NULL 视同 system，语义等价裁定记录于 plan）+ `ownerId`（VARCHAR 50，user 级必填/system 级恒空）；`usageScope` 注释标注 deprecated（不赋值，物理删除留 DDL 治理）；`usages` to-many `tagSet` 去 `pub` → 再生 xmeta `published="false"`（堵 GraphQL selection 绕过 usage 管理面）。codegen 再生成 `_gen/_NopCredential.java`、`_app.orm.xml`、`_NopCredential.xmeta`、i18n、`_create_` 三方言；手写增量 `_add_scope_owner_nop-credential.sql` 三方言（循 `_add_oauth_state_` 先例，scope 无 DDL 默认值）。
- **provider 归属矩阵**（`CredentialProviderImpl`，解密之前、fail-closed、先序 delFlag）：`getCredential`/`getCredentialData` = user 级 owner 唯一（无上下文/非 owner/admin 一律 `nop.err.credential.owner-only`）；`mask`/`testCredential` = owner+admin（`owner-or-admin`）；system（含 NULL）放行（一期零回归）。SPI 签名零变更（`IUserContext.get()` 上下文捕获）。
- **判定工具**：新 `service/CredentialOwnership`（scope 语义/`nop.credential.admin-roles` CSV 缺省 `admin,nop-admin`→`isUserInAnyRole`/owner/可见性/写分级 denial reason）；`CredentialConfigs` +`CFG_CREDENTIAL_ADMIN_ROLES`；`CredentialErrors` +7 错误码（owner-only/owner-or-admin/admin-required/invalid-scope/owner-required/owner-not-allowed/ownership-immutable）。
- **测试**：新增 `TestCredentialProviderOwnership`（12 用例，角色感知 TestUserContext）：owner 明文成功/非 owner 拒/admin 明文不例外/无上下文拒（后台任务语义固化）/NULL scope 存量行无上下文零变化/system 显式值非 admin 零变化/mask·test owner+admin 放行与他人拒绝/delFlag 先于归属（DELETED 不泄露归属）/admin-roles 配置覆盖生效/ContextProvider 线程上下文接线。
- **验证**：`./mvnw clean install -pl nop-credential -am -T 1C` BUILD SUCCESS（service 118/118 含新增 12 + kms-vault 32/32 + web 1/1）；W7-successor 回归 nop-ai 全模块 SUCCESS（`-pl nop-ai -am -T 1C` 中 nop-auth-service 出现 order-dependent tenant-cache flake，**clean tree（stash 后）同命令同样失败**，模块单独运行绿，与本 plan 无关——nop-auth 不依赖 nop-credential）；`./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` BUILD SUCCESS（重跑后）。
- 偏差：无。
- Doc-sync：owner doc 归属章节按 plan 归属 Phase 3 统一补（此处行为变更已由 ORM 注释/错误码/plan 记录）。

### W10-impl 执行 Phase 4：文档同步 + 全模块验证

- **Plan**：`ai-dev/plans/2026-08-14-2342-2-credential-external-kms-key-provider.md`（Phase 4 completed）
- **owner doc**（`docs-for-ai/03-modules/nop-credential.md`）：新增"外部 KMS/HSM 集成（W10，主密钥材料交付模式）"章节（配置项表 7 项 / 装配模型含 default-bean 守卫 / fail-closed 语义 / local→Vault 迁移五步 / keyId→材料不变式与配置禁区）+ 功能概览/子模块表（+`nop-credential-kms-vault`）/加密方案/关键配置/源码锚点同步。
- **设计标注**（`02-phase2-design.md`）三处：§4.1 结论 5 IHttpClient 零 SDK 细化偏离；§4.3 "无 bean 满足 → 启动失败"前提修正为 default-bean 守卫达成（NopIoC default 无条件兜底语义）；§4.3 reencryptAll 已知限制裁定标注（完备性走分页修复路径）。
- **source-anchors**：新增 `CRED-003`（KMS 集成锚点：VaultCredentialKeyProvider `init:144`/`fetchMaterial:248` + app-kms-vault.beans.xml 门控装配 + DefaultCredentialKeyProvider `init:77` 守卫 + reencryptAll `:389` 翻页/`buildReencryptQuery:437`）。
- **验证**：`./mvnw test -pl nop-credential -am -T 1C` 全绿（service 106 + kms-vault 32 + web 1 + 上游）；`check-doc-links.mjs --strict` exit 0（修 2 处自引入断链后；残留 warning 均为 plan 文件反引号 VFS 相对路径提示，W9 先例同口径）；`scan-hollow-implementations.mjs --module nop-credential --severity high` 全量仅 1 条 = 已知 pre-existing（`NopCredentialBizModel.java:111` 标准 save 禁用，一期有意模式，行号 105→111 因 Phase 3 编辑漂移），**本 plan 触碰文件 0 条 NEW**。
- Doc-sync：`docs-for-ai/03-modules/nop-credential.md` + `docs-for-ai/04-reference/source-anchors.md` + 设计文档三处标注。

### W10-impl 执行 Phase 3：reencryptAll 分页完备性修复

- **Plan**：`ai-dev/plans/2026-08-14-2342-2-credential-external-kms-key-provider.md`（Phase 3 completed）
- **修复**：`NopCredentialBizModel.reencryptAll` 单页（limit=1000 无翻页）→ **keyset 游标翻页循环**：`buildReencryptQuery()` 强制 `orderBy credentialId ASC` + `delFlag=0` 过滤 + 可注入页大小（`nop.credential.reencrypt-page-size`，缺省 1000 保持一期语义）；循环 `query.setCursor(page末条.orm_idString())` + `findPageByQuery`（findNext 路径）直至取尽；逐条提交/幂等跳过（active keyId 相同跳过）/失败 fail-closed 语义不变。排序确定性提取为包私有方法供测试断言。
- **验证**：新增 `TestNopCredentialReencryptPagination`（@NopTestProperty 注入页大小 3）：7 条凭证单次执行返回 7 且全部 `cv1:keyB:`（跨 3 页全覆盖）+ 第二次返回 0 + 排序断言（唯一排序键 credentialId 升序/过滤/页大小）。`./mvnw clean install -pl nop-credential -am -T 1C` 全绿（service 106/106 含既有 `reencryptAllReencryptsWithActiveKey` 回归 + kms-vault 32/32 + web 1/1）；checkstyle -Pqa exit 0。
- 偏差：无（游标 vs offset 二选一按实施定案记录在 plan：选 keyset 游标）。
- Doc-sync：No owner-doc update required（`reencryptAll` 断点续跑描述随 Phase 4 KMS 章节核对 owner doc）。

### W10-impl 执行 Phase 2：fail-closed 校验组 + 迁移残余列表测试

- **Plan**：`ai-dev/plans/2026-08-14-2342-2-credential-external-kms-key-provider.md`（Phase 2 completed）
- **实现修正**：`VaultCredentialKeyProvider` 迁移残余解析中"禁含 active key"检查移到 keyId 冲突检查之前——active 必属 Vault 密钥集，若先查冲突会把"残余含 active"吞成通用 keyId 冲突错误（测试驱动发现，两条错误分支各自可达）。
- **验证**：新增 `TestVaultCredentialKeyProvider` 24 例（stub `IHttpClient`，plan 明示的 mock 路径）：fail-closed 全分支（不可达/401/403/404/500/空 body/缺 data.data/非字符串材料/空材料/坏 JSON/master-keys 残留/残余含 active/残余 keyId 冲突/残余条目非法/active 指向残余 only/配置缺失/映射非法×5）+ happy path（材料 round-trip 与 `AESTextCipher` 同材料逐字互解）+ 残余语义（并入解密 keyMap、不作 active 候选）+ WARN 审计正反例（logback ListAppender）+ 运行期未知 keyId fail-closed。`./mvnw test -pl nop-credential -T 1C` 全绿（service 104 + kms-vault 32 + web 1）；checkstyle -Pqa exit 0。
- 偏差：单测的 KV v2 响应体手工拼 JSON 字符串（plain JUnit 无 CoreInitialization，JSON provider 未注册；@BeforeAll 手工 `JSON.registerProvider(JsonTool.instance())` 供 provider 的 `JSON.parse` 使用——运行期该注册由 ReflectionHelperMethodInitializer 完成，非生产代码问题）。
- Doc-sync：No owner-doc update required（KMS 章节归 Phase 4）。

### W10-impl 执行 Phase 1：KMS 参照实现模块 + 同名 bean 门控覆盖 + default-bean 缺失守卫

- **Plan**：`ai-dev/plans/2026-08-14-2342-2-credential-external-kms-key-provider.md`（Phase 1 completed）
- **新模块** `nop-credential/nop-credential-kms-vault`（compile 依赖仅 `nop-credential-api` + `nop-http-api`，无 Vault SDK、不依赖 service）：`VaultCredentialKeyProvider`（KV v2 经 `IHttpClient` GET `/v1/{mount}/data/{path}` + `X-Vault-Token`，启动期物化 `AESTextCipher` 集合，运行期纯内存查找；配置命名空间 `nop.credential.vault.{address,token,keys,active-key-id,migration-keys}`，secret 材料字段名 `passphrase`）+ 模块自有错误码 `VaultCredentialErrors`（`nop.err.credential.vault.*`——模块不依赖 service 故独立定义，前缀延续）。装配：`_vfs/nop/credential/beans/app-kms-vault.beans.xml` 共享模块命名空间（app- 前缀自动装载，Phase 1 Decision 主路径定案成立；VFS 跨 jar 合并 `/nop/credential/beans/`，模块标记来自 nop-credential-dao），同名 bean `nopCredentialKeyProvider` + `if-property key-provider=vault` 门控，非 `ioc:default` 硬覆盖。
- **缺失守卫**（修正设计 §4.3 前提）：`DefaultCredentialKeyProvider` 新增 `@cfg:nop.credential.key-provider|local` + init 守卫——非 local 且 master-keys 非空 → `ERR_CREDENTIAL_MASTER_KEYS_RESIDUAL`；非 local → `ERR_CREDENTIAL_KEY_PROVIDER_MODULE_MISSING`（NopIoC default bean 是无条件兜底，模块缺失时照常回退 local，守卫结构性堵死假安全）；local/未配置零触发。`CredentialErrors` +2 错误码。
- **验证**：`./mvnw clean install -pl nop-credential -am -T 1C` BUILD SUCCESS（service 104/104 + kms-vault 8/8 + web 1/1）；`dependency:tree` 确认 service 无新第三方依赖；`checkstyle:check -Pqa`（service/kms-vault）exit 0。新测试：service `TestDefaultCredentialKeyProvider` 14→19（守卫 5 例）；kms-vault `TestVaultKeyProviderWiring`（4：门控覆盖/材料逐字来自 stub/容器 cipher round-trip/运行期未知 keyId fail-closed）、`TestLocalKeyProviderWithKmsModulePresent`（2：模块在场未选用 → default 生效）、`TestKeyProviderModuleMissingGuard` + `TestKeyProviderGuardMasterKeysResidual`（skip-pattern 跳过 KMS beans 自动装载模拟模块缺失，容器初始化抛守卫错误码）。
- 偏差：(1) 模块缺失模拟从"迷你容器仅装载 credential-defaults"改为 `nop.ioc.app-beans-file.skip-pattern` 跳过 KMS beans 文件——迷你容器在定义构建期即因 CredentialProviderImpl 的类型注入缺平台 bean 失败（autowire 解析在 build 阶段），skip-pattern 语义上等价"部署中无该文件"且保真完整容器。(2) wiring 测试从 NopAutoTest 固定端口改为 plain JUnit + `setTestConfig` 程序化配置（先绑临时端口再写配置再 `CoreInitialization.initialize()`）——`Class.getAnnotation` 不触发静态初始化，静态块 stub 赶不上 `NopJunitExtension.beforeAll` 的容器构建（nopCrudBizInitializer 强制构造 BizModel 依赖链，Vault bean 在 initialize 期即读取材料）；程序化配置顺带免除固定端口冲突。
- Doc-sync：No owner-doc update required（KMS 章节与设计标注归 Phase 4）。

### W9-impl 收口轮：closure audit 发现并修复 CredentialOAuthApiBizModel 注册缺失 Blocker

- **Plan**：`ai-dev/plans/2026-08-14-2342-1-credential-oauth-flow-engine.md`（Plan Status → completed）
- **独立 closure audit**（explore 子 agent，task id `ses_ff680cf39ffeEj6PfgmwDKNnYy`，fresh session）：9 项 Closure Gate 逐一 live-code 核对，8 项 PASS；**1 Blocker FAIL——`CredentialOAuthApiBizModel` 未在任何 beans.xml 注册**：NopIoC 无注解扫描，`CredentialOAuthApi__beginOAuthFlow`/`__oauthCallback` 端点在部署运行时不可达，此前测试以手工 `new` + setter 构造掩盖（Phase 2 该项被误勾）。典型"组件级验证通过 ≠ 系统级可用"案例（plan guide Lesson #8）。
- **修复**：`credential-defaults.beans.xml` 显式 `<bean id="io.nop.credential.service.oauth.CredentialOAuthApiBizModel" ioc:type="@bean:id"/>`（LoginApiBizModel 先例形态）；**不入 `_service.beans.xml`**——该文件为 codegen 产物，只按 ORM 实体生成 BizModel 条目，W11 再生成会覆盖手改（AGENTS.md 生成物硬约束）。新增容器接线测试 `containerRegisteredApiBizModelWiredThroughIoc`（@NopTestConfig 容器注入解析 + 依赖链 fail-closed 验证），`TestOAuthFlowService` 18→19。
- **回归**：`./mvnw clean install -pl nop-credential -am -T 1C` 绿（99/99 + web 1/1）；checkstyle qa profile BUILD SUCCESS（清理 4 个 UnusedImports：NopCredentialBizModel×3 + CredentialProviderImpl×1）；`check-plan-checklist.mjs --strict` 退出码 0；roadmap W9-impl → done。
- 教训：NopIoC 模块的新 BizModel 必须同步 beans 注册 + 容器级接线测试；手工构造 BizModel 的测试不能作为端点可达性证据。

### W9-impl 执行 Phase 4 + 收口：文档同步 + 全模块验证 + closure audit

- **Plan**：`ai-dev/plans/2026-08-14-2342-1-credential-oauth-flow-engine.md`（Phase 4 completed + 全 plan 收口）
- **owner doc**（`docs-for-ai/03-modules/nop-credential.md`）：OAuth 引擎章节与 live 行为逐项核对一致——`authType` 取值域（xdef 内联枚举 + registry 双保险）、`<oauth2>` 元数据必填/可选位、五个引擎保留字段名、发起/回调/惰性刷新语义（含 state 原子一次性消费、WebContentBean HTML 跳转页、行级锁互斥、写路径串行化、disabled 全路径拒绝、唯一解密点引擎通道方法名）；配置项表与 `CredentialConfigs` 逐字对齐（`nop.credential.oauth.callback-base-url` 无缺省必配 / `result-page-url` 可选 / `state-ttl-seconds`=600 / `refresh-window-seconds`=300 类型级可覆盖）。
- **source-anchors**：新增 `CRED-002`（OAuth 出站客户端锚点：OAuthFlowService + CredentialOAuthApiBizModel `beginOAuthFlow:47`/`oauthCallback:59` + StateStore + TokenClient + provider 引擎通道 `engineGetDecryptedFields:232`/`engineUpdateTokenFields:245`/`engineUpdateInLock:283`）。
- **验证**：`./mvnw test -pl nop-credential -am -T 1C` 全绿——nop-credential-service 98/98（TestCredentialCipher 16 / TestDefaultCredentialKeyProvider 14 / TestNopCredentialBizModel 15 / TestCredentialOAuthRefresh 12 / TestOAuthFlowService 18 / TestDefaultCredentialTypeRegistry 10 / TestCredentialProviderImpl 13）；`check-doc-links.mjs --strict` 退出码 0（0 errors，16 warnings 均为 plan 文件内反引号相对路径提示，非断链）；`scan-hollow-implementations.mjs --module nop-credential --severity high` 全量输出仅 1 条 = 已知 pre-existing（`NopCredentialBizModel` 标准 save 禁用，一期有意模式，行号 105→113 因 Phase 3 编辑漂移），**本 plan 触碰文件 0 条 NEW high/critical**。
- **收口**：独立子 agent closure audit（含 Anti-Hollow：回调 → provider 引擎通道 → 密文回写调用链追踪）通过；`check-plan-checklist.mjs --strict` 退出码 0；Plan Status → completed；roadmap W9-impl ❌→✅（closure audit 通过后标记，见 roadmap）。

### W9-impl 执行 Phase 3：惰性刷新 + 互斥 + 分组写语义 + disabled 全路径拒绝

- **Plan**：`ai-dev/plans/2026-08-14-2342-1-credential-oauth-flow-engine.md`（Phase 3 completed）
- **互斥载体（Phase 3 Decision 落地）**：DB 行级锁 `IEntityDao.lockEntity`（SELECT FOR UPDATE，javadoc 明示）+ 事务模板 REQUIRED，持锁完成"读 data → 刷新外呼 → 回写"全程；不选乐观 version（刷新外呼失败后的重读重试链路复杂度高于行锁）。写路径串行化范围：惰性刷新与 saveCredential 分组写共用 `engineUpdateInLock` 同一入口（新增 BiConsumer 扩展重载：元数据与 data 同一锁下 UPDATE 提交，消除两步写间的乐观锁版本竞争），互斥覆盖"刷新 vs 刷新"与"刷新 vs 人工保存"；非刷新取用不持锁可并发。
- **惰性刷新**：`getCredential` oauth2 分支——非临期直接返回（不持锁）；临期（now 距 expiresAt < 刷新窗口，类型级 refreshWindowSeconds 覆盖全局缺省 `nop.credential.oauth.refresh-window-seconds`=300）→ 锁下双重检查 → `OAuthTokenClient.refresh`（grant_type=refresh_token + clientId/clientSecret）→ 新 token 集回写（只写保留字段）→ 返回新明文。刷新失败 fail-closed（`ERR_OAUTH_REFRESH_FAILED` 包装 invalid_grant 等）；refreshToken 缺失且已过期 fail-closed（`ERR_OAUTH_REAUTH_REQUIRED`）。
- **disabled 全路径拒绝**：`loadActiveCredential` 接入 `assertOauth2NotDisabled`（oauth2 类型 status=disabled → `ERR_CREDENTIAL_DISABLED`，覆盖 getCredential/getCredentialData/mask/testCredential + engine 通道 = 发起/回调/刷新/取用全路径）；类型未注册容忍（一期语义零新增失败路径）；非 OAuth 类型维持仅 delFlag。
- **saveCredential 分组写 + typeList 裁剪**：oauth2 类型输入出现保留字段名拒绝（`ERR_CREDENTIAL_RESERVED_FIELD_INPUT`）；更新路径锁下保留字段原样保留 + 人工字段整包替换（未传即删）+ 元数据同锁更新；新建路径一期不变；`typeList` 对 oauth2 类型防御性裁剪保留字段。
- **验证**：`TestCredentialOAuthRefresh` 12/12：临期触发刷新恰一次（计数==1）+ 回写验证 + 窗口内再取用不刷新；非临期零调用；刷新失败 fail-closed 且 data 不变；过期无 refreshToken 抛 reauth；无 refreshToken 未过期返回现值；**4 线程并发取用临期凭证刷新端点计数==1 且全部线程同 token**；**并发"取用刷新 vs saveCredential"无丢失更新**（新 token + 新人工字段并存）；disabled oauth2 取用/脱敏拒绝；disabled 非 OAuth 仍可取（一期语义保持）；saveCredential 保留字段输入拒绝（GraphQL 驱动）；人工替换保留 token 集；typeList 裁剪。全模块 98/98 绿（一期回归 TestNopCredentialBizModel 15/15 + TestCredentialProviderImpl 13/13 零回归）。
- 偏差：分组写首版"元数据先写 + data 后写"两步方案在并发下乐观锁冲突（update-entity-not-found）/顺序 data=null 防御清空 → 改为 engineUpdateInLock BiConsumer 重载（元数据+data 同一锁下 UPDATE）。
- Doc-sync: No owner-doc update required（配置项缺省值与语义写入 owner doc 归 Phase 4）。

### W9-impl 执行 Phase 2：授权码闭环（发起 + 公开回调 + token 回写）

- **Plan**：`ai-dev/plans/2026-08-14-2342-1-credential-oauth-flow-engine.md`（Phase 2 completed）
- **新包 `service/oauth`**：`OAuthTokenClient`（令牌端点协议客户端，form 表单形态参照 `OAuthLoginServiceImpl.newLoginRequest`，不引入 nop-auth-sso 依赖；token endpoint error 字段/非 2xx/传输异常全部显式抛 `ERR_CREDENTIAL_OAUTH_TOKEN_EXCHANGE_FAILED`）、`NopCredentialOauthStateStore`（state 绑定 DB 存储：安全随机 128bit hex state、一次性消费 = 条件 UPDATE + affected-row 判定（W8 先例）、发起时惰性清理过期行）、`OAuthTokenResponse` DTO、`OAuthFlowService`（发起/回调编排）、`CredentialOAuthApiBizModel`（发起 mutation 登录态 + 回调 `@BizQuery @Auth(publicAccess=true)` 返回 WebContentBean HTML 跳转页，Phase 2 Decision）。
- **引擎通道（唯一解密点不变式）**：`CredentialProviderImpl` 新增 `engineGetDecryptedFields` / `engineUpdateTokenFields` / `engineUpdateInLock`（DB 行级锁 SELECT FOR UPDATE + 事务模板 REQUIRED + 会话缓存驱逐后重读；engineUpdateInLock 为 Phase 3 惰性刷新与 saveCredential 分组写的共用串行化入口）；OAuth 引擎类不持有 `CredentialCipher`。
- **bean 装配/依赖**：`credential-defaults.beans.xml` 注册 tokenClient/stateStore/flowService（ioc:default）；`_service.beans.xml` 注册 `CredentialOAuthApiBizModel`（LoginApiBizModel 先例形态）；`nop-credential-service` 新增 `nop-http-api` compile 依赖 + `nop-http-client-jdk` test 依赖（nop-ai-gateway 先例）。
- **配置项**（`nop.credential.oauth.*`）：`state-ttl-seconds`（缺省 600）、`refresh-window-seconds`（缺省 300，Phase 3 消费）、`callback-base-url`（redirect_uri 派生，未配置 fail-closed）、`result-page-url`（回调跳转目标，未配置输出内置静态完成页）。
- **验证**：`TestOAuthFlowService` 18/18（stub 授权+令牌服务器 = JDK HttpServer）：发起 URL 组装/state 持久化/六类发起拒绝分支；回写保留字段+人工字段保留/令牌端点表单协议形态断言（IHttpClient 接线）/响应页无 token 明文/重放/未知/过期 state 拒绝/参数缺失/token endpoint error 显式抛错且 data 不变/禁用凭证回调拒绝；**并发双回调同 state 恰一成功（tokenCallCount==1）**；**端到端 begin → 真实 HTTP GET 授权 URL → 302 Location 解析 → 回调 action → getCredential 取到闭环写入明文**；state 过期行惰性清理。全模块 86/86 绿。
- 偏差：`requireOauth2Credential` 首版误将登录态校验放入回调共用链（回调线程无 user context）→ 拆出：登录态仅发起路径（设计 §3.3 第 4 条回调无登录态语义的正确落地）。
- Doc-sync：设计文档 §3.3 第 3 条追加回调载体裁定标注（WebContentBean HTML 跳转页替代 30x 的显式偏离记录）。

### W9-impl 执行 Phase 1：类型声明扩展 + state 存储模型（xdef / api 模型 / ORM）

- **Plan**：`ai-dev/plans/2026-08-14-2342-1-credential-oauth-flow-engine.md`（Phase 1 completed）
- **xdef**（`nop-kernel/nop-xdefs/.../credential/credential-type.xdef`，Protected Area，本 plan 为 plan-first 载体）：`authType` 收敛为内联枚举 `!enum:none|apiKey|basic|oauth2`（`call-tools-response.xdef` 先例语法）；新增可选子元素 `<oauth2>`（authorizationEndpoint/tokenEndpoint 必填、scopes/refreshWindowSeconds 可选）。先行 `./mvnw install -pl nop-kernel/nop-xdefs -am -DskipTests` 重打包。
- **api 模型**：手写 `CredentialType` 增加 authType 常量、`OAuth2Metadata` 内嵌对象、`OAUTH_RESERVED_FIELD_NAMES`（accessToken/refreshToken/expiresAt/tokenType/scope 五名保留）。
- **registry 加载校验**（全 fail-closed 无静默跳过）：保留字段名占用拒绝 / authType 取值域外拒绝（xdef 枚举 + registry 双保险）/ oauth2 缺端点拒绝 / 非 oauth2 带 `<oauth2>` 元数据拒绝；`setTypesDir` 测试注入位。新增 4 个非法类型文件测试夹具（`_vfs/test/credential-types-{reserved,bad-auth,no-endpoints,misplaced}/`）。
- **示例类型**：`generic-oauth2.credential-type.xml`（clientId/clientSecret 人工字段 + 端点元数据 + refreshWindowSeconds=300）。
- **state 存储模型（Phase 1 Decision 落地）**：新 ORM 实体 `NopCredentialOauthState`（表 `nop_credential_oauth_state`：STATE PK(64)/CREDENTIAL_ID/USER_ID/EXPIRE_AT BIGINT/CONSUMED boolFlag/CREATE_TIME/CREATED_BY，credentialId+expireAt 索引，`no-tenant`，W8 `NopAuthMfaChallenge` 同构）。model-first：源编辑 `model/nop-credential.orm.xml` → codegen 重生成 `_gen/_NopCredentialOauthState.java`、`_app.orm.xml`、三方言 `_create_`/`_drop_`、xmeta/xbiz/web 页面；手写增量 alter 脚本 `deploy/sql/{mysql,oracle,postgresql}/_add_oauth_state_nop-credential.sql`（循 `_add_tenant_` 先例，补 `_create_` 模板不产出的 CREATE INDEX）。BizModel/BizProxy/I*Biz 注册循 W8 全套（`NopCredentialOauthStateBizModel` + `_service.beans.xml`）。
- **验证**：`./mvnw clean install -pl nop-credential -am -T 1C` 全绿（codegen 再生成 + 既有测试 + 新 registry 测试）；`TestDefaultCredentialTypeRegistry` 10/10（新增 5：metadataRoundTrip + 四类非法文件拒绝）。
- 偏差：无（首轮 codegen 生成页面因全列 `ui:show=X` 空布局失败 → 对齐 W8 实体列可见性后重生成通过）。
- Doc-sync：设计文档 §八 W9-impl 段追加 state 载体裁定标注（新 ORM 实体，理由与 plan-first 载体路径）。
