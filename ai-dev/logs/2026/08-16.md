# 2026-08-16

### EXEC_PLANS Cycle 4 — plan 2026-08-16-0920-1 nop-metadata ORM unique-key 完整性收口（P2-01/P2-28/P2-29）

plan `ai-dev/plans/2026-08-16-0920-1-nop-metadata-orm-unique-key-integrity.md`（mission-driver EXEC_PLANS；**该指令同时构成本计划 ORM 源模型变更的人工放行**，沿 `2026-08-14-1448-3` 先例——见 `ai-dev/logs/2026/08-15.md:193`；3 Phase + Closure）。三项裁定（Phase 1 裁决表落档 plan 内）：
- **P2-01 → 应用层查重守卫（选项 ii）+ 保留现 UK**：裸扩列 NULL-distinct 双输排除（目标行仍豁免 + 现有 tagId 承载行保护回退）；哨兵方案排除（''/NULL 双键反面裁定 `NopMetaTableBizModel:178-180` + update 入口绕过 + Oracle ''≡NULL）；守卫 = `NopMetaTagLabelBizModel` GLOSSARY 分支（source=Glossary && tagId NULL && glossaryTermId 非 NULL）按 `(entityType,entityId,source,glossaryTermId,tagId IS NULL)` 查重，save+update 双入口，fail-loud 新 ErrorCode；零 DDL 变更 → 不交付升级 SQL（偏离 R4.2/R4.3 先例理由：先例皆因约束形状改变）。
- **P2-28 → 应用层根域重名守卫 + 模型注释（不改 UK）**：sentinel UK 改造否决（IS NULL 消费面迁移 + 存量 UPDATE + Oracle ''≡NULL + 根域有界小集合）；守卫 = `NopMetaBusinessDomainBizModel` save+update 根域（parentDomainId NULL）按 name 查重 fail-loud 新 ErrorCode；零 DDL 变更 → 不交付升级 SQL。
- **P2-29 → 删冗余 per-scope UK（保留全局 FQN UK，行为零变化）**：蕴含关系（非 NULL FQN 全局唯一 ⇒ per-scope 唯一）；消费者 `AutoClassificationProcessor.findTagByFQN:251-255` 结果集恒同；scope 维度列举由既有 IX 承载；放宽选项否决（FQN=全局身份语义 + seed/自动构建均按全局约定）；删 `UK_NOP_META_GLOSSARY_TERM_G_FQN` / `UK_NOP_META_TAG_CLS_FQN`，交付三方言 `upgrade-nop-meta-fqn-uk.sql`（DROP ×2，无数据前置条件）。

执行事实（Phase 2/3）：

- **守卫落地（test-first 先红后绿）**：`NopMetaTagLabelBizModel` 增 `rejectDuplicateGlossaryTermLabel`（save/update 双入口，super 之后对合并终态查重——部分字段 update 形态经基类合并后才可见完整列值；自排除 tagLabelId；`FilterBeans.isNull(tagId)` 形态沿 `NopMetaTableBizModel:182` 先例）；`NopMetaBusinessDomainBizModel` 由裸 CrudBizModel 增 save+update override + `rejectDuplicateRootDomainName`（根域 `(parentDomainId IS NULL, name)` 查重自排除）。新 ErrorCode `nop.err.metadata.tag-label-duplicate-glossary-term` / `nop.err.metadata.business-domain-duplicate-root-name`（MiscErrors，含 P2-01/P2-28 裁定注释）+ 新 ARG 常量 `ARG_GLOSSARY_TERM_ID`/`ARG_BUSINESS_DOMAIN_ID`。**红态实证**（守卫落地前）：`TestNopMetaTagLabelGlossaryGuard` 5 例中 2 红（重复 GLOSSARY null-tag save 被接受 + update 撞名被接受）、`TestNopMetaBusinessDomainRootNameGuard` 4 例中 2 红（根域重名 save 被接受 + rename 撞名被接受）——红点恰为缺口行为本体，非回归路径（tagId 承载行 DB UK 拒绝、不同 term/不同父域、自更新）在守卫前后均绿。**绿态**：5/5 + 4/4。
- **模型变更 + 再生**：`model/nop-metadata.orm.xml` 删 2 个 per-scope UK 声明（含裁定注释）+ BusinessDomain UK 处 NULL-distinct 注释（comment-only 亦属 ORM 变更，随 EXEC_PLANS 人工放行覆盖）。`./mvnw install -pl nop-metadata -am -DskipTests -T 1C` BUILD SUCCESS；git diff 再生面 = `deploy/sql/{mysql,oracle,postgresql}/_create_`+`_add_tenant_` ×6（纯 per-scope UK 行删除，三方言一致）+ `_app.orm.xml`（−4 行 UK 元素）+ `_NopMetaGlossaryTerm.xmeta`/`_NopMetaTag.xmeta`（各 −1 `<key>` 行）——零手编痕迹，`_drop_` 无 UK 名不受影响。live unique 计数：mysql `_create` 42→40、`_add_tenant` 41→39。
- **门禁与残留扫描**：`check-orm-unique-key-constraint.mjs --module nop-metadata` = 35 UK / 0 hits / exit 0；`run-nop-metadata-invariants.sh` 6-guard 全链 exit 0。被删 UK 名 rg 全仓：**Java/test 面命中 = 2 个测试文件（`TestNopMetaDdlUniqueKeyEmission` assertFalse + `TestNopMetaFqnUniqueKeyBehavior` javadoc），均为钉死「已删除不复发」的负向断言，正向引用零命中**（plan exit criterion 字面「零命中」按此精度落档）；其余命中 = 本 plan/本 log（裁定记录）+ 登记面（已同步）+ 历史审计文档（plan guide 规则 20 不回写）。
- **升级 SQL**：三方言 `deploy/sql/{mysql,oracle,postgresql}/upgrade-nop-meta-fqn-uk.sql`（沿 R4.2/R4.3 先例：适用范围/再生覆盖声明/结果 UK 形状；DROP ×2 无数据前置——drop 约束不因存量数据失败）。P2-01/P2-28 零 DDL 变更不交付脚本（偏离先例理由入裁决表）。
- **登记面/文档同步**：roadmap P2-01/P2-28/P2-29 三条目 ✅ Fixed（plan 号 + 一句结论）；`audit-target-set.md` §2.2（清单 37→35 + 删除注记 + 2 行移除）+ §2.3（计数 42→40/41→39）+ §2 标题/复现脚本预期值同步（I0 轮 37/37 口径保留为历史注记）；`invariant-catalog.md` INV-UK 覆盖率 35/39 + 35/35；`formal-red-list.md` INV-UK 段同步；owner doc `docs-for-ai/03-modules/nop-metadata.md` 新增「语义层 FQN 唯一性与 NULL-distinct 守卫」节（P2-29 全局 FQN 唯一契约 + 升级脚本 + P2-01/P2-28 守卫行为与错误码）。
- 验证：`./mvnw test -pl nop-metadata -am -T 1C` **BUILD SUCCESS**（service **1277/0/0** = 0549-3 基线 1264 + 净 13：TagLabel 守卫 5 + BusinessDomain 守卫 4 + FQN 行为 3 + DdlUniqueKeyEmission +1；首轮 reactor 内 `nop-stream-rocksdb` `incrementalCheckpointIsFasterThanFullScanForLargeState` 计时基准断言 flaky（ratio=1.71，与本模块无关，0549-2 同款先例），复跑全绿）。checkstyle 9174 条 = 既有全仓未接入门禁基线（nop-metadata 文件 0 条、本计划改动文件交集 0）。doc-links 17 errors = 既有跨 mission 基线零新增（唯一 nop-metadata.md 命中为既有 BOUNDARY 行号平移 263→271，内容未变）。
- 偏差记录：① P2-01 守卫按裁决选项 ii 落 save 分支之外**同时覆盖 update 入口**（对称写面，同守卫同键，plan 选项文本只列 save——扩展在裁决表显式记录）；② 「被删 UK 名 Java/测试零命中」按负向断言精度落档（见上）；③ `TestSaveOverrideNullDataGuard` 增 `NopMetaBusinessDomainBizModel` null/empty 两断言（新 override 保持 P2-19 单一形态，守卫在 super 之后不触达 data）。
- **收口（closure audit approved）**：独立 closure audit（fresh session `ses_ff75fe74affelZfiZgtRsLmd17`，review-only 零文件修改）**11/11 检查全 PASS**（守卫接线 file:line、模型 UK 删除/保留全集 + 三方言 DDL 零 G_FQN/CLS_FQN 残留、升级 SQL、focused 测试 20/20 全绿、双门禁 exit 0、roadmap/登记面/owner doc/daily log 逐处核对、Anti-Hollow 运行时链（GraphQL 真实入口 + 回滚行数断言）、git 改动面仅计划内文件、无静默降级），verdict `CLOSURE_AUDIT: approved`（5 条 Minor 均非阻塞；formal-red-list :22 口径注记顺手补齐）。Closure Gates 全绿：install 再生 SUCCESS、`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（service 1277/0/0）、UK 门禁 35/0 exit 0、invariants 6-guard exit 0、checkstyle 既有基线 0 交集、`check-plan-checklist --strict` exit 0、`scan-hollow --severity high` exit 0（0 findings）。Plan Status → `completed`（Completed: 2026-08-16）；roadmap P2-01/P2-28/P2-29 ✅ Fixed。

### EXEC_PLANS Cycle 3 / 执行顺序 5c — plan 2026-08-16-0549-3 IoC 装配债务与注释真值化族（P2-02/P2-30/P2-31/P2-03）

plan `ai-dev/plans/2026-08-16-0549-3-nop-metadata-ioc-cycle-and-comment-truth.md`（mission-driver EXEC_PLANS；3 Phase + Closure）。

- **Fix（P2-02 双向注入环消除 + P2-30 javadoc 真值化，Phase 1）**：`NopMetaQualityCheckpointBizModel` 移除 `@Inject @Nullable MetaQualityCheckpointScheduler scheduler` 字段，改为**懒解析 seam** `lookupScheduler()`——按 `MetaQualityCheckpointScheduler.BEAN_NAME` 经 `BeanContainer.tryGetBean` 查找（nop-job `BeanMethodJobInvoker`/`DefaultJobInvokerResolver` 等 tryGetBean 先例充分）；`notifySchedulerRegister`/`notifySchedulerUnregister` 经 seam 获取实例，未命中返回 null 跳过（与 `@Nullable @Inject` 的 null-on-missing 语义无损对齐，旁路容错语义不变）。`MetaQualityCheckpointScheduler → BizModel` 的 setter `@Inject` 保留（核心路径维持 IoC 注入）——环收敛为 scheduler→bizmodel 单向。**偏差记录（plan 文本 vs 实现）**：plan 写"protected 可覆写"，实测 `io.nop.metadata.service` 测试包对 `...service.entity` 的 protected 成员无编译期访问权（JLS 跨包规则），spy/doReturn stub 不可编译——seam 改 **public**（同样可覆写；非 `@BizQuery/@BizMutation` 不进 GraphQL 面；`TestNopMetaBizInterfaceCompleteness` 守卫只扫接口包，不受影响），机制约束（spy 覆写 + 既有 harness + 不动全局容器）全部保持。
- **P2-30 两处矛盾 javadoc 收敛单一真值**：字段 javadoc（原 :84-88 声称"取代 tryGetBean 服务定位器反模式，通过 IoC 注入"）随字段移除改写为 seam javadoc（懒解析 + 断环理由）；save override javadoc（原 :262-264 声称"经 tryGetBean 懒查找非 @Inject"——与旧字段注入矛盾）改为与终态代码一致的 `lookupScheduler()` 表述。现文件内 tryGetBean 表述全部与 live 代码一致（rg 核对）。
- **既有反射断言重写（强度不降反升）**：`testDeleteFailureKeepsSchedule` 的 `getDeclaredField("scheduler") + assertNotNull`（仅断"字段存在"）重写为 `assertSame(checkpointScheduler, checkpointBizModel.lookupScheduler())`（断"接线生效"——seam 命中容器注册的同一实例）。
- **新增接线测试双态（Minimum Rules #23）**：(a) `testSaveDeleteWiringSchedulerBeanPresent`——save/delete 经 GraphQL 真实入口 → override → seam 命中真实 bean → register/unregister 真实执行，断言真实 LocalJobScheduler 可观察效果（job 出现/消失，比计数器 mock 更强）；(b) `testSchedulerBeanMissingSkipsRegisterWithoutError`——Mockito spy + `doReturn(null)` 覆写 seam（先例 = testDeleteFailureKeepsSchedule 的 spy；不注册/注销全局 provider），save/delete 主路径正常完成（行落盘/删除成功）+ job 集合零变化（跳过不抛、非吞异常）。**实现要点**：(b) 直调 BizModel 需环境 session（裸调时 load/delete 各开新 session → `entity-not-in-session`，首轮红实证）——`orm.runInSession` 包裹（沿 `TestMetaQualityCheckpointScheduler` 直调先例，对齐生产"每请求一 session"语义）；save 直调用 `ServiceContextImpl`+`UserContextImpl`（沿 `TestNopMetaDataProductLinkAssetAggregateRoot` 先例，raw `doSave` 需非 null context 的 `getEvalScope()`）。
- **变异验证（区分力实证）**：seam 临时改恒返回 null → `testSaveDeleteWiringSchedulerBeanPresent` 红（`save override must register cron job ... expected: <true> but was: <false>`）+ `testDeleteFailureKeepsSchedule` 前置断言红（`seam must resolve ... expected: <MetaQualityCheckpointScheduler@...> but was: <null>`，双失败点）；恢复 → 全绿。**rg 证据（断环单向）**：`rg "@Inject" NopMetaQualityCheckpointBizModel.java` 命中 = connectionService/scoreBizModel/httpClient/messageService 四字段注入 + javadoc 字样，无 scheduler 注入；scheduler 侧 `setCheckpointBizModel` 的 `@Inject`（MetaQualityCheckpointScheduler:110-111）保留——环仅剩 scheduler→bizmodel 单向。
- 验证（Phase 1）：`TestNopMetaQualityCheckpointBizModel` 31/31（基线 29 + 2 新增）+ `TestMetaQualityCheckpointScheduler` 8/8 + `TestMetaQualityCheckpointSchedulerCronReadFailure` 2/2 + `TestSilentSwallowFormalization` 29/29；`./mvnw test -pl nop-metadata -am -T 1C` **BUILD SUCCESS**（service **1264/0/0** = 0549-2 基线 1262 + 净 2）。owner doc 无 BizModel↔scheduler 注入关系表述 → No owner-doc update required。
- **Fix（P2-31 BEAN_NAME 注释指向真实注册文件，Phase 2）**：`MetaQualityCheckpointScheduler:83` 注释由"与 app-quality-scheduler.beans.xml 一致"（该文件 live 不存在）改为指向 live 唯一注册点 `app-service.beans.xml` 的 `metaQualityCheckpointScheduler` bean（:40），并注明 `ioc:default="true"` 宿主可覆盖注册语义（jobParams 经 BEAN_NAME 反查本 bean 的依赖关系一并注明）。验证：`rg "app-quality-scheduler.beans.xml" nop-metadata -g '*.java'`（剥 target）**零命中**；指向文件 live 存在（`ls` + `rg metaQualityCheckpointScheduler` :40 核对）；`./mvnw compile -pl nop-metadata -am -T 1C` BUILD SUCCESS（注释变更不破坏构建）。comment-only 无行为面 → No new test required。
- **Decision（P2-03 OrmModelImporter 驻留裁定 + 模块结构表补全，Phase 3）**：裁定落档 owner doc（`docs-for-ai/03-modules/nop-metadata.md`）——`dao/model/OrmModelImporter` **维持 dao 驻留**（依赖方向论据：service 依赖 dao，模型载入/映射属 dao 载入域；同形先例 = `nop-wf-dao` 的 `dao/store/DaoWorkflowModelLoader`；`nop-auth-dao` 不作同形先例引用——无严格同形物，最近邻仅 `dao/mapper` SqlLibMapper 空接口；真实缺口为文档侧非代码迁移需求）。模块结构表 dao 行补 `model/` 子包（`dao/model/OrmModelImporter`，live 路径核对一致）。验证：`node ai-dev/tools/check-doc-links.mjs --strict` **17 errors = pre-existing 基线零新增**（裁定沿用 0226-1/0226-2/0226-3 收口先例：17 全部为其他 mission 归属文件 + 既有 BOUNDARY 提示（nop-metadata.md:263 guard 6 门禁脚本提及，0226-2 既有）；本计划改动文件 0 新增）。documentation-only → No new test required。
- **收口（closure audit approved）**：独立 closure audit（fresh session `ses_ff7edee29ffetApoLGOajsvBHL`，review-only 零文件修改）**10/10 检查全 PASS**（审计者独立重扫：@Inject 单向 rg 证据、seam/notify 方法 file:line 核对、接线测试三处断言形态、app-quality-scheduler 零命中、beans.xml:40 注册、owner doc dao 行 + 裁定段、roadmap ✅、Anti-Hollow 运行时链追踪、git 改动面仅计划内文件、surefire 31/0/0 二次复跑），verdict `CLOSURE_AUDIT: approved`（4 条 Minor 均非阻塞：doc-links exit 1 属已裁定跨 mission 基线、plan 未收口态为本审计前置、plan 措辞"全部为其他 mission 归属文件"按 1/17 = nop-metadata.md:263 既有 BOUNDARY 精确化、nop-format `.rels` EOL-only 零内容差异与本计划无关）。Closure Gates 全绿：`./mvnw compile -pl nop-metadata -am -T 1C` 通过；`./mvnw test -pl nop-metadata -am -T 1C` **BUILD SUCCESS**（service **1264/0/0** = 0549-2 基线 + 净 2）；checkstyle = 既有全仓未接入门禁基线（上游 nop-api-core 9164 条，改动文件交集 0）；`run-nop-metadata-invariants.sh` 6-guard 全链 exit 0；`check-plan-checklist --strict` exit 0；`scan-hollow --severity high` exit 0（0 findings）；roadmap P2-02/P2-03/P2-30/P2-31 ✅ Fixed。Plan Status → `completed`（Completed: 2026-08-16）。

### EXEC_PLANS Cycle 3 / 执行顺序 5b — plan 2026-08-16-0549-2 测试断言强度与 DTO 契约形态族（P2-17/P2-20 + P2-35 测试子项）

plan `ai-dev/plans/2026-08-16-0549-2-nop-metadata-test-assertion-and-dto-contract.md`（mission-driver EXEC_PLANS；3 Phase + Closure）。

- **Fix|Proof（P2-17 接口完备性守卫）**：live 重扫（`biz/INopMeta*Biz.java` 逐文件方法抽取）= 非空 **14**（Table 7 / DataSource 4 / Module 4 / LineageEdge 8 / QualityRule 3 / DataContract 2 / DataProduct 3 / QualityResult 2 / ReconciliationResult 2 / TagLabel 2 / ProfilingRule 1 / QualityCheckpoint 1 / QualityScore 1 / ReconciliationConfig 1），与 plan 基线一致。补 2 接口逐方法断言（executeReconciliation 2 参 / confirmMatch 4 参 / batchConfirmMatches 3 参）；新增 `testCoverageListMatchesFilesystemScanOfAllBizInterfaces` 全集守卫（文件系统扫描 biz 包源目录 `INopMeta*Biz.java` + `getDeclaredMethods()` 反射取方法集 + `COVERAGE` 真值表双向集合相等 + ≥14 sanity；目录定位 3 路径回退）。**变异验证（守卫区分力）**：新增未登记接口 `INopMetaMutationProbeBiz` → 红（`Scan only: [INopMetaMutationProbeBiz]`）；COVERAGE 删除 `batchConfirmMatches` → 红（`Scan only: [batchConfirmMatches]`）；恢复 → 4/4 绿。附带发现：向既有接口加方法形态的变异在编译期即红（BizModel 未实现抽象方法）——该向量由编译器+守卫双层拦截。owner doc IBiz 表（:186 段）增补守卫表述（守卫盲区契约以此为背书）。
- **Decision|Fix（P2-20 冗余 List<Map> 字段处置）**：**消费面终审清单**（rg 全仓 2026-08-16）：main = `NopMetaQualityCheckpointBizModel`（写 2 处 :222/:230）+ `MetaQualityCheckpointScheduler`（写 1 处 :253 + javadoc :201/:246）；test = `TestNopMetaQualityCheckpointBizModel`（:346/:361/:1099）+ `TestMetaQualityCheckpointSchedulerCronReadFailure`（:100-102）；xmeta/view/page/e2e/nop-metadata-web/docs-for-ai **零命中**。**字段级等价/损失对照表（裁决依据）**：
  - `executionResults` 6 键 → `QualityRuleResultDTO`：等价承接 3（qualityRuleId/status/message）+ 无承接 3（ruleName/actualValue/expectedValue）→ **变体 (b)** 增补三字段（actualValue/expectedValue 为 Double，对齐 QualityRuleJudgment 类型）；
  - `executionErrors` 四族键 → `ErrorDTO`：error→message 等价；source/refType/refValue 无承接 → **变体 (c)** 增补三字段；qualityRuleId/ruleName/metaTableId → code/detail 沿 `NopMetaQualityRuleBizModel:265`（code=qualityRuleId, detail=ruleName）标识符惯例承接；scheduler 族直接构造 ErrorDTO（不经 producer 映射）。
  - **裁决 = 变体 (a)+(b)+(c) 后移除两字段**（每个键均有类型化承接，零未裁定信息损失）：DTO 删两 `List<Map>` 字段；BizModel `mapRuleResults` 3→6 键 + 新增 `mapErrorEntries` 填类型化 errors（**变体 (a)**：类型化 errors 此前从不填充）；Scheduler `buildErrorResult` 改构造 `ErrorDTO(source="scheduler")`。
  - **Scheduler buildErrorResult 去向逐项对照**：`{source:"scheduler", error}` → `errors[0]{source="scheduler", message=<error>}`（逐键等价，由 CronReadFailure 测试钉死）。
- **断言重写（强度不降反升，逐场景）**：`exec()` GraphQL selection `executionErrors` → `errors { code message detail source refType refValue }`；缺失规则/缺失表场景（原 :333/:334 contains）保留 contains + 新增 resolution 逐键断言（source=resolution + refType=ruleId/tableId + refValue 三键匹配）；异常规则场景（原 :361）保留 contains + 新增 execution 逐键断言（source=execution + code=r-cp-throw）；CronReadFailure 新增 `source="scheduler"` 断言。残留检查：`rg "executionResults|executionErrors" nop-metadata -g '*.java'`（剥 target）**零命中**（注释提及一并改述为"List<Map> 冗余形态"）。
- 验证：`TestNopMetaBizInterfaceCompleteness` 4/4、`TestNopMetaQualityCheckpointBizModel` 29/29、`TestMetaQualityCheckpointSchedulerCronReadFailure` 2/2、`TestNopMetaDtoResults` 5/5；`./mvnw test -pl nop-metadata -am -T 1C` **BUILD SUCCESS**（注：首轮 reactor 内 `nop-stream-rocksdb` 增量 checkpoint **基准计时断言** flaky（ratio 2.4，与本模块改动无关的 timing 断言），复跑全绿；nop-metadata 各模块两轮均 SUCCESS）。
- **Fix（P2-35 六项测试子项，逐项"修复前→修复后断言强度"对照）**：
  1. `TestAggregationHelper` 无 @Test → **裁定更名** `AggregationTestHelper`（git mv + 类/构造器/3 个实例化类 static import + `TestNopMetaAggregationBizModel` 2 处 javadoc 同步）：原 `Test*` 前缀命中 surefire 默认发现模式但运行 0 例（命名与行为不符，非计数膨胀）；更名后不匹配任何 include 模式（`Test*` / `*Test` / `*Tests` / `*TestCase` 均不命中），命名如实。owner doc 无该类提及 → 无需同步。测试计数不变（原 0 例）。
  2. `TestLimitTargetSetCompleteness` 计数正则单一形态 → 扩展 `@Name\s*\(\s*(?:value\s*=\s*)?"limit"\s*\)`（canonical / value 显式赋值 / 空白变体全覆盖；`countInFile` 改共享 `countNameLimitOccurrences`）；**样例单测红→绿证明**：4 形态样例（canonical+value=+两空白变体）旧正则计 1/4 红（`expected: <4> but was: <1>`），新正则 4/4 绿 + 负样例（`limitOverride`/`offset` 0 误计）。live 源 4 处命中全为 canonical，新正则计数不漂移（methodTableSizeMatchesSourceCount 仍绿）。`@RequestBean` 内 limit 形态经复核不在守卫口径（INV-LIMIT = 显式 @Name("limit") 参数方法；DTO 字段无 @Name 形态），记入方法 javadoc。
  3. `TestCoreMetricsUsage` 注释剥离假阴性/误报 → 正则链替换单遍状态机 `stripJavaCommentsForScan`（行注释 + 跨行块注释完整剥离（回填换行） + 字符串/字符字面量原文保留（字面量内 `//`、`/*` 不作注释起点）；模块 main 无文本块（live rg 0 命中），文本块不在口径）。**样例单测红→绿证明**：(a) 多行块注释内 target 调用旧正则残留（`/\*.*?\*/` 无 DOTALL）→ 红（`expected: <false> but was: <true>`），新剥离器完整剥离 → 绿；(b) 字符串内 `//` 后同行真实调用旧正则被误剥（漏报洞）→ 新剥离器保持可检出；(c) 行注释/单行块注释既有行为不回归。主扫描测试对 live main 仍绿（无存量违例）。
  4. `TestNopMetadataErrorsCentralized`：(a) 镜像断言改独立字面量真值——构造器测试 `assertEquals(CONSTANT.getErrorCode(), e3.getErrorCode())`（同源恒真：构造器回存入参即过）→ `assertEquals("nop.err.metadata.datasource-not-found", ...)` / `"nop.err.metadata.orm-resource-not-found"`；`testCentralizedErrorCodesDefined` 的 3 个 assertNotNull 抽样升格字面量全值断言（datasource-type-not-supported / orm-resource-not-found / quality-expect-pass-when-invalid）。死码面由既有门禁 `check-error-param-consistency.mjs` define 面规则（P2-10）守护，本测试不重复。(b) 硬编码 10 接口清单 → **源码目录动态枚举**（扫 `src/main/java/io/nop/metadata/service/*Errors.java` + Class.forName 加载 + ≥10 sanity）+ 集中化接线断言（发现的每个接口必须是 `NopMetadataErrors` 父接口——新 `*Errors` 接口未接线即红，封 F19 族盲区；门面自身豁免接线检查）。
  5. `testCrossDbAliasOf` 仅 assertNotNull → 三态值断言：显式 alias 原样返回（"r1"）/ null alias → "right" / 空白 alias → "right"（alias-or-"right" 语义两态 + 边界全覆盖，非恒真）。
  6. `testDtoJsonRoundTrip` 无 parse-back → F18 形态 round-trip（code/message/detail 三键 + **P2-20 新增承接字段 source/refType/refValue** 全键回放断言）；`testDtoJsonRoundTripAllTypes` 的 QualityRuleResultDTO 段同步覆盖新增 ruleName/actualValue/expectedValue（42.0/10.0 double 精度断言）——Phase 2 DTO 增补字段自此有 round-trip 覆盖（Test-Mandated Feature Rule）。
- 验证（Phase 3）：`./mvnw test -pl nop-metadata/nop-metadata-service -am -T 1C` BUILD SUCCESS（**1262/0/0**，0549-1 收口基线 1259 + 净 3：+1 接口全集守卫 +1 limit 正则样例 +1 注释剥离样例；项 1 更名计数不变）；门禁聚合链 `run-nop-metadata-invariants.sh` 全绿 exit 0（6 guards：1-4 零命中、5 基线内、6 零命中）。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md` 两处——IBiz 表段增补全集守卫表述（P2-17）；§4 增补 executeCheckpoint 返回值字段清单 + P2-20 裁决记录 + **迁移面结论**（"全仓消费面清点 = 零外部消费（web/e2e/xmeta 零命中），无可迁移面"；GraphQL schema 运行时生成，无静态 schema 文件需同步）。Phase 3 纯测试变更：No owner-doc update required（项 1 更名无 owner doc 提及处）。
- **收口（closure audit approved）**：独立 closure audit（fresh session `ses_ff8284916ffekAvK9RMIPU1Fi6`，review-only 零文件修改）**20/20 检查全 PASS**（审计者独立重扫 14 非空接口与 COVERAGE 键集逐一相符、`rg executionResults|executionErrors` 复跑零命中、DTO/映射/测试断言逐 file:line 核对、owner doc 裁决与迁移面结论验证），verdict `CLOSURE_AUDIT: approved`。Closure Gates 全绿：`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（service 1262/0/0）；`check-plan-checklist --strict` exit 0；`scan-hollow --severity high` exit 0（0 findings）；门禁聚合链 `run-nop-metadata-invariants.sh` exit 0；checkstyle 直跑 9164 条 = 既有全仓未接入门禁基线（本计划改动文件交集 0，逐文件名比对）；doc-links 17 = 既有跨 mission 基线（本计划文件 1 处断裂链接顺手修复，改动文件 0 新增）。Plan Status → `completed`（Completed: 2026-08-16）；roadmap P2-17/P2-20 ✅ Fixed + P2-35 全部子项收口 ✅；Doc-sync 汇总：`docs-for-ai/03-modules/nop-metadata.md`（IBiz 表守卫表述 + §4 契约/裁决/迁移面结论）、`ai-dev/backlog/nop-metadata-invariant-loop-roadmap.md`（P2-17/P2-20/P2-35 处置结果）。

### EXEC_PLANS Cycle 3 / 执行顺序 5 — plan 2026-08-16-0549-1 文档/元数据/依赖卫生族（P2-15/P2-16/P2-32 + P2-35 文档/依赖/结构裁定子项）

plan `ai-dev/plans/2026-08-16-0549-1-nop-metadata-doc-deps-and-metadata-hygiene.md`（mission-driver EXEC_PLANS；5 Phase + Closure）。

- **Fix（P2-15 DTO 计数 + P2-35 文档子项）**：live 重扫 `rg -l "@DataBean" nop-metadata/nop-metadata-api/src/main/java/io/nop/metadata/api/dto/ | wc -l` = **30**（非 doc 宣称 31；另 2 处 `@DataBean` 在非 DTO 文件 `INopMetaDataSourceBiz`/`INopMetaTableBiz`，不在口径内），owner doc :221/:280 两处 31→30，`rg -n "31 个"` 零命中。模块结构表 `nop-metadata-service` 行补 NopMetaSearch xmeta（`_vfs/nop/metadata/model/NopMetaSearch/NopMetaSearch.xmeta`——Pseudo-BizModel 无 ORM 实体；live `find` 全模块确认**无 NopMetaSearch.xwf**）+ 3 审批流行（`_vfs/nop/wf/{metaDataContractApproval,qualityBreachApproval,tagLabelConfirmApproval}/v1.xwf`——全模块仅此 3 个 xwf，live 路径逐一核对）。source-anchors META-001 去 "268 行" 易漂移字面量 → "纯分派器——职责即 7 路径分派，不随行数增长漂移"（live `wc -l` = 274，计数漂移第三例；优先选去字面量而非追改实数）。
- **Fix（P2-16 _templates README）**：计数 32→39（README 句子作用域即 `_NopMeta*.json`，未写全目录 40）。对账（静态口径 (a)）：`ls nop-metadata/nop-metadata-meta/_templates/_NopMeta*.json | wc -l` = 39，`rg -c '<entity ' nop-metadata/model/nop-metadata.orm.xml` = 39，模板名 ↔ 实体 className 尾段双向 diff **为空**（无孤儿模板）；`_MetadataPropagation.json` 为非实体模板（元数据传播配置，全目录 JSON 40）；"保留不动" watch-only 裁定维持，`git status _templates/` 仅 README 变更、JSON 零增删。
- **Fix（P2-32 xmeta 命名空间）**：NopMetaSearch.xmeta 根元素补 `xmlns:i18n-en="i18n-en"`（形态对齐本模块生成物先例 `_NopMetaTableMeasure.xmeta`，`nop-rule-meta` `_NopRuleNode.xmeta` 同形态）；`xmllint --noout` 修复后**零输出**（well-formed + 无未绑定前缀），前形态探针（`_tmp/prefix-probe.xmeta`）显式报 `namespace error : Namespace prefix i18n-en ... is not defined`——xmllint 对该类可恢复错误 exit 0，以输出为零为准（区分力留证）；文件内 4 处 `i18n-en:` 用法均在根声明覆盖下；en/zh i18n yaml（meta + web 模块共 6 文件）NopMetaSearch key **零命中**——缺失现状登记不补译（如需补译随 P2-12 ask-first 轮次，见 plan Non-Blocking Follow-ups）。
- **Fix + Decision（P2-35 依赖子项）**：`nop-wf-api` 显式声明入 `nop-metadata-service/pom.xml`（无版本号，`nop-bom/pom.xml:1084` 托管；main 唯一消费点 `QualityAlertWorkflowProcessor.java:22` `import io.nop.wf.api.WfReference`，原经 nop-wf-core 传递到达）。`nop-search-lucene` scope 裁定 **(a) test scope**：实证 = main 代码零 lucene import（`NopMetaSearchProcessor` 仅 Javadoc 字样）+ 测试仅 import `io.nop.search.api`（lucene 由运行时 SPI 到达）；optional compile 的"宿主可替换 impl"注释与 optional 本身已不传递的事实矛盾（无更强理由维持 (b)）；test scope 后宿主生产搜索自引 `nop-search-*` impl 不变（部署提示同步 owner doc test-scope 基建依赖段）；`rg "<optional>true</optional>" -B3` 该依赖不再命中 optional 形态。
- **Decision（P2-35 结构子项 AggregationHelper）**：live **952** 行混装裁定 classification = `optimization candidate`（Why Not Blocking：行为正确性由 6 hard-gate 门禁 + 1259 测试守护；两次拆分计划（1250-4 / 0722-0900-2）后剩余为可维护性长尾，非正确性缺陷非 contract drift）；Successor Required = no（如未来派生从 roadmap P2-35 条目出发）；落档 plan `Deferred But Adjudicated` 段 + roadmap P2-35 🟡 部分收口标注。
- 验证：`./mvnw compile -pl nop-metadata -am -T 1C` exit 0；`./mvnw test -pl nop-metadata -am -T 1C` **BUILD SUCCESS**（service **1259/0/0** = 0226-3 基线零漂移；NopMetaSearch 回归族 41/41：Processor 11 + Integration 18 + IndexBuilder 10 + GraphQLSchema 2——xmeta 声明修复无行为变化）；`node ai-dev/tools/check-doc-links.mjs --strict` 17 errors = pre-existing 基线零新增（沿 0226-1/0226-2/0226-3 收口先例显式裁定）。
- **收口（closure audit approved）**：独立 closure audit（fresh session `ses_ff8778b9bffeUPrfkszHVJCrh7`，review-only 零文件修改）**10/10 检查全 PASS**（审计者独立重扫：DTO 30、模板 39 双向 diff 空、xmllint 复跑零输出、pom 直读、surefire 1259/0/0、doc-links 17 基线自复跑、`git diff --name-only` 零 orm.xml 零 Java），verdict `CLOSURE_AUDIT: approved`。Closure Gates 全绿：compile exit 0 / test BUILD SUCCESS / `check-plan-checklist --strict` exit 0 / `scan-hollow --severity high` exit 0 / checkstyle 直跑 fail 于上游 nop-api-core 9164 条（与 0226-2 收口记录逐条一致的全仓既有基线，零 Java 改动面）。Plan Status → `completed`（Completed: 2026-08-16）；roadmap P2-15/P2-16/P2-32 ✅ Fixed + P2-35 🟡 部分收口（测试子项显式移交 0549-2）。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md`（DTO 计数 2 处 + 模块结构表 + test-scope 依赖段）、`docs-for-ai/04-reference/source-anchors.md`（META-001 行数表述）、`nop-metadata-meta/_templates/README.md`（计数 + 模板集对账节）、`nop-metadata-service/pom.xml`（wf-api 显式 + lucene test scope）、`ai-dev/backlog/nop-metadata-invariant-loop-roadmap.md`（P2-15/16/32 ✅ + P2-35 🟡）。

### DRAFT_PLANS — Cycle 3 后续 P2 批次清扫第二组三计划起草并过审（active）

mission-driver DRAFT_PLANS 轮：承接 0226-1/2/3 之后的剩余 P2 族（roadmap Follow-up Backlog 未 ✅ 项），按族拣选 3 个计划，执行顺序 1→2→3（N 前缀序）。

- **`2026-08-16-0549-1` 文档/元数据/依赖卫生族**（P2-15/P2-16/P2-32 + P2-35 文档/依赖/结构裁定子项）：owner doc DTO 计数 31→30（:221/:280 两处）、_templates README 计数 32→39（`_NopMeta*` 口径，+模板集 vs 39 实体对账；全目录 JSON 40 含 `_MetadataPropagation.json`）、NopMetaSearch.xmeta 补 `xmlns:i18n-en="i18n-en"` 声明（对齐本模块 `_NopMeta*.xmeta` 生成物先例；orm.xml 根元素自身前缀问题登记 follow-up 不入本计划）、模块结构表补 NopMetaSearch + 3 审批流 xwf 行、source-anchors META-001 行数去漂移、nop-wf-api 显式声明（经 nop-wf-core 传递、nop-bom:1084 托管）、nop-search-lucene scope 显式裁定（main 零 import 实证 + optional 已不传递）、AggregationHelper 952 行混装裁定为 optimization candidate 落档。
- **`2026-08-16-0549-2` 测试断言强度与 DTO 契约形态族**（P2-17/P2-20 + P2-35 测试子项）：接口完备性守卫 12/14→全 14（补 Reconciliation 两接口）+ 文件系统扫描程序化全集守卫（F19 族盲区封堵，盲区契约显式）；P2-20 重锚定——**审计"纯冗余"简述被 live 证伪**（类型化 `errors` 从未被填充、`ruleResults` 为 6→3 键有损投影、scheduler 只写不读），Phase 2 重排为"消费面清单 → 字段级等价/损失对照表 → 显式决策规则（损失未裁定禁删；补承接变体 a/b/c in-scope）→ 实施 → 落档"；P2-35 六项测试子项（TestAggregationHelper 命名误导、两正则假阴性洞以样例单测钉死、镜像断言+硬编码 10 接口清单、assertNotNull 升值断言、round-trip 补 parse-back）。
- **`2026-08-16-0549-3` IoC 装配债务与注释真值化族**（P2-02/P2-30/P2-31/P2-03）：BizModel↔Scheduler 双向 @Inject 断环（BizModel 侧改 protected lookup seam 按 BEAN_NAME 懒解析，scheduler→bizmodel 核心路径保留注入）、两处矛盾 javadoc 收敛真值、BEAN_NAME 注释指向真实注册文件（app-service.beans.xml:40）、testDeleteFailureKeepsSchedule 反射前置断言重写为接线断言（:1053-1059）、接线测试双态（(b) 用既有 Mockito spy 先例 :1069 覆写 seam，不动全局容器）、OrmModelImporter 驻留裁定（先例 nop-wf-dao DaoWorkflowModelLoader；nop-auth-dao 不作同形先例）+ owner doc dao `model/` 行。**P2-27（orm.xml 陈旧注释）显式移出**：源模型文件编辑（含 comment-only）统一随 ORM 族轮次走人工确认门，不由 AI 自行豁免；真值归属已考证（plan 0700-1 Phase 1 D1 端点级互斥 / 架构基线 §2.5.2，非 Cycle 2 D1）记入 follow-up。
- **两轮独立对抗性审查至共识**（每计划两个 fresh session，review-only）：第一轮 3 Blocker + 6 Major（P2-16 计数口径 40→39 防"修漂移再引入漂移"、P2-32 范本失效（orm.xml 自身畸形）换模块内生成物先例、P2-20 基线三处失实（setErrors 从未调/有损投影/只写不读）决策重锚定、P2-27 D1 出处张冠李戴 + 授权自缩禁、反射断言破坏未列、发现机制/测试机制未钉死等）全部修复；第二轮三计划均 **APPROVED**（残余 Minor 已顺手落：No-new-test 注记、cwd 统一、DTO 行号 :31-32、变体 (c) ErrorDTO、Phase 1 Targets 去越界项、spy 先例钉死）。三计划 Plan Status → `active`。
- 未覆盖（留后续轮次）：P2-01/26/27/28/29/34 ORM 族（结构+源模型文件变更统一人工确认门）、P2-05/P2-33 产品级裁定项、P2-12 i18n（ask-first）、orm.xml 根元素前缀声明（随 ORM 族）。

### EXEC_PLANS Cycle 3 / 执行顺序 4 — plan 2026-08-16-0226-3 BizModel 行为/契约形态族（P2-18/P2-19/P2-21/P2-22/P2-24/P2-25）

plan `ai-dev/plans/2026-08-16-0226-3-nop-metadata-bizmodel-behavior-and-contract-form.md`（mission-driver EXEC_PLANS；6 Phase + Closure）。

- **收口（closure audit approved）**：**独立 closure audit**（fresh session `ses_ff8a85ab9ffe5bn0HfAB9b7Hwe`，review-only 零文件修改）**22/22 检查全 PASS**（逐 Phase Exit Criteria file:line 证据 + 审计者独立复跑 4 新测试类 8/8 绿 + 22 项 Anti-Hollow/门禁/roadmap/log 核对），verdict `CLOSURE_AUDIT: approved`。审计唯一附注（接口历史注释含 "@BizMutation" 字面量致 rg 门禁 1 次假阳性）已处置：注释措辞改"变更类"，`rg -A2 '@BizMutation' | rg testConnection` 恢复字面零命中 + datasource 测试 9/9 复跑绿。**Anti-Hollow 双变异验证（执行者）**：P2-24 回退空 lambda → 接线测试红（remark 哨兵 null）；P2-22 恢复旧方法体 → 测试仍绿（ORM 对未变更实体不产生写——"无效果 updateEntity"定性实证，删除为死代码消除）。**Closure Gates**：`check-plan-checklist --strict` exit 0（1 passed）；`scan-hollow --severity high` exit 0；`check-silent-swallow` 0 命中（125 catch）；doc-links 17→17 零新增；checkstyle 改动 16 文件 rule 级零新增（净 -4：QualityScore -3 / Tag -1，沿 1913-3 "HEAD vs now 逐条比对"先例，经 git stash 双态对照）；`./mvnw clean install -DskipTests -pl nop-metadata -am` + `./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（service **1259/0/0** = 0226-2 基线 1251 + 净 8：+3 对抗 +2 防护 +1 接线 +2 approve）。Plan Status → `completed`（Completed: 2026-08-16）；roadmap P2-18/19/21/22/24/25 六条目 ✅ Fixed。**执行环境插曲（记录）**：过程中误触发无关历史 stash pop（xview.xdef/TestNopMetaSearchProcessor 短暂污染），即时发现并 checkout HEAD 还原（无关 stash 条目原样保留，diff 面确认零残留）；plexus JavaxTools in-process 编译器以 ConcurrentModificationException 掩盖 assertEquals 重载歧义真实错误（`-Dmaven.compiler.fork=true` 定位）。

- **Phase 1 Fix（P2-21 DataProduct JsonTool）**：`NopMetaDataProductBizModel` 三处手工拼接（linkAsset/unlinkAsset/getLinkedAssets）替换为 `JsonTool.stringify(Map.of("dataProductId", dataProductId))`；`rg '\\"dataProductId'` 零命中。新增 `TestNopMetaDataProductAdversarialIds` 3 例——(a) 坏字符 ID（`"`+`\`+`\n`）端到端 round-trip：link 写入合法 JSON（parse 回原值）→ getLinkedAssets eq 命中 → unlinkAsset eq 匹配删除（修复前该链路断裂：拼接损坏 → unlink 永不命中）；(b) 正常 ID JsonTool 输出与旧手工拼接逐字节一致（存量行兼容）；(c) 幂等回归（同 ID 双 link 单行）。1913-2 linkAsset 测试族 6/6 无回归。**存量坏行裁定**：旧代码写入的对抗 ID 损坏 metadata 行升级后 unlink 不到——平台生成 ID 不含坏字符（seq 域），影响面≈0，不做数据迁移（裁定记录，本条即一行裁定）。另注：TagLabel.metadata 列 `stdDomain=json`，save 管线本身会拒损坏 JSON——修复前坏 ID 的实际表现是 link 被 domain 校验拒绝（feature 不可用），非静默写坏行；修复后正常可用。Owner-doc 核对：nop-metadata.md DataProduct 段无拼接表述 → No owner-doc update required。
- **Phase 2 Fix（P2-19 save override null-data 防护）**：**重扫清单（2026-08-16 live，`rg -n -A2 'public .* save\('` 全量 override 重扫）**：先解引用形态 6 处定稿——`NopMetaTableDimensionBizModel`（save 内 :58 stringOf metaTableId）、`NopMetaEntityFieldBizModel`（:30 stringOf businessDomainId）、`NopMetaTableJoinBizModel`（:66 validateJoin→stringOf）、`NopMetaTableMeasureBizModel`（:62 stringOf metaTableId）、`NopMetaTableFilterBizModel`（:58 stringOf metaTableId）、`NopMetaTagLabelBizModel`（:70 containsKey("state")）；已有防护 3 处——Module :121 / Table :101（`data == null ? null :` 三元，只防 null）/ Tag :31（`data != null &&`）。**落地**：6 处 + Module/Table/Tag 三处既有防护**全部统一为提前委托形态**（`if (CollectionHelper.isEmptyMap(data)) return super.save(data, context);` 置于方法首行）——Module/Table 三元简化、Tag 冗余 null 判消除，全模块 12 个 save override 单一形态（无"两种形态并存"，无需容忍理由）。新增 `TestSaveOverrideNullDataGuard`：9 处 override × null + empty map 两场景，错误码精确断言 `nop.err.biz.empty-data-for-save`（直调可行——提前委托分支在触达 dao 前抛出，getBizObjName 只读注解）。验证：service 全量 **1256/0/0**（0226-2 基线 1251 + 净 5：+3 对抗 + 2 防护）。Owner-doc 核对：错误语义即模块既有基类契约（CrudBizModel doSave），nop-metadata.md 无 save override 参数校验表述 → No owner-doc update required。
- **Phase 3 Fix（P2-24 computeQualityScore 恢复 defaultPrepareSave）**：`doSave(data, null, (entityData, ctx) -> {}, context)` → `doSave(data, null, this::invokeDefaultPrepareSave, context)`（与基类 save:528 同形态）；`rg '\(entityData, ctx\) -> \{\}'` 该文件零命中。**接线测试**（经容器/xbiz 分发路径，非测试子类直调）：新增测试 delta `_vfs/_delta/default/nop/metadata/model/NopMetaQualityScore/NopMetaQualityScore.xbiz`，用 `<action name="defaultPrepareSave">`（xbiz.xdef:64 内部 action 元素，非 GraphQL 面）覆盖，副作用写 remark 列（既有评分断言不依赖）；新增 `TestComputeQualityScorePrepareSaveWiring`：GraphQL 入口 computeQualityScore → 落盘行 `remark='defaultPrepareSave-invoked'` 哨兵断言 + overallScore=100 评分不变。**变异验证（Anti-Hollow）**：临时回退空 lambda → 该测试红（`expected: <defaultPrepareSave-invoked> but was: <null>`），恢复 → 绿（区分力实证，非恒真断言）。**调用面核对**：computeQualityScore 调用方 = NopMetaQualityCheckpointBizModel:354（autoScore 链，raw impl 注入直调方法体——修复在方法体内生效）+ GraphQL 手动入口；TestNopMetaQualityScoreBizModel 8/8 + Scheduler 8/8 + CheckpointBizModel 29/29 全绿（无"依赖空 prepareSave"的隐含假设；delta 对全模块测试生效、remark 列无既有断言）。Owner-doc 核对：nop-metadata.md 评分段（§4 执行结果 DTO / 自动评分触发）无管线 prepareSave 表述 → No owner-doc update required。
- **Phase 4 Fix + Decision（P2-18 testConnection @BizQuery）**：**前置清点（`rg -n 'testConnection' -g '!target'` 仓库级）**：Java 面 = 接口 `INopMetaDataSourceBiz:26` + 实现 `NopMetaDataSourceBizModel:120`（双面 @BizMutation）+ `TestConnectionResultDTO` javadoc 提及 + `TestNopMetaBizInterfaceCompleteness:49`（方法存在断言，无 operation 语义）+ 测试 6 处 mutation 调用（`TestNopMetaDataSourceBizModel` :64/:79/:92/:102/:113 + `TestNopMetaDataSourceConnectionConfigWritePath:66`）；xmeta/view/page/e2e/nop-metadata-web/nop-metadata-meta 零引用（live 复核确认）；docs 面 = owner doc :187/:271。**落地**：接口 + 实现注解双面 `@BizMutation`→`@BizQuery`；6 处测试调用 mutation→query 同步；`rg -A2 '@BizMutation' -g '*.java' nop-metadata/ | rg testConnection` 零命中 + 仓库无残留 mutation 形态调用。**授权面影响核对（Decision 记录）**：机制事实——无显式 `@Auth` 的 action 由 `ReflectionBizModelBuilder:334` 生成默认权限串 `bizObjName:opType|bizObjName:action`，`GraphQLActionAuthChecker.isAllowAccess` 经 `isPermissionSetSatisfied` **any-of** 判定（`DefaultActionAuthChecker`→`SiteMapProviderImpl` permission→roles 映射）；翻转使粗粒度兜底 `:mutation`→`:query`。裁定：① 默认部署（action-auth 未启用，SSRF P0 审计语境）翻转零影响；② 启用 action-auth 的部署——细粒度 `:testConnection` 条目不受影响、仅授 `:mutation` 者失权（fail-closed 收窄）、仅授 `:query` 者新获权（**SSRF 触发面向只读角色扩大**，需部署侧知情）；建议部署侧为 testConnection 配细粒度功能点、不依赖粗粒度兜底；SSRF 主防御（F2/F5/F7）与注解无关不受影响。测试：datasource 族 67/67 绿（BizModel 9 + WritePath 4 + Security 50 + Crud 1 + InterfaceCompleteness 3）。Doc-sync：owner doc 安全契约段新增 P2-18 bullet（注解语义 + operation 翻转兼容性 + 授权面结论）。
- **Phase 5 Fix + Decision（P2-22 approve 语义诚实化）**：**工作流路径核对结论（live 证据）**：`qualityBreachApproval/v1.xwf` agree 路径 = owner-investigate --agree--> verify 步骤（:45-62 c:script `svc.reJudgeFailClosed(ruleId, resultId)`，**无条件先于流程结束**执行 re-judge）--agree--> to-end；xwf **未挂 wf-approval:notifyResult listener**（唯一 listener = onDisagree :76-95，disagree 结束时直改实体 isFalsePositive=1，不经 reject 方法；基类 oa.xwf 无 listeners）——故 live 工作流 agree 结束路径**不触发 approve**，approve 仅作为 notifyResult 约定挂点（wf-approval.xlib:22-23 会 invoke 'approve'，若宿主后续接线）。**落地终态 (a)**：javadoc 改真值（"无字段变更、不做 re-judge；re-judge 在工作流 verify 步骤先于流程结束完成"）+ 删除无效果 `dao().updateEntity(entity)`（无乐观锁/时间戳诉求、实体未变更），方法体 = `return requireEntity(id, "approve", context)`。**变异核对（行为等价实证）**：临时恢复旧方法体跑 `testApproveIsSideEffectFree` 仍绿——ORM 对未变更 managed 实体的 updateEntity 不产生任何 DB 写（版本列不变），确认 audit"无效果 updateEntity"定性，删除为死代码消除而非行为变更。新增 `TestNopMetaQualityResultApprove` 2 例：approve 无副作用返回实体（status/isFalsePositive/version 三列不变钉死）+ 对照 reject 真实字段变更（isFalsePositive=1）；`TestNopMetadataWorkflowModels` 7/7 绿。**reject 同族核对**：`setIsFalsePositive(1)` 有真实字段变更（:44-46），不在本项范围。Owner-doc 核对：nop-metadata.md 质量结果审批段无 approve 语义表述（:196 仅方法清单）→ 无需同步。**编译器坑（记录）**：`assertEquals(1, byteValue, msg)` 触发 JUnit 重载歧义（Integer,int,String vs int,int,String）→ plexus JavaxTools in-process 编译以 ConcurrentModificationException 掩盖真实编译错误（需 `-Dmaven.compiler.fork=true` 才见真错），修复 = `.intValue()` 显式展开。
- **Phase 6 Decision（P2-25 @RequestBean 例外裁定落档）**：owner doc API 契约段（F4 selection no-op 段之后，沿 :161/:163/:165 既有"例外裁定"表述先例）新增段落：`queryJoinData`（6 参）/`queryAggregation`（10 参）超出 service-layer.md 1–5 参数规则为**显式裁定例外、签名不迁移**——两方法为已发布对外 GraphQL 契约（AR-09/F4 裁定落点，limit/having/orderBy 拒绝点固定 BizModel 入口参数），迁移 @RequestBean 破坏契约兼容。**机制复核**：5 参数规则 owner 文档（service-layer.md:70-71，api-and-graphql.md 无该规则表述）无既有"裁定例外"登记机制 → 按 plan 备选路径在 nop-metadata.md 局部登记。**doc-links 计数**：改动前 17 / 改动后 17（0 新增；17 全部为既有 pre-existing，位于其他 mission 归属文件——沿 0226-1/0226-2 收口同基线）。纯文档 Phase：No new test required（documentation-only adjudication）。

### EXEC_PLANS Cycle 3 / 执行顺序 2 — plan 2026-08-16-0226-2 错误处理/诊断族（P2-09/P2-10/P2-13/P2-14/P2-23 + P2-11 裁定）

plan `ai-dev/plans/2026-08-16-0226-2-nop-metadata-error-code-declare-and-form-cleanup.md`（mission-driver EXEC_PLANS；5 Phase + Closure）。

- **Phase 1 Fix（P2-10 define 声明面一致性 + 死码）**：
  - **门禁扩展**：`check-error-param-consistency.mjs` 新增 define 面两规则——(1b) 对称差：define 尾部 ARG_* 常量**值**（经注册表解析，禁常量名比对）与描述占位符集合对称差为空；(1c) 死码：define 在模块 src/main 剥离注释语料（排除定义文件）零引用即红，分 `dead`（全仓零引用）/`test-only`（仅测试镜像保活）两kind，repo 级复核语料排除本模块 test（防 test 引用误判 alive-elsewhere）；豁免走 define 行 `// invariant-ok:`（豁免清单形态沿 check-silent-wrong-result baseline 先例），输出显式列出。fixture 自检 +2 段（对称差三形态 + 死码三分类），全绿。
  - **重扫清单（2026-08-16 live）**：17 对称差（LineageErrors 9 / SqlErrors 6 / ModuleErrors 2）+ 7 死码（dead 4：`ERR_CHECKPOINT_NOT_FOUND`/`ERR_QUALITY_TABLE_NOT_EXTERNAL`/`ERR_QUALITY_NO_DATASOURCE`/`ERR_RECON_CONFIG_NOT_FOUND`；test-only 3：`ERR_AGGR_TYPE_PROBE_FAILED`/`ERR_PROFILING_RULE_OPERATION_FAILED`/`ERR_MODULE_NOT_FOUND`）。基线预期 17+2，重扫多出 5 个死码（计划 Phase 1 明示"如重扫发现其他死码一并处理"）。
  - **17 处修复取向**（全部"现场真实值优先"= throw 点实传键 → 描述补占位符；throw 点均已在传参、渲染面此前永不可见）：LineageErrors 8 处补 `: {sql}` / `size={size}` / `index={index} edge={edge}` 等；`ERR_LINEAGE_GRAPH_TOO_LARGE` 为第三形态（desc `{edges}` vs 声明 `ARG_EDGE` 值 `edge`，两侧都有但键值不同）→ 声明改 `ARG_EDGES`（`NopMetadataArgs` 新增 `ARG_EDGES = "edges"`，与 throw 点 `.param("edges",…)` 对齐）；ModuleErrors 2 处补 `metaModuleId={metaModuleId}`；SqlErrors 6 处补 `{sql}` / `querySpace={querySpace}`。**第三形态核对结论**：17 处中仅 GRAPH_TOO_LARGE 一例，已收敛；无其他形态。
  - **7 死码删除 + 引用同步**：各定义删除 + 全仓 rg 留证（dead 4 = 仅定义 + Javadoc `{@link}` 陈旧引用；test-only 3 = 定义 + 测试镜像/构造器测试）。引用同步：`TestSilentSwallowFormalization` 删 2 个 `Arguments.of` 镜像项（其注释所指 catch 实际用 `ERR_AGGR_DB_PRODUCT_NAME_FAILED` 等其他码——镜像 stale）；`TestNopMetadataErrorsCentralized` 构造器测试换 `ERR_DATASOURCE_NOT_FOUND`；4 处 BizModel Javadoc 陈旧 `{@link}` 改为如实描述（`requireEntity` 平台 not-found / `ERR_TABLEREF_*` 系列 / D4 扩展后执行范围），`NopMetaQualityRuleBizModel` 类 Javadoc D1→D4 表述同步。**核对**：docs-for-ai 错误处理段无被删死码引用（`rg` 复核零命中）。
  - 验证：门禁 define 面零命中（24→0）+ throw 面零命中 + fixture 全绿；`./mvnw compile -pl nop-metadata -am -T 1C` 通过；`TestNopMetadataErrorsCentralized` 6/6 + `TestSilentSwallowFormalization` 29/29；checkstyle 直跑为既有全仓未接入门禁状态（未新增类别，见收口）。
  - Doc-sync: `ai-dev/audits/nop-metadata-invariants/invariant-catalog.md` INV-ERROR-PARAM ④/③/覆盖率更新（define 面扩展 + P2-10 土壤划除）；`docs-for-ai/03-modules/nop-metadata.md` INV-ERROR-PARAM bullet 增 define 面规则表述。
- **Phase 2 Fix（P2-09 `{error}` 附注参数补齐 + 门禁豁免收口）**：
  - **live 重扫定稿**：`-- {error}`/`error={error}` 描述族 main 范围 32 处 define；throw 点缺 `error` 键实测 7 处（与基线一致，无漂移）：AutoClassificationProcessor:290、LineageTagPropagationProcessor:204、NopMetaModuleBizModel:349/:689、NopMetaTagLabelBizModel:138、MetaModelChangedEventPublisher:167、AggregationHelper:543。
  - **7 处全部补参**（各点均有 in-scope cause）：`.param(ARG_ERROR, NopMetadataHelper.toErrorMessage(e))`（bare 静态导入 3 处 / qualified 3 处）+ AggregationHelper 用文件内既有 `messageOf(e)` 等价形态（对齐同文件 :117/:490 先例；SQLException 语义等价）。附带 NopMetaModuleBizModel:689 的 `.param("path",…)` 字面量顺手升格 `ARG_PATH` 常量（同语句内、零行为差）。**裁定项未触发**：无需"删 `-- {error}` 尾"的点（复核 7 处均有 throwable）。
  - **门禁豁免收口**：`EXEMPT_PLACEHOLDERS` 清空 + 头注释 adjudication (b) 改写为"已收口"表述；fixture `error-placeholder-exempt`（expectHits 0）改 `error-placeholder-missing-param (P2-09 closed)`（expectHits 1，语义 = 豁免已收口、缺参即命中）；`--fixture` 自检全绿；`--module nop-metadata` exit 0（`{error}` 形态进入 hard-gate 覆盖）。
  - **focused 测试**（1913-3 "renders real value + no literal placeholder" 先例形态）：新增 `TestAggregationHelperErrorParam`（checkTableExists 元数据故障 → errorCode + cause 保留 + `getParam("error")` 真实消息 + 消息渲染真实值无字面 `{error}`；P2-08 已建连面裁定核对——SQLException 消息不含连接 URL，无脱敏新增面）；`TestNopMetaModelChangedEvent` 增 `testP209SerializationFailureRendersRealErrorParam`（buildSnapshot 循环引用触发路径：error 参数非 null 空壳 + 无字面占位符 + 身份值照常渲染）。其余 5 点（private/容器依赖路径）由门禁静态守护全覆盖（豁免已收口后任何回归即红）。
  - 验证：`./mvnw test -pl nop-metadata/nop-metadata-service -am -T 1C` BUILD SUCCESS（0 failures）。
  - Doc-sync: `docs-for-ai/03-modules/nop-metadata.md` guard 6 表述（豁免收口）；`invariant-catalog.md` INV-ERROR-PARAM ① 豁免面表述。
- **Phase 3 Fix（P2-13 SQLException 哨兵消除）**：`MetaQualityRuleExecutor` judgeRange/judgeRegex 的 `throw new SQLException("…COUNT(*) returned no row")` 哨兵删除——COUNT(*) 无行改内联显式分支：直接产出 ERROR 判定 + 一条无 throwable 的显式 ERROR 日志（真实 SQLException 仍走既有 catch 不变）。**行为等价对照记录（按计划明确定义逐项）**：(1) judgment 逐字段——status ERROR 不变；message 逐字节等价（旧路径 `messageOf(哨兵)` = "range/regex COUNT(*) returned no row"，新路径硬编码同串，最终 message 均 `…[nop.err.metadata.quality-rule-exec-isolated] range/regex COUNT(*) returned no row`）；details（ruleType/entityType/tableName/tableType/min/max/pattern）不变；actualValue/expectedValue 两版均未设置。(2) 日志——同 logger（MetaQualityRuleExecutor LOG）、同级别 ERROR、同模板 `range/regex SQL execution failed: errorCode={}`、同消息参数（errorCode）；**堆栈消失为预期变化**（旧堆栈来自哨兵异常对象本身，新路径无该对象、不伪造）。(3) regex no-row 不经 `isRegexpUnsupported`（旧路径哨兵消息不匹配方言签名同样落入 ERROR 分支，等价）。**验证**：`rg 'throw new SQLException' nop-metadata-service/src/main` 零命中；新增 `TestMetaQualityRuleExecutorNoRowBranch` 2 例（range/regex no-row → status=ERROR + message 逐字节断言 + details 身份参数 + 不设 SKIP reason；现状此前无直接覆盖）；`TestMetaQualityRuleExecutor*` 全族 50/50 绿。**Owner-doc 核对**：nop-metadata.md 质量规则段无哨兵表述引用（`rg 哨兵|returned no row` docs-for-ai 零命中）→ No owner-doc update required。
- **Phase 4 Fix（P2-14 WARN 异常末参 + P2-23 死码删除）**：
  - **P2-14**：`MetaQualityCheckpointScheduler` 并发拒绝 WARN 追加 `, e` 末参（对齐同文件 :226-228 ERROR 分支与 R4.3 形态；留 P2-14 注释）。新增 `testP214ConcurrentRejectionWarnCarriesThrowable`（沿 `testConcurrentExecuteCheckpointFailFast` 阻塞 webhook 钉锁 harness + R6.5 ListAppender 先例形态）：断言 WARN 事件含 `scheduled-exec-skipped` + checkpointId + **`getThrowableProxy() != null`**（异常对象真实出现在 WARN 事件）、不误升 ERROR、`executeScheduledCheckpoint` 返回错误结果非异常上抛（MA7.5-01 job 存活语义不变）。
  - **P2-23**：`NopMetaReconciliationResultBizModel` `toInt`/`toStr` 死方法 + 2 处不可达 throw 分支删除；2 处 `// invariant-ok: dead code P2-23` 豁免注释随码消除（`rg` nop-metadata/ 零命中）。**连带处置留证**：删后全仓 `rg ERR_RECON_INVALID_SELECTION -g '*.java'` = 仅 ReconErrors.java:42 定义（预期"定义 + 死码 2 处"中的死码已删）→ 定义随之删除；`ARG_VALUE` 孤儿核对 = 全模块仅自身声明 + 被删 define 尾参 2 处 → 一并删除（NopMetadataArgs）。测试引用核对：无测试引用被删码/方法（TestNopMetaQualityScoreBizModel 的 toInt 是测试自身 helper，无关）。
  - 验证：`./mvnw test -Dtest='TestNopMetaReconciliation*,TestMetaQualityCheckpointScheduler*,TestSilentSwallowFormalization,TestNopMetaQualityCheckpointBizModel'` 95/95 绿；门禁 `--module nop-metadata` exit 0（豁免面 6→4：P2-23 两处 allowed 命中随死码消失，剩 4 处变量形态既有裁定）。
- **Phase 5 Decision（P2-11 双轨制显式裁定，无代码变更）**：复核实测（收口时点 live，`-o` 单行口径）= 字面量键 580 vs ARG 常量键 226（qualified 208 + bare 18；总 `.param(` 808，差值 2 为跨行键形态——门禁语句级解析覆盖非盲区）；键值一致性守护复核 = 门禁 `resolveKeyExpr` 对两形态统一解析键值后比对占位符，错配即红（fixture `key-mismatch` 样例钉死区分力）。裁定写入 plan Deferred 段（classification = optimization candidate + Why Not Blocking + Successor=no + 复核实测数据）+ roadmap P2-11 条目标注 ⚖️ 已裁定 deferred。本计划增量零恶化（Phase 2 新增 7 处 `.param` 全部 ARG 形态 + 1 处字面量升格）。
- **收口（closure audit approved）**：**独立 closure audit**（fresh session `ses_ff8e879f2ffeovVN46STUAjznb`，review-only 零文件修改）**20/20 检查全 PASS**（门禁/fixture/17 define 逐点/8 死码 rg/7 补参 file:line/哨兵零命中/WARN 末参/P2-23 三 grep/focused 测试 surefire 记录/roadmap 5 条/owner docs/daily log），verdict `CLOSURE_AUDIT: approved`。Anti-Hollow 注毒双向验证（执行者）：临时注入 desc 去占位符 + 死 define → 门禁 exit 1（2 define-face + 1 dead-define 命中），恢复 → exit 0。Closure Gates 全绿：`check-error-param-consistency --module` exit 0（含 fixture PASS）、`check-silent-swallow` exit 0（125 catch 0 命中）、`scan-hollow --severity high` exit 0、`check-plan-checklist --strict` exit 0（1 passed）、doc-links 17 errors = 既有基线（其他 mission 归属文件 + 既有 BOUNDARY 提示，本计划改动文件 0 新增）、`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（service **1251/0/0**，0226-1 基线 1248 + 净 3：+5 新测试 − 2 参数化镜像 invocation）。Plan Status → `completed`（Completed: 2026-08-16）；roadmap P2-09/10/13/14/23 ✅ Fixed + P2-11 ⚖️ 已裁定；门禁头注释 + invariant-catalog + nop-metadata.md + run-nop-metadata-invariants.sh guard 6 表述同步。checkstyle 仓库权威 qa 配置直跑为既有未接入门禁状态（nop-api-core 9164 条 / service 全文件既有违规，非本计划引入；本计划行均沿所在文件既有风格）。

### EXEC_PLANS Cycle 3 / 执行顺序 1 — plan 2026-08-16-0226-1 安全/脱敏/防御纵深族 4 Phase 全部落地

plan `ai-dev/plans/2026-08-16-0226-1-nop-metadata-defense-depth-and-redaction-consistency.md`（mission-driver EXEC_PLANS；P2-06/P2-07/P2-08/P2-04 族批次清扫，3 Fix + 1 Decision/Proof）。

- **Fix（P2-06 webhook 主机形状 fail-closed）**：`CheckpointActionDispatcher.validateWebhookUrl` 对 `extractWebhookHost` 提取结果新增 `isPlausibleWebhookHostShape` 形状校验（沿 JDBC 侧 F7 `isPlausibleHostShape` 先例的 webhook 适配）——null/空串（空 authority、未闭合 IPv6 括号）、`/` 前缀垃圾串（`https:///hook`→`/hook`）、纯端口（`https://:8080/hook`→`:8080`）、`(` 前缀、`%` 残片显式抛 `ERR_CHECKPOINT_WEBHOOK_URL_BLOCKED`（reason="implausible host shape"），不再静默跳过校验分支（原为 lucky fail-closed，与 F7 防御纵深不对称）。合法向量判定结果零变化。新增对抗测试 2 例（`testMalformedHostShapeFailClosed` 6 畸形向量逐个断言 + `testP206LegitVectorsUnchanged` 5 合法向量零误伤）。
- **Fix（P2-08 驱动消息脱敏）**：`MetaDataSourceConnectionProcessor` 新增 public static `redactJdbcUrlsInText`（`jdbc:\S+` 匹配（大小写不敏感）+ 尾部收尾标点 `.,;!?)]}'":}` 剥离 + `redactJdbcUrl` 脱敏回填；无命中原样返回）。接入点 3 处：① `newNopConnectException`（建连点）；② `MetaQualityRuleExecutor.messageOf` 收敛（单点覆盖该文件 6 个 messageOf 面：judgeCustomSql ERROR/judgeRange ERROR/judgeRegex SKIP+ERROR/queryLong/queryTimestamp）；③ `parseConnectionConfig`——实测发现 Nop JSON 解析器 readerState 上下文窗回显凭据载体输入的**中段片段**（含口令但无 `jdbc:` 前缀锚定，jdbc: 锚定过滤不可覆盖），故该面采用**抑制回显**处理（reason 不带解析器消息，原始异常经新增 cause 参数保留供日志诊断；前置断言钉死泄漏面成立防测试空转）。
- **P2-08 枚举裁定清单**（`rg 'getMessage\(\)' nop-metadata/.../main` 全量 24 站点逐个裁定）：**接入** = newNopConnectException / MetaQualityRuleExecutor.messageOf（上列）+ parseConnectionConfig（抑制+cause）；**排除—非输出面** = `MetaQualityRuleExecutor.isRegexpUnsupported:805`（消息仅用于方言签名匹配）、`MetaTableProfiler:282`（connection/permission 分类匹配）、QualityRuleJudgment.getMessage 等 6 处 DTO/行映射面（judgment message 已在 messageOf 过滤后产生）；**排除—已建连面（URL 回显不可达）** = `ExternalTableStructureReader:87/:125`、`TableReferenceExecutor:85/:93/:100/:117/:124/:136`、`AggregationHelper:495`、`DefaultFilterApplicator:86`、`MetaTableQueryExecutor:160`、`MetaJoinExecutor:726`、`SqlViewFieldTypeInferrer:202`、`MetaTableProfiler:665`、`NopMetadataHelper.toErrorMessage:39`、`NopMetaReconciliationConfigBizModel:139`、`MetaContractChecker:470`——消息源 = 已建连查询/元数据 SQLException（驱动查询错误回显 SQL 文本不回显连接 URL；URL 回显仅发生在建连失败，该路径统一经 newNopConnectException 接入过滤）或 NopException 包装（toErrorMessage 12 个调用方实核：建连失败均为已过滤参数的 NopException）；SQL 文本在 error param 的可见性属 AR-16 邻界既有裁定。**已知边界记录**：`redactJdbcUrl` 只剥 authority userinfo——query 形态口令（`?user=x&password=y`）与无 `://` 的 Oracle thin 形态不在脱敏范围（F6 既有裁定；`testRedactJdbcUrlsInTextQueryFormBoundaryDocumented` 钉死防第三轮审计误报）。新增测试 11 例（`TestMetaDataSourceConnectionMessageRedaction` 8：helper 对抗/尾标点/多 URL/边界 + stub 驱动接线（注册接管 `jdbc:postgresql:`，test classpath 无 PG 驱动零冲突）+ 零误伤 + parseConfig 抑制；`TestMetaQualityRuleExecutorMessageRedaction` 3：custom_sql ERROR 面 / queryLong ERROR param 面 / 零误伤）。
- **Fix（P2-07 custom_sql 持久化面对齐）**：删除 `judgeCustomSql` 的 `j.getDetails().put("sql", sql)`——SQL 全文权威存储在规则本体（`NopMetaQualityRule.sqlExpression` 列优先 / params.sql JSON），details 只保留 sqlHash，与 AR-16 日志面"只记 sqlHash"同口径。消费面核对：`rg 'put\("sql"' main` 修复前仅此 1 点；`rg '"sql"' test` 无断言 details.sql 的测试（SqlSelectFieldExtractor 的 `.param("sql")` 属另一组件）；`rg 'get\("sql")'` 零消费；meta 层 `details` 为通用 JSON 列无 `sql` 专属消费。新增 `TestMetaQualityRuleExecutorCustomSqlDetails` 5 例：PASS/FAIL/ERROR 三路径 details 无 SQL 原文（含敏感字面量断言）+ sqlHash 与规则本体对账（sqlExpression 优先 / params.sql 回退双路径，并断言 sqlExpression 优先性不被 params.sql 抢占）。
- **Decision/Proof（P2-04 KEYWORD_BLACKLIST 函数形态裁定显式化）**：26 条目逐条枚举核对（2026-08-16 live）：callable 函数同形词仅 REPLACE/TRUNCATE/INSERT（字符串替换/数值截断/MySQL INSERT(str,pos,len,newstr)）；其余 23 条目（DROP/CREATE/ALTER/RENAME/UPDATE/DELETE/MERGE/GRANT/REVOKE/COMMIT/ROLLBACK/SAVEPOINT/SET/TRANSACTION/INTO/OUTFILE/DUMPFILE/CALL/EXEC/EXECUTE/SHUTDOWN/LOCK/UNLOCK）均为纯语句/子句/锁关键字（GET_LOCK/RELEASE_LOCK 是不同名函数条目，已在 FUNCTION_BLACKLIST）。裁定写入 `KEYWORD_BLACKLIST` + `scanBlacklist` javadoc（FUNCTION_CALL 只查 FUNCTION_BLACKLIST 是有意设计：函数形态合法、语句形态以 IDENTIFIER token 命中、表达式上下文无 DML/DDL 逃逸；明示勿加交叉检查）；钉死测试 3 例 9 向量（`testP204FunctionHomographsAllowed` 3 函数过 / `testP204StatementFormsStillRejected` 3 语句拒 / `testP204FunctionBlacklistStillRejected` SLEEP/BENCHMARK/LOAD_FILE 拒），均走 `validateStatic` 生产入口；invariant 记录补入 `invariant-catalog.md`（增补注记节）。不改黑名单、不改扫描逻辑。
- 偏差：Phase 2 parseConnectionConfig 落点由"jdbc: 过滤"改为"抑制回显 + cause 保留"（计划留有逐面裁定空间；实测 Nop 解析器回显无锚定片段，过滤语义不足，抑制是该面更强的一致性处理）；其余无偏差。
- 验证：`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（service **1248/0/0**，1913-3 收口基线 1227 + 21 新增）；`./mvnw compile -pl nop-metadata -am -T 1C` 通过；聚焦套件 `TestCheckpointActionDispatcher*` 39/39、`TestMetaDataSourceConnection*` + `TestMetaQualityRuleExecutor*` 111/111、validator 32/32、checkpoint/rule/result BizModel 集成链 65/65 全绿。Closure Gates：check-silent-swallow 0 命中（126 catch 块）、check-error-param-consistency exit 0（豁免面 6 条全部既有）、check-sensitive-literal-leak exit 0、scan-hollow --severity high exit 0、check-doc-links --strict 17 errors（= 08-16 前基线 17，全部位于其他 mission 归属文件 + 既有 BOUNDARY 提示，本计划改动文件 0 新增）。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md` 安全契约段 3 处新增（P2-06 webhook 形状校验 bullet、P2-08 驱动消息脱敏 bullet、F9 bullet 增补 P2-07 持久化面 sqlHash 表述）；`ai-dev/audits/nop-metadata-invariants/invariant-catalog.md` 增补 P2-04 裁定注记。

### DRAFT_PLANS — Cycle 3 后续 P2 批次清扫三计划起草并过审（active）

mission-driver DRAFT_PLANS 轮：roadmap Work Item 全 done，从 2026-08-15 multi-audit Follow-up Backlog（35 项 P2）按族拣选下一批 3 个计划（同 1133/1448 族批次清扫先例），并承接前序计划显式移交的 deferred 项（1913-1→P2-06/P2-08、1913-2→P2-21、1913-3→P2-09/P2-10/P2-11/P2-23）。

- **`2026-08-16-0226-1` 安全/脱敏/防御纵深族**（P2-06/P2-07/P2-08/P2-04）：webhook 主机形状 fail-closed（F7 对齐）、custom_sql details 持久化面 AR-16 对齐、驱动消息脱敏过滤（messageOf 统一落点）、KEYWORD_BLACKLIST 函数形态裁定钉死。
- **`2026-08-16-0226-2` 错误处理/诊断族**（P2-09/P2-10/P2-13/P2-14/P2-23 + P2-11 裁定）：7 处 `{error}` 补参 + 门禁豁免收口、define 面 17 处不一致 + 2 死码 + define 面规则沉淀入 INV-ERROR-PARAM 门禁、SQLException 哨兵消除、WARN 末参、Reconciliation 死码（含 `ERR_RECON_INVALID_SELECTION` 连带处置）。
- **`2026-08-16-0226-3` BizModel 行为/契约形态族**（P2-18/P2-19/P2-21/P2-22/P2-24/P2-25）：DataProduct JsonTool、6 处 save override 提前委托防护、computeQualityScore 恢复 defaultPrepareSave（xbiz delta 接线测试）、testConnection @BizQuery（含 action-auth 授权面核对）、approve 语义诚实化、@RequestBean 例外裁定落档。
- **两轮独立对抗性审查至共识**（每计划两个 fresh session，review-only）：第一轮 3 Blocker + 9 Major（P2-23 幻影活点、getThisObj 分发机制与接线测试冲突、P2-06 畸形 host 行为误述、INSERT 第三函数同形词、门禁 fixture 同步缺口、3 条不可判定 rg 命令、授权面维度缺失等）全部修复；第二轮 3 计划均 approved（残余 Minor 已顺手落：26 条计数、delta 路径/`<action>` 标签提示、fixture 期望翻转清单等）。三计划 Plan Status → `active`（执行顺序 1→2→3，N 前缀序）。
- 未覆盖（留后续轮次）：P2-05/P2-33 产品级裁定项、P2-12 i18n（ask-first）、P2-01/26/27/28/29/34 ORM 族（模型变更需人工确认）、P2-02/03/30/31 IoC 族、P2-15/16/17/20/32/35 文档测试卫生族。

### EXEC_PLANS Cycle 3 / 执行顺序 3 — plan 2026-08-15-1913-3 收口（closure audit approved，全部 3 Phase + 11 Closure Gates 完成）

plan `ai-dev/plans/2026-08-15-1913-3-nop-metadata-error-param-diagnostics-and-guard-test.md` 收口（mission-driver EXEC_PLANS；3 Phase 落地见上列条目，本条目记录收口 pass）。

- **独立 closure audit**（fresh session `ses_ff9667230ffefnp26Et6dzLiWD`，review-only 零文件修改）：**verdict approved——13 项检查全 PASS**（Phase 1 3/3、Phase 2 3/3、Phase 3 3/3、Closure Gates 4/4）；逐点 live 核对 11 活点 + 1 增量 + 3 变量形态修复（`Set.of()` 非 null 空壳、MetaJoinExecutor:210 joinId 不回退、预裁定约束成立）；Anti-Hollow 双向证实（fixture 注毒样例 + live 删参变红记录；3 代表点测试正/负断言非恒真）；Deferred 诚实性（P2-09/P2-10/P2-11/P2-23 均计划内 Non-Goal，无 in-scope defect 降级）。
- **审计附注处置（4 项非阻塞）**：① 收口机械项随本条目完成（11 Closure Gates 全勾 + Closure Evidence 写入 plan）；② 改动集随本收口提交（排除无关 `nop-format/.../.rels`）；③ 全量 `-am` 测试由执行者复跑记录：BUILD SUCCESS（service 1227/0/0；曾现一次 nop-stream 时序基准负载竞态 flake——0 个 nop-stream 文件被改动，复跑绿，非本计划回归）；④ 行号轻微漂移均为语义无损（审计逐点按语义核对）。
- 收口机械项：Plan Status → `completed`（Completed: 2026-08-16）；`check-plan-checklist.mjs --strict` 复跑 exit 0（1 passed / 0 failed）；`scan-hollow-implementations.mjs --module nop-metadata --severity high` exit 0（曾误报 2 条——hollow 扫描器 P6b 正则匹配注释里的英文 "placeholder" 一词，改用中文"占位符"表述后归零，非豁免）；checkstyle 仓库权威 qa 配置逐条比对 16 改动文件 HEAD vs now 违规条目完全一致 = 零新增；doc-links 17 errors 全部位于其他 mission 归属文件（本计划文件 0 命中；前序收口记录为 16，+1 亦非本计划文件）。
- **范围外红点留痕**：Phase 3 全链首跑暴露 guard 1 先在红（`MetaDataSourceConnectionProcessor.percentDecode:529`，plan 1913-1 提交 `1c02c8df7` 引入、该计划收口未重跑聚合链）——沿 INV-SILENT-SWALLOW benign-miss 先例修复（DEBUG 日志 + ErrorCode 上下文 + cause，null 返回语义不变），`TestMetaDataSourceConnectionSecurity` 50/50 无回退。**教训**：守卫链应在每个 plan 收口时复跑（本计划 Phase 3 exit criteria 已把全链复跑钉为必过项，先例已补）。
- **源审计状态说明**：`2026-08-15-0559-multi-audit`（Audit Status: planned）两个后继计划（1913-2/1913-3）现均 completed——其状态变更留待 mission 审计收口流程处理（本计划 front matter 无 `> Source Audits:` 行，按 mission 约定未改动）。
- Doc-sync: roadmap Cycle 3 执行顺序 3 工作项 🔄 → ✅ `done`（含收口摘要）。

### EXEC_PLANS Cycle 3 / 执行顺序 3 — plan 2026-08-15-1913-3 Phase 3 P1-9 守卫修复 + INV-ERROR-PARAM 门禁接入聚合链完成

plan `ai-dev/plans/2026-08-15-1913-3-nop-metadata-error-param-diagnostics-and-guard-test.md` Phase 3（mission-driver EXEC_PLANS）。

- **Fix（P1-9 假绿行）**：`TestLimitNegativeValueInvariant` 方法表 4 行**全部**钉精确错误码（第三列 expectedErrorCode：3 反射行 `nop.err.metadata.pagination-limit-invalid`、inline 行 `nop.err.metadata.search-limit-invalid`）——取"各自钉码"分支（比仅 inline 行钉码区分力更强，成本同为 1 列参数）；`assertEquals(expectedErrorCode, ex.getErrorCode())` 断言。
- **Proof（变异验证，live repo）**：临时移除 `NopMetaSearchBizModel.searchMetadata` 负 limit 检查 → **该测试行变红**：`expected: <nop.err.metadata.search-limit-invalid> but was: <nop.err.metadata.search-engine-unavailable>`（与 plan Current Baseline 预测的漂移路径逐字一致——limit 检查前置于 searchEngine==null）；**其余 3 行不受影响**（1 failure 仅 searchMetadata 行）；恢复后 4/4 全绿。修复前该变异对宽断言假绿（区分力零）。
- **Proof（guard 6 接线）**：`check-error-param-consistency.mjs` 接入 `run-nop-metadata-invariants.sh` 为第 6 个 guard（fail-fast 链 5→6，脚本头注释 guards 清单同步）；`.github/workflows/maven.yml` 无 guard 枚举（invariant-gate job 直接调用脚本，`maven.yml:45-73` 核对）——无需同步。全链 **6 guards exit 0**。
- **[范围外发现并修复（链条红阻断，记录留痕）]**：guard 1（INV-SILENT-SWALLOW）首跑命中 `MetaDataSourceConnectionProcessor.percentDecode:529`（catch IllegalArgumentException → return null）——**先在缺陷**（plan 2026-08-15-1913-1 Phase 1 提交 `1c02c8df7` 引入，本 plan 首次全链复跑暴露；该计划收口时未重跑聚合链）。修复沿扫描器自身 benign-miss 形式化先例（`AggregationHelper.toBigDecimal`，plan 2026-08-14-1448-2）：DEBUG 日志携带 `ERR_DATASOURCE_JDBC_URL_BLOCKED` 错误码上下文 + cause，null 返回语义不变（调用方 fail-closed 契约保持）——guard 1 恢复零命中，`TestMetaDataSourceConnectionSecurity` 50/50 无回退。
- **catalog 登记**：`invariant-catalog.md` 新增「Cycle 3 增补 — error-param 族」节：INV-ERROR-PARAM 条目（①陈述：识别性占位符↔`.param` 键一致性 + 禁 null 空壳/禁静默放过变量形态；②覆盖失败族：P1-6 11 活点 + P1-7 键错配；③证据：源 audit 路径 + 三轨修法 + :216 先例 + P2-10/P2-11 土壤说明；④检测方法 + 342/226 目标集覆盖率 + 注毒自验）+ INV-LIMIT P1-9 假绿教训增补注记（"守卫测试自身须按'移除被守卫检查后必须变红'标准变异验证"）。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md` 错误处理节新增 INV-ERROR-PARAM 契约条目（占位符↔键一致性 + null 空壳禁止 + guard 6 指引）。
- No owner-doc update required（守卫/工具本身）；owner 契约叙述经上述 Doc-sync 覆盖。
- 验证：`./mvnw test -pl nop-metadata/nop-metadata-service -Dtest=TestLimitNegativeValueInvariant` 4/4 绿；`./ai-dev/tools/run-nop-metadata-invariants.sh` exit 0（6 guards）。

### EXEC_PLANS Cycle 3 / 执行顺序 3 — plan 2026-08-15-1913-3 Phase 2 P1-6+P1-7 识别性参数收口完成（三轨修法，权威分母驱动）

plan `ai-dev/plans/2026-08-15-1913-3-nop-metadata-error-param-diagnostics-and-guard-test.md` Phase 2（mission-driver EXEC_PLANS）。

- **Fix（P1-6 三轨修复，分母 = audit 11 活点 ∪ 清扫增量 :106 ∪ 变量形态 3 点）逐点归类**：
  - **轨 1 补齐（上下文有值）**：`NopMetaTableJoinBizModel:164`（update 路径 joinId 自 data map 经 `validateJoin(data)` 重构下沉穿参——validateJoin/validateJoinSide/validateTableEndpoint 链路；create 路径换码，见轨 3）；`MetaTableFieldResolver:383`（增强 for 改下标循环，`elementIndex=i` 传入）；`MemoryFilterEvaluator:86`（补 `.param("name", node.getAttr(FILTER_ATTR_NAME))`，沿同方法 default 分支先例）。
  - **轨 2 穿参（静态工具多跳签名）**：`MetaTableQueryExecutor.executeQuery`（+metaTableId 参数，`NopMetaTableQueryAction:124/:145` 两调用方传 `table.getMetaTableId()`）；`AggregationHelper.requireName`（+metaTableId，3 调用方 EntityAggregationProcessor:58 / EntityEntityJoinAggregationProcessor:59-60 传入）；`CrossDbJoinMerger.firstNonNullKeyType`（+joinId，调用方 `verifyCrossDbKeyTypeConsistency` 传 `join.getJoinId()`）。
  - **轨 3 换码（null 防御/值语义不存在，新增 5 个定点错误码）**：`MetaTableFieldResolver:84`→`ERR_FIELD_RESOLVE_TABLE_NULL`（零占位符）；`:106`（清扫增量）→`ERR_FIELD_RESOLVE_ENTITY_ID_NULL`（零占位符）；`:348/:355/:359`→`ERR_FIELD_RESOLVE_EXTERNAL_BUILD_SQL_UNPARSEABLE`（{metaTableId}，cause 保留）；`MetaTableReferenceResolver:87`→`ERR_TABLEREF_TABLE_NULL`（零占位符）；JoinBizModel create 路径→`ERR_JOIN_TABLE_TYPE_NOT_ALLOWED_ON_CREATE`（metaTableId/side/tableId/tableType，无 joinId 占位符）。**削占位符裁定遵循**：上述被换码的原错误码均有其他已传齐 throw 点（ERR_FIELD_RESOLVE_TABLE_NOT_FOUND :307 端点点位、ERR_FIELD_RESOLVE_BASE_ENTITY_NULL resolveAllowedEntityIds 点位、ERR_FIELD_RESOLVE_EXTERNAL_BUILD_SQL_INVALID :367/:383 元素级点位、ERR_TABLEREF_UNKNOWN_TABLE_TYPE :99-101、ERR_JOIN_TABLE_TYPE_NOT_ALLOWED MetaJoinExecutor:210 joinId 传齐）——未削任何占位符，:209 影响面零变更。
  - **变量形态 3 点（:214/:223/:234）**：确认真实缺参并入分母后修复——entity 分支补 `allowedEntityIds`（:214 实时解析 / :223 已持有）+ `availableFields=Set.of()`（PK 归属校验不适用 name-based 集，渲染 "[]" 非 null 空壳）；sql/external 分支补 `allowedEntityIds=Set.of()`（PK 归属语义不适用）。修复后仍为变量形态（errOnInvalid），经 `// invariant-ok:` 标注人工核对（throw 行上方注释块，扫描器支持前置行+连续注释块解析，fixture 3 样例钉死含"不泄漏到相邻 throw"负例）。
- **Decision（死点裁定）**：`NopMetaReconciliationResultBizModel:159/:165`（`toInt` P2-23 死码，throw 不可达）不补参数（对不可达代码补参无用户可见收益），`// invariant-ok: dead code P2-23, unreachable throw` 豁免标注；死码删除（backlog）后豁免自然消除。
- **Fix（P1-7 方案 A 落地）**：`ExternalTableStructureReader.requireSupportedProductName` 改传 `.param(ARG_DATASOURCE_TYPE, String.valueOf(productName))`——键与 `{datasourceType}` 占位符一致、值为被拒产品名；错误码不变（方案 B 否决理由成立：`TestExternalTableStructureReader:107` 精确断言保持绿）。`MetaDataSourceConnectionProcessor:216` 正确用法无回退（TestMetaDataSourceConnectionSecurity 50/50 绿）。
- **Proof（代表点回归，"renders real value + no literal {placeholder}" 模式）**：① `TestErrorParamRendering`（新增 3 例，GraphQL 端到端）：JoinBizModel create 路径（消息含 metaTableId/tableId/tableType 真实值 + 5 个占位符零字面残留）/ update 路径（joinId 真实渲染）/ Measure 不存在字段 PK（allowedEntityIds 真实渲染）；② `TestMetaTableFieldResolverBuildSql#testMissingColumnNameRendersRealElementIndex`（新增，原 :383 点位：消息含 `elementIndex=1`、无 `{elementIndex}` 字面）+ 既有 non-map 元素用例保持绿；③ `TestExternalTableStructureReader#testUnsupportedDialectStillDialectGateError`（升级：断言 `getParam(datasourceType)=="Oracle"` + 消息含 "Oracle" + 无 `{datasourceType}` 字面）；④ 既有 2 用例随 :348/:355 换码升级断言（UNPARSEABLE + metaTableId + cause 保留）。
- **Proof（Phase 1 扫描器复跑）**：`node ai-dev/tools/check-error-param-consistency.mjs --module nop-metadata` **exit 0**——活点零命中；豁免面 = `{error}` + 2 死点 + 4 变量形态标注（Allowed 清单 6 条全部显式列出，无静默盲区）；`--fixture` 15 样例 PASS（含前置行豁免/块解析/不泄漏负例）。
- 涉及测试更新（签名穿参涟漪，非削弱）：`TestExternalAggregationProcessor`/`TestSqlAggregationProcessor` requireName 3 参调用、`TestSqlPathLogRedaction` executeQuery 6 参调用。
- checkstyle：**以仓库权威 qa 配置（`checkstyle.xml`，`-Pqa`）逐条比对**：16 个改动 main 文件 HEAD vs now 违规条目（line+rule+message）完全一致 + 新增测试文件零违规 = **零新增**（附注：插件默认 sun_checks 口径下这些遗留 query 文件本就海量违规，非仓库门禁面；本次顺带把新增行全部收拢 ≤80 列）。validateJoin 重构为 `validateJoin(Map data)` 后全部方法参数 ≤7（qa ParameterNumber max=7）。
- Doc-sync（owner-doc 裁定）：`docs-for-ai/03-modules/nop-metadata.md` 错误处理叙述未列举具体错误码清单/占位符映射（检查确认无定点换码相关叙述），**No owner-doc update required**——定点换码均为新错误码新增（5 个）+ 参数补齐，无既有文档叙述漂移。
- 验证：`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（service 1227/0/0；一次并行跑出现 H2 竞态 flake，复跑稳定全绿）；聚焦 182 例（含新增 4 + 升级 3）全绿。

### EXEC_PLANS Cycle 3 / 执行顺序 3 — plan 2026-08-15-1913-3 Phase 1 INV-ERROR-PARAM 扫描器先行落地

plan `ai-dev/plans/2026-08-15-1913-3-nop-metadata-error-param-diagnostics-and-guard-test.md` Phase 1（mission-driver EXEC_PLANS）。

- **[Proof] 扫描器落地** `ai-dev/tools/check-error-param-consistency.mjs`（沿 `check-silent-wrong-result.mjs` 注释剥离 + `// invariant-ok:` 豁免机制、`check-sensitive-literal-leak.mjs` 文件遍历 + `--fixture` 自验模式）：对 nop-metadata main 范围 342 处 `new NopMetadataException(` throw 点，交叉 10 个 `*Errors.java` 子接口（226 个 ErrorCode define）描述占位符与 `.param()` 键（字面量 / 限定 `NopMetadataErrors.ARG_X` / 裸 `ARG_X` 三形态 + 裸 `ERR_X` 错误码常量 + 内联 `ErrorCode.define` + 局部变量 builder 形态 `X e = new ...; e.param(...)`）。
- **[Proof] 注毒自验（anti-hollow，live repo）**：临时删除 `MetaJoinExecutor.java:210`（`resolveTableEndpointOrThrow`，历史已修点）的 `.param("joinId", joinId)` → 扫描器变红（该点位命中 `{joinId}`）；恢复后该点位退出命中。`--fixture` 自验 13 样例全 PASS（含 P1-6 缺参形态、P1-7 键错配形态、`{error}` 豁免、变量形态 UNRESOLVED、builder 变量形态 complete/miss 双向、invariant-ok 豁免）。
- **[Proof] 全量首跑 red list（权威分母，2026-08-16 live 实测，exit 1 = 15 violations + 4 UNRESOLVED）**：
  - **audit 11 活点全部命中**：`NopMetaTableJoinBizModel:164`（{joinId}）、`MetaTableFieldResolver:84`（{metaTableId}，null 防御）、`:348/:355/:359`（{elementIndex}，值语义不存在）、`:383`（{elementIndex}，需改循环结构）、`MetaTableQueryExecutor:137`（{metaTableId}）、`AggregationHelper:135`（{metaTableId}）、`CrossDbJoinMerger:171`（{joinId}）、`MemoryFilterEvaluator:86`（{name}）、`MetaTableReferenceResolver:87`（{metaTableId}+{tableType}，null 防御）、`ExternalTableStructureReader:141`（{datasourceType} 键错配 = P1-7）。
  - **清扫增量 1 点**：`MetaTableFieldResolver:106`（`ERR_FIELD_RESOLVE_BASE_ENTITY_NULL` 声明 {metaTableId}，`resolveEntityFieldsByEntityId` 的 metaEntityId==null 分支无可传值——null 防御分支，归轨 3；注意同码 `:150-152`（resolveAllowedEntityIds）joinId…即 metaTableId 传齐，定点削占位符被 Non-Goal 边界禁止）。
  - **2 死点**：`NopMetaReconciliationResultBizModel:159/:165`（`toInt` 死代码，main 零调用）——Phase 2 经 `// invariant-ok:` 豁免裁定。
  - **假阳性排除实证**：`MetaDataSourceConnectionProcessor:306`（builder 变量形态：`e.param("jdbcUrl",...).param("reason",...)` 在后续 return 语句）——扫描器实现变量链 param 收集后正确退出命中（fixture 2 样例钉死该行为，含"builder 变量形态但零 param"的真实缺参仍命中）。
  - **UNRESOLVED 4 点人工归类**（变量形态错误码，静态不可解析）：`MetaTableFieldResolver:214`（传 3/5 参，缺 availableFields+allowedEntityIds）、`:223`（传 4/5，缺 availableFields）、`:234`（传 4/5，缺 allowedEntityIds）——调用方 `NopMetaTableMeasureBizModel:113`/`NopMetaTableDimensionBizModel:91` 传入 `ERR_MEASURE_FIELD_NOT_FOUND`/`ERR_DIMENSION_FIELD_NOT_FOUND`（各声明 5 占位符），**确认为真实缺参点并入分母**（修复后仍为变量形态，须 `// invariant-ok:` 标注人工核对）；`NopMetaLineageEdgeBizModel:115`（调用方传 `ERR_LINEAGE_SQL_PARSE_FAILED`/`ERR_COL_LINEAGE_SQL_PARSE_FAILED`，两 define 描述**零占位符**——"Failed to parse source sql[ for column lineage]"，任何 param 均为附加项）→ **误报，裁定非缺参点**，Phase 2 以 `// invariant-ok:` 标注映射核对结论。
- **[Decision] 退出模式裁定：零命中 hard-gate**（非 mode b 棘轮）。理由：修复后可达零未豁免命中——豁免面仅 (i) `{error}` 占位符（P2-09 附注族）、(ii) 死点 `// invariant-ok:`（随 P2-23 死码删除消除）、(iii) 变量形态点人工归类后的 `// invariant-ok:`（3 个已修复点标注"参数已补齐+映射核对"、1 个误报点标注"define 零占位符"）；未标注 UNRESOLVED 与新缺参点均红。无不可消除的已裁定豁免点，无需基线快照。
- 口径声明（脚本头注释）：catch 块 `e.param(...)` 重抛增补形态不命中（仅扫 `new NopMetadataException(` 构造点，上游构造点负责占位符覆盖）；`.param()` 键不可解析（非字面量非 ARG 常量）报 INTERNAL 违规不静默通过；语句括号不平衡报 INTERNAL 违规。
- No owner-doc update required: 工具脚本不影响 owner 行为（catalog 登记在 Phase 3）。
- 验证：`node ai-dev/tools/check-error-param-consistency.mjs --fixture` exit 0；`--module nop-metadata` exit 1（= 15 violations + 4 UNRESOLVED 预期 red，Phase 2 修复后复跑须 exit 0）。

### EXEC_PLANS Cycle 3 / 执行顺序 2 — plan 2026-08-15-1913-2 收口（closure audit approved，全部 4 Phase + 11 Closure Gates 完成）

plan `ai-dev/plans/2026-08-15-1913-2-nop-metadata-api-write-path-and-contract-semantics.md` 收口（mission-driver EXEC_PLANS；4 Phase 于 08-15 落地，见当日条目，本条目记录收口 pass）。

- **独立 closure audit**（fresh session `ses_ff9c297dbffe2xRinD8ba2zpeL`，review-only 零文件修改）：**verdict approved**——Phase 1-4 Exit Criteria 逐条 live 证据（6/6 + 4/4 + 4/4 + PASS）；11/11 Closure Gates PASS；审计者独立复跑 4 个新增/升级测试类 18/18 绿（执行者全量 `./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS，2026-08-16T00:23 全 reactor）；Anti-Hollow 双链 file:line 级证实：save→validator→entity 列→`testConnection`→真实 H2 `getConnection()`+`getDatabaseProductName()` 消费，`invoke("save")`→TagLabel save→`triggerApprovalIfNeeded`→`trySubmitForApproval`（fail-loud），旧直写 DAO 路径消失；checkstyle 9164 条基线违规零命中本计划 8 个改动 Java 文件；`scan-hollow-implementations.mjs` exit 0；Deferred 诚实性核对（P2-21/P2-33 真实 roadmap 登记、加密存储 §Deferred 裁定）无 in-scope defect 降级。
- **审计附注处置（4 项非阻塞）**：① Phase 4 doc-links 条目按裁定基线修订（实际 exit 1 = 16 pre-existing errors 全在其他 mission worktree 归属文件、本 plan 改动文件 0 命中，与 1913-1 收口记录一致）；② **08-15 log 勘误**：Phase 1 条目"既有 `TestAutoNopMetaDataSourceCrud` snapshot 无变化"表述有误——`nop_meta_data_source.csv` snapshot **已更新**（旧 snapshot 固化了缺陷：CONNECTION_CONFIG 恒空；新 snapshot 钉死 save 落库的配置值，属强化而非漂移）；③ 改动集随本收口提交；④ `nop-metadata-meta` 本地仓库 jar 陈旧（不含修复后 xmeta）导致不带 `-am` 跑新测试会复现修复前症状——以 `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` 刷新本地仓库消除陷阱。
- 收口机械项：Plan Status → `completed`（Completed: 2026-08-16）、11 Closure Gates 全勾、Closure Evidence 写入 plan 文件；`check-plan-checklist.mjs --strict` 复跑 exit 0。
- Doc-sync: roadmap `ai-dev/backlog/nop-metadata-invariant-loop-roadmap.md` Cycle 3 执行顺序 2 工作项 🔄 → ✅ `done`。
- 验证：`node ai-dev/tools/check-plan-checklist.mjs <plan-file> --strict` exit 0；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high` exit 0；`node ai-dev/tools/check-doc-links.mjs --strict` 16 errors = 既有基线（本 plan 文件 0 命中）。
