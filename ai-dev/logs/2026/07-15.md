### 2026-07-15 docs-for-ai 全目录准确性与一致性审计 + 修复

- 审计：4 个独立子 agent 并行核查整个 `docs-for-ai/`（约 120 文档），对全部 HIGH/SEV-1 发现对照源码二次复核
- 发现 7 条已复核严重错误 + 9 疑点 + 5 链接/格式问题；详见 `ai-dev/analysis/2026-07/2026-07-15-docs-for-ai-accuracy-and-consistency-audit.md`
- 修复（共 13 个文件）：
  - `orm-model-design.md`：domain 解析行为改为按属性分述（precision/scale 实为 column 优先，stdDataType 冲突抛异常）；`seq-default` 描述从"数据库自增"改为平台序列号引擎；MySQL VARCHAR 阈值 255→16383；`OrmCompositePKModel`→`OrmCompositePk`；VARCHAR precision 字节数断言改为 dialect 相关
  - `nop-auth.md`：OAuth 实体 `NopOAuth*`→`NopOauth*`，删 `NopOAuthUserConsent` 改为 `NopOauthAuthorizationConsent`
  - `nop-ai.md`：删不存在的 `nop-ai-llm`
  - `nop-code.md`：删 `nop-code-graph`，注明图能力在顶层 `nop-graph`
  - `INDEX.md`：`delFlag`→`deleted`，补 `bizmodel-method-selfcheck.md`/`async-service-guide.md` 路由
  - `project-context.md`：Java 版本按 pom 改为"构建 JDK 17+，编译目标 release 11"
  - 其余：`eql-and-database-compatibility.md`、`tenant-model.md`、`testing.md`、`rpc-and-distributed-rpc.md`、`ioc-and-config.md`、`reusable-modules-overview.md`、`build-approval-flow.md`
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` → docs-for-ai 范围 0 新增断链（剩余 2 条为误报：DSL `[备注](2)` + 检查器 `HEADING_ANCHOR_RE` 不支持 CJK 锚点）。错误总数（6）与修改前一致，全在 `ai-dev/`
- 断链修复：改进 `ai-dev/tools/check-doc-links.mjs` 消除误报根因——①新增 `maskCodeBlocks()` 跳过 fenced 代码块内的"链接"（修 `layout-syntax-reference.md` 的 `[备注](2)`）；②改用 `#`-based 锚点识别替代只支持 ASCII 的 `HEADING_ANCHOR_RE`（修 CJK 页内锚点 `#重新录制-output`）；③`isHistoricalFile` 补入 `ai-dev/lessons/`（与 logs/analysis/audits 一致）。结果：errors 6→0，`--strict` 退出码 0；剩余 73 条均为 `ai-dev/plans/` 的 warning（活跃计划缩写源码路径，检查器已主动降级）
- 偏差：Q5（module-groups.md 漏 10 模块）/Q7/Q8（子模块/实体清单补全）涉及策略未定，未强行补全，留作 open question
- Doc-sync: 本次即修改 `docs-for-ai/` 本身；新增 `ai-dev/analysis/2026-07/2026-07-15-docs-for-ai-accuracy-and-consistency-audit.md`

### 2026-07-15 Nop ORM 模型管理与 BI 元数据能力调研

- 完成主流元数据平台调研分析：阅读 Nop 现有文档（model-first-development.md、delta-customization.md、dql-query.md、platform-extensibility-mechanism.md），全面了解 Nop 现有 ORM 模型管理、Delta 定制和 DQL 维度查询能力
- 下载 6 个主流元数据平台到 `~/sources/`：OpenMetadata、DataHub、Amundsen、Apache Atlas、Marquez（Apache Superset 此前已有）
- 发现 Nop 的 DQL (QueryBean + MdxQueryExecutor) 已实现 BI 语义层核心对齐查询能力，但缺乏指标独立管理、维度层次、血缘追踪和可视化元数据目录
- 关键结论：Nop 的 Delta `x:extends` 机制是业界独有差异化优势，建议分 4 阶段构建 ORM 模型管理中心（模型可视化 → 版本管理 → BI 语义层 → 治理集成），新增 `nop-metadata` 和 `nop-bi` 模块
- 验证：N/A（调研分析 + 设计，非代码改动）
- Doc-sync: 新增 `ai-dev/analysis/2026-07/2026-07-15-nop-orm-model-management-and-bi-metadata-analysis.md`
- Doc-sync: 新增 `ai-dev/design/nop-metadata/` — 含 `README.md`、`00-vision.md`、`01-architecture-baseline.md`
- 设计决策：nop-metadata 放平台内（新建模块），接管 NopReportDataset 的职责；nop-report 退化到纯报表渲染引擎
- 核心设计：MetaModuleVersion 快照式版本管理、MetaView 替代数据集、ORM 模型字段级拆解存储、BI 语义层（MetaViewMeasure/MetaViewField.semanticType）
- 更新设计：明确 nop-metadata 是联邦式元数据管理，补充 MetaExternalDataSource + MetaExternalTable + MetaExternalTableField 实体，引入 QuerySpace + Driver 架构实现跨存储引擎查询（OrmDriver/JdbcDriver 等），明确 nop-dyn 是互补而非替代关系
- 补充 Delta 版本管理：每个模块版本增加 baseVersionId，ORM 模型导入分两次解析（不展开→isDelta=true / 展开→isDelta=false），UI 支持查看 delta 定义 / full 定义 / base 对比三种视图
