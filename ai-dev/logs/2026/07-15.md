# 2026-07-15 Dev Log

## nop-stream: configurable termination checkpoint timeout

- `JobCoordinator.java`: Added `terminationCheckpointTimeoutMs` field (default 60s) + setter
- `TestJobCoordinator.java`: Set 500ms in setUp to avoid 60s blocking
- Replaced hardcoded `get(60, TimeUnit.SECONDS)` in `terminateDrain`/`terminateSuspend`/`terminateExportSavepoint`

## nop-ioc: fix duplicate bean from cross-import of app beans

- Root cause: `app-service.beans.xml` imported `app-engine.beans.xml`, both auto-discovered → duplicate
- Fix 1: removed redundant import from `app-service.beans.xml`
- Fix 2 (framework): `BeanContainerBuilder.addResource()` now registers resource path in `importedResources` to prevent duplicate loading via import chain
- Added regression test `TestImportDedup`

## nop-retry-web / nop-code-web: missing test dependencies

- `nop-retry-web/pom.xml`: added `nop-rpc-cluster` test scope (provides `IRpcServiceInvoker`)
- `nop-code-web/pom.xml`: added `nop-search-api` test scope (provides `ISearchEngine`)
- Pattern: engine module declares infra dependency as `<optional>true</optional>`, web module must bring implementation

## nop-code-web: fix view validation errors

- `NopCodeSymbol.view.xml`: removed non-existent `staticFlag`/`abstractFlag` fields
- `call-hierarchy.view.xml` / `type-hierarchy.view.xml`: replaced invalid `<schema dict>` child with plain `domain="string"`; added `<actions>` for `useFormActions="true"`
- `code-browser.view.xml`: escaped `&&` → `&amp;&amp;`

## amis xxOn expression syntax migration

- Fixed all `visibleOn`/`disabledOn` etc. using legacy `this.xxx` → amis-formula `${xxx}` syntax
- nop-code-web (6 files), nop-dyn-web (1 file)
- Removed invalid `pickArray` filter

## Plan 291: migrate `$xxx` shorthand to `${xxx}` expression syntax

- Analysis: `ai-dev/analysis/2026-07/2026-07-15-amis-dollar-shorthand-vs-expression-syntax.md`
- Plan: `ai-dev/plans/291-migrate-dollar-shorthand-to-expression-syntax.md`
- All 5 phases completed:
  - Phase 1: framework xlib (control.xlib, flux-control.xlib, grid_crud.xpl)
  - Phase 2: 21 business view.xml files (7 modules)
  - Phase 3: 2 page.yaml files (nop-auth)
  - Phase 4: test files (xpl, view.xml, fixture JSON, xpage)
  - Phase 5: docs-for-ai updated with deprecation notice + XPL escaping rules
- Key lesson: in XPL templates, `${}` is expression evaluation. Use `${'$'}{xxx}` to output literal `${xxx}` for AMIS. `$$` is NOT a valid escape.
