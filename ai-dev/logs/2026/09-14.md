# 2026-09-14

### 计划收口：M5-P1 round-1 修复 closure audit——独立复核 4×P1 全部落地，42/42 checklist + pass/receipt 入账

- **独立 closure audit（session opencode-closure-audit-68082，非 EXECUTE 会话）**：逐 Phase 对照 live repo 复核 `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-14-1314-3-m5-p1-round1-remediation.md` 的 4×P1 修复——Phase 1 测试钉子触发真实生产 throw 站点且断言 `getErrorCode()` 值/参数/模板渲染（NopAiAgentErrors/NopAiCoreErrors 定义 + 8 个锚点站点逐一 grep 核对）；Phase 2 三个凭证错误码 ID 点号化、全仓零旧 ID 字符串残留、`TestAiModelCredentialResolver` 前缀守卫 + ChatServiceImpl 消费链 E2E 存在；Phase 3 `IDslTool`/`DslToolImpl` 已下沉 nop-ai-tools（`io.nop.ai.tools.xdsl`），tools pom 换 `nop-converter`、coder xdsl 目录已空、全仓零 `io.nop.ai.coder.xdsl` import（仅 nop-ai-mcp-server 的 coder.simplifier 显式依赖不受影响）；Phase 4 gateway 文档类锚点 10/10 存在 + `ai-gateway-defaults.beans.xml` 5 处装配 bean 确认 + module-groups/nop-ai.md 同步。
- **验证复跑（本 audit 会话）**：`./mvnw test -pl nop-ai/nop-ai-tools,nop-ai/nop-ai-service,nop-ai/nop-ai-agent,nop-ai/nop-ai-core,nop-ai/nop-ai-coder -am -Dtest=...` BUILD SUCCESS，5 测试类 23 例 0 失败（agent 5 / core 3 / service 10+3 / tools 2）；`node ai-dev/tools/check-doc-links.mjs --strict` 0 error（8 warnings 为其他计划存量）；引擎 `plan-check.mjs --strict` 42/42 通过。
- **入账**：plan `## Verification` 追加 `- pass test 20260914-1633 exit=0`；`## Closure` 追加 dispatch/accepted 审计收据 `#audit-2026-09-14-110620-2026-09-14-1314-3-m5-p1-round1-remediation-1-a31dde9a`；roadmap 4×P1 已勾选（EXECUTE 已做）。
- Follow-up: 无 plan-owned 剩余工作。

### 计划执行：M5-P1 round-1 修复 Phase 4——nop-ai-gateway 模块范围文档补全（channel 消息网关 + 扫码登录编排）

- **产物**：`docs-for-ai/03-modules/nop-ai-gateway.md` 新增「模块实际承载能力」总览 + 「Channel 消息网关」「扫码登录编排」「连带依赖清单」三章 + 源码锚点表补 7 行；`module-groups.md` gateway 行扩为三块能力描述 + 依赖边说明；`nop-ai.md` 功能概览与子模块表两处引用同步；计划 `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-14-1314-3-m5-p1-round1-remediation.md` Phase 4。
- **类锚点盘点（Proof）**：channel 传输层 `IChannelConnector`/`ChannelConnectorManager`/`FeishuConnector`（channel/、channel/feishu/ 包）+ 会话映射 `ChannelSessionStoreImpl`（读 `NopAiChannelSession` ORM 实体）+ 业务消息层 `ChannelMessageServiceImpl`；login 端点 `ChannelLoginApiBizModel`（`@BizModel("ChannelLoginApi")`，GraphQL `ChannelLoginApi__loginByScan` / REST `/r/...`）+ 编排 `ChannelLoginScanProcessor` + 错误码 `NopAiGatewayErrors`（`nop.err.ai.channel-login.*`）。
- **装配点（接线验证）**：ai-gateway-defaults.beans.xml 中 `nopChannelConnectorManager`/`nopChannelSessionStore`/`nopFeishuConnector`/`nopChannelMessageService`（ioc:default=true）/`io.nop.ai.gateway.login.ChannelLoginApiBizModel`（FQCN id + collect-beans）全部经 grep 确认存在。
- **连带依赖清单**：channel 块 = nop-ai-agent（ChannelConnectorContext 携带 IAgentEngine）/nop-ai-dao/nop-integration-api/nop-integration-feishu + 部署可选 nop-auth-service（UserChannelResolver）/消息总线（mode 2）；login 块 = nop-biz-auth-core/nop-auth-api + 部署可选 IChannelBindProvider 实现；公共 = nop-gateway/nop-ai-api/nop-ai-core。
- **锚点端到端验证**：文档每个类名逐一 `test -f` 校验在 `nop-ai/nop-ai-gateway/src/main` 存在（11/11 OK）；`NopAiChannelSession` 实体存在于 nop-ai-dao。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 0 error（8 warnings 为其他计划存量）；checker 校验教训：docs-for-ai 文档不得以 `ai-dev/...` 带路径形式引用 platform-dev 文档（BOUNDARY error），且反引号内短路径会被当作文件路径检查——已改为 FQCN 写法 + 纯文本目录说明。
- Doc-sync: 本 Phase 即 owner doc 更新本身（nop-ai-gateway.md / module-groups.md / nop-ai.md）。

### 计划执行：M5-P1 round-1 修复 Phase 3——nop-ai-tools → nop-ai-coder 分层倒置收口（方案 A：契约下沉）

- **裁定（Decision，方案 A）**：`IDslTool`/`DslToolImpl` 从 nop-ai-coder 下沉到 **nop-ai-tools**（`io.nop.ai.tools.xdsl`），不选 toolkit——DslToolImpl 依赖 `IFileOperator`（nop-ai-core，已弃用）与 `DocumentConverterManager`（nop-converter），下沉 toolkit 会把已弃用抽象拖进工具抽象层且需给 toolkit 增 nop-ai-core+nop-converter；tools 是"具体工具实现"层（module-groups.md:17/87），且 DSL 文件工具唯一消费方 `FileToolBizModel` 就在 tools 内，下沉成本最低（2 类 + 1 测试 + 资源 + pom）。
- **变更**：`git mv` 2 主类 + `TestDslToolImpl` + 测试资源 `test.ai-orm.xml`（路径 `io/nop/ai/tools/xdsl/`）；`FileToolBizModel` import 改 `io.nop.ai.tools.xdsl.*`，`getDslTool`（:250-251）补裁定登记注释；**nop-ai-tools pom 移除 `nop-ai-coder` 依赖、新增 `nop-converter` 依赖**（比 coder 全链 image/ui/task-core/orm-model/dao/rpc-model/report/code-analyzer 轻得多）；moved 测试依赖 coder 的 `application.yaml`（H2 datasource 配置），已在 tools test resources 补齐同名文件。
- **消费面复核（Proof）**：全仓 `io.nop.ai.coder` import 仅剩 nop-ai-mcp-server `AiFileTool`（pom 已显式声明 nop-ai-coder 依赖，不依赖 tools→coder 传递，零破坏）；nop-ai-tools main/test 零 `io.nop.ai.coder` import 残留；coder 主代码无其他 IDslTool/DslToolImpl 引用。
- **接线验证**：`getDslTool` 仍被 loadDslFile/saveDslFile/loadDslSchema 等 4 个 BizModel action 调用（FileToolBizModel:207/219/232/246）；moved `TestDslToolImpl` 2 例（ai-orm→orm 转换 + 畸形 XML fail-fast）在 tools 下全绿。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-tools -am`（32 例全绿）/ `./mvnw test -pl nop-ai/nop-ai-coder -am`（67 例 11 skip 全绿）；`node ai-dev/tools/check-doc-links.mjs --strict` 0 error。
- Doc-sync: `docs-for-ai/01-repo-map/module-groups.md` nop-ai-tools 行补注下沉裁定与依赖方向说明（与 live import 一致）。

### 计划执行：M5-P1 round-1 修复 Phase 2——AiModelCredentialResolverImpl 错误码 ID 规范化为 nop.err.ai.*

- **产物**：`AiModelCredentialResolverImpl.java` 3 个 ErrorCode ID 点号化 + `TestAiModelCredentialResolver.java` 新增 2 个测试（前缀守卫 + ChatServiceImpl 消费链 E2E）；计划 `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-14-1314-3-m5-p1-round1-remediation.md` Phase 2。
- **ID 变更**（静态字段名保留，仅 ID/描述改；调用点零改动）：`ERR_AI_CREDENTIAL_FIELD_EMPTY` → `nop.err.ai.service.credential-field-empty`、`ERR_AI_CREDENTIAL_PROVIDER_NOT_AVAILABLE` → `nop.err.ai.service.credential-provider-not-available`、`ERR_AI_CREDENTIAL_RESOLVER_NOT_CONFIGURED` → `nop.err.ai.service.credential-resolver-not-configured`；描述全部改英文（AGENTS.md 约定，含参数占位符 `{credentialId}`/`{field}`）。
- **消费点复核（Proof）**：全仓字符串精确匹配旧 ID = 0 残留（旧 ID 仅存在于定义处，无任何捕获/分支按旧字符串匹配；无别名兼容映射——直接重命名）；消费面仅定义类 + 2 个测试类（静态字段名未变，import/断言自动指向新 ID）；`ChatServiceImpl.resolveApiKeyForRequest`（:286-299）不吞异常、直接传播。
- **守卫断言**：`errorCodeIdsFollowNopErrAiDottedConvention` 断言 3 码精确新 ID + `startsWith("nop.err.ai.")`（防回退到大写常量式）。
- **E2E（Rule #22/#23）**：`failClosedCodePropagatesThroughChatServiceConsumer` 用真实 H2 ORM 栈 seed NopAiModel（provider=`free`，VARCHAR(4) 约束下唯一带 main-resources `.llm.xml` 的 ≤4 字符 provider）+ 真实 resolver + 真实 `ChatServiceImpl`（fake HTTP client 断言不被触达）→ `chat.call()` 捕获 resolver fail-closed 异常，断言 `getErrorCode()=="nop.err.ai.service.credential-field-empty"` 且 `credentialId` param 存活。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-service -am`（39 例全绿，含 10 例 resolver 测试）；`node ai-dev/tools/check-doc-links.mjs --strict` 0 error。
- Doc-sync: `No owner-doc update required`（docs-for-ai 无 nop-ai-service 错误码清单；模块 README 无错误码表，Phase 2 exit criterion 显式裁定）。

### 计划执行：M5-P1 round-1 修复 Phase 1——plan 356 五错误码补 getErrorCode() 测试钉子（nop-ai-agent / nop-ai-core）

- **产物**：`TestNopAiAgentErrorsContract`（agent 5 例）+ `TestNopAiCoreErrorsContract`（core 3 例）；计划 `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-14-1314-3-m5-p1-round1-remediation.md` Phase 1。
- **触发站点盘点（Proof，每码 ≥2 站点，锚点记录于测试 javadoc）**：`ERR_AGENT_INVALID_ARGUMENT`（PlanScheduler:59 / PlanRunner:58 / InSessionCompactionSnapshotArchive:49 / GuardrailRule:45）、`ERR_AGENT_INVALID_STATE`（PlanReplanner:265/270 / PlanExecutor:151 / MemberExecOutcome:230 / InMemoryTeamTaskStore:82 / SecurityCheckpointChain:40）、`ERR_AGENT_INTERNAL_DETAIL`（SmartModelRouter:321/330/334 / FileBackedSessionStore:103 / SessionFileWriter:69/72）、`ERR_AI_CORE_INVALID_ARGUMENT`（CandidateHealthProvider:32，main 唯一站点）、`ERR_AI_CORE_INVALID_STATE`（MockChatService:55 / InMemoryResponseProvider:170 / FileSystemResponseProvider:91 / Media:149 / ThresholdBreaker:141 / FileDiffApplier:139）。
- **测试落地**：8 例全部经公开入口触发真实生产 throw 站点（非 stub），断言 `ex.getErrorCode()` 等于对应 `ErrorCode.getErrorCode()`（值契约）+ ID 字符串与 `nop.err.ai.agent.*` / `nop.err.ai.core.invalid-*` 点号格式一致 + `detail` 参数存在且 `getMessage()` 渲染含 detail（消息模板可渲染，非仅异常类型）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am`（3395 例全绿，含 5 新例）/ `./mvnw test -pl nop-ai/nop-ai-core -am`（382 例 3 skip 全绿，含 3 新例）；`node ai-dev/tools/check-doc-links.mjs --strict` 0 error（8 warnings 为其他计划存量）。
- Doc-sync: `No owner-doc update required`（错误码 ID 不变，仅补测试，Phase 1 exit criterion 显式裁定）。

### 计划执行：M6-P1 round-2 审计 5×P1 逐项修复（nop-ai-agent / nop-ai-tools / nop-ai-shell / nop-ai-gateway）

- **产物**：计划 `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-14-1314-2-m6-p1-round2-remediation.md` 5 个 Phase 全部执行完毕（55/55 checklist 勾选，ledger format：frontmatter `status: active` 与 Phase `Status: planned` 按引擎规则不动，完成态由引擎从勾选推导）；roadmap 工作项 M6-P1 已勾选。
- **① ReActAgentExecutor failed 不发布 EXECUTION_COMPLETED**：`canPublishExecutionCompleted`（ReActAgentExecutor.java:1271-1290）补 `failed` 排除（重试耗尽/不可重试分类后 `finalizeLlmCallResult` 置 failed → reactLoop break → adjudicateTerminal 不再发完成事件/跑 POST_CALL hooks；EXECUTION_FAILED 由失败路径自身发布，不吞）；枚举逐值复核：排除面 = cancelled/forced_stopped/escalated/paused/truncated/waiting/failed，唯一放行终态 = completed（pending/running 到不了此处，running 先转 truncated）；回归测试 `failedSessionDoesNotPublishExecutionCompletedNorPostCallHooks`（POST_CALL 计数=0、EXECUTION_COMPLETED 零发布、EXECUTION_FAILED 已发布）+ 正例 `completedSessionPublishesExecutionCompletedAndRunsPostCallHooks`（POST_CALL=1）。
- **② 同实例重复提交删胜出执行租约**：DefaultAgentEngine.registerExecutionSlot 与 AgentSessionLifecycle 的 resumeSession/wakeSession/acquireRestoreExecutionSlot 4 处同构注册路径统一修复——释放条件从"进入 catch"改为"本调用 putIfAbsent 成功注册执行槽（slotRegistered）"。关键修正（首版用 leaseAcquired=true 仍删租约，被新测试抓出）：**同 owner tryAcquire 是幂等续租**，败者提交也会 tryAcquire 成功，只有 putIfAbsent 失败才标志着"租约归胜出执行"；错误文案 4 处均准确（tryAcquire 失败="locked by another instance"、putIfAbsent 失败="already executing"）。回归测试 `sameInstanceDuplicateSubmitPreservesWinnerLease`（TestSessionTakeoverLockEngineWiring）：败者提交后租约行仍在 + owner 不变 + 胜者在租约 TTL 外继续续租（isHeld 跨 2s 保持）+ 胜者正常完成释放；既有 cross-instance fail-fast 与释放测试保持。
- **③ FileToolBizModel.getProjectDir 沙箱逃逸**：fail-closed 前置校验——raw projectName 含 `/` 或 `\` 一律拒（`StringHelper.fileName` 会按 `/` 截断成 lastPart，逃逸输入必须在截断前被拒）；fileName 结果非空、非 `.`/`..`、过 `isValidFileName`；违规抛 `ERR_AI_TOOLS_INVALID_PROJECT_NAME` + `ARG_VALUE`（与 AiToolsHelper.requireValidSessionId 防御姿态对齐）。内层 Proof：`LocalFileOperator.resolveFile`（:71-89）已含 `..`/`:` 拒绝与 canonical 前缀校验（非同构逃逸），无残留旁路。回归测试 `TestFileToolBizModelProjectDir`：`..`/`.`/`a/b`/`a\b`/`/etc`/空/null 全拒（saveFile 入口 + getProjectDir 直调双路径），逃逸输入零落盘，正常 projectName 读写落 baseDir 内（canonical 前缀断言）。
- **④ Shell &> 合并重定向输出翻倍**：`handleMergeRedirect`（ShellCommandExecutor.java:499-507）弃 `new TeeOutput(fileOutput, fileOutput)` 同实例双 leg，改 stdout/stderr 直指同一 FileShellOutput（每字节恰写一次，顺序保持）；TeeOutput 无其他生产消费者（仅 ShellIOTest 用两个不同实例，语义不变）。回归测试：`echo stdout &> f` 精确断言 `"stdout\n"`（旧 `contains` 弱断言对翻倍全绿）、`&>>` 两次执行恰两行、`echo out &> f` + `echo err >&2 &>> f` 合并两流各一行顺序正确。
- **⑤ FeishuConnector.isBotMentioned @任意成员触发**：改为实例方法精确匹配 bot 自身身份——`start()` 时从 ChannelConfig option `feishu.botOpenId` 解析（resolveCredentials 旁新增 resolveBotOpenId），mentions 数组逐元素解析 `id.open_id` 与 bot open_id 精确相等才判定 @bot；bot 身份未配置 → fail-closed 不触发 + start 时 WARN 日志（不静默）；类注释"bot open_id 精确匹配 deferred"声明已删。回归测试：@其他成员（含 key+open_id 标记）不触发、@bot 触发、混合提及含 bot 触发、未配置身份群消息 fail-closed + DM 不受影响；E2E（TestFeishuConversationE2E）群聊分支补"@他人"负例。owner doc 核实：`docs-for-ai/03-modules/nop-ai-gateway.md` 无 Feishu 触发语义内容 → No owner-doc update required（Phase 5 exit criterion 成立）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am`（3390 例）/ `nop-ai-tools`（30 例）/ `nop-ai-shell`（283 例）/ `nop-ai-gateway`（180 例）全绿；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: No owner-doc update required（4 项行为修复 + 1 项防御语义收严，均不改公开契约，各 Phase exit criterion 已显式裁定）。

### 计划执行：M6-P0 AiFileTool 沙箱逃逸修复（nop-ai-mcp-server，fail-closed 路径包含性校验）

- **产物**：`AiFileTool.java` getResource 重构 + `McpServerErrors.java` 新增 `ERR_MCP_PATH_ESCAPE`（`nop.err.mcp.path-escape`，英文描述，带 `ARG_PATH`）+ `TestAiFileTool.java` 12 个新回归用例（共 15 例全绿）；计划 `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-14-1314-1-m6-p0-aifiletool-path-traversal.md`。
- **修复内容**：getResource 改为 fail-closed——①绝对路径（`/` 前缀）仅允许命中 VFS 资源，未命中一律抛 `ERR_MCP_PATH_ESCAPE`（不再参与落盘路径解析）；②落盘路径（存在文件）canonical 后必须位于 baseDir canonical 之内，越界抛 `ERR_MCP_PATH_ESCAPE`；③尚不存在的新建路径（saveNopFile 场景）先 canonical 解析父目录再拼接文件名校验，越界拒绝，沙箱内放行（新建文件能力保留）；④IO 解析失败同样 fail-closed 抛专用错误码；VFS 回退分支语义按计划保留不变。
- **关键机制修正**：审计基线称"Java `new File(baseDir, absPath)` 直接解析为绝对路径"——实测（Unix）`new File(parent, child)` 对绝对 child **拼接**为 `parent + child`（`fs.resolve` 直接 `parent + child`），绝对路径并不直达主机绝对位置；真实 Unix 逃逸向量是 `..` 段（POSIX 逐组件解析，中间目录存在即可穿越），Windows 盘符路径才是真绝对路径。修复按"一律拒绝绝对路径形态"实现，两种平台语义均 fail-closed。另确认 VFS 分支对主机绝对路径返回 `UnknownResource`（exists=false），不构成主机文件逃逸面。
- **无旁路 Proof**：`loadNopFile`（:71）/`saveNopFile`（:122）均单一入口经 `getResource` 取资源，全部读写（`readText`/`buildFromResource`/`convertResource`/`ResourceHelper.writeText`）只作用于已校验资源；类内无直接 `new File` 或绕过校验的 `writeText`。
- **回归测试**：绝对路径读/写被拒（构造绝对路径 + `/etc/passwd` 形态，断言 `getErrorCode()` 与 `ARG_PATH` 参数）、`../` 与 `foo/../../bar` 逃逸被拒、越界新建目标被拒且不落盘、沙箱内相对读/写/覆盖/新建（含新子目录）、沙箱内 `..` 回退不误伤、merge 到新文件路径（此前不可达分支）；测试 `setUp` 清理 `target/test-ai-file-tool` 防残留。
- **Doc-sync**：docs-for-ai 无更新（`service-layer.md` 仅登记 `AiFileTool:read/write` 权限，未变）；`setBaseDir` javadoc 补一行明确默认 `.` 相对进程 CWD 的沙箱语义。
- 验证：`./mvnw test -pl nop-ai/nop-ai-mcp-server -am` 退出码 0（15 例）；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（9 warnings 为其他计划的既有存量，0 error）。

### Deep Audit R2：nop-ai-agent-design-comparison 任务审计（1×P0 + 5×P1 + 16×P2，结论内联 roadmap）

- **触发**：mission-driver dispatch `2026-09-14-110620` round 2；按 `ai-dev/skills/open-ended-adversarial-review-prompt.md` 执行，4 个并行子代理（agent engine / core+service / tools+toolkit+coder+shell / gateway+dao+api+docs）+ 主 agent 逐项复核源码。
- **记录位置**：结论内联 `ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md`——frontmatter `audit-rounds: 1→2`；新增 `### M6 — Deep Audit Findings R2`（1×P0 + 5×P1 工作项）+ Follow-up Backlog 追加 16×P2（`source: deep-audit round 2`）+ `## Deep Audit Record` 追加 dispatch/accepted 双行（nonce `27384d32`）。未创建 `ai-dev/audits/nop-ai-agent-design-comparison/` 任何文件。
- **P0**：`AiFileTool`（nop-ai-mcp-server）`new File(baseDir, path)` 无包含校验，绝对路径/`../` 任意文件读写（`getResource` :163-179），MCP 工具权限即 LLM 权限，prompt-injection 暴露面下即主机任意文件读覆盖。
- **P1**：①`canPublishExecutionCompleted` 漏 `failed` 状态→失败执行仍发 EXECUTION_COMPLETED + 跑 POST_CALL hooks（ReActAgentExecutor.java:1271-1278）②同实例重复提交 `releaseLockQuietly` 删胜出执行租约→运行中执行被强制 cancel + 双执行窗口（DefaultAgentEngine.java:789-806）③`FileToolBizModel.getProjectDir` 不拒 `..`→沙箱上移一级（:265-271）④Shell `&>` 合并重定向 `TeeOutput(fileOutput, fileOutput)` 同实例两次→输出翻倍（ShellCommandExecutor.java:499-506）⑤`FeishuConnector.isBotMentioned` 仅子串检查→群聊 @任意成员触发 agent（:586-617）。
- **P2**：ThresholdBreaker HALF_OPEN 探测位卡死、ConcurrencyRegistry 补偿竞态+表不收缩、connect-timeout 死配置、FALLBACK javadoc 过期、buildFullContentWithThinking 垃圾默认+死代码、apiStyle 死字段、AgentHookInvoker fail-loud 被吞、null assistant NPE、FALLBACK 循环无上限、ThoughtStorage 任意路径、文件工具无原子写+静默失败、MFA 分支零测试+裸字符串契约、getCapabilities 死面、Feishu 凭证契约矛盾、McpServerErrors 中文描述+setLimit(1)/(2) javadoc 漂移、docs 实体表 12/22+锚点过期。
- **round 1 复核**：M5 4×P1 + Follow-up 9×P2 全部仍存在未修复（credential 错误码测试已部分补上——TestAiModelCredentialResolver 已断言 getErrorCode，但命名违规与 plan-356 五码零断言仍成立）。
- **正向确认**：28 份交付物齐全；断路器/退避数学、AccountChain/ProviderFailoverChain 游标、shell 沙箱（BashSandboxPaths 拒 `..`/symlink）、SSRF 防护、ThoughtStorage sessionId fail-closed、错误码全链 param 均正确；@Inject private 与 Spring 注解零违规；beans.xml 类引用全部可解析。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: No owner-doc update required（审计结论内联 roadmap，未改 docs-for-ai）。

### Deep Audit R1：nop-ai-agent-design-comparison 任务审计（4×P1 + 9×P2，结论内联 roadmap）

- **触发**：mission-driver dispatch `2026-09-14-110620`；按 `ai-dev/skills/deep-audit-prompts.md` 执行，4 个并行维度子代理（扩展矩阵 vs live code / 交付物锚点 / 模块结构+API+异常 / 测试有效性）+ 主 agent 复核。
- **记录位置**：结论内联 `ai-dev/backlog/nop-ai-agent-design-comparison-roadmap.md`——frontmatter `audit-rounds: 0→1`；新增 `### M5 — Deep Audit Findings R1`（4 项 P1 工作项）+ `## Follow-up Backlog`（9 项 P2，`source: deep-audit round 1`）+ `## Deep Audit Record`（dispatch/accepted 双行，nonce `a3f91c2e`）。未创建 `ai-dev/audits/nop-ai-agent-design-comparison/` 任何文件。
- **P1**：①plan 356 新增 5 错误码（628+ throw 站点）测试零 `getErrorCode()` 钉子 ②`AiModelCredentialResolverImpl.java:64-96` 三码违反 `nop.err.ai.*` 命名约定（跨模块公共路径）③`FileToolBizModel.java:250-251` tools→coder 分层倒置 ④nop-ai-gateway channel/login 生产面无 owner 文档（范围漂移）。
- **P2**：报告锚点行号漂移（plan 355 后 118 处，机制零失真）、矩阵 §5.2 措辞、ChannelConnectorContext 裸 IAE、9 个零价值测试方法、roadmap 机器校验声明不可满足（roadmap-check.mjs 无 CLI/不解析 checkbox）、536→535 快照漂移、xbiz 42→44 计数、module-groups:85 表述、10 处 UOE fail-fast 未登记。
- **正向确认**：28 份交付物齐全；72 顶层接口/死点/死代码/接线缺口矩阵声明 10/11 成立；143 锚点抽查零机制失真；@Inject private 与 Spring 注解零违规；plan 356 IAE/ISE 四模块归零字面成立。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。

### 计划执行：owner doc 03-extension-matrix.md 6 项勘误线索修订（WI5 follow-up 收口）

- **产物**：`ai-dev/design/nop-ai-agent/03-extension-matrix.md` 按 WI5 登记的 6 项勘误线索 + live 代码复核修订（纯文档，无代码变更）；计划 `ai-dev/plans/nop-ai-agent-design-comparison/2026-09-14-110620-1-extension-matrix-docfix.md`。
- **① IContentGuardrail**：状态 🟡→✅；功能实现列补 `PromptInjectionGuardrail`（`guardrail/PromptInjectionGuardrail.java:31`）+ `RuleGraphGuardrail`（`guardrail/rule/RuleGraphGuardrail.java:45`）；装配点 `DefaultAgentEngineConfig.setContentGuardrail`（:929）/ `DefaultAgentEngine.Builder.contentGuardrail`（:182，:338 处装配）；实际调用点 `AgentPromptAssembly.java:69`（INPUT）/ `:296`（OUTPUT）；从 §5.2 半闭合清单移除。
- **②③ 计数**：live 复核 `rg '^public interface ' nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/ --glob '!**/_gen/**'` = **72 顶层接口**（另 1 嵌套 `StoreFactory`，共 73 声明）。矩阵重算：核心契约 4 / 真正扩展点 **57** / 回调契约 4 / NoOp-only 2 / 预留原语 1 / 矩阵外 4（`NopAiAgentErrors` + 3 guardrail 测试 SPI）= 68 = 72 − 4。Layer 标题计数改与表格行数一致（4.1 10 / 4.2 16 / 4.3 12 / 4.4 23）。
- **④ REASONING_CHUNK 死点**：确认全库零触发点（仅枚举 `AgentLifecyclePoint.java:11`、注册映射 `DefaultHookRegistry.java:169`、注释 `AgentHookInvoker.java:58`/`:166`、`ReActAgentExecutor.java:1289`、`AgentExecutionResult.java:111`）；`AgentHookInvoker.java:58` 注释与实际不符，已在 §4.2 L2 注与 §6.3 登记。
- **⑤ HookToMiddlewareAdapter 死代码**：`DefaultHookRegistry.java:122` private static 类构造器（:125）全库零引用；:30-31 注释声称 register() 已包裹 middleware 与实际不符（middleware 链实际经 `IHookRegistry.getMiddlewares` 由 `AgentHookInvoker.executeWithMiddleware` 构建），已在 §6.3 登记。
- **⑥ 消费者标注**：抽查 `ISessionStore`（+AgentCompactionCoordinator）、`IToolAccessChecker`/`IPathAccessChecker`/`IPermissionProvider`（→SecurityCheckpointChain）、`IHookRegistry`（→AgentHookInvoker）、`IAgentMessenger`（消费点已含 tools/ATEC，保持）；§4.5 加一行免责注记（DAE/RAE 为粗粒度默认标注）。
- **计数迁移登记**：`IModelSwitchedMessageWriter` / `ICircuitBreaker` / `IRetryPolicy` 接口定义已迁 `nop-ai-core`；`ILevelHintsProducer` 已按 plan 304 合并入 `ISecurityLevelResolver`——4 行移出矩阵并加 L2/L3 注；补登 5 个 live 接口（`IAgentMiddleware` / `SecurityCheckpoint` / `IWaitCoordinator` / `ICompactionSnapshotArchive` / `TaskRunner`，其中 TaskRunner 归回调契约）。
- **§5.1 重算**：✅ 61（89.7%）/ 🟡 2（2.9%）/ 🔴 1（1.5%）/ ⚪ 4（5.9%），合计 68。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: 无 docs-for-ai 更新（owner doc 属 `ai-dev/design/`，不属 docs-for-ai 路由）；roadmap `nop-ai-agent-design-comparison-roadmap.md` 引用计数已同步（66→68）。

### 计划 2261 立项：nop-metadata 概念缩减——彻底删除 NopMetaTable，功能归一 NopMetaEntity（plan-first，待 audit）

- **产物**：`ai-dev/plans/2261-nop-metadata-entity-unification.md`（Status: draft）。依据用户裁定（NopORM 实体 = 物理表设计 + 关联语义标注，逻辑表概念冗余）。4 个 Phase：①目标模型设计裁定（产出 `ai-dev/design/nop-metadata/13-entity-unification.md`，8 项裁定含多 schema/UK 重设计、api DTO 处置）②模型与服务层一次性切换（orm 扩列/子表改名换挂点/11 处 FK 改造/删除 5 实体/执行链改名/sync 与 importer 去双写/4 类字符串软引用/deploy/sql 三方言 DDL）③测试迁移（79 文件逐组处置登记，测试数不低于基线）④端到端验证 + docs/design 收口 + 数据迁移裁定。
- **依赖盘点**：Explore 子代理实测——NopMetaTable 与 NopMetaEntity 零 ORM relation（唯一纽带 baseEntityId 裸列）；非子表 to-one FK 9 个（7 实体 + LineageEdge 双端点）；主源码 94 Java 文件 + 测试 79 文件引用；外部表列结构 JSON 藏于 buildSql；entity 查询路径依赖平台已注册 ORM 实体（不依赖 Table 行合成模型）——归一可行性的关键证据。
- **对抗性审查**：独立子 agent 一轮审查（含想象性分析），结论无 Blocker；按 4 项 Major 修订计划：①清零 grep 扩展到 xmeta/xbiz/xjs/yaml/json/sql + 小写匹配 + "_gen 豁免=重新 codegen 后零命中"定义 ②Phase 1 补多 schema/UK 重设计裁定与 api DTO 处置裁定 ③deploy/sql 三方言 DDL 处置入 Phase 2、Closure Gates 补 `run-nop-metadata-invariants.sh` CI 硬门 ④docs 修订范围扩至 module-groups/source-anchors；另修正 to-one 计数（9 非子表 + 子表侧随子表处置）、补 ModuleBizModel 级联删除重写项、"Phase 2 期间禁改测试"注记。
- 状态：plan-first 区域（ORM 模型结构），实施待 plan audit；执行建议在新 worktree 分支 `feat-nop-metadata-entity-unification`。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（8 warnings 为既有存量）；计划引用的 `ai-dev/tools/run-nop-metadata-invariants.sh` 等路径经审查代理逐一验证存在。
- Doc-sync: No owner-doc update required（计划本身；owner docs 更新是计划 Phase 4 的执行项）。

### 2026-09-12a 本体调研分析补记：Entity/Table 最终裁定与 nop-ontology 立项（无 plan，ad-hoc）

- **裁定来源**：用户给出 NopORM 不变式——"实体 = 物理表设计 + 关联语义标注，任一 table 都有对应的 ORM 模型，任一 ORM 模型本质上都对应物理表设计"，故 NopMetaTable"逻辑表"概念冗余、Entity/Table 双树为结构债；本体的 ObjectType 为唯一资产身份，ORM 实体为其唯一绑定形态。
- **产物**：`ai-dev/analysis/2026-09/2026-09-12a-github-ontology-projects-vs-nop-metadata.md` 顶部增补"裁定补记（2026-09-14）"——修正 §7.2/§7.3 骑墙的 Object Type 映射；废止"在 nop-metadata 上补声明层"后续路径，改为独立 nop-ontology 模块方案（nop-metadata 瘦身为治理运行时）。
- **关联**：本体模块已在 worktree 分支 `feature/nop-ontology` 立项并推进到数据库设计 v2（定义面 8 实体 DB 唯一权威 + 不可变版本快照、动能面 3 实体、三绑定路径归一 ORM 实体、M5 实体定义设计器里程碑，21 WI），见该分支 `ai-dev/backlog/nop-ontology-roadmap.md` 与 `ai-dev/design/nop-ontology/01-data-model.md`。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: No owner-doc update required（分析补记；nop-ontology 的 docs-for-ai 使用规范随其 roadmap WI21 文档收口同步）。
