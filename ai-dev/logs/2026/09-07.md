# 2026-09-07

## 21:00 吸收实施（P0+P1 全部）+ XScript try/catch 勘误（主仓 + nop-app-erp）

- 用户裁定：P0+P1 全部实施。docs-for-ai 共 14 个文件落地吸收：
  - `api-and-graphql.md`：查询字段 filter 运算符白名单（缺省 `[eq,in,dateBetween,dateTimeBetween]`，源证 `ObjMetaBasedFilterValidator.java:38` DEFAULT_ALLOW_FILTER_OP；扩展用 xmeta `allowFilterOp`）+ findPage 只有 `query` 参数（limit 在 QueryBean 内）+ 手写 GraphQL 禁裸 `$var` 用 `@query:`
  - `dql-query.md`：filter 白名单 cross-ref
  - `testing.md`：VarCollector 段修正（teardown 现恢复 no-op 实例，AutoTestCase.java:232，旧"置空且不再恢复"为 docs bug）+ 时间可控性（自定义时钟不得破坏 TestClock 单调/日期同源）+ 测试配置隔离（assignConfigValue ref 跨类存活）+ 快照与并发规则（surefire parallel/reuseForks、@var 引用、错误码集合断言、@AfterAll 注销）+ 运行时字面量批量改写协议（全 reactor 验证门）
  - `xlang-and-xpl-basics.md`：XScript try/catch/finally 支持（JS 语义，plan 2258）
  - `cross-module-entity-reference.md`：tableName 双重前缀反模式
  - `e2e-testing-troubleshooting.md`：日志优先六步法 + E2E 收口门禁
  - `generate-business-code.md`：编码规则日期源（`nopSysCalendar`）绕过 IClock
  - `concurrency-and-transactions.md`：同事务"实体写→DB 直查"须 flushSession
  - `orm-model-design.md`：dict 死状态（每状态值须有 writer）+ domain precision 对齐
  - `auth-and-permissions.md`：权限声明层同步（FNPT/XMeta/角色 seed 三处）
  - `flux-rendering.md`：双引擎能力面差异 + data-source 发布时序与公式护栏
  - `ioc-and-config.md`：name-prefix 收集 vs by-type/optional 禁类签名引用 + lazy-property 创建期不可用 + bean-init-self-wait
  - `delta-customization.md`：常见坑 #3 补"整文件替换丢平台 bean"失败症状
  - `add-bizloader-field.md` + `view-and-page-customization.md`：BizLoader 虚拟字段 custom cell、option j:list、xview 白名单、validate-page-model 安全网
  - `build-approval-flow.md`：*end listener 必须判定结束原因反模式
- **XScript try/catch 勘误（用户纠正，重大）**：我初稿把 erp L15"XScript 无 try/catch"当 P0 吸收进 xlang-and-xpl-basics.md，用户指出 XScript/XPL 现已有 try/catch/finally——核实 master `BuildExecutableProcessor.processTryStatement:628` + `TryExecutable`（catch 绑定异常/吞异常/显式 throw 重抛/finally 必执行）确认成立，提交 `c6d8f66a12`（plan 2258）已在 master（plan 文件状态仍标 active，未同步）。已改正 xlang doc 并勘误 erp 侧：`docs/lessons/15` 全文重写（"无法 try/catch"→"try/catch 已支持，下沉 Java Bean 为架构偏好"）+ lessons/README 索引行 + lesson 17 引用行 + `ErpHrSalary.xbiz` 机制注记两处 + `ErpHrSalaryApprovalGuard/PostApprovalProcessor` javadoc + app-service.beans.xml 注释 + `docs/design/assets/state-machine.md` + `docs/design/human-resource/payroll.md`；历史 log/plan（08-13/14/20）保持 append-only 不回写
- 验证：`check-doc-links.mjs --strict` exit 0（0 errors）；erp 侧 xbiz/beans.xml `xmllint --noout` 通过
- 未提交（主仓 + erp 均未 commit）
- Doc-sync: 上述 docs-for-ai 14 文件即 owner doc 吸收本体；无 INDEX 路由变更（未新增文件）

## 20:00 分析+修复：四个教训/缺陷来源的吸收决策 + docs-for-ai 自包含坏链修复

- 交付：
  - `ai-dev/analysis/2026-09/2026-09-07-lessons-bugs-absorption-decision.md`（Status: open，决策文档）
  - 坏链修复 4 处（docs-for-ai 自包含，`check-doc-links.mjs --strict` exit 0）
- 澄清：`docs/lessons`、`docs/bugs` 位于 **nop-app-erp 外部仓库**（`/Users/abc/app/nop-app-erp/docs/{lessons,bugs}`），本仓无此目录；docs-for-ai 中 `docs/lessons/`、`docs/architecture/i18n-compliance.md`、`deploy/sql/` 引用均为坏链
- 方法：自读本仓 ai-dev/lessons 16 条全文；3 个 explore agent 分别通读 nop-app-erp docs/lessons 18 条、docs/bugs 07-08 月 34 条、09 月 11 条；另 1 个 agent 通读本仓 ai-dev/bugs 17 条；逐一核对 docs-for-ai 既有覆盖
- 决策要点：
  - 吸收策略 = 提纯通用规则落 owner doc 小节（不逐条搬运），与 pitfalls 速查页风格一致
  - 本仓 ai-dev/lessons：无新增独立吸收（06 凭证收敛→model-first-development.md:355、09 UK constraint→invariant-guards INV-UK、12 *end listener→enable-approval-on-entity、15 collect-beans→ioc-and-config 均已覆盖；可选补强 12/15 各一条反模式）
  - 本仓 ai-dev/bugs：3 处需 docs-for-ai 行动——VarCollector testing.md:25-31 描述过时（当前 AutoTestCase.java:232 已恢复 no-op 实例，docs bug）、#9 missing-tenant-id 测试配置隔离新增、#10 部分；#1/#6/#14/#15 并入 ai-dev/lessons
  - nop-app-erp lessons：P0 吸收 L15（xbiz 无 try/catch 下沉 Java Bean，xlang-and-xpl-basics.md 当前无覆盖）、L01（跨模块 tableName 双重前缀，cross-module-entity-reference.md）、L05（E2E 日志优先六步法）；P1 升级 L10（dict 死状态→orm-model-design）、L16/L18（→flux-rendering）
  - nop-app-erp bugs：8 大吸收主题 + 6 项"真正缺口"（GraphQL filter 无 le/ge 用 dateBetween、findPage limit 在 query 内、编码规则日期源绕过 IClock、flux data-source 时序、快照批量改写协议、E2E 收口门禁）
- 坏链修复（4 处 error）：`application-project-pitfalls-{backend,frontend}.md` 去 `docs/lessons/` 引用；`bootstrap-new-domain-module.md` 去 `docs/architecture/i18n-compliance.md` 与 `deploy/sql/` backtick 路径（deploy 是顶层目录触发检查器）
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 errors；仅剩 plan 2258 active 计划的 8 个源码简写路径 warning，未改动以避免与执行中 worktree 冲突）
- Commit: 未提交
- Doc-sync: 本轮只改 ai-dev/analysis + docs-for-ai 坏链文案；owner doc 正式吸收待用户确认实施范围

## 19:00 分析：在 Nop 平台基础上构建 Palantir 式本体层（Ontology）的可行性

- 交付：`ai-dev/analysis/2026-09/2026-09-07-palantir-ontology-on-nop-feasibility.md`（Status: open，纯分析无代码改动）
- 方法：并行 2 个 explore 子代理盘点 Nop 机制（模型即代码/XDef-Delta/nop-dyn/nop-metadata/动作审批/安全/SDK/AI），自行 web 调研 Palantir Ontology 官方文档 + 2025-2026 第三方拆解（OSDK/Ontology-as-code/OMS-OSS-Funnel-Actions 架构/OAG/Ontology MCP 2026-01）
- 核心结论（初步）：
  - 语义半区（object/property/link + 语义层/血缘/质量 + 审批规则）Nop 近乎开箱；动能半区（ActionType 一等抽象 + 动态安全 + 动作审计/撤销）中高可行；平台化规模面（对象索引 Funnel/OSS、TS/Python SDK、治理 UI、OAG/AIP Logic）中低，需新设计
  - 关键 gap：无显式一等本体 DSL（interface/action/object-set 抽象）、无运行时对象索引、TS/Python SDK 生成空白、动态安全策略引擎、跨版本模型迁移、OAG 式 AI 面
  - 建议：Palantir 当需求清单/对标基准，不 1:1 复刻；以 `ontology.xdef`（L0）+ nop-metadata 数据面（L1/L3）+ xbiz/wf 动能（L2）+ GraphQL/MCP AI 面（L8）分层落地；否决 OWL/RDF 栈与 OMS/OSS/Funnel 五服务照搬
  - nop-metadata 角色：本体的数据面/运维面/治理面底座，缺"声明成 ObjectType/ActionType/Interface 的显式一层"
- 验证：无代码变更，无构建门禁；`check-doc-links.mjs --strict` exit 0
- Commit: 未提交（按用户习惯，随后续一起提交）
- Doc-sync: No owner-doc update required（纯 ai-dev/analysis 调研，未改 docs-for-ai 契约）

## 18:00 feat-nop-treesitter worktree 引导：roadmap + mission + 模块骨架 + 首提交

- 来源：上一节（17:00）调研后用户决定做纯 Java 实现并建分支
- 工作分支：`feat-nop-treesitter`（worktree `~/app/nop-entropy-wt/nop-entropy-feat-nop-treesitter`，从 master 切）
- 模块骨架（commit `0d5971af6a`）：
  - 父 pom 加 `<module>nop-treesitter</module>`（位于 `nop-credential` 后）
  - `nop-treesitter/pom.xml`：JDK 17、jar packaging、依赖 `nop-commons` + slf4j + JUnit 5
  - `io.nop.treesitter.TreeSitterBootstrap` 占位 + 2 个 smoke test 通过
  - `.mvn/maven.config` 独立本地仓库（`.nop/repository`），避免污染系统 ~/.m2
  - `./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS（2/0/0/0）
- Roadmap (`ai-dev/backlog/nop-treesitter-roadmap.md`)：14 work items + 4 milestones
  - M1：JSON grammar end-to-end（item 2 ts2java 抽取器 + 3 Subtree arena + 4 LR(1) parser）
  - M2：real-world grammar（item 5 GLR + 6 cursor + 7 external scanner VM → Java grammar 全过）
  - M3：production API surface（item 8 query + 9 incremental + 10 JS/TS + 11 error recovery）
  - M4：ready for downstream consumers（item 12 NopIoC/GraphQL 集成 + 13 性能 benchmark + 14 docs）
  - 硬约束在 roadmap "Cross-cutting concerns" 节明确：禁 JNI/FFM/JNA、锁 Tree-sitter v0.25.x、每阶段用 upstream test/corpus 验证
- Mission (`missions/nop-treesitter.json`)：
  - `model: "opencode-go/deepseek-v4-flash"` + `variant: "max"`
  - `agent: "build"` + `plansDir: "ai-dev/plans/nop-treesitter"` + `auditsDir: "ai-dev/audits/nop-treesitter"`
  - `moduleDir: "nop-treesitter"` + `commitFormat: "feat(nop-treesitter): <description>"`
- 验证：
  - `node tools/mission-driver/src/mission-check.mjs missions/nop-treesitter.json .` → `{valid:true}`
  - `./tools/mission-driver.sh list-steps nop-treesitter` → 5 个 step 完整列出
  - `./tools/mission-driver.sh run nop-treesitter --dry-run --no-monitor` → 16 step 全过、0 active/draft plans（符合预期）
- 踩坑：
  - macOS BSD `sed` 不支持 `+` 量词，导致 worktree 名字提取成空串；改用 `grep -oE`
  - pre-commit hook 调 `ai-dev/tools/node_modules/.bin/sg`，首次 commit 失败 → `cd ai-dev/tools && pnpm install` 后通过
- 后续：用户可在终端运行 `./tools/mission-driver.sh run nop-treesitter` 启动 mission（推荐先监控端口 9300 dashboard 验证）

## 17:00 调研纯 Java Tree-sitter runtime 移植可行性 + 下载参考仓库

- 背景：Nop 平台目前依赖 JNI/FFM 才能用 Tree-sitter 生态（200+ grammar）。用户提出想做纯 Java runtime 移植，目标：单 JAR 分发，零 native 依赖，跨平台含 Android/GraalVM。
- 下载：
  - 在 `~/sources/treesitter/` 下建立源码仓库集（`scripts/download-github.sh` + `download-all.sh` + `verify-downloads.sh`）
  - 17 个仓库全部下载完成：
    - 上游：`tree-sitter`（C runtime + CLI + `lib/binding_web` WASM/JS）
    - 重写参考：`gotreesitter`（纯 Go，206 grammar，~726k LOC）、`tree-sitter-rust-rewrite`（HerringtonDarkholme，~78k）、`tree-sitter-c2rust`（c2rust 翻译，~71k）
    - Java binding 对比：`java-tree-sitter`（FFM）、`tree-sitter-ng`（JNI）、`seart-group-java-tree-sitter`、`kotlin-tree-sitter`、`jtreesitter-type-gen`
    - Node binding：`node-tree-sitter`
    - 工具实例：`graft`（用 gotreesitter 的 VCS）、`stacklit`（代码索引）
    - 5 个 grammar：`tree-sitter-{json,java,javascript,typescript,python}`
  - 脚本特性：指数退避（5s→60s）× 80 次重试；同目标目录锁文件防并发 rm -rf；支持 GIT_TOKEN 提速率
- 脚本踩坑：
  - Bash 中 `GROUPS` 是 shell builtin 变量，会被预填当前用户的 group IDs → 改名为 `SELECTED`
  - array literal 中加 `|| true` 是语法错误
  - 并发跑 17 个 git clone 时，旧 process 的 `rm -rf` 会杀掉新 process 的成功 clone → 改为串行 + 锁文件
- 关键澄清（用户提到的"JS/TS 实现"）：
  - **没有"纯 JS runtime 重写"先例**。所有 JS/TS 实现都依赖 C 编译产物（WASM/NAPI）。
  - 现有 JS/TS 实现：`web-tree-sitter`（WASM 包装）、`tree-sitter-cli`（Rust + WASM 调用）、`node-tree-sitter`（C++ N-API）
- 写了两份 analysis（`ai-dev/analysis/2026-09/`）：
  - `2026-09-07-pure-java-tree-sitter-feasibility.md` — 路线对比（A 纯 Java / B JNI 增强 / C 混合）+ 阶段化路线图
  - `2026-09-07-tree-sitter-runtime-architecture.md` — runtime 12 个子模块的 C/Go 对照表 + 移植设计点
- 结论（open，待用户决策）：
  - 推荐 **路线 C 混合**：短期用 `tree-sitter-ng` 解决需求；长期按 gotreesitter 模式渐进做纯 Java runtime
  - gotreesitter 的 `cmd/ts2go`（≈1800 LOC）证明 parse table 抽取 + 纯语言 runtime 完全可行
  - 关键风险：GLR 实现细节、external scanner VM、GC 压力（ast-grep 教训：早期 arena 内存爆炸）
- 决策点待用户确认：路线选择 / 目标 JDK / 首要 grammar / 模块归属 / 是否回贡献上游
- doc-sync：`docs-for-ai/` 无需更新（这是新项目调研，不属于现有 Nop 模块改动）

## 15:00 nop-vertx-mqtt-server：认证与保活回调携带 clientId/connection（向后兼容的 additive 接口扩展）

- 来源：nop-app-iot plan `docs/plans/2026-09-07-1410-1-mqtt-access-auth-redis-status.md`（M2.1~M2.3 MQTT 设备接入层）
- 变更：
  1. `IMqttAuthChecker` 新增 default 四参重载 `checkAuthAsync(clientId, userName, password, conn)`，缺省委托原三参方法；`VertxMqttServer.handleEndpoint` 改调四参版本并传入 `endpoint.clientIdentifier()`。原因：原签名不携带 clientId 且 conn 恒为 null，应用无法按 clientId=deviceKey 解析认证主体（IoT 设备接入的主身份是 clientId）。
  2. `IMqttHandler` 新增 default `onPing(IMqttConnection conn)`，缺省委托无参 `onPing()`；`MqttConnection.handlePing` 改调 `handler.onPing(this)`；`VertxMqttServer.wrapHandler` 同步转发。原因：保活 ping 原先无法定位设备，应用侧无法做 lastseen 刷新（设备状态 Redis 快路径）。
  - 两处均为新增 default 方法 + 调用点切换，既有实现（`SimpleMqttAuthChecker`、测试内匿名实现与 NoopHandler）无需修改，行为不变。
- 验证：`mvn install -DskipTests -pl nop-network/nop-vertx/nop-vertx-mqtt-server` exit=0；模块 `mvn test` exit=0（TestMqttServerFix / TestMqttConnection / TestMqttMessageRouting / TestMqttTopicMatcher 全绿）。
- doc-sync：接口 javadoc 内已说明契约；无其他 owner doc 需更新。

### 2026-09-07 TestJsDate CI UTC 时区脆弱性修复

- **做了什么**：CI 失败 `io.nop.xlang.expr.TestJsDate#testJsDateJavascriptMethods` 第 95 行 `assertTrue(offset != 0)`——GitHub Actions runner 默认时区 UTC，`JsDate.getTimezoneOffset()` 返回 0，断言失败。采用 Option A：在 `TestJsDate` 的 `setUp/tearDown` 锁定 JVM 默认时区为 `Asia/Shanghai`（与文件内已有 `2024-01-15 10:30:00 UTC+8` 注释语境一致），并在 `@AfterEach` 还原原始时区，避免污染同 JVM 内其他测试。
- **影响范围**：仅 `nop-kernel/nop-xlang/src/test/java/io/nop/xlang/expr/TestJsDate.java`（新增 `import java.util.TimeZone`、新增 `originalTimeZone` 字段、扩展 `setUp/tearDown`），生产代码与公共 API 不变。
- **跨测试污染研判**：`@AfterEach` 由 JUnit 5 保证执行（即使测试方法抛异常）；`BaseTestCase` 不管理时区；其他测试若仅在 `Asia/Shanghai` 下被影响，需要单独看。`testJsDateConstructors` 的字符串构造/数字构造在 Asia/Shanghai 下与 UTC 下解析的 epoch 一致（字符串按系统时区解释、Calendar 反算本地日期）；`toISOString` 用 `Instant.ofEpochMilli` 与时区无关——所以测试内其他断言不受影响。
- **验证**：`./mvnw -pl nop-kernel/nop-xlang -Dtest=io.nop.xlang.expr.TestJsDate` → `Tests run: 3, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。
- **顺带验证**：完整 `nop-xlang` 测试套件 708 用例 / 0 failure / 5 error。**5 个 error 全部来自 `TestXScript`（`try-catch.test.md` 中 `e.name/e.stack` 反射访问 + `js-collections.test.md` 中 `LinkedHashSet.delete`）—— 与时区无关**；通过 `git stash` 回滚本修改后重跑 `TestXScript` 同样 5 个 error 复现，确认是 master 既有问题（`NopException` 反射 `name`/`stack` 属性 + JS 集合 `delete` 方法绑定），与本次改动无关。
- Doc-sync: No owner-doc update required（仅为测试代码本地化时区断言，不改变生产行为）。

### 2026-09-07 GitHub Actions 弃用警告修复（setup-java v4 → v5）

- **做了什么**：CI 日志出现 GitHub runner 弃用警告，迁移 `.github/workflows/maven.yml` 三处 `actions/setup-java@v4` 到 `@v5`（`build`、`invariant-gate`、`e2e` 三个 job 的 JDK 21 步骤）。v5 与 v4 配置完全兼容（`java-version`/`distribution`/`cache` 参数不变），无需调整 `with:` 块。
- **Node 20 警告研判**：用户贴出的另一条警告是 GitHub runner 在节点默认版本升级到 Node 24 后对仍依赖 Node 20 的工作流给出的提示。当前 `maven.yml` 与 `compliance.yml` 所有 Node 步骤都已显式设置 `node-version: 22`（LTS），新运行不会再触发此警告；属于 runner 自身的提示信息，不需要业务侧代码改动。
- **影响范围**：仅 workflow 文件；Java 工具链、构建脚本、CI 逻辑不变。
- **验证**：maven.yml YAML 语法肉眼校验通过（缩进/列表/with 块结构完整），grep 确认 3 处 setup-java 全部升到 v5，且无遗留 setup-java@v4；其余 action 版本（`actions/checkout@v4`、`actions/setup-node@v4`、`actions/upload-artifact@v4`、`pnpm/action-setup@v4`）按各自当前稳定线保持不变。
- Doc-sync: No owner-doc update required（仅为 CI 配置维护，不改变平台行为或约定）。
<!-- 以下为 feat-nop-treesitter worktree 的执行日志 -->


### 2026-09-07 计划 2026-09-07-2228-1-glr-graph-stack-java-corpus Phase 3+4 完成：GLR 引擎 + Java corpus 100%（roadmap item 5 收口）

- **做了什么**：`io.nop.treesitter.parser.glr.GLRParser`（graph-structured stack，C runtime `parser.c`/`stack.c` 语义翻译）：版本化栈（version=head GSS node）、冲突组全部 action 分叉（fork）、state+position 收敛合并（condense + `stack_node_add_link` 语义：等价 link 去重保留高动态优先级、可合并 target 递归合并、MAX_LINK=8）、`ts_parser__reduce` 语义（popCount 按路径切片、同版本 slice 用 select_children 选子、trailing extras 重推、无 goto 时丢弃版本、版本数上限）、accept 选树（动态优先级 → `ts_subtree_compare` 结构序，**取更早树**）、keyword→word 回退（C advance 循环语义，回退后 shift 用回退符号）。`TSParser.parse` 全部改走 GLR（JSON corpus 7/7 字节级回归通过；旧线性 Parser 删除）。`Subtree`/`SubtreeArena` 内联子节点 4→8（对齐 C runtime 内联上限），>8 保留容器链；`TSTree` 新增 `toSexpString(includeFields)`（`ts_node_string` 单行格式：fields/aliases/invisible 扁平化/field 传播，chain container 对结构索引/字段/别名透明）。`ParserOptions(preferShift)` 可配置策略。JavaCorpusTest：上游 `tree-sitter test` 同款比对（expected whitespace 归一化单行 sexp vs `toSexpString`，含 field 标注），**108/108 sections 100% 通过**（≥90% 门槛远超标）。
- **关键决策（GLR 版本复用与 arena 增长）**：GSS 节点按版本 push 创建、condense 合并收敛版本（同 state+position）、reduce 内 slice 复用 stop-node 版本、版本数上限 6（C `MAX_VERSION_COUNT`）；arena 侧表（size/dynPrec）按 id 记录、GLR 不 free 子节点（共享路径安全）；stress fixture（200× 类定义，~200KB）arena 增长线性（`GLRParserTest.arenaGrowthStaysBoundedOnStressFixture` 断言）。动态优先级按 C 语义累计进父节点，select 比较用 `ts_subtree_compare`（symbol→childCount→children 自末向前）——修正了初版"取更晚树"的反向比较（与 `foo.bar::method` method_reference 子选择、`String.class` class_literal(dyn 17) vs field_access 的选择一致）。
- **关键 bug 修复（均在 corpus 驱动下）**：① popCount 双反转导致子序颠倒；② shift token size 漏算 SKIP padding；③ 版本失败应 halt 而非 throw（fork 死路）；④ lex 失败 halt 版本；⑤ slice 共享版本缺 select_children（method_reference 子选择）；⑥ 迭代器 LIFO→FIFO（C `stack__iter` 批次序）；⑦ trailing extras 扫描方向+重推索引（注释归位）；⑧ parentSize 扣 trailing extras；⑨ chain container 破坏 field/alias 结构索引（渲染透明化）；⑩ keyword 回退后 shift 用回退符号（record pattern `int _`）；⑪ addLink 等价 link 缺动态优先级替换（class_literal 胜出）。
- **验证**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS，模块 114 测试 0 失败（GLRParserTest 8 + JavaBlobTest 7 + JavaCorpusTest 1 新增，114 = 105+9）；workspace 268 测试绿；`test-compile`、`clean package -DskipTests` 绿；`checkstyle:check -Pqa` 0 violations（M1 判定方式）。**Java corpus 108/108 (100%)**，**JSON corpus 7/7**。
- **偏差**：① 计划引用 gotreesitter glr.go 作为翻译参考，但其已演化远超 scope（recovery/incremental/pooling），实际以 C runtime `parser.c`/`stack.c` 为语义基准（corpus 验证目标），gotreesitter 仅作 policy 参考；② "prefer-shift by default (C-runtime-compatible)" 与 C 实际"冲突组全部分叉"矛盾——C 兼容默认 = fork-all，prefer-shift 作为可配置选项（`ParserOptions`）默认关闭；③ plan 所述 "~108 sections" 实为 108 且 100% 通过；④ corpus 比对采用上游 `tree-sitter test` 的归一化单行 sexp 格式（含 field 标注开关），非多行 toSExpression。
- Doc-sync: `No owner-doc update required`（模块内部；`blob-format.md` 已在 Phase 1 更新）。

### 2026-09-07 计划 2026-09-07-2228-1-glr-graph-stack-java-corpus Phase 1 完成：blob v2（field/alias/lexer-automaton sections）

- **做了什么**：vendored Java grammar（`tree-sitter-java/src/parser.c` 2.5MB + 6 个 corpus 文件，108 sections，`src/test/resources/upstream/grammars/tree-sitter-java/`）。blob 格式升级 v2（header 96B + 16 sections）：新增 field names / field map slices+entries / alias sequences（row-major prod×max_len）/ non-terminal alias map / 序列化 lexer automata（每 lex fn：state_count、accepts（symbol+at-entry 标志）、per-state 有序 transitions（DNF clause/literal 编码：CHAR_EQ/CHAR_NEQ/RANGE/SET/NONZERO/EOF + skip 标志）、TSCharacterRange sets）；alias symbols（如 Java `alias_sym_type_identifier=320`）并入 symbol names/metadata 表。`ParserCExtractor` 从 `ts_lex`/`ts_lex_keywords` 函数体机械解码 DFA（case 块 → 语句流：`if(cond) ADVANCE/SKIP`、`ADVANCE_MAP`、`ACCEPT_TOKEN`；condition 递归下降解析 → DNF；支持 `('a'<=c && c<='b')`、`set_contains(name,size,lookahead)`、`eof`、`c!=0`、`'c'`/`0x..` 字面量；不可解码结构抛带 state 上下文的 IllegalStateException），并提取 `keyword_capture_token`。`BlobWriter`/`BlobReader`/`Language` 同步扩展（v1 blob 拒绝：typed "unsupported blob format version"）。**运行时 Lexer 换为通用表驱动驱动**（C 语义：accept at-entry/at-exit、ordered first-match transitions、SKIP→token padding、UTF-8 codepoint 解码、keyword capture：identifier→keyword 重扫+`has_actions` 门控；`Lexer.next(lang,src,pos,parseState)` 新签名 + `Lexer.lex` token 级入口）。Parser 增加 keyword→word 回退（C advance 循环的 from-keyword switch）。JSON blob 重新生成并提交（v2, 4227 bytes）；Java blob 提取验证（414KB, 1385 states, 320 symbols + alias 320=type_identifier, 40 fields, 208 productions, kw capture=1, lexer 194/299 states）——Phase 2 的 CLI 产物先用于自检。
- **关键决策（keyword-capture 模型）**：Java 关键词识别采用 C runtime 模型——`keyword_capture_token = sym_identifier`（initializer `.keyword_capture_token = sym_identifier` 证据）+ `ts_lex_keywords` 完整 DFA（always start state 0，与 C `ts_parser__call_keyword_lex_fn(..., 0)` 一致）+ 词法完成条件 `has_actions(parse_state, keyword_symbol)`；blob v1 的 per-parse-state keyword-lex-mode 数据保留编码但不被 runtime 消费（C runtime 同样硬编码 0）。与 blob v1 keyword-lex-mode 模型的差异记录于此。
- **验证**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS，96 测试 0 失败 0 错误。**JSON corpus 7/7 字节级通过**（lexer driver swap 回归门）；BlobRoundTripTest 新增 v2 sections round-trip（field/alias/automaton 逐字段相等）+ v1 拒绝；LexerTest 表驱动 token 断言；`test-compile`/`clean package -DskipTests` 绿。
- **偏差**：① 运行时 Lexer API 改为 parse-state 入口 `next(...)`，token 级测试改用 `lex(...)`（行为不变）；② 计划所述 "25 tokens lexed" 与真实 `TOKEN_COUNT=138`（Java）不符，以 parser.c 常量为准。
- Doc-sync: `src/main/resources/blob-format.md` 重写为 v2（No owner-doc update required beyond 该文档，模块内部契约）。

### 2026-09-07 计划 2026-09-07-1713-3-lr1-parser-json-corpus 执行完成（roadmap item 4 收口，Phase 1-4 全绿）

- **做了什么**：JSON grammar 端到端落地——`Language` blob loader（`io.nop.treesitter.language`，按 blob-format.md 独立解码 9 个 section，校验 magic/version/交叉计数，与 codegen `BlobReader` 交叉验证一致）、`Lexer`（`io.nop.treesitter.lexer`，JSON ts_lex DFA 逐状态翻译为 Java：lex state 0 常规 token / 1 串内容，字节偏移跟踪，无接受即抛 lex error）、`Parser`（`io.nop.treesitter.parser`，LR(1) 线性栈驱动 parse table，语义对齐 C runtime：reduce 弹 `child_count` 个非 extra 条目并把其间 extra 扫入子节点、trailing extras 重新压栈、repeat 规则嵌套 aux 节点、初始 state 1、end token 按 extra 处理；>4 子节点经保留符号溢出链拆分，可见树形不变）、`TSTree`（不可变快照 + `toSExpression()`，corpus 格式：只印 named+visible 节点、隐藏节点透明展开、2 空格缩进）、`TSParser.parse(language, source)` 单一接线路径。上游 `main.txt`（7 sections）vendored 到 `src/test/resources/upstream/grammars/tree-sitter-json/test/corpus/`。
- **验证**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS，91 测试 0 失败 0 错误（新增 LanguageTest 14 + LexerTest 12 + ParserTest 11 + JsonCorpusTest 1）；`test-compile`、`clean package -DskipTests` 绿；`checkstyle:check -Pqa` BUILD SUCCESS（0 violations 判定）。**JSON corpus 7/7 全部字节级通过**（Arrays / String content / Top-level numbers / Exponents / Top-level null / Comments / Multiple top-level objects）。另与真实 tree-sitter CLI（0.24.4）逐节点对比 34 个 corpus 外输入（前导/尾随/成簇 comment、>4 子节点溢出、深层嵌套、UTF-8、`\u` 转义、`[01]` 等）全部一致（`[01]` 差异为上游 ERROR recovery 行为，属 roadmap item 11，本计划明确不做）。
- **关键决策**：① runtime 不复用 codegen `BlobReader`，Language 独立实现 blob 解码（格式文档"可从文档单独实现"契约 + 双实现交叉验证）；② blob 不含 lex 自动机（上游在 ts_lex 函数内），JSON 两态 DFA 翻译自 vendored parser.c；③ 溢出链容器用保留符号 id = `symbolCount()`（不可见、sexp 透明），避免改 plan-2 arena 契约。
- **偏差**：① 计划正文 "14 test sections" 为误计（`====` 分隔线 2 行/section），vendored 文件实为 7 sections，全部通过；② "truex lexes as identifier" 在 JSON 中无 identifier token（keywordLexModeCount=0），实现为整词扫描后 lex error（与上游 token 级行为一致：`true` + 对 `x` 报错），测试按此断言；③ checkstyle 不带 `-Pqa` 时全仓基线失败（nop-api-core 9225 项），模块级警告与 plans 1-2 既有文件同类。
- Doc-sync: `No owner-doc update required`（模块内部；公共 API 文档归 roadmap item 14）。

### 2026-09-07 计划 2026-09-07-1713-2-subtree-arena build verify pass（roadmap item 3 收口）

- **做了什么**：本计划全部 3 个 Phase 及 exit criteria 已勾选；roadmap work item 3 `todo` → `done`（`ai-dev/backlog/nop-treesitter-roadmap.md`）。
- **验证**：`./mvnw -pl nop-treesitter -am test-compile -T 1C` 通过；`./mvnw -pl nop-treesitter -am clean package -DskipTests -T 1C` 通过；`./mvnw -pl nop-treesitter -Pqa checkstyle:check` 0 violations；`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS，53 测试全绿（SubtreeTest 4 + SubtreeArenaTest 13 + SymbolTableTest 6 + SubtreeArenaIntegrationTest 1 + 既有 29）。
- **偏差**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码非 0（32 errors），但全部为本仓库**既有**失效链接（roadmap 对未落地文件的前向引用 `2026-09-XX-*`、`docs-for-ai/03-modules/nop-treesitter.md`、`ai-dev/analysis/2026-09/` 下不存在的 2 个文件，及 `docs-for-ai`、`2258-xlang-...` 等其它 owner 文档）；本次改动新增 0 条失效链接，本计划文件不在报告内。按"diff 最小化、不越权改其它 owner 文档"原则未回写这些前向引用，待对应 roadmap item 落地时自然消解。
- Doc-sync: `No owner-doc update required`（模块内部数据结构；公共 API 文档归 roadmap item 14）。

### 2026-09-07 计划 2026-09-07-1713-2-subtree-arena Phase 3：ARENA-04 测试套件 + 下游消费集成证明

- **做了什么**：补全 ARENA-04 聚焦套件（SubtreeTest 4 + SubtreeArenaTest 13 + SymbolTableTest 6，共 23 个聚焦测试，覆盖 allocate/reuse/grow/intern/equality/parent-arity）与端到端接线证明 `SubtreeArenaIntegrationTest`（深度 3、混合 arity 的嵌套树，节点类型全部来自 `SymbolTable`，仅经 arena 公共 API：`allocate` → `get` → children/siblings/parent 遍历 → 结构断言；8 个节点，capacity 4→8 一次增长）。
- **Smoke profile 注记（roadmap item 13 参考，不做调优）**：每节点列式占用 ≈ 8 int 列 × 4B + parent 4B + live 位 ≈ 37B/节点（整列满员时），vs 堆上 8-int record 对象 ≈ 48–64B/节点 + GC 跟踪；线性解析树的 id 与节点一一对应、无堆对象分配，热路径仅 int[] 写。粗估、未调优。
- **验证**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS，53 测试全绿（新增 IntegrationTest 1）。
- Doc-sync: `No owner-doc update required`（模块内部数据结构）。

### 2026-09-07 计划 2026-09-07-1713-2-subtree-arena Phase 2：SymbolTable 符号驻留

- **做了什么**：新建 `io.nop.treesitter.util.SymbolTable`（`intern(String)→int` / `resolve(int)→String`，同一字符串稳定映射同一 id，id 从 0 起顺序分配不冲突，ArrayList 自动增长；`resolve` 对未驻留 id 抛 `TreeSitterException`，`intern(null)` 抛 `IllegalArgumentException`）。
- **验证**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS，52 测试全绿（SymbolTableTest 6 新增）。
- Doc-sync: `No owner-doc update required`（模块内部工具，公共 API 文档归 roadmap item 14）。

### 2026-09-07 计划 2026-09-07-1713-2-subtree-arena Phase 1：Subtree 值类型 + SubtreeArena 核心

- **做了什么**：新建 `io.nop.treesitter.subtree.Subtree`（record，8 个 int 字段 state/symbol/child0..3/extra/padding，值相等 + parent-arity 语义：child 槽位必须稠密、`NO_ID=-1` 哨兵、由稠密槽位推导 `childCount()`）与 `SubtreeArena`（8 个并行 `int[]` 列 + `live[]`/`parent[]`/自由链表簿记；`allocate(state,symbol,extra,padding,int... children)` 校验子节点必须为 live id，空闲表 LIFO 复用槽位、几何增长（×2，初始容量 4）保留既有内容；`free(id)`/`get(id)`/`parentOf(id)`/`isLive`；无效/从未分配/已释放 id 抛 `TreeSitterException`）。模块级异常 `io.nop.treesitter.TreeSitterException`（英文消息，无静默跳过）。
- **关键决策（roadmap ARENA 关键设计）**：采用并行 `int[]` 列而非每节点堆对象——热路径按 id 寻址零分配，`get(id)` 才物化 record 值快照。
- **验证**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS，46 测试全绿（SubtreeTest 4 + SubtreeArenaTest 13 新增）。
- Doc-sync: `No owner-doc update required`（模块内部数据结构，公共 API 文档归 roadmap item 14）。

### 2026-09-07 计划 2026-09-07-1713-1-ts2java-extractor 收口（build verify pass）

- **做了什么**：nop-treesitter 模块的 `ts2java` parse-table 提取器 + JSON grammar blob 端到端落地（计划 `ai-dev/plans/nop-treesitter/2026-09-07-1713-1-ts2java-extractor.md`，roadmap item 2）。`io.nop.treesitter.codegen` 包包含 `Ts2Java` CLI（main 接受 `<parser.c> <out-blob>`）、`ParserCExtractor`（提取 symbol table / metadata / parse actions / 大小 state table / primary state ids / lex modes / keyword lex modes）、`BlobWriter`（紧凑 big-endian 二进制编码，u16 宽度、越界抛 IllegalStateException）、`BlobReader`（独立最小 reader，校验 magic+version）、`ExtractedGrammar`。上游 `tree-sitter-json/src/parser.c` vendored 到 `src/test/resources/upstream/grammars/tree-sitter-json/`。blob 契约文档 `src/main/resources/blob-format.md`（header 64B + 9 sections，version 1）。端到端 CLI 产出并提交 `src/main/resources/grammars/json/tree-sitter-json-blob.bin`（2260 bytes, 32 states, 25 symbols）。
- **验证**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS；nop-treesitter 29 个测试全绿（bootstrap 2 + Ts2JavaLexModesTest 7 + Ts2JavaExtractionTest 10 + BlobRoundTripTest 10），268 commons 测试 0 失败 0 错误。`checkstyle:check` 通过。
- **偏差**：无。全部 Phase 1-3 及 exit criteria 均已勾选，本条目为 build-verify 收口记录。
- Doc-sync: `blob-format.md` 已新增（loader 契约，供计划 ...-3 消费）；`No owner-doc update required`（模块内部提取逻辑，平台文档不变）。
