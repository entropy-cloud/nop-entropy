# 2026-09-04

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1043-1 item 35 closure——首轮 audit REJECTED 修复后复审 APPROVED，plan completed）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-1043-1-parallel-2pc-sink-gate-removal.md`（item 35，**completed**，Completed: 2026-09-04）
- **Closure audit 首轮**（独立 general subagent fresh session `ses_f9567e2d7ffeiQy2RngPXbR7c5`）：**CLOSURE-AUDIT: REJECTED**——代码/证明/Anti-Hollow/一致性全 PASS（Phase 1-3 Exit Criteria 逐条 live 核验 PASS；接线全链追踪连通），但 Phase 4 发现 **F1 Major**：`docs-for-ai/03-modules/nop-stream-connectors.md:33` 单一事实源段仍留**活态现在时**旧不变式陈述（「`PLANNING_GATE_PARALLELISM_1` ⟺ 2PC 实例（与规划期门禁同键）」——plan 的 rg 退出模式只查 4 个字面量，措辞变体逃逸）；F2/F3 Minor = `connector-design.md` §8.4 枚举清单 + §8.9 D4 摘要行同族残留；F4 Minor = gated surefire 产物被后续默认态套件覆写（Skipped: 1）；F5/F6 Info（未提交工作/依赖 executor mvn 证据）。
- **四项修复**：F1 → 单一事实源段重写为新不变式（2PC ⟹ 声明 PARALLEL + 历史注记废弃）；F2 → 枚举清单改 PARALLEL/SINGLE_INSTANCE（历史值标注已删除）；F3 → D4 行重述；F4 → gated `TestParallel2PcMultiJvmE2E` 复跑（1/1 绿，93.20s，on-disk surefire 恢复 gated 态证据）。
- **Closure audit 复审**（fresh session `ses_f955fb970ffeH2KwDSHFhWktIA`）：**CLOSURE-AUDIT: APPROVED**——F1-F4 四项修复逐项 live 核验 PASS；`PLANNING_GATE_PARALLELISM_1`/错误码/门禁词三向 grep 扫描全部命中均为 supersession/历史表述（ai-dev/logs 命中为过程记录、stream-dsl-design「CONN-01 successor 后无规划期门禁」为正确的现状断言）；check-doc-links --strict exit 0。
- **收口仪式**：Phase 4 roadmap 写回项 + Closure Gates 12/12 勾选 + Closure evidence 双 session 写入 → Plan Status `completed`；roadmap item 35 → `done`（验收摘要含首轮 REJECTED→复审 APPROVED 全程 + 双形态证明锚点）+ Last updated 同步；`check-plan-checklist --strict` exit 0。
- **终态验证**：默认态 `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（×2）；gated 多 JVM 1/1 绿（93.20s 留档）；`check-nop-stream-invariants`（wiring pin re-pin :712→:714 后）/`scan-hollow-implementations`（4 模块 high，0 findings）/`check-doc-links` 全 exit 0。
- **文本一致性核对（规则 19）**：Plan Status completed ↔ 四 Phase completed 且 checklist 全 [x] ↔ Closure Gates 12/12 ↔ Closure evidence（双 audit session + 逐条 PASS 锚点）↔ roadmap item 35 done + Last updated ↔ 本日志五条目（Phases 1/2/3/4/收口）——七处一致。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1043-1 item 35 Phase 4——owner docs 全量 supersession 写回）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-1043-1-parallel-2pc-sink-gate-removal.md`（roadmap item 35，Phase 4）
- **design docs**：`checkpoint-design.md` §6.4.1 整节重写为已落地终态（公开契约「P=N 已证明（LOCAL+多 JVM）」+ 原B1/B2/B3 落地机制映射 + 三裁定引用 + 恢复语义段）；§8.5 表 sink pending transaction 行同步（跨并行度 typed 拒绝 + same-P 1:1 重提交）。`comparison.md` §7.3 Transaction 身份行（P=N 已证明 + 台账复合键/`.sK` 后缀）。`stream-dsl-design.md` §5.2 生效链句（2PC sink 同链消费声明并行度，跨并行度恢复归 checkpoint 语义非声明面）。`connector-design.md` §8.4 矩阵两行 PARALLEL + 单一事实源不变式重述（2PC ⟹ PARALLEL 单向）+ 核对口径行。`distributed-runbook.md` §7 门禁条目 → 已解除 + 新边界（跨并行度恢复 typed 拒绝）。`composite-scenario-design.md` A.0-5/A.0-D#7 路由注记 supersession（原文留档 + 新承载测试指针）。
- **docs-for-ai**：`nop-stream-connectors.md` 矩阵 file/jdbc-2pc 行「并行度」列 → 支持并行（附 per-subtask 机制 + 证明测试锚点）+「硬门禁」节重写为「并行 2PC 能力说明」（机制/子类契约/跨并行度恢复/钉定测试）；`nop-stream-user-guide.md` per-transform 并行度句 + 连接器指引门禁句 → 能力说明；`nop-stream-migration-guide.md` 新增「JDBC 2PC 台账 schema」节（复合主键 DDL + D2 遗留重建口径 + 跨 sink 共享约束）+ 决策速查两行（2PC 并行度变更例外 + 遗留台账重建）；`04-reference/source-anchors.md` STRM-033/036/049 行同步。INDEX.md 无需变更（路由未变，矩阵/能力描述在既有页内更新）。
- **测试 javadoc supersession**：`TestS2RecoveryAndRescaleE2E`（A2-4 注记）、`TestS2RestoreRescaleMultiJvmE2E`（矩阵裁定注记）、`ThrottledScenarioSinks`（并行度句）——均 supersession 注记 + 原文留档 + 新承载测试指针。
- **验证**：`rg "CONN-01|PARALLELISM_NOT_SUPPORTED|规划期门禁 P=1|仅 parallelism=1 已证明" docs-for-ai ai-dev/design nop-stream/*/src` 全部命中均为 supersession 注记/历史留档/新能力描述（唯一 2 处未带注记词的命中 = 新测试 javadoc 中「used to reject」历史描述，属 supersession 表述）；`check-doc-links --strict` exit 0；fraud-example `-DskipTests` 编译绿（javadoc 变更）。
- Doc-sync: 本 Phase 即 doc-sync 本体；roadmap item 35 写回在 closure audit 后执行（Plan Status 置 completed 前）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1043-1 item 35 Phase 3——双形态端到端证明 LOCAL + 真实多 JVM）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-1043-1-parallel-2pc-sink-gate-removal.md`（roadmap item 35，Phase 3）
- **场景资产**：3 份 test-only XDSL（`fraud-parallel-2pc-jdbc.stream.xml` P=2、`...-jdbc-p3.stream.xml` P=3（D1 拒绝变体）、`fraud-parallel-2pc-file.stream.xml` P=2）——拓扑 = 有界 file source(P=1) --HASH keyExpr--> pass-through map(P=N) --FORWARD--> 2PC sink(P=N)（map+sink 链成单 P=N 顶点；HASH 边承重：FORWARD 会把 srcP=1 流量集中到 subtask 0）。共享 plumbing `ParallelTwoPhaseCommitScenarios`（全 named serializable beans：`PassThroughMap`/`LineRowMapper`/复用 `DirectoryFileSourceFunction`；LOCAL 与 distributed 同 bean 形态；数据表 `p2pc_out` 无主键——重复 slip 必须在 multiset 比较中暴露而非 PK 违例）。
- **LOCAL JDBC e2e**（`TestParallel2PcJdbcE2E`）：C2 路由形态（keyed-P>1 + jdbc-2pc P=2）XDSL `env.execute()` 到 JDBC 表行——run 1（slice A 30 keys）multiset 精确 + durable manifest 存在 + **台账 ∃ epoch 由 2 个不同 subtask_id 提交**（per-subtask 复合键 (epoch_id, subtask_id) 的直接行为证据，接线验证 #23）；run 2（写 slice B 后同 P 恢复续算）全量 multiset exactly-once + ledger epoch 严格递增；run 3（P=3 变体对 P=2 checkpoint）typed 拒绝（`ERR_STREAM_2PC_SINK_PARALLELISM_CHANGE_UNSUPPORTED`，old=2/new=3/vertexId 参数；env.execute 外层 `job-execute-failed` 包装经 cause 链断言——沿 `TestXplSourceCancelE2E` 既有 unwrap 模式；fingerprint 守卫不拦截：兼容检查只比对 dagTopology/requirements/participants，per-component 含 parallelism 的 hash 仅为 informational）。
- **LOCAL File e2e**（`TestParallel2PcFileE2E`）：P=2 file sink——∃ epoch N 同时有 `epoch-N.txt`+manifest key `N`（subtask 0 legacy 名）与 `epoch-N.s1.txt`+key `N.s1`（subtask 1 后缀）共存（无互相覆盖）；输出文件集精确（仅 committed epoch 文件，manifest 键集 == 文件集，无 tmp 残留）；两 run（slice 分区 + 恢复续算）后全部 committed 行 multiset exactly-once。
- **DISTRIBUTED gated 多 JVM**（`TestParallel2PcMultiJvmE2E` + `Parallel2PcScenarioPipelineFactory`）：MiniStreamCluster 2 TM 真实 JVM + 共享 H2 AUTO_SERVER 台账/数据表——kill tm-1（真实 SIGTERM，kill 前置条件 = durable manifest + 已提交行）→ 替换 TM → fencing epoch 严格递增（re-assignment 随新代）→ 数据表收敛全量 multiset exactly-once → 共享台账 ∃ epoch 2 subtask_id（跨 JVM per-subtask 提交键）。**sink 族 × 执行形态覆盖矩阵**（javadoc 留档）：JDBC 族 = LOCAL + 真实多 JVM 双形态；File 族 = LOCAL-only 显式标注（多 JVM 需共享输出目录文件系统契约，出本 plan 范围；JDBC 族承载多 JVM 义务——台账在共享 DB 中跨 JVM 可证）。
- **执行偏差（一处）**：JDBC e2e 首版两 run 全量输入 up-front（S2 模式）——run 2 从 final manifest 恢复后无可续算输入，「epoch 严格递增」断言失败（19 vs 19，无缺陷，恢复正确地无事可做）；改为 S1 时间分区模式（run 1 = slice A，run 2 前写入 slice B）使恢复续算真实发生。File 测试同步采时间分区。
- **验证**：focused `TestParallel2PcJdbcE2E,TestParallel2PcFileE2E` → 2/2 绿；gated `TestParallel2PcMultiJvmE2E -Dnop.stream.test.multi-jvm.enabled=true` → 1/1 绿（82.57s，surefire 留档）；默认态 `./mvnw test -pl nop-stream -am -T 1C` → **BUILD SUCCESS**（1:58；途中一次 `TestRocksDBIncrementalRestoreAndBenchmark` 性能断言 flake——机器负载敏感的 ratio 基准，与本次改动无关（零 rocksdb 文件触碰），隔离复跑即绿，全量复跑绿）。
- Doc-sync: No owner-doc update required（本 Phase 交付测试证明；owner docs supersession 归 Phase 4）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1043-1 item 35 Phase 2——门禁解除 + 能力面翻转 + D1(a) typed 拒绝落地）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-1043-1-parallel-2pc-sink-gate-removal.md`（roadmap item 35，Phase 2）
- **门禁移除**：`StreamGraphGenerator.transformSink` 删除 2PC 并行度 fail-fast 分支（含 `TwoPhaseCommitSinkFunction`/错误码/ARG 常量三处 import 清理）；残留注释指向 D1 的 restore-time 拒绝与 §6.4.3 子类契约。
- **能力面翻转**：`ConnectorParallelism.PLANNING_GATE_PARALLELISM_1` 枚举值删除（剩 `PARALLEL`/`SINGLE_INSTANCE`），javadoc 改写为「2PC 端点声明 PARALLEL + copyForSubtask 契约」；两内建工厂（`JdbcTwoPhaseCommitSinkConnectorFactory`/`FileTwoPhaseCommitSinkConnectorFactory`）声明翻转 → `PARALLEL`；`ConnectorCapabilityDescriptor` javadoc iff 不变式改写（无悬空 `{@link}`）；`StreamConnectorCatalog.probeSink` 一致性校验重述为「2PC 实例必须声明 PARALLEL」（非 2PC 不约束——message/batch 族本就声明 PARALLEL，双向 iff 不再成立）。
- **错误码处置**：`ERR_STREAM_2PC_SINK_PARALLELISM_NOT_SUPPORTED` 删除（main 零引用，`ARG_SINK_NAME`/`ARG_PARALLELISM` 一并移除）；新增 `ERR_STREAM_2PC_SINK_PARALLELISM_CHANGE_UNSUPPORTED`（vertexId/oldParallelism/newParallelism，D1 裁定语义）。
- **D1(a) 实现**：`GraphModelCheckpointExecutor.restoreTaskStatesFromSource` 顶点循环新增检测——`oldParallelism > 0 && oldParallelism != newParallelism && isVertex2PcSink(newSubtasks)` → typed 抛出（新增 `isVertex2PcSink` helper：链内任一算子 UDF instanceof TwoPhaseCommitSinkFunction）；**不 gate 于 `vertexKeyed`**（keyed/非 keyed 双形态覆盖）；置于 keyed-rescale 分支之前、per-subtask 合并之前。同 P 恢复（kill/recover）走原 1:1 路径不受影响。
- **测试更新**：`TestStreamGraphGenerator` 两拒绝用例改为构建通过断言（P=2 全管线 StreamGraph→JobGraph→GraphExecutionPlan + per-subtask 拆分：sink 顶点恰 N subtask、UDF 独立实例、assignedSubtaskIndex==taskIndex；setParallelism 升到 P=3 同口径），`TestTwoPhaseCommitSinkFunction` 补 `copyForSubtask` override（镜像内建契约）；`TestJdbcConnectorFactory`/`TestFileMessageConnectorFactories`/`TestStreamConnectorRegistryDiscovery`（矩阵行 + listing 断言 `delivery=TWO_PHASE_COMMIT, parallelism=PARALLEL, recovery=...`）声明翻转。新增 focused `TestTwoPhaseCommitSinkParallelismChangeRestoreE2E`（4 用例：非 keyed scale-up / scale-down typed 拒绝 + 参数断言、keyed 顶点含 2PC sink 同拒、同 P 恢复成功且每 subtask 独立拷贝参与 restoreFromEpoch）。
- 验证：`./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-connector,nop-stream/nop-stream-connector-jdbc,nop-stream/nop-stream-runtime -am -T 1C` → BUILD SUCCESS（runtime 1024 tests, 0 failures；focused 4/4 + 18/18 + 8/8 + 17/17）；`rg "PLANNING_GATE_PARALLELISM_1" nop-stream --type java` 零命中；`rg "ERR_STREAM_2PC_SINK_PARALLELISM_NOT_SUPPORTED" nop-stream/*/src/main` 零命中；`check-nop-stream-invariants` exit 0（wiring-registry setStateBackend pin :712→:714 行号漂移 re-pin，非行为变更）；`scan-hollow-implementations --severity high` exit 0（0 findings）。
- Doc-sync: Phase 1 已落裁定 owner doc（§8.5.2 错误码语义）；Phase 4 承接矩阵/指南 supersession。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1043-1 item 35 Phase 1——D1/D2/D3 三项语义裁定落档）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-1043-1-parallel-2pc-sink-gate-removal.md`（roadmap item 35，CONN-01 successor，Phase 1）
- **D1 跨并行度恢复语义 → 裁定 (a) typed fail-fast**：`checkpoint-design.md` 新增 §8.5.2——2PC sink 顶点 oldP≠newP 时在 rescale 检测点抛 `ERR_STREAM_2PC_SINK_PARALLELISM_CHANGE_UNSUPPORTED`（vertexId/oldParallelism/newParallelism 参数），检测点覆盖 keyed + 非 keyed 两形态（live `rescale` 布尔 keyed-only，窄读漏非 keyed）。证据：operator state 按 index 1:1 无 k'≠k 移交路径（`GraphModelCheckpointExecutor.buildRescaledTaskState`）；scale-down 旧 subtask（index ≥ newP）pendingCommits 静默丢弃（违反 §6.4 durable-未提交必须 re-commit）；非 keyed scale-up generic `ERR_STREAM_CHECKPOINT_EXECUTOR_RESTORE_FAILED` 无 mismatch 语义。拒绝 (b) 身份保持重分布：与 §8.5 表「sink pending transaction 不许跨 subtask 静默迁移」冲突 + 台账键/输出路径携带 subtask 身份使跨 subtask 重提交复杂度远超收益。
- **D2 台账遗留 schema → 裁定 (b) 文档化重建口径**：`checkpoint-design.md` 新增 §6.4.2——`2de622fb6a` 前单列 ledger 表 DROP 后按复合主键 DDL 重建，不加代码探测。证据：旧表上复合列 INSERT/SELECT 响亮失败（column not found），无静默丢数据路径。拒绝 (a) typed 探测：无已发布版本真实用户为 0 + commit 热路径开销 + P-REQ-21 defer 同口径。操作口径落 `nop-stream-migration-guide.md`（Phase 4）。
- **D3 第三方 2PC 子类面 → 裁定运行时 fail-fast**：`checkpoint-design.md` 新增 §6.4.3——未 override `copyForSubtask(int)` 的子类 P>1 契约 = 基类默认抛 `UnsupportedOperationException`（部署期构建 subtask 拷贝时触发，无静默窗口），规划期不做 opt-in 声明。拒绝规划期声明：程序化挂载不经注册面覆盖不全 + `copyForSubtask` override 即能力标记（声明是第二事实源）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（本 Phase 纯文档裁定，无代码变更，无需 mvn）。
- Doc-sync: `ai-dev/design/nop-stream/checkpoint-design.md` 已更新（§8.5.2 / §6.4.2 / §6.4.3 三裁定，含裁定值、live 证据、拒绝面）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0829-1 item 34 closure——audit APPROVED，plan completed）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0829-1-ha-failover-takeover-assignment-conflict.md`（item 34，**completed**，Completed: 2026-09-04）
- **Closure audit**（独立 general subagent fresh session `ses_f95c2772effeJQlv9Yz0e90MO1`）：**CLOSURE-AUDIT: APPROVED**——三 Phase 全部 Exit Criterion 逐条 live 核验 PASS（focused 5/5 现场复跑绿；Anti-Hollow 全链追踪连通 activateAsLeader → seed（真实 getAttemptHistory 读）→ attemptCounters → prepareAssignmentsLocked → assignTask INSERT，`history empty → continue` 分支为带注释的 fresh-job 语义非静默跳过；三层证据齐备）；4 工具门禁 audit 现场复跑 exit 0；Deferred 空 + follow-up 两项确认 watch-only；偏差（harness 存活判据 HA 化）逐行核对仅判据形态修正、强度不降。Findings：F1 Minor（roadmap APPROVED 表述时序先于 audit——closure ritual 补齐后回溯成立）+ F2/F3 Info。
- **收口仪式**：Closure Gates 12/12 勾选 + Closure evidence 写入 → Plan Status `completed`；`check-plan-checklist --strict` exit 0。
- **终态验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（harness 判据修正后复跑）+ `./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` BUILD SUCCESS（checkstyle 随构建）；`check-doc-links --strict` / `check-nop-stream-invariants` / `scan-hollow-implementations --module nop-stream-runtime --severity high` 全部 exit 0。
- **文本一致性核对（规则 19）**：Plan Status completed ↔ 三 Phase completed 且 checklist 全 [x] ↔ Closure Gates 12/12 ↔ Closure evidence 完整（audit session + 逐条 PASS 锚点）↔ roadmap item 34 done + Last updated ↔ runbook §7 已修复条目 ↔ 本日志四条目（Phases 1/2/3/收口）——七处一致。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0829-1 item 34 Phase 3——CHAOS-2 复验 pass + runbook/roadmap 收口）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0829-1-ha-failover-takeover-assignment-conflict.md`（item 34，Phase 3）
- **CHAOS-2 gated 复验 pass**：`./mvnw test -pl nop-stream/nop-stream-fraud-example -am -T 1C -Dtest='TestStabilityExerciseMultiJvm#chaosJcHaFailover' -Dnop.stream.test.multi-jvm.enabled=true -Dnop.stream.test.multi-jvm.preserve-artifacts=true -Dsurefire.failIfNoSpecifiedTests=false` → **verdict=pass**（runId `1788487981277-1`，473.8s，产物留档 `_tmp/mini-stream-cluster/`）。判据齐备：终态收敛（期望集 43 行完整 + checkpoint 94 次推进 max gap 5007ms + 输出不冻结）+ 既有子判据（两轮租约翻转 c0:1→c2:2→c3:3、双 epoch 严格递增 fencing 1000000→2000000→3000000）。
- **产物核对**：全部 4 个 coordinator 日志 **零** `duplicate-key` / 零 `invoke-become-leader-listener-error`；种子化日志面证据 = c2 接管时 `Seeded attempt counter ... 0 -> 1`（随后分配 attempt 2）、c3 接管时 `0 -> 2`（随后 attempt 3）——attempt 接续跨两轮接管可观察。
- **执行偏差（harness 存活判据修正，唯一代码面偏差）**：首轮复验（runId `1788484060762-1`）在**输出收敛后首次**触到 `assertSoakHealth` 的 `coordinatorAlive()`（= index-0）判据——CHAOS-2 按设计 kill coordinator-0，该判据对 HA cell 形态恒假（pre-fix 被输出冻结掩蔽从未到达）。修正 = `TestStabilityExerciseMultiJvm.assertSoakHealth` 增 `haCoordinatorKills` 形参（HA cell 断「至少一个 coordinator 存活」），判据强度不降（收敛/推进/队列/采样判据原样）。次轮（`1788487394000-1`）又暴露 helper 初版遍历 0..count-1 的稀疏索引 bug（kill 后 map 非稠密）→ 改沿同文件 `waitForFencingRotationAnyCoordinator` 的 0..4 固定范围先例；第三轮 pass。另记录：standby coordinator-1 在 leader 持有 ops 端口 8931 时因 `BindException` 早退（pre-existing 演练装置形态，历轮 CHAOS-2 相同，接管由 kill 后 spawn 的 standby 完成，非本 plan 引入）。
- **中途偶发（非本缺陷）**：一次复跑被 H2 `Lock file recently modified` file-lock 竞态挂起（runId `1788484932810-1`，runbook §7 :160 已记录的 gated 偶发族）+ 一次 `rg|head` SIGPIPE 误杀 mvn 遗留孤儿进程——清理后复跑即过。
- **收口写回**：runbook §7 item-34 条目 → 「已修复（2026-09-04，plan `2026-09-04-0829-1`）」（item 28 先例格式：修复要素 + 原缺陷留档 + 复验 runId `1788487981277-1` + 复现命令保留）；roadmap item 34 → `done`（Work Items 行验收摘要 + Last updated 头）。
- **docs-for-ai 裁定**：`docs-for-ai/03-modules/nop-stream.md` rg 核对无 HA 接管语义条目（命中仅为通道 gauge/manifest checksum 无关项）——**No docs-for-ai update required**（owner doc = `ai-dev/design/nop-stream/01-architecture-baseline.md`，Phase 1 已更新）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0829-1 item 34 Phase 2——多 JVM 接管断言面升级 + 红证 A/B + gated 绿）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0829-1-ha-failover-takeover-assignment-conflict.md`（item 34，Phase 2）
- **断言面升级**（`TestMultiJvmCoordinatorFailover`）：两测试从「租约翻转」升级为「接管完成」——kill 前带 deadline 轮询 assignment 表至行落库（`waitForLeaderAndAssignments` 只查租约行，不足以行使缺陷窗口）+ 记录 per-subtask kill 前最大 attempt；接管后带 deadline 轮询断言①新 leader fencing band（`fencing_token ∈ [epoch×1e6, (epoch+1)×1e6)`，`JobCoordinator.deriveHaFencingEpoch` 换算）内存在 assignment 行②attempt_number 严格大于 kill 前对应 subtask 最大值③coordinator-1 日志无 `duplicate-key`/`Unique index or primary key violation` 且无 `invoke-become-leader-listener-error`。新增 helpers：`scanAssignmentRows`/`waitForAssignmentRows`/`readPerSubtaskMaxAttempts`/`waitForTakeoverAssignmentContinuation`（模式仿既有 `waitForAssignmentEpoch`）。`testBrainSplitFencingBoundary` 同步补①②，epoch 严格递增主体断言不变。
- **红证（A/B 对照，选择性 stash）**：`git stash push -- <JobCoordinator.java>`（只回滚 src/main 修复，保留升级后测试）→ gated 运行 **必红**：`Tests run: 3, Failures: 2`——两测试均以预签名失败（`new leader (epoch 2, fencing band [2000000,3000000) must COMPLETE takeover ... pre-kill max {source/0=1, sink/0=1}`，33-35s 轮询超时）；preserve-artifacts 复跑单测试红（runId `1788483771855-1`，留档 `_tmp/mini-stream-cluster/`）——coordinator-1.log 实证缺陷全链：`nop.cluster.invoke-become-leader-listener-error` + `JdbcException[errorCode=nop.err.dao.sql.duplicate-key, sqlState=23505, sqlName=insertTaskAssignment]`。stash pop 后 **gated 复绿**：`Tests run: 3, Failures: 0`（11.9s）。
- **gated 运行**（含修复，两次）：`./mvnw test -pl nop-stream/nop-stream-runtime -am -T 1C -Dtest=TestMultiJvmCoordinatorFailover -Dnop.stream.test.multi-jvm.enabled=true -Dsurefire.failIfNoSpecifiedTests=false` → 3/3 绿（修复后首次 + pop 后复跑）。
- **默认套件**：`./mvnw test -pl nop-stream -am -T 1C` → **BUILD SUCCESS**（fraud-example 117/0/0/15 skipped 全为既有 gated；断言升级对默认态零影响）。
- Doc-sync: No owner-doc update required（纯测试断言面升级，owner 语义已在 Phase 1 落档）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0829-1 item 34 Phase 1——种子化实现 + focused 测试全绿）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0829-1-ha-failover-takeover-assignment-conflict.md`（item 34，Phase 1）
- **实现**：`JobCoordinator` 新增 `seedAttemptCountersFromRegistryLocked()`（recoveryLock 内、epoch 轮转后、assignment 物化前调用）——按 deployment plan 枚举逐 `(vertexId, subtaskIndex)` 读 `ClusterRegistry.getAttemptHistory`，把内存计数抬到 max(内存, 历史最大)；无历史行 → 保持缺省（fresh-job 行为逐字节等价）；读失败 → **de-active（active=false + currentLeadership 清空）后 rethrow typed StreamException**（裁定行为：elector 吞掉 listener 异常后终态 = 显式 STANDBY，非冻结半激活 leader）。`ClusterRegistry` 接口零变更（批量读便捷面裁定 = 不需要，inventory 零 diff）。
- **三项裁定落档**：`ai-dev/design/nop-stream/01-architecture-baseline.md` 新增「跨 leader 接管的 attempt 连续性 — 种子化裁定（G56 补充，item 34）」节（种子化规则 + 拒绝 upsert/删旧插新及理由 + warm gating 对比择「每次激活全量枚举读」+ 读失败 de-active rethrow + 批量读不需要）+ 控制面角色表 JobCoordinator 行补种子化引用。
- **focused 测试**：新 `TestJobCoordinatorAttemptSeedOnTakeover`（5 用例）：`testTakeoverAssignmentContinuesPresetAttemptHistory`（JDBC/H2 预置 1..3 行 → 新鲜实例激活 → 历史 [1..4] 严格接续、旧行 attemptId/epoch 原样保留（G56 append-only）、新行 fencing=deriveHaFencingEpoch(2,0)、fan-out 3 subtasks 接线证明）；`testFreshJobActivationStartsAttemptsAtOne` / `testSameJvmReactivationContinuesWithoutRegressionOrDuplicate`（回归钉定：fresh 从 1 起；同 JVM revoke+re-grant 后 1→2 不回退不重复）；`testInMemorySharedRegistryTakeoverContinuesMonotonically`（InMemory 对等，严格单调即证明 regression-WARN 分支（attemptNumber<=lastAttempt）未被行使）；`testSeedReadFailureDeactivatesCoordinatorAndRethrows`（#24 钉定：typed 异常 + active=false + 零 assignment + 零 assignTask 调用）。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime -am -T 1C -Dtest='TestJobCoordinatorAttemptSeedOnTakeover,TestJobCoordinatorFailoverRestore'` → 11/11 绿（5 新 + 6 既有 failover-restore）；`./mvnw test -pl nop-stream -am -T 1C` → **BUILD SUCCESS**（全模块，含 fraud-example 117 skipped 全为既有 gated）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` BUILD SUCCESS（checkstyle 随构建通过）。
- 工具门禁：`check-nop-stream-invariants` exit 0——**无 pin 受影响**（rg 零命中：wiring-registry/gate-inventory/output-contract/mjs-pins 四 JSON 均无 JobCoordinator 条目，行号漂移无失配面）；`scan-hollow-implementations --module nop-stream-runtime --severity high` exit 0。
- Doc-sync: `ai-dev/design/nop-stream/01-architecture-baseline.md` 已更新（裁定节 + 角色表行）。

### 2026-09-04（mission nop-stream-productization DRAFT_PLANS：item 34 plan 起草——两轮对抗审查后 active）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0829-1-ha-failover-takeover-assignment-conflict.md`（roadmap item 34，**active**）
- **选题**：roadmap 唯一 `todo` = item 34（HA failover 新 leader 接管中止）；前序 plan deferred 项核查——item 33/32 successor 已 done、其余 follow-up 均 watch-only 备忘（Prometheus 联邦、JDBC prune metrics、`TestMultiJvmExactlyOnceRecovery` flake），无可重触发项，本轮仅 1 份 plan。
- **起草前 live 核验**：缺陷主链逐点核实（`attemptCounters` `JobCoordinator.java:194` 内存唯一真值 / `activateAsLeader` :1860 fresh JVM 从 attempt 1 重发 / `JdbcClusterRegistry.assignTask` :202 纯 INSERT 撞 PK / `getAttemptHistory` :250 种子化读源现成 / gated 接管测试只断言租约翻转 / `MiniStreamCluster` 启动 drop 表解释窗口）。
- **Draft review 两轮（独立 general subagent fresh session）**：
  - 第一轮 `ses_f962cd7a7ffemHvOAmPs70B13`：**1 Blocker**——异常吞点实为平台 `AbstractLeaderElector.onBecomeLeader`（nop-cluster-core :183-191）逐 listener 吞掉（无 standby 降级/无重选举/无重试；`active=true` 已先置位 → 终态 = 冻结半激活 leader；演练产物 `1788457688118-1/logs/coordinator-2.log` 实证 `invoke-become-leader-listener-error` 后 elector 继续 leader 身份刷租约）——runbook §7 外推的「listener 异常 → standby」机制不成立，plan 机制叙事 + Phase 1 #24 裁定项 + Non-Goals 安全网表述全部重写，新增「种子化读失败行为」显式 Decision；**1 Major**——Phase 2 缺陷窗口前置轮询（`waitForLeaderAndAssignments` 名不副实只查租约行）+ 断言 deadline 轮询语义 + fencing_token 与 lease leader_epoch 口径差（×1_000_000）——已补；5 Minor 同轮吸收（invariants 无 JobCoordinator pin 的事实修正、行号、warm-path gating 张力、Non-Goals 路由措辞、批量读悬空）。
  - 第二轮 `ses_f96228ed6ffebe5EwXryX8DeSk`：**0 Blocker / 0 Major，consensus**（live 核对全表命中 + 逐 Phase 想象性分析无断层 + Minimum Rules #10/#22/#23/#24/#25 合规 + Anti-Slacking 无禁用词）；5 Minor 全部吸收（gate-inventory 批量读分支需再生成而非 re-pin、attempt 比较钉 per-subtask 口径、红证选择性 stash 只回滚 src/main、Goals InMemory WARN 措辞对齐 baseline、两处 ±1 行号）。
- **Plan 概要**（三 Phase）：Phase 1 = 三项裁定（方案与拒绝面 upsert/删旧插新 / warm 路径 gating / 读失败行为）落 `01-architecture-baseline.md` + 种子化实现 + focused 三族测试（JDBC 预置行接续 / fresh-job 与同 JVM 回归 / InMemory 对等无 regression WARN）；Phase 2 = `TestMultiJvmCoordinatorFailover` 两测试断言升级（缺陷窗口前置轮询 + 新 epoch 行 + per-subtask attempt 接续 + 日志无中止签名）+ 选择性 stash 红证 + gated 绿；Phase 3 = CHAOS-2 `chaosJcHaFailover` gated 复验终态收敛 + 产物核对无 duplicate-key + runbook §7 已修复条目 + roadmap item 34 写回。
- 工具门禁：`check-doc-links --strict` exit 0；`check-plan-checklist --strict` exit 0（active 态结构校验通过）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0233-3 closure——item 33 done，manifest retention 收口验收达成）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0233-3-epoch-manifest-retention.md`（item 33，收口）
- **Closure audit**（独立 general subagent fresh session `ses_f96398efbffe7yvhX5qQHBB4ef`）：**CLOSURE-AUDIT: APPROVED**（0 Blocker / 0 Major / 0 Minor / 4 Info（= 收口仪式步骤、未提交工作、无条件裁剪已文档化、全量证据采信日志+现场复跑））——Phase 1-3 全部 Exit Criterion 逐条 live 核验 PASS（含 JDBC 安全方向硬约束合规：SELECT DESC 观察 → DELETE IN 观察到的超限旧 id，无 NOT-IN keep-set 两步形态）；Closure Gates 8/8 PASS；**Anti-Hollow 全链连通**（真实完成 :614/:648/:707 → onCompletePersistSuccess :864 → async CAS+submit 零 monitor I/O / sync 内联 :907 → retention executor :1315 → D4 时序裁剪 :1363/:1385 → LocalFile/JDBC 文件/行真实消失 + 探针/文件名/行数断言）；audit 现场复跑 focused 18/18 + 四工具 exit 0。
- **收口仪式**：closure-audit checkbox + Closure Gates 8/8 勾选 + Closure evidence 写入 → Plan Status `completed`（Completed: 2026-09-04）；roadmap item 33 → `done`（验收摘要 + audit session）+ Last updated 同步；`check-plan-checklist --strict` exit 0。
- **文本一致性核对（规则 19）**：Plan Status completed ↔ 三 Phase completed 且 checklist 全 [x] ↔ Closure Gates 8/8 ↔ Closure evidence 完整 ↔ roadmap item 33 done ↔ 本日志四条目（Phases 1/2/3/收口）——七处一致。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0233-3 item 33 Phase 3——端到端有界性证明 + gated 多 JVM + runbook 收口）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0233-3-epoch-manifest-retention.md`（item 33，Phase 3）
- **coordinator 级 e2e（LocalFile，端到端）**：新 `TestCheckpointDualPlaneRetentionE2E`——真实 trigger→ACK→durable persist→retention 循环 ×10（maxRetained=3，async 生产形态），断言 `{jobId}/{pipelineId}/` 下 `.checkpoint` 与 `.epoch` 文件**均收敛 ≤ 3 且文件名恰为 newest 3**（SOAK-3 137 文件缺陷 coordinator 级关闭）；restore 不回退 = `loadRetainedEpochManifests` 返回 newest N 全集 + `getLatestCheckpoint` + fresh coordinator `restoreSharedStateRegistry()`（incremental+segmentStore 契约）成功。断言协议防 flaky：硬断言只在收敛终态（bounded async wait），in-flight 余量不在运行中硬断言（Phase 2 focused ⑤ 已钉定）。
- **JDBC 等价**：`TestCheckpointCoordinatorJdbcRetainedManifests` 新增 `manifestRowsStayBoundedAndRestoreStillRebuildsRefCount`——稳定 RocksDB 状态 6 轮完成（maxRetained=3），`stream_epoch_manifest` 行收敛 ≤ 3（retained 读集判定）+ checkpoint 行同界 + newest N 内容断言 + restart `restoreSharedStateRegistry()` 重建共享 SST ref-count=3（裁剪后 ref-count 语义不回退）。
- **gated 多 JVM（C2 形态）**：`TestS2RestoreRescaleMultiJvmE2E` 增终态有界断言——run 2 收敛后 bounded wait `checkpoints/{jobId}/pipeline-0` 下 `.epoch` ≤ 5（`fetchRetainedManifestCount` 同形扫描；KEY_MAX_RETAINED=5；checkpoint interval 400ms → 远多于 maxRetained 的完成轮数）+ `.checkpoint` 同界（双平面）；断言协议（主判据=负载静止终态、余量推导留测试注释）留档于测试注释。
- **回归**：`./mvnw test -pl nop-stream -am -T 1C` **BUILD SUCCESS**（runtime 1015/0/0/10、fraud 117/0/0/15 skipped 全为既有 gated；`TestEpochManifestPersistence` 9/0、`TestJdbcCheckpointStorage` 28/0 含 Stage 51 checksum/篡改面）；**gated C0-C3 套件 7/7 绿**（S1 2 + S2 2 + C2 1 + C3 2，`-Dnop.stream.test.multi-jvm.enabled=true`；首轮 S1 出现一次 H2 file-lock 竞态（`resetS1Tables` 打开共享库时，测试启动阶段、与 checkpoint 无涉，runbook §7 已记录的 gated 偶发族），隔离复跑 + 全套复跑均绿）。
- **runbook §7**：item 33 缺陷条目改「已修复（2026-09-04，plan 0233-3）」+ 新语义一句话（retention 一轮内双平面同裁）+ 验证面清单 + 原缺陷留档（归因修正表述保留）。
- **工具门禁**：`scan-hollow-implementations --module nop-stream/nop-stream-runtime --severity high` exit 0（0 findings；fraud-example 同 0）；`check-nop-stream-invariants` exit 0；`check-doc-links --strict` exit 0（0 errors/0 warnings）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0233-3 item 33 Phase 2——双存储裁剪实现 + coordinator 接线落地）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0233-3-epoch-manifest-retention.md`（item 33，Phase 2）
- **实现落地（按 Phase 1 五项裁定）**：① `ICheckpointStorage.pruneEpochManifests(jobId, pipelineId, maxRetained)` default 方法（返回被裁 epochId 列表，default 空列表仅测试替身域）；② `LocalFileCheckpointStorage` override——write-lock 下列 `*.epoch`、epochId 降序、删除 index ≥ maxRetained 的观察到的旧文件，I/O 失败转 `CheckpointStorageException`（deleteCheckpoint 同风格）；③ `JdbcCheckpointStorage` override——**两步读旧-删旧**（SELECT epoch_id DESC 观察 → DELETE ... epoch_id IN (观察到的超限旧 id)），删除目标硬限定为读取时已观察到的超限旧 epochId（禁 NOT-IN keep-set 两步形态：读写窗口间新完成 manifest 会被误删），GENERIC 无 LIMIT/既有 DELETE 平铺 SQL 与该形态一致，删除行数 INFO 留痕；④ `CheckpointCoordinator.cleanupOldCheckpoints` 扩展 `pruneEpochManifestsForObservedPipelines`（D4 = 同轮 checkpoint 平面删除之后无条件执行——manifest 平面可独立超界（pre-fix 遗留）；D2b = distinct pipelineId ∪ own；per-pipeline WARN 包含 + 下轮自愈；async 只在 `checkpoint-retention-<jobId>` executor、sync-fallback 内联 ACK 线程同栈 deleteCheckpoint（D1(d)））。
- **测试面**：`TestCheckpointRetentionAsync.RetentionStorage` 升级真实 manifest 面（TreeMap 记录 + prune keep-newest-N + pruneCount/pruneArgs/pruneThread 探针 + failPrune 开关），6 个既有 retention 用例全部补 manifest 收敛断言（最终一致/慢 I/O 非阻塞/rapid 串行化/trailing re-run/失败自愈/sync-fallback 内联线程断言）；新增 focused ④ `manifestPruneFailureWarnsAndSelfHealsOnNextCompletion`（prune 持续失败 → checkpoint 平面独立收敛 + manifest 停留超界（无静默跳过）→ 清除失败后下轮自愈）、⑤ `inFlightManifestMarginConvergesWithoutDeletingNewest`（blocked retention 轮间完成的 manifest 短暂超界 → trailing re-run 收敛且 newest 永不误删）；新文件 `TestEpochManifestRetention`（① LocalFile 文件名断言 8→newest3 + ② JDBC H2 行数断言 + ③ 双存储保留集 == `loadRetainedEpochManifests` 读集（D2 钉定）+ below-bound/missing-dir no-op）；**接线验证**（guide #23）= `retentionConvergesToMaxRetainedKeepingNewest` 断言 pruneCount ≥ 3 且每次调用携带 coordinator jobId/pipelineId/maxRetained 参数。
- **invariant 注册表同步**：`gate-inventory.json` `LocalFileCheckpointStorage` 方法表补 `pruneEpochManifests`（I1 双向精确门禁，`TestRuntimeInvariantTableCompleteness` 与 `check-nop-stream-invariants.mjs` 均 exit 0）。
- 验证：focused 15/15 绿（RetentionAsync 8 + EpochManifestRetention 7）；`./mvnw test -pl nop-stream -am -T 1C` **BUILD SUCCESS**（runtime 1015/0/0/10 skipped 全为既有 gated；fraud-example 117/0/0/15 skipped 同）；`node ai-dev/tools/check-nop-stream-invariants.mjs` exit 0。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0233-3 item 33 Phase 1——manifest retention 五项裁定落档）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0233-3-epoch-manifest-retention.md`（item 33，Phase 1）
- **五项裁定落 `checkpoint-design.md` §9.2/§9.3**：D1 = `pruneEpochManifests(jobId, pipelineId, maxRetained)` default 方法返回被裁 epochId 列表（default 空列表仅测试替身域可接受，LocalFile/JDBC 必须真实 override，接线由 focused 测试钉定；拒绝 abstract 全量迁移 ~14 替身纯扰动）；D2 = keep-newest-N per `(jobId, pipelineId)` 按 epochId 降序与 `loadRetainedEpochManifests` 逐字同口径（restore 读集 ⊆ 裁剪保留集；epochId==checkpointId 1:1 锚点 live 核验 `CheckpointCoordinator.java:1565-1566` + `CheckpointSerDe.java:331/:461`）；D2b = 裁剪枚举同轮 `getAllCheckpoints` 的 distinct pipelineId ∪ own pipelineId（与 checkpoint 平面删除基准同读同源）；D3 = 无新 segment 泄漏通道（checkpoint 平面删除已按同 id GC、restore 从保留集重建 ref-count、残余由 restart orphan 扫描回收）；D4 = 同轮 retention 内先删 checkpoint+GC segment 后裁 manifest（两平面不相交，顺序非正确性要求但保证每轮确定/幂等/自愈）。§9.3 方法表补 manifest 族四方法（store/loadLatest/loadRetained/prune）+ `deleteAllCheckpoints` 双平面语义注记。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 errors）。
- Doc-sync: `ai-dev/design/nop-stream/checkpoint-design.md` §9.2/§9.3 已更新。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0233-2 closure——item 32 done，演练观察面收口验收达成）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0233-2-exercise-observation-surface.md`（item 32，收口）
- **Closure audit**（独立 general subagent fresh session `ses_f96cc70f8ffeTNeSGF358tZALv`）：**CLOSURE-AUDIT: APPROVED**（0 Blocker / 0 Major / 1 Minor（= 收口仪式自身步骤：勾选 gates + 写入 evidence，已执行）/ 3 Info（未提交工作 → 收口即提交；bind null-guard 防御性；全量门禁采信日志两轮 BUILD SUCCESS + 现场复跑 focused））——Phase 1-3 逐条 Exit Criterion live 核验（wire 名断言于真实 deployTask 路径 / 503-404 结构化错误 / D1-D2c 落档行号 / OBS 与回归四 runId verdict=pass + OBS-1 samples 序列 9→1024→0 逐点核验 / 泄漏启发式 git diff 未触碰）；Closure Gates 10/10 per-gate 表 PASS；**Anti-Hollow 全链连通**（TaskManagerMain opsHttpPort → ops server 挂 composite → 真实 deploy 注入 metrics + gauge bind → TM /metrics → MiniStreamCluster 端口 → sampler → JSONL；OBS-2 产物实证 260 条 wire 形态 tagged 条目；scan-hollow 两模块 0 findings、无 TODO/空方法体）；audit 现场复跑 focused 7/7 + 8/8 + 四工具 exit 0。
- **收口仪式**：Closure Gates 10/10 勾选 + Closure evidence 写入 → Plan Status `completed`（Completed: 2026-09-04）；roadmap item 32 → `done`（验收摘要 + 四 runId + audit session + 三次节流落点迭代裁定）+ Last updated 同步；`check-plan-checklist --strict` exit 0 + `check-doc-links --strict` exit 0。
- **文本一致性核对（规则 19）**：Plan Status completed ↔ 三 Phase completed 且 checklist 全 [x] ↔ Closure Gates 10/10 ↔ Closure evidence 完整 ↔ roadmap item 32 done ↔ 本日志三条目（Phases 1+2 / Phase 3 / 收口）——七处一致。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0233-2 item 32 Phase 3——演练装置消费双源 + gated OBS e2e + 节流落点裁定）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0233-2-exercise-observation-surface.md`（item 32，Phase 3）
- **D3（TM 端口分配与发现）**：`MiniStreamCluster` 扩展——`withTaskManagerArg`（TM arg 透传，restart 复用同一命令状态）/ `withTmOpsHttpPortBase`（tm-\<i\> → base+i 确定性分配，restart 同端口）/ `taskManagerOpsHttpPort(nodeId)` + `taskManagerOpsHttpPorts()`（sampler 枚举面）；演练装置基址 `exercise.tmOpsHttpPortBase` 默认 8941（避开 JC 8901/8931）。`prepareS2ExerciseCluster`/`startS1ExerciseCluster` 接线 TM 端点（既有 cell 全部自动获得双源采样）。
- **Sampler SPI 扩展（向后兼容）**：`SampleSources.fetchTmMetrics()` default 方法（default 空 map，fakes 不受影响）；`SampleRecord` 增 `tmMetrics`（per-TM wire 形态名 map）+ `channelQueueDepth`（gauge max 聚合，-1=未采样）；`maxChannelQueueDepth` 聚合口径 = 全 TM 面通道 gauge max（javadoc + runbook 落档）；COUNT 代理字段 `queueDepth` 与泄漏启发式口径逐字未动（双记对照）。`ClusterSampleSources.fetchTmMetrics` 每 tick 刮全部 TM 面（死/重启中 TM → 空面不抛异常——chaos 语义）。focused `TestExerciseSampler` +2（maxChannelQueueDepth 聚合/未采样 -1 + tmMetrics/channelQueueDepth 记录与 JSONL 序列化）。
- **节流档位落点裁定（三次 live 证据迭代，runId `1788468752196-1`/`1788469038532-1`/`1788469782853-1`）**：S2 形态（source..watermarks | 远程边 | keyBy..window..sink 两顶点）下 ①sink 节流不饱和通道——window 吸收输入速率，积压落窗口状态（gauge 恒 0/max 6）；②与 source 同链 map 节流只节流生产者（COUNT 增速实为控制面流量）；③**window assigner 节流**（`SteppedThrottleWindowAssigner` 透传包装 `txWindowAssigner` bean，档位文件驱动逐记录 sleep，窗口/聚合/期望输出语义不变）落在消费顶点 processElement 路径 → 消费吞吐 1.96/s vs 源 ~20/s → 通道真实填满至 1024 满容量。三裁定均落 javadoc + runbook §6.1（含第三个 runId 的 aligned barrier 合法排队语义：故意饱和通道期间 barrier 排在积压后不能超越数据，checkpoint 推进断言形态 = "释放后恢复"——OBS-1 不沿用 BP-1 的"节流期间推进"判据的理由落测试 javadoc）。
- **gated e2e（端到端验证，产物留档）**：`TestStabilityExerciseMultiJvm` 新增 `backpressureObservationSurface`（OBS-1）+ `soakObservationSurfaceShort`（OBS-2）。**OBS-1 PASS**（runId `1788469939365-1`，337s）：①TM 面 wire 指标序列（每 tick 非空）②通道 gauge 9→1024 满容量抬升 + 释放后回落 0 ③TM 面吞吐量化差 1.956/s（节流）vs 82.067/s（释放后 42×）④释放后 checkpoint 恢复（136 次推进，节流窗 gap 45s < chaos 判据 120s）+ exactly-once 收敛 + soak health + 泄漏启发式不触发。**OBS-2 PASS**（runId `1788468621334-1`，107s）：52/52 样本含 TM 面指标 + gauge 序列；**gauge 0 vs COUNT 3735 的直接测 vs 代理对照留档**（INSERT-only 后端 COUNT 膨胀而真实水位为 0——本 item 存在价值的直接证据）。**回归**：BP-1 全档 sink 节流 PASS（`1788470606087-1`，resolver 变更不破坏既有形态）+ SOAK-1 PASS（`1788471115747-1`，120s 覆写）。
- **Owner docs**：runbook §5 EX 行扩 OBS 格 + §6.1 演练观察面章节（TM 刮取步骤/gauge 口径 max/COUNT 对照降级说明/节流落点三裁定/aligned barrier 语义/gated 验证锚点）。
- **验证**：focused `TestExerciseSampler` 8/8 绿；全量 `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（两轮：Phase 1+2 后与 Phase 3 后）；gated 4 cell 全 PASS（OBS-1/OBS-2/BP-1/SOAK-1）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0233-2 item 32 Phases 1+2——TM 本地指标端点 + 通道队列水位直测 gauge）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0233-2-exercise-observation-surface.md`（item 32，Phases 1-2）
- **Phase 1（TM 本地指标端点）**：**D1 裁定** = 每 TM 进程本地 pull 端点（拒绝 TM→JC push：pull 与 Prometheus 每进程一 target 架构一致、零 RPC 契约扩张、同机演练可直刮、与 JC 对称；push 需新 RPC 契约 + JC 聚合/基数管理）——落 `docs-for-ai/03-modules/nop-stream.md`（暴露面章节 + D1 段落）。**接线**：`TaskManagerMain` launch args `opsHttpPort`/`opsHttpBind`（对齐 JC raw-arg 模式，默认 0=关闭显式语义）；复用 `StreamOpsHttpServer` null-registry 构造（`/jobs` 族维持既有 503 REGISTRY_UNAVAILABLE/SUBMIT_UNAVAILABLE/STOP_UNAVAILABLE + 404 JOB_NOT_FOUND 结构化语义，服务端零改动）；启动/关闭随进程生命周期；端口避开 JC 8901/演练 8931（装置自 8941 基址分配）。测试 `TestTaskManagerOpsEndpoint` 3 用例：默认关闭（无端口 → 无端点）/ 200+TextFormat 0.0.4 + OpenMetrics 协商 + 503/404 结构化错误 + shutdown 停端点 / **接线验证**（真实 deployTask 路径 source→sink 20 条记录 → `/metrics` 含 wire 形态名 `nop_stream_task_deployed_total`、`nop_stream_io_records_emitted_total`、`nop_stream_io_emit_time_seconds_count/_sum`、`nop_stream_operator_records_in_total`）。
- **Phase 2（通道队列水位直测 gauge）**：新类 `ChannelQueueGauges`（runtime main scope transport 包）——**D2** `nop.stream.io.channel.queue.size`（io 层；tags jobId/edgeId/sourceSubtask/targetSubtask 全身份；per-edge max 为采集口径；无独立溢出 gauge——溢出经 typed 失败可观察，指标面最小）；**D2b** 仅 `subscribe==true` 通道注册（`RemoteGraphExecutionPlanBuilder.buildRemoteOnly` 构建点；零订阅协调器/construct-only 镜像通道不注册防恒 0 噪声）；**D2c** 可变 holder 防洞形态（静态 map 持强引用 holder，每通道身份一个 meter；重注册 `holder.set(newChannel)` 重绑——绕开 micrometer 同 id 二次注册静默丢弃→旧弱引用 NaN/冻结洞；`RemoteInputChannel.close()` 释放绑定置 0；条目永不移除防洞复发，增长有界=进程内通道身份数）。测试 `TestChannelQueueGauges` 4 用例：值随真实 enqueue/dequeue（经 registry 读）/ recovery 关旧建新重绑（同一 meter 对象 + 无冻结值）/ 未关闭重绑跟随最新绑定 / 构建器范围（零订阅零 gauge、单 subtask 恰好该通道 gauge）；`TestTaskManagerOpsEndpoint` 接线用例扩展断言 `nop_stream_io_channel_queue_size` 出现在 TM 面 `/metrics`。
- **Owner docs**：`docs-for-ai/03-modules/nop-stream.md`（io 层指标表 + gauge 条目 D2/D2b/D2c 裁定；暴露面 JC+TM 双端点 + D1 裁定 + TM launch 参数；启动作业/指标采集速查）+ `distributed-runbook.md` §6.1（TM 端点刮取 + 演练观察面双源口径 + gated 验证锚点）+ `metrics.properties.template`（TM 侧多 JVM 说明）。
- **验证**：focused 7/7 绿（TestChannelQueueGauges 4 + TestTaskManagerOpsEndpoint 3）；runtime 全量 1004/0/0/10 绿；全量 `./mvnw test -pl nop-stream -am -T 1C` 见 Phase 3 后收口条目。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 0233-1 item 29 执行完成——flow DSL 编译器产品化三缺口收口）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-04-0233-1-flow-dsl-compiler-productization.md`（item 29，Phases 1-3 全部 completed）
- **Phase 1（xpl source 取消语义）**：`SourceFunction.SourceContext.isCancelled()` default 方法（default false，javadoc 明示无观察能力上下文不得伪造信号；裁定记录 implementers 清单 ~30+ 匿名实现/7 模块 → abstract 机械迁移不值）；生产上下文 `StreamSourceOperator.run()` 匿名 SourceContext override 读 `MailboxExecutor.isCancelled()`（与 `drainControlMails` 协作中止异常同一真值源）；`XplSourceFunction` javadoc 改为可实现的 ctx 轮询模式（`cancel()` 定位=close 路径+测试面）。测试：core `TestSourceContextCancelAccessor`（同一 ctx signalCancel 前后翻转=接线验证；无 mailbox=false；default=false）+ flow `TestXplFunctionWrappers.sourceFunctionBodyExitsLoopViaContextCancelPoll`（可控 override 上下文）。**e2e 形态选择=主形态（runtime 取消面）**：fraud-example `TestXplSourceCancelE2E` + 测试场景文件 `fraud-cancel-xpl.stream.xml`——tiny interval/timeout 使 pending checkpoint 必然超时 → `registerLocalAbortHandler` → `signalCancel()`（生产取消配方）；xpl body 忙轮询无阻塞调用（中断无法结束循环，唯一出口=访问器）断言 execute 有界终止（~1.7s）+ cause chain 含 `ERR_STREAM_CHECKPOINT_ABORTED`（非 supervision-task-failed=非中断退出）+ 数据面元素达 sink。备选 harness 形态被拒：checkpoint-abort 路径在 LOCAL 可达且为生产取消面，无需降级。踩坑记录：跨运行 checkpoint 状态泄漏（默认 jobId 冲突 → fingerprint restore 拒绝）——fixture 显式 jobId/pipelineId + @TempDir storage 隔离。
- **Phase 2（per-transform parallelism 消费）**：core `DataStream.setParallelism(int)`（接口 + `SingleOutputStreamOperator`/`KeyedStream` 协变；`DataStreamSource` 经前者覆盖）+ `sink(fn, parallelism)` 注册重载（sink transformation 终端）；`Transformation.parallelism` final→受控可变（guarded setter：`>=1` typed 拒绝、locked 非 1 拒绝、锁顶点 resolve 仍强制 1——裁定论证=算子构造在 builder 方法内部，逐构造器传参需复制全部重载）。flow 全构建点接线（base + AdvancedTransforms 全系 + `resolveBean`）；HASH 边目标声明值同步隐式 partition 顶点（防 FORWARD modulo 伪分布）；`<window>` 虚拟元素声明 parallelism fail-fast（无消费者不静默）；FL-2 fail-fast 退役删除。测试：core `TestDataStreamParallelism` 7 用例（含锁交互/断链）+ `TestStreamGraphGenerator` 2PC gate 声明路径回归；flow `TestStreamFlowAuditFixes` FL-2 改消费语义（JobVertex 级断言）+ 未声明继承 + 正控制保留 + `TestPerTransformParallelismWiring` 3 用例；e2e `TestPerTransformParallelismE2E`（fraud-example，HASH 边 40 keys 均匀 4 subtask + 共享 map bean 线程观察 ≥2 + latch 强制重叠 + 逐元素精确 once + plan 侧 subtask 数==声明）。踩坑：latch 计数未减导致 200s 超时（修复：观察到 ≥2 线程即 countDown）；chain 顶点名为 `KeyBy -> Map -> Sink` 连接形态（断言改 startsWith）。
- **Phase 3（错误源位置锚点）**：`StreamModelDslBuilder` 36 处 + `AdvancedTransforms` 30 处抛错点全部 `.loc(model.getLocation())`（transform/edge/模型根三级）；`resolveWindowAssigner` 穿参 `t`；bean 解析错误经 `resolveBean` 包装锚定声明元素；`ValidationIssue.sourceLocation` 字段（`file:line`，null 不渲染不伪造）+ `StreamConfValidator.toIssue` 从 `getErrorLocation()` 填充（层 1/层 2 均传播）+ `describe()` `@ file:line` 后缀。测试：`TestErrorSourceLocationAnchors` 8 用例（XDSL fixture 精确行号 ×2 + 程序式 5 族 + 无位置不伪造）+ `TestStreamConfValidator` 层 1/层 2 位置与连接器模式 null 锚点 + fraud-example CLI 集成 `badEdgeReportCarriesFileLineAnchor`（exit 1 + `file:line` 渲染）。踩坑：python 批量替换 2 处字符串不匹配静默跳过（constructors/constructor）——以 rg 计数+断言重核；fixture 行号需按注释行实数（13/17 而非 8/19）。
- **Owner docs**：`stream-dsl-design.md` §5.1（取消语义）/§5.2（并行度解析顺序）/§5.3（位置锚点）；`pre-submit-validation-design.md` D7（位置字段+渲染格式+不伪造）；用户文档族 `nop-stream-user-guide.md`（取消模式+per-transform 并行度）；ops owner doc `nop-stream.md` 错误输出行同步 `[@ file:line]`。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` 全绿（core 1565/flow 115/cep 359/connector 族/runtime 997/fraud-example 113+2 e2e）；hollow-scan（flow+core）exit 0；invariants exit 0；doc-links exit 0；plan-checklist exit 0；独立 closure audit 见 plan Closure 节。

### 2026-09-04（mission nop-stream-productization DRAFT_PLANS：items 29/32/33 三份 plan 起草 + 两轮审查 APPROVED 转 active）

- **Plans**（顺序 = 执行顺序，roadmap 首 `todo` 顺取，item 34 留待下轮）：
  - `ai-dev/plans/nop-stream-productization/2026-09-04-0233-1-flow-dsl-compiler-productization.md`（item 29）：xpl source 取消语义（SourceContext 取消访问器接线 mailbox 标志，default 形态倾向 + 生产 override 硬约束）/ build 期错误源位置锚点（模型 location 已有未消费，NopException.loc 先例，ValidationIssue 增字段）/ per-transform parallelism 消费（core API 入口 Decision + flow 全构建点接线 + FL-2 退役）。
  - `ai-dev/plans/nop-stream-productization/2026-09-04-0233-2-exercise-observation-surface.md`（item 32）：TM 进程本地 pull 指标端点（拒绝 push transport 的 D1 裁定）/ RemoteInputChannel 队列水位 gauge（D2b 仅订阅通道注册 + D2c 重注册防 NaN 洞）/ 演练装置消费（MiniStreamCluster TM arg 透传 + 端口分配 D3）+ gated 多 JVM e2e（wire 形态指标名断言）。
  - `ai-dev/plans/nop-stream-productization/2026-09-04-0233-3-epoch-manifest-retention.md`（item 33）：ICheckpointStorage 裁剪面（default 先例）双存储 + coordinator retention 接线（async 段3a monitor 零新增 I/O / sync-fallback 沿 D1(d) 内联豁免）/ JDBC 删除方向硬约束（禁 NOT IN keep-set 两步竞态形态，防误删 newest）/ 双平面有界性 e2e（主判据 = 停后收敛终态防 flaky）。
- **起草流程**：三路并行 explore subagent live 事实调研（Current Baseline 30+ 锚点/plan，行号级）→ 起草 → 三路独立 general subagent 对抗性审查（含想象性分析）：plan1 首轮 1 Blocker（LOCAL env 入口无取消通道——core `execute()` 无 cancel API、signalCancel 生产调用者全在 runtime，e2e 宿主裁定 fraud-example 测试代码）+ 3 Major（SourceContext 7 模块 ~17 文件爆炸半径入 Baseline、hollow-scan 短名假绿、FORWARD 1→N modulo 伪证明→强制 HASH 边）；plan2 首轮 4 Major（MiniStreamCluster 缺席、TM 端口分配未裁定、dot 形态指标名 wire 上不存在、gauge 全矩阵注册/重注册 NaN 洞）；plan3 首轮 3 Major（sync-fallback 与 monitor 禁令矛盾对齐 D1(d)、JDBC 删除方向竞态、pipelineId 枚举口径 D2b）。全部修复后第二轮复审：plan1/plan3 APPROVED + Minor 顺手修（计数/拼写），plan2 仅 Closure Gates 一行 404→503/404 残留（按复核指引修复，无需重审）→ 三份 `Plan Status: active`。
- **Roadmap 写回**：items 29/32/33 → `planned`（附 plan 路径与审查结论）+ Last updated 头更新（28/31 done 记录保持引用）；item 34 保持 `todo`。前序 plan 的 deferred 项核对：1951-3 两项 Successor=yes 已路由 items 34/33、2217-2/2217-3 的 Successor 项已被对应后续 plan 消费——无未路由可重触发 deferred。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1951-3 closure——items 28/31 done，数据面停摆缺陷族修复验收达成）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-03-1951-3-remote-deploy-dataplane-stability.md`（items 28+31，收口）
- **Closure audit**（独立 general subagent fresh session `ses_f9781e482ffe2cTz95Xlzo5zvZ`）：**CLOSURE-AUDIT: APPROVED**（0 Blocker / 0 Major / 2 Minor（= 收口仪式自身步骤，已执行）/ 3 Info）——Phase 1—4 逐条 Exit Criterion live 核验（订阅三态/有界等待/typed 溢出/未订阅防护/预算分池/JDBC override 实码锚点；五个 runId 产物 verdict 一致）；Closure Gates 16/16 per-gate 表 PASS；Anti-Hollow 全链追踪（MiniStreamCluster → deployTask RPC → SubtaskPlanBuilder → 通道 → sink 36000 行 exactly-once = 运行时连通证明；scan-hollow 0 findings）；诚实性（CHAOS-2/item 33 路由 = 范围外 pre-existing，duplicate-key 同签名存于 item 15 产物 = 证明；无 in-scope 静默降级）；audit 现场独立复跑 focused 17/0/0/0 + 全量 11 模块 BUILD SUCCESS + 四工具门禁 exit 0。
- **收口仪式**：Closure Gates 16/16 勾选 + Closure evidence 写入 → Plan Status `completed`（Completed: 2026-09-04）；roadmap items 28/31 → `done`（验收摘要 + 复验 runId + audit session）+ Last updated 同步；随复验发现路由 items 33/34（`todo`）；`check-plan-checklist --strict` exit 0 + `check-doc-links --strict` exit 0（0/0）。
- **文本一致性核对（规则 19）**：Plan Status completed ↔ 四 Phase completed 且 checklist 全 [x] ↔ Closure Gates 16/16 ↔ Deferred 2 项附理由 ↔ Follow-ups 3 项 ↔ `ai-dev/logs/` 五条目 ↔ roadmap items 28/31 done——八处一致。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1951-3 Phase 4——gated 多 JVM 复验 3 PASS + CHAOS-2 pre-existing 缺陷路由）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-03-1951-3-remote-deploy-dataplane-stability.md`（items 28+31，Phase 4）
- **SOAK-3 全参数 PASS**（runId `1788456304950-1`，281.6s，36000 行 = 旧 jam 阈值 36 倍）：终态 == 期望集（180 窗口行 multiset 无重复无丢失）+ durable epoch 推进 55 次 max gap 5010ms + hang 三独立判据均不触发。**首轮 run `1788455717401-1` 修正演练装置误校准**：`ExerciseSampler.queueDepthUnboundedGrowth` 把 insert-only 后端 send 侧计数单调增长误判泄漏（本 plan Current Baseline 已修正归因「订阅收敛不消除表计数」）——就地修复演练基建（增长信号与「epoch 无推进」耦合 = jam 签名保留，`TestExerciseSampler` 钉定测试同步：jam 形态（增长+冻结）仍 flagged、健康形态（增长+推进）不再误报）后**原参数重跑**合法闭环。
- **CHAOS-1 简化复验 PASS**（runId `1788456616196-1`，547.2s，kill TM ×2 轮持续流量）：每轮 fencing 严格递增（1→2→3，chaos-events outcome=recovered ×2）+ 终态 == 期望集——D3 多 JVM 锚点（真实 kill 恢复能力完整）成立。
- **BP-1 全格复验 PASS**（runId `1788457179765-1`，492.8s）：三档节流 50/200/500ms 窗口推进 29/30/30 次（修复前 200/500ms 档 jam 消除；500ms 档 = D2 有界等待不误伤健康慢消费者实证）+ 释放后 exactly-once 收敛。
- **CHAOS-2 FAIL → pre-existing 缺陷路由 roadmap item 34**（runId `1788457688118-1`，1050s）：租约翻转子判据 PASS（leaseEpoch 1→2→3 ×2）；终态不收敛根因 = 新 leader `activateAsLeader` 完成租约获取 + fencing 轮转（epoch=2000000）后，assignment 重发 INSERT 与旧 leader 存留行**唯一键冲突**（`nop.err.dao.sql.duplicate-key`）→ become-leader listener 中止 → 接管半途而废（无重部署/无 checkpoint/输出冻结于 2/43 窗口；数据面健康：queue 平台 1236 无增长）。**同签名存在于 item 15 产物 `1788385016277-1`**（coordinator-2/3 日志）——item 15 报告「jam 抑制重部署触发」归因**修正**为本缺陷（jam 掩盖了它）；D4 全格复验决策以证据关闭了「新 leader 重发 assignment」遗留问题（机制存在但中止于冲突）。
- **EpochManifest retention 缺失发现 → roadmap item 33**：SOAK-3 健康运行 280s 留档 137 个 `.epoch` 文件（maxRetained=5 只作用于 CompletedCheckpoint 面，manifest 无清理路径）；item 15「retained manifests 有界」正观察系 jam 早停假象（归因修正落 runbook §7）。非正确性缺陷（bounded per run），successor 修复方向 = retention 同步裁剪 manifest（双存储）。
- **C0—C3 + legacy 回归**：场景 gated 7/7 绿（S1×2/S2×2/C2×1/C3×2）+ 序列化 6 绿（默认套件 110/0/0/13 内含）+ legacy 8/8（ExactlyOnceRecovery 首跑失败 = 2026-09-04 早间 plan 1951-2 A/B 裁定的 pre-existing 负载 flake（同签名），隔离复跑绿 2.1s 非回归）；fraud-example 默认套件复跑（启发式修改后）110/0/0/13 绿。
- **runbook §7 更新**：item 28 三条目 → 已修复 + 复验锚点（runId + 判据）；新增 item 34/item 33 诚实披露 + queue-depth 再校准说明 + BP-1 复验结果 + ExactlyOnceRecovery flake 记录；doc-links exit 0。roadmap 追加 items 33/34（`todo`，自进化规则）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1951-3 Phase 3——D3 恢复预算分池 + JDBC retained manifests override）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-03-1951-3-remote-deploy-dataplane-stability.md`（items 28+31，续）
- **D3 实现**：`JobCoordinator`——`requestRecovery(RecoveryCause)`（NODE_FAILURE/TASK_STALL/OTHER，无参入口 = OTHER = 真实故障预算，既有调用方语义不变）；`detectFailures` 分类（租约失效优先真实故障，纯 stall 归 stall 池）；`stallRestartCount`/`maxStallRestarts=3`/`stallRecoveryCooldownMs=30s`（冷却窗内 WARN 跳过、不烧预算不转 fencing；stall cap 超限 failJob）；`globalRecovery(boolean)` 预算分池、fencing/abort/reassign 全同、health/event 序号跨池单调；tuning getters/setters + `getTotalRecoveryCount()`。
- **W-8 实现**：`JdbcCheckpointStorage.loadRetainedEpochManifests` override（epoch 降序 + LIMIT count，typed wrap 对齐 sibling）。
- **focused 测试**：`TestJobCoordinatorStallRecoveryBudget` 5 用例（stall×3 独立预算 + 满风暴后真实恢复仍进行（CHAOS-1 锚点单进程级）/ 冷却窗跳过 / stall cap failJob 真实预算不动 / detectFailures 双向真实分类）；`TestJdbcCheckpointStorage` +1（5 epoch retained 新集最新优先 count 截断）；**接线验证** `TestCheckpointCoordinatorJdbcRetainedManifests`（真实 `restoreSharedStateRegistry` 消费点：3 次 RocksDB 增量 checkpoint 落 JDBC → 重启恢复 registry 每 hash ref-count==3（多 epoch retained 集才可达）+ GC map 覆盖三 epoch）。
- **验证**：runtime 全量 **997/0/0/10 绿**（+7 新用例）；全量 `./mvnw test -pl nop-stream -am -T 1C` 11 模块 BUILD SUCCESS（fraud 110/0/0/13）。owner-doc：`checkpoint-design.md` §9 Restart 恢复段补双存储对等句 + 契约指针。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1951-3 Phase 2——D1 订阅收敛 + D2 队列满语义落地）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-03-1951-3-remote-deploy-dataplane-stability.md`（items 28+31，续）
- **D1 实现**：`RemoteGraphExecutionPlanBuilder.buildRemoteOnly` 增订阅范围参数（null=全订阅/非空=目标 subtask 集/空=零订阅；通道一律构造，全图结构保留）；`SubtaskPlanBuilder.buildSubtaskPlan` 传 assigned 单例集（TM 单 deploy 订阅面 = 本 subtask 输入 topics）；`RpcDistributedExecutor` remoteDeployMode=true 协调器 plan 改零订阅（D1(f) 落地）；`RemoteInputChannel` 构造-订阅分离 + 未订阅读取 typed 快速失败防护（ERR_STREAM_INVALID_STATE）。
- **D2 实现**：`RemoteInputChannel` 入队 `put` → `offer(enqueueOfferTimeoutMs)`（默认 10s 构造可调）；满窗零进展 → `overflowError` typed 标志（新错误码 `ERR_STREAM_CHANNEL_OVERFLOW` 携 timeoutMs/topic）+ finished + EOS 唤醒；read 路径 take/poll 后**先查通道错误再判 EOS**（溢出禁止以正常 EOS 返回 = 禁止静默确认截断数据）；`isOverflowed()` 可观测钩子。
- **focused 测试**：`TestSubscriptionScopeConvergence` 8 用例（中位/汇/源 subtask 订阅清单精确断言（多上游多入边 A→B/D→B 图）+ union 累积 + 全图结构保留 + null 全对/空零订阅读者语义 + 未订阅读取防护）；`TestRemoteInputChannelQueueFull` 3 用例（无读者满队列 dispatch 阻塞 ≈ 有界窗实测 + typed 错误码/参数断言；健康读者 50 条按序不丢不重；健康慢读者连续背压零误报 = BP-1 500ms 档语义钉定）。
- **验证**：runtime 全量 **990/0/0/10 绿**；全量 `./mvnw test -pl nop-stream -am -T 1C` 11 模块 BUILD SUCCESS（fraud 110/0/0/13）；`clean install -DskipTests` 门禁过。owner-doc 裁定 = Phase 1 design doc 与实态一致无偏差回写。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1951-3 Phase 1——items 28+31 设计裁定 D1—D4 落档）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-03-1951-3-remote-deploy-dataplane-stability.md`（items 28+31 合并执行）
- **D1（订阅收敛切面）**：裁定候选①（拒绝惰性订阅②）——构建器订阅范围三态：全订阅（单 JVM 执行器默认，逐字不变）/ 指定 subtask 集（TM remote-deploy 每 deploy = assigned 单例；被收敛通道构造但不订阅，无激活时序假设；producer 全矩阵保留）/ 零订阅（D1(f) **纳入收敛**：RpcDistributedExecutor remoteDeployMode=true 协调器侧 plan 零订阅，结构保留终态零订阅）。硬约束 (a)—(f) 逐条论证 + union 累积 + 未订阅通道读取 typed 防护。
- **D2（队列满语义）**：裁定候选①（拒绝重投协议②）——入队有界等待默认 10s（健康慢消费者槽位释放即成功 = dispatch 层背压保留）；满窗零进展 ⇒ decodeError 同款通道内 typed 溢出标志（禁止以正常 EOS 返回）→ task FAILED → 恢复；重放论证 = JDBC cursor-0 重读 + 新 epoch fencing + producer checkpoint 重发；LOCAL 后端 = 保留进程内背压、静默挂死升级为可见失败。等待窗 10s 与 60s liveness/30s 租约阈值的安全余量论证（租约续期独立线程不经消息后端）。
- **D3（stall 预算区分）**：裁定候选①机制 + ②证据双轨——恢复触发按原因分池（租约到期/FAILED/兼容入口 → 真实故障预算 maxRestarts=3 不变；liveness stall → 独立 maxStallRestarts=3 + 冷却 30s 窗内 WARN 跳过 + 超限 failJob）；检测器分类节点失效优先；fencing 轮转与原因无关。拒绝纯②理由 = 共享预算是结构性缺陷属性（未来任意 stall 源都会静默消耗真实故障预算）。
- **D4（复验范围）**：BP-1 与 CHAOS-2 **均全格复验纳入**（BP-1 = 唯一持续输入+慢消费者排空形态，D2 不误伤核心回归点；CHAOS-2 = 以证据关闭「jam 消除后新 leader 重发 assignment + fencing 轮转」遗留问题，非论证代验证）。
- **落档**：新建 `ai-dev/design/nop-stream/dataplane-transport-design.md`（§一问题/§二 D1/§三 D2/§四 D3/§五 D4/§六 JDBC retained manifests 双存储契约/§七使用契约）+ `README.md` 注册（Updated 头/架构基线层条目/阅读顺序 3b）；`check-doc-links --strict` exit 0（0/0）。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1951-2 closure——item 27 done，F-C/W-5 收敛验收达成）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-03-1951-2-control-plane-fencing-cancel-task.md`（item 27，收口）
- **Closure audit**（独立 general subagent fresh session `ses_f97eeefdaffeKnPxF1UCRJ0CUp`）：**CLOSURE-AUDIT: APPROVED**（0 Blocker / 0 Major / 3 Minor 已处置）——九区逐项 PASS：接口/替身 live rg 复算（4 参全覆盖，作用面外同名签名仅已排除项）、TaskManager fencing/原子性/条件回滚逐行核验（:699-706/:543-551/:576）、唯一生产调用方接线（:2050 `getFencingEpoch()`）、6 测试用例 live 核对（interleave 竞态注入成立性确认）、Anti-Hollow 全链追踪（epoch 端到端载运）、owner-docs 五处核验（docs-for-ai rg 无 cancelTask = 无需求成立）、诚实性（checkbox 39×[x]、Deferred 空、follow-up A/B 证据采信）、一致性（Phase 状态/门禁/日志五处一致）；现场复跑 focused 39/0/0 + hollow/invariants exit 0。
- **收口仪式**：Closure Gates 12/12 勾选 + Closure evidence 写入 → Plan Status `completed`（Completed: 2026-09-04）；roadmap item 27 → `done`（验收摘要 + audit session）+ Last updated 同步；`check-plan-checklist --strict` exit 0；`check-doc-links --strict` exit 0（0/0）。
- **Minor 处置**：杂散工作区噪声 `nop-format/.../_rels/.rels`（行尾抖动，非本 plan 文件）不纳入提交；plan 内 JobCoordinator 调用点行号漂移（:2047/:2050/:2055 同点）非矛盾，留痕。

### 2026-09-04（mission nop-stream-productization EXEC_PLANS：plan 1951-2 全量执行——cancelTask fencing + slot-replace 原子化）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-03-1951-2-control-plane-fencing-cancel-task.md`（item 27，F-C/W-5）
- **Phase 1（盘点 + 裁定）**：仓库级 rg 全量清单落 plan——main 面 M1—M6（接口声明 / TaskManager 唯一实现 / JobCoordinator:2047 唯一生产调用方 / transformer+server+proxy 验证性零变更 / SupervisionLoop+RpcDistributedExecutor 非调用方核对）+ 测试替身 20（nop-stream 19 + 跨模块 nop-sys-dao 1）+ 直调 8 处；同名无关签名显式排除（ITaskExecutionQueue/RpcTaskMonitor/JobScheduleStoreImpl/ResourceRecordConsumerProvider/SplitAssignmentProxy/cancelTaskWithMailbox）。裁定：**D1 直接改签名**（全消费方在仓内编译期迁移；default 桥会静默丢 epoch 或降级为测试期陷阱，JobCoordinator 是生产调用方故 deployTask default-throw 先例前提不成立）；**D2 方案①**（typed `ERR_STREAM_FENCING_TOKEN_MISMATCH` + WARN；不上报 FAILED——被拒时任务在 active 代健康，上报会诱发虚假恢复、让 zombie 间接改变 active 代状态）；**D3 不做跨版本 wire 兼容**（同版本部署不变式，混布行为推演记录：旧收新 fail-open 不承诺 / 新收旧缺参 fail-fast）。
- **Phase 2（实现）**：`IStreamTaskRpcService.cancelTask` 增 `long fencingEpoch`（javadoc 契约）；`TaskManager.cancelTask` 入口 `!=` 校验 + WARN（expected/actual）+ typed throw；`TaskManager.deployTask` slot-replace 由 get→remove→put 收敛为**单原子 `put` 返回 displaced entry**（每个被换出任务恰好交给 displacer cancel + permit release）+ build 失败回滚改条件 `remove(taskKey, runningTask)`（不误删后继条目）；`JobCoordinator` abort handler 传 `getFencingEpoch()`；20 替身 + 8 直调点同步迁移（rg 复核零残留，编译器强制）。
- **Phase 3（验证）**：
  - focused 4 用例（`TestTaskManager`）：stale-epoch 双向拒绝（错误码 + 参数 + 任务存活 + permit 不变）/ valid-epoch 取消 + permit 释放 / 同 key 二次 deploy 原子换槽（displaced 退出、successor 独占、净 permit 0）/ **确定性 build-failure interleave**（阻塞式失败图 park deploy 线程 → successor 换槽运行 → 放行失败 → 断言 successor 条目存活 + permit 守恒；**临时回退旧代码复验 expected 1 but was 0** = 测试真实钉住 W-5 缺陷）。
  - RPC round-trip：`TestStreamControlRpc` 增 `lastCancelEpoch` 断言（epoch 经真实 RPC pair 存活）；`TestDistributedAbortPath` 增 `cancelTaskEpochs == coordinator.getFencingEpoch()`（abort → RPC → server 全链载参）。执行时裁定：不扩展 `probeStaleEpochRejection` cancelTask gated 变体（分段证据充分，与 triggerCheckpoint probe 共享 one-way 拒绝面）。
  - 全量 `./mvnw test -pl nop-stream -am -T 1C` 全绿（首轮 `TestBufferPool`（core，未触碰模块）并行负载 timing 抖动：隔离 + 整体复跑均绿）。
  - 跨模块门禁 `./mvnw test -pl nop-sys/nop-sys-dao -am` 43/43 绿（含迁移后 `TestJobCoordinatorWithSysDaoLeaderElector` 4/4）。
  - gated：fraud-example **13/13 绿**；legacy **6/7 绿**——`TestMultiJvmExactlyOnceRecovery#multiJvmDeployKillRecoverFencing` 经 **git-stash A/B 对照**（同机同命令 baseline 同样失败、同签名 `initial=1 recovered=1`；本 diff 复跑 failover 3/3 绿）判定 pre-existing（恢复检测超时，本机负载相关），非回归，记录 follow-up。
  - 工具门禁：`scan-hollow-implementations --module nop-stream-runtime --severity high` exit 0、`check-nop-stream-invariants` exit 0（无 re-pin）。
- **Owner-doc 同步**：`checkpoint-design.md` 4 处（§2.8.1 D3 option (A) **部分 supersession** 注记——fencing epoch 维度已落地、checkpointId-precise 维度维持拒绝；§8.7 分布式 abort 段补 fencing 语义 + D2 可观测性论证；§13.2 两行契约；§13.2.1 Successor supersession 注记）；`component-roadmap.md` §3 abort 接线/abort 传播通道两行；`distributed-runbook.md` §1 拓扑图注记；`docs-for-ai/03-modules/nop-stream.md` **无需求**（不描述内部控制面 RPC 签名）。
