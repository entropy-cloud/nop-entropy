### nop-ai-agent ICheckpointManager Contract + NoOpCheckpoint + ToolExecutionCheckpoint（Plan 181 — all phases + closure）

- Plan: `ai-dev/plans/181-nop-ai-agent-checkpoint-manager.md` → completed（Phase 1 + Phase 2 + Phase 3 + Closure）
- 交付可靠性层 §5.4 检查点 **记录/检索能力**：`ICheckpointManager` 契约 + `NoOpCheckpoint` 透传默认 + `ToolExecutionCheckpoint` in-memory 功能化实现 + executor/engine 分发循环接线
- Phase 1：契约表面 + NoOp 默认 + 值类型 + 单元测试
  - 新增 `io.nop.ai.agent.reliability` 包（与 `io.nop.ai.agent.security` 平行——可靠性增强非安全治理）
  - `CheckpointType` 枚举：`TOOL_EXECUTION`（L3-4 交付）+ `LLM_TURN` / `COMPACTION`（roadmap A4 前向声明）
  - `Checkpoint` 不可变值类型：sessionId/watermark/seq/timestamp/type/toolName/callId/inputSummary/outputSummary/messageCount/tokenEstimate，工厂构造 + 全字段 equals/hashCode（参照 `DenialRecord` 模式）
  - `ICheckpointManager` 接口：`saveCheckpoint` / `getLatestCheckpoint` / `getCheckpoint(watermark)`，Javadoc 声明线程安全 + 持久化非强制（与 L3-6 L3-G5 一致）+ save-side 先行 / restore-side 延期裁定
  - `NoOpCheckpoint` 透传默认：singleton + `noOp()` 工厂（参照 `NoOpDenialLedger.noOp()` 模式），saveCheckpoint no-op / 检索返回 null
  - `TestNoOpCheckpointAndValue`（14）：NoOp pass-through 语义 + 线程安全（8 线程 ×200 调用）+ Checkpoint 构造/不可变性/factory 校验
- Phase 2：功能化实现 + executor/engine 接线 + 端到端 + 向后兼容
  - `ToolExecutionCheckpoint` 功能化实现（implements `ICheckpointManager`）：`ConcurrentHashMap<String, List<Checkpoint>>` per-session 存储 + `ConcurrentHashMap<String, Checkpoint>` watermark 索引，saveCheckpoint 真实追加、getLatestCheckpoint 返回最后一个、getCheckpoint 精确检索。in-memory 不持久化。对所有工具执行记录 checkpoint（不套用 §5.4a "仅 long-running tool" 限定——in-memory 无 I/O 成本，过滤下沉到持久化层）
  - `ReActAgentExecutor` 扩展：`checkpointManager` 字段 + ctor 参数（null-fallback `NoOpCheckpoint.noOp()`）+ Builder 字段 + Builder setter + `Builder.build()` pass（追加为最后一个扩展参数，在 `postDenialGuard` 之后）
  - `ReActAgentExecutor` 分发循环保存侧接线：工具执行完成后（tool result 加入 context 后、POST_ACTING hook 前）调用 `checkpointManager.saveCheckpoint(Checkpoint.of(...))`，类型 `TOOL_EXECUTION`，含 toolName/callId/input-output 摘要 + messageCount/tokenEstimate 快照 + 递增 seq + watermark
  - `DefaultAgentEngine` engine 接线：`checkpointManager` field（默认 `NoOpCheckpoint.noOp()`）+ `setCheckpointManager`/`getCheckpointManager`（null-fallback）+ `Builder.build()` pass 到 executor
  - `TestToolExecutionCheckpoint`（7）：save→retrieve 往返（单次 + 多次 latest + watermark 精确检索 + nonexistent 返回 null）+ per-session 隔离 + null-session-id 行为
  - `TestCheckpointDispatchPathWiring`（7）：Builder 接线（saveCheckpoint 运行时被调用 + payload 验证）+ 多工具多 checkpoint seq 递增 + **端到端** `engine.execute(request)` → checkpoint 含正确 tool payload + 向后兼容（NoOp 默认 0 副作用）+ Builder 默认 + engine setter/getter null-fallback + per-session 跨 engine 执行隔离
- Phase 3：设计文档 + roadmap 状态同步
  - `nop-ai-agent-reliability.md` §5.4 更新：记录/检索能力标记 ✅ 已落地 + 架构决策（save-side 先行 / in-memory 不持久化 / reliability 包归属 / in-memory 触发范围）+ §5.4a 标注为 roadmap A4 deferred
  - `nop-ai-agent-roadmap.md` §4 L3-4 行 `❌` → `✅`；§2.2 row 54 `❌ 未开始` → `✅ 已落地`；A4 blocked-by L3-4 依赖已满足
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1211 tests, 0 failures, 0 errors（+28 新增 = Phase1 14 + Phase2 14；现有 1183 全部通过，0 副作用）
- 验证：`node ai-dev/tools/check-plan-checklist.mjs ... --strict` exit 0；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-ai-agent --severity high` exit 0
- Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-reliability.md` §5.4 已更新；`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` L3-4 + §2.2 row 54 已 ✅
