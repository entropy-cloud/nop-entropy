### plan-198: nop-ai-agent 异常一致性清理（AUDIT-09-02，吸收 AUDIT-09-03）

- **Plan**: `ai-dev/plans/198-nop-ai-agent-exception-consistency-cleanup.md`
- **Work Item**: AUDIT-09-02（吸收 AUDIT-09-03）
- **Phase 1 — 迁移策略裁定与影响面落档** ✅:
  - 5 条裁定写入 plan：(1) 迁移映射 `throw new IllegalArgumentException(msg[,cause])` → `throw new NopAiAgentException(msg[,cause])`，`throw new IllegalStateException(msg)` → `throw new NopAiAgentException(msg)`，消息文本不变（构造器签名兼容，plan 196 已交付）；(2) 5 处 `catch (IllegalArgumentException)`（`FileSystemSkillProvider:275,284` / `LLMCurator:285` / `AgentMessageEnvelopeJson:108` / `CheckpointJournalReader:177`）**不迁移**——捕获 JDK `Enum.valueOf()` 异常，非本模块 throw 站点消费者；(3) 测试断言迁移 `assertThrows(IllegalArgumentException.class,...)` → `assertThrows(NopAiAgentException.class,...)` 与 `catch (IllegalArgumentException expected)` → `catch (NopAiAgentException expected)`，按需补 import；(4) `LocalAgentMessenger:101-102` `completeExceptionally(IllegalStateException)` 不在 scope（异步完成语义，归 Non-Blocking Follow-ups）；(5) import 策略：按需补 `import io.nop.ai.agent.engine.NopAiAgentException`，无需移除 `java.lang.IllegalArgumentException`（java.lang 隐式导入）
- - No owner-doc update required：变更使模块**符合**已有 `docs-for-ai/02-core-guides/error-handling.md`（"模式二" + 反模式表）
- - **Phase 2 — 执行迁移（main + test）** ✅:
  - main code：`find ... -exec sed -i 's/throw new IllegalArgumentException/throw new NopAiAgentException/g'` 批量迁移 24 个 main 文件的 49 处 throw 站点；`Layer2TurnPruningStrategy.java` 的 1 处 `throw new IllegalStateException` 同步迁移（消息文本不变，构造器签名兼容）
  - import 补充：Python 脚本对 22 个非 engine 包 main 文件 + ISessionStore 共 23 个文件按 io.nop.\* 分组字母序插入 `import io.nop.ai.agent.engine.NopAiAgentException;`（engine 包内文件同包无需 import；DBMessageService/DBDenialLedger 已有 import）
  - test code：批量迁移 `assertThrows(IllegalArgumentException.class,` → `assertThrows(NopAiAgentException.class,`（49 处）+ `catch (IllegalArgumentException expected)` → `catch (NopAiAgentException expected)`（4 处）；12 个非 engine 包 test 文件补 import（engine 包 test 文件同包无需 import）
  - 5 处 `catch (IllegalArgumentException)` Enum.valueOf 翻译块保持不变（FileSystemSkillProvider:275,284 / LLMCurator:286 / AgentMessageEnvelopeJson:108 / CheckpointJournalReader:177）
  - `LocalAgentMessenger:102-103` `completeExceptionally(IllegalStateException)` 未触碰（Non-Goals）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS
- - **Phase 3 — 全量验证与 roadmap 同步** ✅:
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1547 tests, 0 failures, 0 errors**（零回归；迁移后的 `assertThrows(NopAiAgentException.class)` 全部实际捕获对应 `throw new NopAiAgentException`）
  - roadmap §5b 新增 `AUDIT-09-02` 行（`nop-ai-agent-roadmap.md:276`）：`✅ 已修复`，标注 plan 198（吸收 AUDIT-09-03），说明 49 处 IllegalArgumentException + 1 处 IllegalStateException 收敛 + 5 处 Enum.valueOf catch 块不变 + 1547 tests 零回归
- - **Closure 工具**：`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --severity high` 报告 15 个 pre-existing UOE stubs（历史 plan 84/86/97/98），均不在本 plan 迁移文件内，与本 plan 无关
- - **Closure Audit**：独立 closure-audit subagent（fresh session，task_id `ses_1341cae27ffeGM6XSzrMXIdpNR`）已完成 read-only 审计——9 条 closure gates 全 PASS + 3 文件 spot-check 确认真实 throw 站点；evidence 写入 plan 的 `## Closure` 段；Plan Status: completed

### Omnigent（Databricks meta-harness）深度调研 + 与 nop-ai-agent 对比报告

- 无 Plan（ad-hoc 调研）：调研 `~/ai/omnigent`（Databricks 出品、Apache-2.0、Alpha、Python ≥3.12 的"meta-harness"框架），对比 `nop-ai/nop-ai-agent`
- 新增报告：`ai-dev/analysis/agent-survey/2026-06-15-omnigent-vs-nop-ai-agent-deep-comparison.md`（16 章节，从架构哲学/调度模型/执行循环/Agent 定义/工具/策略-安全/记忆会话/多 Agent/可靠性/LLM provider/协作面/测试 12 维度对比）
- **核心发现**：Omnigent 是 agent-survey 中**首个真正意义的 meta-harness 样本**——本身不实现通用 ReAct 循环（委托给被包裹的 Claude Code/Codex/Pi/OpenAI Agents SDK/Databricks Supervisor），而是叠加治理/协作/隔离控制平面。这与 nop-ai-agent（自拥有循环的引擎）属**正交架构原型**，不在同一抽象层
- **修订**（用户 review 后修正三处事实错误）：(1) Nop 有内置 harness 等价物（`DefaultAgentEngine` + `AgentToolExecuteContext` + `IToolManager` + 安全链），区别是 Omnigent harness 可插拔/可声明切换、Nop 是单一内置后端——非"无对应物"；(2) Nop 的隔离是**应用层门控范式**（虚拟 shell `nop-ai-shell` in-JVM 解释无 fork/exec + `IToolFileSystem` 路径禁锢 + 6 层安全链 + worktree 设计），与 Omnigent 的 OS 牢笼（bubblewrap/seatbelt）正交，**非缺口**；`ISandboxBackend` 是 deferred Layer-4 槽（主动选择不做 OS 沙箱）；诚实标注：虚拟 shell 当前尚未接线到 `bash` 工具（`BashExecutor` 仍用真实 OS `sh -c`，见 `nop-ai-shell-design.md` §4.3 进行中设计）；(3) worktree 在 Nop 设计范围内（`nop-ai-agent-branch-affinity-scheduling.md` status `active`，已审计：`IWorktreeManager` + Worktree Registry + Branch Affinity Scheduling），但零 Java 实现——区别是成熟度（Omnigent 已实现 / Nop 仅设计）
- **裁定（修订后）**：Nop 不应走 meta-harness 路线（与 DSL-First + 循环可控哲学冲突 + 复杂度爆炸），也**不应引入 OS 沙箱**（与 in-JVM 应用层门控范式冲突，无 OS 进程即无 OS 攻击面）
- **对 Nop 的真实 P0**：(1) 接线虚拟 shell 到 `bash` 工具（`ShellBashExecutor`，Nop 既有设计闭环的最后一块拼图）；(2) cost 治理（借鉴，`IModelRouter` 默认 PassThrough）；(3) 推进 worktree 已审计设计实现（非借鉴，Nop 自有设计）；(4) 精确 token 估算（借鉴，引入 jtokkit）
- Doc-sync: No owner-doc update required（本报告为分析文档，结论未沉淀为规范）；引用的 `ai-dev/design/nop-ai-agent/*` 均已存在，未改动
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` → 本报告 0 broken link（修订前后均通过）
- **二轮深化**（用户要求对比 Nop 已有设计的具体细节）：新增报告 §十七（Nop 已有设计 vs Omnigent 实现：逐点对比与可借鉴点），分 worktree/隔离/harness 三领域逐点对照。深挖发现**关键反转**：Omnigent worktree 仅 388 行（`host/git_worktree.py`），刻意极简——无 symlink/cleanup_stale/TTL/锁/Registry/资源上限/orphan sweeper；Nop 的 `branch-affinity-scheduling.md`（764 行已审计）设计了 Worktree Registry + 跨节点 orphaned + Branch Affinity Scheduling + 资源上限 + symlink 大目录 + per-worktree 锁 + Fast Resume + crash 策略，**整体领先 Omnigent 实现**。可借鉴的是 5 个具体工程防御（`--end-of-options` argv 注入防护、`git worktree list --porcelain` 主工作树解析、120s 统一超时、fail-loud 安装检测、冲突上限常量化）+ Omnigent 极简哲学的反思（Registry 分层启用）。隔离领域可借鉴 `_cwd_scan` 递归扫描+escaping-symlink 检测、dotfile allowlist、egress 规则 DSL+DNS 安全正则。**harness 领域最高价值借鉴=双看门狗模型**（IDLE 240s 可重置 + ABSOLUTE 3600s 不可重置），填补 Nop"无进展超时检测"空白（当前 Nop 若 LLM 调用 hang 无看门狗能捕获）。已把双看门狗+worktree 防御补入 §16 P1 借鉴清单

### plan-197: nop-ai-agent 同 Session 并发执行竞态修复（AUDIT-14-01）

- **Plan**: `ai-dev/plans/197-nop-ai-agent-session-concurrency-guard.md`
- **Work Item**: AUDIT-14-01
- **Phase 1 — 并发保护策略裁定与设计落档** ✅:
  - 6 条裁定写入 `nop-ai-agent-reliability.md` 新增 §1.3 子节：(1) 去重注册 = `putIfAbsent` + fail-fast（字符串消息，不用 ErrorCode）；(2) 注销 = 值比较 `remove(sessionId, handle)`（CancelHandle 引用相等，无 equals 覆写）；(3) cancel-window 修复 = 选项 A（同步阶段预注册 handle），cancel 信号经由 `ctx.cancelRequested`（已 volatile），不经由 `AgentSession.status`；(4) `CancelHandle.thread` 改为 `volatile Thread`，同步阶段初始化 null，lambda 入口更新为执行线程，`cancelSession` null 检查后再 interrupt；(5) `restoreSession.containsKey` 检查移除（putIfAbsent 是唯一原子 guard）；(6) `AgentSession.status` volatile 不纳入 scope（cancel 信号不经由 status）；`sendMessage` 向后兼容 = fail-fast 异常传播（不新增 error ack）
  - No owner-doc update required for `docs-for-ai/`（本模块无独立 owner doc 章节）
- - **Phase 2 — 实现并发 guard + cancel-window 修复** ✅:
  - `CancelHandle`（`DefaultAgentEngine.java:709-717`）：`final Thread thread` → `volatile Thread thread`（延迟绑定支持）
  - `doExecute`（`:772-803`）：同步阶段创建 handle（thread=null）+ `putIfAbsent` + fail-fast；supplyAsync 失败时 cleanup；lambda 入口 `handle.thread = Thread.currentThread()`；`finally` 改为值比较 `remove(sessionId, handle)`
  - `resumeSession`（`:967-991`）：与 doExecute 完全对称改动
  - `restoreSession`（`:1002-1091`）：移除冗余 `containsKey` 检查；与 doExecute 对称的 putIfAbsent + fail-fast + 值比较 remove + cancel-window 修复
  - `cancelSession`（`:626-655`）：`handle.thread.interrupt()` 前加 null 检查（入队窗口内 forced cancel 不误 interrupt 调用线程）
- - **Phase 3 — Focused 测试 + roadmap 同步** ✅:
  - 新增 `TestDefaultAgentEngineConcurrencyGuard`（5 个 focused 测试）：concurrent-execute-fail-fast / finally-no-misremove / cancel-window-honored / restore-guard-consistent / no-regression-normal-path
  - roadmap §5b：`AUDIT-14-01` 行 ❌ → ✅，落地 plan 标注 197
- - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（零回归）
- - **Closure 工具**：`check-plan-checklist.mjs` exit 0；`scan-hollow-implementations.mjs` exit 0
- - **Closure Audit**：独立 closure-audit subagent（fresh session）已完成；evidence 写入 plan 的 `## Closure` 段；Plan Status: completed

- - **Plan**: `ai-dev/plans/196-nop-ai-agent-exception-base-class.md`
- - **Work Item**: AUDIT-09-01
- - **Phase 1 — 基类切换策略裁定与设计落档** ✅:
  - 5 条裁定写入 plan：(1) 采用 `error-handling.md` "模式二" 模板四构造器（`(String)` → `super(message, null, true, true)`、`(String, Throwable)` → `super(message, cause, true, true)`、`(ErrorCode)` → `super(errorCode)`、`(ErrorCode, Throwable)` → `super(errorCode, cause)`——前两签名保留，100+ 抛出站点零改动；已核对 `NopException.java` 仅有三构造器，无 `(String)` 单参，故字符串构造器走 4 参委派）；(2) 包位置不变（`io.nop.ai.agent.engine`）；(3) 事务回滚无回归（`shouldRollback` 对 NopException 默认回滚，与非 NopException 默认回滚一致）；(4) `getMessage()` 语义变化影响评估（结构化 `getMessage()` 把原文本作为 `errorCode=` 子串嵌入，约 20 处 `.contains()` 断言全部兼容；唯一 `.equals(plainString)` 站点抛 `UnsupportedOperationException`，不受影响）；(5) 不覆写 `param()`/`description()` 协变返回（遵循模板）
  - No owner-doc update required：变更使模块**符合**已有平台规范 `docs-for-ai/02-core-guides/error-handling.md`（"模式二"模板 + 反模式表 line 163 明文点名 `extends RuntimeException`）
- - **Phase 2 — 实现基类切换** ✅:
  - `engine/NopAiAgentException.java`：`extends RuntimeException` → `extends NopException`；新增 `private static final long serialVersionUID = 1L;`；两现有构造器 super 委派改为 4 参（签名不变）；新增 `(ErrorCode)` 与 `(ErrorCode, Throwable)` 构造器；新增 import `ErrorCode`、`NopException`
  - 既有 100+ 抛出站点（`(String)` / `(String, Throwable)`）与 4 处 `catch (NopAiAgentException)` 站点零源码改动（构造器签名保留）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS
- - **Phase 3 — Focused 测试 + roadmap 同步** ✅:
  - 审计既有测试受影响面：无回归（`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1542 tests, 0 failures, 0 errors**，含 8 个新增 focused tests，零回归）
  - 新增 `TestNopAiAgentExceptionBaseClass`（8 个 focused 测试）：(1) `instanceof NopException` + 仍 `instanceof RuntimeException`（向后兼容 catch）；(2) `getErrorCode()` 返回原消息文本；(3) `getMessage().contains` 兼容性断言（结构化 `getMessage()` 把原文本作为 `errorCode=` 子串嵌入）；(4) `.param(...)` 链式 + 可 `throw` 后 `getParam` 取回（**接线验证 #23**——实际调用新能力）；(5) `(ErrorCode)` 构造器 `getErrorCode()` 返回 `ErrorCode.getErrorCode()`（**接线验证 #23**）；(6) `(String, Throwable)` 保留 cause 链；外加 `(ErrorCode, Throwable)` ctor + `(String)` ctor 无 cause 两个 sanity tests
  - roadmap §5b：`AUDIT-09-01` 行 ❌ → ✅，落地 plan 标注 196（`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md:275`）
- - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1542 tests, 0 failures, 0 errors（1534 既有 + 8 新增，零回归）
- - **Closure 工具**：`check-plan-checklist.mjs ai-dev/plans/196-nop-ai-agent-exception-base-class.md --strict` exit 0
- - **Closure Audit**：独立 closure-audit subagent（fresh session）已完成；evidence 写入 plan 的 `## Closure` 段；Plan Status: completed

- - **Plan**: `ai-dev/plans/195-nop-ai-agent-atomic-file-write.md`
- - **Work Item**: AUDIT-14-04
- - **Phase 1 — Crash-Safe 写入策略裁定与设计落档** ✅ (已在前序 run 完成)：
  - `nop-ai-agent-reliability.md` 新增"文件写入 crash-safety"子节，记录：选用 write-to-tmp + `Files.move(ATOMIC_MOVE, REPLACE_EXISTING)` + `finally { deleteIfExists(tmp) }` 模式（对齐仓库内 nop-stream `LocalFileCheckpointStorage` 先例），理由是 POSIX rename(2) 原子性；拒绝"仅 TRUNCATE_EXISTING 改为 write 新文件不 truncate"等替代方案；tmp 命名为 `target.getFileName() + ".tmp"`（sibling，同 mount point，满足 ATOMIC_MOVE 前置条件）；`ioLock` 语义不变（保护同一 writer 实例的并发写不交错，与 move 自身的原子性正交）；裁定为 inline 修复（两个 writer 各自内联 tmp+move+finally，不提取共享 helper——copy-paste 程度可控、不引入新的模块内依赖方向）
- - **Phase 2 — 实现 crash-safe 原子写** ✅ (已在前序 run 完成)：
  - `SessionFileWriter.write()`（`session/SessionFileWriter.java:74-106`）：`synchronized(ioLock)` 块内 → `tmp = sessionFile.resolveSibling(sessionFile.getFileName() + ".tmp")` → `Files.createDirectories(parent)` → `Files.write(tmp, ..., CREATE, WRITE, TRUNCATE_EXISTING)` → `Files.move(tmp, sessionFile, ATOMIC_MOVE, REPLACE_EXISTING)` → `finally { Files.deleteIfExists(tmp); }`；IOException catch 范围包 tmp-write + move 两步，统一转 `NopAiAgentException`；新增 `import java.nio.file.StandardCopyOption`
  - `CheckpointSnapshotWriter.write()`（`reliability/CheckpointSnapshotWriter.java:65-97`）：完全对称改动
  - 两个 writer 均无静默跳过：finally 内 `deleteIfExists` 的 IOException 仅 best-effort 不重抛（避免掩盖主异常）—— 这是 Phase 1 裁定的模式
- - **Phase 3 — Focused 测试 + roadmap 同步** ✅ (本次 run 完成)：
  - 审计既有测试受影响面：无回归（`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1534 tests, 0 failures）
  - 新增 `TestSessionFileWriterAtomicWrite`（7 个 focused 测试）：(1) atomic-write-target-intact + (2) tmp-cleanup (`successfulWriteLeavesCompleteTargetAndNoTmp`) + (3) stale-tmp-recovery (`staleTmpIsOverwrittenAndRemovedAfterWrite`) + (4) pre-move-failure-isolation / crash-safety 核心保证（`preMoveFailureLeavesExistingTargetUntouched`——failure 注入方式：在 tmp 路径预创建空目录，使 `Files.write` 抛 "Is a directory" IOException，断言 target 仍为完整 JSON-A、未被截断）+ (5) overwrite-write (`consecutiveOverwritesLeaveLatestContentAndNoTmp`) + (6) 端到端接线（`fileBackedStoreSaveWritesViaAtomicPathToDisk`——从 `FileBackedSessionStore.save()` 入口经 `SessionFileWriter.write()` 到磁盘 session.json 完整可读，cross-instance reload 验证）+ byte-level sanity (`writtenBytesAreNonEmptyJson`)
  - 新增 `TestCheckpointSnapshotWriterAtomicWrite`（6 个 focused 测试）：mirror SessionFileWriter 的前 5 个行为点 + smoke test through `FileBackedCheckpointManager.flushSnapshot()`（`managerSnapshotWriteUsesAtomicPath`——接线验证 snapshot 写入路径）
  - roadmap §5b：`AUDIT-14-04` 行 ❌ → ✅，落地 plan 标注 195（`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md:274`）
- - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1534 tests, 0 failures, 0 errors（含 11 个新增 focused tests，无回归）
- - **Closure 工具**：`check-plan-checklist.mjs ai-dev/plans/195-nop-ai-agent-atomic-file-write.md --strict` exit 0；`scan-hollow-implementations.mjs --module nop-ai/nop-ai-agent --severity high` exit 0（15 个 pre-existing UOE stubs 不在 `SessionFileWriter.java` / `CheckpointSnapshotWriter.java`，与本 plan 无关）
- - **Closure Audit**：独立 closure-audit subagent（fresh session，task_id `closure-audit-195`）已完成；evidence 写入 plan 的 `## Closure` 段；Plan Status: completed

### goal-driver: 合并 PLAN_DRAFT + PLAN_AUDIT 为单步（v5）

- **背景**：连续自动循环中，PLAN_AUDIT 审查只紧跟 PLAN_DRAFT 发生，独立编排步骤不增加信息只增加 round-trip 开销。审查的独立性来自独立 session sub-agent（不同 task_id），而非独立编排步骤。ROADMAP_CHECK 保持独立——"是否有计划工作"的路由决策（pending→draft vs complete→deep-audit）需要外部判断。
- **改动**：
  - `flows/goal-driver.json`：移除 PLAN_AUDIT 步骤；PLAN_DRAFT 的 `created` 直接 goto EXECUTE_ALL_ACTIVE_PLANS（不再经过 PLAN_AUDIT approved/issues/retry/onMaxRetries 转移）
  - `prompts/plan-draft.md`：合并 plan-audit.md 的 coordinator 内容。agent 先写计划（Phase 1 Author），再切换 coordinator 模式 spawn 独立子 agent 对抗性审查（Phase 2，最多 2 轮，pass criterion = zero Blocker/Major），通过或 degraded 后输出 `created`。明确引用 plan guide §"Before & After Drafting: 迭代对抗性审查"的独立子 agent 要求，防止 AI 遗忘
  - 删除 `prompts/plan-audit.md`（仅主流程 PLAN_AUDIT 使用，deep-audit-loop 有独立的 draft-plans.md）
  - `src/runner.js`：移除 PLAN_AUDIT 的 STEP_KEY_MAP 映射、mock 响应、`_mockPlanAuditCount` 计数器
  - `src/prompts.js`：移除 STEP_NAMES.PLAN_AUDIT 和对应 test step config
- **降级兜底**：pass criterion 未达成时仍输出 `created`（degraded mode），计划进入执行，由下游 closure audit / deep audit 兜底。原引擎级 onMaxRetries degraded 路径不再需要
- **验证**：`node --test test/engine.test.js test/transitions.test.js test/intg.test.js test/goal-driver-verify.test.js` → 43 tests, 43 pass, 0 fail（`transitions.test.js` 中用 PLAN_AUDIT 作为本地 fixture 名测试引擎 retry 机制的用例不受影响）
- **Doc-sync**：`goal-driver-flow-design.md`（mermaid + 步骤说明 + 结论 #5 + 拒绝方案表）、`flow-engine-design.md`（文件树 + mermaid）、`group-step-design.md`（§6.2 加 superseded 注记）已更新
- Commit: TODO

### plan-194: nop-ai-agent 审计 Logger 默认装配（Audit Trail Secure by Default）

- **Plan**: `ai-dev/plans/194-nop-ai-agent-audit-logger-default.md`
- **Work Item**: AUDIT-13-02
- **Phase 1 — 审计 Logger 默认装配策略裁定与设计落档** ✅:
  - `nop-ai-agent-security-and-permissions.md` §4.6 更新（WARN 覆盖范围行加入 `IAuditLogger`；Builder 一致性行同步反映 audit logger null 兜底切换）
  - 新增 §4.7"审计 Logger 默认装配（Audit Trail Secure by Default）"：记录决策（field+setter 默认 `Slf4jAuditLogger` + `resolveExecutor` 透传 + `Builder.build()` null 兜底切换 + `warnIfInsecureDefaults` 扩展检查 `NoOpAuditLogger`）、为什么选 field+setter 而非构造器重载、为什么 setter 触发 WARN（对照 `setDenialLedger`）、拒绝的替代方案、向后兼容原则
  - 设计文档不含类签名/伪代码（只记录决策与契约，遵循 Minimum Rules #14）
- - **Phase 2 — 实现默认审计装配与 WARN 扩展** ✅:
  - `DefaultAgentEngine`：新增 `private IAuditLogger auditLogger = new Slf4jAuditLogger()` 字段（紧邻 `denialLedger`/`postDenialGuard` 字段块）；新增 `setAuditLogger` setter（null 入参保留默认 `Slf4jAuditLogger`，赋值后调用 `warnIfInsecureDefaults` 触发 NoOp WARN）+ `getAuditLogger` accessor
  - `DefaultAgentEngine`：`resolveExecutor` Builder 链新增 `.auditLogger(this.auditLogger)`（紧邻 `.denialLedger(...)`/`.postDenialGuard(...)`）
  - `DefaultAgentEngine`：`warnIfInsecureDefaults` 方法签名扩展为三参（新增 `IAuditLogger`），当 `auditLogger instanceof NoOpAuditLogger` 时发出一次性 WARN（文案点明审计事件丢弃风险 + 装配指引）；构造期调用点 + setter 赋值后调用点均传入 `this.auditLogger`
  - `ReActAgentExecutor.Builder.build()`：line 503 null 兜底从 `new NoOpAuditLogger()` 改为 `new Slf4jAuditLogger()`
  - `TestDispatchPathApprovalGate`：注释更新（"engine has no auditLogger setter" → 反映 plan 194 新能力）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（无回归）
- - **Phase 3 — 测试更新与新增 focused 验证 + 文档/roadmap 同步** ✅:
  - 审计既有测试受影响面：仅 `TestDispatchPathApprovalGate:297-298` 注释更新（反映 engine 现有 setAuditLogger 能力）；无断言变更（SLF4j INFO 不影响行为）
  - 新增 `TestAuditLoggerDefault`（5 个 focused 测试，显式覆盖 5 个行为点）：
    - (1) **default-audit-to-SLF4j**：默认 engine 短构造器执行 bash deny → 断言 `Slf4jAuditLogger` logger 的 INFO 输出含 `AUDIT|DENY|...|bash`（ListAppender 捕获）+ 字段类型断言
    - (2) **setter-replaces-logger**：`setAuditLogger(customLogger)` → custom logger 捕获 bash deny 事件 + 默认 Slf4j logger 未收到事件（替换而非组合）
    - (3) **WARN-on-NoOpAuditLogger**：`setAuditLogger(new NoOpAuditLogger())` → setter 触发一次性 WARN（文案含 `NoOpAuditLogger` + discard/guidance）；构造期默认 Slf4j 不命中 NoOp（断言构造期 0 WARN）
    - (4) **no-WARN-on-Slf4j**：默认构造 + `setAuditLogger(new Slf4jAuditLogger())` → 0 个 NoOpAuditLogger WARN
    - (5) **end-to-end-audit-event**（Anti-Hollow）：`new DefaultAgentEngine(...)` → execute → bash denied → CollectingAuditLogger 记录 AuditEvent（DENY + tool=bash + matchedRule=hardcoded_deny_list）+ deny tool-response message 产生
  - roadmap §5b：`AUDIT-13-02` 行 ❌ → ✅，落地 plan 标注 194
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1523 tests, 0 failures, 0 errors（1518 原有 + 5 新增，无回归）

### plan-192: nop-ai-agent A1 Budgeted Injection — system-prompt 自动消费

- **Plan**: `ai-dev/plans/192-nop-ai-agent-budgeted-injection-system-prompt.md`
- **Work Item**: A1（Budgeted Injection functional consumption）
- **Phase 1 — engine 注入逻辑 + 预算配置 + 单元测试** ✅:
  - `DefaultAgentEngine` 新增 `memoryInjectionBudgetTokens` 字段（shipped 默认 1024）+ `setMemoryInjectionBudgetTokens` / `getMemoryInjectionBudgetTokens` setter/getter
  - `buildBaseExecutionContext`（doExecute + resumeSession 共用）新增 `buildBudgetedMemorySection(sessionId)`：当 `memoryStoreProvider != null` 且 `budget > 0` 时 → `getOrCreate(sessionId)` → `store.readBudgeted(budget, context)` → 非空结果格式化为 `## Working Memory` 段落 → 追加到 system prompt 尾部（单条 ChatSystemMessage，base prompt 在前 memory 段落在后）
  - 空 memory / readBudgeted 返回空 / budget ≤ 0 / null provider → 不注入（向后兼容，无 memory 段落、无多余 system 消息）
  - 新增 `TestBudgetedMemoryInjection`（12 个单元测试）：注入内容正确性 + 预算截断（透传 readBudgeted 排序）+ pinned 优先 + 空 memory 不注入 + budget ≤ 0 禁用 + null provider 跳过 + budget 可配 + formatMemorySection 纯函数测试 + shipped 默认值
- - **Phase 2 — 端到端验证 + resume 路径 + 向后兼容 + 设计文档 + roadmap** ✅:
  - 新增 `TestBudgetedMemoryInjectionEndToEnd`（4 个端到端测试）：
    - **write 后自动注入（Anti-Hollow）**：turn 1 execute → write-memory 工具写入 → turn 2 execute（同 sessionId）→ 断言 turn 2 的 system prompt 含 memory content 且未调用 read-memory
    - **resumeSession 路径**：paused session（含 memory）→ resumeSession → 断言 resume 重建的 system prompt 含 budgeted memory
    - **向后兼容**：无 memory session → system prompt = base prompt（无 memory 段落）
    - **多 item budget 截断端到端**：pinned + high + low priority → budget 截断 low item
  - 设计文档更新：`01-architecture-baseline.md` §四 line 75（Working Memory 行 ✅ system-prompt 自动注入）；`nop-ai-agent-session-engine.md` §6.3（新增 system-prompt 自动消费 bullet）；`nop-ai-agent-roadmap.md` §4 A1 行（描述更正 + 状态 ❌→✅）
- - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1518 tests, 0 failures, 0 errors（1514 原有 + 4 新增 e2e，无回归）
- - **Closure 工具**：`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-ai-agent --severity high` exit 0

- - **触发**：用户指出 EXECUTE_ALL_ACTIVE_PLANS 不应该是一个有内循环的子流程 + PLAN_ROUTER 脚本——引擎已支持 forEach，直接用脚本扫描所有活跃 plan → forEach 执行即可。
- - **总体结构变更**：
  - 删除 `flows/execute-all-active-plans.json`（子流程 + PLAN_ROUTER 脚本）
  - 删除 `flow-loader.js` 中的 `planRouter()` 函数和 "plan-router" SCRIPT_REGISTRY 条目
  - 新增 `flow-loader.js` 中 `scanActivePlans()` → 扫描 `ai-dev/plans/` 写入 `flowVars.items` 为 JSON 数组
  - 注册为 `SCRIPT_REGISTRY["scan-active-plans"]`
  - `flows/goal-driver.json` 的 `EXECUTE_ALL_ACTIVE_PLANS` 从 `type: "subflow"` 改为 `type: "group"`（`maxRounds: 1`），包含：
    - `SCAN_PLANS`（`scan-active-plans` 脚本）→ `ok` 时转入 `EXECUTE_EACH_PLAN`
    - `EXECUTE_EACH_PLAN`（`type: "subflow"`, `flow: "plan-execution"`, `forEach: "items"`, `flowArgs: { PLAN_FILE: "{{forEachItem}}" }`）
- - **engine 修复**：`flowArgs` 模板变量在 forEach 循环中 per-iteration resolve（此前在循环外 resolve，`{{forEachItem}}` 无法展开）
- - **测试适配**：
  - `helpers.js` + `intg.test.js`: `mockSubFlows()` 移除 `execute-all-active-plans` mock
  - `goal-driver-verify.test.js`: 移除 execute-all-active-plans 加载测试；`fullDelegates` 新增 `runScript` 默认返回 `empty` 给 SCAN_PLANS；引用 `PLAN_ROUTER` 的测试改为 `runScript` 控制 `SCAN_PLANS`
  - `intg.test.js`: 引用 `PLAN_ROUTER` 的测试改为 `runScript` 控制 SCAN_PLANS
- - **设计文档**：`goal-driver-flow-design.md` 全篇 v3→v4 同步——§三 mermaid 新增 group 内部子图；§四 改为 SCAN_PLANS + forEach 文档；§七/八/九/十 相应更新
- - **验证**：83/83 pass（比 v3 少 1 个测试：execute-all-active-plans 加载测试已移除）

### goal-driver: v3 堆栈嵌套重构（EXECUTE_ALL_ACTIVE_PLANS + plan-execution 重写）

- **触发**：用户指出 v2 的 PLAN_ROUTER goto 跳转不是正确的设计——应该是"堆栈嵌套"风格，即 EXECUTE_ALL_ACTIVE_PLANS 作为一个自包含循环，执行完所有活跃 plan 再返回。
- **总体结构**：
  ```
  HEALTH_CHECK → EXECUTE_ALL_ACTIVE_PLANS → ROADMAP_CHECK
                                                ├── pending → PLAN_DRAFT → PLAN_AUDIT → EXECUTE_ALL_ACTIVE_PLANS
                                                └── complete → DEEP_AUDIT_LOOP → EXECUTE_ALL_ACTIVE_PLANS
  ```
- **新增文件**：`flows/execute-all-active-plans.json`——内部循环：PLAN_ROUTER(脚本) → 找到活跃 plan → EXECUTE_PLAN(子流程) → 回到 PLAN_ROUTER → ... → 无活跃 plan → done
- **plan-execution 重写**：`flows/plan-execution.json` 从 CLOSURE_VERIFY(group) 改为顺序四步骤：
  - EXECUTE → CLOSURE_SCRIPT_CHECK → CLOSURE_AUDIT → BUILD_VERIFY
  - CLOSURE_AUDIT 返回 `issues` → retry EXECUTE（带审计反馈追加）
  - BUILD_VERIFY 返回 `fail` → retry EXECUTE（带构建错误信息）
  - `prompts/closure-audit.md` 标记从 `complete`/`incomplete` 改为 `approved`/`issues`
- **主流程变更**：`flows/goal-driver.json` 移除 PLAN_ROUTER 步骤（职责移到 execute-all-active-plans 子流程）；增加 EXECUTE_ALL_ACTIVE_PLANS 子流程步骤；所有 goto 目标从 PLAN_ROUTER 改为 EXECUTE_ALL_ACTIVE_PLANS
- **plan-router 脚本变更**：`flow-loader.js` 返回值从 `roadmap` 改为 `done`（仅用于 execute-all-active-plans 子流程内部）
- **测试适配**：`test/helpers.js` mockSubFlows 新增 execute-all-active-plans；plan-execution mock 改为四步骤结构。`test/intg.test.js` 和 `test/goal-driver-verify.test.js` 重写所有引用 PLAN_ROUTER 的测试（不再 mockScriptStep 主流程 PLAN_ROUTER，改为通过 responses.PLAN_ROUTER 控制子流程）
- **设计文档**：`goal-driver-flow-design.md` 全篇重写（§三 顶层流程 mermaid、§四 execute-all-active-plans、§五 plan-execution、§六 deep-audit-loop、§八 容错、§九 拒绝列表同步 12 个条目）
- **验证**：84/84 pass（新增 execute-all-active-plans.json 加载测试）

### goal-driver: 测试修复与对齐（ADVERSARIAL/DRAFT_PLANS 补全 + 结构验证容错）

- **触发**：用户要求修复所有测试，让它们通过（之前 83/83 但有多个潜在问题）。
- **修复内容**：
  - `test/helpers.js`：`mockSubFlows` 的 deep-audit-loop mock 补全 `DRAFT_PLANS` 步骤（之前只有 DEEP_AUDIT + ADVERSARIAL，缺少第三个步骤）。
  - `test/intg.test.js`：集成测试 2/3/4（handles execute entry、retries HEALTH_CHECK）补全 `DRAFT_PLANS` mock 响应。结构验证测试新增 `onMaxRetries.done` 检查（HEALTH_CHECK 的唯一 done transition 在 onMaxRetries 中）。
  - `test/goal-driver-verify.test.js`：`fullDelegates` 默认响应新增 `DRAFT_PLANS`。deep-audit-loop 内部循环测试重写为匹配新的 3-step 结构（DEEP_AUDIT → ADVERSARIAL → DRAFT_PLANS），补全 ADVERSARIAL override。
- **验证**：83/83 pass，含 deep-audit-loop 的 ADVERSARIAL + DRAFT_PLANS 路径覆盖。
- **Doc-sync**: No owner-doc update required（goal-driver 是 ai 工具链，非 docs-for-ai 规范对象）

### nop-ai-agent Secure by Default (Plan 193 — in progress)

- Plan: `ai-dev/plans/193-nop-ai-agent-secure-by-default.md` → Phase 1 + 2 + 3 completed
- **目标**：把 `DefaultAgentEngine` 的 Layer 1 默认装配从 AllowAll 收敛为 Default\*，开箱即拒绝危险工具（`bash`/`write-file`/...）与敏感路径（`~/.ssh/`、`/etc/`、`.env`、`id_rsa` 等），并对显式 opt-in AllowAll 的集成商输出一次性 WARN，让安全降级可见
- **Phase 1 实现细节**（纯设计文档变更，无代码改动）：
  - `ai-dev/design/nop-ai-agent/nop-ai-agent-security-and-permissions.md` 新增 §4.6 "默认装配策略（Secure by Default）"：记录 (a) 选定方案（短构造器 + 字段兜底 + Builder null 兜底全部切换为 `Default*`；`AllowAll*` 保留 public 供显式 opt-in；不引入新全局开关配置项）；(b) 拒绝的三个替代方案（仅 WARN 不改默认值 / 全局开关配置项 / 仅改字段不改构造器链）及不采用原因；(c) WARN 语义（构造期、每个 engine 至多一次、AllowAll* 实例触发、WARN 级别、文案要点、当前只覆盖 Layer 1 两个 checker，Layer 2/3 + NoOpAuditLogger 枚举为 successor 扩展点）；(d) 向后兼容处理原则（测试显式传 AllowAll* 并注释"测试 allow 路径"，或改用非 deny-list 工具名，禁止整体关 checker 的回退）
  - 设计文档不含类签名/伪代码，符合 Minimum Rules #14
- **Phase 2 实现细节**（产品代码变更）：
  - `engine/DefaultAgentEngine.java`：
    - 4-arg 构造器（line 118）兜底从 `new AllowAllToolAccessChecker()` 改为 `new DefaultToolAccessChecker()`
    - 5-arg 构造器（line 125）兜底从 `new AllowAllPathAccessChecker()` 改为 `new DefaultPathAccessChecker()`
    - 最长构造器字段兜底（line 165-166）从 `AllowAll*` 改为 `Default*`
    - 新增 `warnIfInsecureDefaults(toolChecker, pathChecker)` 静态方法：当解析后的 checker 为 `AllowAllToolAccessChecker` 或 `AllowAllPathAccessChecker` 实例时，通过 `LOG.warn(...)` 输出一次性 WARN（每个 engine 实例至多一次，在最长构造器末尾调用）。WARN 文案点明"开箱不安全，危险工具/敏感路径无防护"+ 指引如何显式装配 `Default*`。fail-loud：WARN 通过 `LOG.warn` 真实输出，无空方法体
  - `engine/ReActAgentExecutor.java`：`Builder.build()`（line 502-503）的 checker null 兜底从 `AllowAll*` 改为 `Default*`，与 engine 默认保持一致。同步更新 imports（移除未使用的 `AllowAllPathAccessChecker` / `AllowAllToolAccessChecker`，新增 `DefaultToolAccessChecker`）
- **Phase 2 受影响测试处理**（3 个测试文件，6 个失败用例，全部按 Phase 1 裁定原则处理）：
  - `TestPermissionInReActLoop.testDeniedToolProducesErrorResponse`：显式传 `AllowAllToolAccessChecker` 给 builder，注释"test allow path: isolate permission-provider deny behavior"，让 deny 来自 permission provider 而非 Default\* hardcoded deny-list（消息格式不同）
  - `TestChainRepairerInReActLoop`（4 个用例）：路径参数从 `/etc/hosts`（敏感路径前缀）改为 `/tmp/test-read.txt`（非敏感）。该测试验证 chain repairer 行为，路径是任意选取的，改用非 deny-list 路径符合"测试非 checker 行为时改用安全值"原则
  - `TestSubAgentPermissionEndToEnd.backwardCompatibilitySingleAgentNoSpuriousDenials`：显式传 `AllowAllToolAccessChecker` + `AllowAllPathAccessChecker` 给 6-arg 构造器，注释"test allow path: backward-compat baseline isolates parent-constraint enforcement from Layer 1 deny-list"。该测试验证 single-agent 无 parent constraint 时不产生 spurious denial，AllowAll\* 隔离 Layer 1 行为
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（1496 tests / 0 fail，无回归）
- **Phase 3 实现细节**（测试新增 + roadmap 同步）：
  - 新增 `io.nop.ai.agent.engine.TestSecureByDefault`（6 tests），覆盖 5 个 in-scope 行为点 + builder 一致性：
    1. `defaultEngineDeniesDenyListToolEndToEnd`：默认短构造器引擎端到端拒绝 `bash` 工具调用（`new DefaultAgentEngine(chatService, toolManager) → execute → ReAct 循环 → Layer 1 checker deny`，工具未被调用，产生 deny 响应消息）。**端到端验证（Minimum Rules #22）+ 接线验证（#23）**
    2. `defaultEngineDeniesSensitivePathEndToEnd`：默认引擎端到端拒绝敏感路径 `~/.ssh/id_rsa`（`read-file` 工具名不在 deny-list，但 `path` 参数敏感 → `DefaultPathAccessChecker` 拒绝，工具未被调用）
    3. `defaultEngineAllowsSafeToolAndPath`：默认引擎允许安全工具 + 安全路径（`read-file` + `/tmp/test-data.txt`）正常执行，证明默认 checker 不 over-block
    4. `explicitAllowAllTriggersConstructionWarn`：显式传 `AllowAllToolAccessChecker` + `AllowAllPathAccessChecker` 触发构造期 WARN（用 Logback `ListAppender` 断言 WARN 输出，每个 checker 至少 1 条，文案含 `AllowAllToolAccessChecker` / `AllowAllPathAccessChecker` / `insecure` or `not blocked`）。**无静默跳过验证（Minimum Rules #24）**
    5. `defaultCheckersDoNotTriggerWarn`：默认短构造器（Default*）不触发任何 WARN；显式 Default* 也不触发；混合（AllowAll tool + Default path）只触发 1 条 WARN
    6. `builderNullFallbackUsesDefaultCheckers`：`ReActAgentExecutor.builder()` 不传 checker 时，null 兜底为 `Default*`（`bash` 被拒绝端到端），证明 builder 默认与 engine 默认一致
  - WARN 测试技术：用 `ch.qos.logback.classic.Logger` + `ListAppender<ILoggingEvent>` attach 到 `DefaultAgentEngine` logger，`@BeforeEach` attach / `@AfterEach` detach，过滤 `Level.WARN` 事件断言
  - roadmap §5b：`AUDIT-13-01` 行从 `❌ 未修复` 改为 `✅ 已修复`，标注 plan 193
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（1502 tests / 0 fail，新增 6 tests 无回归）
- Doc-sync: 更新了 `ai-dev/design/nop-ai-agent/nop-ai-agent-security-and-permissions.md` §4.6 + `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` AUDIT-13-01 行

### goal-driver: PLAN_DRAFT 简化（移除 none/revised），深度审计出口改造

- **触发**：生产运行日志追踪发现 PLAN_DRAFT 的 `none`/`revised`/`created` 三路分支导致 AI 频繁选错分支，Plans 被跳过。
- **根因**：Plan 192 在 PLAN_ROUTER 已执行之后才创建（12:22），但 PLAN_AUDIT 失败后 PLAN_DRAFT 重试时 AI 输出 `none`（而非 `revised`），导致计划创建后被跳过，未进入执行。
- **改动**：
  - `prompts/plan-draft.md`：移除 Case B（`none`）和 Case C（`revised`）；来源选择移除#4（无工作项时不创计划）；修订工作流改为输出 `created`（无 FLOW_VARS）。
  - `flows/goal-driver.json`：PLAN_DRAFT 仅保留 `created` 转换；DEEP_AUDIT_LOOP `clean` → `PLAN_ROUTER`（替代 `ADVERSARIAL`）。
  - `flows/deep-audit-loop.json`：DRAFT_PLAN 仅保留 `created` 转换。
  - 两个流程文件的 append template 中 `revised` 改为 `created`。
- **容错**：添加 `none→created` 和 `revised→created` 到两个流程的 `markerAliases`，旧版 marker 自动归一化无需修正重试。
- **退出机制**：不再依赖 ADVERSARIAL 步骤；`DEEP_AUDIT_LOOP` `clean` → `PLAN_ROUTER` → `ROADMAP_CHECK` → `complete` → 循环，由 `maxCycleVisits`（30）自然终止。
- **设计文档**：更新 `ai-dev/design/opencode-goal-driver/goal-driver-flow-design.md`，新增容错设计章节、子流程注意事项、退出机制说明。
- **测试**：83/83 通过（修改了 `transitions.test.js`、`intg.test.js`、`goal-driver-verify.test.js` 中所有引用 `none`/`revised` 的测试用例；为 4 个集成测试和 6 个验证测试添加 DEEP_AUDIT_LOOP cycle break override）。

### goal-driver: 跨平台兼容（macOS + Windows 11 Git Bash）

- **触发**：用户要求 goal-driver 同时兼容 macOS 和 Windows 11（Git Bash 执行环境）。
- **问题**：`sys-snapshot.mjs` 和 `reap-orphans.mjs` 全量使用 macOS-only shell 命令（`sysctl`、`vm_stat`、`ps -eo ...`、`zprint`、`memory_pressure`），在 Windows 上完全不可用。Node.js 检测的是 OS 而非 shell，所以即使在 Git Bash 终端里跑，`process.platform` 仍然是 `"win32"`，Unix 命令无法通过 `cmd.exe` 执行。
- **改动**：
  - **新增 `src/platform.mjs`**：跨平台抽象层，提供 `getAllProcesses()`（Unix 用 `ps`，Windows 用 PowerShell `Get-CimInstance Win32_Process`）、`killPid()`（Unix 用 `process.kill`，Windows 用 `taskkill /T /F`）、`killProcessTree()`、`isAlive()`、`getDescendants()`、`safeExec()`、`powershell()` 等。所有进程对象统一为 `{pid, ppid, pgid, rss_kb, name, cmd}` 格式（Windows pgid=0）。
  - **重写 `src/sys-snapshot.mjs`**：`loadAvg` 改用 `os.loadavg()`（跨平台）；内存统计在 Windows 用 `os.totalmem()/freemem()`，macOS 保留 `vm_stat`；进程列表/cohort/totalRSS 全部改用 `platform.mjs.getAllProcesses()` 单次获取后计算（原来 10+ 次 ps 调用优化为 1 次）；`getDiskFree` 改用 `fs.statfsSync()`（跨平台）；macOS-only 的 `zprint`/`memory_pressure` 加 `IS_MACOS` 守卫，非 macOS 跳过。新增 `platform` 字段记录 `process.platform`。
  - **重写 `src/reap-orphans.mjs`**：`reapProcessGroup` 在 Windows 走 PPID 树遍历（`_getDescendants`）而非 Unix process group kill；`reapStartupOrphans` 孤儿进程检测改为 `ppid===1 || !pidsSet.has(ppid)`（兼容 Unix ppid=1 和 Windows 进程 reparent）；全部 kill 操作通过 `platform.mjs.killPid()` 分发。
  - **更新 `src/executor.js`**：`IS_WIN32` 和 `killTree` 改用 `platform.mjs` 导出；spawn 新增 `opts.shell` 支持（Windows `.cmd` 文件需要 `shell:true`）。
  - **更新 `src/runner.js`**：`IS_WIN32` 和 `killTree` 改用 `platform.mjs`；`runTool` 新增 Windows 命令解析（`./mvnw` → `mvnw.cmd`，`shell:true`）；`killTree` 的 Unix 路径用 `platform.mjs.isAlive()` 替代裸 `process.kill(pid, 0)`。
- **验证**：`node --test ai-dev/tools/opencode-goal-driver/test/*.test.js` → 83 tests / 0 fail。macOS smoke test：`platform.mjs`（696 进程）、`sys-snapshot`（全指标正常）、`reap-orphans`（正常）全部通过。
- **Doc-sync**: No owner-doc update required（goal-driver 是 ai 工具链，非 docs-for-ai 规范对象）

### goal-driver: 系统死机根因分析 + 诊断工具部署 + 孤儿进程回收器

- **触发**：系统 11:43 死机重启（连续第三天），用户要求分析 goal-driver 是否资源泄漏。
- **分析报告**：`ai-dev/analysis/2026-06-15-goal-driver-resource-leak-crash-analysis.md`
- **机器**：Mac Studio M4 Max, 128 GB 统一内存, 16 核 (12P+4E)。
- **根因排查**：
  - **确证**：`/Library/Logs/DiagnosticReports/panic-full-2026-06-15-114525.0002.panic` — kernel panic 非 hard poweroff。Panic string: `zalloc[3]: zone map exhausted while allocating from zone [data.kalloc.1024], likely due to memory leak (20G, 21188208 elements)`。Panicked task: `pid 22214: opencode.exe`。
  - 三次崩溃（06-13/14/15）panic root cause 完全相同，均为 opencode 导致的内核 kalloc.1024 zone 泄漏（~20 GB / 2100 万个分配）。
  - 排除 CPU 过载假设（`-T 1C` 实际并行度受 `-pl` 限制仅 3-5 模块，BUILD_VERIFY 仅 3 分钟）。
- **新增工具**：
  - `ai-dev/tools/opencode-goal-driver/src/sys-snapshot.mjs` — 系统资源快照（load/mem/process cohorts/top-10/disk）。集成 3 处：flow 启动、每 step 前、5 min 心跳。
  - `ai-dev/tools/opencode-goal-driver/src/reap-orphans.mjs` — 孤儿进程回收器。**两层回收**：① executor 子进程退出后 `reapProcessGroup(childPid)` 按 PGID 精确清理同组残留；② flow 启动时 `reapStartupOrphans` 按命令行签名 `[GOAL_DRIVER]` 识别并 kill 上次崩溃遗留的全部进程（opencode + MCP server + Maven JVM）。runner.js 所有 prompt 加 `[GOAL_DRIVER]` 前导标记，在 `ps` 命令行中可见，100% 精确识别。
- **改动文件**：`engine.js`（import + `_sysMon()` + `_warnOrphans()` + snapshot 集成）、`executor.js`（heartbeat snapshot + close 后 `reapProcessGroup`）、`sys-snapshot.mjs`（export fix）、`reap-orphans.mjs`（PGID 精确追踪重写）
- **验证**：`node --test ai-dev/tools/opencode-goal-driver/test/*.test.js` → 83 tests / 0 fail
- **Doc-sync**: No owner-doc update required（goal-driver 是 ai 工具链，非 docs-for-ai 规范对象）

### nop-ai-agent agentName Path Injection Phase 2 — Regression & Boundary Tests (Plan 191 — completed)

- Plan: `ai-dev/plans/191-nop-ai-agent-agentname-path-traversal-fix.md` → Phase 2 completed, Plan Status: completed
- **目标**：为 Phase 1 落地的 `AgentNames` allow-list guard（chokepoint + defense-in-depth 两个 surface）补齐回归与边界测试，证明 traversal-shaped agentName / agentId 在公开 API 边界和 `CallAgentExecutor` 边界均被 fail-closed 拒绝，且合法名称不被 over-block
- **Phase 2 实现细节**（纯测试新增，无产品代码变更）：
  - 新增 `io.nop.ai.agent.engine.TestAgentNameValidation`（19 tests）：
    - `AgentNames.requireValidIdentifier` 单元测试：4 个 valid case（`test-agent` / `my_agent` / `Agent123` / full-charset）pass；11 个 rejection vector（null / empty / `../` / 绝对路径 / `..\` / NUL / 空格 / `.` / `..` / `a.b` / Unicode）均抛 `NopAiAgentException`——镜像 plan-190 `TestSessionIdValidation` 的结构
    - `AgentNames.isValidIdentifier` 谓词形式测试（`CallAgentExecutor` 使用）：valid → true，全部 rejection vector → false
    - **端到端验证（Minimum Rule #22）**：`DefaultAgentEngine.execute` 带 traversal agentName `"../../etc/passwd"` 同步抛 `NopAiAgentException`（loadAgentModel 在 `CompletableFuture.supplyAsync` 构造之前执行，故异常从 `execute()` 直接抛出，非 future）。`ThrowingChatService`（任何调用即抛 AssertionError）的 `callCount` 断言为 0，证明 executor 从未分派、VFS 从未加载 traversal 路径
    - **接线验证（Minimum Rule #23）**：异常消息包含 `agentName` + (`path-injection guard` | `invalid characters` | `[A-Za-z0-9_-]`)，证明 `AgentNames.requireValidIdentifier` 被 `loadAgentModel` 调用（wiring 可从异常观测）
    - **no over-blocking**：`execute` 带合法 `test-agent` 正常完成（status=completed），证明 guard 不阻塞合法名称
  - `TestCallAgentExecutor` 新增 3 tests（a/b/c 三组）：
    - (a) `traversalAgentIdReturnsErrorResultWithoutInvokingEngine`：traversal agentId `"../../../etc/passwd"` → `AiToolCallResult` error result（不抛异常、不调用 `engine.execute`——recording mock 的 `executeCount` 断言为 0）；error 消息含 agentId guard marker
    - (b) `selfBranchUnaffectedByAgentIdAllowList`：`agentId="self"` → resolve 到 parent agentName `"parent-agent"`，正常调用 `engine.execute`（`executeCount`=1），证明 guard 只在 non-`"self"` branch 生效
    - (c) `validAgentIdNotOverBlockedProceedsToEngine`：合法 agentId `"child-agent"` → 正常调用 `engine.execute`（`executeCount`=1），证明 guard 不 over-block 合法名称
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（新增 22 tests + 现有全部 tests 通过，无回归，确认 no over-blocking）
- Roadmap：`nop-ai-agent-roadmap.md` row AUDIT-13-16 从 `❌ 未修复` 改为 `✅ 已修复`，标注 plan 191 为 landing plan
- 关键决策：(1) `executeAcceptsValidAgentNameAfterGuard` 使用 `test-agent`（react mode，默认无 tools，单次 LLM 响应即完成）而非 `test-single-turn-agent`（后者 schema enum 校验 `mode="single-turn"` 不在合法选项内，加载失败）；(2) `CallAgentExecutor` 测试用 anonymous `IAgentEngine` recording mock（接口含两个 abstract method，非 functional interface，不能用 lambda），精确断言 `engine.execute` 是否被调用及 agentName 参数；(3) 端到端测试用 `ThrowingChatService` 作为 sentinel——若 loadAgentModel 未在 VFS 加载前拦截，chat service 会被调用并抛 AssertionError，使"未拦截"路径立即暴露

### nop-ai-agent agentName Path Injection Phase 1 — Chokepoint + Defense-in-Depth Guards (Plan 191 — in progress)

- Plan: `ai-dev/plans/191-nop-ai-agent-agentname-path-traversal-fix.md` → Phase 1 completed, Phase 2 planned
- **目标**：关闭 P2 路径注入 finding [13-16]——caller-controlled `agentName`（来自 public API `AgentMessageRequest.agentName`，间接来自 LLM `call-agent` tool args）未校验流入 `DefaultAgentEngine.loadAgentModel` 的 `"/" + agentName + ".agent.xml"` 拼接，再进 `ResourceComponentManager.loadComponentModel(path)`，使 `../../etc/passwd` 形 agentName 可驱动 VFS 加载任意资源。本 Phase 在 chokepoint 和 defense-in-depth 两个 surface 落地 fail-closed 校验
- **Phase 1 实现细节**：
  - 新增 `io.nop.ai.agent.engine.AgentNames`（`SessionIds` 的 sibling，同一 attack class 不同 surface）：`requireValidIdentifier(String)` 抛 `NopAiAgentException`（fail-closed，no sanitization/truncation/fall-back）+ `isValidIdentifier(String)` 谓词形式（供 fail-with-error-result 契约的 `CallAgentExecutor` 使用）。两者均执行 strict allow-list `^[A-Za-z0-9_-]+$`
  - `DefaultAgentEngine.loadAgentModel`（`:1206`）：在 `"/" + agentName + ".agent.xml"` 拼接**之前**调用 `AgentNames.requireValidIdentifier(agentName)`。单点 guard 覆盖全部三个 caller（`doExecute:627`、`resumeSession:763`、`restoreSession:881`）+ 间接 `CallAgentExecutor → engine.execute` 路径
  - `CallAgentExecutor`（`tool/CallAgentExecutor.java`）：non-`"self"` branch（`:108-110`）在已有 null/empty check 之后、赋值 `targetAgentId` 之前，调用 `AgentNames.isValidIdentifier(agentId)`，失败返回描述性 `AiToolCallResult.errorResult`（经现有 `fail(...)` helper）。`"self"` branch 不变（resolve 到 parent 已校验过的 agentName）
  - 两处 rejection 均硬失败：`loadAgentModel` throws；`CallAgentExecutor` 返回 error result（其 documented contract 是 fail-with-error-result，never throw uncaught）
  - javadoc 更新：`loadAgentModel` 文档新增 fail-closed path-injection guard 段；`CallAgentExecutor` class javadoc 新增 defense-in-depth guard 段（注明 agentId 已 allow-list 校验）
- 验证：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS
- 关键决策：(1) 引入 sibling `AgentNames` 而非复用 `SessionIds.requireValidIdentifier`（后者语义 keyed to sessionId，方法参数名 + javadoc 均指 sessionId；centralization 是 plan 190 non-blocking follow-up，本 plan 不强制）；(2) `CallAgentExecutor` 用谓词形式 `isValidIdentifier` 而非 catch exception，保持其 fail-with-error-result 契约清晰

- Plan: `ai-dev/plans/189-nop-ai-agent-working-memory-tools.md` → all Phases completed, Plan Status: completed
- **Phase 3 实现细节**（纯文档变更，无代码行为变化）：
  - `nop-ai-agent-roadmap.md` §4 Layer 2 row L2-15（`:181`）：状态从 `❌` 改为 `✅`
  - `nop-ai-agent-roadmap.md` §2.1 row M6（`:31`）：`IAiMemoryStore` 从"仅 4 方法"更新为"全 8 方法 + InMemoryAiMemoryStore 功能化实现"；`AiMemoryItem` 字段状态维持 ✅（L1-16）；状态从 `⚠️ 需扩展` 改为 `✅ 已完成`
  - `nop-ai-agent-roadmap.md` §2.2 row "Working Memory (工具)"（`:55`）：状态从 `❌ 未开始` 改为 `✅ 已落地（plan 189）`
  - 跨 Phase 一致性核对：`01-architecture-baseline.md` §四 + `nop-ai-agent-session-engine.md` §6.3 已在 Phase 1/2 中更新到 live baseline
- 验证：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS

### nop-ai-agent Working Memory 工具 Phase 2 — Tool Executors + Bean Registration + End-to-End (Plan 189 — in progress)

- Plan: `ai-dev/plans/189-nop-ai-agent-working-memory-tools.md` → Phase 2 completed, Phase 3 planned
- **目标**：交付三个 Working Memory 工具执行器（`ReadMemoryExecutor` / `WriteMemoryExecutor` / `SearchMemoryExecutor`）+ bean 注册 + `.tool.xml` schema 文件 + 端到端验证
- **Phase 2 实现细节**：
  - `io.nop.ai.agent.tool.AbstractMemoryToolExecutor`（抽象基类）：共享 `resolveStore`（store 解析 + null-fail-fast）+ `resolveArguments`（JSON 优先 + attr fallback，同 CallAgentExecutor/SendMessageExecutor）+ `getStringArg`/`getIntArg`/`getBooleanArg` + `fail` helper。`StoreResolution` 内部类承载 ok/failure 二态结果
  - `io.nop.ai.agent.tool.ReadMemoryExecutor`（`TOOL_NAME = "read-memory"`）：4 种 action——`list`（默认，`getAll(filters)` + 可选 type 过滤）/ `last`（`getLastN(n)`）/ `budgeted`（`readBudgeted(maxTokens, context)`）/ `key`（按 key 查找单条）。返回格式化文本（人类可读列表 + key/type/priority/pinned 标记 + content）
  - `io.nop.ai.agent.tool.WriteMemoryExecutor`（`TOOL_NAME = "write-memory"`）：4 种 action——`add`（默认，`add(item)` + 可选 key/type/priority/pinned）/ `update`（`update(key, item)`）/ `remove`（`remove(key)`）/ `clear`（全部删除）。返回确认摘要（操作 + 剩余总数）
  - `io.nop.ai.agent.tool.SearchMemoryExecutor`（`TOOL_NAME = "search-memory"`）：必需参数 `query`，调用 `store.search(query)` 返回匹配列表。无匹配时返回空成功结果（合法状态）
  - bean 注册：`ai-agent-tools.beans.xml` 新增 3 个 bean 元素（`read-memory` / `write-memory` / `search-memory`），被 toolkit 的 `<ioc:collect-beans by-type="...IToolExecutor"/>` 自动收集
  - 工具 schema：新增 `nop-ai-toolkit/src/main/resources/_vfs/nop/ai/tools/read-memory.tool.xml` / `write-memory.tool.xml` / `search-memory.tool.xml`（遵循现有 call-agent/send-message 的 `tool.xdef` 结构：toolName + description + inputSchema + examples）。这是工具被 `ToolManagerImpl.loadTool()` 加载、并在 `ReActAgentExecutor.buildToolDefinitions()` 中被发现的必要条件
- **Phase 2 测试**（新增）：
  - `TestReadMemoryExecutor`（13 tests）：list/last/budgeted/key 四种 action + store null 时错误结果 + type 过滤 + 空 store 时空列表（合法）+ 未知 action fail-fast + 缺少必需参数 fail-fast + 默认 action=list + non-agent context fail-fast
  - `TestWriteMemoryExecutor`（12 tests）：add/update/remove/clear 四种 action + store null 时错误结果 + 操作后 store 状态正确（findByKey 验证）+ 缺少 content/key 时 fail-fast + 默认 action=add + non-agent context fail-fast
  - `TestSearchMemoryExecutor`（7 tests）：有匹配/无匹配（合法空成功）/匹配 type/key + 缺少 query 时 fail-fast + 空 query 时 fail-fast + store null 时错误结果 + non-agent context fail-fast
  - `TestWorkingMemoryEndToEnd`（2 tests）：
    - **端到端验证（Anti-Hollow Rule #22）**：Agent 在 ReAct 循环中先调用 `write-memory`（写入一条记忆），再调用 `read-memory`（读取验证），断言读回的内容与写入一致——证明从 LLM tool call → `IToolManager.callTool` → 工具执行器 → `AgentToolExecuteContext.getMemoryStore()` → `IAiMemoryStore` 的完整路径连通
    - **接线验证（Rule #23）**：端到端测试中断言 `AgentToolExecuteContext.getMemoryStore()` 非 null（证明分发循环确实注入了 store），且两次工具调用都观察到非 null store（证明 per-session 解析正确）。同时验证 per-session 隔离（不同 sessionId 返回不同 store 实例）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（全部新增 34 tests + 现有 ~600 tests 通过，无回归）
- 设计文档：`01-architecture-baseline.md` §四 Memory 模型已更新——Working Memory 行从"未开始"改为"✅ 工具读写 / ⏳ system-prompt 自动注入 deferred 至 A1"；原 line 78 "Working Memory 是 Session 状态的一部分（持久化）"的契约断言已更新为反映当前 baseline（"per-session in-memory，持久化 deferred 至 L4-3 IMemoryAdapter"）；system-prompt 自动注入断言显式标注 deferred（A1 successor scope）；§四 IAgentMemory 描述同步
- 关键决策：(1) 三个工具均遵循 CallAgentExecutor/SendMessageExecutor 的参数解析模式（JSON 优先 + attr fallback + 异常不泄露）；(2) honest no-config：store 为 null 时返回描述性错误结果（`AiToolCallResult.errorResult` 带明确消息），不返回空成功/空列表假装正常；(3) SearchMemoryExecutor 无匹配时返回空成功结果（合法语义非静默跳过）；(4) 提取公共基类 `AbstractMemoryToolExecutor` 复用 store 解析 + 参数解析逻辑

### nop-ai-agent Working Memory 工具 Phase 1 — Backing Infrastructure (Plan 189 — in progress)

- Plan: `ai-dev/plans/189-nop-ai-agent-working-memory-tools.md` → Phase 1 completed, Phase 2/3 planned
- **目标**：交付 Working Memory 工具的底层设施——`InMemoryAiMemoryStore`（全 8 方法）+ `IMemoryStoreProvider`/`InMemoryMemoryStoreProvider`（per-session 隔离）+ `AgentToolExecuteContext` memory store 字段 + `ReActAgentExecutor` Builder 字段 + 分发循环接线 + `DefaultAgentEngine` pass-through。Phase 2 将交付三个工具执行器 + .tool.xml schema + bean 注册 + 端到端测试；Phase 3 是 roadmap 文档同步
- **Phase 1 实现细节**：
  - `io.nop.ai.agent.memory.InMemoryAiMemoryStore`：`ConcurrentHashMap<key, AiMemoryItem>` + `synchronized` 复合操作。覆盖全部 8 方法（含 `readBudgeted`/`update`/`remove`/`batchAdd` 的功能化实现，无 UOE 残留）。`readBudgeted` 实现 Budgeted Injection 算法（pinned 始终包含 + priority 降序 + tokenEstimate 累计至 maxTokens）；`search` 为 content/type/key 子串匹配（in-memory 无向量检索）；`add` 自动填充 createTime + 经 `AiMemoryItem.getTokenEstimate()` lazy 计算
  - `io.nop.ai.agent.memory.IMemoryStoreProvider`（接口，`IAiMemoryStore getOrCreate(String sessionId)`）+ `InMemoryMemoryStoreProvider`（`ConcurrentHashMap.computeIfAbsent` 原子创建）。per-session 隔离自然实现（不同 session 独立 store）
  - `AgentToolExecuteContext`：新增 `IAiMemoryStore memoryStore` 字段 + 14-arg 构造器（legacy 13-arg delegate 时 store = null，向后兼容）+ `getMemoryStore()` getter
  - `ReActAgentExecutor`：Builder 新增 `IMemoryStoreProvider memoryStoreProvider` 字段 + `memoryStoreProvider(...)` setter + `build()` 传入。分发循环（`ReActAgentExecutor.java:763` 附近）构造 `AgentToolExecuteContext` 时经 `provider.getOrCreate(sessionId)` 解析注入；provider 为 null 时 store = null（工具侧 fail-fast）
  - `DefaultAgentEngine`：默认持有 `InMemoryMemoryStoreProvider`（开箱即用，同 `InMemorySessionStore` 一致）+ `setMemoryStoreProvider`/`getMemoryStoreProvider` + `resolveExecutor` 将 provider 传入 executor Builder
- **Phase 1 测试**（新增）：
  - `TestInMemoryAiMemoryStore`（16 tests）：add→get 往返、getAll type 过滤、getLastN 排序、search 子串匹配（content/type/key + case-insensitive）、readBudgeted pinned + priority 降序 + token budget 截断、update 覆盖、remove 删除、batchAdd、clear、concurrent add（8 线程 × 50 = 400 items 全部保留）、无 UOE 残留
  - `TestInMemoryMemoryStoreProvider`（6 tests）：same sessionId 同实例、不同 sessionId 不同实例、per-session 数据隔离、concurrent getOrCreate 同实例（8 线程 0 mismatch）、null/empty sessionId fail-fast
  - `TestMemoryStoreProviderWiring`（5 tests）：13-arg legacy 构造器 store=null 向后兼容、14-arg 构造器携带 store、provider per-session 隔离、engine 默认 InMemoryMemoryStoreProvider、setMemoryStoreProvider override
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C -Dtest=TestInMemoryAiMemoryStore,TestInMemoryMemoryStoreProvider,TestMemoryStoreProviderWiring,TestIAiMemoryStoreDefaultMethods,TestAiMemoryItem -Dsurefire.failIfNoSpecifiedTests=false` → BUILD SUCCESS（27 new tests + 现有测试通过）
- 关键决策：(1) Provider 模式不修改 `IAiMemoryStore` 接口签名（保持 L1-16 契约稳定），per-session 隔离经 Provider 解析；(2) shipped 默认 = `InMemoryMemoryStoreProvider`（功能化），不是 NoOp——同 `InMemorySessionStore` 一致，工具开箱即用；(3) null-fallback：provider=null 时 context 中 store=null，工具侧 fail-fast（同 SendMessageExecutor honest no-config 模式）；(4) `InMemoryAiMemoryStore` 数据**不**随 `AgentSession` 持久化（持久化 deferred 至 L4-3 IMemoryAdapter）
- 设计文档：`nop-ai-agent-session-engine.md` §6.3 已更新（IAiMemoryStore in-memory 实现标记已落地；Provider 模式记录；持久化标注 L4-3 deferred）

### goal-driver: 审计目录命名改为带时分（YYYY-MM-DD-HHMM）

- **背景**：goal driver 一天内常多次执行审计，审计目录原以 `YYYY-MM-DD` 为前缀，同日多次执行只能靠 `-r2`/`-r3`/字母后缀区分，易混乱。
- **方案**：用引擎变量替换，把日期前缀统一改为 `YYYY-MM-DD-HHMM`（年-月-日-时分），格式由引擎一次性算好，AI 不再自己拼日期。
- **改动**：
  - `ai-dev/tools/opencode-goal-driver/src/config.js`：新增 `timestamp`（`YYYY-MM-DD-HHMM`，与 runDir 的 `ts` 同一 `now` 算出），暴露到 config
  - `ai-dev/tools/opencode-goal-driver/src/main.js`：`vars` 增加 `TIMESTAMP: config.timestamp`
  - `prompts/deep-audit.md` + `prompts/adversarial-review.md`：`{DATE}` → `{{TIMESTAMP}}`（由引擎 `_templateVar` 替换，与 `{{module}}` 同机制）
  - 同步 skill 模板的日期格式（AI 在两条流程里都会读）：`skills/deep-audit-prompts.md`、`skills/open-ended-adversarial-review-prompt.md`、`skills/codex-goal-driven-development-prompt.md`、`skills/age-document-audit-prompt.md`、`audits/README.md` 全部改为 `YYYY-MM-DD-HHMM`，并注明历史记录可能只有 `YYYY-MM-DD`
- **验证**：`node --test ai-dev/tools/opencode-goal-driver/test/*.test.js` → 83 tests / 0 fail；doc-link checker 无新增断链（12 个 pre-existing 断链与本改动无关）
- **范围说明**：plans 仍用 `{NNN}-{slug}.md` 编号前缀（非日期），本次未改

- **根因**：PLAN_DRAFT 只有 `created` 和 `none` 两种 result marker。当 PLAN_AUDIT 返回 `issues` 并通过 `retry: PLAN_DRAFT` + append review feedback 要求 agent 修订计划时，agent 正确地原地编辑了计划文件，但按 prompt 定义只能输出 `none`（因为没有新建文件）。引擎把 `none` 解释为"放弃"→ `goto: DEEP_AUDIT_LOOP`，导致：(1) 计划被静默遗弃在 `planned` 状态；(2) PLAN_AUDIT 的 `onMaxRetries`（设计为审计不过就 degraded-execute）变成死代码，因为 retry 计数器永远到不了 overflow。
- **实际后果**：plan 189 的审计反馈被正确修复（Major line 186 Classification + 2 Minors），但因 marker 错误导致计划未被重新审计就被跳过，引擎转而执行 DEEP_AUDIT → 发现 P0 → 创建并执行 plan 190。
- **修复**（4 文件）：
  - `engine.js` `_formatAppend`：追加文本现在解析 `{{flowVar}}` 模板（之前只解析 `${output}`/`${logFile}`），使 append template 可引用 `{{PLAN_FILE}}`
  - `prompts/plan-draft.md`：新增 Case C — `revised` marker，明确指导 agent 在收到 REVISION REQUEST 时原地编辑已有计划并输出 `revised`
  - `flows/goal-driver.json` + `flows/deep-audit-loop.json`：PLAN_DRAFT/DRAFT_PLAN 新增 `revised → PLAN_AUDIT/AUDIT_PLAN` 转移；PLAN_AUDIT/AUDIT_PLAN 的 issues append template 改为显式 REVISION REQUEST 格式（含 `{{PLAN_FILE}}` 路径 + 明确指示原地修订）；onMaxRetries 增加 degraded-mode 警告 append
- **验证**：新增回归测试 `revised marker routes back to PLAN_AUDIT instead of escaping (regression for plan 189 bug)`；全套 83 tests 通过

### nop-ai-agent Session-Id Path Traversal Fix（Plan 190 — P0 finding [13-15]，全部 2 Phases completed）

- Plan: `ai-dev/plans/190-nop-ai-agent-session-id-path-traversal-fix.md` → Plan Status: completed
- 修复 P0 路径穿越漏洞 [13-15]：caller-controlled `sessionId`（来自 `AgentMessageRequest.sessionId`）未经验证流入 `FileBackedSessionStore` / `FileBackedCheckpointManager` 的 `Path.resolve(sessionId)`，可被 `../../../etc/cron.d/exploit` 形态用于任意文件写/删。修复后为 fail-closed：任何非 `[A-Za-z0-9_-]` 的 sessionId 在到达文件系统前被拒绝
- **Phase 1**（共享校验 helper + 4 处 resolve site 加固）：
  - 新增 `SessionIds.java`（`io.nop.ai.agent.engine` 包，与 `NopAiAgentException` 同包，复用 session/reliability 已有的 engine 依赖方向，无新循环依赖）：两层校验——`requireValidIdentifier(String)`（非空 + `^[A-Za-z0-9_-]+$`）+ `requireContainedPath(String, Path)`（identifier 检查 + `rootDirectory.resolve(id).normalize()` 必须 `startsWith(rootDirectory.normalize())`）。两层均 fail-closed 抛 `NopAiAgentException`
  - `DefaultAgentEngine.resolveSessionId`（`:1173-1178`）接入 identifier 层——覆盖 `execute`/`sendMessage`。UUID fallback 由构造保证安全
  - `FileBackedSessionStore`：新增 `sessionDirPath(sessionId)` 私有 helper（调 `SessionIds.requireContainedPath`），`sessionFilePath` 和 `remove` 的 `rootDirectory.resolve(sessionId)` 全部改走该 helper。覆盖 save/get/loadSessionFile/remove（也守卫 resumeSession/restoreSession/cancelSession 经 `sessionStore.get` 到达的 raw sessionId）
  - `FileBackedCheckpointManager`：`sessionDirPath` 改为调 `SessionIds.requireContainedPath`；`loadSessionFromDisk` 的 `rootDirectory.resolve(sessionId)` 改走 `sessionDirPath`。覆盖 saveCheckpoint/getLatestCheckpoint/getCheckpoints/flushSnapshot 全路径
  - 两个 store 的 class javadoc 增加 fail-closed sessionId 校验说明
  - grep 确认：4 处原始 `rootDirectory.resolve(sessionId)` 全部消除，仅 `SessionIds.requireContainedPath` 内部 1 处（受控 chokepoint）
- **Phase 2**（回归 + 边界测试）：
  - 新增 `TestSessionIdValidation`（15 tests）：valid UUID/allow-list 通过；null/empty/`../`/绝对路径/backslash/NUL/whitespace/`.`/`..`/Unicode 各自 fail-closed；containment 校验返回 normalized dir + 拒绝 null root + 拒绝 traversal id
  - `TestFileBackedSessionStore`（+5 tests）：save/get/remove traversal id 全部抛异常 + temp-dir probe 证明无 out-of-root 文件产生（remove 测试 planted 一个 outside 文件并断言未被删）+ 绝对路径/backslash 拒绝
  - `TestFileBackedCheckpointManager`（+5 tests）：saveCheckpoint/getLatestCheckpoint/getCheckpoints/flushSnapshot traversal id 全部抛异常 + temp-dir probe + 绝对路径拒绝
  - `TestFileBackedCheckpointDispatchPath`（+2 e2e tests）：`engine.execute(request)` 带 traversal sessionId 在 `resolveSessionId` 同步抛 `NopAiAgentException`（store 从未被触达，session root 下无文件）；valid sessionId 仍正常 execute（守卫不过度拦截）。Wiring 验证：异常消息含 "path-traversal guard" 标识
- 验证：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（全部新增 27 tests + 现有测试通过，无回归）
- 关键决策：(1) helper 放 `engine` 包（同 `NopAiAgentException`），复用现有依赖方向；(2) 两层校验分离——engine 层只做 identifier（无 rootDirectory 在作用域），store/checkpoint 层做完整 identifier+containment（也守卫 bypass resolveSessionId 的 resume/restore/cancel 路径）；(3) `requireContainedPath` 返回 normalized contained path，合法 id（无 `.`/`..`）行为不变
- roadmap：`nop-ai-agent-roadmap.md` AUDIT-13-15 ❌→✅

- Plan: `ai-dev/plans/188-nop-ai-agent-compaction-aware-checkpoint-restore.md` → Plan Status: completed
- 交付 **compaction-aware 截断加载**——`FileBackedCheckpointManager.loadSessionFromDisk` 和 `DBCheckpointManager.loadSessionFromDb` 在加载全部 checkpoint 后，截断 per-session `bySession` 列表从最近 `CheckpointType.COMPACTION` checkpoint 开始（inclusive）。`byWatermark`（file-backed）/ DB direct-query fallback（DB-backed）保留全量映射，审计/调试能力不受影响
- **Phase 1**（truncation 实现 + 单元测试）：
  - 新增 `CompactionAwareTruncation.java`（`io.nop.ai.agent.reliability` 包）：共享静态工具类 `truncateToLatestCompaction(List<Checkpoint>)`——扫描 ascending-seq 列表，找最后一个 `type == COMPACTION` 的索引，保留该索引及之后子列表（inclusive）。无 COMPACTION 时返回全量副本（向后兼容）
  - `FileBackedCheckpointManager.loadSessionFromDisk`：加载全部 journal entries 后，`byWatermark` 仍放全量映射（pre + post compaction），`bySession` 放截断后列表（`CompactionAwareTruncation.truncateToLatestCompaction`），`saveCounters` 设为截断后列表大小。journal.md 文件不修改（审计历史完整保留）
  - `DBCheckpointManager.loadSessionFromDb`：加载全部 DB 行、反转为 ascending 后，`byWatermark` 经 `putIfAbsent` 已填充全量（保留），`bySession` 放截断后列表。`ai_agent_checkpoint` 表行不删除，`loadCheckpointFromDb` fallback 仍可解析 pre-compaction watermark
  - 单元测试（FileBacked — 5 tests）：无 compaction 不截断 / 单次 compaction 截断 inclusive / 多次 compaction 截断到最近 / COMPACTION 为最后 checkpoint / 全部 loaded checkpoint 满足 `messageCount ≤ session.messageCount` 不变量
  - 单元测试（DB-backed — 3 tests）：无 compaction 不截断 / 单次 compaction 截断 + getCheckpoint DB fallback / 全部 loaded checkpoint 满足不变量
  - 向后兼容测试（1 test，`TestCompactionAwareCheckpointTruncation.java`）：executor 注入 `NoOpContextCompactor`（默认）+ `FileBackedCheckpointManager` → 多 turn execute → 无 COMPACTION checkpoint 发射 → reload 后 `getCheckpoints` 返回全量列表（无截断）
- **Phase 2**（端到端 restore 验证 + 设计文档 + roadmap）：
  - 端到端测试（file-backed — 1 test）：executor 注入 `messageRemovingCompactor`（移除旧消息的真实压缩器）+ `FileBackedCheckpointManager` + `FileBackedSessionStore` → 预加载 20 turn 消息触发压缩 → COMPACTION checkpoint 发射 + session 持久化含 post-compaction 消息 → crash（丢弃实例）→ 新 `FileBackedCheckpointManager` + `FileBackedSessionStore`（同根目录）→ `getCheckpoints` 仅返回 post-compaction checkpoint（截断生效）→ 全部 loaded checkpoint 满足 `messageCount ≤ session.messageCount` 不变量 → `getCheckpoint(pre-compaction-watermark)` 仍解析 → `engine.restoreSession` 续跑成功
  - 端到端测试（DB-backed — 1 test）：同上但使用 `DBCheckpointManager` + `DBSessionStore` → 截断生效 → `getCheckpoint(pre-compaction-watermark)` 经 DB fallback 解析
  - 端到端测试（向后兼容 — 1 test）：`NoOpContextCompactor` + 多 turn execute → crash → restore → `getCheckpoints` 返回全量列表（无截断）→ restore 成功
  - 端到端测试（多次 compaction — 1 test）：触发 2+ 次压缩 → crash → restore → `getCheckpoints` 截断到最近 COMPACTION boundary
  - `nop-ai-agent-reliability.md` §5.4a：recovery flow step 3 补充 `firstKeptEntryId` 由 COMPACTION checkpoint 位置实现的说明；架构决策中两处 "firstKeptEntryId compaction-aware 截断加载" 从"独立 successor"更新为"已落地（plan 188）"；新增 compaction-aware 截断加载的完整架构决策记录（截断点派生 / bySession 截断 + byWatermark 保留 / COMPACTION inclusive / 向后兼容 / load-time only）
  - `nop-ai-agent-roadmap.md` A4 行：补充 "compaction-aware 截断加载 = plan 188 已落地，`firstKeptEntryId` 由 COMPACTION checkpoint 位置实现"
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（1386 tests，全部新增 + 现有测试通过，无回归）
- 关键决策：(1) 截断点从已有 `COMPACTION` checkpoint type 派生（不新增 `firstKeptEntryId` 字段）；(2) `bySession` = compaction-aware active set（restore 用），`byWatermark` = full history index（audit 用）——职责分离；(3) COMPACTION inclusive（它是 post-compaction 基线，合法恢复起点）；(4) 无 COMPACTION 不截断（向后兼容，全量保留）；(5) load-time only（不删持久化数据）
- Deferred successors：跨进程接管锁（依赖 L4-8 Actor Runtime）/ Checkpoint retention/rotation/TTL policy / compaction-triggered snapshot.json / active-execution re-truncation / per-session seq 跨 restore 持久化

### nop-ai-agent LLM-turn / Compaction Checkpoint Triggers（Plan 187 — 全部 3 Phases completed）

- Plan: `ai-dev/plans/187-nop-ai-agent-llm-turn-compaction-checkpoint-triggers.md` → Plan Status: completed
- 交付 **LLM_TURN + COMPACTION checkpoint 触发点**——`CheckpointType` 枚举三个值（TOOL_EXECUTION / LLM_TURN / COMPACTION）全部由 dispatch loop 发射。6-plan checkpoint 可靠性投资链（177→186→187）的收尾增强
- **Phase 1**（LLM_TURN emission + intra-execution persistence + tests）：
  - `ReActAgentExecutor.java`：dispatch loop 在每次成功 LLM 响应后（token 计账完成后、completion judge 之前）发射 `Checkpoint(type=LLM_TURN)`，携带 outputSummary=assistant 响应内容 / messageCount / tokenEstimate。紧跟 intra-execution persistence（replaceMessages + sessionStore.save，同 plan 183 TOOL_EXECUTION 模式）
  - `checkpointSeq` 改为 `int[]` holder 以透传到 `performCompaction`/`handleForcedStop`；watermark 嵌入 per-execution start time 保证跨 execute() 调用唯一（防 DB-backed manager PK 冲突）
  - `TestCheckpointTriggersLLMTurnAndCompaction.java`：LLM_TURN 发射条件 + 字段完整性 + seq 交错 + 失败不发射 + NoOp 透传 + intra-execution persistence（6 tests）
- **Phase 2**（COMPACTION emission + intra-execution persistence + tests）：
  - `performCompaction` 在实际压缩成功后（消息替换 + token 调整后）发射 `Checkpoint(type=COMPACTION)`。`handleForcedStop` 签名变更透传 `int[] checkpointSeq` holder。NoOpContextCompactor 默认不触发（无实际压缩）
  - `TestCheckpointTriggersLLMTurnAndCompaction.java`：COMPACTION 发射条件 + NoOp compactor 不触发 + 未减轻不触发 + 字段完整性 + 三类型交错 + intra-execution persistence + forced-stop 路径（8 tests）
- **Phase 3**（End-to-end + design doc + Javadoc + roadmap）：
  - 多类型端到端 + 向后兼容测试（2 tests）
  - `CheckpointType.java` Javadoc：LLM_TURN / COMPACTION 从"Reserved... not emitted"更新为"Emitted... (plan 187)"
  - `nop-ai-agent-reliability.md` §5.4a：三个触发点全部标记 ✅ + 架构决策记录
  - `nop-ai-agent-roadmap.md` A4：LLM-turn/compaction triggers = plan 187 已落地
  - 现有测试适配：`TestCheckpointDispatchPathWiring` / `TestFileBackedCheckpointDispatchPath` / `TestRestoreSession` / `TestDBCheckpointManagerEngineWiring` 从 `getLatestCheckpoint` 断言改为 `getCheckpoints` + 类型过滤（LLM_TURN 现在是 trailing checkpoint）
  - 新增 `getCheckpoints(sessionId)` 方法到 `ToolExecutionCheckpoint` / `FileBackedCheckpointManager` / `DBCheckpointManager`（返回 session 的完整 checkpoint 列表，支持按类型过滤）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（1373 tests，全部新增 + 现有测试通过）
- 关键决策：watermark 嵌入 `ctx.getStartTimeMs()` 保证跨 execute() 唯一（restore re-execution 的 LLM_TURN(0) 不与 pre-crash LLM_TURN(0) 碰撞，防 DB PK violation）；`checkpointSeq` 用 `int[]` holder 而非字段（保持 per-execution local 语义）；COMPACTION 仅在实际压缩时发射（NoOp compactor 返回 compactedMessages==null，不进入替换分支）
- Deferred successors：per-session seq 跨 restore 持久化 / compaction-triggered snapshot.json / firstKeptEntryId compaction-aware 加载 / 跨进程接管锁 / Checkpoint retention/rotation/TTL

### nop-ai-agent DB-backed Checkpoint Persistence（Plan 186 — 全部 3 Phases completed）

- Plan: `ai-dev/plans/186-nop-ai-agent-db-backed-checkpoint-persistence.md` → Plan Status: completed
- 交付 **`DBCheckpointManager implements ICheckpointManager`**——`ICheckpointManager` 的第四个实现（与 `NoOpCheckpoint` / `ToolExecutionCheckpoint` / `FileBackedCheckpointManager` 同级），将 `Checkpoint` 持久化到 `ai_agent_checkpoint` 表，任何共享 DB 的服务实例可检索 resume-point watermark（跨进程恢复）
- **Phase 1**（ORM entity + table constant class + schema tests）：
  - `_vfs/nop/ai/agent/orm/app.orm.xml`：新增 `ai_agent_checkpoint` entity（11 列：WATERMARK PK / SESSION_ID nullable / SEQ / CHECKPOINT_TIMESTAMP BIGINT / CHECKPOINT_TYPE VARCHAR / TOOL_NAME / CALL_ID / INPUT_SUMMARY CLOB / OUTPUT_SUMMARY CLOB / MESSAGE_COUNT / TOKEN_ESTIMATE BIGINT）+ 4 新 domain（watermark / callId / checkpointType / seqNumber）
  - `AiAgentCheckpointTable.java`（`io.nop.ai.agent.reliability` 包）：表名/列名/DDL（CREATE TABLE IF NOT EXISTS + SESSION_ID/SEQ 复合索引）常量类。**SESSION_ID 列 nullable**（anonymous checkpoint 可持久化，与 `AiAgentSessionTable`/`AiAgentDenialTable` 的 NOT NULL 不同）
  - `TestAiAgentCheckpointTable.java`：ORM 模型验证 + DDL 建表/索引/幂等 + INSERT/SELECT + null-session-id 验证（9 tests）
- **Phase 2**（DBCheckpointManager implementation + unit tests）：
  - `DBCheckpointManager.java`（`io.nop.ai.agent.reliability` 包）：raw JDBC（与 `DBSessionStore` / `DBDenialLedger` 一致）。write-through `ConcurrentHashMap` cache（byWatermark + bySession + loadedSessions 负缓存）。`saveCheckpoint` 经 `INSERT INTO`（append-only，非 `MERGE INTO` upsert——checkpoint 是追加语义，与 `DBSessionStore.save` 的 upsert 不同）；`getLatestCheckpoint` 经 `WHERE SESSION_ID = ? ORDER BY SEQ DESC`（无 LIMIT 1，warm-cache 模式）；`getCheckpoint` 经 PK lookup。CLOB 列用 `setCharacterStream`/`getCharacterStream`
  - `TestDBCheckpointManager.java`：save→get 往返 + 字段完整性（全字段/nullable/null enum/边界/large CLOB）+ getLatestCheckpoint + getCheckpoint PK lookup + append-only/duplicate-watermark fail-fast + cross-session isolation + anonymous session + empty session + 向后兼容（16 tests，全部经 DB 直接 SQL 验证）
- **Phase 3**（Cross-instance end-to-end + engine wiring + design doc）：
  - `TestDBCheckpointManagerEngineWiring.java`（`io.nop.ai.agent.engine` 包）：**跨实例存活**（核心价值——销毁 mgr A + 建 mgr B 共享 DB → getCheckpoint/getLatestCheckpoint 一致）+ **engine.execute DB-backed 写入验证**（dispatch loop saveCheckpoint 落入表）+ **restoreSession DB-backed checkpoint**（engine.execute → checkpoint 写 DB → crash 模拟 → 新 engine + 新 mgr restoreSession → 从 DB 加载续跑）+ 向后兼容（NoOp/FileBacked 0 行写入 DB）+ DB-backed checkpoint 检索验证（5 tests）
  - `nop-ai-agent-reliability.md` §5.4：更新实现状态 + 架构决策（raw JDBC / hybrid 列布局 / INSERT append-only / write-through cache / getLatestCheckpoint ORDER BY SEQ DESC / 跨进程接管锁仍 deferred）；§1.1 更新 DB-backed checkpoint store 已落地
  - `nop-ai-agent-roadmap.md`：A4 行更新（DB persistence = plan 186 已落地，实现为 `DBCheckpointManager`）
  - Javadoc 更新：`ICheckpointManager.java` / `Checkpoint.java` / `ToolExecutionCheckpoint.java` 从"successor"更新为"landed"
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（1359 tests，全部新增 + 现有测试通过）
- 关键决策：hybrid 列布局（9 scalar 列 + 2 CLOB 用于 inputSummary/outputSummary——这两字段承载全量 tool I/O，非短摘要）；INSERT append-only（非 MERGE INTO upsert——checkpoint 是追加非覆写）；类名 `DBCheckpointManager`（平行于 `FileBackedCheckpointManager`，非 carry-over 引用的 `DBCheckpointStore`）
- Deferred successors：跨进程接管锁（依赖 L4-8 Actor Runtime）/ Checkpoint retention/rotation/TTL policy / LLM-turn/compaction triggers / firstKeptEntryId compaction-aware 加载 / ORM migration tooling

---

- Plan: `ai-dev/plans/185-nop-ai-agent-db-backed-session-store.md` → Plan Status: completed
- 交付 **`DBSessionStore implements ISessionStore`**——`ISessionStore` 的第三个实现（与 `InMemorySessionStore` / `FileBackedSessionStore` 同级），将 `AgentSession` 持久化到 `ai_agent_session` 表，任何共享 DB 的服务实例可 load + take over（跨进程恢复的核心前提）
- **Phase 1**（ORM entity + table constant class + schema tests）：
  - `_vfs/nop/ai/agent/orm/app.orm.xml`：新增 `ai_agent_session` entity（6 列：SESSION_ID PK / AGENT_NAME / STATUS / SESSION_DATA CLOB / CREATED_AT BIGINT / UPDATED_AT BIGINT）+ 3 新 domain（agentName / sessionStatus / epochMillis）
  - `AiAgentSessionTable.java`（`io.nop.ai.agent.session` 包）：表名/列名/DDL（CREATE TABLE IF NOT EXISTS + status index）常量类，遵循 `AiAgentMessageTable` / `AiAgentDenialTable` 模式
  - `TestAiAgentSessionTable.java`：ORM 模型验证 + DDL 建表/索引/幂等 + MERGE INTO upsert 验证（6 tests）
- **Phase 2**（DBSessionStore implementation + unit tests）：
  - `DBSessionStore.java`（`io.nop.ai.agent.session` 包）：raw JDBC（`DataSource` + `PreparedStatement`，与 `DBDenialLedger` / `DBMessageService` 一致）。write-through `ConcurrentHashMap` cache（同 `FileBackedSessionStore`）。`save` 经 `MERGE INTO ... KEY (SESSION_ID)` 等幂 upsert；`get` cache-miss DB lazy-load；`listAllSessions` 全表 `SELECT SESSION_DATA` + `SessionFileReader.deserialize`；`remove` DB DELETE；`forkSession` 同 `FileBackedSessionStore` 逻辑。序列化复用 package-private static `SessionFileWriter.serialize` / `SessionFileReader.deserialize`——零新增序列化代码
  - `TestDBSessionStore.java`：save→get 往返 + 字段完整性（全 status enum / timestamps / nested metadata）+ upsert 等幂 + listAllSessions 发现 + remove + getOrCreate 新建/已存在 + forkSession inherit/not + 边界（空 session / 不存在 / 损坏 JSON skip+warn）+ 向后兼容（20 tests，全部经 DB 直接 SQL 验证）
- **Phase 3**（Cross-instance end-to-end + engine wiring + design doc）：
  - `TestDBSessionStoreEngineWiring.java`（`io.nop.ai.agent.engine` 包）：**跨实例存活**（核心价值——销毁 store A + 建 store B 共享 DB → get/listAllSessions 一致，证明持久化经 DB 而非内存）+ **restoreSession DB-backed**（engine.execute → DB 写入验证 → 模拟 crash → 新 engine restoreSession → 从 DB 加载续跑）+ **restorePendingSessions DB-backed discovery**（2 sessions → crash → 新 engine listAllSessions 从 DB 发现 → 自动恢复）+ 向后兼容（InMemory/FileBacked 0 行写入 DB）+ DB-backed engine 写入表验证（6 tests）
  - `nop-ai-agent-reliability.md` §1.1：新增"实现状态（plan 185 ✅ 已落地）"段落 + 6 项架构决策（raw JDBC / 混合列布局 / write-through cache / MERGE INTO upsert / listAllSessions SQL discovery / 跨进程接管锁仍 deferred）；line 31 "DB-backed session store 是 successor"→"已落地（plan 185）"
  - `nop-ai-agent-roadmap.md`：新增 L3-4d row（DB-backed session store，✅）；L3-4c 移除"DB 发现"from deferred；Layer 4 验收标准补充 plan 185；Updated header 同步
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（全部新增 + 现有测试通过）；`check-plan-checklist.mjs --strict` → exit 0；`scan-hollow-implementations.mjs --severity high` → exit 0
- 关键决策：混合列布局（scalar queryable columns + full session JSON CLOB）——scalar 列支撑未来 status-based SQL 筛选/监控/cleanup，SESSION_DATA CLOB 复用已验证的 SessionFileWriter/Reader 序列化（与 FileBackedSessionStore 100% 一致）
- Deferred successors：跨进程接管锁（依赖 L4-8 Actor Runtime）/ DB-backed checkpoint persistence（DBCheckpointStore）/ Session retention cleanup TTL / Status-based SQL filtering in restorePendingSessions / appendEvent/compact/loadSnapshot/setPlanRef DB-backed

---

- Plan: `ai-dev/plans/184-nop-ai-agent-auto-restore-on-startup.md` → Phase 2 completed
- Phase 2 纯文档更新：auto-restore-on-startup 标记"已落地" + 架构决策记录 + roadmap carry-over 状态同步
- `nop-ai-agent-reliability.md` §1.1（恢复模型）：新增"实现状态（plan 184 ✅ 已落地 — 自动 restore-on-startup）"段落（6 项实现状态：磁盘发现能力 / auto-restore 批量入口点 / 可恢复状态策略 / 顺序恢复+失败隔离 / 显式 opt-in 方法 / fail-fast 语义）+ 4 项架构决策（发现是新 default UOE 方法非 getAll() 修改 / 可恢复策略 running+pending only paused/terminal 跳过 / 顺序恢复非并行 / 显式 opt-in 部署层决定）
- `nop-ai-agent-reliability.md` §5.4a："自动 restore-on-startup 仍是独立 successor"改为"✅ 已落地（plan 184）"，并补充 `restorePendingSessions` + `listAllSessions()` 引用
- `nop-ai-agent-roadmap.md`：新增 L3-4c row（auto restore-on-startup，✅）；Layer 4 验收标准"长任务中断后可以恢复"补充 plan 184；Updated header 同步
- 验证：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS；`node ai-dev/tools/check-doc-links.mjs --strict` → 12 errors（全部 pre-existing，0 新增，git stash 对比确认）
- 确认 deferred successors：并行/限流恢复 + 定时/周期性后台扫描 + 恢复优先级排序 + 自动触发时机自动化 + DB-backed session store 发现（参照 plan 179 模式）——均标注为本计划 successor（已记录在 plan 184 `Deferred But Adjudicated` + `Non-Blocking Follow-ups`）

---

### nop-ai-agent Auto Restore-on-Startup — disk discovery + batch orchestration（Plan 184 — Phase 1）

- Plan: `ai-dev/plans/184-nop-ai-agent-auto-restore-on-startup.md` → Phase 1 completed
- Phase 1 交付 **自动 restore-on-startup**：进程崩溃重启后，引擎自动扫描持久化的未完成 session 并批量恢复——从"手动逐个 restoreSession"（plan 183）进化为"自动扫描全部"。补齐"无人值守自动化"最后一块（roadmap §1 模块定位 + §4 Layer 4"长任务中断后可以恢复"）
- 新增/修改：
  - `ISessionStore.listAllSessions()`（default UOE）：磁盘 session 发现契约。`getAll()` 语义不变（仍 cache-only），新方法返回"此 store 已知的全部 session，含尚未加载到 cache 的持久化 session"。fail-fast default UOE（Minimum Rules #24）
  - `InMemorySessionStore.listAllSessions()` 覆写为返回 `getAll()`（in-memory store 全部 session 都在内存中，无磁盘概念）
  - `FileBackedSessionStore.listAllSessions()` 覆写为扫描 `rootDirectory` 子目录 + 加载每个 `session.json`（加载后存入 cache，后续 `get` 命中）。损坏/截断 JSON 跳过 + LOG.warn（不阻断发现剩余 session；后续 `get(corruptId)` 仍快速失败）。root 不存在或无 session 子目录返回空集合
  - `IAgentEngine.restorePendingSessions(approver, reason)`（default UOE）+ 完整 Javadoc：发现 → 筛选（running/pending 候选；paused 跳过=sticky-pause governance 需人类 resumeSession，plan 180；terminal 跳过）→ 逐个 restoreSession（顺序恢复，非并行——避免并发 LLM 限流）→ 返回摘要
  - `DefaultAgentEngine.restorePendingSessions` 实现：复用现有 `restoreSession`（Wiring Verification——不重复实现 restore 协议，Minimum Rules #23）；逐 session try/catch 失败隔离（一个失败不中止 batch）；发现方法抛 UOE → 抛 `NopAiAgentException`（fail-fast 部署配置错误，非静默空摘要）
  - `SessionRestoreSummary` 值类型：restored / skipped / failed 三桶 + 每项原因（immutable）
- 测试（`TestRestorePendingSessions` 12 tests + `TestFileBackedSessionStore` +8 discovery tests）：
  - **端到端 auto-restore**（核心价值——Minimum Rules #22）：engine A.execute×2 → 模拟 crash（set status=running）→ 新 engine B（同 store/ckpt 根目录）→ `restorePendingSessions` → 两个未完成 session 自动恢复，无需人工指定 sessionId
  - **状态筛选**：8 status 各 1 个 session → running+pending 恢复，paused+5 terminal 跳过（含原因）
  - **失败隔离**：损坏 session.json 在发现阶段跳过（+ 后续 `get` 仍 fail-fast）；restoreSession 抛异常（agent model 不存在）→ 标记 failed，其他候选正常恢复，batch 不中止
  - **空磁盘**：root 不存在/为空 → 返回空摘要（非异常）
  - **向后兼容**：InMemorySessionStore cache 为空 → 空摘要（合法语义）；InMemorySessionStore 含 running session → 正常恢复
  - **fail-fast**：store 不覆写 listAllSessions（default UOE）→ `NopAiAgentException`
  - **Wiring Verification**（Minimum Rules #23）：子类计数 verify `restorePendingSessions` 内部确实调用 `restoreSession`（仅 running/pending 触发，paused/terminal 不触发）
  - **磁盘发现**（8 tests）：新 store 实例发现磁盘 session + 跨实例 grow + 损坏 JSON 隔离 + 无 session.json 子目录忽略 + getAll 语义不变 + default UOE
- 向后兼容：`getAll()` 语义不变；`restoreSession`（plan 183）16 tests 全通过；InMemorySessionStore 默认不受影响
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1296 tests, 0 failures（+20 新增测试，现有全部通过）

---

### nop-ai-agent Crash/Restart Durable Session Restore — Design doc + roadmap sync（Plan 183 — Phase 3）

- Plan: `ai-dev/plans/183-nop-ai-agent-crash-restart-session-restore.md` → Phase 3 completed
- Phase 3 纯文档更新：crash/restart restore protocol 标记"已落地" + 架构决策记录 + deferred successors 依赖关系同步
- `nop-ai-agent-reliability.md` §1.1（恢复模型）：新增"实现状态（plan 183 ✅ 已落地 — 单进程 crash/restart restore）"段落，记录 6 项实现状态（文件级 session 持久化 / intra-execution 持久化 / restore 入口点 / restore 协议 / checkpoint journal 角色 / 单进程 scope）+ 3 项架构决策（session 持久化是必要前提 / restore vs resume 区分 / 单进程 scope）
- `nop-ai-agent-reliability.md` §5.4：实现状态更新为"L3-4 ✅ 已落地 + plan 183 ✅ restore 路径消费"；"save-side 先行，restore-side 延期" 改为"save-side 先行，restore-side ✅ 已落地（plan 183）"
- `nop-ai-agent-reliability.md` §5.4a：状态注释中"crash/restart restore protocol 仍为独立 successor"改为"已由 plan 183 落地"；"crash/restart restore protocol 仍为独立 successor"架构决策项改为"✅ 已落地（plan 183）"
- `nop-ai-agent-roadmap.md`：A4 row 更新"crash/restart restore = plan 183 已落地"；新增 L3-4b row（crash/restart durable session restore，✅）；Layer 4 验收标准"长任务中断后可以恢复"勾选 `[x]`
- 确认 deferred successors：DB-backed session store（参照 plan 179 模式）/ 跨进程 takeover lock（依赖 L4-8）/ 自动 restore-on-startup / plan/todo 状态恢复 / firstKeptEntryId compaction-aware 加载——均标注为本计划 successor（已记录在 plan 183 `Deferred But Adjudicated` + `Non-Blocking Follow-ups`）
- 验证：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS（纯文档变更，无代码行为变化）

---

### nop-ai-agent Crash/Restart Durable Session Restore — restoreSession + checkpoint reconciliation（Plan 183 — Phase 2）

- Plan: `ai-dev/plans/183-nop-ai-agent-crash-restart-session-restore.md` → Phase 2 completed
- Phase 2 交付 **crash/restart restore 端到端协议**：engine restoreSession 入口 + checkpoint journal 消费 + 端到端 crash/restart 存活
- 新增/修改：
  - `IAgentEngine.restoreSession(sessionId, approver, reason)`：default UOE + 完整 Javadoc（与 `resumeSession` 交叉引用区分 crash-restart vs sticky-pause）
  - `DefaultAgentEngine.restoreSession` 实现：检测 session 不在 `runningExecutions` → 从 `sessionStore.get` 加载 → `checkpointManager.getLatestCheckpoint` 一致性校验（best-effort 警告，不阻断）→ status 置 running → 发布 `SESSION_RESTORED` 事件（approver/reason/latestCheckpointWatermark/preRestoreStatus）→ `buildBaseExecutionContext` 重建 → `resolveExecutor` + `executor.execute` 续跑 → post-execution `replaceMessages` + `sessionStore.save` 最终 flush
  - `AgentEventType.SESSION_RESTORED`：新事件类型，参照 `SESSION_RESUMED` 模式
  - Fail-fast 语义：活跃内存中（`runningExecutions` 含 sessionId）→ 抛 `NopAiAgentException`；无持久化状态（`sessionStore.get` 返回 null）→ 抛异常；终态 status（completed/failed/cancelled/forced_stopped/escalated）→ 抛异常；null/空 sessionId → 抛异常
  - Checkpoint journal **首次在 restore 路径被运行时消费**（plan 182 投资兑现，非仅 save-side）：`getLatestCheckpoint` 提供 resume-point 元数据 + 一致性校验（checkpoint.messageCount ≤ session.messageCount；违反时 LOG.warn + 继续恢复）
- 测试（`TestRestoreSession` 16 tests）：
  - **crash/restart 端到端存活**（核心价值——Minimum Rules #22）：engine A.execute → 持久化 → 新 engine B（同 store/ckpt 根目录）→ `restoreSession` → 续跑完成 → SESSION_RESTORED 事件 + 恢复后 status=completed
  - **mid-execution crash restore**：engine A 执行含 tool call → intra-execution save 写入 tool response → 模拟 crash（set status=running）→ engine B restoreSession → 验证恢复后 session 仍含 crash 前 tool response
  - **Anti-Hollow wiring**（Minimum Rules #23）：counting checkpoint manager verify `getLatestCheckpoint` 在 restore 路径确实被调用
  - fail-fast：无持久化状态 / 终态（completed/cancelled/failed）/ null sessionId → 抛 `NopAiAgentException`
  - checkpoint 一致性校验：checkpoint messageCount > session messageCount → 警告 + 继续恢复（best-effort）
  - restore vs resume 互斥：resumeSession 对 in-memory paused session 正常；IAgentEngine default restoreSession UOE
  - SESSION_RESTORED 事件 payload 验证（approver/reason/latestCheckpointWatermark/preRestoreStatus）
  - 向后兼容：InMemorySessionStore + NoOpCheckpoint restoreSession 正常执行（session 在 store 中、不在 runningExecutions、非终态）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1276 tests, 0 failures（+16 新增 Phase 2，现有全部通过）

---

- Plan: `ai-dev/plans/183-nop-ai-agent-crash-restart-session-restore.md` → Phase 1 completed
- Phase 1 交付 **文件级 session 持久化**（crash/restart restore 的必要前提——journal 只存 tool 摘要，完整消息历史来自 session store）
- 新增/修改：
  - `FileBackedSessionStore implements ISessionStore`（drop-in 替换 `InMemorySessionStore`）：per-session `{root}/{sessionId}/session.json` 全量持久化（overwrite-on-write），内部 in-memory cache lazy-load 文件。实现 getOrCreate/get/remove/getAll + forkSession + save
  - `SessionFileWriter` / `SessionFileReader`：JSON 序列化/反序列化 AgentSession 全字段（含 `List<ChatMessage>` 多态往返——`@JsonTypeInfo(property="role")` 经 JsonTool 正确还原 user/assistant/system/tool/custom）。缺失文件返回 null（合法语义），损坏 JSON 快速失败抛 `NopAiAgentException`
  - `ISessionStore.save(AgentSession)`：default UOE 新增（与现有 5 个 default UOE 方法一致）；`InMemorySessionStore` 显式覆写为带 Javadoc 的 no-op（in-memory 共享引用语义）；`FileBackedSessionStore` 覆写为 JSON 全量持久化
  - `AgentSession.replaceMessages(List<ChatMessage>)`：full-sync replace（清空+全量写入，等幂）——支撑 intra-execution 持久化与 post-execution 同步的统一 replace 语义。package-private `restore(sessionId, agentName, createdAt, updatedAt)` 工厂 + `restoreUpdatedAt` 供 SessionFileReader 保留 persisted timestamps
  - `ReActAgentExecutor`：新增 `sessionStore` 字段 + ctor 参数 + Builder setter。在 `saveCheckpoint`（`:918`）后调用 `sessionStore.get(sessionId)` → `session.replaceMessages(ctx.getMessages())` → `sessionStore.save(session)`，实现 **intra-execution 持久化**（crash 中途存活的关键）
  - `DefaultAgentEngine`：`resolveExecutor` 注入 `sessionStore` 到 executor Builder；`doExecute`/`resumeSession` post-execution 同步从 `appendMessages(subList(historyCount, currentCount))` 改为 `replaceMessages(ctx.getMessages())`（等幂全量同步，消除 intra-execution + post-execution 重复 append 冲突）+ 末尾 `sessionStore.save(session)` 最终 flush
- 测试：
  - `TestFileBackedSessionStore`（16）：跨实例往返 + 全 status enum 值 + timestamps 精确保留 + 嵌套 metadata + per-session 隔离 + 边界（空 session/缺失文件/损坏 JSON/缺必填字段）+ getOrCreate cache-miss load + remove 删文件+cache + save 契约桥（default UOE/InMemory no-op/FileBacked 写盘）+ getAll
  - `TestSessionStoreIntraExecutionPersistence`（2）：**Anti-Hollow wiring**——chat service 第二次调用时检查磁盘 session 文件已含 tool response（证明 intra-execution save 在 LLM 第二次调用前已触发）+ no-duplicate-messages（FileBackedStore 多轮执行后 session.getMessageCount() == ctx 完整消息数，cross-instance reload 一致）
  - `TestAgentSession`：+5 tests（replaceMessages 清空+设置/等幂/null 清空/不 mutate input/touch updatedAt）
- 向后兼容：InMemorySessionStore.save 为 no-op，intra-execution 接线无副作用；现有 1258 测试全通过
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1260 tests, 0 failures（+35 新增测试，现有全部通过，0 副作用）

---

- Plan: `ai-dev/plans/182-nop-ai-agent-checkpoint-journal.md` → completed（Phase 1 + Phase 2 + Phase 3 + Closure）
- 交付可靠性层 §5.4a **Checkpoint Journal 双文件持久化格式**：journal.md（追加写入 source of truth）+ snapshot.json（派生缓存）+ 文件级 `ICheckpointManager` 功能化实现（`FileBackedCheckpointManager`，drop-in 替换 `ToolExecutionCheckpoint`，checkpoint 跨进程重启存活）
- Phase 1：格式层 + 读写器 + 往返测试
  - `CheckpointJournalWriter`：将 `Checkpoint` 序列化为 §5.4a `## CP-NNN` markdown 段落并追加到 per-session journal.md（append-only，per-file 锁线程安全）。String 字段 JSON 编码（多行内容转义为单行）。首行 `# Checkpoint Journal - {sessionId}` header
  - `CheckpointJournalReader`：解析 journal.md 重建 `Checkpoint` 列表。`readAll`（全量）+ `readAfter(watermark)`（增量——snapshot-accelerated recovery 消费）。损坏 entry 显式跳过 + 日志记录（非静默吞掉），空/缺失文件返回空列表（Javadoc 声明）
  - `CheckpointSnapshot` 值类型：snapshotId/sessionId/lastWatermark/messageCount/tokenEstimate/createdAt（recovery-critical 字段子集——planStatus/toolResults 属 Non-Goal）
  - `CheckpointSnapshotWriter` / `CheckpointSnapshotReader`：JSON 序列化/反序列化 snapshot.json（覆写语义，per-instance 锁）。缺失文件返回 null（合法语义）
  - `TestCheckpointJournalSnapshotFormat`（14 tests）：journal 往返（全字段 + null 字段 + 多 checkpoint 有序）+ snapshot 往返 + 增量读取（readAfter 正常/末尾/未知 watermark 降级）+ append-only 文件增长 + header 验证 + 边界（空/缺失/header-only/损坏 section 跳过）+ snapshot 覆写
- Phase 2：文件级 ICheckpointManager + watermark 恢复 + dispatch-path drop-in + 端到端 + 向后兼容
  - `FileBackedCheckpointManager`（implements `ICheckpointManager`）：per-session `{root}/{sessionId}/journal.md` + `snapshot.json` 文件布局。saveCheckpoint → journal.md 追加 + 按 snapshotInterval（默认 10）自动生成 snapshot.json + 内存索引更新。lazy-load 从文件恢复（首次访问 session 时）。getLatestCheckpoint/getCheckpoint 从内存索引返回
  - watermark 恢复读取路径：首次访问 session → 读 snapshot.json（如存在）获取 lastWatermark 元数据 → 读 journal.md 全量 entries 重建 checkpoint 列表 + watermark 索引 → 内存索引可用。`CheckpointJournalReader.readAfter`（已测试验证）是 restore-on-restart successor 的增量读取原语
  - `TestFileBackedCheckpointManager`（7 tests）：save→persist→reload→retrieve 跨实例存活 + snapshot 加速恢复 + 降级恢复（无 snapshot 全量扫描）+ per-session 文件隔离 + snapshot 自动生成（interval）+ 跨实例追加
  - `TestFileBackedCheckpointDispatchPath`（5 tests）：drop-in 接线（executor + FileBackedCheckpointManager → journal.md 写入验证）+ **端到端** engine.execute → saveCheckpoint → journal.md → 跨实例 reload → getLatestCheckpoint + 向后兼容（ToolExecution 不写文件 / NoOp 0 副作用 / Builder 默认）
- Phase 3：设计文档 + roadmap 状态同步
  - `nop-ai-agent-reliability.md` §5.4a 更新：格式标记 ✅ 已落地 + 架构决策（文件级 drop-in 模式 / snapshot 生成时机 / long-running 过滤裁定 / Event Log 关系 / recovery-critical 字段子集 / watermark 恢复读取路径 / crash/restart restore 仍为 successor）
  - `nop-ai-agent-roadmap.md` §4 row A4：`❌` → `✅`
- Closure：`check-plan-checklist.mjs --strict` 退出码 0 + `scan-hollow-implementations.mjs --module nop-ai-agent --severity high` 退出码 0 + `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 1237 tests 0 failures

---

### nop-ai-agent ICheckpointManager Contract + NoOpCheckpoint + ToolExecutionCheckpoint（Plan 181 — all phases + closure）

- Plan: `ai-dev/plans/181-nop-ai-agent-checkpoint-manager.md` → completed（Phase 1 + Phase 2 + Phase 3 + Closure）
- 交付可靠性层 §5.4 检查点 **记录/检索能力**：`ICheckpointManager` 契约 + `NoOpCheckpoint` 透传默认 + `ToolExecutionCheckpoint` in-memory 功能化实现 + executor/engine 分发循环接线
- Phase 1：契约表面 + NoOp 默认 + 值类型 + 单元测试
  - 新增 `io.nop.ai.agent.reliability` 包（与 `io.nop.ai.agent.security` 平行——可靠性增强非安全治理）
  - `CheckpointType` 枚举：`TOOL_EXECUTION`（L3-4 交付）+ `LLM_TURN` / `COMPACTION`（roadmap A4 前向声明）
  - `Checkpoint` 不可变值类型：sessionId/watermark/seq/timestamp/type/toolName/callId/inputSummary/outputSummary/messageCount/tokenEstimate，工厂构造 + 全字段 equals/hashCode（参照 `DenialRecord` 模式）
  - `ICheckpointManager` 接口：`saveCheckpoint` / `getLatestCheckpoint` / `getCheckpoint(watermark)`，Javadoc 声明线程安全 + 持久化非强制（与 L3-6 L3-G5 一致）+ save-side 先行 / restore-side 延期裁定
  - `NoOpCheckpoint` 透传默认：singleton + `noOp()` 工厂（参照 `NoOpDenialLedger.noOp()` 模式），saveCheckpoint no-op / 检索返回 null
  - `TestNoOpCheckpointAndValue`（14）：NoOp pass-through 语义 + 线程安全（8 线程 ×200 调用）+ Checkpoint 构造/不可变性/factory 校验
- Phase 2：功能化实现 + executor/engine 接线 + 端到端 + 向后兼容
  - `ToolExecutionCheckpoint` 功能化实现（implements `ICheckpointManager`）：`ConcurrentHashMap<String, List<Checkpoint>>` per-session 存储 + `ConcurrentHashMap<String, Checkpoint>` watermark 索引，saveCheckpoint 真实追加、getLatestCheckpoint 返回最后一个、getCheckpoint 精确检索。in-memory 不持久化。对所有工具执行记录 checkpoint（不套用 §5.4a "仅 long-running tool" 限定——in-memory 无 I/O 成本，过滤下沉到持久化层）
  - `ReActAgentExecutor` 扩展：`checkpointManager` 字段 + ctor 参数（null-fallback `NoOpCheckpoint.noOp()`）+ Builder 字段 + Builder setter + `Builder.build()` pass（追加为最后一个扩展参数，在 `postDenialGuard` 之后）
  - `ReActAgentExecutor` 分发循环保存侧接线：工具执行完成后（tool result 加入 context 后、POST_ACTING hook 前）调用 `checkpointManager.saveCheckpoint(Checkpoint.of(...))`，类型 `TOOL_EXECUTION`，含 toolName/callId/input-output 摘要 + messageCount/tokenEstimate 快照 + 递增 seq + watermark
  - `DefaultAgentEngine` engine 接线：`checkpointManager` field（默认 `NoOpCheckpoint.noOp()`）+ `setCheckpointManager`/`getCheckpointManager`（null-fallback）+ `Builder.build()` pass 到 executor
  - `TestToolExecutionCheckpoint`（7）：save→retrieve 往返（单次 + 多次 latest + watermark 精确检索 + nonexistent 返回 null）+ per-session 隔离 + null-session-id 行为
  - `TestCheckpointDispatchPathWiring`（7）：Builder 接线（saveCheckpoint 运行时被调用 + payload 验证）+ 多工具多 checkpoint seq 递增 + **端到端** `engine.execute(request)` → checkpoint 含正确 tool payload + 向后兼容（NoOp 默认 0 副作用）+ Builder 默认 + engine setter/getter null-fallback + per-session 跨 engine 执行隔离
- Phase 3：设计文档 + roadmap 状态同步
  - `nop-ai-agent-reliability.md` §5.4 更新：记录/检索能力标记 ✅ 已落地 + 架构决策（save-side 先行 / in-memory 不持久化 / reliability 包归属 / in-memory 触发范围）+ §5.4a 标注为 roadmap A4 deferred
  - `nop-ai-agent-roadmap.md` §4 L3-4 行 `❌` → `✅`；§2.2 row 54 `❌ 未开始` → `✅ 已落地`；A4 blocked-by L3-4 依赖已满足
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1211 tests, 0 failures, 0 errors（+28 新增 = Phase1 14 + Phase2 14；现有 1183 全部通过，0 副作用）
- 验证：`node ai-dev/tools/check-plan-checklist.mjs ... --strict` exit 0；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-ai-agent --severity high` exit 0
- # Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-reliability.md` §5.4 已更新；`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` L3-4 + §2.2 row 54 已 ✅

### 2026-06-15 — GraphQL schema auto-dump + debugging docs 完善

- 调研 DevDoc\_\_graphql、\_dump 机制现状和文档覆盖
- 发现缺口：GraphQL schema 仅运行时通过 DevDoc\_\_graphql 可查，启动时无自动 dump 到 \_dump/
- 新增 `BizObjectManager.dumpGraphQLSchema()`：`nop.debug=true` 时，在 delayInit 完成后自动 dump GraphQL schema (SDL) 到 `_dump/{appName}/nop/main/graphql/schema.graphql`
- 完善 `debugging-and-diagnostics.md`：补充 `_dump` vs DevDoc 关系表、GraphQL schema dump 说明、`_dump` 输出内容清单
- 更新 `source-anchors.md`：新增 DBG-005 锚点
- 无 plan（ad-hoc 调研 + 完善）
- 验证：`./mvnw test -pl nop-biz -am` 通过
- Doc-sync: `debugging-and-diagnostics.md`, `source-anchors.md` 已更新

### 2026-06-15 — Error message retrieval docs

- 调研 ErrorCode → Error message 运行时获取链路：`ErrorCode.getDescription()` → `I18nMessageManager.getMessage()` → `ErrorMessageManager.getLocalizedDescription()` / `getErrorDescription()` → `NopException.getDescription()` / `getMessage()`
- `docs-for-ai/02-core-guides/error-handling.md` 原来只覆盖定义和抛出方向，缺少"从 ErrorCode 获取消息"反向链路
- 新增章节 **"从 ErrorCode 获取错误消息"**，包含 ErrorCode 原始描述、i18n 查询、NopException 各方法对比、统一构建 ErrorBean/ApiResponse 的示例
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` → 0 new errors（所有 12 个 errors 均为 ai-dev/ 下 pre-existing）
- 后续调研 i18n 整体机制：i18n 文件结构（`/i18n/{locale}/*.i18n.yaml`）、启用配置（`nop.core.i18n.enabled-locales`、`nop.application.locale`）、前端传 locale 链路（`nop-locale` header → `AuthHttpServerFilter` → `IContext.setLocale` → `ContextProvider.currentLocale()`）、`@i18n:` 前缀语法在 XMeta/XView 中的使用、消息查询层次
- `docs-for-ai/02-core-guides/error-handling.md` 原来完全缺少 i18n 整体机制说明
- 新增章节 **"国际化(i18n)消息机制"**，覆盖文件结构、配置、前后端 locale 传递、`@i18n:` 语法、消息查询层次
- Doc-sync: `docs-for-ai/02-core-guides/error-handling.md` 已更新
  > > > > > > > 1161fa1d7 (docs: 更新开发日志)
