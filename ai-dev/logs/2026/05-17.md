# 2026-05-17 开发日志

### 2026-05-18（凌晨）— Worker 并发控制实现

限流设计文档修正：调研发现 Nop 平台已有完整限流体系（`IFlowControlRunner` + Sentinel 集成 + `IRateLimiter`），不需要引入 resilience4j。Worker 并发控制方案从 Semaphore 改为"查询在途 task 数 + 超限跳过"（count + skip），避免异步回调导致的 Semaphore 泄漏风险。

实现：
- `IJobTaskStore.countRunningTasks(workerInstanceId)` — 查询指定 Worker 当前 RUNNING 状态的 task 数
- `JobWorkerScannerImpl.maxConcurrency` — 配置项 `nop.job.worker.max-concurrency`，默认 0 不限制
- `scanOnce()` 开头检查 runningCount，超限时跳过拉取，未超限时用 `remaining` 作为 batch 上限
- `IJobWorkerMetrics.onRejected()` + Counter `nop.job.worker.task-rejected`
- 2 个新测试通过（skipsWhenAtLimit / allowsWhenBelowLimit）

### 2026-05-17（晚上）— Plan 17 执行

- Plan: `ai-dev/plans/17-nop-job-block-strategy-metrics.md`
- 全部 5 个 phase 完成（OVERLAY cancel + RECOVERY 策略 + Metrics 三件套 + Schedule 聚合 + 限流设计）
- 验证：coordinator 20 + worker 8 = 28 tests, 0 failures
- Doc-sync: `block-strategy-design.md` / `metrics-design.md` 标记为 implemented，`rate-limiting-design.md` 补全

### 2026-05-17（下午）— 吸引子层治理

对比 chaos-flux PPT（26 页方法论）和实际 `docs/` 结构后，识别出核心差距：`ai-dev/design/` 缺少 attractor index（层级 + precedence model），不像 chaos-flux 的 `docs/architecture/README.md` 那样定义了 Governing Principles → Normative Architecture → Platform-Extension → Focused Subsystem 的层级。

**根本原因分析**：两项目做法不同是因为文档受众不同。Chaos-flux 的 `docs/` 是单一统一树（AI-only 项目）；Nop-entropy 有 `docs/`（人工历史）+ `docs-for-ai/`（平台使用知识）+ `ai-dev/`（AI 开发过程），三棵树服务不同读者。这个分裂是正确的。

**从 chaos-flux 借鉴的核心是方法论**（PPT 第 25 页）：五个结构性问题必须有答案（谁定义方向？冲突时信谁？怎么发现漂移？怎么保留记忆？怎么判定完成？），而不是表面目录结构。

- 新增 `ai-dev/design/README.md`：定义层级（Writing Guide → Cross-Cutting → Subsystem）、precedence model（platform principles → subsystem local → conflict resolution）、与 `docs-for-ai/` 和 `docs/` 的关系
- 评估 governing-principles 文档：**不需要**。Nop 的 governing principles（可逆计算、model-first）已在 `docs/theory/` 和 `docs-for-ai/00-start-here/ai-defaults.md` 中覆盖，不像 chaos-flux 需要在项目内重新定义 Flux 七原语
- 更新 `ai-dev/design/00-design-writing-guide.md`：组织方式引用 README 层级
- 更新 `AGENTS.md`：design 行标注 attractor layer，路由增加先看 README

Owner-doc decision: `AGENTS.md` 已更新。`ai-dev/design/README.md` 是新增的 attractor index。

### 2026-05-17（下午）— 文档治理

- Plan 17 (`ai-dev/plans/17-nop-job-block-strategy-metrics.md`) 重写为完整模板格式：补齐 Purpose / Scope / Closure / Deferred But Adjudicated / Non-Blocking Follow-ups / Closure Audit Evidence 等此前缺失的结构
- Plan 16 (`ai-dev/plans/16-nop-job-core-redesign.md`) 从 `docs/plans/job-core-redesign.md` 迁移，验证仓库后标记为 completed（所有 Phase 已在之前实现）
- Plan guide (`ai-dev/plans/00-plan-authoring-and-execution-guide.md`) 同步 chaos-flux 最新版本：补齐 Rule 20（历史计划不回写）、When Executing 第 6 条、数字修正；本地化路径（`docs/` → `ai-dev/`）、构建命令（`pnpm` → `./mvnw`）
- 新增 `ai-dev/design/00-design-writing-guide.md`：定义 design 文档只记录架构决策+使用契约+需求规格，不写代码细节；伪代码判断标准（"应该发生什么" vs "代码怎么写"）
- Plan guide 增加 Rule 10：plans 不放代码片段，与 design guide 对齐
- `AGENTS.md` 更新：design/ 角色增加"需求规格"定位；plans 行明确禁止写入 `docs/plans/`
- `ai-dev/design/nop-job/metrics-design.md` 精简：删除 Java/GraphQL 代码示例，Meter 命名提升为独立"命名契约"章节
- `ai-dev/design/nop-job/block-strategy-design.md` 精简：删除伪代码
- `ai-dev/analysis/2026-05-17-snail-job-vs-nop-job-comparison.md` 修正：阻塞策略从"❌需求设计"改为实际状态

Owner-doc decision: `AGENTS.md` 已更新（plans 路由、design 角色定义）。`ai-dev/logs/index.md` 待更新。

### 2026-05-17（上午）— Plan 15 实现

Plan 15 executorKind 统一路由实现完成，commit `9bbede81c`。

- ORM model 变更（`nop-job/model/nop-job.orm.xml`）：`job/executor-kind` dict 从 int→string（test/rpc/rpcBroadcast），删除 `NopJobSchedule.executorRef`，删除 `NopJobFire.executorSnapshot`，新增 `NopJobFire.executorKind` varchar 列
- 新增 `RpcBroadcastTaskBuilder`：通过 `@Inject @Nullable IDiscoveryClient` 获取实例列表，每个实例一个 task，payload 携带 targetHost/shardingIndex/shardingTotal
- `DefaultJobInvokerResolver` 简化：直接读 `fire.getExecutorKind()`，无 JSON 解析
- `DefaultJobExecutionContextBuilder`：从 taskPayload 读 targetHost，注入 `nop-svc-target-host` header
- Bean 注册：`nopJobTaskBuilder_rpcBroadcast`（engine.beans.xml）、`nopJobInvoker_rpcBroadcast`（service.beans.xml）
- 验证：`./mvnw test -pl nop-job` 通过，所有测试已更新为 string executorKind

### 2026-05-17（上午）— 分析

- 产出分析文档: `ai-dev/analysis/2026-05-17-snail-job-vs-nop-job-comparison.md`
- 关键发现：nop-job 触发器模型和 nop-task 编排能力优于 snail-job；阻塞策略 DISCARD/OVERLAY 已有实现；广播/分片已通过 RpcBroadcastTaskBuilder 实现
- 后续更新：分析文档 Gap 修正，design 文档新增
