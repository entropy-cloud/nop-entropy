# 2026-08-11 开发日志

## nop-datav Plan {1} Share/Export RBAC + Credential Protection：完成

### 2026-08-11（mission-driver EXECUTE：plan {1} 全 2 Phase）

执行 plan `ai-dev/plans/nop-datav/2026-08-10-2025-1-share-export-rbac-and-credential-protection.md` 全部 2 个 Phase，闭合 audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 Dim08-01 [P1]（Share/ExportTask action-auth 缺角色绑定，deny-by-default）与 Dim13-01 [P1]（passwordHash 经继承 CRUD 泄漏 + 水平越权）。

- **Plan Status**：从 active → **completed**。2 个 Phase 全部 completed；Closure Gates 全部 `[x]`。
- **新增能力**：action-auth.xml 角色绑定闭合 + passwordHash 从 GraphQL 出口完全移除（最严格不可读保护）+ RBAC 端到端测试覆盖放行/越权/回归三分支。

**Phase 1（passwordHash 不可见化 + createShare 脱敏）**：

- `nop-datav-meta/.../NopDatavDashboardShare/NopDatavDashboardShare.xmeta`（手写层）增 `<prop name="passwordHash" x:override="merge" published="false" queryable="false" sortable="false"/>`，平台机制：`ObjMetaToGraphQLDefinition` 读 `propMeta.isPublished()` 决定 GraphQL 出口。同仓先例：`NopAuthUser.password`、`NopAiModel.apiKey`。
- `NopDatavDashboardShareBizModel.createShare` 在 `return share;` 前 `share.setPasswordHash(null);`，与 `listShares`/`doToggleShare` 对齐（mask-on-return contract）。
- 新增 focused test `TestNopDatavSharePasswordHashMasking`（3 用例）：
  - `testCreateShareReturnsNullPasswordHash`：createShare 返回 `passwordHash == null`，DB 行持久化 BCrypt 哈希。
  - `testFindPageRejectsPasswordHashSelectionAndDefaultHasNoLeak`：显式选 `passwordHash` 触发 `nop.err.graphql.undefined-field`（schema 移除）+ 默认 findPage 输出无 passwordHash key（双保险）。
  - `testGetRejectsPasswordHashSelection`：get 显式选 passwordHash 同样触发 undefined-field。
- 更新既有 `TestNopDatavShareManagementBizModel.testCreateShareHashesPasswordAndNeverStoresPlain`、`TestNopDatavShareE2E.testFullChainOwnerPublishShareAnonymousAccess` 适配 mask-on-return 新契约（DB 行哈希校验保留）。
- 重生成 `_NopDatavDashboardShare.xmeta`（未手编生成物），并附加收益：`_NopDatavDashboardShare.view.xml` 自动剥离 passwordHash 列（admin UI 也隐藏）。
- owner-doc `permission-sharing-design.md` §223 增补裁定（继承 CRUD admin-only + published=false 共同闭合保护声明）。

**Phase 2（action-auth 角色绑定 + RBAC 端到端测试）**：

- **D1 偏离裁定**：`NopDatavDashboardShare` 与 `NopDatavExportTask` 的继承 CRUD `query`/`mutation` 仅绑 `admin`（**不**绑 `user`），对称。理由：闭合 §223 未覆盖的继承 CRUD 越权 + passwordHash 泄漏入口（share/export 无 RLS）。
- `nop-datav-web/.../auth/nop-datav.action-auth.xml` 新增两个 resource 块：
  - `NopDatavDashboardShare-main`（orderNo 10080-10083）：CRUD admin-only + 4 自定义 action（createShare/listShares/revokeShare/toggleShare）`admin,user`。
  - `NopDatavExportTask-main`（orderNo 10090-10093）：CRUD admin-only + 4 自定义 action（createExportTask/getExportTask/cancelExportTask/downloadExportFile）`admin,user`。
- 同步测试侧 `nop-datav-service/src/test/resources/_vfs/test/datav/auth/app.action-auth.xml`：增 `NopDatavDashboardShare-main` 块 + 修 `NopDatavExportTask-main` CRUD 为 admin-only（原为 admin,user）。
- 新增 focused test `TestNopDatavShareExportRbac`（8 用例，全部经 `graphQLEngine.newRpcContext` → `SiteCacheData.isPermitted` 完整链路，非 XML 文本断言）：
  - 正向：admin 调 listShares 放行；user（owner）调 listShares 放行；admin/user 调 getExportTask 放行（非 ERR_AUTH_NO_PERMISSION）。
  - 反向：user 调 Share findPage 拒绝（admin-only）；user 调 ExportTask findPage 拒绝；admin 调 Share findPage 放行（对照）。
  - 回归：`getSharedDashboard`（publicAccess=true）匿名放行未被破坏。
- owner-doc `permission-sharing-design.md` §49（CRUD 偏离裁定）+ §222 区增补（Share 自定义 action 角色表，对称 §370-377 ExportTask 表）+ §379（ExportTask CRUD admin-only）同步 live baseline。

**验证**：

- `./mvnw test -pl nop-datav/nop-datav-service -T 1C`：389 tests, 0 failures, 0 errors, 0 skipped。
- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- （nop-sys-dao 模块预先存在的 NOP_SYS_SEQUENCE 表错误与本 plan 无关，已确认无修改影响。）

**Anti-Hollow Check**：

- 角色绑定运行时生效（不只是 XML 存在）：`TestNopDatavShareExportRbac` 全部 8 用例经 graphQLEngine → SiteCacheData 完整链路验证。
- 脱敏运行时生效（不只是注释）：`TestNopDatavSharePasswordHashMasking` 全部 3 用例经 GraphQL engine 验证 schema 拒绝 + DB 哈希保留。

**后续**：

- audit `2026-08-10-1516-multi-audit-nop-datav.md` 整体保持 `Audit Status: planned`，等 plan {2}/{3} 完成后由后续轮 closure 一并关闭。
- 若后续需让 `user` 批量查看导出任务列表，应新增 owner-scoped 自定义 action（带 `createdBy` 过滤），而非放开继承 `findPage`。
