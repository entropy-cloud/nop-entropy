### nop-ai-agent IPostDenialGuard + PassThroughPostDenialGuard + FingerprintPostDenialGuard（Plan 178 — Phase 1 + Phase 2 + Closure）

- Plan: `ai-dev/plans/178-nop-ai-agent-post-denial-guard.md` → completed（Phase 1 + Phase 2 + Closure）
- Phase 1：IPostDenialGuard 契约 + DenialResult + DenialReason + DenialSuggestedStep + ActionFingerprint + PassThroughPostDenialGuard 默认 + FingerprintPostDenialGuard 功能化实现 + 单元测试
  - 新增 `DenialReason` 枚举（security 包）：4 个值（HUMAN_REJECTED / THRESHOLD_EXCEEDED / REPEATED_SAME_INTENT / TIMEOUT），收窄审计发现 L3-G1（reason 枚举缺少 TIMEOUT 无法区分超时与人类拒绝）
  - 新增 `DenialSuggestedStep` 枚举（security 包）：4 个值（REPLAN / ASK_USER / LOWER_PRIVILEGE / NARROWER_APPROVAL），对应合法 follow-up 策略
  - 新增 `DenialResult` 不可变值类型（security 包）：遵循 ApprovalDecision/MatrixDecision 的不可变值对象模式——工厂 `of(reason, suggestedNextStep, actionFingerprint, message, retryable)` + 便捷工厂 `repeatedSameIntent(fingerprint, message)`（reason=REPEATED_SAME_INTENT、suggestedNextStep=REPLAN、retryable=false）；reason/suggestedNextStep 非空校验；equals/hashCode 覆盖全部字段。边界清晰：与 IApprovalGate 的 ApprovalDecision（gate 自身决策）和 IDenialLedger 的 DenialRecord（ledger 记录结构）三者描述纵深防御链的不同阶段
  - 新增 `ActionFingerprint` 不可变值类型 + 计算工厂（security 包）：`compute(actionKind, arguments, workDir, criticalEnv)` → SHA-256(actionKind|canonicalArgs|workDir|canonicalEnv)[:32 hex]。canonical 序列化（TreeMap key 排序）保证确定性——相同输入总是产生相同指纹；参数 key 顺序不同但内容相同 → 相同指纹。null 安全（null → 空字符串）。固定 32 hex 字符长度。使用 HashHelper.sha256 + StringHelper.utf8Bytes/bytesToHex（复用 Nop 加密工具）
  - 新增 `IPostDenialGuard` 接口（security 包）：三方法——consultation `checkBeforeDispatch(sessionId, toolName, arguments, workDir) → DenialResult|null`（在 Layer 1 之前）、recording `recordDeniedAction(...)`（在每个 deny 之后）、reset `reset(sessionId)`。Javadoc 说明 default=PassThroughPostDenialGuard、dispatch-path 集成语义、线程安全契约、exact-fingerprint matching 策略
  - 新增 `PassThroughPostDenialGuard` 默认实现（security 包）：无状态单例，checkBeforeDispatch 永远返回 null（语义正确的"不阻止"），recordDeniedAction/reset 显式空方法体（带语义注释——非静默跳过，而是"无状态需要记录"）。遵循 AutoApproveGate/NoOpDenialLedger 先例
  - 新增 `FingerprintPostDenialGuard` 功能化实现（security 包）：`ConcurrentHashMap<String, Set<String>>` per-session 已拒绝 fingerprint 集合（value 用 ConcurrentHashMap.newKeySet() 线程安全 set）。checkBeforeDispatch：sessionId=null → 返回 null（匿名 session 不跟踪）；计算 fingerprint；在 session 集合中 → 返回 DenialResult.repeatedSameIntent；否则返回 null。recordDeniedAction：sessionId=null → no-op；否则 computeIfAbsent 加入集合。reset：移除 session 条目。线程安全——per-session set 独立。exact-fingerprint matching：参数变化自然产生不同 fingerprint = legitimate follow-up 自动放行
  - `DenialLayerSource` 枚举新增 `LAYER3_POST_DENIAL_GUARD` 值（第 6 个值），使 ledger 可区分 guard deny 与其他 layer deny
  - 测试 `TestDenialResult`（13）+ `TestActionFingerprint`（13）+ `TestPassThroughPostDenialGuard`（5）+ `TestFingerprintPostDenialGuard`（12）= 43 单元测试，全部通过
- Phase 2：引擎/Executor 接线 + Dispatch-loop pre-Layer-1 consultation + deny-path recording + 端到端验证 + 设计文档
  - `DefaultAgentEngine` 新增 `postDenialGuard` mutable field（默认 = `PassThroughPostDenialGuard.passThrough()`）+ `setPostDenialGuard`（null 回退默认）+ `getPostDenialGuard`，遵循 denialLedger/approvalGate 接线模式
  - `ReActAgentExecutor.Builder` 新增 `postDenialGuard` 接收方法；构造器对 null 设默认；`DefaultAgentEngine.resolveExecutor()` 将引擎自身的 guard 传递给 Builder
  - dispatch loop 在 toolName 提取后、`toolAccessChecker.checkAccess`（Layer 1）之前，新增 post-denial-guard consultation 步骤：若 guard 返回 DenialResult → 审计日志（matchedRule=`layer3_post_denial_guard`）+ TOOL_CALL_DENIED 事件（含 denialReason + suggestedNextStep）+ error response（message+suggestedNextStep）+ handleDenialAndCheckThreshold（layerSource=LAYER3_POST_DENIAL_GUARD）+ continue/break（与现有 deny 路径一致）；若返回 null → 继续 Layer 1
  - `handleDenialAndCheckThreshold` 签名扩展：新增 `chatToolCall` + `fingerprintWorkDir` 参数，方法体内首先调用 `postDenialGuard.recordDeniedAction(...)`（6 个 deny 点统一覆盖——5 个 Layer 1/2/3 + 1 个 guard consultation deny，形成闭环：guard deny 的 action 也被记录到 guard 自身）
  - 提取 `extractArguments(ChatToolCall)` + `resolveWorkDirString(AgentModel)` 辅助方法
  - 端到端测试 `TestDispatchPathPostDenialGuard`（7）：
    - **场景 A**（单次迭代盲重试阻止）：2 个相同 tool call → 第 1 个 guard consultation 放行 → Layer 1 deny → recording；第 2 个 guard consultation 命中（REPEATED_SAME_INTENT）→ Layer 1 **未被调用**（checkCount=1，证明 guard 在 Layer 1 之前拦截）
    - **场景 B**（跨迭代盲重试阻止）：2 次迭代各 1 个相同 tool call → 迭代 1 Layer 1 deny + recording；迭代 2 guard consultation 命中 → Layer 1 未被调用（checkCount<=1）
    - **场景 C**（参数变化放行——legitimate follow-up）：2 个 tool call 同 toolName 不同 arguments → 第 1 个 deny + recording；第 2 个 guard consultation 放行（不同 fingerprint）→ Layer 1 正常执行（checkCount=2）
    - **接线验证**：CountingFingerprintGuard 计数 consultCount/recordCount 断言 guard 方法在 dispatch loop 确实被调用（非仅组件被传递）
    - **向后兼容**：PassThroughPostDenialGuard 默认（不设 guard）→ Layer 1 对每个 tool call 正常执行（checkCount=2），0 spurious guard-deny
    - 引擎 set/get/null-fallback 接线 + Builder 默认构建不抛异常
  - 设计文档 `nop-ai-agent-security-and-permissions.md` §6.3 更新：L3-7 标记为"已落地"；记录八个架构决策（DenialResult 三类型边界 / consultation 点位于 Layer 1 之前 / recording 闭环包含 guard 自身 deny / exact-fingerprint matching / PassThrough 默认向后兼容 / Fingerprint 纯内存功能化实现 / follow-up 标签显式检测延期 / LAYER3_POST_DENIAL_GUARD 新增）
  - `IDenialLedger.java` + `IApprovalGate.java` Javadoc 更新：IPostDenialGuard 从 deferred 移除，标注为"landed"
- Closure：独立子 agent closure-audit——见 plan 文件 Closure 段落
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1144 tests, 0 failures, 0 errors（+50 新增 = Phase1 43 + Phase2 7；现有 1094 全部通过，0 spurious 拒绝）
- `node ai-dev/tools/check-plan-checklist.mjs ... --strict` → exit 0
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-ai/nop-ai-agent --severity high` → exit 0（新文件无 high/critical 发现）
- roadmap L3-7：❌ → ✅
- doc-sync 裁定：更新 `nop-ai-agent-security-and-permissions.md` §6.3（L3-7 落地状态 + 八个架构决策）+ `IDenialLedger.java`/`IApprovalGate.java` Javadoc（IPostDenialGuard 不再 deferred）



- Plan: `ai-dev/plans/177-nop-ai-agent-denial-ledger.md` → completed（Phase 1 + Phase 2 + Closure）
- Phase 1：IDenialLedger 契约 + DenialRecord + DenialRecordOutcome + DenialLayerSource + NoOpDenialLedger 默认实现 + 单元测试
  - 新增 `DenialLayerSource` 枚举（security 包）：5 个值（LAYER1_TOOL_ACCESS / LAYER1_PERMISSION / LAYER1_PATH_ACCESS / LAYER2_SECURITY_POLICY / LAYER3_APPROVAL_GATE），使每个 deny 来源可归属到 dispatch-path 层
  - 新增 `DenialRecord` 不可变值类型（security 包）：遵循 `AuditEvent`/`ApprovalDecision` 的不可变值对象模式——工厂 `of(sessionId, toolName, layerSource, reason, matchedRule, timestamp)` + equals/hashCode/toString；`layerSource` 非空校验（不允许静默跳过归属信息）
  - 新增 `DenialRecordOutcome` 不可变值类型（security 包）：`recordDenial` 的复合返回值——`count`（累计拒绝计数，记录本次后）+ `thresholdExceeded`（是否已达阈值）；负数 count 校验拒绝
  - 新增 `IDenialLedger` 接口（security 包）：四个方法（`recordDenial(DenialRecord) → DenialRecordOutcome` / `isPaused(sessionId) → boolean` / `getDenialCount(sessionId) → int` / `reset(sessionId) → void`）。Javadoc 说明 (a) default = NoOpDenialLedger；(b) dispatch-path 集成语义；(c) 线程安全契约（多 session 并发访问同一 ledger 实例，per-session 计数独立）；(d) 持久化不保证（收窄 L3-G5）
  - 新增 `NoOpDenialLedger` 默认实现（security 包）：无状态单例，天然线程安全。`recordDenial` 返回固定 `DenialRecordOutcome.of(0, false)`（语义正确的"不计数"结果，非吞异常或 continue）；`isPaused` 永远返回 false；`getDenialCount` 返回 0；`reset` 显式空方法体（带语义注释——非静默跳过，而是"无状态需要重置"）
  - 测试 `TestNoOpDenialLedger`（22）：NoOp 契约（recordDenial 返回 count=0/thresholdExceeded=false、isPaused=false、getDenialCount=0、reset 不抛、多次 recordDenial 仍不 paused、单例共享）+ DenialRecord/DenialRecordOutcome 值语义（工厂捕获所有字段、null layerSource 拒绝、null 可选字段允许、equals/hashCode、不同 layer/reason 不等、toString 可读）+ 功能化 InMemoryCountingLedger（threshold=2）证明契约非空壳（计数真实增长 + 阈值暂停 + per-session 独立 + reset 恢复）
- Phase 2：引擎/Executor 接线 + Dispatch-loop 拒绝记录 + 阈值暂停 + 端到端验证 + 设计文档
  - 新增 `AgentExecStatus.paused` 枚举值：语义独立（治理策略自动触发，vs cancelled/forced_stopped/escalated），向后兼容（plan 157 先例：dict 引用对新值向后兼容）
  - 新增 `AgentEventType.SESSION_PAUSED` 枚举值：语义精确（session 因治理策略暂停），13 个现有值无一可替代
  - `DefaultAgentEngine` 新增 `denialLedger` mutable field（默认 = `NoOpDenialLedger.noOp()`）+ `setDenialLedger`（null 回退默认）+ `getDenialLedger`，遵循 `approvalGate`/`permissionMatrix`/`securityLevelResolver` 接线模式
  - `ReActAgentExecutor.Builder` 新增 `denialLedger` 接收方法；构造器对 null 设默认（`NoOpDenialLedger`）；`DefaultAgentEngine.resolveExecutor()` 将引擎自身的 ledger 传递给 Builder
  - 提取 `handleDenialAndCheckThreshold(sessionId, toolName, layerSource, reason, matchedRule, ctx, agentName)` helper：封装 `recordDenial` → 阈值检查 → 暂停状态标记 + 审计日志（matchedRule=`layer3_denial_ledger`）+ SESSION_PAUSED 事件。5 个 deny 路径统一调用，DRY
  - dispatch loop 全部 5 个 deny 路径（L1 tool access / L1 permission / L1 path access / L2 security policy / L3 approval gate）在现有审计+事件+error response 之后、`continue` 之前调用 `handleDenialAndCheckThreshold`；若返回 shouldAbort=true → `break dispatchLoop`
  - dispatch for-loop 后、`allowedCalls` 执行前：检查 `ctx.getStatus() == paused` → 跳过 allowedCalls 执行 + `continue reactLoop`（**不** break reactLoop——两个机制职责分离：Mechanism 1 跳过当前迭代剩余执行，Mechanism 2 中止 ReAct 循环）
  - ReAct 循环迭代开始（`cancelRequested` 之后、`shouldForceStop` 之前）：检查 `denialLedger.isPaused(sessionId)` → `handleSessionPaused` + `break reactLoop`（唯一的 reactLoop-breaking 机制——position 选择理由：cancelRequested 优先 > pause 治理决策 > force_stop 系统决策）
  - post-loop bookkeeping 排除 `paused`：暂停 session 不发布 `EXECUTION_COMPLETED`、不执行 `POST_CALL` hooks
  - 端到端测试 `TestDispatchPathDenialLedger`（8）：
    - **场景 A**：单次迭代多 tool call deny（threshold=2）→ 第 2 次 deny 后 dispatch for-loop break、session=paused、SESSION_PAUSED 发布、EXECUTION_COMPLETED 不发布、第 2 个 tool call 未执行
    - **场景 B**：跨迭代单 tool call deny（threshold=2）→ 迭代 3 开始 isPaused=true → break reactLoop；LLM 恰好被调用 2 次（3 次迭代但第 3 次开始即中止）
    - **接线验证**：CountingLedger.recordCount 断言 `recordDenial` 在 dispatch loop deny 路径确实被调用（非仅组件被传递）
    - **post-loop 排除验证**：paused session 不发布 EXECUTION_COMPLETED
    - **向后兼容**：NoOpDenialLedger 默认 → 即使 DenyAllTools，session 不被 paused
    - Builder 默认（未设 ledger）构建不抛异常 + 引擎 set/get/null-fallback 接线 + per-session 计数独立
  - 设计文档 `nop-ai-agent-security-and-permissions.md` §6.2 更新：L3-6 标记为"已落地"；记录六个架构决策：(1) 接口契约不强制持久化（收窄 L3-G5）；(2) 全部 5 个 deny 点记录到 ledger；(3) 阈值检查双重机制；(4) paused 语义独立；(5) NoOp 默认保证向后兼容；(6) sticky pause 延期至 successor
  - `IApprovalGate.java` Javadoc 更新：IDenialLedger 从 deferred 移除，标注为"landed"
- Closure：独立子 agent closure-audit（task_id ses_13ac6d10cffeQhsOw8sipsBeh8）PASS——全部 18 项验证项通过 + Anti-Hollow 检查通过（recordDenial 在全部 5 个 deny 路径确实调用、阈值暂停链完整、场景 A+B 测试通过、post-loop 正确排除 paused）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1094 tests, 0 failures, 0 errors（+30 新增 = Phase1 22 + Phase2 8；现有全部测试通过，0 spurious 暂停）
- roadmap L3-6：❌ → ✅
- doc-sync 裁定：更新 `nop-ai-agent-security-and-permissions.md` §6.2（L3-6 落地状态 + 六个架构决策）+ `IApprovalGate.java` Javadoc（IDenialLedger 不再 deferred）



- Plan: `ai-dev/plans/176-nop-ai-agent-approval-gate.md` → completed（Phase 2 完成 + Closure）
- Phase 2：引擎/Executor 接线 + Dispatch-loop Layer 3 咨询点 + 端到端验证 + 设计文档
  - `DefaultAgentEngine` 新增 `approvalGate` mutable field（默认 = `AutoApproveGate.autoApprove()`）+ `setApprovalGate`（null 回退默认）+ `getApprovalGate`，遵循 `permissionMatrix`/`securityLevelResolver`/`levelHintsProducer` 接线模式
  - `ReActAgentExecutor.Builder` 新增 `approvalGate` 接收方法；构造器对 null 设默认（`AutoApproveGate`）；`DefaultAgentEngine.resolveExecutor()` 将引擎自身的 gate 传递给 Builder（咨询链从引擎 → Builder → dispatch loop 完整接通）
  - 重构 `checkLayer2Consultation` 返回值：从 `String`（deny reason / null）改为私有 `SecurityConsultationOutcome`（resolvedLevel + denialReason），使 Layer 3 咨询可复用已解析的 `SecurityLevel`，避免重复 resolve（Closure Gate 要求）
  - dispatch loop 插入 Layer 3 consultation 步骤（Layer 2 matrix 放行之后、`allowedCalls.add` 之前）：`checkLayer3Approval` helper 调用 `approvalGate.requestApproval(level, toolName, channel, principal, sessionId, agentName)`；deny 时记录 `AuditEvent`(DENY + reason + matchedRule=`layer3_approval_gate` + denialKind) + 发布 `TOOL_CALL_DENIED` 事件 + 返回 error response（与 Layer 1/2 deny 路径一致）；approve 时加入 allowedCalls
  - 端到端测试 `TestDispatchPathApprovalGate`（7）：
    - ELEVATED（shell.exec）+ deny-gate → blocked（验证 'Approval denied' error response）
    - RESTRICTED（network.fetch）+ deny-gate → blocked（timeout kind）
    - STANDARD（fs.read）+ functional gate → approved（无 denial）
    - **审计验证**：Builder 注入 CollectingAuditLogger，断言 `layer3_approval_gate` matched rule 出现 + PassThrough matrix 不产生 layer2 denial
    - **接线验证**：CountingGate 用 AtomicInteger 计数器断言 `requestApproval()` 在 dispatch loop 中确实被调用（非仅组件被传递）
    - **向后兼容**：AutoApproveGate 默认 → GROUP channel ELEVATED shell.exec 不被拒绝（0 spurious denial）
    - Builder 默认（未设 gate）构建不抛异常 + 引擎 set/get/null-fallback 接线
  - 设计文档 `nop-ai-agent-security-and-permissions.md` §6.1 更新：L3-5 标记为"已落地（plan 176 ✅）"；记录四个架构决策：(1) ApprovalDecision 作为 IApprovalGate 专用返回类型，与 L3-7 DenialResult 边界清晰；(2) consultation 点位于 Layer 2 matrix 放行之后，deny 路径与 Layer 1/2 一致；(3) AutoApproveGate shipped 默认保证向后兼容；(4) denial reason 区分 human_rejected / timeout（收窄 L3-G1）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1064 tests, 0 failures, 0 errors（+32 新增 = Phase1 25 + Phase2 7；现有全部测试通过，0 spurious denial）
- doc-sync 裁定：更新 `nop-ai-agent-security-and-permissions.md` §6.1（L3-5 落地状态 + 四个架构决策）

### nop-ai-agent IApprovalGate + AutoApproveGate（Plan 176 — Phase 1）

- Plan: `ai-dev/plans/176-nop-ai-agent-approval-gate.md` → in progress（Phase 1 完成）
- Phase 1：IApprovalGate 契约 + ApprovalDecision 值类型 + AutoApproveGate 默认实现 + 单元测试
  - 新增 `ApprovalDenialKind` 枚举（security 包）：HUMAN_REJECTED / TIMEOUT / OTHER，收窄审计发现 L3-G1（审批超时 vs 人类拒绝需可区分）到审批门自身语义
  - 新增 `ApprovalDecision` 值类型（security 包）：不可变，遵循 `MatrixDecision`/`ToolAccessResult` 的 deny-with-reason 模式。approved 携带 approver 标识（null 默认 "system"）；denied 携带 `ApprovalDenialKind` + reason。工厂：`approve(approver)` / `denyHumanRejected(reason)` / `denyTimeout(reason)` / `deny(kind, reason)`（null kind → OTHER）。equals/hashCode/toString 一致
  - 新增 `IApprovalGate` 接口（security 包）：`requestApproval(level, toolName, channel, principal, sessionId, agentName) → ApprovalDecision`，契约风格遵循 `IPermissionMatrix`/`ISecurityLevelResolver`。Javadoc 说明 Layer 3 纵深防御位置（设计 §8：位于 ISecurityLevelResolver 之后、IDenialLedger/IPostDenialGuard/ISandboxBackend 之前）+ dispatch-path 咨询语义 + AutoApproveGate shipped 默认
  - 新增 `AutoApproveGate` 默认实现（security 包）：单例，对所有输入返回 `approve("auto")`。shipped 默认注入引擎——无人值守 Layer 1 自动化不受影响
  - 测试 `TestAutoApproveGate`（7：3 等级全 approve / approver="auto" / null channel+principal 不抛 / null toolName 不抛 / 单例共享 / approved 不携带 denial 字段 / 功能化 gate deny 可观测）+ `TestApprovalDecision`（18：approve 携带 approver / null approver→system / denyHumanRejected / denyTimeout / deny OTHER / null kind→OTHER / HUMAN_REJECTED vs TIMEOUT 可区分 / equals-hashCode 一致 / toString 可读）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 全部通过（+25 新增；现有测试无回归）
- doc-sync 裁定：No owner-doc update required（设计文档 §6.1 更新在 Phase 2）

### nop-ai-agent Dispatch-Path Security Consultation（Plan 175 — Phase 3 + Closure）

- Plan: `ai-dev/plans/175-nop-ai-agent-dispatch-path-security-consultation.md` → completed（Phase 3 完成 + Closure）
- Phase 3：Consultation 点插入 + Executor 接线 + 端到端验证 + 设计文档
  - `ReActAgentExecutor.Builder` 新增 `securityLevelResolver` / `permissionMatrix` / `levelHintsProducer` 接收方法；build() 对 null 设默认（NoOp / PassThrough / DefaultLevelHintsProducer）
  - `DefaultAgentEngine.resolveExecutor()` 将引擎自身的三个组件传递给 Builder（consultation 链从引擎 → Builder → dispatch loop 完整接通）
  - dispatch loop 插入 Layer 2 consultation 步骤（Layer 1 检查之后、`allowedCalls.add` 之前）：`checkLayer2Consultation` helper 产生 hints → `resolver.resolve(toolName, hints)` → 读 `ctx.channelKind`/`principal` → `matrix.check(channel, principal, level)`；deny 时记录 `AuditEvent`(DENY + reason + matchedRule=`layer2_permission_matrix`) + 发布 `TOOL_CALL_DENIED` 事件 + 返回 error response（与 Layer 1 deny 路径一致）；allow 时加入 allowedCalls
  - 端到端测试 `TestDispatchPathSecurityConsultation`（5）：
    - GROUP channel + shell.exec（highImpact→RESTRICTED）→ deny（验证 'Security policy denied' error response 出现）
    - WEBUI channel + shell.exec（RESTRICTED）→ allow（无 denial）
    - **接线验证**：CountingResolver/CountingMatrix 用 AtomicInteger 计数器断言 `resolve()` 和 `check()` 在 dispatch loop 中确实被调用（非仅组件被传递）
    - **向后兼容**：NoOp/PassThrough 默认 → GROUP channel shell.exec 不被拒绝（0 spurious denial）
    - GROUP + STANDARD（fs.read）→ allow；Builder 默认（未设 resolver/matrix）构建不抛异常
  - 设计文档 `nop-ai-agent-security-and-permissions.md` §5.1/§5.3 更新：dispatch-path 咨询从"deferred successor"标记为"已接通（plan 175 ✅）"；记录三个架构决策：(1) channel/principal 经 AgentMessageRequest → AgentExecutionContext 传播（可选字段，null=未知/匿名）；(2) LevelHints 生产组件作为独立可插拔组件（接口 + 功能化默认），trustedSource/writesOutsideWorkspace 有真实评估，crossesTrustBoundary 保守 false；(3) consultation 点位于 Layer 1 之后，deny 路径与 Layer 1 一致（审计 + 事件 + error response）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1032 tests, 0 failures, 0 errors（+5 新增；现有全部测试通过，0 spurious denial）
- doc-sync 裁定：更新 `nop-ai-agent-security-and-permissions.md` §5.1/§5.3（dispatch-path 咨询状态 + 三个架构决策）

### nop-ai-agent Dispatch-Path Security Consultation（Plan 175 — Phase 2）

- Plan: `ai-dev/plans/175-nop-ai-agent-dispatch-path-security-consultation.md` → in progress（Phase 2 完成）
- Phase 2：LevelHints 生产组件 + 引擎接线 + 单元测试
  - 新增 `ToolPathArgKeys`（security 包）：共享 path-arg key 集合（path/file/filePath/...），producer 与 executor 共用，消除 `ReActAgentExecutor.PATH_ARG_KEYS` 私有副本的重复（executor 改为引用 `ToolPathArgKeys.KEYS`）
  - 新增 `ILevelHintsProducer` 接口（security 包）：`produce(toolName, arguments, workDir, ctx) → LevelHints`，契约风格遵循 `IContentTrustEvaluator`/`ISecurityLevelResolver`
  - 新增 `DefaultLevelHintsProducer` 功能化默认实现（非全 false 空壳）：
    - `trustedSource`：经 `IContentTrustEvaluator` 评估 `AGENT_GENERATED` origin（agent 自身推理链 → 默认 trusted=true，可经自定义 evaluator 覆盖）
    - `writesOutsideWorkspace`：经 `ToolPathArgKeys` 提取 path 值，相对路径解析到 workDir（workDir=null 用 JVM CWD），任一路径在 workspace 外 → true；workDir 本身不算 outside；非 string/空白值忽略
    - `needsNetwork`：tool-name 分类（web_fetch/http_request/network_fetch/web_search/curl/http.get/http.post）→ true（`_`→`.` + lowercase 归一化匹配）
    - `highImpact`：tool-name 分类（shell_exec/code_exec/bash/sh/file_delete/rm/rmdir/git_push/git_reset/db_drop/system）→ true
    - `crossesTrustBoundary`：保守 false（无可靠运行时启发式）
    - 鲁棒性：null toolName/arguments/workDir/ctx 不抛异常，返回保守 hints
  - `DefaultAgentEngine` 新增 `levelHintsProducer` mutable field（默认 = `DefaultLevelHintsProducer`）+ `setLevelHintsProducer`（null 回退默认）+ `getLevelHintsProducer`，遵循 `permissionMatrix`/`securityLevelResolver` 模式
  - 测试：`TestDefaultLevelHintsProducer`（21：read-in-workspace/write-outside/relative-inside/shell/bash/web.fetch/http_request/hints 区分 Anti-Hollow/underscore-dot 归一化/null toolName/null args/empty args/null workDir relative/null workDir absolute/non-string arg/blank arg/null evaluator 回退/produce-never-null/custom evaluator 覆盖/toString/workDir-itself-not-outside）+ `TestDefaultAgentEngineLevelHintsProducerWiring`（5：set/get 同实例/null 回退/默认实例/默认功能性区分/构造路径不变）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1027 tests, 0 failures, 0 errors（+26 新增；executor PATH_ARG_KEYS → ToolPathArgKeys.KEYS 重构未破坏 path-access 测试）
- doc-sync 裁定：No owner-doc update required（设计文档 §5.1/§5.3 更新在 Phase 3）

### nop-ai-agent Dispatch-Path Security Consultation（Plan 175 — Phase 1）

- Plan: `ai-dev/plans/175-nop-ai-agent-dispatch-path-security-consultation.md` → in progress（Phase 1 完成）
- Work Item: dispatch-path-consultation（接通 Layer 2 `ISecurityLevelResolver` + `IPermissionMatrix` 咨询到 ReAct/tool-dispatch 路径的"最后一公里"）
- Phase 1：Channel/Principal 字段 + Context 传播 + 单元测试
  - `AgentMessageRequest` 新增 `channelKind`（ChannelKind）+ `principal`（Principal）final 字段 + 6-arg 构造器；现有 4-arg / 2-arg 构造器委托 null（向后兼容，所有现有调用方零影响）
  - `AgentExecutionContext` 新增 `channelKind` + `principal` mutable 字段（getter + setter），默认 null（未知通道/匿名身份），遵循现有 sessionId/chatOptionsModel 的 mutable-field 模式
  - `DefaultAgentEngine.doExecute()` 在 `ctx.getMetadata().putAll(...)` 之后传播 request 的 channel/principal 到 context（null 输入设为 null，语义正确的"未知"）
  - 测试：`TestChannelPrincipalPropagation`（9：request 构造器委托 + context getter/setter/null + **doExecute 传播接线验证**——CapturingEngine 子类 override resolveExecutor 捕获 ctx，验证带 GROUP channel + Principal.user() 的 request 经 doExecute 后 ctx 字段一致；不带 channel/principal 的 request → ctx 字段 null）
  - 偏差：`test-single-turn-agent.agent.xml` 因 agent.xdef mode enum 校验（`single-turn` 不在 `react,plan,single-turn|react` 选项）无法经 `loadAgentModel` 加载 → 改用 `test-react-agent`（react 模式，加载正常）+ mock toolManager(loadTool→null) + mock chatService 返回无 tool-call 的 final 消息，ReAct 循环 1 次迭代完成
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1001 tests, 0 failures, 0 errors（全部现有 + 新增测试通过，向后兼容）
- doc-sync 裁定：No owner-doc update required（设计文档 §5.1/§5.3 更新在 Phase 3）

### nop-ai-agent Per-Agent Glob Path-Rules（Plan 174 — Phase 1 + 2 + 3 + Closure）

- Plan: `ai-dev/plans/174-nop-ai-agent-per-agent-path-rules.md` → completed（Phase 1 + 2 + 3 + Closure 全部完成）
- Work Item: sec-4.3 / L4-1e（per-agent glob allow/deny path-rules: `PathRuleModel` + `PathAccessDecision` + `RuleBasedPathAccessChecker` + cross-level deny-wins inheritance）
- Design contract: `nop-ai-agent-security-and-permissions.md` §4.3（路径访问检查 — glob 路径规则）+ §4.4（路径规则继承）
- Phase 1：path-rule model + DSL + `RuleBasedPathAccessChecker`
  - `agent.xdef` 新增 `<path-rules>` 元素（`xdef:body-type="list"`，子元素 `<rule xdef:name="PathRuleModel" pattern="!string" access="string"/>`）— codegen 生成 `_gen/_PathRuleModel.java`（pattern + access String 字段）并重新生成 `_gen/_AgentModel.java`（`getPathRules(): List<PathRuleModel>`，默认空列表，never null）
  - 新增 `PathAccessDecision` 枚举（ALLOW/DENY）+ `fromString()` fail-closed 解析（null/未识别 → DENY）
  - 新增 `PathRuleModel`（extends `_gen/_PathRuleModel`，增加 `getAccessDecision()` helper）
  - 新增 `RuleBasedPathAccessChecker implements IPathAccessChecker`：first-match-wins within-agent 评估，DENY → `PathAccessResult.deny(reason, "agent_path_rule_deny")`，ALLOW/no-match → 委托全局 checker；`**`-leading 模式 form 调整（workDir-independent）；空规则列表 → 纯 pass-through；null 构造器参数 → `IllegalArgumentException`
  - 测试：`TestRuleBasedPathAccessChecker`（22）、`TestPathAccessDecision`（5）
  - 偏差：javadoc 中 glob 模式 `/**/secrets/**` 的 `*/` 序列会提前关闭 javadoc 注释块 → 使用 HTML 实体 `&#42;&#42;` 转义
- Phase 2：引擎接线 + per-agent path checker 解析
  - `DefaultAgentEngine.doExecute()` 新增 `resolvePerAgentPathChecker(agentModel)`：声明了 `<path-rules>` → `RuleBasedPathAccessChecker(rules, globalChecker)`；未声明 → 全局 checker 不变（向后兼容）
  - `resolveEffectivePathAccessChecker()` 扩展为两参数版本（接受 per-agent base checker）；单参数版本向后兼容委托
  - 组合顺序：全局 deny-list（最内层）→ per-agent 规则 → 父约束（最外层）
  - 测试：`TestPerAgentPathRulesWiring`（7：resolvePerAgentPathChecker 返回类型 + ReAct 循环 DENY/ALLOW/no-match 强制 + 向后兼容 + 与父约束组合）
- Phase 3：继承 + clamping + 端到端 + 文档更新
  - `ParentPermissionConstraint` 扩展 additive `allowedPathRules` 字段（`List<PathRuleModel>`，null=ABSENT）；现有构造器委托 null（向后兼容）；`hasPathRules()` + equals/hashCode/toString 更新
  - `ParentConstrainedPathAccessChecker` 扩展：root-check 后增加 rule-check（deny-wins 跨层语义：任何父 DENY 匹配 → 拒绝 `parent_path_rule_deny`）；root + rule 双维度同时 PRESENT 时两者都检查（fail-closed）
  - `ReActAgentExecutor.computeEffectivePathRules()`：accumulated chain = incoming parent rules + own rules；传入 `AgentToolExecuteContext`（14-arg 构造器，向后兼容）
  - `CallAgentExecutor.buildParentConstraint()` 读取 `allowedPathRules` 并纳入约束
  - `DefaultAgentEngine.resolveEffectivePathAccessChecker()` 当 `hasPathRoots() || hasPathRules()` 时包装
  - 测试：`TestPathRulesInheritanceEndToEnd`（2：rule inheritance 跨层 deny-wins + nested delegation accumulation A→B→C）；`TestParentConstrainedPathAccessChecker` 新增 7 个 path-rules 测试
  - 设计文档 §4.3 + §4.4 + "glob 规则" 更正段全部更新；roadmap 新增 L4-1e ✅
- 偏差：xdef `access="enum:allow,deny|deny"` 的 `|` 被解析为选项的一部分而非默认值分隔符 → 改为 `access="string"` 依赖运行时 `PathAccessDecision.fromString()` 验证（fail-closed DENY 默认）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` — 992 tests, 0 failures, BUILD SUCCESS
- Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-security-and-permissions.md` §4.3 + §4.4 更新；`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` L4-1e 新增 ✅

### nop-ai-agent Security Level Resolver（Plan 173 — Phase 1 + 2 + Closure）

- Plan: `ai-dev/plans/173-nop-ai-agent-security-level-resolver.md` → completed（Phase 1 + 2 + Closure 全部完成）
- Work Item: L2-13（`ISecurityLevelResolver` 接口 + `NoOpSecurityLevelResolver` pass-through 默认 + `LevelHints` 值类型 + 引擎接线）
- Design contract: `nop-ai-agent-security-and-permissions.md` §5.1（安全等级解析）
- Phase 1：值类型 + 接口 + pass-through 默认 + 单元测试
  - 新增 `LevelHints` 不可变值对象（trustedSource / writesOutsideWorkspace / crossesTrustBoundary / needsNetwork / highImpact，`defaults()` 工厂全 false）— 遵循 `Principal` 值对象先例
  - 新增 `ISecurityLevelResolver` 接口：`resolve(String actionKind, LevelHints hints) → SecurityLevel`，复用 plan 172 的 `SecurityLevel` 枚举（非重复定义）
  - 新增 `NoOpSecurityLevelResolver`（final singleton，`noOp()` 工厂，所有 action kind + hints → STANDARD）— 遵循 `NoOpContentGuardrail` / `PassThroughPermissionMatrix` 先例
  - 测试：`TestLevelHints`（5）、`TestNoOpSecurityLevelResolver`（11，含全 action kind × 32 hint 组合 STANDARD + 自定义 `DesignSpecRuleTableResolver` 内部类实现设计 §5.1 规则表，证明契约可做出真实 STANDARD/ELEVATED/RESTRICTED 三级决策 — Anti-Hollow check）
- Phase 2：引擎接线 + 接线验证 + 设计文档 + roadmap
  - `DefaultAgentEngine` 新增 `securityLevelResolver` 字段（默认 `NoOpSecurityLevelResolver.noOp()`）+ `setSecurityLevelResolver`（null-check + NoOp 回退）+ `getSecurityLevelResolver`，遵循 `setPermissionMatrix` 模式，不修改构造器链
  - 测试：`TestDefaultAgentEngineSecurityLevelResolverWiring`（5 tests：set/get 同一性、null 回退、默认 NoOp singleton、全 action kind STANDARD 无 spurious classification、现有构造器路径不变）
  - 设计文档 `nop-ai-agent-security-and-permissions.md` §5.1 更新：契约已落地 + LevelHints 归属决策（resolver 契约需要它作为输入参数）+ SecurityLevel 复用记录（消费者先于生产者）+ dispatch-path 咨询 deferral 记录
  - roadmap `nop-ai-agent-roadmap.md` L2-13 ❌ → ✅
- 偏差：Phase 1 首次测试运行发现 `TestNoOpSecurityLevelResolver` 有 4 个 assertion 失败——根因是测试数据中将 `LevelHints.defaults()`（trustedSource=false）误用为"可信源"场景。`defaults()` 全 false 意味着 `!trustedSource=true`，对 shell.exec/network.fetch/unknown 触发升级是规则表的正确行为。修正测试数据为显式 `new LevelHints(true, ...)` 表示可信源后全部通过。源码 `NoOpSecurityLevelResolver` / `LevelHints` / `ISecurityLevelResolver` 无需修改。
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS
- Closure：独立子 agent closure-audit（task `ses_13bcbc4ebffeCu7U3ZVomnrnY1`）8 项全部 PASS；`check-plan-checklist.mjs --strict` exit 0
- Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-security-and-permissions.md` §5.1 已更新；`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` L2-13 已更新

### nop-ai-agent Permission Matrix（Plan 172 — Phase 1 + 2 + Closure）

- Plan: `ai-dev/plans/172-nop-ai-agent-permission-matrix.md` → completed（Phase 1 + 2 + Closure 全部完成）
- Work Item: L2-14（`IPermissionMatrix` 接口 + `PassThroughPermissionMatrix` pass-through 默认 + 引擎接线）
- Design contract: `nop-ai-agent-security-and-permissions.md` §5.3（通道权限矩阵）
- Phase 1：值类型 + 接口 + pass-through 默认 + 单元测试
  - 新增 `SecurityLevel` 枚举（STANDARD/ELEVATED/RESTRICTED）、`ChannelKind` 枚举（WEBUI/API/DM/GROUP）、`PrincipalRole` 枚举（USER/OPERATOR）— 共享值类型，位于 `io.nop.ai.agent.security` 包
  - 新增 `Principal` 不可变值对象（role/channelId/tenantId，含 `user()` / `operator()` 工厂）
  - 新增 `MatrixDecision` 决策结果值类型（allow/deny + reason + channel/level 审计上下文，遵循 `ToolAccessResult`/`PathAccessResult` 的 deny-with-reason 模式）
  - 新增 `IPermissionMatrix` 接口：`check(ChannelKind, Principal, SecurityLevel) → MatrixDecision`
  - 新增 `PassThroughPermissionMatrix`（final singleton，`passThrough()` 工厂，所有输入 allow）— 遵循 `NoOpContentGuardrail` / `PassThroughModelRouter` 先例
  - 测试：`TestSecurityLevel`（3）、`TestChannelKind`（2）、`TestPrincipal`（5）、`TestPassThroughPermissionMatrix`（13，含全组合 allow + 自定义限制性矩阵 `DesignSpecRestrictiveMatrix` 内部类证明契约可做出真实 allow/deny 决策 — Anti-Hollow check）
- Phase 2：引擎接线 + 接线验证 + 设计文档 + roadmap
  - `DefaultAgentEngine` 新增 `permissionMatrix` 字段（默认 `PassThroughPermissionMatrix.passThrough()`）+ `setPermissionMatrix`（null-check + PassThrough 回退）+ `getPermissionMatrix`，遵循 `setMessenger`/`setSkillProvider` 模式，不修改构造器链
  - 测试：`TestDefaultAgentEnginePermissionMatrixWiring`（5 tests：set/get 同一性、null 回退、默认 PassThrough singleton、全组合 allow 无 spurious deny、现有构造器路径不变）
  - 设计文档 `nop-ai-agent-security-and-permissions.md` §5.3 更新：契约已落地 + SecurityLevel 归属决策（消费者先于生产者，L2-13 复用）+ dispatch-path 咨询 L2-13 依赖记录
  - roadmap `nop-ai-agent-roadmap.md` L2-14 ❌ → ✅
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全通过（928 tests, 0 failures, 0 errors）
- Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-security-and-permissions.md` §5.3 已更新；`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` L2-14 已更新

### nop-ai-agent DB-Backed Messenger（Plan 171 — Phase 1 + 2 + 3 + Closure）

- Plan: `ai-dev/plans/171-nop-ai-agent-db-backed-messenger.md` → completed（Phase 1 + 2 + 3 + Closure 全部完成）
- Work Item: L4-2（`DBMessageService` — 跨进程路由的 DB-backed `IMessageService` 实现）
- Design contract: architecture-baseline §六 — "多实例：DB-backed `MessageService`，请求/结果通过数据库传递"
- Phase 1：nop-dao 依赖 + ORM 模型 + 序列化辅助
  - `pom.xml` 新增 `nop-dao` 依赖（main scope）
  - 新增 ORM 模型 `_vfs/nop/ai/agent/orm/app.orm.xml`（`ai_agent_message` 表：SID, TOPIC, MESSAGE_BODY, STATUS, CONSUMER_ID, CREATED_AT, CLAIMED_AT, CONSUMED_AT）
  - 新增 `AiAgentMessageTable.java`（DDL 常量 + 状态枚举：PENDING=0, CLAIMED=10, CONSUMED=20）
  - 新增 `AgentMessageEnvelopeJson.java`（envelope ↔ JSON 序列化辅助，payload 类型保留 + 往返还原）
  - 测试：`TestAgentMessageEnvelopeJson`（11 tests）、`TestAiAgentMessageTable`（6 tests）
- Phase 2：DBMessageService 实现 + 单元测试
  - 新增 `DBMessageService implements IMessageService, AutoCloseable`（`io.nop.ai.agent.message` 包）
  - send 持久化（INSERT 到 `ai_agent_message` 表，STATUS=PENDING）；后台轮询投递；原子 claim（UPDATE WHERE STATUS=PENDING）防竞争重复
  - consumer 返回值处理：null → consumed；ConsumeLater → 保持 pending 重试；CompletionStage → 等待后处理；非 null → consumed（不触发 ack-topic 路由）
  - `IMessageConsumeContext` 实现 `DbConsumeContext`（send/sendAsync 路由回同一 DBMessageService）
  - 生命周期：`start()` 建表 + 启动轮询线程；`close()` 终止轮询 + 释放线程
  - 测试：`TestDBMessageService`（8 tests：send 持久化、poll 投递、competing consumers、消息持久化跨重启、ConsumeLater 重试、subscription cancel、close 停轮询、context.send 响应路由）
- Phase 3：跨实例端到端 + 设计文档更新
  - 测试：`TestDBMessageServiceCrossInstance`（4 tests：跨实例 send-async、跨实例 request-response via LocalAgentMessenger、request 超时、向后兼容 NoOp fail-fast）
  - 设计文档 `01-architecture-baseline.md` §六：DB-backed 从"设计目标"改为"已落地 L4-2"；记录故障语义（至少一次投递、JVM 崩溃不丢消息、payload JSON 序列化约束）
  - Roadmap `nop-ai-agent-roadmap.md` L4-2：❌ → ✅
  - `nop-ai-agent-actor-runtime-vision.md` §4.2 MessageRouter 行：注明 DB-backed 传输已可作为多实例路由基底
- Closure：`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-ai-agent --severity high` exit 0；`./mvnw test -pl nop-ai/nop-ai-agent` → 900 tests, 0 failures


### 修复 TestJobConcurrency flaky test（全量并行 -T 1C 偶发失败）

- 现象：全量 `./mvnw test -T 1C` 时 `nop-job-coordinator` 的 `TestJobConcurrency.testFullPipelineTwoCoordinators` 偶发失败（`expected:<3> but was:<4>`），单独运行稳定通过 → flaky。该模块本次无产品代码改动（预先存在的 flaky）。
- 根因（trigger 时序边界，非乐观锁问题）：`TestJobConcurrency.newSchedule` 未设 `lastFireTime` → `TriggerSpecHelper.toEvalContext` 的 `getLastScheduledTime()` 返回 0 → `PeriodicTrigger.nextScheduleTime`（fixed-rate 分支，`PeriodicTrigger.java:66`）以 epoch(0) 为起点、按 `period=1000ms` 对齐到整秒点，返回 `>= now` 的下一个整秒。plannerA 顺序处理 3 个 schedule 调用 `calculateNextFireTime`（afterTime=`scheduleStore.getCurrentTime()` 实时钟），若处理跨越整秒边界，不同 schedule 推进后的 nextFireTime 不同（S vs S+1000）；plannerB `fetchDueSchedules` 时 nextFireTime=S 的 schedule 仍 `<= now_B` 被重复 fire → 4 fire（期望 3）。高负载并行下处理耗时才使 plannerB 跨越整秒边界，故单独运行稳定、并行偶发。
- 修复（测试层，让测试专注验证乐观锁竞争、脱离 trigger 时序边界）：`newSchedule` 增大 `repeatIntervalMs` `1000→60000` 并设 `lastFireTime=now-61000`，使 plannerA 推进后 nextFireTime=`now+59000`（远在未来），plannerB 永不重复 fire。所有用 `newSchedule` 的测试均不依赖 interval 具体值或周期触发次数，增大 interval 安全。
- 验证：单独运行 TestJobConcurrency 5 次全绿（6 tests, 0 failures，耗时从失败时 13.57s 降至 ~1.5s）；全量 `./mvnw test -T 1C` BUILD SUCCESS（所有模块含之前因失败被 SKIPPED 的 nop-cli-core/nop-retry-web/app/nop-code-web/app 全通过）。
- Doc-sync: No owner-doc update required（测试 bug 修复，不改变产品行为或约定）

### 执行 plan 172：nop-stream checkpoint 容错契约整改（全部 3 Phase 落地）

- 产出：plan 172 全部 3 个 Phase 完成（`proposed` → `completed`），local 路径 P0 止血 + P1 触发安全/失败可观测/并发一致全部落地
- **Phase 1（abort 接线）**：
  - `CheckpointCoordinator.setAbortHandler(Consumer<Long>)` — 新增 abort 回调，在 `abortPendingCheckpoint` 中调用
  - `GraphModelCheckpointExecutor.registerLocalAbortHandler` + `checkAbortMarker` — executor 侧 abort 标记 + cancel all tasks + 跳过 handleJobTermination
  - 5 个 `executeWithCheckpoint`/`triggerSavepoint`/`executeWithSavepoint` 方法全部接入
  - 测试：`TestCheckpointAbortWiring` — 多输入 stuck-channel e2e + 单 source stuck 变体（2 tests，4.4s）
- **Phase 2（触发原子性 + 失败可观测 + 并发一致）**：
  - `tryTriggerPendingCheckpoint` + `abortPendingCheckpoint` → `synchronized`，消除 TOCTOU
  - `startBarrierScheduler` exception 路径调 `coordinator.incrementTriggerFailures()`；`completePendingCheckpoint` 成功时清零
  - `CheckpointConfig` 新增 `maxConsecutiveCheckpointFailures`（默认 3）
  - `effectiveMaxConcurrent = Math.min(1, config)` 强制 max=1，构造时警告
  - 测试：`TestCheckpointTriggerSafety` — 并发原子性 + 失败计数/重置 + 并发降级 + trigger/abort 互斥（5 tests）
- **Phase 3（对齐超时）**：
  - `InputGate` 新增 `barrierAlignmentTimeout` 字段 + `alignmentStartTime` 追踪
  - `readMultiChannel` 在 parkNanos 前检查累计 elapsed，超时抛 `ERR_STREAM_BARRIER_ALIGNMENT_TIMEOUT`
  - `handleBarrierNonRecursive` 中 `pendingBarrier` 首次设置时记录 `alignmentStartTime`；`resetBarrierState` 重置
  - `CheckpointConfig` 新增 `barrierAlignmentTimeout`（默认 30s）→ `GraphExecutionPlan.build` 新 overload → InputGate 构造传递
  - finished-channel 隐式对齐保留（不计入超时等待）
  - 测试：`TestInputGateAlignmentTimeout` — 慢 channel 超时 + 正常对齐 + finished-channel + 无对齐不超时（4 tests）
- 设计文档更新：`checkpoint-design.md` §2.4（对齐超时）+ §2.8（并发策略强制 max=1）+ §8.7（abort 语义）+ §9.4（CheckpointConfig 表）；`component-roadmap.md` §3 C5 + §5
- 全量测试：`./mvnw test -pl nop-stream/nop-stream-runtime,nop-stream/nop-stream-core -am` 全绿（0 failures）
- Doc-sync: checkpoint-design.md §2.4/§2.8/§8.7/§9.4、component-roadmap.md §3 C5/§5

### 拟制 nop-stream checkpoint 容错契约整改计划（plan 172）

- 产出：`ai-dev/plans/172-nop-stream-checkpoint-fault-tolerance-remediation.md`（proposed）—— 基于 analysis v2.1 共识 + design §13.2 契约 + roadmap §3 C5 缺口，落地 P0+部分 P1 容错契约
- 范围：Phase 1（local abort 接线，P0 止血）、Phase 2（触发原子性 + 失败计数 + 并发能力一致强制 max=1，P1）、Phase 3（InputGate 内联对齐加累计超时，P0）。P2/P3 + BarrierAligner 统一 + distributed abort RPC + cancel marker 移 Deferred（6 个 successor，均有 Why Not Blocking 理由）
- 三轮独立 reviewer 审查共识：第一轮想象性分析发现 3 Blocker（abort 语义未定 / BarrierAligner 模块边界 / API 不兼容）+ 引用核查发现并发能力一致未裁定；第二轮修订验证发现更深 Blocker（SubtaskTask.cancel 状态机 CANCELED≠FAILED 使 abort→job 失败不成立）；第三轮裁定 abort flag 机制 RESOLVED + ACCEPT
- 关键技术决策（经审查锁定）：(1) abort 机制 = executor 注册回调置 abort 标记 + cancel tasks，executeWithCheckout 在 submitAndRun 后/handleJobTermination 前检标记抛异常（绕过 cancel 状态机的 CANCELED 陷阱，免疫 listener 异常吞没）；(2) Phase 3 缩小为给 InputGate 加超时（BarrierAligner 启用是架构重构非简单接入，因 runtime→core 反向依赖 + 分离式/交错式 API 不兼容，移 successor）；(3) 并发能力一致取强制 max=1 路线
- 状态：proposed，待用户决定是否进入执行（plan-first，实施需 audit/确认）
- Doc-sync: No owner-doc update required（plan 拟制，未改产品代码；plan 内 Phase 各自规定了执行时的 design 同步项）

### 根据 barrier/checkpoint 对比分析更新 nop-stream 设计文档

- 触发：将上午产出的对比分析报告（`ai-dev/analysis/2026-06-14-...md`）共识结论沉淀到设计文档
- 产出（按 design 指南"design 不引用 analysis、实现状态归 roadmap"分工）：
  - `ai-dev/design/nop-stream/checkpoint-design.md` 新增 §13 容错约束边界：执行路径分层（local/distributed）+ 9 条容错契约（纯定义，无实现细节）+ 缓解选项（EFFECTIVELY_ONCE）+ 不变量与契约的关系。§2.4/§2.8 补契约交叉引用
  - `ai-dev/design/nop-stream/component-roadmap.md` §3 C5 新增容错缺口表（9 条契约的当前实现状态 + 源码细节 + P0-P3 优先级）+ 已实现能力 + 演进路径 + 缓解选项；§5 已知技术债同步 P0/P1/P2-P3 行
  - `ai-dev/design/nop-stream/README.md` 同步 checkpoint-design §13 描述
- 多轮独立 reviewer 审查（2 轮 3 agent）：第一轮（design 指南合规 + 源码核对）发现 §13 违反"design 不写实现状态/不写 Proposed-vs-Current"，按方案 A 重构（契约留 design、实现状态迁 roadmap）；第二轮（修订验证）裁定 ACCEPT，必改项 5/5 落实，两文件 9 契约一一对应
- 关键修订：(1) checkpoint-design §13 从"实现状态+缺口表"重构为纯"容错契约定义"，实现细节（数值/数据结构/死代码/TOCTOU）全部迁移到 roadmap；(2) §2.4"必须"语义与 §13.2 契约统一（契约说"必须"，实现状态在 roadmap 跟踪）；(3) 丢数据措辞修正（限定为"声明可重放但实际不可重放"）
- Doc-sync: 本次为 design 文档更新本身；doc link checker 通过（我改的文件无 broken link）

### nop-stream barrier/checkpoint 容错对比分析（analysis）

- 触发：修复 `TestProcessingGuaranteeBehavior` hang 时发现 `InputGate.readMultiChannel` 用 `parkNanos` 无限轮询、无对齐超时，且该类在生产路径被实例化（`GraphExecutionPlan.java:269`），由此展开系统性对比
- 产出：`ai-dev/analysis/2026-06-14-nop-stream-barrier-checkpoint-comparison.md` —— 对比 nop-stream vs Flink vs SeaTunnel(Zeta)，覆盖 12 个维度 + 5 类容错场景（超时/新消息/channel 失败/重叠 barrier/并发）+ P0-P3 改进建议
- 多轮独立核查：报告经 **5 位独立 sub-agent reviewer 两轮审查**（第一轮：源码核查员/对比公允性/魔鬼代言人 → v2；第二轮：修订验证裁判/全新独立终审 → v2.1）。两轮均 **ACCEPT，共识达成**，无 BLOCKING 异议
- 关键修订（经核查纠正的初版错误）：(1) "InputGate 不可中断"→ 实为"abort 未接线到已有 `SubtaskTask.cancel()` 中断机制"；(2) 初版 §5.2 引用了死代码 `startCheckpointScheduler`，生产实际用 `startBarrierScheduler` 且无失败计数；(3) 竞态可达性校准：纯周期单线程路径不可达，仅 savepoint/冷路径可达，降为 P1；(4) 补充 local/distributed 双路径、EFFECTIVELY_ONCE 缓解选项、`awaitCompletion` 串行阻塞放大、lease 实际 ~15-20s（非 30s 死常量）
- 最终结论：P0 为 §5.1 abort 接线（生产 hang）；P1 为触发竞态(§5.2)/失败计数(§5.3)/cancel marker(§5.4)/启用 BarrierAligner(§5.5)；P3 为 unaligned checkpoint。行号引用 100% 准确
- 方法：派 2 个 explore agent 深挖 Flink（`~/sources/flink`）和 SeaTunnel（`~/sources/seatunnel`），自查 nop-stream core+runtime 全 checkpoint 链；Hazelcast 作为 SeaTunnel 通信层无独立 barrier 实现
- Doc-sync: No owner-doc update required（纯分析调研，未改产品代码/约定）

### nop-stream-core: 修复 TestProcessingGuaranteeBehavior 无限 hang 导致整个 build 卡死（ad-hoc bug fix）

- 现象：`./mvnw test -T 1C` 卡住 14 分钟无进展，mvn 主进程与 surefire 子进程 CPU 均 0%（hang 死非慢）。jstack 定位到 `nop-stream-core` 的 `TestProcessingGuaranteeBehavior.testAlignedVsNonAlignedBarrierBehavior` 在 `ResultPartition.read:111`（`LinkedBlockingQueue.poll`）上无限阻塞；`nop-stream-core/target/surefire-reports/` 历史堆积 203 个 dump 文件，证明该测试长期反复 hang
- 根因（测试逻辑 bug）：aligned gate（`barrierAlignment=true`）要求所有 channel 收齐 barrier 才能完成对齐；但原测试只给 `p0_aligned` 写了 barrier，`p1_aligned` 既无 barrier 也未 `close()` → `barriersRemaining` 永远 =1，channel 0 永远被 skip，`isAllFinished()` 永远 false → `InputGate.readMultiChannel` 的 `parkNanos` 循环永久自旋。且 surefire 未配置 `forkedProcessTimeoutInSeconds`（pom.xml:201 被注释），测试方法无 `@Timeout`，整个 build 无限挂起
- 深层矛盾：原断言2 `assertFalse(alignedRecords.contains("after-barrier-aligned"))` 在逻辑上不可能在不 hang 的情况下成立 —— aligned（exactly-once）正确语义是对齐完成后释放 barrier 后缓冲数据；要阻塞 after-barrier 就必须让对齐永不完成，而那必然 hang。故断言2 本身错误
- 修复（`nop-stream/nop-stream-core/src/test/java/io/nop/stream/core/execution/TestProcessingGuaranteeBehavior.java`）：
  - 给 `p1_aligned` 补 `CheckpointBarrier`（aligned/non-aligned 两 gate 的 p1 均补齐 barrier，使对齐能完成）
  - 所有 4 个 `ResultPartition` 在 read 前 `close()`（发 EOS，使 `isAllFinished()` 为 true，read 循环自然终止）
  - 修正断言2：`assertFalse` → `assertTrue`（对齐完成后释放 barrier 后数据，符合 exactly-once 语义）
  - 新增时序断言：aligned 下 `firstBarrierIndex < firstRecordIndexWithValue("after-barrier-aligned")`，保留"对齐期间阻塞 barrier 后数据"的验证价值（原断言2 的本意），用正确的、可终止的方式表达
  - 抽取 `collectRecords`/`firstBarrierIndex`/`firstRecordIndexWithValue` 三个 helper
- 按用户要求未启用 surefire 全局超时（理由：不同机器速度不同），改为从测试逻辑层面保证可终止
- 验证：
  - `./mvnw test -pl nop-stream/nop-stream-core -am -Dtest=TestProcessingGuaranteeBehavior -Dsurefire.failIfNoSpecifiedTests=false` → 2 tests, 0 failures, BUILD SUCCESS（无 hang）
  - `./mvnw test -pl nop-stream/nop-stream-core` → 982 tests, 0 failures, 0 errors, 1 skipped, BUILD SUCCESS（无回归、无 hang）
- 清理：删除 `target/surefire-reports/` 下历史 hang 残留的 206 个 `.dump`/`.dumpstream` 垃圾文件
- Doc-sync: No owner-doc update required（测试 bug 修复，不改变产品行为或约定；`docs-for-ai/02-core-guides/testing.md` 无需更新，因未引入新测试约定）

### nop-ai-agent Sub-Agent Path Permission Inheritance（Plan 170 — Phase 1 + 2 + 3 + Closure）

- Plan: `ai-dev/plans/170-nop-ai-agent-path-permission-inheritance.md` → completed（Phase 1 + 2 + 3 + Closure 全部完成）
- Work Item: sec-4.4 / L4-1d（sub-agent path-permission inheritance — path-level）
- Design contract: 文件权限 = 父权限 ∩ 子配置（design §4.4 第二条款）
- Phase 1：Path-Scope Constraint Model + Enforcement Wrapper
  - `ParentPermissionConstraint` 扩展为携带 additive `allowedPathRoots` 字段（`Set<String>`，null=ABSENT，非 null=PRESENT，PRESENT({})=deny all）；backward-compatible 构造器委托为 null；equals/hashCode/toString 包含新字段
  - 新增 `ParentConstrainedPathAccessChecker.java`（enforcement wrapper：ABSENT roots → delegate；PRESENT + path under root → delegate；PRESENT + path outside → deny via `PathAccessResult.deny(reason, "parent_path_permission_constraint")`，reason 标识父 Agent 名称 + 违规路径；constructor fail-fast IllegalArgumentException on nulls）
  - `PathAccessResult` 新增 additive `deny(String reason, String matchedRule)` 工厂（existing `allow()`/`deny(reason)`/`denyByRule(...)` 不变）
  - `DefaultPathAccessChecker` 暴露 `normalizePathStatic` 为 public static（normalizePath 改为委托；deny-list 语义不变）
  - `agent.xdef` 新增 `workDir="string"` 属性 → `_AgentModel.java` 由 codegen 重新生成（_workDir 字段 + getter/setter）
  - 新增测试：`TestParentConstrainedPathAccessChecker`（31 tests：ABSENT/PRESENT/empty 三分支、root matching edge cases、relative path resolution、constructor fail-fast、immutability、equals/hashCode/toString）
- Phase 2：Engine Wiring + Context/Propagation + Integration Tests
  - `AgentToolExecuteContext` 新增 `allowedPathRoots` 字段（3 个 backward-compatible 构造器）
  - `ReActAgentExecutor` 将 `agentModel.getWorkDir()` 替代硬编码 `new File(".")`；新增 `resolveWorkDir`/`computeEffectivePathRoots`/`computeOwnDeclaredPathRoots`/`isUnderAnyRoot` 方法（effective roots = incomingParentRoots ∩ ownDeclaredRoots，ABSENT 为单位元）
  - `CallAgentExecutor.buildParentConstraint` 读取 `getAllowedPathRoots()` 并纳入 `ParentPermissionConstraint`
  - `DefaultAgentEngine` 新增 `resolveEffectivePathAccessChecker(AgentMessageRequest)` + `getPathAccessCheckerForTest()` + 3-arg `resolveExecutor(model, toolChecker, pathChecker)`；`doExecute()` 同时解析 effective tool checker 和 effective path checker
  - 新增测试：`TestSubAgentPathPermissionWiring`（12 tests：context carries roots、call-agent propagation、engine wrapping、fail-fast、backward compat）
- Phase 3：End-to-End Verification + Design Doc Update
  - 新增测试：`TestSubAgentPathPermissionEndToEnd`（4 tests：constraint enforcement、backward compatibility、nested delegation clamping、null workDir = no confinement）
  - 新增 agent fixtures：`test-parent-agent-path.agent.xml`（workDir=/workspace/project-a）、`test-sub-agent-path.agent.xml`、`test-middle-agent-path.agent.xml`（workDir=/workspace/project-a/sub）、`test-leaf-agent-path.agent.xml`、`test-parent-agent-no-workdir.agent.xml`
  - Design doc `nop-ai-agent-security-and-permissions.md` §4.4：文件权限条款从 "deferred" 改为 "已交付 ✅"；记录 5 个架构决策；更正 "glob 规则" 不准确描述
  - Roadmap `nop-ai-agent-roadmap.md`：新增 L4-1d 行，标记 ✅
- Closure：独立 closure audit subagent（ses_13c45db8effeiWMJTSfk0vBrTa）PASS；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-ai-agent --severity high` exit 0；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 871 tests, 0 failures
- Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-security-and-permissions.md` §4.4 updated（delivered status + 5 decisions + glob-rules correction）；`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` L4-1d added ✅

### goal-driver: 优化 PLAN_AUDIT prompt（审查耗时与行为）

- 背景：分析通宵运行 `_tmp/2026-06-13-233645-goal-driver`（9 个完整计划周期），发现 PLAN_AUDIT 平均 **19m38s/次**，是拟制 PLAN_DRAFT（7m48s）的 2.5 倍，是除 EXECUTE 外最大耗时。逐个读取 3 份 audit 日志（P2 plan161 29m、P3 plan162 7m30s、P4 plan163 31m54s）定位根因。
- 根因（4 点）：
  1. **AI 在已 PASS 时主动加轮**：prompt 判据是"0 Blocker + 0 Major 即通过"，但 AI 把 Minor 也当返工触发器。P4 的 R1 本已 0B/0M（仅 11 Minor），AI 主动返工 → 改坏 → R2 查出 2 Major → 再修再审，从 7m 膨胀到 32m。
  2. **主 agent 做了大量与子 agent 重复的 context gathering**：P2 主 agent 在派 review 子 agent 前，自己 Read 15+ 源文件、5 design doc、grep 统计（约 13 分钟），但子 agent 是独立会话看不到，会自己重探索——纯浪费。P3 不做探索、直接派子 agent，反而最快。
  3. **max 5 rounds 太宽松**，AI 倾向用满追求"consensus"。
  4. Revise 子 agent 改 Minor 时引入新 Major（P4 教训）。
- 改动：`ai-dev/tools/opencode-goal-driver/prompts/plan-audit.md` 完整重写，落实 4 条硬规则：
  - **coordinator 约束**：主 agent 只读 `{{PLAN_FILE}}` + plan guide，禁止 Read/Grep/Glob 源码与 design doc；所有 repo 验证交给 review 子 agent。
  - **通过即停**：R1 若 0B/0M 立即 approved，禁止为 Minor 或"consensus"返工。
  - **返工范围限定**：仅 Blocker/Major 触发 revise；revise 子 agent 禁止动 Minor 文本。
  - **轮数收紧**：max 5 → **max 2 review rounds**，禁止 Round 3+ 及 polish/confirm 轮。
- 接口兼容：PLAN_AUDIT step 的 `resultTag`（默认 AI_STEP_RESULT）、transitions（approved→EXECUTE_PLAN / issues→retry PLAN_DRAFT）、`<ISSUES>` 输出格式均不变；prompt 同时被 `deep-audit-loop.json:46` 复用，改动对两者一致适用。
- 验证：`node --test`（core/intg/group-subflow/transitions）→ 63 pass / 0 fail。预期效果：审查行为统一向 P3 看齐（7m30s），平均从 19m38s 降到 ~10m。
- Doc-sync: No owner-doc update required（goal-driver 内部 prompt 调优）。

### nop-ai-agent Sub-Agent Permission Inheritance（Plan 169 — Phase 1 + 2 + 3 + Closure）

- Plan: `ai-dev/plans/169-nop-ai-agent-sub-agent-permission-inheritance.md` → completed（Phase 1 + 2 + 3 + Closure 全部完成）
- Work Item: sec-4.4 / L4-1c（sub-agent permission inheritance — tool-level）
- Phase 1：Permission Constraint Contract + Enforcement Component
  - 新增 `ParentPermissionConstraint.java`（immutable 值对象：parent's effective clamped allowed tool set + parent agent name + parent session ID；`METADATA_KEY = "parentPermissionConstraint"`）
  - 新增 `ParentConstrainedToolAccessChecker.java`（enforcement wrapper：constraint present + tool not in parent set → deny with "parent permission constraint" reason；tool in parent set → delegate；constructor rejects null constraint/delegate）
  - 新增测试：`TestParentConstrainedToolAccessChecker`（18 tests：immutability、defensive copy、null-safe、intersection logic、empty-set max-restriction、explicit denial reason、integration with DefaultToolAccessChecker/AllowAllToolAccessChecker）
- Phase 2：call-agent Integration + Engine Wiring
  - `AgentToolExecuteContext` 新增 `allowedTools` field（overloaded 11-arg constructor；原 10-arg constructor preserved → backward compatible）
  - `ReActAgentExecutor` 新增 `computeEffectiveAllowedTools()`（effective = `incomingParentConstraint ∩ agentModel.getTools()`，no constraint → declared set unchanged）
  - `CallAgentExecutor` 读取 `agentCtx.getAllowedTools()` → 构建 `ParentPermissionConstraint` → 写入 request metadata（所有 3 session modes：new/continue/fork）
  - `DefaultAgentEngine` 新增 `resolveEffectiveToolAccessChecker()`（从 request metadata 读取 constraint → wrap engine's toolAccessChecker；fail-fast on malformed constraint）；`resolveExecutor()` 重载接受 overridden checker
  - 新增测试：`TestSubAgentPermissionWiring`（7 tests：call-agent constraint propagation、null allowedTools backward compat、engine wrapping、fail-fast on malformed、full wiring denial）
- Phase 3：End-to-End Verification + Design Doc Update
  - 新增测试 agent XML：`test-parent-agent.agent.xml`（tools: read-file,call-agent）、`test-sub-agent-wide.agent.xml`（tools: read-file,write-file,bash）、`test-middle-agent.agent.xml`（tools: read-file,call-agent,write-file）、`test-leaf-agent.agent.xml`（tools: read-file,write-file）
  - 新增端到端测试：`TestSubAgentPermissionEndToEnd`（3 tests）
    - constraint enforcement：parent → call-agent → sub-agent write-file/bash denied by parent constraint（TOOL_CALL_DENIED event + "parent permission constraint" reason），read-file allowed
    - backward compatibility：single-agent execution 无 spurious denials
    - nested delegation：A → B（declares more than A allows）→ C，C 的 write-file denied 因为继承 B 的 clamped set 而非 declared set（证明 clamped-set 传播正确）
  - 设计文档更新：`nop-ai-agent-security-and-permissions.md` §4.4（delivered status + 3 decisions：constraint representation = effective clamped set、enforcement = wrapping IToolAccessChecker、propagation via metadata；path-permission deferred with rationale）
  - roadmap 更新：`nop-ai-agent-roadmap.md` 新增 L4-1c (sec-4.4) ✅
- 关键决策：
  - constraint representation = parent's **effective（clamped）** tool set（not declared set）— 这是嵌套委派安全的关键
  - enforcement = wrapping IToolAccessChecker at executor-resolution time（not hardcoded into default checkers）— backward compatible
  - propagation via AgentMessageRequest.metadata（well-known key，复用现有基础设施）
- 测试统计：824 tests total，0 failures，0 errors

### nop-ai-agent call-agent + send-message Tools（Plan 168 — Phase 1 + 2 + 3 + Closure）

- Plan: `ai-dev/plans/168-nop-ai-agent-call-agent-tool.md` → completed（Phase 1 + 2 + 3 + Closure 全部完成）
- Work Item: L4-1b（`call-agent` fork+exec + `send-message` fire-and-forget + 工具上下文增强）
- Phase 1：Tool Context Enrichment
  - 新增 `AgentToolExecuteContext.java`（implements `IToolExecuteContext`，额外暴露 `IAgentEngine` + `IAgentMessenger` + sessionId + agentName）
  - `ReActAgentExecutor` 新增 `engine` / `messenger` Builder 方法，tool-call site 从 `SimpleToolExecuteContext` 切换到 `AgentToolExecuteContext`
  - `DefaultAgentEngine.resolveExecutor()` 传递 `this` 和 `messenger` 到 Builder
  - 新增测试：`TestAgentToolExecuteContext`（5 tests：字段正确填充、接口向后兼容、接线验证 engine/sessionId/agentName、null engine fallback）
- Phase 2：call-agent Functional Implementation（fork+exec）+ Registration
  - 新增 `io.nop.ai.agent.tool.CallAgentExecutor`（fork+exec 模型，直接调用 `IAgentEngine.execute()` 同步执行子 Agent）
  - 支持 agentId 解析（"self" → 当前 agent name）、session 三模式（新建/续用/fork）、JSON 参数解析、超时、fail-fast
  - 删除 hollow mock：`nop-ai-toolkit/.../CallAgentExecutor.java` + `CallAgentExecutorTest.java` + `ai-tools-defaults.beans.xml` 中的 bean 注册
  - 新增 beans XML：`nop-ai-agent/_vfs/nop/ai/beans/ai-agent-tools.beans.xml` 注册 functional executor（auto-collected by `collect-beans by-type`）
  - 新增测试：`TestCallAgentExecutor`（9 tests：named agent 真实子 Agent 响应、self+inheritContext=false/true、session 续用、unknown agentId fail-fast、result sessionId、missing agentId、null engine、non-agent context）
- Phase 3：send-message + End-to-End + Design Docs
  - 新增 `send-message.tool.xml`（schema: targetSessionId + input + correlationId）
  - 新增 `io.nop.ai.agent.tool.SendMessageExecutor`（fire-and-forget via `IAgentMessenger.send()`，NoOp messenger 诚实报告 not delivered）
  - 注册到 `ai-agent-tools.beans.xml`
  - 新增测试：`TestSendMessageExecutor`（5 tests：handler 投递验证、NoOp honest reporting、missing targetSessionId/input、non-agent context）
  - 新增端到端测试：`TestCallAgentSendMessageEndToEnd`（3 tests：call-agent in ReAct loop 全链路验证、send-message 投递到 registered handler、backward compat）
  - 设计文档更新：`nop-ai-agent-multi-agent.md` §Phase 1、`nop-ai-call-agent-dsl.md`、`01-architecture-baseline.md` §六、`nop-ai-agent-actor-runtime-vision.md` §3.3/§10、`nop-ai-agent-roadmap.md`（L4-1b ✅）
- 关键决策：
  - call-agent MVP 采用 fork+exec 模型（直接 `IAgentEngine.execute()`），mailbox-mediated 模型是 Actor Runtime (L4-8) 的目标
  - hollow mock 删除而非保留——functional executor 是唯一的 call-agent provider，避免 by-type collision
  - send-message 在 NoOp messenger 下诚实报告 "not delivered"，不假装成功

### nop-ai-agent Skill Curator（Plan 167 — Phase 1 + 2 + 3 + Closure）

- Plan: `ai-dev/plans/167-nop-ai-agent-skill-curator.md` → completed（Phase 1 + 2 + 3 + Closure 全部完成）
- Work Item: L4-4（`ISkillCurator` `LLMCurator`）
- Phase 1：契约 + NoOp + 结果模型 + 设计文档
  - 新增 `SkillQualityRating.java`（enum: `WELL_DEFINED` / `NEEDS_IMPROVEMENT` / `REDUNDANT`）
  - 新增 `SkillCurationResult.java`（结果模型 + 嵌套 `SkillAssessment` + `SkillCurationMetadata`；`empty()` success factory + `failed()` fail factory；success/fail marker 可区分）
  - 新增 `ISkillCurator.java`（策展契约接口；advisory、non-mutating；消费 `Collection<SkillModel>`）
  - 新增 `NoOpSkillCurator.java`（singleton pass-through，`noOp()` 返回 empty success）
  - 设计文档更新：`skill-system-design.md` 新增 §5.5 "Skill 策展 / Curation"（advisory 语义、non-mutating 契约、与 ISkillProvider 的关系、质量评级分类法、调用模型、LLM 输出格式契约、批处理部分失败语义、拒绝的替代方案）
  - 新增测试：`TestNoOpSkillCurator`（5 tests）、`TestSkillCurationResult`（11 tests）
  - 2 个 Decision：advisory non-mutating 契约语义 + on-demand 调用模型
- Phase 2：LLMCurator 功能实现 + CuratorConfig
  - 新增 `CuratorConfig.java`（config 对象，遵循 `LlmJudgeConfig` 模式：IChatService + system prompt + model override + maxTokens/temperature/maxSkillsPerCall + 输入验证）
  - 新增 `LLMCurator.java`（功能实现，遵循 `LlmCompletionJudge` 模式）：
    - 空注册表 → empty success，不调用 LLM（call count = 0）
    - 批处理策展：注册表超过 maxSkillsPerCall 时分批顺序调用，合并结果
    - 部分失败语义：成功批次贡献评估，失败批次贡献失败标记，总体标记为 fail
    - Fail-open + 显式失败标记：LLM 异常/null/unsuccessful/unparseable → fail marker（非静默空成功）
    - JSON 输出格式契约：parser + default prompt 一致；markdown 代码围栏/prose 包裹均可解析
    - Token 使用量累积
    - 静态工厂：`llm(CuratorConfig)` / `llm(IChatService)`
  - 新增测试：`TestLLMCurator`（30 tests，覆盖 a-j 全部 10 个场景 + 配置验证 + JSON 提取 + 格式错误条目跳过）
- Phase 3：引擎接线 + 集成测试 + 向后兼容
  - `DefaultAgentEngine` 新增 `ISkillCurator skillCurator` 字段（默认 `NoOpSkillCurator.noOp()`）+ `setSkillCurator(ISkillCurator)` setter（null → NoOp fallback）+ `curateSkills()` 按需策展方法（同步，从 `ISkillProvider` 获取注册表，调用 curator，返回 `SkillCurationResult`）
  - 新增测试：`TestSkillCuratorIntegration`（7 tests）：
    - **接线验证**：NoOp 默认返回 empty success（向后兼容）；LLMCurator + FileSystemSkillProvider 产生 fixture skills 评估（call count = 1）；空注册表不调用 LLM（call count = 0）；LLM 异常返回 fail-open fail marker
    - **端到端验证**：FileSystemSkillProvider → engine.curateSkills() → LLMCurator → IChatService.call() → JSON parse → SkillCurationResult
  - 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过；53 个策展相关测试（5+11+30+7），0 failures
- Closure：roadmap L4-4 ❌ → ✅；`scan-hollow-implementations.mjs` exit 0（0 critical/high findings）；`check-plan-checklist.mjs --strict` exit 0

### nop-ai-agent Inter-Agent Messenger（Plan 166 — Phase 3 + Closure）

- Plan: `ai-dev/plans/166-nop-ai-agent-inter-agent-messenger.md` → completed（Phase 1 + 2 + 3 + Closure 全部完成）
- Phase 3：引擎接线 + 端到端 + 设计文档更新
  - `DefaultAgentEngine` 新增 `IAgentMessenger messenger` 字段（默认 `NoOpAgentMessenger.noOp()`）+ `setMessenger(IAgentMessenger)` setter（null → NoOp fallback，遵循 `setSkillProvider` 模式）+ `getMessenger()` 访问器；无构造器链变更
  - 新增测试 `TestDefaultAgentEngineMessengerWiring`（8 个测试，0 failures）：
    - **接线验证**：`setMessenger(x)` → `getMessenger()` 返回同一实例；`setMessenger(null)` → NoOp fallback；默认（未调用 setter）→ NoOp singleton
    - **端到端 A→B 往返**：引擎配置 `LocalAgentMessenger` → 端点 A 向 `agent.B.inbox` 发 REQUEST → B handler 回复 → A 的 `CompletableFuture` 以 B 响应完成（经平台 `LocalMessageService` 全路径）
    - **端到端 fire-and-forget**：引擎 messenger send ASYNC → C handler 收到 payload
    - **向后兼容**：默认 NoOp → request fail-fast（`UnsupportedOperationException`），send 不抛；既有构造路径（`new DefaultAgentEngine(chatService, toolManager)`）不变
  - 设计文档更新：
    - `01-architecture-baseline.md` §六：新增段落标记 Agent 域 messenger 层已接线（`IAgentMessenger`、`agent.*` topic 命名、`setMessenger` 接线、ack-topic 抑制），保持既有设计契约文本不变
    - `nop-ai-agent-actor-runtime-vision.md` §4.2 MessageRouter 行：注明 L4-1 已落地，messenger 可作为路由基底
    - `nop-ai-agent-roadmap.md` §4 L4-1：❌ → ✅
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（721 tests, 0 failures），既有测试不受影响
- Closure：独立子 agent closure-audit PASSED（task ses_13d230bfcffe4o5mt9XqWQ5Av5）；anti-hollow 检查全部验证（send/request/registerHandler 调用链连通、ack-topic 抑制、NoOp fail-fast）；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs` exit 0；43 个新增测试（15+20+8），721 total，0 failures

### nop-ai-agent Inter-Agent Messenger（Plan 166 — Phase 2）

- Plan: `ai-dev/plans/166-nop-ai-agent-inter-agent-messenger.md` — Phase 2 completed（Phase 3 进行中）
- Phase 2：Messenger 接口 + 内存实现 + NoOp 默认
  - 新增 `IAgentMessageHandler.java` — Agent 域函数式 handler 契约（接收 `AgentMessageEnvelope`，返回响应对象或 null）；**不**直接暴露平台 `IMessageConsumer`（Decision M5）
  - 新增 `IAgentMessenger.java` — 行为契约接口：`send(envelope)` fire-and-forget、`request(envelope, timeout)` 返回 `CompletableFuture<Object>`（含超时）、`registerHandler(topic, handler)` 返回可取消 `IMessageSubscription`
  - 新增 `LocalAgentMessenger.java` — 基于 `LocalMessageService` 的内存实现：
    - `send` 委托 `IMessageService.send(targetTopic, envelope)`
    - `request`：lazy 订阅 reply topic（`agent.{senderId}.reply`，按 replyTopic 共享），pendingRequests 按 correlationId → future 分发，`CompletableFuture.orTimeout` 实现超时，future 完成时自动清理 pending entry
    - `registerHandler`：包装 handler 为 `HandlerAdapter`（`IMessageConsumer`），委托 `IMessageService.subscribe`
    - **ack-topic 抑制（Decision M2）**：`HandlerAdapter` 捕获 handler 非 null 返回值，构造 RESPONSE 信封显式发送到 requester 的 reply topic，然后向平台返回 **null** 以抑制 `LocalMessageService.handleMessageResult` 的 `ack-{topic}` 自动路由
    - handler 异常 → log + 不发送响应（MVP：requester 超时）
  - 新增 `NoOpAgentMessenger.java` — pass-through 默认（singleton）：`send` debug-log no-op、`registerHandler` 返回 no-op subscription（cancel 后 isCancelled=true）、`request` **fail-fast** 抛 `UnsupportedOperationException("not yet implemented: no message service configured")`（非静默 null/空 future）
  - Decision M5（Agent 域 handler 契约，非平台 IMessageConsumer）+ M2（ack-topic 抑制）记录在 plan 中
  - 新增测试：`TestLocalAgentMessenger`（14）+ `TestNoOpAgentMessenger`（6）共 20 个测试，0 failures
    - 关键验证：`responseGoesToReplyTopicNotResponderAckTopic` — 在 responder ack-topic 上 spy，验证响应 payload **未**到达 ack-topic，仅到达 requester 的 future（经 reply topic）
    - `mismatchedCorrelationIdDoesNotCompleteOtherRequest` — 共享 reply topic 上不匹配的 correlationId 不会完成另一个 future
    - `handlerExceptionDoesNotPropagateToRequesterButRequestTimesOut` — handler 异常 → requester 超时（MVP）
    - `requestNeverReturnsSilentlyCompletingFuture` — NoOp request 必抛异常，不返回静默 future
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（713 tests, 0 failures），既有测试不受影响
- No owner-doc update required（Phase 3 统一更新 design doc）

### nop-ai-agent Inter-Agent Messenger（Plan 166 — Phase 1）

- Plan: `ai-dev/plans/166-nop-ai-agent-inter-agent-messenger.md` — Phase 1 completed（Phase 2/3 进行中）
- 为 nop-ai-agent 建立 Agent 间通信的消息基础设施，叠加在平台 `IMessageService`/`LocalMessageService` 之上（不重复造轮子）
- Phase 1：依赖 + Agent 消息信封 + Topic 约定
  - `nop-ai/nop-ai-agent/pom.xml` 新增 `nop-message-core` 依赖（已在 nop-bom 管控，无需 version）
  - 新增 `io.nop.ai.agent.message` 包：
    - `AgentMessageKind.java` — 消息种类枚举（REQUEST / RESPONSE / ASYNC）
    - `AgentMessageEnvelope.java` — 不可变值对象：senderId / targetTopic / correlationId / kind / payload / timestamp；构造时校验 kind 非空；equals/hashCode/toString 完整
    - `AgentMessageTopics.java` — topic 命名约定 helper：`inboxTopic`=`agent.{id}.inbox`、`replyTopic`=`agent.{id}.reply`（共享 reply topic，不嵌入 correlationId）、`broadcastTopic`=`agent.broadcast.{scope}`
  - Decision M4（topic 命名空间）：Agent 域用独立 `agent.*` 命名空间，不复用平台 `bro-`/`reply-`/`bat-` 前缀（避免触发 `LocalMessageService.getBroadcastTopics()` 的多路分发特殊逻辑，避免与平台 topic 命名空间混淆）—— 记录在 `AgentMessageTopics` javadoc + plan
  - 新增测试：`TestAgentMessageEnvelope`（6）+ `TestAgentMessageTopics`（9）共 15 个单元测试，0 failures
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（693 tests, 0 failures），既有测试不受影响
- No owner-doc update required（Phase 3 统一更新 design doc）

### nop-ai-agent LlmCompletionJudge 实现（Plan 165 — completed）

- Plan: `ai-dev/plans/165-nop-ai-agent-llm-completion-judge.md` → completed (Phase 1 + Phase 2)
- 实现 design §5.3 Phase 2 "小模型" Judge 半壁：plan 162 交付了确定性规则 Judge（"规则"），本 plan 交付语义 LLM Judge（"小模型"），两者是替代策略而非分层
- 新增 `completion/LlmJudgeConfig.java`：配置 `IChatService chatService`(required)、`systemPrompt`(默认 Judge prompt，解释角色/三裁决/输出格式)、`model`(可选模型名 override via ChatOptions)、`maxTokens`(default 200)、`temperature`(default 0.0)、`fallbackDecision`(default Complete)、`maxContextMessages`(default 20)、`defaultContinuationMessage`。构造时验证：chatService 非空、maxTokens>0、temperature∈[0,2]、maxContextMessages≥0。静态工厂 `defaults(IChatService)` 返回零调参实例
- 新增 `completion/LlmCompletionJudge.java` implementing `ICompletionJudge`，行为语义：
  - **Null/blank guard**：assistantMessage 为 null 或内容空白 → 直接 Complete，不调 LLM（call count=0）
  - **Cancel guard**：`ctx.isCancelRequested()` → fallbackDecision，不调 LLM（call count=0）
  - **Prompt 构造**：system prompt（Judge 指令）+ user message（task goal from `plan.getGoal()` → `agentModel.getDescription()` → "N/A" + 最近 N 条非 system 消息作为 context + 被评判的 assistant 响应）；temperature/maxTokens/model override 设到 ChatOptions
  - **LLM call**：`chatService.call(request, null)` 同步调用，try-catch 包裹，任何异常 → fallback，不向 ReAct 循环传播
  - **Error/null response guard**：response 为 null / `isSuccess()==false` / message 为 null / verdict 内容空白 → fallback
  - **Verdict 解析**：取第一个非空行，case-insensitive 匹配 COMPLETE/CONTINUE/ESCALATE；CONTINUE 用剩余文本作 continuation message（空则用默认），ESCALATE 用剩余文本作 reason；无法匹配 → fallback
  - **Metadata 记录**：每次 decide 后写 `completion.llmJudgeVerdict`（原始首行或 "FALLBACK"）+ `completion.llmJudgeFallback`(Boolean)；token usage 累加到 `ctx.getTokensUsed()`
- Decision D1（standalone Judge，非 RuleBased→LLM 组合）、D2（fail-open default：错误/无法解析→Complete，符合 §5.3 "Judge 裁决是建议不是命令"）、D3（verdict 输出格式：首行单关键词）记录在 plan 中
- 新增 `TestLlmCompletionJudge`（45 个单元测试，0 failures）：覆盖 (a)-(u) 全部场景——三种 verdict、case-insensitive/trim/extra-lines 解析、unparseable/empty/null-message/call-throw/error-response fallback、custom fallback=Continue、null/blank/cancel guard（call count 断言）、maxContextMessages=3 截断、custom systemPrompt/temperature/model、metadata 记录、config 入参校验（null chatService/maxTokens≤0/temperature 越界/maxContextMessages<0）、goal fallback 链、system 消息排除、factory 方法
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（672 tests, 0 failures）
- No owner-doc update required（design §5.3 Phase 2 已指定"用规则或小模型"；实现细节属源码 per guide rule #14）
- Phase 2：新增 `TestLlmJudgeInReActLoop`（6 个端到端集成测试，0 failures）：
  - Scenario 1（Complete）：main 返回无工具响应 → Judge 返回 "COMPLETE" → loop exit `completed`，EXECUTION_COMPLETED 事件发布，metadata `completion.llmJudgeVerdict=COMPLETE`/`fallback=false`
  - Scenario 2（Continue→re-enter→Complete）：Judge 首次返回 "CONTINUE\n...requirement Y" → continuation message 注入 → main 第二次调用 → Judge 返回 "COMPLETE" → `completed`；main 与 Judge 各调用 2 次（独立性）
  - Scenario 3（fallback）：Judge 的 chatService 抛异常 → fallback Complete（fail-open）→ `completed`，无异常传播到 ReAct loop，`completion.llmJudgeFallback=true`/`verdict=FALLBACK`
  - Scenario 4（dead-loop protection）：Judge 始终返回 CONTINUE → `DEFAULT_MAX_COMPLETION_CONTINUES=3` 后引擎 force-exit `completed`；main/Judge/decide 各调用 4 次
  - 接线验证：`CountingJudge` decorator 确认 `LlmCompletionJudge.decide()` 从 `execute()` 的 no-tool-calls 分支被调用；`completion.llmJudgeVerdict` metadata 仅由 LlmCompletionJudge 写入（证明是 LLM Judge 而非 NoOp/RuleBased 在运行）
  - 独立性验证：main `IChatService` 与 Judge `IChatService` 为独立实例，各自计数器互不干扰
- 全量验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（678 tests, 0 failures）；向后兼容：`TestCompletionGateInReActLoop`(6) + `TestRuleBasedJudgeInReActLoop`(6) + `TestRuleBasedCompletionJudge`(29) 全部不变通过

### nop-ai-agent ChainRepairer 4-Stage 实现（Plan 164）

- Plan: `ai-dev/plans/164-nop-ai-agent-chain-repairer.md` → completed
- 替换 `NoOpToolCallRepairer` pass-through 的空壳语义，实现 roadmap L2-2 的"4-stage ChainRepairer"（确定性修复链）
- 新增 `io.nop.ai.agent.repair` 包（6 个源文件）：
  - `ToolSchemaParser.java` — XDEF 属性标注 schema 解析器（package-private helper）。解析 `<schema>` 子元素（工具调用根名）的 attributes + child elements 为 param-name→bare-type map。不使用 `ToolSchemaConverter`（对所有真实工具返回 null）
  - `ToolNameNormalizationStage.java` — Stage 1：case folding + separator normalization（`-`/`.`→`_`），与 `ctx.getAgentModel().getTools()` 精确匹配，唯一匹配时修正名称
  - `ArgumentStructureRepairStage.java` — Stage 2：schema-agnostic。保证 arguments 非空 Map；处理 null/JSON-string/one-element-array 包装；single-wrapper-key unwrap **排除**（无法安全区分冗余 key 与合法单对象参数）
  - `ArgumentValueCoercionStage.java` — Stage 3：schema-aware via `IToolManager.loadTool().getSchema()`。将字符串标量强制转换到 XDEF 声明的基本类型（int/long/number/float/double/boolean）；非基本类型（string/enum:*/full-path/list）不转换；模糊值不转换；IToolManager=null 时降级为 no-op
  - `ArgumentCleanupStage.java` — Stage 4：schema-aware。移除 null 值参数；移除 schema 中未声明的参数（noise）；缺失的 required 参数不伪造（pass-through 让下游报错）
  - `ChainRepairer.java` — 复合修复器，固定 4 stage 顺序（1→2→3→4），每 stage 的输出线程化到下一个；optional IToolManager 构造注入
- Opt-in 接线：
  - `ReActAgentExecutor.Builder.enableChainRepairer()` — 用 Builder 的 toolManager 构造 ChainRepairer。**默认仍是 `NoOpToolCallRepairer.INSTANCE`**（未调用此方法时无行为变化）
  - `DefaultAgentEngine.setToolCallRepairer(IToolCallRepairer)` — 组件 setter，thread 到 `resolveExecutor` 构建的 executor 中。未设置时 executor 默认 NoOp
- Decision D1（4-stage behavioral spec）+ D2（schema-via-IToolManager + XDEF parsing contract）记录在本 plan 中，为权威 spec
- 测试（55 个新增测试，0 failures）：
  - Phase 1：`TestToolSchemaParser`（7）、`TestToolNameNormalizationStage`（10）、`TestArgumentStructureRepairStage`（8）、`TestArgumentValueCoercionStage`（9）、`TestArgumentCleanupStage`（7）
  - Phase 2：`TestChainRepairer`（10）— composition spy/counter、stage ordering、IToolManager=null degradation、full repair chain with schema、never-returns-null、builder opt-in
  - Phase 3：`TestChainRepairerInReActLoop`（4 端到端测试）— malformed call（wrong-case name + string-encoded args + null noise）→ repaired end-to-end、NoOp default backward-compat、pass-through on clean call、repaired name flows to toolManager
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（627 tests, 0 failures）；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs` 无新增 finding
- Doc-sync: roadmap L2-2 ❌→✅；No owner-doc update required（glossary 已定义 `IToolCallRepairer` 为"工具调用修复链"；Decision D1 本计划为权威 spec）
- Deferred（已在 plan 中裁定）：LLM-based repair、schema-aware single-wrapper-key unwrap、DSL `<repair>` 配置、fuzzy tool-name matching、TOOL_CALL_REPAIRED 事件类型

### nop-ai-agent Skill Engine 实现（Plan 163）

- Plan: `ai-dev/plans/163-nop-ai-agent-skill-engine.md` → completed
- 构建第一阶段 Skill 引擎：结构化能力发现（`SkillModel` Scheduling 层）、文件系统注册中心（`*.skill.yaml`）、声明式匹配（`SkillResolver`）、装配结果注入（`SkillAssemblyResult`）、ReAct 循环集成
- 新增 `io.nop.ai.agent.skill` 包（8 个源文件）：
  - `SkillModel.java` — 手写 POJO（Decision (a)：手写模型，不走 xdef/codegen，可后续迁移）；Scheduling 层字段：name/goal/intentSignature/topPattern/dependencies/tags/resourceScope
  - `SkillTopPattern.java` — 枚举（PREPARE | ACT | VERIFY | MANAGE | RETRIEVE | TRANSFORM），phase-1 存储不使用
  - `SkillResourceScope.java` — 枚举（MEMORY | LOCAL_FS | CODEBASE | NETWORK | CREDENTIALS），phase-1 收集不强制
  - `ISkillProvider.java` — 发现契约接口（返回所有注册 skill，keyed by name）
  - `NoOpSkillProvider.java` — pass-through 默认（singleton + `noOp()` 静态工厂），发现零 skill
  - `FileSystemSkillProvider.java` — VFS 扫描 `*.skill.yaml`，缓存结果；缺目录→空集（零配置默认）；坏 YAML→fail-fast `NopAiAgentException`
  - `SkillResolver.java` — 声明式匹配（requiredSkills fail-fast + availableSkills 激活），去重处理（required/available 重叠只激活一次）
  - `SkillAssemblyResult.java` — 装配结果（instructions + toolDependencies + resourceScope + activatedNames），不可变
- Decision (b)：skill 工具依赖复用 `IToolManager.loadTool()` 管线（与 plan 160 talent 一致，design §7.4）
- ReAct 集成：
  - `ReActAgentExecutor.Builder.skillProvider()` + `consultSkills()` 方法（在 `execute()` setup 阶段与 `consultTalents` 并行调用）
  - skill 指令片段 → `injectSystemInstruction`（additive）；skill 工具依赖 → `loadTool()` + `toolDefs.add`（additive，skip-with-warning 同 talent）；resourceScope → DEBUG 日志（phase-1 不强制）
  - `DefaultAgentEngine.setSkillProvider()` setter + `resolveExecutor` 中 `.skillProvider(skillProvider)` 接线
- 测试（47 个新增测试，0 failures）：
  - `TestSkillModel`（10）、`TestISkillProvider`（2）、`TestNoOpSkillProvider`（4）
  - `TestFileSystemSkillProvider`（7）、`TestSkillResolver`（12）、`TestSkillAssemblyResult`（5）
  - `TestSkillEngineInReActLoop`（7 端到端集成测试）：默认 NoOp 不变（向后兼容）、availableSkills 注入指令+工具、missing requiredSkill fail-fast（0 LLM call）、talent+skill additive、skill 工具可调用、FileSystemSkillProvider E2E
- 样本 fixture：`_vfs/skills/{log-analysis,code-review,web-search}.skill.yaml` + `_vfs/skills-malformed/broken.skill.yaml`
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（572 tests, 0 failures, 0 errors）；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs` 0 新增 finding（14 pre-existing 均来自其他子系统）
- Doc-sync: design `skill-system-design.md` §8.1 reconciliation（移除 phase-1 的 topPattern 粗筛，与 §5.2 对齐）；roadmap §2.2 Hook/Skill 引擎 ❌→✅
- Deferred（已在 plan 中裁定）：topPattern 粗筛、intentSignature 精确匹配、scenes/Structural 层、resourceScope 权限叠加、skill-tool 存在性检查、ISkillCurator/LLMCurator、Delta 覆盖、SkillActivationPolicy

### nop-ai-agent RuleBasedCompletionJudge 实现（Plan 162）

- Plan: `ai-dev/plans/162-nop-ai-agent-functional-completion-judge.md` → completed
- 替换 `NoOpCompletionJudge` pass-through 的空壳语义，实现 design §5.3 Phase 2 的"轻量 Judge"
- 新增 `completion/CompletionRuleConfig.java`：配置 `minResponseLength`(default 10)、`escalationRatio`(default 0.9)、`continuationMessage`、`escalationReasonTemplate`，构造时验证入参
- 新增 `completion/RuleBasedCompletionJudge.java`：规则优先级顺序 empty → trivial → near-budget → complete；提供 `ruleBased()` / `ruleBased(config)` 静态工厂方法（遵循 `NoOpCompletionJudge.noOp()` 约定）
- 新增 `TestRuleBasedCompletionJudge`（29 单元测试）：覆盖三条决策分支、边界（minResponseLength、escalationRatio 0/1）、自定义配置、规则优先级、空 ctx 守卫、配置入参校验
- 新增 `TestRuleBasedJudgeInReActLoop`（6 端到端集成测试）：通过 `Builder.completionJudge()` 接线，覆盖 Continue→re-enter→Complete、Escalate→status escalated + 无 EXECUTION_COMPLETED 事件、正常 Complete、dead-loop protection 与规则 Continue 的相互作用、接线验证（CountingJudge decorator 确认 `decide()` 真的被 `execute()` 调用）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（525 tests, 0 failures, 0 errors），`TestCompletionGateInReActLoop` 无变化（向后兼容）
- Doc-sync: No owner-doc update required（design §5.3 Phase 2 已规定 "轻量 Judge：用规则或小模型"，实现细节属于源码）

### nop-ai-agent forkSession 实现（Plan 161）

- Plan: `ai-dev/plans/161-nop-ai-agent-session-fork.md` → completed
- 实现 `IAgentEngine.forkSession` + `ISessionStore.forkSession`（从 UOE 空壳到功能性实现）
- `InMemorySessionStore.forkSession`：查找父 session（fail-fast）→ UUID 子 sessionId → inheritContext 控制 消息快照拷贝/planId 引用/metadata 拷贝 → props 合并（agentName override + metadata）→ parentSessionId link
- `DefaultAgentEngine.forkSession`：解析 parent sessionId（null/empty fail-fast）→ 验证父存在 → 构建 props → 调用 store.forkSession → 发布 `SESSION_FORKED` 事件（payload: parentSessionId/childSessionId/inheritContext）→ 返回 `CompletableFuture.completedFuture(childSessionId)`
- `AgentEventType` 新增 `SESSION_FORKED` 枚举值
- 移除 `TestISessionStoreDefaultMethods.forkSessionThrowsUOE` 和 `TestIAgentEngineDefaultMethods.forkSessionThrowsUOE`
- 新增 `TestInMemorySessionStoreFork`（12 个单元测试）+ `TestDefaultAgentEngineFork`（7 个端到端测试，含 execute→fork→execute 独立性验证）
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` 全部通过（490 tests, 0 failures, 0 errors）
- Doc-sync: 更新了 `01-architecture-baseline.md` §四、`nop-ai-agent-context-model.md` §5.3/§5.4（移除 Plan 深拷贝矛盾）、`nop-ai-agent-session-engine.md` §6.2、`nop-ai-agent-react-engine.md` §3.2、`nop-ai-agent-session-and-storage.md` §9.2

### 2026-06-14 ProfileConfigSource 改为 per-source 解析

- **问题**：`ProfileConfigSource` 在所有 source 合并后统一应用 profile 覆盖，导致低优先级 source（`application.yaml`）的 `%dev.xxx` 值覆盖高优先级 source（系统属性 `-D`）的裸 `xxx` 值，与 Quarkus/Spring 行为不一致
- **修复**：`ConfigStarter.doStart()` 中改为对每个 `IConfigSource` 逐个包装 `ProfileConfigSource` 后再 `CompositeConfigSource` 合并，使 profile 解析在 source 内部完成，跨 source 时 source 优先级决定最终值
- **性能优化**：`IConfigSource` 新增 `hasProfilePrefixedVars()` 方法，`StaticConfigSource` 构造时预计算标志位。`ConfigStarter` 只对包含 `%` 前缀变量的 source 包装 `ProfileConfigSource`，系统属性和环境变量等 source 直接跳过
- **改动文件**：
  - `nop-config/.../starter/ConfigStarter.java`：per-source profile 解析 + hasProfilePrefixedVars 判断
  - `nop-config/.../source/IConfigSource.java`：新增 `hasProfilePrefixedVars()` default 方法
  - `nop-config/.../source/StaticConfigSource.java`：构造时预计算标志位
- **文档更新**：`docs-for-ai/02-core-guides/ioc-and-config.md` 新增"配置优先级"表格和"Profile 解析机制"说明
- **验证**：`java -Dnop.datasource.jdbc-url=jdbc:h2:mem:e2e -Dquarkus.profile=dev -jar ...` 正常启动（11.3s），`-D` 参数成功覆盖 `%dev` profile 的 jdbc-url 值，无 H2 文件锁冲突
- 偏差：无
- doc-sync：更新了 `docs-for-ai/02-core-guides/ioc-and-config.md`

### 2026-06-14 新增 VFS 虚拟文件系统文档

- **问题**：`docs-for-ai/` 缺少 VFS 虚拟文件系统的使用层文档。核心概念（路径解析顺序、类扫描发现、模块无关性、重复检测）分散且不完整
- **新增文件**：
  - `docs-for-ai/02-core-guides/vfs-and-resource-resolution.md` — 完整 VFS 指南：解析链、类扫描、模块无关性、重复检测、namespace 前缀、配置项、常见操作
- **更新文件**：
  - `docs-for-ai/INDEX.md` — 新增 VFS 路由入口条目
  - `docs-for-ai/02-core-guides/delta-customization.md` — 相关文档中增加 VFS 链接
  - `docs-for-ai/04-reference/source-anchors.md` — 新增 VFS-001 ~ VFS-006 实现锚点
- **验证**：文档链接检查通过

### 2026-06-14 Plan 129: DataInitInitializer 启动时 CSV/SQL 数据初始化

- **Plan**: `ai-dev/plans/129-orm-auto-init-database-data.md`
- **功能**：在 `DataBaseSchemaInitializer`（DDL 建表）之后，新增条件 bean `DataInitInitializer`，从可配置的 VFS 目录读取 CSV 按拓扑序插入数据，并执行 SQL 文件
- **改动文件**：
  - `nop-orm/.../OrmConfigs.java`：新增 `CFG_INIT_DATABASE_DATA` 和 `CFG_INIT_DATABASE_DATA_LOCATION` 配置项
  - `nop-orm/.../initialize/DataInitInitializer.java`：核心实现（CSV 加载 + SQL 执行）
  - `nop-orm/.../resources/_vfs/nop/orm/beans/orm-defaults.beans.xml`：注册条件 bean
  - `nop-orm/src/test/.../TestDataInitInitializer.java`：4 个测试用例（正常加载/空CSV/列名不匹配/纯SQL）
  - `nop-orm/src/test/resources/_vfs/_test-init-data*/`：测试数据文件（CSV + SQL）
  - `docs-for-ai/02-core-guides/orm-model-design.md`：新增"启动时数据库初始化"章节
- **关键调试**：
  - SQL 文件发现使用 `VirtualFileSystem.instance().getAllResources(dir, ".sql")` 而非 `getChildren`（后者在 InMemoryDirResource 上返回空）
  - SQL 执行需包裹在 `jdbcTemplate.txn().runInTransaction()` 中以确保事务提交
  - 测试 SQL INSERT 需包含 `TENANT_ID` 列，因为 ORM 按租户过滤（raw SQL 不经过 ORM 自动设置租户）
- **验证**：`./mvnw test -pl nop-persistence/nop-orm -Dsurefire.failIfNoSpecifiedTests=false` 全部 136 测试通过
- 偏差：无
- doc-sync：更新了 `docs-for-ai/02-core-guides/orm-model-design.md`

### 2026-06-14 nop-job 使用层面文档补全

- **背景**：用户反馈 `docs-for-ai/03-modules/nop-job.md` 只描述分布式模式，缺少本地模式（LocalJobScheduler）和实际使用步骤（依赖、IJobInvoker 注册、bean 命名约定、配置项）
- **改动文件**：
  - `docs-for-ai/03-modules/nop-job.md`：从 78 行重写为完整使用指南，覆盖本地模式 vs 分布式模式对比、Maven 依赖坐标、IJobInvoker SPI 契约、`nopJobInvoker_<executorKind>` bean 命名约定（来自 `DefaultJobInvokerResolver.INVOKER_PREFIX`）、JobFireResult 返回值约定、触发器类型表、BizModel 管理命令、配置项参考表（从 `@InjectValue` 源码提取）、源码锚点
  - `docs-for-ai/INDEX.md`：更新 nop-job 路由描述为"定时任务：本地模式 + 分布式模式"
- **源码验证的关键事实**：
  - `nop-job-core` 无 `_vfs/` bean 定义 → LocalJobScheduler 不自动注册，需手动构造
  - `JobCoordinator`/`JobWorker` 继承 `LifeCycleSupport` → 分布式模式引入即自动启动 scanner（单实例也能跑）
  - `JobSpec.jobInvoker` Javadoc 写 `jobInvoker_` 前缀，但 `DefaultJobInvokerResolver` 实际用 `nopJobInvoker_` 前缀 → stale doc，已在模块文档中标注权威约定
  - `NopJobConfigs` 类为空接口 → 配置项直接从各 scanner 类的 `@InjectValue` 提取
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` → exit 0，0 errors
- Doc-sync: `docs-for-ai/03-modules/nop-job.md` 已重写
