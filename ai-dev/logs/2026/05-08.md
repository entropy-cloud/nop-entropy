# AI 开发日志 — 2026-05-08

### 2026-05-08

- 在形式证明中补充跨层次交错示例（§5.1a 和 §6.9a），覆盖 remove 后 merge 重建、replace 清空后代后 merge 恢复、三级交错（ancestor replace + middle remove + descendant merge）以及 remove 后代后 merge 恢复但 ancestor replace 覆盖全部等场景。
- 相关路径：`docs/theory/grc-delta-associativity-formal-proof.md`。
- 关键决策：示例验证了两种 carrier 都天然支持跨层次交错——坐标 carrier 通过逐坐标 LWW，端函数 carrier 通过 `Lift` 从 ∅ 重建虚拟节点和 NF 吸收规则。无需修改任何定理陈述。

### 2026-05-08

- 继续对 `docs/theory/grc-delta-associativity-formal-proof.md` 做发表前形式化复审，修复顺序约束 LWW 误用、`bounded-merge` 非普通 `Merge(D)`、forbidden 组合提前失败、`G` carrier 混用、`Dom(E)` 前提和 `ValState_O/Apply/Final_O` 定义不足等问题。
- 相关路径：`docs/theory/grc-delta-associativity-formal-proof.md`、`nop-task.md`。
- 关键决策：抽象定理仍只覆盖三类明确定义 carrier；真实 Nop/XLang 实现继续作为第 6.11 节证明义务，不能把 `OverrideHelper` 全表或 `bounded-merge` 直接纳入已证明闭包。
- 复审结果：最终子 agent 复查 PASS；未找到满足当前明示前提的定理 3 或 4A/4B/4C 反例，残余风险限定为实现符合性与可序列化 artifact 闭包证明义务。

### 2026-05-08

- 对 `docs/theory/grc-delta-associativity-formal-proof.md` 执行多轮子 agent 对抗性审查并修订到技术报告/附录发表标准。
- 相关路径：`docs/theory/grc-delta-associativity-formal-proof.md`、`docs/theory/generalized-reversible-computation-paper-v2.md`、`nop-task.md`。
- 关键决策：形式证明拆分为坐标 tombstone、局部操作、树端函数三类 carrier；新增 typed root/tombstone 坐标、`MergeTree^P` 定义域闭包、`Final_P/Final_O/Final_S` 类型分离、Nop/XLang 覆盖矩阵和实现证明义务。
- 同步主文：将 Nop/XLang 参考实现、`x:before/x:after` 和完整树操作覆盖表述降级为条件化/证明义务，避免声称当前实现所有分支已被机械化证明。

### 2026-05-08

- 使用独立子 agent 多轮复核 `generalized-reversible-computation-paper-v2.md` 与 `grc-delta-associativity-formal-proof.md`，补回“变化空间设计/语言即坐标系/最小信息表达/变化资产化”等锋芒，并修正 Nop 语义细节。
- 关键决策：`x:extends="A,B"` 按 Nop 保序解析承诺为 `A ⊕ B`；`x:after` 表述为加载期顺序约束并规范化到通用 merge，最终按 stable key 只保留唯一节点；形式证明显式区分坐标 tombstone carrier、局部幺半群 carrier 和树端函数 carrier。
- 复核结果：paper v2、形式证明、Nop 语义三类子 agent 最终均 PASS；证明覆盖 `merge/remove/replace`、ancestor/descendant overlap、stable-key children、ordered constraints 和 `Final/Result` 边界。
- 用户确认：Nop 的 Map/Set 按解析顺序保序；`x:after` 的真实实现是规范化为通用 merge 语义，而不是运行期插入动作或额外节点保留机制。

### 2026-05-08

- 新增 `docs/theory/generalized-reversible-computation-paper-v2.md`，将 GRC paper 重写为可投稿的范式/系统论文稿，正文收束到语义坐标系、差量代数、Nop/XLang 实现和产品线案例，严格结合律证明引用 `grc-delta-associativity-formal-proof.md`。
- 关键决策：形式化要求与 DOP/FOP 文献保持同类，只证明明确定义的核心 calculus；在引言和相关工作中明确 GRC 核心思想公开提出早于 DOP，但不以时间先后替代理论和工程论证。

### 2026-05-08

- 重写 `docs/theory/generalized-reversible-computation-paper-review.md`，按“明确判决”方式评估 GRC paper 的发表水平、创新性、硬伤和投稿路线，并同步 `docs/theory/index.md` 描述。
- 关键决策：评价结论不做温吞平衡；判定其具有高创新性和白皮书/预印本发表价值，但当前形态不适合直接投严格学术 venue，需收束为理论/系统/经验三类之一。

### 2026-05-08

- 继续修订 `docs/theory/generalized-reversible-computation-paper.md`，按独立审查的 P1 问题对齐补强后的结合律证明边界。
- 相关路径：`docs/theory/generalized-reversible-computation-paper.md`、`docs/theory/grc-delta-associativity-formal-proof.md`、`nop-task.md`。
- 关键决策：原 paper 中 `Norm/Pr/Validate` 只作为合并链结束后的确定性后处理；`replace/remove/merge` 与 stable-key children 的结合律只在潜在结构空间和语义商上成立；附录 G 的伪代码必须保留 tombstone、virtual node 和顺序约束证据。第一轮 paper 复审后继续软化新颖性、相关工作、案例证据和 Docker/OpenUSD 类比。
- 复审结果：第二轮 3 个独立子 agent 均给出 PASS；残余风险是案例仍为说明性证据，尚无系统实证数据。
