# 2026-05-27 Dev Log

## nop-stream 模块审计与修复（Plan 62 执行）

### 审计发现

三轮并行审计（接口契约、组件接线、测试覆盖）发现：

| 类别 | P0 | P1 | P2 |
|------|----|----|-----|
| 接口契约 | 1（经验证4个误报） | 8 | 5 |
| 组件接线 | 1 | 1 | 2 |
| 测试覆盖 | 3 | 9 | 4 |

### 已修复（Plan 62）

1. **SimpleStreamOperatorFactory** — 添加 NotSerializableException 优雅降级，非序列化算子返回共享实例而非崩溃
2. **CepOperator** — 将 SimpleKeyedStateStore 回退替换为 MemoryKeyedStateBackend，提供 namespace 隔离
3. **StreamSinkOperator.processWatermark()** — 从空方法体改为推进计时器服务（不转发，因为 sink 无下游）
4. **MemoryKeyedStateBackend.getReducingState()** — 实现完整，新增 MemoryReducingState 内部类
5. **CheckpointType.fromName()** — 从返回 null 改为抛 IllegalArgumentException

### 已验证的设计正确点

- JobGraphGenerator.getRawOperator() — 经分析，当前架构下每个 JobVertex 只调用一次 createOperatorFromFactory，并行 Task 共享 JobVertex 是设计上的，P0 降级为架构改进建议
- JdbcCheckpointStorage 使用 IJdbcTemplate ✅
- Barrier 注入使用 source-pull 模式 ✅
- Keyed state 按 (namespace, key) 复合键隔离 ✅

### 测试状态

- 300 tests, 0 failures, BUILD SUCCESS

## 对抗性审查（阶段 B）

审查发现 6 个 P1 问题，经验证大多为已知设计限制：

| # | 发现 | 验证结果 | 优先级 |
|---|------|---------|--------|
| F-05 | HeapInternalTimerService processing time timer 空实现 | 确认。测试用 MockInternalTimerService 绕过。Processing time 窗口在生产环境下不会触发 | P2（已知限制） |
| F-08 | TwoPhaseCommitSinkFunction.saveState() 返回 null | 确认。但无生产代码实现此接口（只有测试类），不影响用户 | P2 |
| F-10 | setTasksToAcknowledge 非原子 | 理论问题。PendingCheckpoint 只在 coordinator 单线程中使用 | P2 |
| F-13 | acknowledgePrecedingCheckpoint 未实现 | 确认。但无调用方 | P2 |
| F-03 | participant commit 失败后悬挂 transaction | 设计限制，需要后续引入重试机制 | P2 |
| F-07 | restoreFromEpoch 吞 rollback 异常 | 确认，应添加日志。当前不影响正确性 | P2 |

**结论**：对抗性审查未发现新的 P0/P1 live defect。所有发现均为已知设计限制或理论并发问题，归入 Non-Blocking Follow-ups。

### Plan 64: Error Codes, Exception Unification & TypeSerializer Dedup

- 创建 `NopStreamErrors.java`，定义 10 个 ErrorCode（null arg, invalid state, config error, unsupported, serialization, operator error, checkpoint error, state error, invalid arg, init error）
- 重命名 `typeinfo.TypeSerializer` → `SimpleSerializer`，消除与 `typeutils.TypeSerializer` 的同名歧义
- 迁移 93 处硬编码 `StreamException` 到 ErrorCode 模式（覆盖 34 个文件）
- 替换 3 处生产代码 `RuntimeException` 为 `StreamException`（MemoryKeyedStateBackend, EmbeddedDistributedExecutor, PendingCheckpoint）
- 统一 NFA/SkipToElementStrategy 中 7 处 `StreamRuntimeException` → `StreamException`
- 升级 GraphModelCheckpointExecutor 4 处关键操作 LOG.warn → LOG.error
- 300 测试全部通过
