## nop-stream 错误处理硬化（Plan 66，Phase 3 完成）

**完成项**:
- `StreamExecutionEnvironment` 9 处 `IllegalStateException` 替换为 `StreamException(ERR_STREAM_INVALID_STATE)`
- `TaskExecutor` 3 处 `IllegalStateException` 替换为 `StreamException`
- `Task.closeOperatorChains` 关闭异常附加到 `this.error` 字段（作为 suppressed 或直接赋值）
- `SubtaskTask.closeOperatorChains` 同步修改

**新增/修改测试**: 修复 `TestTaskExecutor`、`TestDataStreamPipeline` 中 `assertThrows` 类型

**验证**: 全量测试 307+ 通过。Closure audit 通过（8 项代码抽查全部 ✅）。Plan checklist 通过。

# 2026-05-27 开发日志

## nop-stream 错误处理硬化（Plan 66，Phase 1 & 2）

**范围**: nop-stream-core, nop-stream-cep, nop-stream-runtime

**完成项**:
- P1 修复：`MalformedPatternException` 改为继承 `StreamRuntimeException`（而非 `RuntimeException`）
- P1 修复：`TwoPhaseCommitSinkFunction.restoreFromEpoch` rollback 失败时添加 `LOG.warn`（原为空 catch 吞异常）
- P1 修复：`GraphModelCheckpointExecutor.triggerTerminalSavepoint` 失败时抛出 `StreamException`（原为静默吞掉）
- P1 修复：`NopStreamErrors` 和 `NopCepErrors` 全部 ErrorCode 消息从中文改为英文

**新增测试**:
- `TestMalformedPatternException.java` — 验证继承关系和 catch 行为
- `TestTwoPhaseCommitSinkFunction.java` — 验证 rollback 失败不抛异常
- `TestErrorCodeMessagesEnglish.java` — 验证 ErrorCode 消息不含中文

**审计**: 完成多维度深度审计（01/02/09/15/16/17），发现 5 P1 + 20 P2 + 22 P3。审计结果写入 `ai-dev/audits/2026-05-27-deep-audit-nop-stream-r1/`

**验证**: 全量测试 307+ 通过（含新增测试），构建通过。

**待做**: Phase 3（IllegalStateException 统一替换）属于 P2，后续执行。
