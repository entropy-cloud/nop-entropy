# 2026-08-24


### 回退 AutoTestHelper delVersion 变量屏蔽（精确撤销 b7d7010a19 的 +3 行，来源 nop-app-erp plan 2026-08-24-0900-1）

- **背景**：commit `b7d7010a19`（2026-07-15）在 `nop-autotest/nop-autotest-core/.../execute/AutoTestHelper.java` 的 `isVarCol` 中加入 deleteVersionPropId → true 判断（+3 行），commit message 未提及、无理由、无测试。经技术核实为错误决策：delVersion 插入时由 `OrmEntityIdGenerator` 硬编码初始化为 0（确定性值），无 seq/createTime/clock 类非确定性需要消除；屏蔽反而永久失去 delVersion==0 及逻辑删除递增值的快照断言能力，且 `AutoTestVars.VarsMap` putIfAbsent 命名使捕获顺序变化产生 `delVersion`↔`delVersion_1` 伪 diff（nop-app-erp 2026-08-24 实证：`erp_md_subject.csv` 下标漂移）。
- **改动**：删除 `isVarCol` 中 `getDeleteVersionPropId` 判断块（3 行），其余规则（TAG_SEQ/TAG_VAR/TAG_CLOCK/updateTime/createTime）不动。回退后 blob 恰为 b7d7010a19 前版本（`087a8a25ef`）。保护区域双独立子 agent 批准（nop-app-erp 侧 task `ses_fcc4f760dffe8pi5vbGg11uKGs` / `ses_fcc4f26e7ffeKMG478vFBi15XH`，均 APPROVE；审计补充事实：逻辑删除路径 delVersion=currentTimeMillis 非确定，但 D 行仅做 absence 校验且全消费仓仅 1 处 D 行，无断言影响）。
- **修正案（同日，回退后先导重录暴露的缺口）**：逻辑删除是 UPDATE 而非 ORM delete，被捕获行为 `_chgType=A` 逐字段值校验，DEL_VERSION 时间戳逐次漂移致 3 例 CHECKING 失配（先导模块实证）。「delVersion 确定性」前提对已删除行不成立。修正：`TagVarCollector` 对 deleteVersionPropId 列值条件掩码——`Number && longValue()!=0` → 记录 `*`（既有 MATCH_ALL_PATTERN 通配），`0` 保持字面值断言。经第二组双独立子 agent 批准（task `ses_fcc2b9589ffegvHabZ1DFWZA87` / `ses_fcc2b20a2ffeLZBr3VCEkQ829u`，均 APPROVE，含 FK 零命中/propId≥1/input 侧不受影响等边界实证）。先导域复重录后全绿（155/155、309/309）。
- **影响面**：nop-entropy 自身 0 受影响快照（`rg "@var:[A-Za-z]+@delVersion" -g '*.csv'` = 0，无 `_cases` 数据、无 isVarCol 单测）。下游 nop-app-erp 约 19k 处单元格引用需以 `-Dnop.autotest.force-save-output=true` 全量重录（其 plan 2026-08-24-0900-1 Phase 2/3 承载，本仓无进一步义务）。
- **验证**：`mvn clean install -DskipTests -T 1C` BUILD SUCCESS（exit 0）；`mvn test -pl nop-autotest/nop-autotest-core` BUILD SUCCESS（exit 0）；安装构件 bytecode 无 DeleteVersion 引用。
- **Deferred**：向上游 canonical_entropy 反馈本回退说明由 nop-app-erp plan Deferred 节承载（触发：下次上游同步）。
- Doc-sync: No owner-doc update required（平台行为恢复至 b7d7010a19 之前的长期基线；消费侧认知差由 nop-app-erp `docs/testing/e2e-runbook.md` 注记承载）。

### flux-web CRUD queryForm 缺省水平布局 + 统一 labelWidth（来源 nop-app-erp 查询表单布局对齐诉求，用户批准跳过审计直接实施）

- **背景**：nop-app-erp flux 渲染下 CRUD 查询表单 label 在控件上方（竖排）。溯源结论：非 nop-chaos-flux 缺陷——flux 渲染器 `mode` 缺省 `'normal'`（form-definition.ts:159）是 2026-08-10 用户裁定的语义正确缺省（曾改缺省被纠正回退，见 nop-chaos-next docs/logs/2026/08-10.md 续6）；真正缺口在本仓生成层 `grid_crud.xpl` 的 `<queryForm>` 未输出任何 layout 属性，而 2026-08-10 的水平配置只落在宿主原型页 products.json，对后端生成 schema 不生效。
- **改动**：`nop-web/src/main/resources/_vfs/nop/web/xlib/flux-web/grid_crud.xpl` `<queryForm>` 新增 `layout="horizontal"` + `labelWidth="${filterForm.labelWidth || $config.var('nop.xui.crud.query-label-width') || 80}"`（view.xml form 级 labelWidth → 配置变量 → 缺省 80 三级覆盖；crud 与 picker 共用此模板一并生效；aside 高级筛选表单不在本次范围）。渲染端消费链已核对：data-schema-validation.ts:23/35-41 映射 layout/labelWidth → form.tsx:355 FormLayout 下发 → field-frame.tsx:210-220 同行右侧对齐。不改渲染器缺省，符合「显式设置必须语义正确」既定裁定。
- **验证**：TestFluxWebCrudPage 10/10（新增 testQueryFormHorizontalLayout 断言 layout=horizontal + labelWidth 数值 80）；nop-web 全部 TestFlux* 62 run 0 fail（1 skip 为存量）；nop-app-erp ErpAllFluxPagesTest 全量页面合法 flux JSON 绿。

### docs-for-ai：flux picker 契约 + delta-xlib 定制 + gen-control 清理决策树 + mode 退化链四处补齐（来源 nop-app-erp plan 2026-08-24-1147-1 复盘）

- **背景**：nop-app-erp 修复「未配置 picker 弹层」全仓症状（plan `2026-08-24-1147-1`，已 closure）过程中暴露 docs-for-ai 四处文档空白，导致修复计划三轮返工（v1 目标文件错位 / v2 修复点错位 / 实施期 gen-control 处理方式绕弯）。经 nop-app-erp 侧用户批准（豁免双子代理审批），本次补齐：
- **补丁清单**（均为文档，零代码/零契约变更）：
  1. `02-core-guides/flux-rendering.md` 新增 **§picker 字段级 schema 契约（跨仓库消费端）**：AMIS 键 ↔ Flux 键对照表（valueKey/labelKey 而非 valueField/labelField——P0 教训；pickerDialog 必需性；loadAction 格式；joinValues/extractValue 静默忽略）、bizObjName 派生链陷阱（propMeta 非关系元数据时 getRefBizObjName 返回 null → 字面拼接 null__findPage，正确姿势 relProp 优先 + 安全退化）、delta 覆盖 flux-control.xlib 具体写法、手写 `<gen-control>` AMIS 残留清理决策树（纯形态声明删除 / 含业务逻辑保留转换）。
  2. `06-extensibility/how-to-use-extensibility-in-business-implementation.md` 场景 2 扩充 **Delta 不只适用实体/页面——xlib 同样可以**：delta-xlib 同名覆盖 + `x:extends="super"` tag 级 merge 完整示例（含加载顺序说明与"勿整份复制"警示），指向 nop-app-erp 真实样例。
  3. `03-runbooks/replace-field-with-complex-control.md` 头部加 **Flux 时代注记**：新增控件须按当前渲染引擎契约返回 schema；存量纯 AMIS picker gen-control 应删除（DefaultControl mode 退化链自动接管）；判断标准（有无 onEvent/validations 业务逻辑）。
  4. `02-core-guides/frontend-rendering-pipeline.md` §控件匹配链扩充 **mode 退化链**：list-view→view-* / list-edit→edit-* 的精确退化路径（源码定位 XuiHelper.java:127-145）、view-any 兜底语义、三条实践含义（子表关联列无需手写 gen-control / 只读列出现编辑控件的根因排查入口 / 控件缺失表象是 label 文本非抛错）。
- **验证**：纯文档变更无构建门禁；四处置入内容均经实仓源码核对（XuiHelper.java 行号、picker-renderer.tsx:56-57/325-327、composite-schemas.ts PickerSchema 定义、nop-app-erp delta xlib 实样）。
- **关联**：nop-app-erp `docs/lessons/16-cross-repo-schema-contract-consumer-source-verification.md`（方法论沉淀）+ 其 `docs/logs/2026/08-24.md` 同日条目。

### 独立子代理文档审计（explore, task `ses_fccef0ef5ffeOs9D0RfkImCyg0`）：needs revision → 已修复

- **审计范围**：上节四处 docs-for-ai 补丁 + nop-app-erp lesson 16 + 两份日志 + README 注册，共 7 处改动。
- **结论**：`needs revision`——技术主体全部经得起源码回放（两层退化链逻辑、派生链顺序、null 兜底、契约字段集、测试断言、日志对应均准确），但发现 1 P0 + 3 P1：
  - **P0**：契约表 pickerDialog「必需」表述与源码不符——warning 仅在 `!hasPickerDialog && options.length===0 && !crudMode` 三者同时成立时触发；有 loadAction/options 时缺 pickerDialog 照常打开（默认标题）。已改为条件化表述并区分判定点(:58-59)与 warning 出口(:325-327)。
  - **P1-1**：gen-control 清理注记的 mode 退化链示例缺「须显式声明 editMode=list-edit」限定——默认 list-view 列退 view-* 只读形态非 picker。已补限定 + 交叉引用。
  - **P1-2**：mode 退化链小节把 view-any 兜底并入内层 ：127-145 锚点，实际兜底在外层 :99-101。已拆分两层锚点表述。
  - **P1-3**：gen-control「双模式通用」与正文 AMIS 专属示例（tree-select ↔ flux 实为 type:'tree'）存在张力。已补组件名换算警示。
- **P2 同步处置**：tag 计数统一 75→78（含 x:prototype 别名，含既有 L42/L526 存量处）；delta 样例路径补全 app-erp-all 前缀；~70→73 口径统一。全部落地。
- **回放检验结论（审计者给出）**：五个历史坑（controlLib 重写盲区 / 项目级 control.xlib 错位 / gen-control 绕路 / valueField-valueKey / null__findPage）全部被新文档覆盖拦截，其中坑(4)达四层拦截（文档×3+测试断言门禁）。

### Plan 346 第六单元：check2 gateway-bizauth 18 条处置（Authorization 头大小写/拒绝响应丢失/开放重定向）

- Plan 346 第六单元 `ai-dev/audits/check2/gateway-bizauth.md`（P0×2 P1×4 P2×5 P3×7）：18 条全部修复（4 个嵌套暂缓子项附决策点），18/18 标注。
- P0×2 同根因（生产链路 Vertx/Servlet 均小写化 header key，混合大小写读取恒 null）：AiAuthGatewayInterceptor 读 "Authorization" 曾启用即全量 401，改 `IHttpServerContext.HEADER_AUTHORIZATION` 小写常量；AiRateLimitGatewayInterceptor 读 "X-Forwarded-For" 曾限流 key 全塌缩 default（全局共享 1 QPS 自我 DoS），改 x-forwarded-for + normalizeKey。
- 关键修复：P1 GatewayRejectException 双丢失路径（同步抛出穿透 processRoute 绕过 exceptionally + 异步失败被 ErrorMessageManager 改写 httpStatus=0，叠加旧 writeErrorResponse 兜底 200，客户端实收 HTTP 200——比审计"通用 5xx"更严重，标注修正报告事实；GatewayHandler.exceptionly 原样返回 401/429+Retry-After）；BufferedStreamingPublisher demand=0 收 onComplete 丢缓冲尾部（pendingComplete 挂起终态，request(n) 冲空后补发）；限流桶 Map 无淘汰+key 可伪造（与 P0-2 耦合统筹：LocalCache TTL=refill 周期+maximumSize，key 收敛 IP 形态；可信代理解析暂缓——CFG_AUTH_TRUST_FORWARDED_TENANT 同型开关，默认值影响 LB 后/直连两类部署）；isAllowedRedirectUri startsWith 前缀绕过（URI 边界判定封堵 host 后缀/userinfo/端口伪装三变体）。P2: API key maskKey/debug 日志去 token 原文/两 Local store maxEntries 上界驱逐/Retry-After 三路径钳制 30s/fallback 默认剥离 authorization·x-api-key·cookie（显式开关逃生）+maxRetries 落地。P3: forward(null) 抛 IAE/filterHeaders 双侧小写/checkFileExt 双侧 toLowerCase/configLoaded volatile/ChunkFileUploadHandler 三空 API 抛 UnsupportedOperationException/错误响应兜底 500/getOrCreateInterceptor volatile。
- 验证：新增 30 用例/6 新测试类（TestGatewayRejectResponse/TestForwardProcessor/TestGatewayHttpFilterErrorResponse/TestNopFileStoreBizModel/TestChunkFileUploadHandler 等，biz-file-core 首建测试基建补 junit 依赖）；另修正 6 个既有用例 header 构造为生产小写形态（消除审计指出的测试失真）；stash 红验证失败形态逐条吻合（`GatewayRejectException: 401`/IP B 首请求 429/`expected 203.0.113.10 but was default`/`was 86400000`/`.attacker.com expected false was true`/fallback 头含 `authorization=Bearer sk-primary-key`/`maxRetries expected 1 was 3` 等）；nop-gateway 88 + biz-auth-core 70 + biz-file-core 5 共 163 tests 绿。
- 新发现（只记录）：LocalSmsCodeStore 同源仅惰性清理模式；AiRateLimit 拒绝响应 Retry-After 恒静态 refillIntervalMs/1000 不反映真实等待；单模块测试从 target/project-local-repo 解析兄弟 SNAPSHOT jar 可落后源码（nop-router 旧 jar 曾致 P1-1 红验证假红，已 install 刷新）。
- Doc-sync: No owner-doc update required（均为缺陷收敛；fallback 剥离凭证与 verifyPeer 同属安全缺省增强，缺省行为更安全方向）。

### Plan 346 第五单元：check2 nosql-cdc 25 条处置（PubSub 串投/ClientResources 泄漏/P0 RESP3 实测推翻）

- Plan 346 第五单元 `ai-dev/audits/check2/nosql-cdc.md`（P0×1 P1×2 P2×9 P3×13）：22 修复 + 1 复查非问题 + 2 暂缓，25/25 标注。nop-cdc 无发现（仅空常量接口）。
- P0 复查非问题：LettuceRateLimiter RESP3 MULTI+boolean 必抛 UnsupportedOperationException——审计字节码两点属实（默认协议 RESP3、NestedMultiOutput 无 set(boolean)），但核心前提不成立：真实 Redis 在 EVAL 返回值转换中把 Lua boolean 恒扁平化为整数 1/null，raw socket `HELLO 3` + `EVAL "return {true,42}"` 于 redis 6.2.24/7.2.16/7.4.11/8.10.1 四版本实测均回 `:1`/`:42`，lettuce 实客户端返回 [Long 1, Long 42] 无异常，set(boolean) 分支不可达。误报根因：只用 javap 推断未接真实服务端。
- 关键修复：P1 PubSub 单连接广播缺 topic 过滤（串投，`!topic.equals(channel)` 守卫）；P2 cancelled 标志污染同 topic 其他订阅+resume 复活已取消订阅（状态下放 PubSubSubscription）/ClientResources 每次 doStart 新建不 shutdown（Netty 线程泄漏，线程计数红 167→179）/useSsl 强制 withVerifyPeer(false)（新增 verifyPeer 默认 true）/NosqlCache expireAfterAccess NPE/getAll 不过滤缺失 key/空集合参数守卫补全（putAllAsync 等同族遗漏）/getTopN(0) 全量返回/parseTokenCount 小数 NFE/hash removeIfMatch 非原子（新 hash_remove_if_match.lua，伴生修复数值型值匹配恒 false）。P3 11 处：死常量+死 lua 删除/parseHostPort 校验+IPv6/sentinelNodes 配置半成品落地（lettuce sentinel 模式混用 host/port 实测抛 ISE）/session_set_field.lua EXISTS 守卫防僵尸 session/PubSubConsumeContext.sendAsync 静默吞消息改真实 publish/订阅失败回滚 stale entry/forEachEntryAsync N+1 改 mget/接口 javadoc 归位/RateLimiter 构造 Guard 校验 rate>0。
- 暂缓 2 条：PrefixTextCodec Number/Boolean 往返退化（调用侧在 nop-auth=ask-first、PrefixEncodeHelper 在 nop-core=plan-first，两侧均越界；已落接口 javadoc 值类型契约）；`$d:` 任意类加载白名单（平台级安全决策）。
- 验证：新增 25 用例（TestLettuceNosqlService +14 docker-gated、新 TestLettuceRedisConnectionProvider 6、新 TestLettuceRateLimiterValidation 3、nosql-core 首建测试目录 TestNosqlCache 2 + pom 补 junit 依赖）；stash 红验证 17 处（docker 13 + dockerless 4）失败形态逐条吻合；无 docker 76 run/0 fail（67 skip 均 docker-gated），带 `-Dnop.test.docker.enabled=true` 76/0/0 全跑通，`-am` 链含 nop-cdc BUILD SUCCESS。环境备注：Docker Hub 不可达，测试镜像经 daocloud 拉取重打标签。
- 报告事实纠正 2 处（P2-6 空集合失败形态实为 lettuce 客户端 IAE 非服务端 wrong-number-of-args；putAllAsync 实无防护，审计引用的是 sync putAll）写入标注未改原文。
- 新发现（只记录）：RoundRobinSupplier 构造函数即同步建连，无可达 Redis 时 provider.start() 直接失败（连接非懒建立，泄漏测试因此必须 docker-gated）。
- Doc-sync: No owner-doc update required（均为缺陷收敛与配置项补全，verifyPeer/sentinelNodes 为新增可选配置，缺省行为不变）。

### Plan 346 第四单元：check2 db-migration 16 条处置（xdef 变更类型 CCE 根因修复/引擎接线补全）

- Plan 346 第四单元 `ai-dev/audits/check2/db-migration.md`（P0×2 P1×4 P2×7 P3×3）：16 条全部修复，16/16 标注（另超审计项处置 1 节）。
- 关键修复：P0-1 17 种 xdef 声明变更中 9 种解析即 ClassCastException——根因在 migration.xdef（key-attr 被游离 `">` 截断、变更/precondition 模型缺 `xdef:bean-extends-type`、preconditions 集合丢 bean-sub-type-prop 声明），改 xdef 后用官方生成器 `XCodeGenerator.renderModel('/nop/schema/db-migration/migration.xdef','/nop/templates/xdsl')` 再生 19 个 `_gen` 模型类（换基类非手改），MigrationFileScanner 映射表扩至 16 种执行器并递归回填 dbTypeFilter 嵌套变更；P0-2 insert/update `<column>` 补 `xdef:name` 指向既有模型类（运行时生效）。P1: 历史表 success 列 stdToNativeSqlType 方言化+参数绑定；新建 DdlSyntax 方言族助手（postgres ON 子句/H2 remark/TYPE 语法红验证）；checksum 改序列化变更内容+repeatable 即重跑+versioned 不一致抛错；表级 PK/UK/FK DDL+ForeignKeyAction 映射。P2: 错误码两参+argNames 形态；precondition 引擎接入（5 checker 分派+大小写不敏感）；ignore/runOn/contexts/labels/failOnError 全接线；addRecord 聚合失败+executedCount 只计成功；recordMigration UPDATE-or-INSERT；discoverUniqueKeys/Index null INDEX_NAME NPE。P3: sqlserver→mssql 别名两处共用；排序前拷贝（List.of UOE）；死代码删除。
- 超审计项（plan344 nop-dyn 跟进项镜像）：DEFAULT 值三处裸拼接点补 nop-dyn checkDefaultValue 同款校验（拒换行/分号/SQL 注释，新错误码）；COMMENT 单引号转义核实完好。
- 验证：新增 TestMigrationXdefContract/TestDbMigrationErrors/nine-types fixture + 扩展 6 既有类共 37 用例，全部 stash 法红验证；`./mvnw test -pl nop-persistence/nop-db-migration` 95 tests 绿 + dbtool 4 绿；全仓 grep 无 `io.nop.db.migration` 外部导入（_gen 基类变更影响封闭在模块内）。
- 新发现（记录未修）：H2 自带 USERS 系统表使 tableExists("users") 负向断言恒真；executeMark 解析可用但无执行器静默跳过；precondition checker 依赖 INFORMATION_SCHEMA 在 Oracle 不可用；mssql 无 COMMENT 语法时 remark 静默丢弃。
- 偏差：无（_gen 变更系官方生成器再生，非手改）。
- Doc-sync: No owner-doc update required（均为缺陷收敛；xdef 修复恢复的是文档已声明的 17 种变更契约）。

### Plan 346 第三单元：check2 nop-orm-eql 11 条处置（集合表源多余 join/字面量解析/ILIKE 降级）

- Plan 346 第三单元 `ai-dev/audits/check2/nop-orm-eql.md`（P0×1 P2×5 P3×5）：10 修复 + 1 复查非问题，11/11 标注。
- 关键修复：P0 集合属性表源转换在外层查询生成多余 join（子查询/`_some`/同层级三写法 APP_ROLE 曾出现 2 次——行数放大/非法 SQL；buildRelationJoin 后从 owner propJoins removeIf 撤销注册）；P2 returning `*` 绕过方言检查/hex·bit 字面量双重剥离（裸串 parseRadixValue 直解，`0x0A=10` 曾抛 unsupported-eval）/ILIKE 静默降级 NOT-LIKE 改抛既有死常量 ERR_EQL_NOT_SUPPORT_ILIKE/to-one 表源丢别名（`unknown-alias(d)` 红验证）；P3 死代码 92 行/聚合 `*` 越界 IndexOutOfBounds 改错误码/addTable 先 put 后检查/Date 字面量转 ISO TIMESTAMP/masked 克隆丢失。
- 复查非问题：`_all` 比较符翻转 NULL 语义——Kleene 三值逻辑 NOT(UNKNOWN)=UNKNOWN，六组反转对偶与 NOT 包裹生成等价谓词，标准 `<comparison predicate>` X<>Y ≡ NOT(X=Y)。
- 验证：新增 12 用例（TestEqlCompileSql +9 含新关系测试模型、TestSqlExprTransformHelper +3），stash 全 main 修复 12 用例全红（8F+4E）形态吻合；`./mvnw test -pl nop-persistence/nop-orm-eql` 57 tests 绿；nop-orm 回归 178 tests 绿（TestCollectionOperator 5/5）。
- 新发现（只记录）：nop-core FilterBeanToSQLTransformer contains 直接拼 ilike 不走方言解析（与 P2-3 同源，归 nop-core 单元）；addToManyCollectionJoin 单参重载无调用方（死代码）；to-one 动态关系分支仍不支持 userAlias（测试环境无法构造，保持现状）。
- Doc-sync: No owner-doc update required（均为缺陷收敛）。

### Plan 346 第二单元：check2 nop-xlang 10 条处置（AND/OR 宏差一/DSL JSON 键名/XDef 吞异常）

- Plan 346 第二单元 `ai-dev/audits/check2/nop-xlang.md`（P0×1 P1×1 P2×3 P3×5）：7 修复 + 3 暂缓，10/10 标注。
- 关键修复：P0 AND/OR 全局宏 `subList(1, size-1)` 差一（AND(true,true) 曾恒 false、OR 丢最后一个参数，全局注册影响所有 XLang 表达式）；P1 DslXNodeToJsonTransformer 缺省值键名落原始 XML 名（改 `attr.getPropName() ?: name`）；P2 XDefMergeLoader 吞异常 catch 删除（ref 加载失败上抛）；P3 SimpleSchemaValidator max 分支错误参数错位（ARG_MAX_VALUE/ARG_EXCLUDE_MAX）/hex 有符号解析（parseUnsignedLong）/XplTaskLoader NPE 判空+错误码/两处热路径 INFO→DEBUG。
- 暂缓 3 条：EvalBackendRouter 热路径锁（CLD.size() O(n) vs 无锁双端队列需 benchmark，诊断环契约被 truffle 约 30 处测试消费）；XDslExtender 环引用（需新错误码 i18n 聚合在 nop-cli-core 超单元范围 + 物理 resourcePath 键控设计防 super: 链误报）；bare ISE 16 处错误码化（3 处归属 nop-core CoreErrors + 新码 i18n 聚合同超范围）。
- 超审计新发现 2 项一并修复：EvalHelper:33 `case MINUS` 误用 `MathHelper.min`（应为 minus，`binaryOp(MINUS,1,2)` 曾得 1；真实活路径为 XplParseHelper 常量折叠）；同路径 `staticValue` 把 OptionalValue 包装对象直接传 binaryOp（`const x=1+2` 曾折叠得 0，随链必修 `.getValue()` 解包）——两处均有独立红测试。
- 验证：新增/扩展 12 用例（testAndMacro/testOrMacro/testConstFoldMinus 等），stash 红验证失败形态逐条吻合；`./mvnw test -pl nop-kernel/nop-xlang` 596 tests 绿（2 既有 @Disabled）。
- 新发现（只记录）：SimpleSchemaValidator.validate 调 checkRange 实参顺序颠倒（ARG_BIZ_OBJ_NAME/ARG_PROP_NAME 取值互换，当前调用方多为 null 属潜伏）；nop-core AbstractResourceParser finish-parse-resource INFO 同类噪声归 nop-core 单元。
- Doc-sync: No owner-doc update required（均为缺陷收敛）。

### Plan 346 首单元：check2 nop-commons 28 条 P0-P3 处置（parseQuery 多值/MutableString 坐标系/聚集器 null 语义）

- Plan 346（`ai-dev/plans/346-check2-audit-p0-p3-remediation.md`，check2 第二轮审计 26 报告 447 条处置战役）首单元 `ai-dev/audits/check2/nop-commons.md`（P0×1 P1×5 P2×7 P3×15）：26 修复 + 2 暂缓，28/28 标注。战役立项同日完成（plan346 + nop-metadata 报告补档入库 + roadmap 同步）。
- 关键修复：P0 parseQuery 重复参数名多值收集失效（`ret.put(key,value)`→`put(key,list)`，`a=1&a=2&a=3` 曾得 `{a=3}`）；P1 indexOfIgnoreCase/sumDouble int 累加器/SafeLineReader.countLines 多计/MutableString substring·insert·delete·replace 相对绝对坐标混用；P2 MathHelper.min null 语义对齐/AverageAggregator null 计入分母/ByteString.arrayRangeEquals leftLen 参数误传/FileHelper.countLines 少计末行（附带修空文件 0 容量死循环，报告外新发现）/MutableString.indexOf off-by-one/MapCache.getAllPresent null 值跳过；P3 杂项 14 处（gcd 单元素、toShortHash MIN_VALUE、randomChoose 空表、unescapeJava \u 越界裸 SIOOBE 转错误码、isUSASCII null、isSafeUtf8 德摩根、peekFirstNBytes、writeBytes 父目录、getByIndex 负下标、splitChunk（HEAD 实为 OOM 非 /by zero，已在标注纠正）、disjoint O(n) 重写、volatile×2 等）。
- 暂缓 2 条：IoHelper 原生反序列化 ObjectInputFilter 白名单（平台级安全基线决策）；DateHelper formatter Locale（ROOT/注入式影响全局缓存与 nop-report 可见行为）。
- 验证：新增 4 测试类 + 扩展 6 既有类共 38 用例，stash 法红验证失败形态逐条吻合；`./mvnw test -pl nop-kernel/nop-commons` 268 tests 绿。
- 报告事实偏差纠正 2 处（P3-16 requireNonNull(Boolean) 系死校验非误抛 NPE；P3-23 splitChunk 实为 OOM）均写入标注，未改原表。
- 新发现（只记录）：nop-xlang EvalHelper:33 MINUS 分支疑误用 `MathHelper.min`（应为 minus，`1-2` 将得 1）——待 check2 nop-xlang 单元处置时核实。
- Doc-sync: No owner-doc update required（均为缺陷收敛）。

### Plan 344 Phase 4 第二单元：nop-dyn 15 条 P1/P2/P3 处置（缓存死锁/codegen 校验/实体元数据）

- Plan 344 Phase 4 第二单元 nop-dyn.md（P1×3 P2×9 P3×3；统计表 P2 初记 8 系笔误，按发现列表修正为 9）：15 条全部修复。
- 关键修复：unpublish/generateForApp 缓存 key 改 nopModuleId（否则删除模块残留脏缓存）；getOrmModel 补 synchronized 统一 this→CHM 锁序消 ABBA 死锁（红测试 latch 构造确定性死锁、join 5s 超时形态）；租户 mergedStore 恒 null 改 getResourceStore() 惰性构建；函数 delete 分支不被 validateSource 阻断且不残留 xbiz；generateByAI 参数化+同名查重+checkDataAuth；precision 缺省 1→字符串 100/DECIMAL 38 防 VARCHAR(1)；表名/列名/defaultValue DDL 前校验（nop-dyn 侧 pattern+注入片段，db-migration 侧 DEFAULT/COMMENT 转义属另一模块记录待跟进）；中间表第三关系冲突报错；函数级变更走 reloadModel 轻量刷新免 ORM 全量重载（纯性能优化免红测试，标注含理由）；CFG_DYN_MAX_BIZ_OBJECTS 死配置落地两入口上限；租户清理补 on-unload。
- 验证：新增 26 用例（dao 模块原无测试基建，补 junit test 依赖 + 3 测试类 11 用例；service 5 类 15 用例），stash 回 HEAD 实测 13 处行为红；`./mvnw test -f nop-dyn/pom.xml` BUILD SUCCESS 40 tests（1 既有 skip）；报告 15/15 标注对账。新增 7 错误码（NopDynErrors 3/NopDynDaoErrors 4，zh-CN 惯例；两 Errors 类不在 nop-cli-errors 聚合清单，同 orm-periph 先例无需再生成）。
- 偏差：无（db-migration 侧转义已记录为跨模块跟进项，不扩大本单元范围）。
- Doc-sync: No owner-doc update required（均为模块内缺陷修复，不涉及新约定）。

### Plan 344 Phase 4 首单元：nop-batch 18 条 P1/P2/P3 处置（loader 失败语义/限流/历史记录/错误码）

- Plan 344（`ai-dev/plans/344-check-audit-p1-p2-p3-remediation.md`）Phase 4 首单元 nop-batch.md（P1×5 P2×7 P3×6，P0 已在前批处置）：15 修复（含 2 部分修复）+ 1 暂缓 + append 子项暂缓 + 1 不修复 + 1 子项复查非问题（execCount 排序系审计误读，`addOrderField(name,true)` 降序即取最近记录）。
- 关键修复：AsyncFetch 吞 fetch 异常致假成功（takeBatch 二次检查 exception 再抛）；断点续传输出文件截断 fail-loud（新错误码 ERR_BATCH_OUTPUT_FILE_EXISTS_ON_RECOVERY，append 语义需公共接口扩展暂缓）；JdbcLoader closed 标志防关闭后竞态；fetch 线程 semaphore 永久阻塞改 tryAcquire 循环 + isFinished；rateLimit×singleMode 许可放大（按 items.size() 取许可 + 超时 fail-closed 抛 NopTimeoutException）；重试最终异常丢失（e2 抛出、e 挂 suppressed）；onBeforeComplete 回调无界增长（注册移 setup 一次）；dispatch 链补 loadRetryPolicy 包装；saveProcessed 空实现（成功写 NopBatchRecordResult，失败不写留待重处理）；BatchLoaderHelper 固定 key 数据源串扰（identityHashCode 隔离）；4 处裸异常迁 ErrorCode（BatchErrors+3/BatchDslErrors/DbToolExpErrors/新建 BizReportErrors）；RangeSplitUtils 系 DataX 移植死代码不修复。
- 验证：新增 9 测试类 27 用例，红验证逐一 stash 回 HEAD 实测（阻塞 5.13s/许可 25 vs 5/串扰 [c] vs [x,y]/nothing was thrown 等 13 处）；`./mvnw test -f nop-batch/pom.xml` 15 子模块 BUILD SUCCESS 共 54 tests；报告 18/18 标注对账。
- i18n 聚合：跑 TestGenErrorCodesYaml 再生成 nop-cli-errors.i18n.yaml（zh-CN），顺带补齐前批 20 条滞后（core/dao/eql/orm/xlang/xt——en 侧此前已手工同步，仅 zh-CN 未再生成）；本批 5 条新码 en 手工补译。
- 偏差：`-am` 链路 nop-ioc `TestAop#testDynamicGen` 存量环境失败（干净 HEAD 复现确认，与本单元无关）。
- Doc-sync: No owner-doc update required（均为缺陷收敛，未引入新约定或 API 形态）。
