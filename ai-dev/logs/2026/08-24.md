# 2026-08-24

### Plan 344 Phase 4 第二单元：nop-dyn 15 条 P1/P2/P3 处置（缓存死锁/codegen 校验/实体元数据）

- Plan 344 Phase 4 第二单元 nop-dyn.md（P1×3 P2×9 P3×3；统计表 P2 初记 8 系笔误，按发现列表修正为 9）：15 条全部修复。
- 关键修复：unpublish/generateForApp 缓存 key 改 nopModuleId（否则删除模块残留脏缓存）；getOrmModel 补 synchronized 统一 this→CHM 锁序消 ABBA 死锁（红测试 latch 构造确定性死锁、join 5s 超时形态）；租户 mergedStore 恒 null 改 getResourceStore() 惰性构建；函数 delete 分支不被 validateSource 阻断且不残留 xbiz；generateByAI 参数化+同名查重+checkDataAuth；precision 缺省 1→字符串 100/DECIMAL 38 防 VARCHAR(1)；表名/列名/defaultValue DDL 前校验（nop-dyn 侧 pattern+注入片段，db-migration 侧 DEFAULT/COMMENT 转义属另一模块记录待跟进）；中间表第三关系冲突报错；函数级变更走 reloadModel 轻量刷新免 ORM 全量重载（纯性能优化免红测试，标注含理由）；CFG_DYN_MAX_BIZ_OBJECTS 死配置落地两入口上限；租户清理补 on-unload。
- 验证：新增 26 用例（dao 模块原无测试基建，补 junit test 依赖 + 3 测试类 11 用例；service 5 类 15 用例），stash 回 HEAD 实测 13 处行为红；`./mvnw test -f nop-dyn/pom.xml` BUILD SUCCESS 40 tests（1 既有 skip）；报告 15/15 标注对账。新增 7 错误码（NopDynErrors 3/NopDynDaoErrors 4，zh-CN 惯例；两 Errors 类不在 nop-cli-errors 聚合清单，同 orm-periph 先例无需再生成）。
- 偏差：无（db-migration 侧转义已记录为跨模块跟进项，不扩大本单元范围）。
- Doc-sync: No owner-doc update required（均为模块内缺陷修复，不涉及新约定）。

### Plan 344 Phase 4 首单元：nop-batch 18 条 P1/P2/P3 处置（loader 失败语义/限流/历史记录/错误码）

- Plan 344（`ai-dev/plans/344-check-audit-p1-p2-p3-remediation.md`）Phase 4 首单元 nop-batch.md（P1×5 P2×7 P3×6，P0 已在前批处置）：15 修复（含 2 部分修复）+ 1 暂缓 + append 子项暂缓 + 1 不修复 + 1 子项复查非问题（execCount 排序系审计误读，`addOrderField(name,true)` 降序即取最近记录）。
- 关键修复：AsyncFetch 吞 fetch 异常致假成功（takeBatch 二次检查 exception 再抛）；断点续传输出文件截断 fail-loud（新错误码 ERR_BATCH_OUTPUT_FILE_EXISTS_ON_RECOVERY，append 语义需公共接口扩展暂缓）；JdbcLoader closed 标志防关闭后竞态；fetch 线程 semaphore 永久阻塞改 tryAcquire 循环 + isFinished；rateLimit×singleMode 许可放大（按 items.size() 取许可 + 超时 fail-closed 抛 NopTimeoutException）；重试最终异常丢失（e2 抛出、e 挂 suppressed）；onBeforeComplete 回调无界增长（注册移 setup 一次）；dispatch 链补 loadRetryPolicy 包装；saveProcessed 空实现（成功写 NopBatchRecordResult，失败不写留待重处理）；BatchLoaderHelper 固定 key 数据源串扰（identityHashCode 隔离）；4 处裸异常迁 ErrorCode（BatchErrors+3/BatchDslErrors/DbToolExpErrors/新建 BizReportErrors）；RangeSplitUtils 系 DataX 移植死代码不修复。
- 验证：新增 9 测试类 27 用例，红验证逐一 stash 回 HEAD 实测（阻塞 5.13s/许可 25 vs 5/串扰 [c] vs [x,y]/nothing was thrown 等 13 处）；`./mvnw test -f nop-batch/pom.xml` 15 子模块 BUILD SUCCESS 共 54 tests；报告 18/18 标注对账。
- i18n 聚合：跑 TestGenErrorCodesYaml 再生成 nop-cli-errors.i18n.yaml（zh-CN），顺带补齐前批 20 条滞后（core/dao/eql/orm/xlang/xt——en 侧此前已手工同步，仅 zh-CN 未再生成）；本批 5 条新码 en 手工补译。
- 偏差：`-am` 链路 nop-ioc `TestAop#testDynamicGen` 存量环境失败（干净 HEAD 复现确认，与本单元无关）。
- Doc-sync: No owner-doc update required（均为缺陷收敛，未引入新约定或 API 形态）。
