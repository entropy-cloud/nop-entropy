# 2026-08-23

### Plan 2026-08-23-0405-1（R4）收口：独立 closure audit 16/16 PASS（CLOSURE APPROVED），Plan Status → completed；反转里程碑（R1-R4）done

- 独立 closure audit（fresh read-only explore 子 agent，task `ses_fd4c50d65ffeE2UbAdSLgLvfVZ`）：16 项全部 PASS——Phase 1-4 逐条 live 验证（死 fixture 零命中/陈旧措辞零命中/覆盖对照表 6 断言类 + 抽检 16/16 映射方法实存/docs 四处废止描述零命中 + 文档-live 五项抽查一致/03-04 终核标记零命中 + 统一注解确定性结论/demo pom 收窄）+ plan 维护一致性 + Deferred 诚实性（条件性"终核未定"未触发）。verdict = CLOSURE APPROVED。证据写入 plan Closure 段。
- 收口门禁全部通过（exit 0）：`check-plan-checklist.mjs <plan> --strict`；`scan-hollow-implementations.mjs <三模块 src/main> --severity high` 0 findings；`check-doc-links.mjs --strict` 0 errors；`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS；checkstyle（repo config，main 门禁）exit 0；`./mvnw compile -pl nop-demo/nop-quarkus-demo -am -DskipTests` BUILD SUCCESS。
- Roadmap：R4 work item `planned` → `done`（执行裁定摘录见条目）；**反转里程碑（R1-R4）`todo` → `done`**——nop-plugin 定位反转重构（2026-08-22 裁决 → R1 API 收缩 → R2 编排简化 → R3 兼容回归 → R4 测试/文档收口）全部落地。
- Successor 义务清点：R1（docs 归 R4 + Ownership Deviation 回读）、R2（测试矩阵/docs 归属 + deferred 承接）、R3（jar 轨 config 忽略携带项 + 冻结基线引用）全部在本 plan 收口；无 plan-owned 剩余工作。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-23-0405-1（R4）执行：Phase 4 nop-quarkus-demo 依赖收窄 landed

- Decision：`nop-demo/nop-quarkus-demo/pom.xml` 直接依赖 `nop-plugin-manager` → `nop-plugin-api`。三面安全复核（Current Baseline + 评审核验）：demo src 仅 `DemoPluginCommand.java` import `io.nop.plugin.api.{IPluginCancelToken,IPluginCommand}`；全模块无 `io.nop.plugin.manager` 引用；宿主 `app-demo.beans.xml:16` 兜底 bean `nopPluginCommand_default` 不依赖 manager 类（manager main 无 beans 资源发布、全仓除 demo/bom 外无模块依赖 manager）。
- Proof：`./mvnw compile -pl nop-demo/nop-quarkus-demo -am -DskipTests -T 1C` BUILD SUCCESS（收窄后真实编译暴露验证，无回退需求）。demo 侧测试现状 = `TestDemoBizModel`/`TestFluxYamlPages`（均非 plugin 相关）——compile 为充分验证裁定成立，未跑 demo 测试（非本项门禁）。
- Doc-sync: No owner-doc update required（pom 依赖收窄不改契约面；module-groups 对 demo 无 plugin 依赖描述）。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-23-0405-1（R4）执行：Phase 3 dsh 失效语义终核 + Ownership Deviation 回填 landed

- **dsh 失效语义终核（Decision）**：结论 = **两路径并存确认 + 边界更正**——(1) 弱读 undefined 仅存在于显式探测 API `ctx.reflect.get(name)`（3.x `ctx.get`），strict 下 provider fiber 非 ACTIVE 亦返回 undefined；(2) 属性访问 `ctx.<key>` 是显式抛错路径非弱读（without inject / in inactive context）；(3) 持有的 traceable proxy 方法调用无失效检查——旧表述"traceable proxy 方法调用抛 INACTIVE"修正为：INACTIVE 抛出点在获取边界与 `assertActive()` 生命周期方法（`effect/restart/update`）。Nop 双边界（getService 新调用 + 既有代理调用）抛 INACTIVE，严格性更高（03 §5.5 由"持平偏 Nop"改判 **Nop**）。
- **核验过程（Proof）**：cordis 仓库已由 koishijs/cordis 迁移至 `cordiverse/cordis`（branch main，4.0.0-rc.8，与 dsh 侧基线 `@deepseek-ai/cordis` 4.0.1 的 `packages/core/src` 结构一致，见 analysis `2026-08-21-dsh-plugin-system-reference.md` §10 证据索引）；raw.githubusercontent 不可达，经 GitHub contents API 逐文件取 base64 解码核验。核验位置：`context.ts:39`（ctx = Proxy(this, ReflectService.handler)）；`reflect.ts:62-98`（handler.get 三子路径：:71/:90-91 without inject 抛错、:86-89 in inactive context 抛错、:85 命中返回 traceable 值、:79 root 无 runtime 回落弱读）+ `reflect.ts:150-158`（`get(name, strict)`/`_getImpl`：`if (strict && impl.fiber.state !== FiberState.ACTIVE) return`）；`utils.ts:110-118`（getTraceable 需 tracker）、`:148-155`（createShadowMethod apply 直通无失效检查）、`:157-190`（createTraceable get 无 epoch 检查）；`fiber.ts:97`（INACTIVE_EFFECT 错误码）、`:101`（INACTIVE epoch 常量）、`:224-227`（assertActive：uid===null → 抛）、`:277-278`（effect() 首行 assertActive）、`:451/:461`（restart/update assertActive）、`:471`（dispose → _setEpoch(INACTIVE)）。
- 回填五处：03:290（§5.3 统一注解终核结论 + cordis 源码定位）、03:299（§5.3 对照表失效行为行）、03:326（§5.5 失效语义行）、04:50（§2.2 失效语义行）、04:132（§三 语义差异行）——五处表述统一且相互一致。
- **Ownership Deviation 回读修正（R1 裁定义务清偿）**：01 锚点表 R2 行（:351）改为实况——IPluginInstance 型结构（parentToChildren/级联销毁/快照/pending/isInstanceConfigMatched 签名）随 R1 编译依赖删除，R2 = 残留审计 + InstanceConfigProvider→DefinitionConfigProvider 重命名 + 宽松比较迁移（`PluginManagerImpl.configValueMatches`）+ 错误码族退役 + reconcile 拓扑序编排；与 roadmap R2 条目（已修正）及 R1/R2 plan Closure 记录三方一致。01:336 "待 R1 更正"措辞同步为已完成表述（05 §七已含 2026-08-22 审计更正注解）。
- 验证：`grep -rn "待 R4\|待终核\|R4 对照\|R4 阶段" 03 04` 零命中；`grep -n "待 R1 更正" 01` 零命中；`check-doc-links.mjs --strict` 0 errors。**R1/R2/R3 plan 全部 successor 义务清零**。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-23-0405-1（R4）执行：Phase 2 docs-for-ai 同步 landed

- `docs-for-ai/03-modules/nop-plugin.md` 全文重写（反转后契约）：单层六态状态机（转换图/各态占有表/unload 守卫/FAILED 可重试/失败阈值暂停）、插件级 coeffect（requires=定义 ACTIVATED、if-property 宽松比较、reconcile 拓扑序三规则+时间静止+环检测）、定义级配置域（updateConfig 两态语义 + DefinitionConfigProvider）、activator 参数传递、getService 激活态绑定代理、定义级命令路由（含 getCommandBean 回退链：插件容器→宿主回退→default 兜底）、兼容双轨（aware start/stop 三边界断言）、jar 轨契约（含 R3 携带项：aware start(gav, config) 已 LOADED/ACTIVATED 时忽略 config 为 documented behavior）、HMR（checkChangedAndReload 显式入口 + reload 四步编排）、artifact+SHA256（正确内容保留）、冻结属性集（引 `TestPluginXdef#testAttributeSetFrozen` 守护）、源码锚点表终态化（删 R4 横幅）。
- `INDEX.md:199` nop-plugin 条目、`source-anchors.md` PLG-001/002/005/006/008/009（PLG-006 行号 320→605 + 过滤器修正措辞；PLG-002 删"重写归 R4"注记改正向表述）、`module-groups.md` 核心框架行同步完成。
- 验证：废止机制 grep（IPluginInstance|InstanceState|createInstance|destroyInstance|per-instance|实例配置域|parent 层级|reconcileInstances）nop-plugin.md 零命中；三索引 grep（IPluginInstance|createInstance|两态生命周期|多实例）零命中；`node ai-dev/tools/check-doc-links.mjs --strict` 0 errors。
- **文档-live 一致性抽查清单**（文档段落 ↔ 源码 file:line，逐项核验一致）：
  - 六态枚举名 ↔ `PluginState.java`（UNLOADED/LOADED/ACTIVATING/ACTIVATED/DEACTIVATING/FAILED 全枚举一致；各态对静态定义/子容器/bean/effect 占有关系与 Javadoc 逐行一致）
  - `IPlugin` 方法面 ↔ `IPlugin.java`（`isStateMachineAware` default false :83、`getState` default LOADED :93、生命周期 default 抛 `ERR_PLUGIN_LIFECYCLE_NOT_SUPPORTED` :65-166、invokeCommand 定义级路由 :33-46、activate boolean/deactivate CompletionStage 不对称 :129/:140）
  - `IPluginManager` 方法面 ↔ `IPluginManager.java`（loadPlugin:27/unloadPlugin:32/activatePlugin:53/deactivatePlugin:59/getPlugin:64/getLoadedPlugins:66/reconcilePlugins:75/reloadPlugin:84——与文档 §源码锚点 PLG-005 一致）
  - 错误码 ↔ `PluginApiErrors.java`（ERR_PLUGIN_INACTIVE:20/ERR_PLUGIN_NOT_DEACTIVATED:28/ERR_PLUGIN_LIFECYCLE_NOT_SUPPORTED:37）+ `PluginManagerErrors.java`（ERR_PLUGIN_ACTIVATION_FAILED:59/ERR_PLUGIN_MULTIPLE_SERVICE_CANDIDATES:80/ERR_PLUGIN_SERVICE_PROXY_ONLY_INTERFACE:88/ERR_PLUGIN_RELOAD_NOT_SUPPORTED:114/ERR_PLUGIN_SHA256_MISMATCH:31/ERR_PLUGIN_CHECKSUM_NOT_AVAILABLE:40）
  - jar 轨契约 + R3 携带项 ↔ `AbstractPlugin.java`（类 Javadoc :38-53 门控恒空集/激活回调跳过；`start` aware 分支 :310-323 仅 UNLOADED 补 `load(config)`，已 LOADED/ACTIVATED 忽略 config——documented behavior 措辞与代码一致）
  - `checkChangedAndReload` ↔ `PluginManagerImpl.java:605`（行号锚点刷新；仅跳 UNLOADED、ACTIVATED/中间态进入变更检测）
  - 冻结属性集 ↔ `plugin.xdef`（name 必填/requires csv-set/if-property string/activator bean-name + 唯一子元素 `<beans>`）+ 守护测试 `TestPluginXdef#testAttributeSetFrozen`
  - reconcile 拓扑编排语义 ↔ 覆盖对照表所引 `TestReconcileTopologicalOrder`/`TestCoeffectReconcile` 断言（正/逆拓扑、混合批次、宽松比较）
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-23-0405-1（R4）执行：Phase 1 测试矩阵终态对齐 landed

- Plan: `ai-dev/plans/2026-08-23-0405-1-plugin-r4-test-and-docs-sync.md`。死 fixture 清理：删 `IsolatedCommand.java` + `agent-instance.plugin.xml` 中 `nopPluginCommand_isolated` bean 声明（删除前复核零测试引用，grep 仅命中声明处）。陈旧/失实注释修正 5 文件：`HelloCommand`（定义级路由表述）、`ConfigReaderBean`（定义级配置域 `DefinitionConfigProvider` 表述）、`FailActivator`（**事实矛盾修正**：注释"实例回退 DEACTIVATED…错误带实例 key"vs 实际 = 激活失败置 FAILED 可重试、`lastActivationError` 定义级记录、异常带 pluginId 参数——依 `VfsPluginDefinition.java:399-415` 实文核验）、`AgentInstanceRecorder`（插件激活生命周期表述）、`AgentInstanceActivator`（"自动注册为本次插件激活的 effect"）。
- **裁定**：config 键名 `agent.only-instance` 与 fixture 标识符名（`AgentInstanceRecorder`/`agent-instance.plugin.xml`）保留不改——改名无行为收益且波及既有断言（`TestPluginLifecycle#testConfigDomainDefinitionLevel`/`TestCommandRoutingRegression#testVfsStartStopStateBoundaries` 直读该键）。
- **裁定**：checkstyle 门禁 = main 源码门禁（仓库 checkstyle 默认不含 test 源码——`includeTestSourceDirectory=true` 仅存在于根 pom 注释掉的死配置，qa profile 未启用且 `failOnViolation=false`）；本 Phase test 改动均为 Javadoc/注释级，风格一致性由人工审查与全量测试编译保证。
- **端到端适用性裁定**：本 Phase 为测试整理，端到端链路由 `TestPluginLifecycle#testEndToEndSingleActivationFlow` + `TestCommandRoutingRegression#testEndToEndLoadActivateInvokeDeactivate` 覆盖（见对照表），不重复新建。
- Doc-sync: No owner-doc update required（纯测试整理；docs-for-ai 重写归 Phase 2）。
- 验证：`grep -rn "IsolatedCommand\|nopPluginCommand_isolated" nop-core-framework/nop-plugin --exclude-dir=target` 零命中；陈旧措辞门禁（per-instance/实例配置域/实例容器/实例合并视图/实例覆盖全局/仅实例键/实例生命周期事件/实例 effect/实例回退）test 树零命中（基线 8 行/6 文件 → 0）；`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（0 failures/errors）。

**R4 覆盖对照表**（roadmap R4 断言清单 → test 类#方法，缺口 = 0）：

| Roadmap R4 断言类 | 现测试对应（全部通过中） |
| --- | --- |
| 六态转换（注 a：roadmap 原文"两态转换"为反转前措辞，现契约 = 单层六态） | `TestPluginLifecycle#testSixStateTransitionsObservable`（LOADED→ACTIVATING→ACTIVATED→DEACTIVATING→LOADED 探针全链）；FAILED 进入/可重试：`TestPluginLifecycle#testActivationFailureSetsFailedWithReadableError`；unload 守卫：`TestPluginLifecycle#testUnloadGuardThrowsWhenActivated` + support `TestAbstractPlugin#testUnloadGuardThrowsWhenActivated`；六态枚举契约：api `TestPluginDefaultMethods#testSixStateEnumReferenceable` |
| 门控激活/去激活 | `TestPluginLifecycle#testEndToEndSingleActivationFlow`；门控不满足 no-op false：`TestCoeffectReconcile#testActivatePluginReturnsFalseWhenGateUnsatisfied`；依赖链级联：`TestCoeffectReconcile#testReconcileWiringAndDependencyCascade`；拓扑序（批量激活正/去激活逆/混合批次）：`TestReconcileTopologicalOrder#testBatchActivationForwardTopologicalOrder` / `#testBatchDeactivationReverseTopologicalOrder` / `#testMixedBatchDeactivationGroupProcessedBeforeActivationGroup`；时间静止两窗口：`#testTimeStaticActivateCompletesThenConvergesToDeactivated` / `#testTimeStaticDeactivateCompletesThenReactivates`；环检测：`TestCoeffectReconcile#testCycleDetectionTerminatesAndReportsUnresolved`；配置订阅/更新触发：`#testGlobalConfigSubscriptionTriggersReconcile` / `#testUpdateConfigTriggersReconcile`；宽松比较：`TestCoeffectConfigMatching` 3 @Test + `TestCoeffectReconcile#testDefinitionLevelLooseBooleanComparison` |
| quiescence | `TestPluginLifecycle#testActivatorWiringAndEffectQuiescence`（effects 清空 + LIFO 顺序 + bean destroy 后于 effect 回退）；`TestPluginLifecycle#testScopeContract`（可移除句柄/close 幂等/close 后注册抛异常）；`TestCommandRoutingRegression#testVfsDirectStartWithoutPriorLoadIncludesLoadStep`（stop 后 scope null） |
| HMR（reload 重放 + checkChangedAndReload） | `TestReloadPlugin#testReloadEndToEndDefinitionConfigReplay`（updateConfig 累积值重放）/ `#testReloadWhenGateClosedStaysLoadedUntilReconcile` / `#testReloadLoadFailureLeavesUnloadedRecoverable` / `#testReloadJarTrackFailsExplicitly`（jar 轨显式失败）；`TestChangeDetection#testCheckChangedAndReloadTriggersReload`（ACTIVATED 定义进入变更检测——R2 过滤器修正行为证明）/ `#testCheckChangedAndReloadNoChangeNoOp` / `#testReloadDependentConvergesViaReconcile` |
| INACTIVE 代理失效 | `TestPluginLifecycle#testServiceProxyLifecycleMatrix`（deactivate 后抛 INACTIVE、重激活同一引用恢复、集合代理恢复）；`TestPluginLifecycle#testEndToEndSingleActivationFlow`（新调用与既有代理引用均快速失败）；命令 INACTIVE：`TestPluginLifecycle#testDefinitionLevelCommandRouting`、`TestCommandRoutingRegression#testVfsCommandInactiveBeforeAndAfterDeactivate`、support `TestAbstractPlugin#testAwareInvokeCommandDefinitionLevelRouting` |
| requires 定义级 ACTIVATED 迁移断言（原 `hasActivatedInstanceByName` → 定义级 ACTIVATED） | `TestReconcileTopologicalOrder#testRequiresEvaluatesDependencyActivationNotLoaded`（依赖仅 LOADED → 门控不满足 → activatePlugin false、reconcile 不激活——定义级 ACTIVATED 语义直接断言）；`TestCoeffectReconcile#testReconcileWiringAndDependencyCascade`（提供者 ACTIVATED → 消费者门控满足级联激活） |
| 注 b：删除 parent 层级/级联销毁/实例配置域相关测试 | 已完成于 R1（Current Baseline grep 零命中即证据：`createInstance|destroyInstance|IPluginInstance|InstanceState|hasActivatedInstanceByName|InstanceConfig|DEFAULT_INSTANCE_KEY` 测试树 0 命中；TestParentHierarchy/TestPluginInstanceLifecycle 随 R1 删除） |

- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-23-0405-1（R4）起草：测试矩阵终态对齐 + docs 同步，三轮独立 draft review 共识 → active

- R1-R3 均 completed 后 roadmap 仅剩 R4（`todo`）。按 mission 流程起草 `ai-dev/plans/2026-08-23-0405-1-plugin-r4-test-and-docs-sync.md`，接住全部 successor 义务：R1 docs→R4 + Ownership Deviation 回读（01 锚点表 R2 行 + :336 措辞）、R2 测试矩阵/docs 归属 + 两项 deferred 承接、R3 携带项（jar 轨 config 忽略 documented behavior）+ 冻结基线引用。
- Current Baseline 依 live repo 全量核验：测试树死机制 grep 零命中（R1 已交付主体改造）；真实残留 = 死 fixture（IsolatedCommand + nopPluginCommand_isolated 声明，无测试消费者）+ 5 文件陈旧/失实注释（含 FailActivator.java:10 **事实矛盾**：注释"实例回退 DEACTIVATED 带实例 key"vs 实际 FAILED 可重试）；docs-for-ai 四处滞后（nop-plugin.md 全文/INDEX:199/source-anchors PLG 表/module-groups:12）；design 03/04 五处"待 R4 终核"标记 + 01:351/:336 措辞；demo pom:236 收窄可行（src 仅 api import）。
- 三轮独立对抗性 review（fresh explore 子 agent `ses_fd4e91dd6ffe…`/`ses_fd4e22c52ffe…`/`ses_fd4d84b65ffe…`，含想象性分析 + live grep 实证）：R1 轮 1 Major（"待 R4" grep 模式覆盖不全）+5 Minor；R2 轮 2 Major（残留清单不实——实测 6 文件 8 行；checkstyle includeTestSourceDirectory=true 为 pom 注释死配置、覆盖声明失实）+4 Minor；全部修复并经 R3 轮逐项实证（8 行/6 文件、5 处命中、各零命中门禁可达性全部实跑核实）。R3 轮裁定：共识达成、无 Blocker，1 Minor 措辞错位（PLG-006 子句）已顺带修正。关键裁定：checkstyle 门禁如实降为 main 源码门禁（仓库无 test 源码 checkstyle 硬门禁）；"终核未定"预登记条件性 deferred（watch-only residual）；roadmap"两态转换"字面映射注记纳入覆盖对照表。
- Roadmap：R4 work item `todo` → `planned`（Work Items 块已更新）；反转里程碑保持 `todo`（R4 closure 后派生 done）。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-22-2309-3（R3）收口：独立 closure audit 全区 PASS（CLOSURE APPROVED），Plan Status → completed

- 独立 closure audit（fresh session `ses_fd4f56f78ffeDkEB0zxyv8d6Lw`，read-only explore 子 agent）：Phase 1/2/3 全部 exit criteria PASS（live code 逐行核验 start/stop 边界、非 aware 旧语义保持、两实现点回退链无分叉、失败传播无吞；focused tests 断言实文核验；Anti-Hollow 全链追踪 start→load→activate→容器→命令 bean 连通 + miss 端点全部显式失败；residue grep 零命中独立复跑）；独立测试复跑 BUILD SUCCESS（api 6 + manager 69 + support 15 = 90/90）。1 Minor 观察（非阻塞）：jar 轨 aware `start(config)` 已 LOADED/ACTIVATED 时忽略 config（两轨固有差异，jar 轨无定义级配置域）——已记录为 R4 docs 携带项（plan Non-Blocking Follow-ups）。证据写入 plan Closure 段。
- 收口门禁全部通过：`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` exit 0；`node ai-dev/tools/scan-hollow-implementations.mjs <三模块 src/main> --severity high` 0 findings；`node ai-dev/tools/check-doc-links.mjs --strict` 0 errors；checkstyle（repo config）exit 0；`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS；`./mvnw clean install -DskipTests -am -T 1C` BUILD SUCCESS。
- Roadmap：R3 work item `planned` → `done`（执行裁定摘录见条目）；R4 待执行（反转里程碑保持 `todo`）。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-22-2309-3（R3）执行：兼容路径与命令路由回归 landed（Phase 1-3 全部）

- Plan: `ai-dev/plans/2026-08-22-2309-3-compat-path-command-routing-regression.md`（R3：兼容路径与命令路由回归）。本条目记录执行关键事实、裁定与基线取代。
- **R1 残留缺口暴露并修复（Phase 1 核心变更）**：`AbstractPlugin.start` aware 分支原为无条件 `load(config) + activate()`——已 ACTIVATED 时 start 会把状态重置 LOADED 后重建容器（**旧容器泄漏**）、已 LOADED 时重复 load。修复为状态边界规格（两轨统一）：仅 UNLOADED 补 load 步，已 LOADED/FAILED 直接 activate，已 ACTIVATED 经 activate 幂等 no-op（容器不重建）。`AbstractPlugin.stop` 幂等性复核确认（UNLOADED 态 deactivate no-op + unload 幂等）。同步微开两个测试 seam：`buildBeanContainer` private→protected（激活失败注入）、新增 `getPluginBeansPath()` protected hook（与 `getPluginDefinitionPath()` 对称，无 beans 文件场景可测）。
- **回退链分叉修复（Phase 2 代码变更）**：`AbstractPlugin.getCommandBean` 宿主回退分支原为裸 cast（宿主同名非 IPluginCommand bean 会 CCE），对齐 `VfsPluginDefinition.getCommandBean` 的 instanceof 判定——两实现点收敛同一回退链（插件容器 → 宿主回退 → default bean 兜底），不再分叉。
- **基线取代记录**（Current Baseline 所列 R1 前两轨差异 → 统一新语义）：VFS 轨 start 原不含 load 步（要求已 LOADED）→ 现为 start = load + activate（未先 load 直接 start 到 ACTIVATED，`testVfsDirectStartWithoutPriorLoadIncludesLoadStep` 断言补步差异）；jar 轨 aware start 原为 load + UnsupportedOperationException → 现为 start = load + activate 无条件激活（`testStartStopConvergeToLoadActivateDeactivateUnload` 断言新语义）。
- **执行裁定（Phase 2，非 aware 宿主回退"命中"结构性不可达）**：`containsBean` 经 parent 链（插件容器 parent = 宿主容器）已覆盖宿主 bean 名空间，静态 `BeanContainer.tryGetBean` 回退仅在链上无此名时可达（此时必 miss → default 兜底）；且兼容容器 build 不 start（改造前等价，pre-W2 原始代码即如此）——链上可见名经 `beanContainer.getBean` 显式抛 container-not-started。非 aware 链结构（插件容器优先 → 宿主 → default）不变即 W2-W4 基线保持；全部可达分支已钉死（`testCompatPathW2W4BaselineConfigAndContainer`），不构成缺陷。
- **宿主 fixture（两 JVM 分工设计）**：support `_vfs/main/beans/app-host-commands.beans.xml` 仅注册 `nopPluginCommand_host`（保留链终点 miss → 显式异常可观测性）；manager 同路径注册 host + `nopPluginCommand_default`（兜底命中可观测性）。两轨 aware 宿主回退同标记 `"host:world"`（行为一致观测锚点）。注意 `/main/beans` 装配文件必须 `app-*.beans.xml` 命名（AppBeanContainerLoader.isAppBeans 过滤）。
- 新增测试：support `TestAbstractPlugin` 15 @Test（+5：状态边界/失败传播/宿主回退/链终点/无容器回退）+ `TestHostCommand`；manager 新增 `TestCommandRoutingRegression` 6 @Test（直接 start 补步/状态边界/失败传播/INACTIVE 前后/回退链/端到端）+ `HostCommand`/`HostDefaultCommand` + `TestPluginXdef.testAttributeSetFrozen`。
- **plugin.xdef 属性集冻结确认（Phase 3 Decision）**：属性集 = name（必填 !string）/requires（csv-set）/if-property（string）/activator（bean-name）+ 唯一子元素 `<beans xdef:ref="/nop/schema/beans.xdef"/>` + xdef 标准属性；无 requires-service 等服务级条件属性（01 §五终裁：扩展点已关闭）。逐项 diff 为空；落地机器可检守护测试（XDefinitionParser 解析 xdef 本体断言属性集/子元素集，偏差即失败）——**此为 R4 docs 重写的冻结基线引用点**。
- `DEFAULT_INSTANCE_KEY`：R1 已删常量本体，本 plan 复核全仓零代码引用（仅 ai-dev/docs 历史文本）。多实例路由规则（instances.size()>1 / ERR_PLUGIN_MULTIPLE_INSTANCES）manager/support main grep 零命中。
- 验证（全部 exit 0 / BUILD SUCCESS）：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C`（api 15 + manager 69 + support 15，0 failures）；`./mvnw compile -am -q`；`./mvnw clean install -DskipTests -am -T 1C`；`./mvnw checkstyle:check -Dcheckstyle.config.location=file://<repo>/checkstyle.xml -pl <三模块> -am`；`node ai-dev/tools/scan-hollow-implementations.mjs <三模块 src/main> --severity high` 0 findings。
- Owner docs：deferred to R4（roadmap 归属：docs-for-ai/03-modules/nop-plugin.md 重写 + INDEX/source-anchors 同步）。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-22-2309-2（R2）收口：独立 closure audit 17/17 PASS（CLOSURE APPROVED），Plan Status → completed

- 独立 closure audit（fresh session `ses_fd5111804ffev7YYjeGzoSSgri`，read-only explore 子 agent）：17 个审计项全 PASS——Phase 1 残留 grep 零命中/定义级载体核验、Phase 2 拓扑编排代码逐行核验（doReconcile:261-299 + 级联闭包 + 6 个 focused tests 断言实文核验）、Phase 3 HMR/updateConfig 5 项 focused tests 核验、Anti-Hollow 全链追踪（loadPlugin→reconcile→doReconcile→topo/computeGroup→activate/deactivate 运行时连通、无空方法体/静默 no-op）、Deferred 诚实性（与 01 §五已知限制注解比对）、独立测试复跑 exit 0 全绿。证据写入 plan Closure 段。
- 收口门禁全部通过：`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` exit 0；`node ai-dev/tools/scan-hollow-implementations.mjs <三模块 src/main> --severity high` 0 findings；`node ai-dev/tools/check-doc-links.mjs --strict` 0 errors；checkstyle（repo config）exit 0；`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS。
- Roadmap：R2 work item `planned` → `done`（执行裁定摘录见条目）；R3/R4 待执行。
- Follow-up 归属：docs-for-ai 同步与测试矩阵改写归 R4；start/stop 兼容收敛与 DEFAULT_INSTANCE_KEY 删除归 R3。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-22-2309-2（R2）执行：Phase 3 HMR/updateConfig 语义钉死 landed（验证轮）

- Phase 3 为验证轮（R1 已落地全部目标语义），逐项核对 + focused tests 复跑全绿：
  - reloadPlugin = deactivate（若激活态）→ unload → load（重放 updateConfig 累积值，`getDefinitionConfig` 快照经 `newDef.load(definitionConfig)` 重放）→ reconcile；快照/pending 机制 grep 零残留（Phase 1 清单含 ReloadSnapshot/pendingRebuild/SnapshotInstance）。focused：`TestReloadPlugin.testReloadEndToEndDefinitionConfigReplay`（ACTIVATED reload → 新定义对象、`agent.mode=dev` 重放经 getService 可观察、reconcile 自动回 ACTIVATED）。
  - **裁定**：`checkChangedAndReload` 状态过滤器修正已在 R1 落地（现状 `if (st == UNLOADED) continue`——仅跳过 UNLOADED，ACTIVATED/中间态定义进入变更检测）；plan 所述"`!= LOADED` 跳过 ACTIVATED"为 R1 前旧状。R2 = 验证钉死，非重复修复。focused：`TestChangeDetection.testCheckChangedAndReloadTriggersReload`（ACTIVATED 定义文件变更 → checkChangedAndReload → reload 完成、回 ACTIVATED——ACTIVATED 不被过滤器跳过的行为证明）。
  - jar 轨 reload 显式失败（W6 基线）：`TestReloadPlugin.testReloadJarTrackFailsExplicitly`（ERR_PLUGIN_RELOAD_NOT_SUPPORTED + pluginId 参数断言）。
  - updateConfig 两态：`TestPluginLifecycle.testConfigDomainDefinitionLevel`——LOADED 缓存（activate 后 `reader.getMode()=="dev"` 应用）、ACTIVATED 热应用**可观察传播**（`updateConfig(agent.mode=prod)` 后经 getService 代理读 bean 反映 "prod"，真实 bean 重绑定，非内部 map 重算弱分支）。
  - R1 失败阈值暂停：`TestPluginLifecycle.testFailureThresholdPausesAutoActivation` 保持绿（存在且通过 = 验证方式）。
- 验证：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS；实例错误码全仓 *.java 引用零命中（唯二文本命中 = nop-cluster 日志标记 `my-instance-not-found`，非插件错误码）；下游 nop-quarkus-demo 仅用 api 模块 IPluginCancelToken/IPluginCommand（未触及），三模块 `compile -am` exit 0。
- Owner docs：deferred to R4。未提交 git。

### Plan 2026-08-22-2309-2（R2）执行：Phase 2 reconcile 插件级语义 + 拓扑序 landed

- `PluginManagerImpl.doReconcile` 重写为拓扑编排引擎（01 §三/§五）：
  - `topoSortDefs`：DFS 后序 = 正拓扑序（requires 依赖图，同名多定义全量建边，DFS 根与邻接按 pluginId 排序保证确定性）。
  - `computeDeactivationGroup`：级联闭包——以"组内定义已去激活"的假设态重评估 ACTIVATED 定义门控（requires 腿经 `hasActivatedPluginByNameExcluding` 假设态判定、if-property 腿读全局配置），保证"提供者失效 → 消费者随之入组 → 消费者先退出"；环成员门控强制关闭直接入组。
  - 同一 pass 混合批次裁定落地：先去激活组（逆拓扑序，消费者先退出）后激活组（正拓扑序，提供者先激活；门控处理时点实时评估——同 pass 先激活的提供者即满足消费者 requires 腿）。不动点迭代 + DFS 环检测 + 失败阈值暂停保留（R1 机制验证钉死）。
- `hasActivatedPluginByName` 重构为 `hasActivatedPluginByNameExcluding(name, excludedIds)` 的 null-exclusion 特例（requires 定义级 ACTIVATED + ANY 同名语义保持）。
- 新增 focused tests `TestReconcileTopologicalOrder`（6 @Test）+ 探针（`OrderRecorder`/`OrderRecorderActivator`/`FlipConfigActivator`/`RecoverConfigActivator`）+ fixtures（order-provider/consumer/independent/flip/recover.plugin.xml）：
  - 批量激活正拓扑序（provider 先于 consumer，事件序列断言）；批量去激活逆拓扑序（consumer 先退出，级联闭包验证）；混合批次（同 pass 先去激活组后激活组，三事件序列断言）。
  - requires 求值 = 依赖定义 ACTIVATED（依赖仅 LOADED → 门控不满足、activatePlugin false、reconcile 不激活）。
  - 时间静止两窗口：(a) activate 展开期间条件失效（activator 内翻转配置）→ 激活完整完成后收敛去激活（最终 LOADED）；(b) deactivate 展开期间条件恢复（effect 回退内恢复配置）→ 回退完成后同 pass 重激活（最终 ACTIVATED）。
  - 环检测 W5 基线（unresolved 报告）、端到端配置驱动链、订阅接线验证：既有 TestCoeffectReconcile 7 @Test 对新引擎复跑全绿。
- 验证：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（manager 62 tests）；checkstyle（repo config）exit 0。
- Owner docs：deferred to R4。未提交 git。

### Plan 2026-08-22-2309-2（R2）执行：Phase 1 实例机制残留清零 landed

- Plan: `ai-dev/plans/2026-08-22-2309-2-manager-orchestration-simplification.md`（R2：manager 编排简化）。本条目记录 Phase 1 执行事实与裁定。
- Current Baseline 复核结论（R1 后 live repo）：
  - R1 已删除项验证无残留：`parentToChildren`/级联销毁/`ReloadSnapshot`/`pendingRebuilds`/`isInstanceConfigMatched`/`hasActivatedInstanceByName`（R1 已更名为定义级语义的 `hasActivatedPluginByName`，旧名零命中——按 plan 允许新名存在的裁定记录）。
  - **裁定**：`ERR_PLUGIN_ACTIVATION_FAILED`/`ERR_PLUGIN_ACTIVATOR_NOT_FOUND` 的 define 引用 ARG_INSTANCE_KEY 参数这一前置，R1 已在收口时移除（现 define 仅 ARG_PLUGIN_ID/ARG_ACTIVATOR，消息文案已是插件级）——本 plan 无需再改 define，直接进入引用清零 + 删常量。
  - **裁定**：R1 已落地定义级配置通道（`VfsPluginDefinition.activateInternal` 经 provider 接收定义级配置视图、updateConfig 热应用经 `updateMergedView` 传播，`TestPluginLifecycle.testConfigDomainDefinitionLevel` 可观察）——Phase 1 的"删实例配置域载体"无需升级为实现项，落地方式 = 载体类按定义级语义重命名。
- 代码变更：
  - `InstanceConfigProvider.java` → `DefinitionConfigProvider.java`（定义级配置域语义重命名 + javadoc 重写；实现保持委托包装/合并视图/订阅传播不变，`VfsPluginDefinition` 三处引用同步）。R1 audit 的 3 个 Minor 一并处理：悬空 `{@link PluginInstanceImpl}` 消除（新 javadoc 引 `VfsPluginDefinition`）、`PluginScopeImpl` 两处 "instance 级" 措辞改 "插件级"、`ERR_PLUGIN_ACTIVE_CHILDREN_EXIST` legacy 注记随常量删除消除。
  - `CoeffectConfigHelper.java` 删除；宽松比较逻辑（W5 裁定语义）原样迁移到 `PluginManagerImpl.configValueMatches`（私有静态，`CoeffectEvaluatorImpl.isGlobalConfigMatched` 的实现位置）。
  - `TestCoeffectConfigHelper.java` 删除；保留路径用例迁移到新 `TestCoeffectConfigMatching`（3 @Test：数值宽松等价 Integer 10/Double 10.0/"10" 匹配、11/"10.5"/"abc"/null 不匹配；字符串精确；布尔宽松 Boolean.TRUE/"true" 匹配、FALSE/"false" 不匹配——经 manager→reconcile→isGlobalConfigMatched 全链验证，新 fixtures loose-number/loose-string/loose-bool.plugin.xml）。
  - 实例错误码族退役：manager 侧删 ERR_PLUGIN_INSTANCE_EXISTS/NOT_FOUND/INSTANCES_NOT_EMPTY/NOT_SUPPORTED、ERR_PLUGIN_PARENT_NOT_ACTIVATED/PARENT_CHAIN_CYCLE、ERR_PLUGIN_ACTIVE_CHILDREN_EXIST、ARG_INSTANCE_KEY/ARG_INSTANCE_KEYS；api 侧删 ERR_PLUGIN_MULTIPLE_INSTANCES 与 ARG_INSTANCE_KEYS（plan Out Of Scope 裁定的两处例外常量；其余 api 不动）。删前 grep 全仓 *.java 代码引用零命中（仅定义处）。`DEFAULT_INSTANCE_KEY` manager main 无引用（R3 归属，仅记录）。
- 验证：实例机制 grep（plan Phase 1 清单全路径含 test）零命中；`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（manager 56 tests 0 failures）；`./mvnw checkstyle:check -Dcheckstyle.config.location=file://<repo>/checkstyle.xml -pl <三模块> -am` exit 0。
- Owner docs：docs-for-ai 同步 deferred to R4（roadmap 归属：`docs-for-ai/03-modules/nop-plugin.md` 重写 + INDEX/source-anchors 同步）。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 2026-08-22-2309-1（R1）收口：独立 closure audit 全 9 区 PASS，Major（日志缺口）当场补救，Plan Status → completed

- Plan: `ai-dev/plans/2026-08-22-2309-1-plugin-api-shrink-single-state-machine.md`（R1：nop-plugin API 收缩——单层六态状态机）。本条目补记 R1 执行关键事实 + 本日收口验证与 closure audit。
- 执行事实摘要（live repo 终态，git status 可核）：
  - api：删 `IPluginInstance.java`/`InstanceState.java`；`PluginState` 六态（UNLOADED/LOADED/ACTIVATING/ACTIVATED/DEACTIVATING/FAILED）；`IPlugin` 按 01 §7.1 重写（activate()→boolean、deactivate()→CompletionStage<Void>、getService(s)，删 getInstance/getInstances default）；`IPluginContext` → getPlugin/allPlugins/reconcile；IPlugin Javadoc 的 ServiceLoader 描述修正为 plugin.json + 反射。
  - manager：删 `PluginInstanceImpl.java`（592 行）与 parentToChildren/级联销毁/P2-A 快照/pending 机制；`IPluginManager` 删四个实例抽象方法、新增 activatePlugin/deactivatePlugin/getPlugin/reconcilePlugins；`VfsPluginDefinition`（669 行）承载单激活生命周期（子容器 + activator + effect 自动注册 + in-flight 单飞 + unload 须先 deactivate 守卫 + 失败阈值暂停迁移到定义级）；`ServiceProxy` 改绑插件激活态（scope 句柄判活，INACTIVE 快速失败）；`PluginManagerImpl`（630 行）HMR 过渡语义 = deactivate→unload→load（重放 updateConfig 累积值）→reconcile。
  - support：`AbstractPlugin` 落地 activate()/deactivate()（复用 doStart 容器路径 + 六态迁移；jar 轨门控恒空集无条件激活、激活回调跳过、load() 容忍 xdef 缺失）。
  - 测试面：删 TestPluginInstanceLifecycle/TestParentHierarchy；新增 `TestPluginLifecycle`（14 @Test，含端到端单激活流、六态可观测、幂等激活、unload 守卫、FAILED 可重试、失败阈值暂停、activator 接线 + quiescence）、`StateProbeActivator` + `state-probe.plugin.xml` fixtures、support 侧 `TestHelloCommand` + `nop/plugin.beans.xml`；TestAbstractPlugin（9 @Test，含 jar 轨契约：无 xdef 载体空定义加载成功/有载体仅元数据/空门集无条件达 ACTIVATED）；TestPluginDefaultMethods 收缩后 6 @Test。
- 偏差（Ownership Deviation，执行时生效）：roadmap 记在 R2 名下的 parentToChildren/级联销毁/P2-A 快照/pending/`ICoeffectEvaluator.isInstanceConfigMatched` 全部以 IPluginInstance 为类型或消费者，随 R1 编译依赖一并移除（`ICoeffectEvaluator` 收缩为定义级评估）；R2 = 残留引用审计（grep 清零确认）+ 错误码族退役 + reconcile 拓扑序/语义收口。R4 docs 同步时回读修正 roadmap R2 条目文字与 01-architecture-baseline.md 锚点表 R2 行措辞。
- Doc-sync: docs 同步 deferred to R4（roadmap 归属：docs-for-ai/03-modules/nop-plugin.md 重写 + INDEX/source-anchors 同步）。例外：`check-doc-links.mjs --strict` 为不可降级硬门禁，R1 删除 `IPluginInstance.java` 产生的 2 处死链接（`docs-for-ai/03-modules/nop-plugin.md` 源码锚点表行、`docs-for-ai/04-reference/source-anchors.md` PLG-002）已做最小修复（删死行 + PLG-002 改指 `PluginState.java` + 两处 R1 反转注记），全文重写仍归 R4。
- 验证（本日收口轮，全部 exit 0）：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS、0 failures/0 errors；`./mvnw checkstyle:check -Dcheckstyle.config.location=file://<repo>/checkstyle.xml -pl <三模块> -am` 通过；`node ai-dev/tools/scan-hollow-implementations.mjs <三模块 src/main> --severity high` 0 findings；api 零依赖 grep（io.nop.ioc/io.nop.xlang）0 命中；实例机制 API grep（IPluginInstance|InstanceState|createInstance|destroyInstance|isInstanceConfigMatched）三模块 main 0 命中（遗留错误码常量按 plan 裁定保留至 R2 退役，均有 legacy 注记）。
- 独立 closure audit（fresh session `ses_fd52e5ba8ffeLxeAjjvIL6vdSS`，read-only）：9 个审计区全部 PASS（契约面 vs 01 §7.1/§7.3/§7.6 逐签名一致、零残留、e2e/接线/jar 轨契约测试覆盖、Anti-Hollow 全链代码追踪 + hollow scan 独立复跑、Deferred 诚实性、下游 quarkus-demo 无破坏引用）。1 Major：Phase 1/2/3 的「ai-dev/logs/ 已更新」已勾选但 R1 执行日志条目缺失（08-22 仅记录起草）——本条目即为补救，audit 明示补救后可翻门禁。3 Minor 均为 R2 域内文档陈旧（`PluginManagerErrors.ERR_PLUGIN_ACTIVE_CHILDREN_EXIST` 缺 legacy 注记、`InstanceConfigProvider.java:32` 悬空 `{@link PluginInstanceImpl}`、`PluginScopeImpl` 两处 "instance 级" 措辞），随 R2 残留审计/类删除一并处理，不阻塞 R1 closure。
- Roadmap：R1 work item `planned` → `done`；R2-R4 待执行，反转里程碑保持 `todo`。
- 未提交 git（按 mission-driver 统一 commit 惯例）。
