# 2026-08-23

### Plan 344 Phase 3 第三单元：nop-graphql 15 条 P1/P2/P3 处置（执行引擎/JSON-RPC/WebSocket 订阅）

- Plan 344 Phase 3 第三单元 nop-graphql.md（P1×3 P2×5 P3×7，P0=0；统计表 P3 初记 6 系笔误，按发现列表对账修正为 7）：12 修复 + 2 暂缓 + 1 部分修复（P1-3 两子项修复 + autoconfig 注册暂缓），plan 条目勾选。前一会话中断遗留的未提交 graphql 改动由本会话接续收口：三处测试失败定位修复、七轮红验证、标注与落档。
- P1：① `BeanMethodBatchFetcher` 非 source 参数被首次调用冻结（loaderName 不含参数，第二分支参数静默丢弃整批按首分支计算）→ 参数纳入注册名（`loaderName + "@" + deepHashCode(keyArgs)`），无额外参数保持原名兼容；② WebSocket 订阅 onComplete/onError 后 activeOperations 不清理（id 复用触发 4409 断整连接、滞留条目耗尽 maxActiveOperations）→ 补双参 remove；**红测试发现审计建议的单点 remove 不足**：`sendStreamingMessage` 的 put 在 `stream.subscribe` 之后，同步 publisher 场景清理先于注册执行（remove 空跑）条目仍滞留 → put 调整为先于 subscribe；③ subscription-defaults.beans.xml 三重损坏 → 删除不存在的 `graphQLEngine` property + `start()` 补 `@PostConstruct` + `TestSubscriptionDefaultsBeans` 装配一致性守卫；autoconfig 注册**裁定暂缓**：`GraphQLSubscriptionManager.registerSubscription` 生产代码零调用方（JsonRpcWebSocketHandler 自管 activeOperations），manager 是未接线原型，激活 bean 只会空订阅主题并引入 nopMessageService 启动依赖，功能可用需先设计 WebSocket 会话接入 manager 的接线（设计级变更）。
- P2：① fragment 链式堆叠绕过 maxDepth 深度防护 → **判定语义经红测试修正**：初版"展开总深度 ≤ maxDepth"被平台自有标准 IntrospectionQuery 夹具证伪（test-fragment.graphql 展开 11 层 > 测试 maxDepth 10，生产默认 7 时标准 introspection 展开约 12 层，会拒绝所有标准 GraphQL 客户端）→ 最终限制单路径 fragment 引用嵌套层数 ≤ maxDepth（每 body 已各自受 maxDepth 校验，唯一无界因子是链长，总展开界定约 maxDepth×(maxDepth+1)），附带 fragment 循环引用解析期拒绝（原执行期无限递归 StackOverflow）；② JSON-RPC batch 单 entry 失败拖垮整批 → `executeCommandAsyncSafely`（同步 NopException catch + 异步 exceptionally 收敛为该 entry 错误响应）；③ notification fire-and-forget 吞异常 → whenComplete WARN（异常/错误响应两形态）；④ mutation 顶层字段不串行（异步副作用乱序，违背 GraphQL 规范）→ `_invokeOperations` mutation 分支改 thenCompose 串行链（仍在 operationInvoker 事务作用域内，异常沿链阻断后续），query 保持并行；⑤ DataLoader 首次注册 check-then-act 竞争 → `BeanMethodBatchFetcher` 与 `OrmBatchLoader.makeDataLoader` 均与 dispatchAll 同锁（synchronized(context)）先注册者胜出，显式重复注册守卫保留。
- P3：bare IAE/ISE 6 处错误码化（ERR_GRAPHQL_NULL_REQUEST/EMPTY_BIZ_OBJ_NAME/CLASS_NO_BIZ_MODEL/FRAGMENT_NOT_RESOLVED/NULL_OPERATION_FETCHER/PARSE_INVALID_EXTEND_SYNTAX，i18n zh/en + nop-cli-errors 聚合 7 条）；批量上限 off-by-one（`<=`→`<`，恰好等于上限的批量不再被拒）；空批返回 List.of(...) 类型一致 + executeAsync 空批分支保持 wire format 单错误对象；gRPC 反射服务配置化（`reflection-enabled` 默认 false 对齐 introspection 收紧语义 + 迁移废弃 ProtoReflectionService → V1；模块在父 pom 被注释非默认构建，单独 compile 验证）；GraphQL 路径失败日志对齐 RPC 路径 `logIfNotTraced`；inline fragment 在 fragment 名位置识别 `on` 关键字抛明确错误码（原 "on" 被当 fragment 名静默误解析）。**P3-1 all-or-nothing 错误语义裁定暂缓**：字段级错误隔离 + null 传播是平台级契约变更（改 fetchSelection/_fetchSelections/invokeOperations 各层、影响所有客户端可观察行为），all-or-nothing 是平台自始稳定契约，需设计决策。
- 红验证（单文件 stash 回滚修复→跑测试→恢复，七轮）：参数丢弃 `expected: <2> but was: <1>`、4409 复用断连（second subscription data 超时）+ onError 条目滞留（cleared 超时）、12 链与循环引用均 nothing thrown、ClassCastException: JsonRpcResponse cannot be cast to List（审计预言的潜伏陷阱实测命中）+ 批量 400 拒绝、`events=[start1, start2]`（step2 先于 step1 完成发起）、duplicated-loader 异常逐字吻合、inline fragment 无任何异常。免红测试注明理由：P3-2 异常类型替换（编译期错误码引用+全量回归覆盖）、P3-5 grpc 非默认构建模块、P3-6 纯日志降噪。
- 验证：`./mvnw test -pl nop-service-framework/nop-graphql/nop-graphql-core` **101 tests / 0 failures**（新增 TestGraphQLFragmentDepth×4 / TestGraphQLSerialMutations×1 / TestBeanMethodBatchFetcher×4 / TestJsonRpcService×5 / TestSubscriptionDefaultsBeans×2 + 既有 TestJsonRpcWebSocketHandler+2 / TestGraphQLDocumentParser+1 / MockGraphQLSchemaLoader 支持注入 extraBeans、test.graphql 增 Tree 递归类型）；orm/message 模块 BUILD SUCCESS；grpc 单独 `mvnw compile` 通过；公共行为变化（mutation 串行/fragment 深度/batch 语义）追加回归 `./mvnw test -pl nop-auth/nop-auth-service` **393 / 0 failures**。报告 15 条全部标注（grep findings 15 = 处置标注 15）。
- 偏差：前一会话遗留三处失败测试（本会话定位修复）——WebSocket 两测试系修复自身的 put-after-subscribe 缺陷（非测试问题）；fragment 深度测试系实现语义错误（严格展开深度破坏标准 introspection，重写为引用嵌套语义并新增 introspection 形态回归用例）。
- Doc-sync: No owner-doc update required（缺陷收敛与规范语义修复；fragment 深度与 mutation 串行是消除 DoS 绕过/契约漂移方向，无文档记载旧值）。
