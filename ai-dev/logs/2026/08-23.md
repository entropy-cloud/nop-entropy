# 2026-08-23

### Plan 2026-08-22-2309-1（R1）收口：独立 closure audit 全 9 区 PASS，Major（日志缺口）当场补救，Plan Status → completed

- Plan: `ai-dev/plans/2026-08-22-2309-1-plugin-api-shrink-single-state-machine.md`（R1：nop-plugin API 收缩——单层六态状态机）。本条目补记 R1 执行关键事实 + 本日收口验证与 closure audit。
- 执行事实摘要（live repo 终态，git status 可核）：
  - api：删 `IPluginInstance.java`/`InstanceState.java`；`PluginState` 六态（UNLOADED/LOADED/ACTIVATING/ACTIVATED/DEACTIVATING/FAILED）；`IPlugin` 按 01 §7.1 重写（activate()→boolean、deactivate()→CompletionStage<Void>、getService(s)，删 getInstance/getInstances default）；`IPluginContext` → getPlugin/allPlugins/reconcile；IPlugin Javadoc 的 ServiceLoader 描述修正为 plugin.json + 反射。
  - manager：删 `PluginInstanceImpl.java`（592 行）与 parentToChildren/级联销毁/P2-A 快照/pending 机制；`IPluginManager` 删四个实例抽象方法、新增 activatePlugin/deactivatePlugin/getPlugin/reconcilePlugins；`VfsPluginDefinition`（669 行）承载单激活生命周期（子容器 + activator + effect 自动注册 + in-flight 单飞 + unload 须先 deactivate 守卫 + 失败阈值暂停迁移到定义级）；`ServiceProxy` 改绑插件激活态（scope 句柄判活，INACTIVE 快速失败）；`PluginManagerImpl`（630 行）HMR 过渡语义 = deactivate→unload→load（重放 updateConfig 累积值）→reconcile。
  - support：`AbstractPlugin` 落地 activate()/deactivate()（复用 doStart 容器路径 + 六态迁移；jar 轨门控恒空集无条件激活、激活回调跳过、load() 容忍 xdef 缺失）。
  - 测试面：删 TestPluginInstanceLifecycle/TestParentHierarchy；新增 `TestPluginLifecycle`（14 @Test，含端到端单激活流、六态可观测、幂等激活、unload 守卫、FAILED 可重试、失败阈值暂停、activator 接线 + quiescence）、`StateProbeActivator` + `state-probe.plugin.xml` fixtures、support 侧 `TestHelloCommand` + `nop/plugin.beans.xml`；TestAbstractPlugin（9 @Test，含 jar 轨契约：无 xdef 载体空定义加载成功/有载体仅元数据/空门集无条件达 ACTIVATED）；TestPluginDefaultMethods 收缩后 6 @Test。
- 偏差（Ownership Deviation，执行时生效）：roadmap 记在 R2 名下的 parentToChildren/级联销毁/P2-A 快照/pending/`ICoeffectEvaluator.isInstanceConfigMatched` 全部以 IPluginInstance 为类型或消费者，随 R1 编译依赖一并移除（`ICoeffectEvaluator` 收缩为定义级评估）；R2 = 残留引用审计（grep 清零确认）+ 错误码族退役 + reconcile 拓扑序/语义收口。R4 docs 同步时回读修正 roadmap R2 条目文字与 01-architecture-baseline.md 锚点表 R2 行措辞。
- Doc-sync: docs 同步 deferred to R4（roadmap 归属：docs-for-ai/03-modules/nop-plugin.md 重写 + INDEX/source-anchors 同步）。例外：`check-doc-links.mjs --strict` 为不可降级硬门禁，R1 删除 `IPluginInstance.java` 产生的 2 处死链接（`docs-for-ai/03-modules/nop-plugin.md` 源码锚点表行、`docs-for-ai/04-reference/source-anchors.md` PLG-002）已做最小修复（删死行 + PLG-002 改指 `PluginState.java` + 两处 R1 反转注记），全文重写仍归 R4。
- 验证（本日收口轮，全部 exit 0）：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS、0 failures/0 errors；`./mvnw checkstyle:check -Dcheckstyle.config.location=file://<repo>/checkstyle.xml -pl <三模块> -am` 通过；`node ai-dev/tools/scan-hollow-implementations.mjs <三模块 src/main> --severity high` 0 findings；api 零依赖 grep（io.nop.ioc/io.nop.xlang）0 命中；实例机制 API grep（IPluginInstance|InstanceState|createInstance|destroyInstance|isInstanceConfigMatched）三模块 main 0 命中（遗留错误码常量按 plan 裁定保留至 R2 退役，均有 legacy 注记）。
- 独立 closure audit（fresh session `ses_fd52e5ba8ffeLxeAjjvIL6vdSS`，read-only）：9 个审计区全部 PASS（契约面 vs 01 §7.1/§7.3/§7.6 逐签名一致、零残留、e2e/接线/jar 轨契约测试覆盖、Anti-Hollow 全链代码追踪 + hollow scan 独立复跑、Deferred 诚实性、下游 quarkus-demo 无破坏引用）。1 Major：Phase 1/2/3 的「ai-dev/logs/ 已更新」已勾选但 R1 执行日志条目缺失（08-22 仅记录起草）——本条目即为补救，audit 明示补救后可翻门禁。3 Minor 均为 R2 域内文档陈旧（`PluginManagerErrors.ERR_PLUGIN_ACTIVE_CHILDREN_EXIST` 缺 legacy 注记、`InstanceConfigProvider.java:32` 悬空 `{@link PluginInstanceImpl}`、`PluginScopeImpl` 两处 "instance 级" 措辞），随 R2 残留审计/类删除一并处理，不阻塞 R1 closure。
- Roadmap：R1 work item `planned` → `done`；R2-R4 待执行，反转里程碑保持 `todo`。
- 未提交 git（按 mission-driver 统一 commit 惯例）。
