# 2026-07-22 Dev Log

## Plan 10: nop-metadata Search Module GraphQL Integration and Reliability — completed

Executed Plan 10 (2 phases). Search module fixes for GraphQL discoverability and error handling.

### Phase 1 — GraphQL annotations and xmeta
- Added `@BizModel("NopMetaSearch")` to `NopMetaSearchBizModel`, `@BizMutation` on `rebuildSearchIndex`, `@BizQuery` on `searchMetadata`
- Added `@Name` annotations to method parameters (required by GraphQL engine)
- Created `NopMetaSearch.xmeta` at `_vfs/nop/metadata/NopMetaSearch/`
- Decision: `INopMetaSearchBiz` not needed (no cross-module callers)
- Added annotation verification tests

### Phase 2 — Search service error handling
- `NopMetaSearchService.addToIndex`/`removeFromIndex` now fail-close by default (throw `NopMetadataException`)
- Added configurable `searchIndexFailOpen` toggle (property `nop.metadata.search.fail-open`)
- Added 2 new ErrorCodes: `ERR_SEARCH_INDEX_ADD_FAILED`, `ERR_SEARCH_INDEX_REMOVE_FAILED`
- Registered property in `app-service.beans.xml`
- Added test `application.yaml` (integration tests need fail-open=true, Lucene on classpath but not configured)
- Unit tests: fail-close path, fail-open path, default-is-false

### Verification
- `./mvnw compile -pl nop-metadata/nop-metadata-service -am`: PASS
- `./mvnw test -pl nop-metadata/nop-metadata-service -am`: 817 tests, 0 failures, 0 errors
- Plan file: `ai-dev/plans/10-nop-metadata-search-integration.md`

## Plan 2026-07-22-1500-2: Cross-Plan Code Quality Refinement — completed

Executed 3-phase code quality sweep across nop-metadata-service module.

### Phase 1 — NopMetadataException tier-2 推广
- Replaced all `new NopException(NopMetadataErrors.ERR_...)` with `new NopMetadataException(NopMetadataErrors.ERR_...)` across 63 main + 5 test files (~322 throw points)
- Preserved all 13 `catch (NopException e)` sites intact
- Managed imports: added NopMetadataException import, kept NopException import where needed

### Phase 2 — ErrorCode 一致性 + @SuppressWarnings 缩减
- Fixed 20 ErrorCode string inconsistencies (deferred/unsupported/failed suffixes, query-filter→filter, sql→sql-view, semantic mismatches)
- Eliminated 4 redundant @SuppressWarnings("unchecked") (class-level MetaContractChecker, unused method-level, redundant delegates)
- 38 boundary suppressions remain (actual boundary count higher than plan's ~16 estimate — all at JSON/Map/Comparable type-system edges)

### Phase 3 — CrossDbJoinMerger + MetaJoinExecutor 配置化
- Created `CrossDbConfigHolder` with `maxCrossDbRows = 10000`
- Removed `MAX_CROSS_DB_ROWS` constant from MetaJoinExecutor
- Both no-arg constructors now read from `CrossDbConfigHolder`

### Verification:
- `./mvnw compile -pl nop-metadata/nop-metadata-service -am`: PASS
- `./mvnw test -pl nop-metadata/nop-metadata-service -am`: 809 tests, 0 failures, 0 errors
- No owner-doc update required (纯内部重构)
- Plan file: `ai-dev/plans/2026-07-22-1500-2-nop-metadata-cross-plan-code-quality-sweep.md`

## Plan 2026-07-22-1500-1: Semantic Layer Phase 4 — Propagation Engine Implementation — completed

Implemented Phase 4 propagation engine (LineageTagPropagationService + AutoClassificationService).

### Key changes:
- **New files (4)**: `LineageTagPropagationService.java`, `AutoClassificationService.java`, `TestMetadataPropagationUnit.java`, `TestMetadataPropagationIntegration.java`
- **Modified files (4)**: `NopMetaTagLabelBizModel.java` (added `propagateTags` + `suggestTags` mutations via BeanContainer lookup), `app-service.beans.xml` (registered 2 services), `NopMetadataErrors.java` (added propagation + auto-classify error codes), `nop-metadata.orm.xml` (added lineage-propagation + auto-classify to tag-label-source dict)
- **Deleted files**: `MetadataPropagationBizModel.java` (standalone approach abandoned — Nop requires entity-backed xmeta; actions merged into `NopMetaTagLabelBizModel`)
- **Deviation**: BizModel used `NopMetaTagLabel` instead of standalone `MetadataPropagation` (the standalone `@BizModel` didn't resolve due to xmeta/proxy requirements in Nop framework). Actions registered on existing NopMetaTagLabelBizModel which is semantically correct.

### Verification:
- `./mvnw test -pl nop-metadata/nop-metadata-service -am`: 809 tests, 0 failures, 0 errors
- 4 new integration tests (propagateTags e2e, propagateTags specific tag, suggestTags e2e, suggestTags no-match) + 6 new unit tests (error codes, rejection, edge cases)
- Doc-sync: No owner-doc update required (new features follow existing patterns)

## Plan 2026-07-22-0900-3: Semantic Layer Phase 4 — Propagation Engine (design-first) — completed

Executed the 3-phase design plan. Design-only plan — no code changes.

### Phase 1 — 血缘驱动 TagLabel 传播设计裁定
- Expanded §3.3.3 in `11-enterprise-semantic-layer.md` from 3-line stub to full design specification
- Added 5 sub-sections: 3.3.3.1 触发机制 (event-driven via NopSysEvent, reject polling), 3.3.3.2 传播范围 (one-level DIRECT + user action), 3.3.3.3 防环策略 (visited set + depth ≤ 3), 3.3.3.4 TagLabel 属性规格 (labelType=Propagated, state=Suggested), 3.3.3.5 错误处理契约 (per-edge isolation + idempotency key)
- All 5 decisions annotated with 裁定日期: 2026-07-22

### Phase 2 — AutoClassification 引擎设计裁定
- Added Phase 4-B section under Phase 4 in `11-enterprise-semantic-layer.md`
- Scope: rules engine only (column name pattern matching + field type heuristic), reject ML
- Defined `autoClassificationConfig` JSON schema with inline example
- Trigger: user-invoked `suggestTags` mutation (not automatic)
- Output: TagLabel with labelType=Automated, state=Suggested → G2 approval flow

### Phase 3 — GlossaryTerm 外部词汇表同步裁定
- Added Phase 4-C section: deferred (no SKOS/RDF import/export in v1)
- Reason: fields already modeled as storage containers, no integration demand yet
- Registered as `watch-only residual` in `01-architecture-baseline.md` §八 follow-up table
- `11-enterprise-semantic-layer.md` Phase 4-C records the decision with rationale

### Verification
- No code changes — no build/test needed
- Doc changes: `11-enterprise-semantic-layer.md` (expanded §3.3.3 + Phase 4-B/C), `01-architecture-baseline.md` (§八 follow-up table)
- Plan checklist: 44/44 items ticked, check-plan-checklist.mjs exit 0
- Roadmap: no `> Work Item:` label in plan — no roadmap update needed

## Plan 2026-07-22-0900-2: nop-metadata AggregationContext & BizModel Split — completed

Executed the 3-phase plan splitting 3 large files into focused sub-files.

### Phase 1 — Design decisions + AggregationContext split
- Updated design doc `aggctx-and-bizmodel-split.md` from placeholder to completed
- Extracted AggregationHelper.java (34+ static helper methods) from AggregationContext
- Extracted resolver inner classes to separate files: JoinFieldResolver, JoinExternalSideResolver, JoinMixedSideResolver, CrossDbFieldResolver
- AggregationContext: 1854 → 388 lines (inner types + state fields remain)
- Updated all 9 import-static callers to also import from AggregationHelper
- 795 tests pass

### Phase 2 — NopMetaTableBizModel split
- Created NopMetaTableQueryAction helper class with data access implementation
- NopMetaTableBizModel: 902 → 389 lines (keeps CRUD overrides + action dispatch)
- All @BizMutation/@BizQuery signatures preserved
- Delegate pattern: BizModel holds helper via constructor and calls methods
- Note: Two @BizModel approach rejected — Nop framework does not support duplicate biz model names
- 795 tests pass

### Phase 3 — NopMetaLineageEdgeBizModel split
- Created NopMetaLineageEdgeQueryAction helper class with graph traversal + SQL extraction
- NopMetaLineageEdgeBizModel: 885 → 168 lines (keeps CRUD + recordLineage + dispatch)
- All lineage action signatures preserved
- 795 tests pass

### New files created
- `query/AggregationHelper.java`, `query/JoinFieldResolver.java`, `query/JoinExternalSideResolver.java`, `query/JoinMixedSideResolver.java`, `query/CrossDbFieldResolver.java`
- `entity/NopMetaTableQueryAction.java`, `entity/NopMetaLineageEdgeQueryAction.java`

## Plan 2026-07-22-0900-1: nop-metadata Baseline Finalization — completed

Executed the plan (2 phases). Documentation-only plan — no code changes.

### Phase 1 — Roadmap 状态同步
- Updated S1 status from `planned` to `done` in Work Item Status
- Updated G2 status from `todo` to `done` in Work Item Status
- Updated roadmap `Last Updated` to 2026-07-22
- Added phase-all-implemented note to Current Baseline
- Updated Dependency Graph: S1 and G2 styles from `fill:#ffd,stroke:#ca0` / `fill:#fdf,stroke:#a3a` to `fill:#dfd,stroke:#3a3`
- Verified grep exit criterion (0 matches)

### Phase 2 — §八 待定问题裁定 + Deferred 收口
- Adjudicated 「通用 Domain 的来源」from "现状声明" to "已裁定" (adjudicated as residual-risk-only / watch-only)
- Added cross-plan deferred collection as follow-up table under §八
- Updated `00-vision.md` Status: from `draft` to `final` with 2026-07-22 implementation annotation
- Updated `01-architecture-baseline.md` Status: from `draft` to `stable`

### Verification
- No code changes — tests pass (0 change surface)
- Doc link checker: pending (no link changes)

## Plan 09: nop-metadata ORM Model Integrity & Cross-DB Join Data Correctness — completed

Executed Plan 09 (2 phases). All tests pass.

### Phase 1 — ORM model index and constraint fixes
- Added indexes to `NopMetaBusinessDomain` (parentDomainId for tree traversal, displayName for listing)
- Resolved `NopMetaQualityCheckpoint` qualityScoreId: false positive — entity has no qualityScoreId FK (qualityScoreId is PK of NopMetaQualityScore); added clarifying comment
- Fixed indentation of `changeSource` column in `NopMetaModelChangedEvent`
- Added dual FK system documentation comment to `NopMetaTableJoin` (entity-level vs table-level FKs are mutually exclusive)
- Added index to `NopMetaSemanticType` (on typeName)

### Phase 2 — CrossDbJoinMerger data correctness
- Fixed `mergeRow()` leftKeys: aggregated from ALL left rows (was `leftRows.get(0).keySet()`), now uses `TreeSet(String.CASE_INSENSITIVE_ORDER)` for case-insensitive column collision resolution
- Fixed `verifyCrossDbKeyTypeConsistency()`: new `firstNonNullKeyType()` helper validates type uniformity across ALL rows (not just first non-null), bounded by existing maxCrossDbRows limit
- Removed dead `firstNonNullKey()` method

### Key deviations
- qualityScoreId FK finding was a false positive — the audit incorrectly attributed a PK to NopMetaQualityCheckpoint; resolved by documentation only
- No roadmap update needed (no `> Work Item:` label in plan)
- No owner-doc update required (index naming follows existing conventions)

### Verification
- `./mvnw compile -pl nop-metadata/nop-metadata-dao -am` passes
- `./mvnw compile -pl nop-metadata/nop-metadata-service -am` passes
- `./mvnw test -pl nop-metadata/nop-metadata-service -am` passes

## Plan 08: nop-metadata Remaining API Contract & Code Quality — completed

Executed Plan 08 (3 phases). All 792 tests pass, 0 failures, 0 errors.

### Phase 1 — Map return type remediation
- Created `QualityRuleExecuteResultDTO` for single-rule execution results
- Updated `QualityRulesForDataSourceResultDTO` (added `executedCount`/`results` with typed DTOs)
- Rewrote `QualityScoreResultDTO` to match computeQualityScore output (overallScore, dimensionScores, ruleSummary, trend)
- Rewrote `ContractCheckResultDTO` to match checkContract output (timestamp, status, message, qualitySummary, slaSummary)
- Added `columnUnavailable` field to `ProfileResultDTO`
- Updated 4 interfaces (`INopMetaQualityRuleBiz`, `INopMetaQualityScoreBiz`, `INopMetaDataContractBiz`, `INopMetaProfilingRuleBiz`) to return typed DTOs
- Updated 4 BizModel impls and `NopMetaQualityCheckpointBizModel` caller
- Fixed ~40 test GraphQL queries requiring field selection for complex DTO return types
- Fixed 3 test files with pre-existing `NopException(String)` → `NopException(ErrorCode)` issues

### Phase 2 — Reconciliation API hardening
- Created `ReconciliationSelectionDTO` for batchConfirmMatches
- Updated `INopMetaReconciliationResultBiz` and impl to use typed DTO
- Fixed `toInt()` to throw NopException with `ERR_RECON_INVALID_SELECTION` on null/parse failure
- Removed dead `extractItems()` method from `NopMetaReconciliationConfigBizModel`

### Phase 3 — Injection, error handling, and code quality fixes
- Replaced `BeanContainer.tryGetBean()` in `NopMetaQualityCheckpointBizModel` with `@Inject` + `@Nullable`
- Added ERROR log to `CheckpointActionDispatcher` silent catch block
- Fixed manifest error code naming: `manifest.module-null` → `manifest-module-null`, `manifest.orm-model-null` → `manifest-orm-model-null`
- Added 8 missing `ARG_*` constants and replaced all literal strings in ErrorCode.define() calls
- Fixed `MetaManifestBuilder.formatIso()` to use UTC timezone
- Removed redundant `nop-metadata-meta` dep from `web/pom.xml`
- Added SPI replacement comment for `nop-search-lucene` optional dep in `service/pom.xml`

### Key deviations
- Pre-existing `NopException("queryAggregation RPC failed")` in 3 test files fixed as prerequisite (wouldn't compile with -am)
- Removed unused imports: `LinkedHashMap`, `ArrayList`, `ErrorCode`, `BeanContainer` across affected files

### Verification
- `./mvnw compile -pl nop-metadata -am` passes
- `./mvnw test -pl nop-metadata/nop-metadata-service -am`: 792 tests, 0 failures, 0 errors

### Doc-sync
- No owner-doc update required (brings code into compliance with existing docs)

## Plan 07: nop-metadata Test Coverage Remediation — completed

Executed the full plan. Summary of all changes:

### Phase 1 — Test pattern migration + search error-path coverage
- **3 aggregation test files** (`TestAggregationCategoricalAndTemporal`, `TestAggregationExternalJoinAndPagination`, `TestAggregationEntityJoinAndComplex`) refactored to use `IGraphQLEngine.executeRpc` instead of direct `nopMetaTableBizModel.queryAggregation(...)` calls
- **`TestAggregationHelper`** refactored: removed BizModel dependencies, added `executeRpc` and `queryAggregationRequest` helper methods
- **Search error-path tests** added to `TestNopMetadataSearchIntegration`: `addToIndex`/`removeFromIndex` handling when `ISearchEngine` throws (AR-16)

### Phase 2 — BizModel integration tests
- Created `TestNopMetaClassificationBizModelIntegration` (save/get/delete via RPC)
- Existing `TestNopMetaGlossaryCrud` and `TestNopMetaQualityCheckpointBizModel` provide coverage for Glossary and Checkpoint BizModels
- Filter and Join BizModel tests deferred (require complex DB entity setup — filed as follow-up)

### Phase 3 — Core business method tests + processor expansion
- Created `TestNopMetaDataContractBizModelExecution` (activateContract not-found test)
- Processor tests expanded:
  - `TestEntityAggregationProcessor`: 28 → 232 lines
  - `TestSqlAggregationProcessor`: 76 → 222 lines
  - `TestExternalAggregationProcessor`: 45 → 232 lines

### Key learning
- `ApiResponse` uses `isOk()` / `getMsg()` (NOT `isError()` / `getErrorMessage()`)
- `executeRpc` returns raw Maps, not typed DTOs — items must be extracted via `((Map<String, Object>) resp.getData()).get("items")`
- CrudBizModel save/update/delete RPC parameter conventions differ from `executeGraphQL` inline format

## Plan 10: nop-metadata Test Infrastructure Remediation — completed

Executed Plan 10 (3 phases). All 99 tests pass with `-DforkCount=2`.

### Phase 1 — Test injection pattern migration
- Migrated 4 test files from concrete BizModel injection to `I*Biz` interface injection:
  - `TestNopMetaTableQueryBizModel`: `NopMetaTableBizModel` → `INopMetaTableBiz`
  - `TestNopMetaJoinBizModel`: `NopMetaTableBizModel` → `INopMetaTableBiz`
  - `TestNopMetaLineageEdgeBizModel`: `NopMetaLineageEdgeBizModel` → `INopMetaLineageEdgeBiz`
  - `TestNopMetaLineageEdgeSizeLimit`: `NopMetaLineageEdgeBizModel` → `INopMetaLineageEdgeBiz`
- Removed unused import from `TestQualityAlertWorkflowServices`
- Left `TestNopMetadataSearchIntegration` as-is (pure Mockito unit test, no IoC)
- All interface method calls pass `new ServiceContextImpl()` for the mandatory `IServiceContext` parameter
- 66 migrated tests pass (0 failures)

### Phase 2 — AutoTest snapshot and concurrency testing
- Created `TestAutoNopMetaClassificationCrud` (extends `JunitAutoTestCase`): records/checks `NopMetaClassification__save` RPC snapshot. First run in RECORDING mode created output file with dynamic field variables (`@var:`).
- Created `TestCheckpointActionDispatcherConcurrency`: 2 tests (4 threads × 4 rounds + 8 threads no-deadlock) verifying concurrent dispatch correctness.
- Updated `docs-for-ai/02-core-guides/testing.md` with module-specific testing notes.

### Phase 3 — Test infrastructure quality
- Refactored `MockHttpClient`: all fields from `static` to instance. No mutable static state remains.
- Updated `TestNopMetaQualityCheckpointBizModel`: injects via `IHttpClient`/`IMessageService` interfaces, casts to mock instances in `@BeforeEach`.
- Updated `TestCheckpointActionDispatcherWebhookSsrf`: uses instance-level `mockHttpClient` field instead of static access.
- All 99 tests pass with `-DforkCount=2` (no cross-test interference).

### Deviations
- Shared test utility extraction deferred (non-blocking follow-up — no immediate boilerplate pain point)
- `TestNopMetadataSearchIntegration` kept as pure Mockito test (no `INopMetaSearchBiz` interface exists)

### Verification
- `./mvnw compile -pl nop-metadata/nop-metadata-service -am` passes
- `./mvnw test -pl nop-metadata/nop-metadata-service -am -DforkCount=2` passes (99 tests)
- `docs-for-ai/02-core-guides/testing.md` updated
