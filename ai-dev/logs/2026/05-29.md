# AI 开发日志 — 2026-05-29

### 2026-05-29 API XML focused proof and template fix follow-up

- Plan 58 继续执行：将 `api.imp.xml` 中依赖的派生属性计算收回到 `ApiModel`、`ApiServiceModel`、`ApiMethodModel` 的 `INeedInit` / Java helper，并为 API message 字段补充 `codegenJavaType`，避免 `Timestamp` 一类类型在 API bean 生成时退化成不可编译简名。
- 影响范围：`nop-network/nop-rpc/nop-rpc-model` 的 API model 初始化逻辑、`/nop/rpc/imp/api.imp.xml`、`nop-kernel/nop-codegen` 的 API bean 模板，以及 `nop-rule` / `nop-wf` 基于 source `model/*.api.xml` 重新落盘的 API generated outputs。
- focused 验证：`./mvnw -f "nop-network/nop-rpc/pom.xml" test -pl "nop-rpc-model" -am -T 1C -Dtest=TestApiMethodModel -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- focused 验证：`./mvnw -f "nop-rule/pom.xml" -pl "nop-rule-codegen" -am package -DskipTests -T 1C` 通过。
- focused 验证：`./mvnw -f "nop-wf/pom.xml" -pl "nop-wf-codegen" -am package -DskipTests -T 1C` 通过。
- focused 验证：`./mvnw -f "nop-wf/pom.xml" -pl "nop-wf-web" -am package -DskipTests -T 1C` 通过。
- focused 验证：`./mvnw -f "nop-rule/pom.xml" -pl "nop-rule-web" -am package -DskipTests -T 1C` 通过；构建日志显示 `nop-rule-web` 的 `precompile` 已运行，`nop-rule-api` / `nop-rule-codegen` / `nop-rule-meta` / `nop-rule-web` 全链路 `BUILD SUCCESS`。
- 偏差更正：重跑广义门禁后，真实阻塞并非 `nop-graphql-core`，而是 `nop-rule-service` 在构建 `RuleLogMessageBean` 的 BizModel/xmeta 时仍消费到 unresolved `Timestamp`；根因是 API bean Java 模板已切到 resolved type，但 API-to-xmeta 模板仍输出 `${field.schema.type}`，导致运行时 xmeta 与 Java bean 类型不一致。
- 修复：`nop-kernel/nop-codegen/.../_{messageModel.name}.xmeta.xgen` 改为输出 `${field.codegenJavaType}`，使 API xmeta 与 Java bean 共用 resolved Java type；同时在 `TestApiMethodModel` 增加 `Timestamp -> java.sql.Timestamp` 回归断言，锁定 `ApiMessageFieldModel.getCodegenJavaType()` 的标准类型别名解析。
- 验证：`./mvnw -f "nop-network/nop-rpc/pom.xml" test -pl "nop-rpc-model" -am -T 1C -Dtest=TestApiMethodModel -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- 验证：`./mvnw -f "nop-rule/pom.xml" test -pl "nop-rule-service" -am -T 1C -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- 验证：`./mvnw test -pl nop-rule,nop-wf -am -T 1C -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- closure audit：独立 `general` subagent 首轮审计 Plan 58 结论为 `not completed`；第二次独立审计确认代码与验证证据已充分，剩余仅是计划文本收口。现已同步 Plan 58 的 Phase 2 / Closure Gates / Anti-Hollow 项并将计划标记为 `completed`。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过。
- Doc-sync: `docs-for-ai/02-core-guides/external-app-development.md` 已更新；Plan 58 已同步最新 proof/blocker 与 closure audit，仍保持进行中。

### 2026-05-29 API XML source baseline migration

- Plan 58 进入实施：使用 `nop-cli` 将 `nop-rule/model/nop-rule.api.xlsx`、`nop-wf/model/nop-wf.api.xlsx` 转换并落盘为同目录 `*.api.xml`，同时保留原始 `api.xlsx`；live generator 输入已切到 `nop-rule` / `nop-wf` 各自的 `model/*.api.xml`。
- `nop-ai-coder` 收口 XML 路径：`ai-api-design.task.xml` 的 XML 输出从固定 `model/ai-gen.api.xml` 改为 `model/${appName}.api.xml`，`ai-service-design.task.xml` 同步改为读取同一路径，Excel 导出 `model/${appName}.api.xlsx` 保留不变。
- 新增本地 focused proof：`ApiXmlTaskWiringTest` + `/test/ai/api-xml-wiring.task.xml` 在无在线 AI 依赖下验证 `${appName}.api.xml` 被保存并被 `LoadApiModel` 成功加载，断言 producer/consumer wiring 已接通。
- 验证：`./mvnw test -pl nop-ai/nop-ai-coder -am -T 1C -Dtest=ApiXmlTaskWiringTest -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- 偏差：`nop-rule` / `nop-wf` 的 focused generator 验证与独立 closure audit 尚未完成，因此 Plan 58 仍处于进行中。
- Doc-sync: `docs-for-ai/02-core-guides/external-app-development.md` 已更新。

### 2026-05-29 wf tcc web codegen plugin follow-up

- 为 `nop-wf/nop-wf-web`、`nop-tcc/nop-tcc-web` 补齐 `exec-maven-plugin` 声明后，重新执行 web 模块生成链，验证 `precompile` / `precompile2` / `postcompile` 在 web 层已真正接入 Maven 生命周期。
- `nop-wf/nop-wf-web` 重新生成 `_nop-wf.action-auth.xml`，菜单 icon 与最新模块图标约定保持一致；`nop-tcc/nop-tcc-web` 首次稳定落盘 `auth/` 与 `pages/` 生成资源。
- 验证：`./mvnw install -pl nop-wf/nop-wf-web,nop-tcc/nop-tcc-web -am -DskipTests -T 1C` 通过。
- Doc-sync: No owner-doc update required.

### 2026-05-29 codegen phase docs clarification

- 补充 `docs-for-ai/03-runbooks/debug-codegen-and-generated-files.md`：明确要求先检查子模块 `pom.xml` 是否显式声明 `exec-maven-plugin`，并补齐 `precompile` / `precompile2` / `postcompile` 的 Maven phase 与 classpath 差异说明，避免再把三者当成等价阶段处理。
- 补充 `docs-for-ai/04-reference/source-anchors.md`：新增根 `pom.xml` 中 `exec-maven-plugin` execution 绑定作为生成链排障锚点。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过。
- Doc-sync: `docs-for-ai/03-runbooks/debug-codegen-and-generated-files.md`、`docs-for-ai/04-reference/source-anchors.md` 已更新。

### 2026-05-29 demo custom menu icons

- 修复 `nop-demo/nop-spring-demo` 与 `nop-demo/nop-quarkus-demo` 的手写 demo 菜单图标缺失问题：`功能演示` 从默认 `ion:grid-outline` 改为 `flask-conical`，`报表演示` / `报表传参演示` 改为 `chart-column`，`后台验证` 改为 `shield-check`。
- 根因：这批菜单来自手写 `app.action-auth.xml`，未走此前 ORM/module-meta 图标传播链，因此仍保留默认或旧命名风格 icon。
- 验证：`./mvnw install -pl nop-demo/nop-spring-demo,nop-demo/nop-quarkus-demo -am -DskipTests -T 1C` 通过。
- Doc-sync: No owner-doc update required.

### 2026-05-29 module icon propagation follow-up and sitemap debug dump

- 执行完成两组收尾改动：一组为 `nop-auth` 站点菜单调试导出，另一组为模块级 icon 继续传播到生成产物与 VFS 索引。
- 站点菜单调试：`nop-auth/nop-auth-service/src/main/java/io/nop/auth/service/sitemap/SiteMapProviderImpl.java` 在 `AppConfig.isDebugMode()` 下输出 `/nop/main/site/{locale}-menu.yaml`，便于直接比对运行时菜单树；同时为 `SiteCacheData` 补齐 Quarkus native 反射配置。
- 模块 icon 传播：为 `nop-ai`、`nop-batch`、`nop-code`、`demo-ddd`、`nop-dyn`、`nop-file`、`nop-job`、`nop-retry`、`nop-rule`、`nop-sys`、`nop-task`、`nop-tcc`、`nop-wf`、`nop-oauth` 等模块的生成 `_app.orm.xml`、`_module-meta.json`、`module-meta.json`、`_*.action-auth.xml` 与 `nop-vfs-index.txt` 收口 icon 产物，使 TOPM 菜单与模块元数据一致。
- 验证：`./mvnw install -pl nop-auth,nop-auth/nop-oauth,nop-ai,nop-batch,nop-code,nop-demo/nop-ddd-demo,nop-dyn,nop-file,nop-job,nop-retry,nop-rule,nop-sys,nop-task,nop-tcc,nop-wf -am -DskipTests -T 1C` 通过。
- 验证：`./mvnw test -pl nop-auth -am -T 1C` 通过。
- 偏差：工作区存在大量 `nop-frontend-support/nop-web-site/src/main/resources/...` 未跟踪前端构建产物，与本轮两组改动主题无关，本次未纳入提交。
- Doc-sync: No owner-doc update required.
