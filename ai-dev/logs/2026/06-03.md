# AI 开发日志 — 2026-06-03

### 2026-06-03

- Plan 104 完成：将 `nop-api-core` 中既有 `ICrudApi<T>` 演进为 `ICrudApi<I, O>`，新增 `ITreeApi<O>` 与 `CrudInputBase`，并补 `TestCrudInputBase` 覆盖 `_chgType` 与 `_extAttrs` 的 JSON 展开/吸收语义。
- 在 `nop-kernel/nop-codegen` 新增 `/nop/templates/crud-api/` 模板集，通过 `DefineLoopForOrm` + `meta-gen.xlib` 从 ORM 模型生成 CRUD `InputBean` / `OutputBean` / `Api` 接口；生成层关系 InputBean 实际引用 `_gen` 包中的基类，避免生成层反向依赖保留层。
- 在 `nop-auth/nop-auth-meta/precompile/gen-crud-api.xgen` 接入新模板，成功生成 `nop-auth-api/src/main/java/io/nop/auth/api/{beans,crud}` 下的 CRUD API 文件；抽查 `NopAuthUser` 确认 `password` 存在于 InputBean 且排除于 OutputBean，`NopAuthDept` 正确额外继承 `ITreeApi`。
- 偏差：原 plan / design 预期把 CRUD API 设计成 `ApiResponse`/JAX-RS 风格的新接口，但仓库中已存在 `io.nop.api.core.api.ICrudApi<T>`。经执行中裁定，实际落地为复用并演进既有接口为双泛型版本，保留 `ICancelToken` / `FieldSelectionBean` 风格，只对强类型输入输出做最小正确改动；已同步回 `ai-dev/design/crud/crud-api-codegen-design.md` 与 Plan 104。
- 验证：`./mvnw test -pl nop-kernel/nop-api-core -am` 通过；`./mvnw clean install -pl nop-auth/nop-auth-meta -am -DskipTests` 通过；`./mvnw clean install -pl nop-auth/nop-auth-api -am -DskipTests` 通过；`./mvnw clean install -pl nop-auth -am -DskipTests` 通过；`./mvnw test -pl nop-auth -am` 通过（命令超出 bash 超时，但 Maven summary 已显示 reactor BUILD SUCCESS）。
- Doc-sync: `docs-for-ai/02-core-guides/api-model-and-codegen.md`、`docs-for-ai/03-runbooks/debug-codegen-and-generated-files.md`、`ai-dev/design/crud/crud-api-codegen-design.md`、`ai-dev/plans/104-crud-api-codegen-implementation.md` 已更新。

### 2026-06-03

- 为 `docs-for-ai/` 新增 `02-core-guides/application-development-workflow.md`，把外部业务应用从本地 requirement/design、源 ORM 模型与 `api.xml`、首次 `nop-cli gen`、后续 `mvnw` 增量生成、保留层扩展、BizModel / Processor、Delta、测试到 `*-app` 联调的默认闭环串成一份 canonical guide，并明确外部应用的源 ORM 模型可以是 XML 或 Excel。
- 更新 `docs-for-ai/INDEX.md` 增加该规程入口，避免后续开发只能在 `application-project-defaults.md`、`model-first-development.md`、`external-app-development.md`、`service-layer.md` 和 `testing.md` 之间人工拼接高频做法。
- 关键决策：把“应用开发工作规程”定义为 `02-core-guides/` 级别的默认模式，而不是单一 runbook 或应用项目本地私有经验。
- 下一步：对新规程做一次独立复核，确认它没有把项目经验误写成平台硬规则。

### 2026-06-03

- 为 `docs-for-ai/` 新增 `02-core-guides/reporting-and-notification-integration.md`，补齐外部应用项目里“报表默认走 `nop-report`、邮件/短信默认走 `nop-integration`”的高频默认路线。
- 更新 `docs-for-ai/INDEX.md` 增加该路由，并在 `04-reference/source-anchors.md` 补充 `ISmsSender`、`IEmailSender` 与 `nop-report` 页面/模型锚点。
- 关键决策：将这类问题定义为平台级 canonical owner，而不是让应用项目每次靠 grep / 源码临时确认。
- 下一步：继续完善 `nop-app-mall` 本地 design owner docs 的评论/评价与售后深度语义。
