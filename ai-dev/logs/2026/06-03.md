# 2026-06-03

## Plan 104 — nop-job Round 4 Adversarial Review Remediation

Plan 104 completed. Fixed 6 findings from Round 4 adversarial review (AR-19~AR-24):

- **AR-19 (P1)**: `cancelFire` schedule update now uses `tryUpdateManyWithVersionCheck` with 5 retries, `activeFireCount` recalculated from fresh value
- **AR-20 (P1)**: `insertTasksAndMarkFireDispatching` uses version check before task insertion, throws `NopException` on conflict (txn rollback)
- **AR-21 (P2)**: `completeFireAndUpdateSchedule` retries 5 times, throws on exhaustion instead of silent `updateEntityDirectly`
- **AR-22 (P2)**: `persistSchedule` uses optimistic lock with engine field restoration on version conflict
- **AR-23 (P2)**: `JobCompletionProcessorImpl` uses null-safe `tryLoadSchedule`, marks fire FAILED when schedule deleted
- **AR-24 (P2)**: `NopRetryJobRetryBridge.onFireFailed` returns `null` instead of `jobFireId`

New error codes: `ERR_JOB_FIRE_STATUS_CONFLICT`, `ERR_JOB_SCHEDULE_DELETED`.
All tests pass. Closure audit completed by independent sub-agent.

## Plan 08 Closure — nop-code Frontend Plan Completed

Plan 08 (nop-code-frontend-plan) completed. All 8 phases implemented, build passes.

### Changes
- **NopCodeIndex.view.xml**: Added symbolCounts distribution table to stats dialog (C3/C22)
- **NopCodeSymbol.xmeta**: Removed redundant `kinds` prop, added `ui:filterOp="in"` on `kind` prop for standard Nop filter mechanism (C14)
- **NopCodeSymbol.view.xml**: Query form uses `kind` instead of `kinds`
- **code-browser.view.xml**: Added sourceCode display panel (C9)
- **type-hierarchy.view.xml**: Direction field uses dict dropdown, results rendered as `input-tree` instead of raw JSON (C15/C16)
- **call-hierarchy.view.xml**: Same improvements as type-hierarchy (C18/C19)
- **dashboard.view.xml**: Added symbolCounts distribution table to stats dialog (C22)
- **docs-for-ai/03-runbooks/custom-query-with-querybean.md**: Added multi-select IN query best practice (filter_{field}__in pattern, ui:filterOp)

### Key Lesson
AI created a redundant `kinds` (List<String>) prop because it didn't know Nop's standard `filter_` prefix mechanism for IN queries. The correct pattern is `ui:filterOp="in"` on the existing `kind` prop — the frontend automatically generates `filter_kind__in` field name and the backend receives a standard `FilterBeans.in("kind", values)`.

## Plan Status Fixes — 3 Plans Corrected

`check-plan-status.mjs` reported 4 Active plans, 3 of which were actually completed but had parsing issues. See earlier log entry for details.

## Plan 103 Closure — nop-job Adversarial Review Remediation Completed

Plan 103 fully completed and audited. All 13 in-scope findings (5 P1 + 8 P2) from the 2026-06-03 adversarial review have been fixed.

### Phase 5 (final phase) — AR-4 retryRecordId dead-write

- Added `updateRetryRecordId(String jobFireId, String retryRecordId)` to `IJobFireStore` interface
- Implemented in `JobFireStoreImpl` with real SQL UPDATE via `updateEntityDirectly`
- `JobCompletionProcessorImpl.handleRetryAndAlarm` now calls `fireStore.updateRetryRecordId()` after setting `retryRecordId`
- Fixed compilation errors in 3 test mock stores (MockFireStore, SimpleFireStore) that needed the new interface method
- Commit: `f932aae90`

### Closure Audit

- Independent sub-agent audit (houyi): **AUDIT PASS**
- All 13 findings verified in live code, anti-hollow checks pass
- Build: `./mvnw clean test -pl nop-job -am` BUILD SUCCESS
- 5 items deferred (AR-6, AR-7, AR-14, AR-15, AR-16, F9) with documented rationale

## Plan Status 修复：3 个计划状态解析异常

`check-plan-status.mjs` 报告 4 个 Active 计划，经检查其中 2 个实际已完成但因格式问题解析为 `unknown`/`partially completed`。

### 根因

1. **Plan 20** (`nop-job-audit-remediation`): 所有 Slice 已 completed，Closure Gates 全 `[x]`，但 Plan Status 行使用 `> **Plan Status**: completed`（粗体格式），正则无法匹配 → 解析为 `unknown`
2. **Plan 103-stream** (`nop-stream-2026-06-02-audits-remediation`): 所有 18 个 Phase 已 completed，但 Plan Status 行未更新，仍是 `partially completed`
3. **Plan 06** (`nop-code-feature-completion-plan`): Plan Status 行为 `**superseded** — 注释文字`，正则捕获了尾部的星号和注释

### 修复

- **Plan 20**: `> **Plan Status**: completed` → `> Plan Status: completed`
- **Plan 103-stream**: `> Plan Status: partially completed` → `> Plan Status: completed`
- **check-plan-status.mjs**: 重写 `getPlanStatus()` 正则，兼容 `**bold**` + 注释格式，先捕获整行再剥离 markdown 粗体标记

### 修复后结果

Active 从 4 降到 2：
- Plan 08 (nop-code-frontend-plan, pending) — 不同模块，不涉及
- Plan 103 (nop-job-adversarial-review-remediation, planned) — 正在执行中
