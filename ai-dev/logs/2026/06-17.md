### plan-235: nop-ai-agent Fencing Token — foundational 单调计数器并发写入防护原语（L4-fencing-token）

- **Plan**: `ai-dev/plans/235-nop-ai-agent-fencing-token.md`（Plan Status: planned → **completed**）
- **Work Item**: L4-fencing-token（roadmap §4 Layer 4 新增 ✅ 行；vision §5.1 Fencing Token 协议（lines 252-267）+ §10 Phase 4（Fencing Token 从 successor → 🟡 部分落地）+ §10 line 462 `IFencingTokenService` 接口已存在）
- **交付**：闭合 plan 234/232/230 反复 carry-over 的「Fencing Token」successor——从「零 fencing token（无 `IFencingTokenService` 契约、无单调计数器原语、无 stale-token 拒绝、无 CAS 原子递增）」扩展为「中央 `IFencingTokenService` 单调计数器决策网关 + NoOp shipped 默认（恒 valid = 零回归）+ functional in-memory CAS 实现，提供 issue（原子递增单调计数器）+ validate（严格递增校验 + 高水位更新，stale 拒绝）原语」。vision §5.1 Fencing Token 协议 foundational 原语切片。
  - **Phase 1 — IFencingTokenService 契约 + FencingToken/FencingTokenDecision + NoOp/Default 实现 + focused 测试** ✅:
    - 新包 `io.nop.ai.agent.fencing`（独立包，与 `quota`/`conflict`/`security` 平行——fencing 是通用"单调计数器 + stale 拒绝"原语，关注点分离）：
      - `FencingToken`（不可变 final 字段 + 静态工厂：actorId + monotonicCounter + issuedAt，per vision §5.1 lines 252-267 + glossary line 65）。
      - `IFencingTokenService` 契约（`issue(actorId) → FencingToken` 原子递增单调计数器首次=1；`validate(token) → FencingTokenDecision` strictly greater + 高水位更新；vision §10 line 462 引擎层依赖此接口）。
      - `FencingTokenDecision`（不可变 valid/stale + actorId + presentedCounter + recordedCounter + reason；valid⇒null-reason / stale⇒non-null-reason 不变量，私有构造器强制；stale 工厂 auto-generate reason = "fencing token stale: presented X <= recorded Y"；`isStale()` 便捷逆方法）。
      - `NoOpFencingTokenService`（singleton shipped 默认：validate 恒 valid（零回归，与 NoOpResourceGuard 恒 allow 一致）；issue 返回 placeholder token counter=0 intentional disabled-mode，非空壳非 null）。
      - `DefaultFencingTokenService`（functional in-memory CAS：per-actor 双计数器——issue 计数器 `AtomicLong.incrementAndGet` 保证无重复；recorded 高水位 CAS loop only-if-greater，无 lost update；高水位单调不回退）。
    - validate 返回 decision 非 throw（enforcement-point 反应 throw/broadcast 为 consumer 责任，Decision §6，与 `IResourceGuard.checkConcurrent → QuotaDecision` / `ITeamAclChecker.checkAccess → TeamAclDecision` 一致）。
    - 无 engine 顶层接线（无 wired consumer，`setFencingTokenService` 预留首个 consumer successor，Decision §8，与 plan 218 `IActorRuntime` primitive-先于-consumer 模式一致）。无 DB schema 变更。
    - 测试 `TestFencingTokenService`（22 用例全绿）：FencingToken 工厂 + null-actorId 拒绝 / FencingTokenDecision valid⇒null-reason + stale⇒non-null-reason + reason 文本含 presented/recorded/<= / NoOp singleton + 恒 valid + placeholder 非 null / issue 连续递增首次=1 + per-actor 独立 / validate fresh valid + stale 拒绝 + reason / equal-counter stale（strictly greater） / 高水位单调不回退（#3 valid→#2 stale→#1 stale）+ per-actor 独立 / **原语生命周期端到端**（issue→valid→issue→stale，Anti-Hollow #22）/ **并发 issue 100 线程唯一连续计数**（CAS 原子性）/ 并发 validate 不回退高水位 / 并发 validate ascending + max re-validate stale / Default stale 显式非静默（#24）/ null token NPE。
  - **Phase 2 — 文档更新 + daily log** ✅:
    - `nop-ai-agent-actor-runtime-vision.md` §5.1（Fencing Token 协议块后新增 foundational 原语已落地标注 + 8 项 successor 标注）+ §10 Phase 4（Fencing Token 从 successor 池移出 → 🟡 部分落地 foundational in-memory CAS）+ §10 Phase 5（Fencing Token 从 successor 移出）+ §10 line 464（`IFencingTokenService` 接口已存在 + engine 顶层接线预留）。
    - `nop-ai-agent-roadmap.md` §4 Layer 4 新增 `L4-fencing-token` ✅ 行。
    - 本 daily log 条目。
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS（2482 tests，零回归，Phase 1 前为 2460，本次新增 22 focused 测试）**。
  - **显式 successor（Non-Goals）**：scope_claim 协调信道集成（规则 1，依赖协调协议裁定 + ResourceGuard 协调信道 successor）/ conflict_alert 广播（规则 3，依赖协调信道）/ Compaction·快照写入集成（§6.2 line 311，依赖 compaction 特性）/ DB-backed 跨进程 CAS（line 267 `UPDATE ... SET counter = ? WHERE counter = ?`，依赖存储裁定 + ORM model plan-first）/ Actor 恢复后重新获取 token（规则 4，依赖 DB-backed impl）/ 分支亲和调度注册集成（未实现 successor）/ engine 顶层 `setFencingTokenService` 接线（首个 consumer successor）/ ResourceGuard fencing 校验集成（规则 2，依赖协调信道）均为独立 successor。
- Doc-sync: `nop-ai-agent-actor-runtime-vision.md`（§5.1 / §10 Phase 4 / §10 Phase 5 / §10 line 464）+ `nop-ai-agent-roadmap.md`（§4 Layer 4）。未改 `docs-for-ai/`（本次属 nop-ai-agent 内部并发防护原语扩展，不触及 docs-for-ai 路由）。

### plan-234: nop-ai-agent ResourceGuard + 配额强制 — foundational count-based 并发配额网关（L4-resource-guard-quota）

- **Plan**: `ai-dev/plans/234-nop-ai-agent-resource-guard-quota.md`（Plan Status: active → **completed**）
- **Work Item**: L4-resource-guard-quota（roadmap §4 Layer 4 新增 ✅ 行；vision §4.2 ResourceGuard 行 + §5.2 配额表 + §10 Phase 5 从 successor → 🟡 部分落地 foundational 切片）
- **交付**：闭合 plan 227/228/230/231/232 反复 carry-over 的「ResourceGuard + 配额强制」successor——从「零配额强制（`TeamSpec.maxParallelMembers` 被动 hint 存读不强制、无并发计数配额检查）」扩展为「中央 `IResourceGuard` 配额决策网关 + NoOp shipped 默认（恒 allow = 零回归）+ functional 实现，对三类 count-based 并发配额维度强制」。
  - **Phase 1 — IResourceGuard 契约 + 数据对象 + NoOp/Default 实现** ✅:
    - 新包 `io.nop.ai.agent.quota`：`IResourceGuard`（`checkConcurrent(QuotaDimension, scopeKey, projectedCount, overrideLimit) → QuotaDecision`）+ `QuotaDimension`（三值 `TEAM_PARALLEL_BOUND_MEMBERS` / `TEAM_MEMBERS` / `CONCURRENT_ACTORS_PER_TENANT`）+ `QuotaDecision`（不可变 allow/deny 工厂 + dimension/scopeKey/limit/projectedCount/reason）+ `QuotaConfig`（teamMaxMembers 默认 8 / tenantMaxConcurrentActors 默认 10，`<= 0` = unlimited）+ `NoOpResourceGuard`（singleton shipped 默认恒 allow）+ `DefaultResourceGuard`（override `> 0` 优先 / `<= 0` 回退 QuotaConfig 全局默认，limit `<= 0` = unlimited allow，projectedCount > limit → deny）。
    - 测试 `TestQuotaContract`（17 用例：allow/deny 不变量 + null-reason 拒绝 + 三维度 NoOp 恒 allow + Default denial 三维度 + override 优先于 config + unlimited limit）。
  - **Phase 2 — enforcement 接线（TeamManager + ActorRuntime）** ✅:
    - `InMemoryTeamManager` 构造期可选接收 `IResourceGuard`（默认 NoOp）：`bindMemberSession` 绑定前计数 `isBound()==true` 成员 → `checkConcurrent(TEAM_PARALLEL_BOUND_MEMBERS, teamId, projectedCount, spec.maxParallelMembers)`（idempotent rebind 不增 projectedCount）；`createTeam`（projectedCount = 初始成员数）+ `addMember`（currentMemberCount+1）→ `checkConcurrent(TEAM_MEMBERS, teamId, projectedCount, 0)`。denial fail-fast 抛 `NopAiAgentException`。
    - `DbTeamManager` 新增 `(DataSource, ITenantResolver, IResourceGuard)` 构造器（既有构造器委托 NoOp）：同两维度 enforcement，`bindMemberSession` denial 经 H2 验证在 UPDATE 前抛出（越限行不持久化）。
    - `InMemoryActorRuntime` 新增 `(mailboxLookup, tenantResolver, pollIntervalMs, shutdownTimeoutMs, resourceGuard)` 构造器：`createActor` 创建前 resolve tenant scopeKey（`ITenantResolver`，null = 全局单桶 `__global__`）+ 经 registry tenant 标签派生活跃 Actor 计数 → `checkConcurrent(CONCURRENT_ACTORS_PER_TENANT, tenant, activeCount+1, 0)`。仅 functional guard wired + `isEnabled()==true` 时生效（NoOp/NoOp-runtime = 不强制）。
    - 测试 `TestTeamManagerQuotaEnforcement`（11 用例：InMemory + Db 双 impl NoOp 零回归 + maxParallelMembers/TEAM_MEMBERS 双维度 denial + 接线 spy guard 验证被调用 + Db denial 在 UPDATE 前 + null 回退 NoOp）+ `TestActorRuntimeQuotaEnforcement`（7 用例：NoOp 零回归 + CONCURRENT_ACTORS_PER_TENANT denial + per-tenant scope 独立桶 + 接线 spy + idempotent re-create 不消耗额外配额 + null 回退 NoOp）。
  - **Phase 3 — 端到端验证 + 文档更新** ✅:
    - E2E 测试 `TestResourceGuardQuotaEndToEnd`：同一 `DefaultAgentEngine` + functional `DefaultResourceGuard` + `InMemoryTeamManager`—— lead 声明 `<team maxParallelMembers="2">` 含 3 成员 → engine.execute lead 自动建团+绑定 lead（parallel=1）→ member-a auto-bind（parallel=2）→ **member-b auto-bind 经 `bindMemberSession` → `IResourceGuard.checkConcurrent(TEAM_PARALLEL_BOUND_MEMBERS)` → denial 抛 `NopAiAgentException`，engine.execute future 失败**。Anti-Hollow 断言：bound 计数仍为 2（denial 阻止越限绑定）+ member-b 未绑定 + spy guard 被调用且含 denial decision。完整链路：engine.execute → auto-bind → teamManager.bindMemberSession → guard.checkConcurrent → denial。
    - `nop-ai-agent-actor-runtime-vision.md` §4.2 ResourceGuard 行（successor → 🟡 部分落地）+ §5.2 配额表（三维度标注 ✅ foundational 已落地 + successor 标注）+ §10 Phase 5（空 → 🟡 部分落地 foundational 切片）。
    - `nop-ai-agent-roadmap.md` §4 Layer 4 新增 `L4-resource-guard-quota` ✅ 行。
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS（2460 tests，零回归）**。
  - **显式 successor（Non-Goals）**：LLM 调用频率 rate-limit（`IRateLimiter` 时间窗 token-bucket）/ Compaction 配额池 / storage 配额（需 metrics 基础设施）/ per-agent token/时间累积上限 / Fencing Token（独立 carry-over `L4-fencing-token`）/ DB-backed ACL 表 + permissions override + 权限派生 / ResourceGuard 协调信道（scope_claim/conflict_alert）/ User 隔离 / 排队/等待语义 / DB 持久化配额计数器（跨进程共享）均为独立 successor。
- Doc-sync: `nop-ai-agent-actor-runtime-vision.md`（§4.2 / §5.2 / §10）+ `nop-ai-agent-roadmap.md`（§4 Layer 4）。未改 `docs-for-ai/`（本次属 nop-ai-agent 内部配额能力扩展，不触及 docs-for-ai 路由）。

### plan-233: nop-ai-agent nop-task DAG 集成 — 团队任务→工作流图 + 依赖序同步编排（L4-nop-task-dag-integration）

- **Plan**: `ai-dev/plans/233-nop-ai-agent-task-flow-dag-integration.md`（Plan Status: active → **completed**）
- **Work Item**: L4-nop-task-dag-integration（roadmap §4 Layer 4 line 259 验收标准 `[ ]` → `[x]`；新增 ✅ 行；vision §8.2/§2.2/§10 标注已落地）
- **交付**：闭合 plan 227/229/230/231/232 反复 carry-over 的「nop-task DAG 集成」successor——团队任务从「扁平记录 + 状态机 + blockedBy 仅存储不解析」扩展为「可组织为 nop-task 工作流 DAG 图 + 依赖序同步编排执行」。回应 carry-over 反复标注的「nop-task 模块集成裁定」设计决策。
  - **Phase 1（已在前次 run 落地）** ✅：`nop-ai-agent/pom.xml` 新增 `nop-task-core` 单向编译依赖（无环）；`TeamTaskGraphBuilder` 从团队任务集构造 nop-task 内存 `GraphTaskStepModel`（每任务一节点，`blockedBy`→`waitSteps`，enter/exit 推导），**真实经 nop-task `GraphStepAnalyzer` 环检测**（成环 blockedBy 快速失败，闭合「今日静默存储成环」gap）；`TeamTaskTopology` 就绪/阻塞拓扑查询。
  - **Phase 2 — 依赖序同步编排器 + 端到端验证** ✅:
    - `TeamTaskFlowOrchestrator`：真实经 nop-task 运行时执行图——构造 `GraphTaskStep` + `GraphStepNode` + `TaskImpl`，`ITaskFlowManager.newTaskRuntime(task, false, null)` + `ITask.execute(taskRt).syncGetOutputs()`。nop-task 图调度器保证 B blockedBy A 则 A 完成后 B 才执行（菱形 B/C 在 A 后、D 在 B/C 后）。
    - `MemberAgentTaskStep`（每节点委派步骤）：claim → `IAgentEngine.execute` 同步 join（裁定4 消费已绑定成员不 spawn）→ `completeTask` 标记 COMPLETED；非 completed 终态/抛异常 = 失败抛出（诚实）。
    - **合成 sole-exit sink 节点**：nop-task 图在任一 exit 节点完成即 resolve 整图 future 并取消其余运行中节点；多自然 sink（独立链）会导致首个 sink 完成即取消其余任务。解法：所有真实任务节点标非 exit + 追加一个 waitSteps=全部 taskId 的合成 sink 为唯一 exit（合法拓扑终止标记，不成环），保证无论 DAG 形状每个任务都执行。
    - `TeamTaskFlowResult`（success/completedTaskIds/failedTaskId/skippedTaskIds/startOrder/completionOrder）+ `ExecutionRecorder`（AtomicInteger 计数器在 delegate 真实 start/complete 时刻写入，供 Anti-Hollow 依赖序断言）。
    - 编排器只读消费既有 `IAgentEngine`/`ITeamTaskStore`/`ITeamManager`，**零契约改动**（经独立 audit 验证接口方法零增删改）。
    - 测试（`TestTeamTaskFlowOrchestrator` 8 用例全绿）：线性 A→B→C（Anti-Hollow 断言 `startB > completionA` + `startC > completionB` 证明依赖序真实生效 + 全部 COMPLETED + engine 调用 3 次 wiring）/ 菱形 A→{B,C}→D（B/C 在 A 后、D 在 B/C 后）/ 失败传播（B 失败→C skipped + 仍 CREATED + delegate 未调用；A 抛异常→B skipped）/ 4 个 NoOp fast-fail（空任务/未知团队/成环/未绑定成员）。
  - **Phase 3 — 设计文档 + roadmap/vision 同步 + 全量回归** ✅:
    - 新建 `ai-dev/design/nop-ai-agent/nop-ai-agent-task-flow-integration.md`：4 项设计裁定（集成定位=nop-task 作 DAG 引擎非持久化层 / 依赖映射=blockedBy→waitSteps / 执行模型=同步编排 / 节点解析=消费已绑定成员）+ 5 项拒绝替代方案（DaoTaskStateStore / 自建 DAG / 异步跨进程 / 全局 bean simple-step / decorator），零代码块（design doc 合规）。
    - `nop-ai-agent-roadmap.md` §4 Layer 4 line 259 验收标准 `[ ]`→`[x]` + 新增 `L4-nop-task-dag-integration` ✅ 行；`nop-ai-agent-actor-runtime-vision.md` §8.2 + §2.2（ITaskStep+Decorator 行）+ §10 Phase 3 标注已落地 + successor 保留。
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS（零回归）**；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（新编辑文件零 broken link）；`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` 退出码 0；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-ai/nop-ai-agent --severity high` 退出码 0（新代码零 high 发现）。
  - **独立 closure audit**：fresh-session explore 子 agent（task `ses_12aebbf04ffekNv8Sa7SxFZerK`）对 11 项验证点（A–K：编排器/图桥真实用 nop-task 运行时、completeTask 真实改 store、Anti-Hollow 依赖序断言、无静默跳过 5 种、失败传播、测试 scope、pom 依赖、design doc 合规、roadmap/vision 更新、契约只读）全部判定 **PASS**，CLOSURE AUDIT: PASS。证据写入 plan `Closure` 段。
  - **设计裁定要点**：DD1 取 nop-task 之长（DAG/环检测/调度）避其短（DaoTaskStateStore stub 持久化不可靠 → 团队任务事实源保持 DbTeamTaskStore）；DD3 同步 `syncGetOutputs()` 提供可端到端验证语义（Anti-Hollow #22）；合成 sink 解多自然 sink 问题；失败任务留 CLAIMED 不自动 abandon（重试/恢复为 successor）。
  - **显式 successor（Non-Goals）**：blockedBy 自动调度守护 `L4-blockedBy-resolution-engine` / auto-spawn 成员 agent `L4-auto-spawn-member-agent` / 异步长时跨进程流编排 / LLM 直面编排工具（如 `team-execute-flow`）/ nop-task decorator（retry/timeout/rate-limit）接入 / 运行时动态改图 / DaoTaskStateStore 持久化 均为独立 successor。
- Doc-sync: `ai-dev/design/nop-ai-agent/nop-ai-agent-task-flow-integration.md`（新）+ `nop-ai-agent-roadmap.md` §4 + `nop-ai-agent-actor-runtime-vision.md` §2.2/§8.2/§10。未改 `docs-for-ai/`（本次属 nop-ai-agent 内部能力扩展，不触及 docs-for-ai 路由）。

### docs: pie/DS4 深度分析 + nop-ai-agent 前缀缓存设计完善（填补三个运行时缺口）

- **关联**：`ai-dev/analysis/agent-survey/2026-06-17-pie-ds4-cache-analysis.md`（新）+ `ai-dev/design/nop-ai-agent/nop-ai-agent-llm-layer.md`（§3.3 / §七 / §八 扩展）+ `README.md` 描述更新
- **背景**：调研 `~/ai/pie`（Rust AI coding agent，为驱动本地 DS4/DeepSeek V4 模型而生）。pie 的核心价值是把"前缀缓存命中"从原则声明落地为三项可操作运行时机制。源码级分析（`crates/ai/src/providers/openai_responses.rs`、`utils/retry.rs`、`types.rs`）后，对照 nop-ai-agent §八发现三个缺口。
- **三个缺口与沉淀**：
  1. **§3.3 误导性表述**：原文"DeepSeek 的 reasoning_content 则不应回传"只对托管 API 成立，对本地字节精确 KV 缓存服务器方向相反（reasoning 是 KV checkpoint 的一部分，必须按顺序回放）。改为区分"API 正确性 vs 缓存正确性"两层 + per-model compat flag 驱动。
  2. **§七 retry 缺 409 语义**：本地推理服务器丢失 live continuation state 时返回 409 Conflict（=回放重建）。新增 `CACHE_STATE_LOST`（TRANSIENT 子类）错误分类，重试策略 = 原样重发。正确性前提（客户端总发完整历史）由 ReAct 引擎 `buildBaseExecutionContext` 全量 replay 满足。
  3. **§八 前缀缓存设计过薄**：原仅 4 子节（原则层），现扩展为 8 子节，补入三项运行时机制（8.4 reasoning 回放策略含 ordering-is-load-bearing 约束、8.5 缓存状态丢失恢复、8.6 缓存流量双侧记账与可观测性），并交叉引用 `nop-ai-agent-usage-and-billing.md` §2.1（tokens_cache_write runtime 从不写入的缺口，补充缓存动机论证）。
- **设计裁定**：三项机制全部门控、零硬编码到特定服务器（与 §3.2 纯函数契约 / §8.3 不加数据结构一致）；reasoning 回放承载于 `ILlmDialect.convertMessage()` 纯函数，flag 通过 Dialect 构造或 `{provider}.llm.xml` 注入；不在 `ChatMessage` 加 frozen/prefix 标记（§8.3 已定 + pie 同样否定）。
- Doc-sync: 无代码变更，纯文档。未改 INDEX.md / source-anchors.md（agent-survey 与 nop-ai-agent design 均属 `ai-dev/`，不在 `docs-for-ai/` 路由范围）。

### goal-driver: 修复 cross-cycle appendBuffer 累积导致 DRAFT prompt 无限增长（goal-driver run 失败根因）

- **关联**：`_tmp/2026-06-17-070306-goal-driver/`（nop-ai-agent run，step 22 DRAFT visit #7 failed）
- **根因**：`ai-dev/tools/opencode-goal-driver/src/engine.js` normal goto transition（ROADMAP→DRAFT）的 append 语义是 `existing + appendText` 累积，且 buffer 从不清理。导致 DRAFT prompt 随循环单调增长：visit 1=43K → visit 7=137K（实测 7 个 DRAFT log header 的 `prompt_chars`）。visit 7 时 137K prompt + agent 自身 Read/Grep 内容超过 glm-5.2 有效上下文，子进程静默 exit 0 无 `<AI_STEP_RESULT>` → DRAFT step 无 `onUnknown` → 整个 module `failed`。
- **修复**：`engine.js:722-730` normal goto transition 改为 replace 语义（`set(goto, appendText)`），每次循环 = template + 本轮 append。retry 路径（line 500-503）累积语义**不变**（`transitions.test.js` "retries target step and accumulates append context" 仍保护）。template 在前、append 在后（`_buildPrompt` 既有结构），保持前缀稳定以命中 token cache。
- **验证**：`cd ai-dev/tools/opencode-goal-driver && node --test test/transitions.test.js test/core.test.js test/group-subflow.test.js test/intg.test.js test/closure-script-check.test.js test/goal-driver-verify.test.js` → 83 pass / 0 fail（新增回归测试 "cross-cycle append does not accumulate" ✔）。
- Doc-sync: No owner-doc update required（`ai-dev/design/opencode-goal-driver/goal-driver-flow-design.md` 未声称跨循环累积语义）。

### plan-232: nop-ai-agent 多租户隔离 — TENANT_ID 列 + contextual tenant 解析 + 全 DB store WHERE 注入（L4-multi-tenant-isolation）

- **Plan**: `ai-dev/plans/232-nop-ai-agent-multi-tenant-isolation.md`
- **Work Item**: L4-multi-tenant-isolation（roadmap §4 Layer 4 line 260 验收标准 `[ ]` → `[x]`；vision §5.1 Tenant 隔离从 successor 更新为 foundational 已落地）
- **交付**：闭合 plan 167/218/223/228/230 反复 carry-over 的"多租户 tenantId 隔离"successor——不同租户的 session/team/task/message/checkpoint/lock/denial 持久化数据完全不可见（vision §5.1 Tenant 隔离）。
  - **Phase 1 — tenant 解析机制 + TENANT_ID 列 DDL + 引擎入口点 + ActorRegistry** ✅:
    - `io.nop.ai.agent.security.ITenantResolver`（contextual 接口，`resolveTenantId() → String|null`）+ `NullTenantResolver`（always-null shipped 默认 = backward compatible）+ `ThreadLocalTenantResolver`（thread-local backed，static set/clear/current + INSTANCE 实现；null = 全部可见）+ `TenantSql.whereTenant(col)` 共享 WHERE 片段 helper。
    - 全部 **10 个 Table 常量类**新增 `COL_TENANT_ID` 常量 + DDL 追加 `TENANT_ID VARCHAR(100)` nullable 列（AiAgentSession/NopAiSessionMessage/AiAgentMessage/AiAgentCheckpoint/AiAgentTeam/AiAgentTeamMember/AiAgentTeamTask/AiAgentSessionLock/NopAiChatResponse/AiAgentDenial）。
    - `_vfs/nop/ai/agent/orm/app.orm.xml` 4 实体（ai_agent_message/ai_agent_denial/ai_agent_session/ai_agent_checkpoint）追加 `tenantId` 列 + `tenantId` domain，保持与 Table DDL 一致。
    - `DefaultAgentEngine` 全部 5 入口点 tenant context set/clear：`doExecute`（从 `request.getPrincipal().getTenantId()` null-safe 捕获）在 supplyAsync worker lambda 内 set/finally clear；`resumeSession`/`restoreSession`（无 Principal 源，null = 恢复路径语义）结构化 set/clear；`cancelSession`/`forkSession`（同步，set/clear around DB 操作）。
    - `InMemoryActorRegistry` 新增构造期 `ITenantResolver` + 内部 `actorToTenant` 标签 map（`register` 时打标签，ConcurrentHashMap 不允许 null value → null tenant 记为 absent = legacy/global actor 全租户可见）；`get`/`getBySession`/`getAll` 在 tenant 非空时按标签过滤；**不改 `AgentActor` 构造器 / `IActorRuntime.createActor` 签名**。`InMemoryActorRuntime` 新增 `(mailboxLookup, tenantResolver)` 构造器重载透传。
  - **Phase 2 — DB store 类 tenantId WHERE 注入** ✅:
    - 全部 8 个 DB store + 2 个 recovery handler 新增构造期可选 `ITenantResolver`（默认 NullTenantResolver = 零回归）：**null tenant 时 SQL 与原版 byte-identical**（关键零回归设计）。
    - 每条 SELECT/UPDATE/DELETE：tenant 非空时追加 `AND (TENANT_ID = ? OR TENANT_ID IS NULL)`（参数绑定在现有条件之后）；每条 INSERT：tenant 非空时追加 `TENANT_ID` 列为最后一列 + 写入解析值。
    - `DBSessionStore` 特殊处理（DD#8/9）：tenant 非空时 **cache bypass**（`get`/`getOrCreate`/`save`/`remove`/`getAll`/`listAllSessions` 全部直达 DB）+ MERGE KEY 变为 `(SESSION_ID, TENANT_ID)`；`DBCheckpointManager` tenant 非空时 bySession/byWatermark cache bypass + tenant-scoped latest SELECT。
    - `DefaultOrphanRecoveryHandler`/`DefaultSessionTimeoutHandler` 的 raw JDBC UPDATE on ai_agent_session 注入 tenant WHERE。
  - **Phase 3 — 端到端验证 + 文档** ✅:
    - 测试（31 新增全绿）：`TestTenantResolver`（5，resolver 机制）+ `TestInMemoryActorRegistryTenantIsolation`（5，registry 标签过滤）+ `TestMultiTenantDbIsolation`（9，6 核心 DB store 跨租户隔离 + null-tenant backward compat + 接线验证）+ `TestDefaultAgentEngineMultiTenantIsolation`（3，engine.execute → supplyAsync worker → thread-local → DB store WHERE 完整链路 Anti-Hollow #22/#23）。
    - owner docs：`nop-ai-agent-actor-runtime-vision.md` §5.1（Tenant 隔离 foundational 已落地 + User 隔离 successor 标注）+ ActorRegistry 行；`nop-ai-agent-roadmap.md` §4 Layer 4 line 260 `[ ]` → `[x]` + 完整交付描述。
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **2400 tests 全绿（零回归）**；`compile` 全绿。
  - **设计裁定**：DD#1 tenant 解析 contextual（非方法参数，避免全部 store 接口 breaking change）；DD#2 null tenant = 全部可见（backward compatible）；DD#3 WHERE 非严格（`OR TENANT_ID IS NULL` legacy 数据迁移期可见）；DD#5 ActorRegistry 内部标签 map（不改 AgentActor/IActorRuntime 签名）；DD#6 engine supplyAsync worker lambda 内 set/clear（标准 ThreadLocal 不跨 supplyAsync）；DD#8 DBSessionStore cache bypass when tenant 非空；DD#9 MERGE KEY `(SESSION_ID, TENANT_ID)`。
  - **坑**：`ConcurrentHashMap` 不允许 null value → registry tenant 标签 null 记为 absent（absent = legacy/global 全租户可见，镜像 DB `OR TENANT_ID IS NULL` 语义）；`DbSessionTakeoverLock` INSERT VALUES 占位符数错（3→4 列）导致 "Column count does not match"，修正后通过；lock 表 PK 仍为 `(SESSION_ID)`，跨租户相同 sessionId 独立 lease 不支持（与 session 表 MERGE KEY 不同——lock PK 未改，realistic 部署用 UUID sessionId 无碰撞）；**DBMessageService 后台 poller 线程不继承调用线程的 ThreadLocal tenant**（标准 ThreadLocal 不跨线程，与 engine supplyAsync 边界同类问题）——sendAsync（调用线程）正确写 TENANT_ID，但 poller 线程 delivery 路径当前以 null tenant 运行（per-tenant poller wiring 为显式 successor，类比 IContext 深度集成），测试只验证 store 层 INSERT/WHERE 注入不验证 poller delivery。
  - **显式 successor**：User 隔离（USER_ID 列 + 同租户用户级过滤 + 团队共享例外）/ ResourceGuard 配额 / strict WHERE 模式 / Fencing Token / DB-backed ACL 表 / TeamSpec permissions override / 多租户 LLM Key / IContext 深度集成（含 DBMessageService per-tenant poller wiring）/ 生产数据迁移工具 均为显式 Non-Goals。

---



- **Plan**: `ai-dev/plans/231-nop-ai-agent-team-auto-binding.md`
- **Work Item**: L4-team-auto-binding（roadmap §4 Layer 4 新增 ✅ 行；vision §8.1/§4.2/§10 标注已落地）
- **交付**：闭合 plan 223/225/227/228/230 反复切出的"自动团队绑定 + TeamSpec XDSL 配置化"successor gap——团队可仅凭 `.agent.xml` 配置声明式 materialize。
  - **Phase 1 — agent.xdef schema + 生成模型 + 转换器** ✅:
    - `nop-kernel/nop-xdefs/.../schema/ai/agent.xdef`：新增可选 `<team>`（xdef:name `TeamModel`：teamName/description?/leadAgentName/maxParallelMembers? + `<members>` 列表 name/agentModel/role=`enum:io.nop.ai.agent.team.MemberRole`）+ 可选 `<team-member>`（xdef:name `TeamMemberRefModel`：teamName/memberName）。`mvn install` codegen 生成 `_gen/_TeamModel`/`_TeamMemberModel`/`_TeamMemberRefModel` + hand-written 壳类 `TeamModel`/`TeamMemberModel`/`TeamMemberRefModel`（镜像 `AgentModel`/`AgentPermissionModel`）。
    - 决策修正：inline enum 语法 `enum:LEAD,MEMBER|MEMBER` 经 `EnumStdDomainHandler` 解析有歧义（逗号分隔值不被 `stripedSplit(options,'|')` 识别），改用 fully-qualified enum 类引用 `enum:io.nop.ai.agent.team.MemberRole`（匹配 `agent-plan.xdef` 的 `enum:io.nop.ai.agent.model.AgentExecStatus` 模式），role 类型从 String 升级为 `MemberRole` 枚举。
    - `io.nop.ai.agent.team.TeamModelConverter`：mutable `TeamModel`/`TeamMemberModel` → 不可变 `TeamSpec`/`TeamMemberSpec`；**保证 leadAgentName 以 role=LEAD 进 memberSpecs**（DD#3，缺失则自动补登记 lead 为 LEAD 成员）；缺失必填字段（teamName/leadAgentName/name/agentModel）fail-fast（Minimum Rules #24）。
    - 测试：`TestTeamModelConverter`（13 用例：字段保真 + role 映射 + lead-in-roster guarantee + 边界 + fail-fast）+ `TestTeamXdefLoading`（3 用例：既有 agent.xml 零回归 getTeam()=null + 新 fixture 加载）。
  - **Phase 2 — 引擎三入口点 auto-bind 集成** ✅:
    - `DefaultAgentEngine` 新增私有方法：`precheckTeamDeclarations`（同步 fail-fast：声明 + NoOp → 抛 `NopAiAgentException`）+ `autoBindTeam`（lead `autoBindLead` + member `autoBindMember`）+ `resolveActorId`（Actor 或回退 sessionId）。
    - 三入口点（doExecute/resumeSession/restoreSession）：loadAgentModel 后接入同步 precheck；异步块 createActor 后接入 autoBindTeam（actorId 需 createActor 后可用，DD#7）。
    - lead：`getTeamBySession` 探测先于 `createTeam`（幂等，resume/restore 不重建）+ `bindMemberSession(lead)`（first binding CREATED→ACTIVE）；member：`getActiveTeams()`（返回 CREATED+ACTIVE）按 teamName + **ACTIVE 过滤**解析 teamId（CREATED-only 团队 fail-fast，DD#5）+ 幂等 `bindMemberSession(member)`；bindMemberSession 返回 false / 未找到 ACTIVE 团队 / NoOp 冲突 全 fail-fast（DD#8 + Minimum Rules #24）。
    - 测试：`TestTeamAutoBinding`（10 用例：lead 幂等建团 + lead/member 绑定 + member 未找到 ACTIVE（含 CREATED-only）fail-fast + member 不在花名册 fail-fast + NoOp+声明同步 fail-fast + 无声明零回归 + actorId Actor/sessionId 两路径）。
  - **Phase 3 — 端到端 + 文档同步** ✅:
    - fixtures：`test-team-lead.agent.xml`（`<team>` + 3 成员）+ `test-team-member-a/b.agent.xml`（`<team-member>`）+ `test-team-member-ghost.agent.xml`（fail-fast）+ `test-team-delta-base.agent.xml` + `_delta/default/test-team-delta-base.agent.xml`（Delta 覆盖）。
    - `TestTeamAutoBindingE2E`（Anti-Hollow #22）：执行 lead → 团队 ACTIVE + lead 绑定 → 执行 member A/B → 两成员绑定 → `TeamStatusExecutor`（既有工具）透明消费返回完整 team + 3 members（drop-in 语义）。`TestTeamDeltaCustomization`：`x:extends="super"` + `x:override="replace"` Delta 覆盖 teamName/roster 生效。
    - owner docs 同步：`nop-ai-agent-actor-runtime-vision.md` §8.1（TeamSpec 声明式已落地 + 字段子集/successor 清单）+ §4.2（TeamManager 行 successor 列表更新）+ §10 Phase 3；`nop-ai-agent-roadmap.md` §4 Layer 4 新增 `L4-team-auto-binding` ✅ 行 + line 258 验收标准标注自动团队绑定已交付；`TeamSpec.java`/`TeamMemberSpec.java` Javadoc 更新。
  - **验证**：`./mvnw clean test -pl nop-ai/nop-ai-agent` → 2378 tests 全绿（28 新增）；`compile` + `scan-hollow-implementations.mjs --severity high` → 0 findings；`check-plan-checklist.mjs --strict` → exit 0。
  - **坑**：Delta 机制经实测确认是 `_vfs/_delta/{deltaDir}/` + `x:extends="super"`（非 `/delta/` 自动合并）；删除源 delta 文件后需清理 `target/test-classes` 残留（否则 stale 资源导致跨测试污染）。



- **背景**：用户指出前轮 4 框架调研未覆盖 codex（`~/ai/codex`）和 soloncode（`~/ai/soloncode`）。本轮直读这两个源码补全。
- **codex 源码发现**（OpenAI Codex CLI，Rust）：
  - **skill 存在**（`SKILL.md` + sidecar `agents/openai.yaml`），frontmatter 仅 `name`/`description`/`metadata.short-description`（`core-skills/src/loader.rs:39-53`），grep hook 零匹配。
  - **双 hook 系统**：System A 声明式 ClaudeHooks JSON（10 事件，Command/Prompt/Agent handler，`config/src/hook_config.rs:11-156`）+ System B 命令式 Rust trait（`ToolLifecycleContributor` 等，`ext/extension-api/src/contributors.rs`，编译期注册）。
  - **Plugin manifest 平级携带 skills∥mcpServers∥apps∥hooks**（`core-plugins/src/manifest.rs:47-59`），`PluginManifestHooks` 支持 `Inline(Vec<HooksFile>)`。**第三个独立 plugin-manifest 平级例证**（继 claude-code、PilotDeck）。
  - skill 扩展注册的 contributor 不碰 HooksConfig——skill 与 hook 无直接关联。
- **soloncode 源码发现**（+ 上游 `~/ai/solon-ai`，Java）：
  - **skill 是纯 Markdown**（`SkillDir.java:26-39` 纯数据类，经 `SkillTalent` 暴露工具）。零可执行代码、零 hook。
  - **Talent 仅 `onAttach` 一次性回调**（`Talent.java:78`，reasoning 前触发，语义=初始化）。**38 Talent 实现与 5 ReActInterceptor 实现零交集**，不同注册表、不同派发时机。
  - **`AgentDefinition.java:211-212` 显式预留声明式 hooks 字段但未实现**（`// 暂不解析，保留字段`）——soloncode 考虑过声明式 hooks 但主动 defer。
  - **HarnessExtension** 是命令式 bundle 点：`configure(agentName, builder)` 暴露 `defaultTalentAdd`∥`defaultToolAdd`∥`defaultInterceptorAdd` 三平级方法。
- **对结论的影响**：两框架**进一步强化**（非推翻）6 框架一致结论——skill/talent 都不能声明 hook；bundle 携带 hook 的成熟形态是平级 contribution（codex/claude-code/PilotDeck 的 plugin manifest + soloncode 的 HarnessExtension）。Nop `IContributionRegistry` 已踩此线。soloncode 的「预留 hooks 字段未实现」是「设计选择非遗漏」的最强直接证据。
- **文档更新**：
  - `ai-dev/analysis/agent-survey/2026-06-17-skill-hook-relationship-source-survey.md`：标题 Scope 改 6 框架；新增 §3.5 codex + §3.6 soloncode（含 Talent/ReActInterceptor/HarnessExtension/AgentDefinition.hooks 预留字段）；§4 对比表扩为 6 列；§5.2 平级 contribution 增至 4 个例证；§5.3 增 codex/soloncode 证据；References 增 codex + soloncode 源码路径索引。
  - `skill-system-design.md` §6.4.3 摘要表扩为 6 行 + 三洞见更新（codex PluginManifest 第三例证 / soloncode 预留未实现字段）。

### docs: 源码级复核 4 框架 skill↔hook 关系，强化 §6.4.3 行业先例（skill-system-design.md）

- **背景**：用户质疑前轮调研是否真的看了 `~/ai` 源码（前轮基于 `ai-dev/analysis/agent-survey/` 二手分析）。本轮直接读 4 个框架源码复核。
- **方法**：4 个 explore subagent 并行直读源码（非分析文档），提取 frontmatter 解析器 / hook 注册接口 / plugin manifest schema 的精确 file:line。
- **源码级发现（全部 4 框架一致：skill/command/agent 都不能声明自己的 hook）**：
  - **opencode**（`~/ai/opencode`）：skill frontmatter 仅 `name/description/slash`（`packages/core/src/skill.ts:60-64`）；agent frontmatter 无 hooks 键（`packages/core/src/config/plugin/agent.ts:22-34`）；hook 是命令式 TS plugin（`Hooks` 对象 21 个 V1 hook，`packages/plugin/src/index.ts:222-335`）。
  - **claude-code**（`~/ai/claude-code`，claw 实现）：skill frontmatter 解析器**只取 name/description，其余静默丢弃**（`commands/src/lib.rs:4217-4246`）；hook 是声明式 JSON（settings.json，仅 3 事件 PreToolUse/PostToolUse/PostToolUseFailure，shell 命令，`config.rs:201-225`）；**Plugin manifest 可携带 hooks**（`PluginManifest.hooks`）但合并进**全局** tool-use hook 集，不 scope 到 skill（`main.rs:11930-11935`）。
  - **hermes-agent**（`~/ai/hermes-agent`）：skill 校验器只要求 name/description（`tools/skill_manager_tool.py:217-253`）；hook 命令式 Python（`ctx.register_hook`，19 hook，`plugins.py:128-170,938`）；**plugin.yaml 的 `hooks:` 块是纯装饰——加载器从不读**（`_parse_manifest` `plugins.py:1283-1372` 不取 `hooks`），langfuse 实际在 `__init__.py:995-1004` 用 Python 注册；`provides_hooks` 被解析但零消费者。
  - **pi-agent**（`~/ai/pi`）：`Skill` 接口仅 5 字段无 hook（`packages/agent/src/harness/types.ts:46-57`）；**Tool 无 beforeToolCall/afterToolCall 字段**（`packages/agent/src/types.ts:361-384`），hook 在 loop config 全局（`:262,276`）；skill 加载从不调 `harness.on()`。
- **三个源码级洞见（二手分析未点透）**：(1)「声明式 hook 配置」存在（claude-code JSON / hermes plugin.yaml）但恒在更高层，不嵌套进 skill；(2)「bundle 携带 hook」成熟形态是平级 contribution（claude-code PluginManifest / PilotDeck manifest），Nop `IContributionRegistry` 已是此模式；(3) skill frontmatter 解析器「静默丢弃非 name/description 键」是刻意 schema 收敛，非遗漏。
- **对前轮结论的影响**：源码证据**强化**（非推翻）§6.4 的技术结论——xpl 让 skill 声明式携带 hook 在 Nop 仍可行（form A 不降级），但业界一致选「skill=纯数据 + hook=平级 contribution」分层。Nop 若走 skill 内嵌 xpl hook 是**无先例的原创设计**，可行但有举证责任。
- **文档更新**：
  - 新增完整源码级调研文档 `ai-dev/analysis/agent-survey/2026-06-17-skill-hook-relationship-source-survey.md`（4 框架逐个详细发现 + frontmatter schema + hook 注册接口 + plugin manifest schema + file:line 证据 + 跨框架对比表 + 三洞见 + 对 Nop 启示 + References 源码路径索引）。
  - `skill-system-design.md` §6.4.3 重写为「源码级调研」摘要——4 框架对比表（含 file:line 证据）+ 三个洞见 + 启示段（明确「无先例原创设计 + 举证责任」）+ 指向详细调研文档的链接。
  - §6.4.1/§6.4.2/§6.4.4 保持（xpl 技术结论不受影响）。

### docs: 修正"Skill 加 Hook 会降级"误判 + xpl 声明式 Hook 可行性落档（skill-system-design.md §6.1/§6.4/§8.3）

- **背景**：上一轮回答武断声称"给 Skill 加 Hook 能力会降级为命令式注入器"。用户反驳：(1) skill 在 claudecode 应支持 hook；(2) xpl 可将命令式逻辑以声明式形态暴露，加 hook 不必然降级。经深度调研确认**用户核心直觉正确**，上一轮误判需修正。
- **调研一（agent-survey 跨框架 hook↔skill 关系）**：
  - **Claude Code 系 skill 实际不声明 hook**：SKILL.md 是纯 Markdown，无 lifecycle-hook 字段。Hook↔Skill 是**平级并行**系统，`UserPromptSubmit` hook 单向触发 skill 注入（oh-my-claudecode-analysis.md:51-66,79-86,219）。
  - **无框架把 hook 嵌套在 skill 内**。最接近的声明式先例：PilotDeck Plugin Manifest（`hooks?` 与 `skills?` **平级**字段 + 5 种 Hook 执行器 shell/HTTP/Prompt，pilotdeck-analysis.md:152-166）、Omnigent agent YAML（`policies:` 平级声明式门，omnigent-vs-nop-ai-agent-deep-comparison.md:178-241）。
  - **Nop 自己的 `agent.xdef` 已是声明式 hook 先例**：`<hooks><on event="...">xpl-fn:(event,agentRt)=>void</on>`（nop-ai-vs-mimo-code-deep-comparison.md:120-137），证明声明式 capability-unit 携带 hook 在本代码库可行。
- **调研二（xpl 声明式—命令式二象性）**：
  - `xpl-fn:` domain（`XplStdDomainHandlers.java`）在模型加载期把 XML body 编译成 `IEvalFunction` POJO 字段——**容器仍是声明式数据模型，payload 是函数值**。
  - 1:1 先例：`agent.xdef:43`（AgentHookModel.body:IEvalFunction）、`biz/state-machine.xdef:73-81`（on-entry/on-exit/handle-error）、`task/batch.xdef:55-70`（12 种 listener）、`task/task.xdef:84-92`（catch/finally/when）。
  - **修正后边界**：形态 A（xpl-fn hook body）✅ 保持声明式；形态 B（Java IAgentLifecycleHook 实例）❌ 降级；形态 C（IContributionRegistry runtime 贡献）❌ 命令式。上一轮把 A/B/C 混为一谈。
- **修正立场**：xpl 抹平"声明式 vs 命令式"对立——Skill 用 `xpl-predicate` 表达 `isSupported`、`xpl-fn`/`xpl` 表达 instruction/tools，long-term 可覆盖 Talent 大部分用例。Phase-1 保留 Talent 作已交付 Java SPI 外壳；phase-2+ 评估 Skill 声明式携带 xpl hook + xpl 准入表达式作为更通用声明式路径（§6.4.4）。
- **文档更新**（`ai-dev/design/nop-ai-agent/skill-system-design.md`）：
  - §6.1：第 3 条"Skill 本身不声明 Hook"标注为 **phase-1 选择（非技术限制）** + 指向 §6.4。
  - 新增 §6.4「Skill 携带 Hook 的声明式可行性（设计辨析）」：§6.4.1 xpl 二象性 + 1:1 先例表；§6.4.2 命令式 vs 声明式边界（A/B/C 三形态）；§6.4.3 行业先例（agent-survey 调研，含 Claude Code 平级论、PilotDeck manifest、Omnigent policy）；§6.4.4 对 Talent 定位的影响（xpl 收敛 + Talent 退为程序化快捷外壳）。
  - §8.3 重命名"第三阶段评估项"：新增"Skill 声明式携带 xpl Hook（§6.4 形态 A）" + "Skill 用 xpl 表达动态准入/产出，收敛 Talent 用例"两个评估项。

### docs: Talent vs Skill 关系 + 装配优先级语义落档（skill-system-design.md §2.1）

- **背景**：澄清"talent 和 skill 区别 / talent 优先级如何处理"，并把调研结果更新到设计文档。原 `skill-system-design.md` §2.1 只有定位表 + 一句话，缺代码级对比与优先级语义。
- **代码核对**（`nop-ai/nop-ai-agent`）：
  - `ITalent.java`（`io.nop.ai.agent.talent`）：4 契约 `isSupported`/`onAttach`/`getInstruction`/`getTools`，程序化准入门控，无 priority 字段。
  - `SkillModel.java`（`io.nop.ai.agent.skill`）：声明式 POJO（`goal`/`dependencies`/`resourceScope`/`intentSignature`/`topPattern`），无 priority 字段。
  - `ReActAgentExecutor.java:997-1000`：执行 setup 装配顺序 = `buildToolDefinitions` → `consultTalents` → `consultSkills` → `consultPromptContributions`，均在首轮 LLM 调用前一次性咨询。
  - `consultTalents`（:3114）/`consultSkills`（:3183）/`consultPromptContributions`（:3232）：三者都 additive（instruction `\n\n` 拼接 + 工具经 `IToolManager.loadTool()` union + skip-with-WARN），都经 `injectSystemInstruction`（:3151）插入为单条 `ChatSystemMessage`。
- **关键裁定**：**只有 PROMPT 贡献有显式 `priority`**（int 升序稳定，`nop-ai-agent-hook-skill-engine.md` §8.2）；Talent/Skill 均无 priority 字段——Talent 按注册序、Skill 按声明/解析序。理由：二者是"能力开关"（布尔决策）非"竞争性指令排序"，多激活时 instruction 并列补充，拼接序不影响语义。
- **文档更新**：`ai-dev/design/nop-ai-agent/skill-system-design.md` §2.1 扩充为 §2.1 + §2.1.1（代码级对比表，6 维度）+ §2.1.2（装配顺序 + 优先级语义，含三类能力源排序键对比表 + 设计裁定）。



- **Plan**: `ai-dev/plans/230-nop-ai-agent-team-db-persistence.md`
- **Work Item**: L4-team-db-persistence
- **Phase 1 交付** ✅（AiAgentTeamTable + AiAgentTeamMemberTable + DbTeamManager 全 9 方法 + focused/cross-instance H2 测试）:
  - `io.nop.ai.agent.team` 新增 3 个文件：
    - `AiAgentTeamTable.java`：`ai_agent_team` 表 DDL + 8 列常量（TEAM_ID PK / TEAM_NAME / DESCRIPTION nullable / LEAD_AGENT_NAME / MAX_PARALLEL_MEMBERS INT / STATUS / CREATED_AT BIGINT / DISBANDED_AT BIGINT nullable），`CREATE TABLE IF NOT EXISTS`
    - `AiAgentTeamMemberTable.java`：`ai_agent_team_member` 表 DDL + 7 列常量（TEAM_ID / MEMBER_NAME / AGENT_MODEL / ROLE / SESSION_ID nullable / ACTOR_ID nullable / JOINED_AT BIGINT）+ 唯一约束 `UK_TEAM_MEMBER (TEAM_ID, MEMBER_NAME)`（无物理 FK，与 `ai_agent_team_task.TEAM_ID` 一致）
    - `DbTeamManager.java`：implements `ITeamManager` 全部 9 方法，构造期接收 `DataSource` + `initSchema()` 建两张表（镜像 `DbTeamTaskStore.initSchema`）；raw JDBC 实现——createTeam（UUID + INSERT team + batch INSERT members）/ getTeam（SELECT team + members → 快照重建）/ getTeamBySession（SELECT member SESSION_ID 反查，裁定 4）/ disbandTeam（条件 UPDATE 幂等 + 不存在抛 NopAiAgentException）/ getActiveTeams（SELECT STATUS<>'DISBANDED'）/ addMember（SELECT 校验非 DISBANDED + INSERT，唯一约束违反 → NopAiAgentException）/ removeMember（DELETE affected==1）/ bindMemberSession（UPDATE member + 条件 UPDATE team CREATED→ACTIVE exactly-once，裁定 5）/ getMember（SELECT 重建含绑定）
  - **快照重建读语义（裁定 3）**：每次读 SELECT 团队行 + 成员行重建新鲜 `Team`（fresh `ConcurrentHashMap` member map），区别于 `InMemoryTeamManager` 返回 live 可变对象——调用者对返回 map 的修改不持久化，符合 ITeamManager 契约 "returned members read-only"。Javadoc 显式记录此行为差异。
  - **de-facto baseline 对齐（裁定 6）**：`bindMemberSession` 不校验 DISBANDED（与 `InMemoryTeamManager.bindMemberSession` 运行时行为一致，该方法仅检查 team==null）；`addMember` 在 DML 前校验团队存在 + 非 DISBANDED（与 `InMemoryTeamManager.addMember:153` fail-fast 一致）。TeamStatus doc-vs-code 分歧修复为独立 successor。
  - 测试（全绿，19 tests）：
    - `TestDbTeamManager`（16 tests）：constructorAutoCreatesTables / createAndGetRoundTripsAllFields（含 null description）/ getTeamUnknownReturnsEmpty / addMemberAndGetMember / addMemberDuplicateThrows（唯一约束）/ addMemberOnDisbandedTeamThrows / addMemberOnUnknownTeamThrows / bindMemberSessionActivatesTeam（CREATED→ACTIVE）/ bindMemberSessionOnUnknownMemberReturnsFalse / getTeamBySessionReverseLookup / getActiveTeamsFiltersDisbanded / disbandTeamIsIdempotent / disbandUnknownTeamThrows / removeMemberCleanup / **snapshotIsolationTwoGetTeamCalls**（**裁定 3 验证**：两次 getTeam 返回不同对象实例，修改 snap1.getMembers() 不影响 snap3/DB）
    - `TestDbTeamManagerCrossInstance`（3 tests）：createAddAndBindOnAVisibleOnB / disbandOnAVisibleOnB / **concurrentActivationExactlyOnce**（16 线程跨两实例并发 bindMemberSession，team 最终 ACTIVE 非 CREATED，**裁定 5 验证**）
  - **零回归**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS
- **Phase 2 交付** ✅（drop-in 接线 E2E + 文档更新）:
  - `TestDbTeamManagerWiring.java`（5 tests）：engineHoldsInjectedDbTeamManagerInstance（**接线 #23**：`assertSame(dbMgr, engine.getTeamManager())`）/ setTeamManagerWithNullFallsBackToNoOp / **defaultTeamAclCheckerResolvesRoleViaDbTeamManager**（drop-in：DefaultTeamAclChecker 持有 DbTeamManager，LEAD allow / MEMBER abandon-unclaimed deny / stranger deny null role，证明角色经 DB 解析非缓存）/ **teamStatusExecutorResolvesTeamViaContextDbTeamManager**（**E2E #22 + 接线 #23**：engine.setTeamManager(DbTeamManager) → 17 参 context 持有 engine.getTeamManager() → TeamStatusExecutor 经 getTeamManager() 解析团队返回 teamId/teamName/ACTIVE/members=2/taskCount=1）/ **crossInstanceTeamStatusViaSharedDb**（**E2E 跨实例**：两 engine 各自 DbTeamManager 指向同一 H2，engine A create+bind → engine B team-status 看到 teamId/SharedTeam/ACTIVE/members=2；engine A disband → engine B getTeam DISBANDED）
  - **Owner docs 同步**：
    - `nop-ai-agent-actor-runtime-vision.md` §4.2 TeamManager 行：DB-backed 持久化状态从 successor → **已落地（plan 230）**（`DbTeamManager` raw JDBC + `ai_agent_team`/`ai_agent_team_member` + 条件 UPDATE CAS + 构造期 initSchema）；标注快照重建读语义与 InMemoryTeamManager live 对象的差异；`@BizModel`+ORM+GraphQL 仍为 successor 平台集成方向
    - `nop-ai-agent-roadmap.md` §4 Layer 4：新增 `L4-team-db-persistence` 行（✅）+ line 258 验收标准标注团队状态跨进程 DB 共享基础设施已交付（完整多租户 tenantId/userId 隔离仍为 successor）
- **收口** ✅:
  - **Roadmap 状态**：L4-team-db-persistence（新增行）→ ✅
  - **Anti-Hollow 验证**：接线 E2E 断言 `assertSame(dbMgr, engine.getTeamManager())` + team-status 经 context.getTeamManager() 返回跨实例团队 JSON；无空方法体/无静默跳过（addMember duplicate / disbandTeam unknown / bindMemberSession unknown member 均显式 fail-fast 或返回 empty 按契约）
  - **Plan Status**：planned → completed




- **Plan**: `ai-dev/plans/229-nop-ai-agent-session-timeout-forced-abort.md`
- **Work Item**: L4-8-P4-TimeoutAbort
- **Phase 1 交付** ✅（契约表面 + NoOp 默认 + 超时裁定落档）:
  - `io.nop.ai.agent.runtime.recovery` 新增 4 个文件：
    - `TimeoutAction.java`：枚举 `LOCAL_CANCELLED` / `FORCE_FAILED` / `SKIPPED_REMOTE` / `SKIPPED`，Javadoc 明确每种动作的语义与触发条件（三分裁定结果 + NoOp shipped 默认）
    - `TimeoutOutcome.java`：不可变数据对象（sessionId + action + succeeded + message），镜像 `RecoveryOutcome` 结构
    - `ISessionTimeoutHandler.java`：接口 `handleTimeout(String sessionId) → TimeoutOutcome`，Javadoc 明确线程安全契约 + NoOp 语义 + 三分裁定责任在实现侧 + scan 步骤顺序（timeout 先于 orphan）+ 非静默契约（#24）
    - `NoOpSessionTimeoutHandler.java`：shipped 默认 singleton，`handleTimeout` 返回 SKIPPED outcome（LOG.warn 超时 session ID），零回归
  - **时间戳列裁定（裁定 2）**：核对 `AiAgentSessionTable` 已有 `COL_UPDATED_AT = "UPDATED_AT"`（`BIGINT NOT NULL`），零 schema 变更——`UPDATED_AT` 作为 lastActiveAt 代理（仍在迭代的 session 会刷新该列，不误判超时）
  - **Owner docs 同步**：`nop-ai-agent-actor-runtime-vision.md` §6.3 新增"超时强制中止已落地（plan 229）"callout（三分裁定 + raw JDBC 直读锁表理由 + 步骤顺序 + TimeoutAction/TimeoutOutcome/ISessionTimeoutHandler/NoOpSessionTimeoutHandler/DefaultSessionTimeoutHandler + RecoveryScanResult 7-arg 扩展 + 不注入 ISessionTakeoverLock 裁定 + successor 标注）+ 工作流第 3 步从 `successor：超时强制中止` 改为 `已落地：plan 229`（三分裁定 + per-agent/跨进程取消/优雅等待升级/orphan liveness 仍为 successor）
- **Phase 2 交付** ✅（DefaultSessionTimeoutHandler 三分裁定 + daemon 集成 + RecoveryScanResult 扩展 + 43 测试全绿）:
  - `DefaultSessionTimeoutHandler.java`：构造期接收 `timeoutSeconds` + `IAgentEngine engine` + `DataSource dataSource` + `String instanceId`（null fail-fast NPE）；`handleTimeout` 经 raw JDBC `SELECT LOCK_OWNER, LOCK_EXPIRES_AT FROM ai_agent_session_lock WHERE SESSION_ID=?` 单条 SELECT 三分判定：
    - **LOCAL_CANCELLED**：`LOCK_OWNER == instanceId` 且 `LOCK_EXPIRES_AT > now` → `engine.cancelSession(sessionId, "timeout", true)` forced=true（封装 graceful + thread interrupt）；异常捕获返回 succeeded=false
    - **FORCE_FAILED**：无锁行 / `LOCK_EXPIRES_AT <= now`（orphaned）→ raw JDBC `UPDATE ai_agent_session SET STATUS='failed' WHERE SESSION_ID=? AND STATUS IN ('running','pending')` 条件 WHERE（与 plan 226 ABORT 一致）；affected=0 返回 succeeded=false
    - **SKIPPED_REMOTE**：`LOCK_OWNER != instanceId` 且 `LOCK_EXPIRES_AT > now` → LOG.warn 含 LOCK_OWNER，不干预 DB
    - **不注入 ISessionTakeoverLock**（裁定 1）：`isHeld` 仅返回 boolean 不区分持有者，直读锁表是唯一不扩张 public API 的方式
  - `RecoveryScanResult.java`：7-arg 构造器（原 6 + `List<TimeoutOutcome> timeoutActions`）+ `getTimeoutActions()` getter + `empty()` 返回 `timeoutActions=emptyList()`；现有调用方适配（`ScheduledRecoveryManager.scanOnce` 7-arg / `NoOpRecoveryManager.scanOnce` `empty()` 工厂）
  - `ScheduledRecoveryManager.java`：新增 `sessionTimeoutHandler` 字段（默认 `NoOpSessionTimeoutHandler`）+ `setSessionTimeoutHandler` setter（null 拒绝）+ `getSessionTimeoutHandler` getter + `timeoutSeconds` 字段（默认 30min）+ `setTimeoutSeconds`/`getTimeoutSeconds`；`scanOnce` 步骤顺序重排为 stale lock cleanup → **timeout detection（`selectTimedOutSessions(now - timeoutSeconds*1000)` → 对每个调 `handler.handleTimeout`）** → orphan detection（裁定 3：timeout 先于 orphan 使 terminal 化的 session 被后续 orphan detection 排除，避免冲突）
  - 测试（全绿）：
    - `TestDefaultSessionTimeoutHandler`（7 tests）：LOCAL_CANCELLED 委托 cancelSession forced / LOCAL_CANCELLED 异常 succeeded=false / FORCE_FAILED 孤儿 UPDATE status=failed / FORCE_FAILED-transitioned affected=0 succeeded=false / SKIPPED_REMOTE 远端锁不动 DB / expired-lock-to-FORCE_FAILED 过期锁等同无锁 / 构造 fail-fast（null deps + 非正 timeoutSeconds）
    - `TestScheduledRecoveryManager` 新增 15 tests：defaultTimeoutHandlerIsNoOp / defaultTimeoutSecondsIs30Minutes / setTimeoutSecondsRejectsNonPositive / setSessionTimeoutHandlerRejectsNull / setSessionTimeoutHandlerInjectsNonNoOpHandler（**接线 #23**：recording handler callCount=1）/ noOpTimeoutHandlerDefaultProducesSkippedOutcomes（NoOp 零回归 DB 不变）/ noOpManagerEmptyResultHasEmptyTimeoutActions / nonTimedOutSessionIsNotTimeoutDetected（fresh UPDATED_AT 不超时）/ daemonTimeoutDetectionForceFailsOrphanedTimedOutSession（daemon 集成 FORCE_FAILED）/ **timeoutBeforeOrphanOrderingAvoidsConflict**（**裁定 3 验证**：timeout force-fail 后 orphan handler callCount=0 无冲突）/ endToEndTimeoutForceFailedCompleteScanResult（**E2E #22**：3 session 完整路径）/ daemonTimeoutLocalCancelledWiringWithMockEngine（**接线 #23** LOCAL_CANCELLED daemon 路径）
  - **零回归**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（既有 21 ScheduledRecoveryManager tests + 7 DefaultOrphanRecoveryHandler tests + 全模块零回归 + 新增 22 timeout tests = 全绿）
  - **Anti-Hollow 验证**：`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-ai-agent --severity high` → 0 findings（无空壳/无静默跳过/无吞没异常）
- **收口** ✅:
  - **独立子 agent closure-audit**（task `ses_12cbe0990ffetD2z1YoqZ7M3DP`）：Anti-Hollow 检查 PASS——(a) `ScheduledRecoveryManager.scanOnce:361` 运行时调用 `sessionTimeoutHandler.handleTimeout`（recording handler callCount=1 断言）；(b) 三分裁定三分支各有真实逻辑（LOCAL_CANCELLED 调 cancelSession forced / FORCE_FAILED 执行 raw JDBC UPDATE / SKIPPED_REMOTE LOG.warn）；(c) 无 `continue` 跳过、无空方法体、无吞没异常（所有 SQLException/RuntimeException 路径返回 succeeded=false outcome + LOG.warn）；RecoveryScanResult 7-arg + getTimeoutActions + empty() emptyList 全部验证；11/11 focused 测试类别 PASS；所有 Exit Criteria + Closure Gates substantive checks PASS
  - **Roadmap 状态**：L4-8-P4-TimeoutAbort（新增行）→ ✅



- **Plan**: `ai-dev/plans/228-nop-ai-agent-team-acl-enforcement.md`
- **Work Item**: L4-team-acl-enforcement
- **Phase 1 交付** ✅（ITeamAclChecker 契约 + DefaultTeamAclChecker + NoOp + 引擎接线）:
  - `io.nop.ai.agent.team` 新增 5 个文件：
    - `TeamAclAction.java`：枚举 READ/WRITE/EXECUTE/ADMIN（vision §5.1 AclAction 子集）
    - `TeamAclDecision.java`：不可变数据对象（allowed + reason + resolvedRole）+ `allow(MemberRole)` / `deny(MemberRole, String reason)` 工厂（allow ⇒ reason null；deny ⇒ reason 非空强制 + resolvedRole 可 null）
    - `ITeamAclChecker.java`：接口 `checkAccess(teamId, callerSessionId, toolName, action)`，返回 `TeamAclDecision`（永不 null）
    - `NoOpTeamAclChecker.java`：shipped 默认，恒 `allow(null)`（裁定 4：NoOp = 不增加授权限制，非拒绝；显式 allow 非静默跳过 #24），`noOp()` 单例
    - `DefaultTeamAclChecker.java`：构造期接收 `ITeamManager`（non-null）；静态 `Map<(toolName,action),TeamAclAction>` §5.1 矩阵（7 条映射）；流程 getTeam → 遍历 members 按 sessionId 匹配 → 取 role → 查矩阵 → LEAD 全通过 / MEMBER 通过非 ADMIN；unknown team / unknown session / unknown tuple 均显式 deny（fail closed #24，非静默 allow）
  - 引擎全链路接线（镜像 plan 225 teamManager/teamTaskStore）：
    - `AgentToolExecuteContext.java`：新增 `teamAclChecker` final 字段 + 17 参 endpoint 构造器；既有 16 参 endpoint 构造器改为委托（传 `NoOpTeamAclChecker.noOp()`）→ 5 个直接调用 16 参构造器的测试文件零修改 + `getTeamAclChecker()` getter
    - `DefaultAgentEngine.java`：新增 `teamAclChecker` 字段（默认 NoOp）+ `setTeamAclChecker`（null-safe 回退 NoOp）+ `getTeamAclChecker`；`resolveExecutor` Builder 调用追加 `.teamAclChecker(this.teamAclChecker)`
    - `ReActAgentExecutor.java`：新增 final 字段 + 构造参数 + 赋值 + Builder 字段 + Builder 方法 + Builder.build() 传递 + `AgentToolExecuteContext` 构造追加（全链路）
  - 测试（全绿）：`TestTeamAclDecision`（6 tests：allow/deny/不可变/null reason 拒绝/null resolvedRole）+ `TestDefaultTeamAclChecker`（11 tests：LEAD 全 7 组合 / MEMBER 6 非组合 allow / abandon-unclaimed deny reason 含 ADMIN+LEAD / 非成员 deny null role / 未知 team deny / 未知 tuple fail closed / 实时绑定变化可观察 / 构造拒绝 null manager / getTeam 被调用 stub verify）+ `TestNoOpTeamAclChecker`（5 tests：恒 allow / 7 tuple 全 allow / resolvedRole null / 未知 tuple 也 allow / noOp() 单例）
  - **接线验证 #23**：`TestDefaultTeamAclChecker.checkerReflectsLiveMemberBindingChange` 断言构造 checker 后再 bind 一个 member，下一次 checkAccess 能观察到角色变化（非缓存空转）+ `getTeamFromManagerIsConsulted` 用 stub ITeamManager 计数 verify
  - **零回归**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C -Dtest=TestTeamSendMessageExecutor,TestTeamStatusExecutor,TestTeamTaskCreateExecutor,TestTeamTaskUpdateExecutor,TestTeamTaskUpdateEndToEnd,TestTeamToolsEndToEnd,TestInMemoryTeamManager,TestNoOpTeamManager -Dsurefire.failIfNoSpecifiedTests=false` → BUILD SUCCESS, **65 tests, 0 failures**（既有全部团队测试零回归）
  - **No owner-doc update required**（owner doc 更新在 Phase 3）
- **Phase 2 交付** ✅（4 个团队工具 executor 集成 ACL 检查）:
  - 4 个工具均在团队解析之后、实际操作之前插入 ACL 检查：
    - `TeamSendMessageExecutor.java`：团队解析后 + 参数解析前；toolName="team-send-message" action="send"（required WRITE）
    - `TeamStatusExecutor.java`：团队解析后 + taskStore 查询前；toolName="team-status" action="view"（required READ）
    - `TeamTaskCreateExecutor.java`：团队解析后 + 参数解析前；toolName="team-task-create" action="create"（required WRITE）
    - `TeamTaskUpdateExecutor.java`：任务加载 + 跨团队校验后 + action switch 前；abandon 按当前 task.status 区分 CLAIMED→"abandon-claimed"（EXECUTE）/ CREATED→"abandon-unclaimed"（ADMIN，唯一 MEMBER 拒绝的 operation）
  - 每个工具新增 `honestDenied(callId, toolName, action, TeamAclDecision)` 辅助方法：返回 status="success" + body JSON {allowed:false, toolName, action, resolvedRole, reason}（裁定 6：denial 不中断 ReAct 循环，让 LLM 看到策略反馈）
  - 测试（全绿）：`TestTeamSendMessageExecutorAcl`（4 tests：MEMBER send 投递 / stranger 拒绝 / deny-stub 阻止 messenger / NoOp allow）+ `TestTeamStatusExecutorAcl`（3 tests：MEMBER view 含 taskCount / deny-stub 阻止 taskStore 查询 / NoOp allow）+ `TestTeamTaskCreateExecutorAcl`（4 tests：MEMBER create 落地 store / deny-stub 阻止 createTask / NoOp allow / stranger 拒绝）+ `TestTeamTaskUpdateExecutorAcl`（8 tests：MEMBER claim / MEMBER complete / MEMBER abandon CLAIMED 允许 / **MEMBER abandon CREATED 拒绝 + status 未变（Anti-Hollow #22 关键断言）** / LEAD abandon CREATED 允许 / stranger 拒绝 / NoOp allow abandon-unclaimed / deny-stub 阻止 claimTask）
  - **接线验证 #23**：每个 ACL 测试都用 deny-stub checker + recording store/messenger，断言 denial 实际阻止 store.createTask/claimTask/messenger.send 调用（非仅返回拒绝但操作仍执行）
  - **Anti-Hollow #22**：`memberAbandonUnclaimedTaskIsDeniedAndStatusUnchanged` 断言 MEMBER abandon CREATED 任务被拒绝后，store 中 task.status 仍为 CREATED（操作确实被阻止，非仅返回拒绝但状态被改）
  - **零回归**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C -Dtest=<18 个 team 相关测试> -Dsurefire.failIfNoSpecifiedTests=false` → BUILD SUCCESS, **147 tests, 0 failures**（既有 + Phase 1 + Phase 2 全绿）
- **Phase 3 交付** ✅（端到端 + 设计文档同步 + roadmap 升级）:
  - `TestTeamAclEndToEnd`（4 tests）：(a) `leadAgentFullOperationSetSucceeds`：LEAD engine.execute ReAct 调 team-task-create × 2 + team-task-update abandon-unclaimed → 全部成功（task ABANDONED，证明 LEAD ADMIN 权限经全链路生效）；(b) `memberAgentCollaborativeOpsSucceedButAbandonUnclaimedDenied`：MEMBER ReAct 调 claim + complete（COMPLETED，EXECUTE 允许）→ MEMBER create task → MEMBER abandon-unclaimed 被拒绝（**Anti-Hollow #22 关键断言：task.status 仍 CREATED 未变**；body `{allowed:false, action:"abandon-unclaimed", reason:"...ADMIN..."}`）；(c) `memberAgentSendMessageAndStatusSucceed`：MEMBER ReAct 调 team-send-message（投递到 lead inbox）+ team-status（返回 team JSON）→ READ+WRITE 允许；(d) `noOpCheckerDefaultZeroRegression`：engine 不调用 setTeamAclChecker → 默认 NoOpTeamAclChecker.noOp() singleton → MEMBER abandon-unclaimed 成功（task ABANDONED，pre-228 行为零回归）+ 断言 `engine.getTeamAclChecker() == NoOpTeamAclChecker.noOp()`
  - **端到端验证 #22**：每个测试从 `engine.execute(AgentMessageRequest)` 入口 → ReAct → tool executor → ACL checker → store/messenger，完整路径跑通且 store 状态正确变化/不变
  - **接线验证 #23**：端到端测试断言工具经 context 访问到功能性 DefaultTeamAclChecker（engine.setTeamAclChecker(new DefaultTeamAclChecker(teamManager))），且 MEMBER abandon-unclaimed 被拒绝时 task.status 在 store 中**确实未变**（ACL denial 真正阻止了 store.abandonTask 调用，不只是返回拒绝 JSON）
  - **角色区分验证**：同一 CREATED 任务，LEAD abandon → ABANDONED（成功）；MEMBER abandon → 仍 CREATED（被阻止），矩阵 ADMIN-only 裁定经端到端路径验证
  - **Owner docs 同步**：
    - `nop-ai-agent-actor-runtime-vision.md` §5.1：新增"Team ACL foundational 拦截层已落地（plan 228）"callout（ITeamAclChecker 契约 + DefaultTeamAclChecker §5.1 默认矩阵 + NoOpTeamAclChecker shipped 默认 + 4 工具集成 + abandon-unclaimed ADMIN-only + 引擎全链路接线），标注 TeamSpec permissions override / 权限派生 / 完整 AclResource 枚举 / 多租户隔离 / DB ACL 持久化 / Fencing Token / ResourceGuard 仍为 successor；§4 TeamManager 行同步更新状态
    - `nop-ai-agent-roadmap.md` §4：Layer 4 表新增 `L4-team-acl-enforcement` 行 ✅；验收标准 "多用户可并发运行独立 Actor，租户间资源隔离" 标注团队维度 ACL 基础设施已交付，完整多租户 tenantId/userId 隔离仍为 successor `L4-multi-tenant-isolation`
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS, **2307 tests, 0 failures, 0 errors**（既有 + Phase 1 + Phase 2 + Phase 3 + 全模块零回归；首次运行的 1 个 TestSteeringInjection 时序 flaky failure 重跑已确认非相关且单独运行通过）
  - **Roadmap 状态**：L4-team-acl-enforcement（新增行）→ ✅

### plan-227: nop-ai-agent team-task-update 任务状态机 + DB-backed 共享任务表（raw JDBC CAS 认领）（L4-8-team-task-update）— Phase 1 + Phase 2 + Phase 3 + 收口

- **Plan**: `ai-dev/plans/227-nop-ai-agent-team-task-update.md`
- **Work Item**: L4-8-team-task-update
- **Phase 1 交付** ✅（状态机契约 + in-memory 实现 + team-task-update 工具 + focused 测试）:
  - `TeamTask.java` 扩展：新增 `claimedBy`（String，nullable）字段 + `getClaimedBy()` getter；全参构造追加 `claimedBy` 参数（位于 createdAt 前）；`InMemoryTeamTaskStore.createTask` 传 null claimedBy；`TestInMemoryTeamTaskStore` helper `t1()` 同步 9 参构造
  - `TeamTaskStatus.java` Javadoc 补充 `CREATED → ABANDONED` 转换（消除 enum 图与 store 转换语义 drift）
  - `ITeamTaskStore.java` 新增 3 个状态转换方法 `claimTask/completeTask/abandonTask` 返回 `Optional<TeamTask>`（CAS-empty 非异常控制流）
  - `NoOpTeamTaskStore.java` 3 个转换方法抛 `UnsupportedOperationException`（No Silent No-Op #24）
  - `InMemoryTeamTaskStore.java` 经 `tasks.compute(taskId,...)` 原子 CAS 实现 3 个转换（claim 写入 claimedBy，complete/abandon 保留 claimedBy 裁定 6）
  - `TeamTaskUpdateExecutor.java`（新文件，TOOL_NAME=`team-task-update`）：action 大小写不敏感 + NoOp 诚实短路 + 跨团队 taskId 拒绝 + CAS-empty 转诚实结果（applied=false + currentStatus，非异常）
  - `ai-agent-tools.beans.xml` 注册 team-task-update bean
  - 测试：`TestInMemoryTeamTaskStoreTransitions`（11 tests：claim/重复 claim CAS/complete 需 CLAIMED/abandon 两源/终态拒绝/missing/并发 claim 仅 1 胜出）+ `TestTeamTaskUpdateExecutor`（9 tests：NoOp×2 + claim/complete/abandon + CAS-failure 诚实 + 跨团队拒绝 + 不在团队 + 大小写不敏感）
- **Phase 2 交付** ✅（DB-backed 共享任务表 raw JDBC CAS）:
  - `AiAgentTeamTaskTable.java`（新文件）：`TABLE_NAME=ai_agent_team_task` + 10 列常量 + `DDL_CREATE_TABLE`（CREATE TABLE IF NOT EXISTS + PRIMARY KEY TASK_ID），镜像 `AiAgentSessionLockTable`
  - `DbTeamTaskStore.java`（新文件）：构造期 `initSchema()` 自动建表（镜像 `DbSessionTakeoverLock`）+ 实现全部 7 方法（create INSERT/get×3 SELECT/transition×3 条件 UPDATE on STATUS affected-row-count CAS）；BLOCKED_BY CSV 存储/还原；CAS 失败（0 行）返回 `Optional.empty()`
  - 测试：`TestDbTeamTaskStore`（16 tests，H2 真实 DB：构造期建表 + 字段 round-trip + 跨实例可见性 + CAS 竞争两 store 实例 16 线程仅 1 胜出 + 终态拒绝 + abandon 两源）
- **Phase 3 交付** ✅（端到端 + 设计文档同步 + roadmap 升级）:
  - `TestTeamTaskUpdateEndToEnd`（3 tests）：in-memory 全 ReAct 路径（lead engine.execute team-task-create → member engine.execute team-task-update claim → complete → store 断言 COMPLETED + claimedBy=member → lead team-status taskCount=1，Anti-Hollow #22 + Wiring #23）+ DB-backed 跨实例（store-A create → store-B claim+complete → store-A getTask 见 COMPLETED，CAS 第二 claimer 失败）+ NoOp 默认零回归
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS，**2262 tests, 0 failures**（既有 + 39 新增，零回归）
  - Owner docs 同步：`nop-ai-agent-actor-runtime-vision.md` §8.2（team-task-update ⏳→✅ + DB-backed 已落地 callout）+ §8.3（任务认领 DB 乐观锁+CAS 标注 ✅ plan 227 已实现）+ `nop-ai-agent-roadmap.md` §4 Layer 4 验收标准 + 新增 L4-8-team-task-update 行 ✅
  - 独立子 agent closure audit（explore subagent ses_12d31d7afffe1UHA0a4csSBjo3）：12 项逐一核对 live code/tests 全 PASS，无空壳/无静默跳过，`scan-hollow-implementations.mjs` 0 findings，`check-doc-links.mjs --strict` 0 errors
  - **Roadmap 状态**：L4-8-team-task-update ❌（新增行）→ ✅

### plan-226: nop-ai-agent Orphan Session 恢复模式策略（resume/abort）（L4-8-P4-RecoveryStrategy）— Phase 1 + Phase 2 + 收口

- **Plan**: `ai-dev/plans/226-nop-ai-agent-orphan-recovery-strategy.md`
- **Work Item**: L4-8-P4-RecoveryStrategy
- **Phase 1 交付** ✅（契约表面 + NoOp 默认 + 设计裁定落档）:
  - 新增 `io.nop.ai.agent.runtime.recovery` 包 4 个文件：
    - `RecoveryMode.java`（枚举 RESUME/ABORT/SKIP，Javadoc 明确每种模式语义 + RETRY successor 裁定）
    - `RecoveryOutcome.java`（不可变数据对象：sessionId/mode/succeeded/message，全参构造 + getter）
    - `IOrphanRecoveryHandler.java`（契约接口：`handleOrphan(sessionId) → RecoveryOutcome`，Javadoc 明确线程安全契约 + NoOp 语义 + Wiring 裁定——handler 是 manager 内部策略非引擎层配置点）
    - `NoOpOrphanRecoveryHandler.java`（shipped singleton：SKIP 模式，LOG.warn orphan session ID，零回归与 plan 222 一致）
  - 设计文档同步：`nop-ai-agent-actor-runtime-vision.md` §6.1 恢复策略表（JVM 崩溃重启行标注 RESUME/ABORT/SKIP 首版已落地）+ §6.3 工作流第 2 步（从 successor 升级为部分落地，标注 RETRY successor）
- **Phase 2 交付** ✅（DefaultOrphanRecoveryHandler + daemon 集成 + RecoveryScanResult 扩展 + 测试）:
  - `DefaultOrphanRecoveryHandler.java`（功能实现：按 RecoveryMode 分发——RESUME 委托 `engine.restoreSession` fire-and-forget + 同步异常捕获 succeeded=false / ABORT raw JDBC UPDATE status=failed 条件 WHERE + affected rows=0 succeeded=false / SKIP LOG.warn；构造期 fail-fast RESUME+null engine→NPE、ABORT+null dataSource→NPE；per-session 异常隔离返回 failed outcome 非静默）
  - `RecoveryScanResult.java` 扩展：新增 `recoveryActions: List<RecoveryOutcome>` 字段 + 6-arg 构造器 + `getRecoveryActions()` getter + `empty()` 返回 emptyList（原 5-arg 构造器替换为 6-arg，唯一调用方 scanOnce 已适配）
  - `ScheduledRecoveryManager.java` 集成：新增 `orphanRecoveryHandler` 字段（默认 NoOpOrphanRecoveryHandler）+ `setOrphanRecoveryHandler` setter（null 拒绝 fail-fast）+ `getOrphanRecoveryHandler` getter（testability）+ scanOnce orphan detection 后对每个 orphan 调 `handler.handleOrphan` 汇总到 recoveryActions
  - 测试（33 tests 全绿）：
    - `TestDefaultOrphanRecoveryHandler`（9 tests）：RESUME 触发 restoreSession + RESUME 冲突 succeeded=false + ABORT DB status=failed + ABORT-transitioned succeeded=false + SKIP outcome + 3 构造期 fail-fast + mode 允许 null 交叉依赖
    - `TestScheduledRecoveryManager` 新增 8 tests：默认 handler instanceof NoOp + setter reject null + setter 注入非 NoOp handler 运行时调用（Wiring #23）+ NoOp 默认 SKIP outcomes 零回归 + empty() recoveryActions emptyList + E2E ABORT handler abort orphan session（Anti-Hollow #22）+ daemon 集成 ABORT recoveryActions
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS
  - Owner docs 同步：`nop-ai-agent-actor-runtime-vision.md` §6.1/§6.3 + RecoveryManager 组件行 + Phase 4 行 + `nop-ai-agent-reliability.md` §跨进程恢复行 + `nop-ai-agent-roadmap.md` Layer 4 表新增 L4-8-P4-RecoveryStrategy 行（✅）+ 验收标准行
  - **Roadmap 状态**：L4-8-P4-RecoveryStrategy 新增行 ✅

### plan-225: nop-ai-agent 团队通信工具（L4-8-team-tools）— Phase 2 + 收口

- **Plan**: `ai-dev/plans/225-nop-ai-agent-team-communication-tools.md`
- **Work Item**: L4-8-team-tools
- **Phase 2 交付** ✅（端到端验证 + 设计文档同步）:
  - 测试：`TestTeamToolsEndToEnd`（2 tests）：
    - E2E（Anti-Hollow #22）：lead agent `engine.execute()` → ReAct 循环 → team-send-message(to=worker) → messenger → worker inbox 收到消息（断言 payload + senderId + targetTopic + ASYNC kind）→ team-task-create(subject + description) → taskStore 存入 1 task（断言 subject/description/createdBy）→ team-status → tool response 含 taskCount=1（断言 tool response 含 inbox topic + CREATED + taskCount:1）
    - NoOp 零回归：默认引擎（无 setTeamManager/setTeamTaskStore/setMessenger）→ team-send-message 在 ReAct 循环中被调用 → 诚实报告"not enabled" → agent 正常 completed
  - 接线验证（#23）：端到端测试经 `AgentToolExecuteContext` 访问功能性 teamManager（非 NoOp）+ teamTaskStore（非 NoOp），工具实际操作了团队注册表（消息投递）+ 任务存储（创建任务）
  - Owner docs 同步：
    - `nop-ai-agent-actor-runtime-vision.md` §8.2（团队通信模型标注 foundational 工具已落地：team-send-message/team-status/team-task-create in-memory ✅，team-task-update + DB 持久化 successor）+ §10 Phase 3（补 plan 225 落地标注）+ TeamManager 组件行（从 successor 升级 foundational 落地）
    - `nop-ai-agent-roadmap.md` §4 Layer 4 验收标准 line 252（从"foundational 前置已落地"升级为"团队通信工具 foundational 已落地"）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -T 1C` → BUILD SUCCESS，**2207 tests, 0 failures**（既有 2205 + 2 Phase 2 E2E，零回归）
  - **Roadmap 状态**：L4-8-team-tools ❌ → ✅

### plan-225: nop-ai-agent 团队通信工具（L4-8-team-tools）— Phase 1

- **Plan**: `ai-dev/plans/225-nop-ai-agent-team-communication-tools.md`
- **Work Item**: L4-8-team-tools（vision §8.2 团队通信模型 foundational tool 层）
- **Phase 1 交付** ✅（团队任务存储 + Context 接线 + 3 个工具实现 + focused 测试）:
  - 新增 `io.nop.ai.agent.team` 包 5 个文件：
    - `TeamTaskStatus.java`（枚举 CREATED/CLAIMED/COMPLETED/ABANDONED，本计划只使用 CREATED，其余为 successor 预留）
    - `TeamTask.java`（不可变数据对象：taskId/teamId/subject/description/blockedBy(List<String>)/status/createdBy/createdAt，全参构造 + getter，无 setter）
    - `ITeamTaskStore.java`（契约接口：createTask/getTask/getTasksByTeam/getTasksByCreator）
    - `NoOpTeamTaskStore.java`（shipped 默认：createTask 抛 UOE，查询返回 empty — No Silent No-Op #24；`noOp()` 静态工厂）
    - `InMemoryTeamTaskStore.java`（ConcurrentHashMap 双索引 taskId→TeamTask + teamId→taskIds，线程安全）
  - 新增 `io.nop.ai.agent.tool` 包 3 个 IToolExecutor：
    - `TeamSendMessageExecutor.java`（TOOL_NAME="team-send-message"：经 teamManager 路由解析成员 session → messenger.send 投递到 inbox topic；NoOp 短路诚实报告）
    - `TeamStatusExecutor.java`（TOOL_NAME="team-status"：返回结构化 JSON 含 teamId/teamName/status/members/taskCount；NoOp 短路诚实报告，NoOp taskStore → taskCount=0 不短路）
    - `TeamTaskCreateExecutor.java`（TOOL_NAME="team-task-create"：经 teamManager 反查团队 → taskStore.createTask 返回 {taskId, status:CREATED}；blockedBy 逗号分隔存储；NoOp 短路诚实报告）
  - 接线：
    - `AgentToolExecuteContext` 新增 `teamManager` + `teamTaskStore` 字段 + getter + 新全参构造器（既有构造器委派传 null，向后兼容）
    - `ReActAgentExecutor` Builder 新增 `teamManager()` + `teamTaskStore()` 方法 + 字段；dispatch 循环构造 context 时传入
    - `DefaultAgentEngine` 新增 `teamTaskStore` 字段（默认 NoOpTeamTaskStore.noOp()）+ setTeamTaskStore/getTeamTaskStore；resolveExecutor 接线 teamManager + teamTaskStore 到 builder
    - `ai-agent-tools.beans.xml` 注册 3 个新工具 bean（经 collect-beans 自动收集）
  - 测试（32 tests 全绿）：
    - `TestInMemoryTeamTaskStore`（14 tests）：完整生命周期 + 并发安全（10 线程 × 50 任务无丢失）+ 边界条件 + snapshot 不可变性
    - `TestTeamSendMessageExecutor`（7 tests）：NoOp/null 诚实报告 + 功能性投递到正确 inbox topic + 未绑定 session 错误 + 未在团队错误 + 未知成员错误
    - `TestTeamStatusExecutor`（5 tests）：NoOp/null 诚实报告 + 功能性 JSON 含 team/members/taskCount + NoOp taskStore→taskCount=0 + 未在团队错误
    - `TestTeamTaskCreateExecutor`（6 tests）：NoOp teamManager/taskStore 诚实报告 + 功能性 create 返回 taskId+CREATED + blockedBy 存储 + 未在团队错误 + 缺 subject 错误
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -T 1C` → BUILD SUCCESS，**2205 tests, 0 failures**（既有 2173 + 32 Phase 1，零回归）
  - **NoOp 零回归**：shipped 默认（NoOpTeamManager + NoOpTeamTaskStore + NoOpAgentMessenger）下 3 个工具诚实报告"团队功能未启用"，既有全量测试零回归

### plan-224: nop-ai-agent call-agent 异步 mailbox 请求-响应模型（L4-8-call-agent-async）— Phase 2 + 收口

- **Plan**: `ai-dev/plans/224-nop-ai-agent-call-agent-async-mailbox.md`
- **Work Item**: L4-8-call-agent-async（plan 218/220/223 Non-Blocking Follow-ups；vision §10 Phase 2 剩余项）
- **Phase 2 交付** ✅（功能实现端到端验证 + 设计文档同步）:
  - 测试：`TestCallAgentAsyncEndToEnd`（7 tests）：
    - E2E（Anti-Hollow #22）：parent `engine.execute()` → ReAct 循环 → call-agent async pathway → `messenger.request` → engine-registered handler → 子 Agent `engine.execute()` → RESPONSE → tool result 回灌 parent ReAct → parent 完成（断言子 Agent reply 出现在 tool-response message + TOOL_CALL_STARTED/COMPLETED 事件）
    - 三种子 session 模式：(a) continue-existing（sessionId 非空，结果 sessionId 匹配）、(b) fork-from-parent（agentId="self" + inheritContext=true，CallAgentExecutor 先 forkSession 再 async REQUEST，child session parentSessionId 正确）、(c) create-new（sessionId=null，新 UUID session）
    - handler 异常 → fast error RESPONSE（注入 throwingChat，断言 <20s 返回 failure 而非 timeout）
    - 超时：`call.setTimeoutMs(300)` + slowChat（sleep 5s）→ 断言 <4s 返回 failure（timeout 在 ~300ms 触发）
    - fallback 回退：NoOp messenger → fork+exec（结果 = sub-agent reply，零回归）
  - Owner docs 同步：
    - `nop-ai-agent-actor-runtime-vision.md` §10 Phase 2（call-agent 异步模式从"successor"→"🟡 部分落地"：引擎级 topic 路由 foundational 已交付，per-session inbox 路由 + 异步非阻塞 handler + 多用户隔离仍为 successor）+ §10 注释段（补 plan 224 foundational 落地）
    - `nop-ai-agent-multi-agent.md` §5.1 父子通信（补 call-agent async mailbox 模型 foundational 落地 + 载荷契约）+ §6 Phase 2 行（🟡 + plan 224 落地标注）+ §6 注释段
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS，**2173 tests, 0 failures**（既有 2153 + 13 Phase 1 + 7 Phase 2，零回归）
  - **偏差**：超时测试初版将 `timeoutMs` 放在 JSON input string 中，但 `resolveTimeoutMs` 读 `call.getTimeoutMs()` 字段（非 args），修正为 `call.setTimeoutMs(300)`；非 plan 偏差，是测试 fixture 问题
  - **Roadmap 状态**：L4-8-call-agent-async 无独立 ❌ 行；其归属的 roadmap §4 `L4-8 | Actor Runtime 平台层` 已为 ✅（foundational），本次交付的 call-agent async 是 L4-8 内 Phase 2 successor 项，落地状态记录在 vision §10 Phase 2 + multi-agent.md §6
- Doc-sync: `nop-ai-agent-actor-runtime-vision.md` / `nop-ai-agent-multi-agent.md` 已更新

### plan-224: nop-ai-agent call-agent 异步 mailbox 请求-响应模型（L4-8-call-agent-async）— Phase 1

- **Plan**: `ai-dev/plans/224-nop-ai-agent-call-agent-async-mailbox.md`
- **Work Item**: L4-8-call-agent-async（plan 218/220/223 Non-Blocking Follow-ups；vision §10 Phase 2 剩余项）
- **Phase 1 交付** ✅（引擎级 request handler + CallAgentExecutor async gate + 载荷对象）:
  - 新增 `io.nop.ai.agent.message` 包 2 个不可变载荷数据对象：
    - `CallAgentRequestPayload.java`（REQUEST 载荷：`targetAgentId`/`input`/`resolvedSessionId`(nullable)/`parentConstraintMetadata`(nullable, defensive copy)/`timeoutMs`；全参构造 + getter，无 setter；null targetAgentId 抛 `IllegalArgumentException`）
    - `CallAgentResponsePayload.java`（RESPONSE 载荷：`status`("success"|"failure")/`sessionId`(nullable)/`finalMessage`(null→"")/`error`(nullable)；全参构造 + getter，无 setter；null status 抛 `IllegalArgumentException`）
  - `AgentMessageTopics` 新增 `CALL_AGENT_TOPIC = "agent.call-agent"` 常量 + `callAgentTopic()` helper（call-agent topic 名称唯一来源）
  - `DefaultAgentEngine` 接线：
    - 新增 `volatile IMessageSubscription callAgentSubscription` 字段
    - `setMessenger` 升级：idempotent——先 cancel 既有 `callAgentSubscription` 再注册新 handler（functional messenger）；NoOp messenger 不注册（零回归）；重复 setMessenger 不 double-register
    - 新增 `handleCallAgentRequest(AgentMessageEnvelope)` handler：接收 REQUEST→`CallAgentRequestPayload`→`engine.execute()` `.orTimeout().join()`→`CallAgentResponsePayload`；try/catch 包裹，异常返回 `RESPONSE(status="failure")` 不传播到 messenger dispatch 层（Design Decisions §5）
    - 新增 `extractFinalAssistantMessage` helper（镜像 CallAgentExecutor 逻辑，engine 不依赖 tool 包）
  - `CallAgentExecutor` 新增 async gate：
    - `instanceof NoOpAgentMessenger` 判定（Design Decisions §2，参照 SendMessageExecutor）
    - async pathway（`executeViaMessenger`）：保留全部 session-mode 分支解析（continue/fork/create-new）→ 构建 REQUEST 信封（senderId=parentSessionId fallback "unknown-sender"，correlationId=UUID，targetTopic=callAgentTopic）→ `messenger.request()` → RESPONSE 转 `AiAgentCallResult`
    - fork 模式仍由 CallAgentExecutor 先同步调用 `engine.forkSession()` 获得 childSessionId 再填入 payload（Design Decisions §1）
    - 新增 `dispatch` 路由方法 + `responseToToolCallResult` + `rootCauseMessage` helper
    - fallback pathway：既有 fork+exec `executeSubAgent` 逻辑不变
  - 测试：`TestCallAgentAsyncMailbox`（13 tests）：载荷不可变性/defensive-copy/contract、callAgentTopic helper、handler 注册 idempotent + NoOpt-out + cancel-on-switch、handler 异常→failure RESPONSE（fast，非 timeout）、Phase 1 集成测试（CallAgentExecutor.executeAsync + functional messenger + engine-registered handler → handler 触发 + RESPONSE 返回 sub-agent 实际输出，Anti-Hollow #22/#23）、NoOp fallback fork+exec 零回归
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS，**2166 tests, 0 failures**（既有 2153 + 13 新增，零回归）
  - **偏差**：无。`extractFinalAssistantMessage` 在 DefaultAgentEngine 内复制（10 行）而非提取为共享工具类——engine 不依赖 tool 包（避免循环依赖），plan §3 允许"提取为共享 helper 或复用逻辑"
- Doc-sync: 无（Phase 1 不涉及 owner-doc 更新，vision §10 Phase 2 标注在 Phase 2 更新）

### plan-223: nop-ai-agent TeamManager 团队生命周期管理（L4-8-team-manager）

- **Plan**: `ai-dev/plans/223-nop-ai-agent-team-manager.md`
- **Work Item**: L4-8-team-manager（plan 218/222 Non-Blocking Follow-ups；roadmap §4 Layer 4 line 251 foundational 前置）
- **Phase 1 + Phase 2 全量交付** ✅:
  - 新增 `io.nop.ai.agent.team` 包（9 个文件）：
    - `MemberRole.java`（LEAD/MEMBER，identity tag，不做 ACL 强制）+ `TeamStatus.java`（CREATED→ACTIVE→DISBANDED 状态机，DISBANDED 终态）
    - `TeamMemberSpec.java`（不可变：memberName/agentModel/role）+ `TeamSpec.java`（不可变：teamName/description/leadAgentName/memberSpecs（defensive copy）/maxParallelMembers（<=0 无限制，不强制配额））
    - `TeamMember.java`（运行时成员：immutable memberName/role/joinedAt + mutable sessionId/actorId + `bind()`）+ `Team.java`（运行时团队：immutable teamId/spec/createdAt + mutable members/status/disbandedAt，线程安全由 manager 保证）
    - `ITeamManager.java`（契约：createTeam/getTeam/getTeamBySession/disbandTeam/getActiveTeams/addMember/removeMember/bindMemberSession/getMember）
    - `NoOpTeamManager.java`（shipped singleton：写操作抛 `UnsupportedOperationException`、读操作返回 empty，Minimum Rules #24 无静默跳过）
    - `InMemoryTeamManager.java`（功能实现：`ConcurrentHashMap` 双索引 teamId + sessionId 反查，完整生命周期 + CREATED→ACTIVE 首绑定激活（compute 保证 exactly-once）+ 幂等 disband + disbanded 保留可查询）
  - `DefaultAgentEngine` 接线：新增 `teamManager` 字段（默认 `NoOpTeamManager.noOp()`）+ `setTeamManager`/`getTeamManager`（null-safe fallback，参照 `setActorRuntime` 模式）。引擎当前不在三入口点自动创建团队（团队创建由集成商/工具程序化调用，自动团队绑定是 successor）
  - 测试：`TestTeamSpec`（12 tests：不可变性/defensive copy/unmodifiable/maxParallelMembers 语义/bind/null 校验/枚举稳定性）+ `TestNoOpTeamManager`（10 tests：写操作抛 UOE/读操作 empty/singleton）+ `TestInMemoryTeamManager`（23 tests：完整生命周期/边界条件（重复 memberName/disband 后 addMember/missing 返回 empty）/幂等 disband/并发 8 线程不同团队）+ `TestTeamManagerEngineWiring`（3 tests：NoOp shipped default/setter 注入/null 回退）+ `TestTeamManagerEndToEnd`（1 test：E2E——createTeam + engine.execute()（Actor 注册）+ bindMemberSession（CREATED→ACTIVE）+ getTeamBySession 反查 + disbandTeam + Actor 销毁，Anti-Hollow #22）
  - Owner docs 同步：
    - `nop-ai-agent-actor-runtime-vision.md` §4.2 TeamManager 组件状态（successor → 🟡 部分落地）+ §10 Phase 3（foundational slice 已交付标注）
    - `nop-ai-agent-roadmap.md` §4 Layer 4 验收标准 line 251（"多 Agent 任务可以通过 Flow / Task 组织"标注 foundational 前置 TeamManager 注册表已落地，完整 Flow/Task 仍为 successor）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS，**2153 tests, 0 failures**（含 49 新增 team tests，既有测试零回归）
  - **偏差**：无。ITeamManager 契约 `disbandTeam`/`addMember` 按 plan 返回 `Team`/`TeamMember`（缺失时抛 `NopAiAgentException` 而非返回 Optional），与 plan 指定签名一致。`TestTeamSpec` 一处测试 `teamSpecMemberSpecsSnapshotIsolatesInstances` 被移除（`List.copyOf` 对相同不可变输入可能返回同一实例，是 JDK 优化而非契约——defensive copy 契约已由 source-mutation 测试覆盖）
  - **Roadmap 状态**：L4-8-team-manager 无独立 ❌ 行；其归属的 Layer 4 验收标准 line 251 仍为 `[ ]`（完整 Flow/Task 组织未完成），本次在原文标注 foundational 前置（TeamManager 注册表）已落地
- Doc-sync: `nop-ai-agent-actor-runtime-vision.md` / `nop-ai-agent-roadmap.md` 已更新

### plan-222: nop-ai-agent RecoveryManager 定时扫描 Daemon（L4-8-P4-RecoveryDaemon）

- **Plan**: `ai-dev/plans/222-nop-ai-agent-recovery-manager-daemon.md`
- **Work Item**: L4-8-P4-RecoveryDaemon（plan 221 Non-Blocking Follow-ups 第一条；roadmap §4 Layer 4 line 253）
- **Phase 1 + Phase 2 全量交付** ✅:
  - 新增 `io.nop.ai.agent.runtime.recovery` 包：
    - `IRecoveryManager.java`：opt-in RecoveryManager daemon 契约，3 个方法 `start()` / `stop()`（幂等生命周期）+ `scanOnce() → RecoveryScanResult`（手动触发单次扫描）；Javadoc 明确生命周期语义、调度机制裁定（`IScheduledExecutor` 非 nop-job）、部署层生命周期管理设计契约（引擎不调用 start/stop）、scope（stale lock cleanup + orphan detection，不含恢复策略）
    - `RecoveryScanResult.java`：不可变数据对象（`staleLocksCleaned` int + `orphanSessionsDetected` int + `orphanSessionIds` List<String>（unmodifiable）+ `scanDurationMs` long + `scannedAt` long）+ `empty()` 全零值工厂方法
    - `NoOpRecoveryManager.java`：shipped singleton，`start`/`stop` no-op，`scanOnce` 返回 `RecoveryScanResult.empty()`——显式"no recovery scanning"语义，零回归
    - `ScheduledRecoveryManager.java`：功能实现，构造期接收 `DataSource` + `IScheduledExecutor`（默认 60s fixed-delay，可配置）+ 自动 `initLockSchema`（参照 `DbSessionTakeoverLock`）；`scanOnce` 执行 (a) stale lock cleanup（`DELETE FROM ai_agent_session_lock WHERE LOCK_EXPIRES_AT <= now`，幂等，记录 affected rows）+ (b) orphan session detection（`SELECT ... STATUS IN ('running','pending')` 且无活跃锁，逐个 `LOG.warn`，非静默）+ 组装返回 `RecoveryScanResult`；`start`/`stop` 经 `IScheduledExecutor.scheduleWithFixedDelay` 注册 + `ScheduledFuture.cancel(false)` 注销，`synchronized` + `volatile` handle 保证幂等与线程安全；调度任务经 `scanOnceSafe()` 包装（异常 LOG.warn 非静默杀任务）；SQL 失败抛 `NopAiAgentException`（非吞没，Minimum Rules #24）
  - `DefaultAgentEngine` 接线：新增 `recoveryManager` 字段（默认 `NoOpRecoveryManager`）+ `setRecoveryManager`/`getRecoveryManager`（null-safe fallback to NoOp，参照 `setSessionTakeoverLock` 模式）。**引擎不调用 `start()`/`stop()`**——遵循 `IAgentEngine` 部署层生命周期管理设计契约（`IAgentEngine.java:166-171`），集成商在部署层调用
  - **调度机制裁定落档**：采用 `IScheduledExecutor`（nop-commons，`scheduleWithFixedDelay` 语义，经 nop-core 传递依赖已可用）而非 nop-job `IJobScheduler`（重量级，`nop-ai-agent/pom.xml` 无依赖）；对"每 60s 跑一次 scanOnce"足够；nop-job 集成为 successor 增强方向
  - 测试：`TestScheduledRecoveryManager`（17 tests：stale-lock-cleanup / active-lock-preserved / orphan-detection / terminal-excluded / active-lock-not-orphan / expired-lock-then-orphan / result-fields / E2E / start-wiring / custom-interval / stop-wiring / idempotent / stop-before-start / scheduled-task-runs-scanOnce-Anti-Hollow / NoOp-default / NoOp-singleton / constructor-validation）+ `TestRecoveryManagerEngineWiring`（4 tests：NoOp-shipped-default / setter-injection / null-fallback / NoOp-scanOnce-explicit-zero），全部经真实 H2 DB 验证
  - Owner docs 同步：
    - `nop-ai-agent-actor-runtime-vision.md` §6.3 工作流（接管锁基础层 + 定时扫描 daemon 已落地 + 调度机制裁定）+ §6.3 工作流图（步骤 1 标注部分落地，步骤 2-4 标注 successor）+ §4.2 RecoveryManager 组件状态 + §10 Phase 4
    - `01-architecture-baseline.md` 接管锁段落（更新"无后台扫描线程"→"定时扫描 daemon 提供 stale-lock cleanup"，补 plan 222 daemon 契约）
    - `nop-ai-agent-reliability.md` §1.1（接管锁基础层 + 定时扫描 daemon 已落地）
    - `nop-ai-agent-roadmap.md` §4 Layer 4 验收标准 line 253（补 plan 222 落地）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS，**2104 tests, 0 failures**（含 21 新增 RecoveryManager tests，既有测试零回归）
  - **偏差**：无。plan 的 scanOnce SQL 与 `ScheduledRecoveryManager` 实现完全一致；`constructorValidatesArguments` 测试发现原构造器用 `Objects.requireNonNull`（抛 NPE）与模块异常约定（`NopAiAgentException`）不符，已收敛为显式 null + 范围校验抛 `NopAiAgentException`（符合 AGENTS.md 模块内部异常约定）
  - **Roadmap 状态**：roadmap §4 L4-8 行已为 ✅（接管锁基础层 + daemon 均归属 L4-8）；L4-8-P4-RecoveryDaemon 无独立 ❌ 行（其归属的 Layer 4 验收标准"长任务中断后可以恢复"已为 ✅，本次在原文末尾追加 plan 222 落地说明）
- Doc-sync: `nop-ai-agent-actor-runtime-vision.md` / `01-architecture-baseline.md` / `nop-ai-agent-reliability.md` / `nop-ai-agent-roadmap.md` 已更新
# 2026-06-17 Dev Log

### nop-job-local 模块抽取：从 nop-job-service 迁移到 nop-job-local（无 DAO 依赖）

按照 review 意见，将 local 调度实现从 `nop-job-service`（依赖 `nop-job-dao`）迁移到新建的 `nop-job-local` 模块，该模块仅依赖 `nop-job-core` + `nop-core`，无 DAO/Biz 依赖。

**迁移内容：**
- 新建 `nop-job-local` Maven 模块（`pom.xml` + 目录结构）
- 迁移 3 个 main 类：`BeanMethodJobInvoker`、`BeanContainerInvokerResolver`、`LocalJobConfigLoader`
- 迁移 2 个测试类：`TestBeanMethodJobInvoker`（10 cases）、`TestLocalJobConfigLoader`（7 cases）
- 迁移 `app-local-scheduler.beans.xml`
- 从 `nop-job-service` 删除上述文件
- 添加 `_module` 标记文件以确保模块发现
- 更新 `nop-job/pom.xml`（+module）、`nop-job-service/pom.xml`（+dep）、`nop-bom/pom.xml`（+version mgmt）
- 补齐文档：`ioc-and-config.md` 增加 `_module` 关键约束说明

**验证：**
```
./mvnw test -pl nop-job/nop-job-local -am -o
Tests run: 17, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

**Deviation from plan:** 新增模块而非保持在 nop-job-service；Plan 211 已关闭但未预见模块迁移，下次 plan 更新需补上

**Doc-sync:** `docs-for-ai/02-core-guides/ioc-and-config.md` 已更新（新增第 4 条 `_module` 约束规则）

### nop-job local-config-scheduling (Plan 211) — full implementation + test pass

Implemented Plan 211: YAML config → LocalJobScheduler → BeanMethodJobInvoker, no database.

**Key facts:**
- 3 new DataBean classes in `nop-job-api`: `LocalSchedulerConfig`, `LocalJobConfig`, `LocalInvokerConfig`
- 4 new Java files in `nop-job-service`: `BeanMethodJobInvoker`, `BeanContainerInvokerResolver`, `LocalJobConfigLoader`, `JobApiErrors` (error codes)
- 2 new bean XML files: `app-local-scheduler.beans.xml` (5 beans), `app-service.beans.xml` (import removed — app-local-scheduler auto-loaded by `app-*.beans.xml` pattern)
- 2 test classes: `TestBeanMethodJobInvoker` (10 cases), `TestLocalJobConfigLoader` (7 cases)

**Key fixes during test prep:**
- `BeanMethodJobInvoker` method resolution: no-arg preferred when params empty, resolve before try-catch for fail-fast
- `LocalJobConfigLoader.init()`: Guard against VFS-not-initialized
- Bean XML: `constructor-arg` requires explicit `index` attribute; `ioc:init-method` → `@PostConstruct` annotation (remove XML attr)
- `app-service.beans.xml`: removed `<import resource="app-local-scheduler.beans.xml"/>` (caused duplicate bean registration since `app-*.beans.xml` is auto-loaded)

**Deviation from plan:**
- `app-service.beans.xml` changes: instead of keeping the `<import>`, removed it (auto-discovery handles loading)
- Test class names: `TestBeanMethodJobInvoker` (not `BeanMethodJobInvokerTest`), `TestLocalJobConfigLoader` (not `LocalJobConfigLoaderTest`)
- No explicit `init-method`/`destroy-method` XML attributes on `LocalJobConfigLoader` (uses `@PostConstruct`/`@PreDestroy`)
- `DefaultScheduledExecutor` uses `destroy-method="destroy"` XML attr (has `@PostConstruct` but no `@PreDestroy`)

**Verification:**
```
./mvnw test -pl nop-job/nop-job-service -o -DskipTests=false
Tests run: 58, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

- New tests: `TestBeanMethodJobInvoker` — 10/10 pass
- New tests: `TestLocalJobConfigLoader` — 7/7 pass
- Existing: `TestRpcJobInvoker` — 14/14 pass
- Existing: `TestNopJobScheduleBizModel` — 15/15 pass (was 1 fail before fix)
- Existing: `TestNopJobFireBizModel` — 1/1 pass
- Existing: `TestNopJobTaskBizModel` — 1/1 pass

**Doc-sync:** No owner-doc update required (internal module implementation; no API change)

**Commit:** (pending)
