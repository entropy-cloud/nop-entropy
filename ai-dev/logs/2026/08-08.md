### nop-stream 独立审计 Stage 18：Current Production Finding Disposition（plan 执行）

- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-2000-1-current-production-finding-disposition.md`（3 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**纯审计/数据计划（无生产代码变更）**——产出 `@@DISPOSITION` 裁决表 + disposition validator 子命令，不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。
- 产出：
  - `ai-dev/audits/nop-stream-independent-audit/stage-18-current-production-disposition.md`（42 条 `@@DISPOSITION` 块 + header 统计 disposition×severity 交叉表 + disposition×domain 交叉表）。
  - `ai-dev/audits/nop-stream-independent-audit/evidence-schema.md` 追加 "Stage 18 Supplement — Finding-Disposition Schema" 段落（additive，不改 11 字段/7-value evidence-row 词表）。
  - `ai-dev/tools/check-nop-stream-audit-manifest.mjs` 新增 `disposition` 子命令（支持 `--shard <N>` / `--strict`）+ `self-test` 阳性对照 + `all` 接线。

**Phase 1（Disposition 格式与 validator 基础设施）**：evidence-schema.md 追加 Stage 18 Supplement（`@@DISPOSITION` 格式 + 5-value 词表 `revalidated|stale|active/successor owner|residual-risk|blocked` + 字段依赖规则 + 两套词表关系说明）。validator 实现 `disposition` 子命令：partial 模式（只校验已有行合法性）、strict 模式（completeness：shard N 每个 finding 恰好一条）、owner_plan 路径/sentinel 校验（`roadmap-stage-<N>` 指向非 done stage）、blocked_lane 注册校验、corpus 交叉校验（finding_id/severity 一致）。阳性对照覆盖 9 种已知坏输入 + completeness/no-dup/sentinel。`all` 子命令接线 disposition checker（partial 模式，不跨 shard 强制 completeness）。

**Phase 2（P0/P1/AR 19 条裁决）**：19 条 P0(2)/P1(13)/AR(4) finding 逐条 live-revalidated。结果：P0-1 revalidated（JobCoordinator fencing-epoch-before-lock + late-arrival guard + restart cap，cross-ref EVID-S9-016/S13-012/S14-016）、P0-2 revalidated（TestTaskManagerDaemon 重写有回归力）、P1-1..6 revalidated（keyLayoutVersion fail-fast、ref-count rollback、JDBC separate-txn、permit net-0、InputGate concurrent collections、waitForTerminal fail-loud）、P1-7..10 revalidated（design docs 与 code 对齐）、P1-11..12 stale（_module 已存在、TestTaskExecutorDaemonThreads 已删除）、P1-13 revalidated（@Tag("low-value") 显式标注）、AR-1 revalidated（releaseNode lockstep fix，cross-ref EVID-S12-006）、AR-2/3/4 residual-risk（AR severity 允许，附 non-blocking rationale + successor）。

**Phase 3（P2 23 条裁决 + 全 shard 收口）**：23 条 P2 finding 逐条 live-revalidated。结果：P2-10 revalidated（assignTasks lock-split fix，cross-ref EVID-S13-001）、22 条 residual-risk（每条附 non-blocking rationale + owner_plan 指向 completed remediation plan）。全 shard 42 条 finding 完整性核对通过。Header 统计：15 revalidated / 2 stale / 25 residual-risk / 0 active-owner / 0 blocked = 42。

**Cross-cutting compliance**：零 P0/P1 落到 residual-risk；每条 P2/AR residual-risk 附 non-blocking rationale；3 份 remediation plan owned 15 条 finding 修复状态逐条确认。

**验证**：`node --check` EXIT=0；`disposition --shard 18`（partial）EXIT=0；`disposition --shard 18 --strict` EXIT=0（42 行完整）；`self-test` EXIT=0（5 checker 阳性对照全 PASS）；`all --strict` EXIT=0（5 checker 全 PASS）。独立子 agent closure-audit（task_id ses_02150cfa7ffeySzFUTueEAF0Il）确认 12 Closure Gates 全 PASS + Anti-Hollow 验证（15 revalidated 全有具体证据、42/42 无丢弃、counts 一致）。`check-plan-checklist.mjs --strict` EXIT=0（closure bookkeeping 完成后）。`scan-hollow-implementations.mjs --module nop-stream --severity high` EXIT=1（pre-existing 生产代码空壳，非本 audit-only 计划范围，informational）。roadmap work item 18 `planned → done`。

**Non-blocking follow-up**：(1) residual-risk P2/AR findings 的 owner_plan 指向 completed remediation plans，若后续 reclassify 为 P0/P1 由 successor remediation plan / Stage 23 处理；(2) `scan-hollow-implementations.mjs` nop-stream 生产代码空壳发现属 nop-stream-production remediation track，非本计划。Doc-sync: `No owner-doc update required`（disposition 是审计基础设施；不改 `docs-for-ai/`）。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-0610-2-jdbc-file-cdc-connector-external-effect-audit.md`（3 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + JDBC/file/CDC 三段支持/拒绝矩阵，不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。引用 Stage 9 checkpoint 结论与 Stage 14 transport 结论，不重新审计。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-16-jdbc-file-cdc-connector.evidence.md`（16 条 `@@EVIDENCE` 行 EVID-S16-001..016 + 头部 JDBC/file/CDC 三段 Support/Reject Matrix）。

**Phase 1（JDBC Two-Phase-Commit Sink External-Effect）**：5 行（EVID-S16-001..005）。commit atomicity `e2e-proved`（`JdbcTwoPhaseCommitSink.commit():191-266` 独立 connection `:213` + `setAutoCommit(false):216` + data `:228-230` + ledger `:233` 同一 `connection.commit():235`，positive_proof `TestJdbcTwoPhaseCommitSinkSkeleton#testFullCycleCommitWritesDataAndLedger` embedded H2）；idempotent ledger guard `e2e-proved`（`:219-225` ledgerExists skip + `:340-347` SQL，positive_proof `TestJdbcTwoPhaseCommitSinkDeep#testIdempotentCommitNoDuplicates` recover-safe re-commit no-dup）；saveState-first `e2e-proved`（`:170-182` override buffer→pendingCommits BEFORE super，positive_proof `#testSaveStateCapturesEpochNInThisCheckpoint` + `#testSaveStateLagByOneEpochIsAvoided`）；abort/rollback `e2e-proved`（`abort():276-280` + `rollback():269-273` + commit-failure `connection.rollback():246-252`，positive_proof `#testAbortClearsPendingEntry`）；TWO_PHASE_COMMIT label `e2e-proved`（`:283-285`，ledger idempotency 真防 dup）。冻结 JDBC 矩阵（J1-J5）。

**Phase 2（File Two-Phase-Commit Sink External-Effect）**：5 行（EVID-S16-006..010）。commit atomic-rename+manifest `e2e-proved`（`FileTwoPhaseCommitSink.commit():150-197` ATOMIC_MOVE `:185` + manifest `:193`，positive_proof `TestFileTwoPhaseCommitSink#testCommitAtomicRenamesAndUpdatesManifest`）；final-exists/manifest-missing edge repair `e2e-proved`（`:174-181`，positive_proof `#testCommitFinalExistsManifestMissingRepairsManifest` **有专门 edge-case 测试**，故 e2e-proved 非 residual-risk）；saveState-first temp+abort `e2e-proved`（`:130-142` + `:207-213`，positive_proof `#testSaveStateWritesTempFileAndClearsBuffer` + `#testAbortDeletesTempFile`）；atomic manifest update `e2e-proved`（`updateManifestAtomically():237-256` temp+ATOMIC_MOVE+REPLACE_EXISTING）；TWO_PHASE_COMMIT label `e2e-proved`（`:109-111`，manifest key 真防 dup）。冻结 file 矩阵（F1-F5）。

**Phase 3（CDC Offset Restore + M7-2-P0-2 + Tier-Label + Backend Blocked）**：6 行（EVID-S16-011..016）。CDC offset snapshot/restore round-trip `e2e-proved`（`DebeziumCdcSourceFunction.snapshotState():212-227` + `initializeState():231-258`，positive_proof `TestDebeziumCdcCheckpoint#testSnapshotRestoreRoundTrip` + `#testCdcCheckpointKillRecoverNoDuplicates` mocked in-process）；CDC fail-fast non-Map state `residual-risk`（`:246-250` throw，**无专门 rejection 测试** → manual-trace + Stage 17 successor，非静默 e2e-proved）；CDC REPLAYABLE label+drain `e2e-proved`（`:185-187` + `:190-203`，positive_proof `TestDebeziumCdcSourceFunction#testSourceConsistencyIsReplayable` + `#testTruncateForDrainStopsSource`）；M7-2-P0-2 复验 `e2e-proved` FIXED（`TwoPhaseCommitSinkFunction.restoreFromEpoch():153-196` durable re-commit `:169-181` / non-durable abort `:183-193`，**environment_class: unit / required_lane: unit**（restoreFromEpoch 是组件级不变量），positive_proof `TestTwoPhaseCommitSinkFunction#testRestoreFromEpoch_durablePendingIsCommittedNotAborted` + `TestJdbcTwoPhaseCommitSinkDeep#testRestoreFromEpochMixedDurableAndNonDurable`，cross-ref Stage 19 formal owner）；tier-label validation `e2e-proved`（`StreamRequirementValidator.validateConnectorConsistency():84-114` + `ProcessingGuarantee.java:10-15`，positive_proof `TestConnectorConsistencyCapability#validateConnectorConsistency_debeziumPlusTwoPhaseCommit_passes` + `#validateConnectorConsistency_skipsWhenNotStrictExactlyOnce`，裁定 EFFECTIVELY_ONCE/AT_LEAST_ONCE tier 对本域 connector non-applicable）；external-system blocked `blocked`（PostgreSQL T5 + real-CDC T6 `@@LANE` blocked + Rule S5-1，embedded H2/mock 不得冒充）。冻结 CDC/backend/historical 矩阵（C1-C3,H1,T1,B1,B2,D1）。`TestDebeziumCdcSourceCompletion` `@Disabled`（genuinely broken run() loop）显式记录 + cross-ref Stage 17，不在本计划修复。

**Anti-Hollow 自检**：(a) 14 条 e2e-proved row 的 `positive_proof` 为真实 in-process/unit 实跑测试名（green `./mvnw test`：JDBC 32、file 12、Debezium 18，0 failures/0 errors；`TestDebeziumCdcSourceCompletion` 1 Skipped 即 @Disabled）；(b) `runtime_wiring: wired` 据 in-process 实跑裁定（saveState→pendingCommits→commit→data+ledger/ATOMIC_MOVE+manifest→pendingCommits.remove 确实连通）；(c) external-system PostgreSQL/real-CDC 如实 `blocked` + cross-ref T5/T6 + Rule S5-1（embedded/mock 非冒充）；(d) exactly-once 标签（TWO_PHASE_COMMIT/REPLAYABLE）裁定无虚标（ledger/manifest idempotency 真防 dup）；(e) CDC offset round-trip 路径连通（snapshotState CDC_OFFSETS_KEY→initializeState fromSerializable→setOffsets→engine resume）。无 dishonest classification / fabricated test / stale anchor。

**Source anchor 复核**：执行 agent 逐行核对关键 source（`JdbcTwoPhaseCommitSink:170-182,191-266,219-225,246-252,269-273,276-280,283-285,340-347` / `FileTwoPhaseCommitSink:109-111,130-142,150-197,174-181,207-213,237-256` / `DebeziumCdcSourceFunction:56,120-123,185-203,212-258,246-250` / `TwoPhaseCommitSinkFunction:63-64,153-196` / `StreamRequirementValidator:84-114` / `ProcessingGuarantee:10-15`，**注意 ProcessingGuarantee 现 4 值含 BEST_EFFORT，plan baseline 写 3 值/`:10-14` 已按 live `:10-15` 修正**）+ 7 个测试 class method 名/行号确认（JDBC Skeleton 19 + Deep 13、file Sink 12 + SourceCheckpointRestore 3、Debezium Checkpoint 7 + SourceFunction 10 + SourceCompletion @Disabled、core TwoPhaseCommitSinkFunction、batch ConnectorConsistencyCapability）。全部 16 行 source_anchor 使用 live 核实行号。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（Stage 16 文件本身解析到 16 EVID-S16 行，非空过）；`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` EXIT=0；`node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0（本 plan + 证据文件 0 broken link；5 个 broken link 全在 sibling `...0610-3-test-effectiveness...` plan，非本计划产物）。`./mvnw test -pl nop-stream-connector-jdbc,nop-stream-connector,nop-stream-connector-debezium` **BUILD SUCCESS**——surefire: JDBC Skeleton=19/Deep=13（32 总），file Sink=12/SourceCheckpointRestore=3，Debezium Checkpoint=7/SourceFunction=10/SourceCompletion=1 Skipped(@Disabled)，0 Failures/0 Errors。本审计无生产代码变更（audit-only），Closure Gates 显式声明 closure 依据 = evidence 校验器退出码 + in-process 实跑证据引用。roadmap work item 16 `planned → done`。

**Non-blocking follow-up / 指派**：(1) CDC fail-fast non-Map state rejection 测试覆盖 → Stage 17（test effectiveness）successor；(2) `TestDebeziumCdcSourceCompletion` `@Disabled`（genuinely broken run() loop）修复 → successor remediation plan 或 Stage 17；(3) provision PostgreSQL / real CDC source DB 后 external-system row（EVID-S16-016）从 `blocked` 升级 → connector successor plan（rerun T5/T6 lane）；(4) 独立 closure-audit（CLOSURE_VERIFY）由 dev-loop 后续步骤执行。Doc-sync: `No owner-doc update required`（证据文件是审计产出；不改 `docs-for-ai/`）。**审计未发现 confirmed live defect**（M7-2-P0-2 已 FIXED，sink-external-effect 视角复验通过）。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-0514-3-delta-streammodel-entry-audit.md`（3 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + Delta overlay 支持/拒绝矩阵，不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。吸收 Stage 7 deferral（"Delta overlay behavior itself is Stage 8"）+ 7 个未在 Delta 下 re-exercise 的 fail-fast branch。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-8-delta-streammodel-entry.evidence.md`（14 条 `@@EVIDENCE` 行 EVID-S8-001..014 + 头部 Delta Overlay Support/Reject Matrix + Fingerprint sensitivity/fail-fast under Delta frozen 文本）。

**Phase 1（Delta Overlay Form Inventory & Transform-Level Evidence）**：4 行（EVID-S8-001..004）。Form A 显式-path `e2e-proved`（`test-delta-extends.stream.xml:11` `x:extends` base → `DslModelParser` merge → 可执行 topology，positive_proof `TestStreamModelDeltaExtends#xExtendsDeltaProducesDifferentSinkOutput` execution base `["A","B","C"]` → delta `["A","C"]` drop B）；Form B `_delta/default/` 分层 `e2e-proved`（`_delta/default/.../test-delta-layered.stream.xml:13` `x:extends="super"` auto-activated by `DeltaResourceStoreBuilder:72-77`，positive_proof `#layeredDefaultDeltaProducesUppercaseOutput` execution lowercase → uppercase）；custom-layer non-default `residual-risk`（`DeltaResourceStoreBuilder:68-77` 静默忽略非-default layer，无 fail-fast throw——silent no-op by design，注明 deployment 误配风险，**不得标 fail-fast**）；`x:override` `non-goal`（grep all `.stream.xml` = 0 occurrences）。冻结 Delta overlay 支持/拒绝矩阵（Form A/B SUPPORTED in-process、custom-layer residual-risk、x:override non-goal）。

**Phase 2（Fingerprint Sensitivity & Fail-Fast Under Delta Evidence）**：5 行（EVID-S8-005..009）。fingerprint transform-level sensitive `e2e-proved`（`computeFingerprint():60-86` hash DAG topology sorted transform-id keys → `dagTopologyHash`，positive_proof `TestStreamModelDeltaFingerprint#transformLevelDeltaProducesDifferentFingerprint` assertNotEquals，**model-level unit 属性 → environment_class: unit / required_lane: unit，unit >= unit 成立**）；fingerprint config-only invariant `e2e-proved`（config-only delta interval 60000→30000 不改 transform-id set，positive_proof `#configOnlyDeltaPreservesFingerprintByDesign` assertEquals by-design）；`<streams>` fail-fast under Delta `fail-fast`（delta-introduced `<streams>` 仍抛 UnsupportedOperationException，positive_proof `TestStreamModelDeltaFailFast#deltaAddingStreamsRegistryStillFailsFast`，证明 delta merge 不 bypass fail-fast）；7 untested-under-Delta registry `residual-risk`（sideInputs/environments/schemas/coders/requirements/checkpointParticipants/onStart-onEnd-onError 仅 non-Delta proven，仅 `<streams>` 在 Delta 下 re-tested → Stage 17 successor）；delta-introduced supported-node matrix `e2e-proved`（source/map/filter/sink under Delta，无 cep/window/aggregate Delta fixture → M7-2-P2-1 cross-ref）。

**Phase 3（Historical Finding Revalidation + Delta-vs-Non-Delta Equivalence + Coverage Gaps）**：5 行（EVID-S8-010..014）。M7-2-P2-1（flow pom deps cep）`residual-risk` LIVE（`pom.xml:22` load-bearing for `<cep>`，→ Stage 23）；M7-2-P2-2（duplicate tree）`residual-risk` LIVE + **corpus anchor typo 标注**（finding-corpus.md:159 缺 `nop-stream-flow` module segment，correct path 标注；`_gen/` generated 不 inflate denominator）；M7-2-P2-21（README doc-resolved）`residual-risk`（README:18 现正确 acknowledge cep+xdefs，pom-fact 仍 TRUE → Stage 23）；Delta-vs-non-Delta equivalence `residual-risk`（supported nodes e2e-proved under Delta，cep/window/aggregate gap 如实标 residual-risk）；Stage 7 EVID-S7-028 Delta 影响 `residual-risk`（main/ 无 production loader 对 Delta 入口同样适用，Delta 入口仅 test-invokable → production loader remediation）。冻结最终 Delta 支持/拒绝矩阵。

**Anti-Hollow 自检**：(a) Form A/B row 的 `positive_proof` 为真实 in-process 实跑 execution 测试（`env.execute()` → sink output 改变，非仅 parse 成功/metadata 断言）；(b) `runtime_wiring: wired` 据 in-process 实跑裁定（`x:extends` → `DslModelParser` merge → `StreamModelDslBuilder.of(model).build()` → `env.execute()` 确实连通）；(c) 7 fail-fast gap 如实标 `residual-risk` + Stage 17 successor（非静默当 e2e-proved）；(d) custom-layer silent no-op 标 `residual-risk`（非 fail-fast，因 DeltaResourceStoreBuilder 静默忽略不抛异常）；(e) corpus anchor typo（M7-2-P2-2）显式标注（非静默忽略）。无 dishonest classification / fabricated test / stale anchor。

**Source anchor 复核**：执行 agent 逐行核对关键 source（`stream.xdef:22` `xdef:support-extends="true"` / 4 个 Delta fixture `:11`/`:13`/`:15`/`:10` / `computeFingerprint():60-86` / `DeltaResourceStoreBuilder:68-77` / `failFastOnUnsupportedRegistries():156-189` 8 registry / `pom.xml:22` / `README.md:18`）+ grep `x:override` = 0 occurrences + 3 个 Delta test class 8 @Test method 名/行号确认 + flow/model 30 hand-authored + 30 `_gen/` 确认。全部 14 行 source_anchor 使用 live 核实行号。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 14 EVID-S8 行 / 共 197 行，非空过——`[PASS] evidence` 非 "0 evidence rows yet"）；`all --strict` EXIT=0（manifest+corpus+evidence+qualification 全 PASS）。`./mvnw test -pl nop-stream -am -Dtest='TestStreamModelDeltaExtends,TestStreamModelDeltaFingerprint,TestStreamModelDeltaFailFast' -Dsurefire.failIfNoSpecifiedTests=false` **BUILD SUCCESS**——surefire reports: 5+2+1 = 8 tests run, 0 Failures, 0 Errors, 0 Skipped（所有 evidence 引用测试实跑 PASS）。本审计无生产代码变更（audit-only），Closure Gates 显式声明 closure 依据 = evidence 校验器退出码 + in-process 实跑证据引用。`check-plan-checklist.mjs --strict` EXIT=0。roadmap work item 8 `planned → done`。

**Non-blocking follow-up / 指派**：(1) 7 fail-fast registry Delta re-exercise（sideInputs/environments/schemas/coders/requirements/checkpointParticipants/onStart-onEnd-onError）→ Stage 17（test effectiveness）；(2) no-production-loader for `.stream.xml`（EVID-S7-028 / EVID-S8-014，Delta 入口同样仅 test-invokable）→ production loader remediation plan；(3) delta-introduced `<cep>`/`<window>`/`<aggregate>` fixture（M7-2-P2-1 cross-ref）→ successor test-coverage plan；(4) M7-2-P2-1/M7-2-P2-21 doc-contract → Stage 23；(5) M7-2-P2-2 corpus anchor typo → finding-corpus.md 维护（recorded, not fixed in this audit-only plan）。Doc-sync: `No owner-doc update required`（证据文件是审计产出；不改 `docs-for-ai/`）。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-0514-2-cep-nfa-sharedbuffer-audit.md`（4 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + 支持/拒绝矩阵（CEP Entry CEP1–CEP4 + Branching CEP5 + SharedBuffer/Skip/Timeout CEP6–CEP9 + Dangling/Checkpoint/Watermark/Serializable CEP10–CEP14 + Fraud-Example/Gap CEP-FRAUD/CEP-GAP），不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-12-cep-nfa-sharedbuffer.evidence.md`（22 条 `@@EVIDENCE` 行 EVID-S12-001..022 + 头部 16 行 CEP Support/Reject Matrix）。

**Phase 1（CEP 公共入口 & NFA 线性匹配）**：4 行（EVID-S12-001..004）。keyed entry `e2e-proved`（`CEP.pattern():38` → `PatternStreamBuilder.build():146-160` keyed transform `"CepOperator"`，positive_proof `TestCepPublicApiE2E#testCepPatternCreationFromKeyedStream`）；non-keyed entry `e2e-proved`（M7-2-P0-1 FIXED，`SingleOutputStreamOperatorImpl.forceNonParallel():52-58` 现调 `lockParallelismToOne()` 不再抛，positive_proof `TestCepNonKeyedEntryE2E#cepPatternOnNonKeyedStreamBuildsWithoutThrowing` + `#cepPatternOnNonKeyedStreamProducesMatches`）；NFA linear matching `e2e-proved`（`NFA.process():236-251`→`doProcess():356-433`，positive_proof `TestNFA#testSimplePatternMatch`）；NFA state transition TAKE/IGNORE/PROCEED `e2e-proved`（`computeNextStates():621-756` + `findFinalStateAfterProceed():780-806`，positive_proof `TestNFAExtended#testStrictContinuityWithResults`）。冻结 CEP1–CEP4 矩阵。

**Phase 2（Branching / Skip / Timeout / SharedBuffer Lifetime）**：5 行（EVID-S12-005..009）。NFA branching `e2e-proved`（`computeNextStates()` DeweyNumber versioning + `addComputationState():758-778` lockNode，positive_proof `TestNFAExtended#testFollowedByAnyBranchingWithSkipPastLastEvent:641` 3-way followedByAny branch + SharedBuffer bounded-growth assertion `:688`）；SharedBuffer release/lockstep（O8-2-AR-1）`e2e-proved` LIVE-FIXED（`releaseNode():258-308` parallel stacks `:259-263` + null-branch fix `:274-281` 现在在 null branch pop version，positive_proof `TestSharedBufferExtended#testReleaseNodePopsVersionOnNullEntry:307` null-branch regression）；Lockable refcount release（M7-2-P2-8）`e2e-proved` FIXED（`release():56-66` 现抛 `StreamRuntimeException:62` 非 bare `IllegalStateException`，positive_proof `TestLockableOverRelease#testOverReleaseDoesNotThrowBareIllegalStateException`）；skip 策略矩阵 `e2e-proved`（`AfterMatchSkipStrategy.prune():101-123` + `SkipPastLastStrategy.getPruningId():36-49`，positive_proof `TestCepSkipStrategyE2E` 8 方法覆盖 NoSkip/SkipPastLast/SkipToNext/SkipToFirst/SkipToLast + oneOrMore）；event-time/processing-time timeout `e2e-proved`（`NFA.advanceTime():265-346` + `CepOperator.processTimedOutSequences():718-732`，**caveat**：processing-time timeout 用 `TimerServiceImpl.currentProcessingTime()` 依赖 wall-clock 非 deterministic，positive_proof `TestCepOperatorTimeout#testTimeoutWithProcessingTime`）。

**Phase 3（Dangling 安全网 / Checkpoint Continuation / Serializable Risk）**：5 行（EVID-S12-010..014）。dangling 安全网（O8-2-AR-2）`residual-risk`（`onEventTime():540` / `onProcessingTime():600` predicate `size==1 && completedMatches.empty`，**size==1 in-process proven + size>1 branching gap 如实标 `residual-risk` + Stage 17 successor**，positive_proof `TestCepOperatorDanglingCleanup#testDanglingCleanupReleasesSharedBuffer`；**非静默当 e2e-proved**）；CEP checkpoint continuation `e2e-proved`（`snapshotState():419-426` + `restoreState():429-447` + `open():259` applyPendingRestoreState，positive_proof `TestCepCheckpointRestoreE2E#testE2ENfaStateSurvivesCheckpointRestore` 完整链路 snapshot mid-pattern→close→restore new CepOperator→continue→assert match completes）；watermark persistence `e2e-proved`（`processWatermark():450-459` + watermark state `:415`，positive_proof `TestCepOperatorWatermarkPersistence` 4 方法）；CEP 状态 non-Serializable（O8-2-AR-3）`residual-risk`（7 值类 NFAState/ComputationState/EventId/NodeId/SharedBufferNode/SharedBufferEdge/Lockable 均 non-Serializable，**non-blocking rationale**：当前 state backend 用平台 TypeSerializer 非 Java serialization，watch-only）；M7-2-P0-4 revalidation `e2e-proved` FIXED（`TestCepOperatorDanglingCleanup:114-122` 现 asserts `partialMatchesEmpty` + `operator.getPartialMatches().isEmpty()`）。

**Phase 4（历史 Finding Revalidation + Fraud-Example Scope + Coverage Gap）**：8 行（EVID-S12-015..022）。6 个历史 test-effectiveness finding 复验全标 `residual-risk` + successor（M7-2-P1-13 TestCepOperatorStateBackendWiring couples internal accessors → Stage 17/plan 2026-08-04-2300-3；M7-2-P1-14 TestAfterMatchSkipStrategies partial-fixed → real behavior proven by TestCepSkipStrategyE2E；M7-2-P2-17 TestSharedBuffer assertNotNull → Stage 17；M7-2-P2-18 TestNFAState mirror tests → Stage 17；M8-2-P2-20 TestWatermarkStateRobustness misleading class name → Stage 17；O8-2-AR-4 TestGeographicAnomalyPatternFix zero bug-catching power → Stage 17）；fraud-example scope `non-goal`（`fraud-detection.stream.xml:71-72` linear pattern + `FraudDetectionDemo.java:52` 不调 `env.execute()`，example module include rule 仅 fail-fast/semantic）；no-env-execute coverage gap `residual-risk`（nop-stream-cep 全部 "E2E" 测试为 operator-driven，无 `env.execute()` 级测试 → Stage 17）。冻结最终 CEP Support/Reject Matrix（16 行）。

**Anti-Hollow 自检**：(a) in-process row 的 `positive_proof` 全为真实实跑 `ClassName#method`（逐行 Read + surefire 确认 TestCepOperatorDanglingCleanup 3 测试 / TestCepCheckpointRestoreE2E 3 测试 / TestCepOperatorWatermarkPersistence 4 测试 / TestCepOperatorBasic 4 测试 / TestCepOperatorTimeout 2 测试 / TestCepSkipStrategyE2E 8 测试 / TestCepNonKeyedEntryE2E 2 测试 / TestSharedBufferExtended 9 测试 / TestNFA 5 测试均 PASS）；(b) `runtime_wiring=wired` 据 live 入口→operator→NFA→SharedBuffer→output 接线裁定；(c) dangling size>1 gap 标 `residual-risk` 非静默 e2e-proved；(d) non-Serializable risk 标 `residual-risk` + non-blocking rationale 非静默忽略；(e) checkpoint continuation 端到端路径连通（snapshot→restore→open→continue matching 经 `TestCepCheckpointRestoreE2E` 3 方法证）。无 dishonest classification / fabricated test / stale anchor。

**Source anchor 复核**：执行 agent 逐行核对关键 source（CEP.java:38 / PatternStreamBuilder.java:146-169 / CepOperator.java:415-447/450-459/509-629 / NFA.java:236-251/265-346/621-806 / SharedBufferAccessor.java:258-308 / Lockable.java:56-81 / AfterMatchSkipStrategy.java + SkipPastLastStrategy.java）+ 7 个 non-Serializable 值类 grep 确认（0 命中）+ 全部 test method 名/行号 grep + `@Test` 确认。所有 22 行 source_anchor 使用 live 核实行号。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 22 EVID-S12 行 / 共 174 行，非空过——self-test positive control green；dispositions: 12 e2e-proved / 9 residual-risk / 1 non-goal）。`./mvnw test -pl nop-stream/nop-stream-cep -am -T 1C` **BUILD SUCCESS**（292 tests, 0 failures, 0 errors）——所有 evidence 引用测试实跑 PASS。本审计无生产代码变更（audit-only），Closure Gates 显式声明 closure 依据 = evidence 校验器退出码 + in-process 实跑证据引用。roadmap work item 12 `planned → done`。

**Non-blocking follow-up / 指派**：(1) CepOperator dangling 安全网 size>1 branching 场景测试覆盖 → Stage 17（test effectiveness）；(2) no-`env.execute()` CEP 测试覆盖（linear + branching）→ Stage 17；(3) CEP 状态值类 non-Serializable（O8-2-AR-3）→ watch-only，若平台切换 Java serialization for state 则需 successor remediation plan；(4) O8-2-AR-4 / M7-2-P1-13/P1-14 / M7-2-P2-17/P2-18 / M8-2-P2-20 test-quality → Stage 17 / active remediation plan `2026-08-04-2300-3`。Doc-sync: `No owner-doc update required`（证据文件是审计产出；不改 `docs-for-ai/`）。

---

### nop-stream 独立审计 Stage 14：Data Plane 与多 JVM Recovery 审计（plan 执行）

- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-0514-1-data-plane-multi-jvm-recovery-audit.md`（4 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + 支持/拒绝矩阵（Data-Plane Transport D1–D8 + Wire-Codec Backend W1–W4/DD + Multi-JVM Recovery M1–M6），不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-14-data-plane-multi-jvm-recovery.evidence.md`（21 条 `@@EVIDENCE` 行 EVID-S14-001..021 + 头部 3 个矩阵）。吸收 Stage 13 deferred：cross-JVM fencing（M8-2-P0-1）/ zombie（M8-2-P1-6）residual 复验。

**Phase 1（Data-Plane Transport Record/Barrier/Watermark/Fencing/EOS/Heartbeat）**：6 行（EVID-S14-001..006），全部 `e2e-proved` / `runtime_wiring: wired` / `required_lane: in-process` / `environment_class: in-process`。record transport `TestRemoteDataExchange#testRemoteInputChannelReceivesRecords`（RemoteResultPartition.write:153→encode:163 stamp epochId→send:165 → RemoteInputChannel.onMessage:352→decode:402-406→queue.put:406）；barrier `#testRemoteBarrierExchange`（TYPE_CHECKPOINT_BARRIER:33）；watermark `#testRemoteWatermarkExchange`（TYPE_WATERMARK:34）；epoch fencing discard `#testRemoteInputChannelFencingRejectsStaleMessages`（onMessage:368 在 liveness refresh 前 discard stale-epoch envelope）+ rejection_proof `TestRemoteInputChannelHeartbeat#testWrongEpochHeartbeatDoesNotRefreshLiveness`（wrong-epoch heartbeat 不计 liveness）；EOS `#testRemoteInputChannelEndOfStream`（close:184-187 stamp CONTROL_END_OF_STREAM + onMessage:384-388 EOS branch）；heartbeat/liveness `#testProducerSendsIdleHeartbeatWhenIdle` + `#testConsumerTimesOutWhenSilent`（:252-255 emit + :379 liveness refresh after fence）。

**Phase 2（Wire-Codec Backend Matrix + Deployment-Descriptor Reconstruction）**：5 行（EVID-S14-007..011）。Identity `e2e-proved`（IdentityWireCodec:20 + beans.xml:76）；SysDao `e2e-proved`（SysDaoWireCodec:36 ApiRequest{data:map}，positive_proof `TestDataPlaneSysDaoBackendE2E#recordsTraverseNopSysEventTableExactlyOnce` 4 方法，always-on `@NopTestConfig(localDb=true)` 无 gate——注意此测试在 nop-sys-dao 非 nop-stream）；Kafka/Pulsar **`blocked`**（environment_class: none，gated T3/T4，Rule S5-1 skipped gated test 非 evidence——positive_proof 引用 gated 测试名仅作 rerun target，cross-ref T3/T4 @@LANE + rerun_condition，非静默 e2e-proved）；deployment-descriptor `e2e-proved`（TaskDeploymentDescriptor:51 serializable JobGraph/DeploymentPlan/fencingEpoch，coordinator-built `RemoteGraphExecutionPlanBuilder.buildRemoteOnly:81` vs TaskManager-local rebuild `SubtaskPlanBuilder:61/72-94` 双侧经 `StreamTopicNaming.buildTopic:115` deterministic 接线一致，positive_proof `TestJobCoordinatorRemoteDeploy#remoteDeployModeCallsDeployTaskNotReceiveAssignment` 调 setRemoteDeployMode(true)）。冻结 Wire-Codec Backend Support/Reject Matrix W1–W4+DD。

**Phase 3（T2 Multi-JVM Recovery + T2 Defect Disposition）**：6 行（EVID-S14-012..017）。T2 INFRASTRUCTURE `e2e-proved`（environment_class: multi-jvm，`TestMiniStreamClusterProcessSpawn` 3/3 PASS，real child-JVM spawn + TM registration + coordinator boot；MiniStreamCluster:78/134/137-138/389/404/310）；2 个 T2 deeper defect **`blocked`**——EVID-S14-013 `TestMultiJvmExactlyOnceRecovery:111` 读 `logFileFor("coordinator")` 缺 `-0` 后缀 → NoSuchFileException（MiniStreamCluster:404 写 `"coordinator-"+index`），EVID-S14-014 `TestMultiJvmCoordinatorFailover:129` "coordinator-1 must take over" assertion fails（HA-fencing takeover gap），均 cross-ref Stage 5 T2 @@LANE + Stage 13 EVID-S13-015/016；embedded-vs-multi-JVM boundary `residual-risk`（EVID-S14-015）；cross-JVM fencing residual M8-2-P0-1 `residual-risk`（EVID-S14-016，required_lane: multi-jvm，cross-ref EVID-S13-012/017）；cross-JVM zombie residual M8-2-P1-6 `residual-risk`（EVID-S14-017，cross-ref EVID-S13-013/019）。冻结 Multi-JVM Recovery Matrix M1–M6。

**Phase 4（Unaligned In-Flight Capture + Buffer-Pool Boundary + Historical Finding Revalidation）**：4 行（EVID-S14-018..021）。unaligned in-flight capture `e2e-proved`（RemoteInputChannel.captureInFlightData:277 drain queue preserve EOS + injectElements:298 re-inject at front，positive_proof `TestUnalignedCheckpointBackpressure#testNoDuplicatesRecordsBeforeBarrierNotReplayed`——注意此测试在 `checkpoint/` 包非 `transport/`，方法行 :153）；buffer-pool boundary `e2e-proved`（G53 by-design，RemoteResultPartition/RemoteInputChannel bypass per-job IBufferPool，cross-JVM bound = IMessageService backend，`TestBufferPoolRemoteExclusion` :33/:72）；M8-2-P0-1 cross-JVM distributed mutex revalidation `residual-risk`（EVID-S14-020，JobCoordinator.globalRecovery:980-1004 fencing-epoch-before-lock + late-arrival guard，in-process serialization 证，cross-JVM mutex 需 multi-jvm lane）；M8-2-P1-6 cross-JVM zombie revalidation `residual-risk`（EVID-S14-021，SupervisionLoop.waitForTerminal:492-503 fail-loud，cross-JVM stale-attempt zombie fencing 需 multi-jvm lane）。冻结 Data-Plane Transport Capability Matrix D1–D8。

**Anti-Hollow 自检（独立 closure-audit subagent fresh session 验证，VERDICT: APPROVED）**：(a) Phase-1 transport positive_proof 全为真实 in-process 实跑 `ClassName#method`（逐行 Read 确认 TestRemoteDataExchange 6 方法 + TestRemoteInputChannelHeartbeat 4 方法存在于声称行号）；(b) SysDao row 引用 always-on nop-sys-dao 测试（@NopTestConfig(localDb=true) 无 gate）；(c) Kafka/Pulsar gated-backend 标 `blocked`+`environment_class: none` 非 e2e-proved（gate 注解确认有效）；(d) deployment-descriptor row 引用真实 setRemoteDeployMode(true) 测试；(e) T2 defect 标 `blocked`（log-label mismatch + HA-fencing takeover defect 逐行确认），cross-JVM residual 标 `residual-risk`+`required_lane: multi-jvm` 非静默 e2e-proved；(f) unaligned/buffer-pool 引用真实测试（注意 checkpoint/ 包路径）；(g) finding_id M8-2-P0-1/M8-2-P1-6 均为 corpus 注册 ID（corpus line 67/74）。无 dishonest classification / fabricated test / stale anchor。

**Source anchor 复核**：1 个 explore subagent（fresh session）逐行核对 15 个 source class + 全部 test method 名/行号 + gate 注解。发现并修正 2 处 plan baseline 路径不准确：(1) `TestDataPlaneSysDaoBackendE2E` 实际在 `nop-sys/nop-sys-dao/.../message/`（非 nop-stream-runtime/transport，plan baseline 已正确指向 nop-sys-dao）；(2) `TestUnalignedCheckpointBackpressure` 实际在 `checkpoint/` 包（非 transport），class decl :42 而方法 `testNoDuplicatesRecordsBeforeBarrierNotReplayed` 在 :153（plan baseline :153 指方法行，evidence row 正确使用方法行）。`TestJobCoordinatorRemoteDeploy`（plan baseline 暂定引用）经确认存在于 `coordinator/` 包 :60 调 setRemoteDeployMode(true)，用于 deployment-descriptor row。所有 21 行 source_anchor 使用 live 核实行号。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 21 EVID-S14 行，非空过——dispositions: 12 e2e-proved / 4 blocked / 5 residual-risk）。`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` EXIT=0（无未勾选项 + Closure Evidence 已填）。本审计无生产代码变更，Closure Gates 显式声明 `./mvnw` 不强制，closure 依据 = evidence 校验器退出码 + in-process 实跑证据引用 + 独立 closure-audit APPROVED。roadmap work item 14 `planned → done`。

**Non-blocking follow-up / 指派**：(1) T2 lane deeper defect 修复（TestMultiJvmExactlyOnceRecovery log-label `:111`、TestMultiJvmCoordinatorFailover HA-fencing `:129`）→ independent remediation plan（修复后 EVID-S14-013/014 可重审 e2e-proved）；(2) cross-JVM fencing epoch final revalidation（M8-2-P0-1 distributed mutex）→ remediation plan after T2 defect fixed；(3) cross-JVM zombie task fencing（M8-2-P1-6）→ remediation plan after T2 defect fixed；(4) Kafka/Pulsar real-backend evidence → provision broker（T3/T4 rerun_condition）then re-run。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-1835-3-window-watermark-timer-audit.md`（4 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + window assigner × trigger 结果正确性矩阵 + dormant facility 分类，不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-11-window-watermark-timer.evidence.md`（23 条 `@@EVIDENCE` 行 EVID-S11-001..023 + 头部 Window Assigner × Trigger Result Correctness Matrix WM1–WM6）。

**Phase 1（Window Assigner × Trigger Result Correctness Matrix）**：5 行（EVID-S11-001..005）。tumbling event-time `e2e-proved`（`TestEventTimeWindowE2E#testEventTimeWindowPipeline`）；sliding event-time `e2e-proved`（`TestSlidingEventTimeWindows#testWindowBoundaryAssignment` assigner + 共享 regular-window operator path）；processing-time `e2e-proved`（`TestProcessingTimeWindowIntegration#testProcessingTimeWindowPipeline`）**诚实标注** `HeapInternalTimerService.currentProcessingTime():52` returns `System.currentTimeMillis()` non-deterministic；count trigger `e2e-proved`（`TestWindowEndToEnd#testCountWindowFires` GlobalWindow+CountTrigger）+ **标注** `TestCountTrigger` 15-line stub only tests `canMerge()==false`（finding_id M7-2-P2-9）；global window `e2e-proved`（NeverTrigger:52 always CONTINUE，需显式 trigger）。冻结 WM1–WM6 矩阵。

**Phase 2（Session Merge, AccumulationMode, Pane & Evictor）**：6 行（EVID-S11-006..011）。session merge `e2e-proved`（`TestSessionWindowAdvancedMerge#testLateElementCausesTwoEstablishedSessionsToMerge` late-element-causes-merge + `MergingWindowSet.addWindow:162` + `mergeWindowContents:1336-1480` 3 storage paths + merge guards `:623-639`）；AccumulationMode DISCARDING/ACCUMULATING `e2e-proved`；ACCUMULATING_AND_RETRACTING `fail-fast`（`open():389-395` throws `ERR_STREAM_UNSUPPORTED`，`rejection_proof: TestPaneInfoAndAccumulationMode#testRetractingModeFailsFastOnOpen`）；pane tracking `residual-risk`（`computePaneInfo:903-928` isLast hardcoded false + `snapshotPaneTracking:948` **only TimeWindow-scoped** 非覆盖 gap 标注）；evictor transient-per-fire（G46）`e2e-proved`（`TestEvictorIntegration#testEvictionIsTransientPerFireOnProductionListStatePath` eviction on local ArrayList never writes back）；late-data `e2e-proved`（`TestWindowOperatorIntegration#testLateDataOutputTag` sideOutput path + `#testLateDataDropped` silently-dropped path 双向覆盖）。

**Phase 3（Timer Checkpoint/Restore & Watermark Propagation）**：4 行（EVID-S11-012..015）。timer checkpoint/restore G2 deferred-application `e2e-proved`（`TestTimerCheckpointRestoreE2E#testTimerSurvivesCheckpointAndFiresAfterRestore` 完整链路 register→snapshot→restore→open→processWatermark→fire；`snapshotState:513` 3 keys trigger-accumulators/internal-timers/pane-tracking + `restoreState:545` defer + `open():408-416/461-464` apply + `HeapInternalTimerService.restoreTimers:217-234` bypass currentKeySupplier critical 设计标注）；watermark generation `e2e-proved`（`TestPeriodicWatermarkAdvancement` + `BoundedOutOfOrdernessWatermarks:67/72`）；watermark propagation `e2e-proved`（`TestWatermarkPropagation` + `WindowOperator.processWatermark:494-496` advanceWatermark before super → timers fire inline + monotonic passthrough `:131-137`）；watermark idleness `e2e-proved`（`TestWatermarkIdleDetection`）。

**Phase 4（Dormant Facility Classification, Design Drift & Historical Finding Revalidation）**：8 行（EVID-S11-016..023）。2-input watermark valve `non-goal`（M7-2-P1-12 revalidation，zero `TwoInputStreamOperator`，G47 dormant，`runtime_wiring: unwired`）；reserved triggers/evictors `non-goal`（ContinuousEventTimeTrigger/ContinuousProcessingTimeTrigger/DeltaTrigger/ProcessingTimeoutTrigger/TimeEvictor/DeltaEvictor all `@Internal "API 预留"`）；WatermarksWithWatermarkAlignment + WatermarkOutputMultiplexer `residual-risk`（Coordinator not implemented）；WindowingStrategy design-only fields `residual-risk`（closingBehavior/onTimeBehavior/outputTime absent from live DataBean）；design drift `residual-risk`（SessionEventTimeWindows vs EventTimeSessionWindows naming + TimestampsAndWatermarksOperator core vs runtime，M7-2-P1-16 → Stage 23）；3 条 pure historical revalidation：M7-2-P2-6 performative type safety（`WindowedStreamImpl:194-240` always `Object.class`，`residual-risk`）、M7-2-P2-9 CountTrigger boundary gap（`residual-risk`，active plan 2026-08-04-2300-3）、M7-2-P2-16 WindowOperatorBasic geometry only（`residual-risk`）。共 5 个历史 finding 复验（M7-2-P1-12/P1-16/P2-6/P2-9/P2-16）。

**Anti-Hollow 自检**：(a) window result row positive_proof 全为真实 in-process 实跑 `ClassName#method`（tumbling `TestEventTimeWindowE2E#testEventTimeWindowPipeline` 634 行、processing-time `TestProcessingTimeWindowIntegration` 344 行、session `TestSessionWindowAdvancedMerge` 142 行），非组件 unit 充数；(b) timer checkpoint/restore row `TestTimerCheckpointRestoreE2E#testTimerSurvivesCheckpointAndFiresAfterRestore` 验证 restore→open→fire 完整链路（非仅 snapshot round-trip）；(c) `runtime_wiring=wired` 据 live operator path 裁定；(d) dormant facility 标 `non-goal`/`residual-risk`，无静默 `e2e-proved`；(e) processing-time non-determinism（EVID-S11-003）+ CountTrigger boundary gap（EVID-S11-022）如实标注。

**Source anchor 复核**：4 个并行 explore subagent（fresh sessions）逐行核对 windowing/triggers/evictors/assigners、WindowOperator（2026 行）+ MergingWindowSet、HeapInternalTimerService/TimestampsAndWatermarksOperator/AbstractStreamOperator/watermark classes/PaneInfo/Timer SPI/WindowingStrategy、test class & method 名。发现并修正 1 处 plan baseline 不准确：`CountTrigger` 实际用 `SimpleAccumulator<Long>`（非 baseline 所写 `SimpleAccumulator<LongCounter>`）；PaneInfo `isLast` 在 WindowOperator:928 确实硬编码 false（plan baseline 正确）。所有 23 行 source_anchor 使用 live 核实行号。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 23 EVID-S11 行，非空过）。`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` EXIT=0（无未勾选项 + Closure Evidence 已写入）。本审计无生产代码变更，Closure Gates 显式声明 `./mvnw` 不强制，closure 依据 = evidence 校验器退出码 + in-process 实跑证据引用。roadmap work item 11 `planned → done`。

**Non-blocking follow-up / 指派**：(1) TestCountTrigger onElement boundary gap（M7-2-P2-9/M8-2-P2-23）→ active plan `2026-08-04-2300-3`；(2) 2-input watermark valve real input-count sourcing → successor two-input-operator feature plan；(3) WatermarksWithWatermarkAlignment Coordinator → future feature plan；(4) WindowingStrategy closingBehavior/onTimeBehavior/outputTime → future feature plan；(5) design drift（SessionEventTimeWindows naming、TimestampsAndWatermarksOperator module location）→ Stage 23；(6) processing-time timer deterministic clock injection → future improvement。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-1835-2-state-backend-savepoint-rescale-audit.md`（4 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + state-type×backend 矩阵 + rescale 组合矩阵，不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-10-state-backend-savepoint-rescale.evidence.md`（22 条 `@@EVIDENCE` 行 EVID-S10-001..022 + 头部 State-Type × Backend Matrix ST1–ST4 + Rescale Combination Matrix R1–R6）。

**Phase 1（State-Type × Backend Matrix & Full Snapshot/Restore）**：4 行（EVID-S10-001..004），全部 `e2e-proved` / `required_lane: in-process`。Memory 全 8 state type（ValueState/MapState/ListState/ReducingState/AggregatingState + Internal overloads）`TestMemoryKeyedStateBackendSnapshotRestore`；RocksDB 全 8 state type `TestRocksDBStateTypes#testValueStatePersistsAcrossReopen`（snapshot→reopen→restore→value assertion）；operator state 4 redistribution mode（NONE/UNION/BROADCAST/SPLIT_DISTRIBUTE）`TestMemoryOperatorStateBackend#testSplitDistributeRoundRobin`；key-layout v2（big-endian key-group prefix）`TestRocksDBKeyGroupPrefixLayout#newLayoutUsedAtRuntimeAndRoundTripsValueState`。冻结 State-Type × Backend Matrix ST1–ST4。

**Phase 2（Incremental Checkpoint, Key-Layout Fail-Fast & Ref-Count Integrity）**：4 行（EVID-S10-005..008）。incremental lifecycle（snapshotIncremental→SST content-address→restoreRangeInto range scan）`e2e-proved`（`TestRocksDBIncrementalRangeRestore#incrementalRangeRestoreKeepsOnlyOwnedKeys` + fail-fast `#incrementalRestoreWithoutSegmentStoreFailsFast`）；key-layout fail-fast（M8-2-P1-1 RESOLVED——`restoreState:779/791` 调 `verifyKeyLayoutVersion(strict=true)` 于 typed-marker 与 map-marker 两分支，restoreIncremental 前）`e2e-proved`（`TestRocksDBIncrementalRestoreFailFast` typed+map marker legacy rejection + `TestRocksDBKeyGroupPrefixLayout#incrementalRestoreRejectsOldLayoutVersion`）；ref-count integrity（M8-2-P1-2 PARTIAL——rollback `releaseIncrementalSegments:604/619` 有测试，complete SST ref-count integrity under real RocksDB materialization 标 `residual-risk` 归 active plan 2026-08-04-2300-2）；SST content-addressed sharing `e2e-proved`（`TestRocksDBIncrementalSnapshotStrategy#deterministicDedupAcrossCheckpointsWithNoChanges`）。

**Phase 3（Schema Migration, Savepoint & TTL）**：6 行（EVID-S10-009..014）。schema migration（match `TestStateMigrationEndToEnd#rocksdbIntegerToLongMigrationFullRoundTrip` + no-match fail-fast `#rocksdbNoMigrationFailsFast` throws `ERR_STREAM_STATE_SCHEMA_MISMATCH`）`e2e-proved`；savepoint save/load/restore（triggerSavepoint→storeSavepoint→loadSavepoint→restore）`e2e-proved`（`TestSavepointEndToEnd#testGraphModelExecuteWithSavepointRestoresState`）；savepoint vertex-differential（M7-2-P0-7 RESOLVED vertex-level——`validateReverseVertexDifferential:1404-1453` reject deleted stateful vertex）`e2e-proved`（`TestSavepointVertexSetDifferential#reverse_deletedVertexInCheckpointRejectsRestore`）；TTL eviction 三路径（lazy/snapshot-exclusion/background-sweep）`e2e-proved`（`TestRocksDBStateTtl#listStateAndSweep` + `#snapshotExcludesExpiredEntries`）；schema fingerprint（M7-2-P0-5 RESOLVED——`TestStateSchemaFingerprintEndToEnd` + no-migration fail-fast control）`e2e-proved`；migration crash-recovery + accumulator risk `residual-risk`（no migration-in-progress marker by design + accumulator migration correctness user-function-dependent per design §6.3）。

**Phase 4（Rescale Combination Matrix, Reshard & Historical Finding Revalidation）**：6 rescale 行（EVID-S10-015..020）+ 历史 finding 复验（EVID-S10-006 M8-2-P1-1、EVID-S10-007 M8-2-P1-2、EVID-S10-011 M7-2-P0-7、EVID-S10-013/021 M7-2-P0-5、EVID-S10-022 M7-2-P0-8）。冻结 Rescale Combination Matrix R1–R6：R1 parallelism+aligned `e2e-proved`（`TestKeyGroupRescaleDispatchE2E#scaleUp_parallelism_4_to_16`）；R3 parallelism+channel state `fail-fast`（`assertNoChannelStateOnRescale:1232-1248` throws `ERR_STREAM_CHANNEL_STATE_RESCALE_UNSUPPORTED`，`TestChannelStateRescaleE2E#unalignedCheckpointThenRescale_failsFast`）；R5 same-parallelism 1:1 strict lookup `e2e-proved`；R6 maxParallelism reshard（Stage 37 offline tool）`e2e-proved`（`TestMaxParallelismReshardMigrationE2E#reshardUp128to256_conservesKeysAndRestoresUnderNewMaxParallelism_memory` + old==new fail-fast `#oldEqualsNewMaxParallelism_failsFast`）。

**关键审查点（plan Round-1 B1 INVESTIGATE）裁定结果——发现 confirmed live defect**：R2 parallelism-only rescale + incremental checkpoint。Live code 追踪确认 `buildRescaledTaskState:1286-1289` keyed-merge 走 `toStateDataMap(...).get("states")`，但 incremental checkpoint 把 keyed state 存在 `IncrementalSnapshotResult.MARKER_KEY`（`"__incremental_checkpoint__"`）而非 `"states"`，故 guard `if (!(statesObj instanceof Map)) continue;` **静默丢弃 incremental keyed state**，cross-subtask rescale-merge 后新 subtask 拿到空 keyed state。对比：单 backend partial-range restore（targetKeyGroupRange + restoreRangeInto）正确处理 incremental；offline `MaxParallelismReshardMigration.reshardCheckpoint:158-163` 对同形状 fail-fast（无静默丢弃），证明 executor 的 `continue` 是 bug。EVID-S10-019 `disposition: residual-risk`（NOT e2e-proved，NOT 预判 SUPPORTED）。R4 maxParallelism-at-restore `residual-risk`（documented constraint，`resolveMaxParallelism:1174-1193` 无 runtime fail-fast guard，违反则 key 静默错路由）。两 defect 均按 roadmap 规则指派 successor remediation plan（evidence-only scope，不在本 plan 修复）。

**Anti-Hollow 自检**：(a) snapshot/restore row positive_proof 全为 in-process 实跑 `ClassName#method`；(b) `e2e-proved` row 全部引用真实测试方法（逐文件 grep 确认）；(c) `wired` row 据 live source anchor；(d) EVID-S10-007 ref-count residual 未被静默当作 e2e-proved；(e) R3 REJECTED 有 rejection_proof；(f) R2 parallelism+incremental 经 live 追踪裁定为 residual-risk（confirmed defect），未预判 SUPPORTED；(g) R4 maxParallelism-at-restore 标 residual-risk（NOT fail-fast）。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 22 EVID-S10 行，非空过——非 "0 evidence rows yet" 分支）。本审计无生产代码变更，Closure Gates 显式声明 `./mvnw` 不强制，closure 依据 = evidence 校验器退出码 + in-process 实跑证据引用。roadmap work item 10 `planned → done`。

**Non-blocking follow-up / 指派**：(1) EVID-S10-019 confirmed defect（buildRescaledTaskState 静默丢弃 incremental keyed state）→ successor remediation plan（修复方案：仿 MaxParallelismReshardMigration 对 incremental marker fail-fast，或实现 cross-subtask incremental keyed merge）；(2) M8-2-P1-2 complete SST ref-count integrity → active plan 2026-08-04-2300-2；(3) M8-2-P2-1/2/5/6/7 RocksDB lifecycle/cleanup → active plan 2026-08-04-2300-2；(4) savepoint operatorId-level differential（P0-7 现 vertex-level）→ successor feature plan；(5) `schemaVersion` version-based branching（dormant at 1）→ future feature。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-1835-1-control-plane-ha-fencing-audit.md`（4 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + 支持/拒绝矩阵，不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-13-control-plane-ha-fencing.evidence.md`（21 条 `@@EVIDENCE` 行 EVID-S13-001..021 + 头部 Control-Plane Capability Matrix C1–C6 + Fencing Support/Reject Matrix F1–F6）。

**Phase 1（Control-Plane RPC Entry-to-Effect）**：5 行（EVID-S13-001..005），全部 `e2e-proved` / `runtime_wiring: wired` / `required_lane: in-process`。assignment dispatch positive_proof `TestRpcDistributedExecutorE2E#fullPipelineRunsOverRpcControlPlane`（in-process 跨 RPC control-plane 端到端）；checkpoint trigger `TestJobCoordinator#testTriggerCheckpointSendsBarrier`（task-side fencing throw `TestFencingTokenRejection#staleTokenCheckpointTriggerThrowsFencingMismatch`）；failover/recovery `TestJobCoordinator#testGlobalRecoveryGeneratesNewToken`；termination G23 四模式 `testTerminateCancel`；distributed abort `TestCheckpointAbortWiring#testStuckChannelAbortTerminatesJob`。

**Phase 2（Fencing Stale-Rejection + Leader Election Transition）**：5 行（EVID-S13-006..010）。stale-leader rejection（coordinator side，`collectAck:754-764`/`reportTaskStatus:811-816`/`reportNodeTaskLiveness:872-877`）`e2e-proved`；stale-token rejection（task side，P0-6 hardening，`finding_id: M7-2-P0-6`）`e2e-proved`；encoding dominance（`EPOCH_SCALE:110` + `deriveHaFencingEpoch:1155`，single monotonic long，覆盖 same-leader prior-recovery invariant F5）`e2e-proved`；data-plane discard（`RemoteInputChannel.onMessage:368-372`）`e2e-proved`；leader election transition（`CoordinatorElectionListener:1164`→`activateAsLeader:1192`→`deactivateToStandby:1242`，standby→active→standby→re-active）`e2e-proved`。头部冻结 Fencing Support/Reject Matrix（F1–F6）。

**Phase 3（G32 Failover-Safe Rebuild + Concurrent Recovery Serialization + Zombie Task Fencing）**：4 行（EVID-S13-011..014）。G32 rebuild（`rotateFencingEpochCoreLocked:1081-1138`，storage failure fail-loud + same-leader skip）`e2e-proved`（`TestJobCoordinatorFailoverRestore` 4 方法 + JDBC HA integration）；concurrent recovery serialization（M8-2-P0-1，`globalRecovery:980-1004` fencing-epoch-before-lock + late-arrival guard）`residual-risk`（in-process serialization 证，cross-JVM mutex 标 `required_lane: multi-jvm`）；zombie task fencing（M8-2-P1-6，`SupervisionLoop.waitForTerminal:492-503` fail-loud）`residual-risk`；fencing-epoch-before-lock ordering invariant `e2e-proved`（`required_lane: in-process`，单 JVM 可观察属性，distinct from cross-JVM mutex residual）。

**Phase 4（T2 Defects blocked + Historical Finding Revalidation + Recovery Boundary）**：7 行。2 条 T2 defect `blocked`（EVID-S13-015 `TestMultiJvmExactlyOnceRecovery:111` log-label mismatch；EVID-S13-016 `TestMultiJvmCoordinatorFailover:129` HA-fencing takeover failure），均 `environment_class: none` + `required_lane: multi-jvm` + cross-ref Stage 5 T2 `@@LANE` note，未静默放行。4 条 historical finding revalidation：M8-2-P0-1（`residual-risk`，cross-JVM owned by Stage 14）、M8-2-P1-4 permit leak（`e2e-proved`，`TestTaskManager#testRedeployToOccupiedSlotDoesNotLeakPermit` + `testDuplicateAssignmentDoesNotLeakSemaphore`）、M8-2-P1-6 zombie（`residual-risk`）、M7-2-P0-6 fencing ZERO tests（`e2e-proved`，now covered by `TestFencingTokenRejection` + `TestFencingEpochUnification`）。1 条 local-vs-distributed recovery boundary（`residual-risk`）。

**Anti-Hollow + 独立 closure audit**：独立 closure audit（fresh explore subagent，task `ses_0228ba822ffelvjwQ1QZl2OY0y`，fresh session）9 项检查全 PASS：(a) 14 条 `e2e-proved` positive_proof 全部为真实 in-process 实跑 `ClassName#method`（独立读测试文件确认）；(b) `wired` 抽查 3 row 测试体确 traverses coordinator→RPC→task / 两-coordinator leader 切换 / storage-restore；(c) T2 defect 标 `blocked`；(d) 7 条 cross-JVM `required_lane: multi-jvm` row 均 `residual-risk`/`blocked`，无静默 `e2e-proved`；(e) **25 个抽查 source anchor 全部精确 Δ=0 行**；finding_id 全合法。**Verdict: CLOSURE APPROVED**。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 21 EVID-S13 行 + 全局 86 行 inventory_id 唯一，非空过）；`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` EXIT=0（all 57 items checked + Closure Evidence written）；`node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0（0 errors；10 warnings 均为 sibling plans 1835-2/1835-3 对尚未生成的 Stage 10/11 evidence 文件的 forward-ref，属 DRAFT_PLANS→EXEC_PLANS 管线瞬态产物，将在各自执行时 resolve，非本 plan owned；本 plan 的 evidence 文件 + roadmap + plan 自身 link health 0 broken）。本审计无生产代码变更，Closure Gates 显式声明 `./mvnw` 不强制，closure 依据 = evidence 校验器退出码 + in-process 实跑证据引用。roadmap work item 13 `planned → done`。

**Non-blocking follow-up**：cross-JVM fencing epoch revalidation（M8-2-P0-1 distributed mutex）+ cross-JVM zombie fencing（M8-2-P1-6）→ Stage 14；T2 lane deeper defect 修复 → independent remediation plan 或 Stage 14；`JobCoordinator` scoped-restart per-region counter（Javadoc `:220-224`）→ successor feature plan。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-0010-2-xdsl-streammodel-entry-audit.md`（4 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + 支持/拒绝节点矩阵，不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-7-xdsl-streammodel-entry.evidence.md`（30 条 `@@EVIDENCE` 行 EVID-S7-001..030 + 头部 Support/Reject XDSL 节点矩阵）。

**Phase 1（supported XDSL transform 节点 source-to-sink）**：13 行。6 条 `e2e-proved`（source/map/filter/keyBy/reduce/sink），正向 proof 全部来自真实 `.stream.xml` parse → `StreamModelDslBuilder.of(model).build()` → `env.execute()` → sink 输出断言：source/map/sink = `TestStreamModelDslBuilderE2E#streamXmlEndToEndProducesExpectedSinkOutput`（["A","B","C"]）；filter = `TestStreamModelDeltaExtends#xExtendsDeltaProducesDifferentSinkOutput`（["A","C"]，delta-added filter 丢 B）；keyBy/reduce = `TestAdvancedPipelineE2E#reducePipelineProducesCorrectAggregatedOutput`（[1,2,2,4,6]）。7 条 `component-only`（flatMap/window/aggregate/process/cep/custom/timestampsAndWatermarks）如实标注缺 in-process execute 覆盖（window/aggregate 因 nop-stream-runtime `IWindowOperatorFactory` 缺失只证 dispatch 到 runtime 边界），未静默升级 e2e-proved。

**Phase 2（XDSL → Java/graph trace + 等价性）**：3 行。EVID-S7-014 topology trace `component-only`（传递性论证：`TestDagTopologyConsistency#xdslAndJavaApiProduceSameDagTopology` build-only 证 XDSL transforms/edges ≡ Java Transformation DAG + Stage 6 frozen Java→graph 判据，但 repo 无 XDSL→execute→graph topology 断言测试，故非 e2e-proved）；EVID-S7-015 stable-identity `component-only`（XDSL transform id 仅作 builder streamRegistry key，`Transformation.getId()` 是无关 AtomicInteger，故 XDSL 声明式 id 不沿 Stage-6 identity 链传播——如实记录的架构观察）；EVID-S7-016 checkpoint config 应用 `component-only`（manual-trace applyCheckpointConfig:124-150）。

**Phase 3（unsupported XDSL 节点 fail-fast 矩阵）**：11 行。7 条 `fail-fast`（union/sideOutput/streams/sideInputs/environments/schemas/coders）各带真实 `rejection_proof`（`TestStreamModelDslBuilderFailFast#*`）；3 条 `unverified`（requirements/checkpointParticipants/onStart-onEnd-onError——repo 无对应 rejection 测试，如实标 unverified 而非 fail-fast）；1 条 `fail-fast` manual-trace（unknown transform type，AdvancedTransforms:100-101 防御性 throw，valid XDSL parse 不可达）。

**Phase 4（production-wiring gap + demo dangling + checkpoint unused）**：3 行 `residual-risk`。EVID-S7-028 XDSL 入口无生产接线（`runtime_wiring: partial`——grep `nop-stream/**/src/main/**/*.java` 对 DslModelParser/parseFromResource/.stream.xml 零命中，仅 test 调用，demo 无 Java driver）；EVID-S7-029 demo dangling transforms（fraud-detection.stream.xml 的 split-map/sum-reduce 无 edge 连接，`requireSingleInput:292-306` 会抛 IllegalArgumentException "found 0"，latent build-time fail-fast）；EVID-S7-030 checkpoint unused 字段（storageConfig/storageType/barrierAlignmentTimeout/maxConsecutiveCheckpointFailures/jobId/pipelineId 被 parse 但 applyCheckpointConfig 从不应用）。

**Anti-Hollow + 独立 closure audit**：独立 closure audit（fresh general subagent，task ses_022b1ebb2ffe3sNt3vYFLDeVVt）逐行核对全部 cited 测试方法真实存在、全部 anchor 匹配、3 条 unverified 行诚实无 rejection 测试、production-wiring grep 零命中确认、demo dangling/checkpoint unused 字段确认。**Verdict: PASS**——无 hollow claim、无 fabrication、无静默放行、无 disposition 膨胀。

**覆盖缺口（test-coverage，非 live defect，指派 roadmap item 17 / test-effectiveness remediation）**：(1) `<flatMap>` 缺非-demo in-process e2e fixture（demo 用 flatMap 但 dangling 无 driver）；(2) requirements/checkpointParticipants/onStart-onEnd-onError 缺 rejection 测试；(3) XDSL transform id → StreamNode/JobVertex 无 trace 测试。

**Corpus 交叉**：M7-2-P2-1（flow pom 依赖 cep）确认为 XDSL `<cep>` 编译 load-bearing（AdvancedTransforms import io.nop.stream.cep.*）；M7-2-P2-2（flow/model duplicate tree）仅记录 flow.model.* 是 builder 消费的 binding；M7-2-P2-5（UnknownTypeInformation 强转）仍 live on XDSL path；M8-2-P2-17（pipeline-count drift）stream.xdef:9-10 header 声明 5 箭头/6 名。最终 disposition 归 Stage 19-22。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 30 EVID-S7 行 + 全局 65 行 inventory_id 唯一，非空过）；`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` EXIT=0（1 plan passed）；`./mvnw test -pl nop-stream/nop-stream-flow -am -T 1C` → BUILD SUCCESS，51 tests 0 failures（全部 cited 测试类 green）。roadmap work item 7 `planned → done`。

---



### nop-stream 独立审计 Stage 9：Checkpoint、barrier 与恢复语义审计（plan 执行 + closure）

- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-08-0010-1-checkpoint-barrier-recovery-audit.md`（4 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + 支持/拒绝组合矩阵，不改 nop-stream 生产代码。`> Source Audits:` 行不存在（roadmap-sourced plan），故无 source-audit 关闭步骤。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-9-checkpoint-barrier-recovery.evidence.md`（19 条 `@@EVIDENCE` 行 EVID-S9-001..019 + 头部 Support/Reject 组合矩阵 M1-M7）。

**Phase 1（aligned checkpoint lifecycle）**：3 行。EVID-S9-001 协调器 lifecycle（trigger:369→ACK:423→completePending:453→restore:885）`e2e-proved`（TestCheckpointRecovery#testBasicCheckpointAndRecovery）；EVID-S9-002 executeWithCheckpoint 接线 `e2e-proved`（TestMailboxE2ECheckpoint#testE2ECheckpointAligned，durable checkpoint 落盘）；EVID-S9-003 InputGate BarrierAlignment:787 对齐 `component-only`（unit lane，如实分类）。

**Phase 2（multi-epoch/concurrent）**：3 行。EVID-S9-004 多 epoch 并发 `e2e-proved`（TestMultiEpochCheckpointE2E#testThreeEpochsInFlightCompleteIndependently，3 epoch 同时 in-flight 全完成无交叉污染）；EVID-S9-005 ACK routing by checkpointId `e2e-proved`（TestCheckpointBarrierTrackerConcurrency）；EVID-S9-006 D4 fail-fast（unaligned+multi-in-flight）`fail-fast`（TestInputGateUnalignedFallback#testUnalignedMultiInFlightFailsFastD4Guard 断言 ERR_STREAM_INVALID_STATE）。

**Phase 3（abort/cancel + sink commit-cut）**：3 行。EVID-S9-007 abort 端到端连通性 `e2e-proved`（TestCheckpointAbortWiring#testStuckChannelAbortTerminatesJob，executeWithCheckpoint→timeout→abort→exception 全链；registerLocalAbortHandler:830→tracker.notifyCheckpointAborted:843→InputGate.abortBarrierAlignment:858）；EVID-S9-008 sink commit-cut §6.4 invariant `e2e-proved`（TestExactlyOnceCorrectnessFixes#testSubsumingCommitCommitsAllPendingTransactions）；EVID-S9-009 commit failure/retry `e2e-proved`。

**Phase 4（unaligned+rescale fail-fast + fingerprint + 历史 finding 复验）**：10 行。EVID-S9-010 unaligned+rescale `fail-fast`（assertNoChannelStateOnRescale:1232 throws ERR_STREAM_CHANNEL_STATE_RESCALE_UNSUPPORTED:1242；TestChannelStateRescaleFailFast#rescaleWithNonEmptyChannelStateFailsFast）；EVID-S9-011 recovery fingerprint `e2e-proved`（TestStreamModelFingerprintRecoveryCompat same/different）；8 条历史 P0/P1 finding 复验：M7-2-P0-2/P0-3/P1-11、M8-2-P1-5 = `e2e-proved`（已修且有回归测试）；M7-2-P1-4、M8-2-P0-1、M8-2-P1-2、M8-2-P1-6 = `residual-risk`（部分修，in-process lane 无法证明 cross-JVM/SST 级，明确 successor stage 所有权）。

**Anti-Hollow + 独立 closure audit**：执行中自查发现并修复 1 处 fabrication——EVID-S9-016 初版 cited 不存在的测试方法 `testConcurrentGlobalRecoveryIsSerialized`，修正为真实方法 `concurrentGlobalRecovery_serializesToOneRotation`（TestJobCoordinatorRecoveryConcurrency:139）。独立 closure audit（fresh general subagent，task ses_022bf8563ffeMkjqxFqWd0xKvE）7 项检查全 PASS：validator exit 0（evidence --strict + self-test）、19 行唯一、所有高价值 anchor 逐行核对匹配、所有 28 个 cited `ClassName#method` 均真实存在（无 fabrication）、disposition 诚实（residual-risk 各带 successor-stage 理由、fail-fast 各有断言抛异常的 rejection_proof）、矩阵完整、plan 无遗留 `- [ ]`。**CLOSURE: PASS**。

**覆盖缺口（test-coverage，非 live defect，指派 roadmap item 17 / test-effectiveness remediation）**：(1) 无单条 in-process 测试将 persist→restore→reprocess 经 executeWithCheckpoint 串联（现由 coordinator+storage 测试与 executor+pipeline 测试互补覆盖）；(2) 多 channel fan-in barrier 对齐无 e2e 断言（unit 级已覆盖）；(3) unaligned full-recovery env.execute()-level 测试。

**确认的 live-defect residual（不在本计划修复，按 roadmap 规则指派 successor）**：M7-2-P1-4（StreamOperator.initializeState 通用 hook 被 restoreState/restoreFromEpoch 取代，sources 仍调用）→ Stage 19；M8-2-P0-1（globalRecovery cross-JVM 互斥不可在 in-process 证明，fencing-epoch-before-lock 硬化已存在）→ Stage 13；M8-2-P1-2（incremental ref-count rollback 已存在，RocksDB SST 级完整性）→ Stage 10；M8-2-P1-6（LOCAL 复用 restoreOperatorsFromState，true zombie fencing cross-JVM）→ Stage 13/14。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 19 行，非空过）；`self-test` EXIT=0；15 个 cited 测试类经 surefire 全 green（71 methods，0 failures/errors/skipped）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` EXIT=0。roadmap work item 9 `planned → done`。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-07-2346-2-java-api-graph-local-execution-audit.md`（4 Phase 全部 `completed`，`Plan Status: completed`）
- 性质：**审计计划（无生产代码变更）**——产出 evidence rows + 等价性判据文本，不改 nop-stream 生产代码。
- 产出：`ai-dev/audits/nop-stream-independent-audit/stage-6-java-api-graph-local.evidence.md`（16 条 `@@EVIDENCE` 行 + topology/stable-identity/recovery-inputs 等价性判据段）。

**Phase 1（supported DataStream 构造 source-to-sink）**：9 条 evidence row（source/map/filter/flatMap/process/keyBy/assignTimestampsAndWatermarks/transform/sink）。正向 proof 全部来自真实 `env.execute()` in-process 实跑测试（TestDataStreamPipeline/TestKeyedStreamAggregation/TestAssignTimestampsAndWatermarks/TestEventTimeWindowE2E），sink 输出断言通过。

**Phase 2（graph/plan 编译链 + 算子生命周期）**：EVID-S6-010 编译链（execute():282→StreamGraphGenerator→:285 JobGraphGenerator→:292 PartitionedPlanGenerator→:294 generateLocal→:317 GraphExecutionPlan.build→:346 submitTask）；EVID-S6-011 生命周期（StreamTaskInvokable.invoke 调 operatorChain.open()/finish()/close()，finish() 现 LOC 路径已调用 → M7-2-P1-5 RESOLVED）。

**Phase 3（fan-out/partition/parallelism）**：EVID-S6-012 keyBy 分区（TestKeyedStreamAggregation in-process 多 key 路由）；EVID-S6-013 forceNonParallel/lockParallelismToOne（M7-2-P0-1 RESOLVED：SingleOutputStreamOperatorImpl.java:53-57 调 lockParallelismToOne 不再抛；锁经 Transformation→StreamNode→JobVertex→GraphExecutionPlan 传播）。

**Phase 4（unsupported forms + 等价性判据）**：3 条 non-goal row（connect/union/side-output，`implementation_anchor: none`，确认 DataStream.java 无这些方法）；冻结 topology/stable-identity/recovery-inputs 判据文本。

**Anti-Hollow + 独立 closure audit**：初始独立 closure audit（ses_...36）发现 4 缺陷（1 CRITICAL：transform row 用拒绝测试当正向 proof；2 MODERATE：编译链/forceNonParallel row 标 e2e-proved 但 cited 测试不调 execute()；1 MINOR：process row cited 不存在的测试方法名）。全部修复：transform positive_proof→TestEventTimeWindowE2E#testEventTimeWindowPipeline（真实 .transform()+execute()+sink 断言）；编译链 positive_proof→TestDataStreamPipeline#testSourceMapFilterSink（真实 execute()）；forceNonParallel 降级为 component-only/unit（无 execute() 实跑，如实分类）；process 测试方法名→testProcessElementIsCalled。再审计（ses_...pe）**PASS**——4 缺陷全修复。

**覆盖缺口（非 live defect，指派 successor test-effectiveness remediation / roadmap item 17）**：process(ProcessFunction) 与 forceNonParallel 各缺一条 `env.execute()` source-to-sink 测试，已如实标 component-only（未静默升级 e2e-proved）。

**Corpus 复验**：M7-2-P0-1（forceNonParallel always-throws）RESOLVED；M7-2-P1-5（finish never-called）RESOLVED（LOC 路径）；M7-2-P2-3（Javadoc 引用不存在的 TwoInputStreamOperator）STALE（core main 已 0 匹配）；M7-2-P2-5（UnknownTypeInformation 强转）仍 live（DataStreamImpl.java:140,183,204），flagged for Stage 21；M8-2-P2-18（null operatorStateStore）recovery-scoped（Stage 9/10）。

**验证**：`node ai-dev/tools/check-nop-stream-audit-manifest.mjs evidence --strict` EXIT=0（解析 16 行，非空过）；9 条 cited 测试 + TestCepNonKeyedEntryE2E 经 surefire 全 green（0 failures）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` EXIT=0（审计无生产代码变更）。roadmap work item 6 `todo → done`。

---

### nop-stream 独立审计 Stage 5：环境资格与 gated-evidence 契约（plan 执行中）

- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-07-2346-1-environment-qualification-gated-evidence.md`
- 性质：**纯数据/工具计划**——不改 nop-stream 生产代码，仅产出审计数据文件（`environment-qualification.md` + `evidence-schema.md` 增补段）+ 校验器子命令（`check-nop-stream-audit-manifest.mjs qualification`）。
- Lane baseline 已逐行核对测试源码（gate 属性名/连接串/行号全对）：T1 嵌入式 H2（`TestJdbcTwoPhaseCommitSinkSkeleton:60-62`，无门禁）+ mocked Debezium（`TestDebeziumCdcCheckpoint:215` Mockito mock WorkerConfig，无门禁）；T2 multi-JVM（`TestMultiJvmExactlyOnceRecovery:67` 门禁 `nop.stream.test.multi-jvm.enabled`）；T3 Kafka（`:44-45` 门禁 `.kafka.enabled` + `:57` 连接串 `.kafka.brokers`）；T4 Pulsar（`:49-50` 门禁 `.pulsar.enabled` + `:62` 连接串 `.pulsar.serviceUrl`）；T5 PostgreSQL（repo 无 gated 测试）；T6 真实 CDC（仅 mocked offset-config，无真实 source 测试）。

**Phase 1（T1 unit + 嵌入式 in-process + 规则文本）**：
- 新增 `ai-dev/audits/nop-stream-independent-audit/environment-qualification.md`：T1 `@@LANE` 块（`frozen_strength: in-process`，`status: qualified`），如实标注 H2 嵌入式 in-process（非外部 provision）、Debezium mocked offset-config（非真实 CDC），不冒充外部集成。
- `evidence-schema.md` 增补段（不改 11 字段/7 词表）：Rule S5-1 gated-evidence（gated 测试仅当在 qualified lane 实跑才算证据，缺省 skip 不算）、Rule S5-2 required-lane/blocked-gate（required-lane blocked 行阻塞 Stage 23 ready）、Rule S5-3 lane-mapping（environment_class 不得超过任何 qualified lane 的 frozen_strength）。
- **T1 实跑证据**：`./mvnw test -pl nop-stream/nop-stream-connector-jdbc -am -Dtest=TestJdbcTwoPhaseCommitSinkSkeleton` → `Tests run: 19, Failures: 0, Errors: 0, Skipped: 0`（surefire `nop-stream-connector-jdbc/target/surefire-reports/`）；`./mvnw test -pl nop-stream/nop-stream-connector-debezium -am -Dtest=TestDebeziumCdcCheckpoint` → `Tests run: 7, Failures: 0, Errors: 0, Skipped: 0`（mocked offset-config）。

**Phase 2（T2 multi-JVM 资格认定）**：
- 先 `./mvnw install -pl nop-stream -am -DskipTests` 填充 classpath（MiniStreamCluster.java:137-138 解析 java.class.path + java.home）。
- `TestMiniStreamClusterProcessSpawn`（门禁 `.multi-jvm.enabled=true`）实跑 **PASS 3/3**（2.427s）——证明 lane 基础设施：子 JVM 真实 spawn、TM 经共享 JDBC registry 注册、coordinator 在独立 JVM 启动（`coordinator-0.log` 创建，:70 `logFileFor("coordinator-0")`）。**T2 判定 `qualified`**，`frozen_strength: multi-jvm`。
- **发现 2 个 deeper-test 缺陷（记为下游 capability audit 的 finding，非 lane 资格阻塞）**：(1) `TestMultiJvmExactlyOnceRecovery` ERROR @ :111 ——读 `logFileFor("coordinator")` 但 `spawnJobCoordinator(:404)` 写 label `coordinator-0`→`logs/coordinator-0.log`，`Files.size` 抛 `NoSuchFileException`（log-label 不匹配缺陷，bare `"coordinator"` 应为 `"coordinator-0"`）；(2) `TestMultiJvmCoordinatorFailover` 1/2——`testBrainSplitFencingBoundary(:129)` 失败"coordinator-1 must take over"（HA failover takeover，Stage 13 control-plane/HA capability finding）。exactly-once recovery / HA fencing 能力声明须由 Stage 13/14 取证，不从 lane 资格单独断言。

**Phase 3（T3 Kafka / T4 Pulsar / T5 PostgreSQL / T6 Debezium 如实裁定 + 校验器 qualification 子命令）**：
- T3 Kafka：门禁 `nop.stream.test.kafka.enabled=true`（源码 `TestDataPlaneKafkaBackendE2E:44-45`，**非 `.brokers`**）+ 连接串 `nop.stream.test.kafka.brokers`（:57）。审计环境无 broker（localhost:9092 closed）→ **`blocked`**；门禁有效性已验证（无 flag 时 Skipped=1）。
- T4 Pulsar：门禁 `nop.stream.test.pulsar.enabled=true`（源码 `:49-50`，**非 `.serviceUrl`**）+ 连接串 `nop.stream.test.pulsar.serviceUrl`（:62）。无 service（localhost:6650 closed）→ **`blocked`**（同 T3 门禁机制）。
- T5 PostgreSQL：repo 无 `@EnabledIfSystemProperty`-gated PostgreSQL 测试（JDBC sink 测试均用嵌入 H2）→ **`blocked: no gated PostgreSQL test`**（H2 如实归 T1）。
- T6 真实 CDC：仅 mocked offset-config（Mockito mock WorkerConfig）→ **`blocked: only mocked test exists`**（mocked 如实归 T1）。
- **校验器扩展**：`check-nop-stream-audit-manifest.mjs` 新增 `qualification` 子命令（校验 `@@LANE`：必填字段、`frozen_strength`∈`{unit,in-process,multi-jvm}`、`status`∈`{qualified,blocked}`、blocked 必带 reason+rerun、qualified 必带 expected_positive_result、拒未知字段）；纳入 `all` 组合；self-test 加 qualification 阳性对照（6 坏 1 好）。
- **阳性对照（端到端）**：临时换入坏 `@@LANE` 文件 → `[FAIL] 4 problem(s)` EXIT=1（逐条打印错误）；恢复真文件 → `[PASS]` EXIT=0。
- **pre-existing manifest 漂移修正**：`test-java-files-all` denominator 448→453（Stage-4 冻结后新增 5 个合法手写测试文件，如 TestCheckpointCoordinatorRaceCondition/TestTaskManagerDaemon/TestStreamModuleDiscovery）。仅 measured-count 刷新，格式/词表未改。`node ... all` 现 EXIT=0（manifest+corpus+evidence+qualification 全 PASS）。

**Phase 4（资格记录冻结 + frozen-schema 完整性校验）**：
- `environment-qualification.md` 顶部 Status `draft → frozen`；新增 T1-T6 汇总表（qualified/blocked + frozen_strength 映射）+ frozen-schema 完整性核对结论。
- **完整性核对**：`evidence-schema.md` 11 字段定义不变（grep 计数=11）、7 分类词表不变（计数=7）、lane 强度词表不变（unit|in-process|multi-jvm + none）。本计划仅**追加** "Stage 5 Supplement" 规则段（S5-1/S5-2/S5-3），未改字段/词表。
- **Anti-Hollow**：qualified lane（T1/T2）有实跑 surefire 引用；blocked lane（T3-T6）有非空 reason+rerun_condition；H2 嵌入式/Debezium mocked/PostgreSQL 无测试 性质如实标注未冒充外部集成。
- `node --check` OK；`node ... all` EXIT=0；`node ... self-test` EXIT=0（4 checker 阳性对照含 qualification）。



