# 2026-09-09

## nop-treesitter roadmap item 9 — incremental reparse (plan 2026-09-08-0234-3) 执行开始

- Baseline verified: `./mvnw -pl nop-treesitter -am test -T 1C` green — **270 tests**（plan 起草时 134，item 7/8 落地后增长）。
- 读 C 参考：`lib/src/get_changed_ranges.c`（并行迭代器 + `ts_range_array_add` 合并 + 尾部差值）、`reusable_node.h`、`parser.c` 的 `ts_parser__reuse_node` / `ts_parser__can_reuse_first_leaf` / `ts_parser__advance`。gotreesitter `tree.go` `DiffChangedRanges`/`diffNodes` 作第二参照。
- **关键发现（记录进 plan 的 Decision）**：
  1. C 复用复合节点时经 `breakdown_lookahead` 拆开、实际只 shift 叶子 → Java 侧按**叶子粒度**复用，上层全部由 reduce 重建，形状/production id/dyn-prec 与全量解析构造性一致。
  2. C 的 `ts_tree_edit` 会把旧树**变异**到新坐标并用 `has_changes` 位门控复用；本实现保持 `TSTree` 不可变，复用门控改为**编辑区间相交判定**（同一谓词、无需 write-only 版本列）。arena 版本列按 Minimum Rules #24（防空壳）裁定移除。
  3. `getChangedRanges` 采用叶子 spine + **位置无关内容相等**（借各自 TSTree 保留的 source bytes）+ 并集区间合并 —— 比绝对位置相等更紧（不移位尾部过报），且能报出 `1`→`2` 这类纯 token 内容变化。`TSTree` 因此增加 source 保留（修正 plan baseline 的"无需保留"判断）。
  4. 复用门控保守项：外部 lex 状态不复用；cell 含 REDUCE 不复用（`preferShift=false` 下 shift+reduce 是真 GLR 分叉）；keyword-capture token 不复用；零长叶子不复用。
- Phase 1 landed：`parser/incremental/` 包新增 `TSPoint`（row/UTF-8-byte column + `fromByteOffset`）、`TSRange`（points+bytes+`overlapsOrTouches`）、`TSInputEdit`（old/new byte triple + `of(oldSource,newSource,...)` + `isNoop`），全部不可变 record、typed error 校验；`TSEditValueTypesTest` 10 tests green。

## TODO next
- Phase 2: `ReuseCursor`（叶子 spine + old→new 坐标映射 + 相交门控）、GLR `advance` 复用钩子、`TSParser.parseIncremental`、等价不变量测试。

## nop-treesitter item 9 Phase 2-3 收口

- **Phase 2 landed**（commit 级）：`ReuseCursor`（叶子 spine + old→new 映射 + 相交/相触门控）、GLR `advance` 复用 lookahead 钩子、`TSParser.parseIncremental`（校验快失败）、`TSTree` 保留 source bytes。关键设计沉淀：叶子记录 lex state 于 arena `state` 槽（叶子永不为父、productionId 只在复合节点读取），复用门控 = C `can_reuse_first_leaf` 的 lex-state 相等主路径 ∨ blob 携带的生成器 `reusable` 位回退路径。
- **三个 live defect / 语义缺口在执行中发现并修复**：
  1. `GLRParser.accept` 的 root size 取 GSS base position（恒 0）——item 4 以来所有全量解析的根跨度都是错的；改用 END token endOffset。
  2. 复用相交门控缺"相触"边界（`end == edit.start` 的叶子必须重 lex）——`[1]`→`[12]` 用例：旧 `1` 被复用后与新插入的 `2` 无法组成 `12` token，状态机死掉。对齐 C `ts_subtree_edit` 的 visited-set；noop 编辑照 C 完全跳过。
  3. `mapOldToNew` 纯插入边界（start==oldEnd）映射方向错误——插入点之后的旧坐标应整体移位。
- **Phase 3 landed**：`ChangedRanges.getChangedRanges`——叶子 spine（不可见节点/链容器拍平 + 别名感知有效符号）双指针位置无关内容 diff + `ts_range_array_add` 合并语义。递归结构 diff 先实现后废弃：`array_repeat1` 重复规则在插入后重嵌套，同叶子不同形状击穿子树对齐；叶子 spine 天然免疫（计划 refinement 已记录）。`1`→`2` 纯 token 内容变化可报出（C 靠脏位）。
- 测试：`ParseIncrementalTest` 14 + `ChangedRangesTest` 7；全模块 **301 green**（JSON 7/7、Java 108/108、JS 无回归）；checkstyle -Pqa 0 violations。
- 独立机械校验器（测试内）：raw-symbol 叶子双指针，断言每个分歧叶子对都被报告区间覆盖、区间有序不相交——与主实现的 effective-symbol 路径互为交叉验证。

## item 9 closure

- 独立子 agent closure audit（agent_0ea98b38-7c75-43f7-8fd3-115f20ee4047）：7 维度全 PASS——Phase 1/2/3 Exit Criteria 逐条 live 证据、Anti-Hollow 调用链实读追踪（advance:147 → reuseLeafForPosition → shift:237 消费 + recordReuse）、301 tests、checkstyle 0、hollow scan 0。
- `check-plan-checklist.mjs --strict` 写入 Closure Evidence 后 exit 0；计划 frontmatter → completed。
- roadmap item 9 → `done`（closure-derived）。下一项：item 10（JS/TS grammars）。

## nop-treesitter item 10 — JS/TS grammars 执行记录

- **Phase 1**（vendor + extraction proof）：vendored tree-sitter-typescript（parser.c ×2、common/scanner.h、每方言 scanner.c、4 corpus 文件、PROVENANCE.md，17MB）；`TsGrammarVendorTest` 5 tests（双方言提取不变量 + CorpusUtil 元数据感知 section 计数：TS 112 节/2 个 `:language` 标签/每方言 111 runnable，JS 116）。
- **格式演进 v3→v4**（roadmap 预判的阻塞项，实际触发）：TS small parse table 166,255 词超 u16 → header 该字段 u16→u32（+2 字节，reserved 29→27），`BlobWriter`/`BlobReader`/`Language.decode` 同步 v4，`blob-format.md`（authoritative spec）全量更新，5 个 blob 全部重洗。附带：`LanguageTest` 字节偏移、`ParserTest` 合成 blob、`BlobRoundTripTest`/`JavaBlobTest` 版本断言迁移 v4。
- **scanner DSL 翻译**（Phase 2 核心，多次返工后收敛）：common/scanner.h → `common/scanner.dsl`（双方言共享，`TsBlobTest` 断言逐字节相同）。方法论教训：VM 的 `jmp_if_X` 语义是 **条件成立=直落下一条、不成立=跳 label**——JS dsl 的两行习语（`jmp_if_X <else-label>` + `jmp <true-target>`）是唯一可靠模板；手写 ne-form 全部方向颠倒。最终结构：dispatcher 按 JS 原版 + TS 差异（FS_ASI 门、独立 ternary 分支、`}`+`:` 判别、简化 ws 无 newline 三态、ternary `?.`/`??`/`:`/digit 尾部、U+2028/2029），template/jsx 子程序逐字节照搬 JS。`TsScannerTokenTest` 28/28（每个子程序 accept+reject 路径）。
- **stale blob 教训**：dsl 修好后 blob 必须重洗——token 测试实时编译 dsl（过）而解析用 blob（旧），一度全输入失败；`/tmp` 脚本被清导致 rewash 静默未跑。
- **运行时缺陷修复**（corpus triage 发现，各带回归测试）：
  1. `GLRParser.reduce` 版本槽死位：keyword-capture 大量分叉时组循环内 merge 移除版本，后续 slice 映射到 stale 槽 → `arraycopy length -1` crash（JS 'Reserved words as identifiers' / 'Object destructuring patterns w/ default values'）→ 边界守卫 + `GLRParserTest.keywordForkedReducesSurviveMidLoopVersionRemoval` 回归。
  2. 模块 pom 加 surefire 2G 堆 profile（TS/TSX 大表 corpus 在父默认 1G 下 OOM）。
- **验收**：JS 115/116、TS 110/111、TSX 110/111（各 1 段裁定：JS error-recovery=item 11；TS/TSX 'Classes with extensions' = `B<C>(D)<E>` 类继承 GLR tie-break 与上游 select_tree 相反，99.1% ≥ 95%，select-tree parity 深查记为 successor）。全模块 **342 tests green**、checkstyle -Pqa SUCCESS、hollow scan 0。
