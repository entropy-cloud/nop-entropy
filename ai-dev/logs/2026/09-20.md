# 2026-09-20 开发日志

### 上午（nop-rg 性能收敛循环：plan 2267 拟制 + Phase 1 基线重建）

- Plan 2267（ai-dev/plans/2267-nop-rg-performance-convergence.md）经两轮独立子 agent 对抗性审查后激活（首轮 1 Blocker + 4 Major + 6 Minor 全部修复：large-file 命令 `-DexcludedGroups=` 防空转、吞吐比 gate 复测规则、SIMD e2e 裁定分支规则、corpus 污染防护、判定 JSON 落 `_tmp/` 等；第二轮判定可执行，n1-n3 已折入）。背景：roadmap 15 项全 done 后，按用户指令在 live 代码上重跑 JMH/JFR 收敛循环，**严格字面终止条款 = 连续两轮无候选通过保留条件**（2265 终止系实质性裁定）。
- Phase 1 基线重建完成（判定 JSON 证据 `_tmp/nop-rg-bench/`）：
  - σ_run 连跑 6 次（row0a-d i5 配方 + row0e-f i10 废弃）：**本机噪音显著高于 2265 时期**——期间出现外部负载（非本会话 node/chrome-headless 合计 2+ 核）与持续满载后热节流（row0e-f 1MB 崩至 271 ops/s）。**Phase 2 判定协议据此修订为共测配对**（基线/候选背靠背 3 对交替，配对增益对消漂移），已如实记录。
  - HotspotProfiler（64MB count 口径 12s/653 runs/3166 samples）：**LineCursor.indexOf 41.2%**、PreparedLiteral.readByte 23.3%、matchesAt 14.8%、aggregate 6.7%、advance 5.4%、RecursiveAction.exec 4.1%；isBinary 0.2% 排除。LF 扫描权重较 2265 末轮（8.1%）剧变（R4/R5 count 口径成主 workload 所致）；2265 R2 的 SWAR 否决系 TEXT workload 下测得，count 口径需重验。
  - Vector 场景矩阵（@Param 6 场景）：6B 向量=标量 72-80%（复现 2266 劣化）；**16B：2.5-3.5x 胜出；32B：1.6-1.9x**。e2e 配对判定（long-32B 64MB 共测 3 对交替）：**3/3 方向一致，中位 +5.2%**——长模式 SIMD 为真实 ≥2% 收益，触发收割。

### 中午（nop-rg R1：SIMD 长度阈值策略，plan 2267 保留项 1/循环）

- **R1 落地**：`NopRgVectorLiteralFinderProvider` 增加模式长度阈值策略（`SIMD_MIN_PATTERN_LENGTH=16`）——`--vector` 短模式返回标量等价 PreparedLiteral（消除 72-80% 回退），≥16B 走 SIMD（收割 e2e +5.2%）。crossover 取证：6B 劣化 / 16B 胜出 2.5-3.5x（vec-16b.json 收尾档），8-15B 未测保守归标量。
- 测试：vector 8→10（新增策略 2 例：短模式标量断言 / ≥阈值 Vector 断言）；fuzz 改造为跨阈值双路径 500 例（奇数迭代长模式植入真实命中，vectorPathCases ≥100 断言）——否则阈值策略会架空 Vector 等价性覆盖；testBoundariesMatchScalar 补长模式边界。vector 10/10 + core 53 + cli 25 全绿。
- Doc-sync：cli README `--vector` 行补阈值注记；design 决策 5 实现层级表更新（顺带修正 FoldingByteSearcher 陈旧行——f5d44be31e 已删除该类）；benchmark README Vector 章节重写（6 场景表格 + e2e 配对数据 + 策略说明）。
- 基准设施扩展（Phase 1）：CorpusUtil 生成器命中词/密度参数化（旧签名逐字节等价委托）；VectorCompareBenchmark 场景矩阵化；CoordinatorEndToEndBenchmark 加 scenario/strategy @Param（long-32B corpus 落独立子目录 `64mb-long/` 防 ensureFile 复用污染）。
- Fix：SearchCoordinator.java:35-38 类 javadoc 整块按 live 行为重写（FoldingByteSearcher 失效 link、VECTOR 抛异常陈旧表述、ChunkedFileReader 历史叙事）；grep 全仓零残留。ScalarByteSearcher 失效 import 同步移除。
