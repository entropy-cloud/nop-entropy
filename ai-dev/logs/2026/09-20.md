# 2026-09-20 开发日志

### 晚间二（nop-lint xdef 命名决策修订：kebab → camelCase 直写，用户质询驱动）

- **用户质询成立，推翻我上一轮修复中的一个论据**：此前以"跟随平台全库 kebab 约定"为由保留 kebab-case 标签、只把文档说法改为"自动映射"。实测两个事实：① 平台对无分隔符名做**恒等映射**（`xmlNameToVarName` 无 `-`/`:`/`.` 时短路原样返回，不进含 `toLowerCase` 的 `camelCase()`；`beanPropName` 对首字母小写 camelCase 不改写）——camelCase 声明完全可行；② nop-xdefs 实测 **camelCase 标签 445 种 vs kebab 133 种**，camelCase 本就是平台主流（page/view/xmeta/rule 系），"全库 kebab 约定"论据不成立。
- **修订**：10-xdef-metamodel.md 的 xdef 示例全部标签/属性改 camelCase 直写（`<sameText>`、`stopBy`/`stopByRule`、`xscriptTimeoutMs`、`<controlFlow>` 等）——xdef 声明名 = Java 属性名 = YAML 键**三者同名**，原"键名不变"从矛盾声称变为真话；§2 说明重写为 camelCase 直写 + 恒等映射证据 + 边界约束（声明名须小写字母开头、避免第二字符大写形态——`beanPropName` 会把 `aBc` 改写为 `ABc`；`xdef:` 命名空间属性是平台自有名字不改）；01 §3.2 注释、05 §3、08 §2 矩阵的引用同步统一。
- 验证：kebab/snake 残留 grep 清零；`check-doc-links.mjs --strict` 0 errors。
- Doc-sync：仅 design/nop-lint 内部一致性变更，无平台文档影响。

### 晚间（nop-lint 设计与 roadmap 深度审计：3-agent 对抗审查 → 修复 → 复审共识）

- **审计方式**：3 个独立子 agent 并行（代码事实核验 18 项声称 / 13 文档一致性+规范符合性 15 维度 / 可行性+需求覆盖+roadmap 组织），随后独立验证 agent 复审 + 残修补复再复核。终局裁定 **CONSENSUS-READY**（上一轮 6 轮评审标 READY 的文档集仍查出 3 P0 + 13 P1 + 12 P2）。
- **P0 修复**：① roadmap 缺失——新建 `ai-dev/backlog/nop-lint-roadmap.md`（43 items / 6 派生里程碑 / 6 Waves，唯一动态状态块）+ `missions/nop-lint.json`（mission-driver 校验通过）+ `ai-dev/plans/nop-lint/`、`ai-dev/audits/nop-lint/`；② Phase 1 "4 周 13 项"超载——08-migration 废止 14 周日历口径，改 Phase→Wave 映射 + 现实区间（全项目 24–36 周），最小 CLI 从 Phase 4 提前 Wave 3（消除执行面倒挂）；③ DeadlineXScriptExecutor 草图引用不存在的 `scope.getDeadlineNanos()` 且涉 nop-xlang Protected Area 未声明——07 §4 重写为三路线决策表，**路线 A（`EvalExprProvider.registerGlobalExecutor` 全局包装，WhileExecutable 每回边经 executor，零平台改动）v1 采用**。
- **P1 修复（要点）**：XLang lambda 语法写反（`->` 实为 `=>`，XLangOperator.ARROW 实证）；XLang 反引号字符串**无 `${}` 插值**（XLangParseHelper 实证，示例改 `+` 拼接）；10-xdef "YAML↔XML 保持键名不变"被代码推翻（camelCase↔kebab-case 自动映射，StringHelper.xmlNameToPropName）；YAML 规则加载需 register-model 注册 `DslJsonResourceLoader`（10 §4 补环节+交付物）；parser-class 补"暂无运行时消费方"标注；fix schema 三处归一（静态 {description,template,suggest} vs report() 运行期 {description?,template,capture?}）；xdef 补 requires/xscript-timeout-ms/options/settings；06 §5.6 自立延迟数字（<1s/500 文件 vs 11 的 1–3 分钟）改为指向权威；@SuppressWarnings 补 fast 档归属；设计文档违禁引用 ai-dev/analysis + ~/sources 全部清除；02 §3 新增需求追溯表（check-\*.mjs 24 脚本逐脚本 manifest、check-bean-naming→XNode、反模式 8 条 Phase 3 先交清单、文档一致性 3 条显式排除、吸收 ai-dev/tools/rules/ 下 3 条现有 ast-grep 规则）；设计目录补 README.md（AGE 四要素）；08/索引去执行复选框与评审过程叙事；性能声称改可验收口径（不以超 ast-grep 为目标、Semgrep 10x 限纯 pattern 场景待 benchmark 验证、roadmap Wave 2 加 benchmark item 作为发布门槛）；11 §5 补 pattern 匹配防线（嵌套省略号静态检查 + 软预算）；06 §6.3 补 ASTMapping 边界不对齐消解规则；规则最小版本戳提前 Phase 1；survey §4.1 加勘误块（mjs 实 24 个在 ai-dev/tools/、checkstyle 17/pmd 9、compliance-checker.sh 已不存在、ast-grep 规则链盲区）。
- **复审**：验证 agent 逐项 R1–R13 + 新问题扫描 N1–N5，报 1 P1 + 2 P2 + 3 P3 残留 → 全部修复 → 复核 6/6 RESOLVED，**CONSENSUS-READY**。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 0 errors（9 个 warning 均为无关历史 plan 文件，先在）；`bash tools/mission-driver.sh nop-lint --dry-run --max-cycles 0` 加载校验通过。
- Commit: 见本条目对应提交
- Doc-sync：本任务全部产出即文档（design/nop-lint 14 文件 + backlog roadmap + mission + survey 勘误）；`docs-for-ai/` 无需更新（nop-lint 未实现，无平台使用面变化）。

### 下午（nop-rg plan 2267 Phase 2 优化收敛循环：R2-R4 + 严格终止）

- **循环以严格字面条款终止（R3✗ + R4✗ 连续两轮无候选通过）**，协议 = 共测配对（基线 jar/候选 classes 背靠背 3 对交替，对消本机外部负载漂移），增益约定 = (cand−base)/base（正 = 候选更快）：
  - **R2 回退**：LineCursor LF 扫描 SWAR（JAVA_LONG_UNALIGNED + 小端守卫）——41.2% 热点，配对中位 64MB -0.20% / 512MB -0.35%（候选更慢）。SWAR 类 LF 扫描第二次实证否决（2265 R2 TEXT 口径、2267 R2 count 口径）。
  - **R3 回退**：aggregate count 口径 span-gap 行计数（跳过每跨度行尾扫描）——64MB 中位 -2.43%、512MB 中位 +0.50%，方向不一致且均 <2%。
  - **R4 回退**：count 口径单遍融合扫描（锚点检查+LF 计数共享一遍；PreparedLiteral 暴露 anchorMatchesAt/verifyAt 协作，不改 SPI）——64MB 中位 +0.99%、512MB 中位 +2.23% 但离散度 σ≈3.35%（3σ≈10% ≫ 2.23%）且逐对 JMH CI 大幅重叠，未通过保留条件。**关键实证：连"消除一整遍扫描"都无统计可辨收益 → count 口径 e2e 为带宽/缓存行为受限，第二遍走缓存，指令级/遍数级优化均无效**。
  - 终态 profile：indexOf 48.9%、matchesAt 25.3%、aggregate 8.9%——碎片化，剩余候选（matchesAt 次序重排等）预期 <2% 且被 ±8% 噪声淹没。池复用候选拒绝裁定：仅惠及基准自身（每 op 新建 coordinator），产品为每进程单次搜索。
  - **R1 保留**（上午轮次）：SIMD 长度阈值策略，长模式 e2e 配对中位 +5.2%——循环唯一 ≥2% 收割。
- **R2-R4 全部回退，净代码变更为零**（新增 2 个 count 语义/等价性测试保留：testCountOnlyDistinctLineSemantics、testFusedCountEquivalenceFuzz 60 例确定性 fuzz——守护 count 口径行语义，与实现无关）。
- 吞吐比收尾：64MB = **82.3%**（≥50% ✓）；512MB 六次收尾档中位 49.05%（46.7-50.8 区间）→ 按协议完成显式裁定：**非代码回归**（f5d44be31e 仅 javadoc、R2-R4 回退后 git diff 复核，与 2265 测得 51.3% 时行为等价；rg 侧六次恒定 24.71-25.05，漂移全在 JVM 侧，系本机外部负载与满载疲劳），残差记 watch-only follow-up（安静环境复测），非阻塞。
- **closure audit（独立子 agent，fresh session）关键发现与处置**：①Major：R2-R4 记录表/daily log 18 个配对增益符号颠倒（误用 (base−cand)/base）——全表纠正 + 勘误注记；audit 独立复算确认回退与终止裁定在纠正后不变（R4 512MB 名义 +2.23% 经 3σ 离散度 + CI 重叠复判仍 FAIL）。②Major：doc-links --strict 全局红（audit 时 6 errors）——系并发 nop-lint 会话 audit 期间写入中途状态所致（其收口提交后终态 EXIT=0 errors，9 warnings 为其他 draft 计划 9/14 以来既有断裂，归相应 owner；本计划触达文件 0 断链）。③Minor：cli VectorModeTest 补 ≥16B 长模式 SIMD 路径测试（cli 25→26）——修复 R1 短模式策略导致的 CLI 层 SIMD 覆盖弱化；Phase 1 漏勾 checkbox 补勾；"3 个测试"计数更正为 2 个。
- 验证：core 55（含 large-file 显式组 3/3 `-DexcludedGroups= -Dgroups=large-file -Xmx256m`）+ cli 26 + vector 10 全绿；rg 对照 opt-in 55/55；scan-hollow EXIT=0。
- Doc-sync：benchmark README 新增"收敛循环结论"章节 + corpus 子目录防污染说明。


### 上午（nop-rg 性能收敛循环：plan 2267 拟制 + Phase 1 基线重建）

- Plan 2267（ai-dev/plans/2267-nop-rg-performance-convergence.md）经两轮独立子 agent 对抗性审查后激活（首轮 1 Blocker + 4 Major + 6 Minor 全部修复：large-file 命令 `-DexcludedGroups=` 防空转、吞吐比 gate 复测规则、SIMD e2e 裁定分支规则、corpus 污染防护、判定 JSON 落 `_tmp/` 等；第二轮判定可执行，n1-n3 已折入）。背景：roadmap 15 项全 done 后，按用户指令在 live 代码上重跑 JMH/JFR 收敛循环，**严格字面终止条款 = 连续两轮无候选通过保留条件**（2265 终止系实质性裁定）。
- Phase 1 基线重建完成（判定 JSON 证据 `_tmp/nop-rg-bench/`）：
  - σ_run 连跑 6 次（row0a-d i5 配方 + row0e-f i10 废弃）：**本机噪音显著高于 2265 时期**——期间出现外部负载（非本会话 node/chrome-headless 合计 2+ 核）与持续满载后热节流（row0e-f 1MB 崩至 271 ops/s）。**Phase 2 判定协议据此修订为共测配对**（基线/候选背靠背 3 对交替，配对增益对消漂移），已如实记录。
  - HotspotProfiler（64MB count 口径 12s/653 runs/3166 samples）：**LineCursor.indexOf 41.2%**、PreparedLiteral.readByte 23.3%、matchesAt 14.8%、aggregate 6.7%、advance 5.4%、RecursiveAction.exec 4.1%；isBinary 0.2% 排除。LF 扫描权重较 2265 末轮（8.1%）剧变（R4/R5 count 口径成主 workload 所致）；2265 R2 的 SWAR 否决系 TEXT workload 下测得，count 口径需重验。
  - Vector 场景矩阵（@Param 6 场景）：6B 向量=标量 72-80%（复现 2266 劣化）；**16B：2.5-3.5x 胜出；32B：1.6-1.9x**。e2e 配对判定（long-32B 64MB 共测 3 对交替）：**3/3 方向一致，中位 +5.2%**——长模式 SIMD 为真实 ≥2% 收益，触发收割。

### 中午（nop-rg R1：SIMD 长度阈值策略，plan 2267 保留项 1/循环）

- **R1 落地**：`NopRgVectorLiteralFinderProvider` 增加模式长度阈值策略（`SIMD_MIN_PATTERN_LENGTH=16`）——`--vector` 短模式返回标量等价 PreparedLiteral（消除 72-80% 回退），≥16B 走 SIMD（收割 e2e +5.2%）。crossover 取证：6B 劣化 / 16B 胜出 2.5-3.5x（vec-16b.json 收尾档），8-15B 未测保守归标量。
- 测试：vector 8→10（新增策略 2 例：短模式标量断言 / ≥阈值 Vector 断言）；fuzz 改造为跨阈值双路径 500 例（奇数迭代长模式植入真实命中，vectorPathCases ≥100 断言）——否则阈值策略会架空 Vector 等价性覆盖；testBoundariesMatchScalar 补长模式边界。vector 10/10 + core 53 + cli 25 全绿。
- Doc-sync：cli README `--vector` 行补阈值注记；design 决策 5 实现层级表更新（顺带修正 FoldingByteSearcher 陈旧行——f5d44be31e 已删除该类）；benchmark README Vector 章节重写（6 场景表格 + e2e 配对数据 + 策略说明）。
- 基准设施扩展（Phase 1）：CorpusUtil 生成器命中词/密度参数化（旧签名逐字节等价委托）；VectorCompareBenchmark 场景矩阵化；CoordinatorEndToEndBenchmark 加 scenario/strategy @Param（long-32B corpus 落独立子目录 `64mb-long/` 防 ensureFile 复用污染）。
- Fix：SearchCoordinator.java:35-38 类 javadoc 整块按 live 行为重写（FoldingByteSearcher 失效 link、VECTOR 抛异常陈旧表述、ChunkedFileReader 历史叙事）；grep 全仓零残留。ScalarByteSearcher 失效 import 同步移除。
