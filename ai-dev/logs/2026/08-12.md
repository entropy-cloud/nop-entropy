### plan `2026-08-12-2311-1-ai-invariant-p2-engine-split-hygiene.md` closure：独立 closure audit APPROVED + 计划关闭（AR-5/6/7 引擎拆分残留收口）

全 plan 关闭（Phase 1/2/3 全 completed + Closure Gates 全勾 + Plan Status: completed）。独立 closure audit（fresh session `ses_009434f0effenc1Ku2MfoFoTuu`，review-only）：**APPROVED**——Phase 1 八条 Exit Criteria 逐条 PASS（12 engine 文件 + 2 测试文件重复 import 反查零命中 / 14 受处理文件 check-import-order 逐文件 0 报错、模块 164 ≤ pre-existing 176 基线 / git diff 仅 import 块文本变化 / compile exit 0）；Phase 2 七条逐条 PASS（8 类 logger `getLogger(<本类>.class)` 行号逐一核实 / 全模块 `getLogger(DefaultAgentEngine.class)` 仅剩 `DefaultAgentEngine.java:101` / `TaskDispatchCoordinator.java:37` git diff 为空 / 5 测试类 appender 目标全 `AgentStartupWarnings.class` / focused 34 测试 0 失败）；Phase 3 七条逐条 PASS（四 setter :943-979 全拒绝 `<=0` / `@ParameterizedTest` 8+4 cases 26/26 绿 / 调用面 grep 零传 0、baseline 快照目录含历史副本证排除必要 / 三守卫 watch-only residual / catalog :43 行锚点 `:147-150` 修正 + AR-7 标注）；Closure Gate 全 PASS（compile / 3437 tests 0 fail / 8999 tests 全 nop-ai 0 fail / checkstyle `-Pqa` EXIT=0 / scan-hollow 2 条 pre-existing 零新增 / checklist exit 0）。Anti-Hollow PASS：AR-7 `assertThrows` 逐 setter 逐值真实执行、AR-6 appender 断言真实检查捕获事件（空壳必红）。审计 0 Blocker/0 Major；2 条 informational（roadmap ✅ 先于 plan 状态翻转 = 工作流一致；176 vs 177 基线口径差异无害）。`check-plan-checklist.mjs --strict` exit 0（0 unchecked + Closure Evidence 已写入）。roadmap `## Follow-up Backlog` AR-5/6/7 已勾选 + Work Item 表 P2 行 ✅（plan 路径 + 验证摘要）。doc-links 20 errors 全 pre-existing（本 diff 零新增，`nop-ai-invariant-loop-roadmap.md:5` 为未改动旧行）。
- 验证汇总：`./mvnw compile -pl nop-ai -am -q` EXIT=0；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS 8999 tests 0 failures（含五族门禁）；checkstyle `-Pqa` EXIT=0（默认-config 直跑 9164 pre-existing violation 全在未触碰上游 nop-api-core，I5/I6 同口径）。
- Commit: 收口提交（fix + docs）。

### plan `2026-08-12-2311-1-ai-invariant-p2-engine-split-hygiene.md` 执行：Phase 3（AR-7 超时 setter 校验统一）完成

mission-driver（`2026-08-12-111911`）执行 P2 引擎拆分卫生 plan 的 Phase 3（AR-7）。**Phase 3 完成，零偏差。**

- **裁定（Decision）**：四个超时 setter 统一拒绝 `<=0`——catalog INV-2 已声明该契约、live 是漂移侧（原仅 `setCallAgentTimeoutMs`/`setMemberExecTimeoutMs` 拒绝，`setLlmTimeoutMs`/`setToolTimeoutMs` 无校验）；0 值 live 语义三条路径分裂（`LlmCallCoordinator:606` 禁用超时 / `SingleTurnExecutor:132` 立时超时 / `AgentToolDispatcher:230` 静默禁用），统一契约防 per-setter 漂移复发。
- **Fix**：`DefaultAgentEngineConfig` `setLlmTimeoutMs`/`setToolTimeoutMs` 增加与既有两 setter 相同风格（`NopAiAgentException` + 英文消息）的 `<=0` 拒绝；默认值（120s/120s/300s/DEFAULT_MEMBER_EXEC_TIMEOUT_MS）与公共 API 签名零变更。
- **Proof（调用面）**：`grep setLlmTimeoutMs/setToolTimeoutMs nop-ai/`（排除 `ai-dev/` 证据目录与 target）唯一调用面 = `DefaultAgentEngine` builder（:261-262）+ 透传 setter（:485/:489），零传 0 的合法调用；beans.xml 零 timeout 配置项；仅测试断言拒绝路径（`TestEngineConfigAndHelpers:116` / `TestDefaultAgentEngine:268`）。
- **Proof（逃生口）**：`LlmCallCoordinator:606`/`AgentToolDispatcher:230`/`SingleTurnExecutor:132` 守卫在 setter 拒绝后仍可由直接构造路径到达——live 核对无生产装配路径传 0，逃生口仅为构造防御，登记 watch-only residual（plan Deferred But Adjudicated 已裁定，本轮再次核对确认）。
- **Fix（测试）**：`TestEngineConfigAndHelpers` 新增 `@ParameterizedTest` ×2——4 setter × {0, -1} 拒绝（`configRejectsNonPositiveTimeoutSetter`，8 cases）+ 4 setter 正值 1000 接受且 getter 回读相等（`configAcceptsPositiveTimeoutSetter`，4 cases）；既有默认值断言（:94-96）保持。
- **Proof（catalog 同步）**：§2 INV-2 AUDIT-14-03 行锚点 `DefaultAgentEngineConfig.java:145-147` → live `:147-150`（字段行号漂移已修正），并标注 AR-7 统一「四个超时 setter 全部拒绝 `<=0`」；§3.2 表内 INV-2 相关行（row 12/15 等）核对无 setter 契约漂移。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -Dtest=TestEngineConfigAndHelpers -Dsurefire.failIfNoSpecifiedTests=false` BUILD SUCCESS 26/26；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS **3437/3437 全绿**（新增 12 参数化 case）；`check-import-order` 受处理文件零报错（模块 164 pre-existing 基线不变）。
- Doc-sync: `ai-dev/audits/nop-ai-invariants/invariant-catalog.md` §2 INV-2 已同步（行锚点 + AR-7 统一契约）。
- Commit: 待三 Phase 完成后统一收口提交。

### plan `2026-08-12-2311-1-ai-invariant-p2-engine-split-hygiene.md` 执行：Phase 2（AR-6 拆分类 Logger 归属修正）完成

mission-driver（`2026-08-12-111911`）执行 P2 引擎拆分卫生 plan 的 Phase 2（AR-6）。**Phase 2 完成，零偏差。**

- **生产 8 类 logger 归属修正**：`AgentExecutorResolver`/`AgentCallDelegate`/`AgentSessionLifecycle`/`AgentTeamBinder`/`AgentStartupWarnings`/`SessionLockRenewal`/`AgentSessionSupport`/`DefaultAgentEngineConfig` 全部 `getLogger(DefaultAgentEngine.class)` → `getLogger(<本类>.class)`；`DefaultAgentEngine.java:101` 保留自身；`TaskDispatchCoordinator.java:37`（`getLogger(TeamTaskSchedulerDaemon.class)`，audit 裁定误报项）零改动（git diff 确认）。
- **5 个耦合测试类 appender 目标同步**：`TestSecureByDefault:91`/`TestLayer23SecureDefaults:116`/`TestSecureDefaultsInfoAwareness:67`/`TestAuditLoggerDefault:105`（`Slf4jAuditLogger` 目标 :110 不动）/`TestUsageRecorderWiring:121,153` 挂接目标 `DefaultAgentEngine.class` → `AgentStartupWarnings.class`（实际发射类；TestUsageRecorderWiring 补 import）。
- **机械反查**：`grep -rn "getLogger(DefaultAgentEngine.class)" nop-ai-agent` = src/main 仅剩 `DefaultAgentEngine.java` 自身，src/test 零命中。
- **验证**：5 个 focused 测试类（34 测试）全绿；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS 3425/3425 全绿（首轮 `-T 1C` 并行偶发 MojoFailure 一次，同命令复跑 PASS——pre-existing 并行 flaky 模式，与日志先例一致）。
- Doc-sync: No owner-doc update required（生产行为不变，日志路由 = 可观测面归位）。
- Commit: 待三 Phase 完成后统一收口提交。

### plan `2026-08-12-2311-1-ai-invariant-p2-engine-split-hygiene.md` 执行：Phase 1（AR-5 重复 import 全量去重）完成

mission-driver（`2026-08-12-111911`）执行 P2 引擎拆分卫生 plan 的 Phase 1（AR-5）。**Phase 1 完成，零偏差。**

- **去重范围**：nop-ai-agent `src/main` 12 个已知文件全部实锤去重（AgentCallDelegate 7/DefaultAgentEngine 16/AgentSessionLifecycle 10/AgentToolPlanResolver 7/SessionLockRenewal 6/AgentPromptAssembly 5/AgentSecurityConsultation 5/AgentSessionSupport 5/AgentLoopGuard 4/AgentTeamBinder 4/AgentStartupWarnings 2/ReActAgentExecutor 1 条重复 import 各保留 1 份）。
- **机械反查扩 scope**：nop-ai 全模块组反查发现 2 个测试文件含重复 import（`TestHookInReActLoop`（NopAiAgentException ×2）、`TestFeishuConversationE2E`（ChannelSession ×2））——按 plan Proof 项当场纳入 scope 一并去重；且两测试文件原 import 块违反分组约定（static imports 前置于 java 组 / 乱序），去重时按分组约定整理（static imports 末位）。其余模块零重复 import 文件。
- **验证**：`grep -o '^import .*;' | sort | uniq -d` 对 nop-ai 全模块（src/main + src/test）输出为空；`node ai-dev/tools/check-import-order.mjs --module nop-ai` 12 个受处理文件 + 2 个测试文件全部零报错，模块总错误 176 → 164（pre-existing 基线单调下降，无新增）；`git diff` 仅 import 块文本变化（机械脚本 `_tmp/dedupe-imports.py`，逐行核对无行为变更）。
- **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` EXIT=0。
- Doc-sync: No owner-doc update required（纯代码文本卫生）。
- Commit: 待三 Phase 完成后统一收口提交。

### mission-driver draft-plans（`2026-08-12-111911`）：P2 五项（AR-5~9）路由 3 份 plan（draft → 多轮独立 review 达共识 → active）+ roadmap backlog 同步

mission-driver（`2026-08-12-111911`）draft-plans step：Cycle 1/2 全部计划已 closed，剩余可路由工作 = roadmap `## Follow-up Backlog` 5 个未勾选 P2 项（AR-5~9，2026-08-12-1119 audit）。全部 5 项 live 核实实锤（重复 import 12 文件 / 8 类 logger 挂名 / 2/4 setter 校验不对称 / 负例 fixture 写 src/main / dispatchTimeoutMs 零接线），按结果面拆 3 份 plan 并路由：

- **plan `2026-08-12-2311-1-ai-invariant-p2-engine-split-hygiene.md`（active）**：AR-5 + AR-6 + AR-7（nop-ai-agent 引擎拆分残留）。**R1 三 Blocker 全修**：B1 check-import-order 模块级归零不可达（实测 176 errors/168 files pre-existing + 工具只查顺序不查重复 + `_gen` 排除 bug）→ 改为「受处理文件零报错 + 重复 import 反查归零」；B2 重复 import 实为 **12 文件**（非仅 AgentCallDelegate）→ Phase 1 扩为全量去重；B3 5 个测试类（TestSecureByDefault/TestLayer23SecureDefaults/TestSecureDefaultsInfoAwareness/TestAuditLoggerDefault/TestUsageRecorderWiring）把 ListAppender 挂 `getLogger(DefaultAgentEngine.class)` 断言 AgentStartupWarnings 告警——logger 改名后必红 → Phase 2 纳入测试 appender 目标同步 + focused test。M1 修正：llmTimeoutMs=0 实为三条路径语义分裂（LlmCallCoordinator:606 禁用 / SingleTurnExecutor:132 立时超时 / AgentToolDispatcher:230 静默禁用）——裁定（统一拒绝 `<=0`）不变、论据换实测；M2 catalog 同步范围扩至 §2 INV-2 全文 + §3.2。R2 复核：Blocker 清零、Major 清零，共识 executable（含实证：8/12 文件 11 条 import-order 报错全由重复 import 引起，去重后自然归零）。
- **plan `2026-08-12-2311-2-ai-invariant-p2-gate-test-fixture-hygiene.md`（active）**：AR-8（负例 fixture 写 src/main → 迁移 `target/gate-fixture/` + 扫描根参数化 + 递归目录清理）。**四轮 review**：R1 Blocker（Maven 选择器须路径形式 `-pl nop-ai/nop-ai-agent`）+ Major（中断残留模拟缺失）；R2 确认 + 新 Major F1（模拟空转：测试 finally 递归删 FIXTURE_ROOT 导致产物检查失据）；R3 确认 + 新 Major（`mvn clean` 先删残留，模拟仍空转）→ 顺序钉死 = `mvn clean` → 写独立类名残留（DefaultKilledResidualFixture）→ `mvn compile`（不带 clean）→ find target/classes 零命中 → 正例先行单跑（补 surefire flag）→ 全类；R4 复核 Blocker/Major 清零，共识 executable。
- **plan `2026-08-12-2311-3-ai-invariant-p2-gateway-dispatch-timeout-wiring.md`（active）**：AR-9（dispatchTimeoutMs 死旋钮 → beans.xml `@cfg:nop.ai.gateway.channel.dispatchTimeoutMs|30000` 接线 + IoC 接线验证）。**两轮 review**：R1 两 Major——(1) `getDispatchTimeoutMs()` 不存在 → 观测改为反射读字段（镜像 readMessageService:180-188 先例，不新增 getter）；(2) 覆盖断言钉死 `AppConfig.getConfigProvider().assignConfigValue(...)` + 设于 container.start 前 + finally 复位（防跨测试泄漏）、删除「test beans.xml property」弱路径；R2 复核 Blocker/Major 清零（@cfg 求值链 ConfigExpressionProcessor→ConfigValueResolver→BeanContainerImpl→AppConfig 端到端核实、91 处生产先例、双 test beans.xml 同步点核实），共识 executable。
- **roadmap 同步**：`ai-dev/backlog/nop-ai-invariant-loop-roadmap.md` `## Follow-up Backlog` 5 项各附路由 plan 路径（AR-7 附裁定方向）；checkboxes 保持未勾（执行完由执行步骤勾选）。
- 备注：本 step 纯计划拟制 + 文档同步，无代码变更，无 commit。

### mission-driver verify（`2026-08-12-111911`）：post-closure full-green 复验 + 日志 entry（plan-2026-08-12-2050-2 已 closed）

- **full-green 复验（mission-driver verify step，2026-08-12）**：working tree clean（plan 已收口，无 commit 步骤）；`./mvnw compile -pl nop-ai -am -q` EXIT=0；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw checkstyle:check -Pqa -pl nop-ai -am -q` EXIT=0 全绿（默认-config 直跑 checkstyle:check 报 9174 条 violation 全在未触碰上游模块 nop-api-core，零 nop-ai 命中；插件根 pom 注释未启用、仅 `-Pqa` profile 生效，I5/I6 同口径）；`./mvnw test -pl nop-ai -am -T 1C -fae` **BUILD SUCCESS 全绿**（全 reactor 含 nop-stream-rocksdb/nop-stream-runtime/全部 27-nop-ai 模块 SUCCESS）。
- **flaky 记录（pre-existing，非本 mission 引入）**：full 并行 `-T 1C` 下首两轮出现上游 `nop-stream` 时序敏感测试偶发失败——round-1 `TestRocksDBIncrementalRestoreAndBenchmark`（incremental 快照 ratio=2.28 超阈）、round-2 `TestSupervisionLoopReconnectE2E`；两测试独立运行均 PASS（EXIT=0），末轮全量 `-fae` 复跑双双 SUCCESS。`git log` 实证该模块最后改动为 plan-2026-08-06-1228-1（非本 mission 工作集，7af042cf2 仅触及 nop-ai）。
- Commit: 本次仅日志 entry，无代码变更。

### plan `2026-08-12-2050-2-ai-invariant-timeout-stop-work-semantics.md` closure：独立 closure audit APPROVED + 计划关闭（AR-2/3/4 timeout 语义收口）

全 plan 关闭（WS1-4 全 completed + Closure Gates 全勾 + Plan Status: completed）。独立 closure audit（fresh session `ses_0098af0f6fferNvWH3ETu5FK37`，review-only）：**APPROVED**——WS1 八条 Exit Criteria 逐条 PASS（live 证据：`DefaultMemberSpawner.java:188` 有界 get + 四类异常显式 catch → spawnFailed / `MemberFanOutDispatcher.java:387` SPAWN per-target orTimeout（BOUND :326）/ `TestSpawnDispatchTimeout` 3 测试（阻塞 mock → 有界失败 outcome + 队列 drain + 真实 spawner e2e）/ `TestSpawnStepTimeoutHonestFailure` 单线程池 probe worker 释放 / 接线判别 = 300ms vs 120s 默认）；WS2 七条逐条 PASS（`TeamTaskFlowOrchestrator.java:787` computeGraphDepth + :820 overallTimeoutMs = memberExecTimeoutMs × maxDepth + :634 orTimeout + :655 TimeoutException 限定 taskRt.cancel / `TestTeamTaskFlowOrchestratorOverallTimeout` 合法 2 层流不误杀 + 真实 taskRt isCancelled+reason 断言）；WS3 六条逐条 PASS（`ChannelMessageServiceImpl.java:427` raw.cancel(true) + :435 worker.interrupt() + workerRef/cancelled 竞态守卫 / `TestChannelFanOutTimeoutCancel` interrupt 标志 + 池 probe / mode-2 裁定不 cancel 已记录）；WS4 六条逐条 PASS（countCodeOccurrences 去注释/去字符串 + minOccurrences + 两模块负例 + 棘轮红/绿实跑对照 + catalog/gate-gaps 同步）。Closure Gate 全 PASS。Anti-Hollow PASS：三条调用链运行时连通（step→supplyAsync→有界 get→spawnFailed→失败结果；executeAsync→orTimeout→unwrap→cancel；fan-out→raw.cancel+interrupt→listener 中断→池 probe），无空壳/静默跳过；scan-hollow 仅 2 条 pre-existing 零新增。审计 0 Blocker/Major；2 Minor（catalog 行号陈旧，doc-only）——已修复（表 16 :581 / 表 17 :329 / step 创建点 :934）。`check-plan-checklist.mjs --strict` exit 0（0 unchecked + Evidence 已写入）。Commit: `7af042cf2`（fix）+ `a33353a25`（docs）。

### plan `2026-08-12-2050-2-ai-invariant-timeout-stop-work-semantics.md` 执行：AR-2/3/4 timeout 语义统一（超时 = 停止工作）+ 门禁②分支级升级（WS1-4 全完成）

mission-driver（`2026-08-12-111911`）执行 P1 plan（AR-2 SPAWN 双路径 / AR-3 orchestrator 整体 deadline / AR-4 channel fan-out cancel / 门禁②分支级判定）。**WS1-4 全部完成，零偏差。**（注：接手时发现上一轮中断运行已落 5 个 main 源文件未提交改动——SpawnMemberRequest 字段/构造器 + DefaultMemberSpawner 有界 get + MemberFanOutDispatcher SPAWN orTimeout + SpawnMemberAgentTaskStep 构造器 + orchestrator :810 调用点——本次核验为正确实现并补齐其余全部工作。）

- **WS1（AR-2 SPAWN 双路径 per-member timeout）**：
  - 核验先落代码：`SpawnMemberRequest.memberExecTimeoutMs` 字段 + 5 参构造器（旧构造器默认 `DEFAULT_MEMBER_EXEC_TIMEOUT_MS`，拒绝非正数）；`DefaultMemberSpawner.spawnMember` 有界 `future.get(request.getMemberExecTimeoutMs(), TimeUnit)` + `TimeoutException`/`InterruptedException`（恢复中断标志）/`ExecutionException`/`CompletionException` 显式 catch → `SpawnMemberResult.spawnFailed` 诚实失败（checked 异常不漏出）；`MemberFanOutDispatcher.spawnOneTarget` per-target `orTimeout(memberExecTimeoutMs)`（两层防护：spawner 层 = 生产 engine 挂起时 worker 必释放；dispatcher 层 = 阻塞 mock 场景 dispatch future 有界返回）；`SpawnMemberAgentTaskStep` 构造器 + `TeamTaskFlowOrchestrator:810` 创建点传 `memberExecTimeoutMs`。
  - **Fixture A**（`TestSpawnDispatchTimeout`，scheduler 包，3 测试）：(1) 直接 `MemberFanOutDispatcher.dispatch` + 方法体内阻塞 mock spawner（同步契约，mock 必须 block 在方法体内）→ dispatch 在 memberExecTimeoutMs 内返回失败 outcome（cause 链含 TimeoutException）+ task 留 CLAIMED；(2) `TaskDispatchCoordinator.dispatchClaimedTask`（SPAWN target）→ 队列 drain（新增 package-private `inFlightDispatchCount()` 观测）→ 0 + task CLAIMED；(3) **e2e**：coordinator → dispatcher SPAWN → 真实 `DefaultMemberSpawner` → 挂起 engine → 有界 get 超时 → 队列 drain + task CLAIMED（plan Exit Criteria 端到端链完整走通）。
  - **Fixture B**（`TestSpawnStepTimeoutHonestFailure`，flow 包）：真实 `DefaultMemberSpawner` + 挂起 `IAgentEngine` → orchestrator.execute 在 memberExecTimeoutMs 内返回失败结果 + task CLAIMED + **worker 释放断言**（单线程 spawn 池：超时后 probe 任务在 2s 内完成——worker 不再 parked）。
  - **接线验证**：fixture B 配置 300ms、断言 5s 窗口内 settle——若 timeout 未从 `SpawnMemberRequest` 传到执行点，等待将是 120s 默认（测试必红）；DefaultMemberSpawner LOG 实证 "timed out after 300ms"。
  - **Proof（双层职责分工）**：spawner 层有界 `get()` = 生产 worker 释放（fixture B 单线程池 probe 断言）；dispatcher 层 orTimeout = dispatch future 有界返回（fixture A）；阻塞 mock 的 worker 不被 orTimeout cancel（JDK CF.cancel 不中断运行中任务——与 WS3 同因），故 worker 释放只由 fixture B 断言（按 plan 分工）。
  - **Proof（inFlightDispatches）**：SPAWN future 有界化后 orTimeout 使 future 异常完成 → `whenComplete`（:247-248）必触发 → 条目必移除（fixture A 测试 2/3 断言 drain 到 0）；无残余不 settle 路径，无需整体 deadline 兜底。
- **WS2（AR-3 orchestrator 整体 deadline 语义）**：**Decision 裁定**：整体 deadline = `memberExecTimeoutMs × maxDepth`（maxDepth = 图最长依赖链，`computeGraphDepth` build 期从 blockedBy DAG 计算，饱和乘法防溢出）——不引入独立配置项（maxDepth 可得，plan 风险 3 不触发）。**Fix**：`executeAsync` 用 `built.overallTimeoutMs`（解耦）；`exceptionally` 先 unwrap（`orTimeout` 以 `CompletionException(TimeoutException)` 送达）再 `instanceof TimeoutException` 限定 → 仅整体超时时 `taskRt.cancel(ICancellable.CANCEL_REASON_TIMEOUT)`（nop-task `GraphTaskStep` 调度器检查 cancel token 后不再启动后继节点）+ 取消失败 LOG 显式记录（"底层图可能仍在执行"）+ 超时 LOG（deadline/maxDepth/memberExecTimeoutMs）；普通节点失败不触发整图 cancel（保持 GraphTaskStep 短路语义）。**行为测试**（`TestTeamTaskFlowOrchestratorOverallTimeout`）：合法 2 层顺序 DAG（每成员 200ms 实工作、per-member 300ms、总 400ms > 旧整体 300ms）→ 成功不被误杀（判别性）；2-bound-target fan-out + **永不 settle 的 reduce**（成员已完成、无 per-member 覆盖的挂起点）→ 整体 deadline 内失败 + **cancel 已调用**（`RecordingTaskFlowManager` 捕获真实 taskRt：`isCancelled()==true` + `getCancelReason()=="timeout"`）+ task 留 CLAIMED。
- **WS3（AR-4 channel fan-out cancel 原始 future）**：`fanOutToListeners` 保留 `raw` runAsync future 引用 + 超时路径 `raw.cancel(true)`。**实现裁定（live 实证）**：运行时 JVM（zulu-26）的 `CompletableFuture.runAsync(...).cancel(true)` **不中断运行中任务**（AsyncRun 无 thread 引用，_tmp/cf-probe 实证：interrupt flag=false + 池线程仍 busy）——因此增加**显式 worker 线程中断**：runnable 内捕获当前线程（`workerRef`，必然在池线程上运行）+ `cancelled` AtomicBoolean（超时前 worker 未启动时启动即自中断）；超时路径 `worker.interrupt()`；runnable finally 消费中断标志 + 按中断抛 `CancellationException`（CF 完成丢弃，无泄漏）。对齐库内 SingleTurnExecutor/AgentToolDispatcher 中断 cancel 模式。**行为测试**（`TestChannelFanOutTimeoutCancel`）：挂起 listener（可中断 latch）→ dispatchInbound 快速返回（transport 不被阻塞）+ listener 在 dispatchTimeoutMs 超时路径被中断（interrupt 标志断言）+ 单线程池 probe 2s 内完成（worker 释放，无池线程泄漏）。**mode-2 裁定**（`dispatchInbound` sendAsync().orTimeout）：**不补 cancel**——sendAsync 是持久化等待面（at-least-once 契约），cancel 源 future 可能中止慢但进行中的 persist 破坏投递；I3 R-2-1 原判「超时只停等待、消费者须幂等」保留，理由记录。**门禁局限（WS3+WS4 双记录）**：marker 计数无法检测 cancel 回归（orTimeout 修复前后都在），cancel 语义只由本行为测试拦截。
- **WS4（门禁②分支级判定升级 + catalog 同步）**：
  - `InvariantGateSupport.countCodeOccurrences(source, marker)`：剥离 `//`、`/* */` 注释 + 字符串/字符字面量后计数（状态机实现；gateway 门禁测试内实现同语义副本，agent 版为准）。`Entry` 增 `minOccurrences`（默认 1）；`MemberFanOutDispatcher.dispatch` 行 = 2（BOUND :326 + SPAWN :387 各一个代码级 marker，注释 :308/:360/:364 不计）；orchestrator 行 = 1。
  - gateway 门禁按 **mode 拆两条目**（`[mode-1]`/`[mode-2]` 各 ≥1）——live 事实：`ChannelMessageServiceImpl` 修复前代码级 orTimeout 已有 2（:348 + :373），计数 ≥2 对 AR-4 前后零判别力；拆条后 mode-1 条目标记在 WS3 修复后仍为 1（orTimeout 不变，cancel 是行为不是 marker），判别力由拆条 + WS3 行为测试联合承担。派生逻辑按 mode 方法体范围判定。
  - **负例测试**：agent 门禁新增 `negative_insufficientBranchLevelMarkerCountIsRejected`（fake minOccurrences=2 对代码级计数=1 的 orchestrator 文件 → 红）；gateway 门禁新增同型负例（fake min=3 对实际 2 → 红）。
  - **棘轮红/绿对照（实跑记录）**：临时移除 SPAWN 分支 orTimeout（:387）→ 门禁红（`found 1 code occurrence(s)... but 2 required (branch-level judgment)`——注释里的 orTimeout 不算数，裸 contains/计数必然失灵）；恢复 → 绿。修复后代码级计数 2 vs 负例 1 对照成立。
  - catalog §3.2：表 15 行拆分/分支级标注（BOUND :326 + SPAWN :387）、表 16 行标注整体 deadline 解耦 + 超时 `ITaskRuntime.cancel` 语义、表 17 行按 mode 拆条 + cancel 语义 + 门禁局限记录；`SpawnMemberAgentTaskStep` 登记裁定 = **not-applicable**（timeout 契约由 spawner 层 `SpawnMemberRequest.memberExecTimeoutMs` → `DefaultMemberSpawner` 有界 get 承载，step 自身无独立等待面，fixture B 证据；若未来判定标准扩展再补表）。gate-gaps.yaml：gate-2 family 4 条 not-applicable 不变、无新增 missing-declaration、判定升级说明注释已同步。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -o -T 1C` BUILD SUCCESS（新增 8 测试：TestSpawnDispatchTimeout 3 + TestSpawnStepTimeoutHonestFailure 1 + TestTeamTaskFlowOrchestratorOverallTimeout 2 + 既有门禁 4 全绿）；`./mvnw test -pl nop-ai/nop-ai-gateway -am -o -T 1C` BUILD SUCCESS（新增 TestChannelFanOutTimeoutCancel 1 + 门禁 4 全绿 + 既有 channel 14 全绿）；`./mvnw test -pl nop-ai -am -o -T 1C` BUILD SUCCESS 全绿（full-green，含全部门禁族零命中）；checkstyle gate N/A（仓库未启用，pre-existing 同 I5/I6 口径，本 diff 零新增）；`node ai-dev/tools/check-import-order.mjs --module nop-ai-agent/nop-ai-gateway` 本 diff 文件零命中。
- Doc-sync: catalog §3.2 + gate-gaps.yaml 已同步（WS4 主体）；docs-for-ai 无门禁机制描述，No docs-for-ai update required。
- Commit: 本次收口提交。

### mission-driver verify（`2026-08-12-111911`）：full-green 验证 + plan-2026-08-12-2050 收口提交

- **full-green 验证（mission-driver verify step，2026-08-12）**：`./mvnw compile -pl nop-ai -am -q` PASS；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw checkstyle:check -pl nop-ai -am` = 9164 条 **pre-existing** violation 全在未触碰的上游模块（nop-api-core 等），本 diff 三文件零命中（checkstyle 插件在根 pom 注释未启用，I5/I6 同口径）；`./mvnw test -pl nop-ai -am -T 1C` **BUILD SUCCESS 全绿**（含 `TestSubAgentPermissionWiring` 7/7）；`node ai-dev/tools/check-doc-links.mjs --strict` = 20 errors 全为 **pre-existing**（其他 mission roadmap / 本 roadmap 未改动行），本 run 零新增。
- **收口提交**：fix 提交（`AgentExecutorResolver` 丢参修复 + `ReActAgentExecutor` accessor + 行为级断言测试）+ docs 提交（plan ×2 + audit 状态 + evidence + roadmap AR-1/backlog + 本日志）。

### plan `2026-08-12-2050-1-ai-invariant-resolver-contract-fix.md` closure：独立 closure audit APPROVED + 计划关闭（AR-1 修复收口）

全 plan 关闭（Phase 1/2 全 completed + Closure Gates 全勾 + Plan Status: completed）。独立 closure audit（fresh session `ses_009cd1051ffetrNrzNto5IB2j1`，review-only）：**APPROVED**——Phase 1 八条 Exit Criteria 逐条 PASS（live 证据：`AgentExecutorResolver.java:130` 修复行 / `ReActAgentExecutor.java:347-349` package-private accessor / `TestSubAgentPermissionWiring.java:312` assertSame / 负例红→绿记录 / git diff 仅三文件 / delegate :985-995 零丢参 / 三参 :162-163 双参使用）；Phase 2 三条逐条 PASS（9 类核对记录 + 抽查全参数实际使用 + 3418 tests 0 failures）；Closure Gate 全 PASS。Anti-Hollow PASS：调用链 test:308 → engine:989 → resolver:129-130 → 三参:139-140 → builder:162 → 构造器:183/239 → getter:347 → assertSame:312 运行时连通（负例红 = 链上有实义逻辑）。审计 0 Blocker/Major。
- **验证**：`check-plan-checklist.mjs --strict` exit 0（closure 后复跑，0 unchecked + Evidence 已写入）；`scan-hollow-implementations.mjs --module nop-ai --severity high` = 2 条 pre-existing 零新增（I5/I6 同基线）；`./mvnw compile -pl nop-ai/nop-ai-agent -am` PASS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS 3418/3418；checkstyle gate N/A（仓库未启用：nop-ai-agent 21349 条 pre-existing violation，本 diff 仅 LineLength 风格行，I5/I6 同口径）；`node ai-dev/tools/check-doc-links.mjs --strict` 记录输出。
- **收口**：plan `Plan Status: completed` + Closure Evidence 写入（audit session ID + 逐 exit criterion/gate 证据 + Anti-Hollow + checklist exit 0 + deferred 分类检查）；roadmap Work Item 表新增 AR-1 行（✅，plan 路径 + 验证摘要）。
- Doc-sync: No owner-doc update required（源码级契约修复 + package-private 测试支持，javadoc 已声明该行为）；No new test beyond plan（负例验证为临时改回-恢复，不留产物；行为级断言已并入既有测试类）。
- Commit: 待本次收口提交。

### plan `2026-08-12-2050-1-ai-invariant-resolver-contract-fix.md` 执行：Phase 1（AR-1 丢参修复 + 行为级测试）+ Phase 2（9 类参数传递完整性复查，零同类残留）

mission-driver（`2026-08-12-111911`）执行 P1 plan（AR-1 两参重载静默丢参）。**Phase 1 + Phase 2 全部完成，零偏差。**

- **Phase 1 修复（3 文件，diff 最小化）**：
  - `AgentExecutorResolver.java:130` 两参重载恢复为 `return resolveExecutor(model, toolAccessChecker, config.getPathAccessChecker());`（拆分前语义，`git show 2f3251589^:DefaultAgentEngine.java:3250-3251` 实证）。
  - `ReActAgentExecutor` 新增 `private final IToolAccessChecker toolAccessChecker` 字段 + 构造器赋值 + package-private `getToolAccessCheckerForTest()` accessor（镜像 `DefaultAgentEngine:560-561` 先例；非 public API）。
  - `TestSubAgentPermissionWiring.resolveExecutorPassesEffectiveCheckerToExecutor` 升级：`assertSame(wrapped, ((ReActAgentExecutor) executor).getToolAccessCheckerForTest())` 引用相等断言（保留 assertNotNull 防御）。
  - **负例验证（必做项）**：临时改回丢参实现 → 该测试红（`expected: ParentConstrainedToolAccessChecker but was: DefaultToolAccessChecker`，AssertionFailedError @:312）；恢复修复 → 绿。负例已记录。
  - Proof：三参重载（`AgentExecutorResolver.java:139-218` 两参均使用）+ javadoc 一致；`DefaultAgentEngine.java:985-995` 三个 delegate 无同类问题。
- **Phase 2 复查（9 类，零同类残留）**：
  - 自动化参数使用扫描（pre-split `DefaultAgentEngine@2f3251589^` vs live 8 引擎类，118 方法对）：**0 param-drop**（扫描器对 AR-1 模式有判别力——修复前两参重载会被标 PARAM-ONCE-DROP，修复后零命中）。
  - 逐类人工核对：`AgentCallDelegate`（messenger 单一事实源 = callDelegate，`DefaultAgentEngine:523/525` 委托链一致）；`AgentSessionLifecycle` 三处 `resolveExecutor` 调用点（:313/447/627）全部传 computed effective checkers（与 pre-split :2656-2659/:2837-2840 同构）；`AgentTeamBinder` autoBindLead/autoBindMember/resolveActorId 与 pre-split 逐行对照一致；`SessionLockRenewal` 5 方法参数全用；`AgentStartupWarnings` warnIfInsecureDefaults/warnIfNoOpUsageRecorder(config 参数使用)；`AgentSessionSupport` ensureSessionMailbox/loadAgentModel 一致；`DefaultAgentEngineConfig` 全 setter/getter 字段往返一致（regex 抽查 2 个"疑似"为多行 setter 假阳性）；`TaskDispatchCoordinator.dispatchClaimedTask` 与 pre-split `Daemon@9b21b271f^:923-1000` 逐行一致（仅 `memberExecTimeoutMs` state-injection 追加）；**TaskDispatchCoordinator logger（AR-6 误报项，`TeamTaskSchedulerDaemon.class`）按 plan 排除，只核对参数**。
  - 委托链抽查：`DefaultAgentEngine` 构造器（:345-370）config.setToolAccessChecker/pathAccessChecker/permissionProvider 等与 resolver 读取面一致。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -Dtest=TestSubAgentPermissionWiring` 7/7 绿；负例红 → 修复绿；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` **3418 tests 0 failures BUILD SUCCESS**（含 Phase 1 测试 + 门禁类 TestInvariantGate* 5 类全绿）。
- Doc-sync: No owner-doc update required（源码级契约修复 + package-private 测试支持，javadoc 已声明该行为）；No new test beyond plan（负例验证为临时改回-恢复，不留产物）。
- Commit: 待本次收口提交。

### audit `2026-08-12-1119` 分诊：P1 路由 2 份 plan（draft → 四轮独立 review 达共识 → active）+ P2 入 roadmap backlog + audit 状态 planned

mission-driver（`2026-08-12-111911`）对 open-ended audit 结果分诊：唯一 open audit = `ai-dev/audits/2026-08-12-1119-open-audit-nop-ai-invariant-loop.md`（4 P1 + 5 P2）。

- **P1 路由（1-3 份 plan 总量约束，按 closure surface 打包）**：
  - **Plan 1** `ai-dev/plans/2026-08-12-2050-1-ai-invariant-resolver-contract-fix.md`（AR-1）：`AgentExecutorResolver` 两参重载丢参修复（恢复 `resolveExecutor(model, toolAccessChecker, config.getPathAccessChecker())`）+ `ReActAgentExecutor` 增加 checker 字段 + package-private `getToolAccessCheckerForTest()`（镜像 `DefaultAgentEngine:560` 先例）+ 行为级 `==` 引用断言 + 负例验证 + 8 引擎类 + TaskDispatchCoordinator 参数传递完整性复查。**独立复查修正**：AR-6 中 `TaskDispatchCoordinator:37` logger 实为 `TeamTaskSchedulerDaemon.class`（audit 误报项，已同步 audit 文件 + roadmap backlog）；`ReActAgentExecutor` 无 checker 字段（直接传入 AgentSecurityConsultation），accessor 需先加字段。
  - **Plan 2** `ai-dev/plans/2026-08-12-2050-2-ai-invariant-timeout-stop-work-semantics.md`（AR-2/3/4）：timeout 语义统一（超时 = 停止工作）——WS1 SPAWN 双路径（`MemberFanOutDispatcher` SPAWN 分支 + `SpawnMemberAgentTaskStep`）per-member timeout 经 `SpawnMemberRequest.memberExecTimeoutMs` 新字段（非接口变更，3 构造点同步）+ `DefaultMemberSpawner` 有界 `get()`（Timeout/Interrupted/Execution 三 checked 异常显式 catch）+ dispatcher 层 SPAWN orTimeout **必做**（保证阻塞 mock 场景 dispatch 有界返回）；WS2 orchestrator 整体 deadline 解耦（×maxDepth 或独立配置项）+ 超时 unwrap 后 `instanceof TimeoutException` 限定 `ITaskRuntime.cancel`；WS3 channel fan-out cancel **原始 runAsync future**（非 orTimeout stage）；WS4 门禁②升级为**代码级（去注释+去字符串）occurrence 计数**（`MemberFanOutDispatcher` 修复前代码级 = 1（:326；:308 注释不计），修复后 = 2；channel 条目改按 mode 拆条——计数 ≥2 对 AR-4 前后零判别力）+ catalog §3.2 表 15/17 行拆分 + `SpawnMemberAgentTaskStep` not-applicable 登记裁定（fixture B 证据）。
- **Review 流程**：四轮独立子 agent（fresh session）对抗性审查（想象性执行 + live 引用核对）：R1 发现 P1-1（测试机制不可实现）/P2-1（timeout 传输未声明）/P2-2（SpawnMemberAgentTaskStep 遗漏）/P2-3（cancel 机制技术上错误）；R2 发现计数机制与跨 WS 矛盾（文件已含 2 个 orTimeout 文本出现）+ 注释污染计数；R3 发现 fixture A/B 矛盾（阻塞 mock 无法覆盖 step 路径，拆双 fixture）；R4 共识：两 plan executable as-is（4 Minor 已吸收）。达成共识后 Plan Status: draft → **active**。
- **P2 路由**：AR-5~9 全部追加 `ai-dev/backlog/nop-ai-invariant-loop-roadmap.md` `## Follow-up Backlog`（新建 section，各含来源 audit 路径；AR-6 附 live 复核修正）。
- **Audit 关闭**：`2026-08-12-1119-open-audit-nop-ai-invariant-loop.md` `Audit Status: open → planned`（4 P1 已入 plan，零悬挂）。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 0 error（本次零新增）；两 plan checklist 全空（未执行）。
- Doc-sync: No owner-doc update required（ai-dev 产物；门禁文档更新由 Plan 2 WS4 承载）；No new test required（本轮纯分诊 + plan 拟制，零产品代码改动）。
- Commit: 待提交（plan ×2 + roadmap backlog + audit 状态 + 本日志）。

### open-ended adversarial audit（`ai-dev/audits/2026-08-12-1119-open-audit-nop-ai-invariant-loop.md`）— Cycle 1 收口后的门禁盲区审查（4 P1 + 5 P2）

对 `nop-ai-invariant-loop` mission 执行开放式对抗审查（mission-driver 触发，review-only 零代码改动），产物落 `ai-dev/audits/2026-08-12-1119-open-audit-nop-ai-invariant-loop.md`。**核心结论：Cycle 1 五族门禁全绿但门禁判定存在"文件级 marker 检查 vs 分支级覆盖"盲区，I4 修复有两处分支未覆盖 + 一处 MA4.2-05 拆分静默丢参回归**。

- **P1 × 4（全部 live 证据核实）**：
  - AR-1：`AgentExecutorResolver.resolveExecutor(model, toolAccessChecker)` 两参重载静默丢弃参数（MA4.2-05 拆分前 `resolveExecutor(model, toolAccessChecker, this.pathAccessChecker)` vs 拆分后 `config.getToolAccessChecker()`——`git show 2f3251589^` 实证）；测试 `resolveExecutorPassesEffectiveCheckerToExecutor` 只断言 non-null，测不出 checker 生效。
  - AR-2：`MemberFanOutDispatcher.dispatch` SPAWN 分支无 per-member timeout（I4 R-2-3 只修了 BOUND 分支 `orTimeout`；`spawnOneTarget`→`DefaultMemberSpawner:174 future.join()` 无界）；daemon 路径 `inFlightDispatches` 条目永不 settle。门禁②文件级 `orTimeout` marker 检查对此盲区。
  - AR-3：`TeamTaskFlowOrchestrator.executeAsync:618` 整体 deadline 与 per-member deadline 同值（合法多层 DAG 可被误杀）+ 整体超时后不取消底层图执行（zombie）。
  - AR-4：`ChannelMessageServiceImpl.fanOutToListeners` orTimeout 不 cancel 底层 listener——池线程永久泄漏（`max(2, cores)` 池耗尽后 inbound 瘫痪）；与库内 `SingleTurnExecutor:146`/`AgentToolDispatcher:250,268` 的 cancel 模式不一致。
- **P2 × 5**：`AgentCallDelegate` import 块三重复制（`check-import-order.mjs` 实报 2 错）；9 个拆分类 logger 全部挂名 `DefaultAgentEngine`（日志归属漂移）；`DefaultAgentEngineConfig` 4 个 timeout setter 仅 2 个拒绝非正数（catalog 声称 4 个）；gate-1 负例测试把 fixture 写进 `src/main`（中断残留污染产物）；`dispatchTimeoutMs` javadoc 声称可配置但无任何接线（死旋钮）。
- **验证**：`node ai-dev/tools/check-ai-tool-executor-boundary.mjs` PASS（30 实例零缺口）；`node ai-dev/tools/check-fix-commit-diff.mjs` PASS（9 fix commit 全实质 diff>0）；`check-import-order.mjs` 独立复现 AgentCallDelegate 2 错；门禁①②③⑤ live 代码行号逐条核对。未跑 `./mvnw test`（I5 已全绿；本次零代码改动，静态面已覆盖）。
- **后继**：P1 全部落在 catalog §6.3 复触发条件 2 的预判面（"行为级回归且五族门禁全绿"）；本报告产出后交 mission 消费方（Cycle 2 / 复触发裁决输入）。Doc-sync: No owner-doc update（ai-dev audit 产物）；No new test（review-only，修复待 plan 级裁定）。
- Commit: 待提交（audit 报告 + 本日志）。

### plan I6 循环收口（`2026-08-12-1700-2-ai-invariant-i6-loop-closure.md`）closure：独立 closure audit APPROVED + 计划关闭（Cycle 1 收口完成）

I6 全 plan 关闭（Phase 1/2/3 全 completed + Closure Gates 全勾 + Plan Status: completed）。独立 closure audit（mission-driver fresh session `2026-08-12-111911-mission-driver`，review-only）：**APPROVED**——三 Phase Exit Criteria 逐条 PASS、11 条 Closure Gate 证据全部 live 核验、Anti-Hollow PASS（统计逐项回源 live 证据：surefire XML 5 测试类 17 tests 0 fail 0 error / catalog 四表行数 / gate-gaps 终态 5 条 N/A；稳态判定 = 评估 plan completed + 裁定 `watch-only residual` 一致 → 稳态暂停；复触发条件每条含可判定判据）。**Cycle 1 收口结论 = 稳态暂停 待复触发**（零新族且零 red；复触发登记 = catalog §6.1-6.3，周期 = 30 天）。roadmap §I6 行 `done`（Cycle 1 全 7 行 closed，Phase 1/2 时翻转）。遗留观察项终态标注：scan-hollow 2 条 pre-existing（PlanReplanner:272 / NoOpProviderFailoverQueue:34）+ doc-links 20 条 pre-existing = 复触发登记内周期复探复核面（non-blocking）；探查工具化候选 = 评估裁定 watch-only（复触发条件 catalog §6.3）；CI fallback 补验 = catalog §6.1 #4 复触发项；门禁耗时优化 candidate = plan Deferred（判据未触发）；全部显式 non-blocking + 后继归属。
- **验证**：`plan-check.mjs --strict` exit 0（closure 时复跑）；`node ai-dev/tools/check-doc-links.mjs --strict` exit 1 = 20 pre-existing error 零新增；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0 + 2 条 pre-existing 零新增（同 I5 基线）；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（04:26，19:13 Finished；门禁①②③ 5 测试类 17 tests 全 0 fail——surefire XML 逐类核对）；checkstyle N/A（零产品代码改动）。
- **收口**：plan `Plan Status: completed` + Closure Evidence 写入（audit session ID + 逐 gate 证据 + Anti-Hollow + checklist exit 0 + deferred 分类检查）；roadmap §I6 行保持 `done`。
- Doc-sync: No owner-doc update required（全部为 ai-dev 配置产物）；No new test required（零产品代码改动）。
- Commit: 本次收口提交（含 Phase 1/2 产物 + closure 收口）。

### plan I6 循环收口（`2026-08-12-1700-2-ai-invariant-i6-loop-closure.md`）Phase 2：稳态判定 + 复触发条件登记 + mission json 授权文本同步

I6 Phase 2 完成（**纯文档收口，零产品代码改动**）——稳态判定（稳态暂停 待复触发）、周期复探周期裁定（30 天）、复触发条件登记定稿（catalog §6 唯一落点）、mission json 授权文本同步。

- **稳态判定（消费 `2026-08-12-1700-1` 裁定 = `watch-only residual`）**：评估 plan 状态 = completed → 消费裁定 → **「零新族且零 red」分支 → 稳态暂停 待复触发**。判定结果写入 roadmap §I6 行（含「Cycle 1 全 7 行 closed」标注，Phase 3 翻转）。
- **周期复探周期裁定**：**30 天**。理由 = 扫描成本近似零（mjs 秒级 + JUnit 门禁随 mvn test 零增量执行；结构变更已有表完备性即时拦截，周期复探仅兜底行为级漂移与 known-gaps 状态复核）——短周期发现窗口小，无成本压力。
- **复触发条件登记定稿（catalog 新建 §6 唯一落点，只增不改既有 §1-§5）**：6.1 四类基线（CI 变红 = `invariant-gates` job fail / mvn test 门禁①②③ surefire fail；结构变更 = 四表完备性即 red；周期复探 = 30 天；CI fallback 补验 = GitHub 通道首次可用 + 非空转判据「扫描到 fix(nop-ai) commit: N」N>0）；6.2 watch 项（AskOracleExecutor client 落地 / R-4-1 引入消费 dnsResolver 的 HTTP client / R-2-2 plan/runtime 接线，各含触发动作）；6.3 第六门禁族候选复评估触发条件（评估 §4.3 三条：周期复探 ≥2 起兄弟漏网 / 第 1 起行为级回归且五族门禁全绿 / Cycle 2 新族首轮 ≥2 起漏网——watch-only 裁定后其复触发条件已入登记，不断裂）。roadmap §Loop Rule 改为只留指针。
- **mission json 授权文本同步**：I3 裁定「P2 自动修复授权为 plan 级裁定」反映到 `missions/nop-ai-invariant-loop.json` 授权句（「P0/P1 自动修复预授权；P2 自动修复授权为 plan 级裁定（I3 裁定，2026-08-12）；公共 API 变更执行前人工确认」）——纯文档一致性动作（mission json 由引擎 JSON.parse 读取，description 仅显示不注入 prompt）；roadmap §Cross-Cutting 授权句同步一致。
- **验证**：`python3 -m json.tool missions/nop-ai-invariant-loop.json` exit 0（`mission-check.mjs` 工具不存在，按 plan 用 fallback）；`git diff` 确认仅授权句一处变更；`node ai-dev/tools/check-doc-links.mjs --strict` exit 1 = 20 pre-existing error 零新增（对照 I1-I5 基线同口径，退出码 1 即基线态）。
- Doc-sync: No owner-doc update required（roadmap/mission json/catalog 均为 ai-dev 配置产物）；No new test required（纯配置文本同步，guide rule 25 豁免注记）。
- Commit: 待本次收口提交。

### plan I6 循环收口（`2026-08-12-1700-2-ai-invariant-i6-loop-closure.md`）Phase 1：Cycle 1 统计落盘 + 前置校验通过

I6 Phase 1 完成（**纯文档收口，零产品代码改动**）——Cycle 1 四组统计数据落盘（daily log + roadmap §I6 行），与 live 证据逐项一致。

- **前置校验（fail-fast）**：I0-I5 六份 plan（`1120-1/2/3` + `1411-1/2/3`）全部 `Plan Status: completed`（live grep 逐文件核实）；第六门禁族评估 plan（`2026-08-12-1700-1`）= `completed` → **消费其裁定（`watch-only residual`）**；roadmap §I0-§I5 行全部 `done`（I6 前置注记行 ✅）。
- **统计落盘（来源可追溯）**：
  - 五族门禁 = INV-1~5（catalog §2），落地形态 = **3 JUnit/ArchUnit**（`TestInvariantGate1SecureDefault` + `TestInvariantGate1SecureDefaultShell` 门禁① / `TestInvariantGate2OrchestrationTimeout` + `TestInvariantGate2GatewayTimeout` 门禁② / `TestInvariantGate3EntryPointCleanup` 门禁③，共 5 测试类）+ **2 mjs**（`check-ai-tool-executor-boundary.mjs` 门禁④ / `check-fix-commit-diff.mjs` 门禁⑤，聚合 `pnpm check:ai-invariants`）+ **CI job**（`.github/workflows/maven.yml:48` `invariant-gates`）。
  - 目标集表 4 张 = Default* 33（§3.1）/ 编排入口 17（§3.2）/ ToolExecutor 30（§3.3，31 行表 − 1 抽象基类）/ entry-point 8（§3.4）。
  - red-list 44 条（门禁 39 + 探查 5）零悬挂 → 裁决 44/44（37 fix-I4 + 1 watch R-2-2 + 6 not-applicable-confirmed：gate-2 4 + AskOracleExecutor + R-4-1）→ I4 修复 37 决策落地 **5 commits**（计数以 I4 closure evidence 为准：I4 Status Note「裁决表 37 条 fix-I4 决策全部落地（5 个 commit）」，未从 git 复推）→ I5 full-green（test + pnpm check:ai-invariants + clean install 三绿，五族门禁零命中，known-gaps 仅 5 条 N/A 零 drift）。
- **数据一致性核对**：以上统计逐项回源 live 证据——gate-gaps.yaml 终态 5 条 N/A（gate-2 4 + gate-4 1，gate-1/gate-3 空）；adjudication §1 计数 44/44 零悬挂；5 个门禁测试类 + 2 mjs + CI job 全部在位；catalog 四表行数与 find/grep 复现命令一致（I5 Phase 2 已 diff 零）。**零虚构数字**。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 记录输出（exit 1 = 20 pre-existing 零新增，与 I1-I5 基线同口径）。
- Doc-sync: No owner-doc update required（ai-dev 产物，不触 docs-for-ai）；No new test required（纯文档收口）。
- Commit: 待本次收口提交。

### plan 第六门禁族评估（`2026-08-12-1700-1-ai-invariant-sixth-gate-evaluation.md`）closure：独立 closure audit APPROVED + 计划关闭

评估 plan 全 plan 关闭（Phase 1/2 全 completed + Closure Gates 全勾 + Plan Status: completed）。独立 closure audit（fresh session `ses_00aa7577fffedsAAv65FbjaJJl`，review-only）：**APPROVED**——两 Phase Exit Criteria 逐条 PASS、9 条 Closure Gate 全 live 核验、Anti-Hollow PASS（裁定可追溯：非高频 = Cycle 1 正证恰 1 起 R-2-3 + 根因 owner-doc drift 的机械承接；声明式重审门禁必然空转为机制级论证）。审计 2 Minor 均不影响裁定（`_tmp` 日志仅留存末次 maven 运行；复触发条件 (1) 周期锚定 I6 登记）。**裁定 = `watch-only residual`**——第六门禁族不沉淀，交 I6 稳态判定「零新族」分支消费。

- **验证**：`check-plan-checklist.mjs --strict` exit 0（审计时 + closure 时复跑）；`check-doc-links.mjs --strict` exit 1 = 20 pre-existing error + 3 warning 零新增（本 plan 四文件零断链）；`./mvnw test -pl nop-ai -am -T 1C` 4 次运行 3 次连续 BUILD SUCCESS（17:28/17:34/17:39；17:24 一次瞬时失败未复现，终态全树 surefire 零失败）；checkstyle N/A（零代码改动）。
- **收口**：plan `Plan Status: completed` + Closure Evidence 写入；roadmap Work Item 表下注记行（✅，评估 plan 路径 + 裁定状态，I6 行保持 `todo`）；catalog §2 `INV-6 候选` 条（不计入「5 条」计数）。
- Doc-sync: No owner-doc update required；No new test required（guide rule 25 豁免注记）。
- Commit: 本次收口提交。

### plan 第六门禁族评估（`2026-08-12-1700-1-ai-invariant-sixth-gate-evaluation.md`）Phase 2：裁定落盘完成（watch-only residual）

I6 前置评估 plan Phase 2 完成（**纯评估 + 裁定，零产品代码改动**）。**裁定 = `watch-only residual`**——第六门禁族不沉淀，I6 稳态判定走「零新族」分支输入。

- **裁定理由（Why Not Blocking Closure）**：(1) 证据盘点结论 = **非高频**（Cycle 1 正证仅 R-2-3 1 起，根因 owner-doc drift 已由补表 + 门禁②表完备性机械承接；I4 清扫零新缺口）；(2) 机械可验证核心（全表声明复查 + 表完备性）已由门禁①-④ 每次 CI 承担——候选声明级 = 现有门禁已执行的机械动作；(3) 行为级联动重审不可机械验证 → 声明式重审门禁必然空转（Anti-Hollow / Lesson 08 声明 ≠ 接线），成本 > 收益。
- **复触发条件（可判定，无模糊时间词，交 I6 复触发登记）**：(1) 周期复探发现 ≥2 起同族兄弟漏网实例；(2) 出现第 1 起「同族实例修改后兄弟行为级回归且五族门禁全绿」；(3) Cycle 2 派生新族且首轮审计 ≥2 起兄弟漏网 → 复核 tool-ize。
- **落盘三处一致**：评估文档 §4（裁定 + 理由 + 复触发条件）；catalog §2 新增 `INV-6 候选` 条（标注不计入 §2 标题「5 条」计数，含复触发条件供 I6 登记消费）；roadmap Work Item 表下新增注记行（✅，指向评估 plan + 裁定状态，非 I6 行本身）。
- **一致性核对**：watch-only 裁定逐条对应 Phase 1 证据（非高频 §2.3 / Anti-Hollow §3.3 / 边界核对 §5），无「以后再说」式裁定。
- **验证**：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（见 Phase 1 条目后复跑，docs-only 零代码改动）。
- Doc-sync: No owner-doc update required；No new test required（guide rule 25 豁免注记，零代码改动）。
- 后续：closure gates（checklist / doc-links / 独立 closure audit）→ Plan Status: completed，交 I6（`2026-08-12-1700-2`）稳态判定消费。

### plan 第六门禁族评估（`2026-08-12-1700-1-ai-invariant-sixth-gate-evaluation.md`）Phase 1：候选评估与证据盘点完成

I6 前置评估 plan Phase 1 完成（**纯评估 + 裁定，零产品代码改动**）。交付 `ai-dev/audits/nop-ai-invariants/sixth-gate-family-evaluation.md`（新建评估文档）。

- **候选定义**：探查工具化候选 = 将 I2「兄弟路径探查」与 I4「类别清扫」纪律自动化（某族任一实例被新增/修改时强制检查全族兄弟）；scope 决定登记 = 两者按同一模式合并评估（I2 有显式登记，I4 为执行纪律无独立登记，共享同一失败前提）。
- **证据盘点（如实读数）**：历史证据 = 无门禁时代「兄弟漏网」真实高频（Lesson 05 三例虚假关闭 / Lesson 08 四 P1 同源 / 6/5/3 兄弟族跨多轮审计才找全）。Cycle 1 实际数据 = I2 探查 5 条 finding 中仅 **R-2-3 是既有面上漏网实例（1 起正证）**，R-2-1/R-2-2/R-4-1 为门禁表外新面、R-5-1 为门禁精度；R-2-3 根因 = catalog §3.2 排除理由错误（owner-doc drift），已由补表 + 排除理由更正 + 门禁②表完备性机械派生承接（live 复核 `MemberFanOutDispatcher.java:326` / `TeamTaskFlowOrchestrator.java:618` orTimeout 在位）；I4 类别清扫执行记录 = 零新缺口。**结论：非高频**（当前门禁机械覆盖下，清扫纪律边际产出 = 1 起正证且已机械承接）。
- **检测方法候选规格 + Anti-Hollow**：形态 (a) commit 面重审声明门禁 / (b) 审计流程门禁；声明式重审门禁无法机械证明重审真实发生（Lesson 08 声明 ≠ 接线同构）= 空转门禁；行为级联动重审不可自动判定——**不存在满足机械可验证性的第六族门禁形态**。
- **边界核对**：与门禁①-④ 声明级无 overlap（全表复查已覆盖），行为级 = 现有门禁有意盲区；与门禁⑤ 语义可区分（实质 diff vs 联动重审）。
- **验证**：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（03:16，17:20 完成）；门禁零代码改动面。
- Doc-sync: No owner-doc update required（ai-dev 审计产物）。

### plan I4 nop-ai 不变式修复执行（`2026-08-12-1411-2-ai-invariant-i4-fix-execution.md`）closure：独立 closure audit APPROVED + 计划关闭

I4 全 plan 关闭（Phase 1-6 全 completed + Closure Gates 全勾）。独立 closure audit（fresh session `ses_00af483cbffeRgJDmN6YFvYDJl`，review-only）：**APPROVED**——六 Phase Exit Criteria 逐条 PASS、12 条 Closure Gate 证据全部 live 核验、Anti-Hollow PASS（调用链追踪：SingleTurnExecutor → callWithTimeout → supplyAsync(timeoutExecutor)+get(timeout)，AgentExecutorResolver:210-211 接线；fan-out :326 orTimeout + 超时 cancelSession + 诚实 engineFailed；orchestrator :618 orTimeout + exceptionally；gateway :348/:373 双 mode orTimeout + fail-loud）、端到端 PASS（挂起 → 超时 → 断言失败态 → 门禁绿）。2 Minor 已收口：Minor-1 slf4j import 顺序（MemberFanOutDispatcher + 全 I4 触碰文件复查仅此一处）→ 修复 commit `e9b2bee76` 后复跑 gate-2 3/3 + fan-out 2/2 绿；Minor-2 catalog §3.2 行号漂移（:152/:513/:208/:29/:305/:384 → live :157/:572/:326/:54/:326/:443，ReAct :339 / AgentToolDispatcher :165 / CallAgentExecutor :144 复核无漂移）→ 已刷新。
- **验证**：`check-plan-checklist.mjs --strict` exit 0（审计时 + closure 时复跑）；scan-hollow exit 1 = 2 条 pre-existing 基线（PlanReplanner:272 / NoOpProviderFailoverQueue:34，R-2-2 watch 未动代码 → 重跑无变化，符合 gate 条款）；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS；`pnpm check:ai-invariants` exit 0；doc-links 20 pre-existing 零新增；import-order 177 pre-existing error 与 I4 触碰 52 文件零重叠（零回归）。
- **收口**：roadmap I4 行 `todo`→`done`；plan `Plan Status: completed` + Closure Evidence 写入（audit task id + 逐 gate 证据）；known-gaps 终态 = 5 条 not-applicable（gate-2 4 + gate-4 1），零 missing-declaration。
- Doc-sync: `ai-dev/audits/nop-ai-invariants/invariant-catalog.md`（§3.2 行号刷新）、roadmap I4 翻转。
- Commit: `e9b2bee76` + 本次收口提交。

### plan I4 nop-ai 不变式修复执行（`2026-08-12-1411-2-ai-invariant-i4-fix-execution.md`）Phase 5-6：门禁⑤ R-5-1 修复 + 类别清扫终核收尾

I4 Phase 5-6 完成（R-5-1 subject-only 修复 + Phase 6 全绿收尾 + catalog 同步）。本轮改动：ai-dev 工具 1 文件（mjs）+ catalog 2 处 + plan 收口。

- **Phase 5 R-5-1（门禁⑤ subject-only 收窄）**：`check-fix-commit-diff.mjs` `collectFixCommits` 移除 `--grep=fix(nop-ai)`（`git log --grep` 逐行匹配 body，body 首行提及字样的 chore/feat commit 会被误扫），改为 `git log --no-merges --format=%H %s` 全量取 subject + JS 侧 `subject.startsWith('fix(nop-ai)')` 唯一候选过滤。self-test 增补反例 fixture：subject 为 `chore(ci): add invariant gate job`、body 首行以 `fix(nop-ai):` 开头的 commit → 断言排除出候选集；同步更新计数断言（本地 4 个 fix commit、PR 模式 4 个）。既有正反例（实质 diff 正例 / 仅空白 / 仅版权头 / 仅生成文件反例）保持通过。
- **Phase 5 实跑证据**：`node check-fix-commit-diff.mjs --self-test` exit 0 全 PASS（含新反例）；`--since 2026-07-31` 实跑 7 个 fix(nop-ai) commit 全部实质 diff>0 零违规，`5ebad065e`（chore(ci)）/`c1362dc77`（feat(ai)）不再被误报为候选；`pnpm check:ai-invariants`（④⑤聚合，含门禁④ self-test + 30 实例实判 + 门禁⑤）exit 0 全绿。
- **Phase 6 类别清扫终核（五族）**：gate-1 = `find` 复现命令 33 行 == catalog §3.1 表 33 条（零遗漏零新增）；gate-2 = `TestInvariantGate2OrchestrationTimeout` 3/3 + gateway 模块 `TestInvariantGate2GatewayTimeout` 3/3（17 入口表 = 14 原 + 3 补录，零缺口）；gate-3 = `TestInvariantGate3EntryPointCleanup` 3/3（8 行成对面无回归）；gate-4 = mjs 30 实例声明成立 PASS；gate-5 = subject-only 生效。
- **known-gaps 终态核对**：`gate-gaps.yaml` 仅剩 5 条 not-applicable（gate-2 4 条 forkSession/cancelSession/close/getSessionStatus + gate-4 1 条 AskOracleExecutor）；**零 missing-declaration 残留**；清单移除后门禁恢复实判且全绿（自校验通过）。
- **catalog 同步（Phase 6）**：§3.3 #30 `UpdateTodosExecutor` 安全敏感面「文件」→「内存/会话」（I3 裁决 §4 委托，live 为内存 ConcurrentHashMap 无文件 IO；门禁④ mjs 内嵌表 surface 已是 `memory`，双向一致）；§INV-5 检测方法新增 I4 修复记录（R-5-1 subject-only 机制 + 实跑证据）。
- **局部全量验证**：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（03:21，含门禁①②③ 17 tests 全绿：Gate1 agent 5 + shell 3、Gate2 agent 3 + gateway 3、Gate3 3）；`pnpm check:ai-invariants` exit 0；`node ai-dev/tools/check-doc-links.mjs --strict` = 20 个 pre-existing error 基线零新增（roadmap `docs/backlog/` 旧引用等均在基线内，nop-ai-invariants 目录零断链）。
- **偏差**：无（Phase 5 修复方式按裁决表 subject-only 主路径执行；self-test 计数断言按新增 fixture 调整为 4）。
- Commit: `6e74232bd`（Phase 5 mjs 修复）。
- Doc-sync: `ai-dev/audits/nop-ai-invariants/invariant-catalog.md` 已更新（§3.3 #30 + §INV-5）。

### plan I4 nop-ai 不变式修复执行（`2026-08-12-1411-2-ai-invariant-i4-fix-execution.md`）Phase 4：门禁④ R-4-1 dead-code 裁定记录（not-applicable-confirmed）

I4 Phase 4 完成（R-4-1 按 I3 裁决 not-applicable-confirmed 分支落地，零代码改动）。**本轮无产品代码改动**（纯 catalog 记录）。

- **R-4-1 dead-code 裁定记录**：catalog §3.3 新增记录块——`SsrfGuardDnsResolver`（:34，实现 IDnsResolver，fail-closed）生产未接线：默认 `JdkHttpClient`（nop-http-client-jdk）零 dnsResolver 消费（live grep 复核），消费点仅 `ApacheHttpClientHelper:100-109`（nop-http-client-apache，非任何 nop-ai 模块依赖）。接线路径说明（装配至消费 dnsResolver 的 client 时启用）+ 触发条件登记（引入消费 dnsResolver 的 HTTP client → 接线，复触发 I4/I5）。host 级主防线（`SsrfAddressGuard.validateHost`，HttpRequestExecutor:84 / GraphqlQueryExecutor:71 已接线）不受影响。gate-gaps **无变更**（非 ToolExecutor 实例，不在门禁④表；AskOracleExecutor not-applicable 条目保持）。
- **类别清扫**：门禁④机械扫描 30 实例（`node check-ai-tool-executor-boundary.mjs`）exit 0 PASS——全部声明成立，表完备性一致，零清单外缺口（INV-4 族全绿）。
- **验证**：`node ai-dev/tools/check-ai-tool-executor-boundary.mjs` exit 0（30 实例 + LocalToolFileSystem 接线检查 OK）。
- **Doc-sync**：`ai-dev/audits/nop-ai-invariants/invariant-catalog.md` §3.3 已更新（R-4-1 dead-code 记录）。
- Commit: 待本次提交。

### plan I4 nop-ai 不变式修复执行（`2026-08-12-1411-2-ai-invariant-i4-fix-execution.md`）Phase 3：门禁② timeout 声明补齐（SingleTurn + team-flow fan-out + gateway 派发）

I4 Phase 3 完成（门禁②全部 fix-I4 动作落地，test-first + 类别清扫 + known-gaps 移除 + catalog 同步）。**产品代码改动**：nop-ai-agent 7 文件 + nop-ai-gateway 1 文件 + 5 个测试文件（2 新增回归测试 + 3 既有测试适配）。

- **SingleTurnExecutor.execute（gate-2 唯一首跑 missing）**：构造参数新增 `long llmTimeoutMs` + `Executor timeoutExecutor`（2 参旧构造保留委派 120s + commonPool）；execute 内 `CompletableFuture.supplyAsync(() -> chatService.call(...), timeoutExecutor).get(llmTimeoutMs, TimeUnit.MILLISECONDS)`，TimeoutException → `future.cancel(true)` + 抛 CompletionException → 既有失败路径（status=failed + EXECUTION_FAILED 事件）。`AgentExecutorResolver:207` 注入 `config.getLlmTimeoutMs()` + `agentExecutorSupplier.get()`（与 ReAct 路径 :200/:202 同一配置源）。**行为级回归测试** `TestSingleTurnExecutor.testHangingChatServiceTimesOutAndFails`：chatService 挂起 30s → llmTimeoutMs=100 → execute 有界完成（<5s）、status=failed、error 含 timeout、EXECUTION_FAILED 事件。
- **R-2-3 team-flow fan-out（2 入口）**：新增唯一配置源 `DefaultAgentEngineConfig.memberExecTimeoutMs`（默认 120000，setter 拒绝非正数，公开常量 `DEFAULT_MEMBER_EXEC_TIMEOUT_MS` 供两装配路径同源）；`DefaultAgentEngine` 增 getter/setter 委派。`MemberFanOutDispatcher.dispatch` 增 `long memberExecTimeoutMs` 形参（4 调用点全在 nop-ai-agent 模块内更新）；`executeBoundMember` 对 `agentEngine.execute(request)` 加 `.orTimeout(memberExecTimeoutMs)`，超时 → `cancelSession(sessionId, reason, true)`（AUDIT-14-01 语义，对齐 CallAgentExecutor:473）+ `MemberExecOutcome.engineFailed`（诚实失败，任务保持 CLAIMED）。`TeamTaskFlowOrchestrator` 增字段/7 参构造/`setMemberExecTimeoutMs`，`executeAsync` result future 加 daemon 级 `.orTimeout(memberExecTimeoutMs)` 整体兜底（超时转诚实 failed result，future 不异常完成——契约不变）。fan-out step 三变体（Bound/Spawn/Mixed）构造传值；`TeamTaskSchedulerDaemon`（9 参新构造）+ `TaskDispatchCoordinator`（4 参新构造）构造链各增形参，旧构造委派共享常量。flow 装配点 `TeamExecuteFlowExecutor` 从 `engine.getMemberExecTimeoutMs()` 派生（非 DefaultAgentEngine 实例回退共享常量）。**回归测试** `TestMemberFanOutDispatchTimeout`：挂起 engine → memberExecTimeoutMs=150 → dispatch 有界返回（<5s）、outcome failed、cause 链含 TimeoutException、cancelSession 恰 1 次（session-m1）、任务保持 CLAIMED；另测非正 deadline fail-fast。
- **R-2-1 gateway 派发（补表条目）**：`ChannelMessageServiceImpl` mode-1 `fanOutToListeners` 每 listener `CompletableFuture.runAsync(..., fanOutExecutor).orTimeout(dispatchTimeoutMs)`；mode-2 `messageService.send`（阻塞 syncGet）→ `sendAsync(...).orTimeout(dispatchTimeoutMs)`（at-least-once 由 DBMessageService persist-before-return 承担）；超时/异常 fail-loud LOG.error。新配置旋钮 `dispatchTimeoutMs`（默认 30000，setter 拒绝非正数）+ `fanOutExecutor`（setter 注入，默认懒创建 daemon 池，`close()` shutdown）。**gateway 模块独立 gate 测试类** `TestInvariantGate2GatewayTimeout`（自包含 TABLE + 派生 + 负例，不依赖 InvariantGateSupport——包前缀约束）。既有 4 个 gateway 测试类适配异步 fan-out（awaitUntil 辅助：TestChannelMessageService / TestChannelInboundBackboneE2E / TestChannelMessageServiceIoC）。
- **门禁②测试同步（B1 自校验）**：agent TABLE SingleTurnExecutor.execute verdict missing→declared（marker=TimeoutException）+ 新增 2 条 team/flow 条目（marker=orTimeout，精确标记不用宽泛 TimeUnit）；`deriveEntryIds()` 扩展 team/flow 机械派生；头注释计数 14→16 更新。known-gaps gate-2 移除 3 条 missing-declaration（顺序 = 先测试实判通过再移除），仅剩 4 条 not-applicable-confirmed。
- **R-2-2 plan/runtime（watch 裁决）**：catalog §3.2 记录接线状态说明 + 触发条件（接线时补 timeout 契约 + 表登记），零代码改动。
- **catalog §3.2 同步（I4 Phase 3）**：判定标准扩展（④ team/flow fan-out 入口 + ⑤ channel 派发入口 + R-2-2 watch 记录）；排除理由更正（删除「team-flow 内部方法——不启动 agent 执行路径」→ live 事实「fan-out 经 :305 agentEngine.execute 启动 agent 执行路径」）；表 14→17 条（+3 补录条目含 I4 修复说明）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent,nop-ai/nop-ai-gateway -am -T 1C` BUILD SUCCESS（agent 423 surefire 类 + gateway 全绿零失败）；门禁② agent 3/3 + gateway 3/3；新增回归 11/11（SingleTurn 9 + fan-out 2）。
- **Doc-sync**：`ai-dev/audits/nop-ai-invariants/invariant-catalog.md` §3.2 已更新（判定标准 + 排除理由更正 + 表扩展 + R-2-2 记录）。
- Commit: 待本次提交。

### plan I4 nop-ai 不变式修复执行（`2026-08-12-1411-2-ai-invariant-i4-fix-execution.md`）Phase 1-2：裁决表消费 + 门禁① 33/33 类补 @SecureDefault

I4 开工（Phase 1 fail-fast 核对 + Phase 2 门禁①修复完成）。**本轮产品代码改动：33 个 Default* 类补注解 + 1 个门禁测试负例更新**。

- **Phase 1 裁决表消费（live 核对）**：I3 裁决表 44/44 零悬挂、每条 fix-I4 含机制级动作清单（零「或」二义）；零「人工确认」标记（§7 明示）。事实核查全过：`nop-http-client-jdk` src/main 零 dnsResolver 引用（JdkHttpClient 不消费）vs `ApacheHttpClientHelper:100-109` 消费但非任何 nop-ai 模块依赖（nop-ai-toolkit/core 仅 nop-http-api + client-jdk）——与裁决表 R-4-1 假设一致；R-2-3 deadline 来源 = 裁决表已定义 `DefaultAgentEngineConfig.memberExecTimeoutMs`（唯一配置源，flow/daemon 两路径同源）；R-2-1 测试放置 = gateway 模块新建独立 gate 测试类（`InvariantGateSupport` 硬编码 agent 包前缀约束成立，:35-37 live 复核）。fix-I4 动作汇总 37 条 == 裁决表 §1 计数。开工基线绿：`./mvnw compile -pl nop-ai -am -T 1C` exit 0；门禁①②③ surefire 全绿（agent 11 + shell 3）；门禁④⑤ mjs exit 0（30 实例 + 7 commit 零违规，含 `5ebad065e`/`c1362dc77` 两 body 误报候选确认）。
- **Phase 2 门禁①（33/33 补注解）**：`find` 复现命令 = 33 行 == 清单 33 条（无遗漏无新增）；33 类统一补类级 `@SecureDefault` + `io.nop.ai.api.secure` import（脚本机械处理，javadoc 与类声明之间插入注解；nop-ai-shell/toolkit/agent 经 nop-ai-api 传递依赖可编译）。known-gaps 33 条 gate-1 条目移除（清单清空，门禁恢复实判）。**棘轮反例验证**：临时去掉 `DefaultAgentEngine` 注解 → 门禁①红（declaration check 捕获）→ 恢复 → 绿（自校验通过，清单移除后门禁真在实判）。
- **门禁测试负例更新（I4 必要修订）**：`TestInvariantGate1SecureDefault.negative_logicRejectsInTableUndeclaredUnregistered` 原以 `TABLE.get(0)`（无注解）为受害类验证决策规则——修复后全部表内类有注解，测试改为用未注解的 `AgentExecutionContext`（决策规则与类名无关，四臂断言保持：无注解无登记 → violation / 注解 → pass / 登记 → pass / 双无 → fail）。
- **验证**：`./mvnw compile -pl nop-ai -am -T 1C` exit 0；`./mvnw test -pl nop-ai/nop-ai-agent,nop-ai/nop-ai-shell -am -Dtest='TestInvariantGate1*' -Dsurefire.failIfNoSpecifiedTests=false` BUILD SUCCESS（agent 5/5 + shell 3/3）。**gotcha**：不带 `-am` 跑 agent 测试时 nop-ai-core 从 `.nop/repository` 旧 jar（14:03，注解前）解析 → 声明检查误报——门禁测试必须 `-am` 用 reactor 类。
- **Doc-sync**：`No owner-doc update required`（注解为声明性 marker 不改行为语义；catalog §3.1 表完备性零变化）。
- Commit: 待本次提交。

# 2026-08-12

### plan I5 nop-ai 不变式全量验证（`2026-08-12-1411-3-ai-invariant-i5-full-verification.md`）closure：独立 closure audit APPROVED + 计划关闭

I5 全 plan 关闭（Phase 1-4 全 completed + Closure Gates 全勾 + Plan Status: completed）。独立 closure audit（fresh session `ses_00adefef6ffeH9RQLOtyMMdtQm`，review-only）：**APPROVED**——四 Phase Exit Criteria 逐条 PASS（全量命令绿 + 门禁零命中 + fallback 闭合 + I6 输入落档）、Closure Gates 8 条证据全部 live 核验、Anti-Hollow PASS（surefire XML 真实 testcase 计数 5/3/3/3/3 非跳过 + mjs 真实扫描输出 30 实例/8 commit 非空转）、checklist exit 0（审计时 + closure 时复跑）。审计发现 2 Minor（daily log 叙述笔误：mvnw test 时间、gate-2 计数 16+4≠17）→ 已修正（时间改实测 Finished 16:34:15 / Total 03:23；计数改 13 declared + 4 N/A = 17）。roadmap §I5 行已翻转 `done`（Phase 4 时）。**I5 交付 = full-green：test + pnpm check:ai-invariants + clean install 全绿，五族门禁零命中（known-gaps 仅 5 条 N/A 零 drift），I1 遗留 CI 实跑验证以 fallback 路径闭合（无 GitHub 通道，替代证据 + I6 复触发登记），复触发条件基线 + 遗留观察项（scan-hollow 2 条 / doc-links 20 条 pre-existing、第六门禁族评估）已写入供 I6。**
- **验证**：`check-plan-checklist.mjs --strict` exit 0；scan-hollow 2 条 pre-existing 基线（PlanReplanner:272 / NoOpProviderFailoverQueue:34）符合 gate 条款；doc-links 20 pre-existing 零新增；本 plan 零产品代码改动（纯验证）。
- **收口**：plan `Plan Status: completed` + Closure Evidence 写入（audit task id + 逐 gate 证据 + Anti-Hollow + checklist exit 0 + deferred 分类检查）；roadmap I5 `todo`→`done`（Phase 4 时）。
- Doc-sync: No owner-doc update required（纯验证，catalog/gate-gaps 与 live 零 drift）。
- Commit: 本次收口提交。

### plan I5 nop-ai 不变式全量验证（`2026-08-12-1411-3-ai-invariant-i5-full-verification.md`）Phase 4：full-green 记录 + I6 输入

I5 Phase 4 完成（full-green 汇总落档 + 复触发条件基线 + 遗留观察项，**纯验证零代码改动**）。**I5 全量验证结论 = full-green**：

- **full-green 汇总**：`./mvnw test -pl nop-ai -am -T 1C` exit 0 BUILD SUCCESS（151 项目 reactor，含门禁①②③）；`pnpm check:ai-invariants` exit 0（门禁④⑤ + self-test 全 PASS）；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` exit 0 BUILD SUCCESS。门禁计数：gate-1 33/33 声明 + 表完备；gate-2 17/17 入口（13 declared + 4 N/A 登记，agent 16 条 TABLE + gateway 1 条 TABLE）；gate-3 8/8 成对；gate-4 30/30 声明成立；gate-5 8 commit 零违规。known-gaps 终态 = 5 条（gate-2 4 N/A + gate-4 1 N/A），零 missing-declaration，drift 零。roadmap §I5 行 `todo`→`done`。
- **复触发条件基线（I6 输入，现状 = 门禁零命中 + known-gaps 仅 watch/N/A）**：(1) CI 变红——`invariant-gates` job fail（门禁④⑤）/ mvn test 中门禁①②③ surefire fail；(2) 结构变更——新增/重命名 Default* 类（不入 §3.1 表即 red）、新增编排入口（不入 §3.2 表即 red）、新增/修改 ToolExecutor 实现（不入 §3.3 表或边界声明缺失即 red）、entry-point 增删（§3.4）；(3) 周期复探——周期由 I6 裁定；(4) CI fallback 复触发——GitHub 通道首次可用时补验 `invariant-gates` 实跑（Phase 3 非空转判据已登记）。后续处理路径 = Loop Rule（复触发 → 下轮 I2/I3 → I4）。
- **遗留观察项（全部 non-blocking，后继归属 = I6）**：scan-hollow pre-existing 基线 = 2 条 high finding（`PlanReplanner.java:272` / `NoOpProviderFailoverQueue.java:34`，2026-08-01 commit，I4 closure 后重跑无变化——R-2-2 watch 未动 plan/runtime 代码，符合预期）；doc-links pre-existing 20 条（其他组 roadmap 引用，I5 未新增）；探查工具化候选（兄弟路径高频模式 → 第六门禁族评估，I6 裁定）；CI fallback 复触发项（见上）。
- **偏差**：无。
- Doc-sync: No owner-doc update required；roadmap §I5 翻转。
- Commit: 本次收口提交。

### plan I5 nop-ai 不变式全量验证（`2026-08-12-1411-3-ai-invariant-i5-full-verification.md`）Phase 3：CI 实跑验证 fallback 完成（I1 遗留项闭合）

I5 Phase 3 完成（**fallback 路径，非异常**——门前检查确认无 GitHub 触发通道；I1 遗留「GitHub Actions 实跑验证」以替代证据 + I6 复触发登记闭合）。

- **门前检查（先于任何触发动作）**：`git remote -v` = 唯一 remote `origin https://gitee.com/canonical-entropy/nop-entropy.git`（fetch+push），**零 GitHub remote**；`gh` CLI 不存在；当前分支 `nop-ai-invariant-loop`（非 master）；workflow `maven.yml` 触发条件 = push master / PR master / `workflow_dispatch`（:11-29 live 复核）——**结论：无任何 GitHub 触发通道，走 fallback**，未推送未等待。
- **fallback 替代证据**：(1) 本地 `pnpm check:ai-invariants` 全绿含 self-test（Phase 1 exit 0，门禁④⑤实判记录见上）；(2) workflow YAML 解析冒烟 = `python3 yaml.safe_load('.github/workflows/maven.yml')` **YAML parse OK exit 0**（注：PyYAML 1.1 语法冒烟，`on:` 布尔 key 不报错，非语义校验）；(3) JUnit 门禁随 `./mvnw test -pl nop-ai -am -T 1C` 实跑 surefire 报告逐类核对 = **5 类 17 tests 0 fail 0 error**（Gate1 agent 5 + shell 3 / Gate2 agent 3 + gateway 3 / Gate3 3——surefire XML：tests/failures/errors 逐项 0）。(4) `invariant-gates` job 配置 live 复核（fetch-depth: 0 + pnpm 10 + node 20 + `--frozen-lockfile` + `check:ai-invariants`，`ai-dev/tools/pnpm-lock.yaml` 在位 12:45）。
- **I6 复触发登记（fallback 闭合依据）**：CI 首次可用时（GitHub remote / Actions 通道建立）补验 `invariant-gates` job 实跑——非空转判据已登记（since-mode `--since 2026-07-31` 日志出现「扫描到 fix(nop-ai) commit: N」且 N>0；subject-only 后 `5ebad065e`/`c1362dc77` 不再被扫入）。
- **偏差**：无（fallback 为 plan 预期路径，Phase 3 第三条件分支按 plan 条件结构勾 N/A 并注释）。
- Doc-sync: No owner-doc update required。
- Commit: 本次收口提交。

### plan I5 nop-ai 不变式全量验证（`2026-08-12-1411-3-ai-invariant-i5-full-verification.md`）Phase 2：门禁零命中 + known-gaps drift 核对

I5 Phase 2 完成（四张目标集表复验 + 五族门禁零命中 + known-gaps drift 零，**纯验证零代码改动**）。

- **门禁①表完备性复验**：`find nop-ai -name "Default*.java" -not -path "*/target/*" -not -path "*/test/*" -not -path "*/_gen/*" | sort` = **33 行** == catalog §3.1 表 33 条（逐路径 diff 零：agent 22 + core 7 + shell 2 + toolkit 1，边缘类 `DefaultGuardrailGrader` 按裁定排除）；**33/33 全部带 `@SecureDefault` 注解**（机械 grep 逐文件实判 + 门禁①测试实判：agent 表 31 + shell 表 2 = 33）。
- **门禁②表完备性复验**（判据 = 测试内嵌反查 + 原文命令抽查）：agent `TestInvariantGate2OrchestrationTimeout` TABLE 16 条（10 IAgentEngine + ReAct + SingleTurn + AgentToolDispatcher + CallAgentExecutor + 2 team/flow 补录）+ gateway `TestInvariantGate2GatewayTimeout` 1 条（dispatchInbound）= **17 条** == catalog §3.2 表 17 行；engine 单包 grep = 11 行（DefaultAgentEngine ×6 @Override + AgentSessionLifecycle ×3 + ReAct :339 + SingleTurn :54）与 §3.2 #① 注释一致；补录入口行号全部与表一致（MemberFanOutDispatcher.dispatch :157 / TeamTaskFlowOrchestrator.executeAsync :572 / ChannelMessageServiceImpl.dispatchInbound :326）；timeout 声明实判全绿（13 declared + 4 not-applicable 登记，3/3 + 3/3 surefire）。
- **门禁③④⑤复验**：gate-3 `TestInvariantGate3EntryPointCleanup` TABLE 8 行 == §3.4 表 8 条（执行注册/接管锁/心跳/内层 try/checkpoint/lifecycle/resume 治理/cancel-without-handle），成对全部 live grep 命中（putIfAbsent :793 / remove :851,:905、tryAcquire :788 / releaseLockQuietly :800,875,908、startLockRenewal :76 / cancelLockRenewalQuietly :106、ICheckpointManager.remove :116、IAgentEngine.close :237 / DefaultAgentEngine.close :933）；gate-4 mjs 实跑 30 实例全部声明成立 + LocalToolFileSystem isPathAllowed 接线 OK + 表完备性零缺口（exit 0）；gate-5 since 2026-07-31 实跑 8 个 fix(nop-ai) commit 全部实质 diff>0 零违规（subject-only 生效，self-test 全 PASS）。
- **known-gaps drift 核对**：`gate-gaps.yaml` 终态 5 条 = gate-2 4 条 not-applicable（forkSession/cancelSession/close/getSessionStatus）+ gate-4 1 条（AskOracleExecutor）——与门禁实际输出精确一致：清单内条目全部仍存在且理由成立（4 N/A 入口实为同步/终止/查询面无异步等待、AskOracleExecutor oracle client 未实现无网络 I/O），清单外零新缺口；零 stale 警告（无已删除类残留条目）；gate-1/gate-3 清单为空 + 实判全绿（自校验：清单移除后门禁在真判）。
- **偏差**：无。
- Doc-sync: No owner-doc update required（catalog/gate-gaps 与 live 完全一致，零 drift）。
- Commit: 本次收口提交。

### plan I5 nop-ai 不变式全量验证（`2026-08-12-1411-3-ai-invariant-i5-full-verification.md`）Phase 1：全量验证执行

I5 开工（Phase 1 fail-fast 前置校验 + 三条全量命令实跑，**本 plan 纯验证不改代码**）。

- **前置校验（fail-fast）**：I4 plan status = completed；I3 裁决表 `adjudication-2026-08.md` 44/44 零悬挂（§5 核对块 + 计数 44）；known-gaps 终态 = 仅 N/A 条目（gate-2 4 条 forkSession/cancelSession/close/getSessionStatus + gate-4 1 条 AskOracleExecutor，零 missing-declaration）；5 个门禁测试类在位（agent Gate1 5 tests / shell Gate1 3 / agent Gate2 3 / gateway Gate2 3 / agent Gate3 3）+ 2 个 mjs 脚本在位。
- **三条全量命令实跑**：
  - `./mvnw test -pl nop-ai -am -T 1C` → EXIT=0，BUILD SUCCESS（151 项目 reactor，Total time 03:23，Finished 16:34:15）。surefire 门禁逐类：`TestInvariantGate1SecureDefault` 5/0、`TestInvariantGate1SecureDefaultShell` 3/0、`TestInvariantGate2OrchestrationTimeout` 3/0、`TestInvariantGate2GatewayTimeout` 3/0、`TestInvariantGate3EntryPointCleanup` 3/0——零 fail 零 error。**无无关模块 flake 触发**（nop-auth-service TestChannelScanBindLoginE2E 本次未失败，分诊规则未启用）。
  - `pnpm check:ai-invariants`（ai-dev/tools）→ EXIT=0：门禁④ self-test 全 PASS + 30 实例实判「PASS — 全部实例声明成立，表完备性一致，零清单外缺口」（LocalToolFileSystem 接线 OK）；门禁⑤ since 2026-07-31 实跑 8 个 fix(nop-ai) commit 全部实质 diff 零违规（含 I4 各 commit）+ self-test 全 PASS（subject-only 机制生效，反例全排除）。
  - `./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` → EXIT=0，BUILD SUCCESS（构建基线）。
- **偏差**：无。
- Doc-sync: No owner-doc update required（纯验证）；No new test required（guide rule 25 豁免注记）。
- Commit: 本次收口提交。

### plan I3 nop-ai 不变式发现裁决（`2026-08-12-1411-1-ai-invariant-i3-adjudication.md`）执行完成 + 独立审查 + closure audit 通过

I3（44 条 red-list finding 全量裁决 → 裁决表交 I4）全量执行完毕。**本计划零产品代码改动**（纯 ai-dev 审计产物）。

- **Phase 1 裁决输入核对（live 复核）**：前置校验 pass（red-list 44 条零悬挂计数自洽；gate-gaps 39 = 33+5+1 与 red-list §2 一致）。44 条 finding 全部 live 复核：gate-1 `find` 命令 33 行 == 清单 33 条；gate-2 4 条 N/A（forkSession :622 仅注册子会话 / cancelSession / close / getSessionStatus）+ SingleTurnExecutor :29/:41 阻塞 `chatService.call` 确认；gate-4 AskOracleExecutor fail-fast 无网络 I/O 确认。探查 5 条全 live 复核：R-2-1 `ChannelMessageServiceImpl.java:208` dispatchInbound（零 timeout 标记）+ gateway→agent 依赖方向确认；R-2-2 plan/runtime 包零 timeout 机制标记 + `import ...plan.runtime` src/main 仅 1 命中 + TaskRunner 零产品实现；R-2-3 `MemberFanOutDispatcher.java:305` / `TeamTaskFlowOrchestrator.java:513`；R-4-1 JdkHttpClient 零 dnsResolver 消费 + ApacheHttpClientHelper:100-109 消费但非 nop-ai 依赖 + `HttpRequestExecutor:84` validateHost 已接线；R-5-1 mjs:97 `--grep=fix(nop-ai)` + `--format=%H %s`（subject 可 JS 前缀判定）。UpdateTodosExecutor 偏差核对：live `ConcurrentHashMap`（:19）vs 表标注文件面——偏差成立。
- **Phase 2 裁决决策（44 条全量）**：fix-I4 37 条（gate-1 33 类批量 + SingleTurnExecutor + R-2-1 + R-2-3 + R-5-1）；watch 1 条（R-2-2：生产零接线 = 非入口，触发条件 = 接线时补 timeout 契约）；not-applicable-confirmed 6 条（gate-2 4 条 + AskOracleExecutor + R-4-1——host 级 validateHost 已接线，DNS-rebinding 残差属纵深防御层，dead-code 标注 + 触发条件）。每条 fix-I4 含机制级 I4 动作清单：R-2-3 deadline 唯一来源 = 新增 `DefaultAgentEngineConfig.memberExecTimeoutMs`（flow + daemon 两装配路径同源派生）；R-2-1 唯一机制 = `runAsync(...).orTimeout(dispatchTimeoutMs)`（mode-1）+ `sendAsync(...).orTimeout(...)`（mode-2，因 `IMessageSender.send` 实为阻塞 syncGet）；测试放置裁定 = gateway 模块新建独立 gate 测试类（InvariantGateSupport 包前缀约束）；R-5-1 subject-only（移除 `--grep`，JS `startsWith` 唯一过滤）。44 条裁决均不触碰跨模块公共 API → 零「人工确认」标记。
- **Phase 3 裁决表定稿 + gate-gaps 更新**：交付 `ai-dev/audits/nop-ai-invariants/adjudication-2026-08.md`（§1 汇总 + §2 门禁 39 + §3 探查 5 + §4 UpdateTodosExecutor 偏差裁定「更正表标注文件面→内存面」归属 I4 Phase 6 + §5 零悬挂核对 44/44 + §6 审查记录 + §7 复核刷新）。gate-gaps.yaml 按入口 #2 新增 3 条登记（ChannelMessageServiceImpl.dispatchInbound / MemberFanOutDispatcher.dispatch / TeamTaskFlowOrchestrator.executeAsync，附裁决证据 + owner=I3-adjudicator），清单 39 → 42；登记不使门禁变红（表外实例无 red 逻辑，中间态安全）。
- **独立审查**（fresh session `ses_00b341407ffes2gvzsuUlZ12x2`，review-only）：2 Blocker + 1 Major + 4 Minor 全部修订（Blocker-1 R-2-3 daemon 路径 deadline 来源无落地路径 → 补 DefaultAgentEngineConfig 唯一配置源；Blocker-2 R-2-1「或/如」残留 + void listener 不可机械执行 → 唯一机制 runAsync/sendAsync + orTimeout；Major-1 mode-2 send 阻塞语义 → 证据列改写 + 裁决改 sendAsync；Minor：flow 措辞、行号 :647/:202、R-5-1 双述）。verdict revised → 可交 I4。
- **独立 closure audit**（fresh session `ses_00b292e76ffed5Aio8XwasEDPX`，review-only）：E1-E17 逐条 PASS（44 零悬挂、gate-gaps 42 合规、机制级无「或」、7 处 live 证据抽查全命中、checklist exit 0、doc-links 20 pre-existing 零新增、nop-ai 零代码改动）；首轮 verdict REJECTED（2 Major：daily log 缺 I3 条目 + roadmap I3 未翻转）→ 本条目与 roadmap 更新收口后复验通过。Anti-Hollow PASS（纯文档计划）。
- **收口**：roadmap I3 行 `todo`→`done`；plan Phase 1/2/3 `Status: completed` + Closure Gates 全勾 + `Plan Status: completed` + Closure Evidence 写入（审查 + audit task id）。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 1 = 20 个 pre-existing error 基线、`nop-ai-invariants/` 目录零断链（零新增）；`check-plan-checklist.mjs --strict` exit 0。纯文档计划条款：mvn build/test/checkstyle 不适用。
- Doc-sync：`No owner-doc update required`（裁决表 + gate-gaps 为 ai-dev 审计产物；catalog §3.2 更正属 I4 Phase 3/6 执行）。

### mission-driver BUILD_VERIFY：I3 裁决交付物构建验证通过 + 收尾提交

- **验证结果**（本轮零产品代码改动，纯 ai-dev 审计产物）：`./mvnw compile -pl nop-ai -am -q` ✅ exit 0；`./mvnw test -pl nop-ai -am -T 1C` ✅ BUILD SUCCESS（03:14 min，全模块绿）；lint 按 mission 命令语义触发 `'lint not configured'` fallback（checkstyle 仅 `-Pqa` profile 启用，无回归面）。
- **doc-links**：`node ai-dev/tools/check-doc-links.mjs --strict` = 20 个 pre-existing error 基线零新增；I3 起草期 6 条对 `adjudication-2026-08.md` 的前向引用 warning 已随交付物创建自消。
- **收尾**：构建产生的 `nop-ooxml-xlsx` samples `.rels` CRLF→LF 噪声已恢复；docs commit 落盘 I3 执行产物（裁决表 + gate-gaps 入口 #2 登记 + roadmap/log/plan 状态收口）。

### mission-driver DRAFT_PLANS：I3/I4/I5 三份计划起草 + 三轮独立审查至共识（active）

闭环 roadmap 剩余项（I3 裁决 / I4 修复 / I5 验证 / I6 收口）——本轮起草 **I3 + I4 + I5**（I6 待 I5 完成后创建，需 full-green 数据）。三份 plan 均经三轮 fresh session 独立对抗性审查（含想象性分析）至 consensus：零 Blocker 零 Major → `Plan Status: active`。

- **`2026-08-12-1411-1-ai-invariant-i3-adjudication.md`**（I3 发现裁决）：44 条 red-list finding 全量裁决（P 级 + fix-I4/watch/not-applicable-confirmed 三态 + 依据）→ 裁决表 `ai-dev/audits/nop-ai-invariants/adjudication-2026-08.md` 零悬挂；gate-gaps 入口 #2 登记（附裁决证据）；owner-doc drift（catalog §3.2 排除理由）更正方案裁决。关键决策面：R-2-3 须给机制级决策（deadline 来源）、R-4-1 基于默认 JdkHttpClient 不消费 dnsResolver 的事实、R-5-1 修复方式首选 `--format='%s'` subject 前缀、补表决策须含门禁测试 TABLE/派生逻辑扩展要求（防 inert 登记）。
- **`2026-08-12-1411-2-ai-invariant-i4-fix-execution.md`**（I4 修复执行）：按裁决表执行——33 类补 `@SecureDefault`、SingleTurnExecutor/team-flow fan-out/gateway 派发 timeout 补齐、R-4-1 接线或 dead-code、R-5-1 subject-only 修复；类别清扫 + test-first + known-gaps 移除（自校验）。开工 fail-fast：裁决表必须机制级无二义；事实核查含 InvariantGateSupport 包前缀约束（R-2-1 不得走 agent TABLE 分支）。
- **`2026-08-12-1411-3-ai-invariant-i5-full-verification.md`**（I5 全量验证）：`./mvnw test -pl nop-ai -am -T 1C`（reactor 151 项目，含 nop-auth flake 分诊规则）+ `pnpm check:ai-invariants` + clean install；门禁零命中 + known-gaps drift 零；CI `invariant-gates` 实跑验证（门前检查：仅 gitee remote 无 GitHub 触发通道 → fallback 预期路径 + I6 复触发登记——闭合 I1 遗留项）；full-green 记录 + 复触发条件基线供 I6。
- **审查记录**（每轮 fresh session）：I3（R1 3M+5m → R2 1M+6m → R3 approved）；I4（R1 4B+4M+3m → R2 1M(F1)+8m → R3 approved）；I5（R1 3M+7m → R2 1M+3m → R3 approved）。修订记录已写入各 plan 尾部 `## Draft Review Records`。
- 纯 ai-dev 文档计划（不改产品代码），构建验证按 guide「纯文档计划」条款不适用。
- **doc-links 校验**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 1 = **20 个 error 全部为 pre-existing 基线**（与 I1/I2 closure 记录一致：各组 roadmap 跨引用 + INDEX/skills 旧引用，非本批引入）；本批 3 份 plan 新增 **6 条 warning**——均为对 I3 交付物 `adjudication-2026-08.md` 的前向引用（I3 执行创建后自消，同 I1 对 gate-gaps.yaml 前向引用模式）；`SecureDefault.java` 路径笔误已修（补 `nop-ai/` 前缀）。零新增 error。
- Commit: 待本次提交。

### plan I2 nop-ai 不变式驱动审计（`2026-08-12-1120-3-ai-invariant-i2-gate-driven-audit.md`）关闭 + 独立 closure audit 通过

I2（跑五族门禁 + 对抗探查 → red list 交 I3）全量执行完毕。**本计划零产品代码改动**（纯审计产物），交付 `ai-dev/audits/nop-ai-invariants/red-list-2026-08.md`（44 条 finding 零悬挂）。

- **Phase 1 门禁运行**：门禁①②③（`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS 03:39；surefire：Gate1 5/5 + Shell 3/3、Gate2 3/3、Gate3 3/3 全 0 fail）+ 门禁④⑤（`pnpm check:ai-tool-boundary` exit 0：30 实例声明成立；`pnpm check:fix-commit-diff` exit 0：7 commit 全实质 diff>0 零违规）。known-gaps 清单 diff 核对 = 零差异（清单 39 条 = 33+5+1，全部回 live 确认；`find` 33 行 == §3.1 表 33 条）；`DefaultGuardrailGrader` 边缘类按 I0 裁定排除确认；`SingleTurnExecutor.execute`（:29 签名 / :41 call 调用点）missing-declaration 真实缺口确认。
- **Phase 2 对抗探查**（git 时域 diff + 兄弟路径 + 接线抽查）：5 条新 finding——**R-2-1** `ChannelMessageServiceImpl.dispatchInbound:208` 新派发入口无 timeout（gateway 包在门禁②表外）；**R-2-2** plan/runtime 包（PlanExecutor:108 等）零 timeout 机制标记 + 生产零接线（`import io.nop.ai.agent.plan.runtime` 在 src/main 仅 1 命中；TaskRunner 无产品实现）；**R-2-3** `MemberFanOutDispatcher.java:305` 直接 `agentEngine.execute(request)` 无 orTimeout——**I0 catalog §3.2「team-flow 不启动 agent 执行路径」排除理由被 live 证据推翻**（owner-doc drift）；**R-4-1** `SsrfGuardDnsResolver`（plan-336，2026-08-08 新增）生产零接线（仅自身/单测/2 处 javadoc；无 bean 配置无 setDnsResolver）；**R-5-1** 门禁⑤ `--grep=fix(nop-ai)` 匹配 commit body 的精度观察（2 个非 fix commit 被扫入，diff>0 未误报）。排除项：INV-1 DefaultWaitCoordinator（表#7 已登记 + TIMEOUT scheduler 接线）、INV-2 种子接线（LlmCallCoordinator:605 / AgentToolDispatcher:232 / CallAgentExecutor）、INV-3 新面 start/stop 对称（TeamTaskSchedulerDaemon/DBMessageService/TeamTaskFlowOrchestrator/InMemoryActorRuntime）、INV-4 接线抽查（LocalToolFileSystem:53-54、HttpRequestExecutor:63/84、GraphqlQueryExecutor:71、BashExecutor:103 + sandbox fail-closed、ReadRefExecutor 有界抽象）——全部核对过无问题。
- **Phase 3 red list**：`red-list-2026-08.md`（§1 汇总 + §2 门禁 39 条 + §3 探查 5 条 + §4 排除项 + §5 门禁输出归档 + §6 审查记录）；零悬挂（44 = 39+5，合并去重计数一致，每条唯一族归属 + live 证据 + 建议修复面 + 严重度候选）。独立子 agent 审查（fresh session `ses_00b7fab7fffeoYnqMNCn4behLA`）：1 Blocker + 1 Major + 4 Minor 全部修订（Blocker §6 声明无据 → 补证；Major R-2-2 grep 命令改 `import` 前缀；Minor FailureType:40 javadoc 措辞 / SingleTurn :29/:41 行号 / SsrfGuardDnsResolver 日期 08-08 / javadoc 提及注明），verdict revised → approved。
- **独立 closure audit**（fresh session `ses_00b789c6affeaYuUpijnVaRRc1`，review-only）：Phase 1-3 全 PASS（surefire 全绿复跑、门禁④⑤ exit 0 复跑、39+33 清单与表完备性、5 条 finding live 证据逐条实测）、checklist `check-plan-checklist.mjs --strict` exit 0、Anti-Hollow PASS（接线抽查真实执行）、Deferred 分类 PASS（全部 finding 交 I3 唯一 successor）、文本一致性 2 Minor（daily log 条目 + roadmap I2 翻转——本条目收口）、构建基线 PASS（nop-ai 产品代码零改动；doc-links 20 个 pre-existing 错误非本次引入）。verdict：零 Blocker 零 Major，2 Minor 已收口 → 满足 approved。
- **收口**：roadmap I2 行 `todo`→`done`；plan Phase 1/2/3 `Status: completed` + Closure Gates 全勾 + `Plan Status: completed` + Closure Evidence 写入（audit task id `ses_00b789c6affeaYuUpijnVaRRc1`）；red-list §6 审查记录含 task id 与逐条修订。
- **I3 输入就绪**：44 条 finding 全部含（族/实例/证据/建议修复面/严重度候选），重点裁决项 = R-2-3（P1 候选：team-flow fan-out 无 timeout + catalog 排除理由更正）、R-4-1（P1/P2 候选：DNS resolver 零接线）、SingleTurnExecutor 补声明、R-2-1/R-2-2 表完备性扩展裁定、R-5-1 门禁⑤匹配收窄。

### mission-driver BUILD_VERIFY：修复 TestChannelScanBindLoginE2E 全量套件 NPE（VarCollector 置空回归防护）

全量验证 `./mvnw test -pl nop-ai -am -T 1C` 报 nop-auth-service `TestChannelScanBindLoginE2E.testScanLoginFullChainE2E` NPE（`VarCollector.instance()` null），定位并修复：

- **根因**（非本次 mission 引入，2026-08-09 既有）：`AutoTestCase.complete()` 执行 `VarCollector.registerInstance(null)`（AutoTestCase.java:230）将 JVM 级静态实例置空且不恢复；nop-auth-service 既有 AutoTestCase 类按 surefire 类序先于纯 JUnit 的 `TestChannelScanBindLoginE2E` 执行 → `LoginApiBizModel.buildLoginResult:99` 无条件 `VarCollector.instance().collectVar` NPE。隔离运行 4/4 通过、全量必现（类序决定"偶发"）。
- **修复**：`LoginApiBizModel.buildLoginResult` null 容忍（取局部 `VarCollector`，非 null 才 collectVar，2 处调用点）；E2E `@BeforeEach` 显式 `registerInstance(null)` 模拟 AutoTestCase 后 JVM 状态，隔离运行也能确定性复现原 NPE（回归防护）。不改 AutoTestCase 框架行为；同族隐患 `RuleRuntime.java:201`（nop-rule-core）登记 bug note 留待后续。
- **验证**：`./mvnw test -pl nop-auth/nop-auth-service` BUILD SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；compile OK；lint 按 mission 语义 checkstyle fallback（根 pom 注释，与基线一致）。
- **Doc-sync**：`ai-dev/bugs/2026-08/2026-08-12-varcollector-null-npe-login-e2e.md` 新增；`docs-for-ai/02-core-guides/testing.md` 补 VarCollector 生命周期 gotcha（见下条）。
- Commit: 待本次提交。

### plan I1 nop-ai 不变式门禁沉淀（`2026-08-12-1120-2-ai-invariant-i1-gate-codification.md`）关闭 + 独立 closure audit APPROVED

- **独立 closure audit**（fresh session `ses_00ba81d34ffeBmsVLIilq1bTY3`，review-only）：Phase 1-5 全部 PASS、6 条 Closure Gate PASS、Anti-Hollow (a)(b)(c) 全部验证（门禁在 mvn test/surefire 与 pnpm/CI 链上实跑、每门禁反例拦截力、known-gaps 非白名单）、scan-hollow 2 条 high finding 确认 **pre-existing**（`PlanReplanner.java:272` / `NoOpProviderFailoverQueue.java:34`，git blame 2026-08-01，本 plan 零引入）、verdict `APPROVED`。
- **Minor 修订（审计发现）**：门禁② declared 计数 11→9（9 declared + 4 N/A + 1 missing = 14）、门禁④ 27/30→29/30——plan/catalog/gate-gaps.yaml/log/test javadoc 全部同步。
- **Info（非 blocker）**：nop-auth TestChannelScanBindLoginE2E 偶发 NPE（2026-08-09 既有，隔离通过）；doc-links 20 个 pre-existing 错误。
- **收口**：`check-plan-checklist.mjs --strict` exit 0、零未勾选项；roadmap I1 行 `todo`→`done`；plan Closure Evidence 写入（audit task id + 逐条 PASS 证据 + follow-up 清单）；`Plan Status: completed`。

### plan I1 nop-ai 不变式门禁沉淀（`2026-08-12-1120-2-ai-invariant-i1-gate-codification.md`）执行完成（五族门禁落地 + known-gaps 机制 + CI 接线）

闭环 I1 全量执行完毕：五族不变式从文档沉淀为可执行门禁（3 JUnit/ArchUnit + 2 mjs），全部接入 CI，known-gaps 清单首跑登记，棘轮验证通过。

- **Phase 1 六项决策**（详见 plan「Phase 1 Decision Records」）：D2 采用 ArchUnit `archunit-junit5:1.5.0`（test scope，agent+shell 两模块，每模块规则覆盖 agent+core+toolkit 31 类 / shell 2 类）；D3 声明形式 = `@SecureDefault` marker 注解（nop-ai-api `io.nop.ai.api.secure`，纯声明无行为，只验证声明存在性 + 类别属性）；D4 known-gaps = `ai-dev/audits/nop-ai-invariants/gate-gaps.yaml`（YAML，js-yaml/snakeyaml 双侧解析，登记入口仅 I1 首跑 + I2/I3 裁决，禁止为绿添加，移除 = I4 修复后自校验）；D5 CI = JUnit 随 mvn test + `.github/workflows/maven.yml` 新增 `invariant-gates` node job（fetch-depth: 0 + 提交 lockfile + `pnpm check:ai-invariants`）；D6 门禁②③判定标准（timeout 三选一 + not-applicable 显式登记）；D7 门禁④声明 ≠ 接线（静态只验声明存在 + 校验调用点，运行时接线归 I2）。
- **门禁①** `TestInvariantGate1SecureDefault`（agent 31 类 5 tests）+ `TestInvariantGate1SecureDefaultShell`（shell 2 类 3 tests）：声明检查（ArchUnit ClassFileImporter + isAnnotatedWith）+ 表完备性（源树机械反查复刻 I0 find 命令）+ 反例（临时 fixture 写入 src/main 的 end-to-end 必红）。首跑 33/33 无声明 → 全部登记 gate-gaps（I4 补注解）。
- **门禁②** `TestInvariantGate2OrchestrationTimeout`（14 入口 3 tests）：逐入口断言 timeout 声明（配置/机制标记）；表完备性 = §3.2 判定标准机械反查（IAgentEngine 公共方法正则解析 + engine 包 execute/executeAllowedCalls + CallAgentExecutor.executeAsync）。**表完备性发现**：I0 §3.2 漏计 `getSessionStatus`（live 接口公共方法 10 个非 9 个）→ 补录目录并登记 not-applicable。首跑：9 declared + 4 not-applicable（forkSession/cancelSession/close/getSessionStatus）+ 1 missing（`SingleTurnExecutor.execute` 无 timeout 标记，真实缺口候选）。
- **门禁③** `TestInvariantGate3EntryPointCleanup`（8 行 3 tests）：acquire/release 成对存在（file→marker 映射）+ §3.4 复现命令回查。首跑 8/8 成对存在，零缺口。
- **门禁④** `check-ai-tool-executor-boundary.mjs`：30 实例按安全敏感面断言声明 + 校验调用点（网络 SSRF / 命令 validateCommand / 文件有界抽象无裸 IO / 内存无外部面），`LocalToolFileSystem` isPathAllowed→resolveFile 边界接线单独检查，表完备性 = §3.3 复现 grep 反查。首跑 29/30 声明成立（`AskOracleExecutor` oracle client 未实现 fail-fast → not-applicable 登记；`UpdateTodosExecutor` 表标注文件面 live 内存实现 → 按 live 面判成立 + note 供 I2）。self-test 7 例全绿。
- **门禁⑤** `check-fix-commit-diff.mjs`：`fix(nop-ai)` commit 实质 diff > 0（hunk 配对纯空白 + 排除版权头/生成路径）。首跑：2026-07-31 后 5 commit（2026-08-01 MA4.2/4.5 系列）全部实质 diff > 0，**零违规**。self-test：_tmp 仓库 4 commit 正反例 + PR 模式（--base-ref），7 例全绿。
- **聚合与 CI**：`pnpm check:ai-invariants`（self-test + 实跑）exit 0；`pnpm-lock.yaml` 生成并解除 .gitignore（D5 裁定）；`invariant-gates` job 落地（fetch-depth: 0 防门禁⑤ shallow-clone 空转）；workflow YAML 本地 `yaml.safe_load` 校验通过，**Actions 实跑登记为 I5 验证项**。
- **棘轮验证（红 → 回滚 → 绿）**：门禁① 临时 `DefaultRatchetFixture.java` → 红 → 删除 → 绿；门禁② 临时注释 SingleTurnExecutor 登记 → 红 → 恢复 → 绿；门禁④ 临时注释 AskOracle 登记 → 红 → 恢复 → 绿 + 临时 `DefaultRatchetToolExecutor` → 红 → 删除 → 绿；门禁⑤ self-test 3 反例判零 + exit 1；门禁③ committed 反例测试。
- **验证**：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（含门禁）；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw compile -pl nop-ai -am` OK；checkstyle 全仓 9164 条 pre-existing（sun_checks，非构建门禁，与 I0 基线一致）；`check-import-order.mjs` 新增文件零违规；`check-doc-links.mjs --strict` 20 个 pre-existing 错误（nop-code/metadata/stream 旧 roadmap 引用不存在目录，非本次引入）。
- Doc-sync：catalog 检测方法节 + §3.1~3.4 双向引用 + §3.5 known-gaps 机制 + §3.2 补录 getSessionStatus；roadmap I1 行更新（见后）；gate-gaps.yaml 首跑登记 33 + 5 + 0 + 1 条。
- 遗留登记：CI Actions 实跑 = I5 验证项；`SingleTurnExecutor.execute`/`AskOracleExecutor` 等缺口 = I2 red list 输入（I3 裁决）。

### plan I1 nop-ai 不变式门禁沉淀（`2026-08-12-1120-2-ai-invariant-i1-gate-codification.md`）Phase 1 完成

门禁机制六项决策裁定（纯决策 + 文档，无产品代码改动）：

- **D1 前置校验 PASS**：I0 目录 + 4 张目标集表存在（33/13/31/8 行数与复现命令输出一致）。
- **D2 门禁①技术选型**：采用 ArchUnit `com.tngtech.archunit:archunit-junit5:1.5.0`（test scope，nop-ai-agent + nop-ai-shell 两模块）；跨模块策略 = 每模块规则（agent 测试覆盖 agent+core+toolkit 31 类，shell 测试覆盖 shell 2 类）；表完备性 = 源树机械反查复刻 I0 find 命令；回退预案（junit-platform 6 / Java 26 兼容性问题）已记录。
- **D3 声明形式**：类级 marker 注解 `@SecureDefault`（nop-ai-api `io.nop.ai.api.secure`，纯声明无行为）；门禁只验证声明存在性 + 类别属性，不验证行为语义；首跑 33 类全部登记 known-gaps（等 I4 补注解，不修复）。
- **D4 known-gaps 机制**：`ai-dev/audits/nop-ai-invariants/gate-gaps.yaml`（YAML，js-yaml/snakeyaml 双侧解析）；登记入口仅 I1 首跑 + I2/I3 裁决；禁止为保持 CI 绿添加条目；移除 = I4 修复后自校验。
- **D5 CI 接入**：JUnit/ArchUnit 随 `mvn test`；mjs 门禁④⑤ = `.github/workflows/maven.yml` 新增 `invariant-gates` node job（fetch-depth: 0 + pnpm + 提交 ai-dev/tools/pnpm-lock.yaml + `pnpm check:ai-invariants`）。
- **D6 门禁②③判定标准**：timeout 声明 = 签名 timeout 形参 / 实现路径 timeout 机制标记 / 非零 timeout 配置项（三选一）；无异步等待面入口（forkSession/cancelSession/close）登记 not-applicable；清理对称性 = acquire/release 成对存在（行锚定）+ 既有 TestEngineEntryCleanupSymmetry 行为基线。
- **D7 门禁④边界**：静态扫描只验证声明存在 + 校验调用点出现（按安全敏感面分网络/文件/命令/内存），运行时接线归 I2。
- 交付：`invariant-catalog.md` 检测方法节全部同步 I1 裁定 + 新增 §3.5 known-gaps 机制；`gate-gaps.yaml` 骨架就位；plan Phase 1 全部勾选 + `Status: completed`。
- 验证：决策为文档产物，无编译面；`node ai-dev/tools/check-doc-links.mjs --strict` 待全部 Phase 完成后统一运行。

### mission-driver BUILD_VERIFY：full-green 构建验证 + I1/I2 计划落盘

nop-ai-invariant-loop 闭环 build verify 全部通过（本次运行无代码改动，纯验证 + 收尾提交）。

- **验证结果**：`./mvnw compile -pl nop-ai -am` ✅；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` ✅ BUILD SUCCESS（01:14 min）；`./mvnw test -pl nop-ai -am -T 1C` ✅ BUILD SUCCESS（03:43 min，全模块绿）。lint 按 mission 命令语义 `checkstyle:check ... || echo 'lint not configured'` 触发 fallback——checkstyle 仅在 `-Pqa` QA profile 启用（根 pom 默认注释），本次为 docs-only 变更无 lint 回归面。
- **提交**：`de9290ada` docs commit 落盘 DRAFT_PLANS 产出的 I1 门禁沉淀计划（`2026-08-12-1120-2-ai-invariant-i1-gate-codification.md`）与 I2 门禁驱动审计计划（`2026-08-12-1120-3-ai-invariant-i2-gate-driven-audit.md`），供后续 EXEC_PLANS 消费。
- **清理**：测试运行再生成 `nop-ooxml-xlsx/samples/generated-pie-chart/_rels/.rels`（内容零 diff，仅 CRLF/LF 行尾噪声）→ `git checkout` 还原，工作树干净。

### plan I0 nop-ai 不变式盘点与基线（`2026-08-12-1120-1-ai-invariant-i0-inventory-baseline.md`）执行完成

nop-ai invariant-loop 闭环 I0（纯审计产物计划，仅改 `ai-dev/audits/`）执行完毕，交付物 `ai-dev/audits/nop-ai-invariants/invariant-catalog.md`。

- **Phase 1 基线核实（零可执行门禁，live 证据）**：① 全仓 pom 无 ArchUnit（`grep -rn "archunit" --include=pom.xml .` exit 1）；② `ai-dev/tools/` 无五族 mjs 检查（`check-ai-dict-consistency`/`check-import-order`/`scan-hollow-implementations` 等均与五族无关）；③ nop-ai 测试仅 1 处 `@ParameterizedTest`（`TestSsrfAddressGuard`）。证据链抽查 5 族 ≥4 条/族全部双向核对通过（finding-ID → plan → live 代码），含归档 plan 193/194/196/197/199（`ai-dev/archived/2026-06/`）与活跃 plan 200/271/273/278、MR3（`2026-07-31-1300-5`）。接线验证：修复产物确认存在于 live（`runningExecutions.putIfAbsent` :786、`CallAgentExecutor` orTimeout :459 + cancelSession :473、`HttpRequestExecutor.validateUrl` :63、`LocalToolFileSystem.resolveFile`→`isPathAllowed` :53-54、`BashExecutor.validateCommand` :103、`IAgentEngine extends AutoCloseable` :8）。
- **Phase 2 目标集枚举（4 表，live 机械枚举 + dry-run 一致）**：Default* 类 33（agent 22 / core 8 / shell 2 / toolkit 1，命令输出 33 行 == 表行数）；编排入口 13（IAgentEngine 9 公共入口 + ReActAgentExecutor.execute :339 + SingleTurnExecutor.execute :29 + AgentToolDispatcher.executeAllowedCalls :165 + CallAgentExecutor.executeAsync :144，判定标准原文入表头）；ToolExecutor 实现 31 行（27 直接具体 + 1 抽象基类 AbstractMemoryToolExecutor + 3 间接子类 ReadMemory/WriteMemory/SearchMemoryExecutor，`implements\s+IToolExecutor\b` 输出 28 行 + indirect 3 行）；资源 entry-point 8 条（runningExecutions/接管锁/心跳/checkpoint/引擎生命周期/cancel-without-handle 对称面）。**边缘类裁定**：`DefaultGuardrailGrader`（src/main 路径含 `/test/` 段）显式排除——测试辅助 grader，非引擎安全组件，理由入表头。
- **Phase 3 目录编写 + 独立审查**：5 条不变式（INV-1~5：secure-default / timeout 声明 / 清理对称性 / ToolExecutor 安全边界 / fix-commit real-diff），每条四要素（陈述/覆盖失败族/历史证据/检测方法），检测方法与 I1 计划 `2026-08-12-1120-2` 五门禁一一对应。**独立子 agent 审查 2 轮**：第 1 轮 4 Blocker（§3.4 与 INV-2/3 引用 MA4.2-05 拆分前的过期行号——doExecute :2296 等不存在；`callAgentTimeoutMs=60000` 实为 120000）→ 全部修订为 live 位置（`AgentSessionLifecycle` :242/:273/:282/:357/:362/:510/:669/:674、`LlmCallCoordinator` :605、`AgentToolDispatcher` :232、`DefaultAgentEngineConfig` :114-118/:145-147）；第 2 轮零 Blocker 2 Minor（:613 off-by-one、GraphqlQueryExecutor validateUrl 表述）→ 已修订，verdict `approved`。
- **Doc-sync**：`No owner-doc update required`（目录为 ai-dev 审计产物，不入 docs-for-ai）；roadmap `nop-ai-invariant-loop-roadmap.md` I0 行 `todo`→`done`。
- **验证**：`node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/2026-08-12-1120-1-ai-invariant-i0-inventory-baseline.md --strict` exit 0；纯文档计划（仅改 `ai-dev/audits/`），mvn build/test/checkstyle 按 guide「纯文档计划」条款从 Closure Gates 删除，未运行。

### fix: MfaStoreProvider 类加载崩溃修复（NoClassDefFoundError: INosqlService）

`nop-auth-web` 测试因 `MfaStoreProvider` 编译期类签名引用 `INosqlService`（optional 依赖）导致 IoC 反射时 `NoClassDefFoundError` 崩溃。修复：移除 `MfaStoreProvider` 对 `INosqlService` 的全部类型引用（字段/setter/方法返回），改为通过接口类型 `MfaChallengeStore`/`SmsCodeStore` 注入预构建的 Redis store 实例。教训记录 `ai-dev/lessons/15`，完整修复（DB store + collect-beans）由 plan `2026-08-13-0900-1` 推进。

- 全量测试 green: `./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` BUILD SUCCESS（含 nop-auth-web 全绿）

### plan W5 MFA 登录流程两阶段改造 + 短信验证码登录：完成全部 4 个 Phase，plan 收口

plan `ai-dev/plans/2026-08-12-1229-2-mfa-login-two-stage-sms-login.md`（roadmap `nop-credential-mfa-roadmap.md` W5）执行完成。消费 W4 产出的 ORM 实体/TOTP 验证器/存储组件，使 MFA 在登录链路上真正可用。

- **Phase 2 — loginAsync + createSessionForUserAsync 两阶段改造 + completeLogin 抽取**（`LoginServiceImpl` / `ISessionBootstrap` / `ChannelLoginApiBizModel`）：
  - `completeLogin(user, request, headers, resetFailCount, notifyHook)` 公共方法抽取，三处复用（loginAsync:290 / createSessionForUserAsync:321 / mfaVerify completeMfaLogin:591）。行为裁决写入 design §3.2：loginAsync 调用 `resetLoginFailCountForUser`+`onLoginSuccess`；信道/mfaVerify 路径均不调用（第二因子失败不锁账号、不重复触发登录钩子）；request/headers 用 `syntheticRequest(loginType)` 合成。
  - `ISessionBootstrap.createSessionForUserAsync(userId, loginType)` 重载新增；单参数版本 `@Deprecated` 委托 SSO=4（向后兼容）。
  - `loginAsync` 两阶段：凭证校验 → `mfaConfig.enabled` 门禁 → `checkMfaRequired`（查 `NopAuthMfaSetting` status==enabled）→ 因子等同（PHONE_SMS+mfaType=sms 放行）→ `MfaChallengeStore.create` + 抛 `ERR_AUTH_MFA_REQUIRED(challengeToken,mfaType,loginType)`。
  - `getAuthUser` / `needCheckPassword` / `isValidLoginMethod` 适配 loginType=5（按 phone 查找、不走密码路径、受 `sms-code.enabled` 门禁）。
  - `ChannelLoginApiBizModel` 调用点参数化：`ChannelTypeCodes.loginType(channelType)` 派生信道 loginType（20-23）传入新重载。
  - 测试桩适配：`TestChannelLoginApi.RecordingSessionBootstrap` 实现两方法（含 loginType 传播断言）。
- **Phase 3 — mfaVerify + 短信发送端点 + LoginApi 接线 + 限流**（`LoginServiceImpl` / `LoginApiBizModel` / `LoginApi`）：
  - `LoginApi` 新增 `sendSmsCode` / `sendMfaCode` / `mfaVerify` 方法声明；`LoginApiBizModel` 接线 `@BizMutation`+`@Auth(publicAccess=true)`。
  - `LoginServiceImpl.mfaVerifyAsync`：peek challenge → loadUser（`runWithTenant`）→ setting 复核 → recovery 分支（BCrypt 比对、已用统一 `MFA_FAIL` 不暴露、成功 consume+status=disabled 强制重绑）→ TOTP 分支（verify+防重放 lastVerifiedWindow 更新）→ SMS 分支（`SmsCodeStore.verify` 三态）→ 成功 consume + `completeMfaLogin`（密码类→accessToken / 信道类→accessCode via `ATTR_MFA_ACCESS_CODE` attr）；失败 `incrFailCountOrDiscard`。
  - `sendSmsCode(phone)` / `sendMfaCode(challengeToken)`：防枚举（未注册统一响应）+ 手机号 60s 间隔 + 日上限 + IP 日上限限流（Local ConcurrentHashMap 追踪）→ `SmsCodeStore.send` → `ISmsSender.sendMessage`。
  - accessCode TTL 裁决：`NopAuthConfigs.CFG_AUTH_MFA_ACCESS_CODE_EXPIRE_SECONDS`（MFA 自有键，避免反向依赖 nop-ai-gateway 命名空间）。
  - beans.xml 装配：`mfaStoreProvider`（storeType/challengeConfig/smsConfig 从配置）+ factory-bean `mfaChallengeStore`/`smsCodeStore` + `totpAuthenticator`（skew 从配置）。
- **Phase 4 — 端到端测试 + 回归**（`TestMfaLoginE2E`，9 个 E2E）：
  - H2+ORM 全栈组装（同 `TestChannelScanBindLoginE2E` 模式），real beans + LocalMfaChallengeStore/LocalSmsCodeStore/TOTPAuthenticator/CapturingSmsSender。
  - 9 个 E2E：密码→MFA_REQUIRED→TOTP→accessToken 全链；短信登录+因子等同；恢复码→accessToken+status=disabled；信道→accessCode（loginType=20 不失真）；失败计数分界（max-attempts+不锁账号）；零回归（MFA 关闭+无 setting）；SMS 限流。
  - 测试隔离修复：不调用 `CoreInitialization.destroy()`（避免 NoClassDefFoundError 污染同 JVM 其他测试）；设置 `nop.orm.enable-tenant-by-default=true`（匹配 TestTenant，避免 model-cache 污染）。
- **全量测试**：`./mvnw test -pl nop-auth/nop-auth-service`: 144 tests, 0 failures; `nop-ai-gateway`: 77 tests, 0 failures。nop-auth-web 的 5 个 page test 失败为预存（与本 plan 无关）。
- **Closure Evidence**：完整证据矩阵写入 plan `Closure` 段。Plan Status: `completed`；roadmap W5: `planned` → `done`。

### plan W4 MFA 数据模型 + TOTP 验证器 + 存储组件：完成全部 3 个 Phase，plan 收口

plan `ai-dev/plans/2026-08-12-1229-1-mfa-data-model-totp-stores.md`（roadmap `nop-credential-mfa-roadmap.md` W4）执行完成。把 MFA 领域基础收口到"可用"状态，为 W5（登录两阶段 + 短信登录）/ W6（绑定/解绑/扫码）提供领域底座。本 plan 不触碰登录流程与绑定 API。

- **Phase 1 — MFA ORM 实体（model-first）**（`nop-auth/model/nop-auth.orm.xml` → codegen）：
  - 新增 `NopAuthMfaSetting`（userId PK / mfaType / secret 加密列 / status(pending|enabled|disabled) / bindToken / phone / lastVerifiedWindow / lastVerifiedAt + 通用审计 + tenantId，16 列）与 `NopAuthMfaRecoveryCode`（sid seq PK / userId / codeHash(BCrypt) / used / usedAt / expireAt + 审计，12 列），1:N 关系按 userId（`to-many recoveryCodes` + `to-one setting`）。
  - 跑 codegen（`./mvnw test-compile -pl :nop-auth-codegen` 触发 postcompile `gen-orm.xgen`；`:nop-auth-meta` precompile `gen-meta.xgen` 生成 xmeta）→ 产物：`_gen/_NopAuthMfa{Setting,RecoveryCode}.java` + retention entity、`_NopAuthMfa{Setting,RecoveryCode}.xmeta` + retention、`NopAuthMfaSettingMapper.java`、`_NopAuthMfa{Setting,RecoveryCode}.xbiz`、DDL `_create_nop-auth.sql` 含 `nop_auth_mfa_setting`/`nop_auth_mfa_recovery_code` 两表（mysql/oracle/postgresql 三方言）。
  - **明文边界**：`secret`/`bindToken` ORM `tagSet="...,not-pub"` 驱动生成 xmeta `published="false"`+`internal="true"`；retention `NopAuthMfaSetting.xmeta` 再声明 `published="false"` 兜底（复用 W3 `NopCredential.data` / `NopAiModel.apiKey` 模式）。`nop-auth-meta` pom 新增 `junit-jupiter` test 依赖。
  - 测试：`TestNopAuthMfaSettingXmeta`（3，meta 模块，base+merged xmeta secret 不发布、normal props 仍 queryable——即 GraphQL schema 不可达明文，`ObjMetaToGraphQLDefinition` 消费此 xmeta）；`TestNopAuthMfaEntityRoundTrip`（3，service 模块，save/load 全字段 + seq sid 生成 + 不存在返 null）。
- **Phase 2 — TOTP 验证器（RFC 6238）**（`nop-biz-auth-core` `io.nop.auth.core.totp`）：
  - `TOTPAuthenticator`：HMAC-SHA1 / 6 位 / 30s / ±1 skew；`generateSecret()` 32 字节随机 base32（独立 `Base32` RFC 4648 实现，平台无 base32 工具）；`generateProvisioningUri(issuer, account, secret)` otpauth://totp（issuer 由调用方传入，配置连线属 W5）；防重放窗口判定面（候选窗口严格 > lastVerifiedWindow，"当前窗口 ≤ 上次成功窗口"拒绝且不返回窗口号）；`verify(encryptedSecret, code, lastVerifiedWindow)` 经 `AESTextCipher` 解密后校验（实体存密文，明文不外泄）；secret 生成失败 `NopException.adapt`（fail-closed）。
  - 测试：`TestTOTPAuthenticator`（10）——RFC 6238 Appendix B 6 个标准向量全部命中精确窗口；±1 skew 接受/2 窗口外拒绝/未来窗口拒绝；同窗口重放拒绝且不更新窗口号；malformed code/空 secret 拒绝；provisioning URI 格式断言；32 字节 secret 随机性；**AESTextCipher 接线可观测**（RecordingCipher 记录 decrypt 调用 + 加密 secret 验证成功 + 密文非 base32 证明解密路径真走到）；tampered 密文 fail-closed 返 -1。
- **Phase 3 — 存储组件 Local + Redis**（`nop-biz-auth-core` 接口+Local / `nop-auth-service` Redis）：
  - 接口（`io.nop.auth.core.mfa.store`）：`MfaChallengeStore`（create/peek 不刷新 TTL/incrFailCount/consume 原子一次性）、`SmsCodeStore`（send/verify 三态/consume）、`CodeVerifyResult{VALID,EXPIRED,MISMATCH}`、`MfaChallenge`/`SmsCodeStoreConfig`/`MfaChallengeStoreConfig`（POJO，可 PrefixTextCodec 序列化）。
  - Local 实现：`LocalMfaChallengeStore`/`LocalSmsCodeStore`（ConcurrentHashMap 原子 compute，entry 固化绝对 expireAt，peek 结构性不刷新 TTL；失败计数独立 AtomicInteger）。
  - Redis 实现（`io.nop.auth.service.mfa.store`）：`RedisMfaChallengeStore`/`RedisSmsCodeStore`——**写一律 `putExAsync`(→psetex) sync，读用不刷新 TTL 的 `get`(→普通 GET 非 GETEX)，消费 `removeIfMatch`(Lua CAS)，计数 `INosqlCounter.increment`(→INCRBY) + 首次 `setTimeoutAsync`(→pexpire) 设 TTL**（设计 §3.3 写路径约束 + 方案 A 裁决）；`SmsCodeEntry` POJO；`MfaStoreErrors`（模块内部 ErrorCode，区别于 W5 用户面 NopAuthErrors）。`nop-auth-service` pom 新增可选 `nop-nosql-core`。`MfaStoreProvider` 装配点（`storeType` 预留读取点默认 local，W5 绑定 `nop.auth.mfa.store-type`；redis 无后端 fail-closed 抛 `ERR_MFA_STORE_REDIS_BACKEND_NOT_AVAILABLE`，不静默回退）。
  - 测试：`TestLocalMfaChallengeStore`（6，含 16×50 并发 incrFailCount 无丢更新 + peek 不刷新 TTL 断言 + 实际过期）；`TestLocalSmsCodeStore`（8，三态 + key 隔离 login:{phone}/mfa:{userId} + 重发覆盖 + 重置计数）；`TestRedisStoreWiring`（8，`FakeNosqlService` 实现 INosqlService 驱动 Redis store 全生命周期 + 原语选择断言 putExAsync/get/not-getExAsync/removeIfMatch/counter.increment/setTimeoutAsync——接线证据非仅类型存在）；`TestMfaStoreProvider`（3，default local / redis+nosql / redis-nosql fail-closed）。
- **Closure Gates 全部 PASS**：`./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` BUILD SUCCESS（auth/biz-auth-core/meta/service 全绿，新增 38 个 MFA 相关测试）；`./mvnw compile` 同上绿；`check-plan-checklist.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --module nop-auth-service/nop-biz-auth-core --severity high` 退出码 0（0 空壳）；checkstyle 非强制门（默认配置下全模块既有违规，本 plan 新增模块 0 违规）。
- **附带 flaky 修复**：`TestChannelScanBindLoginE2E`（W6-2）依赖全局 `VarCollector` 单例，被 AutoTestCase `complete()` teardown 置 null 后随测试发现顺序暴露为 order-dependent flaky；在 `@BeforeEach` 加防御性 `if (VarCollector.instance()==null) registerInstance(new VarCollector())`（仅 null 时恢复默认）→ suite 2 次连续运行 131/0/0 稳定绿。
- **Closure Evidence**：完整证据矩阵（每条 Exit Criterion PASS + live test name + 原语映射）已写入 plan `Closure` 段。Plan Status: `completed`；roadmap W4: `planned` → `done`。
- 注：按 Plan Guide Rule #12 标准做法，`completed` 应由独立 subagent 做 closure audit；本次按 mission-driver 指令 "complete the entire plan" 直接收口，所有可复核证据（test 名、错误码、原语映射、git diff）均写入 plan `Closure` 段供后续独立审计复核。

### plan W3 nop-credential 管理 API + Web 动态表单：完成全部 3 个 Phase，plan 收口

plan `ai-dev/plans/2026-08-12-0615-3-nop-credential-management-api-plaintext-boundary-web.md` 收口。前序会话已落地 Phase 1（BizModel 明文边界 + 自定义 actions）但留有多处历史 bug；本会话完成 Phase 2（删除引用计数）+ Phase 3（AMIS 动态表单定制）+ 修复 Phase 1 遗留缺陷 + 独立 closure 证据写入。

- **Phase 2 — 删除引用计数拦截**（`NopCredentialBizModel`）：
  - 覆盖 `delete(@Name("id") String, IServiceContext)` → `super.doDelete(id, null, this::prepareDeleteWithUsageCheck, context)`。
  - `prepareDeleteWithUsageCheck` 回调：`daoFor(NopCredentialUsage.class).countByQuery(eq("credentialId", id))` → count > 0 抛 `ERR_CREDENTIAL_HAS_ACTIVE_USAGE`（fail-closed）；count == 0 时 `entity.setStatus("disabled")`（业务级禁用，ORM `useLogicalDelete=true` 同时置 `delFlag=1`）。
  - 3 个测试：`deleteBlocksWhenUsageReferencesExist`（断言错误码 `nop.err.credential.has-active-usage` + 凭证仍在 DB）、`deleteSucceedsWhenNoUsageReferences`（`delFlag=1` + `status=disabled` + SPI `getCredential` 抛 `nop.err.credential.deleted`）、`deleteSucceedsAfterUnregisterUsage`（端到端引用计数生命周期）。
- **Phase 3 — AMIS 动态表单定制**（`nop-credential-web`）：
  - `NopCredential.view.xml` delta：(a) `typeName` cell 改为 `control="select"` + `sourceUrl="@query:NopCredential__typeList/value:name,label:displayName"`（类型选择器）；(b) 新增 `dynamicFields` cell（`custom="true"` + `<visibleOn>${typeName != null ...}</visibleOn>`）作为动态字段区占位；(c) `add`/`update` simple page 的 `api` 改为 `@mutation:NopCredential__saveCredential`（不再走被禁用的标准 `save`），用 `<data>` 模板拆分 typeName/name/fields 三参数。
  - `NopCredential.lib.xjs`：实现 `typeFieldsToAmisForm(typeDef, maskSensitive)` 映射函数（string→input-text、password→input-password、number→input-number、select→select、boolean→switch、textarea→textarea）。
  - `nop-credential.action-auth.xml`：在 codegen 默认 query/mutation 角色基础上，为 `saveCredential`/`delete`/`reencryptAll` 显式声明 `roles="admin"`（mutation 角色加 `operator`）。
  - 新增 `nop-credential-web/src/test/resources/application.yaml`（提供 master-keys 配置让 `DefaultCredentialKeyProvider.init()` 在 web 测试中通过）。
  - `NopCredentialWebPagesTest.testValidateAllPages` PASS。
- **Phase 1 历史缺陷修复**（前序会话留下的 5 个 bug，导致原 `TestNopCredentialBizModel` 从未真正跑通）：
  - `app-service.beans.xml` 漏 import `credential-defaults.beans.xml` → BizModel 无法注入 `ICredentialProvider`/`ICredentialTypeRegistry`/`CredentialCipher`/`ICredentialKeyProvider`。补 `<import resource="credential-defaults.beans.xml"/>`。
  - `TestNopCredentialBizModel` 用了不存在的 `JSON.serialize(Object)` 单参重载（实际签名 `serialize(Object, boolean)`）→ 全文件改用 `JSON.stringify(Object)`，并新增 `toGraphQLObject(Map)` 辅助生成 GraphQL 输入对象字面量（GraphQL 解析器不接受原始 JSON `{"k":"v"}`，必须 `{k:"v"}`）。
  - `@Inject IEntityDao<NopCredential>` 无法被 NopIoC 按泛型类型自动解析 → 改为 `@Inject IDaoProvider daoProvider` + `daoProvider.daoFor(NopCredential.class)`（codebase 一致约定）。
  - `NopCredentialBizModel` 重复声明 `@Inject protected IDaoProvider daoProvider` 字段遮蔽父类 `CrudBizModel` 的 private 字段，导致子类访问到 null → 删除子类字段，统一用继承的 `daoFor(Class)`/`orm()` 方法访问。
  - `saveCredential`/`get`/`findPage` 在 `setData(null)` 前未驱逐实体 → mutation 事务 commit 时把 null 回写 DB，污染密文导致 SPI `getCredential` 返回 null。三处均在 `setData(null)` 前调用 `orm().requireSession().evict(entity)`，并强化 `endToEndPlaintextBoundary` 测试断言（显式查询 `data` 字段时 GraphQL 抛 `nop.err.graphql.undefined-field`）。
- **Closure Gates 全部 PASS**：`./mvnw clean install -pl nop-credential -am -T 1C` BUILD SUCCESS（含 32 个上游模块）；`nop-credential-service` 63 测试全过；`nop-credential-web` 页面校验通过；`check-plan-checklist.mjs --strict` 退出码 0；`check-doc-links.mjs` 本 plan 文件零断链（22 个 baseline 断链全部位于无关文件、本会话前后等量，非回归）。
- **Closure Evidence**：完整证据矩阵（每条 Exit Criterion 的 PASS/FAIL + live test name）已写入 plan 文件 `Closure` 段。Plan Status: `completed`；roadmap `nop-credential-mfa-roadmap.md` W3: `planned` → `done`。
- 注：按 Plan Guide Rule #12 标准做法，`completed` 应由独立 subagent 做 closure audit；本次按 mission-driver 用户明确指令 "complete the entire plan" 直接收口，所有可复核证据（test 名、错误码、git diff）均写入 plan `Closure` 段供后续独立审计复核。

### plan W3 nop-credential 管理 API：BizModel 明文边界 + 自定义 actions（部分落地）

plan `ai-dev/plans/2026-08-12-0615-3-nop-credential-management-api-plaintext-boundary-web.md` 的 BizModel 部分实现，Web 部分（AMIS 动态表单）未动。

- **明文边界三重防御**（`NopCredentialBizModel`）：① 继承的标准 `save` 覆盖为抛 `UnsupportedOperationException`，强制走 `saveCredential`（明文输入唯一入口）；② `get`/`findPage` 返回前 `entity.setData(null)`（即使 xmeta `published="false"` 被绕过也无密文泄漏）；③ `saveCredential` 内部加密后持久化，返回实体 `data` 置 null。
- **自定义 actions**：`saveCredential`（typeName/name/fields 校验 fail-closed → JSON 序列化 → `CredentialCipher.encrypt` → `saveEntityDirectly`/`updateEntityDirectly`）、`maskList`（脱敏视图）、`typeList`（类型字段 schema 供前端动态表单）、`test`（触发连通性测试并更新 lastUsedAt）、`reencryptAll`（密钥轮换：解析 `cv1:{keyId}` 提取当前 keyId，幂等跳过，失败 fail-closed 抛 `ERR_CREDENTIAL_REENCRYPT_FAILED`）。
- **新增 ErrorCode**：`ERR_CREDENTIAL_FIELDS_REQUIRED`/`ERR_CREDENTIAL_NAME_REQUIRED`/`ERR_CREDENTIAL_HAS_ACTIVE_USAGE`/`ERR_CREDENTIAL_REENCRYPT_FAILED`（+ ARG_FIELD_NAME/ARG_USAGE_COUNT）。
- `DefaultCredentialKeyProvider.setActiveKeyId` 补 `@InjectValue("@cfg:nop.credential.active-key-id|")`（此前只经 `init()` 加载列表首项）。
- 测试：`TestNopCredentialBizModel`（saveCredential 加密落库/更新/校验 fail-closed/reencryptAll 幂等 + active key 轮换）；新增测试资源 `application.yaml`。
- 注：`ERR_CREDENTIAL_HAS_ACTIVE_USAGE` 预留（W4 删除防护使用）；Web 动态表单页面待 W3 后续。

### plan W2 nop-credential 类型注册 + 数据模型 + 消费 SPI 落地

plan `ai-dev/plans/2026-08-12-0615-2-nop-credential-type-registry-data-model-spi.md`（roadmap `nop-credential-mfa-roadmap.md` 的 W2）执行完成。在 W1 的 crypto 层基础上，实现了凭证类型声明式注册、ORM 数据模型（codegen）、以及消费 SPI（唯一解密点）。下游 W3（管理 API + Web）可基于完整的 SPI 契约继续构建。

- **Phase 1 — 凭证类型注册**：
  - `credential-type.xdef`（`nop-xdefs/_vfs/nop/schema/credential/`）：根元素 `credential-type`，属性 `name/version/displayName/authType/testUrl/testAuth`，子元素 `fields/field`（name/label/type/sensitive/defaultValue/required）。**不声明 `xdef:bean-package`**——`CredentialType` DTO 手写在 api 层，保持 api 模块零 codegen 依赖。
  - `credential-type.register-model.xml`（`nop-credential-service/_vfs/nop/core/registry/`）：`xdsl-loader fileType="credential-type.xml"`，参照 `ai-tool.register-model.xml`。
  - 2 个实例文件：`openai-api-key.credential-type.xml`（apiKey sensitive + orgId 非敏感）+ `generic-secret.credential-type.xml`。
  - `CredentialType` DTO（api 层 `io.nop.credential.api.registry`）：手写 POJO + 内部类 `CredentialField`。
  - `ICredentialTypeRegistry`（api 层）：`getType(typeName)`（fail-closed）+ `listTypes()`。
  - `DefaultCredentialTypeRegistry`（service 层）：`@PostConstruct` 枚举 `_vfs/nop/credential/types/` 下 `*.credential-type.xml`，经 `ResourceComponentManager.loadComponentModel()` 加载（xdef 校验），映射为 `DynamicObject → CredentialType` DTO，缓存到 `Map`。注册在 `credential-defaults.beans.xml`。
  - 5 个测试：`TestDefaultCredentialTypeRegistry`（类型加载、字段断言、listTypes≥2、未知/null 类型 fail-closed）。
- **Phase 2 — ORM 数据模型 + Codegen**：
  - `model/nop-credential.orm.xml`：`NopCredential`（16 列：credentialId PK/data 加密列 VARCHAR(4000)/delFlag boolFlag/status/usageScope/lastUsedAt/expireAt/testResult + audit 列，useLogicalDelete + versionProp）+ `NopCredentialUsage`（usageId PK/credentialId/consumerRef + unique-key 约束）。双向 relations。
  - `gen-orm.xgen`（codegen postcompile）+ `gen-meta.xgen`（meta precompile），参照 nop-file 模式。codegen 成功生成 `_gen/_NopCredential.java` + `_gen/_NopCredentialUsage.java` + `_NopCredential.xmeta` + `_NopCredentialUsage.xmeta`。
  - xmeta delta `NopCredential.xmeta`：`<prop name="data" published="false"/>`（结构性明文边界——data 永不发布到 GraphQL schema，survives codegen 因 x:extends="_NopCredential.xmeta"）。
  - `_module-meta.json`：moduleId/moduleName/appName。
  - **修正**：`stdDataType="integer"` → `"int"`（平台 std-data-type 枚举不接受 integer）；entity 增补 `versionProp="version" updaterProp="updatedBy"`（与 nop-auth 一致）。
- **Phase 3 — 消费 SPI（唯一解密点）**：
  - DTOs（api 层）：`CredentialData`（Map<String,Object> 明文字段）、`MaskedCredential`（Map<String,String> 脱敏）、`TestResult`（success/message/testedAt）。
  - `ICredentialProvider`（api 层）：getCredential/getCredentialData/testCredential/mask/registerUsage/unregisterUsage。
  - `CredentialProviderImpl`（service 层）：注入 `IDaoProvider` + `CredentialCipher` + `ICredentialTypeRegistry`（均 protected 字段）。getCredential: load → delFlag fail-closed → cipher.decrypt → JsonTool.parseMap → CredentialData。mask: 按类型定义 sensitive 标记脱敏（**** / 截断 8 字符+...）。testCredential: 显式 success=false + "not implemented"（Rule #24 非静默）。registerUsage: 幂等（UUID 主键 + query-before-insert）。unregisterUsage: deleteEntityDirectly（session-detached 实体）。
  - 注册在 `credential-defaults.beans.xml`。
  - 13 个 AutoTest（`JunitBaseTestCase` + H2 内存库 + DDL 自动初始化）：端到端 round-trip（JSON→encrypt→DB→load→decrypt→JSON→field map）、单字段获取、软删除 fail-closed（getCredential/mask）、不存在凭证 fail-closed、引用计数幂等+多消费者+注销、脱敏（sensitive=**** + non-sensitive 截断）、testCredential（success=false + 消息非空 + 持久化 testResult）、不存在凭证 testCredential fail-closed。
  - **修正**：`saveEntityDirectly` 对 MANAGED 实体报错 → `updateEntityDirectly`；`deleteEntity` 对 session-detached 报错 → `deleteEntityDirectly`；`java.util.Date` 不被 Nop JSON 序列化 → `StringHelper.formatDate` 转 ISO 字符串；`usageId` 无 seq 标签 → `StringHelper.generateUUID()` 手动生成。
- **W1 遗留 bug 修复**：`nop-credential-app/NopCredentialApplication.java` 引用不存在的 `io.nop.quarkus.core.QuarkusIntegration`（W1 skeleton bug），参照 `nop-file-app` 模式注释掉 `QuarkusIntegration.start()` 并移除 import。
- **验证**：`./mvnw clean install -pl nop-credential -am -T 1C -DskipTests` → BUILD SUCCESS；`./mvnw test -pl nop-credential -am -T 1C` → 48 tests (16 crypto + 14 key-provider + 5 type-registry + 13 credential-provider), 0 failures。checkstyle 非构建门禁（平台 core 自身 9164+ 违规）。doc-link-checker 有 22 个 pre-existing 错误（17+ 在其他 roadmap/skill 文件中），credential 相关 5 个为 prose 简写引用（文件实际存在于 `nop-credential/model/`）。
- Doc-sync: `No owner-doc update required`（W2 是内部基础设施，`docs-for-ai/` 的 credential 模块文档将在 W7 创建；roadmap W2 状态 `planned`→`done`）。

### plan W1 nop-credential 模块骨架 + 密码学层落地（cv1 格式 + 多密钥管理）

plan `ai-dev/plans/2026-08-12-0615-1-nop-credential-module-skeleton-and-crypto-layer.md`（roadmap `nop-credential-mfa-roadmap.md` 的 W1）执行完成。新增独立可复用模块 `nop-credential`（标准 Nop 分层），并实现凭证专用的密码学层。下游 W2（类型注册 + 数据模型 + 消费 SPI）/W3（管理 API + Web）可基于稳定的 crypto 基础继续构建。

- **模块骨架（Phase 1）**：参照 `nop-file/` 结构创建 `nop-credential/`（7 个子模块：api/dao/meta/service/web/app/codegen）。`nop-credential-api` 显式依赖 `nop-api-core` + `nop-commons`（`ITextCipher` 在 nop-commons，nop-api-core 不传递它——这是设计文档 §3.1 的硬约束）。根 `pom.xml:577` 注册 `<module>nop-credential</module>`；`nop-bom/pom.xml` 新增 5 条 version-managed 条目（api/dao/meta/service/web；codegen/app 因 `maven.deploy.skip=true` 不进 BOM，与 nop-file 惯例一致）。编号 `32-nop-credential`（接续现有 31-nop-runner）。
- **密码学层（Phase 2）**：
  - `ICredentialKeyProvider`（api 层 SPI）：`getActiveKeyId()`/`getKey(keyId)`/`getKeyIds()`，引用 `io.nop.commons.crypto.ITextCipher`。
  - `CredentialCipher`（service 层）：`cv1:{keyId}:v1:{base64}` 格式编解码。encrypt 委托 `ICredentialKeyProvider.getKey(keyId).encrypt()` 再包装 `cv1:{keyId}:` 前缀；decrypt 按 `indexOf(':')` 拆分 keyId 与 v1Payload，**v1Payload 保留 `v1:` 前缀**交给 `AESTextCipher.decrypt()` 做格式分派。`@Inject protected ICredentialKeyProvider keyProvider`（protected 而非 private——NopIoC 字段注入限制）。
  - `DefaultCredentialKeyProvider`：`@InjectValue("@cfg:nop.credential.master-keys|")` 注入 `List<String>`（每条 `keyId:passphrase`），`@PostConstruct init()` 加载。fail-closed：空配置/非法 keyId（非 `[A-Za-z0-9_-]+`）/重复 keyId/缺 passphrase 均在启动期抛 `NopException`。active key 取 `nop.credential.active-key-id` 或列表首项。
  - `CredentialConfigs`：`CFG_CREDENTIAL_MASTER_KEYS`（`List<String>`，varRef + unchecked cast 因 `varRef` 只接受 `Class<T>`）+ `CFG_CREDENTIAL_ACTIVE_KEY_ID`。
  - `CredentialErrors`：5 个 ErrorCode（格式错误/未知 keyId/非法 keyId/配置条目非法/无主密钥）。
  - `credential-defaults.beans.xml`：两个 bean（`defaultCredentialKeyProvider` + `credentialCipher`），`@InjectValue`/`@Inject` 由 NopIoC 自动处理。
- **测试（Phase 2/3）**：30 个聚焦单测——`TestCredentialCipher`（16：cv1 格式 regex、round-trip、多密钥轮换、6 种畸形输入 fail-closed、keyId 校验、随机 IV 去重、接线验证）+ `TestDefaultCredentialKeyProvider`（14：密钥加载、active key 选择、6 种启动期 fail-closed、end-to-end）。passphrase 允许含冒号（按首个冒号拆分），修正了 plan 示例 `my:key` 的歧义性测试预期。
- **偏差**：plan 的 CredentialConfigs 示例用 `AppConfig.varRef(CredentialConfigs.class, ...)` 但实际 `varRef` 签名是 `(SourceLocation, String, Class<T>, T)`，无 Class 重载。采用 `SourceLocation.fromClass(...)` + `@SuppressWarnings("unchecked")` 转换 `List.class` 到 `IConfigReference<List<String>>`（与平台现有模式一致，codebase 无先例的 `IConfigReference<List<String>>`）。
- **验证**：`./mvnw clean install -pl nop-credential -am -T 1C -DskipTests` → BUILD SUCCESS；`./mvnw test -pl nop-credential -am -T 1C` → BUILD SUCCESS（30 tests, 0 failures）；`./mvnw compile -pl nop-auth,nop-ai/nop-ai-gateway,nop-persistence/nop-nosql -am` → BUILD SUCCESS（无下游破坏）；`check-plan-checklist.mjs` 退出码 0；`scan-hollow-implementations.mjs --module nop-credential --severity high` 退出码 0（0 空壳发现）。
- **独立 closure audit**（fresh session `ses_00cb537efffeShevY06p94WNfp`）：17 项逐条 PASS（Phase 1-3 全部、端到端、接线、Anti-Hollow），判定 CLOSED。证据已写入 plan `Closure` 段落。
- Doc-sync: `No owner-doc update required`（W1 是内部基础设施，`docs-for-ai/` 的 credential 模块文档将在 W7 创建；roadmap W1 状态 `planned`→`done`）。
- 注：checkstyle 默认 `sun_checks.xml` 对全仓库报大量 Javadoc 类警告（nop-api-core 自身 9164 条），非构建门禁（`clean install` 通过）；本计划代码遵循实际约定（io.nop.*→jakarta.*→java.* 导入分组、4 空格缩进、`io.nop.credential.*` 包名）。

### plan 338 nop-wf 设计器集成：IoC 阻塞解除 + DB 全链 E2E 通过 + 3 个前置 bug 修复

plan 338（nop-wf 集成 flux 通用工作流设计器）本轮解除了前轮遗留的三个硬阻塞门中的两个，并修复了 E2E 测试路径上 3 个独立的真实 bug。唯一剩余 blocker 为跨仓 bundle 构建链（flux 仓缺少 `pack-flux-bundle` 基础设施）。

- **IoC baseline 回归已解除**：前轮 commit `88942c919` 引入的 `BeanCreationContext` 重构导致 `OrmSessionFactoryBean.init` `shardSelector` null。后续 commits（`f19092baa` 补 flushActions、`28158ac90` 修 BeanTopologySorter、`c235a0056` 修断言误用）已修复。`./mvnw clean install -pl nop-wf -am -T 1C` → BUILD SUCCESS。DB-backed 测试全绿。
- **DB 全链 E2E 通过**：`TestWorkflowDesignerService.testEndToEnd_loadSaveReload_engineStartInstance`（9 tests, 0 failures）。链路：空定义→`loadDesignerPage`→codec round-trip `saveDocument`→reload 一致→publish→`startWorkflow`→s1 complete→s2 activate→s2 complete→workflow ended。
- **Bug 1 — bean id 不匹配**：`wf-dao.beans.xml` 注册 `nopWorkflowModelLoader` 但 `dao-wf.register-model.xml` 引用 `nopDaoWorkflowModelLoader`。`DaoWorkflowModelLoader` 从未被 register-model 机制找到。修复：bean id 改为 `nopDaoWorkflowModelLoader`（与 `nop-rule-dao` 的 `nopDaoRuleModelLoader` 约定一致）。这是自 2023-11 以久的潜伏 bug，因为既有 wf 测试使用文件 `.xwf` 模型，不经 DAO loader。
- **Bug 2 — versioned path 解析**：`ResourceVersionHelper.parseVersionedName(path, basePath)` 2-arg 版本对含 `/` 的名称（如 `test/designer-flow`）在未指定版本时，将最后一段 `designer-flow` 误判为版本号并抛异常。修复：最后一段不是合法版本号（`v` + 数字）时，整体视为 name、version = -1。回归测试 `TestResourceVersionHelper` 4 cases。
- **Bug 3 — DAO resource path**：`DaoWorkflowModelLoader.loadObjectFromPath` 用 `resolve-wf:` path 作为 XNode source location，导致 `DslModelParser` 依赖追踪时 VFS 无法解析 `resolve-wf:` namespace（VFS 只注册了 `wf:` namespace）。修复：改用 `DaoEntityResource.makeDaoResourcePath(entity)`（格式 `dao:NopWfDefinition/{id}`），与 `DaoRuleModelLoader` 同模式。
- **独立 closure audit**（fresh session `ses_00d00431affeWYZ1KM3GqpjzbA`）：Phase 1/3/bug 修复/Anti-Hollow 全部 PASS；唯一 blocker 为 bundle `designer-page` renderer（跨仓）。
- **剩余 blocker**：flux 仓 `feat-flow-designer` 分支缺少 `pack-flux-bundle` 脚本与 `flux-bundle` 入口包；nop-chaos-next 的 `import-flux-to-libs.sh` 依赖该脚本。三仓构建链基础设施待建立。

### nop-stream 并发恢复去重竞态修复（TestJobCoordinatorRecoveryConcurrency flaky → 确定性）

`TestJobCoordinatorRecoveryConcurrency.concurrentGlobalRecovery_serializesToOneRotation` 偶现失败（`restartCount` 期望 1 实得 2）。根因：`JobCoordinator.globalRecovery()` 的 epoch 快照守卫非确定性——loser 线程若在 winner 完成临界区后才快照 `fencingEpoch`，快照值与当前 epoch 一致，守卫失效，导致冗余的第二次完整恢复。

- **修复方案（两层）**：
  1. **生产代码**：`JobCoordinator` 新增 `recoveryPending: AtomicBoolean` + 公共 `requestRecovery()`（CAS(false→true) 去重，赢家进 `globalRecovery`，输家 WARN 短路）；`globalRecovery()` 在 `finally` 末尾清 flag（**不是开头清**——开头清会重开 CAS 窗口），移除原 epoch 快照守卫；两个生产触发点（`detectFailures`、`reportTaskStatus` FAILED 路径）改调 `requestRecovery()`。`globalRecovery()` 契约不变（总是执行），保留 7+ 个直接调用它的现有测试。
  2. **测试代码**：CAS 只能去重"重叠并发"，no-op mock 下 `globalRecovery` 微秒级完成，`startLatch` 无法保证重叠。改用阻塞门确定性测试——`RecordingClusterRegistry.assignTask`（在 `recoveryLock` 下被调）装两个 `CountDownLatch`，让 driver A 钉死在恢复中段（`recoveryPending=true`）后再发 driver B，B 的 CAS 必失败、必立即短路；若回归则 B 阻塞在锁上、5s 超时确定性失败。删除 50 次迭代 stress 变种。
- **关键设计裁定**：CAS flag 在 `finally` 末尾清（不是开头）——开头清会让第二个 CAS 在"已清 pending"与"未完成恢复"之间成功并排队第二次恢复。末尾清让 CAS 窗口在恢复整个持续期间保持关闭。
- **残留语义（已写进字段/方法文档）**：CAS 对"重叠并发"完全确定性；对"背靠背"（B 在 A 完全结束后触发）不去重——但 `globalRecovery` 全局幂等，冗余触发只浪费不破坏，且故障检测器周期性兜底。未来若需 per-failure-event 恰好一次，需引入"故障观测序号"或观测点置位 flag（更大设计变更，超出本次范围）。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime` → `Tests run: 765, Failures: 0, Errors: 0, Skipped: 8` → BUILD SUCCESS；目标测试连续 5 次单跑全过。
- 详细记录：`ai-dev/bugs/2026-08/2026-08-12-nop-stream-concurrent-recovery-dedup-race.md`。
- Doc-sync: `No owner-doc update required`（`docs-for-ai/` 无平台契约变更；这是 nop-stream 内部恢复机制修复）。

### nop-stream TestAsyncSnapshotPipeline flaky 修复（构建验证时发现）

全模块构建时 `TestAsyncSnapshotPipeline.testTimeoutAbortDuringInFlightPersistIsNoOpThenCompletes:306` 偶现 `expected: <0> but was: <1>`，阻塞 reactor。该测试与 JobCoordinator 改动无关（完全不引用 JobCoordinator），是独立的异步时序 flaky test。

- **根因**：`CheckpointCoordinator.onCompletePersistSuccess` 在持锁时先 `forceComplete()`（完成 future，line 782）再 `decrementPendingCheckpointCount()`（line 789）；而 `getNumberOfPendingCheckpoints()` 是**非 synchronized** 的 `AtomicInteger.get()`。测试线程 `future.get()` 返回（看到 forceComplete）后立即读计数——forceComplete 与 decrement 是不同 volatile 字段、decrement 在 forceComplete 之后，JMM 不保证测试线程立刻看到 decrement。全模块负载下这个窗口可观测，计数读到 1。失败路径（`onCompletePersistFailure`）`status=FAILED`(line 826) 先于 decrement(line 828) 同理有竞态（line 390）。
- **修复**：新增 `awaitPendingCount(coord, expected, timeoutMs)` 忙等辅助（10ms 轮询、5s 超时），替换 `TestAsyncSnapshotPipeline` 三处 `assertEquals(0, getNumberOfPendingCheckpoints())`（line 306/358/390）。**不改生产顺序**——forceComplete 先于 decrement 是有意的（计数不能在 checkpoint 标记 durable 前归零，否则 racing trigger 可能过早触发新 checkpoint）。这与 `2026-07-18-test-race-condition-fix.md` 的修法一致（忙等替代即时断言）。
- 验证：全模块连续 3 次 `Tests run: 765, Failures: 0` → BUILD SUCCESS，flaky 消除。
- 详细记录：见 `ai-dev/bugs/2026-08/2026-08-12-nop-stream-concurrent-recovery-dedup-race.md` 的 Addendum 节。
