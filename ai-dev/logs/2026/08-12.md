# 2026-08-12 开发日志

### Plan 339 closure（completed）：独立 audit PASS-WITH-NOTES，4 个 notes 全部修复并复验

- 关联 Plan：`ai-dev/plans/339-nop-job-dispatch-routing-orthogonalization.md`（Plan Status/Phase Status/Closure Gates 已全部 tick 并置 completed）。
- 独立子 agent（fresh session，task ses_00b7aec0bffe5zWguaXO7dMrNH）closure audit 结论：**PASS-WITH-NOTES**——路由/builder 语义与 live 代码完全一致，容器接线实测通过；4 个非阻塞 note 已全部修复：
  1. `executor-kind.dict.yaml` rpcBroadcast 描述为空（1.4 漏项）→ 已补写澄清。
  2. `invoker-design.md` §3.3 仍展示旧 dispatcher 代码 → 已改写为 v6 新实现（Map 注入 + dispatchMode 唯一路由）。
  3. `nop-job.md` bestFit"必须单 assignment/fail fast"断言与 live 不符（`AdaptiveJobTaskBuilder` 循环多 assignment）→ 已按 live 行为修正。
  4. `ERR_JOB_NO_AVAILABLE_INSTANCE` define 漏声明 `ARG_HEALTHY_COUNT` → 已补。
- 复验：`./mvnw compile -pl nop-job/nop-job-core,nop-job/nop-job-coordinator -am` BUILD SUCCESS；Routing 8/8、ContainerWiring 1 PASS+1 @Disabled、Rpc 6/6、Partition 9/9、Adaptive 15/15 全绿。
- 工具：check-plan-checklist/scan-hollow（0 findings）/check-doc-links（0 errors）退出码均 0。
- checkstyle note：父 pom 中 maven-checkstyle-plugin 被注释（非活跃门禁），全模块手工 checkstyle 844 违规含既往文件，非本 plan 回归；按 AGENTS.md 实际规范人工核对通过。
- Follow-up：H2 修复后启用 ContainerWiring E2E 落库断言；`TestLocalJobScheduler.testPeriodicExecution` 时序 flaky 复跑绿，可考虑宽松化。

### Plan 339 执行（Phase 1 + 2 完成，Phase 3 收口中）：dispatchMode/executorKind 正交化 + TaskBuilder 收敛

- 关联 Plan：`ai-dev/plans/339-nop-job-dispatch-routing-orthogonalization.md`（两轮对抗审查 R1/R2 已吸收，用户批准执行）。
- 性质：**有意行为变更**（用户裁定不考虑兼容性）——`executorKind` 退出 coordinator 路由；service 型 builder 配置缺失从静默 fallback 改为显式抛错。

**Phase 1 — 路由正交化（已完成）**
- `JobDispatcherScannerImpl`：`defaultTaskBuilder` 字段 + `TASK_BUILDER_PREFIX` + `BeanContainer.tryGetBean` 运行时拼接 → `Map<String,IJobTaskBuilder> taskBuilders`（XML property 注入，无 `@Inject`）；`resolveTaskBuilder` 重写：null/blank→`"single"`，`taskBuilders.get(mode)`，缺失抛 `ERR_JOB_DISPATCH_MODE_NOT_IMPLEMENTED`（带 ARG_DISPATCH_MODE/ARG_JOB_FIRE_ID）；新增 package-private `getTaskBuilders()` 供容器级接线测试。
- `app-engine.beans.xml`：新增 `nopJobTaskBuilder_single`；删 `default`/`rpcBroadcast` 别名；dispatcher `<property name="taskBuilders"><ioc:collect-beans as-map="true" name-prefix="nopJobTaskBuilder_" by-type="IJobTaskBuilder"/></property>`。
- `JobCoreErrors.java:100` 错误消息补 bestFit。
- 2 个 dict 描述更新（single 不再走 executorKind 路由；rpcBroadcast 澄清仅 worker 侧 invoker 选择）。
- 测试：`TestJobDispatcherScannerRouting` 重写 8 用例（含 `single`+`executorKind=rpcBroadcast`→single 正交性断言）8/8 绿；`TestJobCoordinatorScanner`/`TestJobConcurrency` 去 StaticBeanContainer、共享 helper 注入 `single` key；新增 `TestJobDispatcherContainerWiring`：map 接线断言 PASS（4 key、类型断言）+ E2E 落库用例 `@Disabled`（H2 blocked，fallback path 注明）。

**Phase 2 — builder 收敛（已完成）**
- 新增 `AbstractServiceTaskBuilder` 共享基类：daoProvider + task 实体填充、`requireServiceName`（缺失/非 String → `ERR_JOB_SERVICE_NAME_REQUIRED`）、`resolveHealthyInstances`（null client → `ERR_JOB_DISCOVERY_CLIENT_REQUIRED`；空/全不健康 → `ERR_JOB_NO_AVAILABLE_INSTANCE`）。
- `RpcBroadcastTaskBuilder`/`PartitionTaskBuilder`/`AdaptiveJobTaskBuilder` 继承基类、删除内嵌 fallback（净删 ~40 行）；`DefaultJobTaskBuilder` 保持独立。
- `JobCoreErrors` 新增 3 错误码 + `ARG_SERVICE_NAME`/`ARG_HEALTHY_COUNT`。
- 测试改造：3 个 builder 测试类 8 个 fallback/CCE 用例 → `assertThrows` + errorCode 断言；正路径用例保留。

**验证**
- `./mvnw install -pl nop-job/nop-job-core,nop-job/nop-job-dao -am -DskipTests` 全绿。
- Builder 专项：`TestRpcBroadcastTaskBuilder` 6/6、`TestPartitionTaskBuilder` 9/9、`TestAdaptiveJobTaskBuilder` 15/15、`TestDefaultJobTaskBuilder` 3/3、`TestLeastLoadedStrategy` 4/4。
- 全模块回归 `-pl nop-job/nop-job-coordinator,nop-job/nop-job-worker,nop-job/nop-job-service,nop-job/nop-job-api -am`：仅 pre-existing H2 失败（见下），其余全绿（含 service 模块 BizModel 测试 15+1+14）。
- `TestLocalJobScheduler.testPeriodicExecution` 偶发超时（`expected > 5 invocations but got 1`，~7s 窗口），复跑 12/12 绿——时序敏感 flaky，与本 plan 无关（nop-job-core 本 plan 仅改 JobCoreErrors 消息文本）。
- 全量编译：`./mvnw test-compile -pl nop-job/nop-job-coordinator -am` BUILD SUCCESS。

**pre-existing H2 环境失败（Closure Gates 豁免，与 plan 338/339 均无关）**
- `TestJobCoordinatorScanner` 21 errors、`TestJobConcurrency` 6 errors、`TestJobStoreImpl` 20 errors、`TestJobFireStoreRace` 11 errors、`TestJobWorkerScanner` 15 errors——全部 sqlState=42S04 / vendorCode=42104 "Table not found"，与 `ai-dev/logs/2026/08-11.md` 记录一致（已 git stash 在 master 基线复现）。

**Doc-sync**
- `docs-for-ai/03-modules/nop-job.md`：dispatchMode 路由段改写为单一路由描述 + 新增 service 型 builder 失败语义/运维后果段。
- `ai-dev/design/nop-job/invoker-design.md`：v6 同步（决策 #8、§2.5/§3.2 代码片段、§3.5 序列图、§3.6 路由优先级+bean 对照表、§6 变更清单 row 11）。
- `ai-dev/design/nop-job/worker-assignment-design.md`：Live Baseline 同步（单一 dispatchMode 路由、失败语义）。
- `source-anchors.md`：核实无 TaskBuilder 锚点，no-op。

- 偏差：无（两轮审查裁定全部按计划吸收；H2 受限用例走注明 fallback path）。
