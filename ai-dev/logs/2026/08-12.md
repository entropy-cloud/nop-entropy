# 2026-08-12

### plan 1217-8（Cycle 2 / I1 输出契约族门禁）完成 — closure audit 通过 + 全部收口（独立子 agent 审计）

- **独立 closure audit**（fresh session `ses_00a08570bffePIWj9TZH40LbQV`）：9/9 项 PASS——plan 文本一致性（4 Phase completed + 全 [x]）；Anti-Hollow（TestOutputContractInvariant 与 scan-output-contract 在 all 路径实际执行；E2E 3 用例真实断言）；registry + pins（4 实现类 / 6 发射点 / 2 pin key 与扫描器精确匹配，无 unpinned 无 stale）；mjs all + self-test exit 0；门禁子集 10 类 / 102 tests / 0 failures；E2E 3/3；文档五处收口；无静默跳过；hollow findings 集与基线一致（零新增）。**Verdict: ready for completed，零 blocking**。
- **收口动作**：Closure 段证据写入（Reviewer / session / 每条 Exit Criterion + Closure Gate PASS 证据）；Closure Gates 14 条全 [x]；`Plan Status: active` → `completed`；roadmap Cycle 2 / I1 行 `todo` → `done`（含执行结果回写）；`check-plan-checklist.mjs --strict` exit 0（Passed: 1）。
- **commit**：门禁类 + 注册表 + pins + fixtures + mjs 工具 + 文档同 commit（committed 回归测试，防漂移）。
- **遗留（显式 successor ownership）**：Cycle 2 / I2（red list + 对抗探查）= 直接 successor；跨 task interim fail-fast = Cycle 2 / I4；线协议 = `HG-01` 人工确认门。

### plan 1217-8（Cycle 2 / I1 输出契约族门禁）执行中 — Phase 4 完成（门禁全绿验证 + 文档收口）

- **门禁子集全绿（2026-08-12 实测）**：`./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime,nop-stream/nop-stream-cep -Dtest='Test*Invariant*'` BUILD SUCCESS——**10 门禁类 / 102 tests / 0 failures**（core 40 = 30+10 新增 TestOutputContractInvariant；runtime 34；cep 28）。
- **既有 E2E 不回归**：`./mvnw test -pl nop-stream/nop-stream-runtime -Dtest=TestSideOutputChainingE2E` 3/3 绿。
- **mjs 全子命令**：`node ai-dev/tools/check-nop-stream-invariants.mjs all` exit 0（inventory / sync / scan-iterations / scan-output-contract / self-test 全 OK）。
- **全量回归**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl nop-stream -am -T 1C` BUILD SUCCESS；`checkstyle:check` 9164 既有基线 violations（nop-api-core 先失败，非本 plan 回归）→ mission 回落 `lint not configured`；`check-import-order.mjs` 新测试文件 0 errors（io.nop.* → third-party → java.* 组序修正 2 处）。
- **门禁统计落档（唯一落点）**：新建 `ai-dev/audits/nop-stream-invariants/cycle2-I1-input.md`——门禁 9 → 10 类 / 102 tests 明细、E2E 3/3、pin 2 条明细（RWO-cross-task-noop / BRWO-cross-task-noop，含 key 语义）、Cycle 2 / I2 输入声明。
- **catalog #6 定稿**：`invariant-catalog.md` §5 不变式 #6 草稿 → 定稿（陈述 / 覆盖族 RL-7 + 跨 task 缺口 / 历史证据 R15-AR-4 + PR-1 / 检测方法 JUnit + mjs / 状态 = 已沉淀）。
- **core-design.md 不变式节**：§6 Side Output 更新（原「当前不支持侧输出」为 stale 陈述，实测已落地 6 发射点 + RL-7 转发）→ 新增 §6.1 不变式（#6 输出契约族，先例 window-design.md / cep-design.md），保留历史决策对照表。
- **roadmap**：Cycle 2 / I1 行 `todo` → `done`（closure audit 通过后正式流转）。
- Doc-sync: catalog + core-design.md + cycle2-I1-input.md + roadmap + logs 五处收口；`check-doc-links.mjs --strict` exit 0（3 条 BROKEN_LINK 为 `2026-08-12-0615-3-...` credential plan 既有基线，非本 plan 引入）。

### plan 1217-8（Cycle 2 / I1 输出契约族门禁）执行中 — Phase 3 完成（mjs scan-output-contract 子命令 + 注册表实现 + 过渡 pin 2 条 + self-test 正反例）

- **mjs 扩展**（`ai-dev/tools/check-nop-stream-invariants.mjs`）：新增 `scan-output-contract` 子命令，静态解析 `nop-stream/*/src/main/java`（全模块）：
  - **嵌套类解析**：`parseTypeStructure`（class/interface/enum + implements-clause 判定，`String.class` 形态经 `(`/`)`/`=`/`;` 令牌拒判防误报）+ `findCollectOutputTagMethod`（成员级 collect(OutputTag) 方法体提取，brace-depth + 括号配对；frame.bodyStart 即开括号，depth 从 0 起——初版 off-by-one 实测修正）+ `classifyCollectBody`（forward（consumer.accept( / .collect( / .accept(）> fail-fast（throw）> no-op（空体/仅注释）；未识别形态显式抛错 = 无静默归类）。
  - **V1** 类级枚举（`implements Output` 全类 ⊆ 注册表，含嵌套 `StreamTaskInvokable$RecordWriterOutput` 等）/**V2** 失效类/**V3** 行为漂移（方法体分类 vs 注册表分类；no-op + pinned-known-violation = 违规照发 + 过渡 pin 吸收=绿；其余不匹配=红；I4 翻转为 fail-fast 后违规串变化 → pin 不匹配 = unpinned + stale 双红，强制同步注册表）/**V4** 新增发射点（OutputTag 声明三形态 regex（字段/局部/参数）+ `.collect(<name>,` 匹配 + 实现类方法体内转发调用排除（TimestampedCollector:98 风格归 V1/V3））/ **V5** 失效发射点。
  - **pin 匹配**：共享 `mjs-pins.json` 按违规串前缀分 scanner 过滤（scan-iterations 只认非 `[scan-output-contract]` pin，scan-output-contract 反之——首跑 `all` 误报 2 stale pin 实测修正）；`validateOutputContractPins` 强制 pin 仅覆盖 cross-task 实例类。
- **先红后绿（mjs，2026-08-12 实测）**：pin 登记前 `scan-output-contract` exit 1，违规 = 2 条（RWO :645 / BRWO :705 空体 no-op vs pinned-known-violation——:705 为方法体 `{` 行，扫描语义；plan 基线 :706 为闭括号行）；pin 登记后 exit 0。
- **注册表实现**：`output-contract-registry.json` 与 Phase 1 设计稿一致（4 实现类 + 6 发射点 + 豁免清单 + V1–V5 + pin schema）；BRWO collectOutputTagLine 校准为 705（扫描语义 = 方法体起始行）。
- **pin 登记**：`mjs-pins.json` 新增 2 条（`key` = 扫描器实际违规串精确匹配；`invariant: "#6"`；`reason` = interim fail-fast 预授权 Cycle 2 / I4 + `HG-01` 人工确认门；`removalTrigger` = I4 修复落地 + 注册表分类更新，禁静默移除）；头注语义扩展为 #2 + #6。
- **self-test 扩展**：V1–V5 各一正一反 fixtures（内存 + committed `fixtures/OutputContractFixture.java`（空体未 pin → V3 红 + 未登记发射点 → V4 红，实测命中）；反例已 pin → 绿；新增类未登记 → 红；参数形态 OutputTag 发射点检出（ProcessOperator 风格）→ 注册绿/未注册红；实现类内部转发（TimestampedCollector:98 风格）→ 不判 V4；V5 失效发射点 → 红；未识别方法体 → 显式抛错）。
- **门禁**：`scan-output-contract` 独立 exit 0；`all` exit 0（inventory / sync / scan-iterations / scan-output-contract / self-test 全 OK）；JUnit 门禁子集 10 类 / 102 tests / 0 failures（core 40 = 30+10 新；runtime 34；cep 28）。
- Doc-sync: `ai-dev/tools/README.md` 工具清单新增 check-nop-stream-invariants.mjs 行（README 实测存在，确定更新）。

### plan 1217-8（Cycle 2 / I1 输出契约族门禁）执行中 — Phase 2 完成（JUnit TestOutputContractInvariant 实现，先红后绿）

- **test-first 先红（2026-08-12 实测，双阶段）**：新增 `TestOutputContractInvariant`（`nop-stream-core/src/test/java/io/nop/stream/core/operators/`，10 用例）。registry-backed 参数化穷举 4 个 main 实现类（读 `output-contract-registry.json`，分类以注册表为准，mirror `InvariantTableCompleteness` 先例）。**红阶段**：临时将注册表 RWO/BRWO 分类改为 `forward`（跨 task 空体实例未声明 pin 语义）→ `Tests run: 10, Failures: 2`（"no forward-behavior harness for RecordWriterOutput/BroadcastingRecordWriterOutput"）；**绿阶段**：恢复 `pinned-known-violation` → 10/10 绿。
- **实现类级断言**：ChainingOutput = 注册消费者转发（tag/值/时间戳断言）+ 无消费者 fail-fast（`ERR_STREAM_SIDE_OUTPUT_NO_CONSUMER` error code 断言）；TimestampedCollector = 透传至被包装 output（recording output 收到）+ **透传目标敏感**（包装 no-op output 丢弃 = 已断言记录）+ 包装反射实例化 `RecordWriterOutput` = 等价跨 task 丢弃（partition.size()==0 断言，注释关联过渡 pin/HG-01）；RWO/BRWO = 反射 `setAccessible(true)` 实例化（`new RecordWriter(new ResultPartition())` / recording inner list），断言空体 no-op（partition.size()==0 / sideReceived 空）。
- **接线断言（Rule #23）**：`testInvokableRegistrationReachesWiredChainingOutput`——`new OperatorChain([op1,op2])` → `new StreamTaskInvokable(chain)` → `registerSideOutputConsumer` → wireOperators 生成的 `ChainingOutput`（op1.getOutput()）collect → 消费者实例命中（共享 consumer map 运行时连通）。
- **端到端证据（Rule #22，归属定案）**：`TestSideOutputChainingE2E`（runtime）3/3 绿实测（端到端转发 / 无消费者 fail-fast / StreamTaskInvokable 接线）；core 门禁类未引用 runtime WindowOperator（依赖方向约束），未新增 core 内 E2E 副本。
- **门禁**：`./mvnw test -pl nop-stream/nop-stream-core` 全量 BUILD SUCCESS；`TestOutputContractInvariant` 10/10；E2E 3/3。
- **编译注意**：test 编译 `-source 11`（根 pom `maven.compiler.release=11`），禁 switch 表达式/规则（Java 14+），用传统 switch + break。
- Doc-sync: catalog #6 草稿（Phase 1 已落）+ core-design.md 不变式节记账到 Phase 4 收口。

### plan 1217-8（Cycle 2 / I1 输出契约族门禁）执行中 — Phase 1 完成（注册表与门禁语义设计定稿）

- **前置检查**：I6（plan `2026-08-12-1217-7`）Plan Status: completed 实测确认，硬前置满足；门禁范围与 I6 裁定一致（adjudication-table.md §6.1）。
- **live 复核（2026-08-12 实测）**：main `Output` 实现类 4 个（`ChainingOutput` :111 转发+无消费者 fail-fast / `TimestampedCollector` :97 纯透传（透传目标敏感）/ `StreamTaskInvokable$RecordWriterOutput` :645 空体 no-op / `$BroadcastingRecordWriterOutput` :706 空体 no-op）；发射点 `.collect(<tag>,` 全仓 grep = 7 处 = 6 发射点 + 1 实现类内部转发（TimestampedCollector:98，V1/V3 管辖）；CEP timed-out 路径经 `SideCollector.collect` → `ctx.output(timedOutPartialMatchesTag,...)` → CepOperator:777（无新增发射点）；tag 声明形态：WindowOperator :179 字段 + :1856 参数 / CepOperator :141 字段 + :770 参数 / ProcessOperator :110/:133 参数。
- **注册表定稿（新建）**：`ai-dev/audits/nop-stream-invariants/output-contract-registry.json`——schemaVersion 1，独立于 gate-inventory.json（`init` 再生，防覆盖）；实现类表（4 类 × {fqcn, 文件, collectOutputTagLine, 分类三态, 处置}）+ 发射点表（6 点 × {文件:行, 算子, 触发场景, tag 形态, 处置}）+ test-only 豁免清单（TestOutput / MockOutput / NopOutput / CapturingOutput，仅扫 `src/main/java`）+ V1–V5 违规语义 + pin schema（`key` 必需，Phase 3 以扫描器实际违规串为准）。gate-inventory.json 确认无需补表（ChainingOutput/StreamTaskInvokable 仅 involvedClasses，非注册类；类级枚举归 scan-output-contract 单一职责，不改 inventory schema）。
- **catalog 草稿节**：`invariant-catalog.md` §5 新增不变式 #6 草稿（陈述 / 覆盖族 RL-7 + 跨 task 缺口 / 历史证据 R15-AR-4 + PR-1 / 检测方法 JUnit + mjs / live 修复状态 in-task fixed + 跨 task 过渡 pin）。
- Doc-sync: `No owner-doc update required` 除 catalog #6 草稿节（本 Phase 为设计定稿 Decision，零代码变更）。
- 验证：设计稿每条 repo-observable（文件:行 + 期望行为 + 断言方式）。

### mission-driver build 验证（2026-08-12）— full-green verification

- **验证范围**：mission `nop-stream-invariant-loop` 构建验证；git diff 确认仅 `nop-stream` 模块组变更，`-pl nop-stream -am` 覆盖到位。
- **结果**：`./mvnw compile -pl nop-stream -am -q` 通过；`./mvnw clean install -DskipTests -pl nop-stream -am -T 1C` BUILD SUCCESS（全模块）；`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS —— runtime surefire **804 tests / 0 failures / 0 errors / 8 skipped**（skipped 均为既有环境依赖跳过）；含 I4/I5 门禁与 E2E 类（TestClusterRegistryConsistencyInvariant 10/10、TestSideOutputChainingE2E、TestWindowOperatorMergingCleanupInvariant 等）全绿。
- **lint 说明**：`checkstyle:check` 非本仓库门禁（未绑定构建生命周期，全库既有基线 violations 9164+，`nop-stream` 既有文件同样命中）→ 按 mission 配置回落 `lint not configured`，与历史轮次一致，非本次回归。

### plan 1217-7（I6 循环收口与 Cycle 2 派生）执行中 — Phase 1-2 完成（收口统计确认 + PD-15 新族裁定 + 跨 task 缺口双层裁决 + Cycle 2 派生落档）

- **收口统计核对（2026-08-12 实测，与 `cycle1-I6-input.md` 一致）**：nop-stream 模块组 10 模块 surefire 汇总 **2822 tests / 0 failures / 0 errors / 10 skipped**（core 1418 / runtime 804 / cep 320 / rocksdb 83 / connector 35 / connector-jdbc 32 / connector-batch 35 / connector-debezium 19 / flow 51 / fraud-example 25，逐模块吻合）；门禁 9 类 / 92 tests / 0 failures（CheckpointIDCounter 8 / SynchronizedCollection 12 / core 表完备 10 / ClusterRegistry 10 / Runtime 表完备 11 / MergingCleanup 4 / WindowRoundTrip 9 / CepRelease 21 / Cep 表完备 7）；mjs `mjs-pins.json` pinnedViolations 空（pin 0）。零出入，不回写 I5 落点。
- **red list 零悬挂确认**：red-list.md §5 7 条 RL 逐条修复证据在案且 commit 可溯源（RL-1..5 → `fcc71fc05` / RL-6 → `58255014b` / RL-7 → `b20fcd0e1`，git log 实测）；RL-3 roadmap backlog 闭合 ✅；I5 零新失败 → 无新增族，新族数 = 1（PD-15）。
- **call-site 盘点（live 实测复核）**：6 发射点全部确认——`ProcessOperator.java:111/:134`、`WindowOperator.java:1030/:1860`、`CepOperator.java:483/:777`；实现类内部转发（`TimestampedCollector.java:98` 纯转发 / `ChainingOutput.java:111` 转发 + fail-fast `ERR_STREAM_SIDE_OUTPUT_NO_CONSUMER`）排除出发射点；**跨 task 缺口实证**：`GraphExecutionPlan.java:454-458` → `StreamTaskInvokable.java:239/:245/:352` → tail setOutput(`RecordWriterOutput` :645 空体 / `BroadcastingRecordWriterOutput` :706 空体) → side-output 静默丢弃 = 同族已确认契约缺口（P1，生产可达）。
- **稳态判定**：新族在案 + 跨 task 缺口在案 → **非稳态 → 派生 Cycle 2**（「稳态暂停」分支不适用）。
- **PD-15 新族裁定**：输出契约族 = 正式新族（I2/I3 登记 + I4 修复基线 `b20fcd0e1` + I5 零新增）→ Loop Rule「新族强制沉淀」派生 Cycle 2 / I1，adjudication-table.md §I6 落档。
- **跨 task 缺口双层裁决**：层次 1 = interim fail-fast（空体 → 抛 `ERR_STREAM_SIDE_OUTPUT_NO_CONSUMER` 风格异常，类内部行为修复，Output 接口零变更，同 RL-7 先例，Rule #24）→ **预授权分派 Cycle 2 / I4**（I3 确认 → I4 执行，I1 门禁过渡 pin 全绿）；层次 2 = 线协议结构性重构（公共内部机制）→ 执行门 = 人工确认 → 登记 **`HG-01` 人工确认待办**（roadmap Follow-up Backlog，状态 `pending human confirmation`，Successor = 人工确认后另立 plan）。四重保护性覆盖（in-task fail-fast + I1 门禁 + 过渡 pin 2 条 + 待办条目），按 Rule #15 归 `Fix` 类，未降级。
- **Cycle 2 / I1 门禁范围裁定**：不变式 #6 一等门禁 = main `Output` 实现类 `collect(OutputTag)` 行为三态（转发 / fail-fast / pin）+ 类级枚举完备性（新增类不入注册表即红）+ call-site 注册表（6 发射点全覆盖，新增即红，跨 task 点标 `HG-01` 关联）+ 过渡 pin（`key` 关联 `HG-01`，移除 = Cycle 2 / I4 落地后）。
- **roadmap 回写**：Work Item 表追加 Cycle 2 / I1–I6 六行（I1 附 PD-15 触发证据：不变式陈述 + 发现 `文件:行`（live 发射点）+ 「PD-n 先例链」派生说明；依赖列裸标签）；Follow-up Backlog 追加 `HG-01` 条目；§Loop Rule + §I6 Phase Details 登记 Cycle 2 触发原因（新族沉淀 + 人工确认门）与复触发三选一（人工确认待办触发 = 人工批准线协议变更）。
- 验证：核对均为文档/裁决工作，零代码变更（`nop-stream/` 未动）；surefire/mjs/git 实测证据全部在案。
- Doc-sync: `No owner-doc update required`（纯文档/裁决计划，无行为契约变更；产出均属 `ai-dev/audits/` + `ai-dev/backlog/` + `ai-dev/logs/`）。


### plan 1217-6（I5 全量验证）完成 — 全量全绿 + 门禁零命中 + full-green 记录 + I6 输入落档

- **前置检查**：I4（plan `2026-08-12-1217-5`）Plan Status: completed 确认，硬前置满足后开始验证（未在 I4 未完成时误执行）。
- **全量测试**（2026-08-12 实测）：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（01:29 min）；nop-stream 模块组 10 模块 surefire 汇总 **2822 tests / 0 failures / 0 errors / 10 skipped**（core 1418 / runtime 804 / cep 320 / rocksdb 83 / connector 35 / connector-jdbc 32 / connector-batch 35 / connector-debezium 19 / flow 51 / fraud-example 25；skipped 均为既有环境依赖跳过：multi-jvm 进程 spawn 6 + Kafka/Pulsar backend E2E 2 + debezium CDC 1 + event-time window E2E 1）。
- **门禁零命中**：mjs `all` exit 0（无 stdout 输出属全绿正常态）；inventory / sync / scan-iterations / self-test 四子命令全 OK exit 0；unpinned=0、stale=0（`mjs-pins.json` pinnedViolations 为空）。JUnit 门禁子集 `./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime,nop-stream/nop-stream-cep -Dtest='Test*Invariant*'` BUILD SUCCESS——**9 个门禁类 92 tests 0 failures**（core 30：CheckpointIDCounter 8 + SynchronizedCollection 12 + 表完备 10；runtime 34：ClusterRegistryConsistency 10 + WindowRoundTrip 9 + WindowOperatorMergingCleanup 4 + 表完备 11；cep 28：CepReleaseSymmetry 21 + 表完备 7），含 I4 翻转/新增用例全部绿。
- **e2e 复跑**：`TestWindowOperatorUnificationE2E` 6/6 绿（runtime）+ `TestWindowEndToEnd` 7/7 绿（core）。
- **新失败处置**：零新失败（I4 遗漏 / 新族 / 上游无关失败均无），正常关闭路径，无 blocked 升级。
- **I6 输入统计落档（唯一落点）**：新建 `ai-dev/audits/nop-stream-invariants/cycle1-I6-input.md`——门禁 9 类 / 92 tests、JUnit 全量 2822、pin 0、red list 7 RL 修复确认（RL-3 闭合）、新族 1（PD-15 输出契约族在案）、e2e 摘要；`gate-inventory.json` 仅作门禁过程记录不承担统计落点。
- **roadmap 流转**：Work Item I5 `planned`→`done`（closure audit 通过后正式记录）。
- 验证：`check-plan-checklist.mjs <本plan> --strict` exit 0；`check-doc-links.mjs --strict` exit 0。
- Doc-sync: `No owner-doc update required`（纯验证计划，无代码变更、无行为契约变更）。

### plan 1217-5（I4 修复执行）执行中 — Phase 5 完成（门禁复跑零命中 + pin 注册表同步 + 文档收口）

- **门禁复跑零命中（2026-08-12 实测）**：五族 JUnit 门禁 92 tests 0 failures（原 84 + ClusterRegistry +2 / SynchColl +2 / MergingCleanup +2 + 3 表完备性不变）；`./mvnw test -pl nop-stream -am -T 1C` 全量 BUILD SUCCESS（nop-stream 族 15 模块）；`check-nop-stream-invariants.mjs all` exit 0（violations ⊆ pins 恒真、无 stale pin）；`./mvnw checkstyle:check -Pqa -pl nop-stream -am` 0 violations（WARN 均为既有基线文件，改动文件零 WARN）。
- **pin 注册表同步**：`mjs-pins.json` pinnedViolations 清空（2PC:83 修复后移除，先红后绿流程在案）；JUnit 4 处 pin 断言翻转记录（ClusterRegistry 2 + MergingCleanup 2）；`gate-inventory.json` 新增 `pinRegistry` 段（mjsPins 状态 / junitPinFlips 4 条 / 保留行为 pin / 涉及类清单）。
- **red-list.md §5 修复状态**：RL-1..7 逐条标注修复证据（commit + 翻转测试名 + 门禁复跑结果）；类别清扫结论四族汇总；RL-3 触发闭合；RL-7 双轨标注（PD-15 基线 + 跨 task no-op 移交 I6 人工确认）。
- **catalog 行号引用同步**：§3 R16-AR-9/18/1/11/R15-AR-8 全部转 fixed（live 行号以修复后为准）；§5 不变式 #2/#5 live 修复状态更新；§6 red list 候选表 + RL-7 新族候选登记。
- **roadmap 同步**：Work Item I4 `planned`→`done`（closure audit 通过后正式记录）；Follow-up Backlog RL-3 条目 ✅ 触发闭合（保留历史处置记录）。
- **工具门禁**：`check-doc-links.mjs --strict` exit 0（0 errors；本 plan 3 处路径引用已修正，剩余 9 warnings 为 1217-6/credential plan 既有）；`scan-hollow-implementations.mjs --module nop-stream --severity high` findings 集与基线 commit（34aed42c1）完全一致（既有 14 项，全为 fail-fast/有意 no-op），**无新增 finding**（沿用 I2 判据 = findings 集一致而非退出码，工具无豁免机制）。
- Doc-sync: `No owner-doc update required`（全部 Phase 为类内部行为修复，行为契约不变；审计文档同步均已在案）。

### plan 1217-5（I4 修复执行）执行中 — Phase 4 完成（输出契约族：RL-7 ChainingOutput 侧输出转发 + fail-fast + 端到端测试）

- **test-first 先红（2026-08-12 实测，双阶段）**：新增 `TestSideOutputChainingE2E`（runtime，3 用例：端到端转发 / fail-fast / StreamTaskInvokable 接线）。既有 43 处 `new ChainingOutput` 构造点零改动兼容（2 构造器保留委托空 map；core/runtime/cep 全量复跑验证）。临时还原纯丢弃行为（保留 API 面）→ `Tests run: 3, Failures: 3` 全红（端到端转发 + fail-fast + 接线三断言）。
- **RL-7 修复**：`ChainingOutput` 新增共享 consumer map 3 参构造器 + `registerSideOutputConsumer`；`collect(OutputTag, record)` 转发注册消费者，无消费者 → 抛 `ERR_STREAM_SIDE_OUTPUT_NO_CONSUMER`（新错误码 + `ARG_OUTPUT_TAG`）；`LOG` 字段删除（LOG.warn+丢弃路径零残留）。`StreamTaskInvokable` 持有共享 map + 公开 `registerSideOutputConsumer`，wireOperators :171/:209 传入共享 map（注册可先于/晚于接线）。`Output` 接口契约零变更。行为变更：默认链式部署下侧输出无消费者 → fail-fast 崩溃（I3 裁决方向，三模块全量 0 failure 证实无既有测试依赖静默丢弃）。
- **类别清扫（输出契约族）**：call-site 全核对（WindowOperator.sideOutput :1029-1031 / CepOperator :482-483 + :770）；`TimestampedCollector.collect(OutputTag)` 纯转发（非丢弃点）；**跨 task no-op（RecordWriterOutput :645 / BroadcastingRecordWriterOutput :706）记录为同族已知实例 + 处置依据（线协议结构性变更）→ 移交 I6 人工确认候选**，不静默修也不静默忽略。
- **门禁**：`TestSideOutputChainingE2E` 3/3；`nop-stream-runtime` 804 / `nop-stream-core` 1418 / `nop-stream-cep` 320 全量 0 failures；`mjs all` exit 0。
- Doc-sync: `No owner-doc update required`（Output 接口契约不变，修复为实现侧）。

### plan 1217-5（I4 修复执行）执行中 — Phase 3 完成（F1 WindowOperator：RL-6 cleanup 收敛 + onProcessingTime 同型修复 + pin 翻转）

- **test-first 先红（2026-08-12 实测）**：翻转 2 个 pin 测试（cleanup 后 [10,80) 必须从 MergingWindowSet 移除 / t=55 必须生成 [55,105) 而非 [10,105)）+ 新增长跑「多次 session 开合 checkpoint 状态收敛」+ 新增 processing-time 分支用例 → `Tests run: 4, Failures: 4`（泄漏行为全确认）。
- **RL-6 修复**（`WindowOperator` onEventTime cleanup :773-782）：补 `mergingWindows.retireWindow(triggerContext.window)`——参数 = **mapping key**（cleanup timer namespace = in-flight window），非 stateWindow value（否则 `MergingWindowSet.java:134-139` 抛 StreamException）。**类别清扫发现 onProcessingTime cleanup 分支（:834-843）同型缺失 → 一并修复**。
- **onProcessingTime 测试**：`HeapInternalTimerService#fireProcessingTimeTimers` 推进路径可测 → 新增 `testProcessingTimeCleanupRetiresMergingWindow`（测试内 `ProcessingTimeMergingSessionWindows` = isEventTime=false + 元素时间戳确定性窗口；生产无 processing-time merging assigner，故为 test-local）。测试要点：fire 后须重新取 MergingWindowSet 实例（每次从持久化状态重建，预取实例 stale）；trigger/cleanup 双 timer 同 time+namespace 在 HashSet 坍缩为单条目，单回调内 fire + cleanup 分支同跑（与 event-time 一致）。
- **类别清扫（F1 族）**：`MergingWindowSet` 生产消费方仅 WindowOperator（全仓 grep）；retireWindow 三调用点（:657 既有 late 路径 + :788/:854 双 cleanup 修复）全部以 key 为参数；生产 MergingWindowAssigner 仅 EventTimeSessionWindows；purge ≠ GC cleanup（Flink 等价语义），不属清扫范围。
- **门禁**：`TestWindowOperatorMergingCleanupInvariant` 4/4（原 2 pin 翻转 + 2 新增）；`TestWindowRoundTripInvariant` 9/9；`TestWindowOperatorUnificationE2E` 6/6；`TestWindowEndToEnd`（core）7/7。
- Doc-sync: `No owner-doc update required`（行为修复与 Flink 语义对齐，无契约变更）。

### plan 1217-5（I4 修复执行）执行中 — Phase 2 完成（F2 TwoPhaseCommitSinkFunction：RL-4 P0 + RL-5 P1 + mjs pin 移除）

- **test-first 先红（确定性红）**：从 `mjs-pins.json` 移除 2PC:83 pin → `scan-iterations` 报 1 violation（`TwoPhaseCommitSinkFunction.java:83 iteration ... (copy-constructor) outside synchronized block`）exit 1。
- **RL-4 修复**（`TwoPhaseCommitSinkFunction.saveState`）：`new TreeMap<>(pendingCommits)` copy 移入 `synchronized (pendingCommits)` 块。
- **RL-5 修复**（`setPendingCommits`）：内部包装 `Collections.synchronizedMap(new TreeMap<>(pending))`（裁决表二选一首选；`StreamSinkOperator.java:157` 既有包装双层无害；全仓无 null 调用面，null → NPE fail-loud）。
- **新增测试**（`TestSynchronizedCollectionInvariant` 10→12）：`testSaveStateConcurrentWithCommitAbortIsSafe`（saveState 与 4 噪声线程 20 轮并发：无 CME + 首 50 条 pending 快照完整）+ `testSetPendingCommitsWrapsUnsafeMap`（确定性断言 = 防御性拷贝，传入 map 后续变更不泄漏进类内视图 + 并发冒烟）。
- **类别清扫（F2 族）**：`pendingCommits` 全使用点（:31/:34/:73/:81/:87-92 + finishCommit :101-118 与 restoreFromEpoch :158-167/:178/:190 对照面已 synchronized）；synchronized 集合 4 类全在门禁表（TwoPhaseCommitSinkFunction/StreamSinkOperator:157/SourceReaderOperator:98/LocalSourceCoordinator:167）；SinkFunction 兄弟核对——`BatchConsumerSinkFunction` buffer 为文档化「unsynchronized by design」普通 ArrayList（单线程执行模型，非不变式 #2 契约违约）；`recover(:67)` 零调用点死方法记录不修。
- **门禁**：`mjs all` exit 0（违规清零 + 无 stale pin）；`TestSynchronizedCollectionInvariant` 12/12；`./mvnw test -pl nop-stream/nop-stream-core` 1418 tests 0 failures。
- Doc-sync: `No owner-doc update required`（类内部行为修复，行为契约不变）。

### plan 1217-5（I4 修复执行）执行中 — Phase 1 完成（F5 ClusterRegistry：RL-1/RL-2/RL-3 修复 + 断言翻转 + 类别清扫）

- **test-first 先红（2026-08-12 实测）**：翻转 `TestClusterRegistryConsistencyInvariant` 两处 pin 断言（JDBC 注册后立即可见 / InMemory per-renewal 5s）+ 新增 2 一致性用例（InMemory 自定义 timeout 300ms 到期后淘汰；JDBC「注册 → 短租约过期 → 重新注册 UPDATE 路径 → 立即可见」）→ `Tests run: 10, Failures: 4`（4 处翻转/新增断言全红）。
- **RL-1 修复**（`JdbcClusterRegistry.registerNode`）：INSERT 分支 `lease_expire_at=0L` → `now + DEFAULT_LEASE_TIMEOUT_MS(15000L)`；**UPDATE 分支补齐**——`SET ... lease_expire_at = ?`（原 :106-110 不刷新，重启后重新注册停留旧过期值）。`getActiveNodes` 过滤语义不变（RL-2 互为表里）。
- **RL-3 触发闭合**（`InMemoryClusterRegistry`）：`leaseTimestamps` 单 map 改为 `leaseStartTimes` + `leaseExpireTimes` 双 map；renewLease 存 `now + leaseTimeoutMs`（参数），registerNode 存 `now + leaseTtlMs`（默认）；getNodeLease / evictExpiredNodes / getActiveNodes 全部读存储 expireAt——无固定 ttl 残留、无空分支/吞异常路径。
- **类别清扫（F5 族）**：全族 grep 核对——InMemory 5 方法 + JDBC INSERT/UPDATE/renewLease/getActiveNodes/getNodeLease 判定一致；`NodeDiscoveryConsistencyChecker` 仅消费 `getActiveNodes()`（无独立 lease 计算，视图自动一致）；`JdbcLeaderElector` 为独立 leader 选举租约（非本族）；`StreamNodeAutoRegistration` 仅接 discovery（兼容）。
- **门禁**：`TestClusterRegistryConsistencyInvariant` 10/10 绿（原 8 + 新增 2）；同族回归 TestInMemoryClusterRegistry 7 / TestInMemoryClusterRegistryFixes 9 / TestJdbcClusterRegistry 21 全绿；`check-nop-stream-invariants.mjs all` exit 0。
- Doc-sync: `No owner-doc update required`（类内部行为修复，行为契约不变）。

### mission DRAFT_PLANS：nop-stream 不变式闭环 Cycle 1 / I4 + I5 计划拟制并转 active（4 轮 + 3 轮独立对抗性审查）

- **plan 1217-5（I4 修复执行）**：以 I3 裁决表 §3 派发清单（WI-1..4）为输入起草——Phase 1 F5 ClusterRegistry（RL-1+2 联合 + **RL-3 backlog 触发闭合**，INSERT/UPDATE 双分支写 `now+15000L`，InMemory 按 per-renewal 参数计算活性）、Phase 2 F2 2PC 并发防护（RL-4 P0 + RL-5 P1，mjs pin 移除语义 + restoreFromEpoch synchronized 迭代块 :158/:178/:190 清扫）、Phase 3 F1 WindowOperator cleanup 收敛（RL-6，`retireWindow(triggerContext.window)` = mapping key 语义，onProcessingTime 同型 + 测试要求）、Phase 4 输出契约族 ChainingOutput 转发（RL-7 双轨，最小契约约束 + 既有 41 处构造点适配 + 跨 task no-op 处置规则移交 I6）、Phase 5 门禁复跑与 pin 注册表同步。4 轮独立 fresh-session 对抗审查修复：`-pl` 裸模块名（nested path）、retireWindow 参数、Phase 4 测试落点（runtime）、checkstyle `-Pqa`、restoreFromEpoch/recover 方法事实、registerNode UPDATE 分支漏项等 → verdict approved。
- **plan 1217-6（I5 全量验证）**：硬依赖 I4 completed（未完成则 Phase 1 blocked、Plan Status 保持 active）；全量 `./mvnw test -pl nop-stream -am -T 1C` + 门禁零命中（mjs all + 9 门禁类）+ e2e 复跑 + full-green 记录 + **I6 输入唯一落点 `ai-dev/audits/nop-stream-invariants/cycle1-I6-input.md`**；新失败处置 = 记录 + 分类（I4 遗漏/新族/上游无关）+ 移交，不因移交关闭。3 轮独立审查（Blocker：JUnit 门禁子集命令模块路径；Major：「新失败不阻塞关闭」自相矛盾 / I6 落点歧义）→ verdict approved。
- **live 事实复核**：全部 red list 行号、门禁 pin（`mjs-pins.json` 唯一 pin 2PC:83）、`MergingWindowSet.retireWindow` 抛异常语义、`restoreFromEpoch`（:153 真实恢复路径，`recover` :67 零调用死方法）、`JdbcClusterRegistry` UPDATE 分支（:106-110 不刷新 lease_expire_at）、`new ChainingOutput` 构造点 41 测试 + 2 main、checkstyle 仅 `-Pqa` profile 生效、`-pl` 嵌套路径解析均实测确认。
- **roadmap 流转**：Work Item I4、I5 `todo`→`planned`（转 active）。
- Doc-sync: `No owner-doc update required`（计划拟制过程产出，无行为契约变更）。


### plan 1217-4 nop-stream 不变式裁决（Cycle 1 / I3）完成 — Closure Audit 通过（consensus review + closure audit 双独立 fresh-session）

独立 fresh-session closure audit（`ses_00aa8e1c9ffeT7fgsLaWiK8fd2`）对 3 个 Phase Exit Criteria + 9 项 Closure Gates 逐条对 live repo 验证，verdict **CLOSED**（无 Blocker/Major；仅 1 个 Minor 簿记项 = Phase 3 日志条目，本次收尾补齐）。

- **共识审查**（fresh session `ses_00ab2c496ffeiTg5aVaY4yO115`，review-only）：verdict **approved**，无 Blocker/Major；4 Minor 全部采纳修复——RL-3 依据补**双向影响面**（配置超时 <15s 延迟淘汰 / >15s 提前淘汰）+ **潜伏性加固实测**（生产唯一调用方 `TaskManager.java:83/:212` 硬编码 15s == `InMemoryClusterRegistry.java:23` 默认 leaseTtlMs 15s，当前生产接线零可见偏差）；WO-2 处置措辞改「并入」；WI-1 门禁复跑去重表述。
- **closure audit 关键验证**：裁决表 7 RL + 3 WO 零悬挂、依据逐条可追溯；live 缺陷点抽样 10 处全部属实（含支撑依据 TaskManager/EmbeddedDistributedExecutor/RpcDistributedExecutor）；I3 期间 `nop-stream/` 代码目录零改动（纯文档计划确认）；`check-plan-checklist.mjs --strict` 与 `check-doc-links.mjs --strict` exit 0。
- **roadmap 流转**：Work Item I3 `planned`→`done`（closure audit 通过后正式合法化，同 I1/I2 先例）。
- 验证：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（03:43，全绿）+ `node ai-dev/tools/check-nop-stream-invariants.mjs all` exit 0（基线复核，本 plan 零代码变更）。
- Doc-sync: `No owner-doc update required`（裁决/派发为过程产出，不改行为契约；产出均属 `ai-dev/audits/` + `ai-dev/backlog/`）。

### plan 1217-4 nop-stream 不变式裁决（Cycle 1 / I3）执行中 — Phase 1-2 完成（裁决表 + 派发清单 + backlog）

- **裁决表**（`ai-dev/audits/nop-stream-invariants/adjudication-table.md` 新建，7 RL + 3 WO 零悬挂）：
  - **RL-1/RL-2（JdbcClusterRegistry AR-9 两命中点）→ P1 派 I4**（注册即可见契约违约，调度器首次 renewLease 前无法发现新节点；历史 P1 一致）。
  - **RL-3（InMemoryClusterRegistry renewLease AR-18）→ P2 入 Follow-up Backlog**（例外裁决分析：嵌入式/单机模式 `EmbeddedDistributedExecutor.java:134`/`RpcDistributedExecutor.java:191` 生产接线，后果 = 固定 15s TTL 替代 per-renewal 的**延迟淘汰**，无数据丢失/恢复语义破坏 → 健壮性级别，维持历史 P2）。
  - **RL-4（2PC saveState 无锁 copy AR-1）→ P0 派 I4**（checkpoint 快照 = 2PC 恢复唯一事实源，无锁 copy 可 CME/撕裂快照 → 核心恢复语义破坏）。
  - **RL-5（setPendingCommits 任意 Map AR-11）→ P1 派 I4**（setter 契约不防护，未来调用方破坏同步保证）。
  - **RL-6（WindowOperator cleanup 不 retireWindow R15-AR-8）→ P1 派 I4**（I2 动态验证确认：映射 persist 无界增长 = 状态泄漏 + cleanup 后重叠元素合并进 stale 范围 = 输出窗口范围错误；错误输出语义支撑历史 P2 上浮 P1，满足 I2「≥P2」建议）。
  - **RL-7（ChainingOutput 静默丢弃 side-output R15-AR-4）→ P1 派 I4 双轨**（默认链式部署 late-data/多输出静默丢失，契约违约无 fail-fast；新族输出契约族 → Cycle 2 派生登记）。
  - **WO-1/WO-3（AR-15、R15-AR-9）verified → 关闭；WO-2 升格 RL-6 → 并入裁决**（转述 I2 不重裁）。
- **假设复核**（adjudication-table §0）：I2 权威版 7 RL 全部在册；差异 2 条（RL-6 升格属预期、RL-7 探查新增按权威版修正基线），无 NEEDS_I2_SUPPLEMENT。
- **P0/P1 派发清单**（§3，4 个 I4 工作项按族）：WI-1 F5 JDBC 注册可见性（RL-1+2 联合）、WI-2 F2 2PC 并发防护（RL-4+5）、WI-3 F1 WindowOperator cleanup 收敛（RL-6）、WI-4 新族 ChainingOutput side-output 转发（RL-7 双轨）；每条含类别清扫范围 + test-first + 门禁复跑要求；**显式声明无结构性重构项（无「需人工确认」门）**。
- **Cycle 2 派生登记**（§4）：**PD-15 输出契约族**（不变式「任何 `Output.collect(OutputTag, X)` 必须转发至注册 side-output 消费者，不得静默丢弃」+ 触发证据 `ChainingOutput.java:84-86`/`WindowOperator.java:1015-1017`/`StreamTaskInvokable.java:171/:209`），显式移交 I6。
- **roadmap Follow-up Backlog 新建**（`nop-stream-invariant-loop-roadmap.md`）：RL-3 入册（位置/严重度/触发条件/验证载体）。
- 验证：裁决依据全部引用 I2 red-list 条目 + 影响面分析，live 行号与 I2 权威版一致；纯决策计划无代码变更。
- Doc-sync: `No owner-doc update required`（裁决/派发为过程产出，不改行为契约）。

### plan 1217-3 nop-stream 不变式审计（Cycle 1 / I2）执行中 — Phase 4 完成（red list 权威化 + 移交 I3）

- **red-list.md 升级为 I2 权威版**：Phase 1-3 结论全部合并——RL-1..5（门禁复核在册）+ RL-6（R15-AR-8
  动态验证升格）+ RL-7（R15-AR-4 探查确认）+ WO-1/3 verified 裁定 + 移交声明。每条含 位置 /
  关联不变式 / 关联 finding-ID / 族标注 / 验证或探查结论 / 修复方向参考，I3 可逐条裁决。
- **零悬挂核对**：catalog §6 全部 8 项有处置（5 候选 RL-1..5 + 3 watch-only WO-1..3）；I2 新增
  RL-6/RL-7 也全部在册。
- **catalog §3 行号同步**：R15-AR-8 引用 :776-783 → :773-783（live 行号以本次复核为准，两处同步）。
- **roadmap 流转**：`ai-dev/backlog/nop-stream-invariant-loop-roadmap.md` Work Item I2 `planned`→`done`
  （closure audit 通过后正式合法化，同 I1 先例）。
- Doc-sync: `No owner-doc update required`（纯审计产出 + 验证测试，无行为契约变更）。

### plan 1217-3 nop-stream 不变式审计（Cycle 1 / I2）执行中 — Phase 3 完成（聚焦对抗探查 + 非族候选评估）

- **聚焦对抗探查**（`I2-probing-report.md` 新建，范围 = gate-inventory 目标集 + 盲区 a-d，非全仓漫游）：
  - **PR-1 → RL-7**：`ChainingOutput.java:84-86` `collect(OutputTag, record)` 仅 LOG.warn + 丢弃
    side-output——R15-AR-4 live 确认未修复（触发链：`WindowOperator.java:1015-1017` sideOutput →
    output.collect(tag) → ChainingOutput 丢弃；生产接线 `StreamTaskInvokable.java:171/:209`）。
    新族候选（输出契约族），不变式陈述 + 触发证据在案，入 red-list §3（RL-7）。
  - **盲区 (d) R15-AR-1..5 修复确认表对照复核**：R15-AR-1（CheckpointBarrierTracker 拒绝路径
    :133/:143 清理 + per-id inFlight）fixed；R15-AR-2（per-id inFlightAlignments，Phase 2 已验证）
    fixed；R15-AR-3（WindowOperator:644 onMerge）fixed；R15-AR-4 仍 live（→PR-1）；R15-AR-5
    （GraphModelCheckpointExecutor:645-662 用实际 chain + 每 task 独立 tracker）fixed。
  - **盲区 (a)(b)(c)**：barrier/timer/restore/并发交错检查（restore 与处理：CepOperator watermarkRestored
    守卫 :249-252、WindowOperator 延迟应用 restore、StreamReduceOperator 透传均无问题）；
    refactor 新 call-site 语义抽查发现 PR-1；builder→operator 14 参全转发（:183-203）无参数遗漏。
- **4 个非族候选全部裁定不升格**：R13-AR-9（ResultPartition.close 阻塞 put = P1-10 有意的背压契约 +
  中断逃生 + `TestResultPartitionDeadlock` 覆盖）；R16-AR-19/AR-20（flush 失败保留 buffer + 抛错 =
  fail-fast retry 语义，catalog R10-AR-10/R13-AR-2 已记 fixed；MessageSinkFunction 在 connector
  模块不在五族门禁范围 + SimpleStreamOperatorFactory:89-95 已 fail-fast 不静默共享）；
  R8-AR-59（live 已修复——非序列化非 shareable 算子显式抛 StreamException）；R16-AR-13
  （unregisterOutput 不重算 combined watermark 属实但 multiplexer 无生产接线——死代码路径，不构成
  当前基线行为问题）。无新增门禁族候选（非族评估）；新族候选仅 PR-1 输出契约族，供 I6 评估。
- 验证：聚焦探查为代码级核对（live 文件:行 证据），无新增测试需求（Phase 2 已验证载体已够）。
- Doc-sync: `No owner-doc update required`（探查为审计产出，不改行为）。

### plan 1217-3 nop-stream 不变式审计（Cycle 1 / I2）执行中 — Phase 1-2 完成（门禁全量 + red list 初稿 + watch-only 3 项动态验证）

- **Phase 1 门禁全量运行（确定性 red list）**：mjs `all`/`inventory`/`sync`/`scan-iterations`/`self-test` 逐命令记录全 exit 0（unpinned=0、stale=0，唯一违规 2PC:83 被 mjs-pins.json[0] 吸收）；五族 JUnit 门禁 + 3 模块表完备性 `./mvnw test -pl nop-stream-core,nop-stream-runtime,nop-stream-cep -Dtest='Test*Invariant*'` surefire 实测 84 tests 0 failures（Window 9 / SyncedCol 10 / CheckpointID 8 / CepRelease 21 / ClusterReg 8 / 表完备 core 10 + runtime 11 + cep 7）。**4 条已知 residual 复核表**：AR-1（2PC:83）、AR-11（:76-78）、AR-9（JDBC :112-115/:172-174）、AR-18（InMemory :68-81）live 行号零漂移、pin 描述精确、行为仍匹配 I1 pin 断言——**无行为漂移、无 stale pin、无新增 residual**（处置二分全部落入「无变化」分支，无 blocked 升级）。确定性 red list 初稿写入 red-list.md（RL-1..5 + 门禁结果 §0）。
- **Phase 2 watch-only 3 项动态验证**（git-committed focused tests 全绿）：
  - **WO-1 AR-15 → verified（移除 watch-only）**：`TestLocalFileCheckpointStorage` 新增 2 测试（`testGetLatestCheckpointSelectsMaxIdUnderOutOfOrderWrites` / `testGetLatestCheckpointAfterDeletingMaxId`）——乱序混合 ID（300→100→250→200）下 getLatestCheckpoint 仍取 max-ID 300、`getLatestCheckpoints(2)` 返回 [300,250] 降序、删除 max 后回退 200。级联解除论证链完整 = CheckpointCoordinator:896-900 单调守卫（live + 门禁③ 佐证）+ 排序选取正确（本次验证）。
  - **WO-2 R15-AR-8 → 升格 red list（RL-6 新条目）**：新测试类 `TestWindowOperatorMergingCleanupInvariant`（2 测试）PIN 确认泄漏——onEventTime cleanup 分支（WindowOperator.java:773-782）只 clearWindowContents + triggerContext.clear()，**未调用 `mergingWindows.retireWindow(stateWindow)`**（对照 Flink 语义缺失）；cleanup 后 MergingWindowSet 仍保留 [10,80)→[10,60) 映射并 persist 入 checkpoint 状态（无界增长），且 cleanup 后到达的重叠元素（t=55）合并进 stale 范围生成 [10,105) 而非 [55,105)（被清理窗口范围复活）。R16 确认表 R15-AR-8 原判 fixed 失准（原修复只覆盖 merge 与迟到元素路径）。修复方向（I4）= cleanup 分支补 retireWindow。I3 建议严重度 ≥P2。
  - **WO-3 R15-AR-9 → verified（移除 watch-only）**：`TestInputGateMultiEpochBarrier` 新增 2 测试（`testAlignedOverlapBarrierQueuedOnBlockedChannelAlignsFreshly` / `testAtLeastOnceOverlappingIdsTrackedPerIdWithoutCrossTalk`）——aligned 模式 barrier 6 在 ch0 排队于 align(5) 阻塞期间、unblock 后全新对齐（不继承 stale 状态）；AT_LEAST_ONCE 模式 id 5/6 跨 channel 交错（5→ch0,6→ch1,5→ch1,6→ch0）各 id 独立对齐、首达发射、全达移除。端到端路径（channel write → handleBarrierNonRecursive → read() 返回）走通。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime -Dtest='TestWindowOperatorMergingCleanupInvariant,TestLocalFileCheckpointStorage'` 2+13 全绿；`./mvnw test -pl nop-stream/nop-stream-core -Dtest='TestInputGateMultiEpochBarrier'` 5/5 绿。
- Doc-sync: `No owner-doc update required`（纯审计运行 + 验证测试，无行为契约变更；red-list.md 属 `ai-dev/audits/`）。

### plan 1217-2 nop-stream 首批门禁（Cycle 1 / I1）完成 — Closure Audit 通过 + 全仓 doc-links 修复

独立 fresh-session closure audit（`ses_00b14ee46ffeSfPdlyMYZ5Roo1`）对 11 项 Closure Gates + 4 个 Phase Exit Criteria 逐条对 live 代码验证，verdict 除 1 个过程项（审计证据记录本身）外全部 PASS，plan 状态 `active`→`completed`，roadmap I1 `done`（此前已置，closure 通过后正式合法化）。

- **门禁执行实证（Anti-Hollow）**：surefire 报告证明 5 族门禁真实执行——TestWindowRoundTripInvariant 9 / TestSynchronizedCollectionInvariant 10 / TestCheckpointIDCounterInvariant 8 / TestCepReleaseSymmetryInvariant 21 / TestClusterRegistryConsistencyInvariant 8（0 failures）+ 3 模块表完备性测试（core 10 / runtime 11 / cep 7）；mjs `all`/`inventory`/`sync`/`scan-iterations` 全退出码 0，live 恰好 1 个违规（2PC:83 无锁 copy）被 pin 吸收（unpinned=0、stale=0）；CI `.github/workflows/maven.yml:44-49` node step 落地；Window 测试真实经 `WindowOperatorFactoryImpl`（非 mock，getRawOperator :220-223）。
- **red list 移交完整性**：red-list.md 4 条 residual（AR-1 saveState 无锁 copy / AR-11 setPendingCommits / AR-9 JDBC lease=0 / AR-18 InMemory 忽略 timeout）逐条对 live 源码核对 `文件:行` 与 pin 位置，无 in-scope defect 被静默降级。
- **表完备性双向验证**：`InvariantTableCompleteness.findViolations`（:166-179）双向精确相等——ghost 表项红、live 新增方法不入表红；方向 A/B fixture（TestInvariantTableCompleteness.java:57-72）执行通过。
- **全量验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（51 模块 01:29）；E2E `TestWindowOperatorUnificationE2E`（6）+ `TestWindowEndToEnd`（7）复跑 0 failures。
- **全仓 doc-links 修复（17 个 pre-existing 错误清零）**：`check-doc-links.mjs --strict` 此前全仓 17 errors（3 个 baseline 既有 + 14 个由 mission setup commit 7bc0b8e13 引入），全部位于非本 plan 文件。按 AGENTS.md「fix broken links before finishing」与 Closure Gate 要求退出码 0，以最小内容保持编辑全部修复：跨仓先例引用（`docs/backlog/ai-invariant-loop-roadmap.md` 等 nop-chaos-flux worktree 文件）改为纯文本标注（与 nop-stream roadmap 既有约定一致）；前向引用（其他 mission 的 invariant-catalog.md / 计划中工具 mjs）改纯文本 + 「待产出」标注；错路径修正（`model/nop-credential.orm.xml`→`nop-credential/model/nop-credential.orm.xml`、`skills/nop-code/audit-prompt.md`→`ai-dev/skills/nop-code/audit-prompt.md`、INDEX.md `nop-runner/nop-cli/target/`→模块目录）。修复后 exit 0（0 errors），仅剩 3 个 active-plan 降级 warning。
- 验证：`node ai-dev/tools/check-plan-checklist.mjs <本plan> --strict` 退出码 0；`check-doc-links.mjs --strict` 退出码 0。

### plan 1217-2 nop-stream 首批门禁（Cycle 1 / I1）执行中 — Phase 4 完成（设计文档「不变式」节 + 全量验证）

Phase 1-3 基础上完成设计文档「不变式」节与全量验证，全绿。

- **设计文档「不变式」节**（按 guide Minimum Rules #14 只记最终设计状态，交叉引用 invariant-catalog §5 双向可核）：
  - `window-design.md` §2.1：门禁 ① 窗口粘合层构造参数完备性（8 参数元组 round-trip 契约 + 门禁位置 + 历史证据 R16-AR-2 等）。
  - `checkpoint-design.md` §1.1：门禁 ②（synchronized 迭代点 + saveState pin-and-record 语义 + mjs 静态部分）+ 门禁 ③（CheckpointIDCounter 原子性/恢复单调性）；§1.1.1 ClusterRegistry lease 与 checkpoint 交互侧（门禁 ⑤ 分层落笔，架构语义契约留在 01-architecture-baseline）。
  - `cep-design.md` §1.1：门禁 ④ SharedBuffer/Lockable 释放对称性。
  - `01-architecture-baseline.md` ClusterRegistry 节（D73 下）：门禁 ⑤ 架构语义契约（两实现语义一致、per-renewal timeout 生效、eviction 一致）+ 已知 residual（AR-9/AR-18）pin 移交 I2。
- **全量验证**：`./mvnw test -pl nop-stream -am -T 1C` 全绿（7 模块 2680 tests，0 failures）；mjs 全量扫描 `node ai-dev/tools/check-nop-stream-invariants.mjs all` 退出码 0（inventory/sync/scan-iterations/self-test 全绿，scan-iterations 输出 = 1 个 pinned violation 被吸收）；端到端既有测试 `TestWindowOperatorUnificationE2E`（6）+ `TestWindowEndToEnd`（7）+ `TestE2EOperatorStateCheckpoint` 全过（Anti-Hollow：门禁未破坏 addSource→sink 链路）。
- **doc-links**：`node ai-dev/tools/check-doc-links.mjs --strict` 全仓 18 个错误**全部 pre-existing 且位于非本 plan 文件**（nop-ai/nop-code/nop-metadata/nop-credential 等其他 mission roadmap 前向引用、`docs-for-ai/INDEX.md:217` target 目录、`invariant-loop-audit-prompt.md` 跨仓先例引用）——与 I0 同款裁定（I0 记录 20 个 pre-existing 错误同理）；本 plan 产出文件（catalog/gate-inventory/red-list/mjs-pins/fixtures/设计文档/plan）零 issue。
- **Anti-Hollow**：`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-stream --severity high` 14 项 findings 全部为既有**故意 fail-fast**（`UnsupportedOperationException` 按 Rule #24 显式抛错，如 GroupPattern/RuntimeContext/Trigger），非本 plan 新增空壳。
- 验证：全量构建命令级（上面两项）+ 门禁自验证（正反 fixture 全过）。

### plan 1217-2 nop-stream 首批门禁（Cycle 1 / I1）执行中 — Phase 3 完成（门禁 ②④⑤ + mjs 静态扫描 + CI 接入）

Phase 1/2 基础上完成门禁 ②（synchronized 迭代 JUnit + mjs 静态扫描）、门禁 ④（CEP 释放对称）、门禁 ⑤（ClusterRegistry 一致性），mjs 扫描器入 CI 裁定落地。

- **门禁 ②（core）**：`TestSynchronizedCollectionInvariant`（10 项）。2PC 迭代点表（saveState/finishCommit/restoreFromEpoch）逐点核对在 gate table + live 变更型方法集；**pin-and-record**：saveState 已知 residual（R16-AR-1）pin 行为语义（快照内容完整，5 条目原样入 snapshot），锁状态由 mjs 扫描器 pin（`mjs-pins.json`）；finishCommit/restoreFromEpoch 迭代点行为证明在 synchronized 块内（4 噪声线程并发 put/remove 200ms 无 CME + 恰好提交 ≤ epochId 条目）；表范围核对 4 个 synchronized 集合类（2PC 在 JUnit 点名表，SourceReaderOperator/StreamSinkOperator/LocalSourceCoordinator 由 mjs 全量扫描兜底，Class.forName 存在性显式核对）。
- **门禁 ④（cep）**：`TestCepReleaseSymmetryInvariant`（21 项，在既有 TestLockable/TestSharedBuffer 之上扩展不重复造轮子）。Lockable 参数化序列表（7 组 × 2 个断言视角）：对称 lock/release 归零、over-release fail-fast 抛 `StreamRuntimeException`（含 message 含 over-release）、releaseOrDetach 归零后 detach 静默、负数 fail-fast、混合序列守恒；SharedBuffer 条目生命周期对称性（6 组 locks×releases：对称 → 条目移除无泄漏，非对称 → refCounter 守恒）；releaseNode 路径事件 refCounter 递减守恒。
- **门禁 ⑤（runtime）**：`TestClusterRegistryConsistencyInvariant`（8 项，H2 内存库 + CoreInitialization）。两实现同语义场景参数化：**registerNode 可见性 pin**（InMemory 立即可见 / JDBC AR-9 pin：写 lease_expire_at=0L → 注册后不可见，renewLease 后可见）；**renewLease per-renewal timeout pin**（JDBC expireAt≈now+5000 生效 / InMemory AR-18 pin：固定 15s TTL，expireAt-startAt≈15s）；eviction 一致性（JDBC 短超时过期移除 / InMemory setLeaseTimestampForTest+evictExpiredNodes）；生命周期一致性正例。差异全部显式断言（pin）并登记 `red-list.md` 移交 I2，未修复。
- **mjs 静态扫描**：`scan-iterations` 命令对 3 模块 src/main 全量扫描——live 恰好 1 个违规（`TwoPhaseCommitSinkFunction.java:83` saveState 无锁 copy），经 `mjs-pins.json` pin 后绿（**零误报**：2PC finishCommit 别名 `pending = getPendingCommits()` + synchronized(pending) 识别合规）；fixtures 目录 `BadSinkFixture.java`（故意注入无锁迭代样例）经 self-test 验证**能红**；empty-pins 实验证明 pin 语义（违规集 ⊆ pin 集 → 绿，新增违规 → 红，stale-pin → 报错）。
- **CI 裁定（Decision：选项 a）**：`.github/workflows/maven.yml` build job 新增 `actions/setup-node@v4`（node 20）+ `node ai-dev/tools/check-nop-stream-invariants.mjs` step（e2e job node 先例）；JUnit 门禁随 `mvn test` 天然入 CI。
- **red list（pin-and-record 载体）**：`ai-dev/audits/nop-stream-invariants/red-list.md` —— 4 条已知 residual（AR-1 saveState 无锁 copy、AR-11 setPendingCommits 任意 Map、AR-9 JDBC lease=0、AR-18 InMemory 忽略 leaseTimeoutMs）+ 3 条 watch-only（AR-15/AR-8/AR-9 相关），每条含 `文件:行` + pin 位置 + I2 修复路径，移交 I2。
- 验证：`./mvnw test -pl nop-stream-core,nop-stream-runtime,nop-stream-cep` 全绿（core 1414 / runtime 793 / cep 320，0 failures 0 errors）；`node ai-dev/tools/check-nop-stream-invariants.mjs all` 退出码 0。

### plan 1217-2 nop-stream 首批门禁（Cycle 1 / I1）执行中 — Phase 2 完成（门禁 ① Window round-trip + 门禁 ③ CheckpointIDCounter）

Phase 1 基础上完成门禁 ① 与门禁 ③ 行为级测试，全绿。

- **门禁 ①（runtime）**：`TestWindowRoundTripInvariant` + 判定辅助类 `WindowRoundTripAssertions`。8 组参数化正例（4 call-site × 有/无 evictor）：从 `WindowedStreamImpl` call-site 经 `IWindowOperatorFactory` 接口 → 真实 `WindowOperatorFactoryImpl` → `WindowOperatorBuilder` → `WindowOperator` 构造器，断言 8 实参 round-trip（windowAssigner/trigger/evictor/allowedLateness/function→wrapped userFunction/elementType→accClass+ListStateDescriptor/keySelector/keyClass；映射表在 helper javadoc 明示）。**接线验证**：非 mock，从 transformation → SimpleStreamOperatorFactory.getRawOperator() 提取真实 WindowOperator。函数槽位：apply/process 解包 `InternalIterable*` 的 `wrappedFunction` 做同一性断言；aggregate 无 evictor 经 `AggregatingStateDescriptor.getAggregateFunction()` 同一性；reduce 行为等价（`reduceFunctionAsAggregate` 包装行为 == call-site 函数）；有 evictor 路径解包 `Buffering*ProcessWindowFunction` 内字段。**反例 fixture**：`DroppingAllowedLatenessFactory`（桩 factory 漏传 allowedLateness）→ helper 必抓 allowedLateness 违规（9 项测试含反例）。
- **门禁 ③（core）**：`TestCheckpointIDCounterInvariant`。4 组 (threads, iterations) 参数化并发（2×500 / 4×500 / 8×250 / 16×100）：`getAndIncrement` 结果集恰好覆盖 `[start, start+N*M)` 无重复无缺口、计数器精确推进 total 步；恢复路径 `set(restoredId)` 后从 restoredId 继续单调递增（单调守卫属 CheckpointCoordinator:896-900 契约，测试注释明示边界）。
- 验证：`./mvnw test -pl nop-stream-runtime -Dtest=TestWindowRoundTripInvariant` 9/9 绿；`./mvnw test -pl nop-stream-core -Dtest=TestCheckpointIDCounterInvariant` 8/8 绿。

### plan 1217-2 nop-stream 首批门禁（Cycle 1 / I1）执行中 — Phase 1 完成（门禁基座 + 表完备性机制）

plan `ai-dev/plans/2026-08-12-1217-2-nop-stream-invariants-cycle1-I1-first-gates.md`（roadmap I1）Phase 1 完成。五族门禁测试类骨架 + 表完备性机制 + mjs 扫描器骨架落地，全绿。

- **门禁测试组织裁定**：五族按模块分布——门禁①（Window round-trip）`nop-stream-runtime/src/test`（`TestWindowRoundTripInvariant`，与 `TestWindowOperatorUnificationE2E` 同包）；门禁②（synchronized 迭代）core（`TestSynchronizedCollectionInvariant`）+ mjs 静态扫描；门禁③（CheckpointIDCounter）core（`TestCheckpointIDCounterInvariant`）；门禁④（CEP 释放对称）cep（`TestCepReleaseSymmetryInvariant`，同包访问 package-private Lockable）；门禁⑤（ClusterRegistry 一致性）runtime（`TestClusterRegistryConsistencyInvariant`）。每族 `@MethodSource` 静态表驱动。
- **表完备性机制（双向精确相等）**：单一事实源 `ai-dev/audits/nop-stream-invariants/gate-inventory.json`（schemaVersion 1，25 个类 × 变更型方法表，按模块分组）；JUnit 侧 `InvariantTableCompleteness`（core test-jar，runtime/cep 复用）+ 三个模块的 `Test{Module}InvariantTableCompleteness`（core 7 类 / runtime 11 类 / cep 7 类反射校验）；mjs 侧 `inventory` 命令（源码枚举双向 diff）。机械分类器（I0 §4.1 操作化）：非 private 声明方法，排除 static/类内 abstract/构造器/synthetic/`*ForTest*`/get* is* has* 只读访问器（`getAnd*`/`getOr*` 除外）；接口方法无修饰符视为 public。
- **mjs 扫描器骨架**：`ai-dev/tools/check-nop-stream-invariants.mjs`（`inventory`/`sync`/`scan-iterations`/`self-test`/`init`/默认 all），风格对齐 `check-nop-stream-audit-manifest.mjs`；`sync` 校验 catalog §4 ⊆ 门禁表（含 exclusion：`CheckpointPlanBuilder.build` static 工厂）；`scan-iterations` 已能抓出 **1 个 live 违规**：`TwoPhaseCommitSinkFunction.java:83` saveState 无锁 copy（R16-AR-1），已记入 `mjs-pins.json` pin-and-record（I2 red list #4），全绿 = 违规集 ⊆ pin 集。
- **自验证 fixture**：JUnit 侧方向 a（表中有 ghost 方法 → 红）与方向 b（代码新增方法不入表 → 红）fixture 测试；mjs 侧 `self-test` 覆盖分类器（class/interface/泛型方法）、迭代扫描正反例（无锁 copy 必红、别名 synchronized 迭代零误报）、pin 语义（pinned 吸收 / stale-pin 报错）、missing-inventory 非零退出（无静默跳过）。
- **catalog 回写**：catalog §4 增加「I1 门禁表」机器可读单一事实源注记，`WindowOperatorFactoryImpl` 4 个 create* 显式落行，`StreamReduceOperator`/`InputGate`/`NFAState` 复核落定，各族「I1 补表」注记（如 WindowedStreamImpl 补 trigger/evictor/allowedLateness/withComponents/transform）。
- 验证：`node ai-dev/tools/check-nop-stream-invariants.mjs all` 退出码 0；`Test*Invariant*` 全绿（core 13 + runtime 19 + cep 10）。`./mvnw test -pl nop-stream-core,nop-stream-runtime,nop-stream-cep -Dtest='Test*Invariant*'` BUILD SUCCESS。

### plan 1217-1 nop-stream 不变式盘点与基线（Cycle 1 / I0）完成

plan `ai-dev/plans/2026-08-12-1217-1-nop-stream-invariants-cycle1-I0-inventory-baseline.md`（roadmap `nop-stream-invariant-loop-roadmap.md` 的 I0）执行完成。纯文档计划（无代码变更），产出 `ai-dev/audits/nop-stream-invariants/invariant-catalog.md`。

- **finding-ID → 失败族映射表**：R8/R10/R11/R12/R13/R14/R15/R16/R17 中五族相关 finding 主集（F1 Operator 族 20 条、F2 SinkFunction 族 9 条、F3 Checkpoint 机制 24 条、F4 CEP NFA 族 18 条、F5 ClusterRegistry 族 2 条），覆盖边界注明（R14 全集不可得，以 R9(R15) 头部确认表为证据上限；**live 修正**：R15 完整发现集实际位于 `2026-05-31-adversarial-review-nop-stream-r9/01-open-findings.md`（文件内自标 Round 15，目录名 r9 与轮次标签不一致））。
- **零门禁基线确认（live 实测）**：`check-nop-stream-invariants.mjs` 不存在、`ai-dev/audits/nop-stream-invariants/` 目录不存在、nop-stream 无 `@ParameterizedTest`。
- **live 修复状态核实**：fixed = R16 AR-2（WindowedStreamImpl 4 call-site :186/:201/:216/:231 全参数转发 → WindowOperatorBuilder:183-203 → WindowOperator 构造器）、R15-AR-3（WindowOperator:644 triggerContext.onMerge）、AR-8（Lockable CAS + 负数抛 StreamRuntimeException）、AR-5 真实修复点（CheckpointCoordinator:896-900 单调 ID 推进）、AR-10（checkpointSuccessMap ConcurrentHashMap :203/:805/:1179）、AR-7（DeweyNumber 溢出检查）、AR-6（CepOperator allTimedOut 守卫）、R13-AR-15/R14-AR-4（SharedBuffer.advanceTime 清理 eventsBufferCache + EventId 冲突检测）、R13-AR-6（WindowOperator:1462-1472 非累加器冲突 fail-fast 抛错）、R10-AR-1/2、R10-AR-10、R12-AR-1/8、R11-AR-3。**residual（I2 red list 候选）**：R16 AR-9（JdbcClusterRegistry:112-115 仍写 lease_expire_at=0L、:172-174 仍按 >now 过滤）、AR-18（InMemoryClusterRegistry:68-81 仍忽略 leaseTimeoutMs）、AR-1（TwoPhaseCommitSinkFunction:83 saveState 无锁 copy）、AR-11（:76-78 setPendingCommits 接受任意 Map）。
- **五族审计目标集清单**（catalog §4）：含「变更型方法」判定标准定稿（public/protected + 状态变更；getter/toString/equals/hashCode 不入表；private 助手不入表），每族含 live `类路径:方法` 引用，I1 表完备性门禁以此为「表」来源。
- **首批 5 条不变式定稿**（catalog §5）：① Window 粘合层构造参数完备性 ② synchronized 集合迭代点 ③ CheckpointIDCounter 原子性/恢复单调性 ④ CEP Lockable/SharedBuffer 释放对称性（over-release fail-fast）⑤ ClusterRegistry 多实现语义一致性。
- **独立共识审查（2 轮）**：round 1（fresh session `ses_00ba633a9ffevLkcrOgYlpkxFU`）verdict revised——1 Major（R15-AR-8/R15-AR-9 未入映射表 + 「全部 finding」声明过宽）+ 4 Minor（timer 归类声明矛盾、R13-AR-6 partial 分类与行号错位、R10-AR-18/R11-AR-7/R12-AR-7 边界漏项、private flush 入表），全部修复；round 2（fresh session `ses_00b9f1b97ffeBwBx6sEc2OMT5g`）verdict approved（逐项 live 核实 PASS）。
- **独立 closure audit**（fresh session `ses_00b9dc485ffe3QTxOeZLRKOaDB`）：Phase 1-4 Exit Criteria + 12 项 Closure Gates 逐条 PASS，Anti-Hollow 20+ finding-ID 全可查、无空壳，verdict CLOSED。plan 状态 `active`→`completed`，roadmap I0 `planned`→`done`（`todo`→`planned` 在本 plan 转 active 时记录）。
- **偏差**：plan 基线声称「R14/R15 证据仅存在于 R16 头部确认表」，live 核实 R15 完整发现集在 r9 目录（文件内自标 Round 15），catalog §0 记录该修正。
- 验证：`check-plan-checklist.mjs <本plan> --strict` 退出码 0；`check-doc-links.mjs --strict` 全仓 20 个 pre-existing 错误（全部位于非本 plan 文件：其他 mission 的 roadmap 前向引用、跨仓先例引用、INDEX.md），本 plan 产出 catalog 零 issue、plan 文件 2 处可解析引用已修（剩余 6 处为 active-plan 降级 warning）——与 0615-2 credential plan 同款裁定。
- Doc-sync: `No owner-doc update required`（纯盘点文档产出，无行为契约变更；catalog 属 `ai-dev/audits/`）。

### plan W3 nop-credential 管理 API：BizModel 明文边界 + 自定义 actions（部分落地）

plan `ai-dev/plans/2026-08-12-0615-3-nop-credential-management-api-plaintext-boundary-web.md` 的 BizModel 部分实现，Web 部分（AMIS 动态表单）未动。

- **明文边界三重防御**（`NopCredentialBizModel`）：① 继承的标准 `save` 覆盖为抛 `UnsupportedOperationException`，强制走 `saveCredential`（明文输入唯一入口）；② `get`/`findPage` 返回前 `entity.setData(null)`（即使 xmeta `published="false"` 被绕过也无密文泄漏）；③ `saveCredential` 内部加密后持久化，返回实体 `data` 置 null。
- **自定义 actions**：`saveCredential`（typeName/name/fields 校验 fail-closed → JSON 序列化 → `CredentialCipher.encrypt` → `saveEntityDirectly`/`updateEntityDirectly`）、`maskList`（脱敏视图）、`typeList`（类型字段 schema 供前端动态表单）、`test`（触发连通性测试并更新 lastUsedAt）、`reencryptAll`（密钥轮换：解析 `cv1:{keyId}` 提取当前 keyId，幂等跳过，失败 fail-closed 抛 `ERR_CREDENTIAL_REENCRYPT_FAILED`）。
- **新增 ErrorCode**：`ERR_CREDENTIAL_FIELDS_REQUIRED`/`ERR_CREDENTIAL_NAME_REQUIRED`/`ERR_CREDENTIAL_HAS_ACTIVE_USAGE`/`ERR_CREDENTIAL_REENCRYPT_FAILED`（+ ARG_FIELD_NAME/ARG_USAGE_COUNT）。
- `DefaultCredentialKeyProvider.setActiveKeyId` 补 `@InjectValue("@cfg:nop.credential.active-key-id|")`（此前只经 `init()` 加载列表首项）。
- 测试：`TestNopCredentialBizModel`（saveCredential 加密落库/更新/校验 fail-closed/reencryptAll 幂等 + active key 轮换）；新增测试资源 `application.yaml`。
- 注：`ERR_CREDENTIAL_HAS_ACTIVE_USAGE` 预留（W4 删除防护使用）；Web 动态表单页面待 W3 后续。

### plan W2 nop-credential 类型注册 + 数据模型 + 消费 SPI 落地

plan `ai-dev/plans/2026-08-12-0615-2-nop-credential-type-registry-data-model-spi.md`（roadmap `nop-credential-mfa-roadmap.md` 的 W2）执行完成。在 W1 的 crypto 层基础上，实现了凭证类型声明式注册、ORM 数据模型（codegen）、以及消费 SPI（唯一解密点）。下游 W3（管理 API + Web）可基于完整的 SPI 契约继续构建。

- **Phase 1 — 凭证类型注册**：
  - `credential-type.xdef`（`nop-xdefs/_vfs/nop/schema/credential/`）：根元素 `credential-type`，属性 `name/version/displayName/authType/testUrl/testAuth`，子元素 `fields/field`（name/label/type/sensitive/defaultValue/required）。**不声明 `xdef:bean-package`**——`CredentialType` DTO 手写在 api 层，保持 api 模块零 codegen 依赖。
  - `credential-type.register-model.xml`（`nop-credential-service/_vfs/nop/core/registry/`）：`xdsl-loader fileType="credential-type.xml"`，参照 `ai-tool.register-model.xml`。
  - 2 个实例文件：`openai-api-key.credential-type.xml`（apiKey sensitive + orgId 非敏感）+ `generic-secret.credential-type.xml`。
  - `CredentialType` DTO（api 层 `io.nop.credential.api.registry`）：手写 POJO + 内部类 `CredentialField`。
  - `ICredentialTypeRegistry`（api 层）：`getType(typeName)`（fail-closed）+ `listTypes()`。
  - `DefaultCredentialTypeRegistry`（service 层）：`@PostConstruct` 枚举 `_vfs/nop/credential/types/` 下 `*.credential-type.xml`，经 `ResourceComponentManager.loadComponentModel()` 加载（xdef 校验），映射为 `DynamicObject → CredentialType` DTO，缓存到 `Map`。注册在 `credential-defaults.beans.xml`。
  - 5 个测试：`TestDefaultCredentialTypeRegistry`（类型加载、字段断言、listTypes≥2、未知/null 类型 fail-closed）。
- **Phase 2 — ORM 数据模型 + Codegen**：
  - `model/nop-credential.orm.xml`：`NopCredential`（16 列：credentialId PK/data 加密列 VARCHAR(4000)/delFlag boolFlag/status/usageScope/lastUsedAt/expireAt/testResult + audit 列，useLogicalDelete + versionProp）+ `NopCredentialUsage`（usageId PK/credentialId/consumerRef + unique-key 约束）。双向 relations。
  - `gen-orm.xgen`（codegen postcompile）+ `gen-meta.xgen`（meta precompile），参照 nop-file 模式。codegen 成功生成 `_gen/_NopCredential.java` + `_gen/_NopCredentialUsage.java` + `_NopCredential.xmeta` + `_NopCredentialUsage.xmeta`。
  - xmeta delta `NopCredential.xmeta`：`<prop name="data" published="false"/>`（结构性明文边界——data 永不发布到 GraphQL schema，survives codegen 因 x:extends="_NopCredential.xmeta"）。
  - `_module-meta.json`：moduleId/moduleName/appName。
  - **修正**：`stdDataType="integer"` → `"int"`（平台 std-data-type 枚举不接受 integer）；entity 增补 `versionProp="version" updaterProp="updatedBy"`（与 nop-auth 一致）。
- **Phase 3 — 消费 SPI（唯一解密点）**：
  - DTOs（api 层）：`CredentialData`（Map<String,Object> 明文字段）、`MaskedCredential`（Map<String,String> 脱敏）、`TestResult`（success/message/testedAt）。
  - `ICredentialProvider`（api 层）：getCredential/getCredentialData/testCredential/mask/registerUsage/unregisterUsage。
  - `CredentialProviderImpl`（service 层）：注入 `IDaoProvider` + `CredentialCipher` + `ICredentialTypeRegistry`（均 protected 字段）。getCredential: load → delFlag fail-closed → cipher.decrypt → JsonTool.parseMap → CredentialData。mask: 按类型定义 sensitive 标记脱敏（**** / 截断 8 字符+...）。testCredential: 显式 success=false + "not implemented"（Rule #24 非静默）。registerUsage: 幂等（UUID 主键 + query-before-insert）。unregisterUsage: deleteEntityDirectly（session-detached 实体）。
  - 注册在 `credential-defaults.beans.xml`。
  - 13 个 AutoTest（`JunitBaseTestCase` + H2 内存库 + DDL 自动初始化）：端到端 round-trip（JSON→encrypt→DB→load→decrypt→JSON→field map）、单字段获取、软删除 fail-closed（getCredential/mask）、不存在凭证 fail-closed、引用计数幂等+多消费者+注销、脱敏（sensitive=**** + non-sensitive 截断）、testCredential（success=false + 消息非空 + 持久化 testResult）、不存在凭证 testCredential fail-closed。
  - **修正**：`saveEntityDirectly` 对 MANAGED 实体报错 → `updateEntityDirectly`；`deleteEntity` 对 session-detached 报错 → `deleteEntityDirectly`；`java.util.Date` 不被 Nop JSON 序列化 → `StringHelper.formatDate` 转 ISO 字符串；`usageId` 无 seq 标签 → `StringHelper.generateUUID()` 手动生成。
- **W1 遗留 bug 修复**：`nop-credential-app/NopCredentialApplication.java` 引用不存在的 `io.nop.quarkus.core.QuarkusIntegration`（W1 skeleton bug），参照 `nop-file-app` 模式注释掉 `QuarkusIntegration.start()` 并移除 import。
- **验证**：`./mvnw clean install -pl nop-credential -am -T 1C -DskipTests` → BUILD SUCCESS；`./mvnw test -pl nop-credential -am -T 1C` → 48 tests (16 crypto + 14 key-provider + 5 type-registry + 13 credential-provider), 0 failures。checkstyle 非构建门禁（平台 core 自身 9164+ 违规）。doc-link-checker 有 22 个 pre-existing 错误（17+ 在其他 roadmap/skill 文件中），credential 相关 5 个为 prose 简写引用（文件实际存在于 `nop-credential/model/`）。
- Doc-sync: `No owner-doc update required`（W2 是内部基础设施，`docs-for-ai/` 的 credential 模块文档将在 W7 创建；roadmap W2 状态 `planned`→`done`）。

### plan W1 nop-credential 模块骨架 + 密码学层落地（cv1 格式 + 多密钥管理）

plan `ai-dev/plans/2026-08-12-0615-1-nop-credential-module-skeleton-and-crypto-layer.md`（roadmap `nop-credential-mfa-roadmap.md` 的 W1）执行完成。新增独立可复用模块 `nop-credential`（标准 Nop 分层），并实现凭证专用的密码学层。下游 W2（类型注册 + 数据模型 + 消费 SPI）/W3（管理 API + Web）可基于稳定的 crypto 基础继续构建。

- **模块骨架（Phase 1）**：参照 `nop-file/` 结构创建 `nop-credential/`（7 个子模块：api/dao/meta/service/web/app/codegen）。`nop-credential-api` 显式依赖 `nop-api-core` + `nop-commons`（`ITextCipher` 在 nop-commons，nop-api-core 不传递它——这是设计文档 §3.1 的硬约束）。根 `pom.xml:577` 注册 `<module>nop-credential</module>`；`nop-bom/pom.xml` 新增 5 条 version-managed 条目（api/dao/meta/service/web；codegen/app 因 `maven.deploy.skip=true` 不进 BOM，与 nop-file 惯例一致）。编号 `32-nop-credential`（接续现有 31-nop-runner）。
- **密码学层（Phase 2）**：
  - `ICredentialKeyProvider`（api 层 SPI）：`getActiveKeyId()`/`getKey(keyId)`/`getKeyIds()`，引用 `io.nop.commons.crypto.ITextCipher`。
  - `CredentialCipher`（service 层）：`cv1:{keyId}:v1:{base64}` 格式编解码。encrypt 委托 `ICredentialKeyProvider.getKey(keyId).encrypt()` 再包装 `cv1:{keyId}:` 前缀；decrypt 按 `indexOf(':')` 拆分 keyId 与 v1Payload，**v1Payload 保留 `v1:` 前缀**交给 `AESTextCipher.decrypt()` 做格式分派。`@Inject protected ICredentialKeyProvider keyProvider`（protected 而非 private——NopIoC 字段注入限制）。
  - `DefaultCredentialKeyProvider`：`@InjectValue("@cfg:nop.credential.master-keys|")` 注入 `List<String>`（每条 `keyId:passphrase`），`@PostConstruct init()` 加载。fail-closed：空配置/非法 keyId（非 `[A-Za-z0-9_-]+`）/重复 keyId/缺 passphrase 均在启动期抛 `NopException`。active key 取 `nop.credential.active-key-id` 或列表首项。
  - `CredentialConfigs`：`CFG_CREDENTIAL_MASTER_KEYS`（`List<String>`，varRef + unchecked cast 因 `varRef` 只接受 `Class<T>`）+ `CFG_CREDENTIAL_ACTIVE_KEY_ID`。
  - `CredentialErrors`：5 个 ErrorCode（格式错误/未知 keyId/非法 keyId/配置条目非法/无主密钥）。
  - `credential-defaults.beans.xml`：两个 bean（`defaultCredentialKeyProvider` + `credentialCipher`），`@InjectValue`/`@Inject` 由 NopIoC 自动处理。
- **测试（Phase 2/3）**：30 个聚焦单测——`TestCredentialCipher`（16：cv1 格式 regex、round-trip、多密钥轮换、6 种畸形输入 fail-closed、keyId 校验、随机 IV 去重、接线验证）+ `TestDefaultCredentialKeyProvider`（14：密钥加载、active key 选择、6 种启动期 fail-closed、end-to-end）。passphrase 允许含冒号（按首个冒号拆分），修正了 plan 示例 `my:key` 的歧义性测试预期。
- **偏差**：plan 的 CredentialConfigs 示例用 `AppConfig.varRef(CredentialConfigs.class, ...)` 但实际 `varRef` 签名是 `(SourceLocation, String, Class<T>, T)`，无 Class 重载。采用 `SourceLocation.fromClass(...)` + `@SuppressWarnings("unchecked")` 转换 `List.class` 到 `IConfigReference<List<String>>`（与平台现有模式一致，codebase 无先例的 `IConfigReference<List<String>>`）。
- **验证**：`./mvnw clean install -pl nop-credential -am -T 1C -DskipTests` → BUILD SUCCESS；`./mvnw test -pl nop-credential -am -T 1C` → BUILD SUCCESS（30 tests, 0 failures）；`./mvnw compile -pl nop-auth,nop-ai/nop-ai-gateway,nop-persistence/nop-nosql -am` → BUILD SUCCESS（无下游破坏）；`check-plan-checklist.mjs` 退出码 0；`scan-hollow-implementations.mjs --module nop-credential --severity high` 退出码 0（0 空壳发现）。
- **独立 closure audit**（fresh session `ses_00cb537efffeShevY06p94WNfp`）：17 项逐条 PASS（Phase 1-3 全部、端到端、接线、Anti-Hollow），判定 CLOSED。证据已写入 plan `Closure` 段落。
- Doc-sync: `No owner-doc update required`（W1 是内部基础设施，`docs-for-ai/` 的 credential 模块文档将在 W7 创建；roadmap W1 状态 `planned`→`done`）。
- 注：checkstyle 默认 `sun_checks.xml` 对全仓库报大量 Javadoc 类警告（nop-api-core 自身 9164 条），非构建门禁（`clean install` 通过）；本计划代码遵循实际约定（io.nop.*→jakarta.*→java.* 导入分组、4 空格缩进、`io.nop.credential.*` 包名）。

### plan 338 nop-wf 设计器集成：IoC 阻塞解除 + DB 全链 E2E 通过 + 3 个前置 bug 修复

plan 338（nop-wf 集成 flux 通用工作流设计器）本轮解除了前轮遗留的三个硬阻塞门中的两个，并修复了 E2E 测试路径上 3 个独立的真实 bug。唯一剩余 blocker 为跨仓 bundle 构建链（flux 仓缺少 `pack-flux-bundle` 基础设施）。

- **IoC baseline 回归已解除**：前轮 commit `88942c919` 引入的 `BeanCreationContext` 重构导致 `OrmSessionFactoryBean.init` `shardSelector` null。后续 commits（`f19092baa` 补 flushActions、`28158ac90` 修 BeanTopologySorter、`c235a0056` 修断言误用）已修复。`./mvnw clean install -pl nop-wf -am -T 1C` → BUILD SUCCESS。DB-backed 测试全绿。
- **DB 全链 E2E 通过**：`TestWorkflowDesignerService.testEndToEnd_loadSaveReload_engineStartInstance`（9 tests, 0 failures）。链路：空定义→`loadDesignerPage`→codec round-trip `saveDocument`→reload 一致→publish→`startWorkflow`→s1 complete→s2 activate→s2 complete→workflow ended。
- **Bug 1 — bean id 不匹配**：`wf-dao.beans.xml` 注册 `nopWorkflowModelLoader` 但 `dao-wf.register-model.xml` 引用 `nopDaoWorkflowModelLoader`。`DaoWorkflowModelLoader` 从未被 register-model 机制找到。修复：bean id 改为 `nopDaoWorkflowModelLoader`（与 `nop-rule-dao` 的 `nopDaoRuleModelLoader` 约定一致）。这是自 2023-11 以久的潜伏 bug，因为既有 wf 测试使用文件 `.xwf` 模型，不经 DAO loader。
- **Bug 2 — versioned path 解析**：`ResourceVersionHelper.parseVersionedName(path, basePath)` 2-arg 版本对含 `/` 的名称（如 `test/designer-flow`）在未指定版本时，将最后一段 `designer-flow` 误判为版本号并抛异常。修复：最后一段不是合法版本号（`v` + 数字）时，整体视为 name、version = -1。回归测试 `TestResourceVersionHelper` 4 cases。
- **Bug 3 — DAO resource path**：`DaoWorkflowModelLoader.loadObjectFromPath` 用 `resolve-wf:` path 作为 XNode source location，导致 `DslModelParser` 依赖追踪时 VFS 无法解析 `resolve-wf:` namespace（VFS 只注册了 `wf:` namespace）。修复：改用 `DaoEntityResource.makeDaoResourcePath(entity)`（格式 `dao:NopWfDefinition/{id}`），与 `DaoRuleModelLoader` 同模式。
- **独立 closure audit**（fresh session `ses_00d00431affeWYZ1KM3GqpjzbA`）：Phase 1/3/bug 修复/Anti-Hollow 全部 PASS；唯一 blocker 为 bundle `designer-page` renderer（跨仓）。
- **剩余 blocker**：flux 仓 `feat-flow-designer` 分支缺少 `pack-flux-bundle` 脚本与 `flux-bundle` 入口包；nop-chaos-next 的 `import-flux-to-libs.sh` 依赖该脚本。三仓构建链基础设施待建立。

### nop-stream 并发恢复去重竞态修复（TestJobCoordinatorRecoveryConcurrency flaky → 确定性）

`TestJobCoordinatorRecoveryConcurrency.concurrentGlobalRecovery_serializesToOneRotation` 偶现失败（`restartCount` 期望 1 实得 2）。根因：`JobCoordinator.globalRecovery()` 的 epoch 快照守卫非确定性——loser 线程若在 winner 完成临界区后才快照 `fencingEpoch`，快照值与当前 epoch 一致，守卫失效，导致冗余的第二次完整恢复。

- **修复方案（两层）**：
  1. **生产代码**：`JobCoordinator` 新增 `recoveryPending: AtomicBoolean` + 公共 `requestRecovery()`（CAS(false→true) 去重，赢家进 `globalRecovery`，输家 WARN 短路）；`globalRecovery()` 在 `finally` 末尾清 flag（**不是开头清**——开头清会重开 CAS 窗口），移除原 epoch 快照守卫；两个生产触发点（`detectFailures`、`reportTaskStatus` FAILED 路径）改调 `requestRecovery()`。`globalRecovery()` 契约不变（总是执行），保留 7+ 个直接调用它的现有测试。
  2. **测试代码**：CAS 只能去重"重叠并发"，no-op mock 下 `globalRecovery` 微秒级完成，`startLatch` 无法保证重叠。改用阻塞门确定性测试——`RecordingClusterRegistry.assignTask`（在 `recoveryLock` 下被调）装两个 `CountDownLatch`，让 driver A 钉死在恢复中段（`recoveryPending=true`）后再发 driver B，B 的 CAS 必失败、必立即短路；若回归则 B 阻塞在锁上、5s 超时确定性失败。删除 50 次迭代 stress 变种。
- **关键设计裁定**：CAS flag 在 `finally` 末尾清（不是开头）——开头清会让第二个 CAS 在"已清 pending"与"未完成恢复"之间成功并排队第二次恢复。末尾清让 CAS 窗口在恢复整个持续期间保持关闭。
- **残留语义（已写进字段/方法文档）**：CAS 对"重叠并发"完全确定性；对"背靠背"（B 在 A 完全结束后触发）不去重——但 `globalRecovery` 全局幂等，冗余触发只浪费不破坏，且故障检测器周期性兜底。未来若需 per-failure-event 恰好一次，需引入"故障观测序号"或观测点置位 flag（更大设计变更，超出本次范围）。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime` → `Tests run: 765, Failures: 0, Errors: 0, Skipped: 8` → BUILD SUCCESS；目标测试连续 5 次单跑全过。
- 详细记录：`ai-dev/bugs/2026-08/2026-08-12-nop-stream-concurrent-recovery-dedup-race.md`。
- Doc-sync: `No owner-doc update required`（`docs-for-ai/` 无平台契约变更；这是 nop-stream 内部恢复机制修复）。

### nop-stream TestAsyncSnapshotPipeline flaky 修复（构建验证时发现）

全模块构建时 `TestAsyncSnapshotPipeline.testTimeoutAbortDuringInFlightPersistIsNoOpThenCompletes:306` 偶现 `expected: <0> but was: <1>`，阻塞 reactor。该测试与 JobCoordinator 改动无关（完全不引用 JobCoordinator），是独立的异步时序 flaky test。

- **根因**：`CheckpointCoordinator.onCompletePersistSuccess` 在持锁时先 `forceComplete()`（完成 future，line 782）再 `decrementPendingCheckpointCount()`（line 789）；而 `getNumberOfPendingCheckpoints()` 是**非 synchronized** 的 `AtomicInteger.get()`。测试线程 `future.get()` 返回（看到 forceComplete）后立即读计数——forceComplete 与 decrement 是不同 volatile 字段、decrement 在 forceComplete 之后，JMM 不保证测试线程立刻看到 decrement。全模块负载下这个窗口可观测，计数读到 1。失败路径（`onCompletePersistFailure`）`status=FAILED`(line 826) 先于 decrement(line 828) 同理有竞态（line 390）。
- **修复**：新增 `awaitPendingCount(coord, expected, timeoutMs)` 忙等辅助（10ms 轮询、5s 超时），替换 `TestAsyncSnapshotPipeline` 三处 `assertEquals(0, getNumberOfPendingCheckpoints())`（line 306/358/390）。**不改生产顺序**——forceComplete 先于 decrement 是有意的（计数不能在 checkpoint 标记 durable 前归零，否则 racing trigger 可能过早触发新 checkpoint）。这与 `2026-07-18-test-race-condition-fix.md` 的修法一致（忙等替代即时断言）。
- 验证：全模块连续 3 次 `Tests run: 765, Failures: 0` → BUILD SUCCESS，flaky 消除。
- 详细记录：见 `ai-dev/bugs/2026-08/2026-08-12-nop-stream-concurrent-recovery-dedup-race.md` 的 Addendum 节。
