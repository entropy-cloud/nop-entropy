# 2026-05-26 Dev Log

## nop-code Plan 55: Deep Audit Remediation

### Summary
Executed 5-phase remediation from deep audit (78 findings). All 6 P1 issues resolved (2 downgraded after investigation). Key P2 fixes applied across security, ORM, IoC, error handling, thread safety, and documentation.

### Phase 1 — Security & Data Integrity (P1)
- **Path traversal fix**: Added `validatePath()` in `CodeIndexService` rejecting `..` in `vfsPath`/`projectPath`/`directoryPath`. Applied at `indexDirectory()` and `triggerIncrementalIndex()` entry points.
  - `nop-code/nop-code-service/src/main/java/io/nop/code/service/impl/CodeIndexService.java`
  - `nop-code/nop-code-service/src/main/java/io/nop/code/service/NopCodeErrors.java`
- **deleteIndex exception handling**: Removed catch(Exception) silent swallowing. Exception now propagates for ORM session rollback.
  - `CodeIndexService.java:deleteIndex()`
- **ORM cascade delete**: Added `cascadeDelete="true"` on NopCodeIndex's 4 to-many relations (files, symbols, dependencies, flows).
  - `nop-code/model/nop-code.orm.xml`
- **Missing table cleanup in deleteIndex**: Added NopCodeUsage, NopCodeFlow, NopCodeFlowMembership deletion before existing cleanup.
  - `CodeIndexService.java:deleteIndex()`
- **sourceCode visibility**: Set `published="false"` in `NopCodeFile.xmeta` override.
  - `nop-code/nop-code-meta/src/main/resources/_vfs/nop/code/model/NopCodeFile/NopCodeFile.xmeta`
- **Permission**: Investigated and confirmed framework auto-generates permissions + action-auth.xml exists → downgraded from P1.
- **Dict type mismatch**: Investigated and confirmed DictBean uses String-based lookup → runtime tolerant → downgraded to watch-only.

### Phase 2 — IoC & XDSL Fixes
- **TypeScript beans.xml**: Added `xmlns:ioc="ioc"`, changed from `ioc:default` to `ioc:bean-type="ILanguageAdapter"` matching Java/Python pattern.
  - `nop-code/nop-code-lang-typescript/src/main/resources/_vfs/nop/code/beans/_lang-typescript.beans.xml`
- **app-service.beans.xml**: Added `xmlns:ioc="ioc"`.
  - `nop-code/nop-code-service/src/main/resources/_vfs/nop/code/beans/app-service.beans.xml`

### Phase 3 — ORM Model & Data Architecture (P2)
- **8 new indexes**: fileId/parentId on Symbol, fileId/enclosingSymbolId on Usage, fileId/indexId on Call, indexId on Inheritance, indexId on AnnotationUsage.
- **New dict**: `code/relation_type` (EXTENDS=10, IMPLEMENTS=20) bound to NopCodeInheritance.relationType.
- **Unique constraint**: `(flowId, symbolId)` on NopCodeFlowMembership.
- **PK rename**: NopCodeDependency `depId` → `id` with service layer sync.
  - `nop-code/model/nop-code.orm.xml`, `CodeIndexService.java`

### Phase 4 — Error Handling & Thread Safety (P2)
- **Thread safety**: `incrementalStatusMap` → ConcurrentHashMap, `analysisCacheMap` → ConcurrentHashMap.
  - `NopCodeIndexBizModel.java`, `CodeIndexService.java`
- **ErrorCode adoption**: 4× UnsupportedOperationException → NopException + ErrorCode, 2× IllegalStateException in DigestHelper → NopException.
  - `CodeIndexService.java`, `DigestHelper.java`, `NopCodeErrors.java`
- **glob→regex fix**: `filterByFilePattern` now escapes regex metacharacters before replacing `*`/`?`.
  - `CodeIndexService.java`
- **Logging**: ManifestStore and ChangeAnalyzer catch blocks now LOG.warn.
  - `ManifestStore.java`, `ChangeAnalyzer.java`

### Phase 5 — Documentation & Code Hygiene (P2)
- **Doc fix**: `debugging-and-diagnostics.md` corrected: nop-code IS in root pom.xml modules.
- **Code hygiene**: `NopCodeApplication.java` `System.out.println("started")` → `LOG.info("started")`.

### Build Results
- `./mvnw clean install -pl nop-code -am` — BUILD SUCCESS
- All nop-code tests pass

## nop-stream Plan 53: CEP + Connector Correctness Fixes

### Changes
- **CepOperator IKeyedStateBackend 对接**：替换自建 `SimpleKeyedStateStore`，使用 `getKeyedStateBackend()` 或从 `stateBackend` 自动创建。CEP 状态现在可被 checkpoint 持久化。
  - `nop-stream/nop-stream-cep/src/main/java/io/nop/stream/cep/operator/CepOperator.java`
- **BatchConsumerSinkFunction Bug N53 修复**：`flush()` 在 `consumer.consume()` 失败时通过 try-finally 保证 `buffer.clear()` 执行。
  - `nop-stream/nop-stream-connector/src/main/java/io/nop/stream/connector/BatchConsumerSinkFunction.java`
  - `nop-stream/nop-stream-connector/src/test/java/io/nop/stream/connector/TestBatchConsumerSinkFunctionFailure.java`
- **runtime → cep 幽灵依赖移除**：从 `nop-stream-runtime/pom.xml` 移除无代码引用的 `nop-stream-cep` 依赖。
  - `nop-stream/nop-stream-runtime/pom.xml`

### Audit Results (First Round)
- 305 → 309 tests all passing (added 4 new tests)
- Checkpoint system: 7/7 PASS
- Multi-sink, cross-task data exchange: PASS
- Window/state management: PASS
- Operator lifecycle: PASS

## nop-stream Plan 54: Fraud Example Quality Fixes

### Changes
- **FraudDetectionDemo**：添加 try-catch 错误处理
- **TestUnusualAmountPattern**：新增 4 个测试（pattern 检测正常/异常金额、alert 生成、getter）

### Test Results
- 3901 tests total, 0 failures, 0 errors, 0 skipped
- BUILD SUCCESS
