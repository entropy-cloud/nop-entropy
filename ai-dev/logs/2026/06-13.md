# 2026-06-13

## Plan 156: Token Counting — ILlmDialect.estimateTokens() + Provider Usage Calibration (L2-16)

**Status**: completed
**Plan**: `ai-dev/plans/156-nop-ai-agent-token-counting.md`

### Changes

Modified files (nop-ai-core):
- `ILlmDialect.java`: Added `default long estimateTokens(List<ChatMessage>)` method delegating to `AbstractLlmDialect.estimateTokensBaseline()`
- `AbstractLlmDialect.java`: Added `public static long estimateTokensBaseline(List<ChatMessage>)` — sum of content.length()/4 + PER_MESSAGE_TOKEN_OVERHEAD (4) per message; null/empty-safe → 0

Modified files (nop-ai-agent):
- `ReActAgentExecutor.java`: Added `ITokenEstimator` field + builder method; calibration feed at usage-accumulation block (captures messagesAtCallTime, calls `tokenEstimator.record()` when promptTokens > 0); passes estimator to CompactionContext in `performCompaction()`
- `DefaultAgentEngine.java`: Constructs `CalibratedTokenEstimator.defaultInstance()`, shares it with executor via builder
- `CompactionContext.java`: Added `ITokenEstimator` field + 6-arg constructor (backward-compatible 5-arg constructor delegates with null)
- `NoOpContextCompactor.java`: Replaced static `chars/4` helper with `resolveEstimator()` that reads from CompactionContext (falls back to default estimator); INSTANCE still usable with factor 1.0
- `MicroCompressionCompactor.java`: Uses `resolveEstimator()` instead of removed static helper

New files (nop-ai-agent):
- `ITokenEstimator.java`: Interface with `estimateTokens(List<ChatMessage>)` and `record(messagesSent, actualPromptTokens)`
- `CalibratedTokenEstimator.java`: EMA-smoothed calibration (α=0.3, clamp [0.25, 4.0]); `defaultInstance()` wraps OpenAI dialect factor 1.0

New test files:
- `TestLlmDialectTokenEstimation.java` (nop-ai-core): 9 tests for dialect default — empty→0, null-safe, monotonic, deterministic, overhead contribution, all 4 dialects inherit
- `TestCalibratedTokenEstimator.java` (nop-ai-agent): 15 tests — factor starts at 1.0, EMA convergence, clamping, outlier dampening, zero/negative skip, calibrated estimate
- `TestTokenEstimatorWiring.java` (nop-ai-agent): 3 tests — ReAct loop feeds calibrator, no feed when usage null, actual promptTokens received
- `TestTokenCountingIntegration.java` (nop-ai-agent): 2 integration tests — end-to-end calibration→compaction accounting, message invariants preserved

Updated test files:
- `TestNoOpContextCompactor.java`: Updated `compactUsesCharsDiv4ForTokens` → `compactUsesCalibratedEstimatorForTokens` (asserts baseline+overhead)
- `TestCompactionDataPathway.java`: Updated to use `resolveEstimator()`; renamed `noOpUsesCharsDiv4TokenEstimation` → `noOpUsesCalibratedEstimatorForTokens`
- `TestMicroCompressionCompactor.java`: Updated `compactionResultCountsAccurate` to use `resolveEstimator()`

### Verification

- `./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS
- Roadmap L2-16 updated from ❌ to ✅
- Doc-sync: No owner-doc update required (internal estimation mechanism)

## Plan 155: Progressive Compression — Layer 0 Pre-Truncation + Layer 1 Micro-Compression (L2-4)

**Status**: completed
**Plan**: `ai-dev/plans/155-nop-ai-agent-progressive-compression.md`

### Changes

Modified files:
- `CompactionResult.java`: Added `List<ChatMessage> compactedMessages` field (nullable) with 6-arg constructor; data pathway for compactors to return modified message lists
- `NoOpContextCompactor.java`: Updated to use chars/4 token estimation instead of messageCount; returns `compactedMessages = null`
- `CompactConfig.java`: Added `maxRecentToolResults` (default 6) and `truncationThresholdChars` (default 8000); added `defaults()` factory method
- `ReActAgentExecutor.java`: `performCompaction()` now builds proper `CompactConfig`, applies compacted messages to context, adjusts tokensUsed by delta; success-path tool results truncated via `ToolResultTruncator`
- `DefaultAgentEngine.java`: Added `IContextCompactor` field defaulting to `MicroCompressionCompactor`; new 9-arg constructor; `resolveExecutor()` uses field instead of `NoOpContextCompactor.INSTANCE`

New files:
- `ToolResultTruncator.java`: Layer 0 truncation utility — head (6000) + marker + tail (1000) for content exceeding threshold; excludes non-truncatable tools (ask-oracle, ask-human)
- `MicroCompressionCompactor.java`: Layer 1 micro-compression compactor — replaces old compressible tool responses in-place with compact placeholders; preserves system messages, first user message, recent N tool results

New test files:
- `TestCompactionDataPathway.java`: 8 tests verifying data flow from compactor to context
- `TestToolResultTruncator.java`: 15 tests for Layer 0 truncation behavior
- `TestMicroCompressionCompactor.java`: 13 tests for Layer 1 compression behavior
- `TestCompactionIntegration.java`: 4 integration tests verifying full wiring and end-to-end flow

Updated test files:
- `TestNoOpContextCompactor.java`: Updated for chars/4 token estimation; added `compactReturnsNullCompactedMessages` and `compactUsesCharsDiv4ForTokens` tests

### Verification

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 372 tests, 0 failures, BUILD SUCCESS
- Roadmap L2-4 updated from ❌ to ✅
- Doc-sync: No owner-doc update required (internal implementation, no public API changes)

## Plan 154: IModelRouter Interface + PassThroughModelRouter + ReAct Integration (L2-10)

**Status**: completed
**Plan**: `ai-dev/plans/154-nop-ai-agent-model-router.md`

### Changes

New files in `io.nop.ai.agent.router` package:
- `RoutingResult.java`: Data object with `ChatOptions options`, `String complexity` (nullable), `String routingReason` (nullable)
- `IModelRouter.java`: Interface with `RoutingResult route(List<ChatMessage>, ChatOptions, AgentExecutionContext)`
- `PassThroughModelRouter.java`: Singleton pass-through implementation; `passThrough()` factory; returns input ChatOptions unchanged with "pass-through" reason

Modified files:
- `ReActAgentExecutor.java`: Added `IModelRouter` field + Builder method; invokes `modelRouter.route()` before each LLM call in the ReAct loop; uses returned `ChatOptions` for `chatService.call()`
- `DefaultAgentEngine.java`: Added `IModelRouter` field with PassThrough default; passes to executor builder via existing composition flow

New test files:
- `TestRoutingResult.java`: 6 tests covering construction, field access, null options rejection, options reference preservation, toString, equality
- `TestIModelRouter.java`: 2 tests verifying interface contract and PassThroughModelRouter assignability
- `TestPassThroughModelRouter.java`: 6 tests covering pass-through without/with tools, empty options, full options preservation, singleton, interface compliance
- `TestModelRouterInReActLoop.java`: 5 integration tests verifying: router called before LLM, receives correct messages/options, returned options used for actual call, custom router can override model/provider, PassThrough default backward compatible

### Verification

- `./mvnw test -pl nop-ai/nop-ai-agent -am -Dmaven.compiler.fork=true` passes (all existing + new tests)
- Roadmap L2-10 updated from ❌ to ✅
- Doc-sync: No owner-doc update required (design doc §6 already specifies router semantics)

## Plan 153: IContentGuardrail Interface + NoOpContentGuardrail + ReAct Integration (L2-7)

**Status**: completed
**Plan**: `ai-dev/plans/153-nop-ai-agent-content-guardrail.md`

### Changes

New files in `io.nop.ai.agent.guardrail` package:
- `GuardrailDirection.java`: Enum with INPUT, OUTPUT
- `GuardrailMode.java`: Enum with OFF, REPORT, ENFORCE
- `GuardrailResult.java`: Abstract base with 3 subtypes: PassResult (singleton), BlockResult (reason), ModifyResult (content)
- `IContentGuardrail.java`: Interface with `GuardrailResult check(GuardrailDirection, String, AgentExecutionContext)`
- `NoOpContentGuardrail.java`: Pass-through singleton always returning PassResult; `noOp()` factory method

Modified files:
- `ReActAgentExecutor.java`: Added `IContentGuardrail` field + Builder method; INPUT guardrail checks last user message content before LLM call (BLOCK prevents LLM, MODIFY replaces content); OUTPUT guardrail checks assistant message content after LLM response before tool dispatch (BLOCK prevents tool dispatch, MODIFY replaces content)
- `DefaultAgentEngine.java`: Added `IContentGuardrail` field with NoOp default; passes to executor builder via new constructor overload chain

New test files:
- `TestGuardrailDirection.java`: Verifies exactly 2 enum values
- `TestGuardrailResult.java`: Verifies PassResult singleton, BlockResult reason, ModifyResult content, equality
- `TestGuardrailMode.java`: Verifies exactly 3 enum values
- `TestNoOpContentGuardrail.java`: Verifies pass-through for INPUT/OUTPUT/null content, singleton, interface compliance
- `TestContentGuardrailInReActLoop.java` (7 tests): INPUT fires before LLM, OUTPUT fires after LLM before tools, INPUT BLOCK prevents LLM, OUTPUT BLOCK prevents tools, INPUT MODIFY replaces content, OUTPUT MODIFY replaces content, NoOp default no interference

### Verification

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` passes (all existing + new tests)
- Roadmap L2-7 updated from ❌ to ✅

## Plan 152: IContextCompactor Interface + NoOpContextCompactor + ReAct Integration (L2-3)

**Status**: completed
**Plan**: `ai-dev/plans/152-nop-ai-agent-context-compactor.md`

### Changes

New files in `io.nop.ai.agent.compact` package:
- `IContextCompactor.java`: Interface with `CompactionResult compact(CompactionContext ctx)`
- `CompactionContext.java`: Immutable data carrier with messages (List.copyOf), compactConfig, sessionId, agentName, executionContext
- `NoOpContextCompactor.java`: Pass-through implementation returning equal before/after token counts, null snapshotId; singleton INSTANCE

Modified files:
- `ReActAgentExecutor.java`: Added `IContextCompactor` field + Builder method; added `shouldTriggerCompaction()` (tokensUsed > maxTokens * 0.8 OR messages.size() > 30) and `performCompaction()` (PRE_COMPACT hook → compact() → POST_COMPACT hook) called at start of each iteration before PRE_REASONING
- `DefaultAgentEngine.java`: `resolveExecutor()` passes `NoOpContextCompactor.INSTANCE` to executor builder

New test files:
- `TestCompactionContext.java` (6 tests): construction, immutability (List.copyOf), null rejection, empty list
- `TestNoOpContextCompactor.java` (6 tests): equal before/after, retainedMessageCount, null snapshotId, sessionId, singleton
- `TestCompactionInReActLoop.java` (6 tests): not called below thresholds, called on token exceed, called on message exceed, PRE/POST hook ordering, no recursive compaction, default NoOp compat

### Verification

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C`: 288 tests, 0 failures (270 existing + 18 new)
- Independent closure audit: all 7 closure gates PASS, all 4 anti-hollow checks PASS
- Roadmap L2-3 updated from ❌ to ✅

## Plan 150: Agent Lifecycle Hook System — IAgentLifecycleHook + HookRegistry + ReAct Integration (L2-12)

**Status**: completed
**Plan**: `ai-dev/plans/150-nop-ai-agent-lifecycle-hook.md`

### Changes

New files in `io.nop.ai.agent.hook` package:
- `AgentLifecyclePoint.java`: Enum with 12 values (PRE_CALL, PRE_REASONING, POST_REASONING, PRE_ACTING, POST_ACTING, ON_ERROR, POST_CALL, REASONING_CHUNK, PRE_COMPACT, POST_COMPACT, BEFORE_TOOL_RESULT_PROCESSED, AFTER_TOOL_RESULT_PROCESSED)
- `HookResult.java`: Abstract base with 3 subtypes — `PassResult` (singleton), `VetoResult(reason)`, `ReenterResult(message)`
- `HookContext.java`: Mutable data carrier with lifecyclePoint, executionContext, data map, toolName, toolCallId
- `IAgentLifecycleHook.java`: Interface with `HookResult onEvent(HookContext ctx)`
- `IHookRegistry.java`: Interface with `getHooks()`, `register()`, `static empty()`
- `NoOpHookRegistry.java`: Pass-through implementation (getHooks→empty, register→throws UOE)
- `DefaultHookRegistry.java`: Stores hooks by lifecycle point; `fromAgentModel()` factory loads from DSL with case-insensitive event-name mapping

Modified files:
- `ReActAgentExecutor.java`: Added `IHookRegistry` field + Builder method; integrated hook invocations at all 12 lifecycle points with veto, re-enter (with max 3 re-entry limit), and failure propagation semantics
- `DefaultAgentEngine.java`: `resolveExecutor()` constructs `DefaultHookRegistry.fromAgentModel()` and passes to executor builder

New test files:
- `TestAgentLifecyclePoint.java` (5 tests): enum count and value names
- `TestHookResult.java` (7 tests): singleton, veto reason, reenter message
- `TestHookContext.java` (5 tests): mutable data, lifecyclePoint, toolName/toolCallId
- `TestDefaultHookRegistry.java` (13 tests): register/getHooks, fromAgentModel, event pattern matching, unmodifiable list
- `TestNoOpHookRegistry.java` (4 tests): empty hooks, register throws UOE
- `TestHookInReActLoop.java` (15 tests): integration tests verifying PRE_REASONING, POST_REASONING, PRE_ACTING per-tool, POST_ACTING per-tool, VetoResult, ON_ERROR, PRE_CALL/POST_CALL ordering, BEFORE/AFTER_TOOL_RESULT_PROCESSED re-enter, re-entry counter, non-re-entrant ReenterResult throws exception, NoOp backward compat

### Verification

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 263 tests, 0 failures, BUILD SUCCESS
- All 214 existing tests pass unchanged (backward compatible)
- 49 new tests added
- Roadmap L2-12 updated from ❌ to ✅

## Plan 149: AgentSession Supplementary Fields: parentSessionId, planId, compactedAt (L1-20)

**Status**: completed
**Plan**: `ai-dev/plans/149-nop-ai-agent-session-fields.md`

### Changes

- `AgentSession.java`: Added three nullable fields: `String parentSessionId`, `String planId`, `Long compactedAt` with getters/setters. Added `markCompacted()` convenience method that sets `compactedAt` to current time and calls `touch()`.
- `TestAgentSession.java`: Added 5 new tests — `testNewFieldsDefaultToNull`, `testParentSessionIdRoundTrip`, `testPlanIdRoundTrip`, `testCompactedAtRoundTrip`, `testMarkCompacted`.

### Verification

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 214 tests, 0 failures, BUILD SUCCESS
- Roadmap L1-20 updated from ❌ to ✅
- No owner-doc update required (design docs already specify these fields)

## Plan 148: Agent Mode Dispatch — DefaultAgentEngine dispatches by mode attribute (L1-19)

**Status**: completed
**Plan**: `ai-dev/plans/148-nop-ai-agent-mode-dispatch.md`

### Changes

- `DefaultAgentEngine.java`: Extracted `resolveExecutor(AgentModel)` method (package-private) that reads `model.getMode()` and returns `ReActAgentExecutor` (react/null/empty), `SingleTurnExecutor` (single-turn), throws `UnsupportedOperationException` (plan), or throws `NopAiAgentException` (unknown). `doExecute()` now delegates to `resolveExecutor()` instead of hardcoding `ReActAgentExecutor`.
- `SingleTurnExecutor.java`: New class implementing `IAgentExecutor` — single LLM call via `chatService.call()`, no tool loop, no iteration increment. Proper ctx lifecycle (`running`→`completed`/`failed`), token tracking, event publication (`EXECUTION_STARTED`→`EXECUTION_COMPLETED`/`EXECUTION_FAILED`), error handling for both non-success responses and exceptions.
- `TestModeDispatch.java`: 6 tests covering all mode dispatch paths — react, null, empty (backward compat), single-turn, plan (UOE), unknown (exception).
- `TestSingleTurnExecutor.java`: 8 tests — single call, ctx status lifecycle, token tracking, event publication, error response, exception handling, no-tool-invocation, no-iteration-increment.
- 3 test `.agent.xml` files: `test-single-turn-agent.agent.xml` (mode=single-turn), `test-plan-agent.agent.xml` (mode=plan), `test-unknown-mode-agent.agent.xml` (mode=unknown).

### Verification

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 209 tests, 0 failures, BUILD SUCCESS
- Roadmap L1-19 updated from ❌ to ✅
- Doc-sync: No owner-doc update required (design doc `nop-ai-agent-dsl.md` §3.3 already specifies the three-way dispatch)

## Plan 147: IContentTrustEvaluator Interface + DefaultContentTrustEvaluator (L1-8b)

**Status**: completed
**Plan**: `ai-dev/plans/147-nop-ai-agent-content-trust-evaluator.md`

### Changes

- `ContentOrigin.java`: New enum in `io.nop.ai.agent.security` with 4 values: `CHANNEL_INPUT`, `WEB_FETCH`, `FILE_READ`, `AGENT_GENERATED`
- `IContentTrustEvaluator.java`: New interface with method `boolean isTrustedSource(ContentOrigin origin, AgentExecutionContext ctx)`
- `DefaultContentTrustEvaluator.java`: Default implementation — CHANNEL_INPUT and AGENT_GENERATED → trusted, WEB_FETCH and FILE_READ → untrusted. Null origin returns false. Switch-based logic (no stub/placeholder).
- `TestDefaultContentTrustEvaluator.java`: 11 test methods covering all 4 origins with/without null context, null origin, and all-values iteration

### Verification

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 195 tests, 0 failures, BUILD SUCCESS
- Roadmap L1-8b updated from ❌ to ✅
- Doc-sync: No owner-doc update required (design doc §5.1 already specifies this interface and implementation)
