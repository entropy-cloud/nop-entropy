# 2026-08-13 开发日志

### Plan 341（nop-job ORM 模型清理）完成：死列/索引 + 字典描述入源 + 接口移除

- 关联 Plan：`ai-dev/plans/341-nop-job-orm-model-cleanup.md`（两轮对抗 review 通过后执行；closure audit 独立子 agent 判定 CONDITIONAL PASS，housekeeping 收口后 completed）。
- 性质：**删除性变更 + 覆盖索引新增**（无行为变化；GraphQL API 删除 6 个死字段为 breaking 但全仓确认无消费者）。

**源模型（nop-job/model/nop-job.orm.xml）**
- 删除：fire `retryRecordId`(18)/`taskCostCpu`(28)/`taskCostMemory`(29) + `IX_NOP_JOB_FIRE_RETRY`；task `workerAddress`(6)/`progress`(21)/`progressMessage`(22)。
- 新增：`IX_NOP_JOB_TASK_RUN_TIME (taskStatus, partitionIndex, startTime, jobTaskId)`、`IX_NOP_JOB_FIRE_DISPATCH_TIME (fireStatus, partitionIndex, startTime, jobFireId)`（服务 fetchRunningTasks / fetchDispatchingFires 的 DESC 游标扫描，消除 filesort）。
- schedule 三列 `taskCostCpu`/`taskCostMemory`/`priority` 保留（dispatcher/bestFit 消费）。
- executor-kind dict 3 option 补 `description` 入源（根治 codegen 覆盖——706c936f2 手工恢复版曾被二次覆盖）。

**Java 清理**
- 删 `IJobFireStore.updateRetryRecordId`（接口 :63 + impl :274-280 + 3 个测试 mock override）；`TestDefaultJobCancelHandler` 删 `setWorkerAddress`。retry 桥接为异步 fire-and-forget，从不回填（原 AR-4 死链路收口）。

**保留层/文档/E2E**
- view ×2（NopJobFire/NopJobTask 布局去死列）、保留层 xmeta ×2（防幽灵 prop）、orm.java、`docs-for-ai/03-modules/nop-job.md`（去 progress）。
- `retry-integration-design.md` §3.1 签名 String→void、删回填步骤与 `retry_record_id` 表行（修复既有 doc-code drift）。
- `store-contract-design.md` 新增「索引布局与死列裁定」小节。
- E2E `fault-tolerance.spec.ts`：删 Phase 4（progress roundtrip，`if(updateResp.ok)` 守卫空转）+ FireItem/TaskItem 接口字段。

**生成物（codegen 重生成，零手改）**
- `_app.orm.xml`/`_gen` entity/views/xmeta/i18n/`_templates`/api beans/deploy sql 全部自动清洗；dict description 与 orm.xml 源逐字一致；双构建（2 轮 codegen）后断言仍通过。

**验证**
- `./mvnw install -pl nop-job/nop-job-codegen -am -DskipTests` + `-pl nop-job/nop-job-meta -am` 双轮 BUILD SUCCESS；2.2b 安装 dao/api/web 新 jar。
- `./mvnw test -pl nop-job/nop-job-coordinator`：163 tests / 0 fail / 27 errors（21+6，全 42S04 H2 空库 = plan 340 baseline 精确一致）/ 2 skipped；`-am` 下 dao 新暴露 31 errors（20+11 全 42S04 空库，SQL 零死列引用，08-12 log 同口径）。
- `./mvnw compile` 受影响模块 BUILD SUCCESS；工具三件套 exit 0（check-plan-checklist 36/47 未勾为 completed 前 warning；scan-hollow 0 项；check-doc-links "No errors found"）。
- 偏差：`-am` 使 dao 模块 H2 测试首次纳入统计（plan 340 基线仅计 coordinator 27）；H2 空库问题按 Deferred 移交 successor（`NN-nop-job-h2-test-init`）。

- Commit: 待 nop-git-master 提交后回填
- Doc-sync: `docs-for-ai/03-modules/nop-job.md` 已更新；`docs-for-ai/INDEX.md`/`source-anchors.md` 核对无锚点变化