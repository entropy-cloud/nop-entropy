# 2026-08-13 开发日志

### Plan 342 收口（completed，2026-08-14 closure audit APPROVED 20/20）

- 独立子 agent（fresh session）closure audit：20/20 PASS，无 blocker；Anti-Hollow 通过（attempt 调用链三路径连通、无空壳）；工具四件套 exit 0（compile / check-doc-links / check-plan-checklist --strict / scan-hollow 0 findings）。
- 文本一致性：Plan Status=completed、5 个 Phase 全 completed、Closure Gates 全勾、daily log 收口记录——check-plan-checklist --strict 在 completed 状态下仍 exit 0。
- 遗留 follow-up：平台级 H2 schema-init 失效（根因：BeanConditionEvaluator 读 AppConfig.var，测试 config 被 nop-config provider 替换后不可见，DataBaseSchemaInitializer 被 if-property 门控禁用）——已记录，属 Non-Goal 范围；nop-retry 新 DB 用例（store/engine 29 个）在 H2 可用环境需跑绿。
- 提交建议：本 plan 变更可拆 3 commits——(1) API 契约 + 引擎缺口（Java 代码）；(2) ORM 死列/残留 + 生成物；(3) 测试 + 文档 + plan/log（沿用 plan 341 拆分模式）。尚未 commit，待用户指示。

### Plan 342 执行 - Phase 3 引擎缺口修复 + Phase 4 测试补齐（completed）

**H2 诊断结论（Proof 前置项）**
- 尝试路径：annotation `initDatabaseSchema=TRUE` / 补 `test/resources/application.yaml`（H2 数据源+init-database-schema）/ 构造器 `setTestConfig` / 命令行 `-Dnop.orm.init-database-schema=true`——**全部失败**。
- 铁证：nop-auth-service TestBizObject（注解+application.yaml+构造器 setTestConfig 三管齐下）本环境同样 `42S04 Table NOP_AUTH_SITE not found`。
- 根因定位：`BeanConditionEvaluator.checkProperty` 读 `AppConfig.var()`；测试 config 经 `setTestConfig` 写入后被 nop-config 替换 provider 丢失，`orm-defaults.beans.xml` 中 `DataBaseSchemaInitializer` 的 `<if-property name="nop.orm.init-database-schema"/>` 门控判定 false → bean 不创建 → 无 DDL。**平台级根因，Non-Goal 范围外**，与 plan 340/341 基线一致（环境限制）。
- 附加效果：application.yaml 使 orm 模型正常加载（插入 SQL 列齐全，不再空列），仅 schema init 不执行。

**引擎代码（Phase 3）**
- `IRetryRecordStore` + `RetryRecordStoreImpl`：新增 `newAttempt(record)`（attemptNo=retryCount+1、WAITING、startTime、requestPayloadSnapshot）+ `saveAttempt(attempt)`；`newRecord` 增加 `partitionIndex = Math.floorMod(hash(ns:groupId:idem), DEFAULT_PARTITION_COUNT)`；`moveToDeadLetter` 保存死信后**删除原 record 行**（幂等键可复用，B1 裁定落地）。
- `RetryEngineImpl.doExecute` 包装 attempt 落盘：RUNNING→SUCCESS/FAILED、endTime/durationMs、errorCode/message/stack（ErrorMessageManager）。
- 设计文档新增 §8「执行语义裁定」：attempt 追踪/分区/死信删除（副作用 a relation 悬空、b attempt 孤儿、c 管理侧 record 消失→死信页为终态视图）/retryFromDeadLetter 手动单次重放 + 错误码；PARALLEL 并发 attemptNo 撞约束记录为 watch-only。

**测试（Phase 4）**
- 新增 `TestRetryScannerImpl`（mock store/executor，**6/6 全绿**）：start/stop 幂等、fetch→lock→process、fetch 空 break、lock 空 continue、processor 异常不传播、running=false 不扫描。
- 新增 `TestRetryRecordStoreImpl`（6 用例，42S04 静态核对）：分区确定性/范围、fetch 过滤（到期+状态）、分区过滤、幂等查询终态忽略、乐观锁 PENDING→RETRYING、死信后删原 record。
- `TestRetryEngineImpl` 扩展 18 用例（42S04 静态核对）：attempt 落盘（数量/attemptNo/status/耗时/errorCode）、延迟路径 attempt、分区确定性、maxRetry 耗尽→死信+record 删除、bizFatal 直接死信、**二次失败→二次死信（B1 防空心闭包）**、deadline 超时、OVERWRITE/PARALLEL、指数退避 jitter=0 确定性、回调 ON_SUCCESS/ON_FAILURE/disabled+`_callback` 后缀、pause/resume missing、retryFromDeadLetter 三错误路径。
- 实测：`Tests run: 39, Failures: 0, Errors: 29`——29 个 error **全部** 42S04（engine 23 + store 6，与降级口径一致）；scanner 6/6 绿；engine 4 绿（2 fluent-config + 2 missing-record 快速失败）。
- 教训：scanner doScan 是 `do{...}while(running)` 循环，mock 测试若 fetch 恒有数据且 running 恒 true 会死循环 OOM——测试须在 processor 内置 running=false 或 fetch 用限次 supplier（真实环境由 scheduled 任务周期性触发，无此问题）。

### Plan 342 执行 - Phase 2 ORM 死列清理与残留删除（completed）

- 源模型 `nop-retry.orm.xml`：删 6 列（policy.saveRecordStrategy/executionTimeoutSeconds、record.taskType/bizNo/contextPayload、deadLetter.bizNo）+ 4 个死 domain + 2 个死 dict（save-record-strategy/retry-task-type，含 dict yaml 文件）；`UK_RETRY_DL_IDEMPOTENT_ID`（unique）→ `IDX_RETRY_DL_IDEMPOTENT_ID`（unique=false）——B1 裁定落地：同幂等键多次失败周期可产生多条死信。
- 死表面 `NopRetryTemplate` 全清：meta xmeta ×2、service xbiz ×2、`_templates/_NopRetryTemplate.json`、web pages 目录（view/lib/page.yaml）、action-auth 资源块、api beans ×3（InputBean/OutputBean/Api）、i18n 条目（codegen 重生成）。
- `RetryRecordStoreImpl.moveToDeadLetter` 删 `setBizNo` 行；`docs-for-ai/03-modules/nop-retry.md` 字段表/退避/块策略/回调说明同步（修 `intervalMs` 不存在字段，补 DeadLetter/Attempt 字段与回调语义）。
- **踩坑记录**：第一轮 `install`（无 clean）后 codegen 从 **target/classes 陈旧 xmeta**（上次构建产物）重新生成了 NopRetryTemplate api beans/pages/i18n，覆盖了我的 action-auth 手工编辑；`clean install` 后模型列表干净（action-auth 编辑保留），但 src 下残留的 round-1 生成物需**再删一次**（clean 不清 src）。最终 round 3 验证：grep `NopRetryTemplate|saveRecordStrategy|executionTimeoutSeconds|bizNo|taskType|contextPayload` 全模块零命中；deploy SQL 死列归零；`_app.orm.xml` 索引同步。
- 验证：round 2（clean install）+ round 3（install）双轮构建稳定（生成物收敛）；git 变更集含 D 文件 12 个（NopRetryTemplate 残留全删）+ 生成物 M 40+。

### Plan 342 执行 - Phase 1 API 契约收敛（completed）

- 代码：`IRetryTask` 删 `withCallback*`/`getCallbackService`/`getCallbackMethod`，`withExecutorId/getExecutorId`→`withExecutorName/getExecutorName`（IRetryTask.java / RetryTaskImpl.java / RetryRecordStoreImpl.java:100 / TestRetryEngineImpl / TestNopRetryJobRetryBridge mock 同步）。
- `NopRetryApiConstants` 删 `DEFAULT_EXECUTION_TIMEOUT_SECONDS`；`NopRetryPolicy` 删 `getExecutionTimeoutMsOrDefault()`。
- 删除 3 个 stale API bean：`NopRetryTemplateInputBean/OutputBean/Api.java`（确认无 codegen 引用）。
- 验证：`compile -pl nop-retry-api,nop-retry-engine,nop-job-retry-adapter -am` + `test-compile` 通过；grep `withCallback|getCallbackService|getCallbackMethod|withExecutorId|getExecutorId|DEFAULT_EXECUTION_TIMEOUT_SECONDS|getExecutionTimeoutMsOrDefault` 全仓零命中。
- 文档：`retry-integration-design.md` §3.5 改写为 policy 驱动回调（原 withCallback→nopJobCompletionService.onRetryComplete 描述已删），§2.1 字段表去 executionTimeoutSeconds/saveRecordStrategy。

### Plan 342（nop-retry 契约收敛）调研 + 修正 plan 起草 + 两轮对抗 review

- 关联 Plan：`ai-dev/plans/342-nop-retry-contract-convergence.md`（Status: draft，已过两轮独立子 agent review，无 blocker，待执行）。
- 前置：上会话完成 nop-retry 四问调研（设计/实现/测试/接口完整度）。本会话完成（a）live 证据复核（withCallback 死 API、partitionIndex 不赋值、attempt 空壳、5 死列、NopRetryTemplate 残留、42S04 测试基线等 10 项缺口全部验证属实）；（b）用户裁定：retry-integration-design.md 按现状语义重写而非坚持 withCallback 原设计；（c）plan 342 起草。

**Plan 342 内容概要（5 phases）**
1. API 契约收敛：删 `withCallback*`、`withExecutorId`→`withExecutorName`、删 `DEFAULT_EXECUTION_TIMEOUT_SECONDS` + `NopRetryPolicy.getExecutionTimeoutMsOrDefault()`、删 NopRetryTemplate API 残留。
2. ORM 死列清理：删 saveRecordStrategy/executionTimeoutSeconds/taskType/bizNo/contextPayload + 死域/死 dict；**`UK_RETRY_DL_IDEMPOTENT_ID` 唯一索引收窄为普通索引**；删 NopRetryTemplate 死表面全清单（含 `_templates/_NopRetryTemplate.json`、service xbiz ×2）；`RetryRecordStoreImpl:246 setBizNo` 随列删除。
3. 测试环境诊断 + 引擎缺口修复：H2 init 诊断前置（对比 setTestConfig 方式，失败降级口径=42S04 清单+静态核对）；attempt 落盘（saveAttempt + attemptNo 序列，UK_RETRY_ATTEMPT_RECORD_NO 不冲突）；partitionIndex 确定性赋值（`Math.floorMod(hash(ns+groupId+idempotentId), 16)`）；决策：死信后删 record 行使幂等键可复用（副作用 a/b/c 一并裁定：relation 悬空、attempt 孤儿、record 历史页消失→终态视图以死信页为准）；retryFromDeadLetter=手动单次重放文档化。
4. 测试补齐：scanner/store 新测试 + TestRetryEngineImpl 扩展。
5. 文档收口：retry-integration-design.md 终稿 + docs-for-ai/03-modules/nop-retry.md + check-doc-links。

**两轮 review 修正要点**
- Round 1（独立子 agent）发现并已修：B1（死信表唯一索引 vs 幂等键复用不自洽——二次失败会撞约束并在事务内回滚 record 状态更新导致 scanner 无限重扫，测试只测第一循环可达空闭包）；M1（H2 诊断从 Phase 4 前置到 Phase 3 首位）；M2（编译断裂点：`getExecutionTimeoutMsOrDefault`、`setBizNo`）；M3（决策 3 副作用裁定不全）。
- Round 2 定向复查：全 PASS，无 blocker，仅 remaining 文本 minor（Item Types 计数、Phase 4 标题等 4 处）已顺手清理。
- 后续：执行前需 mark plan active；执行时按 rule #18 逐 item 勾选。

### Plan 341 提交落库（3 commits）

- Commits: `2bbc54b9d`（feat(job,orm) ORM 死列+索引+dict 源模型，26 文件）、`1574f3bb9`（refactor(job) 移除 updateRetryRecordId，6 文件）、`e6be39826`（docs(job) 文档收敛+E2E spec+plan/log，6 文件）。
- 拆分依据：源模型+生成物 / Java 删除 / 文档 三组独立回退单元。

### Plan 341（nop-job ORM 模型清理）完成：死列/索引 + 字典描述入源 + 接口移除

- 关联 Plan：`ai-dev/plans/341-nop-job-orm-model-cleanup.md`（两轮对抗 review 通过后执行；closure audit 独立子 agent 判定 CONDITIONAL PASS，housekeeping 收口后 completed）。
- 性质：**删除性变更 + 覆盖索引新增**（无行为变化；GraphQL API 删除 6 个死字段为 breaking 但全仓确认无消费者）。

**源模型（nop-job/model/nop-job.orm.xml）**
- 删除：fire `retryRecordId`(18)/`taskCostCpu`(28)/`taskCostMemory`(29) + `IX_NOP_JOB_FIRE_RETRY`；task `workerAddress`(6)/`progress`(21)/`progressMessage`(22)。
- 新增：`IX_NOP_JOB_TASK_RUN_TIME (taskStatus, partitionIndex, startTime, jobTaskId)`、`IX_NOP_JOB_FIRE_DISPATCH_TIME (fireStatus, partitionIndex, startTime, jobFireId)`（服务 fetchRunningTasks / fetchDispatchingFires 的 DESC 游标扫描，消除 filesort）。
- schedule 三列 `taskCostCpu`/`taskCostMemory`/`priority` 保留（dispatcher/bestFit 消费）。
- executor-kind dict 3 option 补 `description` 入源（根治 codegen 覆盖——706c936f2 手工恢复版曾被二次覆盖）。

**Java 清理**
- 删 `IJobFireStore.updateRetryRecordId`（接口 :63 + impl :274-280 + 3 个测试 mock override）；`TestDefaultJobCancelHandler` 删 `setWorkerAddress`。retry 桥接为异步 fire-and-forget，从不回填（原 AR-4 死链路收口）。

**保留层/文档/E2E**
- view ×2（NopJobFire/NopJobTask 布局去死列）、保留层 xmeta ×2（防幽灵 prop）、orm.java、`docs-for-ai/03-modules/nop-job.md`（去 progress）。
- `retry-integration-design.md` §3.1 签名 String→void、删回填步骤与 `retry_record_id` 表行（修复既有 doc-code drift）。
- `store-contract-design.md` 新增「索引布局与死列裁定」小节。
- E2E `fault-tolerance.spec.ts`：删 Phase 4（progress roundtrip，`if(updateResp.ok)` 守卫空转）+ FireItem/TaskItem 接口字段。

**生成物（codegen 重生成，零手改）**
- `_app.orm.xml`/`_gen` entity/views/xmeta/i18n/`_templates`/api beans/deploy sql 全部自动清洗；dict description 与 orm.xml 源逐字一致；双构建（2 轮 codegen）后断言仍通过。

**验证**
- `./mvnw install -pl nop-job/nop-job-codegen -am -DskipTests` + `-pl nop-job/nop-job-meta -am` 双轮 BUILD SUCCESS；2.2b 安装 dao/api/web 新 jar。
- `./mvnw test -pl nop-job/nop-job-coordinator`：163 tests / 0 fail / 27 errors（21+6，全 42S04 H2 空库 = plan 340 baseline 精确一致）/ 2 skipped；`-am` 下 dao 新暴露 31 errors（20+11 全 42S04 空库，SQL 零死列引用，08-12 log 同口径）。
- `./mvnw compile` 受影响模块 BUILD SUCCESS；工具三件套 exit 0（check-plan-checklist 36/47 未勾为 completed 前 warning；scan-hollow 0 项；check-doc-links "No errors found"）。
- 偏差：`-am` 使 dao 模块 H2 测试首次纳入统计（plan 340 基线仅计 coordinator 27）；H2 空库问题按 Deferred 移交 successor（`NN-nop-job-h2-test-init`）。

- Commit: `2bbc54b9d`、`1574f3bb9`、`e6be39826`（见顶部条目）
- Doc-sync: `docs-for-ai/03-modules/nop-job.md` 已更新；`docs-for-ai/INDEX.md`/`source-anchors.md` 核对无锚点变化