# 2026-08-13 Dev Log

### 2026-08-13（plan 2026-08-12-2311-3 — AR-9 dispatchTimeoutMs 接线，Phase 1 完成 + closure audit APPROVED）

- Plan `ai-dev/plans/2026-08-12-2311-3-ai-invariant-p2-gateway-dispatch-timeout-wiring.md`（mission-driver 2026-08-12 分诊；P2 授权 = plan 级裁定 I3；来源 audit `2026-08-12-1119-open-audit-nop-ai-invariant-loop.md` [AR-9]）。
- 做了什么：`ChannelMessageServiceImpl.dispatchTimeoutMs` 死旋钮接线——`ai-gateway-defaults.beans.xml` `channelMessageService` bean（:85-86）新增 `<property name="dispatchTimeoutMs" value="@cfg:nop.ai.gateway.channel.dispatchTimeoutMs|30000"/>`（注释声明默认值须与 `DEFAULT_DISPATCH_TIMEOUT_MS` 同步）；mode1 + mode2 两个 test beans.xml 镜像同步同一 property（:25-26 / :32-33）；`TestChannelMessageServiceIoC` 新增 2 测试——`dispatchTimeoutMsDefaultsTo30000`（反射读字段断言 30000）+ `dispatchTimeoutMsFollowsAssignedConfigValue`（`assignConfigValue` 于 container.start 前设 12345 → bean 反射断言 12345 → finally 复位原值防全局静态泄漏），观测全走反射（不新增 public getter，公共 API 不变）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-gateway -am -Dtest=TestChannelMessageServiceIoC -Dsurefire.failIfNoSpecifiedTests=false` 4/4 绿（2 既有 + 2 新增，@cfg 在 `AppBeanContainerLoader` 测试容器端到端解析——无 Blocker）；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS 全绿；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；checkstyle `-Pqa` EXIT=0（默认-config 直跑 9164 pre-existing violation 全在未触碰上游 nop-api-core，I5/I6 同口径；受处理文件零命中）；`check-import-order --module nop-ai` 164 pre-existing 零新增；`scan-hollow-implementations --module nop-ai --severity high` 2 条 pre-existing（PlanReplanner:272 / NoOpProviderFailoverQueue:34）零新增；全模块反查 `nop.ai.gateway.channel.dispatchTimeoutMs` 命中生产 beans.xml + javadoc(:100) + 测试（接线点/承诺点/测试三角闭环）；`ChannelMessageServiceImpl` 无同类无接线配置字段（FeishuConnector 凭据走已接线的 feishuCredentials bean，ChannelConfig 为运行时载体，零发现 follow-up 不成立）。
- 偏差：无（@cfg 解析、assignConfigValue 覆盖、finally 复位均按计划落地）。
- Doc-sync: No owner-doc update required（javadoc 承诺由接线兑现；`nop.ai.gateway.channel` docs-for-ai 零命中已核实）。
- Closure audit（fresh session `ses_009079c68ffefOAJ8LXmwkGi1i`，review-only）：**APPROVED**——7 条 Exit Criteria 逐条 PASS（beans.xml :85-86 / 双 test beans.xml / 反射观测 + 无 public getter / diff 仅 5 文件 / 4 测试 0 失败 / grep 三角闭环 / plan 文本勾选态），Closure-gate 检查全 PASS（Anti-Hollow：@cfg 链路 ConfigExpressionProcessor→ConfigValueResolver→BeanContainerImpl.getConfigValue→configProvider 源码核实 + 覆盖值断言运行时真实生效；无静默 no-op；无 in-scope defect 降级）。0 Blocker / 0 Major；2 条 informational（日志条目即本条；leak-guard 语义已核实安全）。`check-plan-checklist.mjs --strict` exit 0。
- Commit: 随本 log 条目一并提交。

### 2026-08-13（plan 2026-08-12-2311-2 — Gate-1 负例 fixture 迁移 AR-8，Phase 1/2）

- Plan `ai-dev/plans/2026-08-12-2311-2-ai-invariant-p2-gate-test-fixture-hygiene.md`（mission-driver 2026-08-12 分诊；P2 授权 = plan 级裁定 I3）。
- 做了什么：`TestInvariantGate1SecureDefault.java`（nop-ai-agent）与 `TestInvariantGate1SecureDefaultShell.java`（nop-ai-shell）负例 fixture 从 `src/main/java/io/nop/ai/.../gatefixture` 迁到 `<module>/target/gate-fixture/`（写/扫/删共享常量 `FIXTURE_ROOT`）；`scanSourceTree()` 拆无参重载（正例扫真实 `src/main/java`）+ `scanSourceTree(Path root)` 参数化（负例扫 fixture 根），排除规则原样复用；清理从单文件删除升级为递归删整个 fixture 目录（新增 `deleteFixture(Path)`，shell 侧 finally 内联清理同步升级）。
- 验证（agent 侧中断残留模拟，固定顺序）：`./mvnw clean -pl nop-ai/nop-ai-agent -am` → 手工写 `DefaultKilledResidualFixture.java` 到 `nop-ai/nop-ai-agent/target/gate-fixture/io/nop/ai/agent/gatefixture/`（不删，模拟 kill -9）→ `./mvnw compile -pl nop-ai/nop-ai-agent -am`（不带 clean）→ `find nop-ai/nop-ai-agent/target/classes -name "*gatefixture*"` **零命中**（残留不进产物）→ 残留在场时 `-Dtest=TestInvariantGate1SecureDefault#tableCompleteness_matchesSourceTree` 单跑正例 1/1 绿（门禁不染）→ 全类 5/5 绿，`finally` 递归删除后 `target/gate-fixture` 整个目录消失（残留被一并清除）。
- 验证（shell 侧同构模拟）：`./mvnw clean -pl nop-ai/nop-ai-shell -am` → 写 `DefaultShellKilledResidualFixture.java` 到 `nop-ai/nop-ai-shell/target/gate-fixture/io/nop/ai/shell/gatefixture/` → `./mvnw compile -pl nop-ai/nop-ai-shell -am` → `find nop-ai/nop-ai-shell/target/classes -name "*gatefixture*"` 零命中 → 残留在场先单跑正例 1/1 绿 → 全类 3/3 绿 → 递归删除后 fixture 目录消失。
- 正常路径零残留：`git status --porcelain nop-ai/nop-ai-agent` 仅预期测试文件 M（无未跟踪 fixture）；`find nop-ai/nop-ai-agent/src -name "*gatefixture*"` 零命中；`src/main` 无 `gatefixture` 目录。
- 偏差：无（计划预见到 `Stream.toList()` 在 Java 11 release 下不可用——用 `Collectors.toList()` 替代，属计划外实现细节）。
- 验证（全量）：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS，surefire 汇总 total=4407 failures=0 errors=0 skipped=15；两门禁类 `TestInvariantGate1SecureDefault` 5/5、`TestInvariantGate1SecureDefaultShell` 3/3。
- Doc-sync: No owner-doc update required（测试基础设施修复，不改变门禁契约）。
- Commit: 待独立 closure audit 后提交。

## nop-credential-mfa mission（master 分支工作记录）

# Development Log - 2026-08-13

### plan W7-successor NopAiModel.credentialId 运行时消费读取切换 + 集成点裁决：执行完成

plan `ai-dev/plans/2026-08-13-1118-3-nop-ai-model-credential-runtime-consumption.md`（roadmap W7-successor）执行完成。把 W7 已落地但未接通的 `NopAiModel.credentialId` 接入 LLM 运行时调用链。

- **Phase 1（集成点裁决，gating）**：基于 live-repo 核对（nop-ai-core 无 DAO 依赖、resolveApiKey 在 nop-ai-core、NopAiModel 是定价注册表不在调用路径、运行时配置走 .llm.xml、粒度 provider↔model 不匹配），落五项裁决：**D1 集成层**=新解析器抽象（`IAiModelCredentialResolver` in nop-ai-api，impl in nop-ai-service，钩点 `ChatServiceImpl.buildHttpRequest`）；**D2 优先级链**=`accountKey > credentialId > resolveApiKey`；**D3 依赖方向**=nop-ai-service 新增 nop-credential-api（compile）；**D4 查找键**=provider+modelName 精确匹配，.llm.xml↔NopAiModel 不对应=显式回退+WARN 审计；**D5 fail-closed**=强（credentialId 非空但凭证失效抛 NopException 中止）。
- **Phase 2（实现）**：
  - 新增 `IAiModelCredentialResolver`（nop-ai-api）+ `AiModelCredentialResolverImpl`（nop-ai-service：IDaoProvider + ICredentialProvider 经 `@Nullable` setter 可选注入；findCredentialId 按 provider+modelName 查行；getCredentialData 解析 apiKey；fail-closed 错误码 ERR_AI_CREDENTIAL_FIELD_EMPTY / ERR_AI_CREDENTIAL_PROVIDER_NOT_AVAILABLE / ERR_AI_CREDENTIAL_RESOLVER_NOT_CONFIGURED）。
  - `ChatServiceImpl`：`@Inject @Nullable IAiModelCredentialResolver`（NopIoC optional，未装配→null→零回归）+ `resolveApiKeyForRequest`（accountKey > credentialId > resolveApiKey）。
  - `NopAiModelBizModel.save` override：读旧 credentialId → super.save → `reconcileCredentialUsage`（bind/switch/unbind/noop，consumerRef=`ai:NopAiModel:<id>`）。
  - `nop-ai-service/pom.xml` 加 nop-credential-api；`_service.beans.xml` 注册 `nopAiModelCredentialResolver` bean。
  - **关键坑**：NopIoC `@Nullable` optional 注入须用 setter（无字段 @Inject），否则 required 字段注入在部署无 nop-credential 时启动失败；nop-ai-service 测试须用 `CoreInitialization.initializeTo(INITIALIZER_PRIORITY_REGISTER_COMPONENT)`（低于 IoC），避免 nop-sys SysDictLoader 拉起需 datasource 配置的全局容器（与同模块 TestNopAiBizModelEntityCrud 一致）。
- **Phase 3（E2E + 回归 + docs）**：`TestAiModelCredentialResolver`（resolution/fail-closed/fallback，真实 ORM+H2+fake ICredentialProvider，7 tests）、`TestNopAiModelCredentialUsage`（bind/switch/unbind/noop/consumerRef，7 tests）、`TestChatServiceImplCredentialWiring`（accountKey>credentialId>resolveApiKey 优先级链 + fail-closed 传播 + 零回归，5 tests）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-service,nop-ai/nop-ai-dao -T 1C` → BUILD SUCCESS（nop-ai-core 217 / nop-ai-service 25 / nop-ai-dao 6 全绿，含新增 19 测试，0 回归）。Anti-Hollow：getCredentialDataCalls 计数断言 + bearer token = credential key 断言（调用链出口）。
- **Doc-sync**：design `nop-credential/01-architecture-baseline.md` §3.4（集成点裁决 + resolver 契约）+ §3.5（nop-ai 重分类为已落地 W7-successor）；`docs-for-ai/03-modules/nop-ai.md`（运行时消费表述 Deferred→已落地 + 集成点/consumerRef/优先级链/fail-closed）。docs-for-ai 链接检查 0 错误。
- **Follow-up**：遗留 `DefaultAiChatService.getApiKey` 路径的 credentialId 覆盖（Phase 1 D1 裁定仅接主路径 ChatServiceImpl，Non-Blocking）。

### plan 2251 Bean 命名前缀合规修正：收口（Phase 2 验证 + Phase 3 CI 门控/文档）

plan `ai-dev/plans/2251-bean-naming-prefix-compliance.md`（check-bean-naming 落地）执行完成。

- **Phase 2（改名）验证**：前期已在工作树完成 11 模块 53 个短名 bean id 的 `nop` 前缀改名 + ref/Java 引用同步（nop-auth 6 / db-migration 19 / stream 4 / credential 4 / ai-gateway 4 / job 3 / feishu 3 / 单点 4；nop-cluster 4 按 Phase 1 裁决豁免）。本次收口核对：`node ai-dev/tools/check-bean-naming.mjs` → beanId=0, ref=0, collect-prefix=0, exit 0；源码 grep 旧名零残留（仅 `_dump/` gitignored 生成物与 nop-demo 豁免模块含旧名）。
- **Phase 3（CI 门控 + 文档）**：新建 `.github/workflows/compliance.yml`（`bean-naming` job：checkout + setup-node 20 + `node ai-dev/tools/check-bean-naming.mjs`，push/PR 触发，违规 exit 1）；`docs-for-ai/02-core-guides/code-style.md` §IoC Bean 命名新增「检查工具」「豁免（非违规）」小节（运行命令 + `abstract="true"` 模板/biz_/test/ai-tools:/外部上下文 bean 豁免理由）。
- 验证：`./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` BUILD SUCCESS（190+ tests 0 failures）；`./mvnw compile -pl :nop-credential,:nop-job,:nop-stream,:nop-db-migration,:nop-graphql-core,:nop-metadata-service,:nop-sys-service,:nop-wf-scheduler,:nop-integration-feishu -am -q` EXIT=0；YAML 校验 compliance.yml 合法。
- Doc-sync：`docs-for-ai/02-core-guides/code-style.md` 已更新；doc-link 检查维持 20 错误基线（预存 ai-dev 陈旧链接，本次未新增——修掉 code-style 引用 ai-dev 工具路径的 BOUNDARY 违规）。

### plan W7 存量迁移（NopAiModel.credentialId）+ docs-for-ai 同步：执行

plan `ai-dev/plans/2026-08-13-1118-2-legacy-migration-docs-sync.md`（roadmap W7）执行中。

- **Phase 1 — NopAiModel credentialId 迁移路径（code）**：`nop-ai/model/nop-ai.orm.xml` 的 `NopAiModel` 新增可选普通列 `credentialId`（`credential_id VARCHAR(50)`，propId=17，**不设 refEntityName/to-one 关系**，避免 nop-ai-dao 引入对 nop-credential-dao 的跨模块 DAO 依赖）。`./mvnw install` 触发 codegen 重新生成实体/dao/`_app.orm.xml`/xmeta/i18n/api beans/deploy SQL（mysql/oracle/postgresql，均含 CREDENTIAL_ID 列）——**禁止手编任何 `_` 前缀文件**，全部经 model→codegen 流转。
  - **依赖方向裁定**：`nop-ai-dao/pom.xml` 核对**未新增** `nop-credential-dao` 依赖（credentialId 为普通列、无 ORM 关系，故不需要）。逻辑外键（credentialId → NopCredential）仅在迁移文档描述；未来运行时消费经 `ICredentialProvider`（api 层）而非 DAO。
  - **新功能测试**：`TestNopAiOrmEntityMapping.testCredentialIdColumnRoundTrip`（credentialId 读写 round-trip + 可选 null + propId=17 断言），6 tests 0 failures。
  - **本期不接通运行时消费读取**（消费方切换为 Deferred，避免空壳：字段存在但无消费方读取）。
  - 迁移文档落 `docs-for-ai/03-modules/nop-ai.md`（建凭证→registerUsage→设 credentialId→apiKey 冗余 + 本期边界声明）。
  - 验证：`./mvnw compile -pl nop-ai/nop-ai-dao -am` 绿；`./mvnw clean install -DskipTests -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` BUILD SUCCESS。设计 `nop-credential/01-architecture-baseline.md` §3.4/§3.5 已正确表述边界，无需改动。

- **Phase 2 — docs-for-ai 新增 + 更新（W6 前置 gate 已满足）**：roadmap W6 标 `planned`（书记账本陈旧），但 W6 live 产物已 repo-verified 存在（`NopAuthUserBizModel.bindMfa/confirmMfa/unbindMfa/resetUserMfa/generateRecoveryCodes/getMfaStatus`、`ScanLoginResult.mfaRequired`、`TestMfaUserSelfService`/`TestScanLoginMfa`），故 gate 安全条件满足，可编写引用真实代码的文档。
  - 新建 `docs-for-ai/03-modules/nop-credential.md`：模块定位、cv1 密文格式、`ICredentialProvider` SPI、类型注册、明文边界（xmeta published=false）、管理 API、与 `@sec:` 边界。
  - `docs-for-ai/03-modules/nop-auth.md` 补 MFA 章节：4 个 MFA 实体（MfaSetting/RecoveryCode/MfaChallenge/SmsCode）、TOTP（RFC 6238）、两阶段登录（loginAsync/createSessionForUserAsync/mfaVerify 行号）、短信登录（loginType=5）、存储（db/local/redis）、用户自助/管理员 API（bindMfa~resetUserMfa 行号）、扫码适配、配置项、错误码。
  - `docs-for-ai/02-core-guides/auth-and-permissions.md` 补两阶段登录：loginType 表（1/2/3/4/5/20-23）、ERR_AUTH_MFA_REQUIRED 流程、扫码适配。
  - `docs-for-ai/02-core-guides/ioc-and-config.md` 补 `@sec:` 配置加密章节：DefaultConfigValueEnhancer + AESTextCipher 机制、何时用 `@sec:` vs 凭证库、FeishuCredentials 样例。
  - `docs-for-ai/03-modules/nop-ai.md` NopAiModel 行补 credentialId + 迁移说明（Phase 1 已落地）。
  - **Anti-Hollow**：所有锚点经独立 explore agent 逐条 file:line 核对 live repo，抽查 loginAsync:241/bindMfa:133/CredentialProviderImpl:46/@sec::67/mfaRequired:32 全部命中。
  - **文档链接**：docs-for-ai/ 0 错误（修复了 nop-ai.md 一处 docs-for-ai→ai-dev BOUNDARY 违规）。剩余 20 个 link 错误均为 ai-dev/ invariant-loop audit 路线图/skills 的预存陈旧链接（引用未创建的 audit catalog/工具），与本 plan 无关、out-of-scope。

- **Phase 3 — INDEX + source-anchors + 设计收口核对**：
  - `docs-for-ai/INDEX.md` 新增 nop-credential 模块路由行 + nop-auth MFA 路由注释。
  - `docs-for-ai/04-reference/source-anchors.md` 新增 4 锚点：`AUTH-MFA-001`（LoginServiceImpl 两阶段 + NopAuthUserBizModel bindMfa~resetUserMfa）、`AUTH-MFA-002`（TOTPAuthenticator + DbMfaChallengeStore/DbSmsCodeStore + NopAuthErrors）、`CRED-001`（ICredentialProvider/CredentialProviderImpl 明文边界 + CredentialCipher cv1 + NopCredentialBizModel）、`SEC-CFG-001`（DefaultConfigValueEnhancer @sec: + ConfigStarter 装配 + AESTextCipher + CommonConstants）。
  - **设计收口核对**：`ai-dev/design/nop-auth/` + `nop-credential/` grep `Open Question|开放问题|TODO|TBD` 零命中。`ai-dev/design/README.md` 已索引两子系统（active），修正 nop-credential 行陈旧描述（"对称加密原语下沉 nop-security + @credential: resolver" → 与 baseline 一致："复用 nop-commons AESTextCipher + cv1 格式 + 拒绝 @credential: resolver"）。
  - 文档链接：docs-for-ai/ 维持 0 错误。

### plan W8 MFA 存储数据库实现 + store-type 默认 db：完成全部 3 个 Phase，plan 收口

plan `ai-dev/plans/2026-08-13-0900-1-mfa-db-store-and-default-db.md`（roadmap W8）执行完成。新增 DB 实现 + 默认 store-type=db + 首次建立 Redis 生产接线（条件式，守住类加载安全不变式 ai-dev/lessons/15）。

- **Phase 1 — ORM 实体（model-first）**：`nop-auth/model/nop-auth.orm.xml` 新增 `NopAuthMfaChallenge`（表 `nop_auth_mfa_challenge`，PK `challengeToken` VARCHAR(64)，列 userId/mfaType/loginType/tenantId/phone/`expireAt` BIGINT epoch 毫秒/`failCount` INTEGER default 0 + 审计字段，索引 userId/expireAt）+ `NopAuthSmsCode`（表 `nop_auth_sms_code`，PK `codeKey` VARCHAR(100)，列 phone/`code` 明文/`expireAt`/`failCount` + 审计，索引 expireAt）。参照 `NopAuthSession`（瞬态表，无 version/delFlag）。codegen 产出实体/dao/`_app.orm.xml`/DDL（mysql/postgresql/oracle `_create`/`_drop`，未手编任何 `_` 前缀文件）。**SMS code 明文裁决**：对齐 Local/Redis 行为等价；5min TTL+maxAttempts 防爆破已足够，BCrypt 收益对瞬态数据不显著（恢复码 long-lived 故仍 BCrypt）。`TestDbMfaChallengeStoreEntityRoundTrip` 3/3 绿（save/load/update/delete）。
- **Phase 2 — DbMfaChallengeStore/DbSmsCodeStore**（`nop-auth-service/.../mfa/store/`）：基于 `IDaoProvider`（CRUD `saveEntityDirectly`/`getEntityById`/`updateEntityDirectly`）+ `IJdbcTemplate.executeUpdate`（原子 SQL）。consume 用条件 `DELETE WHERE pk=? AND expire_at>?` + affected-row 判定保证一次性（并发 consume 仅一个 affected>0）；incrFailCount 用 `UPDATE SET fail_count=fail_count+1 WHERE pk=? AND expire_at>?`（SQL 原子递增，对齐 Redis INCRBY 方案 A），读回用 `findInt`（原始 SQL 绕过 ORM 会话缓存避免脏值）。TTL 清理惰性（peek/consume 过期即删）。`TestDbMfaChallengeStore` 8/8 绿（含**并发断言**：20 线程 incrFailCount 最终值=20 无丢失）；`TestDbSmsCodeStore` 9/9 绿（三态/消费/失败计数/maxAttempts 作废/重发/TTL/explicit consume）。无静默跳过。
- **Phase 3 — MfaStoreProvider collect-beans 装配 + 默认 db + 条件式 Redis 注册**：
  - `MfaStoreProvider` 改为 collect-beans name-prefix 声明式装配（`nopMfaChallengeStore_`/`nopSmsCodeStore_`，`as-map`），按 store-type 选择；默认 `STORE_TYPE_DB`；fail-closed（请求类型未注册显式抛异常）。零 `INosqlService` 类型引用（W4 不变式保持）。
  - **关键发现**：collect-beans 必须加 **`ioc:ignore-depends="true"`**——缺此属性时 mfaStoreProvider 经 store bean（→ daoProvider/jdbcTemplate）形成硬依赖链，与 `nopBizObjectManager` 的 @BizModel 收集（`by-annotation`，也用 ignore-depends）产生初始化顺序冲突，导致 `biz_NopAuthUser` proxy 在 runLazyProperties 阶段以空 bizModelBeans 构建（`convert-to-type-fail` for `INopAuthUserBiz`，影响所有加载 app-test.beans.xml 的测试）。加上 ignore-depends 后解除环依赖，全绿。平台 collect-beans 标准模式均带 ignore-depends（biz-defaults/nopBizObjectManager、channelBindService）。
  - store bean `autowire-candidate="false"` + `lazy-init="true"` 隔离不污染类型注入（避免与 `mfaChallengeStore` 工厂产物在 nopLoginService @Inject 处冲突）。
  - Redis bean 经 `ioc:condition`（`if-property store-type=redis` + `on-class INosqlService`）条件激活——classpath 无 nosql 时根本不注册、不被收集、类不加载（ai-dev/lessons/15 类加载安全回归守门）。store-type=redis 但无 Redis store 注册时 provider 显式 fail-closed（不静默回退）。
  - `TestMfaStoreProvider` 7/7（默认 db/local/db/redis 选择 + fail-closed 三态）；`TestMfaStoreWiringDb` 3/3（IoC 接线：store-type=db 返回 DbMfaChallengeStore 实例断言 + Redis bean 在 store-type≠redis 时不被收集 = 类加载安全回归守门）。
- **全量测试**：`./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` BUILD SUCCESS；nop-auth-service 全量 **171 tests, 0 failures, 0 errors**（既有 W4/W5 MFA 测试、TestMfaLoginE2E、TestCompactExtField 等零回归）。
- **文档同步**：设计 `01-architecture-baseline.md` §3.3（DB 实现形态 + 原子性 + 明文裁决）+ §3.7（store-type 默认 db）；roadmap W8 → done。

### MFA 存储数据库实现计划 + store-type 默认 db（用户裁决驱动）

- **背景**：nop-app-erp 启动崩溃根因定位——`MfaStoreProvider.java:20` 编译期直接 `import io.nop.nosql.core.INosqlService`，`nop-auth-service` 将其声明为 `<optional>` 依赖；应用未引入 nosql 时 Quarkus 启动在 `BeanDefinitionBuilder.initFactoryBeans` 反射 `MfaStoreProvider` 方法签名处抛 `NoClassDefFoundError: io/nop/nosql/core/INosqlService`。
- **用户裁决（2026-08-13）**：缺省不使用 Redis；**所有存储必须有基于数据库的实现**；默认 `store-type` 由 `local` 改为 `db`（经确认：新 ORM 实体表方案 + 默认 db）。
- **现状核实**：`MfaChallengeStore`/`SmsCodeStore` 仅 Local（JVM 内 ConcurrentHashMap，重启丢失/多实例不共享）+ Redis（依赖 nop-nosql，optional）双实现，**无数据库实现**（`ai-dev/design/nop-auth/01-architecture-baseline.md` §3.3 存储组件）。ORM 源已有 `NopAuthMfaSetting`/`NopAuthMfaRecoveryCode`（W4），无 challenge/sms code 存储表。
- **新增计划**：`ai-dev/plans/2026-08-13-0900-1-mfa-db-store-and-default-db.md`（draft）——Phase 1 新增 `NopAuthMfaChallenge` + `NopAuthSmsCode` ORM 实体（model-first codegen）；Phase 2 `DbMfaChallengeStore`/`DbSmsCodeStore`（TTL 惰性清理、consume 条件 DELETE 原子一次性、incrFailCount SQL 原子递增、verify 三态）；Phase 3 `MfaStoreProvider` 三态分派 + 默认 db + **类加载崩溃修复**（Redis 分支隔离装配，无 nosql 依赖可启动）。
- **roadmap 登记**：`ai-dev/backlog/nop-credential-mfa-roadmap.md` 新增 W8（todo，指向本 plan）。
- **下一步**：计划独立草案审查 → 执行（codegen 生成两张表 + DB 实现 + 崩溃修复 + 回归验证：无 nosql 依赖启动 + 既有 MFA 测试零回归）。
