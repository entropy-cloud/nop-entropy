# 2026-08-13

### Plan `2026-08-13-1930-3` 执行 + closure：component-roadmap C5 容错缺口表与 §5 技术债表同步（P1-18-02，纯文档）

- **Phase 1 六行回写（全部 Fix，目标 `ai-dev/design/nop-stream/component-roadmap.md`，仅 6 行变更零代码）**：:186 abort 接线行——"distributed 仍 Deferred（lease failover 兜底）"删除，改标 **Stage 39 Phase 3 已落地**（`JobCoordinator.registerDistributedAbortHandler()` :1593 经 `CheckpointCoordinator.setAbortHandler` 注册 + `RpcDistributedExecutor.java:250` 注册 distributed handler / embedded 注册 local handler 共存，§8.7 对照）；:189 abort 传播通道行——"前置依赖：独立控制通道（P1）"过期表述删除，改标 ✅ Stage 39 Phase 3（local mailbox + interrupt / distributed `cancelTask` RPC），保留"无 `CancelCheckpointMarker`"（§13.2.1 Decision-only）；:191 并发能力一致行——`Math.min(1, config)` 强制 max=1 + 降级警告表述删除，改按 live code（`CheckpointCoordinator.java:370` `effectiveMaxConcurrent = config.getMaxConcurrentCheckpoints()` 无 clamp）与 §13.2（Stage 45 task/对齐器层多 in-flight 已满足，aligned 端到端成立，unaligned 保持 single-in-flight §2.8.1 D4）更新，同步删除"task 层 Deferred"过期表述；:193 背压逃生行——"端到端背压验证见 Phase 4"改为 ✅ 已由 E2E 覆盖（`TestUnalignedCheckpointBackpressure` Anti-Hollow capture→persist→restore→replay + `TestUnalignedCheckpointMultiInput` + `TestChannelStateRescaleE2E`）；§5 :307 行——"abort 控制通道 local 已完成（distributed 仍 Deferred）"改"local + distributed 均已落地（Stage 39 Phase 3）"；§5 :308 行——"unaligned checkpoint 未实现（Stage 43 Phase 2–4 进行中）"改为"已实现（Stage 43 Phase 2–3 + 背压 E2E）"，与 :193 自洽。
- **验证**：grep 核对（`registerDistributedAbortHandler` / `effectiveMaxConcurrent` / unaligned 描述）与 live code 一致；`check-doc-links.mjs --strict` exit 0（0 errors，3 warnings 为 credential plan 既有噪音与本变更无关）；变更仅限 `component-roadmap.md`（git diff 6+/6-）。纯文档计划按 plan Closure Gates 声明跳过 mvnw 构建验证。
- **Doc-sync**：`component-roadmap.md`（本 plan 目标文件）；`ai-dev/backlog/nop-stream-invariant-loop-roadmap.md` 追加 ✅ 收口条目（P1-18-02 → closed）；本 log 条目。checkpoint-design.md 不修改（Non-Goal，权威状态已同步）。
- **Closure**：Phase 1 Status completed + 全部 item/Exit Criteria 勾选；`Plan Status: active → completed`（Completed: 2026-08-13）；`check-plan-checklist.mjs --strict` exit 0；独立 closure audit（fresh session）PASS，evidence 在 plan Closure 段。


### Plan `2026-08-13-1930-2` closure：G52 liveness 停滞检测语义修正收口 + 独立 closure audit PASS

- **本 run 动作**：Plan Closure Gates 15/15 + 两 Phase 全部 item/Exit Criteria 勾选 + `Plan Status: active → completed`（Completed: 2026-08-13）；`check-plan-checklist.mjs --strict` exit 0；roadmap 追加 ✅ 收口条目（AR-01 → closed）；checkpoint-design §8.1.3 语义节落档；本 log 收口记录。
- **独立 closure audit（fresh session `ses_0042c0939ffeObFHG9zczBdtNO`，read-only）verdict = PASS**：逐项对照 live 代码——Phase 1 裁定与实现（lastActivityTime :122 / 循环顶 tick :787 / livenessValue 角色分流 :266-273 / COMPLETED remove :903-910 / 停滞分支 :1059 保留）、Phase 2 测试（completed-stale / idle-fresh / core 活性-进度解耦 / E2E 真实 RPC 控制面）、文档（checkpoint-design §8.1.3 / roadmap / log）、Deferred 分类诚实性（AR-03/AR-04 = P2 watch-only 无降级）；Anti-Hollow：两调用链（心跳→liveness→detectFailures→recovery；RunningTask finally→COMPLETED report→remove）运行时连通（E2E + 单测实证），scan-hollow 双模块 0 发现；mjs all exit 0（注册表重钉 501/502/247/285/304/310/465/474 + 892/960 与 live 行号一致）；checkstyle 变更行零新增命中。4 项 minor 发现全部非阻塞（工作树待提交 / roadmap 措辞时序 / AR-04 维持 P2 / import-order 工具为既有噪音）。
- **验证**：全量 `./mvnw test -pl nop-stream -am -T 1C` 8616 tests / 0 failures / 0 errors（core 1479 / runtime 837）；mjs / hollow / doc-links / check-plan-checklist 全 exit 0。
- **Doc-sync**：checkpoint-design.md §8.1.3（语义表 + COMPLETED 排除 + 门控裁定 + 可观测行为 + 测试清单 + 不变式关联）；roadmap AR-01 ✅ 收口节；本 log 条目（执行 + 收口两条目）。

### Plan `2026-08-13-1930-2` 执行：G52 per-task liveness 停滞检测语义修正（AR-01 P1）全 2 Phase 落地

- **Phase 1 Decision（混合方案 = 任务线程活性 + TM 墙钟 + COMPLETED 排除）**：停滞判定的 liveness 信号与数据进度解耦。**MIDDLE/SINK** 上报任务线程循环活性（`StreamTaskInvokable.lastActivityTime`，`processInputGate` 循环顶每次迭代刷新——含 AR-02 空闲返回路径，空闲健康任务 ~250ms 一拍；线程卡死则停止打点老化）；**SOURCE/SELF_CONTAINED** 上报 TM 侧墙钟（阻塞 run 循环无循环边界钩子，空闲源永不老化；源挂死由 node lease / FAILED 报告 / 心跳缺口兜底——文档化限制）；**COMPLETED 排除** = `reportTaskStatus` COMPLETED 时 `subtaskLiveness.remove(key)`（任务已移出 runningTasks 无心跳重加），FAILED 等终态报告记录报告到达时刻。拒绝纯墙钟（心跳存活任务永不超时 → 真停滞静默丢失，plan 硬约束明文禁止）与纯 COMPLETED 排除（不解决空闲面）。**门控裁定**：停滞驱动恢复保持无条件启用（不挂 autoRecoverOnFailedReport——RpcDistributedExecutor 已关 FAILED-report 恢复，停滞路径是其 per-task 恢复唯一触发器；修复后停滞只在真失败时触发，与 FAILED 路径意图收敛），测试钉 `stallDetectionFiresEvenWhenAutoRecoverOnFailedReportDisabled`。
- **Phase 1 Fix**：`StreamTaskInvokable` 新增 `lastActivityTime`/`getLastActivityTime()`/`markActivity()`（循环顶 + 四角色 invoke 起点）+ `TaskManager.heartbeat` 按角色计算 liveness（`livenessValue`）+ `JobCoordinator.reportTaskStatus` COMPLETED 排除/其余 put(now) + 全链 javadoc 同步（TaskProgress/IStreamCoordinatorRpcService/subtaskLiveness/detectFailures）。类别清扫（grep 全仓）：`subtaskLiveness`/`lastProgressTime`/`taskStallDetected`/`taskTimeoutMs` 消费点全部收敛到新语义，零遗留旧语义依赖；wiring-registry 8 行 + output-contract registry 2 行重钉（StreamTaskInvokable 编辑行号漂移）。
- **Phase 2 先红后绿（实测）**：① `TestJobCoordinatorPerTaskFailure.completedTaskWithStaleProgressDoesNotTriggerStallRecovery`（COMPLETED 报告带陈旧进度 → pre-fix recovery 触发红 / post-fix 排除绿）+ `stallDetectionFiresEvenWhenAutoRecoverOnFailedReportDisabled`；② `TestTaskManagerLivenessAndReporting.idleSinkTaskHeartbeatReportsFreshAliveness`（空闲 SINK 心跳新鲜度，pre-fix 上报冻结进度红（800ms 老化 > 300ms 断言窗）/ post-fix 活性新鲜绿）；③ core `TestStreamTaskInvokableActivityLiveness`（活性随空闲循环推进 + 数据进度冻结，pre-fix 编译级红 = 新 API）；④ **E2E `TestRpcDistributedExecutorE2E.idleJobWithNoDataIsNotKilledByStallDetection`**——真实 RPC 控制面（StreamControlRpcServer/Proxy）空闲源作业，post-start 配置 taskTimeoutMs=8s（>5s 心跳）+ maxRestarts=1，两次手动 detectFailures（t≈9s/18s）：**pre-fix 首拍即 recovery（restartCount=1 红）/ post-fix 双拍零恢复作业保持 RUNNING 绿**；既有 `staleLivenessTriggersRecoveryViaDetectFailures`（真停滞）保持绿。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS 0 failures（core 1479 / runtime 837，全 reactor 8616 tests 0 失败）；mjs `all` exit 0（注册表重钉后）；JUnit 门禁类抽查全绿。
- **Doc-sync**：`checkpoint-design.md` §8.1.3 新增 per-task liveness 停滞检测语义节（信号语义表 + COMPLETED 排除 + 门控裁定 + 可观测行为 + 测试清单 + 不变式关联）；roadmap backlog AR-01 状态流转（Phase 3 closure 时最终收口）；本 log 条目。

### Plan `2026-08-13-1930-1` 执行：P1-21-01（MapState 容器值 JSON 恢复重物化）+ P1-09-01（ChannelState 静默丢弃日志化）全 2 Phase 落地

- **Phase 1 Decision = Option B（递归类型感知容器包装器）**：恢复侧无既有元素类型来源（valueClass=raw List.class、JSON 无 @type、`CepOperator.inputSerializer` 生产 null）→ 类型来源必须在**快照侧建立**。Option A（stateInfo 记录 `mapValueElementType` 单字段）无法表达嵌套容器（List<Map<String,List<X>>>），且 RocksDB 快照侧只有字节（无 live 容器对象可取首元素 class）→ 必须 put 时嵌类型信息，退化为 B 的机制而更弱。裁定 **Option B**：`ContainerValueCodec`（core，`io.nop.stream.core.common.state.backend`）在**所有序列化点**（Memory snapshotMapState + RocksDB put/putAll）把容器值（List/Map，嵌套递归）包装为 `{nopContainerType: list|map, nopElementType?, nopItems|nopEntries}`（per-level 元素类型 = live 首元素 class）；**所有恢复/读取点**（Memory `deserializeValue` 容器门 + RocksDB `RocksDBSnapshotSerDe.restoreMapState` + `RocksDBMapState.get()/collectMap()` 运行时读取 + applyMigration）按元素类型递归重物化（JSON-native map → `JsonTool.parseBeanFromText` 成 bean、标量重定类型如 Integer→Long、嵌套包装递归）。无包装 legacy 快照 → 降级 JSON-native 形态 + `LOG.warn`（guide #24 不静默损坏；`hasJsonNativeContainerElements` 区分 live 类型元素不误报）。双后端同一快照格式（探针实测包装器经 `JsonTool.parseMap` 键完好 + `parseBeanFromText` 重物化 Event/Long 成立）。
- **Phase 1 先红后绿（实测）**：core `TestMemoryStateSerDeContainerValueRestore` 5 用例 red（4 CCE：List<Event> / Map<String,Event> / 嵌套 List<List<Event>> / List<Long> Integer→Long）+ CEP `TestCepCheckpointRestoreE2E.testE2EElementQueueSurvivesJsonStorageLayerRoundTrip` red（**生产路径 CCE**：`onEventTime processEvent` LinkedHashMap→DataBeanEvent）→ post-fix 全绿。RocksDB `TestRocksDBSnapshotRestore` +3（运行时 put/get 类型保持 + 自身 JSON round-trip + **跨后端同一 JSON 快照 Memory/RocksDB 恢复一致**）21/21 绿。
- **CEP 存储层测试边界裁定**：SharedBuffer/NFAState 族非 @DataBean → 整快照 JSON round-trip 在序列化点即抛 `ERR_JSON_ONLY_DATA_BEAN_IS_SERIALIZABLE`（探针实测 5 类全 FAIL；既有 P2-INV-6/P2-TST-9 backlog 面）→ CEP 测试仅对 `eventQueuesStateName` state info 做存储层 JSON round-trip（与 storageType=local 同一 JsonTool 机制），其余 keyed states 原样保留；restore 经真实路径（restoreState → applyPendingRestoreState → MemoryStateSerDe.restoreMapState → CepOperator.open 消费）非内存直传。
- **Phase 2 Fix（P1-09-01）**：`ChannelState.fromSerializableForm` 四类跳过全部 `LOG.warn`（畸形 channelIndex 带 key / 非 List 带 value 类名 / 非 Map item 带类名 / decode 失败带 throwable 作末参数 per-element 隔离规则）；删除"observable via logging in the codec path"虚假注释（`StreamElementCodec.decode` 保持 fail-fast 抛错者角色，日志在跳过点记录）；`TestCheckpointSerDeChannelState` +3 负面测试（Logback ListAppender 实证日志留证：畸形索引 + 损坏 payload 跳过且其余记录恢复 + 非 List/非 Map 双旁路）6/6 绿。
- **类别清扫（grep 实证）**：`snapshotMapState`/`restoreMapState` 调用点仅双后端各一（全部覆盖）；`deserializeValue` 12 处全部经容器门；`RocksDBValueSerDe.deserializeMap` 全仓零调用（死代码确认，未引作修复依据）；其余 `RocksDBValueSerDe.deserialize` 消费点（ValueState/ListState/aggregating 族）元素类型来自 descriptor 不在 MapState 容器值 scope；`fromSerializableForm` 同形态静默路径——`CheckpointSerDe` 恢复路径已全 LOG.warn（:111/:240 等实证），HeapInternalTimerService 面 AR-22 fail-fast 已修，core/runtime main 零空 catch 块遗留。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS 0 failures（runtime 833 含 +3）；`./mvnw clean install -DskipTests -pl nop-stream -am -T 1C` SUCCESS；`check-nop-stream-invariants.mjs all` exit 0；repo checkstyle.xml（真实门禁，19 规则）4 模块 0 违规（`checkstyle:check` 默认 CLI 11K+ 条为 Sun 默认规则噪音 = 仓库级既有债，本次变更行对比基线零新增命中）；`scan-hollow` 4 模块 exit 0 零发现；`check-doc-links.mjs --strict` 0 errors。
- **Doc-sync**：`checkpoint-design.md` §2.5 keyed state 行补容器值重物化语义（P1-21-01）+ §2.11.4 补跳过语义可观测与 best-effort 跳过 vs 版本漂移 fail-fast 分工（P1-09-01）；roadmap 追加 ✅ 收口条目；本 log 条目。

### Plan `2026-08-13-1615-1` closure：AR-22（P0）全 3 Phase 收口 + 独立 closure audit PASS

- **本 run 动作**：Phase 3 全量验证 + 收口——全量回归 `./mvnw test -pl nop-stream -am -T 1C` **2944 tests / 0 failures / 0 errors / 10 skipped**（core 1478 / runtime 830 / cep 327 / rocksdb 86 / 其余 0 失败）；JUnit 门禁类全绿（8 类抽查 84 tests / 0）；mjs `all` exit 0；`check-doc-links.mjs --strict` exit 0（0 errors）；`check-plan-checklist.mjs --strict` exit 0；Closure Gates 13/13 + 三 Phase 全部 item/Exit Criteria 勾选 + `Plan Status: active → completed`（Completed: 2026-08-13）；roadmap AR-22 → done + ✅ 收口条目；adjudication-table §18 裁定落档；checkpoint-design §2.1 timer state 行补重物化语义。
- **output-contract 注册表重钉**：`WindowOperator.setKeyType` 接线（6 行）致 WindowOperator 发射点漂移 1192/2022 → **1198/2028**（mjs V4/V5 红 → 重钉后 all 绿），registry `updated` 字段留痕。
- **独立 closure audit（fresh session `ses_005a80178ffeKcoAyqST2eRpY4`）verdict = PASS**：逐项对照 live 代码——Fix（`rematerializeEntry` 双表接入 + 守卫 + `ERR_STREAM_STATE_ERROR` fail-fast）、接线顺序（`open()` :471 setKeyType 先于 :495 restoreTimers）、测试断言层绑定（restore→fire，非 DTO 层）、E2E 真实 JSON 路径（`CheckpointSerDe` + `JsonTool`，run2 零元素）、类别清扫、Deferred 分类（无静默降级）。Anti-Hollow：`restoreState → open → setKeyType → restoreTimers → advanceWatermark → onEventTime → setCurrentKey → getWindowContents → emitWindowContents` 运行时连通（E2E 绿 + 代码追踪）；scan-hollow findings 集与 `34aed42c1` 基线精确一致（touched 文件零命中）。命令实测：core 11/11 + runtime 4/4 + mjs all exit 0 + doc-links 0 errors。
- **checkstyle 复核**：模块既有基线 ~11340 条 pre-existing 违规（已知噪音，前序 plan 落档，mission `|| echo 'lint not configured'` 兜底）；本次变更集新代码行零违规（javadoc 行宽/@return 逐处收口后核验无命中）。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（复跑两遍稳定）；mjs / doc-links / check-plan-checklist 全 exit 0。
- **Doc-sync**：`checkpoint-design.md` §2.1（重物化语义）+ adjudication-table §18（P0 裁定）+ roadmap（AR-22 done + ✅ 收口节）+ 本 log 条目。

### Plan `2026-08-13-1615-1` 执行 Phase 1-2（AR-22 P0）：Timer 键 JSON 恢复重物化修复 + 先红后绿

- **Phase 1 裁定（Decision）= P0**：AR-22 与 AR-01 同机制（JSON round-trip 数值键类型漂移）同后果（静默状态丢失）同触发面（恢复后 timer 回调 `setCurrentKey` 访问键控状态 miss），走 operator state 面 `internal-timers`（`deserializeKey` 修复不覆盖）。落档：adjudication-table.md 新增 §18 + roadmap backlog AR-22 条目状态流转。
- **Phase 1 边界发现（复现测试暴露，已落档）**：非 @DataBean POJO 键在 JSON 持久化序列化点即抛 `ERR_JSON_ONLY_DATA_BEAN_IS_SERIALIZABLE`（`JsonSerializer.java:157-158` onlyForDataBean 守卫）→ checkpoint 响亮失败非静默丢失；测试 POJO 须 `@DataBean` 标注才能走 JSON round-trip（red 首跑 CCE/异常暴露，修正后二次 red 达成预期断言失败）。
- **Phase 1 复现（先红，实测）**：`TestHeapInternalTimerServiceSnapshotRestore` +2——`testLongTimerKeyJsonRoundTripFiresWithLongKey`（red：`ClassCastException: Integer cannot be cast to Long`，fired key 漂移为 Integer）+ `testPojoTimerKeyJsonRoundTripFiresWithPojoKey`（red：fired key = `LinkedHashMap {id=k1, seq=42}`）；`TestTimerCheckpointRestoreE2E` +1（Long 键 + `CheckpointSerDe`/`JsonTool` JSON 持久化 E2E，red：`expected: <1> but was: <0>` 无输出）。
- **Phase 2 Fix（落点 A = HeapInternalTimerService 层）**：`HeapInternalTimerService` 新增 `keyType` 字段 + `setKeyType(Class)` + `restoreTimers` 内 `rematerializeEntry`（机制对齐 `MemoryStateSerDe.deserializeKey`：`keyType != Object.class` 且 `!keyType.isInstance(key)` → `JsonTool` 重物化，失败抛 `ERR_STREAM_STATE_ERROR`，null 键守卫，无静默跳过 guide #24）；`WindowOperator.open()` :464 后 `internalTimerService.setKeyType(keyClass)`（keyClass final 字段双后端通用，restore-before-open 延迟应用模式零改动）；构造点零涟漪（41 处既有构造点不受影响）。
- **Phase 2 验证（先红后绿）**：core 11/11 绿（含新增 fail-fast 用例 `testTimerKeyRematerializationFailureFailsFast`）+ runtime `TestTimerCheckpointRestoreE2E` 4/4 绿（既有 String 键 3 用例不改写）。
- **Phase 2 类别清扫（grep 实证）**：`restoreTimers`/`snapshotTimers`/`TimerSnapshot`/`fromSerializableForm`/`internal-timers` 全仓调用点仅 WindowOperator + CheckpointSerDe 路由 + HeapInternalTimerService 内部 + 测试；双后端 timer 面共享 `deserializeOperatorState`（storage 层后端无关，RocksDB 仅替换键控面 serde = AR-01 面）；CepOperator 免疫（`registeredEventTimeTimers` = Set\<Long\> 时间戳非键控）。
- **验证**：`./mvnw test -pl nop-stream/nop-stream-core -Dtest=TestHeapInternalTimerServiceSnapshotRestore` 11/0；`./mvnw test -pl nop-stream/nop-stream-runtime -Dtest=TestTimerCheckpointRestoreE2E` 4/0；`./mvnw install -pl nop-stream/nop-stream-core -DskipTests` 后 runtime 编译通过。
- **Doc-sync**：`checkpoint-design.md` §2.1 timer state 行补重物化语义（AR-22）+ adjudication-table §18 + roadmap AR-22 条目（Phase 3 收口时最终流转）。

### Plan `2026-08-13-1243-3` closure：跨模块契约修复收口（Phases 1-2 landed full-green + Phase 3 按预设路径取消）+ 独立 closure audit（fresh session）

- **本 run 动作**：Plan Closure Gates 11/11 + 三 Phase 全部 item/Exit Criteria 勾选 + Phase 3 `blocked → cancelled`（**scope 变更 + successor 登记**，非静默搁置）+ `Plan Status: active → completed`（Completed: 2026-08-13）；`check-plan-checklist.mjs --strict` exit 0；`check-doc-links.mjs --strict` exit 0；roadmap 追加 ✅ 收口条目 + Follow-up Backlog 新增 **P1-01-01 successor 条目**（触发条件 = 用户/owner 批准迁移方案）。
- **独立 closure audit（fresh session，CLOSURE_SCRIPT_CHECK FAIL 后独立复检）verdict = APPROVE**：逐项对照 live 代码——Phase 1（`NopStreamOffsetBackingStore` `clearConnector` :101 + `configure` :123-126 / `ensureBound` :214-221 抛 `ERR_DEBEZIUM_CONNECTOR_NAME_REQUIRED`（`_default_` 回退移除）；`DebeziumCdcSourceFunction` `newFreshOffsetStore` :273-276（两 fresh 分支 :241/:250 均走）+ `resolveConnectorName` :279-286 抛 `ERR_STREAM_CONFIG_ERROR`；7 个新测试名 grep 命中）；Phase 2（`checkpoint-design.md:101` §2.4 重叠 barrier 逐 id 对齐 + :179 §2.5 spec-only/未实现 + `nop-stream/README.md:7` DISTRIBUTED 经 `IStreamExecutionDispatcher`）；Phase 3（grep 实证 rocksdb 17+ 类仍驻留 core 命名空间 = 迁移零执行与取消裁定一致；影响面清单在案作 successor 输入）。Anti-Hollow：调用链 `initializeState` → `newFreshOffsetStore` → `clearConnector`/`forConnector` 运行时连通，三处 fail-fast 显式抛异常无 `_default_` 静默绑定；Deferred 分类检查：P1-01-01 = 显式 scope 变更 + successor 所有权（非降级），P2-INV-7 / AR-17/18/19/21 维持既存 backlog watch-only。
- **Phase 3 裁定 = 取消（预设路径执行）**：全仓（logs / plans / git）仍无人工批准记录（第 1/2 次复跑 + 本次审计三次确认）→ 按 plan Phase 3 自 draft 起预设的「未批准 → 取消 + 记录 scope 变更 + successor plan」路径处置；迁移方案 + 影响面清单（main 17+ 类 + test 13 类 + runtime test scope 7 引用测试类）保留在 plan Phase 3 Decision 与 roadmap backlog P1-01-01 条目，**待用户/owner 批准后由 successor plan 执行**（roadmap 工作项 `todo`（待人工批准））。
- **验证**：Phase 1-2 测试证据沿用第 2 次复跑 full-green（`./mvnw test -pl nop-stream,nop-message/nop-message-debezium -am -T 1C` BUILD SUCCESS 0 failures；`TestDebeziumCdcCheckpoint` 12/0 + `TestNopStreamOffsetBackingStore` 7/0）；doc-links / hollow-scan exit 0；本次零代码变更（仅 ai-dev 文档）。
- **Doc-sync**：No owner-doc update required（Phase 3 未执行迁移，module-groups/source-anchors 由 successor plan 迁移后同步；Phase 1/2 文档已在上次执行同步）。

### Mission driver 复跑 plan `2026-08-13-1243-3`（第 2 次复跑）：Phases 1-2 复核 full-green（含 cancel TOCTOU 竞态修复）；Phase 3 人工确认门仍开（不执行）

- **Phase 1/2 复核**：`./mvnw test -pl nop-stream,nop-message/nop-message-debezium -am -T 1C` BUILD SUCCESS 0 failures/0 errors（`TestNopStreamOffsetBackingStore` 7/0、`TestDebeziumCdcCheckpoint` 12/0、`TestDebeziumCdcSourceFunction` 10/0 等；全 nop-stream 组 + nop-message-debezium 绿）。
- **新竞态修复（Phase 1 代码）**：复跑中 `TestDebeziumCdcCheckpoint.testFirstRunRestartStartsFromBeginning` 暴露 **`DebeziumCdcSourceFunction.cancel()` 的 check-then-act TOCTOU 竞态**——`if (source != null) source.stop()` 对 volatile `source` 读两次，run 线程 `finally` 中 `source = null` 可插在检查与调用之间 → NPE。修复：`cancel()` 与 `truncateForDrain()` 改为先局部快照 `ICancellable sub` / `DebeziumMessageSource msgSource` 再判空调用（`DebeziumMessageSource.stop()` 为 `synchronized` 幂等，双线程并发 stop 安全）。修复后单模块 24 tests / 0 failures + 全量复跑绿。
- **验证门禁**：`check-doc-links.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-stream-connector-debezium --severity high` exit 0；`check-plan-checklist.mjs --strict` exit 0（Phase 3 未勾选项仅 warning，plan 未 completed 属预期）。
- **Phase 3（P1-01-01）裁定 = 维持 blocked，本次不执行**：全仓（logs / plans / git）仍无人工批准记录——`人工确认门通过` 项未勾选。按 plan 硬门禁「在获得用户/owner 明确批准前不得开始执行」，本次 run 不执行命名空间迁移；plan 保持 `active`、不关闭，roadmap 工作项维持 ❌。
- **后续触发**：用户批准迁移方案（影响面清单已 grep 实测，见 plan Phase 3 Decision）后，重新运行 mission-driver 即可从 Phase 3 断点继续。

### Plan `2026-08-13-1243-3` 执行 Phase 1（AR-03 P1）：Debezium offset 注册表生命周期 + Phase 2（P1-DOC-01/02/03）：文档契约同步

- **Phase 1 Decision（`_default_` 桶语义 = fail-fast）**：未配置 name 即抛异常并附 connector 配置指引（audit 首选建议，与 No-Silent-No-Op Rule #24 一致）；共享桶选项（Option B）显式拒绝（同 JVM 并发未命名 pipeline 静默互相覆盖 offset 无法可靠检测，留痕设计文档）。异常类型裁定：store 侧（nop-message-debezium，pom 无 nop-stream-core 依赖）用模块本地异常 `NopException(DebeziumErrors.ERR_DEBEZIUM_CONNECTOR_NAME_REQUIRED)`（新增 ErrorCode `nop.message.debezium.connector-name-required`）；源函数侧（connector-debezium，有 core 依赖）用 `StreamException(ERR_STREAM_CONFIG_ERROR)`。
- **Phase 1 Fix（首跑显式清理）**：`DebeziumCdcSourceFunction.newFreshOffsetStore()`——两个 fresh 分支（state==null :232-236 与 raw==null :238-244）先 `clearConnector(name)` 再 `forConnector(name)`；恢复分支（raw 非 null）不清理保留 checkpoint 恢复语义。采用"首跑显式清理"变体而非改 `forConnector` fresh 语义（公开 API 契约：同连接器共享 backing map，`testConnectorNameRegistrySharesDataAcrossInstances` / `testConfigureWithWorkerConfigBindsToRegistry` 保持绿）。
- **Phase 1 Fix（三处 fail-fast 回退点）**：`DebeziumCdcSourceFunction.resolveConnectorName`（null/empty → `StreamException(ERR_STREAM_CONFIG_ERROR)` + 配置指引 detail）+ `NopStreamOffsetBackingStore.configure`（WorkerConfig originals 无 name → `NopException(ERR_DEBEZIUM_CONNECTOR_NAME_REQUIRED)`）+ `ensureBound`（data==null 且无 connectorName → 同上 NopException，杜绝 `_default_` 静默绑定绕过源函数门）。
- **Phase 1 Fix（clearConnector 语义文档化）**：javadoc 更新为"首跑路径清除陈旧 entry（崩溃/redeploy/复用名的上一运行）+ 测试隔离为次要用途"。
- **Phase 1 测试（先红后绿）**：`TestNopStreamOffsetBackingStore` +2（`testConfigureWithoutNameFailsFast` / `testEnsureBoundWithoutConfiguredNameFailsFast`，store 侧断言模块本地异常类型）；`TestDebeziumCdcCheckpoint` +5（`testFirstRunStateNullDoesNotResumeFromStaleOffsets` / `testFirstRunRawNullDoesNotResumeFromStaleOffsets` 两 fresh 分支各一用例 + `testUnnamedConnectorFailsFast` / `testUnnamedConnectorFailsFastOnRawNullBranch` + **E2E `testFirstRunRestartStartsFromBeginning`**：run1 mock 引擎消费产生 offset → 新作业同连接器名首跑 → run2 独立 `emittedRecordKeys` 集合断言首条发射 index==0（从起点重新 snapshot，不跳过数据））。red 证据：修复前 7 条失败（store 2 + connector 5）→ 修复后全绿。
- **Phase 1 既有用例竞态修复**：`testCdcCheckpointKillRecoverNoDuplicates` 在 `-T 1C` 并行调度下暴露 snapshot→cancel 窗口竞态（snapshot 后、cancel 前发射的事件被恢复重放 → 假重复）——改为 **cancel+join 后**再 snapshot（捕获最终 committed offset，断言语义不变：kill/recover 零重复 key）。
- **Phase 1 验证**：`./mvnw test -pl nop-stream,nop-message/nop-message-debezium -am -T 1C` **BUILD SUCCESS 0 failures**（core 1473 / runtime 829 / cep 327 / rocksdb 86 / connector-debezium 24(1 skipped) 等）；`TestDebeziumCdcCheckpoint` 12/0 + `TestNopStreamOffsetBackingStore` 7/0。
- **Phase 1 Doc-sync**：`connector-design.md` §5.4 补 "registry 生命周期（AR-03 修复）" 段——首跑清理 + 三处 fail-fast + Option B 拒绝留痕 + `clearConnector` 语义（不再仅测试使用）。
- **Phase 2 Fix（P1-DOC-01）**：`checkpoint-design.md` §2.4 ALIGNING 行——删除"重叠 barrier 抛 `ERR_STREAM_CHECKPOINT_ABORTED`（line 381）"，改为"重叠 barrier id 按 in-flight 集合逐 id 对齐，迟到/被 abort 的 barrier 被丢弃（Stage 45 多 epoch 重构，§2.8.1 D1；`ERR_STREAM_CHECKPOINT_ABORTED` 在 InputGate 仅 import，唯一抛出点在 GraphModelCheckpointExecutor abort 路径）"。
- **Phase 2 Fix（P1-DOC-02）**：`checkpoint-design.md` §2.5 watermark state 行标注 **spec-only / 未实现**——`TaskEpochSnapshot`/`TaskStateSnapshot`/`EpochManifest` 无 watermark/idle 字段，checkpoint 两包对 watermark 零引用；唯一持久化点 = `HeapInternalTimerService.java:238` task 级 currentWatermark（随 timer state，非 per-channel）；恢复后水位归零重爬（对应 backlog [P2-INV-7]）；§2.5 其余行核对一致（channel state/metrics 等与 live 相符）。
- **Phase 2 Fix（P1-DOC-03）**：`nop-stream/README.md:7`——"DISTRIBUTED 规划中" → "LOCAL 经 `GraphExecutionPlan` + `TaskExecutor`；DISTRIBUTED 经 `IStreamExecutionDispatcher`（`EmbeddedDistributedExecutor` / `RpcDistributedExecutor`，含 Stage 39-42 跨 JVM 能力已落地）；`RuntimeTopology` 仍概念阶段（0 Java 引用），不代表分布式能力缺失"。
- **Phase 2 验证**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 errors）；两 executor 类 `implements IStreamExecutionDispatcher` grep 实证；InputGate `ERR_STREAM_CHECKPOINT_ABORTED` 仅 import（:38）实证；纯文档零代码变更。
- **Phase 3（P1-01-01）状态**：**人工确认门未过**——影响面清单已 grep 实测（rocksdb main 17 类 + test 13 类 + runtime test scope 7 类引用；pom :67-72 test scope 确认；`WindowedStreamImpl.java:162-165` 反射类名 P2-01-03 不列入），迁移方案已向用户提交待批准；批准前 Phase 3 保持 `blocked`，plan 不关闭。


### Mission driver 验证：plan `2026-08-13-1243-2` 收口后构建复核（full-green verification，零代码变更）

- **typecheck**：`./mvnw compile -pl nop-stream -am -q` PASS。
- **build**：`./mvnw clean install -DskipTests -pl nop-stream -am -T 1C` BUILD SUCCESS（nop-stream 组 18 模块全 SUCCESS）。
- **lint**：`./mvnw checkstyle:check -pl nop-stream -am` 9164 条既有违规全部位于 `nop-api-core`（ConvertHelper / SysConverterRegistry 等，本次变更集 `git diff HEAD~2..HEAD` 仅触及 nop-stream-core/flow + nop-xdefs + fraud-example）——pre-existing 噪音，`|| echo 'lint not configured'` 兜底语义生效。
- **test**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS——**2935 tests / 0 failures / 0 errors / 10 skipped**（full-green verification）。
- **收口**：working tree 开工前即 clean（EXECUTE 已分批提交），本次零代码变更零新提交内容；full-green 达成，记录在案。

### Plan `2026-08-13-1243-2` closure：flow DSL 契约修复三 Phase 收口 + 独立 closure audit（fresh session）

- **本 run 动作**：Plan Closure Gates 10/10 + 三 Phase 全部 item/Exit Criteria 勾选 + Phase Status ×3 `planned → completed` + `Plan Status: active → completed`（Completed: 2026-08-13）；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-stream-flow --severity high` exit 0（0 findings）；`check-nop-stream-invariants.mjs all` exit 0；`check-doc-links.mjs --strict` exit 0；roadmap 追加 ✅ 收口条目 + AR-12 标 done（fail-fast 天然覆盖）+ AR-11 watch-only 维持；审计文件 `2026-08-13-0805-multi-audit-nop-stream-invariant-loop.md` 已 `closed`（幂等跳过）。
- **独立 closure audit（fresh session，read-only）verdict = PASS**：逐项对照 live 代码（见 plan Closure 段 evidence）。Anti-Hollow 检查：edge HASH+keyExpr 经 `applyEdgePartition` 实际调用 `keyBy`（`hashWithKeyExprIntoWindowIsAppliedViaKeyBy` 断言 WindowedStream）；checkpoint 六字段经 `applyCheckpointConfig` 映射到 core setter（语义断言 3 测试）；fail-fast 分支全部有测试断言可达（edge 8 参数化 + 窗口 5 参数化 + registry/union/sideOutput/bean）。
- **Follow-up**：无 remaining plan-owned work；P2 批次（fraud 死文件 XDSL-1~4、AR-10 死错误码、AR-11、P2-09-02b core/runtime 裸异常）维持 backlog 触发条件不变。

### Plan `2026-08-13-1243-2` 执行 Phase 3（P1-09-02 P1）：flow 模块错误码体系接入

- **Fix**：`NopStreamErrors` 新增 15 个 `ERR_STREAM_*` 码（duplicate-id / required-attr / required-body / ref-unknown / not-implemented / upstream-type / upstream-null / upstream-not-stream / edge-hash-key-expr-required / edge-hash-redundant / edge-key-expr-without-hash / edge-partition-unsupported / edge-attr-unsupported / window-attr-unsupported / bean-not-found / bean-type-mismatch = 16）+ 12 个 ARG_* 常量；复用既有 `ERR_STREAM_NULL_ARG`/`ERR_STREAM_INVALID_ARG`/`ERR_STREAM_CYCLIC_JOB_GRAPH`/`ERR_STREAM_TYPE_MISMATCH` 系。
- **Fix**：flow 模块（main）裸异常清零——grep `throw new (IllegalArgumentException|IllegalStateException|UnsupportedOperationException)` **0 命中**（修复前 58 处：`StreamModelDslBuilder` 24 + `AdvancedTransforms` 23 + `builder/functions/*` 6 + resolver 2×2 + `EvalActionKeySelector` 1）；全部迁移为 `StreamException(ERR_STREAM_...)` + `.param(...)`（错误码语义逐场景匹配，未盲取死码）；`NopException.param()` 返回类型致 `beanNotFound`/`beanTypeMismatch` helper 返回 `NopException`（编译期修正）。
- **Fix**：既有异常断言迁移——`TestAdvancedTransforms`（4× IAE :97/:142/:314/:332 + 2× UOE :350/:364 → StreamException + 错误码断言；:125/:188 既有 StreamException 断言未动；:268 `Exception.class` 解析器拒绝测试维持）；`TestStreamModelDslBuilderFailFast`（7× UOE → StreamException + `nop.err.stream.not-implemented`）；`TestStreamModelDeltaFailFast`（1× UOE 同上）；`TestBeanFunctionResolver`（2× IAE → `bean-not-found`/`bean-type-mismatch` + 消息保留）。
- **Proof**：`TestStreamErrorCodeContract`（4 用例）——not-implemented registry / cyclic-job-graph / duplicate-id 模型层拒绝（KeyedList `nop.err.commons.list-not-allow-duplicate-key`，builder 级 dup 检查为防御性死路径，grep 验证模型 API 不可构造重复）/**端到端**：调用方 try/catch `StreamException.getErrorCode()` 程序化分支（multi-upstream → `invalid-arg`）。`getErrorCode()` 断言全部非空（`NopException.getErrorCode()` 返回 code 字符串；`getMessage()` 渲染 `desc=` 模板 + params）。
- **验证**：flow 全量 74/0 绿（51 基线 + 23 新增）；nop-stream 组全量 0 失败；mjs invariants all exit 0；scan-hollow flow exit 0（0 findings）。
- **Doc-sync**：No owner-doc update required（error-handling.md §模式二 已登记 StreamException 模块异常类用法）。

### Plan `2026-08-13-1243-2` 执行 Phase 2（P1-XDSL-6 P1）：checkpoint 六字段实现 + 窗口策略属性 fail-fast

- **Fix**：`applyCheckpointConfig` 消费六字段——barrierAlignmentTimeout / maxConsecutiveCheckpointFailures / storageType / jobId / pipelineId / `<storageConfig>`（KeyedList → `Map<String,String>`）映射到 core `CheckpointConfig` setter（setter 全部存在）；**非默认值判定钉死为 `!= xdef 默认`**（30000 / 3 / "local"；jobId/pipelineId null 检查；storageConfig 非空检查），无 `> 0` 守卫（声明 0 不再静默忽略，边界测试在案）。
- **Fix**：`buildWindow` 对 strategy 级 `triggerId`/`allowedLateness`/`accumulationMode` 非默认值 fail-fast（`ERR_STREAM_WINDOW_ATTR_UNSUPPORTED` 含 transform id + 属性名）；**AR-12 天然覆盖**：window 节点级 `<allowedLateness>`/`<triggerId>` 子元素走同一校验（`failFastOnUnsupportedWindowNodeAttrs`）→ roadmap AR-12 标 done。
- **xdef 裁定记录（plan 基线事实修正）**：`stream.xdef:47` 的 `triggerId="!string"`（mandatory）使"triggerId 未设置"默认路径不存在——plan 假定 triggerId 可选（"triggerId 未设置" 为默认）与 live xdef 矛盾；执行时裁定 **minimal relaxation：`!string` → `string`**（属性定义不变，仅去除 mandatory 标记；生成类零变更，验证器层面生效）。不放松则 fail-fast 对每个 strategy 必触发 → window DSL 整体不可构建，违反 Phase 1 exit criteria（WindowedStream 构建成功断言）与 plan 自身决策矩阵。本次执行日志留痕（plan Phase 2 亦已登记说明）。
- **Fix**：`TestAdvancedTransforms` 5 处 fixture 删除 `triggerId="t"`（回默认路径；:115/:180 的 "nop-stream-runtime" 断言依赖 buildWindow 成功、:138 依赖 buildWindow 的 IAE 先于 bean 检查——删除后断言目的保留）；`test-smoke*.stream.xml` 无 checkpoint/窗口属性无需修改。
- **测试**：`TestStreamCheckpointAndWindowContract`（8 用例）——六字段语义断言（含 storageConfig 结构映射）/ 0 值边界（0≠默认 → setter 生效）/ 未声明走 core 默认（barrier=30000、maxConsecutive=3、storageType=local、pipelineId="1" core 默认、jobId=core UUID seed——测试断言已对齐 core 默认值）；窗口 3 属性 + 节点级 2 属性参数化 fail-fast（断言 transform id + 属性名）。triggerId 声明即 fail-fast 由参数化用例 [1] 证明（先红后绿语义在案）。
- **验证**：flow 全量 74/0 绿；nop-stream 组全量 0 失败；checkpoint 语义断言 3/3 绿。
- **Doc-sync**：No owner-doc update required（docs-for-ai 无 stream DSL 用法章节；grep 复核 module-groups/INDEX 仅为模块描述）。

### Plan `2026-08-13-1243-2` 执行 Phase 1（P1-XDSL-5 P1）：edge 分区/流控属性消费或 fail-fast

- **Fix**：`StreamModelDslBuilder` 新增 `validateEdgeDeclarations`（buildTransforms 前置全量校验）+ `applyEdgePartition`（HASH+keyExpr 经 `keyBy(EvalActionKeySelector)` 落地，`requireSingleInput` 消费边属性）+ `resolveEdgePartition` **接入 live 调用链**（`applyEdgePartition` 内部使用，不再死代码）+ `findEdge`/`elementDesc`/`firstDeclaredFlowControlAttr` helpers。决策矩阵执行：HASH+keyExpr 非 keyBy 下游 → keyBy 落地；HASH 无 keyExpr → fail-fast；HASH+keyExpr 下游是 keyBy → redundant fail-fast；keyExpr 非 HASH 边 → fail-fast；REBALANCE/BROADCAST + 四流控属性（flowControlPolicy/queueCapacity/receiveWindow/packetSize）→ fail-fast（core DataStream 无对应算子 API，Non-Goals 裁定）。
- **Fix**：`test-reduce-pipeline.stream.xml:20` e0 `partition="HASH"` → `FORWARD`（下游是 keyBy transform，HASH 无 keyExpr 现在 fail-fast；**先红后绿证据**：还原 HASH → `TestAdvancedPipelineE2E` 红（`edge-hash-key-expr-required`，edgeId=e0）→ 恢复 FORWARD 绿）；`fraud-detection.stream.xml:94/:95` e3 REBALANCE + e4 HASH keyExpr → 均改 FORWARD（破损死文件 P2-03-02/XDSL-1~4 backlog，留痕即可，文件注释已标注）。
- **测试**：`TestStreamModelEdgeContract`（11 用例）——8 个参数化 fail-fast（错误码 + edge id/属性名断言：HASH 无 keyExpr / FORWARD+keyExpr / REBALANCE / BROADCAST / flowControlPolicy / queueCapacity / receiveWindow / packetSize）+ redundant HASH（keyBy 下游）+ **落地语义断言**：HASH+keyExpr 边喂 `<window>` → `registeredStream("w") instanceof WindowedStream`（flow 测试不可检查 transformation DAG，构建期断言为 repo-observable 形式，同 plan exit criteria 说明）+ HASH+keyExpr 进 sink 构建成功。
- **验证**：flow 全量 74/0 绿（Phase 1 新增 11 用例）；nop-stream 组全量 0 失败；grep 复核六属性 getter 全部消费或 fail-fast（`getPartition`/`getKeyExpr`/`getFlowControlPolicy`/`getQueueCapacity`/`getReceiveWindow`/`getPacketSize` 均有引用点）。
- **Doc-sync**：No owner-doc update required（docs-for-ai 无 stream DSL 用法章节；`xdef-and-xdsl.md` 的 partition 示例为 xdef 语法教学，非流语义契约，不涉及）。

### Plan `2026-08-13-1243-1` closure：四 Phase 收口 + 独立 closure audit PASS（10/10）+ roadmap/audit 状态流转

- **独立 closure audit（fresh session `ses_006443c5fffeguO9ilpiYMMCrU`，read-only）verdict = PASS（10/10 检）**：逐项对照 live 代码——deserializeKey 重物化（:836-858，双失败分支抛 StreamException）/ InputGate idle 空返回（:453-458,:576-581）+ `IDLE_RETURN_THRESHOLD_MS=250` / checkAlignmentElapsed 每次循环入口（:512,:595-616）/ processInputGate 空闲-EOS 区分（:730-764）/ removeTriggerAccumulators 7 处调用点全部位于最后一次 `triggerContext.clear()` 之后（含合并循环 window=m 基准、三处补全的 clear）；测试族 6 文件计数/名称全符；Anti-Hollow 全链追踪（driver→fire mail→drainAndRun→TimerServiceManager→HeapInternalTimerService）运行时连通；scan-hollow core/runtime exit 0；mjs invariants all exit 0；3 项 Minor 措辞（plan 行号陈旧：:2047/:2062 → :2101/:2118 等）非阻塞。
- **本 run 动作**：plan Closure Gates 13/13 勾选 + Closure 段 evidence 填写 + `Plan Status: active → completed`（Completed: 2026-08-13）；`check-plan-checklist.mjs --strict` exit 0；`check-doc-links.mjs --strict` exit 0（0 errors，3 条 warnings 为无关旧 plan 文件 `2026-08-12-0615-3` 既有链接，未触碰）；mjs invariants all exit 0（output-contract 注册表 WindowOperator 发射点 1136/1966 → 1192/2022 重钉，plan 1 Phase 4 行号漂移）；roadmap 追加 ✅ 收口条目 + P2-INV-4 标 done（POJO 键被同一 deserializeKey 机制天然覆盖）+ 新登记 AR-22（Timer 键 JSON 恢复物化，类别清扫新发现）；两审计文件 `Audit Status: planned → closed`。
- **验证统计**：nop-stream 组全量 2913 tests / 0 failures（core 1474 / runtime 829 / cep 327 / rocksdb 86 / connector 35 / connector-jdbc 32 / connector-batch 35 / connector-debezium 19 / flow 51 / fraud-example 25）；先红后绿证据四组在案（Phase 1 核心 3/3 红 → 全绿；Phase 2 空闲 E2E 2/2 红 → 全绿；Phase 3 饿死 3/3 红（15.8s）→ 0.9s 全绿；Phase 4 清理 5/5 红 → 全绿）。
- **Follow-up**：无 remaining plan-owned work；AR-22 登记 backlog（P0 变体评估）；"空闲期定时器墙钟触发"不变式 #7 wiring 族扩展候选（I6 评估）。

### Plan `2026-08-13-1243-1` 执行 Phase 4（P1-INV-1 P1）：triggerAccumulators 生命周期清理

- **Fix**：`WindowOperator.removeTriggerAccumulators(key, window)` 前缀删除（`"trigger_"+key+SEP+window+SEP`，与 `getSimpleAccumulator` stateKey 构造严格一致），置于每个路径**最后一次 `triggerContext.clear()` 之后**（方案 (a) 留痕：trigger clear 经 getSimpleAccumulator 会重建 miss 条目，删除 hook 不得在 clearWindowContents 内）。六处删除点：元素 purge、合并元素 purge、onEventTime/onProcessingTime timer PURGE、双 cleanup 分支、合并循环（window=合并窗口 m 为键基准，非 stateWindow）。
- **三处不对称补全**：onEventTime timer PURGE、onProcessingTime timer PURGE、合并路径元素 purge 补 `triggerContext.clear()`（与元素路径/cleanup 分支对称；同时修复 timer PURGE 后 trigger 注册的 fire timer 未删除的泄漏）。
- **测试（先红后绿）**：`TestWindowOperatorTriggerAccumulatorCleanup`（5 用例）——元素 FIRE_AND_PURGE、event-time timer PURGE（真实 processWatermark→timer 路径）、processing-time timer PURGE（真实 fireProcessingTimeTimers 路径）、merge 合并出窗口条目移除、purge 后快照/恢复无死条目；断言全部在清理完成后（防 clear 重建掩蔽）。git stash 还原 → 5/5 红；恢复 → 5/5 绿。
- **回归**：runtime 全量 829/0 绿（含 TestWindowOperator* 82 条窗口族 + E2E 窗口基线）；`:2047` 已关闭守卫行为不变（removeTriggerAccumulators 空 map 早退）。
- **Doc-sync**：window-design.md §8.5 补 triggerAccumulators 生命周期闭环说明（与既有"清除窗口全部状态（窗口内容 + trigger 状态）"契约对齐）。

### Plan `2026-08-13-1243-1` 执行 Phase 2+3（AR-02 P1 / P1-INV-2 P1）：空闲期 PT 定时器触发 + barrier 超时检查解耦

- **Phase 2 Fix（AR-02）**：`InputGate` 空闲有界化——`IDLE_RETURN_THRESHOLD_MS=250`（> channel 心跳超时 150ms；生产 RemoteInputChannel `channelTimeoutMs=0` 无竞争），`readSingleChannel`/`readMultiChannel` 全 channel 零返回累计 ≥ 250ms 返回空（空闲信号，非 EOS）；`processInputGate` 空返回时经 `isAllFinished()`/中断位/cancel flag 区分空闲（continue → 循环顶 drain）与 EOS（break）。空闲任务因此周期性到达循环顶 `processAvailableMails()`，PT fire mail 不再滞留——空闲期窗口按墙钟触发（AR-02 修复核心）。
- **Phase 2 测试（先红后绿）**：`TestIdlePeriodProcessingTimeWindowE2E`（2 用例）——(a) 两顶点图 E2E：源发射 1 元素后空闲 1200ms，测试线程**空闲段中间**轮询 sink 断言窗口已发射 + `jobThread.isAlive()`（排除 EOS drain 掩蔽）；(b) invokable 级直连断言 `internalTimerService.numProcessingTimeTimers()==0` 于空闲期（cleanup timer 触发）。`IDLE_RETURN_THRESHOLD_MS=60000` 模拟修复前 → 2/2 红；恢复 → 2/2 绿。
- **Phase 2 read() 契约核对**：`TestInputGateTermination.testSlowProducerDoesNotCausePrematureTermination` 与空闲空返回冲突 → 重写为"循环内连续 read 直至真实 EOS"（保留慢生产者语义 + 断言迟到记录值）；`TestInputGateMultiEpochBarrier`/`TestInputGateMarkFinishedChannelRace` 3+1 个跨线程 reader 循环改 `isAllFinished()` 判真 EOS（消除竞态窗口）；其余写后即 close 的 read 循环零改动。生产者死亡语义（`TestInputGateSingleChannelRemoteLiveness` 150ms 超时先于 250ms 空闲返回）3/3 全绿；core 模块 InputGate 族 116/116 全绿。
- **Phase 3 Fix（P1-INV-2）**：elapsed 检查移到 `readMultiChannel()` **每次循环入口**（`checkAlignmentElapsed()` 独立方法，pending emission drain 之后、sweep 之前），与数据返回解耦；sweep 级旧检查删除；D4 守卫/错误码/oldest 基准不变。pre-fix 持续流量饿死检查 → 失败检测劣化到 coordinator `checkpointTimeout`（600s）。
- **Phase 3 测试（先红后绿）**：`TestInputGateAlignmentStarvationFix`（3 用例）——持续流量中对齐超时（300ms）触发、持续流量中 unaligned 逃生（200ms）发射、空闲期对齐 elapsed 不被跳过；禁用入口检查 → 3/3 红（15.8s 跑满超时窗口）；恢复 → 0.9s 全绿。
- **回归**：runtime 83 条（PT/CEP E2E、TestE2E*、TestMailboxE2ECheckpoint、TestSupervisionLoop*、TestUnalignedCheckpointBackpressure 等）+ core 1470 全绿。
- **Doc-sync**：mailbox-design.md §3.4/§3.5/§7 同步 read() 空闲有界语义 + 空闲期 drain 闭环；checkpoint-design.md §2.4 补"elapsed 评估与数据返回解耦"说明。

### Plan `2026-08-13-1243-1` 执行 Phase 1（AR-01 P0）：Memory 后端恢复键物化 + 四层证据

- **Fix**：`MemoryStateSerDe.deserializeKey` 按 backend keyType 重物化（`JsonTool.parseBeanFromText(json, keyType)`，对齐 RocksDB `jsonToKey`）；keyType 不匹配或 parse 失败抛 `StreamException(ERR_STREAM_STATE_ERROR)`（无静默跳过）；`keyType == Object.class` 与 String 键直通不变。修复覆盖全部 8 种键控状态（同一 `deserializeKey` 调用点）+ **天然覆盖 POJO 键**（P2-INV-4 POJO 变体 → plan Non-Goals 触发条件满足，标注关闭）。
- **测试（先红后绿）**：`TestMemoryStateSerDeNumericKeyRestore`（5 用例）——Long 键 < 2^31 与 > 2^31、Integer 键、String 回归、Map/List 数值键、重物化失败 fail-fast；pre-fix 红（Long 键 miss + 无异常），post-fix 全绿。
- **跨后端一致性**：`TestRocksDBSnapshotRestore.testNumericKeySnapshotRestoresIdenticallyAcrossBackends`——同一 JSON checkpoint 在 Memory 与 RocksDB 后端恢复结果一致（18/18 全绿）。
- **作业级重启 E2E**：`TestE2ECheckpointAndRecovery.testLongKeyedStateJobRestartRestoresSameKeys`——run1 写 Long 键状态 + coordinator → LocalFileCheckpointStorage JSON 持久化 → 新 coordinator 重启恢复 run2 同键命中（5/5 全绿）。
- **类别清扫结论**（plan 执行记录）：MapState 内层键已按 mapKeyType 重物化（既有）；window 命名空间经 Number 提取免疫；聚合/还原键同面已覆盖；算子级 `key.toString()` 键（AR-20）独立失败面不适用；**新发现 Timer 键同机制受损**（`TimerEntry.fromSerializableForm` 原样 cast，JSON 路径 Long 键 → Integer，timer 回调 setCurrentKey 后状态 miss）——走 operator state 面，本次修复不覆盖，登记 roadmap backlog（P0 变体评估）。
- **验证**：`nop-stream-core` 全量 1470/0 绿 + rocksdb 18/0 绿 + runtime E2E 5/0 绿；owner-doc 核对 = No update required（state-management-design §6 未承诺键类型保持语义）。
- **Doc-sync**：No owner-doc update required（`docs-for-ai/` 不涉及）。

### 审计批次 2026-08-13-0805 处理：P0/P1 派发三份 plan（draft → 独立审查 consensus → active）+ P2 入 backlog

- **触发**：mission-driver 审计循环——`ai-dev/audits/2026-08-13-0805-open-audit-nop-stream-invariant-loop.md`（AR-01 P0 / AR-02/03 P1 / AR-04~21 P2）+ `2026-08-13-0805-multi-audit-nop-stream-invariant-loop.md`（P1 × 9 / P2 × 26）两文件均为 `Audit Status: open`（nop-metadata 审计文件实为 `planned`——grep 命中的 "Audit Status: open" 是 AR-05 正文引用，不在处理范围）。
- **派发（P0/P1 全部入 plan，`Fix` 类）**：
  - Plan `2026-08-13-1243-1-nop-stream-engine-state-recovery-and-timer-fixes.md`（active）：AR-01（Memory 后端恢复键物化，P0）/ AR-02（空闲期 PT 定时器触发，P1）/ P1-INV-2（barrier 超时检查解耦，P1）/ P1-INV-1（triggerAccumulators 生命周期清理，P1），4 Phase。
  - Plan `2026-08-13-1243-2-nop-stream-flow-dsl-contract-fixes.md`（active）：P1-XDSL-5（edge 分区/流控属性消费或 fail-fast）/ P1-XDSL-6（checkpoint 六字段实现 + 窗口策略三属性 fail-fast）/ P1-09-02（flow 58 处裸异常 → StreamException + 错误码），3 Phase；逐属性裁定表（HASH+keyExpr 落地 / 其余 fail-fast；keyExpr 非 HASH 边 fail-fast）。
  - Plan `2026-08-13-1243-3-nop-stream-cross-module-contract-remediation.md`（active）：AR-03（Debezium offset 注册表首跑清理 + 未命名连接器 fail-fast）/ P1-DOC-01/02/03（checkpoint-design §2.4/§2.5、README DISTRIBUTED 回写）/ P1-01-01（rocksdb 命名空间迁移，**人工确认门**，Phase 3 自 draft 起 `blocked`），3 Phase/Workstream。
- **独立审查（3 轮迭代，每轮 fresh session）**：round 1 三份 plan 各发现 Blocker/Major（plan 1：Phase 2 空闲返回空与 `processInputGate` EOS break 语义冲突 + Phase 4 删除 hook 被 triggerContext.clear() 重建；plan 2：逐属性决策未钉 + 不存在的 API 引用 + 19 处既有异常断言迁移缺失 + fixture 错文件；plan 3：`_default_` 决策悬空 + E2E harness 未钉 + `-pl` 门不覆盖 runtime test scope + 双 fresh 分支 + store 侧异常类型）→ round 2 修复后重审（均残留 Minor + plan 1 F1/F2 两个 Major）→ round 3 三份全部 **EXECUTABLE**（仅 Minor 措辞/计数，已全部修掉）→ `Plan Status: draft → active`。审查证据：`ses_0068da4e8ffebhjcxkUDPz72CC` / `ses_0068d958effe4smFVE2oEHfYTo` / `ses_0068d847dffeyeNf4qs76Ob23r`（round 1）、`ses_00682adedffesS0dJ4Pr0onYne` / `ses_006829893ffeHNBEwwod4i0M5t` / `ses_00682859affeS4SBGFInoOYu4n`（round 2）、`ses_00678cbd6ffe7QoxiI4gQg62o4` / `ses_00678bebeffeVrtKX7A1WAogSO` / `ses_00678b17fffe4fTohTzmUuXXGp`（round 3）。
- **P2 入 backlog**：roadmap `ai-dev/backlog/nop-stream-invariant-loop-roadmap.md` Follow-up Backlog 追加两个批次（open-audit 18 条 AR-04~21 + multi-audit 26 条），每条附 source audit 路径；与既有 C3-RL-4..9 backlog 条目同族的已标注（AR-01↔P2-INV-4、P2-INV-3↔C3-RL-6、P2-03-01↔P2-11 等）。
- **审计状态流转**：两文件 `Audit Status: open → planned`（处理记录写入文件头）。
- **验证**：纯文档处理零代码变更；`check-doc-links.mjs --strict` 待收口时跑（三份 plan 未新增 docs-for-ai 链接）。

### Plan `2026-08-13-1040-2`（Cycle 3 / I6）closure run：roadmap 翻转 + plan 收口（独立 closure audit PASS 14/14）

- **独立 closure audit（fresh session `ses_006db0c9affeVdCCi2njUtDcIV`，read-only）verdict = PASS（14/14）**：三 Phase Exit Criteria + 9 条 Closure Gates 全部 PASS——surefire 独立汇总 2895/0/0/10 与 `cycle3-I6-input.md` 逐模块一致 + 门禁子集 11 类 / 112 tests / 0 failures + pin 0（`mjs-pins.json` pinnedViolations = []）+ 报告 mtime 11:15-11:16 在 I5 执行窗口（`find -newermt` 空 + git head `f5e18a0dc` 11:17:11）；red list 零悬挂（§12.2 10 行 + §16 18 行 1:1）；注册表 15 处 live 行号抽查全命中；新族数 0（无已铸 PD-16）；roadmap 回写 ⇔ §17 ⇔ daily log 数字一致（Anti-Hollow）；deferred 分类诚实（C3-RL-8 / C3-PR-2 = watch-only residual + `HG-01` = Fix 人工确认门显式非 watch-only）；无静默降级；checklist / doc-links 工具 exit 0；plan diff 38 处 [x] 翻转 = 实执行。4 项 Minor 观察（措辞级，非阻塞——其中「工作树干净」措辞与 Closure Status Note 模板残留已修正）。
- **本 run 动作**：plan Closure Gates 9/9 勾选 + Closure 段 evidence 填写（`ses_006db0c9affeVdCCi2njUtDcIV`）+ `Plan Status: active → completed`；roadmap Cycle 3 / I6 行 `planned` → `done`（closure audit 通过后置位，附执行结果段）；adjudication-table.md §17 落档完成；`check-plan-checklist.mjs --strict` 终跑 exit 0。
- **Cycle 3 闭环完成**：I1 → I2 → I3 → I5 → I6 全 done（I4 不立 plan）；门禁 11 类 / 112 tests 全绿；red list 零悬挂；新族数 = 0 → **稳态暂停**（Cycle 4 不派生，显式依据 §17.2）；`HG-01` 维持 `pending human confirmation`（四重保护性覆盖）；复触发登记在案（三选一 + C3-RL-8 / C2-PR-2 / `HG-01`）。**循环暂停待复触发**。

### Plan `2026-08-13-1040-2`（Cycle 3 / I6）执行 Phase 3：复触发登记与 roadmap 状态流转

- **复触发条件登记（roadmap §Loop Rule「Cycle 3 / I6 稳态登记」+ §I6 Phase Details 摘要）**：三选一继续生效（① CI 任一不变式门禁变红；② nop-stream 核心类结构变更（新增/重命名 Operator/SinkFunction/Checkpoint 机制/Output 实现类）；③ 周期复探（默认每 major release 或季度，取早））；**Cycle 3 新增触发**——C3-RL-8 CEP 面触发条件（P2 backlog = I4/I5 类别清扫（CEP 面）或 per-state windowTimes 使用面扩展或复探时评估）；C2-PR-2 形态出现（main 匿名 `new Output<>(){}` / `record ... implements Output` / raw `OutputTag` 声明）；`HG-01` 人工批准（批准后另立跨 task 线协议 Successor plan）。人工确认待办触发 = 人工批准跨 task 线协议结构性变更（`HG-01`）。
- **roadmap Cycle 3 / I6 行追加 + 状态流转**：Work Item 表追加（含执行结果段，`todo`→`planned`→`done`——`done` 在 closure audit 通过后置位）；I5 行执行结果段核对在案（I5 已填充）。
- **文本一致性核对**：Plan Status / Phase Status / Exit Criteria / Closure Gates / roadmap 行 / daily log 六处一致（closure 前快照；`done` 置位与 Closure evidence 写入由 closure run 完成）。
- **验证**：纯文档 plan 零代码变更（仅 ai-dev 文档）；`git status` 确认无 src 变更。
- **Doc-sync**：No owner-doc update required（`docs-for-ai/` 不涉及）。

### Plan `2026-08-13-1040-2`（Cycle 3 / I6）执行 Phase 2：稳态/派生裁定与处置登记

- **Cycle 4 / I1 派生裁定 = 维持稳态（Cycle 4 不派生）**：显式依据在案（adjudication-table.md §17.2）——I3 §14 显式无新独立族（C3-PR-3/4/8 不升格 + 理由在案）+ Cycle 3 无新失败类（I5 全绿 2895/0）+ C3-PR-2 watch-only residual 不构成派生输入 + 门禁面完整（11 类 / 112 tests + scan-wiring V1–V5）+ 派生 = 六份 plan 循环机器 vs 零新族收益不匹配 + 复触发登记可覆盖。Loop Rule「新族强制沉淀」强制分支未触发。
- **C3-RL-8 等 backlog 触发条件登记**：C3-RL-8（CEP 面 P2）= I4/I5 类别清扫（CEP 面）或 per-state windowTimes 使用面扩展或复探时评估（roadmap P2-08 条目）；C3-RL-4..9 其余维持既有触发条件，不逐一重裁。
- **`HG-01` 处置登记**：未批准（全仓无人工批准记录）→ 维持 backlog 待办条目 `pending human confirmation`（roadmap 条目已补 Cycle 3 / I6 复核记录）；四重保护性覆盖复核在案；不登记 Successor。
- **roadmap 回写**：§Loop Rule 追加「Cycle 3 / I6 稳态登记」（稳态暂停 + 复触发：三选一 + C3-RL-8 / C2-PR-2 / `HG-01`）；Follow-up Backlog 状态核对（C3-RL-4..9 维持 P2 backlog / C2-RL-3+C2-PR-4、C2-PR-3 维持 P3 `todo` / `HG-01` 更新复核记录）。
- **I6 裁定记录落档**：adjudication-table.md §17 追加（收口统计确认 / 稳态-派生总裁定 / `HG-01` 处置 / 复触发登记摘要 / Cycle 3 收口结论）——供后续 cycle 引用。
- **验证**：纯文档 plan 零代码变更（仅 ai-dev 文档）；裁定输入 = Phase 1 live 复核（on-disk surefire + 注册表 + red list + backlog）。
- **Doc-sync**：No owner-doc update required（`docs-for-ai/` 不涉及）。

### Plan `2026-08-13-1040-2`（Cycle 3 / I6）执行 Phase 1：收口统计确认与稳态判定输入

- **I5 硬前置预检 4 检全过**：(a) `2026-08-13-1040-1` Plan Status = completed；(b) `cycle3-I6-input.md` 存在（I5 唯一落点）；(c) `red-list.md` 头部 = 「Cycle 3 / I2 权威版」；(d) `adjudication-table.md` 无既有 Cycle 3 / I6 裁定节（grep 零命中，无中止残留）——未触发 blocked。
- **统计核对（on-disk surefire vs `cycle3-I6-input.md`，零出入）**：nop-stream 模块组 10 模块 TEST-*.xml 独立汇总 = **2895 tests / 0 failures / 0 errors / 10 skipped**（core 1465/1 / runtime 821/8 / cep 327 / rocksdb 85 / connector 35 / connector-jdbc 32 / connector-batch 35 / connector-debezium 19/1 / flow 51 / fraud-example 25），与落档逐模块一致；门禁子集 11 类 / 112 tests / 0 failures（core 50 / runtime 34 / cep 28 逐类一致）；`mjs-pins.json` pinnedViolations = []（pin 0）。**mtime 窗口**：报告 mtime 2026-08-13 11:15-11:16，I5 收口提交 `f5e18a0dc` 11:17:11 为最后提交（无后续构建覆盖，I6 执行前工作树无 src 变更）。
- **red list 零悬挂**：C3-RL-1..10 全裁决在案（adjudication-table.md §12.2 10 行 + §16 零悬挂表 18 行 1:1 对应）+ C3-PR-1..8 全显式处置（关闭 / watch-only / 并入裁决 / 评估表）；roadmap 2026-08-13 backlog 升级登记（open-audit 8 + multi 4 + 修订 1）与 I3 §15 一致。
- **注册表零漂移 live 抽查**：wiring 7 API × 9 接线点（StreamTaskInvokable :230/:268/:287/:293/:426/:435/:462/:463 + GraphModelCheckpointExecutor :742）全部命中；output-contract 6 发射点（ProcessOperator :111/:134 / WindowOperator :1136/:1966 / CepOperator :507/:814）全部命中——与 C3-RL-2 记录一致（Cycle 3 零代码变更）。
- **新族数 = 0**：I3 §14 显式声明（无新独立族）；全仓 grep 无已铸 PD-16（命中均为否定/保留引用）；I5 零新失败。
- **Cycle 3 执行路径事实**：roadmap I1/I2/I3/I5 行 `done`、I4 行不追加（`ai-dev/plans/` 无 Cycle 3 / I4 文件）、I6 行待追加 → 统计口径 = 门禁 11 类 / 112 tests、red list C3-RL-1..10、新族 0。
- **`HG-01` 状态复核**：roadmap Follow-up Backlog 条目 = `pending human confirmation`；全仓 daily log / backlog 无人工批准记录 → **未批准** → 维持待办登记。
- **稳态判定（初步）**：零新族 + red list 零悬挂 + 门禁全绿 → 稳态暂停候选；Loop Rule「新族强制沉淀」强制分支不触发（I3 §14 显式裁定无新独立族），派生与否 = Phase 2 显式评估。
- **验证**：纯文档 plan 零代码变更；统计核对以 on-disk surefire 为准（I5 落点不回写）。
- **Doc-sync**：No owner-doc update required（`docs-for-ai/` 不涉及）。

### Plan `2026-08-13-1040-1`（Cycle 3 / I5）执行：全量验证与门禁零命中（full-green 记录）

- **前置机械门**：I3（`2026-08-13-0805-3`）Plan Status = completed 确认；`ai-dev/plans/` 无 Cycle 3 / I4 计划文件（grep `cycle3-I4` 零命中，I3 §13「I4 不立 plan」在案）；`cycle3-I1-input.md` 存在（门禁基线唯一落点）——三检全过，未触发 blocked。
- **全量测试**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（01:26 min）。nop-stream 模块组 surefire 汇总 = **2895 tests / 0 failures / 0 errors**（skipped 10：runtime 8 multi-jvm + core 1 + debezium CDC 1，均既有环境依赖跳过）：core 1465 / runtime 821 / cep 327 / rocksdb 85 / connector 35 / connector-jdbc 32 / connector-batch 35 / connector-debezium 19 / flow 51 / fraud-example 25。与 I1 全量回归基线 **2895 精确一致**（Cycle 3 零代码变更）；已知 flaky 基准用例（`TestRocksDBIncrementalRestoreAndBenchmark.incrementalCheckpointIsFasterThanFullScanForLargeState`）本次**未失败**，无需 flaky 处置路径。
- **门禁零命中**：
  - mjs：`node ai-dev/tools/check-nop-stream-invariants.mjs all` **exit 0**（六命令 inventory / sync / scan-iterations / scan-output-contract / scan-wiring / self-test 全绿，全绿模式无 stdout 属正常）；`mjs-pins.json` pinnedViolations = **0**（无 unpinned、无 stale）。
  - JUnit 门禁子集：`./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime,nop-stream/nop-stream-cep -Dtest='Test*Invariant*'` BUILD SUCCESS，**11 类 / 112 tests / 0 failures**（core 50 = 8+12+10+10+10 / runtime 34 = 9+10+4+11 / cep 28 = 21+7），与 I1 门禁基线 112 **精确一致，无计数 anomaly**。
- **相关 e2e 复跑**（8/8 绿）：`TestProcessingTimeWindowProductionE2E` 3/3 + `TestCepProductionExecutionE2E` 4/4 + `TestSupervisionLoopCheckpointReconnectE2E` 1/1（均显式复跑，亦含于全量）。
- **backlog 触发评估**：纯验证零代码变更、无类别清扫动作 → C3-RL-4..9 / C2-RL-3 / C2-PR-3 等 backlog 触发条件（类别清扫 / 复探 / 多消费者接线需求出现）**均未触发**，条目状态未改动（结论一行写入 `cycle3-I6-input.md`）。
- **新失败处置**：**零新失败**（正常关闭路径，无需 blocked 判定）。
- **落档**：full-green 记录 = 本日志条目；I6 输入统计 = `ai-dev/audits/nop-stream-invariants/cycle3-I6-input.md`（**唯一落点**，新建，含门禁 11 类 / 112 tests、全量 2895 tests、pin 0、red list 零悬挂（C3-RL-1..10 全裁决 + C3-PR-1..8 全处置）、新族数 0、e2e 8/8、Cycle 3 执行路径事实（I1→I2→I3→I5→I6，I4 不立 plan）、backlog 触发评估结论）。
- **验证**：纯验证 plan 零代码变更（仅 ai-dev 文档）；`git status` 确认无 src 变更；closure 前快照 `check-plan-checklist.mjs --strict` pass。
- **Doc-sync**：No owner-doc update required（`docs-for-ai/` 不涉及；纯验证 / 文档计划）。

### Plan `2026-08-13-0805-3` closure run：roadmap 翻转 + plan 收口（closure audit APPROVE-WITH-MINORS）

- **独立 closure audit（fresh session `ses_007084e75ffeU1Uo0VUrTZuIWg`）verdict = APPROVE-WITH-MINORS**：三 Phase Exit Criteria + 11 条 Closure Gates 全部 PASS（live 代码 Anti-Hollow 抽查 C3-RL-4/5/6/7/8/9 全部命中；18 行零悬挂 1:1 对应；checklist/doc-links 工具 exit 0；git status 仅 4 个 ai-dev 文档文件 = 纯决策声明成立）；2 项 Minor（日志缺 round 2 共识审查显式记录 / roadmap 行内文本陈旧）——本 closure run 内处置。
- **本 run 动作**：roadmap Cycle 3 / I3 行 `planned` → `done`（附执行结果段）；plan Phase 3 第 2 项 + Exit Criteria + Closure Gates 11/11 勾选；Closure 段 evidence 填写；Plan Status `active → completed`；`check-plan-checklist.mjs --strict` 终跑 exit 0。
- **移交**：无 P0/P1 派发 → I4 不立 plan，下一轮 mission-driver 直接进入 I5（全量验证）或 I6（收口）判定；P2/P3 升级登记（6 条）与修订（1 条）已在 roadmap Follow-up Backlog；无新独立族 → I6 稳态判定输入（零新族）；`HG-01` 人工确认待办延续。

### Plan `2026-08-13-0805-3` 独立共识审查第二轮（Phase 2）：APPROVE-WITH-MINORS

- **重审（fresh session `ses_0070d6d1bffeUBvTilNa9NT1E4`）**：首轮 2 Major + 3 Minor 修复逐条验证 **FIXED**（C3-RL-8 P1→P2 触发面分析 live 复核全部属实：CepOperator :564/:573/:579-591 + NFA.java:736-747 无条件重建 start state + :353 `!isStartState` 守卫；roadmap 行回退 planned；§16 补 C3-PR-3..8 行；升级计数修正；`Pattern.within` 遗产引用消除）；交叉核验零悬挂 18 行 + 分类诚实性 + P2-08 六处一致 PASS；1 条新 Minor（§16/日志 backlog 升级计数 5→6 陈旧）→ 已修正（6 条）；verdict **APPROVE-WITH-MINORS**。
- 观察（非本 plan 引入，移交 backlog 候选）：live javadoc `NFA.java:94/:101`、`TimedOutPartialMatchHandler.java:43` 仍含 Flink 遗产 `@link Pattern#within(Time)`——属既有代码债，建议 CEP 面类别清扫时顺手修订。

### Plan `2026-08-13-0805-3` 执行 Phase 3（Decision | Proof）：收口与移交（三处一致复核 + I4 移交声明）

- **三处一致复核（裁决表 / 派发清单 / backlog 登记）**：adjudication-table.md §12.2（10 条裁决行）+ §13（无 P0/P1 显式声明）+ §15/§16（6 条 backlog 升级 + 1 条修订 + 零悬挂表）↔ roadmap Follow-up Backlog（P2-01..08 + multi P2-11/01/03/05 升级登记 + P2-09 修订）逐条对照一致；C3-RL-8 = P2 在六处（§12.2/§13/§15/§16/roadmap/日志）交叉一致；C3-RL-9 P2-05「需人工确认」四处一致——**无悬挂、无降级、无静默**。
- **I4 移交声明**：**无 P0/P1 派发 → I4 不立 plan**（本 plan Non-Blocking Follow-ups 第 1 条触发「无 P0/P1 派发时本项不触发」）——roadmap I4 行不追加；下一轮 mission-driver 判定直接进入 I5（全量验证）或 I6（收口）。P0/P1 裁决记录 = C3-RL-8 独立共识审查修正 P1 → P2（触发面分析在案，§12.2）。
- **roadmap Cycle 3 / I3 行**：保持 `planned`（`done` 须 closure audit 通过后置位，本 plan 机械门），closure run 时翻转。
- **验证**：`check-doc-links.mjs --strict` 0 errors；`check-plan-checklist.mjs --strict` 当前 pass（closure 前快照）；`git status` 仅 4 个 ai-dev 文档文件变更（纯决策计划零代码变更声明成立）。
- **Doc-sync**：No owner-doc update required。

### Plan `2026-08-13-0805-3` 独立共识审查（Phase 2）：首轮 REJECT + 修复

- **独立共识审查（fresh session `ses_00715bd01ffefNuIbgQy9lFBnl`）首轮 verdict = REJECT**（2 Major + 3 Minor）：
  - **Major 1：C3-RL-8 P1 升格依据不成立**——触发面分析：STEP-5 清理仅在 `partialMatches.size()==1 && completedMatches.isEmpty()` 时触发（CepOperator :564）；`NFA.java:736-747` 事件处理后无条件重建 fresh start state → size==1 ⟹ 唯一 partial match 必为 start state（startTimestamp=-1，previousBufferEntry=null——无节点可 release、惰性清理、下一事件重建）；对 start state，NFA 语义（`isStateTimedOut :352-353` `!isStartState` 守卫）= 永不过期、无超时事件可通知 → 「超时事件静默丢弃」无用户可见损失；真实影响面收敛为恢复快照 skip-strategy 裁剪后单非 start 匹配 + per-state window 边角 = 边界/潜伏 = **P2**（对齐 I2「潜伏地雷」候选级，I3 无新证据反向升格 = 与来源定性冲突）。
  - **Major 2：roadmap Cycle 3 / I3 行提前标 `done` + 声称「独立共识审查 + closure audit 通过」**（本 plan 仍 active、Phase 2/3 未勾选、审查尚未发生）——违反 plan Phase 3「`done`（closure audit 通过后，不得提前）」机械门，审计证据链造假风险。
  - Minor：§16 零悬挂表未显式枚举 C3-PR-3..8（已补行）；roadmap 行「升级 8 条」未计 multi-audit 4 条（§15 已改「open-audit 8 + multi-audit 4 升级 + 1 修订」）；`Pattern.within` javadoc 引用为 Flink 遗产 `@link`（live 公共 API = `Pattern.within(Duration)` :271/:283）。
- **修复（live 复核后全部落实）**：
  1. `adjudication-table.md` §12.2 C3-RL-8 行：P1 → **P2**（依据重写：触发面分析 + I2 定性对齐 + 真实影响面 + 触发条件 + 修复方向候选保留为 backlog 触发时使用）。
  2. `adjudication-table.md` §13：删除 WI-C3-1 派发 → **显式声明「无 P0/P1 项（I4 不立 plan，直接进入 I5 或 I6 判定）」**（沿 Cycle 2 / I3 §8「其余 P0/P1 显式声明」先例）。
  3. `adjudication-table.md` §15：补 C3-RL-8 backlog 升级登记（P2）。
  4. `adjudication-table.md` §16：零悬挂表 C3-RL-8 改 P2/backlog + **补 C3-PR-3..8 显式处置行**（8 条探查发现全显式处置）。
  5. `roadmap`：Cycle 3 / I3 行回退 `planned`（去除提前 done + 未发生审查的通过声明）；P2-08 条目升级登记改 P2 backlog（含触发条件）；升级计数 8 → open-audit 8 + multi-audit 4。
- **验证**：live 复核 `NFA.java:736-747`（无条件重建 start state）与 `Pattern.java:271/:283`（within(Duration) 公共 API）属实；裁决表修正后与 I2 权威版定性一致。
- **后续**：重审（fresh session）通过后继续 Phase 2 收尾 → Phase 3 → closure audit。

### Plan `2026-08-13-0805-3` 执行 Phase 1（Decision）：裁决表拟制（Cycle 3 / I3）

- **I2 硬前置预检 4 检全过**：(a) `2026-08-13-0805-2` Plan Status = completed；(b) `red-list.md` 头部 = 「Cycle 3 / I2 权威版」；(c) `cycle3-I2-probing-report.md` 存在；(d) `adjudication-table.md` 无既有 Cycle 3 节（仅 §1-§6 Cycle 1 + §7-§11 Cycle 2 历史节）——未触发 blocked。
- **假设复核（§12.1，6 条对照表）**：red list 条目集 C3-RL-1..10（3 记录性 + 6 复探确认 + 1 处置记录）符合 plan 声明「I2 可能增减」；仅测试注入复探 = 零新实例（C3-PR-1 → 关闭条目）；守卫 residual = 维持 watch-only（C3-PR-2）；无新族（C3-PR-3/4/8）；P2 触发评估表无实质差异；注册表行号零漂移——**无 NEEDS_I2_SUPPLEMENT**。
- **裁决表落档（adjudication-table.md §12-§16）**：
  - **C3-RL-1/2/3（记录性）**：关闭（门禁全绿 / 注册表 9/9 维持 / 组合面恢复面无缺口）。
  - **C3-RL-4（checkpoint/cluster 面）**：三条均 **P2** → backlog 升级登记（P2-01 资源泄漏 / P2-02 失败路径健壮性——live 复核 future 消费者为有界 `.get(timeout)` / P2-03 潜伏并发 NPE——**live 实测 `getNodeLease` main 零调用方**）。
  - **C3-RL-5/6/7（窗口面）**：均 **P2** → backlog 升级登记（triggerAccumulators 无界 + 纯 FIRE 不清 / evictor descriptor 路径不建时间戳状态 / 合并路径 pane 键错位 + purge 缺 clear）。
  - **C3-RL-8（CEP 面）**：首版裁定 P1 派 I4（`:573/:646` 用 startTimestamp + wt（start state = -1）与 `NFA.isStateTimedOut :352-353` 语义不一致、清理绕过 processTimedOutSequences）；**独立共识审查 REJECT 后修正为 P2 → backlog 升级**（触发面分析：size==1 ⟹ start state、无超时通知义务 → 无用户可见丢失场景，见上节 REJECT 记录）。
  - **C3-RL-9（multi-audit 面）**：四条均 **P2** → backlog 升级登记（beans 重复 id（同目录双文件同容器冲突）/ serializer 死字段 + createDummySerializer null / RocksDB Options 泄漏 / 上帝类 1728 行）。
  - **C3-RL-10（处置记录）**：关闭 + roadmap P2-09 修订（multi P0-01 触发条件已解决，claim 过期）。
  - **仅测试注入复探（C3-PR-1）**：零新实例 → 关闭（plan `2026-08-13-0132-1` Non-Blocking 可闭合）；**守卫 residual（C3-PR-2）**：维持 watch-only。
- **零悬挂复核（§16）**：10 red list + 8 探查发现全部有处置（无 I4 派发——显式声明无 P0/P1；6 条 backlog 升级 + 1 条 backlog 修订 + 3 条关闭 + 1 条 watch-only 维持 + 探查项全显式处置），零悬挂达成。
- **验证**：纯决策计划零代码变更（仅 ai-dev 文档）；`git status` 确认无 src 变更。
- **Doc-sync**：No owner-doc update required。

### Mission driver 收口验证：plan `2026-08-13-0805-2` 构建全绿复核（full-green verification）

- **typecheck**：`./mvnw compile -pl nop-stream -am -q` PASS。
- **build**：`./mvnw clean install -DskipTests -pl nop-stream -am -T 1C` BUILD SUCCESS（18 模块全 SUCCESS）。
- **lint**：`./mvnw checkstyle:check -pl nop-stream -am -q` 报告 9164 条既有违规——全部位于 `nop-api-core`（ConvertHelper / SysConverterRegistry 等），stash 后 clean tree 复跑同样报错（9174 条），**纯 pre-existing 噪音，非本 mission 引入**；命令 `|| echo 'lint not configured'` 兜底语义生效。
- **test**：`./mvnw test -pl nop-stream -am -T 1C` 首跑 `TestRocksDBIncrementalRestoreAndBenchmark.incrementalCheckpointIsFasterThanFullScanForLargeState` 1 条失败（性能基准 ratio=1.715 时序抖动，非正确性断言）；单模块复跑 2/2 绿 + 全量复跑 BUILD SUCCESS 0 failures——**确认 flaky 基准测试**（该测试 2026-08-06 `5663e4061` 已列为 flaky 修过），mission 零代码变更，无回归。
- **收口**：full-green 达成；本次变更全为 docs（plan 收口 + probing report + red-list + roadmap + log），无代码提交。

### Plan `2026-08-13-0805-2` closure audit + 收口：Cycle 3 / I2 不变式驱动审计（独立 closure audit APPROVE，plan 转 completed）

- **独立 closure audit**（fresh session `ses_0072a9085ffez8j8ZIp40n1KPa`）：四 Phase Exit Criteria 逐条对照 live 工件 PASS（P1 mjs all exit 0 实跑 + JUnit 112/112 surefire 实跑；P2 9 接线点 spot-check（StreamTaskInvokable:462/:463、GraphModelCheckpointExecutor:742/:687/:660）+ pins 空表 + TestWiringExistenceInvariant 10/10；P3 probing-report 盲区 a-e 全结论 + C3-PR-1..8 含 live 证据；P4 red-list C3-RL-1..10 每条 file:line + finding + 裁决输入，无 NEEDS_SUPPLEMENT，Cycle 2/1 附录保留）；Closure Gates 13/13 PASS；Anti-Hollow PASS（16 处 file:line 抽查全部命中 live 源码（checkpointSuccessMap:1117 / triggerAccumulators:2043-2071 / STEP-5:573 / getNodeLease:99-108 / beans:34+38+68 / RocksDB:206-210 / TestWindowOperatorCorrectness:551+607 等）+ §0 门禁结果与实跑一致 + E2E 复验 3/3+4/4+1/1）；2 项 minor（消费方行数 10→11 计数陈旧——已修正；mjs 成功静默输出措辞——行为一致非问题）；verdict **APPROVE**。
- **本 closure run 验证**：`check-plan-checklist.mjs --strict` exit 0（Closure Gates 全勾选 + Closure evidence 写入后复跑）；`check-doc-links.mjs --strict` exit 0（0 errors，3 BROKEN_LINK 属他 plan 既有引用非本 plan 引入）；mjs `all` exit 0（六命令，pin 0）；JUnit 门禁 11 类 112 tests / 0 failures 复跑（core 50 / runtime 34 / cep 28）；hollow scan = 14 项既有债零新增（`34aed42c1` 判据）；E2E 8/8（Phase 4 实测）。
- **收口**：plan `Plan Status: active → completed`（四 Phase 全勾选 + Closure Gates 13/13 + Closure evidence 填写）；roadmap Cycle 3 / I2 行 `planned → done`（附执行结果段：门禁全绿 + 注册表 9/9 维持 + 同族复探零新实例 + P2 触发评估 10+23 条 + C3-RL-1..10 零悬挂移交 I3 + 无新族）；plan `2026-08-13-0132-1` Non-Blocking 登记项（同族复探 / 守卫复核）在 backlog 两处残余行更新为已闭合。
- **遗留**：触发确认项 C3-RL-4..9 移交 I3 裁决（plan `2026-08-13-0805-3`，active）；multi-audit P0-01 已解决记录（C3-RL-10）供 I3/backlog 修订；ArchUnit = optimization candidate 延续；`HG-01` 人工确认门延续。

### Plan `2026-08-13-0805-2` 执行 Phase 4（Proof）：red list 权威化移交 I3

- **Anti-Hollow E2E 复跑 8/8 绿**：`./mvnw test -pl nop-stream/nop-stream-runtime,nop-stream/nop-stream-cep -Dtest='TestProcessingTimeWindowProductionE2E,TestCepProductionExecutionE2E,TestSupervisionLoopCheckpointReconnectE2E'` → BUILD SUCCESS（`TestProcessingTimeWindowProductionE2E` 3/3 + `TestCepProductionExecutionE2E` 4/4 + `TestSupervisionLoopCheckpointReconnectE2E` 1/1，0 failures）——生产接线链路入口→出口运行时连通实跑证据在案。
- **red-list.md 权威版定稿**：Cycle 3 / I2 结构——§0 门禁全量结果 + §1 C3-RL-1..10（3 记录性 + 6 复探确认项 + 1 处置记录）+ §2 注册表裁定 9/9 维持 + §3 C3-PR-1..8 + §4 移交声明（零悬挂核对表：I1 登记项全部有处置）；Cycle 2（§5）/ Cycle 1（§6）历史版原样保留为附录。
- **零悬挂复核**：C3-RL-4~C3-RL-9 每条含 live 行号 + finding 追溯 + 修复方向候选，无 NEEDS_SUPPLEMENT 条目。
- **统计落点**：`cycle3-I1-input.md` 追加「Cycle 3 / I2 消费记录」节（统计不新建文件，唯一落点原则）；`cycle3-I2-probing-report.md` 与 red-list.md 交叉引用一致。
- **Rule #25**：No new test required（纯审计运行零代码变更，门禁/E2E 复跑均既有测试）。
- **验证**：全量 `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（Phase 1 实测 0 failures，四 Phase 零代码变更）；门禁 11 类 112 tests 0 failures。
- **Doc-sync**：No owner-doc update required。

### Plan `2026-08-13-0805-2` 执行 Phase 3（Proof | Decision）：聚焦对抗探查（盲区 a-e + P2 触发评估）

- **盲区 a 同族复探**：checkpoint 服务（CheckpointCoordinator :581 / CheckpointBarrierTracker :707 / SharedStateRegistry :1327 内部创建）、watermark 服务（StreamGraphGenerator :458 生产创建 → 接线）、其他 `set*Service`（setTimerService / setCoordinatorRpcService / RPC 面）全 main grep 分类——**零 P0-01 同形态新实例**（test grep 仅命中已登记 API：setStateBackend 39 / setKeyedStateBackend 28 / setOperatorStateBackend 1）；plan `2026-08-13-0132-1` Non-Blocking 登记项处置 = 复探完成可闭合。`TimestampsAndWatermarksOperator` :82-84 守卫复核：4 构造无条件注入（:462）→ 接线后 PTS 恒非 null、守卫生产不可达——plan 1 裁定「接线后自然失效」**成立**。
- **盲区 b 时序组合面**：构造 vs open vs restoreState vs rebuildTask——双算子 restore-before-open 延迟应用（CepOperator :259/:283-285、WindowOperator :486-492）、rebuild 复用 wireTaskCheckpointPipeline（SupervisionLoop :838 = :660）、driver 延迟 invoke() 启动（:475-481）——**无新 null 面/无重复注入/无顺序破坏**。
- **盲区 c 恢复路径**：descriptor 路径 snapshot→restore 服务注入面保持（restoreTimers→registerTimerService 闭环 + applyPendingRestoreState→getter 可用）；联动 P2-01/P2-02 **触发确认**（checkpointSuccessMap :1117 put / fail、abort 路径无 remove；onCompletePersistFailure :826 绕过 pending.fail()，future 消费者 JobCoordinator :1439+）。
- **盲区 d 窗口/CEP 清理语义**：P2-04~P2-08 全部触发确认（live 行号重定位兑现 open-audit 过期行号警示：triggerAccumulators :2043-2071 只增不删 / elementTimestampsState :451-462 else 分支仅建 + :1337 早退 / computePaneInfo :965 / STEP-5 :563-592 startTimestamp 基准）。
- **盲区 e P2 触发评估表**：open-audit 批 P2-01/02/03/04/05/06/07/08 触发（C3-RL-4~C3-RL-8）、P2-09 **不触发（claim 过期）**、P2-10 不触发（工具面）；multi-audit 批 P2-11（beans 重复 id，control-rpc:34 + data-plane:38/:68）/P2-01（createDummySerializer :166-171 返 null）/P2-03（RocksDB :206-210 无 try-with-resources vs IncrementalRestore :151）/P2-05（上帝类 1728 行）4 条触发（C3-RL-9），其余逐条一行依据不触发。
- **非族评估**：setCurrentKey/setKeyContextElement1/2 不升格（V4 排除清单正确）；HeapInternalTimerService 触发机制 vs 记账对齐（driver→mailbox→fireProcessingTimeTimers :140 链路一致）不升格（plan 3 Non-Blocking 闭合）；StreamTaskInvokable 自身 getter 声明不升格。
- **发现**：multi-audit P0-01（merge fail-fast 零回归测试）**已解决**——TestWindowOperatorCorrectness :551/:607 两个 assertThrows 用例 + :554/:610 实例化，open-audit P2-09 claim 过期（C3-RL-10 处置记录）。
- **产物**：新建 `cycle3-I2-probing-report.md`（范围声明 / C3-PR-1..8 / P2 评估表 / 盲区处置 / 非族评估 / 盲区自评）；red-list.md §1 回写 C3-RL-1..10 + §3 探查发现。
- **验证**：JUnit 门禁 11 类 112 tests / 0 failures（Phase 1 记录复用，Phase 3 零代码变更）。
- **Doc-sync**：No owner-doc update required。

### Plan `2026-08-13-0805-2` 执行 Phase 2（Proof | Decision）：注册表一致性裁定与 pin 复核

- **接线点 live 复核 9/9 维持**（零漂移 ±0）：StreamTaskInvokable :462/:463（构造，setupProcessingTimeServices :456-468，javadoc :446-450「constructor — BEFORE any operatorChain.open()」）/ :230/:268/:287/:293/:426（装配布线，wireOperators + wireTailToRecordWriter :422-428 + fan-out 尾 RWO/BRWO）/ :435（快照，setupSnapshotCallbacks :430-438）+ GraphModelCheckpointExecutor :742（checkpoint 装配，:733-744 供给块）；注入时机四分类（构造/装配布线/checkpoint 装配/快照）全部与代码路径语义一致。
- **pin key 匹配**：`pinnedViolations` = 空表 → N/A（零 pin 基线维持，internal-creation = disposition 非 pin）。
- **三方一致**：`TestWiringExistenceInvariant` 为注册表驱动（REGISTRY_REL_PATH :81 + 参数化 7 服务 :136-138），同源 → 一致按构造成立，10/10 绿。
- **接线路径复核**：(a) PTS/TSM 构造注入先于一切 open()（4 构造 :151/:164/:180/:195 调用；PTS driver :475-481 延迟至 invoke() 启动防线程泄漏）；(b) state backend 供给 = 初始 `registerTasksAndTrackers` :660 与重建 `SupervisionLoop.rewireCheckpointPipeline` :838 复用同一 `wireTaskCheckpointPipeline` :687→:742（重建 task 与初始不可区分，P0-02/plan `2026-08-13-0132-2` 在案，单接线点覆盖两路径）；(c) setOutput 关闭面 P1-03 `closeOutputWriters` :509-528 显式遍历全 fanOutWriters（BRWO close no-op 不可依赖，注释 :500-507）。**判定依据全部维持**。
- **回写**：red-list.md §2 落档（复核表 9 行 + pin N/A + 三方一致 + 接线路径 + 裁定 9/9 维持）；无注册表 → JUnit 差异项。
- **验证**：JUnit 门禁 11 类 112 tests / 0 failures（Phase 1 记录复用，Phase 2 零代码变更）。
- **Doc-sync**：No owner-doc update required。

### Plan `2026-08-13-0805-2` 执行 Phase 1（Proof | Decision）：门禁全量运行与确定性 red list 初稿

- **I1 硬前置预检 4 检全过**：(a) `2026-08-13-0805-1` Plan Status = completed；(b) `wiring-registry.json` + `cycle3-I1-input.md` 存在；(c) mjs usage 含 `scan-wiring`（独立运行 `scan-wiring: OK`）；(d) JUnit 门禁类 glob 实测 11 类（含 `TestWiringExistenceInvariant`）——无缺项，未触发 blocked。
- **mjs `all` exit 0**（六命令逐条输出记录：inventory / sync / scan-iterations / scan-output-contract / scan-wiring / self-test 全部 `OK`）；**处置二分 = 无变化**（零违规零 pin：`pinnedViolations: []` 且无 unpinned 违规；无行为漂移；无 stale pin）。
- **JUnit 门禁子集**：`./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime,nop-stream/nop-stream-cep -Dtest='Test*Invariant*'` → BUILD SUCCESS，**11 类 / 112 tests / 0 failures**（core 50 / runtime 34 / cep 28），与 `cycle3-I1-input.md` 落档一致。
- **注册表自洽性复核表**（live 全量核对，零漂移 ±0）：7 服务注入 API（AbstractStreamOperator :140/:148/:152/:165/:173/:181/:320）；接线点 9 处（StreamTaskInvokable :230/:268/:287/:293/:426/:435/:462/:463 + GraphModelCheckpointExecutor :742，wireTaskCheckpointPipeline :687 重建路径调用 :660）；消费方 3 类 10 行（CepOperator 8 行 + WindowOperator :471/:479 + ProcessOperator :46，与 live grep 精确一致）；`getTimeServiceManager()` main 零调用（:345 为 StreamTaskInvokable 自身 getter 声明，非消费方）；state backend 专项——`setKeyedStateBackend`/`setOperatorStateBackend` main 零调用点（internal-creation carve-out 成立，生产直建路径 CepOperator:257 / WindowOperator:421 / AbstractStreamOperator:65 / AbstractUdfStreamOperator:114 全部命中）；test-only 豁免清单无 main 混入。
- **red list 初稿**：red-list.md 重构为 Cycle 3 / I2 权威版（§0 门禁结果 + §1-§4 待回写），Cycle 2 / Cycle 1 历史版移入附录 §5/§6（内容原样保留）。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（0 failures）；`scan-hollow-implementations --severity high` findings = 14 项既有债（11 UOE + 3 not-yet-implemented，零新增，沿 `34aed42c1` 判据）。
- **Doc-sync**：No owner-doc update required（纯审计运行，零代码变更）。

### Plan `2026-08-13-0805-1` closure audit + 收口：不变式 #7 wiring 存在性门禁（独立 closure audit APPROVE，plan 转 completed）

- **独立 closure audit**（fresh session `ses_0074568d2ffeVMwgbssBj4O920`）：四 Phase Exit Criteria 逐条对照 live working tree PASS（Phase 1 注册表 7 服务 × 接线点（文件:行+注入时机）× 消费方表 × disposition；Phase 2 `TestWiringExistenceInvariant` 10 用例（参数化 API 面完备性 :162-210 / 反射完备性 :259-276 / 消费方表自洽 :296-325 / 接线连通 :336-359）+ E2E 3/3+4/4 复跑（fail-fast/WARN 断言复核）；Phase 3 mjs scan-wiring 帮助头语义 + self-test fixtures V1–V5 正反例 + `all` 纳入；Phase 4 门禁 11 类 112 tests / 0 failures + `cycle3-I1-input.md` + catalog #7 已沉淀 + core-design §6.1）；Closure Gates 14/14 PASS；Anti-Hollow PASS（`all`→runScanWiring→evaluateWiring 同路径 + fixtures 双向验证 + scratch 红演示（wiring-point 99999 → V3 exit 1）+ JUnit/E2E 断言非空体）；deferred 分类诚实（out-of-scope improvement / watch-only residual 附理由 + successor）；verdict **APPROVE**。
- **本 closure run 验证**：`check-plan-checklist.mjs --strict` exit 0（勾选 Phase 4 + 全部 Closure Gates + Closure evidence 写入后）；`check-doc-links.mjs --strict` exit 0（0 errors；11 warnings 均为他 plan 前置引用：0805-2/3 引用未来 probing report + 历史 credential plan，非本 plan 引入）；`scan-hollow-implementations --severity high` findings-set = 14 项既有债（全部未改动文件，本次改动零命中——plan 1/2/3 同款裁定）；JUnit 门禁子集 11 类 112 tests / 0 failures；全量 2895 tests / 0 failures（Phase 2 实测）；mjs `all` exit 0。
- **收口**：plan `Plan Status: active → completed`（Phase 1–4 全勾选 + Closure 段 evidence 填写）；roadmap Cycle 3 / I1 行 `done`（closure audit 通过后置位，附执行结果段）+ Follow-up Backlog「生产 wiring 存在性不变式门禁沉淀」✅ 收口；`cycle3-I1-input.md` 落档（Cycle 3 统计唯一落点）。
- **遗留**：无 in-scope follow-up；Non-Blocking（checkpoint/watermark 服务同族"仅测试注入"复探 = I2 探查项；`TimestampsAndWatermarksOperator` watch-only 复核 = I2；ArchUnit = optimization candidate）。

### Plan `2026-08-13-0805-1` 执行 Phase 4（Proof）：门禁全绿验证与文档收口

- **JUnit 门禁子集全绿**：`./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime,nop-stream/nop-stream-cep -Dtest='Test*Invariant*'` → BUILD SUCCESS（**11 门禁类 / 112 tests / 0 failures**，10 → 11，新增 `TestWiringExistenceInvariant` 10 用例）。
- **既有 E2E 不回归**：`TestProcessingTimeWindowProductionE2E`（runtime）3/3 + `TestCepProductionExecutionE2E`（cep）4/4 绿（含 PT 模式无服务 fail-fast 断言复核——归属定案在案）。
- **mjs 全子命令 exit 0**：`node ai-dev/tools/check-nop-stream-invariants.mjs all`（inventory / sync / scan-iterations / scan-output-contract / **scan-wiring** / self-test 全绿，pin 0）；`check-plan-checklist.mjs --strict` exit 0。
- **门禁统计落档**：新建 `ai-dev/audits/nop-stream-invariants/cycle3-I1-input.md`（Cycle 3 门禁统计唯一落点：11 类 / 112 tests / pin 0 / E2E 3/3+4/4 / live 调用点统计 / 全量 2895 tests / 0 failures）。
- **文档收口**：`invariant-catalog.md` #7 草稿 → **定稿（状态 = 已沉淀）**（陈述 / 覆盖失败族 / 历史证据 / 检测方法 / internal-creation 裁定 / live 修复状态）；`core-design.md` §6.1 不变式节补 #7（生产接线路径 + 门禁 + 历史证据，先例 = #6）；roadmap Cycle 3 / I1 行 `planned` → `done`（附执行结果段）+ Follow-up Backlog「生产 wiring 存在性不变式门禁沉淀」条目 ✅ 收口（含残余 = I2 探查项移交）。
- **Anti-Hollow 佐证**：`scan-wiring` 在 `all` 路径实跑（live 红演示在案：临时改 wiring-point 行号 → V3 违规 exit 1 → 恢复 exit 0）；E2E 断言实断言生产接线（驱动 → mailbox → task 线程回调 → 窗口 fire）；JUnit 门禁 10 用例非空体（参数化断言 + 接线连通断言）；无空方法体 / 静默跳过。
- **验证**：全量 `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（2895 tests / 0 failures / 0 errors，Phase 2 实测）；`check-doc-links.mjs --strict` / `scan-hollow-implementations.mjs --severity high` 见 closure gate 复核记录。

### Plan `2026-08-13-0805-1` 执行 Phase 3（Fix | Proof）：mjs `scan-wiring` 子命令（V1–V5）+ self-test + `all` 纳入

- **子命令（`check-nop-stream-invariants.mjs scan-wiring`，live 全绿零命中）**：`evaluateWiring(registry, mainSources, testSources)` 纯函数（fixtures 与生产共用同一代码路径）+ `runScanWiring`：
  - **V1 仅测试注入检测**：7 个已登记注入 API 全模块 main+test 双面扫描，调用点 = 成员访问形态（`\.setX\(` / `this.setX\(` / bare `setX(` / cast 形态 `(Type)).setX(`），方法声明与 javadoc 不计（`{`/`throws` 续接 = 声明）；**receiver 限定**（`declaredTypeOf` 最近声明解析 / 继承 operator 字段面 / cast 类型，`CheckpointConfig.setStateBackend` 等非同面不匹配）；production-wired main=0 → 红（违规串含服务名+注入 API+main/test 统计）；`internal-creation` carve-out 跳过。**live 实测**：setOutput main=5 test=193 / setProcessingTimeService main=1 test=0 / setTimeServiceManager main=1 test=3 / setStateBackend main=1 test=38 / setSnapshotCallback main=1 test=25（cast 形态 `((AbstractStreamOperator<?>) operators.get(i))` 解析在案）；setKeyedStateBackend main=0 test=30 / setOperatorStateBackend main=0 test=1 = internal-creation 不红。
  - **V2 消费方枚举完备性**：`getProcessingTimeService()` / `getTimeServiceManager()` / `registerTimerService(` main 调用点所属类 ⊆ 消费方表——live 实测 owners = {CepOperator, WindowOperator} / {} / {ProcessOperator, WindowOperator}，与注册表精确一致。
  - **V3 失效接线点**（文件:行 + 行含方法名校验，live 红演示在案：临时改 wiring-point 行号 → `V3 stale wiring point ...:99999 (setProcessingTimeService)` exit 1，恢复 exit 0）/ **V4 新注入 API**（operator 面 `set*` 声明参数类型 ∈ 已登记服务类型（含 `Consumer<OperatorSnapshotResult>` 泛型实参匹配；qualified 类型名归一）不入表 → 红；`setKeyContextElement1/2`/`setCurrentKey`/`setMailboxExecutor`/`setProgressMarker` 不触发）/ **V5 失效消费方/服务**（java.* 服务排除）。
  - **无静默跳过**：声明-调用判定用续接字符（`{`/`throws` = 声明，其余 = 调用或硬 fail）；main 侧未解析 receiver 硬 fail（test 侧 lenient 计数防红时崩溃——test 计数仅诊断用）；非 cast 括号 receiver（如 `operators.get(i)).setX(`）确定性不匹配（非 cast 必不 operator 限定）。
- **self-test 扩展**：V1–V5 各一正一反 inline fixtures（正 = 自洽注册表+已接线 main 全绿；反 = 仅测试注入 `main=0 test=1` 红 / 新消费类红 / 失效接线点红 / 新注入 API 红（含 qualified 服务参数形态）/ 非服务 setter 不误红 / 失效消费方红）——`self-test` exit 0。
- **执行中发现并修复**：① `parseTypeStructure` extends 提取对 qualified 父类名只取尾段（fixture `extends io.nop.stream.core.operators.AbstractStreamOperator` 曾得 `extendsNames=['io']` → operator 闭包失效）；② receiver.index 未传（声明解析全 miss）；③ fixture 变量与既有 self-test 变量名冲突（v1–v5 前缀化）；④ test 侧非 cast 括号 receiver 硬 fail → lenient 诊断计数 + 非 cast 确定性不匹配。
- **集成**：`all` 纳入 `scan-wiring`（exit 0）；工具帮助头 + `ai-dev/tools/README.md` 工具清单同步；`mjs-pins.json` 头注语义扩为 #2/#6/#7（预期零 pin，internal-creation = 注册表 disposition 非 pin）。
- **验证**：`node ai-dev/tools/check-nop-stream-invariants.mjs scan-wiring` exit 0；`self-test` exit 0；`all` exit 0（inventory/sync/scan-iterations/scan-output-contract/scan-wiring/self-test 全绿）。

### Plan `2026-08-13-0805-1` 执行 Phase 2（Fix | Proof）：JUnit 门禁 `TestWiringExistenceInvariant`（core）

- **门禁类（新建 `nop-stream-core/.../operators/TestWiringExistenceInvariant.java`，10 用例）**：①`testRegisteredInjectionApiExistsOnAbstractStreamOperator`（注册表驱动参数化 7 条：每个已登记注入 API 必须以登记参数类型存在于 `AbstractStreamOperator`（含 `Consumer<OperatorSnapshotResult>` 泛型实参校验），disposition 自洽——production-wired 必有接线点+注入时机、internal-creation 必零接线点+理由在案）；②`testApiSurfaceCompletenessEveryServiceSetterIsRegistered`（V4 正向反射面：`set*` 且单参类型 ∈ 已登记服务类型 {Output, ProcessingTimeService, TimerServiceManager, IStateBackend, IKeyedStateBackend, IOperatorStateBackend, Consumer} → 必入注册表；`setKeyContextElement1/2`/`setCurrentKey` 等不触发）；③`testConsumerTableIsConsistentWithServices`（服务消费者枚举 ⊆ consumerTable + 每行 service 已注册 + behaviorWithoutService 在案）；④`testTaskConstructionWiresServicesIntoOperators`（**Rule #23 接线连通**：`new StreamTaskInvokable(chain)` 构造后算子 `getProcessingTimeService()`/`getTimeServiceManager()` 非 null + 与 invokable 同一实例（构造注入、先于 open）+ open 后 `numTimerServices()==1` 运行时注册证据——门禁自洽，复述 `TestStreamTaskInvokableProcessingTimeWiring` 关键断言不依赖外部类）。
- **先红后绿（证据在案）**：临时删除注册表 `TimerServiceManager` 条目 → 红：`testApiSurfaceCompletenessEveryServiceSetterIsRegistered` FAILED「service injection API(s) not in wiring-registry.json: [setTimeServiceManager(TimerServiceManager)] — a new service injection API must be registered (V4 semantics)」+ `testConsumerTableIsConsistentWithServices` FAILED「references unregistered service」；恢复注册表 → 10/10 绿。
- **归属定案（复核在案）**：无服务 fail-fast 断言不建 core 副本——`TestCepProductionExecutionE2E.testCepProcessingTimeProcessElementFailsExplicitlyWithoutService`（PT 模式无服务 processElement 抛 `StreamException` 含 "ProcessingTimeService" 参数）4/4 绿；`TestProcessingTimeWindowProductionE2E.testProcessingTimeWindowWithoutDriverWarnsExplicitly`（ListAppender 断言 WARN "will never fire"）3/3 绿；端到端证据 = 两 E2E 承担（core 依赖方向约束）。
- **验证**：`./mvnw test -pl nop-stream/nop-stream-core -Dtest=TestWiringExistenceInvariant` 10/10；`TestProcessingTimeWindowProductionE2E` 3/3 + `TestCepProductionExecutionE2E` 4/4；全量 `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（**2895 tests / 0 failures / 0 errors**）。
- **Doc-sync**：catalog #7 草稿节已落（Phase 1）；core-design.md 不变式节 #7 = 记账到 Phase 4。

### Plan `2026-08-13-0805-1` 执行 Phase 1（Decision）：不变式 #7 wiring 注册表与门禁语义设计定稿

- **注册表定稿（新建 `ai-dev/audits/nop-stream-invariants/wiring-registry.json`，design-final）**：services 表 7 条（live 复核 `AbstractStreamOperator` :140/:148/:152/:165/:173/:181/:320）——①`setOutput`（Output，接线点 = `StreamTaskInvokable` :230/:268/:287/:293/:426，注入时机 = 装配布线：链内 ChainingOutput + tail RecordWriterOutput/BroadcastingRecordWriterOutput）②`setProcessingTimeService`（:462，构造）③`setTimeServiceManager`（:463，构造）④`setStateBackend`（`GraphModelCheckpointExecutor:742`，checkpoint 装配）⑤`setSnapshotCallback`（`StreamTaskInvokable:435`，快照）⑥⑦`setKeyedStateBackend`/`setOperatorStateBackend` = **`internal-creation` disposition（显式裁定，理由在案**：生产经 `stateBackend.createKeyedStateBackend()` 在 open() 内直建（`CepOperator.java:257` / `WindowOperator.java:421` / `AbstractStreamOperator.java:65`），setter 为测试注入后门；V1 carve-out 受棘轮约束，非静默豁免）——每服务 = {fqcn, 注入 API（类:方法+参数类型）, 生产接线点（文件:行+注入时机）, 消费方枚举, disposition, test-only 豁免}。
- **消费方表定稿（live 枚举）**：`getProcessingTimeService()` — `CepOperator` :290/:300/:373/:381/:382/:401/:479/:490（无服务 = PT 模式 fail-fast `ERR_STREAM_STATE_ERROR` + open/统计 timer WARN）、`WindowOperator` :479（PT open WARN）；`registerTimerService(` — `WindowOperator` :471（TSM 四环节注册）+ **`ProcessOperator` :46（core 镜像先例，入枚举不遗漏）**；`getTimeServiceManager()` main 零调用点（仅声明）。每消费方 = {类, 调用点（文件:行）, 所属服务, 无服务时行为}。
- **pin schema + 违规语义 V1–V5 定稿**（写入注册表 violationSemantics 节）：V1 调用点 = 成员访问形态（`\.setX\(` / `this.setX\(` / cast 形态 `(Type)).setX(`），方法声明与 javadoc 不计；**receiver 限定**（声明类型 / this / cast 类型 ∈ operator 类面，`CheckpointConfig.setStateBackend` 等非同面方法不匹配）；production-wired main 零调用点 → 红，internal-creation carve-out；V2 消费方所属类 ⊆ 消费方表；V3 接线点（文件:行）失效 → 红；V4 新 `set*` 注入 API（参数类型 ∈ 已登记服务类型）不入表 → 红（`setKeyContextElement1/2`/`setCurrentKey`/`setMailboxExecutor`/`setProgressMarker` 不触发）；V5 失效消费方/服务 → 红；pin 预期零起步（internal-creation = disposition 非 pin）。
- **`invariant-catalog.md` §5 补 #7 草稿节**（状态 = 草稿，Cycle 3 / I1 沉淀中）：不变式陈述 / 覆盖失败族 / 历史证据（open-audit P0-01/P1-02 + 13 测试类 mock 注入规避）/ 检测方法（JUnit + mjs scan-wiring + 注册表）/ internal-creation 裁定 / live 修复状态。
- **Doc-sync**：No owner-doc update required（Phase 1 为设计定稿 Decision，零代码变更；catalog #7 草稿节已落）。

### Mission-driver DRAFT_PLANS 轮：Cycle 3 派生（Loop Rule）+ 3 份 plans（I1/I2/I3，draft review 通过转 active）

- **派生依据（Loop Rule 预授权）**：open-audit P0-01/P1-02 新失败族「运行时服务注入完整性」→ plan `2026-08-13-0132-1` Fix 收口 → roadmap Follow-up Backlog 派生候选（触发 = plan 1 收口后按 Loop Rule 评估）→ 触发条件满足（2026-08-13）→ **正式派生 Cycle 3**。roadmap Work Item 表追加 Cycle 3 / I1–I3 三行（I1 附触发证据 = 发现 `文件:行` + 不变式 #7 陈述；I4–I6 待 I3 裁决后追加——I4 派发范围取决于 I2 red list）；Loop Rule 节补「Cycle 3 派生登记」（稳态暂停裁定不撤销，三选一复触发继续生效）。
- **产出 3 份 plans（执行顺序 {1}→{2}→{3} 硬串行，文件按 `2026-08-13-0805-N` 前缀排序）**：
  - `2026-08-13-0805-1-nop-stream-invariants-cycle3-I1-wiring-existence-gates.md`（**{1}**）— 不变式 #7 沉淀：接线点注册表 `wiring-registry.json`（7 服务注入 API × 生产接线点 × 消费方枚举）+ `TestWiringExistenceInvariant`（core，API 面完备性 + 接线连通）+ mjs `scan-wiring`（V1–V5：仅测试注入检测 / 消费方枚举完备性 / 失效接线点 / 新注入 API / 失效消费方）+ self-test + committed 回归测试。**执行前裁定在案**：`setKeyedStateBackend`/`setOperatorStateBackend` main 零调用点 → `internal-creation` disposition（V1 carve-out 附理由，受棘轮约束，非静默豁免）；V1 调用点 = 成员访问形态 + receiver 限定（防声明假绿 / 跨类同名假绿）。
  - `2026-08-13-0805-2-nop-stream-invariants-cycle3-I2-invariant-driven-audit.md`（**{2}**）— I1 门禁跨全部服务注入 API 与消费方跑 → 确定性 red list；聚焦对抗探查（checkpoint/watermark 服务同族"仅测试注入"复探 / 注入-恢复时序组合面 / 恢复路径行为盲区 / TimestampsAndWatermarksOperator residual 复核）+ P2 backlog 触发评估（open-audit 10 条 + multi-audit 23 条，候选预枚举）；Anti-Hollow 运行时证据 = 生产接线 E2E 复跑实跑（Phase 4）。
  - `2026-08-13-0805-3-nop-stream-invariants-cycle3-I3-adjudication.md`（**{3}**）— red list 逐条裁决（P0–P3）→ P0/P1 派 I4（六要素 + 类别清扫 + 收尾三连）；新族 → Cycle 4 / I1 派生登记（PD-16 规则）；P2/P3 → backlog；零悬挂；纯决策计划（零代码变更，构建门禁按纯文档条款删除）。
- **Draft Review（独立子 agent，全部 2 轮达成共识）**：round 1 每 plan 各 1 个 fresh session（I1：2 Blocker + 4 Major（含 `internal-creation` 首跑必红与 core 依赖方向两大实质缺陷）；I2：2 Major + 6 Minor；I3：1 Major + 4 Minor）；全部修复；round 2 各 1 个 fresh session（I1/I2 verdict 可执行；I3 修复后 APPROVE），残余 Minor 均并入 → 三 plan 全部 `Plan Status: active`（Draft Review 记录写入各 plan 头部）；roadmap Cycle 3 三行 `todo`→`planned`。
- **关键审查裁定（供执行参考）**：① fail-fast 行为断言归属 cep/runtime 既有 E2E（`TestCepProductionExecutionE2E`「PT 模式无服务 fail-fast」+ `TestProcessingTimeWindowProductionE2E` WARN 断言）——core 门禁类不可引用 cep/runtime；② I2 执行前必须过机械预检（I1 completed + 注册表/落档文件存在 + `scan-wiring` 在 `all` 输出 + 门禁类 ≥ 11），否则 `blocked`；③ I3 执行前必须过机械预检（I2 completed + red-list 头部 = Cycle 3 权威版 + probing report 存在），防陈旧 Cycle 2 基线被误裁；④ P2 计数 = open-audit 10 + multi-audit 23（roadmap 头注「21 条」为陈旧计数，执行时以 live 为准）。
- **遗留（显式 successor ownership）**：三 plan 执行队列由 mission-driver EXEC_PLANS 驱动（{1}→{2}→{3}）；Cycle 3 / I4–I6 待 I3 裁决后按裁决表另立（roadmap 行随附追加）；P2 backlog 触发条件在案（I2 Phase 3-e 评估）；`HG-01` 人工确认待办延续。


### Plan {3} closure audit + 收口：`2026-08-13-0132-3-nop-stream-operator-deploy-contract-fix`（P1-03/P1-04/multi P0-01/multi P1-01 全部落地，独立 closure audit APPROVE，plan 转 completed）

- **独立 closure audit**（fresh session `ses_00780e2c8ffex9HFYwnFbFfbPi`）：四 Phase Exit Criteria 逐条对照 live working tree PASS（Phase 1 beans 模板 grep 零命中 + `<constructor-arg>` 与 live 构造器签名逐一对应 + 负对照测试；Phase 2 `fanOutWriters` 全列表字段 :79 + `closeOutputWriters` :509-536 + `SupervisionLoop:762-772` + `TestFanOutBoundedE2E` 双 EOS 断言；Phase 3 `CepOperator.open()` :283-285 null 守卫 + `onEventTime` :603-605 removeIf + `TestCepEventTimeTimerRegistry` 3 用例；Phase 4 `TestWindowOperatorCorrectness` 双用例双错误码 + state 注入）；Anti-Hollow PASS（3 改动 main 文件 + 3 新测试文件无空体/静默 no-op/吞异常）；verdict APPROVE 无条件。
- **本 closure run 验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl nop-stream -am -T 1C` BUILD SUCCESS；`check-nop-stream-invariants.mjs all` exit 0（**执行中发现并修复**：output-contract 注册表 CepOperator 发射点 500/794 未随 P1-04 编辑重钉（只更新了 updated 注释）→ 重钉 507/814，V4/V5 4 条违规清零）；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations --severity high` findings-set = 仓库级既有债 14 条（全部未改动文件，本次改动零命中——按 plan 1/2 同款裁定，非 exit code）；checkstyle = 仓库级既有债，本次改动文件逐行对比基线零新增命中（新增 javadoc 超长行已修至 ≤80，hunk 内残余 68/69 等为 pre-existing JavadocVariable）。
- **收口**：plan Closure Gates 15 条全勾选 + Closure 段填写（evidence = `ses_00780e2c8ffex9HFYwnFbFfbPi`）→ `Plan Status: completed`；roadmap 补 plan 3 ✅ 收口段；multi-audit `Audit Status: planned → closed`（P0-01/P1-01 → plan 3 已收口）；open-audit 维持 closed（idempotent）。
- **遗留**：无 in-scope follow-up；Non-Blocking（CEP timer 记账结构对齐真实触发机制 / 注释模板转 live bean）在案。

### Plan {3} 执行 Phase 4（multi P0-01）：`2026-08-13-0132-3-nop-stream-operator-deploy-contract-fix` merge fail-fast 回归测试（先红后绿）

- **测试（Proof，先红后绿证据在案）**：`TestWindowOperatorCorrectness` 新增 2 用例——
  - ①`testMergeTwoRawWindowValuesFailsFast`（**双 raw 主路径**）：`MixedTypeWindowOperator(useAccumulator=false)`（raw 模式，`createAccumulatorForWindow` 返回 null）→ 元素 (10,t=10)/(20,t=100) 分别落窗口 [10,60)/[100,150) 存 raw 值 → 桥接元素 (30,t=55) 触发 3 窗口合并 → `assertThrows` + 断言 `ERR_STREAM_WINDOW_NON_ACCUMULATOR_MERGE_CONFLICT`。
  - ②`testMergeAccumulatorWithRawValueFailsFast`（**accumulator+raw 场景**）：`useAccumulator=true`（双窗口均 accumulator）→ **state 注入**：测试用 `getKeyedStateBackend().getMapState(new MapStateDescriptor<>("window-contents", String.class, Object.class))`（与算子 open() 默认 accClass=Object 路径创建的 descriptor 同名同型 → `verifySchemaCompatibility` 通过、共享同一 MemoryMapState）经 `setCurrentKey("key1")` + `setCurrentNamespace("TW:start,end")` 向 **source 窗口** namespace 写入 raw 999（`__window_value__` 键）；target/source 选择由 `MergingWindowSet.addWindow` 的 `mergedWindows.iterator().next()`（HashSet 序）决定——测试**复刻同一 HashSet 先学 target**，注入落在另一个（source）窗口，保证断言确定性：target=accumulator + source=raw → `ERR_STREAM_INVALID_STATE`。
  - **红（实证）**：临时把 `WindowOperator.mergeWindowContents` 两处 throw 改为 `if(false)` 静默跳过 → 2/2 新测试失败（不再抛异常）；恢复 throw 后 2/2 绿（fail-fast 行为被锁定，删 throw 不再"全绿"）。
- **裁定（Decision）**：删死类 + 改名保留路径**本 plan 不可用**——双 raw 场景已实证可构造（`useAccumulator=false` 走通），前提不成立；`MixedTypeWindowOperator` 由零实例化死类转为 2 处真实实例化（false+true 双开关），测试注释记录保留路径裁定防后续误走。
- **验证（Proof）**：`ERR_STREAM_WINDOW_NON_ACCUMULATOR_MERGE_CONFLICT` / `ERR_STREAM_INVALID_STATE` 均在断言代码真实引用（grep 命中），fail-fast 路径零覆盖缺口关闭。
- **回归**：`TestWindowOperatorCorrectness` 全类 9 tests / 0 failures + `TestWindowOperatorBuilder`(10) + `TestPaneInfoAndAccumulationMode`(10) + `TestTriggerAccumulatorsCheckpoint`(2) + `TestWindowRoundTripInvariant`(9) 全绿。
- **Doc-sync**: No owner-doc update required（测试修复面，无 owner-doc 变更）。

### Plan {3} 执行 Phase 3（open P1-04）：`2026-08-13-0132-3-nop-stream-operator-deploy-contract-fix` CEP 事件时间 timer 注册表 open/restore 对称

- **修复（Fix）**：`CepOperator.open()`（:278）`registeredEventTimeTimers` 初始化改为 `if (registeredEventTimeTimers == null)` 守卫——restoreState 可能在 open() 之前执行（`TestCepCheckpointRestoreE2E` 钉死顺序），原先无条件 `new TreeSet<>()` 覆盖恢复结果（AR-9 存储侧已持久化、消费侧 open 即丢）。
- **裁定（Decision）**：注册表语义 = (b) 记账结构 + 清理路径补齐（非 (a) 真实触发机制）——`getSortedTimestamps()` 直接读 `elementQueueState.keys()`、onEventTime 由 watermark 整批推进，注册表（`registerEventTimeTimer`/`deleteEventTimeTimer`/`forEachEventTimeTimer`/snapshot 全量拷贝）是 pending timer 台账，从不驱动执行；接入真实触发机制会与现有 watermark 驱动路径重复且无消费方。**onEventTime 末尾清理**：`registeredEventTimeTimers.removeIf(t -> t <= time)`——条目职责在 watermark 到达时完成（STEP 2 桶已消费 + STEP 3-5 窗口清理已执行），删除过期条目解决"无界增长"（注册表全量进 checkpoint，增长即泄漏）；恢复的注册表只含真正 pending 的 timer。deleteEventTimeTimer 保持原删除语义（记账结构的清理 API，有行为）。
- **先红后绿（证据在案）**：临时回退两个行为 hunk（保留测试 accessor）运行新测试 → 3/3 红：`testRestoredTimersSurviveOpen` expected [10,20] but was []（open 覆盖）、`testSnapshotRestoreRoundTripKeepsTimers` expected [10,35,20] but was []、`testOnEventTimeCleansUpConsumedTimers` expected [20] but was [10,20]（无清理）；恢复修复后 3/3 绿。
- **测试（Proof）**：新增 `TestCepEventTimeTimerRegistry`（cep/operator，3 用例）——①restore→open 顺序：缓冲 ts=10/20 → snapshot → restoreState → open → 注册表仍为 {10,20}；②快照-恢复往返：snapshot→restore→snapshot→restore 两次往返 {10,20,35} 不丢；③清理：watermark 15 后注册表 {20} + 桶 10 已消费，watermark 30 后注册表空 + 队列空（无界增长路径关闭）。配套 `CepOperator.getRegisteredEventTimeTimersForTesting()` package-private accessor（Testing Methods 区）。
- **回归**：`./mvnw test -pl nop-stream/nop-stream-cep` 全模块 327 tests / 0 failures（含 `TestCepCheckpointRestoreE2E` / `TestCepOperatorStateRecovery` / `TestCepProductionExecutionE2E`）。
- **Doc-sync**: `source-anchors.md` STRM-032/037 锚点文件级无漂移（描述 keyed state backend 与 cache-statistics timer，不涉注册表语义）→ No owner-doc update required。

### Plan {3} 执行 Phase 2（open P1-03）：`2026-08-13-0132-3-nop-stream-operator-deploy-contract-fix` fan-out 全 writer 关闭（EOS 完整送达）

- **修复（Fix）**：`StreamTaskInvokable` 新增 `private final List<RecordWriter<Object>> fanOutWriters` 字段——两个 fan-out 构造器（`(chain, fanOutWriters)` / `(chain, fanOutWriters, inputGate)`）此前只保留 `outputWriter = fanOutWriters.get(0)`、丢弃完整列表；新增 `getFanOutWriters()` + `closeOutputWriters()` 私有方法（显式遍历全部 writers 关闭，首错重抛其余 suppressed，镜像 `RecordWriter.close()` 语义）。`invokeSource` finally（保留 `sourceError == null` 成功路径条件——重启语义）与 `invokeMiddle` finally 均改调 `closeOutputWriters()`。`SupervisionLoop.rebuildTask` 生产者角色路径（:750-760）改为 `oldInvokable.getFanOutWriters()` 非空时 `new StreamTaskInvokable(newChain, oldFanOutWriters, null)`（重启后的 fan-out 生产者喂全部边，不只边 1），否则回退单 writer 原路径。
- **先红后绿（证据在案）**：临时红测试 `TempFanOutRedEvidenceTest`（env.fromElements(1..10) → 双 sink，`@Timeout(15)`）对 pre-fix 代码（git stash 主代码）运行 → `TimeoutException: boundedFanOutJobTerminates() timed out after 15 seconds`（作业悬挂：边 2 的 writer 永不 close → sink2 gate 永久轮询 → `awaitCompletion` 永不返回）；删除临时文件 + 恢复修复后 → 正式测试全绿。
- **测试（Proof，Rule #22/#23/#24）**：新增 `TestFanOutBoundedE2E`（core/integration，2 用例）——①`testBoundedFanOutJobTerminatesWithAllSinksEos`：env 全栈（addSource → JobGraphGenerator → GraphExecutionPlan → TaskExecutor → StreamTaskInvokable）一分流双 sink，双 sink 各收全 [1..10] 且作业终止；②`testFanOutCloseTraversesAllWriters`：invokable 级装配（双 writer 双独立 partition + 有界 source），断言 `getFanOutWriters().size()==2`、invoke() 后双 channel drain 到 EOS（各 5 条 record + isFinished）——证明边 2 的 EOS 由运行时 close 路径真实发出、无 writer[0]-only、无经 `RecordWriterOutput.close()` no-op 的静默跳过。
- **回归**：core `TestFanOutBoundedE2E`(2) + `TestParallelGraphExecution`(8) + `TestGraphExecutionPlan`(10) + `TestGraphModelExecution`(7) + `TestE2ESimplePipeline`(4) + `TestDataStreamPipeline`(7) + `TestSubtaskExecution`(5) + `TestConcurrencySafety`(2) + `TestStreamTaskInvokableProcessingTimeWiring`(3) 全绿；runtime `TestSideOutputChainingE2E`(4) + `TestSupervisionLoop*` 全系（20 tests，含 ProducerRegionRestart / ReconnectE2E / CheckpointReconnectE2E / ConsistentCut）全绿。
- **Doc-sync**: No owner-doc update required（fan-out 关闭行为属内部执行面，`source-anchors.md` 无相关锚点漂移）。

### Plan {3} 执行 Phase 1（multi P1-01）：`2026-08-13-0132-3-nop-stream-operator-deploy-contract-fix` beans.xml 注释内部署模板语法修正

- **修复（Fix）**：`stream-control-rpc.beans.xml` 注释内模板（:37-58）——删除 `ioc:configMethod` 占位，`StreamControlRpcServer` 5 参构造全部改 `<constructor-arg index="0..4">`（serviceName/serviceInterface/serviceImpl/messageService/topic），`StreamControlRpcProxyFactory` 4 参构造改 `<constructor-arg index="0..3">`；`stream-data-plane.beans.xml` 注释内模板（:36-49）——`RpcDistributedExecutor.messageService`（private final 构造器参数）改 `<constructor-arg index="0" ref="streamMessageService"/>`，`ioc:bean="true"` 改嵌套 `<property name="dataPlaneWireCodec"><bean class="...SysDaoWireCodec"/></property>`（`RpcDistributedExecutor` 的 `dataPlaneWireCodec` 是 setter 属性，property 注入合法）。
- **验收机制（Proof）**：新增 `TestStreamBeansDeploymentTemplateSyntax`（runtime/ioc，3 用例）——①control-rpc 模板：提取注释内 `<bean>` 片段（首 `<bean` 至末 `</bean>`），断言 5+4 个 `<constructor-arg>` 齐全、零 property-注入-仅构造器类；②data-plane 模板：断言 `<constructor-arg index="0" ref="streamMessageService"/>` + 嵌套 `<bean>`；①②均将片段包裹 `<beans x:schema="/nop/schema/beans.xdef" xmlns:x="/nop/schema/xdsl.xdef" xmlns:ioc="ioc">` 根后经 `DslModelParser().parseFromNode` 全量 schema 校验（check-ns=ioc 下未知属性直接 throw）；③负对照：`ioc:configMethod` 片段解析必抛（证明 schema 校验真实生效，防假绿）。
- **照抄演练（Proof）**：模板片段语法对照 beans.xdef 支持面（`<constructor-arg index="!int" ref/value>` beans.xdef:127-130；嵌套 `<bean>` = BeanPropValue union 成员 beans.xdef:15-18）；两文件全文件 grep `ioc:configMethod` 与 `ioc:bean(?!-)`（负向前瞻防误匹配 `ioc:bean-method`）零命中。
- **回归**：`TestStreamBeansDeploymentTemplateSyntax`（3）+ `TestStreamModuleDiscovery`（2）+ `TestStreamControlRpcBootstrap`（2）全绿 = 7 tests / 0 failures（后两者加载文件本体，注释内模板零影响，验证无回退）。
- **Doc-sync**: No owner-doc update required（docs-for-ai 零引用这两个 beans.xml 与模板语法）。

### Plan {2} closure audit + 全量验证：`2026-08-13-0132-2-nop-stream-checkpoint-recovery-fix`（P0-02/P0-03/P1-01/P1-05 全部落地，独立 closure audit APPROVE，plan 转 completed）

- **独立 closure audit**（fresh session，`ses_007ce280dffegTfR825EljNl8Y`）：4 个 Phase 的 Exit Criteria 逐条对照 live code PASS（证据 = 文件:行 + 测试名 + 独立复跑结果）；Anti-Hollow PASS（21 个改动文件无空方法体/静默吞异常，fail-fast 在案；`check-nop-stream-invariants.mjs all` exit 0）；verdict APPROVE（条件：Phase 3/4 daily-log 条目 + scan-hollow gate 文本对齐——本条 + plan 文本修订已落地）。
- **全量回归**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS——core 1453 / runtime 816 / rocksdb / cep / connector 全绿（合计 0 failures / 0 errors）；mjs `all` exit 0（output-contract 注册表 WindowOperator 行号 1049→1136 / 1879→1966 同步）；`scan-hollow-implementations --severity high` 维持仓库级既有债 14 条（全部为未改动文件，本次改动零命中——按 plan 1 同款裁定：findings-set 相等即通过，非 exit code）。
- **Doc-sync**：`gate-inventory.json` 补 `advanceCheckpointIdCounterAfterRestore`（CheckpointCoordinator 表，表完备性门禁）；`source-anchors.md` 锚点文件级无漂移；roadmap 收口段 + audit 状态流转（下节）。

### Plan {2} 执行 Phase 4（P1-05）：`2026-08-13-0132-2-nop-stream-checkpoint-recovery-fix` InputGate.markFinishedChannel 竞态修复

- **修复（Fix）**：`InputGate.markFinishedChannel` 收集**全部** fullyReceived alignment 后按 checkpointId 升序逐个发射——首个立即发射（`emitCompletedAlignment`：remove + unblock + cleanupAbortedBarriersUpTo），其余入 `pendingBarrierEmissions` 队列（**仍留在 `inFlightAlignments`，发射时才 remove**——避免完成项干扰 `oldestAligning()`/超时门/unaligned D4 guard）；`readMultiChannel` retry 循环顶部 drain 队列（一次 read() 一个），pending 项已被 `abortBarrierAlignment` 中止时经 `abortedBarriers` 丢弃并恢复其 blocked channels（不阻塞后续发射）。AT_LEAST_ONCE 分支保持原 remove-at-full-receipt 语义。替换原"桶序取首个完成 + 只 remove 第一个"逻辑（后访问者 `completed != null` → 泄漏 fullyReceived alignment → 成为 oldestAligning → 超时/unaligned 门永久屏蔽 + barrier 静默丢弃）。
- **触发可达性裁定（执行记录）**：aligned 模式正常读路径（per-channel 有序 barrier + channel 阻塞）两 alignment 无法同时在途；确定性复现 = 测试经公开 API `resumeConsumptionAll()`（与生产 abort 路径同 API）在 align(N) 在途时放行 barrier N+1，使两 alignment 同缺最后一个 channel，finish 该 channel → 同轮完成。
- **测试（Proof，先红后绿证据在案）**：新增 `TestInputGateMarkFinishedChannelRace`（core/execution，3 用例）——①同轮完成：read() 按序返回 `[5,6]`、`inFlightBarrierIds` 空（**红**：修复前 emitted=`[5]` 且泄漏 `[6]`）；②超时门不被屏蔽：两 barrier 发射后新 barrier 7 对齐超时抛 `ERR_STREAM_BARRIER_ALIGNMENT_TIMEOUT`（**红**：修复前泄漏项屏蔽门，7 永不超时）；③abort 丢弃 pending 不阻塞后续：B5 发射后 abort(6)，B7 仍正常发射（latch 同步保证 abort 落在 pending 窗口内）。
- **回归**：`TestInputGate*` 全系 55 tests / 0 failures。
- **Doc-sync**: No owner-doc update required（`source-anchors.md` STRM-021 文件级锚点无漂移）。

### Plan {2} 执行 Phase 3（P1-01）：`2026-08-13-0132-2-nop-stream-checkpoint-recovery-fix` 窗口 aggregate/reduce 状态恢复（方案 1：live 函数复用，双后端）

- **裁定（Decision，执行记录）**：方案 1 = 恢复时优先使用 live 算子持有的 descriptor 的 aggregate 函数实例——`IKeyedStateBackend` 新增 `registerRestoreAggregateFunction(stateName, fn)` / `getRestoreAggregateFunction(stateName)` default 方法（Memory/RocksDB 双后端实现）；`WindowOperator.open()/restoreState()` 在 deferred restore（`applyPendingRestoreState`）前注册 descriptor 的 live 函数。方案 1 收益：旧快照（修复前写入）同样可恢复（serde 恢复路径对 valueType=Object 的旧快照按 live 函数 `createAccumulator()` 推断累加器类型）；方案 2/3（序列化函数实例/可序列化数据类包装）只救新快照且改快照格式，弃。
- **修复（Fix）**：
  - `MemoryStateSerDe.resolveAggregateFunction` / `RocksDBSnapshotSerDe.resolveAggregateFunction`：live 函数优先；无 live 且类名反射无 no-arg 构造器 → 显式 fail-fast（`StreamException` + 明确错误消息，Rule #24）。
  - **执行中发现并修复的阻塞性生产缺陷（全部阻断本 Phase 的作业级重启 E2E，属必要接线工程）**：
    1. **算子状态 JSON 持久化不可用**——`CheckpointSerDe` 直接 JSON 化 `HeapInternalTimerService.TimerSnapshot` / `WindowOperator.PaneTrackingSnapshot` / `SimpleAccumulator` 值报 "only DataBean is serializable" → 窗口作业 checkpoint 持久化必败（`TestTimerCheckpointRestoreE2E` 只测内存路径所以未暴露）。修复：serde 边界（`CheckpointSerDe.serialize/deserializeTaskStateSnapshot`）把三类算子状态转自描述 JSON-safe form（DTO 上 `toSerializableForm()/fromSerializableForm()`，`@type` 标记路由还原；TimeWindow namespace 序列化对齐 MemoryStateSerDe 模式；accumulator 以类名+localValue 重建），in-memory 类型语义不变。
    2. **`SubtaskTask` 双重打开算子链**——`openOperatorChains()` + invokable `invoke()` 内 `operatorChain.open()` 打开同一链两次：首次 open 的 restore 状态被二次 open 重建的 backend 丢弃（恢复必空）+ RocksDB 自持 LOCK（`lock hold by current process`）。修复：移除 `SubtaskTask.run()` 冗余 open（invokable 四角色均自行 open），删除 `openOperatorChains` 方法。
    3. **重建作业 transformation/vertex id 漂移**——全局静态计数器跨 `env.execute()` 递增 → 同构重生成作业的 fingerprint（dagTopologyHash）与 task-location 全不匹配 → 作业级重启永远无法恢复（`fingerprint incompatible` / `stateful vertices absent`）。修复：`buildStreamModel` 按（name + 出现序）分配稳定 map key 与稳定 id（`Transformation.assignStableId`，name-hash + per-build 冲突探测）；`StreamModel.computeFingerprint` 组件 hash 改稳定结构属性（class+name+parallelism+outputType；原 `Object.toString()` 含 identityHashCode 永不稳定）；`isCompatibleWith` 语义不变。
    4. **窗口 contents 累加器类型为 `Object.class`**（`WindowedStreamImpl` 泛型擦除传 Object）——RocksDB 读路径（`RocksDBInternalAggregatingState`/`RocksDBAggregatingState` add/get 反序列化按 descriptor valueType=Object）把 `long[]` 累加器还原成 ArrayList → 用户 `add` CCE（run-1 第二次 add 即炸）；快照 valueType=Object 无法驱动恢复。修复：`WindowOperatorFactoryImpl.createAggregateOperator` 从 live 函数 `createAccumulator()` 推断真实累加器类型（reduce 包装类返回 null 时保持 Object，String 累加器 JSON-native 无碍）；RocksDB aggregating state 构造期解析存储值类型（`resolveStorageValueType`，`type == Object` 时推断，否则用 descriptor 类型）；serde 恢复路径对旧快照（valueType=Object）做同样推断（旧快照兼容）。schema checksum 随之一致（新快照记录推断类型 + 对应 checksum）。
    5. **`KeyedStreamImpl.FieldAggregationReducer` 原地改写存储累加器**——同 JVM 通道共享引用 → 已发射记录被后续 reduce 追溯改写（timing 相关错值，`[1,2,4,6,9]` 变 `[9,6,9,6,9]`；transformation id 变更改变了任务启动序后暴露）。修复：每次 reduce 返回防御性拷贝（no-arg 构造器 + 层级反射字段拷贝；缺 no-arg 构造器显式 fail-fast），测试 `Item` 补 no-arg 构造器。
    6. **`AbstractStreamOperator.close()` 不关闭 keyed state backend**——RocksDB 目录锁跨作业重启/监督重试残留（`lock hold by current process`）。修复：close 时关闭 keyed backend（不置 null，保留测试后检视；`RocksDBKeyedStateBackend.close` 幂等化——defaultCF 置 null）。
- **测试（Proof，先红后绿证据在案）**：
  - 往返单测：`TestDescriptorAggregatingStateRestore`（Memory，2 用例：`assertSame(liveFn, restoredDescriptor.getAggregateFunction())` + offset 行为证明 7+100=107 + 无 live 注册 fail-fast）+ `TestRocksDBDescriptorAggregatingStateRestore`（RocksDB，2 用例同型）。**红**：修复前恢复抛 `NoSuchMethodException`（capturing 匿名类无 no-arg 构造器）。
  - 作业级重启 E2E：`TestE2EWindowAggregateRestore`（runtime/checkpoint，4 用例）——aggregate/reduce × Memory/RocksDB；同一 checkpoint storage 二次 `env.execute()`（经 `CheckpointExecutorFactoryImpl` 公共入口，测试 `@BeforeAll` 注册 factory——**执行中发现** env 路径不自动发现 checkpoint executor factory，此前 E2E 静默无 checkpoint）；慢源设计（burst 全量 + 800ms 静默期，周期 checkpoint 在窗口 fire 前完成全量捕获）；断言 run1 `[55]` → run2 `[210]`（restored 55 + new 155，空恢复会 100）；reduce run1 `1|2|...|10` → run2 `1|...|10|11|...|20`。**红**（修复前）：persist 失败（TimerSnapshot 不可序列化）→ 无 manifest → run2 空恢复。
- **回归**：`TestTriggerAccumulatorsCheckpoint` / `TestCheckpointRecovery` / `TestWindowRoundTripInvariant` / `TestWindowOperatorBuilder` / `TestTimerCheckpointRestoreE2E` / `TestKeyedStreamAggregation` / `TestProcessingTimeWindowProductionE2E` 全绿；`TestWindowRoundTripInvariant` 期望更新（AGGREGATE call-site 的 accClass = 工厂推断类型，非 Object——新增契约，测试内注释在案）。
- **Doc-sync**: No owner-doc update required（`source-anchors.md` 文件级锚点无漂移；跨模块 API 变更记录于本 log + `IKeyedStateBackend` javadoc）。



- **修复（Fix）**：
  - `GraphModelCheckpointExecutor`：`registerTasksAndTrackers` 抽取为 `wireTaskCheckpointPipeline`（package-private static，tracker + registerTask + listener/participant 注册 + state backend 供给，供重建路径复用）+ 新增 `unwireTaskCheckpointPipeline`（旧链算子/UDF 的对偶移除，防多次重启 stale listener 累积）；注入列表改 `CopyOnWriteArrayList`（barrier scheduler 线程迭代 vs supervision 线程 remove/add 并发安全，普通 ArrayList 会 CME 被 scheduler catch 吞掉→tick barrier 静默丢失）；`submitAndRun`/`SupervisionLoop.run` 增加 `allInvokables` + `checkpointConfig` 参数（后者供重建链按配置供给 state backend——Phase 3 预登记的"区域重启 state backend 重新供给"工作按 plan 条款显式并入 Phase 2 修复项 (c)，RocksDB 配置作业区域重启不再静默回退 Memory）。
  - `SupervisionLoop.rebuildTask`：重建 invokable 后 `rewireCheckpointPipeline`——unwire 旧链 → 注入列表 remove old + add new（replace 语义）→ `wireTaskCheckpointPipeline`（setBarrierTracker 触发 setupSnapshotCallbacks → snapshotCallback 非 null）。
  - **TaskLocation 族修正（执行中发现的新缺陷，P0-02 路径内）**：`rebuildTask` 原用 `oldSubtask.getTaskLocation()`（execPlan 构造，jobId=jobGraph 名 + pipeline-0），而 checkpoint 管线（tasksToAcknowledge / ACK 路由 / 状态恢复）全部以 checkpointPlan 位置（config jobId/pipelineId）为键——两族值不同时重建任务 ACK 永不匹配 → 重启后 checkpoint 必然超时 abort（且 `getTaskState` 恢复查找失败）。修正：`findTaskLocationInPlan` 改 package-private，rebuildTask 经 checkpointPlan 解析位置。
- **测试（Proof，先红后绿证据在案）**：
  - E2E `TestSupervisionLoopCheckpointReconnectE2E`（runtime/execution）——2 区域（materialization 边）+ 慢源（60×40ms≈2.4s）+ interval=300ms/timeout=1500ms（首触发在重启完成后、作业在 abort 时点仍在运行，杜绝真空通过）；走 `executeWithCheckpoint` 公共入口；绿态断言 = 无异常 + storage ≥2 manifests（full-ACK 证据）+ 60 条记录恰好一次。**红**（fix stash）：`nop.err.stream.checkpoint-aborted`（重启后 checkpoint 超时 abort）→ ERROR；**绿**：PASS。
  - 接线单测 `TestSupervisionLoopCheckpointRewireWiring`——直调 `SupervisionLoop.run`（真实 coordinator + COW 列表）：重建 invokable ≠ 旧实例、tracker 非 null、注入列表含新不含旧、存活源任务仍在列表、重建链算子 state backend 已供给、participant 重注册不累积（共享 UDF 注册数恒为 1，coordinator shutdown 后归 0）、20 条记录恰好一次。
- **回归**：`TestSupervisionLoop*` 全系 + checkpoint 相关 37 tests / 0 failures（含既有 `TestSupervisionLoopReconnectE2E` 全部用例）。
- **Doc-sync**: `source-anchors.md` STRM 锚点文件级无漂移 → No owner-doc update required。
- **Phase 2/3 边界记录**：state backend 重新供给已并入 Phase 2 修复项 (c)（wireTaskCheckpointPipeline + 线程化 config）。

### Plan {2} 执行 Phase 1（P0-03）：`2026-08-13-0132-2-nop-stream-checkpoint-recovery-fix` manifest 恢复后 checkpoint ID 计数器单调推进

- **修复（Fix）**：`CheckpointCoordinator.advanceCheckpointIdCounterAfterRestore(restoredId)` 新增公共方法（与 `restoreFromCheckpoint` :896-900 同逻辑——`max(restoredId+1, current)` 单调推进，counter 已在 restoredId 之上时不动）；`GraphModelCheckpointExecutor.restoreFromCheckpoint` manifest 恢复成功分支（epoch R 恢复后、fingerprint 校验前）调用该方法——新 checkpoint ID 恒 > R，不再重入影子窗口 [0, R)。
- **测试（Proof，先红后绿证据在案）**：新增 `TestE2EManifestRestoreIdAdvance`（runtime/checkpoint，2 用例）：
  - E2E `testManifestRestoreAdvancesCheckpointIdAcrossRepeatedRuns`——同一 checkpoint storage 连续三次 `executeWithCheckpoint`（慢源 10×60ms、interval=100ms、minPause=150ms），每次断言 latest manifest epoch 严格递增。**红**（仅 executor 调用点 stash）：`shadow-window re-entry detected: run2=4, run3=4` → FAILURE；**绿**（修复在案）：2/2 通过。
  - 单元 `testAdvanceCheckpointIdCounterMonotonicSemantics`——manifest 恢复（epoch 7）→ trigger → ID 8（>R）；counter=10 时 advance(5) 不动 → ID 10；advance(15) → ID 16。
- **回归**：`TestCheckpointRecovery` + `TestE2EMultiVertexCheckpoint` + `TestCheckpointCoordinator` + `TestE2ECheckpointAndRecovery` + `TestCheckpointEndToEnd` + `TestE2EMultipleCheckpoints` 共 47 tests / 0 failures。
- **Doc-sync**: No owner-doc update required（source-anchors STRM-016/STRM-024 文件级锚点无漂移）。
- **Anti-Slacking 边界登记**（plan 原文在案）：manifest 恢复不装配 `latestCompletedCheckpoint` 的回退边界 = out-of-scope（Non-Blocking Follow-ups 复探评估）。

### Plan {1} 执行收口：`2026-08-13-0132-1-nop-stream-runtime-service-wiring`（P0-01 + P1-02，Phase 1+2 全部落地，全量回归绿）

- **Phase 1（生产接线 + TimerServiceManager 全链路）**：
  - 新组件：`TaskProcessingTimeService`（生产 `ProcessingTimeService` 实现，volatile `nextTimerTimestamp` 跨线程只读）+ `ProcessingTimeServiceDriver`（daemon 调度线程，100ms tick + 到期检查，只投递 mail 不执行回调）+ `TimerRegistrationFuture`（`ScheduledFuture` 注册句柄，cancel 移除注册）。
  - 接线：`StreamTaskInvokable.setupProcessingTimeServices()`（**构造函数**无条件注入 PTS/TSM 到算子链；驱动 start/shutdown 在 `invoke()` 起始/finally）。F2 前提修正：`SubtaskTask.run()`/`Task.run()` 在 invoke() 之前 open 链（实测），注入挂构造函数（先于任何 open），驱动挂 invoke()（计划 Exit Criteria 按意图满足，偏差记录在 plan 执行记录 §B）。
  - TimerServiceManager 四环节：生产创建（invokable 构造）/ `setTimeServiceManager`（invokable 构造）/ `WindowOperator.open()` `registerTimerService`（镜像 ProcessOperator）/ 驱动 `fireProcessingTimeTimers`（mail 动作）。`TimerServiceManager.timerServices` 改 `CopyOnWriteArrayList`（open 期注册 vs 驱动迭代安全）；`HeapInternalTimerService`/`TimerServiceManager` 增 volatile due 查询（`nextProcessingTimeTimer`/`hasProcessingTimeTimersDue`）。
  - 守卫：CepOperator `registerCacheStatisticsTimer` null 守卫 + WARN；PT 模式 `processElement` 无服务显式抛 `StreamException`；WindowOperator PT assigner 无 PTS 时 open WARN（"will never fire"）。
  - 决策（plan 执行记录 §A 落档）：回调 = task 线程（mailbox）；驱动 daemon + invoke finally shutdown；触发 = tick + volatile due 检查（非空跑）；异常策略 = PTS 直接回调 fail-fast（任务可见失败）+ TSM 保留既有 catch+log。
- **Phase 2（PT 窗口生产驱动 + cleanup 验证）**：`TestProcessingTimeWindowProductionE2E`（3 用例）——env 全栈 E2E（慢源 41ms 节拍 / 300ms 窗口互质时序 + 末尾静默期收尾 drain 触发末窗）输出断言；invokable 级装配 cleanup 断言 `numProcessingTimeTimers()==0` + `window-contents` map 全空；无驱动 WARN 断言（Logback ListAppender）。
- **测试新增**：core `TestTaskProcessingTimeService`（8）+ `TestProcessingTimeServiceDriver`（9）+ `TestStreamTaskInvokableProcessingTimeWiring`（3：非 checkpoint 注入生效 / 全角色注入 / 回调线程==task 线程）；cep `TestCepProductionExecutionE2E`（4：事件时间 + PT 模式 env 生产路径 E2E、open 无服务不 NPE、PT 模式无服务 fail-fast）；runtime `TestProcessingTimeWindowProductionE2E`（3）。
- **回归**：`./mvnw test -pl nop-stream -am -T 1C` 全绿（core 1448 / runtime 807 / cep 324 / rocksdb 83 / connector 等 0 失败）；mjs `all` exit 0（output-contract 注册表 4 处行号同步：CepOperator 483→500 / 777→794、WindowOperator 1030→1049 / 1860→1879）；`scan-hollow-implementations --severity high` exit 0。
- **文档同步**：`mailbox-design.md` §7「未接线→已接线（PT timer）」；`time-model-design.md` §10.3 生产接线契约；`source-anchors.md` STRM-037 接线状态更新。
- **遗留**：roadmap 门禁沉淀候选维持 `todo`（Cycle 3 / I1 派生）；`TimestampsAndWatermarksOperator` 静默守卫 = watch-only residual（接线后自然失效，语义不破坏）。

### Mission-driver 审计处理轮（2026-08-13-0132）— 2 份 open audits 全部处理 → 3 份 remediation plans（P0×4 / P1×6）+ P2 backlog 承接

- **输入**：open audits = `2026-08-12-1217-open-audit-nop-stream-invariant-loop.md`（P0×3 / P1×5 / P2×10，新失败族：运行时服务注入完整性）+ `2026-08-12-1217-multi-audit-nop-stream-invariant-loop.md`（P0×1 / P1×1 / P2×21）。所有 finding 已对照 live 源码逐条复核（`setProcessingTimeService` main 零调用、`SupervisionLoop.rebuildTask` 无 setBarrierTracker、`GraphModelCheckpointExecutor:946-970` manifest 恢复不推进计数器、`MemoryStateSerDe:430-435` 无参反射、`InputGate:690-710` 桶序、`WindowOperator:1468-1484` fail-fast 零测试引用、beans.xml 注释内模板 `ioc:configMethod`/`ioc:bean` 等均实测在案）。
- **产出 3 份 plans（全部 P0/P1 落地为 Fix 项，无降级）**：
  - `ai-dev/plans/2026-08-13-0132-1-nop-stream-runtime-service-wiring.md`（**{1}**）— open P0-01（CepOperator open NPE）+ P1-02（PT 窗口永不触发）。修复面 = ProcessingTimeService 生产接线（mailbox 投递语义，遵循 `mailbox-design.md` §7 设计意图）+ TimerServiceManager 四环节补齐（生产创建 / setTimeServiceManager / **WindowOperator registerTimerService** / 驱动 fireProcessingTimeTimers）+ null 守卫+WARN 兜底 + PT 窗口 E2E（慢源时序设计）+ 新 PTS 单测（Rule #25）。
  - `ai-dev/plans/2026-08-13-0132-2-nop-stream-checkpoint-recovery-fix.md`（**{2}**）— open P0-02（区域重启重接 checkpoint 管线，走 `executeWithCheckpoint` 公共入口 E2E + replace 语义 + listener 对偶移除）+ P0-03（manifest 恢复计数器单调推进）+ P1-01（窗口 aggregate/reduce 恢复，裁定方案 1 = live 函数复用，防 RocksDB 假绿：作业级重启口径）+ P1-05（markFinishedChannel 按序发射全部完成项，确定性单元测试对齐 `TestInputGateMultiEpochBarrier`）。
  - `ai-dev/plans/2026-08-13-0132-3-nop-stream-operator-deploy-contract-fix.md`（**{3}**）— open P1-03（fan-out 全列表关闭 + 重启路径同步）+ P1-04（CEP timer 注册表 open/restore 对称）+ multi P0-01（merge fail-fast 双场景回归测试；双 raw 可构造 → 保留路径裁定不可用）+ multi P1-01（beans.xml 注释内模板文本级合规验证 + `<constructor-arg>` 修正，含 `RpcDistributedExecutor.messageService` 断链）。
- **Draft Review（独立子 agent，2 轮达成共识）**：round 1 每 plan 各 1 个 fresh session（总发现 2 Blocker + 10 Major，全部修复）；round 2 各 1 个 fresh session（总发现 4 Major + 15 Minor，全部修复；无 Blocker）→ 三 plan 均转 `Plan Status: active`（Draft Review 记录已写入各 plan 头部）。
- **P2 承接（31 条 + 1 条门禁候选）**：open P2-01~P2-10 + multi P2-01~P2-23 全部登记 roadmap Follow-up Backlog（`ai-dev/backlog/nop-stream-invariant-loop-roadmap.md`「2026-08-13 P2 批次」两小节，每条含 source audit path + 建议 + 触发条件）；另登记「生产 wiring 存在性」不变式门禁沉淀候选（Cycle 3 / I1 派生候选，源自 open-audit 总评 + plan 1 Deferred 段）。
- **Audit 状态流转**：两份 audit 均含 P0/P1 → `Audit Status: open` → `planned`（processed 注释含 plan 归属映射）。
- **关键审查裁定（供后续执行参考）**：PT 驱动回调线程 = task 线程（mailbox），接线点须无条件挂 `invoke()` 启动处（本地路径无 setBarrierTracker，挂条件路径 = 假绿）；cleanup 验证 = invokable 级装配 + `numProcessingTimeTimers()==0` + `window-contents` map 空；区域重启 E2E 走 `GraphModelCheckpointExecutor.executeWithCheckpoint`（private static 不可直调）；Phase 3 恢复 E2E 的"重启" = 作业级重启（supervision 区域重启会静默回退 Memory backend）；beans.xml 模板在注释内 → 验收 = 文本提取 + beans.xdef 解析（包裹 `<beans>` 根 + xmlns:ioc）。
- **遗留（显式 successor ownership）**：三 plan 执行顺序 {1}→{2}→{3}（执行队列由 mission-driver EXEC_PLANS 驱动）；P2 批次触发条件在案（backlog）；Cycle 3 / I1 派生候选在案（backlog）。

### Mission-driver VERIFY 轮：plan-2026-08-13-0132 全量验证 full-green verification（mission `nop-stream-invariant-loop`）

- **构建**：`./mvnw compile -pl nop-stream -am` PASS；`./mvnw clean install -DskipTests -pl nop-stream -am -T 1C` BUILD SUCCESS（含 nop-stream-core / nop-stream-cep / nop-stream-runtime / nop-stream-flow / nop-stream-fraud-example）。
- **测试**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS，nop-stream 全模块 surefire 合计 2860 tests / 0 failures / 0 errors；新测试全部执行且绿：`TestTaskProcessingTimeService`（8）、`TestProcessingTimeServiceDriver`（9）、`TestStreamTaskInvokableProcessingTimeWiring`（3）、`TestCepProductionExecutionE2E`（4）、`TestProcessingTimeWindowProductionE2E`（3）。
- **Lint**：`checkstyle:check` 为仓库级既有债（nop-api-core 9164 / nop-stream-core 11155 条，均为未改动文件；本次改动文件零命中），mission 配置按 not-configured 处理。
- **提交**：本次 run 为 batch 模式收口——3 个 feat commit（core / cep / runtime 按包拆分，实现+测试同 commit）+ 1 个 docs commit（plans + audits + design + roadmap + logs + source-anchors）。
