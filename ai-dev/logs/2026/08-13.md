# 2026-08-13

### plan I2+I3 nop-metadata 不变式驱动审计与裁决（Formal Red List + 对抗探查 + 裁决表）

plan `ai-dev/plans/2026-08-13-1930-3-nop-metadata-invariant-driven-audit-and-adjudication.md`（roadmap `nop-metadata-invariant-loop-roadmap.md` Cycle 1 / I2+I3 合并）执行完成。纯审计/裁决计划（不改产品代码），跑 I1 的 4 条门禁产正式 red list + 对抗探查盲区 + 逐条裁决。

- **产出 1 — 正式 Red List** `ai-dev/audits/nop-metadata-invariants/formal-red-list.md`：4 条门禁跨全部目标集确定性复跑，命中 **81（80 silent-swallow + 0 UK + 0 sensitive + 1 limit）**，与 I1 `initial-red-list.md` 快照**完全一致**（I1→I2 期间 service 源码零 commit，无漂移，门禁确定性确认）。每条命中含 `文件:行` + 不变式编号 + 族归属 + 已知/新发现 + 可复跑命令；80 silent-swallow 命中按 5 个语义子类（S1 类型探测回退 / S2 批量失败隔离 / S3 优雅降级 / S4 良性 best-effort / S5 通用 catch+LOG）标注工作形状。
- **产出 2 — 对抗探查笔记** `ai-dev/audits/nop-metadata-invariants/adversarial-probing-notes.md`：5 个盲区方向（I0 漏扫 / 门禁子模式 / ORM index DDL 漂移 / 跨模块 catch / 目标集稳定性）均结论**无新族命中 / 无 I0 漏扫**。方向 3 发现 ORM 63 个 `<index>` 未进 `_create` DDL，经全仓比对（nop-wf/ai/job/retry 同构）+ `ddl.xlib` 生成器源码核查，裁定为**平台级 by-design**（优化类，非正确性缺陷，超 mission 范围），不触发 Cycle 2/I1。
- **产出 3 — 裁决表** `ai-dev/audits/nop-metadata-invariants/adjudication-table.md`：**零悬挂**裁决 81/81 命中 —— silent-swallow 80→**P1/I4 类别清扫**（门禁即权威清扫器）；INV-LIMIT 1（queryTableData）→**P1/I4 + 人工确认**（INV-LIMIT vs MA7.4-03 契约冲突，附两条具体解决路径）；UK/SENSITIVE 零命中（防回退门禁绿）；0 新族。每族 I4 工作项含**具体可执行 grep 命令**（`check-silent-swallow.mjs` + `rg "catch\s*\("` + bizmodel sweep + `rg '@Name("limit")'` + 同义参数 sweep，均经审计执行验证预期计数）+ test-first 要求 + 门禁零命中验收点。无 deferred 项（Anti-Slacking 合规）。
- 验证：4 门禁独立复跑 81 命中；4 条 silent-swallow 抽查（`MetaDataSourceConnectionProcessor:99` / `MetaQualityCheckpointExecutor:156` / `NopMetaLineageEdgeQueryAction:165` / `MetaQualityRuleExecutor:702`）均实；`check-plan-checklist --strict` exit 0；`check-doc-links --strict` 3 新文件 0 broken links（18 errors 全 pre-existing）。
- **独立 closure audit**：fresh session 子 agent（`ses_004c8adbaffewmnLdpsEs3lz0d`，review-only）VERDICT = approved，零 Blocker，逐项 PASS（A 门禁复跑 / B 抽查 / C 对抗探查复跑 / D Anti-Slacking / E 一致性）。
- Doc-sync：No owner-doc update required（裁决为过程产物；owner-doc 同步留待 I4 修复落地后）。
- Commit: 待提交。

### plan I1 nop-metadata 首批不变式沉淀为可执行门禁（First-Batch Invariant Guards）

plan `ai-dev/plans/2026-08-13-1930-2-nop-metadata-invariant-guards-first-batch.md`（roadmap `nop-metadata-invariant-loop-roadmap.md` 的 Cycle 1 / I1）执行完成。把 I0 盘点出的首批 4 条不变式从文档沉淀为可执行 CI 门禁，交付门禁本体 + 初始 red list 快照。

- **产出 1 — Workstream A：3 个 Node 静态扫描器**（`ai-dev/tools/`）：
  - `check-silent-swallow.mjs`（INV-SILENT-SWALLOW）：花括号跨度内多信号判定（catch 块内不出现 throw/NopMetadataException/.errorCode/ErrorCode./BizException/Biz.fatal 即命中）；扫描 130 catch 块 = I0 §1.3，命中 **80**；自验证 fixture PASS。
  - `check-orm-unique-key-constraint.mjs`（INV-UK）：XML-aware 逐 `<unique-key>` 元素核对 constraint+columns（支持跨行）；扫描 37 UK = I0 §2.2，命中 **0**（baseline 校正：37/37 全带属性）；自验证 fixture PASS。
  - `check-sensitive-literal-leak.mjs`（INV-SENSITIVE）：行级共现判定（logger/.param token + JDBC-URL / ≥2 SQL 关键字字面量）；命中 **0**（历史 P2-12/AR-16/AR-23⑩ 已修）；自验证 fixture PASS。
  - 三个扫描器均支持 `--module nop-metadata` 过滤 + `--fixture` 自验证 + 确定性退出码（0 = 零命中，1 = 有命中）。
- **产出 2 — Workstream B：JUnit 参数化穷举**（`nop-metadata-service/src/test/.../invariant/`）：
  - `TestLimitNegativeValueInvariant`（INV-LIMIT）：方法表驱动穷举 4 limit-taking public 方法，断言 limit=-1 抛 ErrorCode 异常；**默认从 surefire 排除**（pom.xml `<excludes>`），由 `-Dtest=TestLimitNegativeValueInvariant` 单独调用，FAIL 项 = limit red list。实测 3 PASS + **1 FAIL（queryTableData 静默封顶，MA7.4-03 裁定）**。
  - `TestLimitTargetSetCompleteness`（表完备性自检）：断言方法表 = 反查全集（反射 + 源文件计数双向核对）；在默认 surefire 集合中运行且 PASS。
  - pom.xml 新增 surefire `<excludes>**/invariant/TestLimitNegativeValueInvariant.java</excludes>`（持久配置，确保默认构建恒绿）。
- **产出 3 — Phase C：初始 Red List 快照** `ai-dev/audits/nop-metadata-invariants/initial-red-list.md`：81 项命中全量记录（80 + 0 + 0 + 1），含棘轮零点声明（= I5 收口基准）+ 门禁运行命令清单（4 条可一键复跑）+ 每条命中可独立 `rg` 定位 + 历史 finding-ID 回溯。
- **Anti-Hollow 验证**：3 scanner fixtures 区分违规/合规有效；limit test 真实反射调用 normalize 方法（非空壳断言）；TestLimitNegativeValueInvariant 对 queryTableData 确定性 FAIL 暴露（非 @Disabled 跳过）。
- 验证：`./mvnw test -pl nop-metadata/nop-metadata-service` → 1050 tests 0 failures（默认绿）；`check-plan-checklist --strict` exit 0；`check-doc-links --strict` 新增文件 0 broken links（18 errors 全 pre-existing）。
- Doc-sync：No owner-doc update required（门禁提升为 hard gate 时在 I5 统一同步 `docs-for-ai/`）。
- Commit: 待提交。

### plan I0 nop-metadata 不变式盘点与基线：不变式目录 + 审计目标集（闭环入口）

plan `ai-dev/plans/2026-08-13-1930-1-nop-metadata-invariant-inventory-and-baseline.md`（roadmap `nop-metadata-invariant-loop-roadmap.md` 的 Cycle 1 / I0）执行完成。纯文档/盘点计划（无代码变更），产出闭环飞轮的入口基线。

- **产出 1 — 审计目标集** `ai-dev/audits/nop-metadata-invariants/audit-target-set.md`：
  - 方法全集（变更型入口面）= 40 `@BizModel` 类（39 entity + 1 search）+ 13 `@BizQuery` + 30 `@BizMutation` + 0 `@BizAction`（nop-metadata 不用此注解）+ 4 limit-taking 入口方法；catch 目标集 = **130 块 / 46 文件**；limit 引用面 = 29 文件。每项配独立 `rg` 复现命令。
  - ORM 全集 = 39 entity + 37 unique-key。
- **产出 2 — 不变式目录** `ai-dev/audits/nop-metadata-invariants/invariant-catalog.md`：4 条不变式（INV-SILENT-SWALLOW / INV-UK / INV-LIMIT / INV-SENSITIVE），每条四要素（陈述/失败族/audit-finding-ID 证据/检测方法）+ 覆盖率回填 + CI 门禁零点基线 + 棘轮规则引用。
- **基线校正（虚假关闭元规则实证）**：plan baseline 沿用旧值「unique-key 1 missing」，但 live XML-aware 核对（逐 `<unique-key>` 元素，含跨行）显示 **37/37 全带 `constraint`+`columns`，0 missing**。以 live 值收口（INV-UK 当前 red list = 0），写入两产出的「基线校正记录/元规则」段。这正呼应 AR-06 虚假关闭先例——以 live 实测为准，不以旧文档为准。
- **CI 门禁基线**：实测零 nop-metadata 专属不变式门禁（check-silent-swallow/check-orm-unique-key-constraint/check-sensitive-literal-leak 三脚本均不存在、无 invariant JUnit）；诚实声明 `tools/rules/` 下 3 条通用 Java ast-grep 规则（empty-catch/getmessage-only/bare-runtimeexception）部分重叠 INV-SILENT-SWALLOW 但非专属、无表完备性。
- 验证：`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` exit 0（passed 1 / failed 0）；`check-doc-links --strict` 两产出引入 0 新断裂链接（20 errors / 21 warnings 均为预先存在的跨文件前向引用，非本计划引入）；13 个 audit-finding-ID 引用经独立 `sed`+`rg` 复核全部在 `arm-index-nop-metadata.md` 对应行命中，无悬空。
- Doc-sync：No owner-doc update required（产出在 `ai-dev/audits/` 下；I1 实现门禁后再考虑 `docs-for-ai/` 同步）。
- Commit: 待提交。
