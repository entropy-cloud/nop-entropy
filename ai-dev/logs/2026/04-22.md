# AI 开发日志 — 2026-04-22

### 2026-04-22

- 落地 relation prop `writeMode` 的元数据链路与 CRUD 基础行为：在 `IObjPropMeta` / `ObjPropMetaImpl` 增加 `getWriteMode()`，补齐 `nop-biz` 中 `inline/link/biz` 的校验、复制与 delayed relation action 执行路径。
- 相关路径：`nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/schema/obj-schema.xdef`、`nop-kernel/nop-xdefs/src/main/resources/_vfs/nop/schema/schema/schema.xdef`、`nop-kernel/nop-xlang/precompile/gen-xlang-xdsl.xgen`、`nop-service-framework/nop-biz/src/main/java/io/nop/biz/crud/`
- 关键决策：`writeMode` 不能只放扩展属性，需进入 `IObjPropMeta` 接口；`obj-schema.xdef` 的 prop 字段变更必须同步检查 `schema.xdef` 这类共享定义点；`nop-xdefs` 之后还要验证 `nop-xlang` 生成与测试。
- 验证：`nop-kernel/nop-xlang/src/test/java/io/nop/xlang/xmeta/TestXMetaRef.java` 通过；新增 `nop-service-framework/nop-biz/src/test/java/io/nop/biz/crud/TestObjMetaBasedValidator.java`，覆盖 writeMode 优先级与 link/biz 嵌套校验差异。
- 下一步：继续补 relation `biz` 模式在 `CrudBizModel` 真实 save/update/delete/unlink 场景下的集成测试，尤其是 toMany 缺席项不自动删除的第一版约束。
