# 2026-08-14

### EXEC_PLANS nop-metadata 代码卫生与死码清扫（F14 / F15 / F16 / F18 / F19）

plan `ai-dev/plans/2026-08-14-1448-1-nop-metadata-code-hygiene-and-dead-code-cleanup.md`（mission-driver EXEC_PLANS）执行完成。5 个 API/文档/代码卫生族 follow-up 项一次性收口，零对外行为变更（删死码 + 强化测试 + 补文档精度）。

- **Phase 1 — 死码删除（F14 KeyValueDTO + F16 resolveDataSourceOrThrow）**：
  - `Fix` F14：删除 `nop-metadata-api/.../api/dto/KeyValueDTO.java`（40 行 `@DataBean`，零生产引用）；移除 `TestNopMetaDtoResults` 中 import（原 :10）+ usage（原 :89）。删除前 grep 复核：`rg -n KeyValueDTO nop-metadata/` 仅命中自身定义 + 该测试引用 → 安全。删除后 `rg -n KeyValueDTO nop-metadata/` → exit 1（零残留）。
  - `Fix` F16：删除 `NopMetaQualityRuleBizModel.resolveDataSourceOrThrow`（原 :312-328，private，全文件零调用点——`judgeByRuleId`/`executeQualityRule` 走 `tableRefResolver.resolve(...)`）。`MetaTableReferenceResolver` 同名方法（不同签名，:181 + 调用 :109/:174）保留。
  - `Fix` F16 连带 javadoc：`MetaDataSourceResolver.java:21-23` javadoc 原称"既有三处 `resolveDataSourceOrThrow` 重复（NopMetaTableBizModel profiling / NopMetaQualityRuleBizModel / NopMetaProfilingRuleBizModel）"。**执行复核发现该表述本身已 stale**——grep 证明另两个 BizModel 实际并无此方法（它们委托 `MetaTableReferenceResolver`）。故 javadoc 改为真值表述（"规范实现位于 MetaTableReferenceResolver，供两 BizModel 复用；NopMetaQualityRuleBizModel 重复死码副本已删除"），而非机械"三→两"。
  - `Proof`：`./mvnw compile -pl nop-metadata -am -T 1C` BUILD SUCCESS（删除零引用代码不破坏编译）。
- **Phase 2 — owner-doc IBiz 方法表精度补齐（F15）**：
  - `Fix`：`docs-for-ai/03-modules/nop-metadata.md` :186-187 把 5 个接口的分组标签（"质量规则/检查点/评分"、"契约/剖析"）展开为显式方法名，与 :182-185 同精度。逐个打开 `nop-metadata-dao/.../biz/INopMeta*Biz.java` 核对 live 签名：
    - `INopMetaQualityRuleBiz` — executeQualityRule / executeQualityRulesForDataSource / judgeByRuleId
    - `INopMetaQualityCheckpointBiz` — executeCheckpoint
    - `INopMetaQualityScoreBiz` — computeQualityScore
    - `INopMetaDataContractBiz` — checkContract / checkContractReadOnly
    - `INopMetaProfilingRuleBiz` — executeProfilingRule
  - `Proof`：文档方法名与 live 接口逐一核对一致（无臆造/遗漏/拼写漂移）；`check-doc-links --strict` 本改动文件 0 新增 broken link（17 pre-existing 均不在本文件）。
- **Phase 3 — 空壳测试强化 + 动态实体守卫（F18 + F19）**：
  - `Fix` F18：`TestNopMetaDtoResults.testDtoJsonRoundTripAllTypes` 6 DTO（TestConnectionResultDTO / SyncExternalTablesResultDTO / CollectCatalogResultDTO / CreateSqlTableResultDTO / QueryTableDataResultDTO / QualityRuleResultDTO）由空壳 `assertNotNull(JsonTool.stringify(dto))` × 6 改为真实 round-trip——每 DTO `stringify` → `parseBeanFromText` → `assertEquals` 关键字段（connected/databaseProductName/syncedTableCount/tableCount/metaTableId+tableName/tableType/qualityRuleId+status）。方法名保持（现为 genuine round-trip，命名与行为一致）。
  - `Fix` F19：`TestAllEntitiesHaveBizModels` 硬编码 `list.add(NopMetaXxx.class)` × 39 清单删除，重写为 `@NopTestConfig(localDb=true)` + `@Inject IOrmTemplate`，`discoverEntities()` 从 `orm.getOrmModel().getEntityModels()` 动态发现（沿 `TestNopMetaUniqueKeysEnforced` 先例）。过滤：包前缀 `io.nop.metadata.dao.entity.` + `_gen` 子包排除 + 下划线前缀类排除（防御 `_NopMeta*` 生成基类，审查 Major-2）。sanity 断言 `entities.size() >= KNOWN_ENTITY_BASELINE(39)` 防止动态发现静默返回空集。新增 ORM entity 自动纳入守卫，不可被"忘记追加"绕过。
  - `Proof`：`./mvnw test -pl nop-metadata/nop-metadata-service -Dtest=TestNopMetaDtoResults,TestAllEntitiesHaveBizModels` → 6 tests, 0 failures（动态发现实体数 ≥ 39 且全部有对应 BizModel）。Anti-Hollow 接线证据：sanity `>= 39` 通过即证明读取真实注册表（空集/固定集会触发断言失败）。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` → **1166 tests, 0 failures**。门禁：`check-silent-swallow` exit 1 系 2 处 pre-existing（MetaTableProfiler:223 javadoc 误匹配 + AggregationHelper:561，均不在本计划改动文件内，`git diff --name-only` 确认），本计划 0 新增；`scan-hollow-implementations --severity high` exit 0；`check-plan-checklist --strict` exit 0；`checkstyle:check` 本改动文件未触发新 error（模块整体 pre-existing javadoc 基线非本计划引入）。
- **Non-Goal 守恒**：F17（safeProductName 7× 去重）+ AR-14 归计划 `2026-08-14-1448-2`；F10-F13（ORM 索引/dict 卫生）归计划 `2026-08-14-1448-3`；本计划不重写/迁移任何生产 DTO、不改任何 `@BizQuery`/`@BizMutation` 行为。
- Roadmap：`ai-dev/backlog/nop-metadata-invariant-loop-roadmap.md` API/文档/代码卫生族 F14/F15/F16/F18/F19 标记 ✅ Fixed（F17 保留待计划 2）。



plan `ai-dev/plans/2026-08-14-1133-3-nop-metadata-lineage-manifest-reconciliation-correctness.md`（mission-driver EXEC_PLANS）执行完成。6 个 P2 metadata-correctness 缺陷全收口：lineage 源表抽取不跨 schema 塌缩、不把 CTE 名报为物理表、manifest 图边不因重复关系膨胀、SQL 视图类型推断不因尾分号失败、reconciliation 匹配不受 JVM locale 影响、reconciliation 候选序列化有界。全部为"文档记录为简化/限制，但 false-drop/false-positive 后果未记录"的元数据正确性缺陷——对元数据目录而言，错误的血缘边/图度数/匹配结果是产品级缺陷。

- **Phase 1 — AR-07 跨 schema 同名表塌缩 + AR-08 CTE 名报为物理源表**：
  - `Fix` AR-07：`SqlSourceTableExtractor.extract` 去重 key 由 `seen.add(simple)`（simpleName）改为 `seen.add(full)`（schema-qualified 名）。`dbo.users` 与 `sales.users` 的 fullName 不同 → 两条独立边，不再因 simpleName="users" 相同而塌缩。残留歧义（同一 fullName 经不同别名引用仍为一条边）在 Javadoc 显式记录。
  - `Proof` AR-07：新增 `TestSqlSourceTableExtractor`（lineage 包，纯解析器单测）4 例跨 schema 去重——dbo.users+sales.users 两条边 / 同一 dbo.users 不同别名去重 / 无 schema 同名表去重 / 混合 qualified+unqualified 不塌缩。
  - `Fix` AR-08：新增 `collectCteNames` 预遍历——收集所有 `SqlSelectWithCte.getWithCtes()` → `SqlCteStatement.getName()`（lowercase），匹配 CTE 名的 `SqlSingleTableSource` 从结果排除。CTE 体内部的物理源表仍被正常收集（符合 SQL 作用域语义）。CTE 名遮蔽同名物理表时 CTE 名被排除、物理表引用不补回（保守排除策略）。
  - `Proof` AR-08：9 例 CTE 排除——`WITH t AS (SELECT 1) SELECT * FROM t JOIN real_table` 只含 real_table / CTE 体物理表仍收集 / CTE 名遮蔽同名物理表排除 / 多 CTE 全排除 / CTE 名大小写不敏感排除 / 无 WITH 的 SQL 不受影响（无静默跳过）/ 子查询源表收集 / UNION 两分支收集 / 空 SQL 显式失败。
  - `Proof` 接线验证：现有 `TestNopMetaLineageEdge*` 集成测试无回归（38 tests, 0 failures——dedup key 改动不破坏既有 lineage 边创建链路）。
- **Phase 2 — AR-09 manifest 邻接表去重 + 自环过滤**：
  - `Fix`：`MetaManifestBuilder.addEdge` 追加前检查 `!list.contains(value)`（去重）+ `key.equals(value)` 时不追加（自环过滤）。原 `childMap.computeIfAbsent(...).add(...)` 无条件追加，现改为统一经 `addEdge(childMap, targetUniqueId, ownerUniqueId)`（复用同一去重+自环逻辑）。
  - `Proof`：新增 `TestMetaManifestBuilder`（manifest 包，纯转换逻辑单测，不依赖 IoC）4 例——重复关系（owner→target×2）邻接表无重复邻居 / 自环（owner==target）不出现 / 复合场景（重复+自环+正常）parentMap 与 childMap 双向均正确 / 正常单一关系不被误过滤。接线验证：`TestNopMetaModuleBizModel`（含 generateManifest 端到端）16 tests 全绿。
- **Phase 3 — AR-11 尾分号 + AR-12 Locale.ROOT + AR-13 limit=50**：
  - `Fix` AR-11：`SqlViewFieldTypeInferrer.inferWithinConnection` 包装前 `trimmedSourceSql = sourceSql.trim()` + `while (trimmedSourceSql.endsWith(";")) strip+trim`。修复前 `"SELECT a FROM t;"` 内层子查询 `SELECT * FROM (SELECT a FROM t;) _t LIMIT 0` 被多数 JDBC 驱动拒绝。`while` 循环覆盖 `;;` 双分号边角（防御性——`;;` 在 EQL 解析器层被判为多语句，在类型推断之前被 `SqlSelectFieldExtractor` 拒绝，但 `while` 循环对经其他路径到达推断器的 `;;` 仍做防御性剥离）。
  - `Proof` AR-11：`TestNopMetaTableQueryBizModel` 新增 2 例——带尾分号 sourceSql 类型推断成功（H2 实跑 INTEGER）+ 带前后空白+尾分号 trim+strip 组合成功。
  - `Fix` AR-12：`LocalReconciliationProcessor.levenshteinSimilarity` 的 `a.toLowerCase()` / `b.toLowerCase()` 改为 `toLowerCase(Locale.ROOT)`。tr-TR 下 `"I".toLowerCase()` → `"ı"`（无点 i）不再导致同一数据在不同 JVM locale 下匹配结果不同。
  - `Proof` AR-12：新增 `TestLocalReconciliationProcessorLocale`（reconciliation 包，package-private 静态方法单测）3 例——tr-TR locale 下 I/i 相似度 1.0（locale-insensitive）/ MICROSOFT/microsoft 1.0 / 默认 locale 正常相似度回归。
  - `Decision` AR-13：硬编码默认 `DEFAULT_CANDIDATE_LIMIT = 50`（不引入 ORM/配置变更，避免撞 ORM Protected Area）。`ReconciliationExecutor.execute` 传入 50 而非 null。config-driven limit 移入 Non-Blocking Follow-ups。
  - `Fix` AR-13：`ReconciliationExecutor.execute` 的 `reconcile(..., null)` 改为 `reconcile(..., DEFAULT_CANDIDATE_LIMIT)`。所有 fuzzy 候选经 `LocalReconciliationProcessor.reconcile` 截断为 ≤50，details JSON 候选序列化有界。
  - `Proof` AR-13：新增 `TestReconciliationExecutorLimit`（reconciliation 包，mock IReconciliationProcessor）3 例——limit 参数被真实消费（非 null，=50）/ 大候选池（100）截断为 50 / 小候选池（3）不截断。
- **验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -am -T 1C` → **1166 tests, 0 failures**（nop-metadata-service）+ web 1 test（nop-auth-service 的 1 个 pre-existing VarCollector NPE 与本次变更无关）；4 条不变式门禁——INV-UK/INV-SENSITIVE/INV-LIMIT 零命中，INV-SILENT-SWALLOW 2 个 pre-existing hit（MetaTableProfiler AR-06 Javadoc 误报 + AggregationHelper AR-10 裁定的 NumberFormatException→null，均非本次引入）；`checkstyle:check` sun_checks.xml 全模块 pre-existing 基线（项目 checkstyle 非真实门禁，本次改动遵循既有代码风格）。
- **Non-Goal 守恒**：不重写 SqlSourceTableExtractor 为完整 SQL 语义分析器（CTE 展开/子查询别名解析超范围）；不改变 reconciliation 匹配算法本身（Levenshtein 复杂度已裁定）；config-driven limit（含 ORM Protected Area 风险）移入 Non-Blocking Follow-ups。
- Doc-sync：`docs-for-ai/03-modules/nop-metadata.md` 新增「lineage/manifest/reconciliation 正确性语义」段（AR-07 full 去重 / AR-08 CTE 排除 / AR-09 邻接表去重+自环 / AR-11 尾分号 / AR-12 Locale.ROOT / AR-13 候选有界）。

### EXEC_PLANS nop-metadata profiler 分类正确性 + probeNumeric 错误区分 + 聚合精度无损（AR-05/AR-06/AR-10）

plan `ai-dev/plans/2026-08-14-1133-2-nop-metadata-profiler-precision-silent-wrong-result.md`（mission-driver EXEC_PLANS）执行完成。3 个 P2 "silent-wrong-result" 缺陷全收口：profiler 列类型分类正确（几何/布尔列不再误归数值）、probeNumeric 连接/权限失败不再静默塌缩为"string stats"、cross-DB 内存聚合精度无损（Long>2^53 不丢位、String 数值不被静默跳过）。三者共享同一特征——无错误信号、结果静默错误，是 plan 0707-2 已修 P1 同族的 P2 兄弟面（AR-10 从 0707-2 Non-Blocking Follow-ups 显式 carry-over）。

- **Phase 1 — AR-05 profiler isNumericType 子串误分类**：
  - `Fix`：`MetaTableProfiler.isNumericType` 由 `upper.contains(kw)` substring 匹配改为 `NUMERIC_TYPE_NAMES` 显式类型名集合（`Set.of`）exact-match（INT/INTEGER/TINYINT/SMALLINT/MEDIUMINT/BIGINT/DECIMAL/NUMERIC/NUMBER/DOUBLE/DOUBLE PRECISION/FLOAT/REAL）。从数值集合移除 `BOOLEAN`（布尔列非数值）与 `BIT`（BIT(1)=boolean/BIT(n>1)=bitfield，保守回退 string stats）。`POINT`（几何类型，含子串 `INT`）不再误判数值 → 不再产非法 `SUM(POINT)` SQL 落入 per-column 错误路径无 stats。
  - `Proof`：`TestMetaTableProfilerClassification`（profiling 包，直接访问 package-private）9 例——POINT/BOOLEAN/BIT/POINTER/PRINT 等"含 INT 子串但非数值"类型返回 false；13 个合法数值类型名（含 PG `DOUBLE PRECISION` 空格复合名）仍 true；大小写/空白不敏感；STRING 类型识别不回归。
- **Phase 2 — AR-06 probeNumeric 吞连接/权限失败**：
  - `Fix`：`MetaTableProfiler.probeNumeric` 的 `catch(SQLException)` 内新增 `isInfrastructureFailure` 分类——只对**可识别的基础设施信号**（SQLState `08*` 连接 / `28*` 授权 / `42*` 表/列不存在含 PG `42501` 权限，或消息含 connection/permission/denied/closed/timeout/communication/does not exist）判为 infra 并 WARN（带 col 上下文）；其余（含 `22*` data exception、H2 vendor `90015` wrong data type、null）一律视为良性类型不匹配 DEBUG 静默。仍 return false 保持剖析不中断（签名不变）。
  - `Decision` H2 SQLState 实测裁定：live H2 诊断确认 `SUM(varchar)` 抛 vendor SQLState `90015`（"wrong data type"），非 SQL 标准 `22*`。故放弃"22* 前缀单判 + 保守 WARN on unknown"方案——会对每个良性类型不匹配产 WARN 噪声淹没真实失败。改为"只识别 infra 信号 → WARN，其余 → 良性"，既满足"基础设施失败有显式信号"又不产噪声。另实测 H2 `SUM(BOOLEAN)` 成功（视 TRUE 为 1）。
  - `Proof`：`TestMetaTableProfilerProbeNumeric`（profiling 包）5 例——`isInfrastructureFailure` 单元（22*/90015 良性、08*/28*/42*/42501 infra、消息线索识别、null 无线索良性）；probeNumeric 数值列 true / 文本列类型不匹配 false 不抛 / 代理连接抛 08006 → WARN 触发（ListAppender 断言）+ 仍 false；跨 Phase 依赖（BOOLEAN 移出数值集合走 probeNumeric 守卫，表级剖析不崩溃）。
- **Phase 3 — AR-10 toBigDecimal 精度丢失 + String 数值静默跳过**：
  - `Fix`：`AggregationHelper.toBigDecimal` 的 `Number` 分支内按 `instanceof` 分派——整数类型（Long/Integer/Short/Byte/AtomicLong/AtomicInteger）改用 `BigDecimal.valueOf(n.longValue())` 无损（Long>2^53 经 `doubleValue()` 丢低位）；浮点（Float/Double）保持 `doubleValue()`（小数不截断）；BigInteger 分支不变。新增 `String` 分支：trim 后 `new BigDecimal(s)`（catch NumberFormatException → null），不再直接 return null 被 `SumAcc` 的 `if(n!=null)` 静默跳过。
  - `Proof`：`TestCrossDbInMemoryAggregationProcessor` 新增 7 例——Long.MAX_VALUE(2^63-1>2^53) 经 toBigDecimal 精确（longValueExact 往返）/ 2^60+12345 精确 / AtomicLong 精确；Double 1.5 保留小数不被 longValue 截断；String "123.45" 解析 / 含空白 trim-then-parse / 整数 String；非数值 String/"abc"/空 → null 不抛；BigInteger/BigDecimal 分支不回归；**接线验证** SumAcc.accumulate(Long.MAX_VALUE)×2 = 2^64-2 精确 + SumAcc.accumulate("123.45"+"76.55")=200.00（String 数值不再静默跳过）+ 非数值 String 跳过不污染聚合。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` → **1141 tests, 0 failures**（nop-metadata-service 1140 + web 1）；4 条不变式门禁仍零命中（INV-LIMIT/INV-UK/TestSilentSwallowFormalization 默认 surefire 全绿）；`checkstyle:check` sun_checks.xml 全模块 pre-existing 基线（项目 checkstyle 非真实门禁，本次改动遵循既有代码风格）。
- **Non-Goal 守恒**：不处理同级 `toBigDecimal`（MemoryOrderByComparator/MemoryFilterEvaluator）——同 `doubleValue()` 精度模式但属 ORDER BY/WHERE 比较路径（非 SUM/AVG 聚合），Long>2^53 精度丢失在排序/过滤上下文影响较小，裁定为 optimization candidate 留待后续清扫。
- Doc-sync：`docs-for-ai/03-modules/nop-metadata.md` cross-DB 聚合精度语义段新增 AR-10（整数 longValue 无损 / 浮点 doubleValue / String 数值解析）。

### EXEC_PLANS nop-metadata JDBC/SQL 防御纵深硬化（F5-F9，defense-in-depth）

plan `ai-dev/plans/2026-08-14-1133-1-nop-metadata-jdbc-sql-defense-in-depth-hardening.md`（mission-driver EXEC_PLANS）执行完成。5 个 P2 安全防御纵深缺口（F5/F6/F7/F8/F9）全收口：JDBC URL 危险参数 blocklist 完备、凭证脱敏无泄漏、空/畸形主机 fail-closed、IPv6 字面量判定不触发 DNS、custom_sql blocklist 覆盖 PG 危险关键字。全部为 defense-in-depth——不改变已修复的 F1/F2 攻击面，而是消除"lucky fail-closed"与"审计者已知但 blocklist 未覆盖"的残余间隙。

- **Phase 1 — F5 反射类加载参数族 + F6 含 `@` 口令脱敏**：
  - `Fix` F5：`MetaDataSourceConnectionProcessor.DANGEROUS_URL_TOKENS` 在 AR-02 原 13 个 token 之上补全 5 个触发反射类加载（RCE-chain 潜力）的参数：MySQL `socketfactory`/`statementinterceptors`/`detectcustomcollatz`，PostgreSQL `sslfactory`（SSL SocketFactory 反射实例化）与 `options=`（向 backend 透传命令行风格选项）。`driverClassName` 白名单缓解但不能替代 URL 参数侧 fail-closed——含上述参数的 URL 在 `validateJdbcUrl` 危险参数检查阶段（主机校验之前）即 fail-fast `ERR_DATASOURCE_JDBC_URL_BLOCKED`。
  - `Fix` F6：`redactJdbcUrl` 改为与 `extractHosts` 一致——定位 authority 段（`://` 后到首个 `/` 或 `?`）后用 `lastIndexOf('@')` 找 userinfo 边界并 substring-replace。删除原 `CREDENTIAL_PATTERN` 正则（在第一个 `@` 处停止，含 `@` 口令尾部泄漏：`user:p@ss@host` → `ss@host` 泄漏）。现在多 `@` 极端用例下仅保留最后一个 `@` 之后内容，口令片段不再出现在任何 ErrorCode 参数或消息。
  - `Proof`：对抗测试 7 例（F5：5 新 token 各一 + 既有 token 回归；F6：含 `@` 口令完整脱敏 + 多 `@` 极端 + 既有语义不回归 + 含 `@` 口令端到端错误路径不泄漏）。
- **Phase 2 — F7 空/畸形主机 fail-closed + F8 IPv6 字面量 DNS 安全**：
  - `Fix` F7：`validateJdbcUrl` 主机校验循环新增 `isPlausibleHostShape` 形状校验——空串（`jdbc:mysql:///db`）、以 `/` 开头（path 片段）、纯端口（`:3306`，以 `:` 开头且无第二个冒号）显式拒绝 `ERR_DATASOURCE_JDBC_URL_BLOCKED`（reason="host unparseable"），不再依赖驱动拒绝的 lucky fail-closed。合法无括号 IPv6 字面量（`::1`，以 `:` 开头但含第二个冒号）不被误伤（关键区分：纯端口单冒号 vs IPv6 字面量多冒号）。
  - `Fix` F8：`HostSecurityUtil.isInternalIpv6Literal` 入口增加 charset 前置过滤——仅"字符集 ⊆ [0-9a-fA-F:.] 且 ≥2 冒号"的输入才调用 `InetAddress.getByName`；否则视为非字面量直接 return false（不触发 DNS）。修复前含 `:` 但含非 hex 字母（g-z）的串（如 `gzzz::1`）路由到本方法后直接调 `getByName` 触发 DNS，违反类 javadoc "纯确定性解析，不触发 DNS" 契约。过滤逻辑与 `isIpLiteral` 的 charset 检查逐项一致（代码审查验证）。
  - `Proof`：对抗测试 7 例（F7：空主机/空主机带端口/合法外网不误伤；F8：非 hex 含 `:` 串无 DNS/合法 IPv6 不回归/单冒号过滤）。F7 接线验证：`TestMetaDataSourceConnectionProcessorExtract.testAllowlistMatchesNormalizedHost`（`::1` 在 allowed-hosts 放行）保持绿（形状校验放行合法 IPv6）。
- **Phase 3 — F9 custom_sql blocklist PG 关键字扩充**：
  - `Fix` F9：`MetaQualityRuleExecutor.CUSTOM_SQL_FORBIDDEN_WORDS` 补全 5 个 PG 脚本执行/时序攻击/系统目录族关键字：`DO`（执行匿名 PL/pgSQL 代码块，RCE 等价面）、`PG_SLEEP`（时序盲注/DoS）、`PG_CATALOG`（系统目录 schema）、`PG_STAT_USER_TABLES`（系统统计视图，信息泄漏）、`WITH`（CTE 递归）。
  - `Decision` DRY 裁定：与 `ExpressionMeasureValidator.KEYWORD_BLACKLIST`/`FUNCTION_BLACKLIST` 保持分离（不抽取共享常量）——语义域（整条 SQL 文本 vs SELECT 片段表达式）、分词与匹配机制（token 精确 + fail-closed vs word-boundary + 字符串字面量先抽取）、演进路径均不同；22 项重叠的"恰好同名"是语义域交集自然结果，非 DRY 缺陷。`PG_SLEEP` 同存两表但匹配形态不同，各自独立维护。
  - `Decision` `WITH` CTE false-positive tradeoff：合法分析型 CTE 查询会被阻断——与既有 over-block 哲学一致（custom_sql 面向受限 SQL 场景，已含 `SET` 等）；合法 CTE 需求应改用子查询或专用 measure 表达式通道。
  - `Proof`：对抗测试 3 例（5 新关键字各一 via 沙箱 + via judge 公开入口接线验证 + WITH CTE false-positive 钉死）。既有 CTE-DML 测试（`testCteWrappedDmlBlockedViaJudgeEntry`）保持绿。
- **验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -T 1C` → **1122 tests, 0 failures**（nop-metadata-service 1121 + web 1）；`checkstyle:check` sun_checks.xml 9058 violations 为全模块 pre-existing 基线（项目 checkstyle 非真实门禁，与本次变更无关，本次改动遵循既有代码风格）。
- **Doc-sync**：`docs-for-ai/03-modules/nop-metadata.md` 安全契约段新增 F5/F6/F7/F8/F9 五段（blocklist 完备 / 含 `@` 脱敏 / 空-畸形主机 fail-closed / IPv6 DNS 安全 / custom_sql PG 族 + WITH tradeoff + DRY 裁定）。

### EXEC_PLANS nop-metadata INV-LIMIT 默认 surefire 防御纵深（F3 残留清理）

plan `ai-dev/plans/2026-08-14-0707-3-nop-metadata-invariant-enforcement-in-default-build.md`（mission-driver EXEC_PLANS）执行完成。F3 残留（INV-LIMIT 默认 surefire 排除 + 陈旧注释）全清理，INV-LIMIT 回归默认 surefire，与 CI `invariant-gate` job 形成 defense-in-depth 双重运行。

- **Phase 1 — INV-LIMIT 回归默认 surefire（防御纵深 + 清理陈旧配置）**：
  - `Fix`：移除 `nop-metadata-service/pom.xml` surefire `<excludes>` 中对 `TestLimitNegativeValueInvariant` 的排除（连同"保证默认恒绿"的陈旧注释一并删除）。该排除前提已失效——底层 limit 静默 clamp 缺陷已于 I4/plan-05 Phase 0 收敛，该测试现全绿。
  - `Decision`：裁定**保留** `run-nop-metadata-invariants.sh` step 4 作 CI defense-in-depth 第二层（聚合入口在 `invariant-gate` job 每个 push/PR fail-fast 复跑，独立于 build job surefire）；仅更新其 step 4 注释中"excluded from default surefire"的陈旧表述。
  - 文档/注释同步：`TestLimitNegativeValueInvariant.java` Javadoc 由"Default surefire exclusion"段改为"Default surefire inclusion（双重运行）"段；`invariant-guards.md` 单条门禁注释 + INV-LIMIT 运行说明段改为双重运行（默认 surefire + CI invariant-gate）。
  - **Anti-Hollow proof**：默认 `./mvnw test -pl nop-metadata -am -T 1C` 输出确认 `TestLimitNegativeValueInvariant` **实际运行**（4 tests, 0 failures，非 skipped），`TestLimitTargetSetCompleteness`（2 tests, 0 failures）。任一重新引入 `limit<0?0:limit` / `Math.abs(limit)` 的改动现于本地默认测试即变红。
- **验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -am -T 1C` → **1106 tests, 0 failures**（nop-metadata-service）+ web 1 test 0 failures；4 条不变式门禁仍零命中（INV-LIMIT 现默认 surefire + CI 双重验证）。
- **F3 P1 前提订正**（独立审查 + lead agent 双重核实 live CI）：F3 审计的 P1 定性建立在"默认 CI 不 guard INV-LIMIT"前提上，该前提与 live repo 不符——I5 已通过 `invariant-gate` job 每个 push/PR fail-fast 运行该测试。F3 重裁为"残留 = 防御纵深/陈旧配置清理"，仍以 `Fix` 收口（满足非可降级），范围严格限定。
- Doc-sync：`docs-for-ai/02-core-guides/invariant-guards.md`（INV-LIMIT 双重运行方式）。

### EXEC_PLANS nop-metadata 静默错算与契约缺口闭环（AR-01 SLA 分数截断 / AR-02 NFE 逃逸 / AR-03 group-key 碰撞 / F4 selection 契约）

plan `ai-dev/plans/2026-08-14-0707-2-nop-metadata-silent-wrong-result-and-contract-fixes.md`（mission-driver EXEC_PLANS）执行完成。4 个 P1 "silent-wrong-result / 契约缺口"缺陷全收口：结果正确（带错误信号或正确数值）、公开参数契约收敛、每项均有回归/对抗测试钉死。

- **Phase 1 — AR-01/AR-02 SLA 正确性（同方法合并）**：`MetaContractChecker.toDurationMillis` 两条换算路径（通用 + week 分支）均改为 `(long)(amount * unitMillis)` 先乘后取整（不再 `(long)amount` 截断——`0.5h` → 1_800_000 ms 而非 0）；不可解析 amount（非数字 String `"oops"`）映射 `ERR_CONTRACT_SLA_INVALID`（带 cause + 错误值），不再逃逸裸 `NumberFormatException`（原 catch 仅捕 ClassCastException）。回归测试 5 例：分数 hour fresh/stale 边界、分数 week fresh、非数字 amount fail-loud、既有合法 SLA 不回归。PASS。
- **Phase 2 — AR-03 cross-DB 内存 group-key 结构化**：`AggregationHelper.memoryGroupBy` 改用 `LinkedHashMap<List<Object>,...>` 结构性 key（值级 equals/hashCode），不再用 `\u0001` 分隔符 + `\u0000` null 哨兵拼接 String。消除控制字符入参碰撞：`("a","\u0001b")` 与 `("a\u0001","b")` 现产出两个不同分组；`null` 与字面量 `"\u0000"` 不碰撞。对抗测试 2 例（分隔符碰撞 + null 哨兵碰撞）；既有 `testCrossDbMemoryHavingMatchesSameDbSqlPath`（R8.3 内存/SQL 一致性）仍绿。PASS。
- **Phase 3 — F4 `selection` 参数契约收敛（裁定：显式 no-op）**：live 验证发现 GraphQL 引擎（`ReflectionBizModelBuilder:392-393`）将**响应字段选择集**（DTO 级 `{ tableType items }`）自动注入 `selection` 参数——非调用方传入的行列过滤。consume（后置 key 过滤）实测会错误清空行内列（3 个 GraphQL 路径测试 red）。由于结果为不透明 `List<Map<String,Object>>`（Map 无字段级 GraphQL 选择语义），裁定选**显式 no-op + owner-doc 声明**（非 breaking）：三方法 Javadoc 显式声明 selection 为 no-op，回归测试 3 例钉死 no-op 契约（非 null selection 不裁剪列；null selection 回归；aggregation 同契约）。与 P2-24 carve-out 边界在 owner-doc 写清（P2-24 = items 返回类型，F4 = selection 参数语义，正交）。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` → **1103 tests, 0 failures**（nop-metadata-service 1102 + web 1）；`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS（8 模块全绿）；`check-plan-checklist.mjs --strict` exit 0（38 items 全勾选）。
- **Doc-sync**：`docs-for-ai/03-modules/nop-metadata.md` 新增三段（selection 显式 no-op / SLA 分数与非法值语义 / cross-DB 内存 group-key 结构化）。

### EXEC_PLANS nop-metadata 安全攻击面闭环（F1 HAVING 注入 / F2 多主机 SSRF / AR-04 POJO 脱敏）

plan `ai-dev/plans/2026-08-14-0707-1-nop-metadata-security-attack-surface-closure.md`（mission-driver EXEC_PLANS）执行完成。3 个 confirmed live defect（2×P0 + 1×P1）全收口：攻击向量关闭、对抗性测试钉死、安全契约（fail-closed 内网拒绝 / 默认脱敏 / 注入 fail-fast）恢复成立。

- **Phase 1 — F1 HAVING 注入回归闭环**：`MetaAggregationExecutor.preprocessHavingArithmetic` 在递归入口对每个节点 `removeAttr(HAVING_EXPR_RESOLVED_ATTR)` 清除客户端可伪造的标记（contained 方案，不改 resolver 签名、不动 6+ 调用点），仅 `expr` 路径合法处理后重新置位。修复前 `TreeBean.createFromJson` 把任意客户端 JSON key 写为 attr，伪造 `havingExprResolved=true` + SQL payload `name` 可绕过 `nameResolverFor` 白名单把恶意 SQL 原文回填 HAVING。活载荷验证（硬门禁）：经真实 `buildExternalAggregationSql` 路径（preprocess → nameResolverFor）构造 forged marker + `(SELECT COUNT(*) FROM mysql.user...)` payload 实跑 → fail-fast `ERR_AGGR_HAVING_UNKNOWN_NAME`。对抗性单测 4 例（preprocess 清除 + 递归子树 + live-path + 既有合法 expr 不回归）。
- **Phase 2 — F2 多主机 JDBC URL SSRF 绕过闭环**：`MetaDataSourceConnectionProcessor` 重构 `extractHost`→`extractHosts`（返回主机列表），覆盖三种 MySQL/PG 多主机 URL 语法：逗号分隔 `h1,h2:port`、`address=(host=h1)(port=p1),address=(host=h2)`、key-value `(host=h1,port=p1),(host=h2,port=p2)`（`splitTopLevelCommas` paren-depth 跟踪，不切括号内逗号）。`validateJdbcUrl` 对列表中**每一**主机执行 `HostSecurityUtil.isInternalHost`。修复前 `extractHost` 在第一个逗号处截断，只校验第一主机，内网第二主机未经校验（驱动故障转移连到未校验主机）。对抗性单测 6 例（逗号/address/key-value 三形式各含内网第二主机被拒 + 全外网不误伤 + 第一主机内网回归）。既有 33 例全绿（含 IPv6/userinfo/IP 记法）。接线验证：`testConnect` 等入口均经前置 `validateJdbcUrl` 拦截。
- **Phase 3 — AR-04 POJO 脱敏缺口闭环**：`MetaModelChangedEventPublisher.buildEntitySnapshot` POJO 回退分支 stringify→parse 得到 Map 后路由回 `redactSensitiveKeys`（Map 分支抽出的共享脱敏 helper），不再以 `JsonTool.stringify` 反射序列化泄露敏感字段。三分支（ORM/Map/POJO）对同一敏感 key 产出一致脱敏。删除"本 helper 实际只接收 ORM 实体"误导注释。对抗性单测 2 例（POJO 含 password/connectionConfig 脱敏 + 三分支一致性断言）。
- **验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；F1/F2/AR-04 受影响测试类全绿（TestHavingArithmeticPreprocess 14、TestExternalAggregationProcessor 38、TestMetaDataSourceConnectionSecurity 37、TestMetaDataSourceConnectionProcessorExtract 6、TestMetaModelChangedEventPublisherSecurity 9）；`checkstyle:check` 项目级 pre-existing violations 全在 nop-api-core 依赖模块（项目 checkstyle 已禁用，与本次变更无关）。
- **Doc-sync**：`docs-for-ai/03-modules/nop-metadata.md` 新增「安全契约（攻击面闭环）」段（HAVING fail-fast / 多主机 SSRF fail-closed / 三分支脱敏一致）。
- **审计盲区声明回应**：F1 reachability（GraphQL TreeBean 绑定路径）由 live-path 测试（buildExternalAggregationSql 真实运行时路径）证实 closed。

### DRAFT_PLANS nop-metadata 不变式闭环再审计 → 3 remediation plan（mission-driver）

mission-driver DRAFT_PLANS 步骤：处理 2026-08-14 不变式闭环再审计（multi + open，两份 `Audit Status: open`）的 P0/P1 共 8 项，拟制 3 个 remediation plan；P2 共 25 项入 mission roadmap Follow-up Backlog。两份审计 `open → planned`。

- **P0/P1 分配（按 closure surface 捆绑）**：
  - `2026-08-14-0707-1-nop-metadata-security-attack-surface-closure.md`（安全攻击面；F1 HAVING 注入回归 P0、F2 多主机 SSRF 绕过 P0、AR-04 POJO 脱敏缺口 P1）→ `active`
  - `2026-08-14-0707-2-nop-metadata-silent-wrong-result-and-contract-fixes.md`（静默错算与契约；AR-01 SLA 分数截断、AR-02 SLA NFE 逃逸、AR-03 group-key 碰撞、F4 selection 静默丢弃）→ `active`
  - `2026-08-14-0707-3-nop-metadata-invariant-enforcement-in-default-build.md`（F3 INV-LIMIT 默认 surefire 防御纵深）→ `active`
- **关键事实订正（F3）**：独立审查 fresh session + lead agent 双重核实 live CI —— F3 的 P1 前提"默认 CI 不 guard INV-LIMIT"**不成立**：I5 已通过 `.github/workflows/maven.yml` 的 `invariant-gate` job（push/PR 触发）+ `run-nop-metadata-invariants.sh` step 4 fail-fast 运行 `TestLimitNegativeValueInvariant`。F3 recommendation 的可接受档（route B）已满足。F3 重裁为"残留 = 防御纵深/陈旧配置清理"，仍以 `Fix` 收口（满足非可降级），但范围严格限定。
- **审查闭环**：每个 plan 经独立 fresh session 对抗性审查（含想象性分析）2 轮至共识（无 Blocker / 无 Major）后转 `active`。round 1 修复要点：plan-1 测试例数 25→33（live 核实）、nameResolverFor 调用面 + contained 方案、address-list 完整测试 URL；plan-2 Phase 3 条件性 Targets + Protected Area（`INopMetaTableBiz`）约束 + owner-doc 路径 + week 分支；plan-3 核心前提订正。
- **P2 → backlog**：`ai-dev/backlog/nop-metadata-invariant-loop-roadmap.md` 新增 `## Follow-up Backlog` 段，登记 F5–F19 + AR-05–AR-14（每项标注源审计路径）。
- **doc-link checker**：`check-doc-links.mjs --strict` 报 17 errors 全为**预存**（HEAD 已存在，跨模块/跨仓库引用，如 roadmap:5 的 nop-chaos-flux `docs/backlog/ai-invariant-loop-roadmap.md`）；本次 3 个新 plan 文件**零** broken links，未引入新断链。预存断链属其它模块范围，不在本任务 scope。

### plan I5+I6 nop-metadata 不变式全量验证、门禁提升与 Cycle 1 收口（含 limit L1 收敛）

plan `ai-dev/plans/2026-08-13-1930-5-nop-metadata-invariant-verify-and-cycle-closure.md`（roadmap Cycle 1 / I5+I6）执行完成。Cycle 1 全量收口：4 门禁零命中、门禁提升为阻断式 hard CI gate、聚合入口搭建、稳态判定、独立 closure audit。

- **Phase 0 — limit L1 收敛（path ①，I4 deferred 项落地）**：I4 Phase 4 limit L1（`queryTableData` 对负 limit 静默封顶）按超时机制拆 successor plan，本计划 Phase 1 baseline 预期"4 门禁零命中"与其冲突。核对 MA7.4-03 原始 finding（`ai-dev/audits/2026-08-05-0856-arm-MA7.4-nop-metadata-lineage-performance.md:65`）确认 **MA7.4-03 范围 = "无 limit 默认值/上限：全表拉入内存"（null/大正值），不含负值**——I4 将负值静默封顶归因为"MA7.4-03 裁定"为误归因（负值封顶是 `<= 0` 子句的附带捆绑，非 MA7.4-03 意图）。故采用 path ①：`NopMetaTableBizModel#normalizeQueryLimit` 改为 `limit < 0` 抛 `ERR_PAGINATION_LIMIT_INVALID`（与 `normalizeJoinQueryLimit`/AR-09 先例对齐），保留 MA7.4-03 实际意图（null/0→默认值、大正值→封顶）。**不变式无弱化**（INV-LIMIT 统一适用 4 入口，无例外）；MA7.4-03 回归测试（`testQueryTableDataDefaultLimitCapsUnboundedResult`，null limit）仍 PASS。`TestLimitNegativeValueInvariant` 4 tests 0 failures（queryTableData 由 FAIL 转 PASS）。
  - owner-doc 同步：`docs-for-ai/03-modules/nop-metadata.md` limit 语义段更新（queryTableData 负值统一显式拒绝）；`invariant-catalog.md` INV-LIMIT 陈述更新（移除已过时的"queryTableData 静默封顶"例外例，文档化 path ① 统一收敛）。
- **Phase 1 — 全量验证与门禁零命中记录**：`./mvnw test -pl nop-metadata -T 1C` → **1081 tests, 0 failures**（nop-metadata-service）+ web 1 test 0 failures，BUILD SUCCESS。4 门禁复跑零命中：`check-silent-swallow.mjs` exit 0（130 块 / 0 hit）、`check-orm-unique-key-constraint.mjs` exit 0（37 UK / 0 hit）、`check-sensitive-literal-leak.mjs` exit 0（0 hit）、`TestLimitNegativeValueInvariant` 4 tests 0 failures。与 I1 棘轮零点 81 项比对：**81 → 0 全清确认**（80 silent-swallow + 1 limit 全清）。
  - 注：`-am` 全量构建时 `nop-stream-rocksdb` 出现 1 次 flaky FAILURE（并行资源竞争），单跑 `./mvnw test -pl nop-stream/nop-stream-rocksdb` 83 tests 0 failures PASS——与本次变更无关（本次仅改 nop-metadata-service）。
- **Phase 2 — 门禁聚合入口 + CI Node 环境 + hard-gate 接入**：
  - 新建 `ai-dev/tools/run-nop-metadata-invariants.sh`（bash 包装器，`set -euo pipefail`，4 门禁序列，`MVN="${MVN:-./mvnw}"` 环境变量切换本地/CI Maven）。
  - `.github/workflows/maven.yml` 新增 `invariant-gate` job（`needs: build`）：checkout + JDK 21（limit 测试）+ pnpm@10 + Node 20（3 扫描器，`cache-dependency-path: ai-dev/tools/pnpm-lock.yaml`）+ `pnpm install --frozen-lockfile`（`working-directory: ai-dev/tools`）+ `MVN=mvn ./ai-dev/tools/run-nop-metadata-invariants.sh`。**非零即红**，无 `continue-on-error`。
  - **hard-gate proof（本地 proof (A)）**：`NopMetaTableBizModel.java` 临时注入空 catch → `check-silent-swallow.mjs` 报 131 块 / 1 hit（`:427`）、**exit 1** → 移除 → 130 块 / 0 hit、**exit 0**。git diff 确认临时代码已清除。
  - 新建 `docs-for-ai/02-core-guides/invariant-guards.md`（门禁清单 + 运行命令 + 棘轮规则 + 表完备性说明 + 复触发条件）；更新 `docs-for-ai/INDEX.md` 路由。doc-link checker：新 owner-doc 0 broken links / 0 BOUNDARY errors。
- **Phase 3 — Cycle 1 收口统计与稳态判定**：
  - 全量指标：门禁数 **4**、red list 总数 **81**、修复数 **81**（80 silent-swallow I4 formalize + 1 limit L1 本计划 Phase 0 path ①）、对抗探查新族数 **0**（I2 五方向）。
  - 稳态判定：4 门禁零命中 + I2 对抗探查 0 新族 → **稳态暂停**，等待复触发条件。
  - 候选不变式裁定（基于 `invariant-catalog.md` §候选不变式 初步评估确认）：① 类型/方言兼容性族（AR-20/AR-23⑧）= 单点命中无兄弟复发 → **不沉淀（watch-only，周期复探）**；② 并发竞态/UK 幂等族（P2-MA3-03）= DB UK 已 fail-loud 兜底 → **不沉淀（watch-only，周期复探）**。两者均**不派 Cycle 2 / I1**。
  - 复触发条件登记：CI `invariant-gate` 变红 / 新增或重命名 processor·bizmodel·ORM entity / 周期复探。
  - 独立 fresh session closure audit：见 plan `Closure` 段 evidence。
- 验证：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -T 1C` 1081+1 tests 0 failures；4 门禁零命中（exit 0 / 0 failures）；`checkstyle:check` nop-metadata 0 violations（pre-existing failures 全在 nop-api-core 依赖模块，项目 checkstyle 已禁用）；`check-doc-links --strict` 新 owner-doc 0 errors；`run-nop-metadata-invariants.sh` 一键 exit 0。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md`（limit 语义）、`docs-for-ai/02-core-guides/invariant-guards.md`（新建）、`docs-for-ai/INDEX.md`（路由）、`invariant-catalog.md`（INV-LIMIT 陈述）。
