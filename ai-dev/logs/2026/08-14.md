# 2026-08-14 开发日志

## nop-datav Plan {3} Service-Layer Contract Correctness：Phase 2（Dim14-02 报告交付 SMTP 移出 session）

### 2026-08-14（mission-driver EXECUTE：plan {3} Phase 2）

执行 plan `ai-dev/plans/nop-datav/2026-08-10-2025-3-service-layer-contract-correctness.md` Phase 2，闭合 audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 **Dim14-02 [P1]**（`ReportDeliveryExecutor.doExecute` 在 `runInNewSession` 内执行同步 SMTP，跨远程调用持有 JDBC 连接，并发 cron 下可致连接池耗尽；且 email 在 SUCCEEDED UPDATE 提交前发出）。

**改动**：

- `ReportDeliveryExecutor`：拆分原 `doExecute`（全程单 session）为：
  - `runDelivery`：编排总入口（`execute` 异步 / `executeSyncForTest` 同步共用）。
  - `runDeliveryInSession`（Part A，session 内）：加载 → grace → running → 校验 → 取数 → 落盘 → **SUCCEEDED 提交**（不再含 sendReport），返回 `PreparedDelivery`（task + delivery 快照）；skipped/failed 返回 null。
  - `sendNotificationOutOfSession`（Part B，session 已关闭）：调 `notificationSender.sendReport`（SMTP）。
  - `recordDeliveredChannels`（新 session，成功回写 deliveredChannels，仅 SUCCEEDED 时）/ `rollbackSucceededToFailed`（新 session，邮件失败强制覆盖 SUCCEEDED → FAILED + task.lastRunStatus 回退）。
  - `PreparedDelivery` 内部 holder（session 内 SUCCEEDED 提交后供 session 外 SMTP 送达用）。
- `execute` / `executeSyncForTest` 均改调 `runDelivery`，两条路径行为一致（同步路径契约更新：返回时邮件已发或回补 FAILED）。
- `MockEmailSender`：扩展 `onSend`（sendEmail 入口回调，供测试观察 SMTP 调用时机的 DB 状态）+ `failOnSend`（模拟 SMTP 失败）；`reset()` 一并清。
- owner-doc：`ReportDeliveryExecutor` 类 javadoc + `schedule-report-design.md §12` ReportDeliveryExecutor 行更新为反映 SMTP 移出 session（§3 scheduler runInNewSession 意图不涉及 executor SMTP，仍与 live 一致）。

**接线验证（Anti-Hollow，非仅函数位置移动）**：

- **sendReport 运行时确于 session 外调用**：`testSendReportHappensAfterSucceededCommit` 在 sendEmail 时机读取该交付记录已提交状态，断言 == **SUCCEEDED**（旧代码 sendReport 在 setStatus(SUCCEEDED) 之前，此处会读到 RUNNING/PENDING），且 deliveredChannels 此时为 null（邮件成功后才回写）。
- **邮件失败回补 FAILED**：`testNotificationFailureRollsBackSucceededToFailed` 注入 sendEmail 抛错，断言终态 **FAILED**（SUCCEEDED 已先提交，被强制覆盖）+ errorMsg 含 "notification" + task.lastRunStatus 回退 failed + 无成功发送记录。消除「邮件已发但记录 RUNNING/SUCCEEDED」窗口。

**验证**：

- `./mvnw test -pl nop-datav -T 1C`：BUILD SUCCESS，**nop-datav-service 398 tests, 0 failures, 0 errors**（含 TestNopDatavReportE2E 15 例：原 13 + Dim14-02 新增 2）。
- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- 既有报告交付 E2E 全绿，未因 session 拆分回归（主线、grace/skipped、5 条 FAILED 路径、恢复、scheduler 吞错）。

**Follow-up**：Phase 1/2 均完成，下一步收口 plan（Plan Status → completed，闭合 audit Dim09-01/Dim14-02）。

---

## nop-datav Plan {3} Service-Layer Contract Correctness：Phase 1（Dim09-01 面板查询错误策略）

### 2026-08-14（mission-driver EXECUTE：plan {3} Phase 1）

执行 plan `ai-dev/plans/nop-datav/2026-08-10-2025-3-service-layer-contract-correctness.md` Phase 1，闭合 audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 **Dim09-01 [P1]**（`PanelParamEvaluator.evaluate` 4 处裸 `IllegalArgumentException` 直穿公共 `getPanelData` GraphQL action，破坏两层错误策略与错误码可观测性）。

**改动**：

- `PanelParamEvaluator.java`：4 处裸 `IllegalArgumentException`（invalid JSON / 非 object / rule 非 object / 缺 source）→ `NopException(ERR_DATAV_INVALID_PARAM_CONFIG).param(ARG_REASON, ...).cause(e)`（invalid-JSON 保留 cause 链）。静态工具层无 panelId 上下文，源点不填 panelId。
- `PanelDataBinder.queryPanelData`（:131）：`evaluate(...)` 外包 `try/catch(NopException ne) { throw ne.param(ARG_PANEL_ID, panelId); }`，补 panelId 后 rethrow（catch 紧贴 evaluate，仍在 executeQuery try 之外）。
- `NopDatavErrors.ERR_DATAV_INVALID_PARAM_CONFIG`：params 声明追加 `ARG_PANEL_ID`。
- **裁定（与 plan 字面建议的偏差）**：message 模板未硬编码 "for panel:{panelId}"，改为中性 `"Invalid param config: {reason}"`。原因：该 ErrorCode 被 `PanelParamEvaluator`（panel paramMapping）、`DashboardParamParser`（dashboard paramConfig，经 `NopDatavDashboardBizModel` 公共 action）、`DashboardFilterUrlCodec/Resolver`（filter）**共用**；硬编码 panel 名词会误标看板/filter 错误（`getDescription()` 用户可见，`{panelId}` 在看板路径为 null → 渲染空）。panelId 仍作为结构化 param（`getParam("panelId")`）供 GraphQL 错误响应/聚合，满足 plan 核心 goal「panelId 上下文保留」。

**测试**：

- 新增 `TestPanelParamErrorContract`（5 例）：4 类配置错误经公共入口 `PanelDataBinder.queryPanelData` 断言 `NopException` + `errorCode == ERR_DATAV_INVALID_PARAM_CONFIG` + `param(ARG_PANEL_ID)==期望` + `param(ARG_REASON)` 非空；invalid-JSON 断言 cause 非空，其余 3 类校验失败无底层异常 cause 为 null（非吞异常）。+ 1 回归保护（合法 paramMapping 正常）。

**验证**：

- `./mvnw install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav -T 1C`：BUILD SUCCESS，**nop-datav-service 396 tests, 0 failures, 0 errors**。
- owner-doc `error-handling.md` 两层策略与本修复一致（公共 API 用 NopException+ErrorCode），无矛盾条款 → No owner-doc update required。

**Follow-up**：Phase 2（Dim14-02 报告交付 SMTP 移出 session）待执行。

---

## nop-datav Plan {2} Export Task State-Machine Correctness：closure audit 闭合

### 2026-08-14（mission-driver EXECUTE：plan {2} closure）

执行 plan `ai-dev/plans/nop-datav/2026-08-10-2025-2-export-task-state-machine-correctness.md` 的收口审计（3 个 Phase 此前均已 completed 并全 `[x]`，本次为 Closure Gates 验证 + plan 关闭），闭合 audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 Dim14-01 [P1]（per-request 恢复误杀在途任务）、Dim07-01 [P1]（cancel 被 SUCCEEDED 覆盖）、Dim16-01 [P1]（cancel 测试为空壳）。

- **Plan Status**：从 active → **completed**。3 个 Phase 全 completed（此前执行）；Closure Gates 11 项全部 `[x]`。
- **独立 closure audit**：fresh subagent（ses_0020b9702ffeTV10AMRkekveTG）8/8 gates PASS，live-code evidence 写入 plan `Closure` 段。

**本次收口验证（live code 追踪，非计划声称）**：

- **Dim14-01**：`NopDatavExportTaskBizModel.createExportTask`（:126-157）无 `recoverInterruptedTasks()` 请求路径调用（仅 :146 注释）；恢复仅由 `NopDatavExportTaskRecovery @PostConstruct init()`（:54-61）启动期执行；public 委托保留于 BizModel:477-479（测试/管理入口，非请求路径）。
- **Dim07-01**：`PanelDataExporter` 经 `BooleanSupplier cancelChecker` 在面板间(:174)/取数后写出前(:117,:182)/写出循环内(writeCsv:209,toSheet:272)真实 `checkCancelled`，命中抛 `ERR_DATAV_EXPORT_FAILED`（:83，退役 backlog Dim09-04 死码）。`executeTask` pre-SUCCEEDED 复检(:264，早于 :269)+ catch 分流(:278，早于 :283 setStatus(FAILED))→ cancel 命中不写 FAILED/SUCCEEDED，终态保留 cancel 线程的 CANCELLED。三层防护（exporter checkpoint + pre-SUCCEEDED 复检 + ORM versionProp 锁 `_app.orm.xml:476`）。
- **Dim16-01**：`testCancelDuringExecutionEndsInCancelled`（TestNopDatavExportE2E:313-379）用 createExportTask 真实 submitExecution（非 seedTask），D4 seam 双 CountDownLatch 确定性 RUNNING 窗口，断言终态 CANCELLED + fileRecordId null + errorMsg 含 cancel。`testCreateExportTaskDoesNotKillInFlightTasks`(:424) Dim14-01 回归。

**本次改动（仅文档漂移修复 + plan 关闭）**：

- `NopDatavExportTaskRecovery.java` javadoc 漂移修复：旧注释声称「BizModel 在每次发起导出前也会调用一次」，Phase 1 已删除该调用，更新为反映当前行为（恢复仅 @PostConstruct）。
- plan `Closure` 段写入 evidence，Closure Gates 11 项 `[x]`，Plan Status → completed。

**验证**：

- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav/nop-datav-service`：BUILD SUCCESS，**391 tests, 0 failures, 0 errors, 0 skipped**。
- 注：`-pl nop-datav -am` 全链路时上游 `nop-auth-service` 有 2 个预存失败（TestBeanLoader bean XML 格式 / TestChannelScanBindLoginE2E VarCollector NPE，平台级，与本 plan 改动无关），nop-datav-service 单模块 391 全绿。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：exit 0，0 unchecked。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：1 high 发现（NotificationSender.java:311）经审查为 **FALSE POSITIVE**（P6b 正则误匹配中文注释 "temp resource"，该处为完整实现，且属 D5 reports 范围，非本 plan 改动文件）。

**关于 audit 关闭**：audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 Remediation 含 3 个 plan（{1}✅、{2}✅ 本次、{3} pending Dim09-01/Dim14-02），故 audit `Audit Status: planned` 暂不关闭，待 Plan {3} 完成后一并关闭。

**Follow-up**：
- no remaining plan-owned work（plan {2} 关闭）。
