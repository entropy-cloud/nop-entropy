# 2026-08-14

### nop-ai-gateway-failover roadmap + mission 起草 + 独立审查共识（R1/R2）

- **新增**：`ai-dev/backlog/nop-ai-gateway-failover-roadmap.md`（W1-W8 + ★ Milestone）+ `missions/nop-ai-gateway-failover.json`（mission-check valid）。
- **Roadmap 结构**：W1 前置 spike（Q7 流式重订阅可行性 + Q2/Q5 决策暴露）→ W2 可靠性子集下沉（18 类 + 异常/错误码迁移 + buildModelKey）→ W3 llm.xdef 并发上限（protected area）→ W4 dialect 双向转换（Anthropic/Gemini/Ollama/Responses）→ W5 模型类路由组 + 选择策略（粒度约束注记）→ W6 本地适配器 + 流式 → W7 网关形态 + converter 扩展 → W8 指标 + docs 收口。依赖主链：W2/W3 → W5 → W6/W7 → W8。
- **审查**：两轮独立 fresh-session 子 agent（review-only）：
  - **R1**（approved，零 Blocker + 3 Major 建议 + 5 Minor）：M-01 依赖图/Stages 缺 W4→W6/W7、W1→W6 边 → 补边至 14 条逐边一致；M-02 W5 粒度超单 plan → 粒度约束 + plan-first 拆分裁定；M-03 mission 授权先例引用失实（credential-mfa 无授权文本，真实先例为 nop-ai-invariant-loop）→ 改引。Minor：Purpose 段、W2 清单补全（IProviderFailoverQueue/伴生类）、SINK-04 补 CompactionAwareTruncation、W4 枚举四 dialect、ROUTE-03 Q10 裁决注。
  - **R2**（**approved 终审**）：R1 全部 8 项修复逐条核实到位，依赖图 ↔ Stages 表 14 边双向吻合，mission 引用先例真实（nop-ai-invariant-loop.json 实测），无 Blocker/Major/回归。共识达成。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 4 pre-existing errors 零新增；`mission-check` valid。
- **启动命令**：`./ai-dev/tools/mission-driver.sh run nop-ai-gateway-failover`（待用户确认启动）。


### nop-ai-gateway 透明账号切换（failover）需求规格 — 独立审查循环（R1-R4）

- **对象**：`ai-dev/design/nop-ai-gateway/02-account-failover-requirement.md`，多轮独立 fresh-session 子 agent 审查（review-only）+ 逐轮修复：
  - **R1**（fail，1 blocker + 2 major）：B1 无视已落地 nop-ai-agent reliability + nop-ai-core 账号链/凭证链现状 → "复用优先" + §四.3 裁决表；M1 converter stream=false；M2 可观测性/密钥治理。
  - **R2**（fail，4 major）：M-1 账号字段结构；M-2 coordinator 双通道语义；M-3 熔断粒度 provider:model；M-4 本地形态包装 ChatServiceImpl 而非重建管线。
  - **需求修订**：用户追加① parseRequestBody 全 dialect 补全（双向转换）；② 账号级并发上限主动限流。
  - **R3**（fail，5 major）：MAJOR-1 429→RATE_LIMITED 分类事实；MAJOR-2 响应方向 buildResponse 仅 default；MAJOR-3 主账号无配置载体 → provider 级缺省；MAJOR-4 全池饱和 → fail-loud + 主动切换不耗预算；MAJOR-5 converter 固定属性 → 动态 dialect 解析 + 两层链。
  - **R4**（fail，1 major + 2 minor + 2 note）：**MAJOR-6 推翻 R3 MAJOR-1 修复方向**——响应级 `parseErrorResponse` + `errorMappings` 已落地（`ChatServiceImpl.java:150`，`default.llm.xml` 含 429 insufficient_quota → QUOTA_EXCEEDED），QUOTA_EXCEEDED **可达**，"body 被丢弃"仅对传输异常路径成立 → 改为双源分类如实陈述；MINOR 402 归属（→QUOTA_EXCEEDED 非 AUTH_INVALID）、并发饱和不走 provider 链决策、动态 dialect 需目标 provider 真实 LlmModel config；NOTE 流式重订阅可行性挂起为 Q7。
  - **R5**（**pass**，2 minor + 5 note）：补 NON_TRANSIENT 契约行、TRANSIENT 双源来源、全池饱和理由润色、concurrencyLimit vs rateLimit（跳过 vs 排队）区分、分类判定精确化、402 可达性注。
  - **R6**（**pass 终审**）：7 项修订逐条复核到位，零新问题——共识达成。剩余风险均为开放项（Q7 流式重订阅可行性、Q2 model 语义）。
- **需求修订（R6 后）**：用户追加三项——① 模型分类路由组（模型按级别/类组织，每类一组候选（模型+账号）形成类内 fallback 链，非全局一组）；② 动态选择机制（组内选择顺序不固定，可插拔选择策略，默认健康度+并发感知，可选规则配置）；③ 网关能力服务所有 agent（不限 nop ai agent），相关功能不放 nop-ai-agent → **LLM 可靠性子集下沉 nop-ai-core**（ThresholdBreaker/AccountChain/LlmErrorClassifier 等，含 NopAiAgentErrors/buildModelKey 迁移；agent 引擎专用 Checkpoint/GoalTracker 等留原处；LlmCallCoordinator 不迁移语义沿用）。
- **R7**（fail，1 major + 4 minor + 3 note）：M1 模型类路由组/选择策略归属矛盾（mermaid 画在复用层 vs §六 拒绝下沉）→ 新增 §4.1 能力归属分层表（策略接口/模型类数据结构 → nop-ai-core；网关编排 → nop-ai-gateway）；m1"模型类间切换"删除；m2 NopAiAgentErrors 迁移补全；m3 Q1 关闭 + channel 依赖不变声明；m4 buildModelKey 移入；n1 策略输入补已尝试候选集。
- **R8**（**pass 终审**）：归属四处一致（§4.1↔§4.2↔§4.3↔§六），3 minor + 4 note（状态行同步、Q6/Q9 已决标注、能力级限定、mermaid 标签、buildModelKey 措辞、术语统一）全部吸收——**共识达成（R1-R8）**。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 4 pre-existing errors，本次零新增。


### nop-ai-gateway 透明账号切换（failover）需求规格记录

- **新增**：`ai-dev/design/nop-ai-gateway/02-account-failover-requirement.md`（状态：草案）+ 更新该目录 `README.md` 阅读顺序。
- **需求要点**（经一轮澄清确认）：AI 后端失败（429/5xx/连接中断/超时/402 余额不足/401 禁用）时自动切换账号（允许跨协议风格 + 换模型），对前端透明；两种部署形态——nop-gateway 独立网关（拦截器链）与本地 `IChatService` 适配器（伪装本机接口，实现新 `IChatService` 而非已废弃 `IAiChatService`）；账号配置经接口抽象（凭证库可接入、直接配置亦可）；动态路由 + 熔断；流式从头重试（以首段缓冲窗口实现透明重试）。
- **现状基线**：`AiFailoverGatewayInterceptor` 仅支持非流式 URL 级 fallback（流式抛 UnsupportedOperationException），本需求升级为账号/模型级 + 流式 + 熔断。
- **开放问题**（记于文档 §五）：模块归属确认、前端 model 参数是否被路由覆盖、缓冲窗口/重试预算/熔断默认值、与 LLM 错误归一化层的对接。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict`（见下方）。

### W12-design closure：独立 closure audit（READY_TO_CLOSE）+ roadmap 同步 done + plan completed

- **Plan**：`ai-dev/plans/2026-08-14-2012-2-mfa-phase2-design.md`（**completed**，2026-08-14）
- **独立 closure audit**（fresh session 子 agent，task `ses_fff27b900ffe6dzou3KPuOhAIS`）：三 Phase Exit Criteria 全 PASS；**内容级 live 锚点抽查 10/10 PASS**（GraphQLExecutor:64/146 检查点、@BizMakerChecker→ReflectionBizModelBuilder:338-340→GraphQLObjectDefinition:224-225/260-261 传播链、putIfAbsentExAsync SETNX + put_if_absent_or_match.lua 幂等去重语义核实、DaoUserContextCache/UserContextImpl 白名单序列化核实、checkMfaRequired 一期原序（:746-747 行号精确）、OAuthLoginServiceImpl 一期遗留绕过属实、§5.3.0 十行锚点全对、MfaVerifyRequest 三字段现状、mfaVerifyAsync 公开+headers 链路、姊妹实体 no-tenant 先例）；deferred 诚实性 PASS（四主题核心设计未降级）；结论 **READY_TO_CLOSE**。audit 发现 1 Minor（§一 索引行"8 项"笔误）已在 closure 回填时修正。evidence 已写入 plan Closure 段。
- **收口动作**：roadmap W12-design `planned` → `done`（header Last updated 同步）；plan `Plan Status: completed` + Closure Gates 10/10 勾选 + Closure Evidence 回填；`check-plan-checklist.mjs --strict` 退出码 0（45/45 全勾）；`check-doc-links.mjs --strict` 退出码 0（2284 文件 / 0 errors）。W12-impl ~ W15-impl 依赖解锁（roadmap 取第一个 `todo` 规则下个工作项为 W9-impl，凭证组先行）。
- **基线观察**：纯文档计划按 plan Closure Gates 说明 mvnw 门禁 N/A（零产品代码变更，git 工作区仅 `ai-dev/` 文档改动）。
- Doc-sync：roadmap（W12-design done + header）+ plan Closure 段 + `ai-dev/design/README.md` + `ai-dev/design/nop-auth/README.md`。

### W12-design 执行 Phase 3：跨主题收口 + 设计→impl 映射 + 索引同步 + doc-links 门禁

- **Plan**：`ai-dev/plans/2026-08-14-2012-2-mfa-phase2-design.md`（Phase 3 completed）
- **§七 deferred 裁定**：7 项全部落 classification（2 out-of-scope improvement：WebAuthn attestation 完整信任链、外部 MFA 服务接入；1 out-of-scope improvement：信道路径可信设备豁免（设计边界）；1 optimization candidate：challenge/验证码 DB store 批量清理（一期 W8 惰性清理裁决沿用）；1 out-of-scope improvement：前端交互；2 watch-only residual：@MfaRequired 标注分布治理（A2-audit 覆盖）、signCount=0 认证器克隆检测增强），每项带 Why Not Blocking 与 Successor Required；各主题"拒绝了什么"表的终局拒绝不重复登记。
- **§八 设计→impl 映射**：W12-impl（消费 §三 + §5.3.0 #5/#6；Protected Area：nop-graphql-core 框架核心五触点 + nop-biz-auth-core store 场景化 + nop_auth_mfa_challenge 加列 DDL + Redis 滚动升级 note）、W13-impl（消费 §四；Protected Area：新 ORM 实体 + IUserContext/序列化白名单两触点（Dao-cache fail-open 测试强制）+ LoginResult/ScanLoginResult + nop-auth-sso OAuth 接入）、W14-impl（消费 §5.3.0 全表 + §5.3.1/2/5；Protected Area：NopAuthMfaCredential + MfaVerifyRequest assertion + 第三方依赖 POC）、W15-impl（消费 §5.3.3 + §六；Protected Area：实体×2 + EmailCodeStore 接口 + rememberDevice/trustedDeviceRegistered 字段）。
- **§九 关系段落**：上游复用（一期基线/nop-nosql 原语/IEmailSender/注解传播先例/审计/lessons 15）、同层（姊妹篇 nop-credential 二期设计）、下游（四 impl plan + docs-for-ai 由 impl 补充 + A2-audit 回归基准）、参照（GitHub sudo 拒绝先例/WebAuthn L2/vision 收敛路径）。
- **索引同步**：`ai-dev/design/README.md` nop-auth 行更新（"一期已落地 + 二期设计已定稿待实施"四主题摘要）；`ai-dev/design/nop-auth/README.md`（Phase 1 已纳入 02 条目）。
- **门禁**：`node ai-dev/tools/check-doc-links.mjs --strict` 首跑 1 error（范围行 `nop-service-framework/nop-graphql-core` 被 checker 按 repo 相对路径解析且路径本身不完整）→ 改为完整 repo 路径 `nop-service-framework/nop-graphql/nop-graphql-core` 后复跑 **退出码 0**（2284 文件，0 errors）。
- 验证：documentation-only（No new test required）。
- Doc-sync：`02-mfa-phase2-design.md` §七/§八/§九 + `ai-dev/design/README.md`。

### W12-design 执行 Phase 2：四主题小节产出（每小节独立子 agent review 后回修）

- **Plan**：`ai-dev/plans/2026-08-14-2012-2-mfa-phase2-design.md`（Phase 2 completed）
- **产出**：`02-mfa-phase2-design.md` §三 操作级（@MfaRequired 注解 + executor 层拦截 + 操作级 challenge 两段式一次性票 + MfaChallengeStore 场景化扩展 + MfaFactorVerifier 共享组件）+ §四 角色策略（NopAuthRoleMfaPolicy 持有约束模型 + 三层判定矩阵 + 受限会话 + 登记 channel proof 防 enrollment attack + OAuth 一期遗留绕过修复）+ §五 因子扩展（形态裁决不枚举化 + §5.3.0 白名单 10 处变更标注 + WebAuthn 三 ceremony + EmailCodeStore 平行设计 + 外部服务 deferred + 恢复码/防重放矩阵）+ §六 可信设备（请求头指纹 + 固定窗口豁免 + 撤销矩阵 + allowTrustedDevice AND 合并）；§一 结论索引 + §二 一期契约兼容性总矩阵落盘。
- **四轮独立 fresh-session review（每小节完成后立即审，发现全部回修）**：
  - §三：task `ses_fff5c4608ffeV6oOYKXa4aY7CB`——2 Blocker（mfaRestricted 悬挂引用未写 §4.3；伪代码 consume 返回值忽略=并发双花）+ 4 Major（markVerified 原子性/TTL/续命契约缺失；MfaFactorVerifier lastVerifiedWindow 场景策略缺失；IOperationMfaChecker 接线载体未定会误触 nop-core；subscription/publicAccess 静默绕过）+ 6 Minor 全部回修：consume 返回值判定、markVerified 三实现原子性表（Redis 派生票键 SETNX——核实 put_if_absent_or_match.lua 为幂等去重非 CAS）、TOTP 窗口统一推进裁决、接线钉 GraphQLEngine+IGraphQLExecutionContext（不触碰 IServiceContext）、subscription/publicAccess 构建期拒绝、GraphQLObjectDefinition 合并搬运、operation 命名契约、租户包裹、批量语义。
  - §四：task `ses_fff4fc385ffehQsa4e38lYweZ7`——2 Blocker（mfaRestricted 事后置标志在 Dao-cache 反序列化全新对象下不持久化=fail-open——live DaoUserContextCache 白名单序列化核实；矩阵行 4 与伪代码矛盾 + 白名单漏 unbindMfa=弱因子用户锁死）+ 5 Major（受限分支须前置于 hasMfaRequired 早退；channel proof 会话标记与 §3.4 原则冲突→改 store 一次性票；LoginResult 无受限信号；角色继承口径未定义→钉 buildUserContext 快照同口径；OAuthLoginServiceImpl 一期遗留绕过→纳入 W13 修复）+ 7 Minor 全部回修。
  - §五：task `ses_fff404b86ffeZPZiPZbNDNVHnZ`——1 Blocker（unbindMfa 的 webauthn 适配整体缺失=按字面实现自锁死→补解绑 ceremony webauthnBeginVerify）+ 3 Major（create 场景重载签名两处矛盾+sessionId 空悬；payload 后置更新原语缺失→改 create 时一次写入 cryptoChallenge；webauthn×操作级联动缺失→MfaFactorCredential 统一载体）+ 5 Minor（factorLevel 归属 W13；send(key) 措辞；dict 落位 nop-biz-auth-core；#8 行号 647-675；ceremony 失败路径）全部回修。
  - §六：task `ses_fff362943ffeBbmqDcTiE3A35W`——1 Blocker（SSO/信道路径豁免语义缺失且"非跨模块 API"声明不成立→裁"信道不适用不登记"，headers 仅密码路径）+ 3 Major（allowTrustedDevice 多角色合并未定义→AND fail-safe + evaluator 复合产出；过期行×唯一约束×max-count 四者交叉撞键→upsert 含过期行复活+max-count 仅计未过期；MfaVerifyRequest/LoginResult 增量未走 Protected Area 声明手续）+ 5 Minor（device-id 改自定义请求头传输；签名前向注记；同 hash 覆盖先于满员判定；null 指纹短路；实体 no-tenant 先例）全部回修。
- 验证：documentation-only（No new test required / No owner-doc update required beyond design docs）。
- Doc-sync：`02-mfa-phase2-design.md` §一~§六 全量落盘。

### W12-design 执行 Phase 1：基线复核 + mfaType 白名单盘点落盘 + 粒度裁决 + 文档骨架

- **Plan**：`ai-dev/plans/2026-08-14-2012-2-mfa-phase2-design.md`（Phase 1 completed）
- **基线复核（Proof，零 drift）**：逐项核对一期 live 锚点与 `00-vision.md`/`01-architecture-baseline.md`——ORM 四实体（`nop-auth.orm.xml:1060` 起 MfaSetting/RecoveryCode/MfaChallenge/SmsCode）、`NopAuthConstants.MFA_TYPE_*`（:42-43）、store 三实现 + `MfaStoreProvider`（collect-beans `nopMfaChallengeStore_`/`nopSmsCodeStore_` + `ioc:condition` 条件激活 + 默认 db）、`LoginServiceImpl` 两阶段链路（:319/:350 `ERR_AUTH_MFA_REQUIRED`、:465-479 challenge 分支含 else fail-closed、:755 因子等同、:388 `completeLogin` 分界裁决）、`NopAuthUserBizModel` API 面（bindMfa:133/confirmMfa/unbindMfa/generateRecoveryCodes/getMfaStatus/resetUserMfa）、`NopAuthConfigs` 15 项配置、`NopAuthErrors` MFA/SMS 11 错误码、`IEmailSender`/`TencentEmailSender`、`ScanLoginResult` MFA 字段——全部与 plan Current Baseline 及设计文档一致，**无 Fix 项**（设计文档中 W6 落地确认注记与 live 同步）。
- **白名单盘点（Proof）**：`MFA_TYPE_` 全量 grep + `"totp"`/`"sms"` 字面量兜底 → 10 处校验/分支/承载点全清单（含 2 处隐性行为：`sendMfaCode:647` 隐式假设 SMS 不检查 mfaType；`verifyFactorForBind` 兜底 return false），落盘于 `02-mfa-phase2-design.md` §5.3.0（表格式，路径+行号+行为，自包含供 W14-impl/W15-impl 直接消费）。
- **粒度裁决（Decision）**：四主题合入单文件 `02-mfa-phase2-design.md` 不拆分——与 W9-design 先例一致；四主题共享一期契约锚点且交叉引用密集（操作级×受限会话白名单复用拦截器、可信设备×角色策略豁免、WebAuthn×challenge 场景化扩展）；roadmap 与 impl plan 链锚定该路径。
- **骨架（Follow-up）**：四主题小节五段结构（设计结论/背景与动机/核心设计/拒绝了什么/与一期契约兼容性）+ §七 跨主题 deferred 裁定区 + §八 设计→impl 映射区 + §九 关系段落全部建立；README 阅读顺序补 `02-mfa-phase2-design.md` 必读条目（第 3 位）。
- 验证：documentation-only（No new test required，Minimum Rules #25）。
- Doc-sync：`02-mfa-phase2-design.md` 骨架 + `ai-dev/design/nop-auth/README.md` 阅读顺序/职责边界。

### mission-driver 验证：`-am` 连带模块合并伪影修复，`:nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` 全绿

- **Mission**：`nop-credential-mfa`（plan `2026-08-14-2012-*`）；本步为 build 验证 + 提交，无产品代码变更（仅测试/资源修复）。
- **合并伪影 1（malformed XML）**：merge `2a822771f`（nop-stream-invariant-loop → 本分支）后 `nop-stream-runtime` 的 `stream-control-rpc.beans.xml` 部署 scaffold 注释**未闭合**（缺 `-->`，`</beans>` 落入注释内），文件无法解析 → `TestStreamControlRpcBootstrap` error + `TestStreamBeansDeploymentTemplateSyntax#controlRpcDeploymentTemplateIsSchemaCompliant` failure。修复：补闭合 `-->`。
- **合并伪影 2（bean id 陈旧断言）**：`4f3afbd6f`（bean nop-prefix 合规）把模板 bean id 改为 `nopStreamMessageService`/`nopChannelMessageService`，但 ① `TestStreamBeansDeploymentTemplateSyntax#dataPlaneDeploymentTemplateIsSchemaCompliant` 仍断言 `ref="streamMessageService"`；② `a2f9946b7`（AR-9）新增的 `TestChannelMessageServiceIoC#dispatchTimeoutMs*` 两处 `getBean("channelMessageService")`。修复：断言/查找统一为 nop-prefixed id（beans.xml 实际 id，语义不变，仅对齐命名合规后的真实 bean 名）。
- **验证**：`./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` → **BUILD SUCCESS**（nop-stream-runtime 842 tests 0 fail、nop-ai-gateway TestChannelMessageServiceIoC 4/4）。typecheck（compile）与 `clean install -DskipTests` 同绿；checkstyle 未接入构建生命周期（root pom 中被注释），standalone goal 仅报 nop-api-core 预存噪声 → lint N/A。
- **flaky 备忘**：`TestRocksDBIncrementalRestoreAndBenchmark`（计时基准，`-T 1C` 并行负载下 ratio 2.58 假红，串行 0.414 通过）与 `TestManyToManyProp`（租户上下文隔离，隔离运行通过）为并行执行 flake，单测隔离均绿，未改产品代码。

### W9-design closure：独立 closure audit（READY_TO_CLOSE）+ roadmap 同步 done + plan completed

- **Plan**：`ai-dev/plans/2026-08-14-2012-1-credential-phase2-design.md`（**completed**，2026-08-14）
- **独立 closure audit**（fresh session 子 agent，task `ses_fff8aa362ffe1RlqC1HREJDJ4H`）：三 Phase checklist/Exit Criteria 全勾核对 PASS；文档 vs live 代码锚点抽查 20+ 处全部属实（含 reencryptAll 分页上限限制、usageScope 全仓库零消费、nop-auth.orm.xml 行锚精确、NopAuthConstants 角色名对齐）；§七 deferred 诚实性 PASS（reencryptAll 限制确认为 W10-impl 显式交付物而非静默 deferred）；结论 **READY_TO_CLOSE**。evidence 已写入 plan Closure 段。
- **收口动作**：roadmap W9-design `planned` → `done`（header Last updated 同步）；plan `Plan Status: completed` + Closure Gates 9/9 勾选 + Closure Evidence 回填；`check-plan-checklist.mjs --strict` 退出码 0（0 未勾选项）；`check-doc-links.mjs --strict` 退出码 0（2284 文件 / 0 errors）。
- **信息性基线观察**：`./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` 在被 `-am` 连带构建的**无关模块** `nop-stream-runtime` 存在 2 个预存失败（`TestChannelMessageServiceIoC`：`channelMessageService` bean 装配错误）——本 plan 零代码变更（git 工作区仅 `ai-dev/` 文档改动），失败与本执行无因果；纯文档计划按 plan Closure Gates 说明 mvnw 门禁 N/A。该预存失败属 nop-stream 模块问题，留给 nop-stream 相关工作项处理，不在本 plan 登记 successor。
- Doc-sync：roadmap（W9-design done + header）+ plan Closure 段。

### W9-design 执行 Phase 3：跨主题收口 + 设计→impl 映射 + 索引/roadmap 同步 + doc-links 门禁

- **Plan**：`ai-dev/plans/2026-08-14-2012-1-credential-phase2-design.md`（Phase 3 completed）
- **§七 deferred 裁定**：9 项全部落 classification（1 watch-only residual：消费链上下文丢失告警审计；3 optimization candidate：PKCE、usageScope 列物理删除、类型级 OAuth 应用凭据共享；5 out-of-scope improvement：status 全局收紧、user 凭证共享、凭证分组授权、KMS proxy 模式、KMS 厂商间迁移），每项带 Why Not Blocking 与 Successor Required；`@credential:` resolver 显式维持一期拒绝不翻案。
- **§八 设计→impl 映射**：W9-impl（消费 §三 + 回补点；Protected Area：nop-xdefs 的 credential-type.xdef 扩展 + 新增 publicAccess REST 面）、W10-impl（消费 §四；无 ORM/API 变更；交付完备性验证——reencryptAll 分页限制的修复/查询）、W11-impl（消费 §五+§六；Protected Area：nop-credential.orm.xml 加列 + 新实体 NopCredentialAuth + 标准 update/batchDelete 禁用；附规模超限拆分提示）。
- **索引同步**：`ai-dev/design/README.md` nop-credential 行"一期不做 RBAC/OAuth/KMS"→"二期设计已定稿待实施"；`ai-dev/design/nop-credential/README.md`（Phase 1 已纳入 02 条目）。
- **roadmap 三处已登记 drift 修正**：①Stage 8 deliverables "`usageScope` 一期占位（`instance`）语义定稿"→"语义定稿（裁定：废弃——live 列无默认值、全仓库无取值消费……）"；②W9-impl 行（Work Items）"复用 OAuthLoginServiceImpl 既有 OAuth 客户端能力"→入站/出站澄清（仅协议级参照，见设计 §三）；③同框架修正 Framework reuse 表 OAuth 行 + Stages 表 8/9 行 reuse 列。粒度裁决为单文件，"落 02-phase2-design.md"表述与裁决一致无需变更。
- **门禁**：`node ai-dev/tools/check-doc-links.mjs --strict` 首跑 1 error（设计文档中 VFS 路径 `_vfs/nop/credential/types/generic-secret.credential-type.xml` 被 checker 按 repo 相对路径解析）→ 改为完整 repo 资源路径后复跑 **退出码 0**（2284 文件，0 errors）。
- 验证：documentation-only（No new test required）。
- Doc-sync：`02-phase2-design.md` §七/§八 + `ai-dev/design/README.md` + roadmap 三处 drift。

### W9-design 执行 Phase 2：四主题小节产出（每小节独立子 agent review 后回修）

- **Plan**：`ai-dev/plans/2026-08-14-2012-1-credential-phase2-design.md`（Phase 2 completed）
- **产出**：`02-phase2-design.md` §三 OAuth（出站客户端引擎：先建实例再授权、state 一次性 bearer capability 绑定 credentialId、token 集引擎保留字段名、惰性刷新跨副本互斥、不组件级复用 OAuthLoginServiceImpl）+ §四 KMS（SPI 零变更、材料交付模式、启动期 fail-closed、密钥来源不混合+迁移残余窗口、同名 bean 覆盖装配、keyId→材料不变式）+ §五 归属（scope/ownerId 语义、usageScope 废弃、上下文捕获身份传播、per-method 归属矩阵、BizModel 两层防御、不依赖 nop-auth）+ §六 RBAC（NopCredentialAuth 二值 use 授权、默认开放可选收紧、判定矩阵、nop.credential.admin-roles 配置自持）；§一 结论索引 + §二 一期契约兼容性矩阵落盘。
- **四轮独立 fresh-session review（每小节完成后立即审，发现全部回修）**：
  - §三 OAuth：task `ses_fffaf6db7ffeTtLxogNyflaBLb`——1 Blocker（闭环数据流断裂：clientSecret 来源/更新目标歧义）+ 6 Major 全部回修：改"先建实例再发起授权"（state 绑定 credentialId）、保留字段名引擎命名空间、分组写语义（saveCredential 保留 token 字段）、跨副本互斥、同形复用措辞（非类引用）、status=disabled 显式增量。
  - §四 KMS：task `ses_fffa87a70ffeM5yFYR6vQPMQvT`——2 Blocker（装配机制三选一未裁、迁移窗口三要素不可推导）+ 5 Major 全部回修：裁"同名 bean 覆盖+配置门控"（修正 MfaStoreProvider 先例误引）、迁移残余列表精确规则（master-keys 残留=拒启、禁含 active key、关窗前置=完备性验证）、材料交付模式术语（纠正 Vault Transit/非导出 CMK 不兼容）、keyId→材料不变式、启动期 fail-closed 一刀切（删运行期抖动条款）、切换与轮换解耦、reencryptAll 分页上限如实登记（W10-impl 交付完备性验证）。
  - §五 归属：task `ses_fffa02adbffeFAosgW1P35lMTU`——3 Blocker（mask/test 与"管理员亦不例外"冲突、user 级 OAuth 回调解密死路、管理员判定无落点）+ 5 Major 全部回修：per-method 校验矩阵、§3.3 回调引擎内部通道联合裁定（唯一解密点不破）、`nop.credential.admin-roles` 配置（缺省 admin,nop-admin）、结论 5 读/写分级措辞、继承动作面收口（update/batchDelete 禁用）、单条访问归一"不存在"防枚举、admin 代建显式允许、NULL scope 视同 system、owner 消亡显式接受。
  - §六 RBAC：task `ses_fff97ac5affeeWq59U1zWNaSfJ`——1 Blocker（软删除×唯一约束矛盾）+ 3 Major 全部回修：改物理删除+幂等 grant/revoke（对齐 usage 惯例）、角色快照时效声明（用户角色回收=下次登录生效）、矩阵 "—" 定义+grant 拒绝 scope≠system、第 5 行信任边界消费方义务+§七 watch-only 登记；Minors（矩阵前置条件拆分、角色单级展开、@Auth 双源收窄、级联措辞、死 roleId 接受、收紧运维语义）一并回修。
- 验证：documentation-only（No new test required / No owner-doc update required beyond design docs）。
- Doc-sync：`02-phase2-design.md` §一~§六 全量落盘。

### W9-design 执行 Phase 1：基线复核 + 粒度裁决（单文件）+ 文档骨架 + RBAC 能力盘点落盘

- **Plan**：`ai-dev/plans/2026-08-14-2012-1-credential-phase2-design.md`（Phase 1 completed）
- **基线复核发现并修正 3 处 drift**（`ai-dev/design/nop-credential/01-architecture-baseline.md`）：①已登记的 `deleted/deleted=true` vs live ORM `DEL_FLAG/delFlag`（§3.3 表格行 + §3.5 删除语义，draft review 实证成立，照证修正）；②§3.3 表格 `id` → `credentialId`（live PK 列名）；③§3.4 管理面方法清单 `save` → `saveCredential`（live 标准 `save` 被覆盖禁用抛异常，明文输入唯一入口为 `saveCredential`，W3 明文边界三重防御的组成部分）；另将 §3.2 主密钥来源措辞对齐 live 配置项名（`nop.credential.master-keys` + `nop.credential.active-key-id`）。其余锚点复核一致：`ICredentialProvider` 6 方法 SPI 面、`NopCredential`（16 列）/`NopCredentialUsage`（credentialId+consumerRef 唯一约束）、`NopCredentialBizModel` 方法面、W7-successor 链（`IAiModelCredentialResolver`/`AiModelCredentialResolverImpl`/`ChatServiceImpl.buildHttpRequest` 钩点均实存）。
- **粒度裁决**：四主题合入单一 `02-phase2-design.md`（不拆分）——四主题共享一期契约锚点与消费侧校验点、交叉引用密集、规模可控（~500 行）、roadmap/impl 链均锚定该路径。结论写入文档头部与本日志。
- **nop-auth RBAC 能力盘点**落盘于 `02-phase2-design.md` §6.0（live 锚点：`NopAuthRole`/`NopAuthUserRole`/`NopAuthRoleResource`/`NopAuthRoleDataAuth` 实体 + `IUserContext`（ContextProvider 线程上下文）/`@Auth`/`GraphQLActionAuthChecker`/`IActionAuthChecker`→`SiteMapProviderImpl`/`IDataAuthChecker`→`DefaultDataAuthChecker` 校验链；盘点结论：授权粒度为 biz 操作级 + bizObj 行级，无实例级授权模型——RBAC 主题的输入 gap）。
- **文档骨架**落盘：四主题小节五段结构（设计结论/背景与动机/核心设计/拒绝了什么/与一期契约兼容性）+ §七 deferred 裁定区 + §八设计→impl 映射区；`nop-credential/README.md` 阅读顺序纳入 `02-phase2-design.md` 条目。
- roadmap 核对：W9-design `planned` + plan 引用已正确（无需修正）。
- 验证：documentation-only，无代码变更（No new test required / No owner-doc update beyond README）。
- Doc-sync：`01-architecture-baseline.md`（3 处 drift 修正）+ `02-phase2-design.md`（骨架+盘点）+ `nop-credential/README.md`（索引）。

### credential-mfa 二期：W9-design / W12-design 两份设计 plan 起草并通过两轮独立审查（mission-driver DRAFT_PLANS）

- 起草 `ai-dev/plans/2026-08-14-2012-1-credential-phase2-design.md`（W9-design：凭证库二期四主题设计文档）与 `ai-dev/plans/2026-08-14-2012-2-mfa-phase2-design.md`（W12-design：MFA 二期四主题设计文档），均为纯文档计划；impl 工作项（W9-W11-impl / W12-W15-impl）依赖设计产出，本轮不起草。
- 两轮独立子 agent 对抗性审查达成共识后两份 plan 标 `active`；roadmap W9-design / W12-design 标 `planned` 并回填 plan 引用。
- 审查发现并处置：W12 首轮 Blocker（repo 级 3 处预存 doc-link 错误会使门禁不可达）→ 当场最小修复；W9 首轮 Major（缺 nop-auth RBAC 能力盘点锚点）等 14 项发现全部回修（含实证 drift：`usageScope` 无 live `instance` 值、`01-architecture-baseline.md` `deleted=true` vs live `DEL_FLAG` 登记为执行期 Fix 项）。
- 顺手修复（与本 mission 无关的 repo 级预存错误，当日早前日志已记录为 pre-existing）：`ai-dev/backlog/nop-ai-invariant-loop-roadmap.md:5`、`nop-code-invariant-loop-roadmap.md:5/7` 三处 broken link（外部项目路径去反引号标注 / `skills/` → `ai-dev/skills/`）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（2284 文件，0 errors）。
- Doc-sync：roadmap（Work Items + header）已同步；设计文档本体待 plan 执行产出。

### credential-mfa roadmap 二期凭证组范围更新：凭证归属统一（系统级/用户级）替代租户隔离

- **用户裁决**：凭证模块不做租户隔离（租户为 Nop 平台全局可开启能力）；凭证归属统一"系统级 + 用户级"两种场景——同一密钥管理服务可能被多个外部系统共用，系统级凭证 = 共享/管理员管理，用户级凭证 = 个人私有。
- **改动**：
  - `ai-dev/backlog/nop-credential-mfa-roadmap.md`：header 范围更新注记；二期边界块追加用户裁决；W9-design 改为四主题（OAuth + 外部 KMS + **凭证归属统一** + RBAC 细粒度授权）；W11-impl 由"RBAC + 租户隔离"改为"凭证归属统一 + RBAC 细粒度授权"（NopCredential 归属字段 scope/ownerId 落库 + `ICredentialProvider` 消费侧归属/授权校验 + BizModel 按归属过滤 CRUD）；A1-audit 加归属绕过探查；Stages 表 8/11 行、Stage details 8/9-11、mermaid I11 节点、Cross-cutting（W11-impl 归属字段 ORM 变更入 Protected Area 清单）、W1/W3 out-of-scope 措辞同步。
  - `ai-dev/design/nop-credential/00-vision.md`（§三/§四）、`01-architecture-baseline.md`（usageScope 行 + §2 需求来源）、`02-phase2-design.md`（占位文档标题与规划范围四主题 + 范围裁决注记）同步。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` → 3 errors 全 pre-existing（nop-ai/nop-code invariant-loop + skills），本次改动零新增。
- **Doc-sync**：roadmap + 三份设计文档已同步；`docs-for-ai/` 无影响。

### CFG_AUTH_MFA_STORE_TYPE 默认值同步为 db（worktree：feat-credential-mfa-phase2）

- **改动**：`NopAuthConfigs.CFG_AUTH_MFA_STORE_TYPE` varRef 默认值 `local` → `db` + @Description 同步；`TestMfaConfigAndErrors.mfaConfigDefaultsAreDocumentedValues` 断言同步为 `db`。
- **背景**：W8（2026-08-13）裁定"缺省不使用 Redis、默认 store-type=db"已落地（MfaStoreProvider 字段默认 db + beans.xml `@cfg:nop.auth.mfa.store-type|db` + `TestMfaStoreWiringDb.testDefaultStoreTypeIsDb` 守门），但 `NopAuthConfigs` 的 varRef 默认值残留 `local`——运行时虽被 @cfg|db 覆盖，varRef 层存在语义漂移，本次消除。
- **验证**：`./mvnw test -pl nop-auth/nop-auth-service -Dtest='TestMfaConfigAndErrors,TestMfaStoreWiringDb,TestMfaStoreProvider'` → 4+3+7 全绿；`TestMfaUserSelfService,TestScanLoginMfa,TestMfaLoginE2E` → 15+4+10 全绿零回归。
- **Doc-sync**：无 docs-for-ai 影响（W8 已同步设计文档 §3.7）。
- **Commit**：`d8c699186`

### credential-mfa roadmap 二期区块独立审查循环（3 轮达成共识）

- **对象**：`ai-dev/backlog/nop-credential-mfa-roadmap.md` 二期区块（W9-design ~ W16-impl + A1-A3 + ★★ Milestone）三轮独立 fresh-session 审查（每轮不同子 agent）：
  - **R1**（round 1）：Verdict Ready，0 Blocking，8 Advisory——mermaid 缺 W7-successor 节点/边、"MfaType 枚举"措辞失真（live 为 VARCHAR 列 + `NopAuthConstants.MFA_TYPE_*` 字符串常量）、provisioning URI 二维码来源 note 缺失、W9/W12-design 四主题合一粒度风险、一期 milestone 条件（W1-W7 vs W1-W8+W7S）不一致、Cross-cutting 二期行缺 W13/W15/W16、W12-design 依赖缺 W8、Rules 交错规则歧义。8 条全部修复。
  - **R2**（round 2）：A7 处置不完整（mermaid 缺 W8 节点/边 + Stages 13 行 Deps 未改 "W4-W8 done"）+ 2 Minor（★ 行 Deps、Stage 13 MfaType 措辞）→ 4 处手工补齐。
  - **R3**（round 3 终审）：4 处修复实读 PASS，六处依赖枚举（Work Items/Stages/Stage details/mermaid/Cross-cutting/Rules）全量对齐，mermaid 25 节点完整无悬空；Verdict **Ready**（1 条 Minor 观察项 = M 里程碑节点缺 W8/W7S 入边，已顺手补齐）。达成共识，区块可执行。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` → 4 errors 全 pre-existing（invariant-loop/skills），credential-mfa 零新增。
- **Doc-sync**：roadmap 二期区块终态（含修复 1-4 + M 入边）；两份占位设计文档不变。


### credential-mfa roadmap 新增二期工作项（设计 → 实现交错 + 审计穿插）

- **改动**：`ai-dev/backlog/nop-credential-mfa-roadmap.md` 追加二期工作项（唯一动态状态块内）：
  - **凭证库二期组**：W9-design（OAuth/KMS/RBAC 三主题设计文档，产出 `ai-dev/design/nop-credential/02-phase2-design.md`）→ W9-impl（OAuth 流程引擎，复用 `nop-auth-sso` `OAuthLoginServiceImpl`）→ W10-impl（外部 KMS/HSM SPI）→ W11-impl（RBAC 授权 + 租户隔离）→ A1-audit（安全审计）
  - **MFA 二期组**：W12-design（操作级 MFA/角色级强制策略/因子扩展/可信设备四主题设计文档，产出 `ai-dev/design/nop-auth/02-mfa-phase2-design.md`）→ W12-impl（操作级 MFA）→ W13-impl（角色级策略引擎）→ W14-impl（WebAuthn/FIDO2）→ W15-impl（邮件验证码 + 可信设备，复用既有 `IEmailSender`/`TencentEmailSender`）→ A2-audit
  - **迁移二期组**：W16-design → W16-impl（nop-integration/nop-metadata credentialId 深度迁移，参照 W7-successor 先例）→ A3-audit（收口全量验证）
  - ★★ Milestone 安全能力二期落地（派生）
- **同步更新**：Stages 表（8-21 行）+ Stage details（Stage 8-21）+ mermaid 依赖图（二期节点）+ Framework reuse（补 `IEmailSender`/OAuth 客户端）+ Cross-cutting（二期跨模块 API plan-first、审计门禁）+ Rules（二期规则：design 先行、组内顺序/组间交错、审计门禁）。
- **占位文档**：`ai-dev/design/nop-credential/02-phase2-design.md`、`ai-dev/design/nop-auth/02-mfa-phase2-design.md` 创建为占位（内容由 W9-design/W12-design 产出），闭合 doc-link。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` → 4 errors 全部 pre-existing（nop-ai/nop-code invariant-loop + skills），本次改动零新增。
- **Doc-sync**：roadmap + 两份占位设计文档（规划产物）；`docs-for-ai/` 无影响。


### roadmap + mission：nop-plugin 增强落地（两轮审查达成共识）

- 新增 `ai-dev/backlog/nop-plugin-enhancement-roadmap.md`（W1-W7 + milestone：plugin.xdef+API → 定义级+兼容 → 实例级+effect+activator → getService 代理+命令 → coeffect → parent+HMR → SHA256+测试+docs）+ `missions/nop-plugin-enhancement.json`（commands 指向三 plugin 模块，prompts 引 6 篇设计 + 4 个审计 prompt，commitFormat `feat(plugin):`）。
- 遵循本项目惯例（对照 nop-credential-mfa：roadmap 在 ai-dev/backlog/、plansDir 共享 ai-dev/plans、引擎在 ai-dev/tools/mission-driver.sh → MISSION_DRIVER_HOME 外部模板）。mission-check 验证 valid。
- **独立子 agent 审查循环（2 轮）达成共识**：第一轮 0P0+3P1+8P2（启动命令路径错/W1 遗漏 IPluginContext/scope.getService 归属矛盾 + 指针行/讨论陈旧行/unload 守卫/prompts 补 02 03/plan-first 标注/措辞/ClassLoader 精确化），全部修复；第二轮核实 11 项全落实、无新 P0/P1，裁决**可发布**。两条 advisory（discussion L347/委托措辞）已顺手修或内化。
- 启动命令：`./ai-dev/tools/mission-driver.sh run nop-plugin-enhancement`。


### 设计：plugin artifact 加载与类加载（05 文档）

- 用户需求：plugin 直接用底层 Nop 平台全部类；自己的类通过 plugin id 从加载器下载 jar 加载；加载器经 http/https URL 下载到本地 repository + sha 校验；接口化 + 缺省 IHttpClient 实现。
- 新增 `ai-dev/design/nop-plugin/05-artifact-loading-design.md`。
- **重要发现**：现有 `HttpPluginResourceResolver` 已实现大部分（IHttpClient + cacheDir + URL 模板 + tmp/move 原子下载），但 **Javadoc 声称"SHA256校验码保证"而代码未实现校验**——设计补齐此缺口（下载后 move 前校验、fail-fast、.sha256 文件、缓存重验）。
- 类加载模型：PluginClassLoader parent=平台 classloader（现状已如此）；getService 的接口类型 T 须宿主可见。
- 拒绝：远程 URL 直载、无校验下载、接口进 api（保持零依赖）、另起新接口名（增强现有 IPluginResourceResolver 契约）。


### 分析：TIS vs Nop 数据集成能力对比

- 用户要求调研 `~/sources/tis`（TIS v5.1.0，批流一体数据集成平台），与 nop-stream/nop-batch/nop-job/nop-metadata/nop-ai 对比，重点找 Nop 未覆盖功能。
- 新增 `ai-dev/analysis/2026-08/2026-08-14d-tis-vs-nop-data-integration-comparison.md`。
- 双路 explore 盘点两代码库。结论：TIS 未覆盖盲区按优先级——**P0**：①DataX 式连接器插件生态与市场（~45 端类型，Doris/StarRocks/Hive/ClickHouse/Paimon/Hudi/ODPS 全部缺失于 Nop）、②集群部署编排与资源管理（K8s DataX worker、Flink 集群编排、HPA——nop-stream 分布式执行本身已实现，见下修正）、③SQL 驱动数据流建模（Presto 解析 + pt 分区改写 + er_rules.yaml + 流作业源码生成编译）、④批流一体管道概念；**P1**：Pipeline AI Agent、数据集成 MCP 工具集、监控告警渠道闭环、Git 配置版本控制；**P2**：数据预览、表映射/DDL 同步 UI、Transformer 规则 UI、DolphinScheduler 推送、微前端插件 UI 等。
- 用户纠正 + 核实：**nop-stream 分布式执行已实现，非 planned**——`RpcDistributedExecutor`（控制面经 IMessageService 走真实 RPC、coordinator↔task RPC proxy）、`JdbcClusterRegistry`/`JdbcLeaderElector`（DB 注册/选主）、`StreamNodeAutoRegistration`、Stage 42 `remoteDeployMode`（deployTask RPC 远端重建 invokable）、跨 JVM 数据面（`DataPlaneMessageServiceAdapter` + `SysDaoWireCodec`/`PulsarStringWireCodec`）、多 JVM 测试 `TestMultiJvmExactlyOnceRecovery` + 独立进程入口 `JobCoordinatorMain`/`TaskManagerMain`。**修正过时文档 `nop-stream/README.md:7`**（原"DISTRIBUTED 模式规划中"→改为已实现细节 + 仅缺 K8s/YARN 编排/HPA）。
- 用户第二次纠正 + 核实：**nop-batch + nop-job 可做分布式批执行**——nop-job 分布式模式支持分片到多 worker（`shardingIndex`/`shardingTotal`/`partitionRange`/`assigned-partitions`/`bestFit`，DB 驱动 coordinator/worker 竞争消费），每 worker 经 `<batch:Execute>` 在 nop-job task 中跑 nop-batch partition（`PartitionDispatchLoaderProvider`/`AsyncFetchPartitionDispatchLoaderProvider` + `partitionIndexField`）。
- 用户第三次纠正 + 核实：**"集群管理"需分层，Nop 应用层已完备**——nop-sys 提供基于数据库的服务注册发现（`NopSysServiceInstance` 实体 + `SysDaoNamingService`(197 行) + `INamingService`/`AutoRegistration`/`CachingNamingService`，模型含 serviceName/clusterName/tags/serverAddr/port/weight/metaData/isHealthy/isEphemeral，等同 Nacos）、选主（`NopSysClusterLeader` + `ILeaderElector`/`AbstractLeaderElector`）、**集群管理 UI**（nop-sys-web 有 `NopSysServiceInstance`/`NopSysClusterLeader` 的 AMIS main/picker/view 页面）、DB 消息总线（`SysDaoMessageService`(504 行) = nop-stream 跨 JVM 数据面后端）。nop-stream 的 `JdbcClusterRegistry`/`JdbcLeaderElector` 即基于其上。真实差距收敛为：**仅缺"容器编排层"——K8s/YARN 部署编排、HPA 弹性伸缩、Pod 生命周期编排**（Nop=应用自管理集群，TIS=依托 K8s 的容器编排集群，层次不同）。报告四处同步修正（B 矩阵分布式执行/集群管理行 + C-P0-2 重命名"容器编排层" + D 节新增"应用层集群管理"优势 + Conclusion 优先级④）。
- Nop 存量优势：元数据治理（列级血缘/质量/对账）、Delta 可逆定制、自研流引擎（nop-stream 窗口/CEP/2PC）、通用编排 DSL（nop-task）、LLM 生态广度、分布式调度（nop-job）、RBAC。
- 补齐优先级建议：连接器 SPI → 批流统一管道 → SQL 数据流建模 → 分布式编排 → 监控告警 → Pipeline AI Agent。


### 设计修正：plugin 专属 XDef（推翻 beans.xdef 扩展）

- 用户纠正："不在 beans.xdef 上扩展。plugin 应该有自己的 xdef"。
- 推翻此前"plugin: 命名空间写入 beans.xdef 根元素"方案（含 plan Phase 0 Protected Area 前置）。
- **plugin.xdef 方案**：plugin 定义独立 DSL（`*.plugin.xml`），requires/if-property/activator 原生属性 + `<beans>` 子元素（复用 bean 模型）；标准 XDSL Delta/校验管线；**不改 beans.xdef**（零共享 schema 变更，plan 不再是 Protected Area 前置）。
- 修正 00（范围）、01（§7.6 载体决策/决策记录/源码锚点/双轨表/7.5）、03（§1.2/Step1/Step2 Delta/Step6/组装图/映射表）、04（外壳/载体）；discussion 追加第 15 轮纠正。
- **plan 切片更新**：原 Phase 0（beans.xdef Protected Area 前置）取消，plugin.xdef 并入首个 plan（新增 schema 低风险）——总量约 4-5 个 plan。


### discussion：记录 nop-plugin 设计修正全过程

- 用户要求把整个设计修正过程记录到 `ai-dev/discussions/`。
- 新增 `ai-dev/discussions/2026-08/2026-08-14-nop-plugin-design-revision.md`（Status: resolved）：按 14 轮记录（需求提出→设计初稿→独立审查→系列架构质疑→IoC 解耦洞察→三大质疑→多实例恢复→接口对比→双参数→返回值→dsh 示例纠错→全面核查），保留用户原始表述与 10 条纠正汇总，含总结（最终设计/关键决策/待定事项/后续行动）。
- 纠正汇总 10 条：事件总线独立、不暴露容器、plugin/IoC 解耦、多实例不推迟、参数传递、双参数、Disposable 返回值、createInstance 返回实例、register disposer 必须收集、示例可运行。


### 文档核查：nop-plugin 与 dsh 示例全面修正

- 用户要求仔细核查文档中所有 nop plugin 与 dsh 示例的正确性。
- 发现并修正：
  1. **01 接口定义不一致**：`IPluginActivator` 接口定义 `void activate(...)` 与实现示例 `Disposable activate(...)` 矛盾 → 接口改为 `Disposable`（`@FunctionalInterface`，Disposable 为 plugin 框架自定函数式接口保持零依赖）。
  2. **03 createInstance 签名不一致**：agent1/agent2 用 3 参、subTools 用 `parent=agent1Tools`（Java 不支持命名参数）→ 统一位置参数 4 参（parent 传 null / 实例）。
  3. **03 getService 多候选自相矛盾**：agent-tools 多 IToolExecutor 实现但示例单值获取 → `tool.bash` 加 `primary="true"`（beans.xdef 合法属性），示例语义自洽。
  4. **03 reconcile 伪代码过期**（单实例视角）→ 更新为定义级+实例级（与 01 §五 一致）。
  5. **03 dsh plugin 示例修正**：`apply(ctx: Context)` 缺 config 参数、`Service` 类型标注不准、硬编码注册与 5.2 patch config 不联动 → 改为 `apply(ctx, config)` 双参数 + config 驱动注册 + 返回值 disposer 收集（register 的 disposer 必须收集，不收集不注销）。
  6. **03 Step 6/Step 3 描述同步**：apply(ctx, config) 双参数、reconcile 定义级语义。
- 最终验证：01/03 全部 activate 签名为 `Disposable`，04 内联引用全部为 `apply(ctx, config)` 双参数，无残留。


### 接口修正：activate 返回值（Disposable）+ 重复调用语义

- 用户问：apply 两次会怎样？apply 有返回值吗？
- 查证 dsh：① `ctx.plugin()` 每次调用**新 fiber**（= 新实例），apply 两次 = 两实例；`ctx.inject` 依赖变化时 apply **卸载重跑**。② apply 返回 `Effect<T>`（disposer/promise/iterable），**返回值自动注册为 effect**（safeCollect：函数收集为 disposer，逆序回退）。
- 修正 Nop：① `IPluginActivator.activate` 返回 `void` → **`Disposable`（可空）**，返回值自动注册为 effect（便捷模式 `return () -> cleanup`，与 `scope.effect()` 等价）；② **createInstance 同 key 两次抛明确异常**（instanceKey 唯一）；③ 重激活语义明确（deactivate→activate **重跑 activator**；并发重复幂等，in-flight 单飞）。
- 同步 01 §四/7.5、03 Step 6、04 对比（返回值/重复调用行）。


### 接口修正：activate(scope, config) 双参数对齐 apply(ctx, config)

- 用户指出：`apply(ctx, config)` 两参数 vs `activate(scope)` 单参数不对称——config 被折叠进 `scope.getConfig()` 了。
- 修正：`IPluginActivator.activate(scope, config)` **双参数**，config 显式参数传递（与 `apply(ctx, config)` 完全对齐，也符合"参数传递而非隐式读取"精神）。
- `scope.getConfig()` 移除（activator 用参数）；实例配置**权威在 `instance.getConfig()`**（随实例持有、任何态可读，实例级 coeffect 依此评估）。
- 同步 01 §四/7.4/7.5、03 Step 6、02 K 项、04 对比（2.1/总览/结论）。


### 接口对比：nop-plugin vs dsh（04 文档）

- 用户问"文档中详细对比 nop plugin 与 dsh 的接口设计了吗"——此前 02（用法评估）/03（场景对照）缺接口级逐项对比。
- 新增 `ai-dev/design/nop-plugin/04-interface-comparison.md`：8 项逐项对比（apply(ctx)/activator、ctx.\<key\>/getService、ctx.effect/scope.effect、ctx.plugin/createInstance、fiber.config/instance.getConfig、inject/coeffect、createScope/实例隔离、provide/accessor/set/beans.xml 声明式 + 生命周期入口）。
- 核心结论：**五个核心机制同构**（激活入口参数传递、可逆副作用、多实例、依赖激活、失效语义）；**Nop 结构性优势**（强类型服务访问、节点级 Delta、声明式外壳、隐藏容器）；**dsh 优势**（运行时动态注册 provide/accessor、fiber restart/update 瀑布否决）；事件总线为独立基础设施不属 plugin 接口。


### 设计达成共识：第七轮确认审查通过（最终）

- 第七轮 pangu 审查：5 项 P1 中 4 项完全闭合，P1-5 仅剩接口落点矛盾（`getConfig` 在 scope 上但声明"随 instance 持有"）。
- 最终修订：**P1-5 落点**——`getConfig()` 移到 `IPluginInstance`（随实例持有、任何态可读，实例级 coeffect 依此评估），`scope.getConfig()` 保留为激活期便捷视图（activator 用）；**P2-E** 文档不一致（02 签名 4 参、03 方法名 `createInstance/destroyInstance`）。
- **审查循环完成（7 轮），设计正式达成共识，可进入 plan 拆分**。
- plan 拆分输入约定（放行条件）：Phase 0 = beans.xdef 变更（Protected Area）；切片输入必须明确 P2-A（HMR 重建实例的配置快照/父链恢复）、P2-B（重复 key 行为）、P2-C（updateConfig × 实例合并视图刷新）；P2-D（deactivate 父实例语义）在实例生命周期切片裁决。
- 设计定稿：多实例（createInstance/instanceKey/parent）、实例配置域（独立 IConfigProvider）、实例级 coeffect、IPluginActivator 参数传递（apply(ctx)/setup(context) 同构）、plugin/IoC 解耦、effect 自管理。


### 设计修订：第六轮审查（有条件通过）+ 5 项 P1 闭环

- 第六轮 pangu 审查：多实例恢复**方向正确、自洽**（与 IoC 解耦正交；dsh `Runtime.fibers` 证实"一定义多实例"是原生语义；实例配置域实现路径存在 `BeanContainerImpl.setConfigProvider`）。有条件通过，需补 5 个 P1 契约缺口。
- 解决 5 项 P1：(1) `createInstance` 签名补 `parent` 参数；(2) **parent 继承语义**（服务查找沿链回退 / 级联销毁 / 配置层叠，对应 dsh `ctx.extend` 三重机制）；(3) `IPluginInstance` 补 `invokeCommand`（per-instance 命令路由）+ 7.1 默认实例规则（仅实例数=1 时可用，多实例抛异常）；(4) **实例配置域实现路径**（aware 路径实例子容器持独立 `IConfigProvider`，废弃全局 `assignConfigValue`，仅兼容路径保留）；(5) `getConfig` 改合并视图（定义默认 ← 实例覆盖），随 IPluginInstance 持有（DEACTIVATED 仍可读）。
- 下一步：第七轮确认审查达成共识。


### 设计修正：多实例恢复（用户质疑）

- 用户质疑：多实例为什么推迟？"此前不是说这一阶段就要做吗？"且"多实例和全局配置有什么关系？与 ioc 没有任何关系"。
- **承认错误**：推迟理由是 IoC 实现细节（全局配置污染 / BeanDefinition 共享），违背刚澄清的"plugin 与 IoC 解耦"原则。多实例是 plugin 框架接口层面概念（instanceKey / 独立 scope / 独立配置域 / 实例级 coeffect），与 IoC 内部实现无关。
- **恢复多实例**：00（目标第 5 项、移除 non-goals 5/7、成功标准 5）；01（§三 多实例状态模型、7.1-7.3/7.6 恢复 createInstance/instanceKey/getParent/getInstances、§五 coeffect 定义级+实例级、§九 拒绝项更新）；02（E/I 项恢复支持、差距/结论更新）；03（Step 4/7 恢复多 agent + subagent parent）。
- **实例配置域**：`scope.getConfig()` 是 plugin 框架自己的配置数据结构（实例化时传入，不写全局 AppConfig），多实例每实例独立；实例级 coeffect 依此评估。
- 下一步：第六轮审查验证。


### 设计达成共识：第五轮确认审查通过

- 第五轮 pangu 审查：**✅ 设计达成共识，可进入 plan 拆分**。无 P0/P1，5 项前置全部解决且技术可行（交叉实测：beans.xdef 已有 ioc: 先例、dsh fiber.inertia 存在、api 零依赖属实、双路径插入点清晰）。
- 4 篇文档全文交叉核对**一致**（状态模型/接口契约/coeffect/HMR/多实例推迟/resolveKey 不做/beans.xdef 变更），**无虚构机制**，源码锚点全部实测准确，dsh 引用全部验证存在。
- 5 项 P2 不阻塞（plan 阶段消解）：P2-1 dsh traceable proxy 对照措辞（INACTIVE_EFFECT 仅 effect 注册时抛，服务读取非活跃返回 undefined，Nop 抛错强于 dsh）、P2-2 非 aware 插件新状态机表示、P2-3 in-flight 中间态 getState、P2-4 配置双通道关系、P2-5 id 兼容策略。
- **审查循环完成（5 轮）**：第一轮 4P0+8P1 → 第二轮 1P0+5P1 → 用户三大质疑（dsh 用法全集/参数传递/spring batch 依据）→ 第三轮修订 → 第四轮可进实施+5 前置 → 第五轮达成共识。
- 下一步：plan 拆分（含 Phase 0：beans.xdef 变更前置 + P2 消解）。


### 设计修订：第四轮审查通过 + 5 项前置解决

- 第四轮 pangu 审查：**✅ 可进入实施（plan-first 拆分）**，无 P0，第二轮 6 项全部落实，用户三大质疑修订与 dsh 实测一致。附 5 项 P1 前置。
- 解决 5 项前置：
  1. IPluginScope 契约补 `getService/getServices/getConfig` + "注册额外服务"裁决（scope 不提供动态注册，activator 经 getService 取 bean 后外部注册）；
  2. 旧插件兼容：`isStateMachineAware()`（default false）+ 兼容路径（非 aware 走旧 start/stop 语义）；
  3. beans.xdef 变更决策记录（共享 schema Protected Area、与 ioc: 命名空间同模式、plan Phase 0 前置；独立 plugin.meta.xml 被拒绝）；
  4. 异步生命周期串行化（in-flight 单飞，类似 dsh fiber.inertia）；
  5. 激活器配置通道 `scope.getConfig()`（scoped，为未来多实例预留）。
- 顺带修正：02-J provide 映射（scope 不动态注册）、03 Step 4 幂等注释。
- 下一步：第五轮确认审查达成共识。


### 设计修订：第三轮审查 + 用户三大质疑修正

- 用户质疑：(1) ctx.tools 不是 dsh 全部用法；(2) @Inject 注入 scope 方式不对，应参数传递；(3) 需读 `docs/theory/why-springbatch-is-bad.md`。
- explore 全量调研 dsh：注册原语全集（Service 子类/provide/accessor/set 4 种 + 事件 on/emit/waterfall/parallel/serial 5 种）、**apply(ctx, config) 参数传递是主通道**、70+ ctx key、createScope/isolate/intercept。
- 读 why-springbatch-is-bad.md：核心洞察 = context 应**参数传递**而非保存为类成员（SpringBatch 的坏设计，Class Component 类比）；NopBatch `setup(context)` + Provider 模式；Hooks 闭包传参类比。
- 修正：**scope 参数传递**——新增 `IPluginActivator.activate(IPluginScope scope)`（与 Cordis apply(ctx)、NopBatch setup(context) 同构），废弃 @Inject 字段注入；03 Step 6 改为 activator 示例；02 补充 J（注册原语全集）/K（apply(ctx) 参数传递）项。


### 设计修订：第二轮独立审查 + 修正

- 第二轮 pangu 审查：第一轮问题**基本解决**（逐条核对通过，dsh 对照经实测全部属实），新发现 **1 P0 + 5 P1**。
- 修正：01 新增"定义来源与资源解析（双轨）"节（本地 VFS beans.xml + uber jar 并存，id 路由、类加载策略）；7.1 补既有方法态语义（invokeCommand 未激活抛 INACTIVE）+ 新方法 default 定性 + **实例身份模型**（固定实例对象，deactivate 只销毁内部）；§四 补 **scope 注入机制**（@Inject 替换虚构 onActivate）+ close 先于子容器 stop + 幂等；§五 补**配置触发责任方**（实现层订阅，API 层零依赖）+ **失败/同步语义**（不阻塞、失败置回 LOADED 重试、activatePlugin 条件不满足 no-op）。03 同步 Step 6 与 §1.4。
- 下一步：第三轮审查验证。


### 设计修订：第一轮独立审查（pangu）+ 改进

- 用户对设计不满意，要求独立子 agent 反复审查改进直到达成共识。
- 第一轮 pangu 审查：**P0×4**（BeansModel 进 api 破坏零依赖 / effect 观测与"不改 nop-ioc"冲突 / 多实例配置隔离缺失 / 03 用虚构 API `app.mount` + "dsh 裸引用悬空"结论错误）+ P1×8 + P2×7。裁决：方向成立，当前形态不可实施。
- 用户决策：多实例推迟（审查建议）。**关键洞察：plugin 与 IoC 解耦**——IoC 只是内部实现机制，plugin API 零依赖；据此"放宽 nop-ioc 加观测面"的决策撤销（IPluginScope 自管理 effect，不观测 IoC）。
- 修订 4 篇文档：00-vision（核心原则 IoC 解耦、多实例降级、non-goals 增 5/6/7）、01-baseline（API 零依赖、IPluginScope 自管理、coeffect 定义级、getService 单值+集合契约、IPluginContext 收敛 registry+reconcile、移除 resolveKey/getHostContainer）、02（修正 dsh PENDING/watchUserPatches/isolate/shadowing 表述、多实例降级）、03（真实 dsh API `ctx.plugin`、coeffect 载体入 beans.xml 根节点、失效语义持平、单实例化）。
- 下一步：第二轮独立审查验证。


### 示例：coeffect 用法 + agent 场景组装

- 新增 `ai-dev/design/nop-plugin/03-coeffect-and-agent-example.md`。
- **coeffect 详解**：spec 是运行时动态激活条件（依赖 plugin ACTIVATED / 配置 true），由 `IPluginContext.reconcile()` 在 context 变更时迭代评估到不动点（依赖链传播）。与 build 时 `ioc:condition` 区别：可逆、可反复激活/去激活。
- **agent 场景示例**（8 步完整组装）：定义 plugin(beans.xml) → Delta 定制工具集(x:override remove) → load + reconcile 条件激活 → createInstance 多实例(agent-1/agent-2 隔离) → getService 强类型代理 → scope.effect 注册可逆副作用(关文件) → 层级实例化(subagent parent) → destroy 回退 effect(quiescence)。串联了全部核心能力。
- **dsh 对照**（§五）：同一场景用 dsh/Cordis 自身机制实现（Service+inject+apply / cordis.yml+patch / ctx.scope / ctx.effect / fiber.mount+dispose），逐项对照。结论：核心机制对等，Nop 在结构层定制粒度（节点级 Delta vs 配置行级 patch）、服务获取类型安全（强类型代理 vs 弱类型裸引用）、封装性（隐藏子容器）上更优。
- 展示 Nop 优势点：Delta 节点级 + remove（dsh 做不到）、不暴露子容器（全程 getService）、会话结束定义不动只销毁实例。


### 评估：dsh plugin 用法在 nop-plugin 中的支持度

- 新增 `ai-dev/design/nop-plugin/02-dsh-usage-coverage.md`，对照 dsh 对 plugin 的 9 类用法逐一评估。
- 范围界定：只评 plugin 核心职责（生命周期/effect/配置/多实例/服务获取/隔离）；事件总线（独立基础设施）、session/turn/inject（agent 框架层）不属 plugin 评估范围。
- 结论：**无 P0 差距**。插件化/可逆/层叠/接缝/多实例/HMR/隔离均有对应机制。差距仅 shadowing 精确语义（消费侧职责）、realm 共享数据语义（待细化）、加载时动态等待（已由 coeffect 覆盖），均低严重度。
- Nop 优势：结构层节点级 Delta（dsh 配置行级）、结构+运行时双可逆、加载/激活分离、不改 IoC。


### 设计修正：IPluginInstance 不暴露内部子容器

- 用户指出 plugin 不应暴露内部 bean container，完全没必要。
- 移除 `IPluginInstance.getContainer()`——子容器是实现细节，暴露它破坏封装且绕过 `getService` 代理安全（有了容器就能 `getBean(name)` 拿裸 bean，悬空风险回归）。消费者只用 `getService(Class<T>)`（强类型 + 生命周期绑定代理）。


### 设计：IPluginInstance.getService 返回代理，不暴露裸对象

- 用户建议 `getServiceProxy` 就够了，无需暴露裸对象（裸引用有悬空风险：实例 deactivate 后引用指向已释放资源）。
- 合并 `getService` + `getServiceProxy` 为单个 `getService(Class<T>)`：默认返回**生命周期绑定代理**（ACTIVATED 路由到 bean、deactivate/destroy 快速失败、reload 指向新实例）。不暴露裸 bean 引用防悬空；`getContainer()` 保留为低级出口。
- 与 `invokeCommand` 互补：`invokeCommand` 按 name 命令分发，`getService` 按类型直接获取服务。


### 设计完善：nop-plugin 多实例（fiber）机制

- 用户确认 plugin 有多实例需求（一定义派生多实例），要求补充。
- 重写状态模型为**两层**：定义级（`UNLOADED→LOADED`）+ 实例级（`ACTIVATED⇄DEACTIVATED`）。一个 LOADED 定义可 `createInstance` 派生 N 个独立实例。
- 引入 `IPluginInstance`（= Cordis fiber）：独立子容器 + `IPluginScope` + `instanceKey`（tenant/agent/session）+ `parent`（层级实例化 agent→subagent）。
- 接口调整：`IPlugin` 改定义级（load/unload，移除 activate/deactivate）；`IPluginManager` 加 `createInstance`/`destroyInstance`/`getInstances`；`IPluginContext` 按 `(pluginId, instanceKey)` 查实例。
- fiber 章节从"暂不显式建模"改为**显式建模多实例**，对应 Cordis fiber 可多次实例化（论文 §4.1）。四层关系：`IPluginContext` > `IPluginManager` > `IPlugin`(定义) > `IPluginInstance`(实例=fiber)。
- 多实例场景：多租户、多 agent、层级实例化（parent 链）。设计结论加第 7 条，模块边界图加入实例层。


### 设计完善：nop-plugin 补充全局上下文 IPluginContext + fiber 讨论

- 用户指出缺全局上下文（Cordis context type Γ∞）和 fiber 概念。
- 补充 `IPluginContext`（全局上下文）：统一承载所有 plugin 的 effect（`allScopes` 全局汇总）+ coeffect（依赖图 / 共享数据 `resolveKey(realm)` / `reconcile`），是 `IPluginManager` 的全局状态后端。对应 Cordis context type Γ∞（论文 §3.3）。
- fiber 判断：**暂不显式建模 Fiber 对象**——单实例下激活实例（子容器+scope）即隐含一个 fiber；异步生命周期用 `CompletionStage`；多实例（多租户/多 agent）留作未来扩展点（引入 pluginId+实例名+parent）。不实现 Cordis fiber 并发交错演算/Confluence（non-goal）。
- 三层关系：`IPluginContext`（全局）> `IPluginManager`（编排门面）> N × `IPluginScope`（per-plugin effect）。
- 更新 `01-architecture-baseline`：设计结论加第 6 条、接口契约加 `IPluginContext` 表 + fiber 章节、模块边界图加 CTX 节点。


### 设计：nop-plugin 增强设计（吸收 Cordis 思想）

- 新增 `ai-dev/design/nop-plugin/`（README + `00-vision` + `01-architecture-baseline`），已登记到 design README。
- 5 条设计结论：(1) 加载/激活两态分离（`UNLOADED→LOADED→ACTIVATED⇄DEACTIVATED`，加载只产出静态 BeansModel，激活才实例化）；(2) `IPluginScope` 系统化 effect（聚合 destroy+subscription+cancel 为可观测列表，LIFO 回退，quiescence）；(3) reactive coeffect 条件激活（activating/deactivating/neutral，运行时动态）；(4) HMR（beans.xml 变更→loader 依赖失效→reload）；(5) **全程不改 nop-ioc**。
- 状态模型用 Mermaid `stateDiagram-v2`；激活/去激活回退用伪代码；接口契约定义 `IPlugin`（getState/load/activate/deactivate）、`IPluginManager`（activatePlugin/deactivatePlugin/reloadPlugin/reconcileActivations）、`IPluginScope`（effect/effects/close）——只定义契约不展开实现。
- 拒绝：改 IoC 动态 register/unregister、独立新入口、照搬 Cordis 形式化演算/元理论、退回配置行级、coeffect 完整类型系统。
- 状态：草案（plan-first），下一步拆 `ai-dev/plans/` 实施计划。


### 修正：Nop 已有 nop-plugin 模块，非新建

- 用户指出 Nop 现在有 plugin 模块。调研确认 `nop-core-framework/nop-plugin`（api/manager/support）已完整实现路径 B。
- `AbstractPlugin.doStart()`（`:118` `loadFromResource(id, res, BeanContainer.instance())` 父容器=宿主）+ `doStop()`（`:100` `beanContainer.stop()`）印证"plugin=子容器、卸载=stop"。`PluginManagerImpl`（`:45` load: 下载+`PluginClassLoader`+ServiceLoader+start / `:64` unload: stop+closeClassLoader）。
- 已有能力：load/unload、类隔离、远程加载、cancel token（`IPluginCancelToken.appendOnCancel`）、结构层 Delta（beans.xml XDSL）。
- 修正 `2026-08-14c`：从"需新建编排层"改为"已有，需增强"。差距：**effect 系统化**（`appendOnCancel` 手动 vs Cordis `ctx.effect()` 自动）+ **HMR**（`getLastChangeTime` 字段有、自动 reload 无）。


### 分析：Nop Plugin 可行性——不改 IoC，plugin = 子容器

- 用户问：改 IoC？IoC 能解决？还是新入口？新增 `ai-dev/analysis/2026-08/2026-08-14c-nop-plugin-feasibility-analysis.md`。
- 结论：**不改 IoC**。验证完整链路：子容器 `stop()`（BeanContainerImpl:558）→ `singletonScope.close()`（:570）→ BeanScopeImpl:92-99 遍历所有 bean → `remove()`（:77-80）→ `destroyBean()`（:588）→ destroy + subscription cancel。**plugin = 子容器、卸载 = stop() 自动回退全部 bean 副作用**。
- 三路径：A 改 IoC（不推荐：enabledBeans final、风险高、违反可逆计算静态结构哲学）/ B 不改+编排层（推荐：零改核心）/ C 独立入口（不推荐：重复造轮子）。
- 新增量小：`IPluginManager` + `IPluginScope`（编排 + effect 聚合）。实现后双层都可逆：节点级结构 Delta（beans.xml 是完整 XDSL）+ 运行时子容器 stop。


### 补充：可逆计算落实 = loader 抽象（被动模式）+ 张量坐标

- 用户指出：可逆计算的落实可封装为 **loader 抽象（被动模式）**——按**张量坐标**（特征向量）加载合并 delta 得到静态 model，再在 model **外部**应用运行时参数。
- 对比文档 `2026-08-14-...comparison.md` §5.3 图后新增"loader 抽象"段落：`modelPath => XDSL => DslModel`（loader，**被动**——只按坐标组装结构）‖ `DslModel => arguments => scope => Result`（运行时，外部参数独立施加）。对应 `generic-delta-composition.md` 的 `loadComponentModel` + 特征向量 `App = (工作流, 报表, 权限, ...)`。
- 与 dsh 差异：dsh 配置层也是 loader（`composeEntries`/`renderConfigDump`），但加载后 `boot()` 直接 mount 成活对象（立即激活 fiber），没把"静态 model"与"运行时参数"分离成独立两段；可逆计算 loader 明确输出静态 DslModel，运行时是独立第二阶段——这正是"被动模式"精确含义。
- **被动模式第二层含义（用户补充）**：依赖追踪 + 自动缓存失效——Loader 内置基于依赖的缓存，任何 Delta 文件修改后相关缓存自动失效并重算静态 model（`decoding-reversible-computation.md` 智能缓存）。dsh 的 `watchUserPatches` 也有缓存失效，但重组须重新激活整棵插件树（活资源 teardown）；可逆计算的失效只重算静态结构、运行时独立，代价远低。


### 修正（重大）：dsh 双层架构——配置层即标准可逆计算，差异在粒度

- 用户三点纠正：(1) plugin 本身是**粗粒度边界**（revertible effects 以 plugin 为单元整体回退）；(2) 可逆计算引入领域结构坐标系，是**非常细粒度的 delta**；(3) dsh 的 YAML 覆盖配置层与 effect 系统**完全无关**，且**正是可逆计算的标准处理方案**。
- 据此重写对比文档 `2026-08-14-...comparison.md` §5.1/5.2/5.3/Conclusion/metadata：旧判断"dsh 没有分离结构/运行时空间""结构未分离"是**错误的**。dsh 实为**双层独立系统**：
  - **YAML 配置层**（声明式、离线）：`ConfigTree = ∅ ⊕ bundle₁ ⊕ bundle₂ ⊕ patches`，`composeEntries`/`renderConfigDump` 可不运行任何 effect 纯离线合并——这正是 `App = Delta x-extends Generator<DSL>`，与可逆计算同构。
  - **运行时 effect 层**（论文 revertible effects）：`boot()` 在配置合并后才 mount/激活/disposer。
- 真正差异是**粒度谱系与逆元完整性**：plugin 整体（粗，revertible effects 边界）→ 配置行/entry（dsh patch 按 id）→ 领域坐标系节点（可逆计算 XDef，细粒度 delta）。dsh 配置层缺结构化逆元（无 remove/deep-merge），可逆计算有。论文 §3 只形式化了运行时层，未覆盖配置层。
- 同步修正论文分析文档 `2026-08-14b` 的相关表述（第四节空间差异）。


### 修正：等价性处理——非"遗忘 vs 保留"对偶，而是不同丢弃判据

- 用户纠正：可逆计算**并不要求保留所有信息**。报表（report）伴随函子例子明确：Excel 正向→对象→逆向 Excel 会**丢失样式等业务无关信息**，只保证"整体结构信息一致"——这本身就是等价。
- 据此修正两篇分析文档（`2026-08-14b-cordis-paper-analysis.md` §3.3、`2026-08-14-...comparison.md` §四）：把原先的"对偶策略（Cordis 遗忘 vs 可逆计算保留）"改为**同一思路（等价非完全恢复）下的两种丢弃判据**——Cordis 按观测性丢弃（不可观察的），可逆计算按业务相关性丢弃（保留结构、丢样式）。`(data, ext_data)` 仅是需逼近严格等式时的可选手段（补差量使近似等价成等式），非强制保留。
- 新增洞察（写入 Open Questions）：`(data, ext_data)` 是否对应 Cordis 中"为某个 key 绑定以避免被遗忘"——即把信息从"可丢弃"提升为"被保留"的统一机制。


### 分析：Cordis 论文 _A Programming Paradigm for Spatiotemporal Composability_ 精读

- 下载论文 PDF（`github.com/cordiverse/paper`，88 页，Draft 2026-08-13）至 `_tmp/cordis-paper.pdf`，全文提取至 `_tmp/cordis-paper.txt`（28 万字符）。新增分析 `ai-dev/analysis/2026-08/2026-08-14b-cordis-paper-analysis.md`。
- 论文形式化核心：**revertible effects** = effect 类型 `Γ→Γ×(Γ→Γ)`（context 变换 + 显式 inverse）；effects 生活在 **monoid（单群）** 下；可撤销用**左逆（left inverse）** `g∘f`（单向），twisted composition `(f₁,g₁)∘(f₂,g₂)=(f₁∘f₂,g₂∘g₁)`。**关键：用 monoid + 左逆，非群**——与可逆计算 `explanation-of-delta.md` "实际差量空间不是真正群"论断同构。
- **等价性问题（§3.3.2 Observational Equivalence）**：论文明确承认 recovery 是"idealization"，"**physical state cannot be recovered as it stood**"（free 不恢复堆布局、生成式名字不恢复），等式只在**观测等价 ≃**（no observer can distinguish）下成立，靠**遗忘**不可观察部分。这与可逆计算伴随函子等价（`A≃Export(Import(A))`）+ `(data,ext_data)` **保留**溢出，是**对偶策略**（一遗一保）。
- 三层同构：(1) 都 monoid 级非 group；(2) 都承认正向再逆向不完全恢复；(3) 都有逆元（左逆 g vs -F）。真正差异在空间：Cordis 可逆于运行时 context `Γ→Γ`（活状态、过程式），可逆计算可逆于结构空间 XDSL（静态坐标系、声明式、可独立于运行时）。方法论：Cordis=PL 理论（effect/coeffect+演算+元理论），可逆计算=物理代数（差量+坐标系+合并规则）。


### 分析（补充）：DeepSeek Harness 与可逆计算——代数框架与等价性

- 上一条分析文档（`ai-dev/analysis/2026-08/2026-08-14-deepseek-harness-vs-reversible-comparison.md`）经用户反馈后补充两大维度，修正了初版过度二分"dsh 无逆元 / 可逆计算有逆元"的倾向。
- 新增第三节"用代数框架统一审视"：采用 `docs/theory/explanation-of-delta.md` 的群结构四要素（封闭性/结合律/单位元/逆元）横向对比 Git/Docker/dsh/Nop。核心发现：**dsh 的 disposer 是比 Docker Whiteout 更接近群逆元的特异性逆元**（每个 effect 有专属 disposer，而非幂等删除）；四者构成连续光谱（Git→Docker→dsh→Nop），差异是代数性质完善程度，非有无。
- 新增第四节"等价性问题"：据 `docs/theory/faq-about-theory-of-nop.md` 第2题与 `explanation-of-delta.md` 伴随函子章节——正向再逆向只达**等价**（`A ≃ Export(Import(A))`，非完全恢复），需补差量才成等式（`A+dA=F(B+dB)`，对应 `(data,ext_data)` 设计）。**dsh 的 disposer 同样面临此问题**：外部副作用不可逆，故 `defensive-patterns.md` 戒律"Dispose must reach quiescence, not just request it"追求的是静默等价而非精确复原——`register∘dispose ≈ identity`。这是二者被表面差异掩盖的深层共鸣。
- Conclusion 改为"相似多于差异"：真正差异是层次（结构层 vs 运行时层逆元）与坐标系稳定性（静态领域坐标 vs 运行时对象身份），以及结构/运行时空间是否分离。


### 分析：DeepSeek Harness 与可逆计算的思想异同

- 新增分析文档 `ai-dev/analysis/2026-08/2026-08-14-deepseek-harness-vs-reversible-comparison.md`，对比外部项目 `~/ai/deepseek-harness`（dsh / Cordis）与 Nop 可逆计算理论。
- 核心结论：两者共享"组合性/非侵入扩展/层叠覆盖"等工程共鸣，但"可逆"层次根本不同——**dsh = 运行时生命周期可逆（reversible effects，时间维度，注册⇄注销）**；**可逆计算 = 结构层可逆（reversible computation，空间/结构维度，合并⇄分解，差量含逆元）**。
- 三个决定性差异：(1) 可逆计算差量**含逆元**（`-F`、`x:override="remove"`），dsh 无（patch 仅替换/插入整行，无代数减法）；(2) 可逆计算在**标准化结构层（XDSL）**做代数运算，dsh 在**运行时对象层**做生命周期回滚；(3) **可逆计算把结构空间分离出来，运行时空间可独立处理**（`docs/theory/generic-delta-composition.md`：`modelPath => XDSL => DslModel => args => scope => Result`，DSL 动态更新独立于状态空间数据迁移），dsh 未分离这两个空间（结构即被消费，HMR 重组须处理活资源一致性）。
- 可借鉴：Nop 可吸收 dsh 在运行时可逆副作用（disposer / scope unwind / teardown-to-quiescence，见 `docs/defensive-patterns.md`）上的精细化经验，作为结构层差量之外的运行时层补充；Nop IoC 当前偏传统 bean 管理，运行时可逆副作用系统化程度低于 Cordis。
- 链接检查：`node ai-dev/tools/check-doc-links.mjs --strict` 报告 18 个错误，**全部来自预先存在的文件**（`ai-dev/backlog/*-invariant-loop-roadmap.md`、`ai-dev/skills/invariant-loop-audit-prompt.md`），新文档未引入任何断链（不在 Issues 表中）。既有断链不属本任务范围，未改。



### 2026-08-14（mission-driver EXECUTE：D1-2/D2-1 deferred follow-up 批量面板查询）— 已收口


### Phases 2-3 - 批量查询实现与端到端验证（450 tests 全绿）

- **主代码**（6 文件）：
  - `io.nop.datav.biz.DashboardDataResult` / `DashboardPanelDataItem`（nop-datav-dao 新 DTO，镜像 PanelDataResult 先例；success 工厂从 PanelDataResult 转换、failure 工厂携带 errorCode/errorMessage）。
  - `INopDatavDashboardBiz.getDashboardData(id, params, panelIds, context)` 声明（@BizQuery + javadoc 契约）。
  - `NopDatavDashboardBizModel.getDashboardData`：requireEntity（D5 live）→ DashboardFilterResolver 一次求值（§4.6）→ sortOrder 加载面板 + panelIds 子集（越权/不存在整体抛 ERR_DATAV_PANEL_NOT_IN_DASHBOARD，条目跟随 sortOrder，重复去重）→ 上限校验先于任何查询（ERR_DATAV_DASHBOARD_PANEL_LIMIT_EXCEEDED）→ 顺序迭代 PanelDataBinder，仅 NopException 捕获为面板级失败条目，其余按看板级传播。
  - `NopDatavErrors`：+`ERR_DATAV_PANEL_NOT_IN_DASHBOARD`、`ERR_DATAV_DASHBOARD_PANEL_LIMIT_EXCEEDED` + ARG_PANEL_IDS/ARG_PANEL_COUNT/ARG_MAX_PANELS。
  - `NopDatavConfigs`：+`CFG_DATAV_DASHBOARD_QUERY_MAX_PANELS`（`nop.datav.dashboard-query.max-panels`，默认 50）。
  - `nop-datav.action-auth.xml`：+`FNPT:NopDatavDashboard:getDashboardData`（orderNo=10015，roles=admin,user，镜像 getPanelData）。
- **测试**（7 新增）：`TestNopDatavDashboardBatchQuery`（6：批量与逐面板组合语义一致——同参数下 columns/rows/hasDataset 逐一相等 + 条目 sortOrder 排序；筛选变化多面板同步；面板级失败隔离含 D3 形态断言；panelIds 子集/去重/不存在/跨看板显式报错；超限拒绝含子集缩限内放行 + 配置覆盖还原；看板不存在整体报错）；`TestNopDatavDashboardDataBatchE2E`（1 主线 E2E：默认筛选→显式筛选差异→无数据集面板条目→破坏 datasetRef 后错误隔离，全程经 biz 入口真实 PanelDataBinder→SQL，无 mock）。
- **实现细节**：nop-datav-dao 新增类后需先 `./mvnw install -pl nop-datav/nop-datav-dao -DskipTests` 再编 service（AOP exec:java 用 dao 的已安装 jar，否则 NoClassDefFound）。
- 验证：`./mvnw test -pl nop-datav -am -T 1C` **BUILD SUCCESS（450 tests, 0 failures, 0 errors，443 既有 + 7 新增）**；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS；typecheck/lint 未配置。
- Doc-sync：Phase 1 已完成两份 design doc（见上），Phase 3 复核实现与 §四裁定无漂移；无 `docs-for-ai/` 变更要求（nop-datav 模块行为，非平台约定变化）。


### 2026-08-14（mission-driver EXECUTE：D3-2 deferred follow-up 删除生命周期级联）


### Phases 2-4 - 实现与验证（443 tests 全绿）

- **主代码**（5 文件）：
  - `NopDatavDashboardBizModel`：覆写 4 参 `doDeleteEntity`（super 后级联）——ReportTask/AlertRule 置 DISABLED + `unregisterTask`/`unregisterRule` 即时注销、分享全量 `enabled=0`、FilterState/DatasetRef/Tab/Panel/Snapshot 按 dashboardId `deleteByQuery` 物理级联；注入两个 scheduler。
  - `NopDatavScreenBizModel`：同机制级联删除 ScreenWidget + ScreenSnapshot。
  - `NopDatavPanelBizModel`：同机制按 panelId 停用 AlertRule + 即时注销；注入 `NopDatavAlertScheduler`。
  - `NopDatavAlertRuleBizModel` / `NopDatavReportTaskBizModel`：**Gap #3 修复**——覆写 4 参 `doDeleteEntity` 即时注销（标准 delete(id) 路径只调 2 参 deprecated afterEntityChange，3 参覆写不触发）；javadoc 同步修正。
  - `NopDatavDashboardShareBizModel.getSharedDashboard`：新增 `requireDashboardAlive` 防御（新错误码 `ERR_DATAV_SHARE_DASHBOARD_NOT_FOUND`）。
  - `NopDatavErrors`：新增上述错误码。
- **测试**：新增 `TestNopDatavDeleteLifecycle`（9 tests：分享拒绝/全量吊销/调度停用+注销/子表+快照级联/防御独立生效/Gap #3 规则/Gap #3 任务/大屏级联/面板停用/端到端全链路）；`TestNopDatavAuditLog` 增 `testAuditLog_shareManagementMutation` + patterns 补 `NopDatavDashboardShare__*`。
- **配置**：`application.yaml` `audit-mutation-patterns` 追加 `NopDatavDashboardShare__*`。
- **关键实现事实**：测试直调 biz 层 delete 需以 `ormTemplate.runInSession` 包裹（镜像生产请求级 session）——`OrmSessionImpl.delete` 有 `contains(entity)` 检查，无环境 session 时 doDelete 加载的实体已 detached（`updateDirectly` 只查 state.isManaged() 故既有 update 测试不受影响）。`requireEntity` 对缺失实体抛 `UnknownEntityException`（nop.err.dao.unknown-entity），面板 not-found 断言按此观察。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am -T 1C` **443 tests, 0 failures, 0 errors**（433 既有 + 10 新增）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS。
- Doc-sync：Phase 1 两份 design doc（见上）；无 `docs-for-ai/` 变更要求（级联为 nop-datav 模块行为，非平台约定变化）。


### Closure

- **Plan 已 closure**：独立子 agent closure audit（fresh session，task `ses_fff414ecaffelVFQhMgau3m5YA`）对照 live code/tests/config 核查 11 项全 PASS，verdict **CLOSABLE**（含 audit 期间 live 复跑全模块 443/0/0）。Closure Gates 全勾；`check-plan-checklist.mjs <plan> --strict` 退出码 0；`scan-hollow-implementations.mjs --module nop-datav --severity high` 0 findings；`check-doc-links.mjs --strict` 退出码 0；roadmap `Last updated` 行已记录本 plan completion。

---

## mission-driver VERIFY：`-pl nop-datav -am` 全链路转绿（上游 nop-auth 两预存失败修复）


### Phase 1 - 设计裁定与 owner docs 增补

- Plan `ai-dev/plans/nop-datav/2026-08-14-2020-2-batch-panel-data-query-api.md` 启动执行。Phase 1 完成 5 项裁定（D1-D5）并写入 owner docs：
  - `runtime-design.md` 新增 §四「批量面板查询 getDashboardData」（D1 归属 NopDatavDashboardBizModel、D2 panelIds 越权引用整体显式报错 ERR_DATAV_PANEL_NOT_IN_DASHBOARD、D3 两级失败语义 + DashboardDataResult/DashboardPanelDataItem DTO 形态 + 含无数据集面板、D4 max-panels 默认 50 + 顺序执行 + 拒绝方案表新增并行 vs 顺序、D5 live 表 + 发布非前置、§4.6 筛选一次求值语义、§4.7 API 契约、§4.8 错误路径），原 §四/§五/§六 顺延为 §五/§六/§七（既有代码引用锚定 §1-§3，无破坏）。
  - `linkage-design.md` 同步三处过期陈述：§三「不引入批量查询」绝对化契约改写为「批量作为优化层由 getDashboardData 提供」；§七「后端批量 vs 前端逐面板」拒绝方案表改写为「前端逐面板（基础模型）+ 后端批量（优化层）」双采用裁定；§十 Non-Goals 的「批量面板查询 API（优化项）」改写为「批量查询的并行执行/结果缓存」（本体已落地）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（6 个 warning 为无关历史 plan 338 的 pre-existing）。


### Closure - 独立审计与收口

- 独立子 agent closure audit（task_id `ses_fff0c4715ffeM2VPFJ3iZbL7Sq`，fresh session）：8/8 PASS（D1-D5 一致性、接线/Anti-Hollow、权限点、错误码、测试断言与现场重跑、docs 一致性、Deferred 诚实性、无静默跳过），判定 CLOSABLE；发现的 1 处 doc 措辞偏差（§4.8 看板不存在实际抛平台 `nop.err.dao.unknown-entity` 而非 ERR_DATAV_DASHBOARD_NOT_FOUND）已当场修正。
- 收口验证：`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` 退出码 0；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high` 退出码 0（0 findings）；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Plan `2026-08-14-2020-2` 置 `completed`（Completed: 2026-08-14，Closure Evidence 已写入 plan 文件）；roadmap `nop-datav-roadmap.md` Last updated 前插本 plan 完成记录。
- Follow-up（non-blocking）：flux 前端切换批量 API（D1-4/D2-4 对接时）、nop-report 缓存复用调研、并行查询（均 Deferred But Adjudicated optimization candidate）。

---

## mission-driver VERIFY：`-pl nop-datav -am` 全链路转绿（nop-auth-web flux 快照同步）


### Phase 1 - 设计裁定与 owner docs 增补

- Plan `ai-dev/plans/nop-datav/2026-08-14-2020-1-dashboard-screen-delete-cascade-lifecycle.md` 启动执行。Phase 1 完成 5 项裁定（D1-D5）并写入 owner docs：
  - `ai-dev/design/nop-datav/permission-sharing-design.md` 新增「删除生命周期与分享吊销」章节（D1 物理删除 + DashboardSnapshot 级联删除裁定及理由、D2 分享逻辑吊销 enabled=0、D4 widget 无调度关联、D5 挂接机制 = 覆写 4 参 doDeleteEntity 后置执行 + 被拒方案表、getSharedDashboard 看板存活防御 + 新错误码 ERR_DATAV_SHARE_DASHBOARD_NOT_FOUND）。
  - `ai-dev/design/nop-datav/schedule-report-design.md` 新增 §26「删除联动停用与即时注销」（D3 双动作 status=DISABLED + unregister*、事务边界与注销失败回滚裁定、Gap #3 修复结论、ReportDelivery/AlertState 保留裁定）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（6 个 warning 为无关历史 plan 338 的 pre-existing）。


### W6 plan Phase 1 落地：parent 层级实例化

- **Plan**：`ai-dev/plans/2026-08-14-2007-3-parent-hierarchy-hmr.md`（nop-plugin-enhancement W6，Phase 1）
- **做了什么**（四项 Decision 全部落地）：
  - **子容器 parent 链接线**：`PluginInstanceImpl.activateInternal` 改 `new BeanContainerBuilder(classLoader, parentContainer)`（parentContainer = 父实例 ACTIVATED 容器，`resolveParentContainer()` 接线前重查——父被 deactivate（容器置 null）显式抛 `ERR_PLUGIN_PARENT_NOT_ACTIVATED`，不静默降级）；builder 路径下 `setConfigProvider` 与 parent 链共存经测试证实（reviewer 核对字段独立成立）。
  - **父子索引归属**：manager 级全局映射 `PluginManagerImpl.parentToChildren`（instance → children，跨定义可观测）+ 实例级 children 列表（`PluginInstanceImpl.children`，父热应用传播 + P2-D 守卫用）；createInstance(parent) 登记、destroy 移除——`onDestroyed` 回调（manager 注入）保证直接 `instance.destroy()` 也同步清理全局映射；级联 destroy = `destroyInstance` 沿全局映射收集闭包（post-order 先子后父）+ `destroy()` 经实例级 children 递归，双路径收敛于 `destroySelf()`。
  - **配置层叠**：`newMergedView()` = 父合并视图 + 定义默认 + 子实例配置（子覆盖父）；**父热应用传播钉死**：`onDefinitionConfigChanged` 重算自身后经 children 递归调用子/孙同名重算（子重算合并视图 + 触发各自 provider 变更）——测试断言子/孙 bean 属性真实重绑定（`getMode()` 往返）。
  - **环防护 + 检查顺序**：`VfsPluginDefinition.checkParentChain`（checkLoaded → 重复 key → 父状态 → 门控 → 创建）：父非 ACTIVATED/非本框架实现 → `ERR_PLUGIN_PARENT_NOT_ACTIVATED`（带新实例 key）；沿 `getParent()` 链 (pluginId, instanceKey) 对去重——父链含自身对或重复对（陈旧同 key 实例/环引用）→ `ERR_PLUGIN_PARENT_CHAIN_CYCLE`。
  - **顶层实例宿主回退不扩展**：顶层 parentContainer 保持 null（01 §三(a) 注解修订落 Phase 2）。
  - **P2-D 显式 deactivate 守卫**（本 Phase 同步落地）：`deactivate()` 检查 ACTIVATED 子实例 → 抛 `ERR_PLUGIN_ACTIVE_CHILDREN_EXIST`（带子 key 参数）；destroy 仍级联（不经守卫）。
  - **顺带修复 pre-existing latent bug**：`InstanceConfigProvider.notifyListeners` 用 `Map.of(varName, oldValue)`——oldValue 为 null（新键出现）时 NPE 被吞成 listener-fail 日志、bean 不重绑定（W5 未暴露：键均预先存在）；改 `Collections.singletonMap`（允许 null 值）。W6 配置层叠传播（父配置新键首次进子视图）必触发。
- **测试**：新夹具 `parent-tools.plugin.xml`（ITool 双候选 + ConfigReaderBean）/ `child-tools.plugin.xml`（仅 ConfigReaderBean，无 ITool——回退可证）；`TestParentHierarchy` 7 用例：跨定义服务沿链回退（两级链：孙→子→父，getServices 同款）、配置层叠（子覆盖父 + 父独有键继承 + 子 bean 读取）、父热应用传播（updateConfig → 子/孙合并视图 + bean 重绑定往返）、级联 destroy（父→子→孙跨定义 registry 全清）、DEACTIVATED 父拒绝（断言错误参数）、P2-D 显式守卫（先子后父放行）、环防护（链上重复 (pluginId, instanceKey) 对 → 显式失败）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（manager 56/56，+7 新用例）退出码 0；`./mvnw compile -pl :nop-plugin-manager -am -T 1C` 通过。
- **Doc-sync**：No owner-doc update required（01 §三(a)/§六 修订落 Phase 2 执行项）；本日志已更新。


### W5 plan closure：独立 closure audit READY_TO_CLOSE + roadmap W5 done

- **Plan**：`ai-dev/plans/2026-08-14-2007-2-coeffect-reconcile.md`（nop-plugin-enhancement W5）
- **独立 closure audit**（fresh session `ses_fff570522ffeVf7ytUKewvMPkH`，research-only）：四个 Phase 全部 Exit Criteria + 15 项 Closure Gates 逐条 PASS，0 Blocker / 0 Major，结论 **READY_TO_CLOSE**；Anti-Hollow 调用链追踪连通（loadPlugin/配置变更/updateConfig → reconcile → 实例 activate/deactivate；createInstance 门控 null 路径）；Deferred 分类诚实性确认（watch-only residual ×2 + 真实优化项 follow-up ×2，无 in-scope live defect 降级）。
- **收口**：Closure 段落完整 evidence 落档（含 audit session id）；`check-plan-checklist.mjs --strict` 退出码 0；Plan Status: completed；roadmap W5 `planned` → `done`。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（api 6/6、manager 49/49、support 10/10）退出码 0；`./mvnw clean install -DskipTests -pl ... -am -T 1C` PASS；下游 `:nop-quarkus-demo -am` compile PASS；`checkstyle:check -Pqa` exit 0（W5 零新增违规；基线 155 条 W1-W4 存量债务经 git-stash 逐字比对一致）；api 零依赖 grep 0 命中；`scan-hollow-implementations.mjs --severity high` 0 findings exit 0；`check-doc-links.mjs --strict` 0 errors exit 0（顺带修复 4 处 pre-existing 断链：invariant-loop 家族文档的 `nop-chaos-flux/docs/backlog/...` 外部引用加前缀、`../skills/nop-code/audit-prompt.md`、`../tools/rules/` 相对路径修正）。
- **Commit**：实现产物未提交（交由 mission-driver 统一 commit，与 W3/W4 惯例一致）。


### W5 plan Phase 4 落地：测试补全（fixtures + TestCoeffectReconcile 全绿）

- **Plan**：`ai-dev/plans/2026-08-14-2007-2-coeffect-reconcile.md`（nop-plugin-enhancement W5，Phase 4）
- **做了什么**：
  - **夹具清单**（requires 一律按 @name）：新增 `model-provider.plugin.xml`（无 activator、空 beans——激活必须成功）、`cycle-a/cycle-b.plugin.xml`（A↔B 互依赖）、`gate-closed.plugin.xml`（requires="missing-dep"）；`FailActivator` 改造含 `attemptCount` 激活尝试计数器（暂停可观测性断言依赖）。
  - **TestCoeffectReconcile**（11 用例）：依赖链级联（2 定义 + 3 实例拓扑：父 destroy/create 经自动 reconcile 级联子实例 deactivate/activate；显式 reconcile 幂等）、实例级 if-property 隔离（实例配置 false vs 全局回退 true；定义级 updateConfig 驱动 activate/deactivate 往返）、createInstance 门控（gate-closed → null；start 门控关闭不创建；满足路径回归）、重复 key 先于门控（先开后关边界）、静态环检测（A↔B → reconcile 终止 + unresolved 报告 + 环成员 deactivate）、失败阈值暂停（5 次连续失败 → 暂停，reconcile 不再尝试（计数不变），显式 activate() 恢复（计数 +1））、自动触发三链路（可控 provider 订阅触发 / 生命周期 create/destroy / updateConfig）、端到端（03 §1.4 场景：loadPlugin → createInstance → 全局配置变化 → 自动 deactivate → 恢复 → 自动 activate）、定义级宽松比较（Boolean.TRUE vs "true"）。
  - **TestCoeffectConfigHelper**（4 用例）：宽松比较器逐类验证（Boolean/Number/String 等价、null 不匹配）。
  - **jar 轨跳过可观测**：`TestPluginManager.testReconcileSkipsJarTrack`（jar 插件在册 → reconcile 正常终止、unresolved 为空）。
  - **关键实现修正**：实例级求值数据源 = **实时合并视图**（`PluginInstanceImpl.getCoeffectConfigView()`，定义默认 + 实例配置实时合并）——`getConfig()` 缓存快照语义保持（P2-C DEACTIVATED 缓存不破），否则 updateConfig 往返无法驱动重激活（快照在 DEACTIVATED 期间不刷新，永久卡旧值）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（api 6/6、manager 48/48、support 10/10）退出码 0；`./mvnw clean install -DskipTests -pl ... -am -T 1C` PASS；下游 `:nop-quarkus-demo -am` compile PASS；checkstyle `-Pqa` exit 0（155 条违规与基线逐字一致——0 新增，W1-W4 遗留存量债务）；api 零依赖 grep 0 命中；scan-hollow 0 findings。
- **Doc-sync**：No owner-doc update required；本日志已更新。


### W5 plan Phase 3 落地：createInstance 门控 + 自动触发接线 + 设计注解

- **Plan**：`ai-dev/plans/2026-08-14-2007-2-coeffect-reconcile.md`（nop-plugin-enhancement W5，Phase 3）
- **做了什么**：
  - **createInstance 门控**（位置钉死：checkLoaded → 重复 key 抛 ERR_PLUGIN_INSTANCE_EXISTS → 门控返回 null → 创建）：定义级 spec 不满足 → no-op null（javadoc 同步）；满足 → 既有行为（创建 + 立即激活）。门控经 Phase 2 注入的 coeffectEvaluator 回引。
  - **start() 兼容路径**：门控关闭不创建实例并记录日志（与 createInstance null 语义一致，不抛异常；行为记入 javadoc）。
  - **自动触发接线（触发点全集钉死）**：(a) load 时订阅定义级 if-property 键（`setGlobalConfigProvider` setter，名称区别于 `setPluginConfigProvider`；缺省 AppConfig provider；SimpleConfigProvider subscribeChange 返回 null → 不存 null 进 map（CHM 禁 null 值的运行时修复））、unload 时取消订阅；(b) loadPlugin/unloadPlugin/createInstance/destroyInstance 成功路径后自动 reconcile；(c) 定义级 updateConfig 经 onConfigChanged 回调（load 时注入 `this::reconcile`）自动 reconcile；(d) 显式 reconcile/reconcileInstances；直接实例 activate/deactivate/destroy 不触发（"全集"诚实性，javadoc 注明）。
  - **评估器注入空安全**：`VfsPluginDefinition.isDefinitionGateSatisfied()` 未注入时保守放行 + 警告日志（manager 路径总是注入）。
  - **W3/W4 测试适配（显式执行项）**：`TestPluginInstanceLifecycle` 新增 model-provider 夹具 + setUp 开门控（requires 腿 = model-provider 加载 + ACTIVATED 实例；if-property 腿 = 全局 agent.tools.enabled=true，捕获原值）+ `@AfterEach` 统一清理（destroy 全部实例 + 恢复全局键原值）；`testUpdateConfigHotApplyAndDeactivatedCaching` 适配（P2-C 缓存契约可观测窗口 = 条件不满足时：显式 deactivate → 关条件 → updateConfig → 断言缓存 → 恢复条件 → 显式 reconcileInstances → 断言新值生效）；`TestPluginManager.testVfsTrackAwareDefinitionState` 内联开门控 + 全局键 finally 恢复（断言语义保留：ERR_PLUGIN_ACTIVATOR_NOT_FOUND）。
  - **设计注解**（owner doc 同步）：03 §1.4（reconcile 不自动创建、依赖链经显式 createInstance + 级联评估）+ 01 §五（实例激活状态受定义级×实例级联合约束；环处理 = 静态依赖图检测 + 防御性迭代上限）——消除与实现之间的语义冲突。
- **验证**：`./mvnw test -pl :nop-plugin-manager` 全绿（33/33）；零依赖 grep 0 命中。
- **Doc-sync**：`ai-dev/design/nop-plugin/03` §1.4 + `01` §五 注解写入；本日志已更新。


### W5 plan Phase 2 落地：IPluginContext 实现 + reconcile 引擎

- **Plan**：`ai-dev/plans/2026-08-14-2007-2-coeffect-reconcile.md`（nop-plugin-enhancement W5，Phase 2）
- **做了什么**：
  - **实现形态 Decision**：`PluginManagerImpl implements IPluginContext`（getInstance/allInstances/reconcile 基于 plugins map + 各定义 registry 单一数据源，不建第二份 registry）；`IPluginManager.reconcileInstances()` 新增（委托 reconcile）；`VfsPluginDefinition` javadoc 同步（"IPluginContext（由 PluginManagerImpl 实现）以同一 registry 为数据源"）；`IPluginContext` javadoc 同步（"静态环检测 + 防御性迭代上限"）。
  - **数据流缝钉死**：load 时注入 `setCoeffectEvaluator(ICoeffectEvaluator)`（包级接口，manager 内私实现：name→pluginIds 索引查 ACTIVATED 实例 + 读全局配置）+ `setOnConfigChanged(this::reconcile)`。
  - **定义级×实例级级联评估 Decision**：定义级 = requires（按 @name 索引，任一 ACTIVE 实例满足）+ 定义级 if-property（读全局配置，宽松比较）；实例级 = 实时合并视图优先 + 全局回退；reconcile 迭代收敛（maxIter = 定义数 + 1 防御上限）；**不自动创建实例**（03 §1.4 与 01 §五冲突按裁决收敛，注解落 Phase 3）。
  - **环处理 Decision**：静态依赖图 DFS 三色检测 → 环成员强制门控关闭（实例级联 deactivate）+ `getUnresolvedPluginIds()`（具体类方法，不进接口）+ 日志。
  - **重入防护**：reconcileLock + in-flight 标记 + dirty 标记（重入置 dirty，本轮后补跑一轮，不丢通知）。
  - **失败阈值**：`PluginInstanceImpl.AUTO_ACTIVATION_FAILURE_THRESHOLD = 5`——连续激活失败 ≥ 阈值暂停自动激活（`isAutoActivationPaused()`），仅显式 activate() 成功恢复；reconcile 对 activate/deactivate 同步等待（FutureHelper.syncGet），失败捕获记录；jar 轨跳过（instanceof VfsPluginDefinition）。
  - **unload 失败路径保护**：unloadPlugin 成功后才清 name 索引/订阅（ERR_PLUGIN_INSTANCES_NOT_EMPTY 不清）；**CHM null 值修复**：SimpleConfigProvider.subscribeChange 返回 null 时跳过存 map。
  - **父链扩展点**：包级 ICoeffectEvaluator 接口承载 W6 父链健康逻辑（不写死进引擎）。
- **验证**：`./mvnw compile -pl :nop-plugin-manager -am -T 1C` 通过（Phase 2 期间）。
- **Doc-sync**：No owner-doc update required（设计注解项 Phase 3 统一落）；本日志已更新。


### W5 plan Phase 1 落地：coeffect spec 解析（VfsPluginDefinition）

- **Plan**：`ai-dev/plans/2026-08-14-2007-2-coeffect-reconcile.md`（nop-plugin-enhancement W5，Phase 1）
- **做了什么**：
  - **VfsPluginDefinition 类型化字段**：`getRequires()`（csv-set → Set<String>，空集合 = 无依赖，含字符串兜底拆分）、`getIfPropertyName()/getIfPropertyExpected()`（if-property 按 `|` 拆分；无 `|` 时缺省 expected 视为 `Boolean.TRUE`）；构造即校验（load 解析期）。
  - **错误码**：`ERR_PLUGIN_INVALID_COEFFECT_SPEC`（PluginManagerErrors，带 pluginId + specAttr 参数）——propName 为空显式抛异常（No Silent No-Op）。
  - **门控评估器回引接口**：新增包级 `ICoeffectEvaluator`（requires/定义级全局配置/实例级合并视图+全局回退三方法；W6 父链扩展点承载）；`VfsPluginDefinition.isDefinitionGateSatisfied()`（评估器未注入时保守放行 + 警告日志）；`setCoeffectEvaluator`/`setOnConfigChanged` 注入缝。
  - **宽松比较器**：`CoeffectConfigHelper.matches`（Boolean/Number/String 三类等价比较，定义级与实例级两端共用）。
- **测试**：新增 `TestPluginCoeffectSpec`（4 用例：requires+if-property 解析具体值、无 `|` 缺省 true、空值 = 无条件、非法格式抛异常断言错误码/specAttr/pluginId）。
- **验证**：`./mvnw test -pl :nop-plugin-manager -Dtest=TestPluginCoeffectSpec` 4/4 绿；`./mvnw compile -pl :nop-plugin-manager -am -T 1C` 通过。
- **Doc-sync**：No owner-doc update required；本日志已更新。


### W4 plan closure：独立 closure audit READY_TO_CLOSE + roadmap W4 done

- **Plan**：`ai-dev/plans/2026-08-14-2007-1-getservice-proxy-command-routing.md`（nop-plugin-enhancement W4）
- **独立 closure audit**（fresh session `ses_fff806802ffeDTh1CUYAycwhDG`，research-only）：7 大审计域（Phase 1/2/3 Exit Criteria + Anti-Hollow 调用链追踪 + 零依赖 grep + 文本一致性 + Deferred 分类）逐条 PASS，结论 **READY_TO_CLOSE**，0 Blocker/Major；2 Minor 均 closure 事务项（Closure evidence 落档、Deferred 段条件措辞收紧——均已执行）。
- **Anti-Hollow 证据**：调用链端到端连通（manager.loadPlugin → VfsPluginDefinition.createInstance → PluginInstanceImpl.activate → getService 代理包装 → Handler 按调用重解析 → doDeactivate 置空 scope → 代理抛 INACTIVE）；E2E 测试持代理引用跨 deactivate 断言 INACTIVE 来自代理自身；scan-hollow 0 findings。
- **收口**：Closure 段落完整 evidence 落档（含 uber jar 轨裁决）；15/15 Closure Gates 勾选；`check-plan-checklist.mjs --strict` 退出码 0；Plan Status: completed；roadmap W4 `planned` → `done`。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（api 6/6、manager 29/29、support 10/10）；`./mvnw clean install -DskipTests -pl ... -am -T 1C` BUILD SUCCESS；`./mvnw checkstyle:check`（项目规则集 checkstyle.xml）exit 0；下游 `:nop-quarkus-demo -am` compile PASS；api 零依赖 grep 0 命中。
- **Doc-sync**：roadmap W4 done；`docs-for-ai/` 使用文档 W7 统一同步（本 plan 无 docs-for-ai 交付物）。
- **Commit**：实现产物未提交（交由 mission-driver 统一 commit）。


### W4 plan Phase 3 落地：测试补全 + uber jar 轨实例化裁决

- **Plan**：`ai-dev/plans/2026-08-14-2007-1-getservice-proxy-command-routing.md`（nop-plugin-enhancement W4，Phase 3）
- **做了什么**：
  - **测试补全**（`TestPluginInstanceLifecycle` +3 用例）：
    - 唯一实现分支：`IConfigReader`（唯一"无 primary 且单实现"接口）→ getService 返回该实现（代理）+ 集合大小为 1（W3 closure 移交项收口）。
    - 多实例命令隔离：新夹具 `IsolatedCommand`（bean id `nopPluginCommand_isolated`，tag 属性经 `${agent.only-instance:none}` 读实例配置域）+ `agent-instance.plugin.xml` 追加 bean 定义；agent-1/agent-2 配不同值 → 命令结果按实例可区分；定义级 invokeCommand 2 实例下抛 ERR_PLUGIN_MULTIPLE_INSTANCES（复核）。
    - 代理生命周期矩阵：ACTIVATED 调用正常 → deactivate 后同代理抛 INACTIVE → 重新 activate 同代理恢复可用（按调用重新解析）→ getServices 集合代理 deactivate 后逐个 INACTIVE、重激活按 bean id 恢复 → destroy 后抛 INACTIVE。
  - **Decision：uber jar 轨实例化评估（裁决：维持显式失败，见 plan Closure）**——(a) 语义完备性：jar 轨 plugin.json 定义无 plugin.xdef/activator 载体，实例化需新增机制（W4/W7 范围外）；(b) roadmap 范围：W7 无 jar 轨实例化交付物；(c) contract gap 判定：`ERR_PLUGIN_INSTANCE_NOT_SUPPORTED` 显式失败保证 No Silent No-Op，不构成 gap——当前支持基线（VFS 轨实例化）契约完备；成功路径经既有测试（jar 轨 createInstance 抛错）钉死。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（api 6/6、manager 29/29、support 10/10）退出码 0。
- **Doc-sync**：No owner-doc update required（裁决记录于 plan Closure + Deferred But Adjudicated 段不变）；本日志已更新。


### W4 plan Phase 2 落地：IPlugin.invokeCommand §7.1 三态路由（VfsPluginDefinition + AbstractPlugin）

- **Plan**：`ai-dev/plans/2026-08-14-2007-1-getservice-proxy-command-routing.md`（nop-plugin-enhancement W4，Phase 2）
- **做了什么**：
  - **错误码**：`ERR_PLUGIN_MULTIPLE_INSTANCES` 落 `PluginApiErrors`（api 模块——support 不依赖 manager；带 pluginId + instanceKeys 参数；ARG_INSTANCE_KEYS 常量同步进 api）。
  - **VfsPluginDefinition.invokeCommand/Async**：三态路由（0 → INACTIVE / 1 → 委托该实例 / >1 → ERR_PLUGIN_MULTIPLE_INSTANCES 带 instanceKeys 诊断），委托复用 W3 per-instance 分发链（IPluginInstance.invokeCommand），不新起分发；类 javadoc 同步。
  - **AbstractPlugin.invokeCommand/Async aware 路径**：替换 `UnsupportedOperationException("(W4)")` stub 为同规则三态路由（grep 确认无残留 marker）；pluginId 参数与既有 INACTIVE 一致（getPluginDefinitionPath()）；兼容路径行为不变。
- **测试**：`TestPluginInstanceLifecycle` 新增 3 用例（定义级单实例路由端到端（hello:world + 事件断言）、多实例抛错（async+sync 双路径、instanceKeys 含两 key）、1 个 DEACTIVATED 实例委托到实例级 INACTIVE（带实例 key 参数））；`TestAbstractPlugin` 新增 AwarePluginWithInstances（override getInstances()）+ StubInstance（invokeCommand 记录调用、DEACTIVATED 模拟实例级 INACTIVE）+ 3 用例（单实例路由接线验证（stub.invoked 断言）、多实例抛错且不静默路由（invoked 为 false）、DEACTIVATED 单实例委托到实例级 INACTIVE 且 stub 真实被调）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（manager 26/26、support 10/10）。
- **Doc-sync**：No owner-doc update required（§7.1 语义 javadoc 已由 W1 写明，本 phase 使其成为真实行为）；本日志已更新。


### W4 plan Phase 1 落地：getService 生命周期绑定代理

- **Plan**：`ai-dev/plans/2026-08-14-2007-1-getservice-proxy-command-routing.md`（nop-plugin-enhancement W4，Phase 1）
- **做了什么**：
  - **Decision 落定（设计 01 §7.2/§7.4 注解已写入）**：代理实现 = `ReflectionManager.instance().newProxyInstance`（GraalVM 注册惯例）；仅接口类型（`getServices(具体类)` 同规则抛错）；handler 每次调用从实例当前 scope 重新解析目标（不捕获一次性引用，重激活后同代理恢复可用）；scope 为 null（deactivate 窗口）按 INACTIVE 不 NPE；包装时立即解析一次（多候选错误仍在 getService 调用点抛）；集合代理以 bean id 为候选键按 id 重新解析；equals/hashCode/toString 与业务方法同规则；`IPluginScope.getService` 保持真实 bean（激活期句柄，instance 级才是代理边界——javadoc 同步澄清）。
  - **实现**：新增 `ServiceProxy`（impl 包，InvocationHandler：state/scope 检查 → 按调用重解析 → 委托）；`PluginInstanceImpl.getService/getServices` 改为代理包装 + 非接口抛 `ERR_PLUGIN_SERVICE_PROXY_ONLY_INTERFACE`（新错误码，PluginManagerErrors，带 beanType）；`PluginScopeImpl` 增两个 package-private 助手（getServiceBean(beanId) / getServiceBeans(Class) 返回 id→bean 映射）；防御性错误码 `ERR_PLUGIN_SERVICE_CANDIDATE_NOT_FOUND`（集合代理重激活后按 id 解析不到的显式失败）。
  - **W3 调用点迁移**：新增夹具接口 `IConfigReader`（io.nop.plugin.test，ConfigReaderBean 实现）；`TestPluginInstanceLifecycle` 6 处 `getService(ConfigReaderBean.class)` → `getService(IConfigReader.class)`。
  - **测试**：新增 `testGetServiceReturnsLifecycleProxy`（Proxy.isProxyClass 断言、ACTIVATED 行为等价、集合代理逐候选可调用、具体类抛错（getService+getServices 两路径）、deactivate 后代理调用抛 INACTIVE 带 instanceKey 参数）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（api 7/7、manager 23/23、support 7/7）；`./mvnw compile -pl :nop-plugin-manager -am -T 1C` 通过。
- **Doc-sync**：`ai-dev/design/nop-plugin/01-architecture-baseline.md` §7.2（代理机制裁定段落）/§7.4（scope 真实 bean 非代理澄清）+ `IPluginScope.java` javadoc 同步；本日志已更新。


### W4/W5/W6 plan 起草 + 独立审查共识（nop-plugin-enhancement）

- **Plans**（新草，均 `active`）：
  - `ai-dev/plans/2026-08-14-2007-1-getservice-proxy-command-routing.md`（W4：getService 生命周期代理 + invokeCommand §7.1 路由 + AbstractPlugin stub 收口）
  - `ai-dev/plans/2026-08-14-2007-2-coeffect-reconcile.md`（W5：coeffect spec 解析 + IPluginContext/reconcile 级联引擎 + createInstance 门控 + 配置订阅自动触发 + W3/W4 测试适配）
  - `ai-dev/plans/2026-08-14-2007-3-parent-hierarchy-hmr.md`（W6：parent 层级 + reloadPlugin 快照 + 变更检测接线 + P2-A/P2-D 裁决）
- **独立审查循环**（fresh-session 子 agent，每轮含想象性分析 + 事实核对）：
  - W4：R1 fail（AbstractPlugin "(W4)" stub 未覆盖 + Phase 测试归属）→ R2 fail（非接口代理规则破坏 6 处 `getService(ConfigReaderBean.class)` 调用点）→ R3 **Ready**（advisory 并入）。
  - W5：R1 fail（reconcile 语义自相矛盾 + createInstance 门控破坏 W3 测试）→ R2 fail（W3 适配不足 / TestPluginManager start 断言 / 数据流缝）→ R3 fail（实例级求值×合并视图缺全局回退 / requires 身份按 @name / 订阅缝不可测）→ R4 fail（testUpdateConfigHotApplyAndDeactivatedCaching 适配 / TestPluginManager 双门控腿 / 链子定义必须 agent-instance）→ R5 fail（缓存适配末尾触发机制钉死：测试环境 assignConfigValue 不触发订阅 → 显式 reconcileInstances）→ **共识达成**。
  - W6：R1 fail（跨定义 parent 链 × per-plugin reload 快照 → 静默丢实例）→ R2 **Ready**（5 advisory 并入：P2-D 双路径裁定显式守卫/reconcile 级联、快照 parent 记 id 对、父热应用传播钉死、file: 可写资源测试机制、检查顺序）→ R3 确认 Ready。
- **关键裁决**（记录于各 plan Closure/Decision，实施时落地）：实例级 if-property 求值 = 合并视图优先 + 全局回退；requires 依赖身份按 `@name` 索引（重复名任一满足）；reconcile 不自动创建实例（03 §1.4 与 01 §五 冲突以 01 为准 + 双文档落注解）；环处理 = 静态依赖图 DFS + 防御性迭代上限；P2-A 快照归 manager（级联闭包 + (pluginId, instanceKey) 父对 + pending 重建）；P2-D = 显式 deactivate 守卫式 / reconcile 路径先子后父级联；顶层实例容器不扩展宿主回退（01 §三(a) 修订）；W5 需适配 `TestPluginInstanceLifecycle`（model-provider 夹具 + setUp 开门控 + 缓存测试窗口重划）与 `TestPluginManager`（内联开门控 + 全局键恢复原值）。
- **状态同步**：roadmap W4/W5/W6 `todo` → `planned`；plan 文件 `Plan Status: draft` → `active`（各附 Review Consensus 行）。


### W3 plan 执行 + closure：实例级生命周期 + effect + activator（五 Phase 全绿 + 独立 audit READY_TO_CLOSE）

- **Plan**：`ai-dev/plans/2026-08-14-1720-3-instance-lifecycle-effect-activator.md`（nop-plugin-enhancement W3，前置 W2 定义级状态机）
- **做了什么**：
  - **Phase 1 — 实例配置域 + P2-C 裁决**：新增 `InstanceConfigProvider`（委托包装器：读路径 + subscribeChange 委托全局 provider 且本地监听表保证 cleanup 恒非 null——防 SimpleConfigProvider 空订阅陷阱；合并视图 = 定义默认 + 实例配置，实例覆盖定义；assignConfigValue 只写实例视图绝不落全局）；容器接线钉死：BeanContainerBuilder 标准管线（AppBeanContainerLoader 同源，VFS 轨 beans 已解析故走 addBeansModel）→ cast BeanContainerImpl → **setConfigProvider 先于 start()**，全仓零 `buildNewInstance` 使用；`VfsPluginDefinition` 增 definitionConfig/beansModel/activatorName 访问口 + load(config) 存默认配置；**P2-C 裁决（记录于 plan Closure）**：updateConfig 对定义默认**合并更新**——ACTIVATED 热应用（重合并 + provider diff 通知，bean 响应式属性真实重绑）、DEACTIVATED 缓存待下次 activate 应用。
  - **Phase 2 — PluginScopeImpl**：effect/effects/close（LIFO 逆序、单 disposer 抛错记录继续、close 幂等、close 后 effect() 抛 IllegalStateException）+ getService(s)（多候选规则：primary 优先 → 唯一 → 多候选抛 ERR_PLUGIN_MULTIPLE_SERVICE_CANDIDATES 翻译 IoC 错误码；返回真实 bean，W4 代理移交）。
  - **Phase 3 — PluginInstanceImpl + manager 实例管理**：固定实例对象（deactivate 保留实例、destroy 移除）；activate = 子容器 + activator + effect，deactivate = **scope.close() 先于容器 stop**（事件序 return-disposed→effect-disposed→bean-destroyed 测试断言）；per-instance in-flight 单飞（lifecycleLock，并发重复 activate 幂等不重跑）；`IPluginManager` 增 createInstance/destroyInstance（§7.3）；实例 registry 唯一持有者 = VfsPluginDefinition；**W2 移交处置**：定义持有类 start/stop 收敛（start = createInstance(默认 key) / stop = destroyInstance + unload），AbstractPlugin start = load + jar 轨 successor 显式失败（新消息）、stop 收敛为 unload；unload 守卫（有实例抛 ERR_PLUGIN_INSTANCES_NOT_EMPTY 带 keys）；jar 轨 createInstance 边界（ERR_PLUGIN_INSTANCE_NOT_SUPPORTED，successor W4/W7）；**unloadPlugin 修正**：holder 仅成功后从 map 移除（守卫抛错不再破坏 manager 状态）。
  - **Phase 4 — activator 调用链**：定位 activator bean（by-name）→ `activate(scope, 合并视图 config)` 双参数（禁字段注入）→ 返回值非 null 自动 scope.effect(returned)；每次 activate 全新 scope；重激活重跑 activator；激活失败 → rollbackActivation（容器 stop 回滚，不残留半激活实例）+ DEACTIVATED + 错误带 instanceKey 参数（非 NopException 包装 ERR_PLUGIN_ACTIVATION_FAILED 保留 cause）。
  - **Phase 5 — 测试**：新增 `TestPluginInstanceLifecycle` 15 用例（E2E 链 / quiescence+scope 契约 / 多实例隔离 / 生命周期往返 / 重复 key / unload 守卫 / 未加载定义 / jar 轨边界 / 激活失败回退 / 配置域合并视图 / P2-C 热应用+缓存 / 并发 activate 单飞 / 多候选错误 / invokeCommand 路由 / start 默认 key 收敛）+ 测试 fixture（ITool/BashTool/SearchTool/AgentInstanceActivator/AgentInstanceRecorder/RecordingBean/ConfigReaderBean/HelloCommand/FailActivator + agent-instance/multi-candidate/activate-fail 三份 plugin.xml）；更新 TestPluginManager（start 收敛断言：activator-not-found + DEACTIVATED 实例 + destroy 清理）与 TestAbstractPlugin（start jar 轨边界消息 / stop 收敛 unload）。
- **偏差**：容器创建用 `BeanContainerBuilder.addBeansModel`（plan 文"loadFromResource"意图 = 标准 builder 管线；VFS 轨 beans 已随 plugin.xdef 解析，再走资源解析会二次解析失败——同管线、同 castable 结果，接线钉死意图（setConfigProvider 先于 start / 禁 buildNewInstance）完整满足，记录于 plan Closure）；测试全局配置基线用 `AppConfig.assignConfigValue`（测试 setup，非 framework 行为）；`${var:default}` Spring 冒号默认语法（pipe 语法解析失败）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（api 6/6、manager 22/22、support 7/7）；`./mvnw clean install -DskipTests -pl ... -am -T 1C` BUILD SUCCESS；`./mvnw checkstyle:check -Dcheckstyle.config.location=file://<root>/checkstyle.xml -pl ... -am` exit 0；scan-hollow 三模块 0 findings；下游 `:nop-quarkus-demo -am` compile 通过；api 零依赖 grep 0 命中；`check-plan-checklist.mjs --strict` 退出码 0。
- **独立 closure audit**（fresh session `ses_fffe404c6ffecLDICMDmwvMX9G`）结论 **READY_TO_CLOSE**：5 Phase Exit Criteria + Closure Gates 逐条 PASS（含 P2-C 热应用经委托 provider 真实触发变更、deactivate 顺序、unloadPlugin 修正、Anti-Hollow 调用链连通），0 Blocker/Major/Minor；1 条 Minor 观察（getService 唯一实现分支无独立用例，W4 测试矩阵补）。
- **Doc-sync**：roadmap W3 `planned` → `done`；`docs-for-ai/` 无影响（使用文档 W7 统一同步）；plan Closure 段落已落档（P2-C 裁决 + audit evidence）。
- **Commit**：实现产物未提交（交由 mission-driver 统一 commit）。


### W1 plan 执行 + closure：plugin.xdef + API 接口层（三 Phase 全绿）

- **Plan**：`ai-dev/plans/2026-08-14-1720-1-plugin-xdef-and-api-layer.md`（nop-plugin-enhancement W1）
- **做了什么**：
  - **Phase 1 — API 新类型（7 个）**：`nop-plugin-api/src/main/java/io/nop/plugin/api/` 新增 `Disposable`（`@FunctionalInterface` 单方法 `dispose()`）、`PluginState`（UNLOADED/LOADED）、`InstanceState`（ACTIVATED/DEACTIVATED）、`IPluginActivator`（`activate(scope, config)` 双参数，javadoc 钉死"返回值非 null 自动注册为实例 effect"）、`IPluginInstance`（12 方法，§7.2 语义全 javadoc）、`IPluginScope`（effect/effects/close LIFO/getService(s)）、`IPluginContext`（registry + reconcile，不暴露宿主容器）。
  - **Phase 2 — IPlugin default 增强**：6 个 default 方法（`isStateMachineAware`=false、`getState`=LOADED 保守值、`getInstance`=null、`getInstances`=空、`load`/`unload` 显式抛 `UnsupportedOperationException` No-Silent-No-Op）；既有 invokeCommand/updateConfig/start/stop javadoc 补 §7.1 态语义（仅文档，签名未变）；`nop-plugin-api/pom.xml` 加 junit-jupiter test-scope 依赖；新增 `TestPluginDefaultMethods` 6 断言把 default 行为固化为回归测试。
  - **Phase 3 — plugin.xdef**：`nop-plugin-manager/src/main/resources/_vfs/nop/schema/plugin/plugin.xdef`（动态模型模式，无 bean-package/name → 根 DynamicObject；`name="!string"`/`requires="csv-set"`/`if-property="string"`/`activator="bean-name"` 按 03 §1.2 钉死；`<beans xdef:ref="/nop/schema/beans.xdef"/>` 复用 BeansModel 根，beans.xdef 零修改）；新增 `TestPluginXdef`：合法示例经 `DslModelParser` 解析成功（根 DynamicObject 字段可读——DynamicObject 属性名 camelCase：`if-property`→`ifProperty`；`<beans>` 解析为 `io.nop.ioc.model.BeansModel` 含 id/class/primary）+ 未知属性抛 `NopException`（schema 真实被消费）。
  - **零依赖不变式**：grep `io.nop.ioc`/`io.nop.xlang` 0 命中（api 主源码）。
- **偏差**：测试 resource 置于 `nop-plugin-manager/src/test/resources/_vfs/nop/plugin/test/`（VFS 路径，走真实 DslModelParser 管线）；`if-property` 断言用 camelCase 属性名（DynamicObject 约定）；checkstyle 用项目自有规则集 `checkstyle.xml` 验证（root pom 中 plugin 配置被注释，`checkstyle:check` 默认走 Sun 规则集误报，项目规则集 0 violation）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（plugin-api 6/6、manager 2/2）；`./mvnw clean install -DskipTests -pl ... -am -T 1C` BUILD SUCCESS；`./mvnw checkstyle:check -Dcheckstyle.config.location=checkstyle.xml -pl ... -am` BUILD SUCCESS。
- **Doc-sync**：No owner-doc update required（纯新增契约类型 + 新增 schema，正式使用文档在 W7）；本日志已更新。
- **Commit**：待 mission-driver 统一提交。


### mission-driver 首轮 DRAFT_PLANS：nop-plugin-enhancement（W1-W3 三份 plan 起草 + 独立审查共识）

- 起草 3 份 plan（执行顺序 1→2→3，覆盖 roadmap 关键路径前三级）：`ai-dev/plans/2026-08-14-1720-1-plugin-xdef-and-api-layer.md`（W1）、`...-2-definition-lifecycle-dual-track.md`（W2）、`...-3-instance-lifecycle-effect-activator.md`（W3）。
- **W1**（plugin.xdef + API 层）：7 新 API 类型（IPluginInstance/IPluginScope/IPluginActivator/IPluginContext/Disposable/PluginState/InstanceState）+ IPlugin 6 个 default 方法（getState 默认 LOADED 保守值/load/unload 默认 UOE/getInstance(s) 空/isStateMachineAware false）+ plugin.xdef **动态模型模式**（无 bean-package/name → DynamicObject，先例 nop-ai-dsl-orm gpt/orm.xdef）+ `<beans xdef:ref="/nop/schema/beans.xdef"/>`（先例 task.xdef:24；跨文件 ref 只解析目标根节点）；requires=csv-set/if-property=`propName|expectedValue`/activator=bean-name（03 §1.2 钉死）。
- **W2**（定义级生命周期+双轨+兼容）：id 统一 String 入口 + ArtifactCoordinates overload 保留（parse 判别，抛错转 VFS 轨）；uber jar 发现机制纠正为 **plugin.json**（PluginClassLoader.loadPlugin 读 /nop/plugin.json → pluginClassName，非 ServiceLoader）；VFS 轨定义持有类（manager 侧）经真实 DslModelParser 解析；AbstractPlugin 升级 aware（load 不建子容器/INACTIVE 错误码放 nop-plugin-api/兼容路径保留 assignConfigValue 全局写入）；非 aware getState()=LOADED 裁定。
- **W3**（实例级+effect+activator）：容器接线钉死（loadFromResource → 转 BeanContainerImpl → setConfigProvider 再 start；**禁用 buildNewInstance**——provider 不传播陷阱）；实例 provider 为委托包装器（subscribeChange 委托全局，防 SimpleConfigProvider 空订阅）；deactivate 顺序 scope.close 先于容器 stop；P2-C 裁决（updateConfig：DEACTIVATED 缓存/ACTIVATED 热应用+变更通知）；registry 唯一持有者=定义持有类（W5 IPluginContextImpl 复用）；uber jar 轨 createInstance 记显式 successor（W4/W7）。
- **独立子 agent 审查循环**（fresh session，按 plan-reviewer-prompt + 想象性执行模板）：W1 两轮（B1 动态模型/beans ref 先例验证 + B2 Rule25 默认方法测试；advisory 全并入）、W2 三轮（plugin.json 纠正、VFS holder 命名、id 定案、Phase 合并、4 个 Major 一轮修完）、W3 一轮（配置域空洞陷阱 buildNewInstance/subscribeChange、jar 轨边界裁定、registry 单一持有者）。全部达成共识（无 Blocker）→ 三份 plan 均置 `active`，roadmap W1-W3 `todo → planned`。
- 关键事实核验（本次会话内）：`BeanContainerImpl.buildNewInstance` 仓内零调用方且不传播自定义 configProvider（BeanContainerImpl.java:113/74）；`SimpleConfigProvider.subscribeChange` 返回 null；`DslBeanModelParser.parseObject` beanClass 为 null 时回落 DynamicObject；`ArtifactCoordinates.parse` 非法输入抛 IllegalArgumentException。
# 2026-08-14


### W2 plan closure：独立 closure audit PASS + roadmap W2 done

- **Plan**：`ai-dev/plans/2026-08-14-1720-2-definition-lifecycle-dual-track.md`（nop-plugin-enhancement W2，全部三 Phase 完成）
- **做了什么**：
  - 独立 closure audit（fresh session `ses_000297b3cffe5LyPw5HhRLUod4`）结论 **READY_TO_CLOSE**：A-F 六组逐条 PASS（Phase 1/2/3 Exit Criteria + Closure Gates + 契约一致性 + Anti-Hollow 调用链连通），0 Blocker/Major，4 Minor 均 non-blocking：M1 未提交（按 W1 先例交由 mission-driver 统一 commit）；M2 unload 未清 instances map（W2 registry 恒空不可观测，守卫随 W3 落地）；M3 jar 轨 aware load 分支分层覆盖（W7 补端到端）；M4 单冒号 id 路由残余——**已顺手补测试** `testVfsPathWithSingleColonRoutesToVfsTrack`（断言单冒号 id 走 VFS 轨显式失败而非 jar 轨 NPE）。
  - Plan 收口：Closure 段落写入完整 evidence；`check-plan-checklist.mjs --strict` 退出码 0；Plan Status: completed；roadmap W2 `planned` → `done`。
- **验证**：manager 模块 7/7（TestPluginManager 5 + TestPluginXdef 2）、api 6/6、support 6/6 全绿；scan-hollow 三模块 0 findings；checkstyle（项目规则集）exit 0。
- **Commit**：实现产物未提交（交由 mission-driver 统一 commit）。
- **mission-driver 验证 + 提交**：full-green verification——`./mvnw compile -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am` PASS；`./mvnw clean install -DskipTests ...` BUILD SUCCESS；checkstyle 项目规则集（checkstyle.xml）exit 0（默认 Sun 规则集误报属已知 pre-existing）；`./mvnw test ...` 全绿（api 6/6、manager 7/7、support 6/6）。代码 + 文档按批次提交。
- **Doc-sync**：roadmap W2 done；`docs-for-ai/` 无影响（使用文档 W7 统一同步）。


### W2 plan Phase 3：测试补全（兼容回归 + 双轨路由 + 定义级状态）+ 潜在缺陷修复（落地）

- **Plan**：`ai-dev/plans/2026-08-14-1720-2-definition-lifecycle-dual-track.md`（nop-plugin-enhancement W2，Phase 3）
- **做了什么**：
  - `TestPluginManager` 空壳替换为真实测试（manager 模块，4 用例）：兼容路径回归（jar 轨非 aware MockPlugin——`loadPlugin` 调 start（groupId/artifactId/version 断言）、`unloadPlugin` 调 stop、getState 保守值 LOADED、unload 后从 getLoadedPlugins 移除）；start 失败清理（MockPluginFail——stop 被调 + entry 不入 map）；VFS 轨 aware 定义级状态（LOADED/空实例/坐标 null/loadTime 真实/start 显式失败/invokeCommand INACTIVE/重复 load 幂等/unload→UNLOADED + getLoadedPlugins 移除 + getInstance(s) 空）；非法 id（不存在路径 → ERR_PLUGIN_DEFINITION_NOT_FOUND 带 pluginId 参数；不可解析路径（invalid.plugin.xml 测试资源，未知属性校验失败）→ 带 pluginId 参数的 NopException）。
  - **双轨路由测试基建**：测试内建 uber jar（`JarOutputStream` 组装 `nop/plugin.json`（pluginClassName + importPackages 路由 `io.nop.plugin.test` 到宿主 CL）+ 类字节）；jar 内插件类包 `io.nop.plugin.jarplugin`（不 import 路由，真从 jar 加载）；跨 classloader 调用序列记录器 `io.nop.plugin.test.MockPluginRecorder`（静态字段，断言 start/stop 真实被调用）。VFS 轨走真实 `DslModelParser` + manager 侧 `VfsPluginDefinition`（复用 W1 测试资源 agent-tools.plugin.xml）。
  - **偏差（潜在缺陷修复）**：`PluginClassLoader.loadPluginConfig()` 的 `getResource("/nop/plugin.json")` 前导 "/" 在 URLClassLoader 下无法解析 jar/dir 内资源（实测 null）——原 uber jar 发现机制从未被测试覆盖（TestPluginManager 是空壳），属潜伏缺陷；修复 = 回退去掉前导 "/" 的变体再查（2 行，类隔离机制零改动，不属 roadmap"不改 PluginClassLoader 隔离"禁区）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` exit 0——api 6/6、manager 6/6（TestPluginManager 4 + TestPluginXdef 2）、support 6/6；api 零依赖 grep 0 命中。
- **Doc-sync**：No owner-doc update required。


### W2 plan Phase 2：AbstractPlugin 改造（aware/兼容双路径）+ support 测试（落地）

- **Plan**：`ai-dev/plans/2026-08-14-1720-2-definition-lifecycle-dual-track.md`（nop-plugin-enhancement W2，Phase 2）
- **做了什么**：
  - `AbstractPlugin`：`isStateMachineAware()` 覆盖为 true（DefaultPlugin 继承链随之 aware，仓内无调用方）；新增 `load(config)`（经 `DslModelParser` + `NopPluginConstants.PLUGIN_XDEF_PATH` 解析 `getPluginDefinitionPath()`（protected 测试接缝，默认 `PLUGIN_DEFINITION_FILE=/nop/plugin.plugin.xml`）→ 持有 `DynamicObject definition` + `PluginState` → LOADED；定义缺失抛 `ERR_PLUGIN_DEFINITION_NOT_FOUND`（带 pluginId 参数），绝不带 null 定义进 LOADED）、`unload()`（丢弃定义回 UNLOADED）、`getState()`（aware=真实状态 / 非 aware=LOADED 保守值）、`getPluginDefinition()` 观察口；`load()` 全程无 AppBeanContainerLoader 调用（子容器创建留在 doStart 兼容路径，W3 接管实例化）。
  - `start(...)/stop()`：aware → `UnsupportedOperationException`（§7.1 语义 start=load+createInstance / stop=destroyInstance+unload，W3 落地前显式失败）；非 aware → 保留旧语义（assignConfigValue 全局写入 + LifeCycleSupport start/stop）。
  - `invokeCommandAsync`：aware → 0 实例抛 `ERR_PLUGIN_INACTIVE`（覆盖宿主容器回退）；非 aware → 现状回退行为不变；实例数>0 时抛"W4 未实现"显式异常。
  - `nop-plugin-support/pom.xml` 加 junit-jupiter test-scope；新增 `TestAbstractPlugin` 6 用例（LOADED 且 beanContainer==null、定义缺失显式失败、unload→UNLOADED、aware invokeCommand INACTIVE、aware start/stop 显式失败、兼容路径 start/stop 语义 + getState 保守值）+ 测试资源 `_vfs/nop/plugin/test/test-plugin.plugin.xml`。
- **验证**：`./mvnw test -pl :nop-plugin-support -am -T 1C` exit 0（TestAbstractPlugin 6/6）；`grep assignConfigValue` 仅 1 处实现位置（AbstractPlugin 兼容路径）。
- **Doc-sync**：No owner-doc update required。


### W2 plan Phase 1：双轨 id 统一 + PluginManagerImpl 状态机化（落地）

- **Plan**：`ai-dev/plans/2026-08-14-1720-2-definition-lifecycle-dual-track.md`（nop-plugin-enhancement W2，Phase 1）
- **做了什么**：
  - `IPluginManager`：新增统一 String 入口 `loadPlugin(String)/unloadPlugin(String)` + `getInstance(pluginId, instanceKey)/getInstances(pluginId)` 家族入口；`loadPlugin(ArtifactCoordinates)/unloadPlugin(ArtifactCoordinates)` 改为 interface default 委托到 String 入口（旧调用方二进制兼容）；`plugin.xlib` 零改动。
  - `PluginManagerImpl`：id 判别（`ArtifactCoordinates.parse` 成功 → uber jar 轨；IllegalArgumentException → VFS 轨）→ 双轨分发；VFS 轨经 `DslModelParser` + plugin.xdef 解析 `*.plugin.xml` 为 `DynamicObject` 静态定义，由新 `VfsPluginDefinition`（`impl/` 下，实现 IPlugin）持有（定义级状态 UNLOADED→LOADED、实例 registry 骨架 ConcurrentHashMap、start/stop 显式失败（W3 前）、invokeCommand 抛 INACTIVE、坐标三 getter=null、loadTime/lastChangeTime=load 时真实值）；uber jar 轨保留 resolver→PluginClassLoader→plugin.json 链路，aware 路由 `plugin.load(config)` / 非 aware `plugin.start(...)`（失败 stop→rethrow、entry 不入 map、classloader 关闭补齐）；unloadPlugin 按 aware 路由 `unload()`/`stop()`。
  - 新增 `nop-plugin-api/PluginApiErrors`（`ERR_PLUGIN_INACTIVE` + `ERR_PLUGIN_DEFINITION_NOT_FOUND`，带 ARG_PLUGIN_ID——manager 与 support 都要抛，放 api 保证 support 可见）；`NopPluginConstants` 新增 `PLUGIN_DEFINITION_FILE=/nop/plugin.plugin.xml`、`PLUGIN_XDEF_PATH=/nop/schema/plugin/plugin.xdef`；`nop-plugin-api/pom.xml` 加 nop-api-core 依赖（零依赖不变式 = 不 import io.nop.ioc/io.nop.xlang，grep 0 命中，仍成立）。
  - **偏差（结构性调整）**：`plugin.xdef` 从 `nop-plugin-manager/src/main/resources/_vfs/nop/schema/plugin/` **迁移到 `nop-plugin-api/src/main/resources/_vfs/nop/schema/plugin/`**（git mv）——W2 Phase 2 要求 `AbstractPlugin.load()`（support 模块）解析 plugin.xdef 定义，而 support 不依赖 manager；xdef 是框架共享契约面，放 api 后两模块均经依赖传递可见（manager→api、support→api）。W1 的 TestPluginXdef 路径不变（同一 VFS 路径 `/nop/schema/plugin/plugin.xdef` 经 manager→api 传递解析），W1 既有测试 8/8 全绿。清理了 manager target 下旧位置的残留资源（duplicate-vfs-resource 报错）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` exit 0（TestPluginXdef 2/2、TestPluginDefaultMethods 6/6）；api 零依赖 grep 0 命中。
- **Doc-sync**：No owner-doc update required（行为变更记录在 W7 docs 同步；本 plan 无独立 owner doc）。




### 2026-08-14（mission-driver VERIFY：mission `nop-datav` build/test gate）

- Mission VERIFY 四命令全绿：`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS（414 模块）；`./mvnw test -pl nop-datav -am -T 1C` BUILD SUCCESS（含 nop-auth-web 26/0/0、nop-datav-service 443/0/0，全模块 0 failures 0 errors）。typecheck/lint 未配置。
- **修正本日早前条目的误判**（见下方「mission-driver VERIFY plan 2026-08-14-1510-1」偏差段）：`nop-auth-web` `TestFluxPage` 2 例失败（`testFluxEditControls`/`testFluxQueryControls`）**不是 stale-target 瞬态，是确定性失败**。根因：`flux-web/grid_crud.xpl` 于 8/8 `8b370156a`（columns 增加序号列）与 8/9 `f3679cd7f`（footerToolbar 置空，分页由 flux 前端原生处理）两次有意变更后，`nop-auth-web` 金样 `edit-flux.page.json`/`query-flux.page.json`（7/31 `1d79e0704` 最后再生成）从未同步——isolated 重跑 `./mvnw test -pl nop-auth/nop-auth-web -Dtest=TestFluxPage` 稳定复现 2 failures。
- 修复：用测试自带 `regenerateSnapshots()`（临时摘除 `@Disabled` 后恢复）再生成金样；diff 仅为两处 pickerSchema 的模板驱动变更（序号列 + footerToolbar `[]`），`view-flux.page.json` 字节不变（与该用例一直通过一致）。
- 另遇 `nop-stream-runtime` `TestAsyncSnapshotPipeline.testTimeoutAbortDuringInFlightPersistIsNoOpThenCompletes` 在 `-T 1C` 高负载下 1 次时序性 flake（单测重跑 12/0/0，全链路重跑该模块 SUCCESS，无代码变更）。
- Doc-sync: No owner-doc update required（测试金样同步，无平台约定变化；修正早前误判属日志勘误，append-only 不回写旧条目）。

---

## 看板/大屏删除生命周期级联与分享访问正确性（plan `2026-08-14-2020-1`）


### 2026-08-14（mission-driver VERIFY plan `2026-08-14-1510-1`：build/test gate）

Mission `nop-datav` VERIFY 步骤要求 `./mvnw test -pl nop-datav -am -T 1C` 全绿。历史日志多次记录的 2 个上游 nop-auth-service 预存失败本次全部修复，全链路（414 模块）BUILD SUCCESS。

**改动（2 处修复）**：

- `nop-auth-service` 快照 `merged.beans.xml` 补齐 `ext:resolved-depends` 属性——plan 337（commit `7e06450ff`，2026-08-07）让 `BeanContainerDumper` 合并输出新增该属性，但该快照从未同步更新，`TestBeanLoader.testEmbeddedBean` 断言失败。
- `nop-autotest-core` `AutoTestCase.complete()`：teardown 时 `VarCollector.registerInstance(null)` → `registerInstance(new VarCollector())`（恢复 no-op 收集器）。根因：autotest 用例结束后全局 collector 为 null，同 JVM 后续普通 JUnit 测试（`TestChannelScanBindLoginE2E`）触发 `LoginApiBizModel` 登录链路的 `VarCollector.instance().collectVar(...)` 即 NPE，order-dependent flaky。详见 `ai-dev/bugs/2026-08/2026-08-14-autotest-varcollector-null-breaks-plain-junit-tests.md`。

**验证**：`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS（414 模块）；`./mvnw test -pl nop-datav -am -T 1C` BUILD SUCCESS（nop-auth-service 117/0/0、nop-auth-web 12/0/0、nop-datav-service 433/0/0 等，全模块 0 failures 0 errors）。

**偏差**：过程中 `nop-auth-web` `TestFluxPage` 曾出现 2 例一次性失败（footerToolbar/index 列快照差异）——重跑后单模块与全链路均稳定通过，且用测试自带 `regenerateSnapshots()` 再生成内容与 HEAD 逐字节一致，判定为首轮中断运行残留的 stale-target 瞬态，非真实缺陷，无代码/快照变更。

- Commit: 见本次 fix/docs 提交
- Doc-sync: `ai-dev/bugs/2026-08/2026-08-14-autotest-varcollector-null-breaks-plain-junit-tests.md` 新增；No owner-doc update required（autotest 内部行为，`docs-for-ai/02-core-guides/testing.md` 未涉及 VarCollector 全局生命周期细节）

---

## 周期性 stuck-task 恢复扫描（plan `2026-08-14-1510-1`）


### 2026-08-14（mission-driver EXECUTE：D5/D3 deferred follow-up 周期性 stuck-task 恢复）

执行 plan `ai-dev/plans/nop-datav/2026-08-14-1510-1-periodic-stuck-task-recovery-scanner.md`。把「仅进程重启（`@PostConstruct`）时清理中断记录」的恢复机制扩展为「周期扫描 + 时间阈值」，关闭「进程存活 + worker 线程因 JVM Error 死亡 → stuck 直到重启」gap（throwable-sweep `2026-08-14-1452-1` 的 Non-Blocking Follow-up）。**Plan 已 closure：独立子 agent closure audit（task `ses_fffaf7e42ffe4bPSlIij2QvOz2`）CLOSABLE 10/10 PASS；Closure Gates 全勾；`check-plan-checklist.mjs --strict` 退出码 0；roadmap `Last updated` 行已记录本 plan completion。**

**Phase 1 改动（实现落地，测试在 Phase 2）**：

- `NopDatavConfigs`：新增 3 个配置项 `CFG_DATAV_STUCK_SCAN_ENABLED`（`nop.datav.stuck-scan.enabled`，默认 true）/ `CFG_DATAV_STUCK_SCAN_INTERVAL_MINUTES`（interval-minutes，默认 10）/ `CFG_DATAV_STUCK_SCAN_TIMEOUT_MINUTES`（timeout-minutes，默认 60）。
- `NopDatavReportDeliveryRecovery`：新增 `scanStuck(int timeoutMinutes)`（基准字段 `startTime`）——仅标记 status ∈ {PENDING,RUNNING} 且 startTime < now−阈值的记录 FAILED（reason="stuck beyond timeout threshold (Xm)"）；`existsTable` 守卫 + 幂等；既有 `init()`/`recoverInterruptedDeliveries()` 不动（重启路径无阈值不变式 F4）。
- `NopDatavExportTaskRecovery`：新增 `scanStuck(int timeoutMinutes)`（基准字段 `createTime`——导出任务无 startTime；updateTime 会被无关写重置不可靠）；既有 `init()`/`recoverInterruptedTasks()` 不动。
- 新增 `io.nop.datav.service.recovery.NopDatavStuckTaskScanner`（普通 IoC bean）：`@Inject @Nullable IJobScheduler`；`@PostConstruct` 注册固定间隔 job（`TriggerSpec.setRepeatInterval(intervalMinutes * 60_000L)`，jobName=`nop-datav-stuck-task-scan`，beanMethod invoker）；`scanStuck()` 读 timeout 配置委托两类 recovery，吞业务错误返回结果 Map（WARN 日志，FAILED-brick 规避，镜像 executeScheduledReport 约定）；`getScheduler()` + `scanStuckForTest()` + `getScanCountForTest()` 测试辅助。
- `app-service.beans.xml`：注册 `nopDatavStuckTaskScanner`（`ioc:default="true"`，setter 注入两个 recovery bean）。

**验证（Phase 1）**：`./mvnw compile -pl nop-datav/nop-datav-service -am -T 1C` BUILD SUCCESS；`rg` 确认 3 配置项 + 两类 recovery `scanStuck(int)` + beans.xml 注册 + `recoverInterrupted*`/`init` 签名未变。

**Phase 2 改动（测试 + 设计文档定稿）**：

- 新增 `TestNopDatavStuckTaskScan.java`（9 tests，覆盖 ①–⑨）：①交付 startTime 超阈值标 FAILED+reason（`testDeliveryBeyondTimeoutMarkedFailedWithReason`）；②阈值内 RUNNING/PENDING 不动（`testDeliveryWithinTimeoutUntouched`）；③导出 createTime 超阈值标 FAILED（`testExportTaskBeyondTimeoutMarkedFailed`）；④阈值内不动（`testExportTaskWithinTimeoutUntouched`）；⑤幂等+终态不动（`testRescanIdempotentAndTerminalRecordsUntouched`）；⑥scanner 吞 recovery 异常不崩（`testScannerSwallowsRecoveryException`）；⑦existsTable=false 安全跳过（`testScanStuckSkipsWhenTableNotExists`，IJdbcTemplate 动态代理）；⑧周期接线（`testPeriodicWiringFireNowInvokesScanStuck`——`fireNow(JOB_NAME)` 经 beanMethod invoker 派发，scanCount 递增 + 超阈值记录端到端标 FAILED）；⑨慢执行竞态（`testSlowExecutionRaceWorkerSuccessOverwritesScanFailed`——扫描 FAILED 后 worker 写 SUCCEEDED 终态正确）。
- **实现修正（经调试发现）**：ORM 审计在 insert 时强制 `createTimeProp=now`（覆盖调用方 seeded 值），导出测试 seed 需经 update 路径回拨 createTime（与生产 stuck 记录「insert 于过去」等效）；`updaterProp=updatedBy` 由 ORM 自动填充（"sys"），显式 setUpdatedBy("system") 与重启恢复一致但不作断言。
- `schedule-report-design.md`：新增 §25「stuck-task 周期恢复」（最终结论：时间基准字段裁定 startTime/createTime、默认值 60min/10min 理由、与重启恢复正交不变式、Dim14-01 关系、retry 正交性、慢执行竞态良性裁定、被拒替代方案表）；header 更新。
- 顺手修复 `ai-dev/backlog/nop-credential-mfa-roadmap.md` 2 个 pre-existing broken link（future deliverable 路径改为非路径措辞，恢复 doc link checker 退出码 0）。

**测试**：`./mvnw test -pl nop-datav/nop-datav-service -T 1C`：**433 tests, 0 failures, 0 errors**（424 既有 + 9 新增）。

**验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。注：`-pl nop-datav -am` 全链路时上游 nop-auth-service 有 2 个预存失败（TestBeanLoader/TestChannelScanBindLoginE2E，平台级，与本 plan 无关，历史日志多次记录），nop-datav-service 单模块全绿。

---

## Executor catch(Throwable) → catch(Exception) Classification Sweep（plan `2026-08-14-1452-1`）


### 2026-08-14（mission-driver EXECUTE：D5 follow-up executor exception classification sweep）

执行 plan `ai-dev/plans/nop-datav/2026-08-14-1452-1-executor-throwable-exception-classification-sweep.md`。将模块文档化的 error-handling policy（"catch Exception, allow Error to propagate"）从 schedulers 扩展到 6 个剩余 `catch (Throwable)` sites（ReportDeliveryExecutor + NopDatavExportTaskBizModel）。

**改动**：

- **Phase 1 — 6 sites catch(Throwable) → catch(Exception)**：
  - `ReportDeliveryExecutor.java`（4 sites）: `execute` async submit lambda（Site #1, behavioral）、`runDeliveryInSession`（Site #2）、`sendNotificationOutOfSession`（Site #3）、`executeSyncForTest`（Site #4, behavioral）。变量 `t` → `e`，catch body 逻辑不变。
  - `NopDatavExportTaskBizModel.java`（2 sites）: `submitExecution` async submit lambda（Site #5, behavioral）、`executeTask`（Site #6）。变量 `t` → `e`。
  - Category A sites（#1, #4, #5）：行为变更——Error 现传播至线程 UncaughtExceptionHandler 而非被 markFailedSafe 吞掉。Category B sites（#2, #3, #6）：code-consistency 对齐——adapt() 已先 re-throw Error，行为不变。
- **Phase 2 — Focused test + design doc**：
  - `MockEmailSender.java`（test）：新增 `failOnError`（type `Error`）+ `setFailOnError` setter；`sendEmail` 先检查 `failOnError` 再 `failOnSend`；`reset()` 同步清除。
  - `TestNopDatavReportE2E.java`：新增 `testErrorPropagatesFromDeliverySyncPath`——注入 `StackOverflowError`，断言 Error 从 `executeSyncForTest` 传播到测试调用方（验证 Site #4 行为变更）。
  - `TestNopDatavExportE2E.java`：类 javadoc 增 Error 传播说明（Site #5 无 sync 入口，经代码审查确认机械变更一致，report 路径 testErrorPropagatesFromDeliverySyncPath 覆盖相同语义）。
  - `schedule-report-design.md` §9：catch(Exception) policy 从 schedulers 扩展到 executor paths，显式命名 ReportDeliveryExecutor + NopDatavExportTaskBizModel，文档化 async-boundary Error 传播 tradeoff。

**测试**：424 tests pass（0 failures），新增 1 test `testErrorPropagatesFromDeliverySyncPath`。

**验证**：
- `rg "catch \(Throwable" ReportDeliveryExecutor.java NopDatavExportTaskBizModel.java`：0 matches。
- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav/nop-datav-service`：424 tests, 0 failures。

---

## nop-datav P2 Audit Backlog Cleanup（plan `2026-08-14-0950-1`）


### EXEC_PLANS nop-metadata 代码卫生与死码清扫（F14 / F15 / F16 / F18 / F19）

plan `ai-dev/plans/2026-08-14-1448-1-nop-metadata-code-hygiene-and-dead-code-cleanup.md`（mission-driver EXEC_PLANS）执行完成。5 个 API/文档/代码卫生族 follow-up 项一次性收口，零对外行为变更（删死码 + 强化测试 + 补文档精度）。

- **Phase 1 — 死码删除（F14 KeyValueDTO + F16 resolveDataSourceOrThrow）**：
  - `Fix` F14：删除 `nop-metadata-api/.../api/dto/KeyValueDTO.java`（40 行 `@DataBean`，零生产引用）；移除 `TestNopMetaDtoResults` 中 import（原 :10）+ usage（原 :89）。删除前 grep 复核：`rg -n KeyValueDTO nop-metadata/` 仅命中自身定义 + 该测试引用 → 安全。删除后 `rg -n KeyValueDTO nop-metadata/` → exit 1（零残留）。
  - `Fix` F16：删除 `NopMetaQualityRuleBizModel.resolveDataSourceOrThrow`（原 :312-328，private，全文件零调用点——`judgeByRuleId`/`executeQualityRule` 走 `tableRefResolver.resolve(...)`）。`MetaTableReferenceResolver` 同名方法（不同签名，:181 + 调用 :109/:174）保留。
  - `Fix` F16 连带 javadoc：`MetaDataSourceResolver.java:21-23` javadoc 原称"既有三处 `resolveDataSourceOrThrow` 重复（NopMetaTableBizModel profiling / NopMetaQualityRuleBizModel / NopMetaProfilingRuleBizModel）"。**执行复核发现该表述本身已 stale**——grep 证明另两个 BizModel 实际并无此方法（它们委托 `MetaTableReferenceResolver`）。故 javadoc 改为真值表述（"规范实现位于 MetaTableReferenceResolver，供两 BizModel 复用；NopMetaQualityRuleBizModel 重复死码副本已删除"），而非机械"三→两"。
  - `Proof`：`./mvnw compile -pl nop-metadata -am -T 1C` BUILD SUCCESS（删除零引用代码不破坏编译）。
- **Phase 2 — owner-doc IBiz 方法表精度补齐（F15）**：
  - `Fix`：`docs-for-ai/03-modules/nop-metadata.md` :186-187 把 5 个接口的分组标签（"质量规则/检查点/评分"、"契约/剖析"）展开为显式方法名，与 :182-185 同精度。逐个打开 `nop-metadata-dao/.../biz/INopMeta*Biz.java` 核对 live 签名：
    - `INopMetaQualityRuleBiz` — executeQualityRule / executeQualityRulesForDataSource / judgeByRuleId
    - `INopMetaQualityCheckpointBiz` — executeCheckpoint
    - `INopMetaQualityScoreBiz` — computeQualityScore
    - `INopMetaDataContractBiz` — checkContract / checkContractReadOnly
    - `INopMetaProfilingRuleBiz` — executeProfilingRule
  - `Proof`：文档方法名与 live 接口逐一核对一致（无臆造/遗漏/拼写漂移）；`check-doc-links --strict` 本改动文件 0 新增 broken link（17 pre-existing 均不在本文件）。
- **Phase 3 — 空壳测试强化 + 动态实体守卫（F18 + F19）**：
  - `Fix` F18：`TestNopMetaDtoResults.testDtoJsonRoundTripAllTypes` 6 DTO（TestConnectionResultDTO / SyncExternalTablesResultDTO / CollectCatalogResultDTO / CreateSqlTableResultDTO / QueryTableDataResultDTO / QualityRuleResultDTO）由空壳 `assertNotNull(JsonTool.stringify(dto))` × 6 改为真实 round-trip——每 DTO `stringify` → `parseBeanFromText` → `assertEquals` 关键字段（connected/databaseProductName/syncedTableCount/tableCount/metaTableId+tableName/tableType/qualityRuleId+status）。方法名保持（现为 genuine round-trip，命名与行为一致）。
  - `Fix` F19：`TestAllEntitiesHaveBizModels` 硬编码 `list.add(NopMetaXxx.class)` × 39 清单删除，重写为 `@NopTestConfig(localDb=true)` + `@Inject IOrmTemplate`，`discoverEntities()` 从 `orm.getOrmModel().getEntityModels()` 动态发现（沿 `TestNopMetaUniqueKeysEnforced` 先例）。过滤：包前缀 `io.nop.metadata.dao.entity.` + `_gen` 子包排除 + 下划线前缀类排除（防御 `_NopMeta*` 生成基类，审查 Major-2）。sanity 断言 `entities.size() >= KNOWN_ENTITY_BASELINE(39)` 防止动态发现静默返回空集。新增 ORM entity 自动纳入守卫，不可被"忘记追加"绕过。
  - `Proof`：`./mvnw test -pl nop-metadata/nop-metadata-service -Dtest=TestNopMetaDtoResults,TestAllEntitiesHaveBizModels` → 6 tests, 0 failures（动态发现实体数 ≥ 39 且全部有对应 BizModel）。Anti-Hollow 接线证据：sanity `>= 39` 通过即证明读取真实注册表（空集/固定集会触发断言失败）。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` → **1166 tests, 0 failures**。门禁：`check-silent-swallow` exit 1 系 2 处 pre-existing（MetaTableProfiler:223 javadoc 误匹配 + AggregationHelper:561，均不在本计划改动文件内，`git diff --name-only` 确认），本计划 0 新增；`scan-hollow-implementations --severity high` exit 0；`check-plan-checklist --strict` exit 0；`checkstyle:check` 本改动文件未触发新 error（模块整体 pre-existing javadoc 基线非本计划引入）。
- **Non-Goal 守恒**：F17（safeProductName 7× 去重）+ AR-14 归计划 `2026-08-14-1448-2`；F10-F13（ORM 索引/dict 卫生）归计划 `2026-08-14-1448-3`；本计划不重写/迁移任何生产 DTO、不改任何 `@BizQuery`/`@BizMutation` 行为。
- Roadmap：`ai-dev/backlog/nop-metadata-invariant-loop-roadmap.md` API/文档/代码卫生族 F14/F15/F16/F18/F19 标记 ✅ Fixed（F17 保留待计划 2）。



plan `ai-dev/plans/2026-08-14-1133-3-nop-metadata-lineage-manifest-reconciliation-correctness.md`（mission-driver EXEC_PLANS）执行完成。6 个 P2 metadata-correctness 缺陷全收口：lineage 源表抽取不跨 schema 塌缩、不把 CTE 名报为物理表、manifest 图边不因重复关系膨胀、SQL 视图类型推断不因尾分号失败、reconciliation 匹配不受 JVM locale 影响、reconciliation 候选序列化有界。全部为"文档记录为简化/限制，但 false-drop/false-positive 后果未记录"的元数据正确性缺陷——对元数据目录而言，错误的血缘边/图度数/匹配结果是产品级缺陷。

- **Phase 1 — AR-07 跨 schema 同名表塌缩 + AR-08 CTE 名报为物理源表**：
  - `Fix` AR-07：`SqlSourceTableExtractor.extract` 去重 key 由 `seen.add(simple)`（simpleName）改为 `seen.add(full)`（schema-qualified 名）。`dbo.users` 与 `sales.users` 的 fullName 不同 → 两条独立边，不再因 simpleName="users" 相同而塌缩。残留歧义（同一 fullName 经不同别名引用仍为一条边）在 Javadoc 显式记录。
  - `Proof` AR-07：新增 `TestSqlSourceTableExtractor`（lineage 包，纯解析器单测）4 例跨 schema 去重——dbo.users+sales.users 两条边 / 同一 dbo.users 不同别名去重 / 无 schema 同名表去重 / 混合 qualified+unqualified 不塌缩。
  - `Fix` AR-08：新增 `collectCteNames` 预遍历——收集所有 `SqlSelectWithCte.getWithCtes()` → `SqlCteStatement.getName()`（lowercase），匹配 CTE 名的 `SqlSingleTableSource` 从结果排除。CTE 体内部的物理源表仍被正常收集（符合 SQL 作用域语义）。CTE 名遮蔽同名物理表时 CTE 名被排除、物理表引用不补回（保守排除策略）。
  - `Proof` AR-08：9 例 CTE 排除——`WITH t AS (SELECT 1) SELECT * FROM t JOIN real_table` 只含 real_table / CTE 体物理表仍收集 / CTE 名遮蔽同名物理表排除 / 多 CTE 全排除 / CTE 名大小写不敏感排除 / 无 WITH 的 SQL 不受影响（无静默跳过）/ 子查询源表收集 / UNION 两分支收集 / 空 SQL 显式失败。
  - `Proof` 接线验证：现有 `TestNopMetaLineageEdge*` 集成测试无回归（38 tests, 0 failures——dedup key 改动不破坏既有 lineage 边创建链路）。
- **Phase 2 — AR-09 manifest 邻接表去重 + 自环过滤**：
  - `Fix`：`MetaManifestBuilder.addEdge` 追加前检查 `!list.contains(value)`（去重）+ `key.equals(value)` 时不追加（自环过滤）。原 `childMap.computeIfAbsent(...).add(...)` 无条件追加，现改为统一经 `addEdge(childMap, targetUniqueId, ownerUniqueId)`（复用同一去重+自环逻辑）。
  - `Proof`：新增 `TestMetaManifestBuilder`（manifest 包，纯转换逻辑单测，不依赖 IoC）4 例——重复关系（owner→target×2）邻接表无重复邻居 / 自环（owner==target）不出现 / 复合场景（重复+自环+正常）parentMap 与 childMap 双向均正确 / 正常单一关系不被误过滤。接线验证：`TestNopMetaModuleBizModel`（含 generateManifest 端到端）16 tests 全绿。
- **Phase 3 — AR-11 尾分号 + AR-12 Locale.ROOT + AR-13 limit=50**：
  - `Fix` AR-11：`SqlViewFieldTypeInferrer.inferWithinConnection` 包装前 `trimmedSourceSql = sourceSql.trim()` + `while (trimmedSourceSql.endsWith(";")) strip+trim`。修复前 `"SELECT a FROM t;"` 内层子查询 `SELECT * FROM (SELECT a FROM t;) _t LIMIT 0` 被多数 JDBC 驱动拒绝。`while` 循环覆盖 `;;` 双分号边角（防御性——`;;` 在 EQL 解析器层被判为多语句，在类型推断之前被 `SqlSelectFieldExtractor` 拒绝，但 `while` 循环对经其他路径到达推断器的 `;;` 仍做防御性剥离）。
  - `Proof` AR-11：`TestNopMetaTableQueryBizModel` 新增 2 例——带尾分号 sourceSql 类型推断成功（H2 实跑 INTEGER）+ 带前后空白+尾分号 trim+strip 组合成功。
  - `Fix` AR-12：`LocalReconciliationProcessor.levenshteinSimilarity` 的 `a.toLowerCase()` / `b.toLowerCase()` 改为 `toLowerCase(Locale.ROOT)`。tr-TR 下 `"I".toLowerCase()` → `"ı"`（无点 i）不再导致同一数据在不同 JVM locale 下匹配结果不同。
  - `Proof` AR-12：新增 `TestLocalReconciliationProcessorLocale`（reconciliation 包，package-private 静态方法单测）3 例——tr-TR locale 下 I/i 相似度 1.0（locale-insensitive）/ MICROSOFT/microsoft 1.0 / 默认 locale 正常相似度回归。
  - `Decision` AR-13：硬编码默认 `DEFAULT_CANDIDATE_LIMIT = 50`（不引入 ORM/配置变更，避免撞 ORM Protected Area）。`ReconciliationExecutor.execute` 传入 50 而非 null。config-driven limit 移入 Non-Blocking Follow-ups。
  - `Fix` AR-13：`ReconciliationExecutor.execute` 的 `reconcile(..., null)` 改为 `reconcile(..., DEFAULT_CANDIDATE_LIMIT)`。所有 fuzzy 候选经 `LocalReconciliationProcessor.reconcile` 截断为 ≤50，details JSON 候选序列化有界。
  - `Proof` AR-13：新增 `TestReconciliationExecutorLimit`（reconciliation 包，mock IReconciliationProcessor）3 例——limit 参数被真实消费（非 null，=50）/ 大候选池（100）截断为 50 / 小候选池（3）不截断。
- **验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -am -T 1C` → **1166 tests, 0 failures**（nop-metadata-service）+ web 1 test（nop-auth-service 的 1 个 pre-existing VarCollector NPE 与本次变更无关）；4 条不变式门禁——INV-UK/INV-SENSITIVE/INV-LIMIT 零命中，INV-SILENT-SWALLOW 2 个 pre-existing hit（MetaTableProfiler AR-06 Javadoc 误报 + AggregationHelper AR-10 裁定的 NumberFormatException→null，均非本次引入）；`checkstyle:check` sun_checks.xml 全模块 pre-existing 基线（项目 checkstyle 非真实门禁，本次改动遵循既有代码风格）。
- **Non-Goal 守恒**：不重写 SqlSourceTableExtractor 为完整 SQL 语义分析器（CTE 展开/子查询别名解析超范围）；不改变 reconciliation 匹配算法本身（Levenshtein 复杂度已裁定）；config-driven limit（含 ORM Protected Area 风险）移入 Non-Blocking Follow-ups。
- Doc-sync：`docs-for-ai/03-modules/nop-metadata.md` 新增「lineage/manifest/reconciliation 正确性语义」段（AR-07 full 去重 / AR-08 CTE 排除 / AR-09 邻接表去重+自环 / AR-11 尾分号 / AR-12 Locale.ROOT / AR-13 候选有界）。


### EXEC_PLANS nop-metadata profiler 分类正确性 + probeNumeric 错误区分 + 聚合精度无损（AR-05/AR-06/AR-10）

plan `ai-dev/plans/2026-08-14-1133-2-nop-metadata-profiler-precision-silent-wrong-result.md`（mission-driver EXEC_PLANS）执行完成。3 个 P2 "silent-wrong-result" 缺陷全收口：profiler 列类型分类正确（几何/布尔列不再误归数值）、probeNumeric 连接/权限失败不再静默塌缩为"string stats"、cross-DB 内存聚合精度无损（Long>2^53 不丢位、String 数值不被静默跳过）。三者共享同一特征——无错误信号、结果静默错误，是 plan 0707-2 已修 P1 同族的 P2 兄弟面（AR-10 从 0707-2 Non-Blocking Follow-ups 显式 carry-over）。

- **Phase 1 — AR-05 profiler isNumericType 子串误分类**：
  - `Fix`：`MetaTableProfiler.isNumericType` 由 `upper.contains(kw)` substring 匹配改为 `NUMERIC_TYPE_NAMES` 显式类型名集合（`Set.of`）exact-match（INT/INTEGER/TINYINT/SMALLINT/MEDIUMINT/BIGINT/DECIMAL/NUMERIC/NUMBER/DOUBLE/DOUBLE PRECISION/FLOAT/REAL）。从数值集合移除 `BOOLEAN`（布尔列非数值）与 `BIT`（BIT(1)=boolean/BIT(n>1)=bitfield，保守回退 string stats）。`POINT`（几何类型，含子串 `INT`）不再误判数值 → 不再产非法 `SUM(POINT)` SQL 落入 per-column 错误路径无 stats。
  - `Proof`：`TestMetaTableProfilerClassification`（profiling 包，直接访问 package-private）9 例——POINT/BOOLEAN/BIT/POINTER/PRINT 等"含 INT 子串但非数值"类型返回 false；13 个合法数值类型名（含 PG `DOUBLE PRECISION` 空格复合名）仍 true；大小写/空白不敏感；STRING 类型识别不回归。
- **Phase 2 — AR-06 probeNumeric 吞连接/权限失败**：
  - `Fix`：`MetaTableProfiler.probeNumeric` 的 `catch(SQLException)` 内新增 `isInfrastructureFailure` 分类——只对**可识别的基础设施信号**（SQLState `08*` 连接 / `28*` 授权 / `42*` 表/列不存在含 PG `42501` 权限，或消息含 connection/permission/denied/closed/timeout/communication/does not exist）判为 infra 并 WARN（带 col 上下文）；其余（含 `22*` data exception、H2 vendor `90015` wrong data type、null）一律视为良性类型不匹配 DEBUG 静默。仍 return false 保持剖析不中断（签名不变）。
  - `Decision` H2 SQLState 实测裁定：live H2 诊断确认 `SUM(varchar)` 抛 vendor SQLState `90015`（"wrong data type"），非 SQL 标准 `22*`。故放弃"22* 前缀单判 + 保守 WARN on unknown"方案——会对每个良性类型不匹配产 WARN 噪声淹没真实失败。改为"只识别 infra 信号 → WARN，其余 → 良性"，既满足"基础设施失败有显式信号"又不产噪声。另实测 H2 `SUM(BOOLEAN)` 成功（视 TRUE 为 1）。
  - `Proof`：`TestMetaTableProfilerProbeNumeric`（profiling 包）5 例——`isInfrastructureFailure` 单元（22*/90015 良性、08*/28*/42*/42501 infra、消息线索识别、null 无线索良性）；probeNumeric 数值列 true / 文本列类型不匹配 false 不抛 / 代理连接抛 08006 → WARN 触发（ListAppender 断言）+ 仍 false；跨 Phase 依赖（BOOLEAN 移出数值集合走 probeNumeric 守卫，表级剖析不崩溃）。
- **Phase 3 — AR-10 toBigDecimal 精度丢失 + String 数值静默跳过**：
  - `Fix`：`AggregationHelper.toBigDecimal` 的 `Number` 分支内按 `instanceof` 分派——整数类型（Long/Integer/Short/Byte/AtomicLong/AtomicInteger）改用 `BigDecimal.valueOf(n.longValue())` 无损（Long>2^53 经 `doubleValue()` 丢低位）；浮点（Float/Double）保持 `doubleValue()`（小数不截断）；BigInteger 分支不变。新增 `String` 分支：trim 后 `new BigDecimal(s)`（catch NumberFormatException → null），不再直接 return null 被 `SumAcc` 的 `if(n!=null)` 静默跳过。
  - `Proof`：`TestCrossDbInMemoryAggregationProcessor` 新增 7 例——Long.MAX_VALUE(2^63-1>2^53) 经 toBigDecimal 精确（longValueExact 往返）/ 2^60+12345 精确 / AtomicLong 精确；Double 1.5 保留小数不被 longValue 截断；String "123.45" 解析 / 含空白 trim-then-parse / 整数 String；非数值 String/"abc"/空 → null 不抛；BigInteger/BigDecimal 分支不回归；**接线验证** SumAcc.accumulate(Long.MAX_VALUE)×2 = 2^64-2 精确 + SumAcc.accumulate("123.45"+"76.55")=200.00（String 数值不再静默跳过）+ 非数值 String 跳过不污染聚合。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` → **1141 tests, 0 failures**（nop-metadata-service 1140 + web 1）；4 条不变式门禁仍零命中（INV-LIMIT/INV-UK/TestSilentSwallowFormalization 默认 surefire 全绿）；`checkstyle:check` sun_checks.xml 全模块 pre-existing 基线（项目 checkstyle 非真实门禁，本次改动遵循既有代码风格）。
- **Non-Goal 守恒**：不处理同级 `toBigDecimal`（MemoryOrderByComparator/MemoryFilterEvaluator）——同 `doubleValue()` 精度模式但属 ORDER BY/WHERE 比较路径（非 SUM/AVG 聚合），Long>2^53 精度丢失在排序/过滤上下文影响较小，裁定为 optimization candidate 留待后续清扫。
- Doc-sync：`docs-for-ai/03-modules/nop-metadata.md` cross-DB 聚合精度语义段新增 AR-10（整数 longValue 无损 / 浮点 doubleValue / String 数值解析）。


### EXEC_PLANS nop-metadata JDBC/SQL 防御纵深硬化（F5-F9，defense-in-depth）

plan `ai-dev/plans/2026-08-14-1133-1-nop-metadata-jdbc-sql-defense-in-depth-hardening.md`（mission-driver EXEC_PLANS）执行完成。5 个 P2 安全防御纵深缺口（F5/F6/F7/F8/F9）全收口：JDBC URL 危险参数 blocklist 完备、凭证脱敏无泄漏、空/畸形主机 fail-closed、IPv6 字面量判定不触发 DNS、custom_sql blocklist 覆盖 PG 危险关键字。全部为 defense-in-depth——不改变已修复的 F1/F2 攻击面，而是消除"lucky fail-closed"与"审计者已知但 blocklist 未覆盖"的残余间隙。

- **Phase 1 — F5 反射类加载参数族 + F6 含 `@` 口令脱敏**：
  - `Fix` F5：`MetaDataSourceConnectionProcessor.DANGEROUS_URL_TOKENS` 在 AR-02 原 13 个 token 之上补全 5 个触发反射类加载（RCE-chain 潜力）的参数：MySQL `socketfactory`/`statementinterceptors`/`detectcustomcollatz`，PostgreSQL `sslfactory`（SSL SocketFactory 反射实例化）与 `options=`（向 backend 透传命令行风格选项）。`driverClassName` 白名单缓解但不能替代 URL 参数侧 fail-closed——含上述参数的 URL 在 `validateJdbcUrl` 危险参数检查阶段（主机校验之前）即 fail-fast `ERR_DATASOURCE_JDBC_URL_BLOCKED`。
  - `Fix` F6：`redactJdbcUrl` 改为与 `extractHosts` 一致——定位 authority 段（`://` 后到首个 `/` 或 `?`）后用 `lastIndexOf('@')` 找 userinfo 边界并 substring-replace。删除原 `CREDENTIAL_PATTERN` 正则（在第一个 `@` 处停止，含 `@` 口令尾部泄漏：`user:p@ss@host` → `ss@host` 泄漏）。现在多 `@` 极端用例下仅保留最后一个 `@` 之后内容，口令片段不再出现在任何 ErrorCode 参数或消息。
  - `Proof`：对抗测试 7 例（F5：5 新 token 各一 + 既有 token 回归；F6：含 `@` 口令完整脱敏 + 多 `@` 极端 + 既有语义不回归 + 含 `@` 口令端到端错误路径不泄漏）。
- **Phase 2 — F7 空/畸形主机 fail-closed + F8 IPv6 字面量 DNS 安全**：
  - `Fix` F7：`validateJdbcUrl` 主机校验循环新增 `isPlausibleHostShape` 形状校验——空串（`jdbc:mysql:///db`）、以 `/` 开头（path 片段）、纯端口（`:3306`，以 `:` 开头且无第二个冒号）显式拒绝 `ERR_DATASOURCE_JDBC_URL_BLOCKED`（reason="host unparseable"），不再依赖驱动拒绝的 lucky fail-closed。合法无括号 IPv6 字面量（`::1`，以 `:` 开头但含第二个冒号）不被误伤（关键区分：纯端口单冒号 vs IPv6 字面量多冒号）。
  - `Fix` F8：`HostSecurityUtil.isInternalIpv6Literal` 入口增加 charset 前置过滤——仅"字符集 ⊆ [0-9a-fA-F:.] 且 ≥2 冒号"的输入才调用 `InetAddress.getByName`；否则视为非字面量直接 return false（不触发 DNS）。修复前含 `:` 但含非 hex 字母（g-z）的串（如 `gzzz::1`）路由到本方法后直接调 `getByName` 触发 DNS，违反类 javadoc "纯确定性解析，不触发 DNS" 契约。过滤逻辑与 `isIpLiteral` 的 charset 检查逐项一致（代码审查验证）。
  - `Proof`：对抗测试 7 例（F7：空主机/空主机带端口/合法外网不误伤；F8：非 hex 含 `:` 串无 DNS/合法 IPv6 不回归/单冒号过滤）。F7 接线验证：`TestMetaDataSourceConnectionProcessorExtract.testAllowlistMatchesNormalizedHost`（`::1` 在 allowed-hosts 放行）保持绿（形状校验放行合法 IPv6）。
- **Phase 3 — F9 custom_sql blocklist PG 关键字扩充**：
  - `Fix` F9：`MetaQualityRuleExecutor.CUSTOM_SQL_FORBIDDEN_WORDS` 补全 5 个 PG 脚本执行/时序攻击/系统目录族关键字：`DO`（执行匿名 PL/pgSQL 代码块，RCE 等价面）、`PG_SLEEP`（时序盲注/DoS）、`PG_CATALOG`（系统目录 schema）、`PG_STAT_USER_TABLES`（系统统计视图，信息泄漏）、`WITH`（CTE 递归）。
  - `Decision` DRY 裁定：与 `ExpressionMeasureValidator.KEYWORD_BLACKLIST`/`FUNCTION_BLACKLIST` 保持分离（不抽取共享常量）——语义域（整条 SQL 文本 vs SELECT 片段表达式）、分词与匹配机制（token 精确 + fail-closed vs word-boundary + 字符串字面量先抽取）、演进路径均不同；22 项重叠的"恰好同名"是语义域交集自然结果，非 DRY 缺陷。`PG_SLEEP` 同存两表但匹配形态不同，各自独立维护。
  - `Decision` `WITH` CTE false-positive tradeoff：合法分析型 CTE 查询会被阻断——与既有 over-block 哲学一致（custom_sql 面向受限 SQL 场景，已含 `SET` 等）；合法 CTE 需求应改用子查询或专用 measure 表达式通道。
  - `Proof`：对抗测试 3 例（5 新关键字各一 via 沙箱 + via judge 公开入口接线验证 + WITH CTE false-positive 钉死）。既有 CTE-DML 测试（`testCteWrappedDmlBlockedViaJudgeEntry`）保持绿。
- **验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -T 1C` → **1122 tests, 0 failures**（nop-metadata-service 1121 + web 1）；`checkstyle:check` sun_checks.xml 9058 violations 为全模块 pre-existing 基线（项目 checkstyle 非真实门禁，与本次变更无关，本次改动遵循既有代码风格）。
- **Doc-sync**：`docs-for-ai/03-modules/nop-metadata.md` 安全契约段新增 F5/F6/F7/F8/F9 五段（blocklist 完备 / 含 `@` 脱敏 / 空-畸形主机 fail-closed / IPv6 DNS 安全 / custom_sql PG 族 + WITH tradeoff + DRY 裁定）。


### 2026-08-14（mission-driver EXECUTE：P2 audit backlog 全量收敛）

执行 plan `ai-dev/plans/nop-datav/2026-08-14-0950-1-p2-audit-backlog-cleanup.md`（P2 audit backlog 收尾）。修复 6 个已确认审计发现（#4 Dim09-03, #7 Dim04-01, #8 Dim04-02, #11 AR-1, #12 AR-2, #17 AR-7）+ 2 minor nits，全部后端，不阻塞 flux。此计划关闭 `nop-datav-audit-followups.md` 全部 17 items + 2 nits。

**改动**：

- **Phase 1 — ORM Contract Alignment（#7, #8）**（`nop-datav/model/nop-datav.orm.xml`）：
  - `NopDatavScreenSnapshot.snapshotContent` 列加 `mandatory="true"`（对齐 screen-design.md:79）。
  - UK 名从 `UK_NOP_DATAV_SCREEN_SNAPSHOT_SCR_VER` 重命名为 `UK_NOP_DATAV_SCREEN_SNAPSHOT_SCREEN_VER`（对齐 screen-design.md:84）。
  - 经 codegen 重新生成 `_app.orm.xml` + `_NopDatavScreenSnapshot.xmeta`，mandatory + UK 名已同步到生成物。纯 metadata 级微调，不改表结构。
- **Phase 2 — Service-Layer Correctness（#4, #17, 2 minor nits）**：
  - **#4 Dim09-03**（`NopDatavErrors.java` + `NopDatavExportTaskBizModel.java`）：新增 `ERR_DATAV_EXPORT_MISSING_SOURCE`（ARG_SOURCE_TYPE + ARG_SOURCE_ID）；`createExportTask` 空源检查从 `validateFormat` 之后前移到之前，替换误用的 `ERR_DATAV_EXPORT_TASK_NOT_FOUND`（语义错误——任务尚未创建不存在 not-found）。`ERR_DATAV_EXPORT_TASK_NOT_FOUND` 定义/import 保留（`requireTask` taskId-not-found 合法路径）。
  - **#17 AR-7**（`DatavListDatasetsExecutor.java`）：`findActiveDatasets` 将 `dao.findAll()` + 内存 status 过滤改为 `QueryBean` + `FilterBeans.eq(status,1)` + `dao.findAllByQuery()`，将 status=1 下推到 SQL 层，避免加载非活跃数据集行（含 `dsText`/`dsMeta` VARCHAR(131072) 大字段）。keyword 过滤仍内存执行（仅对已过滤活跃集）。
  - **Minor nit 1**（`DatavGenerateDashboardExecutor.java`）：`generateId(prefix)` 删除未使用的 `prefix` 参数（UUID 去横线恰好 32 字符，加前缀超 precision=32），3 调用点同步。
  - **Minor nit 2**（`ChatBiToolCallingLoop.java`）：`QUERY_HANDLER` 中 `parseNonStrict` 提取到 `parsedObj` 局部变量，避免同一三元表达式重复解析。
- **Phase 3 — Module Hygiene（#11, #12）**（`nop-datav/pom.xml` + 目录删除）：
  - **#11 AR-1**：删除 `nop-datav-core` 目录（261 行 codegen 产出常量，从未加入 `<modules>`，service 层有手写枚举，无消费者，single-source-of-truth 侵蚀）。
  - **#12 AR-2**：从 `nop-datav/pom.xml` 的 `<modules>` 移除 `nop-datav-chart` + 删目录（空壳模块，仅一个绕过 nop-bom 的硬编码 `poi:5.4.0` 依赖，从未被其他模块依赖）。

**测试**：
- 新增 `testCreateExportTask_emptySource_throwsMissingSource`（3 断言：空 sourceType/空 sourceId/空源+非法 format 均抛 MISSING_SOURCE，证明检查在 format 校验之前）。
- 新增 `testListKeywordFilterCombinedWithActiveStatus`（keyword + status 组合：验证 status=1 下推 SQL 后内存 keyword 过滤正确，且非活跃数据集即使名称匹配 keyword 也不被加载）。
- 既有 421 测试全量回归无变化。

**验证**：
- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav/nop-datav-dao,nop-datav/nop-datav-service -T 1C`：**423 tests, 0 failures, 0 errors**（+2 新测试）。
- `rg "nop-datav-core|nop-datav-chart" nop-datav/`：无代码/配置残留（仅文档记录）。
- Backlog: `ai-dev/backlog/nop-datav-audit-followups.md` #4/#7/#8/#11/#12/#17 + 2 nits → ✅（全部 17 items + 2 nits resolved）。
- Doc-sync: No owner-doc update required（screen-design.md 已是正确值，本计划让 ORM 对齐 design doc；error handling 为内部契约）。

---

## nop-datav ChatBI Error Surface Cleanup（plan `2026-08-14-0937-3`）


### 2026-08-14（mission-driver EXECUTE：ChatBI error contract & observability remediation）

执行 plan `ai-dev/plans/nop-datav/2026-08-14-0937-3-chatbi-error-surface-cleanup.md`（P2 audit backlog sweep — ChatBI 子系统）。修复 5 个已确认审计发现（AR-3, AR-4, AR-5, AR-6, Dim09-04），全部后端，不阻塞 flux。

**改动**：
- **AR-6 / Dim09-04**（`ChatBiToolCallingLoop.java`）：新增 `Logger LOG` 字段；`.join()` 包 try/catch 同时捕获 `CompletionException`+`CancellationException`，while-loop 解包到首个非 CompletionException cause，抛 `NopException(ERR_DATAV_CHATBI_TOOL_EXECUTION_FAILED).param(ARG_TOOL_NAME).param(ARG_REASON).cause(underlying)`（退役死码 Dim09-04）；handler-swallow + QUERY_HANDLER-swallow 两处 `catch(Exception ignore)` 加 `LOG.debug`（不再双层静默吞）。
- **AR-4**（`NopDatavErrors.java` + `DatavQueryDatasetExecutor.java`）：新增 `ERR_DATAV_CHATBI_DATASET_QUERY_FAILED`（ARG_DATASET_SID + ARG_REASON）；查询异常不再误用 panel 路径共享的 `ERR_DATAV_QUERY_FAILED`/`panelId`，保留 `.cause(e)`。
- **AR-5**（`DatavGenerateDashboardExecutor.java` + `DatavGenerateScreenExecutor.java`）：两 executor 改引用同模块 `NopDatavOperatorResolver.SYSTEM_OPERATOR`，删除伪理由的 `NopOperatorFallback` 内联类（grep 全模块 0 引用）。
- **AR-3**（`DatavGenerateScreenExecutor.java`）：`isUniqueConstraintViolation` 收窄为仅 `unique`/`duplicate`，移除过宽的 `constraint`/`uk_`（三方言 H2/MySQL/Postgres UK 仍正确命中；CHECK/FK/NOT NULL 不再误报为重名触发 LLM rename-retry 循环）。

**测试**：
- 新增 `TestChatBiToolCallingLoop.java`（3 用例：failedFuture / cancelled future / chained unwrap；mock IChatService + IToolManager，断言结构化 NopException 的 errorCode+toolName+reason+cause）。
- 新增 `TestDatavGenerateScreenUkHeuristic.java`（9 用例：CHECK/FK/NOT NULL/bare-constraint/bare-uk_/null 全判 false；H2/MySQL/PG UK 判 true）。
- 既有 36 ChatBI + generate/query/screen E2E 全量回归无变化。

**偏差**：
- Phase 1 测试改为继承 `AbstractNopDatavTest`（plan 称「pure unit test」），因循环内 `ChatBiTypeConverter.toAiToolCall` 经 `getArgumentsText()` 依赖 Nop 平台 JSON provider 初始化；仍满足「mock IChatService + IToolManager、直接测 loop」的核心意图，不接触真实 chat/DB 基础设施。
- Phase 1 cancellation 用例的 reason 断言放宽为 JDK-version-robust（JDK 26 上 cancelled future 的 CancellationException message 为 "join"，非 null）。
- Phase 3 采用 plan 文档化的 alternative：`isUniqueConstraintViolation` 由 `private` 改包可见后直接测试（mocking IOrmTemplate ~40 方法接口不实际）；确定性覆盖三方言。

**验证**：
- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav/nop-datav-service`：**421 tests, 0 failures, 0 errors**（nop-auth-service TestBeanLoader 1 失败为 pre-existing，本模块无关，隔离复现确认）。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：exit 0。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：exit 0（1 high 为 NotificationSender:467 注释 false-positive，pre-existing 非本次引入）。
- 独立子 agent closure audit：13/13 claims PASS（file:line 证据）。
- Doc-sync: No owner-doc update required（error handling 为内部契约；ai-design.md 不涉及异常细节与子串匹配策略）。
- Backlog: `ai-dev/backlog/nop-datav-audit-followups.md` #5/#13/#14/#15/#16 → ✅。

---

## nop-datav D5 Report/Alert Reliability Defects（plan `2026-08-14-0937-2`）


### 2026-08-14（mission-driver EXECUTE：D5 reliability defect remediation）

执行 plan `ai-dev/plans/nop-datav/2026-08-14-0937-2-d5-report-alert-reliability-defects.md`（P2 audit backlog sweep）。修复 6 个已确认审计发现（Dim14-03, Dim14-04, Dim09-02, Dim09-05, Dim16-02, Dim16-03），全部后端，不阻塞 flux。

**改动**：

- **Phase 1 — Dim14-03 通知顺序修复**（`AlertEvaluator.java`）：
  - OK→TRIGGERED / TRIGGERED→OK / rearm 3 个状态转换分支：`sendAlertNotification` 移至 `setLastNotifiedTime`/`setLastResolvedTime` 之前。通知成功后才回写时间戳；通知失败时异常向上抛（合约保持），时间戳保持原值（null/旧值），下次评估可重试。
  - OK→TRIGGERED 分支额外在 interim save 前清空 `lastNotifiedTime(null)`，保证重启安全（JVM 崩溃后 lastNotifiedTime=null → rearmSeconds>0 时 shouldRearm(null)=true 立即重试）。
  - 3 新测试：testTriggerNotificationFailureLeavesLastNotifiedTimeNull、testTriggerNotificationSuccessSetsLastNotifiedTime、testRestartSafetyRearmRetriesWhenLastNotifiedTimeNull。
- **Phase 2 — Dim14-04/Dim09-05 调度器异常分类**：
  - `NopDatavReportScheduler.executeScheduledReport` + `NopDatavAlertScheduler.executeScheduledAlert`：`catch (Throwable)` → `catch (Exception)`，允许 `Error`（OOM/StackOverflow）传播。
  - `NopDatavAlertScheduler`：`IllegalArgumentException("missing alertRuleId")` → `NopException(ERR_DATAV_ALERT_RULE_NOT_FOUND).param(ARG_ALERT_RULE_ID, "(absent from job params)")`。
  - 1 新测试：testErrorPropagatesFromExecuteScheduledAlert（StackOverflowError 经 catch(Exception) 不被捕获而传播）。
- **Phase 3 — Dim09-02 告警配置错误码**：
  - `NopDatavErrors.java`：新增 `ERR_DATAV_ALERT_VALUE_REQUIRED`、`ERR_DATAV_ALERT_UNSUPPORTED_OPERATOR`、`ERR_DATAV_ALERT_UNSUPPORTED_AGGREGATION`。
  - `AlertThresholdComparator.java`：2 处 `IllegalArgumentException` → `NopException` + 结构化 ErrorCode。
  - `AlertAggregator.java`：1 处 `IllegalArgumentException` → `NopException` + 结构化 ErrorCode。
  - `TestAlertThresholdComparator` + `TestAlertAggregator`：4 处 `assertThrows(IllegalArgumentException` → `assertThrows(NopException` + ErrorCode 断言。
- **Phase 4 — Dim16-02/Dim16-03 缺失错误路径测试**：
  - `TestNopDatavAlertE2E.testPanelQueryFailureRecordsErrorAndPreservesState`：panel 引用不存在的表 → queryPanelData 抛错 → errorMsg 持久化 + state 保持 OK + evaluate() 正常返回。
  - `TestNopDatavReportE2E.testReportDeliveryExportFailureRecordsError`：report task panel 引用不存在的表 → exportDashboard 抛错 → delivery FAILED + errorMsg（异步轮询模式）。
- **设计文档**：`schedule-report-design.md` §9（catch(Exception) 实现契约）+ §15（通知顺序语义 + 重启安全注释）。

**验证**：
- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav/nop-datav-service`：**409 tests, 0 failures, 0 errors**（含 7 新测试 + 既有全量回归）。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：1 passed, exit 0。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：1 high（pre-existing false positive — NotificationSender:467 附件注释，代码已实现，非本次引入）。

---

## nop-datav IM 渠道通知接入（plan `2026-08-14-0937-1`）


### 2026-08-14（mission-driver EXECUTE：plan D5-1/D5-2 deferred successor — IM/渠道推送）

执行 plan `ai-dev/plans/nop-datav/2026-08-14-0937-1-im-channel-notification-integration.md`（D5-1/D5-2 deferred successor）。把报告交付与告警通知的 IM 渠道从「显式抛 `UnsupportedOperationException`」收口为「经 `IChannelMessageService.sendToUser` 真实发送」。

**改动**：

- `NopDatavErrors.java`：新增 4 个 IM 专用错误码（英文消息）+ 2 个上下文参数 ARG。
  - 报告：`ERR_DATAV_REPORT_CHANNEL_SERVICE_NOT_CONFIGURED`（service 未注入）、`ERR_DATAV_REPORT_ALL_NOTIFY_FAILED`（全渠道零成功）。
  - 告警：`ERR_DATAV_ALERT_CHANNEL_SERVICE_NOT_CONFIGURED`、`ERR_DATAV_ALERT_ALL_NOTIFY_FAILED`。
- `NotificationSender.java`：
  - 新增 `@Inject @Nullable setChannelMessageService` setter（镜像 setEmailSender），null → IM 渠道显式失败。
  - `sendReport`/`sendAlert` IM 分支移除 `UnsupportedOperationException`，落地 **Decision A**（partitionRecipients 按邮箱正则 `^\S+@\S+\.\S+$` 分区为 emailAddrs / userIds）+ **Decision B**（deliverReportViaIm/deliverAlertViaIm → sendToUsersAggregated：逐 userId sendToUser + 逐用户 try/catch catch Exception（非 Throwable）+ WARN 日志，≥1 SENT → 渠道 delivered，全失败 → 抛 ALL_NOTIFY_FAILED）+ **Decision C**（buildChannelMessage 仅 text/markdown，无附件）。
  - email 渠道改为仅当 emailAddrs 非空时投递（向后兼容：纯邮箱任务行为不变）。
  - 类/方法 Javadoc 同步（IM 从「显式抛 UnsupportedOperationException」→ 真实发送 + Decision A/B/C）。
- `MockChannelMessageService.java`（新增）：测试 mock IChannelMessageService，可编程返回 SENT/NO_BINDING/抛异常，记录 sendToUser 调用（userId + message）。
- `test-report-mock.beans.xml`：注册 MockChannelMessageService 为 primary=true（同时供报告 + 告警测试）。
- 测试：
  - `TestNopDatavReportE2E`：`testImChannelThrowsUnsupported` → `testImChannelSendsViaChannelMessageService`（断言 sendToUser 调用次数 + userId + message.text 含报告摘要 + deliveredChannels 含 im + 无附件）；新增 testImChannelAllNoBindingFailsExplicitly / testImChannelServiceNotConfiguredFailsExplicitly / testMixedEmailAndImChannelsPartitionedDelivery。
  - `TestNopDatavAlertE2E`：`testImChannelThrowsUnsupported` → `testImChannelSendsViaChannelMessageService`；新增 testAlertTriggerAndRecoverViaIm / testE2eAlertTriggerViaImChannel（经 scheduler 入口，端到端）。
- `schedule-report-design.md`：§1 IM 从「deferred（UnsupportedOperationException）」收口为已接入 + 新增 §1.1 Decision A / §1.2 Decision B / §1.3 Decision C；§10 渠道范围表 IM 行改为 Decision A/B 语义 + ALL_NOTIFY_FAILED；§11 拒绝替代方案表新增 Decision A/C 拒用项；§12/§17/§23 组件/错误码表更新；Last Updated → 2026-08-14。

**接线验证（Anti-Hollow，rule #22/#23）**：

- sendToUser 运行时被调用：报告/告警 IM 测试 mock verify 调用次数（2/1）+ userId 参数（u1/u2）+ message.text 含语义字段（报告摘要 / 告警 ruleName+state+currentValue）。
- 混合渠道（email+im，recipients 含邮箱+userId）两渠道各投递各自分区，deliveredChannels 含 email+im。
- Decision B 渠道聚合：全 NO_BINDING → delivered 空 → delivery FAILED + errorMsg；单用户 NO_BINDING → WARN（非吞掉）。
- 部署前提：nop-datav-service 仅依赖接口 nop-integration-api，未注入 channelMessageService 时 IM 显式失败。

**验证**：

- `./mvnw clean install -pl nop-datav/nop-datav-service -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav/nop-datav-service`：BUILD SUCCESS，**403 tests, 0 failures, 0 errors**（含 7 新 IM 测试 + 既有 email 全量回归）。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：1 passed, exit 0。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：exit 0（唯一 high 发现为 pre-existing 误报，deliverViaEmail 附件注释，代码实际已实现）。
- 独立子 agent closure audit（ses_001a3533affemWcP3GfvURWE9x）：APPROVE_CLOSURE，12/12 项 PASS。

---

## nop-datav Plan {3} Service-Layer Contract Correctness：Phase 2（Dim14-02 报告交付 SMTP 移出 session）


### Plan `2026-08-14-0900-1` 独立 closure audit PASS（fresh session，`ses_000c16951ffeCO4WIal3ktvPMD`）

- 审计发现 1 项形式问题：14 个 Closure Gates 未勾选即标记 plan completed（执行者疏漏）→ 已补勾全部 14 项（内容均经审计实质验证通过）→ `check-plan-checklist.mjs --strict` exit 0。
- 实质核验全 PASS：① 生产端 RWO :914-923 / BRWO :981-988 转发（emitElement 广播 / 扇出，无 fail-fast throw）；② 消费端 `processInputGate` :830-850 side-output 路由（getId 匹配 + 未命中抛 `ERR_STREAM_SIDE_OUTPUT_NO_CONSUMER`）；③ Anti-Hollow（codec side-output 分支 + 未知类型仍抛 + toWireMap 含 outputTagId）；④ E2E 运行时连通（送达断言 + BRWO assertNotSame + 无消费者 fail-fast 用例）；⑤ 门禁证据（注册表 914/981 + forward 分类 + forwardRe 含 emit 形态 + self-test emit 正例 + mjs all exit 0 + doc-links exit 0）；⑥ checkpoint 路径（ChannelState outputTagId + round-trip 断言）；⑦ roadmap HG-01 closed + 行号重钉；⑧ 测试实测 core 46/46 + runtime 25/25 全绿。
- **最终判定：PASS**（closure 形式门禁修复后）。


### Plan `2026-08-14-0900-1` Phase 4 + closure：HG-01 门禁同步 + 全量验证 + 收口落档

- **注册表迁移**（`output-contract-registry.json`）：RWO/BRWO 分类 `fail-fast` → `forward`（disposition 附 HG-01 批准依据 + 线协议语义 + 无消费者 fail-fast 位置说明）；TimestampedCollector disposition 修订（wrapped RWO 转发语义）；collectOutputTagLine 重钉 892→914 / 960→981（Phase 3 改写使行号下移）。
- **mjs 分类器**（D8）：`classifyCollectBody` `forwardRe` 扩展识别 `emit`/`emitElement` 形态（`check-nop-stream-invariants.mjs:784`——不扩展则 RWO body 抛 `unrecognized collect(OutputTag) method body form` crash）；self-test 新增 emit 形态正例夹具（EmitOutput fixture）。
- **wiring 注册表**：Phase 3 改动使 8 处 StreamTaskInvokable 接线点行号漂移（setOutput/setProcessingTimeService/setTimeServiceManager/setSnapshotCallback），re-pin 501/502/247/285/304/310/465/474 → 502/503/248/286/305/311/466/475。
- **门禁 JUnit**：`assertForwardBehavior` 新增 RWO/BRWO harness 用例（无消费者 fail-fast harness 移除，保留 no-silent-skip default 守卫）；`TestOutputContractInvariant` 14/14 全绿（注册表迁移后参数化用例 [3]/[4] 由红转绿，中间态收敛）。
- **catalog #6 同步**：检测方法用例数修正（JUnit 10→14、E2E 4→6）。
- **roadmap**：`HG-01` 条目 **pending human confirmation → closed**（批准记录 + 落地摘要 + 接线路径行号重钉——645/706 → 914/981、239/245 → 248/286、352 → 462、GraphExecutionPlan 454-458 → 456-461；四重保护性覆盖复核更新：in-task fail-fast 维持、门禁三态分类更新、pin 2 条 = 历史记录（mjs-pins.json 注记在案）、条目 closed）。
- **验证**：全量回归 `./mvnw test -pl nop-stream -am -T 1C` **BUILD SUCCESS 0 failures**（门禁 JUnit 14/14 + E2E 6/6 + codec 19/19 + 路由 2/2）；`mjs all` exit 0（含 scan-output-contract V1–V5 + self-test emit 正例）；`check-doc-links.mjs --strict` exit 0（3 warnings = credential plan 既有，无关）；`check-plan-checklist.mjs` 全项勾选。
- **收口**：plan 0900-1 Status completed（全 4 Phase）、Closure Gates 全过、Closure Evidence 落档；Follow-up = Deferred But Adjudicated（图级 API/StreamEdge.outputTag/DSL 解封 = successor）+ C2-PR-3 + 剩余发射点 E2E 覆盖 backlog。
- **Next**：独立 closure audit（fresh task_id）→ audit 通过后本条目补记 PASS。


### Plan `2026-08-14-0900-1` Phase 3：HG-01 生产端转发 + 消费端路由 + 远程线协议（落地与 Phase 1 裁决零出入）

- **生产端**（`StreamTaskInvokable.java`）：RWO/BRWO `collect(OutputTag, record)` 移除 fail-fast 文案 → 构造 `SideOutputElement` 经 `RecordWriter.emitElement` 广播（D3）；RWO 内层 record `copy(...)`、BRWO 每输出独立元素实例（D5，E2E 断言 `assertNotSame` 双元素实例 + 内层 record）。
- **消费端**：`processInputGate` 新增 `isSideOutput()` 分支——`sideOutputConsumers.keySet()` 按 `getId()` 等值匹配（D4）；命中 → `consumer.accept(innerRecord)`；未命中 → 抛 `ERR_STREAM_SIDE_OUTPUT_NO_CONSUMER`（含 tag id + task role）。`registerSideOutputConsumer` 零改动即双路径服务同一 map（in-task 链式 + 跨 task 入站）。
- **远程路径**（审查 B1）：`DataPlaneWireSupport.toWireMap` 补 `outputTagId` 字段——三个 string codec（Pulsar/Kafka/SysDao）全经此层；反序列化 `parseBeanFromText(StreamMessageEnvelope.class)` 自动还原字段。
- **materialization 双写**（D6）：零代码改动——`SideOutputElement.isRecord()=false` 天然被 `ResultPartition.java:198` 双写门排除，门处补 D6 留痕注释。
- **测试**：`TestOutputContractInvariant` 翻转 3 处（TimestampedCollector 包装 RWO 改为转发断言 :368-384；新增 RWO/BRWO 直接转发 + BRWO 扇出用例）；新增入站路由用例 2 个（注册送达 / 未注册 fail-fast，InputGate 桩 + 真实 SINK 角色 invokable + invoke 线程，复审 F4 方案）；E2E 第 4 用例翻转（fail-fast → 转发断言）+ 新增跨 task 送达 E2E（生产者 window 算子 → 线协议 → 消费 task 注册消费者收到，含 BRWO 双 writer 扇出多 subtask 广播变体 + D5 无别名化断言）+ 无消费者 fail-fast E2E（消费端路由点抛 `ERR_STREAM_SIDE_OUTPUT_NO_CONSUMER`）；3 个 string codec 测试各新增 side-output envelope round-trip 用例（B1 证据）。
- **验证**：E2E 6/6 + codec 19/19 + 路由 2/2 全绿；**先红后绿证据**——stash `processInputGate` 分支后 2 路由用例红（送达不触发、fail-fast 不触发），恢复后全绿；**门禁红中间态已声明并验证**：core 全量 1494 tests 仅 2 失败 = 注册表驱动参数化用例 [3]/[4]（RWO/BRWO 分类仍为 fail-fast，代码已转发——预期红，Phase 4 迁移注册表后转绿）；runtime `-am` 全组同样仅此 2 失败。Phase 3 落地与 Phase 1 裁决零出入（core-design §6.1 无需修正）。
- **Next**：Phase 4（注册表分类迁移 + mjs 分类器 + roadmap 收口 + 全量回归 + 独立 closure audit）。


### Plan `2026-08-14-0900-1` Phase 2：HG-01 side-output 线协议组件级实现（codec/元素类型/ChannelState 字段层）

- **新增** `SideOutputElement`（`io.nop.stream.core.streamrecord`）：携带 `outputTagId` + 内层 `StreamRecord`，`copy()` 深拷贝内层 record 防 BRWO 扇出别名化（D5）；`StreamElement.isSideOutput()`/`asSideOutput()` 加入（final，既有 5 判定零影响）。
- **envelope**：新增 `TYPE_SIDE_OUTPUT_RECORD = "SIDE_OUTPUT_RECORD"` + `outputTagId` 字段（默认 null，既有 5 类型零改动）。
- **codec**：encode 对 side-output 派生 valueType（忽略入参 edge 级 valueType，D2 反例测试锁定）；decode 还原 `SideOutputElement`；未知类型仍抛 `ERR_STREAM_INVALID_STATE`（新增 `unknownEnvelopeType_stillThrows` 锁定无静默跳过）。
- **ChannelState**（审查 B2）：`envelopeToMap`/`mapToEnvelope` 逐字段层补 `outputTagId`；`toSerializableForm` side-output valueType 派生。
- **RemoteInputChannel/RemoteResultPartition 复核**：仅 CONTROL 特判，side-output 走通用 decode 入队，无需改动。
- **验证**：新增 8 用例（codec 6 + channel state 1 + unknown-type 1）全绿；**先红后绿证据在案**——stash 还原 codec 后 2 个代表用例红（decode 抛 `ERR_STREAM_INVALID_STATE`），恢复后全绿；nop-stream-core 全量 **1490 tests / 0 failures / 1 skipped**。
- **Next**：Phase 3（RWO/BRWO fail-fast → emitElement 广播 + processInputGate 消费路由 + toWireMap 字段 + 门禁翻转 + E2E）。


### Plan `2026-08-14-0900-1` Phase 1：HG-01 跨 task side-output 线协议 9 项设计裁决落档

- **裁决（全部落档 core-design.md §6 + invariant-catalog.md #6）**：① **线协议形态** = envelope 新增 `TYPE_SIDE_OUTPUT_RECORD` + `outputTagId` 字段（默认 null，既有 5 类型零改动）+ `StreamElement` 新 side-output 子类型（拒绝复用 STREAM_RECORD 加可选 tagId——语义模糊 + 与既有解码路径纠缠）；② **valueType 派生** = 忽略 edge 级 valueType（`RemoteResultPartition.java:162-164` 只标主输出边），一律从内层 record value 派生类名；③ **生产端通道** = `RecordWriter.emitElement(StreamElement)` 广播全部 partition（`emit(StreamRecord)` 按 partitioner 路由不可用），语义 = 所有下游 subtask 收到 side-output，任一未注册消费者即 fail-fast（拒绝新增 tag 路由 writer 方法——OutputTag 无 partitioner 概念）；④ **消费端查找** = `sideOutputConsumers.keySet()` 按 `getId()` 遍历匹配（OutputTag 构造器 `Guard.notNull(typeInfo)` 禁止 null-typeInfo 查找 key）；⑤ **record copy** = BRWO 多 partition 扇出时内层 record `copy(...)` 防别名化；⑥ **materialization 双写 = 排除（non-blocking）**：`ResultPartition.java:198` 只双写主记录流，不扩展——side-output 为尽力送达增强（消费端无强一致契约）、store 全链扩展超批准面、恢复期丢失不违反不变式 #6（运行期转发契约 vs checkpoint 恢复面独立），Phase 3 测试/文档注明留痕；⑦ **无消费者 fail-fast 移至消费端路由点**（`processInputGate` side-output 分支），与 in-task `ChainingOutput` 语义对齐；⑧ **mjs 分类器** = 扩展 `forwardRe` 识别 emit/emitElement 形态 + self-test 正例夹具（锁定实现形态不可行——RWO 无 consumer map、`writer.emit` 不匹配现有正则）；⑨ **门禁同步** = 注册表 RWO/BRWO 分类迁移 fail-fast → forward（棘轮理由在案）+ JUnit 翻转（含 `testTimestampedCollectorWrappingRecordWriterOutputFailsFast` :368-384）+ E2E 第 4 用例翻转 + 新增送达/无消费者用例。
- **裁决前 live 复核（Proof）**：RWO/BRWO collect(OutputTag) fail-fast 位置（:892-902/:960-970）、envelope 5 类型常量（:32-36）、`StreamElementCodec` 4 分支 + 未知类型抛 `ERR_STREAM_INVALID_STATE`、`processInputGate` 分支面（:780-848）、`OutputTag.java:63` Guard.notNull、`sideOutputConsumers` :133 + `registerSideOutputConsumer` :391-393——全部与 plan Current Baseline 一致，零漂移。
- **验证**：Phase 1 纯文档裁决（无代码变更），未跑 mvnw；grep/read 实证复核 baseline 行号。
- **Doc-sync**：`ai-dev/design/nop-stream/core-design.md` §6/§6.1（线协议设计裁决节 + 不变式 #6 处置状态 fail-fast → forward）；`ai-dev/audits/nop-stream-invariants/invariant-catalog.md` #6（陈述 + 检测方法 + live 修复状态更新）；本 log 条目。catalog :303-307 现存漂移（空体 no-op + pin 2 + E2E 3 用例）已随本次更新修正。
- **Next**：Phase 2（codec/元素类型/ChannelState 字段层实现 + 先红后绿测试）。


### Plan `2026-08-14-0900-2` closure：rocksdb 命名空间迁移（P1-01-01）全 3 Phase 收口 + 独立 closure audit PASS

- **执行**：17 main + 13 test 类从 `io.nop.stream.core.common.state.backend.rocksdb[.incremental]` 迁移至 `io.nop.stream.rocksdb[.incremental]`（git mv + package 声明，commit `6b742c86d`，30 文件 rename 全 99% 相似度 = 纯机械零行为变更）；模块内跨包 import 6 行同步（RocksDBKeyedStateBackend ↔ incremental、3 个 incremental test）；`nop-stream-runtime` test 7 文件引用同步（6 import + FQN 内联 2 文件 6 处——TestE2EWindowAggregateRestore :295-296/:308-309 + 复审发现的 TestRocksDBStateBackendE2E :262-263 RocksDBOptionConfig）。`RocksDBStateBackend.java:15` 的 `MemoryOperatorStateBackend` import（core 既有包）按计划保留不动。
- **验证**：`./mvnw compile -pl nop-stream-rocksdb -am` SUCCESS；`./mvnw test -pl nop-stream-rocksdb,nop-stream-runtime -am -T 1C` BUILD SUCCESS（首跑 1 项失败 = `TestRocksDBIncrementalRestoreAndBenchmark` 性能基准并发 reactor 超限 ratio 2.44，单跑 0.899s PASS——08-02/05/06 日志已文档化 pre-existing flaky，非迁移引入）；`./mvnw test -pl nop-stream -am -T 1C` 全组 **3050 tests / 0 failures** BUILD SUCCESS；grep 实证代码面（nop-stream/** 排除 target/_tmp/ai-dev）旧 FQN 零残留 + 新包就位；mjs all exit 0；doc-links exit 0；check-plan-checklist 仅 4 项既有 FAIL（credential/2250/288/321，变更前 stash 对照证实非本 plan 引入）。install 双仓库：`.nop/repository`（`.mvn/maven.config` 重定向的 head repo，install 实测落盘）+ `~/.m2`（覆盖 8月13 旧 jar，消除旧包路径混淆；新 jar 含 20 新路径 class + 旧路径仅空目录）。
- **偏差**：`TestRocksDBIncrementalRestoreAndBenchmark` 在 -T 1C 全量下偶发超限（计划预期零失败）——已判定 pre-existing flaky（日志 08-02/05/06 同族记录，单跑 PASS），复跑全绿，非本 plan 回归。
- **Doc-sync**：roadmap backlog `P1-01-01` 条目 → `closed`（批准 + 落地记录在案）；`2026-08-13-0805-multi-audit` P1-01-01 条目补处置记录（successor 执行完成 → closed）；plan Closure Evidence 落档；本 log 条目。`No owner-doc update required`（docs-for-ai/ 无 FQN 引用面变更）。


### fix(nop-code): AR-149/150 ORM cascadeDelete successor — NopCodeFile.usages + NopCodeSymbol.usages cascadeDelete 补齐

- Plan: `ai-dev/plans/2026-08-14-0707-1-nop-code-cascade-delete-successor.md`（Status: completed）
- Context: Cycle 1 / I4 Phase 4 的 gated successor（plan-first Protected Area: ORM 模型结构变更）。I4 Phase 4 已落地 service-layer 显式删除降级，本次补齐声明式 ORM cascadeDelete 作为 belt-and-suspenders。
- Changes:
  - `nop-code/model/nop-code.orm.xml`：NopCodeFile.usages (:257) + NopCodeSymbol.usages (:392) 两处 `<to-many>` 新增 `cascadeDelete="true"`。类别清扫确认 NopCodeIndex.usages (:165) 已有，无遗漏。
  - `_app.orm.xml` regen：`./mvnw install -pl nop-code -am -DskipTests` → 生成产物 `_app.orm.xml` (:259, :409) 传播 cascadeDelete="true"，与源模型一致。
  - 新增 `TestCascadeDeleteOrmLevel.java`：2 tests（file-level + symbol-level），验证 `session.delete(entity)` + `session.flush()` 后 NopCodeUsage 行被级联删除。File 级构造不含 symbols/calls 的 file 以避免 FK 约束；Symbol 级构造不含 children/members 的 symbol。参照 `TestOrmRelationNavigation.testCascadeDeleteCompleteness()` 模式。
- Verify: `./mvnw test -pl nop-code -am -T 1C` BUILD SUCCESS（全模块测试全绿，含 TestCascadeDeleteOrmLevel 2/0/0 + 全部回归测试）；`./mvnw install -DskipTests -pl nop-code -am -T 1C` exit 0；`scan-hollow-implementations.mjs --module nop-code --severity high` exit 0。
- Impact: Cycle 1 最后一个 ORM gated successor 关闭。service-layer 降级路径保持不变。Phase 9 @Auth 仍为 gated 阻塞（已 cancelled——auto-derived 权限覆盖）。
- Roadmap: I4 维持 `done*`（ORM cascadeDelete successor ✅）；header 状态 + Work Item Status + Loop Rule T0 三处同步更新。
- Files: `nop-code/model/nop-code.orm.xml`, `nop-code/nop-code-service/src/test/java/io/nop/code/service/TestCascadeDeleteOrmLevel.java`

### EXEC_PLANS nop-metadata INV-LIMIT 默认 surefire 防御纵深（F3 残留清理）

plan `ai-dev/plans/2026-08-14-0707-3-nop-metadata-invariant-enforcement-in-default-build.md`（mission-driver EXEC_PLANS）执行完成。F3 残留（INV-LIMIT 默认 surefire 排除 + 陈旧注释）全清理，INV-LIMIT 回归默认 surefire，与 CI `invariant-gate` job 形成 defense-in-depth 双重运行。

- **Phase 1 — INV-LIMIT 回归默认 surefire（防御纵深 + 清理陈旧配置）**：
  - `Fix`：移除 `nop-metadata-service/pom.xml` surefire `<excludes>` 中对 `TestLimitNegativeValueInvariant` 的排除（连同"保证默认恒绿"的陈旧注释一并删除）。该排除前提已失效——底层 limit 静默 clamp 缺陷已于 I4/plan-05 Phase 0 收敛，该测试现全绿。
  - `Decision`：裁定**保留** `run-nop-metadata-invariants.sh` step 4 作 CI defense-in-depth 第二层（聚合入口在 `invariant-gate` job 每个 push/PR fail-fast 复跑，独立于 build job surefire）；仅更新其 step 4 注释中"excluded from default surefire"的陈旧表述。
  - 文档/注释同步：`TestLimitNegativeValueInvariant.java` Javadoc 由"Default surefire exclusion"段改为"Default surefire inclusion（双重运行）"段；`invariant-guards.md` 单条门禁注释 + INV-LIMIT 运行说明段改为双重运行（默认 surefire + CI invariant-gate）。
  - **Anti-Hollow proof**：默认 `./mvnw test -pl nop-metadata -am -T 1C` 输出确认 `TestLimitNegativeValueInvariant` **实际运行**（4 tests, 0 failures，非 skipped），`TestLimitTargetSetCompleteness`（2 tests, 0 failures）。任一重新引入 `limit<0?0:limit` / `Math.abs(limit)` 的改动现于本地默认测试即变红。
- **验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -am -T 1C` → **1106 tests, 0 failures**（nop-metadata-service）+ web 1 test 0 failures；4 条不变式门禁仍零命中（INV-LIMIT 现默认 surefire + CI 双重验证）。
- **F3 P1 前提订正**（独立审查 + lead agent 双重核实 live CI）：F3 审计的 P1 定性建立在"默认 CI 不 guard INV-LIMIT"前提上，该前提与 live repo 不符——I5 已通过 `invariant-gate` job 每个 push/PR fail-fast 运行该测试。F3 重裁为"残留 = 防御纵深/陈旧配置清理"，仍以 `Fix` 收口（满足非可降级），范围严格限定。
- Doc-sync：`docs-for-ai/02-core-guides/invariant-guards.md`（INV-LIMIT 双重运行方式）。


### EXEC_PLANS nop-metadata 静默错算与契约缺口闭环（AR-01 SLA 分数截断 / AR-02 NFE 逃逸 / AR-03 group-key 碰撞 / F4 selection 契约）

plan `ai-dev/plans/2026-08-14-0707-2-nop-metadata-silent-wrong-result-and-contract-fixes.md`（mission-driver EXEC_PLANS）执行完成。4 个 P1 "silent-wrong-result / 契约缺口"缺陷全收口：结果正确（带错误信号或正确数值）、公开参数契约收敛、每项均有回归/对抗测试钉死。

- **Phase 1 — AR-01/AR-02 SLA 正确性（同方法合并）**：`MetaContractChecker.toDurationMillis` 两条换算路径（通用 + week 分支）均改为 `(long)(amount * unitMillis)` 先乘后取整（不再 `(long)amount` 截断——`0.5h` → 1_800_000 ms 而非 0）；不可解析 amount（非数字 String `"oops"`）映射 `ERR_CONTRACT_SLA_INVALID`（带 cause + 错误值），不再逃逸裸 `NumberFormatException`（原 catch 仅捕 ClassCastException）。回归测试 5 例：分数 hour fresh/stale 边界、分数 week fresh、非数字 amount fail-loud、既有合法 SLA 不回归。PASS。
- **Phase 2 — AR-03 cross-DB 内存 group-key 结构化**：`AggregationHelper.memoryGroupBy` 改用 `LinkedHashMap<List<Object>,...>` 结构性 key（值级 equals/hashCode），不再用 `\u0001` 分隔符 + `\u0000` null 哨兵拼接 String。消除控制字符入参碰撞：`("a","\u0001b")` 与 `("a\u0001","b")` 现产出两个不同分组；`null` 与字面量 `"\u0000"` 不碰撞。对抗测试 2 例（分隔符碰撞 + null 哨兵碰撞）；既有 `testCrossDbMemoryHavingMatchesSameDbSqlPath`（R8.3 内存/SQL 一致性）仍绿。PASS。
- **Phase 3 — F4 `selection` 参数契约收敛（裁定：显式 no-op）**：live 验证发现 GraphQL 引擎（`ReflectionBizModelBuilder:392-393`）将**响应字段选择集**（DTO 级 `{ tableType items }`）自动注入 `selection` 参数——非调用方传入的行列过滤。consume（后置 key 过滤）实测会错误清空行内列（3 个 GraphQL 路径测试 red）。由于结果为不透明 `List<Map<String,Object>>`（Map 无字段级 GraphQL 选择语义），裁定选**显式 no-op + owner-doc 声明**（非 breaking）：三方法 Javadoc 显式声明 selection 为 no-op，回归测试 3 例钉死 no-op 契约（非 null selection 不裁剪列；null selection 回归；aggregation 同契约）。与 P2-24 carve-out 边界在 owner-doc 写清（P2-24 = items 返回类型，F4 = selection 参数语义，正交）。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` → **1103 tests, 0 failures**（nop-metadata-service 1102 + web 1）；`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS（8 模块全绿）；`check-plan-checklist.mjs --strict` exit 0（38 items 全勾选）。
- **Doc-sync**：`docs-for-ai/03-modules/nop-metadata.md` 新增三段（selection 显式 no-op / SLA 分数与非法值语义 / cross-DB 内存 group-key 结构化）。


### EXEC_PLANS nop-metadata 安全攻击面闭环（F1 HAVING 注入 / F2 多主机 SSRF / AR-04 POJO 脱敏）

plan `ai-dev/plans/2026-08-14-0707-1-nop-metadata-security-attack-surface-closure.md`（mission-driver EXEC_PLANS）执行完成。3 个 confirmed live defect（2×P0 + 1×P1）全收口：攻击向量关闭、对抗性测试钉死、安全契约（fail-closed 内网拒绝 / 默认脱敏 / 注入 fail-fast）恢复成立。

- **Phase 1 — F1 HAVING 注入回归闭环**：`MetaAggregationExecutor.preprocessHavingArithmetic` 在递归入口对每个节点 `removeAttr(HAVING_EXPR_RESOLVED_ATTR)` 清除客户端可伪造的标记（contained 方案，不改 resolver 签名、不动 6+ 调用点），仅 `expr` 路径合法处理后重新置位。修复前 `TreeBean.createFromJson` 把任意客户端 JSON key 写为 attr，伪造 `havingExprResolved=true` + SQL payload `name` 可绕过 `nameResolverFor` 白名单把恶意 SQL 原文回填 HAVING。活载荷验证（硬门禁）：经真实 `buildExternalAggregationSql` 路径（preprocess → nameResolverFor）构造 forged marker + `(SELECT COUNT(*) FROM mysql.user...)` payload 实跑 → fail-fast `ERR_AGGR_HAVING_UNKNOWN_NAME`。对抗性单测 4 例（preprocess 清除 + 递归子树 + live-path + 既有合法 expr 不回归）。
- **Phase 2 — F2 多主机 JDBC URL SSRF 绕过闭环**：`MetaDataSourceConnectionProcessor` 重构 `extractHost`→`extractHosts`（返回主机列表），覆盖三种 MySQL/PG 多主机 URL 语法：逗号分隔 `h1,h2:port`、`address=(host=h1)(port=p1),address=(host=h2)`、key-value `(host=h1,port=p1),(host=h2,port=p2)`（`splitTopLevelCommas` paren-depth 跟踪，不切括号内逗号）。`validateJdbcUrl` 对列表中**每一**主机执行 `HostSecurityUtil.isInternalHost`。修复前 `extractHost` 在第一个逗号处截断，只校验第一主机，内网第二主机未经校验（驱动故障转移连到未校验主机）。对抗性单测 6 例（逗号/address/key-value 三形式各含内网第二主机被拒 + 全外网不误伤 + 第一主机内网回归）。既有 33 例全绿（含 IPv6/userinfo/IP 记法）。接线验证：`testConnect` 等入口均经前置 `validateJdbcUrl` 拦截。
- **Phase 3 — AR-04 POJO 脱敏缺口闭环**：`MetaModelChangedEventPublisher.buildEntitySnapshot` POJO 回退分支 stringify→parse 得到 Map 后路由回 `redactSensitiveKeys`（Map 分支抽出的共享脱敏 helper），不再以 `JsonTool.stringify` 反射序列化泄露敏感字段。三分支（ORM/Map/POJO）对同一敏感 key 产出一致脱敏。删除"本 helper 实际只接收 ORM 实体"误导注释。对抗性单测 2 例（POJO 含 password/connectionConfig 脱敏 + 三分支一致性断言）。
- **验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；F1/F2/AR-04 受影响测试类全绿（TestHavingArithmeticPreprocess 14、TestExternalAggregationProcessor 38、TestMetaDataSourceConnectionSecurity 37、TestMetaDataSourceConnectionProcessorExtract 6、TestMetaModelChangedEventPublisherSecurity 9）；`checkstyle:check` 项目级 pre-existing violations 全在 nop-api-core 依赖模块（项目 checkstyle 已禁用，与本次变更无关）。
- **Doc-sync**：`docs-for-ai/03-modules/nop-metadata.md` 新增「安全契约（攻击面闭环）」段（HAVING fail-fast / 多主机 SSRF fail-closed / 三分支脱敏一致）。
- **审计盲区声明回应**：F1 reachability（GraphQL TreeBean 绑定路径）由 live-path 测试（buildExternalAggregationSql 真实运行时路径）证实 closed。


### DRAFT_PLANS nop-metadata 不变式闭环再审计 → 3 remediation plan（mission-driver）

mission-driver DRAFT_PLANS 步骤：处理 2026-08-14 不变式闭环再审计（multi + open，两份 `Audit Status: open`）的 P0/P1 共 8 项，拟制 3 个 remediation plan；P2 共 25 项入 mission roadmap Follow-up Backlog。两份审计 `open → planned`。

- **P0/P1 分配（按 closure surface 捆绑）**：
  - `2026-08-14-0707-1-nop-metadata-security-attack-surface-closure.md`（安全攻击面；F1 HAVING 注入回归 P0、F2 多主机 SSRF 绕过 P0、AR-04 POJO 脱敏缺口 P1）→ `active`
  - `2026-08-14-0707-2-nop-metadata-silent-wrong-result-and-contract-fixes.md`（静默错算与契约；AR-01 SLA 分数截断、AR-02 SLA NFE 逃逸、AR-03 group-key 碰撞、F4 selection 静默丢弃）→ `active`
  - `2026-08-14-0707-3-nop-metadata-invariant-enforcement-in-default-build.md`（F3 INV-LIMIT 默认 surefire 防御纵深）→ `active`
- **关键事实订正（F3）**：独立审查 fresh session + lead agent 双重核实 live CI —— F3 的 P1 前提"默认 CI 不 guard INV-LIMIT"**不成立**：I5 已通过 `.github/workflows/maven.yml` 的 `invariant-gate` job（push/PR 触发）+ `run-nop-metadata-invariants.sh` step 4 fail-fast 运行 `TestLimitNegativeValueInvariant`。F3 recommendation 的可接受档（route B）已满足。F3 重裁为"残留 = 防御纵深/陈旧配置清理"，仍以 `Fix` 收口（满足非可降级），但范围严格限定。
- **审查闭环**：每个 plan 经独立 fresh session 对抗性审查（含想象性分析）2 轮至共识（无 Blocker / 无 Major）后转 `active`。round 1 修复要点：plan-1 测试例数 25→33（live 核实）、nameResolverFor 调用面 + contained 方案、address-list 完整测试 URL；plan-2 Phase 3 条件性 Targets + Protected Area（`INopMetaTableBiz`）约束 + owner-doc 路径 + week 分支；plan-3 核心前提订正。
- **P2 → backlog**：`ai-dev/backlog/nop-metadata-invariant-loop-roadmap.md` 新增 `## Follow-up Backlog` 段，登记 F5–F19 + AR-05–AR-14（每项标注源审计路径）。
- **doc-link checker**：`check-doc-links.mjs --strict` 报 17 errors 全为**预存**（HEAD 已存在，跨模块/跨仓库引用，如 roadmap:5 的 nop-chaos-flux `docs/backlog/ai-invariant-loop-roadmap.md`）；本次 3 个新 plan 文件**零** broken links，未引入新断链。预存断链属其它模块范围，不在本任务 scope。


### plan I5+I6 nop-metadata 不变式全量验证、门禁提升与 Cycle 1 收口（含 limit L1 收敛）

plan `ai-dev/plans/2026-08-13-1930-5-nop-metadata-invariant-verify-and-cycle-closure.md`（roadmap Cycle 1 / I5+I6）执行完成。Cycle 1 全量收口：4 门禁零命中、门禁提升为阻断式 hard CI gate、聚合入口搭建、稳态判定、独立 closure audit。

- **Phase 0 — limit L1 收敛（path ①，I4 deferred 项落地）**：I4 Phase 4 limit L1（`queryTableData` 对负 limit 静默封顶）按超时机制拆 successor plan，本计划 Phase 1 baseline 预期"4 门禁零命中"与其冲突。核对 MA7.4-03 原始 finding（`ai-dev/audits/2026-08-05-0856-arm-MA7.4-nop-metadata-lineage-performance.md:65`）确认 **MA7.4-03 范围 = "无 limit 默认值/上限：全表拉入内存"（null/大正值），不含负值**——I4 将负值静默封顶归因为"MA7.4-03 裁定"为误归因（负值封顶是 `<= 0` 子句的附带捆绑，非 MA7.4-03 意图）。故采用 path ①：`NopMetaTableBizModel#normalizeQueryLimit` 改为 `limit < 0` 抛 `ERR_PAGINATION_LIMIT_INVALID`（与 `normalizeJoinQueryLimit`/AR-09 先例对齐），保留 MA7.4-03 实际意图（null/0→默认值、大正值→封顶）。**不变式无弱化**（INV-LIMIT 统一适用 4 入口，无例外）；MA7.4-03 回归测试（`testQueryTableDataDefaultLimitCapsUnboundedResult`，null limit）仍 PASS。`TestLimitNegativeValueInvariant` 4 tests 0 failures（queryTableData 由 FAIL 转 PASS）。
  - owner-doc 同步：`docs-for-ai/03-modules/nop-metadata.md` limit 语义段更新（queryTableData 负值统一显式拒绝）；`invariant-catalog.md` INV-LIMIT 陈述更新（移除已过时的"queryTableData 静默封顶"例外例，文档化 path ① 统一收敛）。
- **Phase 1 — 全量验证与门禁零命中记录**：`./mvnw test -pl nop-metadata -T 1C` → **1081 tests, 0 failures**（nop-metadata-service）+ web 1 test 0 failures，BUILD SUCCESS。4 门禁复跑零命中：`check-silent-swallow.mjs` exit 0（130 块 / 0 hit）、`check-orm-unique-key-constraint.mjs` exit 0（37 UK / 0 hit）、`check-sensitive-literal-leak.mjs` exit 0（0 hit）、`TestLimitNegativeValueInvariant` 4 tests 0 failures。与 I1 棘轮零点 81 项比对：**81 → 0 全清确认**（80 silent-swallow + 1 limit 全清）。
  - 注：`-am` 全量构建时 `nop-stream-rocksdb` 出现 1 次 flaky FAILURE（并行资源竞争），单跑 `./mvnw test -pl nop-stream/nop-stream-rocksdb` 83 tests 0 failures PASS——与本次变更无关（本次仅改 nop-metadata-service）。
- **Phase 2 — 门禁聚合入口 + CI Node 环境 + hard-gate 接入**：
  - 新建 `ai-dev/tools/run-nop-metadata-invariants.sh`（bash 包装器，`set -euo pipefail`，4 门禁序列，`MVN="${MVN:-./mvnw}"` 环境变量切换本地/CI Maven）。
  - `.github/workflows/maven.yml` 新增 `invariant-gate` job（`needs: build`）：checkout + JDK 21（limit 测试）+ pnpm@10 + Node 20（3 扫描器，`cache-dependency-path: ai-dev/tools/pnpm-lock.yaml`）+ `pnpm install --frozen-lockfile`（`working-directory: ai-dev/tools`）+ `MVN=mvn ./ai-dev/tools/run-nop-metadata-invariants.sh`。**非零即红**，无 `continue-on-error`。
  - **hard-gate proof（本地 proof (A)）**：`NopMetaTableBizModel.java` 临时注入空 catch → `check-silent-swallow.mjs` 报 131 块 / 1 hit（`:427`）、**exit 1** → 移除 → 130 块 / 0 hit、**exit 0**。git diff 确认临时代码已清除。
  - 新建 `docs-for-ai/02-core-guides/invariant-guards.md`（门禁清单 + 运行命令 + 棘轮规则 + 表完备性说明 + 复触发条件）；更新 `docs-for-ai/INDEX.md` 路由。doc-link checker：新 owner-doc 0 broken links / 0 BOUNDARY errors。
- **Phase 3 — Cycle 1 收口统计与稳态判定**：
  - 全量指标：门禁数 **4**、red list 总数 **81**、修复数 **81**（80 silent-swallow I4 formalize + 1 limit L1 本计划 Phase 0 path ①）、对抗探查新族数 **0**（I2 五方向）。
  - 稳态判定：4 门禁零命中 + I2 对抗探查 0 新族 → **稳态暂停**，等待复触发条件。
  - 候选不变式裁定（基于 `invariant-catalog.md` §候选不变式 初步评估确认）：① 类型/方言兼容性族（AR-20/AR-23⑧）= 单点命中无兄弟复发 → **不沉淀（watch-only，周期复探）**；② 并发竞态/UK 幂等族（P2-MA3-03）= DB UK 已 fail-loud 兜底 → **不沉淀（watch-only，周期复探）**。两者均**不派 Cycle 2 / I1**。
  - 复触发条件登记：CI `invariant-gate` 变红 / 新增或重命名 processor·bizmodel·ORM entity / 周期复探。
  - 独立 fresh session closure audit：见 plan `Closure` 段 evidence。
- 验证：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -T 1C` 1081+1 tests 0 failures；4 门禁零命中（exit 0 / 0 failures）；`checkstyle:check` nop-metadata 0 violations（pre-existing failures 全在 nop-api-core 依赖模块，项目 checkstyle 已禁用）；`check-doc-links --strict` 新 owner-doc 0 errors；`run-nop-metadata-invariants.sh` 一键 exit 0。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md`（limit 语义）、`docs-for-ai/02-core-guides/invariant-guards.md`（新建）、`docs-for-ai/INDEX.md`（路由）、`invariant-catalog.md`（INV-LIMIT 陈述）。


### HG-01 / P1-01-01 人工批准落档 + 两份执行 plan 起草（经两轮独立子 agent 对抗审查）

- **批准记录**：用户 2026-08-14 批准两项人工确认门（"两者都批准"）——① `HG-01` 跨 task side-output 线协议结构性重构（自 2026-08-12 Cycle 1 / I6 起维持 `pending human confirmation`，Cycle 2/3 I6 复核均无批准）；② `P1-01-01` nop-stream-rocksdb 命名空间迁移（2026-08-13 plan `2026-08-13-1243-3` Phase 3 预设路径取消 → successor，本次批准触发执行）。roadmap backlog 两条目已落档批准记录（Status → 已批准 + 执行 plan 引用）。
- **Plan 起草**：
  - `ai-dev/plans/2026-08-14-0900-1-nop-stream-hg01-cross-task-side-output-wire-protocol.md`（4 Phase：设计裁决 → codec/元素类型 → 生产/消费端 → 门禁同步）。范围裁定：`getSideOutput` API / `StreamEdge.outputTag` 图模型消费 / DSL `<sideOutput>` 解封 = **Non-Goals**（另立 successor）；消费面 = task 级 `registerSideOutputConsumer`（既有 API）。
  - `ai-dev/plans/2026-08-14-0900-2-nop-stream-p10101-rocksdb-namespace-migration.md`（3 Phase：main 17 类 → test 13 类 + runtime 7 文件 → 全量验证落档）。纯机械迁移，行为零变更。
- **两轮独立子 agent 审查（fresh session）**：第一轮——HG-01 发现 2 Blocker（`DataPlaneWireSupport.toWireMap:38-47` 逐字段枚举层漏 `outputTagId` → 三 string codec 真实远程路径 tag 静默丢失；`ChannelState.envelopeToMap/mapToEnvelope:215-238` 快照逐字段层漏字段）+ 5 Major（mjs `classifyCollectBody:782-790` 正则不识别 emit 形态会 crash、valueType 错位、`testTimestampedCollectorWrappingRecordWriterOutputFailsFast` 漏翻转、`ResultPartition:198` materialization 双写门未裁决、路由/查找/copy 语义未裁决）+ M6 中间态。P1-01-01 发现 1 Major（grep 判据"全仓零残留"被已完成历史计划卡死，违反 Minimum Rule 20）+ 3 Minor（import 归属行号、FQN 3→4 处、无 install 步骤）。**第二轮复审**——全部修复 PASS；新发现 HG-01 F1（mjs 侧中间态声明缺失，已补）、P1-01-01 Major-1（`TestRocksDBStateBackendE2E:262-263` 另有 2 处 `RocksDBOptionConfig` FQN 未收录，grep 实证 :262-263 确认），均已修复。两份 plan 复审结论：可直接执行。
- **验证**：本轮为计划/文档变更，未改代码——未跑 mvnw。grep/read 实证用于复核审查声称（ChannelState :215-238、DataPlaneWireSupport :38-47、ResultPartition :198、TestRocksDBStateBackendE2E :262-263 均确认属实）。
- **偏差**：无（plan 起草流程按 `00-plan-authoring-and-execution-guide.md` 执行；审查轮数超最少 1 轮 = 两轮，因第一轮发现 Blocker 级缺陷）。
- **Doc-sync**：`ai-dev/backlog/nop-stream-invariant-loop-roadmap.md`（HG-01 + P1-01-01 条目批准落档）；两份新 plan 文件；本 log 条目。`No owner-doc update required`（docs-for-ai/ 无面变更）。
- **Next**：两份 plan 定稿后进入执行（每 Phase 勾选 Exit Criteria，收口跑 Closure Gates + 独立 closure audit）。

# 2026-08-14 开发日志

## 批量面板数据查询 API getDashboardData（plan `2026-08-14-2020-2`）


### W6 closure audit 收口：独立审计 + SMS MFA 全链测试补充 + credential-mfa roadmap 状态同步

- **Plan**：`ai-dev/plans/2026-08-13-1118-1-mfa-user-admin-api-scan-adaptation.md`（nop-credential-mfa W6）
- **做了什么**：
  - **独立 closure audit**（fresh session subagent）：W6 4 Phase Exit Criteria + Closure Gates 逐条对 live code 复核（`NopAuthUserBizModel` 五 action、`ScanLoginResult` 四字段、`loginByScanAsync` 同步 try/catch、BIND_EXPIRED 两分支、恢复码 `salt:hash` 与 W5 `verifyRecoveryCode` 兼容、明文边界）；`./mvnw test -pl nop-auth/nop-auth-service -Dtest='TestMfaUserSelfService,TestScanLoginMfa,TestMfaLoginE2E,TestMfaConfigAndErrors'` 32/32 通过。首轮结论 NOT_READY_TO_CLOSE，阻塞项 3 条。
  - **阻塞项 ①修复 — 补日志**：补写 2026-08-13 日志条目（见 `ai-dev/logs/2026/08-13.md` 对应条目，W6 落地当日遗漏）。
  - **阻塞项 ②修复 — 补测试**：`TestMfaLoginE2E` 新增 `testPasswordLoginMfaSmsFullChain`（bind sms → 密码登录 → `ERR_AUTH_MFA_REQUIRED`(mfaType=sms) → `sendMfaCode` 真实调 `ISmsSender` → `mfaVerify(sms)` → accessToken + challenge 一次性消费）——原 plan Phase 4 声称的该 E2E 无测试覆盖，`mfaVerify` SMS 分支补直接测试。
  - **阻塞项 ③自愈**：独立 audit 证据落档 plan Closure 段。
- **验证**：`./mvnw test -pl nop-auth/nop-auth-service -Dtest=TestMfaLoginE2E -DfailIfNoTests=false` → **10 tests / 0 failures**（含新增 SMS 全链用例）；首轮 audit 32/32 全绿。
- **Doc-sync**：
  - `ai-dev/backlog/nop-credential-mfa-roadmap.md`：W6 `planned` → `done`（独立 closure audit 通过）；W7 gate 注记更新（依赖 W6 live 产物的 Phase 2/3 解锁）；★ Milestone「安全能力一期落地」`todo` → `done`（W1-W8 全部 done，派生状态）。
  - `ai-dev/plans/2026-08-13-1118-1-mfa-user-admin-api-scan-adaptation.md`：Closure 段完成状态更新（NOT_READY → completed，阻塞项处置证据 + 复跑结果）。


### 2026-08-14（mission-driver EXECUTE：plan {3} Phase 2）

执行 plan `ai-dev/plans/nop-datav/2026-08-10-2025-3-service-layer-contract-correctness.md` Phase 2，闭合 audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 **Dim14-02 [P1]**（`ReportDeliveryExecutor.doExecute` 在 `runInNewSession` 内执行同步 SMTP，跨远程调用持有 JDBC 连接，并发 cron 下可致连接池耗尽；且 email 在 SUCCEEDED UPDATE 提交前发出）。

**改动**：

- `ReportDeliveryExecutor`：拆分原 `doExecute`（全程单 session）为：
  - `runDelivery`：编排总入口（`execute` 异步 / `executeSyncForTest` 同步共用）。
  - `runDeliveryInSession`（Part A，session 内）：加载 → grace → running → 校验 → 取数 → 落盘 → **SUCCEEDED 提交**（不再含 sendReport），返回 `PreparedDelivery`（task + delivery 快照）；skipped/failed 返回 null。
  - `sendNotificationOutOfSession`（Part B，session 已关闭）：调 `notificationSender.sendReport`（SMTP）。
  - `recordDeliveredChannels`（新 session，成功回写 deliveredChannels，仅 SUCCEEDED 时）/ `rollbackSucceededToFailed`（新 session，邮件失败强制覆盖 SUCCEEDED → FAILED + task.lastRunStatus 回退）。
  - `PreparedDelivery` 内部 holder（session 内 SUCCEEDED 提交后供 session 外 SMTP 送达用）。
- `execute` / `executeSyncForTest` 均改调 `runDelivery`，两条路径行为一致（同步路径契约更新：返回时邮件已发或回补 FAILED）。
- `MockEmailSender`：扩展 `onSend`（sendEmail 入口回调，供测试观察 SMTP 调用时机的 DB 状态）+ `failOnSend`（模拟 SMTP 失败）；`reset()` 一并清。
- owner-doc：`ReportDeliveryExecutor` 类 javadoc + `schedule-report-design.md §12` ReportDeliveryExecutor 行更新为反映 SMTP 移出 session（§3 scheduler runInNewSession 意图不涉及 executor SMTP，仍与 live 一致）。

**接线验证（Anti-Hollow，非仅函数位置移动）**：

- **sendReport 运行时确于 session 外调用**：`testSendReportHappensAfterSucceededCommit` 在 sendEmail 时机读取该交付记录已提交状态，断言 == **SUCCEEDED**（旧代码 sendReport 在 setStatus(SUCCEEDED) 之前，此处会读到 RUNNING/PENDING），且 deliveredChannels 此时为 null（邮件成功后才回写）。
- **邮件失败回补 FAILED**：`testNotificationFailureRollsBackSucceededToFailed` 注入 sendEmail 抛错，断言终态 **FAILED**（SUCCEEDED 已先提交，被强制覆盖）+ errorMsg 含 "notification" + task.lastRunStatus 回退 failed + 无成功发送记录。消除「邮件已发但记录 RUNNING/SUCCEEDED」窗口。

**验证**：

- `./mvnw test -pl nop-datav -T 1C`：BUILD SUCCESS，**nop-datav-service 398 tests, 0 failures, 0 errors**（含 TestNopDatavReportE2E 15 例：原 13 + Dim14-02 新增 2）。
- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- 既有报告交付 E2E 全绿，未因 session 拆分回归（主线、grace/skipped、5 条 FAILED 路径、恢复、scheduler 吞错）。

**Follow-up**：Phase 1/2 均完成，下一步收口 plan（Plan Status → completed，闭合 audit Dim09-01/Dim14-02）。

---

## nop-datav Plan {3} Service-Layer Contract Correctness：Phase 1（Dim09-01 面板查询错误策略）


### 2026-08-14（mission-driver EXECUTE：plan {3} Phase 1）

执行 plan `ai-dev/plans/nop-datav/2026-08-10-2025-3-service-layer-contract-correctness.md` Phase 1，闭合 audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 **Dim09-01 [P1]**（`PanelParamEvaluator.evaluate` 4 处裸 `IllegalArgumentException` 直穿公共 `getPanelData` GraphQL action，破坏两层错误策略与错误码可观测性）。

**改动**：

- `PanelParamEvaluator.java`：4 处裸 `IllegalArgumentException`（invalid JSON / 非 object / rule 非 object / 缺 source）→ `NopException(ERR_DATAV_INVALID_PARAM_CONFIG).param(ARG_REASON, ...).cause(e)`（invalid-JSON 保留 cause 链）。静态工具层无 panelId 上下文，源点不填 panelId。
- `PanelDataBinder.queryPanelData`（:131）：`evaluate(...)` 外包 `try/catch(NopException ne) { throw ne.param(ARG_PANEL_ID, panelId); }`，补 panelId 后 rethrow（catch 紧贴 evaluate，仍在 executeQuery try 之外）。
- `NopDatavErrors.ERR_DATAV_INVALID_PARAM_CONFIG`：params 声明追加 `ARG_PANEL_ID`。
- **裁定（与 plan 字面建议的偏差）**：message 模板未硬编码 "for panel:{panelId}"，改为中性 `"Invalid param config: {reason}"`。原因：该 ErrorCode 被 `PanelParamEvaluator`（panel paramMapping）、`DashboardParamParser`（dashboard paramConfig，经 `NopDatavDashboardBizModel` 公共 action）、`DashboardFilterUrlCodec/Resolver`（filter）**共用**；硬编码 panel 名词会误标看板/filter 错误（`getDescription()` 用户可见，`{panelId}` 在看板路径为 null → 渲染空）。panelId 仍作为结构化 param（`getParam("panelId")`）供 GraphQL 错误响应/聚合，满足 plan 核心 goal「panelId 上下文保留」。

**测试**：

- 新增 `TestPanelParamErrorContract`（5 例）：4 类配置错误经公共入口 `PanelDataBinder.queryPanelData` 断言 `NopException` + `errorCode == ERR_DATAV_INVALID_PARAM_CONFIG` + `param(ARG_PANEL_ID)==期望` + `param(ARG_REASON)` 非空；invalid-JSON 断言 cause 非空，其余 3 类校验失败无底层异常 cause 为 null（非吞异常）。+ 1 回归保护（合法 paramMapping 正常）。

**验证**：

- `./mvnw install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav -T 1C`：BUILD SUCCESS，**nop-datav-service 396 tests, 0 failures, 0 errors**。
- owner-doc `error-handling.md` 两层策略与本修复一致（公共 API 用 NopException+ErrorCode），无矛盾条款 → No owner-doc update required。

**Follow-up**：Phase 2（Dim14-02 报告交付 SMTP 移出 session）待执行。

---

## nop-datav Plan {2} Export Task State-Machine Correctness：closure audit 闭合


### 2026-08-14（mission-driver EXECUTE：plan {2} closure）

执行 plan `ai-dev/plans/nop-datav/2026-08-10-2025-2-export-task-state-machine-correctness.md` 的收口审计（3 个 Phase 此前均已 completed 并全 `[x]`，本次为 Closure Gates 验证 + plan 关闭），闭合 audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 Dim14-01 [P1]（per-request 恢复误杀在途任务）、Dim07-01 [P1]（cancel 被 SUCCEEDED 覆盖）、Dim16-01 [P1]（cancel 测试为空壳）。

- **Plan Status**：从 active → **completed**。3 个 Phase 全 completed（此前执行）；Closure Gates 11 项全部 `[x]`。
- **独立 closure audit**：fresh subagent（ses_0020b9702ffeTV10AMRkekveTG）8/8 gates PASS，live-code evidence 写入 plan `Closure` 段。

**本次收口验证（live code 追踪，非计划声称）**：

- **Dim14-01**：`NopDatavExportTaskBizModel.createExportTask`（:126-157）无 `recoverInterruptedTasks()` 请求路径调用（仅 :146 注释）；恢复仅由 `NopDatavExportTaskRecovery @PostConstruct init()`（:54-61）启动期执行；public 委托保留于 BizModel:477-479（测试/管理入口，非请求路径）。
- **Dim07-01**：`PanelDataExporter` 经 `BooleanSupplier cancelChecker` 在面板间(:174)/取数后写出前(:117,:182)/写出循环内(writeCsv:209,toSheet:272)真实 `checkCancelled`，命中抛 `ERR_DATAV_EXPORT_FAILED`（:83，退役 backlog Dim09-04 死码）。`executeTask` pre-SUCCEEDED 复检(:264，早于 :269)+ catch 分流(:278，早于 :283 setStatus(FAILED))→ cancel 命中不写 FAILED/SUCCEEDED，终态保留 cancel 线程的 CANCELLED。三层防护（exporter checkpoint + pre-SUCCEEDED 复检 + ORM versionProp 锁 `_app.orm.xml:476`）。
- **Dim16-01**：`testCancelDuringExecutionEndsInCancelled`（TestNopDatavExportE2E:313-379）用 createExportTask 真实 submitExecution（非 seedTask），D4 seam 双 CountDownLatch 确定性 RUNNING 窗口，断言终态 CANCELLED + fileRecordId null + errorMsg 含 cancel。`testCreateExportTaskDoesNotKillInFlightTasks`(:424) Dim14-01 回归。

**本次改动（仅文档漂移修复 + plan 关闭）**：

- `NopDatavExportTaskRecovery.java` javadoc 漂移修复：旧注释声称「BizModel 在每次发起导出前也会调用一次」，Phase 1 已删除该调用，更新为反映当前行为（恢复仅 @PostConstruct）。
- plan `Closure` 段写入 evidence，Closure Gates 11 项 `[x]`，Plan Status → completed。

**验证**：

- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS。
- `./mvnw test -pl nop-datav/nop-datav-service`：BUILD SUCCESS，**391 tests, 0 failures, 0 errors, 0 skipped**。
- 注：`-pl nop-datav -am` 全链路时上游 `nop-auth-service` 有 2 个预存失败（TestBeanLoader bean XML 格式 / TestChannelScanBindLoginE2E VarCollector NPE，平台级，与本 plan 改动无关），nop-datav-service 单模块 391 全绿。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：exit 0，0 unchecked。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：1 high 发现（NotificationSender.java:311）经审查为 **FALSE POSITIVE**（P6b 正则误匹配中文注释 "temp resource"，该处为完整实现，且属 D5 reports 范围，非本 plan 改动文件）。

**关于 audit 关闭**：audit `2026-08-10-1516-multi-audit-nop-datav.md` 的 Remediation 含 3 个 plan（{1}✅、{2}✅ 本次、{3} pending Dim09-01/Dim14-02），故 audit `Audit Status: planned` 暂不关闭，待 Plan {3} 完成后一并关闭。

**Follow-up**：
- no remaining plan-owned work（plan {2} 关闭）。
