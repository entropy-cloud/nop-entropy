# 2026-05-11

## Plan 14: nop-job 质量优化 — completed

- Plan: `ai-dev/plans/14-nop-job-quality-fixes.md`
- TimeoutChecker N+1 修复：`scanOnce()` 改为两步批量预加载（fires → schedules），SQL 查询从 2N 降到最多 2
- Planner 锁冲突可观测性：新增 `IJobPlannerMetrics` + `nop.job.planner.due-count` / `nop.job.planner.lock-conflict` Counter，注入 `JobPlannerScannerImpl`
- `JobCoreErrors` 新增 5 个 ErrorCode 替代硬编码字符串
- `LocalJobScheduler` + `IJobScheduler` 标记 @Deprecated（未删除，因测试仍存在）
- 验证：`./mvnw test -pl nop-job` 通过
