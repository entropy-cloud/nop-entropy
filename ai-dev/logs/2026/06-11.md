# 2026-06-11

## Plan 135: AgentEventPublisher 事件流 (L1-9)

**Plan**: `ai-dev/plans/135-nop-ai-agent-event-publisher.md`
**Work Item**: L1-9
**Status**: completed

### 变更摘要

实现 Agent 执行事件发布机制，使外部调用者能订阅执行过程中的事件（工具调用、LLM 响应、最终结果、错误等）。

### 新增文件

- `AgentEventType.java` — 7 个事件类型枚举
- `AgentEvent.java` — 事件数据类（type, sessionId, agentName, timestamp, payload, error）
- `IAgentEventSubscriber.java` — 事件订阅者接口
- `IAgentEventPublisher.java` — 事件发布者接口（publish, addSubscriber, removeSubscriber）
- `DefaultAgentEventPublisher.java` — 内存实现，CopyOnWriteArrayList，subscriber 异常不阻断
- `TestAgentEventPublisher.java` — 9 个单元测试

### 修改文件

- `ReActAgentExecutor.java` — 新增三参数构造函数（IAgentEventPublisher），6 个关键执行点发布事件
- `DefaultAgentEngine.java` — 创建 DefaultAgentEventPublisher 实例，暴露 getEventPublisher()，注入到 executor

### 测试

- `./mvnw test -pl nop-ai/nop-ai-agent -am`: BUILD SUCCESS, 49 tests (0 failures)
- 9 个新测试覆盖：发布通知、多订阅者、移除订阅者、异常隔离、executor 集成（成功/工具调用/失败）、engine 端到端、null publisher 兼容

### Doc-sync

- 更新 `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` L1-9: ❌ → ✅
- No docs-for-ai update required

---

## Security Audit Readiness Analysis (Ad-hoc Analysis)

**Scope**: `ai-dev/design/nop-ai-agent/nop-ai-agent-security-and-permissions.md` 
**Output**: `ai-dev/design/nop-ai-agent/nop-ai-agent-security-audit-readiness-analysis.md`

### 分析内容

对安全模型的 4 层接口（Layer 1-4）进行渐进式设计审计就绪度分析，评估每个接口是否符合 `00-vision.md` 约束 #4（渐进式增强）。

### 关键发现

1. **Critical**: IAuditLogger 归属 Layer 4，但安全决策在 Layer 1 已发生——结构性审计缺口
2. **Critical**: 安全接口与执行模型 Hook 的集成点未在任何文档中定义
3. **Significant**: Hardcoded deny 机制违反渐进式原则（不可通过 XDSL 覆盖）
4. **Significant**: trustedSource 评估无接口定义
5. 整体审计就绪度评分：5/10

### 验证

- 无代码改动，纯设计文档分析
- 建议在安全接口实现前修复 R1（审计层归属）和 R2（Hook 集成点）

### Doc-sync

- 新建 `ai-dev/design/nop-ai-agent/nop-ai-agent-security-audit-readiness-analysis.md`
- No docs-for-ai update required（分析报告在 ai-dev/ 目录）

---

## Plan 134: IAgentEngine Actor 消息入口 + DefaultAgentEngine

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/134-nop-ai-agent-engine-actor-entry.md`

### 改动内容

1. **`IAgentEngine.java`**（新建）：Agent 引擎接口
   - `AgentMessageAck sendMessage(AgentMessageRequest request)` — Actor 消息入口，立即返回 ack
   - `CompletableFuture<AgentExecutionResult> execute(AgentMessageRequest request)` — 便利方法，返回执行结果的 future

2. **`AgentMessageRequest.java`**（新建）：请求封装
   - 字段：agentName (必填), userMessage (必填), sessionId (nullable), metadata (Map, nullable)

3. **`AgentMessageAck.java`**（新建）：确认响应封装
   - 字段：sessionId (必填), status (默认 "accepted")

4. **`AgentExecutionResult.java`**（扩展）：添加 `sessionId` 字段
   - 新增构造函数接受 sessionId 参数
   - `fromContext()` 从 `ctx.getSessionId()` 复制 sessionId

5. **`NopAiAgentException.java`**（新建）：模块级异常类（英文消息）

6. **`DefaultAgentEngine.java`**（新建）：默认引擎实现
   - 构造函数接受 `IChatService` 和 `IToolManager`
   - `execute()` 通过 VFS 组件管理器加载 AgentModel，创建 context，`CompletableFuture.supplyAsync()` 包装 ReActAgentExecutor 异步执行
   - `sendMessage()` 委托 execute()，附加 `exceptionally()` 异常处理器，立即返回 ack
   - sessionId 生成使用 UUID，已有 sessionId 直接复用

7. **`TestDefaultAgentEngine.java`**（新建）：7 个单元测试
   - testSendMessageReturnsAck, testExecuteReturnsResult, testNewSessionGeneration
   - testExistingSessionReuse, testAgentModelLoadingFromDsl, testAgentModelNotFound, testSendMessageAsyncExecution

### 文档更新

- `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` L1-1 ❌→✅
- `ai-dev/design/nop-ai-agent/nop-ai-agent-react-engine.md` §3.2 添加 `execute()` 便利方法说明

### 测试

- `./mvnw test -pl nop-ai/nop-ai-agent -am`: 40 tests, 0 failures, BUILD SUCCESS

---

## Plan 133: ReActAgentExecutor — ReAct 循环核心实现

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/133-nop-ai-agent-react-executor.md`

### 改动内容

1. **`ReActAgentExecutor.java`**（新建）：实现 `IAgentExecutor`，完成标准 ReAct 循环
   - 构造函数接受 `IChatService` 和 `IToolManager`
   - 循环流程：构建请求 → 调用 LLM → 检查工具调用 → 执行工具 → 回灌结果 → 继续推理
   - 结束条件：无工具调用 → completed，达到 maxIterations → completed，LLM 错误 → failed，异常 → failed
   - ChatToolCall → AiToolCall 桥接（name→toolName, arguments→JSON input, 保留原始引用）
   - AiToolCallResult → ChatToolResponseMessage 桥接（成功用 fromToolCall, 失败用 error factory）
   - ChatOptionsModel → ChatOptions 转换 + 附加 tools/toolChoice
   - 工具定义构建：AgentModel.tools → loadTool → ChatToolDefinition（含 schema 转换）

2. **`SimpleToolExecuteContext.java`**（新建）：实现 `IToolExecuteContext`
   - workDir 必选，IToolFileSystem/IThreadPoolExecutor 可选（返回 null）

3. **`ToolSchemaConverter.java`**（新建）：XNode → JSON Schema Map 转换
   - 遍历 XNode 属性和子节点，构建 type/properties/required 结构
   - schema 为 null 或转换失败时返回 null

4. **`TestReActAgentExecutor.java`**（新建）：6 个单元测试
   - testNoToolCallImmediateReturn：无工具调用直接完成
   - testSingleToolCallLoop：单工具调用循环
   - testMultipleToolCalls：多个并行工具调用
   - testMaxIterationsReached：迭代上限终止
   - testLlmCallFailure：LLM 调用失败
   - testToolExecutionError：工具执行错误回灌

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS（33 测试全部通过）
- roadmap L1-5 状态更新为 ✅

---

## Plan 132: IAgentExecutor 执行策略接口

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/132-nop-ai-agent-executor-interface.md`

### 改动内容

1. **`IAgentExecutor.java`**（新建）：Agent 执行策略接口
   - 唯一方法 `CompletionStage<AgentExecutionResult> execute(AgentExecutionContext ctx)`
   - Javadoc 描述职责契约：策略接口，不持有配置，从上下文读取
   - 签名与设计文档 `nop-ai-agent-react-engine.md` §3.1 一致

2. **`TestIAgentExecutor.java`**（新建）：3 个测试
   - 测试 mock executor 返回 fromContext 结果并验证数据一致
   - 测试 CompletionStage 异步正常完成
   - 测试 lambda 实现

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS（3 测试全部通过）
- roadmap L1-3 状态更新为 ✅

---

## Plan 131: AgentExecutionContext 执行上下文数据对象

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/131-nop-ai-agent-execution-context.md`

### 改动内容

1. **`AgentExecutionContext.java`**（新建）：Agent 执行引擎核心数据载体
   - 字段：agentModel, messages, plan, sessionId, chatOptionsModel, currentIteration, tokensUsed, status, maxIterations, metadata, lastError, startTimeMs
   - 工厂方法 `create(AgentModel, String)` 从 AgentModel 提取 maxIterations（null→10）和 chatOptionsModel

2. **`AgentExecutionResult.java`**（新建）：不可变执行结果快照
   - 字段：status, finalMessage, messages, totalIterations, totalTokensUsed, durationMs, error
   - 工厂方法 `fromContext(AgentExecutionContext)` 做 defensive copy

3. **`TestAgentExecutionContext.java`**（新建）：8 个测试覆盖构造、字段读写、默认值、工厂方法

4. **`TestAgentExecutionResult.java`**（新建）：7 个测试覆盖快照生成、defensive copy、不可变性

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS
- roadmap L1-2 状态更新为 ✅

---

## Goal Driver 流程重构

**Scope**: `ai-dev/tools/opencode-goal-driver/`
**Design**: `ai-dev/design/opencode-goal-driver/goal-driver-flow-design.md`

### 改动内容

1. **`flows/goal-driver.json`**：重写顶层流程
   - 移除 `DETECT_START`（死代码，所有 transition 指向同一节点）
   - `HEALTH_CHECK` 从 tool step 改为 AI agent step，失败时支持 3 次无上下文重试
   - 新增 `PLAN_ROUTER` script 步骤，扫描活跃 plan 并路由到执行或 roadmap
   - 主流程中 `EXECUTE_PLAN` 和 `DEEP_AUDIT_LOOP` 改为子流程引用

2. **`flows/plan-execution.json`**（新建）：可复用的 plan 执行子流程
   - `EXECUTE` → `CLOSURE_VERIFY`（group: `SCRIPT_CHECK` + `AI_AUDIT`）→ `BUILD_VERIFY`
   - 各步骤失败时可回退重试

3. **`flows/deep-audit-loop.json`**（新建）：深度审计子流程
   - 自循环：`DEEP_AUDIT` → `DRAFT_PLAN` → `AUDIT_PLAN` → `EXECUTE_PLAN` → `DRAFT_PLAN`（下一个问题）→ ... → `DEEP_AUDIT`（再审计）
   - 直到 `DEEP_AUDIT` 返回 `clean`

4. **`src/flow-loader.js`**：
   - 移除 `detectStartPhase` 和 `execSync` 依赖
   - 新增 `planRouter` 脚本函数（同步文件扫描，无子进程）
   - `closureScriptCheck` 改为直接 import `inspectPlan()` 而非子进程

5. **`prompts/health-check.md`**（新建）：AI agent 可诊断修复编译/测试失败

6. **`ai-dev/design/opencode-goal-driver/`**：
   - 新建 `README.md`（目录索引 + 阅读顺序）
   - 新建 `goal-driver-flow-design.md`（流程设计说明 + Mermaid 图 + 决策理由）

### 关键决策

- `PLAN_ROUTER` 只返回 `execute` 或 `roadmap`，不区分 `audit` 分支 —— roadmap 完成后自然进入 audit
- `EXECUTE_PLAN` 的 `flowArgs` 传递 `PLAN_FILE`，确保子流程可访问当前 plan 路径
- `HEALTH_CHECK` 的 retry 使用 `retry`（无 `append`），保证每次重试是空上下文
- `SCRIPT_CHECK` 直接从 `check-plan-checklist.mjs` 动态 import `inspectPlan()`，不再 spawn 子进程

### 测试修复

3 个集成测试因 flow 结构变化（移除 `DETECT_START`/`FIX_BUILD`、入口改为 `HEALTH_CHECK`、引入子流程）而失败，已修复：

1. **"completes full dev loop + audit loop"**：注入 mock subflows，移除旧引用
2. **"handles execute entry via PLAN_ROUTER"**：替代 `DETECT_START` 测试，计数器防循环
3. **"retries HEALTH_CHECK on failure"**：替代 `FIX_BUILD` 测试

另发现 `DEEP_AUDIT_LOOP` transition 名 `"completed"` 与 engine 子流程返回的 `"complete"` 不匹配，已修正。

### 子流程 marker 透传修复

子流程结果原本丢失了内部最后一步的 marker（硬编码为 `"complete"`/`"failed"`），修复后实际 marker 被透传给父步骤：

1. **`_result(status, stepCount, marker)`**：新增 `marker` 参数，记录最后一步的实际 marker
2. **`_executeSubflowStep()`**：优先使用 `childResult.marker` 而非硬编码
3. **主循环**：`transition.done` 路径将 `result.marker` 传入 `_result()`
4. **级联更新** flow JSON 的 subflow 步骤 transitions 使用实际 marker 名（`"pass"`/`"clean"` 替代 `"complete"`），测试 mock 同步更新

54/54 测试通过。

### 验证

- 无代码改动，流程逻辑通过静态验证（transition 一致性、子流程引用正确、flowArgs 传递链完整）

## Plan 142: Status Enums Unification Confirmation (L1-11)

**Scope**: `nop-ai/nop-ai-agent/` (audit only, no code changes)
**Plan**: `ai-dev/plans/142-nop-ai-agent-status-enums-unification.md`

### 改动内容

1. **审计**: `grep AgentTaskStatus|AgentPlanStatus` → 零命中，确认无需独立枚举
2. **审计**: `grep AgentExecStatus` → 覆盖所有预期文件（AgentExecStatus.java, ReActExecutor.java, AgentExecutionContext.java, AgentSession.java, AgentResultEvent.java, AgentExecutionResult.java + _gen/ 下 5 个文件 + record-mappings）
3. **决策**: 4 值 (pending/running/completed/failed) 满足 Layer 1 需求，无需新增 cancelled 等值
4. **Roadmap 更新**: L1-11 从 ❌ 改为 ✅，附说明文字

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS

## Plan 141: IToolCallRepairer + NoOpRepairer Pass-Through (L2-1)

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/141-nop-ai-agent-tool-call-repairer.md`

### 改动内容

1. **`IToolCallRepairer.java`**: 新增接口，`repair(AiToolCall, AiToolCallResult, AgentExecutionContext): AiToolCallResult`
2. **`NoOpRepairer.java`**: pass-through 实现，直接返回输入 errorResult 不变
3. **`AgentExecutionContext.java`**: 新增 `toolCallRepairer` 字段 + getter/setter
4. **`ReActExecutor.java`**: `executeToolCalls()` catch 块中检查 repairer，非空时调用 `repair()` 并用返回结果替代 errorResult；事件体使用 repaired result 的 output/error body
5. **`TestNoOpRepairer.java`**: 2 个测试（null error, non-null error）
6. **`TestReActExecutor.java`**: 2 个新测试（repairer invoked → called, repairer null → original behavior）

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS, Tests run: 179, Failures: 0

## Plan 140: IPathAccessChecker Path Access Check (L1-8)

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/140-nop-ai-agent-path-access-checker.md`

### 改动内容

1. **`PathAccessLevel.java`**: 新增枚举，`READ`, `READ_WRITE`, `DENY`
2. **`PathAccessResult.java`**: 不可变结果类，持有 `PathAccessLevel` + `reason`，工厂方法 `allowRead()`, `allowReadWrite()`, `deny(String)`
3. **`IPathAccessChecker.java`**: 新增接口，`checkPathAccess(String path, AgentExecutionContext ctx): PathAccessResult`
4. **`DefaultPathAccessChecker.java`**: 默认实现 — 路径规范化（`..` 穿越、符号链接检测）、敏感路径 deny（SSH/AWS/Azure/gcloud/kube/.env/netrc/history）、系统路径 deny（/etc//boot//sys//proc//root）、.nop read-only + .nop/.permissions deny、fail-closed
5. **`AgentExecutionContext.java`**: 新增 `pathAccessChecker` 字段 + getter/setter
6. **`DefaultAgentEngine.java`**: 装配 `DefaultPathAccessChecker`
7. **`TestDefaultPathAccessChecker.java`**: 32 个单元测试
8. **`TestPathAccessCheckerIntegration.java`**: 4 个集成测试

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS, 175 tests, 0 failures

## Plan 139: IToolAccessChecker Tool Access Check (L1-7)

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/139-nop-ai-agent-tool-access-checker.md`

### 改动内容

1. **`IToolAccessChecker.java`**: 新增接口，定义 `checkAccess(String toolName, AgentExecutionContext ctx): ToolAccessResult`
2. **`ToolAccessResult.java`**: 不可变结果类，`allowed` (boolean) + `reason` (String)，工厂方法 `allow()` / `deny(String)`
3. **`DefaultToolAccessChecker.java`**: 3-phase check 实现——hardcoded deny list → glob-enhanced DSL permission matching → fail-closed
4. **`AgentExecutionContext.java`**: 新增 `toolAccessChecker` 字段 (nullable, getter/setter)
5. **`ReActExecutor.isToolDenied()`**: 先检查 `IToolAccessChecker`，再检查 `IPermissionProvider`（defense-in-depth）
6. **`DefaultAgentEngine.java`**: 自动装配 `DefaultToolAccessChecker` 和 `HierarchicalPermissionProvider`
7. **`TestDefaultToolAccessChecker.java`**: 14 个单元测试
8. **`TestToolAccessCheckerIntegration.java`**: 7 个集成测试

### 验证

`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS, 139 tests, 0 failures

## Plan 138: AgentSession 基础会话对象

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/session/`, `AgentExecutionContext`, `DefaultAgentEngine`
**Plan**: `ai-dev/plans/138-nop-ai-agent-session.md`

### 改动内容

1. **`AgentSession.java`**: 新增数据类，持有跨请求状态（sessionId, agentName, messages, tokensUsed, metadata, status, createdAt, updatedAt）
2. **`ISessionStore.java`**: 新增接口，定义 save/load/delete/list 四方法
3. **`InMemorySessionStore.java`**: ConcurrentHashMap-backed 默认实现
4. **`AgentExecutionContext.java`**: 新增 `fromSession(AgentModel, AgentSession)` 工厂方法，从 session 恢复 messages/tokensUsed/metadata
5. **`DefaultAgentEngine.java`**: 集成 session 恢复/保存——新增两参构造函数 `DefaultAgentEngine(executor, sessionStore)`，`execute()` 在执行前 load session 恢复上下文，执行后 sync+save session（try-finally）；向后兼容构造函数自动初始化 InMemorySessionStore
6. **`TestAgentSession.java`**: 4 个测试（construction, field accessors, message append, touchUpdatedAt）
7. **`TestInMemorySessionStore.java`**: 8 个测试（save/load, delete, list, overwrite, null handling）
8. **`TestAgentExecutionContext.java`**: 7 个新测试验证 fromSession（messages/tokens/metadata 恢复, transient 重置, effectiveOptions/maxIterations 从 model）
9. **`TestDefaultAgentEngineSession.java`**: 11 个测试（首次创建 session, 二次恢复历史, 失败保存, metadata 合并, null store fail-fast, token 持久化, agentName 从 session 恢复, 端到端路径, 向后兼容）

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 118 tests, 0 failures, BUILD SUCCESS

## Plan 137: IPermissionProvider Core Security Layer

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/137-nop-ai-agent-permission-provider.md`

### 改动内容

1. **`PermissionResult.java`**: 新增枚举 (ALLOW, DENY, UNMATCHED)
2. **`IPermissionProvider.java`**: 新增接口 `checkToolPermission(toolName, ctx)`
3. **`HierarchicalPermissionProvider.java`**: 默认实现，deny-first 3-source merge（session overrides → DSL permissions → tools set defaults）
4. **`AgentToolDeniedEvent.java`**: 新增事件类 (toolName, reason)
5. **`AgentEventType.java`**: 新增 `TOOL_DENIED` 枚举值
6. **`AgentExecutionContext.java`**: 新增 `permissionProvider` 字段 + getter/setter
7. **`ReActExecutor.java`**: 工具执行前增加权限检查，DENY/UNMATCHED 时跳过执行并返回拒绝响应 + 发布 TOOL_DENIED 事件
8. **`DefaultAgentEngine.java`**: 自动装配 `HierarchicalPermissionProvider` 作为默认权限提供者
9. **`TestHierarchicalPermissionProvider.java`**: 10 个单元测试覆盖 deny/allow/unmatched/default 场景
10. **`TestReActExecutor.java`**: 3 个新集成测试（tool denied, tool allowed, null permission provider）

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 88 tests, BUILD SUCCESS

## Plan 136: nop-ai-agent End-to-End ReAct Validation

**Scope**: `nop-ai/nop-ai-agent/src/test/java/io/nop/ai/agent/engine/TestReActE2EIntegration.java`
**Plan**: `ai-dev/plans/136-nop-ai-agent-e2e-react-validation.md`

### 改动内容

1. **`TestReActE2EIntegration.java`**: 新增端到端集成测试类，3 个测试用例：
   - `testE2E_SingleToolCallRound`: 验证完整工具调用链路（DefaultAgentEngine → ReActExecutor → stub LLM 返回 tool call → stub tool 执行 → LLM 返回最终文本），验证事件序列 TOOL_CALL_START → TOOL_CALL_COMPLETE → AGENT_RESULT，验证系统提示词从 XML 加载，验证工具响应消息存在
   - `testE2E_SingleTurnCompletion`: 验证单轮对话（无工具调用），验证迭代次数 = 1，验证系统提示词从 XML 加载
   - `testE2E_SystemPromptFromAgentXml`: 验证 test-agent.agent.xml 中的 "You are a helpful assistant." 作为第一条消息（系统消息）出现

### 关键设计决策

- 使用 `TestableEngine`（继承 `DefaultAgentEngine`）覆写 `execute()` 方法，保留真实 `loadModel()` 加载 XML，同时接入事件捕获
- StubChatService 和 StubToolManager 作为内部类定义（与其他测试文件一致的模式）
- `test-agent.agent.xml` 无需修改，现有配置足以支撑 e2e 测试场景
- 无生产代码变更，纯测试文件

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS

## Plan 135: AgentEventPublisher 事件流实现

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/135-nop-ai-agent-event-publisher.md`

### 改动内容

1. **`AgentEventType.java`**: 枚举定义 7 种事件类型：TEXT_CHUNK, THINKING_CHUNK, TOOL_CALL_START, TOOL_CALL_COMPLETE, AGENT_RESULT, AGENT_ERROR, AGENT_INTERRUPTED
2. **`AgentEvent.java`**: 抽象基类，包含 sessionId、timestamp、eventType 字段
3. **7 个事件子类型**: AgentTextChunkEvent, AgentThinkingChunkEvent, AgentToolCallStartEvent, AgentToolCallCompleteEvent, AgentResultEvent, AgentErrorEvent, AgentInterruptedEvent
4. **`IAgentEventListener.java`**: @FunctionalInterface，方法 `void onEvent(AgentEvent event)`
5. **`AgentEventPublisher.java`**: 接口定义 subscribe/unsubscribe/publish 方法
6. **`DefaultAgentEventPublisher.java`**: 基于 CopyOnWriteArrayList 的线程安全实现
7. **`AgentExecutionContext.java`**: 新增 eventPublisher 字段及 getter/setter
8. **`DefaultAgentEngine.java`**: execute() 中创建 DefaultAgentEventPublisher 并设置到 context；执行完成后发布 AgentResultEvent
9. **`ReActExecutor.java`**: doExecute() 中在 LLM 响应包含文本时发布 TextChunkEvent；executeToolCalls() 中发布 ToolCallStartEvent 和 ToolCallCompleteEvent
10. **`TestAgentEventPublisher.java`**: 7 个测试覆盖 subscribe/unsubscribe/publish/多 listener/空 listener
11. **`TestAgentEvents.java`**: 10 个测试验证所有 7 种事件类型构造和字段
12. **`TestAgentEventE2E.java`**: 1 个端到端测试验证完整事件序列 [ToolCallStart, ToolCallComplete, AgentResult]
13. **TestReActExecutor.java**: 新增 5 个事件相关测试
14. **TestDefaultAgentEngine.java**: 新增 3 个事件相关测试

### 关键设计决策

- ThinkingChunkEvent 和 AgentInterruptedEvent 类型已定义但不发布（ReActExecutor 暂不解析 thinking 内容，中断机制属于 L2+ 范畴）
- DefaultAgentEngine 总是创建 DefaultAgentEventPublisher 实例，ReActExecutor 中不需要 null 检查
- 事件发布不修改执行状态（只读 context，写入事件流）

### 验证

- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` — BUILD SUCCESS (72 tests, 0 failures)

### Doc-sync

- `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` — L1-9 状态从 ❌ 改为 ✅

---

## Plan 134: IAgentEngine Actor 消息入口

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/134-nop-ai-agent-engine-interface.md`

### 改动内容

1. **`IAgentEngine.java`**: 接口定义，单方法 `AgentExecutionResult execute(AgentMessageRequest request)`
2. **`AgentMessageRequest.java`**: `@DataBean` DTO，含 sessionId（nullable）、agentName、userMessage、metadata
3. **`DefaultAgentEngine.java`**: 实现 `IAgentEngine`：`@Inject` 构造器接受 `IAgentExecutor`，`protected loadModel()` 通过 `ResourceComponentManager` 加载 `.agent.xml`，同步执行流程（resolve sessionId → loadModel → createContext → setMetadata → appendUserMessage → executor.join → returnResult）
4. **`TestDefaultAgentEngine.java`**: 7 个单元测试（successful execution, executor failure, session creation, session reuse, user message appended, metadata propagation, model not found），使用 Testable 子类 override loadModel
5. **`TestDefaultAgentEngineIntegration.java`**: 2 个集成测试（end-to-end model loading, model prompt loaded），使用 CoreInitialization 加载真实 `/test-agent.agent.xml`

### 关键设计决策
- Layer 1 采用同步 `execute()` 返回 `AgentExecutionResult`（而非异步 `sendMessage` 返回 `AgentMessageAck`），因为异步 Actor dispatch 依赖 L1-9 AgentEventPublisher
- `loadModel()` 为 protected 方法，允许测试子类 override 而无需 static mocking

### 验证
- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` — BUILD SUCCESS (7 unit tests + 2 integration tests + all existing tests pass)

### Doc-sync
- `ai-dev/design/nop-ai-agent/nop-ai-agent-react-engine.md` §3.2 更新为同步接口设计，保留异步演进路径
- `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` — L1-1 状态从 ❌ 改为 ✅，§2.1 新增 IAgentEngine 行

---

## Plan 133: ReActExecutor ReAct 循环核心实现

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/133-nop-ai-agent-react-executor.md`

### 改动内容

1. **`ChatOptionsBuilder.java`**: 静态工具类，将 `ChatOptionsModel` 的 7 个公共字段（provider, model, temperature, topP, topK, maxTokens, stop）映射到 `ChatOptions`，null-safe
2. **`ToolCallConverter.java`**: ChatToolCall → AiToolCall 转换器（name→toolName, arguments→JSON string input, String id→int id with fallback to sequential counter），AiToolCallResult → ChatToolResponseMessage 转换器（success→text, failure→error）
3. **`SimpleToolExecuteContext.java`**: 最小 `IToolExecuteContext` 实现，提供 SyncThreadPoolExecutor.INSTANCE 作为 executor 避免 NPE
4. **`ReActExecutor.java`**: 实现 `IAgentExecutor` 的 ReAct 循环：构建 ChatRequest → 调用 LLM → 提取 tool calls → 执行工具 → 追加 tool response → 重复。处理 4 种终止条件（无 tool calls, maxIterations 超限, LLM 错误, 取消）
5. **`TestReActExecutor.java`**: 13 个 JUnit 5 测试覆盖 single-turn、one-tool-round、multi-iteration、maxIterations、LLM-error、tool-error、tool-conversion、non-numeric-id、system-prompt、ChatOptionsBuilder、null-model、token-accumulation

### 关键设计决策
- ToolCallConverter 使用手写 JSON 序列化（`serializeArguments()`）而非 `ChatToolCall.getArgumentsText()`，因为后者依赖 `JSON.stringify()` 需要 JSON provider 初始化
- ReActExecutor 捕获 `executeToolCalls` 中的 tool 异常并转换为 error response message，循环不中断

### 验证
- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` — BUILD SUCCESS (13 tests pass)

### Doc-sync
- `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` — L1-5 状态从 ❌ 改为 ✅
- No owner-doc update required (内部实现类，设计文档已覆盖 ReAct loop 语义)

---
## Plan 131: AgentExecutionContext 执行上下文数据对象

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/131-nop-ai-agent-execution-context.md`

### 改动内容

1. **`AgentExecutionContext.java`**: 创建执行上下文数据对象，包含 12 个字段（agentModel, messages, plan, sessionId, effectiveOptions, currentIteration, tokensUsed, status, maxIterations, metadata, lastError, startTimeMs）。工厂方法 `create(AgentModel, String)` 从 agentModel 提取 maxIterations（null→10）和 effectiveOptions
2. **`AgentExecutionResult.java`**: 创建执行结果数据对象，包含 7 个字段（status, finalMessage, messages, totalIterations, totalTokensUsed, durationMs, error）。静态工厂 `fromContext(AgentExecutionContext)` 生成不可变快照
3. **`TestAgentExecutionContext.java`**: 12 个 JUnit 5 测试覆盖构造、字段读写、maxIterations 默认值、消息可变性、快照不可变性

### 验证
- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` — Tests run: 22, Failures: 0, Errors: 0 (BUILD SUCCESS)

### Doc-sync
- `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` — L1-2 状态从 ❌ 改为 ✅
- No owner-doc update required (纯内部数据对象，无公共 API 变更)

---

## Plan 132: IAgentExecutor 接口定义

**Scope**: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/`
**Plan**: `ai-dev/plans/132-nop-ai-agent-executor-interface.md`

### 改动内容

1. **`IAgentExecutor.java`**: 创建执行策略接口，单个方法 `CompletionStage<AgentExecutionResult> execute(AgentExecutionContext ctx)`。Javadoc 记录策略模式契约（定义执行模式、从上下文读取配置、无流式变体）
2. **`TestIAgentExecutor.java`**: 3 个 JUnit 5 测试验证 lambda/mock 实现、方法签名编译正确、返回值非空

### 验证
- `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` — Tests run: 25, Failures: 0 (BUILD SUCCESS)

### Doc-sync
- `ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md` — L1-3 状态从 ❌ 改为 ✅
- No owner-doc update required (接口定义与设计文档 §3.1 完全一致)
