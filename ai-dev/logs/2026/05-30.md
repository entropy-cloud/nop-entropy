# AI 开发日志 — 2026-05-30

### 2026-05-30 INeedInit docs follow-up

- 将 `INeedInit` 文档继续挂接到 owner guide：`docs-for-ai/02-core-guides/model-first-development.md` 新增“模型加载后的 Java 初始化”小节，明确模型派生逻辑不一定继续放在 XML / imp / codegen 中，必要时可收口到 Java `init()`。
- 扩充 `docs-for-ai/02-core-guides/model-init-and-ineedinit.md`：新增常见误用（在 `init()` 中做外部副作用、merge 后漏调 `init()`、把 `init()` 当 getter 替代、重复追加集合/索引）与排障信号，便于从运行期异常反推初始化缺失问题。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过。
- Doc-sync: `docs-for-ai/02-core-guides/model-first-development.md`、`docs-for-ai/02-core-guides/model-init-and-ineedinit.md` 已更新。

### 2026-05-30 INeedInit usage research and docs sync

- 调研 `INeedInit` 在仓库中的实际语义与调用链：确认接口定义位于 `nop-kernel/nop-api-core`，标准 XDSL 解析入口 `DslModelParser` 会在模型反序列化完成后自动执行 `init()`，除非显式 `disableInit(true)`。
- 梳理了三类代表性用法：`ApiModel` / `ApiServiceModel` / `ApiMethodModel` 的父上下文回填，`StateMachineModel` / `RuleModel` 的运行期索引构建，以及 `OrmModelLoader` 在手工 merge 模型后的显式 `model.init()` + `freeze(true)` 顺序。
- 文档新增：`docs-for-ai/02-core-guides/model-init-and-ineedinit.md`，覆盖 `INeedInit` 的适用场景、自动/手动调用边界、`init()` 中应做与不应做的事情，以及本次 API XML 迁移中的落地方式。
- 路由同步：`docs-for-ai/INDEX.md` 新增 `INeedInit` 路由项；`docs-for-ai/04-reference/source-anchors.md` 新增 `MODEL-INIT-001` 到 `MODEL-INIT-004` 锚点。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过。
- Doc-sync: `docs-for-ai/02-core-guides/model-init-and-ineedinit.md`、`docs-for-ai/INDEX.md`、`docs-for-ai/04-reference/source-anchors.md` 已更新。

### 2026-05-30 API bean short-type format closure

- Follow-up to Plan 58: kept `ApiMessageFieldModel` on `StringHelper.simplifyJavaType(...)` instead of `simplifyStdJavaType(...)`, and retained the local source-style simplification needed to emit `String` / `Long` / `Map<String,Object>` while still preserving `java.sql.Timestamp`.
- 影响范围：`nop-network/nop-rpc/nop-rpc-model` 的 `codegenJavaType` 解析与回归测试，`nop-kernel/nop-codegen` 的 API bean 模板，以及 `nop-rule` / `nop-wf` 重新生成的 API bean / xmeta 产物。
- 偏差：在把生成类型恢复成短名后，`nop-rule-api` / `nop-wf-api` 首次编译暴露 `Map` / `List` / `Set` 缺少 import；未回退类型格式，而是在 API bean 模板补齐 `java.util.List` / `Map` / `Set` import，使短类型风格与编译要求同时成立。
- 验证：`./mvnw test -pl nop-network/nop-rpc/nop-rpc-model -am -T 1C -Dtest=TestApiMethodModel -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- 验证：`./mvnw -f "nop-rule/pom.xml" -pl "nop-rule-codegen" -am generate-test-resources -DskipTests -T 1C` 通过。
- 验证：`./mvnw -f "nop-wf/pom.xml" -pl "nop-wf-codegen" -am generate-test-resources -DskipTests -T 1C` 通过。
- 验证：`./mvnw test -pl nop-rule/nop-rule-service -am -T 1C -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- 验证：`./mvnw -f "nop-wf/pom.xml" test -pl "nop-wf-service" -am -T 1C -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- Doc-sync: No owner-doc update required.
