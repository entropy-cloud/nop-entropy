





# AI 开发日志 — 2026-05-30

### 2026-05-30 INeedInit docs follow-up

- 将 `INeedInit` 文档继续挂接到 owner guide：`docs-for-ai/02-core-guides/model-first-development.md` 新增“模型加载后的 Java 初始化”小节，明确模型派生逻辑不一定继续放在 XML / imp / codegen 中，必要时可收口到 Java `init()`。
- 扩充 `docs-for-ai/02-core-guides/model-init-and-ineedinit.md`：新增常见误用（在 `init()` 中做外部副作用、merge 后漏调 `init()`、把 `init()` 当 getter 替代、重复追加集合/索引）与排障信号，便于从运行期异常反推初始化缺失问题。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过。
- Doc-sync: `docs-for-ai/02-core-guides/model-first-development.md`、`docs-for-ai/02-core-guides/model-init-and-ineedinit.md` 已更新。

### 2026-05-30 INeedInit usage research and docs sync

- 调研 `INeedInit` 在仓库中的实际语义与调用链：确认接口定义位于 `nop-kernel/nop-api-core`，标准 XDSL 解析入口 `DslModelParser` 会在模型反序列化完成后自动执行 `init()`，除非显式 `disableInit(true)`。
- 梳理了三类代表性用法：`ApiModel` / `ApiServiceModel` / `ApiMethodModel` 的父上下文回填，`StateMachineModel` / `RuleModel` 的运行期索引构建，以及 `OrmModelLoader` 在手工 merge 模型后的显式 `model.init()` + `freeze(true)` 顺序。
- 文档新增：`docs-for-ai/02-core-guides/model-init-and-ineedinit.md`，覆盖 `INeedInit` 的适用场景、自动/手动调用边界、`init()` 中应做与不应做的事情，以及本次 API XML 迁移中的落地方式。
- 路由同步：`docs-for-ai/INDEX.md` 新增 `INeedInit` 路由项；`docs-for-ai/04-reference/source-anchors.md` 新增 `MODEL-INIT-001` 到 `MODEL-INIT-004` 锚点。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过。
- Doc-sync: `docs-for-ai/02-core-guides/model-init-and-ineedinit.md`、`docs-for-ai/INDEX.md`、`docs-for-ai/04-reference/source-anchors.md` 已更新。

### 2026-05-30 API bean short-type format closure

- Follow-up to Plan 58: kept `ApiMessageFieldModel` on `StringHelper.simplifyJavaType(...)` instead of `simplifyStdJavaType(...)`, and retained the local source-style simplification needed to emit `String` / `Long` / `Map<String,Object>` while still preserving `java.sql.Timestamp`.
- 影响范围：`nop-network/nop-rpc/nop-rpc-model` 的 `codegenJavaType` 解析与回归测试，`nop-kernel/nop-codegen` 的 API bean 模板，以及 `nop-rule` / `nop-wf` 重新生成的 API bean / xmeta 产物。
- 偏差：在把生成类型恢复成短名后，`nop-rule-api` / `nop-wf-api` 首次编译暴露 `Map` / `List` / `Set` 缺少 import；未回退类型格式，而是在 API bean 模板补齐 `java.util.List` / `Map` / `Set` import，使短类型风格与编译要求同时成立。
- 验证：`./mvnw test -pl nop-network/nop-rpc/nop-rpc-model -am -T 1C -Dtest=TestApiMethodModel -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- 验证：`./mvnw -f "nop-rule/pom.xml" -pl "nop-rule-codegen" -am generate-test-resources -DskipTests -T 1C` 通过。
- 验证：`./mvnw -f "nop-wf/pom.xml" -pl "nop-wf-codegen" -am generate-test-resources -DskipTests -T 1C` 通过。
- 验证：`./mvnw test -pl nop-rule/nop-rule-service -am -T 1C -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- 验证：`./mvnw -f "nop-wf/pom.xml" test -pl "nop-wf-service" -am -T 1C -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false` 通过。
- Doc-sync: No owner-doc update required.
## nop-stream Plan 80 P3 审计修复完成

**Plan**: ai-dev/plans/80-nop-stream-p3-audit-remediation.md (completed)

**Summary**: Executed Plan 80 across 3 phases, fixing 3 P3 findings from 2026-05-28 deep audit. All 3 findings verified still present before fix.

**Phase 1 (commit 0ffdd5fb1)**: ICheckpointStorage throws Exception → CheckpointStorageException (03-02). Created CheckpointStorageException extends StreamException. Updated ICheckpointStorage (13 methods), JdbcCheckpointStorage, LocalFileCheckpointStorage, TestCheckpointCoordinator anonymous impl. NopException pass-through pattern prevents wrapping of StreamException from validateId.

**Phase 2 (commit 699e18a6f)**: MalformedPatternException ErrorCode constructors (09-01). Added (ErrorCode) and (ErrorCode, Throwable) constructors. Added ERR_CEP_MALFORMED_PATTERN to NopCepErrors.

**Phase 3 (commit ab4e793eb)**: TaskExecutor awaitCompletion Javadoc (09-10). Added Javadoc to both awaitCompletion methods explaining that exceptions are logged at DEBUG and callers should check task.getError() on each SubtaskTask.

**Tests**: `./mvnw test -pl nop-stream -am` BUILD SUCCESS after each phase.

## nop-stream Plan 79 Round 11 P1/P2 审计修复完成

**Plan**: ai-dev/plans/79-nop-stream-round11-p1-p2-audit-remediation.md (in progress → pending closure audit)

**Summary**: Executed Plan 79 across 7 phases, fixing 2 P1 + 8 P2 findings (7 Fix + 1 Proof) from Round 11 adversarial review + 2026-05-30 full deep audit. 15-01 CepPatternBuilder generic migration adjudicated as watch-only residual (Pattern<T,F extends T> self-referential generics prevent wildcard usage).

**Phase 1 (commit 27020e1e2)**: TaskManager semaphore leak P1 fix - Added capacitySemaphore.release() to two early-return paths in receiveAssignment() (duplicate task check + putIfAbsent race). Test: testDuplicateAssignmentDoesNotLeakSemaphore.

**Phase 2 (commit 6da1457d1)**: MessageSourceFunction NPE P1 fix - Added shutdownLatch null check at run() start for transient field reinitialization after deserialization. Test: testDeserializedSourceRunDoesNotThrowNPE (uses reflection to simulate null).

**Phase 3 (commit aad57286b)**: TwoPhaseCommitSinkFunction synchronization P2 fix - Wrapped pending entrySet iteration and modification in synchronized(pending) blocks in both finishCommit() and restoreFromEpoch(). Test: testConcurrentFinishCommitNoConcurrentModificationException.

**Phase 4 (commit 75e1f5d20)**: NFA proof + WindowAggregationOperator trigger state P2 fix - NFA: Added documentation comment at continue explaining SharedBuffer release via prune→releaseNode path. WindowAggregationOperator: Added trigger.clear() and triggerState iteration cleanup for source windows during session merge. Test: testSessionWindowMergeClearsTriggerState.

**Phase 5 (commit cea45290b)**: InputGate recursion elimination P2 fix - Replaced 3 recursive readMultiChannel() calls with labeled continue (retry loop). Replaced Thread.sleep(10) with LockSupport.parkNanos(10_000_000L). Tests: testHighWatermarkEventCountNoStackOverflow, testHighBarrierEventCountNoStackOverflow.

**Phase 6 (commit ad7e7c7db)**: ErrorCode migration P2 fix - Replaced 18 string constructors across 11 files with NopStreamErrors.ERR_ constants. Added 10 new ERR_ constants and 3 ARG_ constants. All files verified zero string constructor hits.

**Phase 7 (commit 3b74b15ad)**: WindowOperator type safety P2 fix - Added try-catch around (ACC) value casts in addWindowElement, throwing StreamException(ERR_STREAM_TYPE_MISMATCH) with type info instead of ClassCastException. Added ERR_STREAM_TYPE_MISMATCH to NopStreamErrors. CepPatternBuilder generic migration evaluated but blocked by Pattern<T,F extends T> capture conversion.

**Tests**: `./mvnw test -pl nop-stream -am` BUILD SUCCESS (all modules pass)

## nop-stream Plan 77 Round 10 P0/P1 审计修复完成

**Plan**: ai-dev/plans/77-nop-stream-round10-p0-p1-audit-remediation.md (in progress)

**Summary**: Executed Plan 77 across 4 phases, fixing all 4 P0 + 7 P1 findings from Round 10 adversarial review.

**Phase 1 (commit 1c438074d)**: CheckpointCoordinator 2PC P0 fixes - AR-1: Added checkpointSuccessMap to record original success value for retryFailedCommits (was hardcoded true). AR-2: shutdown() now calls notifyParticipantsFinishCommit(id,false) + notifyCheckpointAborted(id) before clearing pending checkpoints. Tests: TestCheckpointCoordinator (14 tests pass).

**Phase 2 (commit 114d164a8)**: TaskManager semaphore P0 fixes - AR-3/AR-4: Added AtomicBoolean semaphoreReleased to RunningTask with CAS-based release in cancelTask(), run() finally, and updateFencingToken(). Prevents double-release and semaphore leaks. Tests: TestTaskManager (12 tests pass).

**Phase 3 (commit 827e58d4b)**: WindowAggregationOperator P1 fixes - AR-5: Added currentWatermark != Long.MIN_VALUE guard to late-element check. AR-6: Rebuild activeWindowsPerKey in restoreState. AR-7: Removed output.emitWatermark from non-advancing watermark branch. Tests: TestWindowAggregationOperatorLateData (9 tests pass).

**Phase 4 (commit f623ec4e0)**: Timer/InputGate/Connector P1 fixes - AR-8: HeapInternalTimerService 3-step timer rescheduling (removeAll before callback, then conditional delete). AR-9: InputGate barrierEmitted flag for AT_LEAST_ONCE single emission. AR-10: BatchConsumerSinkFunction buffer.clear moved from finally to after successful consume. AR-11: DebeziumCdcSourceFunction draining reset at run() start.

**Tests**: `./mvnw test -pl nop-stream -am` BUILD SUCCESS (1422 tests across all modules)

## nop-stream Plan 76 R1/R2 审计遗留修复完成

**Plan**: ai-dev/plans/76-nop-stream-remaining-audit-remediation.md (completed)

**Summary**: Executed Plan 76 across 6 phases, fixing 8 P2 + 18 P3 audit findings from R1/R2 deep audits that were not covered by Plans 73-75.

**Phase 1 (commit 6a49e8c59)**: BOM+POM governance - Added nop-stream-connector/runtime to nop-bom, removed ${project.version} from fraud-example, removed redundant maven.compiler from runtime pom, deleted IDE debris (.classpath/.project/.settings) from checkpoint/flink/flow, added spec drift comment.

**Phase 2 (commit d2c119dff)**: CheckpointCoordinator consecutiveTriggerFailures counter with threshold alerting, removed dead acknowledgePrecedingCheckpoint method from PendingCheckpoint.

**Phase 3 (commit c93b028cc)**: Replaced assertDoesNotThrow with DAG structure assertions in TestWindowedStreamAggregation, replaced Thread.sleep with CompletableFuture.get(timeout) in TestCheckpointIntegration, created TestProcessingTimeoutTrigger (3 tests), added testKeyedStateBackendRestorePreservesValues integration test.

**Phase 4 (commit 676263197)**: ChainingOutput operatorName context in exceptions, StreamRecord.replace() Javadoc warning, Output.collectElement else branch, KeyedStreamImpl Comparable comments, CheckpointSerDe instanceof Map checks.

**Phase 5 (commit 109fbeca2)**: Removed ~31 lines commented code from SharedBuffer, removed commented exception from StreamRecord, extracted restoreTaskStatesFromSource/FromCheckpoint helpers eliminating ~100 lines duplication, split WindowOperator.processElement into sub-methods. R1-02-05 (NFA.EventWrapper) deferred.

**Phase 6 (commit 96649882d)**: @Deprecated on CoMapFunction/CoFlatMapFunction, TestPrintSinkFunction (2 tests), TestFunctionUtils (5 tests), CepTestUtils shared utility updating 5 CEP test files, CepOperator state backend design Javadoc.

**Tests**: `./mvnw test -pl nop-stream -am` BUILD SUCCESS

**Deferred**: R1-02-05 (NFA.EventWrapper extraction - optimization candidate)

## nop-stream Plan 75 Post-Closure Fix: AR-19 Checkpoint Ghost Regression

**Plan**: ai-dev/plans/75-nop-stream-round9-and-p2-audit-remediation.md (post-closure fix)

**Root Cause**: Plan 75 Phase 3 AR-19 ghost checkpoint fix removed `completableFuture.complete()` from `PendingCheckpoint.acknowledgeTask()`, but `CheckpointCoordinator` relied on a `whenComplete` callback (triggered by that completion) to drive `completePendingCheckpoint()`. This created a deadlock: the future was never completed because the completion trigger was removed, but the code that would complete it only ran when the future completed.

**Fix (commit c89f3e3dc)**:
- `PendingCheckpoint.acknowledgeTask()`: future not auto-completed (keeps AR-19 guarantee)
- `PendingCheckpoint.forceComplete()`: new public method for explicit completion after storage
- `CheckpointCoordinator.acknowledgeTask()`: directly checks `isFullyAcknowledged()` and calls `completePendingCheckpoint()` instead of relying on `whenComplete` callback
- `CheckpointCoordinator.completePendingCheckpoint()`: calls `pending.forceComplete()` after successful storage
- `CheckpointCoordinator.shutdown()`: resets `numPendingCheckpoints` to 0
- Test alignment: `TestRemoteDataExchange` expects `StreamException` (Phase 5 error code migration)
- Test alignment: standalone `PendingCheckpoint` tests use `forceComplete()`

**Tests**: All 1399 tests pass (core 822 + cep 216 + runtime 322 + connector 39)

**Doc-sync**: No owner-doc update required

## nop-stream Plan 75 Round 9 P0/P1 + 全量 P2 审计修复完成

**范围**: nop-stream 全模块（10 子模块）

**Plan**: ai-dev/plans/75-nop-stream-round9-and-p2-audit-remediation.md

**关键改动**:

Phase 1 - CEP P0 (commit 28d806d73):
- SharedBufferAccessor.releaseNode: break→continue 修复引用泄漏
- NFAState.equals/hashCode: 显式 Comparator 替换 Arrays.sort(Object[]) 修复 ClassCastException
- SharedBuffer.registerEvent: eventsBuffer + eventsBufferCache 双写确保崩溃恢复

Phase 2 - Window P0/P1 (commit 089995ab4):
- WindowAggregationOperator: Session Window 合并实现（MergingWindowAssigner 回调）
- EventTimeSessionWindows: Long.MIN_VALUE 时间戳守卫
- purgeWindow: 同时清理 event-time 和 processing-time timers
- WindowAggregationFunction.merge() + AggregateAggregationFunction 委托

Phase 3 - Checkpoint/RT P1/P2 (commit 7b37bd446):
- CheckpointCoordinator: 幽灵 checkpoint 修复（completableFuture.complete 移到存储后）
- TaskManager: Semaphore 容量控制 + completedTasks 上限 1000
- JobCoordinator: AtomicBoolean 双重初始化防护 + null fencingToken 拒绝
- volatile: CollectionReplayableSource.currentOffset
- ConcurrentHashMap.newKeySet: SourceEnumerator.discoveredSplits
- CheckpointBarrierTracker: 防止重复 ACK 下溢

Phase 4 - Connector P2 (commit f69d2c3b3):
- DebeziumCdcSourceFunction: volatile source/subscription
- BatchConsumerSinkFunction: 防御性拷贝 + AutoCloseable 关闭
- MessageSourceFunction: CountDownLatch 替代 Thread.sleep

Phase 5 - 错误码 + 类型安全 (commit 6846d6034):
- ClassNameValidator: StreamException 替换 SecurityException/IllegalArgumentException
- RemoteInputChannel: volatile decodeError 字段传播解码错误
- ~20 处 JDK 原生异常替换为 StreamException + ErrorCode
- OperatorChain 统一异常风格
- StreamComponents.getBean() @Internal 标注
- MemoryStateSerDe 类型校验

Phase 6 - 依赖 + 测试 + 风格 (commit 2308338cd):
- 移除 nop-stream-cep nop-xlang 依赖
- nop-stream-connector nop-batch-core 标记 optional
- 新增 NFACompiler/NFA 测试 6 项
- Pattern.until() 接受 TIMES 量词
- @SuppressWarnings("rawtypes") 清理 raw Pattern 类型
- WindowAggregationOperator allowedLateness 配置

**验证**: `./mvnw test -pl nop-stream/nop-stream-cep,nop-stream/nop-stream-core,nop-stream/nop-stream-connector -am` BUILD SUCCESS

**Commits**: 28d806d73, 089995ab4, 7b37bd446, f69d2c3b3, 6846d6034, 2308338cd

**Doc-sync**: No owner-doc update required

## nop-stream Plan 74 P0/P1 审计修复完成

**范围**: nop-stream 全模块（10 子模块）

**Plan**: ai-dev/plans/74-nop-stream-p0-p1-audit-remediation.md

**关键改动**:

CEP NFA P0 正确性修复 (commit 41f00c7a1):
- NFA.processMatchesAccordingToSkipStrategy: 空匹配结果检查防止 IOOBE
- NFA.findFinalStateAfterProceed/createDecisionGraph: visited set 防止 PROCEED 环无限循环
- 删除 NFA.doProcess 中无效果的 O(n^2) removeIf 死代码
- NFAState.equals/hashCode: PriorityQueue 比较前先排序，确保相同元素不同插入顺序返回 true
- CepPatternBuilder: instanceof CepPatternPartModel → CepPatternSingleModel 修复类型匹配

执行引擎 + 分布式运行时 + Connector P1 修复 (commit bc7ec030f):
- OperatorChain.close() 反向迭代
- StreamTaskInvokable try/finally 确保 outputWriter.close()
- AbstractStreamOperator.processBarrier 快照失败时仍转发 barrier
- TimestampsAndWatermarksOperator processElement/processWatermark 开头重置 idle=false
- TaskManager.RunningTask catch(Throwable)
- GraphModelCheckpointExecutor fallback 跳过 keyed state（仅记 WARN）
- SourceEnumerator.assignSplits break→continue
- JobCoordinator.globalRecovery 调用所有 TM.updateFencingToken
- DebeziumCdcSourceFunction subscribe try-catch + source.stop()

CEP 序列化 + 测试覆盖 + 类型安全 P1 修复 (commit c056a9efd):
- 提取 EvalFunctionCondition 独立 Serializable 类（修复匿名内部类序列化问题）
- MemoryKeyedStateBackend.getTypedNamespace() 返回 Object
- 新增 13 个测试（addQualifier 8 分支、FollowKind 3 变体、lateDataOutputTag、Serializable 验证）

**偏差**:
- R7-AR-8 (propagateKeySelectors) 回退为直接子节点传播：BFS 会跨越类型变更算子（如 aggregate）传播 keySelector，导致 ClassCastException
- R7-AR-9 idle=false 语义变更：idle 在每个元素/水印处理时重置，WatermarkGenerator 需在同一 onEvent 调用中 markIdle()

**延期项**（→ Plan 75）:
- R7-AR-12: EmbeddedDistributedExecutor checkpoint 集成（需分布式检查点架构设计）
- DA-15-05: MemoryInternalAppendingState ACC/IN 泛型约束（Java 泛型擦除限制）

**验证**: `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS，10 子模块全部通过

**Commits**: 41f00c7a1, bc7ec030f, c056a9efd

**Doc-sync**: No owner-doc update required（全部为代码/测试修复，不涉及平台约定变更）
