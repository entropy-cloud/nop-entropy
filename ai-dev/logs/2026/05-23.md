# 2026-05-23

### Plan 42 nop-stream 设计实现 — Phase 0-6 模型层实现

- Plan 42 执行中：7 个 Phase 的模型层接口和数据类全部创建，编译通过，869 个测试全部通过
- **Phase 0**：新增 `io.nop.stream.core.model` 包（StreamModel, StreamComponents, StreamRequirement 枚举 20 值, StreamBackendCapability, StreamModelFingerprint 含 SHA256, StreamRequirementValidator）。扩展 CheckpointType 增加 TERMINAL_SAVEPOINT/EXPORTED_SAVEPOINT。StreamExecutionEnvironment.execute() 现在构建 StreamModel 并校验 requirements。
- **Phase 1**：新增 `io.nop.stream.core.common.state.shard` 包（StateShard 含 stableHash 路由, StatePath 工具类生成 keyed/operator/source/sink 路径）。新增 StateSegmentDescriptor, TaskEpochSnapshot extends TaskStateSnapshot（增加 shard/timer 字段）。
- **Phase 2**：新增 CheckpointParticipant 接口（saveState/prepareCommit/finishCommit/restoreFromEpoch），TwoPhaseCommitSinkFunction extends CheckpointParticipant（default 方法映射）。新增 ProcessingGuarantee 四级枚举, EpochState 七状态, EpochManifest, SourceConsistencyCapability, SinkConsistencyCapability, JobTerminationMode, JobTerminationContext。扩展 CheckpointPlan 增加 checkpointParticipants/processingGuarantee 字段。
- **Phase 3**：新增 `io.nop.stream.core.connector` 包（SourceWorkUnit, RestrictionTracker, DynamicSplitRequest/Response, DrainableSource, WatermarkEstimator）。新增 SourceEnumeratorState。
- **Phase 4**：新增 `io.nop.stream.core.execution.flow` 包（FlowControlPolicy 三值枚举, EdgeConfig, MemoryBudget 含默认分配比例）。
- **Phase 5**：新增 AccumulationMode 三值枚举, WindowingStrategy（持有 windowFnId 字符串引用）, PaneInfo, PaneState。
- **Phase 6**：新增 `io.nop.stream.core.execution.plan` 包（PartitionPolicy 六值枚举, PartitionedPlan 含 VertexPlan/EdgePlan, DeploymentPlan）。新增 PartitionedPlanGenerator（JobGraph→PartitionedPlan）。新增 DeploymentPlanGenerator（PartitionedPlan→DeploymentPlan 本地 runtime）。
- 验证：`./mvnw test -pl nop-stream` BUILD SUCCESS, 869 tests, 0 failures
- 偏差：StreamException 使用 StreamRuntimeException 作为基类而非 NopException（因 NopException 构造器需要 ErrorCode）
- Doc-sync: 待更新

### Plan 41 nop-stream 代码缺口修复 — 执行完成

- Plan 41 所有 5 个 Phase 执行完毕，10 个 Fix 全部实现并通过单测验证
- **Phase 1 RecordWriter 分区 Bug**：JobEdge 新增 `IPartitioner partitioner` 字段，JobGraphGenerator 传递 StreamEdge.partitioner，GraphExecutionPlan.build() 从 JobEdge 读取传给 RecordWriter（不再传 null）。emitElement() 在有 partitioner 时广播到所有分区。新增 4 个单测验证分区选择和广播行为。
- **Phase 2 Checkpoint 存储 Bug**：JdbcCheckpointStorage.storeSavepoint() 写入 savepoint_path 列，loadSavepoint() 按 savepointPath 查询（不再硬编码 "1","1"）。LocalFileCheckpointStorage 的 ensureDirectoryExists/deleteIfExists 改为 WARN 日志（不再吞异常）。新增 5 个单测验证 savepoint 存储加载。
- **Phase 3 Processing Time Timer**：WindowAggregationOperator 新增 processingTimeTimers + processingTimeTimerLookup 字段、advanceProcessingTime() 方法。TriggerContextImpl 的 registerProcessingTimeTimer/deleteProcessingTimeTimer 不再是空方法体。新增 4 个单测验证注册、触发、删除。
- **Phase 4 AggregatingState**：MemoryKeyedStateBackend.getAggregatingState() 返回 MemoryAggregatingState 实例（不再抛 UnsupportedOperationException）。add() 调用 AggregateFunction.add()，get() 调用 getResult()。新增 1 个单测验证累加和获取。
- **Phase 5 空壳代码清理**：PendingCheckpoint.acknowledgePrecedingCheckpoint() 改为抛 UnsupportedOperationException。CollectionReplayableSource.cancel() 设置 running=false，run() 检查退出条件。
- 验证：nop-stream-core 578 tests passed, nop-stream-runtime 163 tests passed
- 偏差：Phase 3 原计划在 processElement 末尾自动调用 advanceProcessingTime(System.currentTimeMillis())，与现有 TestProcessingTimeWindowIntegration 冲突（该测试使用人工控制的时间）。已改为不自动调用，由外部驱动。
- No owner-doc update required
- 等待 closure audit

### nop-stream 分布式 exactly-once 设计修订

- 新增设计修订文档：`ai-dev/design/nop-stream/distributed-exactly-once-design-amendment.md`。
- 关键内容：补齐 `PartitionedPlan`、`DeploymentPlan`、`RuntimeTopology`、`StateShard`、`Epoch Manifest`、source/sink capability contract、checkpoint namespace、coordinator HA、failover/fencing、rescale/model evolution、XDSL/Delta contract 与 exactly-once 验证边界。
- 设计裁定：不把 `nop-stream` 降级为简化版，也不做 Flink clone；以 Nop 模型驱动方式一次性定义分布式 strict exactly-once 架构契约。
- 审查：三轮独立子 agent 架构审查完成；第一轮和第二轮阻塞项已吸收，第三轮确认无必须修改项，可作为当前共识版本。
- 验证：文档变更，无代码测试；验证方式为设计上下文比对 + 三轮独立审查。
- Doc-sync: No owner-doc update required（仅新增 `ai-dev/design/` 架构修订文档，未改变 `docs-for-ai/` 平台使用契约）。
