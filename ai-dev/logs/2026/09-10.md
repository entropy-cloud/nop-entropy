# 2026-09-10

## nop-treesitter item 11 closure（补记）

- 独立子 agent closure audit（agent_98af0c66）：8/8 PASS、0 Blocker/Major。证据写入计划 Closure 段；`check-plan-checklist --strict` exit 0；frontmatter → completed；roadmap item 11 → done，**M3 派生翻转 done**；DebugOneTest scratch 删除（373 tests）。

## nop-treesitter item 12 — Nop 平台集成执行

- 计划 `2026-09-10-0800-1-nop-integration.md` 经两轮独立子 agent 对抗性审查（agent_6e3bd4e1：1 Major——ServiceLoader 自递归语义未写明，5 Minor；agent_16b21acb：7/7 RESOLVED，可执行）。全部修入：main resources 不发布 services 文件 + Default 防御性跳过自身；@Inject setter 先例；IoC 测试 initializeTo(REGISTER_COMPONENT)+start；engine 测试用容器 bean；补 blockquote/Purpose；bean id/错误码/ARG 钉死；roadmap 字面偏差裁决（beans.xml 路径、内置语法硬编码在 provider）。
- **Phase 1 landed**：`ITreeSitterLanguageProvider`（getLanguage null-never-throw + languageNames）+ `DefaultTreeSitterLanguageProvider`（5 内置 blob 缓存 + ServiceLoader 合并第三方、同名 custom 覆盖内置、防御性跳过自身类）+ `TreeSitterBizModel`（@BizModel("TreeSitter")，@BizQuery parseTreeSitter → toSExpression；未知语法抛 `nop.err.treesitter.unknown-language`）+ `TreeSitterErrors` + `_vfs/nop/treesitter/{_module,beans/app-treesitter.beans.xml}` + pom（nop-graphql-core compile / nop-ioc test）。
- **Phase 2 landed**：`TestParseTreeSitterGraphQL`——CoreInitialization.initialize() + `BeanContainer.getBeanByType(TreeSitterBizModel.class)`（容器 bean 进引擎，Anti-Hollow 端到端）→ `TreeSitter__parseTreeSitter` RPC：`{"a":1}`/json 返回 S-expression；python → 结构化错误。README 用户指南（NOP-04）。
- **验收**：全模块 **381 tests green**（373 + 8 新增）；checkstyle -Pqa 0；hollow scan 0。
- 实现注记：`IBeanContainer` 的正确 import 是 `io.nop.api.core.ioc.IBeanContainer`（nop-ioc 的 loadFromResource 返回类型；ai-gateway 先例如此）。NopIoC `<property>` 注入 protected 字段无需 setter 兜底，但按先例仍用 @Inject setter。

## nop-treesitter item 12 closure

- 独立子 agent closure audit（agent_aa0de20a）：7 维度 5 PASS + 1 Major（M-1：同名语法 custom-wins 契约三方文档声明、代码却内置优先——confirmed contract drift）+ 3 Minor（frontmatter draft、缺 Draft Review Record、README 缺 Maven 坐标）。
- M-1 按审计方向 (a) 修复：`loadLanguage` 改 custom-first、`languageNames` 并集简化（死分支清理）、`loadCustomProviders` 提为 protected 覆写点，同名遮蔽测试用 javascript blob 冒充 json（输出 `(program (expression_statement ...))` 可区分）。m-1/m-2/m-3 一并补齐。
- 复验 381 green；`check-plan-checklist --strict` exit 0；frontmatter → completed；roadmap item 12 → `done`。下一项：item 13（性能基准）。

## nop-treesitter item 13 — 性能基准执行

- 计划 `2026-09-10-0900-1-perf-benchmark.md` 两轮审查（agent_28173c4c：3 Major——JMH 默认吞异常使 smoke 空洞须 shouldFailOnError+5 RunResults 断言、C 脚本需输入物化且避开 exec:java mainClass 钉死陷阱、Java/C 工作量须对齐 parse+序列化；agent_f8114af9：全部落实 + 3 处计数/路径残留修正）。
- **Phase 1 landed**：`BenchSources`（种子 0x5EED 确定性 fixture，边界截断，同字节喂两侧）+ `JsonParseBenchmark`（10K/100K/1M）+ `JavaParseBenchmark`（单文件/25 文件工程）+ `TreeSitterBenchmarkRunner`（3×1s warmup、5×1s 测量、gc profiler、failOnError）+ smoke test（fork 0、100ms、5 RunResults 断言）。实现坑：Maven 4/JDK 26 下 test-scope 注解处理器不自动执行——pom 显式 `annotationProcessorPaths` + 清理增量编译残留后 BenchmarkList 生成；`mdep.outputFile`（非 outputClasspathFile）；classpath 需含 target/classes。
- **Phase 2 landed**：`bench/c_harness.c`（C 侧 parse + ts_node_string + free 同工作量）+ `bench/run-c-reference.sh`（物化输入 → JMH 全量 → cc 编译 → C 运行）。**数字**（ratio=C/Java）：json-10k 1416.8/617.8=**2.29x** ✓、json-100k 234.5/91.5=**2.56x** ✓、json-1m 14.8/4.23=**3.50x** ✗、java-single 1169.5/273.5=**4.28x** ✗。PERF-04：arena ~1 节点/2.2 源字节、垃圾 ~550-600 B/源字节、1M 峰值堆增量 ~286MB。结论与差距归因（snapshot 深拷贝/无 subtree pool）写入 `nop-treesitter/docs/perf-tuning.md`（PERF-05），优化候选记录未排期。
