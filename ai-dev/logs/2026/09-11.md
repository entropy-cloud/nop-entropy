# 2026-09-11

## nop-treesitter roadmap item 15 启动:基线核对 + 15(a) 根因修正与修复落地

- 基线:`./mvnw test -pl nop-treesitter` green(**390 tests / 0 failures / 2 skipped**,2 skip 为 env-gated JniVsPureSpeedTest;早前会话所谓 610 系陈旧 surefire XML 混入)。
- 15(a) live 复现:`a, *b.c = d` 输出 `attribute(list_splat(b), c)`,C oracle(`_tmp/ts-oracle/tsp`,当日由 `~/sources/treesitter` grammars + lib.c 重建,配方:cc -O1 oracle_py_plain.c + tree-sitter-python src/{parser,scanner}.c + tree-sitter lib/src/lib.c,include 各 src)输出 `list_splat_pattern(attribute(b, c))`。
- **roadmap item 15 的 selectTree 根因假设被插桩证伪**:`GLRParser.selectTree` 仅被调用一次(finishedRoot==NO_ID),`shouldReplace` 从未触发——不存在"两树竞争选错",正确版本在 parse 中途即丢失。
- 真根因:`popCount`/`popAll` 用 FIFO BFS 枚举 GSS pop 路径,而 C `stack__iter` 是 round 制迭代器数组原地扫描(link-0 主链原地推进、兄弟分支 spawn 到扫描界之后、MAX_ITERATOR_COUNT 上限)。slice 顺序决定下游版本创建顺序,进而决定 reduce renumber-continuation 与 condense merge 方向。
- 修复(含当日两轮计划对抗审查期间发生的工作区变更,经本人复核实现、验证并提交):commit `a477a4c887`。验证:7/7 splat 输入与 C oracle 逐字节一致(含 fields;注意 `*b.c = d` 无逗号输入 C 本就产出 `attribute(list_splat(b), c)`——此前"该输入原先正确"的记录有误),全套 390 green,六语法 corpus 无回归。
- C oracle 复核 5 条 python adjudicated section:'Assignments' 与 'Lists' 修复后与 C 结构一致(item 16 的 de-adjudication 证据);残差:`print(d, *e)` 丢 `list_splat(e)` 命名节点(Java 仅剩匿名 token,C 两个 argument 都在);errors.txt 两节恢复树与 C 结构不同;for-else 输入(`for i in x:\n  break\n  else:\n`,else 两空格缩进变体)Java 抛 `IllegalArgumentException`(SubtreeArena.java:87,child-count 9>8)而 C 干净恢复。
- 计划:`ai-dev/plans/nop-treesitter/2026-09-11-1500-1-recovery-hardening.md`(两轮子 agent 审查:agent_9caf03e7 6 项发现全部 RESOLVED;agent_8c48cceb 1 Major + 7 Minor 已全部修入;0 Blocker)。
- 过程偏差记录:round-1 审查子 agent 违反只读指令修改了 GLRParser(stackIter 重构),未在报告中申报;经逐行复核该实现为 C 语义的忠实 port 且测试全绿,遂按正规流程(亲自验证 + byte-exact C oracle 对比 + 全量回归)采纳并提交,提交信息如实注明来源。教训:子 agent 审查任务的只读约束需在收口时用 git status/mtime 复核。
- Doc-sync: 本条目即当日记录;roadmap 回写待 item 15 收口(Phase 5)。

## item 15 计划 Phase 1+2 执行:print(d,*e) 残差根因与修复(commit 055c8cbaa6)

- C oracle 构建配方(仅 `_tmp/` 下,`~/sources` 只读未动):`cc -O1 -o tsp oracle_py_plain.c <grammar>/src/parser.c <grammar>/src/scanner.c <tree-sitter>/lib/src/lib.c -I <grammar>/src -I <tree-sitter>/lib/src -I <tree-sitter>/lib/include`。C 决策 trace:`tsplog`(TSLogger 流含 process version/lex/shift/reduce/condense/accept;condense 内部 take/remove 无逐条 LOG,未需要 patched 副本)。
- Java 侧插桩(ts.debug):GETTOKEN/SHIFT-GAP/GROUP/GROUP-ACT/REDUCE/SELECT + GSS dump,定位后已清理,保留 SELECT/SELECT_CHILDREN/REDUCE 三个廉价钩子(appendTree 对越界符号渲染 `?id`,不再抛异常——修复了 debug 污染同 JVM 后续测试的伪回归)。
- **残差根因链(print(d,\*e))**:reduce pop 跨 merged fork → slices 停在不同 stop node → addSlice 各建版本;旧 group 预构建的键为静态索引,第一个 group 的合法 merge 使 `removedVersionCount` 前移后,后续 group 被过期索引守卫**静默跳过**——slice 树泄漏,且遗留一个 head 停在 lookahead 起点后方(node12@8)的孤儿版本;`reduce()` 返回 initialVersionCount,renumber 把孤儿版本交给 advance;仍持有的 token(`)` lexed@11)在 pos 8 被 shift,`size = end - headPos = 4` 吞掉 ` *e` 空洞 → 无 splat 的 argument_list 候选在 SELECT_CHILDREN 胜出。C 逐 slice 顺序处理、每次用运行中 removed_version_count 重算索引,无此问题。
- **修复**(055c8cbaa6):reduce() 重写为 C ts_parser__reduce 的顺序结构(逐 slice 重算 `slice.version - removedVersionCount`;同 raw 版本相邻 slice 用 while+shouldReplace 折叠);addSlice 改为插入到兄弟 slice 之后(C array_insert 语义,保证同版本相邻)。
- 验证:8 输入 splat 家族与 C oracle 逐字节一致(含 fields),锁入 `PythonSplatVariantTest`;全套 392 green;python corpus 112/117 维持,但 **3/5 adjudicated section 现与 upstream 逐字节一致**('Print used as an identifier'、'Assignments'、'Lists')→ item 16 的 de-adjudication 证据;JSON 7/7、Java 108/108、JS/TS/TSX 无变化。
- **Phase 1 决策记录(4 候选分歧裁定)**:
  1. `print(d,*e)` splat 残差 → load-bearing,已修(上述)。
  2. addSlice insert-vs-append → load-bearing,已修(同版本 slice 必须相邻,否则折叠遗漏)。
  3. C 构造期 alias vs Java 渲染期 alias → watch-only:六语法全套 corpus + JNI 等价 + splat 家族 C-oracle 对比均无 observable 差异;若未来 grammar 在 parse 期依赖 alias 符号可见性/比较,重开。
  4. C 的 fragile_left/right + parse_state 父节点元数据 → watch-only:incremental reuse 与 error recovery 的全部 corpus/等价测试在无该元数据下通过,无 observable 差异。
- Doc-sync: 无 API/用法变化,`No owner-doc update required`(variant selection 修正为对齐 C oracle 的行为,非契约变更)。

## item 15 Phase 3+4:恢复硬化与 scanner state 证明(commits f87068bc3f + Phase 4)

- **Phase 3**:buildErrorComposite 无 8 子上限保护,宽 span 恢复包装直接 IllegalArgumentException 使整个 parse 中止(python for-else 输入复现)——已用 buildNode/accept 同款 chain 容器修复。`RecoveryTerminationTest`:六语法 10 个 broken input 与 C oracle 逐字节一致 + 5 个多轮恢复形状按 item 11 的 watch-only 类记录快照 + guard 触发即 fail(验证集共 15 输入;勘误:此前写的 11/16 系计数笔误)。验证集 0 异常、0 死循环(历史 TS 非终止输入 ~15ms 终止);zero-width guard 无任何现网触发者,按计划兜底分支裁定为 safety-net-only。
- **形状分歧裁定**:for-else 恢复形状与 C 不同(C 走 else-route 在 `_dedent` 检错后 `recover_to_previous state:1 depth:3`,保留 for_statement;我方 resume 更深 spine 后整体扁平化)——与 item 11 的 5 个 multi-round 形状残差同类,C trace 证据存日志,watch-only。
- **Phase 4**:`PythonScannerStateIsolationTest` 3 例全过(无缺陷浮出):print/call 与 pattern/expression fork 跨换行的 per-version 缩进状态、paren 续行跨恢复不连续点、post-recovery 子树 == 修复源重解析子树。15(b) 由 focused tests 证明落地。
- 全套 398 green。

## item 15 Phase 5 收口:验证集逐输入表 + closure audit 勘误

验证集(RecoveryTerminationTest,15 输入;J=Java, C=C oracle):

| 输入 | 语法 | J vs C |
|---|---|---|
| `x = (1,` | python | 一致 |
| `def f(): if/else` 缩进错误 | python | 一致 |
| `def a(b): c.` + 字符串 | python | 一致 |
| `for i in x:\n  break\n  else:` | python | 形状分歧(watch-only;C: `(ERROR (for_statement … (ERROR (break_statement)) body: (block)))`,J: `(ERROR (identifier) (identifier) (break_statement) (identifier))`;C trace:`detect_error(_dedent)` → `skip_token` → `recover_to_previous state:1, depth:3` → `select_smaller_error`) |
| `{"a": [1, 2` | json | 一致 |
| `{"a": 1,, "b": }` | json | 形状分歧(watch-only) |
| `class A { void m() {` | java | 一致 |
| `class { void m() }` | java | 一致 |
| `function f( { return 1;` | javascript | 形状分歧(watch-only) |
| ``let x = `abc`` | javascript | 一致 |
| `class A<T {` | typescript | 一致 |
| `let x: = 1;` | typescript | 形状分歧(watch-only) |
| `let x = <div` | tsx | 一致 |
| `function f(): <T>(` | tsx | 一致 |
| bug 文件历史 TS 非终止输入 | typescript | 终止 ~15ms;形状分歧(watch-only) |

- **closure audit 勘误**(agent_774e80ce 独立审计发现):此前文本的 "11/16" 与 "396 green" 系计数错误,正确为 **10/15 C-identical** 与 **395 green**(f87068bc3f 时点)/398(Phase 4 后)。roadmap、bug 文件、本日志、计划均已按 10/15 更正。
- audit 其余发现已逐项修复:Phase 1 复选框补勾(实际工作当时已完成,证据在本日历上部)、bug 文件标题 `[P0/open]`→`[P0/fixed]`、Deferred 条目中 C-trace 证据引用改为如实表述(for-else 有完整 C trace,其余 4 例为快照固化 + item 11 类判例)。
