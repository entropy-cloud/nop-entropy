# 2026-06-09 Dev Log

### 执行 Plan 128: nop-ai-agent agent.register-model.xml DSL 加载注册

- Plan 128 完成：创建 `agent.register-model.xml` 使 `.agent.xml` DSL 文件可被 XLang 加载机制识别和装载为 `AgentModel` 对象
- Phase 1 — 创建 agent.register-model.xml 并验证加载：
  - 新增 `agent.register-model.xml` 注册 `agent.xml` fileType，schemaPath 指向 `/nop/schema/ai/agent.xdef`
  - pom.xml 添加 `nop-autotest-junit` test scope 依赖
  - 新增 `TestAgentModelLoading` JUnit 5 单元测试，使用 `CoreInitialization.initializeTo(INITIALIZER_PRIORITY_REGISTER_COMPONENT)` 避免 IoC 初始化（IoC 需 IHttpClient bean，测试环境不可用）
  - 新增 `test-agent.agent.xml` 测试用例文件（最小合法 agent：name + description + prompt）
  - 端到端验证：`.agent.xml` → `ResourceComponentManager.loadComponentModel()` → `AgentModel` Java 对象 → name/description/prompt 字段正确
- 关键实现细节：prompt 类型为 `PromptSyntaxParser.CompositeNode`（包裹 `TextNode` 子节点），不是直接的 `TextNode`
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` — BUILD SUCCESS, 1 test passed
- roadmap：L0-1 状态已更新为 ✅

---
