### plan-256: nop-task isCancelledException 穿透 xpl 包装识别 cancellation — Phase 1+2+3 完成（全 plan）

- **Plan**: `ai-dev/plans/256-nop-ai-agent-xpl-cancellation-wrap-preserve.md`（Plan Status: planned；Phase 1+2+3 completed → 全 plan 完成）
- **Work Item**: `xpl-cancellation-wrap-preserve`（carry-over from plan 254 §Closure Follow-up「xpl 方法调用包装穿透 cancellation 字符 ——独立 successor」）。
- **Phase 1 交付（Fix）**：`TaskStepHelper.java`（`nop-task-core`）`:74-94` cause-chain 解包修复：
  - 原 `isCancelledException`（`:72-80`）三档检查均针对传入异常 `e` 自身，不递归 `getCause()` → 当 `e` 是 `NopEvalException`（xpl `wrapInvokeException` 包装）时全部 false。
  - 修复：抽取出 `isCancelledExceptionSingleLevel(e)` 保留原三档逻辑不变；`isCancelledException` 改为遍历 cause chain——`while (e != null) { if (visited.add(e)) break (cycle guard); if (singleLevel(e)) return true; e = e.getCause(); }`。
  - 防循环（设计裁定 3）：`Set<Throwable> visited = Collections.newSetFromMap(new IdentityHashMap<>())`，自引用 cause（`initCause(this)`）命中 `visited.add` 返回 false → 立即 return false。
  - 覆盖**全部** xpl 包装路径：consumer 侧解包不依赖 producer 侧包装点，任何 `getCause()` 链含 cancellation 类型/errorCode 均被识别。
- **Phase 1 交付（Proof）**：`TestTaskStepHelperIsCancelledException`（`nop-task-core`）从 5 tests 扩展到 **9 tests / 0 fail**：
  - 原有 5（plan 254 真值表）：CancellationException / NopTaskCancelledException / ERR_TASK_CANCELLED errorCode → true；ordinary NopException / ordinary RuntimeException → false（零回归）
  - 新增 4（plan 256 cause-chain）：`xplWrappedCancellation_isCancelled_plan256`（`NopEvalException(ERR_EXEC_INVOKE_METHOD_FAIL, cause=NopTaskCancelledException)` → true，核心新行为）/ `xplWrappedOrdinaryException_notCancelled_plan256`（包装非 cancellation → false，防 over-matching）/ `deeplyNestedCancellation_isCancelled_plan256`（≥2 层包装 → true，防循环 + 深度）/ `xplWrappedCancellationException_isCancelled_plan256`（`NopEvalException` 包装 `CancellationException` → true，cause 第一档变体）
  - 使用真实 `ERR_EXEC_INVOKE_METHOD_FAIL`（`io.nop.xlang.XLangErrors`）构造包装异常，精确复现 `AbstractObjFunctionExecutable.wrapInvokeException` 产出形态
- **blast-radius 评估**：grep 全仓 `isCancelledException` 消费方 = 3 个（`TaskStepExecution` `:230`/`:280` + `SelectorTaskStep` `:43`/`:84` + `TryTaskStepWrapper` `:46`/`:69`），全部经 helper 非直接 instanceof。grep `instanceof NopTaskCancelledException` 仅 helper 内 1 处（无额外 bypass 消费方）。`FutureHelper.isCancellationException`（`nop-api-core`）是独立 generic utility，与 task step cancel 流程无关。「更多异常被识别为 cancelled」对 3 消费方影响可接受：3 处均语义「cancellation 穿透/取消，非普通失败处理」，修复前因不解包 cause 误判为普通失败，修复后行为正确化。
- **验证**：`./mvnw test -pl nop-task/nop-task-core -am -T 1C` → **BUILD SUCCESS**：nop-task-core 57 tests（含 `TestTaskStepHelperIsCancelledException` 9 + `TestTaskStepStateBeanExceptionPersistence` 6 + `TestTaskFlowManager` 25 等全部既有测试）0 failures / 0 errors。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 helper 行为，step/cancel public 执行契约不变，cancelled step 仍抛出、仍不产生 KILLED 终态）。

### plan-256 Phase 2 — E2E cancelled-step 不误判 FAILED

- **Phase 2 交付（Proof）**：`TestReliabilityDecorators.cancelledStep_notMarkedFailed_cancelExclusion`（`nop-task-ext`）从「记录 gap、不严格断言」改为严格断言 cancelled step 经 xpl 包装后**不被标记 FAILED**：
  - 保留 `runTask("test/cancelled-step")` 抛异常（cancelled exception 经 cancel-check throw 传播，cancel != success）。
  - 新增核心断言：`assertNotEquals(Integer.valueOf(60), stepState.getStepStatus())` —— 修复前此值为 60（FAILED-driver 误标记），修复后保持 ACTIVE(10)。
  - 注释块从「xpl 包装使 cancellation 字符丢失」「端到端不可行」改为描述修复后正确行为（cause-chain 解包使命中 cancel-check → throw 跳过 FAILED-driver）。
- **验证（三模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-ext -am -T 1C` → **BUILD SUCCESS**：nop-task-ext 42（含 `TestReliabilityDecorators` 35，`cancelledStep_notMarkedFailed_cancelExclusion` 通过）/ 0 failures。
  - `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-ai-agent 2714 / 0 failures。
  - nop-task-core Phase 1 基线（57）不变（Phase 2 未触及 core）。
- **端到端验证（#22）**：从 `.task.xml` cancelledStep 经 xpl `<source>` 调 `sim.throwCancelled()` → `doInvoke*` 包装为 `NopEvalException(ERR_EXEC_INVOKE_METHOD_FAIL, cause=NopTaskCancelledException)` → `TaskStepExecution` sync catch cancel-check 调 `isCancelledException`（解包 cause → 命中 → true）→ `:281` throw（跳过 `:291-292` FAILED-driver）→ stepStatus != FAILED 完整路径连通可观测。
- **接线验证（#23）**：`isCancelledException` cause-chain 解包在 runtime 被 `TaskStepExecution` cancel-check 调用（E2E 断言 stepStatus != 60 可观测——修复前为 60）。

### plan-256 Phase 3 — composite step cancellation 传播 E2E（Selector + Try）

- **Phase 3 交付（Proof）**：2 个新 `.task.xml` fixture + 2 个新 E2E 测试（`TestReliabilityDecorators`，35→37 tests）：
  - **SelectorTaskStep cancellation 传播**（`composite-selector-cancelled/v1.task.xml` + `selector_cancelledBranch_propagatesCancellation_plan256`）：selector 第一分支 `cancelledBranch` 经 xpl 调 `sim.throwCancelled()` 抛 cancellation → 包装 → 子 step TaskStepExecution cancel-check（cause-chain 解包 → true）throw → `SelectorTaskStep:42` catch → `isCancelledException:43`（cause-chain 解包 → true）→ `:44` throw（不尝试 `fallbackBranch`）。断言：task 抛异常（修复前 selector 视普通失败 → 尝试 fallbackBranch → 返回 "FALLBACK" 不抛）、`counter()==1`（fallbackBranch 未执行——若执行则 counter==2）、`cancelledBranch.getStepStatus() != 60`（cancellation 识别非 FAILED）。
  - **TryTaskStepWrapper cancellation 穿透**（`composite-try-cancelled/v1.task.xml` + `try_cancelledStep_propagatesThroughCatch_plan256`）：xpl step `tryCancelledStep` 带 `<catch>` catchAction，经 xpl 调 `sim.throwCancelled()` 抛 cancellation → 包装 → `TryTaskStepWrapper:65` catch → `:69 !catchInternalException && isCancelledException`（cause-chain 解包 → true）→ `:70` throw（catchAction 跳过）。断言：task 抛异常（修复前 catchAction 被触发吞掉 cancellation → 返回 "SWALLOWED" 不抛）、`counter()==1`（catchAction 未触发——若触发则 counter==2）、`tryCancelledStep.getStepStatus() != 60`。
  - fixture 中 `fallbackBranch` / `<catch>` catchAction 均 `inject("testExecutionCounter").incrementAndGet()` 作为执行标记，使「未尝试下一分支 / catchAction 未触发」可由 counter 量化观测（#24 无静默跳过：断言具体执行计数）。
- **验证（三模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-core 57 / nop-task-ext 44（`TestReliabilityDecorators` 37 = 35 baseline + 2 Phase 3）/ nop-ai-agent 2714，全部 0 failures / 0 errors。
- **端到端验证（#22）**：selector / try 路径从 `.task.xml` 声明 → step 经 xpl 调用抛 cancellation → doInvoke 包装 → SelectorTaskStep/TryTaskStepWrapper 调 `isCancelledException`（解包 cause → true）→ cancellation 正确传播（selector 不尝试下一分支 / try catchAction 跳过）完整路径连通。
- **接线验证（#23）**：`isCancelledException` cause-chain 解包在 runtime 被 `SelectorTaskStep:43` + `TryTaskStepWrapper:69` 调用（E2E 断言 cancellation 传播行为可观测——修复前 catchAction 触发 / selector 视普通失败，修复后穿透/取消）。

---

### plan-255: nop-task TaskFlowAnalyzer nextOnError 校验字段错配 bug 修复 — 全 plan 完成 + closure

- **Plan**: `ai-dev/plans/255-nop-ai-agent-task-flow-analyzer-nextOnError-validation.md`（Plan Status: planned → **completed**；Phase 1 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `task-flow-analyzer-nextOnError-validation-bug`（L4-8，carry-over from plan 254 §Non-Blocking Follow-ups + §Closure Follow-up「`TaskFlowAnalyzer.checkStepRef` nextOnError 校验 bug ——独立 successor」）。修复 `TaskFlowAnalyzer.checkStepRef` 中 `nextOnError` 引用校验的**字段错配** bug，使 task flow 错误处理路由（`nextOnError`）的 analyze-time 模型校验层与运行时行为（plan 254 `nextStepNameOnError`）一致。
- **Phase 1 交付（Fix）**：`TaskFlowAnalyzer.java`（`nop-task-core`）`:57` 字段错配修复：
  - `nextOnError` 分支 `hasStep` 实参从 `subStep.getNext()`（bug）改为 `subStep.getNextOnError()`（正确字段）
  - 进入条件 `:56` `getNextOnError() != null` 与错误参数 `:61` `ARG_NEXT_STEP = getNextOnError()` 一直正确，仅 `:57` 的 hasStep 实参错配
  - 修复前两类语义错误：**假阴性**（合法 nextOnError + null next → `hasStep(null)` 返回 false → 误抛 `ERR_TASK_UNKNOWN_NEXT_STEP`，阻塞所有「只设 nextOnError、不设 next」的合法错误处理配置）+ **假阳性**（无效 nextOnError + 合法 next → `hasStep(next)` 返回 true → 静默放行，运行时路由到不存在 step 才暴露）
  - `next` 分支（`:47-54`）/ `waitSteps`（`:65-74`）/ `waitErrorSteps`（`:76-85`）校验零变更
- **Phase 1 交付（Proof）**：**`TestTaskFlowAnalyzer`（nop-task-core 新增单元，5 tests / 0 fail）**：
  - `nextOnError_valid_andNextNull_analyzePasses`：合法 nextOnError + null next → analyze 不抛错（假阴性回归）
  - `nextOnError_invalid_andNextValid_analyzeRejects`：无效 nextOnError + 合法 next → 抛 `ERR_TASK_UNKNOWN_NEXT_STEP`，`ARG_NEXT_STEP == "nonexistent"`（假阳性回归）
  - `nextOnError_valid_distinctFromNext_analyzePasses`：合法 nextOnError 与 next 不同 → 不抛错
  - `next_valid_analyzePasses`：合法 next → 不抛错（`next` 分支零回归）
  - `next_invalid_analyzeRejects`：无效 next → 抛错（`next` 分支零回归）
- **验证（全模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-core 53（+5 TestTaskFlowAnalyzer vs plan 254 基线 48）/ nop-task-ext 42（含 plan 254 `nextStepNameOnError_routedFailure_failedDriver` E2E 零回归）/ nop-ai-agent 2714，全部 0 failures / 0 errors
- **Closure（独立 subagent audit APPROVED）**：read-only explore agent（task `ses_12493a2b7ffeyDixbix4SHnZmP`，独立 task_id）逐条复核 Exit Criteria 全 PASS：`:57` 字段修复确认 + Anti-Hollow 真实字段引用 + focused 测试覆盖三类场景 + 三模块 surefire 聚合全绿 + 无既有测试断言 buggy 行为（唯一 nextOnError E2E fixture `next-step-on-error-failure/v1.task.xml` 同时设 next+nextOnError 指向同一合法 step，从不触及 bug 失败模式）。
- **Roadmap**: `task-flow-analyzer-nextOnError-validation-bug`（L4-8）新增 ✅ 行 in `nop-ai-agent-roadmap.md`（plan 254 行列出的「TaskFlowAnalyzer nextOnError 校验 bug successor」已闭合）。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部校验 bug 修复，task flow public 契约不变，校验语义从「错误」收敛为「正确」）。
- **Closure 语义**：闭合 plan 254 §Non-Blocking Follow-ups「TaskFlowAnalyzer nextOnError 校验 bug ——独立 successor」。nextOnError 路由现在 analyze-time 校验与运行时消费（plan 254 `TaskStepExecution` 错误分支）字段一致。`nextOnError` 专属错误码（当前复用 `ERR_TASK_UNKNOWN_NEXT_STEP`，optimization candidate）/ xpl 包装穿透 cancellation（nop-xlang successor）/ continuation-skip / afterLoad-beforeSave / EXPIRED-KILLED / 跨重启持久化（DB-backed state persistence successor）均为显式 Non-Goals。

---
