### plan-264: nop-task step-state full-field round-trip in DaoTaskStateStore（闭合 7× carry-over「全量字段持久化」——补齐 entity 已有列 round-trip 缺口）— Phase 1+2 完成（全 plan）

- **Plan**: `ai-dev/plans/264-nop-ai-agent-step-state-full-field-roundtrip.md`（Plan Status: planned → **completed**；Phase 1+2 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `step-state-full-field-persistence`（carry-over, L4-8, P2；7× carry-over 自 plans 257-263，均登记为 optimization candidate）。修正 carry-over 置基前提：`stateBeanData` 列已承载 `resultValue`（plan 257），当前 round-trip 字段集已驱动 cross-restart resume 正确性。真正剩余 gap：`NopTaskStepInstance` entity 比实际 round-trip 字段集更宽——既有 entity 列语义对应 state-bean 属性但在 save/load 一侧被丢弃。
- **交付（无新增 ORM 列，非 Protected Area；仅消费既有列）**：
  - `DaoTaskStateStore.java`（`nop-task-dao`）：
    - **Group A 双向丢弃补齐**（`copyStepStateToEntity` 写 + `toStepStateBean` 读）：`entity.setInternal(state.getInternal())` / `state.setInternal(entity.getInternal())`；`tagSet↔tagText` 经 `TagsHelper.toString(set)`（写，产 CSV `,a,b,`）/ `TagsHelper.parse(text, ',')`（读，逆运算）——复用 `NopWfStepInstance.getTagSet` 同构约定（设计裁定 2）。
    - **Group B 单向写补读回**（`toStepStateBean` 读）：`createTime`/`updateTime` 经 `entity.getCreateTime().toLocalDateTime()` / `getUpdateTime().toLocalDateTime()`（Timestamp→LocalDateTime，设计裁定 3）；save 侧已写，补 load 侧读回使 loaded bean 反映 entity 时间历史（消除「写而不读」）。
    - 类 javadoc 同步更新（移除「全量字段持久化为独立优化」stale 描述）。
  - 新增 `TestDaoTaskStateStoreFullFieldRoundTrip.java`（`nop-task-ext`，7 tests，真实 `DaoTaskStateStore` + `@NopTestConfig(localDb=true)`）：Group A internal+tagSet round-trip + Group A fresh null 安全零回归 + Group B 时间历史读回非 null（含 entity 列断言 + round-trip 等值）+ 既有字段零回归 guard + loadMainStepState 复用 toStepStateBean 同生效 + **cross-restart E2E**（fresh store 实例，full-field 经 DB 序列化边界存活）+ cross-restart legacy 行向后兼容无伪数据。
- **验证（四模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-dao,nop-task/nop-task-ext -am -T 1C` → **BUILD SUCCESS**（nop-task-ext 86 全绿含新增 7）。
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-task/nop-task-dao,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**（nop-task-ext 88 / nop-task-core / nop-task-dao / nop-ai-agent 2714 全量，0 failures / 0 errors）。plans 252-263 全部 reliability 测试保持绿。
- **Anti-Hollow（#22/#23/#24）**：cross-restart E2E 经真实 DB round-trip（fresh store 实例，非 in-memory 引用）证明 Group A 字段经 DB 序列化边界存活（save 值 == fresh load 值）+ Group B 时间历史 loaded 非 null；无 TODO/continue/空方法体/no-op 作为正常实现；fresh/legacy 路径不产生伪数据（向后兼容）。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 DB 序列化字段补齐，step/task public 执行契约不变，无新 ORM 列）。
- **Carry-over 收口**：plan 263 的 `## Follow-up handled by 264-...` 双向链接已确认存在（plan 263:172-174）。`nop-ai-agent-roadmap.md` 新增 `step-state-full-field-persistence` 条目 ✅（此前为 7× carry-over optimization candidate，首次落地为独立 roadmap 行）。
- **明确 Non-Goals**：新增 ORM 列（parentRunId/partitionIndex/bizObj*/extType/extState）/ stateBean 跨 step 中间变量持久化 / stacktrace 完整持久化 / 独立 history 审计表 / composite saveState 频率 / 既有 round-trip 字段集与 resume 行为变更 / stateBeanData 语义变更 均为显式 rejected / out-of-scope / optimization。

---

### plan-263: nop-task mainStep envelope intermediate-state restore（resume 路径加载持久化 mainStep 状态，使中断 task 从 mainStep 中间 flow 位置续跑）— Phase 1+2 完成（全 plan）

- **Plan**: `ai-dev/plans/263-nop-ai-agent-main-step-envelope-resume.md`（Plan Status: active → **completed**；Phase 1+2 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `loadMainStepState`（carry-over, L4-8, P1；3× carry-over 自 plans 258/259/261，plan 262 再次登记为 successor plan candidate）。闭合 nop-task cross-restart resume 的**最后一块功能缺口**：leaf-step 终态经 plan 257 reader 恢复跳过，task-envelope 终态经 plan 259/260 resume 短路，但 **task 中断于 in-flight 状态（mainStep 处于中间 cross-step flow 位置、尚未终态）时，resume 从 `bodyStepIndex=0` 重跑 mainStep**——`ITaskStateStore` 只有 `newMainStepState`（恒 fresh），**没有 `loadMainStepState`**。
- **Phase 1 交付（Follow-up——mainStep 加载契约 + 实现 + 接线）**：
  - `ITaskStateStore.java`（`nop-task-core`）：新增 `default ITaskStepState loadMainStepState(ITaskState taskState, ITaskRuntime taskRt)`（`default` 返回 null 向后兼容，对称 `newMainStepState` 创建路径与 `loadStepState` 子 step 加载路径；所有既有 impl——DefaultTaskStateStore/SnapshotTaskStateStore/SnapshotResumeTaskStateStore/TaskResumeSnapshotTaskStateStore/StateCapturingTaskStateStore/ResumeCapableTaskStateStore/PreloadedStateTaskStateStore——继承 default 即恒回退 fresh，零破坏）。
  - `DaoTaskStateStore.java`（`nop-task-dao`）override：经 `findStepEntity(taskInstanceId, "@main")` + `toStepStateBean`（复用既有 round-trip `bodyStepIndex`/`stepStatus`/result）+ step 级 `afterLoad(taskRt)`（对称 `loadStepState:196`，plan 262 扩展点）；无行返回 null（fresh execute 时 `newTaskState` 只写 task instance 行）。
  - `TaskRuntimeImpl.newMainStepRuntime()`（`nop-task-core`）接线：`recoverMode=true` 时优先 `loadMainStepState`；命中用之（mainStep 从已持久化 `bodyStepIndex` 续跑），未命中回退 `newMainStepState`（零回归）；`recoverMode=false`（fresh execute）恒走 `newMainStepState`（门控对称 `TaskImpl.execute:98` 终态短路条件 `isRecoverMode()`）。
  - **新增测试 fixture**：`test/mainstep-envelope-resume/v1.task.xml`——3 xpl 子步（s1/s2/s3）直接在 `<steps>` 下使 mainStep 为 Sequential（bodyStepIndex 跟踪 s1/s2/s3 位置），各子步 increment 共享 counter（Anti-Hollow 可观测）。
  - `TestMainStepEnvelopeResume.java`（`nop-task-ext`，9 tests，真实 `DaoTaskStateStore` + `@NopTestConfig(localDb=true)`）：
    - 契约：`loadMainStepState_daoLoadsByMainPath_bodyStepIndexRoundTrips_afterLoadFires`（@main path round-trip bodyStepIndex）+ `loadMainStepState_noMainStepRow_returnsNull_freshFallback`（无行回退）。
    - 接线（#23）：`newMainStepRuntime_resumeMode_loadsPersistedMainStepState_bodyStepIndexRestored`（resume 加载 bodyStepIndex=2）+ `newMainStepRuntime_freshMode_alwaysNewMainStepState_bodyStepIndexZero`（fresh 不受影响）+ `newMainStepRuntime_resumeNoRow_fallsBackToFresh_bodyStepIndexZero`（无行回退 fresh）。
    - 语义（#22/#24）：`compositeMainStep_resumeFromBodyStepIndex2_precedingStepsNotReExecuted_subsequentStepRuns`（Anti-Hollow: counter=1 仅 s3）+ `compositeMainStep_freshExecute_allStepsRun_counterThree`（对照组 counter=3）+ `compositeMainStep_resumeFromBodyStepIndex1_precedingStep0Skipped_subsequentStepsRun`（不同 N 值 counter=2）。
    - 零回归：`zeroRegression_terminalTaskResumeShortCircuit_unchanged_mainStepNotReRun`（终态短路不受影响）。
- **Phase 2 交付（Proof——cross-restart E2E）**：
  - `TestMainStepEnvelopeResumeCrossRestart.java`（`nop-task-ext`，4 tests，真实 `DaoTaskStateStore`，**fresh store 实例**模拟进程重启）：
    - `crossRestart_bodyStepIndex2_survivesDbSerializationBoundary_mainStepResumesFrom2`：store A seed bodyStepIndex=2 → store B（fresh 实例）load+resume → counter=1（仅 s3）+ RESULT="OK"（续跑经 DB 序列化边界存活）。
    - `crossRestart_bodyStepIndex1_survivesDbSerializationBoundary_mainStepResumesFrom1`：不同 N 值 counter=2。
    - `crossRestart_freshExecute_controlGroup_allStepsRun_counterThree`：对照组 counter=3。
    - `crossRestart_zeroRegression_terminalTaskShortCircuit_unchanged`：终态 task cross-restart 短路不变。
- **验证（四模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-task/nop-task-dao,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-ext 82（含新增 13）/ nop-task-core 86 / nop-task-dao / nop-ai-agent 2714 全量，全部 0 failures / 0 errors。
- **Anti-Hollow（#22/#23/#24）**：E2E 经真实 DB round-trip（实体↔DB 列 JSON 序列化）证明 mainStep `bodyStepIndex` 经持久化边界存活并驱动续跑；前置已完成子步骤不重复执行（counter 断言，非「从 0 重跑靠 leaf reader 逐一 skip」的伪装）；无 TODO/continue/空方法体/no-op 作为正常实现。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 resume 持久化扩展，`default` 方法向后兼容，task public 执行契约不变）。
- **Roadmap**: `nop-ai-agent-roadmap.md` 新增 `mainstep-envelope-intermediate-state-restore` 条目 ✅（此前为 3× carry-over Non-Goal，首次落地为独立 roadmap 行）。

### plan-262: nop-task task 级 afterLoad/beforeSave 生命周期 hook 语义实现（对称 step 级 ITaskStepState hook + DaoTaskStateStore wiring）— Phase 1+2 完成（全 plan）

- **Plan**: `ai-dev/plans/262-nop-ai-agent-task-state-lifecycle-hooks.md`（Plan Status: planned → **completed**；Phase 1+2 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `task-afterLoad-beforeSave-lifecycle-hook`（carry-over, L4-8, P2；4× carry-over 自 plans 254/255/259/260 反复裁定为 successor plan candidate）。闭合 nop-task 持久化生命周期的**不对称缺口**：step 级 `ITaskStepState` 早已定义 `afterLoad(ITaskRuntime)`/`beforeSave(ITaskRuntime)` 且 `DaoTaskStateStore.loadStepState:188`/`saveStepState:202` 已接线调用，但 **task 级 `ITaskState` 既无此 hook 接口、store 的 `loadTaskState`/`saveTaskState` 也从不调用任何 task 级 load/save 生命周期回调**——custom task state bean 无法在 cross-restart resume 时重建 transient 字段/校验一致性/参与 save 前归一化。
- **Phase 1 交付（Follow-up——task 级 hook 契约 + 实现 + 接线）**：
  - `ITaskState.java`（`nop-task-core`）：新增 `default void afterLoad(ITaskRuntime taskRt)` / `default void beforeSave(ITaskRuntime taskRt)`（`default` no-op 向后兼容，签名严格对称 step 级 `ITaskStepState:82,84`；`ITaskRuntime` 同包无需 import）。
  - `TaskStateBean.java`（`nop-task-core`）：显式 `@Override` 两方法（空体扩展点基类，对称 `TaskStepStateBean:31-38`，使契约表面有具体落地，非仅接口存在 lesson #6）。
  - `DaoTaskStateStore.java`（`nop-task-dao`）接线：`loadTaskState`（`toTaskStateBean` 完成 reconstruction 后、`return` 前 `state.afterLoad(taskRt)`，对称 step 级 `:188`）+ `saveTaskState`（既有状态拷贝到 entity 之前 `state.beforeSave(taskRt)`，对称 step 级 `:202` 在 `copyStepStateToEntity` 之前）。
  - **新增 `protected newTaskStateBean()` 工厂**：使 `toTaskStateBean` 内部构造的 bean 可被子类 override 为可观测实例（load 路径 state 经 store 内部构造，外部无法注入可观测 bean——factory override 是 sanctioned 的 observation 机制，设计裁定 3 execution 裁定）。`newTaskState` 亦经此工厂（一致）。
- **Phase 1 交付（Proof——focused 单测）**：
  - `TestTaskStateLifecycleHooks.java`（`nop-task-ext`，4 tests，真实 DB-backed `DaoTaskStateStore` + `@NopTestConfig(localDb=true)`）：
    - `saveTaskState_invokesBeforeSaveOnce_beforeEntityCopy`：custom `RecordingTaskStateBean` 外部持有，`beforeSave` count==1 + 入参 runtime + **顺序证明**（beforeSave 内 status 突变被 entity.setStatus 持久化观测 → beforeSave 在 entity 拷贝之前）+ anti-over-match（afterLoad 不被调用）。
    - `loadTaskState_invokesAfterLoadOnce_afterToTaskStateBean`：spy store override `newTaskStateBean` 返回 RecordingTaskStateBean，`afterLoad` count==1 + **顺序证明**（afterLoad 捕获到 toTaskStateBean 从 entity 设置的 status → afterLoad 在 reconstruction 之后）+ anti-over-match（beforeSave 不被调用）。
    - `defaultTaskStateBean_hooksAreNoOp_noSideEffect`：默认 `TaskStateBean` 不 override hook，load/save round-trip status/result 正常（no-op 无副作用，零回归）。
    - `loadTaskState_unknownInstance_returnsNull_withoutAfterLoad`：未知 taskInstanceId 返回 null，不触发 afterLoad。
- **Phase 2 交付（Proof——cross-restart E2E）**：
  - `TestTaskStateLifecycleHookCrossRestart.java`（`nop-task-ext`，3 tests，真实 DB-backed `DaoTaskStateStore`，非 in-memory 引用）：
    - `crossRestart_completedTask_hooksSurviveDbSerializationBoundary`：COMPLETED + result，fresh spy store save（beforeSave=1，result 经 **remark JSON** 持久化）→ **fresh spy store 实例**（模拟进程重启）load（afterLoad=1，反序列化 result 可观测）→ hook 调用经 DB 序列化边界存活（#22 端到端）。
    - `crossRestart_failedTask_hooksSurviveDbSerializationBoundary`：FAILED + 含 param exception，save（beforeSave=1，exception 经 **errorBeanData JSON** 持久化）→ fresh store load（afterLoad=1，exception param round-trip 可观测）→ hook 经 errorBeanData 列 JSON 边界存活。
    - `crossRestart_eachTaskStatus_beforeSaveAndAfterLoadCalledExactlyOnce`：遍历 **ACTIVE/COMPLETED/FAILED/KILLED/TIMEOUT** 各终态 save→load round-trip，断言 `beforeSave`==1 + `afterLoad`==1（regression guard 精确计数，防未来 per-status early-return 静默跳过 hook）。
- **验证（四模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-task/nop-task-dao,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-ext 99（含新增 7）/ nop-task-core / nop-task-dao / nop-ai-agent 2714 全量，全部 0 failures / 0 errors。
  - `node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/262-...md --strict` → 退出码 0（所有 checklist 已勾选 + Closure Evidence 已写入）。
  - `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-task --severity high` → 退出码 0（Critical=0, High=0）。
- **端到端验证（#22）**：fresh spy store save（beforeSave 触发 + 实体拷贝/remark/errorBeanData JSON 序列化持久化到 DB）→ DB → **fresh spy store 实例** load（反序列化 + afterLoad 触发）完整路径连通（真实 `DaoTaskStateStore` DB round-trip，非 in-memory 引用），hook 调用经 round-trip 可观测。
- **接线验证（#23）**：custom 子类 + spy factory 断言 hook 在 load/save runtime 被真实调用（调用计数可观测），不只验证类型存在；anti-over-match 各断言另一 hook 不在错误路径被调用。
- **Anti-Hollow（#22/#23/#24）**：E2E 经真实 DB round-trip（实体↔DB 列 JSON 序列化）证明 hook wiring 经持久化边界存活；`default` no-op 为扩展点基类（custom 子类 override 路径有真实计数逻辑与断言），非「正常实现」空壳；无 TODO/continue/吞异常。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部持久化 lifecycle 扩展点，`default` 方法向后兼容，task public 执行契约不变）。
- **Roadmap**: `nop-ai-agent-roadmap.md` 新增 `task-afterLoad-beforeSave-lifecycle-hook` 条目 ✅（此前为 4× carry-over Non-Goal，首次落地为独立 roadmap 行）。

---



- **Plan**: `ai-dev/plans/261-nop-ai-agent-exception-transient-error-bean-persist.md`（Plan Status: planned → **completed**；Phase 1+2 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `exception-transient-optimization`（carry-over, L4-8, P3；6× carry-over 自 plans 247/252/254/258/259/260 反复裁定为 optimization candidate）。闭合 cross-restart exception 持久化的 transient lossy gap——`DaoTaskStateStore` save 经 `ErrorMessageManager.buildErrorMessage` 提取完整 `ErrorBean`（含 errorCode/description/**params**/**cause chain**），但**只取 errCode + errMsg 写入 DB**，其余全丢弃；load 只能从 errCode + errMsg 重构无 params、无 cause 的泛型 `NopException`，使 resume 重抛的 exception 丢失 `.param(...)` 诊断属性与 cause chain。
- **Phase 1 交付（Follow-up——ORM 列新增 + ErrorBean 持久化/重构 wiring）**：
  - `nop-task/model/nop-task.orm.xml`（Protected Area plan-first）：`NopTaskStepInstance` + `NopTaskInstance` 各新增 nullable 列 `errorBeanData`（VARCHAR 4000，displayName「错误Bean数据」/ i18n-en「Error Bean Data」，propId step=54 / task=38）。
  - `./mvnw install -pl nop-task/nop-task-dao -am -DskipTests` regenerate：`_NopTaskStepInstance.getErrorBeanData()/setErrorBeanData(...)` + `_NopTaskInstance` 对应 getter/setter 已生成（Hard Stop：不手改 `_gen`）。
  - `DaoTaskStateStore.java`（`nop-task-dao`）4 出口 wiring：
    - **save**（step `copyStepStateToEntity` + task `saveTaskState`）：在 errCode + errMsg 提取之外，追加完整 ErrorBean JSON 序列化到 `errorBeanData`。`serializeErrorBeanData` 复制 ErrorBean 后用 `buildCauseErrorBean` 递归从 `throwable.getCause()` 补充 cause chain（`buildErrorMessage` 仅在存在 includeCause 映射时才填 cause，普通场景为 null），超 4000/序列化失败非致命跳过有 WARN 日志（#24 无静默跳过）。
    - **load**（step `toStepStateBean` + task `toTaskStateBean`）：`loadException` 优先从 `errorBeanData` 反序列化 ErrorBean 并经 `rebuildExceptionFromErrorBean` 递归构造含 params + cause chain 的 NopException；为空回退 errCode + errMsg（设计裁定 2，历史行兼容）；解析失败 WARN 日志回退。
    - **cause chain 重构（设计裁定 3 选项 b）**：`rebuildExceptionFromErrorBean` 将递归构造的 cause **直接传入构造器**（`new NopException(errorCode, cause, true, true)`），而非先 null-cause 再 `initCause`——`Throwable(String, Throwable, boolean, boolean)` 构造器即使传 null 也会标记 cause 已初始化，导致 `initCause` 抛 `IllegalStateException("Can't overwrite cause")`（调试中发现并修复）。变更限定在 nop-task-dao 模块内，不增强 `NopRebuildException.rebuild`（kernel public API，全仓库多处调用）。
- **Phase 2 交付（Proof——round-trip + compat + cross-restart E2E）**：
  - `TestDaoTaskStateStoreErrorBeanRoundTrip.java`（`nop-task-ext`，5 tests，真实 DB-backed `DaoTaskStateStore` + `@NopTestConfig(localDb=true)`，非 in-memory snapshot）：
    - `stepRoundTrip_preservesParamsAndCauseChain`：含 params + cause 的 NopException 经 save→DB entity（errorBeanData 非空含 mainParam）→load round-trip 保留 params key/value + getCause() 非 null + cause errorCode 匹配（设计裁定 4）。
    - `taskRoundTrip_preservesParamsAndCauseChain`：对称 step 级，task exception 经 saveTaskState→loadTaskState round-trip 保留 params + cause。
    - `stepLoad_fallsBackToErrCodeErrMsg_whenErrorBeanDataNull`：模拟历史行（save 后清空 errorBeanData）→ load 回退 errCode + errMsg 重构，行为与 plan 258/259 一致（无 params 无 cause）。
    - `stepSave_skipsErrorBeanData_whenTooLong_butKeepsErrCodeErrMsg`：超大 params 使 JSON 超 4000 → errorBeanData 跳过（null）但 errCode + errMsg 仍写入 → load 回退 errCode + errMsg（非静默失败）。
    - `crossRestartResume_rethrowsExceptionWithParamsAndCause`：fresh execute（FAILED with params + cause→终态 driver→saveStepState 持久化 errorBeanData）→ **fresh DaoTaskStateStore 实例**（模拟进程重启）loadStepState → resume 重抛 exception 含 params + cause chain——此诊断增量在 plan 258/259 后首次成立（#22 端到端）。
- **验证（四模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-task/nop-task-dao -am -T 1C` → **BUILD SUCCESS**：nop-task-ext 61（含新增 5 + 既有 TestDaoTaskStateStoreRoundTrip 3 零回归）/ nop-task-dao 136 / nop-task-core 全量，全部 0 failures / 0 errors。
  - `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-ai-agent 2714 全量 0 failures / 0 errors。
- **端到端验证（#22）**：fresh execute → step FAILED driver → saveStepState 序列化 errorBeanData 到 DB entity → fresh DaoTaskStateStore loadStepState 反序列化 ErrorBean → resume 重抛 exception 含 params + cause chain 完整路径连通（经真实 DB-backed entity JSON 序列化往返，非 in-memory snapshot 引用拷贝）。
- **接线验证（#23）**：`copyStepStateToEntity`/`saveTaskState` 真实写入 DB entity 的 errorBeanData（entity 字段断言可观测）；`toStepStateBean`/`toTaskStateBean` 真实读取 errorBeanData 并重构含 params + cause 的 exception。
- **Anti-Hollow（#22/#23/#24）**：round-trip 经真实 DB-backed entity 序列化证明 errorBeanData 经持久化存活；load 优先 errorBeanData 而非停留 errCode + errMsg；序列化失败/超长有 WARN 日志非静默吞掉；解析失败回退有日志；无空方法体/TODO/placeholder。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 DB 序列化细节优化，step/task public 执行契约不变）。
- **Roadmap**: `nop-ai-agent-roadmap.md` 新增 `exception-transient-optimization` 条目 ✅（此前为 6× carry-over Non-Goal，首次落地为独立 roadmap 行）。

---



- **Plan**: `ai-dev/plans/260-nop-ai-agent-terminal-killed-timeout-expired-driver.md`（Plan Status: planned → **completed**；Phase 1+2 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `EXPIRED-KILLED-TIMEOUT-terminal-driver`（carry-over, L4；5× carry-over 自 plans 254-259）。闭合 step `EXPIRED(50)`/`KILLED(70)` 与 task `KILLED(40)`/`TIMEOUT(60)` 终态的 read-but-never-written 缺口——常量已定义且被 `isDone()`/`isTerminal()`/resume 短路/reader 反复读取，但生产代码无任何 driver 写入；cancel 与 step-timeout 一律经 `driveTaskFailed` 坍缩为 FAILED，step cancel-check 只 rethrow 不设 status。
- **Phase 1 交付（Fix——step 层 EXPIRED/KILLED driver，设计裁定 1）**：
  - `NopTaskCancelledException.java`（`nop-task-core`）：从无 reason 单例 `NopSingletonException` 改为 reason-carrying `NopException` 子类（新增 `cancelReason` 字段 + `forReason(reason[, cause])` 工厂，保留 `INSTANCE` 兼容），使 cancel reason 能编码进传播到 task seam 的 exception（仍被 `isCancelledException` 第二档识别）。
  - `TaskStepHelper.java`：新增 `getCancelReason(err)`（cause-chain 解包取 reason）/ `resolveStepCancelReason(stepRt, err)`（exception 优先，回退 step token）/ `isTimeoutReason(reason)` / `encodeCancelReason(err, reason)`（把 reason 编码进 rethrow 的 exception）；`checkNotCancelled` 改抛 reason-carrying exception。
  - `TaskStepExecution.executeWithParentRt`：cancel-check 两处出口（async thenCompose err + sync catch）从 bare-rethrow 改为「按 reason 映射 `fail(err)`→`setStepStatus(EXPIRED/KILLED)`→`saveTerminalStateIfDone`→rethrow 编码了 reason 的 exception」（对称 plan 254 FAILED-driver，#24 非静默）。
- **Phase 1 交付（Proof）**：
  - `terminal-state-cancel-timeout/v1.task.xml` + `terminal-state-cancel-kill/v1.task.xml`（fixtures）：xpl step 抛 `NopTaskCancelledException(TIMEOUT/KILL)`。
  - `TestTaskStepCancelledTerminalDriver.java`（`nop-task-core`，5 tests）：timeout→EXPIRED(50) + kill→KILLED(70)（非 ACTIVE/FAILED）+ exception 捕获 + save count==2（接线 #23）+ 普通 FAILED 零回归 + reader 对 EXPIRED/KILLED 终态分支可达（非死代码）。
- **Phase 2 交付（Fix——task 层 KILLED/TIMEOUT driver + resume 区分，设计裁定 2/3）**：
  - `TaskErrors.java`：新增 `ERR_TASK_ALREADY_KILLED` + `ERR_TASK_ALREADY_TIMEOUT`（resume KILLED/TIMEOUT 合成 exception，非一律 already-failed）。
  - `TaskImpl.execute`：sync catch + async thenCompose err 出口从无条件 `driveTaskFailed` 改为 `driveTaskTerminal` 分发——cancellation 按 **Phase-1 exception-carried reason**（`getCancelReason`，非 `taskRt.getCancelReason()`/非 step token）二分 `driveTaskTimeout`(TIMEOUT(60)) / `driveTaskKilled`(KILLED(40))，非 cancellation 维持 `driveTaskFailed`(FAILED(50))，各捕获 exception + saveTaskState；resume else 短路区分 FAILED/KILLED/TIMEOUT（`synthesizeResumeException`，#24 非静默）。
- **Phase 2 交付（Proof——cross-restart E2E）**：
  - `TaskLevelSnapshotTaskStateStore.java`：新增 `preloadTaskSnapshot` 辅助合成 exception 路径测试。
  - `TestTaskKilledTimeoutResumeE2E.java`（`nop-task-core`，7 tests）：cancel→KILLED（fresh execute→snapshot KILLED 非 FAILED→fresh load→resume 命中 KILLED→mainStep 不重跑 save count 不变）+ step-timeout→EXPIRED+TIMEOUT（task TIMEOUT + step EXPIRED）+ resume 区分合成 exception（KILLED→ERR_TASK_ALREADY_KILLED / TIMEOUT→ERR_TASK_ALREADY_TIMEOUT / FAILED→ERR_TASK_ALREADY_FAILED 三者各别）+ in-progress 不误短路（零回归）+ pre-fix gap 证明（cancel/timeout 不再坍缩 FAILED）。
- **验证（四模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-task/nop-task-dao,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-core 86（79 baseline + 5 Phase1 + 7 Phase2 - 重叠）/ nop-task-ext（含 cancelled-step assertNotEquals(60) 兼容新 KILLED(70)）/ nop-task-dao / nop-ai-agent 全量，全部 0 failures / 0 errors。
- **端到端验证（#22）**：fresh execute → step cancel-check EXPIRED/KILLED driver（编码 reason 进 exception）→ task seam driveTaskTerminal 按 reason 二分 KILLED/TIMEOUT → saveTaskState 持久化 → fresh load（snapshot 非 in-memory 引用，recoverMode=true）→ resume 区分短路重抛 完整路径连通。
- **接线验证（#23）**：step cancel-check 出口 + task driver 出口真实调用 saveTerminalStateIfDone/saveTaskState（save count 可观测）；resume 命中时 mainStep 不重跑（save count 不变）。
- **Anti-Hollow（#22/#23/#24）**：E2E 经 snapshot 深拷贝 round-trip 证明 KILLED/TIMEOUT/EXPIRED 终态经持久化存活；cancel-check 不再 bare-rethrow；resume else 分支不再对 KILLED/TIMEOUT 合成 already-failed；无空方法体/静默跳过/no-op。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 task/step 终态 lifecycle wiring，step/task public 执行契约不变）。
- **Roadmap**: `nop-ai-agent-roadmap.md` `EXPIRED-KILLED-TIMEOUT-terminal-driver` 条目 ❌→✅。

---



- **Plan**: `ai-dev/plans/259-nop-ai-agent-task-level-resume-orchestration.md`（Plan Status: planned → **completed**；Phase 1+2 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `task-level-resume-orchestration`（carry-over from plan 258 §Non-Goals「完整 task-level resume 编排（successor plan required）」）。闭合 step 级终态状态机（plans 252-258）的 task envelope 层——使多 step task flow 在 crash-restart / cross-process resume 时，task instance 能被识别为已终态并短路（返回缓存 result / 重抛缓存 exception），而不是被无条件从 mainStep 重新执行。
- **Phase 1 交付（Fix）**：
  - `ITaskState.java`（`nop-task-core`）：新增 task 级终态访问器 `isTerminal()`/`isSuccess()`/`exception()`/`exception(Throwable)`（镜像 `ITaskStepState` 终态契约）。
  - `TaskStateBean.java`：修 `result(TaskStepReturn)` 空壳 no-op 为真实捕获 `setResultValue(result.getResult())`（Minimum Rules #24）；新增 transient `exception` 字段 + 实现 `exception()`/`exception(Throwable)`。
  - `ITaskRuntime.java`：新增 `isRecoverMode()` + `saveTaskState()` 接口方法（`TaskRuntimeImpl` 已有 `isRecoverMode()`，新增 `saveTaskState()` 委托 `stateStore.saveTaskState(this)`）。
  - `TaskImpl.execute`（`nop-task-core`）：实现 task 终态 driver + saveTaskState 接线。mainStep 成功 → `driveTaskCompleted`（setTaskStatus(COMPLETED) + result 捕获 + saveTaskState）；mainStep 抛错 → `driveTaskFailed`（setTaskStatus(FAILED) + exception 捕获 + saveTaskState）。3 个 driver 出口：thenCompose success（async COMPLETED）+ thenCompose error（async FAILED）+ catch（sync FAILED）。
  - `DaoTaskStateStore.java`（`nop-task-dao`）：扩展 `saveTaskState`/`loadTaskState`/`toTaskStateBean` 持久化 task exception（errCode/errMsg，镜像 step 级）+ result（remark JSON，非致命序列化）+ endTime（终态时）。
  - `TaskErrors.java`：新增 `ERR_TASK_ALREADY_FAILED`（resume FAILED 无缓存 exception 时的 fallback error code）。
- **Phase 1 交付（Proof）**：
  - `TaskLevelSnapshotTaskStateStore.java`（`nop-task-core` test，新增）：task 级 snapshot 语义 store（继承 `SnapshotTaskStateStore` step 级 snapshot），每次 saveTaskState 创建深拷贝 snapshot，模拟 DB round-trip。
  - `TestTaskTerminalStateLifecycle.java`（`nop-task-core`，6 tests）：COMPLETED driver 后 taskStatus==COMPLETED（非 ACTIVE）+ result 捕获 + saveTaskState 被调用；FAILED driver 后 taskStatus==FAILED + exception 捕获；接线验证 save count==1；fresh execute 行为（无 ACTIVE-time save）；幂等 upsert；snapshot round-trip（resume load 取回终态）。
- **Phase 2 交付（Fix + Proof）**：
  - `TaskImpl.execute` resume 短路（设计裁定 3）：resume 路径（recoverMode=true）下，`newMainStepRuntime()` 之前检查 `taskState.isTerminal()`；COMPLETED → 返回缓存 result（mainStep 不重跑）；FAILED → 重抛缓存 exception（非静默跳过）；in-progress → 正常 mainStep 重跑。
  - `TaskResumeSnapshotTaskStateStore.java`（`nop-task-ext` test，新增）：task 级 snapshot 语义 store for cross-restart resume E2E（继承 `SnapshotResumeTaskStateStore` step 级 snapshot + task 级 snapshot）。
  - `TestTaskLevelResume.java`（`nop-task-ext`，4 tests）：cross-restart resume COMPLETED（counter 首次=1/resume=0 返回缓存 result）+ FAILED（重抛 exception counter=0）+ pre-fix gap 证明（终态 save 使 snapshot 反映终态）+ in-progress（ACTIVE 非终态）不误短路（mainStep 重跑 counter=1）。
- **验证（四模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-task/nop-task-dao,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-core 74（68 baseline + 6 TestTaskTerminalStateLifecycle）/ nop-task-ext 56（52 baseline + 4 TestTaskLevelResume）/ nop-task-dao 全量 / nop-ai-agent 2714，全部 0 failures / 0 errors。
- **端到端验证（#22）**：fresh execute（saveState=true）→ mainStep 完成 → task 终态 driver → task 终态 saveTaskState 持久化 snapshot → fresh load（getTaskRuntime，snapshot 非 in-memory 引用，recoverMode=true）→ resume 短路判定 isTerminal → 返回缓存/重抛 完整路径连通。
- **接线验证（#23）**：终态 saveTaskState 在 E2E runtime 被 driver 出口调用（snapshot store task save count==1 可观测）；resume 短路命中时 mainStep 不被重跑（counter==0）。
- **Anti-Hollow（#22/#23/#24）**：E2E 经 snapshot 深拷贝 round-trip（非 in-memory 引用），证明终态 taskStatus 经持久化存活；`result()` no-op 已修复为真实捕获；无空方法体/静默跳过。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 task envelope lifecycle wiring，task public 执行契约不变）。
- **Roadmap**: `nop-ai-agent-roadmap.md` 新增 `task-level-resume-orchestration` 条目（L4-8 ✅）。

---

### plan-258: nop-task 终态 step-state DB 持久化（succeed/FAILED driver 后追加 saveStepState——cross-process resume 可观测终态）— Phase 1+2 完成（全 plan）

- **Plan**: `ai-dev/plans/258-nop-ai-agent-terminal-state-db-persist.md`（Plan Status: active → **completed**；Phase 1+2 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `dao-task-state-store-terminal-state-persistence`（carry-over from plan 257 §Closure Follow-up「DaoTaskStateStore 生产路径终态持久化」）。闭合 plan 257 reader 的 DB-side 终态前置——plan 257 reader 经 in-memory 引用语义通过 E2E，但生产 `DaoTaskStateStore` 经 DB round-trip（snapshot 语义）取回 ACTIVE → reader 空转。本计划在终态 driver 后追加 `saveStepState()` 使 DB snapshot 反映 COMPLETED/FAILED 终态。
- **Phase 1 交付（Fix）**：`TaskStepExecution.java`（`nop-task-core`）`executeWithParentRt` 在 succeed/FAILED 终态 driver 设置终态 stepStatus 后追加 `saveTerminalStateIfDone(stepRt)`（条件于 `isDone()==true`，设计裁定 3）：
  - 3 个终态 driver 出口：succeed-driver（COMPLETED，`:306`）+ FAILED-driver async（`:274`，thenCompose error 分支）+ FAILED-driver sync（`:333`，catch 分支）。
  - `saveTerminalStateIfDone` helper（`:354`）：`if (state != null && state.isDone()) stepRt.saveState();` → `stateStore.saveStepState(this)`（幂等 upsert 同一行，设计裁定 2；`DaoTaskStateStore.saveStepState` 按 taskInstanceId+stepPath upsert 已支持）。
  - ACTIVE-time save（`TaskStepExecution:237 stepRt.saveState()`）不变：fresh step 首次 ACTIVE 行写入不受影响（单元测试复核第 0 次 snapshot == ACTIVE）。
- **Phase 1 交付（Proof）**：
  - `SnapshotTaskStateStore.java`（`nop-task-core` test，新增）：snapshot 语义 store（每次 save 创建深拷贝 snapshot，非 in-memory 引用）——模拟 DB round-trip，使 driver 之后对 live bean 的 mutate 不影响已保存 snapshot，暴露 plan 258 要修的 gap。记录 save 历史 + 计数（接线验证 #23）。
  - `TestTerminalStatePersistence.java`（`nop-task-core`，6 tests）：succeed-driver 后 snapshot COMPLETED（非 ACTIVE）+ resultValue；FAILED-driver 后 snapshot FAILED（非 ACTIVE）+ exception；接线验证 save count=2（ACTIVE 1 + 终态 1）；ACTIVE-time save 第 0 次 snapshot=ACTIVE（零回归）；幂等 upsert 同一 stepPath。
  - fixture：`terminal-state-persist-success/v1.task.xml` + `terminal-state-persist-failed/v1.task.xml`。
- **Phase 2 交付（Proof）**：
  - `SnapshotResumeTaskStateStore.java`（`nop-task-ext` test，新增）：snapshot 语义 store for DB-backed resume E2E（与 plan 257 `ResumeCapableTaskStateStore` 引用语义对照，闭合 carry-over production gap）。
  - `TestDbBackedTerminalStateResume.java`（`nop-task-ext`，3 tests）：DB-backed resume E2E。COMPLETED 变体：execute→终态持久化→fresh snapshot load→reader 命中 isDone→step body 不重调用（counter=0）→返回缓存 result；FAILED 变体：execute→终态 FAILED 持久化→fresh load→reader 重抛 exception（非静默跳过，#24）；pre-fix gap 独立证明：snapshot[0]=ACTIVE（isDone false）先于 snapshot[1]=COMPLETED（isDone true），证明若无终态 save resume load 取回 ACTIVE → reader 不跳过。
- **验证（三模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-core 68（62 baseline + 6 TestTerminalStatePersistence）/ nop-task-ext 52（49 baseline + 3 TestDbBackedTerminalStateResume）/ nop-ai-agent 2714，全部 0 failures / 0 errors。
- **端到端验证（#22）**：task execute 入口 → step 完成 → 终态 driver → 终态 saveStepState 持久化 snapshot → fresh load（snapshot，非引用）→ reader isDone true → 跳过 step body / 重抛 exception 完整路径连通（snapshot round-trip 闭合 plan 257 production gap）。
- **接线验证（#23）**：终态 saveStepState 在 E2E runtime 被 driver 出口调用（snapshot store save 计数 ACTIVE 1 + 终态 1 = 2 可观测）。
- **Anti-Hollow（#22/#23/#24）**：E2E 经 snapshot 深拷贝 round-trip（非 in-memory 引用），证明终态 mutate 经持久化存活；无空方法体/静默跳过（`saveTerminalStateIfDone` 真实调 `saveState()`，FAILED reader 真实重抛 exception）。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 DB 持久化 wiring，step public 执行契约不变——fresh step 行为零变更，resume 是 plan 257 已交付的 reader 能力的 DB-side 终态前置）。

---



- **Plan**: `ai-dev/plans/257-nop-ai-agent-task-step-continuation-skip-reader.md`（Plan Status: planned → **completed**；Phase 1+2+3 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `task-step-continuation-skip-reader`（carry-over from plans 252-256，5× 一致裁定为「successor plan required」）。闭合 plans 252-256 状态机 write-side 的 read-side 消费方——使 `ITaskStepState.isDone()/result()` 首次被 production 消费（reader），resume/re-execution 时检查终态跳过 step body 返回缓存 result（COMPLETED）或重抛 exception（FAILED）。
- **Phase 1 交付（Fix）**：`DaoTaskStateStore.java`（`nop-task-dao`）从全 stub（`loadStepState`/`saveStepState`/`newStepState` 等均 return null / 空方法体）重写为真实 DB-backed 实现（extends `AbstractDaoHandler`）：
  - 基于既有 `NopTaskInstance` + `NopTaskStepInstance` entity（`nop_task_step_instance` table 已有全部所需列），**无新增 ORM entity/table**（非 Protected Area）。
  - `saveStepState`：查 findStepEntity(taskInstanceId, stepPath)，找到则 update / 未找到则 create `NopTaskStepInstance`，持久化 stepStatus + resultValue（JSON→stateBeanData VARCHAR 4000）+ exception（ErrorMessageManager 提取 errCode/errMsg）。`saveEntityDirectly/updateEntityDirectly`。
  - `loadStepState`：QueryBean 查询 (taskInstanceId, stepPath) → `toStepStateBean` 重构 `TaskStepStateBean`（stepStatus + resultValue 从 JSON 反序列化 + exception 从 errCode/errMsg 重构 NopException）→ `state.afterLoad(taskRt)`。
  - `newStepState`/`newMainStepState`/`newTaskState`：构造可用 bean（不再 return null）。
  - resume load plumbing 已就绪：`TaskStepRuntimeImpl.newStepRuntime:127` 已调 `loadStepState`，store 返回非 null → `recoverMode=true` + 使用 loaded state（非回退 fresh bean）。
  - 新增 `ERR_TASK_STEP_ALREADY_FAILED`（`TaskErrors.java`）用于 reader FAILED 重抛 fallback。
- **Phase 2 交付（Fix）**：`TaskStepExecution.java`（`nop-task-core`）`executeWithParentRt` 在 step body（`step.execute`）**之前** wiring continuation-skip reader：
  - 检查 `stepRt.getState().isDone()`；COMPLETED → 返回 `state.result()`（缓存 result，step body 跳过）+ 设 RESULT + nextStepName；FAILED → 重抛 `state.exception()`（非静默跳过，设计裁定 4）；fresh（isDone==false）→ 不短路 → step body 正常执行（设计裁定 5 防误跳过）。
  - 这是 plans 252-256 状态机 write-side（succeed/COMPLETED + FAILED driver）的 read-side 消费方。
- **Phase 3 交付（Proof）**：
  - `TestDaoTaskStateStoreRoundTrip.java`（`nop-task-ext`，3 tests）：DaoTaskStateStore save→load round-trip（COMPLETED: isDone+isSuccess+result 非 null；FAILED: isDone+!isSuccess+exception 非 null；fresh: loadStepState 返回 null 回退 newStepState）。local DB（`@NopTestConfig(localDb=true)`）。
  - `TestTaskStepContinuationSkipReader.java`（`nop-task-core`，5 tests）：reader 单元（COMPLETED 跳过返回缓存；FAILED 重抛；fresh 不跳过；ACTIVE 不跳过；isDone 真值表防 over-matching）。经 `PreloadedStateTaskStateStore`（extends DefaultTaskStateStore）预载终态 state。
  - `TestStepContinuationSkipResume.java`（`nop-task-ext`，2 tests）：resume E2E（真实 save→load→skip cycle）。COMPLETED 变体：首次 counter=1 → resume counter=0 + 返回缓存 "OK"；FAILED 变体：首次 counter=1 抛异常 → resume counter=0 + 重抛。经 `ResumeCapableTaskStateStore`（in-memory 引用语义 save→load round-trip，设计裁定 6 允许）。
  - 新增 fixture：`continuation-skip-reader/v1.task.xml`（nop-task-core）、`resume-failed-step/v1.task.xml`（nop-task-ext）。
  - pom：`nop-task-ext` 新增 `nop-task-dao` test 依赖（Phase 1 round-trip 测试需要 DaoTaskStateStore 实体）。
- **验证（三模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-core 62（57 baseline + 5 TestTaskStepContinuationSkipReader）/ nop-task-ext 49（37 TestReliabilityDecorators + 3 TestDaoTaskStateStoreRoundTrip + 2 TestStepContinuationSkipResume + 6 TestTaskFlowDemo + 1 TestTransactionDecorator）/ nop-ai-agent 2714，全部 0 failures / 0 errors。
- **端到端验证（#22）**：从 `.task.xml` execute → step 完成 → `saveStepState` 存引用 → succeed/FAILED driver 原地 mutate 终态 → resume 新 runtime → `TaskStepRuntimeImpl.newStepRuntime:127 loadStepState` 取回终态 → reader `isDone()` true → 跳过 `step.execute` → 返回缓存 result / 重抛 exception 完整路径连通。
- **接线验证（#23）**：reader 在 resume E2E runtime 路径被调用（counter 首次=1、resume=0 证明 step body 被跳过）。
- **Closure（独立 subagent audit APPROVED）**：read-only explore agent（task `ses_12411091cffeGC6CTERgtzuxz7`，独立 task_id）逐条复核 8 audit items 全 PASS：DaoTaskStateStore save/load 非 stub（3 处 return null 均为合法 not-found 守卫）/ reader 在 step.execute 之前 / resume E2E 真实 save→load→skip / Anti-Hollow 端到端调用链连通 / 10/10 plan tests PASS。
- **Roadmap**: `task-step-continuation-skip-reader`（L4-8）新增 ✅ 行 in `nop-ai-agent-roadmap.md`。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 state load + reader 机制，step public 执行契约不变——fresh step 行为零变更，resume 是新增能力）。

---

### plan-256: nop-task isCancelledException 穿透 xpl 包装识别 cancellation — Phase 1+2+3 完成（全 plan）

- **Plan**: `ai-dev/plans/256-nop-ai-agent-xpl-cancellation-wrap-preserve.md`（Plan Status: planned；Phase 1+2+3 completed → 全 plan 完成）
- **Work Item**: `xpl-cancellation-wrap-preserve`（carry-over from plan 254 §Closure Follow-up「xpl 方法调用包装穿透 cancellation 字符 ——独立 successor」）。
- **Phase 1 交付（Fix）**：`TaskStepHelper.java`（`nop-task-core`）`:74-94` cause-chain 解包修复：
  - 原 `isCancelledException`（`:72-80`）三档检查均针对传入异常 `e` 自身，不递归 `getCause()` → 当 `e` 是 `NopEvalException`（xpl `wrapInvokeException` 包装）时全部 false。
  - 修复：抽取出 `isCancelledExceptionSingleLevel(e)` 保留原三档逻辑不变；`isCancelledException` 改为遍历 cause chain——`while (e != null) { if (visited.add(e)) break (cycle guard); if (singleLevel(e)) return true; e = e.getCause(); }`。
  - 防循环（设计裁定 3）：`Set<Throwable> visited = Collections.newSetFromMap(new IdentityHashMap<>())`，自引用 cause（`initCause(this)`）命中 `visited.add` 返回 false → 立即 return false。
  - 覆盖**全部** xpl 包装路径：consumer 侧解包不依赖 producer 侧包装点，任何 `getCause()` 链含 cancellation 类型/errorCode 均被识别。
- **Phase 1 交付（Proof）**：`TestTaskStepHelperIsCancelledException`（`nop-task-core`）从 5 tests 扩展到 **9 tests / 0 fail**：
  - 原有 5（plan 254 真值表）：CancellationException / NopTaskCancelledException / ERR_TASK_CANCELLED errorCode → true；ordinary NopException / ordinary RuntimeException → false（零回归）
  - 新增 4（plan 256 cause-chain）：`xplWrappedCancellation_isCancelled_plan256`（`NopEvalException(ERR_EXEC_INVOKE_METHOD_FAIL, cause=NopTaskCancelledException)` → true，核心新行为）/ `xplWrappedOrdinaryException_notCancelled_plan256`（包装非 cancellation → false，防 over-matching）/ `deeplyNestedCancellation_isCancelled_plan256`（≥2 层包装 → true，防循环 + 深度）/ `xplWrappedCancellationException_isCancelled_plan256`（`NopEvalException` 包装 `CancellationException` → true，cause 第一档变体）
  - 使用真实 `ERR_EXEC_INVOKE_METHOD_FAIL`（`io.nop.xlang.XLangErrors`）构造包装异常，精确复现 `AbstractObjFunctionExecutable.wrapInvokeException` 产出形态
- **blast-radius 评估**：grep 全仓 `isCancelledException` 消费方 = 3 个（`TaskStepExecution` `:230`/`:280` + `SelectorTaskStep` `:43`/`:84` + `TryTaskStepWrapper` `:46`/`:69`），全部经 helper 非直接 instanceof。grep `instanceof NopTaskCancelledException` 仅 helper 内 1 处（无额外 bypass 消费方）。`FutureHelper.isCancellationException`（`nop-api-core`）是独立 generic utility，与 task step cancel 流程无关。「更多异常被识别为 cancelled」对 3 消费方影响可接受：3 处均语义「cancellation 穿透/取消，非普通失败处理」，修复前因不解包 cause 误判为普通失败，修复后行为正确化。
- **验证**：`./mvnw test -pl nop-task/nop-task-core -am -T 1C` → **BUILD SUCCESS**：nop-task-core 57 tests（含 `TestTaskStepHelperIsCancelledException` 9 + `TestTaskStepStateBeanExceptionPersistence` 6 + `TestTaskFlowManager` 25 等全部既有测试）0 failures / 0 errors。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部 helper 行为，step/cancel public 执行契约不变，cancelled step 仍抛出、仍不产生 KILLED 终态）。

### plan-256 Phase 2 — E2E cancelled-step 不误判 FAILED

- **Phase 2 交付（Proof）**：`TestReliabilityDecorators.cancelledStep_notMarkedFailed_cancelExclusion`（`nop-task-ext`）从「记录 gap、不严格断言」改为严格断言 cancelled step 经 xpl 包装后**不被标记 FAILED**：
  - 保留 `runTask("test/cancelled-step")` 抛异常（cancelled exception 经 cancel-check throw 传播，cancel != success）。
  - 新增核心断言：`assertNotEquals(Integer.valueOf(60), stepState.getStepStatus())` —— 修复前此值为 60（FAILED-driver 误标记），修复后保持 ACTIVE(10)。
  - 注释块从「xpl 包装使 cancellation 字符丢失」「端到端不可行」改为描述修复后正确行为（cause-chain 解包使命中 cancel-check → throw 跳过 FAILED-driver）。
- **验证（三模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-ext -am -T 1C` → **BUILD SUCCESS**：nop-task-ext 42（含 `TestReliabilityDecorators` 35，`cancelledStep_notMarkedFailed_cancelExclusion` 通过）/ 0 failures。
  - `./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-ai-agent 2714 / 0 failures。
  - nop-task-core Phase 1 基线（57）不变（Phase 2 未触及 core）。
- **端到端验证（#22）**：从 `.task.xml` cancelledStep 经 xpl `<source>` 调 `sim.throwCancelled()` → `doInvoke*` 包装为 `NopEvalException(ERR_EXEC_INVOKE_METHOD_FAIL, cause=NopTaskCancelledException)` → `TaskStepExecution` sync catch cancel-check 调 `isCancelledException`（解包 cause → 命中 → true）→ `:281` throw（跳过 `:291-292` FAILED-driver）→ stepStatus != FAILED 完整路径连通可观测。
- **接线验证（#23）**：`isCancelledException` cause-chain 解包在 runtime 被 `TaskStepExecution` cancel-check 调用（E2E 断言 stepStatus != 60 可观测——修复前为 60）。

### plan-256 Phase 3 — composite step cancellation 传播 E2E（Selector + Try）

- **Phase 3 交付（Proof）**：2 个新 `.task.xml` fixture + 2 个新 E2E 测试（`TestReliabilityDecorators`，35→37 tests）：
  - **SelectorTaskStep cancellation 传播**（`composite-selector-cancelled/v1.task.xml` + `selector_cancelledBranch_propagatesCancellation_plan256`）：selector 第一分支 `cancelledBranch` 经 xpl 调 `sim.throwCancelled()` 抛 cancellation → 包装 → 子 step TaskStepExecution cancel-check（cause-chain 解包 → true）throw → `SelectorTaskStep:42` catch → `isCancelledException:43`（cause-chain 解包 → true）→ `:44` throw（不尝试 `fallbackBranch`）。断言：task 抛异常（修复前 selector 视普通失败 → 尝试 fallbackBranch → 返回 "FALLBACK" 不抛）、`counter()==1`（fallbackBranch 未执行——若执行则 counter==2）、`cancelledBranch.getStepStatus() != 60`（cancellation 识别非 FAILED）。
  - **TryTaskStepWrapper cancellation 穿透**（`composite-try-cancelled/v1.task.xml` + `try_cancelledStep_propagatesThroughCatch_plan256`）：xpl step `tryCancelledStep` 带 `<catch>` catchAction，经 xpl 调 `sim.throwCancelled()` 抛 cancellation → 包装 → `TryTaskStepWrapper:65` catch → `:69 !catchInternalException && isCancelledException`（cause-chain 解包 → true）→ `:70` throw（catchAction 跳过）。断言：task 抛异常（修复前 catchAction 被触发吞掉 cancellation → 返回 "SWALLOWED" 不抛）、`counter()==1`（catchAction 未触发——若触发则 counter==2）、`tryCancelledStep.getStepStatus() != 60`。
  - fixture 中 `fallbackBranch` / `<catch>` catchAction 均 `inject("testExecutionCounter").incrementAndGet()` 作为执行标记，使「未尝试下一分支 / catchAction 未触发」可由 counter 量化观测（#24 无静默跳过：断言具体执行计数）。
- **验证（三模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-core 57 / nop-task-ext 44（`TestReliabilityDecorators` 37 = 35 baseline + 2 Phase 3）/ nop-ai-agent 2714，全部 0 failures / 0 errors。
- **端到端验证（#22）**：selector / try 路径从 `.task.xml` 声明 → step 经 xpl 调用抛 cancellation → doInvoke 包装 → SelectorTaskStep/TryTaskStepWrapper 调 `isCancelledException`（解包 cause → true）→ cancellation 正确传播（selector 不尝试下一分支 / try catchAction 跳过）完整路径连通。
- **接线验证（#23）**：`isCancelledException` cause-chain 解包在 runtime 被 `SelectorTaskStep:43` + `TryTaskStepWrapper:69` 调用（E2E 断言 cancellation 传播行为可观测——修复前 catchAction 触发 / selector 视普通失败，修复后穿透/取消）。

---

### plan-255: nop-task TaskFlowAnalyzer nextOnError 校验字段错配 bug 修复 — 全 plan 完成 + closure

- **Plan**: `ai-dev/plans/255-nop-ai-agent-task-flow-analyzer-nextOnError-validation.md`（Plan Status: planned → **completed**；Phase 1 completed + Exit Criteria 全勾选 + Closure Gates 全勾选 + Closure Evidence 已写入）
- **Work Item**: `task-flow-analyzer-nextOnError-validation-bug`（L4-8，carry-over from plan 254 §Non-Blocking Follow-ups + §Closure Follow-up「`TaskFlowAnalyzer.checkStepRef` nextOnError 校验 bug ——独立 successor」）。修复 `TaskFlowAnalyzer.checkStepRef` 中 `nextOnError` 引用校验的**字段错配** bug，使 task flow 错误处理路由（`nextOnError`）的 analyze-time 模型校验层与运行时行为（plan 254 `nextStepNameOnError`）一致。
- **Phase 1 交付（Fix）**：`TaskFlowAnalyzer.java`（`nop-task-core`）`:57` 字段错配修复：
  - `nextOnError` 分支 `hasStep` 实参从 `subStep.getNext()`（bug）改为 `subStep.getNextOnError()`（正确字段）
  - 进入条件 `:56` `getNextOnError() != null` 与错误参数 `:61` `ARG_NEXT_STEP = getNextOnError()` 一直正确，仅 `:57` 的 hasStep 实参错配
  - 修复前两类语义错误：**假阴性**（合法 nextOnError + null next → `hasStep(null)` 返回 false → 误抛 `ERR_TASK_UNKNOWN_NEXT_STEP`，阻塞所有「只设 nextOnError、不设 next」的合法错误处理配置）+ **假阳性**（无效 nextOnError + 合法 next → `hasStep(next)` 返回 true → 静默放行，运行时路由到不存在 step 才暴露）
  - `next` 分支（`:47-54`）/ `waitSteps`（`:65-74`）/ `waitErrorSteps`（`:76-85`）校验零变更
- **Phase 1 交付（Proof）**：**`TestTaskFlowAnalyzer`（nop-task-core 新增单元，5 tests / 0 fail）**：
  - `nextOnError_valid_andNextNull_analyzePasses`：合法 nextOnError + null next → analyze 不抛错（假阴性回归）
  - `nextOnError_invalid_andNextValid_analyzeRejects`：无效 nextOnError + 合法 next → 抛 `ERR_TASK_UNKNOWN_NEXT_STEP`，`ARG_NEXT_STEP == "nonexistent"`（假阳性回归）
  - `nextOnError_valid_distinctFromNext_analyzePasses`：合法 nextOnError 与 next 不同 → 不抛错
  - `next_valid_analyzePasses`：合法 next → 不抛错（`next` 分支零回归）
  - `next_invalid_analyzeRejects`：无效 next → 抛错（`next` 分支零回归）
- **验证（全模块零回归）**：
  - `./mvnw test -pl nop-task/nop-task-core,nop-task/nop-task-ext,nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**：nop-task-core 53（+5 TestTaskFlowAnalyzer vs plan 254 基线 48）/ nop-task-ext 42（含 plan 254 `nextStepNameOnError_routedFailure_failedDriver` E2E 零回归）/ nop-ai-agent 2714，全部 0 failures / 0 errors
- **Closure（独立 subagent audit APPROVED）**：read-only explore agent（task `ses_12493a2b7ffeyDixbix4SHnZmP`，独立 task_id）逐条复核 Exit Criteria 全 PASS：`:57` 字段修复确认 + Anti-Hollow 真实字段引用 + focused 测试覆盖三类场景 + 三模块 surefire 聚合全绿 + 无既有测试断言 buggy 行为（唯一 nextOnError E2E fixture `next-step-on-error-failure/v1.task.xml` 同时设 next+nextOnError 指向同一合法 step，从不触及 bug 失败模式）。
- **Roadmap**: `task-flow-analyzer-nextOnError-validation-bug`（L4-8）新增 ✅ 行 in `nop-ai-agent-roadmap.md`（plan 254 行列出的「TaskFlowAnalyzer nextOnError 校验 bug successor」已闭合）。
- **Doc-sync（裁定）**：`No owner-doc update required`（内部校验 bug 修复，task flow public 契约不变，校验语义从「错误」收敛为「正确」）。
- **Closure 语义**：闭合 plan 254 §Non-Blocking Follow-ups「TaskFlowAnalyzer nextOnError 校验 bug ——独立 successor」。nextOnError 路由现在 analyze-time 校验与运行时消费（plan 254 `TaskStepExecution` 错误分支）字段一致。`nextOnError` 专属错误码（当前复用 `ERR_TASK_UNKNOWN_NEXT_STEP`，optimization candidate）/ xpl 包装穿透 cancellation（nop-xlang successor）/ continuation-skip / afterLoad-beforeSave / EXPIRED-KILLED / 跨重启持久化（DB-backed state persistence successor）均为显式 Non-Goals。

---
