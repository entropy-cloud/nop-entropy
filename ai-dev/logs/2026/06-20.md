### plan-278: nop-ai-agent——引擎资源生命周期、恢复与委派边界（AR-02/04/05/09/10）

- **Plan**: `ai-dev/plans/278-nop-ai-agent-engine-resource-lifecycle-recovery-delegation-bounds.md`（4 Phases 全 done；Phase 1-3 此前已 landing，本轮收口 Phase 4 + closure）。
- **Phase 1 AR-04（code+test）**：三个执行入口（doExecute/resume/restore）把 `createActor` 与 `autoBindTeam` 一起移进现有内层 try，使 createActor/autoBindTeam 失败落入对称清理（handle/actor/lock/心跳四重释放），sessionId 不再被永久"砖"。
- **Phase 2 AR-02（code+test）**：`resumeSession` 在 `denialLedger.reset(sessionId)` 旁补 `postDenialGuard.reset(sessionId)`（置于 tenant-scoped try 内），恢复路径真正可恢复——同指纹调用不再被 pre-dispatch 立即拦截、不再 3 轮内 re-pause。
- **Phase 3 AR-05（code+test）**：`CallAgentExecutor` 委派递归深度守卫——经 `AgentMessageRequest.metadata` 专用 key `__nopAiAgent.delegationDepth` 跨 agent 边界传递（非经 `AgentToolExecuteContext`），`MAX_DELEGATION_DEPTH=4`（可配 setter），超深返回 `errorResult`（非 StackOverflowError）。设计文档 `delegation-bounds.md` 已新建。
- **Phase 4 AR-09+AR-10（code+test，本轮 closure）**：
  - **AR-09 引擎生命周期**：`IAgentEngine extends AutoCloseable` + `@Override default void close() throws Exception {}` no-op（~32 in-tree test stub 源码兼容）；`DefaultAgentEngine.close()` override——`AtomicBoolean closed` 幂等守门、仅关闭自创建池（`ownLockRenewExecutor`/`ownAgentExecutor` 标志区分）、不动外部注入池、不取消在途执行、best-effort（RuntimeException 记 WARN 不重抛）。
  - **AR-10 cache 有界化**：`ICheckpointManager` 增加 `default void remove(String sessionId) {}` 幂等 no-op；`FileBackedCheckpointManager.remove` 清五个 cache（bySession/byWatermark/snapshotCache/saveCounters/loadedSessions，on-disk journal/snapshot 不删）；`ToolExecutionCheckpoint.remove` 清 bySession + 对应 byWatermark。引擎**仅终态**调用：三入口 inner finally `if (isTerminalStatus(session.getStatus())) checkpointManager.remove(sessionId)` + cancel-without-handle 分支（`:~2013`）对称调用；**paused 非终态不清理**（保留 checkpoint 供 restoreSession 恢复）。`isTerminalStatus` = completed/failed/cancelled/forced_stopped/escalated/truncated（不含 paused）。
- **测试**：`TestEngineLifecycleAndCheckpointBounded`（10 tests）——close 自创建池 isShutdown / 外部注入池不被关闭 / close 幂等 / completed 触发 remove / paused 不触发 remove（resume 后终态才触发）/ FileBacked.remove 清五 cache / ToolExecution.remove 清 cache / cancel-without-handle 触发 remove / 多 session byWatermark 不无界 / IAgentEngine stub 继承 default close 编译通过。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**（nop-ai-agent 全模块全绿，零回归）。
- **契约变化**：`IAgentEngine extends AutoCloseable`（新增 default `close()`）；`ICheckpointManager` 新增 default `remove(String)`。均为向后兼容的 default 方法扩展（不破坏现有实现者源码）。
- **Doc-sync**：设计 `engine-lifecycle.md`（IAgentEngine.close + ICheckpointManager.remove 契约 + terminal-only 调用门）已新建；`design/nop-ai-agent/README.md` 已加引用；audit tracker 已补 AR-02/04/05/09/10 ✅ 行；roadmap `Last Updated` 已同步。

---



### plan-277: nop-ai-agent——ReAct 循环消息契约与生命周期语义正确性（AR-03/06/07/11/12/13/14）

- **Plan**: `ai-dev/plans/277-nop-ai-agent-react-loop-message-contract-and-lifecycle-semantics.md`（4 Phases 全 done）。
- **AR-03+AR-11（Phase 1，code+test）**：修复 `ReActAgentExecutor` 中破坏 LLM `tool_call_id` 配对契约的两类缺陷。(1) 结果处理 for 循环 re-enter 分支的 `break` 改为 `continue`（BEFORE）/设标志位（AFTER），不再丢弃同批后续 tool 结果；循环后注入 iteration-level re-enter marker user 消息。(2) input guardrail block 从注入孤儿 `role:"tool"`（id=`guardrail-block-input`）改为注入 `ChatAssistantMessage`；output guardrail block 从孤儿 tool 消息改为对每个**真实** tool_call_id 回灌 blocked tool response（有 tool_calls 时）或 mutate assistant content（无 tool_calls 时）。新增测试：`reenterInBatchDoesNotDropOtherToolResults`（N=3 batch）、`afterToolResultProcessedReenterAddsMarkerMessage`、`multiToolBatchMaintainsPairingEndToEnd`（PairingValidatingChatService stub）；更新 `TestContentGuardrailInReActLoop` 断言。
- **AR-06（Phase 2，code+test）**：`reentryCounters` 从 per-execute 局部 map 移入 reactLoop body（per-iteration 重置），消除长会话中合法 re-enter hook 在累计 3 次后静默降级。`DEFAULT_MAX_REENTRIES` 补 Javadoc。新增测试：`reentryCounterResetsPerIteration`（5 iterations 全部兑现）、`reentryCounterForcesPassAfterMaxReentriesWithinIteration`（单 iteration >3 降级）。
- **AR-07+AR-14（Phase 3，code+test）**：(1) 新增 `AgentEventType.SESSION_ESCALATED`，`handleGoalStuck` 改用它（不再复用 `SESSION_PAUSED`）。(2) 新增 `AgentExecStatus.truncated`，达 max-iterations 时设 `truncated`（不再静默改写为 `completed`）；post-loop gate 排除 `truncated`（不发布 EXECUTION_COMPLETED / 不跑 POST_CALL）；`DefaultAgentEngine.isTerminalStatus()` 含 `truncated`。消费者审计：所有 `!= completed` 消费者（`DefaultAgentEngine:845`/`CallAgentExecutor:408`/`MemberFanOutDispatcher`/`MemberAgentTaskStep`/`SpawnMemberAgentTaskStep`）正确处理 truncated→non-success。更新 `TestReActAgentExecutor.testMaxIterationsReached` + `TestSustainerWiring` + `TestSisypheanSustainerEndToEnd` + `TestForcedStop` 断言。新增测试：`goalStuckPublishesSessionEscalatedNotPaused`、`truncatedSessionDoesNotPublishExecutionCompleted`。
- **AR-12+AR-13（Phase 4，test-only）**：(1) `hookFailureAtPrePointIsLoggedNotSwallowed` 从注册 POST_REASONING 改为 PRE_REASONING，断言 status=failed（验证 PRE re-throw）。(2) 两个 re-enter 测试从 `reenterCount >= 1`（弱断言）改为断言 ctx 中存在 re-enter 合成消息 / marker 消息（证明 re-enter 被兑现）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**（2766 tests，0 failures，0 errors）。
- **契约变化**：新增 `AgentExecStatus.truncated`（public enum 新值）、`AgentEventType.SESSION_ESCALATED`（public enum 新值）。max-iterations 终态从 `completed` 变为 `truncated`（downstream `!= completed` 消费者行为不变——truncated 被视为 non-success）。
- **Doc-sync**：`nop-ai-agent-react-engine.md` §5.4 重入语义 + §重入语义段已更新；`nop-ai-agent-reliability.md` STUCK abort 事件 + terminal 列表已更新；`nop-ai-agent-security-and-permissions.md` post-loop gate 排除列表已更新。

---



- **Plan**: `ai-dev/plans/276-nop-ai-agent-carryover-batch-r2-sandbox-threatmodel-roadmap-reconcile-team-members.md`（合并三项独立 carry-over；Phase 1+2+3 全 done，独立 closure audit）。
- **env-source（P2，Phase 1，docs/analysis-only）**：完成 plan 274 显式 deferred 的 `SandboxRequest.environmentVariables` 来源链路追溯。grep 固化证据：main src **0 生产构造点**（仅 `SandboxRequest.java:108` Builder.build 自身，其余 26 处全在 test fixture）、`ISandboxBackend.execute` 在 main src **从未被调用**（`DefaultAgentEngine`/`ReActAgentExecutor` 仅存储 `sandboxBackend` 字段不调用 `execute`）→ env 键当前**非 LLM 可控**。威胁模型结论：plan 274 fail-closed POSIX 键校验定位为 defense-in-depth（为 shell-exec/code-exec 工具执行器接入 sandbox 的独立 successor 预留的正确性），无活跃攻击面。结论固化：设计 `nop-ai-agent-security-and-permissions.md` §7.1 新增「威胁模型注记（plan 276）」段落（覆盖 (a)–(d) 四点）+ roadmap §5c 新增 `env-source` 行（标 ✅ 已追溯）+ plan 276 内固化的 Trace Evidence 块 + plan 274 末尾新增 `## Follow-up handled by 276-*` 可追溯链接。
- **roadmap-§2.2-stale（P3，Phase 2，docs-only）**：修正 `nop-ai-agent-roadmap.md` 与 §4/live repo 的全面不一致——§2.1 M4b（`agent.register-model.xml` 已存在）❌ 缺失 → ✅ 已完成；§2.2 表前说明删除"所有运行时代码均为零实现"过时表述（grep 0 命中）；§2.2 表逐行对齐 §4（ReAct 引擎/Agent Engine/AgentSession/IPermissionProvider/IToolCallRepairer/IContextCompactor/IContentGuardrail/IModelRouter/IRetryPolicy/单元测试 等全部 ❌ → ✅，注明对应 §4 Layer/plan）；§7 审计检查清单修正 7 处与 §4 矛盾的过时 `[ ]` 标记（register-model/LLM 路径/AgentModel 装载/ReAct 闭环/示例文件/xdef 稳定性）；顶部 `> Updated:` 行记录本次修正。
- **15-1（P3，Phase 3，code）**：`Team.members` 类型收敛——`Team.java` 字段（`:54`）/构造参数（`:68`）/getter 返回类型（`:95`）从 `Map<String, TeamMember>` 窄化为 `ConcurrentMap<String, TeamMember>`（import 同步：移除未用的 `java.util.Map`，新增 `java.util.concurrent.ConcurrentMap`），Javadoc 线程安全契约与类型一致。生产构造点 `InMemoryTeamManager:137`（已传 `ConcurrentHashMap`）零破坏；`DbTeamManager:804` 局部变量声明类型 `Map`→`ConcurrentMap`（对象已是 `ConcurrentHashMap`，必要且最小的静态类型同步，+import）；测试构造点 `TestTeamExecuteFlowExecutor:506` 的 `Collections.emptyMap()`（非 ConcurrentMap 实例）迁移为 `new ConcurrentHashMap<>()`（+import）。新增 focused 测试 `TestTeam`（3 tests：`getMembers()` instanceof ConcurrentMap / 返回 manager 持有的 live map / null 拒绝）。消费点核查：`InMemoryTeamManager:220/247/273` 的 `(ConcurrentHashMap) team.getMembers()` 下转仍合法，`NoOpTaskMemberRouter:126`/`InMemoryTeamManager:338` 的 `Map` 消费点因 ConcurrentMap 是 Map 子接口仍兼容。源 plan 272/275 末尾新增 `## Follow-up handled by 276-*`（15-1 收口链接）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**（nop-ai-agent 全模块全绿，零回归）；`-Dtest=TestTeam,TestTeamSpec,TestInMemoryTeamManager,TestDbTeamManager,TestTeamExecuteFlowExecutor,TestSteeringInjection` → 76 tests 全绿（含新 TestTeam 3 tests）。注：全量首跑 `TestSteeringInjection` 有 1 次 order-dependent flaky failure，单独/定点重跑稳定通过（9 tests 0 failures），与本次 docs/code 变更无关（Phase 1/2 纯 markdown 不影响 Java 测试）。
- **契约变化**：`Team.getMembers()` 返回类型 `Map`→`ConcurrentMap`（narrowing，`ConcurrentMap` 是 `Map` 子接口，既有 `Map` 消费点仍兼容）。
- **Doc-sync**：设计 §7.1 注记 + roadmap §2.1/§2.2/§5c/§7 已同步；`No owner-doc update required beyond Javadoc`（设计文档未细化 `Team.members` 字段类型）。

---



- **Plan**: `ai-dev/plans/275-nop-ai-agent-record-mapping-scope-fix.md`（carry-over 20-5 from plan 272；Phase 1+2 全 done）。
- **问题**：`src/main/resources` 的 `agent-plan.register-model.xml` 声明了 `.agent-plan.md` loader，其 `class` 指向 test-scope 的 `nop-record-mapping` 模块（`io.nop.record_mapping.md.MarkdownDslResourceLoaderFactory`）。生产环境（classpath 无 nop-record-mapping）`RegisterModelDiscovery` 捕获 `NoClassDefFoundError` 后 `LOG.warn(nop.register-model.ignore-invalid-loader)` 静默跳过——用户放入 `.agent-plan.md` 不报错也不加载（Silent No-Op，plan guide Lesson #9）。
- **修复（方案 B，test-scope 不变）**：
  - main `agent-plan.register-model.xml`：移除 `.agent-plan.md` loader 条目，仅保留 `.agent-plan.xml`/`.agent-plan.yaml` 的 `xdsl-loader`（框架提供，不依赖 test-scope）。
  - `agentPlan.record-mappings.xml`：从 `src/main/resources/_vfs/nop/record/mapping/` 迁移到 `src/test/resources/_vfs/nop/record/mapping/`（test 运行时 VFS 合并 main+test classpath，mappingName 解析路径不变）。
  - 新增 test `agent-plan-md.register-model.xml`（`name="agent-plan"`，与 main 同名但**文件名不同**避免 VFS 同路径去重），声明 `.agent-plan.md` loader。`RegisterModelDiscovery.discover()` 按 `name` 聚合 + `DeltaMerger` 按 `fileType` key 合并，使 md loader 仅在 test classpath 追加注册。
- **偏差**：plan 步骤 4 原述"新建同名 agent-plan.register-model.xml + x:post-extends"。实际 VFS 对同路径文件做 `addResourceIfAbsent` 去重（`DeltaResourceStoreBuilder:145`），同文件名会被替换而非合并。改用**不同文件名**（`agent-plan-md.register-model.xml`）+ 同 `name` 属性 + plain loaders 声明，由 `discover()` 的 `DeltaMerger` 按 `fileType` key 追加——更稳健且符合平台 register-model 聚合机制。`x:post-extends` 非必需（DeltaMerger key-attr 合并已达成追加语义）。
- **测试**：新增 `TestAgentPlanRegisterModelScope`（3 tests：main register-model 不含 test-scope 类引用 / test register-model 携带 md loader / `.agent-plan.xml` 生产格式端到端加载）+ 新增 `test/test-agent-plan.agent-plan.xml` 测试数据。既有 `TestAgentPlanMarkdownLoader`（md 端到端）、`TestAgentPlanRecordMapping`（mapping）、`TestAgentModelLoading` 均通过。
- **验证**：`grep -rn "record_mapping\|record-mapping\|MarkdownDsl" src/main/resources/` → **0 命中**（exit 1）；`./mvnw clean test -pl nop-ai/nop-ai-agent -T 1C` → **BUILD SUCCESS**（2756 tests，0 failures）。
- **Phase 2（文档同步 + 全量构建）**：plan 272 末尾新增 `## Follow-up handled by 275-*` 可追溯链接；roadmap M4a + §7 检查清单修正 `.agent-plan.md` 描述为 dev/test-only（生产格式 `.agent-plan.xml`/`.yaml`）；`./mvnw clean install -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**。
- **Doc-sync**: No owner-doc update required（main→test 文件迁移不改变 public contract，`nop-record-mapping` 维持 test scope，模块依赖关系不变）；roadmap scope 描述已同步。

---



- **Plan**: `ai-dev/plans/274-nop-ai-agent-docker-sandbox-flag-correctness.md`（bundle dim-13 剩余两项；Phase 1+2 全 done）。
- **AUDIT-13-8（`--cpus` 语义错配）**：`SandboxConfig` 控制 `--cpus` 的公共契约从"CPU 秒级预算（int，默认 30）"改为"CPU 核心数配额（double，默认 1.0）"——`DEFAULT_CPU_SECONDS`→`DEFAULT_CPU_CORES`、字段 `cpuSeconds`(int)→`cpuCores`(double)、`getCpuSeconds()`→`getCpuCores()`、`Builder.cpuSeconds(int)`→`Builder.cpuCores(double)`；构造校验从 `<=0` 改为 `!(cpuCores > 0)`（同时拒绝 0/负/NaN）。`DockerSandboxBackend.buildDockerCommand` 的 `--cpus` 取值从 `Integer.toString(config.getCpuSeconds())` 改为 `Double.toString(config.getCpuCores())`（如 `1.0`/`1.5`/`0.5`）。旧默认 `--cpus 30`（=30 核心配额，资源限制形同虚设）→ 新默认 `--cpus 1.0`（一个核心）。`DockerSandboxBackend`/`NoOpSandboxBackend` "Resource limits mapping" Javadoc 同步。
- **AUDIT-13-9（env 键注入防护）**：`buildDockerCommand` 在拼接 `-e KEY=VALUE` 前对每个环境变量键做 POSIX 名校验（`ENV_KEY_PATTERN = ^[A-Za-z_][A-Za-z0-9_]*$`，静态预编译），非法键（`--privileged`/`1ABC`/`A B`/`A=B`/空串/控制字符等）在 `docker` 进程启动前 fail-closed 拒绝（抛 `SandboxException`）。新增专用 `SandboxFailureReason.INVALID_ENVIRONMENT_VARIABLE`（与 `HOST_PATH_NOT_ALLOWED` 并列，语义="请求在 Docker 调用前因校验失败被拒绝"，沿用 plan 270 `validateHostPath` 先例）。值不校验（单 argv 元素，`=` 切分后值可含任意字符）。`DockerSandboxBackend` 类级 Javadoc 新增「Environment overlay mapping」段说明校验语义。
- **测试**：`TestDockerSandboxBackend` 26 tests（新增 `cpusFlagCarriesFractionalCoreCountQuota`、`cpusFlagDefaultsToOneCore`、`buildCommandAcceptsPosixValidEnvKeys`、`buildCommandRejectsFlagLikeEnvKeyBeforeDockerStarts`、`validateEnvKeyHelperAcceptsAndRejects`；迁移 `buildCommandIncludesAllResourceLimitFlags` 与 conditional integration 的 `.cpuSeconds`→`.cpuCores`）；`TestNoOpSandboxBackend` 13 tests（迁移 `wallTimeTimeoutKillsRunawayProcess`/`defaultsMatchDesignTable`/`sandboxConfigRejectsNonPositiveLimits`，新增 cpuCores=0/NaN fail-closed 断言）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**（2753 tests，0 failures）；`-Dtest=TestDockerSandboxBackend,TestNoOpSandboxBackend` → 39 tests 全绿。grep 验证 `cpuSeconds`/`getCpuSeconds`/`DEFAULT_CPU_SECONDS` 在 `nop-ai-agent` 模块 main+test 中 **0 命中**。
- **Doc-sync**：设计 `nop-ai-agent-security-and-permissions.md` §7.1 资源限制表 CPU 行（`cpuSeconds`|30 → `cpuCores`|1.0）+ 实现约定补录 `INVALID_ENVIRONMENT_VARIABLE` 失败分类；roadmap §5c line 358 `AUDIT-13-8` ⏸ deferred → ✅ 已修复 + 新增 `AUDIT-13-9` ✅ 行。

---



- **Plan**: `ai-dev/plans/273-nop-ai-agent-takeover-lock-heartbeat-renewal.md`（carry-over 14-06 from plan 271；Phase 1+2 全 done）。
- **Phase 1（心跳续期调度 + cancel-on-cleanup 接线）**：
  - `DefaultAgentEngine`：新增 `lockRenewIntervalMs` 字段（默认 `600_000L`=10min，租约 30min 的安全 1/3 分数）+ getter/setter；`<=0` 禁用续期（向后兼容逃生舱）。
  - 新增专用 `lockRenewExecutor`（单线程守护 `ScheduledExecutorService`，线程名 `nop-ai-agent-lock-renew`，lazy init + setter 注入），不复用 `agentExecutor`（非 Scheduled 类型）。
  - 新增 helper：`startLockRenewal(handle, sessionId, ownerId)`（`scheduleWithFixedDelay` 调 `tryRenew`，参照 `ScheduledRecoveryManager.scanOnceSafe` 的 try-catch 范式，续期异常记 WARN 不杀调度）+ `renewOnceSafe` + `cancelLockRenewalQuietly`（null-safe cancel(false)）。
  - 三个入口点（`doExecute`/`resumeSession`/`restoreSession`）：`tryAcquire`+`putIfAbsent` 成功后启动续期任务（handle 上新增 `volatile Future<?> renewHandle` 字段，供 supplyAsync lambda 捕获——解决 effectively-final 约束）；三个 release 路径（putIfAbsent-fail catch / inner finally / outer catch）均 `cancelLockRenewalQuietly(handle.renewHandle)`，与 `releaseLockQuietly` 对称。
  - `NoOpSessionTakeoverLock` 默认下续期任务无害 no-op（`tryRenew` 无条件返回 true），零行为回归，不触发 insecure-default WARN。
- **Phase 2（lease-lost 中止 + Javadoc/owner-doc 同步 + 聚焦测试）**：
  - `handleLeaseLost(handle, sessionId, ownerId)`：续期任务检测到 `tryRenew` 返回 false 时，`ctx.setLeaseLost(true)` + `ctx.setCancelRequested(true)` + `ctx.setCancelReason("takeover lease lost")` + `handle.thread.interrupt()`（复用现有 cancel/abort 机制，prompt 终止），WARN 日志。
  - `AgentExecutionContext`：新增 `volatile boolean leaseLost` + getter/setter（renewal 调度线程写、执行线程 cleanup finally 读，volatile 保证可见性）。
  - 三个入口点的 inner finally：`session.setStatus(ctx.isLeaseLost() ? AgentExecStatus.failed : ctx.getStatus())`——lease-lost 时强制 session 进入 `failed` 终态（执行器的 cancel 路径会置 cancelled，引擎层 override 为 failed——lease-lost 是系统级 double-execution 防护失败，非用户 cancel）。
  - Javadoc 同步：`ISessionTakeoverLock.tryRenew` + class-level「Lease/TTL」段 + `DbSessionTakeoverLock.tryRenew` 段——移除所有「Reserved for manual / future use」「explicit successor」措辞，改为「引擎执行期间按 `lockRenewIntervalMs` 周期自动调用」（plan 273）。
  - Owner-doc 同步：`docs-for-ai/03-modules/nop-ai.md` 可靠性配置表新增 `lockLeaseMs` + `lockRenewIntervalMs` 两行；`docs-for-ai/04-reference/source-anchors.md` `AIREL-001` anchor 更新含 `lockLeaseMs`/`lockRenewIntervalMs`/`getLockRenewExecutor`/`startLockRenewal`/`handleLeaseLost`。
- **测试**：新增 `TestTakeoverLockHeartbeatRenewal`（6 tests）：Phase 1 的 `doExecuteRenewsPeriodicallyAndCancelsOnCompletion`/`resumeSessionRenewsPeriodicallyAndCancelsOnCompletion`/`restoreSessionRenewsPeriodicallyAndCancelsOnCompletion`/`disabledRenewalIntervalSkipsScheduler`（`CountingTakeoverLock` 计数真实 tryRenew，short interval=20ms + slow chat=150ms）；Phase 2 的 `tryRenewExtendsLeaseExpiry`（H2 直查 `LOCK_EXPIRES_AT` 断言续期推后 + 非 owner renew 返回 false）+ `leaseLostAbortsExecutionAndMarksSessionFailed`（`LeaseLosingLock` tryRenew 恒返回 false + 阻塞 chat service，断言 lease-lost 后 session=failed 且续期停止、执行 prompt 终止）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **BUILD SUCCESS**（全模块全绿，零回归）；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（52 pre-existing BROKEN_LINK warnings 均在其他历史 plan 文件，非本次引入）。
- **Doc-sync**：`docs-for-ai/03-modules/nop-ai.md` + `docs-for-ai/04-reference/source-anchors.md` 已更新；Javadoc（`ISessionTakeoverLock.java` + `DbSessionTakeoverLock.java`）已同步。

---

