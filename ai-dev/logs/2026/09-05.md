# 2026-09-05

### 2026-09-05 nop-format 全模块深度缺陷检查与修复（23 项，红测试→修复→绿）

- **产出**：分析报告 `ai-dev/analysis/2026-09/2026-09-05-nop-format-deep-bug-hunt.md`（23 项确认缺陷修复：4×P1 / 12×P2 / 7×P3；另 12 项记录待办、1 项高疑似经回归证伪为设计约定）
- **方法**：6 个并行深读通道覆盖 nop-format 全部约 780 个非生成源文件，候选缺陷主会话逐项亲读源码/接口契约/既有测试复核后修复；每项先写红测试（临时 `git show HEAD:file` 还原修复验证失败形态）再修复转绿
- **代表性 P1**：`SubBinaryDataReader.subInput` 委托 underlying 不推进父 position（嵌套定长区域双重 skip，流式 reader 从首条记录起数据错位）；`RecordAggregateState` 分页 off-by-one（pageSize=N 每页仅 N-1 条，pageSize=1 文件开头多一个空页脚）；`MultiLineConfigParser` 三引号不消费结束分隔符（`"""` 语法完全不可用，下游 ExcelToXptModelTransformer/RuleTableModelParser 受益）；`ExcelDataValidation.sqref` 按 `,` 解析/拼接而 OOXML ST_Sqref 为空格分隔（读 Excel 多区域校验抛 convert-to-type-fail，写出生成非法 xlsx）；`ColorHelper.rgb` 不置 alpha（6 位 hex 经 `new Color(argb,true)` 全透明，`#000000` 与 0 哨兵混淆）；nop-pdf `TableCellBlock` 构造器丢弃 span 参数 + `TableBlock.getCell/getRowCells` 对 ProxyCell 盲转 CCE（任何含合并单元格的表格使整条 PDF 解析管线失败）；tablesaw `findRowArea` 对未写单元格 break（中间空列后整列丢失）与顺序依赖的类型合并（`[19.99,20]` 整列降级 STRING）
- **红证据**（节选）：分页 `expected: aaabbb2 cccddd2 eee1 但实际 aaa1 bbb1 ccc1...`；sqref `convert-to-type-fail value=10 B1:B10`；writeDouble 修复前测试代码编译失败（`不兼容的类型: 从double转换到float`）；PDF `NullPointerException page()`/`IndexOutOfBoundsException`；tablesaw `expected DOUBLE but was STRING`；chart `expected [99,50] but was [1,50]`；markdown `expected a$x$b$y but was a$x$by`（丢 `$`）
- **重要回退（误报证伪）**：`TreeTableDataParser.parseNextFields` 的 `j < maxColIndex` 初判 P1 off-by-one，修复后 `TestExcelHelper.testApi` 回归失败——`test.api.xlsx` 配置 sheet 以最后一列为文档化"说明"注解列，模板生态依赖"不进入最后一列"语义。已回退并补注释固化约定 + 新增 `testLastColumnTreatedAsAnnotationColumn` 锁定行为
- **绿灯验证**：`nop-format` 聚合全量 17 个模块 BUILD SUCCESS（756 tests, 0 failures）；受影响 9 个模块已 install；下游 `nop-report-core`/`nop-rule-core`（消费 MultiLineConfigParser/ExcelDataValidation）回归通过
- **测试资产**：新增/扩展 15 个测试类约 40 个用例 + 4 个 record fixture（test-nested-region / test-pagination / test-fixed-collection-streaming / tablesaw xlsx 生成式测试）
- 偏差：markdown 修复重写 `normalizeMathNode`（首次实现漏推进 prev 指针致插入乱序，由既有 testNormalize 捕获后修正）；分页 fixture 的对象级 `typeRef` 无解析逻辑，改内联 fields
- Doc-sync: No owner-doc update required（nop-format 无专属 docs-for-ai 文档；报表/规则模块文档未涉及本次修正的行为契约）；分析报告已归档 `ai-dev/analysis/2026-09/`

### 2026-09-05 nop-network 文件传输与 MQTT 消息能力实现（设计→实现→测试）

- **设计**：新建子系统目录 `ai-dev/design/nop-network/`（README + `file-transfer-design.md` + `mqtt-messaging-design.md`，已登记 design README 索引）。核心决策：①上传双模式由 `UploadOptions.mode` 显式选择（BINARY 默认=自约定协议 PUT + `x-file-*` 头 + octet-stream 流式；BASE64_FORM=标准 multipart 文本字段），拒绝 AUTO 自动协商；②下载续传走 RFC 9110 Range 语义，`.part` 临时文件即续传状态、完成后摘要校验+原子 rename，206 追加/200 整体重传/416 删 part 重下；③校验来源优先级：显式 > `x-content-sha256` 响应头（兼容 `x-amz-checksum-sha256`）> sidecar `{url}.sha256/.sha1`（Maven/Node 风格），流式双摘要（SHA-256+SHA-1）、续传先重读 .part 预热（拒绝摘要状态序列化——不可移植）；④协议公共逻辑收敛 `nop-http-api`（`FileTransferHelper`/`FileDownloadSink`，零新增依赖），三客户端只做 IO 执行，保证跨实现行为等价（.part 布局/错误码一致）；⑤MQTT：`sendAsync`=QoS1 下行 publish、`MqttSessionManager` 订阅感知（接管/清理随订阅登记）、`MqttTopicMatcher` 规范通配（`+`/`#` 含父层）、`MqttServerMessageService` 双向桥（下行按订阅路由、上行分发 `IMessageConsumer`，payload 为 UTF-8 文本）。拒绝项：Tus/S3 分片（需服务端状态机）、EventBus 转发（引入集群语义）、MessageSendOptions 塞 QoS（污染跨模块契约）等均记录拒绝理由。
- **实现**：`nop-http-api`（UploadMode/选项扩展/协议头/错误码/helper/sink）+ 三客户端 uploadAsync/downloadAsync（apache：`ProgressFileEntityProducer` 直接实现 AsyncEntityProducer——httpcore5 的 writeData/streamEnd 是包私有无法跨包继承；jdk：`ProgressBodyPublisher`；okhttp：`ProgressRequestBody` ForwardingSink 计数）+ MQTT 五个类。nop-http-api 为 nop-*-api 公共模块：全部为**新增字段/类**，既有构造与调用兼容；upload/download 入参 null 守卫 IllegalArgumentException。
- **测试**（新增 5 类 24 用例全绿）：apache `TestFileTransfer` 11（双模式/自定义方法/全量下载/预置 .part 续传且仅请求剩余区间/无 Range 服务端重传/416 恢复/sha 头不匹配失败且无残留/sidecar/requireChecksum/进度）；jdk 5（等价协议矩阵+进度+不匹配清理）；okhttp 4（等价矩阵，修一个真 bug：计数 sink 未 flush 致字节滞留）；`TestMqttTopicMatcher` 4（含 `a/#` 匹配父层 `a`——实现期发现并修复）；`TestMqttMessageRouting` 4（下行 publish/断连报错/按订阅路由下发/上行 subscribe 分发与取消）。实现期另修复：发布监听快照缺陷（晚注册订阅收不到分发→改为运行时读实时列表）、mqtt pom test-scope nop-core 覆盖传递 compile 依赖问题。
- **回归**：http+vertx 8 模块与下游 nop-ai-core（379）/rpc-http/rpc-core 全绿 BUILD SUCCESS；三个既有测试的 UnsupportedOperationException 断言随能力落地更新为 null 守卫断言。
- Doc-sync: 新增 `docs-for-ai/03-modules/nop-network.md`（使用规范）并登记 `INDEX.md` + `source-anchors.md`（NET-001~003）；设计文档措辞与实现对齐（payload 传递契约）；链接检查 0 错误（期间清除 docs-for-ai→ai-dev 边界违规引用 3 处）。
- 偏差：BASE64_FORM 的 base64 文本整体构入请求体（设计文档已声明内存权衡，未设硬上限）；OkHttp sidecar 探测在响应线程上阻塞等待（30s 超时，dispatcher 64 线程下可接受）。

### 2026-09-05 nop-network 全模块深度缺陷审查与修复（44 项，红测试→修复→绿）

- **产出**：分析报告 `ai-dev/analysis/2026-09-05c-nop-network-deep-bug-review.md`（46 项确认缺陷：P0×14 / P1×22 / P2×10；44 项修复 + 2 项记录待议）
- **方法**：4 个并行深读通道覆盖 codec/http/netty/rpc/socket/vertx 全部约 150 个源文件 + 主会话对每项疑似逐一复核源码/依赖字节码（httpcore5 5.3.6 javap、OkHttp 4.12、JDK HttpClient 源码）/调用方 grep；复核推翻 1 项误报（RpcChannelState.prepareSend closed 复检无害）
- **根因在 nop-commons 的 2 项**（各仅 1 个仓库内调用方，grep 核实后修复）：`BinaryScalarType.fromText` 静态块 key 用 `textMap.toString()`（全部 proto 标量类型解析失效）；`RetryPolicy.withRetryDelay` 调 `setMaxRetryDelay`（netty 重连退避参数全部错配）
- **代表性 P0**：OAuth 取 token 首次必 NPE（局部变量未赋返回值）；SSE `LineAsyncDataConsumer` 丢弃 `resultCallback`（execute future 永不完成）；`ConcatenatedBodyPublisher` 重复 onSubscribe（JDK multipart 上传 100% 失败）；OkHttp GET 带 body 必抛 IllegalArgumentException；`ContextHttpServerFilter` 把 nop-timeout 剩余时长当绝对时间（带超时头的请求立即"过期"）；`MessageRpcClient` 未设超时的请求立即超时 + null id 触发 ConcurrentHashMap NPE；netty 重连无视 -1 停止信号（实测 2.4s 内 7338 次热循环）；MQTT 认证器从未调用 + 会话永不清理 + KeepAlive=0 连接即判死；SocketServer stop 后假活不可重启
- **红灯证据**：26 个新测试类约 70 个断言在修复前全部按预期失败（每项失败形态与缺陷机理吻合，如重连风暴计数、`abc==` cookie 丢失、`BufferOverflowException` 替代 IOException）
- **绿灯验证**：nop-commons（268 测试）+ nop-network 全部 17 个子模块 + 下游 nop-stream-runtime（1059）/nop-ai-core（379）/nop-biz（48）/nop-record-netty 全绿 BUILD SUCCESS；受影响模块已 install
- **行为变更说明**：①SSE 派发改按规范"空行触发"，未知字段（retry: 等）不再派发为伪事件、`onComplete` 幂等；②未实现的 `uploadAsync/downloadAsync/sendAsync/subscribe` 统一改抛 `UnsupportedOperationException`（原返回 null 致调用方 NPE）；③`JdkHttpClient.toMultipart` 对不支持的字段类型显式抛错（原静默丢弃）；④netty 服务器始终维护 channelGroup（stop 时关闭存量连接）
- **遗留待议**（报告"遗留问题"节）：MQTT doStart 不等待 listen（生命周期语义决策）；DefaultClientIpFetcher 的 XFF 最左可伪造/Forwarded 多条目解析（部署信任模型，安全语义变更应单独立项）
- 偏差：OkHttp GET-with-body 在 UTF-8 默认字符集的 JDK 上无法本地红灯（JEP 400 后默认 UTF-8），JDK SSE charset 测试为回归守卫而非红证据；`MqttMessageBuilder` payload 拷贝与压缩器异常路径 close 为代码级修复（无独立红测试）
- Doc-sync: 无 docs-for-ai owner doc 需要更新（nop-network 无专属使用文档；SSE/HTTP 客户端行为契约未见于 docs-for-ai，若后续有使用者反馈可补 `03-modules/`）

### 2026-09-05 执行 plan 349：nop-task 分析报告 P0-P3 缺陷全量修复（红测试→修复→绿）

- Plan: `ai-dev/plans/349-nop-task-analysis-remediation.md`（来源：同日 nop-task 模块分析报告；用户指示对所有问题先写红测试再修复，含 task.xdef 拼写修正授权）
- **SUSPEND 契约化**（8 处破坏点一并收口）：`isSuspend()` 身份判断→哨兵名值判断（与 isEnd/isExit 对偶）；TaskImpl 出口新增 SUSPEND 分支（置 SUSPENDED+saveTaskState，不 runCleanup/不 endTask，闭合 check2 两条 P0）；BuildOutput 挂起透传；retry/doRetry 对 SUSPEND 跳过 succeed 且真取消不计次；Sequential/Selector 异步回调补 suspend 检查；fork/fork-n/parallel 聚合扫描传播挂起（删除 AbstractForkTaskStep 死代码）；graph 节点挂起终结图并停止级联；挂起点追加 saveState（stateBean/bodyStepIndex 落盘，resume 从挂起点续跑）
- **状态码对齐**（新发现 P0）：`TaskConstants` 以 ORM 字典为准对齐（SUSPENDED=10/ACTIVE=30/COMPLETED=40/TIMEOUT=50/FAILED=60/KILLED=70，step ACTIVE=30），删除死常量 HISTORY_BOUND；一致性守卫测试锚定 `_NopTaskCoreConstants` 与字典字面值；终态守卫（TaskImpl.driveTask* + TaskStepStateBean.succeed/setStepStatus first-terminal-wins）
- **图模式**：waitError 依赖成功→按跳过级联（修复挂死）；error-handoff 识别（TaskStepExecution 把 nextOnError 失败包装为成功返回，图层还原为失败级联，check2 P1 错误边双根因闭合）；无错误消费者维持 fail-fast；runningCount==0 兜底 completeExceptionally；GraphStepAnalyzer 重名步骤校验（新错误码 ERR_TASK_DUPLICATE_STEP_IN_GRAPH，zh-CN/en i18n 同步）；TaskFlowAnalyzer if/then/else 改递归（孙级校验可达）
- **wrapper 不变量**（取消/超时必须有限时间终结）：ExecutorTaskStepWrapper 取消兜底 completeExceptionally（cancelToken.appendOnCancel）；timeout 改异步竞速（超时强制 TIMEOUT 取消异常终结，同步快路径不变）+ wrapper 恢复外层 token；sleep 取消抛 NopTaskCancelledException；TaskStepRuntimeImpl.cancelToken 加 volatile
- **边界守卫**：fork producer null→空集合；forkN count<=0→空聚合；LoopN 异步 until 改 CONTINUE（修复 END 语义不一致）+ exit 携带 outputs；parallel 聚合未完成分支补 CANCELLED 占位；MultiStepResultBean setter 改替换语义；TaskStepReturn.of 显式 next 优先；nextOnError 失败步骤 resume 改走错误分支（修复重抛拦截）；resume 路径初始化 TaskFlowMetricsImpl；newChildRuntime 取消传播自引用修复；resetGlobalStats 补限流器
- **xdef 修正 + 恢复完整性**：`persisVars`→`persistVars`（官方 xgen 链路再生 4 个 _gen 模型类，非手改；需先 install 更新后的 nop-xdefs）+ retry 注释/persist 注释修正；persistVars 全链路接线（builder→runtime→saveState 捕获→恢复回写）；DaoTaskStateStore stateBeanData 改版本化 wrapper（resultValue/stateBean/outputs/nextStepName/persistVars 同列，旧格式向后兼容，全空清列修残留）；TaskStepStateBean.getStateBean 对 Map 形态按类型还原（loop/fork 状态恢复）；continuation-skip 重放 outputs+动态跳转；`_dao.beans.xml` 条件注册 DaoTaskStateStore（闭合 check2 P3 装配缺口）
- **验证**：新增 TestSuspendContract（7 用例）+ TestPlan349Fixes（19 用例）+ FullSnapshotTaskStateStore/FakeStepRt 测试基建 + 12 个 task fixture + TestDaoTaskStateStoreStateDataWrapperRoundTrip（4 用例，localDb）。红验证：stash 法将全部主代码修复还原后 26/26 新测试变红且失败形态逐条吻合（NPE/挂死/UNKNOWN_NEXT_STEP/状态错位等），恢复后全绿；`./mvnw test -pl nop-task-core,nop-task-dao,nop-task-ext,nop-task-service` 全绿（125+117 等）；既有 TestTaskStepStateBeanLifecycle 一处覆盖方式调整（同 bean 穿越四终态→每态独立 bean，配合终态守卫裁定，覆盖意图不变）
- Doc-sync: `docs-for-ai/03-modules/nop-task.md` 新增「运行时状态码」「挂起与恢复语义（含 fork 跨进程 resume 边界）」「图模式错误边语义」三节；check2 `nop-task.md` 19 条全部回写处置标注（17 修复 + 2 暂缓：fork stepPath 行竞争、限流器参数固化）
- 偏差与 audit 修正：3.4 重名步骤校验无独立红绿测试（xdef key-attr 与 KeyedList.addAllUnique 双重去重使重复状态不可达，校验为纵深防御，plan 已改写裁定）；closure audit 指出 5.9 告警可用既有 IRateLimiter.getPermitsPerSecond() 实现（初判"无访问器"有误）——已补全：computeIfAbsent 后比较速率不一致 LOG.warn；6.7 的 task 输入持久化已实现（closure audit 指出实体已有 taskInputs 列"逻辑流参数"，serialize request→列 + load 恢复对称），仅 taskVars 真无对应列维持暂缓（不新增 ORM 列）；另按 audit 修正 4 处过期状态码注释编号（TaskImpl/TaskStepHelper）、补 suspend-parallel 专属 fixture 用例
- Commit: 见 git log plan 349 提交（nop-task 全模块 + nop-xdefs/task.xdef + _gen 再生 + docs/ai-dev）

### 2026-09-05 nop-task 模块全面质量分析（bug / 易用性 / 语法定义）

- **产出**：`ai-dev/analysis/2026-09/2026-09-05-nop-task-module-analysis.md`（Status: open）
- **方法**：3 个专项审查（step 并发实现 / 状态持久化与上层模块 / API 可用性）+ 主会话逐条回读源码验证关键断言；与 check/check2 两轮审计交叉比对去重。
- **新发现要点**（两轮审计均未覆盖，关键 P0 已主会话独立复核）：①DB 状态码双体系数值错位——引擎写 `TaskConstants`（COMPLETED=30），ORM 列绑定 `ext:dict="task/task-status"` 按 40=已完成 解读，同引擎内 task 级用 TaskConstants、step 级终态用 `_NopTaskCoreConstants` 混用；②SUSPEND 语义未契约化——`TaskStepReturn.isSuspend()` 为对象身份判断（:211-213），8 处实现点各自破坏挂起（含 `AbstractForkTaskStep.java:101-105` 死代码：if 两分支返回值相同）；③GraphTaskStep waitErrorSteps 在被等待步骤成功时 waitFuture 永不完成（图挂死）；④ExecutorTaskStepWrapper 取消后 ret 永不完成 + timeout 只取消 token 不终结 promise（超时后仍可挂死）；⑤nextOnError 与 plan 254 FAILED 终态 driver 矛盾（resume 后错误分支不可达）；⑥终态无守卫可被并发覆盖；⑦task inputs/taskVars 不持久化；⑧TransactionalTaskStepWrapper 副作用提交与终态保存非原子。
- **已知未处置确认**：check2 19 条基本仍存活（plan 346 未清账）；工作区两处未提交修复（TaskImpl metrics 判空、GraphTaskStep runningCount 级联顺序）复核均正确。恢复能力总体评价：saveState/resume 仅对"平铺 sequential + 同步步骤 + 单值输出"窄面可靠，loop/fork/call-task/if/choose/suspend 恢复场景存在副作用重复或状态错误风险。
- **易用性/语法结论**：DSL 表达力同类第一梯队（23 种步骤、声明式 decorator、断点恢复、XDSL 继承）；短板为 Java 入口三步仪式、service 层空壳无生命周期 API（实例表可被通用 CRUD 绕过状态机改写）、魔法变量（RESULT/STEP_RESULT(S)）无 schema 化、xdef 五处定义缺陷（persisVars 拼写/role vs roles/persist 默认值相反/retry 注示矛盾/executor vs bean 命名）、新旧语法三写法并存、错误边语法领先于实现可靠性。
- **修复优先级建议**：P0=SUSPEND 契约化+状态码对齐+metrics NPE；P1=waitError/取消不变量/nextOnError/终态守卫；立项="DB 断点续跑完整性"单一设计定案（check/check2 已两轮重复发现同位置问题，不宜再零散打补丁）；产品化=service 生命周期 action + DaoTaskStateStore 默认装配。
- 验证：本任务为分析（不改产品代码），关键断言逐条附文件:行号并经主会话回读源码确认（TaskStepExecution:252、TaskImpl:159、TaskConstants:79-91、_NopTaskCoreConstants、task-status.dict.yaml、_app.orm.xml:185、TaskStepReturn:211-213、BuildOutputTaskStepWrapper:51、AbstractForkTaskStep:101-105）
- Doc-sync: 无需更新 owner doc（分析报告；修复类 owner doc 更新待对应 plan 执行时进行）

### 2026-09-05 执行 plan 347：nop-tcc / nop-wf 遗留缺陷修复（全部 3 Phase 完成）

- Plan: `ai-dev/plans/347-nop-tcc-wf-remaining-defects.md`（对抗性审查 agent_04ae1694 采纳 3 Major + 全部 Minor 后执行）
- **Phase 1**（nop-tcc，8 项；closure audit 补修 checkTransactionActive null 守卫）：`checkExpiredTransactions` 补偿超时保护（expireGap 上限，超时 WARN 继续下一条）；`TccStatus.fromCode` 边界保护 + `fromCode(Integer)` 重载 + 全局/分支两侧 null 状态安全（null 分支排除并 WARN，脏数据不参与决策不崩溃）；`beginCancelAsync` 专用错误码 `ERR_TCC_INVALID_CANCEL_BRANCH_STATUS`；task 抛 Error 时先 `endAsync` 补偿再原样重抛、registry 恢复；TRY_FAILED 记录失败响应 errorCode；分支清理改为随父记录（按已完结父 txnId 集合 in 删除，消除孤儿分支）；状态更新改 `updateByQuery[id+from状态]` 单语句原子 CAS（from 在内存修改前捕获，冲突 0 行 WARN 跳过，终态不被 stale writer 覆盖）；分支 retryTimes 补偿尝试计数（store 内 cast，不扩 api 接口）
- **Phase 2**（nop-wf，8 项）：`<retry>` 契约落地（`WfStepModel.getRetry()` 首次被引擎消费：maxRetryCount/retryDelay/maxRetryDelay/exponentialDelay 按毫秒实现 + exception-filter 谓词含 `$exception` 变量）；execCount 接通（语义裁定：source 执行计数，取代 ORM 列注释中从未实现的 exec-group 锁规划，注释对齐记入 follow-up）；`handleError` 的 onError XPL 异常保护（不掩盖原始异常）；`guessWfNameFromFilePath` 无分隔符显式报错；`removeStdStartParam` 安全转换 + 缺省不覆盖已设值；`getAllowedActions` 容忍单个 action 校验异常；`endSubFlow` 父流程/父步骤缺失 WARN 跳过；步骤 history 状态回退抛 `ERR_WF_INVALID_STEP_STATUS_TRANSITION`（两个实现类）
- **Phase 3**：全量回归 + 工具检查 + 独立 closure audit（见 plan Closure 段落）
- 测试：tcc 新增 11 用例（42/42，含 closure audit 补修的 testCheckTransactionActiveWithNullStatusNotCrash）；wf 新增 18 用例（132/132，新增 4 个测试模型 sourceRetry/sourceRetryFilter/sourceRetryExhausted/onErrorThrows）；快照实测零影响（testWithdraw/testReject 走 action 路径不执行 source）
- 验证：`./mvnw test -pl nop-tcc/nop-tcc-core,nop-tcc/nop-tcc-dao,nop-wf/nop-wf-core,nop-wf/nop-wf-dao,nop-wf/nop-wf-service,nop-wf/nop-wf-scheduler,nop-demo/nop-spring-gateway` 全绿（tcc 42 + wf 132 + gateway 5，既有套件零回归）
- Doc-sync: `docs-for-ai/02-core-guides/workflow-configuration.md` 新增「source 重试 (`<retry>`)」一节；`check-doc-links.mjs --strict` 0 错误
- Deferred 裁定（见 plan）：TCC 分支级强制重试上限、getTxnGroup 懒加载/branchNo 语义（需 ORM 变更）、EXEC_COUNT 列注释对齐

### 2026-09-05 执行 plan 348：XLang 类型推导遗留缺陷修复（全部 5 Phase 完成）

- **Phase 1**（GenericTypeInferencer）：删除恒真桩 `validateTypeBounds` 及 3 个空转测试（裁定：边界信息只在 `TypeParameterNode` AST，`ITypeVariable` 层无边界 API，真实校验需 public API 变更，被 Out Of Scope 排除）；`bind()` 冲突判定复用原 `isCompatible` 死代码逻辑，互不兼容时上报 `ERR_TYPE_INFER_TYPE_VAR_CONFLICT`（collector null 安全）；raw type 不兼容记 warning（仅入 collector warnings，parser 集成点只消费 errors，端到端不可见属预期）；`testTypeConflict`/`testTypeConflictProducesUnionType` 改写为冲突错误断言。
- **Phase 2**（call 实参校验）：`processCallExpression` 增加实参个数校验（`incompatibleTypes`）与逐位类型兼容校验（`typeMismatch`，跳过含类型变量形参与 any）；**裁定：call-site spread 不可表示**——`CallExpression.getArguments()` 类型为 `List<Expression>` 而 `SpreadElement` 继承 `XLangASTNode`，故无 spread 展开分支（测试固化该 AST 契约）；varargs 对齐维持 Deferred（`IFunctionType` 无 varargs 元数据）。
- **Phase 3**（AST 覆盖）：新增覆写 `ForRangeStatement`（区间数值提升绑定循环变量 + 赋值回写）、`EvalExpression`→any、`CustomExpression`→any、`TemplateExpression`→string、`MacroExpression`→any、`ClassDefinition`（方法走函数声明路径注册作用域）、`FieldDeclaration`（声明类型注册）、`EnumDeclaration`/`EnumMember`（保守 any，不再整棵跳过）、`ImportDeclaration`/`ImportAsDeclaration`/`ExportDeclaration`/`ExportNamedDeclaration`/`ExportAllDeclaration`（递归子节点，使 specifier 覆写可达）；`processNewExpression` 对未解析 `NamedTypeNode`（TypeNameNode）按类型名经 `ReflectionManager.loadClassModel` 解析；`processMemberExpression` computed property 对 List/Map/String 给出元素/值类型。
  - **完整裁定清单**（分发表 vs 覆写对照）：覆写=上述新增+既有全部；"无独立运行时类型语义、随父节点递归、无需单独覆写"=`ExportSpecifier`/`ImportSpecifier`（随父递归）、`Decorator(s)`、`MetaObject/Property/Array`、`QualifiedName`、各 TypeDef 节点、`TypeAliasDeclaration`（类型别名不影响表达式推导）、`OutputXmlAttrExpression`/`OutputXmlExtAttrsExpression`（输出节点无值语义，随 CollectOutput 父节点处理）；`CompilationUnit` 裁定为**推导入口不可达**（唯一入口 `XLangExprParser.buildExecutable` 只接收 Expression 根，dispatch 虽走未覆写的 processCompilationUnit 但该节点不会出现在推导路径上）。不存在仍被整棵跳过且含类型语义的语句级 kind。
- **Phase 4**（窄化）：`getTypeByName("object")` 返回 null 不窄化（原 MAP_TYPE 假设过强）；`x === true/false` 按 boolean 级语义窄化（true→boolean，false→union 移除 boolean），类注释同步修正；`mergeConditionalVariableTypes` 对"有 else、单侧赋值、无 base"并入 NULL。
- **Phase 5**（测试加固）：`testUnionTypeNarrowingFromCondition`、`testLogicalExpressionType` 改强断言；新增端到端测试经 `SimpleExprParser → XLangCompileTool.buildExecutable → XLangExprParser` 集成点断言 `returnTypeInfo`（需临时开启 `CFG_XLANG_TYPE_INFERENCE_ENABLED`，默认 false）；collector errors 断言在单元级覆盖（processor 内部实例不可从外部获取）。
- **验证**：`./mvnw test -pl nop-kernel/nop-xlang` 全量 **722 tests, 0 failures**（2 skipped 既有；基线 699 → +23 新增/改写）。

### 2026-09-05 拟制 plan 348：类型推导遗留缺陷修复计划（plan-first，待执行）

- **产出**：`ai-dev/plans/348-xlang-type-inference-remaining-defects.md`（draft），覆盖第一批修复后的全部确认遗留项：GenericTypeInferencer 去桩/冲突上报、call 实参校验、未覆写 AST kind 补齐、窄化语义修正、空转测试替换。
- **对抗性审查**：两轮独立子 agent 审查。第一轮发现 3 Blocker（`FilterOpExpression` 为幻影条目——无此 AST kind，具体子类已全部覆写；`processNewExpression` callee 实为 `NamedTypeNode` 而非 Identifier；varargs 启发式与个数校验自相矛盾且 `IFunctionType` 无 varargs 元数据）+ 4 Major（完整 kind 裁定清单缺失、`validateTypeBounds` 收敛为删除、冲突上报对现有 union 断言用例的影响、warnings 端到端不可见），已全部落实到计划；第二轮确认可进入执行阶段，余 5 个 Minor 已随手补入计划文本。
- **勘误**：审计报告覆盖矩阵的 filter-op 行已修正（无此 kind，误报）。
- **Deferred 裁定**（记录在 plan）：varargs 对齐（需函数类型 API 扩展）、scope 桥接（独立结果面）、错误 fail-fast（product decision）。
- **状态**：Plan Status 保持 `draft`，等待 plan audit 通过后置 `active` 并执行。

### 2026-09-05 XLang 类型推导 P1/P2 缺陷修复与回归测试（nop-xlang）

- **背景**：`ai-dev/analysis/2026-09/2026-09-05-xlang-type-inference-correctness-audit.md` 审计发现类型推导算法多处缺陷。本次修复其中 8 项并补 9 个回归测试。
- **修复**（`nop-kernel/nop-xlang`）：
  - `TypeInferenceProcessor`：新增 `processSwitchStatement` 覆写（此前 switch 整棵子树不做推导）；`processBlockStatement`/循环（while/do-while/for/for-of/for-in）通过新增 `mergeChildAssignments` 将内层对外层变量的赋值合并回外层（block 传播 + 循环 widening）；`processSequentialNodes` 只收集带 `returnAST` 标记的 return 语句类型（函数返回类型不再混入 `let` 声明等杂音），`union()` 与 `processExpressionStatement` 透传 return 标记（switch/if 内的 return 能计入函数返回类型）；`processLogicalExpression` 右操作数改用同一作用域；字符串 ADD 判断改用 `isStringType`（typeName 而非引用相等，兼容 resolved `java.lang.String`）。
  - `UnionTypeNarrower`：`instanceof`/`typeof`/`!= null` 的假分支从 `state.getVariableType`（声明类型）而非本次条件 result map 取原类型，独立条件下的 else 分支窄化恢复生效。
  - `XLangExprParser.buildExecutable`：类型推导错误改为以 `LOG.warn` 逐条暴露（此前 collector 用完即弃、错误静默丢失），不中断编译。
- **测试**：`TestTypeInferenceProcessor` 新增 9 个用例（switch 推导、switch-return 计入函数返回类型、函数返回类型排除非 return 语句、block 赋值传播、循环 widening、`&&` 右侧赋值传播、instanceof/typeof 假分支窄化、resolved String 的 ADD 推导）。
- **验证**：`./mvnw test -pl nop-kernel/nop-xlang` 全量 699 tests, 0 failures（2 skipped 为既有）。
- **未修**（见审计报告 P2/P3 剩余项）：call 实参校验、varargs/spread、泛型边界 `validateTypeBounds` 桩、编译 scope 桥接、`new Foo()` 构造类型、class/enum/for-range AST 覆盖等。

### 2026-09-05 合并 nop-stream-productization 分支回 master（merge）

- **变更**：`git merge nop-stream-productization`（112 commits，875 文件 +67954/−4997）合入 master。nop-stream 产品化路线图（`ai-dev/backlog/nop-stream-productization-roadmap.md`，35 items 全 done）的成果自此进入主分支：复合场景引擎、可观测性/运维 API、连接器 SPI 注册中心、flow DSL 编译器产品化、数据面/HA/checkpoint 系列加固、docs-for-ai 四新页等。
- **冲突解决（4 处，全文档）**：`docs-for-ai/02-core-guides/flux-rendering.md` 保留 master 侧跨仓链接措辞修正（commit `3ea6b336f1`）；`ai-dev/logs/2026/09-{01,02,04}.md` 三份 add/add 日志做并集（两侧条目均保留）。
- **合并后修复**：4 处 BOUNDARY 违规（`docs-for-ai/03-modules/nop-stream-{connectors,migration-guide,}.md` 反引号引用 `checkpoint-design.md` 被 master 侧分叉后新增的 docs-for-ai→ai-dev 边界规则命中）——改写为不直接链接的文字指称；`check-doc-links --strict` 复跑 0 errors（8 条 BROKEN_LINK 为 master 侧 plan 2258 既有 warning，非本次引入）。
- **验证**：master 全量 `./mvnw test -T 1C` BUILD SUCCESS（合并后集成修复见下一条目）。

### 2026-09-05 合并后集成修复：全量测试转绿

合并暴露了分支侧覆盖矩阵测试与 master 侧新代码（plan 2259 delete 语义、异步 DAG）的集成缺口 + 两处预存缺陷：

1. **xlang delete 族三后端接线**（master plan 2259 新增 `Delete{Property,Attr,ScopeVar}Executable`，分支的翻译覆盖矩阵测试合并后暴露缺口）：truffle 新增 `XDelete{Property,Attr,ScopeVar}Node` + `ExecToTruffleTranslator` 分支/支持集 + `TreeFingerprints` 载荷分支（scope-var 静态名/计算 attr 形态标记）+ `CoverageNodes` 工厂；java 后端 `ExecToJavaTranslator` 分支/支持集 + `XLangSemantics.deleteScopeVar(loc,scope,varName,attr)` 辅助 + `ExecutableTreeFingerprints` 载荷分支 + 测试工厂；覆盖计数断言 120→123。
2. **nop-task `TaskImpl.execute` 失败路径补 metrics null 守卫**（对齐成功路径；此前 recordMetrics=false 时同步失败被吞成 `NopException(NPE)`）。
3. **nop-task `GraphTaskStep` 活跃步骤计数竞态**（预存缺陷；ai-agent team-flow 测试负载下 `ERR_TASK_GRAPH_NO_ACTIVE_STEP` 误报根因）：`runStep` 完成回调中 `runningCount.decrementAndGet()` 移到 `stepFuture.complete(null)` 后继级联之后，消除「本节点已减、后继未加」的瞬态 0 窗口。
4. **nop-stream-connector `TestFileSourceReaderRecovery`**：poll 循环每行让步 1ms，保证快照线程观察窗（并行高负载下的 observedCursors 空误报）。
5. **nop-retry `TestRetryEngineImpl` 退避断言**：执行前预热 DB 估算时钟，使 nextTriggerTime 计算与 updateTime 写入共享同一 TimeData 外推。
6. **nop-auth-web `TestFluxPage`** crud 动作断言按声明意图收窄到「onClick 动作位」（flux v3.4 picker 的 `pickerPopup.type=dialog` 是合法弹层结构，全文字符串断言误报）。

验证：全仓 `./mvnw test -T 1C` BUILD SUCCESS；nop-ai-agent 模块连跑 5 次全绿（修复前模块级连败）。期间 nop-orm/nop-rule-core 等的 `NoClassDefFoundError` 与部分 auth-web 快照失败为本地仓库陈旧 SNAPSHOT 伪象，`clean install` 刷新后消失。
