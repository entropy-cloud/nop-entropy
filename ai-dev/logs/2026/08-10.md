# 2026-08-10 开发日志

## nop-datav D1 阶段完成（后端运行时）

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-10-1000-2-dashboard-runtime-panel-data-binding-refresh.md` 的全部 4 个 Phase：

1. **Phase 1 - 设计文档定稿 + 组件注册表（D1-1）**
   - `runtime-design.md` 从 stub 重写为最终设计文档（Status: final），记录组件注册表方案、数据绑定管线架构、刷新机制设计、与 nop-report/nop-metadata 边界、拒绝的替代方案
   - 实现组件注册表（`io.nop.datav.service.component` 包）：`IPanelComponent` 接口 + `PanelComponentMeta` 元信息 + `PanelComponentRegistry` 静态注册表 + `SimplePanelComponent` 内置实现
   - 覆盖 7 种组件类型（chart/pivot-table/stat-tile/map/table/text/iframe）+ container（布局容器）
   - `PanelTypeMapping`：int panelType ↔ String 组件类型标识映射
   - 扩展 `model/nop-datav.orm.xml` 的 `datav/panel-type` dict：新增 PIVOT_TABLE(50)/MAP(60)/IFRAME(70)；METRIC(20) 复用为 stat-tile
   - 错误码新增：`ERR_DATAV_UNKNOWN_COMPONENT_TYPE`、`ERR_DATAV_INVALID_PANEL_CONFIG`
   - 5 个新单元测试覆盖（`TestPanelComponentRegistry`）

2. **Phase 2 - 数据绑定管线（D1-2）**
   - `nop-datav-service/pom.xml` 新增 `nop-report-dao` 依赖（不引入 nop-report-core/service，避免拉入 XPT 引擎）
   - 实现端到端数据绑定管线（`io.nop.datav.service.query` 包）：
     - `PanelDataBinder`：编排 面板 → DatasetRef → NopReportDataset → SQL 查询 → 结果回传
     - `PanelParamEvaluator`：paramMapping JSON 解析 + 参数求值（source + defaultValue 规则）
     - `PanelSqlBuilder`：`dsText` 中 `${paramName}` 占位符替换为顺序 `?`，通过 `SQL.begin().sqlWithParams(...)` 注入（避免 SQL 注入）
   - `PanelDataResult`（nop-datav-dao）：结构化结果（panelId + componentType + hasDataset + columns + rows），不引用 nop-report 实体类型
   - `NopDatavPanelBizModel.getPanelData`（`@BizQuery`）+ `INopDatavPanelBiz` 接口声明
   - 文本/iframe 类面板（无数据集）返回明确 `hasDataset=false` 标识
   - 错误路径全部显式抛 `NopException`（PANEL_NOT_FOUND / DATASET_REF_NOT_FOUND / DATASET_NOT_FOUND / UNSUPPORTED_DATASET_TYPE / QUERY_FAILED）
   - 8 个新测试覆盖（`TestNopDatavPanelBizModel`）

3. **Phase 3 - 刷新机制（D1-3）**
   - `PanelRefreshConfig`：从 panelConfig JSON 的 `refresh` 区域解析 enabled + intervalSeconds（不新增 ORM 列）
   - `NopDatavPanelBizModel.refreshPanel`（`@BizMutation`）复用 getPanelData 管线触发重新查询
   - 10 个新测试覆盖（`TestPanelRefreshConfig` + refreshPanel 测试）

4. **Phase 4 - 端到端验证（D1-5）**
   - `TestNopDatavPanelDataE2E`：贯穿「创建看板 → 建业务表 → NopReportDataset → DatasetRef → Panel → getPanelData → refreshPanel → publishDashboard」完整链路
   - 覆盖 2 种组件类型（chart + table）
   - 参数化查询验证：相同 panel/datasetRef，不同参数值产生不同结果（证明参数求值真正生效）

### 验证结果

- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS
- `./mvnw test -pl nop-datav/nop-datav-service -T 1C`：38 tests, 0 failures（12 原有 + 5 组件注册表 + 8 面板 + 10 刷新 + 2 e2e + 1 smoke）
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：0 findings（无空壳实现）
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：通过（plan 完成后）

### 技术发现

- nop-report-dao 的 ORM 模型在 `AbstractNopDatavTest.initDatavSchema()` 自动建表循环中被自动创建（因为 `ormSessionFactory.getOrmModel().getEntityModelsInTopoOrder()` 涵盖所有模块），无需修改测试基类
- `NopReportDataset.dsText` 字段虽标注 `stdDomain="json"` + 绑定 JsonOrmComponent，但 `getDsText()` 直接返回 String；对 dsType=sql 的数据集，存的就是纯 SQL 文本（运行时验证假设成立）
- `CrudBizModel` 父类已含 private `daoProvider` 字段和 `daoProvider()` 方法，子类应调用方法而非重新声明字段（否则 NopIoC 不会注入子类覆盖的字段）
- nop 平台 SQL 用 `?` 位置参数 + Marker 机制，不是命名参数；通过 `sqlWithParams(text, paramsList)` 按出现顺序注入
- `IDataRow.toMap()` 直接返回 Map<String,Object>（基于 IDataSetMeta 的字段名），无需自己用 ColumnMapRowMapper
- JsonTool 严格模式对 invalid JSON 抛 NopException（具体错误码为 `nop.err.core.json.strict-mode.*`），wrapper 需统一包装为 datav 域错误码并保留 cause

### 产出文件

- `ai-dev/design/nop-datav/runtime-design.md`（D1 设计文档定稿）
- `nop-datav/model/nop-datav.orm.xml`（panel-type dict 扩展）
- `nop-datav/nop-datav-dao/src/main/java/io/nop/datav/biz/PanelDataResult.java`（新）
- `nop-datav/nop-datav-dao/src/main/java/io/nop/datav/biz/INopDatavPanelBiz.java`（接口扩展）
- `nop-datav/nop-datav-service/pom.xml`（新增 nop-report-dao 依赖）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/NopDatavErrors.java`（新增错误码）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/component/`（6 个新类：PanelComponentTypes/PanelComponentMeta/IPanelComponent/PanelComponentRegistry/SimplePanelComponent/PanelTypeMapping）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/query/`（4 个新类：PanelDataBinder/PanelParamEvaluator/PanelSqlBuilder/PanelRefreshConfig）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/entity/NopDatavPanelBizModel.java`（实现 getPanelData + refreshPanel）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/component/TestPanelComponentRegistry.java`（新）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/query/TestPanelRefreshConfig.java`（新）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/entity/TestNopDatavPanelBizModel.java`（新）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/entity/TestNopDatavPanelDataE2E.java`（新）

---

## nop-datav D0 阶段完成

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-09-2255-1-dashboard-model-crud-and-tests.md` 的全部 4 个 Phase：

1. **Phase 1 - 模块骨架、ORM 模型设计与标准 CRUD 接通**
   - 手工创建 nop-datav 标准 8 件套模块结构（model + codegen/dao/meta/service/web/app/api），基于 nop-job 参考模式
   - 编写 `model/nop-datav.orm.xml`：5 个实体（Dashboard、Panel、DashboardTab、DatasetRef、DashboardSnapshot），含 domains/dicts/i18n-en
   - 通过 `gen-orm.xgen` 触发 codegen 生成 `_app.orm.xml`、`_gen/_NopDatav*.java`、`_service.beans.xml`
   - 手写 5 个保留层 BizModel + 5 个 IBiz 接口，接通 IoC
   - 冒烟测试通过：IoC + DB 管道连通

2. **Phase 2 - 发布/快照自定义 biz 语义**
   - `NopDatavDashboardBizModel` 实现 `publishDashboard`/`getPublishedDashboard`/`rollbackDashboard`
   - 发布时序列化看板编辑内容为 JSON 快照写入快照表，主表更新发布状态/版本
   - 回滚时从历史快照恢复主表字段
   - 所有方法在 `INopDatavDashboardBiz` 接口声明

3. **Phase 3 - 设计文档定稿**
   - `model-design.md` 从 stub 重写为最终设计文档
   - 记录实体职责划分、发布/快照方案选择、DatasetRef 归属方案、拒绝的替代方案

4. **Phase 4 - 单元测试覆盖**
   - `TestNopDatavDashboardBizModel`：11 个测试覆盖 CRUD（4 实体）、发布、查看已发布、回滚、端到端
   - `TestNopDatavSmoke`：冒烟测试验证 IoC 管道连通
   - 全部 12 个测试通过

### 技术发现

- `initDatabaseSchema=true` 的 `DataBaseSchemaInitializer` bean 在 ALL_LAZY IoC 模式下不被触发（`ioc:after` 不构成强制依赖）。创建 `AbstractNopDatavTest` 基类在 `@BeforeEach` 中手工建表作为 workaround。这是全平台预存问题（nop-job 测试单独运行时同样如此）。
- `QueryBean.addOrderField(name, desc)` 第二参数是 `desc`（降序），非 `asc`。初次使用时方向搞反导致版本号查询返回最旧而非最新。
- nop-bom 需注册新增模块的版本管理条目。

### 产出文件

- `nop-datav/model/nop-datav.orm.xml`
- `nop-datav/nop-datav-{codegen,dao,meta,service,web,app,api}/` 全套
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/entity/NopDatav*BizModel.java`（5 个）
- `nop-datav/nop-datav-dao/src/main/java/io/nop/datav/biz/INopDatav*Biz.java`（5 个）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/entity/`（3 个测试类）
- `ai-dev/design/nop-datav/model-design.md`（定稿）
- `nop-bom/pom.xml`（新增 nop-datav 模块条目）
