# 2026-08-10 开发日志

## nop-datav D3-3 看板/面板数据导出完成

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-10-1130-1-dashboard-panel-data-export.md`（4 个 Phase 全部落地）。

1. **Phase 1 - 设计文档定稿（D3-3 导出章节）**
   - `permission-sharing-design.md` 增补 D3-3 全部裁定：任务实体列约定、状态机（pending→running→succeeded/failed/cancelled）、异步执行机制（复用 `GlobalExecutors.globalWorker()` + `IOrmTemplate.runInNewSession`）、进程重启清理（`NopDatavExportTaskRecovery` 非 lazy bean @PostConstruct + existsTable 守卫）、限额（max-rows/max-concurrent-per-user/file-max-length，行数防 OOM 经 `LongRangeBean` 取数层限行）、文件存储（`IFileStore.saveFile` + `UploadRequestBean`）、格式（CSV+UTF-8 BOM / xlsx 多 sheet）、看板级遍历（needsDataset 过滤）、权限（继承 D3-1 RLS 经 `IDataAuthChecker.isPermitted`）、图像导出 out-of-scope 显式拒绝。文档 Status 升级为 D3-1+D3-4+D3-2+D3-3 全部 final。

2. **Phase 2 - ORM 模型与代码生成（NopDatavExportTask）**
   - `nop-datav.orm.xml` 新增 `NopDatavExportTask` 实体（表 `nop_datav_export_task`）：taskId(PK) + sourceType/sourceId/format/status(dict) + params(CLOB JSON) + fileRecordId + rowCount + errorMsg + 标准审计列；索引按 owner/status；新增 3 个 dict（export-source、export-format、export-status）
   - codegen 生成 entity/IBiz/BizModel stub/xmeta/xbiz；web 模块生成管理页 + `_nop-datav.action-auth.xml` 含 `FNPT:NopDatavExportTask:query/mutation`

3. **Phase 3 - 导出服务实现**
   - `NopDatavErrors` 新增 9 个导出错误码（export-task-not-found / type-not-supported / row-limit-exceeded / concurrency-limit / not-owner / not-finished / failed / no-exportable-panels，均英文 + ARG_*）
   - `NopDatavConfigs` 新增 3 个限额配置（CFG_DATAV_EXPORT_MAX_ROWS=100000 / CFG_DATAV_EXPORT_MAX_CONCURRENT_PER_USER=3 / CFG_DATAV_EXPORT_FILE_MAX_LENGTH=100MB）
   - `pom.xml` 新增依赖 `nop-file-dao`（nopFileStore=DaoResourceFileStore）、`nop-excel`、`nop-ooxml-xlsx`（ExcelHelper.saveExcel）
   - `PanelDataExporter`：复用 `PanelDataBinder.queryPanelData` 取数（新增 `rowLimit` 重载，经 `LongRangeBean` 在数据集层限行防 OOM）→ 超限抛 `ERR_DATAV_EXPORT_ROW_LIMIT_EXCEEDED` → CSV（手写 UTF-8 BOM + CsvRecordOutput）/ xlsx（`table.setCell` 成熟写法 + ExcelHelper.saveExcel）
   - `NopDatavExportTaskBizModel`：createExportTask（格式校验 + 来源 RLS + 并发限额 + INSERT pending + 提交 globalWorker 异步）/ getExportTask / cancelExportTask（内存 cancel 标志位 + DB 状态）/ downloadExportFile（owner 校验 + succeeded 校验 + IFileStore.getFile→getResource）；异步执行体 runInNewSession 内 running→取数→saveFile→succeeded/failed，catch Throwable 记 failed 不吞异常
   - `NopDatavExportTaskRecovery`：非 lazy bean @PostConstruct 扫描 pending/running→failed（reason=interrupted by process restart），existsTable 守卫处理测试 schema 生命周期；BizModel 在 createExportTask 前也幂等调用一次
   - `app-service.beans.xml`（保留层）：import nop-file-dao beans（nopFileStore）+ 注册 nopDatavExportTaskRecovery bean（`_service.beans.xml` 为生成物不可手改）
   - 测试 action-auth 增补 NopDatavExportTask 的 4 个自定义 action 权限点 + query/mutation

4. **Phase 4 - 测试与端到端验证**
   - `TestPanelDataExporter`（6 tests）：CSV 写出读回（BOM+表头+数据行）/ xlsx 写出读回（单 sheet 表头+数据）/ 行数限额防 OOM（取数层 LIMIT 探测 maxRows+1）/ 无数据集面板空导出不报错 / 看板级多 sheet（text 面板跳过）/ 看板无可导出面板显式拒绝
   - `TestNopDatavExportE2E`（10 tests）：全链路 CSV（createExportTask→poll→download，断言 NopFileRecord 落库 bizObjName/bizObjId + 内容含数据）/ 看板级 xlsx 多 sheet / image 格式显式拒绝 / 看板级 csv 拒绝 / 并发限额（seed running 行确定计数）/ 非 owner 下载拒绝 / 下载未完成拒绝 / failed 路径（坏 SQL→failed+errorMsg）/ cancelled 路径 / 重启清理（seed running+pending→recovery→failed）

### 验证结果

- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS
- `./mvnw test -pl nop-datav/nop-datav-service -T 1C`：184 tests（168 既有 + 16 新增 D3-3）, 0 failures, 0 errors
- Anti-Hollow：createExportTask→PanelDataBinder.queryPanelData→写出→IFileStore.saveFile 链路运行时连通（E2E 测试断言 NopFileRecord 落库 + 下载文件内容含数据）；image 格式显式抛异常非空壳；行数/并发超限快速失败；后台异常记 failed 非吞掉

### 偏差

- **行数防 OOM 实现简化**：设计文档原拟方案 (a) "包装 PanelSqlBuilder 加 SQL LIMIT"，实际实现采用平台原生 `IJdbcTemplate.executeQuery(sql, LongRangeBean, callback)` 的 `LongRangeBean` 限行机制（跨方言，由 dialect paging 实现，比手写 LIMIT 包装更稳健）。`PanelDataBinder` 新增 `queryPanelData(panelId, panel, params, rowLimit)` 重载，导出路径传 `maxRows+1` 探测。设计意图（取数阶段防 OOM，非取数后校验）完全保留。
- **重启清理触发机制**：设计文档原拟 `IInitializer`，但平台 `ICoreInitializer` 标记 `@Internal`（非 app 代码）。实际实现为独立非 lazy bean `NopDatavExportTaskRecovery`（@PostConstruct + existsTable 守卫），效果等价（IoC 启动后扫描 pending/running→failed）。
- **`_service.beans.xml` 为生成物**：原计划在其中加 file-dao import 和 recovery bean，发现该文件每次 install 被 codegen 重生成。改在保留层 `app-service.beans.xml` 注册（import file-dao beans + recovery bean），这是正确的可手改层。

### 关键文件

- 设计：`ai-dev/design/nop-datav/permission-sharing-design.md`（D3-3 章节）
- ORM：`nop-datav/model/nop-datav.orm.xml`（NopDatavExportTask + 3 dict）
- 取数：`nop-datav-service/.../query/PanelDataBinder.java`（rowLimit 重载）
- 导出：`nop-datav-service/.../export/PanelDataExporter.java`、`NopDatavExportTaskStatus.java`、`NopDatavExportTaskRecovery.java`
- BizModel：`nop-datav-service/.../entity/NopDatavExportTaskBizModel.java`
- 配置/错误码：`NopDatavConfigs.java`、`NopDatavErrors.java`
- beans：`nop-datav-service/.../beans/app-service.beans.xml`
- 测试：`TestPanelDataExporter.java`（6）、`TestNopDatavExportE2E.java`（10）

---

## nop-datav D3-2 看板公共分享链接完成

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-10-1100-2-dashboard-sharing.md`（4 个 Phase 全部落地）。

1. **Phase 1 - 设计文档补充 + 分享模型（D3-2）**
   - `permission-sharing-design.md` 补充 D3-2 全部裁定（存储方案/列约定/令牌/密码哈希/吊销语义/公共访问契约/匿名 RLS/管理权限/action-auth/拒绝的替代方案），文档 Status 升级为覆盖 D3-1+D3-4+D3-2
   - `nop-datav.orm.xml` 新增 `NopDatavDashboardShare` 实体（表 `nop_datav_share`）：shareId(PK) + shareToken(唯一键) + dashboardId(FK+索引) + passwordHash(可空) + expireTime(可空) + enabled(domain `boolFlag`，默认 1) + 标准审计列；唯一键 `UK_NOP_DATAV_SHARE_TOKEN` + 索引 `IX_NOP_DATAV_SHARE_DASHBOARD`
   - codegen 自动生成保留层 entity/IBiz/BizModel/xmeta/xbiz/web pages；`_nop-datav.action-auth.xml` 自动含 `FNPT:NopDatavDashboardShare:query/mutation`；`_service.beans.xml` 自动含 BizModel + BizProxyFactoryBean bean
   - `nop-datav-service/pom.xml` 加依赖 `nop-biz-auth-core`（提供 `IPasswordEncoder` bean `nopPasswordEncoder` = `CompositePasswordEncoder`：SHA256 预哈希 + BCrypt 外层）

2. **Phase 2 - 分享管理 API（D3-2）**
   - `NopDatavDashboardShareBizModel` 实现 createShare/listShares/revokeShare/toggleShare（均 `@Auth(permissions="NopDatavDashboardShare:{action}")`）
   - `NopDatavDashboardOwnerGuard` 共享 helper：加载 Dashboard 并校验 owner（`createdBy == currentUser`）或 admin 角色；非 owner 抛 `ERR_DATAV_NOT_DASHBOARD_OWNER`
   - 密码经 `passwordEncoder.encodePassword(null, null, password)` 哈希（BCrypt 复合，**绕过 `nopPasswordPolicy`**，单 `passwordHash` 列无需 salt）；`listShares` 出参强制 `passwordHash=null`（不泄露哈希）
   - 令牌用 `StringHelper.generateUUID()`（32 位 hex）+ 唯一性冲突重试（最多 8 次，超限抛 `ERR_DATAV_SHARE_TOKEN_GENERATE_FAILED`）
   - `NopDatavErrors` 新增 8 个 D3-2 错误码（share-token-generate-failed / share-not-found / share-token-not-found / share-disabled / share-expired / share-password-required / share-password-mismatch，均英文消息 + ARG_SHARE_ID/ARG_SHARE_TOKEN）
   - `TestNopDatavShareManagementBizModel`（12 tests）：createShare 带/不带密码 + 密码哈希非明文（断言 `$2a` 开头）+ 有效期 + listShares 出参无 passwordHash + listShares 按 dashboard 隔离 + revoke 软禁用保留行 + toggle 可重启用 + 非 owner 拒绝（create/list/revoke）+ missing dashboard/share 显式抛异常

3. **Phase 3 - 公共访问 API（D3-2）**
   - `getSharedDashboard(shareToken, password)` 实现，`@BizQuery` + **`@Auth(publicAccess=true)`**：按 token 经 DAO 唯一键查 share（不经 RLS）→ 校验 enabled → 校验 expireTime → 密码校验（passwordHash 非空则要求 password 经 `passwordEncoder.passwordMatches` 比对）→ **直接经 DAO 读 `NopDatavDashboardSnapshot`（按 dashboardId、snapshotVersion DESC 取首条，不调 `getPublishedDashboard`、不经 requireEntity/RLS）** → 返回快照内容
   - 关键：代码不调用 `IServiceContext.getUserContext()`（`publicAccess` 运行时为空）；纯读不写；空串密码不可绕过要求密码的分享（显式抛 `ERR_DATAV_SHARE_PASSWORD_REQUIRED`）
   - `TestNopDatavSharedDashboardAccessBizModel`（9 tests）：匿名（`anonymousContext()` 不设 userName）有效 token 无密码/带正确密码访问 + 令牌不存在/空 token/禁用/过期/密码缺失/密码不匹配/无快照 各自显式拒绝（断言错误码）

4. **Phase 4 - 端到端验证（D3-2 全链路）**
   - `TestNopDatavShareE2E`（5 tests）：全链路正向（owner 创建+发布带面板看板 → 创建带密码+有效期分享，断言 passwordHash 以 `$2a` 开头 → 匿名凭 token+password 访问 → 断言返回真实快照内容含 "Sales Chart"）+ 吊销拒绝 + 过期拒绝 + 错误密码拒绝 + 管理→公共访问隔离（非 owner 管理被拒 + 匿名访问放行）

### 验证结果

- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS
- `./mvnw test -pl nop-datav -T 1C`：168 tests, 0 failures, 0 errors（含 26 个新增 D3-2 测试）
- `node ai-dev/tools/check-plan-checklist.mjs <plan-file> --strict`：exit 0（所有 checklist 勾选 + Closure Evidence 写入）
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：exit 0（0 findings）
- Anti-Hollow：createShare→persist→getSharedDashboard 链路运行时连通（e2e 测试证明）；`publicAccess=true` 在无登录上下文确实放行（匿名访问测试证明）；密码哈希/校验真实执行（错误密码被拒证明）

### 偏差

- transitive 依赖 `nop-web` 的 `TestFluxWebCrudPage.testCrudPageGeneratesFluxJson` 有 1 个 pre-existing failure（`footerToolbar` 断言），已 git stash 验证在 clean baseline 同样失败，与 D3-2 无关；nop-datav 自身模块测试全绿。
- `TestNopDatavAuditLog.testCombinedE2E_rbacRlsAudit` 在并行满载（`-T 1C` 全模块）时偶发 async-audit 计时 flaky，单独/重跑均通过（非 D3-2 引入）。
- doc-link checker 报 1 ERROR（`docs-for-ai/INDEX.md:217` 引用 `nop-runner/nop-cli/target/` 构建产物目录）为 pre-existing 平台文档问题，非本次引入，且不在 D3-2 scope 内。

## nop-datav D3-1 看板权限（RBAC + RLS）+ D3-4 操作审计日志完成

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-10-1100-1-dashboard-permission-and-audit-log.md` 的 Phase 2/3/4（Phase 1 设计文档已在先前的 plan review 阶段完成）。

1. **Phase 2 - Action 级 RBAC + 鉴权测试基建（D3-1）**
   - 全部 11 个自定义 action impl 方法加 `@Auth(permissions="{BizObj}:{action}")`（5 Dashboard + 4 Panel + 2 FilterState）
   - `nop-datav.action-auth.xml` 手写层补全全部 `FNPT:` 权限点资源的角色绑定（admin 全量；user 查询+面板操作类；自定义 action 权限点手动补入）
   - `NopDatavOperatorResolver` 共享 helper 抽取（Dashboard/FilterState 两处重复 `resolveOperator` 去重）
   - `NopDatavErrors` 新增 `ERR_DATAV_NOT_DASHBOARD_OWNER` 错误码
   - `nop-datav-service/pom.xml` 加 `<scope>test</scope>` 依赖 `nop-auth-service`（提供 nopActionAuthChecker / nopDataAuthChecker / nopGraphQLLogger / nopAuditService bean）
   - `TestNopDatavRbacAuth`（4 tests）：正向 admin 放行 publishDashboard + 负向 user 拒绝 publishDashboard + 正向 user 放行 getPublishedDashboard + 负向 user 拒绝 rollbackDashboard（admin-only）
   - 修正：`testUnregisteredRoleIsDenied` 测试前提错误——平台 `UserContextImpl.isUserInRole` 对 "user" 角色总是返回 true（`总是具有user角色`），故 guest 用户实际上匹配 roles="admin,user" 的权限。改为 `testNonAdminDeniedForAdminOnlyAction`（rollbackDashboard 仅 admin 可访问，user 被拒）

2. **Phase 3 - 行级数据权限 RLS（D3-1）**
   - `nop-datav.data-auth.xml` 配置 Dashboard 行级规则：admin 无 filter（全见）；user `createdBy = $context.userName OR publishStatus = 10`（owner + 已发布）
   - `application.yaml` 启用 `enable-action-auth: true` / `enable-data-auth: true` / `use-data-auth-table: true`
   - 关键发现：data-auth filter 的 `$context.userName` 解析自 `ContextProvider.currentContext().getUserName()`（IContext），而非 `$userContext`（IUserContext）。`$userContext` 非 XPL 编译期已注册全局变量，filter 表达式编译失败。`$context` 是全局变量，在测试中需通过 `ContextProvider.getOrCreateContext().setUserName()` 设置
   - `TestNopDatavDataAuth`（6 tests）：wiring sanity（checker != null）+ owner 过滤（非 owner 看不到 private）+ published 可见 + admin 全见 + fail-closed（admin-only bizObj Snapshot，非 admin 抛 ERR_AUTH_NO_DATA_AUTH）+ fail-closed 直接 checker 调用
   - `AbstractNopDatavTest` 修复：nop-auth-service 测试依赖引入 SysSequenceGenerator，需在 `runLazyActions()` 前创建全部表（含 NOP_SYS_SEQUENCE），改为 override `init()` 方法

3. **Phase 4 - 操作审计日志（D3-4）+ 联合 E2E**
   - `application.yaml` 启用 `enable-audit: true` + `audit-mutation-patterns: NopDatavDashboard__*,NopDatavPanel__*,NopDatavFilterState__*` + `audit-query-patterns: NopDatavDashboard__getPublishedDashboard`
   - 关键发现：`graphQLEngine.executeRpcAsync` 不自动调用 `IGraphQLLogger.onRpcExecute`（仅 `GraphQLWebService` 调用），测试需手动调用（与 nop-auth `TestGraphQLLogger` 同模式）
   - 异步 flush 处理：`FutureHelper.waitUntil(() -> auditService.isAllProcessed(), 10000)` 轮询，非 Thread.sleep
   - `TestNopDatavAuditLog`（4 tests）：成功 mutation 记录（resultStatus=0）+ 失败 mutation 记录（errorCode 非空）+ pattern 未匹配不记录（findPage）+ 联合 E2E（RBAC 拒绝 rollbackDashboard + RLS 过滤 private dashboard + audit 记录拒绝操作）

### 验证结果

- `./mvnw test -pl nop-datav/nop-datav-service -T 1C`：142 tests, 0 failures, 0 errors
- `./mvnw clean install -pl nop-datav/nop-datav-service -am -T 1C -DskipTests`：BUILD SUCCESS
- `node ai-dev/tools/check-plan-checklist.mjs <plan-file> --strict`：exit 0
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：exit 0

### 偏差

- `$context.userName` vs `$userContext.userName`：design doc 原写 `$context.userName`，plan 声称「已核实可用」。实际执行发现 `$userContext` 非 XPL 全局变量（编译失败），`$context` 在测试中需额外设置 IContext.userName。保留 `$context.userName`（生产链路 HTTP auth filter 会填充 IContext），更新 design doc 说明两者区别
- 测试基建修复：nop-auth-service 依赖引入后非 auth 测试的 NOP_SYS_SEQUENCE 表缺失，需统一在 `AbstractNopDatavTest.init()` 中先建表再 runLazyActions

### doc-sync

- `permission-sharing-design.md`：更新 owner 匹配表达式说明（$context vs $userContext 区别）
- `nop-datav-roadmap.md`：D3-1/D3-4 状态 `planned` → `done ✅`
- plan `2026-08-10-1100-1-dashboard-permission-and-audit-log.md`：Plan Status `completed`，全部 Phase/Closure Gates 勾选，Closure Evidence 写入

---

## nop-datav D2-2 图表联动三件套 + D2-3 联动状态服务完成

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-10-1030-2-chart-linkage-and-filter-state.md` 的全部 4 个 Phase：

1. **Phase 1 - 设计文档补充 + 联动配置模型**
   - `linkage-design.md` 补充 D2-2 §8（联动配置存储 panelConfig JSON / 联动结构 / 跳转结构 / URL 模板语法 / 外部参数注入策略 / API 契约 / 错误路径 / 拒绝的替代方案）和 D2-3 §9（filter_state 存储方案 NopDatavFilterState 独立实体 / 实体约定 / 内容契约 / API 契约 / 用户隔离 / 错误路径 / 拒绝的替代方案），文档完整覆盖 D2 全阶段，Status: final
   - ORM 变更：`nop-datav.orm.xml` 新增 `NopDatavFilterState` 实体（stateId/userName/dashboardId/stateContent(clobJson)/标准审计列，唯一键 (userName, dashboardId)）—— plan-first Protected Area，本 plan 为授权 artifact
   - codegen 全链路通过：生成 `_gen/_NopDatavFilterState.java` + 保留层 `NopDatavFilterState.java` + `INopDatavFilterStateBiz.java` + `NopDatavFilterStateBizModel.java` + xmeta/pages/beans 注册
   - 联动配置解析逻辑（`io.nop.datav.service.linkage` 包）：`LinkageRule` POJO + `JumpRule` POJO + `LinkageConfigParser`（panelConfig JSON linkage/jump 区域解析，格式错误显式抛 NopException）
   - 错误码新增：`ERR_DATAV_INVALID_LINKAGE_CONFIG` / `ERR_DATAV_INVALID_JUMP_CONFIG` / `ERR_DATAV_LINKAGE_TARGET_PANEL_NOT_FOUND` / `ERR_DATAV_INVALID_JUMP_TARGET` / `ERR_DATAV_LINKAGE_FIELD_NOT_MATCHED` / `ERR_DATAV_INVALID_FILTER_STATE`
   - 19 个单元测试覆盖（`TestLinkageConfigParser`）

2. **Phase 2 - 联动执行 + 跳转 + 外部参数注入（D2-2）**
   - `NopDatavPanelBizModel.resolveLinkage`（`@BizQuery`）：源面板 + 点击上下文 → 加载联动配置 → 匹配规则 → 返回目标面板 + 应用的筛选参数 Map
   - `NopDatavPanelBizModel.resolveJump`（`@BizQuery`）：源面板 + 点击上下文 → 加载跳转配置 → 匹配规则 → 返回跳转目标（targetType + targetId + params）；external-url 模板 `${field}` 占位符替换；dashboard 类型校验目标看板存在
   - `LinkageExecutor`：联动/跳转执行编排类（加载面板 → 解析规则 → 字段匹配 → 参数合成）
   - `LinkageResult` / `JumpResult`（nop-datav-dao）：结构化返回类型，方法签名同步声明到 `INopDatavPanelBiz` 接口
   - 外部参数注入：复用 D2-1 `resolveFilterValues`（外部参数 = URL/API/embed 传入的全局筛选参数，无独立通道，design doc §8.4 显式说明）
   - 12 个接线测试（通过注入 INopDatavPanelBiz 代理验证调用链运行时连通）

3. **Phase 3 - 联动状态服务（D2-3）**
   - `NopDatavFilterStateBizModel.saveFilterState`（`@BizMutation`）：按 userName + dashboardId 隔离保存/覆盖；BizModel 内部按内容契约序列化为 stateContent JSON 存储
   - `NopDatavFilterStateBizModel.getFilterState`（`@BizQuery`）：按 userName + dashboardId 恢复；**若该用户+看板无保存记录返回 null**（调用方据此判断无已保存状态，非静默返回空对象）
   - `FilterStateCodec`：filter_state JSON 序列化/反序列化（globalFilters + panelSelections[field,value] + urlState），格式错误显式失败
   - `FilterState` / `FilterState.PanelSelection`（nop-datav-dao）：结构化返回类型
   - 方法签名同步声明到 `INopDatavFilterStateBiz` 接口
   - 19 个测试覆盖（11 codec + 7 BizModel wiring + 1 isolation）

4. **Phase 4 - 端到端验证（D2-2 + D2-3）**
   - `TestNopDatavLinkageStateE2E`：5 个端到端测试
     - 联动 E2E：resolveLinkage(region=east) → getPanelData(target B) → 断言仅返回 east 数据（2 行），改 region=north 得 1 行（证明联动 → getPanelData → paramMapping → SQL 链路端到端连通）
     - 跳转 E2E：dashboard 类型 params 注入 + external-url URL 模板 `${region}` 替换
     - 外部参数注入 E2E：API 传 region=north → resolveFilterValues → getPanelData → 2 行 north 数据
     - filter_state 保存/恢复 E2E：globalFilters + panelSelections + urlState 往返一致
     - filter_state 隔离 E2E：用户 A 保存的状态对用户 B 不可见

### 关键设计决策

- **联动配置存储**：源 Panel 的 panelConfig JSON `linkage` 区域（与 D1 refresh + 组件配置同模式，零 ORM 变更）
- **跳转配置存储**：源 Panel 的 panelConfig JSON `jump` 区域
- **filter_state 存储**：新建 `NopDatavFilterState` 独立实体（平台无 user-preference/key-value 机制；Superset filter_state API 后端持久化语义；支持 per-userName+dashboardId 隔离）
- **URL 模板语法**：`${paramName}` 占位符（与 nop 平台 SQL 命名参数语法一致）
- **外部参数注入**：复用 D2-1 `resolveFilterValues`（外部参数 = URL/API/embed 传入的全局筛选参数，无独立通道）
- **跨看板联动 Out Of Scope**：用跳转实现跨看板场景（跳转到目标看板并携带参数）
- **"无规则匹配" 是合法分支**：返回 null 让前端处理（design doc §8.6 显式说明）；配置格式错误才显式失败

### 验证结果

- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS
- `./mvnw test -pl nop-datav/nop-datav-service -T 1C`：128 tests, 0 failures（基线 91 → +19 LinkageConfigParser + 12 LinkageE2E + 19 FilterState + 5 E2E = 128）
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：0 findings（无空壳实现）
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：通过（plan 完成后）
- 独立 closure audit subagent 通过，无 Blocker（仅 2 个 Minor error-code 问题已就地修复）

### 技术发现

- `LinkageExecutor.validateJumpDashboardTarget`：dashboard 类型跳转的目标看板必须运行时校验存在（配置时校验为 non-blocking follow-up），用 `ERR_DATAV_INVALID_JUMP_TARGET` 错误码
- `getFilterState` 返回 null 是合法语义（调用方据此判断无已保存状态），需在 IBiz 接口 javadoc 显式声明
- `stateContent` JSON 结构 `panelSelections` 子元素用 `{field, value}` 对象（非扁平 `{field: value}`），便于扩展多字段+元数据
- H2 测试 DB schema 自动建表循环覆盖了新增的 `nop_datav_filter_state` 表（无需修改测试基类）

### 产出文件

- `ai-dev/design/nop-datav/linkage-design.md`（D2-2/D2-3 设计文档定稿）
- `nop-datav/model/nop-datav.orm.xml`（新增 NopDatavFilterState 实体）
- `nop-datav/nop-datav-dao/src/main/java/io/nop/datav/biz/{LinkageResult,JumpResult,FilterState,INopDatavPanelBiz,INopDatavFilterStateBiz}.java`（接口扩展）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/NopDatavErrors.java`（新增 6 个错误码）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/linkage/`（5 个新类：LinkageRule/JumpRule/LinkageConfigParser/LinkageExecutor/FilterStateCodec）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/entity/NopDatavPanelBizModel.java`（实现 resolveLinkage + resolveJump）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/entity/NopDatavFilterStateBizModel.java`（实现 saveFilterState + getFilterState）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/linkage/{TestLinkageConfigParser,TestNopDatavLinkageE2E,TestFilterStateCodec}.java`（3 个新测试类）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/entity/{TestNopDatavFilterStateBizModel,TestNopDatavLinkageStateE2E}.java`（2 个新测试类）

---

## nop-datav D2-1 全局筛选参数完成

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-10-1030-1-dashboard-global-filter-parameters.md` 的全部 3 个 Phase：

1. **Phase 1 - 设计文档定稿 + 参数定义模型**
   - `linkage-design.md` 从 stub（4 行）重写为最终设计文档（Status: final），记录 D2-1 全部裁定决策：paramConfig JSON 列存储方案（方案 A，clobJson domain）、参数定义结构约定、扁平 key 参数值表示约定（D2-1→D1 集成契约）、全局筛选应用流程、URL 参数同步策略、与 D1 paramMapping 关系、拒绝的替代方案（独立实体 vs JSON 列、批量 vs 逐面板、严格 vs 宽松、json-4000 vs clobJson）
   - ORM 变更：NopDatavDashboard 新增 `paramConfig` 列（propId=18，domain clobJson）—— plan-first Protected Area，本 plan 为授权 artifact
   - 参数定义解析逻辑（`io.nop.datav.service.filter` 包）：`DashboardParamDefinition`（参数定义 POJO）+ `DashboardParamParser`（paramConfig JSON → 定义列表，格式错误显式抛 NopException）+ `DashboardFilterResolver`（校验类型/填充默认值/过滤未定义参数）
   - 发布/快照逻辑更新：`serializeDashboardContent`/`restoreDashboardFromSnapshot`/`updateDashboardFields` 纳入 paramConfig
   - 21 个单元测试 + 1 个快照往返测试

2. **Phase 2 - 全局筛选应用 API + URL 同步**
   - `NopDatavDashboardBizModel.resolveFilterValues`（@BizQuery）：dashboardId + 筛选值 Map → 加载参数定义 → 校验/归一化 → 扁平 key 生效参数 Map
   - `NopDatavDashboardBizModel.parseFilterFromUrl`（@BizQuery）：URL query string → 反序列化 → resolveFilterValues
   - 两个方法签名同步声明到 `INopDatavDashboardBiz` 接口
   - `DashboardFilterUrlCodec`：URL query string 序列化/反序列化（UTF-8 编解码，扁平 key 一致）
   - 错误码新增：`ERR_DATAV_INVALID_PARAM_CONFIG`、`ERR_DATAV_UNKNOWN_PARAM_NAME`（预留严格匹配）、`ERR_DATAV_PARAM_TYPE_MISMATCH`
   - 9 个接线测试（通过注入 INopDatavDashboardBiz 代理验证调用链运行时连通）

3. **Phase 3 - 端到端验证**
   - `TestNopDatavDashboardFilterE2E`：贯穿「创建带参数看板 → 建业务表 → NopReportDataset → DatasetRef（paramMapping 映射含复合 key dateRange.start→start_date）→ Panel → resolveFilterValues → getPanelData → 断言查询结果随筛选值变化」
   - 复合类型 date-range 端到端验证：缩窄日期范围导致查询结果 3→2→1 变化
   - URL 同步 E2E：序列化 → 反序列化 → 查询结果一致
   - 默认值场景：不传筛选值时默认值生效

### 关键设计决策

- **参数值扁平 key 约定**：resolveFilterValues 输入输出均用扁平 key（date-range → `paramName.start`/`paramName.end`），与 PanelParamEvaluator 的 `Map.get(sourceKey)` 完全兼容，无需修改 D1 代码即可消费复合类型参数
- **宽松兼容**：未知参数显式过滤（非报错），保持 D1 向后兼容；类型不匹配显式失败（抛异常）

### 验证结果

- `./mvnw test -pl nop-datav/nop-datav-service`：72 tests, 0 failures（基线 38 → +34 新测试）
- `scan-hollow-implementations.mjs --module nop-datav --severity high`：0 findings
- `check-plan-checklist.mjs --strict`：exit 0
- 独立 closure audit subagent 通过，无 Blocker

---

## nop-datav D1 阶段完成（后端运行时）

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-10-1000-2-dashboard-runtime-panel-data-binding-refresh.md` 的全部 4 个 Phase：

1. **Phase 1 - 设计文档定稿 + 组件注册表（D1-1）**
   - `runtime-design.md` 从 stub 重写为最终设计文档（Status: final），记录组件注册表方案、数据绑定管线架构、刷新机制设计、与 nop-report/nop-metadata 边界、拒绝的替代方案
   - 实现组件注册表（`io.nop.datav.service.component` 包）：`IPanelComponent` 接口 + `PanelComponentMeta` 元信息 + `PanelComponentRegistry` 静态注册表 + `SimplePanelComponent` 内置实现
   - 覆盖 7 种组件类型（chart/pivot-table/stat-tile/map/table/text/iframe）+ container（布局容器）
   - `PanelTypeMapping`：int panelType ↔ String 组件类型标识映射
   - 扩展 `model/nop-datav.orm.xml` 的 `datav/panel-type` dict：新增 PIVOT_TABLE(50)/MAP(60)/IFRAME(70)；METRIC(20) 复用为 stat-tile
   - 错误码新增：`ERR_DATAV_UNKNOWN_COMPONENT_TYPE`、`ERR_DATAV_INVALID_PANEL_CONFIG`
   - 5 个新单元测试覆盖（`TestPanelComponentRegistry`）

2. **Phase 2 - 数据绑定管线（D1-2）**
   - `nop-datav-service/pom.xml` 新增 `nop-report-dao` 依赖（不引入 nop-report-core/service，避免拉入 XPT 引擎）
   - 实现端到端数据绑定管线（`io.nop.datav.service.query` 包）：
     - `PanelDataBinder`：编排 面板 → DatasetRef → NopReportDataset → SQL 查询 → 结果回传
     - `PanelParamEvaluator`：paramMapping JSON 解析 + 参数求值（source + defaultValue 规则）
     - `PanelSqlBuilder`：`dsText` 中 `${paramName}` 占位符替换为顺序 `?`，通过 `SQL.begin().sqlWithParams(...)` 注入（避免 SQL 注入）
   - `PanelDataResult`（nop-datav-dao）：结构化结果（panelId + componentType + hasDataset + columns + rows），不引用 nop-report 实体类型
   - `NopDatavPanelBizModel.getPanelData`（`@BizQuery`）+ `INopDatavPanelBiz` 接口声明
   - 文本/iframe 类面板（无数据集）返回明确 `hasDataset=false` 标识
   - 错误路径全部显式抛 `NopException`（PANEL_NOT_FOUND / DATASET_REF_NOT_FOUND / DATASET_NOT_FOUND / UNSUPPORTED_DATASET_TYPE / QUERY_FAILED）
   - 8 个新测试覆盖（`TestNopDatavPanelBizModel`）

3. **Phase 3 - 刷新机制（D1-3）**
   - `PanelRefreshConfig`：从 panelConfig JSON 的 `refresh` 区域解析 enabled + intervalSeconds（不新增 ORM 列）
   - `NopDatavPanelBizModel.refreshPanel`（`@BizMutation`）复用 getPanelData 管线触发重新查询
   - 10 个新测试覆盖（`TestPanelRefreshConfig` + refreshPanel 测试）

4. **Phase 4 - 端到端验证（D1-5）**
   - `TestNopDatavPanelDataE2E`：贯穿「创建看板 → 建业务表 → NopReportDataset → DatasetRef → Panel → getPanelData → refreshPanel → publishDashboard」完整链路
   - 覆盖 2 种组件类型（chart + table）
   - 参数化查询验证：相同 panel/datasetRef，不同参数值产生不同结果（证明参数求值真正生效）

### 验证结果

- `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests`：BUILD SUCCESS
- `./mvnw test -pl nop-datav/nop-datav-service -T 1C`：38 tests, 0 failures（12 原有 + 5 组件注册表 + 8 面板 + 10 刷新 + 2 e2e + 1 smoke）
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-datav --severity high`：0 findings（无空壳实现）
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：通过（plan 完成后）

### 技术发现

- nop-report-dao 的 ORM 模型在 `AbstractNopDatavTest.initDatavSchema()` 自动建表循环中被自动创建（因为 `ormSessionFactory.getOrmModel().getEntityModelsInTopoOrder()` 涵盖所有模块），无需修改测试基类
- `NopReportDataset.dsText` 字段虽标注 `stdDomain="json"` + 绑定 JsonOrmComponent，但 `getDsText()` 直接返回 String；对 dsType=sql 的数据集，存的就是纯 SQL 文本（运行时验证假设成立）
- `CrudBizModel` 父类已含 private `daoProvider` 字段和 `daoProvider()` 方法，子类应调用方法而非重新声明字段（否则 NopIoC 不会注入子类覆盖的字段）
- nop 平台 SQL 用 `?` 位置参数 + Marker 机制，不是命名参数；通过 `sqlWithParams(text, paramsList)` 按出现顺序注入
- `IDataRow.toMap()` 直接返回 Map<String,Object>（基于 IDataSetMeta 的字段名），无需自己用 ColumnMapRowMapper
- JsonTool 严格模式对 invalid JSON 抛 NopException（具体错误码为 `nop.err.core.json.strict-mode.*`），wrapper 需统一包装为 datav 域错误码并保留 cause

### 产出文件

- `ai-dev/design/nop-datav/runtime-design.md`（D1 设计文档定稿）
- `nop-datav/model/nop-datav.orm.xml`（panel-type dict 扩展）
- `nop-datav/nop-datav-dao/src/main/java/io/nop/datav/biz/PanelDataResult.java`（新）
- `nop-datav/nop-datav-dao/src/main/java/io/nop/datav/biz/INopDatavPanelBiz.java`（接口扩展）
- `nop-datav/nop-datav-service/pom.xml`（新增 nop-report-dao 依赖）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/NopDatavErrors.java`（新增错误码）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/component/`（6 个新类：PanelComponentTypes/PanelComponentMeta/IPanelComponent/PanelComponentRegistry/SimplePanelComponent/PanelTypeMapping）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/query/`（4 个新类：PanelDataBinder/PanelParamEvaluator/PanelSqlBuilder/PanelRefreshConfig）
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/entity/NopDatavPanelBizModel.java`（实现 getPanelData + refreshPanel）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/component/TestPanelComponentRegistry.java`（新）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/query/TestPanelRefreshConfig.java`（新）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/entity/TestNopDatavPanelBizModel.java`（新）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/entity/TestNopDatavPanelDataE2E.java`（新）

---

## nop-datav D0 阶段完成

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-09-2255-1-dashboard-model-crud-and-tests.md` 的全部 4 个 Phase：

1. **Phase 1 - 模块骨架、ORM 模型设计与标准 CRUD 接通**
   - 手工创建 nop-datav 标准 8 件套模块结构（model + codegen/dao/meta/service/web/app/api），基于 nop-job 参考模式
   - 编写 `model/nop-datav.orm.xml`：5 个实体（Dashboard、Panel、DashboardTab、DatasetRef、DashboardSnapshot），含 domains/dicts/i18n-en
   - 通过 `gen-orm.xgen` 触发 codegen 生成 `_app.orm.xml`、`_gen/_NopDatav*.java`、`_service.beans.xml`
   - 手写 5 个保留层 BizModel + 5 个 IBiz 接口，接通 IoC
   - 冒烟测试通过：IoC + DB 管道连通

2. **Phase 2 - 发布/快照自定义 biz 语义**
   - `NopDatavDashboardBizModel` 实现 `publishDashboard`/`getPublishedDashboard`/`rollbackDashboard`
   - 发布时序列化看板编辑内容为 JSON 快照写入快照表，主表更新发布状态/版本
   - 回滚时从历史快照恢复主表字段
   - 所有方法在 `INopDatavDashboardBiz` 接口声明

3. **Phase 3 - 设计文档定稿**
   - `model-design.md` 从 stub 重写为最终设计文档
   - 记录实体职责划分、发布/快照方案选择、DatasetRef 归属方案、拒绝的替代方案

4. **Phase 4 - 单元测试覆盖**
   - `TestNopDatavDashboardBizModel`：11 个测试覆盖 CRUD（4 实体）、发布、查看已发布、回滚、端到端
   - `TestNopDatavSmoke`：冒烟测试验证 IoC 管道连通
   - 全部 12 个测试通过

### 技术发现

- `initDatabaseSchema=true` 的 `DataBaseSchemaInitializer` bean 在 ALL_LAZY IoC 模式下不被触发（`ioc:after` 不构成强制依赖）。创建 `AbstractNopDatavTest` 基类在 `@BeforeEach` 中手工建表作为 workaround。这是全平台预存问题（nop-job 测试单独运行时同样如此）。
- `QueryBean.addOrderField(name, desc)` 第二参数是 `desc`（降序），非 `asc`。初次使用时方向搞反导致版本号查询返回最旧而非最新。
- nop-bom 需注册新增模块的版本管理条目。

### 产出文件

- `nop-datav/model/nop-datav.orm.xml`
- `nop-datav/nop-datav-{codegen,dao,meta,service,web,app,api}/` 全套
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/entity/NopDatav*BizModel.java`（5 个）
- `nop-datav/nop-datav-dao/src/main/java/io/nop/datav/biz/INopDatav*Biz.java`（5 个）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/entity/`（3 个测试类）
- `ai-dev/design/nop-datav/model-design.md`（定稿）
- `nop-bom/pom.xml`（新增 nop-datav 模块条目）
