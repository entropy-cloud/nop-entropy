# 2026-08-10 开发日志

## nop-datav D0 阶段完成

### 完成内容

执行 plan `ai-dev/plans/nop-datav/2026-08-09-2255-1-dashboard-model-crud-and-tests.md` 的全部 4 个 Phase：

1. **Phase 1 - 模块骨架、ORM 模型设计与标准 CRUD 接通**
   - 手工创建 nop-datav 标准 8 件套模块结构（model + codegen/dao/meta/service/web/app/api），基于 nop-job 参考模式
   - 编写 `model/nop-datav.orm.xml`：5 个实体（Dashboard、Panel、DashboardTab、DatasetRef、DashboardSnapshot），含 domains/dicts/i18n-en
   - 通过 `gen-orm.xgen` 触发 codegen 生成 `_app.orm.xml`、`_gen/_NopDatav*.java`、`_service.beans.xml`
   - 手写 5 个保留层 BizModel + 5 个 IBiz 接口，接通 IoC
   - 冒烟测试通过：IoC + DB 管道连通

2. **Phase 2 - 发布/快照自定义 biz 语义**
   - `NopDatavDashboardBizModel` 实现 `publishDashboard`/`getPublishedDashboard`/`rollbackDashboard`
   - 发布时序列化看板编辑内容为 JSON 快照写入快照表，主表更新发布状态/版本
   - 回滚时从历史快照恢复主表字段
   - 所有方法在 `INopDatavDashboardBiz` 接口声明

3. **Phase 3 - 设计文档定稿**
   - `model-design.md` 从 stub 重写为最终设计文档
   - 记录实体职责划分、发布/快照方案选择、DatasetRef 归属方案、拒绝的替代方案

4. **Phase 4 - 单元测试覆盖**
   - `TestNopDatavDashboardBizModel`：11 个测试覆盖 CRUD（4 实体）、发布、查看已发布、回滚、端到端
   - `TestNopDatavSmoke`：冒烟测试验证 IoC 管道连通
   - 全部 12 个测试通过

### 技术发现

- `initDatabaseSchema=true` 的 `DataBaseSchemaInitializer` bean 在 ALL_LAZY IoC 模式下不被触发（`ioc:after` 不构成强制依赖）。创建 `AbstractNopDatavTest` 基类在 `@BeforeEach` 中手工建表作为 workaround。这是全平台预存问题（nop-job 测试单独运行时同样如此）。
- `QueryBean.addOrderField(name, desc)` 第二参数是 `desc`（降序），非 `asc`。初次使用时方向搞反导致版本号查询返回最旧而非最新。
- nop-bom 需注册新增模块的版本管理条目。

### 产出文件

- `nop-datav/model/nop-datav.orm.xml`
- `nop-datav/nop-datav-{codegen,dao,meta,service,web,app,api}/` 全套
- `nop-datav/nop-datav-service/src/main/java/io/nop/datav/service/entity/NopDatav*BizModel.java`（5 个）
- `nop-datav/nop-datav-dao/src/main/java/io/nop/datav/biz/INopDatav*Biz.java`（5 个）
- `nop-datav/nop-datav-service/src/test/java/io/nop/datav/service/entity/`（3 个测试类）
- `ai-dev/design/nop-datav/model-design.md`（定稿）
- `nop-bom/pom.xml`（新增 nop-datav 模块条目）
