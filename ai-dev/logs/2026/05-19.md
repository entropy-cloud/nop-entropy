# 2026-05-19

## Plan 21: nop-job Adversarial Review Remediation — COMPLETED

All 3 phases of Plan 21 executed. 31 adversarial review findings (F1-F15, R2-1 to R2-22) fixed across 39 files.

### Phase 1 — Live Defects (completed)
- F1+F13: Dispatch timeout dead code removed, `startTimeOrNow` fallback added
- F2: Overlay stats (totalFireCount, failFireCount, lastFireStatus, lastEndTime) updated in `cancelFire`
- F3: TERMINAL_FIRE_STATUSES constant centralized in IJobFireStore
- F4: `toTriggerSpec`/`toEvalContext` extracted to shared `TriggerSpecHelper`
- F5: `fireCount` → `totalFireCount` in eval context for maxExecutionCount
- F7: Sharding attributes (targetHost, shardingIndex, shardingTotal) added to task creation
- F12: `insertManualFire` return type changed to `boolean` (fire discarded on active fires)
- R2-1: PARALLEL block strategy sets `activeFireCount = 0` (never blocks)
- R2-3: Removed dead single-task `insertTaskAndMarkFireDispatching` overload
- R2-4: `timeoutSeconds` wired from schedule into timeout checker
- R2-5: SUSPICIOUS treated as pending in `resolveFinalFireStatus`
- R2-12: RECOVERY with no failed fires creates a new fire (previously silently dropped)

### Phase 2 — Contract & Safety (completed)
- F8+R2-10: Worker SUSPICIOUS overwrite race fixed — guard + fresh task reload
- F6: `@Transactional(REQUIRES_NEW)` added to `tryLockTasksForExecute`
- F10: `NopJobTaskBizModel.delete()` overridden to throw `NopException`
- F11: `resolveFinalFireStatus` documented with priority chain and SUSPICIOUS handling
- F15: try-catch added to `overlayFireAndAdvanceSchedule` cancel loop with logging
- R2-19: `OnceTrigger` wired in `TriggerBuilder` for non-cron, non-periodic triggers

### Phase 3 — Dead Feature Cleanup (completed)
- R2-6: `pauseCalendarSpec` JSON parsed into `CalendarSpec` list in `TriggerSpecHelper`
- R2-16: Dead `newTask()`/`saveTask()` removed from `IJobTaskStore` interface and impl
- R2-17: Dead `updateTaskProgress()` removed from interface and impl
- R2-18: `targetHost` documented as reserved for future use in `DefaultJobTaskBuilder`

### Verification
- `./mvnw compile -pl nop-job` ✅
- `./mvnw test -pl nop-job` ✅ (all tests pass)
- Agent overreach in `DefaultJobTaskBuilder` (removed taskPayload population) reverted
