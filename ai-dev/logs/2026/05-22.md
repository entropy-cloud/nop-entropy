# 2026-05-22

### Plan 37 执行：nop-stream Round3 对抗性审查关键缺陷修复

- Plan 37 全部 5 个 Phase 完成 + focused test 补充 + `OperatorSnapshotResult` 便捷方法改进
- 修改 5 个文件：
  - **HeapInternalTimerService.java**（N73+N74）：
    - `advanceWatermark` 复制 Set→ArrayList 避免回调中删除导致 CME
    - `TimerEntry` 持有 (key, namespace, timestamp) 三元组
    - 构造函数重载添加 `Supplier<Object> currentKeySupplier`（向后兼容 null supplier）
    - `HeapInternalTimer.getKey()` 返回 timerEntry.key
  - **StreamTaskInvokable.java**（N83+N84）：
    - `invokeMiddle`/`invokeSink` 在 inputGate 耗尽后发送 `Watermark.MAX_WATERMARK`
    - `processInputGate` 添加 `WatermarkStatus` 处理分支
  - **StreamReduceOperator.java**（N76）：
    - 使用 `OperatorSnapshotResult.putOperatorStateJava()` / `getOperatorStateJava()` 便捷方法
    - 覆写 `snapshotState(StateSnapshotContext)` / `restoreState(OperatorSnapshotResult)`
  - **WindowAggregationOperator.java**（N77）：
    - 新增 `TriggerStateKey<K,W>` 静态内部类替代 String 拼接
    - `triggerState` 类型从 `Map<String, ...>` 改为 `Map<TriggerStateKey<K,W>, ...>`
    - `purgeWindow` 使用 Iterator 安全删除 triggerState 条目
  - **OperatorSnapshotResult.java**（API 改进）：
    - 新增 `putOperatorStateJava/getOperatorStateJava` — Java 序列化便捷方法
    - 新增 `putOperatorStateJson/getOperatorStateJson` — JSON 序列化便捷方法（与设计文档一致）
    - 算子代码不再需要手动处理 ObjectOutputStream/ObjectInputStream
- 新增 focused test：
  - **TestHeapInternalTimerService**：+3 tests（CME 回调删除、key supplier、null supplier）
  - **TestStreamReduceOperator**：+6 tests（reduce 累加、多 key、snapshot/restore 跨实例、空快照恢复）
  - **TestTriggerStateKeyCollision**：+5 tests（`#` 分隔符碰撞、不同 descriptor/窗口、复杂 key）
  - **TestStreamTaskInvokableProcessInput**：+3 tests（WatermarkStatus 不被丢弃、InputGate 读取路由）
- 验证：`./mvnw test -pl nop-stream -am` — BUILD SUCCESS
- N75（KeyExtractingExtractingOutput 跨 chain keyed state）deferred — 需架构设计决策
- 设计债务记录：设计文档规定用 `JsonTool`（JSON 序列化），但 `MemoryKeyedStateBackend` 实际用 Java 序列化。`OperatorSnapshotResult` 现在提供两种便捷方法供算子选择
- Doc-sync: No owner-doc update required

### Plan 34 执行：nop-stream 测试补充

- Plan 34 全部 4 个 Phase 完成（CEP 核心测试移植、Execution 层测试、Watermark/Event-time 测试、Connector 假阳性修复）
- 新增/修改 13 个测试文件，约 55 个新测试用例，全部通过：
  - **CEP 模块（5 文件 → 25 tests）**：
    - `TestDeweyNumber`（10 tests）：解析、比较、increase、addStage、isCompatibleWith
    - `TestNFA`（5 tests）：pattern 匹配成功/失败/followedBy/followedByAny/部分匹配
    - `TestSharedBuffer`（4 tests）：注册事件、多事件、isEmpty、节点注册
    - `TestNFACompiler`（5 tests）：编译验证、状态数、转移、null pattern、重复名称
    - `TestPattern`（1 test，修复）：添加断言验证匹配结果的 size 和各字段值
  - **Execution 层（4 文件 → 21 tests）**：
    - `TestInputGate`（5 tests）：单通道、多通道 watermark 合并、round-robin、完成检测
    - `TestInputGateBarrierAlignment`（4 tests）：基本对齐、乱序 barrier、中间记录、无下溢
    - `TestGraphExecutionPlan`（6 tests）：线性链、菱形 DAG、单节点、有环图、顶点保留、invokables
    - `TestRecordWriter`（6 tests）：单分区、watermark 广播、barrier 广播、partitioner 路由、close EOS
  - **Watermark 层（3 文件，2 个 @Disabled）**：
    - `TestWatermarkPropagation`（3 tests）：单调递增、转发、无重复
    - `TestWatermarkIdleDetection`（1 test，@Disabled N46）
    - `TestWindowOperatorWatermarkReception`（1 test，@Disabled N45）
  - **Connector（2 新文件 + 2 修复）**：
    - 修复 `TestMessageAdapters`：sink 测试先 subscribe 再 send，catch(Exception ignored) → rethrow
    - 修复 `TestBatchLoaderSourceFunction`：catch(Exception ignored) → rethrow
    - `TestBatchConsumerSinkFunctionFailure`（2 tests，@Disabled N53）
    - `TestDebeziumCdcSourceCompletion`（1 test，@Disabled N54）
- 基础设施变更：给 `Event.java` 和 `SubEvent.java` 添加 `equals()`/`hashCode()`
- 验证：`./mvnw test -pl nop-stream-cep,nop-stream-core,nop-stream-connector -am` — 465 tests, 0 failures, 5 skipped（@Disabled bug markers）
- Doc-sync: No owner-doc update required

### Plan 33 执行：nop-stream Checkpoint 测试覆盖补救

- Plan 33 全部 4 个 Phase 完成（Phase 0 序列化探索、Phase 1 CheckpointPlan 测试、Phase 2 E2E 测试、Phase 3 并发测试）
- 新增 7 个测试文件，37 个新测试用例，全部通过：
  - `TestCheckpointPlanSerialization`（nop-stream-core, 5 tests）：序列化输出验证 + TaskLocation Map key equals/hashCode
  - `TestCheckpointPlanBuilder`（nop-stream-runtime, 6 tests）：三 vertex plan 构建 + 异常输入
  - `TestE2EBuildSnapshotFromTaskState`（nop-stream-runtime/execution, 5 tests）：状态路由回归测试，验证只有 mapped operator 的 state 被提取
  - `TestE2EStorageTypeRouting`（nop-stream-runtime/execution, 4 tests）：jdbc storageType 抛异常、local 返回 storage
  - `TestE2EMultipleJobsIsolation`（nop-stream-runtime, 1 test）：jobId 隔离，两个作业的 checkpoint 互不干扰
  - `TestE2EMultiVertexCheckpoint`（nop-stream-runtime, 3 tests）：多 vertex plan 验证 + executeWithCheckpoint 端到端 + coordinator 级别多 vertex checkpoint
  - `TestCheckpointBarrierTrackerConcurrency`（nop-stream-core, 13 tests）：重叠保护 + 并发 ACK（10线程 + CountDownLatch）+ @RepeatedTest(10)
- 生产代码变更（最小可见性）：
  - `GraphModelCheckpointExecutor.buildSnapshotFromTaskState`：`private static` → `static`
  - `GraphModelCheckpointExecutor.createStorage`：`private static` → `static`
- 发现的问题：
  - **JsonTool 反序列化不支持 @DataBean 不可变对象**：`TaskLocation`/`OperatorStateMapping` 有 final 字段 + all-args constructor，`BeanTool.buildBean` 使用默认构造函数 + setter，但无 setter。`jackson-databind` 不在 classpath 中。生产代码不依赖 `CheckpointPlan` 的 JSON 反序列化，不需要修复。
  - **CheckpointBarrierTracker 多余 ACK 触发回调**：`operatorsToAck.decrementAndGet() <= 0` 在计数器已为 0 时仍触发 callback（第三次 ACK 后计数器为 0，第四次降到 -1 再次触发）。记录为 known issue。
- 验证：`./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime -DforkCount=1 -DreuseForks=false` — 138 tests, 0 failures

### Plan 32 执行：nop-stream Checkpoint 完整实现

- Plan 32 全部 7 个 Phase 完成（Phase 1-6 代码实现，Phase 7 测试验证和文档）
- 关键改动：
  - 新增 `TaskLocation` / `OperatorStateMapping` / `CheckpointPlan` 数据类，修复 keyed state 碰撞和状态恢复路由错误
  - 新增 `CheckpointPlanBuilder`，从 `GraphExecutionPlan` 生成 checkpoint plan
  - `TaskStateSnapshot.taskId` 从 `long` 改为 `TaskLocation`，`CompletedCheckpoint.jobId/pipelineId` 从 `long/int` 改为 `String`
  - `ICheckpointStorage` 接口所有方法签名从 `(long jobId, int pipelineId)` 改为 `(String jobId, String pipelineId)`
  - `CheckpointBarrierTracker` 线程安全化：`volatile` + `AtomicInteger` + `synchronized`，重叠保护返回 false
  - `StreamSourceOperator` SourceContext 拦截 barrier 注入（`pollPendingBarrier`），有限 source 兼容回退
  - `GraphModelCheckpointExecutor` 重构：提取 12 个公共方法消除重复，使用 `CheckpointPlan` 精确恢复，修复 6 个 critical bugs
  - `JdbcCheckpointStorage` 基于 `IJdbcTemplate` + `SQL.begin()` 重写，延迟建表，H2 测试通过
  - `CheckpointMetrics` 接入 `CheckpointCoordinator`，complete/abort 更新计数器，executor 退出时日志摘要
  - `ReplayableSourceFunction` 接口 + `CollectionReplayableSource` 参考实现，`StreamSourceOperator` 自动管理 offset
  - `CheckpointConfig` 新增 `jobId`（默认 UUID）和 `pipelineId`（默认 "1"）字段
- 验证：`./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime -DforkCount=1 -DreuseForks=false` — 377 core + 119 runtime tests 全通过
- 偏差：`GraphExecutionPlan.build()` 的 `.get(0)` 单 chain 假设未修复（涉及 `StreamTaskInvokable` 架构变更，当前场景无实际影响）
- Doc-sync: No owner-doc update required for Phase 1-6; Phase 7 文档更新待完成
