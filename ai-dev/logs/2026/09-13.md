# 2026-09-13

### Plan 355 Phase 1+2 完成：nop-ai-agent 5 个超长方法行为保持拆分（extract-method）

- Plan：`ai-dev/plans/355-nop-ai-agent-structure.md`（Phase 1/2 completed；Phase 3-5 未动）。纯 extract-method 重构，行为零变化（不改逻辑/顺序/条件/事件/状态迁移；不改可见性/签名/异常/日志文案；测试零改动——`git status` 仅 5 个 engine 主源码文件 + plan/log）。
- 拆分明细（方法名: 行数，wc 口径）：
  - `ReActAgentExecutor.execute` 706→**145** 行（sustainLoop/reactLoop 双层骨架 + sustain consult 留在 execute）→ `runPreCallPhase`:10 / `runSingleIteration`:89 / `checkIterationStartGuards`:89 / `runPreReasoningGate`:26 / `prepareRoutedRequest`:71 / `recordLlmTurn`:35 / `recordLlmTurnUsage`:25 / `saveLlmTurnCheckpoint`:47 / `adjudicateResponse`:83 / `runToolFanout`:88 / `adjudicateTerminal`:23 / `canPublishExecutionCompleted`:8（终态多枚举链原样搬运为判定方法）/ `publishExecutionCompleted`:32；嵌套类型 `IterationFlow`(PROCEED/CONTINUE_LOOP/BREAK_LOOP)、`ExecutionState`（lastModelKey/messageSeq/checkpointSeq/consecutiveContinues/bailCount 的 per-execute holder，未提升为实例字段）、`RoutedRequest`、`LlmTurn`。
  - `AgentToolDispatcher.executeAllowedCalls` 270→**40** 行 → `buildToolFutures`:75（AR-15 fan-out 构建）/ `awaitToolFutures`:30（14-02 可中断 join）/ `commitToolResults`:145（per-tool 提交循环，reenterRequested 以返回值传出）。
  - `LlmCallCoordinator.doLlmCallWithRetry` 246→**17** 行 → `ensureCircuitAllowsCall`:14 / `runRetryLoop`:66（PRE/POST_LLM_ATTEMPT veto 骨架）/ `handleResponseLevelOutcome`:90（响应级重试决策+双 FALLBACK 通道）/ `handleTransportLevelOutcome`:44 / `finalizeLlmCallResult`:19；嵌套 `RetryFlow`、`RetryState`（attempt/lastError/accountChain/failoverChain/lastClassification/executionVetoCount/routedOptions/llmCallStart/fallbackExhausted 跨 attempt 状态 holder）。
  - `AgentSessionLifecycle.restoreSession` 210→**17** 行 → `validateRestorableSession`:31 / `verifyLatestCheckpoint`:45 / `transitionToRunningAndPublishRestored`:28 / `buildExecutionWiring`:15 / `acquireRestoreExecutionSlot`:24 / `dispatchRestoreExecution`:75；嵌套 `CheckpointVerification`、`ExecutionWiring` record。
  - `DefaultAgentEngine.doExecute` 200→**5** 行 → `assembleExecutionSetup`:57（入口校验/装配）/ `registerExecutionSlot`:38（takeover-lock 注册）/ `dispatchExecution`:118（异步执行+对称清理）；嵌套 `ExecutionSetup` record。
- 关键手法：循环体控制流映射（continue/break/break label → 返回 `IterationFlow`/`RetryFlow`/boolean，调用点语义逐一对应）；lambda 捕获变量经参数/holder 传入；两处死局部变量（`reentryCounters`@原 execute:451、`messagesAtCallTime`@原 execute:629）按"纯移动"原则原样保留。
- 偏差：①测试命令用 `./mvnw test -pl nop-ai/nop-ai-agent`（依赖已 install，未加 -am，结果等价）；②终态判定方法名按实际代码形态命名为 `canPublishExecutionCompleted`（计划草案写 canComplete/canFailed/canSustained，实际 execute 内唯一多枚举链是 POST_CALL/EXECUTION_COMPLETED 发布门）；③自审发现一处契约注释缩短（restoreSession 的 plan 182 checkpoint journal 注释），已恢复原文。
- 验证：每方法拆分后各跑一轮 + 最终一轮，共 6 轮 `./mvnw test -pl nop-ai/nop-ai-agent`，均 **3387/3387 绿**（0 失败 0 跳过）；focused 抽查 `TestModelSwitchedMessage`(5) `TestCircuitAwareRouting`(8) `TestAccountFallbackChain`(4) `TestProviderFailoverChain`(6) `TestExecutionMiddlewareLlmRetry`(8) `TestReActAgentExecutor`(9) `TestSmartModelRouterFallback`(9) `TestPlan271AsyncTimeoutReliability`(5) 共 54/54 绿。纯移动自审：归一化行集合差异审计（AgentToolDispatcher/DefaultAgentEngine 残留 removed=0；其余残留均为流程控制映射/holder 访问/新签名注释）+ 契约注释 marker 计数 old vs new 全等或仅新增（AR-06/AR-03/AR-11/AR-14/W5-3/W3-1/plan 205/187/277/280/327/329/14-02/D4/裁定 C/D/E 等无丢失）。
- Doc-sync: No owner-doc update required（纯内部重构，无对外契约变化）。

### Plan 354 完成：nop-ai-agent 持久化归一与能力复用裁定（审计修复系列 5/8）

- 产物：`ai-dev/plans/354-nop-ai-agent-persistence.md`（completed；独立 closure audit agent_1ef144a9 条件通过——1-5 维度全 PASS，2 项诚实性发现同日修补）。
- 修复：AI-2 双写——`IModelSwitchedMessageWriter` SPI 下沉 nop-ai-core（service 的 core 依赖 test→compile），`OrmModelSwitchedMessageWriter`（nop-ai-service，IEntityDao 管道，11 列语义与 Db 版逐列等价经 audit 核证）+ bean 注册 + 回归测试；`DbModelSwitchedMessageWriter` @Deprecated（DDL 冲突警告，embedded-only）；store 层 13 类 + 3 reader 边界契约文档化（`ai-dev/design/nop-ai-agent/store-layer-contract.md`：表清单/租户软删不适用理由/AI-8/9/10/11 四裁定/AI-19/拒绝的替代方案）+ 16 类 javadoc 引用 + owner doc 补段；AI-16 包移动 credential→infra（纯净移动，beans/测试同步）；AI-17 readOldCredentialId 改 findFirstByExample 强类型 + delete 捕获裁定注释。
- 验证：nop-ai-agent 3387/0、nop-ai-service 37/0（含新 TestOrmModelSwitchedMessageWriter）、core 379/0（-am）；check-doc-links --strict 0 errors；checklist --strict 通过。
- 偏差：包移动触发 AOP 生成缓存失效需 clean 构建；closure audit 修正证据形式（log 截断以 surefire 时间戳为准）与陈旧 javadoc。

- Doc-sync: `docs-for-ai/03-modules/nop-ai.md`（store 层段 + Orm 装配说明）、`ai-dev/design/nop-ai-agent/store-layer-contract.md`（新）。

### Plan 353 完成：nop-metadata 合规收口（审计修复系列 4/8）

- 产物：`ai-dev/plans/353-nop-metadata-conformance.md`（completed；独立 closure audit agent_d43ee06e 8/8 PASS）。
- 修复：MD-1 委托子代理（agent_905fd567）批量转换 15 个 entity BizModel 的跨聚合访问为 I*Biz 注入管道（37 行调用点，context 全链透传，0 null-context），19 处边界保留带裁定注释（16 resolver/writer/inferrer + 3 全图无界加载——主审计复核发现 findList 的 max-page-size=1000 截断风险后裁定回退 dao）；MD-2 实体领域方法下沉（NopMetaTable.isEntityTable/isExternalTable/isSqlTable + NopMetaDataSource.isDisabled，dao 层 NopMetadataDaoConstants 镜像常量并核对值一致）；MD-3 INopMetaDataSourceBiz 补 bindCredential/unbindCredential/migrateDataSourcesCredential 三方法 + CredentialBindResultDTO/CredentialMigrationResultDTO 替代 Map 返回（getter 名与原 Map 键一致保持 GraphQL 面兼容，接口完整性守卫测试 COVERAGE 同步）；MD-5 quality 包状态字面量归零（全走 _NopMetadataCoreConstants）；MD-7 三凭证方法 @Auth(NopMetaDataSource:write) 静态兜底（动态 CSV 双层保留）；MD-9 owner doc ErrorCode 组织描述修正；MD-6 nop-rule 不适配裁定登记；MD-8 Deferred 登记。
- 验证：`./mvnw test -pl nop-metadata/nop-metadata-service` 最终 1339/0（三轮迭代修复：DTO selection 补齐/常量镜像值勘正/import 补漏）；check-doc-links --strict 0 errors；scan-hollow exit 0；check-plan-checklist --strict 通过。
- 偏差：MD-1 经对抗式复核发现委托转换的 3 个全图加载点存在 Biz findList 1000 行上限截断风险——裁定回退 dao 直连并注释（诚实降级而非静默）；audit minor 项（@Override 缺失）已补。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md`（ErrorCode 描述/接口清单补 3 方法/nop-rule 裁定段）。

### Plan 355 完成：nop-ai-agent 结构重构（审计修复系列 6/8）

- 产物：`ai-dev/plans/355-nop-ai-agent-structure.md`（completed；Phase1+2 委托 agent_c2e351b7 + 独立 closure audit agent_a2f1083e 1-5 维度全 PASS，诚实性发现的日志缺口与行数笔误已修补）。
- 修复：AI-1 execute 706→145 行（runPreCallPhase/runSingleIteration/adjudicateTerminal + canXxx 判定组 + IterationFlow/ExecutionState holder，类级字段零新增，契约注释 AR-06/plan 205/187/277 计数零丢失）；AI-12b executeAllowedCalls 270→40、doLlmCallWithRetry 246→17、restoreSession 210→17、doExecute 200→5（归一化 diff：Dispatcher/Engine removed=0 纯移动）；AI-7 ChannelLoginScanProcessor 四步编排 + NopAiGatewayErrors 6 独立码（BizModel 薄入口 21 行，消息文本兼容测试断言）；AI-12a NopAiSession.isIdle/isTerminal（**无仓库内消费点**——运行态走 AgentExecStatus 属 P2-MA1-035 边界，方法为审计指向的稳定判定面）；AI-13 TeamTaskStatus 裁定注释。
- 验证：6 轮迭代测试 + 最终 agent 3387/0、gateway 178/0、dao 6/0（时间戳闭环）；check-doc-links --strict 0 errors（顺带修复 354 引入的 docs-for-ai→ai-dev 边界违规）；checklist --strict 通过。
- **剩余 >150 行方法清单（optimization candidate，Deferred）**：AgentSecurityConsultation.buildCheckpointChain 174、AgentSessionLifecycle.resumeSession 168、TeamTaskUpdateExecutor.doExecuteAsync 159——非审计 Top5，登记后续演进。
- Doc-sync: No owner-doc update required（裁定在代码注释与 plan）。

### Plan 356 完成：nop-ai 异常 ErrorCode 化（审计修复系列 7/8）

- 产物：`ai-dev/plans/356-nop-ai-error-codes.md`（completed；独立 closure audit agent_2fcafe2b 6/6 PASS）。
- 修复：AI-14 四模块 main 裸 IAE/ISE 归零（agent 72/core 11/toolkit 10/shell 6 → 0，detail param 保真原消息 + cause 保留）；AI-15 agent 112 处 message-only NopAiAgentException → ERR_AGENT_INTERNAL_DETAIL.param(detail)（消息保真）；新码 8 个（agent 3/core 2/toolkit 2/shell 1）；NopAiAgentException/NopAiCoreException 协变 param 覆盖；S08 两类裁定保留（BashSandboxException 工具结果协议/ParseException 解析器控制流）；AI-21 *Service 命名 Deferred 登记。
- 验证：四模块全绿（agent 3387/core 379/toolkit 202/shell 280，时间戳闭环）；2 处"原始异常透传契约"断言回退（fatal ISE 透传 + guardrail ISE 透传，audit 核证为传播契约非漏改）；checklist --strict 通过。
- Doc-sync: No owner-doc update required（错误码约定在代码内）。
