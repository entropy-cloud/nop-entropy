# 2026-09-13

## merge origin/master：根治 orm-model oneToOne schema 缺失，全链验证通过

- `git merge origin/master`（b40739b7a6，fetch 后）合入本分支，合并提交 b9655fb3ce。冲突 6 处全部解决：3 个日志（09-07/08/09，主仓修复日志与本 worktree 执行日志互补，取并集并以注释标注 worktree 归属）+ 3 个 docs-for-ai 文件（同义措辞精修，取 master 版）。
- **根治验证**：`nop-code-service -am test-compile` 全链 **MVN_EXIT=0、零 ERROR**（此前在 nop-orm-model 挂 `setOneToOne` 缺符号）；`nop-persistence` 工作树零漂移——59e73c16e4 的 entity.xdef oneToOne 修复合入后，codegen 再生的 `_gen` 类自带该属性，不再剥离。
- 合并后回归：nop-treesitter 0 失败（405+2 env-gated）、nop-code-lang-python 20/20、nop-code-lang-typescript 18/18、BUILD SUCCESS。
- 注：本地 master ref 曾陈旧（fc79056aab ≠ origin/master b40739b7a6），此前"master 未修 schema"的误判已更正。

## JNI→纯Java 迁移：nop-code-lang-python/typescript 切换到 nop-treesitter compat 层

- **迁移内容**：`PythonCodeFileAnalyzer`/`TypeScriptCodeFileAnalyzer` 换 import（`org.treesitter.*` → `io.nop.treesitter.compat.*`，`TSLanguage` → `TreeSitterLanguage`）；两模块 pom 删除 `io.github.bonede:tree-sitter/tree-sitter-python/tree-sitter-typescript` compile 依赖，改依赖 `nop-treesitter:2.0.0-SNAPSHOT`（注意 nop-code 模块族版本是 1.0.0-SNAPSHOT，与根 2.0.0-SNAPSHOT 不同，`${project.version}` 不可用需显式钉版本）。compat 层补两个缺口：`TreeSitterPython` 适配器（**静态接线 `PythonScanner` 工厂**——python blob 不含缩进 scanner，不接线则任何含换行缩进的源码都解析成 ERROR/MISSING）、`TSNode.eq` 静态结构相等（record 语义）。分析器逻辑零改动。
- **验证**：nop-code-lang-python **20/20**、nop-code-lang-typescript **18/18** 全绿（这些测试原本钉住 JNI 行为，构成迁移等价性证据）；nop-treesitter 自身 405 green；两消费模块无 `org.treesitter`/bonede 残留。
- **调试记录**：首跑消费测试 5 处失败（类内方法/嵌套类丢失）——探针定位为 python 外部 scanner 未接线（corpus 测试一直显式接线所以从未暴露）；修复后连 tab/2/3/4/8 空格缩进全部干净解析。
- **附带发现（非本迁移引入，已诊断清楚）**：`-am` 全量重建会在 nop-orm-model 编译失败（`OrmModelInitializer:564` 缺 `setOneToOne` 符号）。根因：上游 !161（38d9e97863，本分支祖先）把 oneToOne 属性**只手改进已提交的 `_gen/_OrmToOneReferenceModel.java`** 未进 schema；**origin/master 的 59e73c16e4（"修复全部单测失败，mvn install 全绿"）才把 oneToOne 正确补进 `entity.xdef`**——本分支分叉点早于该修复，故全量重建 codegen 从缺属性 schema 再生 `_gen` 时剥掉手补 28 行。**master 是修复方而非破坏方**；本分支增量构建不触发所以未暴露。处置：`_gen` 已恢复提交态；建议本分支 merge origin/master（59e73c16e4 是 44 文件大修提交，不宜单摘）以根治；未merge前避免跨 persistence 的 `-am` 全量重建。
- Doc-sync: `docs-for-ai/03-modules/nop-treesitter.md` 架构表新增 compat 迁移层条目 + JNI 迁移状态段。
- Commit: 本条目随迁移提交。

## 后续优化尝试：GLR slice 子树列表去装箱（ad-hoc，用户指定）

- JFR 重归因（收口后 HEAD）：`reduce → popCount → stackIter → appendReversed` 的 `ArrayList<Integer>`（Slice 子树路径）占采样权重 ~36%——装箱 + 扩容拷贝。改造为可增长原语缓冲 `SubtreeList`（cons 链两次走查、一次精确尺寸分配、零装箱），`Slice`/`addSlice`/reduce/`buildParent`/accept/`buildErrorComposite`（签名 List→SubtreeList）/两条 recovery 路径全部切换。
- 验证：405 tests green，corpus 逐字节无回归，等价测试 6/6（三路 walk trace 不变）。
- 实测：json-100k parse+render 分配 **16,958 → 16,098 KB/op（−5.1%）**；JMH 吞吐 183.6 ±29.5 vs 179.3 ±27.8 ops/s——误差带内持平（收益在 GC 压力不在 CPU）。**JFR 采样权重高估了该 site**（大块扩容拷贝被优先采样，短命小对象已被逃逸分析消除）——如实记录。
- 其余候选按 ≥5% 净收益阈值裁定不值得落地：Lexer Token/LexOutcome record→可变复用（~5%，public API 侵入，标量替换大概率已覆盖）、GSS 链接数组合一（~0.7 MB/op）、预留 margin 1.25→1.1（省 ~0.3 MB 但低估一次即触发 ~3.6 MB 整列拷贝，期望值更差）。剩余大头是树自身保留内存（arena 列 + record cache ≈6.7 MB/op），回收需 refcount + tree-release successor 设计。
- Doc-sync: perf-tuning.md 新增 "Follow-up attempt" 小节。
- 结论：**优化空间已基本收敛**——垃圾类分配所剩无几，进一步需要动 tree 生命周期（refcount successor）或 public API（收益低于阈值）。

## perf-closure 计划独立 closure audit 通过，计划置 completed

- 独立子 agent 审计（agent_a85f82b6，fresh session，只读约束 + git status 复核通过）：**0 Blocker；1 Major（Phase 2 的 JNI Exit Criterion 漏勾，证据原已在日志）+ 2 Minor（门禁余量措辞、golden 溯源间接证据链接受）全部处置**。
- Anti-Hollow 逐点代码级验证 PASS（TSNode scratch/fresh 分界、ScannerVM 11 字段清点、render Meta、双 parse 路径预预留+回写、GLR 侧表、validSymbols per-parseState）；数字复算全部自洽（−28%、1.20x/1.26x/1.56x/2.76x）；405 tests 复现；checklist --strict / hollow scan / doc-links 三工具 exit 0。
- 计划 `2026-09-12-1000-1-perf-cursor-and-benchmarks.md` → **completed**（closure evidence 已写入 plan 文件）；roadmap item 17 注记补 successor 收口结果。
- Commit: `070f8a652e`（实现）、`0f9e6200aa`（计划收口）；roadmap 注记与本条日志随下一提交。
- **Roadmap 终态保持 17/17 done + 全部 successor 计划关闭：nop-treesitter mission 彻底结束。**

## perf-closure 计划收口：等价测试抓出并修复 entryVisible 越界、门禁全达标、C 终判 4/4 ≤ 3x、JNI 反超

- **等价性测试 (a)–(f)** 落地为 `cursor/CursorReuseEquivalenceTest`（6/6 green）+ pre-change golden 渲染（`RenderGoldenDump` 在 HEAD 2312ea9eb4 上生成，提交于 `src/test/resources/render-golden/`）。三路 trace（step cursor / gotoChild 下降 / random-access scratch）逐字节一致；query 重叠路径、跨树复用、嵌套访问、json-1m 冷/热 arena 等价全部通过。
- **测试抓出 live defect 并修复**：`TSTreeCursor.entryVisible` 对容器链帧（保留 symbol = symbolCount+aliasCount 越界）与 builtin ERROR(=+1)/ERROR_REPEAT(=+2) 帧直接抛 `symbol id out of range`，`depth()`/`gotoParent`/`currentFieldId` 在任何 >8 子节点或恢复树上可达（WIP 前语义相同，非本次引入）→ chain 帧按不可见、builtin 走 `symbolVisibleOrBuiltin`。
- **Phase 2 门禁复测**（ThreadMXBean，同 session）：ts-8kb walk **75,803 → 153 KB/op（门禁 ≤10 MB/op，495 倍余量）**；json-100k parse+render **22,996 → 16,559 KB/op = −28%**（重导门禁 ≥−15%，超过 site 表 −24% 上界——sized builder 额外消除了 render 增长拷贝）。
- **JNI 对比（JMH）**：pure **42.5 → 101.4 ops/s** vs JNI 45.2 ±1.1 —— parity 维持并**反超 ~2.2x**；分配 86 MB → **3.8 MB/op**。
- **Phase 3 C 终判**（`bench/run-c-reference.sh`，final HEAD，同 session back-to-back）：json-10k 1142 vs 1371 = **1.20x**；json-100k 179 vs 225 = **1.26x**；json-1m 9.2 vs 14.4 = **1.56x**；java-single 404 vs 1115 = **2.76x** —— **4/4 全部 ≤ 3x**（item 13 收口时超标的 json-1m 3.50x、java-single 4.28x 均已入线），主收口门禁直接达成，无需备选 evidence-floor 路径。分配：json-10k 4.2→2.8 MB/op、json-1m 366→262 MB/op。
- perf-tuning.md 新增 "Perf closure (2026-09-13)" 收口节（机制、数字、残余候选：GLR 装箱 ~12% + LexOutcome ~5% JFR 占比，refcount 回收仍归 successor design）。
- 验证：`./mvnw test -pl nop-treesitter` **405 tests green**（399 + 等价测试 6，2 env-gated skip），corpus 无回归。
- Doc-sync: `nop-treesitter/docs/perf-tuning.md` 已更新（见上）；`docs-for-ai/03-modules/nop-treesitter.md` 无契约变化，`No owner-doc update required`。

## perf-closure 计划执行（Phase 1 测量 + Phase 2 落地）：遗留 WIP 三缺陷修复、同 session 基线、自适应预留

- Plan: `ai-dev/plans/nop-treesitter/2026-09-12-1000-1-perf-cursor-and-benchmarks.md`（roadmap 17/17 done 后的用户指定 successor，本日继续执行至收口）。
- **遗留 WIP 处置**：工作树继承自上一会话的未提交 Phase 2 实现（cursor 并行数组帧/resetTo 复用、TSNode thread-local scratch、render sized builder + Meta、ScannerVM 十一字段 reset + per-parseState validSymbols 缓存、GLR scratch 数组、arena 预留、bench 脚本 `test-compile` 修复）。复核发现三处缺陷并当场修复：
  1. **死循环根因**：`SubtreeArena.allocateChildren` 用 `children.length`（scratch 容量）而非 `count` 填 child 槽位 → 节点携带陈旧 id 形成环，`subtreeVisibleDescendantCount` DFS 不终止（`JavaCorpusTest` 挂死 100% CPU 26 分钟复现，jstack 定位）。上一会话遗留的 `halt(7)` 调试探针即为此而设；本日改为快速失败 `IllegalStateException`（TreeNavigator/TSTreeCursor 两处），并修根因。
  2. `TSTreeCursor.gotoChild/gotoNamedChild` 在调用会覆写 found* 字段的 `visibleChildCount/namedChildCount` 之后才读 `foundStructuralIndex/foundAlias` 压帧（脏值）→ 先拷贝到局部变量。
  3. GLRParser `stackScratch.length == 0` 死分支删除。

- **Phase 1 同 session 基线**（HEAD 2312ea9eb4，WIP stash 后测得，C 与 Java 同 session back-to-back）：

  | benchmark | Java (JMH) | C harness | ratio | Java alloc |
  |---|---|---|---|---|
  | json-10k | 1015.9 ±17.0 ops/s | 1396.9 ops/s | 1.38x | 4,188 KB/op |
  | json-100k | 159.6 ±4.0 ops/s | 225.0 ops/s | 1.41x | 23,019 KB/op |
  | json-1m | 7.80 ±0.13 ops/s | 14.5 ops/s | 1.86x | 366,202 KB/op |
  | java-single | 407.8 ±6.8 ops/s | 1120.6 ops/s | 2.75x | — |

  基线当日四项已全部 ≤ 3x（与 item 13 记录的 2.29x/2.56x/3.50x/4.28x 差异属机器/会话状态漂移，计划已预判 ±60% 误差带；终判以 Phase 3 final-HEAD 同 session 复测为准）。

- **ThreadMXBean 精确测量**（`bench/AllocProbe.java`，新增探针）：json-100k parse-only **22,581 KB/op**；parse+render **22,996 KB/op**（render share 仅 414 KB/op）；ts-8kb walk **75,803 KB/op**。
- **Per-site 分配表**（JFR ObjectAllocationSample，同 session）：
  - parse（json-100k）：int[]（arena 列+侧表几何倍增）≈66%、Integer（GLR collected 装箱）≈12%、Object[]（ArrayList backings）≈10%、LexOutcome ≈5%、GSSNode ≈4%、Subtree record ≈4%。
  - walk（ts-8kb）：int[] ≈40%、TSTreeCursor$Frame ≈17%、TreeNavigator$ChildRef ≈17%、Object[] ≈15%、Integer ≈8% —— 全部为 cursor 物化 site。
- **门禁重导（按计划 reconciliation 规则）**：in-scope site（arena 倍增垃圾 ≈4.3MB + 侧表倍增垃圾 ≈0.8MB + render ≈0.4MB）合计 ≈5.5MB / 23MB ≈ **−24% 上界**，原 −50% 门禁不可能由 in-scope site 达成 → Phase 2 分配门禁重导为 **json-100k parse+render −15% vs 同 session 基线**（留 JFR 占比失真余量）；walk 门禁维持 **≤ 10 MB/op**（walk site 全部 in-scope）。
- **设计决策记录**：
  1. **预留 sizing 规则**（计划明令禁止全局常数+大 cap）：first-parse adaptive——`Language.observedNodesPerByte`（volatile，记录已完成 parse 的最大 nodes/byte，仅统计 ≥4KB 输入；取最大值因其单调、抗噪，欠预留由几何增长兜底、过预留由 cap 封顶）；`expectedNodes = min(len × ratio × 1.25 + 64, 2^21)`。首次 parse 未有观察 → 初始容量 4（现行为，一次性学习成本）。最坏浪费公式：`(预留 − 实际节点) × ≈66B/slot`，上界 `min(1.25×observed×len, 2^21) × 66B`。TSParser.parse 与 parseIncremental 均接线并回写 node count。
  2. **GLR 三张侧表**（subtreeSize/DynPrec/ErrorCost）按 `arena.capacity()` 一次性预预留，不再独立倍增。
  3. **thread-local scratch cursor 重入论证**（按计划要求枚举）：`TSNode` 五个结构访问器经 `borrowScratch`（深度计数守卫，重入退化为新 cursor）；`TSNode.cursor()`/`TSTree.cursor()` 永远 fresh（TSQueryCursor.matchChildren 跨 matchSteps 持有 cursor 的重叠路径由此排除）；compat `getChildByFieldName` 用自有 fresh cursor（自带 fieldSearch 中间态，不入池）。ScannerVM 池为 run() 入口全量 reset（11 字段含 resultSymbol=−1 哨兵），scan 期间无重入路径。
  4. 偏差记录：上一会话 Phase 2 代码先行于 Phase 1 测量（违反计划顺序），本日 Phase 1 数据在 stash 后的干净 HEAD 上补测，基线有效性不受影响。
- 验证：`./mvnw test -pl nop-treesitter` **399 green**（修复后两次全量跑均绿）；等价性测试 (a)–(f) 与分配门禁复测见后续条目。
- Doc-sync: perf-tuning.md 待 Phase 3 终判后更新；`No owner-doc update required`（本条目阶段仅测量与内部优化，无契约变化）。
