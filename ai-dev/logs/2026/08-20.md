# 2026-08-20

## nop-ioc 并发初始化死锁修复（Plan 343 执行）

- Plan: `ai-dev/plans/343-nop-ioc-bean-init-deadlock-fix.md` 执行完成
- 设计: `ai-dev/design/nop-ioc/bean-init-concurrency-locking.md`（active）

### 现场取证

- nop-metadata-service 测试 fork JVM 永久挂起，jstack 检测到 Java-level deadlock：main 线程持 `BeanCreationContext`(ctxA) 等 `ProducedBeanInstance`(P)；`nop-job-local-scheduler-15-1` 线程持 P 等 ctxA（锁序反转）。
- 叠加问题：surefire 并行 fork 共享 Lucene 索引目录 `target/search-test-index/nop_meta_metadata`，另一 fork 阻塞于原生 `CreateFile0`（`LuceneSearchEngine.addDocs` 的 `writer.commit()` 等待 write.lock），把死锁放大为永久挂起。
- 根因三点：`runUntil` 持 P monitor 执行 initFunc；`flushInit`/`flushActions` 持 ctxA monitor 执行 action；`BeanDefinition.newObject` 把创建时 ctxA 捕获进 init 闭包，使 ctx 跨线程共享。

### 实施内容（按方案落地）

- `ProducedBeanInstance`：状态机重构为 owner 线程 + wait/notify（`CREATED → PROPERTY_SET → INITIALIZED → LAZY_PROPERTY_SET → DELAY_ACTION_RUN`）。owner 在锁外执行回调，短临界区推进状态并 notifyAll；`PROPERTY_SET` 阶段由创建线程在创建路径内推进（`markPropSet`），异常经 `initError` 记录并唤醒等待者；init 结果在短临界区写回 `bean` 字段（beanMethod 产物场景）。
- `BeanCreationContext`：`flushInit`/`flushActions` 改"锁内快照+drain、锁外执行"，`add*Action` 写入侧同步；init→lazy→delay 顺序保持（快照→执行 分段推进），保证 action 恰好执行一次。
- `BeanContainerImpl.getBean0`：`synchronized(beanDef)` 块收缩为仅 scope 读取/创建/注册，属性赋值与 init 动作登记移到锁外（`BeanDefinition.createInstance` + `setupInstance` 拆分）。

### 验证

- 新增确定性并发回归测试 `TestBeanConcurrentInitDeadlock`（三线程 latch 时序复现死锁场景）。
  - 旧代码（stash 实现改动后运行）：`TimeoutException: timed out after 30 seconds`（2026-08-20 23:48）——证明测试能复现死锁。
  - 新代码：`Tests run: 55, Failures: 0, Errors: 0`（2026-08-20 23:46/23:50）——证明修复有效。
- 既有测试零回归：`./mvnw test -pl nop-core-framework/nop-ioc` 全绿（55 tests，含 TestReentrantCircularRepro、TestBeanContainer、TestBeanDepends、TestLazyProperty）。

### 偏差

- 测试 beans 改用 `lazy-init=true` 而非依赖 `CFG_IOC_APP_BEANS_CONTAINER_START_MODE=ALL_LAZY`（start 期实际创建了 beanD 并阻塞于门闩，证明该配置在测试加载路径下未生效；lazy-init 保证 start() 不预建 P/D，不依赖 start mode 配置）。
- 测试的"T1 已阻塞"同步改为轮询线程状态（BLOCKED/WAITING），因无法在测试 bean 内直接观测 monitor 持有。

### Doc-sync

- `ai-dev/design/nop-ioc/bean-init-concurrency-locking.md` 更新为最终状态
- `ai-dev/design/nop-ioc/README.md` 文档表新增本设计文档
- `docs-for-ai/02-core-guides/ioc-and-config.md` 复核后确认未提及并发初始化锁纪律细节，No owner-doc update required
- `docs-for-ai/INDEX.md` 无需变更（无路由变化）
- `docs-for-ai/04-reference/source-anchors.md` 无需变更（锚点行号未变）

### Follow-up（non-blocking）

- Lucene 索引目录隔离：`TestNopMetaSearchGraphQLSchema` 的 `nop.search.index-dir` 按测试/线程隔离，避免并行 fork 共享目录触发 Windows 原生文件锁挂起。
- 容器启动完成后再启动 job 调度线程（nop-job-local 侧加固，降低触发概率）。

### xlang-execution-optimization：I6 closure——plan completed + roadmap done（truffle 翻译覆盖 A + Q3 残余路径 + 矩阵收口）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-2-i6-truffle-translate-coverage-a.md`（active → completed，三 Phase 已全 completed）。
- **独立 fresh closure audit**（task `ses_fe2cdadf1ffe80jMKndZ543MTp` / marker `ses_audit_i6_truffle_0820_Kx7qN4vZ`，read-only + 定向测试 live 复跑）：三 Phase Exit Criteria 逐项 PASS（a-k：corpus A truffle 列 33/33 复跑绿 + 矩阵 124/124 复跑绿 + 指纹载荷 158/158 复跑绿 + 全模块 384/384 + kind/Q3 实测数字与 surefire XML 逐一核对 + Anti-Hollow 接线追踪无解释器兜底 + fail-fast 断言全 + 文本一致性 + deferred 诚实性 + `git diff` 证实 nop-xlang 零变更）——**verdict CAN CLOSE（0 Blocker / 3 Advisory：Q3 聚合数 47→41（corpus A 口径）、指纹用例数 173→158、新节点数 40→47+1 辅助，三处数字漂移均已在收口时当场修正）**。
- **收口记账**：plan `Plan Status: completed` + Closure 段证据（audit 标识 + 逐项 PASS + 工具门结果 + deferred 检查）；Closure Gates 13 项全勾；`check-plan-checklist.mjs --strict` EXIT=0；`scan-hollow-implementations.mjs`（nop-xlang-truffle）0 findings；roadmap I6 `planned` → `done`（回链 plan + audit 结论 + 两项验收闭合摘要）+ `Last updated` 头刷新。
- Doc-sync: No owner-doc update required（测试域设施 + 模块内部实现；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I6 Phase 3——corpus 覆盖 A truffle 列对拍全绿、kind 覆盖率实测与矩阵推进（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-2-i6-truffle-translate-coverage-a.md`（Phase 3 → completed，三 Phase 全 completed）。
- **corpus 覆盖 A truffle 列 vs 解释器列对拍全绿（roadmap I6 验收第一项）**：`TestCorpusCoverageATruffleColumn` 33/33（静态 20 + 动态 13，三层断言 + cross-compare + 身份断言 = 翻译 AST 经 CallTarget 执行；java 列缺席/不适用按 I1 机制区分；异常单元跨列错误码 + 源位置一致）。
- **truffle 侧覆盖矩阵落地（roadmap I6 验收第二项，与 java 侧同基线同口径 = 同一 `ExecNodeBaseline` 实体）**：`TestTruffleCoverageMatrix` 124 用例——支持集 ↔ `registeredTarget()`（87 类）双向 set 相等 + 87 类逐类最小实例真实翻译（非清单自证，`CoverageNodes` 测试域工厂）+ B 族 35 类 pending fail-fast（`LazyCompiledExecutableFunction`/`GenNodeAttrExecutable` 两类无独立树级形态，pending 证据 = 支持集不含，裁定注记在案）+ `FutureExecutable` 红灯注入红/绿对照。
- **kind 推断全量覆盖率实测（I5 移交第一项闭合）**：`[truffle-frame-stats]` 钩子扩展 reason 五分类逐单元可复跑——corpus v1 4 slots 全部 Int 推断（100%）；corpus 覆盖 A 13 slots 推断 0，原因 NON_LITERAL_WRITE 11 / MIXED_FAMILY 2 / 零写 0 / 字面量族外 0（A 族数据面写入源天然非字面量，推断率为口径性结论；kind 规则不变不虚构）。
- **Q3 残余路径使用统计（I5 移交第二项/设计 Q3 验收闭合）**：`[truffle-q3-stats]` 逐单元——作用域链类别单元 100% scope 链查找路径（slot=0）；slot/绑定类别单元 100% slot 化路径（lookup=0）；corpus 覆盖 A 合计 slot 访问 41 vs scope 查找 21（closure audit 复核一致），两分支各有真实语料覆盖。
- **回归**：`TestCorpusV1TruffleColumn` 22/22 与既有单测全绿（模块 384/0/0）；fail-fast 反例随边界收缩移交 B 族（OutputTextExecutable）+ `GetProperty` 边界不回退守护；不泄漏断言 `TestTruffleDependencyIsolation` 复跑通过。三模块终态：nop-xlang 506/0/2（主代码零变更）、nop-xlang-java 228/0/0、nop-xlang-truffle 384/0/0；`compile`/`clean install -DskipTests`/`-Pqa checkstyle:check` 全部 EXIT 0。
- Doc-sync: No owner-doc update required（测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I6 Phase 2——覆盖 A 翻译落地、Q3 残余路径与帧映射推进（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-2-i6-truffle-translate-coverage-a.md`（Phase 2 → completed）。
- **A 五族 + 并入残余 59 类全部可翻译**：新增翻译节点 47 类（+1 绑定赋值辅助 `XBindingAssign`，nodes 包合计 48 新文件：作用域链 5 = Q3 scope 查找族 / 引用族 5 / 类型操作 4 + 一元合并 / 对象集合 13 / 绑定守卫调试 8 / slot 写 2 / 残余算子 9；同构变体按模式合并），`ResolvedObjFunctionExecutable` 经 `AbstractObjFunctionExecutable` 既有分支覆盖；支持集可编程枚举 87 类。求值顺序逐类保真（短路族分支内求值、复合赋值先读旧值、绑定逐元素按序、map 键先值后）。
- **共享 helper 增量 = 零提取**（"缺哪个提取哪个"裁定：I3 已提取的 33 方法族无缺口），`nop-xlang` 主代码零变更。
- **Q3 残余路径落地**：slot 化优先（引用族/slot 写族走帧 slot）；无法 slot 化的按名访问 → context 持有的 scope 链查找节点（`XScopeReadNode` 等 5 类，经 `XLangContext.requireEvalScope()` 求值窗口存取）；节点 context-independent（双 scope 求值互证测试）。
- **树指纹载荷覆盖**：白名单扩至全部可翻译类（标量载荷 + 子树皆混入）；兜底分支对可翻译类 fail-fast（守卫经 `isNodeClassSupported`），不可翻译类弱哈希（不进缓存）。逐类测试：87 类 payload/subtree 双变体参数化（`TestTreeFingerprintPayloadCoverage` 158 用例 = 86 标量载荷 + 69 子树 + 3 独立）+ 同 sourceKey 载荷差不串用端到端。
- **帧映射推进**：READ/WRITE 声明扩展至 A 族全部 slot 用法来源 + `KindReason` 五分类（INFERRED/MIXED_FAMILY/NON_LITERAL_WRITE/NON_PRIMITIVE_LITERAL/ZERO_WRITE，Phase 3 kind 覆盖率实测统计口径）。
- **单测**：`TestTranslatorCoverageA` 12 用例（Q3 两分支/每族翻译+行为断言/CloneLiteral 深拷贝互证/SourceSection 回映射/slot 越界 fail-fast/支持集自判）；`TestTranslatorFailFast` 边界收缩（GetProperty 反例移交 B 族 OutputTextExecutable + 边界不回退守护）；`TestFrameLayoutMapper` +3（A 族用法声明/kind 原因分类/帧外名不声明帧访问）。
- **验证**：三模块全绿——nop-xlang 507/0/2（主代码零变更基线不动）、nop-xlang-java 228/0/0、nop-xlang-truffle 227/0/0（含 corpus v1 truffle 列 22/22 回归不变）。
- Doc-sync: No owner-doc update required（模块内部实现 + 测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I6 Phase 1——口径对齐、fallback 裁定与矩阵同基线定稿（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-2-i6-truffle-translate-coverage-a.md`（Phase 1 → completed）。
- **前置核验**：I5 `completed`、I3 `completed`（roadmap I3 已 done）——**fallback 未触发**，正常路径直接消费 I3 产物（corpus 覆盖 A 20 静态 + 13 动态单元 + 类别口径）；裁定记录在 plan Phase 1 Execution Notes。
- **同基线共享方式定稿：单一事实源**——truffle 侧不建独立清单，消费 `io.nop.xlang.compare.ExecNodeBaseline`（test-jar，java 侧矩阵同实体）；一致性由结构保证 + `TestExecNodeBaselineFreshness` 新鲜度红灯兜底；排除清单含 `XLangSemantics`，live 138 文件四分区复核无无主残留。
- **矩阵机制 truffle 侧定稿（与 I3 同构）**：支持集可编程枚举 ↔ `registeredTarget()` 双向断言 + 87 类逐类最小实例真实翻译 + B 族 35 类 pending fail-fast + `FutureExecutable` 红灯注入（红/绿对照）；最小实例工厂在 truffle 测试域独立构造（基线口径仍同一事实源）。
- Doc-sync: No owner-doc update required（测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：I3 closure——plan completed + roadmap done（java 转译覆盖 A + 覆盖矩阵机制收口）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-1-i3-java-translate-coverage-a.md`（active → completed，三 Phase 已全 completed）。
- **独立 fresh closure audit**（task `ses_fe312a5ddffeVNdliAXMZZ6EwH`，read-only + 定向测试 live 复跑）：三 Phase Exit Criteria 逐项 PASS（分区计数 138 = live 文件数；矩阵/对拍/基线测试类全部复跑绿：矩阵 124/124、CoverageA java 列 33/33、解释器基线 4/4、v1 java 列回归 22/22、转译级 18/18）；Anti-Hollow 接线追踪完整（`JavaBackendColumn.execute` → `translate` → `JdkJavaCompiler.compile` → 生成实例 invoke，无解释器兜底；矩阵非清单自证——`MinimalNodeFactory` 87 类独立构造真实转译；`FutureExecutable` 为红灯注入合成件非生产 no-op）；`genDebug` 修复验证 = 误引 FQN 全仓清零 + `debug-call` 单元 live 执行证据——**verdict CAN CLOSE（0 Blocker/0 Major/2 Minor 文档漂移：动态单元计数 12→13、watch-only 项补记 Non-Blocking Follow-ups，均已在收口记录时当场修复）**。
- **收口记账**：plan `Plan Status: completed` + Closure 段证据（audit 标识 + 逐项 PASS 锚点 + 工具门结果 + deferred 分类检查）；Closure Gates 12 项全勾；`check-plan-checklist.mjs --strict` EXIT=0；`scan-hollow-implementations.mjs`（nop-xlang / nop-xlang-java）均 0 findings；roadmap I3 `planned` → `done`（回链 plan + audit 结论 + 两项验收闭合摘要）+ `Last updated` 头刷新。
- Doc-sync: No owner-doc update required（测试域设施 + 模块内部实现；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I3 Phase 3——corpus 覆盖 A 扩充、java 列对拍全绿与矩阵落地（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-1-i3-java-translate-coverage-a.md`（Phase 3 → completed）。
- **corpus 覆盖 A**（`io.nop.xlang.compare.CorpusCoverageA` + `xlang-compare/static-a/` 20 xpl + 解释器基线 4 用例）：静态 20（五族每族 ≥1 硬要求 + 异常单元 2：ERR_EXEC_GET_PROP_ON_NULL_OBJ 行 3 / ERR_CONVERT_TO_TYPE_FAIL 行 2）；动态 13（MUTABLE_SCOPE_VAR 3 = ScopeSelfInc/Dec/ScopeAssign 平凡形态唯一出口能力记录；TEMPLATE 1 = ConcatExecutable 唯一出口；STANDARD 9 镜像静态源含动态异常镜像）；schema 四字段齐备。A 范围白名单改判 `ExecNodeBaseline.registeredTarget()` 单一事实源（嵌套同族变体按宿主顶层类归并），消除手工清单漂移。
- **java 列对拍全绿**（`TestCorpusCoverageAJavaColumn` 33/33）：静态单元双列（三层断言 + cross-compare + 身份断言 = 确定性派生生成类实例）；动态单元仅解释器列 + truffle 缺席显式记录；异常单元跨列错误码 + SourceLocation 一致（LOC 常量内嵌生效）。
- **覆盖矩阵落地**（`TestExecTranslationCoverageMatrix` 124/124）：支持集 ↔ `registeredTarget` 双向 set 相等（87 类）；87 类逐类最小实例**真实转译**验证（非清单自证；slot 依赖节点入口帧包装、嵌套变体经顶层类工厂 + ISeqExecutable 接口分派）；B 族 35 类逐类 pending（`isNodeClassSupported=false` + 树节点类转译断言 `ERR_EXEC_TRANSLATE_UNSUPPORTED_NODE`；非树节点 `GenNodeAttrExecutable` 以支持集不含为证）；红灯注入 = 合成 `FutureExecutable` 未注册类 → 支持集不含 + fail-fast 报注入类名 + 基线 `isClassified=false`（红/绿对照可控）。
- **执行中发现并修复的 live 缺陷（in-scope Fix）**：`ExecToJavaTranslator.genDebug` 生成代码误引不存在的 `io.nop.commons.util.ConvertHelper` → 生成源码编译失败；修复 = 与解释器 `DebugExecutable.execute` 同源引用 `io.nop.api.core.convert.ConvertHelper`。发现路径 = `debug-call-static-a` 单元 java 列对拍（对拍机制生效的直接证据）；回归载体 = 该单元在 `TestCorpusCoverageAJavaColumn` 持续执行。简单根因（codegen FQN 笔误）+ 语料回归已覆盖，不另立 bug note。
- **会话中断残留修复**：`CorpusCoverageA` 缺 3 参 `staticUnit` 重载（编译错误）+ scope-assign 族两单元 inputVars/expectedVars 误用（改输入 n=5 + `.scopeVars` 声明执行后态）+ `TestCorpusCoverageAInterpreterBaseline` 规则语义对齐 CorpusV1 口径（每条规则任一成员满足）+ 白名单 6 个属性/集合节点类缺失（后改单一事实源根治）。
- **验证**：`./mvnw test -pl :nop-xlang,:nop-xlang-java -am -T 1C` 全绿（nop-xlang 506/0/2，较 Phase 2 基线 502/0/2 净增 4；nop-xlang-java 228/0/0 含 v1 java 列 22/22 回归不变）；`:nop-xlang-truffle` 37 用例全绿（消费 compare test-jar 无回归）；`./mvnw compile` 三模块 EXIT=0；`-Pqa checkstyle:check` 两模块 EXIT=0。
- Doc-sync: No owner-doc update required（测试域设施 + 转译器笔误修复；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I3 Phase 2——A 五族 + 残余转译落地与共享 helper 增量（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-1-i3-java-translate-coverage-a.md`（Phase 2 → completed）。
- **共享 helper 增量**（`io.nop.xlang.exec.XLangSemantics` +33 方法族：作用域链/引用族/selfAssignValue/类型操作/属性反射全族（PropAccessor 按 propName 全局缓存镜像 per-node 缓存）/构造族 spread/newInstance/绑定守卫/propIn/range/cloneList/cloneMap）；解释器同步改调 ~25 节点类（AbstractExecutable/AbstractPropertyExecutable 成委托壳，MakeProperty 多态保留；30+ 只读访问器增量，I5 先例）。**行为不变回归**：`./mvnw test -pl :nop-xlang -am` 502/0/2 全绿（I2 收口时 498 + 新增基线 4）。
- **转译器注册**：A 44 + 残余 15 类全部落地（求值顺序保真：写族 value 无条件提升单次求值、复合赋值先读旧值；短路族分支内求值；支持集可编程枚举 87 类）。转译缺陷 2 处当场修复：NullCoalesce/ConvertWithDefault 左值/值无条件提升（字面量 ref 不可赋值/不可二次求值）。
- **live 缺陷修复 2 处（in-scope Fix + bug note）**：①`typeof null` NPE（TypeOfExecutable 误引用 `XplInputFormat.value` 枚举常量，提取共享 typeOf 修复为意图语义 "undefined"）；②对象解构 rest 收集 `tail.put(key, value)` 误放整 map（应为 entry.getValue()）。bug note：`ai-dev/bugs/2026-08/2026-08-20-typeof-null-npe.md`、`2026-08-20-object-binding-rest-value.md`。
- **单测**：`TestExecToJavaTranslatorCoverageA` 18 用例（五族+残余源码级断言、不可产生节点合成树、行为级执行、fail-fast 反证 IfExecutable/slot 越界）；fail-fast 反证更新（I2 期用 NullCoalesce 作反例已并入子集 → 换 IfExecutable）。`./mvnw test -pl :nop-xlang-java` 71/71 绿（corpus v1 java 列 22/22 回归不变）。
- Doc-sync: No owner-doc update required（模块内部实现 + 测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I3 Phase 1——覆盖 A 盘点、四分区归属裁定与矩阵口径定稿（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-1-i3-java-translate-coverage-a.md`（Phase 1 → completed）。
- **四分区归属裁定落地**：`nop-xlang` 测试源码新增 `io.nop.xlang.compare.ExecNodeBaseline`（test-jar 发布，I1 先例同包）——live exec/ 138 文件逐一归属 A 族 44 / 并入残余 15 / I2 已落 28 / B 族 35 / 排除 16（逐类理由代码化于 `EXCLUDED_REASONS`），无无主残留；`TestExecNodeBaselineFreshness` 4 用例绿（live 目录扫描 vs 声明分区全等 / 分区互斥 / 排除理由完备 / 红灯注入对照——未知类名判未归属红、已登记判绿）。
- **归属裁定要点**：残余算子 13 类 + `BinaryExecutable` + `ResolvedObjFunctionExecutable` 并入 I3；`VarStatusExecutable`/`DebugIdentifierExecutable` 归 A（数据面/作用域读语义，合成树覆盖）；`LocationFunction`/`ReturnScopeValuesExecutable`/`ExecutableFunctionEvalAction` + 函数邻接 6 类归 B（I4 pending）。扫描粒度裁定 = 文件级（嵌套具体类均为顶层类同族特化变体，instanceof 分派覆盖，javadoc 记录）。共享落点裁定 = 基线/分区/排除清单单一事实源在 nop-xlang 测试源码，矩阵断言测试落 java 侧。
- **产生路径盘点**（scratch 会话 live 诊断后删除）：corpus 可产生 = 作用域链 6（`$scope.x` 族 + 动态可变 scope var 形态的 ScopeSelfInc/Dec）、GlobalVar（`$Math.abs`）、Convert/ConvertWithDefault、InstanceOf、TypeOf、NewObject（`new StringBuilder(16)`）、NewList/NewMap（含 spread）、Get/SetProperty、Get/SetAttr、StaticGetterGetProperty、SelfAssignProperty/Attr、Array/ObjectBindingAssign、Debug、slot 写 3、Neg/BitNot/NullCoalesce/EqNull 族/PropIn/Binary（`7 % 3`）、Concat（动态 `compileTemplateExpr` 出口）；不可产生（→ Phase 2 合成树覆盖）= Reference 族 6 + InitRefSlot/EnhanceRefSlot/BindVar/Cast/GetterGetProperty/SetterSetProperty/MakeProperty/VarStatus/DebugIdentifier/GuardNotEmpty/CloneLiteral/BetweenOp/AssertOp/Range/ResolvedObjFunction，逐类依据记 plan Phase 1 note。无整族不可产生，corpus 硬要求不降级。
- **roadmap 同步**：I3 条目范围行注记归属定稿回链；I4 条目注记 B 族 pending 集移交口径（`ExecNodeBaseline.B_FAMILY` + 矩阵机制消费方式），避免 I4 起草重新推导。
- **执行中发现（watch-only，Phase 2 处置）**：① `TypeOfExecutable.execute` 误引用 `XplInputFormat.value` 枚举常量（应为 `v`）→ `typeof null` NPE；② `ObjectBindingAssignExecutable` rest 路径 `tail.put(entry.getKey(), value)` 误放整 map；③ nop-core `ClassModel.getConstructorForArgs` 多 1 参构造候选误选 0 参构造（`new Date(0)`/`new ArrayList(10)` 报 wrong number of arguments，`new StringBuilder(16)` 正常）——解释器存量行为，corpus 规避，非 I3 缺陷。
- 验证：`./mvnw test -pl :nop-xlang -Dtest=TestExecNodeBaselineFreshness` 4/4 绿。
- Doc-sync: No owner-doc update required（矩阵与裁定属 ai-dev 层产物 + 测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：DRAFT_PLANS——I3/I6 plan 起草 + 两轮独立审查至共识（planned）

- **选题**：roadmap 剩余 todo 中依赖已满足的下一批 = I3（java 转译覆盖 A，dep I2 done）与 I6（truffle 翻译覆盖 A，dep I5 done）；I4/I7 分别依赖 I3/I6 未落地的 baseline（corpus 扩充与矩阵机制），本轮不起草。延迟项对账：W4-audit 移交清单无 I3/I6 项；I2 closure 的 `display()` 急切构造为 non-blocking 优化候选（记入 I3 Non-Blocking Follow-ups）；I5 移交 I6 两项（kind 全量覆盖率实测——出处 I5 Phase 2 Exit Criteria 注记；设计 Q3 残余按名访问路径）已纳入 I6 In Scope。
- **Plan 1（I3，执行顺序 1）**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-1-i3-java-translate-coverage-a.md`。三 Phase：①覆盖 A 盘点 + 四分区归属裁定（live exec/ 具体类无无主残留——起草初盘即具名残余算子 13 类 + BinaryExecutable/ReturnScopeValuesExecutable/LocationFunction/ResolvedObjFunctionExecutable + 函数邻接 7 类；默认并入数据面尾部）+ 矩阵机制定稿（live 包扫描基线、嵌套类粒度、共享口径清单落 nop-xlang test-jar）+ corpus 形态；②A 族转译 + 共享 helper 增量（三类标注法 + 调试对语义裁定）+ 支持集可编程枚举自含判据；③corpus 覆盖 A 扩充 + java 列对拍全绿 + 矩阵推进（红灯注入验证）。
- **Plan 2（I6，执行顺序 2）**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0610-2-i6-truffle-translate-coverage-a.md`。三 Phase：①口径对齐（同基线同口径单一事实源）+ fallback 裁定（触发 = I3 非 completed；增量对账 + 先落地方为事实源，D3 先例）；②A 族翻译 + **树指纹载荷覆盖**（round-1 Major 发现：`TreeFingerprints` 白名单兜底仅混类名+位置，新节点类语义载荷与子树结构必须参与指纹混合或 fail-fast，防同 resourcePath 树变更串用缓存）+ Q3 残余路径（context 持有 scope 链查找节点）；③corpus A truffle 列对拍全绿 + kind 覆盖率实测 + 矩阵推进。
- **审查记录**：两 plan 各两轮独立 fresh session 子agent 对抗性审查（含想象性分析）。I3：round-1（ses_fe3ea32c5ffepBxl4SOq0Psp6J，1 Major+8 Minor）→ 修复 → round-2（ses_fe3e130a8ffesp77XKOFYopYI1，Ready 0 Blocker，2 Minor 当场修复；round-1 的 -Pqa profile 质疑经复核 root pom.xml:472 证伪）。I6：round-1（ses_fe3ea0d2fffeZ6bi7GEqQ3Xog1，1 Major+6 Minor）→ 修复 → round-2（ses_fe3e112a7ffee7ILZKyTfTULX5，Ready 0 Blocker，5 Minor 当场修复）。共识达成 → 两 plan `active`；roadmap I3/I6 `todo` → `planned`（回链 plan + 裁定摘要）。
- **起草期 live 修正**：exec/ 包文件数 137 → **138**（I2 将 `XLangSemantics.java` 落入同包；矩阵排除清单须覆盖；设计/roadmap 的 137 为 W1 时点历史基线）。
- Doc-sync: No owner-doc update required（plan 起草与 roadmap/log 记账；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：I5 closure 补漏——roadmap I5 记账同步（planned → done）

- **背景**：mission-driver closure 复核发现前次 I5 收口遗漏了 roadmap 记账步骤（plan 文件本身已一致：0 未勾选项 + Closure 段证据完整 + `check-plan-checklist.mjs --strict` EXIT=0；log/mission.json 均已就位）。
- **本次动作**：`ai-dev/backlog/xlang-execution-optimization-roadmap.md` I5 条目 `planned` → `done`（回链 plan + closure audit 结论 + 三项验收闭合摘要 + D1/D2/D3 定稿 + guest null 修复），`Last updated` 头同步刷新。
- **工具门 live 复跑（本次 session）**：`./mvnw test -pl :nop-xlang-truffle -am -T 1C` BUILD SUCCESS（truffle 53/0/0 + nop-xlang 回归基线不变）；`checkstyle:check -pl :nop-xlang-truffle -Pqa` EXIT=0；依赖隔离 grep 复核 = nop-kernel 域内 `org.graalvm.truffle/polyglot` 仅本模块 pom；`check-plan-checklist.mjs --strict` EXIT=0。
- Doc-sync: No owner-doc update required（roadmap/log 记账变更；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：I5 closure——plan completed + 三项验收闭合（nop-xlang-truffle 骨架 + 帧/slot 映射 + 子集翻译 + 翻译缓存 + 对拍 truffle 列收口）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-3-i5-truffle-backend-skeleton.md`（active → completed，三 Phase 已全 completed）。
- **独立 fresh closure audit**（mission-driver closure-audit step，read-only 起步、对照 live 代码/测试/工具门逐项核验后落收口）：三 Phase Exit Criteria 逐项 PASS；Anti-Hollow 接线追踪完整（`TruffleBackendColumn → XLangTruffleEval → Context.eval(合成 Source) → XLangLanguage.parse → TranslationCache → RootCallTarget 执行`；`TruffleBackendIdentityRule` 依证据断言翻译根/CallTarget 身份，corpus 测试显式断言非解释器树）；`scan-hollow-implementations.mjs --module nop-xlang-truffle` 0 findings；工具门 live 复跑：compile EXIT=0、`test -pl :nop-xlang-truffle -am -T 1C` BUILD SUCCESS（nop-xlang 498/0/2 基线不变 + truffle 53/0/0 含 corpus 22/22）、`-Pqa checkstyle:check` EXIT=0、依赖隔离 grep 复核仅本模块——**verdict CAN CLOSE（0 Blocker）**。
- **收口记账**：plan `Plan Status: completed` + Closure 段证据（audit 标识 + 逐项 PASS 锚点 + 工具门结果 + deferred 分类检查：SHARED/池/缓存淘汰归 I8、Q1/Q4 归 I12 均为既定责任分工非缺陷降级；执行中 guest null 语义缺陷已在 scope 内修复）；Closure Gates 12 项全勾。注：触发器指定的 `tools/mission-driver/src/plan-check.mjs` 本仓不存在，以 guide 规则 26 的 `check-plan-checklist.mjs --strict` 等价执行（EXIT=0）。
- Doc-sync: No owner-doc update required（测试域设施 + 模块内部实现；docs-for-ai 新模块开发指南同步归 I11）。

### xlang-execution-optimization：EXECUTE I5 Phase 3——表达式子集翻译器 + 翻译缓存 + 对拍 truffle 列激活，corpus v1 全绿（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-3-i5-truffle-backend-skeleton.md`（Phase 3 → completed）。
- **翻译器/缓存落地**（`io.nop.xlang.truffle.translate`）：`ExecToTruffleTranslator`（树翻译非适配包装；子集与 I1 corpus/I2 同口径 + 结构性载体 CallFunc/Seq/Block/SlotAssign/ReturnNull/GuardNotNull/Null；语义敏感操作 generic 路径统一走 `XLangSemantics` 共享 helper（D3）；子集外节点 fail-fast 复用 `nop.err.xlang.exec.translate-unsupported-node` 错误码报节点类名 + SourceLocation，不部分翻译；每编译单元一个 `XLangRootNode` + `RootCallTarget`）；`TranslationCache`（键 = sourceKey + 树指纹的二元组，language 实例作用域，computeIfAbsent 并发安全；缓存淘汰归 I8）；`TreeFingerprints`（递归混合节点类名/源位置/语义载荷，结构相同→同指纹、任一差异→异指纹）；`SyntheticSources`（SourceLocation ↔ 合成 SourceSection 回映射）。
- **truffle 列接入**（`io.nop.xlang.truffle.compare`，test-jar 消费 I1 harness）：`TruffleBackendColumn`（backendId=truffle、静态+动态均适用；sourceKey 口径：静态 = sourceLocationPath（resourcePath 键）、动态 = `dyn:`+源内容哈希（D2）；executedArtifact = 翻译 `XLangRootNode` + executorArtifact = 其 `RootCallTarget`）+ `TruffleBackendIdentityRule`（强身份：翻译根的 sourceTree = 请求树实例、CallTarget 驱动该根——翻译 AST 经 CallTarget 执行，非解释器兜底）+ `TestCorpusV1TruffleColumn`（22 单元参数化全绿：三层断言 + cross-compare + 身份断言；静态单元 java 缺席显式记 skipped/not-registered；动态单元 java "不适用"无 skip 记录——与"缺席"按 I1 列适用性机制区分）。
- **执行中发现并修复 live 缺陷（guest null 语义）**：`method-static-side-effect` 单元（返回 null）truffle 列报 `AssertionError`——根因：`XLangRootNode` 初版把 Java null 原样返回给引擎，`Context.eval` 出口 `PolyglotLanguageContext.asValue` 对 null receiver 有非 null 断言（surefire 默认 `-ea` 激活；本质 = guest 语言顶层不得向引擎返回 Java null，null 须映射为语言级 null 对象）。修复：根节点返回非 null 完成标记 `COMPLETION_MARKER`，真实返回值经求值 handoff 同线程原样交接（既有通路，类型保真），宿主侧 facade 不消费引擎侧返回值。
- **缓存键语义/fail-fast 单测**：`TestTranslationCacheKeySemantics`（同 sourceKey 异指纹→分键不串用/两个 CallTarget；同结构异实例→命中缓存；异 sourceKey 同结构→分键（租户防串用）；D2 动态键 = 内容哈希（异内容异键/同内容同键/端到端分键）；peek 二元组再定位）+ `TestTranslatorFailFast`（GetProperty/If（控制流族，归 I7）fail-fast 报节点类名 + location 且 `.param()` host 可见；slot 越出入口帧帧映射层 fail-fast；嵌套越界无部分翻译逃逸）。
- **帧布局 kind 推断统计（Phase 2 委托 log 记录）**：corpus v1 22 单元实测（`[truffle-frame-stats]` 输出）——4 个含 slot 单元（slot-identifier/combo × 双形态）共 4 slot，全部 `let x = <int 字面量>` 初始化 → 4/4 kind 推断为 Int（100%）；其余 18 单元纯表达式 0 slot；全量覆盖率归 I6 后续实测。
- **验证**：`./mvnw test -pl :nop-xlang-truffle -am -T 1C` BUILD SUCCESS（nop-xlang 498/0/2 回归不变 + nop-xlang-truffle 53/0/0：22 对拍 + 6 帧映射 + 5 接线冒烟 + 4 回映射 + 5 缓存键 + 4 fail-fast + 3 注册 + 3 钉版 + 1 隔离）；mission test 命令（三模块口径）live 复跑 BUILD SUCCESS（230/0/2 + 498/0/2 + 53/0/0）；`-Pqa checkstyle:check -pl :nop-xlang-truffle` **0 violations**；mission lint 命令 live 复跑＝既有形态（`-am` 上游 sun_checks 存量违规 + `|| echo` 降级路径，本模块 `-Pqa` 口径 0 违规）。
- Doc-sync: No owner-doc update required（测试域设施 + 模块内部实现；docs-for-ai 新模块开发指南同步归 I11）。

### xlang-execution-optimization：EXECUTE I5 Phase 2——XLangLanguage/XLangContext + 帧/slot 映射（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-3-i5-truffle-backend-skeleton.md`（Phase 2 → completed）。
- **语言/上下文落地**（`io.nop.xlang.truffle.lang`）：`XLangLanguage`（`@Registration` id=`xl`/name=`XLang`/defaultMimeType=`application/x-xlang`/`contextPolicy=EXCLUSIVE`（D1 编译期常量，I8 切 SHARED 只改注解取值）；无 parser——`parse` 经 `EvalHandoff` 取待翻译树并查语言实例作用域 `TranslationCache`；`initializeMultipleContexts` 继承基类默认（EXCLUSIVE 最小合规形态））+ `XLangContext`（求值窗口协议：根节点入口 `bindEvaluation(scope, output)`/出口 `clearEvaluation`，窗口外访问 fail-fast（`IllegalStateException`）；语言实例只存翻译缓存，节点不存 context 数据）+ `EvalHandoff`（宿主 facade→语言侧同线程交接：嵌套注册/键失配/无 pending 均 fail-fast）+ `XLangTruffleEval`（宿主侧求值 facade：注册 handoff → `Context.eval(合成 Source)` → CallTarget 执行 → 返回值/异常经 handoff 原样回传（`PolyglotException` unwrap，原始 `NopException` 三层断言语料不丢失）；`XLangTruffleException` = `AbstractTruffleException` 包装（引擎认可的语言异常载体，携带定位节点））。
- **帧/slot 映射**（`io.nop.xlang.truffle.frame`）：`FrameLayoutMapper`——程序入口 `CallFuncExecutable.getSlotNames()` slot 布局直译每 RootNode 一份 `FrameDescriptor`（下标一一对应，`SlotMeta` 附着 `FrameSlot` info）；kind 标注规则（不虚构）：slot 全部写入源为同族 primitive 字面量（int/long/double/float/boolean）才标注 primitive kind，混合族/非字面量/零写入/字面量族外（String 等）一律 Object；帧访问模式按实际用法声明（READ=SlotIdentifier/WRITE=SlotAssign 入 `SlotMeta`；MATERIALIZE 表达式子集内无闭包捕获不物化，归 I7）；slot 越出入口帧 fail-fast。
- **单测**（22 项全绿）：`TestXLangLanguageRegistration`（Engine 按 id `xl` 定位/Context 启动/dsl-processor 生成 provider 服务文件 classpath 可见——repo-observable）+ `TestSyntheticSourceMapping`（SourceLocation↔SourceSection path/line/col 往返一致；null 位置不虚构 section；corpus 异常单元期望位置回映射对齐）+ `TestFrameLayoutMapper`（slot 布局一一对应/kind 推断规则六例/mixed 族回落 Object/访问模式按用法/纯表达式空帧/越帧 fail-fast）+ `TestLanguageWiringSmoke`（**接线验证**：parse→缓存查找/构建→CallTarget 获取链路最小冒烟；同键同指纹命中缓存；程序入口帧 slot 写读 round-trip；无 handoff 直接 eval fail-fast；窗口清空验证）。
- **验证**：`./mvnw test -pl :nop-xlang-truffle -am` 全绿；kind 推断比例统计落 Phase 3 执行记录（见上条）。
- Doc-sync: No owner-doc update required（模块内部实现；docs-for-ai 新模块开发指南同步归 I11）。

### xlang-execution-optimization：EXECUTE I5 Phase 1——nop-xlang-truffle 模块落盘 + 钉版冒烟 + 决策 D1/D2/D3 定稿（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-3-i5-truffle-backend-skeleton.md`（Phase 1 → completed）。
- **决策定稿（roadmap/plan 委托项）**：
  - **D1（形态）**：本 plan 以 `contextPolicy = EXCLUSIVE` 过渡形态落地（翻译正确性对拍保守载体，roadmap I5 验收显式允许）；注册结构按"形态 = `@Registration` 编译期常量"组织（`XLangLanguage.CONTEXT_POLICY` 常量入注解），I8 切 SHARED 只改注解取值 + 重编译，不重构；SHARED+池并发验证归 I8。`initializeMultipleContexts` 继承基类默认（EXCLUSIVE 下最小合规形态，SHARED 切换时按需覆写）。
  - **D2（无 resourcePath 动态源缓存形态）**：按**源内容哈希键**入翻译缓存（缓存键 = sourceKey + 树指纹；无 resourcePath 单元的 sourceKey = `"dyn:" + 源串内容哈希`）。理由：与"键 = resourcePath + 树指纹"防串用语义同构（不同内容自然分键，不依赖失效通知）；拒绝"由编译出口持有翻译产物"（需改所有动态编译出口签名，侵入面大，出口统一化归 I9）。
  - **D3（共享 helper 依赖处理）**：I2 已落地 → 直接复用 `io.nop.xlang.exec.XLangSemantics` 共享 helper 基座（truffle 翻译语义敏感操作 generic 路径统一调用，与 java 生成代码同源；不重复提取）。
- **模块落盘**：`nop-kernel/nop-xlang-truffle`（pom：`truffle.version=25.2.4` 三坐标钉同一条 25.x LTS 线——`org.graalvm.truffle:truffle-api` + `org.graalvm.polyglot:polyglot` 为依赖、`truffle-dsl-processor` 经 maven-compiler-plugin `annotationProcessorPaths` 配置；依赖仅 `nop-xlang`，不依赖 `nop-xlang-java`/`nop-javac`）；父 pom modules 注册（`nop-xlang-java` 之后）；mission.json commands 四条按"模块落盘即切换"追加 `:nop-xlang-truffle`（三模块口径）。
- **钉版冒烟复核（配方钉死，25.2.4 实测）**：truffle-api `com.oracle.truffle.api.TruffleLanguage` class major **61**（Java 17）✓；三坐标 jar `META-INF/versions` overlay：truffle-api 仅 `versions/9`+`versions/21` ✓、polyglot 与 dsl-processor 无 overlay 目录 ✓（dsl-processor 不在运行时 classpath，jar 级检查为人工口径记录于此；自动化测试覆盖 truffle-api+polyglot 两坐标 + dsl-processor 以 Phase 2 起的 provider 产物测试间接验证）。自动化落点 `TestTrufflePinnedVersionSmoke`（钉线内更新版本随构建自动重跑同口径检查）。**编译基线**：未覆盖 release（nop-kernel `maven.compiler.release=11` 下编译 major-61 构件 + dsl-processor 生成代码实测可过，无裁定需要记录）。
- **polyglot 冒烟**：空 `Engine.create()` + `Context.newBuilder().engine(engine).build()` 在 stock JDK 21 可运行（`TestTrufflePinnedVersionSmoke#testEmptyEngineAndContextBootOnStockJdk`；语言注册发现归 Phase 2）。
- **不泄漏断言**：`TestTruffleDependencyIsolation`——扫描 nop-kernel 域内全部 pom `<dependencies>` 段（不含 dependencyManagement），断言 `org.graalvm.truffle:*`/`org.graalvm.polyglot:*` 仅命中本模块 pom；豁免口径（nop-js 非内核、org.graalvm.buildtools 构建插件）记录于测试 javadoc。
- **验证**：`./mvnw compile -pl :nop-xlang-truffle -am` EXIT=0（模块进 reactor，dsl-processor annotationProcessorPaths 配置生效）；`./mvnw test -pl :nop-xlang-truffle` 4/4 绿；mission build/typecheck 命令（三模块口径）live 复跑 EXIT=0；lint 命令 live 复跑＝既有形态（`-am` 引入 nop-api-core 的 9173 处 sun_checks 违规为存量现象，`|| echo 'lint not configured'` 降级路径，I5 未引入新违规——本模块 `-Pqa checkstyle` 归 Phase 3 统一验证）。
- Doc-sync: No owner-doc update required（docs-for-ai 新模块开发指南同步归 I11）。

### xlang-execution-optimization：I2 closure——plan completed + roadmap done（nop-xlang-java 骨架 + 子集转译 + 共享 helper 基座 + 对拍 java 列激活收口）

- **独立 fresh closure audit**（task `ses_fe4c3e210ffexDxW6Ji3fpPwTa`，read-only，对照 live 代码/测试/工具门逐项核验）：四 Phase 逐项 PASS；Anti-Hollow 接线追踪完整（corpus → 同树单次编译 → java 列以树实例为翻译源 → JdkJavaCompiler 内存编译 → 生成类实例/入口 Method 证据 → 强身份规则 → 三层断言 + cross-compare）；`scan-hollow-implementations.mjs` 两模块 exit 0；mission test 命令 live 复跑 BUILD SUCCESS（498/0/2 + 53/0/0）——**verdict CAN CLOSE（0 Blocker）**。Advisory 处置：①未提交→本条目提交（`feat(xlang): ...`）；②无 `-am` 单模块测试因本地仓旧 SNAPSHOT 报错＝环境现象（mission build 刷新）；③`display()` 急切构造＝行为等价优化候选（non-blocking，I3+ 可顺带收敛）。
- **收口记账**：plan `Plan Status: completed` + Closure 段证据（审计 task id + 逐项 PASS 锚点 + 工具门结果 + advisory 处置）；roadmap I2 `planned`→`done`（回链 plan + audit 结论 + 执行中裁定摘要：测试域执行通路裁定/FunctionExecutable 系注册表运行时解析/共享分派 handle 按 funcName 分立）；`Last updated` 头刷新。
- **工具门（closure 后复跑）**：`check-plan-checklist.mjs <I2 plan> --strict` **EXIT=0**；`scan-hollow-implementations.mjs --module nop-xlang,:nop-xlang-java`（逐模块）exit 0；`-Pqa checkstyle:check` 两模块 EXIT=0；`check-doc-links.mjs --strict` 0 errors。
- **后继**：I3（java 覆盖 A：数据与作用域族）依赖 I2 已满足可起草；I4 起草时引用本 plan Deferred（`$out` 包装器契约执行验证责任承接）与 Phase 3 契约定稿文本。
- Doc-sync: No owner-doc update required（docs-for-ai 新模块开发指南同步归 I11）。

### xlang-execution-optimization：EXECUTE I2 Phase 4——对拍 java 列激活，corpus v1 静态单元 java vs 解释器全绿（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-2-i2-java-backend-skeleton.md`（Phase 4 → completed）。
- **测试域执行通路裁定（Decision item 落地）**：java 列 = 转译器产出生成源码经 `JdkJavaCompiler`（nop-javac 内存编译 + surefire classpath；先例 nop-core `TestAopCodeGenerator`）编译加载后执行。与设计裁定一致（nop-javac 不承担生产产物编译）；**设计文本缺口**：设计 java §二仅枚举 nop-javac"诊断性编译校验(可选)"用途，未枚举测试域对拍执行通路——本裁定为测试域扩展，生产边界不变（生产通路 = I11 常规构建编译 + I10 classpath 绑定，本 plan 不实现不宣称）；供下次设计文档修订补记（归 W4-audit 移交的设计修订批次）。
- **落地**：`JavaBackendColumn`（`io.nop.xlang.java.compare`，test-jar 消费 I1 harness：backendId=java、静态适用/动态不适用、以请求中同一棵树实例为翻译源、executedArtifact=生成类实例 + executorArtifact=生成类入口 Method）+ `JavaBackendIdentityRule`（强身份规则：执行体必须是本单元确定性派生生成类实例且入口 Method 符合 EvalMethod 约定——替换 I1 `NonInterpreterArtifactIdentityRule` 占位）+ `TestCorpusV1JavaColumn`（22 单元参数化：11 静态双列三层断言+cross-compare+身份断言+truffle 缺席显式记录；11 动态单列 java 不适用无记录）。
- **执行中发现并修复 live 缺陷**：`XLangSemantics` 初版用单一 static `ObjFunctionHandle`，其类级缓存在同 JVM 内跨 funcName 污染（先解析 `charAt` 后 `toUpperCase` 命中 charAt 函数→方法调用失败）——解释器为每节点一 handle（缓存与调用点绑定），生成代码共享表必须按 funcName 分立（`ConcurrentHashMap<funcName, handle>`）。focused 回归 `TestXLangSemantics`（nop-xlang，3 例：跨 funcName 分派不串缓存/Plus 完整语义/guardNotNull 异常携带 loc）。
- **验证**：`./mvnw test -pl :nop-xlang,:nop-xlang-java -am -T 1C` BUILD SUCCESS（nop-xlang 498/0/2 + nop-xlang-java 53/0/0：26 源码断言 + 5 行为级 + 22 对拍）；`-Pqa checkstyle:check -pl :nop-xlang-java` EXIT=0。
- Doc-sync: No owner-doc update required（测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I2 Phase 3——表达式子集转译器 + EvalMethod 约定 + 包装器契约定稿（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-2-i2-java-backend-skeleton.md`（Phase 3 → completed）。
- **转译器**（`nop-xlang-java` `io.nop.xlang.java.translator.ExecToJavaTranslator` + `GeneratedJavaSource`）：纯函数树→Java 源码；程序入口模式（根 CallFunc→生成方法本体，slot 布局→`$v{i}` 局部变量，I1 裁定承接）与纯表达式模式；调用形态引用在多次使用/进条件分支前提升临时变量（单次求值）；And/Or 短路与 ObjFunction 接收者 null 短路以 if 块保真（右侧/实参仅在需要时求值）；fail-fast 报节点类名 + SourceLocation（新错误码 `nop.err.xlang.exec.translate-unsupported-node`），禁止部分生成；SourceLocation 常量按 path:line:col 去重内嵌。生成约定（`io.nop.xlang.java.gen.EvalMethodConvention` + `GeneratedEvalBinding`）：static + 首参 `$scope`（janino 同构）、类名 `Gen_`+resourcePath 派生、`EvalMethodInvoker(MethodInvoker)` 包装。nop-xlang 解释器节点补只读访问器（纯增量）。
- **分派族生成口径**：FunctionExecutable 系 → `XLangSemantics.invokeGlobalFunction`（运行时经同一 `EvalGlobalRegistry` 解析，与编译期 LexicalScopeAnalysis 同源）；StaticFunctionExecutable 系 → `invokeStaticMethodResolved`（与编译期 `getStaticMethodsByName` 同一解析方式）；ObjFunction 系 → `invokeObjMethod`（接收者 null 短路保留）。
- **模板入口包装器契约定稿（W4-audit R1-2 承接；决策记录）**：xpl/xlib 有输出语义单元在声明参数之前追加固定第二隐参 `IEvalOutput $out`（`EvalMethodConvention.OUT_PARAM`）；纯表达式单元不追加。依据：live 输出缓冲由 `EvalRuntime` 携带、不经 `IEvalScope`（设计 java §七）。**执行路径验证归 I4**（corpus 全类别含模板单元样例覆盖 `$out` 通路；本 plan 仅定约，未生成输出通路代码——输出族节点转译属 I4）。
- **验证**：`./mvnw test -pl :nop-xlang-java -am` BUILD SUCCESS——`TestExecToJavaTranslatorSource` 26/26（子集各类节点源码文本断言 + fail-fast 5 例含 CallFunc 非根=局部函数调用边界）、`TestGeneratedCodeBehavior` 5/5（**异常携带内嵌常量行为级兜底**：errorCode + path/line/col 全等；EvalMethodInvoker 包装；assign 副作用落 scope；字符串拼接）；nop-xlang 495/0/2 回归不变；`-Pqa checkstyle:check` EXIT=0。
- Doc-sync: No owner-doc update required（契约属 ai-dev 层；设计文档冻结不改）。

### xlang-execution-optimization：EXECUTE I2 Phase 2——共享语义 helper 基座落地（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-2-i2-java-backend-skeleton.md`（Phase 2 → completed）。
- **盘点清单（逐节点类 × 逐语义分支，三类标注；不以"节点类已引用 helper"整类划为已共享）**：
  - **直译无语义风险**：Literal/Null（常量）；SlotIdentifier/SlotAssign（帧 slot 读写，布局编译期已定）；Block/Seq 族（顺序执行控制流结构）；ReturnNull（弃值返 null）；CallFunc 程序入口（结构性载体）。
  - **已共享（既有实现直接引用）**：Plus 数值路径 MathHelper.add；Minus/Multiply/Divide＝MathHelper；Eq/Ne/StrictEq/StrictNe＝MathHelper.xlangEq（Strict 变体 live 与宽松同实现）；Gt/Ge/Lt/Le＝MathHelper；And/Or＝ConvertHelper.toTruthy+短路；Not＝!toTruthy；CompareOp＝FilterOp.getBiPredicate；ObjFunction 族反射分派＝ObjFunctionHandle。
  - **内联待提取（本 phase 提取收口）**：Plus String 拼接分支（与数值路径统一为 `XLangSemantics.plus`）；ObjFunction 族接收者 null 检查＋调用/异常包装（`invokeObjMethod`/`invokeObjFunction0-3`/`wrapInvokeException`——ERR_EXEC_INVOKE_METHOD_FAIL 的 loc/forWrap/params/bizFatal 传播语义不变）；FunctionExecutable 族 invoke/callN＋addXplStack（`invokeEvalFunction0-3`/`invokeEvalFunction`，stackObj=this 保持 addXplStack 精确对象语义）；StaticFunctionExecutable 解析＋调用＋ERR_EXEC_OBJ_UNKNOWN_METHOD（`invokeStaticMethod`）；GuardNotNull（`guardNotNull`）。
- **落地**：`nop-kernel/nop-xlang/src/main/java/io/nop/xlang/exec/XLangSemantics.java`（共享 helper 基座：算术/比较/真值统一委托 MathHelper/ConvertHelper + 宿主反射分派族解析/调用/异常包装单一实现 + 生成代码用运行时解析入口 `invokeGlobalFunction`/`invokeStaticMethodResolved`）；改调节点：PlusExecutable、AbstractObjFunctionExecutable、FunctionExecutable（5 个 execute 体）、StaticFunctionExecutable、GuardNotNullExecutable；已共享类节点零改动。
- **验证**：`./mvnw test -pl :nop-xlang -am -T 1C` BUILD SUCCESS（495/0/2，与 I1 基线逐位一致＝行为不变证据）；`-Pqa checkstyle:check -pl :nop-xlang` EXIT=0。
- Doc-sync: No owner-doc update required。

### xlang-execution-optimization：EXECUTE I2 Phase 1——nop-xlang-java 模块落盘 + mission.json commands 切换（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-2-i2-java-backend-skeleton.md`（Phase 1 → completed）。
- **模块骨架**：`nop-kernel/nop-xlang-java/` 落盘——pom 依赖方向 `nop-xlang-java → nop-xlang`（compile 仅 nop-xlang；`nop-javac` **test scope**＝测试域编译通路，生产产物编译归常规构建；`nop-xlang` test-jar test scope＝I1 harness/corpus 消费；无 nop-xlang-truffle、无 org.graalvm.*）；包结构 `io.nop.xlang.java.translator`（转译器）+ `io.nop.xlang.java.gen`（生成约定入口），package-info 承载职责说明，无空壳公共方法。父 pom `nop-kernel/pom.xml` modules 注册。
- **commands 切换（"模块落盘即切换"裁定，W3-supplement）**：mission.json 四条命令同一次变更为 `-pl :nop-xlang,:nop-xlang-java` 口径，四条逐条 live 执行通过：typecheck EXIT=0；lint 原命令 fallback 正常（真实门 `-Pqa checkstyle:check -pl :nop-xlang,:nop-xlang-java` EXIT=0）；build BUILD SUCCESS（nop-xlang tests.jar 产出安装）；test BUILD SUCCESS（nop-xlang 495/0/2）。
- Doc-sync: No owner-doc update required（docs-for-ai 新模块开发指南同步归 I11）。

### xlang-execution-optimization：I1 closure——plan completed + roadmap done（三后端对拍框架 + corpus v1 收口）

- **独立 fresh closure audit**（task `ses_fe4ede786ffeuzX4oxTRiiXzli`，read-only，对照 live 代码/测试/工具门逐项核验）：全部 Phase item / Exit Criterion / Closure Gate 逐项 PASS；roadmap I1 验收三项 PASS；Anti-Hollow 接线追踪完整（同树单次编译、每列独立现场、引擎消费真实执行结果与证据、无空方法体/静默捕获）；工具门 live 复跑全绿——**verdict CAN CLOSE（0 Blocker/0 Major/3 Minor）**。Minor 处置：①代码未提交→本条目提交；②roadmap I1 仍 planned→同步 done；③输出层绿路径为空==空（输出节点属 I4）→信息性注记无动作。
- **收口记账**：plan `Plan Status: completed` + Closure 段证据（审计 task id + 逐项 PASS 锚点 + 工具门结果 + Minor 处置）；roadmap I1 `planned`→`done`（回链 plan + closure audit 结论 + 三项执行中裁定摘要）；`Last updated` 头刷新。
- **工具门（closure 后复跑）**：`node ai-dev/tools/check-plan-checklist.mjs <I1 plan> --strict` **EXIT=0**；`scan-hollow-implementations.mjs --module nop-xlang --severity high` exit 0；`-Pqa checkstyle:check -pl :nop-xlang` compare 包 0 违例；`check-doc-links.mjs --strict` exit 0（10 条 warning 均为 I2/I5 plan 引用未落盘模块的既存项）。
- **构建门**：`./mvnw compile -pl :nop-xlang -am` SUCCESS；`./mvnw clean install -DskipTests -pl :nop-xlang -am -T 1C` SUCCESS（tests.jar 产出并安装）；`./mvnw test -pl :nop-xlang -am -T 1C` SUCCESS（nop-xlang 495/0/2 + 依赖模块全绿）。
- **后继**：I2/I5 执行时以 test-jar 消费 harness 与 corpus（消费入口 `io.nop.xlang.compare` 包 + `CorpusV1`），I1 plan Phase 2 Execution note 三项裁定（CallFunc 程序入口包装按程序入口转译/slot 类 let 载体/FunctionExecutable 族属宿主反射分派族）为其对账输入。
- Doc-sync: No owner-doc update required（纯测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I1 Phase 2+3——corpus v1 落地与差异注入自检（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-1-i1-compare-harness-corpus-v1.md`（Phase 2、Phase 3 → completed）。
- **corpus v1**：22 单元（11 静态 `src/test/resources/xlang-compare/static/*.xpl`（`<c:script>` 编译单元）+ 11 动态 `CorpusV1` 内表达式串经 compileSimpleExpr/compileFullExpr 取树，合成 SourceLocation）；6 类 × 双形态全覆盖 + 组合双形态；异常语义单元静态/动态双侧（`'x'.charAt(9)` → NopEvalException `nop.err.xlang.exec.invoke-method-fail`，静态 loc=[xpl 行 2]、动态 loc=[合成 path 行 1]）；算术类含字符串拼接单元；简单方法调用 = 宿主反射分派（实例 ObjFunctionExecutable 族 + 静态 FunctionExecutable 族——`assign('result',1+2)` 兼作副作用层绿路径）。类别覆盖矩阵、schema 四字段、子集白名单 guard（结构性载体显式枚举：CallFunc 程序入口/Block/Seq/SlotAssign/ReturnNull/GuardNotNull）均由 `TestCorpusV1InterpreterBaseline`（24 例）repo-observable 断言。
- **三项执行中裁定**（详记 plan Phase 2 Execution note）：①静态树根 = CallFuncExecutable 程序入口包装（编译前端固有产物），子集纪律按 body 裁定，I2/I5 须按程序入口处理（与其 EvalMethod 约定同构）；②slot 标识符类以 let 声明为最小产生载体（slot 写为结构性载体）；③FunctionExecutable 族裁定为宿主反射分派族成员（注册函数静态分派），I2 转译范围对齐承接。
- **差异注入自检**：`InjectedColumns`（测试假列工厂：返回值/类型篡改、scope 污染、输出污染、异常改写、身份伪装）+ `TestCompareDivergenceInjection`（7 例）——四类分歧各 ≥1 例 FAIL 且断言具体失败维度，同报告解释器列保持 PASS（红/绿对照）；类型分歧 7L vs 7 显式检出；身份伪装（声称 java、实际解释器执行）由 harness 依据证据判 identity-mismatch。列间比对归因裁定：解释器列为参照列，分歧归因非参照列，any-divergence 单元级 FAIL 不弱化。
- **列缺席机制**：`TestColumnAbsenceRecording`（4 例）——静态单元 java/truffle skipped（not-registered）、动态单元仅 truffle skipped（java 不适用无记录）、已注册未声明能力 → capability-not-declared、零执行列单元不判通过。
- **验证**：`./mvnw test -pl :nop-xlang -T 1C` BUILD SUCCESS（495 tests / 0 failures / 2 skipped；compare 框架 39 例全绿：smoke 4 + baseline 24 + injection 7 + absence 4）。
- Doc-sync: No owner-doc update required（纯测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE I1 Phase 1——对拍 harness 落点定稿与骨架落地（active）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-1-i1-compare-harness-corpus-v1.md`（Phase 1 → completed）。
- **落点决策（Decision item 落地）**：harness 核心 + 解释器列 + corpus 同置 `nop-kernel/nop-xlang/src/test/java/io/nop/xlang/compare/`（14 文件，~1100 行），经 pom 追加 `maven-jar-plugin` test-jar 发布（先例 nop-stream-core）；拒绝 nop-core 落点与独立聚合模块（理由记 plan item）。AutoTest 复用裁定维持：矩阵化由 JUnit5 `@ParameterizedTest` 承载，不引入 nop-autotest-junit。
- **关键机制**：列 SPI（`IEvalBackendColumn`：后端标识 + 静态/动态能力 + 执行入口返回执行工件证据）；身份规则注册表（`IBackendIdentityRule`，与列分离——列不得自证身份；无规则 = 显式 FAIL identity-unverifiable；解释器规则 = executedArtifact 同树实例 + executorArtifact == 全局 IExpressionExecutor；java/truffle 临时规则 `NonInterpreterArtifactIdentityRule` 占位，I2/I5 经 `replaceIdentityRule` 强化）；三层断言引擎（返回值 typedEquals；副作用 = 本地变量集封闭化 + `RecordingEvalOutput` 完整 API 调用序列比对，副作用未声明时按"输入不变 + 无输出"断言保证非 vacuous；异常 = 错误码 + SourceLocation path/line + 单侧异常判 FAIL）；≥2 列时叠加列间逐项 cross-compare；每列独立新建等价初始化现场；缺席期望列记 skipped（reason=not-registered / capability-not-declared）；强制路由 API `routeAndExecute(unit, backendId)` 返回含证据的执行结果。
- **验证**：`./mvnw test -pl :nop-xlang -T 1C` BUILD SUCCESS（460 tests / 0 failures / 2 skipped，新增 `TestCompareHarnessSmoke` 4 例：三层断言绿路径、列缺席 skipped、identity-unverifiable 显式 FAIL、强制路由证据返回）；`./mvnw install -pl :nop-xlang -DskipTests` 产出并安装 `nop-xlang-2.0.0-SNAPSHOT-tests.jar`（target + 本地仓双落在）。
- Doc-sync: No owner-doc update required（纯测试域设施；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：DRAFT_PLANS 阶段二首批——I1/I2/I5 三份 plan 起草并两轮独立 review 至共识（active）

- **起草**（roadmap 依赖序取首批 1-3 个 work item：I1（对拍框架+corpus v1，双轨共同前置）→ I2（java 骨架）→ I5（truffle 骨架，与 I2-I4 并行轨道）；未覆盖 I3/I4/I6-I12）：
  - `ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-1-i1-compare-harness-corpus-v1.md`（Work Item I1）
  - `ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-2-i2-java-backend-skeleton.md`（Work Item I2）
  - `ai-dev/plans/xlang-execution-optimization/2026-08-20-0030-3-i5-truffle-backend-skeleton.md`（Work Item I5）
- **Round-1 独立审查**（三份并行 fresh session）：I1=2 Major+7 Minor（身份断言列自证空壳路径/求值现场跨列污染等）；I2=3 Major+3 Minor（Plus"已共享"掩盖 String 拼接分支——live `MathHelper.add` 非数值返 NaN/SourceLocation 常量无行为级验证/"简单方法调用"与 I4 函数闭包族边界不清）；I5=**1 Blocker**+3 Minor（"org.graalvm.* 全仓零出现"为假——live 既有 nop-js polyglot 23.1.2（非内核）与 nop-kernel-cli buildtools 插件，断言按字面第一天就红）。全部修复（plan 文本钉死口径：身份断言=harness 依据执行证据判定列不得自证、每列独立等价初始化求值现场、逐节点类×逐语义分支盘点、子集边界=宿主反射分派族不含 CallFunc 族、不泄漏断言=nop-kernel 域内 truffle/polyglot 依赖仅本模块+显式豁免清单）。
- **Round-2 独立复审**（三份并行新 fresh session）：**三份均 GO（0 Blocker/0 Major），共识达成**，非阻塞 polish 项（异常语义单元≥1 例、Goals 口径残留、I4 对账注记等 8 处一句级文本）已顺手收敛。三份 plan `draft → active`，review 轮次证据（session id）记入各 plan 头部注释。
- **Roadmap 记账**：I1/I2/I5 `todo → planned`（条目回链 plan 路径 + plan 内定稿决策摘要：harness 落点=nop-xlang 测试源码+test-jar / corpus 配比 6 类×双形态 / 无 resourcePath 缓存=源内容哈希键 / EXCLUSIVE 过渡形态落地 SHARED 归 I8 / 包装器契约 R1-2 承接 I2 定约 I4 验证）；`Last updated` 头刷新。W4-audit 移交 #5（R1-2）由 I2 plan 显式承接闭环。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 0 errors。纯计划起草（无代码/设计文档/mission.json 变更）。
- Doc-sync: No owner-doc update required（docs-for-ai 同步归 I11；设计文档冻结不改，机制性措辞偏差记入 I1 plan Non-Blocking Follow-ups 归下次设计文档修订）。

### xlang-execution-optimization：W4-audit closure——plan completed + roadmap done（★ 设计与计划就绪 gate 关闭，阶段二 I1-I12 解冻）

- **独立 fresh closure audit**（task `ses_fe52e917fffeZfS5ITj0F1CccW`，read-only，对照 live 文件/报告/工具门逐项核验）：A1-A4（round-1 报告实体/PASS 轮规格/无跳轮/Phase 2 收缩处置合规）、B1（3 项 Minor 报告+log 双处在位）、C1-C3（`git diff ai-dev/design/`/`missions/` 为空、plans 目录无 I 系列 plan、改动全部在 ai-dev/ 内）、D1-D4（roadmap 回链 live 有效/README 索引/P2 与移交清单逐条归属/当日 log 在位）、E（工具门 pre-closure 记录）、F1-F2（exec/ 137 计数精确、设计文档旧编号抽查吻合）——**全 PASS，verdict CAN CLOSE（0 Blocker/0 Major/0 Minor）**。
- **收口记账**（CAN CLOSE 后执行）：plan `Plan Status: completed` + Closure 段证据（round-1 auditor `ses_fe538d7ceffeT2ljhmyr1y8beI` / closure auditor `ses_fe52e917fffeZfS5ITj0F1CccW`）；roadmap W4-audit `planned`→`done`（条目回链 round-1 报告 + PASS 结论 + 3 项 Minor 处置摘要）；★「设计与计划就绪」里程碑 `todo`→`done`（W1-W4 逐条核对均 done 后同步）；纪律 2 补记解冻声明（"解冻点已达成（2026-08-20 W4-audit done），I 系列可起草 plan"）；`Last updated` 头刷新 2026-08-20；audits README 索引补 round-1 条目（含 verdict）。
- **工具门（closure 后复跑）**：`node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/xlang-execution-optimization/2026-08-19-2350-1-w4-roadmap-workitem-audit-gate.md --strict` **EXIT=0**；`node ai-dev/tools/check-doc-links.mjs --strict` **0 errors**；mission 门 `./mvnw test -pl :nop-xlang -am -T 1C` **BUILD SUCCESS**（456/0/2）。
- **后继**：阶段二解冻，I 系列 plan 按 roadmap 既定顺序起草（首项 I1）；round-1 报告 §七/§八 为后继唯一输入（P2-2 包装器契约责任链显式化归 I2/I4 plan 起草时记账，其余归属见报告）。
- Doc-sync: No owner-doc update required（纯 ai-dev 审计/记账；docs-for-ai 同步归 I11）。

### xlang-execution-optimization：EXECUTE W4-audit——round-1 独立审计即 PASS，gate 关闭（★ 设计与计划就绪，阶段二解冻）

- **Plan**：`ai-dev/plans/xlang-execution-optimization/2026-08-19-2350-1-w4-roadmap-workitem-audit-gate.md`（Phase 1 → completed；Phase 2 → cancelled（round-1 即 PASS，收缩处置：逐项 vacuous 注记勾选，roadmap 未规定最少轮次，单轮 PASS 即可关 gate）；Phase 3 → 收口中）。
- **Round-1 审计**（fresh session `ses_fe538d7ceffeT2ljhmyr1y8beI`，read-only + 唯一报告文件写入；报告落 `ai-dev/audits/xlang-execution-optimization/2026-08-20-roadmap-workitem-audit-round-1.md`）：openAuditPrompt 方法论 + plan 固化口径（五强制维度 + 衔接 (a)-(g) + P3 归入 P2）→ **0 P0 / 0 P1 / 4 P2，verdict PASS**。
  - 五维度全 PASS：粒度（12 条目均达单 plan 量级，I2/I5/I11 上沿有注记）；依赖三处（mermaid/Stages/条目声明）19 工作边 + 2 里程碑边逐边一致、DAG 无环；验收全 repo-observable 且对拍不变式与 I9 显式引用/I12 直接运行两类例外口径落实；复用标注与 13 项 live 锚点一致（exec/ 计数精确 137）；与三组冻结设计逐条目无冲突。
  - W3 移交 3 项 Minor 逐项裁定**非阻塞**：①I6 类别同构引用链完整可解析（I6→I3→java §三分类表）；②I3/I4/I6/I7/I8 复用信息由定稿口径块共享 helper 纪律 + Stages Reuse 列承载，无错误声明/锚点漂移；③编号映射表抽验 10 处覆盖全部 5 个行使分支、语义全对，未行使分支（旧 I2/I5）无引用面，且设计文档零新编号泄漏（冻结完整）——W2 round-5 移交 #17 处置落点经审计落实。
  - 遗留 4 项 P2（R1-1 Stages Critical path bold/plain 无图例 / R1-2 包装器契约验证责任链隐式传至 I4 / R1-3 W3 log 压缩清单 14 源 13 目标转录笔误——不改历史 log，以报告逐项复核为准 / R1-4 设计文档 ~27 处旧 I 编号维持既定"下次设计文档修订"归属）逐条裁定归属，移交后继清单 6 类。
- **FAIL 分支未触发**：无"W3 回补修订轮"（0 P0/P1，roadmap 本 plan 期间仅记账性修改）；设计文档/mission.json/代码零修改（`git diff ai-dev/design/ missions/` 为空实测）。
- **验证**：`./mvnw test -pl :nop-xlang -am -T 1C` **BUILD SUCCESS**（456 tests / 0 failures / 2 skipped，纯文档计划仍按 mission 指令复跑模块门）。
- Doc-sync: No owner-doc update required（本 plan 仅审计/记账；docs-for-ai 同步归 I11）。
