# 2026-09-25

### nop-lint plan 10 收口：独立 closure audit R1 REJECTED→R2 四项整改后满足 completed 条件（plan 10-engine-compile-reuse）

- **closure audit**：独立子 agent agent_cadb8f50（fresh session）——Phase 1/2/3 代码实质全部 PASS（gate 逐字等价推演、全仓编译调用面收敛确认、三模块 782/20/10 独立重跑全绿、Anti-Hollow 调用链追踪）；REJECTED 窄口径 4 项：F1 design 03 增注缺失、F2 基准脚本/smoke 防呆未按字面落地、F3 JFR 证据缺失 + 基准名修正、F4 closure 证据槽引用了 R1 draft review。
- **R2 整改**：design 03 补预编译入口契约五要素（gate 重判/TypeQuerySupport 注入/坏规则时机前移/单编译收敛/legacy 入口保留）；新增 `nop-lint/bench/run-graphql-benchmarks.sh`（域归属再裁定，含 install 警示）+ core smoke 消息刷新（9 基准 ≥9）；补做 `fullLibraryPrecompiled` 30×1s JFR（NodeIterator 22.5% 缓存命中迭代/Lexer 13.2% 解析/编译帧退出热点榜，结论入 perf-baseline）；closure 证据回填；F6 59/62 口径注记、F8 resolveKind 单次调用收敛。
- 验证：doc-links 0 errors；smoke 复绿；core 782/0。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint plan 10 执行：编译复用——预编译入口 + 全消费方迁移 + 单编译收敛（plan 10-engine-compile-reuse）

- **R1 对抗审查**：3 Major（F1 Phase 1 回归清单指错对象→改 TestConstraints/TestL2TypeGate + ConstraintContext 注入形态裁定；F2 engineLintPrecompiled 3 规则口径编译占比仅 ~5-8% 低于噪声带→口径写死 62 规则全库面；F3 LSP 摊平规则表丢弃语言分组→按语言分组迁移语义）+ 8 Minor 全部修订。R1 另核实 CompiledRule 全 final 无状态、跨文件/并发复用安全；byte[] 双重编解码验证到 TSParser.parse(String) 内部 getBytes。
- **Phase 1**：TypeQuerySupport 从编译期捕获改为求值期注入——ConstraintContext 增 typeQueries 分量（2 参便利构造保既有直构点）、TypeOf.holds 从 ctx 取 support（guarded-fail 消息逐字保留）、Constraints.compile 收敛 5 参、CompiledRule 编译产物无 per-file 状态。
- **Phase 2**：LintEngine.lintCompiled 预编译入口（gate 经 gateForTokens 共享逻辑逐文件重判，raw requires 携带使未知 token→SKIP 等价保持）+ byte[] 直通入口；CheckRunner compileOnce 循环外预编译（坏规则启动即抛——时机前移有焦点测试 brokenRuleFailsTheRunAtStartupBeforeAnyFileIsRead：reader 计数 0 证明先于文件循环）；LSP 按语言分组（键小写归一化——修复规则 YAML "Java" vs 客户端 "java" 的大小写 miss）；GraphQL Runtime compiledByLanguage + select 子集化；RuleTestRunner per-suite 编译 + engine 惰性单例。
- **Phase 3**：CompiledNode 单次下降编译（opinion 结构化推导，kindOpinion/flatBranchOpinion 删除，UtilRegistry opinion 编译期一次产出）——同一 pattern 双编译消除；新增 FullLibraryBenchmark（59 条 java 生产规则）：**compilePerCall 0.006s/10.73MB vs precompiled 0.003s/3.82MB——编译复用 -48% 时间/-64% 分配**。
- 验证：core 782/0（+5）、graphql 20/0、maven-plugin 10/0；既有基准（matchAllPatterns 19.4KB、engineLint 1.04MB、parseAndMatch 855KB、engineLintJavaLarge 59.2MB/0.051s、graphqlCheckSource 6.6ms）零回归。
- Doc-sync: `perf-baseline.md` plan 10 增注（全库对照表 + 归属说明）。

### 设计裁定沉淀：复杂度预算 + 性价比门——refactor 总复杂度不超 nop-lint 量级，按 AI 使用价值收敛范围（design/nop-refactor 修订）

- **用户裁定**：复杂度控制在一定范围内、不超过 nop-lint 的复杂度；只做最有价值、性价比最高的功能。
- **vision 修订**（`00-vision.md`）：新增原则 9——复杂度预算（上界锚点 = nop-lint 模块族：主代码约 1.5 万行、模块数 ≤6；超出砍功能不加预算）+ 性价比门（AI 使用价值 × 不可替代性 ÷ 新增复杂度）。§四 Non-Goals 新增"不做结构变换型重构机器"并给出推导表——按 **AI 修改代码的真实形态**重排价值（不照搬人类 IDE 排序）：AI 大多数修改是直接生成/重写代码 + verify 闭环，故 extract/inline/change signature/move 族（JDT 为人类编辑摩擦而生的主力操作）对 AI 边际价值低；批量机械变更价值最高、精确 rename 次之。成功标准加预算线；拒绝项加"全量复刻 JDT 33 种清单"。
- **baseline 修订**（`01-architecture-baseline.md` §七）：复杂度预算线 + 性价比门 + 初始范围裁定——**P0 = codemod 面**（RewriteInput 批量改写，复用 nop-lint pattern/fix 存量，新增量集中契约面）；**P1 = rename**（单模块符号域，阶梯：局部变量→字段→非虚方法→类型）；**out = 结构变换类**（extract/inline/change signature/move 族）。
- **联动同步**：`design/nop-refactor/README.md` 与 `design/README.md` 索引行更新；`2026-09-25d` 增注 R0-R3 收敛为 P0+P1；`2026-09-25e` 增注 M2 降 out、M3 档 3 全线 out、M0 spike 保留为 P1 rename 符号域范围裁定输入。
- **验证**：纯文档任务，无代码变更（No build required）；check-doc-links --strict 退出 0。

### 平台级设计裁定沉淀：自完备设计——不引入外部大型库/系统作为能力源（design 根目录新建跨域约束 + refactor 设计修订）

- **用户裁定**：nop 整体设计是**自完备设计**，不引入外部大型的库（如 jdt 之类）。此为平台级原则，落 `ai-dev/design/README.md` 的 Cross-Cutting Constraints 层。
- **新建** `ai-dev/design/self-contained-design.md`：原则表述 + 判定标准（按"是否成为架构支柱/能力源"判定，非按依赖体积机械划线——禁止语言服务器/IDE 运行时/外部引擎包装；允许适配层内收敛的单一职责工具库（javaparser 经 nop-java-parser 包装）；边界场景窄桥接需进程隔离+常驻池化+fail-visible 且不得升格为能力链引擎（NodeTscBridge 先例））+ 架构支柱测试（移除该外部件能力链是否整体消失）+ 既有一致性证据（nop-treesitter 纯 Java 重写/nop-lint/nop-code/XLang 自研）+ design 硬约束（选型必须回答"为什么不引入外部大型件"）。
- **refactor 设计修订**：`00-vision.md` 加原则 8（自完备能力源自建）+ 拒绝项（jdt.ls 进程桥/OpenRewrite 引擎包装，违反自完备）；`01-architecture-baseline.md` §5 外部引擎段改写（不提供集成面，下游独立选用与本能力无关）+ §6 加拒绝项。
- **调研文档增注同步**：`2026-09-25e` M0 spike"不达标改道 jdt.ls 桥"分叉作废（不达标=收窄自建范围）；`2026-09-25d` OpenRewrite 定位收敛为下游侧独立工具。`design/README.md` 登记跨域约束与 nop-refactor 行更新。
- **验证**：纯文档任务，无代码变更（No build required）；check-doc-links --strict 退出 0。

### 设计裁定沉淀：代码修改能力 AI-First 愿景——GraphQL-first 非 LSP、机器可核验反馈（design/nop-refactor 新建）

- **用户裁定**：nop 的目标是给 AI 自动编写/修改代码提供最方便最快速的工具；**不实现也不需要 LSP**；refactor 能力按 GraphQL 操作接口组织（便于 AI 调用），操作结果必须明确结构化反馈（便于 AI 自检是否满足预期）。此原则约束后续全部 design/plan。
- **落地文档**：新建 `ai-dev/design/nop-refactor/`（AGE owner-doc 模式）——`README.md`（索引）+ `00-vision.md`（Vision 层 active：AI 一等用户原则、GraphQL-first、无状态请求、self-verification 载荷、两段式原子性、批量一等、结构化 fail-closed、快；显式 non-goals：不做 LSP/向导 UI/会话管理/IDE 插件；拒绝 plan-token 会话形态）+ `01-architecture-baseline.md`（目标基线草案：读 nop-code / 查 nop-lint / 改 nop-refactor 三件套闭环；模块拓扑 core+java 适配+graphql 面对齐 nop-lint 模块族；`Refactor__preview/apply` 契约；`RefactorResult.verification{parseOk,errorNodeCount,residualDiagnostics,symbolIntact}` 载荷契约；check→plan→apply→verify 四段；编辑计划不经诊断流表达、仅复用 nop-lint fix 的应用机制）。
- **既有文档增注**：`2026-09-25d`/`2026-09-25e` 两份 analysis 头部加设计裁定增注——涉及 LSP 暴露面/codeAction 的表述以新裁定为准（jdt.ls 桥若启用藏于 GraphQL 契约之后）；`ai-dev/design/README.md` 索引表登记 nop-refactor 行。
- **验证**：纯文档任务，无代码变更（No build required）；check-doc-links --strict 退出 0。

### 深度调研：JDT 支持的 Java 重构全景（本地源码实测枚举）与 Nop 实现路径分档（无关联 plan，analysis 调研任务）

- **触发**：用户问"JDT 支持哪些 Java 重构、如何在 Nop 实现"。直接实读 survey 落位的本地源码 `~/sources/refactor/eclipse.jdt.ui`（含 org.eclipse.jdt.core.manipulation 共享重构核心）+ `~/sources/eclipse.jdt.ls`（LSP 暴露面），逐类枚举，非文档转述。
- **产出**：`ai-dev/analysis/2026-09/2026-09-25e-jdt-java-refactoring-catalog-and-nop-path.md`（Status: open）。
- **JDT 清单实测**：约 33 种——rename 族 14 processor（含虚方法 ripple 的 RippleMethodFinder2）、extract 6（method/temp/constant/class/interface/supertype）、inline 3（InlineMethodRefactoring 的 CallInliner 三件套最复杂）、move/reorg 族 5+、签名/类型改造族（ChangeSignatureProcessor 语义前提最重；typeconstraints 双包类型约束求解器支撑 Generalize/UseSupertype/InferTypeArguments）、introduce/convert 族；jdt.ls handlers 证明 LSP rename/codeAction 协议面足以承载全部。
- **Nop 路径分档**：使能结构对应表（IBinding→JavaParser+SymbolSolver 缺 classpath 装配；SearchEngine→nop-code 缺引用搜索面；LTK Change→nop-lint Fix 管线缺"编辑计划驱动"入口与前置校验器）。33 种按语义前提分三档：档 1 纯语法（nop-lint fix 面）、档 2 单 CU 符号解析约 10 种（R2 第一里程碑：rename 局部→字段→非虚方法→类型阶梯）、档 3 工程级约 15 种（M0 spike 裁定自建 or jdt.ls 进程桥——类比 NodeTscBridge 模式，EPL-2.0 进程隔离）。
- **关键否决**：复刻 LTK 全框架（Undo/脚本/participants）、自建类型约束求解器、按语言切 nop-java-refactor（维持 refactor-core + 语言适配）。
- **验证**：纯调研文档任务，无代码变更（No build required）；check-doc-links --strict 退出 0。

### nop-lint plan 09 完成：children() 按 (tree,id) 缓存——候选 1 落地，真规则库 -84.4%（plan 09-node-cache-traversal）

- **R1 对抗审查**：1 Blocker（静态 WeakHashMap value-holds-key 结构性泄漏→裁定 LintTree 持有 cache 为唯一机制）+ 3 Major（真规则库测量口径缺失/与 plan 08 时序门/cursor 判据量化）+ 5 Minor 全部修订。
- **落地**：`TreeCache`（node 包私有，LintTree 实例字段，三 ConcurrentHashMap）——children/namedChildren 列表与 wrapper 按 arena node id 各物化一次；parent/childByField 走 wrapper 互SANN缓存；equals/hashCode 值语义未动；facade 路径零感知。TestTreeSitterNodeCache 5 例（同列表实例复用/wrapper 身份/跨树隔离/并发协作/反射无 static 结构证明）。
- **量化结果**：matchAllPatterns 分配 **-97.6%**（797→19.4KB/op，时间~百倍）、engineLintJavaLarge **-71%/-72%**（203.9→59.2MB/op，0.180→0.051s/op）、parseAndMatch **-33.8%**（≤1.0MB 门槛达标）、真规则库 graphqlCheckSource **-84.4%**（42.6→6.6ms/op，46.3→9.93MB/op——62× children 乘数收敛直接证据；注意含 plan 08 累计贡献，归属注记在 perf-baseline）。
- **JFR**：cursor 六帧占比 88.4%→61.9%、per-op 归一样本 16.9→3.0（-82%）。
- **closure audit R2**：独立子 agent agent_c7f75fad REJECTED 2 Major——发现 1：候选 2"归 Deferred"静默替换了预注册判据（占比<60%/样本-40% 实测 61.9%/-34.0% 均未达）→ 已在 plan 显式记录 R2 再裁定（候选 2 攻击的 NodeIterator 路径已被候选 1 消除，残余=首物化+parent 路径+解析，替换判据数据在档）；发现 2：plan 09 日志条目缺失→本条补写；发现 3：graphql Δ 归属注记→perf-baseline 已补。
- **警示**：graphql 基准曾因 .m2 旧 nop-lint-core jar 测得虚假持平（42.4ms）——跨模块 JMH 前必须 install 最新依赖模块（perf-baseline 警示段在档）。
- 验证：core 777/0（+5）、graphql 20/0、js 61/0。
- Doc-sync: `perf-baseline.md` plan 09 增注 + `design 03` 节点缓存契约五要素增注。
- 候选 2（单游标下推）显式裁定：Deferred（watch-only）。

### 深度调研：tree-sitter 做 Java/TS 重构的可行性分层分析与 nop-lint 关系定位（无关联 plan，analysis 调研任务）

- **触发**：同日 `2026-09-25-java-refactor-tools-survey.md` 落位 14 仓库后，用户提出三问——如何增加 Java/TS refactor 能力、是否用 tree-sitter 解析、与 nop-lint 什么关系。
- **调研方式**：2 个 Explore 子代理实读 nop-treesitter/nop-lint 全模块（fix 管线逐类核对）+ 2 个网络调研子代理（tree-sitter JVM 绑定与重构边界、OpenRewrite/ast-grep/ESLint/jscodeshift 工程模式），关键结论逐条人工复核。
- **产出**：`ai-dev/analysis/2026-09/2026-09-25d-tree-sitter-refactor-feasibility-and-nop-lint-relationship.md`（Status: open）。
- **核心结论**：①解析分四层——语法发现用自家 nop-treesitter（纯 Java runtime 已生产级，无需外部 JVM 绑定）、改写用既有 byte-range 替换（Fix 模型，保格式）、语义级重构外接既有语义层（Java=nop-java-parser、TS=NodeTscBridge；tree-sitter 硬边界，stack-graphs 已被 GitHub 归档）、大迁移接 OpenRewrite 不自建。②与 nop-lint 三形态：语法级 codemod=fix 面产品化（fix 管线 Fix/TemplateFix/Fixer/FixApplier/UnifiedDiff/multipass 已完整建成，但 62 条生产规则 autoFixable 全 false——"有管线无货"）、语义级重构=兄弟设施（operation 不是 rule，复用底座不进 lint 引擎）、大迁移=外部共存。③落地路线 R0（补 fix 模板，待 plan 11 修 TemplateFix NPE）→R1（transform 产品面，design 先行）→R2（rename 先行）→R3（OpenRewrite 评估），建议排在质量 plan 07-14 之后。
- **验证**：纯调研文档任务，无代码变更（No build required）；check-doc-links --strict 退出 0。

### nop-lint plan 08 收口：独立 closure audit R1 REJECTED→R2 整改→APPROVED，plan completed（plan 08-hotpath-allocation-treatment）

- **closure audit**：独立子 agent agent_b73eec41（fresh session）——五 Phase 实质全部 PASS（归并算法三组数组手工推演等价、FIPS 黄金值独立验算、before/after 数字与 _tmp 原始文件逐一比对、五条调用链追踪、772 测试独立复跑）。R1 REJECTED 窄口径：F1 BaselineEngine 原型未真消除 provider 查找（每次 getInstance().clone()）+ F2 Fixer 孤儿 javadoc + F3 勾选滞后。
- **R2 整改**：BaselineEngine 改静态 SHA256_PROTOTYPE（真消除 provider 查找，3/3 复跑绿）；Fixer 孤儿 javadoc 合并；KindIndex count==0 分支返回空数组（语义与旧空集对齐）；"9 基准"计数笔误修正；勾选翻转 + Closure 证据回填。
- **复核**：check-plan-checklist --strict 退出 0；core 772/0。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint plan 08 执行：热路径分配治理 + 大语料基准基建（plan 08-hotpath-allocation-treatment）

- **R1 对抗审查**：1 Blocker（Phase 4 before/after 时序矛盾→重构为 5 Phase、基准基建先行）+ 3 Major（collectComments 分配前提在 Java 语料不成立——kind 名全小写且 symbolName 返回缓存元素，真实分配面=XML facade/ERROR 节点；canMatchKinds 测试调用点 7 处遗漏；JFR 判据空洞→站点-基准映射+量化门槛）+ 5 Minor 全部修订。
- **Phase 1 基建先行**：bench 新增 5 基准（Java 大语料 203.8MB/op 锚点、XML 大语料、抑制尾站点直测、multiCapture/sha256 micro），smoke 防呆 4→8；before JMH+JFR 全量采集（含 stash 法采集抑制尾真 before）。
- **Phase 2-4 落地**：collectComments regionMatches 滑窗（零分配）；NodeExactEquality children 4→2 + MAX_DEPTH=64 深度防护（design 04 §2 增注）；KindIndex int[] 归并 + canMatchKinds 签名（7 处测试调用点迁移）；multiCaptures 单次快照（三断言契约测试）；sha256 HexFormat + clone 原型（FIPS 黄金值 + 并发测试）；Fixer 规模注记。
- **Phase 5 站点判定**（perf-baseline.md 增注）：suppressionScanXmlLarge **-47.6%**（100,984→52,942 B/op）、sha256Hex **-97.2%**（12,416→344 B/op）、multiCaptureSnapshot **-18.8%**（1,192→968 B/op，40% 门槛如实修正——外层 Map.copyOf 即全部可省项）、KindIndex/NodeExact "before 不可见"（cursor ~90% 主导，改善归 plan 09 复测）；全基准无 >10% 时间回归。
- 验证：core 772 测试全绿（+11）；BenchmarkSmokeTest 一次 JMH 锁冲突复跑后绿（非真实失败）。
- Doc-sync: `perf-baseline.md` 增注 + `design 04 §2` 深度防护增注。

### nop-lint plan 07 收口：独立 closure audit R1 REJECTED→R2 整改→APPROVED，plan completed（plan 07-cli-ecosystem-user-visible-defects）

- **closure audit**：独立子 agent agent_723dcbb3（fresh session）——R1 REJECTED：6 项修复本体全部真实、四条调用链追踪连通、golden 零字节改动（ConsoleReporter git diff 为空）、三模块 790 测试独立重跑 exit 0，但 C6 的 **VFS 分支超限预检无测试**（plan 承诺的交付物）+ 勾选状态不满足硬性前置。
- **R2 整改**：新增 `vfsBranchOversizedResourceIsRejectedBeforeRead`（`AppConfig.getConfigProvider().updateConfigValue` 设 10B 小 cap + 真实 VFS 资源 app-lint.beans.xml，断言 "rejected before read"）；清理 cache 测试死 map（audit Minor 3：单读后同数组引用为结构必然，plan 文本对齐）；全部勾选补齐 + Closure 证据回填。
- **复核**：check-plan-checklist --strict 退出 0（42/42 勾选 + Closure Evidence 在档）；graphql 模块 20 测试（+1）全绿；三模块 791/0。Plan Status → completed。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint plan 07 完成：CLI/生态 6 项用户可见缺陷修复（plan 07-cli-ecosystem-user-visible-defects）

- **深度审计先行**：3 个独立审计 agent（引擎内核/DSL+CLI/语言适配+生态）分片审计 nop-lint 全 6 模块 + 关键 finding 逐条人工读码复核；JMH 基线实测（engineLint 0.002s/op·1.95MB/op、matchAllPatterns 0.001s/op·797KB/op、parseAndMatch 1.29MB/op）。产出 `ai-dev/analysis/2026-09/2026-09-25-nop-lint-quality-optimization-deep-audit.md`（无 Blocker，Major 19/Minor 45，归并 plan 07-14）。Commit 31a0a8e5。
- **plan 07 执行**（R1 对抗审查 agent_0dba73a1：2 Major[Phase1 测试缝可行性、checkFile 第三处扩展名缺陷]+9 Minor 修订后 active）：
  - Phase 1：`--cache` miss 路径单读贯穿——`lintFile` 接收调用方 bytes 快照，哈希与 lint 必然同源（消除"旧哈希+新诊断"错位缺陷）；测试缝 = package-private `SourceReader` 注入重载，`cacheMissReadsEachFileOnceAndReplaysFromTheSameBytes` 计数 reader 断言每 miss 文件恰一次读取。
  - Phase 2：LSP `resolveLanguage`/`MatchCommand` 扩展名 `toLowerCase(Locale.ROOT)` 后查表（`FILE.JAVA` 不再死于 `resolve(null)`）；`NopLintCli` 顶层 catch 对无消息异常输出类名。测试 ×3。
  - Phase 3：CheckMojo 基线开关缺 `baselineFile` 时 `MojoExecutionException` 快速失败（三开关逐一断言，不受 failOnError 支配）；日志桥字节→字符粒度 Writer（fixture 规则 CJK 消息"禁止 System.exit"逐字断言——字节桥下必红）。design 03 §2.1 增注。
  - Phase 4：`Lint__checkFile` cap 前移——磁盘 `Files.size` 预检（"rejected before read"）、VFS `resource.length()`≥0 预检、-1 落回 read 后兜底（不删，防 fail-open）；checkFile 扩展名归一化。design 03 §2.3 增注。测试 ×2。
- 验证：core 761/0（+4）、maven-plugin 10/0（+2）、graphql 19/0（+2），全模块 790 测试绿；golden 输出面未触碰。
- Doc-sync: `ai-dev/design/nop-lint/03-execution-engine.md`（§2.1/§2.3 增注）；perf 面 No owner-doc update required。

### nop-ai-agent 重构 roadmap 拟制完成：unreal-agent 比对补齐 + dsh×unreal 综合 + 两轮独立审查共识（无关联 plan，roadmap 拟制任务）

- **unreal-agent 比对补齐**：既有三方对比只覆盖 dsh/pi；用户要求重构 roadmap 综合 dsh 与 unreal-agent（`~/ai/unreal-agent`，Go async-first harness）。两个并行研究子代理（agent_5366da27 执行/状态面 + agent_a96b067b 工具/上下文/LLM 面）全量实读源码（HEAD `1b9f778453f4`），产出 `ai-dev/analysis/2026-09/2026-09-25-unreal-agent-vs-nop-ai-agent-comparison.md`——裁定 nop 领先 6 / unreal 领先 1（D6 前缀缓存）/ 等价或不可比 3；unreal 独有增量：异步工具占位协议、steering 打断-重建、输入记账不变量 + redelivery 幂等、工具相位检查点 + 保守恢复纪律、心跳即输入、per-provider 缓存键放置、输出预算 schema 内建、Retry-After 双源解析。
- **重构 roadmap**：`ai-dev/backlog/nop-ai-agent-refactor-roadmap.md`（24 WI / M0–M8），与在途 `nop-ai-agent-autonomous-execution-improvement-roadmap.md`（20 项，承接 D1/D4/D6/D8）划界并行，两 roadmap 合计承接或显式裁定处置 99-总报告 ⑤ A–H 全部归组 + unreal 增量。本 roadmap 主线：M1 异步工具执行模型重构（占位协议/心跳/steering 打断/相位检查点）→ M2 输入通道与幂等 → M3 事件与扩展面治理 → M4 session 快照写路径增量化 → M5 工具增量 → M6 LLM 适配层（缓存键放置/观测分型/重试细节）→ M7 流式只评估 → M8 收口。
- **审查循环（用户指定工作流）**：round 1（agent_463e0964，REVISE：4 Major + 6 Minor）→ 全部修复（roadmap 10 处 + unreal 报告 1 处：journal append-only 前提错位、缓存观测 grep 证据修正为"通用通道在位但零配置"、⑤ 承接声明两处口径、nop-kernel/nop-xdefs 保护区域缺失等）→ round 2（agent_213c304c，**CONSENSUS**：修复全 PASS + 无 Blocker/Major）→ 3 项 Minor 账目修正（WI2/WI5 Open Question 收口归属、⑤-E 括注、上游 99 报告 dsh-D2 建议 2 编号笔误顺手修正）。记录回填 roadmap Authoring Review Record。
- **nop 侧 live 核对**（spot check @ `571412f0b2`，写入两文档 Current Baseline）：ChatUsage cache 三字段在位；Anthropic 响应侧解析/请求侧仅 providerHints 透传（:603-604）；AbstractLlmDialect 通用 cache path 通道在位但零配置；ResponsesDialect 无缓存字段；reliability 无 overflow 通道；REASONING_CHUNK 已移除（11 值）；steering 为 round 边界 drain；mailbox/executeParallel Semaphore/spill store 在位。
- **验证**：check-doc-links --strict 0 error；被引 owner doc / 维度报告路径全部存在。纯文档任务，无代码变更（No build required）。
- Commit: 见本日 docs(nop-ai-agent) 提交。
