# 2026-09-24

### nop-lint item 25 autofix 引擎执行完成（Phase 2/3 落地 + owner docs，plan 2026-09-22-2225-1）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-2225-1-autofix-engine.md`（item 25）。Phase 1 已于前日落地（3d376b7800、4e796da950），本日完成 Phase 2/3 全部执行面。
- **Phase 2（应用层）**：新增 `fix/FixApplier`（multipass ≤10：lint → 候选诊断收敛守卫（候选数必须严格下降，只计 fix-bearing 诊断，suppression 元诊断不入计数）→ Fixer.merge → 字节级 splice → 同目录临时文件 + `ATOMIC_MOVE` 原子写 → 重解析守卫（ERROR/missing 节点计数对比，源文件自带 recovery 节点不误报）→ 语法破坏回滚上一轮内容 + 计数；不收敛/超 cap 停止并保留最后成功轮写盘；回滚的 rewrite 不计 applied）；新增 `fix/UnifiedDiff`（手写 unified diff：LCS + 前后缀裁剪 + 4M cell 上限退化为整段替换，3 行上下文 hunk 合并、`\ No newline at end of file` 标记、空侧 `0` 位置约定）；CLI 接线：`CliOptions` 增 `--fix`/`--fix-dry-run`（互斥 fail-closed）、`CheckRunner` fix 流（报告恒描述磁盘最终内容——apply 报残留、dry-run 报原内容 + diff）、`RunSummary`/`ConsoleReporter` 增 fix 计数行（applied/conflicts/nonconvergentFiles/rollbacks，dry-run 标注 no files written）、suggest-only 规则诊断行追加 `(suggestion: <description>)`。
- **Phase 3（e2e + demo）**：`TestCliAutofixEndToEnd`（11 例）覆盖计划承诺三场景：冲突合并（conflict-a/b 同 range，声明序胜出 + conflicts=1）、multipass 收敛（println×2+log×1 → 两轮后全 logger.info，applied=5）、语法破坏回滚（模板产 `throw`，文件还原 + rollbacks=1）；另有写回逐字节断言、dry-run 不写盘 + diff 断言、suggest 不应用、无 fix 规则不动、`NopLintCli.runFull --fix` 全入口退出码 0。nop-lint-nop 新增 fixture-local 套件 `autofix/no-print-demo`（RuleTester 证明诊断面）+ `TestAutofixDemoRule`（公开引擎面证明 match → fix 渲染，RuleTester `.expect` 无 fix 字段契约不变）；`TestNopRuleSuites` 套件计数门禁 12→13。
- **执行中断点恢复**：本次会话从 mission-driver 配额中断处接手，先核实 Phase 1 已落地（xdef/model/parser/CompiledRule/Diagnostic.fix/RuleSetRunner 渲染 + TestFixEngineWiring/TestTemplateFixAndMerge），基线 609 套件全绿后才动 Phase 2。
- 偏差：① `TestConsoleReporter` 触发 javac CME 崩溃——根因是测试缺 `FixApplier` import 时 javac 在报错路径上崩溃（JDK 21 缺陷面），补 import 后消失；② FixStats.applied 语义收窄为"survived on disk"（回滚扣减），计划文本未明定，取诚实口径并写入 javadoc/design 增注；③ diff 格式器两处真实缺陷（hunk 起始行 consumed 用 hunk[0] 应为 hunk[1]；no-newline 标记求值顺序）被测试矩阵捕获即修。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` exit=0（632 tests，0 failures）；`./mvnw -pl nop-lint/nop-lint-nop -am test` exit=0（TestNopRuleSuites 15/15 含新套件、TestAutofixDemoRule 2/2）。
- Doc-sync: `ai-dev/design/nop-lint/03-execution-engine.md` §2.4 autofix 参数面落地增注；`01-pattern-dsl.md` fix 字段落地增注；`08-migration.md` §2 矩阵新增 autofix 行（03 §3 载体/冲突优先级/multipass 增注与 04 §7 已于前日落地）。
- Closure audit：独立子代理 agent_4dcafb7a-24a5-4d14-bfcf-dadbc017100e APPROVED——live 源码逐条核实三 Phase Exit Criteria + Closure Gates，Anti-Hollow 调用链追踪 PASS，审计实跑 core 656/0 + nop 18/0 + hollow scan 0 + doc-links 0；3 Minor（03 §2.3 注记落点漂移、roadmap item 28 状态滞后、日志跨日）均不阻塞，item 28 按其已过 draft review 的 plan 补记 `planned`。roadmap item 25 `planned` → `done`，plan 置 `completed`，Closure 证据入档。
- 收口验证：`node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/nop-lint/2026-09-22-2225-1-autofix-engine.md --strict` EXIT=0。

- Commit: 见本日 feat(nop-lint) 提交 + 收口 docs 提交。
