# 2026-09-24

### nop-lint item 37 收口完成：closure audit APPROVED，roadmap 翻 done（plan 2026-09-24-1500-1）

- **closure audit**：独立子 agent agent_49acc5b9-f126-47ba-a0e7-dcf9bc23e556（fresh session）——5 条 Closure Gates 逐条 PASS：模块/描述符（jar 内 plugin.xml goal=check phase=validate）、8 单测、**审计者独立重跑 e2e 三路径全部复现**（违规→BUILD FAILURE 带 nop/silent-swallow + 计数、干净→SUCCESS、failOnError=false→warn+SUCCESS）、owner doc 三个抽查点一致、checklist/hollow/doc-links 0。1 low（plan 措辞漂移"ProcessBuilder 真跑"→fixture 真跑冒烟，非自动化用例）已随收口校正。
- roadmap item 37 → done。
- Commit: 见本日 docs(nop-lint) 提交。

### nop-lint item 37 Phase 1 完成：nop-lint-maven-plugin 落地（plan 2026-09-24-1500-1）

- **落地**：`nop-lint/nop-lint-maven-plugin`（goal `check` @ VALIDATE，goalPrefix `nop-lint`，maven-plugin-plugin 3.15.2 + plugin-api/core 3.9.16 provided）+ `nop-lint/pom.xml` 聚合注册。`CheckMojo` 零新管线语义：参数面（targets 默认 `${basedir}/src`/profile/failOnError/skip/fix/fixDryRun/baseline 族/rulesPrefix）**拼 CLI 参数语法复用 `CliOptions.parse` 互斥规则**，执行体唯一 = `CheckRunner`（R1 Decision 1 hollow 防线）；失败映射矩阵（error/stale→MojoFailureException、内部错误→MojoExecutionException、writeBaseline 恒成功、failOnError=false 全降级日志）；目标语义（显式缺失=硬错误、默认 src/ 缺失=warn 跳过）；ConsoleReporter 经行缓冲桥接 Maven log。
- **测试**：Decision 4 取第三形态（harness 未赌 Maven 4 兼容性）——Mojo 直接实例化 8 例（CapturingLog + 反射注入）+ **ProcessBuilder 真跑 fixture 工程 `./mvnw validate` 三路径端到端**（违规→BUILD FAILURE 带 `nop/silent-swallow` + 计数；干净→SUCCESS；`-Dnoplint.failOnError=false`→warn+SUCCESS）。执行期发现：@Parameter defaultValue 仅 Maven 注入生效（直实例化需显式置 true，测试注释在案）；无 `<execution>` 的裸 plugin 声明不触发（增注落 design 03 §2.1）。
- **Doc-sync**: design 03 §2.1 v1 落地增注（8 点差异裁定 + 端到端实证）。
- Phase 2（全量回归 + closure audit + roadmap 翻牌）待收口。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 36 收口完成：closure audit APPROVED，roadmap 翻 done，M5 达成（plan 2026-09-24-2300-1）

- **closure audit**：独立子 agent agent_dd37f72f-11e8-4192-9a1e-f0a7b8249b7c（fresh session）——12 条 Closure Gates 逐条 PASS：14/14 规则与套件配对、62 count 门禁双断言、manifest 29 行提升 git diff 实证（16 回溯 + 13 本批；33/33 tier-1 fixture 实存；gate+self-test 0）、Anti-Hollow 抽查 4 条（expect 钉死 + valid 负对照：string-instantiation 的 bytes/charset 转换、random-mod 的 other.nextInt() 接收者）、三模块逐跑 75/729/101 全绿、JMH 零回归增注在档、deferred 分类诚实（数据流通道 = 超出 ESLint 默认语义的 enhancement）。2 Minor（design 06 增注提升行拆分统计 19/10 应为 16/13——已更正；closure bookkeeping——已随收口补齐）。
- roadmap item 36 → done；**Milestone M5（full rule library）翻 done**——items 30–36 全 done，生产库 62 条全带 fixtures，coverage manifest tier-1 = 33 达 auditable state。
- Commit: 见本日 docs(nop-lint) 提交。

### nop-lint item 36 Phase 1–3 完成：PMD/EP P0/P1 批量 14 条 + ESLint 移植 2 + manifest 对账（plan 2026-09-24-2300-1）

- **R1/R2 审查**：agent_5ca2cd40（0 Blocker/3 Major/7 Minor 全修；机制面 10 项 live 核验成立）。
- **落地 14 条生产规则**（48→62，TestProductionRuleCount 双断言同步；TestNopRuleSuites 期望集 52 + 发现数 57；categoryOf 补 4 条 exception/*）：ESLint 移植 2（no-constant-condition、no-self-compare）+ EP 正确性 6（equals-null/self-comparison/self-equals/collection-size-nonnegative/throw-null/random-mod）+ PMD 质量 6（use-collection-isempty/string-instantiation/biginteger-instantiation/no-throw-npe/empty-finally-block/simplify-boolean-expression）。全带 valid+invalid+`.expect` 套件（75/75 真实管线绿）。
- **执行期发现（回写规则头注）**：① if_statement 的 consequence 是必选字段——字面 pattern `if (true)`/`if ($COND)` 靠 error-recovery 永不匹配（item 35 #⑤ if 版），改 kind+condition 字段 xscript 文本判定，且本 blob condition field 带括号包裹（text=`(true)`，剥括号比较）；② any 双分支共享 top-level 约束在未绑定 capture 分支运行期抛 Constraints 异常（requireText 不变式），string-instantiation 改 `$$$ARGS` 序列捕获 + xscript 有序列表面判定（XScriptEngine.buildCaptures：序列捕获=有序 List）；③ finally_clause 无 body field（catch_parameter 同类），改 descendant('block')；④ #1 数据流常量传播通道裁定 = Follow-up（ESLint 默认语义本就不标常量传播变量，字面量版即忠实移植）。
- **manifest 对账**：tier 1: 4→33（回溯 16 行——items 29/35 落地对应行，DoubleBrace PMD+EP 双行；本批 13 行——EqualsNull PMD+EP 双行）。纠错：manifest 源名用全称 `ErrorProne:`，首版脚本用 `EP:` 速记漏翻 10 行已补。mechanism 行对齐 landed 形态（UnusedLocalVariable/SelfAssignment→L3 dataflow 面；metrics 三行去 Phase 3 字样；StringInstantiation 收窄口径；LooseCoupling/TryFailThrowable/ControlStatementBraces 附 approximation 注记）；不提升裁定 EmptyControlStatement/DefaultCharset。gate + self-test 退出码 0；门禁脚本零改动（enum-set/tier-1 fixture/LAYER 词检在提升后依然完备）。
- **验证**：core 729/0 + java 101/0 + nop 75/0；JMH engineLint 0.002±0.001 + parseAndMatch 0.001±0.001 零回归（JFR 未录制，无回归触发）；doc-links 0。注：core+java 同 reactor 并跑时 java 模块出现 38 errors，两模块分别单跑全绿（并发干扰，非代码问题）。
- Doc-sync: design 06 §7.1 v2 对账增注 + design 02 §1 62 口径增注 + perf-baseline.md 增注。
- Commit: 见本日 feat(nop-lint) 提交；closure audit 后 roadmap 翻牌。

### nop-lint item 35 收口完成：closure audit APPROVED，roadmap 翻 done（plan 2026-09-24-1400-1）

- **closure audit**：独立子 agent agent_0dc5a823-4d55-4414-9589-71f1260c0c1a（fresh session）——11 条 Closure Gates 逐条 PASS：枚举 30 条与 rules/ 48 yml 一一对应、count 门禁双断言实跑绿、48/48 规则 suite+expect 全覆盖（脚本核对 + 抽查 9+ 套件钉死行号/messageContains）、deep 7 条经 family 测试类真实管线、Anti-Hollow 抽查（no-md5-digest/system-exit pattern 与 fixture 行精确匹配、TestNopRuleSuites 真实引擎管线非 mock）、三模块 core 729/0 + java 101/0 + nop 61/0 实跑、checklist/hollow 扫描退出码 0、deferred 三项均为 item 28/29 既有路由无降级。3 Minor 全部为 closure bookkeeping，已随收口修复：① design 02 §1 补写 item 35 48 口径增注；② 本收口条目（含校正上一条目中"Phase 3 未动仍 planned"的过时表述——Phase 3 实际已绿：JMH 零回归 + 全量回归 + 增注落档）；③ checklist 勾尽 + Closure 段回填后 --strict 复核。
- roadmap item 35 → done（M5 还差 36）。
- Commit: 见本日 docs(nop-lint) 提交。

### nop-lint item 35 Phase 1+2 完成：规则库 18→48（plan 2026-09-24-1400-1）

- **落地 30 条生产规则**（main 资源 `nop/lint/rules/`）+ 23 个 x:extends 指针套件（suites/）+ 7 个 deep 套件（deep-suites/，独立全量规则副本）+ 3 个 deep family 测试类（TestMetricsRuleSuites/TestL3RuleSuites/TestL4RuleSuites，各装配对应 resolver + deep-suites 总数 11 发现数断言）。反模式 9 条进新 `antipattern/` 类目。
- **#30 冻结**：quality/control-statement-braces（kind if_statement + xscript 判 consequence 非 block；pattern 形态元变量会同时命中 block 体——kind+xscript 收窄）。**#29 替换裁定**：原 no-empty-statement 不可落地——本 grammar blob 无 `empty_statement` 符号（kind 校验 fail-closed 拒载），候选池换 design 06 §4.1 ForLoopCanBeForeach（pattern 带 `{ $$$BODY }`——for 的 body 是必选子节点）。均已回写 plan 枚举表。
- 执行期发现（已回写规则注释）：① grammar kind 名是 `object_creation_expression`；② 关系算子 `inside` 的 inner 只接受 pattern，#14/#15/#17 方法域收窄改走 xscript `node.ancestor`；③ `catch_clause` 无 `parameter` field、`object_creation_expression` 无 `body` field、`modifiers` 是 kind 非 field（改 kind 枚举/descendant）；④ 同 CU 自定义类型经 symbol solver 实际可解析（#17 fixture 按实际行为落 invalid 面）；⑤ query-limit-required 落地形态 = 语句级锚三形态 any-of + `follows` + $Q 同节点再绑定统一 + $M inList + $Q identifier regex（镜像 mjs UNDETERMINED skip；锚在方法调用节点时其父是 declarator 槽，follows 兄弟地平线看不到前置 setLimit 语句）。
- 测试同步（R1 F6）：TestNopRuleSuites.EXPECTED_RULE_IDS 38 + categoryOf 单一派生表（未映射 id 抛错 fail-closed）+ 发现数断言 43；TestProductionRuleCount 48（size+全集双断言）；deep family 3/2/2 + 总数 11。
- 验证：`./mvnw -pl nop-lint/nop-lint-nop test` 61/0；`./mvnw -pl nop-lint/nop-lint-core test -Dtest='TestDeepProfileGate,TestBudgetDegradeLadder'` 19/0。
- 偏差：plan 原口径 no-empty-statement→for-loop-can-be-foreach（grammar 缺符号，替换在案）；query-limit 比冻结机制多了语句级锚的细化（语义不变，spike 证明必要）。
- Phase 3（JMH 复测/全量回归/roadmap 翻 done/closure audit）未动，仍 planned。
- Doc-sync: design 02 §1 数量口径增注留 Phase 3 收口（本条目仅 plan+log）；plan 枚举表 #29/#30 行已回写。
- Commit: 由主会话执行。

### nop-lint item 34 收口完成：closure audit APPROVED，roadmap 翻 done（plan 2026-09-24-1300-1）

- **closure audit**：独立子 agent agent_eb4cdb09（fresh session）——三 Phase Exit Criteria + 全部 Closure Gates 逐条 PASS；手算抽查 3 例吻合（泛型擦除/可覆写矩阵/同名遮蔽独立）；行为迁移审计（L3/L4 探针路生产消费归零，TestDeepProfileGate/TestBudgetDegradeLadder 改写钉死新语义）；工具门禁 checklist/hollow/doc-links 0/0/0；三模块 729/101/30。3 Minor（fast-slice 偶发 flaky pre-existing/探针 javadoc 漂移+死 helper——均已随收口修复）均 non-blocking。
- roadmap item 34 → done（M5 还差 35–36）。
- Commit: 见本日 docs(nop-lint) 提交。

### nop-lint item 34 Phase 2+3 完成：L3/L4 绑定全链路 + DeepResolvers 穿透收敛（plan 2026-09-24-1300-1）

- **Phase 2（接线）**：core `SemanticResolver`/`DataflowResolver` + 两个 Discovery SPI；`JavaSemanticResolver`（solver 装配 + ClassLoaderTypeSolver + JavaNodeIndex 位置定位）/`JavaDataflowResolver`（贪心下探定位方法 + DataflowQueries）；`DeepResolvers` record（四 resolver，无 filePath——R1 C-2）+ 穿透收敛（run 8 参/executeMatch 6 参/LintEngine 构造族收敛）；**L3/L4 门控探针路→resolver 路**（deepAnalyzerLive switch + named-file 要求，R1 C-1 行为迁移：TestDeepProfileGate 3 例 + TestBudgetDegradeLadder 5 例行为级改写）；阶梯闭合记录 L3/L4/SCOPE/METRICS 全量（探针循环退役）；白名单加 VAR_SEMANTIC/VAR_DATAFLOW；SemanticFunctions/DataflowFunctions 绑定。
- demo 套件 semantic-l4（isOverridable，expect 钉行 2-4）+ dataflow-l3（constantValue，expect 钉 const=3——执行期发现：① binding 传 0-based 位置而 DataflowQueries 要 1-based，resolver 补换算；② methodFor 的 findFirst 停在最外层节点，改贪心下探；③ 查询位置锚定声明名/类型关键字容忍 + 引用位置显式拒绝）。
- 测试：TestL3L4Bindings 8 例（L3/L4 各四态 + ceiling + 消息可证伪）。
- **Phase 3**：engineLint 0.002±0.001 零回归（JFR 未录制裁定入 perf-baseline.md）；core 729/0 + java 101/0 + nop 30/0；doc-links 0。
- Doc-sync: design 06 §4.5 L4/L3 落地增注 + design 07 绑定表 semantic/dataflow 两行 + design 11 §2 L3/L4 收口增注 + LintCapability javadoc 探针路→resolver 路收编。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 34 Phase 1 完成：L4 SemanticAnalyzer + L3 查询面内核（plan 2026-09-24-1300-1）

- **落地**：`SemanticAnalyzer`（implementsInterface 用 erased qualified name 比较 + 第二参 FQN/imports/java.lang 归一；isOverridable 纯 AST 修饰符+封闭类 final；isLoggerCall 白名单三 Logger FQN + log 族 + is*Enabled）+ `DataflowQueries`（constantValue/useCount/isSelfAssigned，name-anchored 定位 + 方法屏障 + identity 匹配）+ ConstantPropagation 增 identity 版 `constantValueOf(Record)`（byName 表对同名遮蔽静默塌缩——R1 B-1）。
- 执行期发现：① ReflectionTypeSolver 解不到 slf4j（测试 solver 链增 ClassLoaderTypeSolver(contextClassLoader)）；② 测试位置必须对 fixture 原文计算（method.toString() 重排使行列漂移）；③ 查询位置必须锚定声明名字（initializer 内位置是引用，会答错变量——name-anchored 收紧）。
- 验证：TestSemanticAnalyzer 6/0 + TestDataflowQueries 4/0。
- Doc-sync: No owner-doc update required（§增注随 Phase 2 落）。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 33 收口完成：closure audit APPROVED，roadmap 翻 done（plan 2026-09-24-1130-1）

- **closure audit**：独立子 agent agent_8f71b71d（fresh session）——三 Phase Exit Criteria + 全部 Closure Gates 逐条 PASS；TestScopeAnalyzer 三例手算独立核验吻合（switch 单一作用域/字段顺序无关/lambda 方向特例）；demo e2e 数值可证伪；位置链闭环逐跳核对；工具门禁 checklist/hollow/doc-links 0/0/0；三模块全量 721/91/28 实跑。1 Minor（METRICS/SCOPE 正向阶梯闭合断言缺失——Non-Blocking Follow-up 保留）+ 1 Minor（pre-existing fast-slice 测试偶发 flaky，与本 plan 无因果）+ 2 Nit，均 non-blocking。
- roadmap item 33 → done（M5 还差 34–36）。
- Commit: 见本日 docs(nop-lint) 提交。

### nop-lint item 33 Phase 1–3 完成：ScopeAnalyzer + scope 绑定全链路（plan 2026-09-24-1130-1）

- **Phase 1（内核）**：`ScopeAnalyzer`（nop-lint-java，JavaParser）四查询——definitionOf（类字段全域与顺序无关/局部参数声明点后/switch-block 单一作用域/cross-class=null）、declaredNames、scopeKind、shadows（不预设合法 Java；方向特例钉死）。执行期发现：① JavaParser Range 列为 1-based 字符列、text-block 缩进使手数列号漂移——测试改 pos() 程序化定位；② 外向行走需排除 CompilationUnit（否则类字段被当外层声明重复计数，同作用域重复名误判遮蔽）；③ 直接声明收集需在嵌套作用域边界停止（atScopeRoot 标志曾放行嵌套块泄漏）；④ TryStmt 归为作用域边界（resources 归 try 语句作用域）。
- **Phase 2（接线）**：`ScopeResolver`/`ScopeResolverDiscovery`（core）+ `JavaScopeResolver`（JavaParser 面 LRU 缓存 + SPI）+ `XScriptCompiler.compile` 第三参 boolean→Set<LintCapability>（requires 门控白名单变体推广）+ `ScopeFunctions` 绑定（definition 经 byte 贪心下探回传 CST NodeWrapper）+ 穿透链（LintEngine resolverReady 双态 gate→RuleSetRunner→executeMatch 7 参重载）+ 阶梯 SCOPE 闭合记录（探针循环排除 SCOPE/METRICS）+ RuleTestRunner 5 参 + CheckRunner Discovery + demo 套件 scope-shadow（fixture 首版用 parameter 而规则匹配 variable_declarator——0 诊断的执行期发现，改为局部变量遮蔽字段）。
- 测试：TestScopeAnalyzer 10 例 + TestScopeBinding 6 例 + TestScopeDemoSuite e2e；core 721/0 + java 91/0 + nop 28/0。
- **Phase 3**：engineLint 0.001±0.001 对锚点零回归（JFR 未录制裁定入 perf-baseline.md）；doc-links 0。
- Doc-sync: design 05 §2 v1 落地增注 + design 07 绑定表 scope 行 + design 11 §2 SCOPE 演进增注 + LintCapability.SCOPE javadoc。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 32 收口完成：closure audit APPROVED，roadmap 翻 done（plan 2026-09-24-1000-1）

- **closure audit**：独立子 agent agent_10316e9c（fresh session）——三 Phase Exit Criteria + 全部 Closure Gates 逐条 PASS；TestMetricsEvaluator 11 例中 8 例审计者独立手算复验正确；demo e2e 数值可证伪（score=4 手算吻合）；工具门禁 checklist/hollow/doc-links 0/0/0；XML 路径 Decision 7 fail-closed 核实。2 Minor（METRICS 阶梯闭合记录无专属测试、surefire 陈旧报告）non-blocking，已入 plan Follow-up。
- roadmap item 32 → done（M5 还差 33–36）。
- Commit: 见本日 docs(nop-lint) 提交。

### nop-lint item 32 Phase 2+3 完成：metrics 绑定全链路 + deep 接线 + demo 套件 + 零回归（plan 2026-09-24-1000-1）

- **Phase 2（查询面+接线）**：core `MetricsResolver` 接口（0-based line/UTF-16 col + 具名文件契约）+ `MetricsResolverDiscovery`（ServiceLoader SPI）；`JavaMetricsResolver`（nop-lint-java，JavaTypeResolver 同型自 parse + JavaNodeIndex.minimalContaining + 父链上溯，LRU-32 文件缓存，无 symbol solver）+ META-INF/services 注册；`XScriptCompiler.compile` 白名单变体（requires 含 METRICS 才注册 `metrics` 标识——未声明引用编译期 unresolved 失败）；`MetricsFunctions` 绑定（NodeWrapper 起点 → SourcePositions.lineColUtf16 → resolver）；穿透链 LintEngine 5 参构造（gate: metricsReady = live+具名，else DEGRADE）→ RuleSetRunner.run → executeMatch 6 参重载 → scope 注入；阶梯闭合记录含 METRICS（open = resolver live）；RuleTestRunner 4 参构造 + CheckRunner 默认拾取。
- **demo 套件**：`deep-suites/metrics-complexity`（kind: method_declaration + requires: METRICS + xscript 阈值 report）——valid 零诊断面 + invalid expect 钉死 cyclomatic=4（整方法 range，执行期修正 endLine 2→14）；`TestMetricsDemoSuite` DEEP+provider 真实管线绿（镜像 item 20 l2-suites 形态）。
- **测试**：`TestMetricsBinding` 6 例（live+具名→诊断含 cyclomatic=4 / 无 provider→DEGRADE / unnamed→DEGRADE / fast+standard→ceiling skip / 未声明 requires 引用→编译失败 / 非方法节点→xscript 失败 skip-and-count 不伪造 0）。
- 执行期发现：① 新 5 参 LintEngine 构造与包私有时钟构造重载解析冲突——测试调用点机械适配 6 参（TestBudgetDegradeLadder）；② RuleSetRunner.run 签名再增 (metricsResolver, filePath) 两参——3 个测试文件调用点机械适配；③ RuleTestRunner 构造器去重（python 批量编辑曾产生重复 3 参构造，编译期拦截）。
- **Phase 3（性能守门）**：JMH after 复测 engineLint 0.002±0.001 / parseAndMatch 0.001±0.001——对 item 31 锚点零回归；JFR 未录制（无回归触发，裁定入 perf-baseline.md）。
- 验证：core 715/0 + java 81/0 + nop 27/0 全绿；doc-links --strict exit 0。
- Doc-sync: design 01 §6 MetricsEvaluator 落地增注 + design 07 绑定表 metrics 行 + design 11 §2 CLI provider 拾取演进增注（"CLI 不接探针"裁决的 METRICS 维度演进）。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 32 Phase 1 完成：MetricsEvaluator 三度量内核（plan 2026-09-24-1000-1）

- Plan: `ai-dev/plans/nop-lint/2026-09-24-1000-1-metrics-evaluator.md`（item 32，R1 对抗审查 1 Blocker+4 Major 修订 + R2 增量复核（白名单变体机制自洽无漏洞）+ N1–N5 文本同步后 active）。
- **落地**：`MetricsEvaluator`（nop-lint-java semantic 包，JavaParser AST 方法级，item 30 同型）——cyclomatic = 决策点 +1（每个 &&/|| 节点独立计）；cognitive = SonarSource v1.7 Appendix B 增量表（if/else-if/else 平坦 +1 但抬嵌套、switch 连 case 合计单次 +1、循环/catch/三元 +1+嵌套、lambda 无增量但抬嵌套、逻辑运算符序列 +1/新序列平坦；递归与 labeled jump 入 residual）；NPath = 路径乘积（if/三元/循环 ×2、switch 非 default 标签 n→×(n+1)、catch 每个 ×2、短路算子节点每个 ×2，long 饱和不溢出）。
- 执行期发现：javaparser 3.26.3 类名 `ForEachStmt`（非 ForeachStmt）；三处手算期望修正（每 && 节点独立决策点/嵌套 if 圈数/NPath 双 && 翻倍）；NopException 的 getMessage 含 errorCode 包装改用 contains 断言。
- 验证：`TestMetricsEvaluator` 11/11 全绿（`./mvnw -pl nop-lint/nop-lint-java test -Dtest=TestMetricsEvaluator`）。
- Doc-sync: No owner-doc update required（内核纯新增；design 01 §6 增注随 Phase 2 落）。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 31 收口完成：closure audit APPROVED，roadmap 翻 done（plan 2026-09-24-0900-1）

- **Phase 3（性能守门）**：新增引擎级 JMH 基准 `engineLint`（gate→kind 过滤→匹配→预算边界检查→抑制尾→stats，bench 语料 + 3 旗舰规则）；before 数字在 pre-Phase-2 worktree（@44ed78f9b2）实测，after 复测四基准——engineLint 0.002±0.001 s/op 前后一致、三 pattern 基准不变，零回归；JFR 60×1s 录制热点全在 nop-treesitter 层、预算帧未入热点榜（`_tmp/engine-item31.jfr`）；BenchmarkSmokeTest 门禁更新为 ≥4 基准。
- **closure audit**：独立子 agent agent_3547dea9（fresh session）——三 Phase Exit Criteria + 全部 Closure Gates 逐条 PASS、Anti-Hollow 成立（阶梯/熔断/时间片/探针四机制均有可证伪行为断言）、工具门禁独立重跑（checklist/hollow/doc-links 0/0/0）、deferred 诚实性 PASS；3 Minor（报告面正向路径直接测试补强、before harness 输出未持久化、日志收口条目）均 non-blocking，已记入 plan Follow-up 并兑现 Minor 3。
- 验证：core 709/0 + nop 26/0 + java 70/0；`check-plan-checklist --strict` exit 0；`scan-hollow-implementations --module nop-lint-core --severity high` exit 0；`check-doc-links --strict` exit 0。
- Doc-sync: `nop-lint/docs/perf-baseline.md` 增注（引擎级基准 before/after 表 + JFR 结论 + 预算口径对照）；roadmap item 31 → done（M5 还差 32–36）。
- Commit: 见本日 feat/docs(nop-lint) 提交。

### nop-lint item 31 Phase 2 完成：单文件预算 + 降级阶梯 v2 + 熔断 + fast 时间片（plan 2026-09-24-0900-1）

- **落地**：`FileBudget`（双钟同一预算值：总钟→阶梯、pattern 钟→熔断，重叠时熔断胜出——R2 Minor A；可注入时钟，包私有测试缝；fast 10ms xscript 时间片、<1ms 即耗尽）；`RuleSetRunner` 预算集成（规则边界检查；阶梯有序闭合仅记录真实开启态分析器→`degradedAnalyzers`；闭合后 `CompiledRule.requires` 边界重判走既有 DEGRADE；熔断中止剩余规则→`breakerAbortedRuleIds` 非空即文件 degraded；fast 时间片耗尽→跳过脚本+`xscriptBudgetExceeded` 逐条计数+规则 id 专列+pattern 层诊断（静态 message/匹配节点 range）；fix 门控 fast 不开启/阶梯关闭计数 `fixesDegraded`）；`LintStats` v2 五面 + `RunSummary`（filesDegraded/first-seen union）+ `ConsoleReporter` 渲染（并补上 item 20 遗留的规则级降级渲染缺口）。
- **执行期发现**：① 首版阶梯闭合逐级调用 budget.engageLadder 的幂等卫兵会吞掉后续级——改单次调用带全闭合集；② 测试假时钟读序按"每执行规则 3 次读（边界+匹配起止）"精确编程，DEEP 预算 5s 需 6s 偏移量（首版 600ms 偏移对 500ms STANDARD 有效、对 DEEP 无效——阶梯不触发的假阴性）；③ tightened-deadline 接线证明用真实时钟死循环脚本 + 墙钟 <500ms 断言（收紧 20ms vs 未收紧 1000ms 的行为差）。
- 验证：`TestBudgetDegradeLadder` 9 例全绿（阶梯顺序/边界重判/熔断优先级/fast 时间片/fix 三档/每 lint 调用新预算/健康预算零痕迹）；core 709/0、nop 26/0、java 70/0 全量回归。
- Doc-sync: design 11 §5 落地增注（双钟口径/四级形态/时间片/fix 门控/预算口径五裁定）。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 31 Phase 1 完成：deep 档位 + capability 词表 + 可用性接口（plan 2026-09-24-0900-1）

- Plan: `ai-dev/plans/nop-lint/2026-09-24-0900-1-deep-profile-degrade-ladder-v2.md`（item 31，R1 对抗审查 1 Blocker+7 Major+5 Minor 全修 + R2 增量复核"可直接执行"、3 Minor 已补入后 active）。
- **落地**：`LintProfile.DEEP`（上限 {L1,L2,L3,L4,SCOPE,METRICS} + `fileBudgetMs()` 20/500/5000 design 11 §2 常量 + 阶梯收紧上限常量）；`LintCapability` 增 L3/L4/SCOPE/METRICS（词表裁定：design 06 §4.6 分层名，小写 `dataflow`/`tsc` 不引入，`isDeepAnalyzer()` 判定）；`AnalyzerAvailability` 探针接口（廉价探测、无 provider=fail-closed 降级、capability 名即记录 id）；`LintEngine` 四参构造 + gate 扩展（deep 分析器上限内无探针/探针否 → DEGRADE，L2 路径零改动）；CLI `--profile deep`（usage/错误文案同步）。
- 测试：`TestDeepProfileGate` 10 例（三档门控矩阵/组合 requires 须传 List——`csvSet` 不切逗号字符串的执行期发现/未知 token fail-closed/预算常量/上限超集）+ `TestCliOptions` deep 解析；既有 TestL2TypeGate/TestLintEngine 零改动通过。
- 验证：`./mvnw -pl nop-lint/nop-lint-core test` 700/0 全绿（Phase 1 后全量）。
- Doc-sync: design 11 §2 增注（deep 档 + capability 词表 + AnalyzerAvailability 契约 + CLI deep 不接探针的降级口径）。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 27 Suppression v2 执行完成（ruleset 豁免 + baseline + CI 收紧，plan 2026-09-24-0050-1）

- Plan: `ai-dev/plans/nop-lint/2026-09-24-0050-1-suppression-v2.md`（item 27，三轮对抗审查后 active）。
- **Phase 1（ruleset + 豁免）**：`lint-ruleset.xdef`（`xdef:name="LintRuleset"`，rules xdef:ref lint-rule.xdef + exemptions csv-set 属性）+ `ruleset.yml` 注册进 register-model；`RuleSetModel`（fail-closed：reason 必填/glob 加载期编译/未知豁免规则 id 全量收集后统一拒绝）；`RuleSetLoader` 并存扫描 `*.ruleset.yml` 与 `*.rule.yml`（双源重复 id 拒绝）；CheckRunner 层 `ExemptionFilter` 无状态谓词（normalize+正斜杠 canonical），**过滤先于 severity 累计**（退出码只反映残留），`exemptedDiagnostics` 计数可见；RuleTester 隔离红线（LintEngine/SuppressionFilter/RuleTestRunner 零改动，测试断言钉死）。
- **Phase 2（baseline）**：fingerprint = sha256(ruleId 字节 + 0x0A + range 字节切片)（行漂移免疫、多字节正确）；`BaselineFile` v1（平台 `JsonTool.parseBeanFromYaml/serializeToYaml` + `@DataBean` 映射面，损坏/未知 version fail-closed）；`BaselineEngine`（writeEntries 合并计数 / compute 决策集 + multi-set 消费 + stale 判定）；CLI 三开关互斥 + `--write-baseline`×`--fix` 互斥；**决策集每文件一次性从原始内容 lint 计算，作为无状态谓词作用于全部 fix pass 与 report lint**（消费账本只在原始 lint 推进）；`--baseline-check` stale → 退出码 1（"基线只减不增"执行机构），stale 为 run 级专列（非 Diagnostic、独立清单段渲染）；`--write-baseline` 成功退出 0。
- 测试：`TestRuleSetExemptions`（12：加载/fail-closed 四态/glob 匹配与归一/CheckRunner 过滤/退出码数据流/fix 流不产 fix/引擎面隔离）、`TestBaselineEngine`（11：fingerprint 正反/写合并/消费与 stale/round-trip/fail-closed）、`TestCliBaseline`（10：write→apply→check 生命周期 e2e/退出码断言/flag 解析/互斥/全 CLI 入口）；ConsoleReporter 三处新渲染行（exempted/baseline/stale）由 CheckOutcome 层断言间接覆盖（审计 Minor 指正：无直接 reporter 行断言，后续补强候选）。
- 偏差：① 平台序列化要求 `@DataBean` 注解（计划文本未提及，实测补上）；② `FileBaseline.suppresses` 为键级谓词（同文本族全抑制，apply/check/fix 全模式一致），count 只影响 consumed 计数与 stale 判定——计划决策集裁定的直接推论，closure audit 补记 design 09 §5 精确口径。
- 验证：受影响 6 套件全绿（64 tests）；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 与 `-pl nop-lint/nop-lint-nop -am test` 全量回归 exit=0；`node ai-dev/tools/check-doc-links.mjs --strict` exit=0。
- Doc-sync: design 09 §4/§5 落地裁定增注（fingerprint/路径基准/模式矩阵/§5.3 v1 口径/fix 交互）、design 10 §3 落地增注（LintRuleset/csv-set/DynamicObject 管线）。
- Commit: 见本日 feat(nop-lint) 提交。

### nop-lint item 26 L2 Java symbol solver 执行完成（plan 2026-09-24-0500-1，M4 收官）

- Plan: `ai-dev/plans/nop-lint/2026-09-24-0500-1-l2-java-symbol-solver.md`（item 26，两轮 spike 驱动审查后 active）。Wave 4 全部完成，M4 派生翻 done。
- **落地**：`JavaTypeResolver`（nop-lint-java semantic 包：单文件+ReflectionTypeSolver v1 项目模型；solver parse 前挂载；(filePath,line,col[,expected]) 三元键缓存；异常翻译层 UnsolvedSymbol/IllegalState/UnsupportedOperation→TypeResolutionException；expectedType 简单名经 CU imports 上下文解析、FQN 直比；assignability 走 ResolvedType 祖先链）+ `JavaNodeIndex`（位置键主入口：精确 HashMap+树下降最小包含，named-only）+ `ASTMapping`（§6.3 双向契约，10 条 kind 等价 seed，LintNode 侧查询为 TypedLintNode 前瞻接口——测试钉契约）+ `LineColBytes`（1-based UTF-16 line/col→UTF-8 byte，处理 end-col inclusive 与多字节渐进解码）。
- **降级阶梯 e2e**（TestL2DegradeLadder 4 例）：JDK 接收者真实诊断（resolver→index→symbolsolver 链路运行时消费）/ throwing resolver mid-run 降级（0 诊断+rulesDegraded 计数）/ 无 resolver gate 降级（生产 CLI 现状）/ 非 L2 规则不受影响。
- 执行期修正：JavaParserBuilder 实为 nop-ai-code-analyzer 重依赖（改直接 ParserConfiguration 装配）；setSymbolResolver 非 setSymbolSolver；3.26.3 无 streamForNodes（findAll）与 model.typesystem 包（祖先链替代 ReferenceTypeImpl）；测试位置换算两轮修正（0-based 换算与 multi-byte）。
- 偏差：Phase 1 spike 由审查子代理完成并复核。**closure audit 第 1 轮 REJECTED 实锤 HIGH 缺陷并已修复**：JavaParser 3.26 Node 重写结构等价 equals/hashCode（位置盲），JavaNodeIndex 的 HashMap 键塌缩使重复子树静默继承最后区间（实测 java.lang.Object≠java.lang.String）——ranges 改 IdentityHashMap + 结构等价回归测试（两个全等字面量 rangeOf 必须指向各自行）+ pom description 修正；审计其余 8 条裁定全 PASS，第 2 轮复视后 APPROVED。
- 验证：`./mvnw -pl nop-lint/nop-lint-java test` exit=0（20 新增断言）；`./mvnw -pl nop-lint/nop-lint-core test` exit=0（零改动回归）；`node ai-dev/tools/check-doc-links.mjs --strict` exit=0。
- Doc-sync: design 06 §6.3 ASTMapping 落地增注 + §5.3a Java v1 项目模型裁定（新节）。
- Commit: 见本日 feat/docs(nop-lint) 提交。

### nop-lint item 30 Dataflow analyzer 执行完成（plan 2026-09-24-0600-1，Wave 5 首项）

- Plan: `ai-dev/plans/nop-lint/2026-09-24-0600-1-dataflow-analyzer.md`（item 30，两轮 spike 驱动审查后 active）。
- **Phase 1（内核）**：`DefUseChain`（定义点=VariableDeclarator+Parameter+AssignExpr target+增减量 UnaryExpr；使用点=非定义 NameExpr；SelfAssignment v1=局部/参数 x=x 恒等形态；词法栈作用域归约+类体屏障+lambda 参数屏障）+ `ConstantPropagation`（六字面量形态+编译期字符串拼接+增减量重赋值失效；sealed 三态 Constant/NotConstant/Unknown）+ `DataFlowAnalyzer` 门面（design 06 §4.4 契约名）。
- **执行期发现**：pass 1 建链时漏将 declarator 节点自身加入 definition sites（导致 classify 的 size!=1 恒 false）——pass 1 修正后 18/18 全绿。
- **Phase 2（e2e）**：design 06 §4.4 v1 落地增注（方法内/流不敏感/字段面路由三裁定 + §4.4 全表 10 行映射登记）。
- 偏差：无。
- 验证：`./mvnw -pl nop-lint/nop-lint-java test` exit=0（TestDataFlowAnalyzer 18/0 + 既有 48/0）；`./mvnw -pl nop-lint/nop-lint-core test` exit=0；`node ai-dev/tools/check-doc-links.mjs --strict` exit=0。
- Doc-sync: design 06 §4.4 v1 落地增注。
- Commit: 见本日 feat/docs(nop-lint) 提交。

### nop-lint item 29 PMD/EP coverage manifest v1 + 质量/安全/XNode 规则执行完成（plan 2026-09-24-0330-1）

- Plan: `ai-dev/plans/nop-lint/2026-09-24-0330-1-pmd-ep-coverage-manifest.md`（item 29，三轮 spike 驱动对抗审查后 active）。
- **Phase 1（裁定）**：9 条余量规则裁定**落地 7 / 路由 2**——import-order→maintain-mjs（item 28 manifest 行 #8，序列属性非模式匹配）；xpl-escaping→deferred（round-1 spike：XNode text=trimmed 全等无 contains + XML 路径拒 constraints ⇒ faithful 不可落地；successor=design 06 §3.3 xscript/AST 查询面）。落地 7 条形态全部经真实语法 spike 验证（round-1 审查子代理实测）。
- **Phase 2（规则落地）**：quality 4（no-system-out＝any 接收方+`$M` 方法名捕获；no-return-null＝裸 return null 语句；no-transactional-annotation＝独立 marker-annotation pattern；no-star-import＝普通/static 双分支）+ security 2（no-sensitive-literal＝`$LIT`+kind string_literal+inside 调用关系式 + full-match regex 约束（capture 含引号、sqlhash/arg_ lookahead）+ xscript 通道门控接收方 LOG/log/logger/LOGGER 或 param 方法名；no-hardcoded-crypto＝首参捕获 + regex 钉字面量，限定名构造器双分支）+ XNode 1（nop-orm-unique-key＝xscript 通道：pattern `<unique-key/>` open-world + `NodeWrapper.attrValue` 判 null/blank——round-2 审查 N1 Blocker 裁定：XML pattern/any 面无 attribute 缺失 spec）。套件计数门禁 13→20；`TestProductionRuleCount` 断言生产规则库 11→18。
- **Phase 3（manifest + 门禁）**：`pmd-errorprone-coverage.yml` 186 条全量枚举（design 06 §1=115 PMD + §2=74 EP，3 处跨小节重名去重；tier rubric 五档：1×4/2×165/3×17；tier 2/3 fixture 前瞻语义 N2 差异裁定，mechanism 必须引用 L 层）；门禁 `check-lint-coverage-manifest.mjs`（enum-set 对账 design 表/tier-1 fixture 存在性/mechanism L 层/excluded reason/词表+汇总 + self-test 正控）。**破坏试验 Proof**（执行 + 审计独立 3 变体）：删 tier-1 行→enum-set fail；fixture 字段改名→missing-fixture fail；tier=9→词表 fail；恢复均 exit 0。
- 执行期被套件即测即修的 5 处真实缺陷：any 分支 `nested:` 包装错误（正确形态=直接 matcher 对象）；expect 行号与 endLine 未同步；static import AST 形态缺分支；限定名 `javax.crypto.spec.SecretKeySpec` 构造器缺分支；敏感字面量接收方 `log` 小写不匹配 `LOG` 字面（改 xscript 门控）。
- 偏差：执行日志条目曾遗漏（审计 F1 收口必办），随本收口提交补写；roadmap "Last updated" 时间戳滞后同步刷新（F2）。
- 验证：`./mvnw -pl nop-lint/nop-lint-nop -am test` exit=0（22+1+2+1）；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` exit=0（689/0）；`node ai-dev/tools/check-lint-coverage-manifest.mjs` exit=0（186 vs 189；1×4/2×165/3×17）+ self-test exit=0；`node ai-dev/tools/check-doc-links.mjs --strict` exit=0。
- Doc-sync: design 02 §1 批次落地增注、design 06 §7 manifest v1 落地增注（manifest 路径修正为含模块段）、design 12 行 #20/#21→migrated-pending-switchover、#24→deferred（悬空引用修正）。
- Commit: 见本日 feat/docs(nop-lint) 提交。

### nop-lint item 28 check-*.mjs 迁移 manifest 执行完成（plan 2026-09-22-0854-3）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0854-3-check-scripts-migration-manifest.md`（item 28）。零生产代码变更（design/tools/roadmap 文档面 + 1 个门禁脚本）。
- **Phase 1（盘点与裁定）**：两批独立 Explore 子代理完整读 24 个 check-*.mjs 源码（~14k 行），逐脚本产出子规则数/类别/技术形态/接线点；接线点独立 grep 取证（CI 三处：maven.yml stream-invariants 直连、compliance.yml bean-naming、maven.yml→run-nop-metadata-invariants.sh 间接 4 脚本；pnpm scripts 6 个；无接线 7 个）。**errorcode-param-consistency 裁定：维持 mjs zero-hit hard gate，不立项引擎级 analyzer**——(a) faithful 语义 = 子系统级跨文件 ErrorCode 注册表分析（1174 行），(b) nop-metadata 专名硬编码全仓唯一消费者，(c) 迁移零表达力收益；重估触发 = Wave 5 语义分析器落地且出现第二个 *Errors.java 注册表模块。排除 7（文档 3/流程 2/审计 manifest 1/git 历史 1，逐条带理由）、维持 mjs 7（含 silent-wrong-result 5 子规则逐条映射暂维持 + baseline ratchet 无引擎对应）、已迁移待切换 3（ibiz-interfaces/vfs-violations/silent-swallow）、candidate 5（bean-naming/orm-icons/orm-unique-key/sensitive-literal-leak/xpl-escaping，依赖 item 均 done）、deferred 2（i18n-en-xml、nop-code-invariants 的 query-limit→item 35）。
- **Phase 2（manifest + 门禁）**：`ai-dev/design/nop-lint/12-check-scripts-migration-manifest.md`（24 行逐脚本账本：子规则数/类别/能力映射/依赖 item/switchover 门禁/decommission/状态 + 通用前置四条 + 下线计划节 + 消费关系登记 + errorcode 裁定节）；`ai-dev/tools/check-lint-migration-manifest.mjs`（枚举集==live 集/必填字段/状态词表/分类汇总四重校验 + self-test 正控 6 类坏 fixture 必须被拒）。**破坏试验 Proof**：移除 #19 orm-icons 行 → 3 项违规 exit 1 → 恢复 exit 0。门禁开发期即抓到两处真实缺陷（gate 自身误入 live 脚本集、汇总行 markdown 折行导致解析不全）——被自家校验捕获即修。
- **指针回写**：design 02 §3 迁移行改指 12 号 manifest 为权威 + errorcode 裁定结论；design 00 索引登记 12 号文档；roadmap item 28 注记补 manifest 路径 + 裁定结论 + 分类汇总数字。docs-for-ai 核对无涉及（manifest 属 ai-dev 开发域）。
- 偏差：无（Non-Goals 未触碰——未迁移/下线任何脚本，ast-grep 新增规则未发现）。
- 验证：`node ai-dev/tools/check-lint-migration-manifest.mjs` exit=0；self-test exit=0；破坏试验 fail→restore 记录在案；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` exit=0（回归基线）；`node ai-dev/tools/check-doc-links.mjs --strict` exit=0。
- Doc-sync: design 00 索引/02 §3/12 号新建 + roadmap item 28 注记（见上）。
- Closure audit：独立子代理 agent_7bf60dea APPROVED——manifest 账本 24 行与 live 逐名一致、6 脚本源码抽查全对（silent-wrong-result 恰 5 子规则等）、门禁四路径真实 + 审计独立破坏试验两变体（删行/改非法状态均 exit 1→恢复 exit 0、git 全净）、errorcode 裁定三处一致且履行 2137-3 successor 义务、Anti-Slacking 复核（7 exclude 无伪装/7 maintain-mjs 无敷衍）；唯一 Info 发现（裸 ls 计 25 含门禁自身，自排除有留痕）。roadmap item 28 `planned` → `done`，plan 置 `completed`，Closure 证据入档；check-plan-checklist --strict EXIT=0。

- Commit: 见本日 docs(nop-lint) 提交 + 收口 docs 提交。

### nop-lint item 25 autofix 引擎执行完成（Phase 2/3 落地 + owner docs，plan 2026-09-22-2225-1）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-2225-1-autofix-engine.md`（item 25）。Phase 1 已于前日落地（3d376b7800、4e796da950），本日完成 Phase 2/3 全部执行面。
- **Phase 2（应用层）**：新增 `fix/FixApplier`（multipass ≤10：lint → 候选诊断收敛守卫（候选数必须严格下降，只计 fix-bearing 诊断，suppression 元诊断不入计数）→ Fixer.merge → 字节级 splice → 同目录临时文件 + `ATOMIC_MOVE` 原子写 → 重解析守卫（ERROR/missing 节点计数对比，源文件自带 recovery 节点不误报）→ 语法破坏回滚上一轮内容 + 计数；不收敛/超 cap 停止并保留最后成功轮写盘；回滚的 rewrite 不计 applied）；新增 `fix/UnifiedDiff`（手写 unified diff：LCS + 前后缀裁剪 + 4M cell 上限退化为整段替换，3 行上下文 hunk 合并、`\ No newline at end of file` 标记、空侧 `0` 位置约定）；CLI 接线：`CliOptions` 增 `--fix`/`--fix-dry-run`（互斥 fail-closed）、`CheckRunner` fix 流（报告恒描述磁盘最终内容——apply 报残留、dry-run 报原内容 + diff）、`RunSummary`/`ConsoleReporter` 增 fix 计数行（applied/conflicts/nonconvergentFiles/rollbacks，dry-run 标注 no files written）、suggest-only 规则诊断行追加 `(suggestion: <description>)`。
- **Phase 3（e2e + demo）**：`TestCliAutofixEndToEnd`（11 例）覆盖计划承诺三场景：冲突合并（conflict-a/b 同 range，声明序胜出 + conflicts=1）、multipass 收敛（println×2+log×1 → 两轮后全 logger.info，applied=5）、语法破坏回滚（模板产 `throw`，文件还原 + rollbacks=1）；另有写回逐字节断言、dry-run 不写盘 + diff 断言、suggest 不应用、无 fix 规则不动、`NopLintCli.runFull --fix` 全入口退出码 0。nop-lint-nop 新增 fixture-local 套件 `autofix/no-print-demo`（RuleTester 证明诊断面）+ `TestAutofixDemoRule`（公开引擎面证明 match → fix 渲染，RuleTester `.expect` 无 fix 字段契约不变）；`TestNopRuleSuites` 套件计数门禁 12→13。
- **执行中断点恢复**：本次会话从 mission-driver 配额中断处接手，先核实 Phase 1 已落地（xdef/model/parser/CompiledRule/Diagnostic.fix/RuleSetRunner 渲染 + TestFixEngineWiring/TestTemplateFixAndMerge），基线 609 套件全绿后才动 Phase 2。
- 偏差：① `TestConsoleReporter` 触发 javac CME 崩溃——根因是测试缺 `FixApplier` import 时 javac 在报错路径上崩溃（JDK 21 缺陷面），补 import 后消失；② FixStats.applied 语义收窄为"survived on disk"（回滚扣减），计划文本未明定，取诚实口径并写入 javadoc/design 增注；③ diff 格式器两处真实缺陷（hunk 起始行 consumed 用 hunk[0] 应为 hunk[1]；no-newline 标记求值顺序）被测试矩阵捕获即修。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` exit=0（632 tests，0 failures）；`./mvnw -pl nop-lint/nop-lint-nop -am test` exit=0（TestNopRuleSuites 15/15 含新套件、TestAutofixDemoRule 2/2）。
- Doc-sync: `ai-dev/design/nop-lint/03-execution-engine.md` §2.4 autofix 参数面落地增注；`01-pattern-dsl.md` fix 字段落地增注；`08-migration.md` §2 矩阵新增 autofix 行（03 §3 载体/冲突优先级/multipass 增注与 04 §7 已于前日落地）。
- Closure audit：独立子代理 agent_4dcafb7a-24a5-4d14-bfcf-dadbc017100e APPROVED——live 源码逐条核实三 Phase Exit Criteria + Closure Gates，Anti-Hollow 调用链追踪 PASS，审计实跑 core 656/0 + nop 18/0 + hollow scan 0 + doc-links 0；3 Minor（03 §2.3 注记落点漂移、roadmap item 28 状态滞后、日志跨日）均不阻塞，item 28 按其已过 draft review 的 plan 补记 `planned`。roadmap item 25 `planned` → `done`，plan 置 `completed`，Closure 证据入档。
- 收口验证：`node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/nop-lint/2026-09-22-2225-1-autofix-engine.md --strict` EXIT=0。

- Commit: 见本日 feat(nop-lint) 提交 + 收口 docs 提交。
