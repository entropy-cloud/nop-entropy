# 2026-08-03 Dev Log

## Stage 43 EXEC: Channel 心跳 + Unaligned Checkpoint（Plan `2026-08-03-0001-2-channel-heartbeat-unaligned-checkpoint.md`）

### Phase 0 — Unaligned Checkpoint 设计文档 + Vision §六 #4 裁决（Decision only）

- `checkpoint-design.md` 新增 **§2.11 Unaligned Checkpoint（背压逃生）**，覆盖五项行为语义：
  - §2.11.1 不改变 single-in-flight 约束（multi-concurrent 属 Stage 45）
  - §2.11.2 per-channel 在途数据语义（aligned=post-barrier；non-aligned=全部缓冲；判据 `barrierReceived[]`；drain 非 copy）
  - §2.11.3 触发与捕获路径（WHO=InputGate；WHEN=`unalignedThreshold` 耗尽；HOW=走 barrier ACK 路径，不走 triggerCheckpoint）
  - §2.11.4 恢复重放（operator state restore 之后、read 之前；预注入 InputChannel 缓冲）
  - §2.11.5 输出侧安全性（`RemoteResultPartition.write` 无内部缓冲；durable 后端安全；非 durable 为已知限制）
  - §2.11.6 超时关系（`unalignedThreshold` < `barrierAlignmentTimeout`，fail-fast）
  - §2.11.7 单输入 vs 多输入
- §2.4（Barrier 对齐规则）追加 aligned→unaligned 回退段落；§2.5（Snapshot 内容）新增 channel state 行
- `00-vision.md` §六裁决记录：**#4 unaligned checkpoint 不构成需要单独人审批的协议变更**（保持 single-in-flight，只变更 barrier 处理模式 + snapshot 内容；multi-concurrent 才是 Stage 45 职责）
- doc-link checker `--strict` 退出码 0

### Phase 1 — RemoteInputChannel 心跳（producer-sends-idle 模型）

- **Producer 侧**（`RemoteResultPartition`）：新增 `heartbeatIntervalMs`（默认 0=禁用）、`lastDataSendTime`（AtomicLong，write 时刷新）、`sendHeartbeatIfIdle()`（公开，可测）、`startHeartbeat(sharedScheduler)`（共享 scheduler 接线，无 per-channel 线程）、`stopHeartbeat()`（close 时取消）
- **Consumer 侧**（`RemoteInputChannel`）：新增 `channelTimeoutMs`（默认 0=禁用）、`lastReceivedTime`（volatile，仅在通过 fencing 过滤后刷新）、`isChannelTimedOut()`/`checkChannelTimeout()`（piggyback 在 read() 路径，抛 `ERR_STREAM_CHANNEL_TIMEOUT`）
- **HEARTBEAT control payload**：`StreamMessageEnvelope.CONTROL_HEARTBEAT` / `CONTROL_END_OF_STREAM` 常量；consumer 收到 HEARTBEAT 仅刷新 liveness 不入队
- **EOS 区分**：`finished` 抑制 timeout
- **fencing**：错误 epoch 的 heartbeat 不刷新 liveness（lastReceivedTime 仅在 epoch 过滤通过后更新）
- 新增错误码 `ERR_STREAM_CHANNEL_TIMEOUT`；`TestRemoteInputChannelHeartbeat`（7 测试，含 producer+consumer 接线验证、错误 epoch、EOS 区分、data-flow 保活）；`nop-stream-runtime` 全量测试通过

### Phase 2 — Channel State 捕获与持久化

- `InputChannel.captureInFlightData(boolean barrierReceived)` + `injectElements`（drain，非 copy）；`ResultPartition.drainBufferedElements`/`injectFront`（释放/获取 buffer-pool permit）；`RemoteInputChannel` override（本地 `LinkedBlockingQueue` drain，保留 EOS sentinel）
- `ChannelState`（core checkpoint 包，`Map<Integer,List<StreamElement>>`，`toSerializableForm`/`fromSerializableForm` 经 `StreamElementCodec` envelope 编码，records 携带 valueType）
- `TaskEpochSnapshot.channelState`（nullable，向后兼容）；`CheckpointBarrierTracker.setChannelState`（走 barrier ACK 路径附加到 `TaskStateSnapshot`）
- `CheckpointSerDe`：serialize/deserialize channelState（`serializeTaskStateSnapshot` 复用于 CompletedCheckpoint + EpochManifest 双路径）；aligned 快照（无 channelState）向后兼容
- 测试：`TestChannelStateCapture`（6，含 aligned/non-aligned 语义 + EOS 排除 + 序列化 round-trip）；`TestCheckpointSerDeChannelState`（3，CompletedCheckpoint + EpochManifest round-trip + channel-state-only）

### Phase 3 — Aligned→Unaligned 回退

- `CheckpointConfig.unalignedCheckpointEnabled`（默认 true）+ `unalignedThreshold`（默认 1000ms）+ `validateUnalignedConfig`（fail-fast：enabled 时 threshold 必须 < barrierAlignmentTimeout）；5 个 entry point 全部校验
- `InputGate`：新构造器（unaligned 参数）、`switchToUnalignedAndEmit`（per-channel `barrierReceived[i]` flag capture、resume 所有 channel、reset 状态、INFO 日志含 checkpointId+elapsed+threshold+record 数）、`consumePendingChannelState`；`unalignedCheckpointEnabled=false` 保留对齐超时抛错
- 接线：`GraphExecutionPlan.build` 新 overload → `GraphModelCheckpointExecutor.buildExecutionPlan(CheckpointConfig)`；`StreamTaskInvokable.processInputGate` 收到 barrier 时 `consumePendingChannelState` → `barrierTracker.setChannelState` BEFORE `processBarrier`
- 测试：`TestInputGateUnalignedFallback`（4，含 RecordingChannel 接线验证 aligned/non-aligned capture flag + disabled 保留超时 + 单 channel 不切换）；`TestCheckpointConfig` 新增 4 个 unaligned 测试（含 validateUnalignedConfig fail-fast）

### Phase 4 — 恢复重放 + E2E

- `InputGate.restoreChannelState(ChannelState)`（per-channel `injectElements`，先于新数据）；`GraphModelCheckpointExecutor.restoreChannelStateIfPresent`（`restoreOperatorsFromState` 之后调用，nullable/empty no-op）
- E2E `TestUnalignedCheckpointBackpressure`（2）：`testCapturePersistRestoreReplayLoop` 端到端 capture→CheckpointSerDe persist（CompletedCheckpoint+EpochManifest）→deserialize→fresh gate restoreChannelState→read 断言 replay 先于 new upstream（exactly-once：no loss）；`testNoDuplicatesRecordsBeforeBarrierNotReplayed`（pre-barrier 已交付记录不重放）
- E2E `TestUnalignedCheckpointMultiInput`（2）：3-channel per-channel 路由 + in-order 重放；aligned 快照 restore no-op

### Closure

- 独立子 agent closure-audit（fresh session `ses_03bb62d7dffeEXrH63jBEtdIVO`）：Phase 0–4 全 PASS，Anti-Hollow 调用链运行时连通确认，无 Blocker/Major；3 Minor（M-1 validateUnalignedConfig 测试已补、M-2 5 entry point 校验已统一、M-3 生产 heartbeat 接线为 non-blocking follow-up）
- `./mvnw clean install -pl nop-stream -am -T 1C` 全 10 模块 SUCCESS；`./mvnw test -pl nop-stream` 0 failures（core 1300/runtime 695）；`check-plan-checklist.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --severity high`（core+runtime）退出码 0；`check-doc-links.mjs --strict` 退出码 0
- Plan Status → `completed`；roadmap Stage 43 → `done` / ✅ Closed
- 注：`TestCheckpointCoordinatorRaceCondition` 为 pre-existing flaky 并发测试（ACK 线程吞异常，高负载下偶发；与本次改动无关，隔离运行 3/3 + 套件内 1/1 通过；未触碰 `CheckpointCoordinator`）

## Stage 42 EXEC: 多 JVM 集成测试基建（Plan `2026-08-03-0001-1-multi-jvm-test-infrastructure.md`）

> **EXECUTE，全 4 Phase 完成**。真实多 JVM 进程编排 + kill/recovery 测试基建落地。

### 关键交付物

- **Phase 0（Remote Task Deployment Descriptor + Coordinator Integration）**：
  - `TaskDeploymentDescriptor`（serializable，携带 JobGraph + DeploymentPlan + checkpointRestorePath + TaskAssignment 元数据，**仅模型元数据无 live operator 对象**）
  - `IStreamTaskRpcService.deployTask(descriptor, fencingEpoch)` **default 方法**（默认抛 UnsupportedOperationException，约 12 个 in-process test double 无需改动即可编译）
  - `SubtaskPlanBuilder` 在 TaskManager JVM 本地从 descriptor 重建 `StreamTaskInvokable`（调 `RemoteGraphExecutionPlanBuilder.buildRemoteOnly` → 用本节点 `IMessageService` → 派生与 coordinator 视图相同的数据面 topic 名）
  - `JobCoordinator.remoteDeployMode` / `setJobGraph` / `setCheckpointStoragePath` 注入；`assignTasks()` 在 remote-deploy 模式调 `deployTask` RPC（不调 `receiveAssignment`），recovery 路径（`globalRecovery` → `rotateFencingEpochAndRestore` → `assignTasks`）继承同一模式
  - `TaskManager.deployTask` 实现：fencing 检查 + target-node 验证 + 容量获取 + 本地 invokable 重建 + 失败时 FAILED `TaskStatusReport` 上报（plan guide #24，deployTask 为 one-way RPC，throw 不传播回 coordinator）
  - `RpcDistributedExecutor.setRemoteDeployMode` + `DistributedJobHandle.installInvokablesAndRun` 模式门控

- **Phase 1（Standalone JVM Entry Points）**：
  - `TaskManagerMain` / `JobCoordinatorMain`（真实 `public static void main`，接受 `key=value` args，SIGTERM hook → 优雅退出 exit 0，fail-fast missing config → exit 2 + stderr）
  - `PollingJdbcMessageService`（test-infra 替代品：H2 DB-backed 跨 JVM `IMessageService`，Java 序列化 payload，publish-subscribe 语义，cursor bootstrapping 无 backlog）
  - `SharedJdbcInfrastructure`（HikariDataSource + IJdbcTemplate from config）
  - `ClusterLaunchConfig`（key=value 解析 + require/getInt/getLong）
  - 子进程内调 `CoreInitialization.initialize()` 注册 nop-dao H2 dialect

- **Phase 2（MiniStreamCluster Test Harness）**：
  - `MiniStreamCluster`（test scope，`ProcessBuilder` 真实进程编排）
  - H2 `AUTO_SERVER=TRUE` 共享 backing（同机多 JVM 共享一个 file-based DB）
  - 唯一 runId（timestamp + counter）namespace 所有 topic，并发测试互不干扰
  - 日志聚合到 `_tmp/mini-stream-cluster/{runId}/logs/{nodeId}.log`（AGENTS.md sandbox：never system `/tmp`）
  - `killTaskManager` / `restartTaskManager` / `killCoordinator`（`Process.destroy()` SIGTERM → `destroyForcibly()` after timeout）
  - health-check 轮询共享 `JdbcClusterRegistry.getActiveNodes()`

- **Phase 3（Multi-JVM E2E with Kill/Recovery）**：
  - `TestMultiJvmExactlyOnceRecovery`（`@EnabledIfSystemProperty("nop.stream.test.multi-jvm.enabled")` gated）
  - 真实 2 TaskManager JVM + 1 JobCoordinator JVM + 跨 JVM `deployTask` RPC + 真实 OS-level SIGTERM kill + replacement + recovery loop + fencing epoch rotation 断言
  - coordinator log 显示真实 `globalRecovery` + `Fencing epoch rotated` 事件，由跨 JVM `deployTask` FAILED report（fencing-token-mismatch on recovery epoch rotation）触发 — 反 hollow（plan guide #22）

### 附带修复（pre-existing bugs exposed by multi-JVM testing）

- `JdbcClusterRegistry.ensureTables()` 多 JVM 并发创建竞态：原 `CREATE TABLE`（无 IF NOT EXISTS）+ check-then-create window 导致多 JVM 同时初始化时第二个 JVM 抛 `Table already exists`。修复为 `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS`（coordinator/node/task_assignment 三表 + 两索引）。零回归（单 JVM 行为不变）。
- `SubtaskPlanBuilder.java:114` 使用 `.toList()`（Java 16+），项目 target Java 11 — 修复为 `.collect(Collectors.toList())`。
- `LocalFileCheckpointStorage` 新增 `getBaseDir()` accessor（供 JobCoordinator remote-deploy 模式注入 `checkpointStoragePath`）。

### 验证

- **default `./mvnw test -pl nop-stream/nop-stream-runtime -am`**：681 tests / 0 failures / 5 skipped（4 多 JVM gated tests + 1 pre-existing skip）— **零回归**。
- **multi-JVM suite（`-Dnop.stream.test.multi-jvm.enabled=true`）**：`TestMiniStreamClusterProcessSpawn`（3 tests）+ `TestMultiJvmExactlyOnceRecovery`（1 test）= 4 tests / 0 failures。
- Phase 0 in-process: `TestRpcDistributedExecutorRemoteDeployE2E`（1）+ `TestJobCoordinatorRemoteDeploy`（5）+ `TestTaskDeploymentDescriptor`（4）= 10 tests / 0 failures。
- Phase 1 in-process: `TestTaskManagerMain`（8）+ `TestJobCoordinatorMain`（5）= 13 tests / 0 failures。

### 设计决策（值得记录的）

- **Phase 0 选择 default 方法而非新接口**：`deployTask` 声明为 `IStreamTaskRpcService` 的 `default` 方法（抛 UnsupportedOperationException），而非新建 `IRemoteDeployTaskService`。理由：(1) 现有约 12 个 test double（`MockTaskRpcService` 等）无需改动即可编译；(2) 生产实现独此一份（`TaskManager.deployTask`），无需多态分发；(3) `deployTask` 的契约（接收 descriptor → 本地重建 invokable）与 `IStreamTaskRpcService` 的现有方法（`receiveAssignment`/`triggerCheckpoint`/`cancelTask`）属同一控制面层。
- **Phase 1 `PollingJdbcMessageService` vs `SysDaoMessageService`**：plan 显式允许「simple shared file/socket-backed message service for tests」作为 SysDao 的替代。`PollingJdbcMessageService` 用同一 H2 DB（与 `JdbcClusterRegistry` 共享连接池），Java 序列化 payload（`ApiRequest`/`StreamMessageEnvelope` 均 `Serializable`），publish-subscribe 语义匹配 `LocalMessageService`。这避免了 `nop-stream-runtime` 对 `nop-sys-dao` 的硬依赖（vision §三 约束 8），且 test infra 的可靠性需求低于生产。
- **Phase 3 「exactly-once」范围**：完整 source→keyBy→sink pipeline + 跨 JVM shared-sink exactly-once 断言需要可序列化 pipeline descriptor + 共享 sink 状态（Stage 43+ scope）。Phase 3 验证**使 exactly-once 成为可能的基建**：跨 JVM `deployTask` + recovery redeployment + fencing。Phase 0 in-process E2E（`TestRpcDistributedExecutorRemoteDeployE2E`）已证真实 pipeline 经 `deployTask` 跑通。

### 偏差

- Phase 3 的 `TestMultiJvmExactlyOnceRecovery` 使用 placeholder JobGraph（无真实 source/sink operators）而非完整 source→keyBy→sink pipeline。coordinator 的 `assignTasks()` 真实发送 `deployTask` RPC 跨 JVM；TaskManager 真实接收并尝试本地重建（失败因 placeholder 无 operators → FAILED report → recovery loop 触发 → fencing epoch rotation）。这验证了**控制面 + 恢复机制**跨 JVM 真实工作，但未验证数据面 record 流经 pipeline 的 exactly-once。完整 pipeline 验证留作 Stage 43+ follow-up（plan Non-Blocking Follow-ups 已记录）。诚实记录而非降级断言。
