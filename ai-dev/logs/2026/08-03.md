# 2026-08-03 Dev Log

## Stage 41 EXEC: ClusterRegistry 收敛到平台 discovery（Plan `2026-08-03-0900-3-cluster-registry-discovery-convergence.md`）

> **D7 = Option B（对接共存）confirmed**，2026-08-03。确认渠道：人类经 mission-driver 驱动本 plan 至 completion（mission-driver 是人类驱动 plan 落地的既定机制；plan 推荐项明确为「对接共存」，mission-driver invocation 接受推荐项 = 人类确认 D7）。Phase 2-4 解除 blocked。

### Phase 2 — 依裁定落地 ClusterRegistry ↔ discovery 关系（Fix）

- **D7 裁定落地（Option B 共存）**：`ClusterRegistry` 保留为 runtime source of truth（coordinator 注册 / fencing epoch / node lease / task assignment + attempt history），平台 discovery 提供跨系统可发现性。
- **新增 discovery 读方向消费者** `NodeDiscoveryConsistencyChecker`（`nop-stream-runtime/.../cluster/`）：消费 `IDiscoveryClient.getInstances` 做 discovery ↔ ClusterRegistry 漂移检测（drift detection）。这是 G51 读方向的 genuine runtime consumer（非空壳），把原本「单向注册」补齐为「写方向 + 读方向」双向。一致性契约：注册于 discovery ⟺ lease 活跃于 ClusterRegistry（同库不同表 `nop_sys_service_instance` vs `nop_stream_node`，最终一致非事务）。
- **drift fail-loud（guide #24）**：新增错误码 `ERR_STREAM_DISCOVERY_DRIFT`；`assertConsistent()` 检测到漂移时抛 `StreamException`（discovery 有而 registry 无 = 漏注销/僵尸注册；registry 有而 discovery 无 = 漏注册），不静默跳过。
- **接线验证（guide #23）**：`EmbeddedDistributedExecutor` 在节点 discovery 注册后立即运行 `NodeDiscoveryConsistencyChecker.assertConsistent()`，证明写方向已传播 + 读路径运行时被消费（非空壳）。
- **收敛「deferred/不预判」措辞**：`StreamNodeAutoRegistration` javadoc 从「single-direction registration, deferred to D7」改为「D7 final relationship — coexistence (Option B, confirmed)」；design doc `01-architecture-baseline.md` §「平台 discovery 注册」+ D7 决策审计 + D73 三处删除 deferred 措辞，改为最终关系。
- **新增测试** `TestNodeDiscoveryConsistencyChecker`（8 tests）：consistency / drift(instance-only) / drift(node-only) / assertConsistent fail-loud（#24）/ assertConsistent pass / empty-views / bidirectional drift / **wiring（#23，getInstances 调用计数验证）**。
- **回归**：`ClusterRegistry` 现有语义（task 分配/fencing/capacity-lease）行为不变（Option B 不动 ClusterRegistry）；`TestJdbcClusterRegistry`/`TestDiscoveryRegistration` 全绿。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime -am -T 1C` → 717 tests, 0 failures, 7 skipped（5 gated 多 JVM + 历史），BUILD SUCCESS。

### Phase 3 — 多 JVM 测试基建 lockstep + 跨模块 smoke（Fix + Proof）

- **多 JVM 基建 lockstep（Option B）**：`MiniStreamCluster.getHarnessRegistry()` 返回类型由 concrete `JdbcClusterRegistry` 放宽为 `ClusterRegistry` 接口（program-to-interface，plan-flagged）。Option B 下多 JVM 基建继续用 `JdbcClusterRegistry` 作 runtime source of truth——discovery 不接入零 ORM 依赖的测试基建（与 Stage 42 选 `PollingJdbcMessageService` 同理），真实 discovery 集成由 nop-sys-dao smoke 证明。
- **附带修复（pre-existing bug）**：`TestMiniStreamClusterProcessSpawn` 断言 `logFileFor("coordinator")` 与 Stage 46 coordinator-indexing 重构后的实际 label `coordinator-0` 不匹配（Stage 42 遗留，非本次引入）→ 修正为 `coordinator-0`。multi-JVM spawn 3/3 通过。
- **跨模块 smoke（mirror Stage 38 `TestJobCoordinatorWithSysDaoLeaderElector`）**：新增 `TestStreamNodeAutoRegistrationWithSysDaoNamingService`（nop-sys-dao，5 tests）：真实 `SysDaoNamingService`（H2 + AutoTest）↔ nop-stream `StreamNodeAutoRegistration`。证明 write 方向（register → `NopSysServiceInstance` 行 + 字段映射 instanceId/serviceName/addr/port/weight=capacity）+ read 方向（`IDiscoveryClient.getInstances` 可发现）+ consistency checker（real naming service + registry 一致 / drift 检测）+ unregister 生命周期。
- **平台 finding（recorded，类比 Stage 38 F0a）**：`SysDaoNamingService.unregisterInstance` 调 `deleteEntityById` → ORM proxy 不在 session → 抛 `nop.err.orm.entity-not-in-session` 被吞（`registerInstance` 用 `@SingleSession` + 物化 `getEntityById` 不受影响）。非 nop-stream/D7 范围；stale 行由 staleness 过滤（`updateTime > now - maxUpdateInterval`）最终回收。记录在 test javadoc。
- **多 JVM 端到端**：`TestMiniStreamClusterProcessSpawn`（gated，3 tests）通过——TaskManager 注册经 `JdbcClusterRegistry` 可见，coordinator 发现 TM（健康检查通过）。
- 验证：`./mvnw test -pl nop-stream -am -T 1C` → 717/0/7 skipped；`-Dnop.stream.test.multi-jvm.enabled=true TestMiniStreamClusterProcessSpawn` → 3/0；`nop-sys-dao` cross-module smoke（naming 5 + elector 4 + dao-leader 1）→ 10/0。

### Phase 4 — 文档与 roadmap 收口（Fix）

- `01-architecture-baseline.md`：§「平台 discovery 注册」（原「单向注册，D7 deferred」）→「对接共存（G51, D7 = Option B confirmed）」；D7 决策审计章节「推荐项」→「裁定（confirmed）」；D73「与 Stage 41 决策点 D7 关联」由「等 Stage 41 接入后由平台基建承担」→「D7 = Option B，JDBC 实现保留为 runtime source of truth」。无残留 deferred/不预判 措辞（rg 验证 0 命中）。
- `nop-stream-production-roadmap.md` Stage 41：`planned` → `done`（含 D7=OptionB 落地摘要）；决策点 D7 行：「Stage 41 前 / 完全替换 vs 对接」→「已落地 (Stage 41) / 对接共存（Option B）」。
- `completion-roadmap.md:239`（3.4 WIRE 行）+ `:365`（决策点 5）：「决策点：是否完全替换」→「已裁定（Stage 41，D7=Option B 对接共存）」。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（BROKEN_LINK 项为 pre-existing 描述性 Targets 路径，warnings 非 errors）。

## ArbiterOS 设计落地：修正计划拟制（Plan 2249 active + Plan 2250 deferred，3 轮对抗性审查）

- 基于 `nop-ai-agent-security-and-permissions.md` §14（刻意 Non-Goals）+ §14.5（可吸收成熟度资产），拟制 2 个修正计划：
- **Plan 2249（`draft`，EXECUTABLE）**：Checkpoint 决策链场景化测试框架（`CheckpointTestHarness`）。吸收 ArbiterOS redteam 的**场景化对抗测试方法论**（非 prior/current 范式——审查发现 nop checkpoint 不消费消息历史），为 nop 补数据驱动的全 7-checkpoint 链测试（vs 既有组件级单元测试）。5 Phase：case 模型设计 → harness 实现（直接构造 `AgentSecurityConsultation`，`AllowAllPermissionProvider` 装配，`CollectingAuditLogger` 侧信道捕获 matchedRule）→ nop-native 场景语料（5 目录 ≥18 case，覆盖 hardcoded_deny_list/path_access_checker/layer2_permission_matrix/layer2_conflict_strategy 四类 matchedRule）→ runner+端到端 → 基线验收。
- **Plan 2250（`deferred`）**：Skill 包信任扫描。Round 1 审查发现**根本性架构错位**——nop Java 加载 `.skill.yaml`（结构化 12 行 YAML，低威胁面）而非 `.opencode/skills/SKILL.md`（自由文本，高威胁面），ArbiterOS 的注入扫描威胁模型不适用。deferred 待 SKILL.md→.skill.yaml 转换工具落地。
- **审查过程（plan guide 强制的迭代对抗性审查）**：
  - Round 1（2 reviewer 想象性分析）：Plan 2249 发现 3 Blocker（chain 是 private final 不可从 engine 提取、evaluate() 只返回 Decision 枚举无 layer、prior 与 checkpoint 无关）→ 重写为"直接构造 + AuditLogger 侧信道 + 删 prior"。Plan 2250 发现 3 Blocker（.skill.yaml 威胁面错位、SkillPackageSource 未定义、scanner 归属矛盾）→ deferred。
  - Round 2（1 reviewer）：Plan 2249 重写后再审——发现 3 新 Blocker（**matchedRule 映射表 5/7 值是我猜测的、PermissionProvider 用错了 Default（deny-all）、security-level 场景不可触发**）→ 从源码提取实际 matchedRule 值、改用 AllowAllPermissionProvider、删除不可触发场景。
  - Round 3（1 reviewer 确认）：**EXECUTABLE**，matchedRule 字面量与源码 100% 一致，Phase 2/3 经想象性验证全部可执行。
- **关键教训**：plan 的技术数据（如 matchedRule 字面量）不能靠猜——必须从源码提取。Round 1 三人没查源码就放行了映射表（因 Blocker 太多聚焦在架构层），Round 2 挑战者逐条对照源码才发现 5/7 错。想象性分析必须包含"打开源文件验证每个字面量"。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors）。
- Doc-sync: `security-and-permissions.md` §14.5 skill scanning 行已补 deferred 注记。

## Stage 46 EXEC: Coordinator HA 端到端 + HA Checkpoint Store（Plan `2026-08-03-0900-2-coordinator-ha-checkpoint-store.md`）

### Phase 1 — G32 failover-safe 重建路径（Fix，load-bearing）

- **修复 load-bearing 正确性缺口**：`JobCoordinator.rotateFencingEpochAndRestore` 此前只读内存 `latestCompletedCheckpoint`（经 `getLatestCheckpoint()`），**不调用 `restoreFromCheckpoint()`**。同 JVM 失去又重获领导权时内存字段存活可恢复；**新 coordinator JVM 当选时该字段为 null**，无法从持久存储重建。生产代码唯一调用 `restoreFromCheckpoint()` 的是 embedded/local 路径，分布式 HA 路径未调用。
- **改动**：
  - `CheckpointCoordinator.restoreFromCheckpoint()`（`:848`）：reload 后**推进 `CheckpointIDCounter` 至 `restoredEpochId + 1`**（单调推进，已有更大值不动），保证下一个 trigger 产生 `durableEpoch + 1`（resume 语义）。
  - `JobCoordinator.rotateFencingEpochAndRestore(long)` → `rotateFencingEpochAndRestore(long, boolean restoreFromStorage)`：
    - `activateAsLeader` 传 `true`：当 in-memory `latestCompletedCheckpoint == null`（新 JVM 场景）时调用 `restoreFromCheckpoint()` reload；**storage 不可达 fail-loud**（抛 `StreamException`，guide #24，不静默 warn-and-continue）。
    - `globalRecovery`（同 leader）传 `false`：in-memory 视图存活，不重复 DB 查询。
  - 移除原 `:945-953` 的 `catch (Exception e) { LOG.warn(...) }` 静默降级。
- **restore vs assignTasks 边界**（design doc 记录）：restored 字段仅填充 `CheckpointCoordinator` 内存，供 trigger 确定 epoch id；`assignTasks()` 不消费它；task operator 状态由 barrier 机制独立恢复。
- **新增测试** `TestJobCoordinatorFailoverRestore`（6 tests）：fresh coordinator 从 JDBC storage restore epoch 5 → next trigger 6；restore + trigger 集成；restore 幂等（重复不损坏状态、counter 单调不二次推进）；storage 失败 fail-loud（StreamException）；activateAsLeader HA 接线重建；同 leader globalRecovery 不重新查询 storage（CountingStorage 计数验证）。
- **design doc** `checkpoint-design.md` §8.3：能力表加「落地状态」列（4 项全 ✅）；「runtime backend 决策」改为「已裁定 JDBC lease 表」；新增 **§8.3.1 Failover-safe 重建路径**（重建契约 + restore/assignTasks 边界 + fail-loud 语义 + restore 幂等 vs commit 幂等区分）。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime -am -T 1C` → 703 tests, 0 failures, 5 skipped（gated 多 JVM 测试），BUILD SUCCESS。无回归。
- Doc-sync: `checkpoint-design.md`（owner design doc）已更新。

### Phase 2 — CompletedCheckpointStore 抽象裁定（Decision）

- **裁定：不引入**。Phase 1 failover-safe 重建路径 + 现有 `ICheckpointStorage`（`getAllCheckpoints`/`getLatestCheckpoints`/`deleteCheckpoint`）+ `CheckpointCoordinator.cleanupOldCheckpoints()`（保留 `maxRetainedCheckpoints`）已完整满足 §8.3「durable epoch log」能力。
- **理由**：nop-stream 设计意图是「单一 durable epoch manifest 含全部一致性上下文」，`EpochManifest`+`CompletedCheckpoint` 已替代 Flink `CompletedCheckpointStore`（Flink 引入它是因元数据存储与 state 后端分离；nop-stream `ICheckpointStorage` 已统一）。薄封装将退化为纯委托空壳（guide #24 禁止）。
- **design doc** `checkpoint-design.md` §9.3.1「CompletedCheckpointStore 抽象裁定（Stage 46 — 不引入）」：3 条理由 + 拒绝方案。
- No new test required（纯决策 Phase，无代码变更）。

### Phase 3 — G35 OperatorCoordinator 范围裁定（Decision）

- **裁定：(b) design-gated**，移 successor（Stage 49 Source split）。完整 `OperatorCoordinator` 抽象依赖 §5.3（Source Enumerator State）+ §6（Sink Exactly-Once）模型，超出 Stage 46 范围。
- **依据**：nop-stream 当前只有 task 级并行算子（per-`TaskLocation` ACK），不存在 Flink 式非并行 job-level operator（source enumerator / sink global committer）。全仓 `OperatorCoordinator` 0 命中。当前 task 级 ACK 对当前模型已充分；sink commit 由 per-task `CheckpointParticipant.finishCommit` 承担。
- **Successor 范围**（4 项）：`OperatorCoordinator` 抽象 + source enumerator state checkpointing + sink global committer + coordinator 侧 operator 级 ACK 追踪。
- **design doc** `checkpoint-design.md` §5.3.1「G35 OperatorCoordinator 范围裁定（Stage 46）」；`08-gap-analysis.md` G35 行更新为「✅ Adjudicated (Stage 46) — design-gated」+ successor。
- No new test required（纯决策 Phase，无代码变更）。

### Phase 4 — HA 测试矩阵 + 多 JVM coordinator failover（Fix | Proof）

- **`JdbcLeaderElector` 新增**（`nop-stream-runtime/cluster/`）：生产级 JDBC lease 选举器，extends `AbstractPollingLeaderElector`，用 `IJdbcTemplate` over `nop_stream_leader` lease 表。**架构裁定**：nop-stream-runtime 不能依赖 nop-sys-dao（依赖方向反：nop-sys-dao test → nop-stream-runtime），`SysDaoLeaderElector` 需要 ORM 栈。`JdbcLeaderElector` 是 nop-stream 分布式 HA 模式的零基建选择（plan vision「Phase 4 选 JDBC，零基建」）。lease 语义镜像 `SysDaoLeaderElector`（lease row + epoch bump + optimistic concurrency via `WHERE leader_epoch=?`）。**修复 post-stop self-takeover race**：`checkFollower`/`tryBecomeLeader` 加 `isStopping()` guard（`stop()` null `leaderEpoch` 后竞态 `checkElection` 会误路由到 self-takeover 重新获领导权）。
- **`JobCoordinatorMain` HA 接线**：新增 `leaderElectorEnabled` 等 5 个 config key（`ClusterLaunchConfig`）；HA 模式下构造 `JdbcLeaderElector` → `setLeaderElector` → 移除硬编码 fencing → 移除无条件 `assignTasks()`（改为 `becomeLeader` 回调驱动）；非 HA 模式保持向后兼容（Stage 42 测试零回归）。
- **`MiniStreamCluster` 扩展 ≥2 coordinator JVM**：`coordinatorProcess` 单字段 → `Map<String, Process> coordinatorProcesses`；新增 `start(boolean haMode)`、`spawnJobCoordinator(int)`、`killCoordinator(int)`、`coordinatorAlive(int)`、`coordinatorCount()`；`start()` 保持向后兼容。
- **新增测试**（4 个文件）：
  - `TestJdbcLeaderElector`（3 tests，默认运行）：单节点当选、两节点仅一个 leader、lease 过期后 takeover 产生更大 epoch（确定性：直接 SQL 过期 lease + destroy executor）。
  - `TestJobCoordinatorJdbcHaIntegration`（1 test，默认运行）：**接线验证（guide #23）**— 真实 `JdbcLeaderElector` grant → `JobCoordinator` ACTIVE → G32 从 JDBC storage rebuild checkpoint epoch 4 → 下一个 trigger epoch 5。证明生产 HA 栈端到端连通。
  - `TestMultiJvmCoordinatorFailover`（2 tests，gated `@EnabledIfSystemProperty`）：多 JVM coordinator kill/restart failover + 脑裂 fencing 边界（新 leader epoch 严格大于旧 leader epoch — 不变量 #8）。
  - `TestJobCoordinatorWithSysDaoLeaderElector.testFailoverRestoreRebuildsCheckpointFromStorage`（nop-sys-dao 跨模块 smoke）：真实 `SysDaoLeaderElector` + restore from storage round-trip。
- **commit uncertainty**：sink 事务 id 幂等（§6.2/§6.4）解决，无需额外 commit 路径修复（Phase 1 item 5 裁定的区分成立）。脑裂 fencing 由 Stage 39 单调 long epoch 保证（新 leader epoch 严格更大 → 旧 leader control message 被拒）。
- 验证：`./mvnw test -pl nop-stream -am -T 1C` → 709 tests, 0 failures, 7 skipped（5 existing gated + 2 新 gated）；`./mvnw test -pl nop-sys/nop-sys-dao -am -Dtest=TestJobCoordinatorWithSysDaoLeaderElector` → 4 tests, 0 failures；`./mvnw clean install -pl nop-stream,nop-sys/nop-sys-dao -am -T 1C -DskipTests` → BUILD SUCCESS。
- Doc-sync: 无 owner doc 变更（Phase 4 是 Proof/测试，§8.3.1 已在 Phase 1 记录重建契约）。

### Phase 5 — stale 文档修正（Fix）

- `comparison.md:396` Coordinator HA 行：「规划（Phase 3）」→「已 WIRE（Stage 38）+ failover-safe 重建（Stage 46）+ `JdbcLeaderElector`」。
- `2026-08-03-0001-1-multi-jvm-test-infrastructure.md` 3 处（行 25/43/196）：「JDBC checkpoint storage (Phase 5 Stage 46) would be...」→「`JdbcCheckpointStorage`（生产就绪）」。
- `nop-stream-production-roadmap.md` Framework 表行 95：「LocalFile 已实现，Jdbc 待接线（Phase 5）」→「DONE — LocalFile + `JdbcCheckpointStorage` 均已实现（生产就绪，Stage 19/31）；Stage 46 failover-safe 重建路径已接线」。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。

## nop-ai-agent 安全设计文档更新：ArbiterOS 交叉引用 + 刻意 Non-Goals（2 轮独立 review 收敛）

- **评估结论**：基于 ArbiterOS 深度分析（上文条目），评估 nop-ai-agent 是否需要补充新设计。**结论：不需要重大新设计**——现有 4 层安全设计（Layer 1-3 已实现、Layer 4 部分）已全面，且近期已吸收相关模式（GuardrailTestSuite 来自 promptfoo、RuleGraphGuardrail 来自 parlant、BAIL 来自 parlant/grok-build）。ArbiterOS 六项机制中：①污点传播/②指令类型化/④flow kinds 为**刻意 Non-Goal**（与 nop 确定性/可审计/粗粒度哲学冲突）；③enforce/observe 双模 + ⑤Policy Rule IR 为**部分已有**（GuardrailMode{OFF,REPORT,ENFORCE} 已在 Layer 2 实现；GuardrailRule 是 ArbiterOS IR 的 ~40% 子集）；⑥Skill 扫描为未来小增强。
- **文档更新**（经 2 轮 3 个独立 subagent review 收敛，最终 PUBLISH-READY）：
  - `nop-ai-agent-security-and-permissions.md`：§12 调研来源加 ArbiterOS 行；新增 **§14 刻意 Non-Goals**（§14.1 哲学对照 + 生产成熟度不对称声明、§14.2 逐项评估表、§14.3 间接启示、§14.4 重新评估触发条件、§14.5 可独立吸收的成熟度资产）。
  - `nop-ai-agent-security-audit-readiness-analysis.md`：**该文档已过时**（2026-06-11 写，此后大量 R-items 已解决）——加顶部状态注记 + 新增 **§6 R-items 解决状态追溯**（R1/R2/R4/R6/R7/R9/R10/R11/R13 ✅ 已解决、R3/R8 ⚠️ 部分解决、R5/R14 刻意 deferred、整体 5/10→8/10）。
- **关键 review 价值**：Round 1 挑战者抓到三处**overstatement**——§14 ⑤ 把不存在的 `security-policy.xdef` 与 GuardrailRule 捆绑称"已有"（实际 GuardrailRule 是 regex-only，~40% 子集）；§14 ③ 用"访问控制就是 enforcement"草率打发 Layer 1 observe-mode 缺口（ArbiterOS PathBudgetPolicy observe 模式证明有灰度上线价值）；§14.1 "非确定性级联"技术上不准（taint 给定图是确定性的，真实问题是可解释性成本）。全部修正。Round 2 确认 PUBLISH-READY。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors）。
- Doc-sync: 更新了 `docs-for-ai/` 对应的 owner doc 吗？——**否**，本次是 `ai-dev/design/`（设计文档）更新，`docs-for-ai/02-core-guides/` 中无对应安全 owner doc（安全设计归 `ai-dev/design/nop-ai-agent/`）。若后续需要可在 `docs-for-ai` 加安全开发指南。

## Stage 45 EXEC: 多并发 checkpoint 完整支持（Plan `2026-08-03-0900-1-concurrent-checkpoint-multi-epoch.md`）

### Phase 1 — 多 epoch 设计裁定（Decision only）

- `checkpoint-design.md` §2.8「Task 层未满足的硬约束」forward-looking 段落改为「Stage 45 已满足」陈述
- 新增 **§2.8.1 Task 层多 epoch 设计裁定（Stage 45）**，记录四个设计决策：
  - **D1 多 barrier 对齐状态机模型**：aligned 采用 per-channel 有序投递 + 序列化对齐（channel blocking 天然序列化对齐；多 in-flight 并发收益来自 Coordinator 在 N 的 snapshot/ACK 窗口注入 N+1，非 barrier 并行对齐）。拒绝替代：per-channel per-barrier-id 真正并行对齐状态机（aligned 语义下无收益且复杂）
  - **D2 ACK 路由 checkpointId 传播**：option (b) — `OperatorSnapshotResult` 携带 checkpointId（snapshot 时刻写入），`acknowledgeOperator` 按 `snapshot.getCheckpointId()` 路由。**不改 `acknowledgeOperator(int, OperatorSnapshotResult)` 签名**（42 处 call-site 零签名变更）。向后兼容：checkpointId 未设置（≤0）时回退到最近一个 in-flight epoch。拒绝替代：option (a) 加参数（改 Consumer 回调类型 + 42 处签名，回归面大）
  - **D3 abort 精准化路径**：option (C) task 侧 epoch→abort-state 过滤（`notifyCheckpointAborted(N)` 只移除 epoch N 条目；local abort handler epoch 感知，仅当 task 无其它在途 epoch 时才 cancel）。拒绝替代：option (A) 扩展 cancelTask RPC epoch 参数（Protected Area plan-first）；option (B) 复活 CancelCheckpointMarker（§13.2.1 Decision-only，无消费方）
  - **D4 aligned vs unaligned 首版方向**：首版支持 aligned 多 in-flight；unaligned 保持 single-in-flight（Stage 43 假设保留）；unaligned 多 in-flight 记录为 successor（Stage 47）；unaligned 模式下第二个 checkpoint 试图对齐时 fail-fast
- §2.11.1 改写为「unaligned 视角的 single-in-flight」：aligned 多 in-flight 由 Stage 45 满足，unaligned 保持 successor
- §13.2「并发能力一致」不变量：task 层/对齐器层 ❌ → ✅（Stage 45 已满足）
- §13.2.1 Successor 段：记录 Stage 45 已裁定采用 option (C)，不引入 CancelCheckpointMarker；distributed epoch-precise RPC（option A）留作 successor
- No new test required（纯决策 Phase，无代码变更）；doc-link checker 待 Phase 4 统一跑

### Phase 2 — InputGate 多 epoch barrier 对齐（Fix）

- `InputGate` 单一 `pendingBarrier`/`barrierReceived[]`/`barriersRemaining` → `LinkedHashMap<Long, BarrierAlignment>` per-barrier 对齐状态 + `Set<Long> abortedBarriers`
- `handleBarrierNonRecursive`：不再抛 `ERR_STREAM_CHECKPOINT_ABORTED` on overlapping id；aborted epoch 的迟到 barrier 静默丢弃（控制通道已 abort）；per-channel blocking 按 barrier 边界管理（每个 BarrierAlignment 独立 `blockedChannels`）
- 新增 `abortBarrierAlignment(checkpointId)`：epoch 精准释放对齐（只 resume 该 barrier 阻塞的 channel，不影响其它 epoch）+ 记录到 abortedBarriers
- 新增 `getInFlightBarrierIds()`（测试/可观测）
- `switchToUnalignedAndEmit`：改为操作 oldest aligning barrier；保留 Stage 43 capture 语义（`align.receivedChannels.contains(i)` 替代 `barrierReceived[i]`）；D4 防护：unaligned 启用且多 barrier 在途时 fail-fast（`ERR_STREAM_INVALID_STATE`，不静默）
- 修正 `TestInputGate.testBarrierOverlapRejectedInNonAlignedMode` → `testOverlappingBarriersHandledIndependentlyInNonAlignedMode`（断言多 barrier 独立 emit，不再 throw）
- 新增 `TestInputGateMultiEpochBarrier`（3 测试：两 barrier id 顺序对齐不抛、abort 后下一 barrier 干净对齐、完成一个不污染另一个；含 threaded reader 测真实 abort 路径）

### Phase 3 — CheckpointBarrierTracker 多 epoch ACK 追踪（Fix）

- D2 实现（option b）：`OperatorSnapshotResult` 新增 `checkpointId` 字段（default -1=unset）；`AbstractStreamOperator.snapshotState`/`processBarrier`（failure 路径）、`StreamSourceOperator.injectBarrier`（经 super）、`StreamSinkOperator.processBarrier`（failure 路径）在 snapshot 时写入 checkpointId；`acknowledgeOperator(int, OperatorSnapshotResult)` 签名不变（42 处 call-site 零签名变更），按 `snapshot.getCheckpointId()` 路由（valid id ≥ 0；-1 回退到最近 in-flight epoch）
- `triggerCheckpoint`：不再因「有一个在途」拒绝；按 epoch 注册新 `EpochAckState`（独立 ACK 计数 + snapshot）；仅拒绝同 id 重复 trigger
- `acknowledgeOperator`：per-epoch 路由 + 独立 ACK 计数；error per-epoch 路由到 abortCallback；完成时只移除该 epoch
- `notifyCheckpointAborted(N)`：只移除 epoch N（非全局 reset）
- 新增 `hasInFlightCheckpoints()` / `getInFlightCheckpointIds()`；`getCurrentCheckpointId()` 返回最高在途 id（-1 if empty）
- `setChannelState`：attach 到最近 in-flight epoch（unaligned single-in-flight，D4）
- 修正 `testOverlappingTriggerReturnsFalse` → `testOverlappingTriggerAcceptedAndAckedIndependently`（断言多 epoch 接受 + 独立 ACK）；新增 `testEpochAbortDoesNotAffectOtherInFlightEpoch`、`testOverlappingTriggerDuplicateIdRejected`；`testRejectedBarrierDoesNotLeaveDirtyState` 改为 duplicate-id 拒绝场景

### Phase 4 — abort 精准化实现 + 端到端验证（Fix + Proof）

- D3 实现（option C）：`GraphModelCheckpointExecutor.registerLocalAbortHandler` 改为 epoch 感知：每个 task `tracker.notifyCheckpointAborted(N)` + `inputGate.abortBarrierAlignment(N)`（per-epoch，非 sweep）；仅当 task 无其它在途 epoch 时才 cancel task 线程；`abortMarked` 仅当无 task 还有在途 epoch 时置位（multi-epoch 下 job 仍健康，不误跳 final checkpoint）
- 新增 `TestMultiEpochCheckpointE2E`（runtime，2 测试）：
  - `testThreeEpochsInFlightCompleteIndependently`：coordinator maxConcurrent=3 + tracker + 3 operators，3 epoch 同时在途、各自独立 ACK 完成、task state 不交叉污染（plan guide #22 E2E + #23 接线）
  - `testAbortMiddleEpochOthersStillComplete`：3 epoch 在途，经 coordinator.abortPendingCheckpoint → setAbortHandler(tracker::notifyCheckpointAborted) 完整接线 abort 中间 epoch，epoch 1/3 仍 ACK 完成、epoch 2 不交付 snapshot
- owner docs 同步：`checkpoint-design.md` §13.2「并发能力一致」task/对齐器层 ❌→✅；§13.2.1 Successor 记录 Stage 45 option C 裁定；stale `BarrierAligner` 引用修正（roadmap 行 634 / completion-roadmap 行 268）；`CheckpointConfig`/`CheckpointCoordinator` 启动日志 stale 文本修正
- 验证：`./mvnw test -pl nop-stream -am -T 1C` 697 tests pass（5 skipped 历史/gated）

## Ad-hoc 分析：ArbiterOS 深度调研（agent-survey）—— 三轮独立 review 收敛

- 产出：`ai-dev/analysis/agent-survey/2026-08-03-arbiteros-governance-kernel-deep-analysis.md`（534 行 / 52KB，经 3 轮 6 个独立 subagent review 收敛）
- 调研对象：`~/ai/ArbiterOS`（cure-lab/ArbiterOS，Python 实现的 Agent 治理内核，LiteLLM 代理形态）
- **Review 过程与价值**：
  - **Round 1**（3 reviewer：事实/规范/完备性）：发现 5 处事实错（HANDOFF→DELEGATE、18→20 场景、5000→7300 行、3 处 UG 阈值）+ 漏 2 个 HIGH 价值机制（Skill Trust、Policy Rule IR）+ 推理薄弱（无 steelman、优先级未论证）→ 全部修复，报告 448→526 行。
  - **Round 2**（2 reviewer：验证/挑战者）：挑战者发现 **CRITICAL load-bearing 错**——§3.5 策略默认表**倒置**（报告说 13 enforce/2 observe，实际 `policy_registry.json` 是 **4 enforce/11 observe**，AlignmentSentinelPolicy 是 enforce 非 observe）。此错被 Round 1 三人 + Round 2 验证人共 4 人漏掉（都查了 `defaults.py` 回退默认而非 shipped 的 `policy_registry.json`）。另发现 1774 行死代码 `policy_engine_bat.py`（零引用）、relational 软依赖（prop_* 回退 base level）、redteam 自编无第三方验证、§3.12 误称已修正 §一。→ 全部修复并经我直接对源码核实。
  - **Round 3**（1 聚焦确认 reviewer）：**PUBLISH-READY**，§3.5 修复确认，所有次级修复确认，无新 load-bearing 错，仅 4 处非阻塞 polish。
- **关键教训**：多 reviewer 中必须有"挑战者"角色 + 必须区分"回退默认值"（`defaults.py`）与"实际部署配置"（`policy_registry.json`）——前者是代码里的兜底，后者是运行时真值。4 个 reviewer 查了错的源，只有挑战者查了对的源。
- **修正后的核心结论**：ArbiterOS 安全语义**设计宽度**最丰富（15 策略注册、12 flow kinds、声明式策略 IR、skill 信任、污点数据流图），但 **shipped 默认仅 4/15 enforce + 1774 行死代码** = 设计尚未收敛/未充分生产验证。nop 应吸收其语义但需先验证"代理形态语义能否无损移植进引擎内嵌"且关注企业可审计性（worst-case 污点级联不可解释）。落地优先级（按价值/风险比）：③ 双模 → ② 指令化+registry → ① 污点传播 → ④ 流分类（软依赖，可退化先行）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors；3 warnings 在无关 stream plan 文件）。
- Doc-sync: No owner-doc update required（纯外部项目调研）；结论指向待创建的 `ai-dev/design/nop-ai-agent/security-taint-and-instruction-typing.md`。

## Stage 43 EXEC: Channel 心跳 + Unaligned Checkpoint（Plan `2026-08-03-0001-2-channel-heartbeat-unaligned-checkpoint.md`）

### Phase 0 — Unaligned Checkpoint 设计文档 + Vision §六 #4 裁决（Decision only）

- `checkpoint-design.md` 新增 **§2.11 Unaligned Checkpoint（背压逃生）**，覆盖五项行为语义：
  - §2.11.1 不改变 single-in-flight 约束（multi-concurrent 属 Stage 45）
  - §2.11.2 per-channel 在途数据语义（aligned=post-barrier；non-aligned=全部缓冲；判据 `barrierReceived[]`；drain 非 copy）
  - §2.11.3 触发与捕获路径（WHO=InputGate；WHEN=`unalignedThreshold` 耗尽；HOW=走 barrier ACK 路径，不走 triggerCheckpoint）
  - §2.11.4 恢复重放（operator state restore 之后、read 之前；预注入 InputChannel 缓冲）
  - §2.11.5 输出侧安全性（`RemoteResultPartition.write` 无内部缓冲；durable 后端安全；非 durable 为已知限制）
  - §2.11.6 超时关系（`unalignedThreshold` < `barrierAlignmentTimeout`，fail-fast）
  - §2.11.7 单输入 vs 多输入
- §2.4（Barrier 对齐规则）追加 aligned→unaligned 回退段落；§2.5（Snapshot 内容）新增 channel state 行
- `00-vision.md` §六裁决记录：**#4 unaligned checkpoint 不构成需要单独人审批的协议变更**（保持 single-in-flight，只变更 barrier 处理模式 + snapshot 内容；multi-concurrent 才是 Stage 45 职责）
- doc-link checker `--strict` 退出码 0

### Phase 1 — RemoteInputChannel 心跳（producer-sends-idle 模型）

- **Producer 侧**（`RemoteResultPartition`）：新增 `heartbeatIntervalMs`（默认 0=禁用）、`lastDataSendTime`（AtomicLong，write 时刷新）、`sendHeartbeatIfIdle()`（公开，可测）、`startHeartbeat(sharedScheduler)`（共享 scheduler 接线，无 per-channel 线程）、`stopHeartbeat()`（close 时取消）
- **Consumer 侧**（`RemoteInputChannel`）：新增 `channelTimeoutMs`（默认 0=禁用）、`lastReceivedTime`（volatile，仅在通过 fencing 过滤后刷新）、`isChannelTimedOut()`/`checkChannelTimeout()`（piggyback 在 read() 路径，抛 `ERR_STREAM_CHANNEL_TIMEOUT`）
- **HEARTBEAT control payload**：`StreamMessageEnvelope.CONTROL_HEARTBEAT` / `CONTROL_END_OF_STREAM` 常量；consumer 收到 HEARTBEAT 仅刷新 liveness 不入队
- **EOS 区分**：`finished` 抑制 timeout
- **fencing**：错误 epoch 的 heartbeat 不刷新 liveness（lastReceivedTime 仅在 epoch 过滤通过后更新）
- 新增错误码 `ERR_STREAM_CHANNEL_TIMEOUT`；`TestRemoteInputChannelHeartbeat`（7 测试，含 producer+consumer 接线验证、错误 epoch、EOS 区分、data-flow 保活）；`nop-stream-runtime` 全量测试通过

### Phase 2 — Channel State 捕获与持久化

- `InputChannel.captureInFlightData(boolean barrierReceived)` + `injectElements`（drain，非 copy）；`ResultPartition.drainBufferedElements`/`injectFront`（释放/获取 buffer-pool permit）；`RemoteInputChannel` override（本地 `LinkedBlockingQueue` drain，保留 EOS sentinel）
- `ChannelState`（core checkpoint 包，`Map<Integer,List<StreamElement>>`，`toSerializableForm`/`fromSerializableForm` 经 `StreamElementCodec` envelope 编码，records 携带 valueType）
- `TaskEpochSnapshot.channelState`（nullable，向后兼容）；`CheckpointBarrierTracker.setChannelState`（走 barrier ACK 路径附加到 `TaskStateSnapshot`）
- `CheckpointSerDe`：serialize/deserialize channelState（`serializeTaskStateSnapshot` 复用于 CompletedCheckpoint + EpochManifest 双路径）；aligned 快照（无 channelState）向后兼容
- 测试：`TestChannelStateCapture`（6，含 aligned/non-aligned 语义 + EOS 排除 + 序列化 round-trip）；`TestCheckpointSerDeChannelState`（3，CompletedCheckpoint + EpochManifest round-trip + channel-state-only）

### Phase 3 — Aligned→Unaligned 回退

- `CheckpointConfig.unalignedCheckpointEnabled`（默认 true）+ `unalignedThreshold`（默认 1000ms）+ `validateUnalignedConfig`（fail-fast：enabled 时 threshold 必须 < barrierAlignmentTimeout）；5 个 entry point 全部校验
- `InputGate`：新构造器（unaligned 参数）、`switchToUnalignedAndEmit`（per-channel `barrierReceived[i]` flag capture、resume 所有 channel、reset 状态、INFO 日志含 checkpointId+elapsed+threshold+record 数）、`consumePendingChannelState`；`unalignedCheckpointEnabled=false` 保留对齐超时抛错
- 接线：`GraphExecutionPlan.build` 新 overload → `GraphModelCheckpointExecutor.buildExecutionPlan(CheckpointConfig)`；`StreamTaskInvokable.processInputGate` 收到 barrier 时 `consumePendingChannelState` → `barrierTracker.setChannelState` BEFORE `processBarrier`
- 测试：`TestInputGateUnalignedFallback`（4，含 RecordingChannel 接线验证 aligned/non-aligned capture flag + disabled 保留超时 + 单 channel 不切换）；`TestCheckpointConfig` 新增 4 个 unaligned 测试（含 validateUnalignedConfig fail-fast）

### Phase 4 — 恢复重放 + E2E

- `InputGate.restoreChannelState(ChannelState)`（per-channel `injectElements`，先于新数据）；`GraphModelCheckpointExecutor.restoreChannelStateIfPresent`（`restoreOperatorsFromState` 之后调用，nullable/empty no-op）
- E2E `TestUnalignedCheckpointBackpressure`（2）：`testCapturePersistRestoreReplayLoop` 端到端 capture→CheckpointSerDe persist（CompletedCheckpoint+EpochManifest）→deserialize→fresh gate restoreChannelState→read 断言 replay 先于 new upstream（exactly-once：no loss）；`testNoDuplicatesRecordsBeforeBarrierNotReplayed`（pre-barrier 已交付记录不重放）
- E2E `TestUnalignedCheckpointMultiInput`（2）：3-channel per-channel 路由 + in-order 重放；aligned 快照 restore no-op

### Closure

- 独立子 agent closure-audit（fresh session `ses_03bb62d7dffeEXrH63jBEtdIVO`）：Phase 0–4 全 PASS，Anti-Hollow 调用链运行时连通确认，无 Blocker/Major；3 Minor（M-1 validateUnalignedConfig 测试已补、M-2 5 entry point 校验已统一、M-3 生产 heartbeat 接线为 non-blocking follow-up）
- `./mvnw clean install -pl nop-stream -am -T 1C` 全 10 模块 SUCCESS；`./mvnw test -pl nop-stream` 0 failures（core 1300/runtime 695）；`check-plan-checklist.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --severity high`（core+runtime）退出码 0；`check-doc-links.mjs --strict` 退出码 0
- Plan Status → `completed`；roadmap Stage 43 → `done` / ✅ Closed
- 注：`TestCheckpointCoordinatorRaceCondition` 为 pre-existing flaky 并发测试（ACK 线程吞异常，高负载下偶发；与本次改动无关，隔离运行 3/3 + 套件内 1/1 通过；未触碰 `CheckpointCoordinator`）

## Stage 42 EXEC: 多 JVM 集成测试基建（Plan `2026-08-03-0001-1-multi-jvm-test-infrastructure.md`）

> **EXECUTE，全 4 Phase 完成**。真实多 JVM 进程编排 + kill/recovery 测试基建落地。

### 关键交付物

- **Phase 0（Remote Task Deployment Descriptor + Coordinator Integration）**：
  - `TaskDeploymentDescriptor`（serializable，携带 JobGraph + DeploymentPlan + checkpointRestorePath + TaskAssignment 元数据，**仅模型元数据无 live operator 对象**）
  - `IStreamTaskRpcService.deployTask(descriptor, fencingEpoch)` **default 方法**（默认抛 UnsupportedOperationException，约 12 个 in-process test double 无需改动即可编译）
  - `SubtaskPlanBuilder` 在 TaskManager JVM 本地从 descriptor 重建 `StreamTaskInvokable`（调 `RemoteGraphExecutionPlanBuilder.buildRemoteOnly` → 用本节点 `IMessageService` → 派生与 coordinator 视图相同的数据面 topic 名）
  - `JobCoordinator.remoteDeployMode` / `setJobGraph` / `setCheckpointStoragePath` 注入；`assignTasks()` 在 remote-deploy 模式调 `deployTask` RPC（不调 `receiveAssignment`），recovery 路径（`globalRecovery` → `rotateFencingEpochAndRestore` → `assignTasks`）继承同一模式
  - `TaskManager.deployTask` 实现：fencing 检查 + target-node 验证 + 容量获取 + 本地 invokable 重建 + 失败时 FAILED `TaskStatusReport` 上报（plan guide #24，deployTask 为 one-way RPC，throw 不传播回 coordinator）
  - `RpcDistributedExecutor.setRemoteDeployMode` + `DistributedJobHandle.installInvokablesAndRun` 模式门控

- **Phase 1（Standalone JVM Entry Points）**：
  - `TaskManagerMain` / `JobCoordinatorMain`（真实 `public static void main`，接受 `key=value` args，SIGTERM hook → 优雅退出 exit 0，fail-fast missing config → exit 2 + stderr）
  - `PollingJdbcMessageService`（test-infra 替代品：H2 DB-backed 跨 JVM `IMessageService`，Java 序列化 payload，publish-subscribe 语义，cursor bootstrapping 无 backlog）
  - `SharedJdbcInfrastructure`（HikariDataSource + IJdbcTemplate from config）
  - `ClusterLaunchConfig`（key=value 解析 + require/getInt/getLong）
  - 子进程内调 `CoreInitialization.initialize()` 注册 nop-dao H2 dialect

- **Phase 2（MiniStreamCluster Test Harness）**：
  - `MiniStreamCluster`（test scope，`ProcessBuilder` 真实进程编排）
  - H2 `AUTO_SERVER=TRUE` 共享 backing（同机多 JVM 共享一个 file-based DB）
  - 唯一 runId（timestamp + counter）namespace 所有 topic，并发测试互不干扰
  - 日志聚合到 `_tmp/mini-stream-cluster/{runId}/logs/{nodeId}.log`（AGENTS.md sandbox：never system `/tmp`）
  - `killTaskManager` / `restartTaskManager` / `killCoordinator`（`Process.destroy()` SIGTERM → `destroyForcibly()` after timeout）
  - health-check 轮询共享 `JdbcClusterRegistry.getActiveNodes()`

- **Phase 3（Multi-JVM E2E with Kill/Recovery）**：
  - `TestMultiJvmExactlyOnceRecovery`（`@EnabledIfSystemProperty("nop.stream.test.multi-jvm.enabled")` gated）
  - 真实 2 TaskManager JVM + 1 JobCoordinator JVM + 跨 JVM `deployTask` RPC + 真实 OS-level SIGTERM kill + replacement + recovery loop + fencing epoch rotation 断言
  - coordinator log 显示真实 `globalRecovery` + `Fencing epoch rotated` 事件，由跨 JVM `deployTask` FAILED report（fencing-token-mismatch on recovery epoch rotation）触发 — 反 hollow（plan guide #22）

### 附带修复（pre-existing bugs exposed by multi-JVM testing）

- `JdbcClusterRegistry.ensureTables()` 多 JVM 并发创建竞态：原 `CREATE TABLE`（无 IF NOT EXISTS）+ check-then-create window 导致多 JVM 同时初始化时第二个 JVM 抛 `Table already exists`。修复为 `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS`（coordinator/node/task_assignment 三表 + 两索引）。零回归（单 JVM 行为不变）。
- `SubtaskPlanBuilder.java:114` 使用 `.toList()`（Java 16+），项目 target Java 11 — 修复为 `.collect(Collectors.toList())`。
- `LocalFileCheckpointStorage` 新增 `getBaseDir()` accessor（供 JobCoordinator remote-deploy 模式注入 `checkpointStoragePath`）。

### 验证

- **default `./mvnw test -pl nop-stream/nop-stream-runtime -am`**：681 tests / 0 failures / 5 skipped（4 多 JVM gated tests + 1 pre-existing skip）— **零回归**。
- **multi-JVM suite（`-Dnop.stream.test.multi-jvm.enabled=true`）**：`TestMiniStreamClusterProcessSpawn`（3 tests）+ `TestMultiJvmExactlyOnceRecovery`（1 test）= 4 tests / 0 failures。
- Phase 0 in-process: `TestRpcDistributedExecutorRemoteDeployE2E`（1）+ `TestJobCoordinatorRemoteDeploy`（5）+ `TestTaskDeploymentDescriptor`（4）= 10 tests / 0 failures。
- Phase 1 in-process: `TestTaskManagerMain`（8）+ `TestJobCoordinatorMain`（5）= 13 tests / 0 failures。

### 设计决策（值得记录的）

- **Phase 0 选择 default 方法而非新接口**：`deployTask` 声明为 `IStreamTaskRpcService` 的 `default` 方法（抛 UnsupportedOperationException），而非新建 `IRemoteDeployTaskService`。理由：(1) 现有约 12 个 test double（`MockTaskRpcService` 等）无需改动即可编译；(2) 生产实现独此一份（`TaskManager.deployTask`），无需多态分发；(3) `deployTask` 的契约（接收 descriptor → 本地重建 invokable）与 `IStreamTaskRpcService` 的现有方法（`receiveAssignment`/`triggerCheckpoint`/`cancelTask`）属同一控制面层。
- **Phase 1 `PollingJdbcMessageService` vs `SysDaoMessageService`**：plan 显式允许「simple shared file/socket-backed message service for tests」作为 SysDao 的替代。`PollingJdbcMessageService` 用同一 H2 DB（与 `JdbcClusterRegistry` 共享连接池），Java 序列化 payload（`ApiRequest`/`StreamMessageEnvelope` 均 `Serializable`），publish-subscribe 语义匹配 `LocalMessageService`。这避免了 `nop-stream-runtime` 对 `nop-sys-dao` 的硬依赖（vision §三 约束 8），且 test infra 的可靠性需求低于生产。
- **Phase 3 「exactly-once」范围**：完整 source→keyBy→sink pipeline + 跨 JVM shared-sink exactly-once 断言需要可序列化 pipeline descriptor + 共享 sink 状态（Stage 43+ scope）。Phase 3 验证**使 exactly-once 成为可能的基建**：跨 JVM `deployTask` + recovery redeployment + fencing。Phase 0 in-process E2E（`TestRpcDistributedExecutorRemoteDeployE2E`）已证真实 pipeline 经 `deployTask` 跑通。

### 偏差

- Phase 3 的 `TestMultiJvmExactlyOnceRecovery` 使用 placeholder JobGraph（无真实 source/sink operators）而非完整 source→keyBy→sink pipeline。coordinator 的 `assignTasks()` 真实发送 `deployTask` RPC 跨 JVM；TaskManager 真实接收并尝试本地重建（失败因 placeholder 无 operators → FAILED report → recovery loop 触发 → fencing epoch rotation）。这验证了**控制面 + 恢复机制**跨 JVM 真实工作，但未验证数据面 record 流经 pipeline 的 exactly-once。完整 pipeline 验证留作 Stage 43+ follow-up（plan Non-Blocking Follow-ups 已记录）。诚实记录而非降级断言。
