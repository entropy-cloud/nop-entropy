# 2026-09-03

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 1723-1 Phase 1——展开方法裁定 + core 169 文件通配符导入展开 + 防回潮门禁子命令）

- Plan: `ai-dev/plans/nop-stream-productization/2026-09-03-1723-1-test-wildcard-import-cleanup.md`（item 22）
- **展开方法裁定（Decision，含拒绝替代方案）**：采用**脚本机械展开**——`_tmp/expand-wildcard-imports.mjs`（Node 单遍、可审计、dry-run 默认 + `--write` 显式）。判定规则：类型通配按「裸简单名词法出现」（排除 `.` 前缀限定引用/注释/字符串/关键字/同文件声明类型）展开为实际引用的显式导入；静态方法成员按「裸调用形态」（标识符后随 `(`）判定；常量成员（NopStreamErrors ERR_/ARG_、ProcessingGuarantee 枚举）按裸词判定；限定调用 `Assertions.x(...)` 不新增静态导入；既有显式导入遮蔽 on-demand 时跳过（Java 语义对齐）；冲突防御=同文件同名类型声明/双通配备同一简单名 → CONFLICT 报告不落盘人工处理（本轮 core 0 命中）。成员索引事实源：`jimage list java.base`（java.util/concurrent/atomic）、javap + `unzip -Z1` 于 resolved `junit-jupiter-api-6.0.3.jar`（Assertions 19 静态成员 + api 包类型清单）、repo 源码目录 `*.java`-only 清单（checkpoint/operators/jobgraph/transformation/execution/common.functions/common.state/api-core message）、NopStreamErrors/ProcessingGuarantee 源码解析。导入块按分组约定整体重建（io.nop → third（含 jakarta/javax）→ java → static 最后，组内字典序，组间空行）。**拒绝**：手工逐文件（169+ 文件不可审计、必然漏改）；IDE organize-imports（无 headless 批处理先例、输出不可控）；启用 root pom checkstyle AvoidStarImport（root pom 影响全仓库非 nop-stream 模块，超出 mission 范围，plan Non-Goals 已裁定）。
- **core 169 文件展开**：`169 → 0`（口径 `rg -l '^import .*\*;' nop-stream/nop-stream-core/src/test/java`），757 条显式导入物化，0 冲突文件。**零行为变更证明**：git diff 全树非 import/空行变更行数 = 0（grep 断言）；`./mvnw test -pl nop-stream/nop-stream-core -am -T 1C` **1554 tests / 0 failures / 0 errors / 1 skipped（既有 skip）BUILD SUCCESS**。
- **执行中排障（记录防复发）**：首版包成员索引用 `ls` 生成，把子目录（state/backend、functions/sink、functions/source）当成了包成员类型 → 生成非法导入（`import io.nop.stream.core.common.state.backend;`），`test-compile` 当场拦截；修正为 `*.java`-only 清单后 test 树整体 revert 重展开（760→757 条，3 条 bogus 导入消失）。on-demand import 不导入子包——成员索引只含顶层类型。
- **防回潮门禁落地**：`check-nop-stream-invariants.mjs` 新子命令 `check-wildcard-imports`（main+test 双树、行级 `import x.y.*;`/`import static x.y.Z.*;` 检查、`--module m1[,m2]` scope 参数=本子命令新工作、未知模块名 hard error）；committed 正向自证 fixture `ai-dev/audits/nop-stream-invariants/fixtures/WildcardImportFixture.java`（双形态故意违规）+ self-test 断言必红（inline 双形态计数 + explicit 负控 0 违规）。**接入时序裁定执行**：Phase 1—2 过渡期默认 no-arg 运行面不变（`all` 不含本子命令，无中期红窗）；`--module nop-stream-core` 已清模块 exit 0、runtime/cep 显式 scope 红（预期态）、no-arg 面 exit 0 复核一致。
- **验证**：`node ai-dev/tools/check-nop-stream-invariants.mjs check-wildcard-imports --module nop-stream-core` exit 0；`self-test` exit 0（含新 fixture 断言）；no-arg exit 0；`echo 'typecheck not configured' && ./mvnw clean install -pl nop-stream -am -T 1C -DskipTests && echo 'lint not configured'` 全过。
- No new test required: 纯测试代码导入清理，行为面由全量既有测试（1554）覆盖——显式声明。
- Doc-sync: No owner-doc update required（测试代码风格治理，无契约/行为变更；门禁用法已记录于本条目与 plan，供 item 23 及后续 plan/audit 直接引用：`node ai-dev/tools/check-nop-stream-invariants.mjs check-wildcard-imports [--module m1[,m2...]]`）。

### 2026-09-03（mission nop-stream-productization DRAFT_PLANS：items 22/23/25 三 plan 起草 + 对抗性审查 consensus → active）

- **起草**：三份 plan 落盘（执行顺序 = 文件名 N 位），roadmap items 22/23/25 同写 `planned` + Last updated 同步：
  - `ai-dev/plans/nop-stream-productization/2026-09-03-1723-1-test-wildcard-import-cleanup.md`（item 22，317 test 文件通配符导入 sweep + 防回潮门禁）
  - `ai-dev/plans/nop-stream-productization/2026-09-03-1723-2-core-execution-package-reorg.md`（item 23，Task 执行族下沉子包，零行为变更）
  - `ai-dev/plans/nop-stream-productization/2026-09-03-1723-3-checkpoint-manifest-versioning-checksum.md`（item 25，EpochManifest stateFormatVersion/checksum 落地，D-DRIFT-2 收敛）
- **live 基线复核（起草前置）**：item 22 逐模块通配符计数与 roadmap 全对账（169/108/15/1/5+5+2+4/4/4=317，main 0）；item 23 execution 根包 32 文件（含 package-info）+ 4 主体类声明 + main 8/test 31 importer 复算；item 25 EpochManifest 现字段 + CheckpointSerDe 咽喉（:142/:205，CURRENT=2）+ buildEpochManifest 调用点（:534/:641/:1378）+ 双存储路由 + §2.6 字段表 :201/:203 逐项核实。
- **对抗性审查（独立子 agent，plan 1 两轮、plans 2/3 三轮，均 fresh session）**：
  - 第一轮发现并修复：plan 1 Blocker（基线把静态/非静态形态计数写反——live 真相 = 317 文件全部含 `static Assertions.*`、非静态仅 core 23/runtime 55/connector 2）+ 3 Major（Phase 3 计数 20→24、门禁中期红窗时序裁定、closure 门禁工具具名）；plan 2 Blocker（invariants 双注册表钉定未处理）+ 2 Major（引用面三类编译器盲区、rg 口径未固化）；plan 3 2 Major（checksum 计算位置预承诺且与 monitor 锁作用域冲突、stateFormatVersion 读侧语义缺失）。
  - 第二轮 plan 1 **consensus**；plans 2/3 各余 1 Major：wiring-registry.json（8 条 file:line wiringPoint，scan-wiring V3/V5 硬校验）遗漏、小数/浮点文本往返未钉定（keyedStates 透传/accumulator 任意类型 × `Double.parseDouble` 收窄）——均修复。
  - 第三轮 plans 2/3 **consensus**（session `ses_f994ee87bffeOlyiwxSTUwKcQS` / `ses_f994ec67effeqlO6JV51is2g91`；plan 1 第二轮 session `ses_f9958c841ffeygzYa0XMtK6xbe`）；三 plan `Plan Status: draft → active`。
- **排序裁定**：22 → 23（显式导入使包移动爆炸半径可计数；soft preference 非 hard block，fallback 落 plan 2 baseline）；25 独立并行（D-DRIFT-2 收敛，与 22/23 无文件交集）。
- 验证：三 plan 文本一致性自查（无 checked box、无「四类/20 文件」残留、五类测试口径统一）；本条目为纯 plan 起草轮，无代码变更。
- Doc-sync: No owner-doc update required（plan/roadmap/log 三处更新；owner-doc 变更在各 plan phase 内裁定）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-3 closure——items 21/24/30 done，三审计联动收敛验收达成）

- **Closure audit**（独立 general subagent fresh session `ses_f9981f31cffeGexuLner5zS5Hy`，完整报告 `_tmp/closure-audit-0830-3.md`）：**CLOSURE-AUDIT: APPROVED**（0 Blocker / 0 Major / 0 Minor，2 Info 均预期态）——A 收敛真实性 5/5（单点实现被消费路径真实调用：snapshotKeyedState/restoreKeyedEntries/resolveTypeName、memory+rocksdb getOrCreateState 各 8 重载、applyWholeStorageMigration 4 委托 + applyValueMigration 4+1 继承、WindowAssignerSupport 4 assigner 8 处、SGG 三单点 10 处——无死代码）；B 守卫 3/3（三守卫 + 两对等守卫 typed 错误，11 用例断言错误码+参数）；C 回归面 3/3（矩阵/fixture/写稳定 canonical 化口径/schemaVersion 恒 1）；D 命令复跑 gated 7/7 + 全量 3370/0/0 双跑一致；E 两态计数 17 行复算表对照一致（2 处裁定保留有论证）；F 诚实性 3/3（Deferred 分类/owner-doc §5.3+§6.4+§12.6/无吞异常）。
- **收口仪式**：Closure Gates 11/11 勾选 + Closure evidence 写入 → Plan Status `completed`（Completed: 2026-09-03）；roadmap items 21/24/30 三项同写 `done`（附验收摘要 + audit session）+ Last updated 同步。
- **行为零变更证明（closure gated 面必跑项）**：`./mvnw test -pl nop-stream/nop-stream-fraud-example -am -T 1C -Dtest='TestS1MultiJvmE2E,TestS2MultiJvmE2E,TestS2RestoreRescaleMultiJvmE2E,TestScenarioBackpressureMultiJvmE2E' -Dnop.stream.test.multi-jvm.enabled=true -Dsurefire.failIfNoSpecifiedTests=false` → **7/7 绿 0 skipped**（S1 2 + S2 2 + C2 1 + C3 2；执行者与 audit 各自复跑一致）——状态 SerDe/恢复路径属分布式 checkpoint/restore 数据面，gated 面通过证明收敛不破坏分布式快照恢复主路径。
- **工具门禁**：`check-plan-checklist --strict` exit 0 / `check-doc-links --strict` exit 0（31305 引用 0 错误）/ `scan-hollow-implementations --module nop-stream --severity high` exit 0（0 findings）/ `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（**3370 tests / 0 errors / 0 failures** / 25 skipped=gated+已知 @Disabled）/ `clean install -DskipTests` + typecheck/lint echo 通过。
- plan 无 `> Source Audits:` 前置项（roadmap-sourced plan），source audit 关闭步骤 N/A。
- Doc-sync: No owner-doc update required（owner-doc 变更已在 Phase 2（§6.4）/Phase 3（§12.6）/Phase 4（§5.3）条目落档；本条目为收口仪式，仅 plan/roadmap/log）。
- Follow-up：单点实现进一步模板化（mirror single-point 形态下 core/rocksdb 骨架统一为后续优化候选）；item 22 通配符导入与本 plan 改动文件交集归其统一 sweep。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-3 Phase 5——windowing 与图生成样板收敛 D-4 + 全清单总复算）

- **SETW/SPTW + 溢出守卫收敛**：新增包私有 `WindowAssignerSupport`（validateSliding 单点——原 17/17 行逐字相同构造器校验；`windowOf` 溢出守卫单点——原 4 处复制（TETW/TPTW/SETW/SPTW）；`slidingWindowsFor` 滑动展开循环单点——原每滑动 assigner 一份）。SETW 104→87 行、SPTW 98→79 行；tumbling 双子接 `validateTumbling`/`windowOf`。包私有 helper = 无新公共 API（Non-Goals 遵守）。
- **CETT/CPTT 收敛（Phase 1 裁定方向执行）**：新增包私有基类 `ContinuousIntervalTrigger`（interval/stateDesc/S-8a 校验/nextFireTimestamp 计算）；**CETT 侧不可达 MAX_VALUE 守卫删除**——两向等价论证落注释（fireTimestamp ≤ window.maxTimestamp() ⇒ MAX_VALUE 时必先命中 maxTimestamp 短路分支；补齐审计 §2.1② 只覆盖反向的缺口）；针对性用例 `TestContinuousEventTimeTrigger.testGlobalWindowMaxValueTimerFiresViaMaxTimestampShortCircuit`（GlobalWindow + MAX_VALUE timer 经短路分支 FIRE——钉定守卫可删性的前提）。CETT 156→148、CPTT 148→132 + 基类 76 行；event-time vs processing-time 的 timer 注册面真实差异保留（裁定保留论证：合一需布尔分叉）。
- **StreamGraphGenerator 样板收敛**：节点创建（node + parallelism lock + chainingStrategy + 注册）6 处 → `addOperatorNode` 单点；source 注册双子（transformSource vs transformSourceApi ~85%）→ `addSourceNode` 单点；上游边创建 4 处 → `addUpstreamEdge` 单点（getInputs().get(0) ≡ getInput()——singletonList 语义核对）。670→617 行。
- **回归**：triggers/ 全家族 + evictors/ + windowing 全部既有用例 + 图生成（TestStreamGraphGenerator 17 + TestStreamModelPopulation 5 + TestJobGraphGenerator 10）全绿；S1/S2 场景 LOCAL 测试（TestS1CdcPipelineE2E/TestS1CdcRecoveryE2E/TestS2FileAggregationE2E/TestS2RecoveryAndRescaleE2E/TestS2OfflineReshardE2E，默认套件）随 `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS 全绿。
- **全清单总复算（closure audit 消费，Phase 1 基线同口径对照）**：
  | 清单 | Phase 1 基线 | 终态 | 两态 |
  |---|---|---|---|
  | D-1 restore 家族方法 | 8 | 6（List/InternalList、Aggregating/InternalAggregating 合并）+ restoreKeyedEntries 单循环 | 收敛 |
  | D-1 snapshot 家族方法 | 8 | 1 泛型 snapshotKeyedState + 薄分发（8 分发位点单实现） | 收敛 |
  | D-1 typeName 回退 inline 块 | 11 | 1（resolveTypeName 单点） | 收敛 |
  | D-1 entry-loop 模板 | 16 | 2（snapshot/restore 各一） | 收敛 |
  | D-2 类级克隆对 | 2 | 0（storageKey() 覆写模式） | 收敛 |
  | D-2 applyMigration 逐字复制 | 4 | 1（applyWholeStorageMigration + 4 委托） | 收敛 |
  | D-2 同分支 if/else 化石 | 4 | 0（hoist） | 收敛 |
  | D-3 getXxxState 重载样板 | 8 | 1（getOrCreateState 单点） | 收敛 |
  | D-3 rebind instanceof 阶梯 | 8 路 | 1（AbstractMemoryState 超类型） | 收敛 |
  | D-4 SETW/SPTW 构造器校验 | 17/17 行 | 1（validateSliding） | 收敛 |
  | D-4 溢出守卫 | 4 | 1（windowOf） | 收敛 |
  | D-4 CETT/CPTT 克隆 | ~80% | 共享基类 + 差异面保留 | 收敛（event/processing timer 面裁定保留——真实语义差异） |
  | D-4 SGG 节点/边样板 | 6+4+2 | 1+1+1（addOperatorNode/addUpstreamEdge/addSourceNode） | 收敛 |
  | item 30 SerDe snapshot/restore | 7/8 | 1+map/6（registerRestoredState + restoreValueEntries 单点） | 收敛 |
  | item 30 backend 重载 | 8 | 1（getOrCreateState） | 收敛 |
  | item 30 rebind 阶梯 | — | n/a（rocksdb 状态终态引用，无 rebind 概念——Phase 1 复算裁定） | 裁定保留（结构性不存在） |
  | item 30 状态类克隆对 | 2 + 整值迁移 5 处 | 0 + 1（applyValueMigration） | 收敛 |
  | item 24 三守卫 | 0 | 3（core 咽喉点）+ 2（rocksdb 对等：KeyEncoder namespace + restoreMapState pair） | 声明内行为增量 |
  | RK-4 core 对应面 | 缺失 | applyTtl config 等价保持 | 修复（同族缺口） |
  无「为过门禁做形式收敛」位点；全部收敛位点行为由 Phase 1 固化回归面（矩阵/fixture）+ 既有套件钉定。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（本 phase 后再次全绿）。
- Doc-sync: No owner-doc update required（触发器/图生成行为契约未变，由既有测试钉定——window-design.md 无结构描述需同步）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-3 Phase 4——rocksdb 同族收敛 item 30 + 守卫对等落地）

- **SerDe 收敛（RocksDBSnapshotSerDe 848→733 行，与 core Phase 2 形态对齐）**：snapshot 家族方法 7→1 泛型 `snapshotKeyedState`（固定 header 序 + TTL 过滤 RocksIterator 循环 + 每族 EntryWriter）+ `snapshotMapState`（分组逻辑保留）+ 薄分发（Internal 先于 public——core 侧教训直接复用）；restore 8→6（List/InternalList、Aggregating/InternalAggregating 合并）+ `registerRestoredState`（8 处 states/descriptor/type 三连注册 → 单点）+ `restoreValueEntries`（5 处标量 entry 循环 → 单点）；RK-3 legacy 回退经 `resolveTypeName` 单点保持。
- **backend 收敛（RocksDBKeyedStateBackend 966→914 行）**：8 个 getXxxState 重载 → `getOrCreateState` 单点（create-or-verify + CF 解析 + 注册）；**rebind 阶梯 n/a 复算确认**（rocksdb 状态构造时持 backend+cf 终态引用，无 rebind 概念——Phase 1 裁定兑现）。
- **状态类族收敛**：新增 `AbstractRocksDBState`（backend/cfHandle/ttl + RocksDbTtlAware 实现 + `applyValueMigration` 整值迁移单点——原 5 处逐字复制：Value/Reducing/InternalAppending/Aggregating/InternalAggregating）；List 对 230→54 行、Aggregating 对 260→115 行（storageKey() 覆写模式镜像 core）。**克隆对漂移两处随收敛收口（记录）**：① `resolveStorageValueType` WARN 只在 internal 侧（RK-5 修了双胞胎之一）——统一为 WARN（RK-5 家族对齐）；② internal aggregating 的 applyMigration 用 raw descriptor 类型而自身 get()/add() 用 storageValueType——统一为 storageValueType（与自身读路径一致的内部一致性修复）。
- **守卫对等落地（Phase 1 裁定执行）**：`RocksDBKeyEncoder.deserializeNamespace` TimeWindow 字段守卫（typed 错误含坏内容）+ `restoreMapState` mapValue 逐对校验（state 名 + pair 下标）——与 core 侧同语义；坏数据用例 `TestRocksDBRestoreGuards` 4 个（坏 TimeWindow/好 TimeWindow 经 internal list 恢复/坏 pair #1 下标/非 List mapValue）。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS——rocksdb 全量（TestRocksDBSnapshotRestore 21 用例 + 增量族 4（真实 SST 物化→range restore）+ fail-fast 族）+ 矩阵/fixture 全绿（增量主路径端到端未被收敛破坏）。
- Doc-sync: `ai-dev/design/nop-stream/state-management-design.md` §5.3 增恢复路径坏数据防御对等条目（Phase 2 §6.4 承诺闭环）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-3 Phase 3——core 状态类与后端收敛 D-2+D-3 + RK-4 core 对应修复）

- **D-2 收敛**：新增 `AbstractMemoryState` 基类（backend/ttl/rebind/bindTtl/ttlContext 单点 + `applyWholeStorageMigration` 整库迁移单点）；类级克隆对 2→0——`MemoryInternalListState` 160→52 行（仅覆写 `storageKey()` namespace 守卫 + setCurrentNamespace/getCurrentNamespace）、`MemoryInternalAggregatingState` 191→105 行（仅覆写 storageKey + accumulator 访问器 + S-5 IOException 包装）；applyMigration 整库逐字复制 4→1（+4 个一行委托）；同分支 if/else 化石 4→0（hoist `if(ttl!=null) writeEviction` 后统一 `storage.get`——两分支本就都在 eviction 后取值，语义零变更）；List/Aggregating 公共类引入 `protected storageKey()` 钩子。
- **D-3 收敛**：8 个 getXxxState 重载 → `getOrCreateState` 单点（create-or-verify + registerStateType + Value/Map 家族 serializer adoption 参数化保持原仅两族采用面）；rebindStateBackends 8 路 instanceof 阶梯 → AbstractMemoryState 超类型单路径；MemoryKeyedStateBackend 511→467 行。**排障**：`snapshotOneState` 分发序须 Internal 先于 public（继承后 `instanceof MemoryListState` 匹配子类）——fixture 写稳定测试当场拦截（internal-list 被写成 ListState stateType）。
- **RK-4 core 对应修复（复算发现的同族缺口）**：`MemoryKeyedStateBackend.applyTtl` 增 config 等价检查（`TtlAware.ttlContext()` 访问器新增）——重复 getState 不再重绑全新 TtlContext 抹掉累计 sidecar（rocksdb RK-4 已修，core 侧对齐）。行为增量与 RK-4 同族：TTL 窗口不再静默重置。
- **TTL 行为用例（RK-4 core 对应面）**：`TestMemoryStateTtl.repeatedGetStatePreservesTtlTimestamps`（新增 focused）+ 矩阵 `ttlVariantMemory` 翻转为 repeated-getState 模式（Phase 1 单句柄模式退役）全绿。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS；矩阵/fixture/迁移/TTL/backend 全部既有用例全绿（No new test required beyond RK-4 focused：纯结构重构由 Phase 1 固化回归面覆盖——RK-4 修复属行为修复已配 focused 用例）。**排障记录**：rocksdb 模块测试必须 `-am`（否则消费本地仓库旧 core 构件——TTL matrix 假失败根因）。
- Doc-sync: `ai-dev/design/nop-stream/state-management-design.md` §12.6 增「重复 getState 保持累计窗口（RK-4 两侧对齐）」。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-3 Phase 2——core SerDe 收敛 + item 24 守卫）

- **D-1 收敛（MemoryStateSerDe 993→829 行）**：snapshot 侧 8 个成对家族方法 → 单一 `snapshotKeyedState`（固定 header 序 + TTL 过滤 entry 循环 + 每族 `EntryWriter` payload lambda，Appending 原始语义（不经 valueSer、List 防御拷贝）与 Map 的 P1-21-01 container 包装原样保留）；restore 侧 8 个成对方法 → 6（List/InternalList 合并、Aggregating/InternalAggregating 合并——仅 state 类差异参数化）+ 单一 `restoreKeyedEntries` entry 循环；11 处 inline typeName 回退块 → 单点 `resolveTypeName`（S-12 legacy 回退次序逐族保持，fixture 钉定）；entry-loop 模板 16 → 2（snapshot/restore 各一）。字节格式零变更：Phase 1 fixture 写稳定用例（`TestStateSerdeFixtureStability.resnapshotIsByteIdenticalToFrozenFixture`）在收敛后代码上原样全绿。
- **item 24 三守卫落地（typed 错误 + 定位参数）**：① TimeWindow namespace 字段守卫（`deserializeNamespace(obj, stateName)`——@type 非 String 安全比较 + start/end Number 校验，state 名入参）；② mapValue 逐对校验（非 List 整体 + 逐对 List/size≥2，state 名 + pair 下标）；③ `TaskEpochSnapshot.getKeyGroupRange` start/end 一致性（双侧数值入错误信息）。坏数据路径：裸 NPE/CCE/IAE → `ERR_STREAM_STATE_ERROR` typed 错误（本 plan 声明内唯一行为增量）。
- **守卫行为测试 7 用例**：`TestMemoryStateSerdeRestoreGuards`（5：坏 TimeWindow 断言错误码+stateName 参数+消息定位 / 好 TimeWindow 恢复 / 坏 pair 断言 #1 下标 / 非 List mapValue / 好数据恢复）+ `TestTaskEpochSnapshotKeyGroupRangeGuard`（2：坏记录断言双侧数值 / 好记录+未物化记录+生产 stamping 路径不受影响）。
- **owner-doc 同步**：`state-management-design.md` §6.4 增「恢复路径坏数据防御」段（三守卫 + 定位参数语义 + 两侧对等承诺——Phase 4 rocksdb 侧落地后闭环）；纯结构收敛部分 No owner-doc update required。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（含 rocksdb/cep/runtime/e2e 全模块——MemoryStateSerDe 消费面广泛覆盖）；无静默跳过自查：三守卫拒绝路径全部显式 typed 异常（对照 guide 规则 24）。
- Doc-sync: `ai-dev/design/nop-stream/state-management-design.md`。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-3 Phase 1——基线复算与回归面固化，items 21/24/30 共同载体）

- **清单复算（live，2026-09-03，closure 复算沿用同口径）**：审计锚点全部存在、无第三方改动；行数：MemoryStateSerDe 993 / MemoryKeyedStateBackend 511 / RocksDBSnapshotSerDe 848 / RocksDBKeyedStateBackend 966。计数（位点→基线数字）：
  - **D-1**：restore 家族方法 8；snapshot 家族方法 8；typeName 回退模板 inline 块 11（Value/List/InternalList/Aggregating/InternalAggregating 各 1 + Map/Appending/Reducing 各 2）；entry-loop 模板 16（restore 8 + snapshot 8）。S-12 legacy 回退语义已在位（保持项非缺口）。
  - **D-2**：类级克隆对 2（List/InternalList、Aggregating/InternalAggregating，~85%）；applyMigration 整库逐字复制 4（Reducing/Aggregating/InternalAggregating/InternalAppending）+ 列表元素迁移复制 2 + value/map 各 1；同分支 if/else 化石 4（四类 add() 的 `if(ttl!=null){writeEviction;get}else{get}`）；TTL 读/写/clear 键样板 ×8 类。
  - **D-3**：8 个 getXxxState 重载（85% 同构）+ rebindStateBackends 8 路 instanceof 阶梯。
  - **D-4**：SETW/SPTW 构造器 17/17 行逐字相同（含校验块）；溢出守卫 4 处（TETW:79/TPTW:45/SETW:61/SPTW:60）；CETT/CPTT ~80%（CETT 多 MAX_VALUE 守卫，见裁定）；StreamGraphGenerator 节点创建样板 6 方法 + 边创建 4 处 + Source/SourceApi 双子 ~85%。
  - **item 30**：SerDe snapshot 家族方法 7 个覆盖 8 态（List/InternalList 已参数化共享——core 侧收敛的既有先例）；restore 8；backend 8 重载；状态类 8 个（类级克隆对 2 + 全类共享样板：字段/接口实现/TTL 键助手 evictIfExpired/recordWrite/全扫 applyMigration 骨架）。**rocksdb 无 rebind 阶梯**（states 构造时持 backend+cf 终态引用，无 rebind 概念——plan 该子项裁定 n/a，Phase 4 记录）。
  - **复算中发现（非清单漂移，转 Phase 3 修复）**：core `MemoryKeyedStateBackend.applyTtl` 无 RK-4 对应的 config 等价检查——任意重复 getState 重绑全新 TtlContext 抹掉累计 sidecar（rocksdb 侧 RK-4 已修，core 侧为同族缺口）——正是 plan Phase 3「TTL 行为用例（RK-4 core 对应面）全绿」要求的修复项；Phase 1 回归面对 memory TTL 用单句柄模式钉定现状，Phase 3 修复后翻转为重复 getState 模式。
- **形态裁定（含拒绝替代方案）**：
  - **收敛形态 = core/rocksdb 两侧各自单点（mirror single-point）**，不抽跨后端公共骨架。拒绝：抽公共骨架到 core 由 rocksdb 复用——两侧同构的是「形状」（per-state info map 结构、entries 循环、typeName 回退次序）而非「实现」（memory 遍历 HashMap + serializeWithSerializer/ContainerValueCodec；rocksdb 遍历 RocksIterator + 二进制 key 编解码 + RocksDBValueSerDe），骨架需 5+ lambda 注入点，间接度收益为负；跨后端格式兼容由既有 21 用例 + 本轮矩阵测试双向钉定，无需共享代码保格式；共享骨架会把 rocksdb 序列化语义固化为 core 公共 API 面，与 Non-Goals「无新公共 API」冲突。两侧收敛后的「形状对齐」由跨后端矩阵断言承载。
  - **CETT/CPTT = 提取共享基类（interval/stateDesc/of() 校验）+ 删除 CETT 侧不可达 MAX_VALUE 守卫**。删守卫方向等价性论证（补齐审计 §2.1② 只覆盖「向 CPTT 加守卫」方向的缺口）：fireTimestamp 由 `Math.min(time+interval, window.maxTimestamp())` 产生 ⇒ fireTimestamp ≤ window.maxTimestamp()；fireTimestamp == Long.MAX_VALUE ⟹ window.maxTimestamp() == Long.MAX_VALUE ⟹ 该 timer 触发时 time == window.maxTimestamp() 必先命中短路 FIRE 分支，守卫分支不可达。拒绝：向 CPTT 加死守卫换文本对齐（形式收敛）；两触发器完全合一（onElement/clear/onMerge 的 event-time vs processing-time timer 注册面有真实语义差异，合一需布尔分叉=审计禁止的「差异硬塞布尔参数」）。
  - **rocksdb 守卫对等性 = 对等落地**（W-R1 watch-only 理由「与 core 容忍基线一致」随 core 收紧而失效）：Phase 4 在 rocksdb 收敛后的单一 restore 咽喉点对等落 namespace TimeWindow 字段守卫 + mapValue 逐对校验 + 坏数据 typed 错误用例；TaskEpochSnapshot.getKeyGroupRange 修复在 core（两侧共享 checkpoint 数据对象）。声明内行为增量：坏数据路径裸 NPE/CCE/IAE → typed StreamException。
- **回归面固化（新测试 11 用例 + fixture 资源 2 份，未重构代码上全绿）**：
  - `TestStateFamilyBackendMatrix`（rocksdb 模块，7 用例）：状态族全family（Value/Map/List/InternalList/Reducing/Aggregating/InternalAggregating/Appending + TimeWindow namespace 变体）× 四方向矩阵（mem→mem/mem→rocks/rocks→rocks/rocks→mem）+ TTL 变体双后端（过期条目不入快照、活跃条目存活恢复；rocks 侧含 RK-4 重复 getState 保持）+ namespace 跨后端往返。既有 21 用例/核心 round-trip 套件引用不重写。
  - `TestStateSerdeFixtureStability`（core，4 用例）+ `state-serde-fixtures/{all-families-memory,legacy-type-name-keys}.json`（重构前真实产物冻结）：**读兼容**（fixture 恢复出全部 8 族值）+ **写稳定**（同状态图 re-snapshot 与 fixture 逐字节相等，经 canonicalJson）+ **legacy 键路径 fixture**（`*TypeName` 拼写恢复成功——S-12 语义钉定）+ 覆盖 8 族完备性断言。
  - **捕获层裁定**：测试内 JSON 序列化（JsonTool.serialize(stateData)——local-storage checkpoint 持久化同族格式）；确定性硬标准经 canonicalization 达成：entries 列表序与 state-name map 序源于 TypedNamespaceAndKey/HashMap 迭代（hashCode 混入 namespace Class identity hash，JVM-run 依赖）显式裁定**非格式契约**，比较两侧统一 canonical 化（仅排此两项）；字段序（LinkedHashMap 插入序）与 mapValue 对序（String 键用户 map）保持原样比对，写侧字段序漂移不可逃逸。增量 SST 二进制路径不经本 SerDe，显式排除（既有 4 增量测试承载）。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（两轮）。No production change in this phase（显式声明——仅测试与资源）。
- Doc-sync: No owner-doc update required（纯测试与裁定固化，无行为变更；Phase 2 守卫落地时再评估）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-2 closure——item 20 done，P-REQ-13/14 验收达成）

- **Closure audit**（独立 general subagent fresh session `ses_f9a677b85ffeJzf9OP3uOSpdFH`，完整报告 `_tmp/closure-audit-0830-2.md`）：**CLOSURE-AUDIT: APPROVED**——A（Phase 2 校验核心/入口族/选项名/E2E）、B（Phase 3 分派序/五族实现/接线/残留/Anti-Hollow 调用计数）、C（Phase 4 唯一新依赖/三 fail-closed 码/瞬态解密/main 代码零 testCredential 调用/nop-message-debezium git-clean/序列化边界断言）、D（文档四处）、E（Gates + 三工具 exit 0 + audit 自行复跑 `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS）全 PASS。Findings：F-1（Low）H-2 缺显式处置行——当场收口（design §4.1 补「被 H-1 取代」行 + Closure Gate 3 注记）；F-2/F-3 信息级无功能缺陷。
- **收口仪式**：Closure Gates 10/10 勾选（含 H-1..H-6 逐钩子处置：H-1 采纳/H-2 显式被取代/H-3·H-4·H-5 采纳/H-6 不采纳）+ Closure evidence 写入 → Plan Status `completed`（Completed: 2026-09-03）；roadmap item 20 → `done`（验收摘要 + audit session + Last updated 同步）。
- **工具门禁**：`check-plan-checklist --strict` exit 0 / `check-doc-links --strict` exit 0 / `scan-hollow-implementations --module nop-stream --severity high` exit 0（0 findings）/ `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS / `clean install -DskipTests` + typecheck/lint echo 通过。
- plan 无 `> Source Audits:` 前置项（roadmap-sourced plan），source audit 关闭步骤 N/A。
- Follow-up：REST 化 dry-run/conf-validate（条件触发）/ 平台 `IMessageService` health-check 提案 / 平台 `testCredential()` 真实实现（三项均 non-blocking，条件与归属在案）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-2 Phase 5——文档收口与回归）

- **owner doc 三处落盘**：`docs-for-ai/03-modules/nop-stream.md` 新增「提交前校验（conf-validate / dry-run）」节（命令模板/bean 三形态/连接器模式库级 API/层 1-3 语义/exit 0-1-2 契约/错误输出格式 `[FAIL|SKIP][layer N] ... option '<名>'`/副作用红线豁免表 + 逐族探测能力权威表）+「凭据引用与明文边界」节（`credential:{id}#{field}` 语法/引用驻留+引擎侧瞬态解密/fail-closed 三码/可达性=解密成功不依赖 testCredential/encrypt 等价物=nop-credential CRUD 平台面）；user-guide 连接器指引 +2 路由条目；connectors 目录新增「提交前探测能力（dry-run）」节（逐组件探测表 + 6 个测试锚点文件名）。
- **INDEX/source-anchors**：INDEX 新增提交前校验路由行；source-anchors STRM-052..055 四锚点（core 探测契约与驱动 / flow validate 校验核心 / runtime 入口族 / core 凭据支持 + CDC 接入）。
- **live 抽查发现并修复真实缺陷**：裸 JVM（无 CoreInitialization）跑 `StreamMaintenanceMain conf-validate file=/abs/path` → `virtual-file-system-not-initialized` NopException 逃逸 exit 1（契约=用法错误 2）——`resolveResource` VFS 分支捕获该 NopException 回落本地文件解释；回归测试 `TestStreamConfValidateCommand.uninitializedVfsDoesNotCrashAbsoluteLocalPathLookup`（VFS unregister/register 往返）。抽查三态与文档逐条一致后收口（doc-links 违界引用一处自包含化修复）。
- **工具门禁**：`check-doc-links --strict` exit 0 / `scan-hollow-implementations --module nop-stream --severity high` exit 0 / `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS / `./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` + typecheck/lint echo 通过。
- Doc-sync: `docs-for-ai/03-modules/nop-stream.md`、`nop-stream-user-guide.md`、`nop-stream-connectors.md`、`docs-for-ai/INDEX.md`、`04-reference/source-anchors.md`。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-2 Phase 4——凭据加密接入落地）

- **core 凭据支持（D4）**：新包 `io.nop.stream.core.credentials` + `StreamCredentialSupport`——`credential:{credentialId}#{field}` 语法（前缀识别 + parse 畸形 fail-fast）+ `resolve` 经平台唯一解密点 `ICredentialProvider.getCredentialData` 解密 + 三类 fail-closed typed 错误码（前次 run 已加的 `credential-ref-invalid`/`credential-provider-missing`/`credential-unresolved` 本次接线到实现）。core pom 唯一新增依赖 `nop-credential-api`（nop-bom 管版本；kms-vault 经接口透明）。
- **CDC 连接器接入（D4 必达点）**：`DebeziumCdcSourceFunction` 新增 provider 构造参数 + `transient volatile` 字段（Java 序列化路径不携带 provider——部署描述符/checkpoint 恢复后每 JVM 经 `setCredentialProvider` 重注入）+ `effectiveEngineConfig()` 引擎侧瞬态解密副本（`run()` 构造引擎前：有引用才拷贝（序列化往返保全部字段）→ databaseUser/databasePassword 解密明文仅存于局部副本与引擎实例；原 config 永持引用串；无引用=同实例零开销）+ `checkConnection` 凭据可达性（解密成功=可达；**不调用 testCredential()**——W2 恒失败桩会产生系统性假阴性，Javadoc 记录）。SPI 工厂新增 optional `credentialProvider` OBJECT 参数（程序化装配面）。**nop-message-debezium 零变更**（D4 边界裁定兑现：引用串对平台 POJO 透明）。
- **新增测试 16 用例**：core `TestStreamCredentialSupport`（7）+ debezium `TestDebeziumCredentialIntegration`（9——关键断言：引擎收到的 config password=明文 ∧ 原 config password=引用串（两条约束同时成立的机制自洽证明）；序列化字节含 `credential:mysql-prod#password` 不含明文 secret；反序列化后 provider null→checkConnection fail-closed→重注入恢复；run() 与 checkConnection 双路 fail-closed；dry-run 探测显式呈现凭据失败（Phase 3 清单回填））。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（945+ 用例全绿；一次 -T 1C 下 `TestMetricsExposureE2E.metricsReporterFileSinkWritesSnapshot` @TempDir 清理竞态单发 ERROR——隔离复跑×2 稳定通过，与本 phase 变更无关的既有 flake）。
- Doc-sync: 凭据引用语法/明文边界文档落 Phase 5 统一同步；设计契约已在 pre-submit-validation-design.md D4/D5。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-2 Phase 3——dry-run 连通性验证落地）

- **core 探测契约（D3-②③）**：`ConnectivityCheckable`（`io.nop.stream.core.connector`，仿 DrainableSource marker 先例的 additive 能力接口——Javadoc 钉定三契约：不启动数据路径/无残留红线/不可达必抛）+ `ConnectivityProbeOutcome`（PASS/FAIL/SKIP 三态）+ `StreamConnectivityProber`（分派序：能力接口 → FLIP-27 Source（H-1：createEnumerator+start(no-op 投递上下文)+close）→ TwoPhaseCommitSinkFunction 基契约（H-5：begin+rollback——第三方 2PC sink 兜底）→ 显式 SKIP；异常→FAIL 显式码 `nop.err.stream.connectivity-check-failed`，根因消息保留）。新错误码 `ERR_STREAM_CONNECTIVITY_NOT_SUPPORTED`。
- **五族探测实现**：`JdbcTwoPhaseCommitSink.checkConnection` = begin+幂等台账 DDL+rollback（台账表=豁免幂等对象、台账行=绝不产生的残留）；`FileTwoPhaseCommitSink.checkConnection` = 目录存在断言+begin+rollback；`BatchLoaderSourceFunction.checkConnection` = loaderProvider.setup+AutoCloseable 关闭（null loader 显式 FAIL）；`BatchConsumerSinkFunction.checkConnection` = 构造级 consumer 非空断言（构造即 setup 的天然探测点，不重复 setup）；`DebeziumCdcSourceFunction.checkConnection` = 参数级（connector name typed 校验；不拉引擎不连库——不可达 host 仍 PASS 的深度边界用例钉定 ⑤ 裁定）。Message 双族不实现（③ 显式 skip）。
- **层 3 接线**：flow `StreamConfValidator.probeEndpoints`（图构建成功后逐 source/sink 元素经 bean resolver 解析端点实例并探测；xpl 端点=显式 skip 命名元素）；`validateConnector` connect 分支经 `StreamConnectorCatalog.probeConnectivity`（additive：构造→探测→关闭，连接器模式层 3）；report 增 SKIP severity（exit 0 允许显式 skip——D7 契约）+ probeLog 渲染；runtime CLI `--connect`/`dry-run` 端到端（SKIP 项+probe log 输出断言）。
- **新增测试 24 用例**：flow `TestStreamConnectivityDryRun`（7，含接线验证：RecordingCheckable checkCount 断言/H-1 startCount 断言/第三方 2PC begin+rollback count 断言）+ connector `TestPreSubmitConnectivityProbe`（5：H-1 可达 PASS 输入不变/缺目录显式码 FAIL/file 2PC 无残留（输出目录空断言）/message 双族 skip）+ jdbc `TestJdbcPreSubmitConnectivityProbe`（3：台账行零+表存在豁免/重复探测无累积/池关闭后 FAIL 显式码）+ batch `TestBatchPreSubmitConnectivityProbe`（5）+ debezium `TestDebeziumPreSubmitConnectivityProbe`（3）+ E2E/CLI 3（S1 dry-run 提交前步骤：exit 0+4 sink PASS+cdc source PASS（ReplayableCdcSourceFunction extends DebeziumCdcSourceFunction 故参数级可探）+四台账表行数==0；注入死亡池 sink→exit 1+显式码+端点名；CLI --connect）。Phase 2 的 --connect not-yet-available 桩测试替换为真实行为断言。
- **排障事实**：`JdbcFactory.newJdbcTemplateFor` **急切连接**（newTransactionTemplate 即取连接探方言）——「不可达库」测试不能先建死模板：正确形态=活库建模板→`dataSource.close()`→后续探测连接全失败（tcp://127.0.0.1:1 死 URL 在模板构造期就抛）。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（全模块绿）。
- Doc-sync: 探测能力表/用法文档落 Phase 5 统一同步；设计契约已在 pre-submit-validation-design.md §4.1/§4.2。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-2 Phase 2——conf-validate 独立校验命令落地）

- **flow validate 包（校验核心，D1/D7）**：`StreamConfValidator`（XDSL 模式层 1 DslModelParser 按 stream.xdef 字段级校验 + 层 2 `StreamModelDslBuilder.build()` 完整图构建不 execute；连接器模式 = 描述符参数规格字段级校验（未知参数/缺失必填逐条含参数名）+ catalog 构造探测；层 3 未接线期 `--connect` 显式 not-yet-available 错误）+ `StreamConfValidationReport`（exit 0/1 契约 + render）+ `ValidationIssue`（layer/target/errorCode/paramName 五元组，选项名必含——P-REQ-14 验收对应物）。前次中断 run 遗留的三处编译缺陷修复：NopException 的 `getErrorCode()` 返回 String（非 ErrorCode）、`StreamException` 链式 `.param()` 返回基类需语句式赋值、flow 主代码缺 nop-xlang 编译域（层 1 解析 owner 职责——新增该依赖，测试域原本经 nop-codegen 传递）。
- **runtime 入口族（D1）**：`StreamConfValidateCommand`（file=<VFS 或本地路径> + `--connect`；用法错误含路径不可解析 → exit 2；程序化 resolver 重载供嵌入/E2E——D2 形态 1；`dry-run` 子命令 ≡ `conf-validate --connect`）；`StreamMaintenanceMain` 新增两子命令 + usage 契约（0=通过含显式 skip/1=失败/2=用法错误）；runtime pom 新增 flow 编译依赖（首个 core 外 stream 模块依赖，注释记录 D1 依据）。排障：macOS 绝对本地路径以 `/` 开头先命中 VFS 分支——resolveResource 改为 VFS exists 优先、否则回落本地文件、再否则用法错误。
- **D2 形态 2**：`BeanContainerFunctionResolver`（flow builder 包，包装显式装配的 `IBeanContainer`——nop-api-core 类型零新增依赖；instance API 无 tryGetBean，经 containsBean+getBean 组合实现）。
- **新增测试 24 用例**：flow `TestStreamConfValidator`（10：合法作业通过不启动/层 1 未知元素/层 2 bean 缺失断言选项名=bean 名/required-body/连接器模式合法/未知参数含参数名/缺失必填含参数名/未知类型/--connect 显式 not-implemented×2）+ `TestBeanContainerFunctionResolver`（4：standalone 容器解析/bean-not-found/bean-type-mismatch/null 拒绝）+ runtime `TestStreamConfValidateCommand`（7：用法错误×3 含路径不存在/合法 xpl 内联作业 exit 0/非法模型 exit 1/全局容器缺失 bean exit 1/usage 文档契约）+ fraud-example `TestConfValidatePreSubmitE2E`（3：**S1 拓扑经命令入口 + s1Resolver（D2 形态 1）解析真实 fraud-s1-cdc.stream.xml exit 0** / S2 同理 / 缺失 jdbcSinkGeo bean exit 1 且报告含 bean 名）。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（全模块绿）。
- Doc-sync: 用法文档裁定落 nop-stream.md + user-guide（Phase 5 统一同步，本 phase 记录裁定）；无 docs-for-ai 直接变更。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-2 Phase 1——item 20 七组设计裁定）

- **Phase 1 完成（纯决策，无代码变更）**：七组必答题全部裁定并落档新设计文档 `ai-dev/design/nop-stream/pre-submit-validation-design.md`（D1..D7 + 决策汇总表，每项含拒绝替代方案）：
  - **D1 入口面**：加入 `StreamMaintenanceMain` 入口族（runtime）子命令 `conf-validate`（层 1+2，`--connect` 可选接层 3）与 `dry-run`（≡ conf-validate --connect）；校验核心逻辑落 nop-stream-flow validate 包（模型/bean 解析 owner 模块），runtime 新增 flow 编译依赖（首个 core 外 stream 模块依赖，无环）；REST 不纳入（本地提交前工具，无远程提交需求——记录为 follow-up 候选）。拒绝：独立 main（违反 item 16 入口收敛）/ 校验逻辑放 runtime（复制模型知识）。
  - **D2 bean 来源三形态**：程序化 resolver 注入（库级 API，S1/S2 bean 可解析形态——E2E 用此形态）/ CLI `beans=<csv beans.xml 路径>` 显式 NopIoC 装配 / 缺省回落 GlobalBeanFunctionResolver；与 item 19 注册中心世界并列不互通（§8.3 衔接）。拒绝：仅全局容器（S1/S2 不可解析）/ 第四种 bean 声明面。
  - **D3 逐族探测语义**（消费 H-1..H-6）：① jdbc-2pc = beginTransaction+initializeLedgerTable+rollback 组合（台账行=残留绝不产生；台账表=幂等 DDL 预期对象显式豁免；拒绝仅 SELECT 1——无零 core 改动注入点）；② `ConnectivityCheckable` additive 能力接口采纳（core connector 包，仿 DrainableSource 先例）——jdbc-2pc/file-2pc/batch-loader/debezium-cdc 四族实现，未实现族显式「不支持探测」报告；③ message 族 = 显式 skip（IMessageService 无 health-check，send 探针违反红线）；④ H-6 builder 级回调不采纳（命令面本身不 execute，flow builder 零变更）；⑤ debezium = 构造/参数级 + 凭据解密可达，不真实连库（拒绝 run()+cancel() 拉真引擎）；⑥ 红线 = 不产生作业正常运行不会创建的对象（幂等 DDL 表/输出目录豁免；台账行/epoch 终文件/订阅位点/offset 写入=残留）。file source 走 H-1（createEnumerator+start no-op 上下文）。
  - **D4 凭据机制**：`credential:{credentialId}#{field}` 引用语法；**引用串驻留 Serializable 配置**（DebeziumConfig 序列化/checkpoint 路径携带引用，跨 JVM 恢复后可再解密——自洽）+ **解密只在引擎侧瞬态路径**（连接器启动引擎前瞬态解密副本，原配置与 checkpoint 永无明文）；provider 经构造参数注入（transient）；fail-closed（引用存在而 provider 缺失/凭据不存在 = typed 错误）；可达性 = 解密成功，不依赖 testCredential()（W2 桩）；**不变更 nop-message-debezium**（引用串对平台 POJO 透明，解密点在 connector-debezium = 边界内）；辅助落 core（新增 nop-credential-api 依赖——唯一新增依赖）；kms-vault 经 ICredentialProvider 透明。
  - **D5 encrypt 等价物**：= nop-credential 既有凭据写入/加密平台面（CRUD + cv1: 落库 + 平台唯一解密点）；拒绝 SeaTunnel EncryptConfigServlet 整文件加密（破坏 XDef 校验/Delta/diff）。
  - **D6 item 19 协调**：已落地 → conf-validate 提供连接器模式（注册中心类型名 + 描述符参数规格字段级校验 + 构造探测）；未落地则连接器模式显式不可用、XDSL 模式独立成立。
  - **D7 分层校验与错误契约**：层 1 = stream.xdef 字段级；层 2 = XDSL 模式完整图构建不 execute（bean 解析/类型匹配/FL-1/xpl 编译全触达）+ 连接器模式描述符参数校验；层 3 = --connect 逐族探测+凭据可达；错误输出 = 逐条（层号+元素/端点+错误码+指引，选项名必含）；exit 0=通过（含显式 skip）/1=校验失败/2=用法错误；层 3 未接线期 --connect 显式 not-yet-available 错误。
- **落档**：design README 索引更新（运维层 + 阅读顺序 15 + 扩展方向重编号 + header Updated）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（无新增错误）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-1 closure——item 19 done，P-REQ-28 验收达成）

- **Closure audit**（独立 general subagent fresh session `ses_f9b032081ffe88EUxlu5PIFFlD`，16/16 检查 PASS）：**CLOSURE-AUDIT: APPROVED**——Phase 1 裁定落档（§8 D1..D8 + tis OQ-2 写回 + OLAP 三态）、Phase 2 代码/注册/测试逐项核实（14 类型 + 8 工厂 + 5 beans.xml + 6 错误码 + 6 测试文件用例数与断言内容 + surefire 全绿）、Phase 3 接线真实性（E2E 全链路经 registry 非直接 new + Anti-Hollow 扫描 0 命中）、诚实性（Deferred 仅 OLAP 需求门控条目；Non-Goals 零越界——stream.xdef 与 StreamModelDslBuilder diff 为空）；3 Minor（D6 行内式拒绝替代/ file split source 诚实规则例外/jdbc proxy 有注释）均不阻塞。
- **收口仪式**：Closure Gates 11/11 勾选 + Closure evidence 写入 → Plan Status `completed`（Completed: 2026-09-03）；roadmap item 19 → `done`（附验收摘要 + audit session）+ Last updated 同步；OLAP 裁定不追加无需求 Follow-up（Deferred But Adjudicated 收口，Successor Required: no + revisit 触发点在案）。
- **工具门禁**：`check-plan-checklist --strict` exit 0 / `check-doc-links --strict` exit 0（31275 引用 0 错误）/ `scan-hollow-implementations --module nop-stream --severity high` exit 0 / `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS。
- plan 无 `> Source Audits:` 前置项（roadmap-sourced plan），source audit 关闭步骤 N/A。
- Follow-up：item 20 消费本注册中心/能力面（执行顺序下一项）；类型化 XDSL 声明与 item 20 字段规格一次设计（预案 §8.5）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-1 Phase 3——消费路径接线 + 能力矩阵同步）

- **预绑定消费者接线（Anti-Hollow 核心）**：`TestStreamConnectorRegistryE2E`（runtime，2 用例）——全链路：VFS 发现 5 beans 文件 → BeanContainerBuilder 容器 → catalog → registry.resolve → factory 构造端点 → `env.execute()` → 输出断言。用例 1：file split source（registry）→ batch-consumer sink（registry，batchSize=1）→ 4 行 exactly-once 收集（断言按 multiset——split 分配跨文件顺序非确定，排序断言在全量套件中翻车后修正）；用例 2：batch-loader source（registry，SourceFunction 路径）→ sleepy map（强制跨越 ≥2 个周期 checkpoint）→ file 2PC sink（registry）→ epoch 文件 + manifest 全量 exactly-once + 无 tmp 残留。
- **排障事实（写日志防复发）**：`env.enableCheckpointing()` 单独调用不生效——`StreamExecutionEnvironment.execute()` 仅在 `checkpointExecutorFactory != null` 时走 checkpoint 路径，需 `setCheckpointExecutorFactory(new CheckpointExecutorFactoryImpl())`（TestE2EWindowAggregateRestore / ScenarioTestSupport 同款）；首次版本漏装导致 storage 目录都不创建的静默无 checkpoint 运行。另：FLIP-27 `FileSource` × env 级周期 checkpoint 组合在仓库内无既有测试先例，e2e 2PC 用例改走 batch-loader（SourceFunction 路径，S2 java variant 同款已验证组合）。
- **能力矩阵同步（代码锚点 + 测试钉定机制）**：`docs-for-ai/03-modules/nop-stream-connectors.md` 新增「SPI 注册中心与类型名」节（8 类型名→工厂类→beans.xml 锚点表、注册资格集与不注册理由、单一事实源声明、catalog 装配/枚举/probe 用法示例、item 20 边界、类型化声明未引入说明）+ 能力矩阵 8 行组件名标注 SPI 类型名 + 锚点索引新增 SPI 行（core 注册机制 / 模块工厂 / runtime 发现+E2E 四层）；user-guide 连接器指引新增 SPI 条目；STRM-047..051 五锚点；INDEX connectors 路由行更新。doc-links 首跑 2 错（docs-for-ai 引用 ai-dev 路径违界）→ 两处改为自包含表述后 exit 0。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（全模块绿，e2e 3× 复跑稳定）；`node ai-dev/tools/check-doc-links.mjs --strict` exit 0。
- Doc-sync: `docs-for-ai/03-modules/nop-stream-connectors.md`（SPI 节 + 矩阵标注 + 锚点行）、`nop-stream-user-guide.md`（SPI 条目）、`04-reference/source-anchors.md`（STRM-047..051）、`INDEX.md`（connectors 路由行）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-1 Phase 2——SPI 注册中心落地与 8 组件注册）

- **core 新包 `io.nop.stream.core.connector.registry`**（14 类）：三工厂契约（`IStreamSourceFunctionFactory`/`IStreamSplitSourceFactory`/`IStreamSinkFactory` + 基契约 `IStreamConnectorFactory`）、`ConnectorCapabilityDescriptor`（不可变 + Builder + 方向/一致性构造校验）、`ConnectorParamDescriptor`、`StreamConnectorConfig`（requireXxx fail-fast / optional getXxx）、`StreamConnectorRegistry`（of/fromContainer 聚合 + 构建期校验：类型名/别名重复、typeName/aliases/方向一致性——发现并修复一个设计缺陷：factory.getAliases() 与 descriptor.getAliases() 漂移可能，注册期等值校验堵住）、`StreamConnectorCatalog`（工具入口：discoverConnectorBeansResources VFS 发现 + listConnectors/renderListing + probe 探测含单一事实源运行期校验）、枚举 4 个（Direction/Parallelism/RecoverySemantic/ParamKind）、`ConnectorProbeResult`。`NopStreamErrors` 新增 6 错误码（connector-type-not-found/direction-mismatch/duplicate-type/descriptor-invalid/param-required/capability-mismatch）+ 11 个 ARG 常量。
- **8 工厂 + 5 beans.xml**：`FileSourceConnectorFactory`/`FileTwoPhaseCommitSinkConnectorFactory`（connector 模块 file 包）、`MessageSourceConnectorFactory`/`MessageSinkConnectorFactory`（connector 模块）、`JdbcTwoPhaseCommitSinkConnectorFactory`（经 fluent builder 复用）、`DebeziumCdcSourceConnectorFactory`、`BatchLoaderSourceConnectorFactory`/`BatchConsumerSinkConnectorFactory`；注册载体 `_vfs/nop/stream/beans/connector-{file,message,jdbc,debezium,batch}.beans.xml`（无状态工厂 bean，零构造参数——全部构造输入经配置对象）。
- **新增测试 42 用例**：core `TestStreamConnectorRegistry`（13：枚举/别名优先级/重复/描述符三重校验/未知类型含注册清单/方向错配/必填参数/跨方向同名合法）、模块级 `TestFileMessageConnectorFactories`(11)/`TestJdbcConnectorFactory`(3, H2 真实模板)/`TestDebeziumConnectorFactory`(3)/`TestBatchConnectorFactories`(4)——均含实例级单一事实源断言（描述符值 ≡ 端点 getXxxConsistency()）、聚合级 `TestStreamConnectorRegistryDiscovery`(8：5 beans 文件 VFS 发现→容器装配→8 组件枚举/能力矩阵逐项断言/全部 probe 构造/未知类型全清单/方向错配/renderListing)。
- **偏差裁定（2 项）**：① beans.xml 按家族命名 5 件（base 模块贡献 file+message 两件）而非设计文本初稿的 4 件——§8.3 已回写；② 聚合发现测试落 **runtime**（原拟 fraud-example）：fraud-example 无 nop-ioc 依赖，引入会激活 IocCoreInitializer 改变其全部测试的 full-init 行为（runtime pom surefire 注释明示该风险 + init cap 基建已存在）；runtime 已有 connector/connector-jdbc main 依赖 + nop-ioc test-scope，补 debezium/batch test 依赖即成聚合宿主。
- **验证**：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（全模块绿，含 fraud-example S1/S2 回归 = 向后兼容证据：注册中心与 GlobalBeanFunctionResolver 零交互）；`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` + typecheck/lint echo 通过。
- Doc-sync: connector-design.md §8.3（beans 文件命名细化）；无 docs-for-ai 变更（Phase 3 处理矩阵同步）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0830-1 Phase 1——item 19 裁定与设计基线）

- **Phase 1 完成（纯决策，无代码变更）**：四项必答题全部裁定并落档 `ai-dev/design/nop-stream/connector-design.md` §8（D1..D8，每项含拒绝替代方案）：
  - **D1 工厂形态**：source/split-source/sink 三工厂契约（FLIP-27 `Source` 与 `SourceFunction` 两执行契约按 §4.3 并存事实分立，拒绝 Object 单接口/双创建方法/Source→SourceFunction 适配器三替代）；注册资格集 = 8 端点组件（FileSource/FileTwoPhaseCommitSink/MessageSourceFunction/MessageSinkFunction/JdbcTwoPhaseCommitSink/DebeziumCdcSourceFunction/BatchLoaderSourceFunction/BatchConsumerSinkFunction），Reader/Builder/split 机制件/StreamConnectors 显式不注册（理由随档）。
  - **D2 类型名**：方向作用域命名空间 (direction, typeName)，kebab-case 家族名（file/message/debezium-cdc/batch-loader × file/message/jdbc-2pc/batch-consumer）；别名机制预留、本期 8 工厂全空（测试以带别名测试工厂钉定解析优先级，不留未测分支）。
  - **D3 载体与聚合**：各连接器模块 `_vfs/nop/stream/beans/connector-*.beans.xml` 无状态工厂 bean（NopIoC 承载、无注解扫描、不匹配 `app-*.beans.xml` 模式故不进全局容器）；注册中心从任意容器按类型聚合 + 构建期冲突/一致性校验；工厂无状态裁定（全部构造输入经配置对象：标量参数 + 程序化 OBJECT 参数）；与既有 `BeanFunctionResolver` 世界并列不合并（fraud-example S1/S2 程序化注册不受影响）。关键依赖事实：`nop-stream-core` 仅依赖 nop-commons+nop-core（nop-core 不含 nop-xlang），catalog 容器装配由调用方以既有 BeanContainerBuilder 模式完成——core 不新增 nop-ioc 依赖。
  - **D4 能力描述符**：字段集与 `nop-stream-connectors.md` 矩阵列对齐（方向/交付语义枚举/并行度枚举含 PLANNING_GATE_PARALLELISM_1/恢复语义枚举摘要/参数规格）；单一事实源 = 交付语义测试钉定（描述符值 ≡ 端点实例 getXxxConsistency()）+ 2PC 门禁 instanceof 同键双向蕴含；核对口径 = 结构化相等列（方向/交付语义/并行度）+ 文档专属列（恢复语义 prose 细节/依据/测试锚点）；文档同步机制 = 代码锚点标注 + 发现测试钉定复核清单（拒绝生成式，列长期优化候选）。
  - **D5 XDSL 消费路径**：**本期不引入类型化连接器声明**（8 端点配置面异构 + item 20 conf-validate 应与字段规格一次设计 + maxParallelism 运行时消费属 item 29）；FL-1 全部拒绝面维持不变；预绑定运行时消费者 = `StreamConnectorCatalog`（清单枚举 + probe 探测，落 core，显式不做字段级 conf 校验守 item 20 边界）；CLI 子命令形态归 item 20 一并裁定；类型化声明引入预案（bean+类型名冲突 fail-fast 等）记录不实施。
  - **D6 未知类型名**：fail-fast typed error（含类型名/方向/已注册清单），方向错配独立错误码，禁静默回落 bean 路径。
  - **D7 OLAP 最小集三态**（P-REQ-28 验收必答）：ClickHouse/Doris/StarRocks/Hive = **defer**（JDBC 可达边际成本低但零需求证据 + exactly-once 声明需 per-DB 验证矩阵；revisit 触发点 = OLAP 目标需求进入 Phase S）；Paimon/Iceberg = **exclude**（湖表格式需新依赖面 + Flink 生态耦合，独立 mission 素材）；**本期 go-minimal 集为空**（分期口径满足 P-REQ-28 验收）；roadmap 不追加无需求 Follow-up。
  - **D8 tis OQ-2**：部分采纳——配置面适用 Delta（含连接器工厂 beans.xml 可 Delta 定制=定制既有组件装配），本体分发不适用（新端点=新代码+依赖 jar，须走 SPI 注册）；跳过 Jenkins 插件运行时成立，「市场」产品形态不成立亦不采纳。
- **写回**：tis 报告（`2026-08-14d`）OQ-2 条目勾选 + 裁定落点注记，报告 Status 保持 open（OQ-1/OQ-3 未决如实标注）；connector-design.md Revised + design README 索引更新。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0。
- Doc-sync: `ai-dev/design/nop-stream/connector-design.md` §8 + `ai-dev/design/nop-stream/README.md` 索引 + tis 报告 OQ-2 写回；docs-for-ai 侧无变更（Phase 3 处理矩阵同步）。

### 2026-09-03（mission nop-stream-productization DRAFT_PLANS：起草三份 plan——items 19 / 20 / 21+24+30 → active）

- **选型**：按 roadmap 调度规则（Follow-up 与既有 item 同权、取第一个 todo、不跳过不重排）取 items 19、20、21（21 与 24/30 审计显式要求联动执行避免双倍改动，合并为单一载体 plan）；items 22/23/25—32 留后续轮次，未试图一次覆盖。前序 plan deferred 项核查：最终验收（item 18）的 pending-followup×4 中 P-REQ-28→item 19、P-REQ-13/14→item 20 均被本轮承接；defer revisit×4（P-REQ-9/17/21/15-K8s）触发条件均未满足，维持。
- **起草**（均为 `ai-dev/plans/nop-stream-productization/` 下）：
  - `2026-09-03-0830-1-connector-ecosystem-spi-registry.md`（item 19 / P-REQ-28：SPI 注册中心 NopIoC 承载 + 能力矩阵机制 + OLAP 最小集三态裁定 + tis OQ-2 裁定）；
  - `2026-09-03-0830-2-pre-submit-validation-productization.md`（item 20 / P-REQ-13/14：dry-run 消费 H-1..H-6 钩子清单 + 凭据接入 nop-credential + conf-validate；执行顺序在 item 19 后，两态协调裁定防跳序）；
  - `2026-09-03-0830-3-state-serde-dedup-convergence.md`（items 21+24+30 联动：core/rocksdb SerDe 与状态后端克隆收敛 + item 24 守卫同点落地；格式零变更（fixture 读兼容+写稳定双向证明）、位点级「裁定保留」出口、rocksdb 守卫对等性裁定）。
- **审查迭代**（guide「Before & After Drafting」流程，每 plan 两轮独立子 agent 对抗性审查，均含想象性分析）：
  - 第一轮（3 个 fresh general session）各发现 1 Blocker + 多 Major：plan 1 Phase 2/3 接线验证时序矛盾 + 注册资格集缺失；plan 2 conf-validate bean 解析来源未裁定（S1/S2 bean 仅 test 侧程序化注册，端到端按字面不可满足）+ testCredential() 系 W2 恒失败桩被误当可用能力 + encrypt 验收面无承接；plan 3 fixture 只证读兼容 + 无「不可收敛位点」出口 + rocksdb 守卫对等性（W-R1 理由随 core 收紧失效）——全部按审查意见修复。
  - 第二轮（3 个新 fresh session）三 plan 均**无 Blocker**；各残留 1 Major（plan 1 sink 侧 FL-1 拒绝面遗漏 inputType；plan 2 Phase 3 「构造前解密」措辞回归；plan 3 守卫对等落地分支无执行宿主）+ 少量 Minor——均按审查处方修复后激活。
- **写回**：三 plan `Plan Status: active`；roadmap items 19/20/21/24/30 → `planned`（附 plan 路径与审查记录）+ Last updated 同步。
- 无代码变更（纯计划起草）；本轮未跑构建。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0617-2 closure——item 18 done + M4 done）

- **Phase 4 + closure 完成**：对抗复核（fresh session `ses_f9b630033ffep9Nx2Z3aRXVbaM`，research-only）**APPROVED 无 Blocker/Major**——抽查 10 条 P-REQ 判定（全部 P0 + 16/22-25/26/27/28）+ defer×2 + 治理×3 + 诚实性（对 P-REQ 权威清单 28 条验收行 rg 扫描证实零「持续运行稳定性」条目）+ 一致性，全部 PASS；1 Minor（测试时间溯源措辞）当场修正。closure audit（另一 fresh session `ses_f9b5e4d29ffe0TPQYDZc3h168J`）**CLOSURE-APPROVED**（6/6 检查 PASS，无 Blocking，3 Advisory 均在仪式中处置）。
- **报告定稿**：`Audit Status: resolved`（§5 复核证据 + §6 写回记录写入；章节编号 Advisory 修正）。
- **roadmap 写回（closure 仪式动作，先例 item 8）**：item 18 → `done`（附验收摘要 + 双 audit session）+ **M4 → `done`**（派生态：M2+M3+16—18 全 done）+ Last updated 同步。
- **plan 收口**：Closure Gates 7/7 + Phase 4 4/4 勾选 + Closure evidence 写入；`check-plan-checklist --strict` exit 0；Plan Status → completed。
- **mission 层面**：nop-stream-productization roadmap 主线 items 1—18 全 done、M1—M4 全 done；Follow-up backlog items 19—32（14 项 todo）交由 mission 引擎继续调度。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0617-2 Phase 2/3——defer revisit + 治理核对 + M4 判定）

- **Phase 2 完成**：defer revisit×4 全部裁定**维持**（P-REQ-9：无用户需求证据 + REST/指标/健康面前置面已 met；P-REQ-17：items 13/14/15 全记录零声明式需求证据 + S-2 修复后 P1-09-01 per-path 语义无例外；P-REQ-21：schemaVersion 恒 1 + 无发布版本，与 item 25 联动关系显式记录；P-REQ-15/K8s：两条 defer 条件均未触发 + 应用层基线 fresh（item 8 fencing 复核 + item 14 矩阵 + item 15 租约正向证据））；exclude×5 复核无回潮（含 standby 重启路径核查：CHAOS-1 恢复失败根因 item 28 继发，不构成热备缺失证据）。
- **Phase 3 完成**：items 19—32 逐行治理核对 14/14（来源标注全指向真实 plan/audit、状态与 live 一致、无无主缺陷）；D-GAP 落地链（go×4 载体合法终态、exclude 无回潮、D-DRIFT-1 核销、D-DRIFT-2 pending 有归属、F-2 已消解）；**诚实性检查**（item 28/31 三要素呈现：已知缺陷 + 有归属 + 阻塞持续运行稳定性基线但不构成 P-REQ 未达成——P-REQ-1..28 无任何条目以持续运行稳定性为验收标准，反向核查无条目需降级）；**P0 全 met、M4 判定：成立**。
- Phase 1 全量测试复验：`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（08:10，EXIT=0）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0617-2 item 18 最终验收审计——Phase 1 P-REQ 全清单 live 复核）

- 前置校验 fail-fast 通过：roadmap live 核对 item 17 `done`、item 18 为当前 item（硬依赖满足）。
- **Phase 1 完成**：验收报告草稿落 `ai-dev/audits/nop-stream-productization/2026-09-03-0807-final-acceptance-nop-stream-productization.md`——28 条 P-REQ 三态判定齐备：**met×19 / pending-followup×4（13/14→item 20、20→item 25、28→item 19，全 P1 有归属）/ defer 维持×3（9/17/21）/ adjudicated-excluded×2（18/19）**；无悬空部分满足态。
- **P0 五条可复现验证**（焦点测试 08:02 运行全绿）：P-REQ-1 `TestEngineAndTaskNodeMetrics` 4/4 + 五层指标名族 live（engine×7/task×4/io×3/operator×3/state）；P-REQ-3 `TestMetricsExposureE2E` 2/2 + `TestStreamOpsHttpServer` 6/6（TextFormat 0.0.4 + OpenMetrics 协商）；P-REQ-5 `TestOpsRestLifecycleE2E` 4/4 + REST 路由 :99-214；P-REQ-7 `TestJobHealthStateMachine` 8/8 + gated `TestMultiJvmHealthStateAndAlerts`；P-REQ-15 D-GAP §2.2 四行子裁定 + README:7 诚实声明 + RuntimeTopology 9 位置退役注记复核。
- **本 plan 必答题落定**：P-REQ-26 终态 = **met**（`TestStateSchemaCompatibility` 两类不兼容场景断言 `ERR_STREAM_STATE_SCHEMA_MISMATCH` + stateName + checksum 差异参数——行为级核验为本 plan 首次作出）；P-REQ-27 = **met**（功能逐项 ✓/✗ 对照表由本 plan 补做：`MaxParallelismReshardMigration`（476 行全读）vs Spark `OfflineStateRepartitionRunner`（305 行全读），14 能力项对照 + 3 缺口 watch-only 裁定 + 载体偏离留痕）。
- item 17 交付物 live 重验：user-guide 触发语义映射表 / connectors 目录 10 组件矩阵 / cdc-cookbook 四节 / migration-guide 两轴+版本政策 / quickstart 3 拓扑 + verify.sh——P-REQ-16/22..25 全 met（不采信 closure note，全部回看 live repo）。
- 焦点测试命令与 surefire 输出记录于报告 §1.2；纯审计 plan 无代码变更。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0617-1 closure——item 17 done）

- Plan `2026-09-03-0617-1`（item 17 文档产品化）closure：独立 closure audit（general subagent fresh session `ses_f9b7c0d2dffeD4F4S8PTH3aGRq`，research-only）**CLOSURE-APPROVED** 无 Blocker/Major——五 Phase 全 PASS（触发语义表与 live 类 100% 一致 + 12 测试锚点存在、连接器 3 项语义抽查命中、cookbook/迁移指南锚点真实、脚手架 surefire 3 测试 0F/0E 执行证据在档、三工具门禁 exit 0）+ 文档-代码一致性抽查 5/5 PASS + 7 追加锚点全存在 + 诚实性检查通过（Deferred 无降级、无「语义待证」待路由项）。
- 收口写回：Closure Gates 8/8 勾选 + Closure evidence 写入 plan；`check-plan-checklist --strict` exit 0；Plan Status → completed；roadmap item 17 → done（附交付摘要 + audit session）+ Last updated 同步；item 18 硬依赖满足（保持 planned 可调度）。
- 遗留记录（非缺陷）：closure audit Note ×2——plan Current Baseline「11 类」口径与 live 具体类计数的表述差异（用户指南表穷尽正确，无用户面影响）；audit prompt 中 Triggerable 路径笔误（doc 所写为正确路径）。
- Follow-up 候选登记：「产品级 main-scope 启动入口」（D1b 选项 b 附带）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0617-1 Phase 5——INDEX/锚点同步与门禁收口）

- Plan `2026-09-03-0617-1` Phase 5 完成：INDEX 快速路由 5 行已覆盖全页族 + 快速起步（用户指南/连接器目录/CDC cookbook/迁移指南/quickstart README，≤1 跳可达）；owner doc `nop-stream.md` 头部补用户文档族导航（运维 vs 使用分工）；source-anchors 补 `STRM-046`（脚手架入口）；文档页本身不设锚点裁定（INDEX 路由可达，锚点面向代码而非文档）。
- **门禁复跑与既有缺陷修复**：`check-nop-stream-invariants.mjs` 复跑发现 **9 处 V3 stale wiring pin**（既有缺陷：item 16 指标提交 `a9fe996cf9`/`b89ba71b5e` 移动了 StreamTaskInvokable/GraphModelCheckpointExecutor 行号但未 re-pin 注册表；git stash 对照确认与本 plan 无关）。按工具自身指引「sync the registry」机械 re-pin：502/503/248/286/305/311/466/743/475 → 529/530/259/297/316/322/493/751/502（9 处调用点逐一 grep 核实仍存在，语义零变更，changelog 按注册表惯例追加）→ invariants exit 0（修复 diff 10 行）。
- 全量门禁：`check-doc-links --strict` exit 0；`scan-hollow --module nop-stream --severity high` exit 0；`scan-hollow --module nop-stream/quickstart` exit 0；`./mvnw test -pl nop-stream -am -T 1C` 全绿（Phase 4 已复验，Phase 5 无代码变更）。
- Doc-sync: INDEX/source-anchors/owner-doc 三处更新即本 Phase 交付物。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0617-1 Phase 4——快速起步脚手架）

- Plan `2026-09-03-0617-1` Phase 4 完成：脚手架落 `nop-stream/quickstart/`（`generate.sh` + `verify.sh` + `README.md` + `template/`）——3 入门拓扑（Topology1 最小链路 source→map→filter→sink（Java API）；topology2-window-aggregation.stream.xml（keyBy+2s 事件时间窗口+keyed ValueState 富化+声明式 checkpoint，XDSL）；topology3-cep-pattern.stream.xml（30s 内相邻两笔>1000 CEP，XDSL））。
- **Decision D2（脚手架形态）**：选项 (b) 等价模板目录 + 复制/替换生成脚本。否决 (a) maven archetype：archetype 模块必须加入 nop-stream reactor（本 plan 目标含「不破坏 reactor」）；local catalog + 批处理 `archetype:generate` 链路比 copy+substitute 多一层不确定性；对单产品入门脚手架无增量价值。**无新 maven 模块**（纯模板目录，reactor `<modules>` 未动）。
- **D2 对照 item 11 审计 §2.4 准则表结论**：从零跑通 ✓（生成工程仅依赖本地仓库 nop-stream 构件，3 拓扑自包含断言）；依赖配置透明 ✓（生成 pom 显式 4 依赖：flow/runtime/xlang/junit）；README 质量 ✓；**两扇正门 ✓**（拓扑 1 Java DataStream API + 拓扑 2/3 XDSL `.stream.xml`——审计时「全缺」的最后一项闭环）；核心语义（keyBy/窗口聚合/keyed state/CEP/checkpoint 声明）✓。入门 tier 与 items 12/13 已消化的 fraud-example 高级场景分层：3 拓扑均为自包含最小教学形态，不复制 S1/S2。
- **拓扑 ③ 二选一裁定**：CEP（非 CDC）——CDC 简单模式需 debezium engine + 真实数据库，超出入门 tier 的自包含要求；CEP 走 flow 产品路径（`<patterns>` + 内联 xpl 谓词 + `CepPatternBuilder`）。
- **端到端验证（执行记录）**：`nop-stream/quickstart/verify.sh` → 本地构件检测（已有）→ 生成 `_tmp/quickstart-verify`（com.example:quickstart-verify）→ `./mvnw -f pom.xml test` → **Tests run: 3, Failures: 0, Errors: 0, BUILD SUCCESS**（1.7s）。
- **fail-fast 验证**：generate.sh 目标已存在→exit 1；缺参数/未知选项→exit 2；残留占位符→exit 1（grep 校验）；verify.sh `set -euo pipefail`；生成 pom surefire `forkedProcessTimeoutInSeconds=300`（拓扑失控显式超时失败而非挂起）。
- **脚本自检裁定（Test-Mandated Feature Rule）**：verify.sh 本身即测试载体（驱动生成工程 3 拓扑测试断言端到端跑通）+ generate.sh 占位符残留校验为内建自检，不另立 repo 内 JUnit（记录性裁定）。
- **调试过程沉淀的接线事实**（已在脚手架注释/文档体现）：keyed state 须走 checkpoint 执行路径（`setCheckpointExecutorFactory(new CheckpointExecutorFactoryImpl())` + `IStateBackend` 显式设置——LOCAL 直执路径不装配 keyed 后端）；UDF keyed 状态须惰性获取（open() 先于 keyed store 注入）；`watermarkInterval=0` 逐事件推进水位是有界演示拓扑的确定性闭合方式；有界运行 EOS 发射 MAX_WATERMARK（全部 in-flight 窗口闭合——拓扑 2 断言含泵行）；默认 STRICT_EXACTLY_ONCE 对内联 source/sink fail-fast（拓扑 1 显式声明 AT_LEAST_ONCE = 语义组合最小教学例）。
- owner-doc：INDEX.md 快速路由 +5 行（用户指南/连接器/CDC/迁移/快速起步）；source-anchors 补 `STRM-046`（脚手架入口）。
- 验证：`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` + `./mvnw test -pl nop-stream -am -T 1C` 全绿（reactor 未受影响）；doc-links exit 0。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 0617-1 Phases 1—3——用户文档族落地）

- Plan `2026-09-03-0617-1`（item 17 文档产品化）Phases 1—3 完成：`docs-for-ai/03-modules/` 新增四页用户文档族——`nop-stream-user-guide.md`（DataStream API/XDSL/连接器指引/分布式部署 + P-REQ-16 触发语义映射表）、`nop-stream-connectors.md`（P-REQ-22 连接器能力矩阵）、`nop-stream-cdc-cookbook.md`（P-REQ-23 四节）、`nop-stream-migration-guide.md`（P-REQ-24 两轴+版本策略）。
- **Decision D1（页族布局）**：多页族 `nop-stream-<topic>.md`（user-guide/connectors/cdc-cookbook/migration-guide 四页 + Phase 4 脚手架 README 承载快速起步）；否决单页方案（四主题体量 + 独立可路由性）。INDEX 路由行随 Phase 5 统一落。
- **Decision D1b（test-scope 入口类引用）**：选项 (b)——用户指南「入口类现状」节如实标注 `JobCoordinatorMain`/`TaskManagerMain` 位于 runtime test scope（test-jar 导出供 gated 测试消费），「产品级 main-scope 启动入口」登记为 Follow-up 候选；main-scope 运维面（OpsJobManager/StreamOpsHttpServer/StreamMaintenanceMain）不受限的区分已写明。
- **D-DRIFT-1 修正（9 处全量）**：`rg -n "RuntimeTopology"` 全量清单逐位置处置——README:5（管线叙述改四层+退役注记）、README:7（删除概念阶段句）、`00-vision.md:132`（五层管线+退役注记）、`01-architecture-baseline.md` :13（六→五阶段+注记）/:21（模块树删词）/:33（职责表改 ClusterRegistry 承担+注记）/:100（视角表 5 阶段+注记）/:117（管线图删行，补 :119 退役说明块）/:127（RuntimeTopology 表行删除）。修正后残留命中全部带「概念已退役（2026-09-01 D-GAP 裁定）」注记。
- **P-REQ-16 触发语义映射表**：消费 item 7 审计 §2.1 ② 证据——窗口级 Trigger/Evictor 家族表（10 triggers 文件 + 3 evictors + Triggerable，11 个专属单测名逐一 live 核对存在）+ assigner/countWindow 接线矩阵 8 行 + 作业级等价物三组（CheckpointConfig 20 字段 live 全量、processing-time timer、DRAIN truncation）+ Spark Triggers/Flink/SeaTunnel 对照表（含 by-design 结论：连续流 vs 微批）。
- **P-REQ-22 语义标注 live 核对锚点**（Phase 2，逐行与代码核对，非抄写）：FileSource 双通道状态（`SourceReaderOperator.READER_SPLITS_KEY="source-reader-splits"` :68 + enumerator manifest 通道）；FileTwoPhaseCommitSink/JdbcTwoPhaseCommitSink TWO_PHASE_COMMIT + saveState-first + 台账/manifest 幂等守卫（`TestFileTwoPhaseCommitSink`/`TestJdbcTwoPhaseCommitSinkDeep` 系列）；MessageSource/SinkFunction 声明 AT_LEAST_ONCE 但适配器无 ack/offset 逻辑、实际语义后端承载（Kafka commitSync=at-least-once；LocalMessageService 无订阅者丢弃=best-effort）——如实标注非「语义待证」；2PC P=1 门禁（`NopStreamErrors.java:439` 错误码 + `TestStreamGraphGenerator` 三测）；Debezium REPLAYABLE + `cdc-offsets` key + NopStreamOffsetBackingStore static registry 桥接（`TestDebeziumCdcCheckpoint` offset round-trip ×3 + kill-recover 零重复）；BatchLoader `SOURCE_OFFSET_KEY="source-offset"` :49 + seek 仅置计数器的 nuance 如实写明；BatchConsumer IDEMPOTENT + 失败保留缓冲重试。
- **Decision D3（cookbook 第四节）**：按 plan 预选执行——第四节 = 「故障排查与全新重跑」（reset-state 命令 + 拒绝语义 + 常见故障表 6 行），未改选。cookbook ≥2 live 锚点落地：offset round-trip 三项 + kill-recover 零重复 + fresh-start 不吃陈旧 offset（`TestDebeziumCdcCheckpoint` 具体方法名入文）+ schema 迁移双后端全链（`TestStateMigrationEndToEnd` 4 方法名入文）。
- P-REQ-24 迁移指南：三版本轴（信封 `formatVersion=2`/RocksDB `keyLayoutVersion=2`/指纹 `schemaVersion=1` 前瞻）+ XDSL 轴（Delta 继承/配置派生/bean id 兼容约束）+ 状态轴（reshard 命令族 + StateMigrationFunction 注册语义 + accumulator/CEP `__java_bytes__` 边界）+ 版本策略声明（无已发布版本→前瞻性政策；不承诺未落地机制——manifest 级 stateFormatVersion 显式标注未落地；首次版本递增触发 P-REQ-21 revisit）。
- docs-for-ai 边界适配：文档内不得引用 `ai-dev/` 路径（link checker BOUNDARY error）——触发语义表证据源改为描述性表述，入口类 Follow-up 候选去掉 roadmap 路径引用。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（四新页 + 全仓 2692 文件扫描 0 error）；代码级声称抽查全部命中（错误码行号/state key 常量/22 个测试类名 live 存在）；`./mvnw test -pl nop-stream -am -T 1C` 全绿（纯文档变更，回归确认无破坏）。
- Doc-sync: `docs-for-ai/03-modules/` 四新页即交付物；INDEX/source-anchors 同步留 Phase 5（plan 结构如此）。

### 2026-09-03（mission nop-stream-productization DRAFT_PLANS：items 17/18 计划起草并两轮审查后 active）

- 起草两份 plan（调度取首个 todo item 17 + 其硬依赖后继 item 18；deferred 项核对——P-REQ-9/21 defer revisit 均显式归属 item 18 处理，无需另立 plan）：
  - `ai-dev/plans/nop-stream-productization/2026-09-03-0617-1-docs-productization.md`（item 17 文档产品化）：P-REQ-22 连接器目录 + P-REQ-23 CDC cookbook（第四节 = D3 裁定）+ P-REQ-24 迁移指南首版 + P-REQ-25 快速起步脚手架（D2 archetype vs 模板裁定，纳入 item 11 审计 §2.4 可用性准则含「两扇正门」要求）+ P-REQ-16 触发语义映射表（消费 item 7 审计 §2.1 ② 证据）+ D-DRIFT-1 收口（rg 全量权威，含 README:7）。
  - `ai-dev/plans/nop-stream-productization/2026-09-03-0617-2-final-acceptance-audit.md`（item 18 最终验收审计）：P-REQ-1..28 逐条 live 核验（P0×5 可复现命令 + met 主张一律 live 复核）+ defer 全量 revisit（P-REQ-9/15-K8s/17/21）+ items 19—32 治理核对 + item 28/31 诚实性呈现 + M4 判定 + 报告落 `ai-dev/audits/nop-stream-productization/`（`> Audit Status: resolved` 防引擎误计 open）。Phase 1 含 fail-fast 前置（item 17 必须 done）。
- 独立对抗性审查两轮（每 plan 各两个 fresh session general subagent）：首轮发现并修复 Blocker×2 + Major×4——**plan 1**：`MaxParallelityReshardMigration` 拼写错误（live 为 Parallelism，D-GAP 笔误被继承，会流入用户文档）、test-scope 入口类（JobCoordinatorMain/TaskManagerMain）引用无裁定（新增 D1b）、item 11 审计 §2.4 输入未溯源（补引 + D2 纳入准则表）；**plan 2**：Current Baseline 虚假陈述「P-REQ-26/27 已由 item 11 收口」（live 核实：RK-1/RK-2 为 rocksdb native-handle 修复、P-REQ-27 能力对照表从未产出且无归属——改写为必答题 + Phase 1/3 处置路径）、缺错序执行防护（加 fail-fast 前置）、Phase 4 roadmap 写回与 closure audit 次序结（对齐 item 8 先例：closure auditor 核准后当场写回）。次轮复审：两 plan 均达可执行状态（修复全落地、无新 Blocker/Major），余 Minor（§2.2→§2.1 节号、STRM 锚点措辞、笔误注记补 §3.2）已就地修正。
- 收口写回：两 plan → `active`；roadmap items 17/18 → `planned`（附 plan 路径）+ Last updated 同步；doc-links --strict exit 0 复验。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-2 closure——item 15 done）

- Plan `2026-09-02-2216-2`（item 15）closure：独立 closure audit（general subagent fresh session `ses_f9bd70681ffeIptxAnIo3efADX`，research-only）**CLOSURE-APPROVED** 无 Blocker——七区核验全 PASS（P1 报告/P2 代码+29 单测/P3+P4 live 产物逐数字核对精确匹配/P5 文档/gates/anti-hollow/honesty）。
- Minor ×3 记录性处理：单测计数 32→29 修正（plan+roadmap）；roadmap 预写 audit 结论在 Closure 填入后一致化（教训：audit 结论须在 evidence 写入后再写 roadmap）；doc-links 1 warning（非 error，exit 0）。
- 收口写回：Plan Status → completed（62/62 checklist 全勾 + Closure evidence 含逐格数字核对）；roadmap item 15 → done；plan-checklist 复验 exit 0。
- Follow-up 交接：item 31（item 28 修复范围扩展——解锁后按原矩阵参数重跑即得稳定性基线升级）+ item 32（观察面上收）；此外 no remaining plan-owned work。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-2 Phase 5——报告收口与缺陷路由）

- Plan `2026-09-02-2216-2`（item 15）Phase 5 完成：演练报告收口（Status → resolved + Conclusion；6 格对账表：1 fail（CHAOS-1，根因 item 28 继发）+ 5 triggered-known-defect(item 28)；「确认无异常」观察项显式记录：HA 租约 failover / 50ms 档 C3 语义 / retained 有界 / fencing cap 耗尽前严格递增）。
- 缺陷路由（roadmap 自进化，末尾追加 + todo + 来源标注）：**Follow-up 31**（item 28 证据锐化与修复范围扩展：阈值 ~800—1000 条、无回压传导、recovery cap 耗尽继发、全对订阅残留——修复须覆盖通道收敛+队列满语义+stall 恢复预算区分）、**Follow-up 32**（观察面缺口：TM 指标上报通道 + 队列水位直测 gauge）。
- runbook §7 证据更新：item 28 条目升级为「已定量触发」（签名/阈值/继发失效/复现锚点）+ Stage-31 再次核验未触发 + 背压条目 BP-1 量化补记；roadmap item 15 → done + Last updated 同步。
- 验证：`./mvnw test -pl nop-stream -am -T 1C` 全绿（十模块 0 failures，含 32 个新装置单测默认态）；doc-links exit 0。
- 核心结论（产品化视角）：分布式持续运行稳定性基线被 item 28 阻塞（>10³ 记录场景不可承诺）；短时基线（C0—C3 ≤~500 记录）不受影响；控制面租约 failover 与低档背压健康。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-2 Phase 4——chaos + backpressure 三格全参数执行）

- Plan `2026-09-02-2216-2`（item 15）Phase 4 完成：CHAOS-1/CHAOS-2/BP-1 全参数执行留档（runId `1788383926464-1`/`1788385016277-1`/`1788386243853-1`）。
- CHAOS-1（8 轮随机 kill TM + 轮 7—8 SIGSTOP/SIGCONT 分区等价）：轮 1—2 正常恢复（fencing 1→2→4）；轮 3—8 no-rotation——机制揭示：jam 诱发 taskStall 自动 global recovery ×3 耗尽 recovery cap(=3)，此后真实 kill/租约到期均无法恢复（控制面继发失效，归因 item 28 修复范围须覆盖 stall 恢复预算 vs 真实故障区分）。
- CHAOS-2（HA JC kill ×2）：**租约 failover 完全正常**（leaderEpoch 1→2→3 严格递增 ×2 轮，独立于数据面 jam——正向证据）；failover 后 task fencing 未轮转（新 leader 未重发 assignment）；终态不收敛（jam）。
- BP-1（档位 50/200/500ms）：50ms 档 60s 窗口 checkpoint 推进 ×18（C3 无死锁语义在低档成立）；200/500 档 ×0（累计流量跨过 jam 阈值）——背压非死锁触发器、累计流量才是。
- 驱动装置就地加固 ×3（记录不中断原则）：kill 轮次 fencing 断言改「逐轮记录 + 系列级判定」（round 3 不再吞掉轮 4—8 证据）、HA 轮换等待不再误断言 coordinator-0 存活、BP 档位断言改逐档记录 + 循环后判定（summary 不再丢失档位序列）。全部按原参数重跑闭环。
- 结局分类：CHAOS-1 fail→item 28 根因（控制面继发）；CHAOS-2/BP-1 triggered-known-defect(item 28)；分区裁定项逐条对账落报告（可模拟 ✓ 执行 / 不可模拟 ✗ 显式 / 租约代理 ✓）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-2 Phase 3——soak 三格全参数执行）

- Plan `2026-09-02-2216-2`（item 15）Phase 3 完成：SOAK-1/2/3 全参数执行并留档（`_tmp/mini-stream-cluster/{1788380205294-1,1788381152134-1,1788382070775-1}`，samples.jsonl + run-summary.json + logs/checkpoints/output 全保留）。
- **三格全部 triggered-known-defect（item 28 归属确认）**：remote-deploy JDBC 数据面累计 ~800—1000 条跨 TM 记录后永久停摆——20 行/s jam@40s（输出 6/120）、10 ev/s jam@60s（告警 5/60）、200 行/s jam@5s（输出 0）——与 item 28「1024 槽队列满 dispatch 永久阻塞」定量吻合；停摆签名 = epoch/输出冻结 + 进程全活 + queue 单调涨（22k/18k/41k）+ TM 日志 `RemoteInputChannel Interrupted while enqueueing`；且源侧无回压传导（JDBC 表无界缓冲，12000 行全写完而消费侧死亡）。
- 健康对照：520 记录同管线 exactly-once 完整收敛（Phase 2 smoke）——阈值触发型缺陷，非语义错误；C0—C3 短时基线（≤~500 记录）从未触达阈值。
- 结局闭环：证据锚点 + item 28 归属路由确认落档演练报告「Phase 3」节；无新缺陷、无静默丢弃；hang 处置规则执行（先核验 §7① 归属再判定）。
- owner-doc 裁定：本 phase 基建就地修复（pump 锚定/APPEND/泄漏启发式）均未触及已文档化行为 → No owner-doc update required（runbook §7 证据更新在 Phase 5）。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-2 Phase 2——演练驱动基建三装置 + 参数化联合入口）

- Plan `2026-09-02-2216-2`（item 15）Phase 2 完成：三装置 + 参数化联合入口落在 fraud-example 场景包（test 域，无引擎能力变更）——`ExerciseLoadGenerator`（S2 行/S1 CDC fixture + 期望集单一事实源生成；watermark-pump 尾部锚定 lastData+20s、全部泵事件≤1 窗内不自闭窗）、`ChaosKillPlanner`（种子化随机 kill 时序 + TM kill/分区等价轮分配）、`ExerciseSampler`（JC /metrics HTTP + msg_queue 深度 + durable epoch + retained manifests + 输出行数 + 存活，JSONL 采样 + 停滞/泄漏序列分析）、`ThrottledScenarioSinks.SteppedThrottleFileTwoPhaseCommitSink`（BP-1 档位文件实时节流，缺失/非法 fail-fast）+ `DistributedScenarioSupport`/`S2ScenarioPipelineFactory` level-file 接线、联合入口 `TestStabilityExerciseMultiJvm`（6 方法对应 6 矩阵格；`-Dexercise.*` 参数化 + fail-fast；preserve-artifacts 强制校验；失败格也写 run-summary.json 留证）。
- 新增独立验证用例 32 个（默认态全绿，无 JVM spawn）：`TestExerciseLoadGenerator` 8、`TestExerciseSampler` 6、`TestChaosKillPlanner` 5、`TestSteppedThrottleSinks` 7、`TestExerciseParams` 3 + 既有全套回归 `./mvnw test -pl nop-stream/nop-stream-fraud-example -am` 105/0（+上游模块全绿）。
- **端到端验证（gated 缩比 smoke）**：①soak 20s（520 记录）完整收敛 exactly-once（期望集逐行精确命中——泵模型修复后）；②chaos 1 轮 70s：kill tm-1 → fencing 1→2 严格递增 + 恢复断言 + chaos-events.jsonl/samples.jsonl 全留档；③fail-fast 参数校验 live 证明（durationSec=60 < 调度需求 65s → 拒绝执行）。
- **演练 smoke 发现（引擎侧，按纪律路由不就地修）**：①kill+recovery 在跨 TM 积压 ~150 记录下数据面永久停摆（epoch/output 冻结、队列单调涨、进程全活——C1 小 fixture 从未覆盖）；②持续流 ~800-1000 记录后同样停摆（20s/520 记录收敛 ✓ vs 60s/1300 停摆）；③健康短跑也有 msg_queue 残留增长（全对订阅派生垃圾消息）。三者与 runbook §7① item 28「1024 槽队列满 + 全对订阅」机制族一致——Phase 3/4 按 hang 处置规则归档归属证据，Phase 5 路由。
- 基建就地修复 ×3（自身缺陷）：pump 尾部跨窗自闭合期望污染（改为单窗不闭合锚定 + linger 承担墙钟余量 5s）、chaos-events.jsonl APPEND 缺 CREATE、queue 泄漏启发式过宽（平台期≠无界增长，改为「run 尾仍活跃增长」判定）。
- MiniStreamCluster 增 `taskManagerPid`/`coordinatorPid` 访问器（runtime test-jar，SIGSTOP 分区轮 + 采样用）；runbook §5 演练表登记 EX 矩阵行（owner-doc 裁定：分布式演练落点=runbook §5）。
- 验证：doc-links exit 0；hollow scan exit 0；默认态全绿。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-2 Phase 1——item 15 演练方案设计落档）

- Plan `2026-09-02-2216-2`（item 15 稳定性演练）Phase 1 完成：演练报告文件创建 `ai-dev/analysis/2026-09/2026-09-03-distributed-stability-exercise-report.md`（Status open，Phase 3/4/5 追加结果）。
- 矩阵定义 6 格：SOAK-1（S2 600s 持续 + 120 用户状态增长）、SOAK-2（S1 CDC 600s，60 窗口可计算 RAPID 期望集 + COUNT 防重复掩盖）、SOAK-3（S2 200 行/s×180s 高速率 = item 28 遗留核验观察点）、CHAOS-1（S2 8 轮随机 kill TM，轮 7—8 = SIGSTOP/SIGCONT 分区等价形态）、CHAOS-2（S2 HA JC kill×2 轮租约翻转断言）、BP-1（S2 节流档位 50/200/500ms 递增 + 释放终态完整性）。
- 观察模式裁定：item 16 已于本 plan 启动前 completed（再裁定钩子未触发，观察模式全矩阵一致）——直接指标 = JC opsHttpPort /metrics（checkpoint 族）+ REST checkpoints history；显式缺口 ×2 不静默：TM 侧 io 层指标多 JVM 下不经 JC 暴露（无 TM→JC metric transport，live 核对 TaskManagerMain 无 ops 端点）、队列水位直测 gauge 缺（代理 = nop_stream_msg_queue COUNT）——均记 Follow-up 候选随 Phase 5 路由。
- 分区模拟三态裁定：可模拟 = SIGSTOP 失联等价 + SIGCONT 陈旧复活 fencing 拒绝（轮询后端下与真分区观察面不可区分）；不可模拟 = IP 级/非对称/双主多数派（JDBC 单点共享库无此拓扑）；替代代理 = CHAOS-2 租约 fencing。
- 结局分类三态 + 闭环条件（完整参数留档 run；基建修复后原参数重跑合法、降档标 pass 不允许）+ hang 处置规则（先核验 item 28 归属再判定新缺陷）+ 资源预算（单格 ≤20min、总预算 ≤120min、preserve-artifacts 强制）落档。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-1 closure 收口——item 16 done）

- Plan `2026-09-02-2216-1`（item 16）closure：独立 closure audit 两轮（general subagent `ses_f9c9bf3b5ffe11bJLHcQo0M94j`，fresh session）。初审 **CLOSURE-BLOCKED**：1 Blocker——P-REQ-1 io 层指标 2<3（「每层 ≥3」验收未达且无豁免裁定，Anti-Slacking 捕获的静默 partial）+ 2 Minor（404 路径清单过期；rocksdb benchmark flake 归因确认）。
- Blocker 修复：io 层第三指标 `nop.stream.io.emit.time`（timer）——`RecordWriterOutput.collect` 对 `writer.emit` 计时（发射阻塞 = 下游背压 → 生产侧背压代理，与 runbook §7 背压收口条目直接呼应）；`StreamTaskMetrics`/`TaskMetricsHandle`/`MicrometerStreamTaskMetrics` 同步 + 单测断言（注册/记录/非正忽略）+ owner doc io 表 3 行 + runbook §7 代理清单居首 + MINOR-1 同批修复。复审 **CLOSURE-APPROVED**（emit 计时接线点逐路径核实：单 writer/链式/尾算子/广播输出共 4 条路径；core 1521/runtime 928 复跑绿）。
- Closure Gates 全过：`./mvnw test -pl nop-stream -am -T 1C` 全绿 3191/0/0（十模块）；gated 启用态：场景集 13/13（S1×2/S2×2/C2×1/C3×2/序列化×6）+ legacy 8/8（ProcessSpawn×3/ExactlyOnce×1/Failover×3/HealthStateAndAlerts×1）；hollow scan 0 findings；doc-links 0 errors；plan-checklist exit 0。flake ×2 诚实记录：rocksdb benchmark 与 TestMultiJvmExactlyOnceRecovery 各一次 -T 1C/负载抖动失败，隔离与复跑均绿（后者 2s——SIGTERM shutdown-hook 注销使检测即时，90s 预算主要兜底任务停摆路径）。
- 收口写回：Plan Status → completed（Closure evidence 含逐 P-REQ/逐 gate 验证结果与 audit session）；roadmap item 16 → done（含 ★解锁说明：item 15 指标观察面前提成立，17→18→M4 关键路径解锁）。
- Commit: a9fe996cf9/c6dbc769fe/112c42d269/7569bef24f（P1-P4）、af9d457766（P5）、7dba868ab9（P6）、b89ba71b5e（audit 修复）。
- Doc-sync: plan Closure 节 + roadmap item 16/Last updated；owner doc/runbook 已在前述各 phase 更新。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-1 Phase 6——手册深化与文档收口）

- Plan `2026-09-02-2216-1`（item 16）Phase 6 完成：runbook 深化（§4 状态重置与维护工具 + §6 运维观测面四小节：指标暴露/REST/健康告警/治理，各含 gated 验证命令；头部更新「item 16 已深化 + 权威落点 = docs-for-ai/03-modules/nop-stream.md」声明）；owner doc 补「运维手册」速查章（启动/停止/重置/恢复（savepoint 等价）/指标告警速查/故障排查）——文档成体系收口。
- 路由锚点同步：INDEX.md nop-stream 路由行改指 owner doc + 新增运维契约路由行；source-anchors.md 新增 STRM-038..045（StreamMetricsRegistries/StreamOpsHttpServer/OpsJobManager/JobHealthStateMachine/AlertService/StreamStateResetTool/StreamJobEventBus/RocksDBMetricsRecorder）。
- runbook §7（原 §5）背压条目收口裁定：指标面未含背压直测 gauge → 显式记录「代理观察为准（processing.time/emitted.total/epoch 推进）+ 指标缺口记 Follow-up 候选（归属 item 15 采样装置裁定）」。
- 偏差 ×1（当场修复）：owner doc/INDEX 初版反向引用 ai-dev runbook 触发 doc-links BOUNDARY 错误 ×2 → 移除引用（docs-for-ai 自包含；runbook→docs-for-ai 方向保留）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-stream --severity high` exit 0（0 findings）。
- Doc-sync: owner doc（运维手册章）+ INDEX + source-anchors + runbook 深化；plan Phase 6 勾选。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-1 Phase 5——健康状态机与告警闭环）

- Plan `2026-09-02-2216-1`（item 16）Phase 5 完成（P-REQ-7/12）：**新增 `health` 包**——`StreamJobHealth` 七态（KS 参照裁剪：RECOVERING≈REBALANCING、DEGRADED≈PENDING_ERROR，NOT_RUNNING/PENDING_SHUTDOWN 不引入）+ `JobHealthStateMachine`（迁移合法性表 fail-fast，非法迁移含自迁移抛 IllegalStateException；`job health transition:` 日志前缀 = 跨 JVM 可观察证据）+ `JobHealthListener`。接线进 JobCoordinator 真实路径：start→RUNNING、globalRecovery 入口→RECOVERING/完成→DEGRADED、failJob→FAILED、terminate(CANCEL/DRAIN/SUSPEND)→CANCELED/FINISHED、CHECKPOINT_COMPLETED 事件（真实完成路径经共享事件总线）回愈 DEGRADED→RUNNING；进入 DEGRADED 派发 JOB_DEGRADED 事件（告警路由输入）。
- **新增 `alert` 包**（P-REQ-12，Phase 1 渠道边界裁定 = runtime 内轻量渠道，不引入 nop-integration 依赖）：`IAlertChannel`/`AlertEvent`（jobId/severity/eventType/message/timestamp）+ `AlertService`（路由表：JOB_FAILED→ERROR、RECOVERY_STARTED/JOB_DEGRADED→WARN；渠道异常 containment）+ `LoggingAlertChannel`（`nop-stream alert:` 前缀）+ `WebhookAlertChannel`（JDK HttpClient POST JSON；**异步有界队列投递**——webhook 慢/不可达不阻塞恢复/失败控制路径，队列满丢弃 WARN；构造 fail-fast 校验）。接线：OpsJobManager submit 注册到每作业（新增全参构造器）；JobCoordinatorMain launch 路径（logging 默认 + `alertWebhookUrl` 启用 webhook）。
- 行为面变更 ×3（均含显式语义）：REST 作业摘要/详情新增 `health` 字段；stop 在 RECOVERING 窗口（毫秒级）返回 409 `JOB_STATE_CONFLICT`（健康机 fail-fast 的 REST 映射，恢复后重试）；failJob 对已终态（FAILED/CANCELED）作业从静默 return 改为 WARN 可观察 no-op（健康机终态无出边的守卫）。
- 既有测试适配 ×1：`TestJobCoordinatorLifecycleEvents#testBuiltInLoggingListenerRegisteredByDefault` 期望 2→3（coordinator 总线新增 P-REQ-7 CHECKPOINT_COMPLETED 路由监听器；测试意图「内建 logging listener 默认注册」不变）。
- 新增测试 5 类 20 用例：`TestJobHealthStateMachine`（穷举合法性表）、`TestJobCoordinatorHealthWiring`（真实生命周期驱动 5 路径）、`TestAlertChannels`（路由/containment/webhook JSON+重试/配置 fail-fast）、`TestAlertFaultInjectionE2E`（真实 TM 部署 + 门控 source 消除接线竞态 + 注入 sink 失败 → 真实失败链告警断言）、`TestMultiJvmHealthStateAndAlerts`（gated：kill TM → coordinator 进程日志断言 health 迁移 + JOB_DEGRADED + alert 外发）。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime -T 1C` 全绿（928/0/0）；gated `-Dtest=TestMultiJvmHealthStateAndAlerts -Dnop.stream.test.multi-jvm.enabled=true` 绿（2.5s——SIGTERM shutdown hook 注销使检测即时，无需等租约到期）。
- Doc-sync: `docs-for-ai/03-modules/nop-stream.md` 新增健康状态机语义表 + 告警配置键节 + REST health/409 契约更新。

### 2026-09-03（mission nop-stream-productization EXEC_PLANS：plan 2216-1 Phase 4 收口 + Phase 5/6 执行）

- Plan `2026-09-02-2216-1-observability-ops-productization.md`（item 16）Phase 4 收口：上一执行会话中断（代码已落、plan 未勾选、未提交）。本轮核对 live 代码后确认交付完整：`ops` 包 REST 生命周期端点（`OpsJobManager` submit/stop/registry + `StreamOpsHttpServer` 端点族含 threaddump——Phase 1 分期裁定「随本设计交付」）、`maintain` 包（`StreamStateResetTool` + `StreamMaintenanceMain` reset-state/reshard 入口收敛）、`StreamGovernanceConfig` 治理配置 + `OpsJobManager` 治理扫描。
- 中断遗留修复 ×2：gate-inventory.json 补 `CheckpointCoordinator.pruneOldestCheckpointHistory`（invariant 表完备性门禁红→绿）；metrics.properties.template 治理键与 `StreamGovernanceConfig` 实际键对齐（`job-record.retention-minutes` 曾误写 `job-history.retention-minutes`，补 `checkpoint-history.retention-minutes` + `governance.cleanup-interval-ms`）。
- Phase 4 文档增量：owner doc（`docs-for-ai/03-modules/nop-stream.md`）落 REST API 契约表 / 重置工具 / 治理配置节（P-REQ-5「接口文档落 owner doc」验收项）；runbook 新增 §4 状态重置与维护工具（P-REQ-10「手册章节」验收项），原 §4/§5 顺延为 §5/§6。
- 偏差：`TestRocksDBIncrementalRestoreAndBenchmark#incrementalCheckpointIsFasterThanFullScanForLargeState` 在 `-T 1C` 并行负载下偶发超时比值翻转（best ratio 3.49），隔离复跑通过（ratio 0.638 ≤ 1.0）——既有已知 flake（325be069fd 曾专项加固），非本轮引入。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime -am -T 1C`（排除上述 benchmark flake）全绿：runtime 907 tests / 0 failures，core 1521 tests / 0 failures；新增用例见 plan Phase 4 勾选项（TestOpsRestLifecycleE2E 4 例 + TestStreamStateResetTool 5 例）。
- Commit: 见 git log（Phase 1-4 一并提交，上一会话未提交）。
- Doc-sync: owner doc + runbook 已更新（上述）；`ai-dev/audits/nop-stream-invariants/gate-inventory.json` 同步。
