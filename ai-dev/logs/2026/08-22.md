# 2026-08-22

### Mission nop-plugin-enhancement：R1-R3 计划起草并通过两轮独立 draft review，Plan Status → active

- Mission: nop-plugin-enhancement（DRAFT_PLANS 轮）。起草三份计划（执行顺序 1→2→3），覆盖 roadmap 反转重构前三项（R4 测试+docs 留待后续轮，其基线由 R1-R3 落地结果决定）：
  - `ai-dev/plans/2026-08-22-2309-1-plugin-api-shrink-single-state-machine.md`（R1：API 收缩——单层六态状态机，删 IPluginInstance/InstanceState/实例方法，ServiceProxy 改绑插件激活态，jar 轨契约，Javadoc 修正）
  - `ai-dev/plans/2026-08-22-2309-2-manager-orchestration-simplification.md`（R2：插件级 coeffect + 实例残留清零 + 错误码族退役 + 去激活拓扑序 + HMR 去快照 + checkChangedAndReload 过滤器修正）
  - `ai-dev/plans/2026-08-22-2309-3-compat-path-command-routing-regression.md`（R3：start=load+activate / stop=deactivate+unload 两轨收敛 + invokeCommand 定义级路由 + plugin.xdef 属性集冻结确认）
- Draft review（每 plan 独立 fresh session 子 agent，两轮 + 一轮收口复核）：第一轮发现 2 Blocker + 7 Major（jar 轨 activate() 落点不可实现、isInstanceConfigMatched 归属互斥、R1/R2 边界漂移、grep 路径不可执行、CoeffectConfigHelper/InstanceConfigProvider 消费者事实错误、错误码族清单缺口、HMR LOADED-only 过滤器静默失效等）；全部修复后第二轮 + 收口复核确认 12 项修复 PASS、无 Blocker/Major 遗留，共识达成转 active。
- 关键裁定（记录于 plan 内，执行时生效）：
  - **Ownership Deviation**（R1 plan）：roadmap 记在 R2 名下的 parentToChildren/快照/pending/isInstanceConfigMatched 等全部以 IPluginInstance 为类型，随 R1 编译依赖一并移除；R2 = 残留审计 + 错误码退役 + 拓扑序 + 语义收口。R4 docs 同步时回读修正 roadmap R2 文字与 01 锚点表 R2 行。
  - **jar 轨门控**（01 §二原文语义）：恒空集无条件激活、激活回调跳过（xdef 载体仅元数据，不驱动门控）；AbstractPlugin.activate()/deactivate() 本体由 R1 交付（复用 doStart 容器路径 + 六态迁移），R3 只做 start/stop wrapper 验证与回归基线。
  - **VFS 轨 start/stop 直接收敛**（编译强制）：start = load + activate / stop = deactivate + unload 在 R1 完成，R3 补基线；R3 补状态边界三断言（已 LOADED 不重复 load / ACTIVATED start 幂等 / UNLOADED stop 幂等）。
  - **定义级配置通道**：PluginInstanceImpl 的 setConfigProvider 钉死约束（禁静默回落全局 AppConfig）语义等价迁移为定义级配置域视图，R1 落地、R2 验证（热应用断言要求可观察传播）。
  - 错误码退役顺序：先改 ERR_PLUGIN_ACTIVATION_FAILED/ACTIVATOR_NOT_FOUND 的 define 去 ARG_INSTANCE_KEY → grep 清零 → 删常量；api 侧两常量（ERR_PLUGIN_MULTIPLE_INSTANCES、ARG_INSTANCE_KEYS）为 R2 的 api 模块唯一例外。
- Roadmap Work Items R1/R2/R3 已标 `planned`（`planned` = 有计划且通过独立 draft review，符合状态定义）。
- 未提交 git（按 mission-driver 统一 commit 惯例）。

### Plan 345 收口：独立 closure audit CLOSE_ALLOWED，Plan Status → completed

- Plan: `ai-dev/plans/345-xpath-xtransform-hardening.md`（全部 5 Phase 完成，Closure Gates 全勾，Closure Evidence 已写入 plan）。
- 独立 audit（fresh session ses_fd60ad69effeeoItyGFhwHJxhr）：逐条对照 live code 复核 5 个 Phase 全部 PASS + Anti-Hollow（dispatch/operator 调用链实际连通、17 个改动文件无空壳/静默跳过、无 `_gen/` 手改）+ Deferred 诚实性（5 项均为真实 out-of-scope），判定 CLOSE_ALLOWED。
- 门禁命令：`./mvnw test -pl nop-kernel/nop-xlang` 577/0/0 绿；compile/install -DskipTests（-am -T 1C）BUILD SUCCESS；`checkstyle:check -Pqa` BUILD SUCCESS（qa profile 非门禁，残留 1 WARN：XtTransformContext 包私有构造器 9 参——保留 plan 规定的显式 cache 参数签名，不为 lint 改接口）；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --severity high` exit 0；`check-doc-links.mjs --strict` exit 0。
- 执行中 checkstyle 驱动的两处小修：`XtExprParser` javadoc 改 `<code>` 写法（`{@code ${}}` 使 javadoc 解析器误判）；`XtTransformCompiler.compileRule` 的 switch 拆出 `compileKnownRule`（圈复杂度 16→<15，行为不变，26 测试全绿验证）。
- 测试资产终态：TestXPath 11 + TestXtTransform 26 = 37 个 @Test（门槛 ≥28）。
- 未提交 git（按会话约定由上层 mission-driver / 用户决定；worktree 中还有其他并行会话的未提交改动，避免混入）。

### Plan 345 Phase 5 完成：docs-for-ai 迁入与索引同步

- Plan: `ai-dev/plans/345-xpath-xtransform-hardening.md` Phase 5。
- 改动：新建 `docs-for-ai/02-core-guides/xpath-and-xtransform.md`（xpath 语法 + 8 operator + 内置变量 / xt 文档结构 + 13 规则指令表 + 表达式简写 `${@attr}`/`${$params}` 与 `%{}` 兼容说明 / 9 个错误码清单（2 XPATH + 7 XT，均附触发点与阶段）/ XSLT 差异表，内容对齐实施后 live code）；`source-anchors.md` 新增 XPT-001/002/003 三条 anchor（路径经 ls 验证存在）；`INDEX.md` 快速路由表追加 xpath/xt → owner doc 行。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（新文档 0 issue；15 个 warning 全部来自 plan 文件自身的简写路径引用，非 error）；`./mvnw test -pl nop-kernel/nop-xlang` 577 tests 0 failures。
- Doc-sync: `docs-for-ai/02-core-guides/xpath-and-xtransform.md`（新增 owner doc）+ `04-reference/source-anchors.md` + `INDEX.md`。

### Plan 345 Phase 4 完成：xt 表达式驱动规则端到端覆盖（value/if/choose/script/gen/params/custom-tag/mandatory）

- Plan: `ai-dev/plans/345-xpath-xtransform-hardening.md` Phase 4。
- 改动：新增 8 个 `@Test` + 15 个资源文件，全部走 model load → transform → XNode 断言链路；`GenRule` 补 dummy 节点展开（多根 `xpl-node` 输出被包在 dummy 节点，原实现直接 addChild 会把 dummy 混进输出）；`XtExprParser` 的 `$builtin` 拦截从 defaultFactorExpr 移到 `primaryExpr`（`$` 是合法 Java 标识符起始字符，标准解析器在 primaryExpr 就把 `$params` 当 Identifier 消费掉，走 LexicalScopeAnalysis 全局变量分支报 unresolved-identifier——defaultFactorExpr 永远不会收到 `$` 开头的 factor）。
- 事实修正（与 plan 预写语法的偏差，均源于平台硬规则）：① testXtScript 用 `output.addChild(...)` 而非 `$output.addChild(...)`——`$` 前缀标识符保留给 EvalGlobalRegistry，scope 局部无法注册 `$` 名（LexicalScopeAnalysis 硬规则），ScriptRule 注入的是不带 `$` 的 `output`；`XNode.make('built', null, 'yes')` 三参重载不存在，改为 `let n = XNode.make('built'); n.setContentValue('yes')`（plan 强调的 addChild-vs-addNode 要点保留）；② testCustomTagOutput 的 `xt:attrs` 是 `expr` domain（compileSimpleExpr，不接受 `${}` 包裹），写 `xt:attrs="{'extra':'val'}"`；③ testXtGen 的 `items` 经 params 以 scope 局部变量直接暴露（Phase 2 的 bindParameters）。
- 验证：`./mvnw test -pl nop-kernel/nop-xlang -Dtest=TestXtTransform` 26/26 绿；全模块 577 tests 0 failures；@Test 计数 TestXtTransform 26 / TestXPath 11（Closure Gates ≥17/≥11/≥28 全部满足）。
- Doc-sync: No owner-doc update required（Phase 5 统一落盘）。

### Plan 345 Phase 3 完成：mapping dispatch 修复 + import/inherits/循环引用/编译期 ID 检查落地

- Plan: `ai-dev/plans/345-xpath-xtransform-hardening.md` Phase 3。
- 改动：
  - `XtTransformCompiler` 重构：`compileAll` 管线（imports → own templates → mappings+inherits → main → validateReferences）。mapping cache 产出 `Map<mappingId, Map<tagName, IXTransformRule>>` + `Map<mappingId, defaultRule>`，消除 `getRuleForTag` 对 XtRuleGroupModel 的非法强转（原 ClassCastException 根因）。import 用显式 `new XtTransformModelLoader()` 加载（VFS 路径），按 `prefix:originalId` 登记（空 prefix 用原名），跨文件递归用共享 importStack 防循环 import；被 import 文件在独立子 compiler 中自校验后原样合并。inherits 递归合并（子覆盖父 match、子无 default 继承父、csv 后者覆盖前者），解析栈防 inherits 循环。编译期收集所有 apply-template/apply-mapping 引用，在 cache 定型后统一校验（支持前向引用），缺失抛 ERR_XT_TEMPLATE/MT_MAPPING_NOT_FOUND 并附 source location。
  - `XtTransformContext`：构造器改收两个 mapping cache（替代 `Map<String, XtMappingModel>`）；新增共享 visitedTemplates/visitedMappings（childContext 传递同一实例）；`getCompiledRuleForTag` 查 cache → default → null，mapping 不存在抛 ERR_XT_MAPPING_NOT_FOUND；`getRuleForTag` 标 @Deprecated 委托新方法。
  - `IXTransformContext`：移除 `getMapping`，新增 `getCompiledRuleForTag`/`getVisitedTemplates`/`getVisitedMappings`（全仓无外部调用方，已 grep 复核）。`IXTransform` 从空接口改为 `transform(XNode)` + `transform(XNode, Map)`，`XtTransform implements IXTransform`。
  - `ApplyTemplateRule`/`ApplyMappingRule`：visited add/finally remove，重复访问抛 `ERR_XT_CIRCULAR_REFERENCE`（mapping key 为 mappingId:tagName）。`AbstractSelectorRule` 的 xpath select 异常包装为 `ERR_XT_XPATH_ERROR`；`compileRule` 异常包装为 `ERR_XT_RULE_COMPILE_ERROR`（NopException 透传不二次包装）。`XLangErrors` 新增 4 错误码 + ARG_RULE_TYPE。
  - 新增 8 个测试 + 8 个资源文件（mapping-match/import-a/import-b/import-conflict-a/import-noprefix-conflict/mapping-inherits/template-circular/template-not-found/mapping-not-found/template-mandatory），覆盖 dispatch、import prefix 隔离与冲突、inherits 覆盖/回退/default 继承、循环引用、编译期 ID 缺失、runtime mandatory 六条主路径。
- 执行期事实修正：① VFS 仅挂载 classpath `_vfs/` 目录，被 import 的测试资源放 `src/test/resources/_vfs/test/xt/import-b.xt.xml`（from="/test/xt/import-b.xt.xml"）；② inherits 递归再入已编译的父 mapping 不算 import 冲突（compileMapping 区分 own 定义与 inherits 再入），否则 testMappingInherits 误抛 ERR_XT_IMPORT_CONFLICT。
- 验证：`./mvnw test -pl nop-kernel/nop-xlang -Dtest=TestXtTransform` 18/18 绿（10 旧 + 8 新）；全模块 569 tests 0 failures；`getCompiledRuleForTag` 接线 grep：接口声明+context 实现+deprecated 委托+ApplyMappingRule 调用共 4 处。
- Doc-sync: No owner-doc update required（Phase 5 统一落盘）。

### Plan 345 Phase 2 完成：xt 上下文内置变量统一 + xt 表达式层修复（`${@attr}` 与 `$builtin` 简写落地）

- Plan: `ai-dev/plans/345-xpath-xtransform-hardening.md` Phase 2。
- 改动：
  - `XtTransformContext`：构造器注入 6 个内置变量（node/thisNode/root/output/params/context，恰好 6 处 setLocalValue）；`childContext(node)` 共享同一 output 实例并切换 node/thisNode/root；`childContext(node, newOutput)` 额外 `output.pushNode(newOutput)`（调用方负责 pop）；`setCurrentNode/setRoot/setThisNode` 同步 scope 变量。
  - 规则样板清理：IfRule/ChooseRule/ValueRule/ValueOutputRule/GenRule/CustomTagRule 删除全部 node/context/params 样板；ScriptRule 仅保留 output 幂等注入；EachRule 增加 prevNode/prevThisNode try-finally 恢复；CustomTagRule 改单参 childContext + 保留自身 push/pop，attr 求值移到节点切换后（`${@n}` 看到选中节点）。
  - `XtTransform.transform`：params 各 key 额外暴露为 scope 局部变量（内置名不覆盖），支持 `<c:for items="${items}">` 类引用。
  - **新建 `xt/core/XtExprParser`**（extends XPathExprParser）：① 继承 `@attrName` 简写（运行期读 scope `thisNode`）；② 拦截 `$node/$thisNode/$root/$output/$params/$context` 映射到 scope 局部变量——`$` 前缀在 XLang 平台保留给 EvalGlobalRegistry 全局变量（LexicalScopeAnalysis 硬规则），scope 局部无法注册 `$` 名，故必须在解析层拦截；③ `isExprStart` 在 transform 阶段同时接受 `${}` 与历史 `%{}`；④ 构造器 enableFeatures(ALL)（成员访问/方法调用需要）。`XplStdDomainHandlers.XtExprType/XtValueType` 改用该解析器（原实现的 `%{}`+`@` 组合从未可用——`@` 不被标准解析器接受，plan baseline "核心特性空缺" 的根因之一）。
- 偏差：plan 未显式列出表达式层修复条目，但 Phase 2 Exit Criteria "表达式语法统一用 `${@id=='X'}`" 依赖它（现有 `xt-expr` handler 无法解析任何 `@` 表达式），落在 nop-xlang 模块内、仅影响 xt 专属 std-domain（xt-expr/xt-value 只有 xt.xdef 使用）。`testIfRuleWithEach` 的 match 上补了 `id="${@id}"` 属性以落实 exit criteria 中 `result.child(0).attrText("id")=="X"` 断言。
- 验证：`./mvnw test -pl nop-kernel/nop-xlang -Dtest=TestXtTransform` 10/10 绿（9 旧 + 1 新）；全模块 561 tests 0 failures（含使用 `%{}` 语法的 testRelativePath 不回归）。
- Doc-sync: No owner-doc update required（Phase 5 统一落盘）。

### Plan 345 Phase 1 完成：xpath operator 注册闭环（Plan Status: active，执行期）

- Plan: `ai-dev/plans/345-xpath-xtransform-hardening.md` Phase 1。
- 改动：新建 `HtmlOperator`/`InnerHtmlOperator`（与 OuterXml/InnerXml 同构，调 `adapter().html()/innerHtml()`）；`XPathOperatorRegistry` 静态块补注册 `$html/$innerHtml/$node`（5→8，与 `XLangConstants.XPATH_OPERATOR_*` 常量数 8 对齐）；`TestXPath` 2→11 个 `@Test`（新增 7 operator/union 用例 + 2 错误路径用例，全部带返回值断言；`testAttrFilter` 顺手补了返回值断言）。
- 事实修正（执行期发现）：① union 解析器（`XPathSelectorParser.xpath` + `TextScanner.tryMatch`）不接受 `|` 前的空格，且 `MultiXPathSelector.selectAll` 不去重，故 `testPipeUnion` 用不相交双分支 `//sub/child|/root/child` 断言恰好 3 节点（2 嵌套 child + 1 根 child，与 plan 括号内语义一致）；② `/..` 字面量无法通过 rootComponent 解析（`..` 不是合法 tag 名），`testParentOnRoot` 用 `/root/..` 触达 `ParentSelector` 在 root 上的 `ERR_XPATH_ROOT_NOT_ALLOW_PARENT_SELECTOR` 运行期路径；③ `ERR_XPATH_UNKNOWN_OPERATOR` 在 parse 期抛出（`assertThrows` 包住 `parseXSelector` 而非 `selectOne`）；④ `NopException.getErrorCode()` 返回 String，断言用 `ERR_*.getErrorCode()`（仓库既有模式）。
- 验证：`./mvnw test -pl nop-kernel/nop-xlang -Dtest=TestXPath` 11/11 绿；全模块 `./mvnw test -pl nop-kernel/nop-xlang` 560 tests 0 failures；`grep -c '_instance.registerOperator'` = 8 = `grep -c XPATH_OPERATOR XLangConstants.java` = 8。
- Doc-sync: No owner-doc update required（Phase 5 统一落盘 `xpath-and-xtransform.md`）。

### Plan 345 独立审查通过：draft → active（修复 1 Major 事实错误 + 5 处一致性/路径问题）

- 审查方式：mission-driver review 轮（独立任务），对照 plan-authoring-guide 模板 + live repo 逐条核验 Current Baseline / Goals / Phase items / Exit Criteria / Closure Gates。
- Baseline 核验：除 2 处事实错误外全部属实（13 selector / 8 operator 常量 / 5 注册 / ClassCastException 强转 / import+inherits schema 接受但代码不读 / childContext 忽略 newOutput / 3 缺错误码 / 静态文本测试 / IXTransform 空接口 / XNode 无 attr(String) / IXtTransformOutput 无 addNode / OOXML 两模块使用等）。
- 修复内容：
  ① **Major 事实错误**——baseline 声称 `XtTransformModelLoader` "已注册（modelType=xt）"，实际全仓无任何注册点（`XtTransform.load()` 直接走 DslModelParser），Phase 3 import 项同步改为"复用 `XtTransform.load(IResource)` 或显式 `new XtTransformModelLoader()`"；
  ② **Major 测试缺口**——Goal 9 要求 `$innerHtml` 测试覆盖但 Phase 1 无对应用例，补 `testOperatorInnerHtml`（7+2=9 新用例，TestXPath 目标 11 个，Closure Gates 同步 ≥11/≥17/≥28）；
  ③ **Major 事实错误**——Phase 2 声称"目前 CustomTagRule 不传 newOutput"，实际 `CustomTagRule.java:70` 已调用 `childContext(selected, outputNode)`（因 bug 形同虚设），协调项改写为"改为单参 childContext(selected) 避免双重 push"；
  ④ **Major 逻辑矛盾**——Goal 6 ScriptRule 注（"保留 output 再次确认" vs "完全不需要额外注入"）自相矛盾，统一为"保留幂等 setLocalValue(output)，删 node/context/params"（与 Phase 2 一致）；
  ⑤ **Minor 重复测试**——Phase 4 `testEachNestedIf` 与 Phase 2 `testIfRuleWithEach` 内容完全相同，Phase 4 改标"不新增"，计数 9→8（新增总数 16，Closure Gates 同步）；
  ⑥ **Minor 路径/引用**——`iop/`→`io/nop/` 错字 ×3、Phase 3 Targets 中 `IXTransformContext/IXTransform` 实际在 `xt/` 根目录不在 `core/`、Phase 5 INDEX 表名 "By Code Location"→「快速路由」、Phase 3 Exit Criteria 补 "No owner-doc update required"（guide rule #17）、`getMapping` 移除项补"唯一调用点 ApplyMappingRule 同 Phase 内同步改造"。
- 判定：无未解决 Blocker，plan 可执行，`Plan Status: draft → active`。
- 验证：全部修改经 live repo grep/read 复核（operator 注册数、类路径、test 计数、错误码、xdef 元素等）。
- Doc-sync: No owner-doc update required（review 未触达 docs-for-ai；Phase 5 执行期落盘）。

### nop-plugin 定位反转设计：三 agent 独立审计 + 修复（1 Blocker + 10 Major，无翻案项）

- 审计：三个并行独立子 agent（内部一致性 / 理论忠实度 / 代码现实核查）审查反转后的 design/nop-plugin 全套 + roadmap + 讨论记录第 17 轮。结论：反转裁决成立；R1-R4 基于现有代码可执行（"无外部消费者"经全仓 grep 复核属实）。
- 关键发现与修复：① Blocker——roadmap 头部仍宣称"多实例为设计目标/两层状态机"（mission 循环直接输入，危害最大），已改写为反转后表述 + 历史记录横幅；② 理论限定——"注册内容全部加载时可知"降格为带限定定位声明（per-load 非 per-boot、运行期才知道内容的注册由领域层外部注册表承载）；③ 事实纠正——agent 层承接机制是 tag 可见性系统而非 ContributionRegistry（后者为全局装配期注册表），scope/realm 标注 open handoff 未立项；④ 机制补强——01 新增跨插件去活性拓扑序不变量（消费者先于提供者退出，cordis Theorem 63 对应物缺失被审计抓住）；⑤ 归属对齐——API 面收缩归 R1（IPluginContext 重写与 IPluginManager 抽象方法删除是编译阻塞面，原 R1 清单遗漏）、编排清理归 R2（ICoeffectEvaluator 实例条件/parentToChildren/快照重建/实例错误码族退役）；⑥ 虚假陈述更正——05 的 "ServiceLoader 发现 IPlugin 实现"实为 plugin.json+反射实例化；⑦ jar 轨契约显式裁决（无 xdef 载体→无条件激活、activator 缺省跳过）；⑧ 03/04 对 dsh 非活跃访问行为的矛盾表述统一为双路径描述 + R4 对照 cordis 源码终核。
- 偏差：审计发现的 open-handoff（nop-ai-agent 缺 scope/realm 立项）仅在 02 中标注，未替 agent 层补立项条目（跨模块文档，留待用户裁定）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（修复后复跑）。
- Doc-sync: No owner-doc update required（docs-for-ai 同步维持推迟到 R4 的既定裁定）

### Plan 344 Phase 3/4/5 完成：数字快路径 + writer 标量直写 + 最终对照与差距分析

- **Phase 3**：`TextScanner.tryNextDecimalInteger` 数字直接累加快路径（与字符串快路径同构的两层设计：序列前瞻判定干净整数才消费，溢出/后缀/小数/指数零扰动回落）。**调试发现 Java 陷阱**：混合装箱类型三元表达式 `cond ? Integer.valueOf(..) : Long.valueOf(..)` 被 JLS §15.25 数值提升统一为 long 恒返回 Long（字节码反编译定位），改显式 if/else。重复键检测按行为规格 (c) 裁定保留 containsKey+put（null 值歧义不安全 + sentinel 会泄漏进结果 Map + profiler 显示非热点）。新增测试：Integer/Long 类型边界（含 ±int 边界与 ±Long 边界）、前导零报错、hex、l/f/d 后缀、指数、溢出回落、`{"a":null,"a":1}` 抛 ERR_JSON_DUPLICATE_KEY、XNode 重复属性抛 ERR_XML_DUPLICATE_ATTR_NAME。
- **Phase 4**：`CollectTextJsonHandler.numberValue/booleanValue/writeValue` 的 Integer/Long/Boolean 直写（StringBuilder.append(long) 内部走 Long.getChars 零临时 String；Boolean 用常量字面量），其余 Number 子类型保留原路径。新增逐字符文本一致性断言（含极值与 htmlSafe）。
- **Phase 5 对照**：最终四项组合复测两次被环境负载作废（load 14~22，fastjson2 自身劣化 37%）；权威对照取三次隔离 xlang 运行——基线 423,608 ±12,246 → P2 后 415,674 ±7,239（无回归）→ P3 后 425,175 ±4,892（持平+0.4%）。命令：`java -cp "nop-benchmark/nop-benchmark-json/target/classes:$(cat nop-benchmark/nop-benchmark-json/target/cp-json.txt)" org.openjdk.jmh.Main "EishayParseTreeString\.xlang$" -f 3 -wi 3 -w 2 -i 5 -r 2`
- **差距分析（aspirational ≥30% 实际 ≈0%）**：profiler 实证热点为 LinkedHashMap.newNode ~19.6%（树构建分配，Non-Goal）、skipBlank ~18.7%（safepoint 偏差放大）、词法层合计 <10%。结构性改造（codegen 直接绑定/免 AST）已列入 Deferred successor。实际交付价值：JMH 设施修复、consumeDigits pos 不变量缺陷修复、writer 零分配标量写出、词法快路径+边界测试资产。
- **受影响模块验证**：nop-commons 201 / nop-core 233 / nop-xlang 551 tests 全绿；nop-report-core、nop-ai-core 编译冒烟通过。
- Doc-sync: No owner-doc update required

### Plan 345 起草：xpath 与 xtransform 实现与测试硬化（draft）

- **目标**：把 `iop.xlang.xpath` 与 `iop.xlang.xt` 从"骨架完工+核心特性空缺"提升到"设计文档承诺的功能全部连通且被测试覆盖"。Plan 范围严格限定 `nop-kernel/nop-xlang` 模块内，不引入新外部依赖。
- **审计发现的 in-scope defects**（已纳入 plan）：xpath 缺 `$html/$innerHtml/$node` 注册、mapping dispatch `ClassCastException` bug、import/inherits/循环引用/编译期 ID 检查完全缺失、xt context 内置变量未统一注入、`childContext(node, output)` 忽略 `newOutput` bug、错误码 `ERR_XT_XPATH_ERROR/ERR_XT_RULE_COMPILE_ERROR/ERR_XT_CIRCULAR_REFERENCE` 缺失、xt 测试只覆盖 trivial 路径、docs-for-ai 完全缺失 xpath/xt 章节。
- **5 个 Phase**：Phase 1 xpath operator 闭环与 selector 边界测试（6+ 新增 `@Test`）；Phase 2 xt 上下文内置变量与样板统一（1 新增 `@Test`）；Phase 3 xt mapping dispatch + import + inherits + 编译期检查 + 错误码（8 新增 `@Test`）；Phase 4 xt 表达式驱动规则测试覆盖（9 新增 `@Test`）；Phase 5 docs-for-ai 迁入与索引同步（无代码改动）。
- **审查记录**：两轮独立子 agent 对抗性审查（task ses_第一轮 / ses_第二轮）。第一轮发现 12 个 Blocker + 15 个 Major，集中在 Phase 3 mapping cache 存放位置（违反 `_gen` Hard Stop）、import id 改写在 KeyedList 上不可行、`childContext` 输出根切换语义未明、Phase 4 测试中 `node.attr('id')` 实际不可用（XNode 没有 attr(String)）、`$output.addNode` 接口不存在（应改 addChild）、Phase 2 与 Phase 4 在 ScriptRule 样板上互相矛盾等。第二轮验证全部 12 个 Blocker 已 PASS，修复了 3 处 Minor（`iop`→`io` 路径错字、`EachRule.apply` 恢复语义澄清、`childContext(node, output)` pop 责任归属 NOTE）。
- **关键设计决策（执行期重要）**：
  - Phase 3 mapping dispatch cache 走"外部 Map 层"路径，不改 `_XtMappingModel._matchs` KeyedList 与 `_XtMappingModel.getId()`（按 AGENTS.md `Hard Stop: Generated Files` 禁止）。
  - Phase 3 import id 重写走"在 cache 上用 `prefix + ":" + originalId` 作 lookup key"路径，被 import 的模型子树内容不变。
  - Phase 3 循环引用 visited state 在 `XtTransformContext` 上维护，`childContext` 不复制（保证所有 context 共享同一份 state），`ApplyTemplateRule.apply`/`ApplyMappingRule.apply` 用 try-finally push/pop。
  - Phase 2 ScriptRule 保留 `setLocalValue("output", ...)` 注入（XPL 脚本需要 `$output`），但删除 `node/context/params` 样板。
  - Phase 2 表达式统一用 `${@id=='X'}` xt:expr 内置属性简写，**不**用 `${node.attr('id')}`（XNode 没有 attr(String) 方法）。
  - isBiDirectional / setValue 修复移到 `Deferred But Adjudicated`，本 plan 不做（涉及对象图构建，破坏性较强）。
- **验证**：`node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/345-xpath-xtransform-hardening.md --strict` exit 0。
- Doc-sync: No owner-doc update required（plan 处于 draft，未触达 docs-for-ai；Phase 5 执行期会新增 `xpath-and-xtransform.md`）。

### nop-plugin 定位反转：删除多实例 + 依赖收敛插件级（设计文档全面修订，无 plan，ad-hoc 设计任务）

- 背景：用户基于 `ai-dev/articles/dsh-architecture-from-reversible-computation.md`（§4.6 认识论边界）质疑：Nop 已有编译期 Delta 细粒度定制且服务端无状态，plugin 是否还需实例化机制、依赖是否只需插件级。AI 论证确认方向成立：dsh 需要 fiber/scope/realm 是客户端 harness 特有需求；Nop 插件层注册内容全部加载时可知，运行时结构空间只剩布尔门控。实证：multi-instance 消费者全部在模块内部，nop-ai-agent 零依赖 nop-plugin。
- 裁决（讨论记录第 17 轮，推翻第 7 轮恢复多实例的决策）：一个定义至多一个激活；删除 createInstance/instanceKey/parent 层级/实例配置域/实例级 coeffect/级联销毁/HMR 快照重建；关闭 W8 预留的 requires-service 服务级依赖扩展点。保留两态分离、IPluginScope effect（绑定激活）、activator 参数传递、getService 强类型代理（绑定对象改激活态）。
- 改动：`ai-dev/design/nop-plugin/` 6 篇文档全面修订（00 新增 §〇 定位反转裁决与目标收敛 5→3；01 单层六态状态机 + 接口契约重写 + §7.2 实例 API 删除去向表 + R1 源码锚点标注；02 E/I/H 改判 agent 层职责；03 单激活流示例；04 fiber"有意拒绝"论证；README 基调更新）+ 讨论记录追加第 17 轮 + roadmap 新增 R1-R4 重构 work items。
- 偏差：无。执行范围经用户确认（全面修订设计文档；实现代码重构另行立项）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 errors；首轮自查发现并修复 7 处相对路径错误——检查器将反引号内路径按仓库根解析）。
- Doc-sync: `docs-for-ai/03-modules/nop-plugin.md` 与 INDEX/source-anchors 的同步**有意推迟到 R4**（实现重构落地后随代码一起改，避免文档描述与现存实现脱节）；design 目录为本次 owner。

### Plan 344 Phase 2 完成：字符串快路径 + 诚实负结果记录

- **落地**：`TextScanner.nextJsonString()` 增加快路径 `tryNextSimpleJsonString`——序列前瞻定位终止符，无转义时一次性切片；被跳过字符仍经真实 `read0()` 消费保持底层 reader 同步（不破坏 ICharReader 抽象）；pos 与序列不对齐时防御性回落慢路径。**顺带修复存量缺陷**：`consumeDigits` 不更新 pos（破坏 "pos=序列绝对索引" 不变量，XDefCommentParser 已依赖该不变量做 subSequence 切片）。skipBlank 的 isSpace 谓词内联为同类私有方法。
- **Decision（loose 变体）**：不同步加快路径。理由：闭引号需前瞻判定 + "后跟非终止符的引号被丢弃" 既有语义使分支复杂；剖析证实字符串仅占热点 ~4%；loose 用于 DSL 配置加载非吞吐热路径。行为不变由新增回归测试锁定。
- **测试**：TestTextScanner 新增 8 个用例覆盖计划要求的 8 类边界（无转义/unicode 转义/CR-LF 报错/空串/超长/未闭合/数字后回归/loose 不变+簿记一致性）。调试中发现并修正两处测试自身对 API 语义的误用：match()=consume+skipBlank（含跨行跳过）、loose 闭引号必须后跟终止符否则报 NOT_END、后跟非终止符的引号是丢弃而非内容保留。
- **验证**：nop-commons 全套 199 tests / nop-core 230 tests 全绿；正式门禁 forks=3：xlang 415,674 ±7,239 vs 基线 423,608 ±12,246——误差重叠、降幅 1.9%<5%，判定无回归。
- **诚实负结果**：字符串快路径单独收益 ≈0。stack profiler 热点：LinkedHashMap.newNode ~19.6%（树构建分配，行为契约外）、skipBlank ~18.7%（含 safepoint 采样偏差）、nextJsonString 仅 ~3.9%。词法层天花板有限，≥30% aspirational 目标需结构性改造（已在 Deferred）。Phase 3/4 继续按计划执行（收益小但方向正确），Phase 5 将给出最终对照与差距解释。
- Doc-sync: No owner-doc update required

### GRC 科普文对照理论核修订：补齐形式核心缺口 + 公式排版压缩（无 plan，ad-hoc 文档任务）

- 对象：`ai-dev/articles/grc-universal-software-construction-theory.md`。先压缩 15 处逐行拆分公式为紧凑布局（短式单行、长式 `=` 引出续行、多方程对齐），再对照 `docs/theory` 理论核（paper-v2 / paper-review / theory-overview / misreading-grc）做内容修订。
- 修订内容：① §一 `F0(X0) + Δ` 改 `⊕ Δ` 并注明启发式 `+` 与合并运算 `⊕` 的区别；② 引言补出处（canonical 2007 提出、Nop 为参考实现）；③ §三新增"变化所在的空间本身有质量差异"（差量空间质量阶梯表 + "扩展点预判变化位置，GRC 设计变化空间"），原 1/2 小节顺延为 2/3；④ §四补结合律与差量预合并的数学前提；⑤ §六新增"删除为什么可以安全表达"（潜在结构空间/tombstone/S-N-V 阶段分离）与"从相同才可复用到相关即可复用"（B=A+(-C) 脉络），§六.1 补物理(Landauer)/逻辑(Bennett)可逆计算辨析；⑥ §七新增纵向多阶段生成链、横向多 DSL 图册、多重表象推导+修正三个小节；⑦ §八插入 SPLE 对照小节（原 7/8 顺延为 8/9）；⑧ §十一清单补生成器确定性、并发差量两项边界；⑨ 文末新增延伸阅读链接（相对路径指向 docs/theory 四篇权威文档）。
- 偏差：无。实施范围经用户确认（A + 全部 B）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 errors；5 个 warning 为 `ai-dev/plans/344-*.md` 存量问题，与本改动无关）。章节编号经 grep 复核连续（修复过一处漏改的双 `### 8`）。
- Doc-sync: No owner-doc update required（articles 为过程性文章，非规范文档；所引 docs/theory 未变更）

### Plan 344 Phase 1 完成：JMH 设施修复 + 正式基线固化

- 修复 `nop-benchmark/nop-benchmark-json/pom.xml`：显式配置 maven-compiler-plugin `<proc>full</proc>`。clean compile 后 `META-INF/BenchmarkList` 自动生成，`org.openjdk.jmh.Main "EishayParseTreeString.*"` 可直接启动（不再需要手工 javac -proc:full 补编译）。
- **正式基线固化**。判读规则：forks=3、wi=3、w=2s、i=5、r=2s；"无回归" = 均值差在两轮误差范围内重叠且降幅 <5%，"提升" = 超出误差上界；快速档（forks=1）仅开发中观察，不作为门禁。
  - 命令：`java -cp "nop-benchmark/nop-benchmark-json/target/classes:$(cat nop-benchmark/nop-benchmark-json/target/cp-json.txt)" org.openjdk.jmh.Main "EishayParseTreeString\.(fastjson1|fastjson2|jackson|xlang)$" -f 3 -wi 3 -w 2 -i 5 -r 2 -to 90`
  - 结果（thrpt, Cnt=15, ops/s）：fastjson1 744,360 ±5,497；fastjson2 **1,502,481 ±31,129**；jackson 918,208 ±28,339；**xlang(JsonTool) 423,608 ±12,246**
  - 结论：xlang = fastjson2 的 0.28x（慢 ~3.55x）、jackson 的 0.46x。环境：JDK26 Zulu 26.0.1 / macOS / forks=3。
  - classpath 生成：`./mvnw dependency:build-classpath -Dmdep.outputFile=target/cp-json.txt -pl nop-benchmark/nop-benchmark-json -o`
- 编译验证：clean compile 无新增 javac 警告（仅依赖 POM 的存量 effective-model 警告），BenchmarkList 正常生成。

### Plan 344 起草：Nop JSON parser/writer 快路径优化（含基准基线固化）

- Plan `ai-dev/plans/344-nop-json-parser-writer-fast-path.md` 完成 draft→active：目标是在不动公共契约（loose 语法/注释/SourceLocation/JObject delta）前提下为 JSON 词法层加快路径，Phase 1-5 全部以 `nop-benchmark-json` JMH 门禁验证。
- **基准基线实测**（本机 JDK26 Zulu 26.0.1，forks=1/wi=3/i=3 快速档）：EishayParseTreeString 吞吐 fastjson1 756K ops/s、fastjson2 1544K、jackson 965K、xlang(JsonTool) **429K——慢 fastjson2 约 3.6 倍**，与源文件内历史注释（zulu17/forks=5，3.5 倍差距）一致。瓶颈锚点已核实进 plan Current Baseline：TextScanner 字符串逐字符 append+数字拼串再 parse（`TextScanner.java:900-1008,1112`）、addEntry 双哈希判重（`BuildObjectJsonHandler.java:109`）、writer 标量 toString()（`CollectTextJsonHandler.java:390-416`）。
- **发现并记录设施缺陷**：JDK 23+ 默认禁用隐式注解处理，`nop-benchmark-json` 编译后无 `META-INF/BenchmarkList`，JMH main 无法启动；临时用 `javac -proc:full` 补编译跑通，pom 显式配置 proc=full 列为 Phase 1 修复项。
- 审查记录：两轮独立子 agent 对抗性审查（task ses_fd7404483ffewVbY4Xpl7WTMfE / ses_fd727aaacffemerRWir1AhDGU2）。第一轮修掉 2 Blocker（addEntry null 值歧义改写为行为规格；SourceLocation 惰性化因需 handler 声明机制先行设计而移入 Deferred）、3 Major（门禁补 nop-xlang 下游消费方、快路径双输入表示、JMH forks=3 判读规则）；第二轮确认无 Blocker，补 2 处文本修正（XNode addEntry 完全覆写的子类路由约束、Closure Gates 与 Deferred 的瓶颈点编号一致性）。
- 验证：`./mvnw clean compile -pl nop-benchmark-json -o` 通过；JMH 四项对比可复现（命令与数据见上）。受影响模块测试待执行期按 Phase 门禁运行。
- Doc-sync: No owner-doc update required（纯性能优化计划，无契约变更）
### Plan 344 Phase 3 第二单元：nop-biz 15 条 P1/P2/P3 处置（CrudBizModel/装饰器/校验器）

- Plan 344 Phase 3 第二单元 nop-biz.md（P1×2 P2×6 P3×7，P0=0）：14 修复 + 1 部分修复暂缓（P2-6），plan 条目勾选。
- P1：① `CacheActionDecorator.decorate` 两个分支完全相同（命中缓存也回源、从不写缓存）→ 命中直接返回缓存值；未命中回源后写入（异步 CompletionStage 经 thenApply 正常完成后写；null 结果不写——底层 Caffeine 不支持 null 且 get=null 即 miss；key=null 旁路缓存）；② `doCopyForNew` 对逻辑删除实体必抛 `ERR_BIZ_ENTITY_ALREADY_EXISTS`（源 id 指向存活记录被 findLogicalDeleted 误判）→ `buildEntityDataForSave` 增加 4 参重载（`recoverDeleted=false`），原 3 参 @BizAction 签名不变。
- P2：doCopyForNew 两处 copier 改 `newOrmEntityCopier(objMeta, context, delayedActions)` + `executeDelayedRelationActions`（writeMode=biz 关联不再静默丢弃，与 doSave 对齐）；OrmEntityCopier 租户列块前置 `objMeta != null`（多租户子实体 schema 未解析时不再裸 NPE）；doFindTreeEntityList 数据权限 action 由 findTreePage 归位 findTreeList（权限语义收敛，分页入口不变）；batchUpdate null 元素防御（ignoreUnknown 跳过 / 否则 UnknownEntityException）；addManyToManyRelations/removeManyToManyRelations/updateManyToManyRelations 补 `checkDataAuth(METHOD_UPDATE)`（权限收紧，堵住只读用户篡改关联）；updateByQuery/deleteByQuery 取满 limit 与 asDict 达上限时 WARN（nop.biz.*-truncated），循环分页/抛错/truncated 标志三方案需公共 API 契约裁定 → 暂缓。
- P3：DevStat.clearStats 改 @BizMutation（默认注册门控/SQL 模板暴露治理留产品决策）；ObjMetaBasedValidator simple schema validator 双执行改 else if（SimpleSchemaValidator 内已执行；_validate 对空值先行短路，null 语义不受影响）；BizObjectImpl.method_invoke 两处裸 IAE 改 `NopException(ERR_BIZ_ACTION_NO_SVC_CONTEXT)`（新增错误码；两参场景由误导的 too-many-action-args 修正为缺服务上下文）；isAllowGetDeleted 对 getObjMeta() 判空（无 xmeta 对象返回业务错误而非 NPE）；batchDelete 缺失 id 按原始入参回填返回集（不再写入 null 元素）；DownloadHelper os 提升方法级变量 catch 关闭（抽包内重载 `downloadZip(resource, zipTool, ...)` 供测试注入，公开 API 不变）；resolveQuery 空实现死方法删除（全仓库无调用方）。
- 红验证：备份修复后主文件到 `_tmp/nop-biz-red/` → `git checkout HEAD` 还原（新错误码与 DownloadHelper 重载以编译 shim 补齐、保留 bug 本体）→ 17 处失败形态与审计一致：缓存命中仍回源 `<result-1> but was <result-2>`+缓存恒空+异步不入缓存、copyForNew `nop.err.biz.entity-already-exists id=src-1`、多对多 updateChecks `expected 3 but was 0`、findTree `<findTreeList> but was <findTreePage>`、batchUpdate/batchDelete NPE+null 元素、租户 NPE `IObjSchema.getProp because objMeta is null`、validator `expected 1 but was 2`、两参/无上下文 IAE 逐字吻合、deleted_get NPE、clearStats 注解 null、os 未关闭 `expected true but was false`。P2-1 分阶段红：仅打 P1-2 补丁（剥离源 id）保留单参 copier 的中间态下 testCopyForNewExecutesBizRelationAction 以 `expected: <1> but was: <0>` 失败，证实独立缺陷。免测试注明理由：P2-6 warn 日志（无数值语义）、P3-7 死代码删除（无调用方）。
- 验证：`./mvnw test -pl nop-service-framework/nop-biz` **48 tests / 0 failures**（新增 5 测试类 TestCacheActionDecorator×4 / TestCrudBizModelCrudFlow×4 / TestCrudBizModelBatchAndTreeQuery×4 / TestBizObjectImplMethodInvoke×3 / TestDevStatBizModel×2 + 既有类扩展 TestOrmEntityCopierWriteMode+1 / TestObjMetaBasedValidator+1 / TestDownloadHelper+1，手工 fake 全覆盖无数据库依赖）；公共行为变化（CrudBizModel 基类 copyForNew/batch/tree auth/validator）追加回归 `./mvnw test -pl nop-sys/nop-sys-service,nop-auth/nop-auth-service,nop-wf` **sys 7 / auth-service 393 / wf 8+3 全绿**。报告 15 条全部标注（grep `^### \[P[123]\]` 15 = 处置标注 15）。
- Doc-sync: No owner-doc update required（缺陷收敛与契约实现修复；findTreeList 权限归位与多对多 update 检查是消除越权/契约漂移方向的收敛，无文档记载旧值）。

### Plan 344 Phase 3 首单元：nop-api-core 19 条 P1/P2/P3 处置（跨模块公共 API，最小缺陷收敛）

- Plan 344 Phase 3 第一单元 nop-api-core.md（P1×5 P2×11 P3×3，P0 已在前批处置）：16 修复 + 2 部分修复暂缓（P2-6/P2-8），Phase 3 status → in progress、本条目勾选。
- P1：① `ApiRequest.cloneInstance` 把 `new HashMap<>(properties)` 赋给 `this` 而非 `ret`（克隆体丢全部 properties + 源对象被原地替换）→ 改赋 `ret.properties`；② `OrderFieldBean.cloneInstance` `setDesc(!desc)` 克隆取反 → `setDesc(desc)`（反转语义归 `reverse()`，QueryBean 克隆 orderBy 传播路径同修复）；③ `ConvertHelper.monthDayToString` day 段误用 `getMonthValue()`（12-25 序列化为 12-12）→ `getDayOfMonth()`；④ `ConvertHelper.localDateTimeToMillis` 用 `getRawOffset()` 与反向 `Timestamp.toLocalDateTime()`（含 DST）不对称 → 改 `value.atZone(ZoneId.systemDefault()).toInstant().toEpochMilli()`（Europe/Berlin 夏季往返偏 1 小时实证）；⑤ `ContextTaskQueue.flush` 退出窗口 check-then-act 竞态（最后一次 poll-null 到 endProcess 清 processingThread 之间，其他线程 enqueue+flush 因 processingThread 仍占用而放弃，任务滞留）→ 新增 `endProcessIfIdle()`：锁内原子判定"队列空才清 processingThread 退出，非空继续 drain"，`flush` 重构为 `do { flushTasks(); } while (!endProcessIfIdle())`；happens-before 论证：enqueue 检查与退出判定在同一把锁上互斥，滞留情形不可能再出现。
- P2：`ErrorBean.cloneInstance` 补 `setForPublic`（cause/details 共享浅拷贝裁定维持现状，与 params 共享一致，深拷贝属克隆策略设计决策）；`ApiResponse.cloneInstance` 补 bizFatal+tryResponse；`QueryBean.rightJoin` 误传 `JOIN_TYPE_LEFT_JOIN` → `JOIN_TYPE_RIGHT_JOIN`（全仓库无调用方，零影响）；`addJoin` dimFields 死分支（`List<String>` vs `String` 恒不等）→ 归一化 `Objects.equals(dimFields, leftProps)` 激活结构化路径；`FilterBeans.or` 空析取返回 `alwaysFalse()` + null 元素容忍（同族 `or(List)` 一并修；主代码调用方逐点核查：inRanges/ConditionExprHelper 有守卫，OrmBatchConsumer/DaoQueryHelper 空列表语义修复后才是正确的"匹配零行"）；`ContextProvider.thenOnContext0` whenComplete 回调 try/catch，context 已关闭时 `promise.completeExceptionally(e)`（原异常进被丢弃的依赖 stage，promise 永不完成）；`toFalsy` `d == Double.NaN` 恒 false → `Double.isNaN(d)`；`FieldSelectionBean._flatten` 从不 add → 补 `ret.add(点分路径)` + 子级前缀 `name + "."`；`SysConverterRegistry` 两 Map 改 final ConcurrentHashMap + unknownTypeConverter volatile。暂缓 2 条：P2-6 `registerInstance` 契约（已修 volatile + javadoc 两段式如实化；API 重设计 unregister/replaceInstance 需公共 API 签名裁定）；P2-8 `executeBlocking` 忽略 ordered（已修 javadoc 如实化降级实现；引入 worker 池改变测试/非 vert.x 运行时时序假设，需设计裁定）。
- P3：注册点静态字段加 volatile×5（ContextProvider._instance/AppConfig.s_provider/JSON.s_provider/CoreMetrics.s_clock/NopException.s_errorMessageManager），公共静态常量加 final×5（IntRangeBean.EMPTY/SourceLocation.UNKNOWN/MultiCsvSet.EMPTY/ApiStringHelper.INVALID_DATE/FUTURE_DATE，grep 实证无重新赋值点）；`IntRangeSet` 空集防护（compact `size()<=1` 返回 this；getFirstBegin/getLastEnd 抛 NopException(ERR_CHECK_INVALID_POSITION_INDEX) 替代裸越界）；`ApiMessage` headers 懒初始化 → volatile + getHeaders 双重检查锁 + setHeader/addHeadersIfAbsent 第二、三条非同步初始化路径统一串行化（超审计新发现），保留 TreeMap——实证 `ApiHeaders:454` setHeader(HEADER_BIZ_FAIL, null) 依赖 null 值，排除 ConcurrentSkipListMap。
- 红验证：17 处失败形态与审计一致（12-12、alwaysTrue/NPE、null 丢字段、leftJoin、[] 空集、AIOOBE、13:00 DST 偏 1h、TimeoutException 挂起等）；P1-5 竞态用反射取内部 ReentrantLock 在处理线程进入退出临界区前冻结锁，把纳秒级窗口放大为**确定性**复现（t1 阻塞任务 + marker 任务证明最后一次 poll 已 null）；headers 并发首写压力测试 round=0 即红（expected 4 but 2），中间形态还实证了并发 put 损坏 TreeMap 的 `NPE: Cannot read field "left"`。免红测试注明理由：P2-11/P3-1（数据结构替换与可见性加固无数值语义）、P2-6/P2-8 javadoc 部分。
- 验证：`./mvnw test -pl nop-kernel/nop-api-core` **80 tests / 0 failures**；公共行为变化追加回归 `./mvnw test -pl nop-kernel/nop-api-core,nop-kernel/nop-commons,nop-kernel/nop-core,nop-kernel/nop-xlang` **commons 220 / core 252 / xlang 559 全绿**；MonthDay 消费方 `nop-job/nop-job-core`（与 api-core 同 reactor）**39 tests 绿**。报告 19 条全部标注（grep `^### \[P[123]\]` 19 = 处置标注 20，P0 既有 1）。
- 超出审计新发现（3 项，均已在标注记录）：setHeader/addHeadersIfAbsent 各自独立的非同步懒初始化路径（已修）；or(List) 同族空参数恒真（已修）；header null 值契约实证（ApiHeaders:454/ApiMessage:120）排除了并发 Map 方案。
- Doc-sync: No owner-doc update required（缺陷收敛与防御加固；or 空析取语义修正是消除"过滤条件被绕过"方向性隐患，无文档化契约记载旧值）。

### Plan 344 Phase 2 完成：内核与核心六单元 P1/P2/P3 全量处置（103 条）

- Plan 344 Phase 2（kernel-small / nop-commons / nop-core / nop-core-framework / nop-utils / nop-xlang 六报告共 103 条）全部处置完毕，Phase 2 status → completed。批次 commit：kernel-small `b4da717424`、nop-commons `22129fa9f7`、nop-core `462a64d93d`、nop-core-framework `4ffaebb267`、nop-utils `b7b1b9d037`、nop-xlang 本次提交。四类终态：88 修复 + 8 暂缓 + 3 非问题/子项非问题 + 4 局部不修复裁定（详见各报告逐条标注，六报告 grep 对账 findings=annotated）。
- nop-xlang 单元（14 条 = 10 修复 + 2 暂缓 + 1 不修复 + 1 子项非问题）：feature:off 误用 onAttr 求值（P1，else-if 补全）；XDefMergeLoader 重复引用悬空 ref（refPathDefineNames 复用首名）+吞异常补 WARN；XDslExtender yaml null def 抛 ERR_XDSL_NO_SCHEMA；DeltaMerger uniques 死分支删除；LazyCompiledFunction volatile×2+依赖收集异常守卫（缓存异常重抛判非问题——Java 重抛不覆盖构造期堆栈）；TypeDefinitionParser 裸异常转 NopEvalException（新错误码×2）；ObjMetaRefResolver source 笔误；validateUnion null 值 NPE（静态 BeanTool.getProperty）；DslModelHelper Excel 裸 IAE 转错误码；XPathHelper 编译缓存对齐 JPath.compileWithCache。暂缓：defTypeCache loc 漂移（需统一 loc 重绑定设计）、唯一键空键语义（需契约方向裁定）；不修复：SimpleExprHelper loc=null（缓存设计取舍）。
- **环境敏感测试坑**：既有 `testVirtualNode` 用裸名 `test` 作 feature 变量，与 maven `-Dtest=...` 注入的系统属性冲突，surefire 过滤运行时 `AppConfig.var("test")` 读到 truthy 导致偶发失败（stash 对照证实与 P1 修复无关）；改名 `nop.test.flag-not-set` 修复。
- 红验证：nop-xlang 6 条（P1 两测试分别呈现审计推演的两种错误形态、P2-1 悬空名 MergeRefTarget1、P2-3 NPE、validateUnion NPE、xpath 两实例）；其余五单元红验证数见各自日志条目（16-22 处不等）。
- 验证：`./mvnw test -pl nop-kernel/nop-xlang` 559/0/0（+1 新增）+ nop-core 回归绿；Phase 2 六单元累计测试数见 plan 条目摘要；i18n 聚合本单元补 3 条。
- Doc-sync: No owner-doc update required（缺陷收敛与防御加固，无文档化契约语义变更；唯一键空键契约裁定已在报告标注记录决策点）。

### Plan 344 Phase 2：nop-utils 单元 21 条 P1/P2/P3 处置（八子模块，全部修复）

- Plan 344 Phase 2 第五单元 nop-utils.md（P1×7 P2×7 P3×7，P0 已在前批处置）：**21 条全部修复**，无暂缓/不修复。
- P1：① `GitRepositoryImpl.getWorkingTreeDiff` 重写为 HEAD 树 vs `FileTreeIterator` 工作区 diff（原把 index 首条 blob 当 tree reset 进 `CanonicalTreeParser`，有 index 必抛 not-a-tree、空 index NPE；reader/RevWalk 纳入 try-with-resources）；② `ShellRunner` 写完 inputBytes 后 finally 关闭进程 stdin（读 stdin 到 EOF 的命令不再永久挂起）；③ `ModelBasedTableValidator.validateRow` 把行数据（columnNames×rowAdaptor + rowIndex）放入 scope——有外部上下文时 `scope.newChildScope(rowVars)`（行数据覆盖同名变量、外部变量仍可见、不再污染外部 scope），删除死方法 buildRowScope；④ `TextDiffUtils` 多 change 行号修复（探针实证根因更深：Myers 产出**逆序** change 列表且相邻 DELETE 未合并，generateHunks 整体错乱——修复=正序排序 + 前缀偏移累计（按 revisedSize-originalSize 求和，不能用 endRevised-endOriginal 逐个累加）+ hunk 合并重建为横跨两端的 CHANGE 块）；⑤ `JavaParserCodeFormatter` 改每次 format 新建 JavaParser（共享不可变 ParserConfiguration）；⑥ `Trie.match/matchAll/matchAllValues` 空 path 检查 rootNode 值（"/" 注册后可匹配）；⑦ `addMatchAll` 改 `"{*path}"` 通配段（原字面段 `*path` 建 exact 子节点永不匹配）。
- P2：`splitCommandLine` 反斜杠语义重写（单引号内字面量；引号内外仅转义 `\\ \" \'`；其余保留字面量——Windows 路径不再被吃；悬空反斜杠抛 IAE）；`OsUserHelper`（charAt(i)→(j)、getGroup 改读 getOutput + 按 "Local/Global Group Memberships" 标签定位解析（新纯函数 parseWindowsGroupFromNetUserOutput，找不到抛 IOException）、create* 统一 checkRunResult 校验退出码）；createMacUser 日志 -password 脱敏 + `getSudoCmd` tenantCode 白名单（[A-Za-z0-9_-]，注入拒绝）；`existsRemoteRepository` catch 补 LOG.warn；`BetweenMatchPattern`/`AlwaysFalseMatchPattern` 补 `.addToCollector(state.getErrorCollector())`；`UnifiedDiffApplier.normalizeLine` 默认保留原行/ignoreTrailingWhitespace 仅去行尾（fuzzy 定位分离出独立 fuzzyNormalize 全 trim 且命中后不重复 strict 校验，与既有 fuzzy 测试语义调和）；`ImageCompressHelper.resizeImage` 铺白底 + `bufferedImageToBytes` 检查 `ImageIO.write` 返回值。
- P3：cloneRepository 先记日志后 close；getFileContent/getChangedFilesBetweenCommits resolve 判 null 抛 `ERR_GIT_INVALID_COMMIT_ID`；DesktopHelper 先 isDesktopSupported 后 getDesktop；TriePathRouter 重复 import 删除；applyHunkToText 空 if 改 if/else 链 + context 行从原文件复制（与 apply() 对齐）；ImageTypeMap 三 Map 改 ConcurrentHashMap；ShellRunner catch 先判 stopped.get() 归类 TIMEOUT。
- **超出审计新发现（已修复 6 处）**：`GitRepositoryImpl.commit` 传 null email 给 JGit PersonIdent 必抛 IllegalArgumentException（commit 本身不可用）→ 作者名派生占位邮箱；`RowWiseCollector.addError` 对 `ModelBasedValidator` 传入的 `Collections.emptyMap()` 直接 put 必抛 UnsupportedOperationException（行级报错即崩）→ 复制可变 Map；`TableValidatorCompiler` 不解包 `<condition>` 包裹节点 → 求值抛 `ERR_FILTER_UNKNOWN_OP op=condition` → unwrapCondition（单子节点解包/多子节点包 AND）；Myers 逆序 + hunk 合并重建丢改动（随 P1-4）；`Trie._match` 精确子树死路不回退祖先层 tillEnd 通配（网关默认路由+精确路由混合场景仍 404，随 P1-7）；`OsUserHelper.getGroup` 读 `.getError()` 但 runCommand 开了 redirectErrorStream → 恒空必 AIOOBE（改读 getOutput，随 P2-2）。
- 红验证：备份修复后主文件到 `_tmp/red-verify/` 再 `git checkout HEAD` 还原逐模块验证，17 处失败形态与审计一致：router 3（matchPath null×2 + NPE）、match 2（collector 0 errors）、diff 4（hunk 数错/context-mismatch/nothing thrown/header 空白丢失）、shell 6（cat stdin 挂起 10s 超时 ERR_TIMEOUT、C:\tools\bin→C:toolsbin ×3、悬空反斜杠不抛、getSudoCmd 注入 nothing thrown）、git 4（not-a-tree ×2、NPE id is null ×2）、table-validator 3（UnsupportedOperationException×2 + unknown-op）、image 1（透明像素 rgb=0,0,0）、java-parser 1（并发 3/3 必失败，token manager Index -1 out of bounds——审计预判"难以复现"实为确定性复现）。免红测试 4 条注明理由（Windows 平台路径/headless 环境约束（argLine 未传播，本机跳过）/时序竞态/纯日志与语句顺序调整）。
- 验证：`./mvnw test -pl nop-utils/nop-router,nop-utils/nop-match,nop-utils/nop-diff,nop-utils/nop-shell,nop-utils/nop-git,nop-utils/nop-table-validator,nop-utils/nop-image,nop-utils/nop-java-parser` **8 子模块 167 tests / 0 failures / 4 skipped（存量 @Disabled 网络测试 2 + @Disabled 全仓解析 1 + headless 条件跳过 1）**；公共行为变化（router 匹配语义、diff strict 语义）追加回归 `./mvnw test -pl nop-service-framework/nop-gateway,nop-ai/nop-ai-toolkit` **gateway 68 / ai-toolkit 202 全绿**。报告 21 条全部标注（grep `^### \[P[123]\]` 21 = 新增标注 21，另有 P0 既有标注 2）。
- 基建：nop-image 原无测试，pom 补 junit-jupiter test 依赖；plan 344 Phase 2 勾选 nop-utils 条目。
- Doc-sync: No owner-doc update required（均为缺陷收敛与契约实现修复；fuzzy/strict 语义调和是让实现符合 Config 注释既有语义，无文档化契约变更）。

### Plan 344 Phase 2：nop-core-framework 单元 15 条 P1/P2/P3 处置（nop-ioc/nop-config/nop-plugin/nop-security/nop-log）

- Plan 344 Phase 2 第四单元 nop-core-framework.md（P1×4 P2×5 P3×6，P0 无）：11 修复 + 3 暂缓 + 1 部分修复（javadoc 如实化 + 严格模式暂缓）+ 1 复查部分非问题（ProducedBeanInstance.STATUS_* 已 final）。
- P1：① `BeanConditionEvaluator` unless-property 分支误读 `getIfProperty()`——改按 `getUnlessProperty()` 自身字段求值（仅 unless 配置不再 NPE、if+unless 不再重复求值 if 条件）；② `ChangeSubscriptions.subscribePattern` 误注册到 simpleSubscriptions——改注册 patternSubscriptions（通配 pattern 订阅按 IConfigProvider 公开契约恢复触发）；③ 构造器循环依赖静默产生重复单例——`BeanDefinition` 新增 `inCreationThread` 标记（生命周期=createInstance 即构造器参数解析窗口，单例路径锁内标记/清除），`getBean0` 同线程重入即抛 `ERR_IOC_BEAN_DEPENDS_GRAPH_CONTAINS_CYCLE`（复用既有错误码 + beanName 定位；属性注入环/depends-on 环不受影响，TestReentrantCircularRepro 仍绿）；④ 文件配置源 `refreshConfig` 一次异常永久停止热刷新——整体 try-catch（构造期仍上抛 fail-fast）。
- **超出审计新发现（已修复）**：`AbstractFileConfigSource.refreshConfig` 成功刷新后未回写 `this.vars`（对比 JdbcConfigSource:102 有回写）——`getConfigValues()` 永远返回构造期快照，变更回调方重新拉取读到旧值（热更新实际不生效），且 isChanged 每轮与初始快照比较导致首次变更后每轮重复触发回调。随 P1-4 一并修复并补测试。
- P2：`loadPluginFromJar` 失败路径泄漏 PluginClassLoader（loadPlugin/getPluginConfig 纳入 try、catch 扩 `RuntimeException | Error`、finally 统一 safeClose）；插件僵尸容器三层修复（`destroySelf` deactivate+removeInstance 全程持 lifecycleLock；`doActivate` 入口校验注册表存在性，已销毁实例拒绝激活——新错误码 `ERR_PLUGIN_INSTANCE_ALREADY_DESTROYED`；manager `createInstance`/`destroyInstance` 实例变更段纳入 reconcileLock，锁序恒 reconcileLock→lifecycleLock）；`SecurityHelper.toRSAPublicKey` 裸 RuntimeException 改 `NopException.adapt`。
- 暂缓裁定：P2-6（getBean0 跨线程构造器环死锁——单线程确定性路径已随 P1-3 消除，残余需容器创建锁粒度设计决策：粗粒度锁串行化 concurrentStart vs wait-for 图环检测）；P2-9 严格模式（require-pinned-hash——hash 来源优先级系 ai-dev/design/nop-plugin/05-artifact-loading-design.md §五 W7 修订注解显式裁定，javadoc 信任模型已如实化：header/{url}.sha256 与 jar 同源仅防意外损坏、expectedHashes 是唯一独立信任源且优先级最低）；P3-15（条件求值 5 轮迭代上限——提高上限/超限抛异常均改变深链 bean 启停结果，属语义变更非缺陷收敛）。
- P3：`BeanScopeImpl` add/close 互斥（checkClosed+put 与 closed 置位同锁短临界区，销毁回调仍在锁外；终态校验裸 ISE 改 NopException）；`BeanDefinitionBuilder` 兜底分支错误码化（新错误码 `ERR_IOC_INVALID_PROP_VALUE_TYPE`，i18n zh-CN/en 按序同步）；`BeanDefinition.STATUS_*`×3 与 `BeanTopologySorter.INSTANCE` 加 final；`BeanScopeContext._instance`/`ConfigStarter.g_instance`/`LoggerConfigurator.s_instance`/`BeanContainerImpl.classIntrospection`（+懒初始化本地变量模式）/`VfsPluginDefinition.state` 加 volatile。
- 红验证：新增 6 个测试类 18 用例（TestBeanConditionUnlessProperty 5 / TestConstructorCycleDetection 2 / TestChangeSubscriptions 4 / TestFileConfigSourceRefresh 3 / TestPluginInstanceDestroyActivateRace 3 / TestSecurityHelperRsaKey 1），备份修复后主文件到 `_tmp/` 再 `git checkout HEAD` 还原逐批验证：16 处失败形态与审计一致（NPE getIfProperty null ×5 / 环静默通过 TestCtorCycleA 构造两次 ×2 / 通配订阅静默不触发 ×3 / 刷新异常上抛+快照旧值+重复触发 ×3 / 僵尸激活 nothing-thrown ×2 / bare RuntimeException InvalidKeySpec ×1）。P2-5（classloader 关闭副作用不可确定性观测）与 P3 加固类（volatile/final/竞态窗口/不可达兜底）免红测试，理由已写入报告标注。
- 验证：`./mvnw test -pl nop-core-framework/nop-ioc` **62 tests 绿**；`./mvnw test -pl nop-core-framework/nop-config,nop-core-framework/nop-plugin/nop-plugin-manager,nop-core-framework/nop-security,nop-core-framework/nop-log/nop-log-core -T 1C` **config 30 / plugin-manager 166 / security 2 全绿**；回归 `./mvnw test -pl nop-runner/nop-cli-core -Dtest=TestGenErrorCodesYaml`（i18n 聚合校验绿）+ `./mvnw test -pl nop-auth/nop-auth-service`（IoC 容器重下游，**393 tests 绿**）。报告 15 条全部标注（grep `^### \[P[123]\]` = grep `处置（fix-ai-check` = 15）。
- Doc-sync: No owner-doc update required（均为缺陷收敛与契约实现修复，无文档化契约语义变更；构造器环 fail-fast 与文档既有 `ioc:ignore-depends` 指引一致）。

### Plan 344 Phase 2：nop-core 单元 17 条 P1/P2/P3 处置（框架核心引擎，全部修复）

- Plan 344 Phase 2 第三单元 nop-core.md（P1×3 P2×4 P3×10，P0 已在前批处置）：**17 条全部修复**，无暂缓/不修复。
- P1：① `IFile.getResource("..")` 目录逃逸——根因在 nop-commons `StringHelper.isCanonicalFilePath` 对整段 `"."`/`".."` 漏判（跨模块联动修复，4 个调用方复核均为 fail-safe 收紧）；② `ResourceComponentManager.loadComponentModelByUrl` 的 `?` 分支复制粘贴错误——按接口文档解析 `?paramName=paramValue` 查询参数（`StringHelper.parseSimpleQuery`），识别 `transform`/`sub`（常量 `COMPONENT_URL_PARAM_*` 落在 `IResourceComponentManager`），`#` fragment 保留；③ `ResourceHelper.resolveResourceInDir` 裸 `..` 绕过——fileName 非空时改 `isCanonicalFilePath` 整体校验 + `NopException(ERR_RESOURCE_INVALID_FILE_NAME)`。
- P2：`ReflectionManager.unregisterTypeConverter` 改操作 `registeredConverters`（原误清 `converterCache`）+ 同步清缓存；`CoreInitialization.getDisabled` 判空 `bootstrapConfig`（reinitialize 独立调用/destroy 后调用不再 NPE）；`FileResource.mkdirs` 条件纠正（仅"已存在且非目录"抛 `ERR_RESOURCE_NOT_DIR`，不存在→正常创建）；`SqlLikeUtils` 三个异常工厂（含同类私有 `invalidRegularExpression`）NopException 化（新错误码 ×3）。
- P3 全修 10 条：`ResourceCacheEntry.reloadObject` 对齐 `getObject` 的 `Null.NULL` 占位 + 跳过 `onCreated(null)`；`ClassPathResource.initLength` finally disconnect（HttpURLConnection）+ DEBUG 日志；`AopCodeGenerator` 生成代码 catch 改抛 `NopException(ERR_REFLECT_AOP_INIT_METHOD_MODEL_FAIL, e)` fail-fast（期望附件 MyModel__aop.java 同步）；`DefaultVirtualFileSystem` 字段 volatile + `destroy()` synchronized；`ResourceDependencySet.lastModified` volatile；`ResourceHelper.readBytes` 超 `Integer.MAX_VALUE` 抛 `ERR_RESOURCE_READ_BYTES_TOO_LARGE`（原 `(int)` 截断 → NegativeArraySizeException）；`ErrorMessageManager.clearErrorCodeMappings` 补 `subMappings.clear()`；`DeltaResourceStore.getRawResource` 租户路径无子路径抛 `ERR_RESOURCE_INVALID_PATH`（原 SIOOBE Range [9,-1)）；`ReflectionManager.ClassModelLoader` 记忆化 + 锁内 double-check（并发 miss 不再重复构建 ClassModel）；`ResourceHelper.resolveRelativeResource` 裸 IAE → `NopException(ERR_RESOURCE_NOT_ALLOW_PARENT_PATH)`。
- 新增 CoreErrors 错误码 7 个（aop-init-method-model-fail / invalid-file-name / not-allow-parent-path / read-bytes-too-large / like-invalid-escape-char / like-invalid-escape-sequence / similar-invalid-regex），nop-cli-errors i18n zh-CN/en 按错误码排序同步（en 按文件惯例加引号；zh 既有 17 处含 ": " 的未引号条目为前批存量，严格解析器拒绝但平台加载器容忍，本次未动）。
- 红验证：9 个测试文件（新建 6 个：TestSqlLikeUtils/TestReflectionManager/TestCoreInitialization/TestResourceCacheEntry/TestErrorMessageManager/TestDeltaResourceStore/TestResourceComponentManager，扩展 3 个：TestResourceHelper/TestAopCodeGenerator/nop-commons TestStringHelper）全部在修复前代码先行运行，17 处失败形态与审计逐一吻合（getResource("..") 无异常返回父目录 / transform=资源路径本身抛 undefined-transform / 裸 IAE ×2 / NegativeArraySizeException -2147483648 / StringIndexOutOfBoundsException Range [9,-1) / RuntimeException ×3 / reload 后 loadCount 2→3 / clear 后 subMapping 仍命中 / 并发构建出两个 ClassModel 实例 / NPE / printStackTrace 生成 / mkdirs 抛 not-dir）；修复后全绿。P3-2（连接释放+日志）、P3-4/P3-5（内存可见性）3 条为加固类免测试，理由已写入报告标注。
- 验证：`./mvnw test -pl nop-kernel/nop-core` **252 tests / 0 failures / 2 skipped（存量）**；公共行为变化（StringHelper/isCanonicalFilePath 收紧、资源校验、错误类型）追加回归 `./mvnw test -pl nop-kernel/nop-commons,nop-kernel/nop-xlang,nop-kernel/nop-dataset`（先 install 修复后 commons）**全部 SUCCESS（xlang 551 tests 绿）**。报告 17 条全部标注（grep `^### \[P[123]\]` 17 = 处置标注 17，另有 P0 既有标注 1）。
- 超审计观察：无新缺陷；zh-CN i18n 聚合文件的 17 处严格 YAML 不可解析条目为前批存量（详见上节说明）。
- 提交钩子 ast-grep 门禁：本次触碰文件中两处前批存量空 catch 被拦（`ReflectionManager` isRecord 探测静态块 / `TestStringHelper#testSha256` 的 Mac 计算块），最小整改：前者补显式 `m = null` 降级赋值+注释，后者将手动 Mac 哈希从打印改为与 digest 断言相等、异常改 `fail()`（顺带强化了该测试）。
- Doc-sync: No owner-doc update required（均为缺陷收敛与契约实现修复，无文档化契约语义变更；`IResourceComponentManager` 仅补参数名常量与注释）。

### Plan 344 Phase 2：nop-commons 单元 20 条 P1/P2/P3 处置

- Plan 344 Phase 2 第二单元 nop-commons.md（P1×6 P2×8 P3×6，P0 无）：17 条修复 + 2 条暂缓 + 1 条子项改判非问题（MapCache async）。
- P1 全修：`Lazy.get()` 补 `loaded = true`（含 null 值缓存语义）；`LocalFileLock` 恒 false 返回改 `lock != null` + 异常路径句柄清理（releaseHandles）；`LocalResourceLockManager.tryResetLease/isHoldingLock` 租约条件反转修正 + `ResourceLock.tryResetLease` 空锁 NPE 判空；`FileHelper.writeTextWithLock` truncate 改字节数（中文内容不再截断，与上游 Nacos 对齐）；`NetHelper.findLocalIp` 改走 `LOCALHOST4()` 懒初始化访问器；`LocalCache.putIfAbsent` 改 Caffeine `asMap().putIfAbsent` 原子语义 + `ICache` 接口 javadoc 声明原子性契约。
- P2 修 6 缓 2：throttle 改 `promiseMap.compute` + 任务结束自清理（节流窗口正确重开、无 map 泄漏）、debounce 同步补自清理；`RoundRobinSupplier` 扩容失败从旧长度关闭新建资源；`DefaultRateLimiter.getAcquireFailCount` 返回正确字段；`SequentialTaskExecutor.getTotalExecutionTime` 按纳秒返回 + 字段 volatile；`GlobalCacheRegistry.register` 改 putIfAbsent（失败不再半生效）；`IoHelper.getEncodingFromBOM` 短流部分读取 + 按实际字节数分档匹配；`DateHelper.buildFormatter` ConcurrentHashMap + computeIfAbsent 回填。暂缓：JavaSerializer ObjectInputFilter（同时是 serializeClone 任意类型深拷贝默认实现，白名单必破坏之；JDK `jdk.serialFilter` 全局机制已可用，需独立安全设计裁定）；ExecutorHelper `config.setMaxPoolSize(0)` 突变（live code 复核证实是 DefaultScheduledExecutor.refreshConfig/updateThreadPool 共享 config 的承重约定标记，去除或 clone 均改变刷新语义，需归一化所有权设计）。
- P3 修 5 + 1 子项非问题：FileHelper 资源缺失 NPE 改 `NopException(ERR_FILE_NOT_FOUND)`（复用现有错误码）；裸 RuntimeException×2 改 `NopException.adapt`；静态字段 volatile×4（IoHelper 序列化器×2/GlobalMeterRegistry/ExecutorHelper.SYNC_EXECUTOR）；MutableInt "Atomically" javadoc 纠正 + HighWatermarkSemaphore 统计计数补累加；StringTrie 空串 find 返回 null + add 空键 Guard 拒绝。**改判非问题**：MapCache async 跨线程暴露——审计前提 "futureCall 在公共线程池执行" 有误，`FutureHelper.futureCall/futureRun` 实为调用线程同步执行（FutureHelper.java:122-138），补探针测试 `TestMapCache.testAsyncOnNonThreadSafeRunsSynchronously` 实证并作回归守卫。
- 红验证：先写 16 个测试文件（新建 8 个 + 扩展 8 个）在修复前代码上运行，22 处失败形态与审计逐一吻合（Lazy supplier 执行 3 次、lock() 获锁成功仍抛 acquire-lock-timeout、"中文内容测试123"截断为"中文内"、putIfAbsent 第 63 轮双 true、throttle count=4、promiseMap 残留、totalExecutionTime 1.3e14、短流 unexpected-eof、SIOOBE Index 0、信号量计数恒 0 等），修复后全绿；P1-5 findLocalIp 红验证不可行（需无网卡且 getLocalHost 抛 UnknownHostException 的环境）已在报告标注中说明，以代码级复核为依据。
- 验证：`./mvnw test -pl nop-kernel/nop-commons` **219 tests / 0 failures**；公共行为变化（Lazy/LocalCache/StringTrie/NetHelper/GlobalCacheRegistry/DateHelper）追加回归 `./mvnw test -pl nop-kernel/nop-core,nop-kernel/nop-xlang`（先 `install -DskipTests` 安装修复后 commons）**core 235 + xlang 551 tests 全绿**。报告 20 条全部标注（grep `^### \[P[123]\]` = grep `处置（fix-ai-check` = 20）。
- Doc-sync: No owner-doc update required（均为缺陷收敛与契约实现修复，无文档化契约语义变更；P2-7/P3-2 暂缓决策点已写入报告标注）。

### Plan 344 Phase 2 启动：kernel-small 单元 16 条 P1/P2/P3 处置

- Plan 344 Phase 2 首单元 kernel-small.md（P1×2 P2×8 P3×6，P0 已在 08-22 上午批次处置）：13 修复 + 2 非问题改判 + 2 局部不修复裁定。
- 修复要点：markdown 空文档 NPE（parseRootSection null 守卫）/parseMappingTable 表头当 key/removeStyle 长度守卫；dataset `projectWithRename` 用既有 `renameTo` 复制改名（ProjectDataSetMeta 崩溃路径根因在内层，随之修复）/`hasField` 按投影字段遍历/`defaultReadBatch(0)` 不再吞一条；javac `findClass` 抛 CNFE/fileManager finally close/`getErrorDetail` 防御加固；codegen `GenAopProxy` try-with-resources/`CodeGenTask` printStackTrace→LOG.error/`ClassRenamer` 正则改平衡括号扫描（注解参数含 `)`/`"`/`$` 不再损坏）；record-mapping 键引号 roundtrip（`findQuotedStringEnd` 与 `StringHelper.quote` 的 escapeJava 对称 + `decodeKey` unquote）+ 两处裸 IAE 转错误码（md-table-field-no-item-mapping / null-mapping-name）+ `MarkdownHelper` 两处（pos-list-size-not-match）。
- **改判非问题 2 条**：① 表格尾空单元格——探针实证 `| a | | |` 解析为 3 cells（审计断言 2 有误），7 种行模式与 GFM 语义逐一对照一致，审计建议的修法反而会破坏尾管道风格 roundtrip，补特征化测试固化；② `getErrorDetail` 越界——反编译 janino `LocatedException.getMessage()` 证实恒为 `locStr + ": " + raw`，越界路径不可达，保留防御检查（`>=` 与原可达行为逐位一致）+ 测试固化。
- **超审计新发现**（已修）：`addImageSummarization/changeLinkUrl` 的 `Collections.sort` 只排 posList 不排并行 infos/newUrls，乱序输入时区间与替换值配对错乱；改为 `sortedOrder` 索引副本排序，顺带消除入参突变。
- 不修复裁定：KernelCliValidateCommand verbose 模式 stderr 打印堆栈（CLI 无日志框架、用户显式开关）；TableToMarkdownConverter 的 null IAE（Guard 风格编程契约违规）。
- 红验证抽查：临时还原 6 个修复前主文件，markdown 6 项新测试失败形态（NPE/越界/2-entry map）、dataset 4 项（rename 失效/unknown-column 崩溃/hasField 误 true）、record-mapping 3 项（`"a` 引号残留）均与审计预测一致；恢复后全绿。
- 验证：`./mvnw test -pl nop-kernel/nop-markdown,nop-kernel/nop-dataset,nop-kernel/nop-javac,nop-kernel/nop-codegen,nop-kernel/nop-record-mapping` 156 tests / 0 failures（77+11+5+14+49），kernel-cli 回归 BUILD SUCCESS；i18n 聚合补 3 条（record ×2 + markdown ×1）。
- Doc-sync: No owner-doc update required（缺陷收敛与工具加固，无文档化契约语义变更）。

### Plan 344 Phase 1 完成：check 审计持久层五单元 P1/P2/P3 全量处置（92 条）

- Plan `ai-dev/plans/344-check-audit-p1-p2-p3-remediation.md` Phase 1（nop-dao / db-migration / nop-orm / orm-periph / nop-orm-eql 五报告共 92 条 P1/P2/P3）全部处置完毕，Phase 1 status → completed。四类终态分布：87 修复 + 4 部分修复暂缓 + 3 暂缓 + 3 非问题（详见各报告逐条"处置（fix-ai-check 分支，2026-08-22）"标注，grep 验证五报告 findings 计数 ≤ 标注计数）。
- 批次 commit：nop-dao `3f1111078d`（13 修复 + QuerySpaceEnv.leave 恢复语义超审计修正）、db-migration `5737e7dbb5`、nop-orm `640cf0b359`（GenSqlHelper 排序 NPE 改判非问题）、nop-orm-eql `58e934a36f`（CTE recursive 判非问题 + NOT-ILIKE 同族新发现修复）、orm-periph 本次提交（22 条全部修复）。
- orm-periph 要点：TDengine SQL 生成三缺陷（TAGS 取列模型对象当值/VALUES 恒空/load 缺 FROM+逗号连接主键）、delete/count 漏 querySpace、update 静默丢失改 fail-loud（`ERR_TDENGINE_UPDATE_NOT_SUPPORTED`，TDengine 无 UPDATE 语句）、子表名白名单 `^[A-Za-z0-9_]+$` 防注入；Rpc 驱动 batchModify/batchDelete 补 checkResponse + batchLoadAsync 改按主键匹配（复合键 `COMPOSITE_PK_SEPARATOR`+encodeDupEscape）；`DaoEntityBlockingSource` 无限等待/中断语义/转移计数三修正；`LazyLoadOrmModel` putEntityModel 入锁消竞态 + 表名索引归一化大小写；geo/pdm/tdengine 三模块错误码体系新建（TdEngineErrors/OrmGeoErrors/PdmModelErrors）+ `ERR_ORM_MODEL_JOIN_COLUMNS_NOT_MATCH_PK`。
- 红验证抽查（orm-periph）：临时还原修复前主代码跑新测试——`TestDaoEntityBlockingSource` 3 条全挂（take 抛 `NonPositive:timeout,value=-1`、中断被吞、计数含预置元素）、`TestTdSqlHelper` 3 条全挂（输出呈现 `TAGS Mock for IColumnModel`、空 `VALUES()`、`select ts,value where ts=...,...` 无 FROM），与审计预测形态一致；恢复后全绿。
- 顺带：nop-cli-errors i18n 聚合补 5 条（本战役前四次提交遗漏的 4 条 + 本次 join-columns-not-match-pk；zh/en 同步）。
- 验证：orm-periph 六模块 `./mvnw test -pl nop-persistence/nop-orm-model,nop-orm-drivers/nop-orm-tdengine,nop-orm-pdm,nop-orm-rpc,nop-orm-data,nop-orm-geo` 40 tests / 0 failures / 0 errors；其余四单元测试数见各自报告标注。
- 偏差：前四次批次提交时未同步当日 dev log（会话中断遗留），本条目合并补记。
- Doc-sync: No owner-doc update required（均为缺陷收敛，无文档化契约语义变更）。

### check 系列 P0 清零收尾：上轮修复遗留问题三处 + 下游基线同步

- 上轮（08-21）六单元修复存在未经全量构建验证的遗留问题，本次全量构建逐一暴露并处置：
- **nop-file-dao 编译损坏**：`46575c4278` 在 `DaoResourceFileStore` 引入了三处编译错误（`IEntityDao` 无 `findListByQuery`、`NopException` 无单 String 构造），该模块当时从未被编译。已修正为 `findAllByQuery` + 模块内 ErrorCode 常量（`nop.err.file.invalid-biz-obj-name`/`invalid-file-ext`，FileErrors 在外部构件 nop-biz-file-core 中无法追加），保留原修复意图（commit `21f78d8eb5`，file-retry-tcc.md 已补记）。
- **nop-report-demo 12 个用例失败**：临时 worktree 在分支基点 `29d2fb5f7d` 复测证实为上轮存量回归（与本次 32 条 P0 修复无关）。根因 = `OfficePackage.cloneInstance` 深拷贝修复使此前被共享实例污染丢失的合并区域边框样式（mergeCell 克隆注册的 `sNN` 系列）在渲染输出中正确出现，28 个基线文件（24 个 output 快照 + 4 个 attachment）编码的是缺陷行为。逐文件验证（剥离样式类名后表格内容与文本逐字节一致；09-套打模板 styles.xml 实含 47pt 标题样式佐证）后经 `nop.autotest.force-save-output` 录制与手动覆盖更新基线（commit `4fefbb2143`，format-office.md 已补记）。
- **nop-cli-core DocumentConverterTest 失败**：同一根因的 xlsx→workbook.xml 路径。mergeCell 按合并区域注册克隆样式（每个 merge 一条，无去重），test.orm.xlsx（71 个 cellXfs、约 2000 个合并区域）的 workbook.xml styles 段从 70 条膨胀到 2045 条。方向正确（旧输出边框被串扰污染），基线已更新（styles 段外内容一致）；**样式去重膨胀问题记为后续优化项**。顺带 TestGenErrorCodesYaml 自动同步了 i18n 聚合文件（+8 条：7 条上轮未同步 + 1 条本次 socket，0 删除，commit `c39868ab68`）。
- 复核方法备注：force-save 录制模式下 JunitAutoTestCase 以 `nop.err.autotest.snapshot-finished` 异常结束每个用例属机制设计（录制完成信号），非真实失败；attachment 类基线（assertEquals 直读）不受该开关影响需手动更新。
- 验证：`./mvnw test -pl nop-report/nop-report-demo` 24/0/0；`-pl nop-runner/nop-cli-core -Dtest=DocumentConverterTest` 3/0/0；此后 `./mvnw clean install -T 1C` **BUILD SUCCESS（427 模块、0 SKIPPED、含全部测试）**——该分支首次完成全量构建验证。
- Doc-sync: No owner-doc update required（基线更新与编译修复，无约定变更）。

### check 系列 P0 清零：fix-ai-check 分支处置剩余 32 条 P0（25 份报告）

- 范围：`ai-dev/audits/check/` 54 份报告中，08-21 已处置 6 单元（118 条）之外的**剩余全部 32 条 P0**，横跨 25 个模块。每条流程：live code 复核（防已修/误报）→ 红测试 → 最小修复 → 绿 → 模块全量测试 → 报告条目追加"处置（fix-ai-check 分支，2026-08-22）"标注。结论：32 条中 31 条确认属实并修复，1 条（ai-rest 扫码登录）实施最小加固后完整协议改造标注暂缓。未发现误报。
- 修复清单（按批次 commit）：批次1 `6410a6bfe0`（api-core/core/dataset/utils/dao 六条：thenRun 成功路径、BeanCopier arraycopy 长度 0、BaseRecordInput off-by-one、UnifiedDiffLine 空行、ShellCommand Unix `-c`、JdbcBatcher flush 泄漏）；批次2 `e6010dc6eb`（orm 游标分页三缺陷、db-migration XML type 回填、nop-task 三条 P0、sys 锁过期反向、report min null、batch 分块索引）；批次3 `0b7fc8f903`（auth 登录门控解耦、wf transferActors 鉴权、ai-rest ticket 加固、gateway 默认密钥 fail-closed、ai-tools sessionId 白名单、search Lucene 数值查询、integration 两条）；批次4 `2de622fb6a`（stream 五条含 CepOperator per-key 上下文重构、net-misc 三条、ai-core 流式 args、code Tarjan、graph Edge 等值）。
- 关键设计点：nop-task loop/fork continuation-skip 未采用报告首选的 recoverMode 硬门控（会破坏既有 resume-by-load 语义），改为 task 级 attribute 记录首实例化 stepPath；2PC sink 用 `copyForSubtask(taskIndex)` 语义隔离（基类默认 subtask>0 fail-fast），JDBC ledger 加 subtask_id 列（旧 schema 显式报错需重建）；CepOperator 定时器账本改 per-key Map + watermark 逐 key 切上下文排水，快照新 per-key 格式兼容旧 flat 格式。
- 超出审计的新发现（未修，已记录在报告标注）：DaoQueryHelper.queryToFindPrevSql 空 filter 漏 where；db-migration 9 个非 DbChangeModel 子类的变更类解析期 CCE + column DynamicObject + 14 个 fixture 不符契约；nop-task 延迟重试 async 未压平（随 P0 一并修复）。
- 门禁维护：`check-nop-stream-invariants.mjs` 因 CepOperator 发射点行号漂移报 V4/V5，`output-contract-registry.json` 重钉 507→568、814→960 后 exit 0。`check-nop-stream-audit-manifest.mjs` 三分母（钉于 08-07）改动前即红，属存量，未处置。pre-commit ast-grep 顺带合规化两处既有裸 RuntimeException（TestFutureHelper、SocketServer 启动失败，后者新增 ERR_SOCKET_START_SERVER_FAIL）。
- 环境备注：本仓库 `.m2` 快照与源码存在漂移，不带 `-am` 跑部分模块（nop-auth-service/nop-task-ext/nop-wf-service）会出现 nopSequenceGenerator bean-init-self-wait 或 NoSuchMethodError（stash 基线对照证实与本次改动无关）；`-am` 或全量构建可消除。
- 验证：各模块 `./mvnw test -pl <module>` 全绿（数字见各报告标注）；`node ai-dev/tools/check-doc-links.mjs --strict` exit 0；`./mvnw clean install -T 1C` 全量构建结果见下一条日志。
- Doc-sync: No owner-doc update required（处置为缺陷修复，未新增平台约定；check 报告标注与 SUMMARY 修复进展段落已更新）。

### 全量 install 收口：metadata 静默吞异常合规化 + nop-retry 死表面残留清理

- 目标：`./mvnw clean install -T 1C` 全绿 + CI 全部门禁通过。首跑 BUILD FAILURE @ nop-retry-web。
- **失败一：NopRetryWebPagesTest**——`main.page.yaml` 引用 `/nop/retry/model/NopRetryTemplate/NopRetryTemplate.xmeta` 不存在。根因 = NopRetryTemplate 实体历经 08-19 删→08-20 重引入→再删的反复，最终从 ORM 移除后**生成物未随删**（08-13 清理日志有同清单先例）。删除残留：web `pages/NopRetryTemplate/` 整目录（view/lib/page.yaml/_gen）、api `crud/NopRetryTemplateApi.java` + beans ×2（均 `__XGEN_FORCE_OVERRIDE__` 生成物）、meta `_templates/_NopRetryTemplate.json`；rg 确认零外部引用。注意：模块需 `clean`——target/classes 中旧资源拷贝不会被增量构建清除。
- **失败二：metadata 硬门禁 guard-1（check-silent-swallow）exit 1**——`NopMetaDataSourceBizModel:416` 批量迁移逐行容错 catch 无错误码信号（08-18 W16 新增代码，晚于 08-15 门禁基线）。修复 = 新增 `ERR_DATASOURCE_MIGRATE_ROW_FAIL` define（DataSourceErrors）+ catch 内构造 `NopMetadataException` 记录 errorCode 并入 failures 结构化返回（保留 continue-on-error 语义），同时满足 guard-6 define-face/site-param/consumer 三规则。
- 验证：`clean install -T 1C` **BUILD SUCCESS（427 模块）**；metadata 硬门禁 6 guards 全过（exit 0）；gate④⑤、bean-naming、nop-stream 扫描器、doc-links --strict 全部 exit 0。
- 已知非 CI 项：`check-nop-code-invariants.mjs` 未接入任何 CI/package.json，当前 exit 1（46 hits：12 个 INV-01 位于 nop-code-service 真实代码 + 34 个 INV-02 位于 `_dump/` 生成物——扫描器未排除 _dump）。均为存量状态、与本日改动无关，待单独裁定（接线 + 修复 or 基线登记）。

### nop-ioc self-wait 断环：orm-defaults sequenceGenerator ref 加 ignore-depends + 设计文档补记

- 现象：`./mvnw test -pl nop-datav/nop-datav-service` 62 个测试 error，`ERR_IOC_BEAN_INIT_SELF_WAIT beanName=nopSequenceGenerator`。确定性复现（stash 对照确认与同日 bean 改名无关）。
- 根因链（堆栈逐帧核对）：容器启动创建 `nopSequenceGenerator`（sys-dao 版，setup 中）→ 注入 ormTemplate → template 注入 sessionFactory → 拉起 `$DEFAULT$nopOrmSessionFactory` 完整初始化（bean-method 生产者即使早引用语义也强制走 initBean，`getBeanInstance` 的 `getBeanMethod()!=null && !onlyProducer` 分支）→ `initBean` 开头 resolvedDepends 强制循环 `getBean("nopSequenceGenerator", false)` 要求完整 gen → 而 gen 的 setupInstance 压在同一线程栈上未返回（status<PROPERTY_SET 且 createThread==currentThread）→ `runUntil` 抛 selfWait。
- 为什么以前不炸：commit `428a196557`（08-21 并发死锁修复）拆掉两层面具——①旧 runUntil 对 CREATED 状态嵌套重跑 initFunc 把半成品当成品；②旧 newObject 全程持 beanDef 监视器屏蔽跨线程早引用窗口。环本身一直在配置里：orm-defaults:42 `<property name="sequenceGenerator" ref="nopSequenceGenerator"/>` 无豁免，被 §3.1 分级规则（pos(gen)<pos(factory)）提升为 factory 强制前置。SysSequenceGenerator 自身注入已标 `@IgnoreDepends`，环只剩这一边。
- 修复：该 ref 加 `ioc:ignore-depends="true"`——与同一 bean 定义中 daoListeners/interceptors collect-beans 既有豁免风格对齐（运行期协作者非启动前置）。属性赋值路径本就是早引用语义，不受影响。
- 验证：nop-datav-service 全量 **629 tests / 0 failures / 0 errors**（原 62 errors 清零）；nop-ioc + nop-orm 回归 **195 tests / 0 / 0**。
- Doc-sync：`ai-dev/design/nop-ioc/bean-dependency-semantics.md` 补记 ignore-depends 五种来源（XML property/ref 级、`~` 前缀、@IgnoreDepends、lazy-property 自动、collect-beans 级联）、resolvedDepends 排除语义、by-type 从不贡献 depends 的已知不对称、datav 断环实例；`bean-init-concurrency-locking.md` §五补记早引用可见窗口（跨线程可能返回 PROPERTY_SET 前裸实例，旧实现被监视器屏蔽）。
- 遗留观察（未处理）：`InjectTypeValueResolver` 未覆写 `collectDepends`（by-type 依赖从不进图），是否需要行为化属后续裁定。

### gate-4 表完备性拦截处置：ReadSpillExecutor 补录 §3.3（Loop Rule）

- 现象：门禁④ fail——`io.nop.ai.agent.tool.ReadSpillExecutor: IToolExecutor implementor NOT in the gate table`。根因 = plan `2252-nop-ai-agent-context-compaction-economics.md` 新增 spill 读回工具 `ReadSpillExecutor.java:36`（已在 ai-agent-tools.beans.xml 装配），但未同步登记 invariant-catalog §3.3 目标集表与门禁脚本内嵌表；gate-4 的 completeness grep 反查按 catalog §6 结构变更触发条件（新增 ToolExecutor 不入表即 red）拦截。棘轮按设计工作。
- 修复（双侧登记）：① `invariant-catalog.md` §3.3 新增第 32 行（直接实现，安全敏感面 = 内存/会话：经 session 读 ISpillStore，无文件/网络/命令 IO），表头/复现命令注释/计数核对段同步 31→32 行、grep 输出 28→29 行；② `check-ai-tool-executor-boundary.mjs` EXECUTORS 数组新增条目（surface=memory），头注释与分节注释计数同步（31 实例 = 28 直接 + 3 间接）。
- 历史记录不改：plans/red-list/sixth-gate 评估中的「30 实例」为当时执行事实，保持原样。
- 验证：`node ai-dev/tools/check-ai-tool-executor-boundary.mjs` PASS（目标集 31 实例，表完备性一致，零清单外缺口）；`--self-test` 7 例全 PASS；live grep 复现命令实测 29 行与目录注释一致；`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 issues）。
- Doc-sync: 无 owner-doc 变更（纯审计目录登记；门禁机制与判定规则未变，仅目标集增员）。

### GRC 普适性文章核对：补齐 Lens/DOP/FOP 对比遗漏

- 核对 `ai-dev/articles/grc-universal-software-construction-theory.md` 与 docs/theory 既有详细分析的一致性（对照 `reversible-compuation-vs-bidirectional-transformation.md`、`delta-oriented-programming.md`、GRC 主论文 §2.2、论文 v2 §7、`reversible-computation-a-paradigm-manifesto.md` §三、`xlang-review.md` §十、DSH 文章）。
- 文章修订：Lens 小节补入对齐难题 vs 内禀坐标公设、差量身份（瞬时输入 vs 一等公民资产）、点对点 vs 系统级尺度、"Y=F(X) 且 Δ 为空特例"定位、δ-lens 同态传播 ↔ 同态传递契约；新增 FOP 小节（FST/superimposition/Feature Algebra、无删除缺陷催生 DOP、FeatureHouse 外置工具局限）；DOP 小节补入 Core 来源缺失、坐标系由实现语言承载、差量与全量不同构、三维升维与宽松同态定律 `transport_G`；新增 MDE 与语言工作台小节；引言/第九节/结语同步补 FOP 条目；三.1 补 Git merge 破坏封闭性、patch 基线耦合的代数缺陷。
- 顺带提交此前遗留的 articles 目录重组（删除 `ai-dev/articles/2026-08/` 下 9 个草稿文件，Commit: `4ce0f25f5d`）。
- 偏差：无。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过（exit 0）；修复了重组遗留的 3 处断链（`ai-dev/README.md`、`ai-dev/references/dsh-community-articles/README.md` ×2）。
- Doc-sync: No owner-doc update required（纯文章内容修订，未改变平台约定；`ai-dev/README.md` 仅入口路径修正）。
