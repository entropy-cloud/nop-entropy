# 2026-08-22

### Plan 344 Phase 2：nop-core-framework 单元 15 条 P1/P2/P3 处置（nop-ioc/nop-config/nop-plugin/nop-security/nop-log）

- Plan 344 Phase 2 第四单元 nop-core-framework.md（P1×4 P2×5 P3×6，P0 无）：11 修复 + 3 暂缓 + 1 部分修复（javadoc 如实化 + 严格模式暂缓）+ 1 复查部分非问题（ProducedBeanInstance.STATUS_* 已 final）。
- P1：① `BeanConditionEvaluator` unless-property 分支误读 `getIfProperty()`——改按 `getUnlessProperty()` 自身字段求值（仅 unless 配置不再 NPE、if+unless 不再重复求值 if 条件）；② `ChangeSubscriptions.subscribePattern` 误注册到 simpleSubscriptions——改注册 patternSubscriptions（通配 pattern 订阅按 IConfigProvider 公开契约恢复触发）；③ 构造器循环依赖静默产生重复单例——`BeanDefinition` 新增 `inCreationThread` 标记（生命周期=createInstance 即构造器参数解析窗口，单例路径锁内标记/清除），`getBean0` 同线程重入即抛 `ERR_IOC_BEAN_DEPENDS_GRAPH_CONTAINS_CYCLE`（复用既有错误码 + beanName 定位；属性注入环/depends-on 环不受影响，TestReentrantCircularRepro 仍绿）；④ 文件配置源 `refreshConfig` 一次异常永久停止热刷新——整体 try-catch（构造期仍上抛 fail-fast）。
- **超出审计新发现（已修复）**：`AbstractFileConfigSource.refreshConfig` 成功刷新后未回写 `this.vars`（对比 JdbcConfigSource:102 有回写）——`getConfigValues()` 永远返回构造期快照，变更回调方重新拉取读到旧值（热更新实际不生效），且 isChanged 每轮与初始快照比较导致首次变更后每轮重复触发回调。随 P1-4 一并修复并补测试。
- P2：`loadPluginFromJar` 失败路径泄漏 PluginClassLoader（loadPlugin/getPluginConfig 纳入 try、catch 扩 `RuntimeException | Error`、finally 统一 safeClose）；插件僵尸容器三层修复（`destroySelf` deactivate+removeInstance 全程持 lifecycleLock；`doActivate` 入口校验注册表存在性，已销毁实例拒绝激活——新错误码 `ERR_PLUGIN_INSTANCE_ALREADY_DESTROYED`；manager `createInstance`/`destroyInstance` 实例变更段纳入 reconcileLock，锁序恒 reconcileLock→lifecycleLock）；`SecurityHelper.toRSAPublicKey` 裸 RuntimeException 改 `NopException.adapt`。
- 暂缓裁定：P2-6（getBean0 跨线程构造器环死锁——单线程确定性路径已随 P1-3 消除，残余需容器创建锁粒度设计决策：粗粒度锁串行化 concurrentStart vs wait-for 图环检测）；P2-9 严格模式（require-pinned-hash——hash 来源优先级系 ai-dev/design/nop-plugin/05-artifact-loading-design.md §五 W7 修订注解显式裁定，javadoc 信任模型已如实化：header/{url}.sha256 与 jar 同源仅防意外损坏、expectedHashes 是唯一独立信任源且优先级最低）；P3-15（条件求值 5 轮迭代上限——提高上限/超限抛异常均改变深链 bean 启停结果，属语义变更非缺陷收敛）。
- P3：`BeanScopeImpl` add/close 互斥（checkClosed+put 与 closed 置位同锁短临界区，销毁回调仍在锁外；终态校验裸 ISE 改 NopException）；`BeanDefinitionBuilder` 兜底分支错误码化（新错误码 `ERR_IOC_INVALID_PROP_VALUE_TYPE`，i18n zh-CN/en 按序同步）；`BeanDefinition.STATUS_*`×3 与 `BeanTopologySorter.INSTANCE` 加 final；`BeanScopeContext._instance`/`ConfigStarter.g_instance`/`LoggerConfigurator.s_instance`/`BeanContainerImpl.classIntrospection`（+懒初始化本地变量模式）/`VfsPluginDefinition.state` 加 volatile。
- 红验证：新增 6 个测试类 18 用例（TestBeanConditionUnlessProperty 5 / TestConstructorCycleDetection 2 / TestChangeSubscriptions 4 / TestFileConfigSourceRefresh 3 / TestPluginInstanceDestroyActivateRace 3 / TestSecurityHelperRsaKey 1），备份修复后主文件到 `_tmp/` 再 `git checkout HEAD` 还原逐批验证：16 处失败形态与审计一致（NPE getIfProperty null ×5 / 环静默通过 TestCtorCycleA 构造两次 ×2 / 通配订阅静默不触发 ×3 / 刷新异常上抛+快照旧值+重复触发 ×3 / 僵尸激活 nothing-thrown ×2 / bare RuntimeException InvalidKeySpec ×1）。P2-5（classloader 关闭副作用不可确定性观测）与 P3 加固类（volatile/final/竞态窗口/不可达兜底）免红测试，理由已写入报告标注。
- 验证：`./mvnw test -pl nop-core-framework/nop-ioc` **62 tests 绿**；`./mvnw test -pl nop-core-framework/nop-config,nop-core-framework/nop-plugin/nop-plugin-manager,nop-core-framework/nop-security,nop-core-framework/nop-log/nop-log-core -T 1C` **config 30 / plugin-manager 166 / security 2 全绿**；回归 `./mvnw test -pl nop-runner/nop-cli-core -Dtest=TestGenErrorCodesYaml`（i18n 聚合校验绿）+ `./mvnw test -pl nop-auth/nop-auth-service`（IoC 容器重下游，**393 tests 绿**）。报告 15 条全部标注（grep `^### \[P[123]\]` = grep `处置（fix-ai-check` = 15）。
- Doc-sync: No owner-doc update required（均为缺陷收敛与契约实现修复，无文档化契约语义变更；构造器环 fail-fast 与文档既有 `ioc:ignore-depends` 指引一致）。

### Plan 344 Phase 2：nop-core 单元 17 条 P1/P2/P3 处置（框架核心引擎，全部修复）

- Plan 344 Phase 2 第三单元 nop-core.md（P1×3 P2×4 P3×10，P0 已在前批处置）：**17 条全部修复**，无暂缓/不修复。
- P1：① `IFile.getResource("..")` 目录逃逸——根因在 nop-commons `StringHelper.isCanonicalFilePath` 对整段 `"."`/`".."` 漏判（跨模块联动修复，4 个调用方复核均为 fail-safe 收紧）；② `ResourceComponentManager.loadComponentModelByUrl` 的 `?` 分支复制粘贴错误——按接口文档解析 `?paramName=paramValue` 查询参数（`StringHelper.parseSimpleQuery`），识别 `transform`/`sub`（常量 `COMPONENT_URL_PARAM_*` 落在 `IResourceComponentManager`），`#` fragment 保留；③ `ResourceHelper.resolveResourceInDir` 裸 `..` 绕过——fileName 非空时改 `isCanonicalFilePath` 整体校验 + `NopException(ERR_RESOURCE_INVALID_FILE_NAME)`。
- P2：`ReflectionManager.unregisterTypeConverter` 改操作 `registeredConverters`（原误清 `converterCache`）+ 同步清缓存；`CoreInitialization.getDisabled` 判空 `bootstrapConfig`（reinitialize 独立调用/destroy 后调用不再 NPE）；`FileResource.mkdirs` 条件纠正（仅"已存在且非目录"抛 `ERR_RESOURCE_NOT_DIR`，不存在→正常创建）；`SqlLikeUtils` 三个异常工厂（含同类私有 `invalidRegularExpression`）NopException 化（新错误码 ×3）。
- P3 全修 10 条：`ResourceCacheEntry.reloadObject` 对齐 `getObject` 的 `Null.NULL` 占位 + 跳过 `onCreated(null)`；`ClassPathResource.initLength` finally disconnect（HttpURLConnection）+ DEBUG 日志；`AopCodeGenerator` 生成代码 catch 改抛 `NopException(ERR_REFLECT_AOP_INIT_METHOD_MODEL_FAIL, e)` fail-fast（期望附件 MyModel__aop.java 同步）；`DefaultVirtualFileSystem` 字段 volatile + `destroy()` synchronized；`ResourceDependencySet.lastModified` volatile；`ResourceHelper.readBytes` 超 `Integer.MAX_VALUE` 抛 `ERR_RESOURCE_READ_BYTES_TOO_LARGE`（原 `(int)` 截断 → NegativeArraySizeException）；`ErrorMessageManager.clearErrorCodeMappings` 补 `subMappings.clear()`；`DeltaResourceStore.getRawResource` 租户路径无子路径抛 `ERR_RESOURCE_INVALID_PATH`（原 SIOOBE Range [9,-1)）；`ReflectionManager.ClassModelLoader` 记忆化 + 锁内 double-check（并发 miss 不再重复构建 ClassModel）；`ResourceHelper.resolveRelativeResource` 裸 IAE → `NopException(ERR_RESOURCE_NOT_ALLOW_PARENT_PATH)`。
- 新增 CoreErrors 错误码 7 个（aop-init-method-model-fail / invalid-file-name / not-allow-parent-path / read-bytes-too-large / like-invalid-escape-char / like-invalid-escape-sequence / similar-invalid-regex），nop-cli-errors i18n zh-CN/en 按错误码排序同步（en 按文件惯例加引号；zh 既有 17 处含 ": " 的未引号条目为前批存量，严格解析器拒绝但平台加载器容忍，本次未动）。
- 红验证：9 个测试文件（新建 6 个：TestSqlLikeUtils/TestReflectionManager/TestCoreInitialization/TestResourceCacheEntry/TestErrorMessageManager/TestDeltaResourceStore/TestResourceComponentManager，扩展 3 个：TestResourceHelper/TestAopCodeGenerator/nop-commons TestStringHelper）全部在修复前代码先行运行，17 处失败形态与审计逐一吻合（getResource("..") 无异常返回父目录 / transform=资源路径本身抛 undefined-transform / 裸 IAE ×2 / NegativeArraySizeException -2147483648 / StringIndexOutOfBoundsException Range [9,-1) / RuntimeException ×3 / reload 后 loadCount 2→3 / clear 后 subMapping 仍命中 / 并发构建出两个 ClassModel 实例 / NPE / printStackTrace 生成 / mkdirs 抛 not-dir）；修复后全绿。P3-2（连接释放+日志）、P3-4/P3-5（内存可见性）3 条为加固类免测试，理由已写入报告标注。
- 验证：`./mvnw test -pl nop-kernel/nop-core` **252 tests / 0 failures / 2 skipped（存量）**；公共行为变化（StringHelper/isCanonicalFilePath 收紧、资源校验、错误类型）追加回归 `./mvnw test -pl nop-kernel/nop-commons,nop-kernel/nop-xlang,nop-kernel/nop-dataset`（先 install 修复后 commons）**全部 SUCCESS（xlang 551 tests 绿）**。报告 17 条全部标注（grep `^### \[P[123]\]` 17 = 处置标注 17，另有 P0 既有标注 1）。
- 超审计观察：无新缺陷；zh-CN i18n 聚合文件的 17 处严格 YAML 不可解析条目为前批存量（详见上节说明）。
- 提交钩子 ast-grep 门禁：本次触碰文件中两处前批存量空 catch 被拦（`ReflectionManager` isRecord 探测静态块 / `TestStringHelper#testSha256` 的 Mac 计算块），最小整改：前者补显式 `m = null` 降级赋值+注释，后者将手动 Mac 哈希从打印改为与 digest 断言相等、异常改 `fail()`（顺带强化了该测试）。
- Doc-sync: No owner-doc update required（均为缺陷收敛与契约实现修复，无文档化契约语义变更；`IResourceComponentManager` 仅补参数名常量与注释）。

### Plan 344 Phase 2：nop-commons 单元 20 条 P1/P2/P3 处置

- Plan 344 Phase 2 第二单元 nop-commons.md（P1×6 P2×8 P3×6，P0 无）：17 条修复 + 2 条暂缓 + 1 条子项改判非问题（MapCache async）。
- P1 全修：`Lazy.get()` 补 `loaded = true`（含 null 值缓存语义）；`LocalFileLock` 恒 false 返回改 `lock != null` + 异常路径句柄清理（releaseHandles）；`LocalResourceLockManager.tryResetLease/isHoldingLock` 租约条件反转修正 + `ResourceLock.tryResetLease` 空锁 NPE 判空；`FileHelper.writeTextWithLock` truncate 改字节数（中文内容不再截断，与上游 Nacos 对齐）；`NetHelper.findLocalIp` 改走 `LOCALHOST4()` 懒初始化访问器；`LocalCache.putIfAbsent` 改 Caffeine `asMap().putIfAbsent` 原子语义 + `ICache` 接口 javadoc 声明原子性契约。
- P2 修 6 缓 2：throttle 改 `promiseMap.compute` + 任务结束自清理（节流窗口正确重开、无 map 泄漏）、debounce 同步补自清理；`RoundRobinSupplier` 扩容失败从旧长度关闭新建资源；`DefaultRateLimiter.getAcquireFailCount` 返回正确字段；`SequentialTaskExecutor.getTotalExecutionTime` 按纳秒返回 + 字段 volatile；`GlobalCacheRegistry.register` 改 putIfAbsent（失败不再半生效）；`IoHelper.getEncodingFromBOM` 短流部分读取 + 按实际字节数分档匹配；`DateHelper.buildFormatter` ConcurrentHashMap + computeIfAbsent 回填。暂缓：JavaSerializer ObjectInputFilter（同时是 serializeClone 任意类型深拷贝默认实现，白名单必破坏之；JDK `jdk.serialFilter` 全局机制已可用，需独立安全设计裁定）；ExecutorHelper `config.setMaxPoolSize(0)` 突变（live code 复核证实是 DefaultScheduledExecutor.refreshConfig/updateThreadPool 共享 config 的承重约定标记，去除或 clone 均改变刷新语义，需归一化所有权设计）。
- P3 修 5 + 1 子项非问题：FileHelper 资源缺失 NPE 改 `NopException(ERR_FILE_NOT_FOUND)`（复用现有错误码）；裸 RuntimeException×2 改 `NopException.adapt`；静态字段 volatile×4（IoHelper 序列化器×2/GlobalMeterRegistry/ExecutorHelper.SYNC_EXECUTOR）；MutableInt "Atomically" javadoc 纠正 + HighWatermarkSemaphore 统计计数补累加；StringTrie 空串 find 返回 null + add 空键 Guard 拒绝。**改判非问题**：MapCache async 跨线程暴露——审计前提 "futureCall 在公共线程池执行" 有误，`FutureHelper.futureCall/futureRun` 实为调用线程同步执行（FutureHelper.java:122-138），补探针测试 `TestMapCache.testAsyncOnNonThreadSafeRunsSynchronously` 实证并作回归守卫。
- 红验证：先写 16 个测试文件（新建 8 个 + 扩展 8 个）在修复前代码上运行，22 处失败形态与审计逐一吻合（Lazy supplier 执行 3 次、lock() 获锁成功仍抛 acquire-lock-timeout、"中文内容测试123"截断为"中文内"、putIfAbsent 第 63 轮双 true、throttle count=4、promiseMap 残留、totalExecutionTime 1.3e14、短流 unexpected-eof、SIOOBE Index 0、信号量计数恒 0 等），修复后全绿；P1-5 findLocalIp 红验证不可行（需无网卡且 getLocalHost 抛 UnknownHostException 的环境）已在报告标注中说明，以代码级复核为依据。
- 验证：`./mvnw test -pl nop-kernel/nop-commons` **219 tests / 0 failures**；公共行为变化（Lazy/LocalCache/StringTrie/NetHelper/GlobalCacheRegistry/DateHelper）追加回归 `./mvnw test -pl nop-kernel/nop-core,nop-kernel/nop-xlang`（先 `install -DskipTests` 安装修复后 commons）**core 235 + xlang 551 tests 全绿**。报告 20 条全部标注（grep `^### \[P[123]\]` = grep `处置（fix-ai-check` = 20）。
- Doc-sync: No owner-doc update required（均为缺陷收敛与契约实现修复，无文档化契约语义变更；P2-7/P3-2 暂缓决策点已写入报告标注）。

### Plan 344 Phase 2 启动：kernel-small 单元 16 条 P1/P2/P3 处置

- Plan 344 Phase 2 首单元 kernel-small.md（P1×2 P2×8 P3×6，P0 已在 08-22 上午批次处置）：13 修复 + 2 非问题改判 + 2 局部不修复裁定。
- 修复要点：markdown 空文档 NPE（parseRootSection null 守卫）/parseMappingTable 表头当 key/removeStyle 长度守卫；dataset `projectWithRename` 用既有 `renameTo` 复制改名（ProjectDataSetMeta 崩溃路径根因在内层，随之修复）/`hasField` 按投影字段遍历/`defaultReadBatch(0)` 不再吞一条；javac `findClass` 抛 CNFE/fileManager finally close/`getErrorDetail` 防御加固；codegen `GenAopProxy` try-with-resources/`CodeGenTask` printStackTrace→LOG.error/`ClassRenamer` 正则改平衡括号扫描（注解参数含 `)`/`"`/`$` 不再损坏）；record-mapping 键引号 roundtrip（`findQuotedStringEnd` 与 `StringHelper.quote` 的 escapeJava 对称 + `decodeKey` unquote）+ 两处裸 IAE 转错误码（md-table-field-no-item-mapping / null-mapping-name）+ `MarkdownHelper` 两处（pos-list-size-not-match）。
- **改判非问题 2 条**：① 表格尾空单元格——探针实证 `| a | | |` 解析为 3 cells（审计断言 2 有误），7 种行模式与 GFM 语义逐一对照一致，审计建议的修法反而会破坏尾管道风格 roundtrip，补特征化测试固化；② `getErrorDetail` 越界——反编译 janino `LocatedException.getMessage()` 证实恒为 `locStr + ": " + raw`，越界路径不可达，保留防御检查（`>=` 与原可达行为逐位一致）+ 测试固化。
- **超审计新发现**（已修）：`addImageSummarization/changeLinkUrl` 的 `Collections.sort` 只排 posList 不排并行 infos/newUrls，乱序输入时区间与替换值配对错乱；改为 `sortedOrder` 索引副本排序，顺带消除入参突变。
- 不修复裁定：KernelCliValidateCommand verbose 模式 stderr 打印堆栈（CLI 无日志框架、用户显式开关）；TableToMarkdownConverter 的 null IAE（Guard 风格编程契约违规）。
- 红验证抽查：临时还原 6 个修复前主文件，markdown 6 项新测试失败形态（NPE/越界/2-entry map）、dataset 4 项（rename 失效/unknown-column 崩溃/hasField 误 true）、record-mapping 3 项（`"a` 引号残留）均与审计预测一致；恢复后全绿。
- 验证：`./mvnw test -pl nop-kernel/nop-markdown,nop-kernel/nop-dataset,nop-kernel/nop-javac,nop-kernel/nop-codegen,nop-kernel/nop-record-mapping` 156 tests / 0 failures（77+11+5+14+49），kernel-cli 回归 BUILD SUCCESS；i18n 聚合补 3 条（record ×2 + markdown ×1）。
- Doc-sync: No owner-doc update required（缺陷收敛与工具加固，无文档化契约语义变更）。

### Plan 344 Phase 1 完成：check 审计持久层五单元 P1/P2/P3 全量处置（92 条）

- Plan `ai-dev/plans/344-check-audit-p1-p2-p3-remediation.md` Phase 1（nop-dao / db-migration / nop-orm / orm-periph / nop-orm-eql 五报告共 92 条 P1/P2/P3）全部处置完毕，Phase 1 status → completed。四类终态分布：87 修复 + 4 部分修复暂缓 + 3 暂缓 + 3 非问题（详见各报告逐条"处置（fix-ai-check 分支，2026-08-22）"标注，grep 验证五报告 findings 计数 ≤ 标注计数）。
- 批次 commit：nop-dao `3f1111078d`（13 修复 + QuerySpaceEnv.leave 恢复语义超审计修正）、db-migration `5737e7dbb5`、nop-orm `640cf0b359`（GenSqlHelper 排序 NPE 改判非问题）、nop-orm-eql `58e934a36f`（CTE recursive 判非问题 + NOT-ILIKE 同族新发现修复）、orm-periph 本次提交（22 条全部修复）。
- orm-periph 要点：TDengine SQL 生成三缺陷（TAGS 取列模型对象当值/VALUES 恒空/load 缺 FROM+逗号连接主键）、delete/count 漏 querySpace、update 静默丢失改 fail-loud（`ERR_TDENGINE_UPDATE_NOT_SUPPORTED`，TDengine 无 UPDATE 语句）、子表名白名单 `^[A-Za-z0-9_]+$` 防注入；Rpc 驱动 batchModify/batchDelete 补 checkResponse + batchLoadAsync 改按主键匹配（复合键 `COMPOSITE_PK_SEPARATOR`+encodeDupEscape）；`DaoEntityBlockingSource` 无限等待/中断语义/转移计数三修正；`LazyLoadOrmModel` putEntityModel 入锁消竞态 + 表名索引归一化大小写；geo/pdm/tdengine 三模块错误码体系新建（TdEngineErrors/OrmGeoErrors/PdmModelErrors）+ `ERR_ORM_MODEL_JOIN_COLUMNS_NOT_MATCH_PK`。
- 红验证抽查（orm-periph）：临时还原修复前主代码跑新测试——`TestDaoEntityBlockingSource` 3 条全挂（take 抛 `NonPositive:timeout,value=-1`、中断被吞、计数含预置元素）、`TestTdSqlHelper` 3 条全挂（输出呈现 `TAGS Mock for IColumnModel`、空 `VALUES()`、`select ts,value where ts=...,...` 无 FROM），与审计预测形态一致；恢复后全绿。
- 顺带：nop-cli-errors i18n 聚合补 5 条（本战役前四次提交遗漏的 4 条 + 本次 join-columns-not-match-pk；zh/en 同步）。
- 验证：orm-periph 六模块 `./mvnw test -pl nop-persistence/nop-orm-model,nop-orm-drivers/nop-orm-tdengine,nop-orm-pdm,nop-orm-rpc,nop-orm-data,nop-orm-geo` 40 tests / 0 failures / 0 errors；其余四单元测试数见各自报告标注。
- 偏差：前四次批次提交时未同步当日 dev log（会话中断遗留），本条目合并补记。
- Doc-sync: No owner-doc update required（均为缺陷收敛，无文档化契约语义变更）。

### check 系列 P0 清零收尾：上轮修复遗留问题三处 + 下游基线同步

- 上轮（08-21）六单元修复存在未经全量构建验证的遗留问题，本次全量构建逐一暴露并处置：
- **nop-file-dao 编译损坏**：`46575c4278` 在 `DaoResourceFileStore` 引入了三处编译错误（`IEntityDao` 无 `findListByQuery`、`NopException` 无单 String 构造），该模块当时从未被编译。已修正为 `findAllByQuery` + 模块内 ErrorCode 常量（`nop.err.file.invalid-biz-obj-name`/`invalid-file-ext`，FileErrors 在外部构件 nop-biz-file-core 中无法追加），保留原修复意图（commit `21f78d8eb5`，file-retry-tcc.md 已补记）。
- **nop-report-demo 12 个用例失败**：临时 worktree 在分支基点 `29d2fb5f7d` 复测证实为上轮存量回归（与本次 32 条 P0 修复无关）。根因 = `OfficePackage.cloneInstance` 深拷贝修复使此前被共享实例污染丢失的合并区域边框样式（mergeCell 克隆注册的 `sNN` 系列）在渲染输出中正确出现，28 个基线文件（24 个 output 快照 + 4 个 attachment）编码的是缺陷行为。逐文件验证（剥离样式类名后表格内容与文本逐字节一致；09-套打模板 styles.xml 实含 47pt 标题样式佐证）后经 `nop.autotest.force-save-output` 录制与手动覆盖更新基线（commit `4fefbb2143`，format-office.md 已补记）。
- **nop-cli-core DocumentConverterTest 失败**：同一根因的 xlsx→workbook.xml 路径。mergeCell 按合并区域注册克隆样式（每个 merge 一条，无去重），test.orm.xlsx（71 个 cellXfs、约 2000 个合并区域）的 workbook.xml styles 段从 70 条膨胀到 2045 条。方向正确（旧输出边框被串扰污染），基线已更新（styles 段外内容一致）；**样式去重膨胀问题记为后续优化项**。顺带 TestGenErrorCodesYaml 自动同步了 i18n 聚合文件（+8 条：7 条上轮未同步 + 1 条本次 socket，0 删除，commit `c39868ab68`）。
- 复核方法备注：force-save 录制模式下 JunitAutoTestCase 以 `nop.err.autotest.snapshot-finished` 异常结束每个用例属机制设计（录制完成信号），非真实失败；attachment 类基线（assertEquals 直读）不受该开关影响需手动更新。
- 验证：`./mvnw test -pl nop-report/nop-report-demo` 24/0/0；`-pl nop-runner/nop-cli-core -Dtest=DocumentConverterTest` 3/0/0；此后 `./mvnw clean install -T 1C` **BUILD SUCCESS（427 模块、0 SKIPPED、含全部测试）**——该分支首次完成全量构建验证。
- Doc-sync: No owner-doc update required（基线更新与编译修复，无约定变更）。

### check 系列 P0 清零：fix-ai-check 分支处置剩余 32 条 P0（25 份报告）

- 范围：`ai-dev/audits/check/` 54 份报告中，08-21 已处置 6 单元（118 条）之外的**剩余全部 32 条 P0**，横跨 25 个模块。每条流程：live code 复核（防已修/误报）→ 红测试 → 最小修复 → 绿 → 模块全量测试 → 报告条目追加"处置（fix-ai-check 分支，2026-08-22）"标注。结论：32 条中 31 条确认属实并修复，1 条（ai-rest 扫码登录）实施最小加固后完整协议改造标注暂缓。未发现误报。
- 修复清单（按批次 commit）：批次1 `6410a6bfe0`（api-core/core/dataset/utils/dao 六条：thenRun 成功路径、BeanCopier arraycopy 长度 0、BaseRecordInput off-by-one、UnifiedDiffLine 空行、ShellCommand Unix `-c`、JdbcBatcher flush 泄漏）；批次2 `e6010dc6eb`（orm 游标分页三缺陷、db-migration XML type 回填、nop-task 三条 P0、sys 锁过期反向、report min null、batch 分块索引）；批次3 `0b7fc8f903`（auth 登录门控解耦、wf transferActors 鉴权、ai-rest ticket 加固、gateway 默认密钥 fail-closed、ai-tools sessionId 白名单、search Lucene 数值查询、integration 两条）；批次4 `2de622fb6a`（stream 五条含 CepOperator per-key 上下文重构、net-misc 三条、ai-core 流式 args、code Tarjan、graph Edge 等值）。
- 关键设计点：nop-task loop/fork continuation-skip 未采用报告首选的 recoverMode 硬门控（会破坏既有 resume-by-load 语义），改为 task 级 attribute 记录首实例化 stepPath；2PC sink 用 `copyForSubtask(taskIndex)` 语义隔离（基类默认 subtask>0 fail-fast），JDBC ledger 加 subtask_id 列（旧 schema 显式报错需重建）；CepOperator 定时器账本改 per-key Map + watermark 逐 key 切上下文排水，快照新 per-key 格式兼容旧 flat 格式。
- 超出审计的新发现（未修，已记录在报告标注）：DaoQueryHelper.queryToFindPrevSql 空 filter 漏 where；db-migration 9 个非 DbChangeModel 子类的变更类解析期 CCE + column DynamicObject + 14 个 fixture 不符契约；nop-task 延迟重试 async 未压平（随 P0 一并修复）。
- 门禁维护：`check-nop-stream-invariants.mjs` 因 CepOperator 发射点行号漂移报 V4/V5，`output-contract-registry.json` 重钉 507→568、814→960 后 exit 0。`check-nop-stream-audit-manifest.mjs` 三分母（钉于 08-07）改动前即红，属存量，未处置。pre-commit ast-grep 顺带合规化两处既有裸 RuntimeException（TestFutureHelper、SocketServer 启动失败，后者新增 ERR_SOCKET_START_SERVER_FAIL）。
- 环境备注：本仓库 `.m2` 快照与源码存在漂移，不带 `-am` 跑部分模块（nop-auth-service/nop-task-ext/nop-wf-service）会出现 nopSequenceGenerator bean-init-self-wait 或 NoSuchMethodError（stash 基线对照证实与本次改动无关）；`-am` 或全量构建可消除。
- 验证：各模块 `./mvnw test -pl <module>` 全绿（数字见各报告标注）；`node ai-dev/tools/check-doc-links.mjs --strict` exit 0；`./mvnw clean install -T 1C` 全量构建结果见下一条日志。
- Doc-sync: No owner-doc update required（处置为缺陷修复，未新增平台约定；check 报告标注与 SUMMARY 修复进展段落已更新）。

### 全量 install 收口：metadata 静默吞异常合规化 + nop-retry 死表面残留清理

- 目标：`./mvnw clean install -T 1C` 全绿 + CI 全部门禁通过。首跑 BUILD FAILURE @ nop-retry-web。
- **失败一：NopRetryWebPagesTest**——`main.page.yaml` 引用 `/nop/retry/model/NopRetryTemplate/NopRetryTemplate.xmeta` 不存在。根因 = NopRetryTemplate 实体历经 08-19 删→08-20 重引入→再删的反复，最终从 ORM 移除后**生成物未随删**（08-13 清理日志有同清单先例）。删除残留：web `pages/NopRetryTemplate/` 整目录（view/lib/page.yaml/_gen）、api `crud/NopRetryTemplateApi.java` + beans ×2（均 `__XGEN_FORCE_OVERRIDE__` 生成物）、meta `_templates/_NopRetryTemplate.json`；rg 确认零外部引用。注意：模块需 `clean`——target/classes 中旧资源拷贝不会被增量构建清除。
- **失败二：metadata 硬门禁 guard-1（check-silent-swallow）exit 1**——`NopMetaDataSourceBizModel:416` 批量迁移逐行容错 catch 无错误码信号（08-18 W16 新增代码，晚于 08-15 门禁基线）。修复 = 新增 `ERR_DATASOURCE_MIGRATE_ROW_FAIL` define（DataSourceErrors）+ catch 内构造 `NopMetadataException` 记录 errorCode 并入 failures 结构化返回（保留 continue-on-error 语义），同时满足 guard-6 define-face/site-param/consumer 三规则。
- 验证：`clean install -T 1C` **BUILD SUCCESS（427 模块）**；metadata 硬门禁 6 guards 全过（exit 0）；gate④⑤、bean-naming、nop-stream 扫描器、doc-links --strict 全部 exit 0。
- 已知非 CI 项：`check-nop-code-invariants.mjs` 未接入任何 CI/package.json，当前 exit 1（46 hits：12 个 INV-01 位于 nop-code-service 真实代码 + 34 个 INV-02 位于 `_dump/` 生成物——扫描器未排除 _dump）。均为存量状态、与本日改动无关，待单独裁定（接线 + 修复 or 基线登记）。

### nop-ioc self-wait 断环：orm-defaults sequenceGenerator ref 加 ignore-depends + 设计文档补记

- 现象：`./mvnw test -pl nop-datav/nop-datav-service` 62 个测试 error，`ERR_IOC_BEAN_INIT_SELF_WAIT beanName=nopSequenceGenerator`。确定性复现（stash 对照确认与同日 bean 改名无关）。
- 根因链（堆栈逐帧核对）：容器启动创建 `nopSequenceGenerator`（sys-dao 版，setup 中）→ 注入 ormTemplate → template 注入 sessionFactory → 拉起 `$DEFAULT$nopOrmSessionFactory` 完整初始化（bean-method 生产者即使早引用语义也强制走 initBean，`getBeanInstance` 的 `getBeanMethod()!=null && !onlyProducer` 分支）→ `initBean` 开头 resolvedDepends 强制循环 `getBean("nopSequenceGenerator", false)` 要求完整 gen → 而 gen 的 setupInstance 压在同一线程栈上未返回（status<PROPERTY_SET 且 createThread==currentThread）→ `runUntil` 抛 selfWait。
- 为什么以前不炸：commit `428a196557`（08-21 并发死锁修复）拆掉两层面具——①旧 runUntil 对 CREATED 状态嵌套重跑 initFunc 把半成品当成品；②旧 newObject 全程持 beanDef 监视器屏蔽跨线程早引用窗口。环本身一直在配置里：orm-defaults:42 `<property name="sequenceGenerator" ref="nopSequenceGenerator"/>` 无豁免，被 §3.1 分级规则（pos(gen)<pos(factory)）提升为 factory 强制前置。SysSequenceGenerator 自身注入已标 `@IgnoreDepends`，环只剩这一边。
- 修复：该 ref 加 `ioc:ignore-depends="true"`——与同一 bean 定义中 daoListeners/interceptors collect-beans 既有豁免风格对齐（运行期协作者非启动前置）。属性赋值路径本就是早引用语义，不受影响。
- 验证：nop-datav-service 全量 **629 tests / 0 failures / 0 errors**（原 62 errors 清零）；nop-ioc + nop-orm 回归 **195 tests / 0 / 0**。
- Doc-sync：`ai-dev/design/nop-ioc/bean-dependency-semantics.md` 补记 ignore-depends 五种来源（XML property/ref 级、`~` 前缀、@IgnoreDepends、lazy-property 自动、collect-beans 级联）、resolvedDepends 排除语义、by-type 从不贡献 depends 的已知不对称、datav 断环实例；`bean-init-concurrency-locking.md` §五补记早引用可见窗口（跨线程可能返回 PROPERTY_SET 前裸实例，旧实现被监视器屏蔽）。
- 遗留观察（未处理）：`InjectTypeValueResolver` 未覆写 `collectDepends`（by-type 依赖从不进图），是否需要行为化属后续裁定。

### gate-4 表完备性拦截处置：ReadSpillExecutor 补录 §3.3（Loop Rule）

- 现象：门禁④ fail——`io.nop.ai.agent.tool.ReadSpillExecutor: IToolExecutor implementor NOT in the gate table`。根因 = plan `2252-nop-ai-agent-context-compaction-economics.md` 新增 spill 读回工具 `ReadSpillExecutor.java:36`（已在 ai-agent-tools.beans.xml 装配），但未同步登记 invariant-catalog §3.3 目标集表与门禁脚本内嵌表；gate-4 的 completeness grep 反查按 catalog §6 结构变更触发条件（新增 ToolExecutor 不入表即 red）拦截。棘轮按设计工作。
- 修复（双侧登记）：① `invariant-catalog.md` §3.3 新增第 32 行（直接实现，安全敏感面 = 内存/会话：经 session 读 ISpillStore，无文件/网络/命令 IO），表头/复现命令注释/计数核对段同步 31→32 行、grep 输出 28→29 行；② `check-ai-tool-executor-boundary.mjs` EXECUTORS 数组新增条目（surface=memory），头注释与分节注释计数同步（31 实例 = 28 直接 + 3 间接）。
- 历史记录不改：plans/red-list/sixth-gate 评估中的「30 实例」为当时执行事实，保持原样。
- 验证：`node ai-dev/tools/check-ai-tool-executor-boundary.mjs` PASS（目标集 31 实例，表完备性一致，零清单外缺口）；`--self-test` 7 例全 PASS；live grep 复现命令实测 29 行与目录注释一致；`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 issues）。
- Doc-sync: 无 owner-doc 变更（纯审计目录登记；门禁机制与判定规则未变，仅目标集增员）。

### GRC 普适性文章核对：补齐 Lens/DOP/FOP 对比遗漏

- 核对 `ai-dev/articles/grc-universal-software-construction-theory.md` 与 docs/theory 既有详细分析的一致性（对照 `reversible-compuation-vs-bidirectional-transformation.md`、`delta-oriented-programming.md`、GRC 主论文 §2.2、论文 v2 §7、`reversible-computation-a-paradigm-manifesto.md` §三、`xlang-review.md` §十、DSH 文章）。
- 文章修订：Lens 小节补入对齐难题 vs 内禀坐标公设、差量身份（瞬时输入 vs 一等公民资产）、点对点 vs 系统级尺度、"Y=F(X) 且 Δ 为空特例"定位、δ-lens 同态传播 ↔ 同态传递契约；新增 FOP 小节（FST/superimposition/Feature Algebra、无删除缺陷催生 DOP、FeatureHouse 外置工具局限）；DOP 小节补入 Core 来源缺失、坐标系由实现语言承载、差量与全量不同构、三维升维与宽松同态定律 `transport_G`；新增 MDE 与语言工作台小节；引言/第九节/结语同步补 FOP 条目；三.1 补 Git merge 破坏封闭性、patch 基线耦合的代数缺陷。
- 顺带提交此前遗留的 articles 目录重组（删除 `ai-dev/articles/2026-08/` 下 9 个草稿文件，Commit: `4ce0f25f5d`）。
- 偏差：无。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过（exit 0）；修复了重组遗留的 3 处断链（`ai-dev/README.md`、`ai-dev/references/dsh-community-articles/README.md` ×2）。
- Doc-sync: No owner-doc update required（纯文章内容修订，未改变平台约定；`ai-dev/README.md` 仅入口路径修正）。
