### 2026-07-28（Plan 302 Phase 2 执行：Per-Fire 独立事务改造）

- Plan 302 Phase 2 完成：Completion Processor 从批处理级 `@SingleSession` 改为 Per-Fire 独立事务
  - 改了什么：
    - `scanBatch()`: 移除 `@SingleSession`
    - `completeSingleFire(String fireId)`: 新增 `protected` 方法，标注 `@Transactional(REQUIRES_NEW)` + `@SingleSession`，内联全部完成逻辑（不再调 `completeFireAndUpdateSchedule` / `failFireWithoutSchedule`）
    - `IJobFireStore.getFireById()`: 新增接口方法，返回 null 而非抛异常
    - 删除 `tryCompleteFireAndGetStatus(NopJobFire)` 旧方法
  - AOP 关键认知：Nop 编译时 AOP 是子类覆盖模式，自调用**会**被拦截（不同于 Spring 运行时代理）。但必须通过 IoC 注入实例，`new` 创建无代理
  - 测试改造：
    - `TestJobConcurrency`、`TestJobCoordinatorScanner`：改为 `@Inject IJobCompletionProcessor` 获取 AOP 代理
    - `TestJobE2E.SimpleFireStore`：重构为单 `fires` Map 模拟 DB 表（`fetchRunningFires` 按 status 过滤）
    - `TestJobCompletionProcessor.MockFireStore`：增加 `firesById` Map 支持 `getFireById`
  - 验证：`./mvnw clean test -pl nop-job/nop-job-dao,nop-job/nop-job-coordinator -am` → 156 tests, 0 failures
  - Doc-sync:
    - `docs-for-ai/02-core-guides/concurrency-and-transactions.md`: 新增模式五（Per-Item 独立事务）
- 偏差：实际实现与 plan 有差异（`completeSingleFire` 返回 `Integer` 而非 `FireCompletionResult`；metrics 在方法内部触发而非 `scanBatch`）。原因：保持与 Phase 1 代码结构一致，减少改动面

### 2026-07-28（Plan 302 分析 + review + Phase 1 执行）

- Plan 302 分析阶段完成：分析了 `JobCompletionProcessorImpl.tryCompleteFireAndGetStatus` 的指标误触发问题（双 Coordinator 下 `completeFireAndUpdateSchedule` 返回 `void` 导致 metrics/alarm 在版本冲突后仍触发）
  - 创建 analysis: `ai-dev/analysis/2026-07/2026-07-28-nop-job-completion-processor-architectural-analysis.md`
  - 两轮子 agent 技术审查后修正 12+ 分析错误
  - 验证 ORM 行为：`incOptimisticLockVersion` 在 flush 后正确递增，`orm_clearDirty` 清除脏标志，跨 Fire Schedule 干扰不存在
- Plan 302 起草 + 两轮 adversarial review：
  - Round 1: 11 findings (0 blocker, 5 major, 6 minor) — all fixed
  - Round 2: 5 findings (1 blocker R-2.1: AOP self-invocation on private method, 2 major, 2 minor) — all fixed
  - 共识达成后 plan status 改为 `active`
- **Phase 1 执行**（最小修复：`completeFireAndUpdateSchedule` 返回 boolean + `failFireWithoutSchedule` 改用 `tryUpdateWithVersionCheck`）
  - 改了什么：
    - `IJobFireStore.completeFireAndUpdateSchedule()`: return type `void` → `boolean`
    - `JobFireStoreImpl.completeFireAndUpdateSchedule()`: 返回 `tryUpdateWithVersionCheck(fire)` 结果
    - `JobFireStoreImpl.failFireWithoutSchedule()`: `updateEntityDirectly` → `tryUpdateWithVersionCheck`
    - `JobCompletionProcessorImpl.tryCompleteFireAndGetStatus()`: 检查返回值，false 时跳过指标
    - 更新 3 个 test mock 实现（`TestJobCompletionProcessor`、`TestJobE2E`、`TestJobTimeoutChecker`）
    - `TestJobCompletionProcessor.MockFireStore`: 新增 `simulateConflict` 支持
  - 正确性证明完成：
    - `failFireWithoutSchedule` 调用者（`JobCompletionProcessorImpl` + `JobTimeoutCheckerImpl`）：调用后不读取 fire 实体状态 → 安全
    - `fetchRunningFires` 无外层 Session：`findAllByQuery` 自动创建临时 Session，实体完全加载，标量访问安全
  - 新增回归测试：
    - `TestJobCompletionProcessor.testCompleteFireVersionConflict_skipsMetrics` — 验证版本冲突时跳过指标
    - `TestJobFireStoreRace.testFailFireWithoutSchedule_noThrowOnVersionConflict` — 验证版本冲突时不抛异常
  - 验证：所有既有 + 新增测试通过
    - `TestJobCompletionProcessor`: 20 ✅
    - `TestJobE2E`: 4 ✅
    - `TestJobTimeoutChecker`: 22 ✅
    - `TestJobFireStoreRace`: 11 ✅（+1 新增）
  - Doc-sync:
    - `docs-for-ai/02-core-guides/concurrency-and-transactions.md`: `completeFireAndUpdateSchedule` 签名 `void` → `boolean`
    - `ai-dev/design/nop-job/metrics-design.md`: completion metrics 触发语义（仅 DB 确认后触发）
    - `ai-dev/design/nop-job/retry-integration-design.md`: 签名同步 + 条件分支
- 偏差：无

## Summary

- Plan 302 Phase 1 完成（exit criteria 全部满足）
- Plan 302 Phase 2 待开始（blocked，依赖 Phase 1）
