# 2026-08-06 开发日志

### 2026-08-06 nop-stream 深度审计基线与路线图

- 独立对抗审查（`ses_029a758e0ffez62Y1I4OJdV5Cd`）判定初稿需修订：审计 work item 过大、capability inventory 和外部环境资格不可关闭、stateful/distributed/connector 边界重叠、P2 finding disposition 不完整。
- 已按审查意见修订：独立审计路线图拆为 inventory、环境资格、Java/XDSL/Delta、checkpoint、state、window、CEP、control/data plane、两类 connector、test/tool、docs/finding disposition 共 15 个原子审计项；P2 也必须取得明确 disposition。
- 第二轮独立审查（`ses_02988057affedwW9Jm9cmXzD7K`）继续要求修订：schema/inventory、test governance 与 finding disposition 仍过大；已进一步拆成 source-manifest、环境资格、current corpus、historical P0/P1、historical P2 和 docs/readiness work items，并规定 required external lane blocked 时不能形成 e2e-proved 能力或 production-readiness。
- 第三轮独立审查（`ses_02904e31effeWOvzBUZAlya39L`）确认无 blocker，但指出 finding corpus 分母、P0/P1/P2 disposition 批次、`blocked` schema 和依赖图仍未闭合；已冻结为 item 4 的显式 corpus/validator，按技术域拆为 18-22 五个 disposition work item，并同步所有 stage-table 依赖到图。
- 第四轮独立审查（`ses_028e6545bffebUGXooqGaNiy2i`）仅发现 stage table 的直接依赖未在 Mermaid 中完整显式化；已定义 `Deps` 为 direct hard prerequisites，并补齐图的所有对应 direct edges。
- 完成 `nop-stream` 设计目标、历史审计、最新 production 审计、active remediation plans 与关键 live code 路径的交叉调研；结论是现有 production roadmap 记录建设 ownership，但不能替代独立的端到端能力验证。
- 新增分析 `ai-dev/analysis/2026-08/2026-08-06-nop-stream-audit-baseline-and-roadmap-analysis.md`，固定设计目标、证据分级、active-plan 前置、审计语义与已否决方案。
- 新增独立审计路线图 `ai-dev/backlog/nop-stream-independent-audit-roadmap.md`：保留现有 3 份 active production plans 为 work items 1-3，后续按能力矩阵、入口/本地执行、stateful correctness、真实分布式、connector、测试与文档治理分解。
- 验证：纯文档变更，未运行 Maven；待执行 doc link checker。
- Doc-sync: `docs-for-ai/` 未修改；owner-doc 更新留给后续经审计确认的契约变更。
### nop-metadata-audit-remediation：MR8 R8.2 执行（plan-2026-08-06-0914-2 Phase 3 完成：AR-23③ refresh 失败入 IndexResult + AR-23④ 搜索 limit 负数拒绝）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0914-2-r8-2-diagnostics-logging-group.md`（R8.2 诊断与日志组，AR-16 + AR-23③⑤④）
- Action: EXECUTE Phase 3（red 先于修复判别性落地）
- VERIFY（Phase 3 后）：`TestNopMetaIndexBuilder` 4→5（+1 refresh 失败入 result）+ `TestNopMetadataSearchIntegration` 15→17（+2 limit 负数拒绝 + 归一化语义回归）；`./mvnw test -pl nop-metadata -am -T 1C` **997/0 全绿**（R8.1 基线 986 + 11 新增三 Phase）+ BUILD SUCCESS

**Phase 3（AR-23③ + AR-23④）**：
- **AR-23③ 修复（Fix）**：`NopMetaIndexBuilder.buildFullIndex` refreshBlocking catch 块（原仅 LOG.warn）→ `result.setFailed(result.getFailed() + 1)` + `result.setErrors(List.of("Index refresh failed for type: ..."))`（含 refresh 信息）+ LOG.warn 保持；`indexed` 如实反映已 addDocs 数（setIndexed(docs.size()) 在 catch 之外运行）——计数语义与 addDocs 失败路径（failed += docs.size()）显式区分
- **AR-23④ 修复（Fix）**：`NopMetaSearchBizModel.searchMetadata` limit 分支顺序调整——`limit < 0` → 显式抛新错误码 `ERR_SEARCH_LIMIT_INVALID`（`nop.err.metadata.search-limit-invalid`，MiscErrors Search 段，沿 AR-09 ERR_PAGINATION_LIMIT_INVALID 先例）+ `.param(ARG_LIMIT)`，在设置 request 之前拒绝；null→20 / >100→100 语义保持
- **判别性测试（Proof，red→green 实测）**：`TestNopMetaIndexBuilder` +1——mock refreshBlocking 抛错 → 6 个结果行全部 indexed=1/failed=1 + errors 含 "refresh"（修复前 red：failed=0 静默）；`TestNopMetadataSearchIntegration` +2——limit=-1 → ERR_SEARCH_LIMIT_INVALID + param limit=-1 + verify searchEngine never called（修复前 red：NPE 直通引擎）；limit=50→50 / 200→100 / null→20 归一化语义回归
- 文档同步：`docs-for-ai/03-modules/nop-metadata.md` Pseudo-BizModel 例外段补 `searchMetadata` limit 契约（20/100/负数显式拒绝）+ `rebuildSearchIndex` IndexResult refresh 失败计数语义
- Doc-sync: 模块文档 + 本日志 + plan 文件 Phase 3 勾选

### nop-metadata-audit-remediation：MR8 R8.2 执行（plan-2026-08-06-0914-2 Phase 2 完成：AR-23⑤ 外部表结构错误归类 + NULL 精度）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0914-2-r8-2-diagnostics-logging-group.md`（R8.2 诊断与日志组，AR-16 + AR-23③⑤④）
- Action: EXECUTE Phase 2（red 先于修复判别性落地）
- VERIFY（Phase 2 后）：`TestExternalTableStructureReader` 4→8（+4 red→green：扫描故障归类 / getDatabaseProductName 归类 / 方言门禁不误伤 / NULL 精度）+ `TestNopMetaDataSourceBizModel` 5→7（+2 serializeColumns JSON null 兼容）；`./mvnw test -pl nop-metadata -am -T 1C` **994/0 全绿**（R8.1 基线 986 + 8 新增；计数与既有 1-test run-to-run 波动一致）+ BUILD SUCCESS

**Phase 2（AR-23⑤）**：
- **AR-23⑤ Decision（裁定）**：`ExternalColumnInfo.precision/scale int → Integer`（service 模块 DTO，非 api 模块公共面，plan-first 声明于本 plan）；消费者影响评估——唯一 Java 消费方 `NopMetaDataSourceBizModel.serializeColumns`（:553-554）put null → JSON null（合法）；structure JSON 消费方 `MetaTableFieldResolver` buildSql 解析**不读 precision/scale**（grep 实证 0 引用）；`NopMetaTable.buildSql` JSON 文本消费面无类型化读取 precision/scale；`OrmModelImporter` 的 getPrecision/getScale 属 `IColumnModel`（不同类，无影响）
- **AR-23⑤ 错误归类（Fix）**：`getTables` 扫描 SQLException → 新错误码 `ERR_EXTERNAL_TABLE_SCAN_FAILED`（`nop.err.metadata.external-table-scan-failed`，DataSourceErrors）+ 真实 productName（方言门禁已取）+ 原始异常消息（不再硬编码 "unknown"）；`getDatabaseProductName` 失败同归扫描故障（productName 此时如实 "unknown"——元数据访问故障非方言不支持）；方言门禁 `ERR_DATASOURCE_TYPE_NOT_SUPPORTED` 保持不动；**死码删除**：ERR_DIALECT_NOT_SUPPORTED 全仓 0 引用后删除（沿 R8.1 INVALID_CRON 死码先例）
- **AR-23⑤ NULL 精度（Fix）**：`safeInt` → 返回 Integer（wasNull → null）；`setPrecision/setScale` 接收 Integer；新增 `safeOrdinal`（ORDINAL_POSITION 无 NULL 语义，null 保底 0 保持 int 契约）
- **判别性测试（Proof，red→green 实测）**：`TestExternalTableStructureReader` +4——mock getTables 抛 SQLException → ERR_EXTERNAL_TABLE_SCAN_FAILED + databaseProductName=MySQL（非 "unknown"）+ 原始消息保留；getDatabaseProductName 抛 → 扫描错误码；Oracle → 仍 ERR_DATASOURCE_TYPE_NOT_SUPPORTED（不误伤）；COLUMN_SIZE/DECIMAL_DIGITS NULL → precision/scale null（Integer 局部变量使测试修复前后均可编译——**red 实测**：修复前 3/3 FAIL：errorCode=dialect-not-supported ×2 + precision=0）；`TestNopMetaDataSourceBizModel` +2 serializeColumns null/non-null 双测（null → JSON null 合法性回解析实证 + 数值语义保持）
- 文档同步：`docs-for-ai/03-modules/nop-metadata.md` §失败路径显式化新增「外部表结构扫描故障分类（AR-23⑤，R8.2）」段（scan-failed 错误码 + NULL 精度 JSON null + 消费方不读声明）
- Doc-sync: 模块文档 + 本日志 + plan 文件 Phase 2 勾选

### nop-metadata-audit-remediation：MR8 R8.2 执行（plan-2026-08-06-0914-2 Phase 1 完成：AR-16 质量规则 SQL 日志脱敏）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0914-2-r8-2-diagnostics-logging-group.md`（R8.2 诊断与日志组，AR-16 + AR-23③⑤④，执行顺序 {2} of 3——R8.1 已完成）
- Action: EXECUTE Phase 1（red 先于修复判别性落地）
- VERIFY（Phase 1 后）：`TestMetaQualityRuleExecutorLogRedaction`（新建 3/3）red→green 实测——修复前 3/3 red（queryLong/queryTimestamp/custom_sql 三处 INFO 日志均含完整 SQL 字面量 13800138000/张小明）；focused 质量回归 34/34 全绿（CustomSqlSandbox 19 + Freshness 3 + RegexDialect 6 + ErrorParams 3 + 新 3）；`./mvnw test -pl nop-metadata -am -T 1C` **989/0 全绿**（R8.1 基线 986 + 3 新增）+ BUILD SUCCESS

**Phase 1（AR-16）**：
- **AR-16 Decision（裁定）**：方案 (a) sqlHash——`queryLong`/`queryTimestamp` 新增 `sqlHashOf(sql)` 计算（对齐 judgeCustomSql :295 既有实现），custom_sql 路径复用同源摘要；三处 `LOG.info` 只输出 sqlHash，完整 SQL 降 DEBUG 级保留可调试性；方案 (b) 纯降 DEBUG 不可选（INFO 级无 sqlHash → 可追溯性丢失，与 Exit Criteria 矛盾）
- **AR-16 修复（Fix）**：`MetaQualityRuleExecutor` 三处调用点——`queryLong`（:642）`LOG.info("qualityRule SQL: {}", sql)` → `LOG.info("qualityRule SQL executed: sqlHash={}", sqlHashOf(sql))` + `LOG.debug("qualityRule SQL: {}", sql)`；`queryTimestamp`（:661）同型；`querySingleValue`（:683）`LOG.info("qualityRule custom_sql: {}", sql)` → sqlHash INFO + 完整 SQL DEBUG；details 的 sqlHash（custom_sql :295-296）保持不动
- **判别性测试（Proof，red→green 实测）**：新建 `TestMetaQualityRuleExecutorLogRedaction` 3/3——custom_sql / volume（sql 子查询 sourceSql 内嵌 13800138000）/ freshness（sourceSql 内嵌 张小明）三路径经 `judge` 公开入口 + mock JDBC 执行，ListAppender 捕获断言：INFO 级事件不含敏感字面量/完整 SQL + 含 sqlHash（沿 TestNopMetaSearchProcessor ListAppender 先例）；**red 实测**：修复前 3/3 FAIL（INFO 含完整 SQL）
- Doc-sync: `No owner-doc update required` 显式记录（模块文档无质量规则日志格式契约——仅 errorCode 参数面在 R8.1 AR-13 已治理）；本日志 + plan 文件 Phase 1 勾选

### nop-metadata-audit-remediation：VERIFY full-green 复验（R8.1 收口后 build + test 全绿，plan-2026-08-06-0914）

- Mission: `nop-metadata-audit-remediation`
- Action: VERIFY（mission-driver 验证步骤，R8.1 已由 EXECUTE 提交于 81382e5a0，工作树仅剩 R8.2/R8.3 预创建计划未提交）
- 验证（`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` → **BUILD SUCCESS**；`./mvnw test -pl nop-metadata -am -T 1C` → **985/0 全绿**（R8.1 收口口径 986/0，本跑 985/0 同基线）+ BUILD SUCCESS）：compile step exit 0；checkstyle 维持历史惯例 N/A（插件全仓基线不干净——nop-api-core 9164 违例 / nop-metadata-api 978，mission lint 命令按设计回退 'lint not configured'，R8.1 改动文件无新增类型违例，QualityRuleResultDTO RedundantImport 系 c3162d4da 遗留非本次引入）
- 偏差：无（lint 命令本身按 mission 配置带 `|| echo 'lint not configured'` 回退，非本次引入）
- Commit: （R8.2/R8.3 计划文件随本 VERIFY 提交）
- Doc-sync: 本日志 + R8.2/R8.3 计划落库（`ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0914-2-r8-2-diagnostics-logging-group.md`、`2026-08-06-0914-3-r8-3-import-memory-filter-correctness.md`）；No owner-doc update required

### nop-metadata-audit-remediation：MR8 R8.1 收口（plan-2026-08-06-0914-1 全 4 Phase 完成 + closure，AR-11~15 五项落地，plan completed + roadmap R8.1 → done）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0914-1-r8-1-quality-execution-scoring-correctness.md`（R8.1 质量执行/评分正确性组，AR-11/12/13/14/15，执行顺序 {1} of 3——R8.2 依赖本 plan 完成后执行）
- Action: EXECUTE（Phase 1-4 全执行，red 先于修复判别性落地）+ CLOSE（独立子 agent closure audit + Closure Gates + 工具验证）
- VERIFY（closure 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **986/0 全绿**（R7.3 970 基线 + 16：service 模块 969→985 + web 1；判别性测试 +16 red→green 实测）+ `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` exit 0 + `node ai-dev/tools/check-doc-links.mjs --strict` exit 0 + `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high` exit 0；checkstyle N/A（历史惯例，插件仅 -Pqa profile）

**五项 finding 终态（全部判别性测试 red→green 实证，git stash 逐项 red）**：
- **AR-11**（judgeRegex 方言判定收窄）：子串启发式 → `REGEXP_UNSUPPORTED_SIGNATURES` 签名集合（not supported / unknown function / syntax error at or near（PG 真实签名））；裸 regexp/syntax 排除——MySQL "Got error...regexp" / H2 "syntax" 字面量 → 显式 ERROR；docs P2-08 例外说明收窄同步；TestMetaQualityRuleExecutorRegexDialect 6/6（2 red 实测）
- **AR-12**（调度契约 MA7.5-01）：cpId==null 分支移入 try 内 + 新错误码 `ERR_CHECKPOINT_MISSING_ID`（删除 0 引用死码 INVALID_CRON）+ buildErrorResult 复用；TestMetaQualityCheckpointSchedulerCronReadFailure +1 + TestMetaQualityCheckpointScheduler +1 e2e 遗留 job（2 red 实测：异常逃逸 + job FAILED）
- **AR-13**（错误参数归属）：ErrorCode 声明 `{ruleKey}` → `{sqlHash}` + throw 点 `.param(ARG_SQL_HASH)` 替代 `.param("sql")`（SQL 原文不进错误消息，与 R8.2 AR-16 脱敏一致）；evalExpectPassWhen 签名 +ruleKey 替换 `<evalExpectPassWhen>` 字面量；TestMetaQualityRuleExecutorErrorParams 3/3 + TestEvalExpectPassWhenErrorPath 升级判别断言（red 实测：sqlHash null / message 空洞 / 字面量上下文）
- **AR-14**（异常规则结果行 + DTO 对账）：catch 中 clearSession 之后补写 ERROR 结果行 + flush（非 database 异常规则表进 affectedTableIds → 本次 run autoScore 用 ERROR 行重算不再复用陈旧 PASS）+ summary 补 totalRuleCount/skipCount + CheckpointExecutionResultDTO 新增 skipCount + QualityRuleResultDTO 确定性新增 status/message（nop-metadata-api 两扩展 plan-first）+ BizModel mapRuleResults 映射契约（ruleResults 条目数 = totalRuleCount）；TestNopMetaQualityCheckpointBizModel +2 端到端（run1 PASS+100 → DROP 表 → run2 ERROR 行 + score 100→0；PASS+ERROR+SKIP 混合对账 totalRuleCount=3/ruleResults=3/skip=1）（2 red 实测：totalRuleCount=0 / affectedTableIds=[]）
- **AR-15**（freshness fail-loud）：`ageMinutes<0` → FAIL + details.rawAgeMinutes + "in the future (clock skew suspected)" 诊断消息（不钳制）；TestMetaQualityRuleExecutorFreshness 3/3（2 red 实测未来时间戳 PASS）

**Closure**：
- 独立子 agent closure audit（fresh session `ses_02b189e3effefEqIPmECtciqx1`）：**READY_TO_CLOSE**——五 AR 逐条 live code 证据 PASS（含 file:line + test name）、Anti-Hollow 调用链追踪 PASS（executor catch :156 → buildErrorJudgment → results.add → affectedTableIds → resultWriter.append :176（clearSession :163 之后）→ flushSession，运行时实证 testExceptionRuleWritesErrorRowAndRescoresWithError）、无空壳/无静默跳过/无 live defect 降级（Deferred 段仅 watch-only residual + out-of-scope improvement 逐条附理由）；focused 复跑 9 类 63/63 全绿；非阻塞观察 3 条显式记录（未提交 / scheduler 缺失 id 日志 checkpointId=null cosmetic / Phase 4 待勾选——已在本轮收口）
- 工具验证：check-plan-checklist --strict exit 0（0 未勾选 + Closure Evidence 已写入）/ check-doc-links --strict exit 0 / scan-hollow --severity high exit 0
- Doc-sync: roadmap MR8 段 R8.1 → done（v28 header + 里程碑表 MR8 行）+ arm-index MR8 R8.1 收口记录段（AR-11~15 → fixed 含修复摘要 + 测试证据）+ plan Closure 段 + 本日志；docs-for-ai/03-modules/nop-metadata.md（regex 方言例外收窄 + 检查点执行结果 DTO 段）


### nop-metadata-audit-remediation：MR8 R8.1 执行（plan-2026-08-06-0914-1 Phase 3 完成：AR-13 错误参数归属 + AR-14 异常规则结果行与 DTO 对账）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0914-1-r8-1-quality-execution-scoring-correctness.md`（R8.1 质量执行/评分正确性组，AR-11/12/13/14/15）
- Action: EXECUTE Phase 3（red 先于修复判别性落地——针对性子 stash 实测 red 后再恢复修复版 green）
- VERIFY（Phase 3 后）：focused 84/84 全绿（含 TestNopMetaQualityCheckpointBizModel 27/27、TestMetaQualityRuleExecutorErrorParams 3/3、TestEvalExpectPassWhenErrorPath 10/10、TestNopMetaBizInterfaceCompleteness 3/3、TestMetaQualityScorer 1/1、调度器 e2e 8/8）

**Phase 3（AR-13 + AR-14）**：
- **AR-13 Decision（裁定）**：queryLong/queryTimestamp 静态调用链无 ruleKey 上下文 → 最小方案 = 改 ErrorCode 声明 `{ruleKey}` → `{sqlHash}`（非 `{sql}`——完整 SQL 经 NopException.getMessage 无条件拼入 params 会随 scheduler/executor LOG.error 落日志，与 R8.2 AR-16 脱敏目标冲突）；custom_sql 路径 + evalExpectPassWhen 有 ruleKey 可取 → `.param(ARG_QUALITY_RULE_ID, ruleKey)`（替换字面量 `<evalExpectPassWhen>`）
- **AR-13 修复（Fix）**：`QualityErrors.java` ERR_QUALITY_SQL_NO_ROW/FAILED 占位改 `{sqlHash}`（ARG_SQL_HASH）；throw 点（queryLong/queryTimestamp）`.param("sql", sql)` → `.param(ARG_SQL_HASH, sqlHashOf(sql))`（sqlHashOf 既有 public static 复用）+ error param 保持；`evalExpectPassWhen` 签名 `(String, double, String ruleKey)`，judgeCustomSql 调用点传入真实 ruleKey
- **AR-14 修复（Fix）**：`MetaQualityCheckpointExecutor` catch 块——errors.add → errorCount++ → clearSession **之后**构造 ERROR 判定（buildErrorJudgment：status=ERROR + message + details.error）→ `results.add(buildResultEntry(...))`（catch 路径也进 summary.results）→ 非 database 异常规则的表加入 affectedTableIds → `resultWriter.append` + flushSession（append 失败留证不掩盖原始异常）；summary 补 `totalRuleCount`（= resolution.rules.size()）与 `skipCount`；`CheckpointExecutionResultDTO` 新增 `skipCount` 字段 + `QualityRuleResultDTO` 确定性新增 `status`/`message` 字段（nop-metadata-api 两个确定性扩展同批 plan-first 声明，本 plan 即裁决载体）；BizModel 检查点路径填充 totalRuleCount/skipCount/ruleResults（mapRuleResults 映射契约：qualityRuleId + status + message，其余字段单规则路径语义默认值）
- **判别性测试（Proof，red→green 实测）**：新建 `TestMetaQualityRuleExecutorErrorParams` 3/3（mock 空 ResultSet → ERR_QUALITY_SQL_NO_ROW message 渲染真实 sqlHash + 无 {ruleKey} 字面 + 无 SQL 原文 + 无 sql param；mock SQLException → ERR_QUALITY_SQL_FAILED + sqlHash/error param + cause 保留 + 无 SQL 原文；judge 入口 custom_sql expectPassWhen 非法 → param qualityRuleId=r-custom 非 `<evalExpectPassWhen>`）；`TestEvalExpectPassWhenErrorPath` 升级 3-arg 签名 + 判别断言（ruleKey 归属）；`TestNopMetaQualityCheckpointBizModel` +2——`testExceptionRuleWritesErrorRowAndRescoresWithError`（run1 PASS+score=100 → DROP 物理表 → run2 异常 → ERROR 行落盘（clearSession 后存活）+ latest=ERROR + score 100→0 全链路断言）与 `testCheckpointDtoCountsReconcile`（PASS+ERROR+SKIP 混合 → totalRuleCount=3/ruleResults=3/skipCount=1/passCount=1/errorCount=1 逐条断言 + exec() GraphQL selection 补 totalRuleCount/skipCount/ruleResults）；**red 实测**：针对性 stash 旧代码——AR-14（stash CheckpointExecutor）totalRuleCount=0/affectedTableIds=[] 2 red；AR-13（stash RuleExecutor）TestEvalExpectPassWhenErrorPath 4F+6E（NoSuchMethod 3-arg）+ ErrorParams 3/3 red（sqlHash null / message 空洞 / qualityRuleId=<evalExpectPassWhen>）
- 回归：84/84 focused 全绿（checkpoint BizModel 27 含既有 errorCount 隔离断言、cron fireNow e2e 8、scorer 1、schema resolution 3、接口契约 3）
- 文档同步：`docs-for-ai/03-modules/nop-metadata.md` 检查点 GraphQL 示例 + 新增「执行结果 DTO（AR-14，R8.1）」段（totalRuleCount/skipCount/ruleResults 映射契约 + ruleResults 条目数 = totalRuleCount 对账）
- Doc-sync: 模块文档 + 本日志


### nop-metadata-audit-remediation：MR8 R8.1 执行（plan-2026-08-06-0914-1 Phase 2 完成：AR-11 judgeRegex 方言判定收窄 + docs 同步）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0914-1-r8-1-quality-execution-scoring-correctness.md`（R8.1 质量执行/评分正确性组，AR-11/12/13/14/15）
- Action: EXECUTE Phase 2（red 先于修复判别性落地，git stash 实测 red 后再恢复修复版 green）
- VERIFY（Phase 2 后）：`TestMetaQualityRuleExecutorRegexDialect`（新建 6/6）+ `TestNopMetaQualityRuleBizModel` 18/18 全绿（focused）+ check-doc-links --strict 0 errors

**Phase 2（AR-11）**：
- **修复（Fix）**：`isRegexpUnsupported` 子串启发式（`contains("regexp")||contains("syntax")||function&&not found`）替换为签名集合匹配——`REGEXP_UNSUPPORTED_SIGNATURES = {"not supported", "unknown function", "syntax error at or near"}`（PG 不支持 REGEXP 运算符的真实签名）；**裸 "regexp"/"syntax" 显式排除**（MySQL 非法 pattern 报错 "Got error ... from regexp"、H2 非法 pattern 消息可能含 "syntax" 均为规则级正则错误 → ERROR）；主方案不改方法签名（productName 门控备选未采用）
- **判别性测试（Proof，red→green 实测）**：新建 `TestMetaQualityRuleExecutorRegexDialect` 6/6——mock Connection 抛 "Got error ... from regexp" → ERROR（修复前 SKIP red）/ "not supported" → SKIP+reason / "unknown function" → SKIP+reason / PG "syntax error at or near" → SKIP+reason / H2 消息含 "syntax" 字面量 → ERROR（修复前 SKIP red）/ 真实 H2 合法 pattern → PASS；git stash 修复后 2 red 实测
- **docs 同步**：`docs-for-ai/03-modules/nop-metadata.md` §regex 规则方言例外（P2-08）段更新——SKIP 仅保留给真实方言不支持场景（按签名集合门控），MySQL/H2 支持 REGEXP 的方言上规则级正则错误显式 ERROR（AR-11 行为收紧）；check-doc-links --strict exit 0
- 回归：TestNopMetaQualityRuleBizModel 18/18（含 regex 规则 PASS/FAIL 路径）全绿
- Doc-sync: 模块文档 + 本日志


### nop-metadata-audit-remediation：MR8 R8.1 执行（plan-2026-08-06-0914-1 Phase 1 完成：AR-12 调度契约 + AR-15 freshness fail-loud）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0914-1-r8-1-quality-execution-scoring-correctness.md`（R8.1 质量执行/评分正确性组，AR-11/12/13/14/15，执行顺序 {1} of 3）
- Action: EXECUTE Phase 1（red 先于修复判别性落地，git stash 实测 red 后再恢复修复版 green）
- VERIFY（Phase 1 后）：`TestMetaQualityRuleExecutorFreshness`（新建 3/3）+ `TestMetaQualityCheckpointSchedulerCronReadFailure` 2/2 + `TestMetaQualityCheckpointScheduler` 8/8 全绿（focused）

**Phase 1（AR-12 + AR-15）**：
- **AR-12 修复（Fix）**：`MetaQualityCheckpointScheduler.executeScheduledCheckpoint` 的 `cpId==null` 分支从 try **之外**移入 try **之内**（params null 一并防御），改抛新错误码 `ERR_CHECKPOINT_MISSING_ID`（`nop.err.metadata.checkpoint-missing-id`，语义自足无占位符）并复用既有 `buildErrorResult`——不再复用语义不符的 `ERR_CHECKPOINT_SCHEDULER_INVALID_CRON`（该码全仓 0 引用后删除，沿 R6.6 P2-03 死码先例）；全路径不外抛（MA7.5-01 契约：BeanMethodJobInvoker 把逃逸异常转 JobFireResult.ERROR → LocalJobScheduler 永久 FAILED）
- **AR-15 Decision + 修复（Decision + Fix）**：裁定负年龄（未来时间戳/DB 时钟超前）= **fail-loud**——`ageMinutes < 0` 显式 FAIL（不选钳制 0 → PASS：钳制继续掩盖时钟偏移违约）；details 新增 `rawAgeMinutes` 暴露原始差值（actualValue 保持原始负值不伪造 0），消息含 "in the future (clock skew suspected)" 诊断文本；无 maxAgeMinutes 时负年龄同样 FAIL（负值本身即违约）
- **判别性测试（Proof，red→green 实测）**：新建 `TestMetaQualityRuleExecutorFreshness` 3/3（真实 H2 + 未来时间戳 → FAIL + rawAgeMinutes<0 + 消息诊断 + actualValue=负差值 / 无 maxAgeMinutes 负年龄 FAIL / 过去时间戳 PASS 与超龄 FAIL 回归）；`TestMetaQualityCheckpointSchedulerCronReadFailure` +1 `testMissingCheckpointIdReturnsErrorResultInsteadOfThrowing`（直接调 executeScheduledCheckpoint(空 params) → assertDoesNotThrow + executionErrors 含 checkpoint-missing-id + 不出现 invalid-cron）；`TestMetaQualityCheckpointScheduler` +1 `testLegacyJobMissingCheckpointIdSurvives`（手工构造缺 checkpointId 的 beanMethod JobSpec 直接 addJob → fireNow 经真实 BeanMethodJobInvoker → job WAITING 非 FAILED + 二次 fireNow 存活）——**red 实测**：git stash 修复后 2 FAIL + 2 Error（future PASS / 异常逃逸 / job FAILED）
- 回归：调度器 cron 既有 7 测 + cron-read-failure 既有 1 测全绿
- Doc-sync: `No owner-doc update required` 显式记录（模块文档无错误码清单、无调度器失败路径段；调度失败语义已在 scheduler javadoc MA7.5-01 段在位）；本日志


### nop-metadata-audit-remediation：MR8 R8.0 裁决收口（plan-2026-08-06-0832-1 全 3 Phase 完成 + closure，AR-11~23 全部终态，plan completed + roadmap R8.0 → done）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0832-1-mr8-expander-adjudication.md`（MR8 展开器裁决——Follow-up Backlog AR-11~23 共 13 条逐项裁决：提级安全/正确性类 + 终局归类其余；纯裁决 + 文档计划，无代码变更）
- Action: EXECUTE（Phase 1-3 全执行，逐项 live 代码复核）+ CLOSE（独立子 agent closure audit + Closure Gates + 工具验证）
- VERIFY（closure 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **970 tests / 0 failures / 0 errors / 0 skipped**（与 MR7 R7.3 基线一致，纯文档计划无回归）+ `node ai-dev/tools/check-doc-links.mjs --strict` exit 0 + `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` exit 0

**裁决结果（13 条全部终态：提级 19 + 归类 3，0 悬置）**：
- **提级 19 项**（全部 live 复核确认，0 翻案，归属 roadmap MR8 段 R8.1~R8.4 行）：安全 2（AR-16 `MetaQualityRuleExecutor.java:631,647,669` 三处 LOG.info 完整 SQL/custom_sql 字面量——R6.2 rawJdbcUrl 同类面未治理；AR-23⑩ `MetaModelChangedEventPublisher.java:201-203` 快照 Map 分支跳过敏感列脱敏——AR-07 契约缺口）；正确性/诊断 17（AR-11 isRegexpUnsupported 子串启发式 MySQL 正则错误误判 SKIP；AR-12 cpId==null 在 try 外抛错 + 错错误码违反 MA7.5-01；AR-13 `{ruleKey}` 占位恒无法解析 + expectPassWhen 字面量上下文；AR-14 异常规则不写 ERROR 行 → autoScore 陈旧复用 + DTO totalRuleCount/ruleResults 从不填充；AR-15 负年龄恒 PASS；AR-18 手拼 JSON 不转义 + buildSql 未类型化裸 CCE；AR-19 内存过滤 LIKE/NULL/空 or 三处语义漂移；AR-20 MySQL NULLS FIRST/LAST 无条件拼接 + join 键精确类比较；AR-21 catch-all 吞 R6.4 fail-loud + lexicographic 全局回退；AR-22 SLA 未知单位静默按毫秒；AR-17 scan 级失败部分持久化 + 事件缺失（契约 drift，提级非 docs）；AR-23① delete 先摘 cron 后提交丢调度；AR-23② 索引重建不清陈旧文档；AR-23③ refresh 失败仅 warn 不入 IndexResult；AR-23④ 搜索 limit 负数直通；AR-23⑤ getTables 异常统一误报 ERR_DIALECT_NOT_SUPPORTED + NULL 精度归 0；AR-23⑨ manifest 跨 DRAFTING 模块解析）
- **归类 3 项**（每条附 Why Not Blocking Closure）：AR-23⑥/⑦ → optimization candidate（对账/profiler 均为用户显式触发的低频运维操作，性能面无活跃缺陷证据）；**AR-23⑧ → watch-only residual（live 复核翻案）**——UK_NOP_META_MANIFEST_MODULE_VER（orm.xml:2246）+ UK_NOP_META_MODULE_ID_VER（orm.xml:299）R3.19 36 UK constraint 批已在位（三方言 DDL 实证），read-then-insert 竞态由 DB UK fail-loud 保护，无静默重复；审计"无唯一约束"主张过期
- **分组建议**（写入裁决记录供 R8.x plan 引用）：R8.1 质量执行/评分正确性组（AR-11/12/13/14/15，AR-11 行为收紧须同步 docs P2-08 方言例外说明）；R8.2 诊断与日志组（AR-16 + AR-23③⑤④）；R8.3 导入与内存过滤组（AR-18/19）；R8.4 杂项正确性组（AR-20/21/22/17 + AR-23①②⑨⑩）
- 偏差：无（AR-23⑧ 按 live 证据翻案归类，翻案原因显式记录——计划 Current Baseline 已声明"裁决依据以执行时的 live 复核为准"）
- **Closure**：独立子 agent closure audit（fresh session）READY_TO_CLOSE——三 Phase Exit Criteria 全 PASS（逐条 live code 证据 file:line）、Closure Gates 全 PASS、Anti-Hollow（纯文档计划无新代码——裁决记录与 live 代码行为一致性由 audit 抽样覆盖）、deferred 分类诚实（3 项归类逐条复核无 live defect 降级）；check-plan-checklist --strict exit 0 / check-doc-links --strict exit 0
- Doc-sync: roadmap（MR8 段新增 R8.0 done + R8.1~R8.4 行 + 里程碑表 MR8 行 + header v27）+ arm-index（MR8 裁决记录段 + header 更新）+ plan Closure 段 + 本日志；`No owner-doc update required` 成立（docs-for-ai 无裁决内容变更面——仅 roadmap/arm-index/plan 三处裁决载体）

### nop-metadata-audit-remediation：MR7 R7.3 收口（plan-2026-08-06-0553-3 全 3 Phase 完成 + closure，六项 finding 落地，plan completed + roadmap R7.3 → done + MR7 全段收口）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-3-query-quality-import-correctness-batch.md`（AR-01 P0 / AR-06 / AR-07 / AR-08 / AR-09 / AR-10，执行顺序 {3} of 3）
- Action: EXECUTE（Phase 1-3 全执行，red 先于修复判别性落地）+ CLOSE（独立子 agent closure audit + Closure Gates + 工具验证）
- VERIFY（closure 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **970 tests / 0 failures / 0 errors / 0 skipped**（R7.2 基线 949 + 21 新增：Phase1 +7 / Phase2 +10 / Phase3 +4）+ `./mvnw compile -pl nop-metadata -am` exit 0 + check-plan-checklist --strict exit 0 + check-doc-links --strict exit 0（0 errors，12 warnings 均为其他历史 plan 文件非本 plan 引入）+ scan-hollow --severity high exit 0（0 findings）；checkstyle N/A（历史惯例，插件仅 -Pqa profile）

**Phase 1（JOIN/聚合分页参数修复，AR-01 P0 + AR-09）**：
- 失败用例 red 实测：`TestNopMetaJoinBizModel.testSameDbSqlTableJoinReturnsRealRows`（limit=2/offset=1 → H2 `Invalid value "3" for parameterIndex` SQLException，双绑实证）+ `TestAggregationExternalJoinAndPagination` +2（external↔external / mixed 聚合分页，同型 SQLException）+ `TestMetaJoinTruncateOverflow` +4（负 limit / int 溢出裸 IllegalArgumentException）
- 双绑修复：三处调用点删 `params.add(limit/offset)`（executeJdbcQuery 统一绑定）；entity-entity 正确路径 :291-297 未动
- limit 归一化裁定 (a)-(d) 落地：新错误码 `ERR_PAGINATION_LIMIT_INVALID` + BizModel 入口拒绝（负值显式错误码）+ 缺省有界 1000 + truncate long 运算 defense-in-depth + queryTableData 差异文档化（docs「查询分页契约」段）
- SqlPagination javadoc 双绑定模式注记；GraphQL 端到端分页测试；956/0 全绿

**Phase 2（质量引擎契约修复，AR-06 + AR-07 + AR-10）**：
- AR-06：SLA 已配置 + 无 Catalog → slaFresh=false（契约裁定：无数据即不满足任何已配置 SLA；仅空 map 保持 pass）+ 死分支删除 + "no Catalog data" 可诊断消息 + 3 判别性测试（2 red 实测）；**治理纠正**：commit 9b769490e 对 MetaContractChecker 仅删版权头（real_diff 0 行）——R3.14 虚假关闭确认；R3.x claimed-fixed 18 文件 diff 抽查 17 REAL_DIFF / 唯一 COPYRIGHT_ONLY = MA7.6-05；arm-index「MR7 R7.3 收口记录」+ roadmap R3.14 行纠正
- AR-07：MetaQualityCheckpointExecutor.resolveDefaultSchema 回退 metaSchema（与单规则路径同语义）+ 两处 javadoc 同步 + TestMetaQualityCheckpointExecutorSchemaResolution 3/3（接线验证：mock tableRefExecutor 调 action，ArgumentCaptor 证明 judge 收到 metaSchema；2 red 实测）
- AR-10：MySQL quarter → CONCAT+QUARTER*3-2 季度首日、week → DATE_SUB+WEEKDAY 周一午夜；**附带修复更深 bug**：translate 改 `tpl.replace("%s", columnExpr)`（旧 String.format 遇 DATE_FORMAT 的 %Y 格式符运行时必炸，MySQL 模板从未能执行）；TestGranularityBucketingTemplates 4/4（3 red 实测）；966/0 全绿

**Phase 3（导入事务/索引一致性，AR-08）**：
- 事务裁定：per-path REQUIRES_NEW（沿 NopMetaDataSourceBizModel 先例）+ 索引在事务单元内（失败反向 removeDocs）+ 事件随事务提交；无需 outbox
- importOrmModel 重构（save+persist×2+flush+indexImportedDocs+publishEvent 同一事务单元；失败 → DB 回滚 + 索引对账清理）；importOrmModels catch 语义修正（clearSession 仅会话卫生）
- 级联删除索引清理：Module.delete（collectModuleIndexedIds 三级收集）/ Entity.delete（collectEntityFieldIds）；Table.delete 核对保持不动
- TestNopMetaModuleImportConsistency 4/4（注入点沿 TestNopMetadataSearchIntegration 先例；**修复前 git checkout 旧代码实测 2/2 red**——幽灵索引 + 批量"报失败但已提交"）；docs「导入失败路径语义」段；970/0 全绿

**Closure**：
- 独立子 agent closure audit（fresh session `ses_02b95ac6affekrnpZrVP2KjuBh`）：**READY_TO_CLOSE**——三 Phase Exit Criteria 全 PASS（逐条 live code 证据 file:line + test name）、Closure Gates 全 PASS、Anti-Hollow 调用链逐链追踪 PASS（normalizeJoinQueryLimit 活路径 / indexImportedDocs 在事务 lambda 内 / 测试走 GraphQL/BizModel/executor 真实入口）、focused 74/74 全绿、无空壳/无静默跳过/无 live defect 降级；Minor 观察 3 条显式记录为非阻塞（反射测试私有方法 / 工作树未提交 / doc-links 既有 warnings）
- 工具验证：check-plan-checklist --strict exit 0（0 未勾选 + Closure Evidence 已写入）/ check-doc-links --strict exit 0 / scan-hollow --severity high exit 0
- Doc-sync: roadmap MR7 段 R7.3 → done（v26 header）+ arm-index MR7 R7.3 收口记录（含 AR-06 纠正 + 抽查结果）+ plan Closure 段 + 本日志；docs-for-ai/03-modules/nop-metadata.md（查询分页契约 + 导入失败路径语义两段）

### nop-metadata-audit-remediation：MR7 R7.3 执行（plan-2026-08-06-0553-3 Phase 3 完成：AR-08 导入事务/索引三态一致 + 级联删除索引清理）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-3-query-quality-import-correctness-batch.md`
- Action: EXECUTE Phase 3（red 先于修复判别性落地——修复前用 git checkout 还原旧代码实测 2/2 red，再恢复修复版复验 green）
- VERIFY（Phase 3 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **970 tests / 0 failures / 0 errors / 0 skipped**（966 + 新增 4：TestNopMetaModuleImportConsistency 新建 4/4）

**Phase 3（导入事务/索引一致性，AR-08）**：
- **事务边界裁定（Decision）**：per-path 独立事务（REQUIRES_NEW，沿 NopMetaDataSourceBizModel:512-517 模块先例）+ 索引写入在事务单元内（失败反向 removeDocs 对账）+ 事件发布在持久化+索引成功后（随内层事务提交）——成功三态一致提交 / 失败三态一致回滚；裁定：无需 outbox（不超出事务模板 + 反向清理能力面）
- **事务边界修复（Fix）**：`NopMetaModuleBizModel.importOrmModel` 重构为 `orm().getSessionFactory().txn().runInTransaction(null, REQUIRES_NEW, ...)`——DB 持久化（save + persistModelGraph ×2 + flush）→ 索引写入（新 `indexImportedDocs`：任一 addToIndex 失败 → 已写文档 removeDocs 反向清理后重抛）→ 事件发布，全部在内层事务单元内；`importOrmModels` 批量 catch 语义修正（clearSession 仅会话卫生，不再承担回滚职责——per-path 独立事务已保证失败路径 DB 回滚 + 索引清理）；单路径调用（GraphQL 直连）同样受益（旧实现失败时 DB 回滚但已写 Lucene 文档成幽灵）
- **级联删除索引清理（Fix）**：`NopMetaModuleBizModel.delete` 新增 `collectModuleIndexedIds`（ormModels → entities → entityFields 三级 + tables by metaModuleId），删除后 removeFromIndex 全部；`NopMetaEntityBizModel.delete` 新增 `collectEntityFieldIds`（删除前收集，删除后实体 + 字段一并 removeFromIndex）；`NopMetaTableBizModel.delete` 核对保持不动（:129 已自清索引）；模块/实体删除后搜索不再返回已删实体
- **判别性测试（Proof，red→green）**：`TestNopMetaModuleImportConsistency`（新建，注入点沿 TestNopMetadataSearchIntegration 先例：测试内构造 NopMetaSearchProcessor + mock ISearchEngine（嵌套子类暴露 searchEngine setter），替换容器 bean 的 protected searchService 字段（同包 entity 可访问），@AfterEach 还原防污染）4/4：
  1. `testImportIndexFailureRollsBackDbAndCleansIndex`——mock 第 3 次 addDoc 抛错 → GraphQL 导入失败 + DB 模块/实体零残留 + 恰好已写 2 文档被 removeDocs 反向清理（修复前无清理 → 幽灵 → red 实测）
  2. `testImportBatchPathFailureIsolation`——预跑统计单次导入 addDoc 次数（749）→ 批次 [app, app] 第 2 路径首个 addDoc 抛错 → path1 success + path2 fail + DB 仅 +1 模块（修复前外层事务提交 → +2 → red 实测）+ 索引 0 清理（path2 首写即抛）
  3. `testImportThenDeleteLeavesNoIndexResidue`——端到端：导入成功（749+ docs 索引）→ 模块级联删除 → removeDocs 覆盖全部已索引 doc id + DB 模块/实体零残留
  4. `testEntityDeleteCleansFieldIndex`——实体删除 → 实体 + 级联字段全部 removeFromIndex
- 既有导入测试零回归：TestNopMetaModuleBizModel 13/13、TestNopMetaTableBizModel 26/26
- 文档同步：`docs-for-ai/03-modules/nop-metadata.md` 新增「导入失败路径语义」段（per-path 独立事务、三态一致、级联删除索引清理）
- Doc-sync: 模块文档 + 本日志

### nop-metadata-audit-remediation：MR7 R7.3 执行（plan-2026-08-06-0553-3 Phase 2 完成：AR-06 SLA 虚假关闭实际修复 + AR-07 检查点 schema 回退 + AR-10 MySQL 粒度模板）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-3-query-quality-import-correctness-batch.md`
- Action: EXECUTE Phase 2（red 先于修复判别性落地）
- VERIFY（Phase 2 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **966 tests / 0 failures / 0 errors / 0 skipped**（956 + 新增 10：TestNopMetaDataContractBizModel +3、TestMetaQualityCheckpointExecutorSchemaResolution 新建 3/3、TestGranularityBucketingTemplates 新建 4/4）

**Phase 2（质量引擎契约修复，AR-06 + AR-07 + AR-10）**：
- **AR-06 修复（Fix + Decision）**：`MetaContractChecker.evaluateSla` 契约语义裁定——SLA 已配置（slaMap 非空）但 `catalogAvailable=false` → `slaFresh=false`（无数据即不满足任何已配置 SLA，无论 slaMap 内容，杜绝"仅含 retention 键 map 静默 pass"残余面）；仅当 slaMap 为空（无 SLA 配置）保持既有 pass 语义；`:404-407` 不可达死分支删除（两 key 恒存在故原分支不可达）；失败消息补可诊断原因文本 "no Catalog data"（避免 `SLA not satisfied ()` 空括号）；判别性测试 +3（SLA 配置无 Catalog → FAIL + slaFresh=false + catalogAvailable=false + 消息含 no Catalog data / retention-only map 无 Catalog → FAIL / 无 SLA 无 Catalog → 保持 PASS），修复前 2 个 FAIL red 实测
- **AR-06 治理纠正（Proof + Fix）**：git 逐行核验 commit 9b769490e 对 `MetaContractChecker.java` diff **仅删 7 行版权头**（脚本 real_diff_lines=0）——"MA7.6-05 fixed（R3.14）+ closure audit PASS"为虚假关闭；arm-index 新增「MR7 R7.3 收口记录」段纠正登记（R3.14 虚假关闭 → R7.3 实际修复）+ roadmap R3.14 行标注纠正；**R3.x claimed-fixed 抽查**：18 个声称 fixed 目标文件单 commit（9b769490e）diff 存在性核验——17/18 REAL_DIFF（R3.6/3.8/3.9/3.10/3.11/3.12/3.13/3.14-03/04/3.15/3.19 全部含非版权头实质 diff，spot-check R3.6 prop_get/R3.9 LOG.warn/R3.13 errorCount 小 diff 内容属实），唯一 COPYRIGHT_ONLY = R3.14 P2-MA7.6-05（即 AR-06）——无其他虚假关闭，无未处置项
- **AR-07 修复（Fix）**：`MetaQualityCheckpointExecutor.executeSingleRule` 补齐 `resolveDefaultSchema`（与 `NopMetaQualityRuleBizModel` 单规则路径同语义：schemaPattern null/空/纯空白 → 回退 `table.getMetaSchema()`）；`NopMetaQualityCheckpointBizModel.executeCheckpoint` + `MetaQualityCheckpointExecutor.execute` 两处 javadoc 过期表述（"null/空串表示依赖连接默认 schema"）同步修正；既有检查点测试 grep 核对无 details.schema 断言冲突；判别性测试 `TestMetaQualityCheckpointExecutorSchemaResolution`（新建，mock 基建沿 TestMetaQualityCheckpointSchedulerCronReadFailure 先例）3/3——schemaPattern=null → judge 收到 metaSchema 回退值 / 显式 PUBLIC 优先 / 纯空白回退，**接线验证**：mock tableRefExecutor 直接调用 action，捕获 judge 实参证明 CheckpointExecutor → tableRefExecutor → judge(schema) 调用链真实连通（修复前 null 传透 → 2 red 实测）
- **AR-10 修复（Fix）**：MySQL quarter 模板 → `CONCAT(YEAR(%s),'-',LPAD((QUARTER(%s)*3-2),2,'0'),'-01 00:00:00')`（QUARTER*3-2 ∈ {1,4,7,10} 季度首月，修复前月首与 month 逐字节相同）；week 模板 → `DATE_FORMAT(DATE_SUB(%s, INTERVAL WEEKDAY(%s) DAY), '%Y-%m-%d 00:00:00')`（ISO 周语义周一 00:00:00 + 午夜截断对齐 DATE_TRUNC('week')，弃用 %x-%v 周数字符串）；**附带发现并修复更深 bug**：`GranularityBucketing.translate` 用 `String.format(tpl, columnExpr)`——MySQL 模板内嵌 DATE_FORMAT 的 `%Y/%m/%d` 被 String.format 当作格式符（UnknownFormatConversionException），旧 MySQL 模板**运行时从未能执行**（H2-only 测试不可见）→ 改为文本 `tpl.replace("%s", columnExpr)`；判别性测试 `TestGranularityBucketingTemplates`（新建，沿 TestSqlPaginationOffsetOnly 字符串断言先例）4/4——quarter 季度首日 + 与 month 不同 / QUARTER 映射表达式 + 首日字面量 / week ISO 周一 + 午夜截断 + 无 %x/%v / H2/PG DATE_TRUNC 不回归（修复前 3 red 实测）；TestAggregationCategoricalAndTemporal 25/25 + TestAggregationEntityJoinAndComplex 16/16 零回归
- 文档同步：模块文档无 SLA 评估/粒度模板语义描述（grep 实证）→ `No owner-doc update required`
- Doc-sync: arm-index（MR7 R7.3 收口记录段）+ roadmap（R3.14 纠正标注）+ 本日志

### nop-metadata-audit-remediation：MR7 R7.3 执行（plan-2026-08-06-0553-3 Phase 1 完成：AR-01 P0 JOIN 分页双绑 + AR-09 limit 归一化）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-3-query-quality-import-correctness-batch.md`（AR-01/AR-06/AR-07/AR-08/AR-09/AR-10，执行顺序 {3} of 3）
- Action: EXECUTE Phase 1（red 先于修复判别性落地）
- VERIFY（Phase 1 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **956 tests / 0 failures / 0 errors / 0 skipped**（基线 949 + 新增 7：TestNopMetaJoinBizModel +2（含 GraphQL 端到端分页）、TestAggregationExternalJoinAndPagination +2（external↔external / mixed 分页）、TestMetaJoinTruncateOverflow +4（负 limit 显式错误码 ×2 + int 溢出 long 运算 ×2，去重后净 +3 方法——2 个测试类各新增 2 方法 + 修改既有 1 方法）

**Phase 1（JOIN/聚合分页参数修复，AR-01 P0 + AR-09）**：
- **失败用例（red 确认）**：① `TestNopMetaJoinBizModel.testSameDbSqlTableJoinReturnsRealRows` 改经真实入口传 `limit=2, offset=1`（2 行 seed 剩 1 行，:185 断言同步调整）——修复前 red（`join-table-exec-failed` / H2 `Invalid value "3" for parameter "parameterIndex"`，双绑实证）；② `TestAggregationExternalJoinAndPagination` +2（external↔external 与 mixed same-db 聚合路径 limit=2/offset=1，按分组名断言防隐式行序）——修复前 red（`aggr-exec-failed` 同型 SQLException）；③ `TestMetaJoinTruncateOverflow` +4——负 limit（`limit=-5`）当前抛裸 IllegalArgumentException、`limit=Integer.MAX_VALUE, offset>0`（seed 10 行）当前 int 溢出抛裸 IllegalArgumentException，修复前全部 red
- **双绑修复（Fix）**：`MetaJoinExecutor.executeSameDbTableJoin` / `ExternalExternalJoinAggregationProcessor` / `MixedSameDbJoinAggregationProcessor` 三处删除调用方 `params.add(limit/offset)`，LIMIT/OFFSET 占位符统一由 `executeJdbcQuery`（MetaJoinExecutor / AggregationHelper）内部绑定（对照先例 `MetaJoinExecutor:454-455 fetchTableRows` / `ExternalAggregationProcessor:83-85`）；**`MetaJoinExecutor.executeSameDbJoin:291-297`（entity-entity 路径）未改动**（其 params.add 是唯一绑定点）
- **limit 归一化裁定（Decision，四项全落地）**：(a) 新错误码 `ERR_PAGINATION_LIMIT_INVALID`（`nop.err.metadata.pagination-limit-invalid`，JoinErrors.java）——负 limit 显式拒绝；(b) queryTableData 静默钳制（normalizeQueryLimit）与 queryJoinData/queryAggregation 显式拒绝语义的差异**文档化**（docs-for-ai/03-modules/nop-metadata.md 新增「查询分页契约」段 + normalizeJoinQueryLimit javadoc）——不统一（浏览入口封顶安全 vs 分析入口静默改 limit 会漂移分页语义）；(c) 拒绝点固定在 BizModel 入口（queryJoinData/queryAggregation 参数归一化处）——三条 SQL 路径的 LIMIT 占位符不再直绑负值给 DB；truncate 层（truncateCrossDb / CrossDbJoinMerger.truncate）long 运算 + 负值拒绝作为 defense-in-depth；(d) `limit=null/0` 缺省 = DEFAULT_QUERY_LIMIT(1000)（有界，不提供"无界"选项）
- **SqlPagination javadoc（Fix）**：两种绑定模式（调用方绑定 / executeJdbcQuery 内部绑定）显式文档化 + 双绑警示，消除文档-代码漂移
- **端到端验证**：`testSameDbSqlTableJoinPaginationViaGraphQL` 新增——GraphQL 语义层调用 queryJoinData(limit=2, offset=1) 得到 1 行预期行集（从 API 参数到结果集完整路径）
- **判别性复核（green）**：修复后 focused 三测试类全绿（TestNopMetaJoinBizModel 10/10、TestAggregationExternalJoinAndPagination 26/26、TestMetaJoinTruncateOverflow 9/9）；全量 956/0 无回归
- Doc-sync: docs-for-ai/03-modules/nop-metadata.md（查询分页契约段）+ 本日志；`ai-dev/logs/` 已更新

### nop-metadata-audit-remediation：MR7 R7.2 执行（plan-2026-08-06-0553-2 全 2 Phase 完成 + closure，AR-04/AR-05 custom_sql 沙箱 DML/TCL + H2 文件族收口，plan completed + roadmap R7.2 done）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-2-custom-sql-sandbox-dml-h2-hardening.md`（AR-04 custom_sql 沙箱不拦 DML/TCL——规则 SQL 可在外部数据源账户上修改数据；AR-05 沙箱缺 H2 文件读写族——`SELECT FILE_READ('/etc/passwd')` 可读宿主机任意文件并回显；执行顺序 {2} of 3）
- Action: EXECUTE（Phase 1-2 全执行，red 先于修复判别性落地）+ CLOSE（独立子 agent closure audit + Closure Gates + 工具验证）
- VERIFY（closure 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **949 tests / 0 failures / 0 errors / 0 skipped**（基线 943 + 新增 6：testDmlStatementsBlocked / testCteWrappedDmlBlockedViaJudgeEntry / testTclAndRenameLockUnlockBlocked / testH2FileFamilyBlocked / testH2FileReadBlockedViaJudgeEntry / testSafeSqlWithoutBlacklistedWordsAllowed）+ `./mvnw compile -pl nop-metadata -am` 通过 + `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS + check-plan-checklist --strict exit 0 + check-doc-links --strict exit 0 + scan-hollow --severity high exit 0；全量 `-am` 两次偶遇 nop-stream 预存在 flaky（`TestRocksDBIncrementalRestoreAndBenchmark` 性能阈值 + `TestResultPartitionOverflowBypass` 线程时序断言），均单跑复绿（2/0、3/0），非本 plan 引入（仅触碰 nop-metadata 文件），按 R6.3 降级口径记录

**Phase 1（黑名单扩展 DML/TCL + H2 文件族 + 回归测试）**：
- **先写失败用例（red 确认）**：`TestMetaQualityRuleExecutorCustomSqlSandbox` +6 判别性测试——修复前 focused 运行 **19 tests / 5 FAILURE**（testDmlStatementsBlocked / testCteWrappedDmlBlockedViaJudgeEntry / testTclAndRenameLockUnlockBlocked / testH2FileFamilyBlocked / testH2FileReadBlockedViaJudgeEntry 全红），keep-green 反例（testSafeSqlWithoutBlacklistedWordsAllowed + 既有 14 用例）修复前已绿
- **正例（必须被拒）**：`DELETE FROM t`、`UPDATE t SET a=1`、`INSERT INTO t SELECT * FROM s`、`MERGE INTO t ...`、`REPLACE INTO t ...`（DML 族）；`COMMIT`/`ROLLBACK`/`SAVEPOINT sp1`/`SET TRANSACTION ...`/`START TRANSACTION`/`RENAME TABLE a TO b`/`LOCK TABLE ...`/`UNLOCK TABLES`（TCL 族 + RENAME/LOCK/UNLOCK，逐一唯一钉住条目）；`SELECT FILE_WRITE(...)`/`BACKUP TO ...`/`SELECT CSVWRITE(...)`/`LOAD XML INFILE ...`（H2 文件族 + LOAD XML 序列）；CTE-DML（`WITH x AS (DELETE FROM t) SELECT 1`）与 FILE_READ/CSVREAD 走 `judge()` 公开入口（沿 `testR61NewKeywordsBlockedViaJudgeEntry` 先例，sqlHash 参数提供 judge → judgeCustomSql → validateCustomSqlSandbox 接线证据）
- **反例（必须放行）**：`SELECT updated_at FROM t WHERE updated_at >= '2026-01-01'`、`SELECT COUNT(*) FROM ext_sql_t WHERE 1=0`、`SELECT id FROM ext_sql_t`——tokenizer 不剥离字符串字面量（javadoc :323-324 + `testUnionInsideStringLiteralBlockedFailClosed` 钉死语义），反例避开"关键字样字面量"形态
- **黑名单扩展（Fix）**：`CUSTOM_SQL_FORBIDDEN_WORDS` 补 18 词——DML 族 INSERT/UPDATE/DELETE/MERGE/REPLACE + TCL 族 COMMIT/ROLLBACK/SAVEPOINT/SET/TRANSACTION + RENAME/LOCK/UNLOCK（逐项对齐 `ExpressionMeasureValidator.KEYWORD_BLACKLIST:63-76`，INTO/OUTFILE/DUMPFILE 由既有 INTO+OUTFILE / INTO+DUMPFILE 序列覆盖不重复加单 token）+ H2 文件族 FILE_READ/FILE_WRITE/BACKUP/CSVWRITE/CSVREAD；`CUSTOM_SQL_FORBIDDEN_SEQUENCES` 补 `{"LOAD","XML"}`（H2 LOAD XML INFILE 文件读）
- **javadoc 修正（Fix）**：两处——(1) 黑名单 javadoc（:62-63 原"全覆盖"过度声明）改为逐项列出 DML/TCL 族 + RENAME/LOCK/UNLOCK + PG 文件/目录族 + 脚本导出族 + H2 文件读写族 + LOAD XML 序列说明；(2) `validateCustomSqlSandbox` javadoc（:326-328 原"如 MERGE/REPLACE 不在当前集合内"未来时表述）改写为已入集清单 + 与 ExpressionMeasureValidator 逐项对齐说明；保留 :323-324 字面量误拒 fail-closed 说明
- **判别性复核（green）**：修复后 focused 19/0 全绿；全量 `-pl nop-metadata -am -T 1C` 949/0（既有 `testUnionInsideStringLiteralBlockedFailClosed` 不回归——未动 tokenizer）
- `No owner-doc update required` 成立（沙箱行为收严属安全加固；`docs-for-ai/03-modules/nop-metadata.md` 质量规则章节仅列执行器 META-003 表格行，grep 实证无黑名单范围描述）

**Phase 2（全量回归 + 收口）**：
- `rg -n "FILE_READ|FILE_WRITE|CSVWRITE|CSVREAD|INSERT|UPDATE|DELETE|MERGE|REPLACE|COMMIT|ROLLBACK|SAVEPOINT|TRANSACTION|RENAME|LOCK|UNLOCK" .../main/java` 实证：5 个代表词（FILE_READ/CSVREAD/DELETE/MERGE/SET）在 `CUSTOM_SQL_FORBIDDEN_WORDS` 定义处（MetaQualityRuleExecutor.java:81-88）与 javadoc 两处（:63-69 黑名单 javadoc、:339-342 validateCustomSqlSandbox javadoc）三处一致；LOAD XML 序列在位（:95）
- 全量回归 949/0 全绿（quality 域既有测试 TestMetaQualityRuleExecutor*/checkpoint/scorer 零回归）

**Closure**：
- 独立子 agent closure audit（fresh session，独立 task_id）：**PASS**——核查 AR-04/AR-05 修复代码实证（18 词 + LOAD XML 序列在位）、正反用例判别性（red 5 例实测先红后绿）、CTE-DML/FILE_READ/CSVREAD 经 judge() 公开入口接线证据（sqlHash 参数）、javadoc 三处一致、无静默跳过（ERR_QUALITY_CUSTOM_SQL_BLOCKED + reason/sqlHash）、无静默降级、Anti-Hollow 调用链（judge → judgeCustomSql → validateCustomSqlSandbox → 拒绝在 querySingleValue 前，被拒 SQL 无法到达 executeQuery）、工具退出码全 0
- 工具验证：check-plan-checklist --strict exit 0 / check-doc-links --strict exit 0（0 errors）/ scan-hollow-implementations --severity high exit 0；checkstyle 惯例 N/A（根 pom checkstyle 插件仅存于 `-Pqa` profile，nop-metadata 无独立 checkstyle 命令）
- 提交：独立 commit（`fix(nop-metadata): ...` + `docs(...)` 收口提交）
- Doc-sync: roadmap MR7 段 R7.2 → done + plan closure 段 + 本日志；`No owner-doc update required` 成立

### nop-metadata-audit-remediation：VERIFY 复验（plan-2026-08-06-0553-1 closure 后 full-green 再确认 943/0）+ 审计/后续计划落库

- Mission: `nop-metadata-audit-remediation`
- Action: VERIFY（无未提交 code 变更——R7.1 已由上次 EXECUTE 提交；本次复验 + 收尾落库）
- 验证（mission config 命令）：
  - `./mvnw compile -pl nop-metadata -am -q` → exit 0
  - `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` → BUILD SUCCESS（22.6s）
  - `./mvnw checkstyle:check -pl nop-metadata -am` → 仅 `-am` 引入的 legacy 依赖 nop-api-core 9164 条 pre-existing violations（nop-metadata 0 条）；mission 命令定义 fallback 'lint not configured'，真门禁在 `-Pqa` profile（R6.1 惯例 checkstyle N/A）
  - `./mvnw test -pl nop-metadata -am -T 1C` → **943 tests / 0 failures / 0 errors / 0 skipped**（full-green verification）
- 落库：`ai-dev/audits/2026-08-05-2157-open-audit-nop-metadata-audit-remediation.md`（MR7 审计报告，AR-01~23） + `2026-08-06-0553-2`/`-3` 计划草案（R7.2/R7.3，roadmap 已登记 planned）——DRAFT_PLANS 轮产物此前未提交，本次一并 docs commit
- Commit: 见 docs commit（plan-2026-08-06-0553 审计与后续计划落库）
- Doc-sync: `ai-dev/logs/2026/08-06.md`（本条目）；无 owner-doc 变更（无 code 变更）

### nop-metadata-audit-remediation：MR7 R7.1 执行（plan-2026-08-06-0553-1 全 2 Phase 完成 + closure，AR-02/AR-03 SSRF 变体收口，plan completed + roadmap R7.1 done）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-1-ssrf-host-normalization-variants.md`（AR-02 无括号 IPv6 绕过 + AR-03 FQDN 尾点绕过，执行顺序 {1} of 3——安全面优先）
- Action: EXECUTE（Phase 1-2 全执行，red 先于修复判别性落地）+ CLOSE（独立子 agent closure audit + Closure Gates + 工具验证）
- 提交：独立 commit（`fix(nop-metadata): ...` + `docs(...)` 收口提交，本 plan 完成时一并提交；plan {2}/{3} 后续执行）
- VERIFY（closure 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **942 tests / 0 failures / 0 errors / 0 skipped**（基线 923 + 新增 19：TestHostSecurityUtil +5、TestMetaDataSourceConnectionSecurity +6、TestCheckpointActionDispatcherWebhookSsrf +5、新建 TestMetaDataSourceConnectionProcessorExtract 3）+ `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS + check-import-order 涉改 5 文件 0 错误（全仓 1475 为其它模块 pre-existing）+ check-plan-checklist --strict exit 0 + check-doc-links --strict exit 0 + scan-hollow --severity high exit 0

**Phase 1（HostSecurityUtil 归一化：尾点 + 无括号 IPv6 带端口）**：
- **先写失败用例（red 确认）**：TestHostSecurityUtil +5 判别性测试分三类——**red→green**（修复前运行实测 3 方法全红）：`localhost.`/`a.localhost.`（尾点 FQDN 前缀不命中）、`0.0.0.0.`（尾点数字形态前缀不命中）、`::1:3306`/`::ffff:127.0.0.1:3306`（JDK 26 `getByName("::1:3306")` 不抛异常解析为 0:0:0:0:0:0:1:3306 → 当前判外部放行）；**keep-green**（修复前实测已绿）：`127.0.0.1.`（127. 前缀已拦）、`fe80::1:3306`（JDK 26 解析 link-local 已判内网）；**反例**：`example.com`/`example.com.`/`2001:db8::1`/`[2001:db8::1]:3306`（方括号为调用方职责，util 契约边界守卫）保持放行
- **尾点归一化（Fix）**：`HostSecurityUtil.isInternalHost` 分派前统一剥离一个尾 `.`（仅一个不递归；`example.com..` 剥一个后按既有路径判外部，无静默放行）→ `localhost.`/`a.localhost.`/`127.0.0.1.`/`0.0.0.0.` 全部归一化后命中既有判定
- **无括号 IPv6 带端口主动判读（Fix）**：含 ≥2 冒号且尾部纯数字（端口候选）时，仅当头部解析为 IP 字面量（**16 字节 IPv6 或 4 字节 `::ffff:` mapped**——jshell 实测 `getByName("::ffff:127.0.0.1")` 返回 4 字节 Inet4Address，限定 16 字节会误判回退放行）才剥离端口**递归复用自身判内网逻辑**（单一事实源，mapped 头部由既有 `::ffff:` 分支判 127.0.0.1 内网）；否则整串判定（`::1` 无端口 → 头部 `:` 非字面量 → 整串走 IPv6 路径）；新增 `isIpLiteral` 辅助（DNS 安全：仅"≥2 冒号且字符集 ⊆ [0-9a-f:.]"的串才交给 getByName，hostname 头部前置排除）+ `isAllDigits`
- 判别性复核：21/21 全绿（red→green 全转绿、keep-green 保持、反例保持、既有 16 用例零回归）

**Phase 2（调用侧接线 extractHost / extractWebhookHost + 全量回归）**：
- **extractHost 修复**（`MetaDataSourceConnectionProcessor.java:293` 起）：无括号多冒号 hostPort 按 `lastIndexOf(':')` 分割，仅当尾部纯数字且头部 IP 字面量（同上规则，绝不触发 DNS）时取头部；否则**回退既有首冒号分割**（`127.0.0.1:3306`/`localhost:3306`/`example.com:3306` 单冒号语义维持，绝不把含端口整体串交给 HostSecurityUtil——整体串走 IPv6 判定解析失败放行会击穿基础防护）；`::1` 无端口原样返回
- **extractWebhookHost 同步修复**（`CheckpointActionDispatcher.java:303` 起）：同款 lastIndexOf 规则 + 首冒号回退；唯一判据为通用头部 IP 解析规则不按前缀特判（`FE80::1:3306` 大写同样命中 link-local）
- 测试：`TestMetaDataSourceConnectionSecurity` +5 拒绝用例（`jdbc:mysql://::1:3306/db`、`jdbc:postgresql://fe80::1:5432/db`、`jdbc:mysql://::1/db` 无端口、`jdbc:mysql://::ffff:127.0.0.1:3306/db`、`jdbc:mysql://localhost.:3306/db`，均经 testConnect 真实入口断言 ERR_DATASOURCE_JDBC_URL_BLOCKED + reason 含 host）；新建 `io.nop.metadata.service.connection.TestMetaDataSourceConnectionProcessorExtract`（3 用例，包级 seam `validateJdbcUrl` 直调不断真实连接）——反例 `[2001:db8::1]:3306` 放行 + allowlist 语义核对两测；`TestCheckpointActionDispatcherWebhookSsrf` +4（`http://::1:3306/`、`http://fe80::1:3306/`、`http://::ffff:127.0.0.1:3306/`、`http://localhost.:8080/` 全被拒且不达 IHttpClient.fetch）；既有 127.0.0.1/RFC1918/localhost 拒绝用例零回归
- **allowlist 语义核对结论**：allowed-hosts 精确匹配发生在 host 校验层归一化**之后**——`jdbc:mysql://::1:3306/db` 提取归一化出 `::1`，配置 `::1` 时放行、配置 `::1:3306` 整体串时不匹配仍拒绝（两测断言钉死）；与 R5.1 行为一致（对比点 extractHost 之后），docs-for-ai 无该配置语义描述 → No owner-doc update required 成立
- 验证：聚焦 4 类 84/0 全绿；全量 **942/0**（无 flaky，nop-stream rocksdb 未再触发）

**Closure**：
- **独立子 agent closure audit 两轮**（fresh session `ses_02be7f812ffeUYh62OrDWqR4Ss`）：第 1 轮 **NOT_READY**——发现 `isIpLiteral` 字符集守卫初版仅接受小写十六进制，`FE80::1:3306` 大写形态在提取层被判"非字面量"→ 回退首冒号截断为 `FE80` → 放行（JDBC/webhook 双侧 live 绕过，实测编译追踪，违反计划 :97 声明）→ 修复 `[0-9a-fA-F:.]`（两处）+ 双侧大写回归测试 3 处（webhook blocked / JDBC rejected / util keep-green）+ 聚焦 84/0 + 全量 942/0 复验 → 第 2 轮 **READY_TO_CLOSE**（8 项全 PASS：大写变体编译追踪拦截实证 + surefire 942/0 独立复核 + Minor 无端口 `2001:db8::1` URL 层过拒 = fail-closed 方向 + 计划范围外，认可非阻塞）
- roadmap 新增 **MR7 段**（R7.1 → done，plan 引用 + 942/0 + 大写变体收口 + 独立 closure audit PASS；R7.2/R7.3 登记 planned 待执行）+ header v24
- 独立子 agent closure audit（fresh session，独立 task_id）：PASS——8 项核查全 PASS（AR-02/AR-03 双侧拒绝代码实证 / 正反用例判别性实证 / util 归一化三机制在位 / 提取器回退语义不破坏单冒号 / allowlist 语义两测 / 无静默降级 / Anti-Hollow 调用链连通 extractHost→HostSecurityUtil / 工具退出码全 0）
- 工具验证：check-plan-checklist --strict exit 0 / check-doc-links --strict exit 0（0 errors）/ scan-hollow-implementations --severity high exit 0 / check-import-order 涉改文件 0 错误
- Doc-sync: roadmap MR7 段 + plan closure 段 + 本日志；`No owner-doc update required` 成立（内部校验语义归一化，docs-for-ai 无 host 校验规则细节章节，grep 实证）

### nop-metadata-audit-remediation：DRAFT_PLANS 轮——2026-08-05-2157 open-audit P0/P1 全部入计划（3 个 active plan），P2 批登记 backlog，审计置 planned

- Mission: `nop-metadata-audit-remediation`
- 输入：`ai-dev/audits/2026-08-05-2157-open-audit-nop-metadata-audit-remediation.md`（1 P0 + 9 P1 + 13 P2；0655 审计已在 08-05 处理为 planned，本轮确认无需重处理）
- Action: DRAFT_PLANS（1 P0 + 9 P1 → 3 个计划）+ 计划审查 3 轮（独立子 agent 对抗性审查 + 想象性分析，全部 consensus 达成）+ P2 批登记 + 审计状态更新
- 产物：
  - `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-1-ssrf-host-normalization-variants.md`（AR-02/AR-03：无括号 IPv6 + FQDN 尾点 SSRF 变体；审查 R1→R3：B1~B3/I-1/I-2/F-1 等 Blocker/Major 全部修复）
  - `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-2-custom-sql-sandbox-dml-h2-hardening.md`（AR-04/AR-05：custom_sql 沙箱 DML/TCL + H2 文件族；审查 R1→R2 consensus）
  - `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0553-3-query-quality-import-correctness-batch.md`（AR-01 P0 双绑 + AR-06 SLA 虚假关闭 + AR-07 schema 漂移 + AR-08 导入三态 + AR-09 limit 归一化 + AR-10 MySQL 粒度；审查 R1→R2 consensus）
  - 三个 plan 均 `Plan Status: active`（子 agent 审查 consensus 达成后提升；执行顺序 {1}→{2}→{3}）
  - roadmap `## Follow-up Backlog` 追加 AR-11~23（13 条，来源 `2026-08-05-2157`）；审计文件 `> Audit Status: open → planned`（Processed 注记）
- 审查要点记录：R1 发现 SSRF 计划"端口剥离异常回退"在 JDK 26 上永不触发（`getByName("::1:3306")` 解析为 0:0:0:0:0:0:1:3306）、`fe80::1:5432` 首字符非冒号等；R2 发现提取器"否则整个串"回退缺失会击穿基础防护；R3 发现 `::ffff:127.0.0.1:3306` 16 字节限定会放行 mapped 变体——均已修复并补测试用例
- 提交：未提交（mission 流程决定）
- VERIFY：本轮为计划拟制（无代码变更），未跑 mvn（基线 923/0 维持 08-06 R6.6 收口口径）

### nop-metadata-audit-remediation：MR6 R6.6 收口（plan-2026-08-06-0105-3 执行 + closure，plan completed，20 条终局归类全量终态 + roadmap R6.6 done + arm-index §P2 全量终态 + 独立 closure audit PASS）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0105-3-r6-6-backlog-final-classification.md`（20 条终局归类：P2-03 死码清理 + docs sweep + watch-only 登记，执行顺序 {3} of 3——R6.4/R6.5 先行，本 plan 最后执行）
- Action: EXECUTE（3 Phase 全执行）+ CLOSE（独立子 agent closure audit PASS + Closure Gates 全勾 + 工具验证）
- 提交：未提交（plan Non-Blocking Follow-ups：工作树提交由 mission 流程/用户决定）
- VERIFY（closure 后）：`./mvnw test -pl nop-metadata -am -T 1C` → **923 tests / 0 failures / 0 errors / 0 skipped**（R6.5 基线 925 减 2 个随删码删除的测试方法）+ `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` **exit 0** + `node ai-dev/tools/check-doc-links.mjs --strict` **exit 0 / 0 errors**（13→12 warnings 均为其他历史 plan 预存在）+ `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high` **exit 0**

**Phase 3（watch-only 终态登记 + 收口）**：
- arm-index §P2 20 行全量终态：6 行 done（P2-03/08/22/23/24/26 + AR-09 docs 面，均带 plan-2026-08-06-0105-3 引用）+ 13 行终态登记（P2-05/P2-14~21/P2-25/27/AR-06 + AR-09 DTO 面）+ AR-10 使命完成（P2-01/04 R6.5 fixed、P2-12 R6.2 fixed 已核实）+ MR6 裁决记录段补 R6.6 收口 blockquote + header 更新
- roadmap R6.6 → **done**（plan-2026-08-06-0105-3 引用 + 终态摘要 + 923/0 + 独立 closure audit PASS）+ header v23
- 独立子 agent closure audit（fresh session `ses_02c836b03ffeYjKDExQdDT7Y3x`）：**PASS**——20 项核查全 PASS（21 码 grep 0 残留 / 194 定义无空壳 / ERR_RECON 裁定 (b) javadoc 在位 / 测试同步实证 / design 文档 drift 修正实证 / docs sweep 6 项逐条对照 live / arm-index + roadmap 终态一致 / 工具退出码全 0 / deferred 分类诚实无 live defect 降级）；Minor 2 项已处置（roadmap/arm-index 收口表述先于 plan 勾选——closure 时全部兑现 + 工具复跑全 0；TestNopMetaTagLabelApproval 残留未用 NopMetadataException import——已移除，7/7 复跑绿）
- 验证：`./mvnw test -pl nop-metadata -am -T 1C` → **923/0**（R6.5 925 - 2 删除测试方法）；`node ai-dev/tools/check-plan-checklist.mjs --strict` 0（completed 后复核）；`node ai-dev/tools/check-doc-links.mjs --strict` 0 errors；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high` 0 发现
- Doc-sync: arm-index + roadmap + docs-for-ai/03-modules/nop-metadata.md + ai-dev/design/nop-metadata/（01-architecture-baseline + aggregation-processor-split）已更新；`No owner-doc update required` 仅适用于死码删除面（零行为影响），docs sweep 6 项按专用项落地

**Phase 2（docs sweep：P2-08/P2-22/P2-23/P2-24/P2-26/AR-09）**：
- P2-08：`docs-for-ai/03-modules/nop-metadata.md` §4 质量检查点段补 regex 规则方言例外——方言不支持 REGEXP → SKIP + LOG.warn + `details.reason="regexp-unsupported-dialect"`（对照 `MetaQualityRuleExecutor.judgeRegex:518-556` live 行为，SKIP 为可见结果非静默跳过）
- P2-22：`01-architecture-baseline.md` 5 处 orm.xml 行号锚点全部改为稳定引用——:501 → `NopMetaLineageEdge` 列定义、:713 → `NopMetaPipeline.schedule` 列、:1393/:1413/:1542 → `NopMetaTableMeasure.expression` 列（对照 live orm.xml；其余 7 篇 design 文档无行号锚点不在范围）；复核 `rg "orm\.xml:[0-9]+"` 0 残留
- P2-23：I*Biz 接口包路径表述修正——`nop-metadata-dao/.../biz/` → **`nop-metadata-dao` 模块 `io.nop.metadata.biz` 包**（live 核实 27 个 INopMeta*Biz 接口实际位于 `nop-metadata-dao/src/main/java/io/nop/metadata/biz/`；模块文档两处引用 + 参考文档段均修正）
- P2-24：模块文档补 items `List<Map<String,Object>>` 合理例外声明（queryTableData/queryAggregation/queryJoinData 行结构动态无法固定 DTO，经裁定例外）
- P2-26：模块文档依赖表补 test-scope 基建依赖说明（nop-metadata-codegen/nop-search-core/nop-job-local/nop-autotest-junit/junit/H2+MySQL 驱动/mockito——仅测试支撑不参与运行时装配）
- AR-09：模块文档新增「多 schema 支持（R4.2）」独立小节（插入位置 = 典型使用场景之后、API 契约之前）——metaSchema 可空语义（NULL=默认 schema；entity/SQL 表保持 null，external 表 upsertExternalTable 写入 + normalizeSchemaForMatch 归一化）、4 列 UK `UK_NOP_META_TABLE_MODULE_NAME (metaModuleId, tableName, isDelta, metaSchema)`（tenant 变体 xgen 派生禁手编）、upgrade SQL 三方言部署说明（`deploy/sql/{mysql,postgresql,oracle}/upgrade-nop-meta-table-uk.sql`，pre-R3.19 重复行先 dedupe 否则 fail-fast）

**Phase 1（P2-03：21 死码删除 + ERR_RECON 契约落地）**：
- **前置复核（Proof）**：R6.4/R6.5 落地后基线重跑 `rg -o 'ERR_[A-Z0-9_]+' ... -g '!**/*Errors.java'`：定义 215 → **194**（净删 21）、非定义处 use 405（含注释/日志文案）。21 码逐一核对：main 0 使用、定义恰 1 处、test 引用仅 4 码（ERR_MANIFEST_BUILD_FAILED/ERR_RECON_PARSE_PROPERTIES_FAILED/ERR_DTO_SERIALIZE_FAILED → TestNopMetadataErrorsCentralized；ERR_TAG_LABEL_NOT_FOUND → TestNopMetaTagLabelApproval），其余 17 码 test 0 引用。清单与 plan 21 码完全一致，无增删
- **ERR_RECON 裁定 = (b)**（推荐项）：删除 `ERR_RECON_PARSE_PROPERTIES_FAILED` 定义 + `LocalReconciliationProcessor.parseProperties` javadoc 文档化降级语义（per-row 数据损坏 warn 留证 + 空 Map 降级、不中断整批对账；理由：单行 properties 损坏不应使整批对账失败，R6.0 记录倾向 LOG.warn 留证非静默吞异常；实现从未抛该码，定义与行为不一致属契约漂移，删码后契约一致）
- 删码：AggregationErrors 4（JOIN_MIXED_ENDPOINT_DEFERRED/JOIN_MIXED_CROSS_DB_DEFERRED/JOIN_EXTERNAL_CROSS_QUERY_SPACE/JOIN_CROSS_QUERY_SPACE）+ MiscErrors 11（PROFILING_TABLE_NOT_EXTERNAL/NO_DATASOURCE/DATASOURCE_DISABLED/TABLE_FAILED/CONTRACT_NOT_FOUND/CONTRACT_INVALID_TRANSITION/TAG_LABEL_NOT_FOUND/PROPAGATE_DEPTH_EXCEEDED/PROFILING_RULE_NOT_FOUND/DTO_SERIALIZE_FAILED/SEARCH_INDEX_REBUILD_FAILED）+ ModuleErrors 1（MANIFEST_BUILD_FAILED）+ DataSourceErrors 1（QUERY_TABLE_NOT_FOUND）+ ReconErrors 2（RESULT_NOT_FOUND/PARSE_PROPERTIES_FAILED）+ SqlErrors 2（SQL_VIEW_MODULE_NOT_FOUND/SQL_VIEW_TABLE_NOT_FOUND）；空段注释（DTO/Profiling rule）随删码清理
- 测试同步：TestNopMetadataErrorsCentralized 删 3 断言（:46/:49/:51），:97-98 (ErrorCode) 构造器断言改为存活码 ERR_MODULE_NOT_FOUND（保留构造器覆盖）；TestNopMetaTagLabelApproval 删 ERR_TAG_LABEL_NOT_FOUND 断言（testErrorCodesDefined 保留 ERR_TAG_LABEL_INVALID_LABEL_TYPE）+ 删除 testNotFoundError 方法 + 移除未用 NopException import
- design 文档死码引用处置：`01-architecture-baseline.md:1137/:1244` 改为现行行为（不可同库/跨 querySpace → D10 内存 GROUP BY `CrossDbInMemoryAggregationProcessor`，精确-当-容纳/超限-显式失败；标注 R6-6 更新 + 旧码已删）；`aggregation-processor-split.md:194` 错误码列表删 ERR_AGGR_JOIN_MIXED_CROSS_DB_DEFERRED（delegate 描述同步）；`12-data-contract-and-governance-workflow.md:351` **显式豁免**（历史"已实现"记录性质，非现行行为表述，豁免理由：R2-F1 裁决，删除会破坏历史实现记录完整性）
- 删码复核（判别性）：21 码全仓 grep 0 命中（nop-metadata 口径；唯一残留为 parseProperties javadoc 中对已删码名的说明性提及，非代码引用）；test-compile 通过 + focused 全绿 + 全量 923/0

### nop-metadata-audit-remediation：MR6 R6.5 修复（plan-2026-08-06-0105-2 执行 + closure，plan completed，P2-01/02/04 → fixed + roadmap R6.5 done + 独立 closure audit PASS）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0105-2-r6-5-exception-chain-and-logging.md`（P2-01/02/04 日志类 finding，执行顺序 {2} of 3；R6.4 先行、R6.6 随后）
- Action: EXECUTE（4 Phase 全执行）+ CLOSE（独立子 agent closure audit PASS + Closure Gates 全勾 + 工具验证）
- 偏差：无（parseValidations 改签名加 checkpointId 参数为 plan 预授权裁定，执行时落定并记录；LOG.error 提到分支判断前使 fail-open/fail-closed 两路径统一留根因日志——fail-open 原已 LOG.error，无行为变化）
- 提交：未提交（plan Non-Blocking Follow-ups：工作树提交由 mission 流程/用户决定）
- VERIFY（mission 验证步骤）：`./mvnw compile -pl nop-metadata -am -q` 通过 + `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS + `./mvnw test -pl nop-metadata -am -T 1C` → **925 tests / 0 failures / 0 errors / 0 skipped（full-green verification，service 924 + web 1）**；checkstyle 项目未配置（root pom 插件注释，pre-existing violations 按 plan 口径以 mvn 构建默认检查为准）

**Phase 4（收口）**：
- arm-index §P2 P2-01/02/04 三行 → **fixed**（plan 引用 + 修复摘要 + 测试证据）+ R6.5 收口注 + header 最后更新 + AR-10 状态确认行更新（P2-01/04 已 fixed）
- roadmap R6.5 → **done**（plan-2026-08-06-0105-2 引用 + 925/0 + 独立 closure audit PASS）+ header v22
- 独立子 agent closure audit（fresh session `ses_02c9d65d4ffeyQY8uLuVIbfVNd`）：**PASS**——10 项核查全 PASS（三修复 7 个变更路径全部代码实证 + 测试断言实证；ListAppender 断言 + throwable 末参代码审查；deferred 3 项分类诚实无 live defect 降级；focused 测试复跑 49/0 全绿）；工具退出码全 0（check-plan-checklist --strict 0 / check-doc-links --strict 0 errors / scan-hollow --severity high 0 发现）
- 验证：`./mvnw test -pl nop-metadata/nop-metadata-service -am -T 1C` → **925 tests / 0 failures / 0 errors / 0 skipped**（R6.4 919 + 新增 6 判别性测试：TestNopMetaSearchProcessor 8→11 + TestMetadataPropagationUnit +1 + TestNopMetaQualityCheckpointBizModel 23→25 + TestMetaQualityScorer 新建 1 + TestMetaQualityCheckpointSchedulerCronReadFailure 新建 1）；全量 `-am` 偶遇预存在 nop-stream rocksdb 性能 flaky（`TestRocksDBIncrementalRestoreAndBenchmark.incrementalCheckpointIsFasterThanFullScanForLargeState` ratio=1.27 阈值抖动），**单跑复绿 2/2**，非本 plan 引入（仅触碰 nop-metadata 文件），按 R6.3 降级口径记录；`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；checkstyle 项目级 pre-existing violations（957）按 plan 口径以 mvn 构建默认检查为准
- Doc-sync: arm-index + roadmap 已更新（plan 涉及文档）；`No owner-doc update required` 成立（三修复为内部实现细节补日志/异常链，公开契约零变化，docs-for-ai 无对应细节章节）

**Phase 1（P2-01：NopMetaSearchProcessor fail-closed cause + 日志）**：
- `NopMetaSearchProcessor.addToIndex/removeFromIndex` fail-closed 分支改为 `new NopMetadataException(错误码, e)`（cause 保留）+ param 不变 + LOG.error（entityType/entityId + throwable）移到分支判断之前（fail-open/fail-closed 两条路径均留 ERROR 根因日志）；fail-open 语义保持（断言不回归）
- 测试：`TestNopMetaSearchProcessor` 扩 3 个判别性测试——`assertSame(实例, ex.getCause())` 恒等断言（doThrow 持实例变量先例 :75/:84/:97/:106）+ `getErrorCode()` 断言（ERR_SEARCH_INDEX_ADD/REMOVE_FAILED）+ ListAppender LOG.error 行断言（TestMetaTableProfilerSecurity 先例）；fail-open `assertDoesNotThrow` 既有测试保持
- 接线验证：7 个 BizModel（EntityField/Classification/Entity/GlossaryTerm/Module/Table/Tag）共 18 处 addToIndex/removeFromIndex 真实调用点，处理器为唯一入口
- 验证：TestNopMetaSearchProcessor 11/11 绿；`./mvnw test -pl nop-metadata -am -T 1C` 待 Phase 3 末统一确认（Phase 1 单独跑：919/0 于 service 模块）

**Phase 2（P2-02：AutoClassificationProcessor 正则编译失败日志）**：
- `AutoClassificationProcessor.suggestTags` Pattern.compile catch：补 `LOG.warn`（pattern + classificationId + ruleIndex + throwable 末参，沿同文件 :99-102 先例），continue 语义保持；防刷屏 = 方法级 `Set<String>`（key=classificationId|pattern）单次调用内去重
- 正路径测试从零搭建：`testAutoClassifyInvalidPatternSkippedValidRuleStillWorks`（TestMetadataPropagationUnit，双字段 + 非法规则 `[unclosed` + 合法规则 `user` → 断言 result==1/tagId=tag-b + ListAppender WARN 含 pattern/cls-1 + 非法 pattern WARN 计数==1）
- 验证：TestMetadataPropagationUnit 11/11 绿；全模块 920/0

**Phase 3（P2-04：4 处 quality catch 补日志）**：
- (1) `MetaQualityCheckpointExecutor.parseValidations` **改签名**加 checkpointId 参数（唯一调用点 :209 传 cp.getCheckpointId()）→ LOG.warn（validations is not valid JSON + checkpointId + throwable）；(2) `MetaQualityScorer.readExtConfigDimension` 补 slf4j import + LOG 静态字段 → LOG.warn（ruleId + throwable）；(3) `NopMetaQualityCheckpointBizModel.readAutoScoreConfig` → LOG.warn（checkpointId + throwable，默认开启语义保持）；(4) `MetaQualityCheckpointScheduler.readRegisteredCron` → LOG.warn（`read-registered-cron-failed` + checkpointId + throwable，沿 :322 先例）；4 处默认值语义均保持 + 注释同步
- 测试：`TestNopMetaQualityCheckpointBizModel` +2（corrupt validations → raw impl 精确断言 ERR_CHECKPOINT_NO_RULES + WARN；corrupt extConfig → autoScore=true + 评分落盘 + WARN）；新建 `TestMetaQualityScorer`（CUSTOM_SQL + corrupt extConfig → consistency=100 静态回退 + WARN，位置裁定 = quality 包）；新建 `TestMetaQualityCheckpointSchedulerCronReadFailure`（手工构造 + setter 注入 mock：addJob 抛 + getJobDetail 抛 → 不外抛 + WARN）
- 验证：4 测试类全绿（25/7/1/1）；`./mvnw test -pl nop-metadata -am -T 1C` 全量 925/0——唯一红为预存在 nop-stream rocksdb 性能 flaky（`TestRocksDBIncrementalRestoreAndBenchmark.incrementalCheckpointIsFasterThanFullScanForLargeState` ratio=1.27 阈值抖动），**单跑复绿 2/2**，非本 plan 引入，按 Closure Gates 降级口径记录（R6.3 先例同款）

### nop-metadata-audit-remediation：MR6 R6.4 修复（plan-2026-08-06-0105-1 执行 + closure，plan completed，P2-06/07/09 → fixed + roadmap R6.4 done + 独立 closure audit PASS）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-06-0105-1-r6-4-fail-loud-error-surfacing.md`（P2-06/07/09 fail-loud 修复，执行顺序 {1} of 3；R6.5/R6.6 随后）
- Action: EXECUTE（Phase 2/3 全执行；Phase 1 代码由前序运行落地、本次验证）+ CLOSE（独立子 agent closure audit PASS + Closure Gates 全勾 + 工具验证）
- 偏差：
  - P2-07 fixture 规格（执行期裁定调整，已记录 plan Phase 2 + fixture 注释）：原 duplicate-key 窄窗口实测不成立——delta 内部重复 key 在 merge 阶段 `ChildNodeMap.addByUniqueAttr`（ChildNodeMap.java:222）即抛 ERR_XDSL_MULTIPLE_NODE_HAS_SAME_UNIQUE_ATTR_VALUE，full load 永远先死（ScratchProbe 实测 delta-dup / dup-vs-base 均 FULL FAIL + FILTERED FAIL）。改为实测窄窗口 partial-override：base 含实体 + delta 部分覆盖缺 mandatory tableName → full merge 后合法（且 merge 后 `result.isValidated()=true` 跳过 XDslValidator，DslNodeLoader.loadFromNode:97-101）→ FULL OK；filtered 阶段 validator 直查 delta 原始节点 → ERR_XDSL_ATTR_VALUE_IS_EMPTY → FILTERED FAIL。前序运行的 ScratchProbe + probe-*.orm.xml 探针文件已清理（非交付物）
  - GraphQL 判别器：`getErrors()[0].getMessage()` 为 raw description 不含错误码，初版断言红一次后改 `GraphQLResponseBean.getErrorCode()`（top-level，addError 设置）
  - P2-09 测试环境缺口（已修复）：fail-loud 首次如实暴露既有正路径"假绿"——autotest 环境无操作人（svcCtx.getUserId()=null），`ApprovalFlowHelper.start` start-step allowCallByUser → `DaoUserDelegateService.requireUser(null)` 抛 unknown-entity；旧代码吞掉此异常且 approveStatus=SUBMITTED 先于 wf 启动设置，旧断言因此误判"提审成功"。两个 approval 测试类按仓库先例（AbstractWorkflowTestCase.newServiceContext）注入真实用户（ensureUser 建 NopAuthUser 行 + UserContextImpl via setUserContext + `newGraphQLContext(request, svcCtx)`），正路径现为真实提审成功（日志无 submitForApproval failed 警告）

**Phase 2（P2-07：parseDeltaModel fail-fast）**：
- 错误码裁定 = (a) 新建 `ModuleErrors.ERR_MODEL_DELTA_PARSE_FAILED`（`nop.err.metadata.module-delta-parse-failed`，param path/error）；复用 ERR_ORM_RESOURCE_READ_FAILED 语义偏"读失败"不精确
- `NopMetaModuleBizModel.parseDeltaModel`：catch Exception → 抛 `NopMetadataException(裁定码, e)` + param(path)，LOG.warn 留证于 throw 前；non-OrmModel 兜底显式抛错；javadoc 同步 fail-fast 语义；`!hasExtends` 的 return fullModel 路径保持
- 事务语义验证：GraphQL @BizMutation 事务包装覆盖整个 importOrmModel，`orm().save(module)`（:176）随失败回滚——判别性测试断言失败后 NopMetaModule/NopMetaOrmModel 均 total=0（无部分状态），无需前移 parse
- fixture：`delta-base-one-entity.orm.xml`（base 含实体 BaseE）+ `delta-partial-override.orm.xml`（x:extends 部分覆盖、缺 mandatory tableName）；测试 `testImportOrmModelMalformedDeltaFailsFast`（getErrorCode 判别 + `!contains("nop.err.xlang")` 排除 full-load 死）+ `testImportOrmModelsBatchMalformedDelta`（per-path success=false + 错误码子串、整批不中断、有效路径成功）
- 验证：TestNopMetaModuleBizModel 13/13 绿

**Phase 3（P2-09：提审失败 fail-loud）**：
- 错误码裁定 = 新建 `MiscErrors.ERR_TAG_LABEL_SUBMIT_APPROVAL_FAILED`（`nop.err.metadata.tag-label-submit-approval-failed`，param tagLabelId/error）；现有 ERR_TAG_LABEL_* 仅 INVALID_LABEL_TYPE，复用会稀释语义
- 行为裁定：save 为 CREATE 语义 + @BizMutation 事务回滚覆盖 super.save + saveOrUpdateEntity（失败路径断言按 tagLabelId 查询无行）；`getWfNameFromMeta()==null` 早退降级路径保持（R3.6 裁定）
- `NopMetaTagLabelBizModel.trySubmitForApproval`：catch Exception → 抛 `NopMetadataException(裁定码, e)` + param(tagLabelId)，LOG.warn 留证
- 判别性失败路径（确定性真实失败，零 mock）：data 预置 approveStatus=APPROVED → submitForApproval XPL（approval-support.xbiz:19-26）抛 invalid-status → save 抛裁定码 + 标签不落库；`testDerivedLabelApprovalFailureFailsLoud` 新增；正路径 `testDerivedLabelAutoSubmitsForApproval`（SUBMITTED）不回归
- 验证：TestNopMetaTagLabelApproval 8/8 + Integration 4/4 绿

**Phase 4（收口）**：
- arm-index §P2 P2-06/07/09 三行 → **fixed**（plan 引用 + 修复摘要 + 测试证据）+ MR6 裁决记录段 R6.4 收口注 + header 最后更新
- roadmap R6.4 → **done**（plan-2026-08-06-0105-1 引用 + 919/0）+ header v21
- 独立子 agent closure audit（fresh session `ses_02cc36f7cffeWa7u3Mkp19uNAj`）：**PASS**——21 项代码/测试/终态/anti-hollow 核查全 PASS（三 fail-loud 抛错路径在真实调用链上：importOrmModel:180→parseDeltaModel / save→triggerApprovalIfNeeded→trySubmitForApproval→submitForApproval XPL / MixedSameDbJoinAggregationProcessor.execute→checkEntityTableVisible→withConnection lambda→checkTableExists）；audit 发现 2 项文档缺陷（plan Phase 4 未勾选 + 日志缺条目）已由执行者补齐后复验；工具退出码全 0（check-plan-checklist --strict 0 / scan-hollow --severity high 0 发现 / check-doc-links --strict 0 errors）
- 验证：`./mvnw test -pl nop-metadata -am -T 1C` → BUILD SUCCESS **919 tests / 0 failures / 0 errors / 0 skipped**（service 916 + web 3；909 baseline + 新增 8 判别性测试；全量 `-am` 本跑无 rocksdb flaky，无需降级口径）
- Doc-sync: arm-index + roadmap 已更新（plan 涉及文档）；`No owner-doc update required` 成立（三修复为内部工具方法/内部实现细节，docs-for-ai 无对应细节章节；fail-loud 方向与 R6.0 裁决一致）
- 提交：未提交（plan Non-Blocking Follow-ups：工作树提交由 mission 流程/用户决定）

### nop-metadata-audit-remediation：MR6 R6.3 修复（plan-2026-08-05-2157-3 执行 + closure，plan completed，AR-07/AR-08 → fixed + roadmap R6.3 done + 独立 closure audit PASS）

- Mission: `nop-metadata-audit-remediation`
- Plan: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-05-2157-3-r6-3-null-schema-dup-guard-and-createsql-filter.md`（AR-07 + AR-08 修复，执行顺序 {3} of 3；R6.1/R6.2 先行同批邻项）
- Action: EXECUTE（3 Phase 全执行）+ CLOSE（独立子 agent closure audit PASS + Closure Gates 全勾 + 工具验证）
- 偏差：无（路径 C → C' 为执行期裁定调整，已记录于 plan Phase 1 Item 1 + 代码注释）

**Phase 1（AR-07：upsertExternalTable NULL-schema 并发双插静默重复行，裁定 = 路径 C'）**：
- 裁定（组合裁定 + 执行期调整）：路径 A 单独不充分（NULL 键 UK 不参与冲突）；路径 B（空串占位）不选（Oracle `''`≡NULL 陷阱 + 读路径 5 消费方 blast radius + 迁移面）；路径 C 原样（锁覆盖 find→insert→flush、不跨 commit）经实现验证竞态保留——每线程 mutation 整体事务 commit 在 BizModel 返回后（锁外），后到线程独立会话 READ_COMMITTED find 不可见先到未提交行仍双插 → **调整为路径 C'：per-key 锁 + 每表 REQUIRES_NEW 独立事务提交（锁跨 find→insert→flush→commit）**
- `NopMetaDataSourceBizModel`：新增 `EXTERNAL_TABLE_UPSERT_LOCKS`（ConcurrentHashMap，键 = metaModuleId+'\0'+tableName+'\0'+normalizedSchema）+ `upsertExternalTableGuarded`（synchronized(lock) 内 `txnTemplate.runInTransaction(null, REQUIRES_NEW, txn -> { upsertExternalTable(...); orm().flushSession(); })`）；`syncExternalTables` 循环改调 guarded 版本；并发失败方在锁释放后 find 到先到线程已提交行 → 收敛为 update（不报错、不追加、不静默跳过）；失败路径仍 catch → errors[] 收集（既有 per-table 隔离语义）
- 判别性测试（test-first）：新建 `TestNopMetaTableConcurrentNullSchemaUpsert`（`io.nop.metadata.service.entity` 包，同包访问 protected connectionService 换装 `NullSchemaMetadataProcessor`——JDK 动态代理仅把 getTables 结果集 TABLE_SCHEM 置 null 其余委托真实连接，使真实 sync 走 NULL-schema 分支；H2 恒报 PUBLIC 无法自然产出 null-schema 行，plan 允许"或直插路径"）；20 轮 × 2 线程 CountDownLatch 对齐、GraphQL 公开入口、预建 nop/meta-external 模块隔离 module UK 竞态、每轮独立物理库+独立表名（避免 INFORMATION_SCHEMA.CONSTANTS 行的乐观锁串行化污染——初版 19/20 轮被 CONSTANTS 行锁意外串行化，改 schemaPattern=PUBLIC 后 20/20 轮干净复现）
- **测试先行证据**：未修复代码 20/20 轮 errors=0 + 2 行（schemas=[null,null]）静默重复 → 修复后 20/20 轮 1 行 0 错误
- 验证：`TestNopMetaTableConcurrentNullSchemaUpsert` 1/0 + `TestNopMetaTableMultiSchemaUpsert` 2/0（既有两 schema 共存 + 单 schema update 不回归）

**Phase 2（AR-08：createSqlTable 守卫缺 isDelta/schema 过滤）**：
- `NopMetaTableBizModel.createSqlTable` 守卫补两 filter：`eq(isDelta, 0)` + `isNull(metaSchema)`（FilterBeans.isNull，SQL 语义无歧义），逐字对齐 4 列 UK (metaModuleId, tableName, isDelta=0, metaSchema=NULL)；明确**不做** null/空串归一（`''` 与 NULL 是 UK 中两个不同键，归一会误伤 `""` 外部行）
- `TestNopMetaTableBizModel` 23 → **26**：`testCreateSqlTableDeltaRowDoesNotBlockCreate`（isDelta=1 行不误报）/ `testCreateSqlTableOtherSchemaRowDoesNotBlockCreate`（异 schema 行不误报）/ `testCreateSqlTableTrueDuplicateStillFailsFast`（真重复仍 fail-fast）；Phase 1 走 C' → 路径 B 的 `""` 交互测试不适用（按 plan 衔接规则）
- 验证：三测试类 29/0 全绿

**Phase 3（收口）**：
- arm-index §P2 AR-07/AR-08 两行 → **fixed**（plan 引用 + 修复摘要 + 测试证据）+ MR6 裁决记录段 R6.3 收口注 + header 最后更新 2026-08-06
- roadmap R6.3 → **done**（plan-2026-08-05-2157-3 引用 + 909/0）+ header v20
- 独立子 agent closure audit（fresh session `ses_02d29d01bffeDAKGsxPLFBlkU5`）：**PASS**——代码级 Exit Criteria/Closure Gates 逐项 live 核实（upsertExternalTableGuarded 锁+REQUIRES_NEW 在位 :508-515、sync 循环接线 :193、守卫 4 filter :172-176、三新测试、surefire 26/2/1 全 0 failures、arm-index/roadmap 终态、无静默降级）；工具退出码全 0（check-plan-checklist --strict 0 / scan-hollow --severity high 0 发现 / check-doc-links --strict 0 errors）
- 验证：`./mvnw test -pl nop-metadata/nop-metadata-service -am -T 1C` → BUILD SUCCESS **909 tests / 0 failures / 0 errors / 0 skipped**（service 908 + web 1；全量 `-pl nop-metadata -am` 遇预存在 rocksdb 性能 flaky `TestRocksDBIncrementalRestoreAndBenchmark` ratio=1.0499 超阈，单跑复绿 2/0——非本 plan 引入）
- Doc-sync: arm-index + roadmap 已更新（plan 涉及文档）；`No owner-doc update required` 成立（路径 C' 不改 metaSchema 存储语义；docs-for-ai 无 createSqlTable 守卫细节章节）
- 提交：未提交（plan Non-Blocking Follow-ups：工作树提交由 mission 流程/用户决定）
