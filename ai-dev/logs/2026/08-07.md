### nop-ai Responses 迁移 3：agent 引擎切换到 ChatToolCallMessage + 85 个 mock-LLM 测试迁移（plan 327，全 Phase）

- Plan: `ai-dev/plans/327-nop-ai-responses-migration-3-agent-switch.md`
- Action: EXECUTE（Phase 1-4 全执行）；设计源 `ai-dev/design/nop-ai-responses-migration-design.md`（设计结论 #9、§3.5）
- 前置核对：326 已让 dialect 双轨产出 `response.messages`（含 `ChatToolCallMessage`），325 已就绪 `ChatToolCallMessage.fromChatToolCall`/`callId`。
- 做了什么（引擎从「读 `getMessage().getToolCalls()`」切换到「从 `getMessages()` 提取 `ChatToolCallMessage` 列表」）：
  - **Phase 1（测试 fixture helper）**：新增 `nop-ai-agent/test/.../support/ChatResponseFixtures.java`——`assistantWithToolCalls(text, ChatToolCall...)` / `assistantWithToolCalls(text, List<ChatToolCall>)` / `assistantText(text)` / `assistantWithReasoning(...)` / `foldedAssistantWithToolCalls(text, calls...)`（session-history 折叠形态工厂，避免测试直接 `setToolCalls`），双轨产出（同时填旧 `message.toolCalls` 与新 `messages` 里的 `ChatToolCallMessage`）。单测 `TestChatResponseFixtures` 5/0 验证双轨 callId 集合一致。
  - **Phase 2（ReActAgentExecutor 主路径切换）**：新增 `extractToolCalls(ChatResponse)` 从 `response.getMessages()` 收集 `ChatToolCallMessage` → 转 `List<ChatToolCall>`；新增 `extractAssistantMessage(ChatResponse)` 从 messages 取首个 `ChatAssistantMessage`（fallback `getMessage()`）；`:841` 工具提取循环改用 `responseToolCalls`；`:747/792` 迭代判定改为 `responseToolCalls.isEmpty()`；`buildToolCallSignatures` 改收 `List<ChatToolCall>`；`AgentToolDispatcher.executeAllowedCalls` 签名不变（验证 allowedCalls 来源正确）。ReActAgentExecutor grep `getToolCalls()/hasToolCalls()` 无残留。
  - **Phase 3（5 辅助生产引用点）**：`AgentPromptAssembly.checkOutputGuardrail` 签名 +`List<ChatToolCall> toolCalls` 参数（消除 `hasToolCalls()`）；`AgentSessionLifecycle.recomputeToolExecutionKey` +`ChatToolCallMessage` 主路径 +`getToolCalls()` 过渡 fallback；`Layer2TurnPruningStrategy.assertBoundaryIntegrity` +`ChatToolCallMessage` callId 收集 +legacy fallback；`CallAgentExecutor`/`AgentCallDelegate` `instanceof ChatAssistantMessage` 语义不改（找最后 assistant 文本，`ChatToolCallMessage` 非文本，无需调整）。生产 grep `.hasToolCalls()` 0 残留。
  - **Phase 4（85 个 mock-LLM 测试批量迁移）**：Python 脚本 `migrate_phase4.py` 自动迁移 65 文件 + 并行 agent 手工迁移 19 文件；response 构造 → `ChatResponseFixtures.assistantWithToolCalls(...)`；session-history 构造 → `ChatResponseFixtures.foldedAssistantWithToolCalls(...)`（保留折叠形态，匹配引擎 `ctx.addMessage(assistantMsg)` 实际输出）；修复脚本引入的 7 处 empty-if/else 分支 + 6 处占位文本被替换的 regression。最终 `rg setToolCalls` nop-ai-agent test dir 0 残留（excl. helper）。
- 不动（按 Non-Goals）：`ChatAssistantMessage.toolCalls` 字段不改（329）、`AgentToolDispatcher` 并发结构不改（设计 #9）、流式不改（328）、ResponsesDialect 不实现（330）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` **3404 tests, 0 failures, 0 errors**。`rg setToolCalls --glob '!ChatResponseFixtures.java' nop-ai-agent/src/test/` = 0。`rg '\.hasToolCalls\(\)' nop-ai-agent/src/main/` = 0。
- Doc-sync: `ai-dev/design/nop-ai-responses-migration-design.md` Phase 2 标 ✅；plan Plan Status→completed + 全 Phase items/Exit Criteria/Closure Gates 勾选。
- Commit: 待提交

---

### nop-ai Responses 迁移 2：ChatResponse.messages 列表 + dialect 双轨产出 + ResponseFormat/ContentPart 模型（plan 326，全 Phase）

- Plan: `ai-dev/plans/326-nop-ai-responses-migration-2-dialect-dual-track.md`
- Action: EXECUTE（Phase 1-2 全执行）；设计源 `ai-dev/design/nop-ai-responses-migration-design.md`（设计结论 #2/#4/#7）
- 前置核对：325 已于本日早些时候落地（`ChatReasoningMessage`/`ChatToolCallMessage`/`ApiStyle.responses` 均存在），故 Phase 2 依赖满足、可执行。
- 做了什么（双轨过渡是核心策略：旧 `message`/`getMessage()` 路径保留，新 `messages` 序列就绪供 327 切换）：
  - **Phase 1（nop-ai-api 模型）**：
    - `ChatResponse` +`messages`（`List<ChatMessage>`，`@JsonInclude(NON_EMPTY)`）+ `getMessages()`/`addMessage()`；`getMessage()` 标 `@Deprecated` 委托（messages 首个 `ChatAssistantMessage`，按类型筛选跳过前置 reasoning，无则退回旧 `message` 字段——保证 ReActAgentExecutor/MockChatService 既有调用点行为不变）；`success(List<ChatMessage>)` 工厂（同时播种旧 `message` 字段）；`copy()` 同步 messages。
    - 新增 `ResponseFormat`（type: json_object/json_schema + 可选 schema Map）；`ChatOptions.responseFormat` 字段类型 `String`→`ResponseFormat`；`getResponseFormat():String`/`setResponseFormat(String)` 标 `@Deprecated @JsonIgnore` 委托（type 透传，向后兼容 `WfAiHelper` 的 `"json"` 用法）；新增 `getResponseFormatConfig()`/`setResponseFormatConfig()` 对象访问；copy/merge/Builder 同步。
    - 新增 `ContentPart`（type: text/image/audio，text/detail/imageUrl/data + 工厂）；`ChatUserMessage` +`parts`（`List<ContentPart>`）+ `getParts()`；`getContent()` 委托为 parts 文本拼接（无 parts 时退回旧 content 字段，既有用例返回值不变）；copy 同步 parts。
  - **Phase 2（nop-ai-core 4 dialect parseResponse 双轨）**：OpenAi/Anthropic/Gemini/Ollama 在保留旧 `setMessage`（think/toolCalls 寄居）的同时，按语义顺序产出 `messages`（reasoning→assistant text→tool_calls）：thinking→`ChatReasoningMessage`、assistant 文本→同一 `message` 实例、tool_call→`ChatToolCallMessage.fromChatToolCall`。**修复 OllamaDialect 预存 bug**：`responseMap.get("message.tool_calls")` 字面 key 取不到嵌套结构（历史 tool_calls 永远解析不到），改为 `message`→`tool_calls` 路径取值（与 content 的 `message.content` 路径一致），使双轨真正生效。
  - 测试：`TestChatResponse`(9)/`TestChatOptionsAndUserMessageParts`(12) 新建于 nop-ai-api；4 dialect 测试新增 messages 断言（保留旧断言全绿证双轨）+ reasoning/tool_call 拆分类型断言；`TestChatServiceImplMessagesWiring`(1) 接线验证（Rule #23：ChatServiceImpl.call 非流式路径端到端拿到非空 messages）。
- 不动（按 Non-Goals）：`ChatAssistantMessage.think/toolCalls` 字段不改（329）、流式 StreamAggregator 不改（328）、agent 引擎不切到读 messages（327，双轨保证其继续读 getMessage().getToolCalls()）、ResponsesDialect 不实现（330）、`ILlmDialect.buildResponse` default 不改（328/329）。
- 偏差：① mission-driver 模板硬编码 `nop-stream` 为构建/测试模块，本 plan 目标是 `nop-ai`，按 plan 自身 Closure Gates 用 `nop-ai` 验证（与 08-06 plan 2249、08-07 plan 325 同一模板 artifact）。② owner-doc 裁定：实现严格遵循设计（messages 序列/ResponseFormat 对象/ContentPart 多模态/双轨），无投影语义细化——`No owner-doc update required`（design §五仅 Phase 0 标 ✅，Phase 1-4 无 ❌/✅ 标记可翻）。③ plan 无 `> Work Item:`/`> Source Audits:` 标签，roadmap ❌→✅ 与 source-audit 关闭步骤不适用。
- 验证：`./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-api -am` 关键测试绿（TestAnthropicDialect 14/0 含 tool_use callId Anti-Hollow、TestOllamaDialect 9/0 含 tool_call callId Anti-Hollow、TestOpenAiDialect 11/0 含 reasoning、TestGeminiDialect 7/0、TestChatServiceImplMessagesWiring 1/0 接线）；`./mvnw clean install -pl nop-ai -am -T 1C` **全 reactor BUILD SUCCESS**（含 nop-ai-agent 85 个 mock-LLM 测试——证 getMessage() 旧路径回归）；下游 `nop-wf/nop-wf-ai TestWfAiHelper` 4/0（证 getResponseFormat/setResponseFormat String 向后兼容）。`scan-hollow-implementations.mjs --module nop-ai-core` 退出码 0。
- Doc-sync: `ai-dev/design/nop-ai-responses-migration-design.md` 无需更新（No owner-doc update required）；plan Plan Status→completed + Phase 1/2 items/Exit Criteria/Closure Gates 全勾 + Closure 段填写（独立 closure-audit 留 mission-driver CLOSURE_VERIFY 阶段）。
- Commit: 待提交

---

### nop-ai Responses 迁移 1：消息类型体系扩展 + 序列化基础设施 landing（plan 325，全 Phase）

- Plan: `ai-dev/plans/325-nop-ai-responses-migration-1-message-types.md`
- Action: EXECUTE（Phase 1-2 全执行）；设计源 `ai-dev/design/nop-ai-responses-migration-design.md`（设计结论 #3/#6/#7/#11）
- 做了什么：把 nop-ai 消息模型从「角色折叠」朝「类型开放」推进一步，纯新增 + 单类删除 + 最小破坏：
  - 新增 `ChatToolCallMessage`（type `tool_call`：callId/name/arguments + `fromChatToolCall` 工厂，getContent 派生自 arguments text）。
  - 新增 `ChatReasoningMessage`（type `reasoning`：summary 必填 + detail 可选，getContent=summary）。
  - `ChatMessage` @JsonSubTypes：+tool_call/+reasoning，`ChatToolResponseMessage` key `tool`→`tool_output`，移除 `custom`。
  - `ChatToolResponseMessage.toolCallId` 改名 `callId`；`getToolCallId()`/`setToolCallId()` 标 `@Deprecated @JsonIgnore` 委托（保护 ~20 处既有调用点编译通过）。
  - 删 `ChatCustomMessage.java`（零代码引用，仅 @JsonSubTypes 注销）。
  - `ApiStyle` +`responses` 枚举值（仅枚举，`LlmDialectFactory` 未注册 dialect，330 才接）。
  - `AbstractLlmDialect.normalizeFinishReason` +`completed→stop`/`incomplete→length`（Responses API 终态原因归一）。
  - 测试：`TestChatMessageSerialization`（nop-ai-api，12 case，六型 round-trip + polymorphic list + custom 已注销回归）+ `TestNormalizeFinishReason`（nop-ai-core，8 case）。nop-ai-api pom 加 `jackson-databind:test` 依赖（沿用 nop-api-core 模式）以驱动 golden test。
- 不动（按 Non-Goals）：`ChatAssistantMessage.think/toolCalls/thinkSignature` 寄居字段保留（留 329）、4 个 dialect buildBody/convertMessage 不改（326）、agent 引擎不改（327）、`@JsonTypeInfo property` 仍为 `role`（329 切 type）。
- 偏差：① mission-driver 模板硬编码 `nop-stream` 为构建/测试模块，本 plan 目标是 `nop-ai`，按 plan 自身 Exit Criteria 用 `nop-ai` 验证（与 08-06 plan 2249 同一模板artifact）。② golden test 用 `new ObjectMapper().configure(FAIL_ON_UNKNOWN_PROPERTIES,false)` 模拟生产 JSON provider 配置（@JsonTypeInfo property `role` 在 strict 模式下被当未知字段，生产 provider 同样关闭该开关）。③ plan 原指向 `ai-dev/logs/2026/08-02.md` 追加日志，按 AGENTS.md「一天一个文件」改为新建 `08-07.md`。
- 验证：`./mvnw clean install -pl nop-ai/nop-ai-api -am -T 1C -DskipTests` SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` **全 reactor BUILD SUCCESS**（nop-ai-core/agent/gateway/dao/service/web/toolkit/coder/... 全绿）。新增测试实测执行非 skipped：`TestChatMessageSerialization` 12/0、`TestNormalizeFinishReason` 8/0；持久化 round-trip `TestFileBackedSessionStore` 29/0、`TestDBSessionStore` 20/0（`role` 字段名未变 + tool_output discriminator + callId 字段名变更无历史数据迁移负担）。`rg "ChatCustomMessage" --glob *.{java,xml}` 仅余测试中描述性注释字符串，零代码引用。
- Doc-sync: `ai-dev/design/nop-ai-responses-migration-design.md` Phase 0 标注 ✅ plan 325 落地；plan Plan Status→completed + Phase 1/2 items/Exit Criteria 全勾 + Closure 段填写（独立 closure-audit 留 mission-driver CLOSURE_VERIFY 阶段）。
- Commit: 待提交
