### DataInitInitializer 装配补丁（a3fb3f620）回退：IoC resolvedDepends 机制已保证装配顺序

- 背景：`a3fb3f620` 为 `DataInitInitializer` 加了 `ensureOrmTemplateSessionFactory()` 补丁（`init()` 时若 ormTemplate.sessionFactory==null 则手工补注入），绕过了 IoC 装配顺序问题。plan-337 的 `resolvedDepends` 单一前置依赖机制落地后，`ioc:after` 声明经 `verifyOrderConstraints`（位置校验）+ `BeanDefinition.newObject` 强制前置创建（`getBean(depend, false)` 阻塞等待完整创建含 init）保证：`DataInitInitializer` 的 `@PostConstruct` 执行前，`nopOrmSessionFactory`/`DataBaseSchemaInitializer` 已完整创建，且 prop 注入阶段 `nopOrmTemplate` 按类型 on-demand 创建时其 `sessionFactory` 已由 IoC 完整装配（生产者 mid-creation 不再静默返回部分实例）。补丁冗余，回退。

**变更**：
- `nop-orm/DataInitInitializer.java`：删除 `ensureOrmTemplateSessionFactory()` 及调用、`OrmTemplateImpl` import（纯回退 a3fb3f620）。
- `nop-orm/TestDataInitInitializer.java`：删除 patch 时代回归测试 `testInitWhenOrmTemplateSessionFactoryMissing`（其断言依赖补丁行为）；`testInitWithoutOrmTemplateThrowsNullPointerException` 保留并注释更新（仅记录非容器手工装配遗漏的失败行为）。
- 新增容器级测试 **`nop-orm-geo/TestDataInitInitializerContainer`**（+ `_test-init-data/test_index.csv`）：完整 IoC 容器（orm-defaults 装配）启动即创建 DataInitInitializer，`init()` 无 NPE 即证明补丁不再需要；断言 ormTemplate.sessionFactory 与容器 bean 一致（`assertSame`）+ CSV 数据已落库（`test_index` 表 idx-01）。

**为什么放 nop-orm-geo 而非 nop-orm**：容器级测试需要 nop-ioc test 依赖；若加进 nop-orm，`IocCoreInitializer`（ServiceLoader 注册）会让 nop-orm 所有 `CoreInitialization.initialize()` 的测试类连带启动完整 dao+orm 全局容器，需全量补充 datasource 配置（基类侵入）。nop-orm-geo 已依赖 nop-orm+nop-ioc+nop-config、有现成容器测试模式（TestMultiDataSource），零 pom 改动、不影响其他模块。

**验证**：`./mvnw test -pl nop-persistence/nop-orm -Dtest='TestDataInitInitializer'` 5 tests 绿；`./mvnw test -pl nop-persistence/nop-orm-geo` 3 tests 绿（含 TestMultiDataSource 回归）；nop-orm pom/test 基础设施零改动。

---

### plan-337 closure audit（独立子 agent）+ 收口

- **独立 closure audit（fresh session 子 agent，非实现 session 复用）**：VERDICT PASS。16/16 Closure Gates 核验：live-code 逐文件审查（BeanTopologySorter/BeanDefinition/BeanContainerImpl/IocErrors/BeanContainerDumper/beans.xdef/_gen/WfAiHelper/ChatOptions/TestWfAiHelper/TestBeanDepends + 7 个 beans.xml 资源）；全仓库 grep 残留扫描（`nextBeans`/`dependBeanIds`/旧错误码/ARG 常量 → 0 代码残留；`setResponseFormat(` 仅剩 legacy `AiChatOptions`，符合 329 Deferred 裁定）。
- **独立复跑命令全绿**：`test -pl nop-core-framework/nop-ioc` 53/0（TestBeanDepends 13/13）、`test -pl nop-orm-geo` 2/0（$DEFAULT$ 别名真实生产配置）、`test -pl nop-wf-ai` 4/0（json_object 断言）、`compile -pl nop-ioc -am` PASS（precompile 重跑 `_gen/_BeanValue.java` diff 稳定 4 行 javadoc）、`test -pl nop-biz` 27/0、`compile -pl nop-sys-dao,nop-dbtool` PASS、check-doc-links exit 0。
- **Gate 14（`test -pl nop-orm -am`）裁定**：审计 session 报 28 errors（`nop.err.ioc.empty-config-var`），其归因"nop-orm/pom.xml 未提交改动"经主会话核实**不存在**（git status/diff 均无）；主会话收口复测 **BUILD SUCCESS**（nop-orm 138 tests 0 failures，reactor 无失败）。工作树存在其他-session WIP（DataInitInitializer/TestDataInitInitializer 迁移、BaseTestCase 加 clearTestConfig、nop-orm-geo 未跟踪测试目录），不影响本 plan 收口——提交时须排除在 337 边界外。
- **审计发现并处理**：`test_bean_depends_missing_depends_on.beans.xml:4` 注释与断言矛盾（写"不报错"实为报 `ERR_IOC_UNKNOWN_DEPEND_REF`）——已修正注释。
- 收口后 `Plan Status: draft → completed`；check-doc-links 复跑预期 0 errors 0 warnings（completed plan 自动跳过 broken-link 检查）。

---

### plan-337 Phase 2/3 完成：顺序校验 + 错误码 + 注释同步 + 跨模块回归（含两个 pre-existing bug 修复）

- Plan: `ai-dev/plans/337-nop-ioc-before-after-depends-on-semantics.md`（Phase 2/3 → completed）
- 用户裁定：Phase 不单独关闭、不提交；全部 Phase 结束后统一 closure audit。

**Phase 2（顺序校验与错误码）**：
- `BeanTopologySorter` 新增 `verifyOrderConstraints`：排序后、`fillResolvedDepends` 前，对 `dependsOn`/`ioc:before`/`ioc:after` 三种声明校验——目标存在时 `pos(X) > pos(dependsOn/iocAfter)`、`pos(X) < pos(iocBefore)` 必须成立，否则抛新错误码 `ERR_IOC_BEAN_ORDER_CONSTRAINT_VIOLATED`（`IocErrors.java`，参数 beanName+depend）。无条件执行，不依赖 `allow-cycle`（默认 true）。
- 删除 `ERR_IOC_UNKNOWN_IOC_BEFORE`/`ERR_IOC_UNKNOWN_IOC_AFTER` 与 `ARG_BEFORE`/`ARG_AFTER`（确认全仓库无引用）。
- **存在性校验不对称（设计 §3.3/§3.5 明确）**：`depends-on` 是强声明，缺失在加载期报 `ERR_IOC_UNKNOWN_DEPEND_REF`（既有 `BeanDefinitionBuilder.checkDependRef`）；`ioc:before/after` 是弱声明，缺失静默跳过（目标可能被条件禁用/父容器/可选模块提供）。
- 测试 `TestBeanDepends` 7→13：正常顺序不误报（beanZ ioc:before="beanA" 字母序相反）/ before 环报错 / depends-on 环报错 / 缺失 before 目标忽略 / 缺失 depends-on 目标报 `ERR_IOC_UNKNOWN_DEPEND_REF` / default 别名回归（见下）。

**Phase 3（注释同步 + 跨模块回归）**：
- `beans.xdef` 更新 `ioc:before`/`ioc:after` 注释为"前置依赖"统一描述（移除"创建之后立刻创建"旧表述）；`./mvnw compile` 触发 precompile 重新生成 `_gen/_BeanValue.java`（注释同步，git diff 可观测）。
- `./mvnw clean install -T 1C -DskipTests` 全仓编译通过；nop-ioc 53 tests / nop-orm-geo 2 / nop-biz 27 全绿。
- `docs-for-ai/` 同步裁定：`No owner-doc update required`（ioc 语义变更已入 design doc，无 docs-for-ai owner doc 涉及）。

**过程中发现并修复的两个 pre-existing bug（均在 Phase 3 验证时暴露）**：
1. **`ioc:before` 指向 `ioc:default` 别名时边从未建立**（`BeanTopologySorter`）：`ioc:default=true` 的 bean id 自动加 `$DEFAULT$` 前缀（`IocConstants.DEFAULT_ID_PREFIX`），原始 id 作为 name/别名。旧代码 `sortBeans` 建边与 `fillResolvedDepends` 填充用原始字符串 `other.getIocBefore().contains(bean.getId())` 反向匹配，别名与归一化 id 不相等 → before 边从未建立 → `nop-orm-geo` 的 `H2GisInitializer ioc:before="nopOrmSessionFactory"` 实际顺序错位（字母序 `$DEFAULT$` 排在前，initializer 反而后初始化）。修复：两处反向匹配改为对声明值逐个 `normalizeBeanId` 后与 `bean.getId()` 比较。回归测试 `testBeforeAliasOfDefaultBean`（`test_bean_depends_default_alias.beans.xml`），Anti-Hollow 验证：回退 sortBeans 修复 → 测试 FAIL（顺序违例），恢复后绿。
2. **plan-329 closure 覆盖漏洞致 HEAD 编译破坏**（`nop-wf-ai`）：329 删除 `ChatOptions.setResponseFormat(String)` 委托时只验证了 `-pl nop-ai/*`，未覆盖下游消费者 `nop-wf/nop-wf-ai`（`WfAiHelper.java:77` 仍在调用）→ HEAD 全仓 `clean install` 编译失败，阻塞 plan-337 Phase 3。修复：`WfAiHelper` 改用 `setResponseFormatConfig(ResponseFormat.jsonObject())`（`json_object` 是 `"json"` 的规范取值），`TestWfAiHelper` 断言同步；并清理 `ChatOptions` 中已失效的"String 视图透传"注释。legacy `AiChatOptions`（`@Deprecated(forRemoval=true)`，自有 String 字段）按 329 Deferred 裁定不动。

**验证**：`./mvnw test -pl nop-core-framework/nop-ioc` 53 tests 0 failures（含 TestBeanDepends 13）；`./mvnw test -pl nop-persistence/nop-orm-geo` 2 tests 绿（真实生产配置回归）；`./mvnw test -pl nop-service-framework/nop-biz` 27 tests 绿；`./mvnw clean install -T 1C -DskipTests` 全仓 BUILD SUCCESS；`node ai-dev/tools/check-doc-links.mjs --strict` exit 0。

---

### plan-337 Phase 1 完成：ioc:before/ioc:after/depends-on 统一为 resolvedDepends 单一前置依赖机制

- Plan: `ai-dev/plans/337-nop-ioc-before-after-depends-on-semantics.md`（Phase 1 → completed）
- 目标：移除 `nextBeans`/`dependBeanIds` 双机制，引入 `resolvedDepends` 单一前置依赖；model 的 `dependsOn` 不再被 before/after fold 改写。

**落地（nop-ioc）**：
- `BeanDefinition`：新增 `resolvedDepends` 字段 + getter/setter；`newObject` 强制创建循环改读 `getResolvedDepends()`，且 `getBean(depend, false)`（includeCreating=false，经 synchronized 路径阻塞到依赖完整创建含 init 才返回）；删除 `nextBeans` 字段/getNextBeans/addNextBean、`dependBeanIds` 字段及 getter/setter。
- `BeanTopologySorter`：排序后填充 `resolvedDepends = 声明的 dependsOn ∪ iocAfter ∪ iocBefore 逆边 ∪ (ref 目标且 pos 在前)`；`setDependBeanIds` 逻辑被取代并删除；iocBefore 逆边在 sortBeans 中遍历全量 allBeans（覆盖跨 iocInitOrder 层声明）。
- `BeanDependsBuilder` 整体删除（不再 fold before/after 进 model.dependsOn）。
- `BeanContainerImpl`：构造函数 nextBeans 循环删除；`getBean0` 前向强制创建循环删除；`asyncStartBeans` 改读 `getResolvedDepends()`（TaskExecutionGraph 任务序，保留 TaskGraph——裁定：调度级依赖满足保证并发启动时依赖独立并行创建，取消/错误传播内置）；`getBean0` 中 `awaitGetBean(10000)` 等待分支删除（resolvedDepends 语义明确后为冗余兜底，未完成生产者直接抛 `ERR_IOC_PRODUCER_BEAN_NOT_INITED`）。
- `ProducedBeanInstance`：`awaitGetBean` 方法与 `latch` 字段删除（无调用者后清理死代码）。
- `BeanContainerDumper`：合并定义输出 `ext:resolved-depends`。

**测试（nop-ioc 47 例全绿）**：`TestBeanDepends` 7 例——after 归并强制创建（B.createdCount==1）/ nextBeans 前向联动移除（getBean(B) 后 A==0）/ before 归并（Z==1）/ ref 拓扑过滤（Y==1）/ depends-on 保持（D==1）/ async 任务序（B 先于 A）/ model 不被改写 + dump 含 `ext:resolved-depends`。async 用例经 `BeanContainerBuilder(null).concurrentStart(true)` 构建（`loadFromResource` 不读 concurrent-start 配置，`loadAppContainer` 才读）。
- `BaseTestCase` 新增 `clearTestConfig(String name)`（对称于 setTestConfig，`assignConfigValue(name, null)` 还原 ConfigProvider）。

**验证**：`./mvnw test -pl nop-core-framework/nop-ioc` 47 tests 0 failures；`nop-core` 相关测试（TestTenantResourceStore/TestMarkdownTestFile）4 tests 绿；残留 grep（nextBeans/dependBeanIds/awaitGetBean 调用点）无残留。

---
### nop-stream plan-2026-08-04-2300-3 收口（contract drift / _module / 空心测试完整性）

- Plan: `ai-dev/plans/nop-stream-production/2026-08-04-2300-3-contract-drift-config-test-integrity.md`（mission-driver EXECUTE + CLOSE）
- Action: EXECUTE Phase 3（bookkeeping 收口）+ CLOSE（独立 closure audit）
- 背景：Phase 1（SPI/doc drift 文档对齐代码）、Phase 2（`_module` IoC 发现标记 + `TestStreamModuleDiscovery`）、Phase 3 的代码工作均已在 commit `eeef4e13d` 落地，但 Phase 3 的 checkbox 未勾选、Status 仍 `planned`、Plan Status 仍 `active`、Closure 段空——bookkeeping 落后于实现。

**本次收口**：
- Phase 3 逐项核对 live code（非信任 checkbox）：
  - `TestTaskManagerDaemon`：重写版提供非 null `noopClusterRegistry()`/`noopMessageService()`，调 `tm.start()` + `tm.receiveAssignment(...)`，真实断言 `assertTrue(t.isDaemon(), ...)`；生产 `TaskManager.java:129/:134` 两处 `setDaemon(true)`，移除任一即 FAIL（regression power 成立）。`Tests run: 2, Failures: 0`。
  - `TestTaskExecutorDaemonThreads`：已删除（原版自定义 ThreadFactory 测 Java 恒真式，零生产耦合），commit message 记录原因。
  - `TestSinkTransformation`/`TestOneInputTransformation`：类级 `@Tag("low-value")` + Javadoc。`Tests run: 17/18, Failures: 0`。
- 独立 closure audit（fresh session `ses_023b54eb7ffehrrD9PTvILHCRH`，非实现 session 复用）VERDICT: PASS——逐项 PASS + Anti-Hollow PASS。
- Closure Evidence 写入 plan `Closure` 段；Phase 3 items/Exit Criteria/Closure Gates 全 `[x]`；Phase 3 + Plan Status → `completed`。
- Roadmap `nop-stream-independent-audit-roadmap.md` Work Item 3 `planned`→`done`。

**验证**：`./mvnw compile -pl nop-stream -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS（全模块绿）；`check-plan-checklist.mjs --strict` exit 0。
**scan-hollow**（`--module nop-stream --severity high`）exit 1，14 条 high findings 全在 `src/main` 且与本 plan 零重叠（git show eeef4e13d 实证）——均既有 rule-#24-compliant `UnsupportedOperationException` 守卫/demo 守卫/comment 误报，属 roadmap Follow-up Backlog P2 邻接项（本 plan Non-Goals），不阻塞 closure。

---



- Plan: `ai-dev/plans/330-nop-ai-responses-migration-6-responses-dialect.md`
- Action: EXECUTE（Phase 1 + Phase 2 + Phase 3）+ CLOSE
- 动机：交付 nop-ai Responses 迁移系列的最终目标——新增 `ResponsesDialect`（`ILlmDialect`），注册到 `LlmDialectFactory`（`ApiStyle.responses`），让 nop-ai 经 `dialect=responses` + `chatUrl=/v1/responses` 配置消费 OpenAI Responses 端点（`/v1/responses`）。

**落地**：
- `ResponsesDialect`（`nop-ai-core/.../dialect/ResponsesDialect.java`，新增）继承 `AbstractLlmDialect`，覆盖 buildBody/parseResponse/parseStreamChunk/convertMessage/getRole/getName/buildUrl/setHeaders/convertToolDefinitions：
  - 请求方向：ChatSystemMessage → 顶层 `instructions`；其余消息 → `input[]` typed items（user→message/input_text、assistant→message/output_text、reasoning→reasoning/summary_text、tool_call→function_call(call_id/name/arguments)、tool_output→function_call_output(call_id/output)）；`maxTokens→max_output_tokens`；`responseFormat→text.format`；`store:false`（无状态 design #10）。
  - 响应方向：`output[]` 按 `type` 分派（message→ChatAssistantMessage、reasoning→ChatReasoningMessage、function_call→ChatToolCallMessage）；`usage.input_tokens/output_tokens` → ChatUsage；顶层 `status` 经 normalizeFinishReason 归一。
  - 流式方向：Responses 具名事件（`response.created`/`output_item.added`/`output_text.delta`/`reasoning_summary_text.delta`/`function_call_arguments.delta`/`completed`）→ item 增量 chunk（text/reasoning/tool_call × ADDED/DELTA/DONE 三段式）。
  - 工具定义：override `convertToolDefinitions` 改为 Responses 扁平格式（type/name/description/parameters 平铺），剥离 hosted tools（web_search/file_search/code_interpreter）。
- `LlmDialectFactory` 静态注册块新增 `register(ApiStyle.responses, new ResponsesDialect())`；`ApiStyle.responses` 枚举 doc 更新为已注册。
- 测试：`TestResponsesDialect`（29 例，含纯文本/reasoning/function_call round-trip + 流式事件序列三段式）；`TestResponsesDialectIntegration`（5 例，端到端经 `ChatServiceImpl` 接线：非流式 + 流式 + 工具调用循环）；测试用 config `responses-test.llm.xml`（`apiStyle="responses"`, `chatUrl="/v1/responses"`）+ FakeHttpClient（含异步 SSE event publisher）。

**偏差**：
- 流式集成测试的 FakeHttpClient 需异步发射 SSE 事件（`CompletableFuture.runAsync` + 100ms 延迟），因 `SubmissionPublisher.close()` 在无订阅者时丢弃缓冲——同步发射会在 callStream 返回前完成所有事件处理，测试订阅者来不及附接。
- `parseRequestBody`（前端方向/gateway）沿用接口 default 抛 `UnsupportedOperationException`（Non-Goal，gateway 场景 deferred）。

**验证**：`./mvnw test -pl nop-ai/nop-ai-core -T 1C` = **212 tests, 0 failures, 0 errors, 3 skipped**（skipped 为 local LM Studio 测试）。`scan-hollow-implementations.mjs --module nop-ai/nop-ai-core` exit 0（0 findings）。端到端接线验证：TestResponsesDialectIntegration 断言 request URL 指向 `/v1/responses` + `store=false` + `input[]` 结构 + `function_call_output` 工具结果回传。
- Doc-sync: `ai-dev/design/nop-ai-responses-migration-design.md` 状态「草案」→「最终」；Phase 4 标记 ✅。nop-ai 无 LLM 接入层/dialect 配置的 docs-for-ai owner doc，`No owner-doc update required`。
- **系列收口**：325-330 全部 completed。迁移根目标达成——nop-ai 可消费 Responses 端点 + 消息模型为单一拆分形态。

---



### nop-stream-independent-audit mission 全绿验证（full-green verification）

- Mission: `nop-stream-independent-audit`
- 验证命令：`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests`（BUILD SUCCESS）+ `./mvnw test -pl nop-stream -am -T 1C`（**760 tests, 0 failures, 0 errors, 8 skipped**, BUILD SUCCESS）
- 修复：verify 阶段发现 `TestTaskManagerDaemon` 匿名 `IMessageSubscription` 缺 `isCancelled()`/`isSuspended()` 抽象方法导致编译失败，补齐两个 no-op 实现后全绿。

---

### DataInitInitializer NPE 复现测试（ormTemplate 装配顺序盲区补测）

- 背景：有人反馈缺省配置（开启 `nop.orm.init-database-data=true`）下 `DataInitInitializer` 的 `@PostConstruct` 抛 NPE，声称 ormTemplate 未初始化。根因分析（详见对话）：Nop IoC 中按类型 `@Inject`（`InjectTypeValueResolver.collectDepends` 为空）不参与 `BeanTopologySorter` 的排序依赖图，`orm-defaults.beans.xml` 的 `ioc:after` 又只列了 `nopOrmSessionFactory`+`DataBaseSchemaInitializer`，未列 `nopOrmTemplate`，因此 ormTemplate 及其 sessionFactory 的装配时机无硬约束。
- 测试盲区：原 `TestDataInitInitializer` 4 例均为手工 `new DataInitInitializer()` + set 全部依赖再调 `init()`，绕过 IoC 装配，无法暴露该问题。
- 新增（`TestDataInitInitializer` 共 6 例）：
  - `testInitWhenOrmTemplateSessionFactoryMissing`：模拟 `new OrmTemplateImpl()` 未注入 sessionFactory（对应 commit `a3fb3f620` 修复场景），`init()` 经 `ensureOrmTemplateSessionFactory` 补全后不抛异常且数据（新 CSV `_vfs/_test-init-data-npe/sims_college.csv`，行 `npe-col-01`）成功入库。
  - `testInitWithoutOrmTemplateThrowsNullPointerException`：ormTemplate 为 null 时直接调 `init()`，断言抛 `NullPointerException`——复现报告的注入缺失场景（文档化当前行为；若后续加 `Guard.notNull` 需同步改断言）。
- 判别性验证（red→green 实测）：临时禁用 `ensureOrmTemplateSessionFactory()` 后 `testInitWhenOrmTemplateSessionFactoryMissing` red——`NullPointerException: Cannot invoke "Object.hashCode()" because "key" is null`（即 `runInSession` 内 `OrmSessionRegistry.get(null)`，sessionFactory 为 null），与报告 NPE 同源；恢复补丁后 6 例全绿（`./mvnw test -pl nop-persistence/nop-orm -Dtest=TestDataInitInitializer` BUILD SUCCESS）。
- 结论：NPE 属实且与装配顺序有关；`ensureOrmTemplateSessionFactory` 治标。根治建议：`ioc:after` 增加 `nopOrmTemplate`（显式排序边）+ `init()` 内 `Guard.notNull` 早失败。待定未实施。

---

### nop-ai Responses 迁移 5：删除折叠过渡字段，收敛到单一拆分模型（plan 329）

- Plan: `ai-dev/plans/329-nop-ai-responses-migration-5-folded-fields-removal.md`
- Action: EXECUTE（Phase 1 + Phase 2 + Phase 3）+ CLOSE
- 动机：迁移收敛点——删除所有过渡 `@Deprecated` 字段，nop-ai 消息体系达到设计文档定义的最终单一拆分模型。

**Phase 1（ChatAssistantMessage 寄居字段删除 + dialect 双轨收敛）**：
- 落地：`ChatAssistantMessage` 删除 `think`/`thinkSignature`/`toolCalls` 及 `getFullContent`/`getFirstToolCall`/`hasToolCalls`/`setThink`/`setToolCalls`，仅剩 `content`。4 dialect（OpenAi/Anthropic/Gemini/Ollama）`parseResponse` 移除 `setThink`/`setToolCalls` 双轨填充，纯 `messages` 产出；`convertMessage` 移除 `getThink`/`getToolCalls` 读取，改为按 type 分派 `ChatReasoningMessage`/`ChatToolCallMessage`（`AbstractLlmDialect.getBaseRole` 扩展把二者归为 assistant 角色）。`ChatStreamAccumulator.toAssistantMessage`/`ChatServiceImpl.StreamAggregator`/`MockChatService` 移除寄居字段写入。
- 关键决策：引擎 `ReActAgentExecutor` 按设计 §3.2「上下文.append(response.messages)」将工具调用请求以独立 `ChatToolCallMessage` 追加到 ctx（寄居字段删除后的必然结果，保证多轮工具对话 tool_call_id 配对）；`Layer2TurnPruningStrategy.groupIntoTurns` 同步把 `ChatToolCallMessage` 归入所属 turn；`SmartModelRouter.classify` 仅按会话文本（user/assistant/system）评估复杂度，跳过 `ChatToolCallMessage` 派生 JSON 参数，避免工具调用元数据误判 structured/medium。

**Phase 2（ChatResponse.message 单字段删除 + 聚合访问器）**：
- 落地：`ChatResponse` 删除 `message` 字段与 `getMessage()`/`setMessage()`，新增 `outputText()`（拼接 `ChatAssistantMessage` 文本）/`outputToolCalls()`（收集 `ChatToolCallMessage`）聚合访问器；`getFullContent()` 改为委托 `outputText()`。消费方迁移：`ILlmDialect.buildResponse`、`SingleTurnExecutor`、`LlmCompletionJudge`、`LLMCurator`、`Layer3FullSummaryStrategy`、`MockChatService`、`FileSystemResponseProvider`/`InMemoryResponseProvider`；`ReActAgentExecutor.extractAssistantMessage` 移除 `getMessage()` fallback。
- 测试：`TestChatResponse` 重写为验证 `outputText`/`outputToolCalls`/`getFullContent`/`copy`/`success`；`TestStreamAggregator` 与 4 dialect 测试断言迁移到 `outputText()`/`outputToolCalls()`/messages。

**Phase 3（ChatRequest 便捷方法 + ChatMessage 序列化标识 + legacy 裁定）**：
- 落地：`ChatRequest.getToolCalls()/hasToolCalls()` 从 messages 提取 `ChatToolCallMessage`。`ChatMessage` @JsonTypeInfo property `role`→`type`；因 Nop `JsonTool` 按 getter 名写判别字段（不尊重 `@JsonProperty`）、Jackson 对 List 运行时类型不写 `@JsonTypeInfo` 判别字段，新增 `getType()`（返回 `getRole()`）恒定输出 `type` 字段、`getRole()` 标 `@JsonIgnore`，三者协同保证 Jackson（单对象+List）与 Nop（session store round-trip）均写 `type` 且按 `type` 分派。golden test（`TestChatMessageSerialization`）更新为 `type` 终态。
- legacy 裁定：`AiChatExchange`/`AiAssistantMessage`/`DefaultAiChatService`/`AbstractTextMessage`（`@Deprecated(forRemoval=true)` 独立 legacy pipeline，其 think/toolCalls 非本计划删除对象）裁定为 **deferred**（out-of-scope improvement，独立 successor）。
- `@Deprecated` 残留清理：删除 325/326 引入的 `ChatToolResponseMessage.getToolCallId/setToolCallId`、`ChatOptions.getResponseFormat/setResponseFormat(String)`/`Builder.responseFormat(String)` 委托，全部调用点迁移到 `getCallId()`/`getResponseFormatConfig()`。

**验证**：`./mvnw clean install -pl nop-ai/* -am -T 1C`（含 test）全模块全绿：nop-ai-api 46 / core 178 / agent 3404 / toolkit 129 / shell 277 / gateway 8 = **4042 tests, 0 failures, 0 errors**。`scan-hollow-implementations.mjs --module nop-ai` exit 0；端到端 pairing（`multiToolBatchMaintainsPairingEndToEnd`）成立。`check-plan-checklist.mjs --strict` exit 0。

---

### nop-stream 契约 drift / 配置约定 / 测试完整性收口（plan 2026-08-04-2300-3）

- Plan: `ai-dev/plans/nop-stream-production/2026-08-04-2300-3-contract-drift-config-test-integrity.md`
- Action: EXECUTE（Phase 1 + Phase 2 + Phase 3）+ CLOSE
- 动机：收口 nop-stream 文档/契约诚实性、平台约定符合度与测试完整性——四处 SPI/文档 drift、缺失 `_module` 平台 IoC 发现标记、三个空心测试。

**Phase 1（SPI/文档 drift 收敛，纯文档，4 处全部裁定 文档→代码）**：
- Drift 1 Operator State "尚未实现"：`state-management-design.md` §10.4/§11.9 + `core-design.md` §7.3/§7.4 + `comparison.md` 两行，更新为 live 状态（`IOperatorStateBackend` 4-mode redistribution + E2E 测试），移除 "Phase 0.3" 引用。
- Drift 2 IOperatorStateStore 1-vs-3：§10.1/§10.2 改为描述实际 SPI（`IOperatorStateStore` 仅 `getListState`；4 mode 由 backend restore 时外部注入，用户侧无注册选择）。
- Drift 3 KeyedStateStore 5-vs-2：§2.1/§5.1 改为反映实际 5 accessor（getState/getListState/getReducingState/getAggregatingState/getMapState）。
- Drift 4 StateDescriptor TypeSerializer：§6.1 改为文档化可选 escape hatch（默认 `JsonToolSerializer`，`IStreamSerializer` 显式 opt-in，`MemoryStateSerDe` 按 `instanceof` 分支）。
- 验证：`check-doc-links.mjs --strict` exit 0（0 errors）；纯文档无 mvn 需求。

**Phase 2（`_module` IoC 发现标记补齐）**：
- 落地：新增空文件 `_vfs/nop/stream/_module`（镜像 `nop-auth` 约定）。新增测试 `TestStreamModuleDiscovery`（`io.nop.stream.runtime.ioc`）：`moduleMarkerEnablesDiscovery` 断言 `ModuleManager.getEnabledModuleMap(false)` 含 `nop-stream`（`_module` 驱动 `discover()` 的 `findAll("*/*/_module")`）；`scopedContainerMaterializesDefaultBeansViaModuleMarker` 经 `findModuleResourcesInModules` 遍历发现的 module 的 `beans/` 目录（非硬编码 classpath 路径），加载 `stream-data-plane.beans.xml`，断言 `containsBean("streamMessageService")`/`containsBean("streamDataPlaneWireCodec")` 且为 `IMessageService`。初始化停在 `INITIALIZER_PRIORITY_IOC - 1` 避免 nop-dao datasource。
- 偏差：`stream-control-rpc.beans.xml` 与 `stream-data-plane.beans.xml` 均声明 `ioc:default` `streamMessageService`（控制/数据面共部署镜像），`BeansDefinition.addBean` 的 `ioc:default` dedup 当前被注释掉，故同 scoped 容器加载两文件会 `$DEFAULT$streamMessageService` 冲突；测试选取 data-plane 文件（声明两个目标 bean），仍经 `_module` 驱动发现而非硬编码路径。
- 验证：`TestStreamModuleDiscovery` 2 tests green；`./mvnw test -pl nop-stream -am -T 1C` 全模块 BUILD SUCCESS。

---



- Plan: `ai-dev/plans/nop-stream-production/2026-08-04-2300-2-checkpoint-state-backend-cep-correctness.md`
- Action: EXECUTE（Phase 1/2 前序已完成；本次执行 Phase 3 + Phase 4）+ CLOSE
- 动机：收口 checkpoint 与状态后端四项状态正确性缺陷中的剩余两项——JdbcCheckpointStorage 三处 store 方法的单事务 INSERT-then-UPDATE（PostgreSQL 事务 aborted）与 CEP SharedBufferAccessor.releaseNode null 分支栈 lockstep 失配（refcount 损坏/泄漏）。

**Phase 3（JdbcCheckpointStorage 方言感知 upsert）**：
- 落地：`JdbcCheckpointStorage.storeCheckPoint`/`storeSavepoint`/`storeEpochManifest` 三处原本 inline 的 `txn().runInTransaction(try{INSERT}catch{UPDATE})`（同事务，PostgreSQL 上 aborted）改为经 `resolveUpsertDialect()` 分派：GENERIC → `runInsertOrUpdateSeparateTxns`（INSERT 独立事务，dup-key 后 UPDATE 在新独立事务）；PG/MySQL/H2 → `buildNativeUpsert`（单语句：PG `ON CONFLICT...DO UPDATE SET col=EXCLUDED.col` / MySQL `ON DUPLICATE KEY UPDATE col=VALUES(col)` / H2 `MERGE INTO...KEY(...)`）。新增 `buildNativeUpsertSqlText`（package-private static，可单测）与 `buildNativeUpsert`（实例 helper）。此前已存在但**未接线**的 `UpsertDialect`/`resolveUpsertDialect`/`runNativeUpsert`/`runInsertOrUpdateSeparateTxns` helper 现被三处 store 方法实际调用（消除空壳）。
- 测试：`TestJdbcCheckpointStorage` 新增 `testUpsertSqlShapePerDialect`（确定性验证四方言 SQL 形态）、`testPostgreSqlUpsertVerifiedViaSqlShape`（PG ON CONFLICT+EXCLUDED 锚点）、`testDuplicateKeyUpsertUpdatesData`（H2 MERGE 原生 upsert 行为）、`testStoreLoadRoundTripOnDuplicateKey`（端到端 store→load）、`testGenericDialectFallbackSeparateTransactionUpsert`（oracle 方言→GENERIC 独立事务兜底）。23 测试全绿。
- 偏差：H2 2.3.232 不支持 PostgreSQL 原生 `ON CONFLICT...DO UPDATE SET...=EXCLUDED` 语法（已实证 syntax error），故 PostgreSQL 路径由 SQL-shape 单测确定性验证（运行日志已打印正确 ON CONFLICT SQL），行为执行由 H2 MERGE + GENERIC 独立事务兜底覆盖。真实 PG 集成测试（Testcontainers）列为 follow-up。

**Phase 4（CEP SharedBufferAccessor 栈 lockstep）**：
- 落地：`SharedBufferAccessor.releaseNode:274-279` null 分支（`curBufferNode == null`）在 `continue` 前补 `versionsToExamine.pop()`，恢复"每次迭代弹 1 node + 1 version"不变量。
- 测试：`TestSharedBufferExtended.testReleaseNodePopsVersionOnNullEntry`——构造重叠子图释放（X 双前驱边→live L + removed R），第二次 releaseNode 命中 null 分支；**回退 fix 后该测试 FAIL**（P 泄漏 refCounter=1），证实非空壳。`TestNFAExtended.testFollowedByAnyBranchingWithSkipPastLastEvent`——followedByAny + SKIP_PAST_LAST_EVENT branching E2E，无 over-release 崩溃，SharedBuffer bounded。

- 验证：`./mvnw test -pl nop-stream-runtime,nop-stream-cep -am -T 1C` → 758 tests, 0 failures, 0 errors, 8 skipped（多 JVM gated）；`./mvnw clean install -pl nop-stream-runtime,nop-stream-cep -am -T 1C -DskipTests` → BUILD SUCCESS；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-stream --severity high` exit 0。
- Doc-sync: `ai-dev/design/nop-stream/checkpoint-design.md` §9.3.2 新增 JdbcCheckpointStorage upsert 方言矩阵（PG/MySQL/H2/GENERIC）+ 不扩展 IUpsertHandler SPI 裁定。CEP 修复 `No owner-doc update required`（纯内部 NFA 遍历）。
- Roadmap: `ai-dev/backlog/nop-stream-independent-audit-roadmap.md` item 2 `planned`→`done`。
- Follow-up: 独立 closure-audit（fresh subagent）建议复核 PG 行为验证降级裁定；真实 PG Testcontainers 集成测试（不阻塞 closure）。

---



- Plan: `ai-dev/plans/328-nop-ai-responses-migration-4-stream-items.md`
- Action: EXECUTE（Phase 1 模型 + Phase 2 四 dialect + Phase 3 aggregator，三 Phase 编译耦合同批落地）+ CLOSE
- 动机：把流式模型从「折叠 delta（content/thinking/toolCall 三字段粘在一个 chunk）」重构为「item 增量（itemType/itemIndex/callId?/delta/phase:ADDED|DELTA|DONE）」，删死代码 `ChatToolCallChunk`，补齐 OpenAI 流式 tool_calls 解析缺口，使流式 chunk 与非流式 326 `response.messages` 同构，为 330 Responses 具名事件流铺路。

- 落地：
  - `nop-ai-api/.../chat/stream/`：新增枚举 `StreamItemType`(text|reasoning|tool_call)/`StreamItemPhase`(ADDED|DELTA|DONE)；`ChatStreamChunk` 改字段为 item 增量模型（删 role/content/thinking/toolCall/index）；删除 `ChatToolCallChunk.java`；`ChatStreamAccumulator` 改为按 itemType 累加。
  - `nop-ai-core/.../dialect/`：4 dialect `parseStreamChunk` 全产出 item 增量；`OpenAiDialect` 新增 `parseToolCallDelta`（首见 id/name→ADDED、后续 args→DELTA）补 tool_calls 缺口；`AnthropicDialect` content_block_start/delta 按 block type 映射；`ILlmDialect.buildStreamChunk` default 基于 itemType/phase 重写。
  - `nop-ai-core/.../service/ChatServiceImpl.java`：`StreamAggregator`/`ToolCallAccumulator` 改为 item 状态机，产出 `response.messages`（reasoning → assistant text → tool_calls，与 326 同构），旧 `message` 双轨保留。
  - `nop-ai-core/.../mock/MockChatService.java`：chunk 产出改为 item 增量。
  - 测试：新增 `TestChatStreamChunkSerialization`（6，三段式 + 多 tool_call round-trip）、`TestStreamAggregator` 重写加端到端 tool_call/reasoning/multi-tool 场景（5）；4 dialect 流式断言改 itemType/delta/callId + OpenAI tool_calls 2 项新测试。

- 偏差：plan Phase 3 端到端验证原指向 `TestChatServiceImpl`（@Disabled 需本地 LM Studio），实际落到 `TestStreamAggregator` 跑 SSE data→parseStreamChunk→StreamAggregator→messages 全链路断言（ChatToolCallMessage + arguments 拼接 + callId），语义等价且可离线复现。
- 验证：`./mvnw test -pl nop-ai/nop-ai-api,nop-ai/nop-ai-core,nop-ai/nop-ai-gateway -am -T 1C` → BUILD SUCCESS（0 failure/error）；`./mvnw install -pl nop-ai/nop-ai-agent,nop-wf/nop-wf-ai -am -DskipTests` → BUILD SUCCESS（下游无残留旧 API）；`scan-hollow-implementations.mjs --module nop-ai-core --severity high` exit 0；`check-plan-checklist.mjs --strict` exit 0。
- Doc-sync: No owner-doc update required（`ai-dev/design/nop-ai-responses-migration-design.md` §3.3 已记录 item 增量模型最终状态，实现与之逐字一致）。
- Follow-up: 329 删 `ChatAssistantMessage.think/toolCalls` 寄居字段 + `ChatStreamAccumulator` 旧双轨访问点（Deferred watch-only residual）；330 ResponsesDialect 流式具名事件。

---



- Plan: `ai-dev/plans/nop-stream-independent-audit/2026-08-06-2250-1-evidence-schema-source-manifest-finding-corpus.md`
- Action: EXECUTE（3 Phase 全执行——Phase 1 source manifest、Phase 2 finding corpus、Phase 3 evidence schema + 校验器）+ CLOSE（独立子 agent closure audit + Closure Gates + 工具验证）
- 动机：在 Stages 5-23 任何 capability claim 之前冻结有界的 source manifest、冻结的 finding corpus、证据行 schema 与拒绝缺字段/未知字段/分母不匹配的校验器。纯数据/工具计划，不改 nop-stream 生产代码。

**Phase 1（source manifest 冻结）**：
- 落地：`ai-dev/audits/nop-stream-independent-audit/source-manifest.md` — 7 个声明域 12 条 `@@ENTRY`：(a) Java 公共类型（prod 527 / example 10 / @Internal 121）、(b) XDSL stream.xdef tag 92、(c) Delta *.stream.xml 12、(d) beans 2 文件 + ioc:default 5、(e) connector 16、(f) example stream.xml 1、(g) test lane（448 java / 13 fixture / 4 multi-jvm）
- 每条 `command:` 为可在 HEAD 独立复现的活选择命令（rg/find 经 `wc -l` 出单整数）；校验器 shell-execute 并比对 `expected_denominator`
- 显式 include/exclude 文本：generated `_`-前缀排除、`@Internal` 计入（非排除）、example 模块单独计量、`target/` 排除
- live 10-模块布局作事实基准记录（docs-for-ai drift 由独立 owner-doc plan 收敛，No owner-doc update required）

**Phase 2（finding corpus 冻结与分片）**：
- 落地：`ai-dev/audits/nop-stream-independent-audit/finding-corpus.md` — 4 份审计报告 + 3 份 active remediation plan 全量登记，**全 corpus 97 finding** 跨 5 分片：18=42 / 19=16 / 20=15 / 21=19 / 22=5
- ID 唯一（M8-2-*/O8-2-AR-*/M7-2-*/O7-2-AR-*）；08-02 multi severity P0×2/P1×13/P2×23 与报告一致；intra-pair 去重（open 仅贡献 AR-N 新项）；3 处 recurrence 保留双 ID（StateDescriptor/TestCountTrigger/TestProcessingGuarantee）
- 3 份 active plan 的 deferred-P2 归并并标注 owner plan（Plan1: P2-9/10/15；Plan2: P2-1/5/6/7+O8-2-AR-3；Plan3: P2-11/12/13/14/16/17/23）共 15 处 `deferred:` 标注
- 跨分片归属规则文件顶部显式声明；无静默丢弃（0 pending-adjudication）

**Phase 3（evidence schema + 校验器）**：
- 落地：`ai-dev/audits/nop-stream-independent-audit/evidence-schema.md` — 11 必填字段（inventory_id/disposition 等）+ 7 值分类词表（e2e-proved/component-only/unverified/fail-fast/non-goal/residual-risk/blocked）+ lane 语义
- 落地：`ai-dev/tools/check-nop-stream-audit-manifest.mjs`（487 行）— 3 子命令 `manifest|corpus|evidence` + `self-test` 阳性对照 + `--strict`
- 校验器实测执行 manifest 命令并比对整数；corpus 校验 ID 唯一/分片合计/词表；evidence 校验字段完整 + 词表 + lane 一致性（e2e-proved 要求 env≥required）

**Closure**：
- 验证：`node --check` exit 0；`node …mjs all` exit 0（manifest/corpus/evidence 全 PASS）；`node …mjs self-test` exit 0（阳性对照——3 checker 均拒绝已知坏输入：分母错配/缺字段/未知字段/词表外值/ID 重复/合计不一致/lane 不足）
- 工具：`check-plan-checklist --strict` exit 0（41 项全 [x]）；`check-doc-links --strict` exit 0（3 新文件无 broken link；plan 1 处 prose 路径简写 warning，非阻塞、非本次引入）
- 独立子 agent closure audit（fresh session `ses_0263155c0ffeSeAeMS423MR8li`，read-only，独立重跑全部命令/重算全部分母与分片）→ **PASS**：12 条分母独立重算全匹配（527/10/121/92/12/2/5/16/1/448/13/4）；corpus 42/16/15/19/5=97 ID 唯一；08-02 P0×2/P1×13/P2×23 确认；15 deferred 标注在位；11 字段 + 7 词表完整；源码审视确认校验器非空壳；无 placeholder
- 纯数据/工具计划，不改 nop-stream 生产代码 → `./mvnw` 不强制（Closure Gates 明示豁免）；closure 以校验器退出码为证据
- Deferred：无 in-scope 延期项；docs-for-ai module-groups.md drift 由独立 owner-doc plan 收敛（Non-blocking）

---

### xview `<embed>` 页面类型 + cell view override delta 合并（plan 331 执行 + closure，plan completed，4 Phase 全执行 + 独立 closure audit READY_TO_CLOSE）

- Plan: `ai-dev/plans/331-xview-embed-page-type-and-view-override.md`
- Action: EXECUTE（4 Phase 全执行——Phase 1 schema/codegen、Phase 2 flux embed 渲染、Phase 3 两端 GenDispView 接线、Phase 4 测试+文档）+ CLOSE（独立子 agent closure audit + Closure Gates + 工具验证）
- 动机：xview.xdef `<pages>` 此前无引用型页面类型（无法嵌入外部页面并做 delta 定制）；disp.xdef `<view>`（UiRefViewModel）已声明 override 概念但**未消费**；`WebPageHelper.applyViewOverride` helper 已落地（commit 132b60979）但**无调用方**。本 plan 接通整链。

**Phase 1（schema + codegen）**：
- 裁定：`<override>xjson</override>`（与 `<data>xjson</data>` 一致；前会话 `xdef:unknown-attr="any"` 写法破坏 codegen 已回退，xjson 内容经 `XNode.toXJson()` 即任意结构，无需 unknown-attr）
- 落地：`disp.xdef` `<view>` 增 `<override>xjson</override>`（带 delta 合并语义注释）；`./mvnw install -pl nop-frontend-support/nop-ui -am -DskipTests` BUILD SUCCESS，`_gen/_UiRefViewModel.java` 再生成出 `_override`/`getOverride()`（:75/:256-264）；xview.xdef `<embed>` schema 已在 commit 132b60979 落地（容器 + 页面双形态）

**Phase 2（flux embed 渲染）**：
- `impl_GenPage.xpl:40-42`：complex 分支后新增 `<when test="${pageModel.type == 'embed'}"><c:include src="page_embed.xpl"/></when>`
- 新建 `flux-web/page_embed.xpl`：`!path`→`nop.err.web.embed-page-path-required`；`.view.xml`+page→GenPage；`.view.xml`+grid→GenTable；`.view.xml` 无 page/grid→`nop.err.web.embed-page-ref-required`（显式失败，不静默 noop）；otherwise→`internalLoadPage`（page.yaml→flux.yaml 回退经 toFluxPagePath+ResourceHelper.resolve().exists()）；末尾 `applyViewOverride(base, pageModel.override)` 经 `<_ xpl:attrs="result"/>` 输出
- 关键机制核对：`xpl:return` 经 `XplCompiler.java:229-231` 编译为 `varDecl`（仅赋值，非即时返回），故 `<c:collect xpl:return="base">` 捕获后可继续 applyViewOverride；`<_ xpl:attrs>` 是 xjson 上下文 emit 计算 map 的既定模式（container_tabs LoadPage 先例）

**Phase 3（两端 GenDispView override 接线）**：
- `flux-web.xlib` + `web.xlib` GenDispView 各 3 处 return 改为 `applyViewOverride(base, refView.override)`（page 分支、grid 分支、internalLoadPage 分支）；refView.override 为 null 时原样返回 base，既有输出不变
- **修复 applyViewOverride 只读 bug**：override 来自已冻结组件模型（只读 JObject），`JsonMerger.mergeMap:73` 就地 `remove` x:virtual/x:inherit 触发 `nop.err.core.json.map-is-readonly`（helper 此前无调用方，bug 未暴露）。helper 内先 `CloneHelper.deepClone(override)` 转可写副本（`JObject.deepClone()` 返回非冻结 JObject）

**Phase 4（测试 + 文档）**：
- 夹具：`test-flux-embed.{view.xml,page.yaml,xmeta}` + `test-flux-embed-yaml.page.yaml` + `test-flux-embed-bad.page.yaml` + `test-flux-embed-target.page.yaml`（embed view.xml+page / page.yaml / 无引用 throw）；`test-flux-cell-override.{view.xml,page.yaml,xmeta}`（form cell custom=true 触发 GenInputTable 分支 + view+override）
- `TestFluxWebGen` +4（18→22）：`testEmbedPageLoadsExternalViewAndMergesOverride`（crud 外壳 + override 新增 title + 覆盖 asideClassName）/ `testEmbedPageLoadsPageYamlDirectly`（page.yaml override title + body 保留）/ `testCellViewOverrideMerges`（array-editor + items 单元格 title 合并）/ `testEmbedPageWithoutRefThrows`（`nop.err.web.embed-page-ref-required`）
- 文档：`docs-for-ai/02-core-guides/flux-rendering.md` 新增「embed 页面」+「`<override>` delta 合并语义」两段 + 相关文件补 page_embed.xpl/WebPageHelper；`docs-for-ai/04-reference/source-anchors.md` EXT-009 扩写（embed + applyViewOverride + page_embed + WebPageHelper 锚点）；`ai-dev/design/xview-embed-page-type-design.md` 新建（6 节设计文档）

**Closure**：
- 验证：`./mvnw clean install -pl nop-kernel/nop-xdefs -am -DskipTests` exit 0；`./mvnw test -pl nop-frontend-support/nop-web -am` **75 tests / 0 failures**（基线 71 + 本批 +4）；`./mvnw test -pl nop-kernel/nop-xlang` **456 tests / 0 failures**（disp.xdef 变更无回归）
- 工具：`check-doc-links --strict` exit 0（修 design doc 1 处 partial-path error）；`scan-hollow --module nop-frontend-support/nop-web --severity high` exit 0（0 findings）；`check-plan-checklist --strict` exit 0
- 独立子 agent closure audit（fresh session `ses_0264606abffezKOVRopP1A9kqf`，read-only）→ **READY_TO_CLOSE**：7 区逐条 PASS（schema/embed 渲染/两端 GenDispView 接线/applyViewOverride 只读修复/测试夹具/文档/Anti-hollow 7 调用点实证）；无 discrepancy
- Deferred：AMIS/web.xlib 页面级 embed 接线（与 complex 先例一致，cell 级 override 已两端齐平，Successor Required = no）；容器槽位内嵌 embed 渲染（schema 已允许，flux 分派属 follow-up）
- checkstyle：nop-web/nop-ui 未配 checkstyle 构建门禁（pom 插件注释禁用）；WebPageHelper.java import 分组核对一致
- 产出：plan Plan Status → completed，Closure 段证据已写入；本日志条目

---



- Plan: `ai-dev/plans/328-security-hardening-remediation-planning.md`
- Action: EXECUTE（Phase 1-2 全执行）；纯文档/规划 plan，无代码变更、无 build/test gate。
- 做了什么（把已确认的安全加固 findings 冻结成 durable decision records，并据此创建 4 个 successor draft plans）：
  - **Phase 1（durable baseline）**：扩写 `ai-dev/analysis/2026-08/2026-08-04-security-hardening-baseline.md` —— Finding Ownership 表补全逐行 source anchor + 校验事实；Reclassified Deployment Constraints 统一为单一 Low 类（M-3 SSO/DB 密码从 Medium 修正为 Low，附统一 rationale）；新增 DR-1a JWT issuer/consumer 矩阵（access/refresh/authz-code/SSO 四类 token 的 iss/aud/typ/key/consumer/validation 规则，含 public-contract migration 裁定）、DR-1b `servicePublic` 决策表（7 维：默认值/匿名操作类型/`@Auth` 交互/SYS scope/data-auth/tenant source/tenant-header policy）、DR-1c browser 矩阵（redirect 形式/origin 规范化/proxy TLS/HTTP opt-out/cookie 属性）、DR-1d 密码策略基线、DR-1e admin-skip 默认一致性、DR-2a AES 加密值格式决策需求（7 项：version marker/per-msg IV/concurrency/legacy detect/KDF/fail-closed/compat scope）、DR-2b AES 兼容边界（global vs scoped）、DR-3a Bash backend admission/isolation policy（含 fail-closed 硬要求 + real-backend proof）、DR-3b AI tool runtime facts（sandbox backends=none、workDir 非约束、validator↔transport seam）、DR-4a SSRF enforcement 决策（8 项：enforcement layer/redirect ownership/multi-addr+DNS-rebinding/IPv4-v6 encoded/proxy/connection pinning/fail-closed/public-API migration gate）。Open Questions 全部映射到对应 DR owner。Status→closed（decision records frozen）。
  - **Phase 2（successor drafts）**：创建 `333-auth-browser-boundary.md`（含 nop-auth ask-first gate + blocked 实现切片）、`334-encrypted-value-format.md`（legacy/new/tampered/truncated/concurrent 测试要求 + entry-to-sink ORM/config 接线验证）、`335-ai-bash-isolation.md`（real backend isolation proof + fail-closed）、`336-ai-http-ssrf.md`（resolver-to-transport 接线 proof + 每个 redirect hop 校验）。四个均 `Plan Status: draft`，等用户对对应 DR 给 approved/deferred 裁定后再 promote 到 active。每个 plan 经独立子 agent 想象性分析审查，无 Blocker。
  - **disposition 记录**：全部六类 decision record 裁定为 `deferred-pending-user-authorization`（理由：nop-auth 是 AGENTS.md ask-first 区、AES 改持久化密文语义、Bash/SSRF 改运行时契约——均需用户显式授权才能进 active 实施）。
  - **独立审计（Plan 328 closure audit）**：fresh 子 agent `ses_0270e6b0affeCDMNe4UVyr7CCR` 做想象性分析 + source-anchor 核验。结论：所有 source anchor 准确、finding 覆盖无遗漏无重复、四个 successor 无 Blocker、Phase 2 Exit Criteria 全部被对应 successor 满足。发现并修复两个 Major：① Plan 328 Phase-2/Closure 状态与已存在的 drafts 不一致（已同步：tick 全部 items + Phase 2/Plan Status→completed + 重写 Closure）；② DR-4a/DR-3b 漏记 `nop-http-api` 已有的 `IDnsResolver` 公共 seam（`HttpClientConfig.java:66`、`IDnsResolver.java:38-49`），使"IHttpClient 公共 API 迁移"决策最可能答案为 no——已在 analysis DR-4a/DR-3b 与 Plan 336 Public-API Migration Consideration 补记该 seam，供用户在授权门前评估。
- 偏差：mission-driver 模板硬编码 `nop-stream` 为构建/测试模块，本 plan 是纯文档 plan 无代码，按 plan 自身 Closure Gates 跳过 build/test，仅跑 `check-doc-links.mjs --strict` 与 `check-plan-checklist.mjs` 作为 doc gate。plan 无 `> Work Item:`/`> Source Audits:` 标签，roadmap ❌→✅ 与 source-audit 关闭步骤不适用。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0；`node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/328-...md --strict` 退出码 0（All plans passed）。
- Doc-sync: `ai-dev/analysis/2026-08/2026-08-04-security-hardening-baseline.md` 已更新（即本 plan 的 decision evidence）；`docs-for-ai/` 无需更新（无运行时行为变更）。
- Commit: 待提交

---



- Plan: `ai-dev/plans/327-nop-ai-responses-migration-3-agent-switch.md`
- Action: EXECUTE（Phase 1-4 全执行）；设计源 `ai-dev/design/nop-ai-responses-migration-design.md`（设计结论 #9、§3.5）
- 前置核对：326 已让 dialect 双轨产出 `response.messages`（含 `ChatToolCallMessage`），325 已就绪 `ChatToolCallMessage.fromChatToolCall`/`callId`。
- 做了什么（引擎从「读 `getMessage().getToolCalls()`」切换到「从 `getMessages()` 提取 `ChatToolCallMessage` 列表」）：
  - **Phase 1（测试 fixture helper）**：新增 `nop-ai-agent/test/.../support/ChatResponseFixtures.java`——`assistantWithToolCalls(text, ChatToolCall...)` / `assistantWithToolCalls(text, List<ChatToolCall>)` / `assistantText(text)` / `assistantWithReasoning(...)` / `foldedAssistantWithToolCalls(text, calls...)`（session-history 折叠形态工厂，避免测试直接 `setToolCalls`），双轨产出（同时填旧 `message.toolCalls` 与新 `messages` 里的 `ChatToolCallMessage`）。单测 `TestChatResponseFixtures` 5/0 验证双轨 callId 集合一致。
  - **Phase 2（ReActAgentExecutor 主路径切换）**：新增 `extractToolCalls(ChatResponse)` 从 `response.getMessages()` 收集 `ChatToolCallMessage` → 转 `List<ChatToolCall>`；新增 `extractAssistantMessage(ChatResponse)` 从 messages 取首个 `ChatAssistantMessage`（fallback `getMessage()`）；`:841` 工具提取循环改用 `responseToolCalls`；`:747/792` 迭代判定改为 `responseToolCalls.isEmpty()`；`buildToolCallSignatures` 改收 `List<ChatToolCall>`；`AgentToolDispatcher.executeAllowedCalls` 签名不变（验证 allowedCalls 来源正确）。ReActAgentExecutor grep `getToolCalls()/hasToolCalls()` 无残留。
  - **Phase 3（5 辅助生产引用点）**：`AgentPromptAssembly.checkOutputGuardrail` 签名 +`List<ChatToolCall> toolCalls` 参数（消除 `hasToolCalls()`）；`AgentSessionLifecycle.recomputeToolExecutionKey` +`ChatToolCallMessage` 主路径 +`getToolCalls()` 过渡 fallback；`Layer2TurnPruningStrategy.assertBoundaryIntegrity` +`ChatToolCallMessage` callId 收集 +legacy fallback；`CallAgentExecutor`/`AgentCallDelegate` `instanceof ChatAssistantMessage` 语义不改（找最后 assistant 文本，`ChatToolCallMessage` 非文本，无需调整）。生产 grep `.hasToolCalls()` 0 残留。
  - **Phase 4（85 个 mock-LLM 测试批量迁移）**：Python 脚本 `migrate_phase4.py` 自动迁移 65 文件 + 并行 agent 手工迁移 19 文件；response 构造 → `ChatResponseFixtures.assistantWithToolCalls(...)`；session-history 构造 → `ChatResponseFixtures.foldedAssistantWithToolCalls(...)`（保留折叠形态，匹配引擎 `ctx.addMessage(assistantMsg)` 实际输出）；修复脚本引入的 7 处 empty-if/else 分支 + 6 处占位文本被替换的 regression。最终 `rg setToolCalls` nop-ai-agent test dir 0 残留（excl. helper）。
- 不动（按 Non-Goals）：`ChatAssistantMessage.toolCalls` 字段不改（329）、`AgentToolDispatcher` 并发结构不改（设计 #9）、流式不改（328）、ResponsesDialect 不实现（330）。
- 验证：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` **3404 tests, 0 failures, 0 errors**。`rg setToolCalls --glob '!ChatResponseFixtures.java' nop-ai-agent/src/test/` = 0。`rg '\.hasToolCalls\(\)' nop-ai-agent/src/main/` = 0。
- Doc-sync: `ai-dev/design/nop-ai-responses-migration-design.md` Phase 2 标 ✅；plan Plan Status→completed + 全 Phase items/Exit Criteria/Closure Gates 勾选。
- Commit: 待提交

---

### nop-ai Responses 迁移 2：ChatResponse.messages 列表 + dialect 双轨产出 + ResponseFormat/ContentPart 模型（plan 326，全 Phase）

- Plan: `ai-dev/plans/326-nop-ai-responses-migration-2-dialect-dual-track.md`
- Action: EXECUTE（Phase 1-2 全执行）；设计源 `ai-dev/design/nop-ai-responses-migration-design.md`（设计结论 #2/#4/#7）
- 前置核对：325 已于本日早些时候落地（`ChatReasoningMessage`/`ChatToolCallMessage`/`ApiStyle.responses` 均存在），故 Phase 2 依赖满足、可执行。
- 做了什么（双轨过渡是核心策略：旧 `message`/`getMessage()` 路径保留，新 `messages` 序列就绪供 327 切换）：
  - **Phase 1（nop-ai-api 模型）**：
    - `ChatResponse` +`messages`（`List<ChatMessage>`，`@JsonInclude(NON_EMPTY)`）+ `getMessages()`/`addMessage()`；`getMessage()` 标 `@Deprecated` 委托（messages 首个 `ChatAssistantMessage`，按类型筛选跳过前置 reasoning，无则退回旧 `message` 字段——保证 ReActAgentExecutor/MockChatService 既有调用点行为不变）；`success(List<ChatMessage>)` 工厂（同时播种旧 `message` 字段）；`copy()` 同步 messages。
    - 新增 `ResponseFormat`（type: json_object/json_schema + 可选 schema Map）；`ChatOptions.responseFormat` 字段类型 `String`→`ResponseFormat`；`getResponseFormat():String`/`setResponseFormat(String)` 标 `@Deprecated @JsonIgnore` 委托（type 透传，向后兼容 `WfAiHelper` 的 `"json"` 用法）；新增 `getResponseFormatConfig()`/`setResponseFormatConfig()` 对象访问；copy/merge/Builder 同步。
    - 新增 `ContentPart`（type: text/image/audio，text/detail/imageUrl/data + 工厂）；`ChatUserMessage` +`parts`（`List<ContentPart>`）+ `getParts()`；`getContent()` 委托为 parts 文本拼接（无 parts 时退回旧 content 字段，既有用例返回值不变）；copy 同步 parts。
  - **Phase 2（nop-ai-core 4 dialect parseResponse 双轨）**：OpenAi/Anthropic/Gemini/Ollama 在保留旧 `setMessage`（think/toolCalls 寄居）的同时，按语义顺序产出 `messages`（reasoning→assistant text→tool_calls）：thinking→`ChatReasoningMessage`、assistant 文本→同一 `message` 实例、tool_call→`ChatToolCallMessage.fromChatToolCall`。**修复 OllamaDialect 预存 bug**：`responseMap.get("message.tool_calls")` 字面 key 取不到嵌套结构（历史 tool_calls 永远解析不到），改为 `message`→`tool_calls` 路径取值（与 content 的 `message.content` 路径一致），使双轨真正生效。
  - 测试：`TestChatResponse`(9)/`TestChatOptionsAndUserMessageParts`(12) 新建于 nop-ai-api；4 dialect 测试新增 messages 断言（保留旧断言全绿证双轨）+ reasoning/tool_call 拆分类型断言；`TestChatServiceImplMessagesWiring`(1) 接线验证（Rule #23：ChatServiceImpl.call 非流式路径端到端拿到非空 messages）。
- 不动（按 Non-Goals）：`ChatAssistantMessage.think/toolCalls` 字段不改（329）、流式 StreamAggregator 不改（328）、agent 引擎不切到读 messages（327，双轨保证其继续读 getMessage().getToolCalls()）、ResponsesDialect 不实现（330）、`ILlmDialect.buildResponse` default 不改（328/329）。
- 偏差：① mission-driver 模板硬编码 `nop-stream` 为构建/测试模块，本 plan 目标是 `nop-ai`，按 plan 自身 Closure Gates 用 `nop-ai` 验证（与 08-06 plan 2249、08-07 plan 325 同一模板 artifact）。② owner-doc 裁定：实现严格遵循设计（messages 序列/ResponseFormat 对象/ContentPart 多模态/双轨），无投影语义细化——`No owner-doc update required`（design §五仅 Phase 0 标 ✅，Phase 1-4 无 ❌/✅ 标记可翻）。③ plan 无 `> Work Item:`/`> Source Audits:` 标签，roadmap ❌→✅ 与 source-audit 关闭步骤不适用。
- 验证：`./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-api -am` 关键测试绿（TestAnthropicDialect 14/0 含 tool_use callId Anti-Hollow、TestOllamaDialect 9/0 含 tool_call callId Anti-Hollow、TestOpenAiDialect 11/0 含 reasoning、TestGeminiDialect 7/0、TestChatServiceImplMessagesWiring 1/0 接线）；`./mvnw clean install -pl nop-ai -am -T 1C` **全 reactor BUILD SUCCESS**（含 nop-ai-agent 85 个 mock-LLM 测试——证 getMessage() 旧路径回归）；下游 `nop-wf/nop-wf-ai TestWfAiHelper` 4/0（证 getResponseFormat/setResponseFormat String 向后兼容）。`scan-hollow-implementations.mjs --module nop-ai-core` 退出码 0。
- Doc-sync: `ai-dev/design/nop-ai-responses-migration-design.md` 无需更新（No owner-doc update required）；plan Plan Status→completed + Phase 1/2 items/Exit Criteria/Closure Gates 全勾 + Closure 段填写（独立 closure-audit 留 mission-driver CLOSURE_VERIFY 阶段）。
- Commit: 待提交

---

### nop-ai Responses 迁移 1：消息类型体系扩展 + 序列化基础设施 landing（plan 325，全 Phase）

- Plan: `ai-dev/plans/325-nop-ai-responses-migration-1-message-types.md`
- Action: EXECUTE（Phase 1-2 全执行）；设计源 `ai-dev/design/nop-ai-responses-migration-design.md`（设计结论 #3/#6/#7/#11）
- 做了什么：把 nop-ai 消息模型从「角色折叠」朝「类型开放」推进一步，纯新增 + 单类删除 + 最小破坏：
  - 新增 `ChatToolCallMessage`（type `tool_call`：callId/name/arguments + `fromChatToolCall` 工厂，getContent 派生自 arguments text）。
  - 新增 `ChatReasoningMessage`（type `reasoning`：summary 必填 + detail 可选，getContent=summary）。
  - `ChatMessage` @JsonSubTypes：+tool_call/+reasoning，`ChatToolResponseMessage` key `tool`→`tool_output`，移除 `custom`。
  - `ChatToolResponseMessage.toolCallId` 改名 `callId`；`getToolCallId()`/`setToolCallId()` 标 `@Deprecated @JsonIgnore` 委托（保护 ~20 处既有调用点编译通过）。
  - 删 `ChatCustomMessage.java`（零代码引用，仅 @JsonSubTypes 注销）。
  - `ApiStyle` +`responses` 枚举值（仅枚举，`LlmDialectFactory` 未注册 dialect，330 才接）。
  - `AbstractLlmDialect.normalizeFinishReason` +`completed→stop`/`incomplete→length`（Responses API 终态原因归一）。
  - 测试：`TestChatMessageSerialization`（nop-ai-api，12 case，六型 round-trip + polymorphic list + custom 已注销回归）+ `TestNormalizeFinishReason`（nop-ai-core，8 case）。nop-ai-api pom 加 `jackson-databind:test` 依赖（沿用 nop-api-core 模式）以驱动 golden test。
- 不动（按 Non-Goals）：`ChatAssistantMessage.think/toolCalls/thinkSignature` 寄居字段保留（留 329）、4 个 dialect buildBody/convertMessage 不改（326）、agent 引擎不改（327）、`@JsonTypeInfo property` 仍为 `role`（329 切 type）。
- 偏差：① mission-driver 模板硬编码 `nop-stream` 为构建/测试模块，本 plan 目标是 `nop-ai`，按 plan 自身 Exit Criteria 用 `nop-ai` 验证（与 08-06 plan 2249 同一模板artifact）。② golden test 用 `new ObjectMapper().configure(FAIL_ON_UNKNOWN_PROPERTIES,false)` 模拟生产 JSON provider 配置（@JsonTypeInfo property `role` 在 strict 模式下被当未知字段，生产 provider 同样关闭该开关）。③ plan 原指向 `ai-dev/logs/2026/08-02.md` 追加日志，按 AGENTS.md「一天一个文件」改为新建 `08-07.md`。
- 验证：`./mvnw clean install -pl nop-ai/nop-ai-api -am -T 1C -DskipTests` SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` **全 reactor BUILD SUCCESS**（nop-ai-core/agent/gateway/dao/service/web/toolkit/coder/... 全绿）。新增测试实测执行非 skipped：`TestChatMessageSerialization` 12/0、`TestNormalizeFinishReason` 8/0；持久化 round-trip `TestFileBackedSessionStore` 29/0、`TestDBSessionStore` 20/0（`role` 字段名未变 + tool_output discriminator + callId 字段名变更无历史数据迁移负担）。`rg "ChatCustomMessage" --glob *.{java,xml}` 仅余测试中描述性注释字符串，零代码引用。
- Doc-sync: `ai-dev/design/nop-ai-responses-migration-design.md` Phase 0 标注 ✅ plan 325 落地；plan Plan Status→completed + Phase 1/2 items/Exit Criteria 全勾 + Closure 段填写（独立 closure-audit 留 mission-driver CLOSURE_VERIFY 阶段）。
- Commit: 待提交
