### 2026-07-07（plan 288 sys-event 可靠广播与分区队列实现中）

- Plan: `ai-dev/plans/288-nop-sys-sys-event-reliability-refactor.md`
- 已将 `nop-sys` 的事件持久化模型从“单表混用”收敛为三类 source object：`NopSysEvent` 保留为普通事件 shared queue，新增 `NopSysBroadcastEvent`（append-only 广播流）和 `NopSysBroadcastCursor`（subscriber cursor + lease）；同时给 `NopSysEvent` 补 `CLAIMED` 状态和 `leaseOwner/leaseExpireTime`。
- `SysDaoMessageService` 现已按 topic 语义分流发送路径，普通事件按稳定 `bizObjName|bizKey -> shortHash` 路由分区；广播消费改为 cursor/lease 恢复，不再依赖 JVM 内存 `lastBroadcastEvent`；普通事件消费改为按 `assignedPartitions` 过滤、按分区头 claim、失败阻塞头部、`ConsumeLater` 重排队。
- 新增 focused integration test `nop-sys/nop-sys-dao/src/test/java/io/nop/sys/dao/message/TestSysDaoMessageService.java`，覆盖发送分流、稳定分区键、广播 cursor 推进/失败阻塞、普通事件分区头阻塞与 `ConsumeLater` 语义。
- 验证：`./mvnw -pl nop-sys/nop-sys-dao -Dtest=TestSysDaoMessageService test` 通过。
- Doc-sync: `ai-dev/design/nop-sys/sys-event-architecture.md`、`docs-for-ai/03-modules/nop-sys.md`、`docs-for-ai/04-reference/source-anchors.md`、`ai-dev/plans/288-nop-sys-sys-event-reliability-refactor.md`、`ai-dev/logs/2026/07-07.md` 已更新。

### 2026-07-07（plan 286 收口完成）

- Plan: `ai-dev/plans/286-nop-job-assignment-metadata-refactor.md`
- 收口验证已完成：在前期 bestFit assignment 元数据/assignment-cost/dispatcher preserve 业务改动基础上，继续顺序恢复 `nop-job -am` 上游构建链，新增最小显式依赖 `nop-auth-service -> nop-core`、`nop-job-service -> nop-core`，并补 focused regression test，确认 `Assignment` typed metadata 能通过 `JsonTool.parseBeanFromText(..., Assignment.class)` 与 `BeanTool.buildBean(..., Assignment.class)` 真正落到对象，而不只是内存 setter/getter。
- 独立 closure audit 首轮发现唯一 blocker 是 Phase 2 承诺的 DataBean/JSON/BeanTool 构造路径缺少真实证明；修复后复审通过，确认 runtime wiring `IWorkerAssignmentStrategy -> AdaptiveJobTaskBuilder -> JobDispatcherScannerImpl -> JobFireStoreImpl.insertTasksAndMarkFireDispatching()` 连通，且 bestFit 元数据/cost 会真实落库到 `NopJobTask`。
- 验证：`./mvnw test -pl nop-job/nop-job-coordinator -am` 通过；`./mvnw test -pl nop-job -am` 通过；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-job --severity high` 通过（0 findings）；`node ai-dev/tools/check-doc-links.mjs --strict` 通过；`node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/286-nop-job-assignment-metadata-refactor.md --strict` 通过。
- Doc-sync: `ai-dev/plans/286-nop-job-assignment-metadata-refactor.md`、`ai-dev/logs/2026/07-07.md` 已更新；owner docs `ai-dev/design/nop-job/worker-assignment-design.md`、`docs-for-ai/03-modules/nop-job.md` 保持与 live baseline 一致，无新增 doc drift。

### 2026-07-07（sys-event 重构计划起草）

- 新增 owner plan：`ai-dev/plans/288-nop-sys-sys-event-reliability-refactor.md`，把 `sys-event` 重构收敛为一个单独计划，覆盖 source ORM model、发送路径拆分、广播 cursor/lease、普通事件 partition ownership、chunk-like 优化边界，以及 owner-doc / closure audit 收口。
- 计划明确不把 `sys-event` 建模成 `nop-batch` task，不引入外部 MQ，也不把 completedIndex / chunk completion 当成事件正确性边界；同时要求端到端验证、接线验证与无静默跳过验证。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict`
- Doc-sync: `ai-dev/plans/288-nop-sys-sys-event-reliability-refactor.md`、`ai-dev/logs/2026/07-07.md` 已更新。

### 2026-07-07（sys-event chunk 语义澄清）

- 收敛 `ai-dev/design/nop-sys/sys-event-architecture.md` 的 `3.4 与 nop-batch 的关系` 表述：不再笼统写成“不要复用 chunk runtime”，而是明确区分“可复用的 chunk-like 执行优化”和“不可直接复用的 chunk completion 正确性边界”。
- 文档现在明确允许 `sys-event` 复用 windowed fetch、一次抓取 N 条、bounded session reuse、批量时间窗口扫描等优化；同时明确普通事件的 ack 边界是“单 event + 分区连续成功前缀”，广播的推进边界是“单 event + subscriber cursor 连续成功前缀”，不能把一个预取 chunk 当作统一确认单元。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict`
- Doc-sync: `ai-dev/design/nop-sys/sys-event-architecture.md`、`ai-dev/logs/2026/07-07.md` 已更新。

### 2026-07-07（nop-xlang union schema validator）

- Plan: `ai-dev/plans/287-nop-xlang-union-schema-validator.md`
- 为 `nop-kernel/nop-xlang` 的 `SchemaBasedValidator` 增加 union 分支，按 `subTypeProp -> typeValue` 路由到子 schema，并支持 `typeValue="*"` fallback；缺失/未知 subtype 改为显式 validation error。
- 补 focused regression test：新增 `nop-kernel/nop-xlang/src/test/java/io/nop/xlang/xmeta/TestSchemaBasedValidator.java`，覆盖 subtype 命中、wildcard fallback、缺失 subtype、未知 subtype 无 fallback 四类行为。
- Doc-sync: `ai-dev/design/nop-core/01-union-schema-validation.md`、`docs-for-ai/02-core-guides/xdef-and-xdsl.md`、`docs-for-ai/INDEX.md`、`docs-for-ai/04-reference/source-anchors.md` 已更新。
- 验证：`./mvnw test -pl nop-kernel/nop-xlang -am` 通过；`node ai-dev/tools/check-doc-links.mjs --strict` 通过（0 errors，53 existing warnings）；`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-kernel/nop-xlang --severity high` 失败，但命中 `IObjPropMeta.java` 与 `MacroScriptTagCompiler.java` 的既存高优先级问题，独立 closure audit (`ses_0c4343c6fffexYvIKcKm4eGydH`) 判定与本次 union 修复无关，因此代码改动可交付，但 plan 暂不能 completed。

### 2026-07-07（plan 286 assignment 元数据重构执行中）

- Plan: `ai-dev/plans/286-nop-job-assignment-metadata-refactor.md`
- 将 `nop-job-coordinator` 的 `Assignment` 收敛为 typed bestFit contract：新增 `targetHost`、`shardingIndex`、`shardingTotal`、`partitionRange`，并让 `AdaptiveJobTaskBuilder` 把这些字段映射到现有 `NopJobTask` 列。
- 调整 bestFit builder/dispatcher 语义：`Assignment.cost` 非空时写入 task cost；为空时回退到 schedule cost；`JobDispatcherScannerImpl` 改为保留 builder 已设置的 cost/priority，只为 null 值填充 schedule 默认并做 null→0 归一；bestFit 多 assignment、null assignment、blank `workerInstanceId` 现在都显式 fail fast。
- 补 focused tests：覆盖 `Assignment` DataBean/BeanTool 构造、bestFit assignment 元数据映射、assignment-cost 覆盖 schedule-cost、dispatcher 保留 builder 值、以及 bestFit 端到端落库链路。
- 更新文档：`ai-dev/design/nop-job/worker-assignment-design.md` 已重写为当前 active baseline；`docs-for-ai/03-modules/nop-job.md` 已同步 bestFit 单 assignment、typed assignment 元数据与 dispatcher preserve 规则。
- 验证：`./mvnw test -pl nop-job/nop-job-coordinator -am` 失败，阻塞在仓库无关上游模块 `nop-orm-eql` 的现有编译问题（`SqlProgram.setLocation`）；`./mvnw -pl nop-job/nop-job-coordinator -am -DskipTests install` 继续推进后又阻塞在上游 `nop-orm` AOP 生成的现有 `IJsonHandler` 缺类问题，均尚未进入 `nop-job-coordinator` 本模块测试阶段。
- 追加验证：`./mvnw -pl nop-job/nop-job-coordinator -Dtest=TestAdaptiveJobTaskBuilder,TestLeastLoadedStrategy,TestRpcBroadcastTaskBuilder,TestDefaultJobTaskBuilder,TestPartitionTaskBuilder,TestJobDispatcherScannerRouting test` 通过；`./mvnw -pl nop-job/nop-job-coordinator -Dtest=TestJobCoordinatorScanner#testBestFitAssignmentMetadataAndAssignmentCostEndToEnd,TestJobCoordinatorScanner#testDispatcherPreservesBuilderSetCostAndPriority test` 通过。`./mvnw -pl nop-job/nop-job-coordinator test` 仍被模块内既有无关编译问题阻塞：`JobCompletionProcessorImpl` 缺 `ErrorMessageManager`，失败集中在 completion / timeout / e2e 既有测试，不在本次变更路径上。
- 后续收口进展：顺序 clean / test 后，`nop-orm-eql`、`nop-orm`、`nop-job-coordinator`、`nop-xlang`、`nop-excel`、`nop-antlr4-tool` 的多处“缺类/NoSuchMethod/未重新编译”现象被逐步证实主要来自 worktree 内多个 Maven 任务交错执行后的产物漂移；期间补了最小稳定性修复：`nop-job-coordinator`、`nop-orm-eql`、`nop-orm` 的显式 `nop-core` 依赖，`nop-job-coordinator` 的显式 `nop-orm` 依赖，以及 `MethodModelBuilder` 对 `@Macro` 首参允许 `IEvalScope` 子类型。`MacroScriptTagCompiler` 保持 fail-fast，只在错误信息里回报不支持的 `lang`。
- 当前 blocker 已收敛到 kernel 构建链：`./mvnw test -pl nop-job/nop-job-coordinator -am` 会在上游 `nop-xlang` testCompile 阶段失败，现象表现为 test source 与 live API / generated AST 基类的符号视图不一致，超出 `plan 286` 的业务变更范围。
- Doc-sync: `ai-dev/design/nop-job/worker-assignment-design.md`、`docs-for-ai/03-modules/nop-job.md` 已更新。

### 2026-07-07（record-mapping owner-doc 补齐）

- 新增 `docs-for-ai/02-core-guides/record-mapping.md`，把 `record-mapping` 明确收敛为"字段变换 + 类型转换 + 条件校验"的统一入口，补齐 `when`、`mandatory`、`schema`、`itemMapping`、`patternField`、`varName`、gateway `bodyMapping` 的默认语义与边界。
- 更新 `docs-for-ai/INDEX.md`，新增 Record Mapping 路由入口，避免后续再靠直接读 `record-mapping.xdef` 和实现猜测默认做法。
- 更新 `docs-for-ai/04-reference/source-anchors.md`，补 `MAP-001` 到 `MAP-004` 锚点，覆盖 DSL 定义、执行顺序、递归映射和 gateway 接线。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict`
- Doc-sync: `docs-for-ai/02-core-guides/record-mapping.md`、`docs-for-ai/INDEX.md`、`docs-for-ai/04-reference/source-anchors.md` 已更新。

### 2026-07-07（sys-event 重构设计基线）

- 为 `sys-event` 重构新增 owner-doc：`ai-dev/design/nop-sys/README.md` 与 `ai-dev/design/nop-sys/sys-event-architecture.md`，明确裁定广播事件与普通事件必须分开存储；广播采用 append-only event stream + subscriber cursor + lease 保证单订阅者顺序与不遗漏恢复；普通事件采用 shared queue + `partitionIndex` 分区归属，分区内串行、分区间并行。
- 设计同时明确了与 `nop-batch` 的边界：复用 `StringHelper.shortHash`、`IntRangeSet`、`WeightedPartitionAssigner`、`partitionRange` 约定，不直接复用 chunk / DSL / batch task runtime。
- 更新 `ai-dev/design/README.md`，把 `nop-sys/` 目录登记到设计索引。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict`
- Doc-sync: `ai-dev/design/README.md`、`ai-dev/design/nop-sys/README.md`、`ai-dev/design/nop-sys/sys-event-architecture.md` 已更新；`docs-for-ai/` 暂无 owner-doc 变更，因为本次只建立平台设计基线，尚未落地代码与使用面。

### 2026-07-07（nop-sys 设计文档目录归并）

- 将误放在 `ai-dev/design/sys/` 下的 `sys-event` owner-doc 归并到既有 `ai-dev/design/nop-sys/` 子系统目录，统一 `nop-sys` 设计文档的 owner-doc 入口，避免同一子系统出现两套并行目录。
- 新增 `ai-dev/design/nop-sys/README.md`，把 `sys-event` 与 `compact-ext-field` 一并纳入 `nop-sys` 的阅读顺序与职责边界。
- 更新 `ai-dev/design/README.md` 与当日日志中的路径引用，删除旧的 `ai-dev/design/sys/README.md`。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict`
- Doc-sync: `ai-dev/design/README.md`、`ai-dev/design/nop-sys/README.md`、`ai-dev/design/nop-sys/sys-event-architecture.md`、`ai-dev/logs/2026/07-07.md` 已更新。
