# 2026-07-16

## Plan 0800-2 完成（nop-metadata 联邦查询 — 跨表 JOIN + 指标/维度聚合 P4-2/P4-3）

- Plan: `ai-dev/plans/2026-07-16-0800-2-nop-metadata-cross-table-join-and-aggregation.md`
- 目标：在 0800-1 单表查询基础上，完成联邦查询的「分析查询」能力——MetaTableJoin 多表关联（同库 SQL JOIN / 跨库应用层拼接）+ MetaTableMeasure/MetaTableDimension 自动聚合查询（含时间维度 granularity 分桶）。

### Phase 1 完成：跨表 JOIN 执行（P4-2，D3/D4/D5）
- 设计裁定 D3（同 querySpace→单库 JOIN；不同 querySpace→应用层拼接，关闭 §八 待定问题）/ D4（entity 同库经 `orm().executeQuery` 原生 JOIN SQL，物理表 t1/t2 别名 + 物理列 columnCode；external/sql 同库经 withConnection 原生 JOIN SQL；**经核验 `QueryBean.innerJoin/leftJoin` 是 MDX 内存 dimField 对齐非关系型 JOIN，故 entity 同库走原生 SQL**）/ D5（跨库拼接契约：join key 字符串相等匹配 + 右列 alias_ 前缀 + 无全局分页保证 + 规模上限 MAX_CROSS_DB_ROWS + joinType=right 显式失败）写入 `01-architecture-baseline.md` 新增「§4.4.1 跨表 JOIN 执行」节。
- 新增组件 `io.nop.metadata.service.query.MetaJoinExecutor`：D3 路由 + 同库 JOIN（orm().executeQuery 物理表 JOIN，join key 投影规避保留字）+ 跨库拼接（fetchEntityRows ORM 取数 + 内存合并）。
- 新增 `MetaQueryContext`（JOIN/聚合执行器共享依赖上下文）。
- 新增 `@BizQuery queryJoinData` action（`NopMetaTableBizModel`）。
- 默认过滤器自动应用 helper `DefaultFilterApplicator`（收口 0700-2 follow-up，单表/JOIN/聚合共用）。
- 新增测试 `TestNopMetaJoinBizModel`（5 用例全绿：同库真实关联行 + 跨库合并 + 列前缀 + right/无join/字段未解析 显式失败）。

### Phase 2 完成：指标/维度聚合查询（P4-3，D6/D7）
- 设计裁定 D6（external/sql 经 withConnection 原生聚合 SQL；entity 经 `orm().executeQuery` 物理列 columnCode；ORM 隐式过滤旁路裁定——原生 SQL 不应用隐式过滤，文档显式提示，不静默忽略；expression 型 Measure 显式不支持）/ D7（granularity→分桶翻译表：H2/PG DATE_TRUNC / MySQL DATE_FORMAT；非约定值/不支持方言显式失败）写入「§4.4.2 指标/维度聚合查询」节。
- 新增组件 `MetaAggregationExecutor`（aggFunc sum/count/avg/min/max/countDistinct 翻译；entity/external 路由）+ `GranularityBucketing`（按方言 granularity 分桶）。
- 新增 `@BizQuery queryAggregation` action（`NopMetaTableBizModel`）。
- 新增测试 `TestNopMetaAggregationBizModel`（7 用例全绿：categorical sum/count + countDistinct + 时间 month 分桶（H2 DATE_TRUNC 真实断言）+ 默认过滤器 AMOUNT>15 生效 + expression Measure 显式失败 + 非约定 granularity 失败 + entity 聚合真实分组）。

### 关键发现（偏差/实地验证）
- **`QueryBean.join` 不是关系型 JOIN**：经源码核验 `MdxQuerySplitter` 不读 `conditions`/`joinType`，是 MDX 内存 dimField 对齐合并。故 D4 entity 同库 JOIN 改用 `orm().executeQuery` 原生 SQL（t1/t2 别名），与 D6 entity 聚合一致载体。写入 §4.4.1。
- **EQL 经 `orm().executeQuery` 校验函数名**：`FORMATDATETIME` 被判 unknown-function（EqlCompiler 编译）。故 entity 时间维度完整 granularity 分桶受 EQL 函数白名单限制（首版按物理列直查），完整分桶仅在 external/sql 路径（withConnection 原生 SQL）支持。写入 §4.4.2 D7。
- **`orm().executeQuery` 支持 allowUnderscoreName 原生 SQL**：经 ProbeTest 实证，`SQL.begin().allowUnderscoreName(true).sql("select STATUS from NOP_META_MODULE")` 可执行物理表/列 SQL（GROUP BY/JOIN/count(*) 均通过）。EQL 表别名须在 FROM 声明（`FROM T t1`），table-qualified 列用别名。
- **同库 JOIN 仅投影 join key 列**：部分物理列名是 SQL/EQL 保留字（PRECISION/SCALE/NUMBER），裸拼接触发 parse-fail；故同库 JOIN 仅选 join key（列名安全），跨库路径返回全部属性列。
- **跨库测试需 SQL UPDATE + evict**：`getEntityById` 每次重读 DB（flushSession 不改其读取值）；测试用 `executeUpdate` 直改 NOP_META_ENTITY.QUERY_SPACE + evictAll 触发跨库路由。

### 验证
- `./mvnw clean install -pl nop-metadata -am -T 1C`：BUILD SUCCESS（含测试，0 failure）。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：退出码 0（0 high findings）。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：退出码 0（全 [x]）。
- Doc-sync: `01-architecture-baseline.md` 新增 §4.4.1/§4.4.2（D3-D7），关闭 §八 待定问题。

---

## Plan 0800-1 执行中（nop-metadata 联邦查询 — 单表数据查询接口 P4-1）

- Plan: `ai-dev/plans/2026-07-16-0800-1-nop-metadata-federated-single-table-query.md`
- 目标：建立 nop-metadata 联邦查询执行架构——统一查询 action，对任意 tableType（entity/external/sql）逻辑表返回行数据，按 querySpace 路由到正确执行路径。解除 P2/P3 多个 plan deferred 的「entity/sql 类型表执行（querySpace→数据源解析）」阻塞。

### Phase 1 完成：设计裁定 + querySpace 执行路径解析
- 设计裁定 D1（单表查询三路分派 entity→IOrmTemplate/external→withConnection/sql→withConnection 执行 sourceSql 子查询；entity 路径 `QueryBean.sourceName=entityName`+`orm().findListByQuery`+`isValidEntityName` 前置；方言范围 H2/MySQL/PostgreSQL LIMIT/OFFSET；filter→WHERE 注入防护复用 §2.7.1 D3 白名单）/ D2（sql 表 querySpace 必须非 null 且匹配 NopMetaDataSource，否则显式失败；平台 ORM querySpace fallback 首版不做）写入 `01-architecture-baseline.md` 新增「§4.4 查询执行」节。
- 新增共享组件 `io.nop.metadata.service.datasource.MetaDataSourceResolver`：querySpace→NopMetaDataSource findFirstByQuery 首条（多匹配取首条）；找不到/DISABLED/null 显式失败抛 inline ErrorCode（metadata.datasource-resolve-not-found / -disabled）。独立实现，不重构既有三处重复 resolveDataSourceOrThrow（Non-Blocking Follow-up）。
- 新增测试 `TestMetaDataSourceResolver`（5 用例，found/not-found/null-querySpace/DISABLED/multi-match，全绿）。

### Phase 2 完成：统一单表查询 action + 三类表端到端验证
- 新增 `FilterToSqlTranslator`（`.../service/query/`）：TreeBean filter→SQL WHERE 翻译器。支持标准叶子条件（eq/ne/gt/ge/lt/le/like/in/notIn/between/isNull/notNull）+ 组合（and/or/not）+ alwaysTrue/alwaysFalse；标识符经 `^[A-Za-z_][A-Za-z0-9_]*$` 白名单（复用 §2.7.1 D3）；值用 PreparedStatement 参数绑定（按序返回 params）。不支持的 op/缺字段/缺值 显式失败抛 inline ErrorCode（不静默跳过）。新增测试 `TestFilterToSqlTranslator`（18 用例，全绿）。
- 新增 `queryTableData(@BizQuery)` action（`NopMetaTableBizModel`）：按 tableType 三路分派：
  - **entity** → baseEntityId→NopMetaEntity→entityName + `orm().isValidEntityName` 前置（未注册显式失败）→ `daoProvider().dao(entityName)` 取 IEntityDao → findAllByQuery（filter/limit/offset 委托 QueryBean）→ 实体列名投影转 Map（列名取自 IEntityModel.getColumns）。
  - **external** → querySpace→MetaDataSourceResolver→withConnection→`SELECT cols FROM <table> [WHERE] [LIMIT/OFFSET]`（列名取自 buildSql JSON via fieldResolver；filter→WHERE via translator）。
  - **sql** → D2 querySpace 解析（null/无匹配显式失败）→withConnection→`SELECT * FROM (<sourceSql>) _t [WHERE] [LIMIT/OFFSET]`（filter 应用到外层）。
  - 方言范围 H2/MySQL/PostgreSQL（getDatabaseProductName 运行时识别，其他显式失败）。失败路径全显式（表不存在/querySpace 无数据源/DISABLED/非 jdbc/sql querySpace null 或无匹配/实体未注册/不支持方言/sourceSql 空/未知 tableType）。
- 新增测试 `TestNopMetaTableQueryBizModel`（12 用例，全绿）：entity（importOrmModel 真实查 NopMetaModule 实体表返回真实导入行 + filter 接线）/ external（H2 造数+sync+真实行断言 + filter(amount>15) + 分页）/ sql（H2 造数+createSqlTable+sourceSql 子查询真实行断言）/ 失败路径（表不存在/无数据源/DISABLED/sql querySpace null/无匹配/实体未注册 全显式失败）。

### 关键发现（偏差/实地验证）
- **entity 路径不能用 `orm().findListByQuery(QueryBean)`**：该 API 经 MdxQueryExecutor，要求 QueryBean.fields 非 null（splitFields 遍历 fields，null→NPE），是字段投影/聚合（MDX）查询入口，非"取全部实体行"。改用 `daoProvider().dao(entityName).findAllByQuery(query)` 取实体再按 IEntityModel.getColumns 投影转 Map。
- **TreeBean 非 GraphQL 命名类型**：`$filter: TreeBean` 变量声明解析失败；filter E2E 测试改为直接调 BizModel action 传 `FilterBeans.gt(...)`（filter→WHERE 翻译本身由 `TestFilterToSqlTranslator` 18 用例覆盖）。
- **@BizQuery 返回 Map 不支持 GraphQL 字段选择**（`[Map]不是对象类型`）；与 previewSqlFields/profileTable 一致，queryTableData 测试统一按整 Map 返回后取字段。

### 验证
- `./mvnw clean install -pl nop-metadata -am -T 1C`：BUILD SUCCESS。nop-metadata-service 共 135 tests 全 0 failure（baseline 100 + 新增 35：resolver 5 + translator 18 + query 12）。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：退出码 0（无 high 空壳发现）。
- Doc-sync: `01-architecture-baseline.md` 新增「§4.4 查询执行」节（D1/D2 裁定）已更新。

---

## Plan 0700-2 完成（nop-metadata BI 语义层 Measure/Dimension/Filter/Join 校验与字段解析）

- Plan: `ai-dev/plans/2026-07-16-0700-2-nop-metadata-bi-semantic-validation-and-field-resolution.md` → `completed`
- Roadmap: P3-2 + P3-3 + P3-4 + P3-5 `todo` → `done`；P3 整体 `todo` → `done`
- 收口设计决策 D1（Dimension.dimensionType 取值语义 categorical/temporal/geographical；granularity 自由 string 文档约定值 year/quarter/month/week/day/hour 不新增 dict；Filter.definition 对齐平台 TreeBean filter 树非整个 QueryBean，反序列化 `JsonTool.parseBeanFromText(def, TreeBean.class)`；json-4000 超限显式失败；isDefault 首版强制每表唯一）/ D2（校验落点=引入 save override 新模式重写 `CrudBizModel.save(Map, IServiceContext)`；可用字段集合 entity→手动 query baseEntityId→NopMetaEntityField（baseEntityId null 显式失败）/ external→buildSql JSON columnName / sql→SqlSelectFieldExtractor；entityFieldId 语义按 tableType 重载 entity=PK 硬匹配 external/sql=字段名字符串；expression 型 Measure entityFieldId null 跳过校验；Join 首版仅 entity 实体关联；resolveTableFields 由 0700-1 sql-only 扩展为全 tableType）。收口 §2.5 仅有 Measure/Join 的语义 gap。

### 新增代码
- `io.nop.metadata.service.field.ResolvedTableField`（字段 DTO：name/sourceType/type?）。
- `io.nop.metadata.service.field.MetaTableFieldResolver`（无状态跨表类型字段解析器）：resolve(table, fieldDao) 按 tableType 分派 entity/external/sql；validateFieldReference 封装 entityFieldId 语义重载校验（entity=PK 归属，external/sql=字段名集合）；resolveEntityFieldsByEntityId 供 Join 校验。6 个解析器内 ErrorCode（field-resolve-table-not-found/base-entity-null/no-fields/external-build-sql-invalid/unknown-table-type）。
- `NopMetaTableMeasureBizModel`/`NopMetaTableDimensionBizModel`：save override 调用 `fieldResolver.validateFieldReference`，非法字段引用显式失败；expression 型（entityFieldId null）跳过。各自 inline ErrorCode（measure/dimension-table-not-found + field-not-found）。
- `NopMetaTableJoinBizModel`：save override 校验 left/right NopMetaEntity 存在 + leftField/rightField 属于对应实体字段集合。3 个 inline ErrorCode（join-entity-not-found/field-not-in-entity/entity-id-null）。
- `NopMetaTableFilterBizModel`：save override 校验 definition 反序列化为 TreeBean + isDefault 唯一性。3 个 inline ErrorCode（filter-definition-invalid/empty/default-already-exists）。
- `NopMetaTableBizModel.resolveTableFields` 由 sql-only 扩展为全 tableType 分派（委托 MetaTableFieldResolver），返回结构统一 `{tableType, fields:[{name, sourceType, type?}]}`。移除两个已失效的 sql-only ErrorCode（sql-table-not-sql/sql-source-sql-empty，分派后由 resolver 统一显式失败）。

### 关键发现（偏差/实地验证）
- **entity 表字段引用存储语义重载**：entity 表 entityFieldId 为 NopMetaEntityField 主键（PK 硬匹配 + metaEntityId==baseEntityId 归属校验），external/sql 表 entityFieldId 为字段名字符串（语义重载）。复用同一列而非存 extConfig，避免与扩展属性混淆。
- **save override 是 GraphQL mutation 统一入口**：`CrudBizModel.save(Map, IServiceContext)` 覆盖所有 save 路径（UI/GraphQL/xbiz），override 一次即可，无需新增自定义 action 名、不破坏 CRUD 契约。校验通过后委托 `super.save(...)`。
- **resolveTableFields 扩展不破坏 0700-1 的端到端测试**：sql 表测试 `testResolveTableFieldsMatchesPreview` 仍通过（仅校验 name 字段，跨类型结构兼容）；原 `testResolveTableFieldsFailsOnNonSqlTable` 改为验证 entity 表 baseEntityId null 显式失败（新行为，断言 hasError 仍成立）。

### 测试（`TestNopMetaBiSemanticBizModel` 新增 22 用例，全绿）
- resolveTableFields 跨类型：entity（NopMetaEntityField 集合 2 字段）/ external（buildSql JSON columnName 2 字段）/ sql（SELECT 解析 order_id/cid）/ 失败路径（entity baseEntityId null、external 损坏 buildSql）显式失败。
- Measure save：合法 entityField（PK）/ external 字段名 通过；非法（不属于表实体 / 未知列）显式失败；expression 型（entityFieldId null）跳过校验通过。
- Dimension save：合法 temporal + granularity=month 自由 string 通过；非法字段引用显式失败。
- Join save：合法（left/right 实体存在+字段属于实体）通过；实体不存在 / 字段不属于实体 显式失败。
- Filter save：合法 TreeBean（eq）/ 组合（and+eq+gt）通过；非法 JSON / 空定义 / isDefault 唯一性违反 显式失败。
- **Anti-Hollow**：`testResolverDirectDispatch` 直接调用解析器验证三类分派返回真实字段；`testResolverEntityBaseEntityIdNullThrows` 验证非静默空集；非法引用被拒证明 save override 运行时确实调用了 resolver（接线连通）。

### 验证
- `./mvnw clean install -pl nop-metadata -T 1C`：BUILD SUCCESS。`TestNopMetaBiSemanticBizModel` 22 tests，nop-metadata-service 共 100 tests 全 0 failure。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings，退出码 0。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：退出码 0（所有 checklist 已勾选 + Closure Evidence 已写入）。
- Doc-sync: `01-architecture-baseline.md` §2.5（补齐 Dimension/Filter 实体结构 + 新增 §2.5.2 D1/D2 裁定）已更新。

---

## Plan 0700-1 完成（nop-metadata SQL 视图创建 + SELECT 字段解析）

- Plan: `ai-dev/plans/2026-07-16-0700-1-nop-metadata-sql-view-creation-and-field-parsing.md` → `completed`
- Roadmap: P3-1 + P3-6 ❌(planned) → ✅(done)
- 收口设计决策 D1（字段名解析=alias 优先/列名次之/表达式列标记 `<expr_N>`；通配符 `*`/`t.*`=显式失败，纯 AST 无法展开；多语句=显式失败；非 SELECT=显式失败；CTE 钻入 `getSelect()`；UNION 委托 firstSelect；类型获取=方案 A 仅名不取类型 type=null 不伪造，LIMIT 0/ResultSetMetaData 为 follow-up）/ D2（action 落点=NopMetaTableBizModel，三个 action 契约，fields 项结构统一 `{name, alias?, type?}`）。收口 §八"SQL 视图字段解析"待定问题。

### 新增代码
- `io.nop.metadata.service.sqlview.SqlViewField`（字段 DTO：name/alias?/type?）。
- `io.nop.metadata.service.sqlview.SqlSelectFieldExtractor`（无状态解析器，复用 `EqlASTParser.parseFromText` 纯语法 AST，与 §2.6.1 血缘 `SqlSourceTableExtractor` 同解析器/同无 session 模式）：解析路径 SqlProgram→statements(size==1)→statementKind==SELECT→projections（SqlSelectWithCte 钻入 getSelect()）→字段（SqlAllProjection/空 projections=显式失败，SqlExprProjection alias 优先/SqlColumnName.getName 次之/`<expr_N>` 表达式标记）。5 个解析器内 ErrorCode（sql-empty/parse-failed/multi-statement/not-select/wildcard-not-supported）。
- `NopMetaTableBizModel` 新增 3 个 action：`createSqlTable(@BizMutation)` 校验+解析+建表+返回 fields；`previewSqlFields(@BizQuery)` 纯解析不持久化；`resolveTableFields(@BizQuery)` 加载 sql 表+解析 sourceSql。4 个 BizModel 内 ErrorCode（sql-module-not-found/sql-table-not-found/sql-table-not-sql/sql-source-sql-empty，用 sql- 前缀避免与 lineage 的 lineage-not-sql-table 重名）。

### 关键发现（偏差/实地验证）
- **裸 `SELECT *` 被 EqlASTParser 静默丢弃为空 projections**（实地验证：`SELECT * FROM t` → projections.size()==0；`SELECT t.* FROM t` → 1 SqlAllProjection）。合法显式列 SELECT 必有 ≥1 projection，故空 projections 唯一现实成因是被丢弃的裸 `*` → 显式失败抛 wildcard ErrorCode（不静默返回空字段列表）。
- 表达式列序号按表达式列自身序号编号（仅表达式列递增），而非全部列序号。

### 测试（`TestNopMetaTableBizModel` +11 用例，全绿）
- createSqlTable 成功：多列+别名列+表达式列 → 字段名与 SELECT 输出一致（id/user_name/<expr_1>），type=null（方案 A 不伪造），端到端 getEntityById 返回 tableType=sql+sourceSql 已存。
- 失败路径显式：非 SELECT/空 SQL/语法错误/多语句/通配符(`*` 和 `t.*`)/module 不存在 全部 hasError=true。
- previewSqlFields：字段名一致 + 无持久化副作用（表行数前后相等）。
- resolveTableFields：与 preview 字段一致（结构统一），非 sql 表/不存在表 显式失败。
- **Anti-Hollow**：createSqlTable/resolveTableFields 返回的 fields 来自真实 AST 解析（断言字段名与 SELECT 列匹配、字段数一致），非空壳。

### 验证
- `./mvnw clean install -pl nop-metadata -am -T 1C`：BUILD SUCCESS。`TestNopMetaTableBizModel` 21 tests（含新增 11），nop-metadata-service 78 tests 全 0 failure。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings，退出码 0。
- Doc-sync: `01-architecture-baseline.md` §4.2（新增 §4.2.1 字段解析方案 + §4.2.2 action 契约）+ §八（关闭待定问题）已更新。

### 偏差
- 裸 `SELECT *` 的通配符检测：plan 假设通配符都表现为 `SqlAllProjection`，但实地发现裸 `*` 被解析器静默丢弃为空 projections。实现中对空 projections 显式失败（与 `t.*` 的 SqlAllProjection 一并归为 wildcard 显式失败路径），符合"不静默跳过"裁定。

---

## Plan 0530-2 完成（nop-metadata 数据剖析 Profiling）

- Plan: `ai-dev/plans/2026-07-16-0530-2-nop-metadata-data-profiling.md` → `completed`
- Roadmap: P2-7 ❌(planned) → ✅(done)
- 收口设计决策 D1（建模选型=新建独立实体 NopMetaProfilingRule/Result，不复用 MetaQualityRule；存储形态 per-execution 时序行 + tableStats/columnStats mediumtext+json；profilingRuleId 非 mandatory 以支持 profileTable 无规则直接剖析）/ D2（统计范围+可移植性+降级，live 核查 H2 2.4.240 支持 STDDEV_SAMP/MEDIAN/PERCENTILE_CONT，但 MySQL 无原生 percentile → median/percentiles/distribution 用 in-app 排序计算全方言精确；sizeBytes/lastModified 方言特定 → null+unavailable 标记）/ D3（执行机制=BizModel action + withConnection + 无状态剖析器；主入口 NopMetaTableBizModel.profileTable + 辅助入口 NopMetaProfilingRuleBizModel.executeProfilingRule；列名+类型运行时 DatabaseMetaData.getColumns 解析）

### Phase 1 — 剖析实体建模 + 剖析器 + profileTable action
- **item 1.1 决策（硬前置门禁，Decision only）**：基于 live repo 核查裁定 D1/D2/D3。编写临时 Java 程序验证 H2 2.4.240 STDDEV_SAMP/STDDEV_POP/PERCENTILE_CONT/MEDIAN 均可用（推翻 plan 假设的"H2 无原生 PERCENTILE"），但 MySQL 无原生 percentile → 裁定 median/percentiles/distribution 用 in-app 排序计算（仅依赖可移植 `SELECT col ORDER BY col`），STDDEV_SAMP 用便携 SQL。建模=新建独立实体（剖析结果嵌套统计，避免硬塞 QualityResult 单 actualValue 列）。结论写入 `06-data-quality-extended.md` §三（3.0 决策/3.1-3.3，从 draft 重写为最终设计状态，收敛 Open Questions）+ `01-architecture-baseline.md`（新增 §2.7.2 剖析）。
- **item 1.2 实体落地**：`nop-metadata.orm.xml` 新增 NopMetaProfilingRule（profilingRuleId/ruleName/displayName/tableId/columns/json-4000/stats/json-4000/sampleSize/extConfig + 审计，to-one→NopMetaTable，索引 IX_NOP_META_PROFRULE_TABLE）+ NopMetaProfilingResult（profilingResultId/profilingRuleId(非mandatory)/metaTableId/snapshotTime/tableStats/mediumtext+json/columnStats/mediumtext+json + 审计，to-one→Rule+NopMetaTable，索引 IX_NOP_META_PROF_RESULT_RULE(profilingRuleId,snapshotTime) + IX_NOP_META_PROF_RESULT_TABLE）。`./mvnw clean install -pl nop-metadata -T 1C` BUILD SUCCESS，codegen 生成实体类/BizModel/IBiz/xmeta。
- **item 1.3 剖析器**：新增 `io.nop.metadata.service.profiling.MetaTableProfiler`（无状态）+ `ProfilingSnapshot`（表级结果 holder）+ `ProfilingColumnStats`（列级结果 holder）。输入 Connection+DatabaseMetaData+schemaPattern+tableName+columnsFilter+productName → COUNT(*) 表行数（便携）+ sizeBytes/lastModified null+unavailable（对齐 Catalog §2.3.2 降级）→ DatabaseMetaData.getColumns 运行时解析列名+JDBC类型（不依赖 buildSql JSON 同步）→ per-column try/catch（单列失败收集 errors 不中断整表）→ 类型适配（contains 关键字匹配 DOUBLE PRECISION/CHARACTER VARYING 等：数值列 numericStats，字符串列 stringStats）→ 便携聚合（COUNT DISTINCT/null/empty(emptyCount 类型感知：仅字符串列 WHERE col=''，数值列记 0)/MIN/MAX/AVG/STDDEV_SAMP/MIN(LENGTH)/MAX(LENGTH)/AVG(LENGTH)/topValues(GROUP BY...LIMIT N)）+ in-app（loadSortedDoubles → median/percentiles 线性插值/distribution 等宽 4 桶）。标识符白名单 `^[A-Za-z_][A-Za-z0-9_]*$` 防注入。
- **item 1.4-1.5 profileTable + executeProfilingRule + inline ErrorCode**：`NopMetaTableBizModel`：① `profileTable(@BizMutation, metaTableId, schemaPattern?, columns?, context)` → 加载 NopMetaTable（不存在抛 `metadata.profiling-table-not-found`，非 external 抛 `metadata.profiling-table-not-external`）→ 解析数据源（querySpace→NopMetaDataSource，不存在抛 `metadata.profiling-no-datasource`，DISABLED 抛 `metadata.profiling-datasource-disabled`）→ withConnection callback → 剖析器 → 追加 NopMetaProfilingResult（snapshotTime=now）→ 返回 `{profilingResultId, columnCount, unavailable, columnUnavailable, errors}`。`NopMetaProfilingRuleBizModel`：② `executeProfilingRule(@BizMutation, profilingRuleId, schemaPattern?, context)` → 加载规则（不存在抛 `metadata.profiling-rule-not-found`）→ 自包含物理解析（复用 ErrorCode）→ withConnection → 剖析器（规则 columns 作为列过滤）→ 追加结果（profilingRuleId 关联规则）。6 个 inline ErrorCode 按现有模式定义。

### 测试（`TestNopMetaTableBizModel` +10 用例，全绿）
- 数值列+字符串列真实统计：EXT_PROF(4 行 amount=[10,20,30,40] name=[aaa,bb,ccc,dddd]) → 断言 rowCount=4 真实、sizeBytes/lastModified unavailable（不伪造）、distinctCount=4、AMOUNT.numericStats mean=25/median=25(in-app)/stddev≈12.91(STDDEV_SAMP)/percentiles p50=25 p25=17.5、NAME.stringStats minLength=2/maxLength=4/avgLength=3/topValues 非空。
- 列类型适配：EXT_TYPE(nval INT, sval VARCHAR) → 数值列仅有 numericStats（无 stringStats 伪造），字符串列仅有 stringStats（无 numericStats 伪造）。
- 失败隔离：columns=NOPE_COL（标识符通过白名单但列不存在）→ 全列失败收集 errors，但表级 rowCount 仍真实写入（不中断）。
- 时序追加：重复剖析 2 行（snapshotTime 不同，sleep 1.1s 保证不同），不覆盖。
- executeProfilingRule：按规则执行写入结果（profilingRuleId 关联规则），rowCount 真实。
- 不可执行路径显式：表不存在/非 external/无数据源/DISABLED/非 jdbc 全部 hasError=true。
- **Anti-Hollow**：所有成功路径真实 H2 建连 + 真实物理表数据，断言 NopMetaProfilingResult 写入真实 distinctCount/mean/stddev/median，证明运行时确实通过 P2-1 withConnection callback 建连并对每列执行了聚合 SQL/in-app 排序，非空壳。

### 设计文档更新
- `06-data-quality-extended.md`：Status active；§三 重写为最终设计（3.0 D1/D2/D3 决策 + 3.1 Rule/3.2 Result/3.3 示例，去掉伪码改为 final form JSON）；Open Questions 收敛（剖析增量→已裁定 follow-up，§四/§五 标注为 follow-up 设计意图伪码非最终）。
- `01-architecture-baseline.md`：新增 §2.7.2 数据剖析（建模/执行范围/物理解析+机制/统计范围+降级/时序+失败隔离）。

### 偏差
- profileTable 入口的 profilingRuleId 改为非 mandatory（ORM 层），原 plan §3.1 标注 mandatory=true；原因：profileTable 主入口无规则直接剖析，需允许 profilingRuleId 为 null（已同步到设计文档 §3.2 + plan 1.1 裁定结论）。
- emptyCount 类型感知：数值列不执行 `WHERE col=''`（会 Data conversion error），记 0（数值列无空字符串语义）；已写入设计 §3.0 D2 列类型适配说明。

### 验证
- `./mvnw clean install -pl nop-metadata -T 1C`：BUILD SUCCESS（含测试）。`TestNopMetaTableBizModel` 10 tests、`TestNopMetaQualityRuleBizModel` 13 tests、`TestNopMetaDataSourceBizModel` 17 tests、`TestNopMetaLineageEdgeBizModel` 12 tests、`TestNopMetaModuleBizModel` 11 tests、`TestExternalTableStructureReader` 4 tests，nop-metadata 全模块 67 tests 全 0 failure。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings，退出码 0。
- `node ai-dev/tools/check-doc-links.mjs --strict`：0 errors，退出码 0。
- Doc-sync: `06-data-quality-extended.md` §三 + `01-architecture-baseline.md` §2.7.2 已更新。

---

## Plan 0530-1 完成（nop-metadata 质量规则执行引擎）

- Plan: `ai-dev/plans/2026-07-16-0530-1-nop-metadata-quality-rule-execution-engine.md` → `completed`
- Roadmap: P2-6 ❌(planned) → ✅(done)
- 收口设计决策 D1（执行范围：首版仅 external 表 + field 级 external `params.column` 约定 / 物理解析路径：entityId→NopMetaTable→querySpace→NopMetaDataSource→withConnection / schema 限定）/ D2（执行机制=BizModel action + withConnection，不选 nop-batch；落点 NopMetaQualityRuleBizModel）/ D3（7 类判定语义 + 标识符注入防护）

### Phase 1 — 执行引擎（executeQualityRule 单规则 + executeQualityRulesForDataSource 批量）
- **item 1.1 决策（硬前置门禁，Decision only）**：基于 live repo 核查裁定 D1/D2/D3。H2 已被 P2-1/P2-2/P2-4 测试证明可执行 COUNT(*)、MAX(timestamp)、GROUP BY...HAVING 子查询、IS NULL、PreparedStatement 参数；额外核查 H2 支持 `REGEXP` 操作符（`col NOT REGEXP ?` 可 PreparedStatement 参数化）。**裁定：7 类 ruleType 全部首版可执行**（table 级 volume/freshness/custom_sql + field 级 not_null/unique/range/regex，field 级按 `entityId=external NopMetaTable.metaTableId` + `params.column` 约定）。执行范围：external-only；entity/sql 表 deferred（与 Catalog entity/sql 收集同源）；entityType=database 首版 SKIP+details。执行机制：BizModel action + withConnection（与 collectCatalog/syncExternalTables 一致）。标识符（列名/表名/schema）白名单正则 `^[A-Za-z_][A-Za-z0-9_]*$` 校验防注入，比较值 PreparedStatement 参数。结论写入 `01-architecture-baseline.md` §2.7（含新增 §2.7.1）。顺带修正 §2.7 severity/status 大小写（设计文档原写小写 error/warning/info/pass/fail，统一为大写 INFO/WARNING/ERROR/PASS/FAIL/ERROR/SKIP，与 dict `meta/quality-severity`/`meta/quality-result-status` 一致）。顺带收敛 `06-data-quality-extended.md` §一/§二/Open Questions 为最终设计状态（去掉 mostly Python 伪码、去掉 §二 code-level 字段定义、Open Questions 标注扩展项 follow-up，满足 Rule 14）。
- **item 1.2 质量规则执行器**：新增 `io.nop.metadata.service.quality.MetaQualityRuleExecutor`（无状态，参考 `MetaCatalogCollector` 收集器模式）+ `QualityRuleJudgment`（结果 holder）。输入 Connection + schemaPattern + 规则字段（ruleType/entityType/paramsJson/sqlExpression/threshold/tableName/productName）→ 按 D3 生成检测 SQL（标识符白名单校验、比较值 PreparedStatement 参数）→ 执行 → 返回结构化判定（status/actualValue/expectedValue/message/details）。7 类判定分别实现：volume(COUNT)/freshness(MAX(ts)→ageMinutes)/custom_sql(执行用户 SQL，按 expectPassWhen 判定)/not_null(COUNT WHERE IS NULL)/unique(GROUP BY HAVING)/range(PreparedStatement min/max)/regex(REGEXP 参数化，方言不支持→SKIP)。entityType=database 在执行器入口 SKIP+details。执行器无目录依赖（纯 SQL→判定），物理解析在 BizModel 层。
- **item 1.3-1.5 executeQualityRule + executeQualityRulesForDataSource + inline ErrorCode**：`NopMetaQualityRuleBizModel`：① `executeQualityRule(@BizMutation, qualityRuleId, schemaPattern?)` → 加载规则（不存在抛 `metadata.quality-rule-not-found`）→ entityType=database 直接写 SKIP 结果行（不解析表/数据源）→ 解析目标表（entityId→NopMetaTable，不存在抛 `metadata.quality-table-not-found`，非 external 抛 `metadata.quality-table-not-external`）→ 解析数据源（querySpace→NopMetaDataSource，不存在抛 `metadata.quality-no-datasource`，DISABLED 抛 `metadata.quality-datasource-disabled`）→ withConnection callback → 执行器判定 → 追加一行 NopMetaQualityResult（executeTime=now）→ 返回结果 Map。② `executeQualityRulesForDataSource(@BizMutation, dataSourceId, schemaPattern?)` → 加载数据源（不存在/DISABLED 显式失败）→ 找 querySpace external 表 → 找挂载规则（entityId∈tableIds）→ withConnection callback 内 per-rule try/catch + flushSession/clearSession 隔离 → 写 NopMetaQualityResult → 返回 `{executedCount, results, errors}`。6 个 inline ErrorCode 按现有模式定义（参考 NopMetaDataSourceBizModel/NopMetaLineageEdgeBizModel）。

### 测试（`TestNopMetaQualityRuleBizModel` +13 用例，全绿）
- 7 类 ruleType pass+fail：volume(3 行 minRows=2 PASS / minRows=10 FAIL) / freshness(now-60min maxAge=120 PASS / maxAge=30 FAIL / 缺 timestampColumn ERROR) / custom_sql(COUNT=3 expectPassWhen=gt 0 PASS / eq 0 FAIL / 不返回单值 ERROR) / not_null+unique+range+regex（clean 表 EXT_FLD_OK 全 PASS + dirty 表 EXT_FLD_BAD 全 FAIL，断言真实 actualValue 如 nullCount=1/dupGroups=1/outOfRange=1/notMatching=1）。
- 批量：executeQualityRulesForDataSource executedCount=2 两条结果写入；单规则失败隔离（not_null 引用不存在列 → 执行器抛 NopException → 进 errors 不中断整批，好规则仍写 PASS 结果，坏规则不写结果行）。
- 时序追加：重复执行同一规则 2 行（executeTime 不同），不覆盖。
- 不可执行路径显式：规则不存在 / 目标表非 external / 无注册数据源 / DISABLED / 非 jdbc 全部 hasError=true；entityType=database → SKIP 结果行（hasError=false，status=SKIP，结果行写入）；缺 timestampColumn / custom_sql 不返回单值 → ERROR 结果行。
- **Anti-Hollow**：所有成功路径真实 H2 建连 + 真实物理表数据，断言 NopMetaQualityResult.actualValue 为真实检测结果（rowCount=3/nullCount=1/dupGroups=1 等），证明运行时确实通过 P2-1 withConnection callback 建连并执行了检测 SQL，非空壳。

### 设计文档更新
- `01-architecture-baseline.md`：§2.7 重写为最终设计状态（severity/status 大写修正 + 新增 §2.7.1 执行引擎：执行范围/物理解析路径/field 列引用约定/schema 限定/执行机制/action 落点/7 类判定语义表/标识符注入防护）。
- `06-data-quality-extended.md`：Status draft→active；§一/§二 去伪码、去 code-level 字段定义、基线 7 类引用 §2.7、扩展 ruleType 与 mostly 标注 follow-up；Open Questions 收敛为扩展项未决问题。

### 验证
- `./mvnw clean install -pl nop-metadata -T 1C`：BUILD SUCCESS（含测试）。`TestNopMetaQualityRuleBizModel` 13 tests、`TestNopMetaDataSourceBizModel` 17 tests、`TestNopMetaLineageEdgeBizModel` 12 tests、`TestNopMetaModuleBizModel` 11 tests、`TestExternalTableStructureReader` 4 tests，nop-metadata 全模块 57 tests 全 0 failure。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings，退出码 0。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：退出码 0。

---

## Plan 0420-2 完成（nop-metadata 血缘采集 + 图遍历查询）

- Plan: `ai-dev/plans/2026-07-16-0420-2-nop-metadata-lineage-collection-and-traversal.md` → `completed`
- Roadmap: P2-5 ❌(planned) → ✅(done)
- 收口设计决策 D1（sql_parse 表级范围 + 解析器选型）/ D2（lineageSource 支持范围 + external 列级软引用 + dangling 策略）/ D3（图遍历语义）

### Phase 1 — 血缘填充机制（recordLineage manual + extractLineageFromSql 表级）
- **item 1.1 决策（硬前置门禁，Decision only）**：基于 live repo 核查裁定 D1/D2。**解析器选型关键修正**：计划 Current Baseline 称"nop-orm-eql 未依赖、不在 classpath"，实际核查 `nop-orm/pom.xml:24` 直接依赖 `nop-orm-eql`，而 `nop-metadata-dao`→`nop-orm`，故 `nop-orm-eql` 的 `EqlASTParser` **已传递可用**于 `nop-metadata-service`（无需新增 pom，无循环依赖——`nop-orm-eql` 仅依赖 nop-orm-model/nop-dao/nop-core）。`EqlASTParser.parseFromText(text)` 做**纯语法 AST 解析**（不绑定 ORM session——session 绑定的 `resolvedTableMeta` 是独立 compile 阶段，此处不调用，仅取 `SqlTableName.getName()/getFullName()`）。裁定：sql_parse 仅表级；external 列级软引用（列名字符串，不引实体）；dangling 一律进 unresolved（`sourceTableId` mandatory 无法建悬空边）。结论写入 `01-architecture-baseline.md` §2.6.1/§2.6.2。
- **item 1.2 SQL 源表抽取器**：新增 `io.nop.metadata.service.lineage.SqlSourceTableExtractor`（无状态、无目录依赖）+ `TableReference`（fullName + simpleName）：`EqlASTParser` 解析 → 递归 `forEachChild` 遍历整棵 AST，收集所有 `SqlSingleTableSource.tableName`（含子查询/CTE/JOIN 内层），按 simpleName 去重。SQL 空或解析失败抛 inline `ErrorCode`（不静默返回空列表）。
- **item 1.3-1.5 recordLineage + extractLineageFromSql + inline ErrorCode**：`NopMetaLineageEdgeBizModel`：① `recordLineage(@BizMutation, edges: List<Map>)` → 解析多条边（表级+列级，lineageSource 缺省 manual）→ 批量存在性校验（`FilterBeans.in` 一次查所有引用表 ID，不存在抛 `metadata.lineage-table-not-found`，不静默建悬空边）→ 原子保存全部 → `{recordedEdgeCount}`。② `extractLineageFromSql(@BizMutation, metaTableId)` → 加载 tableType=sql 表（非 sql 抛 `metadata.lineage-not-sql-table`）→ 抽取器解析 sourceSql（失败进 errors）→ `buildTableNameIndex()` 大小写不敏感匹配目录 tableName → 按 `(sourceTableId, targetTableId, lineageSource='sql_parse')` 幂等 upsert 表级边（target=自身，列空）→ 未匹配进 unresolved（不丢）→ `{extractedEdgeCount, unresolved, errors}`。

### Phase 2 — 图遍历查询（向上追溯 / 向下追踪 / 路径查找 / 影响分析）
- **item 2.1-2.4**：`NopMetaLineageEdgeBizModel` 新增四个 `@BizQuery`：`buildLineageGraph()` 一次性 `dao().findAll()` 全量边在内存建 forward/reverse/columnForward 邻接表（架构基线 §2.6.2 策略）。① `getUpstream(id)` 反向 BFS（target→source）+ visited 环检测。② `getDownstream(id)` 正向 BFS（source→target）+ visited。③ `getLineagePath(s,t)` BFS 最短路径（前驱 map 重建）+ visited，无路径返回显式空（不报错）。④ `getImpactAnalysis(id, col?)` 全表级下游 BFS；col 提供时仅沿列级边（sourceColumn/targetColumn==col）过滤 BFS，结果空则回退表级（不静默返回空）。

### 测试（`TestNopMetaLineageEdgeBizModel` +12 用例，全绿）
- recordLineage：表级+列级录入可查（lineageSource 缺省 manual、confidence/transformType 透传）/ 不存在 ID 显式失败 / 空 edges 显式失败。
- extractLineageFromSql：端到端解析 `FROM ORDERS LEFT JOIN CUSTOMERS` → extractedEdgeCount=2 + 两条表级边可查（**接线验证**：真实解析 FROM/JOIN 写入边，非空壳）/ 幂等（重抽不追加）/ unresolved（GHOST_TBL 进 unresolved 不丢不建悬空边）/ 非 sql 类型显式失败 / 不存在 ID 显式失败。
- 遍历：A→B→C 链 getDownstream(A)含 B,C + getUpstream(C)含 A,B / getLineagePath(A,C)==[A,B,C] + 无路径显式空 / 环图 A↔B 不死循环（visited）/ 影响分析 col=x→[T2] 按列过滤、null→[T2,T3] 表级、无该列边回退表级（不静默空）。GraphQL query 暴露验证穿插。

### 偏差
- 计划 Current Baseline 关于 nop-orm-eql 依赖的描述与 live repo 不符（实际已传递可用），在 §2.6.1 决策记录中据实修正并保留"无循环依赖"裁定。
- 解析器未采用 hand-rolled tokenizer，直接复用 `nop-orm-eql` AST（更稳健：正确处理 JOIN/子查询/UNION），限制（不展开 CTE/子查询别名）在设计文档显式记录。

### 设计文档更新
- `01-architecture-baseline.md`：§2.6 新增 §2.6.1 血缘采集（sql_parse 范围/解析器/依赖裁定/限制 + lineageSource 支持范围 + external 列级软引用 + dangling 策略）+ §2.6.2 图遍历语义（四类遍历 + 环检测 + BFS 内存建图策略）。

### 验证
- `./mvnw clean install -pl nop-metadata -T 1C`：BUILD SUCCESS（含测试）。`TestNopMetaLineageEdgeBizModel` 12 tests、`TestNopMetaDataSourceBizModel` 17 tests、`TestNopMetaModuleBizModel` 11 tests、`TestExternalTableStructureReader` 4 tests，全 0 failure。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings。
- `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict`：退出码 0。

---

## Plan 0420-1 完成（nop-metadata Catalog 运行时元数据收集）

- Plan: `ai-dev/plans/2026-07-16-0420-1-nop-metadata-catalog-runtime-collection.md` → `completed`
- Roadmap: P2-4 ❌(planned) → ✅(done)
- 收口设计决策 D1（存储形态：方案 A per-table 结构化行 / 收集范围：首版仅 external / 降级策略：不可用统计 null + details.unavailable 标记 / schema 限定：schemaPattern 限定物理 SQL，null 依赖默认 schema）/ D2（collectCatalog 落点 NopMetaDataSourceBizModel，复用 withConnection callback）

### Phase 1 — NopMetaCatalog 建模 + collectCatalog 收集 action
- **item 1.1 决策（硬前置门禁，Decision only）**：基于 live repo 核查 + H2 能力实测（编 `VerifyH2CatalogCapabilities` 临时探针连真实 H2）裁定——COUNT ✓ / getIndexInfo ✓（distinct 索引名，排除 tableIndexStatistic）/ sizeBytes·partitionCount·lastModified 在 H2 不可用（H2 `LAST_MODIFICATION` 是 BIGINT 计数器非时间戳）→ null + `details.unavailable` 显式标记。存储形态方案 A，收集范围首版仅 external，schema 限定策略（schemaPattern 限定 COUNT/索引物理 SQL；多 schema 为已知限制）。**重写 `05-metadata-import.md` §四**（从"不在本文档展开"收敛为最终设计状态，含 4.1~4.6 全规格，满足 Rule 14）；`01-architecture-baseline.md` 新增 §2.3.2 NopMetaCatalog 实体说明。
- **item 1.2 实体建模（D1 方案 A）**：`nop-metadata.orm.xml` 新增 `NopMetaCatalog`（metaCatalogId PK / metaTableId→NopMetaTable / rowCount(long,mandatory) / sizeBytes(long,nullable) / indexCount(int,nullable) / partitionCount(int,nullable) / lastModified(timestamp,nullable) / **details(domain="mediumtext", stdDomain="json"，未用 json-4000)** / collectedAt(timestamp,mandatory) + 审计）+ to-one metaTable + IX_NOP_META_CATALOG_TABLE(metaTableId, collectedAt)。**偏差**：`stdDataType` 初次误用 `integer`（xdef 不接受，仅允许 `int`），已修正为 `int`。`./mvnw clean install -pl nop-metadata -T 1C` BUILD SUCCESS，_gen 生成 NopMetaCatalog/_NopMetaCatalog（PROP_NAME_* 齐全 + metaTable relation + detailsComponent）。
- **item 1.3 收集器**：新增 `io.nop.metadata.service.catalog.MetaCatalogCollector`（无状态，在 withConnection callback 内调用，不自建连接）：`collectForTable(conn, metaData, schemaPattern, tableName, productName)` → 便携 `SELECT COUNT(*) FROM <schema?.><table>`（rowCount）+ 标准 `getIndexInfo` distinct 索引名排除 STATISTIC（indexCount）；大小/分区/lastModified 首版不实现 → null + 标记 unavailable。配套 `CatalogTableStats` bean（承载统计 + unavailable 列表 + extras）。单表 SQL 异常抛出交 BizModel per-table 隔离。
- **item 1.4 + 1.5 action + 错误码**：`NopMetaDataSourceBizModel.collectCatalog(@BizMutation, dataSourceId, schemaPattern?, context)`（mutation 名 `NopMetaDataSource__collectCatalog`，落点与 testConnection/syncExternalTables 同 BizModel）→ 加载 NopMetaDataSource → 不存在抛 `metadata.datasource-not-found`（不 NPE）→ DISABLED 抛 `metadata.datasource-disabled`（不静默通过）→ 复用 `withConnection` callback 建连 → callback 内 `findExternalTables(querySpace, tableType=external)`（首版全表扫描可接受）→ 收集器逐表收集 → **每表追加一行 NopMetaCatalog（collectedAt=now，时序不覆盖）**→ 单表失败收集 errors + `orm().clearSession()` 隔离不中断整批 → 返回 `Map{collectedCount, errors}`。**错误码复用** BizModel 内既有 inline `ERR_DATASOURCE_NOT_FOUND`/`ERR_DATASOURCE_DISABLED`（D2 仅要求这两个，不新增）。

### 测试（`TestNopMetaDataSourceBizModel` +7 用例，全绿）
- `testCollectCatalogWritesRealRowCount`：端到端 sync→collect→NopMetaCatalog__findPage，断言 **真实 rowCount==5**（EXT_DEPT 插 5 行，证明运行时确实通过 withConnection callback 建连并执行 COUNT，非空壳）+ indexCount>=1（ix_dept）+ **降级**：sizeBytes/partitionCount/lastModified null + details.unavailable 显式列出三者 + details 含 `databaseProductName=H2`。
- `testCollectCatalogAppendsWithSchemaPattern`：**时序语义**——第二次 collect 后 countCatalogRows==2（追加新行，不覆盖）。
- `testCollectCatalogDefaultSchemaNoPattern`：**schema 限定**——不传 schemaPattern，COUNT 用 `<tableName>` 依赖连接默认 schema（H2 PUBLIC），rowCount==2。
- `testCollectCatalogSingleTableErrorBatched`：**单表失败不中断整批**——手工注入指向不存在物理表的 EXT_GONE 目录行，断言 errors 含 EXT_GONE + collectedCount==1（EXT_OK 仍写入真实 rowCount==4）+ EXT_GONE 无 catalog 行（COUNT 失败未伪造）。
- `testCollectCatalogDisabledRejected` / `testCollectCatalogNonJdbcThrows` / `testCollectCatalogNotFound`：失败路径显式失败（不静默跳过）。

### 偏差
- `stdDataType="integer"` 不在 xdef 白名单（允许 `int`），首版建表报 `illegal-prop-value-for-std-domain`，修正为 `int` 后 BUILD SUCCESS。
- indexCount 统计口径：`getIndexInfo` 返回每索引列一行 + H2/MySQL 会返回 `tableIndexStatistic`(TYPE=0) 表级统计行；收集器排除 STATISTIC 行并按 distinct INDEX_NAME 计数，避免多列索引/统计行虚高。

### 设计文档更新
- `05-metadata-import.md` §四：从"不在本文档展开"重写为最终设计（4.1 存储形态 / 4.2 实体 / 4.3 收集范围 / 4.4 统计获取与降级表 / 4.5 schema 限定 / 4.6 action 契约）。
- `01-architecture-baseline.md`：新增 §2.3.2 Catalog 运行时元数据快照（实体/降级/schema 限定全规格）。
- `nop-metadata-roadmap.md`：P2-4 planned → done；未建模实体表 MetaCatalog 标注已建模。

### 验证
- `./mvnw clean install -pl nop-metadata -T 1C`：BUILD SUCCESS。`TestNopMetaDataSourceBizModel` 17 tests（含新增 7）、`TestNopMetaModuleBizModel` 11 tests、`TestExternalTableStructureReader` 4 tests，全 0 failure。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings。
- `node ai-dev/tools/check-doc-links.mjs --strict`：0 errors。

---

## Plan 0225-3 完成（nop-metadata Manifest 元数据快照）

- Plan: `ai-dev/plans/2026-07-16-0225-3-nop-metadata-manifest-snapshot.md` → `completed`
- Roadmap: P2-3 ❌(todo) → ✅(done)
- 收口设计决策 D1（快照粒度：每模块版本一条）/ D2（存储形态：JSON CLOB 单行）/ D3（依赖图首版仅 entity→entity，来源 MetaEntityRelation）/ D4（节点 uniqueId 与边 resolution 规格）

### Phase 1 — NopMetaManifest 实体 + 构建服务 + generateManifest action
- **item 1.1 决策（硬前置门禁，Decision）**：按 D1 裁定每模块版本一条 Manifest；按 D4 裁定 uniqueId = `entity.<moduleId 归一化 slash→dot>.<简单类名>`（简单类名取 className 最后一段，因 live 数据 entityName 列存的是 full className，直接拼会重复）+ relation→边 resolution（refEntityName=className → 全局反查 NopMetaEntity(className) → 其模块 → uniqueId）+ dangling 策略（`unresolved:<className>`，不丢边）。**重写 `05-metadata-import.md` 从 draft → final**（去 Python 伪码、收敛 Open Questions、补 D1/D2/D4 最终规格 + 边方向语义，满足 Rule 14）；`01-architecture-baseline.md` 新增 §2.3.1 NopMetaManifest 实体说明。
- **item 1.2 实体建模（D2 JSON CLOB）**：`nop-metadata.orm.xml` 新增 `NopMetaManifest`（manifestId PK / metaModuleId→NopMetaModule / manifestVersion / generatedAt / nopMetadataVersion / **content(domain="mediumtext", stdDomain="json"，未用 json-4000)** + 审计）+ to-one metaModule + IX_NOP_META_MANIFEST_MODULE。`./mvnw clean install -pl nop-metadata -T 1C` BUILD SUCCESS，_gen 生成 NopMetaManifest/_NopMetaManifest（PROP_NAME_* 齐全）。
- **item 1.3-1.5 构建服务**：新增 `io.nop.metadata.service.manifest.MetaManifestBuilder`（纯转换逻辑，不直接访问 DB）：输入 module + full ORM 模型(isDelta=false)下的实体/关系 + 全局 className→moduleId 反查索引 → 构建 nodes（首版仅 entity 节点）+ sources（querySpace 维度）+ parentMap/childMap（MetaEntityRelation→entity→entity 边，按 D4 resolution）。无关系节点显式空数组（不静默跳过）；跨模块/未导入引用记 `unresolved:<className>` 并计 unresolvedCount（不丢边）。
- **item 1.6 action**：`NopMetaModuleBizModel.generateManifest(@BizMutation, metaModuleId)` → NopMetaManifest：加载模块→查 full ORM 模型→查实体/关系→构建全局 className→moduleId 索引（全量 full 实体，首版可接受）→builder.build→`JsonTool.stringify` 写 content→saveEntity。**快速失败**：moduleId 不存在抛 `metadata.module-not-found`；无 full 模型抛 `metadata.module-full-model-not-found`（均不静默返回空快照）。manifestVersion 同模块版本下重新生成递增。

### 测试（`TestNopMetaModuleBizModel` +2 用例，全绿）
- `testGenerateManifest`：端到端 导入 nop-metadata orm.xml → generateManifest → 解析 content JSON 断言：① nodes 非空且含 `entity.nop.metadata.NopMetaModule`；② **钉死预期边** NopMetaOrmModel.metaModule to-one → NopMetaModule：parentMap[entity.nop.metadata.NopMetaOrmModel] 含 entity.nop.metadata.NopMetaModule + childMap 反向含；③ 每个节点在 parentMap/childMap 都有条目（无静默跳过）+ 至少一个空 parentMap（relation-less 节点显式空数组）；④ 边目标端要么 `entity.` 要么 `unresolved:`（格式约定）。**接线验证**：断言特定 entity→entity 边存在证明运行时确实查询了 MetaEntityRelation 并写入 parentMap/childMap，非空壳。
- `testGenerateManifestModuleNotFound`：缺失 moduleId 快速失败（GraphQL error，不静默返回空）。

### 偏差
- uniqueId 的 `<name>` 段：计划 D4 写"取 NopMetaEntity.entityName"，但 live 数据 entityName 列存的是 full className（`OrmModelImporter.buildEntity` 写 `em.getName()`），直接拼入会产生 `entity.nop.metadata.io.nop.metadata.dao.entity.NopMetaModule`（className 重复）。改为取 className 的简单类名（最后一段），与 D4/§3.2/Exit Criteria 所有示例（`entity.nop.auth.NopMetaUser`）一致。已在 `05-metadata-import.md` §5.3 最终规格中记录此裁定。
- `metadata.generatedAt`：严格 JSON 序列化（`JsonTool.stringify`）不接受裸 `java.util.Date`（`only-data-bean-is-serializable`），builder 内格式化为 ISO-8601 字符串再放入 Map。

### 设计文档更新
- `05-metadata-import.md`：draft → final，重写 §三/§五（D1/D2/D3/D4 最终规格），去 Python 伪码，Open Questions 逐条裁定。
- `01-architecture-baseline.md`：新增 §2.3.1 NopMetaManifest 实体说明。
- `nop-metadata-roadmap.md`：P2-3 todo → done；未建模实体表 MetaManifest 标注已建模。

### 验证
- `./mvnw clean install -pl nop-metadata -T 1C`：BUILD SUCCESS。`TestNopMetaModuleBizModel` 11 tests（含新增 2）、`TestNopMetaDataSourceBizModel` 10 tests、`TestExternalTableStructureReader` 4 tests，全 0 failure。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings。
- `node ai-dev/tools/check-doc-links.mjs --strict`：退出码 0。
- 独立 closure-audit 子 agent（fresh session）见下方 Closure 段。

## Plan 0225-2 完成（nop-metadata 外部表元数据同步）

- Plan: `ai-dev/plans/2026-07-16-0225-2-nop-metadata-external-table-metadata-sync.md` → `completed`
- Roadmap: P2-2 ❌(todo) → ✅(done)
- 收口设计决策 D1（外部表建模归属）+ D2（syncExternalTables 契约）

### Phase 1 — 建模决策 + 外部表同步 action
- **item 1.1a 建模决策（硬前置门禁，Decision only）**：基于 live repo 核查裁定 **方案 A + 子方案 A2**——`NopMetaTable.metaModuleId` mandatory（orm.xml:968）→ 方案 C 拒绝；`NopMetaEntity.ormModelId` mandatory（orm.xml:367）+ `NopMetaEntityField.metaEntityId` mandatory → 方案 B（合成 MetaEntity 链）成本高，拒绝；`meta/table-type` dict 仅 `<option>` 列表可自由加值。选定：引入 `tableType="external"` + 系统模块 `nop/meta-external`（status=RELEASED）作为外部表归属；外部表字段序列化为 JSON 存入 `buildSql`（A2，复用 mediumtext，外部表无"合成 SQL"语义）。**子方案 A1（新建字段存储实体）拒绝**——属 ORM 模型结构变更（Protected Area）且字段级寻址属 P2-5/P2-6 范围。本决策不引入任何新 ORM 实体/列，仅加 dict 枚举值 + 运行时初始化系统模块行，**不属于 Protected Area 结构变更**。
- **item 1.1b 落地**：`nop-metadata.orm.xml` `meta/table-type` dict 新增 `<option code="EXTERNAL" value="external"/>`；codegen 生成 `_NopMetadataCoreConstants.TABLE_TYPE_EXTERNAL="external"`（core:64）。系统模块由 `ensureExternalSystemModule()` 惰性创建（moduleId 查找→缺失则建，status=RELEASED）。
- **item 1.2 方言范围**：首版支持 MySQL/PostgreSQL/H2（H2 服务 AutoTest 真实建连路径）；结构读取走标准 JDBC `DatabaseMetaData.getTables()/getColumns()`（跨方言可移植，等价 information_schema.COLUMNS）。其余方言（ClickHouse system.columns、Oracle 等）显式抛 `UnsupportedOperationException`，多方言全覆盖为 follow-up。
- **item 1.3**：新增 `io.nop.metadata.service.sync.ExternalTableStructureReader`（+ DTO `ExternalTableInfo`/`ExternalColumnInfo`）：callback 内调用，`getTables(null,schema,"%",["TABLE","VIEW"])` + 逐表 `getColumns()`；方言白名单门禁 `requireSupportedProductName`。
- **item 1.4**：`NopMetaDataSourceBizModel.syncExternalTables(@BizMutation, dataSourceId, @Optional schemaPattern)` → `{syncedTableCount, errors}`：复用 P2-1 `withConnection` callback 建连（不自建）→ callback 内 `read()` + upsert。幂等 upsert 按 `(metaModuleId, tableName)` 复合键（`findFirstByQuery` + save/update）。单表失败 `orm().flushSession()`/`clearSession()` 隔离 + 收集 errors 不中断。
- **item 1.5**：DISABLED→`metadata.datasource-disabled`；非 jdbc→连接服务 `UnsupportedOperationException`；不支持方言→读取器 `UnsupportedOperationException`；不存在→`metadata.datasource-not-found`（不 NPE）。四条路径显式失败，无静默跳过。

### 测试（`TestNopMetaDataSourceBizModel` +5 用例，`sync/TestExternalTableStructureReader` +4 用例，全绿）
- `testSyncExternalTablesWritesStructure`：真实 H2 in-mem 库建 ext_dept 表 → GraphQL mutation → 断言 `syncedTableCount=1` + MetaTable(tableType=external, querySpace 跟随数据源) + buildSql 含 DEPT_ID/DEPT_NAME/DECIMAL/`"nullable":false` + 系统模块已建（**anti-hollow**：真实列名/类型证明运行时确实执行 getTables/getColumns）。
- `testSyncExternalTablesIdempotent`：同步两次 → EXT_EMP 记录数稳定为 1（复合键 upsert，不追加）。
- `testSyncExternalTablesDisabledRejected` / `testSyncExternalTablesNonJdbcThrows` / `testSyncExternalTablesNotFound`：三条非法路径 GraphQL error。
- `TestExternalTableStructureReader`（同包，直访读取器门禁）：MySQL/PostgreSQL/H2 支持；Oracle/ClickHouse/MSSQL 拒绝；不支持/null 方言抛 `UnsupportedOperationException`（无真实非 H2 库亦可覆盖"不支持方言显式失败"路径）。

### 设计文档更新
- `01-architecture-baseline.md` §2.5：tableType 表新增 `external` 行；§2.5.1：外部表建模决策 D1（方案 A+A2，含拒绝 B/C/A1 的理由）+ syncExternalTables 契约 D2 + 系统模块 nop/meta-external + 方言范围。
- `nop-metadata-roadmap.md` P2-2：todo → done。

### 验证
- `./mvnw clean install -pl nop-metadata -am -T 1C`：BUILD SUCCESS。`TestNopMetaDataSourceBizModel` 10 tests（5 testConnection + 5 syncExternalTables）、`TestExternalTableStructureReader` 4 tests、`TestNopMetaModuleBizModel` 9 tests（entity/sql 回归未破坏），全 0 failure。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings（含 high）。
- 独立 closure-audit 子 agent（ses_098b3db01fferLoFmWm51KyFRB，fresh session）：9 项 Exit Criteria + 4 维度全 PASS，Anti-Hollow 通过（追踪 syncExternalTables→withConnection→in-callback read(getTables/getColumns)→upsert 全链连通），CLOSURE_APPROVED。

## Plan 0225-1 完成（nop-metadata 外部数据源注册 + 连接验证）

- Plan: `ai-dev/plans/2026-07-16-0225-1-nop-metadata-datasource-registration-and-connection-verification.md` → `completed`
- Roadmap: P2-1 ❌(todo) → ✅(done)
- 收口设计决策 D1（testConnection 契约）/ D2（connectionConfig jdbc 必填字段）/ D3（方言不持久化）

### Phase 1 — 连接服务 + testConnection action（jdbc）
- **item 1.1 API 研究结论**（硬前置门禁）：连接 API 选定平台 `io.nop.dao.jdbc.datasource.SimpleDataSource`（`nop-dao`，`getConnection` 内部走 `DriverManager.getConnection(url, mergedProps)`，`setDriverClassName` 做 `Class.forName`）——平台既有约定，CLI `CliReverseDbCommand`、batch `JdbcConnectionConfig`、dbtool `JdbcMetaDiscovery` 均此模式；不引入 raw `DriverManager`。连接服务**落点 service 层**（`nop-metadata-core` 仅常量），package `io.nop.metadata.service.connection`，提供 **callback 式** `withConnection(type, config, BiConsumer<Connection, DatabaseMetaData>)` 同时服务 P2-2/P2-4/P2-6。Bean 注册在源 retention 文件 `_vfs/nop/metadata/beans/app-service.beans.xml`（非生成的 `_service.beans.xml`），BizModel `@Inject protected` 注入（NopIoC 不支持 private 注入）。AutoTest 连通目标：真实 H2 内存库（H2 + `org.h2.Driver` 在 test classpath）构造 connectionConfig 验证真实建连 + DatabaseMetaData。
- **item 1.2**：`IMetaDataSourceConnectionService` + `MetaDataSourceConnectionService`：`withConnection` callback（建连→action→finally `IoHelper.safeCloseObject` 关闭，SQLException 抛 `metadata.datasource-connect-failed`）；`testConnect` 一次性读 DatabaseMetaData 返回 Map；jdbc 解析 jdbcUrl/username/password（可选 driverClassName）；非 jdbc 抛 `UnsupportedOperationException`；缺必填字段抛 inline `metadata.datasource-config-invalid`。password 允许空串（H2 默认空密码），按 key 存在校验，jdbcUrl/username 非空校验。
- **item 1.3**：`NopMetaDataSourceBizModel.testConnection(@BizMutation @Name("dataSourceId") String, IServiceContext)` → `Map<String,Object>`：`dao().getEntityById` → null 抛 `metadata.datasource-not-found`（不 NPE）→ `status==DISABLED` 抛 `metadata.datasource-disabled` → 调 `connectionService.testConnect`。建连失败 catch 返回 `{connected:false,error}`（D1）。
- **item 1.4**：成功 Map 含 `databaseProductName/databaseProductVersion/driverName/driverVersion`，**不写任何 ORM 列**（D3）。
- **item 1.5**：inline `ErrorCode.define` 定义在 BizModel（`metadata.datasource-not-found`/`-disabled`）+ 连接服务（`metadata.datasource-type-not-supported`/`-config-invalid`/`-connect-failed`），参考 `NopMetaModuleBizModel` 用法。

### 测试（`TestNopMetaDataSourceBizModel`，5 用例全绿）
- `testConnectionSuccessRealConnect`：真实 H2 建连，断言 `connected=true` + `databaseProductName=H2`（**anti-hollow**：真实 productName 证明运行时确实读取 DatabaseMetaData）。
- `testConnectionNotFound` / `testConnectionDisabledRejected` / `testConnectionNonJdbcThrows` / `testConnectionConfigMissingFastFail`：四条非法路径均 GraphQL error，无静默通过。

### 设计文档更新
- `01-architecture-baseline.md` §2.2.1：新增 testConnection 契约（D1）+ connectionConfig jdbc 必填字段约定（D2）+ 按需建连（SimpleDataSource，不注册 ORM 路由）+ 方言不持久化（D3）。
- `nop-metadata-roadmap.md` P2-1：todo → done。

### 验证
- `./mvnw clean install -pl nop-metadata -am -T 1C`：BUILD SUCCESS，TestNopMetaDataSourceBizModel 5 tests pass，既有 TestNopMetaModuleBizModel 9 tests 仍绿。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings。
- `node ai-dev/tools/check-plan-checklist.mjs ... --strict`：exit 0。

## Plan 295 完成（nop-metadata Delta 展开 + 版本发布）

- Plan: `ai-dev/plans/295-nop-metadata-delta-expansion-and-version-release.md` → `completed`
- 收口 plan 292 Deferred「isDelta=false full 展开」+ plan 293 Deferred「G4 baseModuleId to-one」+「G7-code version 硬编码」

### Phase 1 — baseModuleId 自引用 to-one 关系补全
- `nop-metadata/model/nop-metadata.orm.xml` NopMetaModule 新增 `<relations>` 块：`baseModule` to-one 自引用（join `baseModuleId` → `metaModuleId`）。
- 重新生成 `_NopMetaModule.java:1177` 含 `getBaseModule()` / `setBaseModule()`。

### Phase 2 — releaseModule 版本发布 action + version 递增
- `OrmModelImporter.buildModule(IOrmModel, long moduleVersion)`：移除硬编码 `setModuleVersion(1L)`，version 由参数传入。
- `NopMetaModuleBizModel.computeNextModuleVersion(ormModel)`：查询同 moduleId 的 max(moduleVersion)+1（`dao().findFirstByQuery` order by moduleVersion desc）。
- `NopMetaModuleBizModel.releaseModule(@BizMutation)`：加载模块（不存在抛 `metadata.module-not-found`）→ 校验 status==DRAFTING（非 drafting 抛 `metadata.module-not-drafting`）→ 设 RELEASED → `dao().updateEntity`。已 RELEASED 再调抛异常（不可变，无静默跳过）。
- 新增测试：`testReleaseModuleStatusTransition`（DRAFTING→RELEASED, version=1, 接线验证查询 status=RELEASED）、`testReleaseModuleImmutableAlreadyReleased`（re-release 抛异常）、`testVersionIncrementOnReimport`（v1→v2 import 时递增）。

### Phase 3 — Delta full 展开（isDelta 双重存储）
- API 研究（item 3.1）：选定**方案 A** — `DslNodeLoader.INSTANCE.loadDslNodeFromResource(resource, schema, ResolvePhase.filtered)` 获取 x:extends 未合并的 XNode + `SchemaLoader.loadXDefinition` + `new DslModelParser(schema).disableInit(true).parseWithXDef(xdef, deltaNode)` 解析为 OrmModel。标准 API 可用，未触发降级。
- `OrmModelImporter`：所有 build 方法（buildOrmModel/buildEntity/buildField/buildRelation/buildUniqueKey/buildIndex/buildDomain/buildDict）接受 `boolean isDelta` 参数，使用 `b(isDelta)` 替代硬编码 `b(true)`。
- `NopMetaModuleBizModel.importOrmModel` 重构为双解析流程：`persistModelGraph(deltaModel, isDelta=true)` + `persistModelGraph(fullModel, isDelta=false)`，1 module → 2 ormModels → 各自子实体集。`parseDeltaModel` 无 x:extends 时复用 full；有 x:extends 时 filtered 解析；解析失败 catch+LOG.warn 降级到 full。
- `resolveBaseModuleId(sourceContent, resource)`：从 x:extends 推导 base moduleId，base 未导入时返回 null（不阻塞）。
- 创建测试 fixture `_vfs/test/orm/simple.orm.xml`（无 x:extends，2 实体 TestSimpleA/TestSimpleB）。
- 新增测试 `testDeltaDualStorageNoExtends`：导入后 NopMetaOrmModel total=2（isDelta=1 + isDelta=0），NopMetaEntity total=4（2×2）。

### 设计文档更新
- `03-version-management.md`：§1.3 version import 时递增（修正 release 时递增）；§4.2 检查 MetaModule.status（修正 MetaOrmModel.status）；§4.4 新增 releaseModule action 契约。
- `01-architecture-baseline.md` §4.1：双解析流程描述（DslNodeLoader filtered + OrmModelLoader），双重存储结构，降级路径，baseModuleId 推导。

### 验证
- `./mvnw clean install -pl nop-metadata -T 1C`：BUILD SUCCESS，TestNopMetaModuleBizModel 9 tests pass。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：0 findings。
- `node ai-dev/tools/check-plan-checklist.mjs ... --strict`：exit 0。
- 独立 closure-audit 子 agent（ses_099032442ffeOEMT9PYnysmKmD）：6 领域全 PASS，Anti-Hollow 通过。

## Plan 294 完成（nop-metadata 导入引擎完整性补全）

- Plan: `ai-dev/plans/294-nop-metadata-import-engine-completeness.md` → `completed`
- 收口 plan 292 Deferred「UniqueKey/Index 导入」+ plan 293 Deferred「G10 MetaTableJoin to-one」

### Phase 1 — UniqueKey / Index 导入填充
- `OrmModelImporter` 新增 `buildUniqueKey(OrmUniqueKeyModel)` 与 `buildIndex(OrmIndexModel)`：UK 填充 ukName/displayName/columns(逗号分隔)/constraintName/tagSet/isDelta；Index 填充 indexName/displayName/indexType/uniqueIndex(null-safe Boolean→byte)/indexColumns(JSON `[{fieldName,desc}]`)/isDelta。新增 `buildIndexColumnsJson` helper。均非空方法体。
- `NopMetaModuleBizModel.importOrmModel` 在 entity 循环内对 `OrmEntityModel`（instanceof cast）遍历 `getUniqueKeys()` / `getIndexes()` → build → setMetaEntityId → save。
- 新增测试 `testImportProducesUniqueKeys` / `testImportProducesIndexes`：导入后 `__findPage` total > 0，且 Index JSON 含 fieldName。

### Phase 2 — MetaTableJoin to-one 关系补全
- `nop-metadata.orm.xml` NopMetaTableJoin `<relations>` 新增 `leftEntity` / `rightEntity` to-one（join leftEntityId/rightEntityId → NopMetaEntity.metaEntityId）。
- 重新生成 `_NopMetaTableJoin.java` 含 `getLeftEntity()` / `getRightEntity()`。

### Phase 3 — 批量模块导入
- 新增 `@BizMutation importOrmModels(List<String> paths, IServiceContext)`：返回 `List<Map>`（path/metaModuleId/moduleName/success/error），逐路径调用 importOrmModel；catch 异常标记 `success=false`+error 并 `orm().clearSession()` 隔离失败，不静默跳过。
- 测试 `testImportOrmModelsBatch`：[valid, invalid] 路径，断言 2 条目、success=true / success=false / error 均出现（response 不整体报错，证明失败不中断整批）。注：`List<Map>` 在 GraphQL 视为标量 JSON，不做字段选择。

### 验证
- `./mvnw install -pl nop-metadata/nop-metadata-service -am -T 1C`：BUILD SUCCESS，`TestNopMetaModuleBizModel` Tests run: 5, Failures: 0, Errors: 0。
- `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`：Total 0 findings。
- 独立 closure-audit 子 agent：5 项全 PASS，CLOSURE_APPROVED。
- pre-commit lint 修复：`NopMetaModuleBizModel.importOrmModels` catch 块原 `e.getMessage()` 触发 `java-lint-getmessage-only`，新增 SLF4J logger（`LOG.error(msg, e)` 保留完整堆栈）+ `toErrorMessage(Exception)` helper 抽取消息，全绿。

## Plan 291 closure（$xxx → ${xxx} 表达式语法迁移）

- Plan: `ai-dev/plans/291-migrate-dollar-shorthand-to-expression-syntax.md` → `completed`
- 独立 closure-audit 子 agent 验证：5 Phase Exit Criteria + 6 Closure Gates 全部 PASS
- 框架 xlib 模板、业务 view.xml/page.yaml、测试文件中的 AMIS `$xxx` 简写已迁移为 `${xxx}`
- `docs-for-ai/02-core-guides/amis-rendering.md` 标注 `$xxx` 废弃 + XPL 转义规则
- 残留 `nop-auth/test.json` 裁定为 `watch-only residual`（non-blocking）
- 工具验证：check-doc-links exit 0、check-plan-checklist exit 0、scan-hollow exit 0

## nop-metadata 构建验证

- `mvn compile -pl nop-metadata -am`：通过
- `mvn test -pl nop-metadata/nop-metadata-service -am`：全测试通过

## Plan 287 closure（nop-xlang union schema validator）

- Plan: `ai-dev/plans/287-nop-xlang-union-schema-validator.md` → `completed`
- 本轮无代码改动：union validator 修复与 focused test 在 2026-07-07 已落地。本轮为收口复跑，解除先前阻塞 plan 关闭的两个硬门禁。
- 复跑验证（均退出码 0）：
  - `node ai-dev/tools/check-doc-links.mjs --strict`：0 errors / 0 warnings（1443 文件，11576 refs）。
  - `node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/287-nop-xlang-union-schema-validator.md --strict`：1 plan checked，0 failed。
  - `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-kernel/nop-xlang --severity high`：**0 high findings**（先前 `IObjPropMeta.java` 与 `MacroScriptTagCompiler.java` 的既存 finding 已不在扫描结果）。
  - `./mvnw test -pl nop-kernel/nop-xlang -am`：全模块测试通过（含 `TestSchemaBasedValidator` 四个 union 用例）。
- Anti-Hollow 复查：`SchemaBasedValidator.validate()` union 分支经 `validateUnion()` 显式路由到所选子 schema 的既有 object/list/map/simple 校验链；缺失/未知 subtype 走显式 error，无 no-op / 空方法体 / 静默跳过。
- 计划文本一致性：已勾选全部 in-scope checklist（Phase 1 / Phase 3 / Closure Gates），Phase 3 Status → `completed`，Plan Status → `completed`，Closure 段落补 Re-verification evidence。
- Doc-sync: `ai-dev/plans/287-nop-xlang-union-schema-validator.md` 已更新。

## nop-wf use-approval 端到端实现完成（Plan 285 closure）

### Summary

完成 Plan 285（nop-wf use-approval 能力实现）的全部 5 个 Phase，实现从设计到代码到端到端验证的完整落地。

### Changes

#### Phase 5: 端到端集成验证（DIRECT + WORKFLOW）

1. **新增 ORM demo 实体**（`nop-wf/model/nop-wf.orm.xml`）：
   - `NopWfApprovableItem`（DIRECT 模式）：`tagSet="use-approval"` + `useWorkflow="true"`，含 `approveStatus`/`approvedBy`/`approvedAt` 字段
   - `NopWfApprovableForm`（WORKFLOW 模式）：同上，xmeta 配 `wf:wfName="test/approval-form"`

2. **codegen 真实生成物**（经 `nop-wf-codegen/postcompile/gen-orm.xgen`）：
   - `INopWfApprovableItemBiz extends ICrudBiz, IApprovableBiz` ✓
   - `_NopWfApprovableItem.xbiz x:extends="/nop/wf/base/approval-support.xbiz"` ✓
   - `NopWfApprovableItemBizModel extends CrudBizModel implements INopWfApprovableItemBiz` ✓
   - 同理 NopWfApprovableForm 系列

3. **xwf 工作流定义**（`nop-wf-service/src/test/resources/_vfs/nop/wf/test/approval-form/v1.xwf`）：
   - `x:extends="/nop/wf/base/oa.xwf"` + `bizEntityFlowIdProp="nopFlowId"`
   - listener `eventPattern="*end"` 幂等回调 approve action（含状态守卫防 EVENT_AFTER_END bug 重复触发）

4. **xmeta delta**（`NopWfApprovableForm.xmeta`）：
   - `wf:wfName="test/approval-form"` 扩展属性

5. **e2e 测试**（`TestUseApprovalE2E.java`，4 个测试方法）：
   - `testBizObjectHasFiveApprovalMutations`：Anti-Hollow (a) — bizObject operations 含 5 mutation
   - `testDirectApproval_stateTransition`：DIRECT 全状态迁移（submit→approve→reverseApprove→submit→reject→submit→withdraw）
   - `testDirectApproval_idempotencyGuard`：DIRECT 幂等守卫（非法状态抛异常）
   - `testWorkflowApproval_fullChain`：WORKFLOW 全链（submit→wf→nopFlowId 反写→agree→listener→approve）

6. **bug 修正**：
   - `approval-support.xbiz`：`reverseApprove` 从 APPROVED→REJECTED 修正为 APPROVED→SUBMITTED（对齐 plan 规格）
   - `wf-approval.xlib`：`notifyResult` 从 `svcCtx` implicit attr 改为 `wfRt.svcCtx`（svcCtx 在 listener scope 不可用）

### Test Results

- `TestUseApprovalE2E`：4 tests, 0 failures
- `TestUseApprovalComponents`：4 tests, 0 failures
- `TestUseApprovalCodegen`：2 tests, 0 failures
- 既有 nop-wf-service + nop-codegen 测试：89 tests, 0 failures, 0 errors（无回归）
- `node ai-dev/tools/check-doc-links.mjs --strict`：0 errors
- `node ai-dev/tools/check-plan-checklist.mjs`：passed

### Notes

- EVENT_AFTER_END 常量 bug（`"before-end"` 而非 `"after-end"`）导致 listener 触发两次，通过在 listener source 中加状态守卫（仅在 SUBMITTED 时调用 approve）实现幂等
- demo 实体放在 nop-wf ORM model 中（非 test scope），因为 codegen 模板按 `{appName}-dao`/`{appName}-service` 目录输出，test scope 无法利用标准 codegen 管线
