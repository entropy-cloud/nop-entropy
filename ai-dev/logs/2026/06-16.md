### goal-driver step 名称简化 + `--step`/`--list-steps` + `goal-driver.sh` 启动脚本

- **Scope**: `ai-dev/tools/opencode-goal-driver` + `ai-dev/tools/goal-driver.sh`
- **`main.js` 变更**：
  - `--step <STEP>`: 指定步骤作为入口，将其 goto 过渡替换为 done（执行后停止）
  - `--list-steps`: 打印顶层步骤列表后退出
  - 新增 `getTopSteps()` 从 `flows/goal-driver.json` 读取步骤 key
  - 未知 step 名报错并提示 `--list-steps`
- **`goal-driver.sh` 新启动脚本**：帮助信息 + `--list-steps` 透传 + `--step` 透传 + 其他参数透传
- **Step 名称简化**（flows/goal-driver.json + src/runner.js + 3 个测试文件 + design docs）：

  | 原名 | 新名 | 影响文件 |
  |------|------|---------|
  | `HEALTH_CHECK` | `CHECK` | goal-driver.json, runner.js (STEP_KEY_MAP), test descriptions |
  | `EXECUTE_ALL_ACTIVE_PLANS` | `PLANS` | goal-driver.json, test descriptors |
  | `ROADMAP_CHECK` | `ROADMAP` | goal-driver.json, runner.js (STEP_KEY_MAP + mock responses), intg.test.js, verify.test.js |
  | `PLAN_DRAFT` | `DRAFT` | goal-driver.json, runner.js (STEP_KEY_MAP + mock), intg.test.js, verify.test.js, transitions.test.js (`_DRAFT`→`DRAFT` 3 fixtures) |
  | `DEEP_AUDIT_LOOP` | `AUDIT` | goal-driver.json, intg.test.js, verify.test.js |

- **Design doc 同步**：
  - `goal-driver-flow-design.md`: 全文更新 step 名（mermaid 图、§一/三/四/九 说明文字），增加 v8 日期行
  - `flow-engine-design.md`: §6.1/6.3 更新为概念性引用（不硬编码 step 名），§6.4 `HEALTH_CHECK`→`CHECK`
  - `group-step-design.md` 保留旧名（v5 已取代的历史参考，不改）
- **验证**: `node --test test/*.test.js` → 82 tests, 0 fail
- **Doc-sync**: 已完成

---

### plan-220: nop-ai-agent Steering 注入机制（Actor Mailbox → ReAct 推理上下文）（L4-8-steering）

- **Plan**: `ai-dev/plans/220-nop-ai-agent-steering-injection.md`
- **Work Item**: L4-8-steering（plan 218 Non-Blocking Follow-ups 第一条；将 Actor mailbox 消费从 observation-only 升级为 steering-injection）
- **Phase 1 — Steering queue 契约 + 引用传递机制 + 设计裁定落档** ✅:
  - `AgentExecutionContext`：新增线程安全 steering 消息队列字段（`ConcurrentLinkedQueue<ChatMessage>`）+ `enqueueSteering(ChatMessage)` / `drainSteering() → List<ChatMessage>` / `getSteeringQueue()` API；Javadoc 明确线程安全契约（Actor 线程 enqueue、ReAct 线程 drain）
  - `AgentActor`：新增 `volatile Queue<ChatMessage> steeringQueue` 引用字段 + `setSteeringQueue(...)` / `getSteeringQueue()` 访问器；Javadoc 明确关联时机（engine 在 createActor 后、execute(ctx) 前关联）与 null 退路（未关联时消费循环退化为 observation-only record + ack，不丢弃消息）
  - `DefaultAgentEngine`：三个执行入口点（`doExecute` / `resumeSession` / `restoreSession`）的 `supplyAsync` lambda 中，在 `createActor` 返回 `AgentActor` 后、`execute(ctx)` 调用前，调用 `actor.setSteeringQueue(ctx.getSteeringQueue())`（直接用 createActor 返回值，无需 getActorBySession 反查）；NoOp 默认下 `isEnabled()==false` 跳过
  - 设计裁定 1-5 落档：steering queue 位置（ctx，裁定 1）/ 引用传递（createActor 返回值 + volatile，裁定 2）/ 检查点（round 边界，裁定 3）/ 注入语义（append 新消息，裁定 4）/ 元素类型（ChatMessage + MailboxEntry→ChatUserMessage 转换，裁定 5）
  - Owner docs 同步：`02-execution-model.md` §四 改写区分已落地（round 边界检查）vs successor（mid-round 跳过）+ 消除"声称跳过但实际不跳"矛盾；`nop-ai-agent-actor-runtime-vision.md` §10 Phase 2 标注 steering 已落地；`01-architecture-baseline.md` Actor Runtime 段落更新（observation-only → steering-injection）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS

---



- **Scope**: `ai-dev/tools/opencode-goal-driver/src/main.js`, `ai-dev/tools/goal-driver.sh`
- **`main.js` 变更**：
  - 新增 `--step <STEP_NAME>` 解析：将指定步骤作为入口，将其 `goto` 过渡替换为 `done`（执行后即停止，不继续流转）
  - 新增 `--list-steps`：从 `goal-driver.json` 读取并打印顶层步骤列表后退出
  - 未知 `--step` 输出错误并提示 `--list-steps`
- **`goal-driver.sh` 新脚本**：
  - `./goal-driver.sh <module>` — 正常执行完整流程
  - `./goal-driver.sh <module> --list-steps` — 列出可用步骤
  - `./goal-driver.sh <module> --step <STEP>` — 只执行单个步骤后停止
  - `./goal-driver.sh <module> --help` 或 `-h` — 显示帮助信息（含步骤列表）
  - 其余参数（`--dry-run`、`--agent`、`--model` 等）直接透传至 main.js
- **验证**：`node --test test/*.test.js` → 82 tests / 0 fail（零回归）
- **Doc-sync**: No owner-doc update required（goal-driver 内部工具，docs-for-ai 不涉及）

---

### plan-219: nop-ai-agent Sandbox Backend（ISandboxBackend + NoOpSandboxBackend + DockerSandboxBackend）（L4-7）

- **Plan**: `ai-dev/plans/219-nop-ai-agent-sandbox-backend.md`
- **Work Item**: L4-7（roadmap §4 Layer 4；ISandboxBackend 沙箱后端契约表面 + NoOp shipped 默认 + DockerSandboxBackend 功能实现，闭合 L4-7 roadmap gap，Layer 4 全部 ✅）
- **Phase 1 — Contract Surface + NoOp Default + Engine Wiring** ✅:
  - 新增 6 个 sandbox 契约类型（位于 `io.nop.ai.agent.security` 包，与其他纵深防御链接口同包）：
    - `ISandboxBackend` 接口：`SandboxResult execute(SandboxRequest)` 同步阻塞语义 + fail-closed 保证（绝不回退到 host）+ 设计 §7.1 契约
    - `SandboxRequest` 数据对象：command（List<String> argv 风格）+ workingDirectory + environmentVariables + config；Builder + 防御性拷贝 + null/empty 校验
    - `SandboxResult` 数据对象：exitCode + stdout + stderr + executionTimeMs + timedOut；不可变
    - `SandboxConfig` 资源限制：cpuSeconds(30) + memoryMb(1024) + wallSeconds(60) + networkMode(DENY/ALLOW，默认 DENY) + maxOutputBytes(1MiB)；Builder + 正数校验
    - `SandboxException extends NopAiAgentException`：携带 `SandboxFailureReason reason`
    - `SandboxFailureReason` 枚举：DOCKER_UNAVAILABLE / CONTAINER_START_FAILED / TIMEOUT / RESOURCE_LIMIT_EXCEEDED
  - `NoOpSandboxBackend implements ISandboxBackend`（shipped 默认）：ProcessBuilder host 执行 + `redirectErrorStream(true)` 合并 stdout/stderr + **专用 reader 线程异步 drain**（关键修复：避免 inline read 在静默命令上死锁，使 wall-time 超时无法触发）+ `Process.waitFor(wallSeconds, SECONDS)` 超时 + `killTree`（descendants + root destroyForcibly）+ maxOutputBytes 截断 + IOException→CONTAINER_START_FAILED
  - 设计裁定 1-3 落档（写入 plan + ISandboxBackend Javadoc）：(1) 包归属 `io.nop.ai.agent.security`（保持链组件内聚）；(2) 与 nop-ai-shell 关系（不同层/不同模块/不依赖；不复用 ExecutionResult）；(3) setSandboxBackend 不调用 warnIfInsecureDefaults（NoOp 是 Layer 1 设计性基线，非降级）
  - 引擎接线（`DefaultAgentEngine`）：新增 `sandboxBackend` 字段（默认 `NoOpSandboxBackend.INSTANCE`）+ `setSandboxBackend`/`getSandboxBackend`；`resolveExecutor` 透传到 `Builder.sandboxBackend`
  - 接线接线（`ReActAgentExecutor`）：新增 `sandboxBackend` final 字段 + Builder 方法 + `build()` null 兜底 + `getSandboxBackend()` public accessor（供接线测试断言）
  - 新增 2 个测试文件（16 tests 全绿）：`TestNoOpSandboxBackend`（13：正常执行 + 非零 exit + **wall-time 超时 1s 杀进程树** + 截断 + 大输出不死锁 + env 传递 + working directory + 空命令/null 命令/null config fail-fast + config 校验 + defaults 与设计表一致 + launch 失败抛 SandboxException）、`TestSandboxWiring`（3：**engine→resolveExecutor→Builder→field 引用链连通** + 自定义 backend identity 注入到达 executor + null fallback + smoke execute() 返回 populated result）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（零回归）
- **Phase 2 — DockerSandboxBackend Functional Implementation** ✅:
  - `DockerSandboxBackend implements ISandboxBackend`（功能性 opt-in）：构造器接收 `dockerImage` + 可选 `defaultConfig`；通过 Docker CLI（`ProcessBuilder` 调用 `docker run`/`docker kill`/`docker rm -f`）与容器交互——**不引入 Docker Java client 依赖**（裁定 1：CLI 是服务器端通用前提；与 NoOp ProcessBuilder 模型对称）
  - **失败分类（裁定 2）**：`buildDockerCommand` static 方法独立可测（构造 `docker run --rm --name <uuid> --cpus <n> --memory <n>m [--network none] [-v host:/workspace] [--workdir /workspace] [-e K=V]... <image> <cmd...>`）；`classifyFailure(exitCode, captured)` static 方法映射：daemon 连接错误/docker not found → DOCKER_UNAVAILABLE；image missing/permission denied/OCI runtime → CONTAINER_START_FAILED；exit 137 → RESOURCE_LIMIT_EXCEEDED；exit 124 → TIMEOUT；其余非零 → CONTAINER_START_FAILED（保守 fail-closed，绝不静默）
  - 容器清理：`--rm` 保证自然退出后自动删除；超时路径走 `docker kill <name>` + `docker rm -f <name>` 兜底（best-effort，错误吞掉不影响原 timeout 异常）
  - reader 线程异步 drain + maxOutputBytes 截断（与 NoOp 同形状但独立实现，避免两个 backend 耦合）
  - 新增测试 `TestDockerSandboxBackend`（15 tests 全绿）：命令构建 6 tests（**全部 SandboxConfig 字段映射 Docker flags** + ALLOW 省略 --network + working directory 映射 -v/--workdir + 无 workingdir 省略 + env -e 传递 + 前缀顺序稳定性）+ 失败分类 6 tests（success→null + daemon 不可用 + image missing + OOM 137 + timeout 124 + 未分类保守 bucket）+ fail-closed 1 test（docker missing → DOCKER_UNAVAILABLE，docker 存在时 skip）+ 清理保证 1 test（--rm + --name 永远存在）+ 条件性集成 1 test（docker + alpine:3.19 缓存时执行真实 `cat /workspace/hello.txt` 验证挂载，否则 skip）
  - 设计文档 §7.1 同步：补录"实现约定"段落（包归属 / Docker CLI 交互 / 失败分类 / 清理 / warnIfInsecureDefaults 裁定 / successor 工具执行器声明）
  - roadmap §4 L4-7 从 ❌ → ✅（plan 219；Layer 4 全部 ✅）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（零回归；15 新测试 + 既有全过）
- **Closure**：Plan Status → completed；独立 closure-audit subagent 验证全维度 PASS；Anti-Hollow PASS（reader 线程非 stub / 接线 3 测试断言引用链 / smoke execute 端到端 / fail-closed 分类逻辑非空）；无空壳/无静默 no-op 作为正常实现
- **Non-Goals（显式 successor）**：Shell/Code IToolExecutor 消费侧（successor plan）/ ISensitivePathProvider §7.2（successor）/ XDSL `<sandbox>` 配置化（optimization）/ 非 Docker 后端 Bubblewrap/Seatbelt/gVisor/kata（successor）/ Docker 镜像构建管理生命周期 / 容器池化复用（optimization）/ warnIfInsecureDefaults 扩展（watch-only：NoOp 是设计性基线）

---

### plan-218: nop-ai-agent Actor Runtime 平台层基础原语（AgentActor + ActorRegistry + 执行循环）（L4-8）

- **Plan**: `ai-dev/plans/218-nop-ai-agent-actor-runtime.md`
- **Work Item**: L4-8（roadmap §4 Layer 4；Actor Runtime 平台层基础原语，闭合 L4-8 roadmap gap 基础层）
- **Phase 1 — Actor 契约表面 + NoOp 默认 + 设计裁定落档** ✅:
  - 新增 `io.nop.ai.agent.runtime` 包 6 个类型：`AgentActorStatus` 枚举（7 值：CREATED/READY/RUNNING/IDLE/FAILED/RECOVERING/STOPPED，Javadoc 含状态转换图 + foundational 核心转换 vs RECOVERING successor 标注）、`AgentActor` 对象（不可变标识 actorId+sessionId+agentName+createdAt + volatile status+lastActiveAt + synchronizedList receivedMessages + 可选 IMailbox；Javadoc 线程安全契约：status/lastActiveAt volatile，receivedMessages Actor 单线程写 + snapshot 读）、`IActorRuntime` 接口（7 方法：isEnabled→boolean / createActor→AgentActor 幂等 / getActor / getActorBySession / getActiveActors / destroyActor→boolean / destroyAll→int；Javadoc opt-in 契约 + 1:1 身份模型 + 线程安全）、`ActorRegistry` 接口（register/unregister/get/getBySession/getAll）、`InMemoryActorRegistry`（ConcurrentHashMap 双索引 actorId→actor + sessionId→actorId，线程安全，register 替换时清理旧 session index）、`NoOpActorRuntime`（singleton，isEnabled()=false，createActor 抛 UOE 防御，查询返回空，destroy no-op——Minimum Rules #24）
  - 设计裁定 1-7 落档（写入 plan + 设计文档）：(1) opt-in 容器非执行引擎替代；(2) 专用线程 per Actor（Java 11 newSingleThreadExecutor，VT successor 待 Java 21 迁移）；(3) 1:1 映射 + 幂等 createActor；(4) 7 态状态机（foundational 实现核心转换，RECOVERING 预留）；(5) shipped 默认 isEnabled guard 非异常控制流；(6) mailbox 消费 observation-only（不注入 ReAct 上下文）；(7) registry 身份维度 actorId+sessionId+agentName（不含 tenantId/userId）
  - owner docs 同步：`nop-ai-agent-actor-runtime-vision.md` §3.2（状态机 foundational 核心转换 vs RECOVERING successor 标注）+ §4.2（六大组件表补状态列：ActorRuntime ✅ / ActorRegistry ✅ / 其余 successor）+ §10（Phase 1 ✅ 基础层已落地）；`01-architecture-baseline.md` §六（新增 Actor Runtime 基础层段落，与 IMailbox/IContributionRegistry 同级 opt-in 扩展点描述）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS
- **Phase 2 — InMemoryActorRuntime 功能实现 + 引擎接线 + observation-only 消费 + 测试** ✅:
  - 新增 `InMemoryActorRuntime implements IActorRuntime`：依赖 `Function<String, IMailbox> mailboxLookup`（避免 runtime→engine 循环包依赖）+ `ActorRegistry`（InMemoryActorRegistry）；`createActor` 幂等（同 sessionId active actor 返回既有；terminal 状态替换为新实例）+ CREATED→READY→注册 registry + mailboxLookup 获取既有 mailbox（null graceful）+ 启动专用 daemon 单线程 executor 运行 consumption loop
  - observation-only consumption loop（裁定 6）：READY→RUNNING（loop 启动）→ `mailbox.poll()` → 有消息则 `addReceivedMessage` + `LOG.info`（观测可见）+ `mailbox.ack()` + IDLE→RUNNING；无消息则 RUNNING→IDLE + sleep pollIntervalMs；退出条件 status STOPPED/FAILED 或线程 interrupt；poll/ack 异常 `LOG.error` + 状态→FAILED（非静默吞没）；**不注入 session/ctx messages**（AgentActor 无 session/context 引用）
  - 引擎接线（`DefaultAgentEngine`）：新增 `actorRuntime` 字段（默认 NoOpActorRuntime）+ `setActorRuntime`/`getActorRuntime`；三个执行入口点（doExecute/resumeSession/restoreSession）supplyAsync lambda 入口经 `if (actorRuntime.isEnabled())` guard opt-in `createActor`（NoOp isEnabled()==false 时跳走既有路径，无异常控制流）；三个 finally 块经 `getActorBySession` + `destroyActor` 注销（无需修改 CancelHandle 或 AgentExecutionContext）
  - 新增 6 个测试文件（49 tests 全绿）：`TestAgentActor`（10：构造 + 状态转换 + touch + receivedMessages snapshot + null 校验 + toString）、`TestInMemoryActorRegistry`（12：register/get by actorId + getBySession + **replace 时清理旧 session index** + unregister 双索引 + unregister 不误删新 actor 的 session index + getAll + null 安全）、`TestNoOpActorRuntime`（8：**isEnabled()=false** + createActor UOE + 查询返回空 + destroy 返回 false/0 + singleton）、`TestInMemoryActorRuntime`（12：**createActor 幂等返回同一实例** + terminal 替换新实例 + **destroyActor STOPPED+注销+线程 interrupt** + destroyAll count + getActiveActors 排除 STOPPED + null mailbox graceful idle + 参数校验）、`TestActorMailboxConsumption`（5：**poll→record→ack 核心路径** + FIFO 多消息 + **IDLE→RUNNING 转换** + observation-only 无 session/context 引用验证 + ack 永久消费无重投递）、`TestActorRuntimeEndToEnd`（2：**engine.execute 全路径 E2E**——capturing wrapper 断言 createActor×1 + destroyActor×1 + Actor 消费 send-message ASYNC payload + STOPPED + registry 注销 + mailbox drained；**NoOp 默认零回归**）
  - roadmap §4 L4-8 从 ❌ → ✅（plan 218）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → Tests run: 2024, Failures: 0, Errors: 0（+49 新测试，既有 1975 零回归）
- **Closure**：Plan Status → completed；独立 closure-audit subagent（ses_1302571f1ffePhQohIXS6tAhXj）验证 6 维度全部 PASS（契约表面 / Anti-Hollow 引擎接线 3 入口点 count=3 / observation-only 消费循环 poll→record→ack 非 stub / 6 测试文件 / roadmap ✅ / 设计文档更新）；无空壳/无静默 no-op 作为正常实现
- **Non-Goals（显式 successor）**：steering 注入（需 AgentExecutionContext steering queue）/ TeamManager + TeamSpec DSL（Phase 3）/ RecoveryManager + 跨进程接管锁（Phase 4，依赖 nop-job）/ ResourceGuard + 配额强制（Phase 5）/ call-agent 异步 mailbox 模型（Phase 2 剩余）/ DB-backed Actor 状态持久化 / 多租户隔离 / VT 优化（待 Java 21）/ nop-task/stream 集成 / XDSL 配置化——本计划只交付让这些 successor 可构建于其上的 Actor 原语基础

---



- **Plan**: `ai-dev/plans/217-nop-ai-agent-contribution-registry.md`
- **Work Item**: L4-6（roadmap §4 Layer 4；`IContributionRegistry` 7 贡献类型契约 + HOOK/PROMPT 装配期解析，闭合插件运行时贡献注册 gap）
- **Phase 1 — 贡献契约表面 + NoOp 默认 + 设计裁定落档** ✅:
  - 新增 `io.nop.ai.agent.contribution` 包 5 个类型：`ContributionType` 枚举（7 值：TOOL/COMMAND/HOOK/MCP_SERVER/PERMISSION_RULE/PROMPT/ROUTER，与 analysis §2.8 一致）、`HookPayload`（不可变，携带 `AgentLifecyclePoint` + `IAgentLifecycleHook`，构造期校验非空）、`Contribution`（不可变数据对象：type+id+source+priority+payload，构造期校验 type/id/source 非空；提供 `forHook`/`forPrompt` 工厂方法）、`IContributionRegistry` 接口（5 方法行为契约：`register→boolean`/`unregisterSource`/`getContributions(type)`/`getContributions(type,source)`/`getSources`；Javadoc 明确线程安全契约 + 装配期一次解析语义）、`NoOpContributionRegistry`（shipped singleton，`register` 显式返回 false 非静默成功，查询返回空集——Minimum Rules #24）
  - 设计裁定 1-5 落档（写入 `nop-ai-agent-hook-skill-engine.md` §8）：(1) 7 贡献类型→既有扩展点映射表（HOOK→IHookRegistry ✅、PROMPT→injectSystemInstruction ✅、其余 5 类 successor）；(2) 身份与来源作用域——type+id 跨来源唯一（跨来源冲突 fail-fast）、同 source 替换、unregisterSource 整体卸载；(3) 解析时机=装配期一次（与 consultSkills 同阶段，不对运行中 session 热生效）；(4) shipped 默认 NoOp 零行为回归（不扩展 warnIfInsecureDefaults——贡献缺失非安全风险）；(5) HOOK payload 契约——须是 HookPayload，不匹配 log WARN 跳过（fail-visible 非静默吞没）
  - owner docs 同步：`nop-ai-agent-hook-skill-engine.md` 新增 §8「插件贡献注册表」（5 小节：映射表/身份作用域/装配期解析时机/shipped 默认三件套/HOOK payload 契约）；`01-architecture-baseline.md` §六通信模型新增「插件贡献注册表已落地（L4-6 已落地）」段
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS
- **Phase 2 — InMemory 注册表 + Hook/Prompt 装配期解析 + 引擎接线 + 测试** ✅:
  - 新增 `InMemoryContributionRegistry implements IContributionRegistry`：外层 `ConcurrentHashMap<ContributionType, LinkedHashMap<id, Contribution>>`（LinkedHashMap 保注册序）+ 反向索引 `ConcurrentHashMap<source, Set<TypeIdKey>>`；register 复合操作（跨 source 冲突检测/同 source 替换/反向索引同步）在 per-type inner map 上 synchronized；跨 source `type+id` 冲突 fail-fast `NopAiAgentException`（含 type/id/source 上下文，不静默覆盖）；unregisterSource 经反向索引高效定位 + per-type lock 保护下源校验删除；getContributions(type)/(type,source) 返回 priority 升序稳定排序的 ArrayList 快照；getSources 返回不可变 Set 快照
  - 引擎接线（`DefaultAgentEngine`）：新增 `contributionRegistry` 字段（默认 `NoOpContributionRegistry`）+ `setContributionRegistry`/`getContributionRegistry` setter/getter（null 容错回退 NoOp）；`resolveExecutor` 新增私有方法 `resolveHookContributions(hookRegistry)`——在 `DefaultHookRegistry.fromAgentModel(model)` 之后遍历 `getContributions(HOOK)`，把 payload（须为 `HookPayload`）注册到 hookRegistry；payload 不匹配的 HOOK 贡献 log WARN 跳过（fail-visible，非静默吞没，单个坏贡献不阻断整批）；Builder 链新增 `.contributionRegistry(this.contributionRegistry)`
  - 执行器接线（`ReActAgentExecutor`）：构造器 + Builder + field 新增 `contributionRegistry` 参数（默认 NoOp）；`execute()` setup 阶段新增 `consultPromptContributions(ctx)`——与 `consultTalents`/`consultSkills` 同阶段，遍历 `getContributions(PROMPT)`（升序 priority），payload 须为 String，非空片段 `\n\n` 拼接经 `injectSystemInstruction` 注入 system prompt（additive，与 skill instruction 同模式）；非 String payload log WARN 跳过（fail-visible）
  - 新增 4 个测试文件（49 tests 全绿）：`TestInMemoryContributionRegistry`（19：register+typed query / source filter / priority 升序稳定排序 / 同 source 替换 / 同 source 改 priority 保序 / **跨 source 冲突 fail-fast 含上下文 + 不覆盖既有** / 跨 type 同 id 允许 / **unregisterSource 整体卸载多 type** / unregister 未知 source 无副作用 / getSources 全覆盖 + 反映 unregister / 空注册表空查询 / getSources 不可变快照 / register null 校验 / re-register after unregister / null type/source 查询返回空 / NoOp 显式 false 对照）、`TestNoOpContributionRegistry`（7：单例 / **register 返回 false 显式 no-op（Minimum Rules #24）** / 查询返回空不返回 null / unregister 显式 no-op / 空查询不可变 / register null 不 crash / **functional vs NoOp 对照显式 false vs true**）、`TestContributionAndPayload`（9：构造器校验 type/id/source 非空 / payload 允许 null / **forHook 工厂正确包装 HookPayload** / forPrompt payload 为 String / HookPayload 校验 point+hook 非空 / **ContributionType 恰好 7 值与 analysis §2.8 一致**）、`TestContributionResolution`（14：**Anti-Hollow 端到端**——HOOK 贡献 engine.resolveExecutor 装配→hookRegistry→ReAct PRE_REASONING 真实触发断言 fireCount>0；**多 HOOK 贡献 priority 排序断言 prio-1 先于 prio-10**；bad HookPayload WARN 跳过其余注册；**PROMPT 贡献经 capturingChatService 断言出现在 LLM 调用的 system messages**；多 PROMPT priority 排序断言 idxA<idxB；空片段跳过；bad PROMPT payload WARN 跳过；**NoOp 默认零回归**；NoOp register 显式 false；engine setter/getter + null 回退；**HOOK+PROMPT 多 source 共存**；**unregisterSource 后下次 build 不触发**；**HookPayload 携带的 point 真实路由到 POST_REASONING 断言**；engine 无 setter 时 resolveExecutor 仍正常工作）
  - roadmap §4 L4-6 从 ❌ → ✅（plan 217）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → Tests run: 1976, Failures: 0（+49 新测试，既有 1927 零回归）
- **Closure**：Plan Status → completed；独立 closure-audit subagent 待执行验证全部 Phase 1/2 Exit Criteria + Closure Gates；无空壳/无静默 no-op（NoOpContributionRegistry register 显式 false，bad payload WARN 跳过非吞没）/无 untested 接口方法
- **Non-Goals（显式 successor）**：Tool/Permission/Router/Command/MCP Server 5 类深度引擎解析（各自独立 successor——独立配置面或需新建运行时 surface）/ Actor Runtime 多租户装配（L4-8）/ DB-backed 持久化贡献注册表 / XDSL 配置化（`agent.xdef` `<contributions>`）/ 运行时热加载对运行中 session 生效 / 跨 type 全局排序——本计划只交付让这些 successor 可被插入的契约和接线

---



- **Plan**: `ai-dev/plans/216-nop-ai-agent-deferred-ack-mailbox.md`- **Plan**: `ai-dev/plans/216-nop-ai-agent-deferred-ack-mailbox.md`
- **Work Item**: L4-5（roadmap §4 Layer 4；`IMailbox` deferred-ack 邮箱契约，闭合 deferred-ack 消息缓冲 gap）
- **Phase 1 — 邮箱契约表面 + 数据对象 + NoOp 默认 + 设计裁定落档** ✅:
  - 新增 `io.nop.ai.agent.message` 包 4 个类型：`MailboxDeliveryState` 枚举（PENDING/IN_FLIGHT/ACKED/NACKED/DEAD_LETTERED + 状态转换 Javadoc）、`MailboxEntry` 不可变数据对象（deliveryId/deliveryCount/state/envelope/offeredAt/pollectedAt）、`IMailbox` 接口（6 方法 3-phase reservation 行为契约：offer→PENDING、poll→IN_FLIGHT、ack→ACKED 移除、nack→requeue 重投递/DEAD_LETTERED/NACKED、pendingCount/inFlightCount）、`NoOpMailbox` 单例默认（offer=false/poll=null/ack,nack=false/count=0——显式 no-op，非静默成功，Minimum Rules #24）
  - 设计裁定 1-6 落档：(1) 3-phase reservation 语义（offer 预占容量、poll 标记 in-flight、ack/nack 确认/拒绝）；(2) 有界 vs 无界容量——capacity<=0 无界（默认），有界满 offer 返回 false 背压信号（关闭 actor-runtime-vision §11 Open Question）；(3) 线程安全模型——ReentrantLock 保护三集合复合操作；(4) deliveryId 单调递增 AtomicLong，重投递分配新 id；(5) MailboxMessageHandler 适配器不改 IAgentMessenger 接口；(6) shipped 默认 mailboxFactory=null 零行为回归
  - owner docs 同步：`nop-ai-agent-actor-runtime-vision.md` §3.1（Mailbox 标 IMailbox: DeferredAck + 契约描述）+ §11（有界/无界 Open Question 标 [x] 已裁定）；`01-architecture-baseline.md` §通信模型（新增 Deferred-ack 邮箱段 + Mailbox 行更新为 IMailbox）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS
- **Phase 2 — DeferredAckMailbox 功能实现 + 适配器 + 引擎接线 + 测试** ✅:
  - 新增 `DeferredAckMailbox implements IMailbox`（覆盖全 6 接口方法 + 2 inspection 访问器 deadLetterCount/deadLetterEntries）：内部 ArrayDeque(PENDING FIFO) + LinkedHashMap(deliveryId→IN_FLIGHT) + List(DEAD_LETTER)；构造器接受 capacity（<=0 无界）+ maxDeliveryAttempts（默认 5，<1 抛 IllegalArgumentException）；offer 分配新 deliveryId（AtomicLong 单调递增）+ deliveryCount=1；poll 标记 IN_FLIGHT 记录 polledAt；ack 移除；nack(requeue=true) deliveryCount<max 重投递（新 deliveryId + count+1），>=max 转 DEAD_LETTERED；nack(requeue=false) 移除；全程 synchronized(lock) 保护三集合复合操作（等价 ReentrantLock 互斥）；capacity 计 pending+in-flight（poll 不释放槽，ack/nack 释放）
  - 新增 `MailboxMessageHandler implements IAgentMessageHandler`：onMessage→mailbox.offer，成功/失败均返回 null（ASYNC 无响应）；offer 失败（bounded 满/NoOp）log WARN 可见降级非静默吞没
  - 引擎接线：`DefaultAgentEngine` 新增 `mailboxFactory`（Function<String,IMailbox>，默认 null）+ `sessionMailboxes`/`sessionMailboxSubscriptions` ConcurrentHashMap 缓存 + `setMailboxFactory`/`getMailboxFactory`/`getSessionMailbox` 访问器；新增 `ensureSessionMailbox(sessionId)` 私有方法（computeIfAbsent 原子 per-session 去重——创建邮箱 + registerHandler 到 inbox topic agent.{sessionId}.inbox），在 doExecute/resumeSession/restoreSession 三个入口点 sessionId 可用时调用（非 resolveExecutor）；shipped 默认 null 零行为回归
  - 新增 4 个测试文件（35 tests 全绿）：`TestDeferredAckMailbox`（20：offer→poll→ack happy path / poll 空返回 null / ack,nack 未知 id 返回 false / FIFO 顺序 / 有界容量满 offer=false + in-flight 计入容量 + ack 释放槽 / 无界默认永不拒绝 / nack requeue 重投递 deliveryCount+1 新 deliveryId / nack no-requeue 移除 / acked id 不可 nack / dead-letter 达 maxDeliveryAttempts / 构造器校验 / 默认值 / **并发 4 producer + 1 consumer 500 条消息不丢失不重复 distinct deliveryId**）、`TestNoOpMailbox`（7：单例 / offer=false / poll=null / ack,nack=false / count=0 / 与 functional 对比证明显式拒绝）、`TestMailboxMessageHandler`（5：onMessage offer 返回 null / bounded 满 WARN-log 不抛 / NoOp WARN-log 不抛 / 构造器 null 校验 / getMailbox）、`TestMailboxEndToEnd`（3：**Anti-Hollow 端到端**——真实 DefaultAgentEngine ReAct 循环 LLM emit send-message 工具→messenger→inbox topic→MailboxMessageHandler→mailbox.offer，断言 getSessionMailbox 非空 + pendingCount>0 + poll→ack 完整路径；**接线验证** factory 被调用 + mailbox instanceof DeferredAckMailbox；nack requeue 重投递经 registered handler 路径；默认 null factory 不创建邮箱零回归）
  - roadmap §4 L4-5 从 ❌ → ✅（plan 216）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → Tests run: 1927, Failures: 0（+35 新测试，既有 1892 零回归）
- **Closure**：Plan Status → completed；独立 closure-audit subagent 验证全部 Phase 1/2 Exit Criteria + Closure Gates PASS；check-plan-checklist.mjs 退出码 0；无空壳/无静默 no-op（NoOpMailbox 是显式拒绝非静默成功）/无 untested 接口方法

---



- **Plan**: `ai-dev/plans/215-nop-ai-agent-memory-adapter.md`
- **Work Item**: L4-3（roadmap §4 Layer 4；`IMemoryAdapter` 三适配器契约 surface，闭合 Working Memory 后端可替换性 gap）
- **Phase 1 — 适配器契约表面 + NoOp 默认 + 设计裁定落档** ✅:
  - 新增 `io.nop.ai.agent.memory` 包 3 个适配器接口（清晰行为契约 Javadoc）：`IStorageAdapter`（per-session AiMemoryItem CRUD：save/loadAll(typeFilter)/loadByKey→null 表不存在/update/remove/batchSave）、`IEmbeddingAdapter`（embed/embedBatch→double[] + capability-query `isAvailable()`）、`IVectorAdapter`（index(key,vector) upsert/search(queryVec,topK)→相似 key 降序/remove）
  - 新增 3 个 NoOp/fail-fast 默认：`NoOpStorageAdapter`（全 6 方法抛 `NopAiAgentException`）、`NoOpVectorAdapter`（全 3 方法抛 `NopAiAgentException`）、`NoOpEmbeddingAdapter`（`isAvailable()=false` 使调用方走 keyword fallback；`embed`/`embedBatch` 抛 `UnsupportedOperationException` 防御性不可达路径）——无静默空/null 返回（Minimum Rules #24）
  - 设计裁定 1-5 落档：(1) 三独立接口（非单一 IMemoryAdapter）——存储/嵌入/检索三正交关注点各自可独立替换；(2) search fallback 策略——embedding NoOp→keyword 子串匹配（向后兼容 InMemoryAiMemoryStore），embedding 功能化→向量检索（结果空不 fallback 避免语义不一致）；(3) agent 层契约独立于 nop-ai-core `IEmbeddingModel`/`IVectorStore` 类型；(4) shipped 默认不变（`InMemoryMemoryStoreProvider` 保持，adapter-backed 是 opt-in）；(5) NoOp 检测用 capability-query 而非异常（`isAvailable()`，不 catch-and-swallow；仅 embedding 有 keyword fallback，storage/vector NoOp 仍 fail-fast）
  - owner docs 同步：`nop-ai-agent-session-engine.md` §6.3"Deferred（L4-3）"段→"L4-3 已交付"+ 设计裁定 1-5；`01-architecture-baseline.md` §Memory line 78 + line 83（search-memory 标透明增强）
- **Phase 2 — 复合 store + provider + in-memory 功能实现 + 引擎接线 + 测试** ✅:
  - 新增 `AdapterBackedAiMemoryStore implements IAiMemoryStore`（覆盖全 8 方法）：CRUD→`IStorageAdapter`；`add`/`update` 时 `isAvailable()=true` 则 embed+index，false 则只存储；`search` capability-query——true→向量检索（embed query→vector search→resolve keys→load items），false→keyword 子串匹配（复用 `InMemoryAiMemoryStore.search` 逻辑，不调 embed 不 catch-and-swallow）；`readBudgeted` 复用 pinned-first/priority-desc/token-budget 算法。复合 store 为 key 权威（auto-gen UUID），保证 storage 与 vector index 同 key
  - 新增 `AdapterBackedMemoryStoreProvider implements IMemoryStoreProvider`：`ConcurrentHashMap.computeIfAbsent` per-session 隔离；支持共享三元组构造（stateless/sessionId-aware 适配器）+ `StoreFactory` per-session 构造（in-memory 等有状态适配器）
  - 新增 3 个 in-memory 功能性参考实现（test/non-prod）：`InMemoryStorageAdapter`（ConcurrentHashMap + normalize 防御性 copy）、`InMemoryEmbeddingAdapter`（确定性 bigram 频率直方图 L2 归一化，DIMENSION=1024 降碰撞；isAvailable=true；相同文本→相同向量、共享 n-gram→高 cosine、无共享→低相似、空→零向量）、`InMemoryVectorAdapter`（线性扫描 + cosine，top-k 降序，与 nop-ai-core `CosineSimilarity.between` 同公式，agent 层不依赖 nop-ai-core 类型）
  - 引擎接线：**复用现有 `DefaultAgentEngine.setMemoryStoreProvider(IMemoryStoreProvider)`**（接口类型化，`AdapterBackedMemoryStoreProvider` 自然流入 → `resolveExecutor` Builder 链 → `ReActAgentExecutor:1403` → `AgentToolExecuteContext.getMemoryStore()`），无 engine 代码变更，shipped 默认 `InMemoryMemoryStoreProvider` 不变
  - 新增 7 个测试文件（57 tests 全绿）：`TestInMemoryStorageAdapter`（12：CRUD+type filter+null-key+防御性 copy+createTime 排序）、`TestInMemoryEmbeddingAdapter`（8：isAvailable/确定性/维度+归一化/共享高相似/无共享低相似/空零向量/batch 位置对齐）、`TestInMemoryVectorAdapter`（9：index→search/降序排序/upsert/防御性 copy/remove 幂等/topK 限制/维度不匹配）、`TestNoOpAdapters`（4：三 NoOp 全方法 fail-fast）、`TestAdapterBackedAiMemoryStore`（16：全 8 方法 + keyword fallback **不调 embed 计数断言** + 语义排序 + readBudgeted）、`TestAdapterBackedMemoryStoreProvider`（6：per-session 隔离）、`TestAdapterBackedMemoryEndToEnd`（2：**Anti-Hollow 端到端**——真实 `DefaultAgentEngine` ReAct 循环驱动 write-memory→search-memory，断言 `getMemoryStore()` 为 `AdapterBackedAiMemoryStore` 实例 + search 结果含写入内容；含 NoOp embedding keyword-fallback 场景）
  - roadmap §4 L4-3 从 ❌ → ✅（plan 215）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → Tests run: 1892, Failures: 0（+57 新测试，既有 1835 零回归）
- **Closure**：Plan Status → completed；独立 closure-audit subagent（`ses_130f457a6ffecwlEl8NDfGX3xP`）验证 14 项全部 PASS、CLOSURE READY、无空壳/无静默 no-op/无 untested 接口方法；`check-plan-checklist.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --module nop-ai-agent --severity high` 退出码 0
- **Non-Goals（显式 successor）**：DB-backed storage adapter（raw JDBC）/ 真实 embedding API 集成（包装 nop-ai-core `IEmbeddingModel`）/ 真实向量索引（包装 `IVectorStore` 接 FAISS/pgvector/Milvus）/ retention TTL / session fork memory copy / `AgentSession` 序列化 memory / 长期记忆子系统 / XDSL 配置化——本计划只交付让这些 successor 可被插入的契约和接线

---



### plan-214: nop-ai-agent IConflictStrategy 冲突解决策略接口 + FailFastStrategy 默认实现（L2-13a）

- **Plan**: `ai-dev/plans/214-nop-ai-agent-conflict-strategy.md`
- **Work Item**: L2-13a（roadmap §4 Layer 2 最后一个 ❌ 项，闭合后 Layer 2 全部接口具备 pass-through 或功能性默认实现）
- **Phase 1 — 接口 + 数据对象 + FailFastStrategy + InMemoryWriteIntentRegistry** ✅:
  - 新增 `io.nop.ai.agent.conflict` 包（7 个类型）：`ConflictDecision` 枚举（ALLOW/DENY）、`WriteIntent` 数据对象（sessionId/agentName/filePath 规范化绝对路径/operation/timestamp）、`ConflictResult` 数据对象 + `allow`/`deny` 静态工厂、`IConflictStrategy` 接口（`resolve(current, existing):ConflictResult` + `name()`）、`FailFastStrategy` 单例默认实现（空集/全同 session→ALLOW，含跨 session→DENY 含路径+session 列表 reason）、`IWriteIntentRegistry` 接口（`registerAndGetConflicting(intent):Set` 原子 check-then-register + `releaseSession(sessionId)`）、`InMemoryWriteIntentRegistry` 进程内实现（`ConcurrentHashMap<path,ArrayList>` + per-path `synchronized` 块保证 TOCTOU 安全；releaseSession 遍历全路径清除该 session 意图）
  - 新增测试（21 tests 全绿）：`TestFailFastStrategy`（8：空集 ALLOW / null 防御 ALLOW / 全同 session ALLOW / 跨 session DENY + reason 含路径+session / 多 session 列表 / 混合同+跨 session DENY / 单例 / name()）、`TestInMemoryWriteIntentRegistry`（13：首次注册空冲突集 / 跨 session 返回先注意图 / 同 session 不冲突 / 不同路径不冲突 / releaseSession 清除 / 保留其他 session / 释放未知 session no-op / null 防御 / getIntentsForPath 快照 / **并发原子性 50 trials 每 trial 至少一方看到冲突**（TOCTOU 安全）/ 同 session 并发不死锁 / M session 并发全记录+至多一方空集）
- **Phase 2 — 引擎接线 + Dispatch-Path 冲突检测 + Session 清理 + 测试** ✅:
  - `DenialLayerSource` 新增 `LAYER2_CONFLICT_STRATEGY` 枚举值（冲突 deny 专用，不复用 LAYER2_SECURITY_POLICY——冲突检测是协调能力非安全边界）
  - `DefaultAgentEngine`：新增 `conflictStrategy`（默认 FailFastStrategy）+ `writeIntentRegistry`（默认 InMemoryWriteIntentRegistry）mutable 字段 + setter（null 兜底）+ getter + `resolveExecutor` Builder 链传递；`doExecute`/`resumeSession`/`restoreSession` 三个 finally 块新增 `writeIntentRegistry.releaseSession(sessionId)` 调用
  - `ReActAgentExecutor`：新增 `conflictStrategy` + `writeIntentRegistry` final 字段 + 构造器参数 + null 兜底 + Builder 字段 + setter + `build()` null 兜底；**dispatch loop 在 Layer 3 approval gate 之后、`allowedCalls.add` 之前**新增冲突检测步骤：按 `ToolPathArgKeys.KEYS` 提取路径值 → 相对路径解析为绝对（基准=agentModel.workDir 或 JVM CWD，与 `DefaultLevelHintsProducer` 一致）→ `DefaultPathAccessChecker.normalizePathStatic` 规范化（与 Layer 1 path checker 同一函数）→ 每路径生成独立 `WriteIntent` → `registerAndGetConflicting` 原子注册并查冲突 → 总是调用 `conflictStrategy.resolve`（即使空集，便于未来 CoordinationBusStrategy 观察每个写意图 + 接线可验证）→ DENY 走标准 deny 路径（`ChatToolResponseMessage.error` + `ctx.addMessage` + `handleDenialAndCheckThreshold`（LAYER2_CONFLICT_STRATEGY，matchedRule=`layer2_conflict_strategy`）+ audit event + `publishEvent(TOOL_CALL_DENIED)` + `continue`）
  - **设计裁定**：(1) 路径参数识别策略 = 仅对含 `ToolPathArgKeys.KEYS` 的工具调用触发（不做 tool-name 分类，避免与 deny-list 分类集重复且不同步）；(2) 路径规范化 = 复用 `DefaultPathAccessChecker.normalizePathStatic`；(3) 多路径工具 = 每路径独立 WriteIntent，任一冲突则整个调用 deny；(4) 写意图注册时机 = 冲突检测通过后、allowedCalls.add 之前（注册表语义是"写意图"而非"写已完成"）；(5) 总是调用 resolve()（即使空集）= 接口契约 + 接线可验证 + 未来 coordination-bus 可观察每个写意图
  - 新增 3 个测试文件（11 tests 全绿）：`TestConflictStrategyWiring`（5：**resolve 被调用证明接线连通** / 无路径参数→resolve 不调用 / engine 默认 FailFast+InMemory / setter override / setter null 降级）、`TestConflictDetectionDispatchPath`（4：**跨 session 冲突 DENY + 审计事件 + TOOL_CALL_DENIED**（确定性 pre-populate）/ 同 session ALLOW / 无路径参数不触发 / session 释放后重新 pre-populate 观察新冲突）、`TestWriteIntentRegistryLifecycle`（2：execute 完成后 registry 为空 / denied session 的 intent 也被释放只留外部 pre-populate）
  - 设计文档 `nop-ai-agent-multi-agent.md` §4.4 更新：FailFastStrategy + InMemoryWriteIntentRegistry + dispatch-path 冲突检测已落地；CoordinationBusStrategy 标 successor
  - roadmap §4 Layer 2 L2-13a 从 ❌ → ✅（plan 214）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 全部通过零回归
- **Closure**：Plan Status → completed；独立 closure-audit subagent 验证通过

---



- **Plan**: `ai-dev/plans/213-nop-ai-agent-circuit-aware-routing.md`
- **Work Item**: circuit-aware-routing (roadmap L3-1b)
- **Phase 1 — 设计裁定 + Circuit-Aware 解析步骤接线（零回归默认）** ✅:
  - 裁定并落档 5 项设计决策（详见 plan Phase 1）：(1) **解析步骤编排位置** = `route()` 返回后、model-switched 审计检测（plan 205 role=80）之前（解析可能改 routedOptions，检测须看到解析后最终模型；封装为 executor 私有方法 `resolveCircuitAware(...)`，不新增公共类型）；(2) **解析算法** = allowCall(primary)==true→no-op 零开销路径（默认 AlwaysClosed 即此路径） / false→沿 `IModelRouter.getFallback(...)` 链扫描找 allowCall==true 的健康模型 / 链耗尽或全部 false→fail-fast；防御性硬上限 MAX_FALLBACK_SCAN=64 防 buggy 自定义 router 循环；(3) **fail-fast 行为** = 抛 NopAiAgentException 含所有检查过的 model-key=key(state) 列表 + 指引（Minimum Rules #24 不静默跳过）；(4) **原 :994 circuit check 角色** = 方案 (a) 保留为 safety-net（解析已保证 circuit-cleared，此 check 仅并发竞争 trip 时触发 fail-fast，开销一次 allowCall 可忽略）；(5) **routingReason 富化** = 不修改不可变 RoutingResult，circuit-induced switch 经 LOG.warn 记录 + model-switched 审计消息自然体现
  - `ReActAgentExecutor` 修改：新增私有方法 `resolveCircuitAware(routedOptions, router, breaker, sessionId)`（route() 后接线，返回 circuit-cleared options——主模型 allow→原 options，否则扫描回退链返回首个 allowCall==true 的 fallback，全不可用抛 NopAiAgentException）+ `MAX_FALLBACK_SCAN=64` 常量；更新原 :994 circuit check 注释说明其新 safety-net 角色
  - **shipped 默认零回归**：AlwaysClosed.allowCall 恒 true → resolveCircuitAware 首次检查即返回 → 不进入回退链扫描
  - 新增测试文件 `TestCircuitAwareRouting`（8 tests 全绿，含 Phase 1 + Phase 2 全覆盖）
  - 设计文档 `nop-ai-agent-reliability.md`：§3.3 更新（plan 213 ✅ 熔断感知路由解析段）+ §5.2 **改写**（原占位段落 → plan 209 回退链契约 + plan 213 circuit-aware routing 实现状态 + 架构决策）
  - roadmap：新增 L3-1b 行 ✅（plan 213）+ L3 验收标准补充熔断后自动降级
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1803 tests, 0 failures**（零回归）
- **Phase 2 — 功能性 Circuit-Aware 路由验证（ThresholdBreaker + SmartModelRouter + 端到端）** ✅:
  - 端到端测试 `endToEndRealThresholdBreakerPreTrippedResolvesToFallback`：pre-trip 真实 `ThresholdBreaker(2, 60s)` 经 2 次 recordFailure 使 primary=p:primary 转 OPEN → `DefaultAgentEngine.execute()` 端到端 → route() 返回 primary → 解析检测 OPEN → 扫描 SmartModelRouter 三 tier 回退链 → 切换 p:fallback-ok → 成功；断言 primaryCallCount=0 + fallbackCallCount≥1
  - circuit-OPEN→回退续跑（`circuitOpenPrimaryResolvesToCircuitClosedFallback`，ScriptedCircuitBreaker + SmartModelRouter）/ 全部 OPEN→fail-fast（`allModelsCircuitOpenFailsFastWithDiagnostic`，断言 error 含全部 model-key + OPEN + callCount=0）/ model-switched 审计（`circuitResolutionTriggersModelSwitchedAuditMessage`，状态ful `PrimaryTripsAfterTwoAllowCalls` breaker + 两轮 tool-call 场景，断言 fromModel=p:primary toModel=p:fallback1）/ PassThrough 无回退→fail-fast（`passThroughRouterCircuitOpenFailsFast`）/ HALF_OPEN probe 占用→回退（`halfOpenProbeBusyResolvesToFallback`）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1803 tests, 0 failures**
- **Closure**：Plan Status → completed；独立 closure-audit subagent 验证通过

---



- **Plan**: `ai-dev/plans/212-nop-ai-agent-sustainer.md`
- **Work Item**: L3-8
- **Phase 1 — 设计裁定 + 保持器契约 + NoOpSustainer 默认 + 接线（零回归）** ✅:
  - 裁定并落档 5 项设计决策（详见 plan Phase 1）：(1) L3-1 vs L3-8 互斥执行机制 = 部署层文档约束（非运行时 guard，design §11a 原文"由部署场景决定"把决定权交给部署，引擎不抛互斥异常）；(2) ISustainer vs IGoalTracker vs completion-judge-Continue 语义区分（assessGoal 迭代开始边界 STUCK→中止 vs onStop 退出决策点 CONTINUE→强制续跑 vs Continue 自愿续跑带死循环保护，三者操作方向/决策点/触发主体不同）；(3) 可持续退出点清单首版 = 仅 MAX_ITERATIONS 截断（reactLoop while 条件为假自然退出，最客观"被截断"信号；isComplete/isEscalate/shouldForceStop/cancel/pause 不 sustainable）；(4) sustain 续跑迭代预算扩展语义（CONTINUE 扩展原始 maxIterations 步长，递增 per-execution sustainCount，达 maxSustainCount 放行 STOP fail-safe，续跑每轮仍走完整顶部检查链不绕过治理）；(5) 接线点 = reactLoop 自然退出后 + post-loop 终态变更前（CONTINUE 跳过终态变更+事件发布直接重入，STOP 走终态变更+事件发布）
  - 新增 `io.nop.ai.agent.reliability` 包类型：`SustainDecision` 枚举（CONTINUE/STOP）、`SustainStopReason` 枚举（首版仅 MAX_ITERATIONS）、`SustainContext`（reliability-local 不可变数据载体，含 sessionId/stopReason/currentIteration/sustainCountSoFar，不引用 engine 类型对称 IterationSnapshot）、`ISustainer` 接口（`onStop(SustainContext):SustainDecision`）、`NoOpSustainer`（单例 pass-through 默认，恒 STOP、onStop 为显式 no-op 决策而非空方法体占位）
  - `ReActAgentExecutor` 修改：新增 `sustainer` 字段（构造器 null 安全降级 NoOpSustainer）+ Builder `.sustainer()` 方法 + **sustainLoop 包裹 reactLoop**（reactLoop 自然退出 + status=running = MAX_ITERATIONS 截断时构造 SustainContext(stopReason=MAX_ITERATIONS, sustainCountSoFar) 咨询 sustainer.onStop；CONTINUE→`ctx.setMaxIterations(max + originalMaxIterations)` + sustainCount++ + `continue sustainLoop` 重入顶部检查链；STOP→`break sustainLoop` 走 post-loop 终态变更；null 返回抛 NopAiAgentException 合约防御）
  - `DefaultAgentEngine` 修改：新增 `sustainer` 字段（默认 `NoOpSustainer.noOp()`）+ `setSustainer`/`getSustainer`（null 安全降级）+ `resolveExecutor` 调 `.sustainer(this.sustainer)`；sustainer 非安全组件，无 insecure-default WARN
  - 新增 2 个测试文件（9 tests 全绿）：`TestNoOpSustainer`（4：单例 / 恒 STOP 任意 session 含 null / onStop 显式 no-op 100 次调用恒 STOP / 任意 session 隔离）、`TestSustainerWiring`（5：engine 默认 NoOpSustainer / setter override / setter null 降级 / **默认 NoOp MAX_ITERATIONS 退出 completed 零回归**（3 iterations 全跑完 + status=completed）/ **RecordingSustainer onStop 被运行时消费证明接线连通**（onStopCount=1 + stopReason=MAX_ITERATIONS + sustainCountSoFar=0 + callCount=3=originalMaxIterations））
  - 设计文档 `nop-ai-agent-reliability.md`：§5.1a **改写**（原"ISustainer 为未来 successor"→plan 212 Phase 1 ✅ 契约+NoOpSustainer 已落地 + 互斥执行机制裁定 + 可持续退出点清单 + 语义区分）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1781 tests, 0 failures**（零回归）
- **Phase 2 — SisypheanSustainer 功能实现 + 端到端保持验证** ✅:
  - 新增 `SisypheanSustainer`（`io.nop.ai.agent.reliability` 包，opt-in）：**stateless**（仅持 `final int maxSustainCount`，默认 3 `DEFAULT_MAX_SUSTAIN_COUNT`，构造器可配，校验 >=0）。`onStop` 决策逻辑：stopReason=MAX_ITERATIONS + sustainCountSoFar < maxSustainCount → CONTINUE；stopReason=MAX_ITERATIONS + sustainCountSoFar >= maxSustainCount → STOP（ceiling fail-safe）；非 MAX_ITERATIONS stopReason → STOP（defensive，首版引擎只传 MAX_ITERATIONS）。at-least-once 语义：被截断执行至少获得 maxSustainCount 次额外续跑机会。maxSustainCount=0 等价 NoOp（MAX_ITERATIONS 退出点）
  - maxSustainCount 默认值 + sustain 步长裁定：默认 3（构造器参数，不引入 XDSL）；每轮 sustain 扩展步长 = 原始 maxIterations（executor 侧 `ctx.setMaxIterations(max + originalMaxIterations)`，total budget = originalMaxIterations * (1 + maxSustainCount)）
  - 新增 2 个测试文件（14 tests 全绿）：`TestSisypheanSustainer`（10：默认值 3 / 自定义 ceiling / 负数拒绝 / **maxSustainCount=0 等价 NoOp** / MAX_ITERATIONS ceiling 下 CONTINUE / MAX_ITERATIONS 达 ceiling STOP / 超 ceiling STOP / **stateless 跨 session 隔离**（session A 达 ceiling 不影响 session B）/ **at-least-once 恰好 maxSustainCount 次 CONTINUE** / null context 拒绝）、`TestSisypheanSustainerEndToEnd`（4：**端到端 MAX_ITERATIONS→sustain→completion**（originalMax=3 + chatService 前 4 次 tool call 第 5 次 complete，chatCallCount=5 > 3 证明预算扩展 + status=completed）/ **端到端 ceiling→STOP fail-safe**（originalMax=2 + maxSustainCount=2 + 恒 tool call，total budget=6=2*(1+2)，status=completed）/ **接线证明 sustainCountSoFar 递增 0,1,2**（RecordingSustainer 包装 SisypheanSustainer，onStopCount=3 + 全 MAX_ITERATIONS + sustainCounts=[0,1,2]）/ **并发 execute() 独立计数**（同一 stateless SisypheanSustainer(1) 跑两个 session 各得 4=2*(1+1) budget 独立））
  - 设计文档 `nop-ai-agent-reliability.md`：§5.1a 更新（plan 212 ✅ Sisyphean 已落地 + SisypheanSustainer stateless/at-least-once 描述）；§11a 更新（互斥裁定最终状态 = 部署层文档约束）
  - roadmap §4：L3-8 ❌→✅（plan 212）+ 标注互斥裁定为部署层文档约束
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1795 tests, 0 failures**（1781 既有 + 14 新增，零回归）
- **Closure**：Plan Status → completed；独立 closure-audit subagent 验证通过



- **Plan**: `ai-dev/plans/211-nop-ai-agent-goal-tracker.md`
- **Work Item**: L3-3
- **Phase 1 — 设计裁定 + IGoalTracker 契约 + NoOpGoalTracker 默认 + 接线（零回归）** ✅:
  - 裁定并落档 5 项设计决策（详见 plan Phase 1）：(1) IGoalTracker 契约形态 + 包依赖方向（两方法 `recordIteration`/`assessGoal` 读写分离，对称 ICircuitBreaker；`IterationSnapshot` reliability-local 不引用 engine 类型）；(2) `GoalAssessment` 三值枚举（PROGRESSING/STUCK/GOAL_ACHIEVED，GOAL_ACHIEVED 为 LLM-based successor 预留）；(3) 接线点 + STUCK abort 语义（recordIteration 在 LLM 响应后 if(!hasToolCalls) 前；assessGoal 在 shouldForceStop 后 PRE_REASONING 前；STUCK→escalated status + lastError + break）；(4) sessionId=null 行为（不追踪 anonymous，返回 PROGRESSING）；(5) IterationSnapshot 签名 = `toolName:stableArgsString`（args key 排序后序列化）
  - 新增 `io.nop.ai.agent.reliability` 包类型：`GoalAssessment` 枚举（PROGRESSING/STUCK/GOAL_ACHIEVED）、`IterationSnapshot`（reliability-local 不可变数据载体，含 toolCallSignatures List + iteration int）、`IGoalTracker` 接口（`recordIteration(sessionId, snapshot)` / `assessGoal(sessionId):GoalAssessment`）、`NoOpGoalTracker`（单例 pass-through 默认，恒 PROGRESSING、recordIteration 为显式 no-op 而非空方法体占位）
  - `ReActAgentExecutor` 修改：新增 `goalTracker` 字段（构造器 null 安全降级 NoOpGoalTracker）+ Builder 方法 + 循环顶部 assessGoal 检查（shouldForceStop 后、PRE_REASONING hook 前，STUCK 调 handleGoalStuck 设 escalated+lastError+event 并 break reactLoop，不静默跳过）+ LLM 响应后 recordIteration（构造 IterationSnapshot，buildToolCallSignatures 从 assistantMsg.getToolCalls() 提取 `name:sortedArgsJson` 签名，无 tool call 返回空列表）
  - `DefaultAgentEngine` 修改：新增 `goalTracker` 字段（默认 `NoOpGoalTracker.noOp()`）+ `setGoalTracker`/`getGoalTracker`（null 安全降级）+ `resolveExecutor` 调 `.goalTracker(this.goalTracker)`；tracker 非安全组件，无 insecure-default WARN
  - 新增 2 个测试文件（9 tests 全绿）：`TestNoOpGoalTracker`（4：单例 / 恒 PROGRESSING 任意 session（含 null）/ recordIteration 显式 no-op 不改变状态 / recording 不跨 session 泄漏）、`TestGoalTrackerWiring`（5：engine 默认 NoOpGoalTracker / setter override / setter null 降级 / **默认 NoOp 成功路径 completed 零回归** / **RecordingGoalTracker recordIteration+assessGoal 被运行时消费证明接线连通**）
  - 设计文档 `nop-ai-agent-reliability.md`：§5.3 **改写**（原概念级"循环检测"→`IGoalTracker` 契约小节：读写分离契约 + GoalAssessment 三值 + STUCK abort 语义 + 接线点 + NoOp 默认 + SessionGoalTracker 策略 + 架构决策）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -T 1C` → **1757 tests, 0 failures**（零回归）
- **Phase 2 — SessionGoalTracker 功能实现 + 端到端 stuck 检测验证** ✅:
  - 新增 `SessionGoalTracker`（`io.nop.ai.agent.reliability` 包，opt-in）：per-session 滑动窗口追踪 tool-call 签名（`ConcurrentHashMap<String, SessionState>` + per-session `synchronized(SessionState)` + `LinkedList` 窗口，对称 ThresholdBreaker per-entry 同步模式）。`recordIteration` 从 IterationSnapshot 取签名逐个追加窗口、超 `windowSize` 从头部淘汰；`assessGoal` 计数当前窗口内签名频次，任一签名重复达 `stuckThreshold` → STUCK。构造器参数 `windowSize`（默认 5）/ `stuckThreshold`（默认 3），校验 windowSize>=1 / stuckThreshold>=1；windowSize<stuckThreshold 时短路返回 PROGRESSING（窗口内不可能达到阈值）
  - tool-call 签名归一化策略裁定：签名 = `toolName + ":" + stableArgsString`，stableArgsString = args map 经 `TreeMap` key 排序后 `JSON.stringify`（key 顺序差异不产生不同签名）；engine 侧 `buildToolCallSignatures` 从 `assistantMsg.getToolCalls()` 构造，无 tool call 返回空列表
  - "无 tool call"轮次裁定：empty 签名列表 → recordIteration 不追加窗口（不构成 stuck/progress 证据，状态不变）；anonymous（sessionId=null）不追踪，assessGoal 恒 PROGRESSING
  - 多 tool call 轮次：一轮多个签名各贡献一个窗口条目（同轮同签名多次计入重复计数）
  - 新增 2 个测试文件（15 tests 全绿）：`TestSessionGoalTracker`（12：构造校验 / 默认值 / 新签名 PROGRESSING / 同签名达阈值 STUCK / key 排序归一化 / **窗口滑动淘汰后 STUCK 清除** / 无-tool-call 轮次不影响评估 / 多 tool call 各贡献签名 / 多 session 隔离 / 未追踪+anonymous PROGRESSING / threshold>windowSize 永不 STUCK / **32 线程并发 session 独立一致 STUCK**）、`TestSessionGoalTrackerEndToEnd`（3：**端到端同签名循环 abort**——SessionGoalTracker(5,3) 经 Builder 接线，chatService 恒返回同 tool call，3 次 LLM 调用后 iteration 3 assessGoal=STUCK→abort，断言 callCount=3 < maxIterations=10 + status=escalated + error 含 stuck + tracker.assessGoal=STUCK；**NoOp 默认零回归对照**——同循环场景 NoOp 不 abort + completed；**engine 接线**——DefaultAgentEngine.setGoalTracker 经 test-react-agent 端到端 abort escalated）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1772 tests, 0 failures**（1757 既有 + 15 新增，零回归）
- **Closure**：Plan Status → completed

### plan-210: nop-ai-agent 熔断器（ICircuitBreaker + AlwaysClosed + ThresholdBreaker，L3-1）

- **Plan**: `ai-dev/plans/210-nop-ai-agent-circuit-breaker.md`
- **Work Item**: L3-1
- **Phase 1 — 设计裁定 + 熔断契约 + AlwaysClosed 默认 + 接线（零回归）** ✅:
  - 裁定并落档 6 项设计决策（详见 plan Phase 1）：(1) L3-1 vs L3-8 互斥弹性哲学（fail-fast 已选）；(2) per-model 追踪语义（`provider:model` 复合键）；(3) 接线点 + 结果记录归属（circuit 检查 retry 循环外层、recordFailure catch 入口处在 FALLBACK 切换前、非异常失败路径覆盖、recordSuccess 循环后、circuit 检查只覆盖主模型）；(4) HALF_OPEN 并发试探语义（lazy 进入 + 单 probe 原子占位）；(5) circuit OPEN 行为（抛 NopAiAgentException）；(6) AlwaysClosed record 为显式 no-op
  - 新增 `io.nop.ai.agent.reliability` 包类型：`CircuitState` 枚举（CLOSED/OPEN/HALF_OPEN）、`ICircuitBreaker` 接口（`allowCall(modelKey):boolean` / `getState(modelKey):CircuitState` / `recordSuccess(modelKey)` / `recordFailure(modelKey)`，全部 modelKey 参数化）、`AlwaysClosed`（单例 pass-through 默认，恒 CLOSED、恒 allowCall=true、record 为显式 no-op 而非空方法体占位）
  - `ReActAgentExecutor` 修改：新增 `circuitBreaker` 字段（构造器 null 安全降级 AlwaysClosed）+ Builder 方法 + retry 循环外层 circuit 检查（捕获 primaryModelKey 进入循环前，`allowCall==false` 抛 NopAiAgentException 含 model-key + state + 指引，不静默返回）+ catch 入口处 `recordFailure(buildModelKey(routedOptions))`（在 isRetry/isFallback/isStop 分支前、FALLBACK 切换 routedOptions 前）+ `!response.isSuccess()` 分支 `recordFailure` + 成功路径 `recordSuccess`（用最终 routedOptions，可能是 fallback 模型）
  - `DefaultAgentEngine` 修改：新增 `circuitBreaker` 字段（默认 `AlwaysClosed.alwaysClosed()`）+ `setCircuitBreaker`/`getCircuitBreaker`（null 安全降级）+ `resolveExecutor` 调 `.circuitBreaker(this.circuitBreaker)`；breaker 非安全组件，无 insecure-default WARN
  - 新增 2 个测试文件（11 tests 全绿）：`TestAlwaysClosed`（5：单例 / 恒 CLOSED 任意 modelKey / 恒 allowCall=true / recordSuccess+recordFailure 不改变状态 / recording 不跨 modelKey 泄漏）、`TestCircuitBreakerWiring`（6：engine 默认 AlwaysClosed / setter override / setter null 降级 / **默认 AlwaysClosed 单次调用+失败 surface 零回归** / **默认 AlwaysClosed 成功路径 completed 零回归** / **OPEN breaker 拒绝调用 0 calls + status=failed + error 含 OPEN fail-loud**）
  - 设计文档 `nop-ai-agent-reliability.md`：§3.3 标注断路器契约 + AlwaysClosed 默认已落地（plan 210 Phase 1）；§5.1a 标注 fail-fast 哲学已选 + ICircuitBreaker 契约已落地（§5.1 三态状态机实现状态留 Phase 2 更新）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1728 tests, 0 failures**（1717 既有 + 11 新增，零回归）
- **Phase 2 — ThresholdBreaker 功能实现 + 端到端熔断验证** ✅:
  - 新增 `ThresholdBreaker`（`io.nop.ai.agent.reliability` 包，opt-in）：per-model-key 状态机（`ConcurrentHashMap<String, BreakerEntry>` + per-entry `synchronized` + `volatile state`），CLOSED 连续失败计数达 `failureThreshold`（默认 3）→ OPEN；OPEN 经 `cooldownMs`（默认 60000ms）后 lazy 转 HALF_OPEN（无后台线程，下次 `allowCall` 时检查 `now - openedAt >= cooldownMs`）；HALF_OPEN 单 probe 原子占位（首个 caller 放行作 probe，并发 caller 拒绝视为仍 OPEN），probe 成功→CLOSED 复位计数，probe 失败→OPEN 重启冷却；构造器校验（failureThreshold>=1、cooldownMs>=0）；getState 读 volatile 无锁（诊断用）；null modelKey 全方法拒绝
  - 阈值与冷却默认值裁定：构造器参数（failureThreshold=3 / cooldownMs=60000ms），不引入 XDSL 配置化（Non-Goal）；默认值匹配设计 §5.1 "3 轮失败即终止" 例子
  - 新增 2 个测试文件（20 tests 全绿）：`TestThresholdBreaker`（17：构造校验 4 / 默认值 / null modelKey 拒绝 / CLOSED 阈值下不 trip / CLOSED 达阈值 trip OPEN / success 复位计数 / OPEN cooldown 内拒绝 / OPEN cooldown 后 lazy 转 HALF_OPEN / cooldown=0 立即 probe / HALF_OPEN success→CLOSED / HALF_OPEN failure→OPEN 重启冷却 / HALF_OPEN probe 独占性（并发 caller 拒绝）/ per-model 隔离 / **100 线程并发 failures 一致 trip** / **32 线程并发 allowCall 至多 1 个 probe** / 未追踪 key 默认 CLOSED）、`TestThresholdBreakerEndToEnd`（3：**端到端连续失败跨 execute() trip+reject**——ThresholdBreaker(threshold=2)，2 次 execute 各失败 1 次→breaker OPEN，第 3 次 execute 外层检查拒绝（callCount 停在 2）+ error 含 OPEN；**预 trip 后 0 chatService 调用**——pre-trip breaker 后 execute 外层检查立即拒绝（callCount=0）+ error 含 OPEN；**AlwaysClosed 默认零回归**——同场景 5 次 execute 无 OPEN 错误）
  - 设计文档 `nop-ai-agent-reliability.md`：§3.3 更新为实现状态（ThresholdBreaker 已落地）；§5.1 三态状态机实现状态标注（plan 210 ✅）；§5.1a 更新（fail-fast + ThresholdBreaker 功能实现）；§11 延期项"完整断路器策略"划除（已落地）
  - roadmap §4 Layer 3：L3-1 ❌→✅（plan 210）+ §2.2 未实现表 ICircuitBreaker 行 ❌→✅ + Layer 3 验收标准 `连续故障后系统可自动熔断` 勾选
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1748 tests, 0 failures**（1728 既有 + 20 新增，零回归）
- **Closure**：Plan Status → completed


### plan-209: nop-ai-agent SmartModelRouter 功能性路由器（复杂度路由 + 预算感知降级 + Fallback 链消费，L2-23）

- **Plan**: `ai-dev/plans/209-nop-ai-agent-smart-model-router.md`
- **Work Item**: SmartModelRouter / L2-23
- **Phase 1 — SmartModelRouter 核心（复杂度分类 + tier 路由 + 预算感知降级）** ✅:
  - 新增 `io.nop.ai.agent.router.Complexity` 枚举（SIMPLE/MEDIUM/COMPLEX，带 `getKey()` 小写标识 + `getLevel()` cheap→strong 序）
  - `IModelRouter` 新增 `default ChatOptions getFallback(ChatOptions currentOptions)` 方法（默认返回 null = 无回退能力）；`PassThroughModelRouter` 继承 default 无需改动
  - 新增 `SmartModelRouter`（implements IModelRouter，opt-in）：Builder 接收 per-tier 主模型（`tierModel(Complexity, ChatOptions)`）+ 有序回退链（`fallback(Complexity, ChatOptions)`）+ 4 个可配置启发式阈值（默认 mediumChars=200/complexChars=2000/mediumTools=2/complexTools=5）；`route()` 做启发式分类（消息总长度 + 工具数 + 代码 ```/结构化 `{`/`<` 信号）→ 选 tier 主模型（`incoming.copy().merge(tierOptions)` 保留 tools）→ 读 `ctx.getBudgetSnapshot()`，`exceeded==true` 时降级到最高已配置的更便宜 tier（无更便宜则保持并标注 reason）→ 填充 routingReason；无配置模型的 tier 抛 `NopAiAgentException`（Minimum Rules #24）；`complexity` 字段记录原始分类（非降级后 tier），routingReason 解释降级
  - 新增 2 测试文件（19 tests 全绿）：`TestSmartModelRouter`（17：simple/medium/complex 三档路由 × 长度/工具数/代码 / routed options 保留 tools / 预算超限降级 / 不超限不降级 / simple 已最便宜无更便宜 / null snapshot 不降级 / null ctx 不降级 / routingReason 恒非空 / 无配置 tier 抛异常 / builder 校验 / PassThrough 继承 null fallback / SmartModelRouter 无回退链返回 null）、`TestSmartModelRouterWiring`（2：**接线验证**——注入 SmartModelRouter 到 `ReActAgentExecutor.builder().modelRouter(...)`（DefaultAgentEngine 内部委派路径），simple 消息→cheap-model、complex 消息→strong-model 到达 chatService）
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；router 测试 38 tests 全绿
- **Phase 2 — 模型回退链消费 + 端到端验证 + 文档更新** ✅:
  - `SmartModelRouter.getFallback(currentOptions)` 覆写：扫描各 tier 序列 `[primary, fallback1, ...]`，定位当前 model（`provider:model` 复合键匹配）返回下一模型（`current.copy().merge(nextTier)` 保留 tools）；链尾/未知 model 返回 null（fail-loud）
  - `ReActAgentExecutor` 重试循环 FALLBACK 分支改造：原直接抛 `NopAiAgentException` → 先 `modelRouter.getFallback(routedOptions)`；有回退模型则 `routedOptions = fallbackOptions` + `request.setOptions(...)` + `attempt=0` 重置（裁定：新模型的新调用周期）+ `lastError=null` + continue 重试；无回退（含 PassThrough 默认、链耗尽）保持 fail-loud 抛异常（消息含 "FALLBACK"，兼容 `TestRetryPolicyWiring`）。**关键**：`routedOptions` 重赋值使后续 `IUsageRecorder.record()` 的 aiProvider/aiModel 归属回退模型而非原始首选模型（plan 209 usage-attribution 要求）
  - 裁定落档：回退后 attempt 重置为 0（新模型独立重试预算）
  - 新增 `TestSmartModelRouterFallback`（9 tests 全绿）：getFallback 单元（返回下一模型 merged 保留 tools / 链尾返回 null / 未知 model null / PassThrough null）+ **FALLBACK→回退成功**（SmartModelRouter+fallback 配置，chatService 首选模型抛 NopTimeoutException→FALLBACK→回退模型成功，断言 final model=回退模型 + 序列含 primary+fallback）+ **FALLBACK→fail-loud 无配置**（SmartModelRouter 无 fallback 链→status=failed+error 含 FALLBACK）+ **FALLBACK→fail-loud PassThrough**（默认 router→fail-loud）+ **usage 归属**（recording IUsageRecorder，断言唯一 UsageRecord.aiModel=回退模型 ≠ 首选）+ **端到端 DefaultAgentEngine**（8-arg 构造器注入 SmartModelRouter + `setRetryPolicy(alwaysFallback)` + `/test-react-agent.agent.xml`，engine.execute→ReAct→SmartModelRouter→LLM 失败→FALLBACK→回退模型→completed，断言 callCount>=2 + lastModel=回退模型）
  - 设计文档 `nop-ai-agent-llm-layer.md`：§6.4 **重写**（移除"使用便宜模型做 Judge 分类"→改为"启发式分类首版 + Judge LLM 为 successor"，附 tier 路由/预算降级/routingReason/回退链实现状态）；§6.3 六步管线标注（Step 5 ✅ / Steps 1,2,3,4,6 deferred）；§6.5 表加"实现状态"列（5xx/timeout 切换 Fallback Chain ✅）；§7 IRetryPolicy note 的 FALLBACK 子句更新（plan 209 查询 router 回退模型，非恒 fail-loud）
  - roadmap §4 Layer 2：新增 L2-23 行（✅ plan 209）+ Layer 2 验收标准新增 L2-23 checkbox 行
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS 全模块零回归（含 TestRetryPolicyWiring/TestStandardRetryPolicyEndToEnd/TestBudgetProviderWiring 既有测试）；`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0
- **偏差**：(1) `RoutingResult.complexity` 字段记录原始分类（非降级后 tier）——降级后请求仍为 complex 性质，routingReason 解释降级，比记录降级后 tier 在审计消息中更清晰；(2) 默认配置（PassThroughModelRouter + NoRetryPolicy）行为零变化，SmartModelRouter opt-in
- **Doc-sync**: `nop-ai-agent-llm-layer.md` §6.3–6.5 + §7、`nop-ai-agent-roadmap.md` §4 已更新
- **Closure**：Plan Status → completed


### plan-208: nop-ai-agent Layer 1 roadmap 状态同步（A3 / L1-11 / L1-13 陈旧 ❌ → ✅）

- **Plan**: `ai-dev/plans/208-nop-ai-agent-layer1-roadmap-status-sync.md`
- **Work Item**: A3
- **性质**：纯文档计划（owner-doc drift 修正），无任何代码变更
- **Current Baseline 核对（live repo, 2026-06-16）**：
  - A3（PreStop/PostStop ReAct 钩子）：`AgentLifecyclePoint.java:14-15` 枚举存在；`ReActAgentExecutor.java:1282`（BEFORE）+ `:1345`（AFTER）触发点；re-entry 语义（`reentryCounters`、`DEFAULT_MAX_REENTRIES=3`、`:1699-1702` 校验）；`TestHookInReActLoop.java` 3 focused tests（`:371`/`:408`/`:445`）
  - L1-11（缺失枚举）：`AgentExecStatus.java` 存在且全模块使用；`AgentTaskStatus.java`/`AgentPlanStatus.java` 经 `find` 确认不存在（L0-2 统一决策结果，非遗漏）
  - L1-13（测试框架）：205 test files 存在（`find` 确认），多份已关闭 plan closure 引证 1595+ tests 全绿
- **Phase 1 — Roadmap 状态同步** ✅:
  - `nop-ai-agent-roadmap.md` §4 Layer 1：A3（line 147）❌→✅ + 追加实现证据（`ReActAgentExecutor.java:1282,1345` + re-entry + 3 focused tests）；L1-11（line 135）❌→✅ + 追加"已通过 L0-2 与 AgentExecStatus 统一"说明；L1-13（line 137）❌→✅ + 追加"205 test files / 1595+ tests"说明
  - `ai-dev/logs/2026/06-16.md`（本条目）追加执行记录
  - **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0；`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` 退出码 0
- **Closure**：Plan Status → completed


### plan-207: nop-ai-agent LLM 调用重试策略（IRetryPolicy + NoRetry + StandardRetryPolicy，L3-2）

- **Plan**: `ai-dev/plans/207-nop-ai-agent-llm-retry-policy.md`
- **Work Item**: L3-2
- **Phase 1 — 重试契约 + 错误分类 + NoRetry 默认 + 重试循环接线** ✅:
  - 裁定决策返回形态：`IRetryPolicy.shouldRetry(RetryContext)` 返回 `RetryOutcome`（含 `RetryDecision` RETRY/STOP/FALLBACK + `delayMs` 延迟毫秒），`RetryOutcome` 提供 `stop()`/`fallback()`/`retryAfter(ms)` 工厂 + `isRetry()/isStop()/isFallback()` 谓词；全部 public 可见性（policy 实现与重试循环分支都需消费）
  - 新增 `io.nop.ai.agent.reliability` 包类型：`ErrorClassification` 枚举（TRANSIENT/NON_TRANSIENT/RATE_LIMITED/QUOTA_EXCEEDED）、`RetryDecision` 枚举（RETRY/STOP/FALLBACK）、`RetryOutcome`（immutable 决策+延迟，含校验 decision!=null/delayMs>=0）、`RetryContext`（immutable：attempt/lastError/errorClassification/hasStreamedContent）、`IRetryPolicy` 接口（唯一方法 `shouldRetry(RetryContext): RetryOutcome`，契约禁止返回 null）、`NoRetryPolicy`（单例 pass-through 默认，恒返回 `RetryOutcome.stop()`）、`LlmErrorClassifier`（stateless 静态工具：按 `ARG_HTTP_STATUS` 把异常映射为 ErrorClassification——429→RATE_LIMITED、5xx/超时→TRANSIENT、4xx→NON_TRANSIENT、NopTimeoutException→TRANSIENT、未知→TRANSIENT）
  - `ReActAgentExecutor` 修改：新增 `retryPolicy` 字段（构造器 null 安全降级 `NoRetryPolicy`）+ Builder 方法 + 单次 LLM 调用点（`chatService.call`）重试循环包装（catch RuntimeException|Error → classify → 构造 RetryContext → policy.shouldRetry → RETRY(sleep backoff 后重试同一 request，attempt++)/STOP(rethrow 原 exception，区分 RuntimeException vs Error)/FALLBACK(无 chain 时抛 `NopAiAgentException` 记录 FALLBACK，Minimum Rules #24)）；null-guard policy 返回值；新增 `sleepBackoff(ms)` 辅助方法（0/负值 no-op，InterruptedException 重设中断标志+抛 NopAiAgentException）；llmCallStart 在重试循环内每轮重置以记录最终成功调用的耗时
  - `DefaultAgentEngine` 修改：新增 `retryPolicy` 字段（默认 `NoRetryPolicy.noRetry()`）+ `setRetryPolicy`/`getRetryPolicy`（null 安全降级）+ `resolveExecutor` 调 `.retryPolicy(this.retryPolicy)`；retry 非安全组件，无 insecure-default WARN
  - 新增 3 个测试文件（23 tests 全绿）：`TestNoRetryPolicy`（5 tests：单例 / TRANSIENT STOP / RATE_LIMITED STOP / NON_TRANSIENT STOP / QUOTA_EXCEEDED STOP）、`TestLlmErrorClassifier`（13 tests：null→NON_TRANSIENT / NopTimeoutException→TRANSIENT / 429→RATE_LIMITED / 5xx→TRANSIENT / 4xx→NON_TRANSIENT / NopException 无状态码→TRANSIENT / RuntimeException→TRANSIENT / cause chain SocketTimeout→TRANSIENT / ERR_AI_SERVICE_HTTP_ERROR+ARG_HTTP_STATUS 形状）、`TestRetryPolicyWiring`（5 tests：engine 默认 NoRetry / setter override / setter null 降级 / **默认 NoRetry 单次调用+失败 surface 零回归** / **FALLBACK fail-loud**）
  - 设计文档 `nop-ai-agent-llm-layer.md` §7：标注实现状态（plan 207 ✅ Phase 1 + Phase 2）；`nop-ai-agent-reliability.md` §3.1 调用层：标注错误分类+重试已落地
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -T 1C` → **1672 tests, 0 failures**（1649 既有 + 23 新增，零回归）
- **Phase 2 — StandardRetryPolicy 功能实现 + 端到端验证** ✅:
  - 新增 `StandardRetryPolicy`（`io.nop.ai.agent.reliability` 包）：构造器参数 `maxAttempts`（默认 3）/`baseDelayMs`（默认 1000）/`maxDelayMs`（默认 30000），含校验（maxAttempts>=1、baseDelayMs>=0、maxDelayMs>=baseDelayMs）；决策逻辑：TRANSIENT/RATE_LIMITED 在 `attempt < maxAttempts-1` 时 RETRY（指数退避 `min(baseDelay*2^attempt, maxDelay)`，overflow-safe），耗尽时 STOP；NON_TRANSIENT/QUOTA_EXCEEDED 立即 STOP；`hasStreamedContent==true` 时 STOP（流式保护，当前非流式路径恒 false，dormant）
  - 退避参数裁定：构造器参数可配置（非 XDSL，Non-Goal），默认值 maxAttempts=3/baseDelay=1000ms/maxDelay=30000ms
  - 新增 2 个测试文件（17 tests 全绿）：`TestStandardRetryPolicy`（13 tests：构造校验 4 / 默认值 / TRANSIENT 重试到 maxAttempts / maxAttempts=1 无重试 / RATE_LIMITED 重试 / NON_TRANSIENT fail-fast / QUOTA_EXCEEDED fail-fast / 流式保护 STOP / 指数退避公式 / maxDelay 封顶 / baseDelay=0 立即重试）、`TestStandardRetryPolicyEndToEnd`（4 tests：**端到端 transient 重试成功**——chatService 前 2 次 NopTimeoutException 第 3 次成功，断言 callCount=3 + status=completed；**maxAttempts 耗尽 fail-loud**——maxAttempts=2 全失败，断言 callCount=2 + status=failed；**NON_TRANSIENT fail-fast**——400 状态码，断言 callCount=1 + status=failed；**429 RATE_LIMITED 重试**——首次 429 二次成功，断言 callCount=2 + status=completed）
  - roadmap §4 Layer 3：L3-2 ❌→✅（plan 207）+ Layer 3 验收标准 LLM 故障分类 checkbox 勾选
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1689 tests, 0 failures**（1672 既有 + 17 新增，零回归）
- **Closure**：Plan Status → completed


### plan-206: nop-ai-agent 预算控制 hook（L2-22）

- **Plan**: `ai-dev/plans/206-nop-ai-agent-budget-control-hook.md`
- **Work Item**: L2-22
- **Phase 1 — 裁定 + 实现** ✅:
  - 裁定确认（无新增信息，照 Scope § 设计裁定实施）：(1) 预算快照来源 = `IBudgetProvider` 扩展点方案（非 ctx 内联 `estimatedTotalCost` 标量）；(2) 预算执行位置 = router 内部（非 decorator 包装）；(3) token-only 预算裁定 = `exceeded` 仅基于 cost（非 token），router 如需 token 预算读 `ctx.getTokensUsed()`
  - 新增 `io.nop.ai.agent.budget` 包：`BudgetSnapshot`（immutable，4 字段 estimatedTotalCost BigDecimal nullable / totalTokensUsed long / budgetLimit BigDecimal nullable / exceeded boolean；`exceeded` 在构造函数计算 `cost != null && limit != null && cost >= limit`，构造函数仅 3 参数——调用方无法传入不一致的 exceeded 值）、`IBudgetProvider`（接口，唯一方法 `getBudget(AgentExecutionContext)`，契约禁止返回 null）、`NoOpBudgetProvider`（单例 pass-through 默认，返回无限制快照 cost=null/limit=null/exceeded=false，totalTokensUsed 从 ctx 获取）
  - `AgentExecutionContext` 新增 `budgetSnapshot` 字段（nullable，初始 null）+ getter/setter
  - `DefaultAgentEngine` 新增 `budgetProvider` 字段（默认 `NoOpBudgetProvider.noOp()`）+ `setBudgetProvider`/`getBudgetProvider`（null 安全降级 NoOp）+ `resolveExecutor` 调 `.budgetProvider(this.budgetProvider)`；budget 非安全组件，无 insecure-default WARN
  - `ReActAgentExecutor`：构造函数 + Builder 接收 `IBudgetProvider`（null 安全降级 NoOp）；ReAct 循环在 `modelRouter.route()` 调用**前**刷新 `ctx.setBudgetSnapshot(budgetProvider.getBudget(ctx))`，含 null-guard（getBudget 返回 null 时抛 `NopAiAgentException`，不吞异常）；刷新点在 route() 前、在上一轮 token 累积之后
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS
- **Phase 2 — 测试 + 文档更新** ✅:
  - 新增 4 个测试文件（23 tests 全绿）：`TestBudgetSnapshot`（8 tests：cost<limit / cost>=limit 边界 / cost>limit / cost=null / limit=null / both null / token-only 不用 exceeded / equals+hashCode）、`TestNoOpBudgetProvider`（4 tests：单例 / 无限制快照 / null ctx 不抛 / 永不返回 null）、`TestInMemoryBudgetProvider`（7 tests：未超限 / 超限 / 多次 addCost 累计 / null limit 永不超限 / 未追踪 cost=null / setCost 覆盖 / addCost(null) no-op）、`TestBudgetProviderWiring`（4 tests：engine 默认 NoOp / setter override / setter null 降级 NoOp / **端到端预算降级**）
  - `InMemoryBudgetProvider`（test-only 工具，io.nop.ai.agent.budget 测试包）：可配置 budgetLimit + addCost/setCost 手动累计
  - **端到端测试** `budgetExceededTriggersModelDowngradeEndToEnd`：经 `DefaultAgentEngine.execute()`（8-arg 构造函数注入 budget-aware router + `setBudgetProvider(InMemoryBudgetProvider(limit=$1.00))`）→ ReAct 3 轮 → budget-aware router 读 `ctx.getBudgetSnapshot()`，cost 0.00→0.60→1.20，断言 routedModels = [premium, premium, cheap]（超限降级）+ 每轮 route() 时 ctx.getBudgetSnapshot() 非 null + 降级轮 snapshot.exceeded=true。证明完整路径 engine → executor → ctx → router
  - 设计文档 `nop-ai-agent-usage-and-billing.md` §3.6：标注 ✅ 已落地（plan 206 / L2-22），记录 `IBudgetProvider` 扩展点裁定 + 4 组件 + token-only 裁定 + Non-Goals；§6 P2 行标注 ✅ 已交付
  - roadmap §4 Layer 2：L2-22 ❌→✅（plan 206）+ Layer 2 验收标准新增 L2-22 checkbox 行
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → **1649 tests, 0 failures**（1626 既有 + 23 新增，零回归）
- **Closure**：独立 closure-audit subagent（task explore，read-only，fresh session）逐条核对 10 项 Closure Gates → 10/10 PASS（每项含 file:line citation）；Anti-Hollow 三项验证通过（snapshot 运行时刷新+router 可读 / engine→executor→ctx→router 完整连通 / NoOpBudgetProvider 显式 pass-through 非隐藏 gap）；Plan Status → completed



- **Plan**: `ai-dev/plans/205-nop-ai-agent-model-switched-message.md`
- **Work Item**: L2-21
- **Phase 1 — 实现 + 裁定** ✅:
  - 新增 `IModelSwitchedMessageWriter` 接口 + `NoOpModelSwitchedMessageWriter`（pass-through 默认）+ `DbModelSwitchedMessageWriter`（raw JDBC 写 `nop_ai_session_message`，role=80）+ `NopAiSessionMessageTable`（DDL + 列名常量），位于 `io.nop.ai.agent.session` 包，参照 `DbUsageRecorder` + `NopAiChatResponseTable` 模式
  - 裁定：`lastModelKey` 追踪位置 = ReAct 循环局部变量（非 `AgentExecutionContext` 字段，理由：无 fork/restore，同 `checkpointSeq` 先例）；role=80 常量 = 本地 `ROLE_MODEL_SWITCHED = 80`（附注释引用 `_NopAiDaoConstants`，同 `AiAgentSessionTable` 模式）；模型标识 = `nullToEmpty(provider) + ":" + nullToEmpty(model)` 复合键；SEQ = per-execution 内存计数器（`long[] messageSeq` 局部变量，从 1 递增）；metadata = JSON 字符串经 `JsonTool.stringify()` 写入 `metadata` CLOB 列
  - `ReActAgentExecutor` 修改：新增 `IModelSwitchedMessageWriter` 字段 + Builder 方法 + 构造参数；ReAct 循环在 `modelRouter.route()` 返回后检测模型变更（`lastModelKey != null && !currentModelKey.equals(lastModelKey) && sessionId != null`），变更时调用 `writeModelSwitched`，消息不注入 `ctx.getMessages()`
  - `DefaultAgentEngine` 修改：新增 `modelSwitchedMessageWriter` 字段（默认 `NoOpModelSwitchedMessageWriter`）+ `setModelSwitchedMessageWriter` setter + `resolveExecutor` builder 接线
  - `buildModelKey(ChatOptions)` 私有静态辅助方法：从 `ChatOptions` 提取 `provider:model` 复合键
  - **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` 通过
- **Phase 2 — 测试 + 文档更新** ✅:
  - 新增 3 个测试文件（12 tests 全绿）：`TestModelSwitchedMessage`（5 tests：首轮不产生/不变不产生/变更产生+metadata 正确/PassThrough 不产生/不注入 LLM 上下文）、`TestDbModelSwitchedMessageWriter`（5 tests：H2 写入全 NOT NULL 列/null reason+complexity/throw on null sessionId/seq 递增/NoOp 单例）、`TestDbModelSwitchedMessageWriterWiring`（2 tests：DefaultAgentEngine 端到端——功能性 router 切换模型→DB 行+metadata 正确；PassThrough 默认→0 行）
  - 设计文档 `nop-ai-agent-usage-and-billing.md` §3.5：标注实现已落地（plan 205），修正伪代码 API 漂移（`routingResult.selectedModelId` → `routingResult.getOptions().getProvider() + ':' + ...getModel()`，`ctx.lastModelId` → `lastModelKey` 局部变量）
  - roadmap §4 Layer 2：L2-21 ❌→✅（plan 205）+ 验收标准 checkbox 勾选
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → BUILD SUCCESS（全模块零回归 + 12 新 tests 全绿）
- **Closure**：Plan Status → completed



- **Plan**: `ai-dev/plans/204-nop-ai-agent-model-pricing-columns.md`
- **Work Item**: L2-19
- **Phase 1 — NopAiModel 定价列 + estimatedCost 计算** ✅:
  - 编辑 source `nop-ai/model/nop-ai.orm.xml` 的 `NopAiModel` 实体，在 `update_time`（propId=10）后追加 6 列：`input_price_per_1m`/`output_price_per_1m`/`reasoning_price_per_1m`/`cache_read_price_per_1m`/`cache_write_price_per_1m`（DECIMAL(10,4)，propId 11–15）+ `currency`（VARCHAR(3)，propId=16，`ext:defaultValue="USD"`）。DECIMAL 写法对齐 `NopAiChatResponse` 4 个 score 列先例（precision/scale/stdDataType=decimal/stdSqlType=DECIMAL）；default 写法对齐 `ai-gen.orm.xml` 的 `ext:defaultValue`
  - `./mvnw clean install -pl nop-ai/nop-ai-dao,nop-ai/nop-ai-service -am -T 1C -DskipTests` 触发再生成 → `_NopAiModel.java` 含 6 个新 PROP_NAME/PROP_ID 常量（11–16）+ getter/setter（`java.math.BigDecimal` × 5 + `java.lang.String` × 1）+ `_PROP_ID_BOUND` 11→17（验证通过）
  - `NopAiChatResponseBizModel.buildSummarySql` 改造：追加 `LEFT JOIN nop_ai_model m ON r.model_id = m.id` + `SUM(r.prompt_tokens * m.input_price_per_1m / 1000000 + r.completion_tokens * m.output_price_per_1m / 1000000) AS estimated_cost`；表加别名 `r`/`m`，`GROUP BY r.model_id, r.ai_provider, r.ai_model` + `WHERE r.session_id = ?` 保持不变（解除 plan 203 的恒-null 裁定）
  - `summarizeByModel` + `ModelUsageSummary` javadoc 更新：移除"恒为 null"，改为"有定价数据且 model_id 可 join → 计算值；否则 null（两种 graceful degradation：model_id 为 null / 定价列为 null）"
- **Phase 2 — 测试 + 文档更新** ✅:
  - 扩展 `TestNopAiChatResponseSummarizeByModel`（nop-ai-service，7 tests，仍 7 tests 内更新断言）：新增 `MODEL_DDL`（`nop_ai_model` 建表含 6 定价列）+ `insertModelRow` helper；seed `m-x`（inputPrice=1.5/outputPrice=2.5，USD）+ `m-y`（定价 null）。关键：plan 203 留下的既有 `assertNull(x.getEstimatedCost(), "...until L2-19 lands")` 更新为 `assertNotNull` + `assertEquals(0, new BigDecimal("0.00105").compareTo(...))`（600×1.5/1M + 60×2.5/1M = 0.0009+0.00015）；`m-y` 组断言 estimatedCost null（定价列 null → SQL null 传播）；null-modelId 组断言 estimatedCost null（LEFT JOIN miss）。null-modelId/empty-session 既有断言在新 LEFT JOIN SQL 下回归通过
  - 设计文档 `nop-ai-agent-usage-and-billing.md` 更新：§3.2（estimatedCost 已启用计算 + 两种 null 情形）、§3.3（6 列已落地 L2-19 propId 11–16）、§3.4（实现状态含 plan 204、enhanced SQL 标注"已落地实现"、移除"待 L2-19"块）、§6 P1 表（L2-19 已交付）
  - roadmap §4 Layer 2：L2-19 ❌→✅（plan 204）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-service -am -T 1C` → TestNopAiChatResponseSummarizeByModel 7/7 全绿，BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1614 tests 全绿（零回归——定价列不改动 agent 运行时层）
- **Closure**：独立子 agent closure audit（Anti-Hollow / generated-file / 三条 estimatedCost 路径 / 回归验证）；Plan Status → completed



- **Plan**: `ai-dev/plans/203-nop-ai-agent-per-model-usage-aggregation.md`
- **Work Item**: L2-20
- **Phase 1 — ModelUsageSummary DTO + summarizeByModel + 聚合 SQL** ✅:
  - 新增 `nop-ai-dao/.../dao/dto/ModelUsageSummary.java`（@DataBean，8 字段：modelId nullable/aiProvider/aiModel/totalPromptTokens/totalCompletionTokens/callCount/totalDurationMs/estimatedCost nullable）—— nop-ai-dao 首个 DTO，新建 `io.nop.ai.dao.dto` 子包
  - `INopAiChatResponseBiz` 接口新增 `summarizeByModel(@Name("sessionId") String, IServiceContext)`（@BizQuery）—— BizModel public 方法同步到 I*Biz 契约（service-layer 强制规则）
  - `NopAiChatResponseBizModel` 实现 `summarizeByModel`：`StringHelper.isBlank(sessionId)` → 抛 `NopException(ERR_AI_SESSION_ID_REQUIRED)`（不静默返回空，Minimum Rules #24）；否则 `orm().findAll(buildSummarySql(sessionId), ROW_MAPPER)`
  - 聚合 SQL（`buildSummarySql` package-private，BizModel 与测试共享避免漂移）：`SELECT model_id, ai_provider, ai_model, SUM(prompt_tokens) AS total_prompt_tokens, SUM(completion_tokens) AS total_completion_tokens, COUNT(*) AS call_count, SUM(response_duration_ms) AS total_duration_ms FROM nop_ai_chat_response WHERE session_id = ? GROUP BY model_id, ai_provider, ai_model`，bind 变量经 `SQL.param0`
  - `ROW_MAPPER = BeanRowMapper.of(ModelUsageSummary.class, true)`（snake_case → camelCase，H2 大写列名经 `StringHelper.camelCase` 先 toLowerCase 再处理）
  - 设计裁定落地：`estimatedCost` 恒 null（L2-19 定价列未落地，不 join 定价列）；GROUP BY 含 `ai_provider`+`ai_model`（model_id 可能为 null 时按 provider+model 独立成组）
- **Phase 2 — 测试 + 文档更新** ✅:
  - 新增 `nop-ai-service` 模块首个测试 `TestNopAiChatResponseSummarizeByModel`（7 tests）：H2 + `IJdbcTemplate`（经 `JdbcFactory.newJdbcTemplate(newTransactionTemplate(dataSource))`，同 `TestJdbcCheckpointStorage` 模式），seed 7 行（session-A: model-X×3+model-Y×2+null-modelId×1；session-B: 1 行）。断言：(1) session-A 返回 3 组；(2) model-X SUM/COUNT 正确（600/60/3/6000）；(3) model-Y 正确（200/20/2/2000）；(4) null model_id 独立成组（provider=unknown）；(5) session-B 行不泄漏进 session-A；(6) 空 session 返回空列表；(7) null/blank sessionId 抛 `ERR_AI_SESSION_ID_REQUIRED`。测试复用 BizModel 的 `buildSummarySql`+`ROW_MAPPER`（同 `ISqlExecutor.findAll` 契约，验证生产 SQL+映射逻辑，非 stub）
  - 设计文档 `nop-ai-agent-usage-and-billing.md` §3.2 + §3.4 + §6 P1：标注 summarizeByModel 已落地（plan 203）+ estimatedCost 恒 null 裁定 + aiProvider 补充为 GROUP BY 键 + 区分"已落地 SQL"与"L2-19 后增强 SQL"
  - roadmap §4 Layer 2：L2-20 ❌→✅（plan 203）；L2-20 验收 checkbox 勾选
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-service -am -T 1C` → 7 new tests 全绿，BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1614 tests 全绿（零回归）
- **Closure**：独立子 agent closure audit PASS（task ses_1333a89e2ffeV0FG1rZiWVBi2o，8/8 checks PASS，含 Anti-Hollow/anti-silent-skip/generated-file 检查）；Plan Status → completed



- **Plan**: `ai-dev/plans/202-nop-ai-agent-db-usage-recorder.md`
- **Work Item**: L2-18
- **Phase 1 — DbUsageRecorder 实现 + LLM 调用耗时计量** ✅:
  - 新增 `usage/NopAiChatResponseTable.java` 常量类（参照 `AiAgentSessionTable` 模式）：表名 `nop_ai_chat_response` + 16 列名常量 + DDL CREATE TABLE IF NOT EXISTS（列定义对齐 `_NopAiChatResponse.java`，4 个 score 列 nullable 不写）
  - 新增 `usage/DbUsageRecorder.java`（implements IUsageRecorder，raw JDBC，不引用 nop-ai-dao）：
    - `DataSource` 构造器注入 + 防御性 `initSchema()`（与 DBSessionStore 一致）
    - `record()`：modelId 解析（SELECT `nop_ai_model` 按 provider+model_name，列名以 `_NopAiModel.java` 为准；找不到/null → model_id null，graceful degradation）→ INSERT 16 列（UUID 主键）
    - SQL 异常包装为 `NopAiAgentException`（不吞异常）；try-with-resources + 无共享可变状态（线程安全）
  - `ReActAgentExecutor`：`chatService.call()` 前捕获 `llmCallStart`，构造 UsageRecord 时 `setResponseDurationMs(System.currentTimeMillis() - llmCallStart)`
- **Phase 2 — 测试 + 文档更新** ✅:
  - 新增 2 个测试（9 tests）：
    - `TestDbUsageRecorder`（7）：行写入+字段匹配、modelId 找到/未找到/null provider、null duration、SQL 异常包装 NopAiAgentException、null record/null DataSource 防御
    - `TestDbUsageRecorderWiring`（2）：**端到端反空心验证**（注入真实 DbUsageRecorder 到 DefaultAgentEngine，跑完整 ReAct loop，SELECT 断言 DB 行数=2=LLM 调用数、response_duration_ms>0、model_id 解析=mdl-e2e-1）
  - 设计文档 `nop-ai-agent-usage-and-billing.md` §3.1 + §3.2 + §6 P0 表标注 DbUsageRecorder 已落地（raw JDBC，不依赖 nop-ai-dao）
  - roadmap §4 Layer 2：L2-18 ❌→✅（plan 202）；L2-17~L2-18 验收 checkbox 勾选
  - 修复 plan 202 中 17 处 broken link（shorthand 路径 → 全路径），plan 文件 doc-link-check 清零
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1614 tests 全绿（1605 baseline + 9 new，零回归）
- **Closure**：独立子 agent closure audit PASS（task ses_133619b13ffepxbjz0RzjkrZ4g）；`check-plan-checklist.mjs` 退出 0；`scan-hollow-implementations.mjs --severity high` 退出 0。Plan Status → completed



- **Plan**: `ai-dev/plans/201-nop-ai-agent-usage-recorder-interface.md`
- **Work Item**: L2-17
- **Phase 1 — 接口定义 + 数据对象 + NoOp 默认 + 引擎接线** ✅:
  - 新增包 `io.nop.ai.agent.usage`，3 个文件：
    - `IUsageRecorder` 接口（仅 `void record(UsageRecord)`，裁定移除 `summarizeByModel` → L2-20 BizModel，no-hollow-contract）
    - `UsageRecord` 数据对象（@DataBean；sessionId/agentName/requestId/modelId/aiProvider/aiModel/promptTokens/completionTokens/responseDurationMs/responseTimestamp；modelId 与 responseDurationMs 在 agent 运行时层为 null，由 L2-18 recorder 解析）
    - `NoOpUsageRecorder` pass-through 默认（singleton + `noOp()` 静态工厂，与 NoOpCheckpoint 模式一致）
  - `DefaultAgentEngine`：新增 mutable `usageRecorder` 字段（默认 NoOpUsageRecorder）+ `setUsageRecorder` setter（null 兜底 NoOp）+ getter + `resolveExecutor` Builder 链 `.usageRecorder(this.usageRecorder)`（第 26 个组件）
  - `ReActAgentExecutor`：新增 `usageRecorder` final 字段 + 构造器参数（末位）+ null 兜底 + Builder 字段 + setter + `build()` null 兜底
  - ReAct 循环 token 累积点（`ReActAgentExecutor` 原 652-660）：在 `ctx.setTokensUsed(...)` 之后构造 `UsageRecord` 并调用 `usageRecorder.record(...)`；字段来源：sessionId←ctx、agentName←agentModel、requestId←response、aiProvider/aiModel←routedOptions、tokens←response usage、timestamp←currentTimeMillis
  - 新增 3 个 focused 测试（10 tests）：
    - `TestNoOpUsageRecorder`（3）：singleton 身份 + record() 不抛异常 + null 容错
    - `TestUsageRecord`（3）：字段 round-trip + nullable 默认 + token 默认 0
    - `TestUsageRecorderWiring`（4）：NoOp 默认接线 + setter override + null 兜底 + **端到端反空心验证**（注入 RecordingUsageRecorder test-double，跑完整 ReAct loop，断言 record() 调用次数 = LLM 调用次数 = 2，UsageRecord 字段正确：sessionId/agentName/requestId/aiProvider=test-provider/aiModel=test-model/promptTokens/completionTokens 全部匹配）
  - 设计文档 `nop-ai-agent-usage-and-billing.md` §3.2 标注 `summarizeByModel`/`ModelUsageSummary` 裁定移至 L2-20 BizModel
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1605 tests 全绿（1595 baseline + 10 new）
- **Phase 2 — Roadmap 更新** ✅:
  - roadmap §4 Layer 2 表格 L2-17 从 ❌ → ✅，标注 plan 201
- **Closure**：Plan Status → completed

### plan-200: nop-ai-agent Layer 2/3 全套 Secure 默认实现（NoOp/PassThrough → Default*，L23-SDI）

- **Plan**: `ai-dev/plans/200-nop-ai-agent-layer23-secure-default-impls.md`
- **Work Item**: L23-SDI
- **Phase 1 — Secure-Default 语义裁定与设计落档** ✅:
  - 6 条裁定写入 design doc `nop-ai-agent-security-and-permissions.md` 新增 §4.9：
    - (1) DefaultSecurityLevelResolver 采用 trusted-by-default 变体（trusted+highImpact → ELEVATED；untrusted+highImpact → RESTRICTED），解决 shell.exec 被分类为 RESTRICTED 导致引擎不可用的冲突
    - (2) DefaultPermissionMatrix 采用 §5.3 表 + usability-safe null channel（null/unknown → STANDARD+ELEVATED，deny RESTRICTED only），平衡安全与可用性
    - (3) DefaultDenialLedger 纯内存 threshold=3 计数（ConcurrentHashMap），anonymous session 不计数
    - (4) DefaultPostDenialGuard 新建独立类（委托 FingerprintPostDenialGuard，命名一致）
    - (5) WARN 策略迁移到 always-checked（构造期传非 null 给 4 个组件）
    - (6) 向后兼容处理原则（显式 opt-in NoOp/PassThrough + 注释 / 使用非 high-impact 工具名）
  - No owner-doc update required for `docs-for-ai/`（本模块无独立 owner doc 章节）
- **Phase 2 — 实现 Default* 类 + 切换 engine 默认 + WARN 迁移** ✅:
  - 新增 4 个 Default* 类：`DefaultSecurityLevelResolver`（trusted-by-default 规则表）/ `DefaultPermissionMatrix`（§5.3 channel×level + usability-safe null）/ `DefaultDenialLedger`（ConcurrentHashMap threshold=3）/ `DefaultPostDenialGuard`（委托 FingerprintPostDenialGuard）
  - 更新 `DefaultAgentEngine`：4 个字段默认值 + 4 个 setter null 兜底 + 构造期 warnIfInsecureDefaults 传非 null（always-checked 迁移）+ Javadoc 更新
  - 更新 `ReActAgentExecutor`：4 个构造器 null 兜底切换到 Default*
  - 更新 `ISecurityLevelResolver` / `IPermissionMatrix` / `IDenialLedger` / `IPostDenialGuard` 接口 Javadoc 标注新 Default* 默认
  - 更新 `warnIfInsecureDefaults` 方法体 WARN 文案（指向 Default* 替代 NoOp/PassThrough）
  - 受影响既有测试 19 failures 全部修复（按 Phase 1 裁定原则：显式 opt-in NoOp/PassThrough + 注释，或适配 Default* 行为）
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1563 tests 全绿
- **Phase 3 — 测试更新与新增 focused 验证 + 文档/roadmap 同步** ✅:
  - 新增 `TestDefaultSecurityLevelResolver`：8 tests（trusted-standard / trusted-highImpact-elevated / trusted-writes-elevated / untrusted-highImpact-restricted / untrusted-network-restricted / untrusted-elevated / null-hints / normalization）
  - 新增 `TestDefaultPermissionMatrix`：5 tests（standard-all-channels / elevated-except-group / restricted-except-webui / operator-bypass / denial-reason）
  - 新增 `TestDefaultDenialLedger`：7 tests（accumulate / threshold / pause / reset / per-session / anonymous / default-threshold）
  - 新增 `TestDefaultPostDenialGuard`：6 tests（first-call / blind-retry-blocked / different-params / reset / anonymous / per-session）
  - 新增 `TestLayer23SecureDefaultImpls`：6 tests（default-wiring / end-to-end-restricted-layer2-deny / end-to-end-trusted-allowed / end-to-end-denial-pause / end-to-end-blind-retry-block / end-to-end-audit-event）——覆盖端到端 dispatch 路径（Minimum Rules #22, #23）
  - 更新 7 个既有测试适配新 Default* 默认（`TestDefaultAgentEnginePermissionMatrixWiring` / `TestDefaultAgentEngineSecurityLevelResolverWiring` / `TestDispatchPathDenialLedger` / `TestDispatchPathPostDenialGuard` / `TestDispatchPathSecurityConsultation` / `TestLayer23SecureDefaults` / `TestStickyPauseRecovery` / `TestDBDenialLedgerEngineWiring`）
  - roadmap §5b 新增 `L23-SDI` 行：`✅ 已修复`，标注 plan 200
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 1595 tests 全绿（+32 new tests）
- **Closure**：Plan Status → completed；Closure Audit 由独立 subagent 执行

### plan-199: nop-ai-agent Layer 2/3 默认收敛（AutoApproveGate 收紧 + WARN 可见性扩展，[13-4]）

- **Plan**: `ai-dev/plans/199-nop-ai-agent-layer23-secure-defaults.md`
- **Work Item**: [13-4] / AUDIT-13-04
- **Phase 1 — Layer 2/3 收紧策略裁定与设计落档** ✅:
  - 6 条裁定写入 design doc `nop-ai-agent-security-and-permissions.md` 新增 §4.8：
    - (1) 引入 `DefaultApprovalGate` 作为 engine 默认（STANDARD/ELEVATED 批准，RESTRICTED 拒绝 = defense-in-depth），保留 `AutoApproveGate` 为 public opt-in（与 plan 193 `AllowAll*` 模式一致）
    - (2) ELEVATED 仍由默认批准（"确认" ≠ "人类审批"）
    - (3) WARN 噪音策略：AutoApproveGate 行为已变更（构造期+setter 检查），其他 4 个 NoOp/PassThrough 默认仍 insecure（仅 setter 检查，构造期不检查 = 避免每次构造噪音）
    - (4) WARN 调用点：构造器 + 全部 Layer 2/3 setter 赋值后调用 `warnIfInsecureDefaults`
    - (5) 向后兼容：默认 NoOpSecurityLevelResolver 恒返回 STANDARD，RESTRICTED 不可达，既有测试不受影响
    - (6) ReActAgentExecutor.Builder 默认值一致性
  - 拒绝 3 个替代 WARN 策略（照搬 opt-in / 构造期摘要式 / 仅覆盖 AutoApproveGate），理由落档
  - No owner-doc update required for `docs-for-ai/`（本模块无独立 owner doc 章节）
- **Phase 2 — 实现 AutoApproveGate 收紧与 WARN 扩展** ✅:
  - 新增 `security/DefaultApprovalGate.java`：STANDARD/ELEVATED → approve("default")，RESTRICTED → deny(OTHER, reason)，documented defense-in-depth
  - 更新 `security/AutoApproveGate.java` javadoc：标注为 opt-in insecure option（行为未变，仅文档）
  - 更新 `engine/DefaultAgentEngine.java`：approvalGate 字段默认 → `new DefaultApprovalGate()`；`setApprovalGate(null)` 兜底 → `new DefaultApprovalGate()`；`warnIfInsecureDefaults` 扩展为 8-arg 覆盖全部 8 个安全组件（3 Layer1 + 5 Layer2/3）；全部 Layer 2/3 setter（setApprovalGate/setSecurityLevelResolver/setPermissionMatrix/setDenialLedger/setPostDenialGuard）赋值后调用 warnIfInsecureDefaults；构造期仅对 AutoApproveGate（行为已变更）+ Layer1 checker + auditLogger 触发 WARN，不对 4 个 unchanged-insecure-default 组件触发
  - 更新 `engine/ReActAgentExecutor.java`：Builder approvalGate null 兜底 → DefaultApprovalGate；dispatch path javadoc 更新
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 全绿（既有测试零回归）
- **Phase 3 — 测试更新与新增 focused 验证 + 文档/roadmap 同步** ✅:
  - 新增 `TestDefaultApprovalGate.java`：6 tests（restrict-deny / standard-allow / elevated-allow / deny-kind / singleton-free / null-safety）
  - 新增 `TestLayer23SecureDefaults.java`：7 tests（default-is-DefaultApprovalGate / warn-on-AutoApproveGate-setter / warn-on-4-NoOp-setters / no-warn-on-default-construction / no-warn-on-functional-setters / end-to-end-restricted-denied / end-to-end-restricted-audit-event）
  - 更新 `TestAutoApproveGate.java`：补 javadoc 标注 opt-in 行为（测试用例不变，AutoApproveGate 行为未变）
  - roadmap §5b 新增 `AUDIT-13-04` 行：`✅ 已修复`，标注 plan 199
  - **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` → 全绿
- **Closure**：Plan Status → completed；Closure Audit 由独立 subagent 执行
