
### 2026-08-30（页面批量导出工具 WebPageExporter——分支工作树同源落地）

- 同 master 工作树的 WebPageExporter 变更集（`PageProvider` flux 回退 + `PageExportOptions`/`PageExportResult`/`WebPageExporter` + `TestWebPageExporter` + fixture + demo `render-pages.xrun` + cli.md）移植到本工作树（两树该文件族 diff 仅本变更集），并从本工作树 `mvn -pl nop-frontend-support/nop-web -am install -DskipTests` 恢复 m2 一致性。
- 原因：ERP 基线（2026-08-28 绿）的平台 jar 由本分支构建（`SnowflakeSequenceGenerator` 正确拼写）；master 为 `SnowflakeSequenceGeneator`（commit 69835f1fb2）。从 master `-am install` 会破坏与 m2 中本分支构建的 sys-dao 等jar 的二进制兼容（NoClassDefFoundError）。
- 验证：ERP `ErpAllFluxPagesExportTest`（本工具的 ERP 侧消费方）999 页导出 0 失败、3 个 flux.yaml 孪生页面导出==getPage 全等。单元测试与详细记录见 master 工作树 `ai-dev/logs/2026/08-30.md` 与跨仓计划 nop-app-erp `docs/plans/2026-08-30-1126-1-flux-page-export-and-validation.md`。
