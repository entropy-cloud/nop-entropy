# Dev Log 2026-08-30

### 2026-08-30（页面批量导出工具 WebPageExporter：flux 回退对齐 + manifest + 失败收集）

- **背景**：跨仓计划 nop-app-erp `docs/plans/2026-08-30-1126-1-flux-page-export-and-validation.md`（主计划挂 ERP 仓，熵仓不另建仓内计划；配套架构契约见该仓 `docs/architecture/flux-page-export-and-validation.md`）。目标：提供通用「全部页面导出为 JSON」内置工具，导出语义与生产 `PageProvider__getPage` 对齐，供 JS 侧 flux 编译验证消费。
- **修改**：
  - `nop-frontend-support/nop-web/src/main/java/io/nop/web/page/PageProvider.java` — `renderPageTo` 在 flux 模式下应用与 `PageModelLoaderFactory` 相同的 flux.yaml 资源回退（新增 `resolveFluxFallback`），修复 `renderPagesTo` 导出绕过回退导致手写 flux.yaml 页面产物失真的缺陷；输出文件名仍按原始 page.yaml 路径。
  - `nop-frontend-support/nop-web/src/main/java/io/nop/web/page/PageExportOptions.java`（新增）— 导出选项，缺省对齐生产语义（`useResolver=true`、`resolveI18n=true`、`transformPermissions=true`、`threadCount=1`）。
  - `nop-frontend-support/nop-web/src/main/java/io/nop/web/page/PageExportResult.java`（新增）— 导出结果（pages/failedPages/manifestFile）。
  - `nop-frontend-support/nop-web/src/main/java/io/nop/web/page/WebPageExporter.java`（新增）— 编排器：render-mode 切换 + xlib/xpage 双清、enabled-modules × pattern 枚举、单页失败收集不中断、`manifest.json` 落盘（generatedAt/renderMode/pattern/locale/pageCount/pages/failedPages）。
  - `nop-runner/nop-cli/demo/scripts/render-pages.xrun` — 由裸调 `renderPagesTo` 改为 `WebPageExporter` 薄包装，新增可选 `renderMode` 参数（经 `$scope.containsValue` 判存，XLang 对未定义 scope 变量直接抛 `scope-var-is-undefined`，不能裸引用）。
  - `docs/dev-guide/cli.md` — render-pages 节更新：flux 用法、manifest、生产语义说明、classpath 要求、JS 侧验证 CLI 指引。
  - 测试：`nop-web/src/test/java/io/nop/web/page/TestWebPageExporter.java`（新增 4 用例）+ 测试 fixture `_vfs/nop/test/pages/test-export-fail/{good,broken}.page.yaml`。
- **验证**：
  - `mvn -pl nop-frontend-support/nop-web test` — 91 run / 0 failures / 1 skipped（既有基线形态不变；其中 `TestWebPageExporter` 4/4：flux 回退导出内容 == `getPage(path,'zh-CN')` 全等、amis 模式忽略回退、manifest 结构与计数、单页失败收集且并发 threadCount=2 与串行结果一致）。
  - `mvn -pl nop-runner/nop-cli-core test -Dtest='TestNopCli#testRenderPages+testParameter' -Djunit.jupiter.conditions.deactivate="org.junit*DisabledCondition"` — 2/2 通过（类级 `@Disabled` 需条件停用参数激活；脚本升级后 demo 导出路径与断言不变）。注意 `TestNopCli` 常规构建中被 `@Disabled` 跳过（14 skipped），该回归需显式激活执行。
  - `mvn -pl nop-frontend-support/nop-web -am install -DskipTests` — BUILD SUCCESS（新 API 已入本地 m2 供 ERP 消费）。
  - standalone `java ... NopCliApplication run` 直跑观察：main() 启动 Quarkus dev-mode（交互 UI + 仅 main classpath），demo 页面位于 `nop-cli-core` test resources（`src/test/resources/_vfs/app/demo/`），dev-mode 应用类加载器不可见 → 导出 0 页为环境特性而非工具缺陷；CLI 产品化路径是 `repackage` 把业务 `_vfs` 打包进 jar 后执行（已在 cli.md 注明）。
- **已知边界**：`SimplePathPattern` 按层匹配（`*` 不跨目录），缺省 pattern `pages/*/*.page.yaml` 与 `validateAllPages` 一致；更深目录需自定义 pattern（Ant 风格 `**` 不可用于 `findAll`，可用 `findAllByAntPath`）。
