# 2026-08-15

### nop-ai-gateway-failover 验证 run：mission-driver 独立复跑全绿（full-green verification）

- **验证命令**（mission config，`missions/nop-ai-gateway-failover.json`）：`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -q` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（17.1s）；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（02:03 min，core 369 + agent 3387 + gateway 98 全绿，新 13+2 用例实证运行：`TestRuleBasedSelectionStrategy` 13/13、`TestRuleBasedSelectionStrategyIoC` 2/2 零失败）。
- **lint 备注**：`checkstyle:check` 非有效门禁——Maven 默认 Sun 规则集，全仓既有基线大面积违规（nop-api-core 9164 项、AiCoreConstants 等既有文件同款违规），mission lint 命令自带 `|| echo 'lint not configured'` 回退；本次仅顺手修复新文件 `RuleBasedSelectionStrategy.java` 一处 trailing space，其余与基线一致不处理。
- **doc-sync 裁定**：无 owner-doc 新增（W5b 文档回填已在 closure 完成）；本日志追加本验证条目。



### nop-ai-gateway-failover W5 全量构建验证（mission-driver verify 轮）

- **full-green verification**：`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am` PASS；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（19s）；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（01:56 min，core/agent/gateway 全绿）；checkstyle 无有效门禁（既有 9164 条 nop-api-core 遗留基线，`-Pqa` 项目配置下 0 violations，`echo 'lint not configured'` 兜底与 W2-W5 记录一致）；`check-doc-links.mjs --strict` exit 0（0 errors）；`check-plan-checklist.mjs` exit 0。
- **doc-sync 裁定**：本条目为 verify 轮验证记录；W5 交付与 closure 见下方条目。


### nop-ai-gateway-failover W6 排障：流式 out-of-window 测试偶发 flaky 根因 = 上游 JDK SubmissionPublisher 竞态（非适配器缺陷），测试改确定性

- **现象**：`TestChatServiceFailoverAdapterStreaming#outOfWindowFailureBreaksStreamWithoutSwitch` 偶发（~5-10%）：断言 12 元素实收 11 或 0（并伴随 streamCalls=2 重订阅）。
- **排查**：flow/fake 临时插桩 + 隔离复现 `ScratchSubmissionRace`（真实 SubmissionPublisher + MAX demand + 同线程 12 连发 + closeExceptionally）→ **实证 JDK 竞态：closeExceptionally 与同线程 submit 下仅前 4 元素到达订阅者（tail 丢弃）；正常 close() 12/12 无损**。
- **定性**：适配器行为正确（已转发 → 断流报错；未转发仅缓冲 → 窗口内透明重订阅 + Major-1 丢弃缓冲）；丢元素发生在被包装的 `ChatServiceImpl` 内部（Non-Goal 不改）。测试原先的精确计数断言（12）对上游竞态过度指定。
- **修复**：out-of-window 测试 `adapter.setBufferSize(2)`——越窗在"必然到达的前 4 元素"内确定性发生；断言下限（texts ≥ 3 + c1/c2 前缀）+ 不切换（streamCalls=1）+ 计数归零。12/12 循环稳定，全模块复跑绿。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 369 + gateway 130 + agent 3387）。
- **doc-sync 裁定**：plan Phase 4 落地记录已补上游竞态实证条目；本日志已更新。

# 2026-08-15


### 2026-08-15（D2-4 EXECUTE Phase 3 — 闭环 E2E 与终稿校对，全绿）

- 新增 **TestNopDatavDashboardFilterDefE2E**（2 用例，rule #22 + #23）：
  - testFluxDefinitionToDashboardDataClosedLoop：paramConfig（四类参数 region=string/dropdown、
    minAmount=number、saleDate=date、period=date-range）→ `exportDashboardFilter` 产出契约工件
    （字段/control/delimited initialValue/dateRangeValue 元数据定向断言）→ 按定义模拟 flux 提交
    （period 以契约 delimited 形态）→ `resolveFilterValues` 拆分为扁平 key → `getDashboardData`
    （flux-form 参数直传，内部 resolve 自动接受 delimited）→ 断言四类参数全部注入 SQL
    （region 等值/AMOUNT>=minAmount/SALE_DATE>saleDate/period 范围 → 2 行精确行集）+
    默认值路径（工件 initialValue 回放 → 4 行）。
  - testFilterValuesChangeDashboardDataResults（接线验证）：同一看板三种提交
    （region/period/minAmount 变化）→ 行数 2/4/1 与行内容（west 提交只含 west 行）随筛选值真实变化，
    非仅参数透传。
- **验证**：`./mvnw test -pl nop-datav -am -T 1C` 全绿 — datav-service **557/0/0**（530 既有 +
  27 新增：12 exporter + 8 resolver delimited + 5 API + 2 E2E）；`echo 'typecheck not configured' &&
  ./mvnw clean install -pl nop-datav -am -T 1C -DskipTests && echo 'lint not configured'` BUILD SUCCESS。
- 终稿校对：`linkage-design.md` §十一 与实现逐项一致（工件结构/映射表/错误码/delimited 优先级/
  半开默认省略/空定义/无 options 均有定向测试锚点），无偏差回写。
- Doc-sync: 本条目；plan Phase 3 勾选。


### 2026-08-15（D2-4 EXECUTE Phase 2 — 筛选定义产出 API 与形态映射，全绿）

- **产出 action**：`NopDatavDashboardBizModel.exportDashboardFilter`（`@BizQuery` + `@Auth
  NopDatavDashboard:exportDashboardFilter`，admin/user，orderNo 10018）+ 接口 `INopDatavDashboardBiz`
  登记；读取编辑态 live `paramConfig` → `DashboardParamParser` → 新增纯函数
  `io.nop.datav.service.filter.DashboardFilterDefExporter` 产出 §11.2 工件
  （`{dashboardId, fields[], dateRangeValue}`；fields 顺序=声明顺序；无参数看板空定义；不含 options）。
  权限点登记生产 action-auth（orderNo 10018）与测试 auth 两处；`_vfs/i18n/en/_nop-datav-web.i18n.yaml`
  由 codegen 从源 `i18n-en:displayName` 再生（源→生成链路正确，随源提交）。
- **类型/widget 映射**：`DashboardParamDefinition` 钉死常量（widget 接受集 {dropdown, date-picker}、
  control 产出集 {select,input-text,input-number,input-date,date-range}、delimiter ","/valueFormat
  yyyy-MM-dd）；`DashboardFilterDefExporter.resolveControl` 按 §11.3 映射表落地（未声明走类型缺省；
  词表外 → `ERR_DATAV_FILTER_DEF_UNKNOWN_WIDGET`；组合非法 → `ERR_DATAV_FILTER_DEF_WIDGET_TYPE_MISMATCH`，
  无静默回退）；`resolveInitialValue` 产出侧校验 defaultValue（date-range 两段均须 yyyy-MM-dd、
  date/number 同理，非法 → `ERR_DATAV_INVALID_PARAM_CONFIG`），date-range 合并 delimited 串、
  半开/缺省默认省略 initialValue。
- **date-range delimited 接受形态（additive 向后兼容）**：`DashboardFilterResolver.resolveDateRange`
  在两个扁平 key 均缺省时消费 `paramName='start,end'` 标量（恰好一个 delimiter，两段经既有 ISO 校验，
  relative token 天然拒绝；数组/非字符串显式 mismatch）；扁平 key 仍 canonical 且优先；
  `getDashboardData`/`parseFilterFromUrl` 经委托自动获得该形态。既有扁平 key 行为不变。
- **新增测试 25 用例全绿**：`TestDashboardFilterDefExporter`（12：四类缺省/覆盖映射、未知与组合
  非法显式报错、delimited 初值、半开/无默认省略、defaultValue 产出侧校验、工件结构与顺序、空定义、
  dateRangeValue 元数据、无 options 键）；`TestDashboardParamParserAndResolver` +8（delimited 接受、
  扁平优先、无 delimiter/三段/relative/非日期段/数组形态拒绝、单侧扁平存在时标量忽略）；
  `TestNopDatavDashboardFilterApi` +5（exportDashboardFilter biz 代理接线/空定义/未知 widget、
  resolveFilterValues delimited、parseFilterFromUrl URL delimited 兼容）。
- **验证**：`./mvnw test -pl nop-datav -am -T 1C` 全绿（新增 12/29/14 三类 0 失败）；
  `echo 'typecheck not configured' && ./mvnw clean install -pl nop-datav -am -T 1C -DskipTests &&
  echo 'lint not configured'` BUILD SUCCESS。
- Doc-sync: 本条目；plan Phase 2 勾选。


### 2026-08-15（D1-4 收口 — closure audit CLOSABLE，Plan Status → completed）

- 独立子 agent closure audit（task `ses_ffc1ae023ffegQgXqM6T8FKhUf`，fresh session）判定 **CLOSABLE**：
  A（Phase 1 契约 §九 11 项裁定全核对）、B（导出 action + 双 auth 登记 + 13 测试）、C（保存
  reconcile 三路径 live code 行号级核对 + 7 类失败路径错误码精确对上 + 零落库追踪 + 14 测试）、
  D（E2E 3 测试：roundtrip 判据/no-op 幂等/真实 SQL 接线/发布回归）、E（Anti-Hollow：真实 bean
  30/30 实际调用、无空壳、审计者独立复跑 fresh surefire）、F（一致性：orm.xml 无改动、合成规则/
  props 全量替换/source 回显三处 spot-check 与 §九吻合）、G（deferred 诚实性）、H（三工具命令
  独立复跑退出码 0）全 PASS。
- 收口三件套：`check-plan-checklist.mjs --strict` 退出码 0（全部勾选 + Closure Evidence 已写入）；
  `scan-hollow-implementations.mjs` 退出码 0（0 findings）；Plan Status → completed。
- Roadmap：D1-4 `planned`→`done`（附 plan 路径与交付摘要），`Last updated` 前置本条目。
- 构建副产物：`_vfs/i18n/en/_nop-datav-web.i18n.yaml`（生成物）由 codegen 从 action-auth 源
  `i18n-en:displayName` 再生（源→生成链路正确，随源提交）。
- Doc-sync: 本条目；plan Closure 段；roadmap D1-4 行。


### 2026-08-15（D1-4 EXECUTE Phase 2–4 — 导出/保存回写 API + roundtrip E2E，全绿收口）

- **Phase 2 导出 API**：`NopDatavDashboardBizModel.exportDashboardLayout`（`@BizQuery` + `@Auth
  NopDatavDashboard:exportDashboardLayout`，admin/user）+ 纯函数编解码器
  `io.nop.datav.service.layout.DashboardLayoutCodec`（buildExportLayout：类型经 `PanelTypeMapping`
  映射、几何读 layoutConfig、无几何合成（x=0/w=cols/h=4 按 sortOrder 堆叠，游标=max(y+h)）、
  props 去 dataBinding、source=datasetRef:&lt;id&gt;）。权限点登记生产 action-auth（orderNo 10016）
  与测试 auth 两处。新增测试 **TestNopDatavDashboardLayoutExport**（13 用例）：
  testExportDatasetAndTextPanels / testExportTitleFallsBackToPanelName / testExportAllTypeMappings
  / testExportUnknownPanelTypeExplicitError / testExportMultiTabFlattened / testExportLegacyNoGeometrySynthesis
  / testExportPartialGeometrySynthesisAppended / testExportStoredGeometryAndGridParams /
  testExportPropsExcludesDataBinding / testExportEmptyPanelConfigOmitsProps /
  testExportMalformedLayoutConfigExplicitError / testExportStoredGeometryMalformedExplicitError /
  testExportDashboardNotFound。
- **Phase 3 保存回写 API**：`NopDatavDashboardBizModel.saveDashboardLayout`（`@BizMutation` +
  `@Auth NopDatavDashboard:saveDashboardLayout`，admin）两阶段实现——阶段 A 纯校验/纯计算
  （parseSavePayload → max-panels 上界（新配置 `nop.datav.dashboard-layout.max-panels` 默认 50）→
  跨看板 id 检查 → layoutConfig 重建/panelConfig 合并/panelName 派生/服务端 id 生成，全部容量校验
  先于任何写入=校验失败零落库），阶段 B 写入（删除面板物理删 + 其 AlertRule 置 DISABLED 注销 cron
  （镜像删除级联）；更新（displayName←title、panelType、sortOrder=数组下标、绑定/tabId 保留）；
  新建（UUID-32 id、panelName 派生、恒无绑定）；主表 layoutConfig 更新；响应=再导出布局）。
  新增 5 错误码（ERR_DATAV_INVALID_LAYOUT / LAYOUT_DUPLICATE_PANEL_ID / LAYOUT_FOREIGN_PANEL_ID /
  LAYOUT_CONFIG_OVERFLOW / PANEL_CONFIG_OVERFLOW）。新增测试 **TestNopDatavDashboardLayoutSave**
  （14 用例）：testSavePureCreate / testSavePureDeleteDisablesAlertRules / testSaveEmptyPanelsClearsAll
  / testSaveMixedReconcile / testSaveDuplicateIdError / testSaveUnknownTypeErrorFluxHtmlAndDecorative
  / testSaveOverMaxPanelsRejected / testSaveForeignPanelIdError / testSavePayloadStructureErrors /
  testSaveLayoutConfigOverflowRejected / testSavePanelConfigOverflowRejected /
  testSavePreservesBindingTabAndDataBindingRegion / testSavePropsAbsentKeepsPanelConfig /
  testSaveGridParamsUpdateRetainAndLegacyKeysPreserved。接口 `INopDatavDashboardBiz` 登记两 action。
- **Phase 4 roundtrip E2E**：新增 **TestNopDatavDashboardLayoutRoundtripE2E**（3 用例）：
  testFullRoundtripSemanticEquivalence（导出→模拟编辑（移动/新增/删除/改标题/props 全量替换）→保存→
  再导出，按 §9.11 判据断言 + 保存响应≡再导出 + no-op roundtrip L3≡L2 幂等）、
  testGetDashboardDataConsumesSavedPanels（接线验证：保存后面板集经真实 SQL 管线返回数据行 +
  新增无绑定面板 hasDataset=false 条目）、testPublishAfterLayoutSave（发布回归：快照含保存后布局）。
  E2E 修正两处测试侧问题：paramConfig 需声明 region 参数（resolver 白名单语义，镜像既有批量 E2E）、
  H2 列名大写化改用行值断言（containsValue）。
- **验证**：`./mvnw test -pl nop-datav -am -T 1C` 全绿（nop-datav-service **530/0/0**，含新增 30 用例；
  nop-datav-web 1/1）；`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS。
  typecheck/lint not configured（echo 占位按 mission 指令执行）。
- Doc-sync: 本条目；plan Phase 2–4 勾选与 Status: completed；runtime-design.md §九 与实现一致
  （无偏差回写）。


### 2026-08-15（批量查询性能 plan-3 VERIFY — mission 构建验证）

- Mission 验证命令全绿：`./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` BUILD SUCCESS；
  `./mvnw test -pl nop-datav -am -T 1C` BUILD SUCCESS（datav-service 500/0/0）。
- 首轮 `-T 1C` 下 `TestNopDatavAuditLog.testCombinedE2E_rbacRlsAudit` 偶发失败
  （"denied rollback should still be audited"）——单独运行 5/5 绿、4 类组合绿、模块全量无 `-T` 绿、
  复跑精确 mission 命令绿。根因定位为**平台既有 race**（与本 plan 改动无关，datav diff 不触及 audit 路径）：
  `BlockingSourceConsumeTask.run()` 中 `source.drainTo`（队列→0）与 `processingCount.addAndGet(size)`
  之间存在窗口，`isAllProcessed()` 可在记录落库前返回 true，`-T 1C` 多模块并行负载下线程抢占放大该
  窗口使 `flushAudit()` 提前返回。内核并发原语（nop-commons）修复属 Protected Area（plan-first），
  本条目留档为后续修复依据。
- Doc-sync: 本条目；plan 文件收口（EXECUTE 已完成，含 closure audit evidence）。


### EXEC_PLANS Cycle 2 / I1'（续）— Phase 3 聚合入口与 CI 棘轮扩展完成

- **P4 聚合入口**：`run-nop-metadata-invariants.sh` 4→5 门禁（新 step [5/5] INV-SILENT-WRONG-RESULT 以 `--baseline baseline-cycle2/silent-wrong-result.json` 模式 b 调用，退出码语义注释齐全）；头部陈旧计数注释校正（"(130 catch blocks)" → "125 catch blocks / 45 files, comment-stripped scanner count, live 2026-08-15"，与 D3 口径一致）。
- **P5 CI 接线**：`invariant-gate` job 经聚合入口自动覆盖新门禁（workflow 零变更——job 调用的就是聚合入口，`set -e` 传播 gate 5 退出码）；复核实无 `continue-on-error`/`|| true`。
- **P5b 模式归属表**：记入 `initial-red-list-cycle2.md` 头部（gate 5 → 模式 b → baseline 61 键/67 命中零点；判定依据 = 确定性规则"快照非空 → b"）。
- **P6 owner-doc**：`docs-for-ai/02-core-guides/invariant-guards.md` 更新（4→5 门禁表、模式 b baseline 对账语义专节、棘轮扩展、复触发条件措辞）；`INDEX.md` 对应行同步（四族→五条）。注意：owner-doc 不允许引用 ai-dev/ 路径（doc-link checker BOUNDARY 规则）——快照工件以"仓库审计目录"非链接措辞指称。
- **接线双向 proof**（Cycle 1 hard-gate proof 先例）：干净树聚合入口 5 门禁全绿（exit 0，含 INV-LIMIT mvn 步）；向 `MetaContractChecker.java` 注入语法合法的 baseline 外违规（`s.toLowerCase()` 私有方法）→ guard exit 1（EXCESS: new key not in baseline）且聚合入口 exit 1（set -e 传播，CI 将红）；`git checkout` 还原 → guard exit 0；`git status --porcelain -- nop-metadata/` 空（产品 diff 净零）。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（exit 0，0 failures——本计划零产品代码变更，无 JUnit 排除配置）；`check-doc-links --strict` 修改前后差分 = issue 清单完全一致（16 pre-existing errors 不变，本次修改文件 0 新增 broken link；期间自查并修复 2 处 docs-for-ai BOUNDARY/相对路径引用 + 1 处 plan 内路径反引号误链）。


### EXEC_PLANS Cycle 2 / I1'（续）— Phase 2 门禁实现与初始 Red List 快照完成

- **P1 门禁实现**：新建 `ai-dev/tools/check-silent-wrong-result.mjs`（约 700 行，纯 Node 零依赖，与 Cycle 1 扫描器同风格）——5 规则（locale / narrowing-cast / contains-classify / delim-key / bigdec-precision）+ `--rule` 过滤 + `--baseline` 对账（逐键计数 current ≤ baseline，子集计数版语义）+ `--emit-baseline`（快照生成）+ 放行注释 `// invariant-ok:`（识别在注释剥离前，命中移出违规集与对账输入、显式列入 Allowed 节——无静默盲区）+ 括号失衡显式报告（INTERNAL hit，不静默跳过）+ 退出码 0/1/2（baseline 文件缺失/损坏 = 2 显式报错非静默通过）。bigdec 规则以「所在方法体含 `.longValue()` 路由信号」显式排除 `AggregationHelper:554` 受保护形态（签名启发式支持 package-private 方法——fixture 曾抓到该缺陷并修复）。
- **P2 自验证 fixture**：18/18 PASS——每规则违规/合规双向样本（16 检测样本）+ baseline 对账三态（⊆ 绿 / 超计数红 / 新键红）+ 少于 baseline 绿（渐进修复语义）+ 放行注释（violation=0, allowed=1，不产生静默盲区）。
- **P3 初始快照**：`initial-red-list-cycle2.md`（人读层，67 条逐条含 `文件:行` + 复现命令 + 历史同族 finding 关联）+ `baseline-cycle2/silent-wrong-result.json`（机器层，61 键 / 67 命中，字段 = 文件路径 + 命中行归一化文本 + 子族标签 + 计数）。首跑分布：locale 40 / narrowing-cast 0（防回退零点）/ contains-classify 19（15 行）/ delim-key 6 / bigdec 2。
- **实测发现（扫描器 > 初测 rg 粗筛）**：delim-key 初测 rg 3 处系 `|` 转义遗漏，扫描器实测 6 处（新增 `AutoClassificationProcessor:144` warnKey、`NopMetaLineageEdgeQueryAction:258/:487` 三段 `|` 拼接，其中 `:487` 为 `map.put(a+"|"+b+"|"+c, e)` 直接作 map 键——AR-03 同形）——Phase 1 文档（catalog / target-set §5.4/§5.6 / plan tick / log）已按扫描器权威口径同步为 6 处 / 67 总数。
- **验证**：确定性（两次运行 hits 完全一致）；逐规则过滤计数与目标集 §5 一致（40/0/19/6/2）；baseline 对账绿（61 键内 0 超出）、抽键后红（exit 1）、baseline 缺失显式错（exit 2）；`git diff nop-metadata/` 净零（产品代码零改动）。


### mission-driver 验证收口 + TestChannelScanBindLoginE2E VarCollector flake 根因修复

mission `nop-metadata-invariant-loop` plan 1448-3 提交后（HEAD `47baa0128`/`b70686325`，工作树 clean）的独立构建验证 run。四条门禁命令全绿，途中把反复复发的 `TestChannelScanBindLoginE2E` 并发 flake 从"重跑绕过"升级为根因修复。

- **根因（非本 mission 引入）**：`AutoTestCase.complete()`（`nop-autotest-core`）在每个 autotest 方法结束后 `VarCollector.registerInstance(null)` 清空 JVM 级静态；生产代码 `LoginApiBizModel.buildLoginResult:99` 无条件调用 `VarCollector.instance().collectVar(...)`（平台标准可测性钩子，`JunitAutoTestCase` 每 test 重新注册）。`TestChannelScanBindLoginE2E`（08-09 另一 mission 引入，plain class 自装配真实 bean、不走 autotest 生命周期）在 surefire `forkCount=1C` + `parallel=classes`（根 pom 配置）下与其它 autotest 类同 fork 时，静态已被清空 → NPE。单跑 4/4 通过（独占 fork，静态为初始非空实例），reactor 内红否取决于 fork 调度——今日 reactor 2/2 红，重跑不可靠。
- **修复（test-only，6 行）**：`TestChannelScanBindLoginE2E.setUp()` 开头补 `if (VarCollector.instance() == null) VarCollector.registerInstance(new VarCollector());` 恢复生产默认实例；四文件 login 契约（ILoginService/ILoginSpi/IAuthTokenProvider/LoginApiBizModel）零改动，Phase 0 hash 基线不受影响。
- **验证**：`./mvnw compile -pl nop-metadata -am -q` exit 0；`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS（02:27）；`./mvnw checkstyle:check -pl nop-metadata -am` → `nop-api-core` 9164 pre-existing violations exit 1（上游框架模块 0 文件改动，checkstyle 未绑定默认 lifecycle——`clean install` 全绿为证；lint not configured 裁定，同 08-14/08-15 既有记录）；`./mvnw test -pl nop-metadata -am -T 1C` → **BUILD SUCCESS（full-green：nop-auth-service + nop-metadata 全部七模块 SUCCESS，web/app 不再 SKIPPED，08:19）**，修复前该命令因 flake 连续两轮红。单跑 `-Dtest=TestChannelScanBindLoginE2E` 修复前后均 4/4。
- **偏差**：验证步骤超出"只跑命令"落了 test 修复——mission 指令 fail→diagnose→fix→rerun 的直接执行；nop-auth 变更为 test 基建鲁棒性（非认证模型/产品行为），不触发 ask-first。
- Doc-sync: No owner-doc update required——flake 根因属测试装配与 autotest 生命周期交互，`docs-for-ai/02-core-guides/testing.md` 不记载"plain E2E 类须自行恢复 VarCollector"此类 per-class 装配细节（已复核该文档范围）。


### nop-app-erp seed 引用完整性测试先决：OrmTransactionListener NPE 平台回归修复（null-guard，dual-agent-approval）

- **触发**：nop-app-erp plan `2026-08-15-2000-1`（seed 引用完整性测试）Phase 3 要求宿主 `TestAuthSeedLoadingProof` 全绿，但其自 2026-08-13（.m2 jar 重建暴露 `e5ee02b40`）起红测：`DataInitInitializer.executeSqlFiles` 在 bean 创建期提交 SQL 事务时，`nopDefaultTransactionListener.ormTemplate`（`ioc:lazy-property`，循环依赖解耦）尚未赋值 → `OrmTransactionListener.onBeforeCommit` NPE。
- **修复**：`nop-persistence/nop-orm/src/main/java/io/nop/orm/txn/OrmTransactionListener.java`——`onBeforeCommit`/`onAfterCompletion` 增 `ormTemplate == null` 守卫。语义安全：bean 创建窗口期内唯一事务提交路径为 DataInitInitializer 原始 SQL（纯 JDBC 无 ORM session 绑定），`flushSession` 无 session 时本身是 no-op，跳过与已装配后行为等价；容器启动后 `runLazyProperties()` 必已赋值，正常语义不变。beans.xml 的 `ioc:lazy-property` 循环解耦保持不变（eager 注入会重引入事务管理器↔监听器↔ormTemplate 环）。
- **批准**：nop-app-erp 2026-08-15 政策裁决下外部仓库代码 = auto + dual-agent-approval——两个独立 fresh subagent 分别批准（`ses_ffa1d76e4ffekJHb02D8qHPnuL` / `ses_ffa1d6174ffeJbtQmJSLocXXp3`，批准记录落盘于 nop-app-erp plan 文件「Cross-Repo Fix Approvals」节）。
- **验证**：`./mvnw install -pl nop-persistence/nop-orm -am -DskipTests`（.m2 jar 重建）→ nop-app-erp `mvn test -pl app-erp-all -Dtest=TestAuthSeedLoadingProof` 3/0/0 绿；app-erp-all 全量 28/28 绿。
- **相关记录**：nop-app-erp `docs/bugs/2026-08-14-0930-authseed-loading-npe-ormtransactionlistener.md`（问题登记）+ `docs/architecture/seed-data.md`「通用引用完整性校验已落地」注记。


### 2026-08-15（MISSION nop-datav DRAFT_PLANS — 2026-08-15-1913 审计批次 P0/P1 → 3 份 remediation plan）

- 审计 intake：`ai-dev/audits/nop-datav/2026-08-15-1913-multi-audit-nop-datav.md`（P0×3 + P1×12 + P2×48）与 `2026-08-15-1913-open-audit-nop-datav.md`（AR-1 P0 + AR-2/AR-3 P1 + P2×4）完整读取；全部 P0/P1 锚点（action-auth/data-auth 行号、Panel BizModel 四 action、share dirty-flush、导出异步提交、maxRows 钳制、8 UK/22 index DDL 零物化、四级联缺口、错误码英文、两测试缺口）逐一对照 live repo 复核属实。
- 起草 3 份 plan（覆盖全部 4 P0 + 14 P1，每项均为 Fix，无降级）：
  1. `ai-dev/plans/nop-datav/2026-08-15-2146-1-panel-subentity-auth-rbac-closure.md`（P0-01/02、P1-01/02/03/10 权限收口）
  2. `ai-dev/plans/nop-datav/2026-08-15-2146-2-transaction-boundary-async-resource-bounds.md`（AR-1/2/3、P1-04/05/06 事务边界与资源上界）
  3. `ai-dev/plans/nop-datav/2026-08-15-2146-3-ddl-integrity-cascade-hygiene.md`（P0-03、P1-07/08/09/11/12 数据契约物化与卫生）
- Plan review（guide 迭代对抗性审查流程）：第一轮 3 个独立子 agent 并行审查，发现并修复 plan 1：NopReportDatasetAuth 事实断言错误（实体是 nop-report 过时残骸，D4 改为 createdBy/admin 主案）+ Closure Gate 全链口径与 ChatBI executor 测试矛盾（分层口径）；plan 2：P1-06 通知移出事务与 rearm 契约冲突（改 Decision 先行三案）、P1-05 三个真实设计决策点、afterCommit API 事实（无模块先例，ITransactionTemplate 真实形态 + 无事务守护）、可见性断言不可观察（三选一 seam 口径）；plan 3：D1 事实锚点错误（重名兜底在 Screen executor 不在 Dashboard，dashboard 侧 scope 悬空收口）、存量重复数据策略缺失、:1306 UK 缺席、deleteSession 复用陷阱、i18n 目标文件。第二轮 3 个 fresh 子 agent 复审：三份 plan 全部「可转 active」共识，残留 Minor（plan 1 N1/N2、plan 2 N-1/N-2、plan 3 方法名笔误）已顺手修复。
- 三份 plan `draft → active`（Draft Review 记录于各 plan 头部）。
- P2 处置：52 项 P2 全部登记 `ai-dev/backlog/nop-datav-audit-followups.md`（#18-69，含 source audit 路径）；P2-14（名字唯一键作用域）随 plan 3 D1 消化、P2-11 随 plan 3 Phase 2 收敛、AR-7（缓存键耦合）登记为 plan 1 Phase 4 强制裁定项——均在 backlog 标注归属。
- 审计关闭：两份审计 `Audit Status: open → planned`（Remediation 行注明 plan 归属与 P2 triage 去向）。
- Doc-sync: `check-doc-links.mjs --strict` 退出码 0。


### EXEC_PLANS Cycle 3 / 执行顺序 2 — plan 2026-08-15-1913-2 Phase 4 P1-2 owner 文档 I*Biz 契约清单补齐完成（纯文档）

plan `ai-dev/plans/2026-08-15-1913-2-nop-metadata-api-write-path-and-contract-semantics.md` Phase 4（mission-driver EXEC_PLANS）。

- **Fix**：`docs-for-ai/03-modules/nop-metadata.md` "API 契约"节补入 5 个缺失接口及自定义方法签名（逐接口与 `nop-metadata-dao/.../biz/` live 代码核对）：`INopMetaDataProductBiz`（linkAsset/unlinkAsset/getLinkedAssets）、`INopMetaQualityResultBiz`（approve/reject）、`INopMetaReconciliationConfigBiz`（executeReconciliation）、`INopMetaReconciliationResultBiz`（confirmMatch/batchConfirmMatches）、`INopMetaTagLabelBiz`（propagateTags/suggestTags）——清单现覆盖全部 14 个非空接口（程序化复核：39 接口中恰 14 个含自定义 @BizQuery/@BizMutation，与源审计 R1 复核一致）；既有 9 个条目逐一与 live 签名核对零漂移。
- **Fix**：补记 `NopMetaTagLabel.xbiz` approve/reject XPL 事实源说明（approve/reject/submitForApproval/withdrawApproval 由 xbiz extends `/nop/wf/base/approval-support.xbiz` 的 XPL 提供，不在 Java BizModel / I*Biz 接口中——接口只声明 Java 侧方法）。
- **Doc-sync（Phase 3 随附）**：同一节补 DataProduct 挂链语义条目（聚合根校验 + 属主管线，P1-4/P1-5 行为变更随本 Phase 清单一并同步，按 plan 不单独设条目）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 16 errors = 既有基线（本次修改文件 0 命中，与 Cycle 3 前序计划收口记录一致）。
- No new test required: 纯文档变更（`TestNopMetaBizInterfaceCompleteness` 已覆盖代码侧三方闭合）。


### EXEC_PLANS Cycle 3 / 执行顺序 2 — plan 2026-08-15-1913-2 Phase 3 P1-4+P1-5 DataProduct 挂链聚合根校验+属主管线完成

plan `ai-dev/plans/2026-08-15-1913-2-nop-metadata-api-write-path-and-contract-semantics.md` Phase 3（mission-driver EXEC_PLANS）。

- **Decision（entityId 校验裁定）**：linkAsset 的 `entityId` 不做按 entityType 分派的存在性校验——TagLabel 是通用标注表（suggestTags/propagateTags 同样以 `(entityType, entityId)` 引用资产），资产引用完整性属标注域通用语义；linkAsset 侧由 `LINKABLE_ASSET_TYPES` 白名单治理 entityType，entityId 保持不透明引用（与既有模块行为一致）。代码注释 + 本条目记录。
- **Fix（P1-4 聚合根校验）**：`linkAsset`/`unlinkAsset` 入口对 `dataProductId` 做 `requireEntity`（沿 executeReconciliation 惯例）——不存在/伪造 ID 显式抛 `nop.err.dao.unknown-entity`（携带 NopMetaDataProduct + id，聚合根层），与标签层 `nop.err.metadata.link-asset-not-found` 语义区分；"存在产品但无标签"场景错误码保持不变。修复前任意（含不存在）dataProductId 均成功创建/删除 linkage 行（unlinkAsset 对不存在产品误报"标签不存在"）。
- **Fix（P1-5 属主管线）**：`linkAsset` 跨聚合创建改走 TagLabel 属主 save 管线（`bizObjectManager().getBizObject("NopMetaTagLabel").invoke("save", Map.of("data", data), null, context)`，沿 `NopMetaGlossaryTermBizModel#syncTagLabels` 先例）——Automated 标签经 `triggerApprovalIfNeeded` 获得 `state=Suggested` + 自动提审（`wf:wfName=tagLabelConfirmApproval`，fail-loud）；不再手工 setState 直写 DAO（旧路径 Automated 标签永不进审批流，与 TagLabel BizModel save 管线行为分裂）。dedup 前置检查保留在 DataProductBizModel（plan 明示允许）。
- **Proof**：新增 `TestNopMetaDataProductLinkAssetAggregateRoot`（4 例）——① linkAsset 对不存在 dataProductId 抛聚合根错误（非 link-asset 层）+ 零孤儿行；② unlinkAsset 对不存在 dataProductId 抛聚合根错误（非 ERR_LINK_ASSET_NOT_FOUND——错误语义修正钉死）；③ 存在产品但无标签 → ERR_LINK_ASSET_NOT_FOUND 保持；④ **接线验证**：linkAsset 产物 `state=Suggested` + `approveStatus=SUBMITTED`（SUBMITTED 只可能经属主管线的 submitForApproval 产生——直写 DAO 路径恒 null）。既有 `TestNopMetaDataProductLinkAsset` 6 例随行为升级：linkAsset 调用统一经带真实用户的 ServiceContext（wf 启动需真实操作人，沿 TestNopMetaTagLabelApproval.ensureUser 注记；owner 管线下匿名上下文会 fail-loud，这是 P2-09 裁定的管线真实行为）。
- 发现（非本 plan 范围）：GlossaryTerm 传播路径（syncTagLabels/propagateFromGlossaryTerm）在匿名上下文下 submitForApproval 同样失败，但其 try/catch 吞掉后标签仍在同一事务提交（label 落库 + approveStatus=null）——预存行为，见 roadmap Follow-up 判断是否登记。
- 验证：`./mvnw test -pl nop-metadata -am -T 1C -o` BUILD SUCCESS（全模块 reactor 全绿，含升级后 6+4 linkAsset 测试）。
- Doc-sync: linkAsset/unlinkAsset 行为变更（聚合根校验语义）随 Phase 4 owner-doc 清单一并同步（plan 明示不单独设条目）。


### EXEC_PLANS Cycle 3 / 执行顺序 2 — plan 2026-08-15-1913-2 Phase 2 P1-3 lineage sourceTables 契约语义修正完成

plan `ai-dev/plans/2026-08-15-1913-2-nop-metadata-api-write-path-and-contract-semantics.md` Phase 2（mission-driver EXEC_PLANS）。

- **Decision（两项语义裁定）**：
  - **元素语义 = metaTable ID**（与内部已计算结果一致、零额外映射；不新建 id→name 反向映射）；不对称性（unresolved 装完整名/诊断串）已在 owner doc 显式写明各字段语义；
  - **指标级语义 = 宿主表自身 `[metaTableId]`**（自环边语义一致；执行期边界精化：产出 0 条边时空列表——零边时声称宿主为源与边语义不一致，非反证翻转，QueryAction 内注释记录）。
- **Fix**：内部 `LineageExtractResult` 增加 `resolvedSourceTables` 载体（表级上浮 `candidateSourceIds`、列级上浮 `resolvedSourceIds`，去重保序——跨 schema 同 simpleName 可解析到同一 ID；指标级按裁定构造）；`NopMetaLineageEdgeBizModel` 三路径以 resolved 填充 DTO `sourceTables`，移除表级 `setSourceTables(r.unresolved)` 误植（unresolved 字段保留原语义）；DTO 类 Javadoc 补字段语义契约。
- **Proof**：新增 `TestLineageSourceTablesContract`（4 例，typed DTO 断言非字符串匹配）——① 表级：2 已解析 + 1 ghost → sourceTables 精确等于 ID 集、ghost 完整名只在 unresolved、双向不串入；② 列级（CTE 穿透 + 直接引用 + ghost 投影列混合 SQL）：sourceTables = {A,B}、ghost 诊断串只在 unresolved（修复前恒空）；③ 指标级：sourceTables = [宿主表]；④ 指标级 0 边边界：空列表不伪造宿主为源。
- 机械 gate：`rg -n "setSourceTables\(r\.unresolved\)" NopMetaLineageEdgeBizModel.java` 零命中。
- 验证：`./mvnw test -pl nop-metadata -am -T 1C -o` BUILD SUCCESS（全模块 reactor 含 service 全绿）。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md` 血缘示例区补返回字段语义说明（sourceTables=已解析源表 ID / unresolved=未解析描述 / 指标级语义）。


### EXEC_PLANS Cycle 3 / 执行顺序 2 — plan 2026-08-15-1913-2 Phase 1 P1-1 connectionConfig 受控写路径恢复完成

plan `ai-dev/plans/2026-08-15-1913-2-nop-metadata-api-write-path-and-contract-semantics.md` Phase 1（mission-driver EXEC_PLANS）。

- **Decision（方案 A，三项裁定）**：
  - **方案 A（保留层恢复 insertable/updatable）**而非方案 B（专用 mutation）——方案 B 需新增 `INopMetaDataSourceBiz` 跨模块公共契约方法（nop-metadata-dao），成本更高且 save 管线已具备校验/审计能力；
  - **updatable="true"**（凭据轮换）：edit 表单不含该字段 → 表单提交体无该 key → `OrmEntityCopier` 只拷贝存在 key，已存配置不会被误清空；仅显式经 API 提交该键时更新。未采用 `NopAuthUser.password` 的 insertable-only 先例（差异理由：password 有专用 resetPassword 变更入口，connectionConfig 的轮换入口就是 __update 本身，无专用入口可依赖）；
  - **connectionConfigComponent 维持锁死**：惰性 JSON 解析组件（`internal="true"` + lazy），写路径经 connectionConfig 字符串列即可触达，组件无需独立写入口。
- **Fix**：`NopMetaDataSource.xmeta`（保留层）`connectionConfig` 由全锁死改为 `published="false" insertable="true" updatable="true" queryable="false" sortable="false"`（published=false 承载读脱敏契约——R1 已核实 sensitive tag 不参与写路径回写；queryable/sortable 保持 false 防凭据探测）；注释更新（移除失效的"表单配置不在本 plan 范围"表述，补写路径裁定）。`NopMetaDataSource.view.xml`（留存层）add 表单补 `connectionConfig` 字段（layout + textarea cell，沿 NopJobSchedule jobParams 同形态先例），edit/view 表单不含（published=false 使字段不在 GraphQL 查询输出类型，edit 页 `{@formSelection}` 选中会破坏编辑对话框；沿 NopAuthUser.password 仅 add 表单先例）。
- **Proof**：新增 `TestNopMetaDataSourceConnectionConfigWritePath`（4 例）——① save 经 GraphQL 写入 connectionConfig 落库（修复前被 insertable=false 于 `ObjMetaBasedValidator._validate:175-177` 静默丢弃）+ **端到端**：testConnection 真实消费 save 产物（H2 connected=true + databaseProductName=H2）；② update 显式提交该键时轮换（updatable 生效）；③ update 提交体无该键时已存配置不被清空（key 语义实证）；④ GraphQL schema 断言 get 输出类型无 connectionConfig 字段（读脱敏不回退）+ dataSourceId 正常在场。既有 `TestAutoNopMetaDataSourceCrud`（saveDataSource.json5 已在传该字段）snapshot 无变化（save 输出经 published 过滤本就不含该字段）。
- 验证：`./mvnw test -pl nop-metadata -am`（含该测试类 4/4 绿）。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md` 安全契约节新增 connectionConfig 受控写路径与读脱敏条目（P1-1）。


### EXEC_PLANS Cycle 3 / 执行顺序 1 — plan 2026-08-15-1913-1 closure audit approved 并标 completed

- **独立 closure audit**（fresh session `ses_ffa74be50ffesPAQkXfOMGnovF`，review-only 零文件修改）：**12/12 检查全 PASS，verdict approved**——Phase 1/2 Exit Criteria 逐条 live 证据（哨兵+集中抛出行号、4 测试方法行号、rg 门禁独立复跑零命中、11 站点 1+1+1+3+1+4 分布与 DEBUG 配对、owner doc :247-249、daily log 双条目）；Anti-Hollow：`NopMetaDataSourceBizModel#testConnection/#syncExternalTables`（@BizMutation）→ `@Inject IMetaDataSourceConnectionProcessor`（app-service.beans.xml:12-13 接线）→ `buildDataSource` → `validateJdbcUrl:212`（先于 getConnection）运行时链路独立追踪证实；8 改动文件无空壳/静默 no-op；无 in-scope live defect 降级（P2-06/P2-08 为 draft 期 Non-Goals，日志口径实际 8→11 扩张）。
- **收口工具**：`check-plan-checklist --strict` exit 0；`scan-hollow-implementations --module nop-metadata --severity high` exit 0；`check-doc-links --strict` 16 errors = 既有基线（本次修改文件 0 命中）。
- **最终验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-metadata -am -T 1C` 全绿（service 1211 tests / 0 failures / 0 errors，TestMetaDataSourceConnectionSecurity 50/50 + TestSqlPathLogRedaction 2/2）；checkstyle nop-metadata 模块 964 条 pre-existing 全在 api/dto 未改动层，8 个改动文件零命中（plan 门禁口径：改动文件零新增）。
- **Plan Status → completed**（Completed: 2026-08-15，Closure Evidence 全文写入 plan Closure 节）；roadmap Work Item Status Cycle 3 第一行 🔄 active → ✅ done。源审计 `2026-08-15-0559-multi-audit` 保持 planned——其后继 plan `1913-2`（P1-1..P1-5）/`1913-3`（P1-6/7/9）仍 active，待其完成后统一收口。


### EXEC_PLANS Cycle 3 / 执行顺序 1 — plan 2026-08-15-1913-1 Phase 2 sql 路径日志 sqlHash 化完成（P1-8，AR-16 形态）

plan `ai-dev/plans/2026-08-15-1913-1-nop-metadata-ssrf-host-extraction-and-sql-log-redaction.md` Phase 2（mission-driver EXEC_PLANS）。

- **11 处 INFO SQL 全文 → sqlHash 摘要 + DEBUG 全文**（复用 `MetaQualityRuleExecutor.sqlHashOf` public static，沿 `MetaQualityRuleExecutor:675-676` 先例形态）：MetaTableQueryExecutor 1（:103）/ ExternalAggregationProcessor 1（:87）/ MixedSameDbJoinAggregationProcessor 1（:163）/ MetaJoinExecutor 3（:303 entity-entity 形态统一——白名单标识符无泄漏面变更；:369 table-table 经 tableFromForJoin 内嵌 sourceSql；:674 table-endpoint 经 buildTableFromClause 内嵌 sourceSql）/ ExternalExternalJoinAggregationProcessor 1（:128）/ MetaTableProfiler 4（queryLong/queryNullableDouble/queryNullableLong/queryString 四 helper）。
- **Decision（:515/:534 无反证）**：queryNullableDouble/queryNullableLong 消费的 fromClause 与 queryLong/queryString **完全同源**（`buildFromClause` 对 sql 表返回 `"(" + sourceSql + ") _t"`），四 helper 结构等价全部落 sql 全文——按 11 处口径全修，无需降回 8 处。
- **新增回归测试** `TestSqlPathLogRedaction`（2 例）：queryTableData sql 视图路径（`buildSqlSelectSql`+`executeQuery`+mock JDBC）与 profileTable sql 路径（真实 H2 mem，沿 TestMetaTableProfilerSecurity 先例）——断言 INFO 含 sqlHash、不含 sourceSql 全文与敏感字面量，DEBUG 保留全文（降级非删除）；沿 `TestMetaQualityRuleExecutorLogRedaction` ListAppender 先例。
- **机械 gate**：`rg -n 'LOG\.info\([^)]*\{\}[^)]*,\s*(sql|sqlText)\s*\)'` 于 6 目标文件**零命中**（修复前 11 处）。
- 验证：`./mvnw test -pl nop-metadata -am -T 1C` 全绿（service 1211/0/0）；checkstyle 改动文件零新增违规（nop-metadata 模块 964 条 pre-existing 全位于 api/dto 等未改动层，8 个改动文件 0 命中）。
- Doc-sync: No owner-doc update required（日志行为非 owner-doc 契约面；AR-16 形态叙述已在前序计划沉淀）。


### EXEC_PLANS Cycle 3 / 执行顺序 1 — plan 2026-08-15-1913-1 Phase 1 P0 主机提取 fail-closed + query 主机纳入校验完成

plan `ai-dev/plans/2026-08-15-1913-1-nop-metadata-ssrf-host-extraction-and-sql-log-redaction.md` Phase 1（mission-driver EXEC_PLANS）。

- **hostless 属性组 fail-closed**：`MetaDataSourceConnectionProcessor` 新增 `HOSTLESS_ATTRIBUTE_GROUP` 哨兵；`extractSingleHost` 对含 `=`/`(`/`)` 却无 `host=` 键命中的段返回哨兵，`validateJdbcUrl` 主机循环统一抛 `ERR_DATASOURCE_JDBC_URL_BLOCKED`（reason="hostless attribute group (driver implicit localhost) not allowed"，jdbcUrl 经 `redactJdbcUrl` 脱敏入 param）。哨兵取非主机形状串 `"("`——即使哨兵等值判断漏过，也会被 `isPlausibleHostShape` 首字符 `(` 拒绝（纵深）。
- **query 主机纳入逐主机校验**：新增 `extractQueryHosts`（pgjdbc `Driver.parseURL` 语义对齐：值与参数名均 percent-decode、参数名大小写不敏感、重复参数逐值、单值逗号分隔逐值、`hostaddr=` 同 `host=`、空值/裸 token → hostless 哨兵、解码失败原样保留由 `isPlausibleHostShape` 拒绝）+ `percentDecode` helper；`extractHosts` 将其与 authority 主机合并返回。
- **方案裁定（实现位置）**：static 提取链路（extractSingleHost/extractHosts）拿不到 jdbcUrl/redactJdbcUrl 上下文——选**哨兵值 + validateJdbcUrl 集中抛出**（而非下传脱敏后 URL），理由：错误参数脱敏集中单点（`blocked()` helper 已有），哨兵 `(` 的非主机形状提供第二道纵深，改动面最小且不破坏既有 static 签名。
- **方案裁定（大小写口径）**：`HOST_KEY_VALUE_PATTERN` 维持大小写敏感（既有 F2 reason 断言依赖该形态）；`(HOST=...)` 段因无键命中按 hostless 拒绝——**误伤面接受**（驱动侧键大小写不敏感，fail-closed 方向安全），owner doc :249 已记载；query 参数名侧为显式 `toLowerCase(Locale.ROOT)` 大小写不敏感匹配（pgjdbc 语义，非误伤）。
- **方案裁定（提取校验 vs over-block）**：按预裁定采用**提取校验**；over-block（`host=` 加入 `DANGEROUS_URL_TOKENS`）否决——该集合做整 URL `contains`，会误杀 authority 中合法 `(host=good.com,port=3306)` 形态并打挂既有 F2 reason 断言。执行中未发现提取校验不可行的情形，未回退。
- **新增对抗回归** `TestMetaDataSourceConnectionSecurity` +4 例（46→50）：4 实机向量（`(port=3306)`/`address=(port=3306)`/query `host=127.0.0.1`/query `host=169.254.169.254`）+ 9 变体（percent-decode/Host=/HOST=/多值/hostaddr=/裸 token/空值/编码参数名/值带端口）+ 3 不误伤（connectTimeout/applicationName/query 合法外网 host）；既有 F2 多主机 reason 断言不回退。
- 验证：`./mvnw test -pl nop-metadata -am -T 1C` 全绿（service 模块 1211 tests / 0 failures / 0 errors）。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md` F2 契约条目再审计补全（query 参数主机逐主机校验语义 + hostless 属性组 fail-closed + `(HOST=...)` 大小写变体拒绝的误伤面裁定）。


### nop-ai-gateway-failover W8 closure：独立 audit closure-approve + Plan Status completed + roadmap W8 ✅

- **Plan**：`ai-dev/plans/2026-08-15-1615-2-nop-ai-gateway-w8-regression-and-docs-closure.md`（W8 OBS-02/03/04 全 4 Phase 落地）
- **独立 closure audit**：fresh general subagent（`ses_ffafacdf6ffeu4MwcP8IttwqqA`，read-only）6 维全 PASS，verdict closure-approve——(1) 矩阵 + 15 新测试 + 2 个 in-scope Fix（CACHE_STATE_LOST acquire 修复 + onStreamComplete 探活恢复）非空壳实证（file:line）；(2) docs-for-ai 独立页 9 节 + 6 bean/5 配置键/xdef 元素与 live repo 逐一比对 + 路由同步；(3) Q2/Q5 显式保持待人工确认（requirement doc:399/402）；(4) 60 用例实证运行全绿 + check-doc-links/scan-hollow exit 0；(5) Anti-Hollow 两条端到端链路运行时断言实证；(6) deferred 分类诚实（无 live defect 降级）。3 非阻塞观察（落点裁定 7 节 vs 交付 9 节计数措辞（已修正）、矩阵计数记法差异、全量 175 用例以 daily log 记录为准）。
- **关闭动作**：Closure Gates 13 项全勾选 + evidence 落档（audit session + 各 Exit/Gate 验证结果 + Anti-Hollow + deferred 检查）；`Plan Status` → `completed`；roadmap W8 `planned` → `done`（OBS-02/03/04 ✅ 摘要落档，W8 全部 done → milestone 派生 done 由引擎判定）。
- **验证（关闭时复跑）**：`check-plan-checklist.mjs --strict` exit 0（Passed: 1，全勾选 + evidence）；`check-doc-links.mjs --strict` exit 0（No errors found）；`scan-hollow-implementations.mjs nop-ai/nop-ai-gateway --severity high`（位置路径）exit 0（Critical 0 / High 0）；`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -q` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` 连续 2 次 BUILD SUCCESS（gateway 175）+ `./mvnw test -pl :nop-gateway -am` BUILD SUCCESS（64，缓冲层 11 用例）；checkstyle 非有效门禁（9164 项既有基线，mission lint 回退）；import-order 本 plan 变更文件零违规。
- **doc-sync 裁定**：docs-for-ai（nop-ai-gateway.md + INDEX + nop-ai.md + source-anchors）+ requirement Q 表 + roadmap 已同步；本日志已更新。



### nop-ai-gateway-failover W8 收口验证 Phase 4：全量测试 + 链接检查 + 矩阵最终复核

- **Plan**：`ai-dev/plans/2026-08-15-1615-2-nop-ai-gateway-w8-regression-and-docs-closure.md`（W8，Phase 4）
- **验证 1（相关模块全量测试）**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` 连续 2 次 BUILD SUCCESS（chain3/chain4——gateway 175 用例（OBS-01 后 160 + Phase 1 新增 15）全绿、core 369、agent 3387）；**nop-gateway 模块测试**：`./mvnw test -pl :nop-gateway -am` BUILD SUCCESS（64 用例含 `StreamingProcessorBufferingTest` 缓冲层 11/11——W7 follow-up"nop-gateway 改动在 W8 全链回归中复核"已回跑；nop-gateway 本身零改动（本 plan 未触碰），裁定 = 直接回跑复核，无需间接覆盖理由）。
- **验证 2（doc links）**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（No errors found，复核 Phase 2 + Phase 3 修改的 requirement doc）。
- **验证 3（矩阵最终复核）**：矩阵 A/B/C/D 全部 66 格逐格核对 live 测试套件——每个非 N/A 格标注的用例名均存在于对应测试类（`rg "void <用例名>"` 逐条实证：A 20/20、B 20/20、C 10/10、D 14/14），N/A 格理由与代码路径一致（C6 网关非流式 CACHE_STATE_LOST 不可达、D13 取消 = 缓冲层职责由 nop-gateway 测试覆盖）。
- **验证 4（构建链）**：`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -q` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；checkstyle 非有效门禁（Maven 默认 Sun 规则集 nop-api-core 9164 项基线违规，mission lint 命令 `|| echo 'lint not configured'` 回退）；import-order 检查：本 plan 修改的 7 个文件零违规（`TestFailoverMetricsLocal/Gateway` 的 micrometer-before-nop 违规为 OBS-01 既有基线，不在本 plan 变更集）。
- **doc-sync 裁定**：本 Phase 无新 owner-doc 变更（Phase 2/3 已交付）；本日志已更新。



### nop-ai-gateway-failover W8 OBS-04 Phase 3 落地：需求文档 §五 Q 表收口 + §3.x 一致性抽查

- **Plan**：`ai-dev/plans/2026-08-15-1615-2-nop-ai-gateway-w8-regression-and-docs-closure.md`（W8 OBS-04，Phase 3）
- **做了什么**（`02-account-failover-requirement.md` §五 Q 表逐条回填，**仅状态回填与注记，无语义修订**）：
  - **Q2**：推荐默认 = 路由覆盖 model，已落地（本地 `toChatOptions` 四字段下沉 + 网关 `sinkCandidate` properties → converter 覆盖请求体 model）；**显式保持"待人工确认"**（W6/W7 落地记录已引用）。
  - **Q3**：执行期裁定回填（W6 Phase 1）——N=10/T=1000ms（先到者越窗，`nop.ai.gateway.failover.buffer-size|buffer-time-ms`）、重试预算 2（`retry-budget`）、**总延迟上限默认 null**（optimization candidate deferred）；网关形态参数一致（gateway.xdef bufferSize/bufferTimeMs 缺省 10/1000 + bufferEnabled 缺省 false）。
  - **Q4**：**已落档**——OBS-01 先行文档化落地（§3.6 契约表 + 近似语义 + 冷却期三事件 + null-object/fail-fast 边界）+ 实现 + 8 focused 测试。
  - **Q5**：推荐默认 = 确认偏离（RATE_LIMITED/TRANSIENT → 账号链切换），已落地（本地/网关 `isSwitchable` 四类 + 流式重订阅路径）；**显式保持"待人工确认"**。
  - **Q7**：spike 结论（机制 A 最小 nop-gateway 通用改动）+ W7 落地回填 + W8 全链回归复核（缓冲层 11 用例 + 网关流式全链，Phase 4 回跑）。
  - **Q6** 剩余（各 provider 显式非零缺省）标注为部署面取值决策 deferred（本 plan Deferred 段）。
- **§3.x 一致性抽查（live repo 实证）**：§3.1 动作表六分类 = `ChatServiceFailoverAdapter.isSwitchable` + CACHE_STATE_LOST 原地重发分支（W8 Phase 1 新增测试实证，含 409 errorMapping 可达性）；§3.2 切换语义 = `FailoverStreamFlow`（窗口内/外判定、重订阅消耗预算、取消顺序契约）+ `BufferedStreamingPublisher`（字节流层缓冲/重订阅）；§3.3 = `ModelClassRouter`/`ConcurrencyRegistry`/探活恢复（**含 W8 Phase 1 修复的网关流式探活恢复缺口**——onStreamComplete → recordSuccess，补全 §3.3"探活成功 → CLOSED"在网关流式路径的落地）；§3.4 = `llm.xdef` concurrencyLimit 层级语义 + `model-class.xdef` + 规则策略 bean（W5b 记录一致）；§3.7 = `TestDialectBidirectionalConversion`（20 用例）+ converter 接线测试实证（W4 记录一致）。**抽查结论：全部落地注记与 live 代码/测试一致，无 drift**。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（本 Phase 修改 ai-dev 下 requirement doc，随跑）。
- **doc-sync 裁定**：requirement doc Q 表即本 Phase 交付物（无语义修订，R1-R8 审查结论未破坏）；本日志已更新。



### nop-ai-gateway-failover W8 OBS-03 Phase 2 落地：docs-for-ai 使用文档（独立页 + 路由同步 + 链接检查 0 errors）

- **Plan**：`ai-dev/plans/2026-08-15-1615-2-nop-ai-gateway-w8-regression-and-docs-closure.md`（W8 OBS-03，Phase 2）
- **落点裁定（执行期）**：新增独立页 `docs-for-ai/03-modules/nop-ai-gateway.md`（7 节含完整配置示例）——内容量超聚合页单章容量 + failover 使用契约横跨 5 配置面 + 有独立需求/架构文档 = 组内先例（12 子模块无独立页）的例外（与 nop-job.md 同构），理由落档 plan Phase 2。
- **做了什么**：`nop-ai-gateway.md` = 功能概览（两种形态 + 能力清单）/ 形态 B 本地适配器（`nopChatServiceFailoverAdapter` bean + **部署启用方式 = 显式切换注入目标，不覆盖 ioc:default（W6 D4）** + 配置键 buffer-size/buffer-time-ms/retry-budget）/ 形态 A 网关拦截器（**挂载契约 M-7：bean 注册 ≠ 挂载，gateway.xml `<interceptors><interceptor bean=...>` 引用 + F1/B-18 前置 + gateway.xdef streaming bufferEnabled(false)/bufferSize(10)/bufferTimeMs(1000) + primary-provider 配置键**）/ 模型类与账号配置（`model-class.xdef` 结构 + opt-in `_default.model-class.xml` + `llm.xdef` `<accounts>`/`concurrencyLimit` 层级语义 + 动作表 + 透明边界）/ 规则策略（bean 绑定 + ioc:optional + `nop.ai.gateway.rule-selection.rule-name` + resolve-rule 版本化路径 + 输入/输出契约 + computed 谓词模式）/ 指标清单（OBS-01 契约摘要 11 行）/ 源码锚点表。**配置示例全部新建示意片段**（未拷贝 test fixture、未修改任何 `_` 前缀文件）。
- **路由同步**：`INDEX.md` + 「AI 网关 + 透明账号切换」条目；`03-modules/nop-ai.md` 子模块表补 `nop-ai-gateway` 行 + 功能概览 bullet；`04-reference/source-anchors.md` 补 `AIGW-001`~`AIGW-009` 锚点（适配器/流式流/拦截器/回调与监听器/缓冲层/路由原语/熔断分类/配置面/指标）。
- **一致性核对（live repo 逐项比对）**：bean 名（nopChatServiceFailoverAdapter/nopAiGatewayFailoverInterceptor/nopFailoverCircuitBreaker/nopFailoverConcurrencyRegistry/nopAiFailoverMetrics/nopBackendMessageConverter_AI_DIALECT/nopAiRuleBasedSelectionStrategy/nopRuleManager）、配置键（`nop.ai.gateway.failover.*`/`nop.ai.gateway.rule-selection.rule-name`/`nop.ai.llm.{provider}.api-key`（AiCoreConstants CONFIG_VAR_LLM_API_KEY）/`nop.ai.service.default-llm`（AiCoreConfigs））、xdef 元素（gateway.xdef streaming bufferEnabled/bufferSize/bufferTimeMs、model-class.xdef modelClass/candidates/candidate/provider/model/accountRef、llm.xdef concurrencyLimit 层级语义）、规则路径（RuleServiceHelper.buildResolveRulePath = resolve-rule:{ruleName}/v{version}）、指标族与 §3.6 契约表 11 行一致。
- **排障记录**：check-doc-links --strict 首跑 5 errors——(a) docs-for-ai 不得引用 ai-dev 路径（BOUNDARY 规则）：需求/架构文档引用改纯文本表述（去反引号，反引号内裸文件名会被 filename-search 解析进 ai-dev）；(b) 反引号内 VFS 路径（`_vfs/nop/ai/llm/_default.model-class.xml`）被解析为 repo 相对路径 → BROKEN：改纯文本；(c) plan 文件 2 个既有失效链接（Related 行截断的 `2026-08-15-1116-2`/`1116-3`）补全文件名（OBS-01 closure 已知遗留）+ 矩阵行 fixture 路径去反引号。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（No errors found）。
- **doc-sync 裁定**：本 Phase 交付物 = docs-for-ai 文档 + 三处路由同步；本日志已更新。



### nop-ai-gateway-failover W8 OBS-02 Phase 1 落地：全链回归矩阵（66 格）+ 15 新测试 + 2 个 in-scope 行为缺口修复

- **Plan**：`ai-dev/plans/2026-08-15-1615-2-nop-ai-gateway-w8-regression-and-docs-closure.md`（W8 OBS-02，Phase 1）
- **矩阵**（plan 内落档，A/B/C/D 四视图 = 本地/网关 × 非流式/流式）：A 20 格（本地非流式）+ B 20 格（本地流式）+ C 11 格（网关非流式，1 N/A）+ D 15 格（网关流式，1 N/A）——每格标注既有用例名/新增用例名/N/A+理由；gap 判定 = 格内无断言该场景的测试；N/A 理由均引用使事件协议层不成立的事实（如网关非流式 CACHE_STATE_LOST：分类路径 = classifyStreamError（InvokeProcessor 异常参数键 `responseBody` ≠ `ARG_BODY("body")` → body 不可达 → errorMappings 不可命中）+ classifyResponseStatus（启发式），两路径均不产该分类）。
- **新增 15 测试**：非流式本地 +2（CACHE_STATE_LOST 原地重发成功/预算耗尽）；流式本地 +5（TRANSIENT 503 切换 / NON_TRANSIENT 断流 / CACHE_STATE_LOST 同候选重发 / 健康度饱和 fail-loud / 无路由组直通）；网关非流式 +4（429→RATE_LIMITED 切换（InvokeProcessor 内置 3×退避 2s/4s/8s 后异常路径，~15s 慢测试）/ 401→AUTH_INVALID 切换 / 400→NON_TRANSIENT 不切换 / 探活恢复）；网关流式 +4（TRANSIENT 跨 dialect 重订阅 / AUTH_INVALID 跨 dialect 重订阅 / CACHE_STATE_LOST 同候选重发 / 探活恢复 HALF_OPEN→CLOSED）。
- **测试基建**：`gw-cache.llm.xml`（errorMapping httpStatus=409 + errorCodes=cache_state_lost → CACHE_STATE_LOST——ErrorClassification javadoc 语义 409；default.llm.xml 映射均不命中该 code，first-match-wins 安全）；`_default.model-class.xml` + tier-gw-cache；`w7-failover.gateway.xml` + `/chat/nonstream-raw` 路由（**无 converter**——401/400 响应级分类可达性：converter 路由经 `toFrontendResponse` 把非 2xx 归一化为 `ApiResponse.success`（status=0 = isOk()=true）→ 响应级分类仅在无 converter 路由可达，矩阵已落档）；`TestAiGatewayFailoverInterceptorNonStreaming.httpResponse` fake 增强（Nop 风格 body 抽取 status/code/msg）。
- **in-scope 行为缺口修复 1（挂起缺陷）**：`ChatServiceFailoverAdapter.decideNonStreamFailure` CACHE_STATE_LOST 分支直接调 `callAsyncWithOptions`（whenComplete 恒 release）未先 acquire → 原地重发 release 下溢抛 `ERR_AI_AGENT_INVALID_ARG`，异常被 whenComplete 捕获存入被忽略 stage → future 永不完成（**实测挂起，jstack 实证**）。修复 = CACHE 分支 acquire + 并发复查（M-6a）后再重发（对齐 `invokeNonStreamingAttempt` 语义）；流式（`startAttemptWithOptions`）与网关非流式（`invokeNonStreamingAttempt`）路径本就有 acquire，无此缺陷。
- **in-scope 行为缺口修复 2（探活恢复契约漂移）**：网关流式成功路径（缓冲层 onComplete）原无 recordSuccess——探活放行（HALF_OPEN）的流成功后熔断器滞留 HALF_OPEN，违背需求 §3.3"探活成功 → CLOSED"（本地 `FailoverStreamFlow.handleStreamComplete` 已实现）。修复 = `AiGatewayFailoverInterceptor` 新增 `onStreamComplete` 覆写 → `CircuitObservation.recordSuccess`（仅接管请求生效零回归；不记录 request-success 指标——OBS-01 契约表网关流式成功不在指标触发事件内，既有指标测试断言 0 计数保持一致）。
- **排障记录**：CACHE_STATE_LOST 测试初写时先遇 SATURATED 再遇挂起——(a) 错误 mapping httpStatus=429 vs 语义 409（enum javadoc）→ 409 修正；(b) 挂起根因 = 上述缺陷 1（jstack + 临时插桩实证 whenComplete 内 release 下溢吞异常）；(c) 网关流式 CACHE 重发 URL 断言修正（首次 fetch = 路由 URL 表达式（无 accountBaseUrl 不设 property → 表达式兜底 gw-test2）；原地重发 = `dialect.buildUrl(config.baseUrl)`（gw-cache）——同账号断言改以认证头为准）。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` 连续 2 次 BUILD SUCCESS（chain3/chain4，gateway 175 用例 = OBS-01 后 160 + 新增 15；core 369 + agent 3387 全绿）。已知 flaky 记录：`outOfWindowFailureBreaksStreamWithoutSwitch` 在并行模块负载下偶发（根因 = JDK SubmissionPublisher 竞态，W6 log 489 已落档，~5-10% 负载相关，非 W8 引入；既有断言未改写；隔离运行 3/3 绿）。
- **doc-sync 裁定**：矩阵 + 2 处修复落档于 plan Phase 1（in-scope Fix 不得降级 follow-up）；requirement 文档 §3.3 探活恢复语义无需修订（修复补全了既有契约的网关流式落地缺口）；本日志已更新。



### nop-ai-gateway-failover W8 OBS-01 Phase 2 落地：指标服务实现与两形态全事件点接线

- **Plan**：`ai-dev/plans/2026-08-15-1615-1-nop-ai-gateway-w8-observability-metrics.md`（W8 OBS-01，Phase 2）
- **新增**（nop-ai-gateway `io.nop.ai.gateway.failover`）：`IFailoverMetrics`（12 方法接口，javadoc 含 null-object/fail-fast 边界）、`FailoverMetricsImpl`（micrometer 默认实现，`GlobalMeterRegistry`/可注入 registry，nop-job 先例模式，所有方法真实记录 + 记录异常捕获必记日志）、`CooldownEventType`（started/rejected/probe-rejected）、`CircuitObservation`（熔断迁移/冷却期观察辅助——recordFailure/recordSuccess/allowCall 前后 getState 对比，core 原语零改动，metrics 可空 no-op）。
- **接线（本地形态）**：`ChatServiceFailoverAdapter` = 切换计数（callAsyncAttempt attempt>0 非固定候选，超限重选递归不重复计）、成功/失败计数 + Timer（attempt 起始→终止）、熔断记账/迁移/冷却（CircuitObservation）、探活（allowCall 观察）、饱和（selectNextWithProbe rethrow 前）、并发 acquire/release 配对；`FailoverStreamFlow` = 重订阅/切换（attemptsStarted>=2）、成功/失败 + Timer（windowStart→终止）、熔断观察、并发配对（releaseAttempt）。
- **接线（网关形态）**：`AiGatewayFailoverInterceptor` = onRequest 接管计数、invoke 非流式切换/耗时、onError 降级终止计数、onStreamElement 反向转换计数、并发配对；`FailoverProbeSupport` 加 metrics 参数（饱和计数 + 探活观察）；`GatewayStreamingRetryCallback` = 重订阅/切换计数 + 熔断观察 + 饱和（不重试分支）；`GatewayStreamingLifecycleListener` = 流建立/终止并发配对。
- **bean 注册**（`ai-gateway-defaults.beans.xml`）：`nopAiFailoverMetrics`（`ioc:default=true`）+ 适配器/拦截器非 optional `<ref>`（部署面 fail-fast，Phase 1 裁定）。
- **验证**：`./mvnw compile -pl :nop-ai-gateway -am` BUILD SUCCESS；`./mvnw test -pl :nop-ai-gateway -am` 152 用例零回归。
- **doc-sync 裁定**：§3.6 契约已在 Phase 1 更新；本日志已更新。



### nop-ai-gateway-failover W8 OBS-01 closure：独立 audit closure-approve + Plan Status completed + roadmap OBS-01 ✅

- **Plan**：`ai-dev/plans/2026-08-15-1615-1-nop-ai-gateway-w8-observability-metrics.md`（W8 OBS-01 全 3 Phase 落地）
- **独立 closure audit**：fresh general subagent（`ses_ffb504316ffeX7gS4Oa7gajPmu`，read-only）10 维全 PASS，verdict closure-approve——(1) §3.6 契约表（11 指标行 + 近似语义 + 冷却期三事件 + null-object/fail-fast 边界落档）；(2) 指标服务真实实现（12 方法全真实记录 + 异常捕获记日志）；(3) bean 注册 + 非 optional ref（部署面 fail-fast）；(4) Anti-Hollow 接线逐点核（adapter/flow/interceptor/retry-callback/lifecycle-listener 调用链连通）；(5) 8 focused 测试覆盖每个契约类别 + 维度断言 + Timer 仅断言已记录（flaky-free）；(6) nop-ai-core 零改动（git diff 空）；(7) 零回归（160 用例两次全绿）；(8) deferred 分类诚实；(9) 扫描工具全绿；(10) 文本一致性（closure 机械步骤由实现者补齐）。2 非阻塞观察：probe-rejected 仅 0-计数断言（正向触发需并发，代码路径真实可达）；successor plan 1615-2 自身 2 个失效链接归其 closure 处理。
- **关闭动作**：Closure Gates 14 项全勾选 + evidence 落档（audit session + 各验证命令结果 + Anti-Hollow + deferred 分类检查）；`Plan Status` → `completed`；roadmap W8 条目 OBS-01 标记 ✅ done（W8 整体保持 `planned`——OBS-02/03/04 归 successor plan `2026-08-15-1615-2`）。
- **验证（关闭时复跑）**：`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs nop-ai/nop-ai-gateway --severity high`（位置路径）exit 0（Critical 0 / High 0）；`check-doc-links.mjs --strict` exit 0；`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` 连续 2 次 BUILD SUCCESS（160 用例）；checkstyle 非有效门禁（Maven 默认 Sun 规则集全仓基线违规——nop-api-core 9164 项、gateway 1483 项均既有基线，mission lint 命令 `|| echo 'lint not configured'` 回退；顺手移除 FailoverStreamFlow 一处既有未用 import，新增文件与基线风格一致无新违规类型）。
- **doc-sync 裁定**：requirement §3.6 契约 + §3.3 引用已同步；`01-architecture.md` 无架构级变更（No owner-doc update required）；本日志已更新。



### nop-ai-gateway-failover W8 OBS-01 Phase 3 落地：focused 指标测试（8 用例全绿）

- **Plan**：`ai-dev/plans/2026-08-15-1615-1-nop-ai-gateway-w8-observability-metrics.md`（W8 OBS-01，Phase 3）
- **`TestFailoverMetricsLocal`**（5 用例，本地形态，`FailoverMetricsImpl(SimpleMeterRegistry)` 独立 registry）：切换 + 成功率/延迟（429→备份成功：switch=1、failure=1（主账号 account=""）、success=1（key-gw-a）、Timer outcome success/failure 各 1 已记录、acquire==release）；熔断迁移 + 冷却启动（阈值 1 单次失败 CLOSED→OPEN + started=1）；冷却拒绝 + 饱和（长冷却全池 OPEN：gw-test 键 3 候选 + gw-test2 键 2 候选各 REJECTED、saturation{provider=gw-test, model-class=tier-gw}=1）；探活放行（冷却 0：HALF_OPEN 迁移 2、CLOSED 恢复 1、cooldown=0）；流式重订阅 + 切换（attempt1 窗口内失败 → attempt2：resubscribe=1 + switch=1 + 失败/成功各 1 + Timer 已记录）。
- **`TestFailoverMetricsGateway`**（3 用例，网关形态）：流式接管 + 切换/重订阅 + 反向转换 + 生命周期配对（takeover{gw-test2/gw-model-2}=1、switch+resubscribe{gw-anthropic/gw-claude-model}=1、stream-element=2、transition OPEN=1（阈值 1）+ started=1、acquire==release≥2、非流式指标 0）；非流式切换 + 成功率/延迟（网络错误 → gw-backup-a：switch{key-gw-a}=1、failure/success 各 1、Timer 已记录、takeover/stream-element=0）；NON_TRANSIENT 降级终止（degraded{gw-test2}=1、无重试、acquire==release）。
- **排障记录**：micrometer 1.16 移除 `Meter.withTags(String...)` → 改用 `registry.counter(name, k, v, ...)`；`SimpleMeterRegistry` 包名 = `io.micrometer.core.instrument.simple`；micrometer `find(name).tags(...)` 只返回首个匹配 meter → 断言辅助改求和（to-state 等多候选场景）；网关非流式测试阈值 1 时被动重选跳过 OPEN 主账号直接跨 provider 落 gw-test2（与既有测试行为差异的根因）→ 恢复阈值 3 断言 key-gw-a。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` 连续 2 次 BUILD SUCCESS（160 用例含 8 新指标用例全绿，W6 32 + W7 13 + 既有网关用例零回归）。
- **doc-sync 裁定**：无新 owner-doc 变更（契约已在 Phase 1）；本日志已更新。



### nop-ai-gateway-failover W8 OBS-01 Phase 1 落地：指标契约裁定 + §3.6 落档

- **Plan**：`ai-dev/plans/2026-08-15-1615-1-nop-ai-gateway-w8-observability-metrics.md`（W8 OBS-01，Phase 1）
- **事件点盘点（live 代码核实）**：本地形态 = `ChatServiceFailoverAdapter`（非流式 `callAsyncWithOptions` 成功分支 / `decideNonStreamFailure` 切换/失败/预算分支、`callAsyncAttempt` 重选、`selectNextWithProbe`+`probeBrokenCandidates` 饱和与探活、acquire/release 配对）+ `FailoverStreamFlow`（`startAttemptWithOptions` 重订阅/重发、`handleStreamFailure` 记账/重订阅、`handleStreamComplete` 成功、`releaseAttempt` 配对）；网关形态 = `AiGatewayFailoverInterceptor`（`onRequest` 接管、`invokeNonStreamingAttempt` 切换/重发/耗时、`onError` 降级、`onStreamElement` 反向转换）+ `GatewayStreamingRetryCallback.retry`（重订阅/切换）+ `GatewayStreamingLifecycleListener`（流建立/终止 + 并发配对）。全部映射入契约表。
- **契约裁定**：命名族 `nop.ai.gateway.failover.*`；11 指标行（切换/重订阅/熔断迁移/冷却期/成功/失败/耗时 Timer/饱和/并发配对×2/接管/降级/反向转换——部分合并为同类别行）契约表落档 §3.6；**并发近似语义**落档（`getState` 无锁读 → 迁移计数为近似观测）；**冷却期三事件全映射**（started=OPEN 迁移、rejected=OPEN 未期满拒绝、probe-rejected=HALF_OPEN 占用拒绝，均可达）；**观察机制** = 编排层调用点派生（core 原语零改动）；**配置形态** = 不引入开关缺省恒定启用 + 部署面 fail-fast（bean 非 optional ref）/代码面 no-op 降级（null setter 仅测试/手工构造场景）边界落档；"本契约即 §五 Q4 先行文档化的落地"已写入。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 待 Phase 1 完成时随跑（Phase 4 全量跑）。
- **doc-sync 裁定**：requirement §3.6 即本 Phase 交付物；本日志已更新。



### 2026-08-15（D2-4 CLOSURE — plan `2026-08-15-1134-2` 收口，Plan Status → completed）

- 独立子 agent closure audit（fresh session `ses_ffacc7521ffeDjRARm0WA4794a`，research-only）：Phase 1/2/3
  Exit Criteria 全 PASS；Anti-Hollow PASS（E2E 经真实 biz 代理 → H2 真实 DDL + SQL 插值，零 mock，
  行集断言（2/4/1）非透传可得；scan-hollow 0 findings）；契约 vs 实现逐字段比对零 Blocker/Major；
  deferred 项（flux 渲染/对接）分类诚实。1 项非阻塞观察：widget 词表封闭性仅在产出路径强制
  （与 §一/§11.6 契约表述一致，向后兼容取舍）。
- Closure Gates 全部通过并勾选：compile 门（service+web `-am`）exit 0；test 门
  `./mvnw test -pl nop-datav/nop-datav-service -am` 全链 BUILD SUCCESS（上游 nop-stream 两个
  timing 敏感测试偶发 flaky、隔离复跑通过，与本计划无关——本计划仅改 nop-datav + ai-dev 文件）；
  收口复跑 `./mvnw test -pl nop-datav/nop-datav-service`（deps 取自 clean install 后本地仓库）
  **557/0/0** 全绿、surefire XML 留盘（FilterDefE2E 2/2、FilterDefExporter 12/12、
  ParamParserAndResolver 29/29、FilterApi 14/14）；`clean install -pl nop-datav -am -T 1C -DskipTests`
  BUILD SUCCESS；checkstyle 根构建禁用态（沿用 D1-4 裁定，AGENTS.md 人工核验 import 分组/英文
  错误消息）；`check-plan-checklist.mjs --strict` / `scan-hollow-implementations.mjs --module
  nop-datav --severity high` / `check-doc-links.mjs --strict` 均退出码 0。
- Plan 收口：14 项 Closure Gates 勾选、Closure 段写入 audit 证据、Plan Status → completed
  （Completed: 2026-08-15）；roadmap D2-4 `planned` → `done`（D2 全 done）。
- Doc-sync: 本条目；plan Closure 段；roadmap 状态块 + Last updated。


### 2026-08-15（D2-4 EXECUTE Phase 1 — flux dashboard-filter 对齐契约定稿）

- Plan `2026-08-15-1134-2`（D2-4 flux dashboard-filter 约定与 nop-datav 参数模型对齐）Phase 1 完成：
  `linkage-design.md` 新增 **§十一 D2-4 flux dashboard-filter 筛选定义对齐**（11.1–11.7），并同步修订
  既有章节（header Status/Last Reviewed、概述两段、§一 widget 行、§二/§三/§四 修订注、§十 Non-Goals 首行）。
  6 项裁定全部收口：
  - **产出形态**：参数描述符数组（flux 页面轻组装 form 字段）；拒绝完整 form schema（后端越界 UI 细节）、
    并入 D1-4 布局导出物（消费时机/消费者不同）、flux 消费裸 paramConfig（组装规则无契约）。
    D1-4 边界：筛选定义仅经 `exportDashboardFilter` action 产出、不经布局导出物携带。
  - **date-range 值形态与转换归属**：契约形态钉死 delimited 绝对日期串（delimiter=`,`、valueFormat=
    `yyyy-MM-dd`）；**后端拥有双向转换**——产出向 exporter 合并 `{start,end}`→delimited initialValue；
    提交向 `resolveFilterValues`（含 getDashboardData/parseFilterFromUrl 委托路径）增设 delimited 标量
    接受形态（additive 向后兼容，扁平 key 仍 canonical 且优先）。relative 语义显式不支持（核实记录：
    resolver ISO 逐段校验天然拒绝 `today` 类 token；flux 手工 relative 初始值仅 display、不写回发布值，
    排除在消费契约外）；datetime 类 valueFormat 显式出契约（含时间部分值显式失败非静默截断）。
  - **widget 词表**：paramConfig 接受集封闭 {dropdown, date-picker}；control 产出集封闭 {select,
    input-text, input-number, input-date, date-range}；type+widget 映射表钉死（dropdown×date 类组合
    非法显式报错，不静默回退）；未声明 widget 走类型缺省映射。
  - **options 来源**：产出定义不含 options；dropdown 候选值 flux 页面 schema 自供（拒绝 paramConfig
    扩展/数据集推导，理由入 11.3）；页面自证 initialValue∈options。
  - **URL 同步**：canonical = 扁平 key（§四不变，序列化恒扁平）；`?name=start,end` 兼容接受（经
    resolver delimited 路径）。
  - **错误路径**：2 新错误码（UNKNOWN_WIDGET / WIDGET_TYPE_MISMATCH）+ 复用 INVALID_PARAM_CONFIG
    （产出侧 defaultValue 校验）/ PARAM_TYPE_MISMATCH（delimited 提交校验）。
- Doc-sync: 本条目；plan Phase 1 勾选。No new test required（纯 Decision/文档 phase，可验证性由
  Phase 2/3 测试承载）。


### 2026-08-15（D1-4 EXECUTE Phase 1 — 布局对齐契约定稿）

- Plan `2026-08-15-1134-1`（D1-4 flux dashboard editor 布局对齐）Phase 1 完成：`runtime-design.md`
  新增 **§九 flux 布局对齐（D1-4）** 契约章节（9.1–9.12），11 项裁定全部收口：
  - action 命名 `exportDashboardLayout`（admin,user）/`saveDashboardLayout`（admin），规避 D3-3
    `exportDashboard` 数据导出冲突
  - 面板身份：导出 id=panelId；保存按 id 对齐（命中=更新/未命中=服务端 UUID 新建+响应重同步）；
    名字列派生（title←displayName?:panelName；保存 title→displayName，panelName 稳定；新建
    panelName←title 截断 100 或 panel-{n}）；sortOrder←数组下标；重复 id/跨看板 id 显式报错
  - 类型词表：flux↔registry 恒等映射 + 复用 `PanelTypeMapping`；flux `html` 保存显式拒绝
    （扩展注册表+字典裁定 rejected）；6 类装饰/媒体导出不可达、保存 fail-fast
  - 几何/网格：`layoutConfig` 结构钉死（`{cols,rowHeight,gap,height?,panels:{id:{x,y,w,h}}}`），
    未知遗留键保留；json-4000 容量判据（≈58 字符/面板，50 面板≈2.9KB）超限显式报错不截断
  - 存量无几何导出：默认布局合成（x=0,w=cols,h=4，sortOrder 堆叠）；存量自由结构视为无几何
  - props↔panelConfig：导出去 dataBinding；保存 props 存在=权威全量替换 ∪ 保留 dataBinding
    （唯一保护区）；props 缺省 panelConfig 不动
  - 保存载荷=布局对象 `{panels,cols?,rowHeight?,gap?,height?}`（网格含则更新/缺省保留）；
    裸数组裁定 rejected（无网格承载位）
  - tabs：v1 平铺不拒绝（tab 行/tabId 保留，新建 tabId=null）
  - 绑定：source=`datasetRef:<id>` 一等描述；保存 source/props.dataBinding 永不消费（echo-only）
  - 上界：独立配置 `nop.datav.dashboard-layout.max-panels`（默认 50，复用
    `ERR_DATAV_DASHBOARD_PANEL_LIMIT_EXCEEDED`）
  - roundtrip 判据：两次服务端导出比较（网格/长度/顺序/id/type/title/几何/props 深等/source），
    no-op roundtrip L2≡L1
  - 删除面板的 AlertRule 处置：置 DISABLED+注销 cron（镜像删除级联先例）
- Doc-sync: 概述段 D1-4 状态更新（flux 已落地→后端契约见 §九）；本条目。


### 2026-08-15（DRAFT_PLANS 轮 — D1-4/D2-4 两 plan 起草并转 active）

- **阻塞解除核实**：roadmap 最后两个 `todo` 项（D1-4/D2-4）此前 blocked 于「flux 侧落地」——本轮核实
  nop-chaos-flux 已产出 `flux-renderers-dashboard`（editor + 运行态，flux plan `2026-08-09-182611`
  full-green，commit `a2ac7f5d7`）与 dashboard-filter 编排约定
  （`docs/components/dashboard-filter/design.md` + 走查单测 4 条），阻塞解除，可起草后端对齐 plan。
- **Plan 1** `ai-dev/plans/nop-datav/2026-08-15-1134-1-flux-dashboard-editor-layout-alignment.md`
  （D1-4，Work Item: D1-4）：flux `DashboardLayoutSchema` ↔ nop-datav 归一化模型双向对齐——
  契约定稿（11 项裁定：面板身份/名字列派生/action 命名/类型词表（复用 `PanelTypeMapping`，双向缺口
  html ↔ pivot-table/map/container+装饰类）/几何存放（json-4000 容量上界进判据）/存量无几何导出/
  props↔panelConfig 区域/保存载荷形态（flux 编辑器 serialize 仅面板数组）/tabs/绑定保护/上界/
  roundtrip 等价判据）+ 导出/保存回写 action（双 auth 登记）+ reconcile 事务 + roundtrip E2E。
  三轮审查：round1 修 2 Blocker（props 区域映射、存量无几何 bootstrap）+ 3 Major（serializeDashboardContent
  先例归因——`exportDashboard` 是 D3-3 数据导出、保存载荷形态、容量判据）；round2 修 Major 路径前缀 +
  4 Minor（getComponentTypes 归属 screen 侧、PanelTypeMapping 既有腿、displayName 派生、身份异常归
  Phase 3）；round3 验证通过（reviewer 结论「可进入执行（active）：无 Blocker」）。
- **Plan 2** `ai-dev/plans/nop-datav/2026-08-15-1134-2-flux-dashboard-filter-param-alignment.md`
  （D2-4，Work Item: D2-4）：paramConfig ↔ flux dashboard-filter 约定对齐——契约定稿（产出形态与
  D1-4 边界/date-range 转换归属 + delimiter+valueFormat 钉死（手工 relative 初始值仅 display 解析的
  nuance 显式排除消费契约外）/widget 词表 + options 来源/URL 同步 + 既有章节修订）+ 筛选定义产出
  action + 闭环 E2E（定义→模拟 flux 提交→resolveFilterValues→getDashboardData 注入断言）。
  与 D1-4 软顺序可独立先行。三轮审查：round1 修 2 Major（preset 值形态待核实、options 来源缺失）；
  round2 修 Major（date-range 钉死范围超出 §4.1——收窄为 preset 路径钉死 + relative 初始值 nuance）；
  round3 确认按处方局部修订即可放行（第四轮复审经 reviewer 预先豁免，修订已落地）。
- **Deferred 项复核**（本轮不重新起草）：nop-retry 重试仍 blocked（平台无本地 `IRpcServiceInvoker`
  未解除）；图像导出/大屏缩略图仍 blocked（无后端渲染内核，flux React renderer ≠ 后端渲染引擎）；
  按看板显式用户授权 successor 未触发（本轮按「不覆盖全部剩余项」纪律未纳入，留后续轮）。
- Roadmap：D1-4/D2-4 `todo`→`planned`（附 plan 路径与摘要），`Last updated` 前置本轮条目。
- Doc-sync: 本条目 + roadmap + 两 plan 文件；doclinks 检查见下方验证记录。


### nop-ai-gateway-failover W5b closure：独立 audit closure-approve + Plan Status completed + roadmap W5b done

- **Plan**：`ai-dev/plans/2026-08-15-1116-1-nop-ai-gateway-w5b-rule-selection-strategy.md`（W5b 全 4 Phase 落地）
- **独立 closure audit**：fresh general subagent（`ses_ffc1a87a1ffe1SNZnKB2odT0Am`，read-only）10 维全 PASS，verdict closure-approve——(1) Phase 1 裁定内联落档（D1-D5 + P1 + `<expr>` XML 不可用执行期修订）；(2) 实现真实（三分支语义 + fail-loud + javadoc 契约）；(3) bean 注册 ioc:optional 非默认；(4) 13+2 测试覆盖全语义（surefire 实证）；(5) Anti-Hollow（router→策略→IRuleManager→规则文件链路经 E2E 断言连通）；(6) 复用优先（无第二套引擎/健康机制）；(7) 零回归（审计独立复跑 BUILD SUCCESS，routing 25 用例零改动）；(8) 文档/roadmap/log 同步；(9) Deferred 分类诚实；(10) 文本一致性。1 非阻塞观察：roadmap W5b done 时序先于 audit（最终状态一致）。
- **关闭动作**：Closure Gates 14 项全勾选 + evidence 落档（含 Anti-Hollow 检查 + deferred 分类检查 + 各验证命令结果）；`Plan Status` → `completed`；roadmap W5b `planned` → `done`（Phase 4 已置）；requirement §五 Q10/§3.3/§3.4/§4.1 回填（Phase 4）。
- **验证（关闭时复跑）**：`check-plan-checklist.mjs --strict` exit 0（全勾选 + evidence）；`scan-hollow-implementations.mjs --module nop-ai-core --severity high` exit 0（0 findings）；`check-doc-links.mjs --strict` exit 0；`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway -am` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 369 + agent 3387 + gateway 98 全绿）；checkstyle 受影响模块 0 violations（既有 nop-api-core 基线残留，无有效门禁，与 W2-W5 记录一致）。
- **doc-sync 裁定**：本 plan 交付物 = 规则策略类 + 依赖 + bean 注册 + 测试 + 文档回填；本日志已更新。



### nop-ai-gateway-failover W5b Phase 4 落地：文档同步 + roadmap 状态推进

- **Plan**：`ai-dev/plans/2026-08-15-1116-1-nop-ai-gateway-w5b-rule-selection-strategy.md`（W5b，Phase 4）
- **做了什么**：
  - `02-account-failover-requirement.md`：**§五 Q10 回填**（已决并落地：DSL 形态 = `rule.xdef`；IoC 绑定 = nop-ai-gateway `ai-gateway-defaults.beans.xml` 注册 `nopAiRuleBasedSelectionStrategy`（`nopRuleManager` ref `ioc:optional` + ruleName/ruleVersion 属性）；模块归属 = nop-ai-core（§4.1 归属表一致）+ nop-rule-core 依赖无环实证；规则契约 = 输入 model/provider/candidates/health/attempted（不含 accountKey——apiKey 安全裁定；health 键 = Integer）+ 输出 `selectedIndex`（int 不得 mandatory）+ computed 输入谓词模式（`<expr>` XML 不可用实证）+ 语义分支（未命中/无输出 → null；越界/已尝试 → `ERR_AI_AGENT_INVALID_ARG`）+ 单例 stateless）；**§3.3** 动态选择"规则配置（可选实现）"bullet 补落地状态 + 落地状态块补 W5b 收口；**§3.4** 动态选择策略配置工作项补 W5b 落地状态；**§4.1** 归属核查记录（规则策略类落 nop-ai-core 与归属表一致，无需修订）。
  - **roadmap**：W5b `planned` → `done`（交付摘要落档）。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（No errors found）。
- **doc-sync 裁定**：上述文档即本 Phase 交付物；本日志已更新。



### nop-ai-gateway-failover W5b Phase 3 落地：规则策略测试（语义 + IoC + 接线 + 端到端全链）

- **Plan**：`ai-dev/plans/2026-08-15-1116-1-nop-ai-gateway-w5b-rule-selection-strategy.md`（W5b，Phase 3）
- **做了什么**：
  - **测试规则文件**（`_vfs/nop/rule/rule-selection/{name}/v1.rule.xml`，7 个 in nop-ai-core + 1 个 gateway 本地）：preferred（computed 输入 preferredProvider=candidates[1].provider + eq 谓词）/ computed（computed 输入 secondAvailable=health[1].available——健康视图消费证明）/ nomatch（`<alwaysFalse/>`）/ noselect（`<alwaysTrue/>` 无输出）/ outofbounds（selectedIndex 99）/ missing-input（仅声明 model）/ attempted（恒选 index 0）。
  - **执行期实证（契约修订，D2 落档）**：`<expr>` filter op 在 **XML 规则文件中不可用**——`FilterBeanToPredicateTransformer.visitUnknown` 读 `value` attr 强转 `Expression`，XML body 文本不会编译进该 attr → 编译期 NPE（实测）。`<expr>` 仅适用于编程式构造的 filter bean。**契约修订：XML 规则文件一律用 computed 输入模式**（computed 输入须声明在依赖输入之后——scope locals 按声明序写入）。本 plan 测试规则全部采用 computed 模式（契约"至少一种"满足）；策略 javadoc 同步修订。
  - **`TestRuleBasedSelectionStrategy`**（nop-ai-core，13 用例）：computed 规则选中 index 1 / health-based computed 规则 / 未命中 null / 命中无输出 null / 越界 fail-loud（`ERR_AI_AGENT_INVALID_ARG` 断言）/ 命中已尝试 fail-loud / 输入契约断言（candidates 不含 accountKey、health 键 = Integer、attempted = index 集、请求属性传入、options null → model/provider null）/ 未声明输入 → `ERR_RULE_UNKNOWN_INPUT_VAR`（平台硬约束）/ ruleManager 未注入 fail-fast / health null fail-fast / **接线验证**（router 运行时消费规则策略——默认策略会选主账号、规则策略选 backup-1，断言选中结果 = 规则决策）/ **端到端全链**（规则文件 → IRuleManager → 策略 → router → 候选下沉 ChatOptions 四字段 = 规则选择）。
  - **`TestRuleBasedSelectionStrategyIoC`**（nop-ai-gateway，2 用例）：AppnLoader 不加载 autoconfig + 显式 `<import resource="/nop/rule/beans/rule-defaults.beans.xml"/>` → 策略 bean 可解析、ruleManager 注入真实 `nopRuleManager`、select() 经注入管理器执行 gateway 本地规则（Anti-Hollow 行为断言）；无 rule-defaults 场景 → `ioc:optional` ref 解析 null、容器正常启动、首用 fail-fast（`ERR_AI_AGENT_INVALID_ARG`）。
- **排障记录**：gateway IoC 测试首跑 no-loader-for-path——根因 = resolve-rule 版本化文件路径约定（`{ruleName}/v{version}.rule.xml` 文件名承载版本，非目录分层）+ 规则文件是 nop-ai-core test 资源不在 gateway 测试 classpath → gateway 本地规则文件修复。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 全绿含 13 新用例 + 既有 routing 25 用例零回归、gateway 98/98 含 2 新 IoC 用例、agent 全绿）。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 4）；本日志已更新。



### nop-ai-gateway-failover W5b Phase 2 落地：RuleBasedSelectionStrategy + pom 依赖 + bean 注册

- **Plan**：`ai-dev/plans/2026-08-15-1116-1-nop-ai-gateway-w5b-rule-selection-strategy.md`（W5b，Phase 2）
- **做了什么**：
  - **`RuleBasedSelectionStrategy`**（nop-ai-core `io.nop.ai.core.routing`，`ISelectionStrategy` 实现）：setter 注入 `ruleManager`（`IRuleManager`）+ `ruleName`（必填）+ `ruleVersion`（可选，null = 最新）；`select()` 构建固定契约输入集（model/provider/candidates/health/attempted，**不含 accountKey**——安全裁定；health 键 = Integer；attempted = index 集）→ `ruleManager.getRule(ruleName, ruleVersion).executeForOutputs(ruleRt)`（resolve-rule 版本化解析）→ 语义分支：未命中 / 无输出 → null；越界 / 命中已尝试 / 非 Integer 输出 / ruleManager 未注入 / ruleName 空 / candidates null / health null → `ERR_AI_AGENT_INVALID_ARG` + `ARG_MSG` fail-loud（无静默跳过）；javadoc 完整落档输入/输出契约 + 谓词编写模式（`<expr>`/computed，下标禁令）+ index 稳定性前提 + **单例 stateless 契约**（每 select 新建 ruleRt）。
  - **pom**：nop-ai-core 新增 `nop-rule-core` 编译依赖（无环：nop-rule-core deps = rule-api/xlang/ooxml-xlsx）。
  - **bean 注册**（nop-ai-gateway `ai-gateway-defaults.beans.xml`）：`nopAiRuleBasedSelectionStrategy`（ioc:type=ISelectionStrategy，非默认——默认策略不变），`ruleManager` ref `nopRuleManager` **`ioc:optional="true"`**（对齐既有先例，未部署 nop-rule 的容器可启动，首用 fail-fast）+ `ruleName` @cfg（空缺省 = 未配置 → 首用 fail-fast）+ ruleVersion 省略（null = 最新）。
- **验证**：`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway -am` BUILD SUCCESS。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 4）；本日志已更新。



### nop-ai-gateway-failover W5b Phase 1 落地：DSL 形态 + IoC 绑定 + 模块归属 + 规则契约裁定（内联落档）

- **Plan**：`ai-dev/plans/2026-08-15-1116-1-nop-ai-gateway-w5b-rule-selection-strategy.md`（W5b，Phase 1）
- **裁定（全部 live repo 核实）**：[D1] DSL 形态 = 平台 `rule.xdef`（`rule.register-model.xml` 注册 rule.xml/yaml/xlsx 加载器 + resolve-rule 解析链实证；拒绝 xpl 自造第二套——复用优先不变式）；[D2] 规则输入/输出契约（`RuleModelCompiler`/`NormalizeInputExecutableRule`（≥1 input 硬约束、UNKNOWN_INPUT_VAR/mandatory/computed 四码实证）/`NormalizeOutputExecutableRule`（未命中也校验输出 → selectedIndex 不得 mandatory + type=int 引擎 cast 快速失败）/`XLangASTBuilder.buildPropExpr` 无下标支持 → 谓词下标禁令（`<expr>` op 或 computed 输入）/安全裁定（candidates 不含 accountKey = apiKey 明文）/语义分支 ①②③（未命中/无输出 → null；越界/命中已尝试 → `ERR_AI_AGENT_INVALID_ARG` fail-loud）/index 跨调用稳定性前提/单例 stateless 契约）；[D3] IoC 绑定 = setter 注入 ruleManager + ruleName/ruleVersion，注册于 nop-ai-gateway `ai-gateway-defaults.beans.xml`，ref 必须 `ioc:optional`（构建期 ref 校验 + 既有先例实证），null ruleManager 首用 fail-fast；[D4] 模块归属 = nop-ai-core `io.nop.ai.core.routing`（§4.1 归属表优先），pom 新增 nop-rule-core 编译依赖（nop-rule-core deps 实证无 nop-ai = 无环）；[D5] 错误码复用 `ERR_AI_AGENT_INVALID_ARG` + `ARG_MSG` 不新增码；[P1] Proof：nop-ai-core 无 io.nop.rule 引用（grep 实证）、依赖无环、W2/W3/W5 产物零改动。
- **验证**：全部裁定经 live code 逐条核实（文件 + 行号见 plan 内联落档）；grep/proof 实证完成。
- **doc-sync 裁定**：`No owner-doc update required`（正式回填在 Phase 4）；本日志已更新。


# 2026-08-15


### nop-ai-gateway-failover W7 closure：独立 audit closure-approve + Plan Status completed + roadmap W7 done

- **Plan**：`ai-dev/plans/2026-08-15-1116-3-nop-ai-gateway-w7-gateway-interceptor-converter.md`（W7 全 5 Phase 落地）
- **独立 closure audit**：fresh general subagent（`ses_ffb8e0286ffeaulvaUmGueoITH`，read-only）10 维全 PASS，verdict closure-approve——(1) 调用链运行时连通（onRequest → context attribute → StreamingProcessor → BufferedStreamingPublisher → retryCallback → converter → onStreamElement，逐环 file:line 实证）；(2) E2E 复跑 8/8 绿（2 fetch 断言 + B-14 URL + B-10 输出 + 并发归 0）；(3) nop-gateway 零 nop-ai 依赖 grep 实证；(4) 复用优先（消费 W5/W2 原语 + W6 分类，无第二套机制）；(5) AiFailoverGatewayInterceptor 零改动；(6) 无静默跳过/空壳；(7) 文档/roadmap/log 同步；(8) deferred 分类诚实；(9) 文本一致性（5 Phase completed + 全 [x]）；(10) 全量复跑绿（gateway 152 + nop-gateway 64）。
- **关闭动作**：Closure Gates 16 项全勾选 + evidence 落档（含 Anti-Hollow 检查 + deferred 分类检查 + 验证命令结果）；`Plan Status` → `completed`；roadmap W7 `planned` → `done`（Phase 5 已置）；requirement §3.2/§3.3/§4.3/§4.4/Q 表回填（Phase 5）。
- **验证（关闭时复跑）**：`check-plan-checklist.mjs --strict` exit 0（Passed: 1）；`check-doc-links.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-ai-gateway --severity high` exit 0（0 findings）；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（gateway 152 全绿）；`./mvnw test -pl nop-service-framework/nop-gateway` BUILD SUCCESS（64 全绿）；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；checkstyle 按 mission 既有兜底判定（全仓既有基线 9164 违规，非有效门禁）。
- **doc-sync 裁定**：本条目为 W7 收口记录；roadmap 下一 todo = W8。


### nop-ai-gateway-failover W7 Phase 5 落地：网关测试收口 + 文档同步

- **Plan**：`ai-dev/plans/2026-08-15-1116-3-nop-ai-gateway-w7-gateway-interceptor-converter.md`（W7，Phase 5）
- **做了什么**：
  - **测试 harness**（`W7GatewayTestSupport`）：最小全局 BeanContainer（`nopAiGatewayFailoverInterceptor` + `nopBackendMessageConverter_AI_DIALECT` per-test holder；注册于 CoreInitialization 之前——IocCoreInitializer 捕获为 parent、destroy 原样恢复；未知 bean fail-fast）+ 测试 gateway.xml（`/nop/test/w7-failover.gateway.xml`：`<interceptors>` 挂载 + w7-stream/w7-stream-window1/w7-stream-nobuffer/w7-stream-disabled/w7-nonstream 五路由）+ 模型缓存 interceptor 实例 per-test 重置（`getOrCreateInterceptor` 缓存修复——跨测试串用根因）+ tier-gw-switch 模型类（gw-test2 openai → gw-anthropic anthropic 跨 dialect 重订阅夹具）+ gw-anthropic.llm.xml（Phase 3 已建）。
  - **流式全链 E2E**（`TestAiGatewayFailoverInterceptorStreaming` 8 用例）：端到端窗口内失败跨 dialect 重订阅（B-10 反向转换/B-12 context attribute 接线/B-14 base 替换 URL/Major-1 缓冲丢弃/并发配对）+ 缓冲关闭仍计数重订阅 + NON_TRANSIENT 降级终止（B-17）+ 窗口外断流不切换 + 预算耗尽断流 + 并发饱和 fail-loud + 健康度饱和 fail-loud + 无路由组直通零回归。
  - **路由级测试**（`TestAiGatewayFailoverInterceptorNonStreaming` 5 用例）：网络错误 → TRANSIENT → 账号链切换成功（认证头下沉）+ F3 baseUrl 覆盖经 URL 表达式消费（`??` 空合并——XLang 无 elvis）＋ F2 判定同构无双转（streaming.enabled=false）+ 预算耗尽 fail-loud + 无路由组直通。
  - **文档同步**：需求文档 §3.2/§3.3/§4.3/§4.4 落地状态 + Q 表 Q2/Q3/Q5/Q7 处置（Q7 spike 结论回填）；roadmap W7 → done。
  - **执行期发现并修复**：`getBooleanProperty` 对缺失属性返回 null → 拦截器 `isActive` 统一 `Boolean.TRUE.equals` 判空（防 NPE）；非流式响应路径分类差异落档（httpStatus 启发式 + 429/5xx 异常路径恢复响应级分类）；XLang 表达式单 `${}` 包裹（t-expr 模板语义）。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（gateway 152 全绿含 13 新增，core/agent 零回归）；nop-gateway 64 用例全绿；`node ai-dev/tools/check-doc-links.mjs --strict` exit 0。
- **doc-sync 裁定**：上述文档即本 Phase 交付物；本日志已更新。


### nop-ai-gateway-failover W7 Phase 4 落地：拦截器 + 重执行回调 + 并发计数 + beans 注册

- **Plan**：`ai-dev/plans/2026-08-15-1116-3-nop-ai-gateway-w7-gateway-interceptor-converter.md`（W7，Phase 4）
- **做了什么**：
  - **`AiGatewayFailoverInterceptor`**（`io.nop.ai.gateway.failover`，包名裁定同 W6）：`onRequest` 流式路径（判定与 RouteExecutor:76 同构，F2）首次选择 + converter 转换（stream=true）+ properties 下沉 + 回调/监听器经 context attribute 注入（B-12）；`invoke` 非流式路径选择/切换/重试编排（W6 动作表对齐：acquire 后复查、recordFailure/recordSuccess、预算、CACHE_STATE_LOST 固定候选原地重发、全池饱和 fail-loud）；`onError`（B-17：流式可重试 + CACHE_STATE_LOST rethrow，NON_TRANSIENT 降级终止；非流式/未接管纯观测 rethrow）；`onStreamElement` per-attempt 反向转换（B-10）。
  - **`GatewayStreamingRetryCallback`**（M-3/B-12 窄接口）：分类 → 动作表 → 被动重选（含探活恢复，`FailoverProbeSupport` 复用 W6 D5b-D5d 语义）→ `sinkCandidate` 更新 properties（B-10 传播链）→ `dialect.buildUrl(base, chatUrl, apiKey)` base 替换语义重建 HttpRequest（B-14，含目标账号 apiKey：备用 = accountKey 直沉 / 主 = resolveApiKey）。
  - **`GatewayStreamingLifecycleListener`**：fetch 发起 +1 / 终止 -1（释放上次 acquire 记录的键，重订阅 -1 旧 +1 新；缓冲关闭仍计数；B-2/B-13 预检语义）。
  - **`FailoverProbeSupport`**：selectNextWithProbe + 探活遍历（类内 + provider 链扩展候选池，W6 D5c/D5d 同款）。
  - **beans 注册**（ai-gateway-defaults.beans.xml）：`nopAiGatewayFailoverInterceptor`（breaker/registry 复用 W6 单例 bean + converter ref `nopBackendMessageConverter_AI_DIALECT` + retryBudget @cfg + primaryProvider @cfg）；挂载契约落档（M-7：仅 beans 注册不挂载路由，须 gateway.xml `<interceptors>` bean 引用）。
- **验证**：`./mvnw compile -pl :nop-ai-gateway -am` BUILD SUCCESS；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（gateway 139 全绿零回归）。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 5）；本日志已更新。


### nop-ai-gateway-failover W7 Phase 3 落地：converter 流式扩展 + 动态 dialect + 真实 config

- **Plan**：`ai-dev/plans/2026-08-15-1116-3-nop-ai-gateway-w7-gateway-interceptor-converter.md`（W7，Phase 3）
- **做了什么**：
  - **`FailoverConstants`**（`io.nop.ai.gateway.failover`）：per-request property 键（provider/model/accountKey/apiStyle/stream/attempt）+ context attribute 键（router）；baseUrl 复用 nop-gateway `GatewayStreamingConstants.PROP_BASE_URL`（GW-A4/B-9 properties 通道）。
  - **`AiDialectBackendMessageConverter`**：per-request 动态 dialect（property `apiStyle` 优先 → provider 配置 apiStyle → bean 属性兜底；非法枚举 fail-loud）+ model 覆盖（Q2 路由覆盖，property `model` 优先于请求体）+ 真实 config（property `provider` → `LlmConfigHelper.loadConfig` 替换 `new LlmModel()`；缺失 fail-loud）+ stream=true 请求体生成（property `stream`；无 = false 零回归）；`toFrontendResponse`/`toFrontendStreamChunk` 读当前 request properties（per-attempt 反向 dialect，GW-A5/B-10）。
  - **新测试 fixture**：`gw-anthropic.llm.xml`（apiStyle=anthropic，真实 config apiStyle 观测面）。
- **测试**：`AiDialectBackendMessageConverterTest` 新增 10 用例全绿（stream=true/stream 缺省 false/动态 dialect 覆盖 bean/model 覆盖/真实 config apiStyle/缺失 provider fail-loud/非法 apiStyle fail-loud/响应方向动态 dialect/流式 chunk per-attempt dialect），既有 19 用例零改动零回归（29 全绿）。
- **验证**：`./mvnw test -pl :nop-ai-gateway -Dtest=AiDialectBackendMessageConverterTest` BUILD SUCCESS。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 4）；本日志已更新。


### nop-ai-gateway-failover W7 Phase 2 落地：nop-gateway 最小通用改动（缓冲/重订阅层 + 生命周期回调 + wrapper）

- **Plan**：`ai-dev/plans/2026-08-15-1116-3-nop-ai-gateway-w7-gateway-interceptor-converter.md`（W7，Phase 2）
- **做了什么**：
  - **`gateway.xdef`**（nop-kernel/nop-xdefs protected area，本 plan 即 plan-first 载体）streaming 元素新增 `bufferEnabled`（boolean 缺省 false）/`bufferSize`（int 缺省 10）/`bufferTimeMs`（long 缺省 1000）；`./mvnw install -pl nop-kernel/nop-xdefs -am -DskipTests` → nop-gateway 构建触发 `gen-gateway-ast.xgen` 再生成 `_GatewayStreamingModel`（git diff 仅 3 字段 90 行新增，零模板漂移）。
  - **新接口**（`io.nop.gateway.core.streaming`，零 nop-ai 依赖）：`IStreamingRetryCallback`（错误 + route/request/context → 新 `HttpRequest`，null = 不重试——窄接口，fetch + 映射链重跑留在缓冲层内部）+ `IStreamingLifecycleListener`（`onFetchStarted`/`onStreamTerminated(cause)`，exactly-once）+ `GatewayStreamingConstants`（attribute 键 + `nop.gateway.streaming.baseUrl` property 键）。
  - **`BufferedStreamingPublisher`**（`io.nop.gateway.core.executor`，机制 A A1 链内）：单订阅者 publisher；每次 attempt fetch（惰性）+ 映射链重跑；首段缓冲（N/T 先到者越窗，`bufferEnabled` 缺省 false = 零缓冲直通）；窗口内失败 → 丢弃已缓冲元素（Major-1）→ 重执行回调（null = 不重试）→ 重订阅（新窗口）；窗口外失败 → 原样转发错误；委托式 subscription wrapper（cancel 委托当前活跃 attempt 的上游 sub——abort 经 wrapper 生效，B-11）；生命周期回调与缓冲开关解耦（缓冲关闭仍触发计数）；`terminated` 标志 abort 双触发 exactly-once 去重（F4）；重试不重放 onStreamStart/拦截器钩子（B-15）。
  - **`StreamingProcessor`**：缓冲层 engage 条件 = 回调/监听器任一存在或 bufferEnabled（从 context attribute 读取，B-12 注入通道；缺省 null = 零回归直通）；`applyBaseOverride` base 替换语义（request property `nop.gateway.streaming.baseUrl` 覆盖求值 URL 的 scheme://authority 之后部分，保留 path+query；缺省 = 既有表达式求值零回归，B-14）。`StreamingResponse`/`RouteExecutor` 零改动。
- **测试**：`StreamingProcessorBufferingTest` 11 用例全绿（窗口内重订阅端到端 + attempt1 缓冲丢弃 + 回调拒绝断流 + 窗口外不 consult 回调 + 零缓冲直通 + 参数默认关闭 + 生命周期计数 + abort 双触发去重 + 静默取消经 wrapper + 重订阅后 abort 取消新 sub + B-12 注入 + base-url 覆盖 + 零回归）。
- **验证**：`./mvnw test -pl nop-service-framework/nop-gateway` 全绿（64 用例，含既有 AiFailoverGatewayInterceptorTest 兼容回归）；改动文件 grep 实证零 `io.nop.ai` 引用。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 4；gateway.xdef 生成物变更记录在 Phase 4 需求文档 §3.4 同款处理）；本日志已更新。


### nop-ai-gateway-failover W7 Phase 1 裁定落档（GW-A1..GW-A10，live 核实）

- **Plan**：`ai-dev/plans/2026-08-15-1116-3-nop-ai-gateway-w7-gateway-interceptor-converter.md`（W7，Phase 1）
- **裁定（全部 live repo 核实）**：[GW-A1] Q3 参数与 W6 一致（N=10/T=1000ms 先到者越窗、retryBudget=2、总延迟上限 null）；[GW-A2] 流式 converter 接线分工（B-8：拦截器只对流式路径转换，判定与 RouteExecutor:76 同构；非流式保持 executeRouteLogic 既有调用，无双转）；[GW-A3] 请求实例一致性契约（F1：onRequest 原地修改返回同一实例；流式 failover 路由不配 requestMapping/onRequest xpl）；[GW-A4] per-request 信息通道 = `ApiRequest.properties`（B-9：@JsonIgnore 实证 + 拒绝 headers 通道（转发/伪造/顺序耦合））；[GW-A5] per-attempt 反向 dialect（B-10：重执行回调同步更新 properties，onStreamElement 读当前）；[GW-A6] 并发计数通道（B-2/B-11/B-12/B-13/F4：生命周期钩子进 nop-gateway 缓冲层（onFetchStarted/onStreamTerminated）；委托式 subscription wrapper 取代重注入；context-attribute 注入通道（GatewayHandler:77 硬构造断层实证）；abort 双触发 exactly-once 去重；double-booking 接受为预检语义）；[GW-A7] nop-gateway 改动面（机制 A：`BufferedStreamingPublisher` 插在 createMappedPublisher 与 StreamingResponse 之间；回调窄接口（错误+上下文→新 HttpRequest）；base 替换语义（buildStreamingHttpRequest 读 `nop.gateway.streaming.baseUrl` property，缺省表达式求值零回归）；非流式 baseUrl 经 route 表达式读 properties；gateway.xdef streaming 加 bufferEnabled/bufferSize/bufferTimeMs（codegen 再生成）；StreamingResponse.publisher 可替换化裁定不必要；缓冲关闭计数仍触发；未配置 = 零缓冲直通显式声明）；[GW-A8] onError 职责（B-17：可重试 rethrow、NON_TRANSIENT 降级终止、窗口内外唯一归属缓冲层；B-18 链上吞错前提落档）；[GW-A9] 执行依赖（W6 已落地 git 实证 ea07e1d95/54d49a76a，分类动作表对齐）；[GW-A10] Proof：spike 行号复核一致、流式 converter 零调用点 grep 实证、ApiRequest.properties 通道实证、converter/拦截器测试与 bean 注册点盘点。
- **验证**：全部裁定经 live code 逐条核实（文件 + 行号见 plan GW-A1..GW-A10 内联落档）；grep/proof 实证完成。
- **doc-sync 裁定**：`No owner-doc update required`（正式回填在 Phase 4）；本日志已更新。


### nop-ai-gateway-failover DRAFT_PLANS：W5b/W6/W7 三份 plan 起草 + 三轮独立审查 consensus 通过

- **起草**（mission-driver DRAFT_PLANS 轮）：roadmap 剩余 todo = W5b/W6/W7/W8；按 roadmap 规则"取第一个 todo，不跳过、不重排" + 前序计划 deferred 项（W5 → Q10 规则策略 successor、W1 → Q2/Q5 待人工裁决、Q3 执行期裁决、Q7 回填归 W8）挑选 **W5b + W6 + W7** 三份（W8 留后续轮）。全部 Current Baseline 经 live repo 核实（W5 routing 包 8 类 API、nop-rule IRuleManager/RuleModel/rule.xdef、ChatServiceImpl 双源错误/急切启动、W1 spike SPIKE-01/02 结论、nop-gateway 流式链路行号、converter 现状、ApiRequest.properties、NormalizeInput/OutputExecutableRule 硬约束等）。
- **计划清单**（均 `Plan Status: active`，roadmap 对应条目 → `planned`）：
  - `2026-08-15-1116-1-nop-ai-gateway-w5b-rule-selection-strategy.md`（W5b 规则选择策略：rule.xdef DSL + IRuleManager + ioc:optional 绑定 + selectedIndex 输出契约 + 候选 index 防密钥泄露 + `<expr>`/computed 谓词模式 + 单例 stateless）
  - `2026-08-15-1116-2-nop-ai-gateway-w6-local-adapter-streaming-failover.md`（W6 本地适配器：包装 ChatServiceImpl + 探活恢复/探活池含链候选 + per-attempt token + acquire/release 全路径配对 + 流式分类经 parseErrorResponse 恢复 + 缓冲元素丢弃 + 单例共享）
  - `2026-08-15-1116-3-nop-ai-gateway-w7-gateway-interceptor-converter.md`（W7 网关形态：流式 converter 接线分工（B-8 无双转）+ ApiRequest.properties 通道 + 委托式 subscription wrapper + context-attribute 注入通道 + onError 职责划分 + base 替换语义 + nop-gateway 最小通用改动）
- **独立审查**（每 plan 三轮 fresh-session 对抗性审查至 consensus，含想象性分析）：
  - W5b：R1 2 Major（候选 id 泄露 apiKey、规则输入契约缺平台硬约束）→ R2 3 Major（selectedIndex mandatory 漂移、filter-bean 下标不可用、ref 须 ioc:optional）→ R3 **consensus approve**（0 Blocker 0 Major，3 Minor 措辞已在 Phase 1 落档修正：ERR_EXEC_SCOPE_VAR_IS_UNDEFINED fail-loud 非静默、空 list mandatory 措辞、行号微调）。
  - W6：R1 1 Blocker（熔断 HALF_OPEN 探活恢复不可达）+ 6 Major（per-attempt token/配对/流式分类/checkRateLimit 同步抛/单例共享/竞态）→ R2 2 Major（缓冲元素丢弃语义、探活池含链候选）+ 10 Minor → R3 **consensus approve**（0 Blocker 0 Major；Minor-1 非流式 attempt 前取消检查、Minor-2 流式成功 recordSuccess 挂钩已并入 Phase 2/3）。
  - W7：R1 2 Blocker（流式 converter 零接线、并发计数通道无机制）+ 4 Major → R2 1 Blocker（非流式双重转换）+ 8 Major（properties 通道事实修正/per-attempt dialect/委托 wrapper/注入通道/double-booking/base 语义/SSRF/onError 职责）→ R3 **consensus approve**（0 Blocker 0 Major；F1-F4 请求实例一致性/判定同构/非流式 baseUrl/abort 双触发去重已并入 Phase 1）。
- **roadmap 同步**：W5b/W6/W7 三条目 `todo` → `planned`（附 plan 文件引用 + consensus 记录）。
- **遗留**：W8（指标 + 收口 + docs）待 W6/W7 落地后下轮起草；Q2/Q5 人工裁决未收口（W6/W7 实现前必须收口，三 plan 均按 spike 推荐默认起草 + plan-first 回退声明）。



### nop-ai-gateway-failover W6 Phase 1 落地：全部裁定落档（Q3 默认值 + 注册形态 + 分类动作表 + 探活 + 并发 + 单例 + 盘点）

- **Plan**：`ai-dev/plans/2026-08-15-1116-2-nop-ai-gateway-w6-local-adapter-streaming-failover.md`（W6，Phase 1）
- **裁定（全部 live repo 核实，D1-D10 + P1 落档于 plan 文件）**：[D1] 缓冲窗口 N=10/T=1000ms 可配置；[D1a] 窗口内/外 = "是否有数据已送达下游（forwarded）"——N/T 只决定缓冲转透传时点（与需求 §3.2"未转发任何数据时失败可重订阅"精确对齐，免调度器依赖）；[D2] 窗口内失败丢弃已缓冲元素（Major-1）；[D3] retryBudget=2（初始+2 次重订阅），总延迟上限默认 null，主动切换不耗预算；[D4] bean id `nopChatServiceFailoverAdapter`（ioc:type=IChatService，独立 bean 不覆盖 ioc:default）；[D5/D5a] 分类动作表双源合并 + 流式路径 NopException ARG_HTTP_STATUS → parseErrorResponse 恢复响应级分类（M-3）、parseErrorResponse 抛异常回退 classifier（Minor-8）；[D5b] 熔断探活恢复（B-1）：饱和时对 OPEN 候选显式 allowCall 探活；[D5c] 探活池 = 类内 + resolveProviderChainCandidates（Major-2）；[D5d] 先并发复查后探活 + 只探未尝试候选（Minor-1）；[D6] 记账归属 = 编排层逐个 recordFailure/成功 recordSuccess；[D7] 非流式也计数 + acquire/release 全终止路径配对（M-2）+ 一次释放守卫（Minor-3）+ acquire 后复查（M-6a）+ 无路由组不计数（M-6b）；[D8] breaker/registry 单例 bean 自建（nopFailoverCircuitBreaker/nopFailoverConcurrencyRegistry，M-5/Minor-7）；[D9] IRetryPolicy 排除（Mn-4）；[D10] Q2/Q5 沿用 spike 推荐默认。[P1] Proof：`nopChatService` by-type 消费者唯一实证 = `WfAiHelper.java:75`（getBeanByType）→ 独立 bean 零回归；`CandidateHealth.isAvailable()` HALF_OPEN 可用 = 探活闭环前提（`CandidateHealth.java:65-67`）。
- **验证**：裁定引用全部 live 核实（文件+行号见 plan 内联落档）；grep/proof 实证完成。
- **doc-sync 裁定**：`No owner-doc update required`（正式回填在 Phase 4）；本日志已更新。


### nop-ai-gateway-failover W6 Phase 2 落地：ChatServiceFailoverAdapter（LOCAL-01 + LOCAL-02）+ bean 注册

- **Plan**：`ai-dev/plans/2026-08-15-1116-2-nop-ai-gateway-w6-local-adapter-streaming-failover.md`（W6，Phase 2）
- **做了什么**：适配器 `ChatServiceFailoverAdapter`（`io.nop.ai.gateway.failover`）实现 `IChatService`：非流式链（router → selectNextWithProbe（饱和探活遍历）→ acquire+复查（M-6a）→ 四字段下沉 → 委托 → 全终止路径 release → 动作表决策（CACHE_STATE_LOST 原地重发/NON_TRANSIENT 直接失败/切换类 recordFailure+预算+递归重选）→ 成功 recordSuccess）；无路由组直通零回归。流式层 `FailoverStreamFlow` 文件已就位（Phase 3 条目，本 Phase 编译通过）。bean 注册 = `nopFailoverCircuitBreaker`/`nopFailoverConcurrencyRegistry` 单例 + `nopChatServiceFailoverAdapter`（ioc:type=IChatService 非默认，delegate ref nopChatService，@cfg 参数 buffer-size=10/buffer-time-ms=1000/retry-budget=2）。
- **验证**：`./mvnw compile -pl :nop-ai-gateway -am` BUILD SUCCESS；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（gateway 98/98 + core/agent 零回归）。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 4）；本日志已更新。


### nop-ai-gateway-failover W6 Phase 3 落地：FailoverStreamFlow 流式缓冲/重订阅 + 并发计数（LOCAL-03）

- **Plan**：`ai-dev/plans/2026-08-15-1116-2-nop-ai-gateway-w6-local-adapter-streaming-failover.md`（W6，Phase 3）
- **做了什么**：`FailoverStreamFlow`（`io.nop.ai.gateway.failover`，单订阅者 publisher + attempt 状态机）：首段缓冲窗口（N/T 惰性越窗，无调度器；窗口内 = 未转发 forwarded 判定，T 超时无元素 → 失败仍窗口内，对齐需求 §3.2）；窗口内失败 → 分类（流式路径 NopException ARG_HTTP_STATUS/ARG_BODY → parseErrorResponse 恢复响应级分类，Minor-8 回退 classifier）→ recordFailure → selectNextAfterFailure（含 provider 链扩展）→ 预算 → 重订阅（新 ChatOptions + 新窗口 + 新 per-attempt AttemptCancelToken）；窗口外失败断流报错；onComplete → recordSuccess（Minor-2）+ 缓冲全量交付（窗口只延迟不吞）；per-attempt token（M-1：调用方取消传播到当前 attempt，绝不直接取消调用方 token；每次 attempt 前 isCancelled 检查 Minor-5）；输出侧 cancel 可观测（Minor-10：teardown + 计数释放）；取消顺序（先内部订阅 cancel 再 token cancel）；并发 -1 键 = 被终止 attempt 候选下沉值（Minor-4）+ AtomicBoolean 一次释放守卫（Minor-3）；重订阅同步异常（M-4：checkRateLimit 同步抛 → 流式 onError 语义）；本地限流 per-provider 边界落档（Minor-2）。
- **修正在实现期发现的两处逻辑缺陷**：flushBuffer 窗口未越前不得提前流出（下游 demand 不得穿透窗口）；预算判定 off-by-one（`state.index > retryBudget` 才断流，retryBudget=2 = 初始 + 2 次重订阅共 3 次调用）。
- **验证**：`./mvnw compile -pl :nop-ai-gateway -am` BUILD SUCCESS。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 4）；本日志已更新。


### nop-ai-gateway-failover W6 Phase 4 落地：适配器测试收口（LOCAL-04）+ 文档同步 + roadmap W6 done

- **Plan**：`ai-dev/plans/2026-08-15-1116-2-nop-ai-gateway-w6-local-adapter-streaming-failover.md`（W6，Phase 4）
- **做了什么**：
  - 测试 harness（Mn-3）：真实 `ChatServiceImpl` + fake `IHttpClient`；gateway 本地 fixtures（`_vfs/nop/ai/llm/`：`_default.model-class.xml`（tier-gw/tier-gw-sat/tier-gw-rl）+ `_default.llm-failover.xml` + `gw-test/gw-test2/gw-sat/gw-rl.llm.xml`——nop-ai-core test 资源不在本模块 classpath，自行建夹具）。
  - `TestChatServiceFailoverAdapterNonStreaming` 17 用例：切换链（QUOTA/AUTH/RATE_LIMITED/TRANSIENT）、NON_TRANSIENT 不切换、预算耗尽 fail-loud + 跨账号熔断 OPEN、无路由组直通零回归、并发/健康度饱和 fail-loud、**B-1 探活恢复端到端**（OPEN→冷却 0→allowCall→HALF_OPEN→成功→CLOSED）、**Major-2 探活池含 provider 链候选**（链候选被 allowCall 推进到 HALF_OPEN 证据）、**M-6a acquire 后复查**（盲策略选中饱和候选 → 释放 + 跳过 + 重选）、**M-4 同步限流抛**（gu-rl 第二次调用 fail-fast → 切换链候选恢复 / 预算耗尽 fail-loud）、Minor-1 调用方已取消干净终止、接线（策略运行时调用计数）。
  - `TestChatServiceFailoverAdapterStreaming` 15 用例：**E2E 全链**（缓冲→窗口内 QUOTA→重订阅 backup-a→越窗透传→终止 + Major-1 attempt1 缓冲丢弃断言 + recordSuccess CLOSED）、M-3 流式 401→AUTH_INVALID 切换、预算耗尽断流 + 熔断 OPEN、窗口外断流不切换、窗口期成功终止缓冲全量交付、流式探活恢复 CLOSED、M-4 流式同步限流、全池饱和 fail-loud、M-1 per-attempt 隔离（取消只 teardown 当前 attempt + 只 cancel 当前 attempt token）、Minor-5 已取消干净终态（onError CancellationException）、Minor-10 输出侧 cancel 完整 teardown、取消顺序契约（内部订阅先于 token）、单订阅者显式拒绝、并发 +1 挂钩 callStream 调用时刻（在途观测）。
  - **实现期实证修正**：`NopException.getErrorCode()` 返回 String（饱和码比较改用字符串，修 B-1/Major-2 测试）；Guava RateLimiter 首次 tryAcquire 恒成功（限流测试按"同 provider 第二次调用失败"设计，实证 Minor-2 落档边界）；健康度饱和测试用长冷却 breaker 隔离（冷却 0 时 OPEN 候选被探活放行 = B-1 语义）。
  - 文档：requirement §3.1/§3.2/§3.3/§4.4 落地状态 + Q 表 Q2/Q3/Q5 处置记录（Q2/Q5 沿用 spike 推荐默认，人工裁决回填归 W8 OBS-04；Q3 W6 已裁定 N=10/T=1000ms/retryBudget=2）；roadmap W6 `planned` → `done`（交付摘要落档）。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 369 + gateway 130（98 既有 + 32 新增）+ agent 3387 全绿零回归）；`check-doc-links.mjs --strict` 退出码 0。
- **doc-sync 裁定**：requirement + roadmap 为本 Phase 交付物；本日志已更新。



### nop-ai-gateway-failover W6 closure：Plan Status completed + Closure Gates 全勾选 + 验证全绿

- **Plan**：`ai-dev/plans/2026-08-15-1116-2-nop-ai-gateway-w6-local-adapter-streaming-failover.md`（W6）
- **关闭动作**：Closure Gates 14 项全勾选 + evidence 落档（Closure 段：Phase 1 裁定/实现/测试/各验证命令结果 + Anti-Hollow 检查 + deferred 分类检查）；`Plan Status` → `completed`；roadmap W6 `planned` → `done`（Phase 4 已置）。
- **验证（关闭时复跑）**：`check-plan-checklist.mjs --strict` 退出码 0（全勾选 + Closure Evidence 已写入）；`scan-hollow-implementations.mjs --module nop-ai-gateway --severity high` 退出码 0（High 0 findings）；`check-doc-links.mjs --strict` 退出码 0；`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -q` exit 0；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 369 + gateway 130 + agent 3387 全绿）；checkstyle 按 mission 既有兜底裁定（无有效门禁，同 W2-W5 记录）。
- **doc-sync 裁定**：requirement/roadmap 已回填（Phase 4）；本日志已更新。


### nop-ai-gateway-failover W5 closure：独立 audit 通过 + roadmap W5 done + Q10 successor 登记

- **Plan**：`ai-dev/plans/2026-08-15-0849-2-w5-model-class-routing-and-selection.md`（W5 全 5 Phase 落地）
- **独立 closure audit**：fresh general subagent（`ses_ffcabd969ffed4fyTmRQtVAVBR`，read-only）9 维全 PASS，verdict closure-approve——(1) Phase 1 七项裁定内联落档齐全；(2) Phase 2 xdef/codegen/注册/helper/错误码/10 测试；(3) Phase 3 routing 包 7 类 + 7 测试；(4) Phase 4 ModelClassRouter 语义 + 12 测试（接线 + 端到端）+ 6 注册表测试；(5) 复用优先（routing 包仅 javadoc 引用既有机制，运行时消费 LlmConfigHelper/ThresholdBreaker/resolveFailoverChain）；(6) 无空壳（rg 无空方法体/catch-and-ignore；scan-hollow 0 findings）；(7) 零回归（审计复跑 BUILD SUCCESS：core 356 + agent 3387 + gateway 96，35/35 新用例；nop-ai-agent git status 零改动）；(8) 文档/roadmap 同步（requirement §3.3/§3.4/§4.1 归属解读/Q 表 Q8-Q10、error-handling plan 裁定英文码、roadmap W5 done + W5b）；(9) 文本一致性（check-plan-checklist exit 0）。2 Minor 收尾：roadmap 测试计数 28→35 修正；roadmap W5 done 时序经 audit 确认合法。
- **关闭动作**：Closure Gates 11 项全勾选 + evidence 落档（含 Anti-Hollow 检查结果 + deferred 分类检查）；`Plan Status` → `completed`；roadmap W5 `planned` → `done`（W5 交付：model-class.xdef 配置面（Q8）+ 解析 + routing 包（策略/游走/并发注册表）+ 饱和 fail-loud + 35 用例 + 文档回填）；**Q10 规则策略 successor 双登记**（roadmap Work Items `W5b` 条目 + plan Deferred But Adjudicated 段）。
- **验证（关闭时复跑）**：`check-plan-checklist.mjs --strict` exit 0（全勾选 + evidence）；`scan-hollow-implementations.mjs --module nop-ai-core --severity high` exit 0（0 findings）；`check-doc-links.mjs --strict` exit 0；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（审计复跑）。
- **doc-sync 裁定**：本 plan 交付物 = 配置面 + 解析 + routing 组件 + 测试 + 文档回填；本日志已更新。



### nop-ai-gateway-failover W5 Phase 5 落地：文档同步 + roadmap 状态推进

- **Plan**：`ai-dev/plans/2026-08-15-0849-2-w5-model-class-routing-and-selection.md`（W5，Phase 5）
- **做了什么**：
  - `02-account-failover-requirement.md`：§3.3 落地状态块（模型类路由组/动态选择/游走 + 并发记账 + 饱和 fail-loud 已落地 nop-ai-core，指向 plan `2026-08-15-0849-2`；in-core = 游走原语，编排归 W6/W7）；§3.4 两条配置面工作项各附落地状态（Q8 = 新建 `model-class.xdef` 配置面裁定 + 结构；Q10 = 规则策略拆 successor）；**§4.1 归属解读记录**（in-core 游走原语 vs 网关编排的边界显式落档，防 W6/W7 审计误判归属矛盾）；**§五 Q 表回填**（Q8 已决 = 选项②新建配置面；Q9 已决且 W5 已落地；Q10 已决 = 拆 successor plan）。
  - `01-architecture.md`：核查结论 = **无需改动**（其无 §四.2 分层图——分层图在 requirement doc §4.2，已核对与 routing 包一致；方案 C"nop-ai-core 不感知网关"不受 routing 新增影响）。
  - `docs-for-ai/02-core-guides/error-handling.md`：核查通过 + 补一行——`ERR_AI_MODEL_CLASS_SATURATED` 为 **plan 裁定英文业务码**（公共能力组件跨模块消费语义稳定），与既有"转换类错误码英文"例外并列，均以持有类模块为粒度。
  - **roadmap**：W5 `planned` → `done`；**Q10 successor 双登记**——Work Items 新增 `W5b（Q10 successor）` 条目（`todo`）+ plan Deferred But Adjudicated 段（既有）。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（No errors found）。
- **doc-sync 裁定**：上述文档即本 Phase 交付物；本日志已更新。



### nop-ai-gateway-failover W5 Phase 4 落地：候选集游走 + 并发记账 + 全池饱和 fail-loud（ROUTE-04）

- **Plan**：`ai-dev/plans/2026-08-15-0849-2-w5-model-class-routing-and-selection.md`（W5，Phase 4）
- **做了什么**：
  - **ModelClassRouter**（`io.nop.ai.core.routing`）：per-execution 有状态游走器（对齐 AccountChain 模式）。构造 = 请求 model → 类（显式成员归属）→ 候选展开；`hasRoutingGroup()` false = 零回归（调用方走既有单 provider 行为），误用（无路由组调 selectNext）显式 fail-fast。**主动路径** `selectNext()` 仅类内游走，类内全饱和 → `ERR_AI_MODEL_CLASS_SATURATED`（不跨 provider 链）；**被动路径** `selectNextAfterFailure()`（失败标记 = 编排调本方法）类内耗尽后经 `resolveFailoverChain` 扩展（primary = 请求目标 provider，Phase 1 裁定）→ 扩展后仍无可用 → fail-loud。**饱和语义裁定落地**：并发饱和 + 健康度饱和共用错误码（语义 = 无可用候选，参数 ARG_MODEL_CLASS 区分触发类）；**失败记账归属裁定落地**：router 不在内部记熔断（纯选择机制），recordFailure 归编排层（W6/W7）。`toChatOptions` 下沉四字段（复制原 options 覆盖 provider/model/accountKey/accountBaseUrl——主账号 null = 凭证链语义、accountBaseUrl null = provider 根 baseUrl）。
  - **provider 链候选展开**：`LlmConfigHelper.resolveProviderChainCandidates(primaryProvider)`（failover provider 主账号 + 账号链，model = 覆盖或 defaultModel，W3 并发语义解析）+ 共享 `candidateFor` 构建器。
  - **测试**：`TestModelClassRouter` 12 用例（类内游走声明序 + 下沉四字段 / 无路由组零回归 + 误用 fail-fast / 主动路径并发饱和 + 健康度饱和 fail-loud（错误码 + ARG_MODEL_CLASS 断言）/ 被动路径 provider 链扩展选中 test-failover / 链耗尽 fail-loud / 已尝试不重选 / **接线验证**（记录策略：router 运行时调用策略 + 经健康视图读注册表——预置饱和使主账号被跳过返回 backup-s1）/ **端到端全链**（配置 → 解析 → 归属 → 游走 → 选中 → 下沉四字段 → 全池饱和 fail-loud）/ 下沉保留原 options / resolveProviderChainCandidates 独立复用）；`TestConcurrencyRegistry` 6 用例（acquire/currentCount、release 递减、**release 下溢 fail-fast 且计数不回负**、主账号键与账号键独立、null provider fail-fast、8 线程 × 500 对并发无丢失无下溢）。测试夹具：`test-sat.llm.xml`（limit 2/1/1 饱和场景）、`test-failover.llm.xml`、`_default.llm-failover.xml`（test-accounts → test-sat → test-failover 链）、model-class 配置 + tier-sat 类。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 356/0/3 skipped 为既有禁用用例、agent 3387、gateway 96）；`./mvnw clean install -DskipTests` BUILD SUCCESS；checkstyle 无有效门禁（既有 9164 基线，`echo 'lint not configured'` 兜底，与 W2-W4 记录一致）。
- **复用优先不变式**：router/策略消费既有 `LlmConfigHelper`/`ThresholdBreaker`/`resolveFailoverChain`（代码路径实证 + 测试断言），无第二套账号池/熔断/错误分类。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 5）；本日志已更新。



### nop-ai-gateway-failover W5 Phase 3 落地：选择策略接口 + 默认策略（ROUTE-02/03）

- **Plan**：`ai-dev/plans/2026-08-15-0849-2-w5-model-class-routing-and-selection.md`（W5，Phase 3）
- **做了什么**（全部落 `io.nop.ai.core.routing`）：
  - **候选/健康视图**：`ModelClassCandidate` 扩展 `concurrencyLimit`（解析期按 W3 层级语义解析——账号覆盖 → provider 缺省 → null = 不限制；显式 0/负数 = 显式不限制）；`CandidateHealth`（熔断状态复用 `ThresholdBreaker.getState(provider:model)` + 并发计数 + 并发上限；`isCircuitOpen`/`isConcurrencySaturated`（null/≤0 = 不饱和）/`isAvailable`——冷却期复用 CircuitState 不新建判定）；`IModelClassHealth` 接口 + `CandidateHealthProvider`（复用优先：只做既有机制读取投影，无第二套熔断/并发判定）。
  - **并发注册表** `ConcurrencyRegistry`（Phase 3 为健康视图前置创建，Phase 4 补全测试）：per-`(provider, accountKey)` 计数（主账号 = null 键），acquire/release/currentCount 纯计数器原语（不检查上限——饱和判定在 CandidateHealth）；**release 下溢裁定 = 显式 fail-fast**（`ERR_AI_AGENT_INVALID_ARG`，acquire/release 不配对 = 编排缺陷，不静默钳制）；Java 11 source level 约束（record 不可用 → 手写 Key 类）。
  - **策略接口** `ISelectionStrategy`：输入 = ChatRequest + 候选集 + 健康视图 + 本轮已尝试集；输出 = 选中候选或 null（null = 无可用候选，调用方 fail-loud）；javadoc 契约完整（熔断 OPEN/并发饱和跳过不记失败、已尝试不重复、策略只读、健康视图缺失 = 编程错误显式 fail-fast）。
  - **默认策略** `DefaultSelectionStrategy`：健康度 + 并发感知 + 声明序（跳过 OPEN/饱和/已尝试，按声明序返回首个；全跳返回 null）；与既有 `AccountChain` 声明序语义一致；**无 recordFailure 副作用**。
  - **测试** `TestDefaultSelectionStrategy` 7 用例（OPEN 跳过 / 并发饱和跳过 / 声明序 / 已尝试集跳过 / 全跳返回 null / 饱和跳过无熔断失败记账副作用 / null 健康视图 fail-fast）；`TestLlmConfigHelperModelClass` 扩展 limit 断言（解析期 W3 语义：主账号 10 / backup-2=3 / backup-3=0 / test-modelclass null）。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 338/0/3 skipped 为既有禁用用例、agent 3387、gateway 96）。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 5）；本日志已更新。



### nop-ai-gateway-failover W5 Phase 2 落地：model-class 配置面 + codegen 再生成 + 解析入口（ROUTE-01）

- **Plan**：`ai-dev/plans/2026-08-15-0849-2-w5-model-class-routing-and-selection.md`（W5，Phase 2）
- **做了什么**：
  - **新配置面**（Q8 裁定落地）：`nop-kernel/nop-xdefs/.../ai/model-class.xdef` 新建（`xdef:name="ModelClassConfig"`，bean 包 `io.nop.ai.core.model`；modelClasses keyed by id，每类 = id + members（csv-set 成员清单）+ candidates（provider 必填 + model/accountRef 可选）；javadoc 落档归属/展开/零回归语义）。
  - **codegen**：`gen-ai-xdsl.xgen` 新增第 5 行 `renderModel('/nop/schema/ai/model-class.xdef',...)`（硬编码载体，protected area 触点按 plan 流程）→ `./mvnw install -pl nop-kernel/nop-xdefs -am -DskipTests` → nop-ai-core 再生成：`_ModelClassConfig`/`_ModelClassModel`/`_ModelClassCandidateModel` + 3 保留类，**git diff 实证仅新增文件 + xgen 1 行，零模板漂移**。
  - **fileType 注册（附带修复）**：`llm.register-model.xml` 补 `model-class.xml` → model-class.xdef；**同时补 `llm-failover.xml` → llm-failover.xdef**——grep 实证 W1/W2 时代未登记该 fileType（`resolveFailoverChain` 在有真实 failover 文件时会抛 unknown-model-file-type，属既有潜在断点；无文件 = 空列表零回归不受影响），本 plan Phase 4 被动路径 provider 链扩展依赖该注册，最小修复一并落地。
  - **解析入口**（`LlmConfigHelper`）：`MODEL_CLASS_CONFIG_PATH`（`/nop/ai/llm/_default.model-class.xml`）+ `resolveModelClass(modelName)`（显式成员声明归属、首个声明命中、缺省无文件/未归属/空 model/VFS 未初始化 = null 零回归守卫）+ `resolveModelClassCandidates(modelClass)`（候选展开：accountRef 缺省 = 主账号 + 有序账号链（主账号在前）、accountRef = 单账号、model 缺省 = provider defaultModel；未知 accountRef/未知 provider/缺 defaultModel 解析期 fail-fast `ERR_AI_AGENT_INVALID_ARG`/`ERR_AI_SERVICE_NO_DEFAULT_LLMS`；不可变视图）。新运行时值类型 `io.nop.ai.core.routing.ModelClassCandidate`（provider/model/accountKey/accountBaseUrl + getModelKey provider:model + value semantics）。
  - **错误码**：`NopAiCoreErrors` + `ERR_AI_MODEL_CLASS_SATURATED`（`nop.err.ai.model-class.saturated`，英文消息）+ `ARG_MODEL_CLASS`。
  - **测试**：`TestLlmConfigHelperModelClass` 10 用例（首个声明命中/多类归属/未归属 null/空 model null/缺文件零回归（文件移动式）/展开全链 6 候选断言（主账号在前 + accountRef 单账号 + defaultModel 回退）/null 类空集/不可变视图/未知 accountRef fail-fast/未知 provider fail-fast/缺 defaultModel fail-fast）；测试夹具 `_default.model-class.xml` + `test-modelclass.llm.xml`。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 331/0/3 skipped 为既有禁用用例、gateway 96/96）；`git diff` 零模板漂移。
- **doc-sync 裁定**：`No owner-doc update required`（文档回填在 Phase 5）；本日志已更新。



### nop-ai-gateway-failover W5 Phase 1 落地：Q8/Q10/规模/数据结构/包名错误码/并发键/primary 键 7 项裁定

- **Plan**：`ai-dev/plans/2026-08-15-0849-2-w5-model-class-routing-and-selection.md`（W5，Phase 1 全部裁定内联落档于 plan 文件）
- **裁定汇总**：① 规模拆分 = 单 plan 覆盖 ROUTE-01/02/03-默认/04（反空壳原则：router 是本 plan 内部唯一真实消费链）；② **Q8 = 新建 `model-class.xdef` 配置面**（模型类候选集是跨 provider 全局语义，与 `llm.xdef` per-provider 结构与 `llm-failover.xdef` provider 级链语义均不同层；新 xdef 保持既有 xdef 零改动、零回归面最小；落档：`nop-kernel/nop-xdefs/.../ai/model-class.xdef`，`xdef:name="ModelClassConfig"`，bean 包 `io.nop.ai.core.model`，opt-in 路径 `/nop/ai/llm/_default.model-class.xml`，codegen 载体 `gen-ai-xdsl.xgen` 必须新增渲染行）；③ Q10 = successor plan（out-of-scope improvement，Successor Required: yes，Phase 5 登记）；④ 数据结构 = id + members（显式成员声明，model 名全局匹配，首个声明命中消解歧义）+ candidates（provider 必填 + model 可选 = provider defaultModel + accountRef 可选 = 主账号 + 有序账号链展开）；未归属 = 零回归；⑤ 包名 `io.nop.ai.core.routing` + 错误码 `ERR_AI_MODEL_CLASS_SATURATED`（`nop.err.ai.model-class.saturated`，英文消息，饱和语义 = 并发饱和 + 健康度饱和共用此码）；⑥ 并发键 `(provider, accountId)` / 主账号 `(provider, null)`（W3 语义对齐），挂钩时点契约 = callStream 调用时刻（W1 spike 输入），本 plan 只落计数原语；⑦ provider 链扩展 primary 键 = 请求目标 provider（`LlmConfigHelper.getProvider` 解析值）。
- **Proof**：grep 实证既有消费方（`LlmCallCoordinator.java:72,80` / `DefaultAgentEngine.java:152` / `ReActAgentExecutor.java:290` 等）零改动——新增组件全在 `routing` 新包 + helper 新增方法。
- **doc-sync 裁定**：`No owner-doc update required`（正式回填在 Phase 5）；本日志已更新。



### nop-ai-gateway-failover W4 全 3 Phase 落地：ILlmDialect 双向转换补全（请求 + 响应方向闭环）

- **Plan**：`ai-dev/plans/2026-08-15-0849-1-w4-dialect-bidirectional-conversion.md`（W4，DLCT-01~04）
- **Phase 1（parseRequestBody 四 dialect）**：Anthropic（顶层 system→ChatSystemMessage 首位；content blocks 分派 text/thinking/tool_use/tool_result→ChatMessage 子类；tool_use input Map/JSON 字符串双形态经 parseToolInput fail-fast；**convertMessage 伪影检测**——ChatToolCallMessage.getContent()=arguments JSON 与 tool_use 同发的 text block 相等则丢弃）+ Gemini（systemInstruction.parts→system；parts 分派 text/`<thinking>` 包裹文本→reasoning 逆向剥离/functionCall/functionResponse；generationConfig→options；tools[0].functionDeclarations→definitions；同款伪影检测（JSON.stringify(args)/result 相等）+ Ollama（OpenAI 风格 messages 三角色 + thinking/tool_calls/tool 角色；options.num_predict→maxTokens；tools 嵌套格式；tool_calls 同发 content 伪影丢弃）+ Responses（instructions→system；input[] typed items 分派 message/reasoning/function_call/function_call_output；arguments JSON 字符串容错解析；工具扁平格式）。`ILlmDialect` javadoc/UOE 消息同步（"not implemented for this dialect: "+getName()，default 保留快速失败）；`TestResponsesDialect` UOE 断言反转（2 新用例 + assertThrowsUOE 死代码清理）。
- **Phase 2（buildResponse/buildStreamChunk 四 dialect 覆写）**：Anthropic（content blocks 逆向 + stop_reason 逆映射 end_turn/max_tokens/tool_use + usage 含 cache 字段；流式 content_block_start/delta + message_delta + message_start）、Gemini（candidates[0].content.parts + finishReason STOP/MAX_TOKENS/SAFETY + usageMetadata；流式 parts/functionCall/finishReason）、Ollama（message.content/thinking/tool_calls + done_reason 透传（归一化词汇与原生重合）+ prompt_eval_count/eval_count；流式同构）、Responses（output[] typed items 逆向 + status 逆映射 completed/incomplete/failed + usage；流式 output_item.added/output_text.delta/reasoning_summary_text.delta/function_call_arguments.delta/completed/created 具名事件）。**OpenAI 裁定继承 default 不覆写**（default 即 OpenAI 形态）——裁定与两方向字段映射表落档 plan 附录。
- **Phase 3（测试 + 接线 + 文档）**：新建 `TestDialectBidirectionalConversion` 20 用例（5 dialect × 请求 roundtrip 语义相等 + E2E 请求/响应/流式三方向闭环，真实 Provider 样本；roundtrip 有损点按 plan 附录裁定：finish_reason 归一化目标值、Anthropic 显式 setMaxTokens、system 单条、Anthropic usage cache 字段断言；Anthropic/Gemini/Ollama 请求样本不含 reasoning——convertMessage 双 block 伪影落档）；`AiDialectBackendMessageConverterTest` +10 接线用例（frontendLlm ∈ {anthropic, gemini, ollama, responses} × request/response/stream 三调用点，证明新实现经 ILlmDialect 接口分发被既有消费方运行时可达）+ converter javadoc 同步（"前端始终 OpenAI"→"前端可为任意 dialect"）；`01-architecture.md`（双向转换状态块 + 配置表 + "前端恒 OpenAI" 假设放开）、`02-account-failover-requirement.md` §3.7（落地状态回填 + 附带改动已落地标注）、README 同步；Rule 25 逐方法覆盖对账表落档 plan。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（core 321/0 + gateway 96/0 + agent 3387/0，core 3 skipped 为既有禁用用例）；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；checkstyle 无有效门禁（nop-api-core 既有 9164 条基线，`echo 'lint not configured'` 兜底，与 W2/W3 一致）；`check-doc-links.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-ai-core --severity high` 0 findings；grep 抽查无 "one-way"/"前端恒 OpenAI" 残留表述。
- **doc-sync 裁定**：上述 design 文档 + converter javadoc 已同步（本 plan 交付物）；本日志已更新。



### nop-ai-gateway-failover W4/W5 计划草案：独立审查两轮共识 + 激活 + roadmap planned

- **Plans**：`ai-dev/plans/2026-08-15-0849-1-w4-dialect-bidirectional-conversion.md`（W4，执行序 1）、`ai-dev/plans/2026-08-15-0849-2-w5-model-class-routing-and-selection.md`（W5，执行序 2）——roadmap 首个 todo（W4）与下一项（W5，W2/W3 依赖已 done）各 1 plan；W6/W7 依赖 W4/W5 落地，本轮不草拟。
- **起草**：先完整读 roadmap + 需求规格（§3.3/§3.7/§4.1/§五 Q 表）+ 01-architecture + W1/W2/W3 计划（deferred 项：Q2/Q5 待人工裁决、Q8/Q10 归 W5 plan 阶段、Q7 回填归 W8）；W4/W5 全部 baseline 事实经 live repo 核实（dialect 5 类规模/parseRequestBody 仅 OpenAiDialect:62/无 buildResponse 覆写/TestResponsesDialect:346-350 UOE 断言待反转；LlmConfigHelper 三读取入口/llm-failover.xdef/无 ModelClass/SelectionStrategy 类）。
- **两轮独立审查**（fresh subagent，不同 session）：
  - **W4**：R1 无 Blocker、1 Major（roundtrip 有损点未提示）+ 6 Minor；修复后 R2 同意执行，条件 = F4 残留（Goals/Scope"5 个覆写"与"OpenAI 继承 default"矛盾）——已修（Goals/Scope/DLCT-02 改 4 覆写 + OpenAI 继承 default）+ M2-M5 顺手修订（finish_reason 样本取归一化目标值、system/instructions 折叠提示、converter javadoc 同步、Phase 1 Rule 25 声明）。
  - **W5**：R1 无 Blocker、1 Major（`gen-ai-xdsl.xgen` 硬编码 4 行 renderModel，新 model-class.xdef 需先加渲染行否则零生成物）+ 9 Minor；修复后 R2 同意执行、4 Minor 建议全修（7 项裁定计数、recordFailure 归属 = 编排层、ChatOptions 模块路径+声明行号、裁定落档措辞）。
- **激活动作**：两 plan `draft` → `active`（共识达成）；roadmap W4/W5 `todo` → `planned`；W5 plan Phase 1 载有规模拆分裁定（单 plan 覆盖 ROUTE-01/02/03默认/04 + Q10 规则策略拆 successor plan——满足 roadmap 粒度约束）。
- **doc-sync 裁定**：本条目已更新；后续执行期按各 plan Phase 同步 requirement/architecture 文档与 daily log。


### EXEC_PLANS Cycle 2 / I6' — plan 2026-08-15-0820-3 Phase 3 循环收口与 plan completed

- **C1 统计**：门禁 5（4 Cycle 1 + gate 5）/ red list 67 / 修复 46 / 新族 0——三源可溯（formal-red-list-cycle2 §汇总+§棘轮终态记录、adjudication-table-cycle2 §1、plan Phase 1）；终态命中 21（全豁免驻留）。
- **C2 稳态判定**：**稳态暂停**——gate 1-4 零命中 + gate 5 终态 ⊆ 已批准豁免清单（合法驻留）+ 0 新族 → 不派发 Cycle 3 / I1。**C3**：复触发条件（CI 变红 / 新增或重命名 processor/bizmodel/ORM entity / 周期复探）核对 invariant-guards.md §复触发条件与 roadmap Loop Rule 仍准确。**C4**：roadmap Work Item Status 新增 "Cycle 2 / I4+I5+I6（invariant-loop）. 修复执行 + 全量验证 + 循环收口" ✅ done 行（invariant-loop 标注与再审计 remediation 三行消歧）；Follow-up Backlog 核对无未标注残留。
- **C5 独立 closure audit**（fresh session `ses_ffc56693effemooCIKleNl6BNR`，review-only）：**verdict approved，0 Blocker / 0 Major / 3 Minor**（Minor 均非本计划引入）；15 项检查全 PASS——含 6 处修复 spot-check 逐行证实、恒等式 67=46+21+0 双源对齐、聚合入口独立复跑 exit 0、CI 接线无静默放行、Anti-Hollow (a) 运行时调用链（buildDataSource→validateJdbcUrl / @BizMutation→QueryAction / :323 sandbox 调用）(b) e2e 真实走通（GraphQL mutation + tr locale + 落库断言）(c) 46 修复无空壳/静默 (d) hollow 扫描 exit 0。
- **收口工具**：`check-plan-checklist --strict` exit 0；`scan-hollow-implementations --severity high` exit 0；`check-doc-links --strict` 16 errors = 既有基线（本次修改文件 0 命中，closure audit 独立复核一致）。
- **最终验证**：`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（145 模块 reactor 全绿；service 1207/0/0；一次并行运行的上游瞬时失败如实记录——nop-metadata 两次运行均 SUCCESS，完整复跑全绿）。
- **Plan Status → completed**（Completed: 2026-08-15，Closure Evidence 全文写入 plan Closure 节）；Cycle 2 全链（I1'→I6'）闭环，silent-wrong-result 族进入门禁防回退稳态。


### EXEC_PLANS Cycle 2 / I5' — plan 2026-08-15-0820-3 Phase 2 全量验证与门禁棘轮收口完成

- **V1**：`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS——**1207 tests / 0 failures**（service；全 8 子模块 reactor SUCCESS，无 flaky）。
- **V2 门禁终态**：① Cycle 1 四门禁零命中保持；② gate 5 **baseline 终态重写为已批准豁免清单**——修复后 codebase `--emit-baseline` 生成 21 FP 命中/16 计数感知键（contains 18/13 + delim 3/3），note 记终态语义（优化候选维持 = 0）；**豁免驻留非空 → 保持模式 b**（无模式升级，CI workflow 零变更）；③ 对账 exit 0（16 键内 0 超出）；④ **hard-gate 注入 proof**：`MetaContractChecker` 注入 baseline 外 `s.toLowerCase()` → scanner exit 1（EXCESS: new key not in baseline）+ 聚合入口 exit 1（set -e 传播，CI 将红）→ `git checkout` 还原 → 聚合 exit 0，`git status --porcelain -- nop-metadata/` 净零；⑤ 终态聚合入口整体 exit 0 实测双跑（"All 5 nop-metadata invariant guards passed"）。
- **V3 棘轮记录**：`formal-red-list-cycle2.md` 新增 §棘轮终态记录——逐族表 locale 40→0（修 40）/ narrowing 0→0 / contains 19→18（修 1 驻 18）/ delim 6→3（修 3 驻 3）/ bigdec 2→0（修 2）；恒等式 **67 = 修复 46 + 豁免驻留 21 + successor 0** 与裁决表 §1 逐项对齐；终态机制与注入 proof 记录。`initial-red-list-cycle2.md` 模式归属表同步（baseline 状态列 → 终态豁免清单，仍模式 b）。
- **V4 owner-doc**：`invariant-guards.md` 两处同步——门禁表 gate 5 行（终态 live 0/0/18/3/0 = 21 全豁免 + 46 P1 已修）+ 模式 b 专节新增"终态已达（Cycle 2 / I5'）"条（豁免驻留非空保持模式 b、"零命中"准确语义 = 命中集 ⊆ 已批准豁免清单）。CI 接线核实：`invariant-gate` job（maven.yml:45）调用同一聚合入口（:73），无 continue-on-error/|| true，与归属表一致。
- **验证**：`scan-hollow-implementations --module nop-metadata --severity high` exit 0。


### EXEC_PLANS Cycle 2 / I4' — plan 2026-08-15-0820-3 Phase 1 类别清扫修复完成（46 P1 全修）

plan `ai-dev/plans/2026-08-15-0820-3-nop-metadata-silent-wrong-result-fix-and-cycle2-closure.md` Phase 1（mission-driver EXEC_PLANS）。

- **F1 test-first（修复前红实测）**：新增 10 个测试类/扩展共 31 例，修复前对未修实现跑出 **10 项断言失败**，全部为裁决表预测的失效机理：P1-A 安全 2 处实证（tr-TR 下 `jdbc:mysql://...?allowloadlocalinfile=true` **不抛异常绕过 blocklist**；`insert into` 小写**不被 sandbox 拦截**）；allowed-hosts 混合大小写 host 误拒 ×2（MDCP/CAD）；AR-06 消息线索 `CONNECTION REFUSED` 失配（infra→benign 漂移）；CTE 名 `ITEM_INFO`/`item_info` I/i 失配误报物理源表；SLA 单位 `MINUTE` → `unknown sla time unit: mınute`（错误消息直接可见无点 ı）；C8 子串误分类 ×2 + `tinyint` tr 归一化失配；e2e `{edgeCount=0, unresolved=[big_item]}`（tr-TR 静默 lineage 缺失）。
- **F2 修复（46/46）**：locale 40 站点 → `Locale.ROOT`（10 文件：MDCP 5 / MQRE 4 / CAD 4 / MTP 7 / NMLEQA 5 / SCLE 10 / SSTE 2 / MCC 1 / 契约 1 / ETSR 1）；C8 `MetaTableProfiler.isStringType` → exact-match `STRING_TYPE_NAMES`（17 词条 `Set.of`，沿 AR-05 形态）；D1/D5/D6 → 结构性 `List` 键（沿 AR-03 形态：ACP warn 去重键、NMLEQA `seenKeys`/`existingEdgeMap` 键类型 `Set<List<String>>`/`Map<List<String>,…>`）；E1/E2 → `MemoryFilterEvaluator`/`MemoryOrderByComparator` 私有 `toBigDecimal` 委托 `AggregationHelper.toBigDecimal`（AR-10 路由）。配套：`MetaContractChecker.toDurationMillis` private → package-private（测试可见性，先例 AR-12）。
- **类别清扫核对（权威分母 = adjudication-table-cycle2）**：locale 40=修复 40 ✓；contains 19=修复 1+FP 18 ✓；delim 6=修复 3+FP 3 ✓；bigdec 2=修复 2 ✓；恒等式 67=46+21 ✓。rg 辅助复扫残余仅 `LocalReconciliationProcessor.java:124` javadoc 伪站点（已收录归因）——零未收录站点。D1 探查注记：单 cid 单调用内拼接键碰撞数学不可达（碰撞需 cid 含 `|`）——修复依据为类别立场（格式假设依赖 = AR-03 同机理）；D5/D6 探查事实：本仓 SQL 解析器对引号标识符**保引号**归一化（`"a|b"` 含引号存储），旧拼接键在该形态下恰好可区分——结构性键不依赖该格式假设，e2e 钉死引号 pipe 列名两条边落库 + 幂等。
- **F3 FP 处置（方式 (a) baseline 驻留）**：零源码扰动；修复后 live 命中 = 21（contains 18 + delim 3）⊆ baseline 61 键（对账 exit 0），与裁决表 FP 清单一一对应（Phase 2 V2 重写 baseline 为该 21 键豁免清单）。F4 超时机制未触发（零 successor）。
- **门禁态**：locale 0 / narrowing 0 / bigdec 0 / contains 18 / delim 3 = 21 命中（全 FP）；聚合对账 exit 0。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS——**1207 tests / 0 failures**（service 模块 surefire 汇总；基线 1175 → +32）；web 模块 1/1 绿。
- 新增测试：`TestMetaDataSourceConnectionProcessorLocale`(4) / `TestMetaQualityRuleExecutorSandboxLocale`(3) / `TestCheckpointActionDispatcherLocale`(2) / `TestMetaTableProfilerLocale`(3) / `TestMetaTableProfilerClassification`(+4) / `TestSqlExtractorsLocale`(3) / `TestMetaContractCheckerSlaUnitLocale`(2) / `TestExternalTableStructureReaderLocale`(2) / `TestMemoryFilterAndOrderBy`(+4) / `TestNopMetaLineageEdgeLocaleAndKeys`(4, e2e GraphQL)。
- Doc-sync: `docs-for-ai/03-modules/nop-metadata.md`（AR-10 段 Non-Goal → E1/E2 对齐段；AR-12 条后新增 INV-LOCALE 全量段）。


### EXEC_PLANS Cycle 2 / I2'+I3' — plan 2026-08-15-0820-2 正式审计与零悬挂裁决完成

plan `ai-dev/plans/2026-08-15-0820-2-nop-metadata-silent-wrong-result-audit-adjudication.md` 执行（mission-driver EXEC_PLANS，零产品代码变更）。

- **Phase 1（I2' 正式审计）**：`formal-red-list-cycle2.md` 交付——gate 5 正式运行 67 命中/61 键（locale 40 / narrowing 0 / contains 19 / delim 6 / bigdec 2），与 I1' 快照**零漂移**（三层证据：baseline 对账 61 键内 0 超出 exit 0 + git 溯源快照→正式轮 main 零 commit + JSON 双跑 hits 一致）；`adversarial-probing-notes-cycle2.md` 交付——**10 方向**（≥ max(3,2)，2 个 watch-only 族各 1 专属方向，Current Baseline 候选清单 6/6 全探查零排除，catalog watch 边界 3 项，跨子模块 1 项），**0 live 新命中、0 新族**；1 项 latent-form 观察（`MetaTableProfiler.toLong:548` null→0/裸 NFE 形态，调用面 COUNT-only 不可达）登记 backlog OBS-01。
- **Phase 2（I3' 裁决）**：`adjudication-table-cycle2.md` 交付——**恒等式 67 = 46 P1 + 21 FP + 0 优化候选维持 + 0 新族（零悬挂）**。要点：① locale 40/40 全 P1（无一 display-only；含 2 处安全语义缺陷——`MDCP:70` blocklist 常量 `allowLoadLocalInfile` 含 I 的 tr-TR 大小写变体绕过、`MQRE:387` sandbox 关键字 INSERT 含 I 的注入探测绕过；4 处 latent 形态按类别立场判 P1）；② contains 1 P1（`isStringType:567` = AR-05 兄弟）+ 18 FP（fail-closed 安全探测/定界符精确语义/AR-06 既裁线索/合成标记/方言家族 containment，各附读码理由）；③ delim 3 P1（正则 pattern 与 SQL 派生标识符分量不受控）+ 3 FP（`LineageTagPropagationProcessor` entityType=Java 标识符字符集 + entityId=sys ID，碰撞数学不可达）；④ **bigdec 旧 optimization-candidate 裁定推翻**（1133-2 的"影响较小"是影响评估非正确性结论；WHERE 等值 Long>2^53 静默错配 + String 静默跳过缺陷对完整携带；AR-10 修复形态在库）→ 2 条转 P1。
- **B1b 裁定**：FP 标注方式 **(a) baseline 驻留**（四条理由：最小源码扰动 / FP 理由与规则强耦合与位置弱耦合 / 棘轮不弱化 / I5' 收口兼容——终态豁免清单 = 21 键）。
- **roadmap 同步**：Work Item Status 新增 "Cycle 2 / I2+I3（invariant-loop）" ✅ 行；Follow-up Backlog 新增 OBS-01（watch-only，含源审计路径与 Why Not Blocking）。
- 验证：`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（1174 tests 0 failures，零代码变更复验）；聚合入口 5 门禁 exit 0（gate 5 对账 61 键内 0 超出）；`git diff --stat -- nop-metadata/` = 0 行。
- Doc-sync: No owner-doc update required（审计与裁决工件均属 `ai-dev/audits/` + roadmap）。


### EXEC_PLANS Cycle 2 / I1'（收口）— plan 2026-08-15-0820-1 closure audit approved 并标 completed

- 独立 closure audit（fresh session `ses_ffcd67978ffeKU1RIPzNfQ3gyJ`，review-only 零文件修改）：Phase 1 6/6、Phase 2 9/9、Phase 3 7/7 Exit Criteria PASS，Closure Gates 11/11 PASS，Anti-Hollow (a)(b)(c) PASS，red-list 抽查 3 条与 live 代码吻合——**verdict approved，0 Blocker / 0 Major / 4 Minor**。
- 4 Minor 已收口同批修复：① red-list 汇总表键数口径（locale 40 命中/39 键、contains 19 命中/14 键，合计 61 键与 baseline JSON 一致）；② ③ §5.3 与 red-list §3 行数 16/15 → 14；④ target-set §5.4 复现行残留"口径 3"→ 6；⑤（Minor-1）scanner `scanBigdec` 未平衡括号由静默 continue 改为 INTERNAL 显式命中（与 delim-key 对齐）——修后 fixture 18/18、67 hits、baseline 对账、doc-links 16 errors 基线全部复验不变。
- `check-plan-checklist --strict` exit 0（43/43 勾选 + Closure Evidence 写入）；`scan-hollow-implementations --severity high` exit 0；roadmap Work Item Status 新增 Cycle 2 / I1 行（✅ done，plan 0820-1）。
- 交付物清单：`ai-dev/tools/check-silent-wrong-result.mjs`（新门禁）、`ai-dev/tools/run-nop-metadata-invariants.sh`（4→5 门禁）、`ai-dev/audits/nop-metadata-invariants/{invariant-catalog.md,audit-target-set.md,initial-red-list-cycle2.md,baseline-cycle2/silent-wrong-result.json}`、`docs-for-ai/02-core-guides/invariant-guards.md` + `INDEX.md`、plan 文件（completed）。后继 I2'/I3'（0820-2）以 `initial-red-list-cycle2.md` 为漂移核对基线开工。


### EXEC_PLANS Cycle 2 / I1' — nop-metadata silent-wrong-result 不变式沉淀为可执行门禁（plan 2026-08-15-0820-1）

plan `ai-dev/plans/2026-08-15-0820-1-nop-metadata-silent-wrong-result-invariant-guards.md` 执行（mission-driver EXEC_PLANS）。

- **Phase 1（Decision，子族评估裁定与目录更新）完成**：
  - **D1 逐子族裁定**：5 子族全部裁定为**静态扫描可机械化**，落地形态 = 单门禁 `ai-dev/tools/check-silent-wrong-result.mjs`（1 扫描器 × 5 规则，子族标签）——① locale（无法静态区分机器比较/display-only → 全量报告 + I3' 裁决消化，模式 b）；② narrowing-cast（防回退，操作数浮点类型声明判定的确定性规则，live 0）；③ contains-分类（String-receiver `.contains`，receiver 声明类型判定，live 19）；④ 分隔符-key（key 位实参跨度内拼接 + *Key 变量赋值双规则，live 6）；⑤ Number→BigDecimal（方法体 longValue 路由信号显式排除 `AggregationHelper:554` 受保护形态，live 2）。
  - **D2 watch-only 重估**：类型/方言兼容性族、并发竞态/UK 幂等族均**维持 watch-only**（无新复发证据 + 静态不可机械化理由 + 替代防护，已写入 catalog 候选节）。
  - **D3 目录更新**：`invariant-catalog.md` 新增 INV-LOCALE / INV-NARROW / INV-CONTAINS-CLASSIFY / INV-DELIM-KEY / INV-BIGDEC 5 条（四要素齐全）；`audit-target-set.md` 新增 §5（5 子族目标集，live 计数 40/0/19/6/2 = 67 + rg/扫描器复现命令）并**校正 §1.3 漂移**：catch 块 130/46 文件（2026-08-13）→ **125/45**（2026-08-15 live 重测；原因 = 1448-1/1448-2 死码与诊断退化清扫删除带 catch 死方法/死分支 5 处，commit `08b1ef556` 等；rg 原始 126 = 125 真实 + 1 javadoc 伪站点）。
  - 扫描阶段即发现（交 I3' 裁决的类残留新实证）：`MetaTableProfiler.isStringType:567` 仍为子串分类（AR-05 只修了 `isNumericType` 兄弟）、分隔符-key 族 6 处（`LineageTagPropagationProcessor` `#` 拼接 ×3、`AutoClassificationProcessor:144` warnKey、`NopMetaLineageEdgeQueryAction:258/:487` `|` 三段拼接——AR-03 兄弟，初测 rg 3 处系引用转义遗漏，以扫描器 6 处为准）、`ExternalTableStructureReader:148` 方言子串分类、`MemoryOrderByComparator`/`MemoryFilterEvaluator` 两个 `toBigDecimal` 缺陷形态私有拷贝（AR-10 兄弟）。


### DRAFT_PLANS Cycle 2（silent-wrong-result 不变式闭环三计划）起草并共识审查通过

mission `nop-metadata-invariant-loop` DRAFT_PLANS 轮：从 roadmap Follow-up Backlog「静默错算/精度族（建议下轮 Cycle 2 / I1 评估 silent-wrong-result 不变式）」+ plans 0707-2/1133-2 的 carry-over 建议，起草 Cycle 2 三份计划（I1' 门禁沉淀 → I2'+I3' 审计裁决 → I4'+I5'+I6' 修复收口），经三轮独立 fresh-session 对抗性审查至共识后转 `active`。

- **触发依据（live 实测）**：AR-12 只修 1 处 locale 站点，live main 代码仍有 41 处默认 locale case-mapping（11 文件，含 1 javadoc 伪站点，扫描器口径 40）——"修实例不修类别"根因的类别残留实证；精度族 2 站点（MemoryOrderByComparator:132 / MemoryFilterEvaluator:356）为 1133-2 closure 已裁定 optimization candidate 待重裁。
- **计划链**：`ai-dev/plans/2026-08-15-0820-1-nop-metadata-silent-wrong-result-invariant-guards.md`（I1'：5 子族可机械化评估 + 门禁落地 + `initial-red-list-cycle2`（.md）快照 + CI 棘轮扩展）→ `2026-08-15-0820-2-...audit-adjudication.md`（I2'+I3'：正式 red list + 对抗探查 + 零悬挂裁决）→ `2026-08-15-0820-3-...fix-and-cycle2-closure.md`（I4'~I6'：类别清扫修复 + 终态验证 + 循环收口）。
- **三轮审查修复的关键设计**：① 模式 b（快照对账）完整规格——机器可读 baseline（`baseline-cycle2/` 每门禁一文件）、对账键 = 文件路径+命中行文本归一化、逐键计数 current ≤ baseline、I4' 渐进修复期 ⊆ 语义保持 CI 绿；② FP/优化候选豁免终态承载——终态 baseline 重写为"已批准豁免清单"（方式 a FP 驻留 + 优化候选维持条目）或放行注释（`// invariant-ok:`，能力由 I1' 预实现），消解"豁免残留与零命中互斥"；③ JUnit 形态仅限零命中防回退族（不复活 F3 废除的 surefire 排除）；④ 恒等式权威分母 = 裁决表 + successor 拆分项闭合。
- **审查证据**：R1 `ses_ffd310dcaffeo0kRuRwJlPhwp2` / `ses_ffd30f6b1ffekMFumHG83arfcw` / `ses_ffd30d813ffeMV6OJsGXMin6rq`（1 Blocker + 6 Major）；R2 `ses_ffd23a3e3ffeyidB2Otdnj5kb1` / `ses_ffd238965ffeuvZx7zvnhnt55z` / `ses_ffd236f96ffemhAjl0HNWTm1p`（修复 23/23 验证 + 3 Major）；R3 `ses_ffd197af1ffeP9rgmeHaMWiDl4`（23/23 PASS + N3 豁免终态承载，已修）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 全仓 16 errors 均为预存基线（本次修改的 4 个文件 0 命中 broken-link 报告；前向交付物引用已按检查器口径写成无后缀裸名 + 一次性全路径定义）。
- Doc-sync: No owner-doc update required——三份 plan 均为 `ai-dev/plans/` 执行文档，owner-doc 变更由各 plan 执行阶段 owns；roadmap Work Item Status 行由 0820-3 C4 收口时新增（避免双重所有权）。


### nop-ai-gateway-failover W3 closure：独立 audit 通过 + roadmap W3 done

- **Plan**：`ai-dev/plans/2026-08-15-0604-3-w3-llm-xdef-concurrency-limit.md`（W3 全 2 Phase 落地）
- **独立 closure audit**：fresh general subagent（`ses_ffd2d5f90ffeSth8wHeOmZT0LB`，read-only）9 项检查全 PASS，verdict closure-approve——(1) xdef 语义（root `concurrencyLimit="int"` 非 mandatory + javadoc 完整；account 覆盖 + 回退语义；既有属性零改动）；(2) 生成物 codegen 来源（git diff 71 insertions/0 deletions 仅新增字段，_gen 模板形态 + javadoc 传播实证）；(3) 接线（`LlmConfigHelper.java:187-189/202-206` 从配置模型取值，0/负数无 >0 过滤）；(4) 8 用例五态+负数+接线（assertNull 显式钉死不限制，无静默跳过）；(5) 零回归（两既有测试类 git diff 为空，test-accounts 仍 3 账号）；(6) 构建复跑 BUILD SUCCESS；(7) 两 design 文档 + daily log 同步；(8) deferred 分类诚实；(9) 文本一致性（pre-flip 项已收尾）。
- **关闭动作**：Closure Gates 11 项全勾选 + evidence 落档（含 2 Minor 收尾：L100 措辞改"五态读取测试"、Evidence 落档）；`Plan Status` → `completed`；roadmap W3 `todo` → `done`（W3 交付：llm.xdef 并发上限配置面 + 生成物再生成 + helper 双读取入口 + 8 用例 + 文档同步）。
- **验证（关闭时复跑）**：`./mvnw compile -pl :nop-ai-core -am` 通过；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（TestLlmConfigHelperConcurrencyLimit 8/8 + AccountChain 3/3 + TestLlmConfigHelper 9/9，0 failures/0 errors）；checkstyle 无有效门禁（既有 9164 条基线，`echo 'lint not configured'` 兜底）；`check-doc-links.mjs --strict` / `check-plan-checklist.mjs --strict`（29/29 全勾选 exit 0）/ `scan-hollow-implementations.mjs --module nop-ai-core --severity high`（0 findings）全过。
- **doc-sync 裁定**：本 plan 交付物 = 配置面 + helper + 测试 + 2 design 文档同步（Phase 1 已落）；本日志已更新。


### nop-ai-gateway-failover W3 Phase 2 落地：五态读取测试 + 零回归（XDEF-03）

- **Plan**：`ai-dev/plans/2026-08-15-0604-3-w3-llm-xdef-concurrency-limit.md`（W3，XDEF-03）
- **做了什么**：
  - **测试数据**：`test-accounts.llm.xml` 扩展（root `concurrencyLimit="10"`；backup-1 未配置 / backup-2 显式 3 / backup-3 显式 0——既有 3 账号断言零影响）；新增 `test-concurrency-misc.llm.xml`（root 无配置 + misc-1 未配置 + misc-2 显式 -1，覆盖"均未配置 = 不限制"与"负数显式不限制"两态）。
  - **新测试类** `TestLlmConfigHelperConcurrencyLimit`（8 用例）：provider 缺省读取（root 元素）、账号级覆盖生效（3）、账号未配置回退 provider 缺省（10）、显式 0 = 显式不限制不回退（0，provider 缺省 10）、显式 -1 = 显式不限制不回退、均未配置 = null = 不限制（provider + 账号两入口）、主账号路径（account=null 取 provider 缺省；缺省缺失 = null）、接线验证（`LlmAccountModel.getConcurrencyLimit()` 从配置模型取值，非 stub）。
  - **零回归**：`TestLlmConfigHelperAccountChain` 3/3 + `TestLlmConfigHelper` 9/9 全绿（既有 `<accounts>` 配置行为不变）。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（nop-ai-gateway 84/84 等全绿）；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；checkstyle 无有效门禁（既有 9164 条基线，`echo 'lint not configured'` 兜底——与 W2 记录一致）。
- **doc-sync 裁定**：No owner-doc update required（文档同步已含于 Phase 1）；本日志已更新。


### nop-ai-gateway-failover W3 Phase 1 落地：llm.xdef concurrencyLimit 配置面 + codegen 再生成 + helper 读取

- **Plan**：`ai-dev/plans/2026-08-15-0604-3-w3-llm-xdef-concurrency-limit.md`（W3，XDEF-01/02）
- **做了什么**：
  - **类型裁定**：`concurrencyLimit` = `int` 非 mandatory（无 `!` 前缀，缺失 = null = 不限制），禁止 `!int`（否则既有 `{provider}.llm.xml` 校验全失败）。
  - **xdef**（`nop-kernel/nop-xdefs/.../ai/llm.xdef`）：根元素新增 `concurrencyLimit="int"`（provider 级缺省，javadoc 写明 in-flight 并发上限/跳过语义/与 rateLimit 排队区别/显式 0 = 不限制/非 mandatory/主账号取本值）；`<accounts><account>` 新增 `concurrencyLimit="int"`（账号级覆盖，javadoc 写明未配置回退 provider 缺省、显式 0/负数不回退）。
  - **codegen 再生成**：`./mvnw install -pl nop-kernel/nop-xdefs -am -DskipTests`（本地仓库 schema 更新）→ `./mvnw install -pl :nop-ai-core -am -DskipTests` 触发 precompile codegen（`precompile/gen-ai-xdsl.xgen` 渲染 `/nop/schema/ai/llm.xdef`）；`_LlmAccountModel`（`java.lang.Integer` getter/setter/outputJson/copyTo）与 `_LlmModel`（含 root javadoc 传播）再生成，`git diff` 实证 **71 insertions / 0 deletions，仅新增字段，零模板漂移**。
  - **helper**（`LlmConfigHelper.java`）：新增 `resolveConcurrencyLimit(String provider)`（provider 级缺省；null = 不限制；0/负数 = 显式不限制）+ `resolveConcurrencyLimit(String provider, LlmAccountModel account)`（账号级非 null 即返回不回退；null 回退 provider 缺省；account=null 即主账号路径取 provider 缺省）；语义契约落档 helper javadoc（行为契约权威）。
  - **文档同步**：`nop-ai-llm-error-normalization-design.md` :254 账号字段集描述补 `concurrencyLimit` + 层级语义一行；`02-account-failover-requirement.md` §3.4 既有结构清单补 `concurrencyLimit`（含层级语义一行）。
- **验证**：`./mvnw compile -pl :nop-ai-core -am` BUILD SUCCESS；生成物 diff 仅新增字段（模板漂移检查通过）。
- **doc-sync 裁定**：上述两 design 文档已同步（本 Phase 交付物）；本日志已更新。


### nop-ai-gateway-failover W2 closure：独立 audit 通过 + roadmap W2 done

- **Plan**：`ai-dev/plans/2026-08-15-0604-2-w2-reliability-sink-down.md`（W2 全 4 Phase 落地）
- **独立 closure audit**：fresh general subagent（`ses_ffd4ae94affeqUVzzmxqGjN0Yw`）8 项检查全 PASS，verdict closure-approve——(1) 迁移完整性（18 类 + ModelKeys 就位、agent 侧 0 残留、core 0 agent import、`NopAiAgentErrors` 30 码 + 7 ARG 保留）；(2) ModelKeys（原方法删除、11 调用点改引用）；(3) 行为零变更（三代表类仅 package/import/异常类 token）；(4) Anti-Hollow（engine 4 文件运行时引用 + EndToEnd 全绿）；(5) nop-task 反射契约（TaskExceptionRegistry:69 注册串有效）；(6) 测试迁移（73 用例 1:1 + 2 新）；(7) 文档同步（4 文档 + roadmap）；(8) 构建证据（3387/292/84 全绿 + 工具 exit 0）。
- **关闭动作**：Closure Gates 14 项全勾选；`Plan Status` → `completed`；roadmap W2 `planned` → `done`（W2 交付：18 类下沉 nop-ai-core + `NopAiCoreException`/core 错误码 + `ModelKeys.buildModelKey` + 调用方适配 + 测试迁移 + 文档同步）。
- **验证（关闭时复跑）**：`./mvnw compile -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am` 通过；`./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS；`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（agent 3387 + core 292 + gateway 84，0 failures/0 errors）；checkstyle 无有效门禁（nop-api-core 遗留 9164 条基线，touched 模块 0 违规，`echo 'lint not configured'` 兜底）；`check-doc-links.mjs --strict` / `check-plan-checklist.mjs --strict` / `scan-hollow-implementations.mjs --module nop-ai-core --severity high` 全 exit 0。
- **doc-sync 裁定**：本 plan 交付物 = 代码迁移 + 4 文档同步（Phase 4 已更新）；本日志已更新。


### nop-ai-gateway-failover W2 Phase 4：文档同步（SINK-04）

- **Plan**：`ai-dev/plans/2026-08-15-0604-2-w2-reliability-sink-down.md`
- **做了什么**：
  - `nop-ai-agent-reliability.md`：新增 §0.1 模块归属总览表（18 类 + `NopAiCoreException`/core 错误码 + `ModelKeys.buildModelKey` → nop-ai-core；Checkpoint*/GoalTracker/Sustainer/Wait*/CompactionAwareTruncation → nop-ai-agent；`LlmCallCoordinator` 不迁移）；§5.1 ThresholdBreaker 包名/异常引用修正（`io.nop.ai.core.reliability` + `NopAiCoreException`，W2 前为 `NopAiAgentException`）；fail-fast 哲学条目同步。
  - `02-account-failover-requirement.md` §四.1：**SINK-02 偏差说明**落档——core 等价物 = `NopAiCoreException` + 并入 2 项错误码；`NopAiAgentErrors` 其余 30 码 + `NopAiAgentException` 保留（agent 专用 + nop-task 反射契约），与"能力级不依赖 nop-ai-agent"硬约束不冲突。
  - `nop-ai-llm-error-normalization-design.md`：范围声明行 + §3.8 Layer 归属节更新（`LlmErrorClassifier`/`IRetryPolicy`/账号链 → nop-ai-core，`LlmCallCoordinator` 留 agent）。
  - `docs-for-ai/02-core-guides/error-handling.md`：英文转换码例外分界复核——结论 = 例外归属以持有类模块为粒度，新增 W2 下沉修订注（`ERR_AI_AGENT_INVALID_ARG` 持有类入 core、ID `nop.err.ai.agent.invalid-arg` 刻意保留，ID 命名空间跨两模块为有意为之）。
  - roadmap W2：`todo` → `planned`（`done` 待独立 closure audit）。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（No errors found）；grep 抽查无"reliability 子集属 nop-ai-agent"残留；`check-plan-checklist.mjs --strict` exit 0（1 非完成计划未勾选项 = 本 plan 自身，warnings only）；`scan-hollow-implementations.mjs --module nop-ai-core --severity high` exit 0。
- **doc-sync 裁定**：上述文档即本 Phase 交付物；本日志已更新。


### nop-ai-gateway-failover W2 Phase 3：测试迁移 + 双模块全量回归

- **Plan**：`ai-dev/plans/2026-08-15-0604-2-w2-reliability-sink-down.md`（SINK-03）
- **做了什么**：
  - 6 个纯单元测试随迁 nop-ai-core（`TestThresholdBreaker`/`TestStandardRetryPolicy`/`TestNoRetryPolicy`/`TestAlwaysClosed`/`TestLlmErrorClassifier`/`TestProviderFailoverQueue`，共 73 用例 @Test 计数迁移前后 1:1），断言异常类型 `NopAiAgentException`→`NopAiCoreException`、`ERR_AI_AGENT_INVALID_ARG`→core 码（语义断言零改动）；随迁测试零 `io.nop.ai.agent` import。
  - 留守测试更新：reliability 包内 6 个装配/E2E（TestCircuitAwareRouting/TestCircuitBreakerWiring/TestLlmCallCoordinatorErrorResponse/TestRetryPolicyWiring/TestStandardRetryPolicyEndToEnd/TestThresholdBreakerEndToEnd）补 core.reliability import；包外 5 个（TestAccountFallbackChain/TestEngineExtractedCoordinators/TestExecutionMiddlewareLlmRetry/TestProviderFailoverChain/TestSmartModelRouterFallback）import 重写 + 3 处 FQCN 引用修复；`TestEngineExtractedCoordinators` buildModelKey 断言改 `ModelKeys.buildModelKey`。
  - 新公共类型测试（Minimum Rules #25）：`TestModelKeys` 新建（2 用例：null 归一化逐字随迁 + provider/model 独立 null 处理）。
  - 留守类单测（TestSessionGoalTracker/TestSisypheanSustainer/checkpoint 系列）引用 NopAiAgentException 的无需改（异常类保留）——零改动确认。
- **验证**：`./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS（nop-ai-agent 3387 + nop-ai-core 292 + nop-ai-gateway 84，0 failures / 0 errors）；测试计数核对：迁移前 agent 3460（=3387+73 随迁）+ core 219 = 3679 = 迁移后 3387+292（=219+73+2 新增 TestModelKeys）——无测试丢失，差异 = +2 新用例；EndToEnd 全绿（TestThresholdBreakerEndToEnd 3/3、TestStandardRetryPolicyEndToEnd 4/4 等）证明下沉类被 agent 引擎运行时消费；core 内 3 个 skipped 为既有 TestChatServiceImpl 禁用用例（与迁移无关）。
- **doc-sync 裁定**：`No owner-doc update required`（文档同步在 Phase 4）；本日志已更新。


### nop-ai-gateway-failover W2 Phase 1-2 落地：裁定落档 + 18 类可靠性子集下沉 nop-ai-core

- **Plan**：`ai-dev/plans/2026-08-15-0604-2-w2-reliability-sink-down.md`（roadmap W2，SINK-01/02 代码迁移）
- **Phase 1（裁定，全部 live 实证复核）**：
  - 18 类盘点确认（import 面干净，5 类引用 agent 异常共 16 处 throw 站点）；8 个留驻 main 类反向引用仅 javadoc（`{@link}` 7 处 + `{@code}` 6 处），`IModelRouter`/`SmartModelRouter` 仅文本引用不补 import。
  - 包名 `io.nop.ai.core.reliability`；异常策略：新增 `NopAiCoreException`（构造器四件套对齐），`ERR_AI_AGENT_INVALID_ARG`+`ARG_MSG` 并入 `NopAiCoreErrors`（**错误码 ID `nop.err.ai.agent.invalid-arg` 保留**，仅换持有类），agent 其余 30 码 + 7 ARG 常量与 `NopAiAgentException` 保留（保住 nop-task-dao:69 反射注册串契约）。
  - `buildModelKey` → `ModelKeys.buildModelKey`，**`LlmCallCoordinator.buildModelKey` 原方法删除**（10 调用点 + ReActAgentExecutor:613 + 测试断言全部改引用）。
  - nop-task 反射契约核实：注册串持续有效，零改动。
  - 测试迁移裁定：6 纯单元测试随迁 / 6 包内装配 E2E 留守补 import / 5 包外测试留守补 import。
- **Phase 2（代码迁移）**：18 类 `git mv` + package/import 机械变换（脚本化，re-sort import 块）；`NopAiCoreException`/`ModelKeys` 新建；`NopAiCoreErrors` +2（ARG_MSG + ERR_AI_AGENT_INVALID_ARG）；`NopAiAgentErrors` -2（30 码 + 7 ARG 保留）；22 个包外 agent 文件 + 4 个留驻类常量引用换 core 等价码；8 个 engine 文件 import 更新；留驻 8 类补 javadoc import。
- **验证**：`./mvnw clean compile -pl :nop-ai-core,:nop-ai-agent -am` BUILD SUCCESS（首次增量编译因陈旧 target/classes 残留旧 FQCN 触发 AOP codegen NoClassDefFound，clean 后通过——包移动必须 clean 构建）；18 类语义归一化 diff **零差异**（仅 package/import/异常类行）；grep 实证：agent main 对已迁类引用 0 残留、`NopAiAgentErrors` 2 项 0 残留、nop-ai-core 对 `io.nop.ai.agent` import 0 残留；`ModelKeys.buildModelKey` 被 11 个调用点真实调用。
- **doc-sync 裁定**：`No owner-doc update required`（design 文档同步在 Phase 4）；本日志已更新。


### nop-ai-gateway-failover mission VERIFY 运行：构建验证 + AR-9 测试 bean id 修复

- **Plan**：`ai-dev/plans/2026-08-15-0604-1-w1-streaming-resubscribe-spike.md`（W1 纯文档计划，零 src 改动）+ W2/W3 plan 起草（未执行）
- **做了什么**：mission-driver VERIFY 步骤，验证 nop-ai-core/nop-ai-gateway/nop-ai-agent 三模块构建。
- **验证**：
  - `./mvnw compile -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am` 通过。
  - `./mvnw clean install -DskipTests -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` BUILD SUCCESS。
  - `./mvnw test -pl :nop-ai-core,:nop-ai-gateway,:nop-ai-agent -am -T 1C` 首轮 nop-ai-gateway 失败（TestChannelMessageServiceIoC 2 errors），修复后全绿（84/84：nop-ai-gateway 0 failures / 0 errors）。
  - checkstyle：仓库无有效 lint 门禁（nop-api-core 遗留 9164 条基线违规，mission 配置以 `echo 'lint not configured'` 兜底）——非本 mission 引入。
- **修复（VERIFY 发现，非 plan 交付物）**：`TestChannelMessageServiceIoC` AR-9 两用例（`dispatchTimeoutMsDefaultsTo30000` / `dispatchTimeoutMsFollowsAssignedConfigValue`）以陈旧 bean id `channelMessageService` 调 `getBean`，而测试夹具与生产 `ai-gateway-defaults.beans.xml` 均注册 `nopChannelMessageService` → `nop.err.api.unknown-bean-for-name`。此为主线 HEAD 既有失败（nop-prefix 改名 4f3afbd6f 后遗留；同修复已在未合并分支 feat-credential-mfa-phase2 df2b0fb55 存在）。两处 `getBean` 对齐 `nopChannelMessageService`，语义断言零改动。
- **doc-sync 裁定**：No owner-doc update required（修复仅测试 bean id 对齐，无行为/契约变化）；本日志已更新。


### W1 前置 spike：流式重订阅可行性验证 + 决策暴露（roadmap W1 done）

- **Plan**：`ai-dev/plans/2026-08-15-0604-1-w1-streaming-resubscribe-spike.md`（nop-ai-gateway-failover W1，纯调研/文档计划，零 src 改动）
- **做了什么**：完成三个 Phase 的全部代码追踪与结论落档，交付物 = `ai-dev/analysis/2026-08/2026-08-15-w1-streaming-resubscribe-spike.md`。
  - **SPIKE-01 网关形态 Q7**（只读追踪 nop-gateway 五类文件，行号全部 live 核实）：`RouteExecutor.java:76-77` 流式路由直返 `executeStreaming` 不经 `proceedInvoke`（拦截器 `invoke()` 流式路径不可达）；`StreamingProcessor.executeStreaming` 时序（onStreamStart `:71` 早于 attribute 写入 `:95`、fetch 惰性 `ServerEventPublisher` 首次 request 才 sendAsync、`StreamingResponse.publisher` final `:24`）；`GatewayHttpFilter` 响应写阶段读 attribute `:158-159`、订阅注入 `:188`、`abort()` 注入前 no-op 且静默取消（`AbstractServerEventSubscription.cleanup` 无下游信号）；`proceedOnStreamError`/`onStreamError` 全库无生产调用点（grep 核实，仅接口/测试）。**结论：需最小 nop-gateway 通用改动（可行）**——机制 C（拦截器侧替换）不可行（attribute 写后无钩子）、机制 B（onStream* 钩子）四象限 = 能取消/不能感知取消/不能重订阅/能降级终止、推荐机制 A（链内缓冲 + 通用重执行扩展点，零 nop-ai 依赖可保持）；对 W7 输入：并发计数挂钩 = 网关形态 fetch 发起（响应写阶段订阅时）。
  - **SPIKE-02 本地形态 Q7**：`ChatServiceImpl.callStream`（`:176-231`）"单次使用 + 上游急切启动"确认（fetch 在 callStream() 调用栈内同步发起：`:202-205` subscribe→request(MAX)→startRequest）；背压/取消语义用 `_tmp/SubmissionPublisherScratchV1-V6.java`（JDK-only）实证：submit() 无订阅者立即返回丢弃、demand 耗尽时阻塞在 submit()（缓冲 256）、订阅者 cancel 唤醒阻塞 submit()（S6）、close()/closeExceptionally() 在 demand=0 时自身阻塞（S9/S10）、cancelToken 不唤醒阻塞的 SSE 线程 → **取消顺序契约 = 先 cancel 订阅 → 再 cancelToken，窗口期保持 demand**；对 W6 输入：重订阅 = 重调 callStream（新 ChatOptions）、并发计数挂钩 callStream 调用时刻、重订阅重复 checkRateLimit（:182，本地限流可能拦下重试）与 logRequest（:191，日志噪音）。
  - **SPIKE-03 决策暴露**：Q2（前端 model 语义：路由覆盖 vs 保留，推荐覆盖）、Q5（RATE_LIMITED/TRANSIENT → 账号链切换偏离确认，推荐确认）整理为带影响分析/测试面/推荐默认的决策点，写入 spike 文档 + 本日志。**待人工裁决（W6/W7 实现前）**：Q2 model 语义（默认：路由覆盖）、Q5 偏离确认（默认：确认）。
  - **SPIKE-04 结论回馈**：需求文档假设未被推翻（§3.2 字节流层语义成立、§一.4/§3.1 方向一致、§六 裁决不受影响）→ `02-account-failover-requirement.md` **无需修订**（"假设未变，无需修订"落档）；Q7 结论推翻 roadmap W7 "nop-gateway（只读复用）" 表述 → roadmap 同步（W1 `todo`→`done`；W7 module/area → "最小通用改动，零 nop-ai 依赖保持"）；Q2/Q5/Q7 回填 Q 表归 W8 OBS-04。
- **偏差**：无（与计划一致；scratch 实证类落 `_tmp/` 未入 src，实证结论已复述于 spike 文档）。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0；`node ai-dev/tools/check-plan-checklist.mjs 2026-08-15-0604-1-w1-streaming-resubscribe-spike.md --strict` exit 0；构建验证按计划 Closure Gates"纯文档计划"条款不执行（零 src/构建物改动）。
- **独立 closure audit**：fresh general subagent（`ses_ffd8b432fffeqleCLqOuoGq43T`）回查 live repo——SPIKE-01/02 代码位置抽查全 PASS（零 src 误写、proceedOnStreamError 无生产调用点、急切启动链核实），verdict closure-approve；evidence 已写入 plan Closure 段。
- **doc-sync 裁定**：`ai-dev/analysis/2026-08/2026-08-15-w1-streaming-resubscribe-spike.md` 新建（本 plan 交付物）；roadmap W1/W7 已同步；本日志已更新。


### DRAFT_PLANS Cycle 3 — 2026-08-15 multi-audit（P0×1 + P1×9）拟制 3 份 remediation plan 并达成 review consensus

mission-driver DRAFT_PLANS 步骤（源审计 `ai-dev/audits/2026-08-15-0559-multi-audit-nop-metadata-invariant-loop.md`，唯一 open 审计）。

- **路由**：P0（F2 SSRF 双绕过）+ P1-8 → plan `2026-08-15-1913-1`（执行顺序 1）；P1-1/3/4/5/2 → plan `2026-08-15-1913-2`（顺序 2）；P1-6/7/9 → plan `2026-08-15-1913-3`（顺序 3）。全部 P0/P1 落地为 Fix 项，零降级；35 项 P2 全部登记 roadmap Follow-up Backlog（新分组，逐项标注 P2-xx + 源审计路径）。审计状态 open → **planned**。
- **baseline 核对**：draft 前对 9 项发现的代码证据逐文件 live 复核（xmeta 保留层、BizModel、QueryAction、日志站点、测试、owner doc 清单、dao biz 接口包）。
- **迭代对抗性审查（两轮，均 fresh session 独立子代理）**：
  - R1（3 个并行子代理）：plan-1 Blocker×1（over-block 方案自相矛盾——`DANGEROUS_URL_TOKENS` 整 URL contains 会误杀合法 authority `host=` 形态）+ Major×3（query 解析语义未定、profiler :515/:534 推定与调用图相反、checkstyle gate 不可达）；plan-2 Major×2（指标级路径无 resolved 计算、表单字段落点/edit-form initApi 陷阱）+ 正面核实（sensitive tag 不回写、update 流程 key 语义）；plan-3 Major×3（joinId 可得性前提为假、单轨补齐对半数点位不可施、扫描器口径欠裁定）。
  - 修订后 R2（3 个新子代理）：R1 全项 Resolved；新增条件项——plan-1：rg gate 与清单不匹配（:300 越界命中 → 并入 11 处口径）、`?useSSL=false` 假向量（token 已在 blocklist）→ 已换 `?connectTimeout`；plan-2：validator 丢弃点行号 :175-177、删除虚构 tagSet "n" 表述、delta 计数 5；plan-3：变量形态错误码盲区（4 处 throw 点错误码为参数——`MetaTableFieldResolver:214/223/234` 等 3 处疑似真实缺参 → UNRESOLVED 清单 + 入分母）、轨 3 预裁定换码（`MetaJoinExecutor:209` 第二 throw 点 joinId 传齐，削占位符会重引入缺陷）、点位分类勘误。
  - R2 各方预定义 consensus 条件项全部落地并 live 复核（rg 11 处实测、validator 行号、:209 joinId、变量形态 throw 参数计数）→ **consensus 达成**，3 份 plan draft → **active**。
- **roadmap**：Work Item Status 新增 Cycle 3 三行（planned → active 已同步）；Follow-up Backlog 新增"2026-08-15 multi-audit P2 遗留（35 项）"六分组。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` = 16 errors，与本日早前记录的既有基线完全一致（本次修改文件 0 命中；曾引入 1 处新增断链 `_templates/README.md` 与 plan-3 内 3 处，均已修复回落基线）。


### nop-plugin-enhancement W7 closure：SHA256 + 测试矩阵 + docs-for-ai 同步（roadmap W7 + Milestone done）

- **Plan**：`ai-dev/plans/2026-08-15-0013-1-sha256-tests-docs-sync.md`（nop-plugin-enhancement W7，roadmap 最后一个 stage）
- **做了什么**：W7 三 Phase 全部落地并 closure，nop-plugin 增强落地 roadmap（W1-W7）全部完成。
  - **Phase 1**（SHA256）：resolver 下载校验补齐（hash 来源 header → `.sha256` 请求 → expected-hash map；校验后 move + `.sha256` 落盘；缓存重验/遗留重下载/skip 逃生门/无 hash 显式失败）；错误码 `ERR_PLUGIN_SHA256_MISMATCH` + `ERR_PLUGIN_CHECKSUM_NOT_AVAILABLE`；`TestHttpPluginResourceResolver` 12 用例 + 2 条 jar 轨端到端。
  - **Phase 2**（测试矩阵）：审计清单 `ai-dev/audits/2026-08-15-nop-plugin-w7-test-matrix-audit.md`；唯一缺口（W6 移交项父链总未命中）补 `TestParentHierarchy.testParentChainTotalMissThrowsExplicitError`（`ERR_IOC_UNKNOWN_BEAN_FOR_TYPE` 钉死）；W6 移交项裁定 watch-only residual（观察实证 + 无契约污染证据）；W6 复核项确认（checkChangedAndReload/P2-D/reload E2E 已落地）。
  - **Phase 3**（docs）：`docs-for-ai/03-modules/nop-plugin.md` 新建（两态生命周期/多实例/coeffect/getService/activator/命令路由/SHA256/变更检测接线/兼容路径/resolver 注入）+ INDEX/module-groups/source-anchors（PLG-001~009）登记；check-doc-links --strict exit 0。
  - **设计同步**：`05-artifact-loading-design.md` §一.3/§四/§五.1/§五.2/§六/§九 修订注解落地。
  - **roadmap 同步**：W7 `planned` → `done`；★ Milestone（W1-W7 全部 done）→ `done`。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（96/96：api 6 + manager 80 + support 10）；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-plugin-manager --severity high` exit 0；`check-doc-links.mjs --strict` exit 0；api 零依赖 grep 0 命中；checkstyle 无 plugin 文件违规（仓库既有 nop-api-core 9164 条遗留基线，与 W6 记录一致）。
- **独立 closure audit**：fresh general subagent（`ses_ffea652a4ffeNjcS6o209G189F`）逐项回查 live repo——A-E/H 全 PASS（端到端接线、hollow 扫描、deferred 分类诚实），F 文本一致性收尾修正后复跑通过，verdict closure-approve；evidence 已写入 plan Closure 段。
- **doc-sync 裁定**：docs-for-ai 已更新（本 plan 交付物）；本日志已更新。


### nop-plugin-enhancement W7 Phase 3 落地：docs-for-ai 使用文档 + 登记

- **Plan**：`ai-dev/plans/2026-08-15-0013-1-sha256-tests-docs-sync.md`（nop-plugin-enhancement W7）
- **做了什么**：
  - 新建 `docs-for-ai/03-modules/nop-plugin.md`：两态生命周期（LOADED → ACTIVATED/DEACTIVATED）、多实例（instanceKey / parent 层级 / 实例配置域）、coeffect（requires/if-property）、getService 生命周期代理（primary / INACTIVE 快速失败 / 全链未命中 → `ERR_IOC_UNKNOWN_BEAN_FOR_TYPE`）、activator `activate(scope, config)` 双参数、per-instance 命令路由、artifact 加载（service-url 模板 + SHA256 hash 来源优先级 + skip-cache-verify + 无 hash fail-fast 部署要求）、变更检测接线（`checkChangedAndReload()` 宿主定时调用示例）、兼容路径、resolver 宿主注入（beans.xml 示例，`nopHttpClient` 为平台真实 bean）。
  - 登记：`INDEX.md` 快速路由表新增 plugin 条目；`01-repo-map/module-groups.md` 核心框架行补 plugin 子模块说明；`04-reference/source-anchors.md` 新增 `PLG-001`~`PLG-009` 锚点段。
  - 边界修正：docs-for-ai 不允许引用 ai-dev 路径（check-doc-links 边界规则），相关文档段改写为纯文本指引。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0；逐条抽查文档声明与 live 代码锚点/测试证据一致（两态状态机/门控/代理/quiescence/HMR 入口/SHA256 全部有对应测试类与源码锚点）。
- **doc-sync 裁定**：docs-for-ai 已更新（本 Phase 交付物）；本日志已更新。


### nop-plugin-enhancement W7 Phase 2 落地：测试矩阵审计 + W6 移交项裁定

- **Plan**：`ai-dev/plans/2026-08-15-0013-1-sha256-tests-docs-sync.md`（nop-plugin-enhancement W7）
- **做了什么**：
  - **测试矩阵审计**（`ai-dev/audits/2026-08-15-nop-plugin-w7-test-matrix-audit.md`）：逐项核对五类行为测试（quiescence effects 清空、多实例隔离 agent-1/agent-2、coeffect 依赖链激活/去激活、HMR reload 端到端、兼容路径回归）——全部已有且断言真实行为，**不重写任何既有用例**；发现唯一真实缺口 = W6 移交项"父链 IoC 错误码透传"的**总未命中断言**（成功回退/守卫/环已覆盖，无"父链沿途全未命中 → 显式抛错"）。
  - **缺口补测试**：`TestParentHierarchy` 新增 `testParentChainTotalMissThrowsExplicitError`——子容器 + 父链沿途 + 顶层（parentContainer=null 不扩展宿主）均无候选 → `getService(Callable.class)` 显式抛 `ApiErrors.ERR_IOC_UNKNOWN_BEAN_FOR_TYPE`（错误码 + beanType 参数断言）。观察实证：实际错误码即该码（BeanContainerImpl.getBeanByType 未命中即抛），断言按观察结果钉死（beanType 参数格式为容器标准 `interface ...`）。
  - **裁定**：watch-only residual（错误显式抛出、参数完整、无契约污染证据，不做错误码翻译）——理由记录于 plan Closure。
  - **W6 复核项**：W6 completed 确认（plan 状态 + commit 058f09aee）；`checkChangedAndReload()` 显式入口（PluginManagerImpl.java:320）、P2-D 语义、reload 端到端均已落地且有测试（TestReloadPlugin 5 + TestChangeDetection 3），与本 plan 测试项交叉核对无重复/无缺口。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（manager 80/80：+1 新用例全绿）。
- **doc-sync 裁定**：No owner-doc update required（Phase 2 仅测试变更）；本日志已更新。


### nop-plugin-enhancement W7 Phase 1 落地：HttpPluginResourceResolver SHA256 校验补齐

- **Plan**：`ai-dev/plans/2026-08-15-0013-1-sha256-tests-docs-sync.md`（nop-plugin-enhancement W7）
- **做了什么**：补齐 resolver 下载完整性 SHA256 安全底线（Javadoc 声称而代码缺失的 confirmed live gap）。
  - **错误码**：新增 `ERR_PLUGIN_SHA256_MISMATCH`（pluginId/expectedHash/actualHash）与 `ERR_PLUGIN_CHECKSUM_NOT_AVAILABLE`（pluginId/url），及 ARG_EXPECTED_HASH/ARG_ACTUAL_HASH/ARG_URL 参数常量。
  - **resolver 实现**（`HttpPluginResourceResolver.java`）：hash 来源优先级 header（`X-Checksum-Sha256`，大小写双查）→ `{url}.sha256` 请求（trim + 首 token 解析）→ 配置 expected-hash map（key = `groupId:artifactId:version`，setter 注入，覆盖设计 §五.2 载体裁定）；下载后 move 前在临时文件上重算 SHA256（resolver 私有静态 helper，未新增 nop-commons 公共 API）比对（大小写不敏感）→ 通过才 move + 写 `{jar}.sha256`（写失败回滚已 move jar）；`assureParent` 建 Maven 风格子目录；move 前删旧 jar（moveFile 无 REPLACE_EXISTING）；缓存命中重验（篡改 fail-fast `ERR_PLUGIN_SHA256_MISMATCH`）、遗留缓存（无 `.sha256`）重下载、`nop.plugin.skip-cache-verify=true` 同时跳过重验与遗留重下载、无 hash 来源显式失败。
  - **测试**：`TestHttpPluginResourceResolver` 新建 12 用例（手写 IHttpClient stub 覆写 downloadAsync/fetchAsync + DefaultHttpResponse），覆盖成功落盘、校验失败清理、三种 hash 来源、缓存重验通过/篡改失败、skip 配置、无来源显式失败、遗留缓存重下载、`.sha256` 写失败回滚；含 2 条 jar 轨端到端（manager.loadPlugin 完整链路成功 + 校验失败显式失败，`TestPluginManager.buildPluginJar` 基建复用）。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（manager 79/79：新增 12 全绿，既有 67 无回归）。
- **doc-sync 裁定**：`ai-dev/design/nop-plugin/05-artifact-loading-design.md` 已更新（§一.3 状态收口、§四 缓存篡改修订注解、§五.1 工具名更正为 HashHelper、§五.2 载体修订注解、§六 伪代码与修订注解、§九 锚点说明）；本日志已更新。


### nop-plugin-enhancement W7 plan 起草 + 3 轮独立审查共识（→ active）

- **Plan**：`ai-dev/plans/2026-08-15-0013-1-sha256-tests-docs-sync.md`（nop-plugin-enhancement W7，roadmap Stage 7 最后一项）
- **做了什么**：起草 W7 计划（SHA256 校验补齐 + 测试矩阵补全 + docs-for-ai 同步），3 轮独立 sub-agent 对抗性审查达成共识后置为 active。
  - **R1（3 Blocker + 8 Major 全修）**：B1 配置预期 hash 无载体 → 裁定 resolver 级 expected-hash map（`groupId:artifactId:version` key，setter 注入，优先级 header → `.sha256` → map）；B2 缓存篡改语义与设计 05 §四"不匹配→重下载"冲突 → 裁定 fail-fast `ERR_PLUGIN_SHA256_MISMATCH` + 05 §四 修订注解（closure gate 兜底）；B3 EC/Decision 遗留缓存矛盾 → EC 措辞收敛。
  - **R2（Ready，2 one-line pin + 4 minor 并入）**：遗留缓存重下载 move 前须删旧 jar（`FileHelper.moveFile` 无 REPLACE_EXISTING）；EC "绝不静默使用未校验 jar" 补 skip 逃生门限定；05 §五.2 载体修订注解 + §八.5 引用更正；`.sha256` 响应体解析（trim + 首 token）；skip 配置 `@InjectValue` setter 模式。
  - **R3（Ready）**：全部 NB 钉折叠确认，5 个措辞级 advisory 随手修（§五.4→§五.5 引用、case-insensitive 比对、stub 覆写 `downloadAsync`/`fetchAsync`、05 §五.1 "Sha256 工具"→`HashHelper` 更正、EC owner-doc 措辞）。
- **关键裁定（记录于 plan）**：无 hash 来源 → 显式失败（Javadoc 契约兑现，不允许无校验下载）；缓存篡改 → fail-fast（不静默重下载）；遗留缓存（无 `.sha256`）→ 重下载；`nop.plugin.skip-cache-verify=true` 同时跳过重验与遗留重下载（启动优化逃生门）；W6 移交"父链 IoC 错误码透传"默认裁定 watch-only + 测试钉死（先观察实际错误码再断言）；文件 SHA256 摘要 helper 放 resolver 私有静态方法（不新增 nop-commons 公共 API）。
- **roadmap 同步**：`nop-plugin-enhancement-roadmap.md` W7 `todo` → `planned`（独立 draft review 通过）。
- **验证**：`node ai-dev/tools/check-plan-checklist.mjs 2026-08-15-0013-1-sha256-tests-docs-sync.md --strict` 结构检查通过（exit 0，active 计划未勾选项为 warnings）；本日志已更新。


### 2026-08-15（批量查询性能 plan 收口）

- Plan `2026-08-15-0004-3` 收口：独立子 agent closure audit（task ses_ffcae889affeFQHPrUPbiE3I1d，
  fresh session）判定 **CLOSABLE**——A（Phase 1–4 Exit Criteria 逐条 file:line/测试名证据）、
  B（并行等价/latch 并发证明/并行度上界/非 NopException 看板级传播/上限先于查询/接线）、C（缓存命中
  跳 SQL/键区分/TTL/staleness 契约/关闭回归/失败与无数据集不缓存/既有调用方回归/接线）、D（E2E 全链路
  + design doc 一致）、E（Anti-Hollow：调用链追踪 + scan-hollow 0 findings）、F（deferred 诚实性）、
  G（compile/test/checklist/hollow/doclinks 退出码 0，审计者独立复跑 500/0/0）、H（import 分组）全 PASS。
- 收口三件套：`check-plan-checklist.mjs --strict` 退出码 0（51/51 勾选 + Closure Evidence 已写入）；
  `scan-hollow-implementations.mjs --module nop-datav --severity high` 退出码 0；Plan Status → completed。
  Roadmap `Last updated` 前置本条目 + D1-2 行标注 deferred follow-up 已落地（修正一处我引入的失效
  链接后 doclinks 复归 0）。
- Doc-sync: 本条目 + roadmap；plan 文件自含 evidence。


### 2026-08-15（批量查询性能 Phase 4 — 端到端验证与文档收口）

- Plan `2026-08-15-0004-3` Phase 4 完成：新增 `TestNopDatavDashboardPerfE2E`（单测试全生命周期，
  rule #22 端到端 + Anti-Hollow 证据）：多面板看板（全局筛选 region + 无数据集 text + 同数据集
  chart/table + 失败 broken）→「顺序 + 无缓存」基线 →「并行 + 缓存」首次调用结果与基线逐条目一致
  （含失败条目/顺序/无数据集条目）→ 立即重复同参调用缓存命中（hitCount 增长 + miss 数不增长 +
  结果一致）→ 修改筛选参数（north→south）重新执行 SQL（miss 增长 + 1 行正确结果 + 失败面板仍隔离）。
  修复一处自身过严断言（首调并行 chart/table 同键请求内去重命中是 §8.4 合法形态，不作 hitCount==0
  断言），修复后连跑 3 次稳定。
- 文档一致性复核：runtime-design.md §八（8.1–8.6）与实现逐条比对无漂移；`check-doc-links.mjs
  --strict` 退出码 0。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am` 500/0/0 全绿（485 基线 + 6 并行 +
  8 缓存 + 1 E2E）；nop-auth `TestChannelScanBindLoginE2E` 曾在大 reactor 中偶发
  missing-tenant-id（单独运行 4/4 绿，与本改动无关——nop-auth 不依赖 nop-datav，本 plan diff 仅
  nop-datav + ai-dev 文档）。
- Doc-sync: 无新增（Phase 1/2/3 已同步）。


### 2026-08-15（批量查询性能 Phase 3 — 查询结果缓存落地）

- Plan `2026-08-15-0004-3` Phase 3 完成：
  - 新增 `DashboardPanelQueryCache`（io.nop.datav.service.query）：平台 `LocalCache`+`CacheConfig`
    （进程内单节点，先例 NopDatavShareAccessGuard），TTL-only 失效、容量驱逐、行数准入上界（超限
    不缓存不截断）、缓存读/写故障 WARN 显式降级直查；`buildKey` = refDatasetId + 求值后参数 JSON +
    rowLimit（求值 Map 顺序确定 → 无碰撞）；`getHitCount/getMissCount` 暴露 Caffeine stats 作测试
    可观测计数器。
  - `PanelDataBinder` 最小重构：新增 5 参重载（尾参缓存，null=不缓存），键在参数求值后构造（不复制
    求值逻辑）；既有 2/4 参重载委托传 null → getPanelData/refreshPanel/exportDashboard/AlertEvaluator
    行为逐字节等价（回归测试显式证明：cache.enabled=true 下 getPanelData 仍每次直查）。
  - `NopDatavConfigs` +4 配置（cache.enabled 默认 false / ttl-seconds 30 / max-entries 200 /
    max-rows-per-entry 1000）；`NopDatavDashboardBizModel` 惰性构建缓存实例 + `resolveQueryResultCache`
    按请求判定接入 + 测试 seam（`getQueryResultCacheForTest`/`resetQueryResultCacheForTest`）。
  - 新增 `TestNopDatavDashboardQueryCache`（8 tests）：命中跳 SQL（hit/miss 计数 + staleness 语义
    断言）、参数变化 miss、TTL 过期重查（200ms TTL 直构实例 + 500ms 裕量）、TTL 内数据集变更 staleness
    契约（读旧值；关缓存读新值）、缓存关闭零 staleness 回归、失败条目不固化（修复 ref 立即生效）+
    无数据集条目不经缓存、getPanelData 路径不缓存（Non-Goal 边界）、buildKey 无碰撞断言、行数准入
    上界（超限不入缓存、缓存值永不截断）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am` 499/0/0 全绿（491 + 8 新测试）。
- Doc-sync: 实现与 §8.3/§8.4/§8.5 裁定一致，无漂移（无需回写）。


### 2026-08-15（批量查询性能 Phase 2 — 并行执行落地）

- Plan `2026-08-15-0004-3` Phase 2 完成：
  - `NopDatavConfigs` +2 配置（`nop.datav.dashboard-query.parallel.enabled` 默认 true /
    `parallelism` 默认 4）。
  - `NopDatavDashboardBizModel.getDashboardData` 面板迭代段升级为有界并行：任务提交共享
    `GlobalExecutors.globalWorker()`（`CompletableFuture.supplyAsync`），请求内 Semaphore(parallelism)
    许可（排队任务不占许可），结果按下标归位（条目顺序恒等价顺序版），非 NopException join 后按面板
    顺序重抛第一个（D3 看板级失败语义在并行下保持，确定性）；开关关或面板数 ≤1 走原顺序路径。
  - 测试可观测 seam（P1 裁定落位）：`setPanelQueryTaskDecorator`（面板任务装饰器，生产恒 null）+
    `getPanelQueryExecutor`/`setPanelQueryExecutor`（执行器 seam，默认 globalWorker identity 断言）。
  - **实现期裁定修订（已回写 §8.2）**：① worker 任务整体以 `ormTemplate.runInNewSession` 包裹
    （平台先例 NopDatavExportTaskBizModel/ReportDeliveryExecutor；逐 dao 调用自动开 session 在
    worker 线程会残留事务状态）；② 每任务**新建** context 并 `propagateContext` 拷贝调用方
    tenant/locale——禁止多 worker 共享调用方同一 context：平台 `TransactionRegistry` 挂在 context
    上，并发共享导致事务注册表交错损坏（`nop.err.dao.txn.not-registered`，实测复现并以此修复）。
  - 新增 `TestNopDatavDashboardParallelQuery`（6 tests）：并行 vs 顺序逐条目等价（含失败条目/无数据集
    条目/顺序）、latch 并发执行证明（串行必超时）、parallelism=2 上界（max in-flight ≤2 且 ≥2）、
    非 NopException 看板级传播、上限校验零任务提交、执行器跨请求 identity 复用。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am` 491/0/0 全绿（485 基线 + 6 新测试）；
  `TestNopDatavExportE2E` 单独运行 13/13（此前 full-suite 中偶发 NopFileRecord 抖动与本改动无关，
  本次全量亦绿）。
- Doc-sync: `runtime-design.md` §8.2 两处裁定修订（runInNewSession 包裹 + 每任务 context 拷贝）。


### 2026-08-15（批量查询性能 Phase 1 — 设计裁定 P1–P5）

- Plan `2026-08-15-0004-3` Phase 1 完成：`runtime-design.md` 新增 §八「并行执行与结果缓存」
  （P1 执行器=共享 `GlobalExecutors.globalWorker()` + 请求内 Semaphore 并行度；P2 并行安全边界
  ——binder 无状态/注册表只读/`OrmTemplateImpl.runInSession` worker 线程自动开 session
  （OrmTemplateImpl.java:204-211）/`IContext.executeWithContext` 传播 tenant（GenSqlHelper.java:95
  消费 currentTenantId）/JdbcHelper.java:250 消费 callExpireTime；P3 缓存=`io.nop.commons.cache`
  LocalCache、接入点固定 getDashboardData 批量路径、binder 增可选缓存参数重载而既有调用方逐字节等价；
  P4 键=refDatasetId+求值后参数+rowLimit、TTL-only staleness 契约、失败条目与无数据集条目不缓存、
  缓存故障 WARN 降级直查；P5 六配置项——parallel.enabled=true/parallelism=4/cache.enabled=false/
  ttl=30s/max-entries=200/max-rows-per-entry=1000 准入门槛不截断）。
- 既有陈述修订（非仅增补）：§4.4 执行模式行改写为 supersession 标注（顺序执行已被 §八取代，开关关闭
  回落顺序路径）；§五「批量查询：并行 vs 顺序」表加 Supersession 引言（拒绝并行结论不再有效，保留为
  决策历史索引）——无两套终局裁定并存。
- Roadmap 空洞前提纠正：Cross-cutting 缓存策略行「数据集查询缓存复用 nop-report」→「按 nop 标准缓存
  抽象接入 nop-datav 自有路径，nop-report 无数据集查询缓存抽象（已核实）」。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: `ai-dev/design/nop-datav/runtime-design.md`（§八 + §4.4 + §五）、
  `ai-dev/backlog/nop-datav-roadmap.md`（缓存策略行）。


### 2026-08-15（分享访问安全加固 plan 收口）

- Plan `2026-08-15-0004-2` 收口：独立子 agent closure audit（task ses_ffd1b41ccffe6L2tvfc9qIh0Eu，
  fresh session）判定 **CLOSABLE**——A（4 Phase Exit Criteria 逐条 file:line/测试名证据）、B（Anti-Hollow：
  checkShareAccessRateLimit 为 getSharedDashboard 首语句 + recordShareVisit 在快照读取后 + guard bean
  注册且测试共享 singleton 经 fake 时钟改变真实行为证明运行时接线；recordShareVisit catch→WARN 为
  design doc 显式裁定 fail-open）、C（design doc 与实现 R1–R6 逐条比对零漂移）、D（无 in-scope 项降级
  延期）、E（exit criteria 引用的测试名全部存在且断言相符）全 PASS。审计者独立复跑 3 次 485/0/0；
  收口最终复跑 compile + test 485/0/0 BUILD SUCCESS。
- 收口三件套：`check-plan-checklist.mjs --strict` 退出码 0（45/45 勾选 + Closure Evidence 已写入）；
  `scan-hollow-implementations.mjs --module nop-datav --severity high` 退出码 0（0 findings）；
  Plan Status → completed。Roadmap `ai-dev/backlog/nop-datav-roadmap.md` Last updated 前置本条目。
- Doc-sync: 本条目 + roadmap；plan 文件自含 evidence。


### 2026-08-15（分享访问安全加固 Phase 4 — 端到端验证与文档收口）

- Plan `2026-08-15-0004-2` Phase 4 完成：新增 `TestNopDatavShareHardeningE2E`（单测试全生命周期）：
  创建带密码分享 → 匿名正确密码访问成功（快照版本断言）且统计落库经 listShares 暴露（count=1 +
  lastVisitTime）→ 连续错误密码 3 次至阈值 → 被限流（正确密码也被拒，PASSWORD_LOCKED）→ fake 时钟
  推进窗口后恢复（访问成功 + count=2，失败/被拒尝试不计数）→ 撤销分享后访问被拒（SHARE_DISABLED）。
  全程真实 shareBiz 调用链 + 真实持久层 + 真实 guard bean。
- Doc 一致性复核：permission-sharing-design.md「访问限流与访问统计」与实现无漂移（补 1 处细节：
  R5 步骤 5 增「缺密码不计失败」与实现 verifySharePassword 对齐）；`check-doc-links.mjs --strict`
  退出码 0。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am` 485/0/0 全绿（484 + 1 新 E2E）。
- Doc-sync: permission-sharing-design.md 1 处细节对齐；本条目。


### 2026-08-15（分享访问安全加固 Phase 3 — 访问统计与暴露）

- Plan `2026-08-15-0004-2` Phase 3 完成：
  - ORM 源模型 `nop-datav/model/nop-datav.orm.xml` Share 实体 +2 聚合列（`visitCount` BIGINT
    defaultValue=0 / `lastVisitTime` TIMESTAMP），经 `clean install -pl nop-datav -am` 完整再生成链路
    （_gen entity、_app.orm.xml、xmeta queryable/sortable/published、i18n、view.xml、3 方言 deploy SQL
    均由源模型派生，未手改生成物）。
  - `NopDatavDashboardShareBizModel`：快照读取成功后 `recordShareVisit`（R4 定向 SQL
    `VISIT_COUNT=COALESCE(VISIT_COUNT,0)+1, LAST_VISIT_TIME=?`——数据库端原子自增，无 version bump、
    不触碰审计列；写失败 WARN fail-open 显式裁定）；createShare 显式 `visitCount=0`。
    `listShares` 暴露零改动（新列随实体/xmeta 自动可达，passwordHash 屏蔽不回归）。
  - 新测试 `TestNopDatavShareVisitStats`（3 用例，经真实调用链）：两次成功访问后 listShares 计数=2 +
    lastVisitTime 非空且 >= 访问前 + passwordHash 屏蔽不回归；密码失败 + 被限流访问不计数
    （count=0/lastVisitTime=null）；8 线程独立 ORM session 并发访问计数零丢失（原子自增断言）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am` 484/0/0 全绿（481 + 3 新）；
  `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` + `./mvnw test -pl nop-datav -am -T 1C`
  BUILD SUCCESS。
- Doc-sync: 无 owner doc 漂移（实现与 Phase 1 R4 裁定一致）；本条目。


### 2026-08-15（分享访问安全加固 Phase 2 — 访问速率限制）

- Plan `2026-08-15-0004-2` Phase 2 完成：
  - 新增 `NopDatavShareAccessGuard`（`io.nop.datav.service.share`，独立 bean `nopDatavShareAccessGuard`
    注册于 app-service.beans.xml）：R6 自定义固定窗口计数 + 密码锁定时间戳 per-key 状态机，
    LocalCache 容量上界（max-keys 配置，先例 TaskFlowManagerImpl），时钟 seam `setClock(LongSupplier)`
    （默认系统时钟，测试 fake 时钟推进窗口，零 Thread.sleep）。
  - `NopDatavDashboardShareBizModel.getSharedDashboard` 按 R5 次序接线：总速率检查（零 DB）→ 密码锁定
    检查（零 DB）→ token 查库 → enabled/expire → 密码校验（失败记账/成功清账，缺密码不计数）→ 存活
    防御 → 快照读取。R1 组合键 `shareToken|clientIp`（`getRequestClientIp` 读 `nop-client-addr`，
    无 IP 退化占位 "-"）。
  - `NopDatavConfigs` +5（enabled=true / max-password-failures=5 / max-access-per-window=60 /
    window-seconds=600 / max-keys=10000，关闭时逐字节等价）；`NopDatavErrors` +2
    （share-rate-limited / share-password-locked，均含 shareToken + retryAfterSeconds param）+
    ARG_RETRY_AFTER_SECONDS。
  - 新测试 `TestNopDatavShareRateLimit`（7 用例，全部经真实 shareBiz 调用链 = 接线验证）：爆破截断
    （阈值后正确密码也被拒 + 窗口推进恢复）/ 缺密码不消耗失败预算 / 总速率超限拒绝 + 窗口恢复 /
    不存在 token 探测错误码翻转 TOKEN_NOT_FOUND→RATE_LIMITED / 同 token 不同 IP 互不误伤（双向）/
    开关关闭等价（探测与爆破均无限流）/ 默认配置零误伤。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am` 481/0/0 全绿（474 既有 + 7 新）；
  `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` + `./mvnw test -pl nop-datav -am -T 1C`
  BUILD SUCCESS。
- Doc-sync: 无 owner doc 漂移（实现与 Phase 1 裁定一致）；本条目。


### 2026-08-15（分享访问安全加固 Phase 1 — 设计裁定）

- Plan `ai-dev/plans/nop-datav/2026-08-15-0004-2-share-access-hardening-rate-limit-stats.md` Phase 1 完成：
  `ai-dev/design/nop-datav/permission-sharing-design.md` 新增「访问限流与访问统计」章节，裁定 R1–R6：
  R1 组合键 `shareToken|来源IP`（`IServiceContext.getRequestClientIp()` 读 `nop-client-addr`，无 IP 退化
  token 单维显式记录；拒绝纯 token 单维——攻击者可 DoS 热门链接全部访问者）；R2 进程内 LocalCache
  容量上界（默认 10000，先例 TaskFlowManagerImpl）+ 单节点语义/集群边界显式记录 + fake 时钟 seam
  硬要求（禁 Thread.sleep）；R3 两级失败专用计数（密码失败 5 次/窗口锁定、总速率 60 次/窗口，窗口
  600s，开关 enabled=true 关闭时逐字节等价）+ 专用错误码 share-rate-limited/share-password-locked；
  R6 自定义固定窗口计数 + 锁定时间戳状态机（拒绝裸 DefaultRateLimiter：平滑速率无失败计数/锁定，
  且 getAcquireFailCount 平台缺陷；固定窗口边界突发 2× 显式接受）；R4 share 实体聚合列
  visitCount/lastVisitTime（拒绝 audit-query-patterns 明细留痕主形态：聚合口径不被 datav 拥有 +
  retention 漂移 + 写放大；定向 SQL 原子自增无 version bump、不触碰审计列；仅成功访问计数；写失败
  WARN fail-open 显式裁定）；R5 限流先于 token 查库（被限流请求零 DB，探测洪水前 M 次
  TOKEN_NOT_FOUND 超限后统一 RATE_LIMITED）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（6 warnings 均为 338-* 既有无关项）。
- Doc-sync: `permission-sharing-design.md`（唯一 owner doc 变更，旧「公共访问 action 契约」节加交叉引用）；
  本条目。


### 2026-08-15（ChatBI 多轮会话 — plan 收口）

- Plan `2026-08-15-0004-1` 收口：独立子 agent closure audit（task ses_ffd6dd1cbffeG6HC8CDgf70RzJ，fresh
  session）判定 **CLOSABLE**——A（Phase Exit Criteria/Closure Gates 逐条 file:line 证据）、B（Anti-Hollow
  调用链全真实持久层 + 单轮路径绕开会话表有 0 行断言）、C（三失败路径显式错误码 + LLM 零调用断言）、
  D（ai-design.md §10 与实现一致）、E（无 in-scope 项降级延期）全 PASS，无阻塞发现（仅 1 条 cosmetic：
  两个测试文件 package/目录不一致，与既有 TestNopDatavChatBiE2E 同模式，记入 follow-up）。
  审计者独立复跑 `./mvnw test -pl nop-datav/nop-datav-service -am` → 474/0/0。
- 收口三件套：`check-plan-checklist.mjs --strict` 退出码 0；`scan-hollow-implementations.mjs --module
  nop-datav --severity high` 退出码 0（0 findings）；Closure Evidence 已写入 plan `Closure` 段落，
  Plan Status → completed。Roadmap `ai-dev/backlog/nop-datav-roadmap.md` Last updated + D6-1 条目
  已标注 follow-up 落地。
- Doc-sync: 本条目 + roadmap；plan 文件自含 evidence。


### 2026-08-15（ChatBI 多轮会话 Phase 4 — 端到端验证）

- Plan `2026-08-15-0004-1` Phase 4 完成：新增 `TestNopDatavChatBiSessionE2E`（单测试覆盖全生命周期）：
  createChatSession → 第一轮 chatToQuery 会话模式（mock LLM tool-calling → 真实 IToolManager →
  真实 executor → 真实 H2 查询，断言 2 行真实数据 desc 排序）→ 第二轮追问「按月份细分」（捕获
  LLM 请求断言 4 条消息结构 + 第一轮 answer 与 [Result data] 真实数据摘要注入）→ 落库断言（4 条
  消息 seq 1..4、assistant resultJson 含真实行、标题首轮回填）→ getChatSessionHistory →
  deleteChatSession → 续接/查历史均显式 SESSION_NOT_FOUND。
  测试编写修正 2 处：子类遮蔽基类 jdbcTemplate 字段导致注入 null（改用继承字段）；H2 列标签大写
  （改大小写不敏感断言）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service -am` 474/0/0 全绿（Phase 2 的 9 + Phase 3 的
  14 + Phase 4 的 1 = 24 新用例）；`./mvnw test -pl nop-datav -am -T 1C` SUCCESS；
  `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: `ai-design.md` §10 与实现一致性复核无漂移（Phase 2 已回写 S2/S5 微调）；本条目。


### 2026-08-15（ChatBI 多轮会话 Phase 3 — 会话管理 action 与权限）

- Plan `2026-08-15-0004-1` Phase 3 完成：
  - `NopDatavChatBiBizModel` 新增 4 会话管理 action（createChatSession @BizMutation /
    listChatSessions @BizQuery / getChatSessionHistory @BizQuery / deleteChatSession @BizMutation，
    全部 requireSession owner 校验先行）。
  - 权限点：production `nop-datav.action-auth.xml` 新增 `FNPT:NopDatavChatBi:{createChatSession,
    listChatSessions,getChatSessionHistory,deleteChatSession}`（roles="admin,user"，orderNo 10073–10076，
    镜像既有 ChatBI 权限点）+ `NopDatavChatSession-main`/`NopDatavChatMessage-main` 的 query/mutation
    仅绑 admin（镜像 Share/ExportTask D1 先例，user 只经 owner 校验 action 触达）；test 侧
    `_vfs/test/datav/auth/app.action-auth.xml` 对称增补（RBAC E2E 用）。
  - 实现 bug 修复 1 个：`deleteSession` 逐实体 `deleteEntity` 跨 ORM session 报 entity-not-in-session
    → 两级均改 `deleteByQuery` 物理删除（镜像 ScreenBizModel 级联先例）。
- 新测试 14 用例：
  - `TestNopDatavChatBiSessionManagement`（7）：create 归属落库 / list 仅本人 / history 留存内容
    （user 无 resultJson、assistant 有）/ 非本人读/写显式拒绝且状态不变 / 删除后历史+续接+重复删除
    三路径显式 SESSION_NOT_FOUND + 物理删除断言 / 不存在会话全 action 显式报错。
  - `TestNopDatavChatBiSessionRbac`（7，enableActionAuth + graphQLEngine RPC 全链路）：user 调 4
    管理 action 放行；user 调 ChatSession/ChatMessage findPage 被拒（admin-only CRUD 闭合越权入口）；
    admin findPage 放行对照。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service` 473/0/0 全绿；`clean install -pl nop-datav -am`
  与 `-pl nop-datav/nop-datav-app -am`（含 web action-auth 变更）均通过。
- Doc-sync: 无新增 owner-doc 漂移（实现与 §10 裁定 S5 一致）；`ai-dev/logs/2026/08-15.md` 本条目。


### 2026-08-15（ChatBI 多轮会话 Phase 2 — 存储与多轮 chatToQuery）

- Plan `2026-08-15-0004-1` Phase 2 完成：
  - ORM 源模型 `nop-datav/model/nop-datav.orm.xml` 新增 `datav/chat-msg-role` dict +
    `NopDatavChatSession`（userName 归属 + sessionTitle 首轮回填 + 标准审计列）+
    `NopDatavChatMessage`（seq 每会话严格递增 + UK(SESSION_ID,SEQ) + role/content/resultJson CLOB）
    两实体；`./mvnw clean install -pl nop-datav/nop-datav-app -am -DskipTests` 走完整再生成链路
    （dao entity/_gen、_app.orm.xml、xmeta、i18n、pages、deploy sql 均由源模型派生，未手改生成物；
    管线自动生成的 CRUD BizModel 按仓库惯例保留，权限绑定见 Phase 3）。
  - `NopDatavConfigs` +2（`nop.datav.chatbi.history.max-turns` 默认 10 / `max-chars` 默认 20000）；
    `NopDatavErrors` +2（`chatbi-session-not-found` / `chatbi-not-session-owner`）+ ARG_SESSION_ID；
    `ChatBiResult` +sessionId 回显字段。
  - `ChatBiToolCallingLoop.run` 新增 6 参重载（historyMessages 注入点，null 时单轮逐字节等价；
    既有 5 参/2 参入口委托 null 历史回归兼容）。
  - 新增 `ChatBiSessionManager`（createSession/requireSession/listSessions/loadMessages/appendTurn/
    deleteSession/buildHistoryContext；经 IDaoProvider 真实持久层）+ `chatBiSessionManager` bean；
    `chatToQuery` 增加 `@Nullable sessionId` 参数（缺省单轮不变；携带时归属校验→S2 截断注入→
    执行→appendTurn 落库，失败轮不落库）。
  - 实现 bug 修复 2 个（执行中发现）：`findFirstByQuery` 的 orderBy 在该 DAO 路径不生效 → max-seq
    改 `findAllByQuery` desc 取首条；`buildHistoryContext` kept/content 下标配对错误。
  - 设计微漂移回写：S2「RESULT_JSON 非空时追加数据摘要」精确化为「columns/rows 非空时」；S5 拒绝
    方案措辞精确化为「标准 CRUD 权限点仅绑 admin（生成的 CRUD BizModel 保留）」。
- 新测试：`TestChatBiSessionMultiTurn`（9 用例：第二轮 LLM 请求含第一轮问答、轮数/字符双上界截断 +
  最老优先 + 单条超预算截断、Result data 摘要注入、无会话参数单轮回归（请求仅 system+user 且会话表
  零写入）、跨请求持久化（seq 1..4 + resultJson + 标题回填）、不存在/非本人会话显式抛错且 LLM 零调用、
  配置项接线证明）。
- 验证：`./mvnw test -pl nop-datav/nop-datav-service` 459/0/0 全绿（含既有 ChatBI E2E 回归 +
  9 新用例；一次 TestNopDatavAuditLog 偶发 ordering 失败复跑消失，与本变更无关已验证隔离绿）。
  `./mvnw clean install -pl nop-datav -am -T 1C -DskipTests` + `-pl nop-datav/nop-datav-app -am` 通过。
- Doc-sync: `ai-dev/design/nop-datav/ai-design.md` §10 微调（见上）；`ai-dev/logs/2026/08-15.md` 本条目。


### 2026-08-15（ChatBI 多轮会话 Phase 1 — 设计裁定）

- Plan `ai-dev/plans/nop-datav/2026-08-15-0004-1-chatbi-session-history-multi-turn.md` Phase 1 完成：
  `ai-dev/design/nop-datav/ai-design.md` 新增 §10「ChatBI 多轮会话」，含裁定 S1–S5
  （S1 datav 自有 NopDatavChatSession/NopDatavChatMessage 轻量实体、拒绝复用 nop-ai NopAiSession
  实体族；S2 最小集注入 + 双上界取小 + 最老优先丢弃；S3 chatToQuery 可选 sessionId + 显式
  createChatSession action、拒绝客户端生成 id 与首轮无参自动建会话；S4 userName 归属 + 结果行落库
  + seq 序号列 UK 并发显式失败；S5 4 管理 action 全自定义权限点、不建 biz 接口、不开 CRUD），
  附 §10.3 被拒方案汇总与 §10.4 配置增补。§7 follow-up 清单中「会话历史持久化」标记已由 §10 落地。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（6 warnings 均为
  `ai-dev/plans/338-*` 既有无关项）。
- Doc-sync: `ai-dev/design/nop-datav/ai-design.md` 已更新（本 Phase 唯一 owner doc 变更）。


### nop-plugin-enhancement W6 closure 落档 + Phase 3/4 补全（roadmap W6 done）

- **Plan**：`ai-dev/plans/2026-08-14-2007-3-parent-hierarchy-hmr.md`（nop-plugin-enhancement W6，parent 层级 + HMR）
- **做了什么**：W6 最后两个 Phase（3 变更检测接线、4 P2-D 裁决 + 测试补全）落地并 closure。
  - **Phase 3（变更检测）**：裁定显式检查入口形态（`checkChangedAndReload()`，框架不起轮询线程，宿主定时调用；平台无可复用的事件式通知原语，以 `ResourceComponentManager.checkChanged` 为准）；lastModified 来源钉死（load 时记录资源真实 `IResource.lastModified()`，非 `getLastChangeTime` 时钟语义——`DefaultResourceChangeChecker` 严格比对）；测试机制 = `file:` 前缀可写临时目录（`target/plugin-change-test/`，与 TestReloadPlugin 同模式；mtime 显式推进消除同 ms 抖动）。
  - **Phase 4（P2-D 裁决）**：reconcile 路径级联式落地——`doReconcile` 实例评估挂父链健康检查（`isParentChainHealthy`：祖先 DEACTIVATED → 子不激活）+ `deactivateCascadeIfActive`（先递归级联去激活 ACTIVATED 后代再父，保留实例对象——否则子容器挂已停父容器、服务回退抛 `ERR_IOC_CONTAINER_NOT_STARTED` 硬失败）；显式路径守卫式（既有 `doDeactivateWithGuard` + `ERR_PLUGIN_ACTIVE_CHILDREN_EXIST`）。createInstance 检查顺序钉死验证：checkLoaded → 重复 key → 父状态 → 门控 → 创建。
  - **测试**：`TestParentHierarchy` +3（P2-D reconcile 级联、父状态先于门控、重复 key 先于父状态）、`TestChangeDetection` 新建 +3（触发 reload、无变更 no-op、requires 依赖方收敛）；新增夹具 `parent-gated.plugin.xml`。
- **验证**：`./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（83/83：api 6 + manager 67 + support 10）；`check-plan-checklist.mjs --strict` exit 0；`scan-hollow-implementations.mjs --module nop-plugin-manager --severity high` exit 0；api 模块零依赖 grep 0 命中；独立 sub-agent closure audit 通过（audit 指出 roadmap/日志未同步 + 计数笔误，本次落档全部修正）。
- **roadmap 同步**：`nop-plugin-enhancement-roadmap.md` W6 `planned` → `done`（独立 closure audit 通过）。
- **doc-sync 裁定**：No owner-doc update required（W7 统一同步 docs-for-ai）；本日志已更新。


### nop-plugin-enhancement W6 验证运行：full-green verification

- **Plan**：`ai-dev/plans/2026-08-14-2007-3-parent-hierarchy-hmr.md`（nop-plugin-enhancement W6，Phase 1/2 落地 + Phase 3 部分接线）
- **做了什么**：本轮为 mission-driver VERIFY 步骤，无新增代码改动；验证 W6 工作树（parent 层级 + reloadPlugin 快照重建 + 变更检测接线）全链路构建与测试。
- **验证**：
  - `./mvnw compile -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am` 通过。
  - `./mvnw clean install -DskipTests -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS。
  - `./mvnw test -pl :nop-plugin-api,:nop-plugin-manager,:nop-plugin-support -am -T 1C` BUILD SUCCESS（manager 61/61 全绿：TestParentHierarchy 7 + TestReloadPlugin 5 新用例全过，0 failures / 0 errors）。
  - checkstyle：项目未配置该 lint 通道（对 nop-api-core 既有遗留违规 9164 条报错，mission 配置以 `echo 'lint not configured'` 兜底——非本 mission 引入）。
  - `node ai-dev/tools/check-doc-links.mjs --strict` 0 errors。
- **doc-sync 裁定**：No owner-doc update required（W7 统一同步 docs-for-ai）；本日志已更新。


### EXEC_PLANS nop-metadata ORM 缺失索引补建（F10 + F11 + F12）收口

plan `ai-dev/plans/2026-08-14-1448-3-nop-metadata-orm-missing-index-creation.md`（mission-driver EXEC_PLANS，构成 plan-first ORM 变更的人工放行）执行完成并收口。全部为源模型 `nop-metadata/model/nop-metadata.orm.xml` 加性/扩列索引变更（1 扩展 + 4 新增 + 1 新增 = 6 处），不改列、不改 UK、不删字段。

- **F10**：`IX_NOP_META_EVENT_TYPE_TIME` 列清单扩展 `(entityType, changeTime)` → `(entityType, entityId, changeTime)`（两列间插入 `entityId`；覆盖 `WHERE entityType=? AND entityId=? ORDER BY changeTime` 审计查询模式，与兄弟实体 `IX_NOP_META_QRULE_ENTITY`/`IX_NOP_META_TAG_LABEL_ENTITY` 的 entityType 前导组合约定一致，避免冗余第二索引；索引名保留）。
- **F11**：四个软外键列各新增单列索引——`IX_NOP_META_DOMAIN_SOURCE_MODULE(sourceModuleId)`、`IX_NOP_META_TABLE_BASE_ENTITY(baseEntityId)`、`IX_NOP_META_DIM_ENTITY_FIELD(entityFieldId)`（NopMetaTableDimension）、`IX_NOP_META_MEASURE_ENTITY_FIELD(entityFieldId)`（NopMetaTableMeasure）。
- **F12**：`NopMetaQualityResult` 新增 `IX_NOP_META_QRESULT_RUN(runId, executeTime)`（runId 前导按批次过滤 + executeTime 尾列排序，与 `IX_NOP_META_QRESULT_RULE` 正交）。
- **重新生成**：`./mvnw install -pl nop-metadata -am -DskipTests` → `nop-metadata-dao/.../_vfs/nop/metadata/orm/_app.orm.xml` 再生成含全部 6 处（grep 证据：6 个索引名命中，EVENT_TYPE_TIME 列清单含 entityId，:2599-2603）；`clean install` 复跑再生成结果确定性一致。git diff 仅源模型 + `_app.orm.xml` 两个文件各 +17 行（5 个新索引块 16 行 + F10 1 行插入），未手编任何 `_` 前缀文件。
- **验证**：`./mvnw compile -pl nop-metadata -am` exit 0；`./mvnw test -pl nop-metadata -am -T 1C` → **BUILD SUCCESS**（nop-metadata 五模块全绿，1175 tests / 0 failures / 0 errors；前两轮 reactor 分别因 `nop-ai-core` `TestResponsesDialectIntegration` SSE 异步时序、`nop-stream-rocksdb` 增量快照性能基准 ratio=1.53 并发 flake 失败——两者单跑均通过，与本计划 ORM 索引变更无关，第三轮全绿；`nop-auth` `TestChannelScanBindLoginE2E` VarCollector 并发 flake 同 08-15 上一 entry 记录）。`check-orm-unique-key-constraint --module nop-metadata` exit 0（37 UK / 0 hits）；`check-silent-swallow --module nop-metadata` exit 0（125 blocks / 0 hits）；`scan-hollow-implementations --severity high` exit 0（0 findings）。checkstyle CLI `checkstyle:check -pl nop-metadata -am` 在上游 `nop-api-core` exit 1（9164 violations，pre-existing 基线，见 08-15 上一 entry；本计划零 Java 源码变更，lint not configured 裁定）。
- **范围裁定**：F13（quality-trend-direction dict 注释）经独立审查确认为 false positive（orm.xml:104-105 注释已同时覆盖两个 dict），移出本计划——roadmap F13 与审计源 `2026-08-14-0707` F13 已补 false-positive 标注，避免后续重复追踪。
- Roadmap：F10/F11/F12 标记 ✅ Fixed（plan 1448-3）。
- Doc-sync: No owner-doc update required——`docs-for-ai/03-modules/nop-metadata.md` 仅记载搜索索引（search engine）语义与实体清单，不记载数据库 `IX_NOP_META_*` 索引清单，加性性能索引不改变任何已记载行为（已复核）。


### EXEC_PLANS nop-metadata safeProductName 去重与诊断退化清扫（F17 + AR-14）收口

plan `ai-dev/plans/2026-08-14-1448-2-nop-metadata-safeProductName-dedup-and-diagnostic-degradation.md`（mission-driver EXEC_PLANS）执行完成并收口。前一执行 session 已落地 Phase 1/2 全部代码与 Phase 3 代码（工作树未提交、plan checklist 未勾选即中断）；本 session 逐项对照 live repo 核验 Phase 3 落地事实、补齐门禁缺口、完成 closure。

- **Phase 3 核验（AR-14b/c/d/e，代码均已落地，本 session 逐项验证）**：
  - AR-14b：`MetaAggregationExecutor` 死 `LOG` 已删（`rg -n "LOG|Logger" MetaAggregationExecutor.java` 零命中）。
  - AR-14c：`resolveEntityFieldColumn` 签名已无 `propToCol`；`rg -n "resolveEntityFieldColumn" nop-metadata/` 仅定义 + `EntityAggregationProcessor:260/273` 两调用点（已同步去参）；`propToCol` Map 本体保留（`resolveEntityColumns` :61 构造，:63/:64/:85/:88/:121/:214/:247/:269 filter 重写/列集合/bypass-via-eql 用途完整）。
  - AR-14d：`resolveProjections(SqlStatement, String sql)` 已接受 `sql` 形参（:91 `resolveProjections(stmt, sql)` 传入）；:114-116 错误点已改 `.param("sql", sql)` 真实 SQL 文本 + `.param("reason", ...unhandled SELECT statement class...)`。focused test `TestSqlSelectFieldExtractor` 3 例断言 `ex.getParam("sql")` == 真实 SQL 文本。
  - AR-14e：`MetaManifestBuilder` 已用预构造不可变 `DateTimeFormatter.ofPattern("yyyy-MM-dd'T'HH:mm:ss'Z'").withZone(ZoneOffset.UTC)`（`ISO_UTC` :38-39）替换方法内 `SimpleDateFormat`；`TestMetaManifestBuilder.generatedAtIsoUtcFormat` 断言 epoch → `"1970-01-01T00:00:00Z"`（输出格式等价）。
- **门禁修复 1（check-silent-swallow 扫描器误报，根因修复）**：本计划 Closure Gate 要求 `check-silent-swallow.mjs --module nop-metadata` exit 0，但仓库存在 2 处 pre-existing 命中（08-14 日志已记录：`MetaTableProfiler.java:223` javadoc 文本 `catch (SQLException)` + `{@code 90015}` 被误判为真 catch 块——扫描器 catch 检测不识别 `/* */` 块注释；`AggregationHelper.java:564` `catch (NumberFormatException) { return null; }`）。08-14 的先例（plan 1448-1）以"pre-existing 非本计划改动文件"豁免，但本计划 Phase 1/3 修改了 `AggregationHelper.java`，该豁免不再成立——本 session 选择把两处命中真正修掉使门禁字面成立：
  - 扫描器：新增 `maskCommentsAndStrings()`（等长掩码，注释体/字符串内容置空格、保行结构），catch 检测与 brace 匹配改在掩码文本上运行——javadoc 内 `catch (` 不再被检出（130→125 块）。新增 fixture `javadoc-catch`（javadoc 内 catch 不检出）与 `javadoc-then-violation`（javadoc 后真违规仍检出，防过度掩码），8/8 PASS。规则本身（catch 块须含 rethrow/ErrorCode 信号）零放松。
  - `AggregationHelper.toBigDecimal`：NFE catch 按本 mission benign-miss 形式化先例（`MetaTableProfiler.probeNumeric` AR-06）补 `LOG.debug(ErrorCode...)` 可见信号，新增 `NopMetadataErrors.ERR_AGGR_VALUE_NOT_NUMERIC`（`nop.err.metadata.aggr-value-not-numeric`，注册于 `AggregationErrors`）；null 返回语义不变（调用方回退非数值处理），`TestCrossDbInMemoryAggregationProcessor` 既有精度测试无回归。
  - 修后 `check-silent-swallow.mjs --module nop-metadata` → **125 blocks / 0 hits / exit 0**（字面满足 gate）。
- **验证**：`./mvnw test -pl nop-metadata -am -T 1C` → **BUILD SUCCESS，nop-metadata-service 1174 tests, 0 failures** + web 1/1（前两轮 reactor 因 `nop-auth` `TestChannelScanBindLoginE2E.testScanLoginFullChainE2E` VarCollector NPE 并发 flake 失败——该测试 08-09 由另一 mission 引入、单跑 4/4 通过、与本计划改动无关；第三轮全绿）。`scan-hollow-implementations --severity high` exit 0；`check-plan-checklist --strict` exit 0；checkstyle：本 session 新增行零新增 finding（AggregationErrors/AggregationHelper 我方新增块已整理 ≤80 列；模块整体 checkstyle 红为 pre-existing 基线，HEAD 同样 exit 1，非本计划引入）。
- **偏差**：Phase 3 的 4 项代码修复由前一 session 完成、本 session 核验后直接勾选（非重复实现）；门禁修复（扫描器 + NFE catch 形式化 + 新 ErrorCode）为 gate 字面成立的收口工作，超出 plan 字面条目但属 Minimum Rules #13"硬门禁不可降级"的必然要求，已在 Closure 记录。
- Roadmap：`ai-dev/backlog/nop-metadata-invariant-loop-roadmap.md` F17、AR-14 标记 ✅ Fixed（plan 1448-2）。
- Doc-sync: No owner-doc update required（内部 helper 去重 + 失败语义修正；`docs-for-ai/03-modules/nop-metadata.md` 不引用 `safeProductName`/`resolveEntityFieldColumn`/`resolveProjections`/manifest 日期格式，已复核）。工具变更限于 `ai-dev/tools/check-silent-swallow.mjs`（mission 自有 invariant 工具）。
- Doc-link：`check-doc-links --strict` 17→16 errors——本 mission roadmap（`nop-metadata-invariant-loop-roadmap.md:5` 先例引用实为 nop-chaos-flux 仓库路径、本仓库不可解析）随本改动修复为非链接文本，本计划改动文件 0 broken link；剩余 16 处均在其它 mission 的 backlog 文件（nop-ai/nop-code/nop-credential/nop-stream），pre-existing 非本计划改动面。
