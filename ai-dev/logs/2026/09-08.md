# Dev Log 2026-09-08

### 2026-09-08（mvn install 全绿修复）

- 目标：`./mvnw clean install -T 1C` 彻底成功。逐个修复 6 处失败后达成 BUILD SUCCESS。
- 修复内容：
  1. `nop-stream-core/TestResultPartitionOverflowBypass`：`countDown()` 在 producer 线程 `finally` 中执行后立即断言 `isAlive()` 存在调度竞态；断言前补 `producer.join(5s)`。
  2. `nop-stream-rocksdb` / `nop-stream-runtime` / `nop-stream-fraud-example`（pom）：Windows ARM64 上 rocksdbjni 无 aarch64 dll，测试全部 `UnsatisfiedLinkError`，且失败的 native load 残留非守护线程导致 surefire fork 永不退出、reactor 挂死。新增 `skip-tests-windows-arm64` profile（`<os><family>windows</family><arch>aarch64</arch></os>` 激活 `skipTests=true`），仅跳过该平台。
  3. `nop-stream-connector/TestConnectorResourceManagement`：`started` latch 在 `run()` 之前 countDown，`cancel()` 可能先于 `run()` 的生命周期重置（running=true + 新 latch）导致 cancel 信号丢失、线程永不退出。改为在 `subscribe()` 内发信号，保证 cancel 发生在 run 启动后。
  4. `nop-ai-shell/ShellConcurrencyEdgeCaseTest`：stage1 启动后立即断言 stage2 已启动，无自身调度窗口，高负载下假失败；给 stage2 补 1.5s 有界轮询窗口（仍在 blocker 2s sleep 内，不削弱并发语义）。
  5. `nop-retry-engine/TestRetryEngineImpl`：两个测试复用 SID `policy-1`，测试间 DB 状态保留导致主键冲突；第二个测试改用独立 SID `policy-discard`。
  6. `nop-report-pdf`（产品修复）：模板文本中的不换行空格 U+00A0/U+202F/U+2007 在 Helvetica 及 SimHei 回退字体中均不可编码，抛 `font-missing-glyph`。扩展 `PdfStyleHelper.normalizeControlChars` 归一化这三类空格为普通空格，并在 `PdfTableRenderer`/`PdfSheetRenderer` 字形回退选择前先归一化。
- 偏差：无 plan，属 ad-hoc 修复；rocksdb 相关模块选择 pom profile 全模块跳过而非逐测试类 `@EnabledIf`（18+ 个测试类，且需同时规避 fork JVM 挂死）。
- 验证：`./mvnw install -r -T 1C` 分段续跑，最终 BUILD SUCCESS（全部 427 模块安装成功）；单独验证过 `nop-stream-connector`、`nop-ai-shell`、`nop-retry-engine`、`nop-report-pdf/nop-report-demo` 模块测试通过。
- Doc-sync: No owner-doc update required（测试/平台兼容性修复，不改变用户可见 API；ARM64 限制已在 pom 注释中说明）。

<!-- 以下为 feat-nop-treesitter worktree 的执行日志 -->


### 2026-09-08 计划 2026-09-08-0234-2-query-engine-json-highlights closure audit 通过（roadmap item 8 收口）

- **审计结论**：全部 30 项执行项/Exit Criteria + 10 项 Closure Gates [x]，ledger 完成公式满足（`## Verification` pass 行 + `## Closure` dispatch/accepted receipt 配对）。receipt: `#audit-20260908-0714-2026-09-08-0234-2-query-engine-json-highlights-1-a2710acb`。
- **验证套件（closure visit 重跑）**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **270 tests 0 failures 0 errors**（TSQueryParserTest 25、TSQueryCompileTest 15、JsonHighlightsQueryTest 3、TSQueryPredicateTest 9；JSON corpus 7/7、Java 108/108、JS 33/33 回归）；`test-compile`、`clean package -DskipTests` 绿；checkstyle `-Pqa` nop-treesitter 0 violations（plain `checkstyle:check` 失败为 nop-api-core 全仓基线 9225 项，非本 diff）；`scan-hollow-implementations.mjs --module nop-treesitter --severity high` 0 findings。
- **语义核验**：端到端（`TSParser.parse → TSQuery.compile(highlights.scm) → TSQueryCursor` over 7 corpus fixtures + 6 curated snippets，capture 断言 + cursor-walk 可达性 cross-check 成立）；接线验证（executor 仅驱动 `TSParser.parse` 产物 + `TSTreeCursor`，无平行树表示）；predicate 真假路径 + 编译期 fail-loud 每类错误有测试；无 deferred 隐藏 in-scope defect（anchors/quantifiers/`#any-of?` 为 roadmap item 10/14 successor 记录）。
- **Doc-sync**：roadmap item 8 `done`（closure-derived）；No owner-doc update required（模块内部，public API 文档归属 roadmap item 14）。

### 2026-09-08 计划 2026-09-08-0234-2-query-engine-json-highlights Phase 3 完成（TSQueryCursor executor + predicates + highlights.scm 端到端）

- **`TSQueryCursor`**：`TSParser.parse` 产物上 pre-order 行走（`TSTreeCursor`，root-anchored），每节点按 pattern 序尝试全部 pattern；递归 matcher（Type/Wildcard/Anonymous/Alternation + field-qualified/positional child steps，child 向前扫描 + checkpoint 回滚，镜像 C split-state 语义）；capture 按 pattern 步序收集（root 先于 children）；`TSQueryMatch`（patternIndex + capture 列表 + `node(name)`/`captureMap()`）。child 迭代越过 visible parent 的 sibling-bubble 守卫（`child.parent() != parent` break）——C cursor 的 gotoNextSibling 会冒泡，递归匹配必须在父边界停住。
- **Predicates**：`#eq?` 精确文本比较、`#match?` Java `Pattern` 全匹配（编译期 wrap `\A(?:...)\z`）；失败 → match 抑制（C 语义）；capture 缺失 → 抑制。
- **Runtime 增强（byte range 真实化）**：arena 新增 `nodeSize` 列（`setSize`/`sizeOf`）；GLR shift 记录叶节点真实长度（`endOffset - startOffset`，GSS `subtreeSize[]` 保持 gap+token 不动）；reduce 时 composite padding = 首子 start、size = 末子 end - 首子 start（children 已分配故可直接推导）；`TSTree.snapshot` 保留 size；`TSNode` 新增 `endByte()`。`subtreeEquivalent`（GSS 链接合并）的 padding 比较随真实化变严格，JSON/Java/JS corpus 全部回归绿。
- **端到端**：vendored `highlights.scm` over 7 corpus fixtures + 6 curated snippets，expected capture 集全部手推（walk 序 + pattern 序），含 `key: (_) @string.special.key` 只捕获 pair-key、`[null true false]` 只捕获字面量、key 同时被 @string 与 @string.special.key 捕获。cross-check 不变式：capture 节点 (type,startByte) 必须出现在同一 `TSTreeCursor` walk 且 walk 的 named 序列 == s-expression 节点序列。
- **Grammar 事实发现**：vendored tree-sitter-json 的 escape_sequence = `\` + 单字符（`b|f|n|r|t|u` 类，grammar.js 原文），`\u0041` 按上游 lex 为 `\u`(escape) + `0041`(string_content) —— curated snippet 期望按此修正。
- **验证**：新增 `query/JsonHighlightsQueryTest` 3 项 + `query/TSQueryPredicateTest` 9 项，全仓 `./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **270 tests**（JSON 7/7、Java 108/108、JS 33/33 in-scope 回归）；`scan-hollow-implementations.mjs` 0 findings；`checkstyle:check -Pqa` 0 violations。
- Doc-sync: No owner-doc update required（模块内部；`TSNode.endByte()`/`SubtreeArena` 扩展为模块内 API，public 文档归属 roadmap item 14）。

### 2026-09-08 计划 2026-09-08-0234-2-query-engine-json-highlights Phase 2 完成（TSQuery IR + 编译期验证 + capture dedup Decision）

- **`TSQuery`**（compiled IR，nested records）：`Pattern(patternIndex, root, predicates)`；sealed `Matcher` = `TypeMatcher(symbolId, captureId, children)` / `WildcardMatcher` / `AnonymousMatcher` / `AlternationMatcher(elements, captureId)`；`ChildMatcher(fieldId, matcher)`（fieldId 0 = positional）；`Predicate(captureId, text, regex, compiled)`。捕获 slot -1 = 无 capture。`Language` 新增 `symbolId(name, named)` 反向查找（镜像 C `ts_language_symbol_for_name`：只匹配 visible symbol，隐藏 `_`/`document_repeat1` 等 aux 符号不可引用）。
- **Decision（capture 语义，证据 `~/sources/treesitter/tree-sitter/lib/src/query.c:2946` `symbol_table_insert_name` + `ts_query_capture_name_for_id`）**：capture 名按全局 symbol table 内联——同名跨 pattern 共享同一 capture id（dedup），`captureNames()` 按首次出现序返回；同一 pattern 内多处同名 capture 各自产出 capture 条目（`ts_query_cursor_next_capture` 逐条返回语义）；predicate 引用 capture 必须在同一 pattern 内已出现（`query.c` `TSQueryErrorCapture`，`symbol_table_id_for_name` 返回 -1 即报错）；match 内 capture 顺序 = pattern 步序（root 先于 children）。
- **编译期验证全部 fail-loud**：unknown type（含 hidden symbol）/ unknown anonymous token / unknown field / `#match?` regex 编译失败 / predicate 引用未知 capture / 未知 predicate 名（`#any-of?`）→ typed `TreeSitterException` 命名符号。
- **Alternation flattening**：无 capture 的嵌套 alternation 在编译期并入父 elements（镜像 C step 链接语义），带 capture 的保持嵌套。
- **Vendored**：`src/test/resources/upstream/grammars/tree-sitter-json/queries/highlights.scm`（上游原文件 6 patterns）。
- **验证**：新增 `query/TSQueryCompileTest` **15 项**（≥8 门槛远超：每类 matcher IR 形状、field-qualified wildcard child、capture interning、alternation flattening + captured-nested 保持、unknown type/field/anonymous/regex/capture-ref/predicate 错误、vendored highlights 6-pattern 结构断言），全仓 `./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **258 tests**（243 + 15，JSON corpus 7/7、Java 108/108 回归）。
- Doc-sync: No owner-doc update required（模块内部；`Language.symbolId` 为模块内 API，public 文档归属 roadmap item 14）。

- **新增** `io.nop.treesitter.query` 包：`TSQueryParser`（递归下降，含 `;` 注释/空白容忍）、AST（`Query`/`Pattern`/`PatternNode` sealed records：Type/Wildcard/Anonymous/Alternation + capture + field-qualified children、`ChildPattern`、`Predicate.Eq/Match`）。语法覆盖：`(type field: (child)* @cap)`、`(_)`、`"anon"`、`[ p1 p2 ]`（嵌套）、`@capture`（含 `@string.special.key` 点分名）、`(#eq? @c "v")`/`(#match? @c "re")`。上游 `query.c` 对齐：ident 起止字符（alnum/`_`/`-`，续 `.`）、predicate 名含 `?`、字段前缀 `ident:`、`_`/`"lit"` 可作裸 child。
- **严格 fail-loud**（每类均有测试）：unclosed `(`/`[`、`()`/`[]` 空模式、游离 `)`/`]`、`@` 后无 capture 名、unterminated string、裸 ident child；unsupported 构造（`.` anchor、`+`/`*`/`?` quantifier、`#any-of?` 等未知 predicate）抛 `TreeSitterException` 命名构造 + offset，无 ignore-and-continue。
- **验证**：新增 `query/TSQueryParserTest` **25 项**（≥10 门槛远超：type/anonymous/wildcard/field qualifier/alternation incl. nested/capture on pattern + alternation element/注释空白/转义/多 pattern 顺序/predicate 解析/10+ 类 malformed 失败），全仓 `./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **243 tests**（218 基线 + 25，JSON corpus 7/7、Java corpus 108/108 回归，本 phase 未触碰 runtime）。
- Doc-sync: No owner-doc update required（模块内部，public API 文档为 roadmap item 14）。

- **审计结论**：45/45 检查项 [x]（Phase 1-4 全部执行项 + Exit Criteria），ledger 完成公式满足（`## Verification` pass 行 + `## Closure` dispatch/accepted receipt 配对）。receipt: `#audit-20260908-0615-2026-09-08-0234-1-external-scanner-vm-1-a8a3dbe1`。
- **验证套件（本 visit 重跑）**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **218 tests 0 failures 0 errors**（JsCorpusTest 33/33 in-scope + 1 adjudicated error-recovery section，JSON corpus 7/7、Java corpus 108/108 v3 路径回归）；`test-compile`、`clean package -DskipTests` 绿；plain `checkstyle:check` 失败为 nop-api-core 全仓基线 9225 项（非本 diff）；`scan-hollow-implementations.mjs --module nop-treesitter --severity high` 0 findings。
- **端到端/接线/anti-hollow**：`TSParser.parse` → `GLRParser.parse` → `Lexer.next`（`Lexer.java:61-67` externalLexState!=0 时调用 `ScannerVM.scan`，scanner token 经 GLR shift 机制、拒绝回退 DFA）单一路径；JsCorpusTest 6 个 vendored corpus 文件经该路径到归一化 sexp 字节相等；`LexerExternalScanTest` 5 项接线断言 + `JsScannerCCrossCheckTest` 73/73 C 交叉验证；reserved-word filtering 走 `isReservedWord` 真实表查询；v2 blob typed rejection、越界 row/undecodable C 构造 fail-loud。
- **语义抽查**：`destructuring.txt` error-recovery section（期望树含 ERROR/MISSING）按计划 adjudicated-deviation 记录，successor item 11；full JS corpus 显式 successor item 10（本计划只过 external-token-scoped subset）。
- **doc-sync**：roadmap item 7 → `done`、M2 → `done`（closure-derived，5+6+7 齐）；`blob-format.md` v3 为唯一 owner-doc 更新（模块内部 contract）。

### 2026-09-08 计划 2026-09-08-0234-1-external-scanner-vm Phase 3+4 完成（ScannerCompiler DSL + JS scanner.c 翻译 + GLR 集成 + JS corpus）

- **Phase 3（ScannerCompiler + DSL 翻译）**：重写 `scanner/ScannerCompiler.java`（此前文件损坏：`emitInstruction` 方法缺失、`Line` record 3 参构造、label patch 只留最后一处）。DSL 语法：`token` 声明（scanner.c enum 序）+ 标号 + mnemonic 行；编译产物过 `ScannerProgram.validate`。JS `scanner.c` 364 行手写翻译为 `src/test/resources/upstream/grammars/tree-sitter-javascript/src/scanner.dsl`（与 vendored parser.c 同目录，`Ts2Java` 自动编译嵌入 blob scanner-program 段，blob 重新生成 396794 字节）。寄存器分配：`flag`=scanned_comment/has_content/at_newline，`result`=WhitespaceResult 3 态/saw_block_newline，`state`=comment_condition。ws 子程序按 consume 参数双份（ws_false/ws_true），ASI 因 C 语义"comment 块后落穿到 }/换行/iswspace 检查（不重循环到 EOF 检查）"重排控制流。
- **ScannerVM 语义修正**：`Result.startOffset` 改为 skip 调整的 token start 并按 C `ts_lexer_finish` clamp（token start ≤ mark_end，纯 skipped 内容 → 零宽 token）；`blob-format.md` §22 记录 token start 语义。
- **C 交叉验证（gold standard）**：把 vendored scanner.c 编译为 TSLexer harness（实现 C runtime 的 token_start/mark_end/finish-clamp 语义），与 VM 跑同一 73 例 battery（6 个 scan 函数 + dispatcher gates + 平台 iswspace 差异）——**73/73 token-for-token 一致**（symbol/start/end）。过程中纠正了多个 DSL 分支方向反转 bug（`jmp_if_X` 条件成立落穿约定）；发现并确认平台语义：macOS `iswspace(0)=0`（EOF 后 comment 无 ASI）、`/*c*/` EOF → NO_NEWLINE → `!iswspace(0)` → reject。
- **Phase 4（GLR 集成）**：`Lexer.next` 镜像 C `ts_parser__lex`——`externalLexState(parseState) != 0` 时先跑 `ScannerVM.scan`（validSymbols = states 行 ∩ has_actions），scanner token 再验证 hasActions 后走正常 shift 路径；拒绝则 DFA 回退。keyword capture 按 C 语义改为 `has_actions || is_reserved_word`（计划措辞"reserved 过滤"与 C `parser.c` 实际 `has_actions(parse_state, sym) || ts_language_is_reserved_word(...)` 对齐——Java/JSON 无 reserved 数据不受影响）。
- **Phase 1 遗留缺陷修复（JS parse table 提取）**：v0.25.8 生成器用 `[STATE(0)]` designated initializer，旧 regex `\[(\w+)\]` 匹配失败→整个 large parse table 静默为全零（Phase 1 未跑 JS corpus 故未暴露）。regex 放宽为 `\[(?:STATE|SMALL_STATE)?\(?(\w+)\)?\]` 并加"零行 fail-loud"守卫；`ts_parse_table[1][1]` 与编译后 C 结构逐值核对（9 == 9）。
- **验证**：新增 `scanner/ScannerCompilerTest`（10 项：determinism、fail-loud 各构造、多跳转同 label patch 回归）、`scanner/JsScannerTokenTest`（38 项 token 级）、`scanner/JsScannerCCrossCheckTest`（73 例 C 交叉验证固化）、`corpus/JsCorpusTest`（**33/33 in-scope sections pass 100%**，82 个 out-of-scope 记录 skip）、`lexer/LexerExternalScanTest`（5 项接线验证：ASI/template/html_comment external token 走 GLR shift、reserved word property key、scanner 拒绝回退 DFA）。全仓 `./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **218 tests**；JSON corpus 7/7、Java corpus 108/108 在集成 v3 路径回归（external scan 对两者从不触发）。
- **裁定**：`destructuring.txt` "Extra complex literals in expressions" 是 in-scope（含 `string_fragment`）但期望树含 `(ERROR ...)`/`(MISSING ";")`——error recovery 为 roadmap item 11（本计划 Non-Goal），按计划 adjudicated-deviation 路径记录（vendored 文件 + section + 期望 sexp 本身为证据），successor: item 11。
- **roadmap**：item 7 → `done`、M2 → `done`（closure-derived，5+6+7 齐）。
- Doc-sync: `blob-format.md` §22/§23（token start 语义 + scanner.dsl 布局）为唯一 owner-doc 更新（模块内部）。

### 2026-09-08 计划 2026-09-08-0234-1-external-scanner-vm Phase 2 完成（Scanner bytecode ISA + ScannerVM）

- **Decision 记录（final ISA）**：roadmap opcode 集（PUSH_BYTE/PUSH_BYTES/SPAN/ACCEPT/ADVANCE/SKIP/JMP_IF_EQ/JMP_IF_NE/CALL/RET）与 gotreesitter `external_vm.go` 裁定合并为 24 条最终 ISA——每 opcode 的编码/语义记录在 `blob-format.md` §22，逐条 justification 对应 scanner.c 需表达的构造（template-char 循环、whitespace/comment skip 循环、html comment / instanceof 字面量匹配、0x2028/0x2029 多字节、mark_end + result_symbol、dispatcher valid_symbols 门）。
  - 新增：`JMP_IF_IN_RANGE`（IfRuneInRange）、`JMP_IF_WS/ALPHA/DIGIT`（IfRuneClass）、`JMP_IF_VALID`（RequireValid）、`SET_STATE`/`JMP_IF_STATE_EQ`（SetState/RequireStateEq，stateful scanner 预留）、`MARK_END`/`EMIT`（MarkEnd/Emit，EMIT 直接携带预映射 symbol id）、`RET`（顶层返回=scan false，对齐 C `scan()` 返回 false）、`SET_RESULT`/`JMP_IF_RESULT_EQ`（过程返回值，WhitespaceResult 3 态 + jsx_text bool 跨过程传递）、`SET_FLAG`/`JMP_IF_FLAG_EQ`（scanner.c `*scanned_comment` out-param）。
  - 步骤预算：`max(program_len×16, 剩余字节×4+128)`，超限抛 typed `TreeSitterException`（无死循环）。调用栈深度 64、操作数栈 256。
- **`ScannerProgram`**：load-time 校验（未知 opcode / 越界或落指令中间的 jump target / SPAN 操作数栈下溢的 fixpoint 高度分析 / 畸形操作数），全部 typed 拒绝。
- **`ScannerVM`**：解释器（UTF-8 codepoint lookahead、EOF=0、ADVANCE/SKIP、mark-end span、EMIT 产出 `(symbol,start,end)`）；`validSymbols(language, parseState)` 镜像 C `ts_lexer_external_scan`（states[ext] ∩ has_actions）。
- **验证**：新增 `scanner/ScannerVMTest` 22 项（每个 opcode 可观察 focused 测试 + EOF/step-budget/越界/下溢 typed 失败）；全仓 `./mvnw -pl nop-treesitter -am test` BUILD SUCCESS **163 tests**（含 v3 blob 141 项回归）。
- Doc-sync: `blob-format.md` §22 为唯一 owner-doc 更新（模块内部）。

### 2026-09-08 计划 2026-09-08-0234-1-external-scanner-vm Phase 1 完成（Blob v3 + JS grammar vendoring + JS blob 端到端）

- **Decision 记录**：validation-grammar 重裁定——roadmap "Java grammar scanner bytecode" premise void（vendored tree-sitter-java EXTERNAL_TOKEN_COUNT 0、无 scanner.c）；tree-sitter-javascript 为 validation grammar（EXTERNAL_TOKEN_COUNT 8、scanner.c 364 行、corpus 6 文件 232 sections）。ABI 处理裁定：v15 JS grammar 原样 vendoring，extractor 扩展支持其新增（reserved words gated on abi ≥ 15），既有 v14 blob 不动。
- **Blob v3**（backward-incompatible）：header 96 字节新增 `external_token_count`/`external_lex_state_count`/`reserved_word_set_count`/`max_reserved_word_set_size`（u16）+ `scanner_program_length`（u32）；section 8 扩为 `(lex_state, external_lex_state, reserved_word_set_id)` 每 parse state 6 字节；新增 section 16-19（external symbol map / external states / reserved word sets / scanner program）；`blob-format.md` 重写为 v3（含 scanner ISA 全文）。
- **`ParserCExtractor` 扩展**：`external_lex_state`/`reserved_word_set_id` 不再丢弃；提取 `ts_external_scanner_symbol_map`、`ts_external_scanner_states`、`ts_reserved_words`（row 超 `MAX_RESERVED_WORD_SET_SIZE` fail-loud）、`ts_supertype_*` 表（v15 initializer 新增字段，覆盖检查不再报 unhandled）；`TSCharacterRange` 声明 regex 放宽 `static const`；覆盖检查新增 `abi_version`/`supertype_count`/`major_version` 等字段。
- **Vendoring**：`tree-sitter-javascript` parser.c（94KB，LANGUAGE_VERSION 15，1870 states, 261 symbols, EXTERNAL_TOKEN_COUNT 8）+ scanner.c + 6 corpus 文件 → `src/test/resources/upstream/grammars/tree-sitter-javascript/`；JS blob 395KB → `src/main/resources/grammars/javascript/`；JSON/Java blob 以 v3 重建。
- **验证**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **141 tests**（新增 `codegen/JsBlobTest` 7 项：external tables 与 parser.c statics 逐项一致、state 0/1/15 external_lex_state、reserved word sets 14 行、shipped JS blob == fresh CLI run byte-identical、JSON/Java v3 无 external scanner）；JSON corpus 7/7、Java corpus 108/108 在 v3 路径回归；v2 blob typed rejection（LanguageTest + BlobRoundTripTest 覆盖 v1/v2）。
- **接线**：`Ts2Java <parser.c> <blob>` 单 CLI 生成 shipped JS blob；`Language.fromClasspath` 解码 19 sections 并报 external token count 8。
- Doc-sync: `blob-format.md` v3 为唯一 owner-doc 更新（模块内部 contract）。

### 2026-09-08 计划 2026-09-07-2228-2-tree-cursor-tsnode closure audit 通过（roadmap item 6 收口）

- **审计结论**：31/31 检查项 [x]（Phase 1-3 全部执行项 + Exit Criteria）。ledger 结构修正：移除本计划遗留的 legacy `## Closure Gates` section（9 个未勾选项），与同组已收口的 4 个 ledger 计划（1713-1/2/3、2228-1）结构一致——ledger 的完成由 checkbox 全勾 + `## Verification` pass 行 + `## Closure` receipt 派生，无 Closure Gates。
- **验证套件（本 visit 重跑）**：`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **134 tests 0 failures 0 errors**（cursor 20 focused：CursorNavigationTest 11 + CursorFieldTest 7 + UpstreamCursorCrossCheckTest 2；Java corpus 108/108、JSON corpus 7/7 回归）；`test-compile`、`clean package -DskipTests` 绿；`checkstyle:check -Pqa` 0 violations（plain checkstyle 失败为 nop-api-core 全仓基线 9225 项，非本 diff）；`scan-hollow-implementations.mjs --module nop-treesitter --severity high` 0 findings。
- **端到端/接线/anti-hollow**：`TSParser.java:48` → `GLRParser.parse` → `TSTree.snapshot` 单一接线路径，`cursorWalksTheArenaSnapshotThatTheParserProduced` 断言 cursor 行走 parse 产出的同一 arena；`deepNavigationMatchesSExpressionSpine`/`preorderNamedSequenceEqualsSExpressionNamedNodes` + UpstreamCursorCrossCheckTest 108 fixtures 断言 cursor named 前序序列 == 期望 sexp named 节点；容器链透明展平（12 子节点 → 25 visible children）；字段解析走 blob v2 field tables + production-id（subtree state 槽），非手写 map。
- **无静默跳过**：out-of-range 运动返回 false 且位置稳定；未知字段名 → fieldId 0 / 返回 false；越界 fieldId → `TreeSitterException`（均有测试）。
- Doc-sync: roadmap item 6 `done`（closure-derived）、本 closure audit receipt 已写入 plan `## Closure`（#audit-20260908-0224-2026-09-07-2228-2-tree-cursor-tsnode-1-853d2ca4）、No owner-doc update required（模块内部，public API 文档为 roadmap item 14）。

### 2026-09-08 计划 2026-09-07-2228-2-tree-cursor-tsnode 执行完成（roadmap item 6，Cursor + TSNode + 字段导航）

- **改动**（`nop-treesitter`，plan `ai-dev/plans/nop-treesitter/2026-09-07-2228-2-tree-cursor-tsnode.md`，ledger 格式、3 个 Phase 全部执行完毕，closure audit 由下游独立步骤执行）：
  - 新增 `TSNode`（record，`(tree, id, aliasSymbol)`：`type()/named()/isVisible()/isExtra()/startByte()` + 导航委托 cursor）、`TSTreeCursor`（`cursor/` 包：gotoFirstChild/gotoNextSibling/gotoParent/gotoFirstNamedChild/gotoNextNamedChild/gotoChild/gotoNamedChild/gotoChildByFieldId/Name/currentFieldId/currentFieldName/childCount/namedChildCount/depth）、包私有 `TreeNavigator`（flattened 子序列展开容器链、visible/named 计数、alias/field 解析）。
  - `TSTree` 增加 `rootNode()`/`cursor()`；`Language` 增加 `fieldId(String)`。
  - 语义对齐 C runtime `tree_cursor.c`/`node.c`：visible 子级迭代（含匿名 token）、hidden 中间帧跨域 + sibling 上浮（`goto_sibling_internal` 弹栈冒泡）、extra（注释）在非过滤迭代可达、字段名穿越 hidden wrapper 向上解析、`gotoParent` 跳过 hidden 到最近可见祖先；cursor 构造时用 arena parent 链重建祖先帧（容器链透明），使任意节点起点的 cursor 可 gotoParent/gotoNextSibling。
- **验证**：新增 20 个 focused 测试（`cursor/CursorNavigationTest` 11 + `cursor/CursorFieldTest` 7 + `cursor/UpstreamCursorCrossCheckTest` 2），全仓 `./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS **134 tests 0 failures**（含 JSON 7/7、Java 108/108 回归）；`test-compile`、`clean package -DskipTests` 绿；`checkstyle:check -Pqa` 0 violations（plain checkstyle 失败仍为 nop-api-core 全仓基线 9225 项，非本 diff）；`scan-hollow-implementations.mjs --module nop-treesitter --severity high` 0 findings。
- **端到端/接线**：上游 Java corpus 108/108 fixtures 的 cursor-named 前序序列 == 期望 s-expression 的 named 节点序列（≥5 门槛远超）；cursor 行走 `TSParser.parse` 产出的同一 `TSTree`/arena 快照（无第二棵树表示）。
- **roadmap item 13 预留数据**：Java fixture cursor profile——nodes=127、maxDepth=10、cursorSteps=127（每步 1 帧 int 元组，无调优，per plan 要求仅记录）。
- **偏差**：plan 要求"15 个 focused 测试"，实际 20 个（覆盖所有要求领域且额外覆盖 out-of-range 稳定性、字段定向访问）；`gotoChildByFieldId/Name` 按 C `ts_node_child_by_field_id` 语义实现（含 inherited 尾调用）；Phase 3 的 roadmap 翻转按 plan 语义为 closure-derived，由下游 closure audit 收口。
- Doc-sync: No owner-doc update required（模块内部 API，public 文档为 roadmap item 14）；roadmap item 6 → done 由 closure audit 派生记录。

### 2026-09-08 计划 2026-09-07-2228-1-glr-graph-stack-java-corpus closure audit 通过（roadmap item 5 收口）

- **审计结论**：45/45 检查项 [x]（Phase 1-4 全部 Exit Criteria + Closure Gates 语义核验通过），本 visit 重跑验证套件全绿——`./mvnw -pl nop-treesitter -am test -T 1C` BUILD SUCCESS，114 tests 0 failures 0 errors（Java corpus **108/108 = 100%**，JSON corpus 7/7）；`test-compile`、`clean package -DskipTests` 绿；`checkstyle:check -Pqa` 0 violations（plain `checkstyle:check` 失败均为 nop-api-core 全仓基线 9225 项，非本 diff）；`scan-hollow-implementations.mjs --module nop-treesitter --severity high` 0 findings。
- **接线/anti-hollow 核验**：`TSParser.parse` 单一接线路径（`TSParser.java:48` → `GLRParser.parse`），JavaCorpusTest 与 JsonCorpusTest 共用同一路径，旧线性 `Parser` 已删除无第二实现；GLRParserTest 8 项（preferShift 选项 + `arenaGrowthStaysBoundedOnStressFixture` 有界断言 + 冲突组分叉/alias 重标/版本合并可观察测试）。
- **语义抽查**：vendored `parser.c` 常量（320 symbols / 1385 states / 40 fields / 1 alias）与 JavaBlobTest 解码一致；blob v2 `tree-sitter-java-blob.bin` classpath 可加载；keyword capture 走 `ts_lex_keywords` DFA。
- Doc-sync: roadmap item 5 `done`（closure-derived）、`blob-format.md` v2、No owner-doc update required（模块内部）。
