# 2026-08-26

### plan346 Phase 2 收口：nop-dao / xlang-java-truffle 两单元处置（主会话执行——前会话遗留未提交修改的核查、补全与收口）

- **Plan**: `ai-dev/plans/346-check2-audit-p0-p3-remediation.md` Phase 2。前一会话在这两个单元留下未提交的 main 代码修改（报告 0 标注、无测试），本次逐条对照报告复核既有修改、补齐 9 条缺失修复、补全测试与红验证、标注、收口。**Phase 2 全 8 单元 141/141 条处置完毕，Phase 2 → completed**。
- **nop-dao（18 条全修）**：
  - P1×2：JdbcBatcher `onSuccess` 区分 `EXECUTE_FAILED`（→0 行，乐观锁可发现失败）与 `SUCCESS_NO_INFO`（singleChange 保守 1，语义显式化）；`isSupportBatchUpdateCount()` 改读 `features.supportBatchUpdateCount`（激活死配置，DialectFeatures 字段已存在）。残留命令 4 个异常出口统一 `failRemainingCommands`（BatchUpdateException 部分确认后/prepare·addBatch 失败/外层事务设置失败/executeOne stopOnError 中断）。
  - P2×6：`jdbcSet` 空串 updateNull 与 `setJsonString` 均补 +1；SimpleDataSource setCatalog/setSchema 失败关连接；forceTxn 改"先 commit/rollback 后回调"原子语义；**runInTransactionAsync 回滚失败 suppressed——实现层面两轮红→修**：`ResolvedPromise.whenComplete` 对已失败 stage 的 action 异常只记日志不替换失败（audit 建议的 whenComplete 方案无效）→ `exceptionally` 会吞成功回滚后的原始异常 → **`handle` 是两路径都重抛原始异常的唯一正确挂点**，另补同步抛出防御 catch（ResolvedPromise 内联执行路径）。
  - P3×10：metrics 计时 finally 兜底（prepareStatement 移入 try）/rs 死代码/getTableMeta 转义/translator 构造期预编译——**复核推翻报告 Pattern.quote 建议**：duckdb.dialect.xml 的 errorCode 值 `.+_with_name_.+_does_not_exist.*` 本身即正则，字面量化破坏其匹配，保留正则语义仅消除重复编译（测试类 buildMessagePatterns 以编译红验证）/UnknownEntityException toString（复合主键用 List 保真；数组在 NopException.normalizeValue 层即被字符串化，既有行为）/SnowflakeSequenceGeneator→Generator 全量重命名（nop-sys-dao 引用，`-am` reactor 编译验证）/escapeSQLName 空串新错误码 ERR_DIALECT_INVALID_SQL_NAME/listeners CopyOnWriteArrayList/pagination limit 超 int 不设 maxRows/callFunc 读 OUT 参数（H2 函数调用 execute() 恒 ResultSet 形态，OUT 参数是唯一稳定载体——按 execute() 分流的初版被测试推翻）。
  - 新增 6 测试类（TestDialectWriteIndex/TestDialectSQLExceptionTranslator/TestUnknownEntityException/TestAbstractTransactionListeners/TestAbstractPaginationHandler + 扩展 TestSimpleDataSource）+ 既有 3 类扩展（TestJdbcBatcher +7 用例/TestJdbcTemplate +2/TestTransactionTemplateAsync +2）共 37 用例；stash 红 21 处（含 H2 命名内存库泄漏探针——"最后连接关闭即丢库"语义验证无泄漏连接）。nop-dao 132/0/0（45 skip 既有 docker-gated）。
- **xlang-java-truffle（10 条：8 修 + 1 文档方案 + 1 不修复）**：
  - P1×2：FrameLayoutMapper descriptor 声明恒 `FrameSlotKind.Object`（声明 kind=初始运行时 tag，恒 Object 后未初始化读取恒 null 与解释器三后端对齐；推断 kind 保留 SlotMeta 供 XSlotWriteNode typed 分派）；翻译失败关联改 **per-request 标志**（XLangLanguage.parse 抛错路径 `markTranslationFailed` 经 TranslatedEval 透传，后端仅凭标志判定第三分支降级，事件 map 降为细节载荷，协议异常关联整体删除——onTranslationFailure 放宽 package-private 作测试注入通道）。
  - P2×3：close() 加 synchronized 与 pool() 同锁（latch 协调 + 反射断言 pool 字段终为 null 的红验证）；Float NaN/±Infinity 字面量特判；SlotScan 补记 ForOf/ForIn/Try 运行时写入源。
  - P3：XBreak/XContinue INSTANCE 提前返回不设 section；wrapCallFuncException 首参改传 LOC 常量（2 个 golden fixture 经 GeneratedFixtureMain 再生，diff 即该变更）；XLangTruffleFunction scope 契约 javadoc；SyntheticSources 按路径共享网格 Source（pow2 几何增长，>1024 路径清空防泄漏；测试驱动修正：初版 (3,7)+(10,2) 断言共享实例因跨容量重建而误红）。XExprNode @Child 裁定不修复（报告自述 programmatic 节点有意取舍）。
  - 新增/扩展 5 测试类 12 用例；stash 红 8 处；truffle 590 + xlang-java 495 全绿。
- **验证**：`-pl nop-persistence/nop-dao,nop-kernel/nop-xlang-truffle,nop-kernel/nop-xlang-java` BUILD SUCCESS；`-pl nop-sys/nop-sys-dao -am` 编译通过（重命名跨模块验证）；check-doc-links --strict 0 errors。
- Doc-sync: No owner-doc update required（框架内部缺陷处置，无使用约定变化；SnowflakeSequenceGenerator 重命名的 runbook 提及已同步）。
