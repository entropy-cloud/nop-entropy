# 2026-09-22

### nop-lint item 20 Phase 3：真实 tsc e2e + demo L2 规则（plan `2026-09-22-0854-1-tsc-bridge`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0854-1-tsc-bridge.md`（work-item item-20；本段 = Phase 3 收口，三 Phase 全部落地，待独立 closure audit）。
- **真实 tsc e2e（Minimum Rules #22）**：`TestTscBridgeReal` 3 用例实跑于真实 Node v25.3.0 + typescript 5.9.3——临时 TS 工程（types.ts/usage.ts/assign.ts 跨文件引用）initProject → `getTypeAtLocation`（跨文件接口类型渲染含 "Money"）→ `isTypeAssignableTo` 双形态（expectedType 字符串 + NodeRef 跨文件，含 const 字面量 42→number 可赋、number→42 不可赋方向断言）→ tsconfig 变更（strict 翻转）触发 cacheKey 变化 + program 重建 → 工程外查询显式失败。环境契约裁定：Node/typescript 缺失 → `Assumptions.abort`（报告为 skipped，计数可见，永不静默绿）；present-but-broken 照常失败。
- **demo L2 套件**：`nop-lint/no-string-throw`（`requires: "L2"` + `typeOf: $ERR is string`，throw 字符串报、throw Error 不报）落 `l2-suites/` 独立根（默认套件发现不含 L2 套件，避免无 Node 环境误伤）；`RuleTestRunner` 增可选 `TypeResolver` 构造器 + 文件化夹具以真实路径 lint（`IFile`/`ClassPathResource` 双形态适配；VFS 目录资源为 InMemory 合成，经 rule.yml 子文件反查真实父目录）；`TestL2DemoSuite` 套件全绿 + FAST 档 skip 断言。
- **执行期修复 2 处**：(a) `RuleTestRunner.lint` 用 `rule.getLanguage()`（String）误配 4 参 `LintEngine.lint`（需 LintLanguage）→ 引擎补 languageId 版 4 参重载；(b) helper 位置查询对**关键字 token**（`new`）直查 `getTypeAtLocation` 得 error/any 型（assignability 对一切为真）→ `typeAtPosition` 上爬关键字到父表达式（NewExpression → Error 型），`getTypeAtLocation` 与节点引用路径共用；裁定回写 design 06 §5.3 增注。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` exit=0（592/592）；`./mvnw -pl nop-lint/nop-lint-js -am test -T 1C` exit=0（61/61，真实环境实跑无 gating abort）。
- Doc-sync: design 06 §5.3 查询语义增注；design 08 §2 矩阵核对一致（02 §3 无 tsc 行）；roadmap item 20 `todo` → `planned`（done 待 closure audit）；plan Phase 3 全勾 + Verification 记录。

### nop-lint item 20 Phase 2：L2 供给接入引擎（plan `2026-09-22-0854-1-tsc-bridge`）
### 全仓代码审查 roadmap 编制（full-code-review，无 plan——roadmap 编制任务）

- 产物：`ai-dev/backlog/full-code-review-roadmap.md`（v2）+ 轮次子目录 `ai-dev/audits/2026-09/2026-09-22-2020-code-review-full/`（矩阵 `00-scope-and-dimension-matrix.md` + `index.md`）+ `missions/full-code-review.json`。编写依据 `ai-dev/skills/audit-remediation-roadmap-authoring-prompt.md`。本次仅编制 roadmap，未执行审核与修复。
- 关键事实：9 个只读调研子 agent 覆盖全部顶层模块组（kernel/core-framework/persistence/service-framework/业务 13 模块/ai/stream+code+graph/rg+lint+集成/审计覆盖盘点）；绿色构建基线 `./mvnw clean install -DskipTests -T 1C` BUILD SUCCESS（19:56:36+08:00）。roadmap 结构：Wave 0 编排基线（编制期完成 4 项）→ Wave 1 审核 **47 个细粒度工作项**（MA1 结构 8 / MA2 模型服务 7 / MA3 运行时安全 11 / MA4 质量 5 / MA5 文档 3 / MA6 反模式 6 / MA7 增量巡检 7）→ Wave 2 修正（R1.0 展开器动态增补 R1.x + R2.0 裁决）→ Wave 3 整体审核（V.1–V.3 + G.1–G.3）。报告归档约定：Wave 1 每项一报告落轮次子目录，`index.md` 逐项登记 + P0 追踪 + P1 汇总 + P2 裁决台账。
- **独立子 agent 对抗审查（agent_cfa1bcb7-331c-4fae-97af-871727d2fe54，fresh session）**：引用真实性验证零缺陷（15 skill、3 tools、check/SUMMARY.md、各 backlog/audit 路径、代码证据行号全部命中）。结论「3 项必须先修 + 4 项建议」：F1（6.4 拆分为 6.4–6.6 三项：check P0×36 全复核 / P1×209 抽样 / P2P3 归档）、F2（1.2 拆为 xlang+core 两项；3.3 拆为 batch a/b 两项——S 级行为维度拆分硬规则）、F5（审查证据须实际落 daily log——即本条）、F3（依赖图 P0 通道箭头改指 V.1，消除与"不入批量修复"的矛盾）、F4（index 预置 P2 裁决台账节）、F6（状态词表与 mission-driver 解析器差异在横切关注点声明）、F7（V.1 改为先建预存失败清单再回归）。v2 已全部修复；MA1 7→8、MA3 10→11、MA6 4→6（共 47）。
- 验证：`node tools/mission-driver/src/mission-check.mjs missions/full-code-review.json .` → `valid: true`（exit 0）；`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（见下条收口记录）。
- 偏差：无。mission.json 为 roadmap guide 步骤 7 的配套产物（roadmap-check.mjs 为静默 stub，不作为校验依据，以 mission-check + 独立审查为准）。
- Doc-sync: No owner-doc update required（本任务为 ai-dev 过程记录新增，docs-for-ai 无约定变化；roadmap 顶部已注明任务边界——功能开发类 todo 不入本 roadmap）。

### nop-lint item 20 Phase 2：L2 供给接入引擎（plan `2026-09-22-0854-1-tsc-bridge`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0854-1-tsc-bridge.md`（work-item item-20；本段 = Phase 2 收口）。
- **Decision（L2 注入位）**：能力**上限声明 + 运行时就绪门**——`LintProfile.capabilities()` 改为档位上限（FAST=L1；STANDARD=L1+L2），引擎另需注入 `TypeResolver`（core 新 `semantic/` 接口：`isAvailable` 零副作用探测 / `initProject` / `isAssignableTo` / `typeNameAt`，设计 06 §5.3 契约面）且运行对象具名文件路径；上限内不就绪 → **降级出口**，上限外 → `skippedByProfile`。档位仍是运行参数，规则库共用契约不破。
- **交付**：core `semantic/` 三类（`TypeResolver`、`TypeResolutionException`、`TypeQuerySupport`——per-run 查询支撑：resolver + 文件路径 + 源字节，捕获型约束编译期绑定，per-match 上下文两字段裁定形态不动）+ `node/SourcePositions`（UTF-8 byte offset → 0-based line / 0-based UTF-16 col，多字节 BMP 记 1 单位、4 字节代理对记 2 单位，迭代扫描）；`LintStats` 新增 `rulesDegraded`/`degradedRuleIds`；`LintEngine` 三态门（RUN/SKIP/DEGRADE）+ `lint(..., filePath, ...)` 具名重载；`CompiledRule.compile` 三参重载把支撑传入 `Constraints.compile`；`TypeOf` 约束真实求值（无支撑运行保持 guarded fail，消息含 typeOf 操作数）；`RuleSetRunner` 捕 `TypeResolutionException` 求值期降级（计数 + warn + 无诊断）。js 侧 `TscTypeResolver`（惰性桥/默认工程绑定/桥异常 → `TypeResolutionException` 全量映射/`AutoCloseable` 终态）。
- **测试（Minimum Rules #23/#25）**：`TestL2TypeGate` 9 用例——STANDARD×可用 resolver 命中/不命中（resolver 查询计数 ==1，接线验证）、FAST skip（queries==0）、门控降级三型（resolver 缺失/探测失败/无文件路径）、求值期降级（注入 `TypeResolutionException`，executed==1 + degraded==1 + 诊断 0）、`TypeQuerySupport` 两不变量；`TestSourcePositions` 4 用例（ASCII/换行/BMP/代理对）；`TscTypeResolverTest` 6 用例（零副作用探测/显式+默认工程/透传/映射/close 终态）；契约更新 2 处——`TestLintEngine`（fast/standard 不再恒等：L1 规则一致、无供给 L2 规则 standard=degrade/fast=skip，逐档可观察出口）、`TestConstraintEngineEndToEnd.typeOfRuleLoadsThenDegradesWithoutL2InStandard`（STANDARD 无 resolver → degraded + FAST → skipped 双腿）。
- 偏差：执行中发现并修复 1 处自伤——重构 `lint` 时误删 `lint(rules, language, source)` 三参入口（既有测试编译失败），补回（未具名源语义：L2 规则降级）。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS（592/592）；`./mvnw -pl nop-lint/nop-lint-js -am test -T 1C` BUILD SUCCESS（56/56）。注意：js 单模块（无 `-am`）会从本地 m2 取旧 core 构件致 `NoClassDefFoundError`，两模块联跑须带 `-am`（plan 命令即此形态）。
- Doc-sync: design 11 §2（L2 注入位裁定增注）+ §5（降级落地口径行）；plan Phase 2 全勾、Status: completed。

### nop-lint roadmap item 22 翻牌 done + item 20 Phase 1（tsc bridge Node 侧）（plan `2026-09-22-0854-1-tsc-bridge`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0854-1-tsc-bridge.md`（work-item item-20；ledger 格式；本段 = Phase 1 收口 + roadmap 事务）。
- **roadmap 事务**：item 22（constraint evaluator）closure audit 已于 commit f36f882a77 通过（收据 #audit-2026-09-22-162129-...-621006f7），roadmap 行 `planned` → `done`（commit 1f00b7183a）；item 25（deps: 22）解锁，M4 `todo` 未受扰动。
- **Phase 1 交付（前会话 WIP 续完 + 修复）**：`nop-lint-js` 新增 `tsc/` 包——`NodeTscBridge`（惰性 spawn + 握手 + 请求-响应复用 + 每查询 deadline + 崩溃有限重启 + 终态不可用）、`TscProtocol`（newline-delimited JSON 帧 + ready 校验 + 结构化错误帧）、`TscBridgeConfig`（依赖落位 Decision：`ai-dev/tools` devDependency `typescript` ^5.9.2 + 共享 helper 脚本；解析链 property→env→cwd 上溯）、`TscQueryException`/`TscBridgeUnavailableException`；Node 侧 `ai-dev/tools/tsc-bridge/tsc-bridge-server.mjs`（program 缓存键 = tsconfig sha1，覆盖 files/include/exclude/references/paths + 目标选项签名；per-program 查询缓存；isTypeAssignableTo 双形态：expectedType 字符串经 query-program 同 checker 归一、from+to 节点引用同工程校验）。测试：`NodeTscBridgeTest` 9 用例 + `TscProtocolTest` 8 用例，`FakeTscPeer` 以当前 JVM 复Spawn 扮演 peer（零 Node 依赖）。
- **执行修复（前会话 WIP 中断态）**：(a) `TscProtocol` 原用 Jackson 直接依赖（classpath 无）→ 改平台 `JsonTool`；(b) `NodeTscBridge` 残留未定义 `resultOf(request(...))` 调用 → 请求方法本身返回 result，直接消费；(c) 重启预算原在 spawn 时重置 → 耗尽态不可达（违反"不无限重试"）→ 移至构造器一次性初始化；(d) 握手失败（缺 typescript/非 ready 帧）原不落终态 → 改为 EXHAUSTED（重试无意义，fail-visible）；(e) `isEnvironmentUsable` 原把命令参数按可执行检查（classpath 字符串绝对路径恒 false）→ 只查 spawn 首元素；(f) `FakeTscPeer` 嵌套帧 Java `toString` 非法 JSON → 递归编码；once 模式（hang/crash）原在 initProject 即触发 → 改 initProject 直通、仅查询触发，marker 文件区分首次/重启后行为（`hang-once-then-ok`/`crash-once-then-ok` 补齐 marker 逻辑，`garbage-response` 测试对 initProject 断言）；(g) `JsBindingDiscoveryTest` 过时假设（item 21 给 core 注册 `XmlLanguage` 后出现在 js 测试发现列表，TS 源码断言误伤）→ parse 断言收窄到本模块两个绑定。
- 验证：`./mvnw -pl nop-lint/nop-lint-js -am test -T 1C` BUILD SUCCESS（js 50/50，含 tsc 17 用例；core 同 reactor 全绿）。
- Doc-sync: design 06 §5.3 落地增注（依赖落位 + 协议形态 + 进程语义）；plan Phase 1 全勾、Status: completed。

### nop-lint roadmap item 22 Phase 2/3：约束求值器 + 管线接入 + fixtures + 收口（plan `2026-09-22-1045-3-constraint-evaluator`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-1045-3-constraint-evaluator.md`（work-item item-22；ledger 格式，Phase 1 已于本日早前收口，本次执行 Phase 2/3；closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **求值器落地**：`constraint/` 包三件——`Constraint`（`boolean holds(ConstraintContext)`，纯过滤器语义，design 01 §5 契约的落地形态）、`ConstraintContext`（match 节点 + `MetaVarEnv`，`captureText` 经门面切片）、`Constraints.compile`（parsed model → 可执行谓词；sameText/differentText 原文逐字比较不 trim、regex 全匹配、inList 精确成员、notExists per-match 子树（含匹配节点）无内层 pattern 命中、withinDepth 子树深度 ≤ max（最长边数、自身 0，迭代计算防栈溢出）、typeOf = guarded fail（`requires: "L2"` 门 + profile 跳过使求值不可达，不可达被打破即响亮失败——绝不 L1 冒充）。notExists 内层 pattern 编译 fail-closed：`NopLintException` 与后端解析失败（如病理文本的 `TreeSitterException`）统一包装为含规则 id + "invalid inner pattern" 的拒绝（执行期修复，见偏差）。
- **编译期 capture 校验**：`SourcePattern` 新增 `captureNames()`/`multiCaptureNames()`（`$VAR`/`$$VAR` 单节点捕获集、`$$$VAR` 序列捕获集、`$_` drop 名排除）；`CompiledRule` 编译路径新增 `CaptureIndex`（规则全部 pattern 的捕获并集）+ `finish` 单一出口（constrained 规则必经 capture 校验）；引用未声明名 → 抛 `NopLintException`（含规则 id + 约束名 + capture 名），引用 `$$$` 序列捕获 → 显式 "sequence capture" 理由；XML 绑定路径带 constraints → 编译期拒绝（约束依赖 tree-sitter capture 机制，不做 unenforced filter）。
- **管线接入**：`CompiledRule.constraints()` 暴露编译约束；`RuleSetRunner.run` 在匹配后、xscript 前 per-match `applyConstraints`（全成立才保留；被滤 match 计 `LintStats.constraintFilteredMatches` 新口径——"不静默"纪律与 `rulesKindFiltered`/`skippedByProfile` 同形；滤空短路 `continue`；求值异常向上传播不吞）。`LintStats` + `getConstraintFilteredMatches`/`incConstraintFilteredMatches` + toString。管线序核对结论回写 design 03 §1.1 增注（图示 `Constraint.evaluate` 位与 live 一致；约束滤后 match 绝不进 xscript 由测试断言 `xscriptMatchesExecuted=0` 钉死）。
- **fixtures（RuleTester demo 套件形态）**：`suites/constraints/` 7 套——same-text-demo/different-text-demo/regex-capture-demo/in-list-demo/not-exists-demo/within-depth-demo 各 1 valid + ≥2 invalid + `.expect`，typeof-pending-l2（`requires: "L2"`，valid clean 文件，加载成功 + 档位跳过语义，计数断言在 e2e 测试）；规则资源 `valid-constraints.rule.yml`（7 kind 全 round-trip，csv-set 双形态）+ 2 invalid fail-closed fixture。套件经 `TestRuleSuites` 动态发现真实管线全绿。
- **测试（Minimum Rules #25）**：新 `TestConstraints` 6 用例（capture 提取钉死、未声明/序列捕获拒绝含 rule id + 约束名 + capture 名、notExists 内层 pattern fail-closed 编译 + 子树作用域求值、typeOf guarded fail、经真实 parser 路径构造）；新 `TestConstraintEngineEndToEnd` 9 用例——**接线验证（#23）**：`constraintFlipChangesOutcomeAndCounter`（同 match 无约束命中 / 加约束不命中 / 约束成立放行的差分 + `constraintFilteredMatches` 计数断言）、`constraintFilterRunsBeforeXscript`（约束滤后 `xscriptMatchesExecuted=0` vs 成立放行 `=1`——管线序见证）；**每约束引擎级命中/不命中矩阵** differentText/regex/inList/notExists/withinDepth 各 1 用例 + compile 期两类拒绝 + typeOf `skippedByProfile` 全套断言（加载成功 + skipped id + executed=0）；`TestConstraintParsing` 36 用例（Phase 1）；`TestRuleTestRunner.allDiscoveredSuitesAreGreen` 更新 3 → 10 套件（3 demo + 7 constraints，新增 constraints 套件发现断言——资源套件增量触发的既有断言维护，非行为变更）。
- **收口（Phase 3）**：owner-doc 核对——design 01 §3.2/§3.3/§5、design 10 §2/§5 增注与 live 一致（三项 Decision 落点齐全），design 03 §1.1 本轮补管线序核对结论增注 + 图示 `// Phase 2` 标记更新为已交付；roadmap item 22 `todo` → `planned`（行内注明 done 待 closure audit，item 21 先例同形）；item 25（deps: 22）解锁注记与 M4 `todo` 未受扰动（19–29 未全 done，M4 不翻转）。
- 偏差：(a) 执行期修复 1——`Constraints.compileInnerPattern` 原只捕 `NopLintException`，病理 pattern 文本触发后端 `TreeSitterException` 逃逸（`notExistsInnerPatternCompilesFailClosed` 红）→ 改捕 `RuntimeException` 统一包装（仍 fail-closed 不吞，消息含规则 id）；(b) 测试维护 2——`TestConstraintEngineEndToEnd.constraintFilterRunsBeforeXscript` 的 `xscript` 原误置于 `rule:` 容器内被 parser 静默忽略（design 01 §2 schema：xscript/constraints 均顶层字段）→ 移顶层；`TestRuleTestRunner` 套件计数 3 → 10。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0；`./mvnw -pl nop-lint/nop-lint-core -am clean package -DskipTests -T 1C` exit=0；`./mvnw -pl nop-lint/nop-lint-core -am checkstyle:check` 走 mission 既有 `|| echo 'lint not configured'` 裁定路径（nop-api-core 9226 处全仓遗留基线，与既往 plan 一致）；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS（修复后全绿，含 7 个 constraints fixture 套件经 `TestRuleSuites` 动态全绿）；plan Phase 2/3 checklist 全勾（frontmatter `status: active` 保持，ledger 派生状态，不手写 `completed`）；`## Verification`/`## Closure` 留待下游 BUILD_VERIFY/CLOSURE_AUDIT 步骤填写。

### nop-lint roadmap item 22 Phase 1：约束语义裁定 + xdef/模型/解析器扩展（plan `2026-09-22-1045-3-constraint-evaluator`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-1045-3-constraint-evaluator.md`（work-item item-22；ledger 格式，执行记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **三项 Decision 裁定并回写 design 01 §3.2/§3.3 + design 10 §2/§5**：(a) **约束极性** = per-match 谓词过滤器（全称合取：全部约束成立才产诊断；不成立 → match 过滤 + `constraintFilteredMatches` 显式计数）；withinDepth 共享统一极性（成立 = 匹配节点子树深度 ≤ max，深度 = 匹配节点到后代最长边数、自身为 0），深嵌套检测在本极性下不可用 withinDepth 表达（design 01 §3.3 示例已修正为浅层守卫用例并移除 message 字段——withinDepth 表面仅 max）；(b) **notExists 作用域** = per-match 子树（匹配节点为根、含匹配节点本身），可选 `message` 为 v1 保留字段（过滤器极性下不被求值消费，仅随模型存储）；(c) **typeOf × L2 门** = 使用 typeOf 的规则必须声明 `requires: "L2"`，缺声明由 RuleDslParser 加载期 fail-closed 拒绝（报错含规则 id）；带声明规则经 profile 门走 `skippedByProfile`（v1 全档无 L2），绝不以 L1 冒充。
- **xdef/模型/解析器**：`lint-rule.xdef` 新增顶层 `constraints`（body-type=list，元素 `<constraint>` 对象，子标签 = 约束名 camelCase：sameText/differentText（captures csv-set 属性，YAML 列表与 CSV 字符串双形态实测）/regex（capture+pattern）/inList（capture+values csv-set）/typeOf（capture+is）/notExists（pattern+可选 message）/withinDepth（max int））；`RuleDslModel` 新增 `getConstraints()` + `Constraint` 载体（kind + 各形态字段位，capture 引用归一化为裸名）；`RuleDslParser` fail-closed 校验矩阵：未知约束名、单元素多约束键、必填段缺失（regex pattern、inList values、typeOf is、notExists pattern、withinDepth max）、sameText/differentText captures <2、inList values 空/含空项、withinDepth 非整数/负值（max=0 合法 = 匹配节点须为叶）、capture 引用形态（`$NAME`/`NAME` → `[A-Z_][A-Z_0-9]*`）、regex 语法编译期验证、typeOf 无 L2 声明——全部加载期抛 `NopLintException`（消息含规则 id）。发现：xdef 结构层（平台 unknown-prop 校验）先于 parser 拒绝未知约束键，双重 fail-closed。
- **测试（Minimum Rules #25）**：新 `TestConstraintParsing` 36 用例——7 约束合法 round-trip（含字段归一化、保留 message、constraints 空列表合法、7 kind 同列序保真）+ 非法形态矩阵全格断言 + e2e 3 用例（`valid-constraints.rule.yml` 经 xdef 管线 7 kind 全 round-trip，含 csv-set 双形态；两 invalid fixture fail-closed）。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS（520 → 556 tests，0 failures）。
- Doc-sync: design 01 §3.2（约束语义裁定增注 + notExists/极性/typeOf 门）、§3.3（withinDepth 极性 + 示例修正）、§5（约束求值契约落地增注，预写 Phase 2 编译期 capture 校验契约）；design 10 §2（constraints 落地形态增注）、§5（一致性落地增注）；plan Phase 1 全勾（frontmatter `status: active` 保持，ledger 派生状态）。

### nop-lint roadmap item 21 三 Phase 执行完成（plan `2026-09-22-1045-2-xnode-pattern-engine`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-1045-2-xnode-pattern-engine.md`（work-item item-21；ledger 格式，执行记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **Phase 1 交付（XNode 匹配内核）**：nop-lint-core 新增 `xml/` 包 7 类——`XmlTagKinds`（tag 名内联稠密 kind id，既有 kind 位图过滤零特判复用）、`XNodeLintNode`（XNode 只读门面：元素/`#text` trivia/`#comment` extra trivia 三形态，XNode location 语义经实验钉死——location 指向首个 tag-name 字符、col 1-based，元素 range = start-tag span，quote-aware 扫描支持属性值含 `>`）、`XmlSourceParser`（XNodeParser 只读消费 + (line,col)→UTF-8 byte offset 映射，多字节用例钉死）、`XNodePattern`/`XNodePatternCompiler`/`XNodePatternMatcher`/`XmlValueNode`。**三项 Decision 落地**：接线 = `LintLanguage.compileRule` 钩子（default null = tree-sitter 矩阵）+ `CompiledRule.precompiled` 工厂，下游管线零分支；pattern 编译 = 共享 `MetaVarSyntax`/`MetaVarEnv`（同名一致性/`$$$` 语义两路径同源），标量位 `$$$VAR`/`$$`/裸 `$` fail-closed；relation 算子 = `RelationalMatcher`/`StopBy` 经门面原样复用（neighbor/end），`field`/`context+selector`/`stopBy=rule` XML 路径编译期拒绝。测试矩阵 `XNodePatternMatcherTest` 47 用例 + `XmlSourceParserTest` 11 用例（含 body 内 PI 解析期 fail-closed、CDATA 按文本、混合内容 pattern 拒绝）。
- **Phase 2 交付（引擎/RuleTester/CLI 接线）**：`XmlLanguage`（id `xml`，ServiceLoader 注册于 core main resources；`treeSitter()` 契约增注允许 null、`parseIncremental` fail-closed、`preprocessPattern` identity）+ `XmlRuleCompiler`（编译矩阵镜像：pattern/kind/any/all/not/关系 + 深度守卫，regex/field/context/stopBy=rule 拒绝，xscript 同管线放行）；`TargetScanner` 表追加 `xml→xml`、`xbiz→xml`（后者覆盖平台 XML 族模型 `.xbiz`，design 03 §2.4 增注同步）。**Decision（XML 抑制语义）= 落地非 gap**：XNode 注释经门面 `#comment` trivia child 进入既有 `CommentSuppressionScanner`（零 XML 特判，六指令形态/配对/unused/unpaired 全生效），`-->` 终止符与 `*/` 同位剥离（core `rawArguments` 一处扩展）；注解 provider 恒 null。测试：`TestXmlRuleEngineEndToEnd` 10 用例（e2e 诊断行号 + kind 过滤互斥计数 `executed=1/kindFiltered=0` vs `executed=0/kindFiltered=1` 接线见证 + 抑制生效/suppressedDiagnostics 计数/unused-directive 元诊断 + 三类编译拒绝 + 增量拒绝）+ `TestXmlCliWiring` 2 用例（TargetScanner→CheckRunner→LintEngine 全链，ServiceLoader 发现无 stub）+ core 套件 `suites/xml/demo-xml-driver` 全绿。item 19 的 `no-extension-language` 占位 fixture 按其自述前提翻转：更名 `no-fixtures-for-language` 并更新两处断言（XML 现有表项，无夹具仍 fail-closed）。
- **Phase 3 交付（ORM/xbiz 规则 + dogfood）**：**Decision（fixture 归属）= 生产规则**——`nop-lint-nop` 主资源 `rules/nop/nop-orm-mandatory-default.rule.yml`（all+not：mandatory 列减带 defaultValue 同形态，属性开放世界适配真实 orm 列 domain 可缺席）与 `rules/nop/nop-xbiz-auth-not-sole-guard.rule.yml`（faithful 形态裁定：§3.5 示例的 `<action>` 标签在真实 xbiz 不存在——实际为 query/mutation/subscription/action 四 typed 形态且 v1 拒绝 any 嵌套 all，因 xdef 中 `<auth>` 仅为 action 级声明，取 `all: [pattern: <auth>$$$</auth>, inside: <actions>$$$</actions>]` 外延等价，裁定入 design 01 §3.5 增注 + 规则头注）；两套件 valid 1 + invalid 2 + `.expect` 经 `TestNopRuleSuites` 14/14 全绿（12 套件，x:extends 真实管线）。**dogfood（有界，真实 CLI main 跑通）**：`nop-auth/model/nop-auth.orm.xml` → 200 warning（orm 规则真阳性：mandatory 列无 defaultValue；整改/baseline 归 item 27 与平台侧，非 lint 缺陷）；`_dump` 生成 xbiz 两份 → 2 warning（auth 声明命中，_dump 为生成物 = by-design 豁免候选）；两 run 计数口径一致（executed/kindFiltered/skipped 分桶），退出码 0（warning 级）。
- **owner-doc 收口**：design 01 §3.5（XNode 引擎落地增注：编译形态/维度口径/relation 语义/命名空间严格度执行口径/xbiz faithful 形态裁定）、design 03 §1.1（XML 接线裁定增注）+ §2.4（XML 家族扩展名增注）、design 04 §5（XNode 路径对应关系增注）、design 09 §2（XML 抑制落地裁定增注）、design 02 §1（XNode 规则落地增注，与 item 29 批量区分）；roadmap item 21 `todo` → `planned`（done 待 closure audit，successor trigger 注记行内），item 20/22/23/24/28 注记与 M4 `todo` 未受扰动，`Last updated` 保持 2026-09-22。
- 偏差：(a) 计划内未预期——dogfood 需 `.xbiz` 可 lint，扩展名表追加 `xbiz→xml`（一行 + 断言，回写 design 03 §2.4）；(b) `CommentSuppressionScanner.rawArguments` 增加 `-->` 终止符剥离（抑制语义落地的前置，design 09 §2 增注记录）；(c) item 19 遗留占位 fixture 的前提翻转导致其测试更新（fixture 自述的 "until item 21" 条件已达成）。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **520 tests / 0 failures**（449 → 520：+58 xml 内核/e2e、+2 CLI、-2 重命名更新）；`./mvnw -pl nop-lint/nop-lint-nop -am test -T 1C` BUILD SUCCESS **15 tests**（TestNopRuleSuites 14/14）；`./mvnw -pl nop-lint/nop-lint-core -am test-compile/-am clean package -DskipTests -T 1C` 均 exit=0；mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 既有裁定路径（nop-api-core 9226 处全仓遗留基线，与既往 plan 一致）；`node ai-dev/tools/check-doc-links.mjs --strict` exit=0（0 errors）；`scan-hollow-implementations.mjs --module nop-lint-core/-nop --severity high` 双双 exit=0（0 发现）。plan 三 Phase checklist 全勾 + 各 Phase `Status: completed`；frontmatter `status: active` 保持（ledger 派生状态，不手写 `completed`）；`## Verification`/`## Closure` 与 Closure Gates 留待下游 BUILD_VERIFY/CLOSURE_AUDIT 步骤填写。


### nop-lint roadmap item 19 closure audit 通过，item 置 `done`（plan `2026-09-22-1045-1-ts-tsx-language-adaptation` 收口）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-1045-1-ts-tsx-language-adaptation.md`。独立 closure visit（非执行 session 自审）：CLOSURE_SCRIPT_CHECK 报 3 项（`ledger-structure-invalid` + `missing-pass:test` + `no-audit-receipt`），本 visit 全部消解——`## Closure Gates` 11 个 checkbox 位于 ledger 计数域外（01-file-ledger §2.5），按 §4.3 消解先例改为纯 prose 关闭条件记录（同 1420-1/2137-1/2137-2/2137-3/0128-1/0544-1/0544-2 处置），计数域回到 27/27 勾选、0 unchecked；`## Verification` 补 `- pass test 20260922-1152 exit=0`；`## Closure` 补 dispatch/accepted 收口对（#audit-20260922-1152-2026-09-22-1045-1-ts-tsx-language-adaptation-1-ec33f00c）；`node tools/mission-driver/src/plan-check.mjs --strict` exit=0（27 checked / 0 unchecked）。
- 语义复核（对照 live repo）：三 Phase Exit Criteria 逐条核对通过——Phase 1 双绑定（shared adapter + public no-arg ctor + `META-INF/services`）经 `JsBindingDiscoveryTest` 发现断言，expando identity 由两 grammar 解析级断言钉死（`TypeScriptLanguageTest`/`TsxLanguageTest.metaVarTokensParseAsSingleIdentifiers`），suppression null 契约 + 双注释形态 span 断言（`TsSuppressionContractTest`）；Phase 2 扩展名表静态 map 于 `TargetScanner`（双向查询 API、`.mts`/`.cts` 出表、skipped 分桶显式计数、`RuleTestRunner` 夹具扩展名同表反查 fail-closed）+ 端到端/接线验证（`TestJsRuleSuites` pattern/kind 两套件全链 + `TestJsCliWiring` `.ts`/`.tsx` fixture 经 TargetScanner → CheckRunner 全链，TS 绑定被 CLI 运行时真实消费）+ `TsxSpecificsTest` 双 grammar 降级路径钉死；Phase 3 owner-doc 增注（design 01 §1/§4、03 §2.4、05 §6）与 live 一致。Anti-Hollow：`scan-hollow-implementations.mjs --module nop-lint-js/nop-lint-core --severity high` 双双 exit=0（0 发现）。
- 本 visit 实跑验证套件全绿（full-green）：`./mvnw -pl nop-lint/nop-lint-js -am test-compile/-am clean package -DskipTests/-am test -T 1C` 均 exit=0（core 449 + js 33 tests / 0 failures）；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **449 tests / 0 failures**；mission `commands.lint` checkstyle 走 `|| echo 'lint not configured'` 既有裁定路径（nop-api-core 9226 处全仓遗留基线，与既往 plan 一致）。
- 收口翻转：roadmap item 19 `planned` → `done`（successor trigger "closure audit pass" 于本 visit 触发）；周边 item 20/26 依赖注记与 M4 `todo`（派生，19–29 未全 done）均未受扰动。plan frontmatter `status: active` 保持（ledger 派生完成态，不手写 `completed`）。无 in-scope live defect / contract drift 被降级。

### nop-auth plan 2275 执行：限流与登录失败计数的 DB 集群后端

- **产出**：design §3.1/§3.2/§3.5 db 增补 + §四.4 原拒绝项经用户裁定推翻回写；新 plan 2275（一轮对抗性审查：1 Blocker + 5 Major 全部按处方修订——codegen 生命周期、间隔门过期行复活语义、日计数键过期回退语义、TTL 固定窗口钉定、codegen 生成物清单）。
- **Phase 1**：`nop-auth/model/nop-auth.orm.xml` 新增 `NopAuthRateLimitCounter`（表 nop_auth_rate_limit_counter：counterKey PK(150)/counterCount/expireAt）与 `NopAuthLoginAttempt`（nop_auth_login_attempt：attemptKey PK(150)/failCount/expireAt），codegen 生成实体族；三库增量 DDL（_add_rate_limit_and_login_attempt_nop-auth.sql）+ 三库 _create/_drop 全量同步。
- **Phase 2**：`DbSendCodeRateLimiter`（日计数键条件 UPDATE 原子递增 + 过期行删重插；间隔门恒 SETNX + 过期复活——审查 M3/M4 两类键 dup-key 分支语义相反已分别钉定）+ `DbLoginAttemptStore`（incrementLoginFailCount 条件 UPDATE，TTL 注入 UserContextConfig 固定窗口）+ beans `*_db` 注册（store-type 扩为 local|db|redis）。
- **测试**：手工 H2 栈形态（Db bean autowire-candidate=false 不走容器——审查 m1 修正）；TestDbSendCodeRateLimiter 5 用例（顺序间隔拒绝/间隔门过期复活/scope 隔离/16 线程并发 IP 计数无丢失更新/过期惰性重置）+ TestDbLoginAttemptStore 4 用例（16 线程并发递增恰为 N/键维度隔离/过期读 0 新窗口/reset）。**457 tests 0 failures 0 errors**。
- **环境坑**：共享 .m2-repo 被并行会话 install 踩踏导致 nop-core NoClassDefFound 反复失败——以 APFS clonefile 播种私有仓库 `.m2-repo-2275` + `mvnq --no-lock -- -Dmaven.repo.local=...` 隔离解决（显式 -D 优先于 mvnq 注入）。
- Doc-sync: owner doc 集群矩阵与配置项已更新为 `redis` 或 `db`。

# 2026-09-22

### nop-lint roadmap item 19 Phase 3：roadmap 回写与 owner-doc 收口（plan `2026-09-22-1045-1-ts-tsx-language-adaptation`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-1045-1-ts-tsx-language-adaptation.md`（work-item item-19；本条目为执行收口记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **roadmap 回写**：item 19 `todo` → `planned`（draft review 已过 + 三 Phase 执行绿；`done` 待 closure audit 通过后由 CLOSURE_AUDIT 步骤翻转，successor trigger 注记在 roadmap 行内）；周边核对：item 20（deps: 19）/item 26 依赖注记未受扰动，M4 保持 `todo`（派生，19–29 未全 done，本 item 单项完成不翻转），roadmap `Last updated` 同步 2026-09-22。
- **owner-doc 收口核对（与 live 一致性逐点）**：design 01 §1（nop-lint-js 模块就位增注）、§4（TS/TSX expando identity 裁定增注，解析级证据指针）、design 03 §2.4（扩展名表落地形态增注——表归属 TargetScanner、`.mts`/`.cts` 出表裁定、RuleTester 夹具扩展名同源反查）、design 05 §6（TSX 语法行 🔧 Phase 2 → ✅ 语法级已落地，React/JSX 组件分析仍归 Phase 3）——expando 与扩展名表两项 Decision 回写落点齐全，无未回写 Decision。
- **验证汇总（收口态）**：`./mvnw -pl nop-lint/nop-lint-js -am test -T 1C` BUILD SUCCESS（core 449 + js 33，0 failures）；`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0；`clean package -DskipTests -T 1C` BUILD SUCCESS；**下游模块核对** `./mvnw -pl nop-lint/nop-lint-nop -am test -T 1C` BUILD SUCCESS（13 tests，TestNopRuleSuites 12/12——core 侧 RuleTestRunner/TargetScanner 改动对 nop-lint-nop 零回归）；mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 既有裁定路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-*/0128-*/0544-1/0544-2 一致）；`node ai-dev/tools/check-doc-links.mjs --strict` exit=0；`scan-hollow-implementations.mjs --module nop-lint-js/nop-lint-core --severity high` 双双 exit=0。
- plan Phase 3 checklist 全勾 + Status: completed；frontmatter `status: active` 保持（ledger 派生状态，不手写 `completed`）；`## Verification`/`## Closure` 与 Closure Gates 留待下游 BUILD_VERIFY/CLOSURE_AUDIT 步骤填写。

### nop-lint roadmap item 19 Phase 2：CLI 扩展名表 + 端到端规则验证落地（plan `2026-09-22-1045-1-ts-tsx-language-adaptation`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-1045-1-ts-tsx-language-adaptation.md`（work-item item-19；本条目为执行记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **Decision（扩展名表形态，回写 design 03 §2.4 增注）**：`TargetScanner` 从 `extension == language id` 约定升级为显式扩展名→语言 id 表（`java→java`、`ts→typescript`、`tsx→tsx`，小写查表）；**表归属 = 静态 map 于 TargetScanner**（扩展名绑定是 CLI 扫描关注点，`LanguageRegistry` 保持纯 id→绑定解析器），双向查询 API（`languageIdForExtension`/`extensionsForLanguage`）保证两消费方向不漂移；lintable = 扩展名命中表 **且** 映射 id 已注册（表内语言未注册 → skipped 分桶显式计数，纪律同 v1）；**`.mts`/`.cts` v1 裁定不入表**（避免与 `.ts` 主表语义分叉，skipped 分桶显式计数）。
- **附带 core 改动（执行期记录，同 plan Phase 2 Targets 的 core 授权面）**：`RuleTestRunner` 夹具扩展名从硬编码 `*.java` 改为按规则语言经 `TargetScanner.extensionsForLanguage` 反查（java→`*.java`、typescript→`*.ts`、tsx→`*.tsx`）；规则语言无表项（如 xdef 枚举内的 XML，item 21 前无夹具路径）= 套件显式失败（fail-closed，新 broken-suite 夹具 `no-extension-language` + 测试钉死）；既有的 stray-file / missing-expect 等失败路径测试全绿证明零回归。
- **CLI 接线验证（Minimum Rules #23）**：nop-lint-js `TestJsCliWiring`——registry 由 `LanguageRegistry.discoverDefaults()` 装载（ServiceLoader 真实发现，无 stub），`.ts`/`.tsx` fixture 经 TargetScanner → CheckRunner → LintEngine 全链：`.ts` 分类 `typescript`/`.tsx` 分类 `tsx`、两文件各产 1 条诊断（`demo/ts-no-console` 驱动 .ts、`demo/tsx-no-console` 驱动 .tsx——规则按精确 language id 分组语义同时钉死）、`md`/`mts` skipped 分桶各 1 计数断言。
- **端到端验证（Minimum Rules #22）**：nop-lint-js `TestJsRuleSuites`（RuleTester launcher，registry = discoverDefaults）——`demo/ts-no-console`（pattern `console.log($$$ARGS)`，1 valid + 2 invalid）与 `demo/ts-no-enum`（kind `enum_declaration`，1 valid + 1 invalid）两套件 `.rule.yml`（language: TypeScript，大小写不敏感解析链）→ VFS + xdef 管线加载 → RuleDslParser → LintEngine（kind 过滤 → matcher → suppression 尾）→ `.expect` 断言全绿。
- **TSX 专项钉死**：`TsxSpecificsTest` 3 用例——tsx grammar 解析 JSX 零 ERROR（`jsx_element`/`jsx_opening_element`/`jsx_closing_element`/`jsx_attribute`/`jsx_text` 全命中）；typescript grammar 对同源**实测**降级路径 = ERROR 节点出现 + `jsx_element` kindId=-1（两绑定 kind 表互不借用）；扩展名表双向一致性测试（`TestTargetScanner.extensionTableIsConsistentInBothDirections` 等 5 新用例，含大写 `.TS` 不敏感、未注册语言 skipped 分桶、`.mts`/`.cts` 出表）。
- 验证：`./mvnw -pl nop-lint/nop-lint-js -am test -T 1C` BUILD SUCCESS **33 tests / 0 failures**；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **449 tests / 0 failures**（443 → 449：TargetScanner 表 +5、RuleTestRunner 无扩展名语言 fail-closed +1）。
- Doc-sync: design 03 §2.4 扩展名表落地形态增注；plan Phase 2 全勾 + Status: completed（frontmatter `status: active` 保持，ledger 派生状态）。

### nop-lint roadmap item 19 Phase 1：nop-lint-js 模块 + TS/TSX 双语言绑定落地（plan `2026-09-22-1045-1-ts-tsx-language-adaptation`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-1045-1-ts-tsx-language-adaptation.md`（work-item item-19；ledger 格式，执行记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **模块交付**：`nop-lint/nop-lint-js/` 新模块（父 pom `<modules>` 注册 + pom 依赖 nop-lint-core + JUnit 5，口径照抄 nop-lint-java 先例）；`TypeScriptLanguage`（id `typescript`）/`TsxLanguage`（id `tsx`）两绑定经 `TreeSitterLanguageAdapter` 驱动已出货 blob（shared static adapter + public no-arg ctor + `get()` 单例，结构同 `JavaLanguage`）；ServiceLoader 注册文件 `META-INF/services/io.nop.lint.core.lang.LintLanguage` 登记两绑定。
- **Decision（$ expando 语义）= identity 成立**：解析级实验钉死 `$$$ARGS`（连续 `$`）/`$VAR`/`$_X` 在 typescript 与 tsx 双 grammar 中均按单 `identifier` 词法单元保留（`$$$ARGS` 作形参名、`$VAR` 作 variable_declarator name 断言）；两绑定 expando 位传 null（identity），`preprocessPattern` 恒等断言入测。结论回写 design 01 §4 增注；design 01 §1 增注 nop-lint-js 模块就位。
- **测试矩阵（Minimum Rules #25）**：`TypeScriptLanguageTest`/`TsxLanguageTest` 各 10 用例（singleton / parse(String) / parse(byte[]) / preprocessPattern identity ×3 形态 / meta-var 解析级钉死 / 代表性 kind（call_expression、member_expression、variable_declarator、function_declaration、enum_declaration、interface_declaration、jsx_element）+ 未知 kind `-1` + ERROR 恢复 kind `-1` / parseIncremental smoke / suppressionProvider null 契约 / 全树 walk）+ `JsBindingDiscoveryTest` 3 用例（ServiceLoader 双绑定发现 / id 契约 / shared adapter 无重解码）+ `TsSuppressionContractTest` 5 用例（`suppressionProvider()` null 契约 / `//` disable-next-line span / `/* */` disable-open span + unpaired meta / 无指令注释零 span / comment extras 双 grammar 载体验证）。**抑制适配结论：`CommentSuppressionScanner` 经 LintNode 门面天然语言无关，TS 注释形态零参数化改动**（Phase 1 项内的"若需参数化落 core"分支未触发）。
- 验证：`./mvnw -pl nop-lint/nop-lint-js -am test -T 1C` BUILD SUCCESS **28 tests / 0 failures**。
- Doc-sync: design 01 §1/§4 增注；plan Phase 1 全勾 + Status: completed（frontmatter `status: active` 保持，ledger 派生状态）。

### nop-lint roadmap item 23 closure audit 通过，item 置 `done`（plan `2026-09-22-0544-2-relational-rules` 收口）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0544-2-relational-rules.md`。独立 closure visit（非执行 session 自审）：CLOSURE_SCRIPT_CHECK 报 3 项（`ledger-structure-invalid` + `missing-pass:test` + `no-audit-receipt`），本 visit 全部消解——`## Closure Gates` 8 个 checkbox 位于 ledger 计数域外（01-file-ledger §2.5），按 §4.3 消解先例改为纯 prose 关闭条件记录（同 1420-1/2137-1/2137-2/2137-3/0128-1/0544-1 处置），计数域回到 34/34 勾选、0 unchecked；`## Verification` 补 `- pass test 20260922-1024 exit=0`；`## Closure` 补 dispatch/accepted 收口对（#audit-20260922-1024-2026-09-22-0544-2-relational-rules-1-8841a5ad）；`node tools/mission-driver/src/plan-check.mjs --strict` exit=0（34 checked / 0 unchecked）。
- 语义复核（对照 live repo）：三 Phase Exit Criteria 逐条核对通过——Phase 1 四算子 × stopBy 三档（含 rule 档 inclusive 断言）+ field 过滤（`TestRelationalMatcher` 22 用例）；Phase 2 all（kind 交集预过滤 + 全部命中才提交）/not（probe env 隔离）+ xdef/DSL 扩展 + `stopBy=rule` 缺 `stopByRule` fail-closed（`RuleDslParser.java:314-319`）；Phase 3 两条 successor 规则 + fixtures 落地 `nop-lint-nop`，对照表与执行期裁定增注（contextual pattern DSL 延伸）在案。Anti-Hollow：`node ai-dev/tools/scan-hollow-implementations.mjs --module nop-lint-core --severity high` 退出码 0；接线验证——`CompiledRule.compileNodeMatcher` 运行时真实消费关系/组合匹配器（imports + 编译路径核实），套件 invalid 夹具逐行命中断言 + kind 预过滤计数断言（`rulesKindFiltered=1`/`rulesExecuted=0`）。
- 本 visit 实跑验证套件全绿（full-green）：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0；`clean package -DskipTests -T 1C` BUILD SUCCESS；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **443 tests / 0 failures**；`./mvnw -pl nop-lint/nop-lint-nop -am test -T 1C` BUILD SUCCESS **13 tests**（TestNopRuleSuites 12/12 端到端全绿）；mission `commands.lint` checkstyle 走 `|| echo 'lint not configured'` 既有裁定路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-*/0128-*/0544-1 一致）。
- 收口翻转：roadmap item 23 `planned` → `done`（successor trigger "closure audit pass" 于本 visit 触发）；周边 items 21/22/24/28 保持 `todo`、M3 `done`、M4 `todo`（派生，19–29 未全 done）均未受扰动。plan frontmatter `status: active` 保持（ledger 派生完成态，不手写 `completed`）。无 in-scope live defect / contract drift 被降级。

### nop-lint roadmap item 23 Phase 3：successor 规则落地与对照收口（plan `2026-09-22-0544-2-relational-rules`）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0544-2-relational-rules.md`（work-item item-23；本条目为执行记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。Phase 1/2 已于本日先行落地（见下文两条目）。
- **两条 successor 规则 faithful 落地**（`nop-lint-nop` 主资源 `_vfs/nop/lint/rules/exception/`，承接 plan `2026-09-21-2137-3` 两条 `Deferred But Adjudicated` 与 plan 06 `stopBy` 承接项的 deferred 登记，Successor Path 均为本 plan）：
  - **exception/silent-swallow**（id `nop/silent-swallow`，error）：catch 块七信号缺失判定 = `all: [kind: catch_clause]` + 11 个 `not: has: ... stopBy: end, field: body` 信号形状（throw / NopMetadataException 构造 / `.errorCode(` 调用 / ErrorCode·Errors·NopMetadataErrors 三族的调用形 + 字段读形 / BizException 构造 / Biz.fatal 调用）。`field: body` 对齐 mjs 门禁"只扫大括号 span"语义（catch 参数类型不算信号）。
  - **exception/no-log-getmessage**（id `nop/no-log-getmessage`，warning）：`all: [kind: catch_clause, has: $E.getMessage() (end), not: has: throw $$$ (end)]`，与 ast-grep `java-lint-getmessage-only.yml` 逐 matcher 对齐。
  - fixtures：两 suite 各 1 valid + 2 invalid（basic + boundary）+ `.expect`；suite 规则文件为 `x:extends` 指针（夹具驱动主资源规则本体，零重复）。`TestNopRuleSuites` 预期集合断言扩至 9 规范规则 + 1 demo 套件（缺 suite 即红 fail-closed）。
- **Phase 3 执行缺陷发现与修复（核心，`Fix`）**：首跑 `TestNopRuleSuites` 红——silent-swallow valid 夹具 `errorsEnum`（`log(Errors.ERR_X + ...)`）被误报。根因：**java grammar 的 snippet 消歧歧义**——裸 `Errors.$FIELD` 作为 pattern 解析为 `scoped_type_identifier`（类型引用读法，实测 kindId 266/320），而表达式位置的同一形态实为 `field_access`（kindId 164/1）；裸 snippet 无法表达"表达式位置的字段读"信号（前序中断 session 的 scratch 调试即卡在此处）。修复 = 把 design 01 §1 编译管线**既有**的 contextual pattern 能力（`SourcePatternCompiler.contextual(selector, context, lang)`，Phase 1 已实现有测）接入关系匹配器 DSL 面：`lint-rule.xdef` 关系匹配器新增 `context`/`selector` 属性（与 `pattern` 互斥）、`RuleDslModel.Relational` 扩展、`RuleDslParser` 恰一形态 fail-closed 校验（pattern+context 冲突 / selector 缺 context / context 缺 selector 三向拒绝，消息含规则 id）、`CompiledRule.relationalInner` contextual 编译路径；xdef 属性 `pattern` 相应从 `!string` 放宽为 `string`（平台对缺省属性按空值拒绝 `!` 前缀；"必须声明 pattern 或 context+selector 对"的 XOR 权威在 parser，与 10 §2 既有纪律一致）。新增测试：parser 4 例（contextual 往返 / 双形态冲突 / selector 缺 context / context 缺 selector）+ 编译 2 例（contextual `has` 命中表达式位置字段读且其他根标识符不误匹配——歧义回归钉死）。裁定为执行期 scope 延伸（Phase 3 Targets 原列 nop-lint-nop，为满足"规则能加载且夹具绿"的 plan 自身 Exit Criteria 所必需），已回写 design 10 §2 落地形态增注。
- **行为对照记录（同语料对照表，先例 = plan 2137-3 cross-check；语料 = 两规则 suite 夹具本身，mjs/ast-grep 门禁保持不动，切换归 item 28）**：
  - silent-swallow ↔ `check-silent-swallow.mjs`：同语料（suite 14 夹具 = clean 7 块 + basic 2 块 + boundary 5 块，16 块扫描）mjs 门禁实测 **7 hits（L14/24/16/26/37/47/56）↔ nop-lint 7 diagnostics 逐行一致**；clean.java 7 块双方零命中。mjs 自身 `--fixture` 8/8 PASS（baseline 未动）。已裁定 delta（方向均为 nop-lint 严格于 mjs，即更少"伪信号豁免"，不产生漏报）：(1) 字符串字面量内的信号不计（boundary `signalInString`：mjs 文本层会豁免，AST 类型隔离不豁免——design 01 §3.3 已裁定登记）；(2) 信号标识符按**精确标识符**匹配而非 mjs 的子串匹配——mjs `Errors.` 子串可命中任意 `*Errors.` 前缀，nop-lint 按门禁 GOOD_SIGNALS 固定清单枚举三族（Errors/NopMetadataErrors/ErrorCode），清单外前缀（如 `OtherErrors.`）不计；(3) `BizException` 信号收窄为构造形态（裸标识符/`instanceof`/类型位置不计——mjs 文本层会计）；(4) catch 头参数类型（`catch (BizException e)`）双方均不算信号（mjs 只扫 brace span，AST `field: body` 同口径，boundary `headerTypeIsNotASignal` 对齐）。
  - no-log-getmessage ↔ ast-grep `java-lint-getmessage-only.yml`：同语料（4 夹具 = clean 4 块 + basic 2 块 + boundary 2 块）sg CLI（0.43.0，`--filter java-lint-getmessage-only`）实测 **4 hits（basic L13/22 + boundary L13/22）↔ nop-lint 4 diagnostics 逐行一致**；clean.java 双方零命中。已知不精确性（原 ast-grep 规则固有，非本 landing 引入）：yml 头注释的 "Good" 示例 `LOG.error(msg, e.getMessage(), e)` 在该规则自身 matcher 下仍命中（仅 rethrow 豁免）——nop-lint 保持同一语义并在 clean.java 夹具注释记录。附注：同语料反向跑 getmessage-only 于 silent-swallow 夹具，clean.java 的 2 个 getMessage-无-rethrow 块双方（ast-grep 与 nop-lint no-log-getmessage 语义）判定一致。
  - **接线验证（Minimum Rules #23）**：套件 invalid 夹具经 `RuleSetRunner` 真实命中（逐行命中断言 = 匹配器运行时被调用）；all/not 顶层 kind 预过滤计数断言由 `TestCompositeRuleCompile.compositeKindFilterShortCircuitsDisjointFiles`（`rulesKindFiltered=1` + `rulesExecuted=0`）与 `compositeScanProducesDiagnosticsThroughTheRunner`（真实执行产诊断）承担。
  - **端到端验证（Minimum Rules #22）**：`.rule.yml` 主资源 VFS 加载（xdef 校验 + register-model 管线，含新 `context`/`selector` 属性与形态互斥校验）→ `RuleDslParser` → `LintEngine.lint`（kind 过滤 → matcher → suppression 管线尾）→ `TestNopRuleSuites` 断言全绿（12 用例，套件发现集 fail-closed）。
- **收口项**：roadmap item 23 `todo` → `planned`（draft review 已过、三 Phase 执行绿；`done` 待 closure audit 通过后由 CLOSURE_AUDIT 步骤翻转，successor trigger 已注记）；周边核对：items 21/22/24/28 保持 `todo` 未受扰动，M4 保持 `todo`（派生，19–29 未全 done，本 item 单项完成不翻转）；M3 `done` 未受扰动。plan 2137-3（silent-swallow / no-log-getmessage 两条 deferred）与 plan 06（stopBy=rule 配对校验 deferred）的承接关系随本次落地兑现，原 deferred 登记见各 plan `Deferred But Adjudicated` 节。附带清理：删除前序中断 session 遗留的无断言 scratch 调试类 `ScratchDebug3Test.java`（`System.out` dump，非交付物）。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0；`clean package -DskipTests -T 1C` exit=0（**注意**：修复期间 nop-lint-nop 测试曾因 `target/` 内旧 jar 携带旧 xdef 而报 unknown-prop，重新 package 后消解——资源改动必须重打包再跑下游模块测试）；mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-*/0128-* 同一裁定）；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **443 tests / 0 failures**（437 → 443：parser +4、composite compile +2）；`./mvnw -pl nop-lint/nop-lint-nop -am test -T 1C` BUILD SUCCESS **13 tests**（含 TestNopRuleSuites 12）；`node ai-dev/tools/check-doc-links.mjs --strict` exit=0。
- Doc-sync: design 02 §1（Phase 3 承接落地增注）、design 10 §2（关系匹配器 contextual 形态 + pattern 属性放宽增注）、design 01 §3.3（对照表指针回写）、roadmap item 23、本日志条目（持久对照表）。`docs-for-ai/` 无需更新（nop-lint 设计权威在 `ai-dev/design/nop-lint/`）。plan Phase 3 checklist 全勾 + Status: completed；frontmatter `status: active` 保持不写 `completed`（ledger 派生状态）。

### nop-lint roadmap item 23 Phase 2：组合算子 all/not + xdef/DSL 扩展落地（plan `2026-09-22-0544-2-relational-rules`）

- **内核**：`AllMatcher`（合取：scratchpad env clone → 全部子匹配器命中才 adopt 提交，部分合取零泄漏）、`NotMatcher`（probe env 隔离 xor 语义：inner 命中→false，未命中→true，inner 绑定随 probe 丢弃）。与 `RelationalMatcher` 同属 `NodeMatcher` 家族，自由组合（not{all{...}}、all[not{has}] 等）。
- **Decision（item 23/24 算子分界，回写 design 01 §3.4、design 04 §6、roadmap item 23/24 注记）**：本 plan 交付 all/not + 关系四算子（successor 最小组合面）；item 24 承接 matches/utils/any 嵌套 refinement。消除 roadmap 文本（not 归 24）与 plan 2137-3 deferred 指针（not 组合归 23）的歧义。
- **DSL 面**：`RuleDslModel.Matcher` 扩展 all/not/inside/has/follows/precedes 形态 + 新 `Relational` 值对象（pattern/stopBy 默认 end/stopByRule/field）；`RuleDslParser` XOR 集扩展至 `pattern|kind|regex|any|all|not|inside|has|follows|precedes`，嵌套面有界（容器 → all 元素 → 其 not 内层）：`any`/`all` 入 all/not、`not` 嵌 `not` 全部 fail-closed（消息指向 item 24/越界，无静默跳过）；all 元素 atLeastOne + XOR；关系匹配器 pattern 必填、stopBy 枚举校验、**stopBy=rule ⇔ stopByRule 双向配对校验**（承接 plan 06 deferred：缺名/孤名均抛 `NopLintException` 英文消息含规则 id）、field 仅 inside/has。`lint-rule.xdef` 同步（check-mutex props 扩展 + 关系匹配器 attr 形 + 有界嵌套结构声明）。
- **编译面**：`CompiledRule` 新增组合编译路径——`compileNodeMatcher`（深度守卫与 parser 对齐；regex 任意层拒绝；**stopBy=rule 编译期显式拒绝**：utils 注册表归 item 24，不静默降档）、`kindOpinion`（design 01 §4 步骤 5：pattern→possibleKindIds、kind→单元素、relational/not→空意见、all→非空意见保守交集）、`scanTree`（全树前序扫描 + kind 意见 O(1) 预过滤，空意见=不过滤）。既有 pattern/kind/any 路径零改动。
- **测试**：`TestCompositeMatcher`(11：all 全命中/绑定合并/部分失败零泄漏、not xor/隔离、与关系算子组合)、`TestRuleDslParser` 扩展 15 例（关系往返/默认 end/缺 pattern/非法 stopBy/stopByRule 双向配对/field 操作数/双层关系 XOR/pattern+not XOR/all 往返/空 all/all 元素 XOR/any-in-all item-24/not 空/not 嵌 not/xdef 端到端 valid-relational + invalid-stop-by-rule 夹具）、`TestCompositeRuleCompile`(6：kind 交集意见/纯关系无意见/scan 产诊断经 RuleSetRunner/**kind 意见真实喂给 kind-bit 过滤（wiring：rulesKindFiltered=1 + rulesExecuted=0）**/stopBy=rule 编译期拒绝/regex-in-all 拒绝)。
- 修复：`buildMatcher` 初版将 kind/regex 值误置 pattern 槽位（case 贯穿 + 固定首参构造），被既有 TestCompiledRule/TestRuleSetRunner 回归即时捕获并修正——既有套件的回归价值实证。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **437 tests / 0 failures**（404 → 437）。
- Doc-sync: design 01 §3.4、design 04 §6、design 10 §2 增注；roadmap item 23/24 算子分界注记（状态保持 `todo`，Phase 3 回写）；plan Phase 2 全勾 + Status: completed。

### nop-lint roadmap item 23 Phase 1：关系匹配器内核（inside/has/follows/precedes × StopBy 三档 + field）落地（plan `2026-09-22-0544-2-relational-rules`）

- **内核交付**（nop-lint-core `pattern/` 包，全部新增）：`NodeMatcher`（节点级组合单元接口，false 返回时 env 零泄漏契约）、`PatternNodeMatcher`（SourcePattern 单节点适配：kind 预过滤 + probe clone/commit）、`KindNodeMatcher`、`StopBy`（Mode NEIGHBOR/END/RULE + `find(neighbor, multi, finder, env)` 两序列契约；rule 档 `inclusive_until`——stop 节点本身先被 finder 尝试再终止遍历，stop 匹配绑定丢弃）、`RelationalSequences`（惰性祖先链/兄弟表/前序 DFS 迭代器，end 档首中即停）、`RelationalMatcher`（Op 四算子统一实现；neighbor 档按算子分别定义——inside=直接父、has=全部直接子、follows/precedes=紧邻兄弟一个；`field` 语义：has 从 field 子节点为根、inside 要求候选占据祖先 field 槽位、follows/precedes 拒绝 field fail-closed）；`PatternMatcher` 新增公开 `matchPattern` 单节点入口（matchRoot 放宽为包可见）。
- **内核修正（design 04 §4 对齐）**：`PatternMatcher.step` 的 candidate-keyed skip 原仅覆盖 Terminal 目标——Internal 目标遇注释（extra）候选直接 FAIL，与类文档及设计矩阵"失败后按候选可跳性让路"相悖；现扩展到 Internal 目标（同 kind 结构失败因候选 named 非 extra 仍硬失败，AST 严格度行为不变）。既有 84 个 pattern 包测试全绿证明无回归。
- **Decision（注释/字符串掩码语义，回写 design 01 §3.3 增注）**：树级匹配无需显式掩码——注释是 extra（SMART 锁步跳过）且 kind 与任何模式形态不同；字符串字面量是 `string_literal` kind 叶子，kind+text 双重匹配天然隔离。mjs 文本层 `maskCommentsAndStrings` 的关切由类型隔离承接；文本信号"字符串内也算命中"的保守语义收窄为 AST 节点语义（已登记的 delta，Phase 3 对照表裁定）。
- **Decision（relational/all/not kind 预计算策略，回写 design 01 §4 步骤 5 增注）**：pattern→possibleKindIds、kind→单元素、relational/not→空=无意见（通配）、all→非空意见保守交集、any→并集；无意见顶层全量放行（绝不冒充/绝不静默跳过），`rulesKindFiltered`/`rulesExecuted` 计数如实。
- 测试：`TestRelationalMatcher` 22 用例 = 每算子 × 每 stopBy 档 ×（命中/不命中/stop 短路含 stop 节点本身 inclusive 断言/field 过滤/根叶边界）+ 注释交互（neighbor 档被注释阻断 = 与上游 prev/next_sibling 一致、end 档不受影响）+ env 契约（捕获流入/失败零泄漏/同名一致性跨关系成立）。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **404 tests / 0 failures**。
- Doc-sync: design 01 §3.3/§4、design 04 §5 增注；plan Phase 1 全勾 + Status: completed。

### nop-lint roadmap item 18 独立 closure audit APPROVE-CLOSURE：plan 0544-1 收口，item 18 → `done`，M3 派生翻转

- 独立 auditor（CLOSURE_AUDIT fresh visit，#audit-20260922-0700-2026-09-22-0544-1-minimal-cli-1-73ee2347）：**APPROVE-CLOSURE**。独立复跑验证套件全绿——`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` EXIT=0、`clean package -DskipTests` EXIT=0、`test -T 1C` EXIT=0（**382 tests / 0 failures**）、checkstyle 走 mission 既有裁定路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-*/0128-* 一致）；`scan-hollow-implementations.mjs --module nop-lint-core --severity high` 退出码 0。
- Anti-Hollow 复核：`NopLintCli.runFull` 调用链 argv → `CliOptions.parse` → `TargetScanner.scan` → `CheckRunner.run`（`RuleSetLoader` VFS 规则加载 + 共享 `LintEngine.lint`）→ `ConsoleReporter.render` 全链连通；`TestNopLintCliEndToEnd` 端到端 + 接线断言（ServiceLoader 真实 discovery + 抑制差分 `suppressed diagnostics: 1`）证明管线组件被运行时真实消费，非类型存在。
- Exit Criteria / Closure Gates 逐条核对通过（live code + design 03 §2.4 增注 + dogfood 日志证据）；Deferred/follow-up 分类诚实性核实：dogfood 发现均为已裁定项（cli 包 9 warning no-vfs-violation by-design、suppress 包 javadoc 散文触发归 design 09），无 in-scope live defect 被降级。
- 收口状态：plan 0544-1（ledger 格式）Closure Gates 全勾 + `## Verification` pass line + `## Closure` dispatch/accepted receipt 写入；frontmatter `status: active` 保持（派生状态不手写）。Roadmap 回写：item 18 `planned` → `done`；M3 按派生规则翻转 `done`（items 14–18 全 `done`）；19/28 等周边 item 未受扰动。仓库级 dogfood/CI 门禁切换归 item 40。

### nop-rg 第三轮深度审计收口：独立 closure audit APPROVE-CLOSURE，plan 2275 翻 `completed`

- 独立 auditor（fresh session，agent_7e1682b2-e1a4-467d-9c27-e6b586b160cf）：**APPROVE-CLOSURE**。独立复跑全绿——`./mvnw test -pl nop-rg/nop-rg-core,nop-rg/nop-rg-cli,nop-rg/nop-rg-vector -am` EXIT=0（core 56 run + 1 skip 按 mtime 甄别、cli 27、vector 10）、large-file 显式组 3/3、rg 对照 opt-in 9/9、benchmark compile EXIT=0；三工具（check-plan-checklist --strict / scan-hollow --module nop-rg --severity high 0 findings / check-doc-links --strict）退出码全 0。
- P6 判定数据逐位核对（p2275-p6-pair1..3 JSON）：512MB +9.91/+13.08/+9.67%、调用链 NopRgMain→SearchCommand→SearchCoordinator→MatchAggregator→LineCursor.advanceLineStart 端到端实证连通，advanceLineStart 在终态 profile 占 6.1%（真实热点非空壳）；G1 修复与聚焦测试接线核实；P1 回退经 git 证实零残留；吞吐比双档 77.0%/53.2% ≥50% 复核一致。
- audit 6 项 Minor（文本/证据卫生）随收口修复：Phase 2 遗留 test 项补勾、Phase 3 Status 翻转、512MB CI 表述收窄（2/3 严格零重叠、3/3 全正）、runs 432 溯源注记、P1 迭代档筛退不落 JSON 豁免注记、-21.1% 舍入修正。Deferred/follow-up 分类诚实性核实：无 in-scope live defect 被降级。
- 收口状态：plan 2275 `Plan Status: completed`（第四 Phase Exit Criteria + 16 条 Closure Gates 全勾 + Closure Evidence 写入）；三个执行 commit（3ff96f472c / b9470d86d1 / 64de9f7a97）+ 收口 commit；他会话 nop-lint 未提交改动全程未触碰（各 commit 均显式路径暂存）。
- Doc-sync: benchmark README 收敛循环结论（2275 章节）+ cli README 差异记录 + plan 迭代记录表 10 行 + 本日志；design 01 `No update required`（advanceLineStart 包私有、无决策级契约变更）。

### nop-lint roadmap item 18：Minimal CLI（`nop-lint check` + console）落地（plan `2026-09-22-0544-1-minimal-cli` 全 Phase 执行）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0544-1-minimal-cli.md`（work-item item-18；本条目为执行记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **Phase 1（CLI 装配层）**：新包 `nop-lint-core` `io.nop.lint.core.cli`——`NopLintCli`（main + `run` 进程内入口，CoreInitialization 按需初始化/仅自初始化时销毁）、`CliOptions`（v1 参数面 = `check <path>... [--profile fast|standard]`，缺省 standard；未知子命令/未知选项（含 `--max-warnings`/`--rules` 等 item 39 面）/缺值/空白目标全 fail-closed）、`TargetScanner`（目录递归 + 扩展名==语言 id 绑定约定，未知扩展/无扩展按桶显式计数 `SkippedFiles`，lintable 字典序稳定输出，目标不存在抛错）、`RuleSetLoader`（classpath VFS `/nop/lint/rules/` 前缀递归扫 `*.rule.yml` → `RuleDslParser` 加载 → 规范化语言分组；空前缀/加载失败/空白语言均抛错）、`CheckRunner`（逐文件共享 `LintEngine` 执行 + 规则语言未绑定先于任何文件 fail-closed + 单文件读取/解析失败带路径抛出）、`RunSummary`/`CheckOutcome`/`FileFindings`（聚合与稳定序）。测试 `TestCliOptions`(10) + `TestTargetScanner`(7) + `TestRuleSetLoader`(6) + `TestCheckRunner`(5)；新增测试夹具（cli-rules/cli-rules-broken/cli-rules-broken-nolang/cli-rules-unbound 四前缀）与测试域 ServiceLoader provider `TestJavaIdLanguage`（id `java`，使 core 测试可走真实 `discoverDefaults` 全链路；xdef language enum 限制下 `stub` id 无法绑定 xdef 合法规则）。
- **Phase 2（console 输出与退出码）**：`ConsoleReporter`（诊断行 `<path>:<line>[-<endLine>]: <severity>: <ruleId>: <message>`，`LineIndex` 与 RuleTester §4.2 同一换算口径；汇总段 fixed 顺序渲染 scanned/skipped 分桶、error/warning/info/hint/other 计数、`rules loaded/executed/skippedByProfile(含 ids)/kindFiltered`、`suppressedDiagnostics`、`disabledRuleIds`、xscript 四计数）；`NopLintCli.run` 三态退出码（0 无 error 级诊断 / 1 存在 error 级 / 2 内部错误，stderr `nop-lint: error:` 前缀，不产部分结果冒充成功）+ 按需 CoreInitialization（仅自初始化时销毁）。测试 `TestConsoleReporter`(5) + `TestNopLintCliExitCodes`(7：clean→0、warning-only→0、error→1、fast 档、未知选项→2、目标不存在→2 且无 stdout 部分报告、生产规则库缺失→2 部署错误)。
- **Phase 3（端到端 dogfood 与收口）**：`TestNopLintCliEndToEnd`(6)——argv → discoverDefaults（真实 ServiceLoader，stub + java-id 双 provider）→ VFS 规则加载 → `LintEngine` 管线 → console + 退出码全链路（clean→0 / error 违规→1 / 坏输入→2 / 混合严重度聚合计数）；接线验证（Minimum Rules #23）：同文件双命中一抑制（`// nop-lint-disable-next-line demo/no-print`）→ 存活命中渲染 + `suppressed diagnostics: 1` 计数上升 + 被抑制命中不渲染，证明抑制尾部真实消费引擎候选而非类型存在。
- **Dogfood 证明（真实 CLI 二进制 `java -cp` 装配 + 7 条生产规则，切片 = nop-lint-core 自身 main 源 66 文件，按包有界执行）**：engine/lang/node/pattern/rule/type/xscript 七包 41 文件 **零发现**；cli 包 10 文件 **9 warning**（全部 `nop/no-vfs-violation`：CLI 作为文件遍历工具按设计使用 java.nio/Files/Path，规则字面命中——豁免裁定归 item 27/28，本 plan 不翻转任何 CI 门禁）；suppress 包 7 文件在首个文件即**退出 2 显式中止**（`CommentSuppressionScanner.java:61` javadoc 散文含字面指令关键字 `nop-lint-disable-next-line` 且后随非字母 token，按 design 09 fail-closed 契约判为畸形指令——真实 dogfood 发现：文档散文提及指令关键字会触发抑制 DSL，留 design 09 后续裁定，非本 plan scope）。运行口径：`java -cp "core/classes:java/classes:nop/classes:<deps>" io.nop.lint.core.cli.NopLintCli check <dir>`，nop-lint-nop 规则资源与 nop-lint-java 绑定均从 classpath 真实装配。
- **Decision（M3 边界）**：items 14–17 均 `done`，本 plan 收口且 closure audit 通过后 item 18 → `done`，M3 依赖（14–18）即全齐——M3 按 roadmap 派生规则翻 `done`（不提前手翻）。M3 的"run nop-lint on this repo"执行面证据由本次有界 dogfood 提供；仓库级 CI 门禁切换与全仓 dogfood 活动归 item 40（design 06 §8 迁移期）。记日志为准。
- Roadmap 回写：item 18 `todo` → `planned`（draft review 已过；successor: closure-audit trigger: item 18 → done on closure audit pass）；items 14–17 `done` 与 19/28 `todo` 核对未受扰动；M3 `todo` 保持（派生，待 18 done）。frontmatter `status: active` 保持不写 `completed`（ledger 派生状态）。
- Doc-sync: design 03 §2.4 增注 + roadmap item 18 + 本日志条目。`docs-for-ai/` 无需更新（nop-lint 设计权威在 `ai-dev/design/nop-lint/`）。
- **裁定（已回写 design 03 §2.4 增注）**：落点 = core 内 main 类 + 普通 `java -cp` 装配（不建 launcher 模块，零 core→nop/nop-java 编译期依赖）；参数面/规则源/扩展名绑定/console 格式/退出码三态详见 design 03 §2.4。
- 偏差：无 scope 偏差；`--max-warnings` 裁定不进 v1（item 39），`--rules` 覆盖同理。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **382 tests**（Phase 1 后 364 + Phase 2 新增 12 + Phase 3 新增 6，0 failures）。
- Doc-sync: design 03 §2.4 增注 + roadmap item 18 + 本日志条目。

### nop-rg 第三轮深度审计 Phase 3：收敛循环 P6 保留（count 中位 +9.9%）+ P1 筛退 + 字面终止（plan 2275）

- **P6 保留**（count 口径 advanceLineStart，共测配对 3 对全证）：count 快速路径只消费行起点，新增 `LineCursor.advanceLineStart(offset)`（包私有，仅回扫行起点）替代 `advance()`——免每 span 的行尾前向扫描 + 免 LineInfo record 分配。配对（p2275-p6-pair1..3-{base,cand}.json，基线侧 = m2 快照、候选侧 = target/classes，交替 3 对）：512MB 三对全正 **+9.9%/+13.1%/+9.7%**（CI 零重叠）、1MB 三对全正 +7.1%/+13.0%/+4.6%；64MB 方向不一致但 σ ±12-18 与基线漂移同源、CI 全重叠不可判（当日外部负载，row0 双跑自身即 ±11.6%）。稳定口径 3/3 方向一致 ≥ max(2%, 3σ) → 保留。终态 count profile：runs 432→581/12s，残余热点仍为扫描带宽地板。
- **P1 筛退回退**（TEXT ResultPrinter 逐段 print 免中间 String）：迭代档两跑 11.72/11.70 vs 基线 12.09（名义 -3%）——invokedynamic 拼接为 bulk-copy 优化、5 次 synchronized print 反而更慢；代码已还原（git diff 零残留复核）。
- **P2/P3/P4/P5 实现前否决**（各有显式依据）：P2 many-small isBinary 实测占 1.9% 上界封顶；P3 walker 双 stat 算术上界 ≤1.7%；P4 锚点加权解析式 trade-off 净负（锚点前移的 verify 收益 < 跳跃损失）；P5 copyOf 可寻址切片 1.5%。**循环字面终止**：P1✗ + P6 收割后候选池枯竭，穷尽说明落记录表（count/many-small 残余 = 扫描地板 + syscall 面；TEXT 残余 = 输出契约必需）。
- 偏差：P4 原计划组件级先筛，执行时以解析式机理裁定实现前否决（更强的证据形态，免烧机时；记录表留痕）。
- 验证：kept 后全量回归 EXIT=0——core/cli/vector `./mvnw test`、large-file 显式组（`-DexcludedGroups= -Dgroups=large-file -DargLine=-Xmx256m`）、rg 对照 opt-in（`-Dtest.rg.compare=true`）全绿；count 快速路径语义不变（行计数仅依赖 lineStart，既有 count 语义测试 + fuzz/rg 对照守护）。
- Doc-sync: plan 2275 迭代记录表（R1/R2/P2-P5/终止 6 行）；benchmark README 循环结论随 Phase 4 收口更新；design 01 No update required（advanceLineStart 为包私有方法，无决策级契约变更）。

### nop-rg 第三轮深度审计 Phase 2：性能基线重建 + m2 快照（plan 2275）

- row0 基线（收尾档，判定 JSON 落 `_tmp/nop-rg-bench/p2275-*`）：count 双跑 1MB 821.0/788.8、64MB 55.04/43.64（±11.6% 外部负载噪声）、512MB 12.07/11.70 ops/s；TEXT 口径（R1 缓冲 + R5 现行口径）1MB 577.9 / 64MB 12.09 / 512MB 1.568 ops/s（G8 关闭：README 中 2273 的 131.7/2.15/0.269 标注为 R1 前历史值并刷新现行基线）；many-small 31.82±3.14 ops/s。m2 基线快照 install EXIT=0（共测配对基线侧就绪）。
- row0 profile（HotspotProfiler 12s ×3 口径）：count = indexOf 46.6% + readByte 27.1%（扫描带宽地板，与 2267/2273 终态同构——「不再评估」清单维持，无结构漂移）；text = BufferedWriter.write 29.9%（输出必需）+ indexOf 14.0% + **ResultPrinter.print:41 行拼接 11.4%**（P1 入池依据）+ buildLineMatches 分配面 5.3%（P5 记录）；many-small = 行扫描 ~76% + matchesAt 19.3% + isBinary 1.9%（P2 上界封顶 <2% 实现前否决；P3 算术上界 ≤1.7% 实现前否决）。
- 偏差：无（基准零源码改动，README/记录表为 Phase 2 全部落盘物）。
- 验证：基线复用 corpus 逐字节可再生机制（固定种子，bench Setup 重建校验）；`./mvnw test`（Phase 1 后未再改代码，同基线绿）。
- Doc-sync: benchmark README 基线段已刷新；plan 2275 迭代记录表已填 row0 + P2/P3 否决行。

### nop-rg 第三轮深度审计 Phase 1：CLI 组合语义缺陷修复 + API 面收缩（plan `ai-dev/plans/2275-nop-rg-round3-deep-audit-quality-perf.md`）

- Plan 2275 Phase 1（G1-G7）落地。本轮为 2273 之后的第三轮深度审计：新发现 G1（live defect：`-c --json` 组合输出退化——`includeLineText=!count` 使 JSON 模式拿到空 lines，输出零 match 消息的 begin/end 且退出 0；实测 rg 15.1.0 输出模式标志按最后出现者生效）修复为 `includeLineText = !count || json`（优先级契约不变，仅修数据完整性）+ NopRgMainTest 新增回归用例（cli 26→27）+ cli README 差异记录；G2（2273 R1 镜像遗漏：HotspotProfiler TEXT sink 仍 autoflush 8KB + javadoc 陈旧 + 内联 FQN + var）镜像同步为 64KB 缓冲 + 每 run flush；G3/G4/G5 死公共 API 删除（GlobMatcher.hasIncludes/hasExcludes + 构造器收 private、CompiledGlob.getPattern + 孤儿 pattern 字段、ChunkedFileReader(Path) 便捷构造器 + getChunkSize + DEFAULT_CHUNK_SIZE/DEFAULT_OVERLAP）；G6 五文件组内 import 归位（含 2268 watch-only 遗留的 JfrSwitchTest）+ endToEndSearch 死 `throws IOException` 删除；G7 Submatch javadoc 双重过时语义更新（count 路径不构造 Submatch；R5 后 text 为空串非 null）。
- 计划治理：plan 2275 经两轮独立子 agent 对抗性审查（第一轮 agent_d16e2d1d：1 Major + 3 Minor；第二轮 agent_30247617：4 项修订核对 PASS + 新发现 4 处折入，含 core 测试基线口径勘误——默认 surefire 实为 7 类 56 run + 1 skip，large-file 组 3 默认排除，2273 Closure 段的 59+1 系把显式组陈旧报告计入默认口径）。
- 偏差：无实质 scope 偏差。
- 验证：`./mvnw test -pl nop-rg/nop-rg-core,nop-rg/nop-rg-cli,nop-rg/nop-rg-vector -am` EXIT=0——core 56 run + 1 skip（fresh 报告按 mtime 口径核对，陈旧 LargeFileSearchTest 报告已甄别排除）、cli 27（含新增 G1 用例）、vector 10；`./mvnw compile -pl nop-rg/nop-rg-benchmark -am` EXIT=0；grep 门禁：`hasIncludes|hasExcludes|getChunkSize|DEFAULT_CHUNK_SIZE|DEFAULT_OVERLAP` 零残留，`.getPattern()` 仅剩 SearchCommand（有意保留）。
- Doc-sync: cli README「与 rg 的已知差异」已更新（G1 优先级差异）；design 01 `No design-doc update required`（无决策级契约变更）；module-groups.md No update required。

### nop-lint roadmap item 17 独立 closure audit：APPROVED，roadmap item 17 翻 `done`

- 独立 auditor（fresh closure visit，opencode-2026-09-21-142035，model zhipuai-coding-plan/glm-5.3-flash）：CLOSURE_SCRIPT_CHECK 报 2 项（`missing-pass:test` + `no-audit-receipt`），本 visit 全部消解——`## Verification` 补列 pass 行（`pass test 202609220537 exit=0`）、`## Closure` 补 dispatch/accepted 收口对（#audit-202609220537-2026-09-22-0128-3-suppression-v1-1-79e537e5，含 `models={exec,aud}` lineage 后缀，单模型降级如实登记）。`plan-check.mjs --strict` 重跑 exit=0（29/29 勾选，0 unchecked）。
- **亲测复跑**全套验证命令：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0、`clean package -DskipTests -T 1C` exit=0、`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS（**336 tests**，0 failures）；跨模块 `./mvnw -pl nop-lint/nop-lint-java,nop-lint/nop-lint-nop -am test -T 1C` exit=0（nop-lint-java **28 tests** 含 `TestJavaSuppressWarnings` 19，nop-lint-nop **11 tests** 含 `TestNopRuleSuites` 10，与本 plan Exit Criteria 记载逐项一致）；mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-*/0128-1/0128-2 同一裁定）；辅助门禁 `scan-hollow-implementations.mjs --severity high` 三模块（core/java/nop）全部 0 findings exit=0、`check-plan-checklist.mjs --strict` exit=0（29/29）。
- 语义复核：三 Phase 全过——Phase 1 `CommentSuppressionScanner` 六形态 + 配对状态机（行 span 立即闭合不参与配对）+ fail-closed 八路径 + `SuppressionMeta` 固定 id/severity（元诊断不可抑制）；Phase 2 `JavaSuppressWarningsProvider` 三值形态 × 四声明类型 + `nop-lint:` 畸形 id fail-closed + 外来值只匹配字面 id；Phase 3 接线实证 `LintEngine.lint:93-96` 管线尾 `SuppressionFilter` 真实运行（`LintLanguage.java:77` 默认钩子 → `JavaLanguage.java:92` 绑定）+ `suppressedDiagnostics` 计数调和，Anti-Hollow 由同源差分断言（无抑制 2 诊断 vs 有抑制 1 诊断且计数 1）+ nop-lint-nop ServiceLoader 真实绑定套件全绿证明。
- Roadmap 回写：item 17 `planned` → `done`（closure audit approved，successor trigger 兑现：#audit-202609220537）；items 15/16 `done`、item 18 `todo` 未受扰动。frontmatter `status: active` 保持不写 `completed`（ledger 派生状态）。
- Doc-sync: plan 文件（Verification pass 行 + Closure 收口对）+ roadmap item 17 + 本日志条目。

### nop-lint roadmap item 17：Suppression v1 落地（plan `2026-09-22-0128-3-suppression-v1` 全 Phase 执行）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0128-3-suppression-v1.md`（work-item item-17；本条目为执行记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **落地内容**：新包 `nop-lint-core` `io.nop.lint.core.suppress`——`CommentSuppressionScanner`（CST 注释节点导航扫描六形态内联指令 + 配对状态机：`disable-next-line`/`disable-line` 立即行 span 不参与配对、`disable` 开放区域由 `enable`/`enable-all` 闭合、EOF 未闭合 span 延伸至源尾 + `unpaired-disable` info；fail-closed：enable 缺规则列表/enable-all 带参/空规则段/非法 token/一注释双指令均抛 `NopLintException` 含行号，`nop-lint-` 非关键字后缀为普通文本，`--reason` 接受并丢弃）、`SuppressionSpan`（字节半开区间，空规则集 = 全规则，区间相交重叠语义）、`SuppressionProvider`（core 接口）+ `SuppressionFilter`（注释扫描恒开 + 语言 provider 合成，单调判定，unused 按指令粒度：任一 span 抑制过即已使用，注解 span 同入 unused 检查）、`SuppressionMeta`（固定 id `unused-disable-directive` warning / `unpaired-disable` info，元诊断不可被抑制）。`@SuppressWarnings`：`nop-lint-java` `JavaSuppressWarningsProvider`（CST annotation/modifiers → 声明节点 range；三值形态逐值判定 + 数组/命名形态/限定名后缀；`nop-lint:` 空白/畸形 id fail-closed；外来值按兼容规则成为裸 id span 不伪造抑制），经 `LintLanguage.suppressionProvider()` 默认方法钩子装配（缺省 null，core 零文法硬编码）。引擎接线：`LintEngine.lint` 在 `RuleSetRunner` 后构造 `SuppressionFilter` 一次性判定（design 03 §1.1 管线尾，与 per-match 图示位置语义等价）；`LintStats` 新增 `suppressedDiagnostics` 计数域 + `diagnostics` 尾部调和为最终输出数；FAST/STANDARD 一致生效。测试：`TestCommentSuppression`(31) + `TestJavaSuppressWarnings`(19) + `TestSuppressionEngineEndToEnd`(4) + nop-lint-nop `suppression/no-suppress-demo` 套件（注释/注解/unused/unpaired 四场景 + valid，经 `TestNopRuleSuites` 真实管线全绿）。附带重构：`LineIndex` 自 `testing` 包移至 `node` 包（抑制层与测试层共用，"实现一次"口径），新增 byte[] 构造器与 `lineStartByte/lineEndByte/lineCount`。
- **关键裁定（已回写 design 09 §2.3/§3.1、design 03 §1.1/§4 增注）**：(1) 扫描载体 = CST 注释节点导航而非行级扫描（字符串字面量不可抑制 + LintNode 门面语言无关）；(2) 未知形态 = fail-closed 抛错（可识别关键字 + 畸形参数是契约违规），`nop-lint-disabled` 类非关键字后缀是普通文本；(3) 裸 `disable` 仅 `enable-all` 可解除，`enable <rule>` 只关闭具名 span（无匹配开放 disable → unpaired）；(4) RuleTester 抑制夹具契约 = 不扩 `.expect` 字段/不加目录/不加引擎开关，抑制场景用普通 invalid/ 夹具的抑制后差分断言表达（同文件未抑制对等违规证明规则本会命中），元诊断作为普通期望条目，stats 计数由 Java 引擎级测试承担——旧"suppressed.java 不属契约"注记已被本契约取代；(5) `unused-disable-directive` 按指令粒度且覆盖第 1/2 层（design 09 §1 口径）。
- **执行期修正 1（Scanner 设计缺陷，未达 live 即修正）**：初版把 `disable-next-line`/`disable-line` 一并纳入 disable/enable 配对状态机（会被 `enable-all` 错误闭合、EOF 误报 unpaired）——按 design 09 §2.1 语义改为立即闭合的行 span，配对状态机仅服务裸 `disable`；测试矩阵钉死两种 span 形态的差异。
- **执行期修正 2（Java split 语义）**：`String.split(",")` 默认丢弃尾随空串，`// nop-lint-disable r1,` 的尾随逗号静默通过——改 `split(",", -1)` 后空规则段 fail-closed 抛错并有测试。
- **偏差**：夹具套件落 `nop-lint-nop`（plan Targets 仅列 core）——注解抑制夹具需要真实 Java 绑定（`JavaBindingTestSupport` 为 core 测试域的 plain adapter，无 provider，且 core 测试不能依赖 nop-lint-java），nop-lint-nop 经 ServiceLoader 拿真实 `JavaLanguage`，为唯一可用 e2e 落点；`TestNopRuleSuites` 发现集断言相应更新为 7 规范规则 + 1 夹具域 demo 套件（保持精确集合 fail-closed）。Phase 2 无 LintEngine 级注解 e2e（接线归 Phase 3，注解层 e2e 由 nop-lint-nop 套件承担）。
- **验证**：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0（Phase 2 后另跑全仓 test-compile 亦 exit=0）；`clean package -DskipTests -T 1C` exit=0；mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-*/0128-1/0128-2 同一裁定）；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` **BUILD SUCCESS 336 tests**（301 存量 + 31 矩阵 + 4 e2e）；`./mvnw -pl nop-lint/nop-lint-java -am test -T 1C` BUILD SUCCESS **28 tests**（9 存量 + 19）；`./mvnw -pl nop-lint/nop-lint-nop -am test -T 1C` BUILD SUCCESS 全链绿（72/122/336/28/11，含 TestNopRuleSuites 10 用例）。
- Roadmap 回写：item 17 `todo` → `planned`（draft review 已过，`done` 待 closure audit 通过后由 CLOSURE_AUDIT 步骤翻转；successor: closure-audit trigger: item 17 → done on closure audit pass）；items 15/16/18 核对未受扰动（done/done/todo）。frontmatter `status: active` 保持不写 `completed`（ledger 派生状态）。
- Doc-sync: design 09 §2.3（扫描载体/作用域/未知形态/配对语义四段）+ §3.1（提取载体/值形态/unused 覆盖三段）+ design 03 §1.1（管线尾裁定）+ §4（抑制夹具契约，取代两处"不属契约"旧注记）+ roadmap item 17 + 本日志条目。`docs-for-ai/` 无需更新（nop-lint 设计权威在 `ai-dev/design/nop-lint/`）。

### nop-lint roadmap item 16：EditCalculator + 增量解析集成落地（plan `2026-09-22-0128-2-edit-calculator-incremental-parse` 审计反馈后重执行收口）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0128-2-edit-calculator-incremental-parse.md`（work-item item-16）。背景：首轮 EXECUTE 代码与测试已落但 plan checklist 未勾、roadmap 未回写，closure audit 判 FAIL（26 unchecked）后本轮按审计反馈重执行收口。
- **落地内容**（`nop-lint/nop-lint-core`，零 nop-treesitter 改动，零平台改动）：新组件 `lang/EditCalculator`（字节公共前后缀剥离 → 变更中段按行 Myers diff（O((N+M)·D)）→ 每组收缩到字节级的多 hunk diff，输出合法 `TSInputEdit` 序列，null 入参 fail-closed 抛 `NopLintException` 英文消息）；`LintLanguage.parseIncremental(LintTree, byte[])` 接口 + `TreeSitterLanguageAdapter` 实现（diff → `TSParser.parseIncremental`，`oldTree == null` 显式回退全量解析——reuse 计数断言该路径，绑定实例不匹配/解析失败 fail-closed）；`JavaLanguage`/`StubTestLanguage` 接口实现同步。测试：`TestEditCalculator`(17，编辑矩阵逐格断言 + 变换恒等式) + `TestTreeEquivalenceOracle`(7，oracle 焦点测试) + `TestIncrementalParseIntegration`(6，增量/回退/失败三路径 + 接线验证) + `TestIncrementalCorpusEquivalence`(1，126 实例正确性门禁) + `TestIncrementalLintEndToEnd`(3，parse→lint→编辑→增量 reparse→重 lint 诊断集变化端到端)。
- **本轮修复缺陷 1（真缺陷，EditCalculator 编辑编码）**：初版把 `newEndByte` 表达为绝对 new 源偏移，违反 `TSInputEdit` record 契约——该 record 是锚定语义（`newEndByte == startByte + 插入长度`，删除时 `== startByte`；构造校验 `newEndByte >= startByte`），且后端 `ReuseCursor.mapOldToNew` 按每 hunk `newEndByte - oldEndByte` 折叠旧→新坐标位移：绝对坐标在有净删除后的 hunk 上既被构造校验拒绝（corpus gate 实证 `newEndByte 322 must be >= startByte 328`），即便放行也会污染后续 hunk 的复用映射（复用叶 content 不复核，错映射 = 错树）。修复 = 锚定编码 + hunk 锚定越界时向后合并（合并 span 总 delta 与逐 hunk delta 之和精确相等，映射零误差，仅牺牲合并区间复用率；首个 hunk 处合并必终止）。回归：`deleteHeavySecondHunkMergesToStayWithinNewSource`（净删除后尾部 hunk 合并为单合法编辑）+ `insertionHeavySecondHunkKeepsMinimalMultiHunks`（合法场景保持最小多 hunk）；裁定增注回写 design 03 §1.2（TSInputEdit 坐标契约段）。
- **本轮修复缺陷 2（测试假设错误，非产品缺陷）**：`TestTreeEquivalenceOracle` 两例假设 oracle 对纯文本差异敏感——但 oracle 按 plan 契约只比较 type/startByte/endByte 前序序列，`bar`→`baz` 同类型同字节区间，profile 恒等（`differentSourcesAreNotEquivalent` 误报红 + `assertEquivalentNamesTheDivergence` 无异常抛出）。修复 = 改用结构性差异源（增参/改标识符长度），并新增 `textOnlyDifferencesWithIdenticalProfilesAreEquivalentByContract` 钉死契约；对 gate 健全性无影响——被比较两树恒 span 同一 newSource，profile 相等 ⟹ 结构相等，叶内容由同源保证。
- **本轮修复缺陷 3（gate 域裁定）**：随机字节 splice 会让部分编辑后源码超出 Java grammar error-recovery 域——**全量 parse 自身**抛 `error recovery made no progress`（debug 实证 120 随机实例中 ~45 例，零增量-only 失败），此类实例无 oracle 目标。裁定：确定性重采样（seed = base + attempt×7919，`MAX_ATTEMPTS=64`），仅对全量 parse 成功实例断言增量 ≡ 全量，增量-only 失败仍直接传播；门禁实测 **126 实例全过（≥100，seed 20260922 可复现）**。附带修复 `randomlyEdit` 的 `pos==length` splice 越界（ArrayIndexOutOfBounds，重采样新种子序列暴露）。
- **验证**：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0；`clean package -DskipTests -T 1C` exit=0；mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-*/0128-1 同一裁定）；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` **BUILD SUCCESS 301 tests, 0 failures**（267 存量 + 新增 34 全绿）；跨模块核对 `./mvnw -pl nop-lint/nop-lint-java -am test-compile -T 1C` exit=0（`LintLanguage` 新接口方法的下游实现方）。
- Roadmap 回写：item 16 `todo` → `done`（本条目为执行+审计反馈重执行记录；independent closure audit 由下游 CLOSURE_AUDIT 步骤复核）；items 15/17/18 核对未受扰动（done/todo/todo）。frontmatter `status: active` 保持不写 `completed`（ledger 派生状态）。
- Doc-sync: plan 文件（26 个 Phase checklist 全勾 + Closure Gates 按 ledger 01-file-ledger §2.5 计数域纪律改纯 prose，同 0128-1 消解先例）+ design 03 §1.2（锚定坐标契约增注）+ roadmap item 16 + 本日志条目。

### nop-lint roadmap item 15 独立 closure audit：APPROVED，roadmap item 15 翻 `done`

- 独立 auditor（fresh closure visit，opencode-2026-09-21-142035，model zhipuai-coding-plan/glm-5.3-flash）：CLOSURE_SCRIPT_CHECK 报 3 项（`ledger-structure-invalid` + `missing-pass:test` + `no-audit-receipt`），本 visit 全部消解——`## Closure Gates` 8 个 checkbox 为计数域外违规（01-file-ledger §2.5），按 §4.3 消解先例改为纯 prose（同 plan 1420-1 / 2137-1 / 2137-2 / 2137-3 处置）；`## Verification` 补列 0 pass 行、`## Closure` 补 dispatch/accepted 收口对（#audit-202609220300-2026-09-22-0128-1-deadline-executor-1-eb54577b，含 `models={exec,aud}` lineage 后缀）。`plan-check.mjs --strict` 重跑 exit=0（27/27 勾选，0 unchecked）。
- **亲测复跑**全套验证命令：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0、`clean package -DskipTests -T 1C` exit=0、`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS（**267 tests**，0 failures）；mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-1/2137-2/2137-3 同一裁定）；辅助门禁 `scan-hollow-implementations.mjs --module nop-lint/nop-lint-core --severity high` exit=0（0 findings）。
- **closure visit 修复一处测试隔离缺陷（bug fix note）**：首轮 `test` 复跑 `TestLintDeadlineExecutor.installIsIdempotentAndDelegatesToTheInstallTimeExecutor` 红——根因 = 全局 executor 槽位是 JVM 级单例，先行引擎测试（`TestXScriptTimeoutSemantics`/`TestXScriptDeadlineEndToEnd` 经 `LintEngine.lint:77` 触发 `LintDeadlineExecutor.install()`）不恢复槽位，后行断言类拿到残留 wrapper 后幂等 install no-op，委托身份断言（assertSame(prior, delegate)）必然失败；属测试间污染而非产品语义缺陷（267 中仅此 1 失败，其余 deadline 语义/端到端用例全绿）。修复 = 测试内将槽位归一化到底层非 wrapper base（沿 delegate 链解包）后断言安装期委托，顺序无关；复跑 267/267 全绿。回归覆盖即修复本体（断言从顺序敏感改为顺序无关），同类污染风险由本日志登记；产品代码零改动。
- 语义复核：三 Phase 全过——Phase 1 wrapper 安装/委托/透传/过期中止/白名单外注入/1000ms fail-closed/FAST=min(20ms,规则值) 与 live 代码逐点核对；Phase 2 超时独立计数 + 不进失败连击 + 超时率>1% run 级警告（`shouldWarnTimeoutRate` 严格大于）+ 深度 32 走脚本失败路径；Phase 3 `TestXScriptDeadlineEndToEnd` 死循环夹具 deadline 内可控终止（零诊断/不进 `disabledRuleIds`/他规则诊断不受影响）——plan 2137-2 登记的 timeout watch-only residual **实证消除**；接线验证（Minimum Rules #23）由 `expiryMidLoopProvesPerBackEdgeInterception` + 端到端终止证明；零平台改动经 git diff 核实（仅 nop-lint 模块 + docs）。
- Roadmap 回写：item 15 `planned` → `done`（closure audit approved）；items 16–18 未受扰动（均 `todo`）。frontmatter `status: active` 保持不写 `completed`（ledger 派生状态）。
- Doc-sync: plan 文件（Closure Gates prose 消解 + Verification pass 行 + Closure 收口对）+ roadmap item 15 + 本日志条目；同 run 草拟的兄弟 plan（item-16 EditCalculator / item-17 Suppression v1）随本次 docs 提交入库。

### nop-lint roadmap item 15：Deadline 执行器路线 A 落地（plan `2026-09-22-0128-1-deadline-executor` 全 Phase 执行）

- Plan: `ai-dev/plans/nop-lint/2026-09-22-0128-1-deadline-executor.md`（work-item item-15；本条目为执行记录，closure audit 由下游 CLOSURE_AUDIT 步骤执行）。
- **落地内容**（全部在 `nop-lint/nop-lint-core`，零平台改动，git status 核实仅 nop-lint 文件变更）：新组件 `xscript/LintDeadlineExecutor`（路线 A 全局 executor 包装）、`xscript/XScriptDeadline`（record + 三个 scope-local key 常量 `__lintDeadlineNanos`/`__lintRuleId`/`__lintBudgetMs`）、`xscript/XScriptTimeoutException`（extends NopLintException，栈迹抑制）；改造 `XScriptEngine`（四参 `executeMatch` 在 invoke 前 scope 注入，三参路径不注入、直接调用方语义不变）、`CompiledRule`（携带 `xscriptTimeoutMs`）、`LintProfile`（`xscriptBudgetMs`：FAST=min(20ms,规则值)/STANDARD=规则值）、`LintEngine`（lint 入口幂等 `install()`）、`RuleSetRunner`（每 match 构造 deadline + 超时/失败分流 + 超时率>1% run 级警告 `nop.lint.xscript.timeout-rate-high`）、`LintStats`（新计数域 `xscriptTimedOutMatches`）、`RuleDslParser`（`xscriptTimeoutMs` 解析期 fail-closed 校验：非数值/非正数/>1000ms 拒绝，消息含规则 id，顺带封堵原 `intDefault` 对非数值静默取缺省的洞）。
- **关键裁定（已回写 design 07 §4 增注）**：wrapper 委托语义 = 无 deadline 逐字透传（被包装者按平台惯例自身下传，语义与未安装完全一致）；有 deadline 以 `expr.execute(this, rt)` 把**自身**下传（平台 executor 惯例，cf. `DefaultExpressionExecutor`/`DebugExpressionExecutor`）——执行中修正：初版把 `delegate` 下传导致嵌套节点绕过 wrapper（死循环测试挂起暴露），平台事实是嵌套执行用**入参 executor** 而非重新取全局，唯有自传才能获得每节点/每循环回边拦截点。deadline keys 为宿主侧注入值，不进编译白名单（脚本引用即 unresolved-identifier 编译失败，测试钉死）。
- **接线证据（Minimum Rules #23）**：`TestLintDeadlineExecutor.expiryMidLoopProvesPerBackEdgeInterception`——顶层入口通过（deadline 未到）后于循环中途过期，中止只能来自嵌套入口（每次循环回边 `WhileExecutable.passTest`/body 均经 wrapper 再检查）+ 死循环端到端 `TestXScriptDeadlineEndToEnd`（夹具 → `LintEngine.lint` 在 25ms 预算内可控终止，plan 2137-2 `Deferred But Adjudicated` 登记的 timeout watch-only residual 由此**实证消除**）。
- **深度上限 32 载体裁定**（plan 预授权"载体执行时裁定并记录"）：XLang v1 xscript 白名单无用户可定义递归构造，深度计采用**嵌套表达式链**驱动同一 executor 钩子路径（`node.kind()+1+1+...` 40 级，引用 scope 变量防静态折叠）；语义 = deadline 作用域内 per-thread 嵌套计深（finally 回退平衡），超限走脚本失败路径显式中止 + 计入 `xscriptFailedMatches`（非超时路径）。
- **偏差**：无实质 scope 偏差；`RuleDslParser` 非数值 `xscriptTimeoutMs` 从"静默取缺省 100"收紧为 fail-closed 解析错误（超额校验项的自然延伸，符合 fail-closed 口径）。
- **验证**：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0；`./mvnw -pl nop-lint/nop-lint-core -am clean package -DskipTests -T 1C` BUILD SUCCESS；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS **267 tests, 0 failures**（含回归 `TestXScriptRules` 8/8、`TestRuleSetRunner` 3/3、`TestXScriptEngine` 18/18 全绿——deadline 路径不改变既有命中/过滤/异常语义）；新增测试 24 个：`TestLintDeadlineExecutor`(10) + `TestXScriptTimeoutSemantics`(8) + `TestXScriptDeadlineEndToEnd`(2) + `TestRuleDslParser` 预算边界(+6，缺省100/1000边界/1001拒绝/非正拒绝/非数值拒绝/默认值)。mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-1/2137-2/2137-3 同一裁定）；辅助门禁 `scan-hollow-implementations.mjs --module nop-lint/nop-lint-core --severity high` exit=0、`check-doc-links.mjs --strict` exit=0。
- Roadmap 回写：item 15 `todo` → `planned`（draft review 已过，`done` 待 closure audit 通过后由 CLOSURE_AUDIT 步骤翻转）；items 16–18 核对未受扰动（均 `todo`）。
- Doc-sync: `ai-dev/design/nop-lint/07-xscript-engine.md` §3（deadline/深度落地口径）+ §4（路线 A 实施裁定）增注；roadmap item 15；本日志条目。plan 三 Phase checkbox 全部勾选、frontmatter `status: active` 保持（ledger 派生状态，不写 `completed`）。

### nop-lint plan "首批 10 条核心规则" 独立 closure audit：APPROVED，roadmap item 11 翻 `done`、M2 派生翻 `done`

- 独立 auditor（fresh closure visit，opencode-pid-96490，model zhipuai-coding-plan/glm-5.3-flash）：CLOSURE_SCRIPT_CHECK 报 4 项（`ledger-structure-invalid` + `frontmatter-fields-invalid` + `missing-pass:test` + `no-audit-receipt`），本 visit 全部消解——`## Closure Gates` 12 个列 0 checkbox 为计数域外违规（01-file-ledger §2.5），按 §4.3 消解先例改为纯 prose（同 plan 1420-1 / 2137-1 / 2137-2 处置）；frontmatter 移除 ledger 子集外的 `last-reviewed` 字段；`## Verification` 补 pass 行、`## Closure` 补 dispatch/accepted 收口对（#audit-2026-09-21-142035-mission-driver-2026-09-21-2137-3-core-rules-first-batch-1-dc6bf921，含 `models={exec,aud}` lineage 后缀）。`plan-check.mjs --strict` 重跑 exit=0、derivedCompleted=true（27/27 勾选）。
- **亲测复跑**全套验证命令：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0、`clean package -DskipTests -T 1C` exit=0、`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS（**242 tests**，0 failures）；规则模块显式验证 `./mvnw -pl nop-lint/nop-lint-nop -am test -T 1C` exit=0（**10 tests**，0 failures，含 TestNopRuleSuites 9 用例）；mission `commands.lint` 按配置 exit=0（裸 checkstyle 的 nop-api-core 9226 处违规为全仓遗留基线，裁定记录于 plan `## Verification`，与 1420-1 / 2137-1 / 2137-2 同一裁定）。
- 辅助门禁：`scan-hollow-implementations.mjs --severity high` 于 nop-lint-core 与 nop-lint-nop 双模块 exit=0（0 findings）；`check-doc-links.mjs --strict` exit=0（0 errors）。
- Anti-Hollow 通过：主资源 `.rule.yml` → VFS 加载（xdef + register-model 管线）→ suite `x:extends` 指针 → `RuleTestRunner` → `LintEngine.lint` → `.expect` 逐条断言端到端连通；`TestNopRuleSuites` 断言 suite 发现集精确等于 7 条预期 id（缺 suite 即红 fail-closed）；xscript 过滤路径为真断言（no-empty-catch 注释空体豁免、ibiz 注解过滤 valid 夹具——调试期 mods bug 曾使其真实变红，非恒真）。
- 语义复核：27/27 计数域 checklist 与 live repo 一致——7 条规则文件 + 21 夹具 + 9 测试实测存在；design 01 §2（消息插值）/§3.5（`throw new $$$($$$)`）/§4、design 02 §1（分类裁定增注）、design 04 §1（Effective node 措辞）修订在案；cross-check 对照表（唯一 delta=嵌套空块→item 23）与 10/10 分类表、承接项裁定记录于本日志上文条目；deferred 分类诚实（3 条 successor 均有 v1 不可保真原因 + successor path，无 in-scope defect 降级）。
- Roadmap 回写：item 11 `planned` → `done`（closure audit approved，7 落地/3 successor 注记保留）；**Milestone M2 `todo` → `done`**（unlock 条件 items 8–13 全 done 达成）；Wave 3（items 14–18）未受扰动（14 `done`，15–18 `todo`）。frontmatter `status: active` 保持不写 `completed`（ledger 派生状态）。
- Doc-sync: plan 文件（Closure Gates prose 消解 + Verification pass 行 + Closure 收口对 + frontmatter 字段收敛）+ roadmap item 11/M2 + 本日志条目。

### 2026-09-22（nop-lint item 11：首批 10 条核心规则 — Phase 1 分类裁定 + Phase 2 落地 + Phase 3 cross-check）

- Plan: `ai-dev/plans/nop-lint/2026-09-21-2137-3-core-rules-first-batch.md`（roadmap item 11）

#### 执行前核对

- 前置状态核对：roadmap item 10（RuleTester）`done`（plan 2026-09-21-2137-1，closure audit approved 2026-09-21）；item 14（xscript v1）`done`（plan 2026-09-21-2137-2）；item 12（L1 DeclTypeResolver）`done`。本 plan 执行顺序依赖（同组 plan 1/2 先行）已满足，无阻塞。

#### 10 规则分类表（检测语义 / 需求来源 / 最小能力 / v1 形态）

| # | 规则 | (a) 检测语义 | (b) 需求来源 | (c) 最小能力 | (d) v1 形态 |
|---|------|-------------|-------------|-------------|------------|
| 1 | exception/no-raw-exception | 裸 `throw new RuntimeException/Exception/Throwable(...)` | ast-grep `java-lint-bare-runtimeexception.yml` + design 01 §2 + AGENTS.md 异常约定 | 单层 any + pattern | **pattern-only**（any 3 分支）→ 落地 |
| 2 | exception/no-empty-catch | catch 块体为空（无任何处理） | ast-grep `java-lint-empty-catch.yml`（kind catch_clause + has '{}'） | kind 匹配 + 结构空判定 | **kind+xscript** → 落地；落地形态偏差：plan 预期的 pattern `catch ($E) { }` 不是可独立解析的代码片段（tree-sitter 恢复为 program → [ERROR(catch…), block]，effective 根永不到达 catch_clause，实测零匹配），改用 `kind: catch_clause` + xscript 判 body 具名子节点数为 0——严格更精确（仅注释的空体因具名节点非空而天然不报，契合原规则 message 对"注释=有意忽略"的豁免）；已裁定 delta：ast-grep 版还会命中 catch 内嵌套空块（`has` 关系算子 = item 23），v1 不追 |
| 3 | exception/silent-swallow | catch 块内既不 rethrow 也不经 ErrorCode 载体传播 | `check-silent-swallow.mjs`（GOOD_SIGNALS 七信号缺失判定） | `not`+`has` 关系算子 + 注释/字符串掩码 | **successor → item 23**：faithful 语义需后代模式搜索 + 否定 + 掩码；xscript text.contains 近似存在注释/字符串旁路（mjs 自身的 comment-bypass fixture 正是防它），不得以高误报近似冒充落地 |
| 4 | exception/errorcode-param-consistency | throw 站点 `.param(key,...)` 覆盖 ErrorCode 描述中的 `{placeholder}` | `check-error-param-consistency.mjs`（注册表构建 + define-face + throw-site 解析） | 跨文件 ErrorCode 注册表分析 | **successor → item 28**（check-*.mjs 迁移 manifest）：需解析 `*Errors.java` ARG_* 注册表并跨文件解析常量，超出 v1，items 22/23 也不足 |
| 5 | exception/no-log-getmessage | catch 内使用 `e.getMessage()` 且未 rethrow/未传异常对象 | ast-grep `java-lint-getmessage-only.yml`（has + not） | `has`+`not` 关系算子 | **successor → item 23**：faithful 语义需后代匹配 + 否定；v1 xscript 无法枚举后代（`descendant` 仅首个）也无否定原语，text.contains 近似有注释/字符串旁路 |
| 6 | api/ibiz-missing-annotation | `I*Biz` 接口（extends ICrudBiz）方法缺 @BizQuery/@BizMutation/@BizAction | `check-ibiz-interfaces.mjs` | kind 匹配 + 祖先域接口名/extends/注解检查 | **pattern(kind)+xscript** → 落地；已裁定 delta：mjs 按文件名 `I[A-Z]*Biz.java` 圈定、本规则按接口名 `^I[A-Z]\w*Biz$` 圈定（接口名更本质；二者在"文件名=接口名"惯例下等价） |
| 7 | api/ibiz-missing-context | `I*Biz` 接口方法末参必须为 IServiceContext | `check-ibiz-interfaces.mjs` | kind 匹配 + 祖先域参数列表检查 | **pattern(kind)+xscript** → 落地；与 mjs 同口径：仅统计 `formal_parameter`（varargs `spread_parameter` 不计入，无参视为违规） |
| 8 | api/bizmodel-dao-access | CrudBizModel 子类内直接调用 `dao().<method>(...)` 绕过统一流程 | design 00 §2.2 示例 + service-layer.md 反模式表 | pattern + 超类文本过滤 | **pattern+xscript**（pattern `dao().$METHOD($$$ARGS)`；xscript 检查最近 class_declaration 的 `superclass` 文本含 CrudBizModel）→ 落地；已裁定 delta：仅直接 extends 可检（间接继承需 L2 = item 26）；`this.dao()` 合格接收者形态不在 pattern 内（需 item 23 组合） |
| 9 | api/bizmodel-safe-api | CrudBizModel 子类内 `dao().getEntityById/findAllByQuery/saveEntity` 三个绕过调用 | service-layer.md 反模式表（`dao().getEntityById(id)` / `dao().findAllByQuery(query)` / `dao().saveEntity(entity)` 三模板） | pattern any + 超类文本过滤 | **pattern+xscript** → 落地（与 #8 的关系：#8 全量 dao() 访问、#9 钉住三个规范绕过调用，消息指向安全 API，独立抑制粒度） |
| 10 | nop/no-vfs-violation | 直接文件 IO：java.nio.file import、`new File/FileInputStream/...`、`Files.*` 静态调用、`Paths.get/Path.of`、`.toPath()/.relativize()` | `check-vfs-violations.mjs`（PATTERNS 全表） | 多分支纯 pattern（单层 any） | **pattern-only**（any ~30 分支，含 fully-qualified `java.nio.file.Files.*` 变体）→ 落地；已裁定 delta：更深限定链（如 `a.b.Files.readString`，非 `java.nio.file` 前缀）不命中（需 item 23）；mjs 的按文件白名单属抑制域（item 17）；mjs 的 FILES-SIZE 文本误报在 AST 匹配下天然消除（改进项） |

落地清单（7）：no-raw-exception / no-empty-catch / ibiz-missing-annotation / ibiz-missing-context / bizmodel-dao-access / bizmodel-safe-api / no-vfs-violation。
successor 登记（3）：silent-swallow → item 23；no-log-getmessage → item 23；errorcode-param-consistency → item 28（详见 plan `Deferred But Adjudicated`）。

#### 承接项裁定

- **plan 03 deferred（design 01 §3.5 示例）**：`pattern: throw new $$$` 已改为可解析形态 `throw new $$$($$$)`（plan 03 实验证明其解析干净，多子 ERROR 形态 `throw new $$$` 被编译器依 04 §8 P0 拒绝）。
- **plan 03 follow-up（提取公式措辞）**：design 01 §4 步骤 4 与 design 04 §1 Effective node 行已修订为"自根沿唯一子节点链下降，遇多子 Internal 或叶即停"，与 plan 03 设计偏差声明 1 及落地实现一致（item 7 已 done，措辞修订随本 plan 执行）。
- **plan 1420-1 移交（消息插值）**：裁定 **v1 不引入 `{{VAR}}` 捕获插值**，`message` 按字面输出；动态消息经 xscript `report()` 字符串拼接覆盖（本批 ibiz 两规则即用此形态）。结论已回写 design 01 §2 消息字段注。本批规则消息均按该结论编写。

#### 验证（Phase 1）

- `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0（基线回归绿，242 tests）。
- `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors；13 warnings 均为既有计划文件的历史遗留，非本 plan 引入）。
- Doc-sync: design 01 §2/§3.5/§4、design 02 §1、design 04 §1 已更新；分类表与裁定记录于本日志。

#### Phase 2 落地与验证

- 落地 7 条规则于 `nop-lint/nop-lint-nop/src/main/resources/_vfs/nop/lint/rules/{exception,api,nop}/*.rule.yml`（xdef 校验 + `lint.register-model.xml` 管线加载）。
- 夹具：7 个 suite × (1 valid + 2 invalid + `.expect`)，位于 `nop-lint/nop-lint-nop/src/test/resources/_vfs/test/lint/suites/`；suite 规则文件为 `x:extends` 指针（零重复，夹具驱动的就是主资源规则本体），`TestNopRuleSuites` 断言 suite 发现集精确等于 7 条预期 id（缺 suite 即红，fail-closed）+ 主资源规则独立可加载 + suite/主资源规则内容一致。
- xscript 过滤路径为真断言（非空洞）：调试期 mods 取值 bug 曾使 ibiz valid 夹具（含已注解方法）真实变红、no-empty-catch 的"仅注释空体不报"valid 夹具依赖 xscript 判定——过滤路径不满足时测试确实失败。
- 执行链显式记录：`-pl nop-lint/nop-lint-core -am test-compile -T 1C` exit 0；`-pl nop-lint/nop-lint-core -am clean package -DskipTests -T 1C` exit 0；checkstyle 未配置为 nop-lint 门禁（ad-hoc `checkstyle:check` 在未触碰的上游模块 nop-api-core 报 9226 个既有违规）；`-pl nop-lint/nop-lint-core -am test -T 1C` exit 0（242 tests）；`-pl nop-lint/nop-lint-nop -am test -T 1C` exit 0（10 tests，mission `test` 键不覆盖该模块，故显式运行）。

#### Phase 3：ast-grep 行为 cross-check（同语料对照表）

- 语料：`_tmp/lint-crosscheck/`（4 个 Java 文件：BareThrows / EmptyCatches / GetMessageOnly / MixedClean，覆盖命中与非命中形态）。语料为一次性 scratch，运行后删除——避免 repo 级 mjs 门禁（如 bare `check-silent-swallow.mjs`）把语料命中误计为 invariant 违规；对照表为本 plan 的持久记录。ast-grep CLI：`ai-dev/tools/node_modules/.bin/sg scan`（0.43.0，经 `sgconfig.yml` + `--filter`）。
- nop-lint 侧：同语料经 `LintEngine` 加载 nop-lint-nop 主资源规则运行（no-raw-exception + no-empty-catch；getmessage-only 无落地规则，见 successor 裁定）。

| 吸收规则 | 语料文件 | ast-grep 命中（行号） | nop-lint 命中（行号） | 结论 |
|---|---|---|---|---|
| bare-runtimeexception ↔ no-raw-exception | BareThrows.java | 6, 10, 14 | 6, 10, 14 | **对齐**（3/3，含 0-arg/1-arg/2-arg 构造形态；NopException/IllegalArgumentException 非命中双方一致） |
| bare-runtimeexception ↔ no-raw-exception | MixedClean.java | 无 | 无 | 对齐 |
| empty-catch ↔ no-empty-catch | EmptyCatches.java | 8, 39 | 8 | **1 处已裁定 delta**：行 39（catch 内嵌套空块）ast-grep 命中、nop-lint 不命中——relational `has`（item 23），分类表已裁定不追；直接空体（行 8）双方命中；仅注释空体（~行 28）双方均不命中（nop-lint 经 xscript 具名子节点判定对齐 ast-grep 语义）；含日志/rethrow 的 catch 双方均不命中 |
| getmessage-only ↔ （successor，未落地） | GetMessageOnly.java | 8, 16 | —（无规则） | **successor 证据记录**：ast-grep 语义（has getMessage 且非 has throw）确需 `has`+`not`（item 23）；该行为继续由 ast-grep CLI 门禁承担 |
| getmessage-only ↔ （successor，未落地） | MixedClean.java | 无 | — | 对齐（零命中） |

无未解释差异：唯一 delta（嵌套空块）与两条 successor 裁定均已在 Phase 1 分类表登记。

#### Phase 3：Follow-up 登记（承接 plan 05 deferred）

- **ast-grep 多规则多语料矩阵 perf 复测**：触发条件——item 11 落地后（已完成）nop-lint 规则面首次具备与 ast-grep 多规则矩阵对比的代表性（7 条落地规则 + 3 条 successor 待补）。实施归属：非本 plan；待 item 23 落地补齐 successor 规则后、或下一轮 perf 基线修订（roadmap item 31 deep profile / item 13 基线体系）时一并实施。Non-blocking：现有 JMH 基线（item 13）已覆盖规则匹配热路径，矩阵复测是扩展测量而非正确性缺口。

#### roadmap 回写与 M2 派生核对

- roadmap item 11：`todo` → `planned`，注记执行结果（7 落地 / 3 successor）。按 plan 收口项的条件句，`done` 在独立 closure audit 通过后按实际落地清单回写（closure audit 为 mission 下游 CLOSURE_AUDIT 步骤，不在本执行步内自审自封）。
- M2 派生核对：items 8/9/10/12/13 均 `done`，item 11 `planned`（待 closure audit）→ M2 保持 `todo`（派生值未被手写）。Wave 3（items 14–18）状态未受扰动（14 `done`，15–18 `todo`）。

#### 最终验证（全 Phase 收口）

- `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0（242 tests）。
- `./mvnw -pl nop-lint/nop-lint-nop -am test -T 1C` 退出码 0（10 tests）。
- Commit: `b613c933e4`（规则+夹具+测试+pom+hook 夹具豁免）、`f8d6d66233`（design/roadmap/plan/log）
- Doc-sync: design 01 §2/§3.5/§4、design 02 §1、design 04 §1 已更新；roadmap item 11 已回写；本日志为持久 cross-check 记录。`docs-for-ai/` 无需更新裁定：nop-lint 为在研模块，其设计权威在 `ai-dev/design/nop-lint/`，docs-for-ai 尚无该模块使用文档。
- 附带改动：pre-commit hook 与新增 `.ast-grepignore` 排除 RuleTester 夹具树——invalid 夹具是有意违规的测试数据，其违规由 nop-lint 自身 suite 门禁断言；`.ast-grepignore` 对显式传参路径不生效，故 hook 内同时做了路径过滤。
