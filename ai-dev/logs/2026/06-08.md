# 2026-06-08 Dev Log

### 执行 Plan 126: nop-ai-shell 执行引擎与 IO 层重写

- Plan 126 完成：将 nop-ai-shell 从 TaskExecutionGraph 依赖 + 字符串 EOF 标记 + 管道接线 bug + BackgroundExpr 空实现 + FileShellOutput 截断 bug 重写为纯 AST 解释器 + ShellChunk 流式 IO + 有界队列管道
- Phase 1 — ShellChunk 类型体系 + IO 层全面重写：新增 `ShellChunk`（TextChunk/BinaryChunk/EofChunk）+ `AbstractShellInput`（readLine/readAllText/lines 实现）+ 重写 `IShellInput`/`IShellOutput`/所有 IO 实现类 + `FileShellOutput` 用 BufferedWriter 修复截断 bug + `ExecutionResult` 提取为独立顶层类 + 适配 EchoCommandTest/CdCommandTest
- Phase 2 — ShellCommandExecutor 重写：移除 TaskExecutionGraph/IExecution 依赖，executePipeline 改用 CompletableFuture.supplyAsync 并发 + BlockingQueueShellOutput 有界队列流式传输，executeBackground 实现 fire-and-forget（backgroundJobs Map + jobId），executeGroup/executeSubshell 改用 whenComplete 恢复环境，实现 Closeable 接口
- Phase 3 — ICommandChecker + ExternalCommandAdapter：新增 checker/adapter/checkcontext 包，CheckVisitor 遍历 AST 做全量预检（exitCode 126 拒绝），外部命令骨架抛 UnsupportedOperationException（exitCode 127）
- Phase 4 — 测试恢复：移除 @Disabled，修复变量展开测试，新增 20 个测试（管道端到端、Background、环境隔离、close 取消、预检拒绝/通过、DefaultCommandChecker、ExternalCommandAdapter）
- 偏差：原计划 testCloseCancelsBackgroundJobs 使用 CountDownLatch，实际改为 Thread.sleep + AtomicBoolean（更可靠检测中断）；原计划 cmdContext 用匿名内部类（hasFlag 返回 false），实际改用 DefaultShellExecutionContext（正确解析 -l 等标志）；原计划 collectOutput 在 close 之前调用导致阻塞，实际调整为先 close 再 collectOutput
- 关键经验：异步测试必须加类级别 @Timeout + 每个 Future.get() 带超时 + close() 必须在 readAllText() 之前。已补充到 `docs-for-ai/02-core-guides/testing.md`
- 验证：`./mvnw test -pl nop-ai/nop-ai-shell -am` — 191 tests, 0 failures, 0 errors
- doc-sync：更新了 `docs-for-ai/02-core-guides/testing.md`（新增"异步与并发测试的防挂起规则"段落）

### 执行 Plan 127: ReflectionManager Record 类型支持

- 新增 `RecordBeanModelBuilder.java`：通过反射调用 `getRecordComponents()` 发现 record 属性，通过 `MethodModelBuilder.from()` 创建 canonical constructor 的 FunctionModel，设置 immutable + dataBean + serializable
- 修改 `ClassModel.getBeanModel()`：增加 `isRecordClass()` 路由分支（通过反射调用 `isRecord()`，兼容 Java 11 编译目标）
- 修改 `ReflectionManager.getConverterForJavaType()`：增加 `isRecordClass()` 判断，使 record 走 `DataBeanTypeConverter` 路径
- `nop-kernel/nop-core/pom.xml` 增加 `maven-compiler-plugin` 测试编译配置（`<release>16</release>`），使测试代码可使用 record 语法
- `TestRecordReflection.java` 编写 12 个测试：BeanModel 属性、实例化、属性读写、immutable/dataBean、JSON 序列化/反序列化/往返、注解传播、泛型 record、零 component record、紧凑构造函数、类型转换器
- 关键实现细节：record 的 canonical constructor 修饰符为 0（包私有），被 `ClassModelBuilder.discoverConstructors()` 过滤掉，因此 `RecordBeanModelBuilder` 直接通过 `Class.getDeclaredConstructors()` 查找
- `./mvnw test -pl nop-core -am` BUILD SUCCESS，225 tests passed, 0 failures
- 独立 closure audit 通过，所有 exit criteria 和 closure gates PASS

---

### 拟制 Plan 126: nop-ai-shell 执行引擎与 IO 层重写

- 完成设计文档审查修复（上一轮 audit 的 5 CRITICAL + 9 IMPORTANT + 3 NEW issues 全部修复）
- 拟制 `ai-dev/plans/126-nop-ai-shell-implementation.md` 执行计划，4 个 Phase：
  - Phase 1: ShellChunk 类型体系 + IO 层全面重写 + 命令适配（原子提交，避免编译断裂）
  - Phase 2: ShellCommandExecutor 重写（移除 TaskExecutionGraph，纯 AST 解释器）
  - Phase 3: ICommandChecker + ExternalCommandAdapter（预检 + 外部回退骨架）
  - Phase 4: 测试恢复与新增（移除 @Disabled，新增管道/Background/环境隔离/close 测试）
- 经历两轮子 agent 对抗性审查：第一轮发现 2 Blocker + 6 Major → 全部修复；第二轮发现 1 Major + 2 Minor → 全部修复
- 关键设计裁定：保留 `print()`/`println()` 方法名（兼容现有命令）、`AbstractShellInput` 抽象基类实现 `readLine()`、`backgroundJobs` 用 Map<jobId, Future>、`ICommandChecker.check()` 返回 String、`ExternalCommandAdapter` 不依赖 nop-shell 模块
- 更新 `ai-dev/design/nop-ai-shell/02-io-and-pipeline.md`：`writeText()` 改为 `print()`，修正 `println` 注释
- 更新 `ai-dev/design/nop-ai-shell/03-executor-and-async.md`：`writeText()` → `print()`，旧 stdin 模式移除，section 重编号

---

### 执行 Plan 125: Bash 语法解析器 Bug 修复与补全

- **Phase 1 - Lexer Bug 修复**：`BashLexer.tryMatchOperators()` 重构，3 字符匹配块移到 2 字符块之前，拆分 `<&`/`>&` 为独立分支，移除死代码和冗余判断
- **Phase 2 - Redirect.parse() 实现**：新增 `Redirect.parse(String)` 静态方法，基于正则解析 `2>&1`、`&>file`、`>>file` 等重定向字符串，`RedirectTest.java` 12 个测试全部通过
- **Phase 3 - 设计文档对齐**：`04-bash-syntax.md` 更新 LogicalExpr 优先级数值、formatOperand 结合性逻辑、移除 PipelineExpr.redirects、替换 Redirect.parse() 方法体
- **Phase 4 - Parser trailing & 修复**：从 `parsePrimary()` 三个分支移除 BACKGROUND token 处理，统一到 `parseSequence()` 最外层检查；新增 8 个测试覆盖 here-string、merge-append、fd-redirect、export envVar、trailing & 等场景
- **验证**：`./mvnw test -pl nop-ai/nop-ai-shell -am` BUILD SUCCESS，181 tests passed, 0 failures

---

### 执行 Plan 124: nop-ai-agent 存储结构 ORM 模型落地

- `nop-ai/model/nop-ai.orm.xml` 全面重写，变更如下：
  - **Phase 1 - 包名修正**：`ext:basePackageName` 从 `nop.ai` 改为 `io.nop.ai`，所有 entity name/className/refEntityName 同步更新，无残留旧包名
  - **Phase 1 - 方言声明**：新增 `ext:dialect="mysql,oracle,postgresql"`、`ext:mavenVersion`、`ext:platformVersion`（与 nop-job/nop-auth 等对齐）
  - **Phase 1 - Java 目录迁移**：5 个模块（nop-ai-dao、nop-ai-service、nop-ai-app、nop-ai-codegen、nop-ai-web）的 Java 源文件从 `src/*/java/nop/ai/` 迁移到 `src/*/java/io/nop/ai/`，package 声明和 import 语句批量更新
  - **Phase 1 - Codegen 重新生成**：删除旧 `_gen/` 文件，通过 `mvn clean install` 触发 codegen 重新生成 22 个实体类（含新增的 6 个 Agent 运行时表）
  - **Phase 2 - Dict 重命名**：9 个 dict 从 snake_case 改为 kebab-case（`ai/message_type` → `ai/message-type` 等）
  - **Phase 2 - 新增 Dict**：6 个新 dict（`ai/session-status`、`ai/todo-status`、`ai/todo-priority`、`ai/input-delivery`、`ai/event-type`、`ai/compaction-type`），全部 `valueType="int"`
  - **Phase 2 - Dict 扩展**：`ai/message-type` 从 2 种扩展到 8 种，改为 `valueType="int"`
  - **Phase 2 - Domains**：新增 `sessionId`、`messageId`、`version`、`createTime`、`createdBy`、`updateTime`、`updatedBy`、`remark`、`boolFlag` 等 domain
  - **Phase 3 - Agent 运行时表**：新增 6 张表（NopAiSession、NopAiSessionMessage、NopAiSessionInput、NopAiSessionContext、NopAiTodo、NopAiEvent）+ NopAiProject 补充 runtimeMetadata 字段
  - **Phase 3 - ORM 循环依赖修复**：NopAiSession 的 `to-one context` 改为 `to-many contexts`（因为 context 有多个 revision）
  - **Phase 3 - 审计字段**：所有实体手动添加 version/createdBy/createTime/updatedBy/updateTime，使用 domain 引用；NopAiEvent 为 append-only 例外
  - **Phase 3 - 现有表审计字段**：原有 16 张 AI Coder 表也补充了审计字段（domain 引用），与平台规范对齐
  - **Phase 4 - 设计文档验证**：确认 `nop-ai-agent-session-and-storage.md` §16-§17 与 orm.xml 一致，权威声明已存在，README.md 无需更新
- **验证**：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS，全部测试通过

---

### 分支亲和调度设计文档

- 新建设计文档：`ai-dev/design/nop-ai-agent/nop-ai-agent-branch-affinity-scheduling.md`
  - 核心决策：**Git Branch 作为调度亲和单元**
  - 调度单元 = (project, branch)，同一分支同时只在一个节点的一个进程内操作
  - Agent 是分布式开发者——复用 git 的分布式协作模型
  - 双环境共存：Java + Node.js 工具链在同一 session 中共存
  - 子 Agent 按需隔离：默认共享工作目录，探索性任务可选 git worktree
  - 提交与合并：Agent 不自动 push 到主干，小步提交，合并需人工/Merge Agent 触发
  - **文件快照系统**：基于 JGit 的独立外部 git 方案（opencode/cline 验证），tree hash 无 commit，零侵入
  - **Snapshot vs Worktree 关系**：Snapshot 解决 undo/回滚（Layer 2 必须），Worktree 解决并行隔离（Layer 4 可选）
  - Worktree 详细设计：外部存储（`{data_dir}/ai-worktrees/`）、命名分支（`-B`）、Symlink 大目录、Fast Resume、Slug 验证、Orphaned 回收
  - 拒绝了 8 项：分布式文件系统协调、Session 级调度、跨节点 Worktree、Agent 自动 Merge、项目 git 做快照、项目内 Worktree、Detached HEAD、依赖系统 git CLI
  - 对分层架构的影响：Layer 2 新增 `IFileSnapshotService`，Layer 3 新增 `IBranchAffinity`，Layer 4 新增 Worktree 生命周期管理器
  - 数据模型：Affinity Table + Worktree Registry（扩充字段）+ Snapshot Record（嵌入 ai_session_message）
- **自审查第 1 轮并修复 9 个问题**：
  - 🔴 §3.5 表头误写"项目内 git worktree"→ 改为"独立工作目录"（与外部存储决策一致）
  - 🟡 §3.4.4 理由矛盾：说"不影响 worktree 列表"但 linked worktree 必然可见 → 修正理由，补充说明 git 元数据注册行为
  - 🟡 §3.3.3/§3.4.3/§3.4.6 使用 git CLI 语法但决策是 JGit → 添加注释说明为概念性操作
  - 🟡 §3.6.1 分支命名不一致：`explore/{session-id}/{task}` vs `agent/explore/{slug}` → 统一为 `agent/worktree/{slug}`
  - 🟡 §3.5 场景矩阵说"各自快照"但未说明 worktree 的 snapshot 如何工作 → 新增"Worktree 中的快照"说明
  - 🟡 §4.2 快照分层归属不一致 → 统一为 Layer 2 执行扩展
  - 🟢 §5.6 opencode 描述不准确 → 修正为"由用户/API 指定目录"
  - 🟡 §3.1.3 调度器步骤 3 使用 git CLI 且缺失败处理 → 改用 JGit 概念 + 添加切换失败分支
- 更新了 `ai-dev/design/nop-ai-agent/README.md` 注册新文档

---

- 完成 OpenHarness 核心安全防护机制的源码级分析，更新到 `ai-dev/analysis/agent-survey/2026-06-08c-openharness-survey.md`
  - 新增 §七 核心安全防护机制（~350行），覆盖三大子系统：

#### 工作循环防护（query.py, 1057行）

- `max_turns` 双默认值：`run_query`=200, `QueryEngine`=8
- `MAX_SAFE_COMPLETION_TOKENS=128K` — per-request output token cap
- 17 种 prompt-too-long 错误模式检测
- 三级恢复策略：completion limit 适配 → reactive compact（force=True）→ 终止
- 空 assistant 消息检测 + 终止
- `return_exceptions=True` — 多工具并发执行异常隔离
- Tool output offloading — 大输出写文件，返回 preview

#### 上下文压缩（compact/__init__.py, 1725行）

- 四级压缩瀑布：microcompact → context collapse → session memory → full LLM summarization
- Auto-compact 阈值：`context_window - 20K(reserved) - 13K(buffer)` ≈ 167K for 200K context
- `MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES=3` — 连续失败保护
- Tool pair 保护 — 压缩边界不切断 tool_use/result 对
- Reactive compact — API 返回 prompt too long 时紧急压缩
- PTL retry — 压缩请求本身 prompt too long 时截断旧消息重试（最多 3 次）
- 8 类 `CompactAttachment` — 压缩时保留 focus/artifacts/skills 等状态

#### 权限检查（permissions/checker.py, 200行）

- 硬编码敏感路径保护（`.ssh`/`.aws`/`.gcloud`/`.azure`/`.gnupg`/`.docker`/`.kube`）— 不可覆盖
- 三级权限模式：`default`（读写分离确认）/ `plan`（只读）/ `full_auto`
- 用户自定义 path rules（glob）+ command deny patterns
- Bash 安装命令友好提示

#### 运行时 Focus 追踪（query.py）

- 9 类 tool_metadata 追踪器（read_file_state, invoked_skills, async_agent_state, recent_work_log 等）
- `_append_capped_unique()` — 新值推入、重复移到末尾、超限截断
- 压缩时通过 CompactAttachment 注入到新对话

#### Nop 设计建议

- `call-agent` 支持 `maxTurns` 参数，默认 20
- Layer 2 实现 microcompact + context collapse，Layer 3 可选 LLM summarization
- 内置敏感路径保护，不可被 Delta 定制覆盖
- `ai_session_context` 表存储 focus 追踪数据
- 压缩附件保留 goal/artifacts/skills 等关键状态

---

### 上下文

- 累计分析 5 个框架的存储/安全机制：opencode v2, Claude Code, cline, agent-zero, oh-my-claudecode, OpenHarness
- OpenHarness 调研文档从 §一~§五（存储/Worktree）扩展到 §七（安全防护）+ §八（框架对比）
- 核心源文件：
  - `~/ai/OpenHarness/src/openharness/engine/query.py`（1057行）— 核心循环
  - `~/ai/OpenHarness/src/openharness/services/compact/__init__.py`（1725行）— 压缩系统
  - `~/ai/OpenHarness/src/openharness/permissions/checker.py`（200行）— 权限检查
