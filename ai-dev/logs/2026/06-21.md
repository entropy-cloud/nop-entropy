### nop-ai-agent 设计文档体系——新增扩展点全景矩阵（03-extension-matrix.md）

- **背景**：在完成 nop-ai-agent 设计审计（25 子包 / 40+ 设计文档 / 65+ 接口）后，识别出"设计文档缺少统合视图"是文档体系的主要空白——vision §4「渐进式增强」原则定义清晰、被 100+ 处引用、落实到每个扩展点的 NoOp 三件套，但**没有一篇文档把所有扩展点的闭合度集中呈现**，读者必须自己从代码反推。经用户确认按 ROI 顺序逐篇交付，本篇为第 1 篇（最高 ROI）。
- **改动**：
  - 新增 `ai-dev/design/nop-ai-agent/03-extension-matrix.md`（约 280 行）：66 个接口的全景矩阵，按 Layer 1/2/3/4 分组，每行字段 = 接口 / 包 / NoOp 默认 / 功能实现 / InMemory·DB 实现 / 消费者注入点 / 闭合状态（✅🟡🔴⚪）。
  - 编辑 `ai-dev/design/nop-ai-agent/README.md`：把 `03-extension-matrix.md` 加入「架构基线层」+「必读路径」（顺序第 4）。
- **关键发现（写入矩阵 §5/§6）**：
  - **闭合度分布**：✅ 闭合 59 / 🟡 半闭合 3（`ITalent` / `IContentGuardrail` / `IBudgetProvider`，均在 L2，唯一实现是 NoOp）/ 🔴 未闭合 1（`IFencingTokenService`，零消费者，纯原语预留）/ ⚪ 回调契约 3。
  - **命名一致性反例**（5 处「未分离 NoOp 命名」）：`AlwaysClosed` / `NoRetryPolicy` / `AutoApproveGate` / `PassThroughPermissionMatrix` / `PassThroughPostDenialGuard`——语义上是 NoOp 等价物，命名偏离 `NoOp*` 范式。
  - **NoOp 命名误导**（2 处）：`NoOpTaskMemberRouter`（实为功能性默认单成员路由）/ `NullTenantResolver`（横切依赖偏离 NoOp 范式）。
  - **接线缺口**（1 处）：`ICompletionJudge`——DAE 未暴露 setter、resolveExecutor Builder 链未传，引擎运行时永远走 NoOp，功能实现只能由调用方绕过 DAE 自行注入。这是 vision §4「扩展通过添加接口实现」的违反点。
- **方法论价值**：本篇把 `nop-ai-agent-security-audit-readiness-analysis.md` 的"渐进式审计方法论"推广到全子系统——从"安全子系统的 5/10 评分"扩展为"全 66 接口的闭合度索引"。可作为后续 reliability / team / memory / runtime / platform 各写一篇 readiness-analysis 的范式模板。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` → **25 errors**，**全部为预先存在的错误**（涉及 `ai-dev/bugs/2026-06-13-*` 7 个、`ai-dev/design/opencode-goal-driver/*` 多个、`ai-dev/plans/200,201-*` 几个、`ai-dev/design/nop-ai-agent/nop-ai-agent-roadmap.md:104` 1 个），**与本次新增/修改的 2 个文件无关**（grep 验证：`03-extension-matrix` 与 `README` 均未出现在错误清单中）。预先存在的链接错误建议另开任务修复，不在本次范围。
- **Doc-sync**：本篇是设计层文档，描述当前架构事实（接口分布 + 闭合度），不引入新接口或新行为，无需 `docs-for-ai/` 同步。`glossary.md` / `roadmap.md` / `01-architecture-baseline.md` 与本篇是互补关系（术语 / 阶段 / 职责契约 vs 闭合度），未修改。
- **后续建议**（按 ROI 排序，等待用户确认是否继续）：
  1. `05-organic-composition.md`——25 子包的 import 矩阵 + DefaultAgentEngine 单点装配汇合图 + 扩展点闭合性清单（"为什么是有机整体"的显式说明）
  2. `00-vision.md` §7.5——三原则（扩展点正交 / 汇合点单点 / 闭合优先）
  3. 推广 readiness-analysis 方法论到 reliability / team / memory / runtime / platform（5 篇深度审计）
  4. 修复矩阵 §6 标记的接线缺口（`ICompletionJudge` DAE setter）与命名反例（5 处未分离 NoOp 命名）
