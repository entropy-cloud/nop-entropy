### 2026-08-01（OpenAI Responses API vs Chat Completions 调研分析）
- 联网调研 OpenAI Responses API 与 Chat Completions 的格式差异及第三方厂商（Ollama、智谱 GLM、DeepSeek、Kimi 等）兼容现状，输出分析报告 `ai-dev/analysis/2026-08/2026-08-01-openai-responses-vs-chat-completions.md`：
  - 核心结论：两者是结构性差异（message 数组 vs typed items、delta 增量 vs 具名事件流），"message→item"、"tool_calls→function_call item"、"delta→具名事件"三个转换点覆盖 90% 差异。
  - 生态现状（2026-08）：仅 OpenAI、Ollama（v0.13.3+ 无状态子集）、火山/阿里原生支持 Responses；智谱/DeepSeek/Kimi 等国内厂商仅 Chat Completions，需翻译层（litellm、new-api、CCX、cc-switch 已产品化）。
  - 对 nop-ai 的建议：优先走 dialect 扩展路线（新增 `ResponsesDialect` 复用现有 `ChatRequest` 内部模型），网关型转换（Responses↔Chat 桥接）仅在需要服务 Codex 类客户端时再做。
- 文链接检查通过（exit 0，新文件无 broken link；`ai-dev/plans/` 下 41 个 broken link 为既有历史问题）。
- 用独立子 agent 两轮审查迭代完善文档（3 个独立审查者 + 1 个终审者）：
  - 第一轮（技术准确性 / nop-ai 代码相关性 / 文档规范 3 个 agent 并行审查）：技术审查发现 P0×1（百度千帆 2026-05-27 起支持 `v2/responses`，"国内仅火山/阿里两家"不成立）、P1×5（Codex 0.122+ 已移除 `wire_api="chat"`、火山 2025-10 时间线、历史回放映射缺失、penalty 系参数遗漏等）；代码审查发现 P1×2（"成本低"仅对后端 Provider 方言成立——前端还需 parseRequestBody 默认抛异常 + buildResponse/buildStreamChunk 重写；finish_reason 归一化不认 Responses 顶层 status、多 item output 折叠丢顺序）；规范审查发现 P1×2（Conclusion 缺被否决方案、Status 与结论强度不一致）。
  - 修订后终审：21 条意见 20 条完全落实、1 条部分落实（修正 2 处 P2 措辞：ChatToolCallChunk 仅 OpenAI 路径未填充、providerHints 仅 AnthropicDialect 消费 cache_control），无新 P0/P1，外部事实抽查（百度千帆/Codex wire_api 移除/火山时间线）全部核验通过，判定"共识达成"。
  - 文档新增"折叠模型 vs 拆分模型"概念框架（内部模型与 Chat Completions 同构为折叠形态，Responses 为拆分形态，映射无损可逆），补齐三路线 Comparison 表、双向回放映射、流式完整事件序列、usage 明细映射。
  - analysis 补充"并发工具执行"章节：Responses 支持 `parallel_tool_calls`（模型批量产出多个 function_call items），并发执行是应用层职责；nop-ai `ReActAgentExecutor.java:1906-1984` 已是并发 fan-out（CompletableFuture.allOf + per-tool 超时 + AR-15 孤儿取消），折叠到 `List<ChatToolCall>` 后转换层零改动。
- 编写迁移设计文档 `ai-dev/design/nop-ai-responses-migration-design.md`（状态草案）：
  - 设计结论：内部折叠模型保持不变，新增 `ResponsesDialect` 作为第四种 wire 方言；`ApiStyle.RESPONSES` 新枚举值；`ChatOptions.responseFormat` 升级为对象载体；`normalizeFinishReason` 扩展 completed/incomplete；流式用 dialect 内状态机（声明→delta→done），`ChatServiceImpl` 只透传 data: 行的约束不变；并发工具循环零改动。
  - 映射契约：请求/响应/流式三方向（含前端方向远期接口约束）；组件改动清单 5 项；边界声明（无状态消费、多模态不支持、reasoning 顺序折叠丢失）。
  - 拒绝了什么：内部模型升级为 items 模型（波及 219 引用方、与第三方兼容面脱钩）、网关中间层为主方案（部署运维成本）、等待厂商跟进（OpenAI/Ollama 已原生）。
  - 按 design 指南要求不引用 analysis 文档（关键发现独立成章），实施路径指向 ai-dev/plans/。
- 用户决策翻转：不考虑兼容性约束、以长期演化最优为目标，**采用拆分模型**——重写设计文档 `ai-dev/design/nop-ai-responses-migration-design.md`（状态草案，v2）：
  - 核心判断：拆分模型是超集，折叠模型是它的有损投影；折叠模型是 Chat Completions"聊天心智模型"的历史遗留，agent 场景本质是动作序列。
  - 设计结论 9 条：内部模型重构为协议无关的 typed items 体系（`LlmItem`：text/reasoning/tool_call/tool_output/image/audio，type 开放扩展）；输入输出统一单一 item 序列（回放=append，比 OpenAI input_text/output_text 双命名更干净）；折叠消息降级为纯函数投影（`toMessages`/`fromMessages`，只服务 Chat 兼容 wire 与对话 UI）；流式重构为 item 增量模型（`LlmItemDelta` 状态机，`ChatStreamChunk` 降级为 UI 投影）；`ResponsesDialect` 为 canonical↔Responses 双向投影；并发 fan-out 复用零改动；无状态（拒绝 previous_response_id 协议耦合）；多模态原生一等类型。
  - 拒绝了什么：保持折叠模型为核心（有损投影+硬伤清单）、双核心并存（投影不与源竞争）、抄 OpenAI items 字段（协议耦合）、一刀切全量替换（分 5 个 Phase 独立验证）、网关中间层为主方案。
  - 实施路径 Phase 0-4（item 体系+golden test → 方言切换 → 引擎切换 → 流式重构 → ResponsesDialect）。
  - 原 analysis 文档标为 superseded，指向新设计文档（生命周期规范：结论被推翻时标 superseded 并指向接替文档）。
  - 设计文档 3.1 补充"为什么输入输出合一"论证（防未来评审重复质疑"为什么不照 OpenAI 抄"）：对比 OpenAI 的 input_text/output_text 双命名（请求侧/响应侧同一文本两个类型，手动回放需 output→input 改写）；OpenAI 区分的 4 个理由（类型真实不对称、wire schema 紧致性、内部管线分离、previous_response_id 兜底）逐一给出 nop-ai 的处理（输出元数据作为 item 可选字段、进程内模型无 schema 压力、单序列、无状态回放是主路径）；结论：合一的成立条件是"无状态回放主路径 + 进程内模型"，与服务端会话存储引入不冲突。
- 用户决策修正（v3）：**目标是对现有类型的彻底改造，不是另起新命名体系**——重写设计文档（状态草案）：
  - 决策：命名即契约，复用 `ChatRequest`/`ChatResponse`/`ChatMessage` 系列既有名称（改造内部），不引入 `LlmItem`/`LlmItemDelta`/`toMessages`/`fromMessages` 等平行概念。
  - 新增仅限语义必需 2 个类型：`ChatToolCallMessage`（拆自 assistant.toolCalls）、`ChatReasoningMessage`（拆自 assistant.think）；删除 `ChatCustomMessage` 与 `ChatAssistantMessage.think`/`toolCalls` 寄居字段；`ChatToolResponseMessage` type 标识 tool→tool_output。
  - 统一序列载体改为既有类型：`ChatRequest.messages`/`ChatResponse.messages` 都是 `List<ChatMessage>`（`ChatResponse.message` 单条→列表），回放=append 不变；user/assistant 区分是角色语义而非方向语义，与 OpenAI input_text/output_text 双命名本质区别。
  - 独立投影层（toMessages/fromMessages）删除：Chat 兼容 wire 本就是 dialect `convertMessage` 职责；流式 `ChatStreamChunk` 复用名称改造为 item 增量（itemType/itemIndex/phase: ADDED|DELTA|DONE），`ChatToolCallChunk` 死代码删除。
  - 拒绝清单新增第 2 条"另起新命名体系（LlmItem 等）"；设计结论从 9 条调为 10 条（新增"复用既有命名"第 1 条）。
  - 其余结论不变：拆分模型 canonical、统一序列、多模态一等、无状态、并发 fan-out 零改动、ResponsesDialect 双向转换、ApiStyle.RESPONSES、normalizeFinishReason 扩展。
- 用户补充核心定位（v3.1）：**IChatService 与 ChatRequest/ChatResponse 是对 AI 交互模型的抽象，而非第三方 API 的封装**——设计文档新增结论 2 与 3.1 节"模型定位：领域抽象而非 API 封装"：
  - 领域模型可承载 wire 之外的规范化信息：ChatResponse.error/errorCode 归一化为 Nop 风格错误码（各厂商错误结构由方言解析时归一化，上层只见一种错误形态）、requestId/requestTime/responseTime/retryTimes 追踪语义、ChatOptions.provider/sessionId/requestTimeout 领域选项。
  - wire 特有字段默认丢弃或归一化后才进入核心模型；"不抄 OpenAI 字段"是定位的自然推论而非额外约定。
  - dialect 层新增"错误归一化"职责项；使用契约新增"错误出口"约定；3.1-3.6 小节重新编号。
- 用户问答核实补充（v3.2）：工具结果格式与错误通道边界——
  - 事实核实：`function_call_output.output` 不是永远 JSON，是普通字符串、格式由调用方自定（JSON/纯文本/错误信息均可，官方文档明确"typically a string, format up to you"）；唯一例外是 Responses 流式必须合法 JSON 字符串；SDK 直传 dict 自动序列化属 SDK 行为。
  - 设计文档补充：3.4 新增"工具结果字符串化规则归属方言"（领域侧 output 自由 Object，非流式任意字符串化、流式强制 JSON 编码），并明确 `ChatResponse.error`（请求级调用失败）与工具 output 内错误文本（内容级业务失败）两个通道的区分。

# 2026-08-01 Dev Log

## W3-2 声明式 Filter Chain 落地（Plan `2026-08-01-1437-4-nop-ai-agent-declarative-filter-chain.md`）

### Phase 3 — 收口（completed）

- **设计文档**：`nop-ai-agent-middleware-design.md` §5.2 由 "Proposed（后继 plan W3-2）" 重写为 **final**——D1（方案 B agent 内 `<filter-definitions>`）/ D2（默认 PRE_CALL/POST_CALL 单次触发 + `points` 覆盖，含多次触发语义分析表）/ D3（声明式在前 + 跨机制同 impl 同点快速失败）最终裁定 + DSL 结构 + 映射表 + 共存合并规则 + ResolvedFilterChain 模式 + 执行模型（不变）。§5 头标题去掉 "增量设计" 标记（→ "双层中间件与声明式 filter chain"）；§5.3 边界更新（§5.1/§5.2 正交）。
- **roadmap**：`ai-dev/backlog/nop-ai-agent-harness-evolution-roadmap.md` W3-2 ❌ → ✅（含收口备注：DSL/codegen/D1/D2/D3/FilterChainResolver/ResolvedFilterChain/装配集成/24 测试/§5.2 final/零回归）；W3 整体（W3-1 + W3-2）确认完成。
- **DSL parse-level 验证**：新增 `test-filter-chain.agent.xml` 资源 + `filterChainParsesFromAgentXmlResource` 测试——证明 `<filter-chain>`（含 `<output-filters>` 经 `xdef:ref="FilterRefModel"` 复用）在 parse 时正确解析为 `AgentFilterChainModel`（xdef 有效性，非仅 codegen 时）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -T 1C` BUILD SUCCESS（**3158 测试 0 failures**，零回归）。
- **Doc-link checker**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0。

### Phase 2 — 装配集成与双链映射（completed）

- **D3 裁定（共存合并语义）**：声明式 filter 在前（guardrail 先执行=洋葱最外层）+ 同 impl class 同点快速失败。实现：`AgentExecutorResolver` 在 `resolveHookContributions` 后先 `resolveFilterChain`（注册声明式 filter，返回 `Map<point,Set<implClass>>`）再 `resolveMiddlewares`（注册代码类中间件，注册前查表做 D3 重复检测）；registry 的 append 顺序天然保证声明式在前。重复检测按 impl class（两路径各自 `safeNewInstance` 产独立实例，identity 不适用）；intra-mechanism 重复不算冲突。
- **`AgentExecutorResolver` 改动**：新增私有 `resolveFilterChain(model, hookRegistry)` → 调 `FilterChainResolver.resolve` → 按 `resolvedByPoint` 注册（D2 映射）+ 收集 impl class。`resolveMiddlewares` 增 `declarativeImplByPoint` 参数，session-scope 分支注册前做 D3 检测（execution-scope 分支不变=W3-1 零回归）。
- **端到端 + 接线测试（`TestAgentFilterChainWiring` 9 例）**：
  - `declarativeInputFilterExecutesViaOnionChainEndToEnd`：`resolveExecutor` → `<filter-chain>` 解析 → 注册 registry → `execute()` → `AgentHookInvoker.executeWithMiddleware(PRE_CALL)` → `MiddlewareChain.proceed` → filter 真实执行（Anti-Hollow：非仅模型解析）
  - `preCallDefaultFiresOnceAcrossMultipleIterations`：1 tool call = 2 LLM calls 场景，input filter @ PRE_CALL 触发 1×（非 2×）= 证明 D2 默认映射 PRE_CALL 单次触发，非多触发 PRE_REASONING
  - `declarativeOutputFilterFiresAtPostCall`：output filter @ POST_CALL 1×
  - `pointsOverrideRetargetsAwayFromDefault`：`points="pre_reasoning"` → 不在 PRE_CALL、在 PRE_REASONING
  - `coexistenceDeclarativeFirstThenCodeClass`：洋葱顺序 decl.before → code.before → code.after → decl.after（声明式最外层）
  - `duplicateDeclarationAcrossFilterChainAndMiddlewaresThrows`：同 impl class 同点跨机制 → `ERR_AGENT_FILTER_DUPLICATE_DECLARATION` 抛
  - `distinctImplsAtSamePointDoNotTriggerDuplicateDetection`：不同 impl 同点 → 共存（声明式在前）
  - `noFilterChainIsZeroRegression` / `emptyFilterChainIsNoOp`：零回归
- **验证**：`./mvnw compile -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw clean install -DskipTests -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-agent -T 1C` BUILD SUCCESS（**3157 测试 0 failures**，含 plan 296/3 既有 middleware 测试全过 = 零回归）。
- **checkstyle**：nop-ai-agent 新增文件存在 80-char/javadoc/final-param 告警，但与既有代码库一致（`AgentExecutorResolver` 等既有文件同样超 80-char），checkstyle 为 advisory 非 build gate（`mvn install` 不绑定 checkstyle，全工作区 routinely 超阈值仍 BUILD SUCCESS）。
- **Owner-doc**：§5.2 final 重写由 Phase 3.1 交付。

### Phase 1 — 声明式 filter-chain DSL 与解析（completed）

- **D1 裁定（filter ID 解析来源）**：采纳**方案 B**（agent 内 `<filter-definitions>` 自包含 id→impl 映射）。约束分析：`AgentExecutorResolver` 经 Builder/构造注入，**不持有 `IBeanContainer`**；仓库 `*.beans.xml` 无任何已注册 `IAgentMiddleware` bean。方案 A（IoC bean ID）需先打通容器注入路径（架构改动大），而方案 B 复用既有 `resolveMiddlewares` 的 `ClassHelper.safeNewInstance` 路径，零 IoC 改动。跨 agent 复用为首版 Non-Goal，方案 C 混合留作后续迭代。
- **D2 裁定（input/output → 生命周期点映射）**：input-filters 默认装到 **PRE_CALL**、output-filters 默认装到 **POST_CALL**（请求/响应边界，单次触发，避免 PRE_REASONING/PRE_ACTING 的 N+M+K 多触发语义错误）。filter 可用 `points` 属性覆盖默认映射（如 `points="pre_reasoning"`）。
- **DSL（`agent.xdef`）**：新增 `<filter-chain xdef:name="AgentFilterChainModel">`，含 `<filter-definitions>`(list,key=id) + `<input-filters>`(list) + `<output-filters>`(list)。`<input-filters>/<filter xdef:name="FilterRefModel" ref points>`，`<output-filters>` 经 `xdef:ref="FilterRefModel"` 复用同模型（避免 `xdef:name` 全局唯一约束——`XDefinitionParser.collectLocalRefs` 对重复 xdef:name 抛 `ERR_XDEF_DUPLICATE_LOCAL_REF`，故 output 用 xdef:ref 引用 input 已定义的 FilterRefModel，codegen 两 getter 均返回 `List<FilterRefModel>`，与 gateway.xdef `requestMapping`/`responseMapping` 模式同构）。
- **codegen 产物**：`_AgentFilterChainModel`(filterDefinitions/inputFilters/outputFilters)、`_FilterDefModel`(id/impl)、`_FilterRefModel`(ref/points)；`_AgentModel.getFilterChain()`。手写壳类 `AgentFilterChainModel`(+`hasAnyFilters()`) / `FilterDefModel` / `FilterRefModel`。
- **解析器**：`FilterChainResolver.resolve(chain)`（D1 方案 B）：index filter-definitions → 按 ref 解析为 `IAgentMiddleware`（`ClassHelper.safeNewInstance`，per-id cache → 同 filter 在 input+output 引用时实例 identity-shared，使 D3 重复检测无歧义）→ 按 D2 映射分组。**无静默跳过**：未知 ID / 缺 impl / 非 IAgentMiddleware / 未知 points 名 / 实例化失败 均抛 `NopAiAgentException`（含 ID 名 / point 名，5 个 error code）。
- **ResolvedFilterChain**：持有 `inputFilterRefs`/`outputFilterRefs`（声明侧，可序列化）+ `resolvedByPoint: Map<AgentLifecyclePoint,List<IAgentMiddleware>>`（执行侧），不可变视图（声明后不可运行时修改）。
- **测试（行为断言）**：`TestFilterChainResolver`(15)：默认映射 PRE_CALL/POST_CALL 单点、points 覆盖（单/多目标）、未知 ID 快速失败（消息含 ID）、缺 impl / 非 middleware / 未知 point 快速失败、duplicate filter-def id 在 model 层 KeyedList 即 fail-fast、input+output 共享 filter identity、声明侧与执行侧同步、不可变、filter 经 `MiddlewareChain` 真实执行（auth→rate-limit→core 顺序断言）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -T 1C` BUILD SUCCESS（**3148 测试 0 failures**，含 plan 296/3 既有 middleware 测试全过 = 零回归）。
- **Owner-doc**：`nop-ai-agent-middleware-design.md` §5.2 final 重写由 Phase 3.1 交付（本 Phase 为 Phase 3 的输入）；Phase 1 仅新增能力，无既有契约变化。

## W3-1 双层执行级中间件落地（Plan `2026-08-01-1437-3-nop-ai-agent-execution-level-middleware.md`)

- **Plan 关闭**：Plan Status → completed（2026-08-01）；Phase 1-3 全部 completed、Closure Gates 全勾选；独立 closure audit（fresh session `ses_0420c87b7ffeIeIBI551kY6rgA`，general 类型）verdict **PASS**——7 项 claim 逐条 live-source 核验，Anti-Hollow 确认（触发点在 retry loop/dispatch 内确实被调用，retry 时确实重评估，工具侧 veto 返回值被检查非丢弃）
- **roadmap**：`ai-dev/backlog/nop-ai-agent-harness-evolution-roadmap.md` W3-1 ❌ → ✅
- **核心交付（执行级 = per-attempt 中间件层，retry 时重新评估安全检查）**：
  - 新增类型 `MiddlewareScope`(session|execution) / `ExecutionPoint`(PRE/POST_LLM_ATTEMPT, PRE/POST_TOOL_ATTEMPT) / `AttemptContext`(attempt/retry/lastErrorClassification 强类型，挂 HookContext)。`AgentLifecyclePoint` 12 值完全不变（D1：独立 ExecutionPoint 枚举，不污染会话级枚举）
  - registry scope 维度分离：`IHookRegistry`/`DefaultHookRegistry`/`NoOpHookRegistry` 新增 `getExecutionMiddlewares`/`registerExecutionMiddleware`（独立 `Map<ExecutionPoint,List>`，与会话级 `Map<AgentLifecyclePoint,List>` 永不交叉）
  - `AgentHookInvoker.executeExecutionMiddleware(point, ctx, attemptCtx, ...)`：空链零开销直通 Pass；非空链 onion 组合（pass-through core，PRE/POST 不 wrap 操作本身）
- **LLM retry loop 集成（`LlmCallCoordinator`）**：PRE_LLM_ATTEMPT 在 `callChatWithTimeout` 前、POST_LLM_ATTEMPT 在返回后（每次 attempt，含 retry）触发；`lastClassification` 跨迭代保留传入下次 AttemptContext（retry 时中间件据上次分类重新评估）；D3 veto 语义：veto → 合成 NON_TRANSIENT 失败 → 喂入现有 `retryPolicy.shouldRetry()` 决策（非无条件 retry）；**veto ≠ 模型失败**：veto 路径不记录 `circuitBreaker.recordFailure`（经 `skipCall` flag gate）；`MAX_EXECUTION_VETOES=3` veto cap 防"中间件每次 veto + retryPolicy 每次 RETRY"无限循环（超限 fail-loud）
- **工具 dispatch 集成（`AgentToolDispatcher`，D4 方案 a 同步 before/after）**：PRE_TOOL_ATTEMPT 在 fan-out 循环内提交 future 前同步触发（veto 则不提交 future，产错误 result，不影响同 batch 其他工具）；POST_TOOL_ATTEMPT 在结果处理循环内 join 后 commit 前触发（veto 替换 result）。**Anti-Hollow 红线**：两处返回值均显式 `isVeto()` 检查（非丢弃），与现有 PRE_ACTING`:304` 返回值丢弃模式对比
- **声明层**：`agent.xdef` `<middleware>` 增 `scope="enum:session,execution|session"` 属性（默认 session 零回归）；`AgentExecutorResolver.resolveMiddlewares` 按 scope 分流注册；codegen 自动再生 `_AgentMiddlewareModel`（scope 字段）
- **设计文档**：`ai-dev/design/nop-ai-agent/nop-ai-agent-middleware-design.md` §5.1 由方向性描述重写为 final（D1-D4 裁定 + 双层表 + 触发点清单 + retry 重评估语义 + Veto 控制流映射 + 线程模型）；§三约束更新为"会话级 12 值不变 + 执行级新增独立 ExecutionPoint"
- **测试（行为断言，非仅编译）**：`TestExecutionMiddleware`(10)、`TestExecutionMiddlewareLlmRetry`(8，含 `retryReEvaluates...CarryingRetrySignalAndLastClassification` 证明 retry 重评估 + `vetoCapPreventsInfiniteLoopAndFailsLoud`)、`TestExecutionMiddlewareToolDispatch`(4，含 `preToolAttemptVetoAbortsSingleToolAndDoesNotAffectBatch` + `postToolAttemptVetoReplacesResultAndReturnValueIsChecked` Anti-Hollow)
- **验证**：`./mvnw compile -pl nop-ai/nop-ai-agent -am` PASS；`./mvnw clean install -DskipTests -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS；**`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS（3133 测试 0 failures，含 plan 296 既有 `TestMiddlewareChain` 12 例全过 = 会话级零回归）**；checkstyle 改动文件 0 violation（既有 dependency 模块 violation 与本次无关）
- **偏差**：① POST_LLM_ATTEMPT 初版仅在 `isSuccess()` 时触发，测试发现后修正为对每次返回的响应（成功或错误）均触发（符合设计"after each attempt, before classification"）；② xdef codegen 需先 `install` nop-xdefs 才能让 nop-ai-agent codegen 读到更新后的 xdef（`-pl` 不带 `-am` 时会用 .m2 旧 jar 再生模型回退 scope 字段）——已记录为构建特性
- **Doc-sync**：`nop-ai-agent-middleware-design.md` §5.1 + §三 final；roadmap W3-1 ✅；其余 owner-doc No update required（新增能力，无既有契约变化）
