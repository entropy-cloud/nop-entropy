# 2026-08-01 Dev Log

## Plan `323-flux-web-container-body-rendering` Closure — tabs 字段名修复 + tab/step/group body 渲染 + wizard/group 页面类型

- **Plan 关闭**：Plan Status → completed（2026-08-01）；Phase 1-4 全部 completed、Closure Gates 全勾选；`check-plan-checklist.mjs --strict` exit 0；独立 closure audit（fresh session `ses_046b3d7b4ffeL8PTP50RNw4YaW`）verdict APPROVE——见 plan Closure 段
- **Phase 1（tabs 字段名 + body 渲染）**：
  - `flux-web.xlib` 新增内部标签 `GenContainerModel`（outputMode=xjson，页面级/body 级共用容器分派：crud/simple/tabs/wizard/group，otherwise 抛 `nop.err.web.unknown-page-type`）
  - 新建 `flux-web/container_tabs.xpl`（无 page 外壳 tabs 核心）、`container_crud.xpl`（`const pageModel = containerModel` 后复用 `grid_crud.xpl` 无外壳片段）、`container_simple.xpl`（复用 `GenFormBody` 的表单容器，submit/load 语义对齐 page_simple.xpl）
  - `page_tabs.xpl` 重写：`<tabs j:list="true">` → `<items j:list="true">`（Flux `TabsSchema` 字段名 live defect 修复——原输出 `tabs` 字段在 Flux 下 silent no-op）；tab 项 `key` ← `name`（`FluxTabDefaultAttrs` 补 key）；tab body 优先级 `page` > `body`(GenContainerModel 分派) > `name` 兜底，`<body j:list="true">` 保证输出数组
  - `GenLayoutTabs`（form 级 tabs）live defect 修复：tab 项内容 `<tab j:list="true">` → `<body j:list="true">`，补 `<key>`（原内容写在 `tab` 字段，Flux nestedRegions 只提取 `items[].body` → 静默丢失）
  - 测试：`test-flux-tabs.view.xml`/`.page.yaml`/`.xmeta`（page tab 引用 `add` 页面 + body tab 内嵌 simple 容器；`edit` form `layoutControl="tabs"`）；`TestFluxWebGen` +4 测试（page tab items/key、body tab 容器分派、form 级 tabs items[].body、防御性 throw `nop.err.web.unknown-page-type`）
- **Phase 2（wizard 页面类型）**：`impl_GenPage.xpl` wizard 分派；`page_wizard.xpl`（page 外壳——wizard 模型不继承 UiPageModel，外壳仅 name）+ `container_wizard.xpl`（steps 核心：step 优先级 page>body>name、steps[].key←name）；属性映射按设计 §3.3（mode/action*Label 透传；startStep 暂不映射；className/actionClassName/initFetch/initFetchOn/initApi/reload/redirect/target 丢弃）；`GenContainerModel` 增 wizard 分支；`test-flux-wizard.*`（3 step：body simple、body crud、page 引用）+ 测试
- **Phase 3（group 页面类型）**：`impl_GenPage.xpl` group 分派；`page_group.xpl`（page 外壳）+ `container_group.xpl`（GridSchema 核心：columns/gap 透传、autoFlow 枚举映射 `row-dense`→`row dense`、alignItems/justifyItems 过滤 normal/baseline、responsiveColumns 不输出、body 子容器包 `items[].body`、colSpan/rowSpan 经 `DynamicObject.prop_allow` 存在性守卫透传（simple/wizard/group 无 span 字段）、items[].key←name）；`GenContainerModel` 增 group 分支；`test-flux-group.*` + 测试
- **实现要点**：xjson 输出 `<body j:list="true">` 强制数组（无 j:list 时单元素 body 会被 CollectJObjectHandler 折叠为对象）；页面级 body 保持既有折叠语义（测试用 `toBodyList` 归一化）；wizard/group 模型非 UiPageModel，页面外壳不可用 `FluxPageDefaultAttrs`（DynamicObject 未知属性抛错），仅输出 name
- **Phase 4（文档）**：`flux-rendering.md` 新增「页面级容器：tabs/wizard/group 的 Flux JSON 映射」章节（items/steps 字段名、body 数组直接书写、key 映射、page>body>name 优先级、complex/picker 不支持说明）；`layout-syntax-reference.md` §7.1 页面级容器语法 + body 内嵌示例 + page/body 优先级；`source-anchors.md` EXT-009 更新（28 标签 + GenContainerModel + 容器类型）；`check-doc-links.mjs --strict` 0 errors
- **验证**：`./mvnw test -pl nop-frontend-support/nop-web -am` 63 例 0 failures（TestFluxWebGen 17 例含 6 个新测试）；端到端 dump 验证 tabs 输出 `{type:'page', body:{type:'tabs', items:[{key,title,body:[...]}]}}` 与 Flux `TabsSchema` 契约一致；`scan-hollow-implementations.mjs --module nop-frontend-support/nop-web --severity high` exit 0（0 findings）；`check-plan-checklist.mjs --strict` exit 0
- **偏差**：无实质偏差——① 防御性 throw 测试需在 scope 中预置 GenContainerModel 的 5 个 implicit attrs（否则先抛 `tag-no-implicit-attr`）；② wizard 页面外壳因模型非 UiPageModel 改为仅 name 属性（design §3.3 的"包 page 外壳"语义不变）；③ audit 发现 `TestFluxWebGen` 注释位置 cosmetic 问题，已修正
- **roadmap**：plan 323 为 design-sourced 独立特性 plan（无 `> Work Item:` label），`ai-dev/backlog/` 各 roadmap 无对应条目，无需翻转 ❌/✅
- **Commit**: 见 git log（本条目当日提交）
