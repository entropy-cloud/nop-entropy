# 2026-08-01 Dev Log

## Plan `2026-08-01-1437-2-nop-ai-agent-three-level-failure-escalation` 全 3 phase 执行（W2-3：三级失败升级——质量/停滞/基础设施失败各有计数器+阈值+升级动作）

- **背景**：mission-driver EXECUTE 步骤。补齐**单次 task attempt 内**的失败升级能力（§14.4.5 边界）。今日 reliability 重试不区分失败类型——三种失败（质量/停滞/基础设施）无各自的计数器、阈值与升级动作。本计划引入三级失败模型：每级有独立计数器 + max 阈值 + 升级动作，并按 §14.4.5 裁定的边界作为 W1-4 `REPEATED_ERRORS` 信号的**分类输入源**。
- **Phase 1（Decision — design elaboration A-G）**：§13.3 从 4 行表升级为含层归属/分类/计数器/阈值/升级动作/集成点/状态模型/聚合规则的可执行规格。7 项裁定：A=W2-3 宿主层 = **plan 层**（同 W1-4 host，正视两层断连事实，同层聚合可行，生产 wiring 是 §14.5 deferred）；B=失败类型分类经 `TaskOutcome` 携带可空 `FailureType`（`QUALITY`/`STALL`/`INFRASTRUCTURE`），TaskRunner 是分类边界；C=stall 级**保留**（非 defer），信号源 = TaskRunner 报告 STALL（plan 层内组件，轻量可测试），与 W1-4 TASK_STALLED 正交（类型 vs 总数）；D=infra 失败经 INFRASTRUCTURE 计数，plan 级重试直到 maxDispatchRetries → task failed，"业务错误不重试"是 dispatch 层 immediate-retry 概念（§14.5 scope）；E=质量计数与 denial-ledger per-session 安全正交 + 聚合抑制 = **Contribute 模型**（每失败记一条 error + 递增一个 typed 计数器，非双计）；F=升级动作统一 = task `failed`（terminal）+ 构造期 `FailureEscalationPolicy`（disabled 默认零回归）；G=同层聚合管道 = 既有 `recordError → countUnresolvedErrors → REPEATED_ERRORS`（无需新管道）。
- **Phase 2（Fix | Proof — 失败分类 + 三级计数器 + 升级动作）**：
  - **`FailureType` 枚举**（`io.nop.ai.agent.plan.runtime`）：QUALITY/STALL/INFRASTRUCTURE。
  - **`TaskOutcome` 扩展**：增 `failureType` 字段（nullable）+ 新工厂 `failure(errorText, FailureType)`；既有 `failure(errorText)` 退化 null（零回归）。equals/hashCode 含 failureType。
  - **`FailureEscalationPolicy`**（immutable，构造期配置）：`maxAegisRejections`/`staleTaskMaxRetries`/`maxDispatchRetries` 三阈值 + `disabled()` 工厂（全 MAX_VALUE，零回归）+ `shouldEscalate(type, count)`。参数校验 `> 0` fail-fast。
  - **`PlanExecutionState` typed 计数器**：`Map<String, EnumMap<FailureType, Integer>> taskTypedFailures` + `recordTypedFailure`/`getTypedFailureCount`/`resetTypedFailures`。typed 计数独立于 `AgentPlanError`（不侵入 generated model）。
  - **`PlanExecutor` 三级升级**：新构造器接受 `FailureEscalationPolicy`（default disabled）；`drivePhaseTasks` 失败分支读 `outcome.getFailureType()`，typed → `recordTypedFailure` + `shouldEscalate` 检查，超阈值 → task `failed`（terminal），否则 → `pending`（plan 级重试）。成功时 `resetTypedFailures`。既有 3-arg 构造器委托（零回归——所有既有调用点行为不变）。
  - **`PlanReplanner` 重置同步**：`resetStagnationForPhase` + `enactSplit` 新增 `resetTypedFailures`（与 `resetConsecutiveFailures` 同步）。
  - **测试（30 测试全绿）**：`TestThreeLevelFailureEscalation`——TaskOutcome typed/untyped 工厂（5 测试）+ FailureEscalationPolicy disabled/threshold/fail-fast（4 测试）+ PlanExecutionState typed 计数器 increment/independent/reset/fail-fast（4 测试）+ 三级升级端到端 quality/infra/stall + belowThreshold + zeroRegression + disabledPolicy + typedWithDisabledPolicy + errorTextPreserved（8 测试）+ 正交性（quality vs denial-ledger、stall scope per-attempt vs plan-level TASK_STALLED）（2 测试）+ 成功重置（1 测试）+ 聚合 no-double-count + mixedTypes + wiring（3 测试）+ 端到端 quality/infra escalation → REPEATED_ERRORS → plan ESCALATE + W1-4 contract unchanged + typedEscalation exact error/repeatedErrors count（4 测试，Minimum Rules #22/#23）。
  - **无静默跳过**：阈值超显式 task failed（非 continue/吞掉）；无配置显式 disabled() 坍缩；stall 保留非 defer（显式记录）。
- **Phase 3（Fix | Proof — 聚合 + §14.4.5 边界 + 端到端）**：
  - **聚合管道**：使用既有 `recordError → countUnresolvedErrors → StagnationDetector.REPEATED_ERRORS`（**无需新管道**）。W2-3 typed failure 经 `recordError` 自然喂入 W1-4 `REPEATED_ERRORS`。typed 计数器是叠加在 error 记录之上的分类视图。
  - **端到端验证**：quality failures → typed 计数器达阈值 → task `failed` → errors 保留 unresolved → REPEATED_ERRORS 触发 → PlanReplanner ESCALATE。`endToEnd_qualityEscalation_feedsRepeatedErrors_planEscalates` 断言全链连通（finalStatus=escalated + eventsObserved 含 REPEATED_ERRORS + decisionsEnacted 非空）。
  - **接线验证**：`wiring_typedFailures_consumedByExistingRecordErrorPipeline` 验证 typed failure 确实经既有 recordError 管道被 StagnationDetector 消费。
  - **无双计验证**：`aggregation_noDoubleCount_typedCounterEqualsErrorCount` 断言 N failures → N errors, typed counter = N（非 2N）。`endToEnd_typedEscalation_failsTask_repeatedErrorsFromSameErrors` 断言 2 quality failures → exactly 2 errors + REPEATED_ERRORS count=2。
  - **零回归**：W1-4 决策契约不变（`endToEnd_w1_4_decisionContractUnchanged_disabledPolicy` 断言 disabled policy 下 ESCALATE 行为不变）。
  - **design 回写**：§13.3 裁定 A-G + §14.4.5 层归属/聚合 spec + §13.5 实施顺序状态更新。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` = **3111 tests 0 failures**（前基线 3081 + 本计划 30 新测试；零回归——disabled 默认使所有失败 plan 级重试，既有 3-arg PlanExecutor 构造器调用点行为不变，guardrail/denial-ledger/StagnationDetector/PlanReplanner/LLM-call 重试行为不变）。`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` = BUILD SUCCESS。
- **设计约束落地**：裁定 A 层归属 plan 层（正视两层断连，同层聚合可行，生产桥 §14.5 deferred）；裁定 B TaskRunner 分类边界；裁定 C stall 保留（信号源 = TaskRunner，与 W1-4 TASK_STALLED 正交）；裁定 D plan 级重试直到阈值；裁定 E Contribute 模型（无双计）；裁定 F task failed 升级 + 构造期配置；裁定 G 既有聚合管道无需新建。
- Plan: `ai-dev/plans/2026-08-01-1437-2-nop-ai-agent-three-level-failure-escalation.md`

## Plan `2026-08-01-1437-1-nop-ai-agent-wait-for-long-wait-primitive` 全 4 phase 执行（W2-1：WAIT_FOR 长等待原语——挂起不占线程 → 条件满足唤醒恢复）

- **背景**：mission-driver EXECUTE 步骤。补齐 checkpoint 的"等待条件满足后恢复"显式原语。今日 ReAct 执行模型是同步阻塞的——每次退出 `execute()` 都完成返回 future、释放线程，无"挂起会话不占线程、条件满足后唤醒恢复"的能力。本计划增加 `wait_for` 条件 JSONB + `WAIT_FOR` checkpoint 类型 + `waiting` 状态 + 挂起语义 + 唤醒机制（`IWaitCoordinator` + `wakeSession`）+ 防重复挂起（条件重评 Decision H）。
- **Phase 1（Decision — design elaboration A-H）**：§13.1 从 12 行草图升级为含挂起执行模型/唤醒机制/条件 DSL/restore 分支/防重复挂起/序列化范围的可执行规格。8 项裁定：A=wait_for String JSON + 14参of() + WAIT_FOR null key；B=新 waiting 状态隔离 paused denial 耦合；C=外部事件投递 wakeSession + IScheduledExecutor timeout + 新 wakeSession API 不触发 denial reset；D=TIMEOUT/EVENT/USER_INPUT 内联 JSON schema；E=首版 replay + 依赖 H 条件重评防重复挂起 + restorePendingSessions 跳过 waiting；F=全接触点序列化 + CLOB + null 兼容；G=注入式单例 NoOp 默认零回归 + 不引入 nop-job；H=checkWait 三值(NONE/SUSPEND/PROCEED) 条件重评防重复挂起。
- **Phase 2（Fix | Proof — 数据模型 + 序列化）**：`Checkpoint` 增 `waitFor` 字段 + 14 参 `of()` 重载（旧 11/12 参委托传 null）；`CheckpointType.WAIT_FOR`；`app.orm.xml` propId 14 CLOB 列；`AiAgentCheckpointTable` COL_WAIT_FOR + DDL；`CheckpointJournalWriter/Reader`（`waitFor:` 行，legacy 兼容 null）；`DBCheckpointManager`（INSERT/SELECT/readCheckpoint setClob）；equals/hashCode/toString 含 waitFor。TestCheckpointWaitFor = 10 测试全绿。
- **Phase 3（Fix | Proof — 挂起语义 + producer + restore）**：`AgentExecStatus.waiting`（隔离 paused denial 耦合）。新类型 `WaitCondition`(TIMEOUT/EVENT/USER_INPUT + JSON 序列化) + `WaitDecision`(NONE/SUSPEND/PROCEED) + `IWaitCoordinator`(requestWait/checkWait/deliverWake/isWaiting) + `NoOpWaitCoordinator`(零回归)。`DefaultAgentEngineConfig` + Builder + `AgentExecutorResolver` 全链路 wiring。ReAct 循环迭代顶部第 4 个 producer：`checkWait`→SUSPEND→产 WAIT_FOR checkpoint + 设 waiting + break reactLoop + 完成 future（线程释放）。后循环 guard 排除 waiting（不发 EXECUTION_COMPLETED）。`restorePendingSessions` 跳过 waiting（sticky-wait 需显式 wakeSession）。TestWaitForPrimitives = 14 测试 + TestWaitForSuspend = 4 测试全绿。
- **Phase 4（Fix | Proof — 唤醒机制 + 条件求值 + 端到端）**：`DefaultWaitCoordinator`（in-memory ConcurrentHashMap + 注入时钟 LongSupplier + 可选 IScheduledExecutor timeout 调度）。`AgentSessionLifecycle.wakeSession`（gate waiting + deliverWake + 设 running + replay 重入，**不触发** denial reset）+ `DefaultAgentEngine.wakeSession` 委托 + `IAgentEngine.wakeSession` default。`AgentEventType.SESSION_WOKE`。**防重复挂起（Decision H Blocker）端到端验证**：TestDefaultWaitCoordinator = 10 测试（deliverWake→PROCEED、timeout 注入时钟、无 wake 保持 waiting）；TestWaitForWakeEndToEnd = 3 测试（wakeReentryDoesNotReSuspend 断言唤醒后不产第二个 WAIT_FOR checkpoint + 推进至 non-waiting、timeout 注入时钟推进、无 wake 保持 waiting）。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent` = **3081 tests 0 failures**（前基线 3068 + 本计划 13 新测试；零回归——NoOpWaitCoordinator 默认使 checkWait 恒 NONE、paused 路径不变、既有 3 checkpoint 类型不变）。`./mvnw compile -pl nop-ai -am` = BUILD SUCCESS。
- **设计约束落地**：裁定 B 新 waiting 状态隔离 denial 耦合（wakeSession 不调 denialLedger.reset）；裁定 C 新 wakeSession API（非扩展 resumeSession，正视 `:248-252` 门禁 + `:273/282` denial 耦合）；裁定 E replay + 裁定 H 条件重评配合防重复挂起（唤醒重入注册点 checkWait 返回 PROCEED 跳过挂起）；裁定 G NoOpWaitCoordinator 默认零回归 + 不引入 nop-job。
- Plan: `ai-dev/plans/2026-08-01-1437-1-nop-ai-agent-wait-for-long-wait-primitive.md`

## Plan `2026-08-01-1905-3-nop-ai-agent-provider-failover-queue` Phase 2 执行（W2-4：ProviderFailoverQueue + 第三通道集成，端到端跨 provider 故障转移）

- **背景**：Phase 1 裁定 A-E 已落地设计。Phase 2 落地跨 provider 有序故障转移第三通道——schema + codegen + ProviderFailoverQueue（去重）+ ProviderFailoverChain（向前游走）+ LlmCallCoordinator 第三通道集成（账号链耗尽升级到跨 provider）+ 端到端测试。
- **Phase 2（Fix | Proof — schema + 队列 + 集成 + 测试）**：
  - **Schema + codegen（裁定 A）**：新增 xdef `/nop/schema/ai/llm-failover.xdef`（根 `<llmFailover xdef:name="LlmFailoverConfig">`，body 单全局有序 `<providers>` 表，每条 `provider`+可选 `model`/`tier`）。在 `nop-ai-core/precompile/gen-ai-xdsl.xgen` 注册 `renderModel('llm-failover.xdef')` → codegen 生成 `LlmFailoverConfig` + `LlmFailoverProviderModel`（`io.nop.ai.core.model` 包，与 LlmModel 同）。改 xdef 后 `mvn install nop-xdefs` → 重建 nop-ai-core 触发 codegen 验证通过。opt-in `_default.llm-failover.xml`（生产缺省 = 无链 = 零回归）。
  - **provider 链解析（裁定 A）**：`LlmConfigHelper.resolveFailoverChain(primaryProvider)` 加载单全局表，找 primary 位置，返回其**之后**有序子表（failover 目标）。文件缺省/primary 不在表/primary 是表尾 → 空表（无 failover）。经 `VirtualFileSystem.getResource().exists()` 安全检测 opt-in 缺省（非异常）。
  - **ProviderFailoverQueue（裁定 B/D，去重）**：`IProviderFailoverQueue` 契约（recordFailure/recordSuccess/isAvailable）+ `NoOpProviderFailoverQueue` shipped 默认（恒 true、record no-op，零回归）+ `ProviderFailoverQueue` 功能实现（per-provider `ProviderHealth{lastFailureAt, consecutiveFailures, recovered}`，冷却期内 isAvailable=false 防震荡）。**可注入 `LongSupplier nowMs`**（默认 `System::currentTimeMillis`，测试注入 `AtomicLong` 精确推进时间——不复制 ThresholdBreaker `:122` 直接调 currentTimeMillis 反模式）。线程安全 via ConcurrentHashMap + per-entry synchronized。**两层并存不 roll-up**：model 级（ThresholdBreaker per provider:model）门控单模型调用；provider 级（本队列 per provider）门控跨 provider failover 去重。
  - **ProviderFailoverChain（裁定 D，向前游走）**：per-execution 游走器（对称 AccountChain），`nextAvailable(IProviderFailoverQueue)` 向前游走跳过冷却中的 provider（单次调用内 P1→P2→P3 线性向前，cursor 只增不减，单次无震荡）。`IProviderFailoverChainResolver`（`@FunctionalInterface`，对称 IAccountChainResolver）。
  - **第三通道集成（裁定 C/E，嵌套循环）**：`LlmCallCoordinator` 新增 10-arg 完整构造器（+ providerFailoverQueue + providerFailoverChainResolver，null → NoOp/config-based 默认，使 ReActAgentExecutor 现有 7-arg 构造链自动获得第三通道 config-driven 能力，零回归）。QUOTA/AUTH 账号链耗尽分支（原 fail-loud）升级：`recordProviderFailure` → 惰性解析 failoverChain → `doProviderFailover` 取下一可用 provider → **重置** accountChain=null + routedOptions（改 provider/model，清 accountKey，裁定 E）+ 新 circuit key + attempt=0 → continue。全部 provider 耗尽 fail-loud（`buildFallbackExhaustedError` 加 `providerChainExhausted` 标志明示跨 provider 链也耗尽）。成功路径 `recordProviderSuccess` 重置 provider 健康。TRANSIENT 模型 tier 耗尽仍 fail-loud（两通道区分不变）。
  - **测试（全绿，新增 16 测试）**：`TestProviderFailoverQueue`（10 测试）——NoOp 零回归、冷却去重（边界 ms 精确）、成功重置、per-provider 独立、可注入时钟确定性推进、cooldownMs=0 边界、并发安全 smoke、参数 fail-fast。`TestProviderFailoverChain`（6 测试，端到端从 doLlmCallWithRetry 入口，Minimum Rules #22）——账号链耗尽→failover p2→成功（断言 ChatOptions.provider 变化 + 调用序列 + 接线 recordProviderFailure 被调，Rule #23）；全部 provider 耗尽 fail-loud（断言 error 含 account chain + cross-provider）；无 provider 链配置账号链耗尽仍 fail-loud（零回归）；去重跳过冷却中 p2 切 p3（防震荡，断言 p2 未被实际调用）；QUOTA 走账号链+跨 provider 不调 model-tier getFallback（三通道区分）；provider 切换重置 accountChain（p2 走自己账号链）。
  - **无静默跳过**：链耗尽显式 fail-loud 抛异常（非返回 null/STOP 当正常）；参数 null fail-fast（非静默）。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` = **3040 tests 0 failures**（前基线 3024 + 本计划 16；零回归——无 provider 链配置时账号链耗尽仍 fail-loud；账号链/模型 tier 两通道行为不变；QUOTA/AUTH 未配 errorMappings 不可达不变量）。`./mvnw clean install -DskipTests -pl nop-ai -am` = BUILD SUCCESS。
- **设计约束落地**：裁定 A schema 引用既有 {provider}.llm.xml；裁定 E 复用 ChatOptions provider/model（零新字段，ChatServiceImpl 无改动）；裁定 C 嵌套循环（单 while + provider 切换重置内层状态）；裁定 D 混合状态模型（per-execution cursor + 跨调用共享健康）+ 可注入时间源；端到端从 doLlmCallWithRetry 到跨 provider 成功/fail-loud 完整跑通（Rule #22）；ProviderFailoverQueue 运行时被账号链耗尽分支调用（Rule #23 接线验证）。
- Plan: `ai-dev/plans/2026-08-01-1905-3-nop-ai-agent-provider-failover-queue.md`

## Plan `2026-08-01-1905-3-nop-ai-agent-provider-failover-queue` Phase 1 执行（W2-4：跨 provider 有序故障转移 design elaboration 5 裁定 A-E）

- **背景**：mission-driver EXECUTE 步骤。补齐跨 provider 故障转移能力（第三通道）。今日 `LlmCallCoordinator` FALLBACK 有两通道——同 provider 账号链（QUOTA/AUTH→`AccountChain`，W2e-5）+ 模型 tier 链（TRANSIENT→`IModelRouter.getFallback`），无跨 provider 维度。Phase 1 是 design-only phase——把 `nop-ai-agent-reliability.md` §13.4 从 6 行方向草图升级为含 schema/算法/集成点/状态模型/嵌套循环的可执行规格，在实现前裁定 5 个 design gap。
- **Phase 1（Decision — design elaboration）**：§13.4 回写 5 项裁定 + 理由：
  - **裁定 A provider 优先级声明落点**：新 manifest xdef `/nop/schema/ai/llm-failover.xdef`（根 `<llmFailover>`，单全局有序 `<providers>` 表，每条引用既有 `{provider}.llm.xml`）。拒绝 llm.xdef 扩展（单 provider 作用域被打破 + 环风险）与 SmartModelRouter 扩展（conflates 模型 tier 与 provider 两正交维度）。单全局表 → failover 恒向前 → 环不可能由声明构造，状态模型最简。opt-in `_default.llm-failover.xml`，缺省 = 无链 = 零回归 fail-loud。
  - **裁定 B provider 维度健康来源**：per-provider 熔断独立维护在 `ProviderFailoverQueue` 跨调用共享状态（`Map<String, ProviderHealth>`），**非** ThresholdBreaker per-model roll-up（ThresholdBreaker 无 provider 聚合 API，roll-up 语义模糊）。两层并存：model 级（ThresholdBreaker）门控单模型调用；provider 级（ProviderFailoverQueue 共享状态）门控跨 provider failover 去重。Reconcile §13.4 "单 provider 维度"措辞 vs live per `provider:model` 复合事实。
  - **裁定 C 第三通道触发 + 集成点 + 嵌套循环**：被动触发（账号链耗尽时升级，单一集成点 `LlmCallCoordinator.java:177-182`）。主动触发（provider 健康预检）deferred successor（选主动须拆 2a/2b，违单计划可关闭约束）。嵌套循环 = 单 `while(true)` + provider 切换时重置内层状态（accountChain=null + 新 circuit key + attempt=0），语义等价嵌套。TRANSIENT 模型 tier 耗尽仍 fail-loud（两通道区分不变）。
  - **裁定 D failover_switch 去重 + 状态模型 + 可测试性**：混合模式——per-execution 游标（`ProviderFailoverChain`，类比 AccountChain，线性向前单次无震荡）+ 跨调用共享 provider 健康状态（`ProviderFailoverQueue`，类比 ThresholdBreaker 单例，承载去重）。去重 = per-provider 冷却（`now - lastFailureAt < cooldownMs` 跳过，防跨调用震荡）。**可注入 `LongSupplier nowMs`**（非 `System.currentTimeMillis()` 直接调——不复制 ThresholdBreaker `:122` 反模式），测试确定性推进时间。
  - **裁定 E 选项下沉**：复用 `ChatOptions` 现有 `provider`/`model` 字段（`ChatServiceImpl.java:101` 每次 call 按 provider 重载 config/dialect），与 W2e-5 `accountKey` 下沉同模式，无需新字段。切换清 accountKey/accountBaseUrl（新 provider 从主账号开始）。
  - §13.5 追加跳序理由（W2-4 硬前置是 W2e，已满足；与 W2-1/W2-2/W2-3 无硬依赖）。
- **设计约束落地**：裁定 A schema 落点引用既有 `{provider}.llm.xml` 单 provider 结构；裁定 B 与 ThresholdBreaker per-model 事实一致；裁定 E 与 `ChatOptions` provider/model + `ChatServiceImpl.buildHttpRequest` 一致。裁定 C/D 为 Phase 2 设界——所选 schema/集成使 Phase 2 单计划可关闭（被动触发单集成点，无主动预检新增集成面）。
- **No new test required**：design-only phase（plan guide Rule #25 纯 design 例外）。
- Plan: `ai-dev/plans/2026-08-01-1905-3-nop-ai-agent-provider-failover-queue.md`

## Plan `2026-08-01-1905-2-nop-ai-agent-checkpoint-idempotency-key` Phase 3 执行（W2-2：restore 发散检测 + 拒绝降级，行为变更）

- **背景**：Phase 2 落地了 idempotency_key 数据模型 + 序列化（restore 行为未变）。Phase 3 落地 restore 发散检测——从今日"只软校验 messageCount，从不拒绝"升级为"同水位 idempotency_key 不一致 → 拒绝该 checkpoint，降级 session 重放"（design §13.2）。
- **Phase 3（Fix | Proof — restore 发散检测 + 拒绝降级）**：
  - **比较机制实现**（裁定 A）：`AgentSessionLifecycle.restoreSession` 新增 `recomputeToolExecutionKey(session, callId)` 私有方法——遍历持久化 session 的 `ChatAssistantMessage.getToolCalls()`，找到匹配 callId 的 `ChatToolCall`，用 `Checkpoint.computeIdempotencyKey(TOOL_EXECUTION, tc.getName(), tc.getId(), tc.getArgumentsText())` 重算 live key（与 save 时写入 checkpoint 的 inputSummary=argumentsText 同源）。callId 未找到 → 返回 null（视为发散，裁定 A point 5）。
  - **拒绝 + 降级实现**（裁定 B）：restore checkpoint 消费块（原 messageCount 软校验之后）新增：latestCheckpoint.idempotencyKey 非 null（TOOL_EXECUTION）时，重算 live key 比较；不一致（或 callId 未找到）→ `divergenceDetected=true` + `rejectedCheckpointWatermark=watermark` + distinct LOG.warn（"divergence detected: idempotency_key mismatch ... degrading to session replay"，非今日的 "messageCount exceeds"）。**降级不卡死**——拒绝 = 不信任 checkpoint 作 resume-point 校验，session 重放（buildBaseExecutionContext + executor.execute）照常继续（persisted session 是 source of truth）。
  - **事件可观测**（裁定 B）：`SESSION_RESTORED` 事件 payload 新增 `divergenceDetected`（boolean）+ `rejectedCheckpointWatermark`（String），使审计/监控可区分"接受/best-effort fallback"vs"拒绝+降级"。
  - **旧 checkpoint 兼容**（零回归）：idempotencyKey 为 null（旧数据/非 TOOL_EXECUTION/NoOp）→ 跳过发散检测，退回今日 best-effort messageCount 软校验（divergenceDetected=false）。
  - **测试**（全绿，新增 `TestRestoreSessionIdempotencyDivergence` 4 测试，端到端从 restoreSession 入口到恢复完成）：key 匹配 → divergenceDetected=false + 恢复 completed；**key 不一致 → divergenceDetected=true + rejectedCheckpointWatermark 记录 + 恢复仍 completed**（拒绝+降级不卡死，Minimum Rules #22 端到端）；**callId 未找到 → divergenceDetected=true**（消息历史截断=发散）；**null key → divergenceDetected=false**（旧数据 best-effort fallback，零回归）。
  - **无静默跳过**：key 不一致显式 divergenceDetected=true + warn；null key 显式跳过检测（明示策略，divergenceDetected=false）。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` = **3024 tests 0 failures**（Phase 2 的 3020 + Phase 3 新增 4；零回归——key 匹配行为不变；null key 旧数据退回 best-effort；既有 restore/e2e 测试全绿）。
- **设计约束落地**：拒绝路径触发降级重放（非静默返回 null/空 session，非卡死）；端到端验证 restoreSession→恢复完成完整跑通。
- Plan: `ai-dev/plans/2026-08-01-1905-2-nop-ai-agent-checkpoint-idempotency-key.md`

## Plan `2026-08-01-1905-2-nop-ai-agent-checkpoint-idempotency-key` Phase 2 执行（W2-2：idempotency_key 数据模型 + 计算 + 序列化）

- **背景**：Phase 1 裁定 A-G 已落地设计。Phase 2 落地 idempotency_key 的数据模型 + 计算 + 全部序列化接触点（**无 restore 行为变更**——Phase 3 才改 restore）。
- **Phase 2（Fix | Proof — 数据模型 + hash + 序列化）**：
  - **Checkpoint 值对象**（`reliability/Checkpoint.java`）：增 `idempotencyKey`（String，可空）字段。新增 12 参重载 `of(..., idempotencyKey)`（反序列化透传存储 key，裁定 D）；保留 11 参 `of(...)`（100+ 调用点零改动）内部经 `computeIdempotencyKey(type, toolName, callId, inputSummary)` 算 key 后委托 12 参（裁定 C）。`computeIdempotencyKey` 公开静态方法：TOOL_EXECUTION → `sha256Hex(toolName+"|"+callId+"|"+inputSummary)[:32]`（`HashHelper.sha256`/`StringHelper`，同 security `ActionFingerprint` 公式，裁定 F）；LLM_TURN/COMPACTION → null（明示不参与发散检测，非吞掉）。equals/hashCode/toString 含 idempotencyKey。
  - **ORM 模型**（`_vfs/nop/ai/agent/orm/app.orm.xml`，手写源模型非生成）：`ai_agent_checkpoint` 增 `idempotencyKey` 列（propId 13，`idempotencyKey` domain VARCHAR(100)，可空）。WATERMARK 保持 PK。
  - **DDL 常量**（`reliability/AiAgentCheckpointTable.java`）：`DDL_CREATE_TABLE` 加 `IDEMPOTENCY_KEY VARCHAR(100)` 列；新增 `DDL_CREATE_IDEMPOTENCY_INDEX`（`CREATE UNIQUE INDEX` on IDEMPOTENCY_KEY，裁定 G——H2/PostgreSQL 多 NULL 兼容，旧数据/非 TOOL_EXECUTION null key 不冲突）。
  - **DBCheckpointManager**（序列化裁定 E）：`initSchema` 执行新 unique index DDL；INSERT 加 IDEMPOTENCY_KEY 列（index 12，TENANT_ID 顺延 13）；`checkpointColumnsSelect()` SELECT 加列；`readCheckpoint` 读 ResultSet 列透传 12 参 `of()`（**不重算**——透传存储 key 才能检测篡改/发散）。
  - **CheckpointJournalWriter/Reader**（序列化裁定 E）：serializeSection 写 `idempotencyKey:` 行（`encodeString`，null→字面 null）；parseSection 读 `idempotencyKey`（`decodeString`，**缺字段→null**，legacy journal 向后兼容），透传 12 参 `of()`。
  - **CheckpointSnapshot**：**无需改动**（aggregate 缓存只存 recovery-critical 子集，非 per-checkpoint key，裁定 E）。
  - **测试**（全绿，新增 `TestCheckpointIdempotencyKey` 12 测试）：key 非空+确定性；不同输入（toolName/callId/inputSummary）不同 key；null 组件→稳定 key；LLM_TURN/COMPACTION→null（裁定 F）；11 参 of() 算 key == computeIdempotencyKey；12 参 of() 透传显式 key（含篡改 key 保留）+ 接受 null（legacy）；journal 往返保留 key；**legacy journal section（无 key 行）读回 null key**（向后兼容）；DB 往返保留 key（含 null key for 非 TOOL_EXECUTION）；**unique index 拒绝重复 idempotency key**（裁定 G 幂等去重）。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` = **3020 tests 0 failures**（零回归——100+ of() 调用点编译通过；无 key 的 checkpoint 可保存/读取；restore 行为本 phase 不变）。
- **设计约束落地**：无静默跳过（非 TOOL_EXECUTION 显式 null 明示策略；legacy 无 key 行显式 null fallback）；不可变（工厂内算 key，无 withIdempotencyKey 反序列化重建器）；反序列化透传存储 key（不重算）。
- Plan: `ai-dev/plans/2026-08-01-1905-2-nop-ai-agent-checkpoint-idempotency-key.md`

## Plan `2026-08-01-1905-2-nop-ai-agent-checkpoint-idempotency-key` Phase 1 执行（W2-2：design elaboration 7 裁定 A-G）

- **背景**：mission-driver EXECUTE 步骤。补齐 checkpoint 非确定性检测能力（今日 restore 只软校验 messageCount，从不拒绝 checkpoint）。Phase 1 是 design-only phase——把 `nop-ai-agent-reliability.md` §13.2 从 13 行草图升级为可执行规格，在实现前裁定 7 个 design gap（比较机制/拒绝行为/不可变性/of() 参数策略/序列化范围/hash per type/DB 兼容）。
- **Phase 1（Decision — design elaboration）**：§13.2 回写 7 项裁定 + 理由：
  - **裁定 A 比较机制**：restore 时 idempotency_key 与 **从持久化 session 消息历史重算的 key** 比较（遍历 `ChatAssistantMessage.getToolCalls()` 找匹配 callId，重算 `hash(toolName+callId+inputSummary)`）。排除 checkpoint 间比较（WATERMARK 是 PK 无重复）与 tool 执行点对照（restore 不重新执行 tool）。
  - **裁定 B 拒绝行为**：distinct 日志（"divergence detected" vs 今日 "messageCount exceeds"）+ `SESSION_RESTORED` 事件 payload 加 `divergenceDetected=true`/`rejectedCheckpointWatermark`。降级不卡死——session 重放本就是唯一恢复路径（persisted session 是 source of truth），拒绝 = 不信任 checkpoint 作 resume-point 校验，恢复照常经消息历史 replay 继续。
  - **裁定 C 不可变性**：key 在 `Checkpoint.of()` 工厂内计算（单一事实源），不引入 `withIdempotencyKey()`，不在 saveCheckpoint 重建。
  - **裁定 D of() 参数策略**：新增 12 参重载（显式传算好/存储的 key，供反序列化透传）；保留 11 参（100+ 调用点零改动）内部算 key 委托 12 参。反序列化必须透传存储 key（不可重算，否则篡改的 key 被静默修正，失去检测能力）。
  - **裁定 E 序列化范围**：JournalWriter 写 key 行 + JournalReader 读 key + DBCheckpointManager INSERT/SELECT/readCheckpoint 三处 + equals/hashCode/toString + CheckpointSnapshot（不含 key，无需改）。
  - **裁定 F hash per type**：TOOL_EXECUTION → `sha256Hex(toolName+"|"+callId+"|"+inputSummary)[:32]`（同 security ActionFingerprint 公式）；LLM_TURN/COMPACTION → null（明示不做发散检测，非吞掉）。
  - **裁定 G DB 兼容**：idempotency_key 上 unique index；H2/PostgreSQL 多 NULL 兼容（旧数据/非 TOOL_EXECUTION null key 不冲突）；MySQL<8.0.16 单 NULL 限制是 Non-Blocking Follow-up；WATERMARK 保持 PK。
- **验证**：design-only phase（Rule #25：No new test required）。所有裁定与 live 事实一致（restore 从持久化消息重建非重新执行 tool；WATERMARK 是 PK）。
- Plan: `ai-dev/plans/2026-08-01-1905-2-nop-ai-agent-checkpoint-idempotency-key.md`

## Plan `2026-08-01-1905-1-nop-ai-agent-plan-replan-rollback-split` Phase 2 执行（W1-4 余：SPLIT_TASK 运行时 + 集成面）

- **背景**：mission-driver EXECUTE 步骤。收口 W1-4 余项 SPLIT_TASK（任务拆分）运行时 enactment——复用 Phase 1 决策载荷 + executor 可恢复重入，落地 SPLIT enactment + scheduler/executor 集成面（使子任务非死节点）。
- **Phase 2（Fix | Proof — SPLIT_TASK enactment + 集成面）**：
  - **Decision（SPLIT 触发 + 子任务规格 DSL 落点）**：触发条件 = `TASK_STALLED` 且 task 在 `ReplanPolicy.splitSpecs` 有 `SplitSpec`。子任务规格来源 = 构造期 `SplitSpec(parentTaskNo, List<AgentPlanTaskModel> childTemplates)`，**非** `agent-plan.xdef` 新增 `<splitTemplate>`（避免 Protected Area codegen 级联；声明式 xdef 元素可作为未来非破坏性 successor 填充同一 policy）。`ReplanPolicy` 第二参数从 `Set<String> splittableTasks` 升级为 `Map<String, SplitSpec> splitSpecs`（`isSplittable` = spec 存在；`splitSpecFor` 供 apply 查 spec）。
  - **PlanExecutionState 结构 overlay**：新增运行时 task 节点注册 API——`registerRuntimeTask(phase, task)`（taskNo 唯一性校验 + 初始化 pending + 计数清零 + 归属 phase）、`runtimeTaskOverlay()`（供 scheduler 结构来源）、`isRuntimeTask`/`isSplitParent`/`markSplitParent`/`countCompletedRuntimeTasks`。`phaseTaskNos(phase)` 与 `phaseOwningTask(task)` 扩展为含运行时 overlay 节点（子任务归属 parent phase）。
  - **scheduler 结构来源改造**：`PlanScheduler.getReadyTasks` 新增 3-arg 重载 `(plan, statusProvider, runtimeTasks)`——结构来源 = `PlanDagBuilder.collectAllTasks(plan)`（冻结）∪ `runtimeTasks`（overlay）；原 2-arg 委托 3-arg 空 overlay（零回归）。
  - **executor phase 过滤层改造**：`PlanExecutor.readyTasksForPhase` 调 3-arg scheduler 传 `state.runtimeTaskOverlay()`；`collectTaskNos(phase, state)` union `state.phaseTaskNos`（含运行时子节点）→ 子任务被 phase 过滤放行（非死节点）；`countCompleted` 含运行时子任务。
  - **SPLIT_TASK enactment**（`PlanReplanner.apply` → `enactSplit`，运行时副本，冻结模板不突变）：(1) parent `markSplitParent` + status `completed`（占位不再 re-stall）+ resolve 累积错误 + 清零连续失败计数（打破 detect→split 循环——否则 REPEATED_ERRORS 对残留错误重发）；(2) `SplitSpec.childTemplates` 每个经 `registerRuntimeTask(parent phase, child)` 注册为运行时 overlay 节点（初始 pending）。
  - **测试**（全绿）：`TestPlanReplanner`（21：含 `taskStalled_onSplitEligibleTask_decidesSplit` + `apply_splitTask_enactsRealSplit`（parent 标记 + completed + 错误 resolved + 计数清零 + 子任务注册 pending + owning phase + 冻结模板不变）+ `apply_splitTask_withoutSpec_throwsNoSilentSkip`）；`TestPlanExecutorEndToEnd`（15：原 11 + **SPLIT 端到端**（A stall→SPLIT→子任务 A.1/A.2 被 scheduler 返回 + phase 过滤放行 + TaskRunner 真实执行→plan completed，Minimum Rules #22 + #23 接线验证 ran.contains(A.1/A.2)）+ **scheduler 返回运行时子任务**（anti-hollow）+ **phase 过滤放行子任务**（anti-hollow）+ **零回归**（空 overlay 2-arg vs 3-arg 同结果））。
  - **设计约束落地**：冻结模板永不被突变（SPLIT 端到端断言 `plan.getPhases().get(0).getTasks().size()==1` 不变）；无静默跳过（SPLIT 真实插入节点；无 spec 时抛 IllegalStateException）；子任务非死节点（端到端 ran 验证 + scheduler 单测 + phase 过滤单测）。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` = 3008 tests 0 failures；`./mvnw compile -pl nop-ai -am` 全绿；`./mvnw clean install -DskipTests -pl nop-ai -am` BUILD SUCCESS；checkstyle:check 为 advisory standalone goal（9157 既有违规跨 reactor，build 不绑定，非阻断门禁，与既有 runtime 类同）。
- **文档同步**：design §14.4 落地状态段更新（SPLIT wired + 集成面）；§14.4.2 决策契约表 SPLIT 行；§14.4.3 SPLIT 语义 + 子任务规格裁定 + 集成面（scheduler 3-arg + phase 过滤）算法规格。
- Plan: `ai-dev/plans/2026-08-01-1905-1-nop-ai-agent-plan-replan-rollback-split.md`

## Plan `2026-08-01-1905-1-nop-ai-agent-plan-replan-rollback-split` Phase 1 执行（W1-4 余：决策载荷 + ROLLBACK_PHASE 运行时）

- **背景**：mission-driver EXECUTE 步骤。收口 W1-4 余项——把前置 plan 延后的 `ROLLBACK_PHASE`（阶段回退）运行时 enactment 落地，使 plan 级重规划决策空间（§14.4.2 的 5 值）全部可达。
- **Phase 1（Fix | Decision | Proof — 决策载荷 + executor 控制流改造 + ROLLBACK_PHASE enactment）**：
  - **Decision（决策载荷形态）**：引入结果对象 `ReplanDecisionResult(type, targetPhase, targetTaskNo, triggerSignal, reason)`（`io.nop.ai.agent.plan.runtime`）。`decide()` 返回类型 `ReplanDecision` → `ReplanDecisionResult`；`apply(ReplanDecisionResult, state)`。拒绝"apply 传事件"备选——`PlanExecutionResult.getDecisionsEnacted()` 须记录决策，结果对象使记录含载荷（可观测）。结果对象不可变 + `idempotencyKey()`（结构字段 only）+ equals/hashCode。
  - **Decision（触发条件裁定，避免 Protected Area）**：ROLLBACK/SPLIT 触发声明形态 = **构造期 `ReplanPolicy`**（`new PlanReplanner(policy)`），**非** `GateOnFail` 枚举扩展（`{retry,block,escalate}` 不变），**非** 新 xdef `<replanPolicy>` 元素。理由：(1) `GateOnFail` 扩展是 codegen 级联（Protected Area plan-first）；(2) replanner 是运行时组件，策略是构造参数（同 `StagnationDetector(stale,maxErrors)` 既有模式）；(3) 阈值是生产调参（Non-Blocking Follow-up）。`ReplanPolicy.escalateOnly()`（默认）零回归——无策略时所有信号→ESCALATE。
  - **信号→决策映射**（policy 驱动纯函数）：`GATE_EXHAUSTED`/`TASK_STALLED`/`REPEATED_ERRORS` 在 rollback-eligible phase → `ROLLBACK_PHASE`（target=注册前置 phase），否则→`ESCALATE`；`TASK_STALLED` 在 split-eligible task → `SPLIT_TASK`（Phase 2）。批 `decide(events)` 按优先级取最严重：ESCALATE/ABORT > ROLLBACK > SPLIT > CONTINUE。
  - **StagnationDetector 事件增强**：task 级事件（`TASK_STALLED`/`REPEATED_ERRORS`）由 detector 在 `StagnationEvent.targetPhase` 携带 owning phase 名（之前为 null），使 replanner 决策无需重新解析 task→phase 归属，保持 decide() 纯函数性。
  - **executor 控制流改造（ROLLBACK 前置阻断项）**：`PlanExecutor.execute()` phase 循环从单向 for 改为可重入 while——ROLLBACK（可恢复）不终止 + 按 `state.getCurrentPhase()` 重设 phaseIdx 回跳；recoverable 决策计数入 cycle-safety bound（复用 `computeSafetyBound`，超阈值抛 `IllegalStateException` 防 ROLLBACK↔推进 死循环）；ESCALATE/ABORT/gate-BLOCKED 仍终止。引入 `StopOutcome`（proceed/recoverable/terminal）区分可恢复 vs 终止。
  - **ROLLBACK_PHASE enactment**（`PlanReplanner.apply` → `enactRollback`）：(1) 目标 phase task `completed→pending`（仅该 phase）+ phase status→pending；(2) 源 phase（currentPhase）停滞状态清理——`AgentPlanError.resolvedAt` 首个业务 writer + consecutive-failure 清零 + gate-exhaustion marker 清除（`PlanExecutionState.clearGateExhausted` 新增，打破 detect→rollback 循环）+ phase status 回退；(3) currentPhase 移回目标。冻结模板不突变（§14.4.3）。新增 `PlanExecutionState` 辅助：`hasPhase`/`getPhaseNames`/`phaseTaskNos`/`phaseOwningTask`/`resolveErrorsForTask`/`clearGateExhausted`。
  - **级联改造**：`PlanExecutionResult.getDecisionsEnacted()` 类型 `List<ReplanDecision>` → `List<ReplanDecisionResult>`；`TestPlanExecutorEndToEnd.CountingReplanner` 覆盖 decide/apply 新签名；`ReplanDecision` 枚举 javadoc 更新（ROLLBACK/SPLIT 不再"延后 successor"）。
  - **测试**（全绿）：`TestStagnationDetector`（9，task 级事件 owning phase 增强不破坏既有断言）；`TestPlanReplanner`（19：3 信号默认 ESCALATE + 空 batch CONTINUE + **幂等（含载荷）** + policy 驱动 ROLLBACK 决策（GATE_EXHAUSTED/TASK_STALLED/REPEATED_ERRORS on rollback-eligible phase）+ batch 优先级 ESCALATE>ROLLBACK + **ROLLBACK apply 真实 enactment**（task 回 pending、错误 resolved、计数清零、gate marker 清除、currentPhase 移回、resolvedAt 非 null 首个 writer）+ SPLIT/ABORT 快速失败 + decideAndApply）；`TestPlanExecutorEndToEnd`（11：原 7 零回归 + **ROLLBACK 端到端**（task stalled→ROLLBACK→不终止→回退 P1→重新推进→P2 第二次成功→plan completed，Minimum Rules #22）+ **GATE_EXHAUSTED→ROLLBACK 端到端**（gate 耗尽→rollback→重入→gate criterion 翻转后 PASSED）+ **cycle-safety bound**（P1→P1 永久 rollback 抛 IllegalStateException）+ **零回归**（无 policy 时 stall 仍 ESCALATE））。
  - **设计约束落地**：冻结模板永不被突变（既有 `doesNotMutateFrozenTemplate` 测试仍绿）；幂等（`ReplanDecisionResult.idempotencyKey()`）；无静默跳过（ROLLBACK 真实改变状态，ABORT/SPLIT(Phase 1) 抛 UOE）。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS（含上游模块，全 0 failures）；39 runtime 测试绿。
- **文档同步**：design §14.4 落地状态段更新（ROLLBACK wired + 载荷形态 + 触发条件裁定）；§14.4.1 task 级事件 owning phase 载荷；§14.4.2 决策契约表 + 载荷形态裁定 + ReplanPolicy 触发条件裁定 + 信号→决策映射 + 确定性边界；§14.4.3 ROLLBACK enactment 算法规格 + executor 控制流改造。
- Plan: `ai-dev/plans/2026-08-01-1905-1-nop-ai-agent-plan-replan-rollback-split.md`

## Plan 324 flux-web complex 页面渲染 — closure 收口（验证型 plan 关闭）

- **背景**：plan 324 为收口验证型 plan（实现已由 commit `f7c45373d` 落地）。三个 Phase（实现正确性 / 文档一致性 / 工具+closure）此前为 `planned`，本次完成全部执行项与 closure audit，关闭 plan。
- **验证（Phase 1）**：live 核对 `xview.xdef:226-231`（complex `xdef:bean-tag-prop="type"` + 四槽位）、`impl_GenPage.xpl:37-39`（complex 分派）、`page_complex.xpl`（四槽位 `xpl:if` 守卫 + `GenContainerModel` 分派）、`flux-web.xlib:59-61`（`GenContainerModel` otherwise 显式抛 `nop.err.web.unknown-page-type`，no-silent-noop）。`./mvnw test -pl nop-frontend-support/nop-web -am` = BUILD SUCCESS，71 tests 0 failures 1 skipped（pre-existing @Disabled），TestFluxWebGen 18 例（含 `testComplexPageEmitsFourSlots` 端到端断言四槽位 form/crud 分派结果）。
- **文档一致性（Phase 2）**：核对 `flux-rendering.md:354-373`、`source-anchors.md:32` EXT-009、design doc `flux-web-container-body-rendering.md` R3/结论 5/§3.5、本日志 complex 条目均与 live 一致。**附带事实修正**：`flux-web.xlib:28-32` 注释原"body 容器类型不含 picker"不准确（picker 是 `UiContainerModel` 子元素，`xview.xdef:121`），改为"UiContainerModel 六类（含 picker），picker 不在分派之列落入 otherwise 抛错"。`flux-rendering.md:291` 表述本身准确，无需改。
- **工具 + closure（Phase 3）**：`scan-hollow-implementations.mjs --module nop-frontend-support/nop-web --severity high` 退出码 0；`check-doc-links.mjs --strict` 退出码 0；独立子 agent closure-audit（fresh session `ses_0430d603bffeU8DYcyQss1Z5Vm`，非实现者）OVERALL VERDICT: PASS——五个 Closure Gates + Anti-Hollow (a)(b)(c)(d) 逐条核实，deferred 项分类诚实。evidence 已写入 plan `Closure` 段；`check-plan-checklist.mjs --strict` 退出码 0。Plan Status → completed。
- **roadmap**：本 plan 无对应 roadmap 工作项（`> Source` 为 commit + plan 323 successor，非 roadmap）；successor 跟踪在已 closed 的 plan 323，依 plan guide #20 不回写历史计划。
- Plan: `ai-dev/plans/324-flux-web-complex-page-rendering.md`

## Plan `2026-08-01-1505-2-nop-ai-agent-plan-replanner` Phase 2 执行（W1-4 最小首切：停滞检测 + 幂等 replan）

- **背景**：Phase 1 design 裁定后，落地最小首切运行时——PlanExecutor 宿主 + 停滞检测器 + 幂等 replanner（ESCALATE/CONTINUE wired）。
- **Phase 2（Fix | Proof — 最小首切运行时）**：
  - **新增 9 个运行时类**（`io.nop.ai.agent.plan.runtime`）：
    - `PlanExecutor`（宿主）：消费 `PlanScheduler.getReadyTasks`+`PlanRunner.checkGate` 驱动 plan/phase/task 状态机，经外部 `TaskRunner` 回调推进单 task，记录错误 → 使今日"产出无人消费"的两 helper 有了真实消费者。运行在 mutable `PlanExecutionState` overlay（冻结模板只读，freeze 裁定落地）。
    - `PlanExecutionState`（mutable overlay）：镜像 task/phase 运行时 status + 连续失败计数 + `AgentPlanError` 运行时记录 + gate 耗尽标记 + `statusProvider()` 供 scheduler 读运行时状态。
    - `StagnationDetector`（纯查询）：消费 execution state 产出 3 种结构化停滞事件（GATE_EXHAUSTED/TASK_STALLED/REPEATED_ERRORS）。
    - `PlanReplanner`（纯决策 + 执行）：`decide` 纯函数（3 信号→ESCALATE；空→CONTINUE）+ `apply`（ESCALATE→escalated；CONTINUE→noop；ROLLBACK_PHASE/SPLIT_TASK/ABORT→`UnsupportedOperationException`，非静默跳过）。
    - `ReplanDecision`/`StagnationSignalType` 枚举 + `StagnationEvent`（含 `idempotencyKey()`，排除 wall-clock 时间字段）+ `TaskRunner`/`TaskOutcome`/`PlanExecutionResult`。
  - **PlanScheduler 扩展**：新增 `getReadyTasks(plan, statusProvider)` 重载（读运行时 overlay 状态），原 `getReadyTasks(plan)` 委托之（零回归）。PlanRunner/AgentPlanValidator 不改。
  - **Anti-Hollow**：host 真实推进状态机 + 记录真实错误；停滞事件源于真实状态机（task 连续失败 / gate 耗尽经 PlanRunner 真实判定），非合成事件。ROLLBACK/SPLIT 未实现显式抛 `UnsupportedOperationException`。
  - **测试**（全绿）：`TestStagnationDetector`（9：3 信号各 happy/threshold/terminal + 多信号 + 幂等 key）、`TestPlanReplanner`（11：3 信号决策 + 空 batch + **幂等** + ESCALATE/CONTINUE apply + 3 个 unsupported 快速失败）、`TestPlanExecutorEndToEnd`（7：happy path / **TASK_STALLED 端到端** / **REPEATED_ERRORS 端到端** / **GATE_EXHAUSTED 端到端** / **幂等** / DAG 依赖 / **冻结模板不被突变** + CountingDetector/CountingReplanner **接线验证**）。
  - **设计约束落地**：冻结模板永不被突变（`doesNotMutateFrozenTemplate_loadedPlanStaysIntact` 测试 freeze(true) 后执行仍 escalate + 模板 frozen()=true）；幂等（`idempotency` 测试同停滞状态同决策）。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` = 2990 tests 0 failures；`./mvnw clean install -DskipTests -pl nop-ai -am` BUILD SUCCESS；`./mvnw compile -pl nop-ai -am` 全绿。checkstyle:check 为 advisory standalone goal（现有 PlanRunner/PlanScheduler/PlanDagBuilder 同样违反该严格规则，build 不绑定，非阻断门禁）。
- **文档同步**：design §14.4 落地状态段已记录（PlanExecutor/StagnationDetector/PlanReplanner + ESCALATE/CONTINUE wired + ROLLBACK/SPLIT 契约定义延后 successor）；§14.4.3 freeze 裁定机制精确化为 execution-state overlay（ResourceComponentManager freeze(true) + cloneInstance 浅拷贝事实）。
- **Closure**：独立子 agent closure-audit（task ses_0431e95ffffeQvd2uVsWrPcrZQ）验证全部 claims PASS（2990 tests 绿 / hollow-scan 0 / git 确认零回归 / roadmap W1-4 未提前勾选），CLEARED FOR CLOSURE。check-plan-checklist.mjs --strict exit 0；check-doc-links.mjs --strict exit 0。Plan Status → completed。
- **roadmap W1-4 状态（partial-landing）**：依本计划 "roadmap W1-4 勾选声明"，**W1-4 不勾选**——停滞检测 + 幂等 replan 决策（ESCALATE/CONTINUE）+ design 已交付，但 ROLLBACK_PHASE（阶段回退）/ SPLIT_TASK（任务拆分）运行时延后 successor。roadmap `[ ] W1-4` 保留并加 partial-landing 注记；W1-4 完整勾选须等 ROLLBACK/SPLIT successor。
- Plan: `ai-dev/plans/2026-08-01-1505-2-nop-ai-agent-plan-replanner.md`

## Plan `2026-08-01-1505-2-nop-ai-agent-plan-replanner` Phase 1 执行（W1-4 design 补齐 + host 裁定）

- **背景**：mission-driver EXECUTE 步骤。执行 PlanReplanner design + 最小首切 plan。Phase 1 = §14.4 design 补齐（4 pillars + freeze + 幂等 + checkpoint）+ host-runtime 裁定 + §13.3/W2-3 reconcile。
- **Phase 1（Decision — design 裁定）**：
  - **host-runtime 裁定**：选 (i) 自建最小 plan 执行器（`PlanExecutor`）作为 replanner 宿主。理由：(iii) 塌缩为 (i)——现有引擎无 phase-transition hook，ReAct 循环作用单 session，无法驱动 phase/task 状态机；(ii) nop-task 迁移是 Out-Of-Scope successor，违反 host 裁定 gate（所选 host 须使 Phase 2 单计划可关闭），故排除。(i) 消费 `PlanRunner.checkGate`+`PlanScheduler.getReadyTasks` 驱动状态机 + 记录 `AgentPlanError`，使今日"产出无人消费"的两 helper 有了真实消费者。
  - **停滞信号集**：3 个 plan 级信号——`GATE_EXHAUSTED`（gate `RETRY_EXHAUSTED` 结构性判定）、`TASK_STALLED`（task 连续 `staleTaskCycles` 周期无状态推进）、`REPEATED_ERRORS`（同 `relatedTaskNo` 未解决 `AgentPlanError` ≥ `maxErrorsPerTask`）。ReAct 级 `SessionGoalTracker` STUCK 不并入（单 session 级、整 session 中止，语义层不同）。
  - **决策契约**：`ReplanDecision` = {CONTINUE, ESCALATE, ROLLBACK_PHASE, SPLIT_TASK, ABORT}。确定性边界：决策是输入停滞状态纯函数；GATE_EXHAUSTED/TASK_STALLED/REPEATED_ERRORS→ESCALATE，无停滞→CONTINUE。首切 wire ESCALATE/CONTINUE；ROLLBACK/SPLIT 契约定义，调用即快速失败。
  - **freeze 裁定**：`ResourceComponentManager` 加载时 `freeze(true)` 级联深冻结模板，且 `cloneInstance()` 浅拷贝共享冻结子模型 → host 运行在 mutable execution-state overlay（`PlanExecutionState` 镜像 task/phase 运行时 status + 错误记录），冻结 xdef 模板只读不被突变。
  - **幂等 + checkpoint 裁定**：输入状态 hash 仅含结构性/序数字段（信号类型、目标 identity、计数），wall-clock 时间字段排除→同状态同决策。replan 决策**不入 reliability checkpoint**（reliability 是 ReAct-loop 级；plan-level 随 plan 模型自身持久化；幂等由纯函数结构保证，崩溃恢复重放相同停滞状态重现相同决策）。
  - **边界 reconcile**：W2-3 三级失败升级（单 attempt 内）vs W1-4 replan ESCALATE（attempt 累积之上）互补不重叠；`DenialSuggestedStep.REPLAN`（security 单步否认恢复）与 plan 级 replanning 无关，命名偶合。
  - **文档同步**：design §14.4 从"方向 only"重写为含算法规格（§14.4.1-14.4.6）；§14.5 加 host 裁定结论；reliability §13.3 加 W1-4 边界 reconcile 标注。
  - **引用核对**：`PlanRunner.checkGate`（:53）、`PlanScheduler.getReadyTasks`（:50）、`GateCheckResult.Outcome`（:22-49）、`SessionGoalTracker` STUCK（:118-141）、`AbstractComponentModel.freeze()`（`_AgentPlan.java`:752）均与 live 一致。
- Plan: `ai-dev/plans/2026-08-01-1505-2-nop-ai-agent-plan-replanner.md`

## Plan `2026-08-01-1505-1-nop-ai-account-fallback-chain` 全阶段执行（W2e-4-余 + W2e-5 账号回退链 + QUOTA/AUTH→FALLBACK）

- **背景**：mission-driver EXECUTE 步骤。执行账号回退链 plan（W2e 收口）。三 Phase 全部落地。
- **Phase 1（Decision — design 裁定）**：
  - **裁定 A（持久化形态）**：纯配置文件（`{provider}.llm.xml` 的 `<accounts>` 有序备用账号链，apiKey 直配值/生产经 config 变量替换/secret 注入，与 `baseUrl` 同源模式）；拒绝 DB-backed（`NopAiModel.apiKey` 不被 live call path 使用 + ORM 结构变更属 protected area）。`<accounts>` 是备用账号链（不含主账号），主账号 = `resolveApiKey(provider)`（零回归）。额度元数据 `quotaLimit`/`renewAt` 仅声明/诊断，不做主动熔断。
  - **裁定 B（跨层访问契约，核心难点）**：依赖方向 `agent→core→api`，`IChatService.call` 无 account 参数 → agent 层无法直接注入 apiKey。裁定：(1) nop-ai-api 载体 = `ChatOptions` 新字段 `accountKey`/`accountBaseUrl`（均 `@JsonIgnore`，永不序列化进请求体/日志）；(2) 链解析在 nop-ai-core（`LlmConfigHelper.resolveAccountChain`），"下一个账号"语义 + 游走在 agent 层（`AccountChain` + 重试循环局部状态）；(3) 下沉路径：agent 层设 accountKey 到 routedOptions → ChatServiceImpl.buildHttpRequest 读它。拒绝 chat-service 内部自管理（违反正交原则）。
  - 回写 design §3.6 `:252` 占位为裁定 A+B；§4.4 补两通道运行时路径；§6.6 修正"装饰器"措辞为"ChatServiceImpl 内置（非装饰器）"；§6.8 补原子性约束。
- **Phase 2（数据与解析，无行为变更）**：
  - `llm.xdef` 新增 `<accounts xdef:key-attr="id">` + `<account id apiKey baseUrl quotaLimit renewAt>`；两步重打包（`mvn install -pl nop-xdefs` + `mvn clean compile -pl nop-ai-core`）→ codegen `_LlmAccountModel` + `LlmAccountModel` + `_LlmModel._accounts/getAccounts`。
  - `LlmConfigHelper.resolveAccountChain(provider)` 返回有序不可变 `List<LlmAccountModel>`，无链返回 `emptyList()`（非 null）。
  - `ChatOptions` 新增 `accountKey`/`accountBaseUrl`（`@JsonIgnore`），手写 `copy()`/`merge()`/`Builder` 三处同步（Rule #11 防静默丢账号）。
  - `ChatServiceImpl.buildHttpRequest`：读 `options.getAccountKey()`（非空用它，空退 `resolveApiKey`）+ `accountBaseUrl` 覆盖；`resolveBaseUrl` 加 accountBaseUrl 参数。
  - agent 层 `AccountChain`（stateful walker，cursor 推进）+ `IAccountChainResolver`（@FunctionalInterface，生产默认 config-based）。
  - 测试：`TestLlmConfigHelperAccountChain`（3 账号有序/baseUrl/quotaLimit/renewAt + 无链 emptyList + 不可变）、`TestChatServiceImplAccountRequest`（accountKey→Bearer token、accountBaseUrl→URL、无 accountKey→resolveApiKey 零回归）、`TestChatOptions` account 字段（copy/merge/Builder/default-null）。
- **Phase 3（QUOTA/AUTH→FALLBACK + 分类路由 + 账号链游走，原子落地）**：
  - `StandardRetryPolicy`：QUOTA_EXCEEDED/AUTH_INVALID → `RetryOutcome.fallback()`（design §6.8，从 STOP 改）；RATE_LIMITED/TRANSIENT 保持 RETRY；NON_TRANSIENT/CACHE_STATE_LOST 保持 STOP。
  - `LlmCallCoordinator` 重构：新增 8-arg ctor（`IAccountChainResolver`，null 退回 config-based 默认）；保留 7-arg ctor 向后兼容。响应级 FALLBACK 按 `errorClassification` 分流——QUOTA/AUTH→`doAccountSwitch`（账号链，惰性解析 + cursor 跨迭代保留）；else→`doModelTierFallback`（模型 tier，行为不变）。传输级 FALLBACK（catch）恒模型 tier（启发式从不产 QUOTA/AUTH per §6.1）。链耗尽 fail-loud：`fallbackExhausted` 局部变量 + 循环外 `throw`（避免被 catch 误当传输异常重试，design §6.9）。`doFallbackSwitch` 重命名为 `doModelTierFallback`（返回 null 而非 throw，调用方统一 fail-loud）。
  - 测试：`TestStandardRetryPolicy` `quotaExceededFailsFastImmediately` → `quotaExceededReturnsFallback`（断言 isFallback）+ 新增 `authInvalidReturnsFallback`；`TestAccountFallbackChain` 端到端（QUOTA→多级账号切换→成功 / 链耗尽 fail-loud / 无链 fail-loud / QUOTA 不调 getFallback 计数==0 两通道区分）。`TestRetryPolicyWiring`/`TestLlmCallCoordinatorErrorResponse` 零回归通过。
  - 顺手清理 `LlmCallCoordinator` 既有的重复/triplicate import（closure audit 发现的既有 style defect）。
- **构建验证**：`./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-agent,nop-ai/nop-ai-api -am -T 1C` = 2963 tests 0 failures；`./mvnw compile`（全量）exit 0；`./mvnw clean install -DskipTests -pl nop-ai -am` BUILD SUCCESS；`check-plan-checklist.mjs --strict` exit 0；`check-doc-links.mjs --strict` exit 0。
- **文档同步**：design §3.6（裁定 A+B 回写）/§4.4（两通道运行时路径）/§6.6（非装饰器）/§6.8（原子性约束）；`nop-ai-agent-reliability.md` §3.1 实现状态段补 W2e-4/W2e-5；roadmap W2e-4/W2e-5 ❌→✅。
- **Closure**：独立子 agent closure-audit（task ses_04339d5eaffehCwVIYtGjKSkCx）验证 10/10 claims PASS（逐条 live code 证据，CLEARED FOR CLOSURE）。Plan Status → completed。
- Plan: `ai-dev/plans/2026-08-01-1505-1-nop-ai-account-fallback-chain.md`

## Plan `2026-08-01-1440-2-nop-ai-agent-plan-runtime-gating` Phase 2 执行（W1-2 + W1-3 Trigger Rule + DAG）

- **背景**：mission-driver EXECUTE 步骤。执行 Agent Plan DSL 运行时门控 plan Phase 2（Trigger Rule + DAG 就绪计算与加载时校验）。Phase 1（Gate 门控）已完成。
- **Phase 2（Trigger Rule + DAG W1-2/W1-3）**：
  - **xdef 变更**：`agent-plan.xdef` `<task>` 增 `triggerRule` 属性（枚举 `io.nop.ai.agent.plan.model.TriggerRule`）。
  - **新增枚举**：`TriggerRule`（all_success|one_success|none_failed_min_one_success|all_done）在 `io.nop.ai.agent.plan.model` 包。
  - **两步重打包**：①`mvn install -pl nop-xdefs` ②`mvn clean compile -pl nop-ai-agent -am`——`_AgentPlanTaskModel` 含 `_triggerRule` 字段。
  - **新增 `PlanDagBuilder`**（环检测桥）：递归扁平化所有 phase 的 task（含 subTasks）为全局 DAG → 构建 `GraphTaskStepModel` → 调 nop-task 真实 `GraphStepAnalyzer.analyze()` 做环检测（同 `TeamTaskGraphBuilder` 模式）。校验：taskNo 全局唯一、dependsOn 悬空引用、DAG 环——均 fail-fast。空 plan 返回空图（合法）。
  - **新增 `PlanScheduler`**：按 trigger rule + 全局 DAG 拓扑 + 当前 task 状态计算就绪任务集（all_success=所有依赖 completed；one_success=任一 completed；none_failed_min_one_success=≥1 completed 且 0 failed；all_done=所有依赖终结态）。
  - **新增 `AgentPlanValidator`**：加载时校验（currentPhase 存在、phase.name 唯一、taskNo 全局唯一、dependsOn 悬空、DAG 环检测）——**经 `AgentPlan.init()`（`INeedInit` hook）接入真实加载路径**（`DslModelParser.doParseNode0()` 在解析后自动调 `init()`，同 `TaskFlowModel` 模式）。
  - **接线**：`AgentPlan` 实现 `INeedInit`，`init()` → `new AgentPlanValidator().validate(this)`。
  - **测试**：`TestPlanScheduler`（12 测试：4 种 trigger 各 2 场景 + 无依赖/终结态/空 plan 边界）；`TestPlanDagBuilder`（11 测试：有效 DAG/空 plan/环检测（2-node/自环/3-node）/悬空依赖/跨 phase 重复 taskNo/跨 phase 依赖/跨 phase 环/subTask 扁平化/subTask 依赖）；`TestAgentPlanValidatorLoading`（3 端到端测试：**Anti-Hollow 接线验证**——加载含环 plan → 抛异常；加载含悬空依赖 plan → 抛异常；加载有效跨 phase plan → 成功）。全 26 测试绿。
  - **文档同步**：design §14.2/§14.3 落地标注（含 DAG 全局扁平化裁定）；§12 更新"依赖拓扑执行器"为已建立；§7 更新校验项 1/4 接入说明。
  - **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am` 全绿（含上游模块）；`./mvnw compile -pl nop-ai -am` 全绿（跨模块无下游破坏）。

## Plan `2026-08-01-1440-2-nop-ai-agent-plan-runtime-gating` Phase 1 执行（W1-1 Gate 门控）

- **背景**：mission-driver EXECUTE 步骤。执行 Agent Plan DSL 运行时门控 plan（W1-1/W1-2/W1-3）。Phase 1（Gate 门控）已完成。
- **Phase 1（Gate 门控 W1-1）**：
  - **xdef 变更**：`agent-plan.xdef` `<phase>` 增 `<gate>` 子元素——属性 `on-fail`（枚举 `io.nop.ai.agent.plan.model.GateOnFail`）、`max-retries`（int）、`require-explicit-verdict`（boolean）、`verdict`（boolean）；子节点 `<criteria>` 复用 `AgentPlanCriterion`。
  - **新增枚举**：`GateOnFail`（retry|block|escalate）在 `io.nop.ai.agent.plan.model` 包。
  - **新增 model**：`AgentPlanGate`（继承 `_AgentPlanGate`）。
  - **两步重打包**：①`mvn install -pl nop-xdefs` ②`mvn clean compile -pl nop-ai-agent -am`——`_AgentPlanGate` 含 `criteria`/`maxRetries`/`onFail`/`requireExplicitVerdict`/`verdict` 字段；`_AgentPlanPhase` 含 `_gate` 字段，均生成成功。
  - **新增运行时**：`io.nop.ai.agent.plan.runtime.PlanRunner`（`checkGate(phase, attempt)` → `GateCheckResult`）实现完整判定语义：required 全满足→PASSED；required 未满足→按 on-fail（retry/blocked/escalated）；blocking 未满足→hard failure；require-explicit-verdict=true 无 verdict→EXPLICIT_VERDICT_REQUIRED（不静默放行）；retry 达 max-retries→RETRY_EXHAUSTED。
  - **新增 `GateCheckResult`**：不可变值对象，含 Outcome 枚举（PASSED/RETRY/RETRY_EXHAUSTED/BLOCKED/ESCALATED/EXPLICIT_VERDICT_REQUIRED）+ unsatisfiedRequired/Blocking 列表 + attempt/maxRetries/onFail。
  - **测试**：`TestPlanRunnerGateSemantics`（15 测试：无 gate→PASSED、全 required 满足→PASSED、optional 未满足→PASSED、required 未满足×3 on-fail、blocking 未满足硬失败、require-explicit-verdict 无/有 verdict、verdict=true 但 criteria 未满足仍失败、默认 on-fail=block）；`TestPlanGateLoading`（端到端：加载含 gate 的 .agent-plan.xml → PlanRunner.checkGate PASSED）。全 16 测试绿。
  - **文档同步**：design `nop-ai-agent-plan-dsl.md` §14.1 落地标注（含 verdict 字段语义说明）；§7/§8 同步 gate 阻断结束语义 + AgentPlanValidator 接入说明。
  - **构建验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am` 全绿（含上游模块）；现有 plan 加载测试（TestAgentPlanMarkdownLoader/TestAgentPlanRegisterModelScope）无回归。
- Plan: `ai-dev/plans/2026-08-01-1440-2-nop-ai-agent-plan-runtime-gating.md`

## Plan `2026-08-01-1440-1-nop-ai-llm-error-normalization-signal-path` 全阶段执行（W2e-0..3 信号通路）

- **背景**：mission-driver EXECUTE 步骤。执行 LLM 错误规范化信号通路前半段 plan（W2e-0..3 + RATE_LIMITED floor）。三 Phase 全部落地，QUOTA/AUTH→FALLBACK + 账号链延期到 successor（W2e-4-余 + W2e-5）。
- **Phase 1（nop-http 头传递）**：`HttpApiErrors` 新增 `ARG_RESPONSE_HEADERS` 常量；JDK + Apache 两个 `ServerEventPublisher` 非 2xx 抛异常时挂 `.param(ARG_RESPONSE_HEADERS, headers)`。JDK 变体额外修复非 2xx 异常被 `whenComplete` 吞掉的传播缺口（路由到 `onError`，与 Apache 变体一致——Phase 2 流式 onError 依赖此）。为使 headers Map 经 NopException 传递不被 `normalizeValue` 字符串化，修正 `NopException.normalizeValue` 保留 `Map`/`Collection`（渲染仍走 toString，向后兼容）。单测用 in-process `com.sun.net.httpserver.HttpServer`（无外部网络）断言异常可取到 Retry-After 头。
- **Phase 2（类型归属 + ChatResponse + dialect + ChatServiceImpl）**：
  - **类型裁定**：`ErrorClassification` 从 `nop-ai-core` 迁到 `nop-ai-api`（`io.nop.ai.api.chat.ErrorClassification`）——依赖图唯一解（nop-ai-api 是 ChatResponse/core/agent 三方共可见最低层）；删旧 core 枚举 + 删 agent reliability 桥接（双类型收口）；更新 `llm.xdef` 三处 `!enum:`/`classifyError` 引用 + 重打包 nop-xdefs + 重编译 nop-ai-core 使 `_LlmErrorMappingModel` 引用更新；reliability 层（RetryContext/StandardRetryPolicy/LlmErrorClassifier/IRetryPolicy/RetryOutcome/LlmCallCoordinator）全量迁移到统一类型。
  - `ChatResponse` 增 `errorClassification`/`retryAfterMs`/`httpStatus` 三字段 + `copy()` + `error(...)` 工厂。
  - `ILlmDialect.parseErrorResponse`（实现在 `AbstractLlmDialect`）：配置驱动 first-match-wins over `<errorMappings>`，未命中走默认启发式（零回归：429→RATE_LIMITED/5xx→TRANSIENT/4xx→NON_TRANSIENT），归一 Retry-After 多源（retry-after-ms 头 / retry-after 头 / body 字段）为 `retryAfterMs`。复刻 dialect 消息正则（下划线替空白、DOTALL、CASE_INSENSITIVE、全匹配）。
  - `ChatServiceImpl`：非流式非 200 读 body+头→`parseErrorResponse`→返回错误 ChatResponse（不抛 `ERR_AI_SERVICE_HTTP_ERROR`）；流式 `aggregateStreamToResponse.onError` 经 `parseStreamError` helper 从异常取 ARG_BODY/ARG_HTTP_STATUS/ARG_RESPONSE_HEADERS→`parseErrorResponse`→complete 错误 ChatResponse。重构使 dialect/config 在 onError 闭包可用。
  - **契约变更消费者审计**：`LlmCallCoordinator`（Phase 3 接线）/`SingleTurnExecutor`（!isSuccess()+catch 都→failed，健壮）/`LlmCompletionJudge`（!isSuccess()→fallback，原 catch→fallback，健壮）/`Layer3FullSummaryStrategy`（!isSuccess()→degrade，原会崩，改进）。无残留。
  - 测试：`TestLlmDialectErrorResponse`（10：QUOTA/RATE_LIMITED/AUTH/NON_TRANSIENT/TRANSIENT + retryAfterMs 归一 + 零回归启发式 + 非 JSON 不崩）、`TestChatServiceImplErrorResponse`（4：非流式 429 QUOTA/RATE_LIMITED + 503 + 传输异常仍抛，用 fake IHttpClient）。
- **Phase 3（可靠性层 RETRY 决策 + RATE_LIMITED floor）**：
  - `RetryContext` 增 `retryAfterMs`（5-arg 构造器，保留 4-arg 向后兼容）。
  - `LlmCallCoordinator.doLlmCallWithRetry`：`!isSuccess()` 分支从"终止"改为读 `getErrorClassification()`→RetryContext(含 retryAfterMs)→`shouldRetry` 进入 RETRY/STOP/FALLBACK；抽 `doFallbackSwitch` helper 复用（响应级与传输级两路径共享）；QUOTA/AUTH 仍 STOP（零回归）。
  - `StandardRetryPolicy`：RATE_LIMITED 用 `retryAfterMs` 作 floor（`delay=retryAfterMs+uniform(0,jitterCap)`，永不低于 retryAfterMs，可超 maxDelay）；TRANSIENT 纯 full-jitter；无 retryAfterMs 退回纯 full-jitter；抽 `backoffCap` 共享。
  - 测试：`TestStandardRetryPolicy` +5（RATE_LIMITED floor≥retryAfterMs、随 attempt 增长、无 retryAfterMs 退回 full-jitter、TRANSIENT 不受影响、retryAfterMs>maxDelay 也遵守）；`TestLlmCallCoordinatorErrorResponse`（3：RATE_LIMITED/TRANSIENT 触发 RETRY callCount≥2、QUOTA 立即 STOP callCount==1，用 DefaultAgentEngine + 返回错误 ChatResponse 的 IChatService）。
- **构建验证**：Phase 1 `clean install` nop-http 四模块全绿；Phase 2-3 `clean install` nop-ai-api/core/agent 全绿（nop-ai-agent 2916 tests / nop-ai-core 151 tests / 全 0 failures）；全量 `mvn compile` BUILD SUCCESS（跨模块 API 变更无下游破坏）；checkstyle CLI 对存量代码报 9157 无关违例（非实际门禁）。
- **文档同步**：design §3.7（RATE_LIMITED floor 落地）/§3.8（类型归属 nop-ai-api + 依赖图）/结论 #5/§6.2；`nop-ai-agent-reliability.md` 实现状态段；roadmap W2e-0..3 ❌→✅ + W2e-4 RATE_LIMITED floor 标已落地。
- **Closure**：独立子 agent closure-audit（task ses_043a3eae4ffe0dak1HaNMCtNfU）验证 14 项声明全部 PASS（AUDIT RESULT: PASS）。Plan Status → completed。
- Plan: `ai-dev/plans/2026-08-01-1440-1-nop-ai-llm-error-normalization-signal-path.md`

## mission-driver DRAFT_PLANS：nop-ai-agent-harness-evolution 路线图 W2e + W1 计划起草

- **背景**：mission-driver `nop-ai-agent-harness-evolution` 的 DRAFT_PLANS 步骤。读 `ai-dev/backlog/nop-ai-agent-harness-evolution-roadmap.md` 完整 + plan guide + project-context + 相关 design 文档，选 1-3 个 plan。
- **Baseline 核实**（独立子 agent 核对 live repo，非沿用文档）：
  - W2e 配置层（`llm.xdef` `<errorMappings>`/`<errorResponse>`/`<classifyError>`、`ErrorClassification` 枚举、5 provider 配置、codegen + TestLlmErrorMapping）**已落地**；信号通路（W2e-0..5）**未接通**。
  - 关键发现：`ErrorClassification` 在 `nop-ai-core`，但 `ChatResponse` 在 `nop-ai-api`（更低层，仅依赖 nop-api-core）——**循环依赖，ChatResponse 无法引用该枚举**（design §3.8 未考虑依赖方向，是真实阻断项）。
  - W1：`agent-plan.xdef` 仅 `dependsOn` 属性存在（model 层），**无任何运行时层**（PlanRunner/PlanScheduler/PlanReplanner 全仓 0 命中）；`GraphStepAnalyzer` 可复用；`AgentExecStatus.escalated` 已存在。
- **起草 2 个 plan（执行顺序 N=1..2）**：
  - **N=1 `2026-08-01-1440-1-nop-ai-llm-error-normalization-signal-path.md`**（W2e-0..3）：LLM 错误规范化信号通路前半段 + RATE_LIMITED 配额感知重试。延期 QUOTA/AUTH 账号切换（W2e-4-余 + W2e-5）到 successor（需账号链 design）。**先 unblock W2**。
  - **N=2 `2026-08-01-1440-2-nop-ai-agent-plan-runtime-gating.md`**（W1-1/2/3）：Plan 门控 Gate + Trigger Rule + DAG 调度运行时（从零建）。延期 W1-4 PlanReplanner 到 successor（§14.4 仅方向无算法，需 design-first）。
- **对抗性审查**（2 轮独立子 agent）：
  - R1 发现 Blocker：W2e 类型循环依赖 + 双枚举类型不一致 + 账号链未定义 + Phase3 中间态退化；W1 PlanReplanner 欠定义 + Anti-Hollow 接线矛盾 + Gate 判定黑盒 + 重打包命令不完整 + DAG 作用域未定 + plan 过宽。
  - 修订：W2e 裁定类型迁 nop-ai-api + agent 层类型迁移 + 精确零回归措辞 + Phase2↔3 原子约束；W1 拆出 W1-4 + 定义 Gate 判定语义 + 钉死 `AgentPlan.validate()` 接入 + DAG 全局扁平化(含 subTasks) + 两步重打包 + 重命名 AgentPlanValidator。
  - R2 确认 Blocker 已解决，仅余 plan-text 必修项（已全部修复）。两 plan 达成共识 → **Plan Status: draft → active**。
- **验证**：`check-doc-links.mjs --strict` exit 0（0 errors）。
- Plans: `ai-dev/plans/2026-08-01-1440-1-nop-ai-llm-error-normalization-signal-path.md`、`ai-dev/plans/2026-08-01-1440-2-nop-ai-agent-plan-runtime-gating.md`

## Plan `2026-08-01-1306-1-arm-p2-coder-ai-gen-model-refs` 全阶段执行 + closure audit

- **裁定（6 个 coder 文件不改）**：逐文件追踪解析链，全部解析到目标项目 `${outputDir}` 内生成物而非 `nop-ai/model/` 归档文件：
  - 写侧：`ai-coder.task-lib.xml` `saveOrmModel` 写 `${outputDir}/model/ai-gen.orm.xml`（AI 简化版）+ `${outputDir}/${appName}-dao/src/main/resources/_vfs/${appName.replace('-','/')}/orm/ai-gen.orm.xml`（完整版）
  - 读侧：`ai-api-design.task.xml:47` / `ai-service-design.task.xml:33` / `ai-menu-design.task.xml:55` 均经 `ai-coder:LoadOrmModel`（`ai-coder.xlib:209` `ResourceHelper.resolveResourceInDir(inputDir=${outputDir}, "model/ai-gen.orm.xml")`）
  - 模板侧：`app.orm.xml:2` `x:extends="ai-gen.orm.xml"` 与 `{appName}.action-auth.xml:2` `x:extends="ai-gen.action-auth.xml"` 相对模板所在生成项目目录解析，命中 task-lib 写入的完整版与 `ai-menu-design` 写入的 `${appName}-web/.../auth/ai-gen.action-auth.xml`
  - 顺序自洽：`ai-coder.task.xml` 管线 = createProject → designOrm（写）→ designMenu/designApi/designService（读）→ 模板 extends
  - 结论：**不改为 `nop-ai.orm.xml`**（nop-ai 自身源模型与"AI 为新项目生成的中间模型"概念不同，新项目不应 x:extends nop-ai 自身模型）
- **归档文件消费面（全仓 grep）**：`nop-ai/model/ai-gen.orm.xml`（15 `<entity>`）唯一真消费方 = @Disabled `AiGenCodeTaskManual.saveOrmModel()`（:80 读 `../model/ai-gen.orm.xml`）；docs 4 处（`docs/user-guide/ai-coder/ai-coder-task.md:189,202` + docs-en `:188,201`）为管线语义描述非消费方；demo `_gen/` 快照 git 跟踪不动；`_dump`/`target` 为 gitignore 产物
- **处置（移除路径）**：`git rm nop-ai/model/ai-gen.orm.xml`（误导性 header："Retained because templates reference it"与事实不符——模板只引用同名管线产物）+ 0 字节 `nop-ai/model/ai-gen.orm.xlsx`；删除 `AiGenCodeTaskManual.saveOrmModel()` 方法 + 清理 7 个未使用 import（AiOrmConfig/AiOrmModel/XNode/XNodeParser/FileResource/OrmModel/ExcelReportHelper）；`runCodeGen`/`runCodeGenMock` 零变更；转换语义由 `AiConverterTest.testConvertOrm` 覆盖（输出写 target 目录）；**禁止**改指 canonical 源/禁止运行该测试（AI-result 格式与 canonical 格式不兼容，运行会污染 git 跟踪的 `nop-ai.orm.xlsx`）
- **验证**：`./mvnw compile -pl nop-ai -am -q` 通过；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（tests 0 failures 0 errors）；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0；`check-doc-links.mjs --strict` exit 0；最终 grep 确认 main/resources 中 `ai-gen.orm.xml` 仅剩 6 个 coder 引用（裁定保留面）
- **登记**：roadmap §P2/P3 Deferred Successors 新增 2130 successor 关闭行 ✅；arm-index P0-MA2-01 行追加 successor 关闭注记 + :188 证据文本改写为 live 一致（归档文件已移除）；2130 历史计划不回写（guide #20），承接链记录于本 plan Closure
- **Closure**：独立子 agent closure audit 完成（证据记录于 plan `## Closure` 段）；`check-plan-checklist.mjs --strict` exit 0；Plan Status → completed
- Plan: `ai-dev/plans/2026-08-01-1306-1-arm-p2-coder-ai-gen-model-refs.md`

## flux-web complex 页面类型实现（plan 323 successor，用户驱动）

- **背景**：plan 323 将 complex 页面类型标为 `out-of-scope`（Successor Required: yes），原因为 Flux 无 left/right/top/bottom 四槽位容器。用户将 xview.xdef 的 complex schema 重设计为 `header`/`footer`/`aside`/`body` 四槽位（`xdef:ref="UiPageModel"`），正好一一对应 Flux `PageSchema` 的四区域，绕过了原限制。
- **schema 修复**：complex 定义缺 `xdef:bean-tag-prop="type"`（其他页面类型 crud/simple/tabs/wizard/group 均有），补上后 `pageModel.type == 'complex'` 才能命中分派，否则落入 `GenPageDefault` 抛 `nop.err.web.unknown-page-type`。
- **实现**：
  - `flux-web/impl_GenPage.xpl` 新增 complex 分派 → `page_complex.xpl`
  - 新建 `flux-web/page_complex.xpl`：输出 Flux `PageSchema`，`<header>`/`<footer>`/`<aside>`/`<body>` 四槽位各自经 `GenContainerModel` 分派渲染容器列表；空槽位不输出（`pageModel.header?.body?.size() > 0` 守卫）；页面级属性经 `FluxPageDefaultAttrs` 透传。
  - `GenContainerModel` 不变：complex 仅页面级（`UiContainerModel` 的子元素仅 crud/simple/tabs/wizard/group，不含 complex），不作为 body 子容器。
- **测试**：`TestFluxWebGen.testComplexPageEmitsFourSlots`（view.xml 四槽位分别用 simple/crud 容器，断言 page shell `type='page'` + 四槽位数组 + GenContainerModel 分派结果）。simple→form（name 来自 `formModel.id`）、crud→crud（name 来自 `table.name`）。
- **依赖重建注意**：xview.xdef 在 `nop-xdefs`，改源码后须 `./mvnw install -pl nop-kernel/nop-xdefs -am -DskipTests` 才能让 nop-web 测试加载新 schema，否则运行时仍用旧 jar 的 left/right/top/bottom 定义，报 `node-unexpected-tag-name`（allowedNames 仍含 left/right/top/bottom）。
- **验证**：`./mvnw test -pl nop-frontend-support/nop-web` = 71 tests 0 failures（含新增 complex 测试，TestFluxWebGen 17→18；1 既有 @Disabled skip）；无回归。
- **文档**：`docs-for-ai/02-core-guides/flux-rendering.md` 新增 complex 段落 + 标题/介绍改"三种→四种"；`ai-dev/design/flux-web-container-body-rendering.md` 加 R3 修订记录 + 结论 5/§3.5 从"不实现"改为"已实现"。
- **遗留**：complex schema 未暴露 `asidePosition`（aside 固定左侧）；successor plan 归档待定（plan 323 已 closed）。

## Plan `2026-08-01-1223-1-arm-error-handling-doc-drift-sync` Phase 1 — 盘点 live 错误码语言分布与引用面

- **grep 全量盘点（live 复核，9 个 `*Errors.java` 全覆盖）**：
  - `nop-ai-agent` `NopAiAgentErrors`：22 码全部英文（2248-2 UOE 转换 + 0936-1 IAE 转换）
  - `nop-ai-shell` `NopAiShellErrors`：8 码全部英文（2248-2 + 0936-1）
  - `nop-ai-core` `NopAiCoreErrors`：40 码混合——**25 中文既有业务码**（`no-default-llms`、`no-base-url`、`ERR_AI_TOOLS_INVALID_THOUGHT`（:91，同 `nop.err.ai.tools.*` 命名空间）等）+ **15 英文转换码**（2248-2 `ERR_AI_CHAT_GET_SESSION_DEPRECATED` UOE 1 码 + 0936-2 IAE 7 码 + 0936-3 IAE 7 码）
  - `nop-ai-coder` `AiCoderErrors`：5 码混合——2 中文既有（unknown-sql-type/headers-and-data-not-match）+ 3 英文转换码（0936-2 `ERR_AI_CODER_UNSUPPORTED_CONVERSION`/`METHOD_SIGNATURE_NOT_FOUND`/`UNBALANCED_BRACES`）
  - `nop-ai-maven` `NopAiMavenErrors`：2 码全部英文（1834-3 `vfs-invalid-arg`/`vfs-io-failed`）
  - `nop-ai-code-analyzer` `NopAiCodeAnalyzerErrors`：2 码全部英文（1834-3 `stats-io-failed`/`maven-parse-invalid-arg`）
  - `nop-ai-dsl-orm` `GptOrmErrors` 1 中文、`nop-ai-mcp-server` `McpServerErrors` 1 中文、`nop-ai-service` `NopAiErrors` 1 中文（既有业务码，不在例外面）
  - `nop-ai-tools`/`nop-ai-toolkit` 无 main `*Errors.java`（toolkit 仅 test 目录 `TestLocalToolFileSystemErrors` 测试类，非 ErrorCode 接口）——tools 码在 `NopAiCoreErrors` `nop.err.ai.tools.*` 命名空间
- **grep 证据命令**：`rg -n '[\p{Han}]' <4 个全英文文件>` = NO HAN FOUND（agent/shell/maven/code-analyzer 全英文证实）；`rg -c 'define\("'` 计数（22/8/2/2/40/5）；core/coder 中文 vs 英文 define 行清单见上表
- **引用面清单（grep 命中面）**：`docs-for-ai/02-core-guides/error-handling.md:120`（例外条款本身，Phase 2 更新目标）；其余命中均为默认规则/无关表述、与 live 一致：`docs-for-ai/03-runbooks/feature-implementation-checklist.md:64`（"错误描述用中文"——默认规则陈述，不涉及例外范围）、`docs-for-ai/03-runbooks/error-codes-and-nop-exception.md:11`（禁裸 RuntimeException，不涉语言例外）、`docs-for-ai/03-modules/nop-metadata.md:169`（模块级异常类，不涉语言例外）；`ai-dev/design/` 全量 0 命中 → 引用面同步仅 error-handling.md 自身
- **例外条款新措辞裁定**：模块清单 = agent / shell / core / coder / maven / code-analyzer（tools 码在 `NopAiCoreErrors` 随 core 说明）；分界语义**来源驱动**（批次内从裸 IAE/UOE/RTE 逐字转换、保留历史消息 → 英文；既有业务错误码 → 中文）——`ERR_AI_TOOLS_INVALID_THOUGHT`（中文）与 0936-3 英文码同命名空间共存证明语义类型分界不成立；起源叙事覆盖 UOE（2248-2）+ IAE/RTE（0936-1/2/3、1834-3）两类
- **Phase 2 diff**：`error-handling.md:120` 例外条款从"仅 nop-ai-agent / nop-ai-shell"扩展为 6 模块清单（agent/shell/core/coder/maven/code-analyzer，tools 码随 core 说明）+ 分界语义改写为**来源驱动**（转换码英文 / 既有业务码中文，含 `ERR_AI_TOOLS_INVALID_THOUGHT` 与英文码同命名空间共存的证明）+ 起源叙事覆盖 UOE（2248-2）与 IAE/RTE（0936-1/2/3、1834-3）两类转换来源；引用面同步裁定：除 error-handling.md 自身外 **No owner-doc update required**（feature-implementation-checklist.md:64 默认规则陈述、error-codes-and-nop-exception.md:11、nop-metadata.md:169 均与 live 一致不涉例外范围；`ai-dev/design/` 0 命中）
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 errors）
- **Phase 3 登记**：arm-index「P3 追踪（第十二批）」验证段补收口句（1223-1 计划承接 0936-2 登记的 doc drift，例外条款 6 模块清单 + 来源驱动分界 + 双类起源叙事）；roadmap header v14 → v15 + 尾段叙事补 0936-2 → 1223-1 追溯链；`check-doc-links.mjs --strict` exit 0；`check-plan-checklist.mjs <plan> --strict` exit 0（closure 时）
- Plan: `ai-dev/plans/2026-08-01-1223-1-arm-error-handling-doc-drift-sync.md`

## Plan `2026-08-01-0936-3-arm-p3-error-handling-tools-toolkit` Phase 4 — 全量验证 + 登记

- **全量验证**：`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（3631 tests 0 failures）；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0（0 findings）；`check-doc-links.mjs --strict` exit 0（0 errors）；import-order 113 处既有违规（nop-api-core 等，非本次改动，与改动文件零交集——comm 验证 NopAiCoreErrors/FileToolBizModel/ThoughtData/ThoughtStage/ThoughtAnalyzer/LocalToolFileSystem/2 个新测试文件均 0 违规）；checkstyle 未绑定构建生命周期（lint not configured 回退，与 0936-1/2 一致；NopAiCoreErrors 的 JavadocVariable/LineLength 违规为既有文件级模式，新增 7 码沿既有 ErrorCode 字段风格）
- **登记**：arm-index「P3 追踪（第十二批）」新增 P3-MA3-5 → fixed 行 + toolkit 同规范残余行（9 处 1 文件）+ 小节导语补 0936-3 收口 + 验证段补 0936-3 侧证据（23/122/3631 tests）；roadmap 新增「第十二批（错误处理规范 3）」行 ✅（closed 2026-08-01）+ header v13→v14 + 尾段叙事补 0936-3 落地（tools 7 新码 + toolkit 9 处 NopAiException，P3-MA3-5 fixed，MA3.4 全部 P2/P3 收口）
- **Commit**: 待 closure audit 后随计划收口提交
- Plan: `ai-dev/plans/2026-08-01-0936-3-arm-p3-error-handling-tools-toolkit.md`

## Plan `2026-08-01-0936-3-arm-p3-error-handling-tools-toolkit` Phase 3 — nop-ai-toolkit 9 处转换

- **转换**：`LocalToolFileSystem` 9 处裸 IAE（1 文件）全部 → `throw new NopAiException("<verbatim>")`（按 Phase 1 裁定：模块内消费面仅 `getMessage()`、无程序化错误码消费 → 模式二模块异常类；DefaultToolExecutorProvider 先例；`NopException.adapt` 既有使用面保留不动）：:54（"Path not allowed: "）、:92/:112/:141（"File not found: "）、:168（"Directory not found: "）、:225/:244（"File not found: "）、:229/:248（"Target file already exists: "）——消息逐字零漂移、cause 无（原 IAE 无 cause）
- **验证**：`grep -rn "throw new IllegalArgumentException\|throw new RuntimeException" nop-ai/nop-ai-toolkit/src/main/java` = **0 命中**；`./mvnw test -pl nop-ai/nop-ai-toolkit -am -T 1C` BUILD SUCCESS（122 tests 0 failures，上游依赖模块全绿）
- **测试**：断言影响面 0（executor 测试 catch Exception → errorResult，无 IAE 断言）；新增 **1 个 focused 值级测试文件** `TestLocalToolFileSystemErrors`（+9 @Test，零减少）：7 个消息值级断言（path-not-allowed 越界 `../secret.txt`、readText/readLines file-not-found、listDirectory directory-not-found、move/copy source-not-found、move/copy target-exists——NopException 字符串构造器渲染 `NopAiException[seq=..,errorCode=<verbatim>,..]`，断言 `getMessage().contains(逐字)` 与 0936-1/2 同款）+ `testIsPathAllowedSecurityDecisionUnchanged`（in-workdir 相对路径/绝对路径 allowed、`/sub.txt` 根绝对路径 rejected（resolveFileInternal 对绝对路径原样返回，属既有语义）、`../` 越界 rejected、null rejected——安全判定逻辑零变更对照）
- **格式**：转换行 ≤150 列；io.nop import 组空行分隔、static import 最后（MA4.2-14 惯例）
- Plan: `ai-dev/plans/2026-08-01-0936-3-arm-p3-error-handling-tools-toolkit.md`

## Plan `2026-08-01-0936-3-arm-p3-error-handling-tools-toolkit` Phase 2 — nop-ai-tools 7 处转换

- **转换**：7 处裸 IAE（4 文件）全部 → `throw new NopException(<NopAiCoreErrors code>).param(...)`（沿用 SequentialThinkingBizModel 先例，static import NopAiCoreErrors）；`NopAiCoreErrors` 新增 **7 码**（英文描述，消息逐字零漂移——静态消息进 desc、动态拼接进结构化 param 渲染等价）：
  - `ERR_AI_TOOLS_INVALID_PROJECT_NAME`（nop.err.ai.tools.invalid-project-name，desc `projectName must be valid file directory name:{value}` 无空格与拼接渲染等价，ARG_VALUE，FileToolBizModel:237，audit 点名 P3-MA3-5 站点）
  - `ERR_AI_TOOLS_THOUGHT_EMPTY`（nop.err.ai.tools.thought-empty，静态，ThoughtData:51）
  - `ERR_AI_TOOLS_INVALID_THOUGHT_NUMBER`（nop.err.ai.tools.invalid-thought-number，静态，ThoughtData:61）
  - `ERR_AI_TOOLS_INVALID_TOTAL_THOUGHTS`（nop.err.ai.tools.invalid-total-thoughts，静态，ThoughtData:72）
  - `ERR_AI_TOOLS_TOTAL_THOUGHTS_LESS_THAN_NUMBER`（nop.err.ai.tools.total-thoughts-less-than-number，静态，ThoughtData:75）
  - `ERR_AI_TOOLS_INVALID_STAGE`（nop.err.ai.tools.invalid-stage，desc `Invalid ThoughtStage: {value}` 无空格与拼接渲染等价，ARG_VALUE，ThoughtStage:29）
  - `ERR_AI_TOOLS_INVALID_MAX_RESULTS`（nop.err.ai.tools.invalid-max-results，静态，ThoughtAnalyzer:36）
- **验证**：`grep -rn "throw new IllegalArgumentException\|throw new RuntimeException" nop-ai/nop-ai-tools/src/main/java` = **0 命中**；`./mvnw test -pl nop-ai/nop-ai-tools -am -T 1C` BUILD SUCCESS（23 tests 0 failures：既有 16 + 新增 7）
- **测试**：断言影响面 0（TestThoughtStorage/TestSequentialThinkingBizModel 无 IAE 断言）；新增 **1 个 focused 值级测试文件** `TestToolErrorCodeConversion`（+7 @Test，零减少）：7 码全覆盖——invalid-project-name（子类暴露 protected getProjectDir，`a*b` 非法文件名触发，errorCode + ARG_VALUE param + verbatim message）、thought-empty、invalid-thought-number、invalid-total-thoughts、total-thoughts-less-than-number、invalid-stage（errorCode + ARG_VALUE + verbatim `Invalid ThoughtStage: BOGUS`）、invalid-max-results（findRelatedThoughts maxResults=0）
- **格式**：转换行 ≤150 列；io.nop import 组空行分隔、static import 最后（MA4.2-14 惯例）
- Plan: `ai-dev/plans/2026-08-01-0936-3-arm-p3-error-handling-tools-toolkit.md`

## Plan `2026-08-01-0936-3-arm-p3-error-handling-tools-toolkit` Phase 1 — 盘点与逐处裁定

- **grep 全量盘点（live 复核，与 plan baseline + audit 明细一致）**：
  - `nop-ai-tools/src/main/java`：**7 处**（4 文件）——FileToolBizModel.java:237（"projectName must be valid file directory name:"+projectName，audit 原文写 212，live 为 237 以 live 为准）、ThoughtData.java:51（"Thought cannot be empty"）/61（"Thought number must be positive"）/72（"Total thoughts must be positive"）/75（"Total thoughts must be >= thought number"）、ThoughtStage.java:29（"Invalid ThoughtStage: "+value）、ThoughtAnalyzer.java:36（"maxResults must be positive"）
  - `nop-ai-toolkit/src/main/java`：**9 处**（1 文件）——LocalToolFileSystem.java:54（"Path not allowed: "+path，MA6.2-003 安全判定面）、92/112/141（"File not found: "+path）、168（"Directory not found: "+dirPath）、225/244（"File not found: "+fromPath）、229/248（"Target file already exists: "+toPath）
- **逐处裁定分类**：16 处全部为校验/非法状态类（文件名校验、非空/非正数校验、非法枚举值、路径越界安全拒绝、文件/目录不存在、目标已存在），**全部转换**；无实现 JDK 接口契约必须抛 IAE 的站点，保留数 = 0
- **tools 载体裁定（选项 a — 扩展 `NopAiCoreErrors`）**：沿用 SequentialThinkingBizModel 先例（`ERR_AI_TOOLS_INVALID_THOUGHT` 已注册 `nop.err.ai.tools.*` 前缀）；**共享编辑面约束已解除**——兄弟计划 0936-2 已于本批次 closed，其 7 码已落盘 NopAiCoreErrors，本计划串行追加无并行写冲突。新增 **7 码**（英文描述，消息逐字零漂移——静态消息进 desc、动态拼接进结构化 param 渲染等价）：
  - `ERR_AI_TOOLS_INVALID_PROJECT_NAME`（nop.err.ai.tools.invalid-project-name，desc `projectName must be valid file directory name:{value}` 无空格与拼接渲染等价，ARG_VALUE，FileToolBizModel:237）
  - `ERR_AI_TOOLS_THOUGHT_EMPTY`（nop.err.ai.tools.thought-empty，静态，ThoughtData:51）
  - `ERR_AI_TOOLS_INVALID_THOUGHT_NUMBER`（nop.err.ai.tools.invalid-thought-number，静态，ThoughtData:61）
  - `ERR_AI_TOOLS_INVALID_TOTAL_THOUGHTS`（nop.err.ai.tools.invalid-total-thoughts，静态，ThoughtData:72）
  - `ERR_AI_TOOLS_TOTAL_THOUGHTS_LESS_THAN_NUMBER`（nop.err.ai.tools.total-thoughts-less-than-number，静态，ThoughtData:75）
  - `ERR_AI_TOOLS_INVALID_STAGE`（nop.err.ai.tools.invalid-stage，desc `Invalid ThoughtStage: {value}` 无空格与拼接渲染等价，ARG_VALUE，ThoughtStage:29）
  - `ERR_AI_TOOLS_INVALID_MAX_RESULTS`（nop.err.ai.tools.invalid-max-results，静态，ThoughtAnalyzer:36）
- **toolkit 载体裁定（选项 a — 复用 `NopAiException` 字符串构造器）**：理由：① **消息语义零变更硬约束**——复用 CommonErrors `ERR_FILE_*` 会渲染中文描述（"文件不存在: {path}"），改变 AI 可见消息语言，违反 plan 零漂移要求；② IToolFileSystem 虽被 nop-ai-shell/nop-ai-agent 跨模块消费（FileShellInput/FileShellOutput/ShellCommandExecutor/DefaultShellExecutionContext/SimpleToolExecuteContext/AgentToolExecuteContext），但消费面只有 `getMessage()`（executor catch Exception → `AiToolCallResult.errorResult(msg)`），**无程序化错误码消费** → 按 error-handling.md 判断依据（是否被其他模块程序化消费）属模式二"模块内部实现 → 模块异常类 + 英文字符串"；③ 模块内先例 DefaultToolExecutorProvider:51；④ toolkit pom 直接依赖 nop-ai-api；⑤ `isPathAllowed` 判定逻辑（:40-50）与调用链零变更，仅 :54 异常类型变化，MA6.2-003 安全语义不受影响
- **catch IAE 调用面**：tools/toolkit main+test `catch (IllegalArgumentException` = **0 命中**（nop-ai-agent 5 处为 0936-1 核验范围，与本模块零交集）→ 影响面 0；NopAiException/NopException 均为 RuntimeException 子类，`catch (Exception)` 站点（executor 层）不受影响
- **测试断言影响面**：tools 0（TestThoughtStorage/TestSequentialThinkingBizModel 用合法值构造 + 既有 NopException 断言不受影响）；toolkit 0（executor 测试 catch Exception 转 errorResult，无 IAE 断言）
- **owner-doc**：No owner-doc update required（沿 error-handling.md 既有约定，不改变规范本身；核心新增转换码英文描述与 0936-2 先例一致）
- Plan: `ai-dev/plans/2026-08-01-0936-3-arm-p3-error-handling-tools-toolkit.md`

## Implementation: LLM 错误规范化配置层（llm.xdef errorMappings + provider 映射 + ErrorClassification 上移 core）— 全量验证 + 提交

- **实现**（承接 `nop-ai-llm-error-normalization-design.md` 草案，落地其配置层）：`llm.xdef` 新增 `<errorResponse>`（LlmErrorResponseModel：errorTypePath/errorCodePath/errorMessagePath/retryAfterPath prop-path 抽取）+ `<errorMappings xdef:key-attr="id">`（LlmErrorMappingModel：classification/httpStatus/errorTypes/errorCodes/messagePattern/retryAfterPath，first-match-wins 有序规则表，x:extends 可按 id 覆盖）；`ErrorClassification` 从 nop-ai-agent 上移到 `nop-ai-core.model`（纯词汇枚举，新增 AUTH_INVALID/CACHE_STATE_LOST，agent 侧保留 @Deprecated 兼容桥接）；5 个 provider 配置（default/azure/claude/gemini/ollama `llm.xml`）落地 errorResponse + errorMappings 映射（OpenAI 429 双语义 quota vs rate_limit、Anthropic 402 billing_error、Gemini 消息正则、Azure 嵌套 inner_error）
- **测试**：新增 `TestLlmErrorMapping`（模型字段解析 + 映射匹配值级断言）
- **验证**：`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（0 failures）；lint 命令按 mission 配置 `|| echo 'lint not configured'` 回退（checkstyle 未绑定构建生命周期）
- **Commit**: 随 BUILD_VERIFY batch 提交
- Related: `ai-dev/design/nop-ai-agent/nop-ai-llm-error-normalization-design.md`（草案 §3.8 上移理由）

## Plan `2026-08-01-0936-2-arm-p3-error-handling-core-coder` Phase 4 — 全量验证 + 登记

- **全量验证**：`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（03:10 min，0 failures）；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0；`check-doc-links.mjs --strict` exit 0（0 errors）
- **登记**：arm-index「P3 追踪（第十二批 — 错误处理规范）」小节新增 2 行（P3-MA3-2 → fixed 7 码 core 转换 + 3 focused 测试文件、P3-MA3-3 → fixed 3 码 coder 转换 + TestCoderErrorCodeConversion）+ 小节导语补 0936-2 收口 + 验证段补 0936-2 侧证据；roadmap 新增「第十二批（错误处理规范 2）」行 ✅（closed 2026-08-01）+ header v12→v13 + 尾段叙事补 0936-2 落地（core/coder 10 新码，P3-MA3-2/P3-MA3-3 fixed）
- **Commit**: 待 closure audit 后随计划收口提交
- Plan: `ai-dev/plans/2026-08-01-0936-2-arm-p3-error-handling-core-coder.md`

## Plan `2026-08-01-0936-2-arm-p3-error-handling-core-coder` Phase 3 — nop-ai-coder 4 处转换

- **转换**：4 处裸 IAE（3 文件）全部 → `throw new NopException(<AiCoderErrors code>).param(...)`（coder 既有 NopException(AiCoderErrors) 模式，与 AiCoderHelper/AiOrmSqlType 先例一致）；`AiCoderErrors` 新增 **3 码**（英文描述，消息逐字零漂移——静态消息进 desc、动态拼接进结构化 param 渲染等价）：
  - `ERR_AI_CODER_UNSUPPORTED_CONVERSION`（nop.err.ai.coder.unsupported-conversion，desc `Unsupported conversion:{from}->{to}` 无空格与拼接渲染等价，ARG_FROM/ARG_TO——plan 点名的码名 + 双参设计；AiXdefDocumentConverter:21 + AiOrmDocumentConverter:28 共用）
  - `ERR_AI_CODER_METHOD_SIGNATURE_NOT_FOUND`（nop.err.ai.coder.method-signature-not-found，"Method signature not found: {signature}"，ARG_SIGNATURE，JavaMethodReplacer:24——audit 复核段 new finding 显式纳入）
  - `ERR_AI_CODER_UNBALANCED_BRACES`（nop.err.ai.coder.unbalanced-braces，静态，JavaMethodReplacer:33）
- **验证**：`grep -rn "throw new IllegalArgumentException\|throw new RuntimeException" nop-ai/nop-ai-coder/src/main/java` = **0 命中**；`./mvnw test -pl nop-ai/nop-ai-coder -am -T 1C` BUILD SUCCESS（0 failures，上游依赖模块含 nop-ai-core 全绿）
- **测试**：coder 测试断言影响面 0（无既有 IAE 断言）；新增 **1 个 focused 值级测试文件** `TestCoderErrorCodeConversion`（+4 @Test，零减少）：missing-signature（errorCode + ARG_SIGNATURE param + verbatim message）、unbalanced-braces（errorCode + verbatim）、AiOrm/AiXdef unsupported conversion 双转换器（errorCode + ARG_FROM/ARG_TO param + verbatim `Unsupported conversion:txt->orm.xml`），converter 测试用 `ResourceDocumentObject("txt", ByteArrayResource)` 构造非注册类型输入，`@TempDir` 文件驱动 JavaMethodReplacer
- **格式**：转换行 ≤150 列；io.nop import 组空行分隔、static import 最后（MA4.2-14 惯例）
- Plan: `ai-dev/plans/2026-08-01-0936-2-arm-p3-error-handling-core-coder.md`

## Plan `2026-08-01-0936-2-arm-p3-error-handling-core-coder` Phase 2 — nop-ai-core 7 处转换

- **转换**：7 处裸 IAE（6 文件）全部 → `throw new NopException(<NopAiCoreErrors code>).param(...)`（core 既有 NopException(ErrorCode) 模式，static import NopAiCoreErrors，与 PromptModel:96 等先例一致）；`NopAiCoreErrors` 新增 **7 码**（英文描述，消息逐字零漂移——静态消息进 desc、动态拼接进结构化 param 渲染等价）：
  - `ERR_AI_FILE_INVALID_EDIT_TYPE`（nop.err.ai.file.invalid-edit-type，"Invalid edit type"，FileDiffGenerator:213 switch default 防御分支）
  - `ERR_AI_PROMPT_TEMPLATE_NULL`（nop.err.ai.prompt-template-null，"prompt template is null"，PromptModel:88）
  - `ERR_AI_UNSUPPORTED_PARSE_FROM_RESPONSE`（nop.err.ai.prompt-unsupported-parse-from-response，"unsupported parseFromResponse: {parseModel}"，ARG_PARSE_MODEL，ModelBasedPromptTemplate:244）
  - `ERR_AI_UNKNOWN_ROLE`（nop.err.ai.unknown-role，"unknown role:{role}"，ARG_ROLE，desc 无空格与拼接渲染等价，AiMessage:51）
  - `ERR_AI_VECTOR_LENGTH_MISMATCH`（nop.err.ai.vector-length-mismatch，"Length of vector a  must be equal..." 双空格逐字，CosineSimilarity:48）
  - `ERR_AI_SESSION_ID_IS_EMPTY`（nop.err.ai.session-id-empty，静态）+ `ERR_AI_SESSION_ID_INVALID`（nop.err.ai.session-id-invalid，"…sessionId={sessionId}"，ARG_SESSION_ID，ChatLogHelper:65/69）
- **验证**：`grep -rn "throw new IllegalArgumentException\|throw new RuntimeException" nop-ai/nop-ai-core/src/main/java` = **0 命中**（仅 NopAiCoreErrors javadoc 一处文档引用残留）；`./mvnw compile -pl nop-ai/nop-ai-core -am` BUILD SUCCESS；`./mvnw test -pl nop-ai/nop-ai-core -am -T 1C` BUILD SUCCESS（127 tests 0 failures，上游依赖模块全绿）
- **测试**：2 个既有文件断言 `IllegalArgumentException.class` → `NopException.class` + 值级断言（TestCosineSimilarityAndRelevanceScore.testDimensionMismatchRejected：errorCode==ERR_AI_VECTOR_LENGTH_MISMATCH + getMessage contains "Length"；TestChatLogHelper 3 处断言 errorCode==ERR_AI_SESSION_ID_INVALID + 新增 testRequireValidSessionIdEmptyRejected 覆盖 ERR_AI_SESSION_ID_IS_EMPTY）；新增 **3 个 focused 值级测试文件**（+5 @Test，零减少）：TestPromptErrorCodeConversion（PROMPT_TEMPLATE_NULL + UNSUPPORTED_PARSE_FROM_RESPONSE，值级 errorCode/param/message）、TestAiMessageErrorCode（UNKNOWN_ROLE + ARG_ROLE param + verbatim message）、TestFileDiffGeneratorErrorCode（defensive 分支 reflection 调 private determineDiffType(KEEP) 证明运行时可达，InvocationTargetException→NopException 断言）
- **格式**：转换行 ≤150 列；io.nop import 组空行分隔、static import 最后（MA4.2-14 惯例）
- Plan: `ai-dev/plans/2026-08-01-0936-2-arm-p3-error-handling-core-coder.md`

## Plan `2026-08-01-0936-2-arm-p3-error-handling-core-coder` Phase 1 — 盘点与逐处裁定

- **grep 全量盘点（live 复核，与 audit 明细 + plan baseline 一致）**：
  - `nop-ai-core/src/main/java`：**7 处**（6 文件）——FileDiffGenerator.java:213（"Invalid edit type"）、PromptModel.java:88（"prompt template is null"）、ModelBasedPromptTemplate.java:244（"unsupported parseFromResponse: "+parseModel）、AiMessage.java:51（"unknown role:"+role）、CosineSimilarity.java:48（"Length of vector a  must be equal to the length of vector b"）、ChatLogHelper.java:65/69（sessionId path-traversal guard ×2）
  - `nop-ai-coder/src/main/java`：**4 处**（3 文件）——JavaMethodReplacer.java:24（"Method signature not found: "+methodSignature）/33（"Unbalanced braces in method body"）、AiXdefDocumentConverter.java:21 + AiOrmDocumentConverter.java:28（"Unsupported conversion:"+from+"->"+to）
- **逐处裁定分类**：11 处全部为校验/非法状态类（switch default 防御分支、null 校验、非法值校验、unsupported 配置），**全部转换**；无实现 JDK 函数式接口必须抛 IAE 的契约站点，保留数 = 0。FileDiffGenerator:213 为 private enum 防御分支（KEEP 上游已过滤，public API 今日不可达），转换保留（防御演进）+ focused 测试用 reflection 证明错误码运行时可达
- **转换目标 ErrorCode 规划**（英文描述，消息逐字零漂移——静态消息进 desc，动态拼接进结构化 param，渲染等价）：
  - core（`NopAiCoreErrors` 新增 7 码，沿用 live 无 `nop.err.ai.core.*` 前缀现状，按语义归类）：`ERR_AI_FILE_INVALID_EDIT_TYPE`（nop.err.ai.file.invalid-edit-type，静态）、`ERR_AI_PROMPT_TEMPLATE_NULL`（nop.err.ai.prompt-template-null，静态）、`ERR_AI_UNSUPPORTED_PARSE_FROM_RESPONSE`（nop.err.ai.prompt-unsupported-parse-from-response，ARG_PARSE_MODEL）、`ERR_AI_UNKNOWN_ROLE`（nop.err.ai.unknown-role，ARG_ROLE，desc 无空格 `unknown role:{role}` 与拼接渲染等价）、`ERR_AI_VECTOR_LENGTH_MISMATCH`（nop.err.ai.vector-length-mismatch，静态含双空格逐字）、`ERR_AI_SESSION_ID_IS_EMPTY`（nop.err.ai.session-id-empty，静态）+ `ERR_AI_SESSION_ID_INVALID`（nop.err.ai.session-id-invalid，ARG_SESSION_ID）
  - coder（`AiCoderErrors` 新增 3 码，`nop.err.ai.coder.*` 前缀沿既有 2 码）：`ERR_AI_CODER_UNSUPPORTED_CONVERSION`（nop.err.ai.coder.unsupported-conversion，ARG_FROM/ARG_TO，desc `Unsupported conversion:{from}->{to}` 无空格与拼接渲染等价，覆盖 AiXdef/AiOrm 两转换器）、`ERR_AI_CODER_METHOD_SIGNATURE_NOT_FOUND`（ARG_SIGNATURE）、`ERR_AI_CODER_UNBALANCED_BRACES`（静态）
- **异常类型决策**：core 沿既有 `NopException(ErrorCode)` 模式（PromptModel:96 等先例，static import NopAiCoreErrors）；coder 沿既有 `NopException(AiCoderErrors)` 模式（AiCoderHelper/AiOrmSqlType 先例）——不用 NopAiException（coder 虽直接依赖 nop-ai-api，但模块内 ErrorCode 基建已确立，规范一致性优先）
- **catch IAE 调用面**：两模块 main+test `catch (IllegalArgumentException` = **0 命中** → 影响面 0；异常类型 IAE→NopException 均为 RuntimeException 子类，`catch (Exception)` 类站点不受影响
- **测试断言影响面**：core 2 文件（TestCosineSimilarityAndRelevanceScore:48、TestChatLogHelper:212/218/225，均 `assertThrows(IllegalArgumentException.class)`）；coder 0 文件。新增 focused 值级测试规划：core 3 新文件（io.nop.ai.core.model.TestPromptErrorCodeConversion ×2 码、io.nop.ai.core.file.TestFileDiffGeneratorErrorCode reflection ×1 码、io.nop.ai.core.api.messages.TestAiMessageErrorCode ×1 码）+ 2 既有文件补值级断言（VECTOR_LENGTH_MISMATCH/SESSION_ID_*）；coder 1 新文件（TestCoderErrorCodeConversion 覆盖 3 码 4 站点）
- **owner-doc**：No owner-doc update required（沿 error-handling.md 既有约定；core 新增转换码英文描述与 2248-2 先例 + error-handling.md nop-ai 英文例外条款一致）
- Plan: `ai-dev/plans/2026-08-01-0936-2-arm-p3-error-handling-core-coder.md`

## Roadmap + Design: LLM 错误规范化架构三定（ChatServiceImpl 经 dialect 规范化 → ChatResponse）

- **讨论记录**：`ai-dev/discussions/2026-08/2026-08-01-llm-error-normalization-layer-adjudication.md` 记录本轮架构归属裁定的全过程（4 轮：初始异常方案 → 装饰器纠正 → roadmap 归属质疑 → 回 ChatServiceImpl 经 dialect→ChatResponse 最终定调），含用户原始表述与三轮纠正。
- **架构再裁定**（用户第三轮定调，推翻前两版）：错误规范化**在 `ChatServiceImpl` 内经 `ILlmDialect` 完成**，结果放 `ChatResponse` 新字段。理由：ChatServiceImpl 的职责就是经 dialect 规范化 I/O（成功响应已用 `parseResponse` + `contentPath`/`statusPath` 这么做），错误响应是另一种输出，**同构处理**——新增 dialect `parseErrorResponse`（消费 `<errorMappings>`），把 `errorClassification`/`retryAfterMs` 填到 `ChatResponse`。**不用装饰器（第二版方案）、不用新异常类型（第一版方案）**。
- **关键发现支撑裁定**：`LlmCallCoordinator` 今天有两条分裂的错误路径——抛异常走重试（`:122-174`）、`!isSuccess()` 走终止不重试（`:179-187`）。错误进 ChatResponse 后两条统一：`!isSuccess()` 也读 `errorClassification` 进重试决策。响应级错误（拿到 HTTP 响应）→ ChatResponse；传输级错误（无响应）→ 仍抛异常走启发式。`ChatResponse` 已有 `error`/`errorCode`/`isSuccess()`/`ChatResponse.error()` 模式，`OpenAiDialect.parseResponse` 已对空 body 返回 `ChatResponse.error(...)`——错误即输出走同一通道是代码库既有模式。
- **roadmap 归属**（已在上一轮纠正）：backlog `nop-ai-agent-harness-evolution-roadmap.md` 新增 **W2e**（W2 前置必须项），6 个工作项 W2e-0~5。本轮按新架构更新 W2e：W2e-1 改为 ChatResponse 加字段、W2e-2 改为 dialect 加 `parseErrorResponse` + ChatServiceImpl 两路径调它（不抛）、W2e-3 改为重试循环 `!isSuccess()` 升级为重试决策。main 设计 roadmap 已还原全 done。
- **设计文档**（`nop-ai-llm-error-normalization-design.md`）§3.4/§3.5/§3.8/§一(2) 改写为"经 dialect 在 ChatServiceImpl 内规范化→ChatResponse"；新增 §4.0 拒绝装饰器/新异常方案及理由；§3.7 Retry-After 归一改为进 ChatResponse。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0；design doc 仅 §4.0（拒绝方案描述）含 `LlmProviderException`/`ErrorNormalizingChatService`，正文无残留。
- Doc-sync: design（§3.4/3.5/3.8/4.0）+ backlog W2e 已更新

## Analysis: LLM 错误映射配置可行性验证（主流 API + 参考实现）

- **新增分析报告**：`ai-dev/analysis/2026-08/2026-08-01-llm-error-mapping-feasibility-analysis.md`。验证 `nop-ai-llm-error-normalization-design.md` 的 `<errorResponse>`/`<errorMappings>` 配置方案对主流 API 的可行性。
- **参考实现调研**（4 个框架）：nanobot（错误分类金标准——429 双 token 列表 `insufficient_quota` vs `rate_limit_exceeded`、`is_arrearage_response` 切账号触发、Retry-After 三层解析、hasStreamedContent 全部证实于 `providers/base.py`）；litellm（覆盖最广但错误映射是代码驱动——18 个 per-provider 函数 + 子串启发式，~70% 表格形/~30% 过程形）；agentscope-java（重试策略 config-driven 范例 `ExecutionConfig`，但不读 Retry-After/不区分配额）；solon-ai（最薄弱，错误压成字符串，反面教材）。
- **联网核实主流 API 真实错误格式**：OpenAI 同一 429 两种语义（`error.code`: `rate_limit_exceeded` vs `insufficient_quota`，多源印证）；Anthropic 最干净且有专用 402 `billing_error`（官方 docs）；Gemini 429 `RESOURCE_EXHAUSTED` 限流/配额同型需消息正则区分 + Vertex 5xx 包 429 特例；DeepSeek OpenAI 兼容；Azure 嵌套 `inner_error.code` 双拼写。
- **结论**：配置方案对全部主流 provider 可行，~90% 纯配置可达 + ~10% 需 `messagePattern` 正则/XPL 逃生舱。设计无需结构性修改。
- **两处增强已并入设计文档** §3.3：① `messagePattern` 从"可选"升为"必需"（复刻 dialect 消息正则规则）；② 新增可选 `<classifyError>xpl-fn</classifyError>` 全局逃生舱（覆盖 Azure 嵌套多拼写/负向排除，与 llm.xdef 现有 xpl-fn 节点风格一致）。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 errors）。
- Doc-sync: 设计文档 §3.3 + 前置文档链接已更新

## Design: LLM 错误规范化与配额感知恢复（草案 + 双轮独立审查）

- **新增设计文档**：`ai-dev/design/nop-ai-agent/nop-ai-llm-error-normalization-design.md`。解决"模型订阅额度跑到一半耗尽，无法判断该等待还是切换备用账号"的痛点。核心决策：① 配置驱动的错误映射复刻 `dialect.xdef` `<errorCodes>` 模式（新增 `<errorResponse>` + `<errorMappings>` 到 `llm.xdef`），把 provider 异构错误响应规范化为 6 类固定分类；② 规范化落在底层 `ChatServiceImpl`，覆盖流式（默认）+ 非流式路径；③ 固定分类驱动两种恢复（RATE_LIMITED→按 Retry-After 等待 / QUOTA_EXCEEDED+AUTH_INVALID→切换备用账号链）；④ 账号回退链与 `IModelRouter` 模型 tier 回退通道区分（避免 QUOTA 超限误触发模型降级）。
- **双轮独立子 agent 审查达成共识**：R1 发现 3 个 critical/high 问题（根因锚定到错误代码路径——`callAsync` 默认 stream=true 而 line:123 是 stream=false 路径；`<errorMappings>` 误带 `xdef:key-attr` 会破坏 first-match-wins；FALLBACK 通道被模型 tier 占用导致 QUOTA 超限误降级），全部修正。R2 核实 10/10 R1 问题 FIXED，给出 YES-proceed-to-plan，并发现 3 个实现精度项（normalizer 应落在 `callStream.onError` 覆盖直连+聚合两条流式路径 DRY；HTTP-200-with-error-body 已知缺口；`StandardRetryPolicy` 须把 QUOTA/AUTH 从 STOP 改 FALLBACK），已并入 §3.4/§6。
- **关键代码事实核实**（驱动设计修正）：`ServerEventPublisher.java:94-97` 流式非 2xx 已把 `ARG_BODY`+`ARG_HTTP_STATUS` 放进异常（体没丢，是无人规范化它）；`ServerEventPublisher.java:87` 头读进局部变量但未挂异常（Retry-After 两路径都丢）；`ChatServiceImpl.java:91` `stream` 默认 true；`LlmErrorClassifier.java:101-103` 4xx→NON_TRANSIENT；`ErrorClassification.QUOTA_EXCEEDED` 今日是死代码（从未被生产）。
- **登记**：`ai-dev/design/nop-ai-agent/README.md` 阅读顺序新增 `nop-ai-llm-error-normalization-design.md` 条目。
- **验证**：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（0 errors）。
- Doc-sync: No owner-doc update required（设计草案，未改产品行为；落地后同步 `nop-ai-agent-llm-layer.md` §6.5/§7/§7.6 的 deferred→✅）

## Plan `2026-08-01-0936-1-arm-p3-error-handling-agent-shell` Phase 4 — 全量验证 + 登记

- **全量验证**：`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（0 failures）；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0（0 findings）；`check-doc-links.mjs --strict` exit 0（顺带修复 `ai-dev/design/nop-ai-agent/nop-ai-agent-mission-driver-port-design.md` 3 处预存 broken link：:163/:667 CallAgentExecutor 路径补模块前缀、:662 去 `nop-entropy-master/` 前缀）
- **登记**：arm-index 新增「P3 追踪（第十二批 — 错误处理规范）」小节 2 行（P2-MA3-1、P3-MA3-1 → fixed + 转换计数 12/62 + 测试列）；roadmap 新增第十二批行 ✅（closed 2026-08-01）+ header 更新 + 尾段叙事补 0936-1/2/3 并行关系
- **Commit**: 待 closure audit 后随计划收口提交

## Plan `2026-08-01-0936-1-arm-p3-error-handling-agent-shell` Phase 3 — nop-ai-shell 12 处转换

- **转换**：12 处裸 IAE（5 文件）全部 → `throw new NopException(NopAiShellErrors.<code>).param(...)`（shell 无模块异常类，沿 NopAiShellErrors 既有 3 码 NopException 模式）；`NopAiShellErrors` 新增 5 码（英文描述，`nop.err.ai.shell.*` 前缀）：`ERR_AI_SHELL_INVALID_ARG`（invalid argument: {msg}，ShellCommandRegistry:35/40/54/58）、`ERR_AI_SHELL_COMMAND_NOT_FOUND`（Command not found: {commandName}，:62，commandName param 保持逐字）、`ERR_AI_SHELL_EMPTY_COMMAND`（empty command: {msg}，GroupExpr:16 + PipelineExpr:16）、`ERR_AI_SHELL_INVALID_REDIRECT`（invalid redirect: {msg}，Redirect:172/177/197）、`ERR_AI_SHELL_UNKNOWN_SYMBOL`（unknown symbol: {msg}，Redirect:48 + LogicalExpr:36）——消息逐字零漂移
- **验证**：`grep -rn "throw new IllegalArgumentException\|throw new RuntimeException" nop-ai/nop-ai-shell/src/main/java` = **0 命中**；`./mvnw test -pl nop-ai/nop-ai-shell -am -T 1C` BUILD SUCCESS（277 tests 0 failures）
- **测试**：3 个测试文件 12 处断言 `IllegalArgumentException.class` → `NopException.class` + import 补齐（顺序修正）；新增 2 个 focused 值级测试：`ShellCommandRegistryTest.validationFailuresCarryErrorCodeAndVerbatimMessage`（INVALID_ARG + COMMAND_NOT_FOUND 双码，errorCode/param/getMessage 断言）、`CommandModelTest.expressionValidationFailuresCarryErrorCodeAndVerbatimMessage`（EMPTY_COMMAND×2 + UNKNOWN_SYMBOL + INVALID_REDIRECT 四码）；@Test 计数 19/49/12 vs 18/48/12（+2，不减少）；测试文件 IAE 引用 0
- **格式**：所有转换行 ≤150 列；io.nop import 组空行分隔

## Plan `2026-08-01-0936-1-arm-p3-error-handling-agent-shell` Phase 2 — nop-ai-agent 62 处转换

- **转换**：62 处裸 IAE（33 文件）全部 → `throw new NopAiAgentException(NopAiAgentErrors.ERR_AI_AGENT_INVALID_ARG).param(ARG_MSG, "<verbatim>")`（1834-3 NopAiMavenErrors `{msg}` param 先例，消息逐字零漂移，单行 >150 字符自动换行，io.nop import 组补空行分隔）；`NopAiAgentErrors` 新增 `ERR_AI_AGENT_INVALID_ARG`（`nop.err.ai.agent.invalid-arg`，`invalid argument: {msg}`，ARG_MSG）——`nop.err.ai.agent.*` 前缀与既有 21 码一致
- **验证**：`grep -rn "throw new IllegalArgumentException\|throw new RuntimeException" nop-ai/nop-ai-agent/src/main/java` = **0 命中**；`./mvnw compile -pl nop-ai/nop-ai-agent -am` 绿；`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS（nop-orm 首次并行跑 flaky NoClassDefFoundError 系陈旧构建产物，单跑全绿——与本次改动无关，nop-orm 不依赖 nop-ai）
- **测试**：20 个测试文件 51 处 `assertThrows(IllegalArgumentException.class` → `NopAiAgentException.class` + import 补齐；3 处陈旧注释同步（TestSteeringInjection:151、TestParentConstrainedPathAccessChecker:32、TestRuleBasedPathAccessChecker:273）；新增 focused 值级测试 `TestThresholdBreaker.validationFailureCarriesErrorCodeAndVerbatimMessage`（断言 errorCode==ERR_AI_AGENT_INVALID_ARG、params.msg==逐字消息、getMessage().contains(逐字)）；@Test 计数 295 vs HEAD 294（+1，不减少）
- **import-order**：`check-import-order.mjs --module nop-ai` 114 处既有违规（nop-ai-core 等 108 文件，非本次改动），与本次 53 个改动文件**零交集**（comm 验证）——0 新违规；checkstyle 150 列检查：所有转换行 ≤150

## Plan `2026-08-01-0936-1-arm-p3-error-handling-agent-shell` Phase 1 — 盘点与逐处裁定

- **grep 全量盘点（live 复核，与 audit 明细一致）**：
  - `nop-ai-agent/src/main/java`：**62 处** `throw new IllegalArgumentException`（**33 文件**）
  - `nop-ai-shell/src/main/java`：**12 处**（**5 文件**：Redirect 4、ShellCommandRegistry 5、GroupExpr 1、PipelineExpr 1、LogicalExpr 1）
- **逐处裁定分类**：62+12 处全部为参数/状态校验类（null/empty/非正数/非法符号），**全部转换**；经核查无实现 JDK 函数式接口必须抛 IAE 的站点（无 `java.util.function.*` 契约实现站点），保留数为 0
- **转换目标 ErrorCode 规划**（遵循 1834-3 先例 NopAiMavenErrors `{msg}` param 模式 + plan 命名）：
  - agent 新增 1 码：`ERR_AI_AGENT_INVALID_ARG`（`nop.err.ai.agent.invalid-arg`，desc `invalid argument: {msg}`，`ARG_MSG`）——62 处全部走此码，逐字消息进 `.param(ARG_MSG, ...)`，零消息漂移
  - shell 新增 5 码：`ERR_AI_SHELL_INVALID_ARG`（`invalid argument: {msg}`，ShellCommandRegistry:35/40/54/58）、`ERR_AI_SHELL_COMMAND_NOT_FOUND`（`Command not found: {commandName}`，:62）、`ERR_AI_SHELL_EMPTY_COMMAND`（`empty command: {msg}`，GroupExpr:16 + PipelineExpr:16）、`ERR_AI_SHELL_INVALID_REDIRECT`（`invalid redirect: {msg}`，Redirect:172/177/197）、`ERR_AI_SHELL_UNKNOWN_SYMBOL`（`unknown symbol: {msg}`，Redirect:48 + LogicalExpr:36）
- **catch IAE 调用面（5 处逐一核验，全部 JDK 契约来源，与转换站点零交集）**：
  - `LLMCurator:286`：catch `SkillQualityRating.valueOf` 产物；`FileSystemSkillProvider:275/284`：catch `SkillTopPattern/SkillResourceScope.valueOf` 产物；`AgentMessageEnvelopeJson:108`：catch `AgentMessageKind.valueOf` 产物；`CheckpointJournalReader:177`：catch `CheckpointType.valueOf` 产物——均为 JDK `Enum.valueOf` 非法参数产物，不涉及任何转换站点 → **影响面 0**
- **转换异常类型决策**：agent 侧用 `NopAiAgentException(ErrorCode)`（模块既有 200+ 使用面的模块异常类）；shell 侧用 `NopException(ErrorCode)`（shell 无模块异常类，NopAiShellErrors 既有 3 码均走 NopException）
- **测试断言影响面**：agent 20 测试文件、shell 3 测试文件（RedirectTest/CommandModelTest/ShellCommandRegistryTest）；断言均为 `assertThrows(IllegalArgumentException.class, ...)` 类型级断言（第三参为 JUnit 失败消息非断言），转换后改为模块异常类型 + 值级断言
- **owner-doc**：No owner-doc update required（沿既有 error-handling.md 约定，不改变规范本身；`NopAiAgentErrors`/`NopAiShellErrors` 英文描述例外条款已在 error-handling.md 生效）

## flux.yaml 页面回退机制 — 文档 + 实现（Plan 31）

- **特性**：后台支持 `flux.yaml` 页面文件。`nop.web.render-mode=flux` 时，加载任意 `*.page.yaml` 前优先检测同目录 `*.flux.yaml`，命中则改加载它；AMIS 模式下忽略 flux.yaml，行为不变。两入口均覆盖：顶层 `getPage`（`PageModelLoader`）+ 子页面（`flux-web.xlib:LoadPage` 两个分支）。
- **关键改动**：`page.register-model.xml` 注册 `flux.yaml` loader；`WebConstants` 增 `FILE_TYPE_FLUX_YAML` 并加入 `PAGE_FILE_TYPES`；`WebPageHelper` 增 `toFluxPagePath`（复合扩展名 page.yaml→flux.yaml）+ `isFluxMode` 改 public；`PageModelLoaderFactory.loadObjectFromPath` 增顶层回退；`flux-web.xlib:LoadPage` 两分支增 flux 回退。
- **测试**：`TestFluxPageFallback`（5：helper/顶层 flux/amis/直接加载/白名单）、`TestFluxSubpageFallback`（2：tabs 两分支 flux/amis 端到端）。
- **验证**：`./mvnw test -pl nop-frontend-support/nop-web` BUILD SUCCESS（70 tests，1 pre-existing skip）；`./mvnw test -pl nop-auth/nop-auth-web -am -Dtest=TestFluxPage` 通过（12/1 skip，下游无回归）；`scan-hollow-implementations.mjs --module nop-frontend-support/nop-web --severity high` exit 0；`check-doc-links.mjs --strict` exit 0。
- **关键坑（来自 plan 对抗审查）**：切换 render-mode 时必须同时 `clearCache("xlib")`（重编译 web.xlib post-extends）**与** `clearCache("xpage")`（缓存键 locale|path 不含模式），否则 flux 缓存污染 amis 断言。
- **Doc-sync**：`docs-for-ai/02-core-guides/flux-rendering.md` 新增「flux.yaml 页面文件」章节（owner doc，先行落地）；`INDEX.md`、`frontend-rendering-pipeline.md`、`view-and-page-customization.md` 路由补充；`04-reference/source-anchors.md` 增 `EXT-011`。
- Plan: `ai-dev/plans/31-flux-yaml-page-fallback.md`

## Plan `2026-08-01-0746-2-arm-p3-code-hygiene` Closure — 独立 closure audit APPROVE + 计划收口

- **独立 closure audit**（fresh session `ses_045293c86ffeEgfGRSC44XcU8n`）verdict **APPROVE**：13/13 检查项 PASS（grep 命中=7 文件保留集合且 50 行与对照表一致、5 文件 LOG 转换逐文件 live 验证、IFileOperator:330/334/338 getter 无 public 且 toString 保留、DefaultAiChatService:562 @SuppressWarnings 在位且 :548 未动、arm-index:476-484 登记 + roadmap:263 v12、daily log 三 Phase 条目倒序齐全、scan-hollow exit 0、check-doc-links exit 0、deferred 项 JLS 裁定成立）；Anti-Hollow：@Test 计数 12/1/1/1/1 零减少、断言零改动
- **计划收口**：3 Phase Status 全部 completed（Phase 1-3 先期勾选）+ Closure Gates 全勾选 + Plan Status → completed；Closure 段写入独立 audit 证据（session ID + 逐条 PASS + 证据来源）；`check-plan-checklist.mjs --strict` exit 0
- **计划终态**：MA4.2-12（13 文件 System.out 逐文件裁定：5 转 SLF4J / 1 删除死代码 / 7 手动+示例保留）/MA4.2-13（GrepResult 3 getter 冗余 public 移除 + SplitChunk/SplitOptions 裁定不处理）/MA4.1-04（parseToolCalls unchecked cast 补注解）全部 fixed；3 个 commit（92dcb583b/f8a3c8bb6/3fa297bba + 本 closure 收口）；arm-index `fixed`；roadmap 第十一批 ✅
- **Commit**: 随 Closure 提交（见 git log）

## Plan `2026-08-01-0746-2-arm-p3-code-hygiene` Phase 3 — 全量验证 + 登记

- **全量验证**：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（0 failures 0 errors）；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0（0 findings）；`check-doc-links.mjs --strict` exit 0（1842 文件 0 issues）
- **登记**：arm-index 新增「P3 追踪（第十一批 — 代码卫生）」小节 3 行（MA4.2-12/-13、MA4.1-04 → fixed + 修复位置/测试列）；roadmap v12 更新（第十一批第 2 行 ✅ closed 2026-08-01 + 尾段叙事补 0746-2）；计划无 `> Source Audits:` front matter（仅 `> Source:`），source audit closing 步骤按 mission 契约省略
- **Commit**: 见 git log（随 Phase 3）

## Plan `2026-08-01-0746-2-arm-p3-code-hygiene` Phase 2 — GrepResult 冗余 public + unchecked cast 清理（MA4.2-13 + MA4.1-04）

- **GrepResult 3 个 getter 移除 public**（IFileOperator.java:330/334/338）：全库 grep 核验跨包零调用者（唯一消费者 FileToolBizModel:225 仅 `GrepResult::toString`；同包 LocalFileOperator:422 仅构造），`toString()` 保留 public；`@DataBean` 反射序列化风险沿用 plan 既有裁定（@BizQuery 经 convertToGrepStrings 返回 String，无 JSON 暴露路径）
- **SplitChunk/SplitOptions 不处理**（裁定记录已在 plan Deferred But Adjudicated）：跨包消费者 JavaFileSplitter.java:36,92,97 + TestTextSplitter:28 Jackson 反射序列化依赖 getter 可见性，移除 public 会编译失败 + 静默改变序列化输出；live 复核未改动
- **DefaultAiChatService.parseToolCalls:562**：`(Map<String, Object>) arguments` cast 前补局部 `@SuppressWarnings("unchecked")`（AnthropicDialect 同款局部变量级写法，:548 List cast 的既有注解未动）
- **验证**：`./mvnw compile -pl nop-ai/nop-ai-core,nop-ai/nop-ai-tools -am` exit 0；`./mvnw test -pl nop-ai/nop-ai-tools,nop-ai/nop-ai-skills/nop-ai-code-analyzer -am -T 1C` BUILD SUCCESS（FileToolBizModel 下游 + SplitChunk 消费者回归全绿，0 failures 0 errors）
- **偏差**：无（test 命令将 plan 的两条 -pl 合并为一条等价命令执行）
- **Commit**: 见 git log（随 Phase 2）

## Plan `2026-08-01-0746-2-arm-p3-code-hygiene` Phase 1 — 测试 System.out 清理（MA4.2-12，13 文件逐文件裁定）

- **13 文件 × 处置对照表**（grep 命中集合与"保留"集合逐行一致，清理后 `grep -rln "System.out" nop-ai --include="*.java"` = 7 文件 / 50 行，全为保留集合）：

| 文件 | 命中 | 处置 | 理由 |
|---|---|---|---|
| nop-ai-core AiChatServiceManual | 3 | 保留 | @Disabled 手动冒烟测试（需真实 LLM key），输出即手动运行结果 |
| nop-ai-core TestChatServiceImpl | 7 | 保留 | @Disabled 手动测试（本地 LM Studio），输出即手动运行结果 |
| nop-ai-maven VfsMavenUsageExampleRunner | 35 | 保留 | main() 示例运行器，System.out 即示例演示内容（audit "acceptable for manual/exploratory tests"） |
| nop-ai-coder AiGenCodeTaskManual | 1 | 保留 | @Disabled 手动 codegen runner |
| nop-ai-coder TestAiCoder | 2 | 保留 | @Disabled 类级手动回归 |
| nop-ai-coder TestAiCoderHelper | 5 | 转 SLF4J LOG | 启用测试（:119 在 @Disabled 方法内一并转），诊断输出需日志可控 |
| nop-ai-skills TestJavaCodeFileInfoParser | 1 | 保留 | @Disabled 方法手动探索 |
| nop-ai-skills TestJavaFileSplitter | 1 | 转 SLF4J LOG | 启用测试 |
| nop-ai-skills TestJavaParser | 3 | 转 SLF4J LOG | 启用测试 |
| nop-ai-skills MavenModuleStructureTest | 1 | 转 SLF4J LOG | 启用测试 |
| nop-ai-skills TestDeepWikiPrompts | 1 | 保留 | @Disabled 类级 |
| nop-ai-translate TestTextSplitter | 3 | 转 SLF4J LOG | 启用测试 |
| nop-ai-translate FixTranslateDir | 1 | 删除 | `cleanup()` 死代码（调用点 `// cleanup();` 已注释），println 永不可达，零行为变更 |

- **改动**：5 文件加 `private static final Logger LOG = LoggerFactory.getLogger(X.class)`（slf4j import 按 io.nop.* → third-party → java.* 惯例，与 TestChatLogHelper 同款），13 处 `System.out.println` → `LOG.info`；FixTranslateDir 删除 1 行死代码 println；其余 7 文件零改动
- **验证**：`./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-maven,nop-ai/nop-ai-coder,nop-ai/nop-ai-skills -am -T 1C` BUILD SUCCESS（0 failures 0 errors；TestAiCoderHelper 12 run 1 skip、TestJavaFileSplitter/TestJavaParser/MavenModuleStructureTest/TestTextSplitter 各 1 run 全绿，skip 数与 @Disabled 一致，测试数量零减少）
- **偏差**：无（裁定依据与 plan 一致：启用测试转 logger、手动/示例保留、死代码删除）
- **Commit**: 见 git log（随 Phase 1）

## Plan `2026-08-01-0746-1-arm-p3-api-doc-debt` Closure — 独立 closure audit APPROVE + 计划收口

- **独立 closure audit**（fresh session `ses_0454725ebffeeMEaWUE1gNr3gr`）verdict **APPROVE**：Phase 1-4 逐条 Exit Criteria + 8 条 Closure Gates 全部 PASS（rg 0 命中、18 文件迁移指引、17 替代类名文件存在、@Deprecated 属性零 diff、IRequestStore 未改动、IFileOperator @deprecated 英文行、7 接口类级 javadoc、arm-index:463 + roadmap:262 登记、Anti-Hollow 4/4 语义吻合（LocalFileOperator:97-100 notFound marker / ToolManagerImpl:134-137 loadTool / ChatStreamChunk:34,59 / IChatService:33 callStream）、compile 复核 exit 0、check-doc-links 复核 No errors found）
- **计划收口**：4 Phase Status 全部 completed + Closure Gates 全勾选 + Plan Status → completed；Closure 段写入独立 audit 证据（session ID + 逐条 PASS + 证据来源）；`check-plan-checklist.mjs --strict` exit 0
- **计划终态**：MA4.2-07（18 个 deprecated 类迁移指引）/MA4.2-10（45 个接口方法 javadoc，IRequestStore N/A 核验）/MA4.5-009（7 个接口类级 javadoc）/MA4.5-008（IFileOperator deprecated 英文化）全部 fixed；4 个 commit（d72da2f66/44118eec0/cb6850b38/697724505 + 本 closure 收口）；arm-index `fixed`；roadmap 第十一批 ✅
- **Commit**: 随 Closure 提交（见 git log）

## Plan `2026-08-01-0746-1-arm-p3-api-doc-debt` Phase 4 — 全量验证 + 登记

- **全量验证**：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（0 failures 0 errors）；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0；`check-doc-links.mjs --strict` exit 0（No errors found）
- **登记**：arm-index 新增「P3 追踪（第十一批 — API 文档债务）」小节 4 行（MA4.2-07/-10、MA4.5-008/-009 → fixed + 修复位置/测试列）；roadmap v11 更新（第十一批行 ✅ closed 2026-08-01 + 尾段叙事）；无 `> Source Audits:` front matter（仅 `> Source:`），source audit closing 步骤按 mission 契约省略
- **Commit**: 见 git log（随 Phase 4）

## Plan `2026-08-01-0746-1-arm-p3-api-doc-debt` Phase 3 — 小型公开接口类级 javadoc + IFileOperator 语言一致性（MA4.5-009/-008）

- **7 个接口补类级 javadoc**（1-2 句契约 + 关键方法 @param/@return）：IChatLogger（nop-ai-api，logging hook，default=DefaultChatLogger 凭据脱敏）、IToolExecuteContext（nop-ai-toolkit，沙箱工作目录/envs/过期时间/cancelToken/沙箱文件系统/executor，6 getter 全注释，default=ToolExecuteContext 不可变）、IToolCallInterceptor（beforeCall 拒绝语义 + afterCall 观察，默认 no-op）、IToolExecutorProvider（按名解析 executor 注册表）、IDslTool（nop-ai-coder，xdef schema 读取 + DSL 文档类型转换 load/save，4 方法全注释）、ITextClassifier/IDocumentClassifier（nop-ai-core classifier，分类契约）
- **核验仅确认**：IEmbeddingModel 与 ITokenCountEstimator 已有 SPI 裁定类级 javadoc（P1-MA5-003），完整性核验通过不重写
- **IFileOperator @deprecated 英文化**：`@deprecated Use io.nop.ai.toolkit.fs.IToolFileSystem instead (forRemoval=true; removal is future major-version work)`，替代对象与 forRemoval 语义保留；@deprecated 行中文残留 0
- **验证**：`./mvnw compile -pl nop-ai/nop-ai-api,nop-ai/nop-ai-core,nop-ai/nop-ai-toolkit,nop-ai/nop-ai-coder -am -q` exit 0；9 个目标文件类级 `/**` grep 全在
- **Commit**: 见 git log（随 Phase 3）

## Plan `2026-08-01-0746-1-arm-p3-api-doc-debt` Phase 2 — 接口方法 javadoc 补全（MA4.2-10）

- **IFileOperator**（24 个无 javadoc 方法全补，英文）：getResource（含路径逃逸异常条件）/readFileContents/writeFileContents/readFileContent（offset<0→0、limit<=0 无限制、不存在→notFound marker）/readLines/readLines(path,start,count)（既有中文 javadoc 保留）/writeFileContent×2/exists/findFilesByAntPath/findFilesByFilter/listDirectory/readFileContentsByAntPath/findFileByRegex/findFilesByRegex/findFileByAntPath/findFileByFilter/findFileByName/delete/move×2/copy×2/applyDiff/mergeFile（xdef delta 合并语义）；findFilesByGlob/grep/grepFiles/globGrep 5 个既有中文 javadoc 未重复写
- **IPromptTemplate**（11 方法全补，含 getDisplayName/getDescription/getInput(s)/getOutput(s)/applyChatOptions/prepareInputs×2/generatePrompt/processChatResponse）；**IPromptTemplateManager**（2 方法）；**IAiChatResponseChecker**（isAccepted + 类级契约句）；**IAiTextSplitter**（split() + SplitChunk/SplitOptions 内嵌类访问器 15 方法）；**ITokenCountEstimator**（estimate/estimateForMessage，SPI 语义）
- **IRequestStore 核验 N/A**：live 类级 javadoc + saveRequest/getRequestPrompt 均含 @param/@return（中文），audit 误列确认，零改动
- **踩坑记录**：javadoc `{@code}` 内不得出现 `*/` 序列（块注释提前终止，`**/*.java` 与 `**/fileName` 均触发编译错误），改为不含 `*/` 的措辞
- **验证**：`./mvnw compile -pl nop-ai/nop-ai-core -am -q` exit 0；方法级 javadoc 覆盖 python 抽查全部 OK（45 方法）
- **Commit**: 见 git log（随 Phase 2）

## Plan `2026-08-01-0746-1-arm-p3-api-doc-debt` Phase 1 — deprecated 类迁移指引补全（MA4.2-07，18 文件）

- **live 替代面调查结论**（全部经 grep 验证存在于仓库）：消息系 13 文件 → nop-ai-api `io.nop.ai.api.chat.messages.*` 新消息层级（AiMessage/AbstractTextMessage→ChatMessage；AiUserMessage→ChatUserMessage；AiAssistantMessage→ChatAssistantMessage；AiSystemMessage→ChatSystemMessage；AiToolMessage→ChatToolResponseMessage（工具调用本体为 ChatToolCall）；AiToolResponseMessage→ChatToolResponseMessage；ToolCall→ChatToolCall；AiMessageAttachment→ChatAttachment；AiChatUsage→ChatUsage）；AiChatExchange→ChatRequest+ChatResponse（IChatService.callAsync 迁移方向）；Prompt→ChatRequest；MessageStatus 裁定 no direct replacement（流式状态由 ChatStreamChunk index/finishReason 建模）；工具系 4 文件 → IToolDefinition（nop-ai-api，ChatToolDefinition/AiToolModel 实现）+ IToolExecutor/IToolManager（nop-ai-toolkit；ToolSpecificationLoader→IToolManager.loadTool，加载 AiToolModel from *.tool.xml）；IAiChatProgressListener（repo 内零消费者）→ IChatService.callStream 响应式流
- **18 文件 @deprecated 泛化消息全部替换**为具体迁移指引（`Use {@link ...} instead`）；`rg "This internal AI core"` 0 命中；@Deprecated 注解属性（forRemoval 等）零改动
- **替代类名真实性验证**：17 个被引用新类名全量 grep 存在（ + ICancelToken）
- **验证**：`./mvnw compile -pl nop-ai/nop-ai-core -am -q` exit 0；纯 javadoc 变更零行为变更
- **Commit**: 见 git log（随 Phase 1）

## Plan `2026-08-01-0441-1-arm-ma4-2-05-engine-split` Closure — 独立 closure audit APPROVE + 计划收口

- **独立 closure audit**（fresh session `ses_0457f6464ffeNO1eBBcnM36Mzf`）verdict **APPROVE**：行数（954/986/382 + 18 提取类全 <1000）、语义对比（三份 0-diff，baseline 与 662660ab0 字节一致）、focused 测试（45 例值级断言）、Anti-Hollow（调用链追踪连通 + scan-hollow exit 0）、build/test（2907 tests 0 failures）、登记（arm-index/roadmap/design README/audits README）、API 兼容全部 PASS。2 个非阻塞 minor：① plan 文件状态未同步（本次收口修复）；② diff 文件头部 SHA 占位符（compare.sh 改为自动前置 BASE_REF 662660ab0 头部）
- **计划收口**：Phase 1-4 Status 全部 completed + Closure Gates 全勾选 + Plan Status → completed；Closure 段写入独立 audit 证据（逐条 PASS + 证据来源）；`check-plan-checklist.mjs --strict` exit 0
- **MA4.2-05 终态**：ReActAgentExecutor 3728→954 / DefaultAgentEngine 3681→986 / TeamTaskSchedulerDaemon 1108→382；17 提取类（9+8）+ 1 daemon 协调器；45 focused 测试；三份语义对比证据 0-diff 入库（BASE_REF 662660ab0）；arm-index `fixed`；roadmap 第十批 ✅
- **Commit**: 随 Closure 提交（见 git log）

## Plan `2026-08-01-0441-1-arm-ma4-2-05-engine-split` Phase 4 — TeamTaskSchedulerDaemon 拆分（1108 → 382）+ 全量验证 + 登记

- **拆分落地**：`TeamTaskSchedulerDaemon.java` 1108 → **382 行**（<1000）。提取 `TaskDispatchCoordinator`（298 行，同包）：`dispatchClaimedTask` :923-1032（135 行）+ `DispatchTally` :1058-1107 + `resolveSpawnExecutor`/`SpawnWorkerThreadFactory`/`awaitInFlightDispatches` + in-flight 队列与 owned spawn 池字段；主类保留 scanOnce/scanOnceSafe/resolveTeamIdsToScan/start/stop/全部公共访问器（memberSpawner/taskMemberRouter/spawnStepExecutor/awaitInFlightDispatches 委托 coordinator，setDaemonOwnerId/setScanLeaseMs 原体含校验）
- **行为保持**：LOG 身份保持 `TeamTaskSchedulerDaemon.class`（TestSchedulerDaemonCoordination 等 9 个测试文件的 ListAppender 依赖）；stop() 的 owned spawn 池释放 → `dispatchCoordinator.shutdownOwnSpawnExecutor()`；dispatchClaimedTask 调用点 → `dispatchCoordinator.dispatchClaimedTask`（DispatchTally 类型限定）
- **语义对比**：**daemon-semantic-diff.txt RESULT: 0-diff**——三份 diff 全部 0-diff（react/engine/daemon）；whitelist 增补 daemon 分节（_min_sim setScanLeaseMs 0.80/stop 0.65、_new shutdownOwnSpawnExecutor）；compare.sh 从自身目录执行
- **focused 测试**：`TestTaskDispatchCoordinator`（6 例：NoOp 默认/路由抛异常诚实失败/空 plan 诚实失败/同步完成 tally/空队列 await/scanLease 校验）——值级断言
- **全量验证**：`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（**2907 tests 0 failures**）；`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0；`check-doc-links.mjs --strict` exit 0（No errors found）
- **登记**：arm-index「P2 修复追踪」新增 MA4.2-05 `fixed` 行（三文件实测行数 + 17 提取类清单 + 45 focused 测试 + 语义对比证据引用）；roadmap 第十批 🟡 active → ✅（closed 2026-08-01）；`ai-dev/audits/README.md` 新增「证据产物目录（evidence/）」规范（baseline 快照/compare.sh 工具链/diff 输出约定）
- **Commit**: 随 Phase 4 提交（见 git log）

## Plan `2026-08-01-0441-1-arm-ma4-2-05-engine-split` Phase 3 — DefaultAgentEngine 拆分（3681 → 986）

- **拆分落地**：`DefaultAgentEngine.java` 3681 → **986 行**（<1000）。8 个提取类全部同包顶层类：`DefaultAgentEngineConfig`（939，setter/getter 区域 + 35 可选依赖字段 + 全 javadoc）/`AgentSessionLifecycle`（689，resume/restore/buildBaseExecutionContext/buildBudgetedMemorySection/formatMemorySection/CancelHandle/publishCancel*/releaseLockQuietly/isTerminalStatus/restorePendingSessions）/`AgentCallDelegate`（165，messenger 注册+envelope 处理）/`SessionLockRenewal`（115，锁续期）/`AgentExecutorResolver`（271，检查器组合+executor 构建+middleware+hook 贡献）/`AgentSessionSupport`（117，mailbox/sessionId/loadAgentModel）/`AgentTeamBinder`（160，precheck/autoBindLead/Member/resolveActorId）/`AgentStartupWarnings`（154，warnIfInsecureDefaults/warnIfNoOpUsageRecorder/defaultPipelineCompactor），全部 <1000
- **API 兼容**：主类保留全部 60+ 公共 setter/getter 签名（一行 config/helper 委托）；Builder 嵌套保留（`new DefaultAgentEngine.Builder(...)` 测试兼容）；8 构造器保留（私有全参构造器改为装配 config+helpers）；`resumeSession`/`restoreSession`/`restorePendingSessions`/`execute`/`doExecute`/`cancelSession`/`forkSession`/`sendMessage`/`close` 全部保留（resume/restore 委托 lifecycle）；`resolveExecutor` 等 5 个包级 facade 保留（TestChannelPrincipalPropagation 的 CapturingEngine override 语义保持——doExecute 经虚拟分派调用 facade）
- **行为保持细节**：`setMessenger` 全逻辑移入 callDelegate（NoOp 解析+幂等注册）；`setUsageRecorder` 保留 flag 复位（startupWarnings.resetUsageRecorderNoOpWarned）；`setLockRenewExecutor` 的 requireNonNull+own 标志随移（SessionLockRenewal 修复为原体）；`restorePendingSessions` 经 lifecycle 但调用 `engine.restoreSession`（虚拟分派，TestRestorePendingSessions override 计数语义保持）；**LOG 身份保持**——5 个测试类向 `DefaultAgentEngine.class` logger 挂 ListAppender，全部提取类 LOG 使用 `LoggerFactory.getLogger(DefaultAgentEngine.class)`（行为零变化）
- **语义对比**：compare.py 增强（multiset 同名匹配/字段行跳过/facade 匹配/_min_sim 阈值/小体量 ≤15 token 阈值 0.5/委托 stub 白名单）——**engine-semantic-diff.txt RESULT: 0-diff**（baseline 187 成员全部匹配；FUZZY 条目均为白名单委托重写：doExecute 0.90/resumeSession 0.86/restoreSession 0.90/resolveExecutor 0.69 等）；react 复跑仍 0-diff；compare.sh 修复（cd $DIR + --whitelist 参数）；BASE_REF 662660ab0 头部注释
- **focused 测试**：`TestEngineConfigAndHelpers`（14 例：config 默认值=原字段初始化器/round-trip/超时正数校验、startupWarnings 发射、resolver 约束包装+react 模式 executor、teamBinder 快速失败+actorId 回退、callDelegate NoOp 注册+extractFinalAssistantMessage（含 private→包级访问放宽）、lifecycle formatMemorySection/isTerminalStatus/releaseLockQuietly）——全部值级断言
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS（**2901 tests 0 failures 0 errors**）；`wc -l` 主类 986 + 8 提取类全部 <1000
- **Commit**: 随 Phase 3 提交（见 git log）

## Plan `2026-08-01-0441-1-arm-ma4-2-05-engine-split` Phase 2 — ReActAgentExecutor 拆分（3728 → 954）

- **拆分落地**：`ReActAgentExecutor.java` 3728 → **954 行**（<1000）。9 个提取类全部同包顶层类：`ReActAgentExecutorBuilder`（656）/`LlmCallCoordinator`（423）/`AgentHookInvoker`（142）/`AgentSecurityConsultation`（679）/`AgentCompactionCoordinator`（150）/`AgentToolPlanResolver`（389）/`AgentPromptAssembly`（336）/`AgentToolDispatcher`（434）/`AgentLoopGuard`（133），全部 <1000。主类保留 execute()（内联块 A/B/C/D 迁出后 ~600 行）、builder() 工厂（返回新类型）、getSandboxBackend、5 公共常量、buildToolCallSignatures、handleCancellation；构造器 private→package-private（白名单访问放宽）
- **内联块提取**（execute() 895→600）：A 执行前装配 → `AgentPromptAssembly.assembleExecutionSetup`；B toolExecCtx → `AgentToolDispatcher.prepareDispatchContext`；C 扇出+结果提交（228 行）→ `AgentToolDispatcher.executeAllowedCalls`（reentryCounters 局部化、toolExecCtx 参数化）；D 输出 guardrail → `AgentPromptAssembly.checkOutputGuardrail`；steering drain → `AgentToolDispatcher.drainSteering`
- **委托重写**（白名单）：publishEvent/publishErrorEvent/invokeOnError/vetoReason/executeWithMiddleware/invokeHooks → `hookInvoker.*`；performCompaction/shouldTriggerCompaction/resolveMaxContextTokens → `compactionCoordinator.*`；resolveWorkDir/computeEffective*/buildToolDefinitions → `toolPlanResolver.*`；doLlmCallWithRetry/resolveCircuitAware/buildModelKey → `llmCoordinator.*`（resolveCircuitAware 4 参→2 参，router/breaker 吸收为字段）；handleSessionPaused/handleGoalStuck/shouldForceStop/handleForcedStop → `loopGuard.*`；checkInputGuardrail → `promptAssembly.*`；resolveWorkDirString → `securityConsultation.*`；parseToolCallId → `LlmCallCoordinator.parseToolCallId`；DEFAULT_MAX_REENTRIES 常量保留主类以 `ReActAgentExecutor.` 引用
- **引用面更新**（9 处 javadoc + 1 测试）：`ReActAgentExecutor.Builder` 6 处（SmartModelRouter/IModelSwitchedMessageWriter/DbModelSwitchedMessageWriter/NoOpModelSwitchedMessageWriter/InMemoryMemoryStoreProvider/DefaultAgentEngine:1633 + TestSmartModelRouterWiring）→ `ReActAgentExecutorBuilder`；checkPathAccess（ToolPathArgKeys:8）→ `AgentSecurityConsultation`；buildModelKey（ICircuitBreaker:31,46/ThresholdBreaker:12）→ `LlmCallCoordinator`；performCompaction（CheckpointType:44）→ `AgentCompactionCoordinator`；TestSecureByDefault javadoc 同步
- **测试兼容**：`TestToolVisibilityFiltering` 调用 `exec.buildToolDefinitions(...)` → 主类保留包级委托方法；`TestSessionStoreForkMessageFilter` 的 `new DefaultAgentEngine.Builder(...)` 不受影响（DAE Builder 保留）；无反射依赖
- **语义对比**：`compare.py`（name-based 匹配 + body-core 相似度 ≥0.85 + 内联块 containment ≥0.85 + 嵌套类 children 覆盖 + 白名单 per-baseline 分节）——**react-semantic-diff.txt RESULT: 0-diff**（baseline 108 成员全部匹配；FUZZY 条目均为白名单委托重写：builder 0.75/execute 0.67/handleCancellation 0.97 等；INLINE-EXTRACT 6 条含 containment 值）；证据文件头部含 BASE_REF SHA（662660ab0）
- **focused 测试**：`TestEngineExtractedCoordinators`（16 例：hook 传递/否决/中间件/Reenter 守卫/事件发布、buildModelKey/parseToolCallId、NoRetryPolicy 单次调用、TRANSIENT 重试 3 次成功、pause/stuck/forced-stop 状态机、compaction 阈值、工具过滤/权限钳制/workDir）`TestEngineExtractedSecurityAndDispatch`（9 例：checkpoint 链 ALLOW/DENY、denial 阈值暂停、Layer3 审批、dispatcher 扇出提交/错误透传、Builder 构造）——全部值级断言
- **接线验证**：execute() 路径到提取类运行时连通由 2887 例全绿证明（含全部既有 E2E：TestCompactionInReActLoop/TestSkillEngineInReActLoop/TestTalentInReActLoop/TestSecurityCheckpointChain 等）；证据引用写入 react-semantic-diff.txt 头部
- **验证**：`./mvnw test -pl nop-ai/nop-ai-agent -am -T 1C` BUILD SUCCESS（**2887 tests 0 failures 0 errors**）；`wc -l` 主类 954 + 9 提取类全部 <1000
- **Commit**: 随 Phase 2 提交（见 git log）

## Plan `2026-08-01-0441-1-arm-ma4-2-05-engine-split` Phase 1 — 引擎大文件拆分设计（design-first）

- **盘点（live，行号 1-based）**：ReActAgentExecutor 3728 行（execute :1357-2250 共 894 行；4 嵌套类：Builder :769-1344/LlmCallResult :748-763/ToolCallOutput :3683-3691/SecurityConsultationOutcome :3699-3727）；DefaultAgentEngine 3681 行（Builder :472-615 保留；CancelHandle :2212-2237 移出；accessors 区域 :859-1816 ~958 行；doExecute :2271-2470 200 行）；TeamTaskSchedulerDaemon 1108 行（scanOnce :737-865 129 行保留；dispatchClaimedTask :923-1032 110 行 + DispatchTally :1058-1107 50 行移出）
- **设计文档**：`ai-dev/design/nop-ai-agent/engine-class-split.md`（决策+契约+预算+验证策略+拒绝方案；成员级映射表按 Rule #14 不入设计文档）。注册进 `ai-dev/design/nop-ai-agent/README.md`（Java 引擎层节）
- **提取方案**（详见设计文档 §2-4）：ReAct 9 类（ReActAgentExecutorBuilder/LlmCallCoordinator/AgentHookInvoker/AgentSecurityConsultation/AgentCompactionCoordinator/AgentToolPlanResolver/AgentPromptAssembly/AgentToolDispatcher/AgentLoopGuard），预算 ~877 行（余量 123）；DAE 8 类（DefaultAgentEngineConfig/AgentSessionLifecycle/AgentCallDelegate/SessionLockRenewal/AgentExecutorResolver/AgentSessionSupport/AgentTeamBinder/AgentStartupWarnings），预算 ~845 行（余量 155，含 doExecute 调用点委托重写与构造器收敛补充移出群）；Daemon 1 类（TaskDispatchCoordinator），预算 ~863 行
- **嵌套类裁定**：ReAct 4 个全部移出（Builder→顶层类，builder() 工厂保留返回新类型）；DAE Builder 保留（测试 `TestSessionStoreForkMessageFilter:157/175` 直接 `new DefaultAgentEngine.Builder(...)`）、CancelHandle 随生命周期组移出；Daemon DispatchTally 随调度器移出
- **预算可达性**：三文件 <1000 均数学可达（设计文档 §2.4/§3.3/§4.3 预算表；DAE 依赖独立子 agent 复核修正——buildBaseExecutionContext 命名、autoBindTeam 区间、8 个同主题方法随组移出后可达）；无预算不可达出口触发
- **注册面/引用面证据**：三主类无 beans.xml/yaml/工厂/IoC 注册（全仓 grep 0 命中）；编译期引用：`DefaultAgentEngine:3274` builder()（返回类型变更，调用点不变量保持）、`PipelineCompactor:158` DEFAULT_MAX_CONTEXT_TOKENS（常量保留不动）、`TestSessionStoreForkMessageFilter` Builder 直接构造（保留不动）；javadoc 引用随提取更新 9 处：SmartModelRouter:50/IModelSwitchedMessageWriter:17/DbModelSwitchedMessageWriter:27/NoOpModelSwitchedMessageWriter:15/InMemoryMemoryStoreProvider:11/DefaultAgentEngine:1633（Builder 6 处）+ ToolPathArgKeys:8（checkPathAccess）+ ICircuitBreaker:31,46/ThresholdBreaker:12（buildModelKey）+ CheckpointType:44（performCompaction）
- **语义对比方法学**（设计文档 §8）：对比域 = 原文件全部成员集合（含 private）vs 新主类 ∪ 提取类；归一化白名单：访问放宽/状态注入参数追加/成员 relocation/委托调用重写/内联块提取（归一化文本重叠 ≥0.85）；对比基准 = baseline 快照 + 头部 SHA；脚本 `ai-dev/audits/evidence/ma4-2-05/compare.sh` 输出三份 diff 文件提交入库
- **成员级映射表（紧凑清单，独立子 agent 逐条核对，verdict 见下）**：

```
=== ReActAgentExecutor（3728）→ ReActAgentExecutor.java（主类）+ 9 提取类 ===
ReActAgentExecutorBuilder:  Builder :769-1344（40 setter + build()）
LlmCallCoordinator:         doLlmCallWithRetry :637-741; LlmCallResult :743-763;
                            buildModelKey :2325-2336; callChatWithTimeout :2338-2380;
                            parseToolCallId :2382-2397; resolveCircuitAware :2399-2511;
                            MAX_FALLBACK_SCAN :2513-2523; sleepBackoff :2526-2544
AgentHookInvoker:           executeWithMiddleware :2766-2794; invokeHooks :2796-2834;
                            invokeOnError :2836-2842; vetoReason :2844-2849;
                            publishEvent :2851-2856; publishErrorEvent :2858-2863
AgentSecurityConsultation:  buildCheckpointChain :455-635; handleDenialAndCheckThreshold :2259-2323;
                            extractArguments :2546-2553; resolveWorkDirString :2555-2562;
                            checkPathAccess :2865-2898; checkLayer2Consultation :2900-2953;
                            checkLayer3Approval :2955-2999; checkWriteConflict :3001-3116;
                            resolveAbsolute :3118-3131; SecurityConsultationOutcome :3693-3727
AgentCompactionCoordinator: shouldTriggerCompaction :2676-2682; resolveMaxContextTokens :2684-2690;
                            performCompaction :2692-2764
AgentToolPlanResolver:      buildToolDefinitions :3159-3257; intersects :3259-3271;
                            computeEffectiveAllowedTools :3273-3319; resolveWorkDir :3321-3335;
                            computeEffectivePathRoots :3337-3399; computeOwnDeclaredPathRoots :3401-3417;
                            computeEffectivePathRules :3419-3470; isUnderAnyRoot :3472-3496;
                            toToolDefinition :3498-3504
AgentPromptAssembly:        checkInputGuardrail :3133-3147; extractLastUserContent :3149-3157;
                            consultTalents :3506-3550; injectSystemInstruction :3552-3559;
                            consultSkills :3561-3613; consultPromptContributions :3615-3656;
                            buildChatOptions :3658-3681
                            + 新方法 assembleExecutionSetup（execute :1374-1378 内联块）
                            + 新方法 checkOutputGuardrail（execute :1727-1757 内联块）
AgentToolDispatcher:        ToolCallOutput :3683-3691
                            + 新方法 prepareDispatchContext（execute :1816-1851 内联块）
                            + 新方法 executeAllowedCalls（execute :1895-2123 内联块）
                            + 新方法 drainSteering（execute :2142-2150 内联块）
AgentLoopGuard:             handleSessionPaused :2564-2581; handleGoalStuck :2583-2608;
                            shouldForceStop :2635-2651; handleForcedStop :2653-2674
主类保留:                   execute :1357-2250（块 A/B/C/D 迁出后 ~559）; builder() :765-767;
                            getSandboxBackend :1352-1354; handleCancellation :2252-2257;
                            5 公共常量 :171-186; 字段 tokenEstimator/completionJudge/goalTracker/
                            sustainer/sessionStore/checkpointManager/toolCallRepairer/checkpointChain
                            + 9 helper 实例字段

=== DefaultAgentEngine（3681）→ DefaultAgentEngine.java（主类）+ 8 提取类 ===
DefaultAgentEngineConfig:   区域 :859-1816 setter/getter 群（~30 setter + ~28 getter，含 javadoc；
                            messenger 群与 getMailboxFactory 除外）; 对应 ~35 个可选依赖字段 :154-468 内
AgentSessionLifecycle:      resumeSession :2588-2755; restoreSession :2758-2932;
                            buildBaseExecutionContext :2485-2519; buildBudgetedMemorySection :2529-2551;
                            formatMemorySection :2560-2585; CancelHandle :2212-2237;
                            publishCancelRequested :2150-2156; publishCancelled :2158-2163;
                            releaseLockQuietly :1843-1851; isTerminalStatus :3094-3101
                            （注入 Supplier<ExecutorService>——getAgentExecutor 保留主类）
AgentCallDelegate:          registerCallAgentHandler :954-971; handleCallAgentRequest :989-1051;
                            extractFinalAssistantMessage :1059-1072; getMessenger :1078-1080
SessionLockRenewal:         getLockRenewExecutor :1862-1872; startLockRenewal :1908-1917;
                            renewOnceSafe :1927-1940; handleLeaseLost :1953-1965;
                            cancelLockRenewalQuietly :1973-1982
AgentExecutorResolver:      resolveEffectiveToolAccessChecker :3120-3136;
                            resolvePerAgentPathChecker :3178-3184;
                            resolveEffectivePathAccessChecker :3192-3194（单参重载）+ :3217-3238;
                            resolveExecutor :3240-3252 + :3256-3330; resolveHookContributions :3352-3369;
                            resolveMiddlewares :3386-3420（注入 Supplier<ExecutorService>——:3319 消费）
AgentSessionSupport:        getMailboxFactory :1105-1107; ensureSessionMailbox :3433-3454;
                            resolveSessionId :3456-3469; loadAgentModel :3495-3510
AgentTeamBinder:            precheckTeamDeclarations :3542-3551; autoBindTeam :3559-3570;
                            autoBindLead :3580-3606; autoBindMember :3616-3661; resolveActorId :3670-3680
AgentStartupWarnings:       warnIfInsecureDefaults :737-816; warnIfNoOpUsageRecorder :833-841;
                            defaultPipelineCompactor :695-701（不并入 Config——合并超 1000 行阈值）
主类保留:                   Builder :472-615; builder() :616-618; 8 构造器 :620-693;
                            execute :2252-2255; doExecute :2271-2470（内联块调用点委托重写）;
                            cancelSession :2101-2148; forkSession :2166-2207; sendMessage :2240-2249;
                            getSessionStatus :2092-2098; isClosed :3081-3083;
                            getInstanceId :1827-1829; getEventPublisher :818-820;
                            getMemoryInjectionBudgetTokens :1994-1996; getAgentExecutor :2007-2017;
                            超时访问器 :2041-2089; getSessionTakeoverLock :1696-1698 等
                            + 全部 60+ setter/getter 签名一行委托（config/lifecycle 委托）
                            + 9 helper 实例字段（config/sessionLifecycle/callDelegate/lockRenewal/
                            executorResolver/sessionSupport/teamBinder/startupWarnings/agentExecutorSupplier）

=== TeamTaskSchedulerDaemon（1108）→ TeamTaskSchedulerDaemon.java（主类）+ 1 提取类 ===
TaskDispatchCoordinator:    dispatchClaimedTask :923-1032; DispatchTally :1058-1107;
                            resolveSpawnExecutor :510-522; spawnThreadFactory :525-538;
                            awaitInFlightDispatches :556-572（主类保留公共委托）
主类保留:                   scanOnce :737-865; scanOnceSafe :716-730; resolveTeamIdsToScan :867-880;
                            start/stop 生命周期 :686-709; 全部公共 setter/getter :398-666;
                            getScanIntervalSec/getDaemonSessionId/getTargetTeamIds 等
```

- **独立子 agent 映射表核对**：两轮 fresh session。第一轮（`ses_045e7049effeDyBncPvJWGXGxT`）verdict REJECT——DAE 段 3 blocker：① `buildBaselineContext` 实为 `buildBaseExecutionContext`（:2485-2519）；② `autoBindTeam` 区间错（实为 :3559-3570）；③ 8 个同主题方法（precheckTeamDeclarations :3542-3551/releaseLockQuietly :1843-1851/isTerminalStatus :3094-3101/buildBudgetedMemorySection :2529-2551/formatMemorySection :2560-2585/getAgentExecutor（保留主类，Supplier 注入）/resolveMiddlewares :3386-3420/autoBindLead :3580-3606）未随组移出将导致编译失败。ReAct 段 49/49 行与 Daemon 段 5/5 行 APPROVE。修复后第二轮（`ses_045db59edffehuY5twWQ4xo7Qi`）verdict **APPROVE**——37 个移出成员全部在位（±3 行）、跨依赖全部覆盖（a 同类/b 主类公共/c 他类委托）、主类保留成员调用移出成员的委托重写清单完整（doExecute 12 处/cancelSession 3 处等）、无双重移动；3 条 minor 已就地处理（resolveEffectivePathAccessChecker 单参重载 :3192-3194 补入 AgentExecutorResolver；resolveExecutor:3319 getAgentExecutor → AgentExecutorResolver 亦注入 Supplier；getMessenger/getMailboxFactory 移出后主类保留委托签名）

## Mission audit-remediation — 第十批 deferred successor 计划起草（1 plan → active）

- **背景**：audit-remediation roadmap 全部工作项 done（M0-MG + 九批 deferred successors closed，MV 矩阵 open=0）。本轮从先前计划 Deferred But Adjudicated / Non-Blocking Follow-ups 段重开剩余最高价值项 **MA4.2-05**（引擎大文件拆分）——nop-ai 唯一剩余的超 3000 行生产文件治理项（ReActAgentExecutor 3728 / DefaultAgentEngine 3681 / TeamTaskSchedulerDaemon 1108，合计 7409 行）；0206-1 Non-Goals 与 0206-3 Non-Blocking Follow-ups 均显式"另行规划"，第九批先例（P2-MA1-004/005 重开）确立"按严重度排序重开 Successor Required: no 项"的机制。
- **Plan 1** `2026-08-01-0441-1-arm-ma4-2-05-engine-split.md`（active）：MA4.2-05 三文件 design-first 拆分至 <1000 行。Phase 1 = 行数预算 + 提取边界设计（`ai-dev/design/nop-ai-agent/engine-class-split.md`，成员级映射表入 daily log 紧凑清单，独立子 agent 逐条核对）；Phase 2-4 = 三文件拆分 + 语义对比（对比域 = 原文件全部成员集合 vs 新主类 ∪ 提取类，含 private；BASE_REF 快照捕获机制 `ai-dev/audits/evidence/ma4-2-05/baseline/`）+ focused 测试 + 接线验证 + 全量回归。预算不可达出口裁定（recorded scope change，<1000 未达成不得 completed）。
- **对抗性审查**：4 轮独立子 agent 复审（每轮 fresh session）。第一轮 1 Blocker（B1 设计文档与 Rule #14 冲突）+ 4 Major + 5 Minor；第二轮 1 Blocker（B-new-1 证据落位 gitignored `_tmp/`）+ 2 Major（预算算术、映射表落位）+ 5 Minor；第三轮 3 Major（DAE 预算缺口、BASE_REF 时序、跨类迁移归一化）+ 7 Minor；第四轮 1 Major（引用面 22 处）+ 7 Minor 全部修复；第五轮共识核验：0 Blocker 0 Major，残留 Minor 全部被"以 Phase 1 重新盘点为准"机制覆盖 → 标记 active。
- **live 核实关键事实**：execute() 895 行（:1357-2251）、嵌套类 4+2、编译期引用 DefaultAgentEngine:3274 builder() + PipelineCompactor:158 常量、22 处注释/javadoc 引用（约 9 处随提取需更新）、daemon 9 个测试文件。
- **roadmap**：`ai-dev/backlog/audit-remediation-roadmap.md` 更新至 v10——第十批 1 行登记（🟡 active）+ 尾部说明同步；`check-doc-links.mjs --strict` exit 0。
- **Commit**: 未提交（计划起草阶段，执行时随代码提交）

## Plan `2026-08-01-0206-3-arm-p3-test-quality-residual` 执行 — assertTrue-only 升级 / 低价值测试裁定 / 大测试文件拆分

- **Phase 1（MA4.3-09/13 值级断言升级）**：`TestDefaultPermissionMatrix`（5 tests）allow 路径改 `assertEquals(MatrixDecision.allow(), decision)`（all-field equals），deny 路径 `assertDeniedFor` 断言 `getChannel()/getLevel()/getReason()` 结构化字段（GROUP+ELEVATED、API/DM/GROUP/null+RESTRICTED、OPERATOR bypass）。`TestDbSessionTakeoverLockDualInstanceE2E`（2 tests）新增 `readLockOwner()` 经 `AiAgentSessionLockTable` 公开常量直查共享 H2，两场景逐步断言 LOCK_OWNER 字段值变迁（engine-A→null→engine-B→null；过期租约行抢占前保留）。13 个其余 assertTrue-only 文件（12 NoOp + ContentTrustEvaluator）不触碰，裁定记录落盘 plan Deferred 段。
- **Phase 2（MA4.4-01/02/03 裁定）**：01 = 删除 `compressibleToolsSetContainsExpectedTools`（断言内部常量非行为；行为断言 `nonCompressibleToolsPreserved`/`compressedPlaceholderContainsToolInfo` 保留）；02 = 三个 round-trip 测试合并为 `testAllFieldsRoundTrip()`；03 = `TestSecurityLevel` 3→1 tests，保留唯一非编译器保证的长度断言（设计 §5.1 契约守卫），valueOf/ordinal 编译器保证项删除，理由入 javadoc。
- **Phase 3（MA4.2-06 拆分）**：`TestMultiMemberFanOut` 1139 行 → `AbstractMultiMemberFanOutTest`（共享 fixture）+ `Success`（5）/`Failure`（9）/`Routing`（4），18 @Test 不减少；`TestScheduledRecoveryManager` 1076 行 → `AbstractScheduledRecoveryManagerTest` + `ScanOnce`（8）/`Scheduling`（9）/`OrphanHandler`（7）/`Timeout`（12），36 @Test 不减少；拆分文件均 <500 行，场景仍跑真实组件链（真实 H2 + 真实 orchestrator/scanOnce）。
- **arm-index**：新增「P3 追踪（第九批 — 测试质量 P3 残余）」小节，登记 MA4.3-09/13、MA4.4-01/02/03、MA4.2-06 六行。
- **验证**：`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS（3519 tests 0 failures 0 errors，全模块报告扫描）；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0；`check-doc-links.mjs --strict` exit 0；`check-plan-checklist.mjs --strict` exit 0（closure audit 见 plan Closure 段）。
- **Commit**: 未提交

## Plan `2026-08-01-0206-2-arm-p2-tools-structure-residual` 执行 — GraphQLToolSet 条件注册 / ThoughtStorage 裁定 / nop-ai-rag 裁定

- **Phase 1（P3-MA1-016）**：`ai-tools-defaults.beans.xml` 的 `nopGraphQLToolSet` bean 增加 `<ioc:condition><on-bean>nopGraphQLEngine</on-bean></ioc:condition>`（Nop IoC 子元素语法，beans.xdef BeanConditionModel；bean 名匹配非接口名——audit 原建议属性语法不存在）。测试 `TestGraphQLToolSetConditionalRegistration`（4 例）：① xdef 模型解析含条件子元素不报错；② 与 biz-defaults 仓库实例（nopCrudBizInitializer/nopObjDictLoader on-bean）结构一致性；③ 有引擎（biz autoconfig → nopGraphQLEngine）容器接线注入 `@Named("nopGraphQLToolSet")` 成功；④ 无引擎自定义容器（BeanContainerBuilder 仅 ai-tools-defaults）装配不失败且 bean 被跳过。pom 补 `nop-autotest-junit` + `nop-http-client-jdk` test 依赖（后者为 nop-ai-core ai-defaults 的 ChatServiceImpl 按类型装配 IHttpClient 所需，与 nop-ai-core/coder 测试一致）；deepwiki `TestDeepWikiPrompts` 消费面 @Disabled 不变。
- **Phase 2（P3-MA1-013）**：裁定 = **保持文件持久化**（会话级工具：唯一调用方 SequentialThinkingBizModel、每会话数十条 ~1KB 数据、单 JVM ReentrantLock、nop-ai-tools 无 ORM 依赖面）；默认路径 `/nop/ai/sequential-thinking/store`（绝对路径不可写）→ `./_tmp/ai/sequential-thinking/store`（相对 CWD，`FileHelper.resolveFile` 语义；空配置仍回退 `~/.mcp_sequential_thinking`）。**测试暴露 2 个 live defect 就地修复**：① `ThoughtSession` 缺 `@DataBean` → `JsonTool.stringify` 必抛 `ERR_JSON_ONLY_DATA_BEAN_IS_SERIALIZABLE`（storage 写入从未成功）；② `java.time.Instant` 无 JsonTool 序列化器/转换器 → `ThoughtData.timestamp`/`ThoughtSession.lastUpdated` 改 epoch millis `long`（平台惯例同 `CheckpointSnapshot.createdAtEpochMillis`；`ThoughtAnalyzer` 消费点改 `Instant.ofEpochMilli(...)`；`CurrentThoughtInfo`（GraphQL 响应面）保持 Instant 不变）；`ThoughtSession` 补 setter（@DataBean round-trip 需要）。新测试 `TestThoughtStorage`（8 例：addThought 持久化到配置目录+磁盘 round-trip / 相对-绝对路径解析 / null-空回退用户主目录 / 会话隔离 / 阶段过滤+clearHistory / export-import round-trip / 未知会话空列表 / null sessionId 拒绝）+ `TestSequentialThinkingBizModel` 新增成功路径（原 storage 必抛错 → 成功路径从未跑通，现可端到端 processThought → JSON 落盘 → generateSummary）。裁定落盘 `ai-dev/design/nop-ai/03-sequential-thinking-storage.md`；`docs-for-ai/03-modules/nop-ai.md` 新增「工具配置（nop-ai-tools）」节（storage-dir-path + graphql-tool-names）。
- **Phase 3（P3-MA3-003）**：裁定 = **保留空模块 + 文档化为 SPI 预期实现落点**（选项 a）：与 P1-MA5-003 SPI 裁定（IVectorStore/IEmbeddingModel 无生产实现属设计意图）兼容；拒绝 InMemory 最小实现（零消费方投机代码 = Anti-Hollow 风险）；拒绝从父 pom 移除（结构声明价值 + 未来实现 churn，audit 建议即"placeholder + README 说明意图"）。落盘 `nop-ai-rag/README.md` + `ai-dev/design/nop-ai/04-rag-module-position.md`；`docs-for-ai/03-modules/nop-ai.md` 子模块表 nop-ai-rag 行更新。
- **arm-index**：新增「P3 追踪（第九批 — tools/rag 结构残余）」小节，登记 P3-MA1-013/016、P3-MA3-003 三行。
- **验证**：`./mvnw clean install -DskipTests -pl nop-ai -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-ai -am -T 1C` BUILD SUCCESS；`scan-hollow-implementations.mjs --module nop-ai --severity high` exit 0；`check-doc-links.mjs --strict` exit 0；`check-plan-checklist.mjs --strict` exit 0（closure audit 见 plan Closure 段）。

## Plan `2026-08-01-0206-1-arm-p2-skills-code-analyzer-structure` Phases 2-4 — FileLanguageStats 拆分 / 模块职责裁定 / deepwiki pom

- **Phase 2（P2-MA1-005 裁定 = 拆分）**：FileLanguageStats 零生产调用者/零直接测试。拒绝删除（516 行真实实现的 GitHub Languages API 兼容能力，销毁即损失）；拒绝接线（parser/coder 链无自然消费点，强行接线=人工耦合）。拆分 4 关注点：`IgnoredItems`（IGNORED_* 集合）、`FileTreeWalker`（遍历+跳过规则）、`CodeLineAnalyzer`（LineStats+注释检测）、`LanguageStatsAggregator`（LanguageStats/StatType/排序输出）；FileLanguageStats 降为编排入口 516→188 行（<300）。语义对比（归一化脚本）：LanguageStats/StatType/analyzeFileLines/detectComment/isBlockCommentLanguage/isStringLiteral/getStatValue 全部 IDENTICAL，getStatsByType 仅 getComprehensiveStats 调用外移。新测试 `TestFileLanguageStats`（5 方法直接实例化全值断言：Java 12 行/4 注释/6 代码、Python 5 行、build.pom→Maven 局部映射、字节数精确断言、GitHub API 降序字符串、target/.hidden/.log/隐藏文件排除、CodeLineAnalyzer 注释边界）。
- **Phase 3（P3-MA1-014 裁定 = 保持现状 + 边界文档化）**：live 使用面：code→maven→project→git 依赖链；maven 包（10 文件）零外部消费者，nop-shell 仅 MavenProject.java 使用；外部消费者只触 code 包（nop-ai-coder task XML JavaFileSplitter）与 git 包（nop-cli-core CliFileCommand 经传递依赖）。拒绝拆分理由：拆 maven 即 code↔maven 双向环（破环须迁 7/10 maven 文件进 core，拆分自溶解）；拒绝 nop-shell optional/provided（改变模块契约有运行时炸风险）。裁定落盘 `ai-dev/design/nop-ai/02-code-analyzer-module-boundary.md`（含使用约束 + 迁移触发条件），nop-ai/README.md 与 design/README.md 注册。
- **Phase 4（P3-MA1-015）**：deepwiki pom 移除 `nop-ai-code-analyzer`（生产+测试 0 引用），`nop-ai-tools` 改 `<scope>test</scope>`（仅 TestDeepWikiPrompts:10/:38 import FileToolBizModel）。依赖树验证：`nop-ai-tools:jar:...:test` direct，code-analyzer 仅经 tools 传递且 test scope。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-skills/nop-ai-code-analyzer -am -T 1C` BUILD SUCCESS（26 tests 0 failures，含 TestCodeFileInfoStructure 7 + TestFileLanguageStats 5）；`./mvnw test -pl nop-ai/nop-ai-skills/nop-ai-deepwiki -am -T 1C` BUILD SUCCESS；import 顺序：新建/触及文件 0 违规（模块内其余为既有基线）。
- **Doc-sync**: `ai-dev/design/nop-ai/02-code-analyzer-module-boundary.md` 新增（+README 注册）；`docs-for-ai/` 无 deepwiki/code-analyzer 引用，No owner-doc update required

## Plan `2026-08-01-0206-1-arm-p2-skills-code-analyzer-structure` Phase 1 — CodeFileInfo 拆分

- **拆分**：`CodeFileInfo.java` 590 → 216 行（<300）。7 个类型提取为同包顶层文件：`CodeSymbol.java`/`CodeClassInfo.java`/`CodeFunctionInfo.java`/`CodeCallInfo.java`/`CodeVariableInfo.java`/`AccessModifier.java` + 新增 `CodeSymbolInterning.java`（internString/internStringSet/internStringMap 静态工具，CodeFileInfo 及各类型委托调用）。`collectUsedFns()`/`trimPrivateForFunction()`/`isPrivateClass()`/`getClassInfo()`/`getFunctionInfo()` 留在 CodeFileInfo（依赖实例状态），行为不变。
- **消费者适配**：`JavaCodeFileInfoParser.java` 全部 25 处 `CodeFileInfo.X` 限定名 → 顶层类型（同包免 import）；`JavaCodeFileInfoGenerator.java` 2 处同步。全仓 grep `CodeFileInfo.X` 0 残留。`TestJavaCodeFileInfoParser`/`TestJavaCodeFileInfoGenerator` 经核验无嵌套类型引用，无需 import 变更。
- **契约核对**：python 脚本从 git HEAD 提取原嵌套类体，归一化（去 @DataBean/static/缩进/驻留委托差异）后与 6 个新文件逐 token 对比 —— 全部 IDENTICAL；CodeFileInfo 主类字段/方法集合与原文一致（token-multiset 除"移出的 interning 方法"与"私有方法前移"外 0 差异）。
- **新测试**：`TestCodeFileInfoStructure`（7 方法）：顶层类型断言（getEnclosingClass()==null）、继承关系（CodeClassInfo/CodeFunctionInfo extends CodeSymbol）、interning 引用相等性（顶层字段/symbol 字段/Map key-value/null 语义）、getClassInfo/getFunctionInfo 解析、JSON 序列化 round-trip（JsonTool.parseBeanFromText）。
- **验证**：`./mvnw test -pl nop-ai/nop-ai-skills/nop-ai-code-analyzer -am -T 1C` BUILD SUCCESS（21 tests 0 failures，2 @Disabled skipped）；import 顺序：新建/触及文件 0 违规（模块内其余违规为既有基线）。
- **Doc-sync**: No owner-doc update required（内部重构，无模块契约变化）

## Mission audit-remediation — 第九批 deferred successor 计划起草（3 plans → active）

- **背景**：audit-remediation roadmap 全部工作项 done（M0-MG + 8 批 deferred successors closed，MV 矩阵 open=0）。本轮从先前计划 Deferred But Adjudicated / Non-Blocking Follow-ups 段重开剩余高价值 P2/P3 项，起草 3 个计划并注册为第九批。
- **Plan 1** `2026-08-01-0206-1-arm-p2-skills-code-analyzer-structure.md`（active）：P2-MA1-004/005（CodeFileInfo 589 行/7 类型拆分、FileLanguageStats 515 行拆分或删除/接线裁定——live 核实 stats 包零调用者）+ P3-MA1-014（code-analyzer 职责裁定，maven 包 10 文件、nop-shell 仅 MavenProject 使用、GitIgnoreFile 有 nop-cli-core 外部消费者）+ P3-MA1-015（deepwiki pom：nop-ai-code-analyzer 移除 + nop-ai-tools 改 test scope——TestDeepWikiPrompts.java:10 import FileToolBizModel 属测试面）
- **Plan 2** `2026-08-01-0206-2-arm-p2-tools-structure-residual.md`（active）：P3-MA1-016（nopGraphQLToolSet 条件注册——Nop IoC 正确语法 `<ioc:condition><on-bean>nopGraphQLEngine</on-bean></ioc:condition>`，audit 原建议的 `ioc:condition="!missing-bean:..."` 属性语法在 beans.xdef 不存在）+ P3-MA1-013（ThoughtStorage 持久化裁定，live 证据：默认路径 `/nop/ai/sequential-thinking/store` 为文件系统绝对路径不可写）+ P3-MA3-003（nop-ai-rag 空模块裁定）
- **Plan 3** `2026-08-01-0206-3-arm-p3-test-quality-residual.md`（active）：MA4.3-09/13（TestDefaultPermissionMatrix + TestDbSessionTakeoverLockDualInstanceE2E 值级断言升级；ContentTrustEvaluator 裁定保持——`isTrustedSource()` 返回 boolean 无 TrustLevel 枚举，Suggestion 段仅点名 2 文件）+ MA4.4-01/02/03（冗余/round-trip/枚举测试裁定）+ MA4.2-06（TestMultiMemberFanOut 1139 行/18 @Test、TestScheduledRecoveryManager 1076 行/36 @Test 拆分）
- **对抗性审查**：3 轮独立子 agent 复审（每计划 ≥2 个 fresh session）。第一轮各发现 1 Blocker（P1: deepwiki test-compile 依赖面；P2: ioc:condition 语法不存在 + bean id 为 nopGraphQLEngine；P3: TrustLevel 枚举不存在），全部修复后第二轮 P1/P3 达成共识（可直接执行），P2 仍有 1 Major（接线载体 NopAiWebPagesTest 依赖面不含 nop-ai-tools → 改为 nop-ai-tools 自建容器测试 + TestDeepWikiPrompts 消费面证据）；第三轮全部通过（无 Blocker/Major）。
- **roadmap**：`ai-dev/backlog/audit-remediation-roadmap.md` 更新至 v8——第九批 3 行登记（🟡 active 2026-08-01）+ 尾部 watch-only 说明同步；`check-doc-links.mjs --strict` exit 0。
- **Commit**: 未提交（计划起草阶段，执行时随代码提交）

## Plan `323-flux-web-container-body-rendering` Closure — tabs 字段名修复 + tab/step/group body 渲染 + wizard/group 页面类型

- **Plan 关闭**：Plan Status → completed（2026-08-01）；Phase 1-4 全部 completed、Closure Gates 全勾选；`check-plan-checklist.mjs --strict` exit 0；独立 closure audit（fresh session `ses_046b3d7b4ffeL8PTP50RNw4YaW`）verdict APPROVE——见 plan Closure 段
- **Phase 1（tabs 字段名 + body 渲染）**：
  - `flux-web.xlib` 新增内部标签 `GenContainerModel`（outputMode=xjson，页面级/body 级共用容器分派：crud/simple/tabs/wizard/group，otherwise 抛 `nop.err.web.unknown-page-type`）
  - 新建 `flux-web/container_tabs.xpl`（无 page 外壳 tabs 核心）、`container_crud.xpl`（`const pageModel = containerModel` 后复用 `grid_crud.xpl` 无外壳片段）、`container_simple.xpl`（复用 `GenFormBody` 的表单容器，submit/load 语义对齐 page_simple.xpl）
  - `page_tabs.xpl` 重写：`<tabs j:list="true">` → `<items j:list="true">`（Flux `TabsSchema` 字段名 live defect 修复——原输出 `tabs` 字段在 Flux 下 silent no-op）；tab 项 `key` ← `name`（`FluxTabDefaultAttrs` 补 key）；tab body 优先级 `page` > `body`(GenContainerModel 分派) > `name` 兜底，`<body j:list="true">` 保证输出数组
  - `GenLayoutTabs`（form 级 tabs）live defect 修复：tab 项内容 `<tab j:list="true">` → `<body j:list="true">`，补 `<key>`（原内容写在 `tab` 字段，Flux nestedRegions 只提取 `items[].body` → 静默丢失）
  - 测试：`test-flux-tabs.view.xml`/`.page.yaml`/`.xmeta`（page tab 引用 `add` 页面 + body tab 内嵌 simple 容器；`edit` form `layoutControl="tabs"`）；`TestFluxWebGen` +4 测试（page tab items/key、body tab 容器分派、form 级 tabs items[].body、防御性 throw `nop.err.web.unknown-page-type`）
- **Phase 2（wizard 页面类型）**：`impl_GenPage.xpl` wizard 分派；`page_wizard.xpl`（page 外壳——wizard 模型不继承 UiPageModel，外壳仅 name）+ `container_wizard.xpl`（steps 核心：step 优先级 page>body>name、steps[].key←name）；属性映射按设计 §3.3（mode/action*Label 透传；startStep 暂不映射；className/actionClassName/initFetch/initFetchOn/initApi/reload/redirect/target 丢弃）；`GenContainerModel` 增 wizard 分支；`test-flux-wizard.*`（3 step：body simple、body crud、page 引用）+ 测试
- **Phase 3（group 页面类型）**：`impl_GenPage.xpl` group 分派；`page_group.xpl`（page 外壳）+ `container_group.xpl`（GridSchema 核心：columns/gap 透传、autoFlow 枚举映射 `row-dense`→`row dense`、alignItems/justifyItems 过滤 normal/baseline、responsiveColumns 不输出、body 子容器包 `items[].body`、colSpan/rowSpan 经 `DynamicObject.prop_allow` 存在性守卫透传（simple/wizard/group 无 span 字段）、items[].key←name）；`GenContainerModel` 增 group 分支；`test-flux-group.*` + 测试
- **实现要点**：xjson 输出 `<body j:list="true">` 强制数组（无 j:list 时单元素 body 会被 CollectJObjectHandler 折叠为对象）；页面级 body 保持既有折叠语义（测试用 `toBodyList` 归一化）；wizard/group 模型非 UiPageModel，页面外壳不可用 `FluxPageDefaultAttrs`（DynamicObject 未知属性抛错），仅输出 name
- **Phase 4（文档）**：`flux-rendering.md` 新增「页面级容器：tabs/wizard/group 的 Flux JSON 映射」章节（items/steps 字段名、body 数组直接书写、key 映射、page>body>name 优先级、complex/picker 不支持说明）；`layout-syntax-reference.md` §7.1 页面级容器语法 + body 内嵌示例 + page/body 优先级；`source-anchors.md` EXT-009 更新（28 标签 + GenContainerModel + 容器类型）；`check-doc-links.mjs --strict` 0 errors
- **验证**：`./mvnw test -pl nop-frontend-support/nop-web -am` 63 例 0 failures（TestFluxWebGen 17 例含 6 个新测试）；端到端 dump 验证 tabs 输出 `{type:'page', body:{type:'tabs', items:[{key,title,body:[...]}]}}` 与 Flux `TabsSchema` 契约一致；`scan-hollow-implementations.mjs --module nop-frontend-support/nop-web --severity high` exit 0（0 findings）；`check-plan-checklist.mjs --strict` exit 0
- **偏差**：无实质偏差——① 防御性 throw 测试需在 scope 中预置 GenContainerModel 的 5 个 implicit attrs（否则先抛 `tag-no-implicit-attr`）；② wizard 页面外壳因模型非 UiPageModel 改为仅 name 属性（design §3.3 的"包 page 外壳"语义不变）；③ audit 发现 `TestFluxWebGen` 注释位置 cosmetic 问题，已修正
- **roadmap**：plan 323 为 design-sourced 独立特性 plan（无 `> Work Item:` label），`ai-dev/backlog/` 各 roadmap 无对应条目，无需翻转 ❌/✅
- **Commit**: 见 git log（本条目当日提交）
