# 2026-09-21

### nop-lint plan "RuleTester" 独立 closure audit：APPROVED，roadmap item 10 翻 `done`

- 独立 auditor（fresh closure visit，opencode-pid-60889，model zhipuai-coding-plan/glm-5.3-flash）：CLOSURE_SCRIPT_CHECK 报 4 项（`ledger-structure-invalid` + `frontmatter-fields-invalid` + `missing-pass:test` + `no-audit-receipt`），本 visit 全部消解——`## Closure Gates` 9 个列 0 checkbox 为计数域外违规（01-file-ledger §2.5），按 §4.3 消解先例改为纯 prose（同 plan 1420-1 处置）；frontmatter 移除 ledger 子集外的 `last-reviewed` 字段；`## Verification` 补 pass 行、`## Closure` 补 dispatch/accepted 收口对（#audit-2026-09-21-142035-mission-driver-2026-09-21-2137-1-rule-tester-1-ae0c5dfd，含 `models={exec,aud}` lineage 后缀）。
- **亲测复跑**全套验证命令：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0、`clean package -DskipTests` exit=0、`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS（**196 tests**，0 failures，含 TestRuleSuites 2 / TestRuleTestRunner 7 / TestRuleTestRunnerFailures 11 / TestExpectParser 20）；mission `commands.lint` 按配置 exit=0（裸 checkstyle 的 nop-api-core 9226 处违规为全仓遗留基线，裁定记录于 plan `## Verification`）。环境注记：首次 test 运行因并行会话 Maven `target/` 竞争出现 nop-treesitter `NoClassDefFoundError`（环境缺陷非 plan 缺陷），等待 `.mvn/build.lock` 队列后复跑全绿。
- Anti-Hollow 通过：`TestRuleSuites`（`@TestFactory`）在本 visit 随 `./mvnw test` 真实执行；`RuleTestRunner` 运行时真实调用 `RuleDslParser.loadRuleModel`（:119）与 `new LintEngine(registry, profile)`（:185），`.rule.yml` → VFS 加载 → LintEngine → 行号断言一条链连通；meta-test（line-mismatch 行=99 错位）由启动器断言检出，runner 非恒真；缺 `.expect`/空 diagnostics/`fix` 字段/杂散文件全部 fail-closed，无静默跳过。
- 语义复核：30/30 计数域 checklist 与 live repo 一致（2 绿样例套件 + 9 破坏套件、design 03 §4.1/§4.2 增注在案）；Non-Goals 分类诚实（`fix` 期望/抑制夹具/CLI/Maven 出口分别指向 items 25/17/39/37，无 in-scope defect 降级）。
- Roadmap 回写：item 10 `planned` → `done`（closure audit approved）；item 11 保持 `todo`、M2 保持 `todo`（10 已 done、11 未 done，派生状态未受扰动）。frontmatter `status: active` 不写 `completed`（ledger 派生状态）。

### nop-lint plan "RuleTester" Phase 3 执行：样例套件端到端 + meta-test + roadmap 回写（roadmap item 10）

- 端到端验证（Minimum Rules #22）走通：样例套件 `demo/no-console` / `demo/no-dynamic-dispatch`（pattern 规则 + valid/invalid + `.expect`，含跨行节点 line=6/endLine=7 断言）经 `TestRuleSuites`（`@TestFactory`）在 `./mvnw test` 下全绿——`.rule.yml` → VFS 加载（xdef 校验 + x:extends 管线）→ `LintEngine.lint`（真实解析树）→ 行号断言，一条链无手工步骤；`engineOutputOnInvalidFixtureMatchesTheSuiteExpectation` 交叉验证引擎原始输出与套件期望一致（Phase 1 接线验证项随之闭合）。
- meta-test（防空壳验证面）：`metaTestRunnerDetectsShiftedLineExpectation` 使用故意错位（真实 line=5 → 期望 line=99）的 `.expect` 夹具，断言 runner 报出 `no actual diagnostic matches expectation {line=99...}`——证明断言层非恒真。
- Roadmap 回写：item 10 `todo` → `planned`（draft review 已通过；`done` 待独立 closure audit，按 plan Phase 3 两步协议执行）；核对 item 11 保持 `todo`、M2 保持 `todo`（10/11 未 done，派生状态未受扰动）。
- 验证：`-pl nop-lint/nop-lint-core -am test -T 1C` 回归全绿（**196 tests**，0 failures）；`check-doc-links.mjs --strict` 退出码 0（见下条收口记录）。
- Doc-sync: roadmap item 10 + plan 文件（Phase 1/2/3 checklist 全勾、三 Phase status=completed）+ 本日志。plan frontmatter `status: active` 保持（ledger 派生状态，`completed` 由下游 BUILD_VERIFY/CLOSURE_AUDIT 记录后派生）。

### nop-lint plan "RuleTester" Phase 2 执行：RuleTestRunner 扫描/执行/断言 + JUnit 启动器（roadmap item 10）

- 新增 `io.nop.lint.core.testing.RuleTestRunner`：VFS 约定目录 `/test/lint/suites/<category>/<rule-name>/`（`<rule-name>.rule.yml` + `valid/*.java` + `invalid/*.java` 同名 `.expect`）→ `RuleDslParser.loadRuleModel` 真实加载 → `LintEngine.lint` 真实执行 → valid 零诊断断言 + invalid 逐条匹配（ruleId/line/endLine/messageContains，顺序不敏感）；`LineIndex` 将 `Diagnostic.range` UTF-8 字节区间按「起止字节各自落行」映射为 1-based 行号（字节扫描计数 `\n`，多字节字符安全，已单测）。配套 `SuiteResult`/`FixtureFailure` 值对象（失败消息含规则 id、夹具路径、期望 vs 实际对照）。
- fail-closed 守卫全部落地：缺 `.expect`、空 `diagnostics`、套件零夹具、fixture 目录杂散文件、缺规则文件、条数不匹配、字段不匹配——全部显式失败，无静默跳过；发现层（category/suite 级杂散文件、空路径）抛 `NopLintException`。
- JUnit 启动器 `TestRuleSuites`（`@TestFactory` 动态测试）：新增套件目录即自动随 `./mvnw test` 执行，无需改 Java 代码。测试绑定 `JavaBindingTestSupport`（java grammar 绑定注册为 id `java`，与 TestLintEngine 同型——core 模块 classpath 无法依赖 nop-lint-java）。
- 夹具：绿色样例套件 2 个（`demo/no-console`、`demo/no-dynamic-dispatch`，后者含跨行节点 line=6/endLine=7 断言，实证行号换算 Decision）；故意破坏套件 9 个置于 `_vfs/test/lint/broken-suites/`（在启动器扫描路径之外），覆盖全部失败路径 + meta-test 夹具（line=99 错位）。
- 测试 40 个新增（TestExpectParser 20 / TestRuleSuites 2 / TestRuleTestRunner 7 / TestRuleTestRunnerFailures 11），其中 `engineOutputOnInvalidFixtureMatchesTheSuiteExpectation` 直接对照引擎原始输出与套件期望（Minimum Rules #23 接线验证）。
- 偏差：`.expect` 与 `.java` 同居 `invalid/` 目录，fixture 扫描需放行 `.expect` 同名文件（首版扫描过严，测试暴露后修正）；其余无偏差。
- 验证：`test-compile`/`clean package -DskipTests` 退出码 0；`checkstyle:check` 仍为全仓遗留基线（nop-api-core 9226，mission `lint` 键按配置兜底）；`-pl nop-lint/nop-lint-core -am test -T 1C` 全绿（**196 tests**，0 failures）。
- Doc-sync: design 03 §4.1（VFS 目录布局裁定 + fail-closed 约定 + suppressed 夹具归属 item 17）+ plan 文件 + 本日志。

### nop-auth plan 2274 执行：Phase 1-5 全部落地（集群支持 + MFA 大类拆分）

- **Phase 1 限流组件化**：`ISendCodeRateLimiter`（scope=login|bind 等价原 7 Map 共享拓扑）+ `LocalSendCodeRateLimiter`（先递增后检查，32 线程并发突发恰放行 1 个的原子语义平移自 `TestLoginRateLimitAndAudit`）+ `RedisSendCodeRateLimiter`（单键原子 INCRBY+PEXPIRE / SETNX+PX 间隔门，FakeNosqlService 验证跨实例共享）+ `SendCodeRateLimiterProvider`（collect-beans 复刻 MfaStoreProvider，`nop.auth.rate-limit.store-type` 默认 local，fail-closed）；`LoginServiceImpl` 4 Map 与 `NopAuthUserBizModel` 3 Map 及重复限流方法全删；错误 param 统一脱敏 + email key 归一 `ARG_CHANNEL`（design §3.1 唯一用户可见变化）。
- **Phase 2 失败计数与缓存出口**：`ILoginAttemptStore`（接口级原子递增）+ `LocalLoginAttemptStore`（锁内聚）+ `RedisLoginAttemptStore` + provider（`nop.auth.login-attempt.store-type`）；Local bean 注册进 auth-core-defaults（auth-core-only 部署可装配）；`AbstractUserContextCache` 六个失败计数方法委托 store、`getLoginFailCountForIp` 读写前缀缺陷修正（Fix）、verifyCodeCache 注入出口（TTL 保持契约）；`loginFailCountLock` 消灭；缺省路径写读同一性（service 解析自 wired cache 的同一 store 实例）。
- **Phase 3 LoginServiceImpl 拆分**：`LoginMfaFlow`（门禁三层判定/第二因子验证/恢复码条件写/completeLogin 回调端口接缝）+ `MfaCodeSender` + `MfaWebauthnSupport`/`MfaRequests`/`MfaContacts` 单一落点；1556→**959 行**。
- **Phase 4 BizModel Processor 化**：`UserMfaSelfService`（bindMfa 状态机/WebAuthn 三 ceremony/恢复码/可信设备/proof/TOTP 冷却；daoFor→IDaoProvider、txn()→ITransactionTemplate 接缝改写）+ `MfaAccessGuard` 共享落点；2068→**618 行**，21 个 @Biz 注解薄入口签名不变（API 面不变）。
- **验证**：`mvnq -- -pl nop-service-framework/nop-biz-auth-core,nop-auth/nop-auth-service -am test -T 1C` BUILD SUCCESS——nop-biz-auth-core 52 tests + nop-auth-service 447 tests（0 failures 0 errors，3 skipped）；MFA E2E 全套零断言修改通过；测试重接 4 处（§3.6 精确语义）+ FakeNosqlService.remove 补计数键空间语义（对齐真实 Redis DEL）。
- **Phase 5 文档**：`docs-for-ai/03-modules/nop-auth.md` 新增"集群部署（多实例）"矩阵（会话/MFA store/限流/失败计数/验证码缓存 TTL 契约/jwt encKey）+ 新配置项两条 + 本地只读缓存归类；check-doc-links --strict 退出码 0。
- 偏差：测试重接与 param 断言变化均按 design §3.6 预授权清单执行；Redis 间隔门从"首日 putEx 覆盖"简化为"恒 SETNX"（消除 INCR/putEx 双写竞态，行为仅午夜边界更严格）。
- Doc-sync: owner doc 已更新（上）；design/README 此前已更新。

### nop-lint plan "RuleTester" Phase 1 执行：`.expect` 解析与格式裁定（roadmap item 10）

- 新增 `io.nop.lint.core.testing` 包：`ExpectParser`（`.expect` YAML → `ExpectModel`，fail-closed 结构校验：缺/空 `diagnostics` 列表、未知字段、非法行号 `<1`/非整数/`endLine<line`、非映射条目、空 ruleId/messageContains 全部抛 `NopLintException`，英文消息含夹具路径）+ `ExpectDiagnostic`/`ExpectModel` 值对象。
- **Decision 落地（回写 design 03 §4.2 增注）**：`line`/`endLine` 为 1-based 源码行号，`Diagnostic.range` 字节区间按「起止字节各自落行」换算（`startLine`=startByte 所在行、`endLine`=endByte-1 所在行，end 开边界），`endLine` 缺省等于 `line`；v1 拒绝面：`.expect` 出现 `fix` 期望字段抛错并指向 roadmap item 25（autofix），suppression 类夹具归 item 17，不静默忽略。
- 测试 `TestExpectParser`（20 个）：合法解析（全字段/endLine 缺省/多条目保序）+ 全部拒绝路径（结构、未知字段、entry/顶层 `fix`、行号五形态、字段缺失）。
- 偏差：无（`endLine` 可缺省为单行断言属 design 增注内显式裁定，非计划外）。
- 验证：`test-compile`/`clean package -DskipTests` 退出码 0；`checkstyle:check` 仍为全仓遗留基线不通过（nop-api-core 9226，mission `lint` 键按配置 `|| echo 'lint not configured'` 兜底）；`-pl nop-lint/nop-lint-core -am test -T 1C` 全绿（**176 tests**，0 failures，含新增 20）。Phase 1 接线验证项（解析器被 runner 消费）按其自身定义待 Phase 2/3 落地后勾选。
- Doc-sync: design 03 §4.2（v1 裁定增注）+ plan 文件 + 本日志。

### nop-lint plan "L1 DeclTypeResolver" 独立 closure audit：APPROVED，roadmap item 12 翻 `done`

- 独立 auditor（fresh session，opencode-pid-33030）复核 plan `2026-09-21-1420-2-decl-type-resolver-l1`：**亲测复跑** `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` exit=0（156 tests, 0 failures）+ `test-compile`/`clean package -DskipTests` exit=0；mission `commands.lint` 按配置 exit=0（裸 checkstyle 的 nop-api-core 9226 处违规为全仓遗留基线，与本 plan 无关，裁定记录于 plan `## Verification`）。
- Anti-Hollow 通过：`DeclTypeResolver` 真实实现（无空方法体/静默跳过）；接线验证由 `resultSlicesMatchSourceBytes` 证明——真实 tree-sitter java 解析树上 resolver 输出与源码 UTF-8 字节切片逐字符一致；11 个焦点测试覆盖四类声明形态 + 全部失败路径。
- 结构修复：plan `## Closure Gates` 10 个 checkbox 转记叙文（计数域纪律，01-file-ledger §2.5，与 plan 1 收口同型）；`## Verification` 写入 pass 行（键 `test`）；`## Closure` 写入 dispatch/accepted 收口记录（#audit-…-decl-type-resolver-l1-1-5d29ec7e）。`plan-check.mjs --strict` derivedCompleted=true。
- 语义复核：Phase 1/2 Exit Criteria 逐条对照 live repo（组件/测试/design 06 §5.2 契约/日志）均成立；deferred 项分类诚实（AnnotationParser 登记含触发条件，无 in-scope defect 降级）；`check-doc-links.mjs --strict` 0 error。
- Roadmap 回写：item 12 `planned` → `done`（closure audit approved）；M2 剩余 10/11 `todo` 未受扰动。

### nop-lint plan "L1 DeclTypeResolver" Phase 2 执行：消费契约收口 + roadmap 回写（roadmap item 12）

- **Decision 落地**：design 06 §5.2 L1 小节写入消费契约最终形态（一句话）：单方法 API `resolveDeclType(LintNode)`——输入带 `type` 字段的声明节点，输出书写文本保真的类型字符串；非声明节点/无 type 字段/null 入参返回 null（显式无法提取契约）；`var` 返回字面 `"var"`；item 14（xscript）与 L1 类规则直接注入调用。同处更正示意偏差：`child("type")` → `childByField("type")`（LintNode 实际 API）。
- **Follow-up 登记（AnnotationParser 缺口）**：design 06 §4.6 L1 框图另一半（注解类型/注解语义提取）roadmap 无属主 item，且当前无规则消费注解类型——已显式登记于 plan `## Closure` Follow-up，触发条件挂在首个注解语义规则项（Wave 4–5 规则库批次，items 29/35 方向），非静默假设、不在本 plan 降级。
- Roadmap 回写：item 12 `todo` → `planned`（draft review 已通过、两 Phase 执行完毕；`done` 待独立 closure audit）；核对 M2 剩余项未受扰动（9 done / 10 todo / 11 todo）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 error，11 warning 均为其他 plan 文件预先存在）；`-pl nop-lint/nop-lint-core -am test -T 1C` 回归 BUILD SUCCESS（156 tests）。
- Doc-sync: design 06 §5.2（消费契约 + 示意更正）+ roadmap item 12 + plan 文件 + 本日志。

### nop-lint plan "L1 DeclTypeResolver" Phase 1 执行：声明类型提取组件落地（roadmap item 12）

- 新增 `io.nop.lint.core.type.DeclTypeResolver`：单方法 API `resolveDeclType(LintNode)`，按 `childByField("type")` 取声明节点的书写类型文本（四类声明形态：field_declaration / local_variable_declaration / method_declaration 返回类型 / formal_parameter），无 type 字段或非声明节点（含 null 入参）返回 null，无任何规范化/推断。
- **实测修正示意偏差**：design 06 §5.2 示意用 `child("type")`——`LintNode` 实际 API 是 `childByField(String)`（无 `child(String)`）；`var x = 1;` 在 tree-sitter-java 中 `var` 本就占据 `type` 字段（`type_identifier:var`），故"var → 字面 var"由通用字段提取自然满足，无需特判分支（先用探针测试实测解析形态再定实现）。
- 测试 `DeclTypeResolverTest`（11）：局部变量（String/List<String>/String[]/var）、字段（Map<String, Integer>）、方法返回（void/int/List<String>）、形式参数（java.util.Map/List<String>/List<?>）、非声明节点（block/expression_statement/method_invocation/program root）返回 null、无 type 字段声明（class_declaration/variable_declarator）返回 null、null 入参返回 null、`resultSlicesMatchSourceBytes` 接线测试（resolver 输出与源码 UTF-8 字节切片逐字符比对 + verbatim 出现断言，防 off-by-one）。
- 验证：`test-compile`/`clean package -DskipTests` 退出码 0；`checkstyle:check` 在上游模块（nop-api-core 9226 等预先存在违规，含本模块既有文件）即失败——门禁未启用，记 `lint not configured`，与本 plan 无关；`-pl nop-lint/nop-lint-core -am test -T 1C` 全绿（**156 tests**，含新增 11）。
- Doc-sync：design 06 §5.2 L1 示意更正（`child("type")` → `childByField("type")`）+ 消费契约一句话，归 Phase 2 执行（见下条）。

### nop-lint plan "LintEngine 最小引擎" 独立 closure audit：APPROVED，roadmap item 9 翻 `done`

- 独立 auditor（fresh closure visit，opencode-pid-27958，model zhipuai-coding-plan/glm-5.3-flash）：CLOSURE_SCRIPT_CHECK 报 3 项（`ledger-structure-invalid` + `missing-pass:test` + `no-audit-receipt`），本 visit 全部消解——`## Closure Gates` 11 个列 0 checkbox 为计数域外违规（01-file-ledger §2.5），按 §4.3 消解先例改为纯 prose（同 2026-09-14-1638-3-p2-engine-hygiene 处置）；`## Verification` 补 pass 行、`## Closure` 补 dispatch/accepted 收口对（#audit-2026-09-21-142035-mission-driver-2026-09-21-1420-1-lint-engine-minimal-1-8db25918）。
- 验证（本 visit 亲测）：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` exit=0（mission `test` 键）；`./mvnw -pl nop-lint/nop-lint-java -am test -T 1C` exit=0；`plan-check.mjs --strict` exit=0（36/36 勾选，derivedCompleted=true，frontmatter `status: active` 保持不写）；`scan-hollow-implementations.mjs --module nop-lint/nop-lint-core --severity high` exit=0；`check-doc-links.mjs --strict` 0 error；roadmap-check exit=0。
- Anti-Hollow 复核通过：LintEngine → CompiledRule.compile（xscript/regex/未知 kind/缺 matcher 全 fail-closed）→ SourcePattern.matchIn → Diagnostic 运行时连通；kind 过滤在匹配调用前短路（RuleSetRunner.run 先 `canMatchKinds` 再 match）；skippedByProfile 携带规则 id；ServiceLoader 真实发现由 `JavaLanguageDiscoveryTest` 证明。
- lint 裁定：mission `commands.lint` 按配置 exit=0（`|| echo 'lint not configured'` 兜底为 mission 显式降级）；裸 `checkstyle:check` 全仓遗留基线不通过（nop-api-core 9226 / nop-lint-core 680 / nop-lint-java 9，style-only，含已关闭 plan 02–06 时代的文件），与本 plan 无关且 roadmap 终态即以 nop-lint 替代 checkstyle——裁定记录于 plan `## Verification`，不作为本 plan 完成公式项。
- Roadmap 回写：item 9 `planned` → `done`（closure audit 通过）；M2 保持 `todo`（10/11/12 未完成，13 已 done）。frontmatter `status` 不写 `completed`（ledger 派生状态）。
- Doc-sync: plan 文件（Closure Gates 消解 + Verification pass 行 + Closure 收口对）+ 本日志；No owner-doc update required（三 Phase Exit Criteria 已各自裁定，管线/能力集与 design 03 §1.1、design 11 §8 无偏差）。

### nop-lint plan "LintEngine 最小引擎" Phase 3 执行：LintEngine 档位执行 v1 + skippedByProfile（端到端收口，roadmap item 9）

- 新增 `LintProfile`（FAST/STANDARD，v1 能力集均为 `{L1}`，design 11 §8 Phase 1 口径；`LintCapability` 枚举 L1/L2 供 items 14/15/17/25/26 扩挂，未知 requires token 不可满足→skip）、`LintEngine`（registry+profile 构造注入；三个 lint 入口：languageId/String、binding/String、binding/LintTree；管线顺序=design 03 §1.1 v1 子集：语言解析→requires 门槛→编译矩阵→kind 过滤→匹配→Diagnostic→stats）、`LintResult`（diagnostics+stats，design 03 §2.3 去 fix 面）。**Decision：skipped 规则 id 进入 `LintStats.skippedRuleIds`**（有序不可变列表，可观测不静默）。
- 端到端测试（TestLintEngine，10 个）：`valid-any.rule.yml` → loadRuleModel → `lint(rules,"Java",src)` → 2 条 Diagnostic 断言 ruleId/severity/message/range（命中源）；`foo.bar()` 源=执行但零命中（rulesExecuted=1）；无 method_invocation 源=kindFiltered=1；fast/standard 同规则集结果逐项相等；valid-full（requires L1,L2+xscript）两档均 skippedByProfile=1 且 id 可观测——同时证明 requires 门槛先于 xscript 编译拒绝；未知 requires token（dataflow）不可满足；未知语言 id 抛错含原始值；大小写归一端到端（"Java"≡"java"）。
- **JMH follow-up 裁定（承接 plan 06 Non-Blocking Follow-ups，只裁定不实施）**：结论=**不补**规则加载口径 JMH 基准。理由：加载是每进程一次性成本且 design 11 §4 已裁定 CompiledRule 缓存待性能证据立项；当前无 CLI/常驻消费方（item 18 未落地）与规则集规模，无证据支撑测量口径；已写入 `nop-lint/docs/perf-baseline.md` §后续裁定记录（含复核触发条件与分类 `optimization candidate`）。
- Roadmap 回写：item 9 `todo` → `planned`（draft review 已通过、三 Phase 执行完毕；`done` 待独立 closure audit）；M2 剩余项 10/11/12 状态核对未受扰动。
- 验证：`test-compile`/`clean package -DskipTests` 退出码 0；checkstyle nop-lint 源 0 违规（上游预先存在违规记 `lint not configured`）；`-pl nop-lint/nop-lint-core -am test -T 1C` 全绿（**145 tests**，含新增 10）；`-pl nop-lint/nop-lint-java -am test -T 1C` 全绿（9 tests）。
- Owner-doc 裁定：管线顺序与 design 03 §1.1 一致（v1 子集：约束[P2]/xscript/抑制三类环节按 plan 裁定 fail-closed 或 v1 空集，无表述偏差）；能力集与 design 11 §8 一致；No owner-doc update required（本裁定记录于本条）。

### nop-lint plan "LintEngine 最小引擎" Phase 2 执行：规则编译与 kind 位过滤（roadmap item 9）

- 新增 `CompiledRule`（形态执行矩阵 v1：pattern/kind/any 执行，分支 pattern+kind 合取；regex 与 xscript **编译期 fail-closed**，英文消息含规则 id+原因+successor item；未知 kind 引用、缺 severity/message 同样拒绝）、包内 `KindIndex`（单次先序遍历收集文件 kindId 出现集合）与 `RuleSetRunner`（kind 过滤→匹配→Diagnostic 管线尾；`rulesKindFiltered` 与 `rulesExecuted` 每规则互斥=过滤先于匹配的可观测证明）。
- **实测发现并修正夹具语义缺陷（plan 06 时代的隐性偏差）**：匹配内核遵循 ast-grep 大写元变量约定，`$obj`/`$name`/`$arg` 小写名编译为**字面文本 Terminal**（MetaVarSyntax 仅大写为 meta-var），故 `valid-any`/`valid-simple`/`base-rule` 夹具按原文本永远无法真实命中。Phase 3 端到端要求夹具产出 Diagnostic，遂将三夹具元变量改大写（`$OBJ`/`$NAME`/`$$$ARGS`），同步更新 plan 06 两测试类的 5 处字面文本断言（parser 只做往返，不受影响）。valid-full 的 `new FileInputStream($f)` 不动（该夹具本就是 xscript fail-closed 测试载体）。
- 测试：`TestCompiledRule`（13：矩阵执行格 4+夹具并集+meta-var 无 kind 意见+拒绝格 5+canMatchKinds+防御拷贝）、`TestRuleSetRunner`（3：短路互斥计数/全过滤/诊断携带 ruleId+node range）。
- 验证：`test-compile`/`clean package -DskipTests` 退出码 0；checkstyle nop-lint 源 0 违规（上游预先存在违规记 `lint not configured`）；`-pl nop-lint/nop-lint-core -am test -T 1C` 全绿（135 tests）；`-pl nop-lint/nop-lint-java -am test -T 1C` 全绿。
- Owner-doc 裁定：形态矩阵与 design 10/04 DSL 语义无偏差（合取/并集语义按 design 04 §2 实现且测试实证），No owner-doc update required。

### nop-lint plan "LintEngine 最小引擎" Phase 1 执行：值模型与语言发现（roadmap item 9）

- 新增 `io.nop.lint.core.engine` 包：`Diagnostic`（record，4 字段非空校验）、`LintStats`（builder 形态，含 rulesLoaded/rulesExecuted/rulesSkippedByProfile/rulesKindFiltered/diagnostics + skippedRuleIds 有序 id 列表）、`LanguageRegistry`（ServiceLoader 发现 + 手动注册 + 按 id 解析）。**Decision：id 归一 = trim + Locale.ROOT 小写**（夹具 `Java` vs 绑定 `java` 的对齐陷阱）；blank/未知 id 显式抛 `NopLintException`（英文消息含原始值）；同 id 冲突注册 fail-closed（同实例幂等）。
- nop-lint-java 新增 `META-INF/services/io.nop.lint.core.lang.LintLanguage`；`JavaLanguage` 构造器改 public（实例共享一个 static `TreeSitterLanguageAdapter`，grammar blob 每 JVM 仍只解码一次）。**平台实测修正**：JDK 26 `ServiceLoader` 对 classpath（unnamed module）provider 只认 public no-arg 构造器，静态 `provider()` 工厂路径仅对 named module 生效（`ServiceLoader.java:774 inExplicitModule` 判定）——最初用 provider() 方案实测失败，已回退为 public 构造器 + 共享 adapter 方案。
- core 测试新增：`StubTestLanguage`（test-only ServiceLoader provider，java blob + 合成 id "stub"，无 core→java 依赖）+ `TestEngineValueModels`（7）+ `TestLanguageRegistry`（8）；java 模块新增 `JavaLanguageDiscoveryTest`（2，接线验证：ServiceLoader 真实发现 + 发现实例与 `get()` 共享同一 `Language` 后端）。
- 验证：`test-compile` 退出码 0；`clean package -DskipTests` 退出码 0；`checkstyle:check` 仅有 nop-api-core 等上游模块**预先存在**违规（nop-lint 源文件 0 违规，门禁未启用记 `lint not configured`）；`-pl nop-lint/nop-lint-core -am test` 全绿（119 tests，新增 15）；`-pl nop-lint/nop-lint-java -am test` 全绿（9 tests，新增 2）。
- No owner-doc update required（语言发现机制为 plan 02 移交本 plan 的首次定义；使用契约记录于本条与 javadoc：单进程应择一使用 discovery 或手动注册路径，勿混用——同 id 不同实例会按 fail-closed 拒绝）。

### nop-auth 集群支持分析与修正计划立项（plan 2274）

- **产出**：`ai-dev/analysis/2026-09/2026-09-21-nop-auth-cluster-support-and-mfa-split-analysis.md`（resolved，独立核实 11/11 PASS、行号零偏差、无实质遗漏）+ `ai-dev/design/nop-auth/03-cluster-support-and-structure-design.md`（新）+ `ai-dev/plans/2274-nop-auth-cluster-support-and-mfa-split.md`（active，5 Phase）。
- **核心事实**：`LoginServiceImpl`（1556 行）内嵌 4 个 Caffeine 限流 Map + 1 把 JVM 锁，`NopAuthUserBizModel`（2068 行）内嵌 3 个限流 Map + 逐字重复的限流方法（跨类重复共 5 组）；`LocalUserContextCache` 为默认 `nopUserContextCache` 且 `init()` 硬编码 LocalCache 无注入点；`DaoUserContextCache` 只覆写会话四方法——开启 dao 模式后登录失败计数与图形验证码**仍是本地内存**（集群下验证码必失败）；`AbstractLoginService.doGetUserContext` 无 DB 回退，默认装配严格单节点。集群安全资产对照：`DaoLoginSessionStore`/`Db*` MFA store（默认 db）/`MfaStoreProvider` collect-beans 先例/`NosqlCache`/`INosqlCounter` 均已存在。
- **计划要点**：Phase 1 限流组件化（scope 分组等价现状共享拓扑、错误 param 统一脱敏+key 归一、单键原子诚实表述）；Phase 2 `ILoginAttemptStore`（共享缺省单例 + `getLoginFailCountForIp` 既有键前缀缺陷修正）+ verifyCodeCache TTL 保持注入契约（禁直接注入无 TTL 的 `NosqlCache`）；Phase 3/4 两大类 MFA 拆分（完成回调端口/Processor IDaoProvider 接缝，目标双双 ≤1000 行，API 面不变）；Phase 5 集群部署矩阵落 `docs-for-ai`。
- **plan 审查**：两轮独立对抗性审查（第一轮 2 Blocker + 6 Major：触点测试编译矛盾、注入语义未定义、scope 缺失、Redis 复合原子过度承诺、NosqlCache 无 TTL、ipKey bug 等；第二轮确认全部闭合 + 2 新 Major：缺省实例同一性缺口、scan-hollow 模块路径恒绿空转——均已修复，Minor 9 项一并处理）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: design/README 阅读顺序已更新；`docs-for-ai/` 更新归 plan 2274 Phase 5（本条目为立项记录）。

### 晚间（多 agent 并发构建治理：新增 mvnq 包装器 + per-worktree repository 方案分析）

- **产出**：`ai-dev/analysis/2026-09/2026-09-21a-multi-agent-maven-queue-and-per-worktree-repo.md`（Status: resolved）+ 新工具 `ai-dev/tools/mvnq`（bash）与 `ai-dev/tools/MvnqLock.java`（JEP 330 单文件，无需编译）。背景：多 AI agent 同仓并发 `mvn` 互相践踏；要求同 worktree 排队串行、共享依赖走全局仓库、各 worktree install 构件隔离。
- **方案要点**：同一 worktree 经 `.mvn/build.lock`（`FileChannel` 锁）互斥排队，持有者 `kill -9` 后 OS 自动释放（不存在残留死锁），锁文件只写不删防双持有，锁内记录 pid/cmd 供 `--status` 与排队提示；每 worktree 注入 `-Dmaven.repo.local=<root>/.m2-repo`，首次使用从 `~/.m2/repository` 经 APFS clonefile（`cp -Rc`）播种——3.8GB 仓库 17 秒完成、块级共享近似零磁盘增量（依赖本地仓库文件不可变性）。方案依据全部带源码锚点：mvnd 无跨调用排队（`DaemonConnector.connect`）、Maven 4 `-D` 支持 `${session.rootDirectory}` 插值（`BaseParser.populateUserProperties`，早于 rc-5）、`maven.repo.local` 在 mvnd 为 DISCRIMINATING（自动按 worktree 分 daemon 池）。
- **验证**：排队等待/超时 exit 75/崩溃释放（0.3s 内新构建获锁）`--status` busy/idle/仓库注入与显式 `-Dmaven.repo.local` 优先/`--shared-repo`/`--no-lock`/`--no-seed` 全部实测通过；真实 `./mvnw --version` 冒烟通过；`bash -n` 语法检查通过。`.gitignore` 追加 `/.m2-repo/` 与 `/.mvn/build.lock`。
- **偏差**：无。备选方案（mvnd 改码排队、仅 install 时注入仓库、共享仓库+禁 install、mkdir 锁、file:// remote）及否决理由记录在分析文档。
- Doc-sync: No owner-doc update required（开发工具与流程约定，不涉及 `docs-for-ai/` 平台使用文档；AGENTS.md 是否统一路由 mvnq 留待用户拍板，见分析文档 Open Questions）。

### nop-lint plan 06 收口：独立 audit APPROVE-CLOSURE

- 独立审计（task ses_f3d2f6af6ffeHJeLWJlC1Uemrl，fresh session）：亲测复跑 `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 全绿（104 tests）；逐文件核对 xdef/register-model/RuleDslModel/RuleDslParser 与 plan 声称一致；Anti-Hollow 四项全过——(a) 接线实证（maven 日志出现 `unregisterComponentModelConfig:modelType=lint`，注册链路运行时真实发现）、(b) 端到端测试存在（fixture 路径 → 注册链路 → xdef 校验 → parser → matcher 结构断言），(c) 新增类无 TODO/空体/吞异常（唯一 catch 重抛为 NopLintException），(d) 夹具清单 5 合法 + 8 非法逐一有抛异常测试。
- Minor 4 项：plan baseline 历史措辞（design 10 已为准）、rule.json 额外注册（与 javadoc 一致）、RuleDslParser 内联 FQN（已改 import 并复跑 23 tests 全绿）、本 plan 文件 2 个 doc-link warning（shorthand 引用，非 error）。均 non-blocking。
- roadmap item 8 翻 `done`（plan 06）；M2 保持 `todo`（9/10/11/12 未完成）。design 10 §2 已重写为落地形态（含平台 check-mutex attr-only 实测修正），§4 管线补 loadRuleModel 单方法入口。
- check-doc-links --strict 退出码 0（0 errors）；scan-hollow-implementations --module nop-lint/nop-lint-core --severity high 退出码 0。

### nop-lint plan 06 执行：规则 DSL 元模型与加载管线（items Phase 1+2 落地）

- Plan 06（ai-dev/plans/nop-lint/06-lint-rule-xdef-and-dsl-parser.md）：新增 `lint-rule.xdef`（`nop-lint/nop-lint-core/src/main/resources/_vfs/nop/lint/schema/`，Phase 1 字段全集 + 两处 check-mutex 声明 + `xdef:parser-class="io.nop.lint.core.rule.RuleDslParser"`）、`lint.register-model.xml`（model name=lint，`rule.yml`/`rule.json` → DslJsonResourceLoader）、`RuleDslModel`/`RuleDslParser`（`io.nop.lint.core.rule` 包，rule 容器 XOR + any 子项 atLeastOne 校验，全部 NopLintException fail-closed 英文消息含 id 与字段名）、测试夹具 13 个 + 两个测试类。
- **平台实测修正 plan baseline（live repo 与 plan 记载有出入的点，Phase 3 需对齐 design 10）**：
  1. `XDefConstraintValidator` 现已存在且在实例校验期运行（XDslValidator → XDefConstraintValidator.validate），即 plan 所记"check-* 无运行时消费方"已过时；**但它只查属性（node.attrText）**，匹配器是子元素形态 → 若 check-mutex 声明 `atLeastOne="true"` 会对每条合法规则误报。因此 xdef 中两处 check-mutex 保持 `props`+`select`、不写 atLeastOne（声明式意图保留），唯一性运行时强制在 RuleDslParser。
  2. `xdef:name` 值必须是合法 var name：design 10 §2 字面 `xdef:name="lint-rule"` 无法加载（`attr-not-valid-local-ref`），落地为 `xdef:name="LintRule"`。
  3. `message` 必填须显式 `xdef:mandatory="true"`（`<message>!string</message>` 只约束值非空不约束节点存在）。
  4. any 子项用 `<matcher pattern kind regex/>` 对象形态（design §2 的异构同名子标签形态在 body-list 中无法保留标签身份，且无法表达 ast-grep 的分支合取分组）；`files.include/exclude` 与 `metadata.source` 以 csv-set 属性落地（同 tag 重复子节点在 DynamicObject 上会 duplicate-prop 崩溃）；`options`/`settings` map+key-attr 的 YAML 形态为 `options: {key: {value: ...}}`。
  5. `xscript` 以 `string` 落地而非 xpl：xpl std-domain 在 parse 期编译成 ExprEvalAction，不保留原文文本；plan 只保证"文本往返"，XPL 编译归 item 14 RuleCompiler。
- Phase 1→2 顺序裁定：Phase 1 退出项"全部非法夹具抛异常（…或 NopLintException）"中 NopLintException 路径属 Phase 2 parser，两 Phase 在同一次执行中连续落地后统一验证。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` 退出码 0；`./mvnw -pl nop-lint/nop-lint-core -am clean package -DskipTests -T 1C` 退出码 0；`./mvnw -pl nop-lint/nop-lint-core -am checkstyle:check` 在上游模块（nop-api-core 等）存在 9226 个**预先存在**的违规（checkstyle 门禁未在本仓库构建中启用，按 mission 约定记 `lint not configured`，未改动非本任务文件）；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 全绿（nop-lint-core 104 tests：新增 TestRuleDslModelLoading 10 + TestRuleDslParser 13，plan 05 bench smoke 不腐坏）。
- Doc-sync：design 10 对齐修订与 roadmap item 8 回写随 Phase 3 进行（本条为执行记录）。

### nop-lint plan 05 收口：audit APPROVE-CLOSURE（5 Minor 已清偿）

- 独立审计（agent_ffa1e1e0）亲测复现：双脚本实跑、JFR 七帧逐项对照、语料 diff、81 tests 重跑全绿。5 个 Minor（规则 3 零命中声明/alloc 速率落盘值/JMH 版本残留/结构偏差声明/脚本 tmp 路径）随收口修复。
- roadmap item 13 翻 `done`——性能基线门禁建立，后续一切性能声称以 `nop-lint/docs/perf-baseline.md` 为对照。

### nop-lint plan 04 收口：audit APPROVE-CLOSURE，里程碑 M1 达成

- 独立审计（agent_1522f9f5）亲测：80 tests 重跑全绿；自写探针 11 项断言验证链式捕获/文本语义/SMART-AST 分档；调用链逐级实证无 hollow；三工具门禁全 0。
- 清偿条件已补：AST 跨 token 断言、测试计数更正（80）、死三目断言修复。
- roadmap items 4–7 翻 `done`，**M1（pattern kernel usable）翻 `done`**——Wave 1 全部完成，解锁 item 8（规则库）与 item 13（JMH 基线）。
### nop-lint plan 03 收口：独立 audit APPROVE-CLOSURE（附条件已补齐）

- 独立审计（agent_b10e99e6）亲测：45 tests 重跑全绿；自写探针验证编译树形与拒绝行为全部吻合 plan 声称；hollow 扫描 19 文件 0 findings。附条件（同名 `$A` 一致性测试缺失）已补——新增 `m($A, $A)` 与字面 `$A == $B` 用例，并修 bare `$_` 归类（name=null）与测试死三目；core 现 47 tests 全绿。
- roadmap item 3 已翻 `done`（plan 03 引用）；plan 03 置 `completed`，Closure Audit Evidence 入档。

### nop-lint plan 03 预研：pattern CST 形态实验（java blob 实测）

- 为 SourcePatternCompiler（roadmap item 3）拟定编译规则，用临时 JUnit 探针实测 23 个 pattern 的 CST 形态（探针用后即删，本条为留痕）：
  - 裸表达式 pattern（`foo()`/`$A == $B`/`$VAR`/`0`）在 program 级产生**单子同跨度 ERROR 包装**；`class X { $$$ }` 类体与 `if (...) { $$$ }` 块体的裸 `$$$` 同为单子 ERROR 包装——转换期须全局剥离。
  - `throw new $$$` 产生**多子 ERROR**（无法剥离）→ 按 design 04 §8 P0 拒绝编译；等价可解析写法 `throw new $$$($$$)`。
  - 语句 pattern 尾部普遍有零宽 **missing unnamed `;`**（`throw ...`/`return null`），须丢弃；missing **named** 节点真实存在于 `void m(int) {}` 的 formal_parameter 内——转换期拒绝编译（fail-fast）。
  - `$OBJ.dao().$METHOD($$$ARGS)`、`class $C extends CrudBizModel { $$$ }`、`@BizMutation public $$$ $METHOD(...)`（design 01 §3.5 首例）均干净解析。
  - kindId round-trip（node.kindId()==lang.kindId(node.kind())）对 Terminal 根（`;`=77）与 alias（type_identifier=320）实测成立。
- 结论已固化进 plan 03（ai-dev/plans/nop-lint/03-source-pattern-compiler.md）的 Current Baseline/设计偏差声明。
- Doc-sync：No owner-doc update required（design 01 §4/§3.5 措辞修订记为 follow-up）。
### plan 2273 Phase 4——吞吐比复验与收口

- 吞吐比收尾复测（count 口径收尾档，load ~8 残余负载）：64MB Coord 49.605 vs rg 80.378 = **61.7%**；512MB Coord 12.834 vs rg 23.067 = **55.7%**——双档 ≥50% 达成，且 2267 遗留 watch-only follow-up「512MB 安静环境复测」清偿（本日含负载仍 55.7%，优于 2267 中位 49.05%）。
- benchmark README 增 plan 2273 收敛循环结论章节；工具门禁三件套（checklist --strict / scan-hollow / doc-links --strict）待独立 closure audit 复核后随收口 commit 记录退出码。
- Commit: 见 plan 2273 收口提交。

### plan 2273 Phase 3——性能收敛循环：R1/R5 收割，R3/R4 否决，字面终止

- Plan 2273 Phase 3 完成（严格字面终止条款 + 候选池枯竭裁定）。**R1 保留**（TEXT 输出缓冲：CLI autoflush 逐行 flush → 64KB BufferedOutputStream 收尾 flush；共测配对 3 对 TEXT 1MB+64MB：中位 +387%/+403%，3/3 一致，CI 零重叠——字节序列不变）。**R5 保留**（TEXT 口径跳过子匹配文本解码：SearchCommand 增 includeSubmatchText 默认 true，CLI TEXT 置 false；正确配对中位 +8.9%/+13.7%，6/6 正向；CLI 字节序列不变，design 决策 5 契约已记）。**R3 回退**（≤1MB 堆读替代 mmap：配对三对方向不一致 +2/+3.5/-15.2%，eager 复制不敌并行惰性缺页）。**R4 实现前否决**（double-stat 消除算术上界 ≤1.5% < 2%）。R2 同行 memo 实现前否决（corpus 每命中行恰 1 命中，触发面为零）。12B 探测维持 16B 阈值（2267 R1 留口闭合）。
- 噪声事件如实记录：R3 判定期间外部负载 load 飙至 28.5（他会话重度构建），配对 CI 巨幅波动；R5 首次配对因基线快照未随 R1 commit 刷新而作废（p2273-r5-pair* 留档），修正协议后 r5b 有效。
- 终态 profile：count 与 2267 终态同构（indexOf 42.3% + matchesAt 32.1%，扫描带宽地板）；text 剩余热点均为输出/扫描契约必需——无 ≥2% 可识别候选，循环终止。
- 验证：全量回归 `./mvnw test -pl nop-rg/nop-rg-core,nop-rg/nop-rg-cli,nop-rg/nop-rg-vector -am` EXIT=0（core 57 run + 1 skip / cli 26 / vector 10）；large-file 显式组 3/3；rg 对照 opt-in 10/10；kept 优化逐项 commit。
- Doc-sync: `ai-dev/design/nop-rg/01-architecture-baseline.md` 决策 5 增 SearchCommand 解码口径契约；benchmark README 待 Phase 4 终态数字后统一更新。

### nop-lint plan 02 收口：独立 closure audit 复核通过

- 首轮独立审计 REJECT（唯一阻塞：Phase 2 Status 头漏改 planned，属勾选脚本遗漏）→ 按审计处方修复后达到 APPROVE-CLOSURE 条件。审计实测：30 测试重跑全绿、bubble-up 防护逐行推演成立、kindId map 四要素合规、hollow 扫描 11 文件 0 findings。
- roadmap item 2 已翻 `done`（plan 02 引用）；plan 02 置 `completed`，Closure Audit Evidence 入档。
### plan 2273 Phase 2——性能基线重建与新口径场景扩展

- Plan 2273 Phase 2 完成：`CoordinatorEndToEndBenchmark` 增加 `mode=count/text` 口径（text = includeLineText + ResultPrinter 输出至 CLI 等价 autoflush /dev/null sink）与 `many-small` 场景（512×128KB，`-p size=64MB` 钉定）；`VectorCompareBenchmark` 增加 12B 探测场景；`HotspotProfiler` 支持第三参数 `text`。基线快照已 install 至 m2（nop-core+core/vector/cli，含 Phase 1 修复）。
- 关键数据（全部落 plan 2273 记录表 + `_tmp/nop-rg-bench/p2273-*.json|log`）：本机有持续外部负载（opencode ~100% 单核，load 7.3-8.4），row0 绝对值仅作参照；row0 profile count 口径 LF 扫描 ~73% + matchesAt 11.3%，与 2267 终态同主次——无结构漂移，「不再评估」清单维持；TEXT 基线 64MB 2.15 ops/s（count 的 ~1/26，σ 极小）；many-small 33.37 ops/s（每文件开销面 ~30%）；**12B 探测无 ≥2% 组件级胜出（稀疏名义 +2.0% CI 重叠、密集 -1.1%）→ 维持 16B 阈值，2267 R1 留口闭合**。
- 偏差：测量期间发现 `cd` 相对路径失败曾误读 2267 旧 row0 产物——全部新证据改用 `p2273-` 前缀隔离，无新旧混用。
- 验证：`./mvnw compile -pl nop-rg/nop-rg-benchmark -am` EXIT=0；`./mvnw install -pl nop-kernel/nop-core,nop-rg/nop-rg-core,nop-rg/nop-rg-vector,nop-rg/nop-rg-cli -DskipTests` EXIT=0；row0a/row0b/TEXT/many-small 收尾档 + 12B 迭代档全部 EXIT=0。
- Doc-sync: `nop-rg/nop-rg-benchmark/README.md` 更新（基准清单、12B 补测结论、新口径基线、2273 共测配对协议指引）。

### plan 2273 Phase 1——nop-rg 第二轮深度审计：符号链接崩溃修复 + 可维护性清理

- Plan 2273 Phase 1 完成（`ai-dev/plans/2273-nop-rg-deep-audit-quality-perf.md`）：第二轮深度审计发现 A1-A8 全部落地。核心为 A1 两点修复——`ParallelFileWalker` 与 nop-core `GitIgnoreFile.loadSubdirectoryRules` 均不跟随符号链接目录（默认路径下 GitIgnoreFile 规则加载先于 walker 崩溃，仅修 walker 是假修复，draft review B1 实证）；A2 `NopRgMain` JFR 启动失败路径补 `CoreInitialization.destroy()` 对称清理；A3 删死公共 API `FileMatches.isCountOnly()`；A4 行文本解码归一 `LineCursor.text()`（顺带删除恒为 no-op 的 `currentSubmatches.clear()`）；A5 命中区间 `List<long[]>` → 包私有 record `MatchSpan`（`MatchAggregator.aggregate` 收窄包私有）；A6 六文件 import 组内字母序；A7 `MappedFileReader` 空文件 null 契约 javadoc；A8 `HotspotProfiler` 异常路径资源释放。
- 偏差：无实质偏差；MatchAggregator 重写顺带移除 no-op clear（属 A4/A5 重写范围，计划内未单列）。
- 验证：`./mvnw test -pl nop-kernel/nop-core,nop-rg/nop-rg-core,nop-rg/nop-rg-cli,nop-rg/nop-rg-vector -am` EXIT=0——core 56 run + 1 skip（walker 6→8：新增目录环终止 + 文件链接跟随/悬空跳过两组）、cli 26、vector 10、GitIgnoreFileTest 18/18（新增环测试实际执行非跳过）；rg 对照双保险：`-Dtest.rg.compare=true` GlobMatcherTest 10/10 全跑 + cli RgComparisonTest 9/9；`./mvnw compile -pl nop-rg/nop-rg-benchmark -am` EXIT=0。
- Doc-sync: cli README「与 rg 的已知差异」补符号链接三条语义；`ai-dev/design/nop-rg/01-architecture-baseline.md` 决策 2（GitIgnoreFile 契约收窄 + 下游消费者影响面）与决策 4（walker 契约）；`docs-for-ai/01-repo-map/module-groups.md` walker 描述；check-doc-links --strict EXIT=0。
- Commit: 见 plan 2273 Phase 1 commit（git log "plan 2273 Phase 1"）。

### nop-lint plan 01 收口：独立 closure audit APPROVE-CLOSURE

- 独立子 agent（agent_4a9d9e71）live-repo 复核全部通过：重跑三模块 `-am test` 均 BUILD SUCCESS；`check-plan-checklist --strict` 退出码 0；`scan-hollow-implementations --module nop-lint` 全级别 0 findings（实际扫描 3 个 src/main 文件，用 nop-treesitter 93 发现反证工具非空转）；`check-doc-links --strict` 0 errors。
- roadmap item 1 已翻 `done`（plan 01 引用）；plan 01 置 `completed`，Closure Audit Evidence 入档。
- Commit: plan 收口提交见 `git log`（roadmap 翻转 + Closure evidence）。

### nop-lint 工程启动：plan 01 模块骨架落地

- Plan 01（ai-dev/plans/nop-lint/01-module-skeleton.md，roadmap item 1）执行：新建 `nop-lint/` 模块组（35-nop-lint）含聚合 pom + nop-lint-core/-java/-nop 三个子模块，注册进根 pom reactor。依赖方向：core → nop-treesitter（`${project.version}`，nop-bom 未管理该 artifact）+ nop-xlang；java/nop → core。
- 两轮独立子 agent 对抗审查（agent_4c0ab23e / agent_68513349）：第一轮 1 Blocker（依赖版本缺失）+ 2 Major（java.version=21 死属性、dependency:list -q 不可观察）+ 8 Minor 全部修复；第二轮 0 Blocker/Major。骨架继承根 pom release 17，Java 21 升级记入 Deferred But Adjudicated。
- Bootstrap 测试 ×3 全绿：core（java blob → parse → TSNode 遍历断言 method_declaration/throw_statement）、java（传递依赖 + type_identifier）、nop（DefaultTreeSitterLanguageProvider contains 六 built-in）。surefire：各 1 test / 0 failure。
- 接线验证：`dependency:list` 确认 nop-treesitter、nop-xlang compile scope。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am clean test -T 1C` 退出码 0；`-pl nop-lint/nop-lint-java/-nop -am test -T 1C` 均退出码 0。
- 偏差：无（按 plan 执行）。
- Doc-sync：`docs-for-ai/01-repo-map/module-groups.md` 新增 nop-lint 行（Phase 3）。

### nop-lint plan 02：LintNode 门面 + Java 语言适配落地

- Plan 02（roadmap item 2）执行：core 新增 `io.nop.lint.core.node`（LintNode/LintTree/SourceRange/NodeIterator/TreeSitterLintNode）与 `io.nop.lint.core.lang`（LintLanguage/TreeSitterLanguageAdapter）；nop-lint-java 新增 `JavaLanguage`（组合委托 adapter，holder 单例加载 java blob）。
- 三轮独立子 agent 对抗审查（agent_e1dc86aa / agent_28eba74e / agent_c4f8ffab）：发现并修复 3 Blocker 级事实——(1) `Language.symbolId` 不扫 alias 区间致 `type_identifier` 查名 -1（适配器改为自建全名称表 map）；(2) `throws`/`permits` 同名 visible 符号碰撞（named id 优先收表）；(3) TSTreeCursor 从节点出发的帧链上溯至树根、最后子节点上 `gotoNextSibling` 会 bubble up 越出子树（children 枚举改为 childCount 计数驱动——实现期实测 7647 vs 61 节点坐实该风险）。
- 测试：core 18（门面 10 + 适配器 7 + bootstrap 1）+ java 7（绑定 6 + bootstrap 1）全绿；kindId 一致性断言覆盖全部命名节点 round-trip；`$VAR` 词法保留端到端验证。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 与 `-pl nop-lint/nop-lint-java -am test -T 1C` 退出码 0。
- 偏差：LintNode 接口面比设计 01 §5 草图宽（kindId/isNamed/isExtra/isMissing/namedChildren/childByField/遍历），已在 plan Current Baseline 声明为有意扩展。
- Doc-sync：No owner-doc update required（代码级事实归源码，design 01 §5 为架构草图）。

### nop-lint plan 03：SourcePatternCompiler 落地

- Plan 03（roadmap item 3）执行：core 新增 `io.nop.lint.core.pattern`（PatternNode sealed 层级：MetaVar/Terminal/Internal；MetaVarSyntax 五条判定规则——最长前缀 `$$$`→`$$`→`$`、名 [A-Z_][A-Z_0-9]、裸形式有意偏离 ast-grep、下划线名非捕获、`$@`/`$!` 拒绝；SourcePattern 值对象 + possibleKindIds O(1) 过滤；SourcePatternCompiler compile/contextual）；模块级异常 `io.nop.lint.core.NopLintException`（extends NopException，对齐 TreeSitterException 先例）。
- 两轮独立子 agent 对抗审查（agent_d6b137d1 / agent_f4b8c969）：修复 3 Major（meta-var 分类规则不完备→对齐 ast-grep 前缀序+名规则；missing 丢弃策略二分——unnamed `;` 丢弃 / named 拒绝；提取算法与设计 01 §4 公式漂移→偏差声明三条）+ 测试用例纠错（`void $M($$$ARGS)` 实为多子 ERROR，missing-named 真实用例是 `void m(int) {}`）。
- 实现期自审修复：meta-var 分类限叶子节点（否则 program 包装节点文本 `$VAR` 会被误捕获）；`TSTreeCursor` bubble-up 越界由 plan 02 的计数驱动枚举防住。
- 测试：SourcePatternCompilerTest 27 用例（收口补 2 后 29）（旗舰/ERROR 剥离/missing 策略/拒绝路径/contextual 判别/叶锚定）全绿；core clean build 45 tests / 0 failures。审查子 agent 的探针测试残留（PatternProbeTest/ScratchProbeTest）已清除。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am clean test -T 1C` 退出码 0。
- 偏差：见 plan 03 设计偏差声明四条（提取公式/ERROR 收紧/裸形式/contextual 根排除）。
- Doc-sync：No owner-doc update required（design 01 §4/§3.5 措辞修订记 follow-up，item 7/11 时执行）。

### nop-lint plan 04：pattern 匹配内核落地（roadmap items 4–7）

- Plan 04 四阶段+集成一次交付：`MetaVarEnv`（同名一致性/clone 隔离/adopt 提交）、`NodeExactEquality`（收紧版 does_node_match_exactly）、`Strictness`（SMART/AST）、`PatternMatcher`（lockstep 决策矩阵 + 省略号 lookahead probe 回溯 + consume-one 连续省略号）、`SourcePattern.matchIn` 入口、`Match` 值对象。
- 两轮独立审查（agent_42fd0da9 / agent_4e0eb0ec，对照 ast-grep 上游源码）驱动的关键修正：B-1 named 叶编译为带文本 TerminalNode（否则 `dao` 匹配 `get`，旗舰语义塌陷）；lockstep 候选跳过为 **candidate 键控**（goal 键控会让 `f()` 匹配 `f(1,2)`，实现期测试暴露）；AST 不跳注释（上游 should_skip_comment 对 Ast 为 false）；metavar 前仅跳注释、SINGLE 对未命名候选 NoMatch；连续省略号 consume-one；尾部处理偏严（design 04 §3 明文）已声明。
- 实现期裁定：`int[] a = {1, 2,};` 是 Java 唯一合法尾逗号位置，AST 尾部未命名 goal 测试用例据此重构（print($A,) 是 JS 语法，Java 下不可编译）。
- owner-doc 勘误：design 04 §4 AST 行"注释：跳过"→"不跳过"；§2 `skip_cand_for_metavar` 行更正；实现列类名标注为语义映射。
- 测试：PatternMatcherTest 25 用例（lockstep/省略号/M1 端到端验收：throw/chain/class/同名一致性/MetaVar 根 pinning/负例）+ MetaVarEnvTest 8；core 80 tests / 0 failures（此前 84 为含过期 surefire 报告的误加总，独立审计实测更正）。
- 验证：`./mvnw -pl nop-lint/nop-lint-core -am clean test -T 1C` 退出码 0（80 tests，审计实测口径）。
- Doc-sync：design 04 已勘误（见上）；`No owner-doc update required` 之外无其他。

### nop-lint plan 05：JMH 性能基线落地（roadmap item 13）

- Plan 05 执行：core test scope JMH 1.33（同 nop-treesitter 坐标 + annotationProcessorPaths 显式挂载——JDK 23+ 默认不跑 classpath 注解处理器）；`LintBenchmarks` 三基准（compileRuleSet/matchAllPatterns/parseAndMatch）+ `LintBenchmarkRunner` + `BenchmarkSmokeTest`（forks(0)+failOnError+结果数断言，mirror nop-treesitter 纪律）+ `run-benchmarks.sh`/`compare-ast-grep.sh` + 签入语料。
- 基线：parseAndMatch ≈1ms/文件（design 11 §6 的 50ms 预算余量 ~50×）；compileRuleSet ≈0.1ms；alloc 1.3MB/op（优化候选）。ast-grep 0.43.0 同规则 200 文件 400 命中一致（语义对齐），计时口径不对称已声明。
- JFR（60×1s 稳态，4972 ExecutionSample）：~93% 在 nop-treesitter cursor 机制（locateInto 40%/resetTo 13%/flattenedIndexOf 12%），nop-lint 内核帧 ≈1.5%——cursor 复用/children 缓存记录为优化候选，不在本 plan 实施。首错记录：录制必须经 -jvmArgsAppend 注入 forked JVM（父进程录制采不到基准代码）。
- 意外收获：JFR 采样旁证了 plan 04 的实现裁定——nop-lint 匹配内核自身开销极低，语义对齐（ast-grep 400 命中一致）经工具实测确认。
- 验证：`./mvnw -pl nop-lint/nop-lint-core test` 退出码 0（含 BenchmarkSmokeTest）；compare 脚本完整跑通（含匹配数>0 门禁）。
- Doc-sync：`nop-lint/docs/perf-baseline.md` 新增（环境/方法/数字/对比/JFR 热点/复现命令）。
