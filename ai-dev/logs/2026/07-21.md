# 2026-07-21 Dev Log

## Plan Execution: 2026-07-20-2000-3 nop-metadata Quality Alert Workflow

**Status**: Phase 1 + Phase 2 completed; plan closed.

### Changes Made

#### Phase 1 — nop-wf 依赖 + ORM 字段 + 工作流定义

- **ORM model** (`nop-metadata.orm.xml`): Entity `NopMetaQualityResult` added column `isFalsePositive` (byte→boolFlag, nullable, propId=15)
- **Workflow** (`qualityBreachApproval/v1.xwf`): created at `_vfs/nop/metadata/wf/qualityBreachApproval/v1.xwf` with steps start → owner-investigate → verify → end; `onOwnerAgree` listener triggers re-judge via `QualityAlertWorkflowService.reJudge()`; `onDisagree` listener uses `wf-approval:notifyResult` to call BizModel.reject()
- **Generated files** (`_NopMetaQualityResult.java`, `_app.orm.xml`): updated with `isFalsePositive` column
- `nop-wf-core` dependency already existed

#### Phase 2 — WorkflowService + MetaQualityRuleExecutor 集成

- **New** `QualityAlertWorkflowService` (`quality/QualityAlertWorkflowService.java`): service that lazily loads `IWorkflowManager` from BeanContainer; `createAlertWorkflow(result)` creates `qualityBreachApproval` workflow instance via `IWorkflowManager.newWorkflow()`; `reJudge(ruleId, resultId)` reloads rule from DB and executes re-judge
- **Modified** `NopMetaQualityRuleBizModel`: injects `QualityAlertWorkflowService`; `executeQualityRule()` now triggers `createAlertWorkflow()` when `judgment.status=FAIL` AND `rule.severity=ERROR`; added `judgeByRuleId(String ruleId)` public method
- **Modified** `NopMetaQualityResultBizModel`: added `@BizMutation approve(id)` and `@BizMutation reject(id)` — approve handles agree scenario (re-judge), reject sets `isFalsePositive=true` for disagree scenario
- **New ErrorCode**: `ERR_QUALITY_RESULT_NOT_FOUND` in `NopMetadataErrors`
- **New ARG**: `ARG_QUALITY_RESULT_ID`
- **Beans config** (`app-service.beans.xml`): registered `QualityAlertWorkflowService`
- **Tests**: 3 new unit tests `TestQualityAlertWorkflowServices` verifying error codes + reject guards; existing 661 tests fully pass (total 664)
- **Roadmap** (`nop-metadata-roadmap.md`): G3 updated from `todo` to `done`
- **Design doc** (`12-data-contract-and-governance-workflow.md`): Phase 3 marked as implemented

## Plan Execution: 2026-07-20-2000-2 nop-metadata DataContract Approval Workflow

**Status**: Phase 1 + Phase 2 completed; plan closed.

### Changes Made

#### Phase 1 — nop-wf 依赖 + ORM 字段 + xmeta

- **pom.xml**: Added `nop-wf-core` (compile) and `nop-wf-meta` (compile) deps to `nop-metadata-service`
- **ORM model** (`nop-metadata.orm.xml`):
  - Entity `NopMetaDataContract`: added `tagSet="use-approval"` on `<entity>` element
  - Added columns: `approveStatus` (VARCHAR(20), dict=wf/approve-status), `approvedBy` (VARCHAR(50)), `approvedAt` (TIMESTAMP) — all nullable
- **xmeta** (`NopMetaDataContract.xmeta`): added `wf:wfName="metaDataContractApproval"`
- Codegen regenerated `_NopMetaDataContract.xbiz` with `x:extends="/nop/wf/base/approval-support.xbiz"` ✓

#### Phase 2 — Workflow Definition + BizModel approve/reject

- **Workflow** (`metaDataContractApproval/v1.xwf`): created at `_vfs/nop/metadata/wf/metaDataContractApproval/v1.xwf` with steps submit → owner-check → consumer-check → end; `on-end` listener using `wf-approval:notifyResult`
- **BizModel** (`NopMetaDataContractBizModel.java`):
  - `approve` @BizMutation: full impl (check SUBMITTED → set APPROVED/approvedBy/approvedAt → status transition DRAFT→ACTIVE / ACTIVE→DEPRECATED / DEPRECATED→RETIRED)
  - `reject` @BizMutation: full impl (check SUBMITTED → set REJECTED/approvedBy/approvedAt → status=DRAFT + remark)
  - `activateContract`/`deprecateContract`/`retireContract`: marked `@Deprecated`, delegate to `submitForApproval()`
- **Tests**: Updated `TestNopMetaDataContractBizModel` — approve/reject guard tests + all checkContract tests pass

### Verification

`./mvnw test -pl nop-metadata/nop-metadata-service -am` — 663 tests, 0 failures ✓

### Docs Updated

- `ai-dev/design/nop-metadata/12-data-contract-and-governance-workflow.md`: Phase 1 marked ✅
- `ai-dev/design/nop-metadata/nop-metadata-roadmap.md`: G1 `todo` → `done`
- `ai-dev/plans/2026-07-20-2000-2-nop-metadata-datacontract-approval-workflow.md`: Status `completed`

### Deferred

- Full end-to-end workflow runtime test (submitForApproval → wf → listener → approve) deferred to successor plan (requires full wf runtime infrastructure setup)
