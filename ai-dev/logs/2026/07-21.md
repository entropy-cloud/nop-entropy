# 2026-07-21 Dev Log

## Plan 2026-07-21-1200-2 完成：nop-metadata Code Quality, Type Safety, Documentation

执行 `ai-dev/plans/2026-07-21-1200-2-nop-metadata-code-quality-and-docs.md`，覆盖全部三个 Workstream。

### WS-A: Type Safety Antipatterns
- A1: 创建 `ArrayHolderUtils` 共用工具类，消除 3 处 `newArrayHolder()` 重复
- A2: `INopMetaLineageEdgeBiz` 4 个方法返回类型从 `Map<String,Object>` 改为 `LineageRecordResultDTO`/`LineageExtractResultDTO`；扩展 DTO 字段支持完整返回结构
- A3: 消除 `MetaJoinExecutor` 和 `NopMetaTableBizModel` 中 `(IOrmEntityDao)(IOrmEntityDao)` 双转型模式
- A4: `testConnect()` 已返回 `TestConnectionResultDTO`（无需改动）
- A5: `NopMetaQualityCheckpointBizModel.executeCheckpoint` 返回类型改为 `CheckpointExecutionResultDTO`；`save` 参数添加 `@Name("data")`
- A6: 部分减少 `@SuppressWarnings("unchecked")`（A1-A5 根模式消除）

### WS-B: BizModel Restructuring
- B5: `NopMetaQualityCheckpointBizModel` 使用 `requireEntity` 
- B6: `NopMetaTableBizModel.queryEntityData` 双转型消除
- B7: `nop-search-lucene` 改为 optional 依赖
- B8: `NopMetaDataSource.name` 添加唯一键 `uk_meta_datasource_name`
- B1-B4: 延期为优化候选（大文件拆分、xmeta）

### WS-C: Documentation & ORM Fixes
- C1-C2: `docs-for-ai/03-modules/nop-metadata.md` 修复 DTO 位置、模块结构表、core 描述
- C4: 索引命名标准化 `IX_NOP_META_TABLE_LOOKUP` → `IX_NOP_META_TABLE_MODULE_SCHEMA_NAME`
- C5: `NopMetaModelChangedEvent.changeSource` 添加 `ext:dict` + 创建 `change-source.dict.yaml`
- C6: 移除 61 处 json-1000/json-4000 列冗余 `precision` 属性
- C7: `TestNopMetaAggregationBizModel` (2592→25 行) 拆分为 3 个领域测试文件
- C8: 创建 `TestAggregationHelper.java` (593行, 42个帮助方法)
- C9: `ERR_LINEAGE_NOT_SQL_TABLE` → `ERR_LINEAGE_NOT_SQL_VIEW_TABLE` 对齐错误信息

### Verification (1st pass)
- `./mvnw test -pl nop-metadata/nop-metadata-service -am`: 706/707 通过
- `docs-for-ai/03-modules/nop-metadata.md` 已更新
- 偏差：B1-B4 延期优化、B7 改为 optional 而非完全移除、C3 deferred

### Verification (final) — full green
- **根因修复**: `executeCheckpoint` 改用 `requireEntity` 后，`context=null` 时 `checkDataAuth`/`checkMetaFilter` 未做 null 守卫 → NPE
- **修复**: `CrudBizModel.checkDataAuth` + `checkMetaFilter` 添加 `context == null` 提前 return
- **结果**: 707/707 全部通过（原 flaky scheduler 测试 `TestMetaQualityCheckpointScheduler.testCronJobFireNowWritesResultsAndScores` 稳定通过）

## Plan 2026-07-21-1200-1 完成：nop-metadata P1 Runtime Defects

执行 `ai-dev/plans/2026-07-21-1200-1-nop-metadata-p1-runtime-defects.md`，修复 7 个 P1 级别运行时缺陷。

### 代码改动

- **Phase 1**: `NopMetaSearchBizModel` — null guard for `searchEngine` in both `searchMetadata` and `rebuildSearchIndex`; added 2 unit tests
- **Phase 2**: `ExternalTableStructureReader` — replaced String ErrorCode constant with `NopMetadataErrors.ERR_DIALECT_NOT_SUPPORTED`; removed `newErrorCode` helper
- **Phase 3**: `MetaManifestBuilder` — migrated 2 String-based throws to ErrorCode constants; `NopMetadataException(String)` marked @Deprecated
- **Phase 4**: `NopMetadataErrors.java` — synced Javadoc subdomain list (22→38 subdomains) with actual usage
- **Phase 5**: Renamed `NopMetaTable.schema` column to `metaSchema` in ORM model; updated all Java/xmeta references
- **Phase 6**: Added `cascadeDelete="true"` on all strong parent-child to-many relations in ORM model

### Verification

- `./mvnw test -pl nop-metadata/nop-metadata-service -am`: 707 tests, 0 failures, 0 errors
- No owner-doc update required (all changes are internal refactoring)

### Audit Closure

- `ai-dev/audits/2026-07-20-1816-open-audit-nop-metadata.md` → `closed`
- `ai-dev/audits/2026-07-20-1816-multi-audit-nop-metadata.md` → `closed`

## Plan 310 完成：nop-metadata 空接口存根、ORM 模型约束与依赖清理

执行 `ai-dev/plans/310-nop-metadata-stubs-model-deps-cleanup.md`，清理审计发现的 P2/P3 级技术债务。

### 代码改动

- **Phase 1**: 删除 `NopMetadataConstants.java` 和 `NopMetadataConfigs.java` 两个空接口存根文件
- **Phase 2**: 从父 pom 移除 `nop-metadata-api` 死模块，删除整个子目录
- **Phase 3**: `NopMetaDataSource.xmeta` retention 层 `connectionConfig` prop 添加 `queryable="false"` 覆盖
- **Phase 4**: `NopMetaTable` 移除重叠唯一键 `uk_meta_table_module_schema`，保留 `UK_NOP_META_TABLE_MODULE_NAME`；`NopMetaLineageEdge` 新增唯一约束 `UK_NOP_META_LINEAGE_EDGE_SRC_TGT_TYPE`（`sourceTableId, targetTableId, transformType`）
- **Phase 5**: `INopMetaDataContractBiz` 接口中 `activateContract/deprecateContract/retireContract` 添加 `@Deprecated` 注解；`nop-metadata-service/pom.xml` 移除未使用的 `nop-sys-dao` 编译依赖
- **Phase 6**: `NopMetaTable.xmeta` 和 `NopMetaLineageEdge.xmeta` retention 层添加注释说明有意为空

### 验证

- `./mvnw compile -pl nop-metadata -am` — BUILD_OK
- `./mvnw test -pl nop-metadata -am` — TESTS_OK（690 测试通过）
- All Phase Exit Criteria + Closure Gates ticked
- Source Audits (NF-03, AR-04, 03-F04/05/06/09/10, 04-P2-01/02, 07-F1, 01-02, 09-11) closed

## Plan 309 完成：nop-metadata 异常处理与日志修复

执行 `ai-dev/plans/309-nop-metadata-error-handling-fixes.md`，修复审计发现的 6 类异常处理与日志缺陷。

### 代码改动

- **Phase 1**: `MetaModelChangedEventPublisher.buildSnapshot` — `catch (Throwable)` → `catch (Exception)`，改用两参构造器保留 cause chain
- **Phase 2**: `SqlColumnLineageExtractor` — 3 处 `IllegalStateException` cause → `.param("reason", ...)`
- **Phase 3**: 全模块 19 处 `.cause(e)` 链式调用 → `new NopException(ErrorCode, e)` 两参构造器（涉及 ）
- **Phase 4**: `NopMetaModuleBizModel` — 2 处 `LOG.warn("...{}", e.toString())` → `LOG.warn("...", e)` 保留堆栈
- **Phase 5**: `AggregationContext.tryLoadEntityField`（2 处）— catch 块添加 `LOG.warn` 日志记录，消除静默吞异常
- **Phase 6**: `MetaContractChecker` — 5 处中文消息 → 英文（含 buildFailMessage 方法）

### 验证

- `./mvnw compile -pl nop-metadata -am` — BUILD_OK
- `./mvnw test -pl nop-metadata/nop-metadata-service -am` — TESTS_OK
- All Phase Exit Criteria ticked; Source Audits (NF-01/02/04, 09-01/02/03/05) closed
- No owner-doc update required

---

## Plan 308 完成：nop-metadata 接口契约补全

执行 `ai-dev/plans/308-nop-metadata-interface-contract-gaps.md`，修复 3 个 P0/P1 接口缺口 + judgeByRuleId 注解 + 测试覆盖。

### 代码改动

- `INopMetaDataProductBiz`: 新增 `linkAsset`/`unlinkAsset`/`getLinkedAssets` 方法声明（P0 缺口 AR-01/02/03）
- `INopMetaQualityResultBiz`: 新增 `approve`/`reject` 方法声明（P1 缺口 AR-01/02/03）
- `INopMetaDataContractBiz`: 新增 `approve`/`reject` 方法声明（P1 缺口）
- `INopMetaQualityRuleBiz`: 新增 `judgeByRuleId` 方法声明（`@BizQuery`，P2 缺口 07-F2）
- `NopMetaQualityRuleBizModel.judgeByRuleId`: 添加 `@BizQuery` + `IServiceContext context` 参数；返回类型改为 `Map<String, Object>`（DAO 模块不可引用 `QualityRuleJudgment`）
- `TestNopMetaBizInterfaceCompleteness`: 扩展覆盖所有修复接口（NF-05）
- 新增 `TestNopMetaQualityResultBizModel`（4 tests）：approve/reject 正常路径 + 不存在实体异常路径（16-01）

### 验证

- `./mvnw compile -pl nop-metadata -am` — 0 errors
- `./mvnw test -pl nop-metadata/nop-metadata-service` — 690 tests, 0 failures（新增 6 test）
- No owner-doc update required（internal contract compliance）

---

## Plan 307 完成：nop-metadata DTO 迁移与 data-auth 扩展

执行 `ai-dev/plans/307-nop-metadata-dto-migration-data-auth.md`，完成 data-auth 扩展（Phase 2），识别 DTO 迁移（Phase 1）模块依赖约束。

### Phase 1 — DTO 迁移（blocked，已移入 Deferred）

- 发现模块依赖约束：`I*Biz` 接口位于 `nop-metadata-dao`，DTO 类位于 `nop-metadata-service`。dao 模块不能引入 service 模块的依赖。BizProxyFactoryBean 生成的 JDK 代理需要 BizModel implements 接口。
- 解法需在模块结构层面解决（如新建 `nop-metadata-dto` 或接口移入 service），属于独立模块重构计划。
- 所有 DTO 切换回退，剩余 11 个方法继续返回 `Map<String, Object>`。

### Phase 2 — data-auth.xml 扩展 5 个实体

- 为 `NopMetaQualityRule`、`NopMetaProfilingRule`、`NopMetaReconciliationResult`、`NopMetaDataContract`、`NopMetaBusinessDomain` 配置行级权限（`createdBy == $user.userId`，admin 全量访问）
- `data-auth.xml` 从 3 个实体扩展至 8 个
- 新增 `TestDataAuthRowLevelScoping.java`（7 个 test）：`testEightTargetEntitiesHaveRules`、`testAdminAndDefaultRoleRulesExist`、`testUserFilterExpressionCorrect`、`testDefaultRoleIdsIncludesUserKeyword`、`testAdminRoleIdsIncludesAdminKeyword`、`testFileReferencesCorrectSchema`、`testDefaultRoleAuthHasFilter` — 覆盖结构和表达式验证

### 验证

- `./mvnw compile -pl nop-metadata -am` — 0 errors
- `./mvnw test -pl nop-metadata -am` — 686 tests, 0 failures
- No owner-doc update required

---

## Plan 306 完成：nop-metadata 审计快速修复（SqlAggregationProcessor + 死模块移除）

执行 `ai-dev/plans/306-nop-metadata-audit-quick-fixes.md`，修复审计 R-01/R-02。

### Phase 1 — SqlAggregationProcessor IllegalArgumentException → NopException

- `NopMetadataErrors.java`: 新增 `ERR_AGGR_UNSUPPORTED_TABLE_TYPE`（aggr 域，已复用 `ARG_TABLE_TYPE`）
- `SqlAggregationProcessor.java`: 裸 `IllegalArgumentException` → `throw new NopException(NopMetadataErrors.ERR_AGGR_UNSUPPORTED_TABLE_TYPE).param(ARG_TABLE_TYPE, tableType)`；新增 `NopException`/`NopMetadataErrors` import
- `TestSqlAggregationProcessor.java`: 新增 `testExecuteWithUnsupportedTableTypeThrowsNopException` — Mockito mock `AggregationContext` + 直接调用 `processor.execute()` 验证 `NopException` + errorCode 匹配
- 验证: `./mvnw compile -pl nop-metadata -am` 通过；`TestSqlAggregationProcessor` 7 tests 0 failures
- No owner-doc update required

### Phase 2 — 移除 nop-metadata-api 死模块

- `nop-metadata/pom.xml`: 移除 `<module>nop-metadata-api</module>`
- 物理删除 `nop-metadata/nop-metadata-api/` 目录（仅含 996 字节空 jar + pom.xml，无 src/ 目录）
- `gen-crud-api.xgen`: 清理注释中的 `io.nop.metadata.api` 引用（`apiPackageName` → `io.nop.metadata.service`）
- 验证: `grep -r "nop-metadata-api" --include="pom.xml"` = 0；`grep -r "io.nop.metadata.api" --include="*.java" --include="*.xgen" --include="*.xml"` = 0；`./mvnw compile -pl nop-metadata -am` 通过；`./mvnw test -pl nop-metadata/nop-metadata-service -am` 686 tests 0 failures
- No owner-doc update required

### 验证

- `./mvnw compile -pl nop-metadata -am` — 0 errors
- `./mvnw test -pl nop-metadata/nop-metadata-service -am` — 686 tests, 0 failures
- Plan 文件所有 checkbox 已 tick，Phase Status → completed，Plan Status → completed
- 独立子 agent closure-audit 已完成

---

## Plan 2026-07-21-1000-2 完成：nop-metadata BusinessDomain + DataProduct

执行 `ai-dev/plans/2026-07-21-1000-2-nop-metadata-businessdomain-dataproduct.md`，实现企业语义层 Phase 3。

### Phase 1 — ORM 实体定义 + Codegen

- `nop-metadata.orm.xml` 新增 5 个 dict：`meta/business-domain-type` + 4 个 `meta/data-product-*`
- 新增 `NopMetaBusinessDomain` 实体（含自引用 parentDomainId 层级、UK(parentDomainId,name)、索引、relations）
- 新增 `NopMetaDataProduct` 实体（含 FK→BusinessDomain、UK(businessDomainId,name)、索引、relations）
- 运行 codegen + mvn install 编译通过
- 新增集成测试 `TestNopMetaBusinessDomainDataProductCrud`（5 tests）：GraphQL CRUD + 父子层级 + domain→product 嵌套 + UK 声明验证

### Phase 2 — 资产实体追加 businessDomainId + 域继承机制

- 5 个资产实体追加 `businessDomainId` 列（nullable, FK→NopMetaBusinessDomain）+ 索引
  - NopMetaTable（propId=18，SCHEMA 后）、NopMetaEntity（propId=24，extConfig 前）
  - NopMetaEntityField（propId=25，extConfig 前）、NopMetaTableMeasure（propId=11，extConfig 前）
  - NopMetaTableDimension（propId=10，extConfig 前）
- 实现业务域继承机制（BizModel save hook）：
  - EntityField→Entity：未设时从父 Entity 继承
  - TableMeasure/Dimension→Table：未设时从父 Table 继承
  - Table/Entity：用户显式赋值或 null
- 运行 codegen + mvn install 编译通过

### Phase 3 — DataProduct 资产关联

- `NopMetaDataProductBizModel` 新增：
  - `linkAsset(dataProductId, entityType, entityId)` 创建 TagLabel（source=Classification, labelType=Automated, state=Suggested, metadata 含 dataProductId）
  - `unlinkAsset(dataProductId, entityType, entityId)` 删除匹配 TagLabel，找不到抛 ERR_LINK_ASSET_NOT_FOUND
  - `getLinkedAssets(dataProductId)` 查询按 dataProductId 关联的资产
- `NopMetadataErrors.java` 新增 `ERR_LINK_ASSET_NOT_FOUND` + `ERR_LINK_ASSET_ENTITY_TYPE_INVALID`
- 新增集成测试 `TestNopMetaDataProductLinkAsset`（6 tests）
- 685 tests pass

### Doc Updates

- `11-enterprise-semantic-layer.md` 更新 Phase 3 为已实现
- `01-architecture-baseline.md` 更新日期
- `nop-metadata-roadmap.md` S3 标记为 done

## Plan 305 完成：nop-stream DataStream API 完善（ProcessFunction）

执行 `ai-dev/plans/305-nop-stream-datastream-api-completeness.md`，实现 ProcessFunction 全链路。

### Phase 1 — ProcessFunction 核心实现

- `RuntimeContext` 增加 `getKeyedStateStore()` + `getTimerService()` 默认方法（抛 UnsupportedOperationException）
- `StreamingRuntimeContext` 增加 `keyedStateStore`/`timerService` 字段 + setter
- 创建 `ProcessFunction<IN, OUT>` 抽象类：`processElement()` + `onTimer()` + 非静态内部类 `Context`/`OnTimerContext`
- 创建 `KeyedProcessFunction<K, IN, OUT>` 继承 ProcessFunction，增加 `getCurrentKey()` 上下文
- 创建 `ProcessOperator<IN, OUT>`（继承 AbstractUdfStreamOperator，实现 OneInputStreamOperator + Triggerable）
  - `open()` 中创建 `HeapInternalTimerService` 并注册到 `TimerServiceManager`
  - `open()` 中注入 `keyedStateStore`（从 `keyedStateBackend`）和 `timerService` 到 `StreamingRuntimeContext`
- `DataStream` 接口 + `DataStreamImpl` 增加 `process(ProcessFunction<T, R>)` 方法
- `KeyedStream` 接口 + `KeyedStreamImpl` 增加 `process(KeyedProcessFunction<K, T, R>)` 方法
- `InternalTimerServiceTimerWrapper` 桥接 `InternalTimerService` → 用户级 `TimerService`

### 测试

新增 `TestProcessOperator.java`（6 个测试）：
- `testProcessElementIsCalled` — processElement 调用 + Collector 输出
- `testContextTimestamp` — Context.timestamp() 正确返回时间戳
- `testOnTimerViaWatermark` — registerEventTimeTimer + watermark 推进触发 onTimer
- `testKeyedStateAccess` — ValueState 在 KeyedStream 上下文的读写
- `testTimerServiceCurrentWatermark` — currentWatermark() 返回推进后的值
- `testMultipleOutputElements` — 单次 processElement 多输出

### 验证

- `./mvnw compile -pl nop-stream/nop-stream-core -am` — 0 errors
- `./mvnw test -pl nop-stream/nop-stream-core -am` — 935 tests, 0 failures, 0 errors（新增 6 test）
- 独立子 agent closure-audit（ses_07e231780ffeL5lFJHVGGtAdoM） — 12/12 全部 PASS

### Doc-sync

- No owner-doc update required（纯新增 API，不影响现有行为）

## Plan 304 完成：nop-stream JobGraph 管线缺陷修复

执行 `ai-dev/plans/304-nop-stream-jobgraph-defect-fix.md`（4 个 Phase），所有修复编译+测试通过。

### F2 — OperatorChain 生命周期缺失（Phase 1）

`StreamTaskInvokable.java` 的 invokeSource/invokeMiddle/invokeSink/invokeSelfContained 四个分支都未调用 `operatorChain.open()`/`close()`。四个分支均已修复，open 在记录处理前调用，close 在 finally 块中保证执行。

### F8 — fanOutWriters + inputGate 构造冲突（Phase 2）

`GraphExecutionPlan.build()` 当 vertex 同时有 fanOutWriters 和 inputGate 时选择 `StreamTaskInvokable(chain, fanOutWriters)` 构造器（inputGate=null），导致 subtask 被错误分类为 SOURCE 角色而非 MIDDLE。

修复：
- 在 `StreamTaskInvokable` 新增第 4 个构造器 `(OperatorChain, List<RecordWriter>, InputGate)`
- `GraphExecutionPlan.build()` 中先检查 inputGate 再决定构造器

### F1 — ChainingStrategy 架构补全（Phase 3）

- `ChainingStrategy` 枚举已存在（ALWAYS/NEVER/HEAD/HEAD_WITH_SOURCES）
- `StreamOperatorFactory` 接口增加 `getChainingStrategy()` 默认方法返回 `ALWAYS`
- `SimpleStreamOperatorFactory` 增加 `chainingStrategy` 字段 + setter + 构造器参数
- `StreamNode` 增加 `chainingStrategy` 字段及 getter/setter
- `StreamGraphGenerator` 所有 transform 方法传播 operatorFactory 的 chainingStrategy 到 StreamNode
- `JobGraphGenerator.canChain()` 增加 ChainingStrategy 检查：upstream 为 NEVER 或 downstream 为 NEVER/HEAD 则不可链化
- `WindowedStreamImpl.transform()` 为窗口算子设置 `ChainingStrategy.NEVER`

### F9+F4 — JobEdge equals/hashCode + 死代码清理（Phase 4）

- `JobEdge` 增加基于 sourceVertex + targetVertex + partitionType 的 equals/hashCode
- 删除 `OperatorChain.processElement()` 死代码及其 Javadoc 引用

### 验证

- `./mvnw compile -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime -am` — 0 errors
- `./mvnw test -pl nop-stream/nop-stream-core -am` — 929 tests, 0 failures, 0 errors
- `./mvnw compile -pl nop-stream/nop-stream-runtime -am` — 0 errors

### Doc-sync

- No owner-doc update required (all internal behavior fixes)
- `ai-dev/audits/2026-07/2026-07-20-2100-adversarial-review-nop-stream-jobgraph.md` — Audit Status: planned → closed
- Plan file fully updated: all items ticked, Plan Status → completed

---

## Plan 2026-07-21-1000-1 完成：NopMetaTagLabel Governance Workflow

执行 `ai-dev/plans/2026-07-21-1000-1-nop-metadata-taglabel-governance-workflow.md`，为 NopMetaTagLabel 接入 nop-wf 审批流。

### Phase 1 — ORM 字段追加 + xmeta + 工作流定义

- `NopMetaTagLabel` ORM: 新增 `approveStatus`/`approvedBy`/`approvedAt` 列 + 实体级 `tagSet="use-approval"`
- `NopMetaTagLabel.xmeta`: 配置 `wf:wfName="tagLabelConfirmApproval"`
- `NopMetadataErrors.java`: 新增 `ERR_TAG_LABEL_NOT_FOUND`、`ERR_TAG_LABEL_INVALID_LABEL_TYPE` + `ARG_TAG_LABEL_ID`、`ARG_LABEL_TYPE`
- 创建 `tagLabelConfirmApproval/v1.xwf`（reviewer-check 单步审批，`wf-approval:notifyResult` 回调）
- codegen 生成 `_NopMetaTagLabel.xbiz` 含 `x:extends="/nop/wf/base/approval-support.xbiz"`

### Phase 2 — BizModel Hooks + 自动审批触发

- `NopMetaTagLabelBizModel`: save hook 根据 labelType 自动触发审批（Manual→Confirmed，Derived/Propagated/Automated→submitForApproval）
- `NopMetaTagLabelBizModel.propagateFromGlossaryTerm`: DAO save → BizModel invoke
- `NopMetaGlossaryTermBizModel.syncTagLabels`: DAO save → BizModel invoke
- `NopMetaTagLabel.xbiz` retention: override approve/reject actions（设置 state + approveStatus + approvedBy/approvedAt + 驳回理由）
- 新增 `TestNopMetaTagLabelApproval`（6 单元测试）：Manual/Derived 状态转换 + approve/reject GraphQL 路径

### Phase 3 — 端到端集成测试

- 新增 `TestNopMetaTagLabelApprovalIntegration`（4 集成测试）：Manual 跳过审批 + approve/reject GraphQL + 幂等性
- 添加 `nop-wf-service` 为 test 依赖（为后续真实 wf 运行时测试提供基础设施）
- 674 tests 全部通过（6 新单元 + 4 新集成 + 664 存量）

### Doc-sync

- `11-enterprise-semantic-layer.md`: 补充 TagLabel 三个审批字段描述
- `12-data-contract-and-governance-workflow.md`: Phase 2 标记为已实现；修复过期的 `afterApproved`/`afterRejected` 引用为 `approve`/`reject`；移除不存在的 `action` 属性
- `nop-metadata-roadmap.md`: G2/S2-5 全部标记为 done
