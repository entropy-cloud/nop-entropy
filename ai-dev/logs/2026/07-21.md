# 2026-07-21

## Plan 2026-07-20-2000-1: nop-metadata Glossary Phase 2 — Closure Audit

**Plan**: `ai-dev/plans/2026-07-20-2000-1-nop-metadata-glossary-phase2.md`

**Audit result**: PASS — approved for closure

**Phase 1 (ORM 实体 + Codegen)**:
- `NopMetaGlossary` entity with all columns (glossaryId PK, name UK, displayName, description, owner, reviewers json, mutuallyExclusive, namespaces json, extConfig json, audit fields)
- `NopMetaGlossaryTerm` entity with all columns (synonyms/relatedTerms/references/conceptMappings json, iri, tags json, extConfig json) + UK `(glossaryId, fullyQualifiedName)` + index `IX_NOP_META_GLOSSARY_TERM_PARENT`
- `NopMetaTagLabel.glossaryTermId` FK constraint via to-one relation → NopMetaGlossaryTerm
- Codegen output: DAO entities (_gen/*.java), xmeta, i18n, web CRUD pages, action-auth
- Test: `TestNopMetaGlossaryCrud` (4 tests: CRUD + term hierarchy + TagLabel by glossaryTermId + childTerms relation)

**Phase 2 (FQN Auto-Generation)**:
- `NopMetaTagBizModel.save` auto-generates fullyQualifiedName: root tags = `{classification.name}.{tag.name}`, child tags = `{parent.FQN}.{tag.name}`
- Manual FQN preserved when explicitly provided
- Test: `TestNopMetaTagBizModel` (4 tests: root/child/grandchild/manual override)

**Phase 3 (Auto-Propagation Engine)**:
- Path A: `NopMetaGlossaryTermBizModel.syncTagLabels` creates/removes Derived TagLabels from tags JSON with full diff (idempotent)
- Path B: `NopMetaTagLabelBizModel.propagateFromGlossaryTerm` creates derived TagLabels when asset tagged with Glossary source
- Test: `TestNopMetaGlossaryTermPropagation` (6 tests: create/add/remove/idempotent for Path A and Path B)

**Phase 4 (Seed Data)**:
- `SeedGlossaryData.seedGlossaryTerms` creates BuiltIn Glossary + 9 seed terms from NopMetaSemanticType, idempotent
- Triggered from `NopMetaModuleBizModel.releaseModule`
- Test: `TestSeedGlossaryData` (1 test: 9 terms + idempotency)

**Total new tests**: 15 (all passing)

**Anti-Hollow Check**:
- Call chain verified: GraphQL → GlossaryTermBizModel.save → syncTagLabels → TagLabel DAO
- Call chain verified: GraphQL → TagLabelBizModel.save → propagateFromGlossaryTerm → TagLabel DAO
- Call chain verified: releaseModule → seedGlossaryTerms → Glossary/GlossaryTerm DAO
- No empty method bodies, no swallowed exceptions, no silent skip patterns

**Post-Closure Bug Fixes (MISSION_DRIVER)**:
- Fixed `System.currentTimeMillis()` → `CoreMetrics.currentTimestamp()` in `NopMetaTagLabelBizModel` and `NopMetaGlossaryTermBizModel` (violated `TestCoreMetricsUsage`)
- Fixed UUID length (36→32 chars) in `SeedGlossaryData` and `TestSeedGlossaryData` (IDs exceed VARCHAR(32) precision)
- Fixed NPE in `releaseModule` — used `daoProvider` field (non-injected, shadowed parent's private field) instead of `daoProvider()` method
- All 663 tests pass (0 failures, 0 errors)

**Doc-sync**:
- `ai-dev/design/nop-metadata/11-enterprise-semantic-layer.md`: Phase 2 status updated to ✅ 已实现
- `ai-dev/design/nop-metadata/nop-metadata-roadmap.md`: S2-1 through S2-4 marked `done`
