# 2026-09-23

### maker-checker 审核页架构 R4 修正（去 schema 生成端点，改 Flux 内置加载 + 表达式标注，无 plan——设计迭代）

- 承接用户质疑"是否必须新增 `getPageSchema(recordId)` 生成端点？还是用 flux 内置动态加载？"——补做源码调研后**推翻 R3 的生成端点裁决**。
- **新证据（nop-chaos-flux 源码）**：① `DynamicRenderer`（flux-core `DynamicRendererSchema`：`type:'dynamic-renderer'` + `loadAction`/`autoLoad`，响应 data 须为 schema 对象）——Flux 内置动态 schema 加载能力存在；② `FormSchema.loadAction/autoLoad`（flux-renderers-form `form-load-action.ts`）——表单数据运行时拉取为内置能力；③ flux-compiler `runtime-value-compilation.ts`——schema 属性值编译为 static/dynamic 两类 runtime value（嵌套对象递归编译），dynamic 值渲染期对数据作用域重求值——表达式标注机制存在；④ `field-frame.tsx` `labelRemark/remark/description/hint` 渲染链路（chrome rules `value-or-region`/`prop`）确认挂载点。
- **R4 裁决**：审核页 = per-bizObj 静态 review 页（与 main.page.yaml 同族一行 GenPage 调用，PageProvider 按路径缓存；页内零记录数据故缓存安全）；表单经内置 `loadAction` 调 `getReviewDetail(recordId)`（expectedAfter→表单数据域按 name 绑定；beforeData/changedPaths→页面作用域）；变更字段 `inputClassName`/`description` 写 `${}` 表达式（如 `${includes(changedPaths,'price') ? '原值: '+beforeData.price : ''}`）渲染期求值。**不新增 schema 生成端点**；后台回调时机 = `getReviewDetail` 数据加工管线（掩码→merge→可选 `IMakerCheckerReviewDecorator` SPI）。
- **保留 fallback（写明收敛条件）**：R3 的 `getPageSchema(recordId)` 内联方案保留为备选——若 spike 证明 labelRemark/description 表达式未端到端求值（field-frame `title` 须收求值后字符串）或子表行级表达式路径不可用，则回退。
- 文档修订：`04-review-ui-diff-contract.md` §一/§五/§六 R4 重写；`01-architecture-baseline.md` 边界与 API 表同步。
- 偏差：R3 设计（生成端点静态内联）被本修正取代，属于设计迭代而非实现返工（未进入实现阶段）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0。
- Doc-sync: No owner-doc update required。

### DeepWiki 开源替代品调研——12 个项目分析文档入库

- 对 `~/sources/deepwiki/` 下载的 11 个开源项目（12 个目录，含两个 openwiki 同名冲突）进行系统化分析，为每个项目撰写处理流程与抽取模板分析文档。
- 分析维度：项目架构、源代码解析、处理管线、提取模板/提示工程、关键设计决策。
- 产出 12 份文档写入 `ai-dev/analysis/deepwiki-survey/`（编号 01-12）：
  - 01-langchain-openwiki.md — LangChain 官方 CLI，DeepAgents 双层 Agent 编排 + Grounded Claims
  - 02-deepwiki-open.md — 最完整自托管 RAG+LLM 管线，FAISS 向量检索
  - 03-codewiki.md — ACL 2026 论文，LLM 驱动模块聚类 + 递归 Agent 文档生成
  - 04-repowiki-he-yufeng.md — 最轻量零依赖，PageRank 阅读路径 + 4 Pass LLM 管线
  - 05-shariqriazz-openwiki.md — Rust 10 crate 模块化，Tree-sitter + Tantivy 混合搜索
  - 06-repowiki-cli.md — Python CLI，AST+regex 三级深度提取，Ollama 优先
  - 07-repowiki-plugin.md — Claude Code 插件，4 Agent 并行编排
  - 08-pacifio-open-wiki.md — TypeScript，Worker Pool 并行解析 + fumadocs MDX 渲染
  - 09-repowiki-ikrasnodymov.md — Go 零依赖，Git hook 自动触发 + 三层循环防护
  - 10-git-wiki.md — Agent Skill 标准，Karpathy LLM-wiki 模式，qmd 混合搜索
  - 11-opendeepwiki.md — .NET 10 SaaS，4 个 Prompt 文件驱动 catalog/content/mindmap/incremental
  - 12-repositories-wiki.md — TypeScript 三层 LLM (Opus/Haiku/Sonnet) + Tree-sitter
- 数据来源：RESEARCH.md 调研报告、download-deepwiki-repos.sh 下载脚本、各项目源码深度阅读。
- Doc-sync: `ai-dev/analysis/deepwiki-survey/` 为新目录，无需更新 docs-for-ai INDEX.md（analysis 临时目录，非规范性文档）。
- 验证：12 份文档均成功生成，格式统一（metadata block + 结构化章节 + Open Questions + References）。

### maker-checker 审核页渲染 R3 修订（锁定 Flux / 单表单控件级标注，无 plan——设计迭代）

- 承接用户反馈三点：① 不考虑 AMIS，只做 Flux 渲染器；② 双栏对照太复杂，改单表单、控件级自动变换；③ 或以注释（悬浮）显示旧值。
- **源码核验**：Flux 字段基接口 `BoundFieldSchemaBase`（nop-chaos-flux flux-core `types/schema.ts`）原生携带 `labelRemark {icon,content,placement,trigger}`（字段旁图标+悬浮内容）、`description/hint`、`inputClassName`、`readOnly`——"注释显示旧值 + 字段高亮"零渲染器改动即可表达。
- **设计决策**：① 单表单对比（数据 = expectedAfter），否决双栏（无开源实现采用：布局成本高/窄屏不可用/对照负担大；Syncope 亦为单表单逐字段标注）；② 两种字段级呈现——标注模式（缺省，labelRemark+inputClassName）与行内变换模式（二期，flux-renderers-form compare 变体：新值+旧值删除线）；③ **旧值与变更判定在 schema 生成期静态内联**——审核页 schema 按 recordId 动态生成时服务端已持有快照，旧值文本直接固化为 schema 字面量，规避运行时表达式依赖与 remark.content 表达式能力不确定性，未变更字段零开销。
- 文档修订：`ai-dev/design/maker-checker/04-review-ui-diff-contract.md` R3 重写（Flux 挂载点源码核验节 / Mode A 单表单标注 / 落地机制 / 单表单线框图）；`01-architecture-baseline.md` 两处表述同步（去双栏、生成期消费 beforeData/changedPaths）。
- 偏差：无。纯设计任务，未改产品代码。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0（本任务文件零问题）。
- Doc-sync: No owner-doc update required（设计迭代未涉 docs-for-ai 约定变化）。

### nop-batch history/事务原子性修复——consume scope 自动提升为 process 包装

- 无 plan（ad-hoc 缺陷修复）；根因：`BatchTaskBuilder.buildChunkProcessor` 中 `scope=consume`（默认）时 `InvokerBatchConsumer` 先包装 base consumer（内层），`WithHistoryBatchConsumer` 后包装（外层），导致 `saveProcessed` 在业务事务提交**之后**执行——崩溃窗口会造成重启后重复处理；`DaoBatchRecordHistoryStore` 注释声称"同事务"与实际相反。
- 修复（自动提升方案）：`buildChunkProcessor` 检测到 `historyStore != null && scope==consume && transactionalInvoker != null` 时，把 consumer 链的事务包装位置提升到 process 级（`InvokerBatchConsumer` 包在 `WithHistoryBatchConsumer` 外层），打 `nop.batch.history-txn-promoted` INFO 日志；`AddCompleted`/chunk 级包装仍按原 `batchTransactionScope` 判断不受影响。副作用：提升后 processor 也进事务（已写入文档）。
- 同步修正 `DaoBatchRecordHistoryStore.saveProcessed` 反了的注释。
- 新增回归测试 `nop-batch-core/.../TestHistoryTxnPromotion.java`（4 例）：consume+history 提升后 save/consume 均在 invoke 内；consume 无 history 不变；process+history 保持；legacy 错误嵌套 characterization（save 在事务外）。
- 验证：`./mvnw test -pl nop-batch/nop-batch-core -am` EXIT=0（core 全量含新 4 例，0 failures）；`./mvnw test -pl nop-batch/nop-batch-dao -am` EXIT=0（3/3）；`node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0（0 errors，26 warnings 均为既有无关 plan）。
- Doc-sync: `docs-for-ai/02-core-guides/batch-dsl.md` transactionScope 一节重写（含提升语义+四 scope 对照表+源码锚点）；`docs-for-ai/03-modules/nop-batch.md` 修正过时的"historyStore 写路径尚未实现"（实际 `saveProcessed` 已落库并有测试）+ 关键配置项补 `transactionScope`/`historyStore`。
- 偏差：无。

### 对标路线图入库——knowledge-rag + ontology-semantic（WeKnora / OSSIE，审查两轮共识）

- 无 plan（roadmap 新建 + 独立草案审查，非实现）；从 nop-app-erp `docs/backlog/` **整体迁入**本仓 `ai-dev/backlog/`（工作项主落点=平台，与 ERP 应用无关；应用侧 P9/P10 登记已删）。
- 落盘：
  - `ai-dev/backlog/knowledge-rag-roadmap.md` — WeKnora 缺口，18 WI / 5 里程碑（K1–K18），全 `todo`。
  - `ai-dev/backlog/ontology-semantic-roadmap.md` — OSSIE 缺口，12 WI / 5 里程碑（O1–O12），全 `todo`；O1 强制对账分支 `feature/nop-ontology` 21-WI，禁止恢复 ontology.xdef-on-nop-metadata。
- 关键裁决：SQL/表达式方言完全归 EQL `dialect.xml`；多方言表达数组 / OSSIE_SQL_2026 / 可移植 SQL 子集 / RDF 栈均范围外。
- 独立草案审查两轮（fresh 子 agent，证据=两文件文末 Draft Review Record）：R1 `ses_f337528c9ffeGQiCsGJ70TxPB4` needs revision（4 Blocker + 8 Major）→ 修复；R2 `ses_f33605c79ffeSpizwg4HrhWWgU` **passes draft review**（0 Blocker + 0 Major；8 Minor + 1 Nit 已同批回写）。
- 迁入时路径修正：AGE 模板跨仓引用 nop-app-erp `docs/backlog/00-roadmap-authoring-guide.md`；owner-doc NEW 路径改 `ai-dev/design/nop-ai/` 与 `ai-dev/design/nop-metadata/`；删除 `../nop-entropy` 自指。
- 验证：文件计数 K=18 / O=12 与 §2 汇总一致；表-图依赖已对齐；本仓 backlog 无强制 README 索引，以文件本身为登记面。
- 偏差：审查在迁入前于应用仓上下文完成——内容未变仅搬路径，Draft Review Record 保留原 agent 指针，不重跑审查。
- Doc-sync: 本条日志；No owner-doc update required（全部 owner doc 尚为 NEW 未建）。
- 下一步：WI `todo → ready` 仍须 per-item 独立草案审查；O1 开工前与 21-WI 逐 WI 对账。

### nop-rg 第四轮深度审计 Phase 1——G1-G6 质量与契约修复（plan 2276）

- Plan 2276（`ai-dev/plans/2276-nop-rg-round4-deep-audit-quality-perf.md`）Phase 1 完成：两轮对抗性 draft review（agent_2a085865 / agent_9396ca9f，均无 Blocker）后 active。本轮审计新发现并修复：
  - **G1（live 契约偏差）**：字面量枚举 `from = pos + 1` 报告自重叠命中（rg 为非重叠）——SearchCoordinator 两条字面量路径改报告侧 lastReportedEnd 过滤（扫描推进 +1 保持，跨 chunk 持续；draft review 实证不能改推进步长 `pos+patternLength`，分块路径会漏配）；SearchCoordinator 类 javadoc 新增枚举语义。
  - **G2（live defect）**：`NopRgMain.call()` 只捕 `NopRgException|IAE`，裸 `NopException`（CoreInitialization/VFS/GitIgnoreFile 层）逃逸至 picocli 默认处理 → exit 1 与「未命中」混淆，违反「错误 2」契约——catch 放宽为 `NopException|IllegalArgumentException`；`search(Path)` 收窄包私有作测试缝。
  - **G3**：JsonOutput `escape(path)` 每文件一次（原每条 match 消息重复转义；输出字节不变）。
  - **G4**：`--vector` help 文本与 cli README「静默降级」措辞纠正（实际降级时打印 stderr 提示）。
  - **G5**：CorpusUtil 三处内联 FQN + 一处 `var` 改 import/显式类型（F5 家族）。
  - **G6**：`searchFile` 免双列表拷贝（直接消费 literalSpans/regexSpans 返回值）。
- 新增聚焦测试：SearchCoordinatorTest 2 例（非重叠枚举 whole-file `aa`×`aaaaaa` = {0,2,4}、chunked 小阈值 `aaa`×`xxaaaaaax` = {2,5} 跨 chunk 压制，修复前语义下必然失败）；NopRgMainTest 1 例（覆写 search 抛裸 NopException → exit 2 + stderr）。
- 验证：`./mvnw test -pl nop-rg/nop-rg-core,nop-rg/nop-rg-cli,nop-rg/nop-rg-vector` EXIT=0——core **59 run + 1 skip**（基线 56+1 + 新增 3）、cli **28**（27 + 新增 1；RgComparisonTest 9/9 默认跑，rg 15.1.0 在 PATH，JSON submatch 元组与 rg 逐项一致）、vector 10；large-file 显式组 `-DexcludedGroups= -Dgroups=large-file -DargLine=-Xmx256m` **3/3** EXIT=0；`./mvnw compile -pl nop-rg/nop-rg-benchmark` EXIT=0（CorpusUtil 改动）。
- 偏差：无（G2 测试按 draft review 处方用匿名子类覆写 + `ErrorCode.define` 构造裸 NopException）。
- Doc-sync：cli README（G4 措辞）；SearchCoordinator javadoc（G1 枚举语义）；`ai-dev/design/nop-rg/01-architecture-baseline.md` 经复核无匹配枚举语义章节 → **No design-doc update required**（javadoc 为唯一契约载体，plan 预授权分支）。
- Doc-sync（docs-for-ai）：`01-repo-map/module-groups.md` 的 nop-rg 行为模块级概述，不涉枚举语义细节 → No update required。

### nop-rg plan 2276 Phase 2——row0 基线重建

- m2 快照 install（commit 880329fc52 同源 core+cli）；row0 count 双跑（p2276-row0-count-{a,b}.json）：1MB 830.55/951.43、64MB 50.38/45.04（±26% 外部负载噪声）、512MB 13.24±0.66 / 12.76±0.37（±3-5% 稳定）；TEXT 64MB 12.23±0.46（2275 12.09±0.74 同区间）；many-small 34.39±1.08（同区间）。
- 三口径 profile（p2276-profile-*.log）：count/many-small 与 2275 终态同构（byteAt+indexOf+matchesAt 扫描地板；byteAt 43.5/aggregate L49 30.4 系帧间归因漂移非结构变化）——「不再评估」清单维持；TEXT：Writer 31.2%（输出必需）、print:41 10.9%（2275 P1 已证优化方向为回退）、buildLineMatches L71 7.1%（P1 依据）、submatch add 1.0%（P2 预否决依据）。
- P2（submatches 单命中行 List.of）实现前否决：profile 占比 1.0% + 算术上界 ≪1%；walker synchronized 预否决 ≤0.1%（沿 2275 P3 先例）。
- 验证：判定 JSON 与 profile log 落 `_tmp/nop-rg-bench/`（p2276-* 命名）；README 基线漂移 <10% 裁定不动。

### nop-rg plan 2276 Phase 3——收敛循环至字面终止

- P1（LineMatch lineWithTerminator 惰性拼接，免 TEXT 口径每命中行全行拷贝）：迭代档 TEXT 64MB 候选两跑 12.979±0.33 / 13.165±0.24（名义 +6~8% 通过筛选）→ 收尾共测配对 3 对 **-0.01% / +0.46% / -0.65%（中位 -0.01%，3 对 CI 全重叠，σ_pair 0.56%）**→ 未过 max(2%, 3σ) 保留条件 → **回退**（`git diff HEAD -- nop-rg/` 零残留）。筛选档名义收益为基线侧环境漂移伪影——共测配对（基线 = m2 jar）正确对消；机理：concat 分配隐藏在输出 Writer 内存停顿空隙，消除不可辨。
- P2（submatches 单命中行 List.of）实现前否决：row0 TEXT profile submatch add 1.0% + 算术上界 ≪1%。walker synchronized 预否决 ≤0.1%。
- **循环终止（字面条款：候选池枯竭）**：count/many-small 终态 = 扫描带宽地板（五轮实证：2267 R2 SWAR/R3 span-gap/R4 单遍融合 + 2273/2275 复验 + 本轮 profile 无结构漂移）；TEXT 残余 = 输出 Writer 契约必需 + 行解码 + 输出拼接（2275 P1 分段 print 实测更慢）；vector 12B 已闭合。
- 吞吐比收尾：**64MB 56.3% / 512MB 51.6%，双档 ≥50% 达标**（p2276-final-*.json；512MB 与历史 49-53% 区间一致，64MB coord 侧 ±30% 负载噪声、rg 侧 80.28 与历史恒定）。
- 验证：回退后全量 `./mvnw test -pl nop-rg/nop-rg-core,nop-rg/nop-rg-cli,nop-rg/nop-rg-vector` 全绿（core 59+1skip / cli 28 / vector 10）+ large-file 3/3；判定 JSON 落 `_tmp/nop-rg-bench/p2276-p1-pair*` 与 `p2276-final-*`。
- 偏差：配对首轮脚本因 zsh 变量不分词导致 JMH 参数失效（未产生判定数据），改显式参数重跑——数据完整性不受影响。
- Doc-sync：无 kept 优化 → benchmark README 不动、design 01 No update required（裁定记录于 plan Phase 3）。

### nop-rg plan 2276 收口——独立 closure audit APPROVE-CLOSURE

- 独立子 agent closure audit（fresh session，agent_893d370e）：**APPROVE-CLOSURE**——Phase 1-3 Exit Criteria 逐条 PASS；独立复跑 core 59+1skip / cli 28（RgComparison 9/9）/ vector 10 + large-file 3/3 + benchmark compile 全 EXIT=0；G1/G2 接线独立追踪 + G1 语义推演（aba×ababa={0} 与 rg 一致）；P1 配对数据逐位复算（-0.01/+0.46/-0.65%）+ 回退 `git diff 880329fc52..HEAD -- nop-rg/` = 0 行；吞吐比独立复算 56.3%/51.6%；m2 与 target/classes 字节级 cmp 一致；文本一致性五处核对通过；Deferred 分类诚实。1 Minor（记录表 rg scoreError 舍入 0.61→0.60）已随收口修正。
- 三工具：check-plan-checklist --strict EXIT=0（completed 态）、scan-hollow --module nop-rg --severity high EXIT=0（0 findings）、check-doc-links --strict EXIT=0（0 errors，26 warnings 全为他计划既有文件）。
- 遗留 follow-up：--follow、多小文件 syscall 面、测试 import 分组（均延续登记）+ JSON 口径基准（本轮新登记）。
- Plan Status: completed（Completed: 2026-09-23）。

### Code Graph 分析项目调研——10 个开源项目综合对比 vs nop-code

- 对 `~/ai/` 目录下 10 个 code graph 分析项目进行系统化调研，更新此前的两份对比报告（2026-06-05-codegraph-vs-nop-code、2026-05-25-code-review-graph-vs-nop-code）为一份综合报告。
- 调研项目：CodeGraph、code-review-graph、GitNexus、codeflow、ast-outline-rs、understand-anything、graphify、ontology-driven-agent、entrix、DeepCode。
- 关键发现：
  - **GraphQL 已是通用 API**：nop-code 的 BizModel + `IGraphQLEngine` 已提供 universal API 暴露，无需 MCP。10 个竞品使用 MCP 是因为它们是独立工具，缺乏平台级 API 框架。nop-code 作为 Nop 平台模块，GraphQL 已足够。
  - **Tree-sitter 普遍**：8/10 项目使用 tree-sitter 解析，nop-code 仅 Java 用 JavaParser，Python/TS 用 tree-sitter Java 绑定。
  - **nop-code 已有深度分析能力**：社区检测（Leiden+LabelPropagation）、执行流追踪、风险评分变更分析、语义边发现、关键节点分析、图快照对比——这些是大多数竞品不具备的。
  - **graphify 切换到 main 分支**：本地 v6 分支已与 origin/v6 同步，origin/main 有 v2 重大更新（hypergraph 支持、语义相似度边、git hook 自动重建等 36+ commits）。
- 产出综合报告 `ai-dev/analysis/2026-09-23-codegraph-survey-vs-nop-code.md`（672 行），涵盖：跨项目特征矩阵、技术栈对比、AI 集成模式分析、Gap Analysis、P0-P2 优先级建议、nop-code 独特优势。
- **架构决策（2026-09-23，共 4 项）**：
  1. **不引入 MCP**：GraphQL 通过 BizModel 已提供 universal API 暴露（嵌套查询 `@BizLoader`、权限控制 `@Auth`、类型安全、Delta 支持）。AI 客户端通过标准 GraphQL HTTP POST 调用即可。决策记录于 `docs-for-ai/02-core-guides/api-and-graphql.md`。
  2. **存储抽象**：**复用现有 `io.nop.graph.api.IGraph`**（`nop-graph-api`）作为存储抽象边界；`CallGraph`/`SymbolTable` 是内存实现，`CodeCallGraph` 是适配器。不新造 `ICallGraph`/`ISymbolTable`（校正：初版误引入了不存在的接口，经独立审查纠正）。
  3. **集群索引 + 无状态**（目标）：nop-code 定位为集群式索引服务；索引构建可分布式并行，查询路径无状态、可水平扩展，全局算法结果索引期物化。当前用 `CodeCacheManager` 堆内缓存 + 全量 rebuild，属待迁移。
  4. **框架适配不入核心 + 利用数据库图索引**（目标/待决策）：核心只处理通用 AST 解析和 Nop 平台自身；框架适配由 SPI（`IEntryPointPatternProvider`）可插拔、DSL 为远期。生产环境评估 PostgreSQL 图索引（ltree/递归 CTE/Apache AGE），需先定生产 DB 与可移植性；全局算法无法由递归 CTE 求得。
- 核心建议：短期（P0）修复 sourceCode/indexId bug（已完成）、补 xmeta（已完成）、集成 nop-search、增强 `IGraph` 边投影；中期（P1）无状态查询路径、全局算法物化、集群索引、Token 效率、TS 调用图；长期（P2）框架适配 SPI/DSL、交互式可视化、GraphRAG、评测框架。
- 文档更新：
  - `docs-for-ai/02-core-guides/api-and-graphql.md` 新增第 7 条默认结论（GraphQL 已是通用 API，无需 MCP）
  - `docs-for-ai/03-modules/nop-code.md` 新增 "API 暴露策略" 与 "架构原则" 章节
  - `ai-dev/design/nop-code/00-vision.md` 定位改为"集群式服务"，约束增至 9 条、不变量增至 10 条、拒绝清单增 3 项
  - `ai-dev/design/nop-code/01-architecture-baseline.md` 新增 §4.4.1 图存储抽象接口 + §6.2 待办更新
  - `ai-dev/analysis/2026-09-23-codegraph-survey-vs-nop-code.md` 全文去除 MCP 推荐，新增存储抽象/无状态/集群/DB 图索引方向
- **graphify v2 增强吸收决策**：逐模块对比 graphify v2 管线（detect→extract→build→cluster→analyze→report→export），结论为核心功能已全覆盖。吸收 4 项（意外连接复合惊奇评分、图谱问题生成、图谱 Wiki 导出、自动重建改为 VCS 事件驱动），已覆盖 3 项（语义相似度边/置信度/graph_diff），暂缓 1 项（Hypergraph），拒绝 2 项（MCP/Obsidian）。新建设计文档：
  - `ai-dev/design/nop-code/graph-discovery-and-export-design.md`（新增，含 §3.1 惊奇评分维度表、§3.2 问题类型表、§3.3 Wiki 结构、§3.4 VCS 事件驱动重建、§四拒绝清单）
  - `ai-dev/design/nop-code/graph-analysis-design.md` §六 更新为指向新文档
  - `ai-dev/design/nop-code/README.md` 新增文档索引与阅读顺序
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0。
- Doc-sync: `docs-for-ai/` 已更新（api-and-graphql.md、nop-code.md）。

### nop-code 文档独立审查（3 轮）——幻影架构纠正 + 功能补全 roadmap

- 用独立 fresh-session 子 agent 对 nop-code 的设计文档做 3 轮对抗审查，发现并修正**严重文档-代码 drift**：
  - **幻影模块 `nop-code-graph`**：实际已迁移为顶层共享库 `nop-graph/`（`nop-graph-api` + `nop-graph-core`，含 `IGraph`/`Edge`/`LeidenDetector`/`BetweennessCentrality`/`Bfs`/`GraphExporter`/`GraphDiffer`）。`nop-code` 子模块清单中无此模块。
  - **幻影接口 `ICallGraph`/`ISymbolTable`/`InMemoryCallGraph`**：实际存储抽象是 `io.nop.graph.api.IGraph`；`CallGraph`/`SymbolTable` 是内存实现，`CodeCallGraph implements IGraph` 是适配器。用户"接口屏蔽存储形式"目标**已由现有 `IGraph` 满足**，不需新接口。
  - 其他：`igraphexporter`/`CommunityDetectionResult`/`ICommunityDetector` 等幻影类名、`flow → graph → core` 错误依赖链、语义边状态（已实现非待实现）、nop-search 状态（TEXT 非 HYBRID）、框架 IoC 自动发现（当前硬编码 Spring 待迁出）、`nop-graph` 复用范围（当前仅 nop-code，未来供 wf/task/stream）、`CodeIndexApi` 幻影段、`code-query` 废弃权限、`CommunityDetector`/`DEL_FLAG`/dict 路径/`CodeSymbolKind.ROUTE` 缺失等。
- 修正范围（4 个文档 + 2 个 docs-for-ai + 1 个分析报告）：
  - `01-architecture-baseline.md`：新增 §〇 模块边界校正；§4.4.1 重写为 `IGraph` 抽象；§4.4/§4.5 幻影接口改真实类；§6 状态表重写
  - `00-vision.md`：架构定位/约束/不变量/拒绝清单全部对齐 `IGraph` + 现状标注（✅已落地 / ⏳目标 / ⏳待决策）
  - `graph-discovery-and-export-design.md`：重写（模块归属、`IGraph` 输入投影、精确评分运算顺序/阈值/降级、机器可执行 `suggestedQuery` 模板、`GraphWikiDTO` 多文件契约、GraphQL mutation 触发自动重建、前置条件表）
  - `graph-analysis-design.md`/`flow-analysis-design.md`/`semantic-edge-design.md`/`search-integration-design.md`/`query-api-design.md`：模块归属、依赖链、dict、搜索类型、已知缺陷状态、新 API 登记
  - `docs-for-ai/03-modules/nop-code.md` + `docs-for-ai/02-core-guides/api-and-graphql.md`：架构原则区分现状/目标、去幻影引用、Leiden、TESTED_BY、dict 路径、Spring legacy
  - `ai-dev/analysis/2026-09-23-codegraph-survey-vs-nop-code.md` §8：`ICallGraph`→`IGraph`，已完成项标记
- **新建功能补全 roadmap**：`ai-dev/backlog/nop-code-feature-completion-roadmap.md` — 9 里程碑（M0 基线+drift 修正 / M1 无状态查询基座 / M2 图探索与导出 / M3 索引增量 / M4 搜索 / M5 语言解析 / M6 存储集群 / M7 AI 效率与高级分析 / M8 可视化评测 / MG 收口），32 个 Work Item（全 `todo`），覆盖调研 P0/P1/P2 全部建议 + 设计文档全部目标项 + 审查发现的文档 drift；范围外明确排除 MCP/Hypergraph/Obsidian/Elasticsearch/LSP/重构。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0。
- Doc-sync: `docs-for-ai/` 已更新（nop-code.md、api-and-graphql.md）。

### nop-code roadmap 追加 M9——AI 端到端智能验收（压轴）

- 用户要求"最后增加一个 AI 智能测试：对 nop-entropy 建索引并验证它能有效用于 Nop 平台知识获取与应用开发"。
- Roadmap 新增 **M9 里程碑 + 4 个 Work Item**（`nop-code-feature-completion-roadmap.md`，总数 32→36）：
  - N9.1 自我索引与验收基线（nop-entropy 全仓索引规模/耗时；scenario 集/rubric/对照基线）
  - N9.2 场景 A：平台知识获取（20+ 问题，对照 `docs-for-ai` ground truth 评分）
  - N9.3 场景 B：应用开发辅助（新增实体+CRUD/审批流/batch 任务，按构建测试通过+平台合规评分）
  - N9.4 验收报告与缺口回灌（未达标强制回灌 roadmap/lessons，复测通过前 M9/MG 不可收口）
- 新建验收契约 `ai-dev/design/nop-code/ai-e2e-acceptance-design.md`：两场景定义、问题域分层、评分维度、成功判据阈值、对照基线方法（实验组 vs 仅 grep/read 基线）、失败回灌闭环。
- 关键设计：M9 测**任务级有效性**（区别于 N8.3 工具自身指标）；以 nop-entropy 自我索引为最严苛真实语料；必须有对照基线以证明增益；失败不可仅记录（闭环保证"所有缺失功能补充完毕"）。
- 依赖图更新：M9 依赖全部 M1-M8，MG 收口以 M9 通过为前提。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0。
- Doc-sync: `ai-dev/design/nop-code/README.md` 新增验收层索引。
