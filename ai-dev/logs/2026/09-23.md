# 2026-09-23

### nop-rg 第四轮深度审计 Phase 1——G1-G6 质量与契约修复（plan 2276）

- Plan 2276（`ai-dev/plans/2276-nop-rg-round4-deep-audit-quality-perf.md`）Phase 1 完成：两轮对抗性 draft review（agent_2a085865 / agent_9396ca9f，均无 Blocker）后 active。本轮审计新发现并修复：
  - **G1（live 契约偏差）**：字面量枚举 `from = pos + 1` 报告自重叠命中（rg 为非重叠）——SearchCoordinator 两条字面量路径改报告侧 lastReportedEnd 过滤（扫描推进 +1 保持，跨 chunk 持续；draft review 实证不能改推进步长 `pos+patternLength`，分块路径会漏配）；SearchCoordinator 类 javadoc 新增枚举语义。
  - **G2（live defect）**：`NopRgMain.call()` 只捕 `NopRgException|IAE`，裸 `NopException`（CoreInitialization/VFS/GitIgnoreFile 层）逃逸至 picocli 默认处理 → exit 1 与「未命中」混淆，违反「错误 2」契约——catch 放宽为 `NopException|IllegalArgumentException`；`search(Path)` 收窄包私有作测试缝。
  - **G3**：JsonOutput `escape(path)` 每文件一次（原每条 match 消息重复转义；输出字节不变）。
  - **G4**：`--vector` help 文本与 cli README「静默降级」措辞纠正（实际降级时打印 stderr 提示）。
  - **G5**：CorpusUtil 三处内联 FQN + 一处 `var` 改 import/显式类型（F5 家族）。
  - **G6**：`searchFile` 免双列表拷贝（直接消费 literalSpans/regexSpans 返回值）。
- 新增聚焦测试：SearchCoordinatorTest 2 例（非重叠枚举 whole-file `aa`×`aaaaaa` = {0,2,4}、chunked 小阈值 `aaa`×`xxaaaaaax` = {2,5} 跨 chunk 压制，修复前语义下必然失败）；NopRgMainTest 1 例（覆写 search 抛裸 NopException → exit 2 + stderr）。
- 验证：`./mvnw test -pl nop-rg/nop-rg-core,nop-rg/nop-rg-cli,nop-rg/nop-rg-vector` EXIT=0——core **59 run + 1 skip**（基线 56+1 + 新增 3）、cli **28**（27 + 新增 1；RgComparisonTest 9/9 默认跑，rg 15.1.0 在 PATH，JSON submatch 元组与 rg 逐项一致）、vector 10；large-file 显式组 `-DexcludedGroups= -Dgroups=large-file -DargLine=-Xmx256m` **3/3** EXIT=0；`./mvnw compile -pl nop-rg/nop-rg-benchmark` EXIT=0（CorpusUtil 改动）。
- 偏差：无（G2 测试按 draft review 处方用匿名子类覆写 + `ErrorCode.define` 构造裸 NopException）。
- Doc-sync：cli README（G4 措辞）；SearchCoordinator javadoc（G1 枚举语义）；`ai-dev/design/nop-rg/01-architecture-baseline.md` 经复核无匹配枚举语义章节 → **No design-doc update required**（javadoc 为唯一契约载体，plan 预授权分支）。
- Doc-sync（docs-for-ai）：`01-repo-map/module-groups.md` 的 nop-rg 行为模块级概述，不涉枚举语义细节 → No update required。

### nop-rg plan 2276 Phase 2——row0 基线重建

- m2 快照 install（commit 880329fc52 同源 core+cli）；row0 count 双跑（p2276-row0-count-{a,b}.json）：1MB 830.55/951.43、64MB 50.38/45.04（±26% 外部负载噪声）、512MB 13.24±0.66 / 12.76±0.37（±3-5% 稳定）；TEXT 64MB 12.23±0.46（2275 12.09±0.74 同区间）；many-small 34.39±1.08（同区间）。
- 三口径 profile（p2276-profile-*.log）：count/many-small 与 2275 终态同构（byteAt+indexOf+matchesAt 扫描地板；byteAt 43.5/aggregate L49 30.4 系帧间归因漂移非结构变化）——「不再评估」清单维持；TEXT：Writer 31.2%（输出必需）、print:41 10.9%（2275 P1 已证优化方向为回退）、buildLineMatches L71 7.1%（P1 依据）、submatch add 1.0%（P2 预否决依据）。
- P2（submatches 单命中行 List.of）实现前否决：profile 占比 1.0% + 算术上界 ≪1%；walker synchronized 预否决 ≤0.1%（沿 2275 P3 先例）。
- 验证：判定 JSON 与 profile log 落 `_tmp/nop-rg-bench/`（p2276-* 命名）；README 基线漂移 <10% 裁定不动。
