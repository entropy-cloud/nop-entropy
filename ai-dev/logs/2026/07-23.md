# 2026-07-23 Dev Log

## Plan 2026-07-23-0900-1 nop-metadata Infrastructure & Module Boundary Fixes — completed

- **Plan**: `ai-dev/plans/2026-07-23-0900-1-nop-metadata-infrastructure-fixes.md`
- **Phase 1 (POM & BOM)**: Added `<parent>` pointing to `nop-metadata` in `nop-metadata-api/pom.xml`. Removed standalone `<version>`, `java.version=11`, `maven.compiler.*`, and explicit dep versions (now inherited from BOM). Added `nop-metadata-api` to `nop-bom/pom.xml` dependency management (alphabetically before `nop-metadata-codegen`). ✓ compile pass.
- **Phase 2 (Docker Java version)**: Changed `Dockerfile.jvm` and `Dockerfile.legacy-jar` base image from `ubi8/openjdk-17:1.16` to `ubi8/openjdk-21:1.21`. ✓
- **Phase 3 (Empty stubs)**: Removed empty `NopMetadataConstants.java` and `NopMetadataConfigs.java` (no constants needed). Verified `_dao.beans.xml` has zero residual imports (confirmed by grep) — left in place as codegen-generated. ✓ compile + 833 tests pass.
- **Verification**: `./mvnw test -pl nop-metadata/nop-metadata-api,nop-metadata/nop-metadata-service -am` → BUILD SUCCESS (833 tests, 0 failures)
- **Doc-sync**: No owner-doc update required.

## Plan 18 nop-metadata ORM Model Polish — all phases completed

- **Plan**: `ai-dev/plans/18-nop-metadata-orm-model-polish.md`
- **Phase 1 (ORM Index & Comment Fixes)**: Removed redundant `IX_NOP_META_SEM_TYPE_NAME` index from NopMetaSemanticType (维度04-001). Added `IX_NOP_META_DATA_SOURCE_STATUS` index on `status` column for NopMetaDataSource (维度04-002). Fixed NopMetaDataProduct comment from "报表定义（预留）" to "数据产品（Data Product）定义" (维度04-003). Added `stdDomain="json"` to NopMetaQualityCheckpoint.extConfig column (维度04-004). ✓ compile pass.
- **Phase 2 (Dict & Documentation Cleanup)**: Determined `meta/checkpoint-action-type` and `meta/quality-trend-direction` have no column `ext:dict` refs but are referenced via generated constants from Java code → kept with documentation comment. `meta/reconciliation-status` also kept with comment (used by ReconciliationExecutor.java). Fixed `meta/agg-func` internal inconsistency: `countDistinct` → `count_distinct`, updated Java constant `AGG_FUNC_COUNT_DISTINCT` and all code/test references. Added XML comment on `NopMetaModule.baseModuleId` explaining no `cascadeDelete` design intent (维度04-007). ✓ compile + test pass.
- **Phase 3 (Empty Class & ErrorCode Naming)**: Chose Option B for 09-07: documented hyphen ErrorCode convention in `docs-for-ai/02-core-guides/error-handling.md`. Updated `_NopMetadataCoreConstants.java` to restore CHECKPOINT_ACTION_TYPE_* and QUALITY_TREND_DIRECTION_* constants (deleted by codegen during clean build when dicts were temporarily removed from ORM). NF-03: `NopMetadataConfigs.java` is a codegen-generated retention scaffold (template: `nop-codegen/templates/orm/Configs.java.xgen`) — cannot be permanently deleted. Resolved by adding Javadoc documenting its scaffolding purpose. ✓ compile + test pass.
- **Deviation**: NF-03 `NopMetadataConfigs.java` is not "dead code" — it's standard ORM codegen scaffolding. Resolution: add Javadoc documentation instead of deletion.
- **Verification**: `./mvnw test -pl nop-metadata/nop-metadata-service -am` → BUILD SUCCESS
- **Doc-sync**: Updated `docs-for-ai/02-core-guides/error-handling.md` (nop-metadata hyphen convention note).

## Plan 17 nop-metadata BizModel Compliance — all phases completed

- **Plan**: `ai-dev/plans/17-nop-metadata-bizmodel-compliance-remediation.md`
- **Phase 1 (Data Auth Bypass Fix)**: Replaced ~20 `dao().getEntityById()` + null check + manual throw with `requireEntity(id, actionName, context)` across 5 BizModel files (NopMetaDataContractBizModel, NopMetaTableBizModel, NopMetaQualityRuleBizModel, NopMetaModuleBizModel, NopMetaReconciliationConfigBizModel). Also fixed `checkContractReadOnly` (previously missed). Updated `testActivateContract_notFound` to assert `unknown-entity` error code. Removed redundant `checkDataAuth` calls. Removed unused `resolveTableOrThrow` helper. ✓ compile + 836 tests pass.
- **Phase 2 (Interface Completeness)**: Added `checkContractReadOnly` to `INopMetaDataContractBiz` and `propagateTags`/`suggestTags` to `INopMetaTagLabelBiz`. Extended `TestNopMetaBizInterfaceCompleteness` to cover all 3 new methods. ✓ compile + 836 tests pass.
- **Phase 3 (BizModel Pattern Fixes)**: Removed `txn().runInTransaction()` wrappers from `approve()`/`reject()`. Removed `@Deprecated` methods (`activateContract`, `deprecateContract`, `retireContract`) from BizModel + interface + removed `submitForApproval` helper. Changed `dao().saveEntity()` to `orm().save()` in `recordLineage`. NopMetaSearchBizModel confirmed as valid cross-entity orchestration BizModel (xmeta exists). `QueryJoinDataResultDTO`/`AggregationResultDTO` confirmed as `@DataBean` (inner items dynamic by nature). AR-30 already resolved (no `RuntimeException` found). Removed corresponding tests. ✓ compile + 833 tests pass.
- **Phase 4 (N+1 Fix)**: Replaced loop `dao().saveEntity(edge)` with `dao().batchSaveEntities(parsed)` in `recordLineage()`. Extract methods already used `batchSaveEntities`. ✓ compile + 833 tests pass.
- **Closure audit**: Independent subagent verified all phases. Found 3 additional `dao().getEntityById()` patterns in `NopMetaProfilingRuleBizModel.executeProfilingRule` and `NopMetaQualityResultBizModel.approve/reject` — fixed during audit. All now use `requireEntity()`.
- **Deviation**: NopMetaReconciliationConfigBizModel `daoFor(NopMetaTable.class).getEntityById()` left as-is (secondary entity). NopMetaTableBizModel line 89 conditional load left as-is (CREATE/UPDATE detection).
- **Doc-sync**: No owner-doc update required.

- **Plan**: `ai-dev/plans/17-nop-metadata-bizmodel-compliance-remediation.md`
- **Phase 1 (Data Auth Bypass Fix)**: Replaced ~20 `dao().getEntityById()` + null check + manual throw patterns with `requireEntity(id, actionName, context)` across 5 BizModel files (NopMetaDataContractBizModel, NopMetaTableBizModel, NopMetaQualityRuleBizModel, NopMetaModuleBizModel, NopMetaReconciliationConfigBizModel). Also fixed `checkContractReadOnly` (previously missed). Updated `testActivateContract_notFound` to assert `unknown-entity` error code instead of custom `ERR_CONTRACT_NOT_FOUND`. Removed redundant `checkDataAuth` calls where `requireEntity` now handles auth. Removed unused `resolveTableOrThrow` helper. ✓ compile + 836 tests pass.
- **Deviation**: NopMetaReconciliationConfigBizModel line 107-108 (`daoFor(NopMetaTable.class).getEntityById()`) left as-is — secondary entity (different DAO), not a `dao().getEntityById()` pattern. NopMetaTableBizModel line 89 (`id != null ? dao().getEntityById(id) : null`) left as-is — conditional CREATE/UPDATE detection, not a direct bypass.
- **Doc-sync**: No owner-doc update required.

## Plan 16 nop-metadata Build Infrastructure Fixes — completed

- **Plan**: `ai-dev/plans/16-nop-metadata-build-infrastructure-fixes.md`
- **Phase 1 (AR-37)**: Changed `nop-wf-service` scope from `test` to `compile` in `nop-metadata-service/pom.xml`. Workflows (`metaDataContractApproval`, `qualityBreachApproval`, `tagLabelConfirmApproval`) are production features (referenced from xmeta files, actively created by `QualityAlertWorkflowService`). Option A chosen. Also added `ERR_WORKFLOW_MANAGER_UNAVAILABLE` ErrorCode to `QualityErrors.java`. Changed `QualityAlertWorkflowService.createAlertWorkflow()` to throw NopMetadataException instead of silent warn+return-null. Updated test to expect error.
- **Phase 2 (AR-38)**: Deleted empty `_dao.beans.xml` from `nop-metadata-dao`. Removed `<import resource="_dao.beans.xml"/>` from `app-service.beans.xml`. Verified no other source files reference it.
- **Verification**: `./mvnw compile -pl nop-metadata -am` → BUILD SUCCESS; `./mvnw test -pl nop-metadata -am` → BUILD SUCCESS
- **Doc-sync**: No owner-doc update required (scope-only fix, no API/contract change).

## Plan 15 nop-metadata Code Quality & Test Coverage Remediation — completed

- **Plan**: `ai-dev/plans/15-nop-metadata-test-and-code-quality.md`
- **Phase 1 (Error Handling)**: Added `NopMetadataException(String)` and `(String, Throwable)` constructors. Made `NopMetadataArgs` public. Documented hyphen ErrorCode convention in javadoc. Replaced 12 bare `NopException` throws across 5 files (NopMetaDataContractBizModel, MetaQualityRuleExecutor, NopMetaTagLabelBizModel, LineageTagPropagationProcessor, AutoClassificationProcessor) with `NopMetadataException`. Added `ARG_*` constants for inline `.param()` calls. Added trace logging to empty SQLException catch in MetaTableProfiler. ✓ compile + 835 tests pass.
- **Phase 2 (Codegen/XMeta)**: Added `TestAllEntitiesHaveBizModels.java` build-time test (39 entities → 39 BizModels verified). Added codegen reference comment to `nop-metadata-dao/pom.xml`. Replaced duplicate `STATUS_MANUAL` with `_NopMetadataCoreConstants.RECONCILIATION_STATUS_MANUAL`. Removed redundant `@GraphQLReturn` from `NopMetaGlossaryTermBizModel.update()`. ✓ compile + 836 tests pass.
- **Phase 3 (Test Quality)**: Replaced `Thread.sleep(1100)` with clock manipulation via `CoreMetrics.registerClock()` in TestNopMetaQualityRuleBizModel. Added 4 regression tests for uncovered BizModel methods (judgeByRuleId, activateContract, deprecateContract, retireContract). Documented concurrency test as stateless-parallelism-only. Removed 3 pure getter/setter tests from TestCheckpointExtConfigDataBean. AutoTest snapshot integration deferred (existing TestAutoNopMetaClassificationCrud demonstrates pattern; _cases infrastructure needs setup). ✓ compile + 836 tests pass.
- **Deviation**: Phase 3 AutoTest snapshot for DataContract/QualityScore temporarily removed — AutoTest framework requires pre-existing `_cases/input/` files with specific directory structure; adding working tests requires re-recording snapshots in a properly seeded environment.
- **Verification**: `./mvnw test -pl nop-metadata/nop-metadata-service -am` → BUILD SUCCESS (836 tests, 0 failures)
- **Doc-sync**: No owner-doc update required (internal code quality fixes, no public contract change).

## Plan 14 nop-metadata Module Boundary & Convention Alignment — completed

- **Plan**: `ai-dev/plans/14-nop-metadata-module-boundary.md`
- **Phase 1 (Dependency rules)**: Added `nop-metadata-api` dep to `nop-metadata-service/pom.xml`. API module kept (has DTOs); Biz interfaces stay in dao (entity dep prevents move). Updated `docs-for-ai/03-modules/nop-metadata.md` with dependency rule table and doc. ✓ compile + 834 tests pass.
- **Phase 2 (Dict/FK)**: Added local `meta/approve-status` dict replacing cross-module `wf/approve-status`. Renamed FK `entityTableId` → `metaTableId` in NopMetaDataContract (column def, relation joins, indexes). Updated hand-written BizModel + tests. ✓ compile + 834 tests pass.
- **Phase 3 (BizModel conventions)**: Renamed `AutoClassificationService` → `AutoClassificationProcessor`, `LineageTagPropagationService` → `LineageTagPropagationProcessor`. Replaced `BeanContainer.getBeanByType()` with `@Inject` in `NopMetaTagLabelBizModel`. Converted `NopMetaReconciliationConfigBizModel` constructor injection to no-arg+`@PostConstruct`. Added `requireEntity()` in 7 BizModels. Documented xbiz dep in `NopMetaDataContractBizModel` javadoc. Deferred `NopMetaModuleBizModel` processor extraction with TODO comment. Updated beans.xml and test. ✓ compile + 834 tests pass.
- **Phase 4 (ORM naming/docs)**: Renamed `uk_meta_datasource_name` → `UK_NOP_META_DATA_SOURCE_NAME`. Added inline comments to `NopMetaModelChangedEvent` explaining dual audit fields. Documented zero-relations rationale for `NopMetaReconciliationEntity`. Changed duplicate `ext:icon="database"` to `list-checks`. ✓ compile + 834 tests pass.
- **Deviation from plan**: Phase 1 — took third path (keep api, don't populate Biz interfaces, document reason) instead of strictly populate-or-remove. Phase 2 — used same options as wf dict for backward compatibility. Phase 4 — 04-01 mutual exclusion was already implemented in existing `NopMetaTableJoinBizModel.validateJoinSide()`, no new code needed.
- **Verification**: `./mvnw test -pl nop-metadata/nop-metadata-service -am` → BUILD SUCCESS (834 tests, 0 failures)
- **Doc-sync**: Updated `docs-for-ai/03-modules/nop-metadata.md`. Source audit files closed.

## Plan 13 nop-metadata Critical Structural & Security Fixes — completed

- **Plan**: `ai-dev/plans/13-nop-metadata-critical-fixes.md`
- **Phase 1 (01-01)**: Moved 32 DTOs from `nop-metadata-core/dto/` to `nop-metadata-api/dto/`. Updated dao pom: removed core dependency, added api dependency. Updated core pom: added api dependency. Updated 22 imports across 11 dao files + 23 service files. Inlined `MODULE_STATUS_DRAFTING` constant in OrmModelImporter. ✓ compile + test pass.
- **Phase 2 (07-02, AR-32, AR-33, 07-01)**: Added `checkDataAuth()` calls to `approve()`, `reject()`, `checkContract()` in NopMetaDataContractBizModel (3 methods that bypassed data auth). Wrapped `approve()`/`reject()` in `txn().runInTransaction()`. Split CQRS: `checkContract` (@BizMutation, backward-compat) + `checkContractReadOnly` (@BizQuery). Added `@Name("id")` to `NopMetaQualityCheckpointBizModel.delete()`. ✓ compile + test pass.
- **Phase 3 (11-01, 11-04, 11-06, AR-31, AR-28, NF-03)**: Added `connectionConfigComponent` restriction in NopMetaDataSource.xmeta. Added `checkDataAuth(METHOD_SAVE)` before `dao().saveEntity()` in `computeQualityScore`. Tagged `sourceSql`/`buildSql` with `tagSet="sensitive"` in NopMetaTable.xmeta. Hardened application.yaml (introspection disabled, no hardcoded JWT key, debug disabled). Removed empty `NopMetadataConstants.java` and `NopMetadataConfigs.java`. ✓ compile + test pass.
- **Phase 4 (AR-25)**: Audited NopMetaLineageEdgeQueryAction — all 3 extraction methods already use batch operations (`batchLoadExistingSqlParseSourceIds`, `batchLoadExistingColumnParseEdgeMap`, `dao.batchSaveEntities`, `dao.batchUpdateEntities`). N+1 already resolved — documented as resolved-without-change.
- **Deviation from plan**: Phase 2 used `checkDataAuth()` + `dao().getEntityById()` instead of `requireEntity()` from CrudBizModel (to preserve custom error messages expected by tests). Phase 3 used approach (b) for `computeQualityScore` (explicit `checkDataAuth` before `dao().saveEntity()`) instead of approach (a) (which caused test regression). Phase 4 required reading from `entity/` subdirectory, not `lineage/`.
- **Verification**: `./mvnw test -pl nop-metadata/nop-metadata-service -am` → BUILD SUCCESS
- **Doc-sync**: No owner-doc update required (internal fixes, no public contract change).

## ConvertHelper 秒/毫秒时间戳区分修正

`ConvertHelper.stringToLocalDate` / `stringToLocalDateTime` 原对纯数字串一律按**毫秒**解析(`isAllDigit` → `millisToLocalDate(ts)`)。AMIS `input-date` 默认序列化为**秒级** Unix 时间戳(10 位),导致秒时间戳被静默解析成 1970 年附近日期(不报错,比抛异常更危险)。

根因来自 nop-app-erp plan `2026-07-12-1321-3`:6 个报表 BizModel 手写 `LocalDate.parse(v.toString())` strict 解析秒时间戳/空串抛 `DateTimeParseException`。调研发现即便改用平台 `ConvertHelper.toLocalDate`,秒时间戳仍会因单位不匹配被解析成 1970 年。

### 改动
- `ConvertHelper.java` `stringToLocalDate` / `stringToLocalDateTime`:纯数字串 `str.length() <= 10` 按**秒**(`ts * 1000L` 后复用 `millisToLocalDate/Time`,保持默认时区行为一致),>10 位(13 位毫秒)不变。两处对称修正,避免 Date 与 DateTime 对同一秒时间戳解析不一致。
- 边界安全:10 位最大值 `9999999999 * 1000` 远在 long 范围内;`isAllDigit` 已保证纯数字,`length()` 判断可靠。
- 回归面:仅影响"传入 ≤10 位纯数字串且原期望当毫秒"的调用方(10 位毫秒 = 1970-01-01 后约 115 天,作为业务 LocalDate 几乎不可能);13 位毫秒行为不变。大量 `_gen` DAO 实体的 `toLocalDate` 经此路径,但它们的输入来自 DB/序列化,非秒级时间戳,不受影响。

### 验证
- `mvn test -pl nop-kernel/nop-api-core -Dtest=TestConvertHelper` → Tests run: 9, Failures: 0, Errors: 0, BUILD SUCCESS
- 新增 `testEpochString`:13 位毫秒保持毫秒、10 位秒按秒(`assertNotEquals` 证明不再误当毫秒)、空串→null,Date/DateTime 对称。断言基于 `millisToLocalDate/Time` 自洽,不硬编码日期(规避时区不稳)。

### Doc-sync
- 候选 follow-up:`docs-for-ai/02-core-guides/e2e-testing.md` 增"AMIS 秒级时间戳 + Map 参数无 coercion"已知坑(nop-app-erp 侧已在上轮讨论中识别)。
