### 2026-08-18（修复 nop-retry saveAttempt 双保存与测试断言）

- 修复 `RetryRecordStoreImpl.saveAttempt`：按 `attempt.orm_state().isTransient()` 分流，TRANSIENT 走 `saveEntityDirectly` 插入、MANAGED 走 `updateEntityDirectly` 更新。根因：`doExecute` 对同一 attempt 实体保存两次，首次插入后实体变为 MANAGED（`persisterPostSave`），nop-orm 的 `saveDirectly` 只接受 TRANSIENT，二次保存必抛 `ERR_ORM_SAVE_ENTITY_NOT_TRANSIENT`。
- 测试修正（TestRetryEngineImpl）：新增 `expectFutureFailure` helper 解包 `ExecutionException`→`NopException`（4 处）；deadline 用例改走 `executeRetryFromScanner`（deadline 检查属执行路径，重复提交会被 DISCARD 丢弃），并用 `orm_disableAutoStamp(true)` 保留手工设置的老 createTime；`findAttempts/findDeadLetters` 排序改 `addOrderField(name, false)`（第二参是 desc）；分区用例只断言同键同分区；回调用例改用独立回调策略，避免 `callbackPolicyId` 自引用造成回调链递归。
- Mock 修正：`MockRpcServiceInvoker.setResponses` 重置 `responseIndex`，避免跨阶段消费时索引越界回落到 null response。
- 新增回归测试 `TestRetryRecordStoreImpl.testSaveAttempt_shouldInsertThenUpdateSameEntity`（同一 attempt 实体先插后更，终态落库且仅一行）。
- Bug note：`ai-dev/bugs/2026-08/2026-08-18-nop-retry-save-attempt-managed-double-save.md`（含 ORM 状态语义、createTime 自动盖写、`addOrderField` desc 语义、回调自引用风险四条 future-refactor 注意事项）。
- 验证：`./mvnw -pl nop-retry/nop-retry-engine test` 通过（Tests run: 40, Failures: 0, Errors: 0）。
- Doc-sync: 无 owner-doc 变更（纯缺陷修复；ORM 语义注意事项记录于 bug note）

### 2026-08-18（收敛 nop-retry parallel 设计并修改实现）

- 收敛 `nop-retry` 设计：更新 `docs-for-ai/03-modules/nop-retry.md`、`docs-for-ai/INDEX.md`、`ai-dev/design/nop-job/retry-integration-design.md`，删除 `PARALLEL` 作为对外阻塞策略，明确未完成记录的幂等收敛键为 `(namespaceId, groupId, idempotentId)`，明确 `retryFromDeadLetter` 是 replay-once 语义，明确 `callbackEnabled` 是真实总开关。
- 修改实现：`IRetryRecordStore.findPendingRecordByIdempotentId` 改为三元组签名；`RetryRecordStoreImpl` 查重增加 `groupId` 过滤；`RetryEngineImpl` 查重调用改为传 `namespaceId/groupId/idempotentId`，并移除 `PARALLEL` 分支的运行时行为；`NopRetryPolicy` 默认 block strategy 改为 `DISCARD`；`nop-retry/model/nop-retry.orm.xml` 删除 `PARALLEL` 字典项与 `MAX_RETRIES` record 状态项，并将 `UK_RETRY_IDEMPOTENT_ID` 收口为 `(namespaceId, groupId, idempotentId)` 复合唯一键；相关 scanner/store/engine 测试按新签名和默认语义同步调整。
- 偏差：按规范运行 `node ai-dev/tools/check-doc-links.mjs --strict` 后仍失败，但失败项均为仓库既有历史问题（`docs-for-ai/03-modules/nop-job.md`、`docs-for-ai/03-modules/nop-metadata.md` 及多篇 `ai-dev/articles` 断链/边界错误），本次未新增新的 checker 失败，也未在本回合扩大范围清理这些历史问题。
- 偏差：聚焦验证 `./mvnw -pl nop-retry/nop-retry-engine -Dtest=TestRetryEngineImpl,TestRetryScannerImpl,TestRetryRecordStoreImpl test` 失败，暴露 `RetryRecordStoreImpl.saveAttempt` 对已 MANAGED 的 `NopRetryAttempt` 二次 `saveEntityDirectly`、以及若干既有测试断言不匹配（如 `retryFromDeadLetter` 通过 `future.get()` 抛 `ExecutionException` 包裹、deadline 用例假定异常而 live 路径未抛）的问题；本回合未继续扩大 scope 修复这些非本次设计收敛直接引入的缺陷。
- 验证：`./mvnw test -pl nop-retry/nop-retry-dao -am` 通过；`./mvnw test -pl nop-retry/nop-retry-engine -am` 被上游既有 `nop-codegen` Windows 文件锁测试失败阻断；`./mvnw -pl nop-retry/nop-retry-engine -Dtest=TestRetryEngineImpl,TestRetryScannerImpl,TestRetryRecordStoreImpl test` 失败并暴露上述 live 问题；`node ai-dev/tools/check-doc-links.mjs --strict` 失败且失败项为历史存量。
- Doc-sync: `docs-for-ai/03-modules/nop-retry.md`、`docs-for-ai/INDEX.md`、`ai-dev/design/nop-job/retry-integration-design.md` 已更新

### 2026-08-18（补充 nop-job worker-liveness / SUSPICIOUS 文档）

- 调研并串联 `JobTimeoutCheckerImpl`、`JobCompletionProcessorImpl`、`JobFireStateMachine`、`JobScheduleStoreImpl` 的恢复链：worker-liveness 通过 `INamingService.getInstances(AppConfig.appName())` 解析健康实例，以 `workerInstanceId` 为 owner 判据；失联 task 首轮 `CLAIMED/RUNNING -> SUSPICIOUS`，下一轮 `SUSPICIOUS -> TIMEOUT`，completion 在无 pending task 时把 `SUSPICIOUS` 按 `TIMEOUT` 聚合到 fire。
- 文档同步：更新 `docs-for-ai/03-modules/nop-job.md`，补充 worker-liveness 两阶段链及 `SUSPICIOUS` 的并发/聚合语义；更新 `docs-for-ai/04-reference/source-anchors.md` 增加 `JOB-001` 锚点；修正 `ai-dev/design/nop-job/01-architecture-baseline.md` 中 task 状态枚举缺失 `SUSPICIOUS`。
- 偏差：按 AGENTS 规则运行 `node ai-dev/tools/check-doc-links.mjs --strict` 后发现仓库中已存在 46 个历史文档错误（含 `docs-for-ai/03-modules/nop-job.md` 旧相对路径问题、`docs-for-ai/03-modules/nop-metadata.md` 边界错误及多篇 `ai-dev/articles` 断链），本次新增内容未单独引入新的 checker 失败，但未在本回合清理全部历史问题。
- 验证：代码调研基于 `JobTimeoutCheckerImpl.java` / `JobCompletionProcessorImpl.java` / `JobFireStateMachine.java` / `JobScheduleStoreImpl.java` / `TestJobTimeoutChecker.java`；文档检查命令 `node ai-dev/tools/check-doc-links.mjs --strict` 失败，失败原因如上。
- Doc-sync: `docs-for-ai/03-modules/nop-job.md`、`docs-for-ai/04-reference/source-anchors.md` 已更新

### 2026-08-18（修复 JobWorkerScannerImpl resultPayload 残留）

- 修复 `nop-job/nop-job-worker/.../JobWorkerScannerImpl.java`：统一通过 `buildResultPayload` 回写执行结果，在无 `nextScheduleTime/completed` 时显式清空 `resultPayload`，避免 `handleExecutionResult` CAS 重试路径保留旧 payload。
- 新增回归测试 `testRetryClearsStaleResultPayloadWhenNewResultHasNoPayload`：用一次性失败的 `updateTask` 模拟最终状态更新重试，验证数据库中旧 `resultPayload` 会被清空而不是残留。
- 偏差：初次验证命令 `./mvnw test -pl nop-job/nop-job-worker -am -Dtest=TestJobWorkerScanner` 在 reactor 上游模块因 surefire `No tests matching pattern` 失败，已改为在模块目录执行同一测试类。
- 验证：`./mvnw test -Dtest=TestJobWorkerScanner`（在 `nop-job/nop-job-worker` 目录执行）
- Doc-sync: No owner-doc update required

### 2026-08-18（移除 nop-job rpcBroadcast executorKind）

- 删除 `rpcBroadcast` 的 live 配置入口：移除 `nop-job/model/nop-job.orm.xml` 与 `nop-job-meta/.../executor-kind.dict.yaml` 中的选项，移除 `nop-job-service/.../app-service.beans.xml` 中的 `nopJobInvoker_rpcBroadcast` bean，并同步删除 `_NopJobCoreConstants.EXECUTOR_KIND_rpcBroadcast`。
- owner-doc / design 同步：更新 `docs-for-ai/03-modules/nop-job.md` 与 `ai-dev/design/nop-job/invoker-design.md`，明确广播统一使用 `executorKind=rpc` + `dispatchMode=broadcast`，不再暴露 `rpcBroadcast` 命名空间。
- 偏差：未修改 `_app.orm.xml` 等生成物，遵守 generated-files hard stop；后续需通过 codegen / 构建再生。
- 验证：未运行 `./mvnw test`；当前回合先完成源码与文档收敛。
- Doc-sync: `docs-for-ai/03-modules/nop-job.md` 已更新
