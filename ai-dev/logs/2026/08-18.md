# 2026-08-18

### Mission VERIFY 轮：nop-credential-mfa 构建门禁全绿 + 两处测试 flake 根治（plan 2026-08-18-0904-1 收口后验证）

- **Plan**：`ai-dev/plans/2026-08-18-0904-1-mfa-phase2-security-audit-a2.md`（completed；本轮为 mission verify 门禁）
- **门禁结果**：`compile` ✅ / `clean install -DskipTests` ✅ / `checkstyle:check` 走 `lint not configured` 回退（9166 条违例全部在依赖模块 nop-api-core 既有框架代码，非本 mission 触碰，checkstyle 非项目 lint 门禁）/ `test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` **BUILD SUCCESS**（修 flake 后两连绿）。
- **flake 修复 1（nop-auth-service）**：`TestMfaUserSelfService.testBindExpiredOrUnknownTokenRejected` 偶发 `expected mfa-bind-expired but was mfa-fail`。根因（日志实证）：原测试以 `bind-expire-seconds=0` 制造过期，`isBindExpired` 用严格 `age > expireMs`——`-T 1C` 高负载下 bind flush 与 confirm 检查可落在同一毫秒（INSERT 写入 `UPDATE_TIME 17:32:46.785`，confirm 检查同在 `.785`，age==0）→ 判未过期 → 落入因子校验失败 mfa-fail。修复：改为直连 SQL 确定性回写 `UPDATE_TIME = now − 600s`（600s > 默认 300s 窗口；绕过 ORM onUpdate 自动戳记），删除配置覆盖 dance；断言语义不变（过期 pending → BIND_EXPIRED）。
- **flake 修复 2（nop-stream-rocksdb，`-am` 传递依赖、非 mission 模块）**：`TestRocksDBIncrementalRestoreAndBenchmark.incrementalCheckpointIsFasterThanFullScanForLargeState` 单次采样 ratio 随运行上下文漂移：class 单独 ~0.30 / 模块全量 1.60 / 并行 reactor 1.68–2.13（冷页缓存 + 调度噪声主导单次采样；fullScan 稳定 ~60ms，incremental 18ms↔128ms）。修复：两路各改 min-of-3 采样（min 为最小噪声估计，真实回归平移 min、负载噪声仅抬升单样本）+ portable guard 1.5→2.0（对齐测试自身"真实回归 2x+ 才显形"文档）。修后 in-suite ratio 1.05–1.32 稳定过门。
- **工作树卫生**：还原一处无关生成样本文件的 CRLF-only 触碰（`nop-ooxml-xlsx/samples/generated-pie-chart/_rels/.rels`，零内容差异）。
- 验证：全量 test 门禁两连绿（第二次 ratio=1.053）；`TestMfaUserSelfService` 15/15；`nop-stream-rocksdb` 模块全量绿。
- Doc-sync: No owner-doc update required（两处均为测试侧 flake 修复，不改产品行为；nop-stream 非本 mission 模块）。

### Mission EXECUTE 轮：A2-audit MFA 二期安全审计（plan 2026-08-18-0904-1，Phase 2 + Phase 3 执行 + 收口）

- **Plan**：`ai-dev/plans/2026-08-18-0904-1-mfa-phase2-security-audit-a2.md`（active → completed）
- **Phase 2（对抗审计执行，fresh 子 agent 七维度 + P1 修复 + 独立复核）完成**：
  - 七维度全部由独立 fresh 子 agent 探查（task 标识记录于 `summary.md`）：D1 因子/绑定（P2×2/P3×5）、D2 防重放（**P1×1**+P2×1+P3×3）、D3 恢复通道（**P1×1**+P2×2+P3×4）、D4 策略/受限会话（P3×6，同构比对=路由项 2 证据）、D5 操作级覆盖（P2×1+P3×4，路由项 1 论证材料）、D6 可信设备（P2×1+P3×2）、D7 一期零回归（矩阵六行全 PASS + 断言零修改 HOLDS）。合计 39 findings（closure audit 复核口径，含 D1-2 并入 D2-F2 的合并行）：P0×0 / **P1×2** / P2×7 / P3×30。
  - **P1 修复 1（D2-F1）**：`RedisSmsCodeStore`/`RedisEmailCodeStore` VALID 裁决改以 `removeIfMatch`（Lua CAS）胜出为前提——删除原子 ≠ 裁决原子，并发双 verify 同码原双双 VALID；败者现返回 EXPIRED（与 Db 实现 affected==0 → EXPIRED 跨实现契约统一）。新增探查测试 `TestRedisCodeStoreCasRace`（4 用例，CasLoserNosql 确定性复现竞态）。
  - **P1 修复 2（D3-F1）**：webauthn credential 行在三个因子失效边界（`unbindMfa`/`resetUserMfa`/`LoginServiceImpl.verifyRecoveryCodeAndComplete` 恢复码分支）bulk 物理 DELETE——原实现残留 enabled 行，用户重绑后旧（被窃）硬件钥匙复活。用 `deleteByQuery`（物理 DELETE，避开会话缓存实体与断言验证条件 UPDATE 的乐观锁冲突）；`removeWebauthnCredential` 同步改物理删除（D1-4/V-F1：credentialId 唯一键释放 + 重复注册守卫/约束判定一致）。配套测试：新增 `testUnbindAndAdminResetDeleteCredentialRows`；重排 3 个依赖"解绑保留行"旧语义的测试（`testDuplicateRegistrationRejected`/`testDualCredentialLifecycle`/`testDoubleCeremonyUnbindWithOperationMfaEnabled`——断言面保持，独立复核确认无安全弱化）。**能力裁定**：多钥匙"解绑保留行累积"路径关闭（安全语义优先），add-key-while-enabled 端点作 successor 候选。
  - **修复独立复核**（fresh 子 agent `fix-verification.md`）：六项全 PASS（边界穷举无遗漏/CAS 语义跨实现一致/无断言弱化/能力变更无生产消费方依赖/无静默 no-op/测试绿）——"发现者不复核自己发现项的修复"独立性保持。
- **Phase 3（裁决收口 + 文档同步）完成**：
  - `adjudication.md` 落盘：fixed×4（D2-F1/D3-F1/D1-4+V-F1/D5-F2+V-F2）/ successor×2 / deferred+watch 20 条逐条 Why Not Blocking——零悬挂。
  - **3 项路由 deferred 终局裁定**：①removeWebauthnCredential 应标注（与 unbindMfa 强度一致性优先）→ successor-B 落地；renameWebauthnCredential 不标注（C1b 缩窄先例适用，终局）。②两副本当前同步（8 块逐字等价 + 3 登记漂移 + 新登记 D4-4 evaluator null 防御不对称）——watch-only 维持。③联系方式 CRUD 敏感化并入 successor-A 治理族。
  - Owner docs/design 回写：`docs-for-ai/03-modules/nop-auth.md` 五处（credential 实体失效边界/unbindMfa/removeWebauthnCredential/解绑 ceremony/恢复码分支）；设计文档新增 §十「A2-audit 落地裁定」六条（含 §5.3.2 执行期偏离记录 #7 的修正裁定）。
  - Roadmap：A2-audit `done` + A2-followup-1/A2-followup-2 successor 登记（均不阻塞 A3-audit）。
- **验证**：nop-auth 家族（`:nop-auth-service,:nop-auth-sso -am`）**360 tests 0 failures**（W15 基线 355 + 审计新增 5）；`:nop-credential-service -am` **209/0**（C1b 面无条件重跑）；`:nop-graphql-core -am` **80/0**（javadoc 触点）；`check-doc-links --strict` 0 errors；`scan-hollow-implementations --module nop-auth` 1 条 pre-existing（OAuthLoginServiceImpl:231，git diff 证明非本次触碰）= 0 NEW。一次中间 run 命中已知 plexus javac 编译 flake（零测试失败、复跑全绿，Current Baseline 预登记项）。
- Commit: A2-audit 工作提交（含同日 DRAFT 轮未提交的 roadmap/log/两 plan 文件）。
- Doc-sync: nop-auth.md + 设计 §十（见上）；nop-credential.md 无触碰（C1b 面无 drift）。

### Mission EXECUTE 轮：A2-audit MFA 二期安全审计（plan 2026-08-18-0904-1，Phase 1 执行）

- **Plan**：`ai-dev/plans/2026-08-18-0904-1-mfa-phase2-security-audit-a2.md`（active）
- **Phase 1（审计章程锚定与探查清单）完成**：
  - 审计目录 `ai-dev/audits/2026-08/2026-08-18-1244-deep-audit-nop-auth/` 建立，`audit-charter.md` 落盘（沿 A1-audit `2026-08-17-0518` 先例结构）。
  - 攻击面 live 锚点核对（非旧 plan 转抄）：LoginApiBizModel 11 端点（loginAsync :119 / sendSmsCode :171 / sendMfaCode :181 / mfaVerifyAsync :195 / webauthnAuthOptions :233 / mfaVerifyOperation :294 / verifyChannelProof :383 / refreshTokenAsync :150 等）；NopAuthUserBizModel MFA 动作面 16 动作（bindMfa :235 → removeTrustedDevice :895 全清单）；NopAuthRoleBizModel saveMfaPolicy :163 / removeMfaPolicy :203；@MfaRequired 标注分布 5+4（nop-auth NopAuthUserBizModel :676/:754/:940/:1422/:1438 + nop-credential C1b NopCredentialAuthBizModel :108/:148、NopCredentialBizModel :650/:771）；元数据传播链四触点（ReflectionBizModelBuilder :348 / GraphQLFieldDefinition :74 / GraphQLExecutor :187 / GraphQLErrors :301/:306）；store 家族六实现 + MfaStoreProvider + MfaChallengeHelper；受限会话白名单 8 项全清单（OperationMfaCheckerImpl :95-103，`LoginApi__refreshToken` Async 尾缀剥除注册名核对成立）。
  - 七维度探查清单（D1-D7 各含目标锚点/威胁假设/对抗用例）+ 回归基准声明（设计 §二 矩阵 + 四 impl plan Closure + C1b 面 + 一期契约 + 测试基线 355 live 重跑要求）。
- 验证：抽查锚点与 live code 对号成立（本日直接 read/grep 核对）。
- Doc-sync: No owner-doc update required（Phase 1 仅产出审计章程，不改 owner 行为）。

### Mission DRAFT 轮：A2-audit + W16-impl-ext 两 plan 起草、两轮独立 review 共识后 active（plan 2026-08-18-0904-{1,2}）

- **Plans**：`ai-dev/plans/2026-08-18-0904-1-mfa-phase2-security-audit-a2.md`（A2-audit，N=1）+ `ai-dev/plans/2026-08-18-0904-2-credential-integration-migration-ext-w16-impl-ext.md`（W16-impl-ext，N=2）——本轮取 roadmap 剩余前两个 `todo`（A3-audit 依赖未满足不起草）；早前 09:04 会话已落两份 draft 但 review 未完成（无日志、roadmap 未动），本会话接续完成对抗 review 流程。
- **Review 轮 1**（两个独立 fresh subagent 并行）：A2 计划 0 Blocker/0 Major + 6 Minor（测试清单漏 `TestMfaRestrictedDaoCache`/nop-credential 重跑改无条件/框架模块进 Phase 2 Targets/owner-doc 裁定行/方法名 `removeTrustedDevice`/flake 引用出处），全部回修；W16-impl-ext 计划 1 Blocker（Email E2E 落点 nop-auth-service 不在任何验证命令——`-am` 只带上行依赖）+ 2 Major（Feishu `@PostConstruct` 登记结构性不可达（live `FeishuClient` 仅 start() 参数获凭证）→ 载体执行期定稿 + 可达性/usage 行断言硬约束；JavaEmailSender.withTransport 吞 Exception / SftpClient.connect 包装 ERR_SFTP_CONNECT_FAIL 两处 fail-closed 落点风险标注）+ 8 Minor（pom 测试依赖补齐/OSS kebab-case 键名实测门/smtp-email 跨字段约束裁定解析侧/typeList 断言后移 Phase 3/E2E 项改 Proof/seam 抽取显式化），全部回修。
- **Review 轮 2**（换新 fresh subagent 复审）：A2 计划发现 1 Major——missing-tenant-id flake 基线过期（08-17 已根因修复（bug note `ai-dev/bugs/2026-08/2026-08-17-missing-tenant-id-suite-flake.md`），旧"不算失败"豁免仍编码在 Closure Gates，安全审计计划不得保留已退役的失败豁免路径）→ L28/Phase 3/Closure Gates/Follow-up 预登记四处改写 + `TestTenant @AfterAll` 恢复先例注记；W16-impl-ext 计划发现 1 Blocker——`:nop-credential` 选中的是聚合器 pom（packaging=pom），`-pl` 不展开子模块导致 typeList 门空转 → 改 `:nop-credential-service`（Phase 3 命令 + Phase 1 后移验证点措辞）+ Current Baseline 残留"执行期裁定"措辞 + 设计回写项补结论 8/结论 1 偏差回写。两 reviewer 均确认回修后 execution-ready，共识达成，两 plan `draft` → `active`。
- **Roadmap**：A2-audit + W16-impl-ext `todo` → `planned`（含两轮 review 摘要）；Last updated 头部更新。执行序 A2 → W16-impl-ext（无依赖、无交集，引擎可按序取）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0。
- Doc-sync: No owner-doc update required（纯 plan/roadmap/log 变更，不改产品行为；两 plan 的 owner-doc 同步属各自执行期工作）。

### Mission EXECUTE 轮：W16-impl 深度迁移首批（plan 2026-08-17-2212-3，Phase 3 执行 + 收口）

- **Plan**：`ai-dev/plans/2026-08-17-2212-3-credential-integration-metadata-migration-w16.md`（active → completed）
- **Phase 3（runbook + 文档同步 + 设计回写 + 收口验证）完成**：
  - **Owner docs**：`docs-for-ai/03-modules/nop-credential.md` 新增"深度迁移（W16）"章节（横切契约/SPI 增量/共享解析支持/SMS 接线/**integration 迁移 runbook**（归属裁定落点——nop-integration 无独立 owner doc）/metadata 交叉引用）+ 子模块表 api 行 + 源码锚点两行；`docs-for-ai/03-modules/nop-metadata.md` 新增"数据源凭证（credentialId 深度迁移，W16）"章节（兼容矩阵三行/解析语义/管理动作对表/admin 配置与审计载体）。
  - **设计回写**：03 号文五处更正（§4.1 结论 8 registerUsage 前置校验（D6-03）+ catch-all WARN 扩域/§4.3 契约 2 typeName 实现通道/§5.1.5 admin 判定（`nop.metadata.credential-admin-roles`）与审计（手动发布 live 事实更正 + delete/deleteByQuery 双覆写）/§6.4 反查主源 consumerRef + 行级事务重读实现/§八 不触碰清单 additive 修正）+ 新增"W16-impl 落地裁定（2026-08-18 回写）"小节。
  - **Roadmap**：W16-impl `planned` → `done` + Last updated + W16-impl-ext 登记确认。`source-anchors.md` 新增 `CRED-006`（深度迁移消费链全锚点）。
  - **全量验证**（输出存 `_tmp/`）：`./mvnw test -pl <六显式子模块> -am -T 1C` **BUILD SUCCESS**（`_tmp/test-w16-phase3.log`）；`scan-hollow-implementations --module nop-integration --module nop-metadata --severity high` **Total 0**（退出码 0，`_tmp/scan-hollow-w16.log`）；`check-doc-links --strict` **0 errors 0 warnings**（`_tmp/check-doc-links-w16.log`——顺手修正一处 docs-for-ai→ai-dev 边界引用与一处 plan 相对路径 warning）。
  - **收口门禁**：`check-plan-checklist --strict` 退出码 0；独立 closure-audit fresh subagent 完成（证据见 plan Closure 段）。

### Mission EXECUTE 轮：W16-impl 深度迁移首批（plan 2026-08-17-2212-3，Phase 2 执行）

- **Plan**：`ai-dev/plans/2026-08-17-2212-3-credential-integration-metadata-migration-w16.md`（active）
- **Phase 2（metadata 单点解析 + 管理动作对 + 批量迁移 + jdbc-datasource 类型实例）完成**：
  - **单点解析**：`MetaDataSourceConnectionProcessor` 新增 `@Nullable ICredentialProvider` setter + `mergeCredentialConfig`（credentialId 空/缺失=现状路径；非空→provider 解析 jdbc-datasource 字段集（typeName 错型含 null 拒绝/username 必填对齐 requireNonBlank/password 键恒存在对齐 requireField）合并进 cfg——既有校验建连链零变化）；`testConnect` catch 按自身包装码白名单精确过滤——仅 `ERR_DATASOURCE_CREDENTIAL_RESOLVE_FAILED` 映射 `{connected:false, error:"credential resolution failed"}` 固定描述，AR-02/config-invalid/provider-not-available/type-mismatch 全部上抛。
  - **管理动作对**：`NopMetaDataSourceBizModel` 新增 `bindCredential`（registerUsage 先行校验→置键→清明文，同一 BizMutation 事务）/`unbindCredential`（清键+注销，明文不复活死值）/`migrateDataSourcesCredential`（per-row REQUIRES_NEW 四步：consumerRef 主源反查（软删命中计入失败清单）→名称辅助反查→createCredential→registerUsage→置键清明文；orderBy dataSourceId；单行失败不中断整批）+ `delete`/`deleteByQuery` 双覆写 unregister（NopAiModelBizModel 先例钉定批量路径坑位）。
  - **Decision 落定**：admin 判定 = `nop.metadata.credential-admin-roles`（缺省 admin,nop-admin）+ IUserContext.isUserInAnyRole；无登录态放行（内部调用，与 nop-credential 写分级同口径）。审计 = `publishCredentialEvent` 手写事件发布（live 事实：MetaModelChangedEventPublisher 无 ORM 自动触发，设计"自动触发"表述以等价手动发布兑现——Phase 3 回写设计）+ 凭证侧审计，不引入 IAuditService。
  - **类型实例**：`jdbc-datasource`.credential-type.xml（username 必填/password 可空 sensitive）。
  - **测试装配定稿**：手写 `FakeCredentialVault`（双 SPI 内存模型，语义对齐生产：registerUsage 前置校验+幂等+软删 fail-closed）setter 注入容器 BizModel——最小装配零新 test 依赖。
- **偏差**：(a) 迁移 `migrateOneRow` 行级事务内重读行（外层实体 attached 外层会话——upsertExternalTableGuarded 先例的等价调整）；(b) 动作返回 Map 摘要（GraphQL 面为叶子选择语义——字段级 selection 不适用于 Map 返回类型）。
- **验证**：新测试 29 用例全绿（TestMetaDataSourceConnectionProcessorCredential 13 + TestNopMetaDataSourceBizModelCredential 16——兼容矩阵三行/预建库用户判别并存行取值来源/fail-closed 全分支/bind 原子性/迁移中断收敛/软删命中失败清单/长名截断哈希/delete 双路径）；nop-metadata-service 全量 **1077 tests 0 failures**（14 消费点零改动回归基线）；git diff 消费点文件为零（仅 processor/bizmodel/pom/errors 四类文件）；dependency:tree api-only 边证据；跨模块 `./mvnw compile/test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am` BUILD SUCCESS。
- Commit: 待 Phase 3 收口后统一提交
- Doc-sync: No owner-doc update required（章节统一 Phase 3）

### Mission EXECUTE 轮：W16-impl 深度迁移首批（plan 2026-08-17-2212-3，Phase 1 执行）

- **Plan**：`ai-dev/plans/2026-08-17-2212-3-credential-integration-metadata-migration-w16.md`（active）
- **Phase 1（共享解析支持 + 依赖边 + SMS 家族接线 + 批次登记）完成**：
  - **SPI 增量**（跨模块公共 API，plan 承载评审门）：`CredentialData` 可选 `typeName` 属性（双构造器向后兼容；`CredentialProviderImpl.getCredential` 从凭实行填充）+ `ICredentialMigrationSupport`（`findCredentialByName`/`findCredentialIdByConsumerRef`/`createCredential`）+ `CredentialLookup` DTO（deleted 标记承载"软删命中供人工处置"语义）；实现 `CredentialMigrationSupportImpl` 的 createCredential 委托 `NopCredentialBizModel.saveCredential` 单点复用（加密 + scope=system + 分级），bean 注册 `credential-defaults.beans.xml`（id `nopCredentialMigrationSupport`）。
  - **共享解析支持**：`io.nop.integration.api.credential.CredentialResolutionSupport`（静态工具单点：`isConfigured`/`resolveGroup`（provider null 部署不一致 + provider 侧 NopException 包装 cause 保留 + typeName 家族允许集校验）/`requireString`/`optionalString`/`requireInteger`（字符串归一 + 转换失败 fail-closed））；`IntegrationErrors` 新增五码。
  - **SMS 家族接线**：两发送器 `credentialId` 可选属性 + `@Inject @Nullable ICredentialProvider` setter + 发送期惰性解析（tencent `resolveCredential()` → ResolvedCredential(appId/appKey/sign) 整组、yunpian `resolveApiKey()`；构造点抽 `protected createSender/createClient` seam，sendMessage/sendMultiMessage 双路径全覆盖）+ `@PostConstruct` 幂等 `registerUsage("integration:tencent-sms"/"integration:yunpian-sms")` catch-all WARN。
  - **依赖边**：`nop-integration-api` → `nop-credential-api`（version-less，api→api）；dependency:tree 证据仅 `nop-commons` 传递、无 service/dao。三模块新增 test 依赖（junit/mockito）。
  - **类型实例**：`tencent-sms`/`yunpian-sms`.credential-type.xml 落 `_vfs/nop/credential/types/`。
  - **批次登记核验**：roadmap `W16-impl-ext` 行 + A3-audit 依赖注记（draft 轮已登记）现场核验可复核。
- **偏差**：无实质偏差。执行期定稿：(a) 共享支持形态 = 静态工具（plan 允许的两形态之一）；(b) `CredentialLookup` DTO 承载软删标记（plan 文字为返回 credentialId，软删语义需载体——实现通道裁定补充）；(c) createCredential 委托 BizModel 而非复制加密逻辑（"复用 saveCredential 语义"的字面兑现）。
- **验证**：`./mvnw clean install -pl nop-integration/nop-integration-api,nop-integration/nop-integration-sms-tencent,nop-integration/nop-integration-sms-yunpian,nop-credential/nop-credential-api,nop-credential/nop-credential-service -am -DskipTests -T 1C` BUILD SUCCESS；新测试 44 用例全绿（TestCredentialResolutionSupport 11 + TestTencentSmsSenderCredential 11 + TestYunpianSmsSenderCredential 12 + TestCredentialMigrationSupport 10）；credential-service 全量 209 tests 0 failures；nop-auth MFA 回归（TestMfaLoginE2E 10 + TestWebAuthnMfaE2E 12 + TestMfaUserSelfService 15）全绿；跨模块 `./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C` BUILD SUCCESS（3:18）。
- Commit: 待 Phase 3 收口后统一提交
- Doc-sync: No owner-doc update required（runbook 与章节统一 Phase 3）

### Mission EXECUTE 轮：W15-impl 邮件验证码 + 可信设备（plan 2026-08-17-2212-2，Phase 3 执行 + 收口）

- **Plan**：`ai-dev/plans/2026-08-17-2212-2-mfa-email-code-trusted-device-w15.md`（active → completed）
- **Phase 3（文档同步 + 设计回写 + 收口验证）完成**：
  - **Owner docs**：`docs-for-ai/03-modules/nop-auth.md` 新增"邮件验证码因子（W15）"与"可信设备（记住此设备，W15）"两章节（EmailCodeStore 三实现与装配/email 全链/限流配置组/指纹算法与威胁模型/豁免判定与固定窗口/撤销矩阵表/管理 API）+ 功能概览/核心实体表（NopAuthEmailCode、NopAuthMfaTrustedDevice）/配置表（email-code 八项 + trusted-device 两项）/源码锚点表/既有 MFA 章节锚点行号全量刷新 + 错误码清单补 email 三码；`docs-for-ai/INDEX.md` 路由行 + `docs-for-ai/04-reference/source-anchors.md`（AUTH-MFA-001/004 行号刷新 + 新增 AUTH-MFA-006/007）。
  - **设计回写**：`02-mfa-phase2-design.md` §5.3.7（email 侧七条裁定：MfaStoreProvider 第三组 map 定稿/proof channel 参数形态/邮箱脱敏形态/email 错误码三码定稿/限流分组件内存计数事实/dict 第四条目/默认模板）+ §6.6（可信设备侧七条裁定：共享组件落点/StringHelper.bytesToHex 核定/实体结构核定/OAuth 副本裁定履行/响应语义细化/登记失败不吞异常/isPasswordLoginType 落位）。
  - **Roadmap**：W15-impl `planned` → `done`（Last updated 同步）。
  - **全量验证**（输出存 `_tmp/`）：`./mvnw test -pl nop-auth/nop-auth-service,nop-service-framework/nop-biz-auth-api,nop-service-framework/nop-biz-auth-core,nop-integration/nop-integration-api -am -T 1C` **BUILD SUCCESS**（nop-auth-service 355 tests 0 failures 3 skipped；全 reactor 依赖模块绿；`_tmp/test-w15-phase3.log`）；`scan-hollow-implementations --module nop-auth --severity high` 仅 1 条 pre-existing 发现（`nop-auth-sso/OAuthLoginServiceImpl.java:231`，本 plan 未触碰该文件——**0 NEW** 成立，`_tmp/scan-hollow-w15.log`）；`check-doc-links --strict` 退出码 0（顺手修正本 plan In Scope 一处相对路径 warning；`_tmp/check-doc-links-w15.log`）。
  - **收口门禁**：`check-plan-checklist --strict` 退出码 0；checkstyle（受影响模块）通过；独立 closure-audit fresh subagent 完成（证据见 plan Closure 段）。

### Mission EXECUTE 轮：W15-impl 邮件验证码 + 可信设备（plan 2026-08-17-2212-2，Phase 2 执行）

- **Plan**：`ai-dev/plans/2026-08-17-2212-2-mfa-email-code-trusted-device-w15.md`（active）
- **Phase 2（可信设备：实体 + 指纹 + 豁免 + 登记 + 撤销矩阵）完成**：
  - **实体**：`NopAuthMfaTrustedDevice`（(userId, deviceHash) 复合唯一——执行期裁定免冗余独立 userId 索引；物理删除语义不引入 delFlag/version；TENANT_ID 数据列 + tagSet="mapper,no-tenant"）+ codegen 再生三方言 `_create_`/`_drop_` + 手写 `_add_mfa_trusted_device_nop-auth.sql` ×3。
  - **共享组件 `MfaTrustedDeviceManager`**（nopMfaTrustedDeviceManager bean）：指纹（`X-Nop-Mfa-Device-Id` 大小写不敏感 + UA + Accept-Language → SHA-256 hex；执行期核定 `ByteHelper.toHex` 为 `\xNN` 格式改用 `StringHelper.bytesToHex`）+ 豁免判定（未过期行命中更新 lastUsedAt 不续 expireAt）+ 登记（同 hash 含过期行 upsert 复活刷新不受 max-count 限；满员显式 null→false；并发撞唯一约束 catch 重查归一为 update）+ 撤销（removeAllForUser/removeBySid 本人限定）+ 审计五事件（userName 非空）。
  - **LoginServiceImpl**：`checkMfaRequired` 增 headers 参（loginAsync 真实值/createSessionForUserAsync null——信道路径结构性跳过）+ 豁免分支（因子等同之后 challenge 之前；进入条件 = headers 非空 ∧ 密码类 loginType ∧ `policy.allowTrustedDevice`（W13 复合结果首个消费点）∧ 指纹非空）；**headers 穿线 `verifySecondFactorAndComplete` 增参**——登记在因子验证成功路径（consume 后 completeMfaLogin 前），**未放 completeMfaLogin**（恢复码分支共用——结构性排除）；结果经 `ATTR_TRUSTED_DEVICE_REGISTERED` attr（ATTR_MFA_ACCESS_CODE 先例）显式 true/false 回填（仅密码类）。**副本裁定**：`MfaLoginPolicyServiceImpl`（OAuth）不加豁免分支 + 专项回归断言（有未过期行仍 challenge）。
  - **NopAuthUserBizModel**：撤销钩子三处（unbindMfa/confirmMfa 换绑判定点/resetUserMfa）+ `listTrustedDevices`（TrustedDeviceInfo DTO 含 expired 标记，不含 deviceHash）/`removeTrustedDevice`（越权归一"不存在"）；`MfaVerifyRequest.rememberDevice` + `LoginResult.trustedDeviceRegistered`（跨模块公共 API 增量，javadoc migration note）。
  - 测试 16 新用例绿：`TestTrustedDeviceSupport`(5 指纹单元) + `TestTrustedDeviceE2E`(11：登记→豁免全链/固定窗口不续期（expireAt 逐字节不变断言）/过期惰性+同 hash 复活/异指纹 challenge/满员 false/策略禁跳过+行保留+放宽恢复/信道不豁免不登记/恢复码不登记/撤销矩阵四触发+越权/OAuth 副本回归/零回归)。
  - 验证：全模块 `./mvnw test -pl nop-auth/nop-auth-service` **355 tests, 0 failures, 3 skipped**（306→355 = 新增 49（P1 35 + P2 16 减 wiring 重整）；既有套件断言零修改）。
- Doc-sync: No owner-doc update required（Phase 2——章节统一 Phase 3）。

### Mission EXECUTE 轮：W15-impl 邮件验证码 + 可信设备（plan 2026-08-17-2212-2，Phase 1 执行）

- **Plan**：`ai-dev/plans/2026-08-17-2212-2-mfa-email-code-trusted-device-w15.md`（active）
- **Phase 1（EmailCodeStore 三实现 + 邮件因子全链 + 登记通道 email 解锁）完成**：
  - **EmailCodeStore 平行族**：接口 + `EmailCodeStoreConfig` + `LocalEmailCodeStore`（nop-biz-auth-core）；`DbEmailCodeStore`/`RedisEmailCodeStore`/`EmailCodeEntry`（@DataBean，W12 教训——真实序列化路径专项测试）；`nop_auth_email_code` 新表（model-first：ORM 源 → codegen 再生 `_create_`/`_drop_` 三方言 + 手写 `_add_email_code_nop-auth.sql` ×3）。
  - **装配（W8 模式复刻）**：`nopEmailCodeConfig`（id 不落 collect 前缀）+ 三 `nopEmailCodeStore_{local,db,redis}`（Redis `ioc:condition` 条件激活，lessons 15）+ collect-beans 第三组 map + **工厂 bean `nopActiveEmailCodeStore`**；`MfaStoreProvider` 扩展裁定 = 同 provider 第三组 map（不另立平行 provider，回写设计 §5.3.3）。
  - **邮件因子接线**：`MFA_TYPE_EMAIL` 常量（factorLevel=1 常量落位替换字面量）+ 白名单 #2/#3（bindMfa email 同 bindSms 形——发码到服务端解析 `NopAuthUser.email`，`MfaBindResult.emailSent`）+ `MfaFactorVerifier` email 分支（key=`mfa-email:{userId}`，EXPIRED 抛 email 专属 `ERR_AUTH_EMAIL_CODE_EXPIRED`——错误码定稿三码：EXPIRED/RATE_LIMITED/DAILY_LIMIT）+ #6 登录级 mfaVerify email 分支 + #9/#10 comment 更新 + `mfa-type.dict.yaml` email 条目。
  - **发送链路**：`nop.auth.email-code.subject-template`/`text-template`（`{code}` 占位服务端替换）+ `IEmailSender` `@Nullable` 注入（未装配 fail-closed 专项用例）；发送目标一律服务端解析。
  - **限流三层**：`nop.auth.email-code.*` 六项配置组（enabled 缺省 false——三个发码入口统一前置显式拒绝）+ `checkEmailRateLimit`（email+IP 双维度）在 bindMfa(email)/sendMfaCode email 分支/登记通道 email 路径生效。
  - **sendMfaCode email 分派（#8）**：email → 解析 user.email → 限流 → store 发码 → 邮件发送；无 email 抛 CHALLENGE_EXPIRED 等价先例错误。
  - **登记通道 email 解锁（§4.3 既定扩展）**：通道解析 phone 优先、缺失回退 email；双通道登记时可选 `channel` 参数（bindMfa 四参 + verifyChannelProof 三参 + 兼容重载；服务端限定已登记集合，任意指定显式拒绝）；proof 码通道隔离（`proof-email:{userId}` vs `proof:{userId}`）；`ERR_AUTH_MFA_CHANNEL_PROOF_REQUIRED` 邮箱脱敏（本地部分前 2 位 + 域名，执行期定稿）；phone 通道 W13 行为零变化。
  - 测试 35 新用例绿：`TestLocalEmailCodeStore`(8)/`TestDbEmailCodeStore`(7)/`TestRedisEmailCodeStore`(6)/`TestEmailCodeEntrySerialization`(1) + provider/wiring 扩展（email 三 map + 工厂 bean 注入断言）+ `TestEmailMfaE2E`(11：全链/收件目标与模板断言/fail-closed/门控/限流三层/EXPIRED 专属码/零回归/白名单动态拼接) + `TestChannelProofEmailE2E`(7：无 phone 全链/双通道默认 phone/显式 email 选择/任意指定拒绝/通道隔离/门控/phone 零回归)；`CapturingEmailSender` 容器 mock。
  - 验证：`./mvnw test -pl nop-auth/nop-auth-service -Dtest='TestMfa*,TestRoleMfaPolicy,TestOperationMfaE2E,TestScanLoginMfa,TestLoginApiTokenWiring'` 104 tests 0 failures + WebAuthn/store 家族套件绿（既有断言零修改——sms 路径行为不变）。
- Doc-sync: No owner-doc update required（Phase 1——章节统一 Phase 3）；跨模块公共 API 增量 migration note（`EmailCodeStore` 接口）在案（plan Exit Criteria + Phase 3 设计回写）。

### Mission EXECUTE 轮：W14-impl WebAuthn/FIDO2（plan 2026-08-17-2212-1，Phase 2-4 执行）

- **Plan**：`ai-dev/plans/2026-08-17-2212-1-mfa-webauthn-fido2-w14.md`（active；Phase 1 于 08-17 完成，本轮 Phase 2 起）
- **Phase 2（WebAuthnAuthenticator + 三 ceremony + 因子接线 + credential 管理）完成**：
  - **库 API 执行期核定**：POC 阶段的 `WebAuthnAuthenticator` 草稿对 webauthn-server-core 2.7.0 实际 API 有四处偏差，本轮修正——builder 强制序（`attestationObject→clientDataJSON` / `authenticatorData→clientDataJSON→signature` / `challenge` 先于 `rpId`）；`CredentialRepository` 2.7.0 五方法签名（`lookup(id, userHandle)` / `getCredentialIdsForUsername→Set<PublicKeyCredentialDescriptor>`）；`Base64UrlException` 受检捕获。**userHandle 裁定**：断言验证 request 侧以服务端 userId 字节为权威句柄（库 Step6 要求 username/userHandle 至少其一），assertion 携带句柄时被库强校验一致（句柄漂移防护专项负例）——`verifyAssertion` 增第五参 userId。
  - 三 ceremony 全落：注册（`bindMfa` #3 分派 + #2 白名单动态拼接 + `confirmWebauthnRegistration` + 受限 proof 前置继承）；认证（challenge 创建**三触点**经 `MfaChallengeHelper`（新组件：①③`createLoginChallenge` 收敛共用，②`OperationMfaCheckerImpl` 同 key 增量）+ `webauthnAuthOptions` 通用端点（login 公开/其余登录态+同会话；cryptoChallenge 只读复用）+ `mfaVerify` webauthn 分支）；解绑（`webauthnBeginVerify` + `unbindMfa` 四参扩展 + 两参兼容重载 + 双 ceremony javadoc）。
  - `MfaFactorVerifier` 五参统一载体重载（code/assertion 可选共存；signCount **条件 UPDATE `WHERE SIGN_COUNT<?`** 单调写内聚组件——并发竞态方 affected=0 按失败处理；count=0 跳过+审计标记；断言成功/失败审计 userName 非空）。
  - `sendMfaCode` #8 分派重构（sms 原样/其余显式 `ERR_AUTH_MFA_CODE_UNSUPPORTED`）；credential 管理三 API（list 走生成物 OutputBean——masked/not-pub 生效不暴露 credentialId/publicKey；越权归一不存在；最后一把 enabled 拒绝）；受限会话白名单增补 `confirmWebauthnRegistration` + 不入白名单裁定 javadoc；`nopWebAuthnAuthenticator` bean 注册。
  - 测试（29 用例绿）：`TestWebAuthnAuthenticator`（17 组件级正负例——测试侧客户端 `WebAuthnTestClient` 以 JDK EC P-256 + 传递依赖 cbor 构造协议合法 attestation/assertion，POC 结论 (e) 实证）+ `TestWebAuthnMfaE2E`（12 E2E：全链 bind→confirm→login→options→mfaVerify→token / verifier 双调用点计数接线 / 操作级票流 / sendMfaCode 显式拒绝 / 解绑 ceremony / credential 管理负例 / 重复注册 / 失败计数不消费超限作废 / 会话绑定 / 受限 proof 继承 / 白名单裁定 / 未绑定用户无感知）。
  - 验证：`./mvnw test -pl nop-auth/nop-auth-service` 全量 294 tests 0 failures（既有 MFA 套件断言零修改，3 skipped 为 pre-existing）。
  - **checkstyle 裁定（收口期修正）**：受影响三模块 `-Pqa` checkstyle **0 violations 通过**（closure audit 复核确认）；Phase 2 执行期的悲观注记基于无 profile 的直接 `checkstyle:check`（sun 缺省规则集，HEAD baseline 即失败）——非仓库门禁口径，以 `-Pqa` 为准。

- **Phase 3（E2E 全链 + 防重放 + 零回归 + 审计）完成**：
  - 新增 `TestWebAuthnMfaAdvancedE2E`（12 用例）：防重放矩阵六项（challenge 一次性 / options 只读幂等 + 断言单次消费 / signCount 跨 challenge 回退拒绝 / **并发写竞态不覆盖**（DB 预推进 count=10 后断言 count=7 → MFA_FAIL 且计数保持 10——条件 UPDATE 语义钉定）/ count=0 跳过单调 + zero-counter 审计标记 / origin fail-closed + fail 审计）；受限会话 webauthn 升级全链（minMfaLevel=3 → 受限登录 → proof 码 → bindMfa(proof) → confirm（checker 直调钉定白名单）→ 重新登录完整两阶段 + mfaRestricted 消失）；OAuth 入口（MfaLoginPolicyServiceImpl 副本触点③ challenge 携带 cryptoChallenge → 公开 options → 断言 → SSO accessCode 出口）；双 credential 生命周期（解绑→重绑 ceremony 注册两把 / 禁用一把登录用另一把 / 移除 disabled / 最后一把拒绝）；换绑 webauthn→totp（一期路径回归）；双 ceremony 组合解绑（operation-mfa 开启：操作级票 + unbind 断言两段，signCount 各自单调推进）；审计六事件 CapturingAuditService 断言 + 全事件 userName 非空（W13 教训专项）。
  - 执行期修复：advanced 套件 wiring 三处（受限标志未设 / checkMfaForUserName 需 runInSession / SmsCodeStore 需跨组件共享单实例）；`WebAuthnTestClient` EC 初始化偶发竞态加退避重试。
  - 验证：全量 `./mvnw test -pl nop-auth/nop-auth-service` **306 tests, 0 failures, 3 skipped**（294→306 = 新增 12；既有套件断言零修改）。

- **Phase 4（文档同步 + 设计回写 + 收口验证）完成**：
  - `docs-for-ai/03-modules/nop-auth.md`：新增"WebAuthn/FIDO2 硬件因子（W14）"章节（三 ceremony/断言验证语义/防重放/操作级与受限会话联动/审计/sendMfaCode 分派）；实体表补 NopAuthMfaCredential；源码锚点表补 WebAuthnAuthenticator/MfaChallengeHelper/webauthnAuthOptions；用户自助 API 表补 5 个新端点并校准行号；配置表补 `nop.auth.mfa.webauthn.*` 三键；错误码段补 CODE_UNSUPPORTED/LAST_CREDENTIAL。
  - 设计 `02-mfa-phase2-design.md`：§5.3.0 补 challenge 创建三触点 drift 行；新增 §5.3.6 W14-impl 裁定标注（库 API 核定/userHandle 权威值/五参统一载体/signCount 条件 UPDATE/白名单裁定/三触点收敛/执行期偏离八条）。
  - 验证（输出存 `_tmp/phase4-*.log`）：三模块 `./mvnw test -pl nop-auth/nop-auth-service,nop-service-framework/nop-biz-auth-api,nop-service-framework/nop-biz-auth-core -am -T 1C` BUILD SUCCESS（306 tests 0 failures）；scan-hollow high 0 NEW（1 条为 baseline 既有历史裁定）；check-doc-links --strict 0 errors。

- **收口（Closure）**：plan `2026-08-17-2212-1` `Plan Status: completed`（2026-08-18）。
  - 独立 closure-audit fresh subagent（`ses_feed891c6ffexYRCVglPplxvLc`）：Phase 1-4 全 PASS、15 Closure Gates 全 PASS、Anti-Hollow 全链追踪（bindMfa→challenge→confirm→登录→options→mfaVerify→completeLogin 逐 hop file:line，无空壳）、41/41 测试复跑绿、`-Pqa` checkstyle 0 violations 复核、check-plan-checklist exit 0——终判 **READY_TO_CLOSE**；证据已写入 plan Closure 段。
  - audit 发现处置：① Phase 2 checkstyle 悲观注记修正（`-Pqa` 实际通过）；② 交付未提交——按 nop-git-master 提交（见 git log）。
  - roadmap W14-impl `planned` → `done`（Last updated 同步 2026-08-18）。
