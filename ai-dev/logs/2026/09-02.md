# Dev Log 2026-09-02

### 2026-09-02（mission nop-stream-productization EXEC_PLANS：plan 2216-1 Phase 3——暴露面）

- Phase 3 完成（P-REQ-3/4/6）：**`io.nop.stream.runtime.ops` 包**——`StreamOpsHttpServer`（JDK HttpServer，宿主 coordinator 进程；`GET /metrics` 默认 TextFormat 0.0.4 + `Accept: application/openmetrics-text` 协商 OpenMetrics（`# EOF` 结尾）；PrometheusMeterRegistry 启动时挂为组合注册表成员；根 context 兜底结构化 404；未启用配置 start() 显式抛 IllegalStateException——无静默空监听）、`StreamOpsConfig`（enabled/port(0=临时)/bind/metrics.enabled 配置键，默认关闭）、`IOpsJobRegistry`（进程内 coordinator 注册面，Phase 4 扩展）、`StreamMetricsReporter`（log/file 两类周期 sink：MeterPrinter 快照 → stdout 日志或原子重写文件）。
- P-REQ-6 checkpoint 观测：`CheckpointHistoryEntry`（@DataBean，status/triggerTimestamp/durationMs/sizeBytes/failureCause/recordedAt）+ CheckpointCoordinator 有界历史（newest-first，默认 100 条 setCheckpointHistoryMaxEntries 可调）挂在完成/失败/中止三个真实路径（与事件/引擎 meter 同点）；REST `GET /jobs/{jobId}/checkpoints` 返回 overview（CheckpointMetricsSnapshot）+ history。
- P-REQ-4 模板：`_vfs/nop/stream/conf/metrics.properties.template`（prometheus-pull/log/file 三类引擎内建 sink 注释样例 + JMX/PushGateway 外部模式「引擎不内建」诚实标注 + 治理配置键备忘）。
- **两个关键缺陷修复（测试发现）**：① micrometer 弱引用 gauge 语义——gauge 状态对象（supplier/recorder）仅弱持有，GC 后 gauge 从 scrape 输出静默消失 → EngineMetrics/TaskNodeMetrics 静态强引用保留 + RocksDB recorder 由 backend 字段强持有；② RocksDB gauge 在 backend.close() 后读取 closed native handle → **JVM SIGSEGV（exit 134）** → recorder.close() 先于 native close 解引用（synchronized 防 scrape 竞态），readProperty 对 detached handle 返回 NaN。
- 新增测试：`TestStreamOpsHttpServer`（TextFormat004 三级指标族/OpenMetrics 协商/405/结构化 404×2/disabled 拒启）、`TestCheckpointHistory`（COMPLETED/FAILED 带因/ABORTED 带因/有界 newest-first）、`TestMetricsConfigTemplate`（模板存在 + ≥3 sink 样例）、`TestMetricsExposureE2E`（真实 coordinator+TaskManager+checkpoint 完成全链路 → /metrics 三级族 + /jobs/x/checkpoints overview=1/status=COMPLETED/failureCause 字段；file sink 周期快照）、`TestRocksDBMetricsRecorder` 增 close-解引用用例。gate-inventory 补 `setCheckpointHistoryMaxEntries`。
- owner doc Phase 3 段落落位（/metrics 契约 + 配置键 + 挂载语义 + 周期 sink + checkpoint 观测结构）。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime -am -T 1C` BUILD SUCCESS（891 tests）；`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS；doc-links --strict exit 0。

### 2026-09-02（mission nop-stream-productization EXEC_PLANS：plan 2216-1 Phase 2——指标与事件基座）

- Phase 2 完成（P-REQ-1/2/8）：**core 新增 `io.nop.stream.core.metrics` 包**——`StreamMetricsRegistries`（进程级 CompositeMeterRegistry，base SimpleMeterRegistry 成员保累计值 + 后端成员挂载；修正过「无成员 composite 丢增量」问题：成员为空时 counter.increment 被丢弃，加 base 成员后 in-process 读取与测试可见全量历史）、`StreamTaskMetrics`/`TaskMetricsHandle`（可序列化可重绑定 holder——micrometer 实现非序列化，readObject 回落 NOOP）、`MicrometerStreamTaskMetrics`（operator 层 records.in/out + processing.time；io 层 consumed/emitted）。
- **数据面热路径接线**（core）：StreamTaskInvokable 输入门分发点计 recordsIn+链处理计时；RecordWriterOutput.collect 计 recordsOut+emitted（双构造器保留 pinned-contract 单参形态——TestOutputContractInvariant 反射构造契约）；StreamSourceOperator/SourceReaderOperator 增 recordCounter seam（wireMailboxToHeadSource 处接 `taskMetrics::recordsConsumed`）。LOCAL 路径 GraphModelCheckpointExecutor.registerTasksAndTrackers 注入、REMOTE 路径 TaskManager installInvokable/deployTask 注入。
- **runtime 新增 `metrics`/`event` 包**：`EngineMetrics`（engine 层 7 指标，CheckpointCoordinator 完成/失败/中止真实路径 + JobCoordinator.globalRecovery + nodes.active gauge）、`TaskNodeMetrics`（task 层 4 指标，TaskManager 部署/取消/失败/终态路径 + running gauge）；`StreamJobEvent`/`StreamJobEventListener`/`StreamJobEventBus`（监听器异常捕获记录不外抛）+ `LoggingJobEventListener`（`nop-stream job event:` 前缀稳定 marker）；JobCoordinator 构造器注册内建日志监听并把总线注入 CheckpointCoordinator；start/failJob/terminate(CANCEL/DRAIN/SUSPEND)/globalRecovery 真实生命周期点发事件。
- **rocksdb**：`RocksDBMetricsRecorder`（6 个 aggregated-property gauge）挂 RocksDBKeyedStateBackend.openDB() 真实打开路径；注册无副作用不设开关（与 §3.1「注册总是生效」契约一致，设计文档 §3.10 同步修正）。
- 新增测试：`TestMicrometerStreamTaskMetrics`（core，注册/计数/handle 序列化回落/composite 成员挂载）、`TestEngineAndTaskNodeMetrics`、`TestStreamJobEventBus`（派发/异常隔离/null 忽略）、`TestJobCoordinatorLifecycleEvents`（JOB_STARTED/JOB_FAILED 带因/JOB_CANCELED/总线共享/内建监听器默认注册）、`TestRocksDBMetricsRecorder`（非空读数 memtable>0 numKeys≥2 + 六 gauge + 打开路径落 composite）、`TestObservabilityWiringE2E`（LOCAL 真实执行：12 记录全链路 + checkpoint 完成 → engine completed≥1 / io consumed=emitted=12 / operator recordsIn=12；测试 helper 需按 jobId 跨 vertex 求和——多 vertex 同名 meter 语义）。
- 门禁同步：gate-inventory.json 补 `CheckpointCoordinator.setJobEventBus`（change-type method 两向精确匹配门禁）。
- owner doc `docs-for-ai/03-modules/nop-stream.md` 落指标名语义表（五层全量 + 族视图映射 + 事件监听契约）——唯一权威名表。
- 验证：`./mvnw test -pl nop-stream/nop-stream-runtime,nop-stream/nop-stream-rocksdb -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS。

### 2026-09-02（mission nop-stream-productization EXEC_PLANS：plan 2216-1 Phase 1——可观测性正式裁定与设计基线）

- Plan `2026-09-02-2216-1-observability-ops-productization.md`（item 16）Phase 1 完成（Decision phase，无代码变更）。
- 新增 `ai-dev/design/nop-stream/observability-design.md`：P-REQ-1..12 正式三态裁定（go×11 / defer×1——P-REQ-9 Web 控制台 defer 含 revisit 条件：首个用户需求或 item 18 验收前复核）；thread-dump 端点分期裁定 = 随本设计交付（纯读取渲染成本为零，live 证据不支持后置）。
- 关键架构决策（均含拒绝替代方案）：① 指标基座 = micrometer + nop-stream 进程级组合注册表（拒绝全局替换单实例——替换时已注册 meter 不可见）；② 暴露载体 = JDK 内建 HttpServer 运维端点、默认关闭（拒绝 nop-http/Jetty/servlet 容器与消息 RPC 转发）；③ REST 提交语义 = 工厂引用（jobId + pipelineFactoryClass + 参数）由 coordinator 进程受理、复用 start→assignTasks→deployTask 同路径（拒绝 XDSL 文本/序列化 JobGraph）；④ 健康状态机 = 七态扩展自 JobStatus、真实事件驱动（CREATED/RUNNING/RECOVERING≈REBALANCING/DEGRADED≈PENDING_ERROR/FAILED/CANCELED/FINISHED；拒绝独立推断体系）；⑤ AlertChannel = runtime 内轻量渠道（Logging+Webhook；拒绝 nop-integration 依赖）；⑥ 重置与 reshard 入口收敛为同一工具族子命令；⑦ owner doc 落点 = 新建 `docs-for-ai/03-modules/nop-stream.md`（拒绝仅扩展 module-groups.md——契约面超出路由表承载粒度）。
- OpenMetrics 落地能力核验：本地 micrometer 1.14.7 PrometheusMeterRegistry（包 io.micrometer.prometheusmetrics）`scrape(acceptHeader)` 支持内容协商（默认 TextFormat 0.0.4 / Accept openmetrics-text 输出 OpenMetrics）——设计 §3.3 协商方案的 API 前提成立（生效版本由 spring-boot-dependencies 3.5.14 管理 = 1.15.11，API 同形）。
- 设计 README 索引更新（运维层 + 阅读顺序 #14）。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0。

### 2026-09-02（mission nop-stream-productization DRAFT_PLANS：items 16/15 两计划起草 + 三轮审查共识 + roadmap 写回）

- 取剩余 `todo` 前两项可起草工作（item 16/15；item 17 依赖 16 产出、item 18 依赖 16+17，本轮不起草；follow-up items 19—30 维持调度序）。执行序裁定 = 文件名序：item 16 先行（解锁 17→18→M4 关键路径），item 15 偏好其后（消费指标面；runbook §5 明示「稳定性量化属 item 15」以 item 16 为前提）。
- **Plan `2026-09-02-2216-1-observability-ops-productization.md`**（item 16，active）：P-REQ-1..12 正式三态裁定（Phase 1，D-GAP §3.3 初步建议为输入）+ 交付 phase 划分（指标/事件基座 → 暴露面 → 运维操作面 → 健康/告警 → 手册深化）。关键裁定入 plan：REST submit 作业提交语义决策为 Phase 1 必答（否则 Phase 4 空洞）；owner doc 落点裁定属 Phase 1、指标名表唯一权威落位随首增量建文档；既有面扩展非 greenfield（CheckpointMetrics failureCause 已存在、JobStatus/getJobStatus RPC 已有、nop-commons 已依赖 micrometer-prometheus）。
- **Plan `2026-09-02-2216-2-stability-performance-exercise.md`**（item 15，active）：诚实基线——可复用仅 C0—C3 有界演练路径（source fixture 有界、无 chaos 循环驱动、无采样装置），故设 Phase 2 演练驱动基建（负载生成/kill 驱动/采样 + 参数化联合 gated 入口为 Phase 3/4 唯一执行入口）；演练格结局三态分类（pass/fail/triggered-known-defect——item 28 hang 触发以「证据+归属路由」闭环不阻塞收口）；preserve-artifacts 强制（默认删 runDir 则 closure 抽样无据）；执行顺序为偏好非硬前置（先于 item 16 执行时 Phase 1 落代理观察降级裁定）。
- **审查流程**（guide 迭代对抗性审查）：round 1 两独立 subagent（各 1 Blocker：REST submit 语义缺决策 / 演练基建工作量无归属）+ round 2 两独立 subagent（plan1 owner-doc 落点权威链三方矛盾 Major、plan2 联合入口归属 + Phase 3/4 doc 裁定 2 Major）→ 全部修复 + advisory 项同批落地 → round 3 独立 subagent 终审：两 plan「executable — consensus reached」（余 cosmetic minor 非阻塞）。Plan Status draft → active。
- roadmap 写回：items 15/16 `todo → planned`（plan 路径 + 共识记录）+ Last updated 同步。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit 0（见下）。

### 2026-09-02（mission nop-stream-productization EXEC_PLANS：plan 2217-3 closure + roadmap item 14 + ★M3 写回）

- Plan `2026-09-01-2217-3`（item 14 复合场景分布式落地）关闭（Plan Status → completed，Completed: 2026-09-02）：两 Phase 全 completed，Closure Gates 13/13 勾选，Deferred 区 3 条路由裁定（C2 keyed-P>1 → CONN-01 successor/item 29；backpressure 量化 → item 15；离线 reshard 多 JVM → D5 不入矩阵）。
- Closure audit：独立 general subagent（fresh session `ses_f9d9ba880ffeUBCdg4ulKxjRej`）**CLOSURE-APPROVED**——14 项核验 13 PASS（gated 四套件复跑 9/9 绿 + C2/C3 复跑、Anti-Hollow 逐环节链路追踪、P>1 门禁/诚实性/文档一致性核对）+ 1 Blocker（bug note 路径缺 `nop-stream/` 前缀致 doc-links exit 1）当场修复复验 exit 0。证据见 plan Closure 节。
- roadmap 写回：item 14 `todo → done` + **★M3 分布式场景基线 done**（13+14 齐，M1/M2/M3 全绿，余 M4 = items 16—18）+ Last updated 同步。
- Commit: mission-driver 统一收口（nop-git-master）。

### 2026-09-02（mission nop-stream-productization EXEC_PLANS：plan 2217-3 分布式矩阵收口——C2/C3 + 运行手册 + 回归修复）

- 续接中断执行（前 session 已落地 Phase 1 基建 + S1/S2 C0/C1 gated 测试与 7 项分布式缺陷修复，未验证未记录）；本轮完成：
- **默认套件回归修复**：完成态任务保留注册表项（尾部提交）后 `getRunningTaskCount()` 仍返回 `size()` → 6 个 embedded/rpc distributed 测试 60s 超时——`RunningTask.finished` 标志 + 计数只统计未终态任务；`gate-inventory.json` 同步 `abortAllPendingCheckpoints`。
- **C2 restore-rescale**（`TestS2RestoreRescaleMultiJvmE2E`，gated）：run 1（2 TM）durable checkpoint + 部分输出后整集群停止 → run 2 相同 jobId/checkpoint 目录 + 3 TM + 严格更大 fencingEpoch → cursor/keyed/manifest 跨 JVM 恢复、终态 == 全量期望集（TM 日志「restored from durable epoch 2」+ epoch 2/4/5 无重复提交为直接证据）；`MiniStreamCluster` 增 `withJobId`/`withCheckpointDir`/`getRunDir`。keyed-P>1+2PC 形态显式路由 CONN-01 successor + item 29（引擎硬门禁）；离线 reshard 按 D5 不入矩阵。
- **C3 backpressure**（`TestScenarioBackpressureMultiJvmE2E`，gated，S1+S2 两格）：`ThrottledScenarioSinks`（生产 2PC 类子类，bean 内有界节流 + release marker 跨 JVM 控制）——节流期间 durable epoch 严格推进（`waitForDurableEpochAdvanceUnderThrottle`）+ 释放后精确期望集；工厂增 `sinkThrottleMs`/`throttleReleaseFile` 参数（缺省 0 = 原行为）。
- **运行手册初稿**：`ai-dev/design/nop-stream/distributed-runbook.md`（拓扑/启动顺序/checkpoint 与恢复/演练步骤与 gated 命令一一对应/已知边界含 item 28 与 fencingEpoch 缺省警示）；位置裁定 ai-dev/design（stream 文档集中地，docs-for-ai 迁移归 item 17）；设计文档 A.0-D 分布式落地裁定 8 条回写；fraud-example README 分布式命令 + S2 rescale note 更新。
- bug note 落库：`ai-dev/bugs/2026-09/2026-09-02-distributed-scenario-engine-defects.md`（7 项分布式缺陷：remote-deploy checkpoint 接线三空白/barrier 扇出面/timeout abort 级联/新鲜启动 initializeState 缺失/尾部提交丢失/陈旧 attempt 注册表竞态/running 计数语义漂移）。
- 验证：gated 启用态 S1 2/2 + S2 2/2 + C2 1/1 + C3 2/2 + 序列化 6/6 = 13/13 绿 + runtime legacy gated 7/7 绿；`./mvnw test -pl nop-stream -am -T 1C` 默认态 BUILD SUCCESS；hollow（fraud-example+runtime）/invariants/doc-links exit 0。item 28 两项遗留核验：remote-deploy 通道真实流量未触发 hang（13 用例超时内完成）、JDBC 后端 Stage-31 路径未触发（存储均为 LocalFile）——显式记录未绕过。

### 2026-09-02（mission nop-stream-productization EXEC_PLANS：plan 2217-2 closure + roadmap item 13 写回）

- Plan `2026-09-01-2217-2`（item 13 复合场景单进程落地）关闭（Plan Status → completed，Completed: 2026-09-02）：两 Phase 全 completed，Closure Gates 13/13 勾选，Deferred 区 2 条裁定（A2-4 LOCAL P>1 路由 plan 3 / 有界源末窗 EOS in-flight watch-only）。
- Closure audit：独立 general subagent（fresh session `ses_fa1ad6cbfffeumKpvD5XpqeGGx`）**CLOSURE-APPROVED**——A.1—D.13 全 PASS（Phase 1/2 exit criteria live 复核、附录 A 方法名 12/12 命中、6 项引擎修复 live 锚点、场景 5 类 10/10 复跑绿、Anti-Hollow 端到端调用链（XDSL → builder → operators → sink 可观察产物）、路由/bug-note/roadmap/log 诚实性核对；附带观察：交付待 commit——mission-driver 统一收口）。证据见 plan Closure 节。
- roadmap 写回：item 13 `todo → done`（含场景资产/引擎修复/路由裁定摘要）+ Last updated 同步（next todo: item 14）。M3 解锁余 item 14。
- Commit: 待 mission-driver 统一收口（nop-git-master）。

### 2026-09-02（mission nop-stream-productization EXEC_PLANS：plan 2217-2 Phase 2 收口——恢复/rescale/reshard 断言 + 门禁）

- Phase 2 完成：S1 恢复（A1-6/A1-7：run1 W0 片段 → durable manifest 恢复 → offset 重放 → 终表=全量期望集；bob 行依赖恢复的 keyed 历史——hollow restore 可检测；ledger epoch 严格递增）；S2 恢复（W1 跨恢复窗口续算）× memory/RocksDB 双后端（A2-6）；离线 reshard（A2-5：真实场景 savepoint 128→256 + key 守恒 + 迁移后恢复执行 + cursor 保持不重读）；断言→测试映射表落入 plan 附录 A（14 断言全归属 + A2-4 显式路由）。
- A2-4 LOCAL P>1 路由裁定（设计文档 A.0 裁定 5）：引擎 2PC sink 有效并行度 >1 build 期 fail-fast（CONN-01 P1 defer，不可降级硬门禁），与 exactly-once 文件 sink 无法同表 → restore-time parallelism rescale 的 P>1 LOCAL 形态路由 plan 3（矩阵 C2 必格）；LOCAL 侧面覆盖 keyed 续算/双后端/reshard 再路由。
- 测试稳定性修复（3 轮迭代）：类内共享 checkpoint storage/静态 H2 表导致的跨方法污染（per-run storage + 表清理）；「末段 flush 后无 trailing collect → 提交 checkpoint 的 ACK 阻塞在 finish linger → 与 coordinator 关闭竞态」类 flake（全部 fixture 补 trailing 发射，使提交 checkpoint 在发射期内完成）；CDC focused 测试 cancel 竞态（改为动态 consumed 前缀推导）。连续 5 轮 57/57 绿 + `./mvnw test -pl nop-stream -am -T 1C` BUILD SUCCESS。
- 工具门禁：hollow scan（fraud-example --severity high）exit 0；invariants exit 0（`output-contract-registry.json` 三处行号锚点随引擎修复漂移，按扫描器实测行号刷新——纯锚点维护，无规则变更）；doc-links --strict exit 0；plan-checklist --strict exit 0。
- Doc-sync: `composite-scenario-design.md` A.0 落地裁定（7 项）；README 双入口叙事重写（场景资产结构 + Scenario Notes + Known Limitations 更新）；bug note 落库。

### 2026-09-02（mission nop-stream-productization EXEC_PLANS：plan 2217-2 Phase 1 + 场景发现的引擎缺陷就地修复）

- Phase 1 完成：fraud-example 扩展（pom +flow/connector-debezium/connector-jdbc/connector/runtime/rocksdb/h2(test)，D8）；S1 XDSL `fraud-s1-cdc.stream.xml`（26 transforms：共享前缀 source→wm→decode→keyBy→enrich→keyBy + 4 条 cep→keyBy→window→aggregate→sink 链；`<patterns>` 4 欺诈模式 next-链声明；STRICT_EXACTLY_ONCE checkpoint）；S2 `fraud-s2-file.stream.xml` + `fraud-s2-file-delta.stream.xml`（x:extends 黑名单 filter + 边改接）；死文件 `fraud-detection.stream.xml` 删除（Gap A）。
- 场景主代码：`EnrichedTransaction`/`UserHistoryEnricher`（keyed ValueState 均值+prevCity——`UserTransactionHistory` 复活 + UnusualAmount 去 stub）/`FraudAlertPatternFunction`/`AlertCountAggregate`+`AlertSummaryRow`/`TransactionWindowAggregate`+`TxSummaryRow`/`CdcChangeDecoder`/`TransactionLineParser`/`ReplayableCdcSourceFunction`（createMessageSource 注入点 + 生产 offset 路径 + 有界完成 + linger）/`DirectoryFileSourceFunction`（per-file 字节 cursor checkpoint + REPLAYABLE + rescale 空态守卫）。
- **场景发现并就地修复 6 项引擎缺陷**（详见 `ai-dev/bugs/2026-09/2026-09-02-composite-scenario-uncovered-engine-defects.md`）：① JobGraphGenerator 虚拟节点链映射 HashMap 顺序依赖（S-10 unmapped-edge）+ 上游映射时跨 vertex JobEdge 丢失 hash partitioner（P>1 下 keyBy 静默退化为 forward）→ Kahn 拓扑迭代 + 递归 findUpstreamVertex + partitioner 传播；② ProcessOperator（`keyed.process` DSL 正门）未装配 keyed backend → open() 按 Window/Cep 先例装配 + 延迟恢复；③ CEP NFA/SharedBuffer 状态 JSON 持久化不可用（P2-INV-6，引擎测试自认 "not possible in production today"）→ `JavaStreamSerializer`（core typeutils）+ NFAState 可序列化比较器 + EventId/NodeId @DataBean + 状态图 Serializable；④ 自定义序列化 byte[] 经 JSON 持久层退化为不透明 String → `__java_bytes__` base64 marker 往返 + 恢复态采纳算子侧 serializer；⑤ 2PC `pendingCommits` JSON 往返后键变 String → setPendingCommits 键归一化 Long；⑥ 终态 checkpoint barrier 写入已完成 partition 竞态（良性 checkpoint 致 job 失败）→ finished partition 上 barrier 冗余性论证后带日志丢弃（data record 仍 fail-fast）。
- XDSL pattern 链接语义确认：`<single next="..." followKind="next">` 链接属性是链式模式必需（旧死文件同样缺失——其单态化即破损根因之一）；`TestS1PatternDeclarations` 钉定（NFA 级 4 模式精确匹配/拒绝）。
- 引擎 CEP 语义适配（fixture 裁定，非缺陷）：CEP 严格 watermark 门控（ts ≤ wm 即丢）+ 恢复后 watermark 连续性 → fixture 乱序控制在 bounded delay 内、slice 事件时间严格高于前一运行持久化 watermark；S1 四链 per-chain ledger 表（ledger PK 无 vertex 维度）。
- 回归：core 1516 / cep 359 / runtime+connectors+flow+rocksdb / fraud-example（新增 28 用例）全绿；1 项既有测试（TestCepCheckpointRestoreE2E NFA 往返）随序列化形态更新为 marker 语义。
