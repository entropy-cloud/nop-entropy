# 2026-08-17

### W12-impl 执行 Phase 4：文档同步 + 收口验证 + 独立 closure audit（plan completed）

- **Plan**：`ai-dev/plans/2026-08-16-2321-2-mfa-operation-level-stepup.md`（**Plan Status: completed**，四 Phase 全勾选 + Closure Gates 全勾选 + Closure Evidence 写入）
- **docs-for-ai 同步**：`03-modules/nop-auth.md`——新"操作级 MFA"章节（注解声明与两构建期约束/元数据传播链/两段式流程与判定要点/首批标注与 deferral/MfaFactorVerifier/审计/store 场景化与 Redis 滚动升级 note）+ 功能概览 bullet + 核心实体表 MfaChallenge 描述 + 配置表两新项 + 错误码段（ERR_AUTH_OPERATION_MFA_REQUIRED 区分登录期/会话期）+ 源码锚点表三行（OperationMfaCheckerImpl/MfaFactorVerifier/mfaVerifyOperation）；`03-modules/nop-credential.md`——新"敏感操作标注（@MfaRequired，W12 机制可用）"章节（标注示例/开关/前置条件/deferral 状态）；`INDEX.md` 路由行扩展；`04-reference/source-anchors.md` 新增 `AUTH-MFA-003`（操作级 MFA 全链锚点）。
- **设计回写**：`02-mfa-phase2-design.md` 新 §3.6"W12-impl 裁定标注（2026-08-17 回写）"八条——deepClone/nop-biz 两处元数据拷贝触点补充、@BizAudit 装饰性事实与 saveAudit 落点偏离、票传递通道最终裁定（原案成立）、批量拦截实现语义、首批标注与 deferral、MfaFactorVerifier 契约细化、SPI 签名依赖面事实、Redis 滚动升级 migration note（含 @DataBean pre-existing 修复）。
- **roadmap**：W12-impl `planned` → `done`（收口记录 + Last updated 2026-08-17）；W13-impl ~ W15-impl 依赖解锁。
- **收口验证（全绿）**：五模块测试（graphql-core 80 / nop-biz 28 / biz-auth-core 57 / nop-auth-service 223/228——5 error 为 pre-existing missing-tenant-id 顺序 flake，TestMdxQuery 单独运行 5/5 绿）；`./mvnw test -f nop-service-framework/nop-graphql/pom.xml` 聚合 BUILD SUCCESS（graphql-core/orm/message 下游回归）；mission verify `./mvnw test -pl :nop-auth,:nop-ai-gateway,:nop-nosql -am -T 1C`（fork）全 reactor 除同一 pre-existing flake 外全绿（含 842/84/32 用例模块全 PASS）；`scan-hollow-implementations.mjs --module nop-auth --severity high` 本 plan 触碰文件 0 NEW（唯一 high 为 pre-existing OAuthLoginServiceImpl not-impl 占位，W13 范围）；`check-doc-links.mjs --strict` exit 0（0 errors，1 warning 为本 plan 反引号相对路径，W11 先例同口径）；`checkstyle:check -Pqa` 五受影响模块 BUILD SUCCESS；`check-plan-checklist.mjs --strict` exit 0（1 passed / 0 failed）。
- **独立 closure audit**：fresh subagent（task `ses_ff3bf6333ffeecublIk99q2qPG`）只读审计 12/12 检查 PASS，**CLOSURE_APPROVED**——Anti-Hollow 调用链 live 追踪（mutation 入口 → executor 两检查点 → engine 注入透出 → OperationMfaCheckerImpl 判定链 → store 原子原语 → 票消费）完整连通；审计观察项（closure 簿记 + 提交）已在收口动作中完成。
- **pre-existing 登记（Non-Blocking Follow-ups）**：nop-auth-service 全模块套件顺序 flake（全局租户配置跨测试类污染，baseline 复现；测试基建治理）；workspace 全量 test-compile 偶发 plexus javac 进程内 CME（`-Dmaven.compiler.fork=true` 绕过）。
- Doc-sync：`docs-for-ai/03-modules/nop-auth.md` + `03-modules/nop-credential.md` + `INDEX.md` + `04-reference/source-anchors.md` + 设计 §3.6 + roadmap。

### W12-impl 执行 Phase 3：MfaFactorVerifier 抽取 + OperationMfaCheckerImpl + mfaVerifyOperation 端点 + 首批标注 + 审计

- **Plan**：`ai-dev/plans/2026-08-16-2321-2-mfa-operation-level-stepup.md`（Phase 3 completed）
- **`MfaFactorVerifier`**（新，io.nop.auth.service.mfa）：收敛三处因子校验——登录级 `verifySecondFactorAndComplete`（verifyTotp 方法整体删除、SMS 分支内联逻辑迁入组件）、绑定级 `verifyFactorForBind`（方法删除，confirmMfa/unbindMfa 直调组件）、`confirmMfa` 重复验证/窗口推进块（**移除**——组件推进为唯一路径，防静默死代码）。组件契约：布尔返回 + 失败计数/错误码留调用方；SMS EXPIRED 抛错两处行为逐字一致收敛进组件；TOTP authenticator null/secret 空统一返回 false（登录级经调用方 !ok 等价落 MFA_FAIL+计数）；TOTP 窗口统一推进内聚（lastVerifiedWindow+lastVerifiedAt 更新）；未知 mfaType fail-closed false。**等价重构证明**：TestMfaLoginE2E 10 / TestMfaUserSelfService 15 / TestScanLoginMfa 4 全部零断言修改通过（三测试仅 wiring 增补 mfaFactorVerifier setField）。
- **`OperationMfaCheckerImpl`**（新）：§3.3 判定链——operation-mfa.enabled（缺省 false）→ 无用户上下文兜底放行 → store 未装配放行 → setting status==enabled → 票核验（scene/verifiedAt/operation/sessionId 四条件 + 原子 consume 恰一放行）→ create(scene=operation, payload={operation,sessionId}, loginType=0) → 抛 `ERR_AUTH_OPERATION_MFA_REQUIRED`（三元组 errorParams）。票头 `X-Nop-Op-Mfa-Token` 大小写不敏感读取。审计 challenge-issued/ticket-consumed 经 saveAudit（operation+sessionId+challengeToken）。
- **`LoginApiBizModel.mfaVerifyOperation`**（首个登录态 action——省略 @Auth）：peek+scene==operation → 同会话（payload.sessionId）→ 已验证票拒绝重复 → setting 复核（runWithTenant + status==enabled + mfaType 硬校验）→ 组件因子校验 → 失败 incrFailCount 超限作废 + MFA_FAIL → markVerified 一次性（false=CHALLENGE_EXPIRED）→ **成功不签发任何凭证**。审计 verify-ok/verify-fail。请求载体 `MfaVerifyOperationRequest`（nop-biz-auth-api：challengeToken+code，无 recoveryCode 字段）。
- **首批标注**：`NopAuthUserBizModel` 五动作加 `@MfaRequired`——resetUserMfa/resetUserPassword/changeSelfPassword/unbindMfa/generateRecoveryCodes（引擎 schema 元数据专项断言）。
- **beans**：`nopMfaFactorVerifier`（ioc:default）+ `nopOperationMfaChecker`（ioc:type=IOperationMfaChecker, ioc:default——经 GraphQLEngine @Inject setter 按类型注入）。
- **测试**（新 18 用例）：`TestOperationMfaE2E`（容器级 JunitBaseTestCase+localDb，14 用例）——引擎元数据五动作/容器 bean→引擎注入断言/enabled=false 零介入/未启用用户不拦截/全链（引擎 GraphQL 文档路径拦截 errorCode + checker 直调 errorParams 三元组 + challenge scene/payload 钉定 + 错码 MFA_FAIL + 正确码验证 + 重复验证拒绝 + 携票重试 RPC 路径成功且 reset 实际执行 + 票一次性）/票绑定 operation（换操作拒绝且票未消费、原操作仍可用）/票绑定 session（换会话拒绝 + 跨会话验证拒绝）/失败计数超限作废（正确码也 EXPIRED + challenge 消费）/恢复码形态输入 MFA_FAIL + 契约无 recoveryCode 字段/scene≠operation 拒绝（含 login scene）/登录级 operation-token 残留钉定（watch-only）/跨场景 TOTP 窗口重放拒绝/SMS 分支闭环（无码 EXPIRED→发码→验证→重试成功）/审计四事件落 NopAuthOpLog（分批落库轮询容忍 + sessionId 断言）；`TestMfaFactorVerifier`（4 用例）——窗口统一推进+同窗口重放拒绝/SMS EXPIRED 抛错与 VALID/MISMATCH/未知 mfaType fail-closed/null authenticator false。
- **验证**：定向回归 228 用例 0 失败（含一期全部 MFA 套件 + 通道 + GraphQL logger + 引擎级）；graphql-core+nop-biz 108 全绿；全 reactor 227 用例中 221 绿、6 error 为 pre-existing missing-tenant-id 顺序 flake（baseline 已复现，见 Phase 2 记录；新增测试类改变执行顺序使泄漏窗口命中 TestMdxQuery/TestGraphQLMissing，两类单独运行全绿）。
- **执行期环境事实（非本 plan 代码）**：nop-auth-service 全量 test-compile 在本 workspace 触发 plexus javac 进程内 `ConcurrentModificationException`（JavaxToolsCompiler:139，clean baseline 同样复现——环境性）；`-Dmaven.compiler.fork=true` 稳定绕过。
- **偏差**：无功能性偏差（errorParams 三元组断言经 checker 直调取 NopException——GraphQL/RPC 响应层只暴露 errorCode，属响应面事实；引擎级断言 errorCode + 行为）。
- Doc-sync：操作级章节/设计裁定标注回写统一 Phase 4。

### W12-impl 执行 Phase 2：MfaChallengeStore 场景化 + ORM 加列 + 配置/错误码

- **Plan**：`ai-dev/plans/2026-08-16-2321-2-mfa-operation-level-stepup.md`（Phase 2 completed）
- **biz-auth-core**：`MfaChallenge` 增 scene/payload/verifiedAt（Long 可空）+ SCENE_LOGIN/SCENE_OPERATION 常量；`MfaChallengeStore` 接口增 7 参场景 create（老五参 default 方法委托 scene=login/payload=null，一期调用点零改动）+ `markVerified`（boolean 一次性迁移）；`MfaChallengeStoreConfig` 增 `opTicketExpireSeconds`（缺省 60）。Local：markVerified 为 ConcurrentHashMap 原子 compute + AtomicBoolean 恰一次；peek 增票窗口判定（过窗即 challenge 失效删除）。
- **DB store**：条件 `UPDATE SET VERIFIED_AT=? WHERE TOKEN=? AND VERIFIED_AT IS NULL AND EXPIRE_AT>?` + affected-row；peek/consume 的 verifiedAt 经原始 SQL 读回（绕过 ORM 会话缓存，对齐 readFailCount 先例）；peek 过窗惰性删除。
- **Redis store**：派生票键 `{token}:v` 经 `putIfAbsentExAsync`（SETNX+PX=opTicket 窗口）承载一次性；peek 显式赋值/清空 verifiedAt（幂等，不残留旧票状态）；consume 自行 get 存储原对象做 CAS 比对（不复用 peek 修饰对象）+ 成功后 best-effort 清理票键；新增 setConfig（beans 装配 nopMfaChallengeConfig 同源配置）。
- **ORM/DDL（model-first）**：`nop-auth.orm.xml` `nop_auth_mfa_challenge` 加 SCENE VARCHAR(20)/PAYLOAD VARCHAR(500,not-pub)/VERIFIED_AT BIGINT（propId 13-15）→ `mvn install` codegen 再生（`_NopAuthMfaChallenge` _PROP_ID_BOUND 13→16、三方言 `_create_`、xmeta、i18n、InputBean/OutputBean、_app.orm.xml）+ 三方言手写增量 `_add_mfa_challenge_scene_nop-auth.sql`（循 _add_ext_login_unique 先例）。
- **配置/错误码**：`NopAuthConfigs` 增 `nop.auth.operation-mfa.enabled`（false）/`op-ticket-expire-seconds`（60）；`NopAuthErrors` 增 `ERR_AUTH_OPERATION_MFA_REQUIRED`（params challengeToken/mfaType/operation，与一期登录期编码区分）；`auth-service.beans.xml` nopMfaChallengeConfig 增 opTicketExpireSeconds 装配 + redis store config 装配（装配不变式 collect-beans/条件激活/类加载安全零改动）。
- **pre-existing 缺陷修复（执行期发现）**：`MfaChallenge`/`SmsCodeEntry` 缺 `@DataBean`——平台 JSON 序列化缺省仅允许 DataBean（`nop.core.json.serialize-only-data-bean=true` 安全缺省），真实 Redis PrefixTextCodec 写路径会抛 only-data-bean-is-serializable（FakeNosql 存对象引用绕过序列化故 W4/W8 以来未暴露）。本 Phase JSON 兼容测试走真实 `PrefixEncodeHelper.encode → JSON.stringify` 路径暴露后补标记修复。
- **测试**（新 26 用例）：Local 8（场景 create 钉定/老五参委托/markVerified 恰一次+并发恰一赢家/票窗口内可见+过窗失效/consume 不受 markVerified 影响/过期 markVerified false）；DB 8（同语义：条件 UPDATE 恰一次+并发 affected-row/VERIFIED_AT 落库/过窗惰性删除/consume 携带 verifiedAt）；Redis 7（FakeNosqlService 补 putIfAbsentExAsync SETNX+PX 语义：票键恰一次/peek 映射 verifiedAt/票键 TTL 过期不残留/consume 在 peek 修饰后仍 CAS 命中）；`TestMfaChallengeJsonCompat` 3（$d: 编码往返保留新字段/新进程读老 JSON 新字段 null 天然兼容/老进程读新 JSON——缺省 reject 行为记录 + 开启 `nop.core.json.parse-ignore-unknown-prop` 后容忍（迁移缓解措施钉定；challenge TTL 300s 滚动升级排空窗口亦可规避））。
- **验证**：biz-auth-core 57 全绿（含 TestLocalMfaChallengeStore 14）；nop-auth-service 定向套件全绿（TestDbMfaChallengeStore 16/TestRedisStoreWiring 15/TestMfaStoreWiringDb 3/TestMfaLoginE2E/TestMfaUserSelfService 15/TestScanLoginMfa 等 92 用例 0 失败）；`./mvnw clean install -f nop-auth/pom.xml -T 1C -DskipTests` 全 reactor BUILD SUCCESS。
- **偏差（pre-existing flake 基线证实）**：nop-auth-service 全模块套件顺序运行时 `TestChannelScanBindLoginE2E`/`TestGraphQLMissing` 出现 `missing-tenant-id` 错误且跨运行漂移——**clean baseline（`git stash -u` 后）复现同等失败**，证实为全局租户配置（enable-tenant-by-default）跨测试类污染的 pre-existing 测试基建 flake（08-16 收口日志已登记"6 次环境性 flake"同源），与本 plan 变更无关；受影响类单独/成对运行全绿。
- Doc-sync：No owner-doc update required（DDL/加列/配置项属实现细节；Redis 滚动升级 note 随 Phase 4 migration note 登记）。

### W12-impl 执行 Phase 1：框架接线（@MfaRequired + 元数据传播四触点 + executor 两检查点 + SPI 装配）

- **Plan**：`ai-dev/plans/2026-08-16-2321-2-mfa-operation-level-stepup.md`（Phase 1 completed）
- **nop-biz-auth-api**（新 `io.nop.auth.api.mfa` 包）：`@MfaRequired`（RUNTIME/METHOD/无属性）+ `MfaRequiredMeta`（无状态标记单例）+ `IOperationMfaChecker` SPI（签名 `(operationName, IUserContext, requestHeaders)`——模块仅依赖 nop-api-core，IServiceContext 不可达，executor 侧取 `context.getUserContext()` 等价传递）。
- **nop-graphql-core**：`ReflectionBizModelBuilder.buildActionField` 读取注解 + 两构建期约束（`@MfaRequired`+`@BizSubscription` / `@MfaRequired`+`@Auth(publicAccess=true)` → 新错误码 `nop.err.graphql.mfa-required-not-allowed-on-subscription` / `-for-public-access`，fail-fast）；`GraphQLFieldDefinition.mfaRequiredMeta` 字段 + **deepClone 拷贝**（live 缺口补齐——makerCheckerMeta 该缺口维持原状，仅新字段必须补）；`GraphQLObjectDefinition.mergeField` 两分支拷贝；`GraphQLEngine` `@Inject @Nullable setOperationMfaChecker`（setActionAuthChecker 模式）+ `newGraphQLContextFromContext` 透出（makerCheckerEnabled 字段模式）；`IGraphQLExecutionContext`/`GraphQLExecutionContext` getter/setter；`GraphQLExecutor` 两检查点（executeOneAsync:65 / executeAsync:147，auth check 之后）新增 `checkOperationMfa`——仅对带 mfaRequiredMeta 的顶层 field selection 调 checker，订阅两路径（subscribeGraphQL/subscribeRpc）刻意不接（构建期拒绝保证）。
- **nop-biz**：`BizObjectBuildHelper.mergeBizModel` 增 `mfaRequiredMeta` 拷贝（`op.getMfaRequiredMeta() == null` 守卫，makerCheckerMeta live 先例同型）。
- **测试**（新 13 用例）：graphql-core `TestMfaRequiredMetadata`（builder 元数据设置/两约束构建报错/deepClone 保留/mergeField replace=true+false+新字段三分支/merge 不清除既有 meta）+ `TestOperationMfaExecutorWiring`（RPC 单操作路径 checker 计数=1 且收到全名 `bizObj__action`/非敏感方法零调用/无 checker bean 引擎零介入/checker 拒绝使 operation 失败/GraphQL 文档路径仅敏感 field 触发 + 拦截时整批预执行中止无部分执行副作用）；nop-biz `TestBizObjectManager#testMfaRequiredMetaSurvivesBizObjectMerge`（BizObjectManager 全链合并 + getGraphQLDocument deepClone 路径元数据保留）。
- **偏差**：无（SPI 签名以 IUserContext 替代设计伪代码的 serviceContext 属依赖面事实，语义等价——executor 侧从 context 传递）。
- **验证**：`./mvnw test -pl nop-service-framework/nop-graphql/nop-graphql-core,nop-service-framework/nop-biz,nop-service-framework/nop-biz-auth-api -T 1C` 全绿（graphql-core 80 / nop-biz 28 / biz-auth-api BUILD SUCCESS，框架独立测试零回归——无 auth 环境不受影响）；`./mvnw clean install -pl nop-service-framework/nop-graphql/nop-graphql-core,nop-service-framework/nop-biz,nop-service-framework/nop-biz-auth-api -am -T 1C -DskipTests` BUILD SUCCESS（上游 reactor 全量编译安装；三模块自身测试已全量跑过）。
- Doc-sync：No owner-doc update required（框架接线不改变 owner doc 契约；@MfaRequired 用法与 deepClone/nop-biz 触点设计标注统一 Phase 4 回写）。

### W11-impl(B) 执行 Phase 3：文档同步 + 收口验证（docs-for-ai RBAC 章节 + source-anchors CRED-005 + roadmap done 判定）

- **Plan**：`ai-dev/plans/2026-08-16-2321-1-credential-rbac-grant-authorization.md`（Phase 3 completed，进入 closure audit）
- **owner doc**（`docs-for-ai/03-modules/nop-credential.md`）：新增"RBAC 细粒度授权（W11 Part B）"章节（授权数据模型与幂等契约 / 消费侧判定矩阵表（第 2-6 行含第 5 行服务级信任与信任边界推论）/ 角色快照时效与字面 roleId 求交 / 引擎通道豁免裁定（可达性事实措辞）/ admin-only 管理面与七旁路禁用 / 父侧级联清理实现语义 / 收紧运维语义 / Web 授权编辑与共部署语义）+ 功能概览 bullet + 核心实体表 + 管理 API 面（grant/revoke）+ SPI 表与归属 per-method 矩阵交叉引用 + 关键配置 admin-roles 描述扩展 + 源码锚点（`NopCredentialAuthBizModel`）+ DDL 迁移段落补 `nop_credential_auth`。
- **source-anchors**：新增 `CRED-005`（RBAC 细粒度授权锚点：provider `assertRoleAuthForPlaintext`/`findGrantedRoleIds` + `NopCredentialAuthBizModel` + 父侧级联清理 + ORM 实体/关系）。
- **roadmap**：W11-impl `doing` → `done`——Part B 收口记录（plan 路径 + 交付摘要 + 双 plan closure audit 判定条件满足登记）；A1-audit 的依赖链由此解锁（W9/W10/W11 全 done）。
- **验证（全绿）**：`./mvnw clean install -pl nop-credential -am -T 1C` BUILD SUCCESS；`./mvnw test -pl nop-credential -am` 绿（service 166 / kms-vault 32 / web 1）；`./mvnw test -pl nop-ai -am` BUILD SUCCESS（消费链全绿）；`./mvnw checkstyle:check -pl nop-credential -Pqa` exit 0；`scan-hollow-implementations.mjs --module nop-credential --severity high` 全量 13 条 = 基线 6 条 Part A + 本 plan 7 条预裁定禁用动作（NopCredentialAuthBizModel 七动作，Part A 同模式：显式消息 + javadoc + 收口测试断言），**0 条非预裁定 NEW**；`check-doc-links.mjs --strict` exit 0（0 errors；2 warnings 为两份 plan 反引号相对路径提示，先例同口径）；`check-orm-icons.mjs` exit 0（驱动修复 oauth-state 缺失图标）；依赖边界：`dependency:tree` 零 nop-auth 构件 + `import io.nop.auth` 主源码 0 匹配。
- Doc-sync：`docs-for-ai/03-modules/nop-credential.md` + `docs-for-ai/04-reference/source-anchors.md` + roadmap。

### W11-impl(B) 执行 Phase 2：授权管理面（独立 BizModel + action-auth）+ Web 授权编辑 + GraphQL 引擎级测试

- **Plan**：`ai-dev/plans/2026-08-16-2321-1-credential-rbac-grant-authorization.md`（Phase 2 completed）
- **`NopCredentialAuthBizModel`**（codegen 裸骨架上定制，bean 注册零手工动作——`_service.beans.xml` 条目为 regen 自动产物）：查询面（findPage/findList/findFirst/findCount 经 `defaultPrepareQuery` + get/batchGet 前置）运行时 admin-only（`requireCredentialAdmin` 模式，无登录态同样拒绝，对齐 usage 先例）；**grant(credentialId, roleId)**——admin-only + 凭证存在/未删预检（NOT_FOUND/DELETED 归一）+ 拒绝 scope≠system（`auth-not-system-scope` 新错误码）+ roleId 仅非空校验（`role-id-required` 守卫；无跨模块存在性校验）+ 幂等（已存在 no-op 成功，唯一约束兜底并发）+ createdBy/createTime 审计戳；**revoke(credentialId, roleId)**——admin-only + 幂等（不存在 no-op 成功）+ 物理删除；标准 `save`/`update`/`delete`/`batchDelete`/`updateByQuery`/`deleteByQuery`/`copyForNew` 七动作禁用（`UnsupportedOperationException`，Part A 同口径）。
- **级联清理实现裁定（执行期发现）**：`cascade-delete` biz 级联的平台机制是经子 BizModel `delete` 动作逐行调用（`CrudBizModel.deleteReferences → queueCascadeDelete`），与"子 delete 禁用"互斥（首测即暴露：`NopCredential__delete` 级联撞上子 delete 禁用抛错）——授权行级联清理改由父侧 `prepareDeleteWithUsageCheck` 回调内经 dao 显式物理清理（usage 引用计数拦截之后：删除被拦截时授权行与凭证同存）；`auths` 关系保留 `cascadeDelete="true"`（ORM flusher 已加载集合级联）、tagSet 改 `not-pub`（生成 xmeta 验证 `published="false" internal="true"` 与 usages 同口径）。裁定回写设计 §6.3。
- **action-auth delta**：`nop-credential.action-auth.xml` 新增 `FNPT:NopCredentialAuth:query`/`mutation` `roles="admin"`（与生成基件同构嵌套，orderNo 对齐基件 10005/10006；BizModel 运行时判定为第二层）。
- **Web**（`NopCredential.view.xml` 详情页授权编辑最小面）：`view` page `afterForm` 追加"取用授权（RBAC 收紧）"子表（`@query:NopCredentialAuth__findPage` 按 `filter_credentialId` + revoke 撤销按钮含确认文案 + grant 新增对话框：select 角色选择器 source=`@query:NopAuthRole__findList/value:roleId,label:roleName`（跨 bizObj 先例）+ `creatable` 手工输入兜底，standalone 部署空源语义已在 desc/docs 注明）与"使用引用（收紧前核对）"子表（usage 共可见）。`NopCredentialWebPagesTest.validateAllPages` 绿。
- **测试**：新 `TestNopCredentialAuthBizModel` 11 用例（GraphQL 引擎级 + 容器接线即 exit criterion）——admin grant/revoke 幂等往返（重复 grant 恰一行/重复 revoke no-op）/ 非 admin + 无登录态 grant/get/batchGet/findPage 拒绝 / admin 查询面放行（含 createdBy 审计断言）/ grant user 级被拒 / grant 不存在凭证 NOT_FOUND / grant 已删凭证 DELETED / 空 roleId `field-empty-arg`（GraphQL 入口）+ BizModel `role-id-required`（直调守卫）/ 七旁路动作收口 / 凭证删除物理级联清理授权行 / usage 拦截时授权行与凭证同存 / 两层防御端到端（grant → 非授予用户直调 provider 拒 → 授予角色放行 → revoke 后仍拒（余一授权保持收紧态）→ 撤销全部回到开放态第 4 行）。**执行期测试语义修正**：初版"revoke 后再拒"在只剩一条授权时误期望拒绝——撤销全部记录按 §6.3 回到第 4 行开放态（设计语义非缺陷），修正为双授权场景 + 全撤销对称性断言。
- **验证**：nop-credential 全 reactor 绿（service 166（155+11）/kms-vault 32/web 1，零回归）。
- **设计回写**：§6.3 第二条 Part B 裁定标注——独立 BizModel 载体 + 七动作禁用 + roleId 不校验存在性测试固化 + 级联清理实现裁定。
- Doc-sync：设计 §6.3；docs-for-ai RBAC 章节统一在 Phase 3。

### W11-impl(B) 执行 Phase 1：NopCredentialAuth 实体 + provider 判定矩阵串联 + 引擎通道豁免裁定

- **Plan**：`ai-dev/plans/2026-08-16-2321-1-credential-rbac-grant-authorization.md`（Phase 1 completed）
- **ORM（model-first）**：`nop-credential/model/nop-credential.orm.xml` 新实体 `NopCredentialAuth`（表 `nop_credential_auth`：`authId` surrogate PK 对齐 usageId 惯例 + credentialId/roleId（(credentialId, roleId) 唯一键 `UK_NOP_CREDENTIAL_AUTH_CRED_ROLE`）+ createTime/createdBy 审计列；无 delFlag 物理删除对齐 `NopCredentialUsage` 惯例；`tagSet="audit"` 走 ChangeLog；`ext:icon="shield-check"`）+ `NopCredential` 侧 to-many `auths` 关系（`cascadeDelete="true"`、`tagSet="cascade-delete"`、不加 `pub`——对齐 Part A 后 `usages` 口径）。**驱动修复 pre-existing**：`NopCredentialOauthState` 补 `ext:icon="history"`（`check-orm-icons.mjs` 由 exit 1 → 0）。
- **codegen 全链自动产物**（`../mvnw clean install -DskipTests -T 1C` 于 nop-credential reactor 触发 postcompile/precompile，无手编生成物）：`_gen/_NopCredentialAuth.java` + retention `NopCredentialAuth.java` / `INopCredentialAuthBiz.java` / 裸 `NopCredentialAuthBizModel.java` 骨架 / xmeta retention 壳 + `_NopCredentialAuth.xmeta` / xbiz 双件 / `_service.beans.xml` 新 bean 条目（`NopCredentialAuthBizModel` + `biz_NopCredentialAuth` BizProxy）/ `_create_`+`_drop_` 三方言再生成 / `_add_tenant_` 三方言再生成 / web `pages/NopCredentialAuth/` 三件 / i18n 与 `_templates` 增量。
- **手写增量 DDL**：`deploy/sql/{mysql,postgresql,oracle}/_add_credential_auth_nop-credential.sql`（CREATE TABLE 型，循 `_add_oauth_state_` 先例，与 `_create_` 产物逐列一致含唯一键）。
- **provider 判定矩阵串联**（`CredentialProviderImpl`）：`getCredential` 在归属校验之后、`decryptToData` 之前插入 `assertRoleAuthForPlaintext`——仅 system 级（含存量 NULL）；无记录放行（第 4 行，一期行为，单次唯一键最左前缀点查无 join/远程）；有记录 + 无用户上下文（`hasLoginUser` 口径）放行（第 5 行服务级信任）；有记录 + 有用户上下文 → `IUserContext.getRoles()` 与授权 roleId 字面求交，非空放行、空集 fail-closed 抛新错误码 `nop.err.credential.role-not-granted`（`CredentialErrors` 新增，含 credentialId/roleIds 参数），admin 不自动豁免。`getCredentialData` 委托共享同一检查；`mask`/`testCredential` 不加授权检查；惰性刷新发生在检查通过之后。**引擎通道豁免**（Phase 1 Decision）：`engineGetDecryptedFields`/`engineUpdateTokenFields`/`engineUpdateInLock` 不查授权记录——明文不外泄 + 入口已门控 + 第 5 行信任边界一致；两个用户上下文可达调用点（`beginOAuthFlow`→`OAuthFlowService`、`saveCredential` oauth2 分组写）显式登记为裁定覆盖对象。
- **测试**：新 `TestCredentialProviderRbacAuth` 11 用例——矩阵第 4 行（无记录 + 用户上下文放行）/ 第 5 行（有记录 + 无上下文放行）/ 第 6 行四格（命中放行、未命中拒绝 fail-closed 断言 credentialId+roleIds 参数、admin 未授权拒绝、admin 显式 grant 后放行）/ user 级不受授权影响（owner 放行 + 持授权角色非 owner 仍 owner-only 拒绝）/ delFlag 先序（DELETED 不进授权判定）/ mask+test 不查授权 / 引擎通道豁免两用例（engineGetDecryptedFields + engineUpdateTokenFields/engineUpdateInLock 在未命中上下文下放行、同凭证 getCredential 仍拒）。
- **验证**：`TestCredentialProviderRbacAuth` 11/11 绿；nop-credential 全 reactor `../mvnw test` 绿（service 155（+11 新增）/kms-vault 32/web 1，零回归）；`check-orm-icons.mjs` exit 0。
- **设计回写**：`02-phase2-design.md` §6.3 新增"W11-impl Part B 裁定标注（2026-08-17 回写）"——引擎通道豁免裁定（可达性事实措辞：beginOAuthFlow 只返回授权 URL、回调不含 token、saveCredential 有写分级门控；非"GraphQL 不可达"）+ 两个可达调用点登记 + 测试固化引用。
- Doc-sync：设计 §6.3 标注；docs-for-ai RBAC 章节统一在 Phase 3。
