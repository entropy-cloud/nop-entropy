### 2026-05-28 ORM XML icon rollout and checker completion

- Plan 55 执行：为 19 个 in-scope source `model/*.orm.xml` 文件中的全部实体补齐语义化 `ext:icon`，覆盖 `nop-ai`、`nop-batch`、`nop-code`、`nop-dyn`、`nop-file`、`nop-job`、`nop-report`、`nop-retry`、`nop-rule`、`nop-task`、`nop-tcc`、`nop-wf`、`nop-auth` 中遗留缺口，以及 `nop-ai/nop-ai-coder/demo/model/app-demo.orm.xml`、`nop-kernel/nop-kernel-cli/demo/model/demo.orm.xml`。
- Plan 55 执行：将由 `demo-ddd.orm.xlsx` 转出的 source 正式落盘为 `nop-demo/nop-ddd-demo/model/demo-ddd.orm.xml`，并为 DDD demo 的 `Cargo`、`CarrierMovement`、`HandlingEvent`、`Leg`、`Location`、`Voyage` 补充语义化图标。
- Plan 55 补漏：继续将 `nop-ai/nop-ai-coder/src/main/resources/_vfs/nop/ai/tasks/ai-coder.task-lib.xml` 与 `ai-extract-report-tpl.task.xml` 中残留的默认 `model/${appName}.orm.xlsx` 输出切换为 `model/${appName}.orm.xml`，收口 AI coder 模板默认路径与 model-first 约定的漂移。
- 验证：`node ai-dev/tools/check-orm-icons.mjs` 通过，结果为 `OK: all source ORM entities have ext:icon (19 files checked)`。
- 验证：`./mvnw install -pl nop-auth,nop-ai,nop-batch,nop-code,nop-dyn,nop-file,nop-job,nop-report,nop-retry,nop-rule,nop-task,nop-tcc,nop-wf,nop-kernel/nop-kernel-cli,nop-demo/nop-ddd-demo -am -DskipTests -T 1C` 通过，repo 级 source ORM -> downstream generated outputs 再生链正常。
- 验证：`./mvnw install -pl nop-ai/nop-ai-coder -am -DskipTests -T 1C` 通过；`node ai-dev/tools/check-doc-links.mjs --strict` 通过。
- 收口修正：独立 closure audit 指出 `ai-dev/tools/check-orm-icons.mjs` 的 source 选择条件过宽，会接受 `model/` 子树内更深层的 `.orm.xml`；已收紧为只匹配直接命中的 `**/model/*.orm.xml`，随后复跑 `node ai-dev/tools/check-orm-icons.mjs` 与 `node ai-dev/tools/check-doc-links.mjs --strict` 均通过。
- 偏差：owner doc 约定本身未变化，因此未额外更新 `docs-for-ai/02-core-guides/model-first-development.md`；本次只在计划和日志中记录 `No owner-doc update required`。
- 偏差：Plan 55 的 repo-required test gate 最初写成 `./mvnw test`，但用户对本类图标/source-path rollout 明确要求仅以 `mvn install -DskipTests` 为验证基线；本次按计划 gate 中允许的 "explicitly justified equivalent repo-required test baseline" 收口，并在日志保留该裁定。
- Doc-sync: No owner-doc update required.

### 2026-05-28 ORM menu icon rollout for sys/oauth

- Plan 54 执行：为 `nop-sys/model/nop-sys.orm.xml`、`nop-auth/nop-oauth/model/nop-oauth.orm.xml` 中会生成后台菜单的实体补充 `ext:icon`，并确认 `nop-sys`、`nop-oauth` 生成结果从 `blocks` 切换为实体级图标。
- 根因修正：`nop-auth/nop-oauth/nop-oauth-codegen/postcompile/gen-orm.xgen` 仍在从 `model/nop-oauth.orm.xlsx` 生成 ORM，下游未消费已补充 icon 的 `model/nop-oauth.orm.xml`；已切换为基于 `nop-oauth.orm.xml` 生成。
- 验证：`./mvnw install -pl nop-kernel/nop-codegen,nop-sys,nop-auth/nop-oauth -am -DskipTests -T 1C` 通过；`./mvnw install -pl nop-auth/nop-oauth -am -DskipTests -T 1C` 通过。
- 验证：`node ai-dev/tools/check-doc-links.mjs --strict` 通过。
- 验证：`nop-sys/nop-sys-meta/.../_NopSysClusterLeader.xmeta` 已带 `ext:icon="network"`，`nop-sys/nop-sys-web/.../_nop-sys.action-auth.xml` 已输出 `history`、`file-check`、`network` 等图标；`nop-auth/nop-oauth/nop-oauth-meta/.../_NopOauthAuthorization.xmeta` 已带 `ext:icon="shield"`，`nop-auth/nop-oauth/nop-oauth-web/.../_nop-oauth.action-auth.xml` 已输出 `shield`、`badge-check`、`monitor`。
- 偏差：原计划假设 `nop-oauth` 与 `nop-sys` 同样只需重新 install；实际排查发现 `nop-oauth` 仍走 `orm.xlsx` 作为 codegen 源，因此额外修正了 `gen-orm.xgen` 的输入路径。
- Doc-sync: No owner-doc update required.

### 2026-05-28 ORM menu icon propagation

- Plan 53 执行：打通 `model/*.orm.xml` 的 `entity ext:icon -> generated xmeta ext:icon -> generated _*.action-auth.xml icon` 传播链，修改了 `nop-kernel/nop-codegen` 的 xmeta 与 orm-web auth 模板。
- 为 `nop-auth/model/nop-auth.orm.xml` 中会生成后台菜单的实体补充语义化图标，采用 kebab-case Lucide 风格命名，例如 `user-round`、`building-2`、`git-branch`、`plug-zap`。
- 验证：`./mvnw test -pl nop-auth -am -T 1C` 通过；生成结果确认 `nop-auth/nop-auth-meta/.../*.xmeta` 已带 `ext:icon`，`nop-auth/nop-auth-web/.../_nop-auth.action-auth.xml` 已输出对应菜单图标。
- 偏差：`nop-chaos-next` 的标准业务图标白名单未覆盖全部鉴权领域语义，因此沿用其 kebab-case Lucide 解析约定并直接选用了 `shield`、`users`、`briefcase`、`user-round` 等名称。
- Doc-sync: `docs-for-ai/02-core-guides/model-first-development.md` 已更新；`node ai-dev/tools/check-doc-links.mjs --strict` 已通过。
