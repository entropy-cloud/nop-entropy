### 2026-07-25 (evictor / watermark valve / pane 集成 — Stage 21, G46/G47/G48)

- Plan `2026-07-25-1500-2-evictor-watermark-pane.md` completed:
  - **Scope**: 收口窗口/时间模型的三个 P2 集成缺口。经 live 核对，三个 gap 的现状与 roadmap 假设有偏差——G46 的 evictBefore/evictAfter 已接线且 transient 语义与 Flink 一致；G47 的合并数学已 N-capable 但 dormant；G48 的 pane 计算已实现但持久化/isLast/retract 缺。本 plan 据此把每个 gap 收口为"核对 + 补真实 gap + 文档"，避免建造无消费者空壳。
  - Phase 1 (G46 Evictor): 反空壳核对 `emitWindowContents()` evictBefore→process→evictAfter 接线，eviction 作用于局部瞬态 `wrapped`（不写回 state），ACCUMULATING 跨 firing 持久化 + eviction 重算（与 Flink transient-per-fire 一致）。新增 `testEvictionIsTransientPerFireOnProductionListStatePath`（TestEvictorIntegration）覆盖**生产 InternalListState 路径**（非仅 MapState 测试路径），断言 sizesSeen=[1,2,3,4] 证明瞬态。裁定**不引入裁剪持久化**（构成回归）。`window-design.md` §3/§8.7/§11 更新。
  - Phase 2 (G47 Watermark Valve): 修正 `WatermarkStatus.java:61` 悬空 `@link StatusWatermarkValve` → 指向 `IndexedCombinedWatermarkStatus`（grep 全仓零残留）。裁定 `AbstractStreamOperator` 输入数为字面量 2、valve dormant（零 TwoInputStreamOperator 消费者），保留路径不建新算子（Anti-Hollow）。新增 `TestIndexedCombinedWatermarkStatus`（6 测试：N 输入 min-combine + idleness + resume）。`time-model-design.md` §5.4（裁定 + Anti-Hollow 豁免）、§6.4（interval=200 非 0）、§8、§9 修正。
  - Phase 3 (G48 Pane): `paneTracking` 参与 `snapshotState`/`restoreState`（新增 `PaneTrackingSnapshot` 序列化 DTO + `isTimeWindowPaneKey` 限定 TimeWindow），`open()` 延迟应用（与 timer restore 序对齐）。`isLast` 裁定为"清理前不可知，恒为 false"（不采用回填方案，文档化）。`ACCUMULATING_AND_RETRACTING` 在 `open()` 快速失败抛 `UnsupportedOperationException`（非静默）。新增 `testPaneTrackingSurvivesCheckpointRestore`（EARLY→ON_TIME→snapshot→restore→LATE paneIndex=2）+ `testRetractingModeFailsFastOnOpen`。`window-design.md` §6（RETRACTING→ACCUMULATING_AND_RETRACTING spec-only）、§13（live pane 现状 + isLast 裁定 + checkpoint 事实 + TimeWindow 限制）更新。
  - Tests: 475 pass (`./mvnw test -pl nop-stream -am -T 1C`)。新增 9 测试（1 evictor transient + 6 valve math + 2 pane）。
  - Closure audit (独立子 agent, fresh session): all 10 checks PASS, all gates PASS。
  - Roadmap item 21: `todo` → `done`。G46/G47/G48 in `08-gap-analysis.md` marked ✅ Closed。

### 2026-07-25 (mailbox execution model — Stage 17, G22)

- Plan `2026-07-25-1500-1-mailbox-execution-model.md` completed:
  - **Scope**: Introduced minimal mailbox control plane (`Mail` / `TaskMailbox` / `MailboxExecutor`) as the per-task control channel; converted SOURCE checkpoint trigger from cap-1 queue handoff to mailbox-based (drained at `SourceContext.collect()` on the task thread); converted abort to cooperative cancel (cancel flag/mail + retained interrupt). Middle/sink `triggerCheckpoint` explicitly kept synchronous on the injector thread to preserve the cross-task priming invariant.
  - Phase 1 (mailbox primitives): Added `Mail` (closure + CONTROL/NORMAL priority), `TaskMailbox` (multi-producer/single-consumer, non-blocking `poll`, blocking `take` that exits on close, control-priority-first), `MailboxExecutor` (holds mailbox + cancel flag, `processAvailableMails`, `signalCancel`, `runLoop`). 15 focused unit tests (TestTaskMailbox, TestMailboxExecutor). New design doc `ai-dev/design/nop-stream/mailbox-design.md` documents ownership contract + 3 usages + middle/sink-sync rationale + finished-source exception + `synchronized` adjudication.
  - Phase 2 (wiring): `StreamTaskInvokable` now creates/holds a `MailboxExecutor` and wires it to the head source operator. `StreamSourceOperator.offerBarrier()` (non-finished, mailbox-wired) delivers a trigger-checkpoint mail; `SourceContext.collect()` drains it. Removed the `pendingBarriers` cap-1 queue. `processInputGate` (MIDDLE/SINK) polls the mailbox at the loop top (cancel check). `registerLocalAbortHandler` calls `signalCancel()` before `task.cancel()`. Finished-source path stays on injector thread (explicit exception, documented).
  - **Bug found & fixed in `GraphModelCheckpointExecutor.registerTasksAndTrackers`**: it created trackers from the ORIGINAL operator chains (`execVertex.getOperatorChains()`), but the execution plan deep-copies chains for multi-vertex topologies. The tracker referenced different operator instances than the invokable ran, disconnecting checkpoint priming/barrier-injection/ACKs. Fixed to use `invokable.getOperatorChain()` (the live chain). This was a pre-existing latent issue that the mailbox change made visible.
  - Phase 3 (concurrency correctness): Converted `testExtraAckTriggersCallbackAgainKnownIssue` → `testExtraAckIsSafelyIgnored` with strict `assertEquals(1, callbackCount)` (the `operatorsToAck <= 0` guard already holds; soft `>= 1` upgraded to strict assertion). Session window tests observed stable (multi-key flakiness root cause is cross-subtask scheduling — out of scope, separate successor).
  - Tests: 472 pass (`./mvnw test -pl nop-stream -am -T 1C`). New: TestTaskMailbox (10), TestMailboxExecutor (5), TestMailboxWiring (5), TestMailboxE2ECheckpoint (2 aligned+AT_LEAST_ONCE), updated TestSourcePullBarrierInjection (4), updated TestStreamSourceOperator (2), TestE2EWindowOperatorWithCheckpoint, TestCheckpointBarrierTrackerConcurrency (renamed test).
  - Doc-sync: `checkpoint-design.md` §2.3 (SOURCE-trigger-via-mail + middle/sink-sync + finished-source exception) + §8.7 (cooperative cancel); `mailbox-design.md` (new); design README updated.
  - Closure audit (independent subagent): all 7 gates PASS, all 4 anti-hollow checks PASS. Note: `scan-hollow-implementations.mjs --module nop-stream --severity high` exits 1 due to PRE-EXISTING baseline findings (GroupPattern, RuntimeContext, etc.) — zero findings in the new mailbox code.
  - Roadmap item 17: `todo` → `done`.
- **Post-EXECUTE verification (mission-driver)**: full-suite `./mvnw test -pl nop-stream -am -T 1C` initially failed 1/472 — `TestMailboxE2ECheckpoint.testE2ECheckpointAtLeastOnce` was flaky: the `completed.getCheckpointId() > 0` assertion is a Flink-ism that doesn't match this framework's 0-based `CheckpointIDCounter` (first checkpoint id = 0, verified by `TestCheckpointIDCounter`). Under full-suite load only checkpoint 0 completed within the window, so the latest id was 0 and the `> 0` assertion failed (passes in isolation where checkpoints 0–3 complete). Fixed the assertion to `>= 0` (the real "at least one completed" gate is the `assertNotNull` above it; no code treats id 0 as a sentinel). Re-ran: **472/472 pass — full-green verification**.

### 2026-07-25 (mission-driver article — structural restructure → moved to nop-app-erp)

- Article `ai-dev/articles/mission-driver--loop-engineering.md` restructured from 988 → 550 lines, then moved to `~/app/nop-app-erp/docs/articles/mission-driver--loop-engineering.md`.
  - Round 1 audit found 7 structural issues: Quick Start buried at 89%, §五 268-line mega-chapter, "九点五" invalid numbering, deep theory (Codex comparison, attractor lifecycle) blocking entry-level readers, 6 instances of duplicated content, mixed meta-theory + practical operations in §八.
  - Restructure: 6-part, 18-chapter narrative arc — 一 Problem → 二 One-minute intro → 三 Loop nesting & local fault tolerance | 四 Quick start (frontloaded to ~14% position) → 五 Four-layer DSL → 六 Config & customization | 七 Standard workflow → 八 Feedback loop improvement → 九 E2E AI generation → 十 Application scenarios | 十一 nop-app-erp case study (independent chapter) | 十二 Loop Engineering principles → 十三 Plan Loop & verification system → 十四 AGE attractor theory → 十五 Codex goal comparison → 十六 Related frameworks → 十七 Goal-driven vision & economics → 十八 Summary.
  - All 7 Round 1 issues resolved. Two residual echo duplications cleaned (§一/§十七 economics framing, §十一 forward reference to attractor).
- Round 2 convergence audit (independent subagent, fresh session): structure 9/10, language 8.5/10, **converged — no Round 3 needed**.
- Doc link check: 0 errors attributable to the article (55 pre-existing errors elsewhere in `ai-dev/backlog/nop-stream-flink-comparison-roadmap.md` are unrelated).



- Plan `2026-07-25-0800-1-session-window-merge.md` completed:
  - **G1 root cause**: `WindowOperator.mergeWindowContents()` wrote merged accumulator back via `clear()` + `add((IN) targetValue)`. For `AggregatingState`, `add()` applies the `AggregateFunction.add()` transformation (e.g. sums an element), so calling it with an already-merged `ACC` value either throws a type-mismatch exception ("Failed to set merged accumulator") or silently double-counts. The fix introduces `InternalAppendingState.setAccumulator(ACC)` — the inverse of `getAccumulator()` — which stores the raw ACC directly, bypassing the `add()` transformation.
  - Phase 1 (AggregatingState merge path fix):
    - Added `setAccumulator(ACC)` to `InternalAppendingState` interface (nop-stream-core).
    - Implemented in `MemoryInternalAggregatingState` and `MemoryInternalAppendingState` (both: `storage.put(getStorageKey(), accumulator)`, no aggregation).
    - Rewrote `WindowOperator.mergeWindowContents()` AggregatingState path: replaced `clear()+add((IN) targetValue)` with `setAccumulator(targetValue)` (WindowOperator.java ~line 1191).
    - 4 new unit tests: `testSetAccumulatorStoresRawValueWithoutReAggregating`, `testAggregatingStateMergeProducesCorrectResult`, `testSetAccumulatorNullClearsExistingValue` (TestMemoryInternalAggregatingState); `testSetAccumulatorStoresRawValue` (TestMemoryInternalAppendingState). All pass.
    - Doc-sync: `window-design.md` §8.6.1 documents accumulator write-back contract; `state-management-design.md` interface hierarchy updated to list `setAccumulator`.
  - Phase 2 (enable disabled session window E2E tests):
    - Removed `@Disabled` from `TestSessionWindowAdvancedMerge` (3 test methods) — all pass consistently.
    - Removed `@Disabled` from `TestSessionWindowWithPeriodicWatermark` (1 method: `testMultiKeyIndependentSessions`).
    - **Additional bug found and fixed**: `WindowOperator.onEventTime()`/`onProcessingTime()` did not restore `keyedStateBackend.setCurrentKey(timer.getKey())` before calling `getMergingWindowSet()`. For merging windows (session), `getMergingWindowSet()` reads key-scoped state; with a stale key, `getStateWindow()` returned null → window output silently dropped for non-last-processed keys. Fix: restore key context at the start of both methods.
    - **Multi-key test flakiness root cause** (documented in test comment, NOT caused by the merge path): The `TaskExecutor` runs pipeline vertices on separate threads communicating via `LinkedBlockingQueue`. Under certain thread-scheduling timings (JVM warmup-dependent), the multi-key test produces `[12,12]` instead of `[7,12]`. The test passes consistently when run in isolation. Mitigations applied: events ordered by timestamp, `watermarkInterval=0` (deterministic per-element emission, no wall-clock timer). The residual flakiness is a pre-existing execution-engine race, tracked as non-blocking follow-up.
    - Assertions unchanged (key1=12, key2=7, sums verified).
  - Verification: `./mvnw clean install -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime -am -T 1C -DskipTests` SUCCESS; full window test suite (`io.nop.stream.runtime.operators.windowing.*`) passes.

### 2026-07-25 (operator state redistribution — item 12b, incl. Plan 12a prerequisites)

- Plan `2026-07-25-1600-2-operator-state-redistribution.md` completed:
  - Prerequisites (Plan 12a): Created `IOperatorStateBackend` interface, `MemoryOperatorStateBackend` class, `operatorStateBackend` field on `AbstractStreamOperator`, `createOperatorStateBackend()` on `IStateBackend`/`MemoryStateBackend`.
  - Phase 1: Created `RedistributionMode` enum (NONE/UNION/BROADCAST/SPLIT_DISTRIBUTE), added `restoreState(List, int, RedistributionMode, int, int)` overload to `IOperatorStateBackend`, added `checkpointParallelism` field + `isParallelismChanged()` to `OperatorSnapshotResult`. 7 focused tests pass.
  - Phase 2: Implemented UNION merge, BROADCAST clone, SPLIT_DISTRIBUTE round-robin in `MemoryOperatorStateBackend`. 9 focused tests cover distribution scenarios.
  - Phase 3: `BatchLoaderSourceFunction` now implements `ReplayableSourceFunction` (adds `currentOffset`, `seek()`, `getCurrentOffset()`). 3 new tests verify offset tracking + operator checkpoint/restore.
  - Phase 4: E2E test `TestE2EOperatorStateRedistribution` covers same-parallelism restore, scale-up (UNION), scale-down (SPLIT_DISTRIBUTE), latest-checkpoint, and anti-hollow check. 6 scenarios pass.
  - Deviations from plan: `IOperatorStateBackend.restoreState()` overload takes `List<OperatorSnapshotResult>` + int params instead of single `OperatorSnapshotResult` + `RedistributionMode` — needed for batch redistribution logic.
  - Verification: `./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-connector -am -Dtest=...` — all targeted test classes pass (25+9+6+3 tests).
  - Doc-sync: No owner-doc update required. Roadmap item 12b updated: `todo` → `done`.

### 2026-07-25 (watermark fixes / window operator wiring — item 10) + (CEP state integration — item 11)

- Plan `2026-07-25-1430-2-cep-state-integration.md` completed:
  - Phase 1: Added `IStateBackend stateBackend` to `CheckpointConfig`, `setStateBackend()` to `StreamExecutionEnvironment`, configurable backend in `GraphModelCheckpointExecutor.registerTasksAndTrackers()` (replaces hardcoded `new MemoryStateBackend()`). 3 new tests pass.
  - Phase 2: Code trace confirmed snapshot/restore paths fully connected (no fixes needed).
  - Phase 3: Single-input watermark propagation path traced (watermark → `processElement()` → `advanceTime()` → timer service). Multi-input conditional noted (requires Plan 10 successor).
  - Phase 4: `CepOperator` Javadoc updated (removed "hardcoded MemoryKeyedStateBackend" claim). `SharedBuffer` caches upgraded to `LruCache` (ConcurrentHashMap + LinkedHashMap access tracker, write-through to state). 4 new tests pass.
  - Phase 5: `TestCepCheckpointRestoreE2E` — 3 scenarios (NFA state, shared buffer, timer) all pass.
  - All 282 CEP module tests pass, 0 failures.
  - Roadmap item 11 updated to `done`.

- Plan `2026-07-25-1430-1-watermark-fixes.md` completed: wired PaneInfo tracking, AccumulationMode, Evictor.evictAfter(), pane timing (EARLY/ON_TIME/LATE) into WindowOperator
- Phase 1: Added `AccumulationMode` field to `WindowOperator` (default ACCUMULATING), `PaneInfo` computed in `emitWindowContents()` via `computePaneInfo()`, exposed via `InternalWindowContext.getPaneInfo()`
- Phase 1: Added `evictor.evictAfter()` call after `userFunction.process()` in `emitWindowContents()` evictor path
- Phase 1: Removed empty `else {}` blocks at `WindowOperator.java:616,673` (onEventTime/onProcessingTime)
- Phase 1: Updated `WindowOperatorBuilder` with `accumulationMode()` builder method
- Phase 1: Updated `InternalWindowFunction.InternalWindowContext` with `getPaneInfo()` method
- Phase 1: New test class `TestPaneInfoAndAccumulationMode.java` covering EARLY/ON_TIME/LATE timing, ACCUMULATING/DISCARDING modes, evictAfter invocation, anti-hollow read/write check
- Phase 1: Repaired 4 existing test files for constructor API change (`TestEvictorIntegration`, `TestTimeEvictorIntegration`, `TestWindowOperatorEvictorTimestamps`, `TestInternalWindowFunctionAdapters`)
- Deviations from plan: `AccumulationMode` field chosen over `WindowingStrategy` (option b over a) — simpler, avoids coupling; `PaneInfo` placed in `InternalWindowContext` (option c) over `Window` result type (option a) — no interface signature break
- Verification: `./mvnw compile test-compile -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime -am` — SUCCESS
- Doc-sync: No owner-doc update required (internal feature wiring). Roadmap item 10 updated: `todo` → `done`

### 2026-07-25 (checkpoint core fixes — item 9)

- Plan `2026-07-25-1210-3-checkpoint-core-fixes.md` completed: implemented InputGate channel blocking API, operator-level abort callback, BarrierAligner dead code adjudication, maxConcurrentCheckpoints fix
- Phase 1: Added `blockConsumption`/`resumeConsumption`/`resumeConsumptionAll` to InputGate with `Set<Integer> blockedChannels`; replaced implicit `barrierReceived`+`continue` skip with explicit blocking API; `resetBarrierState()` no longer clears blockedChannels; resumeConsumptionAll called on alignment completion
- Phase 2: Implemented `AbstractStreamOperator.notifyCheckpointAborted()` (was empty) — clears `lastSnapshotResult`; added `CheckpointBarrierTracker.notifyCheckpointAborted()` — resets tracker state releasing ACK wait; extended `registerLocalAbortHandler()` in `GraphModelCheckpointExecutor` to propagate abort to tracker + `InputGate.resumeConsumptionAll()`
- Phase 3: Added `@Deprecated` + explanatory javadoc to BarrierAligner; added `@SuppressWarnings("deprecation")` to 3 test files; removed `Math.min(1, ...)` from `CheckpointCoordinator.tryTriggerPendingCheckpoint()`; added maxConcurrentCheckpoints=3 test
- Deviations from plan: N/A — scope aligned with gap analysis findings
- Verification: `./mvnw compile test-compile -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime -am` — SUCCESS (no test runner for full test execution in this environment)
- Doc-sync: No owner-doc update required. Roadmap item 9 updated: `todo` → `done`

### 2026-07-25 (gap analysis — item 8)

- Plan `2026-07-25-1210-2-gap-analysis.md` completed: Deliverable at `ai-dev/analysis/nop-stream/08-gap-analysis.md`
- Merged 144 raw gap entries from 5 comparison docs (03—07) into 87 active + 6 resolved gaps
- Priority distribution: P0=3, P1=24, P2=43, P3=10, Doc=5, Improvement=2
- Cross-referenced with Plan 303 and checkpoint-lifecycle-fixes closures — 6 resolved gaps moved to appendix
- Roadmap corrections: SimpleKeyedStateStore claim inaccurate, watermarkInterval default 200L not 0, NFA state participates in checkpoint
- All gaps mapped to target fix plans (items 9—13) or deferred (unaligned checkpoint → Phase 4, leader election → Phase 3)
- 6 design decision points identified (Key-Group vs StateShard, mailbox model, RPC transport, etc.)
- Owner-doc update inventory: 10 docs need updates
- Roadmap item 8 updated: `todo` → `done`
- Verification: analysis-only (no code changes)

### 2026-07-25 (distributed execution comparison)

- Plan `2026-07-25-1210-1-distributed-comparison.md` completed: Deliverable at `ai-dev/analysis/nop-stream/07-distributed-comparison.md`
- Compared 6 dimensions: execution graph layers, task lifecycle, scheduling model, RPC abstraction, data exchange, cluster management
- Key finding: **Bug/P1 D1** — OperatorChain double-open (Task-level + Invokable-level both call `.open()`). Confirmed ILeaderElector has zero code (designed but not implemented). RPC interfaces are local-only no remote transport.
- Gap table: 20 findings (P0-P3) across Bug/Gap/Hollow/Improvement/Doc classifications
- Verification: analysis-only (no code changes). Roadmap item 7 updated: `todo` → `done`
- Doc-sync: No owner-doc update required (analysis-only)

### 2026-07-25 (checkpoint lifecycle fixes)

- Plan `2026-07-25-0900-1-checkpoint-lifecycle-fixes` completed: fixed two material defects from multi-audit findings 2 & 3
- Phase 1: Added `CheckpointMetrics.recordAborted(String)` — increments `numAbortedCheckpoints` without touching `numFailedCheckpoints`; replaced `recordFailure("Aborted: ...")` call in `CheckpointCoordinator.abortPendingCheckpoint()` with `recordAborted()`
- Phase 2: Added `ERR_STREAM_CHECKPOINT_FAILED` to `NopStreamErrors.java`; updated `PendingCheckpoint.fail()` to use it (was incorrectly using `ERR_STREAM_CHECKPOINT_ABORTED`); `abort()` continues using `ERR_STREAM_CHECKPOINT_ABORTED`
- Verification: `./mvnw test -pl nop-stream/nop-stream-runtime -am` — 458 tests pass, 0 failures, 4 skipped (pre-existing)
- Compilation: `./mvnw compile -pl nop-stream/nop-stream-runtime -am` — SUCCESS
- Doc-sync: No owner-doc update required (internal code fix)
