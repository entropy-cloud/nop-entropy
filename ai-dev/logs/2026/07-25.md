### 2026-07-25 (operator state redistribution — item 12b, incl. Plan 12a prerequisites)

- Plan `2026-07-25-1600-2-operator-state-redistribution.md` completed:
  - Prerequisites (Plan 12a): Created `IOperatorStateBackend` interface, `MemoryOperatorStateBackend` class, `operatorStateBackend` field on `AbstractStreamOperator`, `createOperatorStateBackend()` on `IStateBackend`/`MemoryStateBackend`.
  - Phase 1: Created `RedistributionMode` enum (NONE/UNION/BROADCAST/SPLIT_DISTRIBUTE), added `restoreState(List, int, RedistributionMode, int, int)` overload to `IOperatorStateBackend`, added `checkpointParallelism` field + `isParallelismChanged()` to `OperatorSnapshotResult`. 7 focused tests pass.
  - Phase 2: Implemented UNION merge, BROADCAST clone, SPLIT_DISTRIBUTE round-robin in `MemoryOperatorStateBackend`. 9 focused tests cover distribution scenarios.
  - Phase 3: `BatchLoaderSourceFunction` now implements `ReplayableSourceFunction` (adds `currentOffset`, `seek()`, `getCurrentOffset()`). 3 new tests verify offset tracking + operator checkpoint/restore.
  - Phase 4: E2E test `TestE2EOperatorStateRedistribution` covers same-parallelism restore, scale-up (UNION), scale-down (SPLIT_DISTRIBUTE), latest-checkpoint, and anti-hollow check. 6 scenarios pass.
  - Deviations from plan: `IOperatorStateBackend.restoreState()` overload takes `List<OperatorSnapshotResult>` + int params instead of single `OperatorSnapshotResult` + `RedistributionMode` — needed for batch redistribution logic.
  - Verification: `./mvnw test -pl nop-stream/nop-stream-core,nop-stream/nop-stream-connector -am -Dtest=...` — all targeted test classes pass (25+9+6+3 tests).
  - Doc-sync: No owner-doc update required. Roadmap item 12b updated: `todo` → `done`.

### 2026-07-25 (watermark fixes / window operator wiring — item 10) + (CEP state integration — item 11)

- Plan `2026-07-25-1430-2-cep-state-integration.md` completed:
  - Phase 1: Added `IStateBackend stateBackend` to `CheckpointConfig`, `setStateBackend()` to `StreamExecutionEnvironment`, configurable backend in `GraphModelCheckpointExecutor.registerTasksAndTrackers()` (replaces hardcoded `new MemoryStateBackend()`). 3 new tests pass.
  - Phase 2: Code trace confirmed snapshot/restore paths fully connected (no fixes needed).
  - Phase 3: Single-input watermark propagation path traced (watermark → `processElement()` → `advanceTime()` → timer service). Multi-input conditional noted (requires Plan 10 successor).
  - Phase 4: `CepOperator` Javadoc updated (removed "hardcoded MemoryKeyedStateBackend" claim). `SharedBuffer` caches upgraded to `LruCache` (ConcurrentHashMap + LinkedHashMap access tracker, write-through to state). 4 new tests pass.
  - Phase 5: `TestCepCheckpointRestoreE2E` — 3 scenarios (NFA state, shared buffer, timer) all pass.
  - All 282 CEP module tests pass, 0 failures.
  - Roadmap item 11 updated to `done`.

- Plan `2026-07-25-1430-1-watermark-fixes.md` completed: wired PaneInfo tracking, AccumulationMode, Evictor.evictAfter(), pane timing (EARLY/ON_TIME/LATE) into WindowOperator
- Phase 1: Added `AccumulationMode` field to `WindowOperator` (default ACCUMULATING), `PaneInfo` computed in `emitWindowContents()` via `computePaneInfo()`, exposed via `InternalWindowContext.getPaneInfo()`
- Phase 1: Added `evictor.evictAfter()` call after `userFunction.process()` in `emitWindowContents()` evictor path
- Phase 1: Removed empty `else {}` blocks at `WindowOperator.java:616,673` (onEventTime/onProcessingTime)
- Phase 1: Updated `WindowOperatorBuilder` with `accumulationMode()` builder method
- Phase 1: Updated `InternalWindowFunction.InternalWindowContext` with `getPaneInfo()` method
- Phase 1: New test class `TestPaneInfoAndAccumulationMode.java` covering EARLY/ON_TIME/LATE timing, ACCUMULATING/DISCARDING modes, evictAfter invocation, anti-hollow read/write check
- Phase 1: Repaired 4 existing test files for constructor API change (`TestEvictorIntegration`, `TestTimeEvictorIntegration`, `TestWindowOperatorEvictorTimestamps`, `TestInternalWindowFunctionAdapters`)
- Deviations from plan: `AccumulationMode` field chosen over `WindowingStrategy` (option b over a) — simpler, avoids coupling; `PaneInfo` placed in `InternalWindowContext` (option c) over `Window` result type (option a) — no interface signature break
- Verification: `./mvnw compile test-compile -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime -am` — SUCCESS
- Doc-sync: No owner-doc update required (internal feature wiring). Roadmap item 10 updated: `todo` → `done`

### 2026-07-25 (checkpoint core fixes — item 9)

- Plan `2026-07-25-1210-3-checkpoint-core-fixes.md` completed: implemented InputGate channel blocking API, operator-level abort callback, BarrierAligner dead code adjudication, maxConcurrentCheckpoints fix
- Phase 1: Added `blockConsumption`/`resumeConsumption`/`resumeConsumptionAll` to InputGate with `Set<Integer> blockedChannels`; replaced implicit `barrierReceived`+`continue` skip with explicit blocking API; `resetBarrierState()` no longer clears blockedChannels; resumeConsumptionAll called on alignment completion
- Phase 2: Implemented `AbstractStreamOperator.notifyCheckpointAborted()` (was empty) — clears `lastSnapshotResult`; added `CheckpointBarrierTracker.notifyCheckpointAborted()` — resets tracker state releasing ACK wait; extended `registerLocalAbortHandler()` in `GraphModelCheckpointExecutor` to propagate abort to tracker + `InputGate.resumeConsumptionAll()`
- Phase 3: Added `@Deprecated` + explanatory javadoc to BarrierAligner; added `@SuppressWarnings("deprecation")` to 3 test files; removed `Math.min(1, ...)` from `CheckpointCoordinator.tryTriggerPendingCheckpoint()`; added maxConcurrentCheckpoints=3 test
- Deviations from plan: N/A — scope aligned with gap analysis findings
- Verification: `./mvnw compile test-compile -pl nop-stream/nop-stream-core,nop-stream/nop-stream-runtime -am` — SUCCESS (no test runner for full test execution in this environment)
- Doc-sync: No owner-doc update required. Roadmap item 9 updated: `todo` → `done`

### 2026-07-25 (gap analysis — item 8)

- Plan `2026-07-25-1210-2-gap-analysis.md` completed: Deliverable at `ai-dev/analysis/nop-stream/08-gap-analysis.md`
- Merged 144 raw gap entries from 5 comparison docs (03—07) into 87 active + 6 resolved gaps
- Priority distribution: P0=3, P1=24, P2=43, P3=10, Doc=5, Improvement=2
- Cross-referenced with Plan 303 and checkpoint-lifecycle-fixes closures — 6 resolved gaps moved to appendix
- Roadmap corrections: SimpleKeyedStateStore claim inaccurate, watermarkInterval default 200L not 0, NFA state participates in checkpoint
- All gaps mapped to target fix plans (items 9—13) or deferred (unaligned checkpoint → Phase 4, leader election → Phase 3)
- 6 design decision points identified (Key-Group vs StateShard, mailbox model, RPC transport, etc.)
- Owner-doc update inventory: 10 docs need updates
- Roadmap item 8 updated: `todo` → `done`
- Verification: analysis-only (no code changes)

### 2026-07-25 (distributed execution comparison)

- Plan `2026-07-25-1210-1-distributed-comparison.md` completed: Deliverable at `ai-dev/analysis/nop-stream/07-distributed-comparison.md`
- Compared 6 dimensions: execution graph layers, task lifecycle, scheduling model, RPC abstraction, data exchange, cluster management
- Key finding: **Bug/P1 D1** — OperatorChain double-open (Task-level + Invokable-level both call `.open()`). Confirmed ILeaderElector has zero code (designed but not implemented). RPC interfaces are local-only no remote transport.
- Gap table: 20 findings (P0-P3) across Bug/Gap/Hollow/Improvement/Doc classifications
- Verification: analysis-only (no code changes). Roadmap item 7 updated: `todo` → `done`
- Doc-sync: No owner-doc update required (analysis-only)

### 2026-07-25 (checkpoint lifecycle fixes)

- Plan `2026-07-25-0900-1-checkpoint-lifecycle-fixes` completed: fixed two material defects from multi-audit findings 2 & 3
- Phase 1: Added `CheckpointMetrics.recordAborted(String)` — increments `numAbortedCheckpoints` without touching `numFailedCheckpoints`; replaced `recordFailure("Aborted: ...")` call in `CheckpointCoordinator.abortPendingCheckpoint()` with `recordAborted()`
- Phase 2: Added `ERR_STREAM_CHECKPOINT_FAILED` to `NopStreamErrors.java`; updated `PendingCheckpoint.fail()` to use it (was incorrectly using `ERR_STREAM_CHECKPOINT_ABORTED`); `abort()` continues using `ERR_STREAM_CHECKPOINT_ABORTED`
- Verification: `./mvnw test -pl nop-stream/nop-stream-runtime -am` — 458 tests pass, 0 failures, 4 skipped (pre-existing)
- Compilation: `./mvnw compile -pl nop-stream/nop-stream-runtime -am` — SUCCESS
- Doc-sync: No owner-doc update required (internal code fix)
