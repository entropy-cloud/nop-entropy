# AI 开发日志 — 2026-05-25

### 2026-05-25 (nop-code Plan 52 执行)

- Plan 52 完成：nop-code 从基础代码索引服务升级为完整代码智能分析平台，6 个 Phase 全部落地
- 关键改动：
  - Phase 1: 修复 7 个已确认 bug（CallHierarchy ID 不一致、Usage 表未填充、Python/TS 适配器未注册、sourceCode 返回 null、indexId 硬编码、无缓存、File ID 碰撞）
  - Phase 2: 新建 `nop-code-graph`（从 core 迁出 CommunityDetector/EntryPointScorer/ImpactAnalyzer）和 `nop-code-flow`（空壳骨架）模块
  - Phase 3: 集成 nop-search，searchCode 支持 Lucene 搜索 + DB fallback
  - Phase 4: 实现执行流追踪（FlowDetector + 五维关键度评分）、风险评分变更分析（ChangeAnalyzer）、死代码检测（DeadCodeDetector），新增 ORM 表 nop_code_flow + nop_code_flow_membership
  - Phase 5: 图分析增强（Hub/Bridge 节点、知识缺口分析、GraphML/Mermaid/JSON 导出、图快照对比、社区分裂增强）
  - Phase 6: 补充 findByAnnotation/findImplementations 查询、TESTED_BY 边类型、findDependentFiles 2-hop BFS、nop-code-api 外部 RPC 接口
- Closure audit 发现 diffGraph 和 getAffectedFlows 为空壳实现，已修复
- 验证：`./mvnw test -pl nop-code -am` BUILD SUCCESS
- Doc-sync: No owner-doc update required（nop-code 为独立模块，不影响 docs-for-ai/ 中的平台使用文档）

### 2026-05-25

- 执行完成 plan 20 Phase 7: nop-stream 并发安全与错误处理修复
- 涉及文件：
  - `RemoteInputChannel.java`: `onMessage()` decode 后重新检查 `finished` 标志避免与 `close()` 的竞态
  - `WindowOperator.java`: 替换 `RuntimeException` → `StreamException`
  - `GraphModelCheckpointExecutor.java`: 替换 `RuntimeException`/`IllegalStateException` → `StreamException`
  - `JdbcCheckpointStorage.java`: TaskLocation 解析失败时添加 `LOG.warn`
  - `CepOperator.java`, `SharedBuffer.java`: raw cast 添加 inline 注释
- 已验证（无需修改）：
  - 14-04: TaskManager.stop() 已有 awaitTermination
  - 14-05: waitForInvokable() 已使用 CountDownLatch
  - 14-06: acknowledgeOperator() 已是 synchronized
  - 14-07: onMessage() 已有 if(finished) return
  - 14-13: TaskStateSnapshot 已使用 ConcurrentHashMap
- 编译: `compiler:compile` 三个模块 BUILD SUCCESS

- 执行完成 plan 50: docs-for-ai 与 ai-dev 文档链接全面修复
- 相关路径：`ai-dev/tools/check-doc-links.mjs`、`ai-dev/plans/50-doc-link-check-fix-all.md`、`ai-dev/design/00-design-writing-guide.md`
- 关键决策：
  - D1: `docs-for-ai/90-maintenance/` 允许 BOUNDARY 豁免引用 `ai-dev/`
  - D2: `AGENTS.md`、`nop-entropy-e2e/` 加入 BOUNDARY outside 豁免
  - D3: 历史文件（logs/analysis/audits/archive/completed plans）跳过 BROKEN_LINK 检查
  - 新增：设计文档引用约束——禁止引用 discussion/analysis 等时效性文档
  - 新增：archive 目录归档文件跳过检查
- 结果：346 errors + 4 warnings → 0 errors + 0 warnings，`--strict` exit code 0
- 活跃计划中的源码路径修正：子模块路径需加组前缀（如 `nop-job-core` → `nop-job/nop-job-core`）
- 不存在的源码文件（forward reference）改为纯文字，去除反引号

## nop-stream-cep 测试增强

- 分析 nop-stream-cep 测试覆盖度：原 49 个测试方法 vs Flink CEP 的 316 个测试
- 新增 6 个测试文件，共 ~90 个新测试方法，总数从 49 提升至 138（覆盖率 2.8x）
- 新增测试覆盖领域：
  - **Pattern API 验证** (`TestPatternValidation.java`): 25 个方法，覆盖 contiguity, subtyping, OR filter, quantifier 异常, NOT 模式限制, until 限制, consume strategy 等
  - **NFA 扩展集成** (`TestNFAExtended.java`): 20 个方法，覆盖 no-condition NFA, subtype, strict continuity, oneOrMore, zeroOrMore, optional, times, times range, branched pattern, time window, skip策略等
  - **NOT 模式** (`TestNotPattern.java`): 7 个方法，覆盖 notNext block, notFollowedBy, with intermediate events, before optional, with times 等
  - **SharedBuffer 详情** (`TestSharedBufferExtended.java`): 6 个方法，覆盖 full lifecycle, extract order, multi-edge clearing, counter clearing, long path release, cache behavior 等
  - **Greedy 量词** (`TestGreedy.java`): 6 个方法，覆盖 greedy zeroOrMore, oneOrMore, in-between, non-greedy comparison, times range 等
  - **迭代条件** (`TestIterativeCondition.java`): 6 个方法，覆盖 prev-pattern dependency, ABAC pattern, branching, simple condition, subtype condition 等
- 已知限制：`notNext` + `next`（双重 strict contiguity）在当前 NFA 实现下不支持，移除 `testNotNext`（保留 `testNotNextBlocksMatch`）

## Phase 9: 测试覆盖度完善

- **16-05**: `TestDebeziumCdcSourceFunction` — `testRunCollectsChangeEventsViaSourceContext` 已覆盖 run() 生命周期 (complete)
- **16-06**: `TestConnectorConsistencyCapability` — 无 `assertEquals(X,X)` 模式 (complete)
- **16-07**: `TestEmbeddedDistributedExecution` — Javadoc 已存在 (complete)
- **16-08**: `TestCepOperatorStateRecovery` — 新增 `testNewOperatorInfrastructureAfterStateRestore`，验证新 operator 在 state restore 后基础设施正常
- **16-09**: @Disabled tests — 更新 `TestDebeziumCdcSourceCompletion` 和 `TestBatchConsumerSinkFunctionFailure` 的注释，说明 genuine broken 原因
- **16-10**: `MemoryKeyedStateBackend` — `TestStateSnapshotRoundTrip.java` 已覆盖 snapshot/restore (complete)
- **16-11**: `TestWindowOperatorWatermarkReception` (core) — 添加 Javadoc 说明命名与实际测试范围不匹配
- **16-12**: `TestCepSkipStrategyE2E` — 新增 `testSkipToNextWithOneOrMore` 覆盖 skipToNext + 量词组合
- 修复预存在的 `TestE2EStorageTypeRouting` 测试：`IllegalStateException` → `StreamException`
- 构建：`mvnw test -pl nop-stream -am` BUILD SUCCESS

## Plan 51: nop-stream 剩余缺陷收口

- 完成所有 11 个 Phase，Plan 标记 completed
- 关键修复：
  - **GAP-01 (P0)**: `ICheckpointedFunction` 接口 + `SinkFunction.finish()` + `AbstractUdfStreamOperator` 生命周期恢复
  - **GAP-02 (P0)**: `TypeSerializer`/`BasicTypeInfo`/`SimpleTypeSerializer` 体系 + `DataStreamImpl`/`WindowedStreamImpl` 类型推断
  - **GAP-03 (P1)**: 审计确认已通过 `snapshotState()`/`restoreState()` 正确 checkpoint，新增 `TestTriggerAccumulatorsCheckpoint`
  - **GAP-04 (P1)**: CEP 19 文件 Guava → `Guard`/`ConcurrentHashMap`/Stream API；Micrometer → `LongAdder`
  - **GAP-05 (P1)**: 审计确认 `KeyedStreamImpl` 是构建器而非运行时处理器，key 隔离在 `KeyExtractingOutput`/`WindowOperator`/`StreamReduceOperator` 中正确实现，新增 keyed state 隔离测试
  - **GAP-06 (P1)**: `IWriteStatus` 背压接口 + `ResultPartition`/`RecordWriter` 实现
  - **GAP-08 (P2)**: `StreamComponents` 注册表连接 + `WindowedStreamImpl` windowingStrategyId 构造路径
- Plan 49 接管项（Phase 7）：并发安全 + 错误处理 + 类型安全修复
- Phase 9: CEP 测试 49→138 方法，覆盖率 2.8x
- Phase 10: `check-import-order.sh` + 全模块 import 分组排序
- Phase 11: `./mvnw test -pl nop-stream -am` BUILD SUCCESS
- `WindowOperator.triggerAccumulators` 访问级别改为 `protected`（供测试子类访问）

### GRC 审计文档概念收束

- 修订 `docs/theory/grc-question-answer-audit.md`：将强判断的问题域统一收束为"复杂软件演化构造"，删除用"变化如何表达、定位、生成、组合、验证、升级和定制"等长串动词替代概念的表述。
- 调整总结评价：明确"复杂"指多阶段生成、多 DSL、多产品线 / 多租户、长期定制升级和跨层差异交织；"演化构造"指软件在持续变化中仍能保持可构造、可合并、可验证和可治理。
- 验证：文档变更，无代码测试；已重读修订段落并用 grep 检查关键表述一致性。
- Doc-sync: No owner-doc update required（仅修订 `docs/theory/` 理论审计材料，未改变 `docs-for-ai/` 平台使用契约）。

### GRC 审计文档表述修订

- 修订 `docs/theory/grc-question-answer-audit.md`：新增"证据强度与问题归类"，将理论定义、形式化支点、实现锚点、工程治理缺口、企业采用问题分层。
- 调整最终饱和记录和综合评价：保留"GRC 在软件演化构造问题域内最深刻、最完整"的强判断，同时限定其不等同于软件工程总理论、实现符合性证明或经验优越性证明。
- 验证：文档变更，无代码测试；已重读修订段落并检索关键表述一致性。
- Doc-sync: No owner-doc update required（仅修订 `docs/theory/` 理论审计材料，未改变 `docs-for-ai/` 平台使用契约）。
