### 2026-07-26 (晚)

- 继续修复 auth-resource 浏览器测试
  - 根因查明「500 模块渲染失败」: SiteMapApi 对 TOPM 类型资源返回 `children: null`（而非 `[]`），SPA 侧边栏渲染器遍历时崩溃
  - RPC 创建的资源在浏览器 CRUD 表中不可见（`APIRequestContext` 与浏览器会话隔离），改用 UI 表单创建
  - 修复测试 8 缺少 `clickView`、测试 9 `request` 参数去除导致引用错误
  - 添加 `cleanupTestResources`、修复 `.catch(() => {})` 为 `resp.ok` + `console.warn`
  - 全部 38 tests passed
- 更新 `e2e-testing-troubleshooting.md`：新增 `children: null` 根因、curl 排查命令、修复/规避方式表
- 在 `frontend-rendering-pipeline.md` 新增「SPA 路由 → page.yaml 映射」章节：action-auth.xml → SiteMapApi → SPA 路由 → PageProvider__getPage 完整链路
- 分析 commit `72249cacf`（`$var→${var}` XPL 语法修正，199 文件），确认语法修复、行为等价，无需新增测试

### 2026-07-26

- 深度分析 SoloEngine (Python AI agent platform) 与 nop-ai (Java/Nop) 的架构设计差异，撰写对比报告
- 深度分析 SoloEngine 与 Mission Driver (Node.js FSM dev-loop engine) 的架构设计差异，撰写对比报告
- SoloEngine 位于 `/Volumes/data/ai/soloengine`，因 macOS 卷权限限制，通过 Python API 访问源码
- 两个分析报告写入 `ai-dev/analysis/2026-07/`
  - `2026-07-26-soloengine-vs-nop-ai-design-comparison.md`
  - `2026-07-26b-soloengine-vs-mission-driver-comparison.md`
- No owner-doc update required（分析报告不修改 platform conventions）

### 2026-07-26 (下午/evening)

- E2E 测试排障，修复 nop-auth-e2e 浏览器测试失败
  - `Navigation.ts`: `resolveBaseUrl` 添加 `BASE_URL` 环境变量兜底（之前只检查 `E2E_BASE_URL`）
  - `Navigation.ts`: `waitForMenuLoaded` 改用 `getEngineType()` 分流 — AMIS 检查 `.cxd-Page,#main-content,main`，chaos/flux 检查 `nav button[class*="flex-1"]`
  - `nop-job-app/pom.xml`: 添加 `nop-rpc-cluster` 依赖（之前 test scope 在 service 模块，app 模块启动时找不到 bean）
  - 确认 nop-auth RPC tests 15/15 pass，browser login tests 4/4 pass
  - 确认 nop-code-app 和 nop-job-app 后端正常启动并响应
- 发现 auth-resource browser tests 在创建+删除 TOPM 资源后页面显示「500 模块渲染失败」
  - 根因定位：`beforeEach` 中的 `ensureDefaultSite` 在首次 INSERT 后，后续调用因 `siteId: 'main'` 已存在而报 `duplicate-key`（被 `.catch(() => {})` 吞掉）
  - SiteMapApi 返回正常（9 项），但 SPA 侧菜单渲染异常（`.menu-scroll` 空）
  - 确认 `login` 函数（来自 `Navigation.ts`）不拦截 SiteMapApi — 测试用的是真实登录，不是 mock
- 编写 `docs-for-ai/02-core-guides/e2e-testing-troubleshooting.md`（E2E 测试排障指南）
  - 涵盖：「500 模块渲染失败」→ 侧边栏菜单为空 / AMIS CRUD 不可见 / `waitForMenuLoaded` 引擎选择器 / `ensureDefaultSite` duplicate-key / RPC 参数格式 / 测试间状态泄漏 / Playwright context 生命周期
  - 注册到 `docs-for-ai/INDEX.md`（新增「E2E 测试排障」路由）和 `00-required-reading-e2e-testing.md`（浏览器 UI 测试→按场景选读表格）
