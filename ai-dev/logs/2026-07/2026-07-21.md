### 2026-07-21上午（Plan 311 DTO 模块重构迁移）

- Plan 311 完成：方案 (a') 将 DTO 从 service/dto 和 dao/dto 移入 nop-metadata-core/dto，统一包名 `io.nop.metadata.core.dto`。11 个高频 BizModel 方法返回类型从 `Map<String, Object>` 迁移为对应 `@DataBean` DTO。

- 范围：24 个 DTO + ErrorDTO + KeyValueDTO 从 service/dao 移入 core；INopMetaTableBiz 7 个方法 + INopMetaDataSourceBiz 4 个方法的接口签名和 BizModel 实现同步变更；TestConnectionResultDTO 新增 `databaseProductVersion` 字段。

- 测试适配：690 个测试用例的 GraphQL 查询追加 field selection（DTO 类型需要选择子字段），4 个断言从 `collectedCount` 改为 `tableCount`。

- 验证：`./mvnw clean test -pl nop-metadata -am` 通过，690 tests, 0 failures。

- Design doc: `ai-dev/design/nop-metadata/02-dto-module-restructure-decision.md` 新建，`api-dto-spec.md` 更新 DTO 位置描述。
- Doc-sync: No owner-doc update required（API 行为向后兼容，仅返回类型变更为 DTO 但不改变语义）。
