# 2026-07-25 Dev Log

## Executed Plan: 13-streammodel-rectify

**Plan**: `docs/plans/nop-stream-flink-comparison/2026-07-25-1600-3-streammodel-rectify.md`

### Phase 1 — StreamComponents population in graph construction

- Added `StreamModel` field to `StreamGraph` with getter/setter
- Added `StreamModel` field to `JobGraph` with getter/setter
- Modified `StreamGraphGenerator.generate()` to call `populateStreamModel()` which:
  - Registers all transforms by ID
  - Registers all stream edges as stream entries
  - Detects `CheckpointParticipant` sinks and registers them
  - Detects `TwoPhaseCommitSinkFunction` and adds `TWO_PHASE_COMMIT_SINK` requirement
  - Always adds `DISTRIBUTED_EXECUTION` requirement
  - Detects windowing strategies via operator factory class name
- Modified `JobGraphGenerator.generate()` to propagate `StreamModel` from `StreamGraph` to `JobGraph`
- Added `TestStreamModelPopulation` with 5 focused tests

### Phase 2 — StreamModelFingerprint compile-time validation

- Modified `PartitionedPlanGenerator.generate()` to call `validateFingerprint()`:
  - Retrieves `StreamModel` from `JobGraph`
  - Computes fingerprint from the model
  - Calls `isCompatibleWith()` against the received fingerprint
  - Throws `NopException` with clear message on mismatch
- Updated `StreamExecutionEnvironment.execute()` to use JobGraph's populated StreamModel for fingerprint
- Added `TestFingerprintValidation` with 6 focused tests

### Phase 3 — IStreamSerializer interface + JsonToolSerializer + MemoryStateSerDe bridge

- Created `IStreamSerializer<T>` extending `TypeSerializer<T>` with `serialize()`/`deserialize()`
- Created `JsonToolSerializer<T>` implementing `IStreamSerializer` wrapping `JsonTool`
- Added `TypeSerializer` field (getter/setter) to `StateDescriptor`, default `JsonToolSerializer`
- Added serializer-aware helper methods to `MemoryStateSerDe`:
  - `getSerializerIfAvailable()` — returns non-default `IStreamSerializer` from descriptor
  - `serializeWithSerializer()` — uses serializer when available
  - `deserializeValue()` overload with descriptor parameter — uses `IStreamSerializer.deserialize()` for byte[] entries
- Updated ALL restore methods to pass descriptor to `deserializeValue()`
- Updated ALL snapshot methods to use serializer when available
- Added `TestIStreamSerializer` (8 tests) and `TestSerializerBridge` (2 tests)

### Phase 4 — Owner-doc synchronization + source-anchors

- Updated `state-management-design.md` §6.3 to describe `IStreamSerializer` bridge
- Added 20 nop-stream anchor entries to `source-anchors.md` (STRM-001 to STRM-020)
- Ran doc link checker — source-anchors.md links now all valid

### Closure Gates Fulfilled

- G40 addressed: IStreamSerializer bridge makes TypeSerializer actually used in serialization
- G41 addressed: StateDescriptor carries serializer reference  
- G59 groundwork: IStreamSerializer provides basis for schema versioning
- Anti-Hollow checks verified: StreamComponents populated, fingerprint throws, IStreamSerializer called
- All 53 tests pass
- No in-scope live defects deferred

### Files Changed

**New files:**
- `IStreamSerializer.java` — serialization bridge interface
- `JsonToolSerializer.java` — default serializer implementation
- `TestStreamModelPopulation.java` — Phase 1 tests
- `TestFingerprintValidation.java` — Phase 2 tests
- `TestIStreamSerializer.java` — Phase 3 tests for serializer
- `TestSerializerBridge.java` — Phase 3 tests for MemoryStateSerDe bridge

**Modified files:**
- `StreamGraph.java` — added StreamModel field
- `StreamGraphGenerator.java` — added populateStreamModel() with full component registration
- `JobGraph.java` — added StreamModel field
- `JobGraphGenerator.java` — propagate StreamModel
- `PartitionedPlanGenerator.java` — fingerprint validation
- `StateDescriptor.java` — TypeSerializer reference
- `MemoryStateSerDe.java` — serializer-aware snapshot/restore
- `StreamExecutionEnvironment.java` — use populated model's fingerprint
- `NopStreamErrors.java` — no changes needed (used existing ERR_STREAM_INVALID_STATE)
- `TestOperatorStateWiring.java` — fixed pre-existing StreamRuntimeException constructor calls
- `state-management-design.md` — updated IStreamSerializer section
- `source-anchors.md` — added nop-stream entries
- `backlog/nop-stream-flink-comparison-roadmap.md` — item 13 → done
- Plan file — all phases marked completed
