
## Plan 06 BizModel API Completeness & Contract Alignment — 执行完成

- Plan 06 全部 3 个 Phase 执行完成：
  - Phase 1: NopMetaSearchBizModel 从 @BizModel 重构为普通 Processor 类（无实体关联字段，xmeta 无意义）；确认全部 7 个 CrudBizModel 已实现对应 I*Biz 接口。
  - Phase 2: NopMetaDataSourceBizModel 中 3 处 `dao().getEntityById()` 替换为 `requireEntity()`；创建 `RecordLineageDTO` 并更新 `recordLineage()` 签名；`importOrmModels()` 返回类型从 `List<Map>` 改为 `List<ImportOrmModelResultDTO>`（同时更新 INopMetaLineageEdgeBiz 和 INopMetaModuleBiz 接口）；移除 NopMetaTableBizModel 中 3 个 @Deprecated 包装方法；nop-metadata-api/pom.xml 添加 `<parent>`；nop-bom/pom.xml 注册 nop-metadata 子模块。
  - Phase 3: orm.xml 添加 `meta/change-source` 字典定义；retention NopMetaDataSource.xmeta 添加 `sortable="false"`；Glossary→GlossaryTerm 添加 `cascadeDelete="true"`；TagLabel 添加联合唯一约束 `(entityType, entityId, tagId, source)`。
- 测试适配：testImportOrmModelsBatch 添加 GraphQL 字段选择（因 DTO 返回类型变更）；testIdempotentDerivedLabel 改为 testUniqueConstraintDuplicatePrevented（唯一约束阻止重复）。
- 验证：`./mvnw compile -pl nop-metadata -am` 通过；`./mvnw test -pl nop-metadata -am` 713 测试全部通过，0 失败/0 Error。
- Plan Status: `completed`，所有 `[x]` 勾选。
- 审计文件状态: `ai-dev/audits/2026-07-21-2039-multi-audit-nop-metadata.md` 和 `2026-07-21-2039-open-audit-nop-metadata.md` → Audit Status 从 `planned` 改为 `closed`。
- Doc-sync: No owner-doc update required.

## Plan 312 closure — nop-metadata 搜索索引

- Plan 312 全部 4 个 Phase 执行完成：
  - Phase 1: 搜索策略设计 — 审计 ISearchEngine API、确定6个首版可搜索实体、字段映射与权重、索引策略、API 签名设计、Maven 依赖确认、写入 `01-architecture-baseline.md` §九。
  - Phase 2: 基础设施 + 全量索引构建 — `pom.xml` 添加 `nop-search-api` + `nop-search-lucene` 依赖；`NopMetaIndexBuilder` 实现 6 实体全量索引构建；`NopMetaSearchBizModel` 暴露 `rebuildSearchIndex` GraphQL mutation；搜索 ErrorCode 定义。
  - Phase 3: 增量索引 — `NopMetaSearchService` 组件封装 `addDoc`/`removeDocs` 容错操作；6 个目标实体 BizModel（Classification/Tag/GlossaryTerm/Table/Entity/EntityField）save/delete hook 添加索引更新；`NopMetaModuleBizModel.importOrmModel` 导入路径索引覆盖。
  - Phase 4: 搜索 API + 集成测试 — `searchMetadata` GraphQL query（单一 topic + tagSet 按 entityType 过滤）；`SearchResultDTO`/`SearchHitDTO` 返回结构；17 新增单元测试覆盖全量索引、增量索引、搜索路径。
- 新增文件：`NopMetaIndexBuilder.java`、`NopMetaSearchService.java`、`NopMetaSearchBizModel.java`、`SearchHitDTO.java`、`SearchResultDTO.java`、`IndexResult.java`、3 个测试文件。
- 修改文件：`pom.xml`、`NopMetadataErrors.java`、`01-architecture-baseline.md`、`app-service.beans.xml`、6 个 BizModel 文件、`nop-metadata-roadmap.md`。
- 验证：`./mvnw test -pl nop-metadata/nop-metadata-service -am` 全量 705 测试通过，0 失败/0 Error。
- 闭合：Plan Status `completed`，所有 `[x]` 勾选，roadmap 新增 SR 搜索项标记 `done`。
