# 2026-08-09

### 2026-08-09（EXECUTE：nop-stream independent closure-audit of coordinator/runtime concurrency/recovery hardening — roadmap item-1 flip）

- Plan：`ai-dev/plans/nop-stream-independent-audit/2026-08-09-1330-1-coordinator-runtime-recovery-hardening-closure-audit.md`（2 phase，独立 fresh-session closure-audit，audit/doc-only，不改 nop-stream 生产代码）。
- 关键改动（事实摘要）：
  - **Phase 1 — Live-code verification of four P0/P1 fixes + non-vacuous regression-test audit**（fresh session，逐文件:行号核对，与 production plan `2026-08-04-2300-1` Exit Criteria 一致）：
    - **P0 JobCoordinator 恢复互斥**：`JobCoordinator.java` `recoveryLock`（`ReentrantLock`，`:248`）三 lock 站点 confirmed —— `assignTasks()`（`:513` 签名，`:544 lock / :548 unlock`）、`globalRecovery()`（`:1012` 签名，`:1020` snapshot `epochAtEntry`，`:1023 lock / :1074 unlock`，`:1031-1036` 锁内迟到短路守卫 WARN+return 非 fall-through）、`activateAsLeader(LeaderEpoch)`（`:1224` 签名，`:1253 lock / :1261 unlock`）。锁内 callee `prepareAssignmentsLocked`（`:546/:1072/:1259`）+ `rotateFencingEpochCoreLocked`（`:1069/:1258`，定义 `:1113`，Javadoc `:1087` "Must be called while holding recoveryLock"）。RPC fan-out `executeAssignmentFanOut` 锁外（`:550/:1077/:1263`）。Javadoc `:539-542 / :1013-1019 / :1248-1251` 注 "P1 hardening"。
    - **P1 InputGate 并发安全集合**：`InputGate.java` `inFlightAlignments`（`:99` `ConcurrentHashMap`）、`abortedBarriers`（`:111` `ConcurrentHashMap.newKeySet()`）、`blockedChannels`（`:124` `ConcurrentHashMap.newKeySet()`）、`BarrierAlignment.receivedChannels/blockedChannels`（`:790/:791` `ConcurrentHashMap.newKeySet()`）全部并发安全结构；`abortBarrierAlignment`（`:728`）add-before-remove 顺序（`:729 add → :730 remove`）闭合 re-creation race（Javadoc `:717-726`）。
    - **P1 TaskManager 许可守恒**：`TaskManager.deployTask`（`:367`）entry `tryAcquire`（`:404`）是 redeploy 路径唯一 acquire；re-occupy slot 块（`:426-435`）只 `release` 旧 permit（`:433`），无 `acquireUninterruptibly` 重 acquire；Javadoc `:418-425` 显式注明 "That extra acquire is removed"，净 permit 变化 0。
    - **P1 SupervisionLoop fail-loud**：`SupervisionLoop.waitForTerminal`（`:492`）超时分支 `throw new StreamException(ERR_STREAM_SUPERVISION_ZOMBIE_TASK_TIMEOUT)`（`:503`，非静默 fall-through）；`NopStreamErrors.java:397-402` 定义该错误码且与 `ERR_STREAM_SUPERVISION_RESTART_EXHAUSTED`（`:375-378`，region-restart 预算耗尽）区分（distinct code + message + params，Javadoc `:393-395` 注明区分）。
    - **回归测试非空壳**（逐个读测试体，硬断言确认）：`TestJobCoordinatorRecoveryConcurrency`（`assertEquals(restartCount delta==1)` `:177-179`、`assignTask count==2` `:191-193`、`countKey==1` `:198-200`、`fencingEpoch==currentEpoch` `:245-246`）；`TestInputGateMailboxAbort`（`assertNull(err)` `:132-133`、`assertEquals(barrierCount, aborts)` `:134-135`、`assertTrue(inFlight empty)` `:139-141`、`assertFalse(permanent block)` `:148-149`）；`TestTaskManager.testRedeployToOccupiedSlotDoesNotLeakPermit`（`assertEquals(0, runningTaskCount)` `:357-358`、`assertEquals(2, availablePermits==capacity)` `:359-361`）；`TestSupervisionLoopZombieTaskTimeout`（`assertThrows(StreamException)` `:54-56`、`errorCode contains supervision-zombie-task-timeout` `:59-61`、`getMessage contains taskKey` `:64-65`、`waitForTerminal 不抛 on terminal` `:80-81`）。无空方法体 / `continue` 跳过 / 吞异常。
    - **Anti-Hollow（Rule #23 接线验证）**：四个修复运行时调用链触达 confirmed —— `recoveryLock` 被 `globalRecovery`（`:1012`）+ `activateAsLeader`（`:1224`）调用；`abortBarrierAlignment` 被 `GraphModelCheckpointExecutor.registerLocalAbortHandler`（`:830` 定义，`:858` 调用，5 个 call sites `:128/:196/:271/:336/:400`）→ checkpoint abort 入口调用；`deployTask` permit 路径被 recovery redeploy 经 `executeAssignmentFanOut`（`:671`，`:674` 调 deployTask，由 `assignTasks :550` / `globalRecovery :1077` / `activateAsLeader :1263` 触发）；`waitForTerminal` 被 `SupervisionLoop.restartRegion`（`:382`，`:437` 调用，rebuild phase `:441-449` 之前）触达。
  - 验证（Phase 1）：`./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS（408 modules，0 failures）。
  - **Phase 2 — Test execution + Anti-Hollow E2E + closure evidence + roadmap item-1 flip**（同 plan 续作，全 completed）：
    - 四个回归测试 fresh run PASS：runtime `Tests run: 5, Failures: 0, Errors: 0, Skipped: 0`（TestJobCoordinatorRecoveryConcurrency 2 + TestSupervisionLoopZombieTaskTimeout 2 + TestTaskManager#testRedeployToOccupiedSlotDoesNotLeakPermit 1）；core `Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`（TestInputGateMailboxAbort）。
    - 全量 `./mvnw test -pl nop-stream -am -T 1C` → BUILD SUCCESS（全模块，0 failures；nop-stream-runtime `Tests run: 762, Failures: 0, Errors: 0, Skipped: 8`）。
    - 3 个 E2E 测试 PASS：TestDistributedExactlyOnce（7/0/0）、TestSupervisionLoopConsistentCut（3/0/0）、TestSupervisionLoopReconnectE2E（2/0/0）——覆盖 checkpoint 期间 task 故障 → 恢复 → exactly-once。
    - 独立 closure-audit evidence 写入 production plan `2026-08-04-2300-1` `Closure` 段新建子段 `### Independent Closure-Audit (CLOSURE_VERIFY)`（Reviewer fresh session ses 2026-08-09-125203 + 逐条 Gate `:137-:146` PASS/FAIL + 文件:行号锚定 + Anti-Hollow + Deferred 分类 + 结论）；已有 EXECUTE-pass evidence 未删改。
    - M3 gate 文本矛盾处理：production plan gate `:141` 追加注记子行（满足时间延后 + evidence 指针）；`Follow-up` deferred-flip 条目标记 **RESOLVED 2026-08-09**。
    - roadmap flip：`ai-dev/backlog/nop-stream-independent-audit-roadmap.md` Work Item 1 `planned` → `done`；`Last updated` 头注更新（23 work items 全 done，readiness 判定不变 bounded）。
- 偏差：无。纯审计计划（无生产代码变更）。
- 验证（全 plan）：
  - `node ai-dev/tools/check-doc-links.mjs --strict` → 退出码 0（2135 files，22313 refs，0 errors）。
  - `node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` → Passed: 1, Failed: 0（Minimum Rule #26）。
  - `./mvnw test -pl nop-stream -am -T 1C` → BUILD SUCCESS（0 failures）。
  - 本 plan 自身 Closure 段已填（Status Note + Reviewer fresh session + Evidence 逐条 + Follow-up）。Plan Status: completed；Phase 1/2 均 completed；0 unchecked items。
- Doc-sync：`No owner-doc update required`（read-only code audit + closure evidence 追加 + roadmap 状态 flip，不改 `docs-for-ai/`）。
- 后续（non-blocking follow-up）：§2a 4 行 lane-blocked（Kafka/Pulsar/PostgreSQL/Debezium 外部 backend 供应）out of audit scope，解决后可触发 readiness re-audit 追求 blanket ready（本 closure 不改 readiness 判定）；production plan P2 邻接项仍归 roadmap Follow-up Backlog。mission-driver 循环下一 CLOSURE_VERIFY round（如触发）可复核本 audit/doc-only plan 的 closure。
