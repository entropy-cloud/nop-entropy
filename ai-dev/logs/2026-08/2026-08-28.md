# 2026-08-28

### 2026-08-28（补充：RpcPollTaskManager 增加 cancelToken 即时取消回调，不等下一轮询周期）

- **做了什么**（回应"考虑了 cancelToken 了吗"→ 确认既有覆盖后按用户要求加即时感知）：`RpcPollTaskManager.register` 在 cancelToken 非 null 时经 `ICancelToken.appendOnCancel` 挂即时回调——令牌被取消的瞬间 `cancelRemote` + `resolve ERROR(CANCELED)`，不再等下一轮询周期（此前最坏延迟 = poll-interval-ms）；轮询内 `isCancelled()` 检查保留为兜底（覆盖无回调能力的令牌实现）。监听器引用存入 `PollEntry.onCancelListener`（volatile），条目终结清理（`cancelEntry`）时 `removeOnCancel` 摘除防泄漏（removeOnCancel 调用防御性 try/catch，不阻断 handle cancel + entries.remove）。register 顺序：先挂监听器再调度——平台 `Cancellable`（nop-commons，`appendOnCancel` 唯一生产实现）在令牌已取消时同步立即触发回调，`register` 末尾的 `future.isDone()` 兜底清理覆盖该立即终结路径。
- **调研事实**：平台 `ICancelToken` 生产实现为 `io.nop.commons.lang.impl.Cancellable`（ArrayList + synchronized，appendOnCancel 已取消即同步触发，取消线程同步回调）；当前 nop-job worker 执行链 `jobCtx.getCancelToken()` 默认 null（`IJobExecutionContext:22` default null，`DefaultJobExecutionContextBuilder`/`WorkerJobExecutionContext` 不覆写）——即时路径在接入真实令牌的部署形态下生效，null 时行为与之前完全一致。
- **验证**：新增 `TestRemoteJobInvoker.testCancelTokenListener_cancelsImmediatelyWithoutPollTick`——getJobStatus 返回永不完成的 future（堵死轮询兜底路径），只有即时回调能终结 future；断言 CANCELED 错误码 + 远程 cancelJob 被调 + `removeOnCancel` 恰好摘除 1 个监听器。`./mvnw test -pl nop-job/nop-job-coordinator -am` 全模块 211 tests / 0 failures / 0 errors（2 skip 为预存 @Disabled）。
- **Doc-sync**：`remote-worker-design.md` §3.5（poll 段落 + 伪代码 + 关键语义表 cancelToken 行，注明即时回调与"当前链路 token 默认 null"事实）；`docs-for-ai/03-modules/nop-job.md` REST Worker 段落同步。

### 2026-08-28（补充：RpcPollTaskManager 改挂平台 IScheduledExecutor，每 entry 独立注册 fixed-delay 轮询）

- **做了什么**（回应"不要用 ScheduledExecutorService，Nop 有 IScheduledExecutor 之类的接口；不要一批批处理，每个 poll 单独注册自己的定期轮询时间，直接用这个 executor 去执行"）：`RpcPollTaskManager` 删除自建 `Executors.newScheduledThreadPool` 与 `pollLoop` 全表扫描循环——(1) 调度改挂平台 `io.nop.commons.concurrent.executor.IScheduledExecutor`（`@Inject` setter 注入；未注入时 fallback `GlobalExecutors.globalTimer()`，与 `AbstractPollingLeaderElector` 同一模式）；(2) `register` 时为每个 `PollEntry` 独立注册 `scheduleWithFixedDelay`（周期 `nop.job.remote.poll-interval-ms`），句柄存 entry 的 volatile 字段，`future.whenComplete` 终结时 `cancel(false)` 句柄并移出注册表（handle 设置前 future 已终结的竞态由 register 末尾 `future.isDone()` 兜底）。`poll-threads` 配置语义不变但移到 beans.xml：`app-engine.beans.xml` 新增专用池 bean `nopRpcPollScheduledExecutor`（`DefaultScheduledExecutor`，池大小 `@cfg:nop.job.remote.poll-threads|2`，destroy-method=destroy）注入 manager；manager 类内 `pollThreads` 字段/setter 删除。fixed-delay 下 `pollOnce` 的整体 try/catch 保证不会因异常终止该 entry 后续调度（JDK fixed-delay 语义：runnable 抛异常即停止）。
- **动机**：自建 `ScheduledExecutorService` 绕开平台 executor 抽象（配置/监控/生命周期接不上）；单一调度循环批量扫描让每个条目的实际轮询节拍受制于全表扫描节拍。注册表集中管理（RemoteJobInvoker 不自持周期任务）的设计结论保留，变的只是"谁驱动节拍"：从"单循环扫表派发"改为"条目自持句柄、终结自取消"。
- **验证**：`./mvnw test -pl nop-job/nop-job-coordinator -am test` 全绿——coordinator 20 测试类 0 失败（TestRemoteJobInvoker 12 / TestJobCoordinatorScanner 27 含 e2e rpcPoll 链路 / TestJobDispatcherContainerWiring 4 run 1 skip 走真实 app-engine.beans.xml 装配（新增 executor bean 装配验证）/ TestDefaultRpcPollTaskClient 13 等）。测试零改动（`new RpcPollTaskManager()` 直接 new 的场景走 globalTimer fallback，语义不变）。
- **偏差**：无计划文件（ad-hoc 重构，续 08-28 系列收敛）。
- **Doc-sync**：`ai-dev/design/nop-job/remote-worker-design.md`（header/§3.1 架构图/§3.5 核心段+伪代码+关键语义表/§3.11 配置表/§四 新增"自建 executor + 批量扫描循环"拒绝行/Open Questions 追加再收敛（二））；`docs-for-ai/03-modules/nop-job.md` REST Worker 段落（含装配入口新增 `nopRpcPollScheduledExecutor`）。

### 2026-08-28（补充：RpcPollTaskManager 时间源改用 CoreMetrics，遵循 docs-for-ai 硬规则）

- **做了什么**：`RpcPollTaskManager` 两处 `System.currentTimeMillis()`（deadline 检查 + `computeDeadline`）改为 `CoreMetrics.currentTimeMillis()`，新增 `io.nop.api.core.time.CoreMetrics` 导入。理由：`docs-for-ai/04-reference/common-java-helpers.md` 与 `bizmodel-method-selfcheck.md`（规则 #18）明确**所有取当前时间的写法一律走 `CoreMetrics`**，禁止裸用 JDK 时间 API——autotest 注入的 `TestClock` 保证时间戳严格单调递增并与 ORM 自动 `createTime`/`updateTime` 同源，业务代码用 `System.currentTimeMillis()` 会自外于这条时间线，导致过期/调度类查询在测试里 `count=0`、生产埋下多时钟源不一致隐患。本次新引入的 manager 必须遵守。前两轮的代码复制了原 RemoteJobInvoker 的 `System.currentTimeMillis`（同模块其他组件沿用旧写法未修）属遗留违规，本次一并修正。
- **验证**：`./mvnw -pl :nop-job-coordinator -am test` 全绿；`grep` 确认 manager 文件无任何裸 JDK 时间 API。
- **范围**：仅修正我引入的代码（`RpcPollTaskManager`），不动同模块的 `JobPlannerScannerImpl`/`JobTimeoutCheckerImpl`/`JobCompletionProcessorImpl` 等历史违规——它们是 pre-existing 范围，本轮 AGENTS.md "Avoid noisy refactors; keep diffs minimal" 不做范围外清理。

### 2026-08-28（补充：RpcPollTaskManager 收敛为纯内存驱动，不再每 tick SELECT DB）

- **做了什么**（回应"manager 是否从数据库检查在执行的工作"）：移除 `RpcPollTaskManager` 对 `IJobTaskStore` 的依赖以及 `pollOnce` 里的 `taskStore.loadTask + isConcurrentlyFinalized` 短路——register 时把 schedule/fire/task/cancelToken/deadline **冻结**到 PollEntry，每轮只看本地持有的 `cancelToken.isCancelled()` / `deadline` / `future.isDone()`，不 SELECT `nop_job_task`。DB 权威留给 scanner writeback + TimeoutChecker；并发终结语义由 worker 上报的 `TaskStatusBean`（CANCELLED/TIMEOUT/NOT_FOUND）经映射完成。删除 `testConcurrentlyFinalized_stopsPolling`（断言的行为已不在 manager）；`MockTaskStore.freshTask` 字段清理。
- **动机**：用户指出"manager 是从数据库检查在执行的工作吗？在内存中直接记录处理就可以"——entries 注册表就是事实源，每任务每 poll 周期一次 DB loadTask 既无准确快照边界（与 scanner/TimeoutChecker 不在同一事务）又让 manager 偏离了"驱动在途轮询"的纯粹职责；墙钟 deadline + cancelToken + worker status 三重内存信号已足够驱动循环，超时/取消终态自然走 worker 上报。
- **验证**：`./mvnw -pl :nop-job-coordinator -am test` 全绿——TestDefaultRpcPollTaskClient 13 / TestRemoteJobInvoker 12（删 1）/ TestJobCoordinatorScanner 27（e2e `testE2E_rpcPoll_wallClockTimeout` 仍通过——证明纯内存 deadline 检测端到端有效）/ 容器接线 4 run 1 skip。
- **Doc-sync**：`remote-worker-design.md` §3.5 伪代码与关键语义表（"集中式轮询管理器（纯内存）"行 + "状态权威仍在 DB（但管理器不查 DB）"行替换原"状态权威始终在 DB"）；§四"拒绝了什么"新增 `manager 每 tick SELECT DB` 拒绝行；Open Questions 追加"2026-08-28 同日再收敛"注记；header 日期/状态同步。

### 2026-08-28（补充：HttpRpcPollTaskClient → DefaultRpcPollTaskClient 改名，不绑定 HTTP）

- **做了什么**：`HttpRpcPollTaskClient` 更名为 **`DefaultRpcPollTaskClient`**（测试类同步 `TestHttpRpcPollTaskClient` → `TestDefaultRpcPollTaskClient`，文件经 git mv 保留历史）。理由：实现只依赖平台 `IRpcServiceInvoker` 抽象（注册中心路由 / urlMap 静态路由），**无任何 HTTP 专属代码**（不碰 IHttpClient/URL/状态码），"Http"命名名不符实；按 nop-job 惯例（DefaultJobTaskBuilder/DefaultJobInvokerResolver/...）命名为 Default。bean id `nopRpcPollTaskClient` 不变（本就传输无关）；`NopJobApiConstants` 两处注释同步。
- **验证**：`./mvnw -pl :nop-job-coordinator -am test` 全绿（重命名后 TestDefaultRpcPollTaskClient 13 / TestRemoteJobInvoker 13 / TestJobCoordinatorScanner 27 / 容器接线 4 run 1 skip）。
- **偏差**：已关闭的 plan 2254、历史 audit 记录（ai-dev/audits/check*/）、历史日志（08-19）按 append-only 原则不回写，仅更新规范文档（design 定稿）。
- **Doc-sync**：`remote-worker-design.md` 6 处引用；`docs-for-ai/03-modules/nop-job.md` 无该类名引用无需改。

### 2026-08-28（补充：IRpcPollTaskClient 三方法增加 ICancelToken 透传底层 RPC）

- **做了什么**：`IRpcPollTaskClient` 三方法（startJob/getJobStatus/cancelJob）新增 `ICancelToken cancelToken` 参数（可传 null）；`HttpRpcPollTaskClient` 将其透传 `IRpcServiceInvoker.invokeAsync(..., cancelToken)`——token 在 RPC 在途时被取消则框架中止该调用（无需等待读超时）。调用侧：`RemoteJobInvoker.startJob` 传 `jobCtx.getCancelToken()`、`cancelAsync` 传同一 token；`RpcPollTaskManager` 的 getJobStatus/cancelJob 传轮询条目持有的 token。与 DB 模式 `RpcJobInvoker`（`RpcJobInvoker.java:64/93` 已传 `jobCtx.getCancelToken()`）契约对齐。
- **验证**：`./mvnw -pl :nop-job-coordinator -am test` 全绿——TestHttpRpcPollTaskClient 新增 `testCancelToken_passedThroughToRpcInvoker`（三方法 assertSame token 到达 RecordingInvoker）+ 其余 12 用例；TestRemoteJobInvoker 13 / TestJobCoordinatorScanner 27 等 19 个测试类 0 失败。
- **偏差**：无（直接续作 08-28 主条目，独立 commit）。
- **Doc-sync**：`remote-worker-design.md` §3.4/§3.5（伪代码 + 关键语义表）；`docs-for-ai/03-modules/nop-job.md` REST Worker 段落。

### 2026-08-28（nop-job rpcPoll 客户端全异步化 + 集中式轮询管理器 RpcPollTaskManager）

- **做了什么**（plan 2254 后续增强，非 2254 范围内新需求）：
  - `IRpcPollTaskClient` 三方法（startJob/getJobStatus/cancelJob）全部改为异步（返回 `CompletionStage`）；`HttpRpcPollTaskClient` 相应异步化（`FutureHelper.futureCall` 包参数组装 + RPC 链式 `thenApply` 映射，同步校验失败如 serviceName 缺失也经 future 失败透传，删除 `FutureHelper.syncGet` 阻塞路径）。
  - 新增**集中式轮询管理器** `RpcPollTaskManager`（`nopRpcPollTaskManager` bean）：单一注册表（`taskId → PollEntry`）管理全部在途 rpcPoll call，单一调度循环（`scheduleWithFixedDelay`，`nop.job.remote.poll-interval-ms`）定期批量异步发起 `getJobStatus`；每轮只处理"promise 未终结、未取消（cancelToken）、未超时（墙钟 deadline）、未并发终结"的条目，终态 resolve 后经 `future.whenComplete` 自动移出注册表；pollOnce 派发到小型线程池（`nop.job.remote.poll-threads`，默认 2）。
  - `RemoteJobInvoker` 瘦身：删除自有 `pollExecutor`/`poll-interval-ms`/`poll-threads`（配置移到管理器），`invokeAsync` 只做异步 startJob + 成功注册到管理器，`cancelAsync`/超时取消路径改为异步 cancelJob；poll 段状态映射（TaskStatusBean→JobFireResult）与墙钟/令牌过滤逻辑整体迁入管理器。
  - beans 装配：`app-engine.beans.xml` +`nopRpcPollTaskManager`，`RemoteJobInvoker` 经 `@Inject` 按类型注入（容器接线测试断言 assertSame）。
- **动机**：V1 形态下每任务一个 `scheduleWithFixedDelay` 周期任务，调度句柄/空转项随在途任务数线性堆积、轮询节拍分散；同步客户端下单次挂起 RPC 独占轮询线程。异步接口 + 单表单循环后，一个挂起的 getJobStatus 不占线程、不阻塞其他任务轮询（check2 P1-2 语义从"线程池兜底"升级为"异步天然不占线程"）。
- **验证**：`./mvnw -pl :nop-job-coordinator -am test` 全绿——coordinator 19 测试类 0 失败（TestRemoteJobInvoker 13 / TestHttpRpcPollTaskClient 12 / TestJobCoordinatorScanner 27 含两条 e2e rpcPoll 链路 / TestJobDispatcherContainerWiring 4 run 1 skip（plan 339 预存 H2 问题 @Disabled）/ 其余 15 类）；上游 dao 24+11+1、service 13+15+4、worker 16+15+6 均随 `-am` 重跑通过。挂起不阻塞测试改写为异步语义（task-a 的 getJobStatus 阻塞时 task-b 的 poll 仍完成）。
- **偏差**：无计划文件（2254 已关闭，本次为直接实现 + 设计文档定稿更新）；`RemoteJobInvoker.toError(ErrorCode, String)` 两参变体在重构中保留并用于缺管理器配置的错误路径。
- **Doc-sync**：`ai-dev/design/nop-job/remote-worker-design.md`（§3.1/§3.4/§3.5/§3.6/§3.11/§四/Open Questions 同步异步化 + 管理器归属）；`docs-for-ai/03-modules/nop-job.md` REST Worker 段落更新；`Index/source-anchors` 无锚点变更。