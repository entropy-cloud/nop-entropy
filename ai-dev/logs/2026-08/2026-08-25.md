# 2026-08-25

### 2026-08-25（nop-autotest-core：TestClock 锚定仿真毫秒线上游化，dual-agent 双批准）

- **做了什么**：`TestClock` 增加可选锚定仿真能力（faketime 模型）：
  - 新增静态锚点 `installAnchor(long)`/`clearAnchor()`/`isAnchorActive()`（AtomicReference<Anchor>，全 final 字段安全发布）；锚定后毫秒线 = `anchorMillis + (真实now − 安装时墙钟)`，碰撞分支 `lastTime++` 语义不变——严格单调永不重复在裸线/锚线/切换序列下均保持。
  - `currentDate()/currentDateTime()` 本就派生自毫秒线 → 日期与时间戳单源一致自动成立。
  - pom 补 `junit-jupiter`（test scope，无版本号，BOM 管理与兄弟模块一致）；新增 `TestTestClockAnchoredSim` 5 用例（冻结纪元携带 / 万次严格递增 / 三源同源+激活防空证 / clear 回真实线 / **暖实例不回拨契约 pinning**）。
  - javadoc 四条契约注记：暖实例不回拨（高水位 lastTime++ 直至墙钟追平）、锚点跨测试类持久须消费方自行清理、clear 只切线不回拨、贴午夜/长跑自然翻日。
- **动机**：应用层 nop-app-erp 已实证同构语义修复快照 flake（其仓 bug `2026-08-25-frozen-clock-millis-testclock-displacement`），本次把能力下沉平台供无冻结扩展套件直接使用；应用层 ThreadLocalFrozenClock 保持线程本地变体不变（并行保险形态，Q6 决策兼容）。
- **治理**：不触 Protected Areas（非 ORM/nop-*-api/auth/生成管线/框架核心引擎）；按调用方仓 dual-agent-approval 流程执行两独立子代理 fresh-session 审查——A（治理视角）：APPROVE 0B/0M/4m；B（技术正确性视角）：APPROVE 0B/2M/2m。全部 findings（文档契约 ×3 + pinning 测试 ×1 + 弱断言加固 ×1 + 提交范围纪律）已在本批落实；审查确认 worktree 中 nop-web-site 资产增删系其他会话预存，与本变更无关。
- **验证**：`./mvnw -pl nop-autotest/nop-autotest-core test` = **5/0/0 BUILD SUCCESS**（两位审查者各自独立复跑亦绿）；无锚点路径与原实现逐字节等价（审查 A 以 `git show HEAD:` 比对确认零默认回归面）。
- **Doc-sync**：本日志；调用方仓 bugs/successor 节回填。
