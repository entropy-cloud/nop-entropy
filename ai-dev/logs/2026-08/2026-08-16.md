# 2026-08-16

### 2026-08-16（nop-job-store 复用 batchGetEntityMapByIds 简化）

- 上一条日志的"后续（可选）"项，已执行。
- 改动（事实摘要）：
  - `JobFireStoreImpl.batchLoadFires` / `JobScheduleStoreImpl.batchLoadSchedules`：删除手写 `QueryBean` + `FilterBeans.in` + 循环组装，改为 `return (Map) dao.batchGetEntityMapByIds(ids)`（PK 均为 String 主键，实体无逻辑删除，语义与旧查询等价：缺失 id 不出现在结果中）。
  - 顺带清理两个文件的 `ArrayList`/`HashMap` 未使用 import。
- 验证：`./mvnw compile -pl nop-job/nop-job-dao -am` → EXIT=0。`mvnw test` 下 `TestJobStoreImpl`/`TestJobFireStoreRace` 共 31 errors（H2 "Table not found"）为环境问题，已通过 `git stash` 基线复验确认改动前同样失败，与本次改动无关。
- 验证（orm 侧）：`./mvnw test -pl nop-persistence/nop-orm -am -Dtest=TestEntityDao` → 18 tests, 0 failures（含新增 `testBatchRequireEntityMapByIds`）。
- Doc-sync: `docs-for-ai/02-core-guides/concurrency-and-transactions.md` 的缓存行为 API 表已补两行 Map 方法（上一日志条目）。

### 2026-08-16（API 增强：IEntityDao 增加 batchRequireEntityMapByIds）

- 需求：`IEntityDao` 按 id 集合返回 `Map<id, entity>`，try 版本允许缺失、require 版本缺失即抛错，方法名体现返回 Map 结果。
- 现状：try 版本已存在（`batchGetEntityMapByIds`，OrmEntityDao 中跳过 MISSING 实体），此前 `JobFireStoreImpl.batchLoadFires` 仍手写 `QueryBean` 查询属于冗余。
- 改动（事实摘要）：
  - `nop-persistence/nop-dao/.../IEntityDao.java`：新增 `Map<Object, T> batchRequireEntityMapByIds(Collection<?> ids)`（require 语义，缺失抛 `UnknownEntityException`）。
  - `nop-persistence/nop-orm/.../OrmEntityDao.java`：复用 `batchRequireEntitiesByIds`（内部 `checkAllEntityValid` 校验）后组装 Map。
  - `TestEntityDao.java`：新增 `testBatchRequireEntityMapByIds`（存在/缺失/抛错三条路径，与 `batchGetEntityMapByIds` try 语义对照）。
- 验证：`./mvnw test -pl nop-persistence/nop-orm -am -Dtest=TestEntityDao` → Tests run: 18, Failures: 0, Errors: 0, BUILD SUCCESS。
- 后续（可选）：`JobFireStoreImpl.batchLoadFires` / `JobScheduleStoreImpl.batchLoadSchedules` 可改用 `batchGetEntityMapByIds` 删除手写 QueryBean 代码。