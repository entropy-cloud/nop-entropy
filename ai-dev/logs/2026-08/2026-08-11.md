# 2026-08-11

### 2026-08-11（PLAN 338：nop-wf flux 设计器集成 — Phase 1/2/3 实施 + IoC baseline 修复）

- **Plan**：`ai-dev/plans/338-nop-wf-flux-designer-integration.md`
- **做了什么**：
  - **Phase 1（server-side，commit 27d096eb0 已 landing）**：`WfGraphDocumentCodec` / `NopWfDesignerErrors` / `WfDesignerConfigBuilder` / `WfDesignerConstants` / `WorkflowDesignerService` 全部已实现；bean 经 `nop-wf-service/_vfs/nop/wf/beans/app-service.beans.xml` 显式 `<bean>` 注册；保存链路走 XNode 原始层 + `WfModelParser` 引擎加载路径校验。
  - **Phase 2（前端装配）**：`designer.flux.yaml` 重写为 `dynamic-renderer` + spinner fallback，loadAction 指向 `/r/WorkflowDesignerService__loadDesignerPage`；`designer.page.yaml` 简化为 AMIS 入口（flux 模式自动回退到 `.flux.yaml`）；`NopWfDefinition.view.xml` design 行操作的 drawer 增加 `<data><wfDefId>${id}</wfDefId></data>` 传参。
  - **Phase 2 测试**：`TestFluxYamlPages` 扩展为 4 个 case（基础加载、dynamic-renderer 结构断言、loadAction URL 与 wfDefId 绑定断言、spinner fallback 断言）。
  - **Phase 3 端到端（codec 层）**：`TestWfGraphDocumentCodec` 增加 `testEditFlowEngineLoadable`（空定义 → 编辑 → 回写 → `WfModelParser.parseWorkflowNode` 引擎加载成功）+ `testCycleRejectedByEngineLoadPath`（有环图引擎加载路径拒绝）。
  - **IoC baseline 修复**：`BeanCreationContext.runInitActions/runLazyPropActions/runDelayActions` 改为 drain-then-iterate 模式 —— 原代码在迭代 `TreeMap.values()` 时，`action.run()` 触发递归 `getBean` → `flushActions` → 同 Map 的 `addInitAction`，导致 `ConcurrentModificationException`。修复后先取出快照再迭代，新增 action 进入新 Map 由下一轮 flush 处理。该 bug 由 commit `88942c919`（BeanCreationContext 重构）引入。
- **偏差**：
  - **bundle 前置未完成**：现 `nop-web-site/assets/pkg-nop-chaos-flux-*.js.gz` 三份 bundle 解压 grep `designer-page` 均为 0 命中。设计器渲染依赖 flux 仓 → nop-chaos-next 仓 → nop-web-site 三仓链路重建 bundle，超出本仓库单仓实施范围。bundle 升级是独立的跨仓任务。
  - **DB 集成测试受阻于 baseline IoC 回归**：`TestWorkflowDesignerService` 与 `TestFluxYamlPages`（以及既有 `TestUseApprovalE2E`）在 bean 容器初始化阶段失败：`OrmSessionFactoryBean.init()` 报 `shardSelector` 为 null（producer bean 属性注入顺序问题），同样是 commit `88942c919` 引入的 baseline 回归。CME 修复解决了崩溃，但 producer bean 属性注入顺序问题仍未解决，需要 IoC 框架层继续排查。本计划的 codec/config 层测试（10 个，全绿）不受影响。
- **验证**：
  - `./mvnw test -pl nop-wf/nop-wf-service -am -Dtest='TestWfGraphDocumentCodec,TestWfDesignerConfigFixture'` → **10 tests, 0 failures, 0 errors**（含新增 2 个端到端用例）
  - `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-wf-service --severity high` → **0 findings**
  - `node ai-dev/tools/check-doc-links.mjs --strict` → 2 errors（均为 `ai-dev/backlog/nop-credential-mfa-roadmap.md` 的 pre-existing broken links，与本计划无关；本计划新增的 6 个 warning 均为对外部仓 flux/chaos-next 路径的引用）
- **Doc-sync**：
  - `docs-for-ai/02-core-guides/workflow-configuration.md` 新增"工作流设计器"章节（入口、API 契约、转换器规则、bundle 前置）
  - `ai-dev/design/nop-wf/workflow-designer-integration.md` 与实现核对一致（toolbar 装配面、dynamic-renderer、合成节点保留 id 等裁定已落实）
- **Closure status**：Phase 1/2/3 实施完成，但 DB-backed 端到端测试（Phase 3 第一条 "loadDesignerPage → saveDocument → 引擎启动实例"）受阻于 baseline IoC 回归；bundle `designer-page` renderer 未在当前 `nop-web-site` 资产中。Plan Status 维持 `active`，待 IoC baseline 修复 + bundle 重建后由独立 closure audit 收口。
