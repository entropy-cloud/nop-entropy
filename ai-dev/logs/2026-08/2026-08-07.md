# 2026-08-07

### 2026-08-07（EXECUTE：nop-stream 控制面/运行时并发与恢复硬化 plan 1）

- Plan：`ai-dev/plans/nop-stream-production/2026-08-04-2300-1-coordinator-runtime-concurrency-recovery-hardening.md`（4 phase 全部 completed，Plan Status: completed）。
- 关键改动（事实摘要）：
  - **P0 JobCoordinator 恢复竞态**：新增 `recoveryLock`（`ReentrantLock`）串行化 `globalRecovery`/`rotateFencingEpochCoreLocked`/`prepareAssignmentsLocked`；将 `assignTasks` 拆为锁内 `prepareAssignmentsLocked`（物化 assignment 入 working set）+ 锁外 `executeAssignmentFanOut`（RPC fan-out 不持锁）；迟到守卫（获锁前 snapshot `epochAtEntry`，锁内重检 fencingEpoch，迟到方 WARN 短路、不递增 restartCount/recoveryGen）。原 `rotateFencingEpochAndRestore` 重命名为 `rotateFencingEpochCoreLocked`（不再内嵌 assignTasks）。
  - **P1 InputGate 跨线程**：`inFlightAlignments`(`LinkedHashMap`→`ConcurrentHashMap`)、`abortedBarriers`/`blockedChannels`/`BarrierAlignment.{receivedChannels,blockedChannels}`(`HashSet`→`ConcurrentHashMap.newKeySet()`)；`oldestAligning` 改按 min checkpointId（`ConcurrentHashMap` 不保插入序）；`abortBarrierAlignment` 改 **add-before-remove** 顺序（闭合"remove 与 add 之间 task 线程重建已 abort 的 alignment"的 re-creation race）。
  - **P1 TaskManager 许可泄漏**：移除 `deployTask` 重占用 slot 路径的额外 `capacitySemaphore.acquireUninterruptibly()`（净许可账目由 -1 修正为 0）。
  - **P1 SupervisionLoop zombie**：`waitForTerminal` 超时改抛 `ERR_STREAM_SUPERVISION_ZOMBIE_TASK_TIMEOUT`（新增独立错误码，区别于 `ERR_STREAM_SUPERVISION_RESTART_EXHAUSTED`）；`waitForTerminal` 改 package-private + `budgetMs` 参数以便聚焦测试。
  - 新增回归测试：`TestJobCoordinatorRecoveryConcurrency`（2）、`TestInputGateMailboxAbort`（1）、`TestTaskManager.testRedeployToOccupiedSlotDoesNotLeakPermit`（1）、`TestSupervisionLoopZombieTaskTimeout`（2）。
- 偏差：**Phase 2 方案 (a)（abort 经 task mailbox CONTROL 邮件投递）经实施验证不可行**——`InputGate.read()` 在 barrier 对齐期间阻塞，仅在调用方 `processInputGate` 循环顶部排空 mailbox，故 mailbox 投递的 abort 无法解除被 abort 的对齐的阻塞（epoch-precise abort 会死锁至 alignment timeout=30s 后抛异常，回归）。signalCancel 可用 mailbox 是因配合 `task.cancel()` 中断解阻塞，非 mailbox 排空；epoch-precise 分支无中断。改用方案 (b)（并发安全集合），零回归达成实际目标（三个集合跨线程访问不再 CME / 不丢 barrier / 不永久阻塞）。裁定记录于 plan Phase 2 + `checkpoint-design.md` §2.8.1 D3。
- 验证：
  - `./mvnw clean install -pl nop-stream -am -T 1C -DskipTests` → BUILD SUCCESS。
  - `./mvnw test -pl nop-stream -T 1C` → BUILD SUCCESS（全模块；nop-stream-core 1384 tests / nop-stream-runtime 751 tests，0 failures；E2E `TestDistributedExactlyOnce` 7/7、`TestSupervisionLoopConsistentCut` 3/3、`TestSupervisionLoopReconnectE2E` 2/2 通过；`TestMultiJvmExactlyOnceRecovery` 为 multi-JVM 测试标准 suite skipped，由 in-process distributed 测试覆盖）。
- Commit: 未提交（待用户指示）。
- Doc-sync：`ai-dev/design/nop-stream/failover-design.md`（§2.3 恢复互斥约定 + waitForTerminal fail-loud 语义）、`ai-dev/design/nop-stream/checkpoint-design.md`（§2.8.1 D3 abort 并发安全 + 方案 a 拒绝理由）已更新；Phase 3 `No owner-doc update required`。
- 后续：roadmap work-item-1 `planned`→`done` flip 受 roadmap 规则约束（"done only after independent closure-audit evidence"），属后续 CLOSURE_VERIFY mission step。
