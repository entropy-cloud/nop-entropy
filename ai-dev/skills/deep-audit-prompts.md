# nop-entropy 深度审核提示词手册

> **定位**: 本文档是一份面向 AI 的多维度深度审核提示词集合，专门针对 Nop 平台的实现模块。每个维度提供一个可复用的"维度正文"，由主 agent 与共享前缀拼接后派发给专用子 agent 执行。
> **前提**: 执行审核前必须先阅读 `docs-for-ai/INDEX.md`、`AGENTS.md` 和相关架构文档，以当前代码和文档为准。
> **输出格式**: 每个发现按统一格式输出（见附录 A）。

---

## 审核总览

本手册覆盖以下 **21 个审核维度**，分为 8 大类：

| 类别 | 维度编号 | 维度名称 | 审核目标 |
|------|---------|---------|---------|
| **A. 架构与模块边界** | 01 | 依赖图与模块边界 | Maven 模块间依赖是否合规，是否遵守分层规则 |
| | 02 | 模块职责与文件边界 | 单一职责、文件大小、生成产物与手写代码边界 |
| | 03 | API 表面积与契约一致性 | BizModel 公开接口、GraphQL schema、I*Biz 接口是否收敛 |
| **B. 模型与数据架构** | 04 | ORM 模型与实体设计 | 实体建模、关系定义、域使用、displayName 本地化 |
| | 05 | 生成管线完整性 | model→codegen→dao→meta→service→web 链路是否正确闭合 |
| | 06 | Delta 定制合规性 | Delta 文件位置、x:extends 使用、升级兼容性 |
| **C. 服务层与业务逻辑** | 07 | BizModel 规范遵循 | CrudBizModel 继承、注解使用、方法归属 |
| | 08 | IoC 与 Bean 配置 | 注入方式、beans.xml 语法、生成文件不可手改 |
| | 09 | 错误处理与错误码 | NopException 使用、ErrorCode 定义、异常链保留 |
| **D. XLang 与模型层** | 10 | XDSL 与 XLang 正确性 | x:schema 引用、x:extends/x:override 语义、XDSL 合规 |
| | 11 | XMeta 与 BizModel 对齐 | xmeta 字段与 BizModel 方法返回值、GraphQL schema 一致性 |
| **E. 框架集成与运行时** | 12 | GraphQL 与 API 层 | GraphQL 引擎使用、selection 机制、字段权限 |
| | 13 | 安全与权限模型 | 权限注解、数据权限、字段级权限 |
| | 14 | 异步与事务模式 | 事务边界、异步处理、txn().afterCommit 使用 |
| **F. 工程质量** | 15 | 类型安全与泛型使用 | 原始类型、泛型擦除、接口契约精度 |
| | 16 | 测试覆盖与质量 | AutoTest 模式、测试边界、遗漏路径 |
| | 17 | 代码风格与规范 | 命名、import 顺序、注释规范 |
| **G. 文档与一致性** | 18 | 文档-代码一致性 | docs-for-ai 文档与实际实现的对齐 |
| | 19 | 命名与术语一致性 | 统一术语、实体名/字段名一致性 |
| | 20 | 跨模块契约一致性 | 模块间接口契约、版本兼容 |
| **H. 测试有效性** | 21 | 单元测试有效性 | 测试是否真正能捕获 bug、是否存在反模式 |
| **H. 测试有效性** | 21 | 单元测试有效性 | 测试是否真正能捕获 bug、是否存在反模式 |

---

## 调度架构

### 总体流程

```
主 agent（读取本文档，选择目标模块 + 要执行的维度）
  │
  ├── 准备阶段：读取模块文档，生成命令基线
  │
  ├── 阶段一 — 迭代深挖（每个维度独立）
  │   ├── Task → 维度 01 第 1 轮（初审）
  │   ├── Task → 维度 01 第 2 轮（追加深挖）
  │   ├── ...（最多 10 轮，直到无新发现）
  │   │
  │   ├── Task → 维度 02 第 1 轮（初审）
  │   ├── ...
  │   └── [各维度独立执行]
  │
  ├── 阶段二 — 独立复核（每个维度独立）
  │   ├── Task → 维度 01 维度复核（读取完整文件，逐条保留/降级/驳回）
  │   ├── Task → 维度 01 子项 A 复核（高风险逐条）
  │   ├── Task → 维度 01 子项 B 批量复核（低风险批量）
  │   │
  │   ├── Task → 维度 02 维度复核
  │   ├── ...
  │   └── 汇总已复核通过的结果 → summary.md
  │
  └── 阶段三 — 跨维度汇总（所有维度完成后）
      └── 主 agent 生成深度审核汇总报告
```

### 调度方法

1. 主 agent 读取本文档，选择**目标模块**和要执行的**维度**。
2. **准备阶段**：
   a. 主 agent 读取目标模块的 `docs-for-ai/` 相关文档、`ai-dev/design/` 设计文档。
   b. 运行基础命令生成基线（如 Maven 依赖树、文件行数统计、checkstyle 等）。
   c. 如果存在 `ai-dev/audits/` 下的历史审计报告，快速浏览已有发现类型，避免重复。
3. **阶段一 — 迭代深挖**：
   a. 主 agent 用"共享提示词前缀 + 该维度正文"拼接出完整 prompt，派发**第 1 轮（初审）子 agent**。
   b. 初审完成后，将发现保存到 `ai-dev/audits/{日期}-deep-audit-{模块名}-{标识}/{维度编号}-{名}.md`。
   c. 如果第 1 轮有发现，主 agent 派发**第 2 轮（追加深挖）子 agent**，prompt 中包含：
      - 共享提示词前缀
      - 该维度正文（相同）
      - 已保存发现全文
      - 深挖追加指令（见下文"深挖追加提示词模板"）
   d. 第 2 轮完成后，新发现追加到同一个维度文件（标注 `## 深挖第 2 轮追加`）。
   e. 重复 c-d，直到某轮输出无新发现或达到 10 轮上限。
   f. 如果第 1 轮就无发现，直接进入阶段二维度复核（复核 agent 确认零发现）。
4. **阶段二 — 复核**：
   a. 深挖结束后，主 agent 派发一个**独立的维度复核子 agent**，输入为该维度完整文件（含所有深挖追加），要求它重新读代码与文档，输出"保留 / 降级 / 驳回"的逐条判断。
   b. 维度复核完成后，主 agent 必须对高风险或不确定发现项，再派发**独立的子项复核子 agent**逐项验证；低风险项可按文件或按模式批量复核。
5. 只有在"深挖完成 + 维度复核 + 必要的子项复核"都完成后，该维度结果才允许进入最终汇总。
6. 所有维度完成后，主 agent 汇总**已复核通过**的结果，生成「深度审核汇总报告」（格式见附录 A）。

### 深挖追加提示词模板

从第 2 轮起，主 agent 在维度正文后追加以下内容：

```text
---

## 深挖追加指令（第 N 轮）

以下是本维度前 N-1 轮已保存的全部发现：

[粘贴前 N-1 轮的完整发现文本]

你的任务：

1. 读取上述已有发现，理解本维度已经覆盖了哪些文件、模式和路径。
2. 基于已有发现暴露的文件、模式和关联路径，**继续深挖**本维度中尚未覆盖的盲区：
   - 已有发现涉及的文件是否有同类型问题未被检出？
   - 已有发现的模式是否在其他文件中也存在？
   - 是否有与已有发现相关的、但尚未检查的代码路径？
   - 该维度的执行步骤中是否有尚未充分覆盖的步骤？
3. **只输出新发现的条目**。不要重复已有发现中已经报告的内容。
4. 如果经过仔细检查后确实没有发现新的问题，输出："未发现新的问题。深挖结束。"
5. 如果仍能找到零散细节，但这些细节只是低价值、机械重复、缺少明确风险或缺少可行动建议，不要为了凑数量继续报告；输出："未发现新的高价值问题。深挖结束。"
6. 每个新发现仍需遵守统一的发现格式（文件路径 + 行号 + 证据片段 + 严重程度 + 现状 + 风险 + 建议）。

【重要】发现完整性要求：
- 每个新发现必须按完整格式输出（### 标题 + 文件 + 行号 + 3-10行证据代码片段 + 严重程度 + 现状 + 风险 + 建议 + 误报排除）。
- 禁止将发现压缩为关键词、一句话摘要或表格行。
- 禁止只输出标题列表然后说"详情见上方"。
- 如果发现数量较多导致输出较长，宁可分多个子 agent 输出，也不要压缩每个发现的内容。
```

### 保存与追加规则

主 agent 在保存深挖结果时必须遵守以下规则：

1. **第 1 轮结果**：将子 agent 输出的完整发现正文写入文件。如果子 agent 输出被压缩（发现条目少于 8 行），主 agent 必须要求重新输出。
2. **追加深挖结果**：在文件末尾追加 `## 深挖第 N 轮追加` 标题，然后写入新发现的完整正文。**不修改、不压缩、不覆盖**前 N-1 轮已保存的内容。
3. **复核结论追加**：在深挖全部结束后，追加 `## 维度复核结论` 和 `## 子项复核结论`。复核结论引用已有发现编号（如 `[维度04-01]`），不重写发现内容。

### 并行策略

- 同批次的**第 1 轮（初审）**可以并行派发。
- 同一维度的**深挖追加轮次**必须串行（每轮依赖前一轮结果）。
- 同一维度的**维度复核**必须在该维度深挖阶段全部结束后进行。
- 同一维度的**子项复核**必须在该维度复核完成后进行。
- **不同维度之间**的深挖、复核可以并行（互不依赖）。

批次策略已移至"结果输出与归档"节中统一维护。

> **注意**：同批次内各维度的第 1 轮初审可并行，但每个维度自身的深挖追加轮次是串行的。实际调度时，主 agent 可按批次并行派发各维度第 1 轮，然后对有发现的维度继续串行深挖，最后再并行派发各维度的复核。

### 调度示例

**第 1 轮（初审）并行派发示例**：

```
请用 Task tool 并行派发以下 3 个子 agent：

1. subagent_type="explore", description="维度01: 依赖图与模块边界 (第1轮初审)"
   prompt = （共享提示词前缀 + 维度 01 正文）

2. subagent_type="explore", description="维度04: ORM 模型与实体设计 (第1轮初审)"
   prompt = （共享提示词前缀 + 维度 04 正文）

3. subagent_type="explore", description="维度07: BizModel 规范遵循 (第1轮初审)"
   prompt = （共享提示词前缀 + 维度 07 正文）
```

**深挖追加轮次示例**（维度 01 第 1 轮有 5 个发现）：

```
维度 01 第 1 轮发现已保存到 ai-dev/audits/2026-05-18-deep-audit-nop-code-full/01-dependency-graph.md。
第 1 轮有 5 个发现，继续深挖：

Task(
  subagent_type="explore",
  description="维度01: 依赖图与模块边界 (第2轮深挖)",
  prompt = 共享提示词前缀 + 维度 01 正文 + 深挖追加指令（第 2 轮）+ 已有发现全文
)
```

如果第 2 轮有新发现，追加到文件并继续第 3 轮；如果无新发现，深挖结束。

**复核调度示例**（维度 01 深挖结束，共 3 轮 8 个发现）：

```
1. 派发一个独立子 agent 做"维度01整体复核"
   - 输入：维度01完整文件（含 3 轮深挖全部发现）
   - 要求：重新读 live code / 文档，输出逐条"保留 / 降级 / 驳回"清单

2. 对维度01中的高风险或不确定发现，再分别派发独立子 agent
   - 输入：某一条发现全文 + 对应文件路径
   - 要求：只复核这一条，输出"成立 / 降级 / 驳回"与原因

3. 对低风险重复项，可按文件或模式做批量复核
   - 输入：同一文件中的同类发现列表，或同一模式的多条发现
   - 要求：输出逐条保留/降级/驳回结果，不得只给总评

4. 只有通过独立复核的条目才允许进入最终汇总
```

### 结果输出与归档

每个维度的迭代深挖 + 复核完成后，所有已通过独立复核的结果必须保存到 `ai-dev/audits/` 目录下的专用子目录中。

**目录结构**：

```
ai-dev/audits/{year}-{month}-{day}-deep-audit-{模块名}-{简短标识}/
├── 01-dependency-graph.md
├── 02-module-responsibility.md
├── 03-api-surface.md
├── 04-orm-model.md
├── 05-codegen-pipeline.md
├── 06-delta-customization.md
├── 07-bizmodel-conformance.md
├── 08-ioc-beans.md
├── 09-error-handling.md
├── 10-xdsl-xlang.md
├── 11-xmeta-bizmodel-alignment.md
├── 12-graphql-api.md
├── 13-security-permission.md
├── 14-async-transaction.md
├── 15-type-safety.md
├── 16-test-coverage.md
├── 17-code-style.md
├── 18-doc-code-consistency.md
├── 19-naming-consistency.md
├── 20-cross-module-contract.md
├── 21-unit-test-effectiveness.md
└── summary.md
```

**命名规则**：

- 子目录名格式：`{YYYY}-{MM}-{DD}-deep-audit-{模块名}-{简短标识}`（如 `2026-05-18-deep-audit-nop-code-full` 或 `2026-05-18-deep-audit-nop-auth-service`）
- 每个维度一个 md 文件，文件名格式：`{维度编号}-{英文简短名}.md`
- `summary.md` 保存主 agent 的汇总报告（格式见附录 A）

**要求**：

1. 每个维度文件包含该维度的所有深挖轮次结果 + 复核结论（保留/降级/驳回标注）。文件内部结构为：
   - 第 1 轮（初审）发现（每个发现必须是完整的 `###` 级条目，不是关键词列表）
   - `## 深挖第 2 轮追加`（如有，每个追加发现同样是完整条目）
   - `## 深挖第 N 轮追加`（如有，同上）
   - `## 维度复核结论`（复核子 agent 输出，引用发现编号，不重写内容）
   - `## 子项复核结论`（如有逐条复核，同上）
   - `## 最终保留项`（表格形式，包含编号、严重程度、文件路径、一句话摘要）
2. 汇总报告必须包含深挖统计（每维度深挖轮次数、每轮发现数）和复核统计（初审总数、保留/降级/驳回数）
3. 同一天的多次审计使用不同标识后缀区分
4. **内容质量门禁**：主 agent 保存维度文件前必须检查：
   - 每个发现条目是否包含完整格式（文件、行号、证据片段、严重程度、现状、风险、建议）
   - 深挖轮次是否是完整发现而非关键词列表
   - 复核结论是否引用了已有发现编号而非独立重写
   - 如果检查不通过，必须要求子 agent 重新输出后再保存

### 最低内容量参考

作为经验参考（非硬性上限/下限），一个有 5 个发现的维度文件，按完整格式输出通常在 120-200 行。如果 5 个发现只写出了 40 行，几乎可以确定发生了压缩。

### 批次策略（更新）

| 批次 | 维度 | 可否并行 |
|------|------|---------|
| 第一批 | 01, 04, 07 | 互相独立，可并行 |
| 第二批 | 05, 08, 09 | 互相独立，可并行 |
| 第三批 | 02, 06, 10, 11, 15 | 互相独立，可并行 |
| 第四批 | 03, 12, 13, 14 | 互相独立，可并行 |
| 第五批 | 16, 17, 18, 19, 20, 21 | 互相独立，可并行 |

### 自动化工具基线

主 agent 在派发维度前，应优先运行以下命令并将输出提供给子 agent：

1. `./mvnw dependency:tree -pl {目标模块}` — 依赖树基线（维度 01）
2. `find {目标模块} -name "*.java" -exec wc -l {} + | sort -rn | head -30` — 文件行数基线（维度 02）
3. `./mvnw checkstyle:check -pl {目标模块}` — 代码风格基线（维度 17，如已配置）
4. `./mvnw test -pl {目标模块}` — 测试报告基线（维度 16）
5. `find {目标模块} -name "_*.xml" -o -name "_*.java"` — 生成产物清单（维度 05）

如果子 agent 需要但主 agent 未提供基线，子 agent 应明确标注"缺少工具基线"，不能把手工估算当确定事实。

### 子 Agent 提示词装配结构

下文每个维度给出的内容是**维度正文**，不是可单独复制执行的完整 prompt。主 agent 派发前，必须先拼接共享前缀。

完整 prompt 由两部分组成：

```
1. 共享提示词前缀（固定，所有维度相同）
2. 维度正文（本维度目标 + 必读文档 + 执行步骤 + 额外输出要求）
```

### 共享提示词前缀

主 agent 在派发任一维度前，应先附加以下共享前缀：

```text
你正在审核 nop-entropy 项目中的 [目标模块] 模块。这是一个基于 Nop 平台的 Java 多模块 Maven 工程。

Nop 平台核心特征：
- 模型优先开发（model → codegen → dao → meta → service → web → app）
- XLang 语言体系（XDSL、XBiz、XMeta、XView 等）
- NopIoC（基于 JSR-330，非 Spring）
- BizModel 服务层（GraphQL 自动映射）
- Delta 定制机制（x:extends + x:override）

执行前先阅读：
1. docs-for-ai/INDEX.md（文档导航基线）
2. AGENTS.md（代码规范、验证命令、agent 工作流）
3. docs-for-ai/01-repo-map/module-groups.md（模块分组）
4. docs-for-ai/01-repo-map/domain-module-pattern.md（标准业务模块骨架）
5. docs-for-ai/02-core-guides/（相关核心指南）
6. ai-dev/design/ 中与目标模块相关的设计文档（如有）
7. 本维度列出的 owner 文档

注意：当前审计基线按 **live code 为准** 执行。不以历史日志、已关闭计划或口头约定为准。

通用审计口径：
1. 以当前代码为准，不以历史日志、已关闭计划或口头约定为准。
2. 不重复报告已收敛的问题。
3. 不把"看起来不优雅"当问题，必须有结构性原因或可量化风险。
4. 对 Nop 平台的模型驱动开发模式保持克制。代码生成的产物（以 `_` 前缀标记的文件）不应作为审计发现的对象，除非生成模板本身有误。审计重点是手写代码和模型定义。
5. 对框架的约定大于配置模式保持克制。BizModel 返回实体对象、`@Inject` 使用 `protected` 字段、`_*.xml` 是代码生成产物，这些是平台标准模式，不是问题。
6. 每个发现必须可定位：文件路径 + 行号范围 + 3-10 行证据片段。
7. 区分生成代码与手写代码。生成代码的问题应追溯至模型或模板，而不是标记生成产物本身。
8. 初审输出只是线索，不是最终事实；最终结论必须经过独立复核。
9. 如果某条发现看起来像历史上的高频误报模式，必须明确写出"为什么这次不是同类误报"。

严重程度判级：
- P0: 当前已构成错误行为、安全违约、核心数据损坏风险、或违反 CI/硬性架构红线。
- P1: 高概率回归、核心契约漂移、跨模块边界错误、或会误导后续开发的文档/公开面问题。
- P2: 真实维护成本或局部缺陷，但可排期处理。
- P3: 低优先级但真实存在的问题。

Nop 平台常见误报校准（以下**不是**问题，不应作为审计发现）：
- BizModel 方法返回 ORM 实体对象（平台标准模式，xmeta + GraphQL selection 控制字段可见性）
- `@Inject` 注入 `protected` 字段而非 `private`（NopIoC 限制，非设计缺陷）
- `_` 前缀的生成文件内容（除非追溯到模型/模板层面的错误）
- I*Biz 接口放在 dao 模块（代码生成的 DAO 层 CRUD 契约，绑定 DAO 实体）
- `jakarta.inject`/`jakarta.annotation` 出现在 core 层（Java 标准规范，不算框架依赖）
- 内部服务接口返回 core 层模型（内部以高性能为目标的合理选择）
- CrudBizModel 的标准继承模式（setEntityName + extends CrudBizModel<T>）
- xbiz 文件中的方法签名与 BizModel Java 方法不完全一致（GraphQL 引擎通过反射调用，xbiz 是声明性元数据）
- 多模块的 pom.xml 中存在必要的传递依赖声明（Maven 最佳实践要求显式声明直接使用的依赖）

自动化工具有效性规则：
1. 已被 Maven 构建、checkstyle、编译器类型检查覆盖且当前通过的问题不得重复报告。
2. 主 agent 提供的命令基线输出应作为起点，不是让子 agent 重新手工扫描。
3. 如果主 agent 已提供 Maven 依赖树，子 agent 不需要再自行构建依赖图。
4. checkstyle/checkstyle 报告的机械问题只在主 agent 未提供基线时才手工报告。

【发现条目完整性 — 强制规则，不可违反】：
1. 每个发现必须按统一格式（附录 A）完整输出，包含文件路径、行号范围、3-10 行证据代码片段、严重程度、现状、风险、建议。
2. **禁止将发现压缩为一行标题或表格单元格。**
3. **禁止用摘要替代完整条目。**
4. **证据代码片段不可省略。** 如果因上下文限制无法贴出代码，必须明确标注"[证据片段待补充]"并列出需要查看的具体行号范围。
5. 每个发现条目最少 8 行（文件、行号、证据、严重程度、现状、风险、建议、误报排除），少于 8 行的条目视为不合格。
6. 复核结论表是补充索引，不能替代发现正文。

如果本维度需要命令输出（如 Maven 依赖树、checkstyle、测试报告），优先由主 agent 先生成基线，再把结果连同本 prompt 一起提供给你；不要假设你一定能直接运行命令。
```

维护规则：

- 共享方法论、严重程度判级、命令基线策略只维护在"共享提示词前缀"中。
- Nop 平台特定的误报校准直接写在共享前缀的"常见误报校准"节中。
- 各维度正文只保留该维度特有的目标、owner 文档、执行步骤、特例说明和额外输出要求。
- 如果某个维度需要额外约束，只写该维度新增部分，不要重复抄写共享前缀内容。

---

## 发现条目完整性守则

> **本节是所有子 agent 的硬性约束，与通用审计口径同级。** 违反本节规则的维度文件视为不合格，主 agent 必须要求子 agent 重新输出。

### 问题：发现被过度压缩

历史审核中，维度文件的发现被层层压缩为关键词或表格行，导致读者无法理解问题是什么、影响是什么、该怎么做。以下是典型的压缩反模式及其正误对照：

**反模式 1：深挖轮次只记关键词**

```markdown
## 深挖轮次

- 第 1 轮: ORM 关系定义缺失、displayName 未本地化。
- 第 2 轮: 错误码定义不规范、缺少 .param()。
```

**正确做法**：每轮深挖的每个发现必须是完整条目：

````markdown
## 第 1 轮（初审）

### [维度04-01] NopCodeSymbol 实体缺少与 NopCodeFile 的关系定义

- **文件**: `nop-code/model/nop-code.orm.xml:45-60`
- **证据片段**:
  ```xml
  <entity name="NopCodeSymbol" tableName="nop_code_symbol">
    ...
  </entity>
  ```
- **严重程度**: P1
- **现状**: NopCodeSymbol 实体定义中没有到 NopCodeFile 的 to-one 关系，导致无法通过 GraphQL 的 relation 查询获取关联文件信息。
- **风险**: 前端页面需要额外发起查询获取关联数据，增加请求次数和复杂度。
- **建议**: 在 ORM 模型中添加 `<to-one name="file" refEntityName="NopCodeFile" joinKey="fileId"/>` 关系定义。
- **误报排除**: 不是设计上有意解耦，实际使用场景需要频繁获取关联文件信息。
- **复核状态**: 未复核
````

### 维度文件质量检查清单

主 agent 在接收子 agent 输出后、保存文件前，必须检查以下各项。任何一项不通过，都必须要求子 agent 补充完整后再保存：

1. **每个发现条目 ≥ 8 行**：文件路径、行号范围、证据片段、严重程度、现状、风险、建议、误报排除缺一不可。
2. **证据片段存在**：每个发现必须有 3-10 行代码片段，或明确标注 `[证据片段待补充]`。
3. **深挖轮次不是关键词列表**：每一轮的每个发现都是完整的 `###` 级条目，不是一行关键词。
4. **复核结论引用了发现编号**：不是独立重写，而是引用上方已有的发现条目编号。
5. **最终保留项有文件路径和严重程度**：不是纯文字列表。
6. **零发现维度也需说明检查范围**：列出读过的关键文件和检查过什么，不能只写"零发现"。

---

## A. 架构与模块边界

### 维度 01：依赖图与模块边界

**子 Agent 提示词**：

```text
以下为"维度 01"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 01：依赖图与模块边界

目标：检查 Maven 模块间依赖是否存在真实的边界问题，例如 api→service 反向依赖、core→dao 反向依赖、web→dao 跨层依赖、循环依赖等。

必读文档：
- docs-for-ai/01-repo-map/module-groups.md
- docs-for-ai/01-repo-map/domain-module-pattern.md
- 目标模块的 pom.xml 及各子模块 pom.xml

标准分层依赖规则：
1. api 层不依赖任何业务实现层（只定义接口和 DTO）
2. dao 层只依赖 api 和 nop-persistence 框架
3. core 层只依赖 api 和框架核心（nop-kernel、nop-core-framework），不依赖 dao
4. service 层依赖 api + core + dao
5. web 层依赖 service（不直接依赖 dao）
6. app 层依赖 web + service，引入具体运行时框架（Quarkus/Spring）
7. codegen 依赖 model 和 nop-kernel 的代码生成工具
8. meta 依赖 dao（需要实体定义来生成 XMeta）
9. 不允许循环依赖（A→B→A）
10. 框架特定依赖（Quarkus/Spring）只出现在 app 模块

执行步骤：

1. 读取目标模块下所有子模块的 pom.xml，提取 dependencies 中所有 io.nop.* 引用。
2. 构建完整的内部依赖图，以文本形式绘制。
3. 对照上述分层规则逐条检查。
4. 对每个违规或可疑依赖，指出：
   - 哪个子模块的 pom.xml
   - 违规的依赖声明
   - 违反了哪条规则
   - 是否存在合理的例外理由
5. 检查是否存在循环依赖的迹象。
6. 检查是否有子模块缺少必要的依赖声明（编译通过但依赖传递导致隐性耦合）。
7. 检查生成产物（_*.xml、_gen/*.java）是否被手写代码正确引用。

输出格式：

对每个发现：
### [维度01] 简短标题
- **文件**: 模块路径/pom.xml
- **严重程度**: P0/P1/P2/P3
- **现状**: 一句话描述
- **风险**: 不修复的后果
- **建议**: 修复方向

最后输出：
1. 完整的依赖图（ASCII art 或文本）
2. 违规清单（按严重程度排序）
3. 合规的模块清单
4. 总结评估
```

---

### 维度 02：模块职责与文件边界

**子 Agent 提示词**：

```text
以下为"维度 02"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 02：模块职责与文件边界

目标：识别职责混合的超大文件、模块边界越权、生成代码与手写代码混放等问题。

必读文档：
- docs-for-ai/01-repo-map/domain-module-pattern.md
- docs-for-ai/02-core-guides/model-first-development.md
- 目标模块的目录结构

执行步骤：

1. 统计目标模块各子模块的 Java 文件数量和行数，识别超过 500 行的文件。
2. 检查每个子模块是否职责单一：
   - dao 模块是否只包含 ORM 实体、DAO 接口和 I*Biz 契约
   - service 模块是否只包含 BizModel、Processor、beans 配置
   - meta 模块是否只包含 XMeta 和 i18n 资源
   - web 模块是否只包含 view/page 资源和页面生成脚本
   - app 模块是否只包含启动类和配置
3. 检查是否存在手写代码误放在生成文件目录中（会被下次生成覆盖）。
4. 检查 `_` 前缀生成文件是否有手写修改痕迹。
5. 检查是否有本应属于另一层的功能被错误放置（如 DAO 逻辑出现在 service 层）。
6. 检查是否有超大类（>500 行）承担了过多职责。

输出格式：同标准发现格式。
```

---

### 维度 03：API 表面积与契约一致性

**子 Agent 提示词**：

```text
以下为"维度 03"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 03：API 表面积与契约一致性

目标：检查 BizModel 公开接口、GraphQL schema、I*Biz 接口是否收敛一致，是否存在多余的暴露或缺失的契约。

必读文档：
- docs-for-ai/02-core-guides/service-layer.md
- docs-for-ai/02-core-guides/api-and-graphql.md
- 目标模块的 xbiz、xmeta 文件

执行步骤：

1. 收集所有 @BizModel 注解的类，列出其公开方法（@BizQuery、@BizMutation、@BizLoader）。
2. 检查对应的 xmeta 是否覆盖了所有公开方法需要的字段。
3. 检查 I*Biz 接口是否包含了所有被外部调用的方法。
4. 检查是否存在方法参数或返回值使用了 Map<String, Object> 而非类型安全的结构。
5. 检查 GraphQL schema（由 xmeta 自动生成）是否与 BizModel 方法签名一致。
6. 检查是否存在已定义但从未使用的 @BizQuery/@BizMutation 方法（死 API）。
7. 检查 xmeta 中的字段权限控制（creatable/updatable/readable）是否合理。

注：
- BizModel 方法返回实体对象是平台标准模式，不算问题。
- 内部服务接口返回 core 层模型是合理的，不算问题。
- I*Biz 接口放在 dao 模块是代码生成的 CRUD 契约，不算问题。

输出格式：同标准发现格式。
```

---

## B. 模型与数据架构

### 维度 04：ORM 模型与实体设计

**子 Agent 提示词**：

```text
以下为"维度 04"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 04：ORM 模型与实体设计

目标：检查 ORM 模型定义的质量，包括实体建模、关系定义、域使用、displayName 本地化、主键设计等。

必读文档：
- docs-for-ai/02-core-guides/model-first-development.md
- .opencode/skills/nop-database-design/SKILL.md（数据库设计规范）
- 目标模块的 model/*.orm.xml

执行步骤：

1. 读取目标模块的 model/*.orm.xml（源模型，非生成的 _app.orm.xml）。
2. 检查每个实体的：
   - 主键设计是否规范（sid? varchar(32)? 自增?）
   - 字段类型是否使用了合适的域（domain）
   - 关系定义是否完整（to-one/to-many 是否与实际业务关系匹配）
   - displayName 是否已本地化（是否有对应的 i18n key）
   - 审计字段（createTime/updateTime/createdBy/updatedBy）是否配置正确
   - 字段命名是否遵循 snake_case 数据库命名规范
3. 检查是否有定义了完整表结构但从未被使用的实体。
4. 检查索引定义是否覆盖了常见查询场景。
5. 检查字典（dict）定义是否与实际枚举/常量一致。
6. 检查实体的 tagSet/status 字段是否有对应的字典定义。
7. 检查关系定义的级联行为是否正确（cascade delete 是否有误删风险）。

输出格式：同标准发现格式。
```

---

### 维度 05：生成管线完整性

**子 Agent 提示词**：

```text
以下为"维度 05"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 05：生成管线完整性

目标：检查从 model→codegen→dao→meta→service→web 的完整生成链路是否正确闭合，每一步的生成脚本和产物是否一致。

必读文档：
- docs-for-ai/01-repo-map/domain-module-pattern.md（真实生成链路节）
- docs-for-ai/02-core-guides/model-first-development.md
- 目标模块的 codegen 脚本

执行步骤：

1. 确认源模型文件（model/*.orm.xml）是否存在且格式正确。
2. 检查 codegen 子模块的生成脚本（postcompile/gen-orm.xgen）是否引用正确的源模型。
3. 检查 dao 子模块的生成产物（_app.orm.xml、Entity.java、I*Biz.java）是否与源模型一致。
4. 检查 meta 子模块的生成脚本（precompile/gen-meta.xgen、postcompile/gen-i18n.xgen）是否正确执行。
5. 检查 web 子模块的生成脚本（precompile/gen-page.xgen）是否正确生成 view/page 文件。
6. 检查 service 子模块的 xbiz 文件是否与 BizModel 方法对应。
7. 检查生成产物的时间戳是否比源模型更新（如果更旧，说明生成未执行或失败）。
8. 检查 pom.xml 中的 Maven 插件配置是否正确触发了各步骤的生成。

输出格式：同标准发现格式。
```

---

### 维度 06：Delta 定制合规性

**子 Agent 提示词**：

```text
以下为"维度 06"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 06：Delta 定制合规性

目标：检查 Delta 定制文件是否遵循正确的位置、语法和升级兼容性规则。

必读文档：
- docs-for-ai/02-core-guides/delta-customization.md
- 目标模块的 _vfs/_delta/ 目录

执行步骤：

1. 查找目标模块中所有 Delta 文件（src/main/resources/_vfs/_delta/）。
2. 检查每个 Delta 文件：
   - 是否使用了 `x:extends="super"`（必须）
   - 是否使用了正确的 `x:override` 属性（replace/merge/remove）
   - 文件路径是否对应原始路径
   - 是否在不必要的地方使用了 Delta（应直接修改源模型的手写场景）
3. 检查是否有 Delta 文件覆盖了不应该被覆盖的内容。
4. 检查 Delta 的 x:override="remove" 是否正确移除了目标节点。
5. 检查是否有 Delta 文件与原始文件产生冲突或循环继承。
6. 检查 Delta 中的新增字段是否需要 `tag="not-gen"` 标记。

输出格式：同标准发现格式。
```

---

## C. 服务层与业务逻辑

### 维度 07：BizModel 规范遵循

**子 Agent 提示词**：

```text
以下为"维度 07"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 07：BizModel 规范遵循

目标：检查 BizModel 的实现是否遵循平台规范，包括继承模式、注解使用、方法归属、参数规范等。

必读文档：
- docs-for-ai/02-core-guides/service-layer.md
- 目标模块的 BizModel 类

执行步骤：

1. 收集目标模块中所有 @BizModel 注解的类。
2. 检查每个 BizModel：
   - 是否有对应的 ORM 实体和 xmeta（不允许"伪 BizModel"）
   - 如果是标准实体服务，是否正确继承了 CrudBizModel<T>
   - 构造函数是否调用了 setEntityName()
   - 查询方法是否使用了 @BizQuery + QueryBean + doFindList/doFindPage
   - 修改方法是否使用了 @BizMutation + requireEntity + updateEntity/save/delete
   - 参数是否使用了 @Name（少量参数）或 @RequestBean（多参数）
   - 返回多字段数据是否使用了 @DataBean
   - 是否存在用 Map<String, Object> 代替类型安全结构的反模式
3. 检查 @BizLoader 的使用是否正确（数据加载器的注册和使用）。
4. 检查是否有 BizModel 方法应该属于另一个聚合根。
5. 检查 Processor 类的使用是否合理（复杂流程才需要）。
6. 检查是否有直接使用 DAO 而非通过 CrudBizModel 的安全 API 的场景。

注：
- BizModel 返回实体对象是平台标准模式，不算问题。
- BizModel 方法返回值不需要 DTO，xmeta + GraphQL selection 控制字段可见性。

输出格式：同标准发现格式。
```

---

### 维度 08：IoC 与 Bean 配置

**子 Agent 提示词**：

```text
以下为"维度 08"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 08：IoC 与 Bean 配置

目标：检查 IoC 注入方式和 beans.xml 配置是否正确。

必读文档：
- docs-for-ai/02-core-guides/ioc-and-config.md
- 目标模块的 beans.xml 文件

执行步骤：

1. 收集目标模块中所有 beans.xml 文件（包括生成和手写的）。
2. 检查是否有手写修改了 _service.beans.xml 等生成文件（不允许）。
3. 检查 app-service.beans.xml 等手写 beans 文件的语法是否正确。
4. 检查 Java 代码中的注入方式：
   - @Inject 字段是否为 protected 或 package-private（不允许 private）
   - @InjectValue 的语法是否正确（@cfg:key|default）
   - 是否有误用 Spring 注解（@Value、@Autowired）的情况
5. 检查 bean 的命名是否遵循 nop* 前缀约定（平台内置 bean）或避免 nop* 前缀（业务 bean）。
6. 检查模块是否通过 _module 文件正确注册。
7. 检查 beans.xml 中的 import 路径是否正确。
8. 检查是否有循环依赖的 bean 配置。

输出格式：同标准发现格式。
```

---

### 维度 09：错误处理与错误码

**子 Agent 提示词**：

```text
以下为"维度 09"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 09：错误处理与错误码

目标：检查错误处理是否遵循 NopException + ErrorCode 规范。

必读文档：
- docs-for-ai/02-core-guides/error-handling.md
- 目标模块的 ErrorCode 定义和异常使用

执行步骤：

1. 搜索目标模块中所有 throw 语句，检查是否使用了 NopException。
2. 检查是否有随手抛 RuntimeException("中文消息") 或 IllegalArgumentException 的反模式。
3. 检查 ErrorCode 的定义是否规范：
   - 错误码命名是否遵循 nop.err.{模块}.{错误描述} 格式
   - 是否使用 ErrorCode.define() 定义
   - 是否定义了参数常量（ARG_*）
4. 检查 NopException 的 .param() 是否正确传递了上下文参数。
5. 检查异常链是否被正确保留（catch 后 re-throw 是否传入 cause）。
6. 检查是否有吞掉异常（catch 后什么都不做）的情况。
7. 检查日志使用是否规范（SLF4J，不是 System.out/System.err）。
8. 检查是否有硬编码中文错误消息（应使用错误码 + i18n）。

输出格式：同标准发现格式。
```

---

## D. XLang 与模型层

### 维度 10：XDSL 与 XLang 正确性

**子 Agent 提示词**：

```text
以下为"维度 10"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 10：XDSL 与 XLang 正确性

目标：检查目标模块中所有 XDSL 文件（orm.xml、xmeta、xbiz、beans.xml、view.xml、page.yaml 等）的语法和语义正确性。

必读文档：
- docs-for-ai/02-core-guides/model-first-development.md
- docs-for-ai/02-core-guides/delta-customization.md
- docs-for-ai/04-reference/safe-api-reference.md
- 目标模块中所有 XDSL 文件

执行步骤：

1. 检查所有 XDSL 文件的 x:schema 引用是否指向正确的 xdef 定义文件。
2. 检查 x:extends="super" 的使用场景是否正确（Delta 文件必须用，非 Delta 文件不应误用）。
3. 检查 x:override 属性（replace/merge/remove）的语义是否符合预期：
   - replace：完全替换（常见于字段覆盖）
   - merge：合并子节点（常见于 beans.xml 导入）
   - remove：删除节点（常见于 Delta 定制裁剪）
4. 检查是否有 XDSL 文件缺少必要的命名空间声明。
5. 检查自定义 XDSL 扩展是否遵循了 XDef 模式定义。
6. 检查 beans.xml 中的 bean 定义是否与实际 Java 类路径一致。
7. 检查是否有 XDSL 文件引用了不存在的资源路径。
8. 检查 xbiz 文件中的方法声明是否与对应 BizModel Java 类的方法签名兼容。

输出格式：同标准发现格式。
```

---

### 维度 11：XMeta 与 BizModel 对齐

**子 Agent 提示词**：

```text
以下为"维度 11"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 11：XMeta 与 BizModel 对齐

目标：检查 xmeta 定义与 BizModel 方法实现之间的对齐关系，确保 GraphQL schema 的字段定义、字段权限和类型声明与实际行为一致。

这是 Nop 平台特有的关键审计维度。xmeta 定义了 GraphQL object type，BizModel 提供方法实现，二者必须保持一致。

必读文档：
- docs-for-ai/02-core-guides/service-layer.md
- docs-for-ai/02-core-guides/api-and-graphql.md（如果存在）
- 目标模块的 xmeta 文件和 BizModel 类

执行步骤：

1. 对每个 BizModel 类，找到对应的 xmeta 文件。
2. 检查 xmeta 中定义的字段是否覆盖了 BizModel 公开方法需要暴露的所有字段。
3. 检查 BizModel 的 @BizLoader 方法是否有对应的 xmeta prop 定义。
4. 检查 xmeta 中的字段类型是否与 BizModel 方法的实际返回类型兼容。
5. 检查 xmeta 的字段权限（creatable/updatable/readable）是否与 BizModel 方法的业务语义一致：
   - 只读字段不应标记为 creatable 或 updatable
   - @BizMutation 方法修改的字段应在 xmeta 中标记为 updatable
6. 检查 xmeta 中的 dict 定义是否与实际枚举/常量一致。
7. 检查 xmeta 中的 displayName 是否已本地化（是否有对应的 i18n key）。
8. 检查是否有 xmeta 定义了字段但 BizModel 中没有对应的获取路径（死字段）。
9. 检查是否有 BizModel 方法暴露了数据但 xmeta 中没有定义对应字段（未受控暴露）。

注：BizModel 返回实体对象是平台标准模式，GraphQL 引擎通过 xmeta + selection 控制字段可见性。但如果有方法返回了不应暴露的字段且 xmeta 未限制，这就是真实问题。

输出格式：同标准发现格式。
```

---

## E. 框架集成与运行时

### 维度 12：GraphQL 与 API 层

**子 Agent 提示词**：

```text
以下为"维度 12"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 12：GraphQL 与 API 层

目标：检查 GraphQL API 层的正确性，包括查询/变更映射、selection 机制、分页、排序和过滤。

必读文档：
- docs-for-ai/02-core-guides/service-layer.md
- docs-for-ai/02-core-guides/api-and-graphql.md（如果存在）
- docs-for-ai/04-reference/safe-api-reference.md
- 目标模块的 BizModel 类和 xmeta

执行步骤：

1. 检查 GraphQL 查询类型映射：@BizQuery 方法是否正确映射为 GraphQL query。
2. 检查 GraphQL 变更类型映射：@BizMutation 方法是否正确映射为 GraphQL mutation。
3. 检查分页查询是否正确使用 QueryBean + doFindPage/doFindList。
4. 检查 FieldSelectionBean 的使用是否正确（不应手动构建，应由框架注入）。
5. 检查是否有手动序列化/反序列化绕过 GraphQL selection 的情况。
6. 检查 RPC 路径（/r/）和 GraphQL 路径（/q/）的行为是否一致。
7. 检查 API 版本兼容性：是否有公开 API 的签名发生了破坏性变更。
8. 检查是否有硬编码的 SQL 或 HQL 绕过 ORM 层的情况。

输出格式：同标准发现格式。
```

---

### 维度 13：安全与权限模型

**子 Agent 提示词**：

```text
以下为"维度 13"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 13：安全与权限模型

目标：检查权限控制是否正确实现，包括方法级权限、数据权限、字段级权限。

必读文档：
- docs-for-ai/02-core-guides/（安全相关指南）
- 目标模块的 xmeta 权限配置
- 目标模块的 BizModel 权限注解

执行步骤：

1. 检查 BizModel 方法上的权限注解（如果有）是否正确配置。
2. 检查 xmeta 中的字段级权限（creatable/updatable/readable）是否合理。
3. 检查是否有敏感字段（密码、密钥等）未在 xmeta 中限制可见性。
4. 检查数据权限（DataAuth）的使用是否正确。
5. 检查是否有绕过权限检查的直接数据访问路径。
6. 检查 SQL 注入风险（是否有拼接 SQL 而非参数化的场景）。
7. 检查是否有未经验证的用户输入直接传入底层操作。

输出格式：同标准发现格式。
```

---

### 维度 14：异步与事务模式

**子 Agent 提示词**：

```text
以下为"维度 14"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 14：异步与事务模式

目标：检查事务边界是否正确，异步处理是否安全。

必读文档：
- docs-for-ai/02-core-guides/service-layer.md
- 目标模块中涉及事务和异步的代码

执行步骤：

1. 搜索 txn() 的使用，检查事务边界是否合理。
2. 检查 txn().afterCommit() 是否在事务上下文中调用（@BizQuery 等无事务场景不能直接用）。
3. 检查是否有长事务风险（事务中包含远程调用或重计算）。
4. 检查异步处理（如有）是否有正确的错误处理和重试机制。
5. 检查并发场景下是否有竞态条件（乐观锁、悲观锁是否正确使用）。
6. 检查是否有资源泄漏（未关闭的连接、流等）。

输出格式：同标准发现格式。
```

---

## F. 工程质量

### 维度 15：类型安全与泛型使用

**子 Agent 提示词**：

```text
以下为"维度 15"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 15：类型安全与泛型使用

目标：检查类型安全是否存在问题，泛型使用是否正确。

必读文档：
- docs-for-ai/02-core-guides/code-style.md
- 目标模块的 Java 代码

执行步骤：

1. 搜索原始类型使用（Raw Type），检查是否有遗漏泛型参数的情况。
2. 检查 @SuppressWarnings("unchecked") 的使用是否合理（平台代码可能有些是必要的）。
3. 检查接口契约的泛型精度（I*Biz<T> 的 T 是否正确指定）。
4. 检查是否有不必要的强制类型转换（instanceof + cast）。
5. 检查是否有 Object 类型参数或返回值可以收窄到更具体的类型。
6. 检查 DTO / RequestBean / DataBean 是否有合理的类型定义。
7. 检查泛型擦除是否导致了运行时问题。

注：对 Nop 平台的动态边界（XDSL 解析、动态 schema 等）保持克制，这些场景下 Object 或泛型通配符可能是合理的。

输出格式：同标准发现格式。
```

---

### 维度 16：测试覆盖与质量

**子 Agent 提示词**：

```text
以下为"维度 16"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 16：测试覆盖与质量

目标：检查测试是否真正约束了高风险行为，而不是只覆盖 happy path。

必读文档：
- docs-for-ai/02-core-guides/testing.md
- 目标模块的测试代码

执行步骤：

1. 收集目标模块的所有测试文件。
2. 评估测试覆盖的质量（不是数量）：
   - 是否覆盖了核心业务逻辑的边界条件
   - 是否覆盖了错误路径（不合法输入、并发、资源不可用等）
   - AutoTest 快照是否与当前代码一致
   - 是否存在"看起来很多测试但保护力很弱"的位置
3. 检查测试是否依赖了不应该依赖的实现细节。
4. 检查是否有核心 BizModel 方法缺少对应的测试。
5. 检查测试的 setup 是否过重（影响测试可维护性）。
6. 检查是否有测试使用了 mock 但 mock 的行为与真实实现不一致。
7. 检查是否有集成测试使用了正确的 NopAutoTest 基类。

注：AutoTest 的快照比对机制是 Nop 平台的标准测试模式，不需要对快照文件本身审计。

输出格式：同标准发现格式。
```

---

### 维度 17：代码风格与规范

**子 Agent 提示词**：

```text
以下为"维度 17"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 17：代码风格与规范

目标：检查代码风格是否遵循仓库规范。

必读文档：
- docs-for-ai/02-core-guides/code-style.md
- 根目录 checkstyle.xml（如果有）
- 目标模块的 Java 代码

执行步骤：

1. 检查命名规范：
   - 类名 PascalCase、方法名 camelCase、常量 UPPER_SNAKE_CASE
   - 接口名 I + PascalCase
   - 包名 io.nop.<module-name>.*
2. 检查 import 分组是否正确（java.* → jakarta.* → third-party → io.nop.*）。
3. 检查行宽和缩进（4 空格，行宽 80-120 字符）。
4. 检查是否有不必要的注释（AI 风格的过度注释、模板化的 Javadoc）。
5. 检查是否有 System.out/System.err 的使用。
6. 检查是否有未使用的 import。
7. 检查是否有不必要的 public 修饰（内部类、包内工具类等）。

注：此维度的发现默认最高 P2，除非风格问题掩盖了真实的逻辑错误。不要把纯粹的格式偏好报告为问题。

输出格式：同标准发现格式。
```

---

## G. 文档与一致性

### 维度 18：文档-代码一致性

**子 Agent 提示词**：

```text
以下为"维度 16"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 16：文档-代码一致性

目标：检查 docs-for-ai/ 中的文档与实际代码实现是否对齐。

必读文档：
- docs-for-ai/INDEX.md
- docs-for-ai/ 中与目标模块相关的所有文档
- ai-dev/design/ 中与目标模块相关的设计文档（如有）

执行步骤：

1. 读取 docs-for-ai/ 中与目标模块相关的所有文档。
2. 对文档中描述的每个功能点，验证实际代码是否存在且行为一致。
3. 检查文档中引用的文件路径是否正确（文件是否存在于描述的位置）。
4. 检查文档中描述的 API 签名是否与实际 BizModel 方法一致。
5. 检查 ai-dev/design/ 中的设计文档状态是否与实现状态一致（计划 vs 已完成）。
6. 检查文档中是否有过时的约束或规则（代码已改变但文档未更新）。
7. 检查 docs-for-ai/04-reference/source-anchors.md 中的锚点是否仍然有效。

输出格式：同标准发现格式。
```

---

### 维度 19：命名与术语一致性

**子 Agent 提示词**：

```text
以下为"维度 17"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 17：命名与术语一致性

目标：检查目标模块中是否存在命名不一致、术语偏离、双词汇等问题。

必读文档：
- docs-for-ai/02-core-guides/code-style.md
- 目标模块的 ORM 模型、BizModel、接口定义

执行步骤：

1. 检查实体名在 ORM、BizModel、接口、文档中是否一致。
2. 检查字段名在数据库列名（snake_case）、Java 属性名（camelCase）、GraphQL 字段名之间是否一致。
3. 检查是否有同一概念使用了不同名称的情况（如 status/state、type/kind）。
4. 检查错误码前缀是否与模块名一致。
5. 检查 bean 名称是否与类名有合理的对应关系。
6. 检查 xmeta 中的 displayName 是否与实体的 displayName 定义一致。
7. 检查是否有违反 Java 命名惯例的自定义命名模式。

输出格式：同标准发现格式。
```

---

### 维度 20：跨模块契约一致性

**子 Agent 提示词**：

```text
以下为"维度 18"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 18：跨模块契约一致性

目标：检查目标模块与外部模块之间的接口契约是否一致。

必读文档：
- docs-for-ai/01-repo-map/module-groups.md
- 目标模块的 api 子模块（如有）
- 调用目标模块的其他模块（如有）

执行步骤：

1. 检查目标模块对外暴露的接口（api 子模块或 I*Biz 接口）是否稳定。
2. 检查接口签名变更是否会导致调用方编译失败。
3. 检查 DTO 定义是否与接口签名匹配。
4. 检查事件（如有）的发布和订阅是否匹配。
5. 检查目标模块依赖的其他模块的接口版本是否兼容。
6. 检查配置项（yaml/properties）是否与代码中的 @InjectValue 一致。
7. 检查是否有跨模块的硬编码依赖（应通过接口或配置解耦）。

输出格式：同标准发现格式。
```

---

## H. 测试有效性

### 维度 21：单元测试有效性

**子 Agent 提示词**：

```text
以下为"维度 21"的维度正文。派发时必须与上文"共享提示词前缀"拼接。

审核维度 21：单元测试有效性

目标：检查单元测试是否真正具有捕获 bug 的能力，而不是只凑覆盖率数字。

**本维度使用独立审计提示词**：`ai-dev/skills/unit-test-antipatterns.md`。执行前必须先完整阅读该文件，其中的反模式清单（P-1 到 P-8）和优先级排序规则是本维度的审计基线。

必读文档：
- ai-dev/skills/unit-test-antipatterns.md（单元测试反模式清单 — 本维度的核心审计依据）
- docs-for-ai/02-core-guides/testing.md
- 目标模块的测试代码

执行步骤：

1. 阅读完整 `ai-dev/skills/unit-test-antipatterns.md`，理解 8 类反模式（P-1 到 P-8）。
2. 收集目标模块的所有测试文件。
3. 对每个测试文件逐方法检查：
   - 是否命中 P-1（纯 getter/setter 往返测试）
   - 是否命中 P-2（测试元数据属性而非行为）
   - 是否命中 P-3（只测 happy path）
   - 是否命中 P-4（测试与实现高度耦合）
   - 是否命中 P-5（过度使用 assertNotNull）
   - 是否命中 P-6（测试方法名不表达预期行为）
   - 是否命中 P-7（测试之间有隐式依赖）
   - 是否命中 P-8（无效的负面测试）
4. 对每个命中的反模式，用"核心逻辑改成错误实现后测试是否仍通过"来验证：
   - 如果测试仍通过 → 该测试无保护力，标记为 P1
   - 如果测试会失败但断言只是镜像常量 → 该测试与实现耦合，标记为 P2
5. 评估测试整体的优先级分配是否合理（核心算法是否有最多测试、数据模型是否被过度测试）。
6. 统计有效测试与低价值测试的大致比例。

严重程度判级补充：
- P1：测试无法捕获任何真实 bug（纯 getter/setter 测试、assertNotNull 遍历枚举）
- P2：测试保护力弱但存在边际价值（只测 happy path 但至少验证了基本集成）
- P3：测试命名或结构问题（方法名不表达意图、测试间有隐式依赖）

输出格式：同标准发现格式。每条发现必须引用命中的反模式编号（如"命中 P-1"）并给出具体证据。
```

---

## 附录 A：发现条目统一格式

每个发现条目必须遵循以下格式：

```markdown
### [维度{NN}-{序号}] 简短标题

- **文件**: `相对路径/文件名.java:行号范围`
- **证据片段**:
  ```java
  // 3-10 行代码片段
  ```
- **严重程度**: P0/P1/P2/P3
- **现状**: 一句话描述当前问题是什么
- **风险**: 不修复会怎样
- **建议**: 修复方向
- **误报排除**: 为什么这不是同类误报
- **复核状态**: 未复核 / 已保留 / 已降级 / 已驳回
```

---

## 附录 B：汇总报告格式

```markdown
# 深度审核汇总报告

## 基本信息

- **审核模块**: [目标模块名]
- **审核日期**: YYYY-MM-DD
- **执行维度**: [列出实际执行的维度编号和名称]
- **目标范围**: [描述审核的代码范围]

## 执行统计

| 维度 | 深挖轮次 | 初审发现数 | 追加发现数 | 保留 | 降级 | 驳回 |
|------|---------|-----------|-----------|------|------|------|
| 01   | 3       | 5         | 3         | 6    | 1    | 1    |
| ...  | ...     | ...       | ...       | ...  | ...  | ...  |

## 按严重程度分布

| 严重程度 | 数量 | 主要类别 |
|---------|------|---------|
| P0      | N    | ...     |
| P1      | N    | ...     |
| P2      | N    | ...     |
| P3      | N    | ...     |

## 关键发现摘要

### P0 发现
[列出所有 P0 发现的编号、文件、一句话摘要]

### P1 发现
[列出所有 P1 发现的编号、文件、一句话摘要]

## 总评

[1-3 段对目标模块整体质量的评估]

## 优先修复建议

[按优先级排列的修复建议]

## 本次审核盲区自评

[说明本次审核可能遗漏的方面]
```

---

## 附录 C：模块特定调优指南

不同类型的模块在审计时应有不同的侧重点。以下是常见模块类型的建议维度选择：

### 标准业务模块（如 nop-auth, nop-job）

优先维度：01, 04, 05, 07, 09, 11, 16

### 框架核心模块（如 nop-kernel, nop-core-framework）

优先维度：01, 03, 10, 15, 16, 18

### AI 子系统模块（如 nop-ai）

优先维度：01, 03, 07, 10, 11, 13, 16

### 集成模块（如 nop-spring, nop-quarkus）

优先维度：01, 08, 10, 12, 14, 18

### 测试基础设施模块（如 nop-autotest）

优先维度：01, 03, 10, 15, 16
