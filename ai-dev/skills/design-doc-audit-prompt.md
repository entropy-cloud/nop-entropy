# 设计文档审计提示

在将 `ai-dev/design/` 作为正式产品的稳定应用层行为基线进行审计时使用此提示。

在需求到设计更新后、依赖设计真相的实施前或 `ai-dev/design/` 在漂移后需要重新验证时使用。不要将其用作需求综合、架构审查、计划审计、结束审计或路线图规划的替代品。

```text
首先阅读这些文件：
- `AGENTS.md`
- `docs-for-ai/INDEX.md`
- `docs-for-ai/00-start-here/project-context.md`
- `ai-dev/design/README.md`（如果存在）
- 任务、计划、待办项或用户请求指定的相关需求文件
- `ai-dev/design/` 下的所有文件

仅在目标文档依赖它们时阅读：
- 当需求含义或源意图有争议时，`ai-dev/discussions/` 或需求输入下的相关文件
- 当设计文档引用实现策略或横切技术行为时，`ai-dev/architecture/`（如存在）下的相关文件
- 项目的持久化契约/模型真相源，仅用于验证所有权边界或检测 schema/契约漂移
- 同类系统对照资料（竞品调研、行业标杆分析等），仅用于维度 1（功能覆盖度）的外部基准对照

将 `ai-dev/design/` 作为支持产品行为的应用层 owner 文档进行审计。

不要首先审计措辞润色。优先考虑会导致错误实现、不清晰产品行为、演示级捷径、不良 owner-doc 边界、重复维护点或错误关闭的发现。

**重要**：维度 1（功能覆盖度）与维度 2-12（已有文档质量）性质不同。维度 2-12 审查"已写文档写得好不好"，维度 1 审查"该写的功能是否都写了"。两者都不可省略。

检查这些维度：

1. 功能覆盖度与领域完整性
   - 仅靠审查已有文档之间的内部一致性，无法发现"从未被设计的整个功能"。审计必须主动探测覆盖度：以同类成熟系统（竞品、行业标杆、同类产品）作为外部基准，核查基准系统中被视为标配的核心能力，在被审计的设计集合中是否都有对应的 owner doc 或显式的"产品基线外"声明。
   - 当需求文档处于早期或不完整状态时，不能仅用"设计对齐需求"作为完整性判据——需求本身可能存在缺口。
   - 缺失的功能按其对产品定位的必要性分级：核心标配缺失为 blocker，重要能力缺失为 major，长尾/可选能力缺失为 note。
   - 注意：覆盖度探测不要求穷举所有边缘功能；聚焦"该产品类型下被广泛视为标配的核心能力"。
   - **反模式**：仅凭"需求文档没写"就认为功能缺失不构成问题——需求本身可能存在缺口。必须用同类系统对照做完整性探测。

2. 产品基线
   - 文档描述正式产品行为，而非临时原型/演示行为。
   - 功能可以以小而完整的切片发布，但设计不得暗示一次性、仅模拟、部分质量或后续重写行为，除非明确限制于产品基线之外的开发/测试基础设施。
   - 支付、退款、权限、数据删除、集成和账户管理行为要么定义为正式产品行为，要么显式路由到需求澄清。

3. 稳定与时间敏感责任
   - 稳定设计文档回答产品行为是什么，而不是团队本周恰好要实现什么。
   - 实现顺序、待办状态、计划状态、当前阻塞、活动工作和路线图排序不会在设计文档中重复。
   - 如果需要路线图信息，它集中在待办/路线图所有者中。

4. 需求对齐
   - 设计声明与相关的实现就绪需求对齐。
   - 原始输入、原型行为、历史审计文本或聊天记忆不会覆盖综合需求。
   - 需求与设计之间的任何矛盾分类为：设计漂移、需求差距、有意基线变更或需要人工决策。
   - 设计不会静默添加或删除影响用户可见行为、数据/模型形状、API 行为、认证/权限行为或外部集成行为的支持行为。

5. Owner-doc 边界
   - `ai-dev/design/` 解释业务语义、角色、工作流、状态含义和支持的用户/管理员结果。
   - 它不重复表目录、逐字段 schema、字典/状态代码目录、API 契约、生成代码行为或平台特定实现细节。
   - 实现关注点（事务、锁定、缓存、调度机制、集成协议细节、模块连接）要么不存在，要么路由到 `ai-dev/architecture/`（如存在）。

6. 跨设计一致性
   - 概览文档、域设计文档和角色/权限文档在存在的内容、谁可以使用它以及哪个 owner doc 控制细节上达成一致。
   - 同一概念不会在多个文件中使用冲突的名称、生命周期状态、所有权或资格规则进行描述。

7. 域语言和有界上下文清晰度
   - 每个主要业务概念都有一个自然的所属域文档；相邻文档引用或汇总而不成为竞争所有者。
   - 面向业务的语言不会被表名、枚举代码、类名、框架机制或复制的原始输入术语取代。
   - 跨域流程描述为业务工作流，并在需要时路由到架构以获取编排细节。

8. 工作流和状态清晰度
   - 核心用户/管理员流程具有足够的业务级序列细节用于实施规划，而无需虚构行为。
   - 状态转换标识触发者、业务前提条件以及生成的状态或结果。
   - 终端、异常、退款/取消/重试、超时、回退和集成失败路径在影响产品行为时在业务级别指定。
   - 状态名称面向业务；存储代码或枚举目录由契约/模型工件拥有。

9. 角色、权限和受保护操作
   - 角色文档描述的业务权限与功能/域文档一致。
   - 仅限管理员、仅限用户、仅限超级管理员、系统启动和外部回调操作不模糊。
   - 敏感行为（支付、退款、数据删除、账户管理、权限管理）有明确的 owner-doc 基线或升级。

10. 页面和交互行为
    - 设计文档描述重要页面或交互结果，当这些结果是支持应用行为的一部分时。
    - 当遗漏会导致实施猜测时，在业务级别涵盖所需验证、资格、空/错误状态和用户可见反馈。
    - UI/原型细节不视为需求，除非相关需求或设计 owner doc 已接受它们。

11. 配置和操作语义
    - 面向业务的配置行为与技术调度、存储、部署或集成机制分离。
    - 仅在影响支持应用行为时定义操作默认值、回退和管理员可见控件。

12. 维护成本和重复
    - 设计事实有一个自然的 owner doc；其他文档仅在有助于导航时链接或汇总。
    - 重复的矩阵、功能状态列表和复制的规则块视为风险，除非它们故意是该信息的单一所有者。

严重性指南：
- `blocker`：可能导致错误实现、演示级行为、不安全权限/支付/退款行为、真相源违规，或该产品类型下被广泛视为标配的核心功能在设计中完全缺失。
- `major`：留下实质性歧义、跨文档冲突、边界漂移、高维护重复，或重要（非绝对标配但常见）功能在设计中缺失。
- `minor`：提高清晰度或导航，但不阻止实现。
- `note`：剩余风险、观察项或可选清理。

按严重性排序，首先返回发现。对于每个发现，包括：严重性、受影响文件和行（如果可用）、问题、重要性原因以及建议的处理方式。

然后返回：
- 裁决：通过/失败
- 审查范围
- **功能覆盖度结论**：对照外部基准识别出的功能缺口
- 需求/设计冲突分类摘要
- Owner 边界摘要
- 域边界摘要
- 维护成本摘要
- 剩余风险或跳过的区域

如果没有 blocker 或 major 发现，说 `Verdict: pass` 并仍然列出剩余风险。注意：功能覆盖度的 blocker/major（整个功能从未设计）即使已有文档质量完美，也应判 fail。
```
