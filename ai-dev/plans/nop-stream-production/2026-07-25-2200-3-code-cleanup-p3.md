# 代码清理与 P3 次要改进（G68, G64, P3）

> Plan Status: active
> Last Reviewed: 2026-07-25
> Source: `ai-dev/backlog/nop-stream-production-roadmap.md` Stage 23；`ai-dev/analysis/nop-stream/08-gap-analysis.md` G68/G64（G62 见 Decision）；`ai-dev/backlog/nop-stream-production-roadmap.md` Follow-up Backlog（CheckpointMetricsSnapshot / PartitionPolicy）；Plan `2026-07-25-0800-3` Non-Blocking Follow-ups（BarrierAligner removal → Stage 23）；Plan `2026-07-25-1500-1` Non-Blocking Follow-ups（synchronized 评估 → Stage 23）
> Mission: nop-stream-production
> Work Item: 23
> Related: Plan `2026-07-25-0800-3-multi-input-barrier-alignment`（BarrierAligner @Deprecated deferred removal → 本 plan）

## Outdated Note（draft review 修正）

> 初稿把 `TimerService` 列为死类、G62 列为接口引入、G64 描述为「fail-fast 决策点」。经独立 review 核对（`ses_066704b79ffeBFxFVroJmo9iA0`）：(1) `TimerService`（core）被 `ProcessOperator`/`StreamingRuntimeContext`/`ProcessFunction`/`RuntimeContext`/`CepOperator` 等 5+ 生产类使用（grep 确认），**非死类**——删除会 break main 编译，已移出 scope；(2) G62 引入无消费者的接口即空壳，降级为 Decision（仅记录设计决策，接口引入延后至有真实消费方的 successor）；(3) G64 的 call-site fail-fast **已存在**（`WindowedStreamImpl.java:180-181` 等 4 处抛 `StreamException`），G64 实际工作仅为消除 `:161-163` 空 catch（Rule #24 style violation），非行为变更。

## Purpose

清理 nop-stream 代码债务与 P3 次要改进，收口 G68（OperatorChain javadoc）、G64（反射工厂空 catch 消除），并清理多个跨 plan deferred 的代码卫生项（CheckpointMetricsSnapshot.toString、PartitionPolicy 死代码、BarrierAligner 删除、@Deprecated 死类清理）。G62（MergingState）经核对降级为 Decision。**仅清理与重构，无功能性变更。** 本 plan 在写 baseline 前已逐条 live 核对，剔除「已不存在」「前提错误」「非死类」的目标。

## Current Baseline

> 全部为 live repo 核对结果（explore `ses_066773b86ffemT065aeil7Lra3`；review `ses_066704b79ffeBFxFVroJmo9iA0` 复核）。

### VALID 清理目标

- **G68 OperatorChain.open() javadoc 与实现矛盾**（VALID）：`OperatorChain.java:93-94` javadoc 称「opened in forward order」，实现 `:102` `for (int i = operators.size() - 1; i >= 0; i--)` 为**逆序**。修 javadoc 不改代码。
- **G64 反射工厂空 catch（Rule #24 style violation）**（VALID，reframed）：`WindowedStreamImpl.java:149-167` `getFactory()` 在 `:161-163` 有**空 catch 块**（吞错）。**但 call-site fail-fast 已存在**：`:180-181`/`:194-196`/`:209-211`/`:224-226` 四处在 `factory==null` 时抛 `StreamException("...requires nop-stream-runtime on classpath...")`。故 G64 行为已 fail-fast，实际工作为消除空 catch（style/Rule #24），非行为变更。其余反射点 `WindowOperator.java:1899`（`getSimpleAccumulator`）**已 fail-fast**（`:1902-1904` 抛异常），无需改动。
- **CheckpointMetricsSnapshot.toString() 缺 failureCause**（VALID，roadmap Follow-up）：`CheckpointMetricsSnapshot.java:26` 字段 + `:75` getter 存在，但 `toString()`（`:79-90`）漏 `failureCause`。
- **PartitionPolicy UNION/SINGLETON 死代码**（VALID，roadmap Follow-up）：`PartitionPolicy.java:12-17`；`UNION`/`SINGLETON` grep 全模块（main+test）= 0 引用。
- **BarrierAligner 删除**（VALID，Plan 16 deferred）：`BarrierAligner.java:33` `@Deprecated`，生产调用者 = 0，仅 16 处 test 引用（`TestBarrierAligner*`）。`AlignedBarrier.java` 仅被 BarrierAligner 使用，可同删。**注意 `docs-for-ai/04-reference/source-anchors.md:215` STRM-033 引用 BarrierAligner 全路径，须同步更新**。
- **@Deprecated 死类清理**（VALID，已剔除非死类）：经核对零外部生产调用者的死类——`WindowOperatorTimerService`、`ProcessWindowAggregationFunction`、`WindowAggregationFunction`、`ReduceAggregationFunction`、`AggregateAggregationFunction`、`ApplyAggregationFunction`、`CoMapFunction`、`CoFlatMapFunction`（8 个）。**`TimerService`（core）被 `ProcessOperator:126`/`StreamingRuntimeContext:15`/`ProcessFunction:17`/`RuntimeContext:30`/`CepOperator:164` 等使用，非死类，移出 scope。** **`TestWindowAggregationFunction.java`（test）使用 `WindowAggregationFunction`，须连同 test 同删。**

### 已剔除目标（前提失效/已满足）

- **WindowOperator 空 else 块**（INVALID）：grep = 0，已不存在。
- **触发路径冗余 synchronized**（INVALID）：`StreamSourceOperator` 无 synchronized；`CheckpointBarrierTracker` 的 synchronized（`:60/:102/:183`）真跨线程仍需保留。
- **TODO/FIXME 标记**（INVALID）：grep 非 test 非 _gen source = 0，已清理。

## Goals

- G68：修正 OperatorChain.open() javadoc（与逆序实现对齐）。
- G64：消除 `WindowedStreamImpl.getFactory()` 空 catch（Rule #24），保持既有 call-site fail-fast 行为。
- 清理跨 plan deferred 代码卫生项：CheckpointMetricsSnapshot.toString 补 failureCause、PartitionPolicy 死代码删除、BarrierAligner + AlignedBarrier + 相关 test 删除、8 个 @Deprecated 死类（含相关 test）删除。
- G62：记录 MergingState 设计 Decision（不引入无消费方的空壳接口）。
- 全程无功能性变更（纯重构/清理），`./mvnw test` 全过。

## Non-Goals

- 任何功能性/行为变更（roadmap Stage 23 明示 Out of scope）。
- 引入无消费方的 MergingState 接口（空壳，见 Decision）——降级为 Decision，延后至有真实消费方的 successor。
- `TimerService` 删除（非死类，被 5+ 生产类使用）。
- synchronized 清理（前提失效）、空 else 块（已不存在）、TODO/FIXME（已清理）。
- 非 nop-stream 模块清理。

## Scope

### In Scope

- G68：OperatorChain.open() javadoc 修正。
- G64：`WindowedStreamImpl.getFactory()` 空 catch 消除（保持 fail-fast 行为）。
- CheckpointMetricsSnapshot.toString() 补 failureCause。
- PartitionPolicy UNION/SINGLETON 删除。
- BarrierAligner + AlignedBarrier + 其 test（`TestBarrierAligner*`）删除。
- 8 个 @Deprecated 死类 + `TestWindowAggregationFunction` 删除。
- G62 Decision：在 `state-management-design.md` 记录 MergingState 设计决策（不引入空壳接口）。
- `source-anchors.md` STRM-033 更新（BarrierAligner 删除后）。

### Out Of Scope

- TimerService 删除（非死类）、synchronized 清理（前提失效）、MergingState 接口引入（空壳风险）、功能性变更、非 nop-stream 模块。

## Execution Plan

### Phase 1 - 文档与死代码清理（低风险，无行为变更）

Status: planned
Targets: `OperatorChain.java:85-123`、`CheckpointMetricsSnapshot.java:79-90`、`PartitionPolicy.java:12-17`、`BarrierAligner.java`、`AlignedBarrier.java`、`TestBarrierAligner*.java`、8 个 @Deprecated 死类、`TestWindowAggregationFunction.java`、`docs-for-ai/04-reference/source-anchors.md`

- Item Types: `Fix | Follow-up`

- [ ] G68：`OperatorChain.open()` javadoc「forward order」改为「reverse order（tail→head）」，与 `:102` 实现一致。
- [ ] `CheckpointMetricsSnapshot.toString()` 补 `failureCause` 字段输出，新增断言 toString 结果含 failureCause 子串。
- [ ] `PartitionPolicy` 删除 `UNION`、`SINGLETON` 枚举值（零引用）。
- [ ] BarrierAligner 删除：移除 `BarrierAligner.java`、`AlignedBarrier.java`（仅 BarrierAligner 使用）及其 test（`TestBarrierAligner.java`、`TestBarrierAlignerStarvation.java`、`TestBarrierAlignerLoopComplete.java`）。
- [ ] @Deprecated 死类清理：`WindowOperatorTimerService`、`ProcessWindowAggregationFunction`、`WindowAggregationFunction`、`ReduceAggregationFunction`、`AggregateAggregationFunction`、`ApplyAggregationFunction`、`CoMapFunction`、`CoFlatMapFunction` + `TestWindowAggregationFunction.java`（使用 WindowAggregationFunction）。**每删一个前确认零引用，删除后 `./mvnw test-compile -pl nop-stream -am` 通过。**
- [ ] 删除 `WindowOperatorTimerService` 后清理残留 javadoc 文本引用：`HeapInternalTimerService.java:29` 含 `* The legacy {@code WindowOperatorTimerService} has been deprecated in favor of this class.`（`{@code}` 非 `{@link}`，不 break 编译但 grep 会命中）。同步更新该 javadoc（移除对已删除类的引用）。

Exit Criteria:

- [ ] `OperatorChain.open()` javadoc 描述逆序（repo-observable：`OperatorChain.java:93-94` 文本）。
- [ ] `CheckpointMetricsSnapshot.toString()` 输出含 `failureCause`（新增断言覆盖）。
- [ ] grep `PartitionPolicy.UNION|PartitionPolicy.SINGLETON` = 0。
- [ ] grep `BarrierAligner|AlignedBarrier` 跨 nop-stream = 0（类与 test 均删除）。
- [ ] grep 死类名（`WindowOperatorTimerService` 等 8 个）在 nop-stream **生产代码**（main）= 0 残留引用；`HeapInternalTimerService.java:29` javadoc 文本引用已清理；`./mvnw test-compile -pl nop-stream -am` 通过（**注意：用 test-compile 而非仅 compile，因含 test 类删除**）。
- [ ] **无静默跳过**：删除类前确认零引用，不得留悬空 import；编译失败立即停止修复而非注释绕过。
- [ ] **No end-to-end verification required: pure dead-code cleanup, no behavior change.**
- [ ] owner-doc 更新：`docs-for-ai/04-reference/source-anchors.md` STRM-033 条目移除（BarrierAligner 删除后无指向目标）；运行 `node ai-dev/tools/check-doc-links.mjs --strict`。
- [ ] `ai-dev/logs/` 对应日期条目已更新。

### Phase 2 - G64 空 catch 消除 + G62 Decision

Status: planned
Targets: `WindowedStreamImpl.java:149-167`、`ai-dev/design/nop-stream/state-management-design.md`

- Item Types: `Fix | Decision`

- [ ] G64：`WindowedStreamImpl.java:161-163` 空 catch 块消除。**注意 call-site fail-fast 已存在**（`:180-181` 等四处 `factory==null` 抛 `StreamException`）。决策：在 catch 块内记录日志 + 保持 defaultFactory=null（让既有 call-site fail-fast 生效），消除「空 catch 吞错」的 style 违规，**不改行为**。
- [ ] G62 Decision：在 `state-management-design.md` 记录 MergingState 设计决策——**不**在本 plan 引入无消费方的空壳接口。当前唯一 merge 调用点在 `WindowOperator.mergeWindowContents()`（`:1294`，`mergeFunction.apply` `:1333` / `accumulator.merge` `:1417`）。引入 MergingState 接口须同时迁移该调用点使其有真实消费方，属优化（非清理），延后至 successor。
- [ ] G62 roadmap 文本同步：更新 `ai-dev/backlog/nop-stream-production-roadmap.md` Stage 23 的 G62 条目，记录为 Decision-only resolution（对齐 `08-gap-analysis.md:135` 标注的「可选项」），避免 roadmap→plan 治理 drift。

Exit Criteria:

- [ ] `WindowedStreamImpl.java:161-163` 不再有空 catch 块（repo-observable：catch 块有日志或 rethrow；断言「runtime 不在 classpath」场景行为不变——仍由 call-site 抛 StreamException）。
- [ ] G64 测试构造：因 `./mvnw test -pl nop-stream -am` 会把 nop-stream-runtime 加入 test classpath（`getFactory()` 返回非 null，`factory==null` 分支不触发），须用明确方式构造该场景——推荐重构 `getFactory()` 使其可注入 forced-null（包级可见 setter 或测试子类），或用 surefire `<classpathDependenciesExcludes>` 排除 nop-stream-runtime。断言 `apply()` 抛 `StreamException`（验证 call-site fail-fast 未被破坏）。
- [ ] G62 Decision 已记录于 `state-management-design.md`（repo-observable：文档章节 + 延后理由 + successor 路径）。
- [ ] G62 roadmap Stage 23 条目已更新为 Decision-only。
- [ ] **无静默跳过**：catch 块非空（有日志或 rethrow）；G64 行为零变更（call-site fail-fast 断言仍通过）。
- [ ] **Anti-Hollow 豁免说明**：G62 本 plan 仅 Decision 无代码（不引入空壳接口）；G64 仅消除空 catch（行为不变，既有 fail-fast 保留）。
- [ ] **No end-to-end verification required: G64 行为零变更，G62 仅 Decision。**
- [ ] owner-doc 更新：`state-management-design.md` 记录 G62 Decision（状态类型抽象决策 + 延后理由）。
- [ ] `ai-dev/logs/` 对应日期条目已更新。

## Closure Gates

- [ ] G68/G64 收敛：javadoc 修正、空 catch 消除（行为不变）。
- [ ] 跨 plan deferred 代码卫生项清理：toString、PartitionPolicy 死代码、BarrierAligner + AlignedBarrier、8 个 @Deprecated 死类 + 相关 test。
- [ ] G62 Decision 已记录（非空壳接口）。
- [ ] 无功能性变更（纯重构/清理），`./mvnw test` 全过。
- [ ] 必要 focused verification（toString 断言、G64 call-site fail-fast 断言、`test-compile` 通过）已完成。
- [ ] 不存在被静默降级到 deferred 的 in-scope live defect。
- [ ] 受影响 owner docs（`source-anchors.md` STRM-033、`state-management-design.md` G62 Decision）已同步。
- [ ] 独立子 agent closure-audit 已完成并记录证据。
- [ ] **Anti-Hollow Check**：G64 call-site fail-fast 在运行时仍生效（非仅改注释）；无新增空壳接口。
- [ ] `./mvnw test -pl nop-stream -am -T 1C` 通过。
- [ ] checkstyle / 代码规范检查通过。

## Deferred But Adjudicated

### G62: MergingState 接口引入

- Classification: `optimization candidate`
- Why Not Blocking Closure: 经核对，引入无消费方的接口即空壳（Anti-Hollow）。唯一 merge 调用点在 `WindowOperator.mergeWindowContents()`，迁移它使接口有真实消费方属优化（非清理），须同时改 WindowOperator 内部 merge 逻辑。本 plan 记录 Decision 并延后。
- Successor Required: `yes`
- Successor Path: 未来 window/state 重构 plan（迁移 WindowOperator merge 调用点 + 引入 MergingState 接口）

### synchronized 触发路径清理

- Classification: `watch-only residual`
- Why Not Blocking Closure: 经核对前提失效——`StreamSourceOperator` 无 synchronized，`CheckpointBarrierTracker` 的 synchronized 真跨线程仍需保留。无清理对象。
- Successor Required: `no`

### TimerService 删除

- Classification: `watch-only residual`
- Why Not Blocking Closure: 经核对非死类（被 ProcessOperator/StreamingRuntimeContext/ProcessFunction/RuntimeContext/CepOperator 使用）。删除需先重构 ProcessOperator 的 InternalTimerServiceTimerWrapper 层，超出「纯清理」范围。
- Successor Required: `no`

## Non-Blocking Follow-ups

- 若后续 WindowOperator merge 逻辑重构，可消费 MergingState 接口（G62 successor）。
- 若 ProcessOperator timer wrapper 层重构，可重新评估 TimerService 去留。

## Closure

Status Note: <<完成时填写>>
Completed: <<YYYY-MM-DD>>

Closure Audit Evidence:

- Reviewer / Agent: <<独立子 agent>>
- Audit Session: <<session ID>>
- Evidence:
  - 每条 Exit Criterion 验证结果（PASS/FAIL + live code path / test name）
  - 每条 Closure Gate 验证结果
  - `node ai-dev/tools/check-plan-checklist.mjs <plan-file> --strict` 退出码 0
  - `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-stream --severity high` 退出码 0
  - Deferred 项分类检查：无 in-scope live defect 被降级

Follow-up:

- G62 MergingState 接口引入 → 未来 window/state 重构 plan
- synchronized 清理 → 无清理对象（前提失效）
- TimerService 去留 → ProcessOperator timer wrapper 重构后评估
