# 代码清理与 P3 次要改进（G68, G62, G64, P3）

> Plan Status: draft
> Last Reviewed: 2026-07-25
> Source: `ai-dev/backlog/nop-stream-production-roadmap.md` Stage 23；`ai-dev/analysis/nop-stream/08-gap-analysis.md` G68/G62/G64；`ai-dev/backlog/nop-stream-production-roadmap.md` Follow-up Backlog（CheckpointMetricsSnapshot / WindowOperator / OperatorChain / PartitionPolicy）；Plan `2026-07-25-0800-3` Non-Blocking Follow-ups（BarrierAligner removal → Stage 23）；Plan `2026-07-25-1500-1` Non-Blocking Follow-ups（synchronized 清理评估 → Stage 23）
> Mission: nop-stream-production
> Work Item: 23
> Related: Plan `2026-07-25-0800-3-multi-input-barrier-alignment`（BarrierAligner @Deprecated，deferred removal → 本 plan）；Plan `2026-07-25-1500-1-mailbox-execution-model`（synchronized 评估 → 本 plan）

## Purpose

清理 nop-stream 的代码债务与 P3 次要改进项，收口 G68（OperatorChain javadoc）、G62（MergingState 中间接口）、G64（反射工厂加载优化），并清理多个跨 plan deferred 的代码卫生项（CheckpointMetricsSnapshot.toString、PartitionPolicy 死代码、BarrierAligner 删除）。**仅清理与重构，无功能性变更。** 本 plan 在写 baseline 前已逐条 live 核对，剔除「已不存在」或「前提错误」的目标。

## Current Baseline

> 全部为 live repo 核对结果（explore subagent `ses_066773b86ffemT065aeil7Lra3`）。逐条标注 target 有效性。

### VALID 清理目标

- **G68 OperatorChain.open() javadoc 与实现矛盾**（VALID）：`OperatorChain.java:93-94` javadoc 称「opened in forward order」，但实现 `OperatorChain.java:102` `for (int i = operators.size() - 1; i >= 0; i--)` 为**逆序**。cleanup 也逆序（line 113）。修 javadoc，不改代码。
- **G62 MergingState 中间接口缺失**（VALID）：grep `MergingState` = 0。`AggregatingState`（7 行）、`ReducingState`（7 行）无 merge 方法；merge 逻辑内联于 `WindowOperator.mergeWindowContents()`（`WindowOperator.java:1294-1438`）+ `InternalAppendingState` 的 get/set accumulator。
- **G64 反射工厂加载优化**（VALID）：3 处反射实例化。优先 `WindowedStreamImpl.java:157-163`——`Class.forName("...WindowOperatorFactoryImpl")` + `newInstance()`，**空 catch 块**吞错（line 161-163，注释「nop-stream-runtime not on classpath」）。其余：`WindowOperatorFactoryImpl.java:139`、`WindowOperator.java:1899`。
- **CheckpointMetricsSnapshot.toString() 缺 failureCause**（VALID，roadmap Follow-up）：`CheckpointMetricsSnapshot.java:26` 字段存在、getter 存在（line 75），但 `toString()`（line 79-90）**漏 failureCause**。
- **PartitionPolicy UNION/SINGLETON 死代码**（VALID，roadmap Follow-up）：`PartitionPolicy.java:12-17` 6 枚举值；`UNION`/`SINGLETON` grep 全模块（main+test）= 0 引用。
- **BarrierAligner 删除**（VALID，Plan 16 deferred）：`BarrierAligner.java:33` `@Deprecated`，生产调用者 = 0，仅 16 处 test 引用（`TestBarrierAligner*`）。Plan 16 明确「Consider removing BarrierAligner entirely in Stage 23」。
- **@Deprecated 死类清理**（VALID）：10 个 `@Deprecated` 类零外部生产调用者（含 `WindowOperatorTimerService`、`TimerService`、`ProcessWindowAggregationFunction`、`WindowAggregationFunction`、`ReduceAggregationFunction`、`AggregateAggregationFunction`、`ApplyAggregationFunction`、`CoMapFunction`、`CoFlatMapFunction`）。

### 已剔除目标（前提失效，不进入 scope）

- **WindowOperator 空 else 块**（INVALID）：grep `else\s*\{\s*\}` 全窗口类 = 0 命中。已不存在。
- **触发路径冗余 synchronized**（INVALID）：`StreamSourceOperator.java` 无 `synchronized`（mailbox 已接管）；`CheckpointBarrierTracker` 的 `synchronized`（line 60/102/183）是真跨线程（trigger 在 task 线程外、acknowledgeOperator 在 task 线程内），**仍需保留**。Plan 17 的 follow-up 经核对不适用。
- **TODO/FIXME 标记**（INVALID）：grep `// TODO|// FIXME|XXX|HACK` 非 test 非 _gen source = 0。已清理。

## Goals

- G68：修正 OperatorChain.open() javadoc（与逆序实现对齐）。
- G62：引入 MergingState 中间接口，使 merge 能力在状态类型层面显式化（不迁移全部逻辑，仅建立抽象）。
- G64：优化反射工厂加载（消除空 catch 吞错，fail-fast 或显式 fallback）。
- 清理跨 plan deferred 代码卫生项：CheckpointMetricsSnapshot.toString 补 failureCause、PartitionPolicy 死代码删除、BarrierAligner 删除、@Deprecated 死类清理。
- 全程无功能性变更（纯重构/清理），`./mvnw test` 全过。

## Non-Goals

- 任何功能性/行为变更（roadmap Stage 23 明示 Out of scope: 功能性变更）。
- 把 WindowOperator merge 逻辑全部迁移到新 MergingState 接口（本 plan 仅建立抽象，迁移是后续优化）。
- BarrierAligner 的 reference 用途恢复（生产已 `@Deprecated` 无调用者，直接删除含其 test）。
- `synchronized` 清理（经核对前提失效，synchronized 仍需保留）。

## Scope

### In Scope

- G68：OperatorChain.open() javadoc 修正。
- G62：MergingState 中间接口引入（抽象层，不强制迁移全部 merge 调用点）。
- G64：反射工厂加载优化（WindowedStreamImpl 空 catch → fail-fast/显式 fallback）。
- CheckpointMetricsSnapshot.toString() 补 failureCause。
- PartitionPolicy UNION/SINGLETON 删除。
- BarrierAligner 删除（含其 test 类）。
- @Deprecated 死类清理（10 个，零外部调用者）。

### Out Of Scope

- 功能性变更、synchronized 清理（前提失效）、空 else 块清理（已不存在）、TODO/FIXME（已清理）。
- WindowOperator merge 逻辑完整迁移到 MergingState。
- 非 nop-stream 模块清理。

## Execution Plan

### Phase 1 - 文档与死代码清理（低风险，无行为变更）

Status: planned
Targets: `OperatorChain.java:85-97`、`CheckpointMetricsSnapshot.java:79-90`、`PartitionPolicy.java:12-17`、`BarrierAligner.java`、`TestBarrierAligner*.java`、10 个 @Deprecated 死类

- Item Types: `Fix | Follow-up`

- [ ] G68：`OperatorChain.open()` javadoc「forward order」改为「reverse order（tail→head）」，与 `OperatorChain.java:102` 实现一致。
- [ ] `CheckpointMetricsSnapshot.toString()` 补 `failureCause` 字段输出。
- [ ] `PartitionPolicy` 删除 `UNION`、`SINGLETON` 枚举值（零引用）。
- [ ] BarrierAligner 删除：移除 `BarrierAligner.java`、`AlignedBarrier.java`（若仅 BarrierAligner 使用）及其 test 类（`TestBarrierAligner.java`、`TestBarrierAlignerStarvation.java`、`TestBarrierAlignerLoopComplete.java`）。
- [ ] @Deprecated 死类清理：逐个确认零外部生产调用者后删除（`WindowOperatorTimerService`、`TimerService`、`ProcessWindowAggregationFunction`、`WindowAggregationFunction`、`ReduceAggregationFunction`、`AggregateAggregationFunction`、`ApplyAggregationFunction`、`CoMapFunction`、`CoFlatMapFunction`）。**每删一个前确认无残留引用，删除后 `./mvnw compile` 通过。**

Exit Criteria:

- [ ] `OperatorChain.open()` javadoc 描述逆序（repo-observable：`OperatorChain.java:93-94` 文本）。
- [ ] `CheckpointMetricsSnapshot.toString()` 输出含 `failureCause`（断言 toString 结果含 failureCause 子串）。
- [ ] grep `PartitionPolicy.UNION|PartitionPolicy.SINGLETON` = 0（已删除）。
- [ ] grep `BarrierAligner` 跨 nop-stream = 0（类与 test 均删除）。
- [ ] 删除的 @Deprecated 死类 grep = 0 残留引用，`./mvnw compile -pl nop-stream -am` 通过。
- [ ] **无静默跳过**：删除类前确认零引用，不得留下悬空 import；编译失败立即停止修复而非注释绕过。
- [ ] No new test required: 纯死代码删除与 javadoc/toString 修正，原有测试覆盖足够（toString 新增字段补一条断言即可）。
- [ ] `ai-dev/logs/` 对应日期条目已更新。

### Phase 2 - G62 MergingState 中间接口 + G64 反射工厂优化

Status: planned
Targets: 新增 `MergingState` 接口、`AggregatingState`/`ReducingState`、`WindowedStreamImpl.java:157-163`、`WindowOperatorFactoryImpl.java:139`、`WindowOperator.java:1899`

- Item Types: `Fix | Decision`

- [ ] G62：引入 `MergingState<IN,OUT,S extends State>` 中间接口（声明 merge 相关契约方法），`AggregatingState`/`ReducingState`（如适用）声明继承/实现该接口。决策点：接口方法集——参考 Flink `MergingState`（mergeNamespaces 语义），但适配 nop-stream 的 `InternalAppendingState` get/set accumulator 模式。**不强制迁移 WindowOperator 全部 merge 调用点**（仅建立抽象层）。
- [ ] G64：`WindowedStreamImpl.java:157-163` 空 catch 块改为显式行为：要么 fail-fast（抛 `NopException` 明确「nop-stream-runtime 不在 classpath」），要么显式 fallback 带日志（不得静默吞错）。决策点：选择 fail-fast（符合 Minimum Rules #24）。
- [ ] `WindowOperatorFactoryImpl.java:139`、`WindowOperator.java:1899` 反射点审查：已有失败处理则保留，无则补 fail-fast。

Exit Criteria:

- [ ] `MergingState` 接口存在于 nop-stream-core 状态包（repo-observable：接口文件 + `AggregatingState`/`ReducingState` 声明）。
- [ ] `WindowedStreamImpl.java:161-163` 不再有空 catch 块（repo-observable：catch 块抛异常或显式 fallback；断言「runtime 不在 classpath」场景抛异常而非静默返回 null）。
- [ ] G62 决策点（接口方法集）已裁定并记录于 `ai-dev/design/nop-stream/state-management-design.md`（状态类型抽象决策）。
- [ ] **无静默跳过**：反射失败路径抛异常或返回错误，无空 catch。
- [ ] **接线验证**：`MergingState` 接口被 `AggregatingState`/`ReducingState` 实际声明（非仅定义无人用）。
- [ ] 新增功能测试：`MergingState` 接口的实现类有对应测试（Test-Mandated Feature Rule）；空 catch 修复有断言覆盖（runtime 缺失抛异常）。
- [ ] owner-doc 更新：`state-management-design.md` 记录 MergingState 抽象决策（Phase 2 改变状态类型抽象 baseline）。
- [ ] `ai-dev/logs/` 对应日期条目已更新。

## Closure Gates

- [ ] G68/G62/G64 收敛：javadoc 修正、MergingState 抽象建立、反射空 catch 消除。
- [ ] 跨 plan deferred 代码卫生项清理：toString、PartitionPolicy 死代码、BarrierAligner、@Deprecated 死类。
- [ ] 无功能性变更（纯重构/清理），`./mvnw test` 全过。
- [ ] 必要 focused verification（toString 断言、MergingState 测试、空 catch fail-fast 断言）已完成。
- [ ] 不存在被静默降级到 deferred 的 in-scope live defect。
- [ ] 受影响 owner docs（`state-management-design.md`）已同步。
- [ ] 独立子 agent closure-audit 已完成并记录证据。
- [ ] **Anti-Hollow Check**：MergingState 接口被实际声明（非空壳）；反射 fail-fast 路径实际抛异常（非仅改注释）。
- [ ] `./mvnw test -pl nop-stream -am -T 1C` 通过。
- [ ] checkstyle / 代码规范检查通过。

## Deferred But Adjudicated

### WindowOperator merge 逻辑完整迁移到 MergingState

- Classification: `optimization candidate`
- Why Not Blocking Closure: 本 plan 仅建立 MergingState 抽象层。把 `WindowOperator.mergeWindowContents()` 全部 merge 调用迁移到接口方法属更大重构，且当前 merge 逻辑正确（G1 已修复），无 live defect。
- Successor Required: `no`

### synchronized 触发路径清理

- Classification: `watch-only residual`
- Why Not Blocking Closure: 经核对前提失效——`StreamSourceOperator` 无 synchronized，`CheckpointBarrierTracker` 的 synchronized 真跨线程仍需保留。无清理对象。
- Successor Required: `no`

## Non-Blocking Follow-ups

- 若后续 WindowOperator merge 逻辑重构，可消费 MergingState 接口。
- 若 `@Deprecated` 死类删除后外部下游模块报缺失（理论上零调用者），作为兼容性 follow-up。

## Closure

Status Note: <<完成时填写>>
Completed: <<YYYY-MM-DD>>

Closure Audit Evidence:

- Reviewer / Agent: <<独立子 agent>>
- Audit Session: <<session ID>>
- Evidence:
  - 每条 Exit Criterion 验证结果（PASS/FAIL + live code path / test name）
  - 每条 Closure Gate 验证结果
  - `node ai-dev/tools/check-plan-checklist.mjs <plan-file> --strict` 退出码 0
  - `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-stream --severity high` 退出码 0
  - Deferred 项分类检查：无 in-scope live defect 被降级

Follow-up:

- WindowOperator merge 迁移 → 后续优化（无 successor plan 必需）
- synchronized 清理 → 无清理对象（前提失效）
