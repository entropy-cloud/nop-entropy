# 32 — State TTL

> Plan Status: active
> Last Reviewed: 2026-08-02
> Source: `ai-dev/backlog/nop-stream-production-roadmap.md` Stage 32; G42, G43
> Related: Stage 30 (`2026-08-02-0955-1-rocksdb-state-backend`)

## Purpose

为 keyed state 引入生存时间（TTL）与过期清理。状态条目在指定 TTL 后自动失效，通过 lazy eviction（访问时检查）+ RocksDB compaction filter（后台批量清理）实现。解决 `state-management-design.md` §11 列出的"无 TTL/无驱逐"限制。

**Phase 1-2（Memory TTL）可在 Stage 30 之前独立执行。Phase 3（RocksDB TTL + compaction filter）blocked on Stage 30。**

## Current Baseline

- `StateDescriptor`（`nop-stream-core/.../state/StateDescriptor.java`）携带 `name`/`valueType`/`defaultValue`/`serializer`——**无 TTL 配置项**；是 plain `Serializable`（非 `@DataBean`，无 `equals/hashCode`），添加字段在 Java 序列化层面向后兼容
- 所有 keyed state 实现（memory）**无任何过期逻辑**——状态只增长不收缩
- `MemoryStateSerDe`（749 行）是 checkpoint 快照的 normative spec：8 种 stateType 分支（`snapshotValueState`/`snapshotMapState`/`snapshotAppendingState`/`snapshotListState`/`snapshotReducingState`/`snapshotAggregatingState`/`snapshotInternalAggregatingState`/`snapshotListStateFromPublic`），**每个方法内部直接序列化存储 value**。`MemoryKeyedStateBackend.snapshotState()`（`:310`）仅委托 `new MemoryStateSerDe(this).snapshotState(states)`——**entry 遍历逻辑在 SerDe 的 per-type 方法内，不在 backend 层**
- `MemoryStateSerDe.restore*` 方法从 type names 重建 `StateDescriptor`（如 `restoreValueState` 构造 `new ValueStateDescriptor<>(stateName, valueClass)`）——**不携带 TTL config**。`getState()` 对已恢复 state 仅调用 `verifySchemaCompatibility`，不替换 descriptor
- **存储表示因 state 类型而异**：`MemoryReducingState` 存储 `SimpleAccumulator<T>` 对象；`MemoryInternalAppendingState` 存储 raw ACC 值；`MemoryValueState` 存储 `T`；`MemoryMapState` 存储 `Map<UK,UV>`——统一 `TtlValue<T>` 包装不可行
- `StateSchemaResolver.fromDescriptor` 只读 `stateType`/`valueType`/`mapKeyType`/`accumulatorType`/`aggregateFunctionType`——TTL 自然不进入 schema 签名（无需代码改动保证）
- `state-management-design.md` §11.1 列出"无内存控制——状态只增长不收缩... 无 TTL/驱逐/spill"为已知限制
- G42（P2）: 缺少 `StateTtlConfig`；G43（P2）: 缺少 TTL 装饰器 + compaction filter

## Goals

- `StateTtlConfig`：TTL 时长、更新类型（OnCreateAndWrite / OnReadAndWrite）、清理策略
- `StateDescriptor` 可选携带 `StateTtlConfig`，且 TTL config 在 checkpoint restore 后保持活跃
- Memory 后端：lazy eviction（访问时检查 sidecar 时间戳，过期返回默认值/空）
- RocksDB 后端（Stage 30 前置）：lazy eviction + compaction filter（后台批量清理）
- TTL 过期的 entry 在 checkpoint snapshot 时不被包含

## Non-Goals

- RocksDB backend 本身（Stage 30 前置——Phase 3 only）
- Operator state 的 TTL（仅 keyed state）
- Event-time / watermark-based TTL（首版仅 processing-time）
- CEP SharedBuffer 的 TTL（CEP 有自己的过期逻辑）

## Scope

### In Scope

- `StateTtlConfig` + `StateTtlUpdateType` 枚举 + `TtlCleanupStrategy`
- `StateDescriptor.setTtlConfig(StateTtlConfig)` 可选配置
- TTL 上下文（sidecar 时间戳追踪）：`Map<TypedNamespaceAndKey, Long> lastAccessTimestamps` 由 backend 维护，**与存储 value 分离**（兼容 accumulator-based states）
- Memory 后端 TTL lazy eviction + snapshot 过期排除
- RocksDB 后端 TTL（Phase 3，blocked on Stage 30）：lazy eviction + `AbstractCompactionFilter`
- `state-management-design.md` 更新

### Out Of Scope

- Event-time TTL（需 watermark 集成）
- Per-map-entry TTL（首版 MapState TTL 为 per-key+namespace 粒度——整 map 作为单元过期；per-UK-entry 为后续优化）

### Design Decisions

- **时间戳追踪方式**：**per-state sidecar map**（每个 state 持有自己的 `Map<TypedNamespaceAndKey, Long> lastAccessTimestamps`），不修改存储 value 结构。Rationale：(1) accumulator-based states（Reducing/Aggregating/Appending）存储 `SimpleAccumulator`/raw ACC 值，包装为 `TtlValue<T>` 会破坏 `(ACC) current` 类型转换和 fusion 逻辑；(2) 不同 state 可有不同 TTL duration，全局 sidecar 会因 `TypedNamespaceAndKey`（不含 state name）在不同 state 间碰撞
- **TTL 拦截机制**：**intrusive modification**——每个 state 类（`MemoryValueState`/`MemoryMapState`/etc.）增加可选 `TtlContext` 字段。TTL 启用时，read 方法（`value()`/`get()` 等）先检查 sidecar 时间戳，过期则返回默认值 + **同时删除存储 entry**（`storage.remove(key)`，不只删 sidecar）；write 方法更新 sidecar 时间戳。**不使用 wrapper/decorator pattern**——`MemoryStateSerDe.snapshotState()` 使用 `instanceof` 分发（`:77-96`），wrapper 会破坏分发逻辑。TTL 上下文绑定后，SerDe 的 `instanceof` 分发不受影响（state 类类型不变，TTL 是内部行为）
- **TTL 粒度**：per-key+namespace（一个 `TypedNamespaceAndKey` 一个时间戳）。MapState 的整 map 作为单元过期（per-UK-entry 为后续优化）
- **TTL config 持久化**：`StateTtlConfig` **不通过 `MemoryStateSerDe` 持久化**（它是运行时行为，非 schema 契约）。restore 后，`getState()` 用 **live descriptor**（用户代码中携带 TTL config 的 descriptor）注册 state——即用户代码在 restore 后重新调用 `getState(descriptor)` 时传入带 TTL 的 descriptor。**关键不变量**：`getState()` 在 state 已存在（restored）时，需检查 live descriptor 是否携带 TTL config，若是则将 TTL 上下文绑定到该 state（当前 `getState()` 仅 `verifySchemaCompatibility`，需增加 TTL rebind 逻辑）
- **清理策略**：lazy eviction（所有后端，访问时检查）+ RocksDB compaction filter（后台批量清理，RocksDB 专属）；snapshot 时跳过已过期 entry
- **Processing-time only**：首版不实现 event-time TTL

## Execution Plan

### Phase 1 — StateTtlConfig + StateDescriptor Integration

Status: planned
Targets: `nop-stream-core/.../state/StateTtlConfig.java`, `nop-stream-core/.../state/StateDescriptor.java`

- Item Types: `Proof`

- [ ] `StateTtlConfig`（`Serializable`）：`ttl`（`Duration`）、`updateType`（`StateTtlUpdateType`：`Disabled`/`OnCreateAndWrite`/`OnReadAndWrite`）、`cleanupStrategies`（lazy eviction 默认启用；compaction filter 可选）
- [ ] `StateTtlUpdateType` 枚举
- [ ] `TtlCleanupStrategy`（lazy eviction + compaction filter flags）
- [ ] `StateDescriptor` 增加可选 `StateTtlConfig ttlConfig` 字段 + getter/setter（默认 `StateTtlConfig.DISABLED`）
- [ ] 验证（Proof，非代码改动）：确认 `StateSchemaResolver.fromDescriptor` 自然不包含 TTL（只读 type 字段）——schemaChecksum 不受 TTL config 影响

Exit Criteria:

- [ ] `StateTtlConfig` 可构造并 Java 序列化 round-trip
- [ ] `StateDescriptor` 携带 TTL config 不影响 `schemaChecksum`（验证同一 state 结构有无 TTL 产出相同 checksum）
- [ ] `StateTtlConfig.DISABLED` 作为默认值，不影响现有无 TTL 的 state 行为
- [ ] **Test-Mandated Feature Rule**：`TestStateTtlConfig` 验证构造 + 序列化
- [ ] `./mvnw test -pl nop-stream-core -am` 通过
- [ ] No owner-doc update required (config infrastructure; doc update in Phase 3)

### Phase 2 — Memory Backend TTL (Sidecar Timestamps + Lazy Eviction + Snapshot Exclusion)

Status: planned
Targets: `nop-stream-core/.../state/backend/memory/MemoryKeyedStateBackend.java`, `nop-stream-core/.../state/backend/memory/MemoryStateSerDe.java`, `nop-stream-core/.../state/backend/memory/Memory*State.java`

- Item Types: `Proof`

- [ ] `TtlContext`：per-state sidecar `Map<TypedNamespaceAndKey, Long> lastAccessTimestamps` + `StateTtlConfig` 引用。仅在 state descriptor 的 TTL 非 disabled 时激活
- [ ] 每个 state 类（`MemoryValueState`/`MemoryMapState`/`MemoryListState`/`MemoryReducingState`/`MemoryAggregatingState`/`MemoryInternalAppendingState`/`MemoryInternalAggregatingState`/`MemoryInternalListState`）增加可选 `TtlContext` 字段
- [ ] TTL 拦截（intrusive）：read 方法先检查 `now - lastAccessTimestamps.get(key) > ttl` → 返回默认值/空 + **同时删除存储 entry**（`storage.remove(key)`）+ 从 sidecar 移除时间戳（**双重清理**，避免 stale data 泄漏）；write 方法更新时间戳
- [ ] `MemoryKeyedStateBackend.getState(descriptor)` 统一 TTL 绑定：在现有 lazy-create-or-verify 模式中增加 TTL 逻辑（集中化，避免 8 处重复）：descriptor 携带非 disabled TTL → 创建/绑定 `TtlContext` 到 state 对象
- [ ] **TTL rebind on restore**：`getState(descriptor)` 在 state 已存在（restored）时，除 `verifySchemaCompatibility` 外，检查 live descriptor 的 TTL config → 绑定 `TtlContext`（sidecar 初始为空，restored entries 在首次访问时视为 "now" 时间戳——按 `OnCreateAndWrite` 语义给予新 TTL 窗口）
- [ ] **snapshot 过期排除**：修改 `MemoryStateSerDe` 的 8 个 per-type snapshot 方法（`snapshotValueState`/`snapshotMapState`/etc.），遍历 entries 时咨询 backend 的 TTL 上下文跳过已过期 entry。通过传入 backend 的 TTL 判定 predicate 实现（不修改 SerDe 的格式契约，仅过滤遍历结果）
- [ ] `clear()` 不受 TTL 影响

Exit Criteria:

- [ ] `TestMemoryStateTtl`：写入 entry → 等待超过 TTL → `value()`/`get()` 返回默认值/空（lazy eviction 生效，存储 entry 也被删除）
- [ ] **Write-after-expiry 无 stale data**：写入 → 过 TTL → 再写入新 entry → 读取无旧 entry 残留（lazy eviction 同时清理 storage + sidecar，验证无混合数据）
- [ ] `OnCreateAndWrite`：仅写更新时间戳；`OnReadAndWrite`：读也刷新时间戳
- [ ] TTL=disabled 时行为与无 TTL 完全一致（无性能退化）——现有测试全部通过
- [ ] **TTL restore 存活**：snapshot → restore → `getState(liveDescriptorWithTtl)` → TTL 仍活跃（entry 在 TTL 后过期）
- [ ] **snapshot 过期排除**：写入 → 过 TTL → snapshot → 恢复 → 过期 entry 不存在
- [ ] **TTL state checkpoint round-trip**：TTL-enabled state 经 `snapshotState()` → `restoreState()` 后，用户值正确（sidecar 时间戳不破坏序列化——因为序列化的是 raw user value，非时间戳）
- [ ] **无静默跳过**：TTL 启用但 lazy eviction 未覆盖的 state 类型分支抛 `UnsupportedOperationException`
- [ ] **Test-Mandated Feature Rule**：memory TTL 各有命名测试类
- [ ] `./mvnw test -pl nop-stream-core -am` 通过

### Phase 3 — RocksDB Compaction Filter + Design Doc

Status: planned（**blocked on Stage 30**）
Targets: `nop-stream-rocksdb/src/...`, `ai-dev/design/nop-stream/state-management-design.md`, `ai-dev/logs/`

- Item Types: `Proof | Follow-up`

- [ ] RocksDB 后端 TTL sidecar：同 Phase 2 的 sidecar 时间戳模式，value 在 RocksDB 中仍为 raw user value（sidecar 在内存或 RocksDB metadata CF）
- [ ] RocksDB `AbstractCompactionFilter`（JNI）：compaction 时检查 entry 的 sidecar 时间戳，过期 entry 从输出 SST 中丢弃
- [ ] compaction filter 注册到 TTL 启用的 column family
- [ ] RocksDB 后端 `snapshotState()` 过期排除：复用 Phase 2 的 TTL 过滤 predicate。**注意**：若 Stage 30 已将 `MemoryStateSerDe` per-type 逻辑抽取到共享 `StateSnapshotFormat` helper，TTL predicate 需穿透 helper API（非在 RocksDB 层重新实现）
- [ ] 端到端测试：source → keyed state with TTL → 写入 → 等待过期 → checkpoint → restore → 验证过期 entry 不恢复
- [ ] 更新 `state-management-design.md`：新增 TTL 小节（config、sidecar 方式、清理策略、processing-time only 限制、MapState 粒度）；§11.1 更新 TTL 限制状态；**§11.6 修正陈旧声明**（与 Stage 30 一致——`AbstractStreamOperator.snapshotState()` 是活跃路径）
- [ ] 更新 `ai-dev/logs/` 对应日期条目
- [ ] G42/G43 gap 关闭追踪：`08-gap-analysis.md` 标记 ✅ + 本 plan 路径

Exit Criteria:

- [ ] RocksDB compaction filter 在 compaction 后减少过期 entry 数量（测试验证 SST 文件中过期 entry 被丢弃）
- [ ] **端到端验证**：TTL state → 过期 → checkpoint → restore，过期 entry 不恢复（从 `env.addSource()` 到 sink 完整路径）。**前置**：checkpoint 管线（barrier → coordinator → snapshot → restore）已由 Stage 30 端到端测试验证可用
- [ ] **接线验证**：compaction filter 在 RocksDB TTL column family 的 compaction 过程中被调用（测试或日志验证）
- [ ] **Anti-Hollow Check**：compaction filter 有真实过滤逻辑（非空壳），lazy eviction 在运行时触发
- [ ] `state-management-design.md` TTL 小节已写入；§11.1/§11.6 已更新
- [ ] `./mvnw test -pl nop-stream -am -T 1C` 通过
- [ ] `ai-dev/logs/` 对应日期条目已更新

## Closure Gates

- [ ] `StateTtlConfig` 可通过 `StateDescriptor` 配置
- [ ] Memory 后端支持 TTL lazy eviction + snapshot 过期排除 + restore 后 TTL 存活
- [ ] RocksDB 后端支持 TTL lazy eviction + compaction filter（Phase 3，after Stage 30）
- [ ] TTL=disabled 不影响现有行为（向后兼容）
- [ ] TTL state checkpoint round-trip 正确（用户值不因 sidecar 时间戳而损坏）
- [ ] G42/G43 gap 关闭追踪已更新
- [ ] 设计文档已更新
- [ ] 不存在被静默降级到 deferred / follow-up 的 in-scope live defect
- [ ] 独立子 agent closure-audit 已完成并记录证据
- [ ] **Anti-Hollow Check**：TTL 逻辑在运行时确实生效
- [ ] `./mvnw compile -pl nop-stream -am`
- [ ] `./mvnw test -pl nop-stream -am -T 1C`
- [ ] `node ai-dev/tools/check-plan-checklist.mjs <this-plan> --strict` 退出码 0
- [ ] `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-stream --severity high` 退出码 0
- [ ] `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0
- [ ] checkstyle / 代码规范检查通过

## Deferred But Adjudicated

### Event-time / watermark-based TTL

- Classification: `optimization candidate`
- Why Not Blocking Closure: Event-time TTL 需要 watermark 集成与乱序处理，复杂度显著高于 processing-time TTL。首版 processing-time TTL 覆盖主要使用场景。
- Successor Required: no
- Successor Path: (future enhancement, not on current roadmap)

### Per-UK-entry TTL for MapState

- Classification: `optimization candidate`
- Why Not Blocking Closure: 首版 MapState TTL 为 per-key+namespace 粒度（整 map 过期）。per-UK-entry 需更复杂的 sidecar 结构，为后续优化。
- Successor Required: no
- Successor Path: (future optimization)

## Non-Blocking Follow-ups

- TTL metrics（过期 entry 计数、eviction 速率）——可观测性增强
- Full snapshot scan cleanup（主动遍历删除过期项）——依赖 lazy + compaction filter 已足够

## Closure

Status Note: (待 closure audit 后填写)
Completed: (待定)

Closure Audit Evidence:

- Reviewer / Agent: (待定)
- Evidence: (待定)
