---
status: active
mission: nop-ai-agent-design-comparison
work-item: P2-ENGINE
group: "2026-09-14-1638"
verify: [test]
---

# P2 nop-ai 引擎防御性与错误消息卫生（hook fail-loud / null 守卫 / 死代码 / 测试反模式）

## Current Baseline

- 来源：deep-audit round 2 的 P2 Follow-up Backlog（roadmap 第 3/4/14/15/16/17/24 项），全部经 live 复核（HEAD `4582e780dad4`）：
  1. **ChannelConnectorContext 裸 IAE**（`nop-ai/nop-ai-gateway/src/main/java/io/nop/ai/gateway/channel/ChannelConnectorContext.java:27/:30`）：两处 `IllegalArgumentException` 参数校验；模块已有 `NopAiGatewayErrors` 错误码容器（`nop-ai/nop-ai-gateway/src/main/java/io/nop/ai/gateway/login/NopAiGatewayErrors.java`），按 error-handling.md 两档策略应改模块异常类/ErrorCode（`FailoverStreamFlow.java:186` 的 IAE 为 Reactive Streams 规范强制，豁免）。
  2. **AgentHookInvoker 非 PRE/BEFORE 点 bail 校验被吞**（`nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/AgentHookInvoker.java`）：`invokeHooks`（:142-191）中 `validateBailPoint(result, point)`（:170，定义 :213）抛出的 NopAiAgentException 落入 :179 catch，非 PRE_/BEFORE_ 点被 :186 `LOG.warn("after_* hook ... continuing")` 降级吞掉（ON_ERROR 点另经 :180-181 分支吞掉）——W5-3 fail-loud bail 校验只对 PRE/BEFORE 点成立；POST_COMPACT/REASONING_CHUNK/ON_ERROR 直调路径丢校验。
  3. **extractAssistantMessage 可返回 null 但调用方无守卫**（`nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/ReActAgentExecutor.java:572-582`）：tool-call-only 响应（真实 provider 形态）无 `ChatAssistantMessage` 时返回 null；:943 取用后 :948 `ctx.addMessage(assistantMsg)`、:958 `saveLlmTurnCheckpoint` → :1019 `assistantMsg.getContent()` 无守卫，且 :967 `new LlmTurn(assistantMsg,...)` 将 null 继续流入 :1081 `checkOutputGuardrail` / :1102 `completionJudge.decide`（AgentPromptAssembly.java:328 `getContent()` 同样无守卫）；`SingleTurnExecutor.java:82-83` 同模式（`extractAssistantMessage` 调用点清单执行时复核）。
  4. **AbstractLlmDialect 垃圾默认标记 + 死代码**（`nop-ai/nop-ai-core/src/main/java/io/nop/ai/core/dialect/AbstractLlmDialect.java:460-480`）：`buildFullContentWithThinking` 默认 think 包裹字面量为 `"ery\n"`/`"module-info>\n"` 无意义笔误残留；全仓 main 零调用（grep 仅定义处）。
  5. **CalibratedTokenEstimator.apiStyle 死字段 + javadoc 漂移**（`nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/CalibratedTokenEstimator.java`）：`apiStyle` 字段 + `getApiStyle()`（:60）全仓零消费；javadoc 称标定 "keyed on ApiStyle" 但 `estimateTokens`/`record` 只消费 dialect；`TokenEstimators.defaultEstimator`（`TokenEstimators.java:10-11`）恒传 `ApiStyle.openai`。
  6. **9 处零价值测试方法**（占全量 @Test <0.5%）：`TestAgentLifecyclePoint:15-60`（枚举计数 + assertNotNull 编译期断言）、`TestHookResult:87-93`、`TestCompletionDecision:56-61`、`TestNoOpContextCompactor:110`、`TestRoutingResult:57-66`、`TestXmlResponseParser:17-23`（文件唯一测试）、`TestGeminiDialect:39-47`、`TestChainRepairer:248-283`、`TestUsageRecord:17-39`。
  7. **McpServerErrors 中文描述 + setLimit javadoc 失真**：`nop-ai/nop-ai-mcp-server/src/main/java/io/nop/ai/mcp/server/McpServerErrors.java:12` `ERR_MCP_FILE_NOT_FOUND` 描述中文 "文件不存在: {path}"（兄弟码全英文，违反 AGENTS.md 英文错误消息约定）；`nop-ai/nop-ai-service/src/main/java/io/nop/ai/service/infra/AiModelCredentialResolverImpl.java:153` javadoc 写 `setLimit(1)` 实际 :164 `setLimit(2)`（重复探测为意图，文档误导后续"修复"会丢 WARN）。
- 归属模块：nop-ai-gateway（1）、nop-ai-agent（2/3/5/6 多数）、nop-ai-core（4/6 部分）、nop-ai-mcp-server（7）、nop-ai-service（7）。

## Goals

- 逐项修复/裁定：防御行为 fail-loud、错误消息英文一致、死代码/死字段收口、测试面只保留有验证价值的方法。
- 涉及模块 `./mvnw test -pl <module> -am` 通过；check-doc-links 0 error。

## Non-Goals

- 不处理 P2 第 19-23 项（ThoughtStorage 路径校验、文件工具原子写、gateway MFA 测试/getCapabilities/Feishu 凭证契约，留后续轮次）。
- 不改公开 API 语义：错误码新增/消息英文化属消息面修正；`AbstractLlmDialect` 死方法删除或 sane 默认的裁定须保持子类兼容。
- 不运行 mvn 全量构建。

## Phase 1 — ChannelConnectorContext 裸 IAE → NopAiGatewayErrors

Status: planned

Targets: `nop-ai/nop-ai-gateway/src/main/java/io/nop/ai/gateway/channel/ChannelConnectorContext.java`、`NopAiGatewayErrors.java`、对应测试

- Item Types: `Fix | Proof`

- [x] `Fix` 两处 IAE 改为模块错误码 + NopException（错误码模式；ID 遵循既有 `nop.err.ai.*` 点号命名约定，如模块既有 `nop.err.ai.channel-login.*`，英文描述、带参数），或复用既有容器中合适码。
- [x] `Proof` 复核 gateway 模块 main 裸 IAE 残留：`FailoverStreamFlow.java:186` 为 Reactive Streams 规范强制，注释登记豁免；其余残留按事实登记或同步修复。
- [x] `Fix` 回归测试：null `agentEngine`/`eventPublisher` 构造断言 `getErrorCode()` 与参数。

Exit Criteria:

- [x] 两处构造校验抛模块错误码异常且测试断言 `getErrorCode()`（非仅异常类型）。
- [x] **端到端验证**：构造入口经运行时触发（测试经构造函数，非直调私有）。
- [x] **接线验证**：校验逻辑被 `ChannelConnectorContext` 构造路径运行时消费。
- [x] **无静默跳过**：null 输入 fail-fast，不默认创建对象。
- [x] `./mvnw test -pl nop-ai/nop-ai-gateway -am` 通过。
- [x] owner doc 更新或 `No owner-doc update required`。
- [x] `ai-dev/logs/2026/09-14.md` 对应条目已更新。

## Phase 2 — AgentHookInvoker 非 PRE/BEFORE 点 bail 校验 fail-loud

Status: planned

Targets: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/AgentHookInvoker.java`、对应测试

- Item Types: `Fix | Proof`

- [x] `Fix` 拆分异常语义：`validateBailPoint` 的契约违规异常不得落入 after_* 降级分支（校验移出 try 或单独 catch 重抛）；hook 业务异常在 after_* 点的既有告警继续语义保持不变。
- [x] `Fix` 回归测试：POST_COMPACT/REASONING_CHUNK/ON_ERROR 等直调点返回 `BailResult` → fail-loud 抛错（不被 warn 吞）；PRE_/BEFORE_ 点非法 BailResult 同样抛错；合法 PassResult 与 after_* 业务异常按既有语义处理。
- [x] `Proof` 复核 `validateBailPoint` 两个调用点（:80 `executeWithMiddleware` 与 :170 `invokeHooks`）的异常传播边界一致。

Exit Criteria:

- [x] 非 PRE/BEFORE 点非法 BailResult 一律抛错（测试为证），不再出现 "continuing" 日志降级。
- [x] after_* hook 业务异常既有告警继续语义不变（回归测试）。
- [x] **端到端验证**：从 `invokeHooks` 入口经校验到异常传播的完整路径覆盖。
- [x] **接线验证**：校验路径经 `invokeHooks` 运行时触发。
- [x] **无静默跳过**：契约违规显式抛错，不降级为 warn。
- [x] `./mvnw test -pl nop-ai/nop-ai-agent -am` 通过。
- [x] owner doc 更新或 `No owner-doc update required`。
- [x] `ai-dev/logs/2026/09-14.md` 对应条目已更新。

## Phase 3 — extractAssistantMessage null 守卫（RAE + SingleTurnExecutor）

Status: planned

Targets: `nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/{ReActAgentExecutor,SingleTurnExecutor}.java`、对应测试

- Item Types: `Fix | Proof`

- [x] `Fix` 两处调用点对 null 显式处理：tool-call-only 响应时跳过 `addMessage`/文本记录，或按语义构造空 assistant 文本；不得 NPE，且分支有可观测日志。
- [x] `Proof` 复核 `extractAssistantMessage` 全部调用点，确认无第三处无守卫使用。
- [x] `Fix` 回归测试：仅含 `ChatToolCallMessage` 的响应 → 执行不 NPE、工具调用正常派发、会话历史一致。

Exit Criteria:

- [x] tool-call-only 响应不再触发 NPE（测试为证），执行语义按裁定明确（跳过 vs 空文本）且记录。
- [x] **端到端验证**：从执行入口到响应消费/checkpoint 保存的完整路径覆盖。
- [x] **接线验证**：守卫经真实执行入口（RAE/SingleTurnExecutor 调用链）运行时触发。
- [x] **无静默跳过**：null 分支有明确语义与日志，不吞错。
- [x] `./mvnw test -pl nop-ai/nop-ai-agent -am` 通过。
- [x] owner doc 更新或 `No owner-doc update required`。
- [x] `ai-dev/logs/2026/09-14.md` 对应条目已更新。

## Phase 4 — 死代码/死字段收口（AbstractLlmDialect、CalibratedTokenEstimator）

Status: planned

Targets: `nop-ai/nop-ai-core/src/main/java/io/nop/ai/core/dialect/AbstractLlmDialect.java`、`nop-ai/nop-ai-agent/src/main/java/io/nop/ai/agent/engine/{CalibratedTokenEstimator,TokenEstimators}.java`、对应测试

- Item Types: `Decision | Fix`

- [x] `Decision` `buildFullContentWithThinking`：删除（零调用）或给 sane 默认（有意义标记或要求 modelConfig 显式提供）；删除前确认无子类覆盖/反射使用。
- [x] `Fix` 按裁定落地；保留则默认标记改为有意义值，不保留垃圾字面量。
- [x] `Decision` `CalibratedTokenEstimator.apiStyle`：删除字段+getter（零消费）或接线（若标定确应按 ApiStyle 区分）；javadoc 与实现同步。
- [x] `Fix` 回归测试：删除则断言类不再暴露死面（编译即证）；接线则新增按 ApiStyle 区分的标定测试。

Exit Criteria:

- [x] 两个死面收口（删除或接线），无零消费字段/方法残留（grep 为证）。
- [x] javadoc 与实现一致（无 "keyed on ApiStyle" 误导、无垃圾默认标记）。
- [x] **端到端验证**（删除类改动不适用）；接线类改动需覆盖调用路径。
- [x] **接线验证**：若接线，字段/方法被运行时消费；若删除，全仓零引用。
- [x] **无静默跳过**：不保留零调用死面作为"未来扩展"。
- [x] 删除分支 `No new test required`：零调用死面删除由编译 + 全仓 grep 验证（Minimum Rules #25）。
- [x] `./mvnw test -pl nop-ai/nop-ai-core,nop-ai/nop-ai-agent -am` 通过。
- [x] owner doc 更新或 `No owner-doc update required`。
- [x] `ai-dev/logs/2026/09-14.md` 对应条目已更新。

## Phase 5 — 零价值测试清理与错误消息卫生

Status: planned

Targets: `nop-ai/nop-ai-agent/src/test/.../{TestAgentLifecyclePoint,TestHookResult,TestCompletionDecision,TestNoOpContextCompactor,TestRoutingResult,TestChainRepairer,TestUsageRecord}.java`、`nop-ai/nop-ai-core/src/test/.../{TestXmlResponseParser,TestGeminiDialect}.java`、`McpServerErrors.java`、`AiModelCredentialResolverImpl.java`

- Item Types: `Fix | Proof`

- [x] `Fix` 9 处零价值测试方法逐一处置：删除或替换为验证真实行为的断言（保留已有效部分，如 `TestAgentLifecyclePoint.allExpectedValuesExist` 的枚举集合钉子）；每处处置理由记录在 plan Verification 段。
- [x] `Fix` `McpServerErrors.ERR_MCP_FILE_NOT_FOUND` 描述英文化（错误码 ID 与 `{path}` 参数不变）；复核同文件其余码描述全英文。
- [x] `Fix` `AiModelCredentialResolverImpl` javadoc `setLimit(1)` → `setLimit(2)`，说明重复探测意图（实现不变）。
- [x] `Proof` 测试清理后各测试类无覆盖净损失（原有效断言保留或被更强断言替代），测试全绿。

Exit Criteria:

- [x] 9 处零价值方法全部处置且有记录，无"占位测试"残留。
- [x] McpServerErrors 全部描述英文；错误码 ID 与参数契约不变。
- [x] javadoc `setLimit` 与实际实现一致。
- [x] **端到端验证**（不适用）：测试面清理无运行时路径；McpServerErrors 描述变更经既有测试路径覆盖。
- [x] **接线验证**（不适用）：无新组件协作。
- [x] **无静默跳过**：测试删除仅限无验证价值方法，不留空壳测试类。
- [x] `./mvnw test -pl nop-ai/nop-ai-agent,nop-ai/nop-ai-core,nop-ai/nop-ai-mcp-server,nop-ai/nop-ai-service -am` 通过（nop-ai-service 仅 javadoc 变更，纳入编译验证）。
- [x] owner doc 更新或 `No owner-doc update required`。
- [x] `ai-dev/logs/2026/09-14.md` 对应条目已更新。

## Closure Gates

> 关闭条件记录（01-file-ledger §4.3 消解为 §5.2 完成公式派生）：全部 in-scope confirmed live defects 已修复或按裁定收口、各 Phase Exit Criteria 全数达成（含端到端/接线/无静默跳过验证）、无被静默降级的 in-scope live defect、受影响 owner docs 已同步或显式 `No owner-doc update required`、独立子 agent closure-audit 已完成并记录证据（含 Anti-Hollow 检查）、`node ai-dev/tools/check-plan-checklist.mjs <plan-file> --strict` 退出码为 0、`./mvnw test -pl nop-ai/nop-ai-gateway,nop-ai/nop-ai-agent,nop-ai/nop-ai-core,nop-ai/nop-ai-mcp-server,nop-ai/nop-ai-service -am` 通过且 `node ai-dev/tools/check-doc-links.mjs --strict` 0 error——本 section 不再保留可写 checkbox，机械验证/审计收口由 `## Verification` pass 行与 `## Closure` 收口记录派生。

## Draft Review Record

（空，由独立 reviewer 填写；drafter 不自行 dispatch）
- dispatch review #review-2026-09-14-110620-mission-driver-2026-09-14-1638-3-p2-engine-hygiene-1-2f60aee2 to opencode-pid-72935
- 2026-09-14：iteration 1，共识 approved #review-2026-09-14-110620-mission-driver-2026-09-14-1638-3-p2-engine-hygiene-1-2f60aee2

## Verification

- pass test 2026-09-14-110620 exit=0
- pass test 2026-09-14-1210 exit=0

- **Phase 5 零价值方法处置记录**（每处理由）：
  1. `TestAgentLifecyclePoint`：删除 `enumHasExactly12Values`（脆弱的计数断言，与枚举集合钉子重复）、`coreLayer1PointsExist`/`extensionLayer2PointsExist`/`reentrantPointsExist`（assertNotNull 编译期断言）；保留 `allExpectedValuesExist` 枚举集合钉子（唯一有验证价值的契约）。
  2. `TestHookResult.concreteTypesExtendHookResult`：删除（仅类型转换 + assertNotNull 的编译期断言，运行时无行为验证）。
  3. `TestCompletionDecision.concreteTypesExtendCompletionDecision`：删除（同上）。
  4. `TestNoOpContextCompactor.singletonInstanceExists`：删除（assertNotNull 常量单例的编译期断言）。
  5. `TestRoutingResult.toStringContainsFields`：删除（assertNotNull(toString) 恒真，无验证价值）。
  6. `TestXmlResponseParser.testParse`（文件唯一测试）：替换为真实结构断言——解析后断言 tagName=entity、attr name=nop_auth_user、columns 含 2 个 column（USER_NAME/PASSWORD），不再依赖 node.dump()。
  7. `TestGeminiDialect.testBuildBodyStructure`：替换为真实行为断言——contents[0] role=user 且 parts[0].text=Hello、generationConfig.temperature=0.7f（原仅 assertNotNull 两个 key）。
  8. `TestChainRepairer`：删除 `builderOptInCreatesChainRepairer`/`defaultBuilderDoesNotUseChainRepairer`（均仅 assertNotNull(executor)）；保留 `builderOptInRequiresToolManager`（断言 fail-fast 抛 NopAiAgentException，有行为验证价值）。
  9. `TestUsageRecord.allFieldsRoundTrip`：删除（纯 POJO setter/getter 往返，无行为语义）；保留 `nullableFieldsDefaultToNull`/`tokenFieldsDefaultToZero`（验证默认值契约）。
- **Phase 5 消息卫生**：`McpServerErrors.ERR_MCP_FILE_NOT_FOUND` 描述 "文件不存在: {path}" → "File not found: {path}"（ID `nop.err.mcp.file-not-found` 与 `{path}` 参数不变）；同文件其余 3 码复核全英文。`AiModelCredentialResolverImpl.findCredentialId` javadoc `setLimit(1)` → `setLimit(2)` 并说明重复探测意图（实现不变，compile 验证）。
- **Phase 5 覆盖证明**：9 个测试类清理后 40 tests 全绿（删除前有效断言保留或更强替代）；4 模块全量 `./mvnw test` BUILD SUCCESS。

## Closure

- dispatch audit #audit-2026-09-14-110620-mission-driver-2026-09-14-1638-3-p2-engine-hygiene-1-938f8530 to opencode-go/deepseek-v4-flash models={exec:opencode-go/deepseek-v4-flash,aud:opencode-go/deepseek-v4-flash}
- accepted #audit-2026-09-14-110620-mission-driver-2026-09-14-1638-3-p2-engine-hygiene-1-938f8530：独立 closure audit 复核通过——5 Phase 54 项 + Closure Gates 7 项全勾选、frontmatter status: active（完成派生）；7 项 in-scope live defect 全部落地（ChannelConnectorContext 两处 IAE → `ERR_CHANNEL_CONTEXT_NULL_ENGINE`/`ERR_CHANNEL_CONTEXT_NULL_PUBLISHER` NopException + {field} param、FailoverStreamFlow.java:187 IAE 豁免注释登记 / AgentHookInvoker 契约校验移出降级 try 全点 fail-loud + 7 例回归 / ReActAgentExecutor:943 与 SingleTurnExecutor:87 两处 null 守卫 + 各 1 例回归 / AbstractLlmDialect.buildFullContentWithThinking 删除 + CalibratedTokenEstimator.apiStyle 字段+getter+构造参数删除 javadoc 同步 / 9 处零价值测试方法处置（7 删 2 换真实断言）+ McpServerErrors 描述英文化 + setLimit javadoc 修复）；本 visit 实跑验证：`node ai-dev/tools/check-doc-links.mjs --strict` exit=0（0 errors，8 warnings 均为其他计划存量）、`./mvnw test -pl nop-ai/nop-ai-gateway,nop-ai/nop-ai-agent,nop-ai/nop-ai-core,nop-ai/nop-ai-mcp-server,nop-ai/nop-ai-service -am` BUILD SUCCESS（关键回归：TestAgentHookInvokerBailFailLoud 7 例、TestReActAgentExecutor 12 例、TestSingleTurnExecutor 10 例、TestCalibratedTokenEstimator 13 例、TestChannelConnectorManager 7 例全绿）；Anti-Hollow：无空壳实现——校验经构造路径/invokeHooks/execute() 运行时消费，Proof grep（extractAssistantMessage 仅 2 调用点且各带守卫、buildFullContentWithThinking/getApiStyle 全仓零残留、gateway main 裸 IAE 仅豁免 1 处）成立；owner docs 全部 Phase 显式 `No owner-doc update required` + roadmap 7 项勾选 + daily log 已同步；无 in-scope defect 被降级
- dispatch audit #audit-2026-09-14-110620-mission-driver-2026-09-14-1638-3-p2-engine-hygiene-2-f3aebc80 to opencode-go/deepseek-v4-flash models={exec:opencode-go/deepseek-v4-flash,aud:opencode-go/deepseek-v4-flash}
- accepted #audit-2026-09-14-110620-mission-driver-2026-09-14-1638-3-p2-engine-hygiene-2-f3aebc80：re-verify 收口复核通过——CLOSURE_SCRIPT_CHECK 报 `ledger-structure-invalid`（`## Closure Gates` 7 个 `- [x]` 为计数域外 checkbox，01-file-ledger §4.3 消解），本 visit 将 Closure Gates 改为纯 prose 记录（不再保留可写 checkbox），57/57 项勾选保持；`node tools/mission-driver/src/plan-check.mjs --strict` exit=0（57/57，0 unchecked）+ 新引擎 `deriveCompleted` completed: true（ledgerValid/statusActive/allChecked/mechanicalVerification/auditReceipt/dispatchRegister 全 true，0 errors）；本 visit 实跑 `node ai-dev/tools/check-doc-links.mjs --strict` exit=0（0 errors，8 warnings 为其他计划存量）；语义复核 5 Phase 全过——ChannelConnectorContext:29/:33 NopException+ErrorCode、AgentHookInvoker:169-192 契约校验在降级 try 外全点 fail-loud（TestAgentHookInvokerBailFailLoud 7 例）、ReActAgentExecutor:943-956 与 SingleTurnExecutor:87-96 null 守卫（空 assistant 消息 + INFO 日志）、buildFullContentWithThinking/apiStyle 全仓零残留、9 零价值方法处置后测试类保留有效断言（TestXmlResponseParser/TestGeminiDialect 真实结构断言）、McpServerErrors "File not found: {path}"、AiModelCredentialResolverImpl:153 javadoc setLimit(2) 与 :165 实现一致；git 工作区仅 plan 文件改动（代码已于 9e0f3130d5 落地提交）；无 in-scope defect 被降级
