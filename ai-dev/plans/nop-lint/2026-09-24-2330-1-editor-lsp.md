# 编辑器集成：LSP 服务器（fast 档 + 增量解析）（roadmap item 41）

> Plan Status: active
> Review: R1 spike 前置裁定 + 执行期实时核对（lsp4j 本地不可得 → 手写 Content-Length 帧 + 平台 JsonTool 序列化；Phase 1 exit criteria 全部 live 达成）
> Last Reviewed: 2026-09-24
> Source: ai-dev/design/nop-lint/11-performance-profiles.md §2/§6（fast 档 <50ms/文件预算）、03-execution-engine.md §2（执行面）、05-eslint-alignment.md §1（编辑器对标）
> Related: roadmap item 41（deps: 16, 18 done）；前序 item 16（EditCalculator/parseIncremental——本 plan 直接消费）、18（CheckRunner 编排形态）

## Purpose

为编辑器场景交付 **LSP 服务器 v1**：`NopLintLanguageServer`（传输无关的消息处理核心）+ stdio 入口（VS Code / Neovim 等 LSP 客户端开箱即用）。诊断经 **fast 档**（<50ms/文件预算）+ **增量解析**（didChange 的 full-document sync 经 EditCalculator → parseIncremental 复用未变叶子）。零平台修改。

## Current Baseline（live 核对 2026-09-24）

- `LintLanguage.parseIncremental(oldTree, newSource)` 与 `EditCalculator` 已落地（item 16，corpus gate 126 实例增量 ≡ 全量）；`LintEngine.lint(rules, languageId, path, source)` 单文件口径；`LintProfile.FAST` 存在（FAST_CAPABILITIES = {L1}，deep 规则 skipByProfile）。
- 语言发现 = `LanguageRegistry.discoverDefaults()`（ServiceLoader）；规则集 = `RuleSetLoader`（classpath VFS 前缀）。
- 仓库无 LSP 面；nop-java-parser 依赖树内无 lsp4j（**依赖形态 spike 在 Phase 1**：lsp4j 本地可得性 → 不可得则手写 Content-Length JSON-RPC 帧 + 最小 JSON 序列化——诊断结构简单，手写面可控；裁定随执行记录）。

## Goals

1. **`NopLintLanguageServer`（传输无关核心）**：处理 `initialize`（capabilities：full-document sync + 文本诊断）、`textDocument/didOpen`（解析 + lint + 诊断缓存）、`didChange`（full sync：新旧树 EditCalculator → parseIncremental → lint）、`didClose`（清缓存 + 清诊断）、`shutdown/exit`。诊断以 `textDocument/publishDiagnostics` 推送（LSP line/character 0-based 换算）。
2. **stdio 入口** `NopLintLspLauncher`（main）：Content-Length 帧循环读写 stdin/stdout，日志走 stderr。
3. **语言/规则装配**：与 item 38 同款懒单例——registry discoverDefaults + 规则集一次加载；fast 档钉死（编辑器预算面，design 11）。
4. **测试**：消息核心单测（initialize→didOpen→didChange→didClose 全生命周期 + 诊断推送断言 + 未注册语言 fail-closed）+ stdio 帧回环端到端（launcher 进程内起一条真实帧流）。

## Non-Goals

- incremental range sync（v1 全文档同步；range sync 归后续优化）
- codeAction/quickfix、formatting、definition 等编辑器扩展能力（v1 只做诊断推送）
- VS Code 插件打包 / Neovim 配置分发（客户端对接文档一节说明通用 LSP 配置即可）
- watch 模式与 CI 缓存（item 43）

## Execution Plan

### Phase 1 - LSP 核心 + stdio 入口 + 测试

Status: completed
Targets: `nop-lint/nop-lint-core/src/main/java/io/nop/lint/core/lsp/`（服务器与帧循环与 CLI 同模块——复用语言/规则装配，不新建模块）

- Item Types: `Proof`

- [x] 依赖形态 spike 裁定：lsp4j 本地 .m2 **不可得** → 手写 Content-Length 帧循环 + 平台 JsonTool 序列化（协议面仅 1 请求/响应 + 1 通知，手写面可控——NopLintLspLauncher javadoc 在案）
- [x] `NopLintLanguageServer`：initialize/didOpen/didChange/didClose/shutdown+exit 消息核心 + fast 档装配 + 诊断 LSP 双轴坐标换算（1-based line + UTF-8 byte → 0-based line + UTF-16 char）
- [x] `NopLintLspLauncher` stdio 入口（Content-Length 帧 + stderr 日志隔离）
- [x] 测试：TestNopLintLanguageServer 7 例（生命周期/诊断内容与 0-based 坐标/增量 didChange 清空/未知方法/未知语言 fail-closed/shutdown 标记）+ TestNopLintLspLauncher 帧回环端到端（真实帧流 → framed stdout 解析回 JSON-RPC 断言）
- [x] `ai-dev/logs/` 条目更新

Exit Criteria:

- [x] 全部新测试绿（8/0：7 生命周期 + 1 帧回环端到端——initialize 响应与 publishDiagnostics 从 framed stdout 解析回断言，非 mock）
- [x] **接线验证**：didChange 后诊断随内容变化更新（violating → clean 断言空诊断——增量解析路径被真实消费）
- [x] **无静默跳过**：未注册语言 throw NopLintException 断言消息、未知方法返回 error 响应（fail-closed）
- [x] JMH engineLint 0.002±0.001 + parseAndMatch 0.001±0.001 零回归（perf-baseline.md 增注）
- [x] `ai-dev/logs/` 条目更新

### Phase 2 - owner docs + 全量回归 + 收口

Status: in progress
Targets: `ai-dev/design/nop-lint/03-execution-engine.md §2`（LSP 面增注）、roadmap

- Item Types: `Proof`

- [x] design 03 §2 LSP 编辑器面落地增注（能力面/装配形态/依赖裁定/客户端对接通用配置）
- [x] 全量回归 nop-lint 子模块 + doc-links 0（core 751/0 含 LSP 8 测；doc-links 0）
- [ ] roadmap item 41 → done（closure audit 后）

Exit Criteria:

- [ ] 全量绿 + doc-links 0
- [ ] roadmap item 41 → done

## Closure Gates

- [ ] LSP v1 五方法可用（initialize/didOpen/didChange/didClose/shutdown）
- [ ] stdio 帧回环端到端测试存在（真实 initialize → publishDiagnostics 断言）
- [ ] fast 档钉死（deep 规则 skipByProfile 不触发）
- [ ] owner docs 与 live 一致
- [ ] 独立子 agent closure audit 写入 Closure 段
- [ ] 全模块测试通过；checklist/hollow/doc-links 门禁 0/0/0

## Deferred But Adjudicated

### range sync / codeAction / 客户端插件打包

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: v1 全文档同步已满足编辑器诊断面；扩展能力属编辑器体验增强
- Successor Required: `no`（候选池）

## Closure

Status Note: （closure 时填写）
Completed: （closure 时填写）

Closure Audit Evidence:

- Reviewer / Agent: （closure 时填写）
- Evidence: （closure 时填写）

Follow-up:

- （closure 时填写）
