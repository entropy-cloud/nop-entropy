# CLI completion（roadmap item 39）

> Plan Status: completed
> Review: R1 combined 审查 agent_41185046（3.1 测试改写豁免认领/3.2 --rules 位点与语义/3.3 match 语言解析/3.4 test 路径与退出映射/3.5–3.7 格式形状与命名——全部落 Decision）
> Last Reviewed: 2026-09-24
> Source: ai-dev/design/nop-lint/03-execution-engine.md §2.4（输出格式/退出码/match/test 子命令）、item 18 落地裁定（v1 参数面之外归本 item）
> Related: roadmap item 39（deps: 18, 25 done）；前序 item 18（最小 CLI）、25（--fix-dry-run）、27（baseline 三开关）

## Purpose

CLI 完整化（design 03 §2.4）：`--format` 四种机器可读输出（sarif/checkstyle-xml/json/junit-xml + console 默认）、`match`/`test` 子命令、`--max-warnings`、`--rules` 覆盖。`--fix-dry-run`/baseline/`--profile` 已在 items 18/25/27 落地（不在本 plan 重复范围）。

## Current Baseline

- `NopLintCli`（nop-lint-core cli 包）：`check` 子命令全参数面（profile/fix/baseline 族）+ exit codes 0/1/2（TestNopLintCliExitCodes 钉死）；`ConsoleReporter` 人类可读输出。
- v1 裁定排除面（item 18）：`--max-warnings`、`--rules`、`--format`、match/test 子命令——本 plan 全量承接。
- exit 语义现状：0 无违规；1 error 级违规或 baseline stale（item 27）；2 内部错误。
- `RuleTestRunner`（test 子命令的执行体）与 SourcePattern.matchIn（match 子命令执行体）均已存在。

## Goals

1. `--format console|sarif|checkstyle-xml|json|junit-xml`：四个新 Reporter（与 ConsoleReporter 同接口位），输出到 stdout；`--format` 与 fix 流正交（报告描述最终磁盘内容语义保持）。
2. `match` 子命令：`nop-lint match <pattern> <file>` —— 编译 pattern 并对单文件匹配，输出命中（规则作者调试面；design 03 §2.4 的作者体验位）。
3. `test` 子命令：`nop-lint test <suites-path>` —— 跑 RuleTestRunner 套件，输出结果摘要 + exit（green=0，红=1，加载错误=2）。
4. `--max-warnings N`：warning 级诊断超 N → exit 1（error 语义不变）。
5. `--rules <id,id,...>`（R1 3.2 位点与语义裁定）：实现位点 = `RuleSetLoader.loadRuleSet` 之后、`verifyRuleLanguages` 之前过滤（**显式 --rules 是刻意收窄：某语言规则被全量过滤后不再触发未绑定语言检查**——与 v1 防静默的语义相反是有意裁定）；未知 id fail-closed（过滤前校验）；`--rules` × `--fix` × baseline 组合合法，baseline fingerprint 消费跟随过滤后残差（增注落档）。
6. 退出码契约保持：0/1/2 语义 + 新增 max-warnings 触发的 1。

## Non-Goals

- 不做 `--watch`/incremental 面（item 41/43）
- 不做 SARIF 2.1.0 全量特性（v1：runs/results/artifacts 最小面，schema 合规即可）
- 不改 check 管线语义（Reporter 只读诊断流）

## Key Decisions（执行前裁定，审查确认）

1. **Reporter 接口位（R1 3.5 统一）**：新增 `Reporter` 接口 `render(CheckOutcome, Writer)`；CLI 传 stdout Writer、测试传缓冲（golden 断言对机器格式；ConsoleReporter golden 字节级不变——PrintStream→Writer 的行分隔符行为以现有输出为准）；**机器格式不含 dry-run diff 块与 suggestion 标注**（仅诊断条目——与 fix 流正交的精确含义）；sarif severity 映射 = error→error/warning→warning/info+hint→note（R1 3.6）；junit-xml 形状 = testsuite per 文件、testcase per 诊断（name=ruleId）；`--rules` 重定义 design §4.3 的同名参数（彼处=套件路径）——owner-doc 增注点名（R1 3.7）。
2. **match 子命令（R1 3.3 补齐）**：语言解析 = 文件扩展名 → `TargetScanner.languageIdForExtension` → registry.resolve（查无 → exit 2；`--language` 可覆盖）；输出行:列 = `node.range()` + `LineIndex.lineOfByte/lineStartByte` 换算（live Match record 无 range 字段——design §1.1 旧图已过时，以本推导为准）+ 文本首行；pattern 编译失败 exit 2。
3. **test 子命令**：包装 `RuleTestRunner.runSuites(path)`；deep-suites 不在此默认面（test 子命令扫给定 path——deep 面经显式 path 可跑）；套件红 → exit 1。
4. **sarif 面**：v1 最小合规（version/drivers/results + 规则 id/severity 映射 error→error/warning→warning/message/range）；不产出 fullSchema 全字段。

## Execution Plan

### Phase 1 - Reporter 接口 + 四格式 + --format/--max-warnings/--rules

Status: completed
Targets: `nop-lint/nop-lint-core/src/main/java/io/nop/lint/core/cli/`（Reporter 接口 + 四实现 + CliOptions）、测试

- Item Types: `Proof`

- [x] Reporter 接口 + ConsoleReporter 迁 Writer 面（行结束符钉 \n，PrintStream 构造器委托——golden 字节级不变）+ 四新 Reporter 落 MachineReporters + CliOptions 三新参数（fail-closed）+ NopLintCli 装配（含 CheckRunner ruleFilter 位点）
- [x] 测试：TestNopLintCliCompletion 13 例（四格式片段断言 + 未知 format + 负 max-warnings + rules 白名单/未知 id）+ 行为迁移认领（见 Exit Criteria）

Exit Criteria:

- [x] 全部新测试绿 + **行为迁移认领（R1 3.1）**：TestNopLintCliExitCodes.unknownOptionExitsTwoWithStderrUsage 改写为 `--bogus` 未知选项 + 新增 maxWarningsGateForcesExitOneBeyondTheLimit 正例（0 触发/5 余量）；TestCliOptions.deferredOptionsFailClosedAsUnknown 同步改写 `--bogus`；CliOptions javadoc/USAGE 串同步更新；ConsoleReporter golden 断言逐字未动（TestConsoleReporter 10/0）
- [x] **接线验证**：--format 输出流经真实 check（TestNopLintCliCompletion 四格式测试经 runFull 真实管线渲染断言）
- [x] Owner-doc：design 03 §2.4 CLI 完整化落地增注（五格式/参数面/match/test 语义）
- [x] `ai-dev/logs/` 条目更新

### Phase 2 - match/test 子命令 + 全量回归 + 收口

Status: completed

- [x] `match` 子命令（Decision 2）+ `test` 子命令（Decision 3）+ NopLintCli 初始化作用域内分发（match/test 参数面各自独立 fail-closed）。执行期发现：LineIndex.lineOfByte 返回 1-based；RuleTestRunner.runSuites 扫描契约是三级 `<suites-root>/<category>/<suite>`（fixture 布局随矫正）
- [x] 测试：match 真实命中（行:列 + 计数）/坏 pattern 退出 2、test 绿（green 根）/红（mixed 根，无 expect 的 invalid fixture）/路径错三态
- [x] 全量回归 lint 模块 + doc-links 0（core 743/0 + java 101/0 + nop 75/0 + plugin 8/0；doc-links 0）
- [x] roadmap item 39 → done（closure audit APPROVED 后翻转）

Exit Criteria:

- [x] 全部测试绿 + doc-links 0
- [x] roadmap item 39 → done

## Closure Gates

- [x] 五格式 + match/test + --max-warnings/--rules 全部可用且 fail-closed
- [x] 退出码契约测试钉死
- [x] owner docs 与 live 一致
- [x] 独立子 agent closure audit 写入 Closure 段
- [x] 全模块测试通过；checklist/hollow 门禁 0/0

## Closure

Status Note: 五格式 + 三参数 + match/test 子命令全部落地（fail-closed 面 + 真实管线测试 13 例），行为迁移认领完成（--bogus 改写 + max-warnings 正例），golden 字节级不变经 git diff 取证；audit APPROVED 后随收口补 §4.3 同名参数重定义点名（audit low #1）并翻转 roadmap。
Completed: 2026-09-24

Closure Audit Evidence:

- Reviewer / Agent: 独立子 agent agent_3eff2e72-b9ce-4211-b184-079f7f77fdde（fresh session）
- Evidence:
  - Gate 五格式/子命令/参数 fail-closed：live 核对全成立（Reporter 接口、MachineReporters 四类、ConsoleReporter Writer 面、CliOptions 三参数、CheckRunner applyRuleFilter 位点、MatchCommand/TestCommand、初始化作用域分发）（PASS）
  - Gate 退出码契约：TestNopLintCliExitCodes 8 例（含迁移后用例）+ TestNopLintCliCompletion 13 例真实管线断言（PASS）
  - Gate owner docs：design 03 §2.4 item 39 增注逐条与 live 一致；§4.3 点名注记已补（PASS）
  - Gate 全模块：core 743/0 + java 101/0 + nop 75/0 + plugin 8/0（Mojo 复用 PrintStream 面重构后仍绿）；checklist/hollow/doc-links 门禁 0（PASS）
  - Anti-Hollow：13 例 completion 测试为 runFull 真实管线断言非 mock；golden 认领经 git diff 实证（PASS）
  - Audit low #1（§4.3 点名注记缺失）已随收口补写

Follow-up:

- no remaining plan-owned work
