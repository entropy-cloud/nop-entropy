---
status: active
mission: nop-lint
work-item: "item-14"
group: "2026-09-21-2137"
verify: [test]
---

# xscript 引擎 v1：编译白名单 + node/captures/report 契约（roadmap item 14）

## Current Baseline

以下事实均已对照 live repo（2026-09-21）核实：

- 引擎 v1 已落地（item 9 done）：`CompiledRule` 形态矩阵 v1 把**含 `xscript` 字段的规则编译期 fail-closed**（英文消息含规则 id 与原因）——本 plan 用真实执行替换该拒绝路径；regex 匹配器的拒绝路径不变（语义归 item 22）。
- nop-lint-core 已依赖 nop-xlang（pom 实测），零新模块依赖即可使用 XLang 执行体系。
- XLang 执行体系（design 07 §1）：表达式编译一次 → `IExecutableExpression`，每 match 新建子 `IEvalScope` 绑定上下文；语法要点（design 07 §6 已按 XLang 校对）：lambda 箭头 `=>`（不识别 `->`）、反引号字符串是普通字面量（无 `${}` 插值）、正则走 `String.matches()`。
- API 契约（design 07 §2.1–§2.3）：v1 绑定 `node`/`captures`/`report` 三对象；`typeAnalyzer`（L2，item 26）与 `scopeAnalyzer`（Phase 3，item 33）不在本 plan。NodeWrapper 方法表 8 方法（kind/text/child/children/ancestor/descendant/siblings/range），其衬底是 `LintNode` 现有 API（`kind()/text()/childByField()/children()/parent()/range()` 等，均已存在）。
- L1 消费契约已就绪（item 12 done，design 06 §5.2 已修订）：`io.nop.lint.core.type.DeclTypeResolver.resolveDeclType(LintNode)` → 书写类型文本或 null——item 14 是其首个注入消费方（roadmap item 12 plan 收口时显式移交）。
- 安全模型（design 07 §3）：编译期白名单 scope（只注册 `node`/`captures`/`report` 与少量内置全局，不注册 import/Class/Resource 类访问器）；单 match 诊断数上限（默认 100）；调用深度上限 32；脚本异常 → 跳过该 match + internal warning（不杀规则/文件/run）；连续失败阈值（50）→ 禁用规则并上报；可观测计数进 LintStats。
- **deadline 不在本 plan**：路线 A（`EvalExprProvider.registerGlobalExecutor` 全局包装）属 item 15（deps: 14，紧随本 plan）。`xscriptTimeoutMs` 已被 `RuleDslModel` 携带（默认 100）但 v1 不强制——残留风险（脚本内死循环可挂起执行）必须显式登记，不得静默。
- 设计硬边界：nop-xlang/nop-core 是 Protected Area，**零平台改动**；白名单必须经 XLang 现有公开编译入口实现，不得触碰平台内部。
- `report({fix})` 的 autofix 面属 item 25——v1 对 fix 子字段显式拒绝。
- Wave 3 剩余 items 15/17/18 均以本 plan 为前置（deps: 14 或 9+dogfooding）；API 类规则（item 11 的 4 条）的检测实现路径依赖本 plan 的 xscript 能力。

## Goals

- 规则 `xscript` 字段真实可执行：matcher（pattern/any/kind）命中后逐 match 执行脚本；脚本不调用 `report()` 则该 match 无诊断（xscript 可作过滤器）；可多次 `report()` 产出多条诊断。
- 编译期白名单：违禁构造（import/类定义/静态反射调用/文件网络访问全局函数）编译失败 → 规则 fail-closed（英文消息含规则 id），杜绝沙箱静默放行。
- NodeWrapper/captures/report 运行时绑定落地（design 07 §2.2/§2.3 语义）；L1 `DeclTypeResolver` 按消费契约注入 xscript 上下文。
- 资源与错误语义落地：诊断数上限、脚本异常跳过 + 计数、连续失败禁用规则，全部可观测（LintStats/日志）。
- 端到端：`.rule.yml`（pattern + xscript）→ `LintEngine.lint` → `Diagnostic` 一条测试走通。
- 完成后 roadmap item 14 具备翻 `done` 条件（items 15/17/18 与 item 11 API 规则的前置）。

## Non-Goals

- deadline executor 与 `xscriptTimeoutMs` 强制（item 15，路线 A）；fast 档 20ms 收紧（design 11 §7）。
- `typeAnalyzer`（L2，item 26）与 `scopeAnalyzer`（Phase 3，item 33）绑定。
- `report({fix})` 执行面（item 25）——v1 显式拒绝该子字段。
- regex 匹配器执行（item 22 口径）——维持编译期 fail-closed 现状。
- nop-xlang / nop-core / nop-xdef 平台改动（Protected Area，硬边界）。
- 抑制判定（item 17）、多文件与增量解析（item 16）、CLI（item 18）。

## Phase 1 — XScript 编译器与白名单（Decision + Proof）

Status: completed
Targets: `nop-lint/nop-lint-core`（main + test）

- Item Types: `Decision | Proof`

- [x] **Decision（编译入口）**：按 nop-xlang 现有公开编译入口落定 compile-once 形态（规则加载时编译一次 → 可执行表达式）与白名单 scope 的实现载体；结论（含所选公开 API 面）记录进日志并回写 design 07 §1 增注。硬边界：现有公开 API 无法实现白名单时，停止并回到 plan 修订，不得改平台。——裁定：`XLang.newCompileTool()` + `allowUnregisteredScopeVar(false)` + `ScopeVarDefinition.readOnly` 白名单注册；`parseFullExpr` → import AST 预扫描拒绝（XLang 词法分析会静默注册 import，公开 API 无禁用开关，预扫为调用方职责）→ `buildEvalAction` → `ExprEvalAction`；落点 `io.nop.lint.core.xscript.XScriptCompiler`，零平台改动。
- [x] 新增 xscript 编译路径：规则加载时编译 `xscript` 字段；编译失败 → 规则 fail-closed 抛 `NopLintException`（英文消息含规则 id 与原因），替换现"含 xscript 字段即拒绝"的矩阵格。（`CompiledRule.compile` 经 `XScriptCompiler` 产出 `XScriptEngine`；`TestCompiledRule.xscriptRuleCompilesIntoExecutableEngine` 证明替换格）
- [x] 违禁构造测试矩阵（至少 4 类，Minimum Rules #25）：import / 类定义 / `Class.forName` 式反射 / 文件或网络访问全局函数——每类断言编译期显式失败（非运行期静默）。（`TestXScriptCompiler`：Java 式/JS 式 import、`class Evil{}` 解析期失败、`Class.forName`、`readText(...)` 未知全局函数，另加未知绑定与 `typeAnalyzer` 未注册用例）
- [x] 合法子集测试：let/if/for..of/return、`=>` lambda、`String.matches()`、反引号字面量各至少一例编译通过（锚定 design 07 §6 语法口径）。（`legalSubsetWithLetIfForOfLambdaMatchesCompiles` 等；执行中确认 for..of 循环变量须显式 `for (let x of ...)`，已锚定为口径）

Exit Criteria:

- [x] 编译矩阵全格有测试断言（违禁 4 类拒绝 + 合法子集通过）（Minimum Rules #25）。
- [x] **无静默跳过**：编译失败路径全部显式抛错，无忽略分支（Minimum Rules #24）。
- [x] **零平台改动**经 diff 核实（仅 nop-lint 模块内变更）。（git status：变更全部位于 `nop-lint/nop-lint-core` + `ai-dev/`；nop-xlang/nop-core/nop-xdef 无 diff）
- [x] `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0。
- [x] owner-doc：design 07 §1 编译入口增注完成。
- [x] `ai-dev/logs/` 对应日期条目已更新。

## Phase 2 — 运行时绑定（NodeWrapper / captures / report / L1 注入）

Status: completed
Targets: `nop-lint/nop-lint-core`（main + test）

- Item Types: `Fix | Proof`

- [x] NodeWrapper 落地（design 07 §2.2 方法表 8 方法），映射到 `LintNode` 现有 API；`range()` 输出行列形态（需携带源码字节做换算）；`ancestor/descendant/siblings` 遍历语义与空/null 返回契约有单测。（`io.nop.lint.core.xscript.NodeWrapper` + `SourceMap`（1-based 行 + 1-based 字节列）；`TestNodeWrapper` 9 用例全部锚定真实 Java 解析树）
- [x] captures 绑定：pattern 捕获（`MetaVarEnv`）→ `Map<String, NodeWrapper|List>`，单节点与序列形态保持 design 04 捕获语义，有单测。（`MetaVarEnv.singleCaptures()/multiCaptures()` 快照；`TestXScriptEngine.capturesExposeSingleAndSequenceShapes` 等）
- [x] `report(diag)` 函数：`message` 必填（缺 → 抛错）；`severity` 缺省取规则 severity；`node`/`capture` 目标覆盖语义；`fix` 子字段 v1 显式拒绝（英文消息指向 item 25）。每条语义有测试。（`TestXScriptEngine` report 契约组 7 用例；capture 覆盖 node 的优先级经测试裁定并回写 design 07 §2.3）
- [x] L1 注入：`DeclTypeResolver` 按 design 06 §5.2 契约暴露给 xscript（绑定名执行时落定）；绑定后脚本内可对声明节点取书写类型文本。（绑定名裁定为 `declType`；`declTypeResolvesTheWrittenTypeOfDeclarations` + `declTypeInjectionWorksThroughTheEngine`）
- [x] 单元测试：绑定面全路径（含越界/空捕获/非法 report 参数）。

Exit Criteria:

- [x] 绑定面全部语义有焦点测试断言（Minimum Rules #25）。
- [x] **接线验证**（Minimum Rules #23）：NodeWrapper 包装真实解析树（`JavaLanguage` 解样的 `.java` 源码）工作，`ancestor/child` 在真实树上断言，非自造节点自证。（core 模块 classpath 无 nop-lint-java，按既有先例用 `JavaBindingTestSupport` 同型绑定（同一 tree-sitter-java grammar blob），`TestNodeWrapper`/`TestXScriptEngine` 全部断言真实解析树）
- [x] **无静默跳过**：非法 report 参数/未知 capture 名等失败路径显式抛错（Minimum Rules #24）。
- [x] `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0。
- [x] owner-doc：design 07 §2.1 L1 绑定名增注完成；§2.3 fix 拒绝面增注完成。
- [x] `ai-dev/logs/` 对应日期条目已更新。

## Phase 3 — 引擎集成、资源语义与端到端收口

Status: completed
Targets: `nop-lint/nop-lint-core`（main + test）、`ai-dev/backlog/nop-lint-roadmap.md`

- Item Types: `Fix | Decision | Proof`

- [x] 引擎管线集成：xscript 规则 = matcher 命中 → 逐 match 执行脚本 → 收集 `report()` 诊断；无 report 则该 match 无诊断（过滤语义，与 pattern-only 规则并行共存）；管线顺序维持 design 03 §1.1。（`CompiledRule.matchWithCaptures` + `RuleSetRunner.runXscriptRule`；`kindFilterStillRunsBeforeXscriptExecution` 证明管线顺序）
- [x] 资源语义落地：单 match 诊断数上限（默认 100，超出终止脚本 + 告警计数）；脚本异常 → 该 match 跳过 + internal warning 计数（不杀规则/文件/run）；连续失败达阈值（50）→ 禁用该规则并上报；上述计数进入 `LintStats` 或结构化日志（可观测，计数域不静默）。（`LintStats` 新增 4 计数域 + `nop.lint.xscript.*` warn 日志；`StackOverflowError` 兜底含入异常跳过语义）
- [x] **Decision（timeout 残留登记）**：`xscriptTimeoutMs` v1 携带不强制（item 15 承接路线 A deadline）；"无 deadline 下脚本死循环可挂起"的残留风险在本 plan Closure 与日志显式登记（watch-only residual，触发条件 = item 15 落地即消除）。（已登记于 plan `Deferred But Adjudicated` + 2026-09-21 日志；Closure 收据由独立 audit 步骤记录）
- [x] 端到端测试：`_vfs` 测试夹具 `.rule.yml`（pattern + xscript：ancestor/child 检查 + 条件 report）→ `LintEngine.lint` → 断言诊断 ruleId/severity/message/range；同规则条件不满足时零诊断（过滤路径）；脚本异常路径的"跳过 + 计数"断言。（夹具 `xscript-report.rule.yml`；`TestXScriptRules` 8 用例）
- [x] 收口项：roadmap item 14 状态回写（draft review 通过置 `planned`，closure audit 通过置 `done`）；核对 items 15–18 状态未受扰动。（item 14 → `planned`；15–18 保持 `todo`）

Exit Criteria:

- [x] **端到端验证**（Minimum Rules #22）：`.rule.yml` 资源 → 引擎 → 诊断断言一条测试走通，含命中/过滤/异常跳过三路径。
- [x] **接线验证**（Minimum Rules #23）：脚本真实经 Phase 1 编译产物执行（非桩/非旁路）——由端到端测试的报告断言证明。（`fixtureScriptReportsDiagnosticThroughTheWholePipeline` 断言脚本拼接的消息/覆盖 severity，只有真实执行编译产物才可能产出）
- [x] **无静默跳过**：诊断超限、脚本异常、连续失败禁用三类路径全部显式（计数或上报），无一处忽略（Minimum Rules #24）。
- [x] `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0。
- [x] owner-doc：design 07 §3 计数口径与 LintStats 承载方式如有偏差即修订；timeout 残留登记完成。（§3 增注：v1 run 级 4 计数域 + 保留已产出诊断的 cap 语义 + SOE 兜底；偏差已修订）
- [x] `ai-dev/logs/` 对应日期条目已更新（含 timeout 残留登记）。

## Closure Gates

> 关闭条件记录（01-file-ledger §4.3 消解为 §5.2 完成公式派生）：三个 Phase（编译白名单、运行时绑定、引擎集成端到端）执行项与 Exit Criteria 全数勾选（32/32 计数域 checklist），文件内无未勾选的 in-scope 项；无 in-scope confirmed live defect / contract drift 被静默降级到 deferred / follow-up（timeout 残留按 watch-only residual 显式裁定，见 `Deferred But Adjudicated`，successor 为 roadmap item 15）；Anti-Hollow Check 由独立 closure audit 复核 `.rule.yml` → 编译白名单 → matcher 命中 → 脚本执行 → `report()` → `Diagnostic` 端到端调用链连通（`TestXScriptRules` 夹具断言证明），无空方法体/静默跳过/no-op 作为正常实现；owner docs（design 07 §1/§2/§3）已与 live baseline 一致，roadmap item 14 状态已回写；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0；独立子 agent closure-audit 证据写入下方 `## Closure` 段落（含 plan-checklist 与 hollow-scan 检查结果）——本 section 不再保留可写 checkbox（计数域纪律，01-file-ledger §2.5），机械验证/审计收口由 `## Verification` pass 行与 `## Closure` 收口记录派生。

## Deferred But Adjudicated

### `xscriptTimeoutMs` deadline 强制（路线 A executor 包装）

- Classification: `watch-only residual`
- Why Not Blocking Closure: v1 编译期白名单已封死违禁构造，脚本异常/诊断超限/连续失败三类资源路径均 fail-fast + 计数；唯一残留是脚本内死循环可挂起单次 lint 执行，属资源边界而非契约缺陷。路线 A（`EvalExprProvider.registerGlobalExecutor` 全局包装）由 roadmap item 15（deps: 14）紧随承接，item 15 落地即消除该残留。
- Successor Required: `yes`
- Successor Path: `ai-dev/backlog/nop-lint-roadmap.md` item 15

## Draft Review Record

- dispatch review #review-2026-09-21-142035-mission-driver-2026-09-21-2137-2-xscript-engine-v1-1-49debfd7 to opencode-2026-09-21-142035
- 2026-09-21：iteration 1，共识 approved #review-2026-09-21-142035-mission-driver-2026-09-21-2137-2-xscript-engine-v1-1-49debfd7

## Verification

- pass test 2026-09-21-142035-mission-driver exit=0

- closure-visit 复核（2026-09-21，独立 auditor opencode-pid-78565）：`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` BUILD SUCCESS，**242 tests，0 failures**（含 TestXScriptCompiler 11 / TestNodeWrapper 9 / TestXScriptEngine 18 / TestXScriptRules 8 / TestCompiledRule 13，均 exit=0），即 frontmatter `verify` 键 `test` 对应 mission `commands.test`。
- typecheck：`./mvnw -pl nop-lint/nop-lint-core -am test-compile -T 1C` exit=0。
- build：`./mvnw -pl nop-lint/nop-lint-core -am clean package -DskipTests -T 1C` exit=0（nop-lint-core 41 main + 10 test sources 全量编译，jar 产出）。
- lint 裁定：mission `commands.lint`（`./mvnw -pl nop-lint/nop-lint-core -am checkstyle:check -q 2>/dev/null || echo 'lint not configured'`）按配置 exit=0；裸 `checkstyle:check` 为全仓遗留基线不通过（nop-api-core 9226 处 style-only 违规，与本 plan 无关且 roadmap 终态即以 nop-lint 替代 checkstyle），mission 已显式将其降级为非阻塞，不计入本 plan 完成公式（与 plan 1420-1 / 2137-1 同一裁定）。
- 收口修正（pre-commit ast-grep `java-lint-getmessage-only` 拦截）：`RuleSetRunner` match-failed warn 去除 `error={}` 占位、异常对象作为 SLF4J 末位实参传入（保留完整堆栈，结构化日志键 `nop.lint.xscript.match-failed` 不变）；修正后复跑 `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 仍 **242 tests，0 failures**，exit=0。

## Closure

Status Note: 三个 Phase 全部落地并经独立 closure audit 复核通过：Phase 1 编译白名单（`XScriptCompiler`：`XLang.newCompileTool()` + `allowUnregisteredScopeVar(false)` + `ScopeVarDefinition.readOnly` 白名单 + import AST 预扫拒绝，违禁 4 类编译期 fail-closed，零平台改动经 git diff 核实）、Phase 2 运行时绑定（`NodeWrapper`/`SourceMap` 8 方法映射 `LintNode`，captures 单/序双形态，`report` 契约含 `fix` 显式拒绝，L1 `declType` 绑定）、Phase 3 引擎集成（matcher 命中 → 逐 match 执行 → `report()` 收集；诊断超限/脚本异常/连续失败禁用三路径 fail-fast + `LintStats` 4 计数域；端到端 `xscript-report.rule.yml` → `LintEngine.lint` → 诊断断言）。32/32 计数域 checklist 全勾，无 in-scope live defect 被降级（timeout 残留 watch-only 显式登记，successor item 15；`fix` 执行面归 item 25、regex 匹配器归 item 22，均 Non-Goals 有 roadmap 归属）。
Completed: 2026-09-21

Closure Audit Evidence:

- dispatch audit #audit-2026-09-21-142035-mission-driver-2026-09-21-2137-2-xscript-engine-v1-1-547feb53 to opencode-pid-78565 models={exec:zhipuai-coding-plan/glm-5.3-flash,aud:zhipuai-coding-plan/glm-5.3-flash}
- accepted #audit-2026-09-21-142035-mission-driver-2026-09-21-2137-2-xscript-engine-v1-1-547feb53：独立 auditor（fresh closure visit）复核通过——亲测复跑全套 mission 命令（test-compile / clean package -DskipTests / test 均 exit=0，**242 tests 全绿**；mission `lint` 键按配置 exit=0，裸 checkstyle 遗留基线裁定见 `## Verification`）；Anti-Hollow 通过：`.rule.yml` → VFS 加载 → `CompiledRule.compile` 调 `XScriptCompiler.compile`（CompiledRule.java:89-90，规则加载期编译一次）→ matcher 命中 → `RuleSetRunner.runXscriptRule`（RuleSetRunner.java:61-65）逐 match 执行编译产物 → `report()` → `Diagnostic` 端到端链路真实连通，`TestXScriptRules` 8 用例（命中/过滤/异常跳过/连击禁用/诊断超限/declType 注入）在本 visit 随 `./mvnw test` 真实执行；`scan-hollow-implementations.mjs --module nop-lint/nop-lint-core --severity high` exit=0（0 findings），新增代码无空方法体/静默跳过/no-op（`NodeWrapper`/`resolveDeclType` 的 null 返回为 design 07 §2.2 契约语义且有单测）；机械修复消解 CLOSURE_SCRIPT_CHECK 4 项（Closure Gates 计数域外 7 checkbox 按 01-file-ledger §4.3 消解为纯 prose、frontmatter 移除子集外 `last-reviewed`/`source` 字段、`## Verification` 补 pass 行、`## Closure` 补 dispatch/accepted 收口对），`plan-check.mjs --strict` 重跑派生完成公式全绿；owner docs 抽查一致（design 07 §1/§2.1/§2.3/§3 增注在案）；deferred 分类诚实，无降级。
- Reviewer / Agent: mission-driver closure auditor（独立 visit，opencode-pid-78565，model zhipuai-coding-plan/glm-5.3-flash）
- Evidence: 本文件 `## Verification` pass 行（2026-09-21-142035-mission-driver）；`ai-dev/logs/2026/09-21.md` closure audit 条目；roadmap item 14 → `done`

Follow-up:

- no remaining plan-owned work（deadline executor 路线 A 由 item 15（deps: 14）承接并消除 timeout 残留；`report({fix})` 执行面、regex 匹配器、抑制、多文件增量、CLI 分别归 items 25/22/17/16/18）
