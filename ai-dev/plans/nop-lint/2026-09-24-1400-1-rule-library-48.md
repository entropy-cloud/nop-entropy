# 规则库扩至 48+（roadmap item 35）

> Plan Status: draft
> Last Reviewed: 2026-09-24
> Source: ai-dev/design/nop-lint/02-rule-library.md §1（首批 20 + 反模式 8 + 迁移吸收 ~20 的 48+ 口径）、06-pmd-errorprone-alignment.md §4.1–§4.7（机制映射）+ 12-check-scripts-migration-manifest.md、01 §3.2/§3.3（约束/动作面）、11 §2（deep 档）
> Related: roadmap item 35（deps: 29 done）；前序 items 29（首批 20 清单）、32–34（metrics/semantic/dataflow/scope 绑定——本 item 的 deep 规则直接消费）

## Purpose

把生产规则库从 18 条扩至 **48 条**（首批 20 余量 1 + 反模式 8 + 迁移吸收 21），每条带 RuleTester fixtures（Wave-2+ 硬约束），并把 count 门禁（`TestProductionRuleCount`）钉到 48。本 plan 的**枚举清单即首个交付物**（roadmap Wave 5 明文要求），Phase 1 逐条 spike 校验可行性后冻结。

## Current Baseline

- 生产规则 18 条（exception 6 + api 4 + quality 4 + security 2 + nop 4 XNode/XML——TestProductionRuleCount 枚举断言钉死）；全部带 RuleTester 套件（TestNopRuleSuites 真实管线）。
- 机制面（全部可用）：pattern/kind/any/all/not/关系（inside/has/follows/precedes/stopBy）/约束（sameText/differentText/regex/inList/typeOf/notExists/withinDepth）/xscript 绑定（node/captures/report/declType/**metrics/scope/semantic/dataflow**——items 32–34）/autofix 模板/XNode XML 规则。
- item 29 路由遗留：query-limit-required（未落地，首批 20 的第 20 条）、xpl-escaping（deferred，XML text 无 contains——本 plan 不翻案）。
- deep 档 provider 全通（items 32–34）：`requires: METRICS/SCOPE/L4/L3` 规则在 CLI 之外的装配（RuleTestRunner 显式构造）可真实运行；demo 套件形态 = deep-suites/。

## Goals

1. **枚举清单（本 plan §Execution Plan 前置表）**：30 条新规则的 id/category/机制/manifest 锚点，Phase 1 逐条 spike 后冻结。
2. 落地 30 条生产规则于 `nop-lint-nop/src/main/resources/_vfs/nop/lint/rules/`，每条 1 个 suite（valid ≥1 + invalid ≥1 + `.expect` 钉死）。
3. deep 规则（metrics/dataflow/semantic family）的 suite 走 `deep-suites/` + 显式 provider 构造（items 32–34 形态），其余走默认 suites + TestNopRuleSuites。
4. `TestProductionRuleCount` 与 `TestNopRuleSuites` 期望集更新到 48；count 门禁断言钉死。
5. 生产规则库 48 条 = 首批 20 中的 19（xpl-escaping deferred 不计）+ 新 29？**数量裁定**：48 = 18 现有 + 30 新增；首批 20 的"第 20 条"query-limit-required 含在新增内。

## Non-Goals

- xpl-escaping 翻案（维持 deferred）；errorcode-param-consistency（item 28 路由不变）
- autofix 模板批量补齐（现有规则不动；新规则 autoFixable=false 为主，除非 pattern 天然可模板）
- manifest tier 3 全量（~190 条池是 manifest 口径，非 48+ 口径——design 02 §106）
- ESLint 数据流规则移植与 PMD/EP P0/P1 批量的主体（item 36；本 plan 只吸收其中零成本可实现的条目，item 36 不重复计）

## 枚举清单（30 条，Phase 1 spike 后冻结；机制列 = 使用的执行面）

### A. Nop 特有（2）
| # | id | 机制 | manifest/设计锚点 |
|---|----|------|------|
| 1 | nop/xbiz-query-limit-required | XNode：xbiz query action 缺 limit/totalCount 属性判 null report（nop-orm-unique-key 同型） | design 02 §1 第 20 条 |
| 2 | nop/no-direct-datasource-inject | pattern：`@Inject` + `DataSource` 类型声明（字段/参数） | API 契约扩展（manifest tier2） |

### B. 反模式（8，调研识别——本表即识别结果）
| # | id | 机制 |
|---|----|------|
| 3 | antipattern/double-brace-init | kind: instance_initializer + inside object_creation（关系）或 xscript 文本判定（spike 定形态） |
| 4 | antipattern/system-exit | pattern `System.exit($$$)` |
| 5 | antipattern/print-stack-trace | pattern `$E.printStackTrace($$$)` |
| 6 | antipattern/empty-if-block | pattern `if ($C) {}`（空块字面形态，spike 解析验证；否则 xscript 判 body 空） |
| 7 | antipattern/new-primitive-boxing | any：`new Integer($X)`/`new Long($X)`/`new Double($X)`/`new Float($X)`/`new Short($X)`/`new Byte($X)`/`new Character($X)` |
| 8 | antipattern/empty-sync-block | pattern `synchronized ($M) { }`（同 6 spike） |
| 9 | antipattern/catch-npe | pattern `catch (NullPointerException $E)` |
| 10 | antipattern/throw-in-finally | 关系：`finally { throw $$$ }`（has 于 finally 块） |

### C. 度量（3，deep 档——requires: METRICS，deep-suites/）
| # | id | 机制 |
|---|----|------|
| 11 | quality/method-cyclomatic-complexity | xscript `metrics.cyclomatic(node) > 10`（method_declaration） |
| 12 | quality/method-cognitive-complexity | xscript `metrics.cognitive(node) > 15` |
| 13 | quality/method-npath-complexity | xscript `metrics.npath(node) > 200` |

### D. L3 数据流（2，deep 档——requires: L3，deep-suites/）
| # | id | 机制 |
|---|----|------|
| 14 | quality/unused-local-variable | xscript `dataflow.useCount(node) == 0`（variable_declarator；排除捕获形：无效 Java 不产 useSites 的场景在 fixture 层回避） |
| 15 | quality/self-assigned-local | xscript `dataflow.isSelfAssigned(node)` |

### E. L4 语义（2，deep 档——requires: L4，deep-suites/）
| # | id | 机制 |
|---|----|------|
| 16 | api/no-constructor-overridable-check | 演示形态：`semantic.isOverridable` 报告可覆写 public 方法（类 final 判定的消费示例） |
| 17 | api/no-nonslf4j-logger-call | xscript `semantic.isLoggerCall(node)` == false 且方法名 in log 族（inList 约束 + semantic 判定组合） |

### F. 质量/安全迁移吸收（12，manifest tier 2 中当前机制可实现者）
| # | id | 机制 |
|---|----|------|
| 18 | security/no-runtime-exec | pattern `Runtime.getRuntime().exec($$$)` |
| 19 | security/no-class-forname | pattern `Class.forName($$$)` |
| 20 | security/no-md5-digest | pattern `MessageDigest.getInstance($ALG)` + inList {MD5, MD4} |
| 21 | security/no-des-encryption | pattern `$C.getInstance($ALG)` + inList {DES, DESede}（接收方开放——转换服务面） |
| 22 | exception/no-catch-throwable | pattern `catch (Throwable $E)` |
| 23 | exception/no-throw-exception-base | any：`throw new Exception($$$)`/`throw new Throwable($$$)` |
| 24 | quality/no-string-eq-compare | pattern `$A == $B` + inside if/while（关系收窄）+ sameText 约束排除 null 字面量一侧（spike） |
| 25 | quality/loose-coupling-hashset | pattern `HashSet $X = new HashSet($$$)` |
| 26 | quality/replace-hashtable | pattern `Hashtable $X = new Hashtable($$$)` |
| 27 | quality/replace-vector | pattern `Vector $X = new Vector($$$)` |
| 28 | quality/empty-while-body | pattern `while ($C) { }`（同 6/8 spike 形态） |
| 29 | quality/assert-equals-reversed? —— 候选池 | 执行期 spike 从 manifest tier 2 短名单定（见 Phase 1） |
| 30 | 候选池（同上） | 同上 |

（#29/#30 = 执行期从 manifest tier 2 短名单中 spike 冻结：候选 = unused-parameter? / magic-number? / no-dup-literal?——候选必须满足"当前机制可实现 + 低误报 fixture 可写"，冻结后回写本表并在 plan 记录裁定。）

## Execution Plan

### Phase 1 - 枚举冻结（spike）+ 创作约定

Status: planned
Targets: 本 plan 枚举表、`nop-lint-nop` 规则目录

- Item Types: `Decision | Proof`

- [ ] spike：B/C/D/E/F 各条 pattern/绑定形态在真实 grammar 下验证（含 #6/#8/#28 空块形态、#3 双括号形态、#24 sameText 排除形态、#10 finally 关系形态）；不可行条目按候选池替换并记录裁定
- [ ] 冻结枚举表（回写本 plan，含每条最终机制）；fixture 模板约定（valid/invalid ≥2/expect 钉死）

Exit Criteria:

- [ ] 30 条全部冻结（无 TBD）；不可行者替换记录在案
- [ ] `ai-dev/logs/2026/09-24.md` 条目更新

### Phase 2 - 批量落地（4 子批：Nop+反模式 10 → 度量+L3/L4 7 → 质量/安全 7 → 余量 6）

Status: planned
Targets: `nop-lint/nop-lint-nop/src/main/resources/_vfs/nop/lint/rules/`、`deep-suites/`、`TestNopRuleSuites`/`TestProductionRuleCount`/deep 套件测试

- Item Types: `Fix | Proof`

- [ ] 子批 1：#1–#10（规则 + 套件 + 期望集更新）
- [ ] 子批 2：#11–#17（deep 套件 + deep 测试期望）
- [ ] 子批 3：#18–#24
- [ ] 子批 4：#25–#30
- [ ] `TestProductionRuleCount` → 48 枚举断言；`TestNopRuleSuites` 期望集扩展

Exit Criteria:

- [ ] 每子批 `./mvnw -pl nop-lint/nop-lint-nop test` 全绿（套件真实管线）
- [ ] 每条规则有可证伪 expect（消息/行号钉死）
- [ ] 生产规则计数 = 48 且被测试钉死

### Phase 3 - 性能守门 + 全量回归 + 收口

Status: planned
Targets: `nop-lint/docs/perf-baseline.md`、roadmap、全模块测试

- Item Types: `Proof`

- [ ] JMH after 复测（48 条规则库的 matchAllPatterns/engineLint 对锚点对照；规则数 3→48 的量级变化本身不计回归——基线口径是 3 条旗舰规则）；如回归 JFR 定位
- [ ] 全量回归三模块 + doc-links 0
- [ ] roadmap item 35 → done（closure audit 后）；`ai-dev/logs/` 收口记录

Exit Criteria:

- [ ] perf-baseline.md 增注 + 全量绿 + doc-links 0
- [ ] roadmap item 35 → done
- [ ] `ai-dev/logs/` 收口记录

## Closure Gates

- [ ] 枚举清单 30 条冻结且与 live 规则一一对应
- [ ] 生产规则库 48 条且 count 门禁钉死
- [ ] 全部 30 条带 RuleTester fixtures 且真实管线绿（Wave-2+ 硬约束）
- [ ] deep 规则经 deep-suites/provider 构造真实管线绿
- [ ] 无被静默降级的 in-scope 项；候选池替换裁定记录在案
- [ ] owner docs（design 02 §1 数量口径增注）与 live 一致
- [ ] 独立子 agent closure audit 写入 Closure 段
- [ ] **Anti-Hollow Check**：每条规则的 expect 可证伪（消息钉死）；抽查规则真实触发
- [ ] core/java/nop 三模块测试通过
- [ ] `check-plan-checklist --strict` 退出码 0
- [ ] `scan-hollow-implementations --module nop-lint-nop --severity high` 退出码 0

## Deferred But Adjudicated

### xpl-escaping / errorcode-param-consistency / manifest tier 3 其余

- Classification: `watch-only residual` + successor（既有路由不变）
- Why Not Blocking Closure: 既有裁定（item 28/29）；48+ 口径不需要
- Successor Required: `no`

## Non-Blocking Follow-ups

- 规则 autofix 模板批量补齐
- 复杂度阈值配置化（>10/>15/>200 为 v1 常量）

## Closure

Status Note: （closure 时填写）
Completed: （closure 时填写）

Closure Audit Evidence:

- Reviewer / Agent: （closure 时填写）
- Evidence: （closure 时填写）

Follow-up:

- （closure 时填写）
