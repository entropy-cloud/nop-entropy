---
status: active
mission: nop-lint
work-item: "item-17"
group: "2026-09-22-0128"
verify: [test]
---

# Suppression v1：内联注释 + @SuppressWarnings（roadmap item 17）

## Current Baseline

以下事实均已对照 live repo（2026-09-22）核实：

- 管线位置（design 03 §1.1）：抑制判定位于 xscript 之后、`Diagnostic` 输出之前——当前 `RuleSetRunner` 直接产出诊断（`engine/RuleSetRunner.java:74,103-104`），nop-lint 主源码树无任何抑制实现（全量文件清单核对：无 Suppression 相关类）。
- Design 09 §2 内联注释 DSL 六形态（行注释与块注释均可）：`nop-lint-disable-next-line <rules>`、`nop-lint-disable-line`、`nop-lint-disable <rules>`、`nop-lint-disable`（全规则）、`nop-lint-enable <rule>`、`nop-lint-enable-all`；§2.2 语义：诊断 range 与注释作用范围**有重叠即抑制**（含多行诊断）；`disable`/`enable` 须配对，文件结束仍处 disable → `unpaired-disable`（info）；未命中任何诊断的 disable → `unused-disable-directive`（warning）。
- Design 09 §3 `@SuppressWarnings`（Java）三种值形态：`"nop-lint:<rule-id>"`（推荐）、裸 rule id（兼容）、`"all"`；作用范围 = 注解所在的类/方法/字段/参数**声明节点**。PMD/ErrorProne 别名值（`"PMD.AvoidUsingVolatile"` 等）依赖 manifest（06 §7），归 item 29——本 plan Non-Goal。
- 档位口径（design 11 §2 fast 行）：抑制判定 v1 全量（内联注释 + @SuppressWarnings）**不按档位裁剪**，FAST/STANDARD 均启用。
- 交互规则（design 09 §6）：每个诊断独立判定；被抑制的诊断不产 fix（v1 `report({fix})` 已被 item 14 显式拒绝，无冲突面）；RuleTester fixtures 不受 baseline/exemptions 影响（两者属 v2/item 27）。
- RuleTester 现状（design 03 §4.1 增注 + `testing/RuleTestRunner.java:47,50`）：suite = `<rule-name>.rule.yml` + `valid/*.java`（零诊断）+ `invalid/*.java` + 同名 `.expect`，fail-closed；「suppressed.java 类抑制夹具不属于 RuleTester 契约（roadmap item 17）」——**本 plan 必须给出抑制夹具契约**。
- 元诊断归属：`unused-disable-directive`（warning）/ `unpaired-disable`（info）为引擎生成的固定 rule id，非 `.rule.yml` 规则（无加载面）；「可关闭」旋钮依赖豁免配置，归 item 27——本 plan Non-Goal。
- `@SuppressWarnings` 提取需 Java CST 导航（注解节点挂接声明节点）；实现载体（core 通用抑制 provider 接口 + `nop-lint-java`（现有 `JavaLanguage.java` 绑定）实现 vs 其他落点）执行时裁定。
- 前置已满足：item 9（LintEngine minimal）done 是本 item 唯一路标依赖。

## Goals

- 内联注释抑制按 design 09 §2 全量落地：六形态 + 行/块注释 + 重叠语义（含多行诊断）+ `enable`/`enable-all` 恢复 + `unpaired-disable`/`unused-disable-directive` 产出。
- `@SuppressWarnings` 按 design 09 §3 落地：三种值形态 × 声明节点范围（类/方法/字段/参数）。
- 单调判定语义（design 09 §1）：诊断命中任一层有效抑制即不输出；被抑制诊断不进 `LintResult`。
- 引擎接线：抑制判定落在 design 03 §1.1 管线位（xscript 后、输出前）；抑制计数进入 `LintStats`（计数域可观测，不静默）。
- RuleTester 抑制夹具契约（Decision）+ 夹具组；端到端：同一规则同一源码，无抑制 → 诊断；有抑制 → 零该规则诊断 + 元诊断按场景断言。
- roadmap item 17 状态回写（draft review 通过置 `planned`，closure audit 通过置 `done`）。

## Non-Goals

- v2 层：规则集 YAML exemptions 与 baseline 文件 + CI stale 检查（item 27；design 09 §4/§5）。
- `@SuppressWarnings` 的 PMD/ErrorProne 别名值识别（item 29，manifest 未建）。
- `unused-disable-directive` 的关闭旋钮 / 抑制配置面（item 27 exemptions 承载）。
- `--report-suppressed` 与 SARIF `suppressionKind` 标记（item 39 CLI 输出格式）。
- 非 Java 源（TS 随 item 19；XML/ORM/xbiz 随 item 21 XNode）。
- autofix 交互面（fix 本体 = item 25；本 plan 仅保持「被抑制不产 fix」的语义注记）。

## Phase 1 — 内联注释抑制引擎（Decision + Fix + Proof）

Status: completed
Targets: `nop-lint/nop-lint-core`（main + test）

- Item Types: `Decision | Fix | Proof`

- [x] **Decision（扫描与作用域模型）**：裁定注释扫描载体（源码行级扫描 vs CST comment 节点导航）与抑制作用域表示、重叠判定口径（diagnostic range 与作用范围重叠即抑制）；结论记日志并回写 design 09 §2 增注。（裁定：CST 注释节点导航，行级扫描被拒——字符串字面量不可抑制 + LintNode 门面语言无关；作用域 = 字节半开区间 span，空规则集 = 全规则，重叠 = 区间相交。见 design 09 §2.3。）
- [x] 六形态 DSL 落地 + `enable`/`enable-all` 恢复 + disable/enable 配对状态机。（`suppress/CommentSuppressionScanner`：六形态 + 配对状态机 + `disable-next-line`/`disable-line` 立即闭合行 span 不参与配对；裸 disable 仅 enable-all 可解除，enable 具名 span 逐规则闭合。）
- [x] `unused-disable-directive`（warning）/ `unpaired-disable`（info）产出：固定 rule id + 固定缺省 severity，作为 `Diagnostic` 进入结果集。（`suppress/SuppressionMeta` 固定 id/severity；`SuppressionFilter` 产出并附加于结果集尾部，元诊断本身不可被抑制——测试钉死。）
- [x] **Decision（未知形态处置）**：无法解析的指令参数（如空规则名、畸形 token）的处理显式裁定（fail-closed 报错 vs 忽略并计数）——二选一，不得静默含糊；结论回写 design 09 §2。（裁定：fail-closed 抛 `NopLintException`（消息含行号）：enable 缺规则列表、enable-all 带参数、空规则段、token 超出字母表、一注释双指令；`nop-lint-` 非关键字后缀（如 `nop-lint-disabled`）为普通文本忽略；`--reason` 接受并丢弃。见 design 09 §2.3。）
- [x] 单元测试矩阵：每形态 × 命中/不命中、多行诊断重叠、enable 恢复、EOF 未配对、unused 指令、行注释与块注释两载体。（Minimum Rules #25）（`TestCommentSuppression` 31 例全绿：六形态命中/不命中、多行重叠、enable 恢复、EOF 未配对、unused 判定、行/块双载体、多字节偏移、字符串字面量非指令、8 条 fail-closed 路径、provider span 合成。）

Exit Criteria:

- [x] 矩阵全格有断言（Minimum Rules #25）。（31/31 例逐格断言，见 `TestCommentSuppression`。）
- [x] 未知形态处置按裁定显式化（抛错或计数 + 日志），无静默分支（Minimum Rules #24）。（8 条 fail-closed 断言 + 2 条 prose-ignored 边界断言，无静默容错分支。）
- [x] `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0。（BUILD SUCCESS 332 tests, 0 failures，含存量 301 + 新增 31。）
- [x] owner-doc：design 09 §2 增注完成（扫描载体 + 未知形态裁定）。（design 09 新增 §2.3 v1 落地裁定四段：扫描载体、作用域表示、未知形态处置、配对语义与元诊断口径。）
- [x] `ai-dev/logs/` 对应日期条目已更新。（`ai-dev/logs/2026/09-22.md` item 17 条目，随本 Phase 追加。）

## Phase 2 — @SuppressWarnings 集成（Decision + Fix + Proof）

Status: completed
Targets: `nop-lint/nop-lint-core`、`nop-lint/nop-lint-java`（main + test）

- Item Types: `Decision | Fix | Proof`

- [x] **Decision（提取载体与接线）**：裁定 `@SuppressWarnings` 抑制范围的提取载体与装配方式（core 抑制 provider 接口 + `nop-lint-java` CST 实现或其他落点），保证 core 不引入 Java 文法硬编码；结论记日志并回写 design 09 §3 增注。（裁定：core `SuppressionProvider` 接口 + `LintLanguage.suppressionProvider()` 默认方法钩子，`JavaSuppressWarningsProvider` 以 CST 导航实现（annotation/modifiers → 声明节点）；拒绝引擎层硬编码文法与 ServiceLoader 独立发现。见 design 09 §3.1。）
- [x] 三种值形态解析 + 声明节点作用范围（class/method/field/param）提取。（逐值判定（数组逐值贡献 span）、value= 命名形态、限定名后缀匹配、marker 无 span；span = 声明节点 range。）
- [x] 单元测试：每值形态 × 每声明类型 × 重叠/不重叠诊断；`"all"` 与具体 rule id 的覆盖差异。（Minimum Rules #25）（`TestJavaSuppressWarnings` 19 例全绿：3 值形态 + 数组/命名/限定名/marker、4 声明类型 range 逐字节断言、内外重叠/不重叠、多行诊断、`all` 覆盖差异、3 条 fail-closed 路径、外来值不伪造抑制、绑定钩子接线。）

Exit Criteria:

- [x] 矩阵全格有断言（Minimum Rules #25）。（19/19 例逐格断言，见 `TestJavaSuppressWarnings`。）
- [x] 非法注解值路径显式处置（识别失败不伪造抑制），无静默分支（Minimum Rules #24）。（`nop-lint:` 空白/畸形 rule id fail-closed 抛 `NopLintException` 各有断言；外来值（unchecked/PMD 别名）按兼容规则成为裸 id span，不伪造抑制——断言其只匹配字面 id。）
- [x] `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0。（BUILD SUCCESS 332 tests；跨模块 `./mvnw -pl nop-lint/nop-lint-java -am test -T 1C` BUILD SUCCESS 28 tests；全仓 test-compile exit=0。）
- [x] owner-doc：design 09 §3 增注完成（提取载体裁定）。（design 09 新增 §3.1 v1 落地裁定三段：提取载体与装配、值形态解析、unused 覆盖注解层。）
- [x] `ai-dev/logs/` 对应日期条目已更新。（`ai-dev/logs/2026/09-22.md` item 17 条目，随本 Phase 追加。）

## Phase 3 — 引擎接线、夹具契约与端到端收口（Fix + Proof + Decision）

Status: completed
Targets: `nop-lint/nop-lint-core`（main + test）、`ai-dev/design/nop-lint/03-execution-engine.md`、`ai-dev/backlog/nop-lint-roadmap.md`

- Item Types: `Fix | Proof | Decision`

- [x] 管线接线：抑制判定落 design 03 §1.1 管线位（xscript 之后、`Diagnostic` 输出之前），STANDARD/FAST 两档一致生效；`LintStats` 新增抑制计数域。（`LintEngine.lint` 在 `RuleSetRunner` 后构造 `SuppressionFilter` 判定全部候选（管线尾，与 per-match 图示位置语义等价——匹配内核对抑制无感知）；`LintStats.suppressedDiagnostics` 计数 + `diagnostics` 尾部调和为最终输出数；FAST/STANDARD 一致性有测试钉死。）
- [x] **Decision（RuleTester 抑制夹具契约）**：裁定「应被抑制的诊断」与元诊断的夹具表达（`.expect` 扩展字段或独立夹具约定），保持既有 fail-closed 纪律（不引入可静默通过的夹具形态）；结论回写 design 03 §4。（裁定：三拒——不扩 `.expect` 字段、不加夹具目录、不加"关闭抑制"引擎开关；抑制场景 = 普通 invalid/ 夹具的抑制后差分断言（同文件未抑制对等违规证明规则本会命中），元诊断 = 固定 rule id 普通期望条目，空期望列表仍被 fail-closed 拒绝；stats 计数由 Java 引擎级测试承担。见 design 03 §4 抑制夹具契约段。）
- [x] 夹具组：至少一个 suite 覆盖注释抑制、注解抑制、unused-directive、unpaired-disable 四场景。（`nop-lint-nop` `suites/suppression/no-suppress-demo/`：comment-suppressed / annotation-suppressed / unused-directive / unpaired-disable 四 invalid 夹具 + valid/clean.java；落 nop-lint-nop 的偏差裁定（注解抑制需真实 Java 绑定）记日志。）
- [x] 端到端测试：夹具 → 真实管线（`RuleTestRunner` 或 `LintEngine`）→ 抑制生效断言 + 元诊断断言 + `LintStats` 计数断言。（`TestNopRuleSuites` 经 ServiceLoader 真实绑定跑套件全绿；`TestSuppressionEngineEndToEnd` 4 例：抑制/无抑制差分 + 元诊断 + `suppressedDiagnostics`/`diagnostics` 计数 + FAST/STANDARD 一致。）
- [x] 收口项：roadmap item 17 状态回写（draft review 通过置 `planned`，closure audit 通过置 `done`）；核对 items 15/16/18 状态未受扰动。（item 17 `todo`→`planned`；`done` 待独立 closure audit 通过后由下游 CLOSURE_AUDIT 步骤翻转——successor: closure-audit trigger: item 17 → done on closure audit pass；15 `done`/16 `done`/18 `todo` 未受扰动。）

Exit Criteria:

- [x] **端到端验证**（Minimum Rules #22）：规则命中 → 抑制 → 结果/计数全链路走通。（规则命中（无抑制 2 诊断）→ 抑制（1 诊断 + suppressedDiagnostics=1）→ 元诊断（unused/unpaired 进结果与计数）——`TestSuppressionEngineEndToEnd` + nop-lint-nop 套件。）
- [x] **接线验证**（Minimum Rules #23）：抑制判定被引擎管线真实调用（有抑制 vs 无抑制的诊断集差异断言即为运行时调用证据）。（同一源码差分：无抑制注释 = 2 诊断，有 = 1 诊断且计数为 1——差异只能来自 SuppressionFilter 真实运行；注解层经 nop-lint-nop 套件 ServiceLoader 真实绑定证明。）
- [x] `./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0。（BUILD SUCCESS 336 tests, 0 failures；跨模块 nop-lint-java 28 / nop-lint-nop 11 全绿。）
- [x] roadmap item 17 状态回写正确。（`planned`，注记四场景落地；`done` 翻转 successor 归 closure audit。）
- [x] owner-doc：design 03 §1.1/§4、design 09 §2/§3 增注与 live baseline 一致。（design 03 §1.1 管线尾裁定、§4 抑制夹具契约（取代两处旧"不属契约"注记）、design 09 §2.3 四段、§3.1 三段。）
- [x] `ai-dev/logs/` 对应日期条目已更新。（`ai-dev/logs/2026/09-22.md` item 17 条目。）

## Closure Gates

> 关闭条件记录（01-file-ledger §2.5 计数域纪律，按 §4.3 消解先例改纯 prose，同 plan 1420-1 / 2137-1 / 2137-2 / 2137-3 / 0128-1 / 0128-2 处置；独立 closure audit 证据由下游 CLOSURE_AUDIT 步骤写入下方 `## Closure` 段落，本 section 不再保留可写 checkbox，机械验证/审计收口由 `## Verification` pass 行与 `## Closure` 收口记录派生）：三个 Phase（内联注释抑制引擎、`@SuppressWarnings` 集成、引擎接线与端到端收口）执行项与 Exit Criteria 全数勾选（29/29 计数域 checklist，`check-plan-checklist.mjs --strict` 实测确认），文件内无未勾选的 in-scope 项残留；无 in-scope confirmed live defect / contract drift 被静默降级到 deferred / follow-up（执行期两处修正——scanner 行 span 形态缺陷与 split 尾随逗号语义——均在落地为 live 前修复并有回归测试钉死，无降级项）；行为契约达成：内联注释六形态（行/块双载体）+ `@SuppressWarnings` 三值形态（含数组/命名/限定名）+ 单调判定语义（被抑制诊断不进 `LintResult`、不产 fix——v1 无 fix 面，语义注记保持）+ 元诊断（`unused-disable-directive` warning / `unpaired-disable` info）产出，Goals 逐条落地；引擎接线达成：抑制判定落 design 03 §1.1 管线位（管线尾、xscript 后、输出前），STANDARD/FAST 两档一致生效（差分测试钉死），`LintStats.suppressedDiagnostics` 计数可观测；RuleTester 抑制夹具契约已裁定并落档（design 03 §4），`suppression/no-suppress-demo` 夹具组覆盖注释抑制/注解抑制/unused-directive/unpaired-disable 四场景 + valid 干净夹具，经真实 ServiceLoader 绑定管线全绿，fail-closed 纪律保持（空期望列表仍被拒绝、`.expect` 无新增字段）；受影响 owner docs 已同步：design 09 §2.3/§3.1、design 03 §1.1/§4 增注与 live baseline 一致，roadmap item 17 回写 `planned`（`done` 翻转 successor 归 closure audit），items 15/16/18 未受扰动；Anti-Hollow Check：接线证据 = 同源差分断言（无抑制注释 2 诊断 vs 有抑制 1 诊断且 `suppressedDiagnostics`=1，差异只能来自 `SuppressionFilter` 在 `LintEngine.lint` 内的真实运行）+ 注解层经 nop-lint-nop ServiceLoader 真实绑定套件全绿，无空方法体/静默跳过/no-op 作为正常实现（fail-closed 路径全部有抛错断言）；独立子 agent closure-audit 证据待下游 CLOSURE_AUDIT 步骤写入 `## Closure`；`./mvnw -pl nop-lint/nop-lint-core -am test -T 1C` 退出码 0（BUILD SUCCESS 336 tests, 0 failures；nop-lint-java 28、nop-lint-nop 11 全链绿）。

## Draft Review Record

- dispatch review #review-2026-09-21-142035-mission-driver-2026-09-22-0128-3-suppression-v1-1-79e3d9a9 to opencode-2026-09-22
- 2026-09-22：iteration 1，共识 approved #review-2026-09-21-142035-mission-driver-2026-09-22-0128-3-suppression-v1-1-79e3d9a9

## Verification

- pass test 202609220537 exit=0

## Closure

- dispatch audit #audit-202609220537-2026-09-22-0128-3-suppression-v1-1-79e537e5 to opencode-2026-09-21-142035 models={exec:zhipuai-coding-plan/glm-5.3-flash,aud:zhipuai-coding-plan/glm-5.3-flash}
- accepted #audit-202609220537-2026-09-22-0128-3-suppression-v1-1-79e537e5：独立 closure audit 复核通过——CLOSURE_SCRIPT_CHECK 报 2 项（`missing-pass:test` + `no-audit-receipt`），本 visit 全部消解：`## Verification` 补列 pass 行、`## Closure` 补 dispatch/accepted 收口对（本对，含 `models={exec,aud}` lineage 后缀，单模型降级如实登记）。`plan-check.mjs --strict` 重跑 exit=0（29/29 勾选，0 unchecked）。语义复核三 Phase 全过——Phase 1：`CommentSuppressionScanner` 六形态 + 配对状态机（`disable-next-line`/`disable-line` 立即行 span 不参与配对）+ fail-closed 八路径（`NopLintException` 含行号）+ `SuppressionMeta` 固定 id/severity（元诊断不可抑制，测试钉死）；Phase 2：`JavaSuppressWarningsProvider` CST 声明节点 range 提取 + 三值形态（数组逐值/命名/限定名后缀/marker）+ `nop-lint:` 畸形 id fail-closed + 外来值按兼容规则只匹配字面 id 不伪造抑制；Phase 3：引擎接线实证——`LintEngine.lint:93-96` 在 `RuleSetRunner.run` 后构造 `SuppressionFilter(language.suppressionProvider())` 一次性判定 + `suppressedDiagnostics` 计数 + `diagnostics` 尾部调和（design 03 §1.1 管线尾），`LintLanguage.suppressionProvider()` 默认钩子（`LintLanguage.java:77`）经 `JavaLanguage.java:92` 真实绑定；接线验证（Minimum Rules #23）由同源差分断言（无抑制注释 2 诊断 vs 有抑制 1 诊断且计数 1）+ `TestSuppressionEngineEndToEnd` 4 例 + nop-lint-nop `suppression/no-suppress-demo` 四场景套件经 ServiceLoader 真实绑定全绿。本 visit 亲测复跑全套验证命令：`./mvnw -pl nop-lint/nop-lint-core -am test-compile/-am clean package -DskipTests/-am test -T 1C` 全部 exit=0（**336 tests, 0 failures**）；跨模块 `./mvnw -pl nop-lint/nop-lint-java,nop-lint/nop-lint-nop -am test -T 1C` exit=0（nop-lint-java **28 tests** 含 `TestJavaSuppressWarnings` 19，nop-lint-nop **11 tests** 含 `TestNopRuleSuites` 10）；mission `commands.lint` 裸 checkstyle 走 `|| echo 'lint not configured'` 路径（nop-api-core 9226 处全仓遗留基线，与 1420-1/2137-*/0128-1/0128-2 同一裁定）；`scan-hollow-implementations.mjs --severity high` 三模块（core/java/nop）全部 0 findings exit=0；`check-plan-checklist.mjs --strict` exit=0（29/29）。Anti-Hollow：无空方法体/静默跳过/no-op，fail-closed 路径全部有抛错断言。roadmap item 17 回写 done（closure audit approved 触发，successor 兑现）；无 in-scope defect 被降级
