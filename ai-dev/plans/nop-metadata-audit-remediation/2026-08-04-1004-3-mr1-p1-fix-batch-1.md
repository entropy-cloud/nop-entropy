# MR1 P1 批量修复（第一批：结构 + ORM/Biz）

> Plan Status: completed
> Last Reviewed: 2026-08-04
> Draft Review: 3 轮独立子 agent 对抗性审查通过（第 1 轮 4 Major + 4 Minor 全部修复；第 2 轮 F-1 Major + 5 Minor 全部修复；第 3 轮 F-7 Major + 3 Minor 全部修复；第 4 轮最终验证 4/4 PASS，1 Minor 措辞已顺手修正，裁定可执行）。Session: ses_03579f700ffeJR0txdPNvMAEzP / ses_0356f3546ffem8SBxKgBBMNlT3 / ses_03567686effemX0Ey7wcvMLfYb / ses_0355ae874ffe7Np1jA0V99CyrF。
> Mission: nop-metadata-audit-remediation
> Work Item: MR1（R1.0 展开器 + R1.x 修复执行）
> Source: `ai-dev/backlog/nop-metadata-audit-remediation-roadmap.md`（MR1 里程碑）、`ai-dev/audits/arm-index-nop-metadata.md`（P1 汇总）、`ai-dev/audits/2026-08-04-0900-arm-MA1.3-nop-metadata-api-contract.md`（P1-MA1-001）、`ai-dev/audits/2026-08-04-0935-arm-MA2.1-nop-metadata-orm-model.md`（P2-MA2-01/02/03）
> Related: 执行顺序 `{3}` of 3 — 启动门禁：MA1+MA2 全部 done（roadmap R1.0 Deps = MA1+MA2 done，已满足）；本 plan 是首个**代码变更**计划（roadmap 规则 3：修复 plan 产物 = 代码变更 + 测试），后续 MR2/MR3 修复计划沿用本 plan 的展开-裁决-修复-验证模式。

## Purpose

承接 MA1+MA2 审计产出的第一批 P1 修复：通过 R1.0 展开器把 MA1+MA2 的 P1 发现（含待裁决 P2 项）汇总排序并展开为 roadmap 具体修复工作项行，然后执行 R1.x 修复（P1-MA1-001 陈旧类引用 + P2-MA2-01/02/03 裁决），全程保持绿色基线，经独立 closure audit 后收口。

## Current Baseline

经 2026-08-04 live repo 核对（引用均与 roadmap 一致，已二次确认文件与计数）：

- roadmap MR1 两行（R1.0/R1.x）状态为 `todo`；**Deps（MA1+MA2 done）已满足**；MA1/MA2 报告已登记 arm-index
- **P1-MA1-001（唯一 open P1，归 MR1）**：`nop-metadata-service/src/main/resources/_vfs/nop/metadata/NopMetaSearch/NopMetaSearch.xmeta:7` 的 `<schema type="List&lt;io.nop.metadata.core.dto.SearchHitDTO&gt;"/>` 引用不存在的类——DTO 迁移（c3162d4da，plan-2026-07-23-0838）已把 SearchHitDTO 迁至 `io.nop.metadata.api.dto.SearchHitDTO`，xmeta 未同步；`io.nop.metadata.core.dto` 包不存在（全仓 grep 仅命中该 xmeta 与其产物副本）；BizModel 实际返回 api.dto（`NopMetaSearchBizModel.java:10,86-88`）；风险 = GraphQL schema 对 `NopMetaSearch.items` 的类型解析指向缺失类（现有测试直连 service 不经 GraphQL schema，绿色基线未暴露）
- **P2-MA2-01/02/03（MA2.1 新增/复核，归 MR1/MR2 裁决）**：
  - P2-MA2-01：`nop-metadata/model/nop-metadata.orm.xml:3304-3317` NopMetaTagLabel.tag/glossaryTerm 的 `refPropName="tagLabels"` 指向不存在的反向集合（NopMetaTag 3202-3224、NopMetaGlossaryTerm 3047-3069 均无 tagLabels to-many）；其余 37 个 to-one 的 refPropName 脚本全量校验均能找到反向集合
  - P2-MA2-02：`nop-metadata/model/nop-metadata.orm.xml:3499-3506` NopMetaDataProduct.businessDomain 的 `refPropName="dataProducts"` 指向不存在的反向集合（NopMetaBusinessDomain 3403-3418 无 dataProducts）
  - P2-MA2-03：`nop-metadata/model/nop-metadata.orm.xml:747`（`code="PRIMARY"`）/ `920`（`code="CONSTRAINT"`）SQL 保留字用作列 code（历史 `2026-07-20-1554#维度04-02` 仍 open）
- 修复模式约束：ORM 模型结构变更属 Protected Area（plan-first）——本 plan 即裁决载体；改动必须走 model-first（改 `model/*.orm.xml` 源模型 → `mvn clean install -DskipTests` 重新生成 → 手写保留层保持），禁止手编 `_gen/` 产物（roadmap 规则 9 + AGENTS.md）
- GraphQL 回归测试基础设施（live）：`TestNopMetaTableBizModel.java:59` 已有 `@Inject IGraphQLEngine graphQLEngine` 注入模式（`IGraphQLEngine.getOperationDefinition/getTypeDefinition/resolveSelection` 可作 schema 断言 API，IGraphQLEngine.java:43-52）；**注意 `searchMetadata` 依赖 `@Inject @Named("nopSearchEngine")`（NopMetaSearchBizModel.java:46-49），引擎不可用时抛 `ERR_SEARCH_ENGINE_UNAVAILABLE`**——nop-metadata-service 自身 beans.xml 无 ISearchEngine 注册，可用性依赖 nop-search-lucene `search-defaults.beans.xml`（`ioc:default=true` 注册 LuceneSearchEngine），**且现有 search 测试（TestNopMetadataSearchIntegration）全部用 `@Mock ISearchEngine` + 手动注入，无任何现成证据证明 LuceneSearchEngine 能在测试容器初始化成功**——e2e 段必须先验证引擎可用性（详见 Phase 2 裁决），预期主路径可能失败，降级链须就位
- **P2-MA2-01/02 事实基补充（live + MA2.2 报告）**：反向集合**已由 codegen 自动派生存在**——`_gen/_NopMetaTag.java:994-1002` 有完整 `_tagLabels` OrmEntitySet + `getTagLabels()`、`_app.orm.xml:2863-2868`（Tag）/2719-2724（GlossaryTerm）/3037（BusinessDomain）有 to-many、`_NopMetaTag.xmeta:118` 等有 `ext:kind="to-many"` prop；MA2.2 报告 L43 已记录这是"refPropName 自动派生的反向关联（codegen 侧按 Nop 标准语义正常派生）"。**因此 P2-MA2-01/02 的编译失败风险不存在，真正缺口 = 显式声明缺失**（无 cascadeDelete（Tag 删除后 TagLabel 孤儿行）、无 displayName/i18n（i18n yaml `tagLabels: null`）、xmeta prop 为 `internal="true"`（GraphQL 层不可导航））——Phase 1 裁决输入与本 plan 修复收益定位以此为准
- **P2-MA2-03 证据基纠正（live）**：MySQL DDL 用反引号引用（`` `PRIMARY` ``）可正常解析；**未引号的是 Oracle DDL**（`PRIMARY SMALLINT default 0` / `CONSTRAINT VARCHAR2(100)`）——裁决依据以 Oracle 事实为准；**本纠正为本 plan 的 live 核查结论，MA2.1 报告原文仍为旧说法（"MySQL 下解析失败"），报告归属错误（见 Phase 1 arm-index 修正项）**
- **Nop ORM 反向集合同步语义（live 核对）**：`DynamicOrmEntity.internalSetRefEntity` 只写 FK 不把实体推入反向集合；反向同步仅在 `OrmEntitySet.orm_newItem`（child→parent 方向）与 DB 加载时发生——反向集合断言必须走 set → save/flush → 重载 → 断言的 DB-backed 流程（Phase 3 约束）
- 绿色基线：813 tests / 0 failures（M0.4，2026-08-04 实测；范围 `-pl nop-metadata -am -T 1C`）

## Goals

- R1.0：汇总 MA1+MA2 P1 发现并排序，向 roadmap 追加 R1.x 具体修复工作项行（引用 `P<级别>-<里程碑>-<序号>` 作为 Deps，含本 plan 覆盖与后续批次的边界声明）
- P1-MA1-001 修复：xmeta schema type 改为 api.dto 类 + GraphQL 字段选择回归测试（防回归：schema 类型解析 + 字段选择查询）
- P2-MA2-01/02/03 裁决：逐项裁决（修复 or 维持 P2 deferred），裁决修复的走 model-first 落地 + 测试；裁决 deferred 的必须记录明确 `Why Not Blocking Closure`
- 绿色基线保持（build + test 全绿）+ 独立子 agent closure audit 通过

## Non-Goals

- 不修复 MR2 归属项：P1 `2026-07-19-1118#维度20-01`（currentTimeMillis 2 处）、`2026-07-23-0714#维度07-003`（getEntityById 残余 ~10 处）、`维度07-004`（DTO 动态行 Map）、`维度09-02/03/06`（吞异常）、`维度11-04`（computeQualityScore 绕过）、`2026-07-21-2039#维度07-03`（11 参数）、`2026-07-19-1118#维度02-01` 命名残余、AutoTest 增量等——全部待 MA3/MA4 审计后由 MR2 承接
- 不执行 MA3-MA7 审计（后续计划）
- 不处理 P3（deferred successor，roadmap 规则 1）

## Scope

### In Scope

- R1.0：MA1+MA2 P1 发现汇总排序、P2-MA2-01/02/03 裁决、roadmap 追加 R1.x 行（含 Deps 引用）
- R1.x：P1-MA1-001 修复（xmeta 一行 + 回归测试）
- R1.x：P2-MA2-01/02/03 裁决修复（若裁决 in-scope：orm.xml 源模型 + codegen 再生成 + 反向集合/保留字处置 + 测试）
- 绿色基线验证 + 独立 closure audit

### Out Of Scope

- MR2/MR3 修复（后续批次）
- 任何审计（MA3-MA7）
- `docs-for-ai/` 文档修改（MA5 覆盖；本 plan 若改变行为则按 Exit Criteria 声明）

## Execution Plan

### Phase 1 - R1.0 P1 发现展开器

Status: completed
Targets: `ai-dev/backlog/nop-metadata-audit-remediation-roadmap.md`（MR1 状态表）+ `ai-dev/audits/arm-index-nop-metadata.md`

- Item Types: `Decision`

- [x] **启动门禁核查**：确认 MA1+MA2 全部 done（roadmap MA1 四行 + MA2 四行 done）；未满足则不启动并上报
- [x] 汇总 MA1+MA2 审计报告的 P1 级发现：P1-MA1-001（MA1.3，唯一 open P1 归 MR1）；P2-MA2-01/02/03（MA2.1，裁决候选，本阶段裁决）；MA2 报告其他 P1 级条目核对（MA2 无新增 P1，确认无遗漏）
- [x] 对 P2-MA2-01/02/03 逐项裁决（Decision）：live 核对 orm.xml 现状与影响面（**事实基：反向集合已由 codegen 自动派生，编译失败风险不存在；真正缺口 = 显式声明缺失（cascadeDelete/displayName/tagSet/internal xmeta prop）**；SQL 保留字生成风险以 Oracle DDL 未引号事实为准），**影响面核查手段：`rg -n "getTagLabels|getDataProducts|tagLabels|dataProducts" nop-metadata -g "*.java" -g "!**/target/**" -g "!**/_gen/**"`（手写代码是否引用反向导航/集合方法，排除生成物；`_gen/` 命中属预期）等具体 grep 作为裁决证据输入**，裁决为（a）本 plan 修复（model-first，Phase 3）或（b）维持 P2 deferred（记录明确 Why Not Blocking Closure + 业务影响评估）；裁决依据写入 roadmap R1.x 行。**注意：本裁决通道由 MA2.1 审计报告显式开辟（"修复归 MR1/MR2 裁决"），是 roadmap 规则 1（只处理 P0/P1）的显式例外——roadmap R1.x 行必须注明"MA2.1 裁决例外"，防后续 MR 把"修 P2"当默认先例**
- [x] **arm-index 修正项**：更新 arm-index 时一并修正 P2-MA2-03 的实体归属错误（现写"NopMetaEntityUniqueKey:747 / NopMetaEntityRelation:920"，实际 `PRIMARY` 在 **NopMetaEntityField**:747、`CONSTRAINT` 在 **NopMetaEntityUniqueKey**:920）
- [x] 向 roadmap 追加 R1.x 具体修复工作项行（每行引用 finding ID 作为 Deps：`P1-MA1-001`、`P2-MA2-01/02/03`），并更新 MR1 状态表（R1.0 → done）
- [x] 更新 arm-index：P1-MA1-001 修复状态与归属同步、P2-MA2-01/02/03 裁决结果登记

Exit Criteria:

> 每个 Phase 完成后，必须逐条勾选本节。所有 `[x]` 后才能将 Phase Status 改为 `completed`。

- [x] roadmap MR1 段出现具体 R1.x 行（引用 `P<级别>-<里程碑>-<序号>` Deps），R1.0 → done
- [x] P2-MA2-01/02/03 三项均有裁决结论（修复 or deferred + 理由），裁决记录可追溯（roadmap 行或 arm-index）
- [x] arm-index P1 追踪已同步（P1-MA1-001 标 in-progress）
- [x] 文档变化：roadmap + arm-index 更新；`No owner-doc update required`（docs-for-ai 不变）
- [x] `ai-dev/logs/` 对应日期条目已更新

### Phase 2 - R1.x P1-MA1-001 修复（xmeta 陈旧类引用）

Status: completed
Targets: `nop-metadata/nop-metadata-service/src/main/resources/_vfs/nop/metadata/NopMetaSearch/NopMetaSearch.xmeta` + 对应测试文件

- Item Types: `Fix | Proof`

- [x] **先复现再修复（本项必须先于 xmeta 修改项执行）**：修复前先在当前代码上经 `IGraphQLEngine` 跑一次目标 GraphQL 查询（选择 `items` 字段），记录实际失败/退化行为（pre-fix 基线；MA1.3 报告的"类型构建失败或字段类型退化"为假设，以实测为准）
- [x] 修改 `NopMetaSearch.xmeta:7`：`io.nop.metadata.core.dto.SearchHitDTO` → `io.nop.metadata.api.dto.SearchHitDTO`（一行修复；同步核对全仓 `io.nop.metadata.core.dto` 引用确认无其他残余——含 `_gen`/target 产物副本与 ai-dev/design 文档陈旧引用（后者归 MA5.1，见 Non-Blocking Follow-ups））
- [x] 新增回归测试（落点：`io.nop.metadata.service.search` 测试包，参照 `TestNopMetadataSearchIntegration` 先例；复用 `TestNopMetaTableBizModel.java:58-59` 的 `IGraphQLEngine` 注入模式）：
  - （a）schema 类型解析断言：经 `IGraphQLEngine.getTypeDefinition`/`getOperationDefinition`（或 GraphQL introspection）断言 `NopMetaSearch.items` 类型解析成功且指向 `io.nop.metadata.api.dto.SearchHitDTO`（不再指向缺失类）
  - （b）端到端查询断言：经 `IGraphQLEngine` 查询选择 `items` 字段；**先裁决搜索引擎获取方式**：验证 LuceneSearchEngine 经 `search-defaults.beans.xml`（`ioc:default=true`）自动注册且测试可初始化（可验证则直接用），否则在测试模块注册 test-scope stub ISearchEngine bean；**若 e2e 段因引擎基建不可行**，降级方案 = schema 级断言（a）+ 显式记录降级理由（不得静默省略（b））
- [x] 运行 `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C`（重新打包验证 XDSL 加载）后运行 `./mvnw test -pl nop-metadata -am -T 1C`
- [x] 绿色基线保持：813/0（或修复后重新实测记录）

Exit Criteria:

> 每个 Phase 完成后，必须逐条勾选本节。所有 `[x]` 后才能将 Phase Status 改为 `completed`。

- [x] `grep -rn "io.nop.metadata.core.dto" nop-metadata`（含 `*.xml` 与 `*.xmeta`，排除 target/）无残留（或仅剩余合法引用）；xmeta:7 已指向 api.dto
- [x] 新增回归测试验证（a）schema 类型解析成功（items 字段）；（b）端到端 GraphQL 请求通过（若降级为 schema 级断言，必须记录经裁决的降级理由）——测试名可追溯且断言行为（非仅存在性）
- [x] pre-fix 复现记录已写入测试或日志（修复前后行为对比可追溯）
- [x] `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` + `./mvnw test -pl nop-metadata -am -T 1C` 均 BUILD SUCCESS，测试 0 failures
- [x] 文档变化：`No owner-doc update required`（xmeta 修复同步既有契约，无约定变更；若修复揭示 GraphQL 行为变化则记录 finding 而非改 docs-for-ai）
- [x] `ai-dev/logs/` 对应日期条目已更新

### Phase 3 - R1.x P2-MA2-01/02/03 裁决修复（model-first）

Status: completed
Targets: `nop-metadata/model/nop-metadata.orm.xml` + `_gen/` 再生成产物 + 测试

- Item Types: `Fix | Decision | Proof`

- [x] 按 Phase 1 裁决执行（若全部裁决 deferred，本 Phase 只记录裁决文档，标记为纯 Decision 完成）：
  - [x] P2-MA2-01（若 in-scope）：**修复收益定位 = 显式 to-many 声明 + 控制语义，而非"打通运行时反向导航"（codegen 已自动派生 getTagLabels()，见 Current Baseline）**：NopMetaTag / NopMetaGlossaryTerm 补显式 `tagLabels` to-many（完整 XML 规格：`<to-many name="tagLabels" refEntityName="NopMetaTagLabel" refPropName="tag|glossaryTerm" join="leftProp: tagId|glossaryTermId（PK 列名，实测 orm.xml:3154/2984 primary="true"；参照 sibling 模式 childTerms leftProp="glossaryTermId"（3062-3067） / entityFields leftProp="metaEntityId"（650-654））, rightProp: tagId|glossaryTermId"/>`，cascadeDelete 按 sibling 模式裁决并写明理由——Tag/GlossaryTerm 删除后 TagLabel 孤儿行风险（生成 MySQL DDL 实测 0 条 FOREIGN KEY，无 DB 级约束）必须显式裁定），displayName 双语/tagSet 与既有关系风格一致；保留层与生成层同步
  - [x] P2-MA2-02（若 in-scope）：**修复收益定位同 P2-MA2-01**：NopMetaBusinessDomain 补显式 `dataProducts` to-many（完整 XML 规格：`<to-many name="dataProducts" refEntityName="NopMetaDataProduct" refPropName="businessDomain" join="leftProp: businessDomainId（PK 列名，实测 orm.xml:3350; 注意 3429-3433 处 businessDomainId 为 FK、dataProductId 3430 才是 DataProduct 的 PK）, rightProp: businessDomainId" tagSet="pub,ref-pub"/>`，displayName 双语，cascadeDelete 按治理语义裁决并写明理由）
  - [x] P2-MA2-03（若 in-scope）：`code="PRIMARY"`（:747，实体 **NopMetaEntityField** 的 primaryField）/ `code="CONSTRAINT"`（:920，实体 **NopMetaEntityUniqueKey** 的 constraintName）保留字列 code 处置（model-first 裁决：改名 or 保留 + 显式注释；**裁决依据以 Oracle DDL 未引号事实为准**（MySQL 反引号可解析；MA2.1 报告"MySQL 解析失败"为旧说法，本 plan live 核查已纠正）；改名列名需评估对既有数据/契约影响，写入裁决记录；若裁决保留，Why Not Blocking Closure 必须更硬（如"Oracle 部署未实际使用 + 生成链路不产生裸列名"）并记录残余风险）
- [x] model-first 流程：改 `model/nop-metadata.orm.xml` 源模型 → `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` 重新生成 → 核对 `_gen/` 产物与手写保留层一致（AGENTS.md 硬约束：不手编生成物）；**同时核对并提交再生成的 `deploy/sql/**/_create_nop-metadata.sql`（gen-orm.xgen 渲染含 deploy/sql 模板，改名列将改变 DDL）**
- [x] 为每个落地修复补测试（Test-Mandated Feature Rule）：
  - 反向集合测试（DB-backed 流程，Nop ORM 语义约束）：`set to-one → save/flush → 重载实体 → 断言反向集合内容`（`DynamicOrmEntity.internalSetRefEntity` 只写 FK 不推内存反向集合，内存级断言必然失败；可复用 `TestNopMetaBusinessDomainDataProductCrud` 现有会话模式）；**断言必须能区分新增语义**（如 cascadeDelete 级联行为、xmeta prop 的 `internal="true"` 消除（GraphQL 可导航）、生成物与源模型对称性），不能只验证"getTagLabels() 返回非空"（该行为 codegen 已自动派生，修复前后无差异）
  - 保留字处置（若改名）：生成 SQL/列名回归测试断言（如 `NopMetaEntityField.primaryField` / `NopMetaEntityUniqueKey.constraintName` 两个改名点的映射列名与查询行为）
- [x] 运行 `./mvnw test -pl nop-metadata -am -T 1C`，绿色基线保持

Exit Criteria:

> 每个 Phase 完成后，必须逐条勾选本节。所有 `[x]` 后才能将 Phase Status 改为 `completed`。

- [x] P2-MA2-01/02/03 每项有明确终态（fixed + 测试 / deferred + Why Not Blocking Closure），无悬置
- [x] 修复项经 model-first 落地：orm.xml 源模型变更 + 再生成产物核对记录（含 deploy/sql DDL 变更入库）；无手编 `_gen/` 产物
- [x] 每个修复项有对应测试（验证正确结果而非仅无错误）：反向集合经 DB-backed 流程断言（set → save/flush → 重载 → 断言）、保留字处置后的行为断言
- [x] **接线验证**：显式 to-many + refPropName 声明下的运行时双向导航连通（真实 ORM 会话 + flush/reload 后反向集合内容正确 + 新增控制语义生效，见 Nop ORM 同步语义约束与"断言必须能区分新增语义"约束；注意 codegen 自动派生集合在修复前即已可用，本判据必须与区分性断言配合）
- [x] `./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS，测试 0 failures
- [x] 文档变化：若修复改变 public contract（如实体新公开属性），同步 `docs-for-ai/02-core-guides/model-first-development.md` 或对应 owner doc；否则写 `No owner-doc update required`
- [x] `ai-dev/logs/` 对应日期条目已更新

## Closure Gates

- [x] R1.0 展开完成（roadmap R1.x 行 + Deps 引用 + R1.0 → done）
- [x] P1-MA1-001 修复 landed（xmeta + 回归测试，无 core.dto 残留）
- [x] P2-MA2-01/02/03 全部终态明确（fixed 或 deferred + 理由）
- [x] 所有 in-scope confirmed live defects 已修复（P1-MA1-001 不可降级 deferred）
- [x] 不存在被静默降级到 deferred / follow-up 的 in-scope live defect 或 contract drift
- [x] 受影响的 owner docs 已同步到 live baseline，或明确写明 `No owner-doc update required`
- [x] 独立子 agent / 独立审阅者 closure-audit 已完成并记录证据（evidence 写入本 plan Closure 段）
- [x] **Anti-Hollow Check**：closure audit 已验证（a）xmeta 修复后 GraphQL schema 类型解析真实生效（测试走 IGraphQLEngine 而非 mock）、（b）反向集合修复后 ORM 双向导航在运行时连通（非仅 orm.xml 存在 to-many 声明）、（c）无空方法体/静默跳过/no-op 作为正常实现
- [x] `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C`（含模型再生成）
- [x] `./mvnw test -pl nop-metadata -am -T 1C`
- [x] checkstyle / 代码规范检查通过（nop-metadata 无独立 checkstyle 命令，以 mvn 构建默认检查为准；历史计划惯例记 "checkstyle N/A"，本 plan 以构建默认检查覆盖）
- [x] `node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/nop-metadata-audit-remediation/2026-08-04-1004-3-mr1-p1-fix-batch-1.md --strict` 退出码 0（closure 时）
- [x] `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata-service --severity high` 退出码 0（closure 时）
- [x] `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0

## Deferred But Adjudicated

### P2-MA2-01/02/03 中裁决为 deferred 的项（若有）

- Classification: `watch-only residual` 或 `out-of-scope improvement`（按裁决）
- Why Not Blocking Closure: <<Phase 1 裁决记录的实际理由：影响面评估 / roadmap 规则 1 P2 范畴 / 业务影响可接受；P2-MA2-03 若裁决保留须基于 Oracle DDL 未引号事实与部署实际使用情况>>
- Successor Required: `yes | no`
- Successor Path: <<如需要则填写：MR4 跨维度裁决或后续批次>>

## Non-Blocking Follow-ups

- MR2 归属项清单维持（见 Non-Goals），不因本 plan 提前
- 若 Phase 2 修复揭示 GraphQL 行为差异，记录为 MR2 finding
- 设计文档陈旧引用 `io.nop.metadata.core.dto`（`ai-dev/design/nop-metadata/02-dto-module-restructure-decision.md:73`、`api-dto-spec.md:213` 仍把 core.dto 写成 DTO 归宿）→ 归 MA5.1（设计文档-代码 drift 审计）处理，本 plan 不触碰

## Closure

Status Note: 三个 Phase 全部执行完毕——R1.0 展开（roadmap R1.1/R1.2/R1.3 行 + Deps 引用 + R1.0 done）、P1-MA1-001 xmeta 修复 + GraphQL 回归测试、P2-MA2-01/02/03 全部 model-first 落地 fixed（显式 tagLabels/dataProducts to-many + cascadeDelete + 保留字列 code 改名）；接线验证另发现并修复 topic 非法缺陷（nop_meta_metadata）；全量构建/测试全绿（`-pl nop-metadata -am`），经独立子 agent closure audit 批准后关闭。
Completed: 2026-08-04

Closure Audit Evidence:

- Reviewer / Agent: 独立子 agent（fresh session）
- Audit Session: ses_034540960ffeJYmokSDedIPFF4
- Evidence:
  - A. Plan 一致性：三 Phase Status=completed；Closure Gates 14 项已全部勾选；`node ai-dev/tools/check-plan-checklist.mjs <plan> --strict` 退出码 0（本 Closure 段 Evidence 写入后复核）
  - B. P1-MA1-001：xmeta:7 = api.dto（PASS）；`rg "io.nop.metadata.core.dto" nop-metadata -g "!**/target/**"` 零命中（PASS）；`TestNopMetaSearchGraphQLSchema` 2/0/0/0 行为断言（schema items 元素类型 = g_io_nop_metadata_api_dto_SearchHitDTO 且可解析 + 真实 Lucene 引擎 e2e total=0/limit=10/items=[]）；topic 修复 `NopMetaSearchService.TOPIC="nop_meta_metadata"` + BizModel 用常量（PASS）
  - C. P2-MA2-01/02/03：orm.xml 显式 to-many 三处（cascadeDelete=true + displayName 双语 + tagSet="pub,ref-pub" + join 正确）；列 code 改名 IS_PRIMARY/CONSTRAINT_NAME；`_app.orm.xml`/deploy/sql 三方言 DDL/i18n yaml/xmeta 生成物全部核对一致（PASS）；`TestNopMetaTagLabelReverseNavigation` 5/0/0/0、`TestNopMetaBusinessDomainDataProductCrud` 8/0/0/0、`TestNopMetaReservedWordColumns` 2/0/0/0（全部 DB-backed 行为断言）
  - D. Anti-Hollow：GraphQL 测试走真实 IGraphQLEngine + LuceneSearchEngine（非 mock）；反向集合测试走 runInNewSession DB-backed 重载 + cascade 区分性断言；变更源文件无空方法/吞异常；`scan-hollow-implementations.mjs --module nop-metadata-service --severity high` = 0 findings（PASS）
  - E. 绿色基线：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS（31.4s）；`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS 0 failures（3:19；含 4 个新测试类 17 用例全绿）（PASS）
  - F. 无静默降级：arm-index P1-MA1-001=fixed、P2-MA2-01/02/03=fixed；roadmap R1.0-R1.3 全部 done；Deferred 区无 in-scope defect 伪装（PASS）
  - `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0（0 errors）

Follow-up:

- P2-MA3-01（NopMetaSearch.xmeta 位于 model/ 扫描根之外不可达）→ MR2 处理（本 plan 已实证确认：GraphQLBizModels 仅扫描 `<module>/model/` 下 xmeta，路径 5 段限制；修复后 schema 仍由 DTO 驱动）
- 设计文档陈旧引用（`ai-dev/design/nop-metadata/02-dto-module-restructure-decision.md:73`、`api-dto-spec.md:213` 仍把 core.dto 写成 DTO 归宿）→ MA5.1
- 旧 topic `nop-meta-metadata` 索引孤儿数据需经 `rebuildSearchIndex` 全量重建迁移（部署侧操作项）
- 其余 no remaining plan-owned work
