# MR2 P1 批量修复（第二批：运行时 + 工程）

> Plan Status: completed
> Last Reviewed: 2026-08-04
> Draft Review: 3 轮独立子 agent 对抗性审查通过（第 1 轮 7 Major + 3 Minor 全部修复；第 2 轮 10 处修复 PASS + 6 Minor 全部修复；第 3 轮最终验证 0 Blocker / 0 Major / 3 Minor，其中 3 Minor 全部修复，裁定可执行）。Session: ses_0344214ebffe8fxesJ7fX04Z6i / ses_034396931ffesjFYAom0v7iDBX / ses_03430426affeQ1s8c6hoJKwe5z。
> Mission: nop-metadata-audit-remediation
> Work Item: MR2（R2.0 展开器 + R2.x 修复执行）
> Source: `ai-dev/backlog/nop-metadata-audit-remediation-roadmap.md`（MR2 里程碑）、`ai-dev/audits/arm-index-nop-metadata.md`（P1 汇总）、`ai-dev/audits/2026-08-04-1136-arm-MA3.1-nop-metadata-xdsl.md`（P1-MA3-001/002）、`ai-dev/audits/2026-08-04-1355-arm-MA4.4-nop-metadata-test-coverage-rest.md` + `2026-08-04-1415-arm-MA4.6-nop-metadata-test-effectiveness-core.md`（P1-MA4-401/701、P1-MA4-601）、`ai-dev/audits/arm-unclosed-findings-nop-metadata.md`（历史 P1/P2 归属）
> Related: 执行顺序 `{3}` of 3 — 启动门禁：MA3+MA4 全部 done（roadmap R2.0 Deps = MA3+MA4 done，已满足）、MR1 completed；本 plan 是第二个**代码变更**计划（roadmap 规则 3：修复 plan 产物 = 代码变更 + 测试），沿用 MR1（2026-08-04-1004-3）的展开-裁决-修复-验证模式。

## Purpose

承接 MA3+MA4 审计产出的第二批 P1 修复：通过 R2.0 展开器把 MA3+MA4 的 P1 发现（含历史审计归入 MR2 的 P1 项与待裁决 P2 项）汇总排序并展开为 roadmap 具体修复工作项行，然后执行 R2.x 修复（xwf 审批流可用性、approve/reject 遮蔽、空洞测试改造、历史 P1 残余），全程保持绿色基线，经独立 closure audit 后收口。

## Current Baseline

经 2026-08-04 live repo 核对（引用均与 roadmap/arm-index 一致，已二次确认文件与计数）：

- roadmap MR2 两行（R2.0/R2.x）状态为 `todo`；**Deps（MA3+MA4 done）已满足**；MR1 completed（其 Follow-up 登记 P2-MA3-01 归 MR2）；MA5/MA6/MA7 尚未审计（本 plan 不依赖）
- **MR2 修复清单（arm-index §P1 open 项 + 历史归入 MR2 项，全部 live 核实）**：**P1 必修 4 项** = P1-MA3-001（xwf 审批流不可用）、P1-MA3-002（approve/reject 遮蔽）、P1-MA4-401/701（judgeByRuleId 空洞测试）、P1-MA4-601（18 个空壳测试）+ 历史 P1 = `2026-07-19-1118#维度20-01`（currentTimeMillis 2 处）；**历史 P2 必修或裁决 4 项** = 07-003（getEntityById 残余）、07-004（DTO 动态 Map）、11-04（xmeta 校验绕过）、07-03（11 参数）——全部经 Phase 1 裁决后落地或记录 deferred 终态
  - **P1-MA3-001**（MA3.1-01/02/03 合并）：3 个 xwf 部署在解析器不可达的 VFS 路径 `/nop/metadata/wf/`（`WorkflowManagerImpl` resolveInDir=`/nop/wf`）→ 工作流启动必然失败；`wf-approval:notifyResult` 未 import 对应 xlib → 模型加载期 ERR_XPL_NOT_ALLOW_UNKNOWN_TAG；qualityBreachApproval 两个 listener 用 xdef 未定义 `appState` 属性 → ERR_XDSL_ATTR_NOT_ALLOWED——**3 条审批流全部不可用**
  - **P1-MA3-002**（MA3.1-07）：NopMetaDataContract Java `approve`/`reject` 被 approval-support XPL 遮蔽（BizObjectBuildHelper merge 优先级），状态生命周期 DRAFT→ACTIVE→DEPRECATED→RETIRED 经 GraphQL 不可达——**单一事实源裁定 + 正路径测试**。**裁决输入（MA3.1 报告建议方向）**：(a) MA3.1 建议"倾向保留工作流 XPL 路径，删除 Java approve/reject（+ 接口方法）或把生命周期逻辑移入保留层 xbiz override（参照 NopMetaTagLabel.xbiz 做法）"；(b) 若删 Java 实现，`INopMetaDataContractBiz` 接口方法同步删除属跨层公共接口变更（Protected Area，须按 plan-first 声明，本 plan 即裁决载体），**且契约测试 `TestNopMetaBizInterfaceCompleteness:83-86` 程序化钉死 `INopMetaDataContractBiz.approve/reject` 声明——删接口必须同步更新该断言，并补 XPL 事实源的正路径语义断言（不得顺手删除断言替代）**；(c) 本裁定与 MA3.1-08（`_NopMetaDataContract.xbiz`/`_NopMetaTagLabel.xbiz` 生成文件被手改加 approval-support x:extends，生成边界违规）**强耦合**——移除 extends 即恢复 Java 为事实源，保留 XPL 事实源则须把 extends 移入保留层，两者不能独立裁决（**MA3.1-08 的 P2 裁决从属于本裁定结果**）；(d) `TestNopMetaTagLabelApprovalIntegration` 已用 GraphQL 正路径钉死 TagLabel 的 XPL 行为（MA3.1-09），TagLabel 侧修复不得破坏该测试
  - **P1-MA4-401/701**（同一 finding，MA4.4/MA4.7 双报告）：judgeByRuleId 测试为空洞断言（`assertNotNull(resp)` + ruleId 用不存在的 `__not_exist__`），核心逻辑改错测试仍通过——**重写为行为断言（status 语义 + 错误码）**
  - **P1-MA4-601**：6 个 AggregationProcessor 测试类 18 个空壳测试（instanceof/canInstantiate/NPE-on-null），execute() 改错全部仍通过——**最小行为测试改造**；同源 P2-MA4-301（3 个 JOIN AggregationProcessor 空壳）在 Phase 1 裁决
  - **`2026-07-19-1118#维度20-01`**：`System.currentTimeMillis()` 违反 DDD-006 锚点，残余 2 处（`nop-metadata-dao/.../OrmModelImporter.java:58,68`）——机械修复
  - **`2026-07-23-0714#维度07-003`**：`dao().getEntityById()` 替代 `requireEntity()` 残余——**精确口径以 MA2.3 报告为准：共 18 处 / 12 文件，分三类**——A 类合法豁免 2 处（`NopMetaTableBizModel:89`/`NopMetaModuleBizModel:128` 的 CREATE 快照，`id!=null` 守卫，**不能改**）、B 类 @BizMutation 跨实体 6 处、C 类 save override 校验 10 处（`TableJoin/TableMeasure/TableDimension/EntityField/Tag/TagLabel`，属"save override 新模式"，跨实体校验查找**不能无脑换成主实体 requireEntity**）——**实际处置 B+C = 16 处 / 11 文件；修复路径 = 收敛共享 helper 或登记豁免清单，按三类分别处置**（MA2.3 第 8 项建议）
  - **`2026-07-23-0714#维度07-004`**（**P2，必修或裁决**）：DTO 内 `List<Map<String,Object>>` 未类型化（QueryJoinDataResultDTO/AggregationResultDTO.items）——**裁决关键输入（MA4.2 复核结论）：已存在的 `AggregationRowDTO` 全仓库零引用且与执行器实际产出的扁平 alias-key 行结构不匹配——若走强类型路线须先定行结构（Phase 1 行结构裁定），不能直接复用现成 DTO**；契约影响面由 R2.0 裁决
  - **`2026-07-23-0714#维度11-04`**（**P2，必修或裁决**）：computeQualityScore 绕过 xmeta insertable 验证——computeQualityScore 本身已修复（NopMetaQualityScoreBizModel:33-62），绕过本质存活于 `QualityResultWriter.java:40-52` 的 `resultDao.saveEntity(row)`（结果表路径，规则路径 NopMetaQualityRuleBizModel:351-353 + 检查点路径 MetaQualityCheckpointExecutor:122-137 双入口；xmeta 侧 mandatory/dict/fail-fast 校验被跳过）——**修复方向性选项（Phase 1 裁决，deferred 须记录 Why Not Blocking Closure）**：(a) QualityResultWriter 落盘前做 status 字典/fail-fast 显式校验（参照 xmeta 校验语义；QualityResultWriter 为 service 层共享 helper，不持有 xmeta 上下文，故为显式字段校验而非走 xmeta 管线）或 (b) 裁决 deferred（记录 Why Not Blocking Closure）
  - **`2026-07-21-2039#维度07-03`**（**P2，必修或裁决**）：queryAggregation 11 参数未用 @RequestBean（live 核实签名仍 11 参数）——**契约影响面三件套（MA3.2 复核记录）：`INopMetaTableBiz.java:75-85` 镜像签名、`TestNopMetaBizInterfaceCompleteness:54` 程序化钉死 11 参契约（`assertDeclaresMethod(..., "queryAggregation", 11)`）、GraphQL rpc 参数结构——改造必须三件套同步（接口签名 / 契约测试 / GraphQL 兼容）；Phase 1 裁决是否允许改接口签名，结局路径：允许 → 三件套同步落地；不允许 → 登记 deferred + Why Not Blocking Closure 并同步调整 Exit Criteria（参照 11-04 降级措辞）**
- **MR2 P2 裁决候选（arm-index P2 表归 MR2 项 + MA4 跨批次登记，Phase 1 逐项裁决）**：P2-MA3-01（NopMetaSearch.xmeta 位于 model/ 扫描根之外不可达 + 假 javadoc，MR1 Follow-up 显式归本 plan）、P2-MA3-02（entity 路径数据查询绕过 data-auth 过滤合并：queryTableData/queryJoinData/queryAggregation 裸 DAO/EQL）、P2-MA3-03（upsertExternalTable schema 维度未进 DB UK——需 ORM 模型变更，plan-first）、P2-MA4-001/002（静默吞异常 2 处）、P2-MA4-101（CheckpointExtConfig 强类型 DTO 生产死代码）、P2-MA4-301（3 个 JOIN AggregationProcessor 空壳）、P2-MA4-303（分页测试只验行数不验行集）、P2-MA4-501（`*Service` 命名违规 2 处：NopMetaSearchService 复发 + QualityAlertWorkflowService）、P2-MA4-602（helper 镜像测试跨 4 文件重复 30+ 方法）
- **其他裁决候选（MA3/MA4 跨批次登记，Phase 1 逐项裁定归属）**：MA4.5 版权头剥离（154 文件整域风格，MA4.5 裁定"归 MR2/决策层"）、AutoTest 快照增量（16-01，MA4.3/4.4 建议 MR2 增量，建议至少覆盖本 plan 触碰的关键流）、MA3.1-04/05/06/08（xwf 相关 P2：start 悬空、WfRuntime 属性、不存在的 API、生成 xbiz 手改 x:extends——**与 P1-MA3-001/002 同文件，裁决是否随 P1 一起修复**）；**明确不在候选：MA4.7 非阻塞 P2（P2-MA4-702 常量镜像断言、P2-MA4-703 TestNopMetaDtoResults 低价值）——审计报告已裁定非阻塞，不入本 plan 候选**（closure 时防止"P2 候选不全"质疑）
- **修复模式约束**：xwf/xbiz/XDSL 修改走保留层或源模型（AGENTS.md Hard Stop：禁止手编 `_gen/` 产物；MA3.1-08 生成边界违规的修复即把 x:extends 从生成文件移到保留层/源模型）；P2-MA3-03 若裁决修复需 ORM 模型变更（Protected Area，plan-first——本 plan 即裁决载体）且走 model-first（改 `model/*.orm.xml` → 重新生成 → 核对 `_gen/` 与 deploy/sql DDL）；P1-MA3-002 若删 Java 实现涉及 `INopMetaDataContractBiz` 接口变更（Protected Area，同 plan-first 声明）
- **xwf 端到端验证容器可行性（live 核实）**：nop-wf-service 在 nop-metadata-service 中是 compile scope（pom.xml:41），其 beans 理论上会在测试容器加载；但**仓库中无任何在 nop-metadata 容器内启动真实工作流的先例**（TestQualityAlertWorkflowServices 纯单元测试、TagLabelApproval 集成测试不经引擎）——端到端段必须先验证容器内 wf engine bean 可用性，不可用时降级：注册 test-scope bean 或参照 nop-wf-service 自身测试容器配置模式（`nop-wf-service/src/test/resources/_vfs/nop/wf/test/`），并记录降级理由（不得静默省略）
- 绿色基线：813 tests / 0 failures（M0.4 口径，2026-08-04 实测；MR1 closure 后新增 4 测试类 17 用例，当前实测约 830，以重新实测记录为准；范围 `-pl nop-metadata -am -T 1C`）

## Goals

- R2.0：汇总 MA3+MA4 P1 发现 + 历史归入 MR2 的 P1 项并排序，向 roadmap 追加 R2.x 具体修复工作项行（引用 `P<级别>-<里程碑>-<序号>` 或轮次限定 ID 作为 Deps，含本 plan 覆盖与后续批次的边界声明）
- 修复 MA3+MA4 P1 项（P1-MA3-001/002、P1-MA4-401/701、P1-MA4-601）+ 历史归入 MR2 的 P1 项（20-01）——**必修**，每项带回归测试（Test-Mandated Feature Rule）；历史 P2 项（07-003/07-004/11-04/07-03）经 Phase 1 裁决后落地（每项带测试）或记录 deferred 终态（Why Not Blocking Closure）
- P2 候选（P2-MA3-01/02/03、P2-MA4-001/002/101/301/303/501/602、MA4.5 版权头、AutoTest 增量、MA3.1-04/05/06/08）逐项裁决（修复 or 维持 P2 deferred + Why Not Blocking Closure），裁决修复的走对应落地路径
- 绿色基线保持（build + test 全绿）+ 独立子 agent closure audit 通过

## Non-Goals

- 不修复 MR3 归属项：MA5/MA6/MA7 审计发现（尚未产出）
- 不执行 MA5-MA7 审计（后续计划）
- 不处理 P3（deferred successor，roadmap 规则 1）
- 不重做 MR1 已修复项（P1-MA1-001 等，仅在接线验证中发现回归时上报）

## Scope

### In Scope

- R2.0：MA3+MA4 P1 发现 + 历史归入 MR2 的 P1 项汇总排序、P2 候选裁决、roadmap 追加 R2.x 行（含 Deps 引用）
- R2.x：P1 必修项修复执行（P1-MA3-001/002、P1-MA4-401/701、P1-MA4-601、20-01）+ 历史 P2 项按裁决落地（07-003/07-004/11-04/07-03）
- R2.x：P2 裁决修复（若裁决 in-scope：xwf 关联 P2 随 P1 修复、P2-MA3-01/02/03、P2-MA4-001/002/101/301/303/501/602）
- 绿色基线验证 + 独立 closure audit

### Out Of Scope

- MR3/MR4 修复（后续批次）
- 任何审计（MA5-MA7）
- `docs-for-ai/` 文档修改（MA5 覆盖；本 plan 若改变行为则按 Exit Criteria 声明）

## Execution Plan

### Phase 1 - R2.0 P1 发现展开器

Status: completed
Targets: `ai-dev/backlog/nop-metadata-audit-remediation-roadmap.md`（MR2 状态表）+ `ai-dev/audits/arm-index-nop-metadata.md`

- Item Types: `Decision`

- [x] **启动门禁核查**：确认 MA3+MA4 全部 done（roadmap MA3 四行 + MA4 七行 done）且 MR1 completed；未满足则不启动并上报
- [x] 汇总 MA3+MA4 审计报告的 P1 级发现（P1-MA3-001、P1-MA3-002、P1-MA4-401/701、P1-MA4-601）+ 历史归入 MR2 的 P1/P2 项（20-01、07-003、07-004、11-04、07-03），逐一 live 核对现状与影响面（证据手段：`rg -n` 定位残余 + 读代码确认语义），确认无遗漏、无已修复误标
- [x] 对 P2 候选逐项裁决（Decision）：P2-MA3-01/02/03、P2-MA4-001/002/101/301/303/501/602、MA3.1-04/05/06/08（xwf 关联，与 P1-MA3-001/002 同文件）、MA4.5 版权头剥离（154 文件）、AutoTest 增量（16-01）；裁决为（a）本 plan 修复（Phase 2/3 落地）或（b）维持 P2 deferred（记录明确 Why Not Blocking Closure + 业务影响评估）；**裁决依据写入 roadmap R2.x 行；本裁决通道沿用 MA2.1 先例（审计报告显式裁决例外），roadmap R2.x 行注明，防后续 MR 把"修 P2"当默认先例**
- [x] **P1-MA3-002 单一事实源裁定（Decision）**：approve/reject 逻辑定位于 Java BizModel or XPL 单一事实源；**裁定基于 Current Baseline 四条裁决输入**（MA3.1 建议方向（删 Java + 接口方法 or 保留层 xbiz override）、接口删除的 Protected Area 声明、与 MA3.1-08 的耦合（extends 移入保留层）、TestNopMetaTagLabelApprovalIntegration 约束），裁定结果写入 roadmap 行与设计记录
- [x] **11-04 修复裁决（Decision）**：基于 Current Baseline 方向性选项（(a) QualityResultWriter 落盘前 status 字典/fail-fast 显式校验（参照 xmeta 校验语义）或 (b) deferred + Why Not Blocking Closure）裁决修复路径，写入 roadmap 行
- [x] **07-03 契约影响面裁决（Decision）**：基于 Current Baseline 三件套（INopMetaTableBiz 镜像签名 / TestNopMetaBizInterfaceCompleteness:54 契约测试 / GraphQL rpc 兼容）裁定是否允许改接口签名及同步范围，写入 roadmap 行
- [x] **07-004 行结构裁定（Decision）**：基于 MA4.2 复核结论（AggregationRowDTO 结构不匹配不可复用）裁定类型化方案（先定行结构再类型化）或维持 deferred + Why Not Blocking Closure，写入 roadmap 行
- [x] **P1-MA4-601 空壳测试改造范围裁定（Decision）**：18 个空壳测试的最小行为测试改造方案（断言 execute() 真实输出/分派语义而非 instanceof），同源 P2-MA4-301 一并裁定
- [x] **MA3.1-08 生成边界裁决（Decision）**：x:extends 移入保留层/源模型的处置**从属于 P1-MA3-002 裁定结果**（见 Current Baseline (c)：移除 extends = Java 事实源，保留 extends = XPL 事实源须移层），不独立裁决
- [x] 向 roadmap 追加 R2.x 具体修复工作项行（每行引用 finding ID 作为 Deps），并更新 MR2 状态表（R2.0 → done）
- [x] 更新 arm-index：P1 项修复状态与归属同步、P2 候选裁决结果登记

Exit Criteria:

> 每个 Phase 完成后，必须逐条勾选本节。所有 `[x]` 后才能将 Phase Status 改为 `completed`。

- [x] roadmap MR2 段出现具体 R2.x 行（引用 `P<级别>-<里程碑>-<序号>` 或轮次限定 ID Deps），R2.0 → done
- [x] 全部 P2 候选与裁定项（P1-MA3-002、11-04、07-03、07-004 行结构、P1-MA4-601、MA3.1-08）均有裁决结论（修复 or deferred + 理由），裁决记录可追溯（roadmap 行或 arm-index）
- [x] arm-index P1 追踪已同步（MR2 归属项标 in-progress）
- [x] 文档变化：roadmap + arm-index 更新；`No owner-doc update required`（docs-for-ai 不变）
- [x] `ai-dev/logs/` 对应日期条目已更新

### Phase 2 - R2.x P1 修复执行（运行时 + 工程）

Status: completed
Targets: `nop-metadata/nop-metadata-service/src/main/resources/_vfs/`（xwf/xbiz/xmeta）+ `nop-metadata/nop-metadata-service/src/main/java/io/nop/metadata/service/` + `nop-metadata/nop-metadata-dao/` + 对应测试

- Item Types: `Fix | Proof`

- [x] **先复现再修复**：修复前先对目标缺陷跑复现（xwf：加载 xwf 模型验证不可达/标签错误；approve/reject：GraphQL 查询验证遮蔽；空洞测试：证明改错逻辑测试仍通过），记录 pre-fix 基线（MA3.1/MA4 报告为假设，以实测为准）
- [x] P1-MA3-001 修复：3 个 xwf 迁移至解析器可达路径（`/nop/wf` 或 resolveInDir 覆盖范围）+ listener 重写为 c:script（内置 taglib，无 xlib import 依赖；MA7.6-06 修正原「补 `wf-approval` xlib 的 x:config import」描述）+ 删除 `appState` 非法属性；**生成边界遵守**（xwf 为手写保留层文件，直接修改；若涉及生成物则回指源模型）
- [x] P1-MA3-002 修复：按 Phase 1 单一事实源裁定落地 approve/reject（Java BizModel 或 XPL 择一），消除遮蔽；状态生命周期 DRAFT→ACTIVE→DEPRECATED→RETIRED 经 GraphQL 正路径可达
- [x] P1-MA4-401/701 修复：judgeByRuleId 测试重写为行为断言（status 语义 + 错误码；ruleId 用真实存在值），证明逻辑改动会被测试捕获
- [x] P1-MA4-601 修复：6 个 AggregationProcessor 测试类 18 个空壳测试改最小行为测试（按 Phase 1 裁定方案）
- [x] 历史 P1/P2 项修复：20-01（OrmModelImporter.java:58,68 currentTimeMillis → 时钟注入/锚点时间，**回归保护 = 扩展 `TestCoreMetricsUsage` 扫描范围至 `nop-metadata-dao/src/main`**——MA4.4 记录其当前只扫 service/src/main，不扩展则 dao 修复无回归保护）、07-003（按 Phase 1 三类处置：A 类豁免 2 处不动 + B 类 6 处改 requireEntity/等价 + C 类 10 处收敛共享 helper 或登记豁免，数据鉴权语义保持）、07-004（QueryJoinDataResultDTO/AggregationResultDTO.items 类型化，按 Phase 1 行结构裁定落地；**不得直接复用结构不匹配的 AggregationRowDTO**）、11-04（按 Phase 1 裁决：QualityResultWriter 落盘前 status 字典/fail-fast 显式校验或 deferred）、07-03（queryAggregation @RequestBean 改造，按 Phase 1 契约影响面裁决三件套同步）
- [x] 为每个落地修复补测试（Test-Mandated Feature Rule）：xwf 正路径（工作流模型加载 + 启动/审批流转，**端到端前提 = MA3.1-04/05/06 裁决 in-scope，见 Phase 2 依赖声明**）、approve/reject GraphQL 正路径、judgeByRuleId 行为断言、processor execute() 行为断言、requireEntity 数据鉴权、时钟注入（含 TestCoreMetricsUsage 扩展）、DTO 类型化编译期/序列化断言
- [x] **Phase 2 依赖声明**：P1-MA3-001 的"启动/审批流转端到端"以 MA3.1-04/05/06 裁决 in-scope 为前置（start 悬空 → ERR_WF_UNKNOWN_STEP、`${wfRt.status}` 不存在 → ERR_EXEC_UNKNOWN_PROP 是修复后直接启动/运行失败点）；**若 Phase 1 裁决 04/05/06 deferred，Phase 2 端到端范围收敛为模型加载 + 已裁决 in-scope 的流程，并在 Exit Criteria 记录经裁决的降级理由（不得静默省略）**；xwf 端到端先验证容器内 wf engine bean 可用性（见 Current Baseline 容器可行性），不可用时按降级链处理
- [x] 运行 `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` 后运行 `./mvnw test -pl nop-metadata -am -T 1C`
- [x] 绿色基线保持：813/0（或修复后重新实测记录）

Exit Criteria:

> 每个 Phase 完成后，必须逐条勾选本节。所有 `[x]` 后才能将 Phase Status 改为 `completed`。

- [x] 全部 P1 必修项 landed（P1-MA3-001/002、P1-MA4-401/701、P1-MA4-601、20-01）+ 历史 P2 项按裁决落地或记录 deferred 终态（07-003/07-004/11-04/07-03），无悬置
- [x] **端到端验证**：xwf 审批流从模型加载到流程启动/审批转发的完整路径已跑通（正路径测试；**范围与降级条件见 Phase 2 依赖声明——MA3.1-04/05/06 若裁决 deferred，端到端范围收敛为模型加载 + in-scope 流程并记录降级理由**）；approve/reject 经 GraphQL 入口到状态迁移的完整路径已验证
- [x] **接线验证**：修复后 xwf 经 `WorkflowManagerImpl` 解析链可达（非仅文件移动）；approve/reject 单一事实源在运行时被调用（GraphQL → BizModel/XPL → 状态迁移断言）
- [x] **无静默跳过**：修复项无空方法体/吞异常/continue 跳过；未实现的裁决 deferred 项有明确失败语义（如抛异常）或已移出 scope
- [x] 每个修复项有对应行为测试（验证正确结果而非仅无错误），pre-fix 复现记录可追溯
- [x] `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` + `./mvnw test -pl nop-metadata -am -T 1C` 均 BUILD SUCCESS，测试 0 failures
- [x] 文档变化：若修复改变 public contract（如 DTO 类型化、@RequestBean 签名），同步 `docs-for-ai/` 对应 owner doc 或记录 finding；否则写 `No owner-doc update required`
- [x] `ai-dev/logs/` 对应日期条目已更新

### Phase 3 - R2.x P2 裁决修复（若 Phase 1 裁决 in-scope）

Status: completed
Targets: 按 Phase 1 裁决确定的文件集合（xwf/xbiz/xmeta/Java/测试/orm.xml）

- Item Types: `Fix | Decision | Proof`

- [x] 按 Phase 1 裁决执行（若全部裁决 deferred，本 Phase 只记录裁决文档，标记为纯 Decision 完成）：
  - [x] xwf 关联 P2（MA3.1-04/05/06/08，若裁决随 P1 修复）：start 悬空步骤、WfRuntime 属性、监听器 API、生成 xbiz 手改 x:extends 的生成边界修复（x:extends 从生成文件移到保留层/源模型）
  - [x] P2-MA3-01（若裁决 in-scope）：NopMetaSearch.xmeta 移动至 model/ 扫描根可达位置 + 假 javadoc 修正（MR1 Follow-up 显式归本 plan）
  - [x] P2-MA3-02（若裁决 in-scope）：queryTableData/queryJoinData/queryAggregation 数据鉴权过滤合并（参照 CrudBizModel 数据权限语义）——**Phase 1 裁决 deferred（R2.14：裸 DAO/EQL 路径合并需跨 3 executor 查询管线设计；enable-data-auth 双开关默认 false + app 未配置无实际暴露；Successor MR4）**
  - [x] P2-MA3-03（若裁决 in-scope）：**model-first 流程**（Protected Area）——`nop-metadata/model/nop-metadata.orm.xml` UK 扩展（metaSchema 维度，需处理 null 语义）→ `./mvnw clean install -DskipTests` 重新生成 → 核对 `_gen/` 产物与 deploy/sql DDL；或裁决 deferred（记录 Why Not Blocking Closure）——**Phase 1 裁决 deferred（R2.15：ORM UK 变更 plan-first + 单 schema 部署无影响；Successor MR3/DDL 管线）**
  - [x] P2-MA4-001/002（若裁决 in-scope）：2 处静默吞异常补 LOG.warn（NopMetaTagLabelBizModel.getWfNameFromMeta、SqlViewFieldTypeInferrer.safeProductName）
  - [x] P2-MA4-101（若裁决 in-scope）：CheckpointExtConfig 死代码处置（消费方改 parseBeanFromText 或撤 DTO javadoc 声明）
  - [x] P2-MA4-301（若裁决 in-scope）：3 个 JOIN AggregationProcessor 空壳测试最小行为测试（与 P1-MA4-601 同源方案）
  - [x] P2-MA4-303（若裁决 in-scope）：分页测试补首行内容断言
  - [x] P2-MA4-501（若裁决 in-scope）：`*Service` 命名违规 2 处改名（NopMetaSearchService + QualityAlertWorkflowService，随 02-01 历史归属）——**Phase 1 裁决 deferred（R2.16：改名涉及 beans.xml bean id + 19 文件引用 + xwf c:script import 全量同步，纯命名规范项无功能影响；Successor MR3 命名治理批次）**
  - [x] P2-MA4-602（若裁决 in-scope）：helper 镜像测试收敛为单一 helper 测试文件——**Phase 1 裁决 deferred（R2.17：纯测试重组；P1-MA4-601 修复后残余镜像无功能风险；Successor MR3 测试治理批次）**
  - [x] MA4.5 版权头剥离 / AutoTest 增量（若裁决 in-scope）：按 Phase 1 裁定范围执行——**Phase 1 裁决均 deferred（R2.18 版权头 154 文件独立批次、R2.19 AutoTest 快照需专门录制会话；Successor MR3）**
- [x] 为每个落地修复补测试（Test-Mandated Feature Rule），区分性断言（验证修复新增语义而非修复前已存在行为）
- [x] 运行 `./mvnw test -pl nop-metadata -am -T 1C`，绿色基线保持

Exit Criteria:

> 每个 Phase 完成后，必须逐条勾选本节。所有 `[x]` 后才能将 Phase Status 改为 `completed`。

- [x] 每个 P2 候选有明确终态（fixed + 测试 / deferred + Why Not Blocking Closure），无悬置
- [x] 修复项落地路径合规：xwf/xbiz 走保留层或源模型，ORM 变更走 model-first（无手编 `_gen/` 产物，P2-MA3-03 含 deploy/sql DDL 核对）
- [x] 每个修复项有对应行为测试（验证正确结果而非仅无错误）
- [x] **接线验证**（如适用）：修复项在运行时调用链连通（如 data-auth 过滤在查询路径生效、命名改名后引用全量同步）
- [x] **无静默跳过**：新增/修改代码无空方法体/吞异常/静默返回
- [x] `./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS，测试 0 failures
- [x] 文档变化：若修复改变 public contract，同步对应 owner doc；否则写 `No owner-doc update required`
- [x] `ai-dev/logs/` 对应日期条目已更新

## Closure Gates

- [x] R2.0 展开完成（roadmap R2.x 行 + Deps 引用 + R2.0 → done）
- [x] 全部 P1 必修项 landed（P1-MA3-001/002、P1-MA4-401/701、P1-MA4-601、20-01）+ 历史 P2 项（07-003/07-004/11-04/07-03）按裁决落地或记录 deferred 终态，各带行为回归测试
- [x] P2 候选全部终态明确（fixed 或 deferred + 理由）
- [x] 所有 in-scope confirmed live defects 已修复（P1 项不可降级 deferred）
- [x] 不存在被静默降级到 deferred / follow-up 的 in-scope live defect 或 contract drift
- [x] 受影响的 owner docs 已同步到 live baseline，或明确写明 `No owner-doc update required`
- [x] 独立子 agent / 独立审阅者 closure-audit 已完成并记录证据（evidence 写入本 plan Closure 段）
- [x] **Anti-Hollow Check**：closure audit 已验证（a）xwf/approve-reject 等修复的运行时调用链真实连通（非仅文件存在）、（b）空洞测试改造后逻辑改动会被捕获（行为断言）、（c）无空方法体/静默跳过/no-op 作为正常实现
- [x] `./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C`
- [x] `./mvnw test -pl nop-metadata -am -T 1C`
- [x] checkstyle / 代码规范检查通过（nop-metadata 无独立 checkstyle 命令，以 mvn 构建默认检查为准；历史计划惯例记 "checkstyle N/A"）
- [x] `node ai-dev/tools/check-plan-checklist.mjs <本plan文件> --strict` 退出码 0（closure 时）
- [x] `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high` 退出码 0（closure 时；**工具 --module 需整组目录 `nop-metadata` 或嵌套路径，顶层 `nop-metadata-service` 路径无效（空转返回 0），并附 --severity info 阳性对照证明扫描真实执行**）
- [x] `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0

## Deferred But Adjudicated

### R2.0 中裁决为 deferred 的 P2 候选（若有）

- Classification: `watch-only residual` / `out-of-scope improvement`（逐项）
- Why Not Blocking Closure: 见 roadmap R2.7/R2.9/R2.14~R2.19 行 + arm-index §P2（每项均有业务影响评估与 successor）
- Successor Required: `yes`（R2.7/R2.9 → MR4；R2.14 → MR4/专门 data-auth plan；R2.15 → MR3/DDL 管线；R2.16/R2.17/R2.18/R2.19 → MR3 对应治理批次）
- Successor Path: MR3 批量修复或 MR4 跨维度裁决（roadmap 对应行）

## Non-Blocking Follow-ups

- MR3 归属项清单维持（见 Non-Goals），不因本 plan 提前
- 若 Phase 2 修复揭示行为差异（如 xwf 流程语义、approve/reject 契约），记录为 MR3 finding
- 旧 topic `nop-meta-metadata` 索引孤儿数据重建（部署侧操作项，MR1 Follow-up 延续，非本 plan 代码范围）
- **nop-auth-web 页面渲染测试（TestFluxPage.testFluxViewControls / TestWebPage.testViewControls）在含未提交 frontend 变更（nop-app-erp flux-crud-migration 任务：control.xlib/flux-control.xlib/index.html + assets）的工作树中失败；clean HEAD 工作树实测通过（独立 worktree 验证）——与本 plan 无关，归该任务处理；本 plan 验证以 clean 基线（stash 无关变更）实测全绿**

## Closure

Status Note: 全部 Phase 完成（Phase 1 R2.0 展开 + Phase 2 R2.x P1 修复 + Phase 3 R2.x P2 裁决修复），独立 closure audit APPROVED
Completed: 2026-08-05

Closure Audit Evidence:

- Reviewer / Agent: 独立 closure-audit 子 agent（task ses_030eb461dffepouUpXXGpQ5QDB，2026-08-05）——首轮 12 项核查 10 PASS / 2 FAIL（FAIL-1 MA3.1-08 生成文件"未恢复标准模板"、FAIL-2 Plan Status 未改 completed）；FAIL-2 已修正（Plan Status → completed）；FAIL-1 经证据复核**重新裁定**：codegen 模板 `_{entityModel.shortName}.xbiz.xgen`（nop-kernel/nop-codegen，commit 270b2b536）对 use-approval 实体自动产出 approval-support extends，ORM 模型 tagSet="use-approval"（nop-metadata.orm.xml:2458/:3264）为事实源；本 plan 执行期手工移除后经 `mvn clean install` 被 codegen 恢复（git diff 归零实证）——MA3.1-08 "codegen 不产出该 extends" 前提不成立，生成文件维持生成产物原状即正确终态（非手改违规）
- Audit Session: 见日志 ai-dev/logs/2026/08-04.md §MR2 closure（独立子 agent 核查记录）
- Evidence:
  - **Phase 1（R2.0）**：roadmap v9 出现 R2.1~R2.19 行（Deps 引用 P<级别>-<里程碑>-<序号>），R2.0 → done；全部 P2 候选与裁定项有裁决结论（修复 or deferred + 理由），裁决记录可追溯（roadmap 行 + arm-index §P2）
  - **Phase 2（R2.1~R2.5 + R2.8）P1 修复**：P1-MA3-001（3 个 xwf 迁移至 `/nop/wf/` 解析器可达路径 + start/属性/API/eventPattern 修复 MA3.1-04/05/06，listener 重写为 c:script（无 xlib import 依赖；MA7.6-06 修正原「x:config import」描述），模型加载 + 启动/审批流转 e2e 通过 TestNopMetadataWorkflowModels 全链路测试）；P1-MA3-002（approve/reject 单一事实源 = 保留层 XPL，Java + 接口方法删除，契约测试更新 + XPL 正路径 GraphQL 断言，MA3.1-08 处置见下）；P1-MA4-401/701（judgeByRuleId 行为断言：正路径 PASS/actualValue + 负路径错误码）；P1-MA4-601（7 类 21 空壳 → 行为断言：self-join/未注册实体/无数据源/混合 JOIN 空表 错误码）；20-01（OrmModelImporter 2 处 currentTimeMillis → CoreMetrics + TestCoreMetricsUsage 扫描扩展至 dao/src/main）
  - **MA3.1-08 处置（独立 closure audit 复核后重新裁定）**：`_NopMetaDataContract.xbiz`/`_NopMetaTagLabel.xbiz` 的 `x:extends="/nop/wf/base/approval-support.xbiz"` 经实证为**代码生成产物**——codegen 模板 `nop-kernel/nop-codegen/.../_{entityModel.shortName}.xbiz.xgen` 对 `tagSet="use-approval"` 实体自动产出该 extends（commit 270b2b536，2026-07-03，附 TestUseApprovalCodegen 测试），ORM 源模型 `nop-metadata.orm.xml:2458/:3264` 声明 use-approval tag 即事实源；本 plan 执行期曾手工移除该 extends，`mvn clean install` 后即被 codegen 按模板恢复（git diff 归零实证）——**audit 报告 MA3.1-08 "标准 codegen 不产出该 x:extends" 的前提不成立，非手改违规、无生成边界缺陷**；MA3.1-08 处置 = 维持生成产物原状 + 保留层 multi-extends 链（保留层>生成层>approval-support）承载 override（R2.2 目标达成）
  - **历史 P2 项**：07-003（裁决豁免清单，R2.6 done）、07-004（deferred R2.7）、11-04（QualityResultWriter status 字典 fail-fast 校验 + ERR_QUALITY_RESULT_STATUS_INVALID + 单元测试，R2.8）、07-03（deferred R2.9）
  - **Phase 3（P2 裁决修复）**：MA3.1-04/05/06/08 随 P1 修复；P2-MA3-01（xmeta 移入 model/ 可达路径 + javadoc 修正，R2.10）；P2-MA4-001/002（LOG.warn 2 处，R2.11）；P2-MA4-101（CheckpointExtConfig parseBeanFromText 两消费方，R2.12）；P2-MA4-301（随 601 统一行为测试）；P2-MA4-303（分页首行内容断言）；其余裁决 deferred（R2.14~R2.19，Why Not Blocking 全部记录）
  - **Anti-Hollow Check**：(a) xwf 经 WorkflowManagerImpl 解析链可达（TestNopMetadataWorkflowModels 模型加载 + metaDataContractApproval/tagLabelConfirmApproval 端到端启动→审批→end→XPL 回调→状态迁移断言，非仅文件存在）；(b) 空洞测试改造后逻辑改动会被捕获（judgeByRuleId 正路径断言 actualValue、processor 错误码断言均针对 execute() 真实语义）；(c) 新增/修改代码无空方法体/静默跳过（scan-hollow-implementations exit 0 + LOG.warn 补录）
  - **构建验证**：`./mvnw clean install -DskipTests -pl nop-metadata -am -T 1C` BUILD SUCCESS（31s）；`./mvnw test -pl nop-metadata -am -T 1C` BUILD SUCCESS（3:07）；nop-metadata 子树 822 tests / 0 failures / 0 errors / 0 skipped（clean 基线，见上 nop-auth-web 注记）；check-plan-checklist --strict exit 0；scan-hollow-implementations --module nop-metadata --severity high exit 0（附 --severity info 阳性对照）；check-doc-links --strict exit 0；checkstyle N/A（模块无独立命令，mvn 构建默认检查通过）
  - **Deferred 项分类检查**：全部 deferred 项为 P2 候选（roadmap 规则 1 范畴）且均带 Why Not Blocking Closure + Successor，无 in-scope live defect 被降级；confirmed live defect 全部修复（P1 六项）

Follow-up:

- nop-auth-web 页面渲染测试失败归因 nop-app-erp flux-crud-migration 任务的未提交 frontend 变更（非本 plan 范围，clean HEAD 实测通过）
- MR3 归属项清单维持（Non-Goals）；旧 topic 索引孤儿重建为部署侧操作项（MR1 Follow-up 延续）
