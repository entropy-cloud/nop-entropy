# W1-design 双后端设计文档撰写（xlang-execution / xlang-java / xlang-truffle 三组）

> Plan Status: completed
> Last Reviewed: 2026-08-19
> Mission: xlang-execution-optimization
> Work Item: W1-design
> Source: ai-dev/backlog/xlang-execution-optimization-roadmap.md（W1-design 条目）
> Related: 2026-08-19-2050-2-w2-design-review-gate.md（后继：设计审查 gate）

## Purpose

完成 roadmap W1-design 工作项：产出三组设计文档（xlang-execution 统一架构、xlang-java 转译器、xlang-truffle 运行时），使双后端设计达到"内容点全覆盖、跨文档一致、可进入 W2-review 独立审查"的成稿状态。

## Current Baseline

- 知识输入已就绪（roadmap Sources，实施前必读）：
  - `ai-dev/design/xlang-truffle/01-truffle-knowledge.md`（290 行；§4 多线程模型、§7 XLang→Truffle 映射草案、§10 Open Questions）
  - `ai-dev/analysis/2026-08/2026-08-16-truffle-graalvm-ecosystem-research.md`
  - `~/sources/graal`（oracle/graal sparse clone，SL 参考实现）
- 三个设计目录均为占位状态：
  - `ai-dev/design/xlang-execution/README.md` 与 `ai-dev/design/xlang-java/README.md` 显式声明"占位、不承载设计结论"
  - `ai-dev/design/xlang-truffle/` 仅有知识层 01 + README；README 明确过渡态豁免："架构设计启动后，决策与契约必须沉淀到 00/02，01 不得继续承载决策"
- 现解释器基线：`nop-kernel/nop-xlang/src/main/java/io/nop/xlang/exec/` 下 137 个 Executable 节点类
- 复用锚点已存在于 live repo：
  - `nop-kernel/nop-core/src/main/java/io/nop/core/resource/component/ResourceComponentManager.java`（含 `IResourceLoadingCache`）
  - `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/script/ScriptCompilerRegistry.java`
  - `nop-kernel/nop-xlang/src/main/java/io/nop/xlang/janino/JaninoScriptCompiler.java`（EvalMethod 调用约定先例）
  - `nop-kernel/nop-javac/`（JdkJavaCompiler 等 Java 源编译通路）
- 目标模块尚不存在：`nop-kernel` 下无 `nop-xlang-java` / `nop-xlang-truffle`（由阶段二 I1/I3 创建，本 plan 不建模块）
- roadmap 阶段二冻结中（纪律 2）：W4-audit done 前 I 系列不得起草 plan；本 plan 不触碰阶段二条目内容

## Goals

- 三组设计文档全部成稿（新建 5 篇正文 + 更新 3 个 README）：
  - `ai-dev/design/xlang-execution/`：`00-vision.md` + `01-architecture-baseline.md` + README 更新（占位 → 真实索引）
  - `ai-dev/design/xlang-java/`：`01-architecture-baseline.md` + README 更新
  - `ai-dev/design/xlang-truffle/`：`00-vision.md` + `02-architecture-baseline.md` + README 更新 + 01 决策内容迁出收口
- roadmap W1 条目列出的全部内容点逐点覆盖（见各 Phase Exit Criteria 的核对清单）
- `01-truffle-knowledge.md` §10 的 Open Questions 全部在 00/02 中给出答案或显式关闭
- 三组文档决策一致：后端选择机制、模块边界、依赖方向、三后端对拍基准无冲突表述

## Non-Goals

- 不做任何代码/构建变更（不创建 Maven 模块、不写 Java、不改 pom）
- 不执行 W2 的独立审查（后继 plan 的工作）
- 不修订 roadmap 阶段二 I1-I7 的内容（W3-supplement 的工作；阶段二冻结）
- 不改 `docs-for-ai/`（设计实现后的使用规范同步按 roadmap 归 I6）
- 设计文档不包含代码级细节（类签名/字段定义/实现步骤），遵守 `ai-dev/design/00-design-writing-guide.md`

## Scope

### In Scope

- 上列 5 篇新建设计文档 + 3 个 README 更新 + 01 决策迁移收口
- 设计文档间交叉引用与一致性核对
- roadmap `W1-design` 条目状态同步（本 plan 激活时 `planned`；closure audit 通过后 `done`）

### Out Of Scope

- 阶段二 I1-I7 条目内容（冻结中）
- `missions/xlang-execution-optimization.json` 修改（W3 的工作）
- 任何审查报告撰写（W2 的工作）

## Execution Plan

### Phase 1 - xlang-execution 统一架构组（愿景 + 双后端统一架构基线）

Status: completed
Targets: `ai-dev/design/xlang-execution/00-vision.md`（新建）、`01-architecture-baseline.md`（新建）、`README.md`（更新）

- Item Types: `Decision` | `Proof`

- [x] `00-vision.md`：问题定位（native image 内宿主 Java 无 JIT、运行时动态脚本无 JIT 两条痛点）、三后端分工原则（编译期确定→java / 运行时→truffle / 兜底→解释器）、成功标准（含"同一 Executable 树三后端执行结果一致"对拍基准与性能目标口径——只定方向性门槛：java 后端不低于解释器、truffle 后端 GraalVM 形态显著优于解释器；量化数值由 I7 基准计划落定，vision 层不发明具体数字）、显式 non-goals、不可违反约束（依赖方向 `新模块 → nop-xlang → nop-core`、Truffle 依赖不泄漏进内核其他模块）
- [x] `01-architecture-baseline.md`：后端选择机制（判定输入、判定时机、降级路径的决策树）、后端注册 SPI（以 `ScriptCompilerRegistry` 为先例的注册/发现契约，仅到接口契约级）、三后端对拍验证框架（对拍对象、断言口径、纳入回归的方式）、模块边界与依赖方向图（Mermaid 拓扑图）、与 `ResourceComponentManager` 和构建管线的集成边界
- [x] `README.md` 更新：移除占位声明，改为目录索引 + 阅读顺序（必读/按需）+ 各文档职责边界 + AGE 声明（design-writing-guide 子系统目录必备项）

Exit Criteria:

- [x] 两篇正文覆盖 roadmap W1 条目 xlang-execution 组的 4 个内容点：①后端选择机制 ②后端注册 SPI ③对拍验证框架 ④模块边界与依赖方向——逐点在文档中有对应章节
- [x] 每个关键设计决策具备三要素：选了什么 / 为什么 / 拒绝了什么替代方案（design-writing-guide 必须项）
- [x] 无 "Proposed vs Current" 演进叙事；不引用 `ai-dev/analysis/`、`ai-dev/discussions/`（关键结论以独立章节融入本文档）
- [x] 纯文档 phase，无代码变更（No build verification required for this phase）
- [x] `ai-dev/logs/` 当日条目已更新

### Phase 2 - xlang-java 转译器架构组

Status: completed
Targets: `ai-dev/design/xlang-java/01-architecture-baseline.md`（新建）、`README.md`（更新）

- Item Types: `Decision` | `Proof`

- [x] `01-architecture-baseline.md` 覆盖 roadmap W1 条目 xlang-java 组的 6 个内容点：①Executable→Java 转译器总体结构 ②~137 节点类的映射策略（分类策略与全覆盖路径，节点清单基线以 live `exec/` 137 文件为准）③SourceLocation 保真（生成代码错误栈/行号回映射到源 XPL 位置）④生成类加载与 `ResourceComponentManager` 集成（"生成类优先 / 解释器兜底"决策树）⑤`_gen/` 构建任务接入方式（不绕过 codegen 管线、不手改生成物）⑥EvalMethod 调用约定（以 janino 先例为基线并说明异同）
- [x] 与 xlang-execution `01-architecture-baseline.md` 对齐：java 后端在注册 SPI 与选择机制中的位置
- [x] `README.md` 更新：占位声明 → 目录索引 + 阅读顺序 + AGE 声明

Exit Criteria:

- [x] 上述 6 个内容点逐点在文档中有对应章节（可逐点核对）
- [x] 决策三要素齐全；无代码级签名展开；无演进叙事；引用约束同 Phase 1
- [x] 与 xlang-execution 组的选择机制/SPI 表述一致（无冲突决策）
- [x] 纯文档 phase，无代码变更（No build verification required for this phase）
- [x] `ai-dev/logs/` 当日条目已更新

### Phase 3 - xlang-truffle 运行时架构组（含 01 决策迁移收口）

Status: completed
Targets: `ai-dev/design/xlang-truffle/00-vision.md`（新建）、`02-architecture-baseline.md`（新建）、`01-truffle-knowledge.md`（决策迁出收口）、`README.md`（更新）

- Item Types: `Decision` | `Proof`

- [x] `00-vision.md`：定位（JVM 部署形态下运行时动态脚本的 JIT 提速）、成功标准、显式 non-goals（将 01 §7.3 草案正式化）、与 java 后端的互补边界（编译期 vs 运行时分工）
- [x] `02-architecture-baseline.md` 覆盖 roadmap W1 条目 xlang-truffle 组的 5 个内容点：①XLangLanguage/XLangContext 设计 ②帧/slot 映射（与 `LexicalScopeAnalysis` slot 分配的关系）③多线程架构（Context 池 + 共享 Engine + ContextPolicy.SHARED，从 01 §4/§7.2 草案提炼为正式决策）④两级内联缓存准则 ⑤与 nop-js Engine 的共享评估；另含对象映射（01 §7.1 提炼）与依赖坐标钉 LTS 线（truffle-api / truffle-dsl-processor / polyglot 25.x；若 01 §10 Q5 的 JDK 基线兼容性无法在本阶段确认，则写明条件钉版并移交 W2 裁决，不做无依据硬钉）及"Truffle 依赖不泄漏"约束
- [x] 01 决策迁移收口：01 中决策性内容（§4.4 选型、§7 映射草案等）迁入 00/02；01 迁移后定位为"知识参考层"（外部框架 API 速查 + SL 源码地图），其与 design-writing-guide 的字面张力由 README 显式声明豁免边界（或按 truffle README 既定选项整体迁往 `ai-dev/analysis/`，执行时二选一裁定并同步更新 README）；README 文档状态表更新（00/02 active、01 职责缩限、过渡态豁免解除）
- [x] 01 §10 Open Questions 清账：逐条在 00/02 中给出答案，或显式标记为"移交 W2-review 裁决"并列入交接清单

Exit Criteria:

- [x] 上述 5 个内容点 + 另含项（对象映射、依赖钉版）逐点在 00/02 中有对应章节（可逐点核对）
- [x] 01 不再承载决策性结论；README 状态表与实际文件职责一致；§10 Open Questions 清零或显式移交
- [x] 决策三要素齐全；无代码级签名展开；引用约束同 Phase 1（`~/sources/graal` 作为源码锚点可引用）
- [x] 纯文档 phase，无代码变更（No build verification required for this phase）
- [x] `ai-dev/logs/` 当日条目已更新

### Phase 4 - 跨文档一致性收口

Status: completed
Targets: 三组设计文档、`ai-dev/backlog/xlang-execution-optimization-roadmap.md`（仅状态块）

- Item Types: `Proof`

- [x] 跨文档一致性核对：后端选择机制、模块边界、依赖方向、对拍基准在三组文档中表述一致；发现冲突当场修复，无法当场裁定的分歧显式记入当日 log 并列入 W2-review 输入清单
- [x] 三篇 architecture-baseline 之间的交叉引用链接有效
- [x] `node ai-dev/tools/check-doc-links.mjs --strict` 通过
- [x] roadmap `W1-design` 条目状态与 plan 状态一致（本 plan 激活时为 `planned`）

Exit Criteria:

- [x] 一致性核对结论（含遗留分歧清单，如有）已写入当日 `ai-dev/logs/`
- [x] `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0
- [x] 纯文档 phase，无代码变更（No build verification required for this phase）
- [x] `ai-dev/logs/` 当日条目已更新

## Closure Gates

> 纯文档计划：`./mvnw test` / `./mvnw compile` 等构建验证条目按 guide 规则移除。

- [x] 5 篇新建正文 + 3 个 README 全部成稿，roadmap W1 条目三组内容点（4 + 6 + 5）逐点覆盖核对通过
- [x] 三组文档决策一致（选择机制 / 模块边界 / 依赖方向 / 对拍基准），无未裁定的跨文档冲突
- [x] `01-truffle-knowledge.md` 不再承载决策，§10 Open Questions 清零或显式移交 W2-review
- [x] 全部文档符合 `ai-dev/design/00-design-writing-guide.md`（决策三要素 / 无代码级展开 / 引用约束 / README AGE 要求）
- [x] 无静默跳过：设计中明确"暂缓决策"的点已显式标记并列入 W2-review 输入，未以留白方式跳过
- [x] `node ai-dev/tools/check-doc-links.mjs --strict` 退出码 0
- [x] `node ai-dev/tools/check-plan-checklist.mjs ai-dev/plans/xlang-execution-optimization/2026-08-19-2050-1-w1-design-docs.md --strict` 退出码 0（closure 时执行）
- [x] roadmap `W1-design` 状态与 plan 状态一致
- [x] No docs-for-ai update required: 设计实现后的 docs-for-ai 同步按 roadmap 归 I6，本 plan 不改 `docs-for-ai/`
- [x] 独立子 agent closure audit 已完成且证据写入下方 Closure 段
- [x] `ai-dev/logs/` 收口条目已记录

## Deferred But Adjudicated

（无——W1 范围内的全部内容点均为 closure 必需项，不允许延期）

## Non-Blocking Follow-ups

- 无（W2-review 是 roadmap 既定后继，不属于本 plan 的 follow-up）

## Closure

Status Note: 纯文档计划全部收口——5 篇新建正文 + 3 个 README 更新 + 01 决策迁出收口完成；roadmap W1 三组内容点（4+6+5）逐点覆盖并经独立 closure audit 抽查确认；01-truffle-knowledge 不再承载决策、§10 五条 Open Questions 逐条裁定（Q5 条件钉版确认点 + 2 项 watch-only 触发口径显式移交 W2-review，无留白跳过）；三组文档四维一致（遗留分歧清单为空）；工具门 check-doc-links / check-plan-checklist 均 exit 0。后继 W2-review（≥2 轮独立审查 gate）按 roadmap 既定顺序接管。
Completed: 2026-08-19

Closure Audit Evidence:

- Reviewer / Agent: 独立子 agent closure audit（fresh session，read-only），task `ses_fe5d07c96ffe1HNvccVC96XHAV`，2026-08-19
- Evidence:
  - A1 execution 组 4 内容点 PASS（01-architecture-baseline §三选择机制/§四SPI/§五对拍/§二Mermaid 逐点对号）；A2 java 组 6 内容点 PASS（§二~§七 逐点对号；节点基线 live 实测 137 文件）；A3 truffle 组 5+2 内容点 PASS（02 §三/§四/§五/§六/§八 + 对象映射表 + §二条件钉版条款非硬钉）；A4 truffle 00-vision PASS（定位/成功标准/non-goals 正式化/互补边界表）
  - B guide 合规 PASS：三篇 baseline 各有"拒绝了什么"节、两篇 vision 的 non-goals 含拒绝理由；5 篇正文 0 处 "Proposed vs Current"、0 处 analysis/discussions 引用；3 README 均含 AGE 声明 + 阅读顺序 + 职责表
  - C 01 收口 PASS：§4.4/§7/§8 决策迁出为指针、§10 清账表 5 问全裁定（Q5 显式移交）、header 与 README 状态表一致、豁免边界钉死为外部框架知识
  - D 跨文档一致性 PASS：选择机制/模块边界/依赖方向/对拍基准四维无冲突表述
  - E 交叉引用 PASS；F plan 完整性 PASS（4 phases completed、items 全勾、log 四条 phase 条目在档）
  - G `node ai-dev/tools/check-doc-links.mjs --strict` EXIT=0（0 errors；2 warnings 为 W3 plan 未来模块前向引用，当日日志已文档化）；`ls .../exec/ | wc -l` = 137
  - Anti-Hollow（文档版）PASS：各文档章节清单与实际内容逐一对号（非 stub）——SHARED 完整论证、Q1-Q5 一手证据（`~/sources/graal` suite.py javaCompliance 17+ / sdk 25.3.4.1）、10 类节点翻译模式实名表
  - Deferred 分类检查：无 in-scope live defect 被降级；3 项 Minor（01 内 analysis 路径引用 ×2、roadmap Last updated 头）——前两项已当场修复（改为一手锚点/自含事实），第三项随 closure 记账刷新
  - `node ai-dev/tools/check-plan-checklist.mjs <plan-file> --strict` EXIT=0（closure 后复跑，见当日 log）
- Minor findings: 0 Blocker / 0 Major / 3 Minor → verdict CAN CLOSE

Follow-up:

- no remaining plan-owned work（后继 W2-review 由 roadmap 既定顺序接管）
