# MR1 — P1 Batch Fix for MA1+MA2 Findings

> Plan Status: completed
> Note: Filename uses timestamp prefix per mission-driver convention (overrides Plan Guide Rule #21 for this workflow)
> Last Reviewed: 2026-07-31
> Source: `ai-dev/backlog/audit-remediation-roadmap.md` §MR1, `ai-dev/audits/arm-index.md` §P1
> Mission: audit-remediation
> Work Item: MR1

## Purpose

Fix all open P1 findings from MA1 and MA2 audit milestones across nop-ai module group. This covers the R1.0 expander step (aggregate findings into concrete fix work items) and the R1.x implementation step (code changes + tests).

## Current Baseline

- MA1+MA2 audits are complete: 7 audit reports produced, 17 P1 findings open (see arm-index.md §P1)
- P0-MA2-01 already fixed via `2026-07-30-2130-arm-fix-p0-ma2-01.md`
- P1-MA1-001 (nop-diff unused dependency) already fixed in-place during MA1.1 audit
- All findings are documented with file locations, severity, and repair recommendation in their respective audit reports
- MR1 R1.0 (expander) has not been executed — P1 findings are listed but not expanded into concrete commit-level work items

## Goals

- Expand all 17 open P1 findings into concrete fix work items with specific file paths and expected changes
- Implement all fixes with corresponding tests
- No P1 findings from MA1+MA2 remain open after this plan
- Build and test pass for affected modules

## Non-Goals

- Fixing P2/P3 findings (deferred within the audit framework)
- Fixing MA3/MA4/MA5/MA6 findings (separate MR plans)
- Re-auditing already-audited areas
- Creating new audit reports

## Scope

### In Scope

Fixing these 17 P1 findings:

| Finding ID | Source | Description |
|-----------|--------|-------------|
| P1-MA1-002 | MA1.1 | Deprecated parallel API (IAiChatService etc.) — mark `@Deprecated(forRemoval=true)` + add deprecation javadoc |
| P1-MA1-003 | MA1.3 | FileToolBizModel uses deprecated IFileOperator — migrate to current API |
| P1-MA1-010 | MA1.3 | nop-ai-skills zero IoC bean registration — add beans.xml |
| P1-MA1-017 | MA1.4 | nop-ai-maven module name/content mismatch — rename or document |
| P1-MA1-018 | MA1.4 | nop-ai-codegen zero production Java code — assess and document |
| P1-MA1-019 | MA1.4 | nop-ai-codegen postcompile references non-existent ORM model — fix reference |
| P1-MA1-031 | MA1.5 | NopAiSessionContext refPropName mismatch (context vs contexts) |
| P1-MA1-032 | MA1.5 | Five naming conventions coexisting — consolidate or document |
| P1-MA1-033 | MA1.5 | Duplicate ORM session/message models (nop-ai-agent vs nop-ai) |
| P1-MA2-002 | MA2.1 | NopAiProject missing audit propagation properties |
| P1-MA2-003 | MA2.1 | NopAiRequirement version type conflict (VARCHAR vs optimistic lock int) |
| P1-MA2-004 | MA2.1 | NopAiSessionContext refPropName="context" should be "contexts" |
| P1-MA2-005 | MA2.1 | _dao.beans.xml empty — no interface Biz bean registration |
| P1-MA2-014 | MA2.2 | XDSL codegen scripts output not verifiable — add verification |
| P1-MA2-018 | MA2.3 | 9 deprecated snake_case dict files (nop-ai-meta) — archive/deprecate |
| P1-MA2-023 | MA2.5 | NopAiModel.apiKey exposed as queryable/sortable in xmeta — restrict |
| P1-MA2-024 | MA2.5 | NopAiSession duplicate to-many context/contexts — resolve duplication |

### Out Of Scope

- MA3/MA4/MA5/MA6 findings (separate MR plans)
- P2/P3 findings from any milestone
- Cross-module conflict resolution with MA3/MA5/MA6 findings (MR4)

## Execution Plan

### Phase 1 — R1.0: Expand findings into concrete work items

Status: completed
Targets: `ai-dev/audits/arm-index.md`, individual audit reports

- Item Types: `Decision | Follow-up`

> **Note**: Phase 2 and 3 already contain a first-pass work item breakdown.
> Phase 1 is the **decision-making** step: resolve open choices (either/or fixes),
> select specific migration targets for ambiguous findings, and confirm the
> expansion is complete. After Phase 1 resolves all unresolved decisions,
> Phase 2/3 checklists become the authoritative execution list.

- [x] For each of the 17 P1 findings, resolve open either/or decisions:
  - P1-MA1-017: Decide: rename nop-ai-maven OR add explanatory doc → **selected: add doc**
  - P1-MA1-018: Decide: add production code OR document as intentional → **selected: document**
  - P1-MA1-032: Decide: consolidate naming OR document coexistence → **selected: document**
  - P1-MA2-005: Decide: populate _dao.beans.xml OR explain empty → **selected: explain**
  - P1-MA2-003: Decide: add nanoLock column OR remove versionProp → **selected: remove versionProp**
- [x] Identify the target API for P1-MA1-003 migration (FileToolBizModel away from deprecated IFileOperator)
- [x] Resolve P1-MA1-033/P1-MA2-024 ORM duplication: decide canonical model (nop-ai.orm.xml) and delta strategy for nop-ai-agent
- [x] Determine whether P1-MA1-031 and P1-MA2-004 are the same fix (same refPropName field) or separate — **same fix, owned by Phase 3 (including Java side verification)**
- [x] Confirm independence: MA1 code/API fixes (Phase 2) and MA2 ORM model fixes (Phase 3) address independent concerns — no ordering dependency blocks execution
- [x] Update `arm-index.md` with expanded R1.x work items and the decisions made

Exit Criteria:

- [x] Each P1 finding has a concrete fix prescription: exact file paths, expected change, test requirement
- [x] Findings are grouped into executable batches with clear dependency order
- [x] Complex findings requiring design decisions are flagged with proposed resolution path
- [x] No owner-doc update required (fixes implement audit recommendations; design docs will be updated if baseline changes in Phase 2)
- [x] `ai-dev/logs/` corresponding date entry updated

### Phase 2 — Implement P1 fixes for MA1 findings

Status: completed
Targets: `nop-ai/nop-ai-core/` (IAiChatService deprecation), `nop-ai/nop-ai-tools/` (FileToolBizModel), `nop-ai/nop-ai-skills/` (beans), `nop-ai/nop-ai-maven/` (module name), `nop-ai/nop-ai-codegen/` (postcompile ref)

- Item Types: `Fix | Decision | Proof`

**Phase 1 R1.0 decisions applied:**
- P1-MA1-017: Add explanatory README clarifying nop-ai-maven content vs name (no rename — avoids breaking existing consumers)
- P1-MA1-018: nop-ai-codegen zero production Java confirmed intentional per audit — add comment in module root and update postcompile ref
- P1-MA1-032: Retain existing naming conventions but add clarifying comment to each public API point
- P1-MA1-033/P1-MA2-024: ORM model in `nop-ai/model/nop-ai.orm.xml` is canonical; `nop-ai-agent` delta removes its duplicate entity definitions
- P1-MA1-031/P1-MA2-004: Single fix in ORM model (`nop-ai.orm.xml` line 1275 refPropName="context"→"contexts"); fix owned by Phase 3

- [x] Fix P1-MA1-002: Add `@Deprecated(forRemoval=true)` + `@deprecated` javadoc to `IAiChatService` (nop-ai-core), `IAiChatSession`, `IAiChatFunctionTool`, `IAiChatToolSet`, `AiChatOptions`, `AiChatExchange`, `AiMessage`, `ToolCall`, `AiChatUsage` — all in `nop-ai/nop-ai-core/src/main/java/io/nop/ai/core/api/`
- [x] Fix P1-MA1-003: Migrate `FileToolBizModel` (`nop-ai-tools/`) from deprecated `IFileOperator` to `IFileOperator` replacement (identified in R1.0: use `IFileToolExecutor` from `nop-ai-core` if applicable, otherwise document migration path)
- [x] Fix P1-MA1-010: Add beans.xml for nop-ai-skills module; register existing `IToolExecutor` implementations from `nop-ai-skills` sub-modules as beans (see MA1.3 audit report for exact class list)
- [x] Fix P1-MA1-017: Add README.md to `nop-ai/nop-ai-maven/` explaining module content (VFS/resource operations, not Maven build tooling) vs module name origin
- [x] Fix P1-MA1-018: Add comment to `nop-ai-codegen/` root explaining zero production Java is intentional (test-only bootstrap + postcompile scripts). Fix postcompile ref to point to `nop-ai.orm.xml` (handled with P1-MA1-019)
- [x] Fix P1-MA1-019: Fix postcompile ORM model reference in `nop-ai-codegen` from `ai-gen.orm.xml` to `nop-ai.orm.xml`
- [x] Fix P1-MA1-032: Add clarifying Javadoc comments to each public API class documenting the naming convention choice

Exit Criteria:

- [x] All P1 MA1 findings (P1-MA1-002 through P1-MA1-033) have fix commits with focused tests
- [x] Each deprecated API element has `@Deprecated(forRemoval=true)` with `@deprecated` javadoc pointing to replacement
- [x] nop-ai-skills has functional beans.xml with necessary bean definitions
- [x] **No silent no-op**: Deprecated API elements must NOT be removed — only annotated. P1-MA1-003 migration must compile against the target API (not just add UnsupportedOperationException to the old one)
- [x] `./mvnw compile -pl nop-ai -am` passes
- [x] `./mvnw test -pl nop-ai -am` passes
- [x] Docs update: `docs-for-ai/` receives a note about `IAiChatService` deprecation with migration path to `IChatService`
- [x] `ai-dev/logs/` corresponding date entry updated

### Phase 3 — Implement P1 fixes for MA2 findings

Status: completed
Targets: `nop-ai/model/nop-ai.orm.xml`, `nop-ai/nop-ai-dao/`, `nop-ai/nop-ai-meta/`, `nop-ai/nop-ai-service/`

- Item Types: `Fix | Decision`

**Phase 1 R1.0 decisions applied:**
- P1-MA2-003: Remove `versionProp="version"` from NopAiRequirement (string version identifier, not optimistic lock); add `nanoLock` column if optimistic locking needed later (out of current scope)
- P1-MA2-005: Add explanatory comment in `_dao.beans.xml` confirming empty is intentional — service-layer `BizProxyFactoryBean` handles registration
- P1-MA2-018: Add deprecation comment to each of the 9 snake_case dict files; do not delete (may be referenced by external codegen templates in nop-ai-coder)
- P1-MA2-024: Resolved by ORM model fix — `NopAiSession` keeps `to-many name="contexts"`; `NopAiSessionContext.session` refPropName→"contexts" (handled by P1-MA2-004)

- [x] Fix P1-MA2-002: Add `createTimeProp="createTime" createrProp="createdBy" updateTimeProp="updateTime" updaterProp="updatedBy"` to `NopAiProject` entity in `nop-ai/model/nop-ai.orm.xml`
- [x] Fix P1-MA2-003: Remove `versionProp="version"` from NopAiRequirement entity; add comment noting string version column is not for optimistic locking
- [x] Fix P1-MA2-004: Change `refPropName="context"` to `refPropName="contexts"` in `NopAiSessionContext.to-one` relation (`nop-ai.orm.xml` line 1275)
- [x] Fix P1-MA2-005: Add explanatory comment to `_dao.beans.xml` confirming empty is intentional (see MA2.1 audit report P1-MA2-005)
- [x] Fix P1-MA2-014: Add verification mechanism for XDSL codegen script outputs (validate that codegen runs produce expected file structure)
- [x] Fix P1-MA2-018: Add deprecation header comments to 9 snake_case dict files in `nop-ai-meta`; do not delete
- [x] Fix P1-MA2-023: Restrict NopAiModel.apiKey exposure in xmeta — remove `queryable="true"` and `sortable="true"`; keep `insertable/updatable` restricted
- [x] Regenerate code: `./mvnw clean install -DskipTests -pl nop-ai -am`
- [x] Verify P1-MA1-031/P1-MA2-004 fix: confirm Java side references (`NopAiSessionContext`, `NopAiSession` relations) are consistent with updated `refPropName="contexts"` in regenerated code

Exit Criteria:

- [x] All P1 MA2 findings (P1-MA2-002 through P1-MA2-024) have fix commits with focused tests
- [x] Regenerated code (`./mvnw clean install -DskipTests -pl nop-ai -am`) reflects ORM model changes (NopAiProject audit props, NopAiSessionContext refPropName)
- [x] `NopAiModel.apiKey` is no longer queryable/sortable in xmeta
- [x] `_dao.beans.xml` has explanatory comment confirming empty is intentional
- [x] **No silent no-op**: All ORM model changes are applied to `nop-ai.orm.xml` (source model), not generated files. No fix creates an empty method body or swallows exceptions
- [x] `./mvnw compile -pl nop-ai -am` passes
- [x] `./mvnw test -pl nop-ai -am` passes
- [x] ORM model changes are in `model/nop-ai.orm.xml` only — generated `_*.xml` and `_gen/` are regenerated via build
- [x] ORM model changes documented in `docs-for-ai/02-core-guides/model-first-development.md` if ORM pattern changed; otherwise "No owner-doc update required"
- [x] `ai-dev/logs/` corresponding date entry updated

### Phase 4 — Update arm-index + closure

Status: completed
Targets: `ai-dev/audits/arm-index.md`

- Item Types: `Fix | Follow-up`

- [x] Update `arm-index.md`: mark all 17 P1-MA1/MA2 findings as `fixed`
- [x] Run `./mvnw clean install -pl nop-ai -am` full build confirmation
- [x] Run `./mvnw test -pl nop-ai -am` test confirmation
- [x] Independent closure audit

Exit Criteria:

- [x] `arm-index.md` P1 section shows all 17 MA1/MA2 findings as `fixed`
- [x] `./mvnw clean install -pl nop-ai -am -T 1C` passes
- [x] `./mvnw test -pl nop-ai -am -T 1C` passes
- [x] No owner-doc update required (changes internal to nop-ai; audit status reflects in arm-index.md)
- [x] `ai-dev/logs/` corresponding date entry updated

## Closure Gates

- [x] All 17 P1 findings from MA1+MA2 have fix commits with tests
- [x] `./mvnw clean install -pl nop-ai -am` passes
- [x] `./mvnw test -pl nop-ai -am` passes
- [x] No confirmed live defect downgraded to deferred/follow-up
- [x] No owner-doc update required (fixes are internal corrections; no public contract changes)
- [x] Independent sub-agent closure audit completed with evidence
- [x] **Anti-Hollow Check**: all fix commits verified (a) changes are applied to source files (not generated), (b) no empty method bodies or silent no-op as "fixes"
- [x] `./mvnw compile -pl nop-ai -am`
- [x] `./mvnw test -pl nop-ai -am`

## Deferred But Adjudicated

### P2/P3 findings from MA1+MA2

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: These are lower-severity (cosmetic, informational) findings explicitly deferred by the roadmap framework. No confirmed live defect remains among them.
- Successor Required: `no`
- Successor Path: N/A (handled by platform maintenance)

## Non-Blocking Follow-ups

- MA3+MA4 P1 findings will be handled by MR2 (next plan batch)
- MA5+MA6 P1 findings will be handled by MR3

## Closure

Status Note: All 17 P1 findings from MA1+MA2 audits fixed and verified. Build and tests pass.
Completed: 2026-07-31

Closure Audit Evidence:

- Reviewer / Agent: Independent sub-agent (ses_04b25d559ffenAmhn5gGr036hS)
- Evidence: All Phase 2/3 exit criteria PASS except one: _dao.beans.xml was missing comment (P1-MA2-005). Fixed in subsequent pass. Re-verified: build+tests pass, comment now present. All 17 P1 findings confirmed fixed in live code.
