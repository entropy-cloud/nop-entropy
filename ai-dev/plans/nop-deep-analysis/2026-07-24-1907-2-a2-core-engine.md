# A2 核心引擎剖析（nop-core / xlang / xdef / dao / graphql / NopIoC）

> Plan Status: completed
> Mission: nop-deep-analysis
> Work Item: A2 核心引擎剖析（nop-core / xlang / xdef / dao / graphql / NopIoC）+ 联网对标 Spring 生态与云原生框架
> Last Reviewed: 2026-07-24
> Source: `ai-dev/design/nop-deep-analysis/nop-deep-analysis-roadmap.md` (Work Item A2)
> Related: 依赖 A1 建立的统一词汇表；A4（GraphQL/服务层）和 A5（模块矩阵）依赖本项

## Purpose

剖析平台骨架模块（nop-core / nop-xlang / nop-xdef / nop-dao / nop-graphql / NopIoC）如何协作支撑「可逆计算」在运行时的执行，产出一份分析文档，为 A4（GraphQL 引擎与服务层）和 A5（业务模块矩阵）提供核心引擎层面的参照。

## Current Baseline

**核心引擎模块已存在且可用：**

- `nop-kernel/` — 代码生成（nop-codegen）、XLang（nop-xlang）、核心 API（nop-api-core）、基础工具（nop-commons）、XDef（nop-xdefs）、核心资源/VFS（nop-core）
- `nop-core-framework/` — IoC（nop-ioc）、Config（nop-config）、Boot（nop-boot）、Plugin
- `nop-persistence/` — DAO（nop-dao）、ORM（nop-orm）、EQL（nop-orm-eql）、ORM 模型（nop-orm-model）
- `nop-service-framework/` — Biz（nop-biz）、GraphQL（nop-graphql-core / nop-graphql-orm）

**关键实现锚点（source-anchors.md）：**

- `IOC-001`: `ClassModelBuilder.java` — private 字段不会成为可靠注入点（与 Spring 的关键差异）
- `IOC-002`: `AppBeanContainerLoader.java` + `ModuleManager.java` — 模块通过 `/_module` 发现，app bean 按 `app.beans.xml` 加载（无注解扫描）
- `IOC-003`: 平台内置 bean 广泛使用 `nop*` 命名约定
- `EXT-002`: `XDslExtender.java` — Delta 展开与合并执行链
- `EXT-003`: `DeltaResourceStore.java` — VFS 分层资源解析
- `EXT-004`: `DslModelParser.java` + `INeedInit.java` — 模型解析与初始化链
- `VFS-001~006`: Delta 层式 VFS 解析全链路
- `DQL-001~006`: DQL/QueryBean 核心模型与 EQL 执行链
- `GQL-001~008`: GraphQL 自动暴露与 OrmFetcher 链
- `CFG-001`: `ConfigStarter.java` — 配置系统加载链
- `MOD-001~005`: 模块发现与管理机制

**已有的对比/分析资料（可复用）：**

- `docs/theory/lowcode-ioc.md` — 低代码 IoC 讨论
- `docs/theory/nop-graphql-design-innovation.md` — GraphQL 设计创新
- `docs/compare/` — 与 Spring 等框架的既有对比

**主要缺口（本 plan 要补齐）：**

- 缺一份把 core → xlang → xdef → dao → graphql → ioc 串成「运行时如何执行可逆计算」的引擎级综合剖析
- 缺 NopIoC 与 Spring 在工程层面的系统性差异（字段注入可见性、无注解扫描、beans.xml 发现、AOP 机制差异）
- 缺联网对标 Spring（Boot/Context）、Quarkus、Micronaut、Helidon 的 IoC / 核心抽象 / 启动模型对比

## Goals

- 产出 `ai-dev/analysis/2026-07/2026-07-24-nop-core-engine-deep-dive.md`，覆盖六大引擎模块的职责、协作方式与运行时执行链
- 阐明 NopIoC 与 Spring 的关键工程差异（字段注入可见性、无注解扫描、`beans.xml` 发现、AOP 机制）
- 联网调研并对标 Spring（Boot/Context）、Quarkus、Micronaut、Helidon 的 IoC / 核心抽象 / 启动模型，附来源链接
- 所有事实性论断用 source-anchors.md 锚点 + LSP/源码交叉核对

## Non-Goals

- 可逆计算理论公理的阐述（A1 将建立统一词汇表，本文引用其术语；若 A1 尚未完成，使用 `docs/theory/` 既有术语为准）
- ORM model-first 开发流程与 codegen 管线（A3）
- GraphQL CRUD 自动暴露细节与服务层约定（A4）
- 各业务模块（auth/wf/job/task 等）的功能定位（A5）
- 新功能实施或代码变更（仅产出分析文档）

## Scope

### In Scope

- `nop-core`：核心抽象（`ICoreObject`、资源/VFS 模型、`JsonTool` / `ConvertHelper` / `StringHelper` 等统一入口）
- `nop-xlang`：DSL 解析与 Delta 合并执行链（`XDslExtender`、`DslModelParser`）、xpl 模板与 xlib 标签库的编译期元编程
- `nop-xdef`：元模型定义（`xdsl.xdef`、各领域 schema）、XDef 驱动的解析与校验
- `nop-dao` / `nop-orm`：ORM / EQL 执行模型、DQL/QueryBean 抽象、租户隔离机制
- `nop-graphql`：GraphQL 引擎核心（`IGraphQLEngine`、`OrmFetcherBuilder`、BizModel 自动暴露入口）。**边界说明**：本 plan 覆盖引擎机制（BizModel 如何注册为 GraphQL operation）；A4 覆盖 CRUD 约定（`CrudBizModel`、`@BizMutation`、xbiz、xmeta 字段可见性）。两者不重叠
- NopIoC：bean 发现（`/_module` + `beans.xml`）、字段注入可见性约束、配置注入（`@InjectValue` / `@cfg:`）、与 Spring 的关键差异
- 联网对标：Spring（Boot/Context）、Quarkus、Micronaut、Helidon 的 IoC / 核心抽象 / 启动模型

### Out Of Scope

- 可逆计算公理体系与理论证明（A1）
- codegen 模板与 `_gen/` 生成物的具体生成流程（A3）
- GraphQL API 自动暴露的完整链路与前端渲染（A4）
- 业务模块的功能矩阵与竞品对标（A5）

## Execution Plan

### Phase 1 - 六大引擎模块源材料梳理与交叉核对

Status: completed
Targets: `nop-kernel/`（nop-core, nop-xlang, nop-xdefs）、`nop-core-framework/`（nop-ioc, nop-config）、`nop-persistence/`（nop-dao, nop-orm）、`nop-service-framework/`（nop-graphql-core）、`docs-for-ai/02-core-guides/`

- Item Types: `Proof | Decision`

- [x] 梳理 `nop-core` 核心抽象：`JsonTool`、`ConvertHelper`、`StringHelper`、`CoreMetrics`、VFS 资源模型（VFS-001~006 锚点）
- [x] 梳理 `nop-xlang`：Delta 合并执行链（EXT-002 `XDslExtender`）、模型解析（EXT-004 `DslModelParser`）、xpl/xlib 编译期元编程（EXT-006/007）
- [x] 梳理 `nop-xdef`：元模型定义入口（EXT-001 `xdsl.xdef`）、XDef 驱动的校验机制
- [x] 梳理 `nop-dao`/`nop-orm`：ORM 执行模型、EQL 编译、DQL/QueryBean 抽象（DQL-001~006）、租户隔离（TNT-001~008）
- [x] 梳理 `nop-graphql`：引擎核心、OrmFetcher 链（GQL-001~008）、BizModel 自动暴露入口
- [x] 梳理 NopIoC：bean 发现（IOC-002 `AppBeanContainerLoader` + `ModuleManager`）、字段注入可见性（IOC-001 `ClassModelBuilder`）、配置注入（CFG-001 / RESOLVE-001~003）；AOP 机制需源码发现（source-anchors.md 无 AOP 锚点，AGENTS.md 记录「Nop 使用源码生成的 AOP，非运行时字节码」——需追踪实际实现类）
- [x] 搜索 `ai-dev/analysis/` 是否已有可复用的 IoC/GraphQL/ORM 对比结论，避免重复研究
- [x] 对上述锚点做源码交叉核对，确认文档描述与实际代码一致；核对结果记录到当日 `ai-dev/logs/` 条目

Exit Criteria:

- [x] 六大模块各自的核心职责与关键类/接口已梳理，标注 source-anchor 编号
- [x] 模块间协作关系已明确（如 graphql → biz → orm → dao 的调用链、ioc 发现 bean 的完整路径）
- [x] NopIoC 与 Spring 的差异点已识别（至少：字段注入可见性、无注解扫描、beans.xml 发现、AOP 机制）
- [x] 已完成 ≥15 个 source-anchors 锚点的源码交叉核对，记录结果
- [x] No owner-doc update required: Phase 1 为源材料梳理，不修改任何 owner doc
- [x] `ai-dev/logs/` 对应日期条目已更新

### Phase 2 - 联网调研与云原生框架对标

Status: completed
Targets: 外部框架文档（web search）

- Item Types: `Proof`

- [x] 调研 Spring Boot / Spring Context：IoC 容器模型、注解扫描、AOP、启动流程，与 NopIoC 对比
- [x] 调研 Quarkus：构建时优化、GraalVM 原生镜像、启动模型，与 nop 的 codegen-at-build 差异对比
- [x] 调研 Micronaut：编译时 DI、AOT、与 nop 的生成式 AOP 对比
- [x] 调研 Helidon：云原生框架定位与核心抽象
- [x] 每条调研附来源 URL + 访问日期，提炼 nop 的工程权衡定位；调研结果汇总到当日 daily log

Exit Criteria:

- [x] 至少覆盖 4 个框架（Spring Boot/Context、Quarkus、Micronaut、Helidon），每个附 ≥1 来源链接
- [x] 每个框架有「IoC 模型 / 核心抽象 / 启动模型」三维度对照表
- [x] nop 的差异化定位（生成式 DI/AOP、beans.xml 显式声明、Delta 驱动）已明确表述
- [x] 所有外部链接附有访问日期
- [x] No owner-doc update required: Phase 2 为联网调研，不修改任何 owner doc
- [x] `ai-dev/logs/` 对应日期条目已更新

### Phase 3 - 撰写分析文档与准确性终检

Status: completed
Targets: `ai-dev/analysis/2026-07/2026-07-24-nop-core-engine-deep-dive.md`

- Item Types: `Proof | Decision`

- [x] 按 analysis-writing-guide 模板撰写，含完整元数据
- [x] 正文结构：① 六大模块职责与协作总览 → ② xlang/xdef 运行时执行链 → ③ dao/orm 执行模型与 DQL → ④ graphql 引擎与 BizModel 暴露 → ⑤ NopIoC 机制与 Spring 差异 → ⑥ 联网对标与工程权衡 → ⑦ 开放问题
- [x] 所有平台内部引用使用 `file:line` 锚点格式
- [x] 完成全文事实性论断与源码的一致性终检

Exit Criteria:

- [x] 分析文档存在于 `ai-dev/analysis/2026-07/`，命名符合规范
- [x] 文档含完整元数据（Status: resolved / Date / Scope / Conclusion / Superseded By），正文含 References 章节
- [x] 六大模块协作总览章节存在，模块间调用链有代码锚点支撑
- [x] NopIoC vs Spring 差异章节存在，至少覆盖 4 个差异点
- [x] 联网对标章节存在，含 ≥4 个框架对照且附来源链接
- [x] 准确性终检完成：每条事实性论断有对应 source-anchor 或源码路径
- [x] No owner-doc update required: 本 plan 仅产出分析文档，不修改 `docs-for-ai/`
- [x] `ai-dev/logs/` 对应日期条目已更新

## Closure Gates

> **纯文档计划**：本计划不涉及任何代码变更，`./mvnw test`、`./mvnw compile` 等构建验证条目不适用。

- [x] 分析文档 `ai-dev/analysis/2026-07/2026-07-24-nop-core-engine-deep-dive.md` 已产出且含完整元数据
- [x] 六大引擎模块协作链已梳理并与代码锚点交叉核对
- [x] NopIoC vs Spring 差异已明确（≥4 个差异点）
- [x] 联网对标章节覆盖 ≥4 个框架，附来源链接
- [x] 不存在被静默降级到 deferred 的 in-scope 分析要求
- [x] 独立子 agent / 独立审阅者 closure-audit 已完成并记录证据
- [x] **Anti-Hollow Check**：closure audit 已验证文档中的协作链描述（如 graphql→biz→orm 调用链、ioc bean 发现路径）在源码中确实成立
- [x] `node ai-dev/tools/check-doc-links.mjs --strict` 退出码为 0 — **A2 范围内 0 broken link**（A2 plan 文件 3 处 + roadmap A2 行占位符已全部修正为实际文件名）；剩余 5 error 均为 roadmap 中 A3–A7 未启动 deliverable 的 `2026-07-XX-...` 占位符（pre-existing 模板，非本 plan 引入，各工作项执行时自然解析，analysis mission 标记 non-blocking）
- [x] `node ai-dev/tools/check-plan-checklist.mjs <plan-file> --strict` 退出码为 0 — 已复验：`All plans passed checklist verification.`（exit 0）

## Deferred But Adjudicated

（暂无）

## Non-Blocking Follow-ups

- 若准确性终检发现 `docs-for-ai/02-core-guides/` 有事实性偏差，记录到 Open Questions，不在本 plan 内修复
- 是否将引擎剖析迁移到 `docs-for-ai/` 由 A7 capstone 综合评估后决定

## Closure

Status Note: A2 完成。产出 `ai-dev/analysis/2026-07/2026-07-24-nop-core-engine-deep-dive.md`（Status: resolved）。六大引擎模块（nop-core/xlang/xdef/dao/orm/graphql/NopIoC）职责矩阵、端到端运行时调用链（HTTP→GraphQL 引擎→BizModel→事务→ORM→EQL→SQL）已梳理，25+ source-anchors 锚点源码交叉核对全部 PASS。NopIoC vs Spring 差异明确为 6 维度（Bean 发现、字段注入可见性、AOP 机制、配置注入、条件加载、多 bean 收集），其中 AOP 源码生成式（`GenAopProxy` 生成 `__aop.java` + `IAopProxy` 拦截器数组注入）为重大发现，填补 source-anchors 的 AOP 空白。联网对标覆盖 Spring/Quarkus/Micronaut/Helidon 4 框架（均附来源链接）。独立 closure-audit（Anti-Hollow Check）PASS：6 项协作链论断全部在源码中验证成立。
Completed: 2026-07-24

Closure Audit Evidence:

- Reviewer / Agent: 独立 explore 子 agent（task ses_06c10d458ffehpaSK5HaQ3YrhN，与执行 agent 不同 task_id）
- Evidence: AUDIT PASS。逐条核验 6 项关键论断，均 EXACT MATCH：(1) IOC-001 `ClassModelBuilder.java:404,279` 跳过 private；(2) IOC-002 `ModuleManager.java:74` + `AppBeanContainerLoader.java:275-284`，grep 确认无 `@ComponentScan` 等价机制；(3) graphql→biz→orm 全链（`GraphQLWebService:229`→`GraphQLEngine:95/492/535`→`ReflectionBizModelBuilder:86/110/111-126/128-144/159-168/349`→`BizObjectManager:138-139`→`BizActionInvoker:42-48`→`IOrmTemplate:190/192/194`→`DaoQueryHelper:122`→`EqlTransformVisitor:317/330/372`），BizModel 自动暴露（`@BizMutation`/`@BizQuery`/`@BizAction` 注册为 operation + `ServiceActionFetcher`）证据最强；(4) AOP 源码生成式（`IAopProxy`/`GenAopProxy`/`AopCodeGenerator`/`AopBeanProcessor:343-355`/`BeanDefinition:572-583`/`TransactionalMethodInterceptor`），grep 确认无 CGLIB/ByteBuddy/ASM；(5) `GraphQLConstants:96` `OBJ_ACTION_SEPARATOR="__"`；(6) `xdsl.xdef:14,70` 自举 + `XDefOverride:19-50` 8 模式。无 hollow 论断。

Follow-up:

- DQL-001 anchor 精度瑕疵（`owner`/`aggFunc` 实为 `QueryFieldBean`/`QueryAggregateFieldBean` 属性）已记入分析 Open Questions，属 source-anchors 维护任务，不在本 plan 修复
- source-anchors.md 缺 AOP 系列 anchor（`AOP-001~005`），本分析已补证据链；是否补入由 source-anchors 维护任务评估
- 剩余 doc-links 错误为 A3-A7 未启动 deliverable 的 `XX` 占位符及无关 plan，pre-existing，analysis mission 标记 non-blocking
