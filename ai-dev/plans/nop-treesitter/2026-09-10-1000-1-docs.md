---
status: completed
mission: nop-treesitter
work-item: "14"
group: "2026-09-10-1000"
verify: [test]
---

# Documentation: docs-for-ai module page, Javadoc coverage, wiki entry, INDEX routing (M4, part 3)

> Plan Status: active
> Last Reviewed: 2026-09-10
> Source: ai-dev/backlog/nop-treesitter-roadmap.md item 14; docs-for-ai/03-modules style (nop-file.md)

## Purpose

External consumers can adopt nop-treesitter without reading source: module page in docs-for-ai, complete Javadoc on public classes, a wiki-style entry, and INDEX routing (DOC-01..04).

## Current Baseline

- Items 2-13 `done`; 383 tests green; module public surface: TSParser/TSTree/TSNode/TreeSitterException + language/lexer/parser(glr, incremental)/subtree/query/scanner/cursor/util/provider/biz packages; integration story (item 12) and perf story (item 13) already documented in `nop-treesitter/README.md` and `nop-treesitter/docs/perf-tuning.md`.
- Javadoc scan (2026-09-10): every public class carries class-level Javadoc except `biz/TreeSitterErrors.java` (constants interface, `FileErrors` precedent has none either — verify against the precedent before adding).
- `docs-for-ai/03-modules/` holds per-module owner docs (`nop-file.md` style: 功能概览/快速开始/...); `docs-for-ai/INDEX.md` routes them in the By-Task table; `docs-for-ai/04-reference/source-anchors.md` carries implementation anchors.
- Repo rule: after touching `docs-for-ai/` or `ai-dev/`, `node ai-dev/tools/check-doc-links.mjs --strict` must exit 0.

## Goals

- **DOC-01**: `docs-for-ai/03-modules/nop-treesitter.md` — architecture overview, public API, grammar registration (built-ins + ServiceLoader), GraphQL/IoC usage, performance characteristics (linking perf-tuning.md), recovery semantics summary.
- **DOC-02**: Javadoc on every public class in `io.nop.treesitter.*` (close the `TreeSitterErrors` gap if the precedent check warrants; verify the rest by script).
- **DOC-03**: `nop-treesitter/docs/wiki.md` — wiki-style entry (short orientation page linking README/perf-tuning/docs-for-ai page).
- **DOC-04**: `docs-for-ai/INDEX.md` By-Task entry for nop-treesitter + `docs-for-ai/04-reference/source-anchors.md` anchors for the module's key entry points.

## Non-Goals

- Code changes of any kind (doc-only plan; build gates dropped per guide).
- Translating or rewriting README/perf-tuning (linked, not duplicated).
- Wiki platform integration beyond the `docs/wiki.md` file the roadmap offers as the alternative.

## Scope

### In Scope

- The four deliverables above; content accuracy checked against live code (class names, resource paths, error codes, GraphQL operation name, benchmark numbers copied from perf-tuning.md).
- Doc link checker pass.

### Out Of Scope

- Anything under `docs/` (repo-wide generated docs); translations.

## Execution Plan

### Phase 1 — All four deliverables

Status: completed
Targets: `docs-for-ai/03-modules/nop-treesitter.md`, `docs-for-ai/INDEX.md`, `docs-for-ai/04-reference/source-anchors.md`, `nop-treesitter/docs/wiki.md`, Javadoc gap

- Item Types: `Proof`

- [x] `docs-for-ai/03-modules/nop-treesitter.md` following the `nop-file.md` house style: 功能概览（纯 Java、无 JNI、ERROR/MISSING 恢复、五内置语法）、快速开始（Maven 坐标、直接 API、NopIoC/GraphQL 用法）、语法注册（内置 + ServiceLoader custom-wins）、架构要点（blob v4 格式、GLR + arena、增量重parse）、性能特征（链接 perf-tuning.md 的 3x 判定表）、错误码。
- [x] Javadoc completeness — **scan scope adjudicated: top-level public types AND nested public records** (the roadmap says "every public class"; nested records are public API surface). Known gaps: `biz/TreeSitterErrors` (top-level) + nested records in `lexer/Lexer` (LexOutcome), `language/Language` (Action/ActionGroup/FieldMapSlice/FieldMapEntry + the automaton's Transition/Clause), `query/PatternNode` (Type/Wildcard), `query/Predicate` (Eq/Match). **`TreeSitterErrors` gets class Javadoc** — repo majority (15 of 23 `*Errors` interfaces have it) and the exit criteria both point the same way; `FileErrors` is the minority style, not the binding precedent.
- [x] `nop-treesitter/docs/wiki.md`: one-page orientation (what it is, where to start: README → docs-for-ai page → perf-tuning; FAQ-style pointers for common tasks).
- [x] `docs-for-ai/INDEX.md`: rows in the `## 快速路由` table routing to `03-modules/nop-treesitter.md`; `docs-for-ai/04-reference/source-anchors.md`: new anchors with IDs TS-001 (`TSParser.parse`/`TSTree`), TS-002 (`Language.fromClasspath` + blob paths), TS-003 (`ITreeSitterLanguageProvider` ServiceLoader contract), TS-004 (`TreeSitterBizModel` + error code); the module page carries the house-style `## 源码锚点` section referencing those IDs plus `## 相关文档`.

Exit Criteria:

- [x] All four files exist; every class name, path, error code and query name mentioned in them matches live code (spot-checked by the closure audit).
- [x] Javadoc scan reports zero undocumented public types (annotation-aware scan, 15 gaps closed).
- [x] `node ai-dev/tools/check-doc-links.mjs --strict` exits 0 (0 errors; 24 pre-existing+new broken links fixed incl. roadmap placeholder paths).
- [x] `ai-dev/logs/2026/09-10.md` updated.

## Closure Gates

> **关闭条件**：只有本 section 所有条目以及每个 Phase 的 Exit Criteria 全部勾选为 `[x]` 后，才能将 frontmatter `status` 改为 `completed`。纯文档计划：构建验证条目按 guide 豁免。

- [x] 所有 in-scope confirmed live defects 已修复（审计 Major：6 个接口内嵌 record 补 Javadoc；Minor：模块页 corpus 数字 JS↔TS 互换修正）
- [x] 所有 in-scope confirmed contract drifts 已收敛（文档内容与 live 代码一致——审计 6 处可执行指令逐一致）
- [x] 行为/契约结果已达成：DOC-01/02/03/04 全部落地
- [x] 必要 focused verification 已完成（link checker strict 0 errors；Javadoc 注解+隐式 public 感知扫描 0 缺口）
- [x] 不存在被静默降级到 deferred / follow-up 的 in-scope live defect 或 contract drift
- [x] 受影响的 owner docs 已同步（本计划的交付物即 owner docs）
- [x] 独立子 agent / 独立审阅者 closure-audit 已完成并记录证据（写入 `## Closure` 段）
- [x] **Anti-Hollow Check**：closure audit 已抽查文档中的类名/路径/错误码/示例与 live 代码一致（6 处全 PASS）
- [x] 构建豁免：纯文档计划（Javadoc 注释补充），`./mvnw -pl nop-treesitter -am test` 复跑 383 green
- [x] checkstyle / 代码规范检查通过（不适用——无源码逻辑变更；link checker 0 errors 代替）

## Closure

Status Note: DOC-01..04 全部落地：docs-for-ai 模块页（house style 含源码锚点/相关文档节）、wiki 一页导航、INDEX 快速路由两行、source-anchors TS-001..004；Javadoc 扫描口径（顶层 public + 嵌套 record，注解感知 + 隐式 public 感知）0 缺口。**附带修复 24 个存量 doc 链接错误**（roadmap 占位计划路径、不存在的 analysis 引用、2258-xlang 源码路径、start-here/runbook 外部引用），checker 0 errors / exit 0。383 tests green。审计 Major（6 个接口内嵌 record 缺 Javadoc——扫描器漏过隐式 public）与 Minor（模块页 corpus 数字 JS↔TS 互换）均已修复并复扫验证。
Completed: 2026-09-10

Closure Audit Evidence:

- Reviewer / Agent: 独立子 agent（fresh session）agent_d3115026-5e44-438f-9955-0230f76f93c5
- Audit Session: agent_d3115026-5e44-438f-9955-0230f76f93c5
- Evidence:
  - DOC-01..04 存在性 + 5 项事实抽查：PASS（类名/blob 路径/错误码/GraphQL 操作名/性能数字逐一致）
  - 文档 vs live 代码（文档计划的端到端）：PASS——6 处可执行指令（Maven 坐标、fromClasspath、GraphQL 查询、ServiceLoader 文件名、beans 内容、custom-wins 行为）逐一致
  - 链接检查：checker exit 0、0 errors（审计实测）
  - Javadoc：审计确认 15 处修复落地 + 新发现 6 处隐式 public 缺口（审计 Major）——已补齐并复扫 0 缺口（隐式 public 感知扫描；private/package-private 不在口径）
  - Minor 修复：模块页 corpus 数字改为 JS 116/116、TS 110/111、TSX 110/111
  - 行为：`./mvnw -pl nop-treesitter -am test` 383 green（closure 复跑）
  - `node ai-dev/tools/check-plan-checklist.mjs --strict`：closure 动作完成后退出码 0
  - Deferred 分类检查：无 deferred 项

## Deferred But Adjudicated
## Non-Blocking Follow-ups

- （无）
