---
status: active
mission: nop-treesitter
work-item: "13"
group: "2026-09-10-0900"
verify: [test]
---

# Performance benchmark: JMH suites, C-reference comparison, memory profile, tuning log (M4, part 2)

> Plan Status: active
> Last Reviewed: 2026-09-10
> Source: ai-dev/backlog/nop-treesitter-roadmap.md item 13; nop-benchmark JMH pattern (jmh 1.33, annprocess provided)

## Purpose

Establish measurable performance characteristics of the pure-Java tree-sitter runtime (PERF-01..05): reproducible JMH benchmarks for JSON and Java parsing, a C-runtime comparison harness for the roadmap's "within 3x of C" target, a memory profile, and a tuning log.

## Current Baseline

- Items 2-12 `done`; 381 tests green; checkstyle 0; hollow scan 0.
- In-repo JMH precedent: `nop-benchmark/pom.xml` uses `jmh-core` 1.33 (compile) + `jmh-generator-annprocess` 1.33 (provided), benchmarks in `src/main/java` with a `main()` runner (`TestReflection`). JMH benchmarks in test scope work with the same annotation processor auto-discovery (processor on the test-compile classpath).
- C reference: `~/sources/treesitter/tree-sitter` (lib/src/lib.c) + vendored-equivalent grammars at `~/sources/treesitter/grammars/` — machine-local, read-only; the item 11 oracle (`_tmp/ts-oracle/tsoracle`) already builds from them with a `cc` one-liner pattern proven in this repo.
- `TSParser.parse(Language, String|byte[])` + `TSTree.toSexpString(false)` are the hot path; `SubtreeArena.size()` exposes allocated node count for the arena profile.

## Goals

- **PERF-01/02**: JMH benchmarks in `src/test/java/io/nop/treesitter/bench/` — JSON parse at 10 KB / 100 KB / 1 MB deterministically generated sources; ops/s + gc.alloc.rate.norm recorded.
- **PERF-03**: Java parse benchmark — single ~10 KB file and a 25-file synthetic project per op.
- **PERF-04**: memory profile — JMH GC profiler results plus arena node-count growth across input sizes, recorded in the tuning log.
- **PERF-05**: `nop-treesitter/docs/perf-tuning.md` — methodology, numbers from this machine, C comparison, what worked / what didn't.
- Roadmap target verdict: Java-vs-C throughput ratio on JSON and Java stated with evidence; if the 3x target is missed, the measured gap and its cause are documented (the target is a measurement outcome, not a gate to hide).

## Non-Goals

- Performance optimization work itself (any tuning beyond what items 2-12 already shipped is out of scope; findings become recorded candidates).
- Committing benchmark inputs (all fixtures are generated deterministically at setup).
- CI integration of the full benchmark run (minutes-long; only a smoke test runs in the suite).

## Scope

### In Scope

- `nop-treesitter/pom.xml`: test-scope `jmh-core` + `jmh-generator-annprocess` (version property 1.33).
- `src/test/java/io/nop/treesitter/bench/`: `BenchSources` (seeded deterministic generators for the three JSON sizes and the Java file/project), `JsonParseBenchmark`, `JavaParseBenchmark` (`@Benchmark` methods returning the sexp length through a `Blackhole`), `TreeSitterBenchmarkRunner` (main: warmup 3 × 1 s, 5 × 1 s, gc profiler, text results to `_tmp/`), `TreeSitterBenchmarkSmokeTest` (fork=0, 1+1 tiny iterations — proves every benchmark method executes; runs in the normal suite).
- `bench/run-c-reference.sh`: builds the C reference harness from `~/sources/treesitter/tree-sitter` + `~/sources/treesitter/grammars/{tree-sitter-json,tree-sitter-java}` into `_tmp/ts-bench/` (clear error + non-zero exit when the reference checkout is absent — no silent skip), times the same generated inputs via a small C main loop, prints ops/s.
- `nop-treesitter/docs/perf-tuning.md` (PERF-05).

### Out Of Scope

- Everything else in the module; incremental-parse benchmarks (mechanism covered by item 9's equivalence tests; can be a follow-up).

## Execution Plan

### Phase 1 — JMH benchmarks + smoke test

Status: completed
Targets: `pom.xml`, `src/test/java/io/nop/treesitter/bench/`

- Item Types: `Proof`

- [x] `BenchSources`: seeded (`new Random(0x5EED)`) generators — `jsonSource(int targetBytes)` (nested objects/arrays/strings/escapes/numbers; generates until ≥ targetBytes, then truncates at the last complete top-level value boundary — final sizes documented in perf-tuning.md; THE SAME materialized bytes feed both the Java benchmarks and the C harness), `javaSingleFile()` (~10 KB class with deterministic methods), `javaProjectFiles()` (25 distinct classes). Well-formedness asserted by rendering each source once and asserting the sexp contains neither `(ERROR` nor `(MISSING` (no public isError API — adjudicated).
- [x] `JsonParseBenchmark` (@State(Scope.Benchmark), @BenchmarkMode(Throughput)/AverageTime both acceptable — pick Throughput): `parse10K`, `parse100K`, `parse1M` — each `TSParser.parse` + `Blackhole.consume(tree.toSexpString(false).length())`.
- [x] `JavaParseBenchmark`: `parseSingleFile`, `parseProject25Files`.
- [x] `TreeSitterBenchmarkRunner` main: OptionsBuilder includes all benchmarks (5 methods across 2 classes), .addProfiler("gc"), warmup 3×1s, measurement 5×1s, 1 fork, result text written to the repo-root `_tmp/bench-result.txt` (path resolved from the working directory documented as repo root in the runner header; runner asserts it).
- [x] `TreeSitterBenchmarkSmokeTest`: JMH `Runner` with fork 0 / warmup 1 × 100 ms / measurement 1 × 100 ms, **`.shouldFailOnError(true)`** (JMH default swallows benchmark exceptions — without this the smoke is hollow), and asserts the returned `Collection<RunResult>` has **exactly 5 results** (5 benchmark methods across the 2 classes). Runs in the normal suite without the gc profiler.
- [x] pom: jmh test-scope deps.

Exit Criteria:

- [x] Smoke test green in the normal suite (`-Dtest=TreeSitterBenchmarkSmokeTest` and full module suite stays green), with the 5-result assertion proving every @Benchmark method executed successfully.
- [x] `TreeSitterBenchmarkRunner` executed manually once; `_tmp/bench-result.txt` contains throughput + gc.alloc.rate.norm for all 5 benchmark methods (numbers recorded into perf-tuning.md in Phase 2).
- [x] `ai-dev/logs/2026/09-10.md` entry added.

### Phase 2 — C comparison, memory profile, tuning log

Status: completed
Targets: `bench/run-c-reference.sh`, `nop-treesitter/docs/perf-tuning.md`

- Item Types: `Proof | Follow-up`

- [x] `run-c-reference.sh`: `set -euo pipefail`; **step 1 materializes the benchmark inputs**: invokes the test-scope `BenchSourcesDump` main via `./mvnw -pl nop-treesitter dependency:build-classpath -Dmdep.outputClasspathFile=...` + plain `java -cp` (NOT `exec:java` — the repo's pluginManagement pins its mainClass to `io.nop.codegen.task.CodeGenTask`), writing the five generated sources (3 JSON sizes + java single file + java project) into `_tmp/ts-bench/inputs/`; the C comparison covers the 4 single-stream inputs (json 10K/100K/1M + java single file), the 25-file project verdict is Java-internal (per-benchmark scope recorded in perf-tuning.md); step 2 compiles a C harness measuring the SAME work as the Java benchmarks — parse + `ts_node_string` + `free` per iteration (no printing; clock_gettime around the loop, ops/s printed) — against `~/sources/treesitter/tree-sitter/lib/src/lib.c` + the two grammar `parser.c`s (vendored copies under `src/test/resources/upstream/grammars/` as fallback); absent checkout → `echo "C reference not available at ~/sources/treesitter" >&2; exit 1`; cc failure propagates its diagnostics through `set -e`.
- [x] Run the script on this machine; record Java ops/s (from Phase 1's run — same parse+serialize work per op) vs C ops/s per input in perf-tuning.md; state the x-of-C verdict per benchmark against the roadmap 3x target (work-parity: both sides parse AND serialize; the serialization share of the Java op is also measured by a dedicated `serializeOnly`-style note if needed for interpretation).
- [x] Arena node-count profile (`ArenaProfileProbe` main committed): record `SubtreeArena.size()` after parsing each JSON size (from a probe in the smoke test or the runner), plus gc.alloc.rate.norm from the JMH profile.
- [x] `docs/perf-tuning.md`: methodology, environment (CPU/RAM/JDK), tables, verdicts, what-worked/what-didn't (int-array arena, linear fast path, per the roadmap's guidance), recorded optimization candidates.

Exit Criteria:

- [x] `run-c-reference.sh` exits 0 on this machine with ops/s output; perf-tuning.md contains the Java-vs-C table and explicit 3x-target verdicts (or, if the checkout is absent on a future machine, the script's documented failure mode).
- [x] perf-tuning.md exists with methodology + all 5 benchmark methods' numbers + memory profile section.
- [x] **端到端验证**: the numbers in perf-tuning.md trace to artifacts on this machine (`_tmp/bench-result.txt`, script output) — recorded in the daily log.
- [x] `ai-dev/logs/2026/09-10.md` updated.

## Closure Gates

> **关闭条件**：只有本 section 所有条目以及每个 Phase 的 Exit Criteria 全部勾选为 `[x]` 后，才能将 frontmatter `status` 改为 `completed`。

- [ ] 所有 in-scope confirmed live defects 已修复（含执行中发现的新缺陷）
- [ ] 所有 in-scope confirmed contract drifts 已收敛（无已知入口）
- [ ] 行为/契约结果已达成：PERF-01/02/03/04/05 全部落地
- [ ] 必要 focused verification 已完成（smoke test 进套件；全量 JMH + C 对比在日志与 tuning 文档留痕）
- [ ] 不存在被静默降级到 deferred / follow-up 的 in-scope live defect 或 contract drift（性能差距按 roadmap 语义是记录项而非隐藏项）
- [ ] 受影响的 owner docs 已同步（perf-tuning.md 为本项 owner doc；docs-for-ai 链接归 item 14）
- [ ] 独立子 agent / 独立审阅者 closure-audit 已完成并记录证据（写入 `## Closure` 段）
- [ ] **Anti-Hollow Check**：closure audit 已验证 benchmark 方法真实执行（smoke test）且 C 对比脚本真实产出数字（非占位）
- [ ] `./mvnw -pl nop-treesitter -am test -T 1C` 通过
- [ ] checkstyle / 代码规范检查通过（`checkstyle:check -Pqa` 0 violations）

## Closure

## Non-Blocking Follow-ups

- （无）
