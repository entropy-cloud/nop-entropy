---
status: active
mission: nop-treesitter
work-item: "10"
group: "2026-09-09-0800"
verify: [test]
---

# JavaScript/TypeScript grammars + full corpus acceptance (M3, part 3)

## Current Baseline

- Items 2-9 `done`. Full module suite: **301 tests green** (`./mvnw -pl nop-treesitter -am test -T 1C`), JSON corpus 7/7, Java corpus 108/108, JS corpus 33/33 in-scope sections.
- External-scanner machinery landed in item 7: `ScannerVM` + `ScannerCompiler` DSL (mnemonics include `jmp_if_valid/ws/alpha/digit/eq/ne/in_range`, `jmp_if_flag/result/state_eq`, `set_flag/result/state`, `push_byte(s)`, `span`, `mark_end`, `call/ret`, `emit`, `fail`) + `ScannerProgram` shipped inside the blob. The JS scanner is a hand translation vendored at `src/test/resources/upstream/grammars/tree-sitter-javascript/src/scanner.dsl`.
- Shipped blobs: JSON, Java, JavaScript (`src/main/resources/grammars/*/tree-sitter-*-blob.bin`). Vendored upstream sources live under `src/test/resources/upstream/grammars/<grammar>/` (parser.c + corpus + scanner); the vendored JS parser.c is byte-identical to `~/sources/treesitter/grammars/tree-sitter-javascript/src/parser.c`.
- `JsCorpusTest` currently runs only the 33 sections whose expected trees reference one of JS's 8 external tokens; its recorded error-recovery exclusion list covers exactly `destructuring.txt: 'Extra complex literals in expressions'` (expects `(ERROR ...)` / `(MISSING ";")` — item 11 machinery).
- Upstream sources at `~/sources/treesitter/grammars/tree-sitter-typescript/` (vendoring source, MIT): `typescript/src/parser.c` (282K lines), `tsx/src/parser.c`, `common/scanner.h` (347 lines) + per-dialect `src/scanner.c` wrappers (13-line stateless shims), shared corpus `test/corpus/{declarations,expressions,functions,types}.txt` = **112 sections** (36+15+10+51; a section is one `=== title ===` … closing-`===` block — naive `grep -c '^==='` double-counts), zero `ERROR`/`MISSING` expectations, exactly 2 `:language(...)`-tagged sections (`expressions.txt` `Type assertions` → typescript, `Type arguments in JSX` → tsx). Full JS corpus = **116 sections**; exactly one section (the adjudicated one above) expects error-recovery nodes.
- Feasibility probes already run against live code (2026-09-09, jshell over `target/classes`): `ParserCExtractor.extract` on `typescript/src/parser.c` → 383 symbols / 5870 states / 1193 large states / 166 tokens / 10 external symbols / 11 external-state rows in ~950ms; on `tsx/src/parser.c` → 400 symbols / 5986 states / 1167 large states / 172 tokens in ~660ms. Both fit the blob's u16 state encoding. Count convention: extractor symbol counts include 7 runtime-synthesized symbols on top of parser.c's `SYMBOL_COUNT` (376 typescript / 393 tsx) — the same delta as the shipped JS blob.

## Goals

- **JTS-01/02/03**: ship `src/main/resources/grammars/typescript/tree-sitter-typescript-blob.bin` and `src/main/resources/grammars/tsx/tree-sitter-tsx-blob.bin` (existing naming convention; the JavaScript blob already ships); each loads via `Language.fromClasspath` with its external symbol map, external states and compiled scanner program.
- **JTS-04**: upstream corpus runners at ≥ 95% pass per grammar, running **100% of what upstream ships**: JS 115 of 116 sections (the single error-recovery section stays adjudicated to item 11), TS 111 (110 unlabeled + `:language(typescript)`), TSX 111 (110 unlabeled + `:language(tsx)`). The shared TS corpus carries per-section `:language(...)` metadata (e.g. `expressions.txt` `Type assertions` → typescript, `Type arguments in JSX` → tsx); upstream runs it per-dialect with that filter, and the two labeled sections are provably dialect-bound (`type_assertion` appears 644× in the typescript parser.c and 0× in tsx; jsx_* nodes only exist in tsx). The corpus reader parses `:`-prefixed section-header metadata generically. **Roadmap count correction (recorded for the roadmap write-back):** roadmap JTS-04's "≥ 150 JS / ≥ 200 TS / ≥ 100 TSX tests" was derived by double-counting each section's opening and closing `===` lines; the true upstream corpus sizes are 116/112 total (115/111 runnable after dialect/adjudication filters). The acceptance intent — run the entire upstream corpus, ≥ 95% — is unchanged and now bound to the real numbers.
- The TS/TSX external scanner (common/scanner.h) hand-translated into `scanner.dsl` and compiled into both blobs, with the translation kept under `src/test/resources/upstream/grammars/tree-sitter-typescript/common/` next to its C source (JS keeps its dsl next to its scanner.c).
- Every failure below 100% is individually recorded in the test as an adjudicated deviation with the failing section name and a checked root cause (runtime defect → fix in this plan; upstream-expectation vs our documented bounded semantics → recorded list). The ≥ 95% line is the acceptance floor, not a target to hide behind: any fixable defect found by the corpus is fixed here.

## Non-Goals

- Error recovery: `(ERROR` / `(MISSING` expectations (the single adjudicated JS section; TS corpus has none) — item 11.
- Performance work (item 13) and documentation (item 14).
- Python or other grammars (not in this roadmap item).
- Blob format changes: if extraction of TS/TSX required format evolution that would be a blocker to adjudicate here, not a silent format bump (probe says none is needed).

## Scope

### In Scope

- Vendoring: `typescript/src/parser.c`, `tsx/src/parser.c`, `common/scanner.h`, the per-dialect `src/scanner.c` wrappers (13-line stateless shims), `test/corpus/*.txt` (commit vendored files; provenance comment).
- Blob builds: extraction + ship for typescript and tsx; `ScannerCompiler.compile(scanner.dsl, symbolMap)` per dialect.
- `scanner.dsl` hand translation of common/scanner.h (10 external tokens incl. `function_signature_automatic_semicolon`; the `error_recovery` enum entry is also declared — 10 token declarations in enum order, since `ScannerCompiler` validates the declared count against the symbol map — but never emitted).
- Runners: extend `JsCorpusTest` to the full corpus; new `TsCorpusTest` / `TsxCorpusTest` (shared corpus, per-dialect blob, `:language` metadata filter = unlabeled + own dialect) with per-section pass/fail accounting and the adjudicated-deviation list pattern from JsCorpusTest.
- Fixes to the runtime for corpus-revealed defects (lexer/parser/scanner-VM/loader), each with a focused regression test.

### Out Of Scope

- Queries/highlights for TS/TSX; Nop platform integration (item 12); incremental-parse interplay with the TS scanner (already covered by the item 9 conservative external-lex-state exclusion).

## Execution Plan

### Phase 1 — Vendor TS/TSX upstream sources + extraction proof (no blob shipped)

Status: completed
Targets: `src/test/resources/upstream/grammars/tree-sitter-typescript/**`, extraction/section-counting tests

- Item Types: `Proof | Fix`

- [x] Vendor from `~/sources/treesitter/grammars/tree-sitter-typescript/`: `typescript/src/parser.c`, `tsx/src/parser.c`, `common/scanner.h`, the per-dialect `src/scanner.c` wrappers, `test/corpus/*.txt` (4 files), with a PROVENANCE comment (source repo, MIT, commit-less vendoring policy per JS precedent).
- [x] Extraction proof test: `ParserCExtractor.extract` runs on both vendored parser.c files with loader-level invariants asserted (symbol/state/token/external counts per the probe numbers; states fit u16). No blob is written in this phase — a blob with `externalTokenCount > 0` and no scanner program is never built; writing and shipping happens in Phase 2 after the DSL exists.
- [x] Corpus reader helper: counts sections as `=== title ===` … closing-`===` blocks (metadata-aware, `:`-prefixed header lines parsed generically), asserts 112 vendored sections and the per-dialect filter (110 unlabeled + 1 own-dialect = 111 runnable; the 2 labeled sections by name).

Exit Criteria:

- [x] Extraction and section-counting tests green against the vendored files; no blob artifact produced by this phase.
- [x] No owner-doc update required (module-internal; public grammar list documented in item 14).
- [x] `ai-dev/logs/2026/09-09.md` entry added.

### Phase 2 — TS/TSX scanner DSL translation + blob build/ship + scanner token tests

Status: completed
Targets: `src/test/resources/upstream/grammars/tree-sitter-typescript/common/scanner.dsl`, `src/main/resources/grammars/{typescript,tsx}/`, blob build/load tests, scanner tests

- Item Types: `Fix | Proof`

- [x] Hand-translate `common/scanner.h` into `scanner.dsl` (JS precedent — the JS dsl lives next to its scanner.c; this one lives in `common/` next to scanner.h): dispatcher with ALL of scanner.h's `external_scanner_scan` branches — the `TEMPLATE_CHARS` gate (reject when `AUTOMATIC_SEMICOLON` also valid), the `JSX_TEXT` first-win, the ASI/`FUNCTION_SIGNATURE_AUTOMATIC_SEMICOLON` branch with `scanned_comment` fallback to `TERNARY_QMARK`, the standalone `if (valid_symbols[TERNARY_QMARK])` branch, and the four-condition `HTML_COMMENT` gate (`HTML_COMMENT && !LOGICAL_OR && !ESCAPE_SEQUENCE && !REGEX_PATTERN`) — plus subroutines `scan_template_chars` (has_content flag + `mark_end` per iteration), `scan_whitespace_and_comments` (scanned_comment out-param → flag register), `scan_automatic_semicolon` (`}`-then-`:` ternary disambiguation via LOGICAL_OR validity, newline break, operator lookahead cases incl. `+`/`-`/`!` second-char checks and the `in`/`instanceof` walk, `{`-gate via FUNCTION_SIGNATURE validity), `scan_ternary_qmark` (whitespace run then `?`, `?.`/`??` rejection, `: ) ,` rejection, `.` digit continuation), `scan_closing_comment` (`<!--`/`-->` walks + line scan + `mark_end`), `scan_jsx_text` (saw_text/at_newline truth table — needs two registers, e.g. flag + state). The DSL declares all 10 token names in enum order including `error_recovery` (never emitted; `ScannerCompiler` validates the declared-token count against the external symbol map length).
- [x] Build and ship `src/main/resources/grammars/typescript/tree-sitter-typescript-blob.bin` and `src/main/resources/grammars/tsx/tree-sitter-tsx-blob.bin`: extraction + `ScannerCompiler.compile(dsl, symbolMap)` per dialect.
- [x] Blob build/load tests (mirror `JsBlobTest`): loader counts (symbols/states/tokens/externalTokenCount/external symbol map/external states rows/keyword capture), shipped blob byte-identical to a fresh extraction+compile, `ScannerProgram.validate`.
- [x] Token behavior tests: mirror `JsScannerTokenTest`'s token-for-token pattern for the TS scanner (template chars, ASI accept/reject matrix incl. `+ +`/`++` disambiguation and `instanceof`, ternary `?` vs optional chaining, jsx_text truth table, html comment) against hand-derived expectations from scanner.h; scanner state rows from the blob gate the dispatcher exactly as parser.c declares. Dispatcher emits only after subroutine RET (JS precedent — EMIT terminates the VM), so subroutines return via the result register.

Exit Criteria:

- [x] Both blobs load via `Language.fromClasspath` with nonzero externalTokenCount, validated scanner programs, and loader counts matching the extraction; shipped blob byte-identical to a fresh build (`TsBlobTest` 5/5).
- [x] Scanner program compiles for both dialect maps; `ScannerProgram.validate` passes.
- [x] Token-level tests green covering every `scan_*` subroutine branch reachable per scanner.h (each subroutine's accept AND reject paths exercised at least once) — `TsScannerTokenTest` 28/28.
- [x] No owner-doc update required; `ai-dev/logs/2026/09-09.md` updated.

### Phase 3 — Full corpus runners + acceptance + defect triage

Status: completed
Targets: `JsCorpusTest` (full), `TsCorpusTest`, `TsxCorpusTest`, runtime fixes

- Item Types: `Fix | Proof`

- [x] `JsCorpusTest`: run all 116 sections; the only exclusion is the one adjudicated error-recovery section (kept with its upstream-evidence comment); everything else must pass.
- [x] `TsCorpusTest`: 111 sections (110 unlabeled + `:language(typescript)`) against the typescript blob.
- [x] `TsxCorpusTest`: 111 sections (110 unlabeled + `:language(tsx)`) against the tsx blob.
- [x] Triage every failing section: root cause → (a) runtime defect → fix + focused regression test in this plan, or (b) documented bounded-semantics deviation → moved to the test's recorded adjudication list with a one-line reason.
- [x] Acceptance assertion (single test semantics, per review M3): (1) every non-passing section appears by name in the recorded adjudication list (unlisted failures fail the suite), (2) the adjudicated list per grammar stays within the roadmap's ≥ 95% floor (≤ 5 per grammar given 115/111/111 runnable sections), (3) exact pass counts printed and recorded in the plan Verification section. The 95% floor is a reporting baseline, not the assertion.
- [x] Roadmap count correction recorded at write-back: JTS-04's original ≥ 150/≥ 200/≥ 100 numbers were double-counted (`===` open+close lines); the plan binds acceptance to the true corpus sizes.

Exit Criteria:

- [x] **端到端验证**: corpus sections flow `source text → TSParser.parse(language blob) → TSTree.toSExpression()` compared byte-for-byte against upstream expected trees — the same public path as platform consumers.
- [x] Per-grammar pass rates ≥ 95% with exact counts recorded: **JS 115/116, TS 110/111, TSX 110/111** (each 1 adjudicated).
- [x] Every non-passing section appears by name in a recorded adjudication list with a reason; any unlisted failure fails the suite.
- [x] No silent skip: the corpus reader counts sections from the vendored files (drift between vendored corpus and runner fails loudly).
- [x] `./mvnw -pl nop-treesitter -am test -T 1C` green — **342 tests** (301 baseline + new runners; final count 343 after the GLR regression test).
- [x] `ai-dev/logs/2026/09-09.md` updated.

## Closure Gates

> **关闭条件**：只有本 section 所有条目以及每个 Phase 的 Exit Criteria 全部勾选为 `[x]` 后，才能将 frontmatter `status` 改为 `completed`。

- [ ] 所有 in-scope confirmed live defects 已修复（corpus 揭示的 runtime defect 全部修复 + 回归测试）
- [ ] 所有 in-scope confirmed contract drifts 已收敛（无已知入口）
- [ ] 行为/契约结果已达成：TS/TSX blob 加载可用；三语法全量 corpus ≥ 95%（JS 除 1 个 item 11 裁定 section 外全过）
- [ ] 必要 focused verification 已完成（blob build/load tests、scanner token tests、corpus runners）
- [ ] 不存在被静默降级到 deferred / follow-up 的 in-scope live defect 或 contract drift（adjudication list 每条有因）
- [ ] 受影响的 owner docs 已同步，或明确 No owner-doc update required（module-internal；公开文档归 item 14）
- [ ] 独立子 agent / 独立审阅者 closure-audit 已完成并记录证据（写入 `## Closure` 段）
- [ ] **Anti-Hollow Check**：closure audit 已验证（a）TS/TSX blob 真实驱动 parse 路径（corpus 从 `Language.fromClasspath` 到 s-expression 比对），（b）scanner DSL 无空分支/静默 fail-as-success，（c）无 TODO/FIXME 充当完成实现
- [ ] `./mvnw -pl nop-treesitter -am test -T 1C` 通过（301 基线 + 新增全绿）
- [ ] checkstyle / 代码规范检查通过（`checkstyle:check -Pqa` 0 violations）

## Draft Review Record

- 2026-09-09: iteration 1 adversarial review by independent subagent (agent_c5d9e4ce-251c-49bf-af26-eae071c42407): 1 Blocker (corpus `:language()` metadata + per-dialect filtering), 3 Major (blob naming convention; Phase 1/2 order contradiction → Phase 1 narrowed to vendor+extract; acceptance semantics → adjudicated-list-exhaustive + floor + counts), 4 Minor (symbol-count convention note, 10-token DSL declaration, dispatcher branch completeness + two-register jsx_text + dispatcher-emit note, wrapper/dsl-location wording).
- 2026-09-09: iteration 2 review by independent subagent (agent_7008e0fd-0a51-4dc0-b7d9-1f0a7653ce61) caught: the round-1 section counts were double-counted (`===` open+close lines) — true sizes 112 TS / 116 JS / 111 runnable per dialect, roadmap JTS-04 numbers corrected with evidence; the Phase 1 narrowing replacement had silently failed (order-dependent string replace) — Phase 1/Phase 2 boundary now really narrowed (this rewrite); wrapper wording residue fixed. All applied and verified (stale-number grep clean).

## Verification

## Closure
