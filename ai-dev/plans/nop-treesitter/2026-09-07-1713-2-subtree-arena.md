---
status: active
mission: nop-treesitter
work-item: "3"
group: "2026-09-07-1713"
verify: [test]
---

# Subtree arena (compact int32 indices, slot reuse, symbol interning)

## Current Baseline

- `nop-treesitter/` scaffolded: `pom.xml` (JDK 17, `nop-commons`, slf4j, junit-jupiter), registered in root `pom.xml` `<modules>`; `TreeSitterBootstrap` + `TreeSitterBootstrapTest` (2 tests) green on `./mvnw -pl nop-treesitter -am test -T 1C` (commit `0d5971af6a`).
- No `io.nop.treesitter.subtree` package exists; no arena, no symbol interning, no per-node value type.
- Read-only reference: gotreesitter `~/sources/treesitter/gotreesitter/arena.go` (MIT, translate don't copy, never modify). Roadmap ARENA-01..04 define the deliverable set.
- Roadmap item 3 is `todo`; the stages table names this plan as its owner.
- Sibling plans in this group: `2026-09-07-1713-1-ts2java-extractor.md` (independent, parallel) and `2026-09-07-1713-3-lr1-parser-json-corpus.md` (consumes this plan's arena API).

## Goals

- `Subtree` value representation with int fields only — no per-node heap object on the hot path.
- `SubtreeArena` backed by parallel `int[]` arrays with `allocate` / `free` / `get` operations, free-list slot reuse, and geometric growth.
- `SymbolTable` interning (`String ↔ int`) for named tokens.
- 12 focused unit tests (allocate / reuse / grow / intern / equality / parent-arity) plus an integration proof that a nested tree can be built and navigated purely through the public arena API — the exact surface the LR(1) parser plan consumes.

## Non-Goals

- No inline-small-node optimization (roadmap item 5).
- No compact external-node metadata (roadmap item 7).
- No parser / lexer integration (roadmap item 4 — sibling plan `...-3-lr1-parser-json-corpus`).
- No performance benchmarking beyond a smoke profile note (roadmap item 13).

## Phase 1 — Subtree value representation + arena core

Targets: `nop-treesitter/src/main/java/io/nop/treesitter/subtree/`, `src/test/java/io/nop/treesitter/subtree/`

- Item Types: `Fix | Decision | Proof`

- [ ] `Subtree` value type (record with int fields per roadmap ARENA-01: state, symbol, child0..3, extra, padding) with value equality and parent-arity semantics.
- [ ] `Decision` recorded in code or log: parallel `int[]` columns (one per `Subtree` field) chosen over per-node heap objects, per roadmap critical design; documented in `ai-dev/logs/`.
- [ ] `SubtreeArena` (ARENA-02) with `allocate(...)`, `free(int)`, `get(int)`: free-list slot reuse, geometric growth preserving contents.
- [ ] Unit tests: allocate returns monotonically increasing ids until first free; `free` → next `allocate` reuses the slot; grow preserves prior contents; `get` returns consistent field values; parent-arity invariant (childN fields reference valid ids).
- [ ] No silent skip: `get` / `free` on an invalid or never-allocated id throw a typed exception instead of returning null / 0 (Minimum Rules #24).

Exit Criteria:

- [ ] `Subtree`, `SubtreeArena`, and their tests exist under `io.nop.treesitter.subtree` and pass.
- [ ] Slot-reuse behavior is observable in a test: after `free`, arena capacity does not grow on next `allocate`.
- [ ] No silent skip verified: invalid-id access raises, covered by a focused test.
- [ ] `No owner-doc update required` — module-internal data structure; public API docs are roadmap item 14.
- [ ] `ai-dev/logs/{year}/{month}-{day}.md` entry added (reverse-chronological top insert).

## Phase 2 — SymbolTable interning

Targets: `nop-treesitter/src/main/java/io/nop/treesitter/util/`, `src/test/java/io/nop/treesitter/util/`

- Item Types: `Fix | Proof`

- [ ] `SymbolTable`: `intern(String) → int`, `resolve(int) → String`, stable ids (same string → same id), growing table.
- [ ] Unit tests: interning identity across inserts, reverse lookup round-trip, growth beyond initial capacity preserves mappings, no id collisions.

Exit Criteria:

- [ ] `SymbolTable` tests pass and cover identity / reverse-lookup / growth.
- [ ] `No owner-doc update required` — module-internal utility; public API docs are roadmap item 14.
- [ ] `ai-dev/logs/` entry updated.

## Phase 3 — full test suite + downstream-consumer integration proof

Targets: subtree package + tests

- Item Types: `Proof | Follow-up`

- [ ] Complete the roadmap ARENA-04 suite: 12 focused unit tests covering allocate / reuse / grow / intern / equality / parent-arity.
- [ ] Integration proof (wiring): a test builds a nested tree (depth ≥ 3, mixed arity, node types from `SymbolTable`) purely through arena public API, then navigates it (root → children → siblings → parent) — proving the API surface is complete for the parser plan to consume.
- [ ] Follow-up: smoke profile note (rough allocation counts per parse tree shape) recorded in the daily log for roadmap item 13 reference — no tuning in this plan.

Exit Criteria:

- [ ] **端到端验证**: the nested-tree test runs from `allocate` entry through navigation to structural assertions (entry: arena API; exit: traversed tree with expected shape) — no component of the arena public surface left uncalled.
- [ ] **接线验证**: `SymbolTable` is used by the integration test for node types (not a parallel hand-rolled map), proving interning is on the arena's consumption path.
- [ ] All 12 ARENA-04 tests pass; `./mvnw -pl nop-treesitter -am test -T 1C` green.
- [ ] `No owner-doc update required` — module-internal data structure.
- [ ] `ai-dev/logs/` entry updated.

## Draft Review Record

- dispatch review #review-2026-09-07-171303-mission-driver-2026-09-07-1713-2-subtree-arena-1-256ce7a1 to ses_f84c56c66ffepgKHm0tu024wBj
- 2026-09-07：iteration 1，共识 approved #review-2026-09-07-171303-mission-driver-2026-09-07-1713-2-subtree-arena-1-256ce7a1

## Verification

## Closure