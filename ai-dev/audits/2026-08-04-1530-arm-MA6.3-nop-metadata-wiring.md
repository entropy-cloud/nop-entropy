# 审计报告：MA6.3 — nop-metadata 接线完整性（维度 H03）

> Mission: `nop-metadata-audit-remediation`
> 里程碑: MA6（wiring integrity verification）工作项 6.3
> 日期: 2026-08-04
> 审计范围: `nop-metadata/` 全模块组 — beans.xml bean class 存在性、@Inject 可解析性、生成/手写边界、5 条 BizModel→Executor→DAO 运行时调用链、@BizModel 注册
> 审计方法: 只读文件扫描 + 调用点 grep/read 逐跳核实（entry file:line → 每跳 file:line → 出口）
> 对照源: MA2.4（ioc-and-config 合规，2026-08-04-0935）、MA3.1（xdsl，2026-08-04-1136）——本审计不重复其维度
> 审计方式: 纯 RESEARCH（只读），零代码变更

## 摘要

- **总发现数**：0 新增 P0/P1/P2；3 项 watch-only（均为已知问题状态复核，未新增）
- **五条调用链全部连通**（a/b/c/d/e 全 PASS），无 hollow-wiring（孤儿组件）——组件级扫描 0 孤儿
- **裁决**：**PASS** — bean class 存在性 100%（3/3 框架类经平台模块核实）、@Inject 19 唯一类型全部可解析、生成边界干净、@BizModel 40/40 已注册

## 一、Bean class 存在性（任务 1）

全部 `<bean>` 的 class / ioc:type（含 `ioc:type="@bean:id"` 即 class=id）经脚本提取后逐一核实：

| 来源 | bean 数 | class 解析 | 结果 |
|------|---------|-----------|------|
| `_service.beans.xml` | 39 BizModel（`ioc:type="@bean:id"`） | 39/39 存在于 `service/entity/NopMeta*BizModel.java` | OK |
| `_service.beans.xml` | 39 proxy（`class=io.nop.biz.proxy.BizProxyFactoryBean`） | 存在于 `nop-service-framework/nop-biz/.../proxy/BizProxyFactoryBean.java` | OK |
| `_service.beans.xml` | 39 `ioc:type=io.nop.metadata.biz.INopMeta*Biz` | 39/39 存在于 `nop-metadata-dao/.../biz/`（手写接口） | OK |
| `app-service.beans.xml` | 11 | 11/11 存在于 `service/` 对应包 | OK |
| `test-mock.beans.xml` | 2 | MockHttpClient / MockMessageService 存在于 test 源码 | OK |
| `_dao.beans.xml` | 0（空壳） | — | OK |

框架类平台核实：`BizProxyFactoryBean`（nop-biz）、`IHttpClient`（nop-http-api）、`IMessageService`（nop-api-core）均在平台模块中存在。

**结论：0 unresolvable bean。PASS。**

## 二、@Inject 可解析性（任务 2）

37 个 @Inject 字段（19 唯一类型）+ 3 构造器注入 + 3 setter 注入。唯一类型解析：

| 注入类型 | 次数 | 解析到的 bean 定义 |
|----------|------|--------------------|
| IMetaDataSourceConnectionProcessor | 6 | app-service:12（`ioc:default=true`） |
| NopMetaSearchService | 8 | app-service:51 |
| MetaModelChangedEventPublisher | 3 | app-service:29 |
| IDaoProvider | 3+2+1 | 平台 `nopDaoProvider` |
| QualityAlertWorkflowService | 1 | app-service:33 |
| MetaContractChecker | 1 | app-service:17 |
| IReconciliationProcessor | 1 | app-service:23（LocalReconciliationProcessor implements） |
| MetaQualityCheckpointScheduler | 1 | app-service:40 |
| NopMetaIndexBuilder | 1 | app-service:44 |
| LineageTagPropagationProcessor | 1 | app-service:61 |
| AutoClassificationProcessor | 1 | app-service:68 |
| INopMetaTableBiz | 1 | _service:90 `biz_NopMetaTable`（primary=true） |
| NopMetaQualityScoreBizModel | 1 | _service:229 |
| IOrmTemplate / IHttpClient / IMessageService / IWorkflowManager / ISearchEngine(@Named) | 1/1/1/1/3 | 平台默认（nop-orm / nop-http / nop-api / nop-wf / nop-search-lucene） |

接口实现验证：`LocalReconciliationProcessor implements IReconciliationProcessor`（:46）、`MetaDataSourceConnectionProcessor implements IMetaDataSourceConnectionProcessor`（:46）。

**结论：无 nop.metadata 类型缺失 bean 定义。PASS。**

## 三、生成 vs 手写边界（任务 3）

- `_service.beans.xml`（生成）与平台模板 `nop-kernel/nop-codegen/.../templates/orm/{appName}-service/.../_service.beans.xml.xgen` **逐字同构**（每实体 2 bean 对：`ioc:type="@bean:id"` + `BizProxyFactoryBean`，`xpl:if="!notGenCode"`）；39 实体 × 2 = 78 bean 与模板输出完全一致。生成文件引用手写 BizModel/INopMeta*Biz 属模板设计契约（模板即如此发射），非越界。
- `app-service.beans.xml`（手写，11 bean 全带设计注释）只引用手写 service 类。
- `_dao.beans.xml` 空壳（5 行）。
- `test-mock.beans.xml` 仅 test 目录，引用 test 类。
- 手写性核实：BizModel / INopMeta*Biz 均 git 跟踪的历史 plan 提交文件；`_service.beans.xml` 每次随实体 codegen 增量再生成。

**结论：边界干净，无违规。PASS。**

## 四、调用链连通性（任务 4）

### a. query aggregation — PASS

| 跳 | 位置 |
|----|------|
| 入口 `NopMetaTableBizModel.queryAggregation` @BizQuery | `NopMetaTableBizModel.java:242` |
| → `aggregationExecutor.executeAggregation(...)` | `NopMetaTableBizModel.java:254`（executor 构造 :77） |
| `MetaAggregationExecutor.executeAggregation` 分派 | `query/MetaAggregationExecutor.java:74`（processor 选择 :104-117，join 分派 :123-156） |
| 处理器：EntityAggregationProcessor / ExternalAggregationProcessor / SqlAggregationProcessor(→委托 External :34) / 4 个 Join Processor | `MetaAggregationExecutor.java:107/109/111/143-155` |
| 出口：`orm().executeQuery`（EQL） | `EntityAggregationProcessor.java:160`、`EntityEntityJoinAggregationProcessor.java:146`、`MetaJoinExecutor.java:310` |
| 出口：`withConnection` + JDBC | `ExternalAggregationProcessor.java:73/97-101`、`MetaJoinExecutor.java:376` |
| 表数据路径：queryTableData→queryAction→`MetaTableQueryExecutor.executeQuery` / `findAllByQuery` | `NopMetaTableQueryAction.java:127/147/93` |

### b. lineage — PASS

| 跳 | 位置 |
|----|------|
| 入口 `NopMetaLineageEdgeBizModel`（@BizModel:32）recordLineage/extractLineageFromSql/extractColumnLineageFromSql/extractMeasureLineage/getUpstream/getDownstream/getLineagePath/getImpactAnalysis | `NopMetaLineageEdgeBizModel.java:59/105/119/132/147/152/157/164` |
| → `queryAction()`（NopMetaLineageEdgeQueryAction，构造 :48-51） | 各方法 `:110/123/136/...` |
| 解析器：SqlSourceTableExtractor.extract / SqlColumnLineageExtractor.extract / ExpressionMeasureValidator | `NopMetaLineageEdgeQueryAction.java:161/217/305` |
| 出口：`dao.batchSaveEntities/batchUpdateEntities/findAllByQuery/deleteEntity` | `NopMetaLineageEdgeQueryAction.java:192/268/271/398/422/458/493` |

### c. sqlview — PASS（watch-only 备注见 MA6.3-06）

| 跳 | 位置 |
|----|------|
| 入口：createSqlTable @BizMutation / previewSqlFields @BizQuery / resolveTableFields @BizQuery（无独立 NopMetaSqlView*BizModel，sqlview 功能宿主于 NopMetaTableBizModel） | `NopMetaTableBizModel.java:136/172/180` |
| → SqlSelectFieldExtractor.extract | `NopMetaTableBizModel.java:143/174`、`NopMetaTableQueryAction.java:192` |
| → SqlViewFieldTypeInferrer.inferTypes | `NopMetaTableBizModel.java:145/277`、`sqlview/SqlViewFieldTypeInferrer.java:112` |
| 出口：`withConnection` + `SELECT * FROM (...) _t LIMIT 0` + ResultSetMetaData（真实 JDBC） | `SqlViewFieldTypeInferrer.java:127-133` |

### d. import — PASS（无 codegen 跳，见 MA6.3-07）

| 跳 | 位置 |
|----|------|
| 入口 `NopMetaModuleBizModel.importOrmModel` @BizMutation | `NopMetaModuleBizModel.java:165` |
| → OrmModelLoader.loadFromResource（XDSL 解析） | `NopMetaModuleBizModel.java:171` |
| → OrmModelImporter.buildModule/buildOrmModel/buildEntity... | `OrmModelImporter.java:46/62`、调用 `NopMetaModuleBizModel.java:177/294` |
| 出口：`orm().save` + `orm().flushSession` + dao 查询 | `NopMetaModuleBizModel.java:183/296/196`、`NopMetaModuleBizModel.java:279`（findFirstByQuery） |

### e. quality — PASS

| 跳 | 位置 |
|----|------|
| 入口 `NopMetaQualityRuleBizModel.executeQualityRule` @BizMutation（@BizModel:81） | `NopMetaQualityRuleBizModel.java:130` |
| → TableReferenceExecutor.execute → `MetaQualityRuleExecutor.judge` | `NopMetaQualityRuleBizModel.java:158-162`（executor :97） |
| 出口：JDBC `st.executeQuery` ×5 | `MetaQualityRuleExecutor.java:431/474/552/568/590` |
| → `QualityResultWriter.append` → `resultDao.saveEntity` | `NopMetaQualityRuleBizModel.java:351-352`、`QualityResultWriter.java:40-50` |
| 告警链：→ QualityAlertWorkflowService.createAlertWorkflow → `wfManager.newWorkflow("qualityBreachApproval",1L)` | `NopMetaQualityRuleBizModel.java:170`、`QualityAlertWorkflowService.java:76` |
| 检查点链：scheduler.registerCheckpoint / dispatcher.dispatch → MetaQualityCheckpointExecutor → 共享 writer | `NopMetaQualityCheckpointBizModel.java:245/370` |

### 孤儿组件扫描（hollow-wiring 缺陷类）

全部 `service/` 公开组件类交叉引用扫描：**31 个零引用类全部为 @BizModel**（经 beans.xml + GraphQL 分派可达，预期）；executor/processor/writer/extractor/builder 等组件全部有真实调用点（TagLabel:52/59、Search:51/82、Checkpoint:245/370、Manifest:478、Seed:409、Catalog:271/339、Sync:190、Reconcile:136、Scorer:36）。**无孤儿。**

## 五、@BizModel 注册（任务 5）

- 40 个 `@BizModel` 类，40/40 文件存在。
- 注册：39 个 entity BizModel 在 `_service.beans.xml`（`ioc:type="@bean:id"`），1 个 `NopMetaSearchBizModel` 在 `app-service.beans.xml:57`。40/40 均有显式 bean。
- 与 39 个 `INopMeta*Biz` 手写接口一一对应（接口完整性另由测试 `TestNopMetaBizInterfaceCompleteness` 约束，MA2.4 已核）。

**结论：PASS。**

## 六、已知问题状态复核（cross-check only）

| 已知问题 | 状态（2026-08-04） | 与本审计的联动 |
|----------|--------------------|----------------|
| P2-MA3-01：NopMetaSearch.xmeta 位于 `/nop/metadata/NopMetaSearch/`（model/ 扫描根外） | **仍 live**（文件原位未动） | 不阻塞本审计结论；搜索 BizModel bean 注册正常 |
| P1-MA3-001：3 个 xwf 位于 `/nop/metadata/wf/`（不可达） | **仍 live** | 联动点：qualityBreachApproval 工作流被 `QualityAlertWorkflowService.java:76` 调用，模型不可加载 → `NopMetaQualityRuleBizModel.java:170-173` try/catch 降级为 ERROR 日志（告警链退化为静默丢失，无崩溃）——见 MA6.3-08 |
| MA3.1-07：approval-support XPL 遮蔽（`_NopMetaDataContract.xbiz:4` / `_NopMetaTagLabel.xbiz:4` 手改 `x:extends`） | **仍 live** | 生成边界维度已由 MA3.1 登记，本审计不重复 |

## 七、发现清单

### [MA6.3-01] bean class 存在性 — 100% 可解析
- **位置**: 4 个 beans.xml（main 3 + test 1），共 92 bean 引用（78 生成 + 11 手写 + 2 mock + 1 空壳）
- **验证**: 脚本提取全部 class/ioc:type/ioc:type=@bean:id → 与 `src/main/java` + `src/test/java` + 平台模块 glob 比对
- **证据**: 见第一节表格；框架类 `BizProxyFactoryBean`（nop-service-framework/nop-biz/src/main/java/io/nop/biz/proxy/BizProxyFactoryBean.java）、`IHttpClient`（nop-network/nop-http/nop-http-api/.../client/IHttpClient.java）、`IMessageService`（nop-kernel/nop-api-core/.../message/IMessageService.java）
- **裁决**: PASS — 0 缺失

### [MA6.3-02] @Inject 可解析性 — 19 唯一类型全部命中 bean 定义
- **位置**: 37 个 @Inject 字段（main）+ 3 构造器 + 3 setter
- **验证**: 逐类型映射 bean 定义（第二节表）；接口注入经 `ioc:default=true` 单实现解析
- **证据**: `NopMetaReconciliationConfigBizModel.java:77`（IReconciliationProcessor←app-service:23）、`NopMetaQualityCheckpointBizModel.java:83`（NopMetaQualityScoreBizModel←_service:229）、`LocalReconciliationProcessor.java:46`、`MetaDataSourceConnectionProcessor.java:46`
- **裁决**: PASS — 无 nop.metadata 类型缺 bean

### [MA6.3-03] 生成/手写边界 — 干净
- **位置**: `_service.beans.xml`（生成）vs `app-service.beans.xml`（手写）vs `_dao.beans.xml`（空壳）vs `test-mock.beans.xml`（test）
- **验证**: 与平台模板 `nop-kernel/nop-codegen/src/main/resources/_vfs/nop/templates/orm/{appName}-service/.../_service.beans.xml.xgen` 逐字同构（78 bean 对）；手写文件 0 触碰生成物
- **证据**: 模板 `_service.beans.xml.xgen:9-16`（c:for 每实体 2 bean）；app-service.beans.xml:8 仅 import `_service.beans.xml`
- **裁决**: PASS

### [MA6.3-04] 调用链 a（query aggregation）— 连通
- **验证**: 每跳 file:line 见第四节 a；出口为 `orm().executeQuery`（EQL）与 `withConnection`+JDBC 双形态
- **证据**: `NopMetaTableBizModel.java:254`→`MetaAggregationExecutor.java:74`→`EntityAggregationProcessor.java:160`/`ExternalAggregationProcessor.java:73`
- **裁决**: PASS

### [MA6.3-05] 调用链 b（lineage）— 连通
- **验证**: 8 个 @BizQuery/@BizMutation 全部经 `queryAction()` 达 DAO 出口
- **证据**: `NopMetaLineageEdgeBizModel.java:105`→`NopMetaLineageEdgeQueryAction.java:144`（`SqlSourceTableExtractor.extract:161`→`dao.batchSaveEntities:192`）
- **裁决**: PASS

### [MA6.3-06] 调用链 c（sqlview）— 连通，watch-only 备注
- **验证**: 无独立 `NopMetaSqlView*BizModel` 类——sqlview 功能宿主于 `NopMetaTableBizModel`（createSqlTable/previewSqlFields/resolveTableFields），`SqlViewFieldTypeInferrer` 真实执行 LIMIT 0 JDBC
- **证据**: `NopMetaTableBizModel.java:145/277`→`SqlViewFieldTypeInferrer.java:112/127-133`
- **裁决**: PASS — 备注（watch-only）：若架构文档/接口契约声明了独立 NopMetaSqlView 入口，与实际宿主不符需在 MA5 文档一致性轮核对（本审计判定当前实现自洽、无死链）

### [MA6.3-07] 调用链 d（import）— 连通；无 codegen 跳（设计澄清，非缺陷）
- **验证**: importOrmModel→OrmModelLoader→OrmModelImporter→`orm().save`；全模块 grep `CodeGen/renderModel/codegen` 在 import/manifest 路径 0 命中
- **证据**: `NopMetaModuleBizModel.java:165/171/174/177/183/296`；`OrmModelImporter.java:46/62`
- **裁决**: PASS — 设计为 parse→entity-graph→persist（无运行时 codegen），与任务假设的 "codegen 跳" 不同但链路完整达 DAO

### [MA6.3-08] 调用链 e（quality）— 连通；告警子链受 P1-MA3-001 降级（watch-only）
- **验证**: executeQualityRule→judge（JDBC ×5）→QualityResultWriter.saveEntity 主链完整
- **证据**: `NopMetaQualityRuleBizModel.java:130/158-162/351-352`；`MetaQualityRuleExecutor.java:431`；`QualityResultWriter.java:50`
- **联动（watch-only）**: 告警子链 `QualityAlertWorkflowService.java:76` 调 `wfManager.newWorkflow("qualityBreachApproval")`，目标 wf 在不可达目录（P1-MA3-001）→ 调用失败被 `NopMetaQualityRuleBizModel.java:170-173` 捕获记 ERROR 日志。**调用链存在但模型不可达，告警功能当前实际不生效（降级为日志）**；P1-MA3-001 修复后自动恢复。不新增缺陷登记
- **裁决**: PASS（主链）；watch-only（告警子链依赖 P1-MA3-001 修复）

### [MA6.3-09] @BizModel 注册 — 40/40 全覆盖
- **验证**: 40 @BizModel 类全部存在且全部有显式 bean（39 `_service` + 1 `app-service:57`）
- **裁决**: PASS

### [MA6.3-10] 孤儿组件扫描 — 0 孤儿
- **验证**: 全模块类引用扫描，31 个零引用类全部为 @BizModel（预期）；12 个 bean 注册组件全部有真实调用点
- **证据**: TagLabel:52/59、SearchBiz:51/82、CheckpointBiz:245/370、ModuleBiz:478/409、DataSourceBiz:271/339/190、ReconcileBiz:136、ScoreBiz:36
- **裁决**: PASS

## 八、汇总表

### 1) Bean class 存在性表

| beans.xml | bean 数 | 解析成功 | 失败 |
|-----------|---------|---------|------|
| `_service.beans.xml`（生成） | 78（39×2） | 78 | 0 |
| `app-service.beans.xml`（手写） | 11 | 11 | 0 |
| `_dao.beans.xml`（生成空壳） | 0 | 0 | 0 |
| `test-mock.beans.xml`（test） | 2 | 2 | 0 |
| **合计** | **91** | **91** | **0** |

### 2) @Inject 解析表

| 类别 | 数量 | 解析结果 |
|------|------|---------|
| 模块手写 bean 注入 | 24 | 全部命中 app-service（11 类 bean） |
| 生成 bean 注入（BizModel/接口） | 2 | 命中 _service |
| 平台 bean 注入（IDaoProvider/IOrmTemplate/IHttpClient/IMessageService/IWorkflowManager/ISearchEngine） | 11 | 平台默认 |
| **合计** | **37** | **0 缺失** |

### 3) 调用链连通性表

| 链 | 入口 | 出口形态 | 裁决 |
|----|------|---------|------|
| a. query aggregation | NopMetaTableBizModel:242 | EQL `orm().executeQuery` + JDBC `withConnection` | **PASS** |
| b. lineage | NopMetaLineageEdgeBizModel:59-164 | DAO batchSave/findAll/delete | **PASS** |
| c. sqlview | NopMetaTableBizModel:136/172/180 | JDBC LIMIT 0 + ResultSetMetaData | **PASS** |
| d. import | NopMetaModuleBizModel:165 | DAO `orm().save`+flush | **PASS** |
| e. quality | NopMetaQualityRuleBizModel:130 | JDBC ×5 + DAO saveEntity | **PASS** |

## 九、裁决与残留风险

### 裁决：**PASS，无新增 P0/P1/P2**

- bean class 存在性 91/91；@Inject 37/37；生成边界干净；5 条调用链全部连通至真实出口；孤儿组件 0；@BizModel 40/40
- 全部已知问题（P2-MA3-01 / P1-MA3-001 / MA3.1-07）状态 live 复核，与本审计无新增联动缺陷

### 残留风险

- **R1（watch-only）**: 告警子链依赖 P1-MA3-001（wf 目录不可达），当前 `createAlertWorkflow` 调用必失败、仅 ERROR 日志。修复 P1-MA3-001 后建议补 `TestQualityAlertWorkflow` 类测试验证模型可加载
- **R2（信息）**: 无独立 NopMetaSqlViewBizModel，sqlview 功能宿主于 NopMetaTable——若对外契约文档声明独立入口，MA5 文档一致性轮需对齐
- **R3（信息）**: import 链无运行时 codegen 跳（parse→persist 设计）；若后续引入 codegen 能力，需保证走生成边界（`_gen`/模板）而非手写文件

## 文档变化

`No owner-doc update required`（纯审计，零代码变更；发现均为 watch-only/信息级，无文档口径漂移）
