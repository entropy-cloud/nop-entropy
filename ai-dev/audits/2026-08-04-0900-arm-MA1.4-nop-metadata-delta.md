# 审计报告：MA1.4 — nop-metadata Delta 定制合规性（维度 06）

> Mission: `nop-metadata-audit-remediation`
> 里程碑: MA1（结构与依赖审计）工作项 1.4
> 日期: 2026-08-04
> 审计范围: `nop-metadata/*/src/main/resources/_vfs/**` 全部 XML（90 个源文件）
> 对照源: `2026-07-20-1554-deep-audit-nop-metadata/06-delta-customization.md`（07-20"无 Delta"结论）+ `2026-07-19-1118-multi-audit-nop-metadata.md` 维度 06（clean）

## 摘要

- **总发现数**：0（`_delta/` 目录存在性回归确认 + x:extends 全量扫描 100% 合规）
- **`_delta/` 结论**：**无 Delta 层**——07-20"无 Delta"结论回归确认
- **x:extends 扫描**：43 个 XML 文件含 x:extends，全部目标存在且指向合法源，0 违规

## 一、`_delta/` 目录存在性回归（07-20 结论确认）

- 命令：`find nop-metadata -path "*_vfs/_delta*" -not -path "*/target/*"` → **0 结果**
- 8 子模块逐一核对（api/app/codegen/core/dao/meta/service/web）：均无 `_delta/` 目录
- `x:extends="super"` 使用数：**0**（super 仅用于 delta 层覆盖场景；本模块无 delta 层故不适用——符合 plan 判定标准）
- 与 07-20-1554 维度 06 结论（"无 Delta 定制文件"）及 07-19 维度 06（clean）一致：本模块为 in-source 模块（`model/nop-metadata.orm.xml` 自持源模型），定制通过非下划线保留层文件完成，符合 `delta-customization.md` 规则

## 二、x:extends 使用扫描结果（范围：`nop-metadata/*/src/main/resources/_vfs/**` 全部 XML = 90 个源文件；含 `target/` 产物副本不重复统计）

| 类别 | 文件数 | x:extends 目标 | 目标存在性 |
|------|--------|---------------|-----------|
| 手写 view.xml（39 实体页） | 39 | `_gen/_NopMeta*.view.xml`（保留层引用生成物） | ✅ 39/39 全部存在（逐一验证） |
| 手写 orm 保留层 `app.orm.xml` | 1 | `_app.orm.xml`（生成全量模型） | ✅ 存在（`dao/_vfs/nop/metadata/orm/_app.orm.xml`） |
| 手写 action-auth 保留层 `nop-metadata.action-auth.xml` | 1 | `_nop-metadata.action-auth.xml`（生成 auth 基线） | ✅ 存在（`web/_vfs/nop/metadata/auth/`） |
| 手写 app 级 action-auth `app.action-auth.xml` | 1 | `nop-metadata.action-auth.xml`（同 vfs 命名空间跨模块资源合并） | ✅ 存在（解析路径合法） |
| 生成 `_service.beans.xml` | 1 | `/nop/biz/defaults/service-base.beans.xml`（平台默认 beans 基线） | ✅ 平台标准路径 |
| **合计** | **43** | — | **0 缺失** |

> 其余 47 个 XML（39 个 `_gen/_NopMeta*.view.xml` 生成视图、`_dao.beans.xml`、`_app.orm.xml` 等生成物）不含 x:extends，属生成产物，不适用审计判定。

## 三、合规/违规清单

- **合规**：43/43。判定标准逐条核对：
  1. 生成文件 `x:extends` 目标存在且指向合法源 ✅（`_app.orm.xml`/`_nop-metadata.action-auth.xml`/`_service.beans.xml`）
  2. 手写文件 `x:extends="_gen/_NopMeta*.view.xml"` 保留层引用生成物 = 本模块既有惯例（plan Current Baseline 记录）✅
  3. `_delta/` 不存在时 `x:extends="super"` 不适用 ✅（0 用例）
  4. 无 `x:override="remove"` 误用、无循环继承、无跨模块非法覆盖（app.action-auth 覆盖 web 同 vfs 路径文件属标准分层资源合并，非 delta 违规）
- **违规**：0

## 四、历史发现对照

| 轮次限定 ID | 描述 | 本审计结论 |
|------------|------|-----------|
| `2026-07-20-1554#维度06`（07-20"无 Delta"） | 8 子模块无 Delta 定制文件 | ✅ 回归确认（live 0 `_delta/`） |
| `2026-07-19-1118#维度06`（clean） | 无 Delta 文件，in-source 模块约定 | ✅ 一致 |

## 五、总结评估

nop-metadata Delta 定制**完全合规**："无 Delta 层"结论经 2026-08-04 live 回归确认；x:extends 使用全部集中在保留层惯例（手写引用生成物），43/43 目标存在、0 违规。本维度无 finding，无 P0/P1/P2/P3 新增项。

## 文档变化

`No owner-doc update required`（审计报告为证据层；结论与 `docs-for-ai/02-core-guides/delta-customization.md` 规则一致）
