# MA5.3 审计报告：命名与术语一致性（维度 19）

> **里程碑**: MA5（`nop-metadata-audit-remediation` roadmap，Item 5.3）
> **轮次**: 2026-08-04（live repo 审计）
> **报告**: `ai-dev/audits/2026-08-04-1610-arm-MA5.3-nop-metadata-naming.md`
> **状态**: done（待独立 closure audit）
> **审计方式**: 独立子 agent 深挖（维度 19 维度正文：五方对齐程序化比对 + 机械扫描）+ 主 agent 全部 finding 源码级 live 复核（orm.xml 列对、JOB_NAME_PREFIX、xmeta displayName、测试类名、双拼写计数）
> **维度来源**: `ai-dev/skills/deep-audit-prompts.md` 维度 19
> **计划**: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-04-1543-1-ma5-doc-consistency-audit.md` Phase 3

## 审计范围

nop-metadata 8 子模块命名面：实体↔BizModel↔I*Biz↔xmeta↔i18n 五方对齐、`*Service`/`*BizModel`/`*DTO`/`*Entity` 命名规范、列名 code↔name 机械变换、同概念双名、错误码前缀、bean 命名、topic 命名、i18n displayName 术语。

## 命名规范核对表

| 规范 | 实测 | 结论 |
|------|------|------|
| 实体↔BizModel↔I*Biz↔xmeta↔i18n 五方对齐 | 39/39 一致（entity className ↔ `@BizModel("NopMetaX")` ↔ `INopMetaXBiz` ↔ `NopMetaX.xmeta` ↔ i18n en/zh entity label，含值一致性 0 差异） | ✅ |
| BizModel 类名规则（类名 = 注解名 + "BizModel"） | 40/40 一致（+1 = NopMetaSearch 虚拟模型，无实体/接口，符合虚拟 BizModel 模式） | ✅ |
| 实体↔表名（`nop_meta_*` 全称约定） | 39/39（历史 recon 缩写问题已解决：`nop_meta_reconciliation_*`） | ✅ |
| 列名 code↔name 机械变换 | 668 列仅 3 处非机械对（UNIQUE/uniqueIndex、IS_STATIC/staticDict、+已裁决 IS_PRIMARY/primaryField） | ⚠️ 2 处新发现 |
| 同概念双名扫描（status/state、type/kind、*Id/*ID） | 无 state/kind；`status`/`type` 一致；`*Id` 全小写 d；`configId` 全库统一 | ✅（1 处 dataSource 双拼写 + 1 处 entityId 歧义除外） |
| 错误码前缀 | 209/209 `ErrorCode.define` 全部 `nop.err.metadata.*`；NopMetadataErrors extends 全部 10 个子接口 | ✅ |
| bean id ↔ class 名（FQN 约定） | 10/11；唯一例外 `metaQualityCheckpointScheduler`（类内 BEAN_NAME 常量 + 注释，调度器短名注册惯例，有意设计） | ✅ |
| prop displayName 三方一致（orm↔xmeta↔i18n） | 10 实体 195 列 0 差异（zh+en） | ✅ |
| `*Service` 违规扫描 | 仅 P2-MA4-501 2 处存活；无 *Controller/*Manager；MockMessageService 排除（implements 框架 IMessageService） | ⚠️ 见下 |

## P2-MA4-501 交叉核对记录（不重复登记为 MA5 新发现）

| 违规类 | 位置 | live 状态（2026-08-04 复核） |
|--------|------|------------------------------|
| `NopMetaSearchService` | `service/search/NopMetaSearchService.java:16` | **仍存活**（MR2 待修，命名违规代码修复归 MR2） |
| `QualityAlertWorkflowService` | `service/quality/QualityAlertWorkflowService.java:34` | **仍存活**（MR2 待修） |

补充交叉核对：历史 4 处违规中已改名的 2 处（AutoClassificationService/LineageTagPropagationService → *Processor）live 确认已修复；`*Service` grep 全模块 0 新增（测试 mock MockMessageService 按 MA4.5 裁定不算违规）。

## 发现清单

| ID | 位置 | 问题 → live 证据 | P 级 | 建议 | 归属 |
|----|------|------------------|------|------|------|
| P2-MA5-301 | `orm.xml:383`（DATA_SOURCE_ID/dataSourceId）vs `orm.xml:392`（DATASOURCE_TYPE/datasourceType） | **同实体 dataSource/datasource 双拼写**：GraphQL 暴露面同时出现两种拼写；main 代码 74×dataSourceId vs 61×datasourceType；dict 名已单字（meta/datasource-*）；平台主流单字（nop-auth 134 处） | P2 | 统一为 `datasource*`（或全量 data_source*）；ORM 变更需 plan-first | MR2（带 plan-first 旗标，与 P2-MA4-501 批次分开评估迁移成本） |
| P3-MA5-302 | `orm.xml:984` | `code="UNIQUE" name="uniqueIndex"` 非机械对（期望 `unique`）；非运行时问题——三方言 DDL 均正确引号（Oracle/PG `"UNIQUE"`、MySQL 反引号），与已裁决 PRIMARY/CONSTRAINT（未引号报错）不同类；name 有意避免 Java `unique` | P3 | 接受现状或 code→`UNIQUE_INDEX` 求机械一致 | MR3 |
| P3-MA5-303 | `orm.xml:1134` | `code="IS_STATIC" name="staticDict"` 非机械对（期望 IS_STATIC↔isStatic 或 STATIC_DICT↔staticDict）；3-way 内部自洽（xmeta:52 + i18n:201 均 staticDict），仅 code↔name 不机械；DDL 无风险（IS_STATIC 非保留字） | P3 | 对齐 pair（STATIC_DICT 或 isStatic）；ORM 变更 plan-first | MR3 |
| P3-MA5-304 | `service/_vfs/nop/metadata/NopMetaSearch/NopMetaSearch.xmeta:2` | 虚拟模型 displayName `元数据搜索/Metadata Search` 打破全模型统一 "Meta X" 模式（39 实体全部 `Meta ...`）；`_nop-metadata.i18n.yaml` 与 web i18n 均无 NopMetaSearch label | P3 | `Meta Search` + 补 i18n/web label | MR3 |
| P3-MA5-305 | `orm.xml` entityId 6 列（QualityRule:1978/ReconciliationEntity:2720/ModelChangedEvent:2857/TagLabel:3286 等多态挂载引用，带 entityType 限定）vs metaEntityId 9 列（EntityField/Relation/UniqueKey/Index 等严格 FK） | 同概念双名歧义（多态引用 vs 严格 FK 语义差异已文档化——orm.xml:2714 注释"不直接 FK…有意设计"），但 `entityId`/`metaEntityId` 命名无法自描述差异 | P3 | 文档注明多态/严格 FK 区分，或改名 `assetId`/`refEntityId`（改名需 plan-first） | MR3（文档注明为主） |
| P3-MA5-306 | `service/src/test/.../TestQualityAlertWorkflowServices.java` | 复数测试名镜像违规类名——MR2 改名 QualityAlertWorkflowService 时必须连带同步改名测试 | P3 | `TestQualityAlertWorkflowDispatcher`（随 MR2 改名批次） | MR2（连带） |
| P3-MA5-307 | `quality/MetaQualityCheckpointScheduler.java:76` | `JOB_NAME_PREFIX = "nop-meta-quality-checkpoint-"` 连字符命名——与 `nop_meta_*` 下划线约定不同 namespace（job 名非 topic，平台 job 名惯例 camelCase），为全仓仅剩的 `nop-meta-*` 连字符标识（旧 topic `nop-meta-metadata` 已修复） | P3 | `nop_meta_quality_checkpoint_` 或 camelCase | MR3 |

**汇总：0 P0 / 0 P1 / 1 P2 / 6 P3**。

## 已登记项交叉核对（不重复登记）

- `IS_PRIMARY/primaryField`（P2-MA2-03 已裁决 fixed，orm.xml:747 + xmeta:70 确认）✅
- `CONSTRAINT_NAME/constraintName`（同上）✅
- `IndexResult`（@DataBean 非 *DTO 命名，api/dto/IndexResult.java:16，MA4.2 已知）✅
- `CheckpointExtConfig`（Config bean 模式非 DTO，P2-MA4-101 覆盖）✅
- topic `nop_meta_metadata` 正确；旧 `nop-meta-metadata` 仅存 NopMetaSearchService.java:22 javadoc 历史说明（有效记载）✅
- P2-MA3-01（NopMetaSearch.xmeta 不可达）live 确认仍在，已登记不重复 ✅

## 文档变化

`No owner-doc update required` — 纯审计，命名修复归 MR2/MR3；本报告为 MR2（P2-MA4-501 改名 + P2-MA5-301 裁决）交叉对账输入。

## 测试

`No new test required`: 纯审计计划零代码变更。

## 汇总

- 0 P0 / 0 P1 / 1 P2 / 6 P3：命名面整体高一致（五方对齐 39/39、错误码 209/209、表名 39/39）
- 唯一 P2（P2-MA5-301 dataSource 双拼写）为 API 面同概念双名，需 ORM 模型变更（plan-first），归 MR2 裁决
- 交叉核对 P2-MA4-501 两处 `*Service` 违规仍存活，确认 MR2 修复归属；MA5.3 报告为 MR2 改名执行交叉对账输入（plan Non-Blocking Follow-ups 声明）
- 无静默降级：全部发现有明确归属（MR2/MR3）
- 审计方式偏差：无
