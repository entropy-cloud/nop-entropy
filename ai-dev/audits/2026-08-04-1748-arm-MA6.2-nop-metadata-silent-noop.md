# MA6.2 审计报告：静默跳过检测（维度 H02）

> **里程碑**: MA6（nop-metadata-audit-remediation roadmap，Item 6.2）
> **轮次**: 2026-08-04（live repo 审计）
> **报告**: `ai-dev/audits/2026-08-04-1748-arm-MA6.2-nop-metadata-silent-noop.md`
> **状态**: done（待独立 closure audit）
> **审计方式**: 独立子 agent 全量扫描（283 main 文件、60 个含 catch 文件逐一读）+ 主 agent 关键 finding 源码级复核
> **维度来源**: `ai-dev/skills/open-ended-adversarial-review-prompt.md`（H02 静默跳过）
> **计划**: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-04-1543-2-ma6-residual-risk-audit.md` Phase 2

## 审计范围

nop-metadata 8 子模块 main Java 静默跳过面：空 catch、catch-and-swallow、catch→return null 类模式（MetaQualityScorer 先例）、条件不满足静默返回、`// TODO`/`// FIXME` 标记当作完成；与已登记项（P2-MA4-001/002、07-23 维度09 家族、P2-MA5-401）交叉核对。

## 发现清单

| ID | 位置 | 模式 | 问题 → live 证据 | P 级 | 建议 | 归属 |
|----|------|------|------------------|------|------|------|
| MA6.2-001 | `entity/AutoClassificationProcessor.java:132-133` | catch-and-continue | `catch (Exception e) { continue; }` 包 `Pattern.compile(pattern)`——规则配置 regex 非法时该规则静默跳过（无日志、无错误上浮），自动分类特性静默停止应用该规则；"配置错误"与"无匹配"不可区分 | P3 | 补 LOG.warn + 计数 | MR2 |
| MA6.2-002 | `profiling/MetaTableProfiler.java:191-194` | catch-and-swallow → 数据失真 | `WHERE col=''` SQL 失败（含真实错误：列被删/权限/连接）catch 后 `setEmptyCount(0L)` 无日志——SQLException 与"CLOB 不支持比较"不可区分，产生错误的空串统计；同族 P3：`queryNullableDouble:441-444` / `queryNullableLong:460-462`（NumberFormatException→null 无日志） | P2 | 区分真实 SQLException（记 0 + LOG.warn），仅类型不支持静默 | MR2 |
| MA6.2-003 | `quality/MetaQualityScorer.java:268-270` | catch→return null（配置降级） | `readExtConfigDimension` 非法 extConfig JSON 静默 `return null` 无日志——维度覆盖被丢弃，规则按默认维度评分，零信号 | P3 | 补 LOG.warn | MR2 |
| MA6.2-004 | `quality/MetaQualityCheckpointExecutor.java:357-358` | catch→return empty（不对称） | `parseValidations` 非法 JSON 静默 `return Collections.emptyList()`，而兄弟路径 `actions` 解析（:314-318）有 LOG.warn——畸形 validations 得到空规则集，随后被 `ERR_CHECKPOINT_NO_RULES` 门（:110-120）报出误导性"无规则"而非"配置非法" | P3 | 与 actions 路径对齐补 LOG.warn | MR2 |
| MA6.2-005 | `entity/NopMetaQualityCheckpointBizModel.java:340-343` | catch→fail-open | `readAutoScoreConfig` 解析失败静默 `return true`（autoScore 默认开）无日志——配置笔误静默改变检查点行为 | P3 | 补 LOG.warn（意图保留） | MR2 |
| MA6.2-006 | `search/NopMetaIndexBuilder.java:110-112` | 部分失败吞没 | `refreshBlocking` 失败仅 LOG.warn，failed 计数不增、`result.setIndexed(docs.size())` 仍报全部成功——索引陈旧但结果宣称已索引 | P3 | failed 计数 + 结果反映 | MR2 |
| MA6.2-007 | `quality/MetaQualityRuleExecutor.java:714-716` | catch→return null（配置错误隐藏） | `getDouble` NumberFormatException→null 无日志——非数值 minRows/maxAgeMinutes 参数被静默当"未配置"回退默认，按与用户意图不同的语义判分 | P3 | 补 LOG.warn | MR2 |

## 已登记项交叉核对表

| 登记项 | 登记位置 | live 状态（2026-08-04） | 结论 |
|--------|---------|------------------------|------|
| P2-MA4-001（getWfNameFromMeta catch-all 无日志） | `NopMetaTagLabelBizModel.java:106-112` | **仍存在**（:111 catch → :112 return null，无日志） | 确认未修复，MR2 |
| P2-MA4-002（safeProductName 唯一无日志实现） | `SqlViewFieldTypeInferrer.java:198-202` | **仍存在**（:198-204；8 个 safeProductName 副本中唯一无日志） | 确认未修复，MR2 |
| 07-23 维度09：MetaQualityRuleExecutor catch 600/607 | :600/607 | **仍存在**（:600-602 注释性 catch→bool fallback；:607-609 catch→return null） | 确认未修复，MR2 |
| 07-23 维度09：MetaDataSourceConnectionProcessor 311-316 | :311-316 | **仍存在**（:314-316 `catch (NumberFormatException ignored)` + 注释） | 确认未修复，MR2 |
| 07-23 维度09：CheckpointActionDispatcher 323-328 | :323-328 | **仍存在**（:325-327 同模式） | 确认未修复，MR2 |
| P2-MA5-401（getProp("wf:wfName") 恒 null） | `NopMetaTagLabelBizModel.java:106-110` | **仍存在**（:108-110 `getObjMeta().getProp("wf:wfName")`，SchemaImpl.getProp 仅查 props map → 恒 null） | 确认未修复，MR2（与 P2-MA4-001 同方法不同根因） |
| AggregationContext.safeProductName | — | **不存在**（MA4.1 复核确认），仅 `AggregationHelper.safeProductName` 存在且有日志 | 不列入（符合计划预期） |

## 合法性确认（非发现）

- 全部 8 个 safeProductName 副本中 7 个有 WARN/ERROR 日志（唯一无日志者 = P2-MA4-002 已登记）
- `AutoClassificationProcessor:257-260` / `LineageTagPropagationProcessor:184-187`：LOG.error + 上下文，合法
- `NopMetaModuleBizModel:236-240/281-284`：WARN + 文档化 fallback，合法
- `NopMetaSearchService:58-85`：fail-open 但 LOG.error，合法（搜索降级设计）
- `MetaQualityCheckpointExecutor:104-120`：显式 throw 门禁，非静默
- 全模块无完全空 catch、无 debug-only swallow、无吞异常后继续假装成功的核心执行路径
- 唯一 TODO（NopMetaModuleBizModel.java:87）为纯重构提示，标注代码全部实现，合法

## 文档变化

`No owner-doc update required` — 纯审计零代码变更；修复归 MR2（roadmap 规则 1）。

## 测试

`No new test required`: 纯审计计划零代码变更。

## 汇总

- **0 P0 / 0 P1 / 1 P2 / 6 P3**（新增）；已登记 7 项全部确认仍 open（MR2），无修复、无漂移
- 模块整体纪律良好：无空 catch、异常重抛全部带 ErrorCode、fallback 大多有日志
- 新增发现全部为"文档化注释 fallback 缺日志"类低危静默路径（quality/profiling/search 侧链），核心执行域无静默跳过
- 无静默降级：全部发现有明确归属（MR2）
- 审计方式偏差：无

## 盲区自评

- 运行时才触发的静默路径（如特定 SQL 方言失败分支）静态扫描可能漏检；本审计以 60 个含 catch 文件全读覆盖
- 非 Java 资源（xpl/xml 中 c:script 的静默跳过）由 MA3.1 XDSL 审计覆盖，不在本维度
