# MA6.4 审计报告：敏感信息泄露扫描（维度 H05）

> **里程碑**: MA6（nop-metadata-audit-remediation roadmap，Item 6.4）
> **轮次**: 2026-08-04（live repo 审计）
> **报告**: `ai-dev/audits/2026-08-04-1748-arm-MA6.4-nop-metadata-sensitive-leak.md`
> **状态**: done（待独立 closure audit）
> **审计方式**: 独立子 agent 全量扫描（main + test + resources + deploy，533 文件）+ 主 agent 关键 finding 源码级复核
> **维度来源**: `ai-dev/skills/open-ended-adversarial-review-prompt.md`（H05 敏感信息泄露）
> **计划**: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-04-1543-2-ma6-residual-risk-audit.md` Phase 4

## 审计范围

nop-metadata 8 子模块 main + test + 配置文件：JDBC URL/密码/令牌/API Key/连接串在日志、错误消息、配置、测试夹具中的泄露；连接串脱敏链路回归核对（凭据管理深挖归 MA7.2，本项只做泄露面扫描）。基线对照：MA3.3（redaction 链验证 + ARG_RAW_JDBC_URL 残余观察 P3）。

## 发现清单

| ID | 位置 | 泄露类型 | 问题 → live 证据 | P 级 | 建议 | 归属 |
|----|------|---------|------------------|------|------|------|
| MA6.4-01 | `connection/MetaDataSourceConnectionProcessor.java:233,241,251` + `NopMetadataArgs.java:58` | error-message-leak（错误路径凭据回显） | 3 处 `ERR_DATASOURCE_JDBC_URL_BLOCKED` 均 `.param(ARG_RAW_JDBC_URL, jdbcUrl)` 附原始 URL（含 user:pass@），脱敏副本仅用于模板 description；`NopException.getMessage()`（NopException.java:325-331）无条件拼接 params → 原始 URL **确定性**进入：①日志（GraphQLEngine.java:581 / NopException.logIfNotTraced / NopMetaDataSourceBizModel.java:197,284 / MetaQualityCheckpointExecutor.java:161）②API 响应（NopMetadataHelper.toErrorMessage:38-43 → ErrorDTO.message，NopMetaDataSourceBizModel.java:200,287）③webhook 外部端点（CheckpointActionDispatcher.java:217 JsonTool.stringify(summary) 含 errors）——比 MA3.3"可能被 dump"更确定；测试 `TestMetaDataSourceConnectionSecurity.java:256-258` 固化此行为 | P3 | 移除 ARG_RAW_JDBC_URL 或改用脱敏值（MA7.2 归口） | MA7.2/MR2 建议 |
| MA6.4-02 | `quality/CheckpointActionDispatcher.java:225,231,255,263` + `QualityErrors.java:66-70,75-82` | error-message-leak（webhook URL 零脱敏） | webhook URL 可内嵌凭据（`https://user:pass@host/`）或 query token，错误路径模板直接渲染 `{url}` 入用户可见 description + params；与 JDBC 路径不同（有 redactJdbcUrl），webhook 路径零脱敏；MA3.3-05 只核 allowlist 逻辑未覆盖此点 | P3 | 错误消息/params 用脱敏 URL（剥离 userinfo/query） | MA7.2 |
| MA6.4-03 | `connection/MetaDataSourceConnectionProcessor.java:381-386`（`newNopConnectException` 取 `e.getMessage()`）、`TableReferenceExecutor.java:93-109`、`MetaTableQueryExecutor.java:139-141` + `DataSourceErrors.java:27-30,77-83,110-113` | error-message-leak（driver 消息透传） | `{error}` = driver SQLException 原文，部分 driver 失败消息携带完整 `jdbc:mysql://user:pass@host:...` → 进用户可见描述 + 日志；driver 相关但模块嵌入前未脱敏 | P3 | 嵌入前对 `{error}` 做 URL 凭据脱敏 | MA7.2 |
| MA6.4-04 | `nop-metadata-app/src/main/resources/application.yaml:7` | config-leak（弱默认密钥） | `enc-key: change-me-in-production` 占位符（注释声明生产用 `NOP_AUTH_JWT_ENC_KEY` env 注入）；若生产部署未覆盖 env，JWT 密钥为公开已知值 | P3 | 部署纪律项（env 覆盖），非代码缺陷 | 部署侧/watch |
| MA6.4-05 | 平台 `GraphQLWebService.java:82,95-97,161` | log-leak（平台侧 informational） | GraphQL 请求变量/完整 query/响应 body DEBUG/INFO 全量日志；nop-metadata mutations 以 `dataSourceId` 引用凭据，本模块无直接触发面 | P3 informational | 平台治理（日志级别/脱敏），非 nop-metadata 缺陷 | 平台（记录） |
| MA6.4-06 | `reconciliation/LocalReconciliationProcessor.java:189-191` | log-leak（低危 informational） | `LOG.warn("parseProperties failed -- propertiesJsonSnippet={}", ...)` 前 200 字符原始业务 JSON；该配置为列匹配属性非凭据载体 | P3 informational | 防御纵深留意，无凭据风险证据 | 记录 |

## 通过项（无缺陷）

- **MA6.4-07 测试夹具扫描**：全为本地 H2（`jdbc:h2:mem` + sa/空密码）或明显伪造值；`admin:s3cret@prod-db` 类值仅出现在 NEGATIVE 脱敏/拒绝断言（期望被 redact/block），属合法对抗性测试
- **MA6.4-08 脱敏链回归核查（无绕过）**：orm.xml:397 connectionConfig `tagSet="sensitive"`（生成侧 _app.orm.xml:376 一致）+ :1284,1291 sourceSql/buildSql 亦 sensitive；留存层 NopMetaDataSource.xmeta:14-15 published/insertable/updatable/queryable/sortable 全 false；`MetaModelChangedEventPublisher.java:70-88,196-231` REDACTED_VALUE + 兜底列名（不读实际值）；anti-hollow 断言 `TestMetaModelChangedEventPublisherSecurity.java:147` `Mockito.verify(entity, never()).orm_propValueByName("connectionConfig")`；新序列化路径排查（搜索索引仅收 MetaEntity/MetaTable/MetaEntityField 无 connectionConfig；details JSON 仅元数据）**未发现任何绕过点**
- **MA6.4-09 硬编码凭据全量扫描（0 命中）**：533 文件（含 yaml/properties/_vfs/beans/deploy SQL/autotest json5）；deploy/sql 为纯 DDL；唯一"凭据"为 H2 sa/空密码（合法 dev）与注释掉的 mysql `nop-test` 示例（application.yaml:31-33）

## 连接串脱敏链路回归核对结论

脱敏主链路完整成立：ORM tagSet（sensitive）→ xmeta 写禁（published=false + insertable/updatable=false）→ 事件 REDACTED_VALUE + 兜底列名 → anti-hollow never-read 测试。**无回归、无绕过**。主要残留为错误路径凭据回显（MA6.4-01/02/03），全部 P3 且归 MA7.2 凭据管理深挖（本项按计划只做泄露面扫描）。

## 文档变化

`No owner-doc update required` — 纯审计零代码变更；修复建议归 MA7.2/MR 批次。

## 测试

`No new test required`: 纯审计计划零代码变更。

## 汇总

- **0 P0 / 0 P1 / 0 P2 / 4 P3 + 2 informational**；通过项 3 个（测试夹具/脱敏链/硬编码凭据）
- 凭据脱敏主链路完整成立，无硬编码凭据、无真实测试夹具泄露
- 主要泄漏面集中在错误路径凭据回显（NopException.getMessage() 无条件拼接 params 使 raw JDBC URL 确定性进入日志/API/webhook）
- 无静默降级：全部发现有明确归属（MA7.2/MR）
- 审计方式偏差：无

## 盲区自评

- 运行时触发才出现的泄露路径（特定 driver 错误消息格式）静态扫描为近似评估；MA7.2 深挖时建议补充 driver 级实际错误消息验证
- 平台层 GraphQL 全量日志（MA6.4-05）为平台治理面，本模块审计只能记录
