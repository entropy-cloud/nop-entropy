# MA6.1 审计报告：空壳实现扫描（维度 H01）

> **里程碑**: MA6（nop-metadata-audit-remediation roadmap，Item 6.1）
> **轮次**: 2026-08-04（live repo 审计）
> **报告**: `ai-dev/audits/2026-08-04-1748-arm-MA6.1-nop-metadata-hollow-scan.md`
> **状态**: done（待独立 closure audit）
> **审计方式**: 自动化扫描（`scan-hollow-implementations.mjs` 整组 8 子模块 + 阳性对照）+ 独立子 agent 对抗性人工扫描（111 个 medium finding 逐一体读 + 接口/桩模式全量扫描）+ 主 agent 关键 finding 源码级复核
> **维度来源**: `ai-dev/skills/open-ended-adversarial-review-prompt.md`（H01 空壳实现）
> **计划**: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-04-1543-2-ma6-residual-risk-audit.md` Phase 1

## 审计范围

nop-metadata 8 子模块 main Java（283 文件）空壳实现面：接口有声明无实现、空方法体、`throw UnsupportedOperationException` 作为正常路径、stub/placeholder 填充、单行 `return null` 作方法体的占位语义。

## 自动化扫描结果（工具 + 退出码 + 模块范围 + 阳性对照）

### 扫描命令与范围

| 项目 | 值 |
|------|-----|
| 命令 | `node ai-dev/tools/scan-hollow-implementations.mjs --module nop-metadata --severity high`（整组目录；工具不支持 glob，逐模块调用须嵌套路径） |
| 模块范围 | `nop-metadata/` 整组 8 子模块（api/core/codegen/dao/meta/service/web/app） |
| 扫描对象 | src/main only（工具默认 `--src-only`） |
| main Java 文件数 | **283**（`find nop-metadata -path "*/src/main/java/*.java" -not -path "*/target/*"` 实测） |
| test Java 文件数 | **100**（对照基线） |
| 退出码 | **0** |

### 主扫描（--severity high）：0 findings

| Severity | Count |
|----------|-------|
| Critical | 0 |
| High | 0 |
| **Total** | **0** |

### 阳性对照（--severity info）：112 findings —— 证明扫描真实执行，非空转伪证

| Severity | Count | 说明 |
|----------|-------|------|
| Medium | 111 | P2b 单行 return null（73）+ P8 continue 类（38）等 |
| Low | 1 | P4 TODO（NopMetaModuleBizModel.java:87） |
| **Total** | **112** | 明细见 `_tmp/ma6/scan-info-full.txt` |

> **阳性对照意义**（防空转伪证）：`--module nop-metadata-service` 等顶层无效路径会静默返回空结果 + exit 0，与"真干净"不可区分（MR1 closure 记录即为此类空转）。本计划强制 --severity info 对照：112 条带 file:line 的 finding 明细证明工具真实遍历了 283 个 main Java 文件。

## 人工扫描清单（对抗性）

- **接口有声明无实现**：36 个 I*Biz 接口全部有实现类（dao/biz 接口 ↔ service/entity BizModel 1:1 脚本交叉核对）；`IMetaDataSourceConnectionProcessor`→`MetaDataSourceConnectionProcessor.java:46`；`IReconciliationProcessor`→`LocalReconciliationProcessor.java:46`；`AggregationProcessor`→7 个实现类全部有真实逻辑。**0 孤儿接口**。
- **空方法体**：main Java 全量扫描 0 处。
- **UnsupportedOperationException 作为正常路径**：main 0 处（仅 javadoc 引用历史修复：`MetaDataSourceConnectionProcessor.java:40,205`、`ExternalTableStructureReader.java:137` 说明维度09-07 已将 UOE 改为 NopException）。
- **stub/placeholder/dummy**：main 0 处（`_gen/` 排除）。
- **TODO/FIXME 当作完成**：唯一 TODO 为 `NopMetaModuleBizModel.java:87` 重构提示（模块 579 行，标注代码全部实现，合法）。

## 发现清单

| ID | 位置 | 问题 → live 证据 | P 级 | 建议 | 归属 |
|----|------|------------------|------|------|------|
| MA6.1-001 | `nop-metadata-api/.../dto/AggregationRowDTO.java`（全文件） | **死 DTO**：main/test 零引用（rg 命中仅限自身源码文件 + ai-dev 文档引用）；与已登记 P2-MA4-101（CheckpointExtConfig 死 DTO）同族——"DTO 存在"易被误读为"功能存在" | P2 | 接线使用或删除；MA4.2 已发现其与执行器行结构不匹配 | MR2 |
| MA6.1-002 | `query/TestCrossDbInMemoryAggregationProcessor.java:16-31` | **3 个空壳测试（P1-MA4-601 家族未登记的第 7 个类）**：`testImplementsAggregationProcessor`（instanceof）+ `testExecuteWithNullContextThrowsNpe` + `testCanInstantiate`——execute() 改错（如恒返回空）全部仍通过；MA4.6 登记 6 类 18 个，实际 7 类 21 个 | P1 | 归入 P1-MA4-601 家族修复（MR2 最小行为测试改造），登记计数修正为 7 类/21 个 | MR2 |
| MA6.1-003 | `query/CrossDbInMemoryAggregationProcessor.java:36-38` | **execute() 零契约保护（P2-MA4-301 家族第 4 个）**：`execute(null)` → 首语句 `context.getTable()` 裸 NPE；该处理器是 JOIN 跨库 fallback（`MixedSameDbJoinAggregationProcessor.java:78` 调用）；`SqlAggregationProcessor.java:26` 同模式（委托门面，非 JOIN） | P2 | 与 P2-MA4-301 同批补 null 契约校验；登记修正为 4 个 JOIN 路径处理器 | MR2 |
| MA6.1-004 | `entity/AutoClassificationProcessor.java:256`、`entity/LineageTagPropagationProcessor.java:183` | **静默 instanceof-miss**：`bizObjectManager().invoke("save",...)` 成功后 `result instanceof NopMetaTagLabel` 不成立则 `return null`，写入成功但结果静默丢失、无日志 | P3 | 补 LOG.warn 或显式错误（与 P2-MA4-001/002 同类但影响小，写入已成功） | MR2 |

## 已登记项交叉核对（P1-MA4-601 / P2-MA4-301 家族）

| 登记 | 登记状态 | live 复核（2026-08-04） |
|------|---------|------------------------|
| P1-MA4-601（6 类 18 个空壳测试） | MA4.6 登记，MR2 | **登记不完整**：实际 7 个 AggregationProcessor 测试类全部携带相同空壳三联（instanceof + NPE-on-null + canInstantiate），21 个空壳测试；第 7 个类 `TestCrossDbInMemoryAggregationProcessor` 的 3 个未登记（MA6.1-002） |
| P2-MA4-301（3 个 JOIN processor 单元层空壳） | MA4.3 登记，MR2 | **登记不完整**：除 3 个 JOIN processor（EntityEntityJoin:39 / ExternalExternalJoin:40 / MixedSameDbJoin:41）外，`CrossDbInMemoryAggregationProcessor.java:36` 同模式（MA6.1-003） |
| P2-MA4-101（CheckpointExtConfig 死 DTO） | MA4.2 登记，MR2 | 确认仍在（死代码）；同类新增 AggregationRowDTO（MA6.1-001） |

## 文档变化

`No owner-doc update required` — 纯审计零代码变更；审计报告为证据层，修复归 MR2（roadmap 规则 1：P1 归 MR，P2/P3 deferred successor 批次）。

## 测试

`No new test required`: 纯审计计划零代码变更。

## 汇总

- **0 P0 / 1 P1 / 2 P2 / 1 P3**（P1/P2 均为已登记家族的登记外实例，非新家族）
- 自动化扫描 + 阳性对照证明工具真实执行：high=0、info=112（73 return-null + 38 continue + 1 TODO），111 个 medium 全部人工裁定为合法语义（map miss / optional / 显式失败 / 有日志 fallback），无隐藏空壳
- 人工对抗扫描 0 新增独立空壳；空壳风险集中在 601/301 家族登记缺口（本报告已修正计数）
- 无静默降级：全部发现有明确归属（MR2）
- 审计方式偏差：无

## 盲区自评

- 工具无 info 级 pattern，medium 依赖人工裁定——111 个已全部体读，但"未来新增代码引入空壳"不在扫描能力内（工具仅静态 pattern）
- `_gen/` 生成代码排除在扫描外（生成物由模型驱动，MA2.2 已审计生成管线完整性）
