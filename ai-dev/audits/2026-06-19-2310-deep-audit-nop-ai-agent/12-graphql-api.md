# 维度 12：GraphQL 与 API 层

## 适用范围

**N/A。** 在 `nop-ai-agent/` 全模块 glob `**/*.graphql`、`**/*.api.xml`，结果 **0 命中**；无 @BizQuery/@BizMutation。本模块无 GraphQL schema、无 api.xml。GraphQL 与 API 层无审计对象。

（模块对外的"API 层"是 Java 公共接口，已在维度03 审计；跨模块契约在维度20 审计。）

## 最终保留项

| 编号 | 严重程度 | 文件路径 | 一句话摘要 |
|------|---------|---------|-----------|
| — | — | — | N/A（无 GraphQL） |
