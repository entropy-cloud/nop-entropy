# 维度 07：BizModel 规范遵循

## 适用范围

**N/A。** 在 `nop-ai-agent/` 全模块 grep `@BizModel|@BizQuery|@BizMutation|CrudBizModel`（include *.java），结果 **0 命中**。

本模块是 AI Agent 框架库（runtime/engine/session/store/router/tool 等内部组件），不对外暴露 Nop BizModel 服务层，无 GraphQL/DSL API。BizModel 规范无审计对象。

## 最终保留项

| 编号 | 严重程度 | 文件路径 | 一句话摘要 |
|------|---------|---------|-----------|
| — | — | — | N/A（无 BizModel） |
