# 审计报告：MA1.1 — nop-metadata 依赖图与模块边界（维度 01）

> Mission: `nop-metadata-audit-remediation`
> 里程碑: MA1（结构与依赖审计）工作项 1.1
> 日期: 2026-08-04
> 审计范围: `nop-metadata/` 全部 8 子模块 `pom.xml` + 源码 import 抽样 + `mvn dependency:tree` / `mvn dependency:analyze`
> 对照源: `ai-dev/audits/arm-unclosed-findings-nop-metadata.md`（M0.3 权威清单）+ 历史审计文件（07-19/07-20-1554/07-21-2039/07-23-0714）
> 审计方式: live repo 双轨验证（pom 全文 + 源码 import 抽样 + maven 依赖工具输出）

## 摘要

- **总发现数**：0 新增 P0/P1；1 项历史 P1 确认已修复（含回归证据）；4 项历史 P2 确认已修复；3 项历史 P3 确认仍 open（低风险维护项）；1 项历史 P2 重复登记记录为 fixed 证明缺失（见对照说明）
- **依赖图健康度**：无循环依赖；无分层红线违规（规则 1-10 全部通过）；Quarkus 框架依赖仅存在于 app 模块
- **零引用依赖**：codegen 4 个 compile 依赖全部零引用（历史 P3 确认），core 2 个声明依赖字节码级零引用，api 的 `nop-api-core` 判定为 analyze 误报（import 抽样 32 处 `DataBean` 实际引用）

## 完整依赖图（live 2026-08-04，来自 8 份 pom.xml 全文核对）

```
nop-metadata-api      compile: nop-api-core                                    [main 32 DTO]
nop-metadata-core     compile: nop-metadata-api, nop-api-core                  [main 2 常量]
nop-metadata-codegen  compile: nop-ooxml-xlsx, nop-orm, nop-graphql-core,
                               nop-xlang-debugger                              [main 0 / test 1 runner]
nop-metadata-dao      compile: nop-api-core, nop-metadata-api, nop-orm
                      test:   nop-metadata-codegen                            [main 120]
nop-metadata-meta     test:   nop-metadata-codegen, nop-metadata-dao          [resources only]
nop-metadata-service  compile: nop-metadata-api, nop-metadata-core, nop-metadata-dao,
                               nop-wf-core, nop-wf-meta, nop-wf-service, nop-metadata-meta,
                               nop-biz, nop-http-api, nop-biz-file-core, nop-config,
                               nop-ioc, nop-search-api, nop-search-lucene(optional), nop-job-api
                      test:   nop-metadata-codegen, nop-search-core, nop-job-local,
                               nop-autotest-junit, junit-jupiter, junit-jupiter-params,
                               h2, mysql-connector-j, mockito-core              [main 128 / test 94]
nop-metadata-web      compile: nop-metadata-service
                      test:   nop-metadata-codegen                              [main 0 / test 2]
nop-metadata-app      compile: nop-metadata-service, nop-metadata-web          [main 1]
```

> 注：`nop-metadata-web/pom.xml` 与 `nop-metadata-app/pom.xml` 中其余依赖（nop-web、nop-quarkus-web-orm-starter、nop-auth-*、nop-web-amis-editor、quarkus-jdbc-*）为平台/运行时依赖，未在上图重复标注；`-am` reactor 上游依赖模块（nop-biz/nop-auth/nop-wf/nop-job/nop-search/nop-dyn 等）经 M0.4 绿色基线验证无异常。

## 分层规则对照（规则来源：`ai-dev/skills/deep-audit-prompts.md` 维度 01）

| 规则 | 结果 | 证据 |
|------|------|------|
| 1. api 不依赖业务实现层 | ✅ | api 仅依赖 nop-api-core |
| 2. dao 只依赖 api + persistence 框架 | ✅ | live pom：api + nop-orm；`nop-metadata-core` 已移除（见回归验证） |
| 3. core 只依赖 api + 框架核心 | ✅ | core 依赖 api + nop-api-core |
| 4. service 依赖 api + core + dao | ✅ | service pom 显式声明三者 |
| 5. web 依赖 service，不直连 dao | ✅ | web compile 仅 service |
| 6. app 依赖 web + service | ✅ | app pom 仅两者 + 运行时框架 |
| 7. codegen 依赖 model + 代码生成工具 | ⚠️ | 4 个 compile 依赖均无 main 代码引用（见零引用清单） |
| 8. meta 依赖 dao | ✅ | test scope（生成期依赖） |
| 9. 无循环依赖 | ✅ | 依赖图严格 DAG |
| 10. 框架特定依赖仅 app | ✅ | Quarkus 仅 app |

## 历史发现对照（以 M0.3 清单为权威，逐条核对）

### 已修复（含回归证据）

| 轮次限定 ID | 描述 | 修复验证证据（live 2026-08-04） |
|------------|------|-------------------------------|
| `2026-07-19-1118#维度01-02` / `2026-07-20-1554#维度01-01`（07-23 summary P1 `维度01-01` dao→core） | dao 编译依赖 nop-metadata-core，违反规则 2 | ✅ **已修复**。`git log -S "nop-metadata-core" -- nop-metadata/nop-metadata-dao/pom.xml` 定位修复提交 `c3162d4da`（plan-2026-07-23-0838，2026-07-23）；live pom 仅声明 api + orm（compile）+ codegen（test）；dao 源码 `grep -rn "io.nop.metadata.core"` 0 命中；service main 中 10 处 `_NopMetadataCoreConstants` 引用（合法：service 依赖 core） |
| `2026-07-21-2039#维度01-01` | api POM parent 指向 nop-entropy 而非 nop-metadata | ✅ **已修复**。live parent = `nop-metadata` + `<relativePath>../pom.xml</relativePath>`；修复提交 `9b23cf9ce`（plan-2026-07-21-2039）+ `c7ccc3b66` |
| `2026-07-21-2039#维度01-05` / `2026-07-23-0714#维度01-001` | nop-metadata（含 api）未注册 nop-bom | ✅ **已修复**。`nop-bom/pom.xml:1148-1194` 现含全部 8 个 nop-metadata 子模块（api/core/codegen/dao/meta/service/web/app）；修复提交 `9b23cf9ce` 起 |
| `2026-07-23-0714#维度01-02`（07-23 summary P3） | service 缺少对 -api 的依赖 | ✅ **已修复**。service pom 显式声明 `nop-metadata-api`（live 行 16-20） |
| `2026-07-20-1554#维度01-02` | service compile 依赖 nop-sys-dao，零 Java 引用 | ✅ **已修复**。live service pom 无 nop-sys-dao；移除提交 `8a20fde01`（plan-2026-07-21-1521） |
| `2026-07-19-1118#维度01-01` | api 为零内容死模块（0 源文件、0 消费者） | ✅ **已解决**。api 现有 32 个 `@DataBean` DTO（311 plan 承接 307 blocked 项，DTO 迁入 api），消费者：core/dao/service/meta(web 经 service 传递) |

### 仍 open（低优先维护项，M0.3 归集后经本审计复核确认）

| 轮次限定 ID | 描述 | 严重程度 | 归属 |
|------------|------|---------|------|
| `2026-07-20-1554#维度01-03` | codegen 编译依赖 nop-xlang-debugger，零 Java 引用 | P3 | deferred（见零引用清单） |
| `2026-07-21-2039#维度01-03`（= M0.3 P3 清单 `2026-07-21-2039#维度01-03`） | dao 声明 codegen test-scope 依赖但无 `src/test` 测试代码 | P3 | deferred（M0.3 已登记为依赖清理项） |
| `2026-07-20-1554#维度01-04` / `2026-07-21-2039#维度01-04` | core 模块过度轻量（仅 2 常量文件）/ service、web 对 meta 用 compile scope 可改 runtime | P3 | deferred（模块结构调整候选，roadmap Non-Blocking Follow-ups 已记录） |

### 对照说明（历史文件内部矛盾，不仲裁）

- **07-23 summary vs 维度文件矛盾**：`2026-07-23-0714-multi-audit-nop-metadata/summary.md` 统计表记维度 01 有 5 发现且含 **P1 dao→core 违规**（`维度01-01`），而同一轮次 `01-dependency-graph.md` 维度文件声称 dao 依赖 "api + nop-orm，codegen test scope ✅"（仅 1 条完整发现：api 未注册 BOM，P2）。按 plan 约定与 M0.3 清单裁定：**以 `arm-unclosed-findings-nop-metadata.md` 为权威对照源**——该清单已确认 dao→core 为已修复项（修复提交 `c3162d4da` 同时将 32 个 DTO 从 core 迁至 api，解释了 summary 与维度文件的时点差异：summary 成稿早于/未同步修复提交）。本报告记录矛盾存在，不仲裁归属；修复到位性由 live pom + import 抽样双重确认。
- **07-21 维度01-01（api parent）**：live 已修复，记 fixed（见上表）。

## 零引用依赖清单（判定方法：`mvn dependency:analyze` 输出 + 源码 import 抽样双轨并用以避免单轨误报）

| 模块 | 声明依赖 | analyze 判定 | import 抽样核实 | 结论 |
|------|---------|-------------|----------------|------|
| nop-metadata-codegen | nop-ooxml-xlsx, nop-orm, nop-graphql-core, nop-xlang-debugger | 全部 unused | 模块仅 1 个 test runner（`NopMetadataCodeGen.java`），import 仅 nop-codegen/nop-commons/nop-core/nop-api-core（经父 POM 传递）；4 个声明依赖零引用 | **零引用确认**（历史 `2026-07-20-1554#维度01-03` P3 残余；codegen 0 main 的固有形态） |
| nop-metadata-core | nop-metadata-api, nop-api-core | 全部 unused | 2 个常量文件无任何 import（字节码级零引用） | 零引用确认（P3；声明仅为分层定位与未来常量依赖预留，属模块结构问题，与 core 过轻联动） |
| nop-metadata-api | nop-api-core | unused（analyze 报） | 32/32 DTO import `io.nop.api.core.annotations.data.DataBean` | **analyze 误报**（注解引用为 analyze 盲区）；实际使用，非零引用 |
| nop-metadata-dao | nop-metadata-codegen（test） | — | 无 src/test 目录 | 冗余 test-scope 声明（历史 P3 确认） |

## 发现清单（本审计新增）

- **新增 P0/P1：0 项**
- 新增发现均为 P3 级观察（零引用依赖详情见上表，全部并入历史 P3 登记，不重复开新 ID）
- 无新 P2：`nop-metadata-api` 未注册 BOM / dao→core / parent 指向 / service 缺 -api 依赖 4 项历史 P2 全部 live 确认修复

## 总结评估

nop-metadata 8 子模块依赖图**健康**：无循环依赖、无分层违规、无 P0/P1 未闭包项。历史 4 项 P2 依赖问题（dao→core、BOM 注册、api parent、service 缺 api）全部修复且有 git 证据；残余 3 项 P3（codegen 零引用依赖、dao 冗余 test-scope、core 过轻/meta scope）为依赖卫生维护项，按 roadmap 规则 1 记 deferred（非 live defect，无功能影响）。07-23 summary 的 P1 dao→core 矛盾已在对照说明记录（不仲裁，以 M0.3 清单为准）。

## 文档变化

`No owner-doc update required`（审计报告为证据层；`ai-dev/design/nop-metadata/01-architecture-baseline.md` 经核对与 live 依赖图一致，无 drift）
