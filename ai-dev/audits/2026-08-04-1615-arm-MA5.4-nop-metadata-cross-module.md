# MA5.4 审计报告：跨模块契约一致性（nop-sys/nop-auth/nop-wf/nop-code 依赖面 + dict/querySpace）

> **里程碑**: MA5（`nop-metadata-audit-remediation` roadmap，Item 5.4）
> **轮次**: 2026-08-04（live repo 审计）
> **报告**: `ai-dev/audits/2026-08-04-1615-arm-MA5.4-nop-metadata-cross-module.md`
> **状态**: done（待独立 closure audit）
> **审计方式**: 独立子 agent 深挖（`cross-module-dependency-audit-prompt.md` 流程；依赖面逐 pom 核实 + 工作流契约核对 + dict/querySpace 全量扫描）+ 主 agent 全部 finding 源码级 live 复核（xmeta 根属性 getProp 语义——SchemaImpl.java:839-845、approval-support.xbiz:30 对照、wf/approve-status dict 存在性、dict yaml 位置）
> **维度来源**: `ai-dev/skills/cross-module-dependency-audit-prompt.md`
> **计划**: `ai-dev/plans/nop-metadata-audit-remediation/2026-08-04-1543-1-ma5-doc-consistency-audit.md` Phase 4
> **复用基线**: MA1.1 依赖图报告（`2026-08-04-0900-arm-MA1.1-nop-metadata-dependency-graph.md`）+ MA1.2 模块边界报告（`2026-08-04-0900-arm-MA1.2-nop-metadata-module-boundary.md`）

## 跨模块契约面核对表（每依赖面有结论）

| 依赖面 | pom 依赖 | 源码类型引用 | 结论 |
|--------|----------|--------------|------|
| nop-sys | 8 子模块 pom 全零引用 | 0 命中（main+test） | **N/A**（无依赖面；与 MA1.1 结论一致） |
| nop-code | 8 子模块 pom 全零引用 | 0 命中（`io.nop.codegen.XCodeGenerator` 为 nop-kernel/nop-codegen 框架工具，非 nop-code 业务模块） | **N/A**（无依赖面；与 MA1.1 结论一致） |
| nop-auth | 仅 `nop-metadata-app/pom.xml:37-42`（nop-auth-web + nop-auth-service，app 级平台依赖） | 0 个 `io.nop.auth` 类型 import；仅 `MetaManifestBuilder.java:137` Javadoc 字符串 `nop/auth`（moduleId slash→dot 示例，非类型引用） | **N/A 契约面**（app 运行时依赖，源码零引用；与预置一致） |
| **nop-wf** | `nop-metadata-service/pom.xml:33-42`（wf-core/wf-meta/wf-service，**compile**）——唯一 compile 级契约面 | 3 个 `io.nop.wf` import 全部在 `QualityAlertWorkflowService.java:20-22`（IWorkflowManager/IWorkflow/WfReference） | **契约面存在；名称级一致，解析级 broken**（见下） |
| nop-search | search-api compile + search-lucene optional + search-core test | NopMetaSearchService/BizModel/NopMetaIndexBuilder | 一致（MA1.1 结论复用） |
| nop-job | job-api compile + job-local test | MetaQualityCheckpointScheduler cron（BeanMethodJobInvoker） | 一致（MA1.1 结论复用） |
| nop-biz | biz + biz-file-core compile | 39 BizModel 基类/CrudBizModel | 一致（MA1.1 结论复用） |

**结论：预置依赖面现状全部确认（nop-sys/nop-code 零依赖 N/A、nop-auth 仅 app pom 运行时依赖且源码零类型引用、nop-wf 唯一 compile 级契约面），与 MA1.1 依赖图报告一致，无新发现。**

## nop-wf 契约核对（名称级一致 / 解析级 broken）

| 引用来源 | 工作流名 | xwf 文件 | xwf wfName attr | 名称匹配 |
|----------|---------|----------|-----------------|----------|
| `NopMetaDataContract.xmeta:3` `wf:wfName` | `metaDataContractApproval` | `service/_vfs/nop/metadata/wf/metaDataContractApproval/v1.xwf` | 一致 | ✅ 名称级 |
| `NopMetaTagLabel.xmeta:3` `wf:wfName` | `tagLabelConfirmApproval` | `service/_vfs/nop/metadata/wf/tagLabelConfirmApproval/v1.xwf` | 一致 | ✅ 名称级 |
| `QualityAlertWorkflowService.java:76` `newWorkflow("qualityBreachApproval", 1L)` | `qualityBreachApproval` | `service/_vfs/nop/metadata/wf/qualityBreachApproval/v1.xwf` | 一致 | ✅ 名称级 |

解析级 broken（**P1-MA3-001 交叉核对确认，不重复登记**）：3 个 xwf 位于 `/nop/metadata/wf/`，解析器 `resolveInDir="/nop/wf"`（`nop-wf-core/.../registry/wf.register-model.xml:4`）不可达——`ResourceComponentManager.resolveModelLoader`（nop-core:171-204）仅探测 `/nop/wf/{name}`；fallback DB 路径需 NopWfDefinition seed（无）；对照 nop-wf 自身 E2E 工作流 `_vfs/nop/wf/test/approval-form/v1.xwf`（可解析、E2E 通过）——修复方向明确：xwf 移至 `/nop/wf/{name}/v1.xwf`。附带确认 P1-MA3-001 三个子项（xlib import 缺失 → `ERR_XPL_NOT_ALLOW_UNKNOWN_TAG`；qualityBreachApproval/v1.xwf:69 listener `appState` 非法属性——wf.xdef:83-87 仅允许 id/eventPattern/source）与 P1-MA3-002（Java/XPL approve-reject 双实现行为分叉）仍 open，均归 MR2。

**QualityAlertWorkflowService API 面**：`IWorkflowManager.newWorkflow(name, 1L)` + `IWorkflow.start(Map, IServiceContext)` + `WfReference` 返回类型全部 API 合法；**未实现任何 nop-wf callback/listener 接口**——回调为 XPL 脚本（qualityBreachApproval/v1.xwf:53-65 `BeanContainer.getBean(QualityAlertWorkflowService.class.getName()).reJudge(...)`），bean 注册 app-service.beans.xml:33-34，结构一致（解析不可达前无运行时风险）。

## dict 跨模块引用

- orm.xml + 39 xmeta 全部 `ext:dict` 引用 = **31 个本地 `meta/*`**（0 个跨模块 `wf/*`/`auth/*`/`sys/*` 引用）；31/31 在 `nop-metadata-meta/_vfs/dict/meta/*.dict.yaml`（35 个 yaml）或 orm.xml 内嵌 dict 中存在，0 缺失、0 值冲突。
- **修正 MA5.1 复核结论**：`wf/approve-status` dict **存在**于 nop-wf（`nop-wf/nop-wf-meta/_vfs/dict/wf/approve-status.dict.yaml`，选项 UNSUBMITTED/SUBMITTED/APPROVED/REJECTED 与 `meta/approve-status` 完全一致）——nop-metadata 有意维护本地平行副本（避免 meta→wf-meta 依赖），无 broken 引用；MA5.1 P3-MA5-163 措辞已修正（drift 点为设计文档引用名与 live 列引用名不一致）。
- 1 处格式不统一：`meta/change-source.dict.yaml` 用 map 格式（`IMPORT: 导入`），兄弟 yaml 用 `options:` 列表——值一致，纯格式。

## querySpace 路由

- orm.xml `querySpace` 仅作为**业务列**（NopMetaDataSource.querySpace :386/:589、NopMetaTable.querySpace :1280），**零实体级 `querySpace="..."` 路由属性**——不存在跨模块 querySpace 路由面。
- service 代码把 querySpace 当 join 路由业务元数据：`EntityAggregationProcessor.java:175` / `MetaTableReferenceResolver.java:147` fallback `DaoConstants.DEFAULT_QUERY_SPACE`（"default"）——一致。
- external 数据源 connect-on-demand 且有意不注册进 ORM querySpace 路由（`IMetaDataSourceConnectionProcessor.java:19-20` Javadoc + app-service.beans.xml:11 注释）——契约一致，无违规。

## Drift 清单（file:line + 期望修正 + P 级 + 归属）

| ID | 位置 | 发现 | P 级 | 归属 |
|----|------|------|------|------|
| P2-MA5-401 | `service/entity/NopMetaTagLabelBizModel.java:106-115`（getWfNameFromMeta） | **Java 侧 `getObjMeta().getProp("wf:wfName")` 恒返回 null**：`wf:wfName` 是 meta 根属性（NopMetaTagLabel.xmeta:4 根元素 attr），而 `SchemaImpl.getProp`（nop-kernel/nop-xlang/.../xmeta/impl/SchemaImpl.java:839-845）仅查 props map → `triggerApprovalIfNeeded`（:86-99）因 wfName==null 提前 return，Derived/Propagated/Automated 标签的 `submitForApproval` 自动提审路径**静默失效**（XPL 侧 `approval-support.xbiz:30` `objMeta['wf:wfName']` 属性访问可用——修复对照基准）；与已登记 P2-MA4-001（catch-all 静默吞异常，MA4.1）为同一方法不同根因，不合并 | P2 | **MR2**（与 P1-MA3-001/002 wf 集成修复同批：改用属性访问 + 回归测试） |
| P3-MA5-402 | `nop-metadata-meta/_vfs/dict/meta/approve-status.dict.yaml` vs `nop-wf-meta/_vfs/dict/wf/approve-status.dict.yaml` | 平行重复 dict（4 选项完全一致）：nop-metadata 有意本地副本避免 meta→wf-meta 依赖——选型需文档化（11-enterprise-semantic-layer.md:240 引用名随之修正，见 MA5.1 P3-MA5-163） | P3 | MR3（文档裁定 + 选型说明） |
| P3-MA5-403 | `nop-metadata-meta/_vfs/dict/meta/change-source.dict.yaml` | map 格式 vs 兄弟 yaml `options:` 列表——纯格式不统一，值一致 | P3 | MR3 |

**汇总：0 P0 / 0 P1 新增 / 1 P2 新增 / 2 P3 新增**（P1-MA3-001/002 与 P2-MA3-01 为交叉核对确认，不重复计数）。

## 复用 MA1.1/MA1.2 结论清单（不重复审计）

- 依赖表/分层规则 10 条通过/零引用依赖：MA1.1 报告 :16-38/:40-53/:81-88 —— 本阶段 8 pom 复核一致
- 8 子模块职责 + `*Service` 命名残余（P2，归 MR2）：MA1.2 报告 :18-28/:68-75 —— 本阶段 QualityAlertWorkflowService 命名交叉核对（同 P2-MA4-501，见 MA5.3 报告）
- 方法论（机制 B/DAG/跨模块引用检查）：`cross-module-dependency-audit-prompt.md`

## 文档变化

`No owner-doc update required` — 纯审计；P2-MA5-401 代码修复归 MR2，P3-MA5-402/403 文档项归 MR3（含 MA5.1 P3-MA5-163 修正联动）。

## 测试

`No new test required`: 纯审计计划零代码变更（P2-MA5-401 修复时补回归测试，归 MR2）。

## 汇总

- 依赖面现状与预置完全一致（nop-sys/nop-code N/A、nop-auth 仅 app 运行时依赖零源码引用、nop-wf 唯一 compile 契约面）
- 新增 1 P2（P2-MA5-401 TagLabel 自动提审 Java 路径静默失效——getProp 无法读根属性，与 P2-MA4-001 不同根因）+ 2 P3（dict 平行副本选型文档化、dict yaml 格式）
- 已登记 wf 集成缺陷（P1-MA3-001/002、P2-MA3-01）全部交叉核对确认仍 open，归 MR2 不变
- 无静默降级：全部发现有明确归属（MR2/MR3）
- 审计方式偏差：无
