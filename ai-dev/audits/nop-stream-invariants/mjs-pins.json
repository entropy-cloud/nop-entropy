{
  "schemaVersion": 1,
  "updated": "2026-08-13 (Cycle 3 / I1: invariant #7 wiring-existence family added — expected zero pins; internal-creation is a registry disposition, NOT a pin)",
  "note": "Pinned violations for check-nop-stream-invariants.mjs scan-iterations (invariant #2: synchronized collection iteration points must be inside a synchronized block), scan-output-contract (invariant #6 / PD-15 output-contract family: any Output.collect(OutputTag, X) must be forwarded to a registered side-output consumer, never silently dropped; no registered consumer -> fail-fast) AND scan-wiring (invariant #7 / Cycle 3 I1 wiring-existence family: every runtime service injection API consumed by main code must have a production wiring point; only-test injection = red; new main consumer classes / new injection APIs must be registered in wiring-registry.json). Semantics = pin-and-record: violations ⊆ pins → green (CI stays green despite known residuals); NEW violations (not pinned) → red; a pin whose violation no longer exists → stale-pin error (the residual was fixed — remove the pin). Each key MUST match the scanner's violation string exactly. scan-wiring pins: expected zero at I1 (2026-08-13) — the adjudicated internal-creation carve-out (setKeyedStateBackend / setOperatorStateBackend main zero call sites) is a wiring-registry disposition, NOT a pin; any future pin must reference an adjudicated watch-only residual / known exemption with a removalTrigger (no silent removal). PIN REMOVAL TRACE (2026-08-12, Cycle 2 / I4 plan 2026-08-12-1217-11): the 2 invariant-#6 transition pins (RWO-cross-task-noop / BRWO-cross-task-noop) were removed because their removalTrigger was satisfied - the Cycle 2 / I4 interim fail-fast fix landed (RecordWriterOutput / BroadcastingRecordWriterOutput collect(OutputTag) empty-body no-op -> throw ERR_STREAM_SIDE_OUTPUT_NO_CONSUMER) and the registry classification was updated (pinned-known-violation -> fail-fast, output-contract-registry.json). NOT a silent removal: fix + registry migration + pin removal landed in the same commit. HG-01 wire-protocol support remains a human confirmation gate (enhancement, does not affect pin removal). Differences are transferred to I2 red list via ai-dev/audits/nop-stream-invariants/red-list.md; I1 does NOT fix them.",
  "pinnedViolations": []
}
