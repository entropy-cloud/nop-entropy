{
  "schemaVersion": 1,
  "updated": "2026-08-12",
  "note": "Pinned violations for check-nop-stream-invariants.mjs scan-iterations (invariant #2: synchronized collection iteration points must be inside a synchronized block). Semantics = pin-and-record: violations ⊆ pins → green (CI stays green despite known residuals); NEW violations (not pinned) → red; a pin whose violation no longer exists → stale-pin error (the residual was fixed — remove the pin). Each key MUST match the scanner's violation string exactly (relative path:line iteration ...). Differences are transferred to I2 red list via ai-dev/audits/nop-stream-invariants/red-list.md; I1 does NOT fix them.",
  "pinnedViolations": []
}
