Fix compilation errors in module {module}. Run ./mvnw clean install -pl {module} -am -T 1C and fix all errors until the build passes.

Output <AI_STEP_RESULT>fixed</AI_STEP_RESULT> or <AI_STEP_RESULT>failed</AI_STEP_RESULT>.