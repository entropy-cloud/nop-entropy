<task x:schema="/nop/schema/task/task.xdef" xmlns:x="/nop/schema/xdsl.xdef">
    <steps>
        <xpl name="transientFailStep">
            <decorator name="retry" retry:maxRetryCount="2" retry:retryDelay="0"/>
            <source><![CDATA[
                import io.nop.api.core.exceptions.NopException;
                const counter = inject("testExecutionCounter");
                const n = counter.incrementAndGet();
                throw new NopException("nop.err.test.transient-failure-" + n);
            ]]></source>
            <output name="RESULT"/>
        </xpl>
    </steps>

    <output name="RESULT">
        <source>
            return RESULT
        </source>
    </output>
</task>
