<?xml version="1.0" encoding="UTF-8" ?>

<!--
[模型类路由组声明（plan 2026-08-15-0849-2，设计 §3.3，Q8 裁定：新建配置面）]

模型按"级别/类"组织：每个模型类 = 逻辑路由组，包含一组候选（备选模型 + 账号组合）。
请求携带的模型映射到某模型类后，在该类候选集内游走——不是全局一组账号（设计 §3.3）。
候选集可跨 provider（引用 {provider}.llm.xml 账号链）、跨账号（<accounts> 账号链）、跨模型
（同类候选可含多个 model）。

opt-in：配置文件 _vfs/nop/ai/llm/_default.model-class.xml。缺省（无文件）= 无路由组 =
零回归（沿用既有单 provider 行为）。请求 model 未归属任何类（成员清单未命中）同样解析为
"无路由组"（零回归）。未知 accountRef（引用的账号 id 不在 {provider}.llm.xml <accounts> 中）
与未知 provider 在解析期 fail-fast（不静默吞——Minimum Rules #24）。

候选展开语义（解析期，LlmConfigHelper 家族）：
- accountRef 未配置：候选展开为"主账号 + 该 provider 有序账号链"（主账号在前）；
- accountRef 配置：只展开为该账号（未知 id 解析期 fail-fast）；
- model 未配置：使用该 provider 的 defaultModel。

@id          模型类唯一标识（x:extends 合并键 + 诊断用）
@members     归属本类的 model 名清单（csv-set；model 名全局匹配，首个声明命中消解歧义）
-->
<modelClass x:schema="/nop/schema/xdef.xdef" xmlns:x="/nop/schema/xdsl.xdef"
            xmlns:xdef="/nop/schema/xdef.xdef"
            xdef:name="ModelClassConfig" xdef:bean-package="io.nop.ai.core.model"
>

    <modelClasses xdef:body-type="list" xdef:key-attr="id">
        <modelClass id="!string" xdef:name="ModelClassModel" members="csv-set">

            <!--
            [模型类候选集（声明顺序 = 默认策略的声明序）]

            @provider    候选 provider 名称（引用 {provider}.llm.xml，必填）
            @model       可选候选 model；省略 = provider 的 defaultModel
            @accountRef  可选账号引用（{provider}.llm.xml <accounts> 中账号 id）；
                         缺省 = 主账号 + 该 provider 有序账号链展开
            -->
            <candidates xdef:body-type="list">
                <candidate provider="!string" xdef:name="ModelClassCandidateModel"
                           model="string"
                           accountRef="string"
                />
            </candidates>

        </modelClass>
    </modelClasses>

</modelClass>
